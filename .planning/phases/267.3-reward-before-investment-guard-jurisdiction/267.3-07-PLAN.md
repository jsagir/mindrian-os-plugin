---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 07
type: execute
wave: 6
depends_on: ["267.3-06"]
files_modified:
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md
  - commands/mva-report.md
  - commands/new-surface.md
  - commands/operator.md
  - commands/opportunities.md
  - commands/organize.md
  - commands/present.md
  - commands/qualify-opportunity.md
  - commands/query.md
  - commands/reanalyze.md
  - commands/research.md
  - commands/room.md
  - commands/rs-explain.md
  - commands/rs-fetch.md
  - commands/rs-thesis.md
  - commands/scheduled-tasks.md
  - commands/skill.md
  - commands/snapshot.md
  - commands/speakers.md
  - commands/splash.md
  - commands/stance.md
  - commands/status.md
  - commands/update.md
  - commands/visualize.md
  - commands/whitespace.md
  - commands/wiki.md
  - skills/*/SKILL.md
autonomous: false
requirements: [GUARD-07, GUARD-02, GUARD-05]
canon_parts: ["Part 7", "Part 11", "Part 12"]

must_haves:
  truths:
    - "Each of the final 25 commands carries an interactive_first_reward chosen by the ruled rubric, with its first-reward moment cited by line."
    - "commands/status.md does not claim a reward, because the rule doc names a status report as a non-reward in its own words."
    - "A human ruled every row the rubric could not settle, before any frontmatter was written."
    - "The full audit reads zero missing and zero invalid across every commands/*.md in the repo."
  artifacts:
    - path: ".planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md"
      provides: "Batch B section: 25 reasoned rows plus their navigator ruling"
      contains: "Batch B"
  key_links:
    - from: "267.3-CLASSIFICATION.md"
      to: "commands/*.md"
      via: "each row cites the line where that command first delivers something to the user"
      pattern: "commands/[a-z-]+\\.md:"
---

<objective>
Declare the final 25 commands and drive the whole-tree audit to zero.

Purpose: this is the plan that makes `node scripts/check-reward-before-investment.cjs` read
zero missing and zero invalid for the first time since the rule shipped in Phase 118-06, and
therefore the plan that makes plan 267.3-08's fail-closed release gate possible.

Output: 25 reasoned rows, a navigator ruling, 25 frontmatter declarations, regenerated
mirrors, and a green whole-tree audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md
@docs/reward-before-investment-rule.md
@lib/core/mva-rule-linter.cjs
</context>

## Artifacts this plan produces

| Artifact | Kind | What it is |
|---|---|---|
| `267.3-CLASSIFICATION.md` Batch B section | phase doc extension | 25 rows plus a batch-B navigator ruling section |
| 25 `interactive_first_reward` declarations | frontmatter | One per batch-B command, completing the tree |
| A zero-missing whole-tree audit | measured state | The first time `check-reward-before-investment.cjs` reads 113 compliant / 0 missing / 0 invalid |

<tasks>

<task type="auto">
  <name>Task 1: Classify batch B against the ruled rubric</name>

  <read_first>
    - `267.3-CLASSIFICATION.md` Part 1 (the rubric), plan 04's 17 rows, and the Batch A section with its ruling. Apply the rubric as ruled and as already calibrated by batch A; do not re-derive it and do not drift from the batch-A treatment of similar commands.
    - `267.3-DECISIONS.md` D-B: the closed vocabulary.
    - `docs/reward-before-investment-rule.md`: the four qualifying tests and, for this batch specifically, the sentence naming a `/mos:status` output as a non-reward.
    - The 25 batch-B command files, each read from frontmatter down to the first user-visible output: `mva-report`, `new-surface`, `operator`, `opportunities`, `organize`, `present`, `qualify-opportunity`, `query`, `reanalyze`, `research`, `room`, `rs-explain`, `rs-fetch`, `rs-thesis`, `scheduled-tasks`, `skill`, `snapshot`, `speakers`, `splash`, `stance`, `status`, `update`, `visualize`, `whitespace`, `wiki`.
    - `data/first-touch-surfaces.json`: `commands/splash.md` is a declared first-touch surface there, which is direct evidence for how `splash` should be classified.
  </read_first>

  <files>.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md</files>

  <action>
Append a `## Batch B (plan 267.3-07)` section with one row per command, in the same shape as
plan 04's Part 2 and plan 06's Batch A: command name, `hitl_shape`, `body_shape`, the
`path:line` of the first user-visible delivery, one plain sentence describing it, the
proposed value, the runner-up and why it lost, and a HIGH or NEEDS-RULING marker.

Four rows in this batch have direct evidence in the source documents and must cite it rather
than reasoning from scratch:
- `status`: `docs/reward-before-investment-rule.md` states in its own words that a
  `/mos:status` output is not a reward. Quote that line in the row. `status` therefore takes
  a non-reward term, and the row must say which and why the scripting opt-out is or is not
  the right one given whether `commands/status.md` offers a scripting flag.
- `splash`: it is a declared first-touch surface in `data/first-touch-surfaces.json`, so its
  first delivery is the greeting itself; cite the registry entry.
- `whitespace`: `body_shape: methodology` with 8 script-invoking subcommands, the clearest
  test of the rubric's methodology-versus-report boundary. Argue it in a paragraph.
- `opportunities` and `research`: both are candidates for a genuine unsolicited-surface
  reward under the rule doc's own funding example. Argue each in a paragraph.

Apply the scripting tie-break mechanically as in batch A: grep each command proposing
`--none (scripting only)` for a real `--no-interactive`, `--script` or `-q` path and record
the grep result in the row.

Write no frontmatter in this task.

House rules: hyphens only, no em-dashes, no emoji.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const a=require('node:assert/strict');const i=t.indexOf('## Batch B');a.ok(i>0,'Batch B section missing');const sec=t.slice(i);const L=['mva-report','new-surface','operator','opportunities','organize','present','qualify-opportunity','query','reanalyze','research','room','rs-explain','rs-fetch','rs-thesis','scheduled-tasks','skill','snapshot','speakers','splash','stance','status','update','visualize','whitespace','wiki'];for(const c of L)a.ok(sec.includes(c),'missing row: '+c);const cites=(sec.match(/commands\/[a-z-]+\.md:\d+/g)||[]);a.ok(cites.length>=25,'expected 25 path:line citations, found '+cites.length);a.equal(sec.indexOf('—'),-1,'em-dash found');a.ok(/not a reward/i.test(sec),'status row does not cite the rule doc non-reward line');console.log('BATCH_B_ROWS_OK',cites.length)"</automated>
    <automated>node -e "const fs=require('fs');const cp=require('child_process');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const sec=t.slice(t.indexOf('## Batch B'));const L=['mva-report','new-surface','operator','opportunities','organize','present','qualify-opportunity','query','reanalyze','research','room','rs-explain','rs-fetch','rs-thesis','scheduled-tasks','skill','snapshot','speakers','splash','stance','status','update','visualize','whitespace','wiki'];let bad=0;for(const c of L){const row=sec.split('\n').find(l=>l.includes(c));if(!row||!row.includes('--none (scripting only)'))continue;const has=cp.spawnSync('grep',['-qE','--no-interactive|--script|(^|[^a-z])-q([^a-z]|\$)','commands/'+c+'.md']).status===0;if(!has){console.error('SCRIPTING OPT-OUT WITHOUT A SCRIPTING FLAG: '+c);bad++}}if(bad)process.exit(1);console.log('SCRIPTING_TIEBREAK_HELD')"</automated>
    <automated>git diff --name-only -- commands/ skills/ | wc -l | grep -qx 0 && echo NO_FRONTMATTER_WRITTEN_YET</automated>
  </verify>

  <acceptance_criteria>
    - `267.3-CLASSIFICATION.md` contains a `## Batch B (plan 267.3-07)` section with all 25 command names and at least 25 `path:line` citations.
    - The `status` row quotes the rule doc's own non-reward sentence.
    - The `splash` row cites `data/first-touch-surfaces.json`.
    - `whitespace`, `opportunities` and `research` each carry a reasoning paragraph.
    - Every row proposing the scripting opt-out is backed by a real scripting flag in that command's body.
    - Every proposed value is a member of the live `REWARD_TYPES`.
    - Zero em-dash characters; no frontmatter written.
  </acceptance_criteria>

  <done>The final 25 proposed declarations exist, the four evidence-backed rows cite their evidence, and the scripting opt-out is mechanically proven wherever proposed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator ruling on batch B</name>

  <read_first>
    - `267.3-CLASSIFICATION.md` `## Batch B (plan 267.3-07)` section, all 25 rows.
  </read_first>

  <what-built>
25 proposed values completing the tree, produced by the same ruled rubric batch A used, with
four rows backed by direct source evidence (`status` against the rule doc's own non-reward
sentence, `splash` against `data/first-touch-surfaces.json`, and reasoning paragraphs for
`whitespace`, `opportunities` and `research`). Zero frontmatter written; nothing committed.
  </what-built>

  <how-to-verify>
1. Open `267.3-CLASSIFICATION.md` and jump to `## Batch B (plan 267.3-07)`.
2. Read every NEEDS-RULING row first.
3. Read the four argued rows: `status`, `whitespace`, `opportunities`, `research`. These are
   the boundary cases that define where the rubric draws its line, so your ruling here sets
   precedent for any future command.
4. Compare batch B's treatment of similar commands against batch A's. If a command here got
   a different term than its batch-A twin for no stated reason, that is drift and is worth
   flagging.
5. Spot-check three HIGH rows against their command files.

After this ruling and Task 3, the whole-tree audit reaches zero for the first time since the
rule shipped, and plan 267.3-08 turns that into a fail-closed release gate. A wrong
declaration approved here becomes a permanently green gate over a false claim.
  </how-to-verify>

  <action>
Present the batch-B table with NEEDS-RULING rows first and the four argued rows second.
Record the ruling as a `### Batch B navigator ruling` subsection: the date, the final value
for every NEEDS-RULING row, any HIGH row overridden with the reason, and the final approved
list of 25. Task 3 reads values only from that subsection.
  </action>

  <resume-signal>Type "approved" to accept all 25 as proposed, or name the rows to change and their correct values.</resume-signal>

  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const a=require('node:assert/strict');const i=t.indexOf('### Batch B navigator ruling');a.ok(i>0,'batch B ruling section missing');const sec=t.slice(i);const L=['mva-report','new-surface','operator','opportunities','organize','present','qualify-opportunity','query','reanalyze','research','room','rs-explain','rs-fetch','rs-thesis','scheduled-tasks','skill','snapshot','speakers','splash','stance','status','update','visualize','whitespace','wiki'];for(const c of L)a.ok(sec.includes(c),'ruling missing final value for '+c);console.log('BATCH_B_RULING_COVERS_25')"</automated>
    <automated>git diff --name-only -- commands/ skills/ | wc -l | grep -qx 0 && echo NOTHING_WRITTEN_YET</automated>
  </verify>

  <acceptance_criteria>
    - A `### Batch B navigator ruling` subsection exists, dated, naming a final value for all 25 commands.
    - Every NEEDS-RULING row has a ruled value; every override records its reason.
    - Any drift from batch A's treatment of a similar command is either corrected or explained.
    - `git diff --name-only -- commands/ skills/` is still empty.
  </acceptance_criteria>

  <done>A human has ruled on the final 25 values and set the precedent for the boundary cases.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the final 25 declarations, regenerate mirrors, commit, and prove the tree is zero</name>

  <read_first>
    - `267.3-CLASSIFICATION.md` `### Batch B navigator ruling`: the approved value per command. Use only this subsection.
    - A batch-A file already declared (for example `commands/graph.md`) for the exact key placement, quoting and provenance-comment style.
  </read_first>

  <files>commands/mva-report.md, commands/new-surface.md, commands/operator.md, commands/opportunities.md, commands/organize.md, commands/present.md, commands/qualify-opportunity.md, commands/query.md, commands/reanalyze.md, commands/research.md, commands/room.md, commands/rs-explain.md, commands/rs-fetch.md, commands/rs-thesis.md, commands/scheduled-tasks.md, commands/skill.md, commands/snapshot.md, commands/speakers.md, commands/splash.md, commands/stance.md, commands/status.md, commands/update.md, commands/visualize.md, commands/whitespace.md, commands/wiki.md, skills/*/SKILL.md (regenerated mirrors)</files>

  <action>
For each of the 25 files, insert exactly one `interactive_first_reward: <ruled value>`
frontmatter line plus one provenance comment above it, adjacent to `hitl_shape` and
`hitl_why`, matching the quoting convention already in use. Two added lines per file, zero
removed lines, no body edits, no reordering of existing keys.

Then run `node scripts/build-skill-mirrors.cjs` once, followed by
`node scripts/build-skill-mirrors.cjs --check`.

Commit through the full pre-commit hook with `COMMIT_NO_VERIFY` UNSET:
`fix(267.3): declare interactive_first_reward for batch B (25 commands), whole tree now green`

Then run the full audit and record its real output. The expected reading is that every
`commands/*.md` on disk is compliant, missing is 0 and invalid is 0. Derive the compliant
count from `ls -1 commands/*.md | wc -l` rather than writing a literal, since the command
count moves between phases and a hardcoded number would go stale (the same lesson
`scripts/session-start`'s `LIVE_COMMAND_COUNT` comment already records). If the audit is not
zero, do NOT adjust the instrument: report the residue, name the files, and stop.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const {parseFrontmatter,REWARD_TYPES}=require('./lib/core/mva-rule-linter.cjs');const a=require('node:assert/strict');const L=['mva-report','new-surface','operator','opportunities','organize','present','qualify-opportunity','query','reanalyze','research','room','rs-explain','rs-fetch','rs-thesis','scheduled-tasks','skill','snapshot','speakers','splash','stance','status','update','visualize','whitespace','wiki'];for(const c of L){const fm=parseFrontmatter(fs.readFileSync('commands/'+c+'.md','utf8'));a.ok(fm&&fm.interactive_first_reward,'no declaration: '+c);a.ok(REWARD_TYPES.has(String(fm.interactive_first_reward).trim()),c+' invalid value: '+fm.interactive_first_reward)}console.log('BATCH_B_25_DECLARED')"</automated>
    <automated>node -e "const cp=require('child_process');const L=['mva-report','new-surface','operator','opportunities','organize','present','qualify-opportunity','query','reanalyze','research','room','rs-explain','rs-fetch','rs-thesis','scheduled-tasks','skill','snapshot','speakers','splash','stance','status','update','visualize','whitespace','wiki'];let bad=0;for(const c of L){const o=cp.execSync('git diff HEAD~1 -U0 -- commands/'+c+'.md',{encoding:'utf8'});const add=o.split('\n').filter(l=>/^\+/.test(l)&&!/^\+\+\+/.test(l));const del=o.split('\n').filter(l=>/^-/.test(l)&&!/^---/.test(l));if(add.length!==2||del.length!==0){console.error(c+': expected 2 added 0 removed, got '+add.length+'/'+del.length);bad++}}if(bad)process.exit(1);console.log('EVERY_DIFF_IS_TWO_ADDED_LINES')"</automated>
    <automated>node -e "const {scanCommands}=require('./lib/core/mva-rule-linter.cjs');const fs=require('fs');const a=require('node:assert/strict');const r=scanCommands('commands');const onDisk=fs.readdirSync('commands').filter(f=>f.endsWith('.md')).length;a.equal(r.missing.length,0,'missing: '+r.missing.map(m=>m.path).join(', '));a.equal(r.invalid.length,0,'invalid: '+r.invalid.map(m=>m.path).join(', '));a.equal(r.compliant.length,onDisk,'compliant '+r.compliant.length+' != on-disk '+onDisk);console.log('WHOLE_TREE_GREEN',onDisk,'commands, 0 missing, 0 invalid')"</automated>
    <automated>node scripts/check-reward-before-investment.cjs && echo FULL_AUDIT_EXITS_ZERO</automated>
    <automated>node scripts/build-skill-mirrors.cjs --check</automated>
    <automated>node scripts/check-plugin-path-anchoring.cjs --check && echo ANCHORING_STILL_GREEN</automated>
    <automated>bash tests/run-all-267.3.sh</automated>
    <automated>git log -1 --format=%B | grep -qi 'no-verify\|COMMIT_NO_VERIFY' && (echo BYPASS_REFERENCED; exit 1) || echo NO_BYPASS</automated>
  </verify>

  <acceptance_criteria>
    - All 25 batch-B commands parse with an `interactive_first_reward` that is a member of the live `REWARD_TYPES` and equals its ruled value.
    - Every one of the 25 diffs is exactly 2 added lines and 0 removed lines.
    - `scanCommands('commands')` returns `missing: 0`, `invalid: 0`, and a compliant count equal to the number of `.md` files on disk, derived at run time and not hardcoded.
    - `node scripts/check-reward-before-investment.cjs` exits 0 for the first time since the rule shipped.
    - `node scripts/build-skill-mirrors.cjs --check` and `node scripts/check-plugin-path-anchoring.cjs --check` both exit 0.
    - `bash tests/run-all-267.3.sh` exits 0.
    - The commit landed with the full pre-commit hook running and `COMMIT_NO_VERIFY` unset.
  </acceptance_criteria>

  <done>Every shipped command declares its first reward, the whole-tree audit exits 0, and the gate plan 267.3-08 wires has something green to gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| batch B ruling -> permanent precedent | The boundary cases ruled here define how every future command is classified |
| a green audit -> a fail-closed gate | Plan 08 promotes this number to a release blocker; a false green becomes a permanent blind spot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.3-31 | Spoofing | `status` declaring a reward the rule doc says is not one | mitigate | Task 1 requires the row to quote the rule doc's own non-reward sentence; verify asserts the phrase is present in the section |
| T-267.3-32 | Spoofing | drift between batch A and batch B for similar commands | mitigate | Task 2's checkpoint step 4 asks the navigator to compare against batch A and flag unexplained drift |
| T-267.3-33 | Tampering | adjusting the audit to reach zero | mitigate | The action forbids it explicitly: report the residue and stop. The compliant count is derived from disk at run time, never written as a literal |
| T-267.3-34 | Tampering | frontmatter churn beyond the declaration | mitigate | Per-file assertion of exactly 2 added, 0 removed lines |
| T-267.3-35 | Elevation of privilege | hook bypass to land the batch | mitigate | `COMMIT_NO_VERIFY` forbidden; verify asserts no bypass in the commit message |
| T-267.3-SC | Tampering | package installs | accept | Zero npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- `node scripts/check-reward-before-investment.cjs` exits 0 with missing 0 and invalid 0.
- `node scripts/check-reward-before-investment.cjs --surfaces .` exits 0.
- `node scripts/build-skill-mirrors.cjs --check` exits 0.
- `node scripts/check-plugin-path-anchoring.cjs --check` exits 0.
- `bash tests/run-all-267.3.sh`, `bash tests/run-all-118.sh`, `bash tests/run-all-271.sh`, `bash tests/run-all-267.1.sh` all exit 0.
- `git diff --name-only -- commands/ skills/` is empty.
</verification>

<success_criteria>
- Every `commands/*.md` in the repo declares an `interactive_first_reward`, and the whole-tree audit exits 0 for the first time since Phase 118-06.
- The four boundary rows cite their evidence rather than asserting a judgment.
- Every diff is two lines; mirrors agree; the hook passed unmodified and unbypassed.
</success_criteria>

<output>
Create `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-07-SUMMARY.md` when done.
</output>
