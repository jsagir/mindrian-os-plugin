---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 02
type: execute
wave: 2
depends_on: ["267.3-01"]
files_modified:
  - data/first-reward-surfaces.json
  - lib/core/mva-rule-linter.cjs
  - scripts/check-reward-before-investment.cjs
  - lib/core/mva-rule-linter.test.cjs
  - tests/test-267.3-surface-declarations.cjs
  - tests/run-all-267.3.sh
  - docs/reward-before-investment-rule.md
autonomous: true
requirements: [GUARD-01, GUARD-02, GUARD-09]
canon_parts: ["Part 7", "Part 8", "Part 11"]

must_haves:
  truths:
    - "A surface with no frontmatter can declare an interactive_first_reward that the linter reads and validates."
    - "The vocabulary contains an honest term for a conversational methodology command and one for a diagnostic surface, each recorded as a canon amendment with a written reason."
    - "The existing commands/*.md path behaves identically after the change: same buckets, same exit codes, same staged filter."
    - "A registry entry naming a file that does not exist on disk fails the check rather than passing silently."
  artifacts:
    - path: "data/first-reward-surfaces.json"
      provides: "Out-of-frontmatter first-reward declaration registry plus its inline schema"
      contains: "interactive_first_reward"
      min_lines: 30
    - path: "lib/core/mva-rule-linter.cjs"
      provides: "scanDeclaredSurfaces() plus the amended REWARD_TYPES"
      exports: ["scanCommands", "scanFiles", "scanDeclaredSurfaces", "validateFrontmatter", "parseFrontmatter", "REWARD_TYPES"]
    - path: "scripts/check-reward-before-investment.cjs"
      provides: "the --surfaces CLI mode over the registry"
      contains: "--surfaces"
    - path: "tests/run-all-267.3.sh"
      provides: "phase test aggregator that fails loudly on zero discovered files"
      min_lines: 15
  key_links:
    - from: "scripts/check-reward-before-investment.cjs"
      to: "lib/core/mva-rule-linter.cjs"
      via: "require of scanDeclaredSurfaces"
      pattern: "scanDeclaredSurfaces"
    - from: "lib/core/mva-rule-linter.cjs"
      to: "data/first-reward-surfaces.json"
      via: "registry read at check time, path passed by the caller"
      pattern: "first-reward-surfaces"
    - from: "docs/reward-before-investment-rule.md"
      to: "lib/core/mva-rule-linter.cjs"
      via: "the canon-amendment section names every REWARD_TYPES member"
      pattern: "REWARD_TYPES"
---

<objective>
Build the declaration contract the ruling chose, and amend the vocabulary so an honest
backfill becomes possible.

Purpose: `scripts/session-start` is a bash hook with nowhere to put a frontmatter key, and
67 of 113 commands have no honest word to declare. This plan gives the first problem a
registry and the second a vocabulary. It declares nothing about any specific surface; that
is plans 03, 04, 06 and 07.

Output: `data/first-reward-surfaces.json` (schema complete, zero surface rows), a
`scanDeclaredSurfaces()` export, a `--surfaces` CLI mode, the amended `REWARD_TYPES`, the
rule-doc canon-amendment record, and the phase test aggregator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md
@lib/core/mva-rule-linter.cjs
@scripts/check-reward-before-investment.cjs
@docs/reward-before-investment-rule.md
@data/first-touch-surfaces.json
@data/hitl-shape-declaration-schema.json
</context>

## Artifacts this plan produces

| Artifact | Kind | What it is |
|---|---|---|
| `data/first-reward-surfaces.json` | new registry | Registry-is-the-table declaration contract for surfaces with no frontmatter. Carries its own `_doc` schema and a `surfaces: []` array. |
| `REWARD_TYPES` amendment | vocabulary change | The new closed-vocabulary members ruled in D-B, each with a canon-amendment reason in the rule doc. |
| `scanDeclaredSurfaces(registryPath, repoRoot)` | new linter export | Reads the registry, validates every record, classifies into the same three buckets `scanFiles` uses. |
| `--surfaces` CLI mode | new CLI mode | `node scripts/check-reward-before-investment.cjs --surfaces [repoRoot]`, same exit contract as the existing modes. |
| `tests/test-267.3-surface-declarations.cjs` | new test | Fixture-driven proof the registry path classifies correctly and fails closed. |
| `tests/run-all-267.3.sh` | new aggregator | Phase test runner; fails loudly rather than printing green on zero discovered files. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mint the registry schema and amend REWARD_TYPES</name>

  <read_first>
    - `267.3-DECISIONS.md` (this phase dir): D-A the mechanism ruling, and D-B the literal new vocabulary member strings. Copy those strings character for character; do not re-derive or improve them.
    - `data/first-touch-surfaces.json` (whole file): the record shape being mirrored (`version`, `canon_parts`, `purpose`, `surfaces[]` with `id`/`file`/`kind` plus per-registry semantic fields and a `rationale` escape).
    - `data/hitl-shape-declaration-schema.json` (`_doc` block): the `surface_count_principle`, `validation_rule` and `default_on_miss` phrasing this registry copies structurally.
    - `lib/core/mva-rule-linter.cjs` lines 33-56: the `REWARD_TYPES` frozen Set and its per-member comment style.
    - `docs/reward-before-investment-rule.md`: "What counts as first variable reward", "Application to existing flows", "The scripting override", "Detection mechanism (Phase 118-06)".
  </read_first>

  <files>data/first-reward-surfaces.json, lib/core/mva-rule-linter.cjs, docs/reward-before-investment-rule.md</files>

  <behavior>
    - Test 1: `REWARD_TYPES` still contains all six original members (`reframe_question`, `instant_brief`, `schema_preview`, `calibration_distribution_preview`, `paragraph_preview`, `--none (scripting only)`).
    - Test 2: `REWARD_TYPES` contains each new member ruled in D-B, spelled exactly as `267.3-DECISIONS.md` records it.
    - Test 3: `REWARD_TYPES` is still frozen, so no caller can mutate the vocabulary at runtime.
    - Test 4: every member of `REWARD_TYPES` appears verbatim in `docs/reward-before-investment-rule.md` (doc-to-code parity, both directions).
    - Test 5: `data/first-reward-surfaces.json` parses as JSON and has `version`, `_doc`, `surfaces`.
    - Test 6: every value in `_doc.reward_vocabulary` is a member of `REWARD_TYPES`, and the two are the same size, so the registry cannot mint an extra term.
  </behavior>

  <action>
Three edits in this order.

(1) Create `data/first-reward-surfaces.json`. Mirror the record shape of
`data/first-touch-surfaces.json` rather than inventing one (Canon Part 7). Top-level keys:
`version` (integer 1), `canon_parts` (include "Part 7" and "Part 11"), `_doc` (the inline
schema, following the `data/hitl-shape-declaration-schema.json` `_doc` convention), and
`surfaces` (an array, EMPTY here; plan 267.3-03 populates it).

`_doc` must carry: `purpose` (one sentence naming this the declaration contract for
first-touch surfaces with no frontmatter, and naming `scripts/session-start` as the
motivating case); `reward_vocabulary` (array copying every `REWARD_TYPES` member verbatim);
`reward_vocabulary_note` (states this array MIRRORS `lib/core/mva-rule-linter.cjs`'s
`REWARD_TYPES`, that parity is test-enforced, and that a new term is a canon amendment to
the rule doc plus the Set, never a registry-local invention); `kind_vocabulary` (start with
`shell_script`, `injected_prose`, `cjs`; add no kind no surface uses); `record_shape` (the
required per-surface fields: `id` unique non-empty, `file` repo-relative path that must
exist on disk, `kind` from `kind_vocabulary`, `anchor` a grep-able literal or variable name
locating the prose inside the file, `interactive_first_reward` from `reward_vocabulary`,
and `why` one non-empty Feynman-simplified sentence naming the actual first-reward moment);
`validation_rule` (a record is valid when every required field is present, `file` resolves
to an existing file under the repo root, `kind` and `interactive_first_reward` are in their
vocabularies, and `why` is non-empty); `default_on_miss` set to `"reject (fail closed)"`
with the sentence that a missing file, unknown kind, unknown reward value, or empty `why`
is a hard error and not a silent pass; and `surface_count_principle` (the count of
declaring surfaces is never hardcoded, it is always `surfaces.length` read at check time,
phrased the same way `data/hitl-shape-declaration-schema.json` phrases its own).

(2) Amend `REWARD_TYPES` in `lib/core/mva-rule-linter.cjs`. Add exactly the members ruled
in D-B to the frozen Set at lines 49-56, each with a comment in the same style as the
existing five (name, one-line meaning, motivating surface class). Extend the header comment
at lines 33-48 to state the vocabulary was amended by phase 267.3 under navigator ruling
D-B, keeping the existing sentence that future additions are canon amendments and not
command-level inventions. Change nothing else in this task: `parseFrontmatter`,
`validateFrontmatter`, `scanFiles` and `scanCommands` stay byte-identical.

(3) Record the canon amendment in `docs/reward-before-investment-rule.md`. Add a section
`## Vocabulary amendments` after "Detection mechanism (Phase 118-06)". For each new member
record: the exact token, the surface class it describes, the reason the existing six could
not describe it (cite `267.3-AUDIT.md` section 3), the phase and ruling that minted it
(267.3, D-B), and the date. Update every place the doc enumerates the allowed values so
doc and Set stay in parity. Add a one-line pointer to `data/first-reward-surfaces.json` in
the "Detection mechanism" implementation list beside the existing library, CLI and
pre-commit-hook bullets.

House rules: hyphens only, no em-dashes, no emoji. Canon Part 8: zero network, zero Brain
calls.
  </action>

  <verify>
    <automated>node -e "const j=require('./data/first-reward-surfaces.json');const {REWARD_TYPES}=require('./lib/core/mva-rule-linter.cjs');const a=require('node:assert/strict');a.equal(j.version,1);a.ok(Array.isArray(j.surfaces));a.ok(j._doc&&Array.isArray(j._doc.reward_vocabulary));a.ok(Array.isArray(j._doc.kind_vocabulary)&&j._doc.kind_vocabulary.length>0);a.ok(j._doc.record_shape&&j._doc.validation_rule&&j._doc.default_on_miss&&j._doc.surface_count_principle);for(const v of j._doc.reward_vocabulary)a.ok(REWARD_TYPES.has(v),'registry term not in REWARD_TYPES: '+v);a.equal(j._doc.reward_vocabulary.length,REWARD_TYPES.size,'vocabulary size drift');a.ok(Object.isFrozen(REWARD_TYPES));for(const t of ['reframe_question','instant_brief','schema_preview','calibration_distribution_preview','paragraph_preview','--none (scripting only)'])a.ok(REWARD_TYPES.has(t),'original term lost: '+t);a.ok(REWARD_TYPES.size>6,'vocabulary was not amended');console.log('T1_OK size',REWARD_TYPES.size)"</automated>
    <automated>node -e "const fs=require('fs');const {REWARD_TYPES}=require('./lib/core/mva-rule-linter.cjs');const doc=fs.readFileSync('docs/reward-before-investment-rule.md','utf8');const a=require('node:assert/strict');a.ok(/##\s+Vocabulary amendments/.test(doc),'amendment section missing');a.ok(doc.includes('first-reward-surfaces.json'),'registry pointer missing');for(const t of REWARD_TYPES)a.ok(doc.includes(t),'rule doc missing member: '+t);a.equal(doc.indexOf('—'),-1,'em-dash in rule doc');console.log('T1_DOC_PARITY_OK')"</automated>
    <automated>node -e "const fs=require('fs');let bad=0;for(const f of ['data/first-reward-surfaces.json','lib/core/mva-rule-linter.cjs','docs/reward-before-investment-rule.md'])if(fs.readFileSync(f,'utf8').includes('—')){console.error('EMDASH in '+f);bad++}process.exit(bad)"</automated>
  </verify>

  <acceptance_criteria>
    - `data/first-reward-surfaces.json` parses as JSON with `version: 1`, a `_doc` carrying all eight named keys, and `surfaces` of length 0.
    - `REWARD_TYPES.size` is strictly greater than 6, still frozen, still containing all six original members unchanged.
    - `_doc.reward_vocabulary` is the same length as `REWARD_TYPES` and every element is a member.
    - `docs/reward-before-investment-rule.md` contains a `## Vocabulary amendments` section naming, per new term, the surface class, the reason, the ruling id D-B, and the date; and every `REWARD_TYPES` member appears verbatim in the doc.
    - `git diff -- lib/core/mva-rule-linter.cjs` shows changes confined to the header comment block and the `REWARD_TYPES` declaration.
    - Zero U+2014 characters in all three touched files (third verify command exits 0).
  </acceptance_criteria>

  <done>A surface record shape exists on disk with a fail-closed validation rule, the vocabulary has honest terms for the two classes the audit proved unrepresentable, and doc and code state the same vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scanDeclaredSurfaces() and the --surfaces CLI mode</name>

  <read_first>
    - `lib/core/mva-rule-linter.cjs` lines 145-251: `scanFiles`, `scanCommands`, `module.exports`. The new function copies `scanFiles`'s bucket shape exactly.
    - `scripts/check-reward-before-investment.cjs` lines 49-181: `stagedCommandFiles`, `runStaged`, `main`, `report`. The new mode reuses `report` rather than printing its own table.
    - `data/first-reward-surfaces.json` (from Task 1): `_doc.record_shape` and `_doc.validation_rule` are what this function enforces.
    - `267.3-DECISIONS.md` D-A, in case the ruling constrained the function signature.
  </read_first>

  <files>lib/core/mva-rule-linter.cjs, scripts/check-reward-before-investment.cjs</files>

  <behavior>
    - Test 1: one fully valid record lands in `compliant` with its `value`; `ok` is true.
    - Test 2: `interactive_first_reward` outside `REWARD_TYPES` lands in `invalid`, `reason: 'invalid_value'`, value echoed.
    - Test 3: no `interactive_first_reward` key lands in `missing`, `reason: 'missing_field'`.
    - Test 4: a `file` that does not exist on disk lands in `missing`, `reason: 'file_not_found'`, so a declaration cannot outlive its surface.
    - Test 5: empty or absent `why` lands in `missing`, `reason: 'missing_why'`.
    - Test 6: `kind` outside `_doc.kind_vocabulary` lands in `invalid`, `reason: 'invalid_kind'`.
    - Test 7: two records sharing an `id` land in `invalid`, `reason: 'duplicate_id'`.
    - Test 8: an unreadable or unparseable registry returns `ok:false` with one `missing` entry, `reason: 'registry_read_error'`, and does not throw.
    - Test 9: an empty `surfaces` array returns `ok:true` with three empty buckets and does not throw.
    - Test 10: `scanCommands('commands')` results are unchanged by this edit (no regression to the frontmatter path).
  </behavior>

  <action>
(1) Add `scanDeclaredSurfaces(registryPath, repoRoot)` to `lib/core/mva-rule-linter.cjs`.
Return the same object shape `scanFiles` returns, `{ ok, compliant[], missing[], invalid[] }`,
each entry `{ path, id, reason?, value? }`. Reusing the shape is what lets the CLI's existing
`report()` print it without a second renderer (Canon Part 7).

Read the registry with `fs.readFileSync` plus `JSON.parse` inside a try/catch. On any read
or parse failure return `{ ok:false, compliant:[], missing:[{ path: registryPath,
reason:'registry_read_error', error: e.message }], invalid:[] }`. Never throw: a gate that
crashes is worse than a gate that reports.

Resolve each record's `file` against `repoRoot` (default `path.join(__dirname, '..', '..')`,
the climb `lib/core/*.cjs` already uses) and confirm it with `fs.statSync(...).isFile()`.
Validation order, so the reported reason is the most specific: duplicate `id`, then missing
`file` on disk, then `kind` membership against `_doc.kind_vocabulary`, then the
`interactive_first_reward` presence and enum check, then the non-empty `why` check. Delegate
the value check to the EXISTING `validateFrontmatter` by passing it the record object, since
a record carrying an `interactive_first_reward` key is shaped exactly as that function
expects; this is the Part 7 reuse that stops the two paths drifting on the enum.

Add `scanDeclaredSurfaces` to `module.exports`. Leave `parseFrontmatter`,
`validateFrontmatter`, `scanFiles` and `scanCommands` untouched. Extend the file header to
state the second scope in one short paragraph: the linter now validates declarations from
two sources, frontmatter for surfaces that have it and the
`data/first-reward-surfaces.json` registry for surfaces that do not, with one shared value
enum and one shared bucket shape. Keep the existing Canon Part 8 line intact.

(2) Add the `--surfaces` mode to `scripts/check-reward-before-investment.cjs`. Route
`process.argv[2] === '--surfaces'` to a new `runSurfaces()` ahead of the `--staged` branch.
It resolves `repoRoot` from `process.argv[3] || REPO_ROOT_DEFAULT`, resolves the registry at
`path.join(repoRoot, 'data', 'first-reward-surfaces.json')`, prints
`mva-rule-linter: scanning N declared surfaces` using the registry's OWN `surfaces.length`
(never a literal), lists each surface id and file, then calls the EXISTING `report()` with
the result. Do not fork `report()`. Update the header comment block to document three modes
rather than two and name the release gate plan 267.3-03 will wire. Keep the exit-code
contract identical: 0 clean, 1 violation, 2 ungateable.
  </action>

  <verify>
    <automated>node -e "
const a=require('node:assert/strict'),fs=require('fs'),os=require('os'),path=require('path');
const L=require('./lib/core/mva-rule-linter.cjs');
a.equal(typeof L.scanDeclaredSurfaces,'function','scanDeclaredSurfaces not exported');
const d=fs.mkdtempSync(path.join(os.tmpdir(),'frs-'));
fs.writeFileSync(path.join(d,'real.txt'),'x');
const V=[...L.REWARD_TYPES][0];
const w=(o)=>{const p=path.join(d,'reg.json');fs.writeFileSync(p,JSON.stringify(o));return p};
const base={version:1,_doc:{kind_vocabulary:['shell_script','injected_prose','cjs']},surfaces:[]};
const rec={id:'a',file:'real.txt',kind:'shell_script',anchor:'X',interactive_first_reward:V,why:'w'};
let r=L.scanDeclaredSurfaces(w({...base,surfaces:[rec]}),d);
a.equal(r.ok,true);a.equal(r.compliant.length,1);
r=L.scanDeclaredSurfaces(w({...base,surfaces:[{...rec,interactive_first_reward:'bogus'}]}),d);
a.equal(r.invalid[0].reason,'invalid_value');
const noField={...rec};delete noField.interactive_first_reward;
r=L.scanDeclaredSurfaces(w({...base,surfaces:[noField]}),d);
a.equal(r.missing[0].reason,'missing_field');
r=L.scanDeclaredSurfaces(w({...base,surfaces:[{...rec,file:'ghost.txt'}]}),d);
a.equal(r.missing[0].reason,'file_not_found');
r=L.scanDeclaredSurfaces(w({...base,surfaces:[{...rec,why:''}]}),d);
a.equal(r.missing[0].reason,'missing_why');
r=L.scanDeclaredSurfaces(w({...base,surfaces:[{...rec,kind:'nope'}]}),d);
a.equal(r.invalid[0].reason,'invalid_kind');
r=L.scanDeclaredSurfaces(w({...base,surfaces:[rec,{...rec,anchor:'Y'}]}),d);
a.ok(r.invalid.some(x=>x.reason==='duplicate_id'));
r=L.scanDeclaredSurfaces(path.join(d,'nope.json'),d);
a.equal(r.ok,false);a.equal(r.missing[0].reason,'registry_read_error');
r=L.scanDeclaredSurfaces(w(base),d);
a.equal(r.ok,true);a.equal(r.compliant.length+r.missing.length+r.invalid.length,0);
console.log('T2_OK all 9 registry predicates');
"</automated>
    <automated>node -e "const {scanCommands}=require('./lib/core/mva-rule-linter.cjs');const r=scanCommands('commands');const a=require('node:assert/strict');a.equal(r.invalid.length,0,'frontmatter path regressed');a.ok(r.compliant.length+r.missing.length>100,'command tree not scanned');console.log('T2_NO_REGRESSION compliant',r.compliant.length,'missing',r.missing.length)"</automated>
    <automated>node scripts/check-reward-before-investment.cjs --surfaces . && echo SURFACES_MODE_OK</automated>
    <automated>node scripts/check-reward-before-investment.cjs --staged . ; test $? -le 1 && echo STAGED_MODE_STILL_WORKS</automated>
  </verify>

  <acceptance_criteria>
    - `scanDeclaredSurfaces` is exported and is a function.
    - All nine registry predicates pass: compliant, invalid_value, missing_field, file_not_found, missing_why, invalid_kind, duplicate_id, registry_read_error, empty-array-is-ok.
    - `scanDeclaredSurfaces` never throws for any of those inputs, including an absent registry file.
    - `scanCommands('commands')` still returns `invalid: 0` and scans more than 100 files.
    - `node scripts/check-reward-before-investment.cjs --surfaces .` exits 0 against the empty registry and prints a count read from `surfaces.length`, not a literal.
    - `node scripts/check-reward-before-investment.cjs --staged .` exits 0 or 1, never 2, in a clean tree.
    - `git diff -- lib/core/mva-rule-linter.cjs` shows the `parseFrontmatter`, `validateFrontmatter`, `scanFiles` and `scanCommands` bodies unchanged.
  </acceptance_criteria>

  <done>A registry record is validated by the same value enum and reported through the same renderer as a frontmatter declaration, every fail-closed case fails closed, and the pre-existing commands path is provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the new behavior in tests and stand up the phase aggregator</name>

  <read_first>
    - `lib/core/mva-rule-linter.test.cjs` (whole file, 332 lines): the in-tree runner pattern, `run(name, fn)`, `freshLinter()`, `mkTempCommandsDir()`, `writeFixture()`, and the trailing summary line.
    - `tests/test-245-reward-guard-staged.cjs`: the staged-mode test this plan must not regress.
    - `tests/run-all-271.sh`: the aggregator pattern to copy (glob discovery, PASS/FAIL/SKIP counters, loud failure on zero discovered files).
    - `tests/run-all-118.sh` line 44: the existing registration of `../lib/core/mva-rule-linter.test.cjs`, which must keep working.
    - `lib/memory/run-feynman-tests.cjs` line 1524: the CI registration of the same test file, a second consumer of its exit contract.
  </read_first>

  <files>lib/core/mva-rule-linter.test.cjs, tests/test-267.3-surface-declarations.cjs, tests/run-all-267.3.sh</files>

  <action>
(1) Extend `lib/core/mva-rule-linter.test.cjs` with vocabulary-parity tests, appended after
the existing tests and before the summary line, in the file's existing `run(name, fn)` style:
`REWARD_TYPES` still holds all six original members and is still frozen; `REWARD_TYPES.size`
matches the count of tokens the rule doc's allowed-values list names; and
`data/first-reward-surfaces.json`'s `_doc.reward_vocabulary` is set-equal to `REWARD_TYPES`.
Update the file header comment to name the phase that added them.

(2) Create `tests/test-267.3-surface-declarations.cjs` as a standalone CJS test in the same
in-tree runner style (no framework, `node:assert/strict`, pass/fail counters, non-zero exit
on failure). Cover the ten behaviors from Task 2's `<behavior>` block using temp-directory
fixtures, plus two spawn-level tests over the real CLI: `spawnSync('node',
['scripts/check-reward-before-investment.cjs','--surfaces',tmpRepo])` exits 1 and names the
offending surface id when the fixture registry carries a bad value, and exits 0 on a clean
fixture registry. Build the temp repo as a directory containing
`data/first-reward-surfaces.json` plus the referenced fixture files, so the CLI's `repoRoot`
argument is genuinely exercised rather than defaulted.

(3) Create `tests/run-all-267.3.sh` copying the structure of `tests/run-all-271.sh`:
discover `tests/test-267.3-*.cjs` by glob, run each with `node`, count PASS/FAIL/SKIP, print
a final `PASS=n FAIL=n SKIP=n` line, exit non-zero on any FAIL, and exit non-zero with a
loud message if the glob discovers zero files. Also invoke
`node lib/core/mva-rule-linter.test.cjs` and `node tests/test-245-reward-guard-staged.cjs`
from inside the aggregator so a regression to either shows up in this phase's own runner.
`chmod +x` it.

(4) Regression sweep. Run `bash tests/run-all-118.sh` and record its real PASS/FAIL numbers
in the summary. That suite owns the linter; a change to the linter that reds it is a
stop-work condition, not a number to explain away.

House rules: pure CJS, node built-ins only, hyphens not em-dashes, no emoji.
  </action>

  <verify>
    <automated>node lib/core/mva-rule-linter.test.cjs</automated>
    <automated>node tests/test-267.3-surface-declarations.cjs</automated>
    <automated>bash tests/run-all-267.3.sh</automated>
    <automated>bash tests/run-all-118.sh</automated>
    <automated>node tests/test-245-reward-guard-staged.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node lib/core/mva-rule-linter.test.cjs` exits 0 and its summary line reports zero failures.
    - `node tests/test-267.3-surface-declarations.cjs` exits 0 and reports at least 12 passing checks (10 unit behaviors plus 2 spawn-level CLI checks).
    - `bash tests/run-all-267.3.sh` exits 0 and prints a `PASS=` line with a non-zero PASS count.
    - Running `bash tests/run-all-267.3.sh` in a tree where the `tests/test-267.3-*.cjs` glob matches nothing exits non-zero (prove it once with a temporary rename, then restore).
    - `bash tests/run-all-118.sh` exits 0.
    - `node tests/test-245-reward-guard-staged.cjs` exits 0.
    - `tests/run-all-267.3.sh` has the executable bit set (`test -x`).
  </acceptance_criteria>

  <done>The registry path, the vocabulary parity, and the unchanged frontmatter path are all pinned by tests that run from one phase command, and the suite that owns the linter is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry file -> linter | Hand-authored JSON crosses into a gate's verdict; a malformed or stale record must fail closed, never pass |
| registry `file` field -> filesystem | A repo-relative path read from data and resolved against the repo root |
| vocabulary -> every downstream declaration | Plans 03, 04, 06 and 07 classify against these literal strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.3-05 | Tampering | `data/first-reward-surfaces.json` | mitigate | `default_on_miss: reject`; `scanDeclaredSurfaces` fails closed on missing file, unknown kind, unknown value, empty `why`, duplicate id, and unparseable registry; each case is a named test |
| T-267.3-06 | Spoofing | a declaration outliving its surface | mitigate | `file_not_found` predicate: every record's `file` is `statSync`-confirmed at check time, so a deleted or renamed surface reds the gate instead of silently passing |
| T-267.3-07 | Elevation of privilege | registry minting a new reward term | mitigate | `_doc.reward_vocabulary` is set-equality tested against `REWARD_TYPES`; a registry-local term fails the test |
| T-267.3-08 | Denial of service | gate crash on malformed JSON | mitigate | `JSON.parse` wrapped in try/catch returning `registry_read_error`; Test 8 proves the function does not throw |
| T-267.3-09 | Tampering | silent regression of the commands path | mitigate | `scanCommands` regression assertion in Task 2 verify plus `bash tests/run-all-118.sh` and `tests/test-245-reward-guard-staged.cjs` in Task 3 |
| T-267.3-10 | Repudiation | undocumented vocabulary change | mitigate | `## Vocabulary amendments` section records token, class, reason, ruling id and date; doc-to-code parity is test-enforced both directions |
| T-267.3-SC | Tampering | package installs | accept | Zero npm/pip/cargo installs in this plan; node built-ins only |
</threat_model>

<verification>
- `node scripts/check-reward-before-investment.cjs` (full audit) still exits 1 with the same missing count as before this plan; this plan adds a mechanism, it declares nothing.
- `node scripts/check-reward-before-investment.cjs --surfaces .` exits 0.
- `bash tests/run-all-267.3.sh`, `bash tests/run-all-118.sh` and `node tests/test-245-reward-guard-staged.cjs` all exit 0.
- `git status --short -- commands/ skills/` still shows exactly the 16 held command files and 16 held mirrors, unchanged by this plan.
- No em-dash characters in any touched file.
</verification>

<success_criteria>
- A surface with no frontmatter has a machine-readable, fail-closed way to declare its first-reward contract, and the linter reads it through the same enum and the same renderer as frontmatter.
- The vocabulary has an honest term for both classes `267.3-AUDIT.md` section 3 proved unrepresentable, each recorded as a canon amendment.
- The pre-existing commands path is provably unchanged: same buckets, same exit codes, both owning test suites green.
- `tests/run-all-267.3.sh` exists, is executable, and fails loudly on zero discovered files.
</success_criteria>

<output>
Create `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-02-SUMMARY.md` when done.
</output>
