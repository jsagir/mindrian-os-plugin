---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 01
subsystem: governance / lint-jurisdiction
tags: [reward-before-investment, declaration-contract, canon-amendment, lint-scope, phase-271-unblock]
requires: []
provides:
  - "267.3-AUDIT.md: the measured jurisdiction map (reachable / unreachable / vocabulary-fit)"
  - "267.3-DECISIONS.md: the D-A / D-B / D-C ruling plus GUARD-01 through GUARD-10"
  - "The two literal REWARD_TYPES strings plans 02/04/06/07 code against"
affects:
  - ".planning/phases/267.3-*/267.3-02-PLAN.md through 267.3-08-PLAN.md"
tech-stack:
  added: []
  patterns: ["registry-is-the-table declaration contract (data/first-touch-surfaces.json precedent)"]
key-files:
  created:
    - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md
    - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md
  modified: []
decisions:
  - "D-A ruled: a new sibling registry data/first-reward-surfaces.json, modeled on data/first-touch-surfaces.json, NOT an extension of it (its consumers key off version-stamping semantics)"
  - "D-B ruled: exactly two new REWARD_TYPES members, methodology_reframe and --none (diagnostic surface), each recorded as a canon amendment with a written reason"
  - "D-C ruled: keep --staged as the commit gate unchanged, add the whole-tree audit to scripts/verify-release fail-closed, promoted only at phase close after the backfill lands"
  - "options b, c and d rejected with written reasons, preserved not deleted"
metrics:
  duration: ~35 min
  completed: 2026-08-27
---

# Phase 267.3 Plan 01: The Measured Jurisdiction Map and the Jurisdiction Ruling Summary

The reward-before-investment guard's real reach was measured against its assumed reach, and the
navigator ruled the three linked design questions (declaration mechanism, vocabulary amendment,
lint scope) that six downstream plans build against.

## What was built

Two phase documents, no source code. Zero files under `lib/`, `scripts/`, `commands/`,
`skills/` or `data/` were touched, exactly as the plan's success criteria require.

### `267.3-AUDIT.md` (517 lines, 5 sections)

The measured jurisdiction map. Every factual claim carries a `path:line` citation and every
number was produced by running the command shown next to it.

- **Section 1** - the live full-audit run pasted head and tail, the three live numbers with
  their denominator, and four independent proofs that the guard's jurisdiction is exactly
  `commands/*.md` at commit time only.
- **Section 2** - the unreachable surface inventory: four `scripts/session-start` injected-prose
  surfaces, all nine `SessionStart` entries enumerated from `hooks/hooks.json` with a
  prose-emission verdict each, and the `data/first-touch-surfaces.json` precedent.
- **Section 3** - the vocabulary-fit finding: five of six `REWARD_TYPES` members were minted
  against exactly one flow each, ten conversational methodology commands have no honest term,
  and nine diagnostic / report commands have none either while `--none (scripting only)`
  actively mislabels them.
- **Section 4** - the blocked work, measured: the held 16 commands plus 16 mirrors, anchoring
  gate 10c RED with its single `commands/doctor.md:262` violation, and `DEFERRED-271-D1`'s two
  proofs that the missing declarations are pre-existing.
- **Section 5** - D-A, D-B and D-C stated as unanswered questions with candidate options and
  the evidence bearing on each.

### `267.3-DECISIONS.md` (417 lines)

The navigator ruling, `option-a` with no amendments, dated 2026-08-27, with the reasoning
recorded, all three rejected options preserved with their reasons, the two new vocabulary
strings written out literally, and the ten `GUARD-NN` requirement IDs minted.

## The live numbers

| Reading | Value | Command |
|---|---|---|
| compliant | 46 | `node scripts/check-reward-before-investment.cjs` |
| missing | 67 | same run |
| invalid | 0 | same run |
| denominator | 113 | `ls -1 commands/*.md \| wc -l` |
| linter exit | 1 | measured without a pipe |
| anchoring gate 10c | 1 violation, exit 1 (RED) | `node scripts/check-plugin-path-anchoring.cjs --check` |
| held command files | 16 | `git status --short -- commands/` |
| held skill mirrors | 16 | `git status --short -- skills/` |

The 46 / 67 / 0 over 113 reading MATCHES the plan-time expectation exactly. Delta: none.

Every one of the 67 carries `reason=missing_field`, not `no_frontmatter`, so this is a
declaration gap and not a malformed-file gap. That is a finding the plan did not anticipate and
it makes the backfill mechanically simpler than feared: 67 files already have a frontmatter
block to add a key to.

## The ruling in one table

| Question | Ruled | Where it lands |
|---|---|---|
| D-A, declaration mechanism | new sibling registry `data/first-reward-surfaces.json` | 267.3-02, 267.3-03 |
| D-B, vocabulary amendment | exactly two new members: `methodology_reframe`, `--none (diagnostic surface)` | 267.3-02, consumed by 04 / 06 / 07 |
| D-C, lint scope | `--staged` commit gate unchanged, whole-tree audit added to `scripts/verify-release` fail-closed, promoted only when green | 267.3-03, 267.3-08 |

The full amended vocabulary is eight members: the original six unchanged, plus the two above.

## Deviations from Plan

### Auto-fixed Issues

None. No bugs, no missing critical functionality, no blocking issues. This plan wrote two
documents and touched no code.

### Measured deltas recorded rather than silently corrected

Three places where the live reading differed from the plan's text. In every case the LIVE
reading was recorded and the delta named, never the other way around.

**1. `scripts/session-start` line numbers.** The plan named the `MODE_ROUTING` `context=`
assignment at `:706` and `MODE_MENU` at `:704`. The live file has them at `:707` and `:705`
respectively; `:704` is the preceding comment and `:706` is blank. The plan's other six
citations (`:647`, `:648`, `:657`, `:666`, `:680`, `:714`) are exact. Recorded in
`267.3-AUDIT.md` Section 2.1 under "Live line-number deltas versus the plan text."

**2. The conversational-methodology class is ten commands, not seven.** The plan named
`analyze-systems`, `challenge-assumptions`, `find-connections`, `lean-canvas`, `mullins`,
`score-innovation`, `systems-thinking`. The regenerated signal table adds `auto-explore`,
`compare-ventures` and `leadership`, all three reading `body_shape: methodology` plus a
conversational body. Two further `methodology` commands, `causal` and `whitespace`, read `CODE`
bodies and are named for completeness without being pre-assigned. The plan's acceptance
criterion asked for "at least 7"; the live count is 10. Recorded in `267.3-AUDIT.md` Section
3.2.

**3. The diagnostic / operator class is nine commands.** The plan named eight (`doctor`, `heal`,
`dogfood-flush`, `hmi-status`, `dial-memory-refresh`, `feynman-timeline-refresh`, `diagnostics`,
`update`) plus `status` discussed separately, which is nine in total. Recorded as nine in
`267.3-AUDIT.md` Section 3.3.

### Concurrent-session commit in the shared working tree

Between this plan's two commits, an unrelated concurrent session committed `f89ee0c7`
("docs(273): cross-link dpapathanasiou/simple-graph as verified prior art") touching
`.planning/ROADMAP.md` only. This is the known two-sessions-one-tree situation documented in
`CLAUDE.md`. It does not touch any file this plan owns and this plan's commit `de93364a` sits
intact underneath it. Noted so a later reader is not confused by the interleaved history.

## Authentication Gates

None.

## Verification

All four of the plan's `<verification>` items pass.

| Check | Result |
|---|---|
| `267.3-AUDIT.md` and `267.3-DECISIONS.md` both exist | PASS |
| Both free of em-dashes (U+2014) | PASS, asserted inside both automated verifies |
| `node scripts/check-reward-before-investment.cjs` still exits 1 | PASS, `linter_exit=1` |
| The 16 held commands plus 16 held mirrors still untouched | PASS, 16 and 16 |
| Task 1 automated verify | prints `AUDIT_OK` |
| Task 1 signal-table regeneration | ran, output reproduced in `267.3-AUDIT.md` Sections 3.2 and 3.3 |
| Task 2 automated verify | prints `DECISIONS_OK` |
| Zero source files modified under `lib/` `scripts/` `commands/` `skills/` `data/` | PASS, `git diff cf7eabf3 HEAD` lists only `.planning/` paths |

## Known Stubs

None.

## Threat Flags

None. This plan introduced zero network calls, zero Brain calls, zero package installs, and no
new security-relevant surface. Local repo file reads and two document writes only, consistent
with the plan's `T-267.3-03` accept disposition (Canon Part 8).

## Commits

| Task | Commit | What |
|---|---|---|
| Task 1 | `de93364a` | `docs(267.3-01): the measured jurisdiction map (267.3-AUDIT.md)` |
| Task 2 | `db0856b0` | `docs(267.3-01): the jurisdiction ruling (267.3-DECISIONS.md)` |

## What plan 267.3-02 now has that it did not have before

- A ruled mechanism to build: `data/first-reward-surfaces.json` with a five-field record shape
  copied from an existing registry, and `scanDeclaredSurfaces()` sharing the existing
  `validateFrontmatter` classifier so the two declaration surfaces cannot drift.
- Two literal strings to code against, character for character: `methodology_reframe` and
  `--none (diagnostic surface)`.
- The instruction that the amendment lands in code AND in
  `docs/reward-before-investment-rule.md` together, because
  `lib/core/mva-rule-linter.cjs:15-17` says a code-only change would be a command-level
  invention.
- Ten requirement IDs with a mapping of which plan satisfies each.

## Self-Check: PASSED

All three created files verified present on disk. Both commit hashes verified present in
`git log --all`. Summary contains zero em-dash characters.

| Claim | Verified |
|---|---|
| `267.3-AUDIT.md` exists | FOUND |
| `267.3-DECISIONS.md` exists | FOUND |
| `267.3-01-SUMMARY.md` exists | FOUND |
| commit `de93364a` exists | FOUND |
| commit `db0856b0` exists | FOUND |
