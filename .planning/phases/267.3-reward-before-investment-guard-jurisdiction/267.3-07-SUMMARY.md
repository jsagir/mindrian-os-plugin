---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 07
subsystem: reward-before-investment-guard
tags: [mva-rule-linter, frontmatter-declaration, checkpoint-ruling, guard-07, guard-02, guard-05]

requires:
  - phase: 267.3-04
    provides: "the ruled rubric (Part 1 of 267.3-CLASSIFICATION.md) plus the 17 worked-example rows"
  - phase: 267.3-06
    provides: "the batch-A precedent this plan reused directly (the router sub-case for show/help/mos, the export/dashboard persisted-vs-ephemeral distinction, the funding undocumented-default precedent) plus the 25/88 missing/compliant baseline this plan moved to 0/113"
provides:
  - "commit 42192e25: 25 batch-B classification rows appended to 267.3-CLASSIFICATION.md, each cited to a path:line first-delivery"
  - "commit eb748e16: the navigator ruling on the 3 NEEDS-RULING rows (opportunities, research, whitespace), all methodology_reframe, no new vocabulary term minted, no HIGH row overridden"
  - "commit b416e1a5: 25 commands/*.md declarations + 25 regenerated skills/*/SKILL.md mirrors, landed through the full unmodified pre-commit hook"
  - "full audit missing count 25 -> 0, compliant 88 -> 113, invalid held at 0 -- the first time since Phase 118-06 the whole-tree audit exits zero"
affects:
  - "plan 267.3-08 (promotes the full-audit gate to fail-closed in scripts/verify-release now that the audit reads zero missing/zero invalid)"
  - "any release cut (verify-release, doctor --acceptance both read the audit)"

tech-stack-added: []
tech-stack-patterns:
  - "The router sub-case (a command that dispatches to another EXISTING command with zero net-new behavior of its own, per its own stated doctrine) extends cleanly to deprecated soft-alias stubs (organize, query, visualize), not just live dispatchers (show, help, mos)."
  - "A first-touch surface with a hardcoded, verbatim, never-varying tagline fails qualifying test 1 (unpredictable) by construction; the diagnostic-opt-out's broadened 'no first reward of its own' reading extends a third way to cover static branding copy, alongside the pre-existing status-report and router sub-cases."
  - "A completed multi-stage pipeline that assembles and renders its whole deliverable within one invocation (rs-fetch) takes methodology_reframe under TB-5, distinct from a pipeline that prints a structure and then defers execution to a later run (pipeline, intel-pipeline, which take schema_preview)."
  - "When a rubric flag names two commands as 'candidates for a genuine unsolicited reward under the rule doc's own funding example' (opportunities, research), the correct response is to argue the tension honestly and mark NEEDS-RULING rather than force either a diagnostic-opt-out or a reward term -- the navigator's ruling settles it under the existing methodology_reframe term without minting a tenth vocabulary member."

key-files-created:
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-07-SUMMARY.md
key-files-modified:
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md
  - "25 commands/*.md (mva-report, new-surface, operator, opportunities, organize, present, qualify-opportunity, query, reanalyze, research, room, rs-explain, rs-fetch, rs-thesis, scheduled-tasks, skill, snapshot, speakers, splash, stance, status, update, visualize, whitespace, wiki)"
  - "25 skills/*/SKILL.md (regenerated mirrors of the above, via build-skill-mirrors.cjs)"

key-decisions:
  - "opportunities ruled methodology_reframe (navigator): the scan subcommand's live Grants.gov/Simpler Grants search matched against the room's own domain/geography/stage clears the bar under the existing term; no new vocabulary member minted despite the genuine tension with the diagnostic-surface reading."
  - "research ruled methodology_reframe (navigator, accepted as proposed): contextualization against the room's own claim graph (the percent match score per finding) clears qualifying test 4 even though the underlying web content is not itself MindrianOS-exclusive, following the compare-ventures precedent."
  - "whitespace ruled methodology_reframe (navigator), declared against the map subcommand path: the mullins precedent (naming the weakest domains is an analysis) governs over the diagnostics precedent (a self-referential health scoreboard), because map pairs each gap with a specific recommended framework."
  - "status ruled --none (diagnostic surface), quoting docs/reward-before-investment-rule.md:59 verbatim (\"A /mos:status output is not a reward\"), the one row with direct textual evidence rather than an inference."
  - "splash ruled --none (diagnostic surface) as a new documented sub-case (static, hardcoded first-touch copy that never varies, distinct from the router sub-case), flagged explicitly for navigator attention and left standing with no override."
  - "present and snapshot both ruled live_deliverable, following the export precedent (persisted, shareable HTML files) rather than the dashboard precedent (ephemeral live server) -- distinguished from wiki's bare-invocation path, which stays --none (diagnostic surface) because the persisted --export flag is not the default path being declared."
  - "rs-fetch ruled methodology_reframe under TB-5 because its transcript is the whole deliverable of one invocation (nothing deferred), distinct from pipeline/intel-pipeline's print-then-execute schema_preview shape."

requirements-completed: [GUARD-07, GUARD-02, GUARD-05]

duration: "~23min across 3 task commits (02:09-02:32 local); one navigator-ruling checkpoint pause between Task 1 and Task 2 not counted in that span"
completed: 2026-08-28
---

# Phase 267.3 Plan 07: Batch B Reward Declarations Summary

The final 25 `commands/*.md` files (batch B of the 267.3-06/07 split) now carry a ruled
`interactive_first_reward` declaration: 12 `--none (diagnostic surface)`, 9
`methodology_reframe`, 2 `live_deliverable`, 2 `schema_preview`, landed through the unmodified
pre-commit hook after a navigator checkpoint ruled the 3 rows the rubric could not settle on its
own. `node scripts/check-reward-before-investment.cjs` now exits 0 with 113/113 compliant, 0
missing, 0 invalid -- the first time the whole-tree audit has been green since the rule shipped
in Phase 118-06.

## Performance

- **Duration:** ~23 min across 3 task commits (checkpoint pause not counted)
- **Started:** 2026-08-28T02:09:23+03:00 (worktree base)
- **Completed:** 2026-08-28T02:32:30+03:00
- **Tasks:** 3/3 completed
- **Files modified:** 51 (1 classification doc across 2 commits + 25 commands + 25 mirrors)

## Accomplishments

- Classified all 25 batch-B commands against the plan-04 rubric, each row citing the
  `path:line` where the command first hands the navigator something it produced
- Paused at the plan's own `checkpoint:human-verify gate="blocking"` for the 3 rows the rubric
  genuinely could not settle (`opportunities`, `research`, `whitespace`), surfacing full
  per-row reasoning and both readings for each rather than forcing a fit
- Recorded the navigator's ruling (all 3 settled as `methodology_reframe` under the existing
  vocabulary, no tenth term minted, no HIGH row overridden) before writing a single byte of
  frontmatter
- Applied all 25 declarations as exactly-2-line diffs, regenerated the 25 skill mirrors, and
  landed the commit through the full, unmodified pre-commit hook (`COMMIT_NO_VERIFY` unset)
- Moved the full audit from 25 missing / 88 compliant to 0 missing / 113 compliant, invalid
  held at 0 throughout -- `node scripts/check-reward-before-investment.cjs` exits 0 for the
  first time since Phase 118-06, which is what makes plan 267.3-08's fail-closed release gate
  possible

## Task Commits

Each task was committed atomically:

1. **Task 1: Classify batch B against the ruled rubric** - `42192e25` (docs)
2. **Task 2: Navigator ruling on batch B** (checkpoint, human-verify) - `eb748e16` (docs)
3. **Task 3: Apply the final 25 declarations, regenerate mirrors, commit, prove the tree is
   zero** - `b416e1a5` (fix)

No separate plan-metadata commit: this executor ran in worktree isolation mode, where
`STATE.md`/`ROADMAP.md` updates are the orchestrator's responsibility after this wave merges.

## Files Created/Modified

- `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md` -
  Batch B section (25 rows) plus the navigator ruling subsection
- `commands/{25 files}.md` - one `interactive_first_reward` line plus one provenance comment
  each, adjacent to `hitl_shape`/`hitl_why`/`hitl_stages`, matching the existing quoting
  convention (plain scalars unquoted, `--none (...)` values double-quoted)
- `skills/{25 files}/SKILL.md` - regenerated mirrors via `node scripts/build-skill-mirrors.cjs`

## Decisions Made

See `key-decisions` in the frontmatter above for the seven load-bearing calls; full reasoning
for every row (not just the flagged ones) lives in `267.3-CLASSIFICATION.md`'s `## Batch B` and
`### Batch B navigator ruling` sections.

Two additional patterns worth naming here because they extend precedent rather than restate it:

- Three deprecated soft-alias stubs in this batch (`organize`, `query`, `visualize`) all reuse
  the router sub-case first established for live dispatchers (`show`, `help`, `mos` in prior
  plans) -- a command that passes another command's output through verbatim inherits that
  target's reward, whether the redirect is a live routing decision or a deprecation shim.
- `rs-thesis` (a lookup of an already-computed thesis by `discovery_id`) reuses the
  `explain-decision` precedent (batch A) exactly: a rendered log of a past computation is not a
  fresh reward, even when the content being retrieved is itself valuable.

## Deviations from Plan

None - plan executed exactly as written. The plan's own Task 1 flagged four rows for
argued-in-full treatment (`status`, `splash`, `whitespace`, `opportunities`/`research`); all
four were argued as instructed, and the two genuinely unsettled among them (`opportunities`,
`research`, plus `whitespace`) were correctly marked NEEDS-RULING rather than forced to a
confident value, per the objective's explicit instruction not to guess past a genuine judgment
call.

## Issues Encountered

None during execution. The pre-commit hook's first invocation on Task 3 correctly rejected the
commit on stale skill mirrors (`build-skill-mirrors --check` found 25 DIVERGES), which is the
hook working as designed, not a bug: `node scripts/build-skill-mirrors.cjs` was run to
regenerate them, `--check` confirmed clean, and the same commit landed on the second attempt
through the full unmodified hook.

## Pre-existing Test Debt (confirmed unaffected, not fixed here)

Two items already logged in
`.planning/phases/267.3-reward-before-investment-guard-jurisdiction/deferred-items.md` were
re-observed during this plan's post-commit verification sweep and confirmed still present,
unrelated to this plan's changes (this plan touched only `commands/*.md` frontmatter and
`skills/*/SKILL.md` mirrors):

- **DEFERRED-267.3-D1** - `bash tests/run-all-118.sh` still fails 3 of 16 suites
  (`lib/agents/mva/test-all-six-agents.cjs`, `lib/core/mva-deck-builder.test.cjs`,
  `lib/core/mva-option-router.test.cjs`), none of which reference `mva-rule-linter`,
  `first-reward-surfaces`, or `check-reward-before-investment` (the linter's own suite,
  `mva-rule-linter.test.cjs`, is green: 15/15). Pre-dates this plan.
- **DEFERRED-267.3-D3** - `bash tests/run-all-267.1.sh` still fails 1 of 2 discovered suites
  (`test-267-1-first-install-hooked-audit.cjs`, a GAP I-1 pinning assertion that now finds 4
  `~/.mindrian-user.md` references instead of 1, all 3 extra hits in `lib/mcp/tools/identity.cjs`,
  a Phase 270 file this plan never touched). Pre-dates this plan.

Both were already root-caused and assigned to their correct owners (a `/gsd-quick --validate`
pass, or Phase 267.2/270 respectively) in the deferred-items log by an earlier plan in this
phase; no new entry was needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

The whole-tree audit is green (113/113 compliant, 0 missing, 0 invalid) for the first time
since Phase 118-06, which is the precondition plan 267.3-08 needs to promote
`node scripts/check-reward-before-investment.cjs`'s full-audit mode to a fail-closed gate inside
`scripts/verify-release`. No blockers. The two pre-existing, unrelated test-suite failures noted
above are already tracked and do not block 267.3-08.

---
*Phase: 267.3-reward-before-investment-guard-jurisdiction*
*Completed: 2026-08-28*

## Self-Check: PASSED

- FOUND: `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-07-SUMMARY.md`
- FOUND: commit `42192e25` (Task 1: classify batch B)
- FOUND: commit `eb748e16` (Task 2: navigator ruling)
- FOUND: commit `b416e1a5` (Task 3: apply declarations + mirrors)
- FOUND: commit `82b234bf` (plan-completion docs commit, this SUMMARY.md)
