---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 03
type: execute
wave: 3
depends_on: ["267.3-02"]
files_modified:
  - data/first-reward-surfaces.json
  - scripts/session-start
  - scripts/verify-release
  - tests/test-267.3-session-start-declaration.cjs
autonomous: true
requirements: [GUARD-03, GUARD-04, GUARD-08]
canon_parts: ["Part 6", "Part 7", "Part 11", "Part 12"]

must_haves:
  truths:
    - "scripts/session-start's FIRST_INSTALL branch has a first-reward declaration the linter reads and validates."
    - "Every injected-prose branch of the session-start cold-start path carries its own declaration, not one blanket declaration for the whole file."
    - "A developer editing scripts/session-start finds the declaration from inside that file, without knowing the registry exists."
    - "A release cannot be cut while a declared surface is missing, invalid, or points at a file that no longer exists."
    - "The declared value describes what the branch actually delivers today, not what its prose promises."
  artifacts:
    - path: "data/first-reward-surfaces.json"
      provides: "Populated surface records for the session-start injected-prose branches"
      contains: "session-start"
    - path: "scripts/session-start"
      provides: "In-file pointer comments anchoring each declared branch to its registry record"
      contains: "first-reward-surfaces.json"
    - path: "scripts/verify-release"
      provides: "Fail-closed release gate over the surface registry"
      contains: "--surfaces"
    - path: "tests/test-267.3-session-start-declaration.cjs"
      provides: "Tripwire binding each registry anchor to a live literal in the file it names"
      min_lines: 40
  key_links:
    - from: "data/first-reward-surfaces.json"
      to: "scripts/session-start"
      via: "record file plus anchor literal, both verified on disk at check time"
      pattern: "scripts/session-start"
    - from: "scripts/verify-release"
      to: "scripts/check-reward-before-investment.cjs"
      via: "--surfaces invocation, fail-closed"
      pattern: "--surfaces"
---

<objective>
Give the single most-first flow in the product a first-reward declaration the guard can read.

Purpose: `scripts/session-start` is the prose every user meets before any command, and it
is structurally outside the guard because a bash script has no frontmatter. Plan 02 built
the registry; this plan fills it for the real surfaces and locks it behind a release gate.

Output: populated surface records, in-file anchor comments so the declaration is findable
from the bash script itself, a tripwire test binding each anchor to a live literal, and a
fail-closed `scripts/verify-release` gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md
@data/first-reward-surfaces.json
@lib/core/mva-rule-linter.cjs
@docs/reward-before-investment-rule.md
@.planning/research/2026-08-27-hooked-first-install-audit.md
</context>

## Artifacts this plan produces

| Artifact | Kind | What it is |
|---|---|---|
| Surface records for `session-start` | registry rows | One record per injected-prose branch: FIRST_INSTALL, UPDATE, MODE_ROUTING, plus any further branch the audit's section 2 inventory names |
| Anchor comments in `scripts/session-start` | in-file pointers | A comment above each declared branch naming its registry id and declared value, so the declaration is discoverable from the file it governs |
| `tests/test-267.3-session-start-declaration.cjs` | tripwire test | Proves every registry `anchor` literal still exists in the file it names, so a rename reds the gate instead of orphaning the declaration |
| `verify-release` surfaces gate | release gate | `check-reward-before-investment.cjs --surfaces`, fail-closed, sited beside gate 10c |

<tasks>

<task type="auto">
  <name>Task 1: Classify and declare the session-start injected-prose branches</name>

  <read_first>
    - `267.3-AUDIT.md` section 2 (the unreachable surface inventory) and section 3 (the vocabulary-fit finding). Section 2's table is the authoritative list of branches to declare; do not re-derive it from memory.
    - `267.3-DECISIONS.md` D-B: the exact vocabulary member strings available.
    - `scripts/session-start` lines 640-716 in full: `ONBOARD_STATUS` and `ONBOARD_TYPE` at 647-648, `COLD_START_MENU` at 657, the FIRST_INSTALL `context=` assignment at 666, the UPDATE `context=` at 680, `MODE_MENU` at 704, the MODE_ROUTING `context=` at 706, and the mode-behavior append at 714. Line numbers may have shifted in a shared tree; grep for the literals rather than trusting the numbers.
    - `.planning/research/2026-08-27-hooked-first-install-audit.md`: the Reward-leg score of 2/10 and GAP R-1. That audit is the evidence for what FIRST_INSTALL actually delivers today, which is what the `why` field must describe.
    - `data/first-reward-surfaces.json` `_doc.record_shape` and `_doc.validation_rule`.
  </read_first>

  <files>data/first-reward-surfaces.json, scripts/session-start</files>

  <action>
(1) For each branch named in `267.3-AUDIT.md` section 2, pick its
`interactive_first_reward` value from the vocabulary ruled in D-B, and write a `why` that
describes what the branch ACTUALLY delivers today, not what its prose promises. That
distinction is the whole point of the declaration:
`.planning/research/2026-08-27-hooked-first-install-audit.md` scored the FIRST_INSTALL
Reward leg 2/10 precisely because the payload asserts a reward as a prose instruction to the
model with no wired call. A `why` claiming a delivered reward the code does not produce is a
false declaration, which is the exact failure plan 271-03 refused to commit.

Where a branch genuinely does not deliver a first reward today, declare the honest opt-out
term from D-B, say so in `why`, and cross-reference Phase 267.2, which OWNS repairing GAP
R-1. This phase declares the contract; it does not repair the onboarding flow.

Hard constraints on `scripts/session-start` edits: change no executable line, edit no prose
payload string, and do not touch the SEED-021 line inside the FIRST_INSTALL branch.
Reverting that line is explicitly Phase 267.2's W0 work per the navigator's 267.1-06 ruling,
and doing it here would repeat the scope error that ruling reversed.

(2) Append one record per branch to `data/first-reward-surfaces.json`'s `surfaces` array,
matching `_doc.record_shape` exactly. Set `file` to `scripts/session-start`. Set `kind` to
`injected_prose` for the `context=` payload branches. Set `anchor` to a literal string that
is grep-able and unique to that branch (for example the `ONBOARD_TYPE` comparison literal,
or the `COLD_START_MENU` / `MODE_MENU` variable name). The anchor is what Task 2's tripwire
greps for, so it must be a real substring of the file, never a paraphrase.

(3) Add a short comment block in `scripts/session-start` immediately above each declared
branch, in the file's existing comment style. Each comment names the registry id, the
declared value, and the path `data/first-reward-surfaces.json`. Comments only, nothing
executable. This is what makes the declaration findable by a developer editing the bash file
who has never heard of the registry.

House rules: hyphens only, no em-dashes, no emoji.
  </action>

  <verify>
    <automated>node scripts/check-reward-before-investment.cjs --surfaces . && echo SURFACES_GREEN</automated>
    <automated>node -e "const j=require('./data/first-reward-surfaces.json');const a=require('node:assert/strict');const ss=j.surfaces.filter(s=>s.file==='scripts/session-start');a.ok(ss.length>=3,'expected at least 3 session-start records, got '+ss.length);const ids=new Set();for(const s of ss){a.ok(s.id&&!ids.has(s.id),'duplicate or empty id');ids.add(s.id);a.ok(s.anchor&&s.anchor.length>3,'anchor too short: '+s.id);a.ok(s.why&&s.why.length>20,'why too thin: '+s.id);a.equal(s.why.indexOf('—'),-1,'em-dash in why: '+s.id)}console.log('T1_OK records',ss.length)"</automated>
    <automated>bash -n scripts/session-start && echo BASH_SYNTAX_OK</automated>
    <automated>git diff -U0 -- scripts/session-start | grep '^[+-]' | grep -v '^[+-][+-][+-]' | grep -v '^[+-][[:space:]]*#' | wc -l | grep -qx 0 && echo COMMENTS_ONLY_OK || (echo "NON-COMMENT LINE CHANGED IN session-start"; exit 1)</automated>
    <automated>git diff -- scripts/session-start | grep -c 'SEED-021' | grep -qx 0 && echo SEED021_UNTOUCHED</automated>
  </verify>

  <acceptance_criteria>
    - `data/first-reward-surfaces.json` contains at least 3 records whose `file` is `scripts/session-start`, one per branch named in `267.3-AUDIT.md` section 2, with unique ids.
    - Every such record has a non-empty `anchor` that is a literal substring of `scripts/session-start`, and a `why` longer than 20 characters with no em-dash.
    - `node scripts/check-reward-before-investment.cjs --surfaces .` exits 0.
    - `bash -n scripts/session-start` exits 0.
    - The `scripts/session-start` diff contains ONLY comment lines: the fourth verify command reports 0 non-comment changed lines.
    - The `scripts/session-start` diff contains zero lines mentioning `SEED-021`.
    - No `why` field claims a reward the branch does not actually deliver; any branch scored as not delivering one carries the D-B opt-out term plus a Phase 267.2 cross-reference.
  </acceptance_criteria>

  <done>The most-first flow in the product carries a machine-readable, honest first-reward declaration, and a developer reading only the bash file can find it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anchor tripwire test binding each registry record to a live literal</name>

  <read_first>
    - `data/first-reward-surfaces.json` (populated by Task 1): every record's `file` and `anchor`.
    - `tests/test-267.3-surface-declarations.cjs` (from plan 02): the in-tree runner style to match.
    - `tests/run-all-267.3.sh` (from plan 02): the glob it discovers by, so this file is picked up automatically.
  </read_first>

  <files>tests/test-267.3-session-start-declaration.cjs</files>

  <behavior>
    - Test 1: for every record in `surfaces`, `record.file` resolves to an existing file under the repo root.
    - Test 2: for every record, `record.anchor` appears as a literal substring of that file's contents; a missing anchor fails with the record id in the message.
    - Test 3: `surfaces` contains at least one record whose `file` is `scripts/session-start` and whose `anchor` locates the FIRST_INSTALL branch.
    - Test 4: every `record.id` is unique across the whole registry.
    - Test 5: every `record.interactive_first_reward` is a member of the linter's live `REWARD_TYPES` (not a copy of the list, the live Set).
    - Test 6: no `why` field contains a U+2014 em-dash.
    - Test 7: a synthetic registry whose anchor is absent from its file fails the same assertion, proving the tripwire actually fires rather than passing vacuously.
  </behavior>

  <action>
Create `tests/test-267.3-session-start-declaration.cjs` in the same in-tree runner style as
`tests/test-267.3-surface-declarations.cjs`: no framework, `node:assert/strict`, `run(name,
fn)` counters, a trailing `PASS/FAIL` summary, and a non-zero exit on any failure.

The point of this file is the anchor binding, which `scanDeclaredSurfaces` deliberately does
NOT enforce: the linter checks that the FILE exists, and this test checks that the anchor
still points at something inside it. Without it, a future refactor that renames
`COLD_START_MENU` or restructures the `ONBOARD_TYPE` branches would leave a declaration
pointing at prose that no longer exists, and every gate would stay green.

Test 7 is what stops this file passing vacuously: build a temp registry with a deliberately
absent anchor and assert the same predicate function returns a failure for it. Extract the
predicate into a small local function so the live sweep and the synthetic case share one
implementation.

Naming must match the `tests/test-267.3-*.cjs` glob so `tests/run-all-267.3.sh` discovers it
with no aggregator edit.

House rules: pure CJS, node built-ins only, hyphens not em-dashes, no emoji.
  </action>

  <verify>
    <automated>node tests/test-267.3-session-start-declaration.cjs</automated>
    <automated>bash tests/run-all-267.3.sh</automated>
    <automated>node -e "const fs=require('fs'),cp=require('child_process');const p='scripts/session-start';const o=fs.readFileSync(p,'utf8');const j=require('./data/first-reward-surfaces.json');const anc=j.surfaces.find(s=>s.file==='scripts/session-start').anchor;fs.writeFileSync(p,o.split(anc).join('ANCHOR_REMOVED_FOR_TEST'));const r=cp.spawnSync('node',['tests/test-267.3-session-start-declaration.cjs'],{encoding:'utf8'});fs.writeFileSync(p,o);if(r.status===0){console.error('TRIPWIRE DID NOT FIRE');process.exit(1)}console.log('TRIPWIRE_FIRES_OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/test-267.3-session-start-declaration.cjs` exits 0 and reports at least 7 passing checks.
    - `bash tests/run-all-267.3.sh` discovers the new file without any aggregator edit and exits 0.
    - The live-mutation proof passes: removing an anchor literal from `scripts/session-start` makes the test exit non-zero, and the file is restored byte-identically afterwards (`git diff --stat -- scripts/session-start` unchanged from Task 1's state).
    - Test 5 reads `REWARD_TYPES` from `lib/core/mva-rule-linter.cjs` at run time rather than hardcoding the token list.
  </acceptance_criteria>

  <done>A declaration cannot silently outlive the prose it describes: renaming or deleting the anchored branch reds this test.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the surface registry into scripts/verify-release, fail-closed</name>

  <read_first>
    - `scripts/verify-release` lines 310-380: gate 10b (skill mirrors) and gate 10c (plugin path anchoring). Gate 10c is the model to copy: it captures output, captures the exit code, and fails the release on non-zero.
    - `scripts/check-reward-before-investment.cjs`: the `--surfaces` mode's exit-code contract (0 clean, 1 violation, 2 ungateable).
    - `.planning/ROADMAP.md` Phase 271 Wave 5 entry: the 271-05 discipline that a gate is wired fail-closed and never relaxed to make the board green, and the record that `verify-release` is ALREADY red on gate 10c.
  </read_first>

  <files>scripts/verify-release</files>

  <action>
Add a new gate immediately after gate 10c, numbered in that file's own scheme (`10d` if 10c
is the last of the 10-series; read the file and follow whatever numbering is live). Model it
line for line on the 10c block:

- Echo a bold gate heading naming it as the first-reward surface declaration gate.
- Run `node "$PLUGIN_ROOT/scripts/check-reward-before-investment.cjs" --surfaces "$PLUGIN_ROOT"`,
  capturing both output and exit code the way the 10c block captures `ANCHOR_OUT` and
  `ANCHOR_CODE`.
- On exit 0, emit a `pass` line naming the gate and the surface count.
- On any non-zero exit, emit a `fail` line, print the captured output so the offending
  surface id is visible in the release log, and fail the release.

Explicitly do NOT wire the whole-tree `commands/` audit here. That gate is plan 267.3-08's
work and must not be turned on until the 67-command backfill lands, or `verify-release` gains
a second red for work this plan does not do. Record that sequencing decision in a comment
above the new gate so a future reader does not "fix" the omission.

Do NOT relax, allowlist, or soften gate 10c. It is correctly red on
`commands/doctor.md:262` and plan 267.3-05 turns it green by doing the work, not by
adjusting the instrument.

House rules: hyphens only, no em-dashes, no emoji. Match the surrounding bash style exactly.
  </action>

  <verify>
    <automated>bash -n scripts/verify-release && echo VERIFY_RELEASE_SYNTAX_OK</automated>
    <automated>grep -q 'check-reward-before-investment.cjs" --surfaces' scripts/verify-release && echo GATE_WIRED</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/verify-release','utf8');const a=require('node:assert/strict');const i=s.indexOf('--surfaces');a.ok(i>0,'gate not found');const j=s.indexOf('check-plugin-path-anchoring');a.ok(j>0&&j<i,'new gate must sit AFTER gate 10c');a.equal(s.indexOf('—'),-1,'em-dash in verify-release');console.log('GATE_PLACEMENT_OK')"</automated>
    <automated>node -e "const fs=require('fs'),cp=require('child_process'),path=require('path');const p='data/first-reward-surfaces.json';const o=fs.readFileSync(p,'utf8');const j=JSON.parse(o);j.surfaces.push({id:'ab-fixture',file:'scripts/session-start',kind:'injected_prose',anchor:'x',interactive_first_reward:'definitely_not_a_real_reward',why:'temporary A/B fixture'});fs.writeFileSync(p,JSON.stringify(j,null,2));const r=cp.spawnSync('node',['scripts/check-reward-before-investment.cjs','--surfaces','.'],{encoding:'utf8'});fs.writeFileSync(p,o);if(r.status===0){console.error('GATE DID NOT FIRE ON A BAD RECORD');process.exit(1)}console.log('AB_FIXTURE_PROVES_GATE_FIRES exit='+r.status)"</automated>
    <automated>git diff -- scripts/verify-release | grep -E '^-' | grep -v '^---' | grep -iE 'anchor|10c' | wc -l | grep -qx 0 && echo GATE_10C_UNTOUCHED</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/verify-release` exits 0.
    - The new gate invokes `check-reward-before-investment.cjs --surfaces` and sits AFTER the `check-plugin-path-anchoring` block in the file.
    - The A/B fixture proof passes: injecting a record with an invalid reward value makes `--surfaces` exit non-zero, and `data/first-reward-surfaces.json` is restored byte-identically (`git diff --stat -- data/first-reward-surfaces.json` unchanged from Task 1's state).
    - The diff removes zero lines belonging to gate 10c or the anchoring gate.
    - A comment above the new gate states why the whole-tree `commands/` audit is deliberately NOT wired here and names plan 267.3-08 as its owner.
    - Zero em-dash characters in `scripts/verify-release`.
  </acceptance_criteria>

  <done>A release cannot be cut while a declared surface is missing, invalid, or orphaned, and the gate was proven to fire against a deliberately broken fixture rather than assumed to work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session prose -> every user | `scripts/session-start` is the first bytes a user ever sees; a false claim here is the highest-blast-radius honesty failure in the product (Canon Part 12) |
| registry anchor -> bash file | A hand-authored literal pointing into a file another session may be editing concurrently |
| new gate -> release cut | A gate wired wrong either blocks every release or silently passes everything |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.3-11 | Spoofing | a `why` claiming a reward the branch does not deliver | mitigate | Task 1 requires `why` to describe delivered behavior, cites the 2/10 Reward-leg audit as the evidence base, and routes any non-delivering branch to the D-B opt-out plus a Phase 267.2 cross-reference |
| T-267.3-12 | Tampering | accidental edit to session-start executable lines in a shared tree | mitigate | Comments-only diff assertion (verify 4) plus `bash -n` syntax check plus the SEED-021 zero-hit assertion |
| T-267.3-13 | Tampering | an orphaned anchor after a future refactor | mitigate | `tests/test-267.3-session-start-declaration.cjs` greps every anchor at test time, and the tripwire is itself proven to fire by live mutation |
| T-267.3-14 | Denial of service | new gate blocking every release for the wrong reason | mitigate | A/B fixture proof: the gate fires on a bad record and exits 0 on the real registry, both demonstrated, following 271-05's precedent |
| T-267.3-15 | Elevation of privilege | quietly relaxing gate 10c to make the board green | mitigate | Verify 5 asserts the diff removes zero anchoring-gate lines; GUARD-10 covers this phase-wide |
| T-267.3-SC | Tampering | package installs | accept | Zero npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- `node scripts/check-reward-before-investment.cjs --surfaces .` exits 0 with a non-zero surface count.
- `bash tests/run-all-267.3.sh` exits 0.
- `bash -n scripts/session-start` and `bash -n scripts/verify-release` both exit 0.
- `bash tests/run-all-267.1.sh` exits with the same PASS/FAIL numbers as before this plan (the session-start suite must not move; this plan adds comments only).
- `git diff --numstat -- commands/ skills/` still reads 16 files / 30 lines, unchanged: this plan touches no command file or mirror.
- `node scripts/check-plugin-path-anchoring.cjs --check` still reports the SAME violation count as before this plan (expected 1 in the working tree, 31 at HEAD). This plan neither fixes nor worsens it.
</verification>

<success_criteria>
- Every injected-prose branch named in the audit carries its own registry record with an honest `why`, a live anchor, and a vocabulary-valid value.
- A developer reading only `scripts/session-start` can find the declaration and the registry path.
- The anchor tripwire is proven to fire by live mutation, not assumed.
- `scripts/verify-release` gains a fail-closed surfaces gate, proven against an A/B fixture, with gate 10c untouched.
</success_criteria>

<output>
Create `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-03-SUMMARY.md` when done.
</output>
