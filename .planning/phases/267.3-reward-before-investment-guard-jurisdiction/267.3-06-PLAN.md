---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 06
type: execute
wave: 5
depends_on: ["267.3-05"]
files_modified:
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md
  - commands/admin.md
  - commands/agentshield.md
  - commands/auto-explore.md
  - commands/correct-reference-now.md
  - commands/dashboard.md
  - commands/diagnostics.md
  - commands/dial-memory-refresh.md
  - commands/dogfood-flush.md
  - commands/eureka.md
  - commands/explain-decision.md
  - commands/export.md
  - commands/feynman-timeline-refresh.md
  - commands/funding.md
  - commands/graph.md
  - commands/hat-briefing.md
  - commands/heal.md
  - commands/help.md
  - commands/hmi-status.md
  - commands/ingest-methodology.md
  - commands/intel-pipeline.md
  - commands/jtbd.md
  - commands/memory-cortex-reach.md
  - commands/memory.md
  - commands/models.md
  - commands/mos.md
  - skills/*/SKILL.md
autonomous: false
requirements: [GUARD-07, GUARD-02, GUARD-05]
canon_parts: ["Part 7", "Part 11", "Part 12"]

must_haves:
  truths:
    - "Each of the 25 commands in batch A carries an interactive_first_reward chosen by the ruled rubric, with its first-reward moment cited by line."
    - "No command with hitl_shape F.0 or F.1 and no scripting flag in its body carries the scripting opt-out."
    - "A human ruled every row the rubric could not settle, before any frontmatter was written."
    - "The full audit's missing count drops by exactly 25 and its invalid count stays at zero."
  artifacts:
    - path: ".planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md"
      provides: "Batch A section: 25 reasoned rows plus their navigator ruling"
      contains: "Batch A"
  key_links:
    - from: "267.3-CLASSIFICATION.md"
      to: "commands/*.md"
      via: "each row cites the line where that command first delivers something to the user"
      pattern: "commands/[a-z-]+\\.md:"
---

<objective>
Declare the first 25 of the 50 commands that remain undeclared after the Phase 271 unblock.

Purpose: the guard only fires on files a commit happens to stage, so an undeclared command
is invisible debt until an unrelated sweep trips over it. That is exactly how Phase 271
stalled. Closing the whole tree is what turns the guard from a tripwire into a gate.

Output: 25 reasoned classification rows, a navigator ruling on whatever the rubric could not
settle, 25 frontmatter declarations, and regenerated mirrors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md
@docs/reward-before-investment-rule.md
@lib/core/mva-rule-linter.cjs
</context>

## Artifacts this plan produces

| Artifact | Kind | What it is |
|---|---|---|
| `267.3-CLASSIFICATION.md` Batch A section | phase doc extension | 25 rows in the same shape plan 04 established, plus a batch-A navigator ruling section |
| 25 `interactive_first_reward` declarations | frontmatter | One per batch-A command |
| Regenerated skill mirrors | generated | Via `build-skill-mirrors.cjs`, run once at the end of Task 3 |

<tasks>

<task type="auto">
  <name>Task 1: Classify batch A against the ruled rubric</name>

  <read_first>
    - `267.3-CLASSIFICATION.md` Part 1 (the rubric) and its `## Navigator ruling` section from plan 04. The rubric is already ruled; apply it, do not re-derive it. Read plan 04's 17 rows as worked examples of the row shape and the level of evidence expected.
    - `267.3-DECISIONS.md` D-B: the closed vocabulary. Minting a new term here is forbidden; a genuine gap is a NEEDS-RULING row, not an invention.
    - `docs/reward-before-investment-rule.md`: the four qualifying tests, the two named disqualifiers, and the scripting override's exact wording.
    - The 25 batch-A command files, each read from frontmatter down to the first user-visible output: `admin`, `agentshield`, `auto-explore`, `correct-reference-now`, `dashboard`, `diagnostics`, `dial-memory-refresh`, `dogfood-flush`, `eureka`, `explain-decision`, `export`, `feynman-timeline-refresh`, `funding`, `graph`, `hat-briefing`, `heal`, `help`, `hmi-status`, `ingest-methodology`, `intel-pipeline`, `jtbd`, `memory-cortex-reach`, `memory`, `models`, `mos`. You do not need the full body of a 400-line file to find its first reward; stop reading once you have located it.
  </read_first>

  <files>.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md</files>

  <action>
Append a `## Batch A (plan 267.3-06)` section to `267.3-CLASSIFICATION.md` with one row per
command, in the exact shape plan 04's Part 2 used: command name, `hitl_shape`, `body_shape`,
the `path:line` where the command first delivers something to the user, one plain sentence
describing that first delivery, the proposed value, the runner-up and why it lost, and a HIGH
or NEEDS-RULING marker.

Apply the ruled tie-break rules mechanically and record when one fires. In particular, this
batch contains the highest concentration of the trap plan 04's rubric exists to catch: many
of these commands shell out to scripts and look like scripting surfaces, but
`--none (scripting only)` is legitimate ONLY where the body genuinely offers a
`--no-interactive`, `--script`, or `-q` invocation path. Grep each candidate's body for such
a flag before proposing that value, and record the grep result in the row. A command like
`heal` or `dogfood-flush` that runs code but has no scripting flag and no variable reward
takes the diagnostic opt-out term from D-B, not the scripting one.

Two rows deserve extra care and should be argued rather than asserted: `eureka`
(`hitl_shape: F.8`, a fire-and-poll long-running operation whose payoff is genuinely
unpredictable, so the reward terms are live candidates) and `funding` (`hitl_shape: F.8`,
and `docs/reward-before-investment-rule.md` names "a Tavily-sourced funding opportunity
surfaced unsolicited" as a worked example of a real reward, which is direct evidence for
this exact command).

Write no frontmatter in this task.

House rules: hyphens only, no em-dashes, no emoji.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const a=require('node:assert/strict');const i=t.indexOf('## Batch A');a.ok(i>0,'Batch A section missing');const sec=t.slice(i);const L=['admin','agentshield','auto-explore','correct-reference-now','dashboard','diagnostics','dial-memory-refresh','dogfood-flush','eureka','explain-decision','export','feynman-timeline-refresh','funding','graph','hat-briefing','heal','help','hmi-status','ingest-methodology','intel-pipeline','jtbd','memory-cortex-reach','memory','models','mos'];for(const c of L)a.ok(sec.includes(c),'missing row: '+c);const cites=(sec.match(/commands\/[a-z-]+\.md:\d+/g)||[]);a.ok(cites.length>=25,'expected 25 path:line citations, found '+cites.length);a.equal(sec.indexOf('—'),-1,'em-dash found');console.log('BATCH_A_ROWS_OK',cites.length)"</automated>
    <automated>node -e "const fs=require('fs');const cp=require('child_process');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const sec=t.slice(t.indexOf('## Batch A'));const L=['admin','agentshield','auto-explore','correct-reference-now','dashboard','diagnostics','dial-memory-refresh','dogfood-flush','eureka','explain-decision','export','feynman-timeline-refresh','funding','graph','hat-briefing','heal','help','hmi-status','ingest-methodology','intel-pipeline','jtbd','memory-cortex-reach','memory','models','mos'];let bad=0;for(const c of L){const row=sec.split('\n').find(l=>l.includes(c));if(!row)continue;if(!row.includes('--none (scripting only)'))continue;const has=cp.spawnSync('grep',['-qE','--no-interactive|--script|(^|[^a-z])-q([^a-z]|\$)','commands/'+c+'.md']).status===0;if(!has){console.error('SCRIPTING OPT-OUT PROPOSED WITHOUT A SCRIPTING FLAG: '+c);bad++}}if(bad)process.exit(1);console.log('SCRIPTING_TIEBREAK_HELD')"</automated>
    <automated>git diff --name-only -- commands/ skills/ | wc -l | grep -qx 0 && echo NO_FRONTMATTER_WRITTEN_YET</automated>
  </verify>

  <acceptance_criteria>
    - `267.3-CLASSIFICATION.md` contains a `## Batch A (plan 267.3-06)` section with all 25 command names.
    - At least 25 `commands/<name>.md:<line>` citations appear in that section.
    - Every row proposing `--none (scripting only)` is backed by a real scripting flag in that command's body: the second verify command exits 0.
    - The `eureka` and `funding` rows each carry a reasoning paragraph, not just a table cell.
    - Every proposed value is a member of the live `REWARD_TYPES`.
    - Zero em-dash characters in the appended section.
    - `git diff --name-only -- commands/ skills/` is empty: no frontmatter written.
  </acceptance_criteria>

  <done>25 proposed declarations exist, each traced to a line of its own command body, with the scripting opt-out mechanically proven wherever it was proposed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator ruling on batch A</name>

  <read_first>
    - `267.3-CLASSIFICATION.md` `## Batch A (plan 267.3-06)` section, all 25 rows.
  </read_first>

  <what-built>
25 proposed `interactive_first_reward` values produced by applying the rubric the navigator
already ruled in plan 04. Each row cites the line where the command first delivers something
to the user. The scripting opt-out was mechanically checked against each command's body.
Zero frontmatter written; nothing committed.
  </what-built>

  <how-to-verify>
1. Open `267.3-CLASSIFICATION.md` and jump to `## Batch A (plan 267.3-06)`.
2. Read every row marked NEEDS-RULING first. Those are the ones the rubric could not settle
   and they need your call.
3. Read the `eureka` and `funding` reasoning paragraphs. Those two have the strongest case
   for a real reward term, and `funding` has direct support in the rule doc's own worked
   examples.
4. Spot-check three HIGH rows against their command files: does the cited line actually
   describe that command's first delivery?
5. Scan the diagnostic-opt-out rows as a group (`heal`, `dogfood-flush`, `hmi-status`,
   `dial-memory-refresh`, `feynman-timeline-refresh`, `diagnostics`). If that group looks
   over-applied, say so; the failure mode here is a rubber stamp that makes the audit green
   without making the product honest.

These become permanent frontmatter. A wrong declaration is worse than a missing one.
  </how-to-verify>

  <action>
Present the batch-A table with NEEDS-RULING rows listed first. Record the ruling as a
`### Batch A navigator ruling` subsection: the date, the final value for every NEEDS-RULING
row, any HIGH row overridden with the reason, and the final approved list of 25. Task 3 reads
values only from that subsection.
  </action>

  <resume-signal>Type "approved" to accept all 25 as proposed, or name the rows to change and their correct values.</resume-signal>

  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const a=require('node:assert/strict');const i=t.indexOf('### Batch A navigator ruling');a.ok(i>0,'batch A ruling section missing');const sec=t.slice(i);const L=['admin','agentshield','auto-explore','correct-reference-now','dashboard','diagnostics','dial-memory-refresh','dogfood-flush','eureka','explain-decision','export','feynman-timeline-refresh','funding','graph','hat-briefing','heal','help','hmi-status','ingest-methodology','intel-pipeline','jtbd','memory-cortex-reach','memory','models','mos'];for(const c of L)a.ok(sec.includes(c),'ruling missing final value for '+c);console.log('BATCH_A_RULING_COVERS_25')"</automated>
    <automated>git diff --name-only -- commands/ skills/ | wc -l | grep -qx 0 && echo NOTHING_WRITTEN_YET</automated>
  </verify>

  <acceptance_criteria>
    - A `### Batch A navigator ruling` subsection exists, dated, naming a final value for all 25 commands.
    - Every NEEDS-RULING row has a ruled value.
    - Any override is recorded with its reason.
    - `git diff --name-only -- commands/ skills/` is still empty.
  </acceptance_criteria>

  <done>A human has ruled on all 25 batch-A values and the ruling is written down.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the 25 batch-A declarations, regenerate mirrors, commit</name>

  <read_first>
    - `267.3-CLASSIFICATION.md` `### Batch A navigator ruling`: the approved value per command. Use only this subsection.
    - `commands/onboard.md` lines 1-20: the frontmatter placement and provenance-comment style.
    - Any command file plan 267.3-04 already declared (for example `commands/deck.md`), to copy the exact key placement and quoting this phase settled on.
  </read_first>

  <files>commands/admin.md, commands/agentshield.md, commands/auto-explore.md, commands/correct-reference-now.md, commands/dashboard.md, commands/diagnostics.md, commands/dial-memory-refresh.md, commands/dogfood-flush.md, commands/eureka.md, commands/explain-decision.md, commands/export.md, commands/feynman-timeline-refresh.md, commands/funding.md, commands/graph.md, commands/hat-briefing.md, commands/heal.md, commands/help.md, commands/hmi-status.md, commands/ingest-methodology.md, commands/intel-pipeline.md, commands/jtbd.md, commands/memory-cortex-reach.md, commands/memory.md, commands/models.md, commands/mos.md, skills/*/SKILL.md (regenerated mirrors)</files>

  <action>
For each of the 25 files, insert exactly one `interactive_first_reward: <ruled value>`
frontmatter line plus one provenance comment above it, placed adjacent to `hitl_shape` and
`hitl_why` so the declaration family stays together. Match the quoting convention plan
267.3-04 settled on exactly.

Add nothing else. Do not reorder, reformat, requote or normalize any existing key. Do not
touch any body line. Each file's diff should be exactly two added lines.

Then run `node scripts/build-skill-mirrors.cjs` once, followed by
`node scripts/build-skill-mirrors.cjs --check`.

Commit through the full pre-commit hook with `COMMIT_NO_VERIFY` UNSET:
`fix(267.3): declare interactive_first_reward for batch A (25 commands)`
If the hook refuses, fix the declaration rather than bypassing. Record the commit hash.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const {parseFrontmatter,REWARD_TYPES}=require('./lib/core/mva-rule-linter.cjs');const a=require('node:assert/strict');const L=['admin','agentshield','auto-explore','correct-reference-now','dashboard','diagnostics','dial-memory-refresh','dogfood-flush','eureka','explain-decision','export','feynman-timeline-refresh','funding','graph','hat-briefing','heal','help','hmi-status','ingest-methodology','intel-pipeline','jtbd','memory-cortex-reach','memory','models','mos'];for(const c of L){const fm=parseFrontmatter(fs.readFileSync('commands/'+c+'.md','utf8'));a.ok(fm&&fm.interactive_first_reward,'no declaration: '+c);a.ok(REWARD_TYPES.has(String(fm.interactive_first_reward).trim()),c+' invalid value: '+fm.interactive_first_reward)}console.log('BATCH_A_25_DECLARED')"</automated>
    <automated>node -e "const cp=require('child_process');const L=['admin','agentshield','auto-explore','correct-reference-now','dashboard','diagnostics','dial-memory-refresh','dogfood-flush','eureka','explain-decision','export','feynman-timeline-refresh','funding','graph','hat-briefing','heal','help','hmi-status','ingest-methodology','intel-pipeline','jtbd','memory-cortex-reach','memory','models','mos'];let bad=0;for(const c of L){const o=cp.execSync('git diff HEAD~1 -U0 -- commands/'+c+'.md',{encoding:'utf8'});const add=o.split('\n').filter(l=>/^\+/.test(l)&&!/^\+\+\+/.test(l));const del=o.split('\n').filter(l=>/^-/.test(l)&&!/^---/.test(l));if(add.length!==2||del.length!==0){console.error(c+': expected 2 added 0 removed, got '+add.length+'/'+del.length);bad++}}if(bad)process.exit(1);console.log('EVERY_DIFF_IS_TWO_ADDED_LINES')"</automated>
    <automated>node scripts/build-skill-mirrors.cjs --check</automated>
    <automated>node scripts/check-reward-before-investment.cjs 2>&1 | grep -E '^  (compliant|missing|invalid):'</automated>
    <automated>node scripts/check-plugin-path-anchoring.cjs --check && echo ANCHORING_STILL_GREEN</automated>
    <automated>bash tests/run-all-267.3.sh</automated>
    <automated>git log -1 --format=%B | grep -qi 'no-verify\|COMMIT_NO_VERIFY' && (echo BYPASS_REFERENCED; exit 1) || echo NO_BYPASS</automated>
  </verify>

  <acceptance_criteria>
    - All 25 batch-A commands parse with an `interactive_first_reward` that is a member of the live `REWARD_TYPES` and equals its ruled value.
    - Every one of the 25 diffs is exactly 2 added lines and 0 removed lines.
    - `node scripts/build-skill-mirrors.cjs --check` exits 0.
    - The full audit's missing count is exactly 25 lower than after plan 267.3-05, and invalid is still 0.
    - `node scripts/check-plugin-path-anchoring.cjs --check` still exits 0.
    - `bash tests/run-all-267.3.sh` exits 0.
    - The commit landed with the full pre-commit hook running and `COMMIT_NO_VERIFY` unset.
  </acceptance_criteria>

  <done>25 more commands are declared, their mirrors agree, every diff is provably two lines, and the hook passed on its own.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rubric -> 25 permanent declarations | A rubber-stamped batch would make the audit green without making the product honest |
| mirror generator -> skills tree | One run rewrites many files; a concurrent session editing skills would collide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.3-26 | Spoofing | rubber-stamping the diagnostic opt-out across a whole class | mitigate | Task 2's checkpoint explicitly asks the navigator to scan that class as a group and name it if over-applied |
| T-267.3-27 | Spoofing | scripting opt-out on a command with no scripting flag | mitigate | Task 1's second verify greps each proposing command's body for `--no-interactive`, `--script` or `-q` and fails the task if absent |
| T-267.3-28 | Tampering | frontmatter churn beyond the declaration | mitigate | Per-file assertion that every diff is exactly 2 added, 0 removed lines |
| T-267.3-29 | Elevation of privilege | inventing a vocabulary term to fit a hard command | mitigate | Values asserted to be members of the live `REWARD_TYPES`; a genuine gap is a NEEDS-RULING row for the human, never an invention |
| T-267.3-30 | Elevation of privilege | hook bypass to land the batch | mitigate | `COMMIT_NO_VERIFY` forbidden; verify asserts the commit message references no bypass |
| T-267.3-SC | Tampering | package installs | accept | Zero npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- Full audit missing count down by exactly 25 versus the post-267.3-05 reading; invalid 0.
- `node scripts/build-skill-mirrors.cjs --check` exits 0.
- `node scripts/check-plugin-path-anchoring.cjs --check` exits 0 (unchanged by this plan).
- `bash tests/run-all-267.3.sh`, `bash tests/run-all-118.sh`, `bash tests/run-all-271.sh` all exit 0.
- `node scripts/check-reward-before-investment.cjs --surfaces .` exits 0 (unchanged by this plan).
</verification>

<success_criteria>
- 25 human-ruled, vocabulary-valid declarations exist, each traced to a cited line.
- The scripting opt-out was mechanically proven wherever proposed, never assumed.
- Every diff is two lines, mirrors agree, and the commit went through the unmodified hook.
</success_criteria>

<output>
Create `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-06-SUMMARY.md` when done.
</output>
