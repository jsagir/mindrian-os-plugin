---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 08
type: execute
wave: 7
depends_on: ["267.3-07"]
files_modified:
  - scripts/verify-release
  - docs/reward-before-investment-rule.md
  - CHANGELOG.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/debug/knowledge-base.md
autonomous: false
requirements: [GUARD-08, GUARD-09, GUARD-10, GUARD-01, GUARD-07]
canon_parts: ["Part 6", "Part 7", "Part 11"]

must_haves:
  truths:
    - "A release cannot be cut while any commands/*.md is missing or carries an invalid interactive_first_reward."
    - "The debt is now visible without waiting for an unrelated commit to stage the files."
    - "The rule doc states the current enforcement surface accurately: two declaration sources, three CLI modes, one commit gate and two release gates."
    - "The reasoning trail lives in both the repo and the rethinking-mindrianos room, cross-linked in both directions."
    - "No gate anywhere in the repo was relaxed, allowlisted, edited, or bypassed to make the board green."
  artifacts:
    - path: "scripts/verify-release"
      provides: "The whole-tree commands audit promoted to a fail-closed release gate"
      contains: "check-reward-before-investment.cjs"
    - path: "docs/reward-before-investment-rule.md"
      provides: "Accurate enforcement-surface description plus the jurisdiction ruling summary"
      contains: "first-reward-surfaces.json"
    - path: ".planning/REQUIREMENTS.md"
      provides: "The ten GUARD-NN requirement IDs registered at phase close"
      contains: "GUARD-01"
  key_links:
    - from: "scripts/verify-release"
      to: "scripts/check-reward-before-investment.cjs"
      via: "full-audit invocation, fail-closed"
      pattern: "check-reward-before-investment"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the Phase 267.3 Requirements line names the registered GUARD IDs"
      pattern: "GUARD-"
---

<objective>
Turn the green tree into a gate, and close the paper trail.

Purpose: a backfill that is not gated decays. Phase 118-06 shipped the rule with six
declarations and no backfill, and the rest accreted by accident over three months; Phase
245-02 then narrowed the gate to staged files, which made the remaining debt invisible until
plan 271-03 tripped over it. This plan closes that loop: the whole tree is green, so the
whole tree becomes a release gate.

Output: the fail-closed release gate, an accurate rule doc, the CHANGELOG entry, the ROADMAP
and REQUIREMENTS registrations, the knowledge-base entry, the Dev-Research compositing
trail, and a phase-wide no-relaxation audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/reward-before-investment-rule.md
@CLAUDE.md
</context>

## Artifacts this plan produces

| Artifact | Kind | What it is |
|---|---|---|
| `verify-release` full-audit gate | release gate | The whole-tree `commands/` audit, fail-closed, proven against an A/B fixture |
| Rule-doc enforcement section | doc update | Two declaration sources, three CLI modes, one commit gate, two release gates, stated accurately |
| CHANGELOG entry | release doc | The phase's user-visible and contributor-visible change |
| GUARD-01 through GUARD-10 | requirement registration | Registered in `.planning/REQUIREMENTS.md` at phase close, per the Phase 270 precedent |
| Knowledge-base entry | debug KB | The debt-ratchet lesson, so the next narrow-scoped gate is designed with a whole-tree audit from day one |
| Room compositing trail | research record | `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-reward-guard-jurisdiction/`, mirrored and cross-linked both directions |

<tasks>

<task type="auto">
  <name>Task 1: Promote the whole-tree audit to a fail-closed release gate and correct the rule doc</name>

  <read_first>
    - `scripts/verify-release`: the 10-series gates, including the surfaces gate plan 267.3-03 added and gate 10c's capture-output-capture-code pattern.
    - `scripts/check-reward-before-investment.cjs`: the full-audit default mode and its exit-code contract.
    - `docs/reward-before-investment-rule.md`: the "Detection mechanism (Phase 118-06)" section, its "Two scopes (Phase 245-02)" table, and the `## Vocabulary amendments` section plan 267.3-02 added.
    - `.planning/ROADMAP.md` Phase 271 Wave 5 entry: the 271-05 precedent of proving a new gate against an A/B temp fixture rather than assuming it fires.
  </read_first>

  <files>scripts/verify-release, docs/reward-before-investment-rule.md</files>

  <action>
(1) Add the whole-tree commands audit to `scripts/verify-release` as a gate, sited next to
the surfaces gate plan 267.3-03 added so the two reward gates read together. Model it on the
same capture-output, capture-code, pass-or-fail pattern gate 10c uses. Invoke
`node "$PLUGIN_ROOT/scripts/check-reward-before-investment.cjs" "$PLUGIN_ROOT/commands"`
(the full-audit mode, no flag). On non-zero, print the captured output so the offending file
names appear in the release log, and fail the release.

Prove it fires before declaring it done: copy `commands/` to a temp directory, strip the
`interactive_first_reward` line from one file there, run the CLI against the temp directory,
confirm exit 1, then run it against the real `commands/` and confirm exit 0. Record both
outputs in the summary. This is the 271-05 A/B discipline, and it is the difference between
a wired gate and a decorative one.

Do not relax, allowlist or soften any existing gate. Do not remove the surfaces gate.

(2) Correct `docs/reward-before-investment-rule.md`'s enforcement description, which is now
materially out of date. The "Detection mechanism (Phase 118-06)" section still describes a
single frontmatter-only surface, and the "Two scopes (Phase 245-02)" table still says two
when there are now three. Rewrite both to state the live truth:
- Two declaration sources: frontmatter for surfaces that have it, and
  `data/first-reward-surfaces.json` for surfaces that do not.
- Three CLI modes: full audit (default), `--staged` (the commit gate), and `--surfaces` (the
  registry audit), each with its scope and its consumer.
- One commit gate (`scripts/hooks/pre-commit-room-minto-guard.sh`'s `mva-rule-linter` arm,
  still staged-scoped) and two release gates (the surfaces gate and this plan's full-audit
  gate).
- A short "Why both scopes exist" paragraph: a staged-scoped gate cannot measure a repo-wide
  gap, which is why 67 of 113 commands went undeclared for three months and why plan 271-03
  stalled. Cite `271-03-SUMMARY.md` and DEFERRED-271-D1.
Keep the existing rule text, the qualifying tests and the `## Vocabulary amendments` section
intact; this is a correction to the enforcement description, not a rewrite of the rule.

House rules: hyphens only, no em-dashes, no emoji.
  </action>

  <verify>
    <automated>bash -n scripts/verify-release && echo SYNTAX_OK</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/verify-release','utf8');const a=require('node:assert/strict');a.ok(s.includes('--surfaces'),'surfaces gate missing');a.ok((s.match(/check-reward-before-investment\.cjs/g)||[]).length>=2,'full-audit gate not added alongside the surfaces gate');a.equal(s.indexOf('—'),-1,'em-dash');console.log('BOTH_REWARD_GATES_WIRED')"</automated>
    <automated>node -e "const fs=require('fs'),os=require('os'),path=require('path'),cp=require('child_process');const d=fs.mkdtempSync(path.join(os.tmpdir(),'rbi-ab-'));const c=path.join(d,'commands');fs.mkdirSync(c);for(const f of fs.readdirSync('commands').filter(x=>x.endsWith('.md')))fs.copyFileSync(path.join('commands',f),path.join(c,f));const victim=fs.readdirSync(c)[0];const t=fs.readFileSync(path.join(c,victim),'utf8');fs.writeFileSync(path.join(c,victim),t.split('\n').filter(l=>!/^interactive_first_reward:/.test(l)).join('\n'));const bad=cp.spawnSync('node',['scripts/check-reward-before-investment.cjs',c],{encoding:'utf8'});const good=cp.spawnSync('node',['scripts/check-reward-before-investment.cjs','commands'],{encoding:'utf8'});if(bad.status===0){console.error('A/B FAILED: stripped fixture passed');process.exit(1)}if(good.status!==0){console.error('A/B FAILED: real tree does not pass');console.error(good.stderr);process.exit(1)}console.log('AB_PROOF_OK stripped='+bad.status+' real='+good.status)"</automated>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/reward-before-investment-rule.md','utf8');const a=require('node:assert/strict');a.ok(d.includes('--surfaces'),'rule doc does not document the --surfaces mode');a.ok(d.includes('first-reward-surfaces.json'),'rule doc does not name the registry');a.ok(/271-03|DEFERRED-271-D1/.test(d),'rule doc does not cite the debt-ratchet evidence');a.equal(d.indexOf('—'),-1,'em-dash');console.log('RULE_DOC_CORRECTED')"</automated>
    <automated>git diff -- scripts/verify-release | grep -E '^-' | grep -v '^---' | grep -icE 'anchor|surfaces|mirror' | grep -qx 0 && echo NO_EXISTING_GATE_REMOVED</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/verify-release` exits 0 and the file invokes `check-reward-before-investment.cjs` at least twice (surfaces gate plus full-audit gate).
    - The A/B proof passes: a `commands/` copy with one declaration stripped exits 1, and the real `commands/` exits 0. Both outputs recorded in the summary.
    - `docs/reward-before-investment-rule.md` documents three CLI modes, two declaration sources, one commit gate and two release gates, and cites `271-03-SUMMARY.md` or DEFERRED-271-D1 for why both scopes exist.
    - The `verify-release` diff removes zero lines belonging to the anchoring, surfaces or mirror gates.
    - Zero em-dash characters in either file.
  </acceptance_criteria>

  <done>The whole tree is gated at release time, the gate was proven to fire against a stripped fixture, and the rule doc describes the enforcement surface that actually exists.</done>
</task>

<task type="auto">
  <name>Task 2: Register the requirements, update the ROADMAP and CHANGELOG, and file the knowledge-base lesson</name>

  <read_first>
    - `267.3-DECISIONS.md`: the ten GUARD-NN statements as ruled.
    - `.planning/REQUIREMENTS.md` around line 360: the "Phase 270 - Memory and Context Operator MCP (minted at plan time...)" block. Copy that block's exact shape: a heading naming the phase and the minting date, a short paragraph explaining the ROADMAP read TBD and where the IDs were minted, then `- [x] **ID**: statement` rows.
    - `.planning/ROADMAP.md` Phase 267.3 section: the `**Requirements**: TBD` line, the `**Plans:** 0 plans` line, and the `- [ ] TBD` plan placeholder.
    - `CHANGELOG.md` top entries: the format and the current unreleased or top version heading.
    - `.planning/debug/knowledge-base.md`: the summary-block format used when a defect resolves.
    - `docs/RCA-TEMPLATE.md` only if the knowledge-base format is unclear from the file itself.
  </read_first>

  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, CHANGELOG.md, .planning/debug/knowledge-base.md</files>

  <action>
(1) Register GUARD-01 through GUARD-10 in `.planning/REQUIREMENTS.md` following the Phase 270
block's shape exactly. Each row states the requirement and, where a number settles it, the
measured result (for example the final compliant count derived from disk, the anchoring gate
exit code, the surfaces count). Mark a row `[x]` only if it is genuinely satisfied; a row
that is not gets `[ ]` with a one-line statement of what remains.

(2) Update the ROADMAP's Phase 267.3 section:
- Replace `**Requirements**: TBD` with the ten GUARD IDs.
- Replace `**Plans:** 0 plans` with the real count and status.
- Replace the `- [ ] TBD` placeholder with the eight-plan list, grouped by wave, each with a
  one-line objective and its `[x]` state.
- Add a phase-outcome paragraph recording, with measured numbers: the ruled D-A / D-B / D-C,
  the vocabulary terms added, the surfaces declared, the commands declared (17 plus 25 plus
  25 equals 67), the final whole-tree audit reading, and the fact that both new gates were
  proven against A/B fixtures.
- Add an explicit SCOPE paragraph stating what this phase did NOT absorb, so a later reader
  does not assume it: DEFERRED-271-D2 (`agents/larry-extended.md`'s simultaneous `hitl_shape`
  and `connector.excluded:true`, plus the 52 `skills/*/SKILL.md` advisory violations) is a
  Canon Part 11 SHAPE-declaration question, not a reward-declaration question, and remains
  OPEN without an owner. Phase 267.2 still owns repairing the FIRST_INSTALL Reward and
  Investment legs; this phase declared the contract and did not repair the flow.

(3) Add a CHANGELOG entry under the current top version heading, written for a contributor
rather than an end user, naming the new registry, the vocabulary amendment, the two new
release gates, and the 67 declarations.

(4) Add a `.planning/debug/knowledge-base.md` summary block for the debt-ratchet lesson: a
gate narrowed to staged files stops measuring the repo-wide gap it was built to close, so a
staged-scoped gate needs a whole-tree audit wired at the same time, not later. Cite Phase
118-06 (shipped with 6 declarations, no backfill), Phase 245-02 (narrowed to `--staged`),
plan 271-03 (stalled at 28 of 45), and this phase (whole-tree audit gated at release). This
is the transferable lesson, and it applies to any future gate in this repo, not just this one.

House rules: hyphens only, no em-dashes, no emoji.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const a=require('node:assert/strict');for(let i=1;i<=10;i++){const id='GUARD-'+String(i).padStart(2,'0');a.ok(r.includes(id),'REQUIREMENTS.md missing '+id)}a.equal(r.indexOf('—'),-1,'em-dash');console.log('ALL_10_GUARD_IDS_REGISTERED')"</automated>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/ROADMAP.md','utf8');const a=require('node:assert/strict');const i=r.indexOf('### Phase 267.3');const j=r.indexOf('### Phase 268',i);const sec=r.slice(i,j>0?j:undefined);a.ok(!/\*\*Requirements\*\*: TBD/.test(sec),'Requirements still TBD');a.ok(!/- \[ \] TBD/.test(sec),'plan placeholder still present');a.ok(sec.includes('GUARD-'),'GUARD ids not listed');a.ok(/DEFERRED-271-D2/.test(sec),'scope paragraph does not name what was NOT absorbed');for(const p of ['267.3-01','267.3-02','267.3-03','267.3-04','267.3-05','267.3-06','267.3-07','267.3-08'])a.ok(sec.includes(p),'plan row missing: '+p);a.equal(sec.indexOf('—'),-1,'em-dash');console.log('ROADMAP_SECTION_COMPLETE')"</automated>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('CHANGELOG.md','utf8');const a=require('node:assert/strict');a.ok(c.includes('first-reward-surfaces.json'),'CHANGELOG does not name the registry');a.ok(/interactive_first_reward/.test(c),'CHANGELOG does not name the field');console.log('CHANGELOG_OK')"</automated>
    <automated>grep -q '267.3' .planning/debug/knowledge-base.md && grep -q '245-02' .planning/debug/knowledge-base.md && echo KB_ENTRY_OK</automated>
  </verify>

  <acceptance_criteria>
    - All ten GUARD-NN IDs appear in `.planning/REQUIREMENTS.md` under a Phase 267.3 heading matching the Phase 270 block's shape, each with its statement and, where applicable, its measured result.
    - The ROADMAP's Phase 267.3 section has no `TBD` placeholders left, lists all eight plans grouped by wave, carries a measured phase-outcome paragraph, and carries an explicit scope paragraph naming DEFERRED-271-D2 and Phase 267.2's retained ownership.
    - `CHANGELOG.md` names the registry, the field, the vocabulary amendment and the two new gates.
    - `.planning/debug/knowledge-base.md` carries the debt-ratchet lesson citing Phases 118-06, 245-02, 271-03 and 267.3.
    - Zero em-dash characters in any touched file.
  </acceptance_criteria>

  <done>Anyone opening the ROADMAP, REQUIREMENTS, CHANGELOG or knowledge base sees what this phase ruled, what it measured, and what it deliberately did not absorb.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase gate roll-up, no-relaxation audit, and the Dev-Research compositing trail</name>

  <read_first>
    - `CLAUDE.md`, the "Dev-Research Compositing (Rethinking Room)" section: the mandate that a phase touching MindrianOS's own architecture files its reasoning trail in BOTH the phase directory and `~/MindrianRooms/rethinking-mindrianos/research/`, mirrored to `~/MindrianOS/research/`, cross-linked both directions.
    - `.planning/ROADMAP.md` Phase 270's `Dev-Research Compositing` paragraph: the record that `scripts/write-scope-check` blocked plan 270-12's room write because the registry's active room was `launchpad-02`, and that the correct response was to escalate, not to bypass the guard.
    - `267.3-AUDIT.md`, `267.3-DECISIONS.md`, `267.3-CLASSIFICATION.md`, and every `267.3-0N-SUMMARY.md` produced so far.
  </read_first>

  <what-built>
The full phase: a jurisdiction audit and ruling, a declaration registry for surfaces with no
frontmatter, a vocabulary amendment, declarations for the session-start injected-prose
branches, 67 human-ruled command declarations, Phase 271's held work committed with no
bypass, and two new fail-closed release gates each proven against an A/B fixture.
  </what-built>

  <how-to-verify>
Run the roll-up and read the numbers, not the prose.

1. `node scripts/check-reward-before-investment.cjs` should exit 0 with missing 0, invalid 0.
2. `node scripts/check-reward-before-investment.cjs --surfaces .` should exit 0.
3. `node scripts/check-plugin-path-anchoring.cjs --check` should exit 0.
4. `node scripts/build-skill-mirrors.cjs --check` should exit 0.
5. `bash tests/run-all-267.3.sh`, `bash tests/run-all-118.sh`, `bash tests/run-all-271.sh`,
   `bash tests/run-all-267.1.sh` should all exit 0.
6. `scripts/verify-release` should now be runnable without the Phase 271 block. Confirm it is
   no longer red for that reason.
7. The no-relaxation audit, which is the one that matters most:
   `git log --oneline <phase-start>..HEAD -- scripts/hooks/ scripts/verify-release scripts/check-plugin-path-anchoring.cjs lib/core/mva-rule-linter.cjs`
   Every commit listed should be an ADDITION (a new gate, a new function, a new vocabulary
   member). If any commit weakened a predicate, removed a check, or added an allowlist entry,
   that is a finding and this checkpoint should not be approved.
8. The room trail: confirm
   `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-reward-guard-jurisdiction/`
   exists with the trail written, that it is mirrored to `~/MindrianOS/research/`, and that
   the cross-links resolve in both directions.

Known blocker to expect on step 8: `scripts/write-scope-check` may refuse the room write if
the registry's active room is not `rethinking-mindrianos`. That is the room-context bleed
already on the WATCH list, not a misfiling. Do NOT bypass the guard. The unblock is one
navigator action, `/mos:rooms switch rethinking-mindrianos`, after which the staged file
copies into place unchanged, exactly as Phase 270 recorded.
  </how-to-verify>

  <action>
Write the compositing trail to
`~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-reward-guard-jurisdiction/`,
mirrored to `~/MindrianOS/research/`, covering: the governance finding (an enforcement
mechanism that could not see its most important surface, and the debt ratchet that hid the
rest), the ruled D-A / D-B / D-C with their rejected alternatives, the vocabulary-fit finding
that the original six terms could not describe 67 of 113 commands, the classification rubric
and its boundary rulings, and the measured before-and-after numbers. Cross-link back to this
phase directory, and add the room-to-repo half as a line in the ROADMAP's Phase 267.3 section.

If `scripts/write-scope-check` blocks the write, stage the file, report the block, and ask
the navigator for the room switch. Record the staged-not-landed state honestly in the
summary rather than reporting the trail as filed.

Then present the roll-up table: every gate, its command, and its real exit code, plus the
no-relaxation audit's commit list with a one-line verdict per commit.
  </action>

  <resume-signal>Type "approved" to close the phase, or name the failing gate or the weakened check you found.</resume-signal>

  <verify>
    <automated>node scripts/check-reward-before-investment.cjs && node scripts/check-reward-before-investment.cjs --surfaces . && node scripts/check-plugin-path-anchoring.cjs --check && node scripts/build-skill-mirrors.cjs --check && echo ALL_FOUR_GATES_GREEN</automated>
    <automated>bash tests/run-all-267.3.sh && bash tests/run-all-118.sh && bash tests/run-all-271.sh && bash tests/run-all-267.1.sh && echo ALL_FOUR_SUITES_GREEN</automated>
    <automated>node -e "const cp=require('child_process');const files=['scripts/hooks/','scripts/verify-release','scripts/check-plugin-path-anchoring.cjs','lib/core/mva-rule-linter.cjs'];const o=cp.execSync('git log --oneline -20 -- '+files.join(' '),{encoding:'utf8'});console.log('NO-RELAXATION AUDIT, review each line:');console.log(o)"</automated>
    <automated>node -e "const fs=require('fs'),os=require('os'),path=require('path');const p=path.join(os.homedir(),'MindrianRooms/rethinking-mindrianos/research/2026-08-27-reward-guard-jurisdiction');if(!fs.existsSync(p)){console.error('COMPOSITING TRAIL NOT LANDED: '+p);process.exit(1)}console.log('ROOM_TRAIL_LANDED',fs.readdirSync(p).join(', '))"</automated>
  </verify>

  <acceptance_criteria>
    - All four gates exit 0: full audit, surfaces audit, path anchoring, skill mirrors.
    - All four test suites exit 0: 267.3, 118, 271, 267.1.
    - The no-relaxation audit's commit list is reviewed line by line and every commit is an addition, never a weakening, a removal, or a new allowlist entry. State this verdict explicitly in the summary.
    - The room compositing trail exists at `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-reward-guard-jurisdiction/`, is mirrored to `~/MindrianOS/research/`, and both cross-link directions resolve. If `write-scope-check` blocked it, the summary says so plainly and names the pending navigator action instead of claiming the trail is filed.
    - The navigator has approved the phase close.
  </acceptance_criteria>

  <done>Every gate is green by work rather than by relaxation, the reasoning trail lives in both homes, and a human has signed off on the close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| green board -> permanent gate | Promoting a number to a release blocker makes any falsehood in it permanent |
| phase close -> future readers | The ROADMAP and REQUIREMENTS entries become the record later sessions plan against |
| repo -> room | The compositing write crosses a room-scope guard that is currently mis-bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.3-36 | Tampering | a decorative gate that never fires | mitigate | A/B fixture proof in Task 1: a stripped copy of `commands/` must exit 1 and the real tree exit 0, both outputs recorded, following the 271-05 precedent |
| T-267.3-37 | Elevation of privilege | weakening a gate somewhere in the phase to reach green | mitigate | Task 3's no-relaxation audit walks the git log for every gate and hook file and requires a per-commit verdict; GUARD-10 is registered as a requirement |
| T-267.3-38 | Repudiation | claiming a scope this phase did not cover | mitigate | The ROADMAP scope paragraph names DEFERRED-271-D2 and Phase 267.2's retained ownership explicitly |
| T-267.3-39 | Spoofing | reporting the compositing trail as filed when the guard blocked it | mitigate | Task 3 requires the staged-not-landed state to be stated plainly; the verify checks the room path exists on disk |
| T-267.3-40 | Elevation of privilege | bypassing `scripts/write-scope-check` to land the room trail | mitigate | Task 3 forbids the bypass and names the one navigator action that unblocks it, per the Phase 270 precedent |
| T-267.3-SC | Tampering | package installs | accept | Zero npm/pip/cargo installs anywhere in phase 267.3 |
</threat_model>

<verification>
- `node scripts/check-reward-before-investment.cjs` exits 0, missing 0, invalid 0.
- `node scripts/check-reward-before-investment.cjs --surfaces .` exits 0.
- `node scripts/check-plugin-path-anchoring.cjs --check` exits 0.
- `node scripts/build-skill-mirrors.cjs --check` exits 0.
- `bash -n scripts/verify-release` exits 0, and the A/B fixture proof passes both directions.
- `bash tests/run-all-267.3.sh`, `run-all-118.sh`, `run-all-271.sh`, `run-all-267.1.sh` all exit 0.
- `.planning/REQUIREMENTS.md` carries all ten GUARD IDs; the ROADMAP's Phase 267.3 section carries no TBD.
</verification>

<success_criteria>
- The whole-tree commands audit is a fail-closed release gate, proven to fire against a stripped fixture.
- The rule doc describes the enforcement surface that actually exists: two declaration sources, three CLI modes, one commit gate, two release gates.
- The ROADMAP, REQUIREMENTS, CHANGELOG and knowledge base all carry measured numbers and an explicit statement of what this phase did not absorb.
- The no-relaxation audit confirms every gate-touching commit in the phase was an addition.
- The reasoning trail lives in the repo and in the rethinking-mindrianos room, cross-linked both ways, or its blocked state is reported honestly.
</success_criteria>

<output>
Create `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-08-SUMMARY.md` when done.
</output>
