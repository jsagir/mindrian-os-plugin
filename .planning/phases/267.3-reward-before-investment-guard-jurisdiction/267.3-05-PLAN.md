---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 05
type: execute
wave: 4
depends_on: ["267.3-03", "267.3-04"]
files_modified:
  - commands/doctor.md
  - skills/doctor/SKILL.md
  - .planning/ROADMAP.md
  - .planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md
autonomous: true
requirements: [GUARD-06, GUARD-10]
canon_parts: ["Part 6", "Part 7"]

must_haves:
  truths:
    - "Phase 271's 16 held command files and their 16 mirrors are committed through the full pre-commit hook with no bypass."
    - "commands/doctor.md:262's bare references/ citation is anchored, so the anchoring gate reads zero violations."
    - "scripts/verify-release gate 10c passes, which means a release cut is no longer blocked by Phase 271."
    - "The ROADMAP and Phase 271's deferred-items register both reflect the closure, with the commit hashes recorded."
  artifacts:
    - path: "commands/doctor.md"
      provides: "The last unanchored references/ citation, anchored"
      contains: "${CLAUDE_PLUGIN_ROOT}/references/personality/voice-dna.md"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 271 row updated from CLOSED-PARTIAL with the real closing numbers"
      contains: "271-03"
  key_links:
    - from: "commands/doctor.md"
      to: "scripts/check-plugin-path-anchoring.cjs"
      via: "the anchored citation the gate greps for"
      pattern: "CLAUDE_PLUGIN_ROOT.*references/personality/voice-dna"
    - from: ".planning/ROADMAP.md"
      to: "the unblocking commits"
      via: "commit hashes recorded in the Phase 271 Wave 3 row"
      pattern: "271-03"
---

<objective>
Turn the red gate green by doing the work, not by adjusting the instrument.

Purpose: Phase 271 closed CLOSED-PARTIAL with 16 command files plus 16 regenerated mirrors
held uncommitted in a shared working tree and `scripts/verify-release` gate 10c red, blocking
every release cut. The block was the missing declarations, which plan 267.3-04 has now
ruled and applied. This plan lands the held work and closes the loop.

Output: the held anchoring edits committed with no hook bypass, `commands/doctor.md:262`
anchored, gate 10c green, and the paper trail updated in both the ROADMAP and Phase 271's
deferred-items register.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md
@scripts/check-plugin-path-anchoring.cjs
</context>

## Artifacts this plan produces

| Artifact | Kind | What it is |
|---|---|---|
| The unblocking commits | git history | Plan 271-03's 16 held command files plus their 16 mirrors plus this plan's `doctor.md` fix, landed through the full pre-commit hook with no `COMMIT_NO_VERIFY` |
| `commands/doctor.md:262` anchor | source fix | The one non-backticked bare citation a backtick-anchored sweep could not reach (DEVIATION-271-03-A) |
| Phase 271 closure record | roadmap and register | The ROADMAP Wave 3 row and `deferred-items.md` DEFERRED-271-D1, both updated with the real numbers and hashes |

<tasks>

<task type="auto">
  <name>Task 1: Anchor commands/doctor.md's bare citation and regenerate its mirror</name>

  <read_first>
    - `.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md`, section DEVIATION-271-03-A: the exact site, the exact prescribed fix shape (prefix insertion only, prose and absence of backticks left untouched), and the note that the gate's `ANCHOR_SHORT_RE` matches on the prefix and does not require backticks.
    - `commands/doctor.md` around line 262: the sentence `See references/personality/voice-dna.md for voice patterns.` Grep for `references/personality/voice-dna.md` rather than trusting the line number, since plan 267.3-04 inserted a frontmatter line and shifted the body.
    - `scripts/check-plugin-path-anchoring.cjs`: the `ANCHOR_SHORT_RE` pattern and the `--check` and `--report` modes.
    - Two already-anchored examples from plan 271-03's committed batch (`git show 598fdb7c -- commands/ | head -60`) to copy the exact anchor form.
  </read_first>

  <files>commands/doctor.md, skills/doctor/SKILL.md</files>

  <action>
Insert the `${CLAUDE_PLUGIN_ROOT}/` prefix immediately before
`references/personality/voice-dna.md` in `commands/doctor.md`. Insert the prefix and nothing
else: do not add backticks, do not reword the sentence, do not touch the surrounding
`per D-19` prose. This keeps the change a pure prefix insertion consistent with the other 93
sites plan 271-03 already landed, and DEVIATION-271-03-A already ruled this exact shape.

Then run `node scripts/build-skill-mirrors.cjs` once and confirm `skills/doctor/SKILL.md`
picks up both the anchor and the declaration plan 267.3-04 added.

Do not touch any other citation in `commands/doctor.md`. Do not re-run the anchoring sweep
over any other file. The 16 held files are already anchored and verified; this task adds the
one site that sweep could not reach.
  </action>

  <verify>
    <automated>grep -c 'CLAUDE_PLUGIN_ROOT}/references/personality/voice-dna.md' commands/doctor.md | grep -qx 1 && echo DOCTOR_ANCHORED</automated>
    <automated>grep -v '^#' commands/doctor.md | grep -cE '(^|[^/{])references/personality/voice-dna\.md' | grep -qx 0 && echo NO_BARE_CITATION_LEFT</automated>
    <automated>node scripts/build-skill-mirrors.cjs --check</automated>
    <automated>node -e "const cp=require('child_process');const o=cp.execSync('git diff -U0 -- commands/doctor.md',{encoding:'utf8'});const lines=o.split('\n').filter(l=>/^[+-]/.test(l)&&!/^[+-][+-][+-]/.test(l));const off=lines.filter(l=>!l.includes('voice-dna')&&!l.includes('interactive_first_reward')&&!/^\+\s*#/.test(l));if(off.length){console.error('UNEXPECTED doctor.md CHANGES:');off.forEach(l=>console.error('  '+l));process.exit(1)}console.log('DOCTOR_DIFF_CONFINED',lines.length,'lines')"</automated>
    <automated>node scripts/check-plugin-path-anchoring.cjs --check && echo ANCHORING_GATE_GREEN</automated>
  </verify>

  <acceptance_criteria>
    - `commands/doctor.md` contains exactly one `${CLAUDE_PLUGIN_ROOT}/references/personality/voice-dna.md` and zero remaining bare occurrences of that path outside comment lines.
    - The `commands/doctor.md` diff contains only the anchor line, the declaration line from plan 267.3-04, and its provenance comment.
    - `node scripts/build-skill-mirrors.cjs --check` exits 0.
    - `node scripts/check-plugin-path-anchoring.cjs --check` exits 0 in the working tree: violations reads 0.
  </acceptance_criteria>

  <done>The last unanchored citation in the repo is anchored, and the anchoring gate is green in the working tree for the first time since Phase 271 opened.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the held Phase 271 work through the full pre-commit hook, no bypass</name>

  <read_first>
    - `.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md`, the "Execution state of plan 271-03 at the point of escalation" table: the pre-sweep HEAD, the committed batch hashes, and the exact resume instruction ("stage-and-commit, plus the one-line `doctor.md` prefix insertion and one final `build-skill-mirrors.cjs`").
    - `scripts/hooks/pre-commit-room-minto-guard.sh` lines 285-315: the `mva-rule-linter` arm this commit must now pass rather than bypass.
    - `.planning/ROADMAP.md` Phase 271 Wave 3 row: the 16 named files.
    - `CLAUDE.md` "WORKSPACE GUARD": every commit runs from `/home/jsagi/dev/MindrianOS-Plugin/`, never the plugin install cache.
  </read_first>

  <files>(no source edits; this task stages and commits work produced by plans 267.3-04 and 267.3-05 Task 1)</files>

  <action>
Confirm the workspace first: `pwd` must be `/home/jsagi/dev/MindrianOS-Plugin`. This is a
shared working tree with concurrent sessions, so re-measure before staging rather than
trusting the plan's numbers.

Run `node scripts/check-reward-before-investment.cjs --staged .` against a dry stage first if
you want a preview, then stage the 17 command files, their regenerated mirrors, and nothing
else. Do NOT stage `.planning/` files in this commit; the paper-trail update is Task 3.

Commit with a conventional message naming both phases, since the diff carries Phase 271's
anchoring and this phase's declarations:
`fix(267.3): declare interactive_first_reward for 17 commands and land Phase 271-03's held anchoring`

Hard constraints, in order of importance:
- `COMMIT_NO_VERIFY` must be UNSET. The whole point of this plan is that the hook now passes
  on its own. If the hook refuses, STOP and report why; do not bypass, do not allowlist, do
  not edit the hook. A refusal means a declaration is wrong or missing, which is information,
  not an obstacle.
- If the hook's `mva-rule-linter` arm reports any file as missing or invalid, fix the
  declaration (re-reading `267.3-CLASSIFICATION.md`'s ruling section) rather than removing
  the file from the commit.
- Record the resulting commit hash or hashes; Task 3 writes them into the ROADMAP.

After committing, re-run the gates and record their real output:
`node scripts/check-plugin-path-anchoring.cjs --check`, `bash tests/run-all-271.sh`,
`node scripts/check-reward-before-investment.cjs`, and `bash tests/run-all-267.3.sh`.
  </action>

  <verify>
    <automated>test "$(pwd)" = "/home/jsagi/dev/MindrianOS-Plugin" && echo WORKSPACE_OK</automated>
    <automated>git log -1 --format='%H %s' | grep -qi '267.3' && echo COMMIT_LANDED</automated>
    <automated>git diff --name-only -- commands/ skills/ | wc -l | grep -qx 0 && echo NOTHING_HELD_ANYMORE</automated>
    <automated>node scripts/check-plugin-path-anchoring.cjs --check && echo ANCHORING_GREEN_AT_HEAD</automated>
    <automated>bash tests/run-all-271.sh</automated>
    <automated>node scripts/check-reward-before-investment.cjs 2>&1 | grep -E '^  (compliant|missing|invalid):'</automated>
    <automated>git log -1 --format=%B | grep -qi 'no-verify\|COMMIT_NO_VERIFY' && (echo "BYPASS REFERENCED IN COMMIT MESSAGE"; exit 1) || echo NO_BYPASS_RECORDED</automated>
  </verify>

  <acceptance_criteria>
    - The commit exists and was made from `/home/jsagi/dev/MindrianOS-Plugin`.
    - `COMMIT_NO_VERIFY` was not set for it; the full pre-commit hook ran and passed. State this explicitly in the summary with the hook's own output.
    - `git diff --name-only -- commands/ skills/` returns zero files: nothing is held in the working tree anymore.
    - `node scripts/check-plugin-path-anchoring.cjs --check` exits 0 at HEAD.
    - `bash tests/run-all-271.sh` exits 0 with all four arms passing (it read PASS=3 FAIL=1 before this plan; the failing arm was the anchoring gate).
    - The full reward audit's invalid count is 0 and its missing count is exactly 17 lower than `267.3-AUDIT.md` section 1 recorded.
    - No gate file was edited: `git diff --name-only HEAD~1 -- scripts/check-plugin-path-anchoring.cjs scripts/hooks/ scripts/verify-release` is empty.
  </acceptance_criteria>

  <done>Phase 271's blocked work is in history, the anchoring gate is green at HEAD, and the commit went through the same hook that refused it before, unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Close the paper trail in the ROADMAP and Phase 271's deferred register</name>

  <read_first>
    - `.planning/ROADMAP.md` Phase 271 section: the `**Plans:**` status line reading CLOSED-PARTIAL, the Wave 3 row marked `[~]`, and the Wave 5 row's sentence that `verify-release` reads 31 passed / 1 failed.
    - `.planning/phases/271-.../deferred-items.md`: DEFERRED-271-D1 (status OPEN, BLOCKING), DEVIATION-271-03-A, and the execution-state table.
    - The real gate output captured in Task 2, so every number written here is measured rather than predicted.
  </read_first>

  <files>.planning/ROADMAP.md, .planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md</files>

  <action>
Update the ROADMAP's Phase 271 entry:
- Change the Wave 3 row from `[~]` to `[x]` and replace its PARTIAL numbers with the real
  final ones (45 of 45 files, 94 of 94 sites), naming the unblocking commit hash from Task 2
  and citing Phase 267.3 as the phase that ruled the declaration question.
- Update the `**Plans:**` status line from CLOSED-PARTIAL to the real closing state, keeping
  the honest history rather than deleting it: state that it closed partial on 2026-08-27 and
  was completed by Phase 267.3 on the date of this commit.
- Update the Wave 5 row's `verify-release` sentence with the re-measured passed/failed
  counts from Task 2's real run. Do not carry the stale 31 passed / 1 failed forward.
- Update Phase 267.3's own `**Blocks:**` line to record that the block is cleared.

Update `deferred-items.md`:
- Mark DEFERRED-271-D1 RESOLVED, naming Phase 267.3 plans 04 and 05, the commit hash, and
  the fact that the fix was 17 human-ruled declarations rather than a hook bypass or an
  allowlist. Keep the original text; append the resolution, do not rewrite the history.
- Mark DEVIATION-271-03-A applied, naming the same commit.
- Leave DEFERRED-271-D2 (`agents/larry-extended.md`'s simultaneous `hitl_shape` and
  `connector.excluded:true`) OPEN, and update its "natural home" sentence: this phase ruled
  the reward-declaration jurisdiction, not the Part 11 shape-declaration jurisdiction, so
  D-2 needs its own owner. Record that explicitly rather than letting a reader infer this
  phase absorbed it.

House rules: hyphens only, no em-dashes, no emoji. Every number written here comes from
Task 2's captured output.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/ROADMAP.md','utf8');const a=require('node:assert/strict');a.ok(!/\[~\] 271-03-PLAN\.md/.test(r),'Wave 3 row still marked partial');a.ok(r.includes('267.3'),'ROADMAP does not cite the unblocking phase');a.equal(r.indexOf('—'),-1,'em-dash in ROADMAP');console.log('ROADMAP_UPDATED')"</automated>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md','utf8');const a=require('node:assert/strict');a.ok(/DEFERRED-271-D1[\s\S]{0,4000}RESOLVED/.test(d),'D1 not marked resolved');a.ok(/DEFERRED-271-D2[\s\S]*OPEN/.test(d),'D2 must stay OPEN');a.ok(d.includes('267.3'),'resolution does not name the resolving phase');a.equal(d.indexOf('—'),-1,'em-dash found');console.log('DEFERRED_REGISTER_UPDATED')"</automated>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md','utf8');const a=require('node:assert/strict');a.ok(/DEVIATION-271-03-A[\s\S]{0,3000}(applied|APPLIED)/.test(d),'DEVIATION-271-03-A not marked applied');console.log('DEVIATION_MARKED_APPLIED')"</automated>
    <automated>gsd-tools query commit "docs(267.3): close Phase 271's blocked wave and update the deferred register" --files .planning/ROADMAP.md .planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md</automated>
  </verify>

  <acceptance_criteria>
    - The ROADMAP's Phase 271 Wave 3 row is `[x]` with the real final counts and the unblocking commit hash.
    - The Phase 271 `**Plans:**` line records both the original CLOSED-PARTIAL and the completion by Phase 267.3, with dates.
    - The Wave 5 `verify-release` sentence carries the re-measured passed/failed counts, not the stale 31/1.
    - Phase 267.3's `**Blocks:**` line records the block as cleared.
    - `deferred-items.md` marks DEFERRED-271-D1 RESOLVED naming plans 267.3-04 and 267.3-05 and the commit hash, marks DEVIATION-271-03-A applied, and leaves DEFERRED-271-D2 OPEN with an explicit note that this phase did not absorb it.
    - Original text is appended to, not rewritten: the pre-existing DEFERRED-271-D1 body is still present verbatim.
    - Zero em-dash characters in either file.
  </acceptance_criteria>

  <done>Anyone reading the ROADMAP or the deferred register sees the true closing numbers, the resolving commit, and an honest statement of what stayed open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-commit hook -> git history | The hook is the gate that refused this exact commit once; bypassing it defeats the phase |
| shared working tree -> staging | Concurrent sessions may add or move files between measurement and staging |
| recorded numbers -> future readers | The ROADMAP is read as fact by later planning sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.3-21 | Elevation of privilege | `COMMIT_NO_VERIFY` bypass | mitigate | Task 2 forbids it, the acceptance criteria require the hook's own passing output, and a verify asserts the commit message references no bypass |
| T-267.3-22 | Tampering | relaxing the anchoring gate or the hook to force green | mitigate | Acceptance criterion asserts `git diff HEAD~1` touches no gate or hook file; GUARD-10 covers the phase |
| T-267.3-23 | Tampering | staging unrelated concurrent-session changes | mitigate | Task 2 stages an explicit file list, excludes `.planning/`, and re-measures the working tree before staging |
| T-267.3-24 | Repudiation | stale numbers carried forward into the ROADMAP | mitigate | Every number written in Task 3 comes from Task 2's captured real output; the stale 31/1 is named explicitly as the thing not to carry forward |
| T-267.3-25 | Spoofing | implying this phase closed DEFERRED-271-D2 | mitigate | Task 3 requires D2 to stay OPEN with an explicit note that the Part 11 shape jurisdiction is a different question |
| T-267.3-SC | Tampering | package installs | accept | Zero npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- `node scripts/check-plugin-path-anchoring.cjs --check` exits 0 at HEAD.
- `bash tests/run-all-271.sh` exits 0.
- `bash tests/run-all-267.3.sh` and `bash tests/run-all-118.sh` exit 0.
- `node scripts/build-skill-mirrors.cjs --check` exits 0.
- `git diff --name-only -- commands/ skills/` is empty.
- `git diff --name-only HEAD~2 -- scripts/hooks/ scripts/check-plugin-path-anchoring.cjs` is empty: no gate was touched to achieve any of this.
</verification>

<success_criteria>
- Phase 271's held work is committed, with the pre-commit hook passing unmodified and unbypassed.
- The anchoring gate reads zero violations at HEAD, so a release is no longer blocked by Phase 271.
- The ROADMAP and the deferred register both carry measured closing numbers, the resolving commit, and an honest statement of what remains open.
</success_criteria>

<output>
Create `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-05-SUMMARY.md` when done.
</output>
