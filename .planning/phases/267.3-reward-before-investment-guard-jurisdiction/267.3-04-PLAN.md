---
phase: 267.3-reward-before-investment-guard-jurisdiction
plan: 04
type: execute
wave: 3
depends_on: ["267.3-02"]
files_modified:
  - .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md
  - commands/analyze-systems.md
  - commands/causal.md
  - commands/challenge-assumptions.md
  - commands/compare-ventures.md
  - commands/deck.md
  - commands/diagnose.md
  - commands/doctor.md
  - commands/find-connections.md
  - commands/leadership.md
  - commands/lean-canvas.md
  - commands/mullins.md
  - commands/pipeline.md
  - commands/publish.md
  - commands/score-innovation.md
  - commands/show.md
  - commands/suggest-next.md
  - commands/systems-thinking.md
  - skills/analyze-systems/SKILL.md
  - skills/causal/SKILL.md
  - skills/challenge-assumptions/SKILL.md
  - skills/compare-ventures/SKILL.md
  - skills/deck/SKILL.md
  - skills/diagnose/SKILL.md
  - skills/doctor/SKILL.md
  - skills/find-connections/SKILL.md
  - skills/leadership/SKILL.md
  - skills/lean-canvas/SKILL.md
  - skills/mullins/SKILL.md
  - skills/pipeline/SKILL.md
  - skills/publish/SKILL.md
  - skills/score-innovation/SKILL.md
  - skills/show/SKILL.md
  - skills/suggest-next/SKILL.md
  - skills/systems-thinking/SKILL.md
autonomous: false
requirements: [GUARD-05, GUARD-07, GUARD-02]
canon_parts: ["Part 7", "Part 11", "Part 12"]

must_haves:
  truths:
    - "Each of the 17 commands blocking Phase 271 carries an interactive_first_reward value chosen from its own first-reward moment, with that moment cited by line."
    - "A rubric exists that a later plan can apply to the remaining 50 commands without re-deriving the reasoning."
    - "A human reviewed the 17 classifications before any frontmatter was written."
    - "No frontmatter key other than interactive_first_reward was added, removed, or reordered in any of the 17 files."
    - "The 30 held path-anchoring edits in these files survive the frontmatter change untouched."
  artifacts:
    - path: ".planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md"
      provides: "The rubric plus one reasoned row per command with its cited first-reward moment"
      min_lines: 80
      contains: "analyze-systems"
  key_links:
    - from: "267.3-CLASSIFICATION.md"
      to: "commands/*.md"
      via: "each row cites the command file and the line where its first reward is delivered"
      pattern: "commands/[a-z-]+\\.md:"
    - from: "commands/*.md"
      to: "lib/core/mva-rule-linter.cjs"
      via: "interactive_first_reward frontmatter key validated against REWARD_TYPES"
      pattern: "interactive_first_reward"
---

<objective>
Declare an honest `interactive_first_reward` for the 17 commands that are blocking Phase 271,
and leave behind the rubric that makes the remaining 50 tractable.

Purpose: Phase 271 has 16 command files plus 16 regenerated skill mirrors sitting
uncommitted in a shared working tree, and `scripts/verify-release` gate 10c is red, so no
release can be cut. The block is entirely these missing declarations. Plan 271-03 correctly
refused to guess the values inside a mechanical anchoring sweep, per the linter's own binding
decision B5. This plan does the judgment work that refusal deferred.

Output: `267.3-CLASSIFICATION.md` (the rubric plus 17 reasoned rows), a human ruling on the
17, and the frontmatter declarations themselves. Committing Phase 271's held work is plan
267.3-05, deliberately not this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-AUDIT.md
@.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-DECISIONS.md
@docs/reward-before-investment-rule.md
@lib/core/mva-rule-linter.cjs
@commands/onboard.md
@.planning/phases/271-bare-reference-path-resolution-audit-45-of-113-commands-cite/deferred-items.md
</context>

## Artifacts this plan produces

| Artifact | Kind | What it is |
|---|---|---|
| `267.3-CLASSIFICATION.md` | phase doc | The classification rubric (signals in, vocabulary term out, with the tie-break rules) plus one row per classified command carrying its cited first-reward moment and the ruled value |
| 17 `interactive_first_reward` declarations | frontmatter | One honest declaration per blocked command file |
| 17 regenerated skill mirrors | generated | `skills/*/SKILL.md` twins carrying the same declaration, via `build-skill-mirrors.cjs` |

<tasks>

<task type="auto">
  <name>Task 1: Build the rubric and classify the 17 blocked commands</name>

  <read_first>
    - `267.3-DECISIONS.md` D-B: the exact vocabulary tokens now available, including the two new ones.
    - `267.3-AUDIT.md` section 3 (why the original six could not cover these classes) and section 4 (the list of 17 blocked files).
    - `docs/reward-before-investment-rule.md`: "What counts as first variable reward" (the four qualifying tests: unpredictable, intelligible, valuable, uniquely attributable), the explicit non-rewards, and "The scripting override".
    - The four calibration examples, read as WORKED EXAMPLES of the judgment being asked for, not as templates to copy: `commands/onboard.md` frontmatter (`reframe_question`, with the honest inline "Remediation tracked as follow-up phase" comment at line 12), `commands/find-bottlenecks.md` (`schema_preview`), `commands/new-project.md` (`instant_brief`), `commands/grade.md` (`calibration_distribution_preview`). Each is genuinely tailored to its command; note WHY each fits before classifying anything.
    - Each of the 17 command files, read far enough to find where the command first delivers something back to the user: `analyze-systems`, `causal`, `challenge-assumptions`, `compare-ventures`, `deck`, `diagnose`, `doctor`, `find-connections`, `leadership`, `lean-canvas`, `mullins`, `pipeline`, `publish`, `score-innovation`, `show`, `suggest-next`, `systems-thinking`. Read the frontmatter in full plus the body down to the first user-visible output; you do not need the whole body of a 270-line file to find its first reward.
  </read_first>

  <files>.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md</files>

  <action>
Write `267.3-CLASSIFICATION.md` in two parts.

**Part 1, the rubric.** Name the signals that go in and the vocabulary term that comes out,
with explicit tie-break rules. The available signals, all readable without guessing:
`hitl_shape` (an `F.0` decision-close or `F.1` single-fork surface is interactive by
declaration, so `--none (scripting only)` is provably wrong for it, which is exactly the
error a first-pass guess made on `publish` during 271-03); `body_shape` (a `methodology`
body is a Larry-narrated reasoning flow, an `E (Action Report)` body is a report);
`serves_jtbd` and `teaching` (what the command is for, in the author's own words); whether
the body shells out to a script or narrates; and the first user-visible output in the body.

State the four qualifying tests from the rule doc as the gate every candidate value must
pass, and state the two disqualifiers the rule doc names outright: a grade is not a reward,
and a `/mos:status`-style report is not a reward.

Write the tie-break rules explicitly, at minimum these three:
- `--none (scripting only)` requires a real `--no-interactive` / `--script` / `-q` invocation
  path in the command body. If the body has no such flag, this term is FORBIDDEN regardless
  of how script-heavy the command looks.
- A command with `hitl_shape: F.0` or `F.1` reaches a genuine human fork, so it is
  interactive by its own declaration and cannot take a scripting opt-out.
- When two terms both seem to fit, prefer the one whose minting flow (per the rule doc's
  "Application to existing flows") is closest in shape, and record the runner-up and why it
  lost. A row with no recorded runner-up when two plausibly fit is an incomplete row.

**Part 2, the 17 rows.** One row per command, as a table plus a short reasoning paragraph
each. Every row carries: command name, `hitl_shape`, `body_shape`, the `path:line` where the
command first delivers something to the user, one sentence describing that first delivery in
plain language, the proposed value, the runner-up value if any and why it lost, and a
confidence marker of HIGH or NEEDS-RULING.

Mark a row NEEDS-RULING rather than forcing a value when the command genuinely does not fit.
An honest NEEDS-RULING row is worth more than a confident wrong one: Task 2 exists to rule
exactly these. Do NOT invent a vocabulary term here; the vocabulary was closed by D-B and a
new term would be a second canon amendment, not a classification.

Write NO frontmatter in this task. Task 3 applies the values after the human ruling.

House rules: hyphens only, no em-dashes, no emoji, Feynman-simplified prose.
  </action>

  <verify>
    <automated>test -f .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md && test "$(wc -l < .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md)" -ge 80 && echo CLASSIFICATION_EXISTS</automated>
    <automated>for c in analyze-systems causal challenge-assumptions compare-ventures deck diagnose doctor find-connections leadership lean-canvas mullins pipeline publish score-innovation show suggest-next systems-thinking; do grep -q "$c" .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md || { echo "MISSING ROW: $c"; exit 1; }; done; echo ALL_17_ROWS_PRESENT</automated>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const a=require('node:assert/strict');a.equal(t.indexOf('—'),-1,'em-dash found');const cites=(t.match(/commands\/[a-z-]+\.md:\d+/g)||[]);a.ok(cites.length>=17,'expected at least 17 path:line first-reward citations, found '+cites.length);a.ok(/HIGH|NEEDS-RULING/.test(t),'no confidence markers');console.log('CITATIONS_OK',cites.length)"</automated>
    <automated>node -e "const fs=require('fs');const {REWARD_TYPES}=require('./lib/core/mva-rule-linter.cjs');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const used=[...REWARD_TYPES].filter(v=>t.includes(v));console.log('vocabulary terms used in classification:',used.length,used.join(' | '))"</automated>
    <automated>git diff --name-only -- commands/ | wc -l | grep -qx 16 && echo HELD_16_UNCHANGED || (echo "commands/ diff moved during classification"; exit 1)</automated>
  </verify>

  <acceptance_criteria>
    - `267.3-CLASSIFICATION.md` exists, is at least 80 lines, and contains a Part 1 rubric and a Part 2 table.
    - Part 1 states the four qualifying tests, the two named disqualifiers, and at least the three tie-break rules named in the action, including the rule that `--none (scripting only)` is forbidden without a real scripting flag in the body.
    - All 17 command names appear.
    - At least 17 `commands/<name>.md:<line>` citations appear, one per row, pointing at the first user-visible delivery.
    - Every row carries a proposed value drawn only from the live `REWARD_TYPES`, plus a HIGH or NEEDS-RULING marker.
    - Zero em-dash characters.
    - `git diff --name-only -- commands/` still lists exactly 16 files: this task wrote no frontmatter.
  </acceptance_criteria>

  <done>Every blocked command has a proposed declaration traceable to a specific line of its own body, and the reasoning is reusable for the remaining 50.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator review of the 17 classifications before any frontmatter is written</name>

  <read_first>
    - `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md` (produced by Task 1, both parts)
  </read_first>

  <what-built>
A rubric and 17 proposed `interactive_first_reward` values, each traced to the line where
that command first delivers something to the user, with runner-up values and their rejection
reasons recorded. Zero frontmatter has been written yet. Nothing is committed.
  </what-built>

  <how-to-verify>
1. Open `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md`.
2. Read Part 1's rubric. Confirm the tie-break rules match your intent, especially the rule
   that `--none (scripting only)` is forbidden for any command with no real
   `--no-interactive` / `--script` / `-q` path. This is the rule that would have caught the
   271-03 `publish` error.
3. Scan Part 2's table. For each row, the question is only this: does the cited line actually
   describe that command's first reward, and does the proposed term describe it honestly?
4. Pay closest attention to two groups. First, every row marked NEEDS-RULING: these are the
   ones the rubric could not settle and they need your call. Second, `publish`, `doctor` and
   `pipeline`: `publish` is `hitl_shape: F.0` and was already misclassified once;
   `doctor` and `pipeline` are the two whose class is most argued.
5. Spot-check two or three HIGH rows against the actual command file to confirm the cited
   line says what the row claims.

The output you are approving becomes 17 permanent frontmatter declarations, and a wrong
declaration is worse than a missing one: it makes the guard report green on a false claim.
  </how-to-verify>

  <action>
Present the classification table and the NEEDS-RULING rows. Record the navigator's ruling
back into `267.3-CLASSIFICATION.md` as a `## Navigator ruling` section: the date, the final
value for every NEEDS-RULING row, any HIGH row the navigator overrode along with the reason,
and an explicit line stating the final 17 values as approved. Task 3 reads its values from
that section, not from the Part 2 proposals, so an overridden row cannot leak through.
  </action>

  <resume-signal>Type "approved" to accept all 17 as proposed, or name the rows to change and their correct values.</resume-signal>

  <verify>
    <automated>grep -q '## Navigator ruling' .planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md && echo RULING_RECORDED</automated>
    <automated>node -e "const fs=require('fs');const {REWARD_TYPES}=require('./lib/core/mva-rule-linter.cjs');const t=fs.readFileSync('.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-CLASSIFICATION.md','utf8');const sec=t.slice(t.indexOf('## Navigator ruling'));const a=require('node:assert/strict');for(const c of ['analyze-systems','causal','challenge-assumptions','compare-ventures','deck','diagnose','doctor','find-connections','leadership','lean-canvas','mullins','pipeline','publish','score-innovation','show','suggest-next','systems-thinking'])a.ok(sec.includes(c),'ruling section missing final value for '+c);a.ok([...REWARD_TYPES].some(v=>sec.includes(v)),'ruling names no vocabulary term');console.log('RULING_COVERS_ALL_17')"</automated>
    <automated>git diff --name-only -- commands/ skills/ | wc -l | grep -qx 32 && echo NOTHING_WRITTEN_YET</automated>
  </verify>

  <acceptance_criteria>
    - `267.3-CLASSIFICATION.md` contains a `## Navigator ruling` section dated, naming a final value for all 17 commands.
    - Every NEEDS-RULING row has a ruled value in that section.
    - Any override of a HIGH row is recorded with its reason.
    - `git diff --name-only -- commands/ skills/` still lists exactly 32 files (the 16 held commands plus their 16 held mirrors): this checkpoint wrote no frontmatter.
  </acceptance_criteria>

  <done>A human has ruled on all 17 values, the ruling is written down, and Task 3 has an unambiguous list to apply.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the 17 declarations and regenerate the mirrors</name>

  <read_first>
    - `267.3-CLASSIFICATION.md` `## Navigator ruling` section: the approved value per command. Use ONLY this section, never the Part 2 proposals.
    - `commands/onboard.md` lines 1-20: the exact frontmatter placement and comment style for the key, including the precedent of an inline provenance comment above it.
    - `scripts/build-skill-mirrors.cjs` header and its `SKIP_LIST` around line 175: which command files do and do not get a mirror, and the `--check` mode's contract.
  </read_first>

  <files>commands/analyze-systems.md, commands/causal.md, commands/challenge-assumptions.md, commands/compare-ventures.md, commands/deck.md, commands/diagnose.md, commands/doctor.md, commands/find-connections.md, commands/leadership.md, commands/lean-canvas.md, commands/mullins.md, commands/pipeline.md, commands/publish.md, commands/score-innovation.md, commands/show.md, commands/suggest-next.md, commands/systems-thinking.md, skills/*/SKILL.md (regenerated mirrors)</files>

  <action>
For each of the 17 command files, insert exactly one frontmatter line,
`interactive_first_reward: <ruled value>`, plus one comment line directly above it in the
`commands/onboard.md:12` style recording the provenance: the phase and plan that declared it
and a short pointer to the cited first-reward moment. Place the key adjacent to the other
declaration-family keys (`hitl_shape`, `hitl_why`) so a reader finds the declarations
together.

Hard constraints on the diff:
- Add nothing else. Do not reorder, reformat, quote, unquote, or normalize any existing
  frontmatter key. The 16 of these files already carry uncommitted path-anchoring edits from
  plan 271-03 held in the working tree, and those 30 body-line changes must survive
  byte-identically. Verify this, do not assume it.
- Quote the value only if the ruled token requires it. A token beginning with `--` must be
  quoted in YAML; the plain identifier tokens must not be, matching how the 46 already
  compliant commands spell theirs. Check an existing compliant file and match it exactly, and
  confirm the linter's own `parseFrontmatter` (which strips surrounding quotes at line 103)
  accepts your spelling by running the audit.
- `commands/doctor.md` gets its declaration here but NOT its path-anchoring fix. The bare
  `references/personality/voice-dna.md` citation at line 262 is plan 267.3-05's work, kept
  separate so the two concerns stay in separate diffs.

Then run `node scripts/build-skill-mirrors.cjs` once and confirm the regenerated
`skills/*/SKILL.md` twins carry the new key (the mirrors do propagate this field: an existing
compliant pair such as `commands/grade.md` and `skills/grade/SKILL.md` both carry it). Run
`node scripts/build-skill-mirrors.cjs --check` afterwards and require a clean report.

Commit nothing in this plan. Staging and committing, including the 16 held anchoring edits,
is plan 267.3-05's job and must go through the full pre-commit hook with no bypass.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const {parseFrontmatter,REWARD_TYPES}=require('./lib/core/mva-rule-linter.cjs');const a=require('node:assert/strict');const L=['analyze-systems','causal','challenge-assumptions','compare-ventures','deck','diagnose','doctor','find-connections','leadership','lean-canvas','mullins','pipeline','publish','score-innovation','show','suggest-next','systems-thinking'];for(const c of L){const fm=parseFrontmatter(fs.readFileSync('commands/'+c+'.md','utf8'));a.ok(fm,'no frontmatter: '+c);a.ok(fm.interactive_first_reward,'no declaration: '+c);a.ok(REWARD_TYPES.has(String(fm.interactive_first_reward).trim()),c+' declares invalid value: '+fm.interactive_first_reward)}console.log('ALL_17_DECLARED_AND_VALID')"</automated>
    <automated>node -e "const cp=require('child_process');const L=['analyze-systems','causal','challenge-assumptions','compare-ventures','deck','diagnose','find-connections','leadership','lean-canvas','mullins','pipeline','publish','score-innovation','show','suggest-next','systems-thinking'];let bad=0;for(const c of L){const o=cp.execSync('git diff -U0 -- commands/'+c+'.md',{encoding:'utf8'});const lines=o.split('\n').filter(l=>/^[+-]/.test(l)&&!/^[+-][+-][+-]/.test(l));const nonRef=lines.filter(l=>!l.includes('references/')&&!l.includes('interactive_first_reward')&&!/^\+\s*#/.test(l));if(nonRef.length){console.error('UNEXPECTED CHANGE in '+c+':');nonRef.forEach(l=>console.error('   '+l));bad++}}if(bad){process.exit(1)}console.log('DIFFS_CONFINED_TO_ANCHORING_PLUS_DECLARATION')"</automated>
    <automated>node scripts/build-skill-mirrors.cjs --check</automated>
    <automated>node -e "const {parseFrontmatter}=require('./lib/core/mva-rule-linter.cjs');const fs=require('fs');const a=require('node:assert/strict');let n=0;for(const c of ['analyze-systems','deck','publish','show','systems-thinking']){const p='skills/'+c+'/SKILL.md';if(!fs.existsSync(p))continue;const fm=parseFrontmatter(fs.readFileSync(p,'utf8'));a.ok(fm&&fm.interactive_first_reward,'mirror missing declaration: '+p);n++}a.ok(n>=4,'too few mirrors checked');console.log('MIRRORS_CARRY_DECLARATION',n)"</automated>
    <automated>node scripts/check-reward-before-investment.cjs 2>&1 | grep -E '^  (compliant|missing|invalid):'</automated>
    <automated>node scripts/check-plugin-path-anchoring.cjs --check; echo "anchoring exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - All 17 command files parse with an `interactive_first_reward` whose value is a member of the live `REWARD_TYPES`.
    - Each of the 17 declarations equals the value recorded in `267.3-CLASSIFICATION.md`'s `## Navigator ruling` section.
    - For each of the 16 held files, the git diff contains only lines that are path-anchoring changes, the one declaration line, or the one provenance comment line: the second verify command exits 0.
    - `node scripts/build-skill-mirrors.cjs --check` exits 0 with a clean report.
    - At least 4 sampled `skills/*/SKILL.md` mirrors carry the new declaration.
    - The full audit's missing count has dropped by exactly 17 from the number recorded in `267.3-AUDIT.md` section 1, and its invalid count is still 0.
    - Nothing is committed: `git log -1 --format=%H` is unchanged from the start of this task.
  </acceptance_criteria>

  <done>The 17 commands that blocked Phase 271 carry human-ruled, vocabulary-valid declarations, their mirrors agree, and the held anchoring edits are provably intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classification -> permanent frontmatter | A judgment written into 17 shipped files that later readers will treat as settled |
| shared working tree -> this plan's edits | 16 files carry another plan's uncommitted work; a careless rewrite destroys it |
| mirror generator -> skills tree | One `build-skill-mirrors.cjs` run rewrites many files at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-267.3-16 | Spoofing | a false declaration greening the guard on an untrue claim | mitigate | Rubric with explicit disqualifiers, a `path:line` citation per row, and a blocking human ruling before any frontmatter is written; the `publish` precedent is named as the worked failure |
| T-267.3-17 | Tampering | destroying plan 271-03's held anchoring edits | mitigate | Per-file diff assertion that every changed line is either a `references/` anchoring line, the declaration line, or its comment; run before and after the mirror regeneration |
| T-267.3-18 | Tampering | frontmatter reformatting churn | mitigate | Same per-file diff assertion; the action forbids reordering, requoting or normalizing any existing key |
| T-267.3-19 | Elevation of privilege | inventing a vocabulary term at command level | mitigate | Task 1 forbids minting terms; the verify asserts every value is a member of the live `REWARD_TYPES`, which only D-B can extend |
| T-267.3-20 | Repudiation | an overridden classification silently reverting to the proposal | mitigate | Task 3 reads values only from the `## Navigator ruling` section, and the verify compares applied values against it |
| T-267.3-SC | Tampering | package installs | accept | Zero npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- `node scripts/check-reward-before-investment.cjs` missing count dropped by exactly 17; invalid still 0.
- `node scripts/build-skill-mirrors.cjs --check` exits 0.
- `node scripts/check-plugin-path-anchoring.cjs --check` reports the SAME violation count as before this plan (expected 1 in the working tree). This plan touches no citation.
- `bash tests/run-all-267.3.sh` and `bash tests/run-all-118.sh` both exit 0.
- `git log -1 --format=%H` unchanged: this plan commits nothing.
</verification>

<success_criteria>
- 17 human-ruled, vocabulary-valid declarations exist in frontmatter, each traceable to a cited line of its own command body.
- `267.3-CLASSIFICATION.md` carries a rubric a later plan can apply to the remaining 50 without re-deriving the reasoning.
- Plan 271-03's 30 held anchoring edits survive byte-identically.
- Nothing is committed, and no gate was relaxed.
</success_criteria>

<output>
Create `.planning/phases/267.3-reward-before-investment-guard-jurisdiction/267.3-04-SUMMARY.md` when done.
</output>
