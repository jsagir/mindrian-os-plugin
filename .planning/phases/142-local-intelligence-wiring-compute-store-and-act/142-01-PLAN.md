---
phase: 142-local-intelligence-wiring-compute-store-and-act
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-142.sh
  - tests/test-cascade-surface-loop-fires.cjs
  - tests/test-spine-navigates-decide.cjs
  - tests/test-brain-md-tier-rise.cjs
  - tests/test-derivation-drain-fires.cjs
  - tests/test-post-compact-nav04-closure.cjs
  - tests/test-fileval-readback-surface.cjs
  - tests/test-decide-part8-invariant.cjs
  - tests/fixtures/room-142-fixture.cjs
autonomous: true
canon_parts: [Part 2, Part 4, Part 8, Part 9]
requirements: [CASC-01, CASC-02, NAV-02, NAV-03, NAV-04, FILEVAL-03]
must_haves:
  truths:
    - "tests/run-all-142.sh runs to completion (no crash) and exits non-zero while suites are RED"
    - "Every Phase 142 requirement has exactly one named loop-fires acceptance suite registered in the aggregator"
    - "The aggregator mirrors tests/run-all-141.sh structure so Phase 146 can compose it"
  artifacts:
    - path: "tests/run-all-142.sh"
      provides: "Phase 142 scoped test aggregator (mirrors run-all-141.sh)"
      contains: "CJS_SUITES"
    - path: "tests/fixtures/room-142-fixture.cjs"
      provides: "Caller-owned fixture room.db builder for the spine-navigates + tier-rise suites"
      exports: ["buildFixtureDb"]
    - path: "tests/test-decide-part8-invariant.cjs"
      provides: "Part-8 5-tripwire forbidden-substring sweep over the CASC-02 touched files"
  key_links:
    - from: "tests/run-all-142.sh"
      to: "the 7 CJS suites"
      via: "CJS_SUITES array"
      pattern: "test-.*\\.cjs"
---

<objective>
Wave 0 test scaffold for Phase 142. Create the `tests/run-all-142.sh` aggregator and one RED loop-fires acceptance suite per requirement, plus a caller-owned fixture room.db and a Part-8 invariant sweep. This is the Nyquist floor: every requirement gets an executable test that ASSERTS THE LOOP FIRES before any implementation/verification plan lands.

Purpose: Phase 142 is a VERIFY-AND-CLOSE phase (4 of 5 requirements shipped by prior phases; CASC-02 the one genuine build). The deliverable that LOCKS shipped work is an acceptance test, not a re-implementation. These suites are deliberately structured so the Phase 146 loop-fires gate (ACPT-01..05) can compose them without rework.

Output: `tests/run-all-142.sh` + 7 CJS suites (all RED now) + `tests/fixtures/room-142-fixture.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-CONTEXT.md

<interfaces>
<!-- The aggregator pattern to mirror. Executor must copy this structure exactly (per-suite header ownership comment, SHELL_SUITES/CJS_SUITES arrays, run-to-completion loop, non-zero exit on any failure). -->

From tests/run-all-141.sh (the pattern to mirror):
- Header comment block maps each suite -> the requirement ID it owns.
- `SHELL_SUITES=()` (empty here) and `CJS_SUITES=( ... )` arrays.
- Loop runs each suite, prints `>>> name: PASSED|FAILED|MISSING`, tracks FAILED_TESTS, exits 1 if any failed.
- "MUST run to completion (no crash) even when any suite fails."
- bash only. No emoji. No em-dashes (hyphens only).

From tests/fixtures/room-141-fixture.cjs (the fixture pattern to mirror):
```
module.exports = { buildFixtureDb, applySchema };
```
- Builds an in-memory / tmp node:sqlite db with the Phase 109 nodes-provenance schema: nodes (id, type, properties, source_path, created_by, confidence, review_status, created_at, last_seen_at, source_section), edges (source, target, type, properties), sessions, fragments.
- Caller owns the handle (never a real room.db).
- Seeds a section node + claim nodes + a CONTRADICTS edge so getNeighborhood ranking populates.

From tests/test-room-context-part8-invariant.cjs (the Part-8 sweep pattern to mirror):
- Reads the TARGET source file, asserts ZERO forbidden requires (packet, brain-client) and ZERO forbidden projection tokens (projectText, shortText, hashText, safeNodeProjection, resolvePrivacyMode, PRIVACY_MODES) and ZERO sha256/createHash call sites.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the room-142 fixture + the aggregator skeleton</name>
  <files>tests/fixtures/room-142-fixture.cjs, tests/run-all-142.sh</files>
  <read_first>
    - tests/fixtures/room-141-fixture.cjs (the fixture pattern to clone: schema + seed shape + buildFixtureDb/applySchema export)
    - tests/run-all-141.sh (the aggregator pattern to clone: header ownership map, CJS_SUITES, run-to-completion loop, non-zero exit)
  </read_first>
  <action>
    Create `tests/fixtures/room-142-fixture.cjs` by cloning `room-141-fixture.cjs`: same Phase-109 nodes-provenance schema (nodes/edges/sessions/fragments), export `{ buildFixtureDb, applySchema }`. Seed it richer than 141 so the spine-navigates + tier-rise suites have signal: one `room:` root node, one `section` node, at least two `claim` nodes joined by a CONTRADICTS edge (so getNeighborhood ranks a real neighborhood), and at least two `fragments` rows in one session (so the getRoomContext Leg-B seed resolves). The fixture db handle is caller-owned -- the fixture NEVER opens a real room.db and NEVER imports room-db.cjs.

    Create `tests/run-all-142.sh` mirroring `run-all-141.sh` exactly: same `set -uo pipefail`, SCRIPT_DIR resolution, empty SHELL_SUITES, and a CJS_SUITES array listing all 7 suites by filename (test-cascade-surface-loop-fires.cjs, test-spine-navigates-decide.cjs, test-brain-md-tier-rise.cjs, test-derivation-drain-fires.cjs, test-post-compact-nav04-closure.cjs, test-fileval-readback-surface.cjs, test-decide-part8-invariant.cjs). Reproduce the header ownership comment mapping each suite to its requirement ID (CASC-01, CASC-02, NAV-02, NAV-03, NAV-04, FILEVAL-03, plus the Part-8 gate spanning CASC-02). Reproduce the run-to-completion loop and the non-zero exit on any failure. bash only, hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-142.sh; test $? -ne 0 && echo "RED-as-expected (suites not yet present/passing)"; grep -c 'test-.*\.cjs' tests/run-all-142.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const f=require('./tests/fixtures/room-142-fixture.cjs'); if(typeof f.buildFixtureDb!=='function') process.exit(1)"` exits 0 (buildFixtureDb is exported and callable).
    - `bash tests/run-all-142.sh` runs to completion without a bash crash and exits non-zero (suites MISSING/RED), printing a per-suite line for each of the 7 suites.
    - `grep -cE 'test-(cascade-surface-loop-fires|spine-navigates-decide|brain-md-tier-rise|derivation-drain-fires|post-compact-nav04-closure|fileval-readback-surface|decide-part8-invariant)\.cjs' tests/run-all-142.sh` returns 7.
  </acceptance_criteria>
  <done>The fixture builder is exported and the aggregator lists all 7 suites and runs to completion (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Write the 7 RED loop-fires acceptance suites (one per requirement + Part-8 gate)</name>
  <files>tests/test-cascade-surface-loop-fires.cjs, tests/test-spine-navigates-decide.cjs, tests/test-brain-md-tier-rise.cjs, tests/test-derivation-drain-fires.cjs, tests/test-post-compact-nav04-closure.cjs, tests/test-fileval-readback-surface.cjs, tests/test-decide-part8-invariant.cjs</files>
  <read_first>
    - tests/test-cascade-surface-e2e.cjs (the shipped CASC-01 side-channel e2e pattern; the loop-fires suite asserts findings SURFACE, not just that the file exists)
    - tests/test-fileval-readback.cjs (the FILEVAL-02 readback contract that FILEVAL-03 surfaces)
    - tests/test-room-context-part8-invariant.cjs (the forbidden-substring + forbidden-require sweep to clone for test-decide-part8-invariant.cjs)
    - lib/core/navigation-engine.cjs (decide() trace fields: brain_md_tier_mode, the decision_trace struct the spine + tier suites assert against)
    - lib/core/brain-derivation-queue.cjs (enqueue + drain signatures: enqueue(roomDir, section, previous_hash, new_hash, reason); async drain(roomDir, {maxEntries, dryRun}) -> {processed, dispatched, ...})
  </read_first>
  <action>
    Write each suite as a RED IIFE harness (node assert/strict). Each suite asserts THE LOOP FIRES, not merely that code exists. Each suite that needs a graph uses `buildFixtureDb` from room-142-fixture.cjs (caller-owned). Name and structure them so Phase 146 ACPT-* can compose them.

    - `test-cascade-surface-loop-fires.cjs` (CASC-01): fire `scripts/post-write` against a tmp-room fixture artifact (set MINDRIAN_ROOMS_HOME to the tmpdir per the e2e pattern), then assert the room-proactive surfacing CONTRACT: `<roomDir>/.mindrian/last-cascade.json` exists, parses, and carries a non-empty `proactive_intelligence.newFindings` path that the SKILL.md trigger keys off (the `^post-write: cascade complete` / `^queued MINTO regen` advisory prefix is present on the envelope). Assert findings would surface (newFindings non-empty -> renderable). Do NOT touch the renderer.

    - `test-spine-navigates-decide.cjs` (CASC-02): build the fixture db, call decide(turn, context) where context carries the getRoomContext-derived neighborhood leg (the field Plan 02 adds). Assert the decision_trace reflects the navigated neighborhood (a trace field populated from getNeighborhood ranking, NOT just stored-edge presence). HARD: assert `routing_source` is UNCHANGED (still legacy) -- CASC-02 navigates but does NOT flip routing_source (that is Phase 144). RED now (the wiring field does not yet exist).

    - `test-brain-md-tier-rise.cjs` (NAV-02): on a fixture/tmp section, assert that with BRAIN.md ABSENT decide() emits `brain_md_tier_mode === 'tier_0'`, and after a BRAIN.md is present (mode_a/mode_b quadruple), `brain_md_tier_mode` rises above tier_0. Prove the rise is observable in the trace.

    - `test-derivation-drain-fires.cjs` (NAV-03): enqueue an entry via brain-derivation-queue.enqueue, then call drain(roomDir, {dryRun:true}) and assert the entry is DISPATCHED within the drain (result.dispatched non-empty for the matching-hash entry) -- proving the queue drains within a session rather than sitting.

    - `test-post-compact-nav04-closure.cjs` (NAV-04): assert by reference + presence -- `scripts/restore-post-compact-context.cjs` exists and is wired in hooks/hooks.json SessionStart, and `.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md` status is `passed`. This suite LOCKS the close-by-reference (Plan 04 cites it); it is the regression fence that NAV-04 stays wired.

    - `test-fileval-readback-surface.cjs` (FILEVAL-03): using the fixture db + navigation.fileEvidenceWithReadback, assert that a write that did NOT land returns `{ ok:false, reason:'filing_did_not_land' }` (the honesty signal Larry must surface, not swallow) and a write that landed returns ok:true with the readback fields. Plan 04 wires the surfacing; this suite is its contract.

    - `test-decide-part8-invariant.cjs` (Part-8 gate over CASC-02): clone the room-context Part-8 sweep. TARGET set = the files CASC-02 touches (lib/core/navigation-engine.cjs, lib/core/navigation-engine-shared.cjs, scripts/intent-classifier.cjs CASC-02 region). Assert ZERO new forbidden requires (no packet/brain-client egress added by the wiring) and ZERO forbidden projection tokens introduced near the getRoomContext wiring. This is the Phase 90 5-tripwire pattern as a plan gate.

    Every suite: hyphens only, no em-dashes, no emoji. Each must FAIL now (RED) and pass once Plans 02-04 land.
  </action>
  <verify>
    <automated>for s in cascade-surface-loop-fires spine-navigates-decide brain-md-tier-rise derivation-drain-fires post-compact-nav04-closure fileval-readback-surface decide-part8-invariant; do node tests/test-$s.cjs >/dev/null 2>&1; echo "$s exit=$?"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 7 suite files exist and are syntactically valid: `for s in tests/test-cascade-surface-loop-fires.cjs tests/test-spine-navigates-decide.cjs tests/test-brain-md-tier-rise.cjs tests/test-derivation-drain-fires.cjs tests/test-post-compact-nav04-closure.cjs tests/test-fileval-readback-surface.cjs tests/test-decide-part8-invariant.cjs; do node --check "$s" || exit 1; done` exits 0.
    - `bash tests/run-all-142.sh` runs to completion and exits non-zero; the summary lists the RED suites (Nyquist floor established; no suite reports MISSING).
    - The generated suites contain no em-dash (U+2014): `python3 -c "import glob,sys; sys.exit(1 if any(chr(0x2014) in open(f,encoding='utf-8').read() for f in glob.glob('tests/run-all-142.sh')+glob.glob('tests/test-*loop-fires.cjs')+glob.glob('tests/test-spine-navigates-decide.cjs')) else 0)"` exits 0 (hyphens only).
  </acceptance_criteria>
  <done>All 7 suites exist, are valid CJS, are RED, and are registered in the aggregator which runs to completion.</done>
</task>

</tasks>

<verification>
- `bash tests/run-all-142.sh` runs to completion (no bash crash) and exits non-zero while suites are RED.
- Each of CASC-01, CASC-02, NAV-02, NAV-03, NAV-04, FILEVAL-03 has exactly one named suite in CJS_SUITES; plus the Part-8 gate suite.
- The fixture exports buildFixtureDb and never opens a real room.db.
- No em-dashes anywhere (hyphens only).
</verification>

<success_criteria>
The Nyquist floor is established: every Phase 142 requirement has an executable loop-fires acceptance suite (RED), the aggregator mirrors run-all-141.sh and runs to completion, and the fixture + Part-8 sweep are ready for Plans 02-04 to turn GREEN.
</success_criteria>

<output>
Create `.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-01-SUMMARY.md` when done.
</output>
