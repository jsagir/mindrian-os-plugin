---
phase: 142-local-intelligence-wiring-compute-store-and-act
plan: 04
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - tests/test-brain-md-tier-rise.cjs
  - tests/test-post-compact-nav04-closure.cjs
  - tests/test-fileval-readback-surface.cjs
autonomous: true
canon_parts: [Part 2, Part 8, Part 9]
requirements: [NAV-02, NAV-04, FILEVAL-03]
must_haves:
  truths:
    - "BRAIN.md derivation lifts a room's sections out of tier_0 -- with BRAIN.md present, decide() emits brain_md_tier_mode above tier_0 (NAV-02, proven end-to-end on a real section)"
    - "Post-compact re-injection survives an auto-compact boundary -- NAV-04 is closed by reference to Phase 95.5 (status: passed, 5/5) and locked by a regression fence asserting the real two-hop wiring path (coordinator loads the consumer; hooks.json registers the coordinator on the compact matcher)"
    - "A readback that did not land is SURFACED, not swallowed -- FILEVAL-03 honesty rule proven over the shipped fileEvidenceWithReadback"
    - "A readback that DID land is surfaceable to the navigator -- FILEVAL-03 REMIND half proven: an ok:true result carries non-empty, human-readable round-trip readback fields (the recall the navigator would be shown), not computed-then-discarded"
    - "All BRAIN.md derivation stays Part-8-clean via buildBrainQueryContext (hash + enum only); NO new Brain query surface is added"
  artifacts:
    - path: "tests/test-brain-md-tier-rise.cjs"
      provides: "NAV-02 loop-fires acceptance: BRAIN.md present -> tier_mode rises above tier_0 in a decision trace"
      contains: "tier_0"
    - path: "tests/test-post-compact-nav04-closure.cjs"
      provides: "NAV-04 close-by-reference regression fence asserting the two-hop path (coordinator-loads-consumer + hooks.json compact-matcher registers coordinator) and citing 95.5-VERIFICATION.md"
      contains: "sessionstart-coordinator"
    - path: "tests/test-fileval-readback-surface.cjs"
      provides: "FILEVAL-03 acceptance over fileEvidenceWithReadback: honesty rule (filing_did_not_land surfaced) AND remind half (ok:true carries non-empty human-readable readback fields)"
      contains: "filing_did_not_land"
  key_links:
    - from: "lib/core/folder-memory.cjs readQuadruple"
      to: "lib/core/navigation-engine.cjs decide() brain_md_tier_mode"
      via: "shared.resolveTierMode"
      pattern: "resolveTierMode"
    - from: "lib/core/brain-derivation.cjs deriveSection"
      to: "buildBrainQueryContext"
      via: "the DOWN-lane chokepoint (hash + enum only)"
      pattern: "buildBrainQueryContext"
    - from: "scripts/sessionstart-coordinator.cjs"
      to: "scripts/restore-post-compact-context.cjs"
      via: "the post-compact event require keyed in the coordinator dispatch table"
      pattern: "restore-post-compact-context"
---

<objective>
VERIFY-AND-CLOSE NAV-02, NAV-04, and FILEVAL-03.

- **NAV-02 (VERIFY + close any auto-fire gap):** The consumption side is shipped -- decide() reads readQuadruple and sets brain_md_tier_mode via resolveTierMode (tier_0 when BRAIN.md absent, higher when present). This plan proves end-to-end on a real section that derivation FIRES -> BRAIN.md is written -> tier_mode rises above tier_0 in a decision trace. If derivation does not auto-fire for an un-derived section, close that ONE wiring gap.
- **NAV-04 (close-by-reference):** Phase 95.5 shipped the post-compact re-injection consumer (95.5-VERIFICATION.md status: passed, 5/5). This plan confirms 95.5 covers the NAV-04 wording and locks it with a regression fence that asserts the REAL two-hop wiring path (restore-post-compact-context.cjs is loaded by sessionstart-coordinator.cjs, and hooks.json registers the coordinator on the SessionStart compact matcher) -- NOT a naive grep for the consumer in hooks.json, which false-fails because the consumer is never named directly in hooks.json. No new build.
- **FILEVAL-03 (verify/thin-wire):** The shipped fileEvidenceWithReadback already returns `{ ok:false, reason:'filing_did_not_land' }` on a failed write. This plan proves BOTH halves of FILEVAL-03: (a) the honesty rule (a filing that did not land is SURFACED, not swallowed) AND (b) the REMIND half (after a successful filing, the readback the navigator would be shown is actively surfaceable -- ok:true with non-empty, human-readable round-trip fields, not computed-then-discarded). It thin-wires the surfacing only if the test proves a half is not already surfaced.

Purpose: lock the shipped local-loop memory substrate with loop-fires acceptance tests composable by the Phase 146 gate. All derivation stays Part-8-clean (buildBrainQueryContext: hash + enum + section slug only); add NO new Brain query surface.

Output: three suites GREEN; NAV-04 closed by reference via the two-hop fence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-CONTEXT.md
@.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md
@.planning/research/v1.13.1-larryreach-fanout/raw-slices/SLICE-C.md

<interfaces>
<!-- Shipped substrate. Verify against it; do not add a new Brain query surface. -->

NAV-02 consumption (SHIPPED):
- lib/core/folder-memory.cjs `readQuadruple(sectionPath)` -- the only allowed BRAIN.md read path; returns the triple + brain field.
- lib/core/navigation-engine-shared.cjs `resolveTierMode(quadruple, brainAvailable)` -> 'mode_a' | 'mode_b' | 'tier_0'. tier_0 when brain null/parse_failed/unavailable; mode_a when brain present + reachable; mode_b when brain_offline-exempt.
- lib/core/navigation-engine.cjs decide() sets trace.brain_md_tier_mode from resolveTierMode (absent quadruple -> 'tier_0').

NAV-02 derivation (DOWN lane, SHIPPED, Part-8-clean):
- lib/core/brain-derivation.cjs `async deriveSection(roomPath, section, options)` -- writes the section BRAIN.md; gated on brainClient.isAvailable(); options.dry_run short-circuits with would_query count and no write.
- lib/core/brain-derivation.cjs `buildBrainQueryContext(triple, sectionSlug)` -- the DOWN-lane chokepoint that strips the local triple to sha256 hashes + enum scalars + section slug. NO user prose crosses. This is the ONLY Brain-adjacent surface; do NOT add another.

NAV-04 (SHIPPED by Phase 95.5 -- the real two-hop wiring path):
- scripts/restore-post-compact-context.cjs (~274 lines) reads `<roomDir>/.mindrian/last-post-compact.md`, re-injects TRIPLE_CONTEXT as hookSpecificOutput.additionalContext at session-start. It is NOT named directly in hooks.json.
- scripts/sessionstart-coordinator.cjs is the script REGISTERED in hooks.json on the SessionStart `startup|clear|compact` matcher. Its event-dispatch table loads the consumer via `require('./restore-post-compact-context.cjs').contribute` keyed on the post-compact event (coordinator line ~55: `'post-compact': () => { try { return require('./restore-post-compact-context.cjs').contribute; } ... }`).
- The wiring is therefore a TWO-HOP path: hooks.json -> sessionstart-coordinator.cjs -> restore-post-compact-context.cjs. A naive grep for `restore-post-compact-context.cjs` in hooks.json FALSE-FAILS even though wiring is functionally present (95.5-VERIFICATION.md: status passed).
- scripts/post-compact write_post_compact_side_channel() writes the stamped side-channel.
- 95.5-VERIFICATION.md: status passed, 5/5 truths verified, all artifacts WIRED.

FILEVAL-03 (substrate SHIPPED by FILEVAL-02):
- lib/core/navigation.cjs re-exports `fileEvidenceWithReadback(db, params)` (from lib/core/navigation/file-evidence-readback.cjs). It writes the EvidenceClaim via the LOCKED writeEvidenceClaim path, READS the row back, and returns `{ ok:false, reason:'filing_did_not_land' }` when the readback shows the row did not land (or fields did not round-trip), else `{ ok:true, node_id }` with the validated readback. artifact_path (D-10) and the 4 LOCKED provenance fields round-trip through properties JSON.
- HONESTY half: the `{ ok:false, reason:'filing_did_not_land' }` return is the signal Larry must SURFACE, not swallow. Already covered.
- REMIND half: the readback row IS read back and validated internally (the `landed` properties object), but the shipped success return is `{ ok:true, node_id }` -- the round-trip readback values that the navigator would be shown are NOT (yet) returned to the caller. The REMIND assertion proves they are surfaceable; if the test proves they are computed-then-discarded, thin-wire the already-computed `landed` round-trip values into the existing ok:true return (no new surface).
- Pattern to mirror: tests/test-fileval-readback.cjs (the FILEVAL-02 fixture-first contract).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the NAV-02 tier-rise suite GREEN (BRAIN.md present -> tier_mode rises above tier_0)</name>
  <files>tests/test-brain-md-tier-rise.cjs</files>
  <read_first>
    - lib/core/navigation-engine-shared.cjs (resolveTierMode rules: tier_0 vs mode_a vs mode_b)
    - lib/core/navigation-engine.cjs (decide(): how trace.brain_md_tier_mode is set from the quadruple; the absent-quadruple -> tier_0 path)
    - lib/core/folder-memory.cjs (readQuadruple -- the BRAIN.md read path; the brain field shape resolveTierMode consumes)
    - lib/core/brain-derivation.cjs (deriveSection dry_run path + buildBrainQueryContext -- proof the DOWN lane is the only Brain surface)
  </read_first>
  <action>
    Complete `test-brain-md-tier-rise.cjs` to prove NAV-02 fires. On a tmp section: (1) with NO BRAIN.md present, call decide() with the section's quadruple and assert `trace.brain_md_tier_mode === 'tier_0'` (the dumb-mode baseline the SLICE-C evidence flags); (2) write a BRAIN.md for the section (either by running deriveSection against a fixture/mock-available Brain, OR by composing a valid brain quadruple field directly so readQuadruple resolves a present, fresh brain), then call decide() again and assert `brain_md_tier_mode` rises ABOVE tier_0 (mode_a when brainAvailable, mode_b when brain_offline-exempt). The assertion is on the decision TRACE, the observation surface named in the CONTEXT. Additionally assert the Part-8 cleanliness of the derivation surface: a grep over lib/core/brain-derivation.cjs confirms deriveSection's only Brain-context builder is buildBrainQueryContext (no new query surface). If derivation does NOT auto-fire for an un-derived section in the live session path (the one possible NAV-02 gap), surface it in the SUMMARY as the single wiring gap to close; otherwise the consumption proof stands and no production code changes.
  </action>
  <verify>
    <automated>node tests/test-brain-md-tier-rise.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-brain-md-tier-rise.cjs` exits 0 (GREEN): tier_0 with BRAIN.md absent, and a tier above tier_0 with BRAIN.md present, observed in the decision trace.
    - `grep -c "buildBrainQueryContext" lib/core/brain-derivation.cjs` returns >= 1 AND `grep -cE "brain_query|brainQuery|new Brain query surface" lib/core/brain-derivation.cjs` shows no second Brain-context builder introduced (the DOWN-lane chokepoint remains the only Brain-adjacent surface).
    - `git diff --name-only` shows no production-code change unless the SUMMARY documents a proven auto-fire gap requiring the one wiring fix.
  </acceptance_criteria>
  <done>The NAV-02 suite proves BRAIN.md derivation lifts a section out of tier_0 in a decision trace, with the derivation staying Part-8-clean and no new Brain query surface.</done>
</task>

<task type="auto">
  <name>Task 2: Close NAV-04 by reference + lock with a two-hop regression fence</name>
  <files>tests/test-post-compact-nav04-closure.cjs</files>
  <read_first>
    - .planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md (status passed, 5/5; the artifact + key-link table proving the consumer is WIRED)
    - scripts/sessionstart-coordinator.cjs (the post-compact event-dispatch entry that loads the consumer via `require('./restore-post-compact-context.cjs').contribute`)
    - hooks/hooks.json (the SessionStart entry that registers sessionstart-coordinator.cjs -- matcher startup|clear|compact -- NOT the consumer directly)
  </read_first>
  <action>
    Complete `test-post-compact-nav04-closure.cjs` as the NAV-04 close-by-reference regression fence asserting the REAL two-hop wiring path. The consumer is NOT named directly in hooks.json -- it is loaded by the SessionStart coordinator, which IS the registered script. A naive grep for `restore-post-compact-context.cjs` in hooks.json would FALSE-FAIL even though wiring is functionally present (95.5-VERIFICATION.md status: passed). So assert the two-hop path: (1) `scripts/restore-post-compact-context.cjs` exists; (2) `scripts/sessionstart-coordinator.cjs` loads it -- a grep for `restore-post-compact-context` in the coordinator returns a hit (the post-compact require entry); (3) `hooks/hooks.json` registers `sessionstart-coordinator.cjs` in a SessionStart entry whose matcher includes `compact` (e.g. startup|clear|compact) -- the auto-compact boundary NAV-04 targets; (4) `.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md` has frontmatter `status: passed`. (Optionally also assert `scripts/post-compact` writes the stamped side-channel, the up-lane writer.) This LOCKS NAV-04 via the actual two-hop chain: hooks.json -> coordinator -> consumer. Map the NAV-04 wording (post-compact re-injection consumer so memory does not degrade across an auto-compact boundary) onto the 95.5 verified truths and record the close-by-reference in the SUMMARY citing 95.5-VERIFICATION.md. No new build -- this is verification + a fence only.
  </action>
  <verify>
    <automated>node tests/test-post-compact-nav04-closure.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-post-compact-nav04-closure.cjs` exits 0 (GREEN): the two-hop path is asserted -- consumer exists; `scripts/sessionstart-coordinator.cjs` loads `restore-post-compact-context`; `hooks/hooks.json` registers `sessionstart-coordinator.cjs` on a SessionStart entry whose matcher includes `compact`; 95.5 status passed.
    - The test does NOT grep `hooks/hooks.json` for `restore-post-compact-context.cjs` directly (that would false-fail); it asserts the coordinator hop instead -- `grep -c "restore-post-compact-context" scripts/sessionstart-coordinator.cjs` returns >= 1, and `grep -c "sessionstart-coordinator" hooks/hooks.json` returns >= 1 with the enclosing matcher including `compact`.
    - `grep -c "status: passed" .planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md` returns 1 (the close-by-reference citation is valid).
    - `git diff --name-only` includes only the test file (no production change -- close-by-reference).
  </acceptance_criteria>
  <done>NAV-04 is closed by reference to Phase 95.5 and locked by a regression fence asserting the real two-hop wiring (hooks.json registers the coordinator on the compact matcher; the coordinator loads the post-compact consumer), not a naive direct-grep that would false-fail.</done>
</task>

<task type="auto">
  <name>Task 3: Turn the FILEVAL-03 suite GREEN (honesty: a non-landed filing is surfaced; remind: a landed filing's readback is surfaceable)</name>
  <files>tests/test-fileval-readback-surface.cjs</files>
  <read_first>
    - lib/core/navigation/file-evidence-readback.cjs (fileEvidenceWithReadback: the readback assertion + the { ok:false, reason:'filing_did_not_land' } return; the round-trip field checks; note the success path currently returns { ok:true, node_id } and the validated `landed` round-trip values are read but not returned)
    - tests/test-fileval-readback.cjs (the FILEVAL-02 fixture-first contract to mirror -- caller-owned fixture db, ok:false on failed write, provenance preservation)
    - tests/fixtures/room-142-fixture.cjs (the caller-owned fixture db builder)
  </read_first>
  <action>
    Complete `test-fileval-readback-surface.cjs` to prove BOTH halves of FILEVAL-03 over the SHIPPED fileEvidenceWithReadback. Using the caller-owned fixture db:

    HONESTY half (already covered by the substrate): a simulated failed write (the row does not land, or a locked provenance field does not round-trip) returns `{ ok:false, reason:'filing_did_not_land' }` -- the signal Larry must SURFACE, not swallow. Assert the failure is RETURNED as a structured result (surfaceable), never silently swallowed (no thrown-and-caught-to-null path that would hide it).

    REMIND half (the new positive-path assertion): a successful filing returns ok:true, AND the `readback` fields are populated with non-empty, human-readable round-trip values -- the same values that would be rendered to the navigator as recall of what was just filed (e.g. the round-tripped LOCKED provenance fields and artifact_path read back from the row). Assert these readback fields are non-empty and human-readable on the ok:true return, proving the REMIND half is actively surfaceable, not computed-then-silently-discarded. If the test proves the shipped success return surfaces only `{ ok:true, node_id }` and the validated round-trip values are computed but discarded, thin-wire the already-computed `landed` round-trip values into the existing ok:true return through the existing fileEvidenceWithReadback function (no new surface, no new Brain-adjacent path), and document it in the SUMMARY. This is the disciplined escape hatch: strengthen the surfacing ONLY if the test proves it is not already surfaced.
  </action>
  <verify>
    <automated>node tests/test-fileval-readback-surface.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-fileval-readback-surface.cjs` exits 0 (GREEN): HONESTY -- `{ ok:false, reason:'filing_did_not_land' }` on a non-landed write (surfaced, not swallowed); REMIND -- ok:true on a landed write WITH non-empty, human-readable round-trip readback fields (the recall the navigator would be shown).
    - The suite asserts the failure result is a returned structured value (surfaceable) -- `grep -c "filing_did_not_land" tests/test-fileval-readback-surface.cjs` returns >= 1 -- AND the positive path asserts non-empty, human-readable readback fields on the ok:true return (the REMIND proof).
    - `git diff --name-only` shows only the test file UNLESS the SUMMARY documents a proven not-surfaced gap requiring the disciplined thin-wire of the already-computed round-trip values through the existing fileEvidenceWithReadback return (no new surface).
  </acceptance_criteria>
  <done>The FILEVAL-03 suite proves both obligations over the shipped readback: a filing that did not land is surfaced as a structured honesty signal, and a filing that DID land carries non-empty human-readable readback fields the navigator can be reminded with -- with no new surface added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local triple -> BRAIN.md derivation -> Brain | The ONLY Brain-adjacent surface is the DOWN-lane buildBrainQueryContext (hash + enum + slug). No user prose crosses (Part 8). This plan adds NO new Brain query surface. |
| readback result -> Larry surface | The filing-did-not-land honesty signal AND the landed-readback remind values are LOCAL; surfaced to the navigator, never egressed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-08 | Information Disclosure | BRAIN.md derivation surface | mitigate | The NAV-02 suite asserts buildBrainQueryContext is the sole Brain-context builder; no new query surface; derivation stays hash/enum/slug only. |
| T-142-09 | Repudiation | a filing silently fails (honesty rule) | mitigate | FILEVAL-03 suite asserts the failure is RETURNED as { ok:false, reason:'filing_did_not_land' } and surfaceable -- never swallowed to null; the REMIND half asserts a landed filing's readback is surfaceable so the navigator can be reminded of what was filed. |
| T-142-10 | Tampering | NAV-04 consumer silently unwired by a future change | mitigate | The NAV-04 two-hop regression fence asserts the real wiring chain (hooks.json registers sessionstart-coordinator.cjs on the compact matcher; the coordinator loads restore-post-compact-context.cjs) + 95.5 passed status stay present -- not a naive direct-grep that would false-fail. |
</threat_model>

<verification>
- `node tests/test-brain-md-tier-rise.cjs` GREEN (NAV-02 tier rise observed in trace; derivation Part-8-clean).
- `node tests/test-post-compact-nav04-closure.cjs` GREEN (NAV-04 closed by reference + two-hop fence: coordinator-loads-consumer + hooks.json compact-matcher registers coordinator).
- `node tests/test-fileval-readback-surface.cjs` GREEN (FILEVAL-03 both halves: honesty + remind).
- Part 8 grep sweep over the derivation surface: `grep -E "buildBrainQueryContext" lib/core/brain-derivation.cjs` confirms the single DOWN-lane chokepoint; no second Brain-context builder introduced.
- `git diff --name-only` shows only the three test files unless a proven gap is documented in the SUMMARY.
</verification>

<success_criteria>
NAV-02 is proven end-to-end (BRAIN.md present -> tier_mode rises above tier_0 in a decision trace, derivation Part-8-clean), NAV-04 is closed by reference to Phase 95.5 and locked by a two-hop regression fence asserting the real wiring chain (hooks.json -> sessionstart-coordinator.cjs -> restore-post-compact-context.cjs), and FILEVAL-03's BOTH halves are proven over the shipped readback (honesty: a non-landed filing is surfaced; remind: a landed filing's readback is surfaceable) -- all three as loop-fires acceptance tests composable by the Phase 146 gate, with no new Brain query surface and no re-implementation of shipped code.
</success_criteria>

<output>
Create `.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-04-SUMMARY.md` when done.
</output>
