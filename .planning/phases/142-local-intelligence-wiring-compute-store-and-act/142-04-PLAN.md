---
phase: 142-local-intelligence-wiring-compute-store-and-act
plan: 04
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - tests/test-brain-md-tier-rise.cjs
  - tests/test-post-compact-nav04-closure.cjs
  - tests/test-fileval-readback-surface.cjs
autonomous: true
canon_parts: [Part 2, Part 8, Part 9]
requirements: [NAV-02, NAV-04, FILEVAL-03]
must_haves:
  truths:
    - "BRAIN.md derivation lifts a room's sections out of tier_0 -- with BRAIN.md present, decide() emits brain_md_tier_mode above tier_0 (NAV-02, proven end-to-end on a real section)"
    - "Post-compact re-injection survives an auto-compact boundary -- NAV-04 is closed by reference to Phase 95.5 (status: passed, 5/5) and locked by a regression fence"
    - "A readback that did not land is SURFACED, not swallowed -- FILEVAL-03 honesty rule proven over the shipped fileEvidenceWithReadback"
    - "All BRAIN.md derivation stays Part-8-clean via buildBrainQueryContext (hash + enum only); NO new Brain query surface is added"
  artifacts:
    - path: "tests/test-brain-md-tier-rise.cjs"
      provides: "NAV-02 loop-fires acceptance: BRAIN.md present -> tier_mode rises above tier_0 in a decision trace"
      contains: "tier_0"
    - path: "tests/test-post-compact-nav04-closure.cjs"
      provides: "NAV-04 close-by-reference regression fence citing 95.5-VERIFICATION.md"
      contains: "95.5"
    - path: "tests/test-fileval-readback-surface.cjs"
      provides: "FILEVAL-03 honesty-rule acceptance over fileEvidenceWithReadback"
      contains: "filing_did_not_land"
  key_links:
    - from: "lib/core/folder-memory.cjs readQuadruple"
      to: "lib/core/navigation-engine.cjs decide() brain_md_tier_mode"
      via: "shared.resolveTierMode"
      pattern: "resolveTierMode"
    - from: "lib/core/brain-derivation.cjs deriveSection"
      to: "buildBrainQueryContext"
      via: "the DOWN-lane chokepoint (hash + enum only)"
      pattern: "buildBrainQueryContext"
---

<objective>
VERIFY-AND-CLOSE NAV-02, NAV-04, and FILEVAL-03.

- **NAV-02 (VERIFY + close any auto-fire gap):** The consumption side is shipped -- decide() reads readQuadruple and sets brain_md_tier_mode via resolveTierMode (tier_0 when BRAIN.md absent, higher when present). This plan proves end-to-end on a real section that derivation FIRES -> BRAIN.md is written -> tier_mode rises above tier_0 in a decision trace. If derivation does not auto-fire for an un-derived section, close that ONE wiring gap.
- **NAV-04 (close-by-reference):** Phase 95.5 shipped the post-compact re-injection consumer (95.5-VERIFICATION.md status: passed, 5/5). This plan confirms 95.5 covers the NAV-04 wording and locks it with a regression fence. No new build.
- **FILEVAL-03 (verify/thin-wire):** The shipped fileEvidenceWithReadback already returns `{ ok:false, reason:'filing_did_not_land' }` on a failed write. This plan proves the honesty rule (a filing that did not land is SURFACED, not swallowed) and thin-wires the surfacing only if the test proves it is not already surfaced.

Purpose: lock the shipped local-loop memory substrate with loop-fires acceptance tests composable by the Phase 146 gate. All derivation stays Part-8-clean (buildBrainQueryContext: hash + enum + section slug only); add NO new Brain query surface.

Output: three suites GREEN; NAV-04 closed by reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-CONTEXT.md
@.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md
@.planning/research/v1.13.1-larryreach-fanout/raw-slices/SLICE-C.md

<interfaces>
<!-- Shipped substrate. Verify against it; do not add a new Brain query surface. -->

NAV-02 consumption (SHIPPED):
- lib/core/folder-memory.cjs `readQuadruple(sectionPath)` -- the only allowed BRAIN.md read path; returns the triple + brain field.
- lib/core/navigation-engine-shared.cjs `resolveTierMode(quadruple, brainAvailable)` -> 'mode_a' | 'mode_b' | 'tier_0'. tier_0 when brain null/parse_failed/unavailable; mode_a when brain present + reachable; mode_b when brain_offline-exempt.
- lib/core/navigation-engine.cjs decide() sets trace.brain_md_tier_mode from resolveTierMode (absent quadruple -> 'tier_0').

NAV-02 derivation (DOWN lane, SHIPPED, Part-8-clean):
- lib/core/brain-derivation.cjs `async deriveSection(roomPath, section, options)` -- writes the section BRAIN.md; gated on brainClient.isAvailable(); options.dry_run short-circuits with would_query count and no write.
- lib/core/brain-derivation.cjs `buildBrainQueryContext(triple, sectionSlug)` -- the DOWN-lane chokepoint that strips the local triple to sha256 hashes + enum scalars + section slug. NO user prose crosses. This is the ONLY Brain-adjacent surface; do NOT add another.

NAV-04 (SHIPPED by Phase 95.5):
- scripts/restore-post-compact-context.cjs (274 lines) reads `<roomDir>/.mindrian/last-post-compact.md`, re-injects TRIPLE_CONTEXT as hookSpecificOutput.additionalContext at session-start. Registered in hooks.json SessionStart entry 11/11 (matcher startup|clear|compact).
- scripts/post-compact write_post_compact_side_channel() writes the stamped side-channel.
- 95.5-VERIFICATION.md: status passed, 5/5 truths verified, all artifacts WIRED.

FILEVAL-03 (substrate SHIPPED by FILEVAL-02):
- lib/core/navigation.cjs re-exports `fileEvidenceWithReadback(db, params)` (from lib/core/navigation/file-evidence-readback.cjs). It writes the EvidenceClaim via the LOCKED writeEvidenceClaim path, READS the row back, and returns `{ ok:false, reason:'filing_did_not_land' }` when the readback shows the row did not land (or fields did not round-trip), else ok:true with the readback. artifact_path (D-10) round-trips through properties JSON.
- Pattern to mirror: tests/test-fileval-readback.cjs (the FILEVAL-02 fixture-first contract).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the NAV-02 tier-rise suite GREEN (BRAIN.md present -> tier_mode rises above tier_0)</name>
  <files>tests/test-brain-md-tier-rise.cjs</files>
  <read_first>
    - lib/core/navigation-engine-shared.cjs (resolveTierMode rules: tier_0 vs mode_a vs mode_b)
    - lib/core/navigation-engine.cjs (decide(): how trace.brain_md_tier_mode is set from the quadruple; the absent-quadruple -> tier_0 path)
    - lib/core/folder-memory.cjs (readQuadruple -- the BRAIN.md read path; the brain field shape resolveTierMode consumes)
    - lib/core/brain-derivation.cjs (deriveSection dry_run path + buildBrainQueryContext -- proof the DOWN lane is the only Brain surface)
  </read_first>
  <action>
    Complete `test-brain-md-tier-rise.cjs` to prove NAV-02 fires. On a tmp section: (1) with NO BRAIN.md present, call decide() with the section's quadruple and assert `trace.brain_md_tier_mode === 'tier_0'` (the dumb-mode baseline the SLICE-C evidence flags); (2) write a BRAIN.md for the section (either by running deriveSection against a fixture/mock-available Brain, OR by composing a valid brain quadruple field directly so readQuadruple resolves a present, fresh brain), then call decide() again and assert `brain_md_tier_mode` rises ABOVE tier_0 (mode_a when brainAvailable, mode_b when brain_offline-exempt). The assertion is on the decision TRACE, the observation surface named in the CONTEXT. Additionally assert the Part-8 cleanliness of the derivation surface: a grep over lib/core/brain-derivation.cjs confirms deriveSection's only Brain-context builder is buildBrainQueryContext (no new query surface). If derivation does NOT auto-fire for an un-derived section in the live session path (the one possible NAV-02 gap), surface it in the SUMMARY as the single wiring gap to close; otherwise the consumption proof stands and no production code changes.
  </action>
  <verify>
    <automated>node tests/test-brain-md-tier-rise.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-brain-md-tier-rise.cjs` exits 0 (GREEN): tier_0 with BRAIN.md absent, and a tier above tier_0 with BRAIN.md present, observed in the decision trace.
    - `grep -c "buildBrainQueryContext" lib/core/brain-derivation.cjs` returns >= 1 AND `grep -cE "brain_query|brainQuery|new Brain query surface" lib/core/brain-derivation.cjs` shows no second Brain-context builder introduced (the DOWN-lane chokepoint remains the only Brain-adjacent surface).
    - `git diff --name-only` shows no production-code change unless the SUMMARY documents a proven auto-fire gap requiring the one wiring fix.
  </acceptance_criteria>
  <done>The NAV-02 suite proves BRAIN.md derivation lifts a section out of tier_0 in a decision trace, with the derivation staying Part-8-clean and no new Brain query surface.</done>
</task>

<task type="auto">
  <name>Task 2: Close NAV-04 by reference + lock with a regression fence</name>
  <files>tests/test-post-compact-nav04-closure.cjs</files>
  <read_first>
    - .planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md (status passed, 5/5; the artifact + key-link table proving the consumer is WIRED)
    - hooks/hooks.json (the SessionStart entry wiring restore-post-compact-context.cjs -- matcher startup|clear|compact)
  </read_first>
  <action>
    Complete `test-post-compact-nav04-closure.cjs` as the NAV-04 close-by-reference regression fence. Assert: (1) `scripts/restore-post-compact-context.cjs` exists; (2) hooks/hooks.json wires it in a SessionStart entry whose matcher includes `compact` (the auto-compact boundary NAV-04 targets); (3) `scripts/post-compact` exists and writes the stamped side-channel; (4) `.planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md` has frontmatter `status: passed`. This LOCKS NAV-04: the consumer that survives an auto-compact boundary stays wired. Map the NAV-04 wording (post-compact re-injection consumer so memory does not degrade across an auto-compact boundary) onto the 95.5 verified truths and record the close-by-reference in the SUMMARY citing 95.5-VERIFICATION.md. No new build -- this is verification + a fence only.
  </action>
  <verify>
    <automated>node tests/test-post-compact-nav04-closure.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-post-compact-nav04-closure.cjs` exits 0 (GREEN): consumer exists, SessionStart `compact`-matcher wiring present, post-compact writer present, 95.5 status passed.
    - `grep -c "status: passed" .planning/phases/95.5-post-compact-memory-pipeline-consumer/95.5-VERIFICATION.md` returns 1 (the close-by-reference citation is valid).
    - `git diff --name-only` includes only the test file (no production change -- close-by-reference).
  </acceptance_criteria>
  <done>NAV-04 is closed by reference to Phase 95.5 and locked by a regression fence asserting the post-compact consumer stays wired across the compact boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Turn the FILEVAL-03 honesty-rule suite GREEN (a filing that did not land is surfaced)</name>
  <files>tests/test-fileval-readback-surface.cjs</files>
  <read_first>
    - lib/core/navigation/file-evidence-readback.cjs (fileEvidenceWithReadback: the readback assertion + the { ok:false, reason:'filing_did_not_land' } return; the round-trip field checks)
    - tests/test-fileval-readback.cjs (the FILEVAL-02 fixture-first contract to mirror -- caller-owned fixture db, ok:false on failed write, provenance preservation)
    - tests/fixtures/room-142-fixture.cjs (the caller-owned fixture db builder)
  </read_first>
  <action>
    Complete `test-fileval-readback-surface.cjs` to prove the FILEVAL-03 honesty rule over the SHIPPED fileEvidenceWithReadback. Using the caller-owned fixture db: (1) a successful filing returns ok:true with the readback fields populated (proving Larry can REMIND the navigator what was filed -- the positive honesty path); (2) a simulated failed write (the row does not land, or a locked provenance field does not round-trip) returns `{ ok:false, reason:'filing_did_not_land' }` -- the signal Larry must SURFACE, not swallow. Assert that the failure is RETURNED as a structured result (surfaceable), never silently swallowed (no thrown-and-caught-to-null path that would hide it). This is the contract the room-proactive / Decision-Capture surface keys off to tell the navigator "that filing did not land." If the shipped function already returns the structured ok:false (it does per FILEVAL-02), no production change is needed -- the suite is the FILEVAL-03 acceptance fence. Only thin-wire a surfacing call if the test proves the honesty signal is computed but not surfaced anywhere; in that case wire the existing result through the existing surface (no new surface), and document it in the SUMMARY.
  </action>
  <verify>
    <automated>node tests/test-fileval-readback-surface.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-fileval-readback-surface.cjs` exits 0 (GREEN): ok:true on a landed write (remind path), `{ ok:false, reason:'filing_did_not_land' }` on a non-landed write (surfaced, not swallowed).
    - The suite asserts the failure result is a returned structured value (surfaceable), proving the honesty rule -- `grep -c "filing_did_not_land" tests/test-fileval-readback-surface.cjs` returns >= 1.
    - `git diff --name-only` shows only the test file unless the SUMMARY documents a proven not-surfaced gap requiring a thin wire through an existing surface.
  </acceptance_criteria>
  <done>The FILEVAL-03 suite proves a filing that did not land is surfaced as a structured honesty signal over the shipped readback, with no new surface added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local triple -> BRAIN.md derivation -> Brain | The ONLY Brain-adjacent surface is the DOWN-lane buildBrainQueryContext (hash + enum + slug). No user prose crosses (Part 8). This plan adds NO new Brain query surface. |
| readback result -> Larry surface | The filing-did-not-land honesty signal is LOCAL; surfaced to the navigator, never egressed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-08 | Information Disclosure | BRAIN.md derivation surface | mitigate | The NAV-02 suite asserts buildBrainQueryContext is the sole Brain-context builder; no new query surface; derivation stays hash/enum/slug only. |
| T-142-09 | Repudiation | a filing silently fails (honesty rule) | mitigate | FILEVAL-03 suite asserts the failure is RETURNED as { ok:false, reason:'filing_did_not_land' } and surfaceable -- never swallowed to null. |
| T-142-10 | Tampering | NAV-04 consumer silently unwired by a future change | mitigate | The NAV-04 regression fence asserts the SessionStart compact-matcher wiring + 95.5 passed status stay present. |
</threat_model>

<verification>
- `node tests/test-brain-md-tier-rise.cjs` GREEN (NAV-02 tier rise observed in trace; derivation Part-8-clean).
- `node tests/test-post-compact-nav04-closure.cjs` GREEN (NAV-04 closed by reference + fence).
- `node tests/test-fileval-readback-surface.cjs` GREEN (FILEVAL-03 honesty rule).
- Part 8 grep sweep over the derivation surface: `grep -E "buildBrainQueryContext" lib/core/brain-derivation.cjs` confirms the single DOWN-lane chokepoint; no second Brain-context builder introduced.
- `git diff --name-only` shows only the three test files unless a proven gap is documented in the SUMMARY.
</verification>

<success_criteria>
NAV-02 is proven end-to-end (BRAIN.md present -> tier_mode rises above tier_0 in a decision trace, derivation Part-8-clean), NAV-04 is closed by reference to Phase 95.5 and locked by a fence, and FILEVAL-03's honesty rule is proven over the shipped readback -- all three as loop-fires acceptance tests composable by the Phase 146 gate, with no new Brain query surface and no re-implementation of shipped code.
</success_criteria>

<output>
Create `.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-04-SUMMARY.md` when done.
</output>
