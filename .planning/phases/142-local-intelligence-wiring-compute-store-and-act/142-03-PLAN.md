---
phase: 142-local-intelligence-wiring-compute-store-and-act
plan: 03
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - tests/test-cascade-surface-loop-fires.cjs
  - tests/test-derivation-drain-fires.cjs
autonomous: true
canon_parts: [Part 4, Part 8]
requirements: [CASC-01, NAV-03]
must_haves:
  truths:
    - "Filing an artifact surfaces the cross-relationship cascade findings to Larry mid-session via the side-channel the bash hook writes (CASC-01 -- shipped by Phase 95, LOCKED by a loop-fires test)"
    - "An enqueued brain-derivation entry drains within a session (NAV-03 -- shipped by the UserPromptSubmit drain hook, LOCKED by a loop-fires test)"
    - "Neither requirement is re-implemented; the renderer (room-proactive) and the drain hook are not modified unless a verification test fails"
  artifacts:
    - path: "tests/test-cascade-surface-loop-fires.cjs"
      provides: "CASC-01 end-to-end loop-fires acceptance: post-write -> side-channel -> surfaceable findings"
      contains: "last-cascade.json"
    - path: "tests/test-derivation-drain-fires.cjs"
      provides: "NAV-03 loop-fires acceptance: enqueue -> drain dispatches within a session"
      contains: "drain"
  key_links:
    - from: "scripts/post-write"
      to: "<roomDir>/.mindrian/last-cascade.json"
      via: "write_cascade_side_channel"
      pattern: "last-cascade\\.json"
    - from: "hooks/hooks.json UserPromptSubmit"
      to: "scripts/brain-derivation-drain.cjs"
      via: "command hook"
      pattern: "brain-derivation-drain"
---

<objective>
VERIFY-AND-LOCK CASC-01 and NAV-03. Both are SHIPPED (CASC-01 by Phase 95 side-channel + room-proactive reader; NAV-03 by the UserPromptSubmit drain hook + session-start queue scan). This plan turns the two Wave-0 RED suites GREEN by proving each loop FIRES end-to-end, and closes the ONE wiring gap each only IF the acceptance test fails. No re-implementation.

Purpose: Phase 142 is a loop-fires phase. The correct output for shipped work is an executable acceptance/regression test that locks it (SEED-008 Acceptance Contract discipline at the per-requirement level), composable by the Phase 146 gate.

Output: `test-cascade-surface-loop-fires.cjs` (CASC-01) and `test-derivation-drain-fires.cjs` (NAV-03) GREEN. The room-proactive renderer (byte-identical per Phase 95) and the drain hook are NOT modified unless their acceptance test proves a real gap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-CONTEXT.md

<interfaces>
<!-- Shipped substrate. Verify against it; do not rebuild it. -->

CASC-01 (Phase 95, SHIPPED):
- scripts/post-write `write_cascade_side_channel(room_dir, file_path, section, cascade_output)` writes the cascade payload atomically to `<roomDir>/.mindrian/last-cascade.json` (mktemp + mv -f, soft-fail). Root keys: timestamp, file_path, section, cascade_status, classification, git_commit, graph_index, proactive_intelligence.
- The PostToolUse envelope advisory starts with `post-write: cascade complete ` OR `queued MINTO regen for ` -- the two prefixes the skill keys off.
- skills/room-proactive/SKILL.md reads `<roomDir>/.mindrian/last-cascade.json` when the trigger prefix fires, and surfaces `proactive_intelligence.newFindings` (confidence >= 0.60, max 2) via the Decision Capture flow. The renderer is BYTE-IDENTICAL per Phase 95 -- DO NOT touch it.
- Existing pattern to mirror: tests/test-cascade-surface-e2e.cjs (copies the committed fixture tree to a tmpdir, sets MINDRIAN_ROOMS_HOME, fires post-write, asserts the side-channel file + its 8 root keys).

NAV-03 (drain hook, SHIPPED):
- hooks/hooks.json wires `node "${CLAUDE_PLUGIN_ROOT}/scripts/brain-derivation-drain.cjs"` on UserPromptSubmit (timeout 2000).
- scripts/session-start scans the queue via lib/core/brain-derivation-queue.cjs.
- lib/core/brain-derivation-queue.cjs: `enqueue(roomDir, section, previous_hash, new_hash, reason)` and `async drain(roomDir, { maxEntries=5, dryRun })` -> { processed, skipped, re_enqueued, errors, dispatched }. dryRun collects entries that WOULD fire in result.dispatched.
- scripts/brain-derivation-drain.cjs: parent exits within ~100ms; for each eligible entry (current triple hash matches queue hash AND Brain available) spawns a detached deriveSection child. CLI modes include --dry-run and --room <roomDir>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the CASC-01 loop-fires suite GREEN (verify mid-session surfacing end-to-end)</name>
  <files>tests/test-cascade-surface-loop-fires.cjs</files>
  <read_first>
    - tests/test-cascade-surface-e2e.cjs (the tmpdir-copy + MINDRIAN_ROOMS_HOME + fire-post-write + assert-side-channel pattern to extend)
    - scripts/post-write (write_cascade_side_channel: the side-channel write contract + the two advisory prefixes)
    - skills/room-proactive/SKILL.md (the trigger detection + the newFindings surfacing contract -- the byte-identical renderer; read to assert the contract, NOT to modify)
  </read_first>
  <action>
    Complete `test-cascade-surface-loop-fires.cjs` so it proves the CASC-01 loop FIRES end-to-end (stronger than the existing e2e key-presence suite): copy a fixture room tree to a tmpdir, set MINDRIAN_ROOMS_HOME, fire scripts/post-write against a fixture artifact inside a recognized section, and assert: (1) the post-write envelope advisory starts with one of the two recognized prefixes (`post-write: cascade complete ` or `queued MINTO regen for `) -- the trigger the room-proactive skill keys off; (2) `<roomDir>/.mindrian/last-cascade.json` exists, parses, and carries `proactive_intelligence` with a `newFindings` array; (3) when the fixture cascade produced a finding at confidence >= 0.60, that finding is present in newFindings and is therefore surfaceable by the SKILL.md Decision Capture flow. This asserts the WIRE from filing -> side-channel -> surfaceable finding, which is exactly CASC-01. Do NOT modify scripts/post-write or skills/room-proactive/SKILL.md. If the end-to-end assertion fails because of a genuine plumbing gap (the side-channel is not written, or the prefix does not match the skill trigger), STOP and surface the gap in the SUMMARY as a NEW FAILURE for a /gsd:debug session -- do not silently patch the renderer.
  </action>
  <verify>
    <automated>node tests/test-cascade-surface-loop-fires.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-cascade-surface-loop-fires.cjs` exits 0 (GREEN): the side-channel file is written with a non-empty proactive_intelligence path and the advisory prefix matches the room-proactive trigger.
    - `git diff --name-only` does NOT include scripts/post-write or skills/room-proactive/SKILL.md (no re-implementation; renderer untouched).
  </acceptance_criteria>
  <done>The CASC-01 suite proves filing -> side-channel -> surfaceable finding end-to-end, with zero changes to the shipped writer/renderer.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the NAV-03 loop-fires suite GREEN (verify the queue drains within a session)</name>
  <files>tests/test-derivation-drain-fires.cjs</files>
  <read_first>
    - lib/core/brain-derivation-queue.cjs (enqueue + async drain signatures + the dryRun -> result.dispatched contract + the hash-match eligibility rule)
    - scripts/brain-derivation-drain.cjs (the UserPromptSubmit entry: --dry-run / --room modes; parent-exits-fast contract)
    - hooks/hooks.json (the UserPromptSubmit wiring line that proves drain fires per turn)
  </read_first>
  <action>
    Complete `test-derivation-drain-fires.cjs` so it proves NAV-03 FIRES: on a tmp room, enqueue an entry via `brain-derivation-queue.enqueue(roomDir, section, previous_hash, new_hash, reason)` with hashes arranged so the entry is eligible, then run the drain in dry-run (either `drain(roomDir, { dryRun:true })` directly OR spawn `scripts/brain-derivation-drain.cjs --room <tmp> --dry-run`) and assert the eligible entry appears in `result.dispatched` (it WOULD fire) -- proving the queue drains within a session rather than sitting for days. Additionally assert the hook wiring is present: hooks/hooks.json contains the `brain-derivation-drain.cjs` UserPromptSubmit command (the regression fence that the auto-drain trigger stays wired). Use dry-run so the test does not require Brain availability or spawn real deriveSection children. If the enqueued entry does NOT appear in dispatched under a matching hash (a real auto-fire gap), STOP and surface it in the SUMMARY as the ONE wiring gap to close -- per the CONTEXT classification, only close it if the test proves it.
  </action>
  <verify>
    <automated>node tests/test-derivation-drain-fires.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-derivation-drain-fires.cjs` exits 0 (GREEN): an enqueued eligible entry is reported in the drain's dispatched set (would-fire), proving the queue drains within a session.
    - `grep -c "brain-derivation-drain" hooks/hooks.json` returns >= 1 (the UserPromptSubmit auto-drain wiring is present -- regression fence).
    - `git diff --name-only` does NOT include scripts/brain-derivation-drain.cjs or lib/core/brain-derivation-queue.cjs unless the SUMMARY documents a proven auto-fire gap that required the one wiring fix.
  </acceptance_criteria>
  <done>The NAV-03 suite proves an enqueued entry drains within a session and the UserPromptSubmit auto-drain wiring is present, with no re-implementation of the shipped drain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filing (post-write) -> side-channel file -> room-proactive skill | LOCAL only per Canon Part 8; the side-channel read never queries the Brain or any network surface. |
| UserPromptSubmit -> drain -> deriveSection child | The drain spawns local deriveSection children; the DOWN-lane derivation stays Part-8-clean (hash/enum only). This plan exercises dry-run only -- no Brain egress in the test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-05 | Information Disclosure | cascade side-channel read | mitigate | The acceptance test runs entirely LOCAL with MINDRIAN_ROOMS_HOME pinned to a tmpdir; no network surface exercised; the skill reads the file LOCAL-only per Phase 95. |
| T-142-06 | Spoofing | test fires against the user's real active room | mitigate | Every spawnSync sets MINDRIAN_ROOMS_HOME to the tmpdir copy (Pitfall 2 from 95.1-RESEARCH) so resolve-room never falls through to ~/MindrianRooms/. |
| T-142-07 | Tampering | drain test triggers real Brain calls | mitigate | The NAV-03 test uses dry-run (drain dryRun:true / --dry-run), which collects would-fire entries without spawning deriveSection children or requiring Brain availability. |
</threat_model>

<verification>
- `node tests/test-cascade-surface-loop-fires.cjs` GREEN and `node tests/test-derivation-drain-fires.cjs` GREEN.
- `git diff --name-only` shows only the two test files (no renderer / no drain re-implementation) unless a proven gap is documented in the SUMMARY.
- These two suites are registered in tests/run-all-142.sh (from Plan 01) and now pass.
</verification>

<success_criteria>
CASC-01 and NAV-03 are LOCKED by loop-fires acceptance tests that prove each loop fires end-to-end, with zero re-implementation of the shipped Phase 95 cascade plumbing and the shipped drain hook. Any genuine gap is surfaced, not silently patched.
</success_criteria>

<output>
Create `.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-03-SUMMARY.md` when done.
</output>
