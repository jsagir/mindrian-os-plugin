---
phase: 142-local-intelligence-wiring-compute-store-and-act
plan: 02
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - lib/core/navigation-engine.cjs
  - lib/core/navigation-engine-shared.cjs
  - scripts/intent-classifier.cjs
autonomous: true
canon_parts: [Part 9, Part 4, Part 8]
requirements: [CASC-02]
must_haves:
  truths:
    - "The Phase 109 navigation spine reads the local graph neighborhood to route a decision -- routing reflects the getNeighborhood-ranked neighborhood, not just stored-edge presence"
    - "The neighborhood read goes through the navigation.cjs chokepoint on the caller-owned db handle (caller never has getRoomContext open room.db)"
    - "trace.routing_source is UNCHANGED (still legacy) -- CASC-02 navigates but does NOT flip the source (Phase 144's single change)"
    - "Per-turn assembly stays inside the existing ~1200ms NAV budget"
  artifacts:
    - path: "lib/core/navigation-engine.cjs"
      provides: "decide() consumes the navigated-neighborhood leg from context and reflects it in the decision trace"
      contains: "neighborhood"
    - path: "scripts/intent-classifier.cjs"
      provides: "runNavigationEngine wires getRoomContext output into the decide() context via the chokepoint on the caller-owned roomDb handle"
      contains: "getRoomContext"
  key_links:
    - from: "scripts/intent-classifier.cjs runNavigationEngine"
      to: "lib/core/navigation.cjs getRoomContext"
      via: "caller-owned roomDb handle through the chokepoint"
      pattern: "getRoomContext\\("
    - from: "lib/core/navigation-engine.cjs decide()"
      to: "context.roomContext (navigated neighborhood)"
      via: "trace field populated from getNeighborhood ranking"
      pattern: "roomContext|neighborhood"
---

<objective>
CASC-02 -- the one genuine net-new build of Phase 142. Wire Phase 141's `getRoomContext` (the 3-leg local fusion whose Leg C is `getNeighborhood` graph-ranking) into the navigation `decide()` so the routing decision reflects the NAVIGATED graph neighborhood, not merely stored edges. This makes the Phase 109 spine actually navigate (Canon Part 9: SQL is the local mind), realizing the SLICE-B "stored-not-navigated" gap closure.

Purpose: Today `decide()` reads `readQuadruple` (BRAIN.md presence -> tier_mode) but never calls `getRoomContext` / `getNeighborhood` -- the spine stores the graph but does not walk it for routing. This plan walks it.

Output: `runNavigationEngine` (caller, owns the db handle) computes the neighborhood via the chokepoint and threads it into `decide()`'s context; `decide()` reflects it in the decision trace.

HARD BOUNDARY (Phase 144 fence): this plan MUST NOT flip `trace.routing_source` from `legacy` to `engine`. That single change is Phase 144 (NAV-01). CASC-02 makes the spine navigate; the source flip stays out of scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-CONTEXT.md
@.planning/research/v1.13.1-larryreach-fanout/raw-slices/SLICE-B.md

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

From lib/core/navigation.cjs (the chokepoint -- the ONLY allowed graph read path, Canon Part 9):
- `getRoomContext: roomContext.getRoomContext` -- exported.
- `getNeighborhood: neighborhoodMod.getNeighborhood` -- exported.
- `openRoomDbForCaller(roomDir)` / `closeRoomDbForCaller(db)` -- the caller-owned-handle lifecycle (already used by runNavigationEngine).

From lib/core/navigation/room-context.cjs (the Leg fusion -- caller owns db):
- `async function getRoomContext(db, roomId, opts)` where opts = { seedFragments?, topK?, fragmentWindow?, maxDepth? }.
- The module NEVER opens room.db and NEVER requires node:sqlite -- the db handle is caller-owned (mirror evidence-claim.cjs).
- Returns the fused context: Leg A home-view summary + Leg B recentMessages + Leg C relevantNodes (the getNeighborhood graph-ranked neighborhood).

From scripts/intent-classifier.cjs runNavigationEngine (the caller -- already owns roomDb + navigationMod):
- Opens `roomDb = navigationMod.openRoomDbForCaller(roomDir)` and closes it in finally (Pitfall 5 leak guard already in place).
- Builds the `context` object: { quadruple, brainAvailable, userPersona, intentSignal, operator, sectionPath, problemType, jtbd, roomState:{ db: roomDb, roomDir } }.
- Builds `turn = { userText: conversationSeed, sectionPath, sessionId }` then `callDecideWithTimeout(navEngine.decide, turn, context, NAV_HARD_TIMEOUT_MS)`.
- `deriveConversationSeed(navigationMod, roomDb)` already produces the LOCAL seed (the same seed getRoomContext should consume as seedFragments).

From lib/core/navigation-engine.cjs decide(turn, context) -> decision:
- Reads context.quadruple (or readQuadruple fallback), sets trace.brain_md_tier_mode via shared.resolveTierMode.
- trace = decision.decision_trace; current trace fields include brain_md_tier_mode, sql_signals, icm_scope, minto_reasoning, intent_persona, chosen_rationale.
- routing_source lives on the trace and is set by the LEGACY path -- DO NOT change its value here.

From lib/core/navigation-engine-shared.cjs:
- emptyDecision() seeds decision_trace with brain_md_tier_mode:'tier_0' and the other default fields. Add the new neighborhood trace field default here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread getRoomContext output into the decide() context (caller side)</name>
  <files>scripts/intent-classifier.cjs</files>
  <read_first>
    - scripts/intent-classifier.cjs (runNavigationEngine: the roomDb open at the navigationMod.openRoomDbForCaller call, the context object assembly, the deriveConversationSeed + turn build before callDecideWithTimeout, the closeRoomDbHandle finally)
    - lib/core/navigation/room-context.cjs (getRoomContext(db, roomId, opts) signature + that it is async + caller-owns-db contract)
    - lib/core/navigation.cjs (getRoomContext re-export -- the chokepoint door)
  </read_first>
  <behavior>
    - With a populated room.db, runNavigationEngine computes a roomContext via the chokepoint and passes it on context.roomContext to decide().
    - getRoomContext receives the SAME conversation seed (seedFragments) already derived by deriveConversationSeed -- one seed, both consumers.
    - On any getRoomContext fault or absent room.db, context.roomContext is null and the decision proceeds exactly as before (graceful degradation; no throw, no leak).
    - The added read is awaited INSIDE the existing deriveConversationSeed().then() chain so it stays within the single NAV_HARD_TIMEOUT_MS race envelope -- no second timeout, no added await outside the race.
    - The caller-owned roomDb handle is still closed exactly once in the finally (no double-close, no leak).
  </behavior>
  <action>
    In `runNavigationEngine`, after `roomDb` is opened via `navigationMod.openRoomDbForCaller` and inside the existing `deriveConversationSeed(navigationMod, roomDb).then(conversationSeed => ...)` chain, call `navigationMod.getRoomContext(roomDb, <roomId>, { seedFragments: conversationSeed, topK, maxDepth })` through the chokepoint (NOT a direct room-context.cjs require -- the substrate guard requires the navigation.cjs door per Canon Part 9). Resolve the roomId from the active room (reuse the roomDir-derived id already available). Attach the result as `context.roomContext` before calling `callDecideWithTimeout`. Wrap the getRoomContext call in try/catch (or `.catch(() => null)`) so a fault degrades to `context.roomContext = null`. Keep the read on the caller-owned handle; getRoomContext never opens room.db itself (141-03 contract). Do NOT add the roomContext to the `turn` object or any path that could reach buildBrainPacket -- it rides context for the LOCAL routing decision only (D-03a fence). Stay synchronous-await INSIDE the existing race so the 1200ms budget is unchanged.
  </action>
  <verify>
    <automated>node tests/test-spine-navigates-decide.cjs && node tests/test-decide-part8-invariant.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getRoomContext" scripts/intent-classifier.cjs` shows the call goes through the navigationMod chokepoint (navigationMod.getRoomContext), not a direct require of room-context.cjs.
    - `grep -c "openRoomDbForCaller\|closeRoomDbForCaller" scripts/intent-classifier.cjs` confirms the caller-owned handle lifecycle is preserved (open + close both present); no new direct `node:sqlite` require is introduced.
    - `node tests/test-decide-part8-invariant.cjs` exits 0 (no packet/brain-client egress added; the neighborhood ride is LOCAL-only).
  </acceptance_criteria>
  <done>runNavigationEngine threads a chokepoint-computed neighborhood into context.roomContext on the caller-owned handle, degrading to null on fault, within the existing timeout race.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: decide() consumes the navigated neighborhood + reflects it in the trace (engine side)</name>
  <files>lib/core/navigation-engine.cjs, lib/core/navigation-engine-shared.cjs</files>
  <read_first>
    - lib/core/navigation-engine.cjs (decide(): the trace assembly block where brain_md_tier_mode / sql_signals / icm_scope / minto_reasoning are set; the chosen_rationale composition)
    - lib/core/navigation-engine-shared.cjs (emptyDecision() default decision_trace shape; resolveTierMode)
    - tests/test-spine-navigates-decide.cjs (the CASC-02 contract this code must satisfy: neighborhood reflected in trace; routing_source UNCHANGED)
  </read_first>
  <behavior>
    - emptyDecision() seeds the new trace field (e.g. trace.navigated_neighborhood) to a safe default (null / empty) so every decision carries it.
    - When context.roomContext carries a non-empty getNeighborhood-ranked neighborhood (Leg C relevantNodes), decide() reflects it in the decision trace (a structured summary: the ranked neighbor ids/types/scores that informed routing) and references it in chosen_rationale.
    - When context.roomContext is null/absent, the trace field stays at its safe default and decide() behaves exactly as before (Tier 0 / legacy path unaffected).
    - trace.routing_source is NEVER assigned 'engine' in this plan -- its value is identical to the pre-CASC-02 behavior (the Part-144 fence).
  </behavior>
  <action>
    In `navigation-engine-shared.cjs` `emptyDecision()`, add a defaulted trace field for the navigated neighborhood (default null/empty) so the trace shape is stable across all paths. In `navigation-engine.cjs` `decide()`, read `context.roomContext` (safe-default to null). When present and its Leg-C neighborhood (relevantNodes) is non-empty, populate the new trace field with a structured, non-prose summary (neighbor ids + types + ranking scores -- scalars and slugs only, never raw bodies) and append a clause to `chosen_rationale` noting that routing reflected the navigated neighborhood. Do NOT change `routing_source` (assert by omission -- no assignment of 'engine' anywhere in this diff). Keep the read defensive: a malformed/absent roomContext falls through to the existing behavior with the trace field at default. Never throw -- preserve the emptyDecision() fall-through discipline.
  </action>
  <verify>
    <automated>node tests/test-spine-navigates-decide.cjs && node tests/test-brain-md-tier-rise.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-spine-navigates-decide.cjs` exits 0: with a populated fixture neighborhood, the decision_trace reflects the navigated neighborhood AND trace.routing_source is unchanged (still legacy).
    - `grep -n "routing_source" lib/core/navigation-engine.cjs | grep -i "engine"` returns no NEW assignment of routing_source to 'engine' in the CASC-02 diff (the Phase-144 fence holds).
    - The trace neighborhood field is scalar/slug-only: `node tests/test-decide-part8-invariant.cjs` exits 0 (no raw prose / no egress tokens introduced).
  </acceptance_criteria>
  <done>decide() reflects the navigated neighborhood in the trace and rationale when present, degrades gracefully when absent, and leaves routing_source untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db (LOCAL graph) -> decide() trace | Local graph neighborhood crosses into the routing decision trace. Must stay LOCAL (Part 8): scalars/slugs/scores only, never raw prose, never an egress path. |
| caller (intent-classifier) -> chokepoint (navigation.cjs) | The only allowed graph read path (Part 9). A direct room-context.cjs / node:sqlite require would breach the substrate guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Information Disclosure | getRoomContext neighborhood reflected in decision trace | mitigate | Trace field carries neighbor ids/types/ranking scalars only; test-decide-part8-invariant.cjs sweeps the touched files for forbidden projection tokens + packet/brain-client requires (Phase 90 5-tripwire). |
| T-142-02 | Tampering | routing_source semantics | mitigate | Plan asserts NO assignment of routing_source='engine' in the diff (grep gate); the Phase-144 fence is a hard plan boundary. |
| T-142-03 | Denial of Service | per-turn latency (1200ms NAV budget) | accept | getRoomContext benchmarked ~0.7-1.0ms (~1200x under budget per 141-03); the read is awaited inside the existing race envelope, adding no new timeout. |
| T-142-04 | Information Disclosure | caller-owned db handle leak / double-close | mitigate | The existing closeRoomDbHandle finally (Pitfall 5) is preserved; no new handle is opened; getRoomContext never opens room.db. |
</threat_model>

<verification>
- `node tests/test-spine-navigates-decide.cjs` GREEN: neighborhood reflected in trace, routing_source unchanged.
- `node tests/test-decide-part8-invariant.cjs` GREEN: no egress introduced by the wiring (Part-8 grep sweep over the 3 touched files).
- `bash tests/run-all-142.sh` -- the CASC-02 suite + Part-8 gate are GREEN (other suites may still be RED until Plans 03/04).
- Part 8 grep sweep over touched files: `grep -rE "projectText|hashText|shortText|safeNodeProjection|require\\(['\"][^'\"]*packet|require\\(['\"][^'\"]*brain-client" lib/core/navigation-engine.cjs lib/core/navigation-engine-shared.cjs scripts/intent-classifier.cjs` returns zero NEW matches attributable to the CASC-02 diff.
</verification>

<success_criteria>
The Phase 109 spine NAVIGATES: decide() routes with the getNeighborhood-ranked neighborhood threaded through the navigation.cjs chokepoint on the caller-owned handle, reflected in the decision trace and rationale, within the 1200ms budget, with routing_source untouched (Phase 144 fence) and zero Part-8 egress.
</success_criteria>

<output>
Create `.planning/phases/142-local-intelligence-wiring-compute-store-and-act/142-02-SUMMARY.md` when done.
</output>
