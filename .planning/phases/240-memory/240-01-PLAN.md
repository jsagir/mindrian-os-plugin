---
phase: 240-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-240.sh
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03]

must_haves:
  truths:
    - "tests/run-all-240.sh discovers every tests/test-240-* file by GLOB, with no hand-maintained execution list anywhere in it"
    - "A harness run that discovers zero tests/test-240-* files EXITS NONZERO instead of printing a clean green summary"
    - "A .sh leg that self-SKIPs on environment is tallied as SKIP, never as a pass it did not earn"
    - "The Tri-Polar daemon-parity assumption (lib/mcp/ carries no divergent JTBD trigger copy) is a machine-checked gate, not a one-time grep in a research doc"
    - "The parity gate cannot pass vacuously: it fails if lib/mcp/ contains zero .cjs files, and its own pattern is proven to bite against a seeded probe"
  artifacts:
    - path: "tests/run-all-240.sh"
      provides: "Phase 240 glob-discovery aggregator plus the Tri-Polar JTBD parity gate"
      min_lines: 120
      contains: "found -eq 0"
  key_links:
    - from: "tests/run-all-240.sh"
      to: "tests/test-240-*.cjs and tests/test-240-*.sh"
      via: "shopt -s nullglob for loop, no hand list"
      pattern: "for t in tests/test-240-\\*"
    - from: "tests/run-all-240.sh parity gate"
      to: "lib/mcp/"
      via: "grep -rl for a jtbd reference in the MCP-first daemon path"
      pattern: "lib/mcp"
---

<objective>
Build the Phase 240 verification harness FIRST, before any fix exists, and close the one Open Question the research left unverified (A7 / Q4, Tri-Polar daemon parity) as a permanent machine-checked gate rather than as a grep buried in a planning document.

Purpose: `tests/run-all-240.sh` does not exist. Every other plan in this phase declares its automated command against it, and the phase gate is `bash tests/run-all-240.sh` green. Wave 0 discipline (config `nyquist_validation: true`) means the harness lands before the fixes, so no fix in this phase can be declared done against a harness that was written to fit it.

Output: one aggregator in the `tests/run-all-236.sh` shape (Canon Part 7: 240-PATTERNS.md rates that file an EXACT match and the repo's best exemplar, so it is copied structurally rather than reinvented), carrying the Tri-Polar parity gate and its own anti-vacuity self-test.

**Resolution R-01, the parity question is CLOSED as clean and becomes a gate, not a task.** 240-RESEARCH.md Assumption A7 and Open Question Q4 flag the `MINDRIAN_MCP_FIRST` daemon path as unverified, citing Phase 241-05's precedent where the shared `mindrian-core` Stop path was blind on all three surfaces. 240-PATTERNS.md Correction 3 measured it: `grep -rln 'jtbd' lib/mcp/` returns ZERO hits. Independently re-confirmed at planning time against current `main`, same result. So there is no divergent trigger copy to fix, and this phase must NOT spend a task re-investigating it. What it owes instead is a tripwire: the parity claim is only true until someone adds a JTBD reference under `lib/mcp/`, and at that moment MEM-01's trigger fix would need a sibling. Encoding the grep as a gate leg converts a dated observation into a standing check.

**Resolution R-02, the harness legitimately exits 1 until the first tests/test-240-* file lands, and that IS this plan's mutation proof.** Phase 236 recorded the same shape in its own words (STATE.md: 236-02 had not landed, "an absent file cannot fail, only be absent"). If this plan lands before 240-02 in the same wave, `bash tests/run-all-240.sh` will hit the `found -eq 0` guard and exit 1. That is the guard working exactly as designed, and Task 2 uses that observation as the executed mutation rather than staging a synthetic one. The header therefore enumerates all four expected 240 test filenames so a missing one is visible by READING the harness, which is the discipline `run-all-236.sh:20-27` states in its own words.

**Working-tree hazard, flag before starting (240-RESEARCH.md Assumption A6).** `git status` is NOT clean on this checkout: `lib/statusline/ctx-window.cjs`, `scripts/context-monitor`, `scripts/statusline-fallback-echo.cjs`, `package-lock.json` and three statusline test files are modified, plus many untracked `.planning/debug/*.md`. None of it belongs to Phase 240. Task 1 records the pre-existing dirty set BEFORE any edit so nothing foreign is swept into a Phase 240 commit and so no later `git diff --quiet` style assertion trips on it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/240-memory/240-RESEARCH.md
@.planning/phases/240-memory/240-PATTERNS.md
@.planning/phases/240-memory/240-VALIDATION.md
</context>

## Artifacts this phase produces

Full phase-wide inventory. This plan creates only the first row; the rest are listed so every plan's executor can see the whole surface and avoid colliding on a name.

| Symbol | Kind | Created by | Location |
|---|---|---|---|
| `tests/run-all-240.sh` | new file (bash aggregator) | 240-01 | `tests/` |
| Tri-Polar JTBD parity gate leg | gate leg inside the aggregator | 240-01 | `tests/run-all-240.sh` |
| `tests/test-240-memory-store-hermetic-fence.sh` | new file (bash fence) | 240-02 | `tests/` |
| `mos_tree_content_hash` / `mos_tree_path_hash` | bash functions | 240-02 | `tests/test-240-memory-store-hermetic-fence.sh` |
| `bumpTurnCount(roomDir, expectedJtbd)` | new exported function | 240-03 | `lib/hmi/jtbd-state.cjs` |
| `current.turn_count` | new persisted field on `jtbd-state.json` `current` | 240-03 | `lib/hmi/jtbd-state.cjs` |
| `current.manual_set` | new persisted field on `jtbd-state.json` `current` | 240-03 | `lib/hmi/jtbd-state.cjs` |
| `tests/test-240-jtbd-manual-override-roundtrip.cjs` | new file (test) | 240-03 | `tests/` |
| `transitioned` boolean (replaces the unconditional early return) | local control-flow change | 240-04 | `scripts/jtbd-update.cjs` |
| `tests/test-240-jtbd-continuous-promotion.cjs` | new file (test) | 240-04 | `tests/` |
| reachability + counter-persistence source tripwire legs | gate legs appended to the aggregator | 240-04 | `tests/run-all-240.sh` |
| `tests/test-240-jtbd-event-survives-rebuild.cjs` | new file (test) | 240-05 | `tests/` |
| `.planning/phases/240-memory/deferred-items.md` | new file (residual register) | 240-06 | `.planning/phases/240-memory/` |

No new CLI flag, no new command, no new agent, no new pipeline, no new skill. **Canon Part 11 (CIRS) is NOT APPLICABLE to this phase**: every symbol above is either a test file, a bash gate leg, an internal library function, or a persisted JSON field. None is an invocable surface across the four CIRS classes, so no `hitl_shape` declaration is owed. Confirm this by running `node scripts/check-shape-declaration.cjs` at the end of Task 2 and recording that the declared-surface count is unchanged.

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-existing dirty tree, then author the glob-discovery aggregator</name>
  <files>tests/run-all-240.sh</files>
  <read_first>
    - `tests/run-all-236.sh` IN FULL, 287 lines. 240-PATTERNS.md rates it an EXACT structural match and it is the file being copied. Read all of it, not just the body: lines 20-27 (the glob-not-list header contract), 50-52 (the `found -eq 0` guard's own written justification), 114-167 (`set -uo pipefail`, `run`, `run_may_skip`, `strip_comments`, the two glob loops, the guard), 185-241 (the `must_catch` / `must_not_catch` self-test idiom), 243-281 (the sweep plus its `SWEPT -eq 0` and non-vacuity legs), 283-286 (the footer).
    - `.planning/phases/240-memory/240-PATTERNS.md` the `NEW tests/run-all-240.sh` section, which quotes the exact body to copy and states that `run_may_skip` is REQUIRED for 240 (unlike 236) because `tests/test-240-memory-store-hermetic-fence.sh` is a `.sh` leg.
    - `.planning/phases/240-memory/240-VALIDATION.md` Wave 0 Requirements, which names the four expected `tests/test-240-*` files.
    - `.planning/phases/240-memory/240-RESEARCH.md` Runtime State Inventory, the "Working-tree state (planner hazard)" row.
  </read_first>
  <action>
    FIRST, before writing anything, capture the pre-existing dirty working tree into the SUMMARY: run `git status --short` and `git stash list`, and transcribe the output verbatim. Do NOT stash, do NOT revert, do NOT clean. The statusline modifications and the untracked `.planning/debug/*.md` files are foreign to Phase 240. Every commit in this phase must stage ONLY its own named files by explicit path, never `git add -A` and never `git add .`. State this in the SUMMARY as a standing constraint for the whole phase.

    Create `tests/run-all-240.sh`, executable, `set -uo pipefail` (NOT `-e`, per the aggregator convention: an inner failing leg must not terminate the harness).

    Header, in Phase 240's own words, not copied prose. It must state four things: (1) discovery is by GLOB over `tests/test-240-*` for both `.cjs` and `.sh`, so adding a Phase 240 test requires no edit here, and there is deliberately no hand-maintained execution list because a list is a second place to forget something; (2) the four mandatory test filenames enumerated by name so a missing one is visible by reading, each tagged with its requirement and its owning plan: `test-240-memory-store-hermetic-fence.sh` (MEM-03, plan 240-02), `test-240-jtbd-manual-override-roundtrip.cjs` (MEM-01, plan 240-03), `test-240-jtbd-continuous-promotion.cjs` (MEM-01, plan 240-04), `test-240-jtbd-event-survives-rebuild.cjs` (MEM-02, plan 240-05); (3) that the `found -eq 0` guard is load-bearing and must not be softened, because a harness that discovers nothing must FAIL rather than print green, which is the same false-success shape this milestone exists to close; (4) that plan 240-04 appends two further source tripwire legs to this file, so a reader of this header knows the leg list is not final until Wave 3.

    Body: copy the `run`, `run_may_skip` and `strip_comments` helper shapes from `run-all-236.sh:121-150` verbatim in structure. `run_may_skip` tallies exit 0 plus a line matching `^SKIP` as SKIP rather than PASS. `strip_comments` is `grep -vE '^[[:space:]]*(//|\*|/\*|#)'` so header prose can neither trip a grep nor satisfy one.

    Discovery: `shopt -s nullglob`, `found=0`, one loop over `tests/test-240-*.cjs` invoking `run "$(basename "$t")" node "$t"`, one loop over `tests/test-240-*.sh` invoking `run_may_skip "$(basename "$t")" bash "$t"`, then `shopt -u nullglob`, then the `if [ "$found" -eq 0 ]` guard printing `!!! no tests/test-240-* files discovered` and `exit 1`.

    Footer: the `======` banner reading `Phase 240: PASS=$PASS FAIL=$FAIL SKIP=$SKIP` and a final `[ "$FAIL" -eq 0 ]`.

    Every leg must be fully hermetic and make ZERO network reach. No emoji. No em-dashes anywhere in the file.

    Do NOT add the MEM-01 source tripwire legs here. They belong to plan 240-04, which lands the fix they guard; a tripwire that is red for two waves makes wave-merge verification unreadable. Say so in the header.
  </action>
  <verify>
    <automated>bash -n tests/run-all-240.sh && bash tests/run-all-240.sh; echo "exit=$? (1 is CORRECT while zero tests/test-240-* files exist, see R-02)"</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short` output from BEFORE any edit is transcribed verbatim in the SUMMARY, and the SUMMARY states that every Phase 240 commit stages named paths only.
    - `bash -n tests/run-all-240.sh` exits 0 (syntax valid).
    - `test -x tests/run-all-240.sh` succeeds.
    - `grep -c 'found -eq 0' tests/run-all-240.sh` is at least 1.
    - `grep -c 'run_may_skip' tests/run-all-240.sh` is at least 2 (the definition and at least one use).
    - `grep -c 'test-240-\*\.sh' tests/run-all-240.sh` is at least 1, and the same for `test-240-*.cjs`, proving discovery is by glob.
    - The header names all four expected test filenames: `grep -c 'test-240-memory-store-hermetic-fence.sh' tests/run-all-240.sh`, `... test-240-jtbd-manual-override-roundtrip.cjs`, `... test-240-jtbd-continuous-promotion.cjs`, `... test-240-jtbd-event-survives-rebuild.cjs` are each at least 1.
    - There is no hand-maintained array of test filenames used for EXECUTION: `grep -cE '^(SUITES|CJS_SUITES|SHELL_SUITES|TESTS)=\(' tests/run-all-240.sh` is 0.
    - `grep -cP '\x{2014}' tests/run-all-240.sh` is 0 (zero em-dashes).
    - Running the harness with zero `tests/test-240-*` files on disk prints `no tests/test-240-* files discovered` and exits 1. Transcribe the observed output in the SUMMARY.
  </acceptance_criteria>
  <done>The aggregator exists, discovers by glob, and refuses to print green over an empty discovery.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Tri-Polar daemon-parity gate with a self-test that proves it bites</name>
  <files>tests/run-all-240.sh</files>
  <read_first>
    - `tests/run-all-236.sh:185-241`, the `must_catch` / `must_not_catch` pair. This is the repo's canonical "prove the gate bites before trusting it" idiom and both probes must run the EXACT pipeline the real sweep runs.
    - `tests/run-all-236.sh:243-281`, the sweep plus its `SWEPT -eq 0` anti-vacuity guard and its separate non-vacuity leg. Copy the two-part shape: a sweep and a proof the sweep had something to discriminate.
    - `.planning/phases/240-memory/240-PATTERNS.md` Correction 3, which records the measured `grep -rln 'jtbd' lib/mcp/` result and instructs the plan to CITE the grep rather than re-open A7.
    - `.planning/phases/240-memory/240-RESEARCH.md` Assumption A7 and Open Question Q4, for the Phase 241-05 precedent this gate exists to guard against.
    - `hooks/hooks.json` lines 1-10 only, for the `MINDRIAN_MCP_FIRST` dispatch note that makes the daemon path a real second surface rather than a hypothetical one.
  </read_first>
  <action>
    Append two gate legs to `tests/run-all-240.sh`, after the discovery loops and the `found -eq 0` guard, before the footer.

    Leg 1, the parity self-test. Define `PARITY_TMP="$(mktemp -d)"` with a `trap 'rm -rf "$PARITY_TMP"' EXIT`. Define `parity_hits()` taking a directory and returning the output of `grep -rl 'jtbd' "$1" 2>/dev/null || true`. Then two probes, both running that exact function so the self-test cannot diverge from the real sweep: a `must_catch` probe that writes a file containing the literal `jtbd` under `$PARITY_TMP/probe-dirty/` and asserts `parity_hits` reports it, and a `must_not_catch` probe that writes a file containing only unrelated text under `$PARITY_TMP/probe-clean/` and asserts `parity_hits` reports nothing. Tally the pair as one PASS or FAIL leg named `tri-polar parity self-test: the gate actually bites`.

    Leg 2, the parity sweep plus its non-vacuity guard, tallied as ONE leg named `tri-polar parity sweep: lib/mcp carries no divergent JTBD trigger`. Three checks inside it, all three must hold:
      (a) NON-VACUITY FIRST: count `.cjs` files under `lib/mcp/` with `find lib/mcp -type f -name '*.cjs' | wc -l`. If the count is 0, FAIL with the message that `lib/mcp/` is empty or gone, so the parity claim below proves nothing. Print the count on success so the leg's scope is visible in the log.
      (b) `PARITY_HITS="$(parity_hits lib/mcp)"`. If non-empty, FAIL and print each hit, with the message: a JTBD reference has entered the MCP-first daemon path, so the Phase 240 MEM-01 trigger fix may now need a sibling there; re-verify Tri-Polar parity and update this gate deliberately rather than deleting it. Cite Phase 241-05 as the precedent (the shared `mindrian-core` Stop path was blind on Desktop, Cowork AND CLI under the flag).
      (c) On success print the exact evidence sentence so the log is self-documenting: `lib/mcp/ carries zero jtbd references across N .cjs files; the MINDRIAN_MCP_FIRST path routes JTBD promotion through the same lib/hmi/across-session-memory.cjs module, so there is no divergent trigger copy.`

    Both legs increment `PASS` or `FAIL` using the same `PASS=$((PASS+1))` pattern the file already uses, and print `>>> <label>: PASSED` / `FAILED` so the footer tally stays correct.

    Then run `node scripts/check-shape-declaration.cjs` and record its declared-surface count in the SUMMARY, confirming Canon Part 11 CIRS is not applicable to this phase because the count is unchanged and no new invocable surface was born.

    No em-dashes. No emoji. Zero network reach.
  </action>
  <verify>
    <automated>bash -n tests/run-all-240.sh && bash tests/run-all-240.sh 2>&1 | grep -E 'tri-polar parity (self-test|sweep)'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/run-all-240.sh` exits 0.
    - `bash tests/run-all-240.sh` output contains `>>> tri-polar parity self-test: the gate actually bites: PASSED` and `>>> tri-polar parity sweep: lib/mcp carries no divergent JTBD trigger: PASSED`.
    - The sweep's success line prints a NON-ZERO `.cjs` file count for `lib/mcp/`. A count of 0 must FAIL the leg. Prove this by temporarily pointing the non-vacuity `find` at a `mktemp -d` empty directory, observing the leg go red, and restoring. Transcribe both observations.
    - EXECUTED MUTATION, both directions, transcribed verbatim in the SUMMARY: create `lib/mcp/__parity-probe.cjs` containing a single comment line mentioning `jtbd`, re-run the harness, confirm the parity SWEEP leg reports FAILED and names that file, then DELETE the probe file and confirm the leg returns to PASSED. Confirm with `git status --short lib/mcp` that the probe is gone and `lib/mcp/` is clean.
    - `bash tests/run-all-240.sh` still exits 1 overall while zero `tests/test-240-*` files exist, because the `found -eq 0` guard runs before these legs. Confirm the guard is not accidentally bypassed by the appended legs.
    - `node scripts/check-shape-declaration.cjs` runs and its declared-surface count is recorded in the SUMMARY as unchanged.
    - `grep -cP '\x{2014}' tests/run-all-240.sh` is 0.
    - `git status --short` shows ONLY `tests/run-all-240.sh` added relative to the pre-existing dirty set captured in Task 1. No foreign file staged.
  </acceptance_criteria>
  <done>Tri-Polar parity is a standing machine-checked gate with a proven-biting pattern and a non-vacuity floor, and A7 / Q4 are closed without a re-investigation task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source tree -> the parity gate's verdict | The gate's answer is derived entirely from `grep` over `lib/mcp/`, so an empty or relocated `lib/mcp/` silently changes the answer. |
| harness discovery -> the phase gate's green | `bash tests/run-all-240.sh` is the phase's declared gate. A harness that discovers nothing and prints green would certify an unverified phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240-01 | Repudiation | `tests/run-all-240.sh` printing a clean summary over an empty discovery | mitigate | The `found -eq 0` guard exits 1 before any leg runs, its load-bearing status is stated in the header, and Task 1 proves it by observing the real exit-1 while zero `tests/test-240-*` files exist. |
| T-240-02 | Repudiation | the parity gate passing because `lib/mcp/` is empty or moved | mitigate | The non-vacuity check counts `.cjs` files under `lib/mcp/` and FAILS the leg at zero, proven by pointing it at an empty `mktemp -d` and observing red. The count is printed on success so the scope is auditable in the log. |
| T-240-03 | Repudiation | the parity pattern being blind to a real divergent copy | mitigate | A `must_catch` / `must_not_catch` pair runs the identical `parity_hits` function the sweep runs, plus an executed real mutation that plants `lib/mcp/__parity-probe.cjs` and observes the sweep go red, then removes it. |
| T-240-04 | Tampering | header comment prose satisfying or tripping a gate grep | mitigate | `strip_comments` is defined and used by any leg that greps source, per `run-all-236.sh:146-150`. The parity gate greps FILENAMES via `grep -rl`, so a comment mentioning `jtbd` inside `lib/mcp/` is a deliberate catch, not a false positive: any mention there warrants a human re-check of parity. |
| T-240-05 | Tampering | a foreign working-tree change swept into a Phase 240 commit | mitigate | Task 1 transcribes the pre-existing dirty set before editing, and every commit in this phase stages named paths only. `git add -A` and `git add .` are forbidden phase-wide. |
| T-240-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable. Zero packages installed or added. 240-RESEARCH.md Package Legitimacy Audit records that this phase installs nothing and that the Phase 87 zero-runtime-dependency invariant forbids it; any proposed install is a scope escape. |

ASVS note: no auth, session, access-control or crypto surface is touched by this plan. V7 Error Handling applies only in the sense that the harness must report an empty discovery as an error rather than silence, which T-240-01 covers.
</threat_model>

<verification>
1. `bash -n tests/run-all-240.sh` exits 0.
2. `bash tests/run-all-240.sh` prints both parity legs PASSED.
3. With zero `tests/test-240-*` files on disk, the harness prints `no tests/test-240-* files discovered` and exits 1; observed output transcribed.
4. Executed parity mutation transcribed in both directions, and `lib/mcp/` confirmed clean afterward.
5. Executed non-vacuity mutation (empty directory) transcribed in both directions.
6. `node scripts/check-shape-declaration.cjs` declared-surface count unchanged; Canon Part 11 recorded as not applicable to this phase.
7. Zero em-dashes in `tests/run-all-240.sh`.
</verification>

<success_criteria>
- The Phase 240 harness exists, discovers by glob, and cannot print green over an empty discovery.
- Tri-Polar daemon parity is a standing gate with a proven-biting pattern and a non-vacuity floor, closing 240-RESEARCH.md A7 / Q4 without a re-investigation task.
- The pre-existing foreign working-tree changes are recorded and provably excluded from this phase's commits.
</success_criteria>

<output>
Create `.planning/phases/240-memory/240-01-SUMMARY.md` when done.
</output>
