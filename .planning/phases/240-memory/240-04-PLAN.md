---
phase: 240-memory
plan: 04
type: execute
wave: 3
depends_on: ["240-03"]
files_modified:
  - scripts/jtbd-update.cjs
  - tests/test-240-jtbd-continuous-promotion.cjs
  - tests/run-all-240.sh
autonomous: true
requirements: [MEM-01]

must_haves:
  truths:
    - "N consecutive same-topic turns through the REAL scripts/jtbd-update.cjs hook produce a Layer 2 in_flight row, where today they produce zero"
    - "The trigger is a turn-count signal that grows on non-transition turns, decoupled from setCurrent's topic-change gate"
    - "setCurrent and the SENS-05 reweight still fire ONLY on a real transition, so Phase 100 behavior above the additive block stays byte-identical: no per-turn history row, no entered_at drift"
    - "The test proves a real classification actually occurred at or above threshold before it asserts anything about promotion, so it cannot pass vacuously on a room where the operator gate fired first"
    - "The test seeds an operator in the target JTBD's affinity set rather than lowering the JUST_TALK 0.8 threshold: the fix is test-side, production threshold logic is untouched"
    - "Turns past the threshold produce exactly ONE in_flight row, updated in place, not one row per turn"
    - "Restoring the unconditional early return turns the gate red, and so does removing the persisted turn_count"
  artifacts:
    - path: "scripts/jtbd-update.cjs"
      provides: "A named transition boolean replacing the unconditional early return, plus a bumpTurnCount call on the non-transition branch"
      contains: "bumpTurnCount"
    - path: "tests/test-240-jtbd-continuous-promotion.cjs"
      provides: "MEM-01 SC1 end-to-end proof driving the real hook N times with a non-vacuity classification guard"
      min_lines: 200
      contains: "MINDRIAN_ROOMS_HOME"
    - path: "tests/run-all-240.sh"
      provides: "Reachability and counter-persistence source tripwire legs"
      contains: "isTransition"
  key_links:
    - from: "scripts/jtbd-update.cjs non-transition branch"
      to: "lib/hmi/jtbd-state.cjs bumpTurnCount"
      via: "a direct call on the same-jtbd turn, gated by the classified jtbd"
      pattern: "jtbdState\\.bumpTurnCount"
    - from: "scripts/jtbd-update.cjs Phase 103-05 additive block"
      to: "lib/hmi/across-session-memory.cjs promoteIfEligible"
      via: "now reachable on non-transition turns because the early return became a boolean"
      pattern: "acrossSession\\.promoteIfEligible"
---

<objective>
Close the REACHABILITY half of MEM-01's deadlock and prove SC1 end to end through the real hook.

Purpose: 240-RESEARCH.md Finding 1 proved the deadlock live. Twelve consecutive same-topic turns against a seeded room produced eleven `no transition` early returns, exactly ONE promotion attempt (on turn 1, which failed at `turnCount = 1`), zero Layer 2 rows, and no `room.db` at all. The Phase 103-05 promotion block sits at `scripts/jtbd-update.cjs:213-257`, BELOW the unconditional `return` at `:167-170`, so on continuous work the promotion is not merely gated, it is never invoked. Corroborated by the real machine: `~/MindrianRooms/.memory/jtbd-history.json` reads `"rooms": {}` and all five lines in the real `audit.log` carry a test slug. In roughly two months of real use, not one genuine user promotion has ever occurred.

Output: a reachable promotion block, a per-turn counter tick, the SC1 end-to-end test, and two source tripwire legs on the phase harness.

**Locked navigator decision D-3, honoured exactly: the JUST_TALK 0.8 threshold fix is TEST-SIDE, NOT a production-code change.** 240-PATTERNS.md Correction 1 establishes two things. The file is `lib/conversation/operator.cjs` (`defaultState` at 115-129 returns `current: 'JUST_TALK'`, `getCurrent` at 132-134 returns that default when the file is absent), NOT `lib/hmi/operator.cjs`. And neither that file nor `lib/hmi/jtbd-classifier.cjs` is a Phase 240 modify target. The `const threshold = (operator === 'JUST_TALK') ? 0.8 : 0.6;` at `jtbd-classifier.cjs:193` is a deliberate anti-false-positive choice for casual conversation mode, and lowering it globally would change behavior for every JUST_TALK user to make one test convenient. **Both files are READ-ONLY reference for this phase. A diff touching either is a scope escape and must be rejected.** The test instead seeds `<roomDir>/.mindrian/conversation-operator.json` with an operator in the target JTBD's affinity set, verified at planning time: `decide-pursue`'s `operator_affinity` is `["BUILD_ROOM","METHODOLOGY"]`, and `explore`'s is `["JUST_TALK","EXPLORE_CAPTURE"]`, which is exactly why the bare default both raises the threshold to 0.8 AND boosts the `explore` fallback above any token-stratum candidate.

**R-11, the early return becomes a NAMED BOOLEAN and `setCurrent` plus the SENS-05 reweight stay behind it. This is Pitfall 3 and it is the single most dangerous edit in the phase.** The `return` at `:167-170` currently guards THREE things at once: the `setCurrent` write at `:175-181`, the SENS-05 reweight fire at `:191-210`, and the Phase 103-05 promotion at `:213-257`. Removing it outright unblocks all three, which would rewrite `entered_at` on every turn (silently destroying the dwell signal), append one history row per turn, and blow through `HISTORY_MAX = 50`. The file states its own contract at `:214-215`: "Phase 100 Stop hook behavior remains byte-identical above." Preserve it literally.

**R-12, the promotion block does NOT move. Only the guard changes shape.** 240-RESEARCH.md:398 names the minimal shape in its own words: "convert `:167-170`'s `return` into a boolean (skip only `setCurrent` and the SENS-05 reweight) rather than moving the block, so Phase 100's documented byte-identical above contract is visibly preserved." Relocating the block above `setCurrent` is forbidden: it would make the promotion read a `current` that predates the transition.

**R-13, promotion fires on every turn past the threshold, not only on the crossing, and the test asserts the bound instead.** 240-RESEARCH.md Open Question Q3 raises the write-volume worry and offers crossing-only as a mitigation. This plan declines it, for a stated reason: `promoteIfEligible` already UPDATES an existing `in_flight` row in place rather than appending (`across-session-memory.cjs:412-416`), so growth is bounded to one row per JTBD per room regardless of turn count, and adding crossing-only logic would need new state to remember whether the threshold was already crossed, which is more surface for no data-shape gain. What this plan owes instead is the assertion: Task 2 drives 6 turns and asserts the store holds exactly ONE `in_flight` entry for that JTBD with a `turn_count` that grew. Carry the contention risk to plan 240-06's deferred-items register with the Q3 citation.

**R-14, the MEM-03 fence is re-run in Task 3 and the deferred mutation from plan 240-02 Task 2 is executed there.** Plan 240-02 sandboxed `tests/test-jtbd-hook-integration.cjs` pre-emptively and recorded honestly that the mutation could not be reddened yet, because `promoteIfEligible` still returned null before any store write. After THIS plan lands, it can. Task 3 owns that executed mutation.

**Pitfall 7, restated because this plan is the one most likely to trip it:** the fix landing on `main` does NOT make it live in the running session. Hooks load from the plugin install cache and never hot-reload (standing HARD RULE, four independent occurrences over three weeks, open RCA at `.planning/debug/live-session-running-stale-plugin-cache-fixes-inert.md`). Every SC1 observation in this plan runs `node /home/jsagi/dev/MindrianOS-Plugin/scripts/jtbd-update.cjs` directly with an explicit `MINDRIAN_ROOMS_HOME` and `CLAUDE_ACTIVE_ROOM`, exactly as every probe in the research did. Never conclude anything from in-session hook behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/240-memory/240-RESEARCH.md
@.planning/phases/240-memory/240-PATTERNS.md
@.planning/phases/240-memory/240-VALIDATION.md
@.planning/phases/240-memory/240-03-SUMMARY.md
</context>

## Artifacts this plan produces

| Symbol | Kind | Location |
|---|---|---|
| `transitioned` boolean (replaces the unconditional early return) | local control-flow change | `scripts/jtbd-update.cjs` |
| `tests/test-240-jtbd-continuous-promotion.cjs` | new file (test) | `tests/` |
| reachability source tripwire leg | gate leg appended to the harness | `tests/run-all-240.sh` |
| counter-persistence source tripwire leg | gate leg appended to the harness | `tests/run-all-240.sh` |

The full phase-wide inventory is in `240-01-PLAN.md`. Canon Part 11 (CIRS) is NOT APPLICABLE: `scripts/jtbd-update.cjs` is an already-registered hook whose `hooks/hooks.json` entries are unchanged, so no invocable surface is born. `hooks/hooks.json` gets ZERO edits in this plan; confirm with `git diff --stat hooks/hooks.json` being empty.

<tasks>

<task type="auto">
  <name>Task 1: Convert the unconditional early return into a named boolean and tick the counter on a same-topic turn</name>
  <files>scripts/jtbd-update.cjs</files>
  <read_first>
    - `scripts/jtbd-update.cjs` IN FULL, 274 lines. Every relevant site: the header contract at 11-16, `isTransition` at 115-122, `main` at 124, the classifier-null early return at 162-165, THE EDIT SITE at 167-170, the `setCurrent` call at 175-181, the SENS-05 reweight block at 183-211, the Phase 103-05 additive block at 213-257 with its `byte-identical above` comment at 214-215 and its never-throw catch at 252-256, the debug line at 259-263, and the final defensive net at 266-273.
    - `.planning/phases/240-memory/240-PATTERNS.md` the `scripts/jtbd-update.cjs` section, which quotes the early return verbatim (note the source literally contains the character `±`, not an em-dash, so the CLAUDE.md sweep is clean there today and that line must not be "fixed" unless it is rewritten anyway), quotes `isTransition` verbatim, names the two things that MUST stay behind the boolean, and quotes the never-throw envelope verbatim.
    - `lib/hmi/jtbd-state.cjs` as edited by plan 240-03, specifically the new `bumpTurnCount(roomDir, expectedJtbd)` and its documented contract and return values.
    - `.planning/phases/240-memory/240-03-SUMMARY.md`, to confirm `bumpTurnCount` landed as a TOP-LEVEL export (not under `_internal`) and to read the observed round-trip key list.
    - `.planning/phases/240-memory/240-RESEARCH.md` Pitfall 2 and Pitfall 3 IN FULL, plus Finding 1's recommended-trigger subsection.
    - `scripts/memory-resume-nudge.cjs:90-123`, `backfillFromWithinSession`. It already bypasses the topic-change gate and calls `promoteIfEligible` at line 119 with the same `current` object, so it is fixed for free by plan 240-03's persisted field. Read it to confirm it needs NO edit, and record that confirmation. Do not edit it.
  </read_first>
  <action>
    Make exactly one control-flow change plus one call. Nothing moves.

    Replace the block at 167-170. Instead of `if (!isTransition(current, result)) { debugLog(...); return; }`, compute `const transitioned = isTransition(current, result);` and then an `if (!transitioned) { ... } ` branch that does two things and does NOT return: keep the existing `debugLog(roomDir, 'no transition; same jtbd, delta within ±0.15')` line byte-identical (the test in Task 2 observes that exact string as one of its three distinguishable log states), and call `jtbdState.bumpTurnCount(roomDir, result.jtbd)`. Wrap the `bumpTurnCount` call in its own `try` / `catch (_e) {}` so a counter failure can never escape into a hook that runs on every user turn, even though `bumpTurnCount` is itself already never-throw. Optionally extend the existing debug line with the returned count via a SEPARATE `debugLog` call rather than editing the original string.

    Then guard the two Phase 100 behaviors with `if (transitioned) { ... }`, containing BOTH the `jtbdState.setCurrent` call at 175-181 AND the entire SENS-05 reweight block at 183-211 including its own `try` / `catch`. Neither may run on a non-transition turn. Keep the SENS-05 comment block intact.

    Leave the Phase 103-05 additive block at 213-257 EXACTLY where it is, at the same indentation level as before, now reached on both paths. Do not move it, do not re-indent it into the `if (transitioned)` branch, do not change its `try` / `catch`, do not change its `roomSlug` resolution, and do not change its `turnCount` computation at 242-244: that computation already prefers `cur.turn_count` when it is a number and, as of plan 240-03, it now is. Update ONLY the comment at 214-215 to say that the block is now reached on non-transition turns too, that `setCurrent` and the SENS-05 reweight remain behind the transition boolean so Phase 100 behavior above is still byte-identical, and to cite MEM-01 plus plan 240-04.

    Update the file's header contract paragraph at lines 11-16 to describe the new two-path shape: a transition still writes state and fires the reweight; a non-transition turn now ticks `current.turn_count` and still reaches the across-session promotion gate. State that this is the MEM-01 fix and that the trigger is a turn count decoupled from the topic-change gate.

    Do NOT change `isTransition` or `CONFIDENCE_DELTA_THRESHOLD`. Do NOT change the classifier-null early return at 162-165: a below-threshold turn correctly does nothing, and ticking a counter for a JTBD the classifier did not identify would be inventing signal. Do NOT edit `lib/hmi/jtbd-classifier.cjs`, `lib/conversation/operator.cjs`, `hooks/hooks.json`, `scripts/memory-resume-nudge.cjs`, or `scripts/jtbd-command.cjs`.

    No em-dashes in new prose. Do not convert the existing `±` character.
  </action>
  <verify>
    <automated>node -c scripts/jtbd-update.cjs 2>/dev/null || node --check scripts/jtbd-update.cjs; node tests/test-jtbd-hook-integration.cjs && node tests/test-connector-tier-d-hooks.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/jtbd-update.cjs` exits 0.
    - `node tests/test-jtbd-hook-integration.cjs` exits 0 with all 9 classes passing, and class 8's reported `max` is still under 500 ms. Transcribe the class 8 line: this is the direct evidence that making the promotion block reachable did not blow the latency budget.
    - `node tests/test-connector-tier-d-hooks.cjs` exits 0. It asserts at source level that `jtbd-update.cjs` still routes to `sensor-jtbd-reweight` through the navigation chokepoint, so moving the reweight behind the boolean must not break the wiring assertion.
    - `node tests/test-jtbd-classifier.cjs`, `node tests/test-hmi-poll-hook.cjs` and `node tests/test-hmi-compliance-e2e.cjs` behave as before. For the last one, confirm the SAME pre-existing failure set (240-RESEARCH.md Finding 4 records it exits 1 while hermetic) and transcribe the failure names so a new failure is distinguishable from the old ones.
    - PITFALL 3 OBSERVED DIRECTLY, not inferred. Against a `mktemp -d` room seeded with an operator in `decide-pursue`'s affinity set, drive the hook FOUR times with the same cue message under an explicit hermetic `MINDRIAN_ROOMS_HOME`. Then read `<roomDir>/.mindrian/jtbd-state.json` and assert and transcribe: `history.length` is exactly 1 (one transition row, no per-turn rows); `current.entered_at` is byte-identical to the value after turn 1; `current.turn_count` is 4. If history grew past 1 or `entered_at` moved, the boolean conversion leaked and the change is wrong.
    - `grep -c 'const transitioned = isTransition(' scripts/jtbd-update.cjs` is 1.
    - `grep -c 'jtbdState.bumpTurnCount' scripts/jtbd-update.cjs` is 1.
    - The unconditional return is gone: `grep -vE '^[[:space:]]*(//|\*|/\*)' scripts/jtbd-update.cjs | grep -A2 -E 'if \(!isTransition\(' | grep -cE '^[[:space:]]*return;[[:space:]]*$'` is 0.
    - `git diff --stat lib/hmi/jtbd-classifier.cjs lib/conversation/operator.cjs hooks/hooks.json scripts/memory-resume-nudge.cjs scripts/jtbd-command.cjs` is EMPTY. This is the D-3 scope-escape check and it is not optional.
    - `scripts/memory-resume-nudge.cjs` confirmed to need no edit, with the reason recorded in the SUMMARY.
    - `grep -cP '\x{2014}' scripts/jtbd-update.cjs` is 0, and the pre-existing `±` at the `no transition` debug line is still present.
  </acceptance_criteria>
  <done>The promotion block is reachable on continuous work, the counter ticks per turn, and setCurrent plus the SENS-05 reweight provably still fire only on a real transition.</done>
</task>

<task type="auto">
  <name>Task 2: Prove SC1 end to end through the real hook, with a non-vacuity guard and a write-volume bound</name>
  <files>tests/test-240-jtbd-continuous-promotion.cjs</files>
  <read_first>
    - `tests/test-memory-hook-integration.cjs:95-130`, for the `readMemoryFile` reader idiom and the explicit child-env-injection shape `env: Object.assign({}, process.env, { MINDRIAN_ROOMS_HOME: home })`. Explicit injection, never inheritance.
    - `tests/test-jtbd-auto-anchor-empirical.sh:118-152` as edited by plan 240-02, for the operator seed JSON and the empirically-proven 4-cue message. The exact cue string `should we pursue this idea, pivot or double down or kill it or keep it` was measured to score 0.800 with `BUILD_ROOM` seeded. Port both to CJS.
    - `scripts/jtbd-update.cjs` as edited in Task 1, specifically: `main` returns immediately unless `process.argv[2]` is `userprompt` or `stop` (126-127); the message is read from `CLAUDE_USER_MESSAGE` or a stdin JSON envelope (80-94); the debug log path is `<roomDir>/.mindrian/jtbd-update.log` under `MINDRIAN_DEBUG=1` (68-75).
    - `lib/hmi/jtbd-taxonomy.json`, the `entries` array. Confirm for yourself that `decide-pursue` has `operator_affinity` `["BUILD_ROOM","METHODOLOGY"]` and the six cues, and that `explore` has `operator_affinity` `["JUST_TALK","EXPLORE_CAPTURE"]` with an empty cue list. This is why the operator seed is load-bearing.
    - `lib/core/resolve-active-room.cjs`, to confirm how `CLAUDE_ACTIVE_ROOM` overrides room resolution so the test can point the hook at a temp room with no registry entry.
    - `.planning/phases/240-memory/240-RESEARCH.md` Pitfall 1 IN FULL, including its LIVE PROOF, and the three distinguishable debug-log states table in 240-PATTERNS.md's `NEW tests/test-240-jtbd-continuous-promotion.cjs` section.
    - `.planning/phases/240-memory/240-VALIDATION.md` rows `240-01-01`, `240-01-02` and `240-03-01`, the three requirements this one file answers.
  </read_first>
  <action>
    Create `tests/test-240-jtbd-continuous-promotion.cjs`. Header must state, in Phase 240's own words: this file answers MEM-01 SC1's continuous-work leg; it drives the REAL `scripts/jtbd-update.cjs` via `spawnSync` rather than calling library functions, because the defect was reachability inside the hook and a library-level test cannot see it; it seeds the operator rather than lowering the JUST_TALK threshold, per locked decision D-3, and both `lib/hmi/jtbd-classifier.cjs` and `lib/conversation/operator.cjs` are READ-ONLY for this phase; and it asserts a real classification happened BEFORE asserting promotion, because Pitfall 1 showed twelve turns on a bare room logging `below_threshold, top:decide-pursue@0.60` every time, so a test without that guard measures nothing and, worse, would pass on the FIRST run against unmodified source.

    Harness: reuse the `pass` / `fail` / `assert(cond, label, err)` counter and `process.exit(failed === 0 ? 0 : 1)` tail from `tests/test-jtbd-transition-graph-wiring.cjs:31-40, 246-255`.

    Fixture builder `makeSeededRoom()`: `fs.mkdtempSync` a rooms home, create `<home>/<slug>` with `.mindrian/`, write a `.room-root` sentinel, write `<room>/.mindrian/conversation-operator.json` with `current: 'BUILD_ROOM'` and the same shape as the shell suite's seed, and optionally a `STATE.md`. Do NOT write a `.rooms/registry.json`: leaving it absent is deliberate, because `resolveRoomDirForSlug` then returns null and `logGraphTransition` is a graceful no-op, which keeps the test fast and keeps its subject the Layer 2 store rather than `room.db`. Say so in a comment. Return `{ home, roomDir, slug }`.

    Driver `runHook(home, roomDir, message)`: `spawnSync(process.execPath, [HOOK_SCRIPT, 'userprompt'], { env: Object.assign({}, process.env, { MINDRIAN_ROOMS_HOME: home, CLAUDE_ACTIVE_ROOM: roomDir, CLAUDE_USER_MESSAGE: message, MINDRIAN_DEBUG: '1' }), encoding: 'utf8', input: '' })`. Return the result. `argv[2]` must be the literal `userprompt`.

    Log reader `readDebugLog(roomDir)`: read `<roomDir>/.mindrian/jtbd-update.log`, returning `''` when absent.

    Five tests.

    Test 1, THE NON-VACUITY GUARD, and it runs first. Seed a room, drive ONE turn with the 4-cue message. Assert the hook exited 0. Then assert on the debug log that a REAL classification happened: it must contain a line matching `event=userprompt jtbd=decide-pursue conf=0.8`, and it must NOT contain `classify null/below-threshold`. Fail with a message naming Pitfall 1 and instructing the reader to check the operator seed and the taxonomy affinity if this leg is red. Every test below depends on this leg, so state that in the label.

    Test 2, THE ANTI-VACUITY CONTROL, which proves the guard in test 1 is not decorative. Seed a room with NO operator file at all, so `operator.getCurrent` returns the `JUST_TALK` default and the threshold is 0.8. Drive one turn with the same cue message. Assert the debug log DOES contain `classify null/below-threshold` and that `<roomDir>/.mindrian/jtbd-state.json` was NOT created. This reproduces Pitfall 1 under observation and demonstrates that the operator seed in test 1 is what makes the rest of the file meaningful.

    Test 3, SC1 PROPER. Seed a room, drive the SAME cue message SIX consecutive times. Assert in order: the first turn's log carries the `event=userprompt jtbd=decide-pursue` line (a real transition); turns 2 through 6 each produce a `no transition; same jtbd` line, so the count of that substring in the log is 5 (this is the direct evidence the non-transition path was taken, not that the classifier changed its mind); `<roomDir>/.mindrian/jtbd-state.json` has `current.turn_count === 6`, `history.length === 1`, and `current.entered_at` byte-identical to its post-turn-1 value; and finally `<home>/.memory/jtbd-history.json` exists with `rooms[slug].in_flight` containing an entry whose `jtbd` is `decide-pursue`. That last assertion is SC1: today it is absent after any number of turns.

    Test 4, THE WRITE-VOLUME BOUND (240-RESEARCH.md Q3, resolution R-13). From test 3's store, assert `rooms[slug].in_flight` has exactly ONE entry for `decide-pursue`, that its `turn_count` is greater than or equal to 3, and that `rooms[slug].in_flight.length` is 1. Six turns past the threshold must produce one in-place-updated row, not six rows. Also assert `<home>/.memory/audit.log` line count is bounded and record the observed count, so a future change in write volume is visible in the SUMMARY rather than invisible.

    Test 5, THE THRESHOLD IS NOT CROSSED EARLY. Fresh room. Drive exactly TWO turns. Assert `current.turn_count === 2` and that `<home>/.memory/jtbd-history.json` either does not exist or has no `in_flight` entry for `decide-pursue`, because `NOISE_FLOOR_TURNS` is 3. This is the false-positive guard: without it, a change that promoted on every turn regardless of the floor would look identical to a correct fix.

    Hermeticity: every test uses its own `mkdtempSync` home injected explicitly into the child env, and each is removed in a `finally`. Assert nothing about the real store. Zero network reach. No em-dashes. No emoji.

    TWO MUTATIONS, each executed live, observed, transcribed verbatim, and reverted:
      M4: reinstate the unconditional `return;` inside the `if (!transitioned)` branch in `scripts/jtbd-update.cjs`. Expect tests 3, 4 and 5's turn_count assertions to change and test 3's Layer 2 assertion to go RED. Record exactly which assertions reddened and their messages.
      M5: delete `turn_count: 1` from `newCurrent` in `lib/hmi/jtbd-state.cjs` AND make `bumpTurnCount` return `null` without writing. Expect test 3 red at the Layer 2 assertion, because the gate falls back to counting history rows targeting the current jtbd and that count stays at 1. Record the observed outcome rather than the predicted one.
    Re-run and confirm green after each revert. Confirm `git diff --quiet lib/ scripts/` at the end so no mutation survived.
  </action>
  <verify>
    <automated>node tests/test-240-jtbd-continuous-promotion.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-240-jtbd-continuous-promotion.cjs` exits 0 with 5 tests passing. Transcribe the full output.
    - Test 1 asserts on `conf=0.8` and on the ABSENCE of `classify null/below-threshold`. Test 2 asserts the presence of that same string on an unseeded room. Both observed outputs transcribed, so the Pitfall 1 mechanism is demonstrated in both directions.
    - Test 3's `no transition; same jtbd` substring count is exactly 5 across 6 turns, transcribed. This is the proof the non-transition branch was taken.
    - Test 3 asserts `history.length === 1` and `entered_at` byte-identity across 6 turns. Both are Pitfall 3 gates and both must be in the file, not just in the SUMMARY.
    - Test 4 asserts exactly ONE `in_flight` entry, and the observed `audit.log` line count is recorded in the SUMMARY.
    - Test 5 confirms no promotion at `turn_count` 2.
    - THE TEST MUST FAIL AGAINST PRE-FIX SOURCE. Before trusting it, `git stash` this plan's `scripts/jtbd-update.cjs` change (or check out the pre-Task-1 version into a scratch copy) and confirm test 3 goes RED. 240-RESEARCH.md Pitfall 1 states the warning sign plainly: if this gate is green before the fix exists, it is measuring nothing. Transcribe the pre-fix red output, then restore.
    - M4 and M5 each executed, transcribed with observed output, reverted, and green re-confirmed after each.
    - `git diff --quiet lib/ scripts/` passes after all mutations are reverted.
    - Hermetic: `$HOME/MindrianRooms` recursive digest identical before and after the run. Transcribe both.
    - `grep -cP '\x{2014}' tests/test-240-jtbd-continuous-promotion.cjs` is 0.
  </acceptance_criteria>
  <done>SC1 is proven end to end through the real hook, with a non-vacuity guard demonstrated in both directions, a write-volume bound, a noise-floor false-positive guard, and a confirmed pre-fix red.</done>
</task>

<task type="auto">
  <name>Task 3: Add the two source tripwire legs, execute plan 240-02's deferred mutation, and run the full phase regression set</name>
  <files>tests/run-all-240.sh</files>
  <read_first>
    - `tests/run-all-240.sh` as authored by plan 240-01, in full, including the header note stating that this plan appends two further legs and the existing `strip_comments`, `run`, `run_may_skip`, PASS / FAIL tally and parity legs.
    - `tests/run-all-236.sh:243-281`, the sweep plus its `SWEPT -eq 0` guard and its separate non-vacuity leg. Copy the two-part shape: a sweep, and a proof the sweep had something to discriminate.
    - `.planning/phases/240-memory/240-PATTERNS.md` the "Optional regression-gate leg" paragraph in the `NEW tests/run-all-240.sh` section, which names exactly this shape: a source sweep asserting the unconditional `return` has not come back, plus a non-vacuity leg asserting `isTransition` still exists.
    - `.planning/phases/240-memory/240-02-SUMMARY.md`, the recorded deferred mutation for `tests/test-jtbd-hook-integration.cjs`. This task executes it.
    - `.planning/phases/240-memory/240-VALIDATION.md` the Regression Suites table and the Sampling Rate section, for the exact commands and expected results.
  </read_first>
  <action>
    Append two gate legs to `tests/run-all-240.sh`, after the parity legs and before the footer. Both must use the file's existing `strip_comments` helper wherever they grep source, so header prose can neither trip a gate nor satisfy one.

    Leg 3, `MEM-01 reachability: the unconditional early return has not come back`, tallied as ONE leg with three checks that must all hold:
      (a) NON-VACUITY FIRST: `strip_comments scripts/jtbd-update.cjs | grep -c 'isTransition'` must be at least 2 (the definition and the call). At zero or one, FAIL with the message that the predicate this gate discriminates against is gone, so the sweep below proves nothing.
      (b) The named boolean must be present: `strip_comments scripts/jtbd-update.cjs | grep -c 'const transitioned = isTransition('` must be exactly 1.
      (c) The bare early return must be ABSENT: `strip_comments scripts/jtbd-update.cjs | grep -A2 -E 'if \(!isTransition\(' | grep -cE '^[[:space:]]*return;[[:space:]]*$'` must be 0. On failure print the offending context and the message that reinstating the unconditional return restores the MEM-01 deadlock in which the Phase 103-05 promotion block is never invoked on continuous work, citing the live evidence: twelve same-topic turns produced eleven early returns and zero Layer 2 rows.

    Leg 4, `MEM-01 counter persistence: current carries the fields the gate reads`, tallied as ONE leg with four checks:
      (a) NON-VACUITY FIRST: `strip_comments lib/hmi/jtbd-state.cjs | grep -c 'const newCurrent = {'` must be exactly 1. At zero, the literal this gate guards has been restructured and the sweep proves nothing.
      (b) `strip_comments lib/hmi/jtbd-state.cjs | grep -c 'turn_count'` must be at least 1 and `... | grep -c 'manual_set'` must be at least 1.
      (c) `strip_comments lib/hmi/jtbd-state.cjs | grep -c 'function bumpTurnCount'` must be exactly 1.
      (d) `strip_comments scripts/jtbd-update.cjs | grep -c 'bumpTurnCount'` must be at least 1, so the hook actually calls the writer rather than the writer merely existing.
      On failure, name which check failed and cite MEM-01 plus plan 240-03.

    Update the harness header to enumerate the four legs now present (parity self-test, parity sweep, reachability, counter persistence) alongside the four test filenames, so the full leg list is visible by reading.

    THEN execute plan 240-02's deferred mutation, which is now meaningful: remove the `MINDRIAN_ROOMS_HOME` injection from `tests/test-jtbd-hook-integration.cjs`'s `runHook`, run `bash tests/test-240-memory-store-hermetic-fence.sh`, and confirm the fence now reports MUTATED for that suite under the sandboxed HOME (because class 8's ten same-message turns cross `NOISE_FLOOR_TURNS` and write a real `jtbd-history.json` plus an `audit.log` line). Restore the injection, re-run, confirm the fence returns to UNMUTATED. Transcribe both observations. This closes the honestly-deferred proof from plan 240-02 Task 2 and is the direct demonstration that fixing MEM-01 would have opened a new leak.

    THEN run the full phase regression set from 240-VALIDATION.md and record every result with its expected value: `bash tests/run-all-240.sh`, `bash tests/run-all-236.sh` (expect PASS=12 FAIL=0, the MEM-02 hard dependency), `bash tests/run-all-127.3.sh`, `node tests/test-across-session-memory.cjs` (36/36, class 11 flake noted), `node tests/test-jtbd-transition-graph-wiring.cjs` (6 tests / 14 assertions), `node tests/test-memory-hook-integration.cjs` (10/10), `node tests/test-129-spine-substrate.cjs` (15/15), `node tests/test-memory-command.cjs` (24/26 expected, SAME two pre-existing Brain Mode A failures), `node tests/test-150-brain-egress.cjs` (PASS), `node tests/test-jtbd-hook-integration.cjs`, `node tests/test-jtbd-state-io.cjs`, `node tests/test-jtbd-command.cjs`, `node tests/test-connector-tier-d-hooks.cjs`. Any deviation from the expected value must be diagnosed and reported, never rounded to green.

    No em-dashes.
  </action>
  <verify>
    <automated>bash -n tests/run-all-240.sh && bash tests/run-all-240.sh && bash tests/run-all-236.sh && bash tests/run-all-127.3.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/run-all-240.sh` exits 0.
    - `bash tests/run-all-240.sh` exits 0 and its summary shows the three landed `tests/test-240-*` files plus four gate legs, all PASSED. Transcribe the summary line and the full leg list.
    - EXECUTED MUTATION for leg 3, both directions: reinstate the bare `return;` inside the `if (!transitioned)` branch, confirm leg 3 reports FAILED and names the offending context AND that `tests/test-240-jtbd-continuous-promotion.cjs` also goes red, then restore and confirm both PASSED. Transcribe.
    - EXECUTED MUTATION for leg 3's non-vacuity check: temporarily rename `isTransition` to something else, confirm the non-vacuity check FAILS the leg with its written message, restore. Transcribe.
    - EXECUTED MUTATION for leg 4, both directions: delete `function bumpTurnCount` from `lib/hmi/jtbd-state.cjs`, confirm leg 4 reports FAILED naming check (c), restore, confirm PASSED. Transcribe.
    - EXECUTED MUTATION for leg 4's non-vacuity check: temporarily rename `newCurrent`, confirm the non-vacuity check FAILS the leg, restore. Transcribe.
    - PLAN 240-02's DEFERRED MUTATION EXECUTED, both directions, with the fence's own MUTATED and UNMUTATED verdicts transcribed. Confirm afterwards that `tests/test-jtbd-hook-integration.cjs` is restored and `$HOME/MindrianRooms` is byte-identical to its pre-run digest.
    - `bash tests/run-all-236.sh` reports PASS=12 FAIL=0 SKIP=0. This is the MEM-02 hard dependency and any deviation blocks plan 240-05.
    - `bash tests/run-all-127.3.sh` green, and `$HOME/MindrianRooms` digest unchanged across it.
    - Every regression command in the Task 3 list is run and its result recorded against its expected value, including the two named pre-existing `test-memory-command.cjs` failures and the pre-existing `test-hmi-compliance-e2e.cjs` failure set.
    - `git diff --quiet lib/ scripts/` and `git diff --stat hooks/hooks.json` empty after all mutations are reverted.
    - `grep -cP '\x{2014}' tests/run-all-240.sh` is 0.
    - `git status --short` shows only this plan's three named files relative to the dirty set 240-01 recorded.
  </acceptance_criteria>
  <done>Both halves of the MEM-01 fix have permanent source tripwires with proven non-vacuity, plan 240-02's deferred mutation is executed rather than left owed, and the whole phase regression set is recorded against expected values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| every user turn on all three surfaces -> the hook process | `scripts/jtbd-update.cjs` is registered at `hooks/hooks.json:408` (UserPromptSubmit) and `:180` (Stop). Any new throw path or latency here degrades every turn for every user. |
| within-session Layer 1 state -> shared per-USER Layer 2 store | This plan is what makes that crossing actually happen for the first time. |
| test process -> the developer's real per-user store | The test drives the real hook, which now writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240-21 | Denial of Service | the reachability change unblocking `setCurrent` on every turn | mitigate | Pitfall 3, the most dangerous edit in the phase. `setCurrent` and the SENS-05 reweight stay inside `if (transitioned)`. Task 1's acceptance criteria include a direct four-turn observation asserting `history.length === 1` and `entered_at` byte-identity, and Task 2 test 3 pins the same two facts across six turns. Harness leg 3 stops the guard from being deleted later. |
| T-240-22 | Denial of Service | added per-turn latency on a hook that runs on every turn | mitigate | The non-transition path adds one `readState` plus one atomic write plus, past the floor, one lockfile-guarded store update. `logGraphTransition` is a graceful no-op when the room is unregistered, so no `room.db` is opened in the test fixture. Task 1's acceptance criteria require `tests/test-jtbd-hook-integration.cjs` class 8 to stay under its 500 ms budget with the observed max transcribed. |
| T-240-23 | Denial of Service | a `bumpTurnCount` failure escaping into Larry's turn | mitigate | The call is wrapped in its own `try` / `catch (_e) {}` in addition to `bumpTurnCount` being never-throw internally, and the final defensive net at `jtbd-update.cjs:266-273` is untouched. |
| T-240-24 | Tampering | test writing into the live user store now that the write path is live | mitigate | Every test fixture injects `MINDRIAN_ROOMS_HOME` explicitly into the child env. Plan 240-02 already sandboxed the one other suite that drives the real hook, and Task 3 executes the mutation proving that sandbox is load-bearing. The `$HOME/MindrianRooms` digest is asserted unchanged around every run. |
| T-240-25 | Repudiation | an SC1 test that passes without the classifier ever firing | mitigate | Pitfall 1, demonstrated live in the research as twelve `below_threshold` turns. Test 1 asserts a real `conf=0.8` classification and the ABSENCE of `classify null/below-threshold`; test 2 is the control that reproduces the vacuous state on purpose; and the acceptance criteria require confirming the test goes RED against pre-fix source. |
| T-240-26 | Elevation of Privilege | lowering the JUST_TALK 0.8 threshold to make a test pass | mitigate | Locked decision D-3 and 240-PATTERNS.md Correction 1. The fix is entirely test-side (operator affinity seed). Task 1's acceptance criteria include an explicit empty-diff check over `lib/hmi/jtbd-classifier.cjs` and `lib/conversation/operator.cjs`, so a scope escape fails the task rather than passing review. |
| T-240-27 | Denial of Service | unbounded growth of the Layer 2 store and `audit.log` under a far higher promotion rate | accept | 240-RESEARCH.md Q3, resolution R-13. Bounded by the pre-existing in-place `in_flight` update (`across-session-memory.cjs:412-416`), the 100-entry per-room array cap with archive spillover, and `audit.log`'s 10000-line bound with 1 percent sample-rate truncation. Test 4 asserts exactly one `in_flight` row across six turns and records the observed `audit.log` line count. Lock-contention behavior under concurrent Cowork rooms is carried to plan 240-06's deferred-items register with the Q3 citation rather than solved here. |
| T-240-28 | Information Disclosure | Canon Part 8 LOCAL to BRAIN egress from the newly live promotion path | mitigate | `logGraphTransition`'s payload carries `to`, `kind`, `roomSlug`, `created_by`, `source_path` plus scalar extras (`turn_count`, `confidence`, `manual`, `ts`) and is untouched by this plan. `audit.log` lines carry only `timestamp | roomSlug | action | jtbd`. Regression-guarded by `node tests/test-150-brain-egress.cjs` in Task 3's regression set. |
| T-240-29 | Repudiation | concluding the fix works from live in-session hook behavior | mitigate | Pitfall 7 and a standing HARD RULE. Every observation runs `node` against the dev workspace path with explicit `MINDRIAN_ROOMS_HOME` and `CLAUDE_ACTIVE_ROOM`. The plugin install cache is never consulted and nothing in this plan may be verified by typing into the running session. |
| T-240-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable. Zero packages. |

ASVS note: V4 Access Control as the Canon Part 8 boundary, covered by T-240-28. V7 Error Handling as the never-throw envelope, covered by T-240-23. No auth, session, input-validation or crypto surface is newly touched.
</threat_model>

<verification>
1. `node --check scripts/jtbd-update.cjs` exits 0.
2. `node tests/test-240-jtbd-continuous-promotion.cjs` exits 0 with 5 tests; full output transcribed.
3. The test confirmed RED against pre-fix `scripts/jtbd-update.cjs`; pre-fix output transcribed.
4. Four-turn direct observation: `history.length === 1`, `entered_at` byte-identical, `turn_count === 4`.
5. `tests/test-jtbd-hook-integration.cjs` 9/9 with class 8 max under 500 ms, transcribed.
6. `bash tests/run-all-240.sh` green with 3 test files and 4 gate legs.
7. Six executed mutations transcribed: M4, M5, leg 3, leg 3 non-vacuity, leg 4, leg 4 non-vacuity. Plus plan 240-02's deferred fence mutation in both directions.
8. `bash tests/run-all-236.sh` PASS=12 FAIL=0 SKIP=0. `bash tests/run-all-127.3.sh` green.
9. Empty diff over `lib/hmi/jtbd-classifier.cjs`, `lib/conversation/operator.cjs`, `hooks/hooks.json`, `scripts/memory-resume-nudge.cjs`, `scripts/jtbd-command.cjs`.
10. `git diff --quiet lib/ scripts/` after reverts. Zero em-dashes in all three files. `$HOME/MindrianRooms` digest unchanged.
</verification>

<success_criteria>
- Six consecutive same-topic turns through the real hook produce a Layer 2 `in_flight` row, where today any number of turns produces zero.
- The trigger is a turn count that grows on non-transition turns, and `setCurrent` plus the SENS-05 reweight provably still fire only on a real transition.
- The test cannot pass vacuously: a real classification above threshold is asserted first, the vacuous state is reproduced on purpose as a control, and the test is confirmed red against pre-fix source.
- The JUST_TALK threshold problem is closed test-side with an empty diff over both production threshold files.
- Turns past the floor produce one in-place-updated row, not one row per turn.
- Both halves of the fix have permanent source tripwires whose non-vacuity checks are proven to bite.
</success_criteria>

<output>
Create `.planning/phases/240-memory/240-04-SUMMARY.md` when done.
</output>
