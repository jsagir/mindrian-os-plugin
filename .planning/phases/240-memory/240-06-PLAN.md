---
phase: 240-memory
plan: 06
type: execute
wave: 4
depends_on: ["240-02", "240-04", "240-05"]
files_modified:
  - .planning/phases/240-memory/deferred-items.md
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03]

must_haves:
  truths:
    - "Every residual this phase deliberately did not fix is written down with its citation and its reason, so no finding dies in a SUMMARY nobody re-reads"
    - "The Dev-Research Compositing mandate is honoured: this phase's reasoning trail exists in the rethinking-mindrianos room alongside the three sibling 2026-07-28 RCAs, cross-linked back to the phase"
    - "The frozen 13-line graph-edge-pending.log residue is documented as dead history with no live producer or consumer, and confirmed still 13 lines and still unwritten after the whole phase ran"
    - "The full phase gate is executed and every result is recorded against its expected value, with no deviation rounded to green"
    - "Zero em-dashes across every file this phase touched, verified by an executed sweep rather than asserted"
  artifacts:
    - path: ".planning/phases/240-memory/deferred-items.md"
      provides: "The residual register for every finding this phase surfaced and deliberately did not fix"
      min_lines: 60
  key_links:
    - from: ".planning/phases/240-memory/deferred-items.md"
      to: "~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-phase-240-memory/"
      via: "cross-reference in both directions, per the CLAUDE.md Dev-Research Compositing mandate"
      pattern: "rethinking-mindrianos"
---

<objective>
Close the phase honestly: write down every residual, honour the Dev-Research Compositing mandate, and run the phase gate with every result recorded against its expected value.

Purpose: this phase surfaced eight findings it deliberately did not fix and made four design decisions whose reasons live only in plan prose. CLAUDE.md carries a standing mandate that any GSD phase touching MindrianOS's own architecture composites with the `rethinking-mindrianos` room, and it says the failure mode explicitly: a phase artifact that only says "per the room's research" without the citation, and a room entry that reaches a verdict and never lands in a phase, both count as incomplete. Phase 240 is squarely MindrianOS-own-architecture work, and the three sibling 2026-07-28 RCAs are already filed in that room.

Output: a residual register and a dated research entry, plus an executed phase gate.

**This plan writes ZERO code and ZERO tests.** `git diff --quiet lib/ scripts/ tests/` must pass at the end. It also does NOT write `STATE.md`, `ROADMAP.md` or `REQUIREMENTS.md`: phase-level closure is the orchestrator's step after `gsd-verifier`, and Phase 236 was explicit that leaving the ROADMAP checkbox unchecked until verification is correct rather than a shortfall.

**Pitfall 8, and it is why the tracking-write boundary above is drawn where it is.** `gsd-tools.cjs state.record-session` has an open bug (`.planning/debug/gsd-phase-complete-cross-phase-corruption.md`): it overwrites `stopped_at` and `last_activity` with a stale unrelated value read from the Session Continuity block instead of the argument passed to it. It was hit and hand-corrected FOUR separate times during Phase 236 alone and again in 239. The convergent discipline across 236, 237 and 239 is to hand-edit tracking files directly and `git diff`-review every write. Additionally, the harness classifier blocks any Bash argv containing the literal token that means "finished", which was hit in both 236 and 237. This plan therefore calls no `gsd-tools` write verb at all, and if the orchestrator later needs a tracking write it hand-edits.

**Pitfall 9 note:** `.planning/config.json` sets `use_worktrees: false`, so the worktree base-mismatch FATAL recorded in Phase 239 should not bite. But `parallelization: true`, so if any dispatch in this phase DID use worktree isolation, `Agent(isolation="worktree")` bases new worktrees off `origin/main` rather than local `HEAD`, and this repo pushes only at release time. If a worktree dispatch is used, push local `main` to `origin` after confirming a clean fast-forward BEFORE dispatching.

**`.planning/` is gitignored** (`.gitignore` line 79 is `.planning/*`), so committing the residual register needs `git add -f`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/240-memory/240-RESEARCH.md
@.planning/phases/240-memory/240-PATTERNS.md
@.planning/phases/240-memory/240-VALIDATION.md
@.planning/phases/240-memory/240-02-SUMMARY.md
@.planning/phases/240-memory/240-03-SUMMARY.md
@.planning/phases/240-memory/240-04-SUMMARY.md
@.planning/phases/240-memory/240-05-SUMMARY.md
</context>

## Artifacts this plan produces

| Symbol | Kind | Location |
|---|---|---|
| `.planning/phases/240-memory/deferred-items.md` | new file (residual register) | `.planning/phases/240-memory/` |
| `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-phase-240-memory/` | new directory (research trail, outside the repo) | the rethinking-mindrianos Data Room |

The full phase-wide inventory is in `240-01-PLAN.md`. Canon Part 11 (CIRS) is NOT APPLICABLE to this phase; confirm one final time in Task 2.

<tasks>

<task type="auto">
  <name>Task 1: Write the residual register and file the Dev-Research Compositing trail</name>
  <files>.planning/phases/240-memory/deferred-items.md</files>
  <read_first>
    - All four prior SUMMARYs for this phase (`240-02`, `240-03`, `240-04`, `240-05`) plus `240-01-SUMMARY.md`. Any finding an executor recorded that is not in the list below must ALSO be captured; the list below is the floor, not the ceiling.
    - `.planning/phases/237-reach-mechanism/deferred-items.md` for the house format of a residual register in this repo. Match its structure rather than inventing one.
    - `.planning/phases/240-memory/240-RESEARCH.md` Assumptions Log (A1 through A7) and Open Questions (Q1 through Q4) IN FULL. Every item there needs a disposition in the register: closed, accepted, or deferred with a citation.
    - `.planning/debug/resolved/graph-edge-pending-undrained-dead-letter-queue.md`, read enough to confirm its `resolved` status and its Option A rejection, since the register cites it as the authority for not backfilling.
    - `./CLAUDE.md` the Dev-Research Compositing section IN FULL, for the two-homes rule and the exact failure mode it names.
    - `ls ~/MindrianRooms/rethinking-mindrianos/research/`, to see the naming convention of the five existing 2026-07-28 entries and to match it.
  </read_first>
  <action>
    Create `.planning/phases/240-memory/deferred-items.md` in the house format. It must carry every item below, each with: a short title, the requirement or research citation, what was observed, why it was NOT fixed in this phase, and what closing it would take. Do not compress an item into a single sentence that loses the reason; a register whose entries cannot be acted on is a list, not a register.

    Items, at minimum:
      D-240-01, `tests/test-navigation-chokepoint-hook.cjs` writes into the real `$HOME/.mindrian/telemetry/navigation-bypass.jsonl`. 240-RESEARCH.md Finding 4 conclusion 4 and Assumption A4. Same defect class as MEM-03 but a DIFFERENT store, outside MEM-03's literal wording ("memory store"). Its `test6_runtimeSoftDefenseJsonl` deliberately writes to `os.homedir()` and asserts the line count grew. Closing it means the same sandboxed-root treatment plan 240-02 applied to the two JTBD suites.
      D-240-02, `scripts/memory-resume-nudge.cjs:93` and `:127` each resolve `MINDRIAN_ROOMS_HOME` with their OWN inline fallback rather than calling the module's `ROOMS_HOME()`. 240-RESEARCH.md Runtime State Inventory, secrets row. A third copy of the same resolution and a latent drift site. Canon Part 7 consolidation candidate; out of MEM scope.
      D-240-03, `/mos:jtbd set` still does not promote immediately. 240-RESEARCH.md Assumption A5 and plan 240-03 R-10. `scripts/jtbd-command.cjs` has zero `promoteIfEligible` call sites, so a manual set produces no Layer 2 row until the next `jtbd-update` turn or the next SessionStart backfill. SC1 was read as a field-persistence requirement, not an immediacy requirement. Closing it is one call site plus one test.
      D-240-04, a `manual_set: true` can outlive its `expires_at` window. Plan 240-03 R-09 and threat T-240-16. Bounded because an existing `in_flight` row is updated in place, and the next auto transition overwrites the flag. Record why `manualOverrideActive` was deliberately NOT ANDed into the gate: doing so reddens `tests/test-jtbd-transition-graph-wiring.cjs` test 1, and ORing it would not enforce expiry.
      D-240-05, lock contention and FIFO pressure under a far higher promotion rate. 240-RESEARCH.md Open Question Q3 and threat T-240-27. The promotion path went from effectively never firing to potentially firing on most turns in every open room. Bounded by the in-place `in_flight` update, the 100-entry per-room cap with archive spillover, and `audit.log`'s 10000-line bound with 1 percent sample-rate truncation. Include the observed `audit.log` line count plan 240-04 test 4 recorded. Concurrent-Cowork behavior is unmeasured. Also record that `tests/test-across-session-memory.cjs` class 11's multi-process race is pre-existing flaky timing.
      D-240-06, the frozen `graph-edge-pending.log` residue. Locked decision D-1, 240-RESEARCH.md Finding 2, and the resolved RCA. Exactly 13 lines, byte-unchanged since 2026-07-28T02:42:14Z, both slugs (`test-jtbd-promote`, `test-jtbd-127.3-empirical`) absent from the current registry so any replay is a structural no-op. NOT backfilled, by human-approved decision. State plainly that a future plan proposing a drainer is reversing that decision. Record the RE-MEASURED line count from Task 2.
      D-240-07, the pre-existing failures this phase must not be blamed for or credited with fixing. `tests/test-memory-command.cjs` at 24/26 with two Brain Mode A failures, named. `tests/test-hmi-compliance-e2e.cjs` exiting 1 while hermetic, with its failure set named. `tests/test-graph-derivation-verdict.cjs`'s two FEYNMAN/Timeline assertions if still failing. Each with the observed failure names, so a future reader can tell an old red from a new one.
      D-240-08, the unrelated dirty working tree present throughout this phase. 240-RESEARCH.md Assumption A6 and plan 240-01 Task 1. List the files plan 240-01 recorded. State that none was touched or committed by Phase 240 and that they still need an owner.
      D-240-09, `isRoomOptOut` at `lib/hmi/across-session-memory.cjs:252` joins `roomSlug` into a path raw (`path.join(ROOMS_HOME(), roomSlug)`), unlike `resolveRoomDirForSlug`'s exact registry match. 240-RESEARCH.md Security Domain V5 and threat T-240-18. Pre-existing, not introduced by this phase, no traversal reachable today because the slug comes from the registry, but it is the one place worth a second look.
      D-240-10, both mandated MCP grounding legs were unavailable for this phase's research. 240-RESEARCH.md Environment Availability and Open Question Q1. Context7 and langtalks-graph-expert both returned `No such tool available` (the documented upstream tool-stripping bug for agents with a `tools:` frontmatter restriction, anthropics/claude-code#13898) and `ctx7` is not on PATH. Discharged by file:line reads and live observation, which for a phase entirely about this repo's own code is the stronger evidence. Recorded as a declared gap, not papered over, and NOT closed by firing an unasked WebSearch (standing MCP-stack-awareness HARD RULE).

    Then file the Dev-Research Compositing trail. Create `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-phase-240-memory/` matching the naming convention of the five existing 2026-07-28 entries, and write the durable reasoning trail there. It must carry, in Feynman-simplified prose:
      The finding that matters most, stated plainly: the Layer 2 across-session memory promotion had a structural DEADLOCK, not a threshold problem. The counter meant to detect continuous work could only grow by NOT working continuously, because history rows were appended only inside `setCurrent`, which the topic-change gate guarded. In roughly two months of real use, not one genuine user promotion ever occurred, and the live store's `"rooms": {}` plus five test-written `audit.log` lines are the receipt.
      The second finding, which is the more general lesson: fixing MEM-01 CREATED a new test leak. `tests/test-jtbd-hook-integration.cjs` read hermetic only because the promotion never reached the write. A hermeticity census measured against broken code can certify a suite that becomes non-hermetic the moment the bug is fixed. This is why MEM-03 was sequenced before MEM-01.
      The third finding: MEM-02's root fix had already shipped two days before the research ran, and ROADMAP SC2 as originally worded ("the pending log shrinks") was unsatisfiable by design. The correct move was to reframe the criterion and build the gate, not to build the drainer a human had already approved against.
      The mutation lesson, generalised: the OBVIOUS mutation for a survival property was the wrong one. Removing the transaction wrap leaves the survival assertions green, because what protects the rows is the scoped DELETE, not atomicity. A plan that words its mutation from the criterion's own prose can ship green with the bug intact.
      Cross-link back explicitly to `.planning/phases/240-memory/` (the RESEARCH, PATTERNS, VALIDATION and the six plans and their SUMMARYs) and to the resolved RCA at `.planning/debug/resolved/graph-edge-pending-undrained-dead-letter-queue.md`. Then add a cross-reference line in `deferred-items.md` pointing at the room entry, so the link exists in BOTH directions as the mandate requires.

    No em-dashes anywhere. No emoji. No individual's real name. Feynman-simplified, JTBD-oriented prose.
  </action>
  <verify>
    <automated>test -f .planning/phases/240-memory/deferred-items.md && grep -c 'D-240-' .planning/phases/240-memory/deferred-items.md && ls ~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-phase-240-memory/</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/240-memory/deferred-items.md` exists and `grep -c 'D-240-'` is at least 10, one per item above, plus any additional finding a prior SUMMARY recorded.
    - Every item carries a citation that resolves. Spot-check at least four by opening the cited file and line and confirming the claim, and record which four were checked.
    - D-240-06 records a RE-MEASURED line count for `~/MindrianRooms/.memory/graph-edge-pending.log`, taken in Task 2, not the research's 13 copied forward. If the count is no longer 13, that is a finding and must be escalated, not smoothed over.
    - D-240-07 names the ACTUAL observed failure names from this phase's runs, not a generic "pre-existing failures" phrase.
    - D-240-10 states the grounding gap was discharged by file:line reads and live observation, and that no unasked WebSearch was fired.
    - `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-phase-240-memory/` exists with at least one substantive `.md` file, and its name matches the convention of the five existing 2026-07-28 entries.
    - The room entry cross-links to `.planning/phases/240-memory/` AND `deferred-items.md` cross-links to the room entry. Both directions confirmed by grep.
    - `grep -cP '\x{2014}' .planning/phases/240-memory/deferred-items.md` is 0, and the same for every `.md` file created in the room entry.
    - `git diff --quiet lib/ scripts/ tests/` passes: this task wrote no code and no tests.
    - The register is committed with `git add -f` since `.planning/` is gitignored.
  </acceptance_criteria>
  <done>Every residual has a written reason and a resolving citation, and the reasoning trail lives in both homes with links in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the full phase gate and record every result against its expected value</name>
  <files>.planning/phases/240-memory/deferred-items.md</files>
  <read_first>
    - `.planning/phases/240-memory/240-VALIDATION.md` the Sampling Rate section ("Before `/gsd-verify-work`") and the Regression Suites table, for the exact commands and their expected results.
    - `.planning/ROADMAP.md` the Phase 240 section, all three REVISED Success Criteria, so each gate result can be mapped to the criterion it serves.
    - `./CLAUDE.md` the Verification section, for the four repo-wide gates: `scripts/verify-release`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`, plus `node scripts/doctor.cjs --acceptance`.
    - All five prior SUMMARYs, to assemble the definitive list of files this phase touched for the em-dash sweep.
  </read_first>
  <action>
    Run the phase gate. Record EVERY command with its observed result beside its expected value in the SUMMARY. Any deviation is diagnosed and reported, never rounded to green.

    Phase suites: `bash tests/run-all-240.sh` (expect all four `tests/test-240-*` files plus four gate legs, FAIL=0), `bash tests/run-all-236.sh` (expect PASS=12 FAIL=0 SKIP=0, the MEM-02 hard dependency), `bash tests/run-all-127.3.sh` (expect green).

    Targeted regressions from 240-VALIDATION.md: `node tests/test-across-session-memory.cjs` (36/36, class 11 flake noted if it appears), `node tests/test-jtbd-transition-graph-wiring.cjs` (6 tests / 14 assertions), `node tests/test-memory-hook-integration.cjs` (10/10), `node tests/test-129-spine-substrate.cjs` (15/15), `node tests/test-memory-command.cjs` (24/26 with the SAME two named Brain Mode A failures), `node tests/test-150-brain-egress.cjs` (PASS, the Canon Part 8 MEM-04 zero-prose invariant), `node tests/test-jtbd-state-io.cjs`, `node tests/test-jtbd-command.cjs`, `node tests/test-jtbd-hook-integration.cjs`, `node tests/test-jtbd-classifier.cjs`, `node tests/test-connector-tier-d-hooks.cjs`, `node tests/test-cross-room-memory.cjs`, `node tests/test-navigation-memory-events.cjs`, `node tests/test-150-memory-nodes.cjs`, `node tests/test-236-rebuild-preserves-journal.cjs`.

    Repo-wide gates: `node scripts/doctor.cjs --acceptance`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs`, `node scripts/check-shape-declaration.cjs` (record the declared-surface count and confirm it is UNCHANGED from the value plan 240-01 recorded, which is the final Canon Part 11 CIRS confirmation for this phase).

    THE EM-DASH SWEEP, executed not asserted. Assemble the definitive list of files this phase touched from the five SUMMARYs. It should be: `tests/run-all-240.sh`, `tests/test-240-memory-store-hermetic-fence.sh`, `tests/test-jtbd-auto-anchor-empirical.sh`, `tests/test-jtbd-hook-integration.cjs`, `lib/hmi/jtbd-state.cjs`, `lib/hmi/across-session-memory.cjs`, `tests/test-240-jtbd-manual-override-roundtrip.cjs`, `scripts/jtbd-update.cjs`, `tests/test-240-jtbd-continuous-promotion.cjs`, `tests/test-240-jtbd-event-survives-rebuild.cjs`, plus the six PLAN files, the six SUMMARY files and `deferred-items.md`. Run a per-file em-dash count over every one and record the table. Every count must be 0. Note that `scripts/jtbd-update.cjs` legitimately contains the character `±` in its `no transition` debug string, which is NOT an em-dash and must be left alone.

    THE LIVE-STORE FINAL AUDIT. Record, verbatim: the full contents of `~/MindrianRooms/.memory/jtbd-history.json`; the line count and last line of `~/MindrianRooms/.memory/audit.log`; the line count and last-modified timestamp of `~/MindrianRooms/.memory/graph-edge-pending.log`. Compare each against the values 240-RESEARCH.md's Runtime State Inventory recorded (`"rooms": {}` with `last_updated` 2026-07-28; 5 `promote` lines all carrying the slug `test-jtbd-127.3-empirical`; exactly 13 lines unchanged since 2026-07-28T02:42:14Z). Every one of the three should be UNCHANGED by this entire phase, which is the strongest single statement MEM-03 can make. Feed the re-measured `graph-edge-pending.log` count back into `deferred-items.md` D-240-06. If ANY of the three moved, that is a leak this phase failed to close: escalate it in the SUMMARY as a blocking finding, do not absorb it.

    THE FOREIGN-CHANGE AUDIT. Run `git status --short` and compare against the pre-existing dirty set plan 240-01 Task 1 recorded. The delta must be exactly this phase's own named files. Confirm no statusline file, no `package-lock.json`, and no untracked `.planning/debug/*.md` was staged or committed by any Phase 240 commit. Verify with `git log --oneline --name-only` scoped to this phase's commits.

    Do NOT run `scripts/release.sh`, do NOT bump any version, do NOT cut a release. ROADMAP Gate 0 (the official v1.15.0 stable close-out) governs the release train and is a separate navigator decision. Do NOT write `STATE.md`, `ROADMAP.md` or `REQUIREMENTS.md`: that is the orchestrator's step after `gsd-verifier`. Do NOT call any `gsd-tools.cjs` write verb (Pitfall 8).

    Do NOT run `generate-claude-md`. Phase 236 recorded it as UNSAFE on this repo: it produced 8 insertions and 22 deletions, overwriting the live Conventions block including the no-em-dashes rule with "not yet established" placeholders, because `.planning/CONVENTIONS.md` and `.planning/ARCHITECTURE.md` do not exist.
  </action>
  <verify>
    <automated>bash tests/run-all-240.sh && bash tests/run-all-236.sh && bash tests/run-all-127.3.sh && node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-240.sh` exits 0 with all four `tests/test-240-*` files discovered and all four gate legs PASSED. Transcribe the summary line and the discovered set.
    - `bash tests/run-all-236.sh` reports PASS=12 FAIL=0 SKIP=0.
    - `bash tests/run-all-127.3.sh` green.
    - Every targeted regression command run, with its observed result recorded beside its expected value. The two `test-memory-command.cjs` failures confirmed to be the SAME pre-existing pair by name.
    - `node scripts/doctor.cjs --acceptance`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check`, `node scripts/check-render-coverage.cjs` all run and recorded. Any pre-existing failure named and confirmed pre-existing by checking out the file at `HEAD~` if needed, never assumed.
    - `node scripts/check-shape-declaration.cjs` declared-surface count UNCHANGED from plan 240-01's recorded value. Canon Part 11 CIRS confirmed not applicable to this phase, one final time.
    - EM-DASH SWEEP TABLE present in the SUMMARY, one row per phase-touched file, every count 0. The `±` in `scripts/jtbd-update.cjs` confirmed still present and confirmed not an em-dash.
    - LIVE-STORE FINAL AUDIT recorded verbatim for all three files, each compared against 240-RESEARCH.md's recorded values. All three unchanged, or the deviation escalated as a blocking finding.
    - `deferred-items.md` D-240-06 updated with the re-measured `graph-edge-pending.log` line count.
    - FOREIGN-CHANGE AUDIT recorded: `git status --short` delta is exactly this phase's named files, and `git log --oneline --name-only` over this phase's commits shows no statusline file, no `package-lock.json`, and no untracked `.planning/debug/*.md`.
    - `git diff --quiet lib/ scripts/ tests/` passes: this plan changed no code and no tests.
    - No version bumped, no release cut, no `gsd-tools.cjs` write verb called, `generate-claude-md` not run, and `STATE.md` / `ROADMAP.md` / `REQUIREMENTS.md` untouched. Confirm each with an empty diff or an explicit statement.
    - THE THREE ROADMAP SUCCESS CRITERIA MAPPED. For each of SC1, SC2 and SC3, state which test file and which executed mutation proves it, quoting the observed mutation output from the owning plan's SUMMARY. A criterion with no named mutation is not closed, and saying so is the correct outcome rather than claiming it.
  </acceptance_criteria>
  <done>The phase gate is executed with every result recorded against its expected value, the live store is proven untouched, and each ROADMAP criterion is mapped to the specific test and executed mutation that proves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase findings -> future sessions | A finding that lives only in a SUMMARY nobody re-reads is functionally lost. The register and the room entry are the durable surface. |
| `gsd-tools.cjs` write verbs -> the tracking files | An open corruption bug makes those verbs a hazard to `STATE.md`, `ROADMAP.md` and `REQUIREMENTS.md`. |
| the developer's real per-user store -> the phase's own closing audit | The final audit reads the real store; it must never write to it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240-41 | Repudiation | a deliberately-unfixed finding disappearing into a SUMMARY | mitigate | The register carries ten named items, each with a citation, an observation, a reason for non-closure, and what closing it would take. The acceptance criteria require spot-checking at least four citations against the cited file and line. |
| T-240-42 | Repudiation | a phase artifact citing "per the room's research" with no citation, or a room entry that never lands in a phase | mitigate | The exact failure the CLAUDE.md Dev-Research Compositing mandate names. Cross-links are required in BOTH directions and confirmed by grep in the acceptance criteria. |
| T-240-43 | Tampering | `state.record-session` corrupting `stopped_at` and `last_activity` | mitigate | Pitfall 8, reproduced four times in Phase 236 and again in 239. This plan calls no `gsd-tools` write verb and does not touch the three tracking files at all; phase closure is the orchestrator's hand-edited step after verification. |
| T-240-44 | Tampering | `generate-claude-md` overwriting the live Conventions block including the no-em-dashes rule | mitigate | Phase 236 recorded it as UNSAFE on this repo with a measured 8-insertion / 22-deletion information loss, because `.planning/CONVENTIONS.md` and `.planning/ARCHITECTURE.md` do not exist. Explicitly forbidden in Task 2. |
| T-240-45 | Repudiation | a phase gate whose deviations are rounded to green | mitigate | Every command must be recorded with its observed result BESIDE its expected value, pre-existing failures must be named individually, and a criterion with no named mutation must be reported as not closed rather than claimed. |
| T-240-46 | Tampering | a foreign working-tree change committed under a Phase 240 commit | mitigate | The foreign-change audit compares `git status --short` against plan 240-01's recorded pre-existing dirty set and inspects `git log --name-only` over this phase's commits for statusline files, `package-lock.json`, and untracked `.planning/debug/*.md`. |
| T-240-47 | Tampering | the closing audit writing to the store it is auditing | mitigate | The live-store audit is read-only: `cat`, `wc -l` and `stat` only. No test is invoked with the real store as its root, and all three measured values must be UNCHANGED from the research's recorded baseline. |
| T-240-48 | Elevation of Privilege | cutting a release inside a phase plan | mitigate | ROADMAP Gate 0 (the official v1.15.0 stable close-out) gates every v1.16.0 release cut and is a separate navigator decision. `scripts/release.sh` is forbidden, no version may be bumped, and the acceptance criteria require an explicit confirmation. |
| T-240-49 | Information Disclosure | a real individual's name entering the room entry or the register | mitigate | Standing HARD RULE: no individual's name attached to PWS anywhere, and no real names or emails of testers or advisors in tracked repos. The room entry uses roles and pseudonyms only. |
| T-240-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable. Zero packages. This plan runs existing scripts and writes markdown. |

ASVS note: no auth, session, access-control, input-validation or crypto surface is touched. V7 Error Handling applies to the gate-reporting discipline, covered by T-240-45.
</threat_model>

<verification>
1. `.planning/phases/240-memory/deferred-items.md` exists with at least 10 `D-240-` items, four citations spot-checked and named.
2. `~/MindrianRooms/rethinking-mindrianos/research/2026-07-30-phase-240-memory/` exists with a substantive trail, cross-linked in both directions.
3. `bash tests/run-all-240.sh` green with four test files and four gate legs; summary line transcribed.
4. `bash tests/run-all-236.sh` PASS=12 FAIL=0 SKIP=0. `bash tests/run-all-127.3.sh` green.
5. Every targeted regression and every repo-wide gate run and recorded against its expected value.
6. `node scripts/check-shape-declaration.cjs` declared-surface count unchanged; Canon Part 11 confirmed not applicable.
7. Em-dash sweep table present, every count 0, and the `±` in `scripts/jtbd-update.cjs` confirmed intact.
8. Live-store final audit recorded for all three files and all three unchanged from the research baseline, with the re-measured `graph-edge-pending.log` count fed into D-240-06.
9. Foreign-change audit recorded; no foreign file in any Phase 240 commit.
10. SC1, SC2 and SC3 each mapped to a named test file and a quoted executed mutation output. No version bumped, no release cut, no `gsd-tools` write verb called, `generate-claude-md` not run, tracking files untouched, `git diff --quiet lib/ scripts/ tests/` passes.
</verification>

<success_criteria>
- Every finding this phase surfaced and deliberately did not fix is written down with a resolving citation and an actionable reason.
- The Dev-Research Compositing mandate is honoured with links in both directions.
- The three real-store files the research measured are proven byte-unchanged by the entire phase, which is the strongest single statement MEM-03 can make.
- The phase gate is executed with every result recorded against its expected value and no deviation rounded to green.
- Each ROADMAP success criterion is mapped to the specific test file and executed mutation that proves it, or reported as not closed.
</success_criteria>

<output>
Create `.planning/phases/240-memory/240-06-SUMMARY.md` when done.
</output>
