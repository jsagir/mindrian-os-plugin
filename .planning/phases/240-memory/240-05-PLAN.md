---
phase: 240-memory
plan: 05
type: execute
wave: 3
depends_on: ["240-03"]
files_modified:
  - tests/test-240-jtbd-event-survives-rebuild.cjs
autonomous: true
requirements: [MEM-02]

must_haves:
  truths:
    - "A real across-session promote, park and complete each write a real jtbd_transitioned memory_event row into that room's room.db through the Phase 150 memory cortex"
    - "Each of those three rows SURVIVES a real rebuildGraph with an identical node id AND a byte-identical properties blob"
    - "The rebuild did real work in the same run: it returned success with artifacts greater than zero, so survival is not survival-by-doing-nothing"
    - "Exactly one row per kind exists, so a deduped no-op cannot present as a pass: logJtbdTransition derives a dedupe_key from [type, kind, from, to] and a repeat inside 60 seconds returns deduped true with no INSERT"
    - "The retired graph-edge-pending.log is never re-created by any of the three lifecycle calls"
    - "Adding memory_event to INDEXER_OWNED_NODE_TYPES turns the survival assertions RED, and removing the transaction wrap does NOT, which is the correct mutation and the recorded counter-demonstration"
    - "The promote leg's current object comes from a real setCurrent then getCurrent round trip, never a hand-constructed literal"
  artifacts:
    - path: "tests/test-240-jtbd-event-survives-rebuild.cjs"
      provides: "MEM-02 SC2: the test that JOINS the promote path to the rebuild-survival path, which no existing test does"
      min_lines: 220
      contains: "buildFixtureRoom236"
  key_links:
    - from: "lib/hmi/across-session-memory.cjs logGraphTransition"
      to: "lib/core/navigation/memory-events.cjs nodes row type memory_event"
      via: "navigation.logJtbdTransition, resolved through a registry entry the test must write itself"
      pattern: "jtbd_transitioned"
    - from: "lib/core/lazygraph-ops.cjs clearIndexerOwnedRows"
      to: "the surviving memory_event rows"
      via: "memory_event is absent from INDEXER_OWNED_NODE_TYPES, so the scoped DELETE cannot reach it"
      pattern: "INDEXER_OWNED_NODE_TYPES"
---

<objective>
Close MEM-02 by building the regression gate the phase actually owes: the test that JOINS the promote path to the rebuild-survival path. No such test exists today.

Purpose: 240-RESEARCH.md Finding 2 verified against HEAD, not against the RCA text, that MEM-02's root fix already shipped in commit `3c9afa2e` and that the survival half is already TRUE. Re-confirmed at planning time: repo-wide grep for `writeGraphEdge` and `GRAPH_EDGE_LOG` returns zero hits outside `.planning/`; `INDEXER_OWNED_NODE_TYPES` at `lib/core/lazygraph-ops.cjs:81` is `Object.freeze(['Artifact', 'Section'])` with `memory_event` absent; `rebuildGraph`'s `BEGIN` / `COMMIT` / `ROLLBACK` sit at `:668` / `:743` / `:745`. The research also ran the end-to-end probe live: promote wrote a real `memory_event` node and that node survived a real `rebuildGraph` with an identical id while the rebuild reported `artifacts: 1`.

So this plan writes no production code. That is the point rather than a shortfall, and this repo has the receipt: STATE.md records that Phase 236 Plan 02 also changed no production source, pinning behavior the repo gets right but had never asserted, because the repo had already been burned once by exactly that gap (Phase 233-03, RCA CLAIM-12).

Output: one test file joining two paths that are each covered and never connected.

**Locked navigator decision D-1, honoured exactly: MEM-02 SC2 is satisfied by the ALREADY-SHIPPED fix, and NO drainer is built.** `graph-edge-pending.log` and `writeGraphEdge` are deleted; `logGraphTransition` writes into `memory_events` at all three lifecycle sites (`across-session-memory.cjs:435`, `:481`, `:521`). The RCA's Option A drainer was analysed, explicitly rejected, and human-approved against on 2026-07-28, and it would have needed a new frozen edge type in `edges.cjs`'s `ALLOWED_EDGE_TYPES` plus a JTBD node type that does not exist in the schema. **Any plan or task that builds a drainer, or that asserts `graph-edge-pending.log` SHRINKS, reverses a human-approved decision and must be rejected.** The 13 orphan lines on disk are frozen historical residue with no live producer or consumer; plan 240-06 documents them, and nothing backfills them.

**R-15, the exact division of labour, which is why this test is not redundant.** `tests/test-jtbd-transition-graph-wiring.cjs` proves the WRITE (promote / park / complete each produce a `jtbd_transitioned` row). `tests/test-236-rebuild-preserves-journal.cjs` proves GENERIC survival (the 236 fixture seeds a `node_created` memory_event and it survives a rebuild). **Nothing joins them.** 240-VALIDATION.md row `240-04-01` says so in its own words. The specific row an across-session promote writes has never been put through a rebuild, and that specific row is what ROADMAP SC2 names.

**R-16, THE MUTATION WORDING IS LOAD-BEARING AND THE OBVIOUS WORDING IS WRONG. Read this before writing a single assertion.** ROADMAP SC2 says the rows survive "riding Phase 236's transaction wrap", which invites the mutation "remove the transaction wrap". That mutation ships GREEN with the bug intact. `tests/test-236-rebuild-preserves-journal.cjs:24-32` warns about it in its own header, and STATE.md records the live result from Phase 236 Plan 02: under wrap removal "the WAL test's scenario 3 stayed GREEN, because 236-01's scoped DELETE means the irreplaceable rows are never touched even when atomicity is gone". Scenario 2 measures the wrap; scenario 3 measures the DELETE scope; neither incidentally covers the other. **The correct mutation is: add `'memory_event'` to `INDEXER_OWNED_NODE_TYPES` at `lib/core/lazygraph-ops.cjs:81`.** Task 2 runs BOTH, and records the wrap-removal green as a deliberate counter-demonstration so the next reader inherits the finding instead of rediscovering it.

**R-17, three setup traps that each produce a silent pass, all three confirmed on disk at planning time.**
  Trap A, the registry. `tests/helpers/fixture-room-236.cjs:215-219` writes a `.room-root` sentinel but NOT `.rooms/registry.json`. Without a registry entry `resolveRoomDirForSlug` returns null and `logGraphTransition` returns null as a graceful no-op, so the whole test would pass with zero rows ever written. The test must write the registry entry itself.
  Trap B, dedupe. `logJtbdTransition` (`spine-events.cjs:182-189`) derives a `dedupe_key` from `[type, kind, from, to]`, so a second identical call inside the 60 second idempotency window returns `{ok:true, deduped:true}` with NO INSERT. `promoteIfEligible` does not return that flag (it returns `{action:'promoted'}`), so the flag is unreadable from the caller. The test must therefore assert on the ROW COUNT per kind, which is a strictly stronger check, and must separately demonstrate the dedupe window is real.
  Trap C, hand-fed state. `tests/test-jtbd-transition-graph-wiring.cjs:94-97` hand-feeds `turn_count` and `manual_set`, fields that before plan 240-03 existed nowhere in production. The promote leg here must go through a real `jtbdState.setCurrent` then `jtbdState.getCurrent`, which is why this plan depends on 240-03.

**Pitfall 7 note:** everything here runs `node` directly against `/home/jsagi/dev/MindrianOS-Plugin/`. Nothing is concluded from in-session behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/240-memory/240-RESEARCH.md
@.planning/phases/240-memory/240-PATTERNS.md
@.planning/phases/240-memory/240-VALIDATION.md
@.planning/phases/240-memory/240-03-SUMMARY.md
</context>

## Artifacts this plan produces

| Symbol | Kind | Location |
|---|---|---|
| `tests/test-240-jtbd-event-survives-rebuild.cjs` | new file (test) | `tests/` |

The full phase-wide inventory is in `240-01-PLAN.md`. This plan writes ZERO production code: `git diff --quiet lib/ scripts/` must pass at the end. Canon Part 11 (CIRS) is NOT APPLICABLE. No new fixture helper is created: `tests/helpers/fixture-room-236.cjs` is reused per Canon Part 7, and 240-PATTERNS.md rates it an EXACT analog.

<tasks>

<task type="auto">
  <name>Task 1: Join the promote path to the rebuild-survival path, reusing the Phase 236 fixture</name>
  <files>tests/test-240-jtbd-event-survives-rebuild.cjs</files>
  <read_first>
    - `tests/test-236-rebuild-preserves-journal.cjs` IN FULL. It is the primary analog. Specifically: the header at 18-45 (the mutation warning and the non-vacuity rationale, both of which must be carried into the new file's header), the imports and harness at 62-128 (`ok` / `fail` / `async scenario`, `makeScratchDir`, `rmrf`, `runRebuild`, `withOpenRoom`), and the byte-for-byte assertion shape at 137-140.
    - CRITICAL SIGNATURE FACT, stated in that file's own comment: `rebuildGraph` is `async function rebuildGraph(conn, roomDir, _visited)` and takes an OPEN `DatabaseSync` connection as its FIRST argument. Do NOT call `rebuildGraph(roomDir)`.
    - `tests/helpers/fixture-room-236.cjs` IN FULL, 365 lines. Note: `buildFixtureRoom236(tmpDir, opts)` is ASYNC and creates the room at `tmpDir/room`; it returns `{ roomDir, dbPath, memoryEventId, claimId, opportunityId, artifactFiles, section }`; `opts.artifactCount` defaults to 3 and `opts.indexArtifacts` defaults to true; it writes `.room-root` with `{"slug":"fixture-236"}` but NO `.rooms/registry.json` (Trap A); `readNodeRow` returns the RAW `properties` string on purpose so a comparison is a real byte comparison and not a re-parse; and every seed call asserts `ok === true` AND `notEqual(deduped, true)`, which is the discipline this test must copy for its own seeding.
    - `tests/test-jtbd-transition-graph-wiring.cjs` IN FULL, for `makeRegisteredRoom`'s registry literal (49-63), `withRegisteredRoom` (66-75), `loadFreshModule` (43-46), the `readJtbdTransitions` reader (77-85), and test 6's `graph-edge-pending.log` absence assertion (218-235).
    - `lib/core/navigation/memory-events.cjs:732-760`, `findRecentChanges`, whose returned rows carry `{ id, eventType, targetNodeId, createdAt, sourcePath, properties }` with `properties` ALREADY PARSED. The `id` is what feeds `readNodeRow` for the raw byte comparison.
    - `lib/hmi/across-session-memory.cjs:330-540`: `resolveRoomDirForSlug` (339-353, note it prefers `abs_path` then `path` and joins a relative `path` against `ROOMS_HOME()`), `logGraphTransition` (355-378), `promoteIfEligible` (384-447), `parkJtbd` (453-487) and `completeJtbd` (493 onward, signature `completeJtbd(roomSlug, jtbd, completionShape, completionEvidence)`).
    - `lib/core/lazygraph-ops.cjs:33-90` (the ownership rationale block and both frozen allowlists) and `:126-152` (`clearIndexerOwnedRows`) and `:660-750` (`rebuildGraph` with its `BEGIN` at 668 and `COMMIT` / `ROLLBACK` at 743 / 745). READ ONLY. This plan edits no production file.
    - Resolutions R-15, R-16 and R-17 in this plan's objective, IN FULL.
  </read_first>
  <action>
    Create `tests/test-240-jtbd-event-survives-rebuild.cjs`. Header must state, in Phase 240's own words:
      What it answers: MEM-02 SC2 as REVISED on 2026-07-30. It proves the promote, park and complete lifecycle each reach the Phase 150 memory cortex and that those specific rows survive a real `rebuildGraph`.
      Why it exists when two suites already pass: `tests/test-jtbd-transition-graph-wiring.cjs` proves the write and `tests/test-236-rebuild-preserves-journal.cjs` proves generic survival with a `node_created` row; nothing joins them, so the row an across-session promote actually writes has never been put through a rebuild.
      THE MUTATION, verbatim in intent: adding `'memory_event'` to `INDEXER_OWNED_NODE_TYPES` at `lib/core/lazygraph-ops.cjs:81` turns the survival scenarios RED. Removing the `BEGIN` / `COMMIT` / `ROLLBACK` wrap does NOT, and a plan that words its mutation that way ships green with the bug intact. This file pins DELETE SCOPE, not atomicity. Carry the Phase 236 header's warning shape and cite STATE.md's live wrap-removal result.
      What is deliberately NOT here: no drainer, and no assertion that `graph-edge-pending.log` shrinks. That file's producer was deleted in commit `3c9afa2e` and the RCA's Option A was human-approved against. Cite locked decision D-1.

    Harness: copy `ok` / `fail` / `async scenario(name, fn)`, `makeScratchDir`, `rmrf`, `runRebuild(roomDir)` and `withOpenRoom(roomDir, fn)` from the 236 analog (its harness is async, which this file needs). Track `passed` / `failed` and `process.exit(failed === 0 ? 0 : 1)`.

    Fixture wrapper `withJoinedFixture(fn)`, which is the piece that closes Trap A:
      `makeScratchDir('240-survive')` as the scratch root, then `await buildFixtureRoom236(scratch, { artifactCount: 3, indexArtifacts: true })`.
      Write `<scratch>/.rooms/registry.json` as `{ version: 1, rooms: [{ slug: 'fixture-236', abs_path: fx.roomDir, path: 'room' }] }`. Use `abs_path` set to the ABSOLUTE `fx.roomDir` so `resolveRoomDirForSlug` returns it directly without joining, and include the relative `path` too so the entry matches the shape production writes.
      Set `MINDRIAN_ROOMS_HOME` to `<scratch>`, saving and restoring the prior value in `finally`, and `rmrf` the scratch root in `finally`.
      Call `loadFreshModule()` for `across-session-memory.cjs` so `ROOMS_HOME()` re-resolves against the scratch root.
      FIXTURE SELF-CHECK, non-negotiable: immediately assert that `mem._internal.resolveRoomDirForSlug('fixture-236')` returns exactly `fx.roomDir`. If it returns null, throw a clearly-labelled FIXTURE error naming Trap A. Without this, every scenario below would pass over zero rows.

    Reader helper `readTransitions(roomDir, kind)`: open the room db, call `memoryEvents.findRecentChanges(db, 0, { eventType: 'jtbd_transitioned', limit: 100 })`, filter on `r.properties && r.properties.kind === kind`, close the db, return the array. Copy the shape from `test-jtbd-transition-graph-wiring.cjs:77-85`.

    Six scenarios.

    Scenario 1, the three lifecycle calls each write exactly one row, driven through real production writers.
      For the PROMOTE leg, close Trap C: require `lib/hmi/jtbd-state.cjs`, call `jtbdState.setCurrent(fx.roomDir, { jtbd: 'decide-pursue', confidence: 0.9, evidence: ['fixture-240'], trigger: 'manual_set', manual: true })`, then `const cur = jtbdState.getCurrent(fx.roomDir)`, assert `cur.manual_set === true` with a message naming plan 240-03 (if this assertion is red, 240-03 did not land and the rest of this scenario is meaningless), then call `mem.promoteIfEligible('fixture-236', { current: cur, history: jtbdState.history(fx.roomDir, 50) })` and assert the result is `{ action: 'promoted', jtbd: 'decide-pursue' }`. Do NOT hand-construct `current`.
      Then `mem.parkJtbd('fixture-236', 'decide-pursue', 'fixture-240 park')` and assert `{ action: 'parked' }`.
      Then `mem.completeJtbd('fixture-236', 'decide-pursue', 'fixture-240 completion shape', [])` and assert a non-null result.
      Then assert `readTransitions(roomDir, 'promote').length === 1`, `... 'park').length === 1`, `... 'complete').length === 1`. Exactly one, not at-least-one: a count of zero means the write never happened (Trap A) and a count above one means something double-wrote.
      Capture the three node ids and, via `withOpenRoom` plus `readNodeRow`, the three RAW `properties` strings, for scenario 3.

    Scenario 2, the dedupe window is real, which is what makes scenario 1's exact-one counts meaningful (Trap B). Immediately re-run `mem.promoteIfEligible` with the same real `current`, then assert `readTransitions(roomDir, 'promote').length` is STILL 1. State in the assertion message that `logJtbdTransition` derives a `dedupe_key` from `[type, kind, from, to]` and returns `{ok:true, deduped:true}` with no INSERT inside 60 seconds, so a test expecting two rows would fail for the wrong reason. This scenario converts a trap into a pinned property.

    Scenario 3, THE JOIN. Capture `countPopulations` before, run `await runRebuild(fx.roomDir)`, capture the rebuild's returned object and `countPopulations` after. Then assert:
      all three `jtbd_transitioned` rows still exist with the SAME node ids;
      each row's raw `properties` string is BYTE-IDENTICAL to the value captured in scenario 1 (compare the raw strings, never re-parsed objects, which is why `readNodeRow` returns raw);
      each row's `type` is still `memory_event`;
      the 236 fixture's own `memoryEventId`, `claimId` (still `review_status` confirmed) and `opportunityId` (with `readStageHistory` length unchanged) also survived, so the scenario measures the whole irreplaceable population rather than only the new rows.

    Scenario 4, NON-VACUITY: the rebuild did real work in the same run. Assert the object returned by `rebuildGraph` has `success === true` and `artifacts` greater than 0, and that `countPopulations` after the rebuild shows a non-zero `Artifact` count. Copy the rationale from the 236 analog's scenario 4 header: without this leg, a change that made `rebuildGraph` delete nothing at all would pass scenario 3 while doing nothing.

    Scenario 5, the retired dead-letter file is never re-created. After all three lifecycle calls and the rebuild, assert `<scratch>/.memory/graph-edge-pending.log` does NOT exist. Note in the assertion message that this mirrors `test-jtbd-transition-graph-wiring.cjs` test 6 and is a weak signal alone, which is exactly why scenarios 1 and 3 are the load-bearing ones. Also assert that `<scratch>/.memory/jtbd-history.json` DOES exist, so the Layer 2 write is confirmed alongside the graph write.

    Scenario 6, Canon Part 8 payload discipline. Read the three rows' parsed `properties` and assert each carries only scalars and enum-shaped handles: `kind` is one of `promote` / `park` / `complete`, `to` is the jtbd slug string, `roomSlug` is the slug, `created_by` is `system`, `source_path` starts with `across-session:`, and there is NO field whose value is a multi-sentence prose string. Seed a canary: write a recognisable prose marker into the room's own `ROOM.md` before the lifecycle calls and assert that marker appears in NONE of the three serialized `properties` blobs. Copy the canary idiom from Phase 236's Canon Part 8 gates.

    Hermeticity: everything under `os.tmpdir()`, `MINDRIAN_ROOMS_HOME` restored in `finally`, scratch root removed. Zero network reach. No em-dashes. No emoji. Deterministic: no randomness in any asserted value.
  </action>
  <verify>
    <automated>node tests/test-240-jtbd-event-survives-rebuild.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-240-jtbd-event-survives-rebuild.cjs` exits 0 with 6 scenarios passing. Transcribe the full output.
    - The fixture self-check is present and its failure message names Trap A. Prove it bites: temporarily skip the `.rooms/registry.json` write, observe the FIXTURE error (not a silent pass, not a green scenario), restore. Transcribe both.
    - Scenario 1 asserts EXACTLY 1 row per kind, not at-least-1. Confirm by reading the assertions.
    - Scenario 1's promote leg passes a variable obtained from `jtbdState.getCurrent`. Audit every `promoteIfEligible` call site in the file and confirm NONE passes a hand-constructed literal. State the audited count.
    - Scenario 3 compares RAW `properties` strings. Confirm `readNodeRow` is used and that no `JSON.parse` sits between the capture and the comparison.
    - Scenario 4's rebuild `artifacts` count is greater than 0 and the observed number is transcribed.
    - Scenario 6's prose canary marker is confirmed absent from all three blobs, and the marker string is transcribed.
    - `git diff --quiet lib/ scripts/` passes: this plan changed no production code.
    - `node tests/test-236-rebuild-preserves-journal.cjs` still exits 0, and `bash tests/run-all-236.sh` still reports PASS=12 FAIL=0 SKIP=0. The MEM-02 hard dependency must remain intact.
    - `node tests/test-jtbd-transition-graph-wiring.cjs` still exits 0 at 6 tests / 14 assertions.
    - `bash tests/run-all-240.sh` discovers this file and reports it PASSED. Transcribe the summary line.
    - Hermetic: `$HOME/MindrianRooms` recursive digest identical before and after the run. Transcribe both.
    - `grep -cP '\x{2014}' tests/test-240-jtbd-event-survives-rebuild.cjs` is 0.
    - No new fixture helper was created: `git status --short tests/helpers/` is empty.
  </acceptance_criteria>
  <done>The promote path and the rebuild-survival path are joined by one test, with the registry trap, the dedupe trap and the hand-fed-state trap each closed and each converted into a pinned assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the correct mutation, and record the WRONG one staying green as a deliberate counter-demonstration</name>
  <files>tests/test-240-jtbd-event-survives-rebuild.cjs</files>
  <read_first>
    - `lib/core/lazygraph-ops.cjs:60-90`. The header explicitly warns that widening `INDEXER_OWNED_NODE_TYPES` "to be safe" reintroduces the data loss at a narrower scope. Read it so you understand what the mutation is simulating, and so you restore it exactly.
    - `lib/core/lazygraph-ops.cjs:660-750`, `rebuildGraph`'s `BEGIN` at 668 and `COMMIT` / `ROLLBACK` at 743 / 745, which is the SECOND mutation site. Note `:567` / `:571` / `:577` is a DIFFERENT transaction (`indexArtifact`); do not confuse them.
    - `tests/test-236-rebuild-preserves-journal.cjs:18-32`, the header's mutation note and its explicit "NOTE ON WHAT DOES NOT TURN THIS RED" paragraph.
    - `.planning/STATE.md`, the Phase 236 Plan 02 entry, for the recorded live result that wrap removal left the survival scenario green because the scoped DELETE means the irreplaceable rows are never touched even when atomicity is gone.
    - `.planning/phases/240-memory/240-RESEARCH.md` Pitfall 4 IN FULL, and resolution R-16 in this plan's objective.
    - `scripts/hsi-to-graph.cjs`, DO NOT OPEN FOR EDITING. It is Phase 242 / MOAT-01 territory and the Phase 236 planner resolutions record that it stays provably disjoint. Confirm with an empty diff at the end.
  </read_first>
  <action>
    Run both mutations against `lib/core/lazygraph-ops.cjs`, one at a time, restoring fully between them. Every observation goes in the SUMMARY verbatim, including the failure messages.

    MUTATION A, THE CORRECT ONE. Change `INDEXER_OWNED_NODE_TYPES` at line 81 from `Object.freeze(['Artifact', 'Section'])` to `Object.freeze(['Artifact', 'Section', 'memory_event'])`. Run `node tests/test-240-jtbd-event-survives-rebuild.cjs`. EXPECT scenario 3 RED, and record precisely which of its assertions failed and with what message. Also run `node tests/test-236-rebuild-preserves-journal.cjs` under the same mutation and record whether it too reddens, since it should: the two tests guard the same allowlist from different rows. Then restore line 81 EXACTLY, re-run both, confirm both green, and confirm `git diff --quiet lib/core/lazygraph-ops.cjs`.

    MUTATION B, THE WRONG ONE, run deliberately as a counter-demonstration and NOT as proof of anything. Comment out `rebuildGraph`'s `BEGIN` at 668 and its `COMMIT` at 743 (leave the `ROLLBACK` path syntactically valid, or comment the whole try-wrap consistently so the file still parses). Run `node tests/test-240-jtbd-event-survives-rebuild.cjs`. EXPECT it to stay GREEN. Record the observed result. If it stays green, that is the finding and it must be written into the SUMMARY as the empirical confirmation that ROADMAP SC2's "riding Phase 236's transaction wrap" phrasing does NOT describe what protects these rows. If it unexpectedly reddens, that is a DIFFERENT and more interesting finding: record it, state which assertion reddened, and do not quietly adopt the wrong mutation as the phase's proof. Restore both lines exactly and confirm `git diff --quiet lib/core/lazygraph-ops.cjs`.

    Then append a short `MUTATIONS RUN` block to the test file's own header recording both outcomes in the file itself, in the shape `tests/test-236-rebuild-preserves-journal.cjs:18-32` uses, so a future reader of the test inherits the finding without reading a SUMMARY. State: mutation A reddens scenario 3; mutation B does not redden anything; this file pins DELETE SCOPE, not atomicity; the transaction wrap is pinned by `tests/test-236-rebuild-preserves-journal.cjs` scenario 2 instead. This is the only edit this task makes to the test file.

    Then run the MEM-02 regression set and record each result against its expected value: `bash tests/run-all-236.sh` (expect PASS=12 FAIL=0 SKIP=0), `node tests/test-236-rebuild-preserves-journal.cjs`, `node tests/test-jtbd-transition-graph-wiring.cjs` (6 tests / 14 assertions), `node tests/test-129-spine-substrate.cjs` (15/15), `node tests/test-navigation-memory-events.cjs`, `node tests/test-150-memory-nodes.cjs`, `node tests/test-150-brain-egress.cjs` (the Canon Part 8 MEM-04 zero-prose invariant), and `bash tests/run-all-240.sh`.

    No em-dashes. Do not edit any production file beyond the two temporary mutations, and confirm both are fully reverted.
  </action>
  <verify>
    <automated>node tests/test-240-jtbd-event-survives-rebuild.cjs && bash tests/run-all-236.sh && bash tests/run-all-240.sh && git diff --quiet lib/ scripts/ && echo "production tree clean"</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION A executed, scenario 3 observed RED with the failing assertion and message transcribed verbatim, then restored and re-confirmed green.
    - Under MUTATION A, `node tests/test-236-rebuild-preserves-journal.cjs`'s behavior is also recorded, whichever way it goes.
    - MUTATION B executed, the observed result transcribed. If green, the SUMMARY states in plain words that the transaction-wrap phrasing does not describe what protects these rows and cites Pitfall 4 plus STATE.md's Phase 236 Plan 02 result. If red, the SUMMARY records which assertion reddened and does NOT adopt it as the phase's mutation proof.
    - Both mutations fully reverted: `git diff --quiet lib/core/lazygraph-ops.cjs` passes, and `git diff --quiet lib/ scripts/` passes.
    - `git diff --stat scripts/hsi-to-graph.cjs` is EMPTY. Phase 242 / MOAT-01 stays provably disjoint.
    - The test file's header carries a `MUTATIONS RUN` block recording both outcomes, so the finding lives in the file and not only in a SUMMARY.
    - Every command in the regression set is run and recorded against its expected value. `bash tests/run-all-236.sh` reports PASS=12 FAIL=0 SKIP=0.
    - `bash tests/run-all-240.sh` green with all four `tests/test-240-*` files (once plan 240-04 has also landed) and all gate legs. If 240-04 has not landed yet in this wave, record the exact discovered set rather than rounding it to green.
    - `node tests/test-150-brain-egress.cjs` PASSES, so scenario 6's Canon Part 8 discipline is corroborated by the existing egress guard.
    - `grep -cP '\x{2014}' tests/test-240-jtbd-event-survives-rebuild.cjs` is 0.
    - Hermetic: `$HOME/MindrianRooms` recursive digest identical before and after every run in this task.
  </acceptance_criteria>
  <done>The correct mutation is proven live, the wrong one is proven inert and recorded in the test file itself, and the production tree is confirmed byte-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a destructive reindex -> irreplaceable `nodes` rows | `rebuildGraph` deletes and repopulates a table that mixes derived rows (`Artifact`, `Section`) with original rows (`memory_event`, `claim`, `opportunity`) and has no discriminator column. The frozen allowlist IS the boundary. |
| across-session memory writer -> `room.db` | Every graph write must route through `lib/core/navigation.cjs`, the single SQL navigation chokepoint (Canon Part 9). |
| LOCAL room content -> `memory_event` payload | Canon Part 8: payloads carry scalars and enum handles only, never prose. |
| test process -> the developer's real per-user store | The test drives real `promoteIfEligible`, which writes a Layer 2 store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240-30 | Tampering | a future edit widening `INDEXER_OWNED_NODE_TYPES` and silently erasing the JTBD journal | mitigate | THIS IS MEM-02's gate. Scenario 3 asserts identical node ids AND byte-identical raw `properties` for all three lifecycle rows plus the 236 fixture's own three irreplaceable rows, and Task 2 proves the gate reddens under exactly that widening. The `lazygraph-ops.cjs:60-90` header already warns against widening; this test makes the warning enforceable. |
| T-240-31 | Repudiation | a mutation proof that ships green with the bug intact | mitigate | Pitfall 4 and R-16. The mutation is worded as widening the allowlist, never as removing the transaction wrap. Task 2 runs the wrong mutation on purpose, records it staying green, and writes that finding into the test file's own header so the next reader cannot repeat the error. |
| T-240-32 | Repudiation | the whole test passing over zero rows because the room is unregistered | mitigate | Trap A. `tests/helpers/fixture-room-236.cjs` writes `.room-root` but not `.rooms/registry.json`, so `resolveRoomDirForSlug` would return null and `logGraphTransition` would be a graceful no-op. The fixture wrapper writes the registry entry and then asserts `resolveRoomDirForSlug('fixture-236')` equals the room dir, throwing a labelled FIXTURE error otherwise. Proven to bite by temporarily skipping the registry write. |
| T-240-33 | Repudiation | a deduped no-op presenting as a successful write | mitigate | Trap B. `logJtbdTransition` derives a `dedupe_key` from `[type, kind, from, to]` and a repeat inside 60 seconds returns `{ok:true, deduped:true}` with no INSERT, and `promoteIfEligible` does not surface that flag. The test asserts EXACTLY one row per kind, and scenario 2 pins the dedupe window as a property rather than leaving it as a hazard. |
| T-240-34 | Repudiation | survival by doing nothing | mitigate | Scenario 4 asserts the rebuild returned `success` with `artifacts` greater than 0 and that the post-rebuild `Artifact` count is non-zero, copying the 236 analog's non-vacuity rationale. |
| T-240-35 | Repudiation | a test proving the fix while hand-feeding fields production never writes | mitigate | Trap C. The promote leg goes through a real `jtbdState.setCurrent` then `getCurrent` and asserts `manual_set === true` first, with a message naming plan 240-03. The acceptance criteria require auditing every `promoteIfEligible` call site for literals. |
| T-240-36 | Information Disclosure | user prose leaking into a `memory_event` payload | mitigate | Canon Part 8. Scenario 6 asserts each payload carries only scalars and enum-shaped handles, and seeds a recognisable prose marker into the room's `ROOM.md` then asserts it appears in none of the three serialized blobs. Corroborated by `node tests/test-150-brain-egress.cjs` in Task 2's regression set. |
| T-240-37 | Tampering | raw SQL added outside the navigation chokepoint | mitigate | The test writes through `navigation.*` and the production writers only; its sole direct SQL is the reads inside `readNodeRow` and `countPopulations`, both already in `tests/helpers/fixture-room-236.cjs`, and `tests/` is on `scripts/check-substrate.cjs`'s ALLOWED_DIRECT_IMPORT list. No production file is edited, so no new write path exists. |
| T-240-38 | Tampering | reversing a human-approved RCA decision by building a drainer | mitigate | Locked decision D-1 and 240-PATTERNS.md's `No Analog Found` row. The test asserts `graph-edge-pending.log` is never re-created and asserts NOTHING about it shrinking. The header states the drainer is deliberately absent and must stay absent. |
| T-240-39 | Tampering | colliding with Phase 242 / MOAT-01 on `scripts/hsi-to-graph.cjs` | mitigate | That file is never opened and its diff is asserted empty in the acceptance criteria, matching the Phase 236 planner resolution that keeps the two phases provably disjoint. |
| T-240-40 | Tampering | the test writing into the developer's live per-user store | mitigate | `MINDRIAN_ROOMS_HOME` is set to a `mkdtempSync` scratch root and restored in `finally`, and the `$HOME/MindrianRooms` digest is asserted unchanged around every run in both tasks. |
| T-240-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable. Zero packages. `node:sqlite` `DatabaseSync` is built in; the floor is `engines.node >= 22.16.0` (Phase 236 GRAPHDB-03) and this machine runs v22.23.1. No `better-sqlite3` idiom may be used: `DatabaseSync` has no `transaction(fn)` helper, confirmed four independent times in Phase 236. |

ASVS note: V4 Access Control as the Canon Part 8 boundary, covered by T-240-36. V5 Input Validation applies to `resolveRoomDirForSlug`, which looks the slug up by exact registry match and never concatenates raw; unchanged here. V6 Cryptography not applicable. V7 Error Handling as the graceful-degradation envelope around `logGraphTransition`, unchanged.
</threat_model>

<verification>
1. `node tests/test-240-jtbd-event-survives-rebuild.cjs` exits 0 with 6 scenarios; full output transcribed.
2. The fixture self-check proven to bite by temporarily skipping the registry write; both observations transcribed.
3. Scenario 3 compares raw `properties` strings for all three lifecycle rows plus the 236 fixture's three irreplaceable rows.
4. Scenario 4's observed `artifacts` count transcribed and greater than 0.
5. Scenario 6's prose canary confirmed absent from all three blobs; marker string transcribed.
6. MUTATION A executed, scenario 3 red, message transcribed, restored, green re-confirmed.
7. MUTATION B executed, observed result transcribed, and the finding written into the test file's own header.
8. `git diff --quiet lib/ scripts/` passes. `git diff --stat scripts/hsi-to-graph.cjs` empty.
9. `bash tests/run-all-236.sh` PASS=12 FAIL=0 SKIP=0. `node tests/test-jtbd-transition-graph-wiring.cjs`, `node tests/test-129-spine-substrate.cjs`, `node tests/test-navigation-memory-events.cjs`, `node tests/test-150-memory-nodes.cjs`, `node tests/test-150-brain-egress.cjs` all as expected.
10. `bash tests/run-all-240.sh` green with the discovered set recorded exactly. Zero em-dashes. `$HOME/MindrianRooms` digest unchanged.
</verification>

<success_criteria>
- The promote, park and complete lifecycle each write exactly one real `jtbd_transitioned` memory_event, and each row survives a real `rebuildGraph` with an identical id and byte-identical properties.
- The rebuild is proven to have done real work in the same run, so survival is not survival-by-inaction.
- The registry trap, the dedupe trap and the hand-fed-state trap are each closed and each converted into a standing assertion.
- Adding `memory_event` to `INDEXER_OWNED_NODE_TYPES` is proven live to turn the gate red, and removing the transaction wrap is proven live NOT to, with that finding recorded inside the test file.
- No drainer is built and nothing asserts the retired log shrinks: the human-approved RCA decision stands.
- The production tree is byte-clean and Phase 242's file is provably untouched.
</success_criteria>

<output>
Create `.planning/phases/240-memory/240-05-SUMMARY.md` when done.
</output>
