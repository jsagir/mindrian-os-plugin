---
phase: 240-memory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-jtbd-auto-anchor-empirical.sh
  - tests/test-jtbd-hook-integration.cjs
  - tests/test-240-memory-store-hermetic-fence.sh
autonomous: true
requirements: [MEM-03]

must_haves:
  truths:
    - "tests/test-jtbd-auto-anchor-empirical.sh has NO code path by which it can reach the developer's real MindrianRooms store: its root is an owned mktemp -d directory, and hermeticity is structural rather than a cleanup trap that has to remember 9 paths"
    - "The whole cleanup apparatus is one rm -rf of the owned root, so the three paths the old trap missed (.memory/audit.log, .memory/ROOM.md, .rooms/.room-graph/rooms.db) cannot survive"
    - "The suite stays hermetic when invoked through tests/run-all-127.3.sh, not only standalone"
    - "tests/test-jtbd-hook-integration.cjs injects a sandboxed MINDRIAN_ROOMS_HOME into every hook subprocess, closing the NEW leak that plan 240-04's trigger fix would otherwise open in 7 of its 9 classes"
    - "The fence hashes EVERY file under the store tree recursively, contents plus relative paths plus the path SET, so a created-and-left file changes the verdict even when jtbd-history.json reads clean"
    - "A deliberately seeded non-hermetic fixture turns the fence RED, and it is seeded against a sandboxed HOME so proving the fence never pollutes the store the fence protects"
    - "A hermetic suite that exits NONZERO does NOT turn the fence red: the fence measures store mutation, never exit code"
  artifacts:
    - path: "tests/test-240-memory-store-hermetic-fence.sh"
      provides: "MEM-03 SC3 recursive store-tree hash fence with a must_catch / must_not_catch pair under a sandboxed HOME"
      min_lines: 180
      contains: "sha256sum"
    - path: "tests/test-jtbd-auto-anchor-empirical.sh"
      provides: "Structurally hermetic empirical JTBD regression suite on an owned mktemp root"
      contains: "mktemp -d"
    - path: "tests/test-jtbd-hook-integration.cjs"
      provides: "Sandboxed MINDRIAN_ROOMS_HOME for every jtbd-update.cjs subprocess"
      contains: "MINDRIAN_ROOMS_HOME"
  key_links:
    - from: "tests/test-jtbd-auto-anchor-empirical.sh"
      to: "an owned mktemp -d root exported as MINDRIAN_ROOMS_HOME"
      via: "shell variable interpolated into both node -e heredocs and inherited by room-registry"
      pattern: "export MINDRIAN_ROOMS_HOME"
    - from: "tests/test-jtbd-hook-integration.cjs runHook"
      to: "a per-suite mkdtemp rooms home"
      via: "explicit child env injection, never inheritance"
      pattern: "MINDRIAN_ROOMS_HOME"
    - from: "tests/test-240-memory-store-hermetic-fence.sh"
      to: "the JTBD and memory suite set"
      via: "sandboxed HOME plus env -u MINDRIAN_ROOMS_HOME so a leaky suite materializes the store inside the sandbox"
      pattern: "env -u MINDRIAN_ROOMS_HOME"
---

<objective>
Close MEM-03. Make the one measured leak structurally impossible, pre-empt the NEW leak that MEM-01's fix would open, and build the fence that turns hermeticity from a habit into a gate.

Purpose: 240-RESEARCH.md Finding 4 measured 16 candidate suites with a sandboxed `HOME` and found exactly ONE that writes into `$HOME/MindrianRooms`: `tests/test-jtbd-auto-anchor-empirical.sh`, with a 9-path footprint whose own cleanup trap misses three paths. That residue is directly observable on this machine right now: `~/MindrianRooms/.memory/audit.log` holds 5 lines all carrying the slug `test-jtbd-127.3-empirical`, and `~/MindrianRooms/.memory/jtbd-history.json` reads `"rooms": {}`. A `jtbd-history.json`-only hash would have read clean for two months while `audit.log` was being polluted, which is why SC3 says the WHOLE `.memory/` tree.

Output: a structurally hermetic empirical suite, a pre-emptively sandboxed hook-integration suite, and `tests/test-240-memory-store-hermetic-fence.sh`.

**R-03, THE PLANNER FOUND A SECOND LEAK THAT DOES NOT EXIST YET AND WILL EXIST THE MOMENT MEM-01 LANDS. This is why MEM-03 is Wave 1 and MEM-01 is Wave 2, inverting 240-VALIDATION.md's provisional wave numbers.**

The research census measured hermeticity against CURRENT, BROKEN code. `tests/test-jtbd-hook-integration.cjs` reads hermetic today only because `promoteIfEligible` always returns null at the turn gate before it ever reaches `atomicUpdateMemory`. Verified at planning time: its `runHook` helper (line 61-67) builds the child env as `Object.assign({}, process.env, env)` where `env` carries only `CLAUDE_ACTIVE_ROOM` and `CLAUDE_USER_MESSAGE`. `MINDRIAN_ROOMS_HOME` is set in exactly ONE place in that file (class 5, line 180, deliberately a non-existent path). So classes 1, 2, 3, 4, 6, 7 and 8 drive the real hook against the REAL `~/MindrianRooms`. Class 8 alone fires the same message 10 consecutive times.

The moment plan 240-04 makes the promotion block reachable and plan 240-03 makes `turn_count` grow, class 8's third turn crosses `NOISE_FLOOR_TURNS = 3` and `promoteIfEligible` writes `~/MindrianRooms/.memory/jtbd-history.json` plus an `audit.log` line, for real, on the developer's machine. **Fixing MEM-01 CREATES a leak.** MEM-03's requirement text is "The JTBD test suite cannot write into the user's live memory store", so covering this is inside MEM-03's scope, not a scope escape. It must land BEFORE MEM-01, which is why this plan is Wave 1 and 240-03 / 240-04 depend on it.

**R-04, the empirical suite's fix is the sandboxed-root redirect, NOT "add the 3 missing paths to the trap".** 240-PATTERNS.md asks the planner to pick between the two on size and on Canon Part 7 fit. The redirect wins on three counts. It is smaller: one `mktemp -d` plus one `rm -rf` replaces the 47-line Python cleanup apparatus at lines 67-113. It is stronger: adding three paths to a trap leaves the failure mode intact, because the trap is still a list someone has to keep in sync with what the suite creates, and this suite already proved that list drifts. And it matches the repo's own `withTmpRoot` ownership discipline (`tests/test-across-session-memory.cjs:100-111`): create the root, use it, delete it, never touch a root you do not own.

**R-05, the suite IGNORES an inherited MINDRIAN_ROOMS_HOME, deliberately.** An honoured override re-opens exactly the problem being closed, because the trap would then have to scrub a root the suite does not own. The fence does not need to inject a root either: it measures whether the store was mutated, and a suite that always uses its own throwaway root satisfies that by construction. Record the ignored-override as a deliberate design note in the file header so a future reader does not "fix" it back.

**Pitfall 7 note for the executor:** every observation in this plan runs scripts directly from `/home/jsagi/dev/MindrianOS-Plugin/`. Never conclude anything about hook behavior from the running Claude Code session, which loads hooks from the stale plugin install cache and never hot-reloads.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/240-memory/240-RESEARCH.md
@.planning/phases/240-memory/240-PATTERNS.md
@.planning/phases/240-memory/240-VALIDATION.md
</context>

## Artifacts this plan produces

| Symbol | Kind | Location |
|---|---|---|
| `tests/test-240-memory-store-hermetic-fence.sh` | new file (bash fence) | `tests/` |
| `mos_tree_content_hash <root>` | bash function | `tests/test-240-memory-store-hermetic-fence.sh` |
| `mos_tree_path_hash <root>` | bash function | `tests/test-240-memory-store-hermetic-fence.sh` |
| `store_fingerprint <root>` | bash function (emits both hashes plus an ABSENT sentinel) | `tests/test-240-memory-store-hermetic-fence.sh` |
| owned `mktemp -d` rooms root | behavior change | `tests/test-jtbd-auto-anchor-empirical.sh` |
| sandboxed `MINDRIAN_ROOMS_HOME` in `runHook` | behavior change | `tests/test-jtbd-hook-integration.cjs` |

The full phase-wide inventory is in `240-01-PLAN.md`. Canon Part 11 (CIRS) is NOT APPLICABLE: nothing here is an invocable surface.

<tasks>

<task type="auto">
  <name>Task 1: Make tests/test-jtbd-auto-anchor-empirical.sh structurally hermetic on an owned mktemp root</name>
  <files>tests/test-jtbd-auto-anchor-empirical.sh</files>
  <read_first>
    - `tests/test-jtbd-auto-anchor-empirical.sh` IN FULL, 231 lines. The whole file is the edit surface: line 57 is the root of the leak, lines 62-114 are the cleanup function and its `trap cleanup EXIT`, line 117 calls `cleanup` as a pre-clean, line 118 creates the room via `scripts/room-registry create`, lines 149-151 and 182-211 are the three subprocess invocations that inherit or interpolate the root.
    - `tests/test-across-session-memory.cjs:100-111`, the canonical `withTmpRoot` ownership idiom this fix mirrors: create, set env, use, restore prior env, `rmrf`.
    - `scripts/room-registry` lines 1-40 and 310-340. Confirm for yourself that line 26 resolves `ROOMS_HOME="${MINDRIAN_ROOMS_HOME:-$HOME/MindrianRooms}"` so an exported override is honoured, and note lines 336-337: `create` spawns `sync-rooms-graph` and `sync-rooms-brain` DETACHED in the background against `$ROOMS_HOME`. Those background writers are what produce `.rooms/.room-graph/rooms.db`, one of the three paths the old trap missed.
    - `tests/run-all-127.3.sh` lines 40-50, the `SHELL_SUITES` array that lists this suite and sets no `MINDRIAN_ROOMS_HOME`. The fix must hold when invoked this way, not only standalone.
    - `.planning/phases/240-memory/240-RESEARCH.md` Finding 4 in full, for the measured 9-path footprint and the three unscrubbed paths.
    - `.planning/phases/240-memory/240-PATTERNS.md` the `tests/test-jtbd-auto-anchor-empirical.sh` section, for the verbatim line 57 and the two child-injection sites.
  </read_first>
  <action>
    Replace line 57's `ROOMS_HOME="${MINDRIAN_ROOMS_HOME:-${HOME}/MindrianRooms}"` with an OWNED throwaway root, created unconditionally and exported so every subprocess inherits it: create it with `mktemp -d` using a recognisable template such as `mos-jtbd-anchor-XXXXXX`, assign it to `ROOMS_HOME`, and `export MINDRIAN_ROOMS_HOME="${ROOMS_HOME}"`. Any inherited `MINDRIAN_ROOMS_HOME` is deliberately overwritten, not honoured.

    Collapse the cleanup. Replace the entire `cleanup()` function body (the `rm -rf "${TEST_ROOM}"` plus BOTH `python3` heredocs, lines 68 through 112) with a single guarded `rm -rf` of the owned root. Guard it so it can never delete something it does not own: refuse to remove unless `ROOMS_HOME` is a non-empty absolute path that matches the `mos-jtbd-anchor-` template and is not `/`, `$HOME`, or `$HOME/MindrianRooms`. Print a loud line and skip the removal if the guard rejects, rather than deleting blind. Keep `trap cleanup EXIT`.

    Delete the now-dead pre-clean call at line 117 (`cleanup` before `room-registry create`): a freshly minted `mktemp -d` root has nothing to pre-clean, and calling the trap function early would delete the root the very next line writes into. Replace it with a `mkdir -p "${ROOMS_HOME}"` if `mktemp -d` semantics need it (it does not, the directory already exists) or simply remove the call.

    Handle the detached background writers. `scripts/room-registry create` at line 118 spawns `sync-rooms-graph` and `sync-rooms-brain` detached against `$ROOMS_HOME`. They may still be writing when the EXIT trap fires, so the `rm -rf` must be tolerant (`|| true`) and a leftover directory under `/tmp` is acceptable. Add a short comment saying exactly this, so a future reader does not add a `wait` or a sleep that makes the suite flaky. Do NOT try to kill them.

    Update the header. Rewrite the "Cleanup discipline" paragraph at lines 44-49 to describe the new contract in Phase 240's own words: the suite owns its rooms root, an inherited `MINDRIAN_ROOMS_HOME` is deliberately ignored, hermeticity is structural rather than a path list, and the reason the old list-based trap was replaced is that it missed three of the nine paths the suite creates (`.memory/audit.log`, `.memory/ROOM.md`, `.rooms/.room-graph/rooms.db`) which is why the real store accumulated 5 leaked audit lines over two months. Reference MEM-03 and plan 240-02.

    Leave `set -euo pipefail` at line 53 as is. Leave the two `node -e` heredocs' `process.env.MINDRIAN_ROOMS_HOME = '${ROOMS_HOME}'` interpolations at lines 184 and 207 as is: they now interpolate the owned root and are correct unchanged. Leave every assertion and both halves of the protocol unchanged; this task changes WHERE the suite writes, never WHAT it proves.

    No em-dashes. No emoji. No `python3` dependency left in the cleanup path.
  </action>
  <verify>
    <automated>bash -n tests/test-jtbd-auto-anchor-empirical.sh && bash tests/test-jtbd-auto-anchor-empirical.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/test-jtbd-auto-anchor-empirical.sh` exits 0.
    - `bash tests/test-jtbd-auto-anchor-empirical.sh` prints `PASS: JTBD auto-anchor empirical regression test` and exits 0.
    - `grep -c 'mktemp -d' tests/test-jtbd-auto-anchor-empirical.sh` is at least 1.
    - `grep -c 'export MINDRIAN_ROOMS_HOME' tests/test-jtbd-auto-anchor-empirical.sh` is at least 1.
    - `grep -c 'python3' tests/test-jtbd-auto-anchor-empirical.sh` is 0. The whole Python cleanup apparatus is gone, not merely bypassed.
    - `grep -c 'HOME}/MindrianRooms' tests/test-jtbd-auto-anchor-empirical.sh` is 0 and `grep -c 'homedir' tests/test-jtbd-auto-anchor-empirical.sh` is 0. There is no code path to the real store.
    - HERMETICITY OBSERVED, not assumed. Capture `find "$HOME/MindrianRooms" -type f | LC_ALL=C sort | xargs -r sha256sum | sha256sum` before and after a standalone run; the two digests must be identical. Transcribe both.
    - HERMETIC UNDER A SANDBOXED HOME TOO: run `env -u MINDRIAN_ROOMS_HOME HOME="$(mktemp -d)" bash tests/test-jtbd-auto-anchor-empirical.sh` and confirm NO `MindrianRooms` directory is created inside that sandbox HOME (`find "$SANDBOX" -maxdepth 2 -name MindrianRooms` is empty). This is the exact census technique from 240-RESEARCH.md Finding 4, and it is the direct proof the leak is closed.
    - HERMETIC THROUGH THE AGGREGATOR: `bash tests/run-all-127.3.sh` runs green, and the same `$HOME/MindrianRooms` digest is unchanged before and after. Transcribe.
    - EXECUTED MUTATION, transcribed in both directions: restore line 57 to `ROOMS_HOME="${MINDRIAN_ROOMS_HOME:-${HOME}/MindrianRooms}"`, re-run under a sandboxed `HOME`, confirm `MindrianRooms/.memory/audit.log` and `MindrianRooms/.memory/ROOM.md` DO appear inside the sandbox, then restore the fix and confirm they do not. This is the leak reproduced and closed under observation, with zero risk to the real store.
    - `grep -cP '\x{2014}' tests/test-jtbd-auto-anchor-empirical.sh` is 0.
  </acceptance_criteria>
  <done>The one measured leak is structurally impossible, the trap is one guarded rm -rf, and the closure is proven by a reproduced-then-fixed leak under a sandboxed HOME.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-empt the NEW leak MEM-01 would open in tests/test-jtbd-hook-integration.cjs</name>
  <files>tests/test-jtbd-hook-integration.cjs</files>
  <read_first>
    - `tests/test-jtbd-hook-integration.cjs` IN FULL, 9 assertion classes. Specifically: `makeRoom` (line 34-38, a bare `mkdtemp` with a `.room-root` sentinel and NO registry), `runHook` (line 61-67, the single child-spawn helper whose env is `Object.assign({}, process.env, env)`), class 5's deliberate fake `MINDRIAN_ROOMS_HOME` at line 175-180 (the ONLY place the variable appears, and it must keep its deliberate fake value), class 8's latency budget at line 237-264 which fires the same message 10 consecutive times and asserts `max <= 500` ms, and line 262 which contains the file's ONE em-dash.
    - `tests/test-memory-hook-integration.cjs:101-110`, the canonical child-process env-injection idiom: `env: Object.assign({}, process.env, { MINDRIAN_ROOMS_HOME: home })`, explicit, never inheritance.
    - `lib/hmi/across-session-memory.cjs:49-55`, `ROOMS_HOME()`, to confirm it re-reads the env on EVERY call and falls back to `path.join(os.homedir(), 'MindrianRooms')` when `MINDRIAN_ROOMS_HOME` is unset. That fallback is the leak.
    - `lib/hmi/across-session-memory.cjs:384-447`, `promoteIfEligible`, to confirm for yourself that the turn gate at line 398 returns BEFORE `atomicUpdateMemory` at line 403, which is the only reason this suite reads hermetic today.
    - `.planning/phases/240-memory/240-02-PLAN.md` resolution R-03 above, for why this is MEM-03 scope rather than a scope escape.
  </read_first>
  <action>
    Add a per-suite sandboxed rooms root and inject it into every hook subprocess.

    Near the other module-level constants, create one sandbox root for the whole file with `fs.mkdtempSync(path.join(os.tmpdir(), 'jtbd-hook-rooms-'))`, assigned to a named constant such as `SANDBOX_ROOMS_HOME`. Register a `process.on('exit', ...)` handler that removes it with `fs.rmSync(..., { recursive: true, force: true })` inside a try/catch, matching the file's existing best-effort `cleanup` style.

    Change `runHook(event, env)` so the child env is built as `Object.assign({}, process.env, { MINDRIAN_ROOMS_HOME: SANDBOX_ROOMS_HOME }, env)`. Note the ORDER: the caller-supplied `env` comes LAST so class 5's deliberate fake non-existent path still overrides the sandbox. Class 5 is testing the registry-resolution-fails path and must keep its own value. Add a comment above the assign stating the ordering rationale and citing MEM-03 plus plan 240-02 R-03: this suite drives the real hook, and once plan 240-04 makes the across-session promotion block reachable, an unsandboxed run would write into the developer's real `~/MindrianRooms/.memory/`.

    Fix the file's one em-dash at line 262, inside the `Class 8: Latency budget` ok() string, replacing that single character with a hyphen (locate it with `grep -nP '\x{2014}' tests/test-jtbd-hook-integration.cjs`). Do NOT sweep any other line in this file: it is not otherwise in this phase's scope and a wide diff makes the phase's own change unreadable.

    Do NOT change any assertion, any class, any threshold, or `makeRoom`. Do NOT add a registry entry: leaving the sandbox registry EMPTY is deliberate and valuable, because `resolveRoomDirForSlug` then returns null and `logGraphTransition` is a graceful no-op, which keeps class 8's 500 ms budget intact by never opening a `room.db`. Say so in the comment.
  </action>
  <verify>
    <automated>node tests/test-jtbd-hook-integration.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-jtbd-hook-integration.cjs` exits 0 with all 9 classes passing, and class 8's reported `max` is still under 500 ms. Transcribe the class 8 line.
    - `grep -c 'MINDRIAN_ROOMS_HOME' tests/test-jtbd-hook-integration.cjs` is at least 3 (the sandbox constant, the `runHook` injection, and class 5's preserved deliberate fake).
    - The injection order is provably caller-last: `grep -n 'Object.assign({}, process.env' tests/test-jtbd-hook-integration.cjs` shows the sandbox object BEFORE the `env` spread argument on that line.
    - Class 5 still passes and still uses its non-existent fake path. Confirm by reading its assertion output, not by assuming.
    - `grep -cP '\x{2014}' tests/test-jtbd-hook-integration.cjs` is 0.
    - HERMETICITY OBSERVED: `$HOME/MindrianRooms` recursive digest identical before and after a run. Transcribe both.
    - HERMETIC UNDER A SANDBOXED HOME: `env -u MINDRIAN_ROOMS_HOME HOME="$(mktemp -d)" node tests/test-jtbd-hook-integration.cjs` creates NO `MindrianRooms` directory inside that sandbox HOME.
    - MUTATION DEFERRED WITH A WRITTEN REASON, not omitted: removing the `MINDRIAN_ROOMS_HOME` injection CANNOT redden anything yet, because `promoteIfEligible`'s turn gate still returns before any store write. Record this honestly in the SUMMARY, state that plan 240-04 Task 3 owns the executed mutation for this leg once MEM-01 lands, and do not claim a proof that was not run.
    - `git diff --stat tests/test-jtbd-hook-integration.cjs` shows a small diff confined to the sandbox constant, the exit handler, `runHook`, the comment, and line 262. No unrelated line touched.
  </acceptance_criteria>
  <done>The leak MEM-01 would have opened is closed before MEM-01 lands, class 8's latency budget survives, and the deferred mutation is recorded rather than faked.</done>
</task>

<task type="auto">
  <name>Task 3: Author the recursive store-tree hash fence with a must_catch / must_not_catch pair</name>
  <files>tests/test-240-memory-store-hermetic-fence.sh</files>
  <read_first>
    - `tests/run-all-236.sh:185-241`, the `must_catch` / `must_not_catch` self-test pair. Both probes MUST run the exact same pipeline the real verdict runs, because a self-test that takes a shortcut proves the pattern bites while saying nothing about whether the real path then swallows the bite.
    - `tests/test-127-03-acceptance-gates.sh:30-40`, the sandboxed-`HOME` plus `env -u` shape (`HOME="$TMPDIR_G1" env -u MINDRIAN_BRAIN_KEY timeout 15 node -e ...`). This is Analog B and the `env -u` idiom to reuse for unsetting `MINDRIAN_ROOMS_HOME`.
    - `.planning/phases/240-memory/240-RESEARCH.md` Pitfall 5 IN FULL (what to hash and why a single filename is wrong) and Pitfall 6 IN FULL (why the seeded non-hermetic fixture must run under a sandboxed HOME), plus Finding 4 conclusion 5 (two suites exit nonzero even when hermetic, and the fence must not convert a pre-existing red into a false hermeticity alarm).
    - `.planning/phases/240-memory/240-PATTERNS.md` the `NEW tests/test-240-memory-store-hermetic-fence.sh` section and the `No Analog Found` table row, which records that NO existing `tests/*.sh` hashes a directory TREE and that `tests/helpers/fs-instrument.cjs` answers a different question (in-process reads, not cross-process writes) and must not be bent to fit.
    - `lib/hmi/across-session-memory.cjs:49-55` for `ROOMS_HOME()`'s resolution order, which is what makes `env -u MINDRIAN_ROOMS_HOME` plus a sandboxed `HOME` the correct measurement seam.
  </read_first>
  <action>
    Create `tests/test-240-memory-store-hermetic-fence.sh`, executable, `set -uo pipefail` (NOT `-e`: suites under measurement are allowed to exit nonzero and the fence must survive that to reach its verdict).

    Header, in Phase 240's own words. State: this fence answers MEM-03 SC3; it measures STORE MUTATION and never exit code, because `tests/test-hmi-compliance-e2e.cjs` and `tests/test-memory-command.cjs` both exit nonzero while hermetic (the latter's 2 Brain Mode A failures are documented pre-existing) and converting those into hermeticity alarms would be a false red; it hashes the whole tree because the leaking suite's old trap deliberately scrubbed its slug out of `jtbd-history.json` while leaving `audit.log` and `ROOM.md`, so a single-filename hash read clean on this machine for two months.

    Define three hash functions, all `LC_ALL=C` sorted so the digest is stable across locales:
      `mos_tree_content_hash <root>`: if the root is absent, echo `ABSENT`. Otherwise `find "$root" -type f -print0`, `LC_ALL=C sort -z`, and for each file emit one line of `<relative-path>  <sha256-of-contents>`, then pipe the whole stream through `sha256sum` and emit just the digest. The relative path is part of the hashed stream so a rename is a change.
      `mos_tree_path_hash <root>`: if the root is absent, echo `ABSENT`. Otherwise `find "$root" -mindepth 1 -print0` (files AND directories, no `-type` filter), `LC_ALL=C sort -z`, and hash the newline-joined list. Directories are included on purpose so a created-and-left EMPTY directory or empty file still changes the verdict even though it contributes no content bytes.
      `store_fingerprint <root>`: echo both digests on one line, space separated. This is the single value compared before and after.

    Define the verdict helper `assert_unmutated <label> <before> <after>`: equal fingerprints mean UNMUTATED, unequal mean MUTATED, and print both fingerprints on a mismatch so the log names what moved.

    Leg 1, the SELF-TEST that the fence bites, run FIRST and before any suite. Under a `mktemp -d` probe root, run four probes through the exact `store_fingerprint` function:
      `must_catch` "a leaked audit line": create `<probe>/.memory/audit.log` with one line, confirm the fingerprint changed.
      `must_catch` "a created-and-left EMPTY file": create `<probe>/.memory/ROOM.md` with zero bytes, confirm the fingerprint changed. This is the leg that proves the path hash is load-bearing, since the content hash alone would be blind to it.
      `must_catch` "a created-and-left EMPTY directory": `mkdir <probe>/.rooms/.room-graph`, confirm the fingerprint changed.
      `must_not_catch` "no change at all": re-fingerprint with nothing touched, confirm equal.
      Tally the four as ONE leg. If any probe misbehaves, FAIL the leg with a message saying the fence is blind and every leg below proves nothing.

    Leg 2, the LIVE-STORE confirmation. Capture the real store root as `${MINDRIAN_ROOMS_HOME:-$HOME/MindrianRooms}` BEFORE any sandboxing, fingerprint `<real-root>/.memory` and `<real-root>/.rooms`, run Leg 3's whole suite sweep, then re-fingerprint and assert unmutated. This leg is read-only against the real store and is the literal SC3 sentence. It can only go red if a suite reaches the real store in a HOME-independent way, which is exactly the residual worth catching.

    Leg 3, the SUITE SWEEP under a sandboxed HOME. Create `SANDBOX_HOME="$(mktemp -d)"`. Build the suite set by GLOB with a floor, never a hand list: `tests/test-jtbd-*.cjs`, `tests/test-jtbd-*.sh`, `tests/test-across-session-memory.cjs`, `tests/test-memory-hook-integration.cjs`, `tests/test-cross-room-memory.cjs`, `tests/test-memory-command.cjs`, plus `tests/run-all-127.3.sh`. EXCLUDE anything matching `tests/test-240-*` so the fence can never recurse into the harness that discovers it. Assert the discovered count is at least 10 and FAIL the leg with an explicit vacuity message if it is not. Fingerprint `$SANDBOX_HOME/MindrianRooms` (expect `ABSENT ABSENT` initially), then run every suite with `env -u MINDRIAN_ROOMS_HOME HOME="$SANDBOX_HOME" timeout 300 <interpreter> <suite>`, printing each suite's exit code for the record but NOT letting it affect the verdict. Re-fingerprint and assert unmutated, which for a fully hermetic set means still `ABSENT ABSENT`.

    Leg 4, the DELIBERATELY NON-HERMETIC FIXTURE, seeded against its OWN sandboxed HOME so proving the fence never touches the real store (Pitfall 6). Create a second `mktemp -d` as `LEAK_HOME`. Write a tiny throwaway probe under a `mktemp -d` scratch directory (NOT inside `tests/`, so it is never discovered by any glob) that appends a line to `$HOME/MindrianRooms/.memory/audit.log`, creating parents. Fingerprint `$LEAK_HOME/MindrianRooms` before, run the probe with `env -u MINDRIAN_ROOMS_HOME HOME="$LEAK_HOME"`, fingerprint after, and assert the fence reports MUTATED. A report of UNMUTATED here FAILS the leg, because the fence would then be blind to a real leak. Remove the scratch probe.

    Leg 5, the EXIT-CODE INDEPENDENCE guard. Under a third `mktemp -d` HOME, run a throwaway probe that writes nothing to the store and exits 1. Assert the fence reports UNMUTATED and that the leg PASSES despite the nonzero exit. This pins Finding 4 conclusion 5 so a future edit cannot quietly fold exit codes into the verdict.

    Footer: a `PASS=` / `FAIL=` tally, a banner, and a final `[ "$FAIL" -eq 0 ]`. Emit no line starting with `SKIP` unless the fence genuinely cannot run, since `run_may_skip` in `tests/run-all-240.sh` tallies such a line as SKIP.

    Every temporary root gets a `trap` that removes it. Zero network reach. No em-dashes. No emoji. Use `sha256sum` (already used by `tests/run-all-1441.sh` and `tests/test-127-01-migration-safety.sh`); do not hand-roll a digest and do not add a dependency.
  </action>
  <verify>
    <automated>bash -n tests/test-240-memory-store-hermetic-fence.sh && bash tests/test-240-memory-store-hermetic-fence.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/test-240-memory-store-hermetic-fence.sh` exits 0 and `test -x` succeeds.
    - `bash tests/test-240-memory-store-hermetic-fence.sh` exits 0 with all five legs PASSED. Transcribe the full output.
    - `bash tests/run-all-240.sh` now discovers the fence via `run_may_skip` and reports it PASSED, and the harness no longer hits the `found -eq 0` guard. Transcribe the summary line.
    - The suite sweep's discovered count is printed and is at least 10. Prove the floor bites: temporarily narrow the glob to one suite, observe the vacuity FAIL, restore. Transcribe both.
    - The self-test's empty-file and empty-directory probes both report CHANGED. Prove the path hash is load-bearing: temporarily make `store_fingerprint` emit only the content hash, observe those two probes go red, restore. Transcribe both. This is the direct proof of Pitfall 5's design requirement.
    - Leg 4 reports MUTATED for the seeded leak, and the leak lands ONLY inside `$LEAK_HOME`. Confirm afterward that `$HOME/MindrianRooms/.memory` is byte-identical to its pre-run digest, so proving the fence did not pollute the store the fence protects.
    - Leg 5 PASSES with a probe that exits 1, proving the verdict ignores exit codes.
    - `grep -c 'test-240-' tests/test-240-memory-store-hermetic-fence.sh` shows the exclusion is present, and the sweep's discovered list printed in the output contains no `test-240-*` entry.
    - `grep -c 'env -u MINDRIAN_ROOMS_HOME' tests/test-240-memory-store-hermetic-fence.sh` is at least 3 (sweep, leak probe, exit-code probe).
    - `grep -cP '\x{2014}' tests/test-240-memory-store-hermetic-fence.sh` is 0.
    - `git status --short` shows only this plan's three named files changed relative to the dirty set 240-01 recorded. No probe file, no scratch directory, no `lib/mcp` residue left behind.
  </acceptance_criteria>
  <done>Hermeticity is enforced by a gate that provably bites on a real seeded leak, is blind to exit codes, cannot pass on an empty discovery, and never touches the real store while proving any of it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> the developer's real per-user store at `~/MindrianRooms/.memory/` | The store is shared mutable per-USER state with no schema discriminator between real and test-written rows. This is the boundary MEM-03 exists to defend. |
| `HOME` / `MINDRIAN_ROOMS_HOME` environment -> store root resolution | `ROOMS_HOME()` re-reads the env on every call, so the root a subprocess writes to is decided entirely by environment, which is both the leak and the fix. |
| detached background writers (`sync-rooms-graph`, `sync-rooms-brain`) -> the owned temp root | `room-registry create` spawns them detached; they may still be writing when the EXIT trap fires. |
| the fence's own proof fixture -> the store the fence protects | Proving a leak detector works requires causing a leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-240-06 | Tampering | a test suite writing into the developer's live per-user memory store | mitigate | THIS IS MEM-03. Task 1 removes every code path to the real store from the one measured leaker by giving it an owned `mktemp -d` root, and Task 2 injects a sandboxed `MINDRIAN_ROOMS_HOME` into the suite that would newly leak once MEM-01 lands. Both are proven by a recursive before/after digest of `$HOME/MindrianRooms` and by a sandboxed-`HOME` census run. |
| T-240-07 | Repudiation | a fence that reads clean while the store is polluted | mitigate | Pitfall 5's exact failure. The verdict hashes contents, relative paths, AND the path set including directories, so a created-and-left empty file or empty directory changes it. Proven by temporarily reducing the fingerprint to the content hash alone and observing two self-test probes go red. |
| T-240-08 | Tampering | the fence's proof fixture polluting the real store | mitigate | Pitfall 6. The seeded non-hermetic probe runs under its own `mktemp -d` `HOME` with `MINDRIAN_ROOMS_HOME` unset, and the acceptance criteria re-confirm the real store's digest afterward. The probe lives under a scratch `mktemp -d`, never inside `tests/`, so no glob can pick it up. |
| T-240-09 | Repudiation | a hermetic suite with a pre-existing nonzero exit read as a hermeticity failure | mitigate | Finding 4 conclusion 5. The verdict is store mutation only; exit codes are printed for the record and explicitly excluded. Leg 5 pins this with a probe that exits 1 and writes nothing. |
| T-240-10 | Repudiation | the fence passing because it discovered no suites | mitigate | The sweep is glob-discovered with an at-least-10 floor that FAILS the leg on a shortfall, and the floor is proven to bite by narrowing the glob and observing the vacuity failure. |
| T-240-11 | Denial of Service | a guarded `rm -rf` deleting a root the suite does not own | mitigate | The cleanup refuses to remove unless the path is absolute, non-empty, matches the `mos-jtbd-anchor-` template, and is not `/`, `$HOME`, or `$HOME/MindrianRooms`. On rejection it prints loudly and skips rather than deleting blind. |
| T-240-12 | Denial of Service | detached background writers racing the EXIT trap | accept | `room-registry create` spawns `sync-rooms-graph` and `sync-rooms-brain` detached. The `rm -rf` is tolerant and a leftover directory under `/tmp` is harmless. Killing them or sleeping would make the suite flaky, so the residual is accepted and documented in the file rather than papered over. |
| T-240-13 | Information Disclosure | `sync-rooms-brain` firing during the empirical suite | accept | Canon Part 8 concern, pre-existing and unchanged by this plan. It is spawned by `room-registry create`, not by anything added here, and under the owned temp root it has no real user content to carry. Noted rather than silently ignored; not this phase's scope. |
| T-240-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable. Zero packages. `sha256sum` and `mktemp` are coreutils; the Python dependency is REMOVED by Task 1, reducing rather than adding surface. |

ASVS note: V6 Cryptography applies as integrity checking only, discharged with `node`-free `sha256sum` from coreutils, never a hand-rolled digest. V7 Error Handling / Logging applies to the fence's verdict messages, which must name what moved rather than reporting a bare pass or fail.
</threat_model>

<verification>
1. `bash tests/test-jtbd-auto-anchor-empirical.sh` green, and `$HOME/MindrianRooms` digest unchanged before and after.
2. Under a sandboxed `HOME`, the empirical suite creates no `MindrianRooms` directory; with line 57 mutated back it DOES. Both observations transcribed.
3. `bash tests/run-all-127.3.sh` green, digest unchanged.
4. `node tests/test-jtbd-hook-integration.cjs` green with 9 classes, class 8 max under 500 ms, digest unchanged, zero em-dashes.
5. `bash tests/test-240-memory-store-hermetic-fence.sh` exits 0 with five legs PASSED, full output transcribed.
6. The content-hash-only mutation reddens the empty-file and empty-directory probes; restored.
7. The narrowed-glob mutation reddens the vacuity floor; restored.
8. Leg 4 reports MUTATED for the seeded leak, and the real store is confirmed untouched afterward.
9. `bash tests/run-all-240.sh` discovers the fence and reports it PASSED.
10. Zero em-dashes across all three files.
</verification>

<success_criteria>
- The single measured leak is structurally impossible, not merely trapped, and its closure is proven by reproducing the leak under a sandboxed HOME and then closing it.
- The suite that MEM-01's fix would have turned into a second leaker is sandboxed before MEM-01 lands.
- A recursive whole-tree fence exists, provably bites on a real seeded leak, ignores exit codes, refuses to pass on an empty discovery, and never pollutes the store it protects.
</success_criteria>

<output>
Create `.planning/phases/240-memory/240-02-SUMMARY.md` when done.
</output>
