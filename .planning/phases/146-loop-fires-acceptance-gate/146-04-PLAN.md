---
phase: 146-loop-fires-acceptance-gate
plan: 04
type: execute
wave: 2
depends_on: [146-01, 146-02, 146-03]
files_modified:
  - scripts/doctor.cjs
  - tests/run-all-146.sh
  - commands/explain-decision.md
autonomous: true
requirements: [ACPT-01, ACPT-02, ACPT-03, ACPT-04, ACPT-05]
canon_parts: [Part 3, Part 6, Part 8]
must_haves:
  truths:
    - "doctor --dogfood-acceptance composes all 5 ACPT drivers and exits 0 only when every hermetic leg passes (the milestone-ships-as-Larry-Reaches gate)"
    - "tests/run-all-146.sh composes the 5 ACPT suites AND re-runs the upstream run-all-{144,1441,145} gates so 146 certifies the FULL connector surface, not a sample"
    - "The gate has an honest exit-code contract: exit 0 = milestone ships as Larry Reaches; non-zero = the milestone is renamed (it does not ship as Larry Reaches)"
    - "The live-Brain ACPT-05 mode_a arm is NOT required for the CI gate (its hermetic mode_b arm is); the operator's in-room live proof is named, not faked"
  artifacts:
    - path: "scripts/doctor.cjs"
      provides: "--dogfood-acceptance gate class: runs the 5 ACPT drivers as child processes with its own non-zero-on-any-failure exit contract; --help + usageText updated"
      contains: "dogfood-acceptance"
    - path: "tests/run-all-146.sh"
      provides: "the Phase 146 aggregator: 5 ACPT suites + re-run of run-all-{144,1441,145} for full-surface certification"
      min_lines: 60
  key_links:
    - from: "scripts/doctor.cjs --dogfood-acceptance"
      to: "tests/test-acpt-0{1,2,3,4,5}-*.cjs"
      via: "child_process spawn of each ACPT driver"
      pattern: "test-acpt-0"
    - from: "tests/run-all-146.sh"
      to: "tests/run-all-144.sh + tests/run-all-1441.sh + tests/run-all-145.sh"
      via: "re-run of the upstream gates (full-surface certification)"
      pattern: "run-all-1441.sh"
---

<objective>
Build the gate host + aggregator that composes the 5 ACPT drivers (Plans 01-03)
into the single milestone gate -- the deliverable that certifies the loop FIRES
across the FULL connector surface.

Two surfaces, per the audit Section 5 (gate host = doctor; the loop-fires gate is
hosted in doctor --acceptance class) and the phase shape (an aggregator composing
all 5 ACPT + re-running the upstream run-all gates):

1. scripts/doctor.cjs gains a NEW gate flag --dogfood-acceptance (a NEW gate
   class with its OWN exit-code contract -- 0 = all ACPT hermetic legs passed;
   non-zero = a leg failed -- separate from the existing release --acceptance,
   mirroring how --acceptance is "NOT a class flag" with its own contract). It
   extends the existing --acceptance host pattern (reuse the --brain-smoke /
   buildAcceptanceChecklist child-process-point shape) by running the 5 ACPT
   drivers as child processes and aggregating their exit codes.
2. tests/run-all-146.sh composes the 5 ACPT suites AND re-runs the upstream
   run-all-144.sh + run-all-1441.sh + run-all-145.sh gates so Phase 146 certifies
   the FULL surface (143.3 Tier A + 144 criticals + 144.1 Tiers B/C/D + the
   registry-complete 114-surface coverage + 145 cadence), not a sample.

Exit 0 = the milestone ships as "Larry Reaches". Non-zero = the milestone is
renamed (per the ROADMAP Phase 146 mandate). The gate is honest: the live-Brain
ACPT-05 mode_a arm is the operator's in-room proof (human-verified in Plan 03),
NOT a CI requirement; the CI gate requires the hermetic mode_b arm.

Output: the doctor gate flag + the run-all-146.sh aggregator + an explain-decision
command-doc note pointing at --dogfood-acceptance as the loop-fires assertion host.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/MINDRIAN-CANON.md

# The gate host to extend (the existing --acceptance class + child-process point pattern):
@scripts/doctor.cjs

# The upstream gates to re-run (full-surface certification):
@tests/run-all-144.sh
@tests/run-all-1441.sh
@tests/run-all-145.sh

# The assertion-surface command doc:
@commands/explain-decision.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: doctor --dogfood-acceptance gate class</name>
  <files>scripts/doctor.cjs, commands/explain-decision.md</files>
  <read_first>
    - scripts/doctor.cjs parseArgs (lines ~105-234: flags object + the arg loop; --acceptance/--pre-tag/--brain-smoke registration; --all activation; the class-flag vs own-contract distinction documented at lines ~147-163)
    - scripts/doctor.cjs usageText (lines ~236-312: the flag help blocks; the "Release-gate runner" section the new gate doc sits beside)
    - scripts/doctor.cjs runAcceptance + buildAcceptanceChecklist (lines ~2327-2951: the checklist-point shape { id, label, run: async () -> { ok, finding, detail } } + the runner that filters, runs each point, collects failed[], returns summary; the child_process.spawnSync pattern in the verify-release point at ~2447 is the exact model for spawning the ACPT drivers)
    - scripts/doctor.cjs main dispatch (line ~3480: `if (flags.acceptance) { ... }` -- the parallel dispatch site for the new flag)
  </read_first>
  <action>
    Add a new flag --dogfood-acceptance to parseArgs (flags.dogfoodAcceptance = false; register `else if (arg === '--dogfood-acceptance') flags.dogfoodAcceptance = true;`). Document it in usageText in a new "Loop-fires gate (Phase 146 -- the milestone gate)" block: it runs the 5 ACPT dogfood drivers and has its OWN exit-code contract (0 = all ACPT hermetic legs passed; non-zero = a leg failed); it is NOT a class flag (the class-flag-always-exit-0 invariant does NOT apply); per the ROADMAP mandate exit 0 = the milestone ships as "Larry Reaches", non-zero = the milestone is renamed.

    Implement runDogfoodAcceptance(opts) modeled on runAcceptance: build a list of the 5 ACPT drivers (tests/test-acpt-01-engine-fires.cjs ... test-acpt-05-brain-derive-tier-rise.cjs), each a point { id: 'ACPT-01'..'ACPT-05', label, run: spawn `node <driver>` via child_process.spawnSync with a 120000ms timeout, ok = status === 0 }. Collect per-point pass/fail + a final summary { total, passed, failed, failed_points }. Honor --json (machine-readable result) consistent with the existing acceptance output. The ACPT-05 driver's LIVE mode_a arm self-skips when Brain is unreachable (Plan 03), so the gate passes on the hermetic legs without a reachable Brain -- the gate must NOT require Brain.

    Wire the dispatch in main: add `if (flags.dogfoodAcceptance) { const result = await runDogfoodAcceptance({ pluginRoot: PLUGIN_ROOT }); print/render; process.exit(result.summary.failed > 0 ? 1 : 0); return; }` near the existing `if (flags.acceptance)` site, BEFORE the class-flag exit-0 path so the own-contract exit code wins. Keep Part 8: the drivers are LOCAL/hermetic; --dogfood-acceptance adds zero network surface in doctor itself.

    Update commands/explain-decision.md with a short note: explain-decision is the loop-fires assertion surface (routing_source: engine + fired reach_id + posture); the Phase 146 loop-fires gate is hosted at `doctor --dogfood-acceptance` (and tests/run-all-146.sh). Do NOT change explain-decision's behavior -- doc note only. No em-dashes anywhere.
  </action>
  <verify>
    <automated>node scripts/doctor.cjs --help | grep -i "dogfood-acceptance" && node -e "const d=require('./scripts/doctor.cjs');" 2>/dev/null; node --check scripts/doctor.cjs && echo DOCTOR_PARSES</automated>
  </verify>
  <done>node --check scripts/doctor.cjs passes; node scripts/doctor.cjs --help shows the --dogfood-acceptance block with its own-exit-code contract; the flag dispatches runDogfoodAcceptance which spawns the 5 ACPT drivers and exits non-zero iff any failed; commands/explain-decision.md names the gate host; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: tests/run-all-146.sh aggregator (5 ACPT + upstream re-runs)</name>
  <files>tests/run-all-146.sh</files>
  <read_first>
    - tests/run-all-144.sh (the EXACT runner skeleton to clone: set -uo pipefail, SCRIPT_DIR, SHELL_SUITES + CJS_SUITES arrays, the per-suite PASS/FAIL loop that runs to completion even on failure, the final tally + exit 1 if any failed; CJS entries resolve relative to tests/ and may be "../lib/..." )
    - tests/run-all-1441.sh (the pattern for composing OTHER run-all-*.sh aggregators as SHELL_SUITES + a direct-invocation point -- the 146 aggregator re-runs run-all-144/1441/145 the same way)
    - tests/run-all-145.sh header (the "what this aggregator PROVES" + per-suite plan-ownership header convention to mirror)
  </read_first>
  <action>
    Create tests/run-all-146.sh by cloning the run-all-144.sh skeleton exactly (bash only, no emoji, no em-dashes, runs to completion, exits non-zero if any constituent failed). Header documents what it PROVES: the loop FIRES end-to-end across the FULL surface -- all 5 ACPT criteria (each driving a REAL shipped unit) PLUS the upstream gates re-run so 146 certifies the full connector surface, not a sample.

    Compose two groups:
    (a) CJS_SUITES = the 5 ACPT drivers (test-acpt-01-engine-fires.cjs, test-acpt-02-websearch-hat-scoped.cjs, test-acpt-03-first-material-explore.cjs, test-acpt-04-filing-cascade-surfaces.cjs, test-acpt-05-brain-derive-tier-rise.cjs).
    (b) SHELL_SUITES = the upstream aggregators re-run for full-surface certification: run-all-144.sh (engine flip + real-fired-sensor), run-all-1441.sh (registry complete + exhaustive 114-surface coverage + Part-8 boundary), run-all-145.sh (cadence + Phase-140 hardening). Mirror run-all-1441.sh's way of running a sibling run-all as a shell suite.

    Per-suite plan-ownership header lines map each ACPT driver to its plan (01/01/02/02/03) and name the three upstream gates as the full-surface re-runs. The final tally exits 0 only when every constituent (5 ACPT + 3 upstream) is green -- this exit 0 is the "milestone ships as Larry Reaches" signal; non-zero renames the milestone. chmod +x the file.
  </action>
  <verify>
    <automated>bash -n tests/run-all-146.sh && echo SYNTAX_OK && grep -q "run-all-1441.sh" tests/run-all-146.sh && grep -q "test-acpt-05" tests/run-all-146.sh && echo COMPOSES_OK</automated>
  </verify>
  <done>bash -n tests/run-all-146.sh passes; the aggregator lists all 5 ACPT drivers AND re-runs run-all-144.sh + run-all-1441.sh + run-all-145.sh; it runs to completion on any failure and exits non-zero iff any constituent failed; executable bit set; zero em-dashes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the full loop-fires gate end-to-end</name>
  <what-built>
    The complete Phase 146 gate: `doctor --dogfood-acceptance` (composes the 5 ACPT
    drivers with its own exit-code contract) AND `tests/run-all-146.sh` (the 5 ACPT
    drivers + the re-run of run-all-144/1441/145 for full-surface certification).
    Exit 0 = the milestone ships as "Larry Reaches"; non-zero = it is renamed.
  </what-built>
  <how-to-verify>
    1. Run `bash tests/run-all-146.sh` from the repo root and confirm it exits 0
       with every constituent green: the 5 ACPT drivers PASS and the three upstream
       gates (run-all-144 / run-all-1441 / run-all-145) re-run PASS. Confirm the
       ACPT-05 live mode_a arm either RAN (Brain reachable) or SKIPPED honestly
       (Brain down) -- and that the hermetic mode_b arm proved the tier rise either
       way.
    2. Run `node scripts/doctor.cjs --dogfood-acceptance` and confirm it reports
       all 5 ACPT points passed and exits 0.
    3. Negative sanity (prove it is not a false-green): temporarily break one ACPT
       driver's assertion (or point the gate at a cold room) and confirm BOTH
       surfaces exit non-zero and name the failing ACPT point. Revert.
    4. Confirm the gate required NO live Brain / live web / live Vercel to reach
       exit 0 (the hermetic legs carry the CI gate); the live ACPT-05 arm is the
       operator's in-room proof, named not faked.
  </how-to-verify>
  <resume-signal>Type "approved" once both gate surfaces exit 0 green on the hermetic legs and the negative sanity confirms non-zero on a broken leg; or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor --dogfood-acceptance -> ACPT drivers | doctor spawns LOCAL node drivers as child processes; no network surface added in doctor |
| run-all-146.sh -> upstream gates | re-runs sibling run-all-*.sh; all LOCAL/hermetic |
| ACPT-05 live arm -> Brain | the only optional network touch (the live mode_a arm); self-skips when Brain is down; never a CI requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-13 | Spoofing (false-green) | the aggregate gate | mitigate | the gate composes drivers that each carry an adversarial honest-negative (cold room -> legacy; empty input -> null finding; unavailable derivation -> tier_0); Task 3 negative-sanity step breaks a leg and confirms non-zero -- the Part-6 prove-it-fires-not-exists mandate, enforced at the aggregate |
| T-146-14 | Spoofing (false-green via skip) | ACPT-05 live arm in the gate | mitigate | the gate requires the hermetic mode_b arm (not the live mode_a arm) so a down Brain can never make the gate fake mode_a; the live arm self-skips honestly and the gate still proves the tier rise via mode_b |
| T-146-15 | Tampering (sampling not full surface) | run-all-146.sh | mitigate | the aggregator re-runs run-all-1441.sh (the exhaustive 114-surface coverage gate) + run-all-144.sh + run-all-145.sh, so the gate certifies the FULL connector surface, not a 5-criteria sample (the ROADMAP fully-wired mandate) |
| T-146-16 | Information Disclosure | doctor gate child processes | accept | doctor spawns LOCAL drivers only; the drivers' own Part-8 sweeps (Plans 01-03) guard egress; doctor adds zero network surface (low risk) |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | this plan adds NO new package installs (node built-ins + bash + existing project libs only); no legitimacy checkpoint required |
</threat_model>

<verification>
- node --check scripts/doctor.cjs passes; node scripts/doctor.cjs --help shows the --dogfood-acceptance block
- bash -n tests/run-all-146.sh passes; it lists the 5 ACPT drivers + re-runs run-all-144/1441/145
- bash tests/run-all-146.sh exits 0 with all constituents green (hermetic legs); a broken leg makes it exit non-zero (Task 3 negative sanity)
- node scripts/doctor.cjs --dogfood-acceptance exits 0 with all 5 ACPT points passed (hermetic legs); the gate needs NO live Brain/web/Vercel
- grep -c em-dash (U+2014) across scripts/doctor.cjs diff + tests/run-all-146.sh + commands/explain-decision.md diff returns 0
</verification>

<success_criteria>
The Phase 146 milestone gate exists in two composable surfaces (doctor
--dogfood-acceptance + tests/run-all-146.sh), each with an honest exit-code
contract: exit 0 (all 5 ACPT hermetic legs + the re-run full-surface gates green)
= the milestone ships as "Larry Reaches"; non-zero = the milestone is renamed. The
gate certifies the FULL connector surface (via the run-all-1441 re-run), drives the
REAL shipped unit for every criterion, carries adversarial negatives, and requires
no live Brain/web/Vercel to be CI-green.
</success_criteria>

<output>
Create `.planning/phases/146-loop-fires-acceptance-gate/146-04-SUMMARY.md` when done
</output>
