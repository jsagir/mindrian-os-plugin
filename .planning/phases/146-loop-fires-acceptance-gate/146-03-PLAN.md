---
phase: 146-loop-fires-acceptance-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-acpt-05-brain-derive-tier-rise.cjs
autonomous: false
requirements: [ACPT-05]
canon_parts: [Part 8, Part 9]
user_setup:
  - service: brain
    why: "ACPT-05 live arm proves a FULL Brain-query derivation flips tier_mode to mode_a; this requires a reachable Brain MCP. The hermetic CI arm (mode_b via a LOCAL no-Brain derivation) needs no Brain and runs autonomously."
    env_vars:
      - name: MINDRIAN_BRAIN_KEY
        source: "Brain MCP key resolver (lib/core/resolve-brain-key.cjs) -- the live derivation arm only"
    dashboard_config:
      - task: "Brain MCP reachable (mindrian-brain.onrender.com) for the live mode_a arm"
        location: ".mcp.json brain server + /mos:setup brain"
must_haves:
  truths:
    - "BRAIN.md derives for a room's section -> readQuadruple returns a non-null brain field -> resolveTierMode rises above tier_0 (mode_b on the hermetic LOCAL-derivation arm; mode_a on the live-Brain arm)"
    - "The hermetic arm proves the tier rise with NO Brain reachable (a LOCAL no-Brain-query derivation produces a fresh brain-authored BRAIN.md -> mode_b)"
    - "The derivation + tier-read path carries zero user content toward Brain (Part 8); the tier read is a pure LOCAL readQuadruple (Part 9 SQL/file is the local mind)"
  artifacts:
    - path: "tests/test-acpt-05-brain-derive-tier-rise.cjs"
      provides: "ACPT-05 dogfood driver: BRAIN.md derive -> readQuadruple -> resolveTierMode above tier_0; hermetic mode_b arm always runs; live mode_a arm gated on Brain reachability"
      min_lines: 100
  key_links:
    - from: "tests/test-acpt-05-brain-derive-tier-rise.cjs"
      to: "lib/core/folder-memory.cjs readQuadruple + lib/core/navigation-engine-shared.cjs resolveTierMode"
      via: "the LOCAL quadruple read + the Section-5 tier resolver"
      pattern: "resolveTierMode"
---

<objective>
Build the ACPT-05 dogfood driver: prove that when BRAIN.md derives for a room's
section, tier_mode rises above tier_0 -- driving the REAL readQuadruple +
resolveTierMode, honest about the hermetic-vs-live split.

ACPT-05 is the one criterion whose FULL form (a live Brain-query derivation that
flips tier_mode to mode_a) genuinely needs a reachable Brain. Per the hard fence
(if a criterion cannot be proven hermetically, mark that leg autonomous:false +
human_needed AND ship a hermetic mock leg for CI; never silently fake green) this
plan ships BOTH:

- HERMETIC ARM (always runs in CI, autonomous): a LOCAL no-Brain-query derivation
  (the path lib/core/brain-derivation.cjs deriveSection already supports, which
  writes a fresh, brain-authored BRAIN.md from the LOCAL triple with NO Brain
  query) produces a parseable BRAIN.md. readQuadruple then returns a non-null
  brain field, and resolveTierMode(quadruple, brainAvailable=false) returns
  mode_b -- ABOVE tier_0. This proves the tier rise without any Brain.
- LIVE ARM (autonomous:false, human_needed): with Brain reachable
  (brain-client.isAvailable()===true), a full deriveSection produces a fresh
  BRAIN.md and resolveTierMode(quadruple, true) returns mode_a. This arm SKIPS
  (does not fail) when Brain is unreachable, and the operator runs it manually
  per the human_needed note. Canon Part 9: the tier read is a pure LOCAL read.

Purpose: ACPT-05 closes the "navigation tier rises when intelligence derives"
proof. The hermetic arm makes the gate CI-green honestly; the live arm is the
in-room Part-6 proof the operator runs against a real Brain.

Output: one node-runnable acceptance suite with a clear exit code and an explicit
SKIPPED status for the live arm when Brain is unreachable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/MINDRIAN-CANON.md

# The REAL units under test (drive these, never stub them):
@lib/core/folder-memory.cjs
@lib/core/navigation-engine-shared.cjs
@lib/core/brain-derivation.cjs
@lib/core/brain-client.cjs

# Shared synthetic-room fixture from Plan 01:
@tests/dogfood/fixtures/synthetic-room.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ACPT-05 hermetic mode_b arm (always runs, autonomous)</name>
  <files>tests/test-acpt-05-brain-derive-tier-rise.cjs</files>
  <read_first>
    - lib/core/navigation-engine-shared.cjs resolveTierMode (tier_0 when brain null/parse_failed/staleness unavailable; mode_a when brainAvailable===true + brain non-null + staleness !== unavailable; mode_b when brain unreachable + stale_reason === 'brain_offline'; this is the Section-5 resolver -- the tier the ACPT-05 trace shows)
    - lib/core/folder-memory.cjs readQuadruple (composes readTriple + a brain field parsed from BRAIN.md; brain is null when BRAIN.md absent, a full struct when it parses; LOCAL read, never queries Brain)
    - lib/core/brain-derivation.cjs deriveSection lines ~570-680 (the IDEMPOTENT + LOCAL no-Brain-query derivation path: when Brain is not queried it still writes a schema-valid, FRESH, brain-authored BRAIN.md from the LOCAL triple with staleness:fresh + author:brain + brain_graph_version:0; this fires NO Brain call -- the hermetic source of the tier rise)
  </read_first>
  <behavior>
    - Test A (baseline tier_0): build a synthetic room section with NO BRAIN.md; readQuadruple(sectionPath) -> brain === null; resolveTierMode(quadruple, false) === 'tier_0'. This is the BEFORE state (the tier the gate must rise above).
    - Test B (derive -> tier rises, hermetic): run the LOCAL no-Brain-query deriveSection against that section (the path that writes a fresh brain-authored BRAIN.md without querying Brain). Assert a BRAIN.md now exists. readQuadruple(sectionPath) -> brain non-null with staleness consistent with the LOCAL derivation (fresh + brain_offline-class). resolveTierMode(quadruple, false) === 'mode_b' -- ABOVE tier_0. This is the ACPT-05 proof on the hermetic arm: BRAIN.md derived -> tier_mode rose above tier_0.
    - Test C (Part 8 / Part 9): assert the BRAIN.md the LOCAL derivation wrote contains no user-content sentinel from the fixture triple body (the derivation carries only generic handles); assert the tier read is a pure LOCAL readQuadruple (no Brain client require in the read path -- assert by a comments-filtered no-egress sweep over the produced BRAIN.md + the assertion that the hermetic arm made no Brain call, i.e. brain-client.isAvailable was not required to reach mode_b).
    - Test D (honest negative): a section whose BRAIN.md is written with staleness:unavailable (or parse_failed) -> resolveTierMode stays tier_0. The tier does not rise on a broken/unavailable derivation (not a false-green).
  </behavior>
  <action>
    Create tests/test-acpt-05-brain-derive-tier-rise.cjs as a single node-runnable suite (ok/fail + process.exit shape) implementing Tests A-D for the HERMETIC arm. Reuse makeSyntheticRoom from tests/dogfood/fixtures/synthetic-room.cjs (Plan 01) to build a section directory carrying the LOCAL triple (ROOM.md/STATE.md/MINTO.md) deriveSection reads; all triple content MUST be obviously fictional. Drive the REAL deriveSection LOCAL-derivation path + REAL readQuadruple + REAL resolveTierMode (no stubs of the units under test, per the Part 6 dogfood mandate -- drive the REAL brain-derive, not a mock of it). The hermetic arm must reach mode_b with NO Brain reachable. Clean tmp fixtures in finally.
  </action>
  <verify>
    <automated>node tests/test-acpt-05-brain-derive-tier-rise.cjs</automated>
  </verify>
  <done>node tests/test-acpt-05-brain-derive-tier-rise.cjs exits 0; baseline is tier_0; after the LOCAL no-Brain derivation the BRAIN.md exists and resolveTierMode rises to mode_b (above tier_0) with NO Brain reachable; the unavailable/parse_failed negative stays tier_0; no user content in the produced BRAIN.md; the tier read is a pure LOCAL readQuadruple.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: ACPT-05 live mode_a arm (human-verify, requires reachable Brain)</name>
  <what-built>
    The SAME suite (tests/test-acpt-05-brain-derive-tier-rise.cjs) carries a LIVE
    arm guarded by brain-client.isAvailable(): when Brain is reachable it runs a
    FULL deriveSection (a real Brain-query derivation) against the fictional
    fixture section, then asserts readQuadruple -> brain non-null staleness:fresh
    and resolveTierMode(quadruple, true) === 'mode_a' -- the richest tier rise.
    When Brain is UNREACHABLE the live arm prints SKIPPED (live-Brain arm: Brain
    unreachable; hermetic mode_b arm already proved the tier rise) and the suite
    still exits 0 on the hermetic arm. The suite NEVER fakes mode_a when Brain is
    down -- it skips honestly (no silent fake-green).
  </what-built>
  <how-to-verify>
    1. Ensure Brain is reachable: run `/mos:setup brain` and confirm
       `node -e "console.log(require('./lib/core/brain-client.cjs').isAvailable())"`
       prints true (or set MINDRIAN_BRAIN_KEY per the user_setup block).
    2. Run `node tests/test-acpt-05-brain-derive-tier-rise.cjs` and confirm the
       LIVE arm RAN (not SKIPPED) and asserted resolveTierMode === 'mode_a' after
       a full Brain-query deriveSection.
    3. Confirm the suite exits 0 with both arms green.
    4. Part 8 spot-check: confirm the live deriveSection carried only generic
       handles to Brain (no fixture user-content sentinel in any Brain query
       payload) -- the existing Phase 90 Part-8 tripwires + the suite's no-egress
       sweep over the produced BRAIN.md both hold.
  </how-to-verify>
  <resume-signal>Type "approved" once the live mode_a arm ran green against a reachable Brain, or describe the failure (and keep the hermetic mode_b arm as the CI-green proof).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deriveSection -> Brain | the LIVE arm queries Brain; only generic handles (framework names, phase ids, enums, hashes) may cross (Part 8) |
| BRAIN.md -> tier read | the tier rise is read from a LOCAL BRAIN.md via readQuadruple; no Brain call at read time (Part 9 SQL/file is the local mind) |
| fixture triple -> derivation | the LOCAL triple feeds deriveSection; obviously-fictional content only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-09 | Spoofing (false-green) | ACPT-05 tier-rise driver | mitigate | the hermetic arm reaches mode_b via a REAL LOCAL deriveSection (not a hand-written BRAIN.md); Test D asserts an unavailable/parse_failed derivation stays tier_0; the live arm SKIPS honestly when Brain is down (never fakes mode_a) -- Part 6 prove-it-fires-not-exists |
| T-146-10 | Information Disclosure | live deriveSection Brain query | mitigate | the live arm relies on the shipped Phase 90 5-tripwire Part-8 sweep (deriveSection's buildBrainQueryContext chokepoint carries only generic handles); the suite adds a no-egress sweep over the produced BRAIN.md asserting no fixture user-content sentinel landed in the local artifact |
| T-146-11 | Information Disclosure | BRAIN.md -> tier read | accept | readQuadruple is a pure LOCAL read with no Brain call (Part 9); low risk -- the read path is already covered by the navigation-engine Part-8 invariant suite |
| T-146-12 | Tampering | synthetic fixture triple | mitigate | the fixture ROOM.md/STATE.md/MINTO.md are obviously fictional; no real client data enters the derivation (Part 6 dogfood proxy) |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | this plan adds NO new package installs (node built-ins + existing project libs only); no legitimacy checkpoint required |
</threat_model>

<verification>
- node tests/test-acpt-05-brain-derive-tier-rise.cjs exits 0 with the HERMETIC mode_b arm green (CI-autonomous, no Brain needed)
- The LIVE mode_a arm RUNS green against a reachable Brain (Task 2 human-verify) OR SKIPS honestly when Brain is down (never fakes mode_a)
- grep -c em-dash (U+2014) on the created file returns 0
- The suite drives the REAL deriveSection + readQuadruple + resolveTierMode (no stubs of the units under test)
- The no-egress sweep over the produced BRAIN.md finds zero user-content sentinel
</verification>

<success_criteria>
ACPT-05 has a node-runnable driver whose hermetic mode_b arm proves the tier rises
above tier_0 with NO Brain reachable (CI-green honestly), and whose live mode_a arm
proves the richest tier rise against a reachable Brain (human-verified) or skips
honestly. It composes into the Plan 04 aggregator (the aggregator requires only the
hermetic arm to be green; the live arm is the operator's in-room proof).
</success_criteria>

<output>
Create `.planning/phases/146-loop-fires-acceptance-gate/146-03-SUMMARY.md` when done
</output>
