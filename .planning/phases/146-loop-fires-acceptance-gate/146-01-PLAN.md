---
phase: 146-loop-fires-acceptance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-acpt-01-engine-fires.cjs
  - tests/test-acpt-02-websearch-hat-scoped.cjs
  - tests/dogfood/fixtures/synthetic-room.cjs
autonomous: true
requirements: [ACPT-01, ACPT-02]
canon_parts: [Part 2, Part 3, Part 8]
must_haves:
  truths:
    - "A scripted dogfood turn driven by a REAL fired sensor produces routing_source: engine in a decision trace (not legacy)"
    - "explain-decision renders the fired reach_id + posture for that engine turn"
    - "A turn referencing external facts surfaces a hat-scoped WebSearch reach via the REAL sensorExternalFact + hatScopeFor"
    - "The dogfood's web reach carries no user content (generic topic-handle enum only) per Part 8"
  artifacts:
    - path: "tests/test-acpt-01-engine-fires.cjs"
      provides: "ACPT-01 dogfood driver: real fired sensor -> decide() -> routeActivation -> engine + persisted trace + explain-decision render"
      min_lines: 90
    - path: "tests/test-acpt-02-websearch-hat-scoped.cjs"
      provides: "ACPT-02 dogfood driver: external-fact turn -> sensorExternalFact -> hat-scoped reach + Part-8 no-user-content assertion"
      min_lines: 80
    - path: "tests/dogfood/fixtures/synthetic-room.cjs"
      provides: "shared synthetic (obviously-fictional) fixture-room builder used by every ACPT driver"
      min_lines: 40
  key_links:
    - from: "tests/test-acpt-01-engine-fires.cjs"
      to: "lib/core/navigation-engine.cjs decide() + lib/core/skill-activation-router.cjs routeActivation"
      via: "direct require + the production decide()->routeActivation boundary"
      pattern: "routeActivation"
    - from: "tests/test-acpt-02-websearch-hat-scoped.cjs"
      to: "lib/core/sensors/sensor-external-fact.cjs + lib/core/sensors/hat-scoping-table.cjs"
      via: "direct require of the shipped sensor + hat-scope table"
      pattern: "sensorExternalFact"
---

<objective>
Build the ACPT-01 and ACPT-02 dogfood drivers: the two hermetic, fully-autonomous
acceptance legs that PROVE the loop FIRES (Canon Part 6) by driving the REAL
shipped units, not stubs.

- ACPT-01: a scripted dogfood turn whose REAL fired sensor (a SENS-06 CONTRADICT
  side-channel, the shipped CASC-01 contract) flips routing_source legacy->engine
  through the production decide()->routeActivation boundary, persists a decision
  trace, and renders the fired reach_id + posture via explain-decision. Per D
  (LARRYREACH-CONNECTOR-AUDIT.md Section 5) explain-decision is THE assertion
  surface; navigation-engine.cjs decide() is the routing_source: engine source.
- ACPT-02: a scripted dogfood turn referencing external facts surfaces a
  hat-scoped WebSearch reach via the REAL sensorExternalFact + hatScopeFor
  (Canon Part 2 web hat-scoping), asserting Part 8 (the reach carries only a
  generic topic-handle enum, never the user's turn text or artifact bytes).

Purpose: ACPT-01 and ACPT-02 are the two criteria that can run fully hermetically
(no live Brain, no live web), so they are the autonomous core of the gate. They
compose into the Plan 04 aggregator (doctor --dogfood-acceptance + run-all-146.sh).

Output: two node-runnable acceptance suites with clear exit codes + a shared
synthetic-room fixture builder.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/MINDRIAN-CANON.md

# The REAL units under test (drive these, never stub them):
@lib/core/navigation-engine.cjs
@lib/core/sensors/sensor-external-fact.cjs

# The exact production-boundary pattern ACPT-01 composes (REAL fired sensor, no stub):
@tests/test-nav01-populated-room-engine-fires.cjs

# The assertion surface (ACPT-01 renders fired reach_id + posture from a persisted trace):
@scripts/explain-decision-command.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared synthetic-room fixture + ACPT-01 engine-fires driver</name>
  <files>tests/dogfood/fixtures/synthetic-room.cjs, tests/test-acpt-01-engine-fires.cjs</files>
  <read_first>
    - tests/test-nav01-populated-room-engine-fires.cjs (the EXACT real-fired-sensor -> decide() -> routeActivation pattern to reuse; makePopulatedContradictRoom writes the SENS-06 CASC-01 side-channel at roomDir/.mindrian/last-cascade.json with a CONTRADICT newFinding; decide() returns fire_skill "Devil's Advocate"; routeActivation returns source engine + reason engine_fire_skill_set)
    - lib/core/navigation-engine.cjs (decide() consumes dispatchSensors; the tier_0 path fires sensors too; chosen_rationale names reach_id/posture, Part-8 clean)
    - scripts/explain-decision-command.cjs (renderTrace emits "## Routing\n- source: engine" when t.routing_source set, and the reach_id/posture appear in chosen_rationale; resolveActiveRoomDir + readTraceFile read roomDir/.mindrian/decision-traces/<session>.json with shape { traces: [...] })
    - scripts/intent-classifier.cjs lines ~1337-1400 (how a persisted trace turn carries routing_source: the router routing.source is merged into the trace turn record written to .mindrian/decision-traces/<session>.json)
  </read_first>
  <behavior>
    - synthetic-room.cjs exports makeSyntheticRoom(opts) -> absolute roomDir under os.tmpdir(): an obviously-FICTIONAL fixture (room slug like "acme-fictional-widgets-DOGFOOD"; never real client data per the threat model) with .mindrian/ created; opts.contradict===true writes the SENS-06 CASC-01 side-channel (last-cascade.json with proactive_intelligence.newFindings: [{type:'CONTRADICT', confidence:0.9}]); returns { roomDir, cleanup }.
    - Test A (POSITIVE, REAL sensor, NO stub): build a contradict room; call engine.decide({signals:['artifact_filed'], sectionPath:null}, {quadruple:null, brainAvailable:false, roomDir}); assert decision.fire_skill === "Devil's Advocate" (a canonical verb); feed to router.routeActivation(decision, ['room-passive']); assert routing.source === 'engine' AND routing.reason === 'engine_fire_skill_set'. MOS_NAV_TEST_FIRE_SKILL deleted from env (the proof is the SENSOR, not the stub).
    - Test B (TRACE PERSIST + RENDER): write a decision-traces/<session>.json carrying one turn with routing_source: routing.source, the fired reach_id ('contradiction'), and posture ('pull_back') merged into the turn (mirroring the intent-classifier merge shape: turn.routing_source + chosen_rationale naming the reach). Run scripts/explain-decision-command.cjs against that fixture room (MINDRIAN_ROOMS_HOME pointed at the fixture parent + an active-room registry, OR --session) and assert stdout contains "source: engine" AND "contradiction" AND "pull_back".
    - Test C (NEGATIVE, honest): a cold room (no side-channel, brain absent) -> decide() fire_skill null -> routeActivation source === 'legacy'. The gate is not a false-green: a silent loop reports legacy.
    - Test D (Part 8 provenance): the chosen_rationale names reach_id + posture ONLY; assert it contains NO user-content tokens (the fixture writes a sentinel string into the side-channel finding body and the rationale must NOT contain it).
  </behavior>
  <action>
    Create tests/dogfood/fixtures/synthetic-room.cjs exporting makeSyntheticRoom per the behavior block, modeled on makePopulatedContradictRoom in test-nav01-populated-room-engine-fires.cjs but generalized (opts.contradict, opts.activeRegistry to also write a .rooms/registry.json under the rooms-home so explain-decision's resolveActiveRoomDir resolves the fixture). Room slug and all fixture content MUST be obviously fictional (threat model: no real client data).
    Create tests/test-acpt-01-engine-fires.cjs as a single node-runnable suite (ok/fail counters + process.exit(failed===0?0:1), mirroring test-nav01-populated-room-engine-fires.cjs structure) implementing Tests A-D. Drive the REAL decide() + routeActivation + the REAL scripts/explain-decision-command.cjs (spawn via child_process with the fixture env). Do NOT set MOS_NAV_TEST_FIRE_SKILL anywhere (delete it defensively at the top). Clean tmp fixtures in finally. Per ACPT-01: routing_source must come from the REAL navigation-engine decide() on a fired sensor (audit hard fence), reusing the 144 real-fired-sensor pattern.
  </action>
  <verify>
    <automated>node tests/test-acpt-01-engine-fires.cjs</automated>
  </verify>
  <done>node tests/test-acpt-01-engine-fires.cjs exits 0; all four checks pass; the positive proves engine via a REAL fired sensor (no MOS_NAV_TEST_FIRE_SKILL); explain-decision renders source: engine + the fired reach_id + posture; the negative honestly reports legacy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ACPT-02 hat-scoped WebSearch driver (Part 2 + Part 8)</name>
  <files>tests/test-acpt-02-websearch-hat-scoped.cjs</files>
  <read_first>
    - lib/core/sensors/sensor-external-fact.cjs (sensorExternalFact(turn, _tuple, ctx): turn.text matched against EXTERNAL_FACT_PATTERNS -> generic topic handle enum like 'competitor'/'market'/'state-of-the-art'; turn.active_hat or ctx.active_hat selects the hat; the reach carries the hat-scoped tool set + a tool_choice_gate flag; the reach payload carries the GENERIC handle, never turn.text)
    - lib/core/sensors/hat-scoping-table.cjs (hatScopeFor(hat): White=data/arxiv, Green=patents+arxiv+deep-research, Black=failure-cases, Yellow=success-cases, Red=intuition-only/no web tool, Blue=synthesis)
    - lib/core/insight-sensors.cjs (dispatchSensors runs sensorExternalFact in the registry; SENS-04 is the external-fact row; makeReach freezes evidence to a flat scalar bag)
    - docs/MINDRIAN-CANON.md Part 2 (External Web hat-scoping) + Part 8 (the WebSearch reach carries no user content; SIGNAL->LOCAL yes, SIGNAL->Brain no)
  </read_first>
  <behavior>
    - Test A (REAL sensor fires, hat-scoped): call sensorExternalFact({text:'how does our COMPETITOR position against the market leader?', active_hat:'White'}, {}, {}) and assert it returns a non-null reach whose reach_id is one of the frozen 5 and whose evidence/tool-set reflects hatScopeFor('White') (data/arxiv) per Part 2.
    - Test B (hat scoping varies by hat): Green -> reflects patents+arxiv+deep-research; Red -> intuition-only (NO web tool surfaced). Assert the Red reach surfaces no web tool (Part 2 Red = no external tool).
    - Test C (Part 8 no user content): the turn text carries an obviously-fictional proprietary sentinel string (e.g. 'SECRET-MARGIN-42pct-FICTIONAL'). Assert the returned reach (JSON.stringify of the whole reach object) does NOT contain that sentinel anywhere -- only the generic category handle (e.g. 'competitor'/'market') rides. This proves the Part 8 hard fence: the web reach carries no user content.
    - Test D (MCP-stack-ask gate): when the hat is NOT preconfigured, the reach carries the tool_choice_gate flag (no silent WebSearch dispatch) per the MCP-stack-ask rule + Part 8.
    - Test E (no-match negative): a turn with no external-fact phrasing returns null (the sensor does not over-fire).
  </behavior>
  <action>
    Create tests/test-acpt-02-websearch-hat-scoped.cjs as a single node-runnable suite (same ok/fail + process.exit shape) implementing Tests A-E. Drive the REAL sensorExternalFact + hatScopeFor (require both directly; never reimplement the patterns). The Part 8 assertion (Test C) is the load-bearing one: stringify the entire reach and assert the fictional proprietary sentinel is absent while the generic handle is present. Mark every fixture string obviously fictional (threat model: synthetic data only). This driver proves ACPT-02 hermetically; per the audit it is a scripted assertion over the sensor-external-fact + hatScopeFor path rather than a live web call (prefer hermetic). A live WebSearch leg is NOT required for this criterion and is NOT added here (no autonomous:false needed -- the hat-scoping + Part-8 fence are fully assertable on the LOCAL sensor).
  </action>
  <verify>
    <automated>node tests/test-acpt-02-websearch-hat-scoped.cjs</automated>
  </verify>
  <done>node tests/test-acpt-02-websearch-hat-scoped.cjs exits 0; the REAL sensorExternalFact fires a hat-scoped reach that varies correctly by hat (White/Green/Red); the Part-8 fence proves the reach carries the generic handle only and never the fictional proprietary sentinel; the no-match negative does not over-fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dogfood turn -> WebSearch | external-fact turn text crosses into a (potential) web query; only the generic topic handle may cross |
| dogfood turn -> Brain | none in this plan (ACPT-01/02 are LOCAL-only); decide() + sensors are pure-local |
| fixture room -> disk | tmp synthetic fixtures written under os.tmpdir() and cleaned in finally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Information Disclosure | ACPT-02 web reach (sensor-external-fact) | mitigate | Test C stringifies the entire reach and asserts a fictional proprietary sentinel is ABSENT; only the generic category enum rides (Part 8 SIGNAL->web carries no user content) |
| T-146-02 | Spoofing (false-green) | ACPT-01 engine-fires driver | mitigate | the POSITIVE is driven by a REAL fired sensor (CASC-01 side-channel) with MOS_NAV_TEST_FIRE_SKILL deleted from env; the NEGATIVE asserts a silent loop honestly reports legacy (adversarial negative case -- the Part-6 prove-it-fires-not-exists mandate) |
| T-146-03 | Tampering | synthetic fixture room | mitigate | fixture room slug + content are obviously fictional (acme-fictional-widgets-DOGFOOD); no real client data ever enters a fixture (Part 6 dogfood proxy) |
| T-146-04 | Information Disclosure | ACPT-01 trace render | mitigate | Test D asserts chosen_rationale names reach_id+posture ONLY and contains no user-content sentinel from the side-channel finding body |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | this plan adds NO new package installs (node built-ins + existing project libs only); no legitimacy checkpoint required |
</threat_model>

<verification>
- node tests/test-acpt-01-engine-fires.cjs exits 0 (ACPT-01: REAL fired sensor -> engine + render + honest legacy negative + Part-8 provenance)
- node tests/test-acpt-02-websearch-hat-scoped.cjs exits 0 (ACPT-02: hat-scoped WebSearch reach + Part-8 no-user-content + no over-fire)
- grep -c em-dash (U+2014) across all three created files returns 0
- Both suites delete MOS_NAV_TEST_FIRE_SKILL defensively and drive the REAL shipped units (no stubs of decide()/sensorExternalFact)
</verification>

<success_criteria>
ACPT-01 and ACPT-02 each have a node-runnable hermetic driver that drives the REAL
shipped unit, passes its positive, asserts its Part-8 fence, and (ACPT-01) carries
an adversarial honest-negative. Both compose into the Plan 04 aggregator unchanged.
</success_criteria>

<output>
Create `.planning/phases/146-loop-fires-acceptance-gate/146-01-SUMMARY.md` when done
</output>
