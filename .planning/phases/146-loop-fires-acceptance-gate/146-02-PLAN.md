---
phase: 146-loop-fires-acceptance-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-acpt-03-first-material-explore.cjs
  - tests/test-acpt-04-filing-cascade-surfaces.cjs
autonomous: true
requirements: [ACPT-03, ACPT-04]
canon_parts: [Part 2, Part 3, Part 4, Part 6, Part 8]
must_haves:
  truths:
    - "First material in a dogfood session triggers the REAL first-material detection -> auto-explore composes a finding -> the room is non-empty by turn 2 (domain tree + whitespace map + candidate Opportunity Bank entries)"
    - "Filing an artifact surfaces the cross-relationship cascade findings to Larry mid-session via the shipped last-cascade.json side-channel + room-proactive surfacing contract"
    - "The first-material + cascade paths carry zero Brain/web egress (Part 8 LOCAL-only)"
  artifacts:
    - path: "tests/test-acpt-03-first-material-explore.cjs"
      provides: "ACPT-03 dogfood driver: detectFirstMaterial -> composeAutoExploreFinding -> room non-empty assertion"
      min_lines: 90
    - path: "tests/test-acpt-04-filing-cascade-surfaces.cjs"
      provides: "ACPT-04 dogfood driver: filing -> last-cascade.json cascade findings -> room-proactive surfacing contract assertion"
      min_lines: 80
  key_links:
    - from: "tests/test-acpt-03-first-material-explore.cjs"
      to: "lib/agents/auto-explore-agent.cjs detectFirstMaterial + composeAutoExploreFinding"
      via: "direct require of the shipped first-material + finding-composer units"
      pattern: "composeAutoExploreFinding"
    - from: "tests/test-acpt-04-filing-cascade-surfaces.cjs"
      to: "skills/room-proactive/SKILL.md surfacing contract + roomDir/.mindrian/last-cascade.json"
      via: "the shipped CASC-01 side-channel the post-write cascade writes and room-proactive reads"
      pattern: "last-cascade.json"
---

<objective>
Build the ACPT-03 and ACPT-04 dogfood drivers: the two acceptance legs that prove
the Engine-1 first-material loop and the cascade-surfacing loop FIRE end-to-end
against the REAL shipped units.

- ACPT-03: the first material in the session triggers /mos:explore-domains (the
  shipped auto-explore-fingerprint -> auto-explore-agent path) and the room is
  non-empty by turn 2 -- a composed finding carrying a domain tree + whitespace
  map + candidate Opportunity Bank entries (the Canon Part 2 Engine 1 Act 1
  payload). Drive the REAL detectFirstMaterial + composeAutoExploreFinding; assert
  the room artifact exists post-trigger with non-empty candidate buckets.
- ACPT-04: filing an artifact surfaces the cross-relationship cascade findings to
  Larry mid-session. Drive the REAL post-write CASC-01 side-channel contract
  (roomDir/.mindrian/last-cascade.json) + the room-proactive surfacing contract
  (skills/room-proactive/SKILL.md: when newFindings is non-empty present max 2 for
  APPROVE/REJECT/DEFER). Assert the cascade findings are present and surfaceable.

Purpose: ACPT-03/04 close the "insight enters -> intelligence surfaces" half of
the loop. Both run hermetically by driving the LOCAL composer + side-channel
contract directly (the upstream Python whitespace/rs scripts feed the composer via
on-disk JSON, which the fixture supplies as obviously-fictional inputs -- the
REAL composer + REAL side-channel are exercised, not stubbed).

Output: two node-runnable acceptance suites with clear exit codes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/MINDRIAN-CANON.md

# The REAL units under test (drive these, never stub them):
@lib/agents/auto-explore-agent.cjs
@scripts/auto-explore-fingerprint.cjs
@scripts/auto-explore-fire.cjs
@skills/room-proactive/SKILL.md

# Shared synthetic-room fixture from Plan 01 (use it; do not duplicate):
@tests/dogfood/fixtures/synthetic-room.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ACPT-03 first-material -> auto-explore -> room non-empty driver</name>
  <files>tests/test-acpt-03-first-material-explore.cjs</files>
  <read_first>
    - lib/agents/auto-explore-agent.cjs (detectFirstMaterial(args) -> { material_id } using store.computeMaterialId; composeAutoExploreFinding({material_id, whitespace, rs, analogy}) -> finding object with candidate buckets domain/trends/reverse-salients/cross-domain, each carrying score + BQ template; returns null only when ALL pipelines yield zero candidates)
    - scripts/auto-explore-fire.cjs (the orchestrator: reads roomDir/.mindrian/whitespace-results.json + .rs-engine-results.json + discovery-cycle-results.json, calls composeAutoExploreFinding, atomic-writes roomDir/.mindrian/auto-explore-<material_id>.json; this is the room-non-empty artifact)
    - lib/memory/explored-materials-store.cjs (computeMaterialId(roomDir, relativeFilePath, mtimeMs) -> 32-char hex; appendMaterial JSONL ledger; the first-material detection chokepoint)
    - docs/MINDRIAN-CANON.md Part 2 Engine 1 (the Act 1 payload = domain tree + whitespace map + candidate Opportunity Bank ADDs, all HSI-scored)
  </read_first>
  <behavior>
    - Test A (first-material detection fires): build a synthetic room (makeSyntheticRoom from Plan 01); call detectFirstMaterial with a fictional first artifact path; assert a non-null material_id (32-char hex) -- the first material is detected (the SENS-01 firing point).
    - Test B (room non-empty by turn 2): seed the room with obviously-fictional whitespace-results.json ({gaps:[...]}) + discovery-cycle-results.json ({analogy_whitespace:{zones:[...]}}); call composeAutoExploreFinding({material_id, whitespace, analogy}); assert the returned finding is non-null AND carries non-empty candidate buckets across >=2 of {domain (whitespace/domain-tree), cross-domain (candidate Opportunity Bank ADDs)}; assert the domain bucket reflects the whitespace gaps (the whitespace map) and the cross-domain bucket reflects the analogy zones (the candidate Opportunity Bank entries). This is the ACPT-03 "domain tree + whitespace map + candidate Opportunity Bank entries" non-empty assertion.
    - Test C (artifact lands in the room): run scripts/auto-explore-fire.cjs against the seeded room (spawn via child_process with roomDir + a fictional file_path + material_id) and assert roomDir/.mindrian/auto-explore-<material_id>.json exists and parses to a finding with non-empty candidates -- the room is non-empty by turn 2.
    - Test D (Part 8 LOCAL-only): a comments-filtered forbidden-substring sweep over the finding artifact JSON asserts no Brain/web egress tokens leaked into the room artifact; the composer + fire path made no network call (assert by absence + by the no-egress sweep over the produced artifact).
    - Test E (honest negative): a room with empty whitespace + empty analogy -> composeAutoExploreFinding returns null (the loop does not fabricate a finding from nothing -- not a false-green).
  </behavior>
  <action>
    Create tests/test-acpt-03-first-material-explore.cjs as a single node-runnable suite (ok/fail + process.exit shape) implementing Tests A-E. Reuse makeSyntheticRoom from tests/dogfood/fixtures/synthetic-room.cjs (Plan 01). Drive the REAL detectFirstMaterial + composeAutoExploreFinding + scripts/auto-explore-fire.cjs (no stubs of the units under test, per the Part 6 dogfood mandate -- drive the REAL auto-explore, not a mock). All seeded JSON (whitespace gaps, analogy zones, file paths) MUST be obviously fictional. Clean tmp fixtures in finally.
  </action>
  <verify>
    <automated>node tests/test-acpt-03-first-material-explore.cjs</automated>
  </verify>
  <done>node tests/test-acpt-03-first-material-explore.cjs exits 0; first material is detected (real material_id); composeAutoExploreFinding yields a non-empty finding (domain tree + whitespace map + candidate Opportunity Bank entries); auto-explore-fire writes the finding artifact into the room (room non-empty by turn 2); the empty-input negative returns null; no Brain/web egress in the produced artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ACPT-04 filing -> cascade findings surface driver</name>
  <files>tests/test-acpt-04-filing-cascade-surfaces.cjs</files>
  <read_first>
    - scripts/post-write (write_cascade_side_channel writes the full cascade payload atomically to roomDir/.mindrian/last-cascade.json with proactive_intelligence.newFindings; emits the additionalContext one-liner "post-write: cascade complete")
    - skills/room-proactive/SKILL.md lines ~30, ~87-160 (the surfacing CONTRACT: on the "post-write: cascade complete" trigger, read roomDir/.mindrian/last-cascade.json; if proactive_intelligence.newFindings has 1+ items with confidence >= 0.60, present max 2 highest-confidence for APPROVE/REJECT/DEFER via Decision Capture; LOCAL-only per Part 8; this is the Part 4 every-choice-is-graph-data surface)
    - tests/test-nav01-populated-room-engine-fires.cjs (the last-cascade.json side-channel shape: { proactive_intelligence: { newFindings: [{ type, confidence }] } })
    - tests/dogfood/fixtures/synthetic-room.cjs (makeSyntheticRoom opts.contradict already writes a last-cascade.json; extend the fixture-builder usage for a multi-finding cascade if needed)
  </read_first>
  <behavior>
    - Test A (cascade side-channel written + readable): build a synthetic room and write a last-cascade.json carrying proactive_intelligence.newFindings with 2+ obviously-fictional findings (e.g. a CONTRADICT + an INFORMS, each confidence >= 0.60). Assert the file parses and newFindings is non-empty -- the cross-relationship cascade findings are present after filing.
    - Test B (surfacing-contract assertion): assert the room-proactive surfacing contract holds against this side-channel: filter newFindings to confidence >= 0.60, sort by confidence desc, take max 2 -- assert exactly the 2 highest-confidence fictional findings would be surfaced (the "surfaces the cross-relationship cascade findings to Larry mid-session" assertion). Drive the SAME filter+sort+cap logic the SKILL.md contract documents (encode it from the contract; assert it against the fixture). This proves ACPT-04: filing -> cascade findings surface mid-session.
    - Test C (Part 8 LOCAL-only): the side-channel read is LOCAL; a comments-filtered forbidden-substring sweep over the cascade payload + the test asserts no Brain/web egress token rides in the cascade findings (the cascade is graph-local per Part 8).
    - Test D (Part 4 graph-data): assert each surfaced finding carries the fields the APPROVE/REJECT/DEFER Decision Capture needs (type + confidence) so the decision becomes a typed edge (Canon Part 4); a finding missing those fields is NOT surfaceable.
    - Test E (honest negative): a cascade with empty newFindings (the cascade ran, found nothing) surfaces zero findings -- the loop does not fabricate a finding (not a false-green).
  </behavior>
  <action>
    Create tests/test-acpt-04-filing-cascade-surfaces.cjs as a single node-runnable suite (ok/fail + process.exit shape) implementing Tests A-E. Drive the REAL shipped contract: the last-cascade.json side-channel the REAL scripts/post-write writes and the REAL skills/room-proactive/SKILL.md surfacing rule reads. Encode the surfacing rule (confidence >= 0.60, sort desc, max 2) directly from SKILL.md and assert it against the fixture -- this is the faithful proxy for the conversational surfacing step that Larry performs from the skill (a hermetic CI-safe proxy for the Part 6 in-room proof). All findings MUST be obviously fictional. Clean tmp fixtures in finally.
  </action>
  <verify>
    <automated>node tests/test-acpt-04-filing-cascade-surfaces.cjs</automated>
  </verify>
  <done>node tests/test-acpt-04-filing-cascade-surfaces.cjs exits 0; the cascade findings are present after filing; the room-proactive surfacing contract surfaces exactly the 2 highest-confidence findings (cross-relationship cascade surfaced mid-session); each surfaced finding carries APPROVE/REJECT/DEFER-ready fields (Part 4); the empty-cascade negative surfaces nothing; no Brain/web egress in the cascade payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| first material -> auto-explore | a filed artifact triggers the Engine-1 composer; only LOCAL room JSON feeds it |
| filed artifact -> cascade side-channel | the cascade payload crosses into roomDir/.mindrian/last-cascade.json (LOCAL) |
| cascade findings -> Larry | the room-proactive skill surfaces findings; LOCAL-only per Part 8 |
| fixture room -> disk | tmp synthetic fixtures under os.tmpdir(), cleaned in finally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-05 | Spoofing (false-green) | ACPT-03 explore driver | mitigate | Test E asserts empty whitespace + empty analogy -> composeAutoExploreFinding returns null (the loop never fabricates a finding from nothing -- Part 6 prove-it-fires-not-exists) |
| T-146-06 | Spoofing (false-green) | ACPT-04 cascade driver | mitigate | Test E asserts an empty-newFindings cascade surfaces zero findings; the surfacing contract is encoded from SKILL.md and asserted, not assumed |
| T-146-07 | Information Disclosure | auto-explore finding artifact + cascade payload | mitigate | Test D (03) + Test C (04) run a comments-filtered no-egress sweep over the produced room artifact + cascade payload; the composer + side-channel are LOCAL-only (Part 8 cascade is graph-local) |
| T-146-08 | Tampering | synthetic fixture room + seeded JSON | mitigate | all whitespace gaps, analogy zones, cascade findings, and file paths are obviously fictional; no real client data enters a fixture (Part 6 dogfood proxy) |
| T-146-SC | Tampering | npm/pip/cargo installs | accept | this plan adds NO new package installs (node built-ins + existing project libs only); no legitimacy checkpoint required |
</threat_model>

<verification>
- node tests/test-acpt-03-first-material-explore.cjs exits 0 (ACPT-03: first material detected + room non-empty by turn 2 + honest empty-input negative + no egress)
- node tests/test-acpt-04-filing-cascade-surfaces.cjs exits 0 (ACPT-04: cascade findings present + surfaced mid-session + Part-4 fields + honest empty negative + no egress)
- grep -c em-dash (U+2014) across both created files returns 0
- Both suites drive the REAL shipped units (detectFirstMaterial/composeAutoExploreFinding/auto-explore-fire + last-cascade.json/room-proactive contract); no stubs of the units under test
</verification>

<success_criteria>
ACPT-03 and ACPT-04 each have a node-runnable driver that drives the REAL shipped
unit, proves the loop fires (room non-empty by turn 2; cascade findings surfaced
mid-session), asserts its Part-8 fence, and carries an adversarial honest-negative.
Both compose into the Plan 04 aggregator unchanged.
</success_criteria>

<output>
Create `.planning/phases/146-loop-fires-acceptance-gate/146-02-SUMMARY.md` when done
</output>
