---
phase: 144.1-connector-retrofit-sweep
plan: 05
subsystem: connector-spine
tags: [connector, agents, reach-spine, larryreach, RETRO-07, frozen-vocabulary, WFL-01]

# Dependency graph
requires:
  - phase: 144.1-04
    provides: "generator agents/-walk (build-connector-registry.cjs lists agents/*.md as kind=agent, surface prefix agent:<base>)"
  - phase: 143.3
    provides: "connector: frontmatter contract + generator + --check tripwire + connector-registry.json"
provides:
  - "7 spine-eligible agents carry connector: blocks discovered by the Plan-04 agents walk (agent: surfaces in the registry)"
  - "connector-registry.json grown 46 -> 53 (7 source: agent connectors)"
  - "explicit out-of-spine classification for framework-runner + larry-extended (Phase-144-ownership reasons) for the Plan-08 allow-list"
affects: [144.1-06, 144.1-07, 144.1-08, 146]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Agent connector block: identical # --- Phase 144.1 connector frontmatter --- delimited additive YAML on an agent .md, parsed by the same parseConnectorFrontmatter"
    - "Null-framework agent connector = framework: null + filing: none + NO surface (the only WFL-01-legal null-framework shape: firesCommand fires whenever filing != none OR surface set)"
    - "Distinct sub_mode per agent disambiguates agent connectors from their command siblings (grading-agent vs grade-compass) so every (sensor,reach,sub_mode) tuple stays unique"

key-files:
  created: []
  modified:
    - agents/grading.md
    - agents/investor.md
    - agents/opportunity-scanner.md
    - agents/research.md
    - agents/persona-analyst.md
    - agents/brain-query.md
    - agents/reverse-salient-agent.md
    - data/connector-registry.json
    - .planning/phases/144.1-connector-retrofit-sweep/deferred-items.md

key-decisions:
  - "WIRE 7 proactive reach-surfacing agents; OUT-OF-SPINE framework-runner + larry-extended (execution/routing surfaces owned by Phase 144)"
  - "investor + opportunity-scanner declared framework: null + filing: none + no surface (WFL-01-legal); the audit's fileEvidence intent is a noted deviation"
  - "hierarchy_rank continues the phase-wide distinct integer sequence at 46-52 (max command rank was 45)"

patterns-established:
  - "Pattern 1: agent connectors join the spine by declaration only -- zero generator change needed (Plan 04 already added the walk)"
  - "Pattern 2: a null-framework reach (contradiction/brain_consult/deep_research with no methodology) is legal when it fires nothing on APPROVE (filing: none + no surface)"

requirements-completed: [RETRO-07]

# Metrics
duration: 22min
completed: 2026-06-07
---

# Phase 144.1 Plan 05: Agent Connector Frontmatter (RETRO-07b) Summary

**The 7 proactive reach-surfacing agents now declare connector: frontmatter (grading, investor, opportunity-scanner, research, persona-analyst, brain-query, reverse-salient-agent); the 2 pure execution/routing agents stay explicitly out-of-spine; the registry grows 46 -> 53 with the agent connectors discovered by the Plan-04 walk and --check stays green.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-06-07
- **Completed:** 2026-06-07
- **Tasks:** 2 of 2
- **Files modified:** 9 (7 agents + registry + deferred-items)

## Accomplishments

### Task 1: Audit-classify all 9 agents; record the 2 out-of-spine reasons

Partitioned the 9 agents in `agents/` into WIRE (7) and OUT-OF-SPINE (2):

**WIRE (7) -- proactive reach-surfacing agents:**

| Agent | reach_id | sensors | framework | posture | filing | sub_mode | surface | rank |
|-------|----------|---------|-----------|---------|--------|----------|---------|------|
| grading | brain_consult | SENS-07 | PWS Triple Validation Compass | hold | fileEvidenceWithReadback | grading-agent | F.0 | 46 |
| investor | contradiction | SENS-07 | null | pull_back | none | investor-agent | (none) | 47 |
| opportunity-scanner | deep_research | SENS-04, SENS-06 | null | push_forward | none | opportunity-scan | (none) | 48 |
| research | deep_research | SENS-04, SENS-06 | Hypothesis-Driven Problem Solving | push_forward | fileEvidenceWithReadback | research-agent | F.1 | 49 |
| persona-analyst | context_block | SENS-06 | Six Thinking Hats | hold | memory_event_only | persona-hats | (none) | 50 |
| brain-query | brain_consult | SENS-03 | null | hold | none | brain-query | (none) | 51 |
| reverse-salient-agent | context_block | SENS-02 | Reverse Salient Analysis | pull_back | memory_event_only | rs-agent-finding | F.0 | 52 |

**OUT-OF-SPINE (2) -- for the Plan-08 allow-list:**

- **framework-runner:** "pure execution/filing surface - owns no reach; its bare-Write-> fileEvidenceWithReadback fix is Phase 144, not a connector."
- **larry-extended:** "loop HEAD / routing surface - reads decide() and emits context_block via the engine; its turn-1 connector wiring is owned by Phase 144 (file-ownership fence). Declares no connector in 144.1."

Both out-of-spine reasons cite Phase 144 ownership (file-ownership fence / engine repoint), not scope reduction.

### Task 2: Agent connector blocks (the 7 spine-eligible) with unique tuples

Added one delimited `# --- Phase 144.1 connector frontmatter ---` block to each of the 7 agents, transcribing the audit values with a unique sub_mode per agent. The three null-framework agents (investor, opportunity-scanner, brain-query) use the only WFL-01-legal null-framework shape: `framework: null` + `filing: none` + no `surface:` key. Regenerated `data/connector-registry.json` (46 -> 53). The 7 agent connectors appear as `source: agent` with `agent:<base>` surface ids.

## Verification

- `node scripts/build-connector-registry.cjs --check` -> `connector-registry: OK`, exit 0 (53 connectors).
- Task 1 verify: `test -f agents/framework-runner.md && test -f agents/larry-extended.md` -> OK.
- Task 2 verify: all 7 agents carry exactly one `connects_to_spine: true`; framework-runner + larry-extended carry ZERO -> "OK 7 wired, 2 out-of-spine".
- Frozen vocabulary: all 7 agent reach_ids are in the frozen 5; all 7 postures in the frozen 3.
- Tuple uniqueness: 62 distinct (sensor,reach,sub_mode) tuples across commands + agents + existing; zero duplicates.
- Em-dash scan across the 7 agent files: 0.
- All 4 frameworks used (PWS Triple Validation Compass, Hypothesis-Driven Problem Solving, Six Thinking Hats, Reverse Salient Analysis) confirmed present in `data/framework-names.json` (112 names).

## Deviations from Plan

### Noted deviations (documented, not auto-fixed)

**1. [Audit intent vs WFL-01] investor + opportunity-scanner filing**
- **Issue:** The LARRYREACH audit rows (items 19, 26) list `fileEvidenceWithReadback` for investor and opportunity-scanner. But both declare `framework: null` (no `frameworks:` block on the agent), and WFL-01 makes the only legal null-framework config `filing: none` + no surface (firesCommand fires whenever filing != none OR a surface is set, and a null framework cannot resolve via commandsForFramework).
- **Resolution:** Followed the PLAN's explicit instruction (Task 2 action) -- `filing: none` + no surface for both. The audit's fileEvidence intent is recorded here as the sanctioned deviation. The plan itself flagged this as "a noted SUMMARY deviation".

## Deferred Issues

**DI-144.1-A (updated):** `tests/test-connector-part8-boundary.cjs` CHECK 2 asserts every connector `framework` is a generic name in `framework-names.json` and does NOT exempt `framework: null`. It fails first on `/mos:funding` (a prior-plan command connector, PRE-EXISTING on HEAD). Plan 05 added 3 more legitimately-null-framework connectors (agent:brain-query, agent:investor, agent:opportunity-scanner) that share the SAME test-side gap. The SHIPPED generator `--check` (the canonical gate per the plan fences) is GREEN at 53 connectors; CHECK 1/3/4 of the part8 boundary test PASS. The fix is test-side (exempt `framework: null` from CHECK 2) and remains OUT OF SCOPE for the agent-frontmatter plan -- the null-framework shape is contract-legal (CONNECTOR-CONTRACT additive-degrade rule) and WFL-01-legal. Logged in `deferred-items.md`.

## Threat Surface Scan

No new security-relevant surface. The connector blocks carry only the 11 schema keys (frozen enums + a published framework name or null + a render-label sub_mode + an integer rank). No free-text body field, no user content. T-144.1-13 (tuple collision) mitigated by distinct sub_mode per agent + the --check tuple validation. T-144.1-14 (null-framework firing WFL-01) mitigated by filing: none + no surface on all three null-framework agents. T-144.1-15 (free-text leak) mitigated by the 11-key schema (no off-schema field added).

## Self-Check: PASSED
