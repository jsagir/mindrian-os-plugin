---
phase: 144.1-connector-retrofit-sweep
plan: 04
subsystem: infra
tags: [connector-spine, registry-generator, agents-walk, cjs, larry-reaches]

# Dependency graph
requires:
  - phase: 143.3-connector-spine-and-intelligence-orchestrator
    provides: scripts/build-connector-registry.cjs (generator + nested connector parser + --check tripwire)
  - phase: 144-navigation-engine-legacy-engine-flip
    provides: the engine-side consumer that reads the same connector spine
provides:
  - scripts/build-connector-registry.cjs source-discovery extended to walk agents/*.md (kind agent)
  - three-way surfaceName branch (command -> /mos:, skill -> skill:, agent -> agent:)
  - tests/test-connector-agents-walk.cjs proving the agents walk + agent: surface-prefix contract
affects: [144.1-05, 144.1-06, 146]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Third source-dir leg in listSourceFiles() byte-mirrors the commands leg (readdirSync + .md filter + sort + try/catch yields [])"
    - "Deterministic source order: commands, then skills, then agents -> byte-stable registry"

key-files:
  created:
    - tests/test-connector-agents-walk.cjs
  modified:
    - scripts/build-connector-registry.cjs

key-decisions:
  - "parseConnectorFrontmatter left byte-unchanged: an agent connector: block is identical in shape to a command's, so the nested parser handles it as-is"
  - "Agents walk is purely local (no Brain touch); the only Brain touch stays the build-time --refresh-names read"
  - "Registry intentionally stays at 46: no agent declares a connector yet (Plan 05 adds them); the walk finds zero agent connectors and that is correct"

patterns-established:
  - "Pattern: a new spine-eligible source kind joins by adding a sorted leg in listSourceFiles() + a surfaceName prefix branch; the parser and filter are unchanged"

requirements-completed: [RETRO-07]

# Metrics
duration: ~12min
completed: 2026-06-07
---

# Phase 144.1 Plan 04: RETRO-07a Generator Walks agents/ Summary

**The connector generator now walks a third source dir (agents/*.md, kind agent, surface "agent:<base>") byte-mirroring the commands leg, with a dedicated walk test; the nested connector parser and the one-connector-per-file contract are byte-unchanged and the registry intentionally stays at 46 (zero agent connectors declared yet).**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-06-07T19:56:00Z (approx)
- **Completed:** 2026-06-07T20:08:21Z
- **Tasks:** 2
- **Files modified:** 2 (1 modified, 1 created)

## Accomplishments
- `scripts/build-connector-registry.cjs` source-discovery extended: `AGENTS_DIR` constant + a third leg in `listSourceFiles()` that walks `agents/*.md`, sorted, byte-mirroring the commands leg (including the try/catch that yields `[]` when the dir is absent). Order stays commands -> skills -> agents.
- The `buildRegistry()` `surfaceName` branch is now three-way: command -> `/mos:` + base, skill -> `skill:` + base, agent -> `agent:` + base.
- `parseConnectorFrontmatter` is byte-unchanged: an agent `connector:` block is structurally identical to a command's, so the existing nested-map parser handles an agent file with no edits.
- `tests/test-connector-agents-walk.cjs` (137 lines) created, mirroring the part8-boundary test idiom (ok/fail counters, per-check PASS/FAIL, exit non-zero on failure): CHECK 1 (module exposes buildRegistry + parseConnectorFrontmatter; agents/ has >=1 .md), CHECK 2 (the SAME parser parses an agent-shaped connector block, connects_to_spine coerces to true), CHECK 3 (every source===agent connector carries an "agent:" surface prefix; vacuously true until Plan 05, documented).

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend listSourceFiles() + surface-id mapping to walk agents/*.md** - `244b6fb3` (feat)
2. **Task 2: Walk test proving agents are discoverable** - `18b6e995` (test)

**Plan metadata:** (this commit) (docs: complete plan)

## Files Created/Modified
- `scripts/build-connector-registry.cjs` - Added `AGENTS_DIR`; third sorted agents leg in `listSourceFiles()`; three-way `surfaceName` branch with the `agent:` prefix. Parser, filter, validations, indexes, and `--refresh-names` all untouched.
- `tests/test-connector-agents-walk.cjs` - New 3-check walk test locking agent discovery + parser-handles-agent + agent: surface-prefix contract; pure (no Brain, no network).

## Decisions Made
- Left `parseConnectorFrontmatter` byte-unchanged. The agent `connector:` block has the identical nested shape; touching the parser would risk the one-connector-per-file contract for no benefit.
- Kept the source order deterministic (commands, then skills, then agents) so the on-disk registry is byte-stable across machines.
- Did NOT register the new test in a runner: `tests/run-all-1441.sh` does not exist yet (Plan 06 creates it and will register this suite). Out of scope for this plan.

## Deviations from Plan

None - plan executed exactly as written. Both Task 1 and Task 2 acceptance criteria passed on the first verification run.

## Issues Encountered

**Pre-existing, out-of-scope test failure surfaced (NOT fixed, logged):**
`node tests/test-connector-part8-boundary.cjs` CHECK 2 ("registry generic-handles-only") fails because the `/mos:funding` connector (source: command, added by a prior plan) carries `framework: null`, which is not in `data/framework-names.json`. This is NOT caused by Plan 04: the agents walk added zero connectors (registry stays 46), and `/mos:funding` was already in the committed registry before this plan. Plan 04's verification contract names only CHECK 1 (brain-client locality), which PASSES. The failure is logged to `.planning/phases/144.1-connector-retrofit-sweep/deferred-items.md` (DI-144.1-A) for the owning command-connector plan to resolve. Per the scope boundary, only issues directly caused by the current task's changes are auto-fixed.

## Verification Results
- `grep -c 'AGENTS_DIR' scripts/build-connector-registry.cjs` = 3 (>= 2): PASS
- `buildRegistry()` runs without throwing: PASS
- `grep -c "'agent:'" ...` = 1 (>= 1): PASS
- `parseConnectorFrontmatter` body byte-unchanged (only the listSourceFiles header comment mentions it): PASS
- `node scripts/build-connector-registry.cjs --check` exits 0: PASS
- registry stays at 46 connectors (in-memory and on-disk): PASS
- `node tests/test-connector-agents-walk.cjs` exits 0 (3/3 checks): PASS
- `grep -c '—' tests/test-connector-agents-walk.cjs` = 0 (zero em-dashes): PASS
- `tests/test-connector-part8-boundary.cjs` CHECK 1 (brain-client locality) still PASSES
- zero em-dashes across both changed files: PASS

## Canon Compliance
- **Part 7 (Reuse Before Build):** the agents leg byte-mirrors the existing commands leg; no new dependency, no new parser. A new source kind joins by adding a sorted leg + a prefix branch.
- **Part 8 (Graph Boundary):** the agents walk is purely local (a filesystem readdir). No top-level brain-client require added; the only Brain touch stays the build-time `--refresh-names` read. part8-boundary CHECK 1 confirms locality.
- **One-connector-per-file contract:** preserved. Each agent file yields at most one connector record, exactly like a command (the unchanged parser flushes one `out.connector`).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The generator is now the structural prerequisite for Plan 05 (agent connectors): an agent that declares a `connect-to-spine` `connector:` block will now be discovered and registered with an `agent:<base>` surface.
- Plan 06 should register `tests/test-connector-agents-walk.cjs` in `tests/run-all-1441.sh` when it creates that runner.
- One deferred item (DI-144.1-A: `/mos:funding framework: null` vs part8 CHECK 2) awaits the owning command-connector plan.

## Self-Check: PASSED

- FOUND: scripts/build-connector-registry.cjs (modified, AGENTS_DIR present)
- FOUND: tests/test-connector-agents-walk.cjs (created)
- FOUND: commit 244b6fb3 (Task 1)
- FOUND: commit 18b6e995 (Task 2)

---
*Phase: 144.1-connector-retrofit-sweep*
*Completed: 2026-06-07*
