---
phase: 144.1-connector-retrofit-sweep
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - scripts/auto-explore-drain.cjs
  - scripts/check-pending-breakthrough.cjs
  - scripts/jtbd-update.cjs
  - scripts/on-agent-complete
  - scripts/on-file-changed
  - scripts/memory-completion-detector.cjs
  - scripts/mva-detect.cjs
  - scripts/brain-derivation-drain.cjs
  - tests/test-connector-tier-d-hooks.cjs
autonomous: true
requirements: [RETRO-03]
canon_parts: [Part 4, Part 8, Part 9]
must_haves:
  truths:
    - "Each Tier D hook NOT already fixed in Phase 144 fires its sensor through the navigation chokepoint (not an ad-hoc detached spawn)"
    - "jtbd-update routes to sensor-jtbd-reweight.cjs (SENS-05)"
    - "on-file-changed inherits the post-write fix (cascade through file-evidence-readback + artifact_filed memory_event)"
    - "Tier D hooks emit a memory_event the engine can observe; no bare Write that bypasses cascade"
  artifacts:
    - path: "tests/test-connector-tier-d-hooks.cjs"
      provides: "asserts each Tier D hook routes through the navigation chokepoint / fires its sensor"
      min_lines: 40
  key_links:
    - from: "scripts/jtbd-update.cjs"
      to: "lib/core/sensors/sensor-jtbd-reweight.cjs"
      via: "require + invoke"
      pattern: "sensor-jtbd-reweight"
---

<objective>
Wire the Tier D sensor-firing hooks (audit Section 3 items 61-68) that were NOT already
fixed in Phase 144 so they fire their sensors through the navigation chokepoint and emit a
memory_event the engine can observe. These hooks are .cjs/script files, NOT markdown, so
they do NOT carry registry connector: blocks (the generator walks .md only); they
participate by FIRING the sensors that the already-registered command/skill/agent
connectors consume.

Purpose: a connector declares a sensor_triggers list, but a sensor only matters if a hook
actually FIRES it. Phase 144 wired the 4 critical blocking hooks (auto-explore-fingerprint,
post-write, jtbd-update->sensor-jtbd-reweight per the engine repoint, check-pending-
breakthrough). This plan wires the REMAINING Tier D hooks and the ones that inherit the
post-write fix, closing the firing side of the spine.

Output: 8 hook scripts routing through the navigation chokepoint + a Tier D firing test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/CONNECTOR-CONTRACT.md
@lib/core/sensors/sensor-types.cjs
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- 8 Tier D hooks routed through the navigation chokepoint, each emitting a memory_event.
- jtbd-update wired to sensor-jtbd-reweight.cjs (SENS-05).
- on-file-changed inheriting the post-write cascade + artifact_filed memory_event.
- tests/test-connector-tier-d-hooks.cjs asserting the firing wiring.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the Phase-144 fix boundary; wire the post-write-inheriting hooks</name>
  <read_first>
    - LARRYREACH-CONNECTOR-AUDIT.md Section 3 Tier D table (items 59-68) + Section 1 (which hooks Phase 144 already fixed) + Section 7 (the retrofit-sweep inherits post-write's fix)
    - scripts/post-write (the SENS-06 cascade + artifact_filed memory_event the inheritors mirror) - read its fire-sensor + memory_event idiom
    - scripts/on-agent-complete, scripts/on-file-changed (the SubagentStop + file-change hooks that inherit the post-write cascade)
    - lib/core/sensors/sensor-types.cjs (REACH_IDS / makeReach) + lib/core/navigation.cjs (the chokepoint the firing routes through)
  </read_first>
  <action>
    First confirm, from the audit Section 1 + Phase 144 SUMMARYs if present, which Tier D
    hooks Phase 144 already fixed (auto-explore-fingerprint, post-write, the jtbd-update
    engine repoint, check-pending-breakthrough). Do NOT re-edit a hook Phase 144 already
    fixed; if check-pending-breakthrough / jtbd-update were fully handled in 144, narrow this
    plan to the genuinely-remaining hooks and record the narrowing in the SUMMARY.
    For the post-write-inheriting hooks (on-agent-complete = SubagentStop, on-file-changed):
    route their cascade through file-evidence-readback and emit an artifact_filed memory_event,
    mirroring the post-write idiom EXACTLY (the audit item 65 says on-file-changed "inherits
    the post-write fix"). on-agent-complete fires SENS-06 (+SENS-04 for the research agent).
    No bare Write that bypasses cascade. Route every fire through the navigation chokepoint
    (lib/core/navigation.cjs), never an ad-hoc detached spawn. No em-dashes. Keep these hooks
    purely LOCAL (Part 8): the firing carries only generic handles + LOCAL scalars, never
    user content to the Brain.
  </action>
  <acceptance_criteria>
    - on-file-changed routes its cascade through file-evidence-readback and emits artifact_filed (grep for the readback + memory_event idiom)
    - on-agent-complete fires SENS-06 through the navigation chokepoint
    - No new bare Write that bypasses cascade introduced in these hooks
    - The SUMMARY records which hooks were already-fixed-in-144 and therefore untouched
    - grep -rc '—' across edited hooks returns 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'navigation' scripts/on-file-changed && grep -q 'navigation' scripts/on-agent-complete && echo OK</automated>
  </verify>
  <done>The post-write-inheriting hooks fire their sensors through the chokepoint and emit memory_event; no bare Write.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the remaining Tier D drain/detect hooks + the firing test</name>
  <read_first>
    - LARRYREACH-CONNECTOR-AUDIT.md Tier D rows: auto-explore-drain (61), jtbd-update->sensor-jtbd-reweight (63), memory-completion-detector (66), mva-detect (67), brain-derivation-drain (68)
    - scripts/auto-explore-drain.cjs, scripts/memory-completion-detector.cjs, scripts/mva-detect.cjs, scripts/brain-derivation-drain.cjs, scripts/jtbd-update.cjs
    - lib/core/sensors/sensor-jtbd-reweight.cjs (the SENS-05 reweight entry jtbd-update routes to)
    - tests/test-connector-part8-boundary.cjs (the CJS test idiom)
  </read_first>
  <action>
    Wire each remaining Tier D hook to fire its sensor through the navigation chokepoint and
    emit a memory_event:
    - auto-explore-drain.cjs: SENS-01 (deep_research, auto-explore-domains, push_forward) - mirror the fingerprint firing pattern Phase 144 established
    - jtbd-update.cjs: route to sensor-jtbd-reweight.cjs (SENS-05) if Phase 144 did not already; if 144 did, leave it and note in SUMMARY
    - memory-completion-detector.cjs: SENS-06 (jtbd-inference is a sub_mode label, not a framework - fire SENS-06, pull_back, memory_event_only)
    - mva-detect.cjs: SENS-05 (mva-brief sub_mode, push_forward, memory_event_only)
    - brain-derivation-drain.cjs: SENS-03 (brain_consult, brain-derivation sub_mode, hold, memory_event_only)
    All firing routes through lib/core/navigation.cjs; all carry only generic handles + LOCAL
    scalars (Part 8). Then create tests/test-connector-tier-d-hooks.cjs (mirror the part8
    test idiom) asserting: each edited hook requires/invokes the navigation chokepoint (grep
    the source for the chokepoint require) and jtbd-update references sensor-jtbd-reweight.
    The test is a source-presence assertion (these hooks run in live sessions; a source-level
    wiring assertion is the Nyquist-satisfying automated check). No em-dashes.
  </action>
  <acceptance_criteria>
    - Each remaining Tier D hook requires/invokes lib/core/navigation.cjs (the chokepoint)
    - jtbd-update.cjs references sensor-jtbd-reweight (or is documented as already-144-fixed)
    - node tests/test-connector-tier-d-hooks.cjs exits 0
    - No firing path sends user content to the Brain (Part 8); fires carry generic handles + scalars only
    - grep -rc '—' across edited hooks + the test returns 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-connector-tier-d-hooks.cjs && grep -q 'sensor-jtbd-reweight' scripts/jtbd-update.cjs && echo OK</automated>
  </verify>
  <done>The remaining Tier D hooks fire their sensors through the chokepoint; jtbd-update routes to the reweight; the firing test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook -> sensor -> navigation chokepoint | the firing carries generic handles + LOCAL scalars only |
| hook -> Brain | NONE permitted; the firing is purely LOCAL (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-16 | Information disclosure | a hook firing carrying user content to the Brain | mitigate | fires carry only generic handles + LOCAL scalars; route through navigation.cjs (the Part-9 local chokepoint); the Tier D test + grep sweep assert no Brain egress |
| T-144.1-17 | Tampering | a hook using a bare Write that bypasses cascade | mitigate | route cascade through file-evidence-readback; acceptance asserts no new bare Write |
| T-144.1-18 | Denial of service | ad-hoc detached spawn bypassing the sensor layer | mitigate | fire through the navigation chokepoint, not a detached spawn (audit item 59 pattern) |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | no package installs; pure Node built-ins |
</threat_model>

<verification>
- node tests/test-connector-tier-d-hooks.cjs exits 0.
- Each Tier D hook routes through lib/core/navigation.cjs; jtbd-update references the reweight.
- No bare Write bypassing cascade; no Brain egress from any hook firing.
</verification>

<success_criteria>
The Tier D hooks not already fixed in 144 fire their sensors through the chokepoint and emit
memory_event; on-file-changed inherits the post-write fix; jtbd-update routes to
sensor-jtbd-reweight; zero Brain egress; zero em-dashes.
</success_criteria>

<output>
Create `.planning/phases/144.1-connector-retrofit-sweep/144.1-06-SUMMARY.md` when done.
</output>
