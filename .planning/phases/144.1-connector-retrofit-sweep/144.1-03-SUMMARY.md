---
phase: 144.1-connector-retrofit-sweep
plan: 03
subsystem: connector-spine
tags: [connector, tier-c, retrofit, intelligence-orchestrator, RETRO-02]
requires:
  - "data/connector-registry.json (32 connectors from Plans 01/02)"
  - "scripts/build-connector-registry.cjs (generator + --check)"
  - "data/framework-names.json (allowlist)"
provides:
  - "14 additive connector: blocks on the remaining Tier C command surfaces"
  - "connector-registry.json grown 32 -> 46 connectors"
  - "explicit empty sensor_triggers on no-sensor exploration commands"
affects:
  - "data/connector-registry.json (regenerated)"
  - "intelligence-orchestrator dispatch (more reach surfaces now resolvable)"
tech-stack:
  added: []
  patterns:
    - "additive connector: frontmatter block (Phase 143.3 contract)"
    - "null-framework + filing none + no surface so WFL-01 does not fire"
    - "distinct (sensor,reach,sub_mode) tuples per multi-surface framework"
key-files:
  created: []
  modified:
    - commands/explore-trends.md
    - commands/explore-futures.md
    - commands/explore-domains.md
    - commands/scout.md
    - commands/operator.md
    - commands/funding.md
    - commands/reanalyze.md
    - commands/macro-trends.md
    - commands/map-unknowns.md
    - commands/lean-canvas.md
    - commands/root-cause.md
    - commands/user-needs.md
    - commands/systems-thinking.md
    - commands/scenario-plan.md
    - data/connector-registry.json
decisions:
  - "No-sensor exploration commands (explore-futures, macro-trends, map-unknowns, lean-canvas) declare sensor_triggers: [] (valid empty) rather than a fabricated sensor"
  - "Null-framework commands (scout, operator, funding, reanalyze) use framework: null + filing: none + NO surface so firesCommand is false and WFL-01 never trips"
  - "reanalyze keyed to the contradiction reach to reuse the file-meeting contradiction wiring at runtime"
  - "hierarchy_rank continued the distinct integer sequence 32..45 from the prior max of 31"
metrics:
  duration: "~4 min"
  completed: 2026-06-07
  tasks: 2
  files: 15
---

# Phase 144.1 Plan 03: Tier C Batch-B Command Connectors Summary

Landed additive `connector:` frontmatter on the remaining 14 Tier C command surfaces (audit Section 3 items 44-58, less structure-argument which Plan 02 owns), growing the connector registry from 32 to 46 connectors with green `--check`.

## What Was Built

**Task 1 (items 44-50) - commit c45e2159:** connector blocks on explore-trends, explore-futures, explore-domains, scout, operator, funding, reanalyze.
- explore-trends -> `S-Curve Analysis`, SENS-04, push_forward, sub_mode `trends-scurve`, rank 32.
- explore-futures -> `Scenario Planning`, no sensor (`sensor_triggers: []`), hold, sub_mode `futures-scenario`, rank 33.
- explore-domains -> `Domain Selection`, SENS-01, hold, sub_mode `domain-select`, rank 34.
- scout -> null framework, SENS-04+SENS-07, hold, filing none, NO surface, sub_mode `scout`, rank 35.
- operator -> null framework, SENS-05, hold, filing none, NO surface, sub_mode `operator`, rank 36.
- funding -> null framework, SENS-07, hold, filing none, NO surface, sub_mode `funding`, rank 37.
- reanalyze -> `contradiction` reach, null framework, SENS-06, hold, filing none, NO surface, sub_mode `reanalyze`, rank 38.

**Task 2 (items 51-58) - commit 17abff73:** connector blocks on macro-trends, map-unknowns, lean-canvas, root-cause, user-needs, systems-thinking, scenario-plan.
- macro-trends -> `PEST Analysis`, no sensor, hold, sub_mode `macro-pest`, rank 39.
- map-unknowns -> `Knowns and Unknowns Matrix Framework`, no sensor, hold, sub_mode `unknowns-matrix`, rank 40.
- lean-canvas -> `Lean Canvas`, no sensor, hold, sub_mode `lean-canvas`, rank 41.
- root-cause -> `Root Cause Analysis`, SENS-06, hold, surface F.2, sub_mode `root-cause-5why`, rank 42.
- user-needs -> `Jobs to Be Done (JTBD)`, SENS-06, hold, surface F.2, sub_mode `user-needs-jtbd`, rank 43.
- systems-thinking -> `Systems Thinking`, SENS-06, hold, surface F.2, sub_mode `systems-thinking-loop`, rank 44.
- scenario-plan -> `Scenario Planning`, SENS-06, hold, surface F.2, sub_mode `scenario-plan`, rank 45.

## Verification

- `node scripts/build-connector-registry.cjs --check` exit 0 (46 connectors).
- `node scripts/build-command-registry.cjs --check` exit 0 (92 commands, unchanged by connector blocks).
- Each of the 14 commands carries exactly one `connects_to_spine: true` line.
- reanalyze keyed to `contradiction`; explore-futures declares empty `sensor_triggers`.
- All reach_ids are frozen-5; all postures frozen-3.
- Zero `(sensor,reach,sub_mode)` tuple collisions across all 46 connectors (programmatic scan).
- Multi-surface frameworks keep distinct tuples:
  - JTBD: analyze-needs / discover / jtbd / user-needs / validate all distinct by sensor+sub_mode.
  - Systems Thinking: analyze-systems (`systems-analysis`) vs systems-thinking (`systems-thinking-loop`).
  - Scenario Planning: explore-futures (`futures-scenario`, no sensor) vs scenario-plan (`scenario-plan`, SENS-06).
- Null-framework connectors (scout/operator/funding/reanalyze) use `filing: none` with NO surface, so `firesCommand` is false and WFL-01 does not fire.
- `grep -rc '—'` across all 14 files returns 0 (zero em-dashes).
- All 9 framework names validated present in `data/framework-names.json` before authoring.

## Deviations from Plan

### Auto-fixed Issues
None - plan executed exactly as written.

### Noted Audit Intent (memory_event)
The audit (Section 3 rows 47-50) tagged scout/operator/funding/reanalyze as `memory_event_only`. Per the plan's CRITICAL WFL-01 rule, `memory_event_only` still fires `firesCommand` and would require a resolvable framework (which these dash-placeholder rows do not have). The plan therefore directs `filing: none` + null framework + NO surface for these rows, which is what was applied. The original `memory_event_only` intent is recorded here as the documented deviation; runtime memory-event behavior for these surfaces is a future concern outside this additive-wiring plan.

## Pre-existing Observation (not introduced here)
`hierarchy_rank` carries two pre-existing duplicate values (1 and 3) among the original 32 connectors from Plans 01/02. The new connectors use unique ranks 32-45. The generator does not enforce rank uniqueness (rank is a tie-break field), so this is neither a regression nor a `--check` failure.

## Known Stubs
None.

## Threat Flags
None - no new network endpoints, auth paths, file access, or schema changes at trust boundaries. All edits are additive generic-handle YAML; the registry remains plugin-local and validated against the framework allowlist at build time (Canon Part 8 preserved).

## Commits
- c45e2159: feat(144.1-03): Tier C batch-B connector blocks items 44-50
- 17abff73: feat(144.1-03): Tier C batch-B connector blocks items 51-58

## Self-Check: PASSED
- SUMMARY.md present.
- All 14 command files present with exactly one connects_to_spine each.
- Both task commits (c45e2159, 17abff73) exist in git history.
