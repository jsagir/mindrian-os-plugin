---
phase: 144.1-connector-retrofit-sweep
plan: 08
subsystem: testing
tags: [connector-spine, registry, coverage-gate, part-8, allow-list, ci-tripwire]

# Dependency graph
requires:
  - phase: 143.3-connector-spine-and-intelligence-orchestrator
    provides: the connector contract + generator + --check tripwire + framework-names allowlist
  - phase: 144.1-04
    provides: the agents/-walk in build-connector-registry.cjs
  - phase: 144.1-05
    provides: the 7 agent connectors + the 2 out-of-spine agent reasons (framework-runner, larry-extended)
  - phase: 144.1-06
    provides: test-connector-tier-d-hooks.cjs (Tier-D firing wiring scan)
  - phase: 144.1-07
    provides: test-connector-filing-sweep.cjs (fileEvidenceWithReadback filing sweep)
provides:
  - the regenerated fully-populated connector-registry.json proven --check green (53 connectors: 46 commands + 7 agents)
  - data/connector-out-of-spine-allowlist.json (61 explicit out-of-spine surfaces with one-line reasons)
  - tests/test-connector-exhaustive-coverage.cjs (the RETRO-07c wired-XOR-allowlisted count gate)
  - tests/run-all-1441.sh (the phase verification aggregator, 13/13 green)
  - the DI-144.1-A fix (Part-8 CHECK 2 exempts framework null)
affects: [Phase 146 fully-wired acceptance gate, connector-retrofit-sweep closure]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "exhaustive-coverage count gate: derive the live surface set from disk, assert wired XOR allowlisted, never hardcode the count"
    - "out-of-spine allow-list with three reason categories (genuine out-of-spine / wired-in-another-phase / out-of-spine agent)"

key-files:
  created:
    - data/connector-out-of-spine-allowlist.json
    - tests/test-connector-exhaustive-coverage.cjs
    - tests/run-all-1441.sh
  modified:
    - tests/test-connector-part8-boundary.cjs

key-decisions:
  - "framework null is the legal WFL-01 no-framework config, not smuggled content; CHECK 2 exempts it, a non-null framework must still be a generic name"
  - "a surface is wired XOR allow-listed, never both; the coverage gate fails on a double-classified surface"
  - "Tier A surfaces whose connectors are owned by Phase 143.3/144 (rs-*, causal, persona, hat-briefing, diagnostics, file-meeting, brain-derive, diagnose, suggest-next, act) are allow-listed with an ownership reason so 144.1 counts them classified without claiming them"

patterns-established:
  - "count-identity gate: wired + allowlisted === live derived from fs.readdirSync, the single proof of exhaustive coverage"
  - "allow-list keys use the SAME id scheme as the registry (/mos: command, skill:, agent:) so the set-difference is exact"

requirements-completed: [RETRO-06, RETRO-07]

# Metrics
duration: 18min
completed: 2026-06-07
---

# Phase 144.1 Plan 08: Registry Complete + Exhaustive Coverage Gate Summary

**RETRO-06 + RETRO-07c capstone: the connector registry is fully populated and --check green (53 connectors), and a disk-derived count gate proves all 114 live surfaces are classified exactly once (53 wired + 61 explicitly out-of-spine), closing Phase 144.1.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-06-07
- **Completed:** 2026-06-07
- **Tasks:** 3 completed
- **Files modified:** 4 (1 modified, 3 created)

## Accomplishments

### Task 1 (RETRO-06): registry regenerated, --check green across the whole phase
- Ran `node scripts/build-connector-registry.cjs` -> 53 connectors (46 commands + 7 agents). The on-disk registry was already byte-stable from prior plans 01-07 (zero diff after regen), so the population landed in the prior plans; this plan proves it complete.
- `node scripts/build-connector-registry.cjs --check` exits 0: every reach_id in the frozen 5, every posture in the frozen 3, every surface-bearing framework resolves via commandsForFramework() (WFL-01), no (sensor,reach,sub_mode) tuple collision.
- The registry carries `source: command` AND `source: agent` entries (no skill opted into the spine; all 13 skills are HOW-layer substrate, correctly out-of-spine).
- `grep -c` em-dashes on the registry returns 0.

### Task 2 (RETRO-07c): out-of-spine allow-list + the exhaustive-coverage count gate
- Built `data/connector-out-of-spine-allowlist.json`: 61 keys (the exact set-difference live \ wired), each with a one-line reason in three categories:
  - **Genuine out-of-spine (audit Section 4):** routing surfaces (mos, pipeline), read-only/observability (status, graph, memory, speakers, room, explain-decision, feynman-timeline-refresh, dashboard, wiki), output/export (present, publish, export, snapshot, vault, splash, MOSDeckEngine skill), system/admin/config (models, setup, doctor, onboard, rooms, update, admin, help, radar, dogfood-flush, scheduled-tasks), HOW-layer skills (brain-connector, context-engine, room-passive, room-proactive, ui-system, pws-methodology, conversation-mode, mva-pipeline, mullins-scaffold, client-discovery-interview, intelligence-orchestrator, larry-personality), deprecated stubs (organize, heal, query, visualize, hmi-status).
  - **Wired in another phase:** rs-fetch, rs-experts, rs-explain, rs-thesis, causal, persona, hat-briefing, diagnostics (Phase 143.3); file-meeting, brain-derive, diagnose, suggest-next, act (Phase 144).
  - **Out-of-spine agents:** framework-runner, larry-extended (Phase 144.1-05 reasons).
- Built `tests/test-connector-exhaustive-coverage.cjs`: derives the live surface set via `fs.readdirSync` (never a hardcoded 114), loads the wired set + allow-list keys, and asserts: (1) zero silently-unclassified surfaces, (2) zero double-classified surfaces, (3) every allow-list key is a live surface with a non-empty reason, (4) the 2 out-of-spine agents are present, (5) the count identity `wired + allowlisted === live`.
- Result: **live=114, wired=53, allowlisted=61, 53+61=114.** 6/6 checks pass.

### DI-144.1-A fix (carried deferred item)
- `tests/test-connector-part8-boundary.cjs` CHECK 2 now exempts `framework === null` (the legal WFL-01 no-framework config: /mos:funding + agent:brain-query + agent:investor + agent:opportunity-scanner). A non-null framework must still be a generic name in framework-names.json. The test now exits 0 over the full 53-connector registry (4/4 threat paths). DI-144.1-A marked RESOLVED in deferred-items.md.

### Task 3: run-all-1441.sh aggregator
- Created `tests/run-all-1441.sh` mirroring run-all-1433.sh: the --check tripwire (direct invocation) + 11 CJS suites (4 new 144.1 + 7 carried 143.3 connector/drift/part8) + a standalone Part-8 grep sweep over the populated registry + the allow-list.
- **bash tests/run-all-1441.sh exits 0: 13/13 green.**

## Final connector count

53 connectors = 46 commands + 7 agents + 0 skills. (Skills are all HOW-layer substrate and correctly out-of-spine.) This matches the live registry that prior plans 01-07 populated; Plan 08 proves it complete and exhaustively classified rather than adding connectors.

## Deviations from Plan

None for Rules 1-4. The plan's Task 1 anticipated possibly fixing offending source connectors if --check reported drift; --check was already green (registry byte-stable from prior plans), so no source connector needed fixing. The DI-144.1-A fix was an explicitly-scoped carried item, not an unplanned deviation.

## Acceptance Criteria

- [x] `node scripts/build-connector-registry.cjs --check` exits 0; registry carries `source: agent`; zero em-dashes
- [x] `node tests/test-connector-exhaustive-coverage.cjs` exits 0; count derived from disk; wired + allowlisted === live (53 + 61 = 114)
- [x] allow-list has a non-empty reason for every key; the 2 out-of-spine agents present; zero em-dashes
- [x] DI-144.1-A fixed: Part-8 CHECK 2 exempts framework null; test exits 0 over full registry
- [x] `bash tests/run-all-1441.sh` exits 0 (13/13 green)
- [x] No reach_id outside the frozen 5; no posture outside the frozen 3; no tuple collision; no 6th reach minted

## Known Stubs

None. The allow-list deliberately catalogs deprecated stubs (organize, heal, query, visualize, hmi-status) with a "removal v1.14.0" reason; these are intentional out-of-spine classifications, not implementation stubs.

## Self-Check: PASSED

- FOUND: data/connector-out-of-spine-allowlist.json
- FOUND: tests/test-connector-exhaustive-coverage.cjs
- FOUND: tests/run-all-1441.sh
- FOUND commit c210ac21 (DI-144.1-A fix)
- FOUND commit bc389e40 (allow-list + coverage gate)
- FOUND commit 8575a54c (aggregator)
