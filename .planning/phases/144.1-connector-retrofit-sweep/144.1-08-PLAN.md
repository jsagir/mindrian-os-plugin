---
phase: 144.1-connector-retrofit-sweep
plan: 08
type: execute
wave: 3
depends_on: ["144.1-01", "144.1-02", "144.1-03", "144.1-04", "144.1-05", "144.1-06", "144.1-07"]
files_modified:
  - data/connector-registry.json
  - data/connector-out-of-spine-allowlist.json
  - tests/test-connector-exhaustive-coverage.cjs
  - tests/run-all-1441.sh
autonomous: true
requirements: [RETRO-06, RETRO-07]
canon_parts: [Part 6, Part 7, Part 8]
must_haves:
  truths:
    - "data/connector-registry.json is regenerated and populated for every spine-eligible command + skill + agent"
    - "node scripts/build-connector-registry.cjs --check exits 0 (frozen banks + WFL-01 + no tuple collision)"
    - "the exhaustive-coverage count gate asserts EVERY command + skill + agent on disk is either in the registry OR on the out-of-spine allow-list with a reason - zero silently-unclassified surfaces"
    - "the allow-list enumerates the genuine out-of-spine surfaces (audit Section 4) each with a one-line reason"
    - "run-all-1441.sh runs the new suites + the carried 143.3 + Part-8 sweep and exits non-zero on any failure"
  artifacts:
    - path: "data/connector-registry.json"
      provides: "the fully populated connector registry (commands + skills + agents)"
      contains: "\"source\": \"agent\""
    - path: "data/connector-out-of-spine-allowlist.json"
      provides: "explicit out-of-spine classification with a reason per surface"
      min_lines: 40
    - path: "tests/test-connector-exhaustive-coverage.cjs"
      provides: "the count gate: wired OR allow-listed, zero unclassified"
      min_lines: 50
    - path: "tests/run-all-1441.sh"
      provides: "the phase verification aggregator"
      min_lines: 30
  key_links:
    - from: "every commands/*.md + skills/*/SKILL.md + agents/*.md on disk"
      to: "registry.connectors[] OR allowlist"
      via: "test-connector-exhaustive-coverage.cjs set-difference"
      pattern: "wired-or-allowlisted"
---

<objective>
The capstone wave: regenerate and populate data/connector-registry.json for every
spine-eligible surface, prove --check green, and land the RETRO-07c exhaustive-coverage
count gate that asserts EVERY command + skill + agent on disk is either WIRED (in the
registry) or EXPLICITLY out-of-spine (on the allow-list with a reason) - zero silently-
unclassified surfaces. Add run-all-1441.sh as the phase aggregator.

Purpose: this is the gate that lets Phase 146 certify a FULLY WIRED MindrianOS across the
full surface set (not a sample). It verifies the whole phase: all connectors validate, the
registry is complete, and coverage is provably exhaustive.

Output: a populated registry, the out-of-spine allow-list, the count-gate test, and the
run-all-1441.sh aggregator - all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/CONNECTOR-CONTRACT.md
@scripts/build-connector-registry.cjs
@tests/run-all-1433.sh
@tests/test-connector-part8-boundary.cjs
@data/connector-registry.json
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- A regenerated, fully populated data/connector-registry.json (commands + skills + agents).
- data/connector-out-of-spine-allowlist.json: the explicit out-of-spine classification.
- tests/test-connector-exhaustive-coverage.cjs: the wired-or-allowlisted count gate.
- tests/run-all-1441.sh: the phase verification aggregator.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the registry; prove --check green across the whole phase</name>
  <read_first>
    - scripts/build-connector-registry.cjs (the default-write branch + the four --check validations) - now walking agents/ after Plan 04
    - data/connector-registry.json (the current 8-connector state to be replaced by the full population)
    - The Plan 01/02/03 command connectors + Plan 05 agent connectors (the inputs being registered)
  </read_first>
  <action>
    Run node scripts/build-connector-registry.cjs to regenerate data/connector-registry.json
    from all the connector: blocks landed in Plans 01/02/03 (commands) + Plan 05 (agents),
    discovered via the Plan 04 agents walk. Commit the regenerated registry. Then run
    node scripts/build-connector-registry.cjs --check and confirm exit 0: every reach_id in
    the frozen 5, every posture in the frozen 3, every filing!=none/surface-bearing framework
    resolves via commandsForFramework() (WFL-01), and NO (sensor,reach,sub_mode) tuple
    collision across the whole populated set. If --check reports a stale registry, a non-frozen
    value, an unresolvable framework, or a tuple collision, FIX the offending source connector
    (in its owning Plan-01/02/03/05 file) and regenerate until --check is green. The registry
    is GENERATED - never hand-edit it. Confirm the registry now carries source: command,
    source: skill, AND source: agent entries.
  </action>
  <acceptance_criteria>
    - node scripts/build-connector-registry.cjs --check exits 0
    - data/connector-registry.json carries >= 1 entry with "source": "agent"
    - The registry connector count equals (Tier A from 143.3) + (Plan 01: 13) + (Plan 02: 14) + (Plan 03: 14) + (Plan 05: 7 agents) minus any audit-confirmed overlaps; the SUMMARY records the exact final count
    - No reach_id outside the frozen 5; no posture outside the frozen 3; no tuple collision
    - grep -c '—' data/connector-registry.json returns 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/build-connector-registry.cjs && node scripts/build-connector-registry.cjs --check && grep -q '"source": "agent"' data/connector-registry.json && echo OK</automated>
  </verify>
  <done>The registry is fully populated (commands + skills + agents) and --check is green.</done>
</task>

<task type="auto">
  <name>Task 2: The out-of-spine allow-list + the exhaustive-coverage count gate (RETRO-07c)</name>
  <read_first>
    - LARRYREACH-CONNECTOR-AUDIT.md Section 4 (the no-connector buckets: routing surfaces, read-only/observability, output/export, system/admin/config, HOW-layer skills, guard hooks, deprecated stubs)
    - The Plan 05 SUMMARY (the 2 out-of-spine agents: framework-runner, larry-extended, with reasons)
    - The actual on-disk surface set: ls commands/*.md (92), ls -d skills/*/ (13: includes client-discovery-interview + MOSDeckEngine + ...), ls agents/*.md (9). NOTE: the live counts are 92 + 13 + 9 = 114, not the 91 + 12 + 9 = 112 the CONTEXT estimated; the gate MUST enumerate the live directories, never a hardcoded 112.
    - tests/test-connector-part8-boundary.cjs (the registry-read test idiom)
    - data/connector-registry.json (the wired set)
  </read_first>
  <action>
    Build data/connector-out-of-spine-allowlist.json: an object keyed by surface id (using the
    SAME id scheme as the registry: "/mos:<base>" for commands, "skill:<base>" for skills,
    "agent:<base>" for agents) -> a one-line reason string drawn from audit Section 4 buckets
    + the Plan 05 agent reasons. EVERY surface that is NOT in the registry MUST appear here
    with a reason. Include: the routing surfaces (mos, pipeline, suggest-next, act, on-task-
    complete-style command surfaces), read-only/observability (status, graph, memory, speakers,
    room, explain-decision, feynman-timeline-refresh, dashboard, wiki, diagnostics if not
    wired), output/export (present, publish, export, snapshot, vault, splash + MOSDeckEngine
    skill), system/admin/config (models, setup, doctor, onboard, rooms, update, admin, help,
    radar, dogfood-flush, scheduled-tasks), HOW-layer skills (brain-connector, context-engine,
    room-passive, room-proactive, ui-system, pws-methodology, conversation-mode, mva-pipeline,
    mullins-scaffold, client-discovery-interview, intelligence-orchestrator itself, larry-
    personality), and the 2 out-of-spine agents (framework-runner, larry-extended). Deprecated
    stubs (organize, heal, query, visualize, hmi-status) get a "deprecated stub - removal
    v1.14.0" reason. Each reason is one line, no em-dashes.
    THREE reason CATEGORIES for the allow-list (a surface is allow-listed iff it is NOT in the
    144.1 registry): (a) GENUINELY out-of-spine per audit Section 4 (routing / read-only /
    output / system / HOW-layer skill / guard / deprecated) - reason cites the bucket;
    (b) WIRED IN ANOTHER PHASE - the Tier A critical surfaces whose connectors land in 143.3
    (rs-fetch, diagnostics, rs-experts, rs-explain, causal, persona, hat-briefing) or Phase 144
    (file-meeting, brain-derive, diagnose, suggest-next, act) carry the reason "connector owned
    by Phase 143.3/144, not 144.1" so the gate counts them classified without 144.1 claiming
    them; (c) the 2 out-of-spine agents (framework-runner, larry-extended) with their Plan-05
    reasons. IMPORTANT: if a surface listed here as "wired in 143.3/144" is ALREADY in the live
    registry when this plan runs (143.3/144 shipped first), REMOVE it from the allow-list so it
    is counted as wired, not allow-listed - a surface is wired XOR allow-listed, never both; the
    coverage gate asserts no-overlap and FAILs on a double-classified surface.
    Then create tests/test-connector-exhaustive-coverage.cjs. It MUST:
    (1) enumerate the live surfaces by reading the directories: fs.readdirSync(commands) .md,
    fs.readdirSync(skills) dirs with a SKILL.md, fs.readdirSync(agents) .md - building the
    full surface-id set with the SAME id scheme. NEVER hardcode 112/114; derive the count.
    (2) load the registry wired set (surface ids) and the allow-list keys.
    (3) assert: every live surface id is in (wired UNION allowlisted); FAIL listing any
    surface that is in NEITHER (silently unclassified).
    (4) assert: no surface is in BOTH wired and allowlisted (a surface is wired XOR excluded).
    (5) assert: every allow-list entry has a non-empty reason string.
    (6) print the totals: live=<N>, wired=<W>, allowlisted=<A>, and assert W + A === live.
    Exit non-zero on any failure. No em-dashes. Pure (no Brain, no network).
  </action>
  <acceptance_criteria>
    - node tests/test-connector-exhaustive-coverage.cjs exits 0
    - The test derives the live surface count from the directories (no hardcoded 112/114)
    - wired + allowlisted === live (every surface classified exactly once)
    - data/connector-out-of-spine-allowlist.json has a non-empty reason for every key
    - The two out-of-spine agents (framework-runner, larry-extended) appear on the allow-list with their Phase-144-ownership reasons
    - grep -rc '—' on the allow-list + the test returns 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-connector-exhaustive-coverage.cjs</automated>
  </verify>
  <done>The count gate proves every live command + skill + agent is wired-or-allowlisted; zero silently-unclassified surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: run-all-1441.sh aggregator (new suites + carried 143.3 + Part-8 sweep)</name>
  <read_first>
    - tests/run-all-1433.sh (the bash PASS/FAIL loop + CJS_SUITES array + the tripwire invocation + the Part-8 grep sweep + the tally/exit pattern - mirror it EXACTLY)
    - The new suites: test-connector-agents-walk.cjs (Plan 04), test-connector-tier-d-hooks.cjs (Plan 06), test-connector-filing-sweep.cjs (Plan 07), test-connector-exhaustive-coverage.cjs (this plan)
    - tests/test-connector-part8-boundary.cjs (carried; must stay green over the now-populated registry)
  </read_first>
  <action>
    Create tests/run-all-1441.sh mirroring run-all-1433.sh's structure: (a) the CI tripwire as
    a direct invocation (node scripts/build-connector-registry.cjs --check); (b) a CJS_SUITES
    array running the four NEW 144.1 suites PLUS the carried 143.3 connector suites
    (test-connector-registry.cjs, test-connector-tripwire.cjs, test-connector-part8-boundary.cjs,
    test-reach-ids-drift.cjs, test-posture-ids-drift.cjs) so the frozen-bank + Part-8 fences
    re-run over the now-populated registry; (c) a standalone Part-8 grep sweep over the
    populated registry + the allow-list for forbidden free-text body fields (mirror the
    run-all-1433.sh grep sweep). Run to completion even when a suite fails; print per-suite
    PASS/FAIL + a final tally; exit 1 if any failed. bash only, no emoji, no em-dashes.
  </action>
  <acceptance_criteria>
    - bash tests/run-all-1441.sh exits 0 with all suites green
    - The aggregator runs the 4 new suites + the carried 143.3 connector/drift/part8 suites + the --check tripwire + a Part-8 grep sweep
    - The aggregator runs to completion and prints a per-suite PASS/FAIL tally even on failure
    - grep -c '—' tests/run-all-1441.sh returns 0
  </acceptance_criteria>
  <verify>
    <automated>bash tests/run-all-1441.sh</automated>
  </verify>
  <done>run-all-1441.sh runs the full 144.1 verification + carried fences and exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source connectors -> generated registry | the registry is generated, never hand-edited |
| live surfaces -> coverage classification | every surface wired XOR allowlisted; no silent skip |
| registry + allow-list -> Brain | generic handles only; no user content (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-22 | Tampering | a hand-edited registry drifting from the frontmatter | mitigate | --check byte-compares regenerated vs on-disk; hard exit 1 |
| T-144.1-23 | Repudiation | a surface silently unclassified (neither wired nor excluded) | mitigate | the exhaustive-coverage count gate asserts wired + allowlisted === live |
| T-144.1-24 | Information disclosure | a free-text body field on a connector or allow-list entry | mitigate | Part-8 grep sweep + ALLOWED_CONNECTOR_FIELDS gate over the populated registry |
| T-144.1-25 | Tampering | hardcoded 112/114 masking a new surface | mitigate | the gate derives the live count from the directories, never a constant |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | no package installs; regen + tests + JSON only |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exits 0 over the fully populated registry.
- node tests/test-connector-exhaustive-coverage.cjs exits 0 (wired + allowlisted === live).
- bash tests/run-all-1441.sh exits 0.
- The registry carries source: command, skill, AND agent entries.
</verification>

<success_criteria>
The registry is fully populated and --check green; the exhaustive-coverage count gate proves
every live command + skill + agent is wired-or-allowlisted with zero silent skips; run-all-1441.sh
is green; the baton is ready for Phase 146. Zero type changes; zero em-dashes; no 6th reach.
</success_criteria>

<output>
Create `.planning/phases/144.1-connector-retrofit-sweep/144.1-08-SUMMARY.md` when done.
</output>
