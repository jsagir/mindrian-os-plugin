---
phase: 144.1-connector-retrofit-sweep
plan: 02
subsystem: connector-spine
tags: [connector-retrofit, tier-c, methodology-commands, RETRO-02, RETRO-05]
requires:
  - "143.3 connector spine + generator + --check tripwire (shipped)"
  - "144 navigation-engine legacy->engine flip (shipped)"
  - "144.1-01 Tier B command connectors + connector-registry at 18 (shipped)"
provides:
  - "14 Tier C batch-A command connector: blocks (analyze-systems, compare-ventures, leadership, mos-reason, dial-memory-refresh, new-project, mullins, validate, value-proposition, analyze-timing, dominant-designs, build-knowledge, beautiful-question, structure-argument)"
  - "leadership.md Phase-122 frontmatter (kind/frameworks: [Adaptive Leadership]/produces) -- WFL-01 prereq"
  - "value-proposition.md name/file drift recorded as frontmatter note"
  - "structure-argument.md The Pyramid Principle chosen as primary connector framework key"
  - "data/connector-registry.json regenerated to 32 connectors"
  - "data/command-registry.json regenerated (Adaptive Leadership now resolves to /mos:leadership)"
affects:
  - "data/connector-registry.json"
  - "data/command-registry.json"
tech-stack:
  added: []
  patterns:
    - "Additive Phase 144.1 connector: frontmatter block (delimited)"
    - "Null-framework connector (new-project) uses filing: none + no surface to pass WFL-01"
    - "memory_event_only filing fires WFL-01 (compare-ventures/dial-memory-refresh frameworks resolve)"
    - "Connector framework: may differ from a surface's own frameworks: list when --check resolution is satisfied (dial-memory-refresh frameworks: [] but connector framework Reverse Salient Analysis resolves)"
key-files:
  created:
    - .planning/phases/144.1-connector-retrofit-sweep/144.1-02-SUMMARY.md
  modified:
    - commands/analyze-systems.md
    - commands/compare-ventures.md
    - commands/leadership.md
    - commands/mos-reason.md
    - commands/dial-memory-refresh.md
    - commands/new-project.md
    - commands/mullins.md
    - commands/validate.md
    - commands/value-proposition.md
    - commands/analyze-timing.md
    - commands/dominant-designs.md
    - commands/build-knowledge.md
    - commands/beautiful-question.md
    - commands/structure-argument.md
    - data/connector-registry.json
    - data/command-registry.json
decisions:
  - "Task 1 RETRO-05 cleanups (leadership Phase-122 frontmatter, value-proposition name/file note, structure-argument primary key) were found already staged in the working tree from an interrupted prior session; verified correct against the plan acceptance criteria and committed as this plan's Task 1."
  - "Regenerated BOTH command-registry.json (Adaptive Leadership -> /mos:leadership) and connector-registry.json (18 -> 32) in THIS plan because the shipped pre-commit hook runs both --check guards when commands/*.md is staged; deferring to Plan 08 would block the commit. Rule 3 blocking-issue auto-fix."
  - "new-project carries framework: null + filing: none + no surface (declares no frameworks: block); WFL-01 firesCommand gate does not fire."
  - "dial-memory-refresh connector framework is Reverse Salient Analysis (per audit item 35) even though its own frameworks: list is []; the --check only requires the connector framework to resolve via commandsForFramework(), which it does. The audit's memory_event_only intent is honored."
metrics:
  duration_sec: 540
  completed: 2026-06-07
  tasks: 2
  files_changed: 16
---

# Phase 144.1 Plan 02: Tier C Batch-A Command Connectors + RETRO-05 Cleanups Summary

Landed additive `connector:` frontmatter on the 14 Tier C batch-A methodology/state commands (audit Section 3 items 31-43 plus item 56), each with a unique `(sensor, reach, sub_mode)` tuple vs the 18 existing connectors and a resolvable framework (WFL-01), plus the three RETRO-05 prereq cleanups that gate them (leadership's missing Phase-122 frontmatter, value-proposition name/file drift, structure-argument primary framework key). Both registries regenerated; the shipped CONN-03 `--check` is green (exit 0); connector-registry grew 18 -> 32.

## What was built

### Task 1 (commit 68ac21a2) - RETRO-05 cleanups gating Tier C batch-A connectors
- **leadership.md**: added the Phase-122 methodology block (`kind: methodology`, `frameworks: ["Adaptive Leadership"]`, `produces: "room/team-execution/leadership/*"`, `inputs: []`, `autonomous_safe: true`). This is the audit's hard prereq: `commandsForFramework("Adaptive Leadership")` returned `[]` before (no command declared it), so leadership's connector would have failed the WFL-01 `--check`. After regenerating command-registry, it resolves to `["/mos:leadership"]`.
- **value-proposition.md**: recorded the intentional name/file relationship (the file is `value-proposition.md` but `name: validate-proposition`) as a six-line frontmatter comment. The file is NOT renamed (renaming would break the generator surface id `/mos:value-proposition` and the resolver key `/mos:validate-proposition`). The drift is now documented, not silently carried.
- **structure-argument.md**: kept the dual `frameworks: ["The Pyramid Principle", "MECE (...)"]` list intact and recorded (via comment) that the connector's single `framework:` field keys to the PRIMARY, "The Pyramid Principle" (audit item 56).

### Task 2 (commit 2a534556) - Tier C batch-A connector blocks
Added one delimited `# --- Phase 144.1 connector frontmatter ---` block to each of 14 commands. Tuples (sensor | reach | sub_mode), all unique vs the 18 existing and each other:

| Command | reach_id | sensors | framework | posture | filing | sub_mode | rank | surface |
|---|---|---|---|---|---|---|---|---|
| analyze-systems | context_block | SENS-06 | Systems Thinking | push_forward | fileEvidenceWithReadback | systems-analysis | 18 | - |
| compare-ventures | brain_consult | SENS-03 | PWS Triple Validation Compass | hold | memory_event_only | venture-compare | 19 | - |
| leadership | context_block | SENS-05 | Adaptive Leadership | hold | fileEvidenceWithReadback | adaptive-leadership | 20 | - |
| mos-reason | context_block | SENS-06 | The Pyramid Principle | push_forward | fileEvidenceWithReadback | minto-reason | 21 | - |
| dial-memory-refresh | context_block | SENS-02 | Reverse Salient Analysis | hold | memory_event_only | dial-refresh | 22 | - |
| new-project | context_block | SENS-01,06 | null | push_forward | none | new-project | 23 | - |
| mullins | context_block | SENS-03,06,07 | Mullins Model | hold | fileEvidenceWithReadback | mullins-seven | 24 | F.2 |
| validate | context_block | SENS-06 | Jobs to Be Done (JTBD) | hold | fileEvidenceWithReadback | validate-jtbd | 25 | F.2 |
| value-proposition | context_block | SENS-06,07 | PWS Value Proposition | hold | fileEvidenceWithReadback | value-prop | 26 | - |
| analyze-timing | context_block | SENS-06 | S-Curve Analysis | push_forward | fileEvidenceWithReadback | timing-scurve | 27 | - |
| dominant-designs | context_block | SENS-06 | Dominant Design | push_forward | fileEvidenceWithReadback | dominant-design | 28 | - |
| build-knowledge | context_block | SENS-06 | Ackoff Pyramid | push_forward | fileEvidenceWithReadback | ackoff-pyramid | 29 | - |
| beautiful-question | context_block | SENS-06 | Beautiful Question Framework | push_forward | fileEvidenceWithReadback | beautiful-question | 30 | - |
| structure-argument | context_block | SENS-06 | The Pyramid Principle | hold | fileEvidenceWithReadback | pyramid-argument | 31 | - |

`hierarchy_rank` continues distinct past the Tier B ranks (18..31, in audit-item order, a deterministic choice). `plan_gated: false` on all. `web_scope: null` on all (none reach the web in this batch). Surfaces `F.2` on mullins + validate per the audit; no surface elsewhere.

## Verification (all pass)
- `node scripts/build-connector-registry.cjs --check` -> `connector-registry: OK`, exit 0 (32 connectors)
- `node scripts/build-command-registry.cjs --check` -> `command-registry: OK`, exit 0
- Each of the 14 commands has exactly one `connects_to_spine: true` line (Task 2 `<automated>` verify -> "OK 14 Tier C batch-A connectors")
- `validateConnectors()` CLEAN: all reach_ids in the frozen 5; all postures in the frozen 3; every firing connector's framework resolves via `commandsForFramework()` (WFL-01); no `(sensor, reach, sub_mode)` tuple collision
- `commandsForFramework("Adaptive Leadership")` -> `["/mos:leadership"]` (was `[]`)
- Task 1 acceptance: `grep -v '^#' commands/leadership.md | grep -c 'kind: methodology'` = 1; `grep -c 'frameworks:.*Adaptive Leadership'` = 1; produces glob present; value-proposition note line present; structure-argument keeps both framework keys
- Zero em-dashes across all 14 touched files (and the three Task 1 files)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Regenerated command-registry.json AND connector-registry.json in this plan**
- **Found during:** Task 1 (committing the leadership Adaptive Leadership declaration)
- **Issue:** The PLAN states the registries are regenerated in Plan 08. However the SHIPPED `.git/hooks/pre-commit` runs BOTH `build-command-registry.cjs --check` AND `build-connector-registry.cjs --check` whenever any `commands/*.md` is staged. Committing WITH hooks (per the contract, never `--no-verify`) would FAIL on a stale registry. Leadership's connector WFL-01 guard also requires command-registry to already map Adaptive Leadership -> /mos:leadership.
- **Fix:** Ran `node scripts/build-command-registry.cjs` (Task 1) and `node scripts/build-connector-registry.cjs` (Task 2) and committed the regenerated JSON alongside the frontmatter. Both `--check` guards pass green at commit time. (Same Rule-3 deviation as Plan 01.)
- **Files modified:** data/command-registry.json, data/connector-registry.json
- **Commits:** 68ac21a2, 2a534556

### Noted Intent / Observations (not auto-fixes)

**2. Task 1 cleanups were pre-staged from an interrupted prior session**
- On execution start, `git status` showed the three RETRO-05 files (leadership.md, value-proposition.md, structure-argument.md) already staged with exactly the Task 1 changes. These were verified line-by-line against the Task 1 `<action>` and `<acceptance_criteria>`, found correct, and committed as this plan's Task 1. No rework was needed.

**3. dial-memory-refresh connector framework vs its own frameworks: list**
- The audit (item 35) assigns dial-memory-refresh the framework "Reverse Salient Analysis" with `memory_event_only`. The command's own `frameworks:` list is `[]` (it is `kind: utility`). The connector contract section 2 says the connector `framework` "MUST equal the surface's existing `frameworks:` value", but the SHIPPED `--check` only enforces that the framework RESOLVES via `commandsForFramework()` (to any command), not that it matches the surface's own list. The plan explicitly directs this value, and Reverse Salient Analysis resolves (to find-bottlenecks/rs-experts/rs-explain/rs-fetch/rs-thesis), so `--check` passes. The audit's `memory_event_only` filing intent is honored exactly.

**4. Pre-existing duplicate hierarchy_rank (1, 3) in Tier A connectors**
- The registry carries duplicate ranks at 1 (find-analogies/find-connections) and 3 (score-innovation/whitespace) from the Tier A cohort (Phase 143.3). These are out of scope for this plan and were left untouched. The `--check` does NOT enforce global rank uniqueness (ranks are tie-broken by the Intelligence Hierarchy order). This plan's 14 new ranks (18..31) are all distinct and collision-free.

## TDD Gate Compliance
Not a TDD plan (`type: execute`). No RED/GREEN gate required.

## Self-Check: PASSED
- All 14 command files + both registries present and committed.
- Commit 68ac21a2 present in `git log` (Task 1 RETRO-05 cleanups).
- Commit 2a534556 present in `git log` (Task 2 connector blocks).
- connector-registry.json has 32 connectors; `--check` exit 0; `validateConnectors()` clean.
