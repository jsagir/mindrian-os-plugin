---
phase: 144.1-connector-retrofit-sweep
plan: 05
type: execute
wave: 2
depends_on: ["144.1-04"]
files_modified:
  - agents/grading.md
  - agents/investor.md
  - agents/opportunity-scanner.md
  - agents/research.md
  - agents/persona-analyst.md
  - agents/brain-query.md
  - agents/reverse-salient-agent.md
autonomous: true
requirements: [RETRO-07]
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 8]
must_haves:
  truths:
    - "Every spine-eligible agent of the 9 declares a valid connector: block (connects_to_spine: true)"
    - "Pure execution/util agents (framework-runner, larry-extended) are NOT wired here and are recorded on the out-of-spine allow-list (Plan 08) with a reason"
    - "Each agent connector picks a unique (sensor,reach,sub_mode) tuple vs all commands and the 8 existing"
    - "Agent connectors are discovered by the Plan-04 agents walk and appear as agent: surfaces"
  artifacts:
    - path: "agents/investor.md"
      provides: "contradiction-reach agent connector (SENS-07, pull_back)"
      contains: "connects_to_spine: true"
    - path: "agents/research.md"
      provides: "deep_research agent connector routing filing through fileEvidenceWithReadback"
      contains: "deep_research"
  key_links:
    - from: "agents/*.md connector blocks"
      to: "data/connector-registry.json connectors[] with source: agent"
      via: "Plan-04 agents walk in build-connector-registry.cjs"
      pattern: "connects_to_spine:\\s*true"
---

<objective>
Declare connector: frontmatter on every spine-eligible agent (RETRO-07b). Audit-classify
each of the 9 agents: the proactive reach-surfacing agents wire; the pure execution/util
agents stay explicitly out-of-spine (recorded on the Plan-08 allow-list with a reason).

Purpose: agents are part of the rewiring per locked decision 5 (exhaustive coverage). The
proactive agents (investor, opportunity-scanner, grading, research, reverse-salient-agent,
persona-analyst, brain-query) each own a reach and must declare it so the orchestrator can
route to them.

Output: 7 agent .md files with additive connector: blocks, discovered by the Plan-04 agents
walk, green against --check (Plan 08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/CONNECTOR-CONTRACT.md
@scripts/build-connector-registry.cjs
@commands/research.md
@data/connector-registry.json
@agents/research.md
@agents/investor.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- 7 agent connector blocks (grading, investor, opportunity-scanner, research, persona-analyst,
  brain-query, reverse-salient-agent), each with a unique (sensor,reach,sub_mode) tuple.
- The explicit classification rationale for the 2 out-of-spine agents (framework-runner,
  larry-extended), to be transcribed onto the Plan-08 allow-list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Audit-classify all 9 agents; record the 2 out-of-spine reasons</name>
  <read_first>
    - agents/grading.md, agents/investor.md, agents/opportunity-scanner.md, agents/research.md, agents/persona-analyst.md, agents/brain-query.md, agents/reverse-salient-agent.md (descriptions + any existing frameworks:)
    - agents/framework-runner.md, agents/larry-extended.md (the two out-of-spine candidates)
    - LARRYREACH-CONNECTOR-AUDIT.md Section 1 (larry-extended + framework-runner are Phase 144 engine-repoint / filing-fix items) + Section 3 Tier A/B rows for the agents (items 4, 13, 14, 18, 19, 26, 39) + Section 4 (out-of-spine buckets)
  </read_first>
  <action>
    Produce the classification (record it in the SUMMARY and hand it to Plan 08 for the
    allow-list). WIRE these 7 (proactive reach-surfacing): grading, investor,
    opportunity-scanner, research, persona-analyst, brain-query, reverse-salient-agent.
    OUT-OF-SPINE these 2, each with a one-line reason for the allow-list:
    - framework-runner: "pure execution/filing surface - owns no reach; its bare-Write->
      fileEvidenceWithReadback fix is Phase 144, not a connector."
    - larry-extended: "loop HEAD / routing surface - reads decide() and emits context_block
      via the engine; its turn-1 connector wiring is owned by Phase 144 (file-ownership
      fence). Declares no connector in 144.1."
    This task writes NO frontmatter; it only fixes the classification so Task 2 wires exactly
    the 7 and Plan 08 allow-lists exactly the 2. No em-dashes in the recorded reasons.
  </action>
  <acceptance_criteria>
    - The SUMMARY lists all 9 agents partitioned into WIRE (7) and OUT-OF-SPINE (2) with a reason per out-of-spine agent
    - The two out-of-spine reasons cite Phase 144 ownership (not "too hard" / not scope reduction)
  </acceptance_criteria>
  <verify>
    <automated>test -f agents/framework-runner.md && test -f agents/larry-extended.md && echo "OK classification targets present"</automated>
  </verify>
  <done>9 agents partitioned; 7 to wire, 2 out-of-spine with Phase-144-ownership reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Agent connector blocks (the 7 spine-eligible) with unique tuples</name>
  <read_first>
    - commands/research.md lines 25-38 (the connector block shape + delimiter - identical for an agent)
    - data/connector-registry.json (existing tuples) PLUS the Plan 01/02/03 command tuples (avoid collision across the whole phase)
    - LARRYREACH-CONNECTOR-AUDIT.md rows: research-agent (4), persona-analyst (13), brain-query (14), grading (18), investor (19), opportunity-scanner (26), reverse-salient-agent (39)
    - docs/CONNECTOR-CONTRACT.md sections 2/3/7
  </read_first>
  <action>
    Add an additive connector: block ("# --- Phase 144.1 connector frontmatter ---") to each
    of the 7 agents, transcribing audit values EXACTLY with a UNIQUE sub_mode so each
    (sensor,reach,sub_mode) tuple is distinct across the WHOLE phase (commands + agents +
    the 8 existing). Agents share frameworks with their command siblings (e.g. grading agent
    and /mos:grade both key PWS Triple Validation Compass) - the distinct sub_mode keeps the
    tuple unique. Per the audit:
    - grading (agent): brain_consult, SENS-07, "PWS Triple Validation Compass", hold, fileEvidenceWithReadback, surface F.0, sub_mode grading-agent
    - investor (agent): contradiction, SENS-07, framework null, pull_back, filing: none, NO surface, sub_mode investor-agent (agents/investor.md declares no frameworks: block; CRITICAL WFL-01 RULE: firesCommand fires whenever filing != none OR a surface is set, so the ONLY legal null-framework config is filing: none + no surface; the audit fileEvidence intent is a noted SUMMARY deviation)
    - deep-grade has no agent; skip
    - research (agent): deep_research, SENS-04/06, "Hypothesis-Driven Problem Solving", push_forward, fileEvidenceWithReadback, sub_mode research-agent, plan_gated: true, web_scope: green (it is a deep_research reach like /mos:research)
    - opportunity-scanner (agent): deep_research, SENS-04/06, framework null, push_forward, filing: none, NO surface, sub_mode opportunity-scan (no frameworks: block; null framework -> filing none + no surface to pass WFL-01; audit fileEvidence intent noted as a SUMMARY deviation)
    - persona-analyst (agent): context_block, SENS-06, "Six Thinking Hats", hold, memory_event_only, sub_mode persona-hats
    - brain-query (agent): brain_consult, SENS-03, framework null, hold, none (audit row: filing none) - filing: none means WFL-01 does NOT fire, so a null framework is legal here, sub_mode brain-query
    - reverse-salient-agent: context_block, SENS-02, "Reverse Salient Analysis", pull_back, memory_event_only (mirror find-bottlenecks but distinct sub_mode), surface F.0, sub_mode rs-agent-finding
    hierarchy_rank: continue the phase-wide distinct integer sequence; web_scope null except
    research (green); plan_gated false except research (true).
    Do NOT add a connector to framework-runner or larry-extended (Task 1 classified them
    out-of-spine; Plan 08 allow-lists them). No em-dashes.
  </action>
  <acceptance_criteria>
    - Each of the 7 agents has exactly one connects_to_spine: true line; framework-runner and larry-extended have ZERO
    - All reach_ids frozen-5; all postures frozen-3
    - Every (sensor,reach,sub_mode) tuple is distinct across commands + agents + the 8 existing (verified green by --check in Plan 08)
    - brain-query, investor, and opportunity-scanner null-framework rows all use filing: none + NO surface (the only legal null-framework config under WFL-01)
    - grep -rc '—' across the 7 agent files returns 0
  </acceptance_criteria>
  <verify>
    <automated>for a in grading investor opportunity-scanner research persona-analyst brain-query reverse-salient-agent; do c=$(grep -c 'connects_to_spine: true' agents/$a.md); [ "$c" = "1" ] || { echo "FAIL $a ($c)"; exit 1; }; done; [ "$(grep -c 'connects_to_spine: true' agents/framework-runner.md)" = "0" ] && [ "$(grep -c 'connects_to_spine: true' agents/larry-extended.md)" = "0" ] && echo "OK 7 wired, 2 out-of-spine"</automated>
  </verify>
  <done>7 agents wired with unique tuples; 2 explicitly unwired; em-dash free; discovered by the Plan-04 walk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent frontmatter -> generated registry | additive YAML; generic handles only |
| agent connector framework -> resolver | commandsForFramework; zero Brain calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-13 | Tampering | agent connector colliding with its command sibling's tuple | mitigate | distinct sub_mode per agent (e.g. grading-agent vs grade-compass); --check tuple validation (Plan 08) |
| T-144.1-14 | Spoofing | null-framework agent connector firing WFL-01 | mitigate | brain-query uses filing: none; investor/opportunity-scanner use memory_event_only + no surface |
| T-144.1-15 | Information disclosure | an agent connector leaking user content via a free-text field | mitigate | only the 11 schema keys; test-connector-part8-boundary ALLOWED fields gate |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | no package installs; additive frontmatter only |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check green after Plan 08 regen (agent connectors now present as source: agent).
- The registry connectors[] includes source: agent entries with surface "agent:<base>".
- framework-runner + larry-extended carry no connector (allow-listed in Plan 08).
</verification>

<success_criteria>
7 spine-eligible agents wired with unique tuples; 2 execution/routing agents explicitly
out-of-spine with Phase-144-ownership reasons; zero type changes; zero em-dashes.
</success_criteria>

<output>
Create `.planning/phases/144.1-connector-retrofit-sweep/144.1-05-SUMMARY.md` when done.
</output>
