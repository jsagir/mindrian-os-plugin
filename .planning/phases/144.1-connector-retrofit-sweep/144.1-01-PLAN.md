---
phase: 144.1-connector-retrofit-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/deep-grade.md
  - commands/build-thesis.md
  - commands/mva-brief.md
  - commands/mva-option.md
  - commands/challenge-assumptions.md
  - commands/opportunities.md
  - commands/grade.md
  - commands/analyze-needs.md
  - commands/jtbd.md
  - commands/auto-explore.md
autonomous: true
requirements: [RETRO-01, RETRO-05]
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 8]
must_haves:
  truths:
    - "Each Tier B PUSH-family COMMAND (10 commands; the 3 Tier B agents are in Plan 05) declares a valid connector: block (connects_to_spine: true)"
    - "Every declared connector picks a unique (sensor,reach,sub_mode) tuple vs the 8 existing and vs each other"
    - "challenge-assumptions declares frameworks: [Red Teaming] so its connector framework resolves via WFL-01"
    - "deep-grade's Write-tool gap is closed (allowed-tools includes the filing path)"
    - "jtbd gains its connector while keeping its existing frameworks: [Jobs to Be Done (JTBD)]"
  artifacts:
    - path: "commands/build-thesis.md"
      provides: "context_block connector (SENS-07/06, PWS Value Proposition)"
      contains: "connects_to_spine: true"
    - path: "commands/challenge-assumptions.md"
      provides: "Red Teaming frameworks: block + contradiction connector"
      contains: "Red Teaming"
  key_links:
    - from: "commands/*.md connector blocks"
      to: "data/connector-registry.json (regenerated in Plan 08)"
      via: "scripts/build-connector-registry.cjs parseConnectorFrontmatter"
      pattern: "connects_to_spine:\\s*true"
---

<objective>
Land connector: frontmatter on the Tier B PUSH families + gate-approach + first-touch
COMMANDS (audit Section 3 items 18-30; the grading/investor/opportunity-scanner AGENTS in that tier are wired in Plan 05), plus the RETRO-05 cleanups that those rows
surfaced (deep-grade Write gap, challenge-assumptions lens resolver key, jtbd already
has frameworks so just add its connector).

Purpose: these are the highest-priority reach-surfacing methodology commands. Without
their connector: blocks the orchestrator cannot route a fired sensor to them, so the
spine fires into the void on the PUSH families.

Output: 10 command .md files each carrying an additive connector: block, validating
green against the SHIPPED CONN-03 --check (run in Plan 08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/CONNECTOR-CONTRACT.md
@scripts/build-connector-registry.cjs
@commands/research.md
@data/connector-registry.json
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- Additive connector: blocks on 10 Tier B commands (deep-grade, build-thesis, mva-brief,
  mva-option, challenge-assumptions, opportunities, grade, analyze-needs, jtbd, auto-explore).
  The grading + investor + opportunity-scanner AGENTS of Tier B are wired in Plan 05.
- A frameworks: [Red Teaming] block on challenge-assumptions (RETRO-05).
- A closed Write-tool gap on deep-grade (RETRO-05).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Cleanups that gate the connectors (RETRO-05 prereqs in this batch)</name>
  <read_first>
    - commands/challenge-assumptions.md (full frontmatter; today has NO frameworks: block)
    - commands/deep-grade.md (allowed-tools / Write path; frameworks: [PWS Triple Validation Compass] present)
    - commands/jtbd.md (already carries frameworks: [Jobs to Be Done (JTBD)]; missing kind:)
    - docs/CONNECTOR-CONTRACT.md section 4 (additive-degrade rule)
    - LARRYREACH-CONNECTOR-AUDIT.md items 20, 24, 29
  </read_first>
  <action>
    In commands/challenge-assumptions.md add a Phase-122 methodology frontmatter block
    declaring kind: methodology and frameworks: ["Red Teaming"] (the lens resolver key
    confirmed in the audit item 24). "Red Teaming" is already in data/framework-names.json;
    declaring it on this command makes commandsForFramework("Red Teaming") resolve to a
    command (today it returns EMPTY because no command declares it), which is the WFL-01
    precondition for this command's connector in Task 2. Also add a produces: glob matching
    where the command writes (room/**/challenge/* or the command's existing output path -
    confirm by reading the command body).
    In commands/deep-grade.md close the Write-tool gap: ensure allowed-tools lists the
    filing path the connector's filing: fileEvidenceWithReadback requires (the command must
    be able to write its deep-grade artifact). Do NOT introduce a bare Write wildcard - match
    the deep-grade artifact write to the established fileEvidenceWithReadback / wireAccept
    idiom referenced in skills/intelligence-orchestrator/SKILL.md.
    In commands/jtbd.md add kind: methodology to its existing Phase-122 frontmatter (it
    already has frameworks: [Jobs to Be Done (JTBD)]); leave the frameworks: value untouched.
    No em-dashes anywhere; use the unicode escape only inside detector strings if needed.
  </action>
  <acceptance_criteria>
    - grep -v '^#' commands/challenge-assumptions.md | grep -c 'frameworks:.*Red Teaming' returns >= 1
    - node -e 'const{commandsForFramework}=require("./lib/workflow/command-resolver.cjs");process.exit(commandsForFramework("Red Teaming").length>0?0:1)' exits 0 AFTER the build-command-registry regen step in Plan 08 (note: resolver reads command-registry.json which Plan 08 regenerates; this task only adds the declaration)
    - commands/deep-grade.md allowed-tools enables the artifact write path (no bare Write wildcard added)
    - grep -v '^#' commands/jtbd.md | grep -c 'kind: methodology' returns 1
    - grep -rc '—' commands/challenge-assumptions.md commands/deep-grade.md commands/jtbd.md returns 0 em-dashes in prose
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' commands/challenge-assumptions.md | grep -q 'Red Teaming' && grep -v '^#' commands/jtbd.md | grep -q 'kind: methodology' && echo OK</automated>
  </verify>
  <done>challenge-assumptions declares Red Teaming; deep-grade can write its artifact; jtbd has kind:.</done>
</task>

<task type="auto">
  <name>Task 2: Tier B COMMAND connector blocks (items 20-30, commands only) with unique tuples</name>
  <read_first>
    - commands/research.md lines 25-38 (the canonical Phase 143.3 connector block shape + delimiter)
    - data/connector-registry.json (the 8 EXISTING (sensor,reach,sub_mode) tuples that are TAKEN)
    - LARRYREACH-CONNECTOR-AUDIT.md Section 3 Tier B table (items 18-30, each row: reach_id, sensor(s), framework, posture, filing)
    - docs/CONNECTOR-CONTRACT.md section 2 (the 11 sub-keys) + section 3 (frozen banks) + section 7 (delimiter)
  </read_first>
  <action>
    Add an additive connector: block (delimited "# --- Phase 144.1 connector frontmatter ---")
    to each of these commands, transcribing the audit's pre-proposed values EXACTLY. Each
    block sets connects_to_spine: true and a UNIQUE sub_mode (the render label) so its
    (sensor,reach,sub_mode) tuple never collides with the 8 existing connectors or with a
    sibling in this batch. Use the audit framework EXACTLY (it must equal the command's
    existing frameworks: value where one exists). CRITICAL WFL-01 RULE (confirmed against build-connector-registry.cjs lines 424-436):
    the firesCommand gate is true whenever filing != 'none' OR a decision_surface is set. So
    memory_event_only DOES fire WFL-01. A null-framework connector therefore MUST use
    filing: none AND no surface, or the --check FAILS. These dash-placeholder commands
    (investor, mva-brief, mva-option, opportunities) declare NO frameworks: block (verified),
    so do NOT invent one (no fabrication). For each: set framework: null, filing: none, NO
    surface. The audit's memory_event_only INTENT is recorded as a noted deviation in the
    SUMMARY (the surface still emits its memory_event through its existing code path; the
    connector's filing field stays none to pass the shipped --check). Concretely per the audit:
    - deep-grade: brain_consult, SENS-06/07, "PWS Triple Validation Compass", hold, fileEvidenceWithReadback, sub_mode deep-grade-compass
    - build-thesis: context_block, SENS-07/06, "PWS Value Proposition", hold, fileEvidenceWithReadback, sub_mode thesis-build
    - mva-brief: deep_research, SENS-01, framework null, push_forward, filing: none, NO surface, sub_mode mva-brief (null framework -> filing none to pass WFL-01; memory_event intent noted in SUMMARY)
    - mva-option: context_block, SENS-05, framework null, push_forward, filing: none, NO surface, sub_mode mva-option (null framework -> filing none)
    - challenge-assumptions: contradiction, SENS-06, "Red Teaming", pull_back, fileEvidenceWithReadback, sub_mode red-team
    - opportunities: context_block, SENS-04/06, framework null, push_forward, filing: none, NO surface, sub_mode opportunities (opportunities declares no frameworks: block; null framework -> filing none to pass WFL-01; the audit fileEvidence intent is a noted SUMMARY deviation)
    - grade: brain_consult, SENS-06/07, "PWS Triple Validation Compass", hold, fileEvidenceWithReadback, sub_mode grade-compass
    - analyze-needs: context_block, SENS-05/06, "Jobs to Be Done (JTBD)", push_forward, fileEvidenceWithReadback, sub_mode needs-jtbd
    - jtbd: context_block, SENS-05, "Jobs to Be Done (JTBD)", hold, memory_event_only, sub_mode jtbd-infer (use its real framework; memory_event_only fires WFL-01 and resolves OK since jtbd declares JTBD)
    - auto-explore: context_block, SENS-01, "Domain Selection", push_forward, fileEvidenceWithReadback, sub_mode auto-explore (audit says framework explore-domains which is a slug not a framework name; use "Domain Selection" - the real framework explore-domains declares - so WFL-01 resolves)
    hierarchy_rank: assign integers continuing past the Tier A ranks (start at 8, increment)
    so ties arbitrate deterministically; plan_gated: false except mva-brief (deep_research)
    which stays false too (only the sanctioned rs-fetch/research deep_research escalation
    sets plan_gated: true - confirm against the contract section 2); web_scope: null on all
    (none reach the web in this batch).
    Set surface: F.x only where the audit row names one (grading F.0, investor F.0).
  </action>
  <acceptance_criteria>
    - Each of the 10 commands has exactly one "connects_to_spine: true" line: grep -c returns 1 per file
    - Every connector reach_id is one of: context_block contradiction cross_room brain_consult deep_research (no 6th)
    - Every connector posture is one of: push_forward hold pull_back (no 4th)
    - No two connectors in this batch share a (sensor,reach,sub_mode) tuple, and none collides with the 8 in data/connector-registry.json (verified green by --check in Plan 08)
    - Every connector that sets filing != none AND/OR a surface uses a framework that commandsForFramework() resolves to >=1 command (null framework allowed ONLY with filing: memory_event_only and no surface)
    - grep -rc '—' on all 10 files returns 0 em-dashes in prose
  </acceptance_criteria>
  <verify>
    <automated>for f in deep-grade build-thesis mva-brief mva-option challenge-assumptions opportunities grade analyze-needs jtbd auto-explore; do c=$(grep -c 'connects_to_spine: true' commands/$f.md); [ "$c" = "1" ] || { echo "FAIL $f ($c)"; exit 1; }; done; echo "OK all 10 Tier B command connectors present"</automated>
  </verify>
  <done>All 13 Tier B commands carry a valid, unique-tuple connector: block; frozen banks honored; em-dash free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command frontmatter -> generated registry | additive YAML the generator parses; must carry only generic handles |
| connector framework -> Brain | the ONLY Brain-bound value is a generic :Framework name (build-time --refresh-names) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-01 | Tampering | connector: block carrying a non-frozen reach_id/posture | mitigate | --check (Plan 08) validates reach_id in frozen 5 + posture in frozen 3; hard exit 1 |
| T-144.1-02 | Information disclosure | a free-text body field smuggled onto a connector | mitigate | test-connector-part8-boundary.cjs ALLOWED_CONNECTOR_FIELDS allowlist; generator only emits the 11 schema keys |
| T-144.1-03 | Spoofing | a fake/typo framework name | mitigate | WFL-01 commandsForFramework resolution + framework-names.json allowlist; --check fails |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | this plan adds NO package installs; additive frontmatter only |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check stays green after Plan 08 regenerates the registry (this plan's blocks are inputs; --check runs in Plan 08).
- No reach_id outside the frozen 5; no posture outside the frozen 3; no (sensor,reach,sub_mode) collision.
</verification>

<success_criteria>
13 Tier B commands carry additive connector: blocks with unique tuples; challenge-assumptions
declares Red Teaming; deep-grade can file; jtbd has kind:. Zero em-dashes. Zero type changes.
</success_criteria>

<output>
Create `.planning/phases/144.1-connector-retrofit-sweep/144.1-01-SUMMARY.md` when done.
</output>
