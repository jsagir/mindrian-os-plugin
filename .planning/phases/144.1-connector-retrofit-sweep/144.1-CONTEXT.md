---
phase: 144.1
phase_name: Connector Retrofit Sweep (Tiers B/C/D)
gathered: 2026-06-07
status: Ready for planning (a NEW session runs /gsd:plan-phase 144.1, AFTER 143.3 + 144)
source: Derived from the LARRYREACH connector audit (.planning/research/LARRYREACH-CONNECTOR-AUDIT.md, Workflow wf_023417b2-da6, 140 components). 143.3 CONN-04 lands Tier A; this phase lands the remaining 51 connectors so 146 can certify a FULLY WIRED MindrianOS.
managed_under: MindrianOS-Plugin / decisions
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 8]
requirements: [RETRO-01, RETRO-02, RETRO-03, RETRO-04, RETRO-05, RETRO-06, RETRO-07]
depends_on: [143.3 (the connector contract + generator + empty registry), 144 (the engine repoint + the 4 blocking sensor hooks + the reverse-salient-agent body), 122 (command-resolver / framework-names.json)]
blocks: [146 (the fully-wired acceptance gate cannot pass until every spine-eligible surface declares its connector)]
research: .planning/research/LARRYREACH-CONNECTOR-AUDIT.md
---

# Phase 144.1: Connector Retrofit Sweep (Tiers B/C/D) - Context

## The one-paragraph why

The LARRYREACH connector audit (140 components) found the spine fully specified and 100% unwired: zero connectors exist, `connector-registry.json` is empty. 143.3 CONN-04 retrofits the 17-item algorithmic cohort (Tier A). Phase 144 repoints the routing surfaces and fixes the 15 critical blockers. This phase finishes the job: it lands the remaining **51 connectors** (audit Tiers B, C, D) so EVERY spine-eligible command, agent, and hook declares its `connector:` frontmatter and the registry is fully populated. Without this sweep, 146 can only prove the loop fires on a handful of surfaces; with it, 146 certifies a fully wired MindrianOS. The 72 correctly-out-of-spine surfaces (routing / read-only / output / system / HOW-layer skills / guard hooks / deprecated stubs) are explicitly excluded - this phase does NOT touch them.

## Spec (the audit is the research)

Read `.planning/research/LARRYREACH-CONNECTOR-AUDIT.md` - Sections 3 (connector retrofit backlog, Tiers B/C/D, items 18-68 with proposed connector blocks), 4 (no-connector exclusions), 6 (counts), 7 (phasing). Every connector block here is pre-proposed there (reach_id from the frozen 5, sensor_triggers, framework exact key, posture from the frozen 3, filing, surface).

## Locked decisions (navigator, 2026-06-07)

1. **Own phase, gated after 143.3 + 144, blocks 146.** 143.3 = Tier A; 144 = engine + critical blockers; 144.1 = the rest; 146 = certify fully wired.
2. **0 type changes** (audit confirmed all 140 correctly typed) - this phase only ADDS connector frontmatter + routes filing; it never re-types a surface.
3. **Never mint a 6th reach / 4th posture.** Every connector validates against the frozen vocabulary in `larry-personality`; `--check` (143.3 CONN-03) must stay green after each batch.
4. **All filing surfaces route through `fileEvidenceWithReadback`** (or `memory_event_only` / `none` per the audit row) - no bare `Write`.
5. **Navigator directive (2026-06-07): exhaustive coverage of ALL 91 commands + ALL 12 skills + ALL 9 agents - agents are part of this rewiring.** No surface is silently skipped: every one of the 112 surfaces is either WIRED (declares a valid `connector:`) or EXPLICITLY classified out-of-spine with a one-line reason (the audit Section 4 buckets stay valid for genuine non-reaching surfaces: routing / read-only / output / system / guard / deprecated). The coverage assertion is a count gate, not a sample. **Agents require a generator amendment first (RETRO-07): `scripts/build-connector-registry.cjs` currently walks `commands/*.md` + `skills/*/SKILL.md` ONLY - it does NOT walk `agents/*.md`, so agents are structurally unable to join the spine until the walk is extended.**

## Requirements

- **RETRO-01 (Tier B - PUSH families + gate-approach + first-touch, audit items 18-30):** connector frontmatter on grading, investor, deep-grade, build-thesis, mva-brief, mva-option, challenge-assumptions, opportunities, opportunity-scanner, grade, analyze-needs, jtbd, auto-explore. Register + `--check` green.
- **RETRO-02 (Tier C - methodologies / state / rest, audit items 31-58):** connector frontmatter on the remaining methodology + state commands (analyze-systems, compare-ventures, leadership, mos-reason, dial-memory-refresh, new-project, mullins, validate, value-proposition, analyze-timing, dominant-designs, build-knowledge, beautiful-question, explore-*, scout, operator, funding, reanalyze, macro-trends, map-unknowns, lean-canvas, root-cause, user-needs, structure-argument, systems-thinking, scenario-plan).
- **RETRO-03 (Tier D - sensor-firing hooks, audit items 59-68):** connector frontmatter / sensor wiring on the hooks NOT already fixed in 144 (auto-explore-drain, check-pending-breakthrough, jtbd-update->sensor-jtbd-reweight, on-agent-complete, on-file-changed, memory-completion-detector, mva-detect, brain-derivation-drain). Inherit the post-write fix from 144.
- **RETRO-04 (filing sweep):** every connector with `filing: fileEvidenceWithReadback` actually routes its writes through it (no bare `Write`); cascade fires; memory_event emitted.
- **RETRO-05 (cleanups the audit surfaced - do as part of the owning rows):** add `leadership`'s missing Phase-122 frontmatter (frameworks/kind/produces) BEFORE its connector; resolve `value-proposition` name/file drift (validate-proposition vs value-proposition); close the `deep-grade` Write-tool gap; pick `structure-argument`'s primary framework key (Pyramid Principle) and confirm `challenge-assumptions`' lens resolver key (Red Teaming).
- **RETRO-06 (registry complete + CI):** `connector-registry.json` populated for all spine-eligible surfaces; `build-connector-registry.cjs --check` green (every reach_id in 5, every framework resolves via `commandsForFramework()`, every posture in 3, no (sensor,reach,sub_mode) collisions); a run-all suite + Part-8 boundary sweep over the new blocks.
- **RETRO-07 (agents into the spine + exhaustive-coverage gate - navigator directive):** (a) extend `scripts/build-connector-registry.cjs` source-discovery to ALSO walk `agents/*.md` (today it walks `commands/` + `skills/` only - the `kind: 'agent'` records mirror the `kind: 'command'` / `kind: 'skill'` shape; the nested `connector:` parser is unchanged); (b) declare `connector:` frontmatter on every spine-eligible agent of the 9 (audit-classify each: a proactive reach-surfacing agent like investor / opportunity-scanner / research / reverse-salient-agent / persona-analyst likely wires; a pure execution/util agent stays explicitly out-of-spine with a reason); (c) the **exhaustive-coverage count gate**: a test asserts all 91 commands + 12 skills + 9 agents (112 total) are each either in `connector-registry.json` OR on an explicit out-of-spine allow-list with a reason - zero silently-unclassified surfaces. `--check` + Part-8 sweep stay green.

## Non-goals
- WIRING the genuine out-of-spine surfaces (audit Section 4: routing / read-only / output / system / guard / deprecated) - they get NO connector. But per locked decision 5 they are no longer silently skipped: each is EXPLICITLY recorded on the out-of-spine allow-list with a reason (the RETRO-07 count gate enforces this), so the 112-surface coverage is provably complete.
- Any type change (0 needed).
- The engine repoint + the 4 critical hooks + the reverse-salient-agent body (those are Phase 144).
- A 6th reach or new selector (frozen).

## Definition of done (hands the baton to 146)
The generator walks `commands/` + `skills/` + `agents/`; every spine-eligible surface (across all 91 commands + 12 skills + 9 agents) declares a valid `connector:`; the registry is fully populated; the RETRO-07 exhaustive-coverage count gate proves all 112 surfaces are wired-or-explicitly-excluded (zero silently-unclassified); `--check` + the Part-8 sweep are green. At this point 146's `doctor --acceptance` can assert the loop fires end-to-end across the full surface set, not a sample - a FULLY WIRED MindrianOS that includes the agents.

## UI hint
No new UI. Connectors render through the existing Shape-F dial-TUI (143.1).
