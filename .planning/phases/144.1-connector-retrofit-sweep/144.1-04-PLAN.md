---
phase: 144.1-connector-retrofit-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-connector-registry.cjs
  - tests/test-connector-agents-walk.cjs
autonomous: true
requirements: [RETRO-07]
canon_parts: [Part 7, Part 8]
must_haves:
  truths:
    - "scripts/build-connector-registry.cjs listSourceFiles() ALSO walks agents/*.md (kind: 'agent')"
    - "agent records mirror the command/skill record shape; the nested connector: parser is UNCHANGED"
    - "the one-connector-per-file contract is preserved; the agents walk is deterministic (sorted)"
    - "the generator's only Brain touch stays the build-time --refresh-names read; the agents walk is purely local"
  artifacts:
    - path: "scripts/build-connector-registry.cjs"
      provides: "agents/*.md source discovery (kind: 'agent')"
      contains: "AGENTS_DIR"
    - path: "tests/test-connector-agents-walk.cjs"
      provides: "asserts the agents walk discovers agent .md files and emits agent: surface ids"
      min_lines: 30
  key_links:
    - from: "listSourceFiles()"
      to: "agents/*.md"
      via: "fs.readdirSync(AGENTS_DIR)"
      pattern: "AGENTS_DIR"
---

<objective>
Extend scripts/build-connector-registry.cjs source-discovery to ALSO walk agents/*.md so
agents can structurally join the spine (RETRO-07a, the navigator directive). Today
listSourceFiles() walks commands/ + skills/ ONLY; an agent cannot be registered no matter
what connector: block it declares. This plan adds the agents walk and the test that proves it.

Purpose: this is the structural prerequisite for the agent connectors in Plan 05. The
generator amendment MUST land before any agent declares a connector, or those declarations
are silently dropped (connects_to_spine filter never sees them).

Output: a generator that walks three source dirs (commands, skills, agents), emitting
kind: 'agent' records with surface id "agent:<base>", plus a dedicated walk test. The
parseConnectorFrontmatter parser is UNCHANGED (the nested connector: shape is identical
across all three source kinds).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@scripts/build-connector-registry.cjs
@docs/CONNECTOR-CONTRACT.md
@tests/test-connector-part8-boundary.cjs
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- scripts/build-connector-registry.cjs extended: AGENTS_DIR constant + an agents leg in
  listSourceFiles() emitting { kind: 'agent', file, base }, mirroring the commands leg.
- The surface-id branch in buildRegistry() extended so kind 'agent' -> "agent:" + base
  (mirroring "/mos:" for command and "skill:" for skill).
- tests/test-connector-agents-walk.cjs: asserts listSourceFiles includes agents and the
  registry can carry an agent: surface.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend listSourceFiles() + surface-id mapping to walk agents/*.md</name>
  <read_first>
    - scripts/build-connector-registry.cjs lines 69-72 (DIR constants), 247-278 (listSourceFiles), 308-309 (surface-id branch), 281-364 (buildRegistry)
    - The commands leg (lines 250-258) and skills leg (lines 260-275) - the EXACT pattern to mirror
    - docs/CONNECTOR-CONTRACT.md section 1 (the same commands/*.md and skills/*/SKILL.md files gain one nested key; agents join the same way)
  </read_first>
  <action>
    Add AGENTS_DIR = path.join(REPO_ROOT, 'agents') beside COMMANDS_DIR / SKILLS_DIR.
    In listSourceFiles() add a THIRD leg after the skills leg: readdirSync(AGENTS_DIR),
    filter .md, sort (deterministic), and push { kind: 'agent', file: path.join(AGENTS_DIR, f),
    base: f.replace(/\.md$/, '') } - byte-mirroring the commands leg, including the try/catch
    that yields [] when the dir is absent. Keep the existing commands + skills legs untouched
    and keep the overall sorted order stable (commands, then skills, then agents) so the
    registry is byte-stable across machines.
    In buildRegistry() extend the surfaceName branch (currently:
    src.kind === 'command' ? '/mos:' + base : 'skill:' + base) to a three-way:
    command -> '/mos:' + base, skill -> 'skill:' + base, agent -> 'agent:' + base.
    Do NOT touch parseConnectorFrontmatter (the nested connector: block is identical in an
    agent .md). Do NOT touch the connects_to_spine filter, the validations, the sensor_index /
    framework_index build, or the --refresh-names branch. Preserve the one-connector-per-file
    contract: each agent file yields at most one connector record, exactly like a command.
    No em-dashes; if a comment needs the dash glyph use a hyphen.
  </action>
  <acceptance_criteria>
    - grep -c 'AGENTS_DIR' scripts/build-connector-registry.cjs returns >= 2 (declaration + use)
    - node -e 'const m=require("./scripts/build-connector-registry.cjs"); const r=m.buildRegistry(); process.exit(0)' runs without throwing
    - The surfaceName branch maps kind agent to an "agent:" prefix (grep "'agent:'" scripts/build-connector-registry.cjs returns >= 1)
    - parseConnectorFrontmatter is byte-unchanged (git diff shows no edits inside the function body lines 140-213)
    - node scripts/build-connector-registry.cjs --check still exits 0 with the CURRENT registry (no agent connectors declared yet, so the registry is unchanged; the walk just finds zero agent connectors)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'AGENTS_DIR' scripts/build-connector-registry.cjs | grep -qE '[2-9]' && node -e 'require("./scripts/build-connector-registry.cjs").buildRegistry()' && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <done>The generator walks agents/*.md, emits agent: surface ids, and --check stays green with zero agent connectors declared.</done>
</task>

<task type="auto">
  <name>Task 2: Walk test proving agents are discoverable</name>
  <read_first>
    - tests/test-connector-part8-boundary.cjs (the CJS test idiom: ok()/fail() counters, exit code)
    - scripts/build-connector-registry.cjs module.exports (buildRegistry, parseConnectorFrontmatter)
    - tests/run-all-1433.sh CJS_SUITES array (the registration pattern Plan 06 will mirror for run-all-1441.sh)
  </read_first>
  <action>
    Create tests/test-connector-agents-walk.cjs mirroring the part8-boundary test idiom
    (ok/fail counters, exit non-zero on any failure). Assert:
    (1) the generator module loads and exposes buildRegistry;
    (2) a temp agent fixture with a valid connector: block IS discovered - write a fixture
    .md into a temp dir is overkill; instead assert that listSourceFiles (if exported) OR a
    direct readdirSync of agents/ shows >=1 .md and that parseConnectorFrontmatter on a
    crafted agent-shaped string with a nested connector: returns out.connector with
    connects_to_spine: true (proving the SAME parser handles an agent file);
    (3) buildRegistry() does not throw and any agent connectors it finds carry an "agent:"
    surface prefix (assert: every connector whose source === 'agent' has surface starting
    "agent:"). If no agent connectors exist yet (Plan 05 not run), this assertion is
    vacuously true - that is acceptable and documented in a comment.
    No em-dashes. Keep the test pure (no Brain, no network).
  </action>
  <acceptance_criteria>
    - node tests/test-connector-agents-walk.cjs exits 0
    - The test asserts parseConnectorFrontmatter handles an agent-shaped connector block
    - The test asserts source==='agent' connectors carry an 'agent:' surface prefix
    - grep -c '—' tests/test-connector-agents-walk.cjs returns 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-connector-agents-walk.cjs</automated>
  </verify>
  <done>The agents-walk test passes and locks the agent discovery + agent: surface-prefix contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agents/*.md -> generator -> registry | new local file walk; must stay local (no Brain) |
| generator -> Brain | unchanged: only the build-time --refresh-names read touches the Brain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-10 | Elevation of privilege | agents walk accidentally importing brain-client at top level | mitigate | test-connector-part8-boundary CHECK 1 still asserts brain-client require sits only inside refreshNames; this plan adds no top-level require |
| T-144.1-11 | Tampering | non-deterministic agent ordering breaking byte-stable registry | mitigate | the agents leg sorts before push, mirroring the commands leg |
| T-144.1-12 | Tampering | parser drift breaking the one-connector-per-file contract | mitigate | parseConnectorFrontmatter is byte-unchanged; acceptance asserts no edits in its body |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | no package installs; pure Node built-ins |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check exits 0 (registry unchanged; zero agent connectors yet).
- node tests/test-connector-agents-walk.cjs exits 0.
- test-connector-part8-boundary.cjs CHECK 1 (brain-client locality) still passes.
</verification>

<success_criteria>
The generator walks commands + skills + agents; agent surfaces map to "agent:<base>"; the
nested parser and the one-connector-per-file contract are preserved; the Brain touch is
unchanged. Plan 05 can now register agent connectors.
</success_criteria>

<output>
Create `.planning/phases/144.1-connector-retrofit-sweep/144.1-04-SUMMARY.md` when done.
</output>
