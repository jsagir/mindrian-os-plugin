---
phase: 144.1-connector-retrofit-sweep
plan: 07
type: execute
wave: 2
depends_on: ["144.1-01", "144.1-02", "144.1-03", "144.1-05"]
files_modified:
  - tests/test-connector-filing-sweep.cjs
autonomous: true
requirements: [RETRO-04]
canon_parts: [Part 4, Part 8, Part 9]
must_haves:
  truths:
    - "Every connector declaring filing: fileEvidenceWithReadback actually routes its writes through fileEvidenceWithReadback (no bare Write)"
    - "Cascade fires and a memory_event is emitted on the filing path for those surfaces"
    - "A single assertion test enumerates all fileEvidenceWithReadback connectors and proves the routing"
  artifacts:
    - path: "tests/test-connector-filing-sweep.cjs"
      provides: "enumerates fileEvidenceWithReadback connectors from the registry and asserts each surface routes filing through the readback chokepoint (no bare Write)"
      min_lines: 50
  key_links:
    - from: "data/connector-registry.json (filing: fileEvidenceWithReadback)"
      to: "the owning command/agent .md filing path"
      via: "fileEvidenceWithReadback / wireAccept"
      pattern: "fileEvidenceWithReadback"
---

<objective>
The filing sweep (RETRO-04): prove that every connector that DECLARES filing:
fileEvidenceWithReadback actually ROUTES its artifact writes through it - no bare Write that
bypasses the cascade and the memory_event. The deliverable is a single enumeration-driven
assertion test plus any remediation it surfaces.

Purpose: a connector that declares fileEvidenceWithReadback but whose command body still
does a bare Write breaks the moat (cascade never fires, no artifact_filed memory_event, the
engine never sees the write). This sweep is the contract that the declaration matches the
behavior.

Output: tests/test-connector-filing-sweep.cjs (registry-driven; auto-covers every current
AND future fileEvidenceWithReadback connector) + any bare-Write remediation in the owning
surface files it surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/CONNECTOR-CONTRACT.md
@skills/intelligence-orchestrator/SKILL.md
@tests/test-connector-part8-boundary.cjs
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- tests/test-connector-filing-sweep.cjs: the registry-driven filing-routing assertion.
- Any bare-Write -> fileEvidenceWithReadback remediation the sweep surfaces (in the owning
  command/agent files; recorded per-surface in the SUMMARY).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Registry-driven filing-sweep assertion test</name>
  <read_first>
    - skills/intelligence-orchestrator/SKILL.md (the fileEvidenceWithReadback + wireAccept idiom + surfaceFileEvidenceResult FILEVAL remind)
    - lib/core/navigation/file-evidence-readback.cjs (the fileEvidenceWithReadback export) + lib/core/findings-wirer.cjs (wireAccept fallback)
    - tests/test-connector-part8-boundary.cjs (the registry-read + per-connector loop idiom)
    - data/connector-registry.json (note: regenerated in Plan 08; this test reads it live - run it AFTER Plan 08, or have it call buildRegistry() in-memory so it does not depend on the on-disk regen ordering)
  </read_first>
  <action>
    Create tests/test-connector-filing-sweep.cjs. Build the registry IN-MEMORY via
    require("../scripts/build-connector-registry.cjs").buildRegistry() (so the test does not
    depend on the on-disk regen ordering vs Plan 08). Enumerate every connector where
    filing === 'fileEvidenceWithReadback'. For each such surface, resolve its owning source
    file (command -> commands/<base>.md from "/mos:<base>"; agent -> agents/<base>.md from
    "agent:<base>"; skill -> skills/<base>/SKILL.md). Assert the owning file's body routes
    filing through the readback path: it references fileEvidenceWithReadback OR wireAccept OR
    delegates to the intelligence-orchestrator filing (the orchestrator owns the filing for
    sensor-routed surfaces). FAIL a surface that declares fileEvidenceWithReadback but does a
    bare Write to a room path without going through the readback/wireAccept/orchestrator path.
    Be precise about the bare-Write heuristic: a literal Write tool call writing room/** in
    the command body that is NOT inside the orchestrator-filing or readback idiom. Print
    per-surface PASS/FAIL and exit non-zero on any failure. No em-dashes. Pure (no Brain,
    no network).
  </action>
  <acceptance_criteria>
    - node tests/test-connector-filing-sweep.cjs exits 0 (or names the exact offending surfaces if any)
    - The test enumerates fileEvidenceWithReadback connectors from buildRegistry() (not a hardcoded list), so it auto-covers future connectors
    - The test resolves command/agent/skill surfaces to their owning files correctly
    - grep -c '—' tests/test-connector-filing-sweep.cjs returns 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-connector-filing-sweep.cjs</automated>
  </verify>
  <done>The registry-driven filing sweep passes; every fileEvidenceWithReadback connector routes its writes through the readback path.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate any bare-Write surfaces the sweep flags</name>
  <read_first>
    - The Task 1 test output (the offending surfaces, if any)
    - skills/intelligence-orchestrator/SKILL.md (the correct filing idiom to convert TO)
    - The owning command/agent file(s) the sweep flagged
  </read_first>
  <action>
    For each surface Task 1 flagged (bare Write where fileEvidenceWithReadback is declared),
    convert the filing in the owning command/agent body to route through
    fileEvidenceWithReadback (or wireAccept fallback, or delegate to the orchestrator filing),
    so cascade fires and an artifact_filed memory_event is emitted. Do NOT change the
    connector's filing value; align the BEHAVIOR to the DECLARATION. If the deep-grade
    Write-tool gap (closed in Plan 01 Task 1) still surfaces here, finish it. If the sweep
    flags ZERO surfaces (all already route correctly), record that in the SUMMARY and make no
    edits - that is a valid passing outcome. Add the flagged files to this plan's
    files_modified in the SUMMARY if any remediation was needed (note: editing a Wave-1 file
    here is safe because this plan depends_on those plans, so it runs strictly after them).
    No bare Write wildcard; no em-dashes.
  </action>
  <acceptance_criteria>
    - node tests/test-connector-filing-sweep.cjs exits 0 after remediation
    - Any remediated file routes filing through fileEvidenceWithReadback / wireAccept / orchestrator (no bare room/** Write)
    - The SUMMARY lists remediated files (or states "zero surfaces flagged")
    - grep -rc '—' across any remediated files returns 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-connector-filing-sweep.cjs && echo "OK filing sweep green"</automated>
  </verify>
  <done>Every fileEvidenceWithReadback connector's behavior matches its declaration; the sweep is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector declaration -> command behavior | the declared filing must match the actual write path |
| filing path -> graph | cascade + memory_event must fire (Part 4 / Part 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-19 | Repudiation | a bare Write filing with no memory_event (no audit trail) | mitigate | the sweep test FAILs any fileEvidenceWithReadback connector that bare-Writes; remediation routes through readback so artifact_filed is emitted |
| T-144.1-20 | Tampering | cascade not firing because filing bypassed the chokepoint | mitigate | route filing through fileEvidenceWithReadback / wireAccept; cascade fires |
| T-144.1-21 | Information disclosure | the filing path leaking LOCAL content to Brain | accept | filing is a LOCAL write to room/**; Part 8 boundary already enforced by the orchestrator filing path (no Brain in the write) |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | no package installs; test + frontmatter only |
</threat_model>

<verification>
- node tests/test-connector-filing-sweep.cjs exits 0 (registry-driven, auto-covers all fileEvidenceWithReadback connectors).
- Any remediated surface routes filing through the readback path; cascade + memory_event fire.
</verification>

<success_criteria>
Every connector declaring fileEvidenceWithReadback routes its writes through it; the
registry-driven sweep is green; zero bare Write that bypasses cascade; zero em-dashes.
</success_criteria>

<output>
Create `.planning/phases/144.1-connector-retrofit-sweep/144.1-07-SUMMARY.md` when done.
</output>
