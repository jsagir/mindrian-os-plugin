---
phase: 144.1-connector-retrofit-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/explore-trends.md
  - commands/explore-futures.md
  - commands/explore-domains.md
  - commands/scout.md
  - commands/operator.md
  - commands/funding.md
  - commands/reanalyze.md
  - commands/macro-trends.md
  - commands/map-unknowns.md
  - commands/lean-canvas.md
  - commands/root-cause.md
  - commands/user-needs.md
  - commands/systems-thinking.md
  - commands/scenario-plan.md
autonomous: true
requirements: [RETRO-02]
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 8]
must_haves:
  truths:
    - "Each remaining Tier C command declares a valid, unique-tuple connector: block"
    - "The no-sensor exploration commands (explore-futures, macro-trends, map-unknowns, lean-canvas) declare sensor_triggers: [] (valid empty)"
    - "reanalyze reuses the file-meeting contradiction wiring (contradiction reach)"
  artifacts:
    - path: "commands/scenario-plan.md"
      provides: "context_block connector keyed to Scenario Planning"
      contains: "Scenario Planning"
    - path: "commands/reanalyze.md"
      provides: "contradiction-reach connector"
      contains: "reach_id: contradiction"
  key_links:
    - from: "commands/*.md connector blocks"
      to: "sensor_index / framework_index in data/connector-registry.json"
      via: "build-connector-registry.cjs"
      pattern: "connects_to_spine:\\s*true"
---

<objective>
Land connector: frontmatter on the remaining Tier C methodology/state commands (audit
Section 3 items 44-58): the explore-* family, scout, operator, funding, reanalyze, and
the remaining methodology commands (macro-trends, map-unknowns, lean-canvas, root-cause,
user-needs, systems-thinking, scenario-plan).

Purpose: completes the command-surface coverage of the spine. Several of these are
"no sensor" surfaces (sensor_triggers: []) that still declare a reach so a future sensor
or a direct invocation routes correctly.

Output: 14 command .md files with additive connector: blocks, green against --check (Plan 08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/CONNECTOR-CONTRACT.md
@scripts/build-connector-registry.cjs
@commands/research.md
@data/connector-registry.json
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- 14 additive connector blocks completing the Tier C command sweep.
- Explicit sensor_triggers: [] declarations on the no-sensor exploration commands.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Tier C batch-B connector blocks (items 44-50) with unique tuples</name>
  <read_first>
    - commands/research.md lines 25-38 (connector shape + delimiter)
    - data/connector-registry.json (existing tuples - TAKEN)
    - LARRYREACH-CONNECTOR-AUDIT.md Section 3 Tier C rows 44-50
    - commands/reanalyze.md (today: name/body_shape/serves_jtbd only; audit says reuse file-meeting wiring)
    - docs/CONNECTOR-CONTRACT.md sections 2/3/7
  </read_first>
  <action>
    Add an additive connector: block to each command, transcribing audit values EXACTLY with
    a UNIQUE sub_mode per surface:
    - explore-trends: context_block, SENS-04, "S-Curve Analysis", push_forward, fileEvidenceWithReadback, sub_mode trends-scurve
    - explore-futures: context_block, sensor_triggers: [] (no sensor), "Scenario Planning", hold, fileEvidenceWithReadback, sub_mode futures-scenario
    - explore-domains: context_block, SENS-01, "Domain Selection", hold, fileEvidenceWithReadback, sub_mode domain-select
    - scout: context_block, SENS-04/07, framework null, hold, filing: none, NO surface, sub_mode scout (no frameworks: block; null framework -> filing none)
    - operator: context_block, SENS-05, framework null, hold, filing: none, NO surface, sub_mode operator (no frameworks: block; null framework -> filing none)
    - funding: context_block, SENS-07, framework null, hold, filing: none, NO surface, sub_mode funding (no frameworks: block; null framework -> filing none + no surface to pass WFL-01; audit F.2 + memory_event intent noted as a SUMMARY deviation)
    - reanalyze: contradiction, SENS-06, framework null, hold, filing: none, NO surface, sub_mode reanalyze (no frameworks: block; null framework -> filing none; reuses the file-meeting contradiction wiring at runtime; memory_event intent noted in SUMMARY)
    hierarchy_rank: continue the distinct integer sequence; plan_gated: false; web_scope: null.
    CRITICAL WFL-01 RULE: firesCommand fires whenever filing != none OR a surface is set, so memory_event_only DOES fire WFL-01. For every dash-placeholder row (no real frameworks: block - do NOT invent one) use framework: null + filing: none + NO surface; record the audit memory_event_only intent as a SUMMARY deviation.
  </action>
  <acceptance_criteria>
    - Each of the 7 commands has exactly one connects_to_spine: true line
    - reanalyze reach_id is contradiction
    - explore-futures declares sensor_triggers: [] (empty is valid)
    - All reach_ids frozen-5; all postures frozen-3; sub_modes unique vs all prior batches and the 8 existing
    - Null-framework connectors all use filing: none with NO surface (so WFL-01 does not fire)
    - grep -rc '—' across the 7 files returns 0
  </acceptance_criteria>
  <verify>
    <automated>for f in explore-trends explore-futures explore-domains scout operator funding reanalyze; do c=$(grep -c 'connects_to_spine: true' commands/$f.md); [ "$c" = "1" ] || { echo "FAIL $f ($c)"; exit 1; }; done; grep -q 'reach_id: contradiction' commands/reanalyze.md && echo "OK batch-B-1"</automated>
  </verify>
  <done>7 commands wired; reanalyze on contradiction; explore-futures empty-sensor valid.</done>
</task>

<task type="auto">
  <name>Task 2: Tier C batch-B connector blocks (items 51-58) with unique tuples</name>
  <read_first>
    - commands/research.md lines 25-38
    - data/connector-registry.json (existing tuples)
    - LARRYREACH-CONNECTOR-AUDIT.md Section 3 Tier C rows 51-58
    - commands/structure-argument.md (its primary key chosen in Plan 02 - keep consistent if it also appears here; it does NOT, structure-argument is in Plan 02)
    - docs/CONNECTOR-CONTRACT.md sections 2/3/7
  </read_first>
  <action>
    Add an additive connector: block to each command with a UNIQUE sub_mode:
    - macro-trends: context_block, sensor_triggers: [], "PEST Analysis", hold, fileEvidenceWithReadback, sub_mode macro-pest
    - map-unknowns: context_block, sensor_triggers: [], "Knowns and Unknowns Matrix Framework", hold, fileEvidenceWithReadback, sub_mode unknowns-matrix
    - lean-canvas: context_block, sensor_triggers: [], "Lean Canvas", hold, fileEvidenceWithReadback, sub_mode lean-canvas
    - root-cause: context_block, SENS-06, "Root Cause Analysis", hold, fileEvidenceWithReadback, surface F.2, sub_mode root-cause-5why
    - user-needs: context_block, SENS-06, "Jobs to Be Done (JTBD)", hold, fileEvidenceWithReadback, surface F.2, sub_mode user-needs-jtbd
    - systems-thinking: context_block, SENS-06, "Systems Thinking", hold, fileEvidenceWithReadback, surface F.2, sub_mode systems-thinking-loop
    - scenario-plan: context_block, SENS-06, "Scenario Planning", hold, fileEvidenceWithReadback, surface F.2, sub_mode scenario-plan
    Note: user-needs + analyze-needs + validate + jtbd all key "Jobs to Be Done (JTBD)" but
    differ by sub_mode (and sensor set), so their (sensor,reach,sub_mode) tuples stay
    distinct - this is the legal multi-surface-per-framework case the framework_index
    supports. Same for systems-thinking + analyze-systems on "Systems Thinking", and
    scenario-plan + explore-futures on "Scenario Planning". Confirm each tuple is distinct.
    hierarchy_rank: continue the distinct sequence; plan_gated: false; web_scope: null.
  </action>
  <acceptance_criteria>
    - Each of the 7 commands has exactly one connects_to_spine: true line
    - The four JTBD-keyed surfaces (analyze-needs, validate, jtbd, user-needs) have distinct (sensor,reach,sub_mode) tuples
    - All reach_ids frozen-5; all postures frozen-3
    - No tuple collision across the whole phase (verified green by --check in Plan 08)
    - grep -rc '—' across the 7 files returns 0
  </acceptance_criteria>
  <verify>
    <automated>for f in macro-trends map-unknowns lean-canvas root-cause user-needs systems-thinking scenario-plan; do c=$(grep -c 'connects_to_spine: true' commands/$f.md); [ "$c" = "1" ] || { echo "FAIL $f ($c)"; exit 1; }; done; echo "OK batch-B-2"</automated>
  </verify>
  <done>7 commands wired; multi-surface-per-framework tuples distinct; em-dash free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command frontmatter -> generated registry | additive YAML; generic handles only |
| multi-surface framework -> framework_index | several commands share a framework; tuples must stay distinct |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-07 | Tampering | duplicate (sensor,reach,sub_mode) across JTBD-keyed surfaces | mitigate | unique sub_mode per surface; --check tuple-collision validation (Plan 08) |
| T-144.1-08 | Tampering | non-frozen reach_id/posture | mitigate | --check frozen-bank validation |
| T-144.1-09 | Spoofing | null-framework connector firing WFL-01 with a surface | mitigate | null-framework rows use memory_event_only + NO surface so firesCommand skips |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check green after Plan 08 regen.
- framework_index shows multiple surfaces under JTBD / Systems Thinking / Scenario Planning with distinct tuples.
</verification>

<success_criteria>
The remaining 14 Tier C commands wired; no-sensor commands declare empty sensor_triggers;
multi-surface frameworks keep distinct tuples; zero type changes; zero em-dashes.
</success_criteria>

<output>
Create `.planning/phases/144.1-connector-retrofit-sweep/144.1-03-SUMMARY.md` when done.
</output>
