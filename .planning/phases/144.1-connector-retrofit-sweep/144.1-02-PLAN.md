---
phase: 144.1-connector-retrofit-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/leadership.md
  - commands/value-proposition.md
  - commands/structure-argument.md
  - commands/analyze-systems.md
  - commands/compare-ventures.md
  - commands/mos-reason.md
  - commands/dial-memory-refresh.md
  - commands/new-project.md
  - commands/mullins.md
  - commands/validate.md
  - commands/analyze-timing.md
  - commands/dominant-designs.md
  - commands/build-knowledge.md
  - commands/beautiful-question.md
autonomous: true
requirements: [RETRO-02, RETRO-05]
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 8]
must_haves:
  truths:
    - "leadership declares its missing Phase-122 frontmatter (kind/frameworks/produces) BEFORE its connector"
    - "value-proposition name/file drift is resolved (name: matches the value-proposition file or the command-registry maps it cleanly)"
    - "structure-argument's primary framework key is The Pyramid Principle (single key for the connector)"
    - "Each Tier C batch-A command declares a valid, unique-tuple connector: block"
  artifacts:
    - path: "commands/leadership.md"
      provides: "Phase-122 frontmatter + Adaptive Leadership connector"
      contains: "Adaptive Leadership"
    - path: "commands/structure-argument.md"
      provides: "context_block connector keyed to The Pyramid Principle"
      contains: "The Pyramid Principle"
  key_links:
    - from: "commands/leadership.md frameworks: [Adaptive Leadership]"
      to: "commandsForFramework(\"Adaptive Leadership\")"
      via: "command-resolver WFL-01"
      pattern: "Adaptive Leadership"
---

<objective>
Land connector: frontmatter on the first batch of Tier C methodology/state commands
(audit Section 3 items 31-43), and resolve the RETRO-05 cleanups these rows surfaced:
leadership's missing Phase-122 frontmatter (a hard prereq - without frameworks:
[Adaptive Leadership] the resolver returns EMPTY and the connector fails WFL-01),
the value-proposition name/file drift, and structure-argument's primary framework key.

Purpose: these methodology commands own the bulk of the spine's reach surfaces. Their
connectors let the orchestrator route the methodology sensors (mostly SENS-06) to the
right command on APPROVE.

Output: 14 command .md files with additive connector: blocks (+ leadership's prereq
Phase-122 block), all green against --check (Plan 08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/LARRYREACH-CONNECTOR-AUDIT.md
@docs/CONNECTOR-CONTRACT.md
@scripts/build-connector-registry.cjs
@commands/research.md
@data/connector-registry.json
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- leadership.md Phase-122 frontmatter (kind: methodology, frameworks: [Adaptive Leadership], produces:) + connector.
- value-proposition.md name/file drift resolution.
- structure-argument.md connector keyed to the single primary framework The Pyramid Principle.
- 11 further Tier C batch-A connector blocks.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RETRO-05 cleanups that gate Tier C batch-A connectors</name>
  <read_first>
    - commands/leadership.md (today: name/body_shape/serves_jtbd only - NO kind/frameworks/produces)
    - commands/value-proposition.md (name: validate-proposition drifts from the value-proposition file)
    - commands/structure-argument.md (frameworks: [The Pyramid Principle, MECE ...] - dual; pick the primary)
    - LARRYREACH-CONNECTOR-AUDIT.md items 33, 39, 56
    - docs/COMMAND-FRONTMATTER.md (the Phase-122 frontmatter contract leadership must match)
  </read_first>
  <action>
    leadership.md: add the Phase-122 methodology frontmatter block - kind: methodology,
    frameworks: ["Adaptive Leadership"] (already in framework-names.json), and a produces:
    glob matching where the command writes (read the command body to find its output path).
    This is the audit's hard prereq: commandsForFramework("Adaptive Leadership") returns
    EMPTY today because NO command declares it; this declaration makes it resolve, which is
    the WFL-01 precondition for leadership's connector in Task 2.
    value-proposition.md: resolve the name/file drift. The command file is value-proposition.md
    but name: is validate-proposition. The command-registry + resolver already map
    "PWS Value Proposition" -> /mos:build-thesis + /mos:validate-proposition (the resolver
    keys off the name: field). Decide per the audit item 39: align the surface id the
    connector registers under with the resolver. The generator registers a command under
    "/mos:" + filename-base (value-proposition), while the resolver maps the framework to the
    name: (validate-proposition). Keep both consistent: leave name: validate-proposition
    (the shipped resolver key callers already use) and add a one-line frontmatter comment
    documenting the intentional file/name relationship so the drift is recorded, not silently
    carried. Do NOT rename the file (that would break the generator surface id and any caller).
    structure-argument.md: it declares two frameworks. For the connector's single framework:
    field pick the PRIMARY key "The Pyramid Principle" (audit item 56). Leave the frameworks:
    list intact (the resolver still resolves both); the connector's framework: is the single
    primary. No em-dashes.
  </action>
  <acceptance_criteria>
    - grep -v '^#' commands/leadership.md | grep -c 'frameworks:.*Adaptive Leadership' returns >= 1
    - grep -v '^#' commands/leadership.md | grep -c 'kind: methodology' returns 1
    - commands/leadership.md declares a produces: glob
    - commands/value-proposition.md frontmatter records the name/file relationship (a comment or note line present)
    - commands/structure-argument.md keeps frameworks: with both keys; the connector framework: (Task 2) is The Pyramid Principle
    - grep -rc '—' on the three files returns 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' commands/leadership.md | grep -q 'Adaptive Leadership' && grep -v '^#' commands/leadership.md | grep -q 'kind: methodology' && echo OK</automated>
  </verify>
  <done>leadership has its Phase-122 block; value-proposition drift recorded; structure-argument primary key chosen.</done>
</task>

<task type="auto">
  <name>Task 2: Tier C batch-A connector blocks (items 31-43) with unique tuples</name>
  <read_first>
    - commands/research.md lines 25-38 (connector block shape + delimiter)
    - data/connector-registry.json (8 existing tuples - TAKEN)
    - LARRYREACH-CONNECTOR-AUDIT.md Section 3 Tier C table rows 31-43
    - docs/CONNECTOR-CONTRACT.md section 2 + 3 + 7
  </read_first>
  <action>
    Add an additive connector: block ("# --- Phase 144.1 connector frontmatter ---") to each
    command, transcribing the audit values EXACTLY with a UNIQUE sub_mode per surface:
    - analyze-systems: context_block, SENS-06, "Systems Thinking", push_forward, fileEvidenceWithReadback, sub_mode systems-analysis
    - compare-ventures: brain_consult, SENS-03, "PWS Triple Validation Compass", hold, memory_event_only, sub_mode venture-compare
    - leadership: context_block, SENS-05, "Adaptive Leadership", hold, fileEvidenceWithReadback, sub_mode adaptive-leadership (now resolvable via Task 1)
    - mos-reason: context_block, SENS-06, "The Pyramid Principle", push_forward, fileEvidenceWithReadback, sub_mode minto-reason
    - dial-memory-refresh: context_block, SENS-02, "Reverse Salient Analysis", hold, memory_event_only, sub_mode dial-refresh (audit says "Reverse Salient" - use the resolvable full name "Reverse Salient Analysis"; memory_event_only fires WFL-01 and it resolves)
    - new-project: context_block, SENS-01/06, framework null, push_forward, filing: none, NO surface, sub_mode new-project (new-project declares no frameworks: block; null framework -> filing none to pass WFL-01)
    - mullins: context_block, SENS-03/06/07, "Mullins Model", hold, fileEvidenceWithReadback, surface F.2, sub_mode mullins-seven
    - validate: context_block, SENS-06, "Jobs to Be Done (JTBD)", hold, fileEvidenceWithReadback, surface F.2, sub_mode validate-jtbd
    - analyze-timing: context_block, SENS-06, "S-Curve Analysis", push_forward, fileEvidenceWithReadback, sub_mode timing-scurve
    - dominant-designs: context_block, SENS-06, "Dominant Design", push_forward, fileEvidenceWithReadback, sub_mode dominant-design
    - build-knowledge: context_block, SENS-06, "Ackoff Pyramid", push_forward, fileEvidenceWithReadback, sub_mode ackoff-pyramid
    - beautiful-question: context_block, SENS-06, "Beautiful Question Framework", push_forward, fileEvidenceWithReadback, sub_mode beautiful-question
    - value-proposition: context_block, SENS-06/07, "PWS Value Proposition", hold, fileEvidenceWithReadback, sub_mode value-prop
    - structure-argument: context_block, SENS-06, "The Pyramid Principle", hold, fileEvidenceWithReadback, sub_mode pyramid-argument
    hierarchy_rank: continue the integer sequence past Plan 01's ranks so all ranks stay
    distinct across the phase (coordinate by reading the audit ordering; rank by audit item
    number is a deterministic choice). plan_gated: false on all; web_scope: null on all.
    CRITICAL WFL-01 RULE: the firesCommand gate fires whenever filing != none OR a surface is set, so memory_event_only DOES fire WFL-01. For any dash-placeholder row (no real frameworks: block - do NOT invent one) set framework: null + filing: none + NO surface so WFL-01 does not fire; record the audit memory_event_only intent as a SUMMARY deviation.
  </action>
  <acceptance_criteria>
    - Each of the 14 commands has exactly one connects_to_spine: true line
    - All reach_ids in the frozen 5; all postures in the frozen 3
    - All sub_modes unique within this batch and vs Plan 01 and vs the 8 existing (no tuple collision; verified by --check in Plan 08)
    - Every filing!=none / surface-bearing connector uses a framework that resolves via commandsForFramework() to >=1 command
    - grep -rc '—' across all 14 files returns 0
  </acceptance_criteria>
  <verify>
    <automated>for f in analyze-systems compare-ventures leadership mos-reason dial-memory-refresh new-project mullins validate analyze-timing dominant-designs build-knowledge beautiful-question value-proposition structure-argument; do c=$(grep -c 'connects_to_spine: true' commands/$f.md); [ "$c" = "1" ] || { echo "FAIL $f ($c)"; exit 1; }; done; echo "OK 14 Tier C batch-A connectors"</automated>
  </verify>
  <done>14 Tier C batch-A commands carry valid unique-tuple connectors; leadership resolvable; em-dash free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command frontmatter -> generated registry | additive YAML; generic handles only |
| framework name -> resolver | commandsForFramework reads command-registry.json only; zero Brain calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144.1-04 | Tampering | a non-frozen reach_id/posture | mitigate | --check frozen-bank validation (Plan 08) |
| T-144.1-05 | Spoofing | leadership connector with unresolvable Adaptive Leadership | mitigate | Task 1 declares frameworks: first; WFL-01 then resolves; --check fails otherwise |
| T-144.1-06 | Tampering | structure-argument dual-framework ambiguity | mitigate | single primary key The Pyramid Principle on the connector |
| T-144.1-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan |
</threat_model>

<verification>
- node scripts/build-connector-registry.cjs --check green after Plan 08 regen.
- commandsForFramework("Adaptive Leadership") resolves after Task 1 + Plan 08 command-registry regen.
</verification>

<success_criteria>
14 Tier C batch-A commands wired; leadership/value-proposition/structure-argument cleanups landed;
frozen banks honored; zero type changes; zero em-dashes.
</success_criteria>

<output>
Create `.planning/phases/144.1-connector-retrofit-sweep/144.1-02-SUMMARY.md` when done.
</output>
