---
phase: 353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted
plan: 02
type: execute
wave: 2
depends_on: ["353-01"]
files_modified:
  - data/section-job-canon.json
  - lib/core/section-registry.cjs
  - scripts/build-section-command-ledger.cjs
  - data/section-command-ledger.json
  - lib/core/room-skeleton-scaffold.cjs
  - lib/core/frontmatter-schemas.cjs
  - templates/room-skeleton/section-contracts/
  - tests/test-275-section-schema.cjs
  - lib/core/navigation/jtbd-anchor.cjs
  - lib/core/navigation/section-gate.cjs
  - lib/core/navigation.cjs
  - lib/mcp/tools/views.cjs
  - lib/mcp/tools/claim.cjs
  - lib/core/section-ruling-candidates.cjs
  - lib/core/navigation-engine.cjs
  - lib/core/doctor/section-ruling-module.cjs
  - data/doctor-modules.json
  - scripts/release.sh
  - tests/fixtures/310-release-step-block-hashes.txt
  - tests/fixtures/341-release-step-block-hashes.txt
  - tests/test-353-section-canon.cjs
  - tests/test-353-ledger-shape.cjs
  - tests/test-353-ruling-doc.cjs
  - tests/test-353-anchor-edge.cjs
  - tests/test-353-filing-gate.cjs
  - tests/test-353-decide-budget.cjs
  - tests/test-353-doctor-section-ruling.cjs
  - tests/test-353-release-wiring.cjs
  - tests/fixtures/353-jev-ledger-responses.json
autonomous: true
requirements: [ICM-353-02, RULE-10, RULE-11, RULE-12, RULE-13, RULE-14, RULE-15, RULE-16, RULE-17, RULE-18, RULE-19, RULE-20, RULE-21]
canon_parts: [7, 8, 9, 11]
user_setup:
  - service: typesafe-jev
    why: "The Jev-scored ledger rebuild is a navigator-invoked pre-release act (R-353-G). Nothing in this plan calls Jev on the test path or the release path; the build script only reaches Jev when the navigator runs it without --offline-seed."
    env_vars:
      - name: TYPESAFE_API_KEY
        source: "~/.secrets/typesafe.env, mode 600, the dev-time key. Never committed, never printed, never read by lib/ or hooks/."

must_haves:
  truths:
    - "Every core and extended section declares the job it exists to do, from one shipped home, with the four vocabulary-gap sections declared rather than forced onto a wrong job"
    - "Each section's CONTEXT.md states its job, its methodology sequence, its writing rules, its gates and its checks, above prose a human authored, and regenerating it never destroys that prose"
    - "A claim or artifact filed into a section lands an anchor edge to that section's jtbd node, and a job mismatch is disclosed rather than hidden"
    - "The runtime orders commands from shipped data with zero vendor calls, and falls back to today's sensor order whenever the ledger has nothing to say"
    - "doctor section-ruling names every ruling-document drift class with a non-empty detail"
    - "A release asserts the ledger is not stale without calling Theo or Jev, and the audited opt-out is named in the release log"
  artifacts:
    - path: "data/section-job-canon.json"
      provides: "the section JTBD canon, one home, with empty ratification fields for the navigator"
      contains: "ratification"
    - path: "scripts/build-section-command-ledger.cjs"
      provides: "Theo pull, Jev scoring, the union join, the offline --check"
    - path: "data/section-command-ledger.json"
      provides: "the shipped relevance ledger"
      contains: "rows"
    - path: "lib/core/navigation/jtbd-anchor.cjs"
      provides: "mintJtbdAnchor, JTBD_ANCHOR_ID"
      exports: ["mintJtbdAnchor", "JTBD_ANCHOR_ID"]
    - path: "lib/core/navigation/section-gate.cjs"
      provides: "resolveFocusSection, evaluateFilingGate"
      exports: ["resolveFocusSection", "evaluateFilingGate", "GATE_MODES"]
    - path: "lib/core/section-ruling-candidates.cjs"
      provides: "buildLedgerCandidates, the pure tierCandidates producer"
      exports: ["buildLedgerCandidates", "__resetLedgerCache"]
    - path: "lib/core/doctor/section-ruling-module.cjs"
      provides: "sync check/fix runner for the section-ruling doctor module"
      exports: ["check", "fix"]
  key_links:
    - from: "lib/core/room-skeleton-scaffold.cjs"
      to: "data/section-command-ledger.json"
      via: "the ruling document's methodology sequence is read from the ledger row"
      pattern: "section-command-ledger"
    - from: "lib/mcp/tools/views.cjs"
      to: "lib/core/navigation/jtbd-anchor.cjs"
      via: "mint then SOURCED_FROM edge"
      pattern: "mintJtbdAnchor"
    - from: "lib/core/navigation-engine.cjs"
      to: "lib/workflow/f-selector-ranker.cjs"
      via: "the ledger candidates enter through the existing tierCandidates input"
      pattern: "tierCandidates"
    - from: "scripts/release.sh"
      to: "scripts/build-section-command-ledger.cjs"
      via: "Step 2.4 offline --check"
      pattern: "build-section-command-ledger.cjs --check"
---

<objective>
Give every section a ruling system: the job it exists to do, the methodology sequence that serves that job, the writing rules, the human gates, and the checks that fail when any of it is violated. Root the ordering in shipped, Theo-derived, Jev-scored data instead of in a per-turn vendor call.

Purpose: this is the unit of value of the whole phase (R1). Plan 01 made a folder answerable; this plan makes it governable.
Output: the section job canon, the ledger build script and the shipped ledger, the generated six-part ruling document, the `jtbd:<job_id>` anchor and the filing gate, the `decide()` eligibility filter feeding `rankForSelector`'s existing `tierCandidates` seam, the `section-ruling` doctor module, and the offline release staleness check.
</objective>

## Decisions this plan implements (locked; see 353-CONTEXT.md)

| Id | What it binds here |
|----|--------------------|
| D-353-1 | This is plan 2 of 3: the ruling system with its relevance ledger, on top of plan 1's addressing layer |
| D-353-3 | Code filters the 113 commands and orders survivors from shipped data; no Theo or Jev call in the turn path; below the floor or with no row the sensor order applies unchanged; `decide()` stays inside its 1200 ms budget, measured |
| D-353-6 | The shipped ledger is truth and `default_methodologies` becomes a derived view; the rebuild is the Spike 002 puller plus Jev batches of 20; Jev unavailable keeps the last ledger behind an audited flag |
| D-353-7 | The ruling document is the section's generated Layer 2 `CONTEXT.md`: six marked, fingerprinted parts above the preserved authored Inputs / Process / Human check prose |
| D-353-8 | The filing gate on `artifact_file` and `claim_write`, `flag` by default with a disclosure, `strict` refuses; every new claim lands an anchor edge to `jtbd:<job_id>`; legacy claims out of scope |
| D-353-9 | Doctor module `section-ruling` registered with the existing row shape; `--fix` on a real room stays the navigator's explicit act |
| D-353-10 | Canon Parts 7 (section registry, command registry vocabulary, navigation door, doctor engine, Spike 002 puller), 8 (no room content to Theo or Jev), 9 (anchors and gates through the door; a human confirms a truth claim), 11 (no new invocable command surface) |
| R-353-A | The `section-ruling` row ships WITHOUT `auto_heal`; Phase 352 classifies it, proposed FALSE |
| R-353-C | The ledger row joins on the UNION of `produces`-into-this-section (seeded from the hand-authored contract tables) and `serves_jtbd` matching the job or its secondary, so the four vocabulary-extension jobs are never empty |
| R-353-D | `claim_write` resolves its section through `getActiveFocus`; its born-wired MCP schema is not widened |
| R-353-F | Requirement ids `RULE-10..21` here; `ICM-353-02` is the roadmap working label |
| R-353-G | The Jev-scored rebuild is a navigator-invoked pre-release act; `release.sh` Step 2.4 gains only the offline `--check` with `--no-ledger-check` |
| R-353-H | The dial's top three is delivered through `rankForSelector`'s existing `tierCandidates` seam; the frozen six-member reach vocabulary and the Part 3 scalars are untouched |
| R-353-I | The `jtbd:` node is never `type: 'claim'`; mint-then-edge is blocking because `writeEdge` probes no endpoint |
| R-353-J | The anchor edge type is `SOURCED_FROM`; no new edge member; `SHARES_JOB`, `MAPS_TO_SECTION` and `PART_OF` are explicitly rejected and the rejection is recorded |
| R-353-K | The `job_mismatch` disclosure rides `mcp_client_event_logged` with a label; no new `EVENT_TYPES` member; the two already-red size assertions are reported, never edited |
| R-353-L | `tests/test-275-section-schema.cjs` lines 330 and 366 get a named amendment task with a phase-cited comment |
| R-353-M | The generator CREATES the ruling document when none exists, because only one fleet room has section CONTEXT.md files today |
| R-353-N | `tierCandidates` is filled from the ledger row, ordered by the ruling sequence then by score, with confidence carried; the floor is calibrated on the labeled set and written into the ledger header with the hit rate it produced |
| WD-353-1 | Top-K 3 (already `MAX_K`), recency window N = 5, filing gate default `flag`, and a ledger confidence floor that is a DIFFERENT scalar from the dial's frozen 0.70 |
| WD-353-2 | Resolved by R-353-J to `SOURCED_FROM` against the shipped allow-list; no new member |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-CONTEXT.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-RESEARCH.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-PATTERNS.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-RESEARCH-GROUNDING-jev.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-VALIDATION.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-01-SUMMARY.md
@.claude/skills/spike-findings-MindrianOS-Plugin/SKILL.md
@docs/superpowers/specs/2026-09-17-icm-section-ruling-system-design.md
</context>

## Standing rules for every task in this plan

1. Rules 1 through 9 of `353-01-PLAN.md` apply unchanged. In particular: `tests/run-all-353.sh` is NEVER edited by this plan; its `run_if` legs already name every test file below.
2. Canon Part 8 ceiling on every vendor call: only a framework NAME, a JTBD STATEMENT and a GLOSSARY LINE (a `definition`, else the first sentence of a `description` capped at 140 characters) may cross to Jev. Never room content, never a full Theo description, never a user string.
3. No file under `lib/` and no file under `hooks/` may reference `api.typesafe.ai`, a Jev client, or `scripts/build-section-command-ledger.cjs`. The vendor lives in `scripts/` only, and only at dev time.
4. Add no member to `EVENT_TYPES` (102 members, two exact-size assertions already RED), to `ALLOWED_EPISTEMIC_TYPES` (exactly 10, asserted exactly), or to `ALLOWED_EDGE_TYPES` (`SOURCED_FROM` already carries this semantics). Do not touch `lib/hmi/dial-reach-orchestrator.cjs`. Do not add a sensor.
5. Cypher is two FIXED string texts; every variation rides in `$params`. `scripts/check-substrate.cjs` rule `m4-cypher-interpolation` refuses a staged commit that concatenates a variable into a `MATCH (` line.
6. Mint the anchor node and confirm `ok === true` BEFORE any `writeEdge` names that id. `writeEdge` probes no endpoint (the FK was removed in Phase 169 D-169-11), so the wrong order silently manufactures a dangling edge row.
7. Bookkeeping never blocks a write (Canon Part 9). A failed mint means no edge, never a refused claim, unless the operator set `strict`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the section JTBD canon and the four vocabulary-extension members</name>
  <read_first>
    - `lib/core/section-registry.cjs` lines 17-44 (`CORE_SECTIONS` 11 slugs, `EXTENDED_SECTION_META` `personas`, `STRUCTURAL_DIRS`) and 153-165 (`getMeta`'s synthesized fallback for an unknown section)
    - `353-RESEARCH-GROUNDING-jev.md` (the measured per-section table: four unambiguous at 93-100 percent, two with a declared secondary, four at 19-32 percent exposing the vocabulary gap)
    - `353-CONTEXT.md` OQ-353-1 (the ruled default: extend by exactly four members) and R-353-C (the join consequence)
    - `353-RESEARCH.md` Seam 5 (the measured 16-member `serves_jtbd` union with counts, why nothing enumerates it, and `lib/memory/per-command-jtbd-derivation.test.cjs:52-70`'s orphan tolerance)
    - `templates/room-skeleton/section-contracts/problem-definition.md` (the `One job:` sentence that is the canon's prose source)
  </read_first>
  <files>data/section-job-canon.json, lib/core/section-registry.cjs, tests/test-353-section-canon.cjs</files>
  <behavior>
    - `data/section-job-canon.json` declares a row for all 11 `CORE_SECTIONS` slugs plus `personas`, with `job_id`, `secondary_job_id` (string or null), `vocabulary_extension` (boolean), `source`, `probe_confidence` (number or null).
    - The rows match the Jev probe exactly: problem-definition `find-problem`; market-analysis `understand-market`; team-execution `plan-execution`; personas `understand-market`; strategy `find-bottleneck` with secondary `explore`; competitive-analysis `understand-market` with secondary `validate-idea`; business-model `model-business`; financial-model `model-finances`; legal-ip `protect-assets`; solution-design `design-solution`, the last four with `vocabulary_extension: true`. `funding` and `opportunity-bank` take their probe answers with the measured confidence recorded.
    - The file carries `ratification: { ratified_by: null, ratified_at: null }` and a `ratification_note` naming the navigator act that fills them. JSON has no frontmatter, so this object IS the frontmatter equivalent and says so.
    - `getSectionJob(slug)` returns `{ job_id, secondary_job_id, vocabulary_extension, source, probe_confidence }` for a known slug and `{ job_id: null, ... , reason: 'undeclared' }` for an unknown one, so the doctor can report "undeclared" rather than guess.
    - `VOCABULARY_EXTENSION_JOBS` already exists: plan 01 Task 6 declared it (with `JOB_VOCABULARY` and `isDeclaredJob`) in this same file, because the birth card needed the four members in wave 1. This task CONSUMES it and never re-declares it; `data/section-job-canon.json`'s `vocabulary_extension_jobs` header is a derived view asserted equal to the export, member for member.
    - No command markdown frontmatter is edited, so `node scripts/build-command-registry.cjs --check` stays green and the four new members remain taxonomy orphans of the same class as the three already shipped.
  </behavior>
  <action>
Create `data/section-job-canon.json` with the rows and the ratification object above. Header keys: `$schema_note`, `schema_version: 1`, `phase: "353-icm-section-ruling-system"`, `canon_parts: [7, 9]`, `probe`: `{ vendor: "jev", model: "jev-1.13.0", probed_at: "2026-09-17", what_crossed: "section slug, the shipped One job sentence, and the 16 job ids with one-line glosses; no room content" }`, then `ratification`, `vocabulary_extension_jobs` (written FROM `section-registry.cjs::VOCABULARY_EXTENSION_JOBS`, which plan 01 Task 6 landed; one home for that fact, this is the derived view) and `sections`.

Extend `lib/core/section-registry.cjs` with `getSectionJob(slug)` only, added to the existing `module.exports` line. `VOCABULARY_EXTENSION_JOBS`, `JOB_VOCABULARY` and `isDeclaredJob` are already exported there from plan 01 Task 6; do not touch them and do not re-declare the four members. Read the canon JSON once at module load into a module-level constant; this file is required by walkers on hot paths, so a per-call read is forbidden. Do NOT copy the `job_id` values into `CORE_SECTIONS` or into `SECTION_METADATA`: the canon file is the one home for this fact (icm-architect invariant 8), the registry tables keep owning label and color, and `room-skeleton-scaffold.cjs`'s `SECTION_METADATA` keeps owning statement and purpose.

State in the canon file's `$schema_note`, in one sentence each: why four members were added rather than forcing a wrong job onto four folders (Jev measured 19-32 percent with flat distributions, and the 16 jobs were built for commands, not sections); that these four are section-canon values only in this phase and no command adopts them yet; and that `secondary_job_id` is carried into the ruling document's methodology sequence as the second block, never dropped.

Add `tests/test-353-section-canon.cjs`.
  </action>
  <verify>
    <automated>node tests/test-353-section-canon.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before the canon file exists, `node tests/test-353-section-canon.cjs` exits non-zero naming the missing `data/section-job-canon.json`. Record the line in the commit body.
    - `node -e "const c=require('./data/section-job-canon.json'); const r=require('./lib/core/section-registry.cjs'); const slugs=Object.keys(r.CORE_SECTIONS).concat(Object.keys(r.EXTENDED_SECTION_META)); for(const s of slugs){ if(!c.sections[s]) { console.error('uncovered '+s); process.exit(1);} } if(Object.keys(c.sections).length!==12) process.exit(1)"` exits 0
    - `node -e "const c=require('./data/section-job-canon.json'); if(c.ratification.ratified_by!==null||c.ratification.ratified_at!==null) process.exit(1)"` exits 0
    - `node -e "const r=require('./lib/core/section-registry.cjs'); if(r.getSectionJob('legal-ip').job_id!=='protect-assets') process.exit(1); if(r.getSectionJob('no-such-section').job_id!==null) process.exit(1); if(r.VOCABULARY_EXTENSION_JOBS.length!==4) process.exit(1)"` exits 0
    - `node -e "const r=require('./lib/core/section-registry.cjs'); const c=require('./data/section-job-canon.json'); const a=r.VOCABULARY_EXTENSION_JOBS.slice().sort().join(','); const b=(c.vocabulary_extension_jobs||[]).slice().sort().join(','); if(a!==b) process.exit(1); for(const row of Object.values(c.sections)){ if(!r.isDeclaredJob(row.job_id)) process.exit(1); if(row.secondary_job_id && !r.isDeclaredJob(row.secondary_job_id)) process.exit(1); }"` exits 0 (every canon job_id is a vocabulary member, and the header is a derived view rather than a second declaration)
    - `node scripts/build-command-registry.cjs --check` exits 0
    - `node lib/memory/per-command-jtbd-derivation.test.cjs` exits 0
    - `git diff --stat commands/` reports no change
  </acceptance_criteria>
  <done>Every section declares its job from one shipped home, the four vocabulary-gap sections are declared honestly rather than mislabeled, and the navigator's ratification slot is waiting and empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/build-section-command-ledger.cjs - Theo pull, union join, Jev scoring, floor calibration, offline --check</name>
  <read_first>
    - `.planning/spikes/002-jev-section-framework-ranker/rank.cjs` lines 145-195 (`pullTheoFrameworks`: `CYPHER_PREFIX` 160-167, `CYPHER_OTHER` 168-175, the 36 alnum buckets plus catch-all, the recursive split at 177-188) and its `jev(key, body)` plus `pool(items, n, fn)` helpers
    - `.claude/skills/spike-findings-MindrianOS-Plugin/references/section-framework-ledger.md` (the IP egress ruling) and `references/jev-typed-decisions-api.md` (endpoint, body shape, `400 * 2 ** attempt` backoff, batch 20 at concurrency 4)
    - `lib/core/brain-client.cjs` around line 920 (`query(cypher, params)`, the three recognized return shapes, and `{ records: [], error: 'brain_query_unrecognized_shape' }` for a fourth)
    - `scripts/build-harness-manifest.cjs` lines 1040-1070 (the three-branch `main()`, `runCheck()`'s `'<name>: OK'` contract and non-zero exit on drift, and the `require.main === module` export split)
    - `templates/room-skeleton/section-contracts/*.md` (`## Commands that write here`, the hand-authored ground-truth half the join seeds from)
    - `data/command-registry.json` (`commands[]` 15 keys each, `framework_index`, `curated_chains`) and `data/connector-registry.json` (where `hitl_shape` actually lives, per Seam 5)
    - `353-CONTEXT.md` R-353-C, R-353-G, R-353-N; `353-RESEARCH.md` Seam 8, Pattern 5, Pitfall 8
  </read_first>
  <files>scripts/build-section-command-ledger.cjs, tests/fixtures/353-jev-ledger-responses.json, tests/test-353-ledger-shape.cjs</files>
  <behavior>
    - `--check` performs ZERO network calls (no Theo, no Jev) and asserts only: the ledger parses; `plugin_version` matches `lib/core/repo-version.cjs`; `built_at` is an ISO string and its age is reported; `theo_frameworks` is a non-negative integer; `jev_model` is a string or null; every `rows` key parses as `<job_id>|<problem_type>|<stage>` with a `job_id` known to `data/section-job-canon.json`. It prints `section-command-ledger: OK` and exits 0, or prints the named drift and exits non-zero.
    - `--offline-seed` builds the ledger from LOCAL sources only (the canon, the command registry, the connector registry, the eleven contract tables), writes `build_mode: "offline-seed"`, `jev_model: null`, per-candidate `confidence: null`, and `source: "ground-truth"` or `"framework-matched"` on every candidate.
    - The default (no flag) build pulls Theo, scores with Jev, and writes `build_mode: "jev-scored"` with the six cost keys taken from the VENDOR-RETURNED usage: `wall_ms`, `jev_calls`, `input_tokens`, `output_tokens`, `estimated_cost_usd`, plus `cost_basis: "vendor-claimed rate, unverified"`.
    - The join for a row keyed `(job_id, problem_type, stage)` is the UNION of (a) commands whose `produces` path names the section, seeded from the section contract's own `## Commands that write here` table and never recomputed from scratch, and (b) commands whose `serves_jtbd` includes the section's `job_id` or its `secondary_job_id`. A section whose `job_id` is one of the four vocabulary-extension members therefore still gets a non-empty row from half (a).
    - `--jev-fixture <path>` injects a recorded response file instead of calling the vendor, so the scoring path is deterministic in tests with zero network.
    - Floor calibration (R-353-N): with Jev confidences present, the build sweeps candidate floors over `evals/icm/cases/turns.json` and writes the floor that maximizes top-3 hit rate into the header as `confidence_floor` with `floor_basis` and `floor_hit_rate`. With `--offline-seed` it writes `confidence_floor: null` and `floor_basis: "not calibrated: offline-seed build carries no vendor confidence"`.
    - The key is read from `~/.secrets/typesafe.env` only, is never printed, never logged, and never written into the ledger. A missing key under the default build exits non-zero with a named reason and writes nothing.
    - An unrecognized Theo shape (`brain_query_unrecognized_shape`) is a NAMED failure that aborts the build, never an empty result silently shipped as a ledger.
  </behavior>
  <action>
Create `scripts/build-section-command-ledger.cjs`. CJS, `process.argv` switch-case router (the `gsd-tools.cjs` pattern), no Commander, no yargs, no new dependency. Port `pullTheoFrameworks` verbatim from the spike: two fixed Cypher texts assembled by joining a line array of string literals, every variation in `$params`, `ROW_CAP` 100 with 36 alnum buckets plus the `NOT toLower(left(f.name,1)) IN $alnum` catch-all and the recursive split to depth 2 on a cap hit, the `coalesce(f.alias_of,'') = '' AND coalesce(f.canonical,true) <> false` canonical filter, and the `(res && (res.rows || res.records)) || []` read.

Header comment states the divergence from the eleven `build-*.cjs --check` siblings in its own paragraph: this builder's `--check` does NOT rebuild, because a rebuild needs Theo and a vendor key, and the release path must stay key-free and Part-8 clean (R-353-G).

Port the Jev client (`jev(key, body)`, `pool(items, n, fn)`, `400 * 2 ** attempt` backoff, max 4 attempts, retry on 429 and 529, batch 20, concurrency 4). Build each Jev question from the framework NAME, its JTBD STATEMENT and its GLOSSARY LINE only. Add an explicit `assertEgressCeiling(payload)` function that throws if any string in the outgoing body exceeds 140 characters outside the allowed statement field, or if a key outside the allowed set appears; call it immediately before every `fetch`. That function is the Part 8 ceiling made executable rather than documented.

Write the ledger with a stable key order and a trailing newline so `--check` can byte-compare when a future phase wants that. Create `tests/fixtures/353-jev-ledger-responses.json` as the recorded response fixture (hand-authored, small, covering two job ids).

Add `tests/test-353-ledger-shape.cjs` covering: the offline seed build is deterministic (two runs, byte-identical output); `--check` makes zero network calls (assert by stubbing `global.fetch` to throw and by asserting `brain-client` is never required in the `--check` path); the union join yields a non-empty row for `business-model` (the vocabulary-extension case); the six cost keys are present, numeric and non-negative after a `--jev-fixture` build; `estimated_cost_usd` is labeled vendor-claimed; a simulated unrecognized Theo shape aborts with a named reason.
  </action>
  <verify>
    <automated>node tests/test-353-ledger-shape.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before the script exists, `node tests/test-353-ledger-shape.cjs` exits non-zero naming the missing script. Record the line in the commit body.
    - `node scripts/build-section-command-ledger.cjs --check` prints `section-command-ledger: OK` and exits 0
    - `grep -cE "MATCH \(.*\\$\{|MATCH \(.*' \+" scripts/build-section-command-ledger.cjs` returns 0 and `node scripts/check-substrate.cjs` reports no new violation
    - `grep -c "TYPESAFE_API_KEY" scripts/build-section-command-ledger.cjs` returns at least 1 and `grep -cE "console\.(log|error)\([^)]*key" scripts/build-section-command-ledger.cjs` returns 0
    - `grep -rn "api.typesafe.ai" lib hooks | wc -l` returns 0
    - `node tests/test-353-ledger-shape.cjs` exits 0 and its output contains `offline-seed deterministic`, `--check made zero network calls`, `business-model row non-empty`, and `estimated_cost_usd labeled vendor-claimed`
  </acceptance_criteria>
  <done>The ledger can be rebuilt by the navigator with the vendor, rebuilt deterministically without it, and checked offline on the release path.</done>
</task>

<task type="auto">
  <name>Task 3: ship data/section-command-ledger.json as the offline seed, with the real-build command documented</name>
  <read_first>
    - `scripts/build-section-command-ledger.cjs` (the script from Task 2, specifically its `--offline-seed` branch and the header key list)
    - `templates/room-skeleton/section-contracts/*.md` (all eleven `## Commands that write here` tables, the ground-truth half)
    - `data/section-job-canon.json` (the twelve job ids the rows are keyed on)
    - `353-CONTEXT.md` D-353-6 and R-353-G (the shipped ledger is truth; the Jev-scored rebuild is a navigator act)
  </read_first>
  <files>data/section-command-ledger.json, docs/SECTION-COMMAND-LEDGER.md</files>
  <action>
Run `node scripts/build-section-command-ledger.cjs --offline-seed` and commit the produced `data/section-command-ledger.json`. Do NOT hand-edit the file: if a row looks wrong, fix the join in the script and rebuild, so the file stays a pure function of its inputs.

The shipped seed must be honest about what it is. Assert in the test, not only in prose, that `build_mode` is `offline-seed`, `jev_model` is `null`, `confidence_floor` is `null` and every candidate's `confidence` is `null`. Nothing in the repo may describe this file as Jev-scored until the navigator's rebuild replaces it.

Write `docs/SECTION-COMMAND-LEDGER.md`: what the ledger is, the exact command for the navigator's pre-release Jev-scored rebuild (`node scripts/build-section-command-ledger.cjs` with the dev-time key exported in the operator's shell), the fallback ruling (Jev unavailable keeps the last ledger and the release names the audited flag), the egress ceiling, and the sentence that no user machine ever carries a Jev key or dependency. Link it from `docs/RELEASE-CEREMONY-RULING-SYSTEM.md`'s step list only if that file already enumerates per-step docs; do not invent a new enumeration.

Extend `tests/test-353-ledger-shape.cjs` with the shipped-file legs rather than creating a second test file.
  </action>
  <verify>
    <automated>node tests/test-353-ledger-shape.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const l=require('./data/section-command-ledger.json'); if(l.build_mode!=='offline-seed'||l.jev_model!==null||l.confidence_floor!==null) process.exit(1); const keys=Object.keys(l.rows); if(keys.length===0) process.exit(1); for(const k of keys){ if(k.split('|').length!==3) process.exit(1); }"` exits 0
    - `node -e "const l=require('./data/section-command-ledger.json'); const c=require('./data/section-job-canon.json'); const jobs=new Set(Object.values(c.sections).map(s=>s.job_id)); for(const j of jobs){ if(!Object.keys(l.rows).some(k=>k.split('|')[0]===j)) { console.error('no row for '+j); process.exit(1);} }"` exits 0
    - `node scripts/build-section-command-ledger.cjs --check` exits 0
    - `grep -rn "Jev-scored" data/section-command-ledger.json` returns nothing
    - `test -f docs/SECTION-COMMAND-LEDGER.md` exits 0 and `grep -c "never carries a Jev key" docs/SECTION-COMMAND-LEDGER.md` returns at least 1
  </acceptance_criteria>
  <done>A real ledger ships, every canon job has a row, and the file states plainly that it has not yet been vendor-scored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: the ruling document generator and the CONTEXT.md frontmatter schema arm</name>
  <read_first>
    - `lib/core/room-skeleton-scaffold.cjs` lines 300-330 (T-275-13: why no substitution runs over contract prose), 354-379 (`writeSectionContracts` as it stands), 500-545 (the MINTO sentinel idiom and the `SECTION_NAMES` FROZEN TABLE CONTRACT comment)
    - `templates/room-skeleton/section-contracts/problem-definition.md` in full (the fixed heading order, the `Do NOT load:` line, the single Human check)
    - `lib/core/frontmatter-schemas.cjs` lines 279-289 (`selectSchemaKey`) and the `artifact-default` schema
    - `353-RESEARCH.md` Pattern 1 (the generated-block-in-an-authored-file target shape) and R-353-M (only `axiom` has CONTEXT.md files today, so the generator must CREATE the document when none exists)
    - `~/.claude/skills/icm-architect/references/core.md` lines 23-30 (the L2 band, 200-500 tokens)
  </read_first>
  <files>lib/core/room-skeleton-scaffold.cjs, lib/core/frontmatter-schemas.cjs, tests/test-353-ruling-doc.cjs</files>
  <behavior>
    - A section with no `CONTEXT.md` gets one created from the shipped template plus the generated parts (R-353-M).
    - A section with an existing `CONTEXT.md` keeps every byte outside the marked region byte-identical; only the frontmatter and the region between `<!-- mos:ruling:begin` and `<!-- mos:ruling:end -->` are replaced.
    - The generated region carries six numbered parts in this order: Job, Methodology sequence, Writing rules, Gates, Checks, Commands that write here. The methodology sequence carries the section's `secondary_job_id` block second when one is declared.
    - Frontmatter carries exactly `icm_layer: 2`, `job_id`, `ruling_fingerprint`, `generated_at`. `ruling_fingerprint` is a sha256 over the canon row plus the ledger row plus a writer-contract version string, and recomputes identically on a second generation from unchanged inputs.
    - Two consecutive generations leave the file byte-identical.
    - The whole document's `estimateTokens` stays at or under 500 (the icm-architect L2 band); overflow goes to `references/SECTION-SCHEMA.md`, which `writeReferenceDocs` already ships.
    - `renderTemplate` substitution never runs over contract prose.
    - `selectSchemaKey('<any>/CONTEXT.md')` returns `'CONTEXT.md'` and that schema's `optional` list contains `icm_layer`, `job_id`, `ruling_fingerprint` and `generated_at`, so a regeneration emits zero `unknown` violations.
  </behavior>
  <action>
Rewrite `writeSectionContracts(roomDir, sectionList, result)` in `lib/core/room-skeleton-scaffold.cjs` as the ruling document generator. Compose the generated block as a separate string and splice it; never substitute into the authored text. Keep the existing warning and error strings (`contract_template_missing:<slug>`, `contract_write_failed:<slug>`) so existing callers and tests keep matching, and add `ruling_regenerated:<slug>` to `result` alongside the existing `contracts_created` array rather than repurposing it. Keep the atomic write. Preserve the `Do NOT load:` line and the single Human check by never touching anything below `<!-- mos:ruling:end -->`.

Read the methodology sequence from `data/section-command-ledger.json` for `(job_id, room problem_type, room stage)`, falling back to the `(job_id, '*', '*')` row and then to an empty sequence with a stated "no ledger row" line inside part 2. Never call Theo or Jev from this code path; it runs at room birth on a user machine.

Add the `CONTEXT.md` arm to `selectSchemaKey` in `lib/core/frontmatter-schemas.cjs` plus a `CONTEXT.md` schema with `required: []` and an `optional` list of exactly the four generated keys, with a phase-cited comment. Without this arm, `CONTEXT.md` falls through to `artifact-default` and every regeneration lands four `unknown` violations through a PostToolUse hook.

Add `tests/test-353-ruling-doc.cjs` covering every behavior above, including the authored-prose byte-preservation check (author a paragraph below the end marker, regenerate, diff the tail) and the L2 token ceiling.
  </action>
  <verify>
    <automated>node tests/test-353-ruling-doc.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before the rewrite, `node tests/test-353-ruling-doc.cjs` exits non-zero because the landed `CONTEXT.md` has no `mos:ruling:begin` marker. Record the line in the commit body.
    - `node tests/test-353-ruling-doc.cjs` exits 0 and its output contains `authored prose byte-preserved`, `fingerprint stable on regeneration`, `created when absent`, and `L2 band <= 500`
    - `node -e "const s=require('./lib/core/frontmatter-schemas.cjs'); if(s.selectSchemaKey('/x/problem-definition/CONTEXT.md')!=='CONTEXT.md') process.exit(1); const o=s.SCHEMAS['CONTEXT.md'].optional; ['icm_layer','job_id','ruling_fingerprint','generated_at'].forEach(k=>{ if(!o.includes(k)) process.exit(1); })"` exits 0
    - `grep -c "renderTemplate" lib/core/room-skeleton-scaffold.cjs` is unchanged from its pre-task value, and `grep -n "renderTemplate" lib/core/room-skeleton-scaffold.cjs` shows no call inside `writeSectionContracts`
    - `grep -rn "mos:ruling:begin" lib/core/room-skeleton-scaffold.cjs | wc -l` returns at least 1
  </acceptance_criteria>
  <done>Every section's CONTEXT.md becomes a generated ruling document above prose a human owns, and the frontmatter hook stays quiet about it.</done>
</task>

<task type="auto">
  <name>Task 5: the named amendment of the two Phase 275 assertions</name>
  <read_first>
    - `tests/test-275-section-schema.cjs` lines 20-40 (the WR-02 gray-matter ruling), 300-335 (the template-marker block ending at the line 330 assertion `no contract template has YAML frontmatter`), and 350-370 (the landed-file loop ending at the line 366 assertion `every landed CONTEXT.md is byte-identical to its template`)
    - `lib/core/room-skeleton-scaffold.cjs` lines 538-544 (the FROZEN TABLE CONTRACT comment: the house idiom for a deliberate, versioned, phase-cited amendment)
    - `353-CONTEXT.md` R-353-L (the amendment is a named task, not a stumble)
  </read_first>
  <files>tests/test-275-section-schema.cjs</files>
  <action>
Amend `tests/test-275-section-schema.cjs` deliberately, with a phase-cited comment block above each change written in the FROZEN TABLE CONTRACT voice.

Line 330 (`no contract template has YAML frontmatter`): the assertion STAYS and stays green, because Phase 353 adds frontmatter to the LANDED document at write time and never to the template on disk. Add the comment stating exactly that, so a future reader does not conclude the invariant was quietly narrowed: the templates remain frontmatter-free by design (T-275-13's reasoning is unchanged), and the landed file's frontmatter is composed by the generator.

Line 366 (`every landed CONTEXT.md is byte-identical to its template`): this assertion is now FALSE by design and is replaced. The replacement pins the new invariant in two parts, both asserted: (a) every landed `CONTEXT.md` contains `<!-- mos:ruling:begin` and `<!-- mos:ruling:end -->` and a `ruling_fingerprint` in its frontmatter; (b) every byte of the landed file BELOW the end marker is byte-identical to the corresponding region of its template. Iterate the same `scaffold.SECTION_NAMES` list the current loop iterates.

Do not touch any other assertion in the file. Do not renumber. The commit body records the before and after text of the replaced assertion verbatim.
  </action>
  <verify>
    <automated>node tests/test-275-section-schema.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-275-section-schema.cjs` exits 0
    - `grep -c "no contract template has YAML frontmatter" tests/test-275-section-schema.cjs` returns 1
    - `grep -c "byte-identical to its template" tests/test-275-section-schema.cjs` returns 0
    - `grep -c "Phase 353" tests/test-275-section-schema.cjs` returns at least 2
    - `grep -c "mos:ruling:end" tests/test-275-section-schema.cjs` returns at least 1
  </acceptance_criteria>
  <done>The two Phase 275 invariants are amended on purpose, with the phase cited, and the replacement pins what is now true.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: the jtbd anchor node, minted before any edge ever names it</name>
  <read_first>
    - `lib/core/navigation/goal-anchor.cjs` in full (160 lines): the five header rules at 27-56, the body at 100-138, `on_conflict: 'nothing'` and why it is load-bearing
    - `lib/core/node-insert.cjs` lines 108-130 (`ALLOWED_EPISTEMIC_TYPES`, exactly 10, `observation` already a member) and the `insertNode` signature around 200-215
    - `lib/core/navigation/edges.cjs` lines 32-90 (`ALLOWED_EDGE_TYPES`, 44 members, `SOURCED_FROM` present) and 1048-1080 (`writeEdge` validates two id strings and a type, and probes no endpoint)
    - `lib/core/navigation.cjs` lines 250-262 (the `mintGoalAnchor` / `GOAL_ANCHOR_ID` re-export block and its comment)
    - `tests/test-345-gate-anchor.cjs` lines 36-73 (the module-load SKIP guard and `makeFixtureRoom`/`cleanupFixtureRoom`)
    - `353-CONTEXT.md` R-353-I and R-353-J; `353-RESEARCH.md` Pitfalls 4 and 5
  </read_first>
  <files>lib/core/navigation/jtbd-anchor.cjs, lib/core/navigation.cjs, tests/test-353-anchor-edge.cjs</files>
  <behavior>
    - `JTBD_ANCHOR_ID(jobId)` returns `'jtbd:' + jobId` for a non-empty string and `null` otherwise.
    - `mintJtbdAnchor(db, jobId)` inserts a node of type `'jtbd'`, NEVER `'claim'`, with `source_path: 'system:jtbd-anchor'`, `created_by: 'system'`, `epistemic_type: 'observation'`, `review_status: 'proposed'`, `on_conflict: 'nothing'`, and returns `{ ok: true, node_id, created }` or `{ ok: false, reason, detail }`. It never throws.
    - A second mint of the same id returns `created: false` and leaves `last_seen_at` untouched.
    - No member is added to `ALLOWED_EPISTEMIC_TYPES`, `ALLOWED_EDGE_TYPES` or `EVENT_TYPES`.
    - `navigation.mintJtbdAnchor` and `navigation.JTBD_ANCHOR_ID` resolve through the chokepoint.
    - A `SOURCED_FROM` edge written to an id that was never minted lands as a dangling row, which the test proves once as the reason the ordering rule exists.
  </behavior>
  <action>
Create `lib/core/navigation/jtbd-anchor.cjs` as a near-verbatim structural clone of `goal-anchor.cjs` with the four substitutions: the `'jtbd:'` id prefix, `source_path: 'system:jtbd-anchor'`, `epistemic_type: 'observation'`, and node type `'jtbd'`. Never use a raw `INSERT INTO nodes`; go through `insertNode` (`scripts/check-substrate.cjs` rule `raw-graph-write` refuses the staged commit otherwise). The one bare `SELECT id FROM nodes WHERE id = ?` that `goal-anchor.cjs` uses to compute `created` is a read and is allowed; keep it identical.

The module header must carry, in the same voice as `goal-anchor.cjs`'s: (1) the node is a projection, the homes are `data/section-job-canon.json` and `.mindrian/room-map.json`, do not build a second store here; (2) the type is `'jtbd'` and NEVER `'claim'`, with the reason spelled out (a claim-typed anchor with nothing above it lands in Phase 343's `unanchored_claims` numerator as a brand-new unanchored claim in every room, which would manufacture up to about 500 new unanchored claims fleet-wide while this phase's own criterion 3 claims to close that gap); (3) the BLOCKING mint-then-edge ordering rule with the D-169-11 citation; (4) the node-census increment this creates, stated plainly (one node per declared job per room).

Re-export both from `lib/core/navigation.cjs` immediately after the `mintGoalAnchor` / `GOAL_ANCHOR_ID` pair, with a comment repeating the mint-before-edge rule.

Add `tests/test-353-anchor-edge.cjs`, including the leg that proves an unminted target produces a dangling edge row (written once, as the documented reason for the ordering rule, then cleaned up).
  </action>
  <verify>
    <automated>node tests/test-353-anchor-edge.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before the module exists, `node tests/test-353-anchor-edge.cjs` exits non-zero naming the missing `jtbd-anchor.cjs`. Record the line in the commit body.
    - `node -e "const n=require('./lib/core/navigation.cjs'); if(typeof n.mintJtbdAnchor!=='function'||typeof n.JTBD_ANCHOR_ID!=='function') process.exit(1); if(n.JTBD_ANCHOR_ID('find-problem')!=='jtbd:find-problem') process.exit(1)"` exits 0
    - `grep -c "'claim'" lib/core/navigation/jtbd-anchor.cjs` returns 0 and `grep -c "'jtbd'" lib/core/navigation/jtbd-anchor.cjs` returns at least 1
    - `grep -cE "INSERT INTO (nodes|edges)" lib/core/navigation/jtbd-anchor.cjs` returns 0
    - `node lib/core/node-insert-epistemic.test.cjs` exits 0 (the 10-member exact assertion is untouched)
    - `node tests/test-353-anchor-edge.cjs` exits 0 and its output contains `type is jtbd not claim`, `re-mint is idempotent`, and `unminted target yields a dangling edge (why the order is blocking)`
  </acceptance_criteria>
  <done>Every section job has a payload-free anchor node of a non-claim type, and the mint-then-edge order is proven rather than asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: the filing gate on artifact_file and claim_write, with the anchor edge</name>
  <read_first>
    - `lib/mcp/tools/views.cjs` lines 180-260 (`fileArtifact`: the `mcp_client_event_logged` call at 195-200, the `REASONING_NODE_ID` mint at 208, the `writeReasoningNode` call at 214-224, the return shape at 250-256), the `artifact_file` registration at 317 with its `section` parameter, and the `connectors` export at 371-378
    - `lib/mcp/tools/claim.cjs` lines 85-175 (`writePathRefusal`, `resolveEffectiveSessionId`, `resolveSessionRoomDir`, `openRoomDbForCaller`, `writeClaimNode`, the `claim_write` schema with NO `section` parameter, and the `connectors` export at 163-174)
    - `lib/core/navigation/focus.cjs` lines 16-30 (`getActiveFocus(db, sessionId)` returns `{ sessionId, focusNodeId, focusType, setAt, setBy }` or null) and 32-35 (`lookupNodeType`)
    - `lib/core/navigation/reasoning-write.cjs` lines 99-135 (where `section` rides in the node's properties JSON) and 155-192 (the shipped claim-to-anchor `SOURCED_FROM` write loop and its never-fail-the-call comment)
    - `353-CONTEXT.md` D-353-8, R-353-D, R-353-J, R-353-K; `353-RESEARCH.md` Seam 7, Pitfalls 2 and 4
  </read_first>
  <files>lib/core/navigation/section-gate.cjs, lib/core/navigation.cjs, lib/mcp/tools/views.cjs, lib/mcp/tools/claim.cjs, tests/test-353-filing-gate.cjs</files>
  <behavior>
    - `resolveFocusSection(db, sessionId)` returns `{ ok: true, section }` by reading the active focus node's `section` property, or `{ ok: false, reason: 'no_focus' | 'no_section_on_focus' | 'read_failed' }`. It never throws and never widens an MCP schema.
    - `evaluateFilingGate({ section, servesJtbd, mode })` returns `{ verdict: 'match' | 'mismatch' | 'unresolved', section_job, declared_job, reason }`. `unresolved` is the honest answer when no section resolves; it is never silently treated as a match.
    - `GATE_MODES` is exactly `flag` and `strict`. `flag` is the default.
    - Under `flag`, a mismatch still lands the write, and a `mcp_client_event_logged` memory event is logged with `label` unchanged for the tool plus the added properties `job_mismatch: true`, `declared_job`, `section_job`, `reason`. No new `EVENT_TYPES` member is minted.
    - Under `strict`, a mismatch refuses with `{ ok: false, reason: 'job_mismatch' }` and writes nothing.
    - On every successful claim or artifact write into a resolved section, `mintJtbdAnchor` runs FIRST; only on `ok === true` does a `SOURCED_FROM` edge get written from the new node to `jtbd:<job_id>`. A failed mint means no edge and never a refused write.
    - `artifact_file` resolves the section from its existing `section` parameter. `claim_write` resolves it through `resolveFocusSection`; its MCP schema and its `connectors` export are byte-unchanged.
    - Filing N claims into a fixture room yields N `SOURCED_FROM` edges whose `target_id` starts with `jtbd:` (criterion 3, 100 percent).
  </behavior>
  <action>
Create `lib/core/navigation/section-gate.cjs` exporting `resolveFocusSection`, `evaluateFilingGate` and `GATE_MODES`. Put the focus read and the node-properties read here, behind the navigation door, rather than in the MCP tool layer. Read the section's `job_id` through `section-registry.cjs::getSectionJob`, and fall back to the PARENT section's job when a sub-room declares none (D-353-5's parent fallback), recording `reason: 'parent_fallback'` so the doctor can report the undeclared sub-room. The sub-room half of that fallback is reached ONLY when plan 01 Task 6's birth card left `job_id` absent (RULE-29): a sub-room that declared its own job at birth resolves from its own ROOM.md `job_id` and never through the parent, and the two cases are distinguishable by the `job_source: declared` key. Re-export the three from `lib/core/navigation.cjs`.

Wire `fileArtifact` in `lib/mcp/tools/views.cjs`: run the gate after the section is known and before the write; under `flag` add the four disclosure properties to the EXISTING `logMemoryEvent` call rather than adding a second event; under `strict` return the refusal. After a successful `writeReasoningNode`, mint the anchor, then write the `SOURCED_FROM` edge with `properties: { relation: 'sourced_from', origin: 'artifact_file' }` following `reasoning-write.cjs`'s loop, and add `anchor_edge` to the returned object without changing or removing any existing return key.

Wire `claim_write` in `lib/mcp/tools/claim.cjs`: resolve the section through `resolveFocusSection` using the session id the handler already computes, run the same gate, and land the same mint-then-edge pair after `writeClaimNode` succeeds. Do NOT add a `section` parameter, do NOT touch the `connectors` export, and do NOT regenerate the connector registry for a schema change, because there is no schema change.

The mode is read from an environment-independent source in this order: an explicit `mode` on the tool call if one already exists, else the room's own setting if one exists, else `flag`. Do not invent a new environment variable in this plan; state the resolution order in the module header.

Add `tests/test-353-filing-gate.cjs`.
  </action>
  <verify>
    <automated>node tests/test-353-filing-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before the gate exists, `node tests/test-353-filing-gate.cjs` exits non-zero naming the missing `section-gate.cjs`. Record the line in the commit body.
    - `node tests/test-353-filing-gate.cjs` exits 0 and its output contains `flag lands the write`, `strict refuses`, `unresolved is not a match`, `mint precedes edge`, and `N of N claims carry an anchor edge`
    - `node -e "const c=require('./lib/mcp/tools/claim.cjs'); const src=require('node:fs').readFileSync('lib/mcp/tools/claim.cjs','utf8'); if(/section:\s*z\./.test(src)) process.exit(1)"` exits 0 (no `section` parameter was added)
    - `git diff lib/mcp/tools/claim.cjs | grep -c "connectors"` returns 0 and `git diff lib/mcp/tools/views.cjs | grep -c "hitl_shape"` returns 0
    - `node -e "const m=require('./lib/core/navigation/memory-events.cjs'); if(m.EVENT_TYPES.size!==102) process.exit(1)"` exits 0
    - `node scripts/build-connector-registry.cjs --check` exits 0
    - `node tests/test-353-anchor-edge.cjs` exits 0
  </acceptance_criteria>
  <done>A write into a section is checked against that section's job, disclosed when it does not match, refused only in strict mode, and always anchored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: the ledger candidate producer and the decide() eligibility filter</name>
  <read_first>
    - `lib/workflow/f-selector-ranker.cjs` lines 880-975 (the D10 purity invariant, `MAX_K = 3` at 87, the `tierCandidates` arg documented at 941, the registry load at 972-973) and the export block at 1082
    - `lib/core/orchestration-candidate-lift.cjs` lines 79-110 (`buildTierCandidates`, the producer shape to copy)
    - `lib/core/navigation-engine.cjs` lines 945-1000 (`decide()`'s entry, `decideStartMs`, the single `dispatchSensors` consumption) and 1046-1100 (the SENS-16 producer block, its try/catch soft-fail, its ctx-override test seam, and its cost comment against the 1200 ms budget)
    - `lib/hmi/dial-reach-orchestrator.cjs` lines 110-150 (READ ONLY, to see the frozen six-member reach vocabulary this task must not touch)
    - `data/connector-registry.json` (where `hitl_shape` lives, since the command registry has no such key) and `data/command-registry.json` (`produces`, `autonomous_safe`, `serves_jtbd`)
    - `353-CONTEXT.md` D-353-3, R-353-H, R-353-N; `353-RESEARCH.md` Seam 6, Pitfalls 9 and 10
  </read_first>
  <files>lib/core/section-ruling-candidates.cjs, lib/core/navigation-engine.cjs, tests/test-353-decide-budget.cjs</files>
  <behavior>
    - `buildLedgerCandidates({ jobId, problemType, stage, ledger, decisionTraces, recencyWindow })` is pure and synchronous: no db, no network, no write, no event.
    - Code eligibility filters the 113 commands on five conditions before ordering: the command's `produces` glob names this folder; the stage gate admits the room's stage; `autonomous_safe` is honored; the command has not run in this folder within the last N turns (N = 5, WD-353-1); the command carries a declared HITL shape, read from `data/connector-registry.json` and NOT from the command registry, which has no such key.
    - Survivors are ordered by the ruling document's sequence, and within a sequence step by the ledger score, with the ledger confidence carried on every candidate.
    - The confidence gate is: when a candidate's `confidence` is `null` it qualifies only if its `source` is `ground-truth`; otherwise it qualifies only when `confidence >= ledger.confidence_floor`.
    - With no ledger row, an empty survivor set, or a ledger that fails to parse, the producer returns `null` and `decide()` behaves byte-identically to today (the sensor order, unchanged).
    - The ledger JSON is read once and cached at module level; `__resetLedgerCache()` exists for tests. A per-turn file read is a defect.
    - `decide()` with the producer enabled stays inside the 1200 ms budget on the fixture rooms, measured through `_meta.latencies_ms`, and the producer's own share is reported separately.
    - `lib/hmi/dial-reach-orchestrator.cjs` is byte-unchanged. `SENSOR_REGISTRY` and `SENSOR_REGISTRY_IDS` are byte-unchanged. `MAX_K`, `DIAL_REACH_K`, `RECOMMEND_FLOOR` and `MARGIN_THRESHOLD` are byte-unchanged.
  </behavior>
  <action>
Create `lib/core/section-ruling-candidates.cjs` exporting `buildLedgerCandidates` and `__resetLedgerCache`. Header comment states in one paragraph why the confidence floor here is a DIFFERENT scalar from the dial's `RECOMMEND_FLOOR` 0.70 and from `REGISTRY_DEFAULT_BRAIN_CONFIDENCE` 0.5: this one is a vendor score-confidence over a ranked command list, that one is a Canon Part 3 frozen detent over six machine reaches, and nothing in this phase moves a frozen scalar.

Add the producer block to `decide()` in `lib/core/navigation-engine.cjs` immediately after the existing SENS-16 block, copying that block's shape exactly: a braced block, LOCAL reads at ctx-assembly time only, caller-threaded `ctx.*` overrides winning as the test seam, a try/catch that soft-fails to `null`, and a comment stating the measured cost against the 1200 ms budget with the real number from the test. Thread the result into the existing `rankForSelector` call as `tierCandidates`; add no new argument to `rankForSelector` and change no scalar inside it.

Add `tests/test-353-decide-budget.cjs`. It must print three numbers the SUMMARY quotes: the producer's own wall time, `decide()`'s total `_meta.latencies_ms` with the producer on, and the same with it off.
  </action>
  <verify>
    <automated>node tests/test-353-decide-budget.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before the producer exists, `node tests/test-353-decide-budget.cjs` exits non-zero naming the missing `section-ruling-candidates.cjs`. Record the line in the commit body.
    - `node tests/test-353-decide-budget.cjs` exits 0 and prints lines matching `producer ms:`, `decide ms with producer:` and `decide ms without producer:`, with the with-producer total under 1200
    - `git diff --stat lib/hmi/dial-reach-orchestrator.cjs lib/core/insight-sensors.cjs` reports no change
    - `node -e "const r=require('./lib/workflow/f-selector-ranker.cjs'); if(r.MAX_K!==3) process.exit(1)"` exits 0
    - `grep -cE "readFileSync\(.*section-command-ledger" lib/core/section-ruling-candidates.cjs` returns 1 (one read, at module scope or behind the cache, never per call)
    - `grep -cE "\bawait\b|new Promise" lib/core/section-ruling-candidates.cjs` returns 0
    - `node tests/test-245-priority-complete.cjs` exits 0 and `node scripts/build-connector-registry.cjs --check` exits 0
  </acceptance_criteria>
  <done>Shipped data orders the commands the dial offers, through the seam that already existed, with zero vendor calls and a measured budget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: doctor module section-ruling plus its registry row</name>
  <read_first>
    - `lib/core/doctor/room-map-module.cjs` (the module plan 01 wrote, including its `recoverable: false` fixture-prefix guard, which this one copies)
    - `lib/core/doctor/room-md-module.cjs` lines 22-26, 136-148, 160-206 (the contract, the D-03 detail rule, the fix-then-recheck shape)
    - `data/doctor-modules.json` (the seven-key row shape and the `room-map` row plan 01 added)
    - `353-CONTEXT.md` D-353-9, R-353-A, R-353-M
  </read_first>
  <files>lib/core/doctor/section-ruling-module.cjs, data/doctor-modules.json, tests/test-353-doctor-section-ruling.cjs</files>
  <behavior>
    - `check(ctx)` and `fix(ctx)` are synchronous and return a non-empty `detail` on every path including `skip` and `ok`.
    - `check` reports four drift classes, each in its own array: `ruling_fingerprint_drift` (the document's `ruling_fingerprint` disagrees with a recomputation from canon plus ledger), `sections_without_job_id`, `subrooms_without_job_id` (reported as parent-fallback, not as an error), and `claims_without_anchor` counted only for claims created after this phase's `introduced_version`.
    - `check` sets `recoverable: false` outside the `tests/fixtures/icm-rooms` prefix, so `--fix` cannot regenerate a ruling document in a real fleet room without the navigator's explicit act.
    - `fix` regenerates ruling documents through the Task 4 generator and re-runs `check`.
    - The registry row carries exactly seven keys, `introduced_version: "2.0.0-beta.48"`, `cadence: "always"`, `flag: null`, `fix_supported: true`, `runner: "lib/core/doctor/section-ruling-module.cjs"`, and no `auto_heal` key.
  </behavior>
  <action>
Create `lib/core/doctor/section-ruling-module.cjs` as a structural sibling of `room-map-module.cjs`. Sync only. Never back-require `scripts/doctor.cjs`. Count `claims_without_anchor` through the navigation read path, never through a raw `SELECT` in the module, and scope the count to claims created after the module's `introduced_version` so legacy claims stay out of scope (D-353-8's closing sentence).

Register the row in `data/doctor-modules.json` immediately after the `room-map` row. Carry the same R-353-A note forward in the header: Phase 352 classifies `auto_heal` for this row when it lands (proposed: FALSE, because regenerating a ruling document rewrites a file a human may have authored prose into, which is a judgment fix).

Add `tests/test-353-doctor-section-ruling.cjs`, driving temporary copies of the fixture rooms.
  </action>
  <verify>
    <automated>node tests/test-353-doctor-section-ruling.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before the runner exists, `node tests/test-353-doctor-section-ruling.cjs` exits non-zero naming the missing runner. Record the line in the commit body.
    - `node -e "const m=require('./data/doctor-modules.json'); const r=m.modules.find(x=>x.id==='section-ruling'); if(!r||Object.keys(r).length!==7||('auto_heal' in r)) process.exit(1); if(m.modules.length!==26) process.exit(1)"` exits 0
    - `node -e "const m=require('./data/doctor-modules.json'); const fs=require('node:fs'); for(const r of m.modules){ if(!fs.existsSync(r.runner)) process.exit(1); }"` exits 0
    - `grep -cE "\basync function (check|fix)|\.then\(" lib/core/doctor/section-ruling-module.cjs` returns 0
    - `grep -cE "SELECT .* FROM (nodes|edges)" lib/core/doctor/section-ruling-module.cjs` returns 0
    - `node tests/test-353-doctor-section-ruling.cjs` exits 0 and its output contains all four drift class names
    - `node scripts/doctor.cjs --acceptance` exits 0
  </acceptance_criteria>
  <done>Ruling-document drift, undeclared jobs and unanchored new claims are all reported with named details, and nothing auto-heals a real room.</done>
</task>

<task type="auto">
  <name>Task 10: release.sh Step 2.4 offline ledger check, the audited opt-out, and the step-hash re-pin</name>
  <read_first>
    - `scripts/release.sh` lines 140-170 (the flag variables, the `--no-theo-check` / `--no-theo-notify` precedent, `USAGE_BLOCK`, and the `case` arm list) and lines 356-375 (Step 2.4's three existing `--check` gates with their ABORT and Recovery lines)
    - `tests/run-all-310.sh` lines 90-155 (the step-block tripwire: per-header sha256 compared to the fixture, set equality, and the `-ne 30` header count) and lines 260-300 (`ALLOWED_FILES` and `EMDASH_FILES`)
    - `tests/fixtures/310-release-step-block-hashes.txt` header (the reproducible generating command) and `tests/fixtures/341-release-step-block-hashes.txt` header
    - `353-CONTEXT.md` R-353-G; `353-RESEARCH.md` Seam 8 and Pitfall 12
  </read_first>
  <files>scripts/release.sh, tests/fixtures/310-release-step-block-hashes.txt, tests/fixtures/341-release-step-block-hashes.txt, tests/test-353-release-wiring.cjs</files>
  <action>
Edit `scripts/release.sh` with exactly two changes. First, in the Step 0 flag block: add `NO_LEDGER_CHECK=0` with a one-line comment naming Phase 353 and R-353-G in the `--no-theo-check` voice, add `--no-ledger-check` to the `case` arm list, and add it to `USAGE_BLOCK`. Second, in Step 2.4: add a fourth gate that runs `node "$PLUGIN_DIR/scripts/build-section-command-ledger.cjs" --check` when `NO_LEDGER_CHECK` is 0, with an ABORT line and a `Recovery:` line naming the navigator's rebuild command, and, when the flag IS engaged, an echoed line naming the flag and its consequence (a deliberately stale ledger ships and the release log says so). Add nothing else. Do not add a rebuild. Do not add a Theo or Jev call. The release path stays key-free.

Re-pin the step-block hashes. Regenerate using the command documented in each fixture's own header and replace ONLY the lines for the blocks this task changed (`# --- Step 0: Parse bump type + flags ---` and `# --- Step 2.4: coverage gates ...`). The header count stays 30; no block is added or renamed. In the same commit, record in the fixture header a dated, phase-cited line saying which two block hashes were re-pinned and why, following the `quick task 260917-o1y` precedent already in the 310 fixture.

Pre-existing red to REPORT and not fix: `tests/fixtures/341-release-step-block-hashes.txt` is already stale at HEAD against two blocks that landed after it (`# --- Step 0.6: Theo command-registry stamp gate ...` and `# --- Step 5.6: Notify Theo of the new version ...`), and its `STEP_BLOCK_COUNT` still reads 28 against 30 real blocks. Re-pin only the two blocks THIS task changes; leave that pre-existing staleness exactly as found, name it in the SUMMARY, and do not renumber `STEP_BLOCK_COUNT`.

Add `tests/test-353-release-wiring.cjs` asserting: exactly one `build-section-command-ledger.cjs --check` call site in `release.sh`; its byte offset falls inside the Step 2.4 block; `--no-ledger-check` appears in `USAGE_BLOCK` and in the `case` list; no `TYPESAFE_API_KEY` and no `api.typesafe.ai` literal appears anywhere in `release.sh`; and no `build-section-command-ledger.cjs` invocation exists without `--check` in that file.
  </action>
  <verify>
    <automated>node tests/test-353-release-wiring.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/release.sh` exits 0
    - `node tests/test-353-release-wiring.cjs` exits 0
    - `grep -c "no-ledger-check" scripts/release.sh` returns at least 3 (variable, case arm, usage)
    - `grep -cE "TYPESAFE_API_KEY|api\.typesafe\.ai" scripts/release.sh` returns 0
    - `bash tests/run-all-310.sh` exits 0
    - `bash scripts/release.sh patch --dry-run 2>&1 | grep -c "section-command-ledger"` returns at least 1
    - `grep -c "STEP_BLOCK_COUNT" tests/fixtures/341-release-step-block-hashes.txt` returns 1 and its value is still `28` (the pre-existing staleness is reported, not edited)
  </acceptance_criteria>
  <done>A release asserts the ledger is current without a single vendor byte crossing the wire, the opt-out is audited and named, and the byte-level scope tripwire is re-pinned for exactly the two blocks this phase touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room disk -> plugin code | Section names, ROOM.md frontmatter and authored CONTEXT.md prose are user-owned and untrusted |
| plugin -> Theo (`theo-mcp.onrender.com`) | Dev-time only, build script only, generic methodology handles only (Canon Part 8) |
| plugin -> Jev (`api.typesafe.ai`) | Dev-time only, build script only, framework name plus JTBD statement plus glossary line only |
| operator shell -> build script | The dev-time `TYPESAFE_API_KEY` at `~/.secrets/typesafe.env` mode 600 |
| MCP client -> `artifact_file` / `claim_write` | Client-supplied `section`, `serves_jtbd` and content cross into the graph write door |
| shipped data -> turn path | `data/section-command-ledger.json` is read every turn and decides what the dial offers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-353-09 | Information disclosure | room content reaching Jev or Theo (Canon Part 8) | mitigate | Task 2: `assertEgressCeiling(payload)` runs before every `fetch` and throws on any key outside the allowed set or any over-140-character glossary string; plan 03's tripwire proves no `api.typesafe.ai` literal exists under `lib/` or `hooks/` |
| T-353-10 | Information disclosure | the dev-time key leaking into a shipped file, a log or a hook | mitigate | Task 2: the key is read from `~/.secrets/typesafe.env` only, never printed, never written into the ledger; Task 10 asserts `release.sh` contains no key literal; the acceptance criteria grep for a `console.log` of a key |
| T-353-11 | Tampering | Cypher injection into the Theo pull | mitigate | Task 2: two FIXED Cypher texts, every variation in `$params`, verbatim port of the spike puller; `check-substrate.cjs` rule `m4-cypher-interpolation` refuses the staged commit otherwise |
| T-353-12 | Tampering | a dangling `SOURCED_FROM` edge to an unminted anchor | mitigate | Tasks 6 and 7: mint first, confirm `ok === true`, then `writeEdge`; the dangling-edge behavior is proven once in the test as the reason the order is blocking |
| T-353-13 | Spoofing | a claim-typed anchor inflating the unanchored-claims census | mitigate | Task 6: node type is `'jtbd'`, never `'claim'`, asserted by grep and by test; the reason is in the module header |
| T-353-14 | Tampering | YAML injection into the ruling document frontmatter | mitigate | Task 4: every string value passes through `escapeYamlDoubleQuoted`; the generated block is composed separately and spliced, never substituted into prose (T-275-13) |
| T-353-15 | Denial of service | `strict` mode refusing every write because the section will not resolve | mitigate | Task 7: `unresolved` is a distinct verdict from `mismatch`; `strict` refuses only a proven mismatch, and `flag` is the default |
| T-353-16 | Repudiation | a job mismatch landing silently | mitigate | Task 7: the mismatch rides the existing `mcp_client_event_logged` event with four named properties; no new `EVENT_TYPES` member, so the two already-RED exact-size assertions stay untouched |
| T-353-17 | Tampering | a stale or hand-edited ledger shipping as truth | mitigate | Tasks 3 and 10: the shipped file is a pure function of `--offline-seed`, declares `build_mode` and `jev_model: null`, and Step 2.4's offline `--check` fails a release whose ledger drifted from the shipped `plugin_version` |
| T-353-18 | Elevation of privilege | `doctor section-ruling --fix` rewriting authored prose in a real room | mitigate | Tasks 4 and 9: the generator never touches bytes below `mos:ruling:end`, and `check` sets `recoverable: false` outside the fixture prefix |
| T-353-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package. `353-RESEARCH.md`'s Package Legitimacy Audit records "not applicable, zero installs". Jev and Theo are reached with the Node built-in `fetch` and `lib/core/brain-client.cjs`, both already shipped |
</threat_model>

## Artifacts this plan produces

**New files:** `data/section-job-canon.json`, `scripts/build-section-command-ledger.cjs`, `data/section-command-ledger.json`, `docs/SECTION-COMMAND-LEDGER.md`, `lib/core/navigation/jtbd-anchor.cjs`, `lib/core/navigation/section-gate.cjs`, `lib/core/section-ruling-candidates.cjs`, `lib/core/doctor/section-ruling-module.cjs`, `tests/fixtures/353-jev-ledger-responses.json`, and the eight `tests/test-353-*.cjs` legs named in the tasks.

**New exports**
- `lib/core/section-registry.cjs`: `getSectionJob(slug)`, `VOCABULARY_EXTENSION_JOBS`.
- `lib/core/navigation/jtbd-anchor.cjs`: `mintJtbdAnchor(db, jobId)`, `JTBD_ANCHOR_ID(jobId)`.
- `lib/core/navigation/section-gate.cjs`: `resolveFocusSection(db, sessionId)`, `evaluateFilingGate(args)`, `GATE_MODES`.
- `lib/core/section-ruling-candidates.cjs`: `buildLedgerCandidates(args)`, `__resetLedgerCache()`.
- `lib/core/doctor/section-ruling-module.cjs`: `check(ctx)`, `fix(ctx)`.
- `lib/core/navigation.cjs` re-exports: `mintJtbdAnchor`, `JTBD_ANCHOR_ID`, `resolveFocusSection`, `evaluateFilingGate`.
- `lib/core/room-skeleton-scaffold.cjs`: `writeSectionContracts` keeps its name and gains the generator behavior; `result.ruling_regenerated[]` is new.

**New CLI flags:** `scripts/build-section-command-ledger.cjs` `--check`, `--offline-seed`, `--jev-fixture <path>`; `scripts/release.sh` `--no-ledger-check`.

**New data keys**
- `data/section-job-canon.json`: `ratification.ratified_by`, `ratification.ratified_at`, `vocabulary_extension_jobs[]`, `sections.<slug>.{job_id, secondary_job_id, vocabulary_extension, source, probe_confidence}`.
- `data/section-command-ledger.json`: `built_at`, `build_mode`, `plugin_version`, `theo_frameworks`, `jev_model`, `confidence_floor`, `floor_basis`, `floor_hit_rate`, `wall_ms`, `jev_calls`, `input_tokens`, `output_tokens`, `estimated_cost_usd`, `cost_basis`, `rows["<job_id>|<problem_type>|<stage>"][] = {command, framework, score, confidence, source}`.
- Ruling `CONTEXT.md` frontmatter: `icm_layer`, `job_id`, `ruling_fingerprint`, `generated_at`.
- `frontmatter-schemas.cjs`: a `CONTEXT.md` schema key.
- `data/doctor-modules.json`: one row with `id: "section-ruling"`.
- `mcp_client_event_logged` properties: `job_mismatch`, `declared_job`, `section_job`, `reason`.

**New node type:** `jtbd` (graph node type string, not an enum member of any frozen set). **No new edge type, epistemic type, event type, sensor, MCP tool or MCP parameter.**

<verification>
- `bash tests/run-all-353.sh` exits 0.
- `node tests/test-275-section-schema.cjs` exits 0 with the amended assertions.
- `node scripts/doctor.cjs --acceptance` exits 0 with 26 doctor modules registered and every runner present.
- `node scripts/build-command-registry.cjs --check`, `node scripts/build-connector-registry.cjs --check`, `node scripts/build-orchestration-projection.cjs --check` and `node scripts/build-section-command-ledger.cjs --check` all exit 0.
- `bash tests/run-all-310.sh` exits 0 after the re-pin.
- `node scripts/check-substrate.cjs` reports no new violation against the recorded pre-plan baseline.
- `node lib/core/node-insert-epistemic.test.cjs`, `node lib/memory/per-command-jtbd-derivation.test.cjs` and `node tests/test-245-priority-complete.cjs` all exit 0.
- The two pre-existing RED `EVENT_TYPES` tests and the pre-existing 341 step-hash staleness are re-run, reported unchanged, and never edited.
</verification>

<success_criteria>
- Every core and extended section has a declared job from one home, with the four vocabulary-gap sections declared rather than mislabeled, and the navigator's ratification slot empty and waiting.
- A section's CONTEXT.md carries six generated, fingerprinted parts above byte-preserved authored prose, and regenerating it twice is a no-op.
- 100 percent of claims and artifacts filed into a resolved section on a fixture room carry a `SOURCED_FROM` edge to a non-claim `jtbd:` anchor (criterion 3).
- `decide()` with the ledger filter stays inside 1200 ms, measured, with the producer's own share reported.
- A release fails on a stale ledger without any network call, and the audited opt-out names its consequence.
</success_criteria>

<output>
Create `.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-02-SUMMARY.md` when done. It must quote: the three measured latency numbers from Task 8; the ledger's row count and the count of rows produced by the ground-truth half versus the framework-matched half; the N of N anchor-edge number from Task 7; the verbatim before and after text of the replaced Phase 275 assertion; and the pre-existing reds re-run unchanged (the two `EVENT_TYPES` size assertions and the 341 step-hash staleness).
</output>
