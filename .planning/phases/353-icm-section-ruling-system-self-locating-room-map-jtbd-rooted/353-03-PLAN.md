---
phase: 353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted
plan: 03
type: execute
wave: 3
depends_on: ["353-02"]
files_modified:
  - evals/icm/README.md
  - evals/icm/checklists/
  - evals/icm/claude-judge-baseline.json
  - evals/icm/last-run.json
  - scripts/eval-icm-writers.cjs
  - scripts/doctor.cjs
  - tests/test-353-reach-hitrate.cjs
  - tests/test-353-grader-agreement.cjs
  - tests/test-353-tripwires.cjs
  - .planning/REQUIREMENTS.md
  - .planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-VALIDATION.md
  - CHANGELOG.md
autonomous: true
requirements: [ICM-353-03, RULE-22, RULE-23, RULE-24, RULE-25, RULE-26, RULE-27, RULE-28]
canon_parts: [7, 8, 9, 11]
user_setup:
  - service: typesafe-jev
    why: "The writer grading run needs the dev-time key. Every test leg that would call Jev skips loudly with a named detail when the key is absent; nothing on the release path ever calls the vendor."
    env_vars:
      - name: TYPESAFE_API_KEY
        source: "~/.secrets/typesafe.env, mode 600. Never committed, never printed, never read by lib/ or hooks/."

must_haves:
  truths:
    - "Each writer this phase ships is graded against a checklist derived from its own contract, on fixture rooms only, never on a real room"
    - "The grader's agreement with a once-authored Claude-judge baseline is measured and stated, with the metric named"
    - "Both top-3 hit rates, sensor order and ledger-fed candidates, are reported with their delta, and a regression is reported as a finding rather than tuned away"
    - "The release acceptance run asserts the eval is fresh without making a single vendor call"
    - "Three tripwires prove the vendor never reached lib/, hooks/ or a real room"
    - "Every RULE row closes with measured proof or is honestly left open"
  artifacts:
    - path: "scripts/eval-icm-writers.cjs"
      provides: "the dev-time writer eval runner and its De Stijl report"
    - path: "evals/icm/README.md"
      provides: "what each checklist grades and the fixture-only rule"
    - path: "evals/icm/claude-judge-baseline.json"
      provides: "the once-authored judge baseline agreement is measured against"
    - path: "evals/icm/last-run.json"
      provides: "the artifact the acceptance point reads instead of calling a vendor"
  key_links:
    - from: "scripts/doctor.cjs"
      to: "evals/icm/last-run.json"
      via: "the icm-ruling-eval-fresh acceptance point reads the file, never the vendor"
      pattern: "icm-ruling-eval-fresh"
    - from: "tests/test-353-reach-hitrate.cjs"
      to: "lib/core/section-ruling-candidates.cjs"
      via: "the paired run with and without tierCandidates"
      pattern: "buildLedgerCandidates"
    - from: "scripts/eval-icm-writers.cjs"
      to: "tests/fixtures/icm-rooms/"
      via: "the --room refusal outside this prefix"
      pattern: "icm-rooms"
---

<objective>
Grade the writers this phase ships, on fixtures only, and prove with numbers that the ruling system is better than what it replaced or say plainly that it is not.

Purpose: plans 01 and 02 built mechanisms. This plan is where the phase's claims become measured facts, and where the vendor is proven to be nowhere near a user machine.
Output: `evals/icm/` with per-writer checklists and a once-authored judge baseline, the dev-time eval runner with a De Stijl report, the paired hit-rate measurement for criterion 4, the agreement measurement for criterion 6, the `icm-ruling-eval-fresh` acceptance point, the three tripwires, and the phase close-out.
</objective>

## Decisions this plan implements (locked; see 353-CONTEXT.md)

| Id | What it binds here |
|----|--------------------|
| D-353-1 | This is plan 3 of 3: Jev grading last, after self-location and the ruling system exist |
| D-353-4 | Grading inputs are fixture rooms under `tests/fixtures/icm-rooms/` only, never a real room; the runner uses the dev-time key and never runs in a user hook |
| D-353-9 | The fleet half of criterion 1 stays a navigator act; this plan automates nothing against a real room |
| D-353-10 | Canon Parts 7 (port the spike's renderer and client rather than authoring new ones), 8 (structure only crosses to Jev), 9 (grading is bookkeeping, never a truth claim), 11 (no new invocable surface; the eval runner is a script) |
| R-353-F | Requirement ids `RULE-22..28` here, closed with measured proof in Task 7 together with RULE-01..21 and RULE-29; `ICM-353-03` is the roadmap working label |
| R-353-N | Criterion 4 is the paired run over the same labeled turns with both numbers and the delta reported; a regression is a finding, not a tuning target |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-CONTEXT.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-RESEARCH.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-PATTERNS.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-VALIDATION.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-01-SUMMARY.md
@.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-02-SUMMARY.md
@.claude/skills/spike-findings-MindrianOS-Plugin/SKILL.md
</context>

## Standing rules for every task in this plan

1. Rules 1 through 9 of `353-01-PLAN.md` and rules 2 through 7 of `353-02-PLAN.md` apply unchanged.
2. Fixture rooms only. `scripts/eval-icm-writers.cjs` refuses any `--room` that does not resolve under `tests/fixtures/icm-rooms/`, and contains no `~/MindrianRooms` path (D-353-4).
3. A leg that needs the vendor and has no key SKIPS LOUDLY with a named detail. It never silently passes and never quietly returns a zero.
4. Only measured numbers are quoted as facts. A vendor-claimed rate is labeled as vendor-claimed every time it appears.
5. The labeled turn set at `evals/icm/cases/turns.json` was authored in plan 01, before any ledger existed. It is READ here, never re-labeled, and a task that adjusts a label to improve a score is a falsified measurement.

<tasks>

<task type="auto">
  <name>Task 1: evals/icm scaffold and the five per-writer checklists</name>
  <read_first>
    - `evals/eureka/README.md` and the `evals/eureka/` directory listing (the family shape: README, `cases/`, report `.md` plus `.json` pairs, a baseline JSON)
    - `docs/superpowers/specs/2026-09-17-icm-section-ruling-system-design.md` section 3 Unit 3 (the five writers and what is code-checkable versus what needs a Jev question)
    - `lib/core/room-map.cjs`, `lib/core/room-skeleton-scaffold.cjs::writeSectionContracts`, `lib/mcp/tools/views.cjs::fileArtifact` and `lib/mcp/tools/claim.cjs` (each writer's own shipped contract, which is what its checklist is derived from)
    - `evals/icm/cases/turns.json` (authored in plan 01; read only)
  </read_first>
  <files>evals/icm/README.md, evals/icm/checklists/scaffolder.md, evals/icm/checklists/ruling-writer.md, evals/icm/checklists/minto-refresher.md, evals/icm/checklists/claim-filer.md, evals/icm/checklists/entity-extractor.md</files>
  <action>
Create `evals/icm/README.md` stating: which five writers are graded; that every checklist item is derived from that writer's own shipped contract and cites the file and function it comes from; the fixture-only rule with the reason; that the vendor half of the grading runs only with the dev-time key from the operator's shell; and the metric this phase uses for criterion 6 (see Task 4) named explicitly.

Create one checklist per writer under `evals/icm/checklists/`. Each item is a single checkable proposition with a `kind` of either `code` (a deterministic assertion the runner computes) or `jev` (a typed question), and each cites the contract it comes from:
- `scaffolder.md`: the self block exists on every blocked kind and on no artifact folder; the block's fingerprint equals the map's; a missing root ROOM.md was created from the identity template. All `code`.
- `ruling-writer.md`: the six parts are present in order; the frontmatter carries the four keys; `ruling_fingerprint` recomputes; authored prose below the end marker is byte-preserved (all `code`); the methodology sequence fits the section's job (one `jev` Score question over the framework name, the JTBD statement and the glossary line, nothing else).
- `minto-refresher.md`: the governing thought is present and non-template (`code`); the governing thought summarizes the section's artifacts (one `jev` Noul question).
- `claim-filer.md`: the anchor edge is present; `serves_jtbd` was honored or the mismatch was disclosed (both `code`); the `epistemic_type` chosen is plausible for the claim shape (one `jev` question over the epistemic enum and a structural summary, never the claim text).
- `entity-extractor.md`: no template words appear in the extracted entity (`code`); the entity names a concept rather than a sentence fragment (one `jev` question).

Every `jev` item names, in its own line, exactly which fields cross the wire, and that line must be consistent with the Part 8 ceiling: a name, a JTBD statement, a glossary line, an enum. If a checklist item cannot be graded without sending room prose, it is a `code` item or it does not exist.
  </action>
  <acceptance_criteria>
    - `ls evals/icm/checklists/*.md | wc -l` returns 5
    - `grep -l "kind: code" evals/icm/checklists/*.md | wc -l` returns 5
    - `grep -c "fixture rooms only" evals/icm/README.md` returns at least 1
    - `grep -rn "room content" evals/icm/checklists/*.md | wc -l` returns at least 5 (every `jev` item names what does and does not cross)
    - `grep -rl "$(printf '\xe2\x80\x94')" evals/icm` returns no files
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test $(ls evals/icm/checklists/*.md | wc -l) -eq 5 && grep -q "fixture rooms only" evals/icm/README.md'</automated>
  </verify>
  <done>Each of the five writers has a checklist derived from its own contract, with the code half and the vendor half separated and the egress ceiling stated per item.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/eval-icm-writers.cjs - the fixture-only runner and its De Stijl report</name>
  <read_first>
    - `.planning/spikes/002-jev-section-framework-ranker/report.cjs` (the header contract: static De Stijl HTML, no CDN, no deps, inline CSS, hyphens only; the palette `--red:#D40920`, `--blue:#1356A2`, `--yellow:#F7D842`, `--black:#111`, `--white:#FAFAF7`, the 3px and 6px black rules, IBM Plex Sans)
    - `.planning/spikes/002-jev-section-framework-ranker/rank.cjs` (the `jev(key, body)` and `pool(items, n, fn)` helpers and the backoff)
    - `scripts/build-section-command-ledger.cjs` (the Jev client and `assertEgressCeiling` written in plan 02; reuse the same shapes rather than authoring a second client idiom)
    - `scripts/check-render-coverage.cjs` lines 85-135 (the render entry-point discovery rule) and `tests/test-209-declared-implies-wired.cjs` line 227 (`assert.equal(cjsEntriesBefore.length, 16, ...)`)
    - `353-CONTEXT.md` D-353-4; `353-RESEARCH.md` Seam 9 and Pitfall 12
  </read_first>
  <files>scripts/eval-icm-writers.cjs, evals/icm/last-run.json, evals/icm/last-run.html, tests/test-353-tripwires.cjs</files>
  <behavior>
    - `--room <path>` resolves the path and REFUSES with a named reason and a non-zero exit unless the resolved path is under the resolved `tests/fixtures/icm-rooms` prefix. A relative path, a `..` traversal and a symlink that escapes the prefix are all refused.
    - The source file contains no `~/MindrianRooms` literal and no home-directory room path.
    - The code half of every checklist runs with no key and no network, and reports per-item pass or fail.
    - The vendor half runs only when the key resolves from `~/.secrets/typesafe.env`; with no key, the runner prints `SKIP: jev half -- no TYPESAFE_API_KEY` and still completes the code half with a report.
    - Every outgoing payload passes the same egress ceiling assertion the ledger builder uses; a payload carrying room prose throws before any `fetch`.
    - The runner writes `evals/icm/last-run.json` with `{ run_at, plugin_version, rooms: [...], items: [...], code_pass, code_total, jev_pass, jev_total, agreement, jev_model, key_present }` and a De Stijl HTML report next to it. The key itself never appears in either file.
    - Every string interpolated into the HTML is escaped, so a fixture room name containing `<script>` cannot produce executable HTML.
  </behavior>
  <action>
Create `scripts/eval-icm-writers.cjs`. CJS, `process.argv` switch-case router, zero new dependencies, native `fetch`. Port the De Stijl renderer from the spike's `report.cjs` rather than authoring a new palette; keep its no-CDN, inline-CSS, hyphens-only contract and say so in the header.

Implement the fixture-prefix guard as the FIRST thing the router does, before any file read, computed by resolving `tests/fixtures/icm-rooms` against the plugin root and comparing resolved prefixes, never by substring match. Refuse with `eval-icm-writers: refused -- <path> is outside tests/fixtures/icm-rooms (D-353-4: never a real room)`.

Reuse plan 02's Jev client shape and its `assertEgressCeiling`; if that function is not exported from the build script, export it there rather than writing a second copy, and say in the header that the two callers share one ceiling.

Before naming the file final, run `node scripts/check-render-coverage.cjs` and `node tests/test-209-declared-implies-wired.cjs`. If the HTML emission causes this script to be discovered as a 17th render entry point, do NOT rename the file and do NOT raise the 16 literal: declare the script excluded through the existing connector exclusion path (`connectors.excluded: true` with a stated reason) exactly as a render-only surface already does, and record which route was needed in the SUMMARY.
  </action>
  <verify>
    <automated>node tests/test-353-tripwires.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before the runner exists, `node tests/test-353-tripwires.cjs` exits non-zero naming the missing `scripts/eval-icm-writers.cjs`. Record the line in the commit body.
    - `node scripts/eval-icm-writers.cjs --room /tmp 2>&1 | grep -c "refused"` returns 1 and the command exits non-zero
    - `node scripts/eval-icm-writers.cjs --room tests/fixtures/icm-rooms/alpha-room` exits 0 and writes `evals/icm/last-run.json`
    - `grep -cE "MindrianRooms|os\.homedir\(\)" scripts/eval-icm-writers.cjs` returns 0
    - `grep -rn "api.typesafe.ai" lib hooks | wc -l` returns 0
    - `node -e "const r=require('./evals/icm/last-run.json'); if(typeof r.code_total!=='number'||typeof r.key_present!=='boolean') process.exit(1); if(/TYPESAFE/.test(JSON.stringify(r))) process.exit(1)"` exits 0
    - `node scripts/check-render-coverage.cjs` exits 0 and `node tests/test-209-declared-implies-wired.cjs` exits 0
  </acceptance_criteria>
  <done>The writers can be graded on fixtures with one command, the vendor half degrades honestly without a key, and the runner cannot be pointed at a real room.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the paired top-3 hit-rate measurement (criterion 4)</name>
  <read_first>
    - `lib/core/section-ruling-candidates.cjs` (plan 02's producer and its confidence gate)
    - `lib/workflow/f-selector-ranker.cjs` lines 905-975 (`rankForSelector`'s args, `MAX_K` 3, and the `tierCandidates` no-op behavior when the arg is absent)
    - `evals/icm/cases/turns.json` (the labeled turns authored in plan 01, read only)
    - `353-CONTEXT.md` R-353-N (both numbers, the delta stated, and the floor written into the ledger header with the hit rate it produced)
  </read_first>
  <files>tests/test-353-reach-hitrate.cjs</files>
  <behavior>
    - The test runs `rankForSelector` twice over the SAME labeled turns: once with `tierCandidates` absent (today's order, the baseline) and once with the ledger candidates.
    - Top-3 hit rate is the fraction of turns whose labeled `expected_commands` intersect the returned top 3.
    - Both numbers and their delta are PRINTED, in a line the SUMMARY can quote verbatim.
    - The assertion is `withLedger >= baseline`. A regression fails the test and is reported as a real finding; the fix is never to loosen the assertion or to re-label a turn.
    - The labeled file's `_labeling_note` is asserted present, so the honesty condition travels with the measurement.
    - No vendor call, no db write, no network.
  </behavior>
  <action>
Add `tests/test-353-reach-hitrate.cjs`. Read the labeled turns, build the ledger candidates through `buildLedgerCandidates` for each turn's `(job_id, problem_type, stage)`, and run the paired comparison. Print exactly three lines: `baseline top-3 hit rate: <n>`, `with-ledger top-3 hit rate: <n>`, `delta: <n>`.

State in the file header, in two sentences, why the labels were authored in plan 01 and why re-labeling them here would falsify the result. If the shipped ledger is the offline seed (`build_mode: "offline-seed"`, every confidence null), print that fact on its own line so the number is read for what it is: a ground-truth-ordering measurement, not a vendor-scored one. Do not skip the leg in that case; the comparison is still meaningful and criterion 4 asks for both numbers, not for a vendor.
  </action>
  <verify>
    <automated>node tests/test-353-reach-hitrate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - RED first: before plan 02's producer is importable, `node tests/test-353-reach-hitrate.cjs` exits non-zero naming the missing module. Record the line in the commit body.
    - `node tests/test-353-reach-hitrate.cjs` exits 0 and prints all three lines in the stated format
    - `node tests/test-353-reach-hitrate.cjs 2>&1 | grep -c "build_mode"` returns at least 1
    - `git diff --stat evals/icm/cases/turns.json` reports no change
    - `grep -cE "fetch\(|api\.typesafe" tests/test-353-reach-hitrate.cjs` returns 0
  </acceptance_criteria>
  <done>Criterion 4 has both numbers and a delta, measured against labels that predate the thing being measured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: the Claude-judge baseline and the agreement metric (criterion 6)</name>
  <read_first>
    - `evals/eureka/212-critic-baseline.json` (the shape of a once-authored baseline in this repo)
    - `evals/icm/checklists/*.md` (the five checklists from Task 1; the baseline holds one verdict per `jev` item)
    - `scripts/eval-icm-writers.cjs` (the runner's per-item output shape from Task 2)
    - `353-RESEARCH.md` Validation Architecture row 6 (the planner picks the metric and states it)
  </read_first>
  <files>evals/icm/claude-judge-baseline.json, evals/icm/README.md, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs</files>
  <behavior>
    - The metric is EXACT AGREEMENT: the fraction of graded items on which the runner's verdict equals the baseline's verdict. It is stated by name in `evals/icm/README.md`, in the baseline file itself, and in the test output. Spearman is explicitly not used, because the checklist verdicts are categorical rather than ranked.
    - `evals/icm/claude-judge-baseline.json` is authored ONCE, carries `{ authored_at, authored_by: "claude-judge", method, items: [{ checklist, item_id, verdict, rationale }] }`, and is never regenerated by the runner.
    - `agreement >= 0.8` is asserted.
    - With no key, the test prints `SKIP: grader agreement -- no TYPESAFE_API_KEY (criterion 6 unmeasured this run)` and exits 0. It never reports agreement as 1.0, as 0, or as passed when it did not run.
    - The runner writes the computed `agreement` into `evals/icm/last-run.json`.
  </behavior>
  <action>
Author `evals/icm/claude-judge-baseline.json` once, by grading each `jev` checklist item against the fixture rooms by hand, with a one-line rationale per item. Record the authoring method in the file. Do not let the runner write this file; a baseline the grader can rewrite measures nothing.

Add the agreement computation to `scripts/eval-icm-writers.cjs` and add `tests/test-353-grader-agreement.cjs` covering the assertion, the loud skip, and the "never silently green" property (the skip path must be distinguishable from a pass in the exit output, not just in a comment).

Write the metric name into `evals/icm/README.md` in the sentence Task 1 reserved for it.
  </action>
  <verify>
    <automated>node tests/test-353-grader-agreement.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-353-grader-agreement.cjs` exits 0
    - With `TYPESAFE_API_KEY` unset, `node tests/test-353-grader-agreement.cjs 2>&1 | grep -c "SKIP: grader agreement"` returns 1 and the exit code is 0
    - `node -e "const b=require('./evals/icm/claude-judge-baseline.json'); if(!b.authored_at||!Array.isArray(b.items)||b.items.length===0) process.exit(1); for(const i of b.items){ if(!i.rationale) process.exit(1); }"` exits 0
    - `grep -c "exact agreement" evals/icm/README.md` returns at least 1 and `grep -ci "spearman" evals/icm/README.md` returns at least 1 (the rejected alternative is named)
    - `grep -cE "claude-judge-baseline" scripts/eval-icm-writers.cjs` returns at least 1 and `grep -cE "writeFileSync\([^)]*claude-judge-baseline" scripts/eval-icm-writers.cjs` returns 0
  </acceptance_criteria>
  <done>Criterion 6 has a named metric, a baseline the grader cannot rewrite, and a skip path that cannot be mistaken for a pass.</done>
</task>

<task type="auto">
  <name>Task 5: the icm-ruling-eval-fresh acceptance point</name>
  <read_first>
    - `scripts/doctor.cjs` lines 738-758 (the `--acceptance` hard-abort contract and exit codes) and 1857-1905 (the `capability-ledger-fresh` point in full: `severity`, `applies_to`, the in-process `require` plus `check({})`, the `DOCTOR_TEST_FAIL_POINT` arm, and the `DOCTOR_SKIP_<NAME>=1` escape)
    - `tests/test-doctor-acceptance-self-coverage.cjs` header lines 17-27 (the per-fixture pass/fail signature across five scaffolded broken-state fixtures, and the absence of any point-count assertion)
    - `evals/icm/last-run.json` (the artifact this point reads)
    - `353-RESEARCH.md` "The acceptance point (ICM-353-03)"
  </read_first>
  <files>scripts/doctor.cjs</files>
  <action>
Add a 22nd acceptance point modeled byte-for-byte on `capability-ledger-fresh`: `id: 'icm-ruling-eval-fresh'`, `severity: 'blocker'`, `applies_to: ['pre-tag', 'full']`, a `DOCTOR_TEST_FAIL_POINT === 'icm-ruling-eval-fresh'` arm, and a `DOCTOR_SKIP_ICM_EVAL=1` hermetic-CI escape.

What it asserts: `evals/icm/last-run.json` parses; its `plugin_version` is not more than one release behind the running version; and, when `agreement` is a number, it is at least 0.8. What it must NEVER do: call Jev, call Theo, spawn the eval runner, or read a key. It reads one JSON file and returns.

Degrade rules, both required so the five self-coverage fixtures stay green: a MISSING `evals/icm/last-run.json` returns `ok: true` with a named detail saying the eval has not been run on this machine; an `agreement` of `null` (the no-key run) returns `ok: true` with a named detail saying criterion 6 was unmeasured. Neither degrade is silent and neither reports a number it does not have.
  </action>
  <verify>
    <automated>node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/doctor.cjs --acceptance` exits 0
    - `node scripts/doctor.cjs --acceptance 2>&1 | grep -c "icm-ruling-eval-fresh"` returns at least 1
    - `DOCTOR_SKIP_ICM_EVAL=1 node scripts/doctor.cjs --acceptance` exits 0
    - `node tests/test-doctor-acceptance-self-coverage.cjs` exits 0
    - `grep -cE "typesafe|eval-icm-writers\.cjs'" scripts/doctor.cjs` returns 0
    - With `evals/icm/last-run.json` temporarily renamed, `node scripts/doctor.cjs --acceptance` still exits 0 and its output names the missing file
  </acceptance_criteria>
  <done>The release train checks the eval is fresh by reading one local file, and degrades honestly on every machine that never ran it.</done>
</task>

<task type="auto">
  <name>Task 6: the three tripwires</name>
  <read_first>
    - `353-VALIDATION.md` "Tripwires this phase ships" (the three exact propositions)
    - `tests/run-all-345.sh` (the STRAT-11 tripwire precedent: a source scan asserted as a test leg, comment lines excluded)
    - `scripts/check-substrate.cjs` lines 245-247 (`isPureLineComment`, why a comment mentioning a banned literal is safe and why a count must exclude comments)
    - `hooks/hooks.json` (the SessionStart entries and their timeouts; nothing in this phase belongs in a hook)
  </read_first>
  <files>tests/test-353-tripwires.cjs</files>
  <action>
Write `tests/test-353-tripwires.cjs` with exactly three legs, each a source scan with comment lines excluded so header prose cannot self-invalidate the gate:
1. No file under `lib/` contains the literal `api.typesafe.ai` in a non-comment line. A hit means the vendor leaked into the turn path.
2. No file under `hooks/` references `eval-icm-writers` or `build-section-command-ledger` in a non-comment line. A hit means a vendor-touching script reached a user machine's hook budget.
3. `scripts/eval-icm-writers.cjs` contains no `~/MindrianRooms` path and no `os.homedir()` room resolution, and a `--room` outside `tests/fixtures/icm-rooms` is refused (assert by invoking the script with a temporary directory and checking the non-zero exit and the refusal string).

Use the `grep -v '^\s*#'` / comment-strip discipline for each count and print the scanned file count per leg so a silent zero-file scan cannot pass as a green.
  </action>
  <verify>
    <automated>node tests/test-353-tripwires.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-353-tripwires.cjs` exits 0 and prints three lines each containing `files scanned:` with a count greater than 0
    - `node tests/test-353-tripwires.cjs 2>&1 | grep -c "refused"` returns at least 1
    - Temporarily adding the literal `api.typesafe.ai` to a non-comment line of a scratch file under `lib/` makes the first leg fail; the scratch file is removed in the same task and the negative-control result is recorded in the commit body
    - `bash tests/run-all-353.sh` exits 0
  </acceptance_criteria>
  <done>Three source-level facts prove the vendor never reached the turn path, a hook, or a real room, and each gate is proven to actually scan something.</done>
</task>

<task type="auto">
  <name>Task 7: phase close-out - RULE rows with measured proof, the validation map, and the changelog</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 2426-2470 (the Phase 349 NOTIFY family: the section header paragraph and the `- [x] **NOTIFY-NN**: ... **Measured:** (date) ...` row shape) and the `## Traceability` paragraph at 2619 onward
    - `.planning/phases/353-.../353-VALIDATION.md` (the per-task verification map this task closes, and the `nyquist_compliant: false` frontmatter key)
    - `CHANGELOG.md` lines 1-6 (the `## [Unreleased] -- v2.0.0-beta.48 (in progress)` heading and its `### Added` list)
    - `353-01-SUMMARY.md` and `353-02-SUMMARY.md` (the measured numbers every row quotes)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-VALIDATION.md, CHANGELOG.md</files>
  <action>
Close every `RULE-01` through `RULE-29` row in `.planning/REQUIREMENTS.md` (RULE-29 is plan 01 Task 6's birth-time job declaration, minted after the first plan-checker pass and closed here with the same measured-proof rule). A row flips to `- [x]` only with a `**Measured:** (2026-09-DD)` block quoting the exact command and its exact output, in the NOTIFY-01 voice. A row that did not actually get measured on this machine STAYS `- [ ]` with a one-line reason, because an unmeasured tick is the failure this register exists to prevent. Quote the real numbers: the legE token number, the fleet per-kind counts, the three `decide()` latencies, the anchor-edge N of N, both top-3 hit rates and the delta, the agreement value or its honest skip, and the ledger's row counts by join half.

Update `353-VALIDATION.md`: fill the per-task verification map with one row per task across all three plans (task id, plan, wave, RULE ids, threat ref, secure behavior, test type, automated command, file-exists mark, status), and set `nyquist_compliant: true` in the frontmatter ONLY if every task's named `<automated>` command actually ran and exited 0 during execution. Otherwise leave it false and name what did not run.

Add the CHANGELOG bullet under `## [Unreleased] -- v2.0.0-beta.48 (in progress)` in the `### Added` list: one entry for the ICM section ruling system naming the room map and `icm_self` blocks, the section job canon with the four vocabulary-extension members, the shipped relevance ledger and its offline release check, the generated six-part ruling document, the filing gate with `jtbd:` anchor edges, the two doctor modules, and the fixture-only writer eval. Hyphens only. Do not bump any version by hand; `scripts/release.sh` owns the five-place lockstep.

Report, do not fix, in the SUMMARY and in the close-out: the two pre-existing RED `EVENT_TYPES` exact-size assertions and the pre-existing `tests/fixtures/341-release-step-block-hashes.txt` staleness.
  </action>
  <verify>
    <automated>bash tests/run-all-353.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RULE-" .planning/REQUIREMENTS.md` returns at least 29
    - `grep -c "^- \[x\] \*\*RULE-" .planning/REQUIREMENTS.md` plus `grep -c "^- \[ \] \*\*RULE-" .planning/REQUIREMENTS.md` equals 29
    - Every `- [x] **RULE-` row is followed within 12 lines by a line containing `**Measured:**` (assert by script, not by eye)
    - `grep -c "nyquist_compliant" .planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-VALIDATION.md` returns 1
    - `grep -A 4 "## \[Unreleased\]" CHANGELOG.md | grep -c "ICM section ruling"` returns 1
    - `grep -rl "$(printf '\xe2\x80\x94')" CHANGELOG.md .planning/REQUIREMENTS.md` returns no files
    - `bash tests/run-all-353.sh` exits 0 and `node scripts/doctor.cjs --acceptance` exits 0
  </acceptance_criteria>
  <done>Every requirement is closed with a quoted measurement or honestly left open, the validation map is filled, and the release notes name what shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> eval runner | The dev-time key and the `--room` argument both enter here |
| eval runner -> Jev (`api.typesafe.ai`) | Dev-time only, structure only, never room prose |
| fixture disk -> HTML report | Fixture room names and checklist strings become HTML |
| eval artifact -> release train | `evals/icm/last-run.json` is read by a blocker-severity acceptance point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-353-19 | Information disclosure | the eval runner pointed at a real room | mitigate | Task 2: resolved-prefix containment against `tests/fixtures/icm-rooms` as the first router action, refusing relative paths, `..` traversal and escaping symlinks; Task 6 asserts the refusal from outside |
| T-353-20 | Information disclosure | room prose crossing to Jev in a grading payload | mitigate | Tasks 1 and 2: every `jev` checklist item names its fields, and the shared `assertEgressCeiling` throws before any `fetch` on a payload that carries anything else |
| T-353-21 | Information disclosure | the dev-time key reaching a committed file, a log or the report | mitigate | Task 2: the key is read from `~/.secrets/typesafe.env`, never printed, never written into `last-run.json` or the HTML; an acceptance criterion greps the artifact for the token name |
| T-353-22 | Tampering | HTML injection through a fixture room name into the report | mitigate | Task 2: every interpolated string is escaped; the renderer ships no CDN, no external script and no eval |
| T-353-23 | Repudiation | a grading run silently reported as passed when it never ran | mitigate | Tasks 4 and 5: the no-key path prints a named SKIP that is distinguishable from a pass, `agreement: null` is carried through to the acceptance point, and the point reports "unmeasured" rather than a number |
| T-353-24 | Tampering | a measurement improved by re-labeling the turn set or rewriting the baseline | mitigate | Task 3 asserts `evals/icm/cases/turns.json` is unchanged; Task 4 asserts the runner never writes the baseline file |
| T-353-25 | Denial of service | a vendor call on the release path stalling a release | mitigate | Task 5: the acceptance point reads one local JSON file, never spawns the runner and never calls a vendor; Task 6 leg 2 proves no hook references either vendor-touching script |
| T-353-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package. `353-RESEARCH.md`'s Package Legitimacy Audit records "not applicable, zero installs"; the runner uses the Node built-in `fetch` and no client library |
</threat_model>

## Artifacts this plan produces

**New files:** `evals/icm/README.md`, `evals/icm/checklists/{scaffolder,ruling-writer,minto-refresher,claim-filer,entity-extractor}.md`, `evals/icm/claude-judge-baseline.json`, `evals/icm/last-run.json`, `evals/icm/last-run.html`, `scripts/eval-icm-writers.cjs`, `tests/test-353-reach-hitrate.cjs`, `tests/test-353-grader-agreement.cjs`, `tests/test-353-tripwires.cjs`.

**New CLI flags:** `scripts/eval-icm-writers.cjs` `--room <path>`, `--out <path>`, `--code-only`.

**New data keys:** `evals/icm/last-run.json` `run_at`, `plugin_version`, `rooms[]`, `items[]`, `code_pass`, `code_total`, `jev_pass`, `jev_total`, `agreement`, `jev_model`, `key_present`.

**New acceptance point:** `icm-ruling-eval-fresh` in `scripts/doctor.cjs`, severity `blocker`, `applies_to: ['pre-tag', 'full']`, with a `DOCTOR_SKIP_ICM_EVAL=1` escape.

**New requirement rows:** `RULE-01` through `RULE-28` in `.planning/REQUIREMENTS.md`, closed here with measured proof.

**No new command, agent, pipeline, skill, MCP tool, sensor, edge type, epistemic type or event type.**

<verification>
- `bash tests/run-all-353.sh` exits 0 with every leg PASSED or loudly SKIPPED with a named reason.
- `node scripts/doctor.cjs --acceptance` exits 0 including the new point, and `DOCTOR_SKIP_ICM_EVAL=1` also exits 0.
- `node tests/test-doctor-acceptance-self-coverage.cjs`, `node tests/test-209-declared-implies-wired.cjs` and `node scripts/check-render-coverage.cjs` all exit 0.
- `node scripts/verify-release` reports no new failure against the pre-plan baseline.
- The three tripwires pass with a non-zero scanned-file count each, and the negative control for leg 1 is recorded.
- The pre-existing reds are re-run and reported unchanged.
</verification>

<success_criteria>
- Five writers are graded against contract-derived checklists on fixture rooms only.
- Criterion 4 reports both top-3 hit rates and their delta, against labels authored before the ledger existed.
- Criterion 6 reports exact agreement against a once-authored baseline, or states plainly that it was unmeasured.
- The release train asserts eval freshness with zero vendor bytes.
- Every RULE row is closed with a quoted measurement or left open with a stated reason.
</success_criteria>

<output>
Create `.planning/phases/353-icm-section-ruling-system-self-locating-room-map-jtbd-rooted/353-03-SUMMARY.md` when done. It must quote: both hit rates and the delta; the agreement value or the named skip; the tripwire scanned-file counts and the leg-1 negative control; which RULE rows closed and which stayed open with their reasons; and the pre-existing reds re-run unchanged.
</output>
