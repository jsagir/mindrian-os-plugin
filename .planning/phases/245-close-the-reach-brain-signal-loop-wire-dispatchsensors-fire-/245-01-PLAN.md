---
phase: 245-close-the-reach-brain-signal-loop-wire-dispatchsensors-fire-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/sensors/sensor-priority.cjs
  - lib/core/insight-sensors.cjs
  - scripts/build-connector-registry.cjs
  - tests/run-all-245.sh
  - tests/test-245-sensor-id-stamped.cjs
  - tests/test-245-priority-complete.cjs
autonomous: true
requirements: [REQ-4]

must_haves:
  truths:
    - "Every reach returned by dispatchSensors carries evidence.sensor_id naming the sensor that produced it"
    - "A frozen SENS_PRIORITY doctrine table ranks every registered sensor, sourced from SENSOR_REGISTRY and not from command frontmatter"
    - "A sensor registered without a SENS_PRIORITY entry fails the build closed"
    - "bash tests/run-all-245.sh discovers and runs every tests/test-245-* file by glob and fails when discovery is empty"
  artifacts:
    - path: "lib/core/sensors/sensor-priority.cjs"
      provides: "SENS_PRIORITY frozen ordered table + sensorPriorityRank lookup"
      exports: ["SENS_PRIORITY", "sensorPriorityRank"]
    - path: "lib/core/insight-sensors.cjs"
      provides: "SENSOR_REGISTRY_IDS parallel identity array + central sensor_id stamp in dispatchSensors"
      contains: "SENSOR_REGISTRY_IDS"
    - path: "scripts/build-connector-registry.cjs"
      provides: "sensor-side SENS_PRIORITY completeness gate under --check"
      contains: "SENS_PRIORITY"
    - path: "tests/run-all-245.sh"
      provides: "glob-discovery phase runner with a found-eq-0 guard"
  key_links:
    - from: "lib/core/insight-sensors.cjs"
      to: "lib/core/sensors/sensor-priority.cjs"
      via: "require + completeness assertion"
      pattern: "sensor-priority"
    - from: "scripts/build-connector-registry.cjs"
      to: "lib/core/insight-sensors.cjs"
      via: "SENSOR_REGISTRY_IDS enumeration at check time"
      pattern: "SENSOR_REGISTRY_IDS"
---

<objective>
Give every fired candidate reach a stable sensor identity and mint the doctrine-authored
priority table that Requirement 4's tie-break will sort on (D-20, D-21), with a fail-closed
build gate that enumerates the sensor side (D-22 as CORRECTED by 245-RESEARCH.md F-3, NOT the
command-frontmatter `sensor_index`).

Purpose: today `rankFiredCandidates` breaks a same-`reach_id` collision by `a.index - b.index`,
which is `SENSOR_REGISTRY` file order. 12 of 18 registered sensors can fire `context_block`, so
"which sensor's payload wins" is decided by whoever edited the registry array last. This plan
supplies the two things the tie-break needs before it can exist: a per-reach sensor identity, and
an ordered doctrine table covering every registered sensor.

Output: `lib/core/sensors/sensor-priority.cjs` (new), `SENSOR_REGISTRY_IDS` + a central
`sensor_id` stamp in `dispatchSensors`, a `--check` completeness gate, and the phase test runner.

This plan does NOT change any ranking behavior. The comparator branch that consumes
`SENS_PRIORITY` lands in 245-07.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/245-close-the-reach-brain-signal-loop-wire-dispatchsensors-fire-/245-SPEC.md
@.planning/phases/245-close-the-reach-brain-signal-loop-wire-dispatchsensors-fire-/245-CONTEXT.md
@.planning/phases/245-close-the-reach-brain-signal-loop-wire-dispatchsensors-fire-/245-RESEARCH.md
@lib/core/insight-sensors.cjs
@lib/core/sensors/sensor-types.cjs
</context>

## Artifacts this phase produces

Symbols created by Phase 245 (this plan's share is marked NEW HERE; the rest are listed so a
source-grounding pass does not flag them as drift):

| Symbol | Kind | Home | Plan |
|--------|------|------|------|
| `SENS_PRIORITY` | frozen ordered array | `lib/core/sensors/sensor-priority.cjs` | 01 (NEW HERE) |
| `sensorPriorityRank(sensorId)` | function | `lib/core/sensors/sensor-priority.cjs` | 01 (NEW HERE) |
| `SENSOR_REGISTRY_IDS` | frozen array | `lib/core/insight-sensors.cjs` | 01 (NEW HERE) |
| `evidence.sensor_id` | reach evidence field | produced by `dispatchSensors` | 01 (NEW HERE) |
| `tests/run-all-245.sh` | file | `tests/` | 01 (NEW HERE) |
| `commitDispatched(roomDir, sections)` | function | `lib/core/brain-derivation-queue.cjs` | 02 |
| `_isProvablyEmptyPayload(payload)` | function | `lib/core/part8-egress-guard.cjs` | 03 |
| `empty_payload` | classify class token | `lib/core/part8-egress-guard.cjs` | 03 |
| `VERB_REACH_AFFINITY` | frozen constant | `lib/core/verb-reach-affinity.cjs` | 04 |
| `verbReachAffinity(verb)` | function | `lib/core/verb-reach-affinity.cjs` | 04 |
| `scripts/derive-verb-reach-affinity.cjs` | build-time script | `scripts/` | 04 |
| `trace.brain_pattern_verb` | decision-trace field | `lib/core/navigation-engine.cjs` | 05 |
| `trace.brain_pattern_verb_confidence` | decision-trace field | `lib/core/navigation-engine.cjs` | 05 |
| `sensorPerspectiveLock` (SENS-17) | sensor function | `lib/core/sensors/sensor-perspective-lock.cjs` | 06 |
| `buildSignalNudges(input)` | function | `lib/workflow/reach-hedge-ranker.cjs` | 07 |
| `FUSION_CEILING` | constant | `lib/workflow/reach-hedge-ranker.cjs` | 07 |
| `NUDGE_FRACTION_CAP` | constant | `lib/workflow/reach-hedge-ranker.cjs` | 07 |
| `SENSOR_TOP_FRACTION` | constant | `lib/workflow/reach-hedge-ranker.cjs` | 07 |
| `SENSOR_OTHER_FRACTION` | constant | `lib/workflow/reach-hedge-ranker.cjs` | 07 |
| `BRAIN_VERB_FRACTION` | constant | `lib/workflow/reach-hedge-ranker.cjs` | 07 |
| `DEFAULT_FUSION_BASE` | constant | `lib/workflow/reach-hedge-ranker.cjs` | 07 |
| `composeDialReachScores(...)` | function | `scripts/intent-classifier.cjs` | 08 |

<tasks>

<task type="auto">
  <name>Task 1: Mint the SENS_PRIORITY doctrine table module</name>
  <files>lib/core/sensors/sensor-priority.cjs</files>
  <read_first>
    - lib/core/sensors/sensor-types.cjs (TRIGGER_TIERS ordering doctrine at the `Phase 172-07` block, `isContextTier` / `isFallbackTier`, and the `makeReach` evidence contract you must stay compatible with)
    - lib/core/insight-sensors.cjs lines 696-793 (the `SENSOR_REGISTRY` array and its canonical order; this is the authoritative sensor set, NOT the `lib/core/sensors/` directory listing)
    - .planning/phases/245-close-the-reach-brain-signal-loop-wire-dispatchsensors-fire-/245-CONTEXT.md decisions D-19, D-20, D-22, D-23
  </read_first>
  <action>
Create `lib/core/sensors/sensor-priority.cjs` (CJS, `'use strict'`, BSL 1.1 header, hyphens only,
no em-dashes) exporting exactly two things plus the constant:

  - `SENS_PRIORITY`: an `Object.freeze`d ordered array of sensor-id strings, index 0 = highest
    priority. Commit this exact 18-entry order:

    Group A, confirmed room-state / projected-cortex facts:
    `'SENS-08'`, `'SENS-10'`, `'SENS-11'`, `'SENS-14'`, `'SENS-02'`, `'SENS-RECENCY'`
    Group B, explicit turn signals and shipped side-channel markers:
    `'SENS-01'`, `'SENS-06'`, `'SENS-13'`, `'SENS-15'`, `'SENS-12'`, `'SENS-07'`, `'SENS-03'`
    Group C, derived / reweighted intent signals:
    `'SENS-05'`, `'SENS-04'`, `'SENS-09'`, `'SENS-SHOW'`
    Group D, FALLBACK lexical tier (last by doctrine):
    `'SENS-16'`

  - `sensorPriorityRank(sensorId)`: returns the 0-indexed position in `SENS_PRIORITY`, or
    `SENS_PRIORITY.length` (the worst rank, never `Infinity` and never `NaN`) for a non-string,
    an empty string, or an id absent from the table. Mirror the defensive shape of
    `canonicalRegistryRank` in `lib/workflow/reach-hedge-ranker.cjs`.

Write the ordering doctrine into the file header as a numbered rule set, because this table is
the doctrine surface D-20 requires and a future editor must be able to place a new sensor without
guessing:
  1. Canon Part 11 R3 trigger-tier precedence: a sensor keyed on navigator problem-state
     (`signal` / `context` tier) outranks one keyed on lexical match (`content` / `keyword`
     FALLBACK tier). This is why `SENS-16` (bm25 lexical content relevance) is last.
  2. Within a tier, evidence durability decides: a confirmed graph or projected-cortex fact
     outranks a transient marker file, which outranks a derived reweight.
  3. Remaining ties fall to canonical `SENSOR_REGISTRY` order, so the table is a documented
     refinement of today's behavior, never an arbitrary reshuffle.

Note explicitly in the header that ids are NOT all numeric: `'SENS-RECENCY'` and `'SENS-SHOW'`
are the real self-declared ids of `sensor-recency.cjs` and `sensor-show-share.cjs`. Do not
renumber them and do not invent numeric aliases.

Also note in the header that `lib/core/sensors/sensor-temporal-blindness.cjs`,
`sensor-types.cjs` and `hat-scoping-table.cjs` are deliberately absent from this table:
`sensor-temporal-blindness.cjs` is not a member of `SENSOR_REGISTRY` (it is unregistered), and
the other two are a shared contract and a pure lookup, not sensors. The completeness gate in
Task 3 enumerates `SENSOR_REGISTRY`, not the directory, precisely so these three do not read as
false gaps.

`SENS-17` is intentionally NOT in this table yet; plan 245-06 inserts it and the Task 3 gate is
what forces that insertion.
  </action>
  <verify>
    <automated>node -e "const p=require('./lib/core/sensors/sensor-priority.cjs'); const a=p.SENS_PRIORITY; if(!Object.isFrozen(a)) throw new Error('not frozen'); if(a.length!==18) throw new Error('want 18 got '+a.length); if(new Set(a).size!==18) throw new Error('duplicate ids'); if(p.sensorPriorityRank('SENS-08')!==0) throw new Error('SENS-08 must rank 0'); if(p.sensorPriorityRank('SENS-16')!==17) throw new Error('SENS-16 must rank last'); if(p.sensorPriorityRank('NOPE')!==18) throw new Error('unknown id must rank worst'); if(p.sensorPriorityRank(null)!==18) throw new Error('null must rank worst'); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./lib/core/sensors/sensor-priority.cjs')"` exits 0
    - `SENS_PRIORITY` is frozen, has exactly 18 entries, zero duplicates
    - The array's first six entries are exactly `SENS-08`, `SENS-10`, `SENS-11`, `SENS-14`, `SENS-02`, `SENS-RECENCY` in that order
    - The array's last entry is exactly `SENS-16`
    - `sensorPriorityRank` returns `18` for `null`, `undefined`, `''`, `42`, and `'SENS-17'`
    - `grep -cP '\\x{2014}' lib/core/sensors/sensor-priority.cjs` returns 0 (no em-dash characters; the codepoint form keeps this criterion from embedding the character it forbids)
    - The file header contains the three numbered ordering rules and the explicit note naming `SENS-RECENCY` and `SENS-SHOW` as non-numeric real ids
  </acceptance_criteria>
  <done>The frozen priority table exists, is defensive on unknown input, and carries the authoring doctrine a future sensor author needs.</done>
</task>

<task type="auto">
  <name>Task 2: Stamp evidence.sensor_id centrally in dispatchSensors</name>
  <files>lib/core/insight-sensors.cjs</files>
  <read_first>
    - lib/core/insight-sensors.cjs lines 696-793 (the `SENSOR_REGISTRY` array in canonical order and the `dispatchSensors` soft-fail loop you are extending)
    - lib/core/sensors/sensor-types.cjs lines 217-294 (`makeReach`: it validates `reach_id` / `posture` against the frozen banks, keeps only string/number/boolean evidence values, and returns a deeply frozen struct; you must rebuild through this factory, never mutate a frozen reach)
    - lib/core/sensors/sensor-memory-cortex.cjs (a representative sensor showing the evidence bag shape you are adding one key to)
  </read_first>
  <action>
Implement D-21's requirement (every fired reach carries `evidence.sensor_id`) as a SINGLE
registry-driven stamp rather than 18 hand edits, one per sensor file.

Mechanism note for reviewers, state it in the code comment too: D-21 describes the stamp as
"a one-line add per sensor". This plan delivers the identical observable outcome through one
central stamp because this repo has already been bitten twice by hand-maintained parallel sensor
declarations drifting (D-15's three commands falsely declaring `SENS-05` for `hats`, and F-3's
`sensor_index` missing four real sensors). A central stamp makes it structurally impossible for a
newly registered sensor to ship unstamped, which is exactly what the Task 3 fail-closed gate
needs. Canon Part 7 (one mechanism, not eighteen).

Concretely, in `lib/core/insight-sensors.cjs`:

1. Immediately after the `SENSOR_REGISTRY` array declaration, add a frozen
   `SENSOR_REGISTRY_IDS` array whose element at index `i` is the self-declared sensor id of
   `SENSOR_REGISTRY[i]`. Commit this exact 18-entry order, which mirrors the registry array
   element for element:
   `'SENS-01'` (sensorFirstMaterial), `'SENS-06'` (sensorArtifactFiled), `'SENS-02'`
   (sensorLaggingComponent), `'SENS-03'` (sensorMethodologyDecision), `'SENS-07'`
   (sensorGateApproach), `'SENS-04'` (sensorExternalFact), `'SENS-05'` (sensorJtbdReweight),
   `'SENS-08'` (sensorMemoryCortex), `'SENS-RECENCY'` (sensorRecency), `'SENS-09'`
   (sensorDiffusionAdoption), `'SENS-SHOW'` (sensorShowShare), `'SENS-10'` (sensorCircularity),
   `'SENS-11'` (sensorExpertSkill), `'SENS-12'` (sensorRoomPick), `'SENS-13'` (sensorEureka),
   `'SENS-14'` (sensorOpportunityHarvest), `'SENS-15'` (sensorUrlIngest), `'SENS-16'`
   (sensorContentRelevance).
   Add a lockstep comment: the two arrays are index-parallel and a new sensor must be appended to
   BOTH, and the Task 3 `--check` gate plus `tests/test-245-priority-complete.cjs` fail closed
   when they diverge.

2. Inside `dispatchSensors`'s registry loop, after the existing `REACH_IDS` membership check and
   the `isReachEligibleForTurn` turn-stage gate, and before `out.push(reach)`: rebuild the reach
   through `makeReach` with `evidence` extended by `sensor_id: SENSOR_REGISTRY_IDS[i]`. Use the
   registry array index for `i` (convert the `for...of` to an indexed loop, or track the index
   alongside). Preserve `reach_id`, `posture`, `dispatch`, `companions` and `signal` verbatim.
   If `makeReach` returns null for any reason, push the ORIGINAL reach unchanged (soft-fail
   discipline: a stamp failure must never drop a reach that already passed the gates).
   If `SENSOR_REGISTRY_IDS[i]` is not a non-empty string, push the original reach unchanged.

3. Export `SENSOR_REGISTRY_IDS` from `module.exports`. Also add the missing
   `sensorRoomPick: sensorRoomPick` export while you are in the exports block: it is registered
   in `SENSOR_REGISTRY` but absent from `module.exports`, a pre-existing gap the Task 3 gate would
   otherwise make harder to reason about.

Canon Part 8: `sensor_id` is a literal constant read from a frozen in-repo array. It is never
derived from turn text, room content, or any user-suppliable value. `makeReach` already drops
non-primitives, so the struct stays a flat scalar bag.

Regression surface to check and repair before declaring done: run the sensor-facing suites and
fix any assertion that deep-compares a reach's `evidence` object, since those now see one extra
key. Start with `node tests/test-213-reach-wired.cjs`, `node tests/test-203-reach-sensor.cjs`,
`node tests/test-160-recency-reach-signal.cjs`, `node tests/test-diffusion-adoption-sensor.cjs`,
then `grep -rln "evidence" tests/*.cjs` and run the sensor-related hits. Do NOT loosen a Part-8
sweep assertion to accommodate the new key; if a Part-8 test enumerates allowed evidence keys,
add `sensor_id` to its allowlist explicitly and note why in the test comment.
  </action>
  <verify>
    <automated>node tests/test-213-reach-wired.cjs && node tests/test-203-reach-sensor.cjs && node tests/test-160-recency-reach-signal.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const s=require('./lib/core/insight-sensors.cjs'); if(s.SENSOR_REGISTRY_IDS.length!==s.SENSOR_REGISTRY.length) throw new Error('parallel arrays diverged'); if(!Object.isFrozen(s.SENSOR_REGISTRY_IDS)) throw new Error('not frozen'); if(typeof s.sensorRoomPick!=='function') throw new Error('sensorRoomPick not exported'); console.log('ok');"` prints `ok`
    - A synthetic dispatch that fires `sensorMemoryCortex` (call `dispatchSensors({}, {}, {freshContradictions: 1})`) returns at least one reach whose `evidence.sensor_id === 'SENS-08'`
    - Every reach returned by that same dispatch has a non-empty string `evidence.sensor_id`
    - Every returned reach is still `Object.isFrozen`, and its `reach_id`, `posture`, `dispatch` and `signal` values are unchanged from the pre-stamp behavior
    - `node tests/test-213-reach-wired.cjs`, `node tests/test-203-reach-sensor.cjs`, `node tests/test-160-recency-reach-signal.cjs`, `node tests/test-diffusion-adoption-sensor.cjs` all exit 0
    - `grep -cP '\\x{2014}' lib/core/insight-sensors.cjs` returns 0
  </acceptance_criteria>
  <done>Every reach `dispatchSensors` emits carries a frozen, constant `evidence.sensor_id`, and the two parallel registry arrays are exported for the gate to enumerate.</done>
</task>

<task type="auto">
  <name>Task 3: Fail-closed completeness gate plus the phase test runner</name>
  <files>scripts/build-connector-registry.cjs, tests/run-all-245.sh, tests/test-245-sensor-id-stamped.cjs, tests/test-245-priority-complete.cjs</files>
  <read_first>
    - scripts/build-connector-registry.cjs (find the `--check` branch and the `sensor_index` construction near line 596; read enough of the CLI entry point to add a second, sensor-side assertion without touching the byte-compare of `data/connector-registry.json`)
    - tests/run-all-244.sh (the glob-discovery runner shape, the found-eq-0 guard, and the `run_may_skip` helper you are mirroring)
    - .planning/phases/245-close-the-reach-brain-signal-loop-wire-dispatchsensors-fire-/245-RESEARCH.md Pitfall 3 and finding F-3 (why `sensor_index` cannot be the gate's input)
  </read_first>
  <action>
Three artifacts.

(a) `scripts/build-connector-registry.cjs`: add a sensor-side completeness assertion that runs
under `--check`. It must require `lib/core/insight-sensors.cjs` and
`lib/core/sensors/sensor-priority.cjs` and assert all three of:
  - `SENSOR_REGISTRY.length === SENSOR_REGISTRY_IDS.length`
  - every id in `SENSOR_REGISTRY_IDS` appears in `SENS_PRIORITY`
  - every id in `SENS_PRIORITY` appears in `SENSOR_REGISTRY_IDS`
On any failure print the offending id list to stderr and exit non-zero, matching the existing
`--check` fail-closed exit convention. Do NOT add any new key to `data/connector-registry.json`
and do NOT touch the `sensor_index` build: per F-3, `sensor_index` is built from command
frontmatter `sensor_triggers`, currently has 13 keys, and is already missing SENS-10, SENS-11,
SENS-12 and SENS-16, three of which are `context_block` colliders. A gate built on it would
silently exempt the exact sensors Requirement 4 exists to order. Say this in the code comment so
the next reader does not "fix" it back.

(b) `tests/run-all-245.sh`: mirror `tests/run-all-244.sh` exactly in shape. Discovery is by GLOB
over `tests/test-245-*` (both `.cjs` and `.sh`), never a hand-maintained execution list. Keep the
found-eq-0 guard: zero discovered files must FAIL, not print green. Enumerate the phase's
mandatory test filenames in the header as a human reading checklist:
`test-245-sensor-id-stamped.cjs`, `test-245-priority-complete.cjs`, `test-245-tiebreak-deterministic.cjs`,
`test-245-drain-budget.cjs`, `test-245-drain-no-silent-loss.cjs`, `test-245-trigger-arms.cjs`,
`test-245-trigger-negative.cjs`, `test-245-egress-contentless.cjs`, `test-245-verb-affinity.cjs`,
`test-245-brain-verb-computed.cjs`, `test-245-sens17-hats.cjs`, `test-245-sens17-no-double-fire.cjs`,
`test-245-nudge-bound.cjs`, `test-245-dial-reactivity.cjs`, `test-245-brain-verb-not-starved.cjs`.
`chmod +x tests/run-all-245.sh`.

(c) The two tests. `tests/test-245-sensor-id-stamped.cjs` must drive `dispatchSensors` through
its real signature (never call a sensor function directly) with at least three distinct synthetic
contexts that each fire a different sensor, and assert every returned reach has a non-empty
string `evidence.sensor_id` that is a member of `SENSOR_REGISTRY_IDS`. Include a Part-8
assertion: the stamped value must be strictly equal to a member of the frozen array, never a
substring of any turn text the fixture supplied. `tests/test-245-priority-complete.cjs` must
assert the same three set relations the `--check` gate asserts, plus that `SENS_PRIORITY` is
frozen and duplicate-free, so the invariant is pinned both at build time and at test time.

Both tests follow the repo convention: bare `node` scripts, no framework, exit non-zero on
failure, self-contained with no shared fixture file.

Mutation proof before declaring done: temporarily remove one id from `SENS_PRIORITY`, confirm
BOTH `node scripts/build-connector-registry.cjs --check` and
`node tests/test-245-priority-complete.cjs` exit non-zero, then restore. Record the mutation and
its observed exit codes in the plan SUMMARY.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node tests/test-245-priority-complete.cjs && node tests/test-245-sensor-id-stamped.cjs && bash tests/run-all-245.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-connector-registry.cjs --check` exits 0 on the unmodified tree
    - With one id deleted from `SENS_PRIORITY`, `node scripts/build-connector-registry.cjs --check` exits non-zero AND its stderr names the missing id
    - With one id deleted from `SENS_PRIORITY`, `node tests/test-245-priority-complete.cjs` exits non-zero
    - `git diff --stat data/connector-registry.json` is empty after running `node scripts/build-connector-registry.cjs` (this plan must not change the generated registry)
    - `bash tests/run-all-245.sh` exits 0 and reports at least 2 discovered test files
    - `bash tests/run-all-245.sh` exits non-zero when run against a directory with zero `tests/test-245-*` matches (verify by temporarily pointing the glob at a nonexistent prefix, or by asserting the found-eq-0 branch exists in the script source)
    - `grep -n "sensor_index" scripts/build-connector-registry.cjs` shows the new gate does NOT read `sensor_index`, and a comment explains why
  </acceptance_criteria>
  <done>A sensor registered without a priority entry cannot pass `--check`, and the phase has a glob-discovery runner that fails on empty discovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| turn text / room content -> sensor evidence | untrusted user bytes could be smuggled into a reach's evidence bag and then into `trace.context_assembly.facts[]` |
| in-repo constants -> build gate | a hand-maintained parallel array can drift from the thing it mirrors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-245-01 | Information Disclosure | `dispatchSensors` sensor_id stamp | mitigate | `sensor_id` is read from the frozen `SENSOR_REGISTRY_IDS` literal array only, never derived from turn text; `makeReach` drops non-primitives and freezes; `tests/test-245-sensor-id-stamped.cjs` asserts strict membership in the frozen array |
| T-245-02 | Tampering | `SENS_PRIORITY` ordering | mitigate | Frozen with `Object.freeze`; duplicate-free and completeness both asserted at build (`--check`) and test time; ordering doctrine written into the file header so a silent reshuffle is reviewable |
| T-245-03 | Repudiation | a sensor ships without a priority entry and silently degrades to file order | mitigate | Fail-closed `--check` gate enumerating `SENSOR_REGISTRY_IDS`, mutation-proven in Task 3 |
| T-245-04 | Denial of Service | stamp failure drops a fired reach | mitigate | Soft-fail: a null `makeReach` result or a bad id pushes the ORIGINAL reach unchanged; `dispatchSensors`'s existing per-sensor try/catch is untouched |
| T-245-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: this phase installs zero external packages (245-RESEARCH.md Package Legitimacy Audit, "not applicable"). No `package.json` change is permitted by any task in this plan; if one becomes necessary, stop and run the Package Legitimacy Gate first |
</threat_model>

<verification>
- `node scripts/build-connector-registry.cjs --check` exits 0
- `node scripts/build-orchestration-projection.cjs --check` exits 0
- `node scripts/check-render-coverage.cjs` exits 0
- `bash tests/run-all-245.sh` exits 0
- `node tests/test-158-reach-orchestrator-pure.cjs` and `node tests/test-158-reach-byte-stable.cjs` both exit 0 (D-03 purity tripwires must stay green even though this plan does not touch the orchestrator)
- `git diff --stat data/connector-registry.json` is empty
</verification>

<success_criteria>
- Every reach emitted by `dispatchSensors` carries `evidence.sensor_id` from the frozen id array
- `SENS_PRIORITY` covers exactly the 18 registered sensors, frozen and duplicate-free
- Deleting any `SENS_PRIORITY` entry reddens both the build gate and the test (mutation-proven, recorded in the SUMMARY)
- `tests/run-all-245.sh` exists, is executable, discovers by glob, and fails on empty discovery
- Zero ranking-behavior change: the tie-break comparator is untouched in this plan
</success_criteria>

<output>
Create `.planning/phases/245-close-the-reach-brain-signal-loop-wire-dispatchsensors-fire-/245-01-SUMMARY.md` when done.
Record: the committed `SENS_PRIORITY` order, the `SENSOR_REGISTRY_IDS` mapping, every existing test that needed an evidence-shape update and why, and the Task 3 mutation-proof exit codes.
</output>

