---
phase: 143.1-dial-tui-capability-selector
plan: 06
type: execute
wave: 3
depends_on: ["143.1-01", "143.1-02", "143.1-03", "143.1-04", "143.1-05"]
files_modified:
  - tests/run-all-1431.sh
  - tests/test-dial-end-to-end-states.cjs
  - .planning/phases/143.1-dial-tui-capability-selector/143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md
autonomous: true
requirements: [DIALTUI-11]
canon_parts: [Part 3, Part 7]

must_haves:
  truths:
    - "tests/run-all-1431.sh aggregates every Phase 143.1 suite and mirrors run-all-143.sh exactly"
    - "An end-to-end test renders all five UI-SPEC states through orchestrator -> labels -> presenter and closes a reach through closeReach"
    - "DIALTUI-11 is marked complete-by-reference (UI-SPEC approved); gsd-ui-phase is NOT re-run"
    - "The two-K separation (MAX_K=3 vs DIAL_REACH_K=5), the 0.70 floor, the Part-8 {framework} seam, and the navigation.cjs write-locality are asserted as standing gates"
  artifacts:
    - path: "tests/run-all-1431.sh"
      provides: "Phase 143.1 scoped aggregator mirroring tests/run-all-143.sh"
      min_lines: 60
    - path: "tests/test-dial-end-to-end-states.cjs"
      provides: "Full-pipeline S1-S5 render + a closeReach round-trip"
    - path: ".planning/phases/143.1-dial-tui-capability-selector/143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md"
      provides: "DIALTUI-11 complete-by-reference record (UI-SPEC approved; no gsd-ui-phase re-run)"
  key_links:
    - from: "tests/run-all-1431.sh"
      to: "all Phase 143.1 CJS suites"
      via: "CJS_SUITES list aggregating every test from Plans 01-05"
      pattern: "CJS_SUITES"
---

<objective>
Close the phase: build tests/run-all-1431.sh (mirroring run-all-143.sh), add an end-to-end state-matrix + closeReach round-trip test, and record DIALTUI-11 as complete-by-reference (the UI-SPEC is approved; gsd-ui-phase is NOT re-run).

Purpose: One aggregator gate proves the dial renders correctly in each mode and that the standing fences (two-K, 0.70 floor, Part-8 seam, write-locality) hold. DIALTUI-11 is DONE by reference - re-running gsd-ui-phase is an explicit anti-pattern (AP8).

Output: the aggregator + the e2e test + the complete-by-reference record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-CONTEXT.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-01-SUMMARY.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-02-SUMMARY.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-03-SUMMARY.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-04-SUMMARY.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-05-SUMMARY.md

<interfaces>
<!-- The aggregator to mirror + the modules to wire end-to-end. Do NOT re-explore. -->

From tests/run-all-143.sh (mirror this exactly):
  - set -uo pipefail; SHELL_SUITES + CJS_SUITES arrays; per-suite PASS/FAIL; runs to completion even on failure; exits non-zero on any failure
  - bash only, no emoji, no em-dashes

Phase 143.1 suites to aggregate (from Plans 01-05):
  test-dial-reach-orchestrator.cjs        (Plan 01)
  test-dial-label-bank-drift.cjs          (Plan 02)
  test-dial-close-reach.cjs               (Plan 03)
  test-dial-render-states.cjs             (Plan 04)
  test-dial-memory-renderer.cjs           (Plan 05)
  test-dial-graph-relationship-layer.cjs  (Plan 05)
  test-dial-end-to-end-states.cjs         (this plan)

The full pipeline: buildReachList (Plan 01) -> composeLabel (Plan 02) -> dial-presenter render (Plan 04) -> closeReach commit (Plan 03) -> dial-memory render (Plan 05).

DIALTUI-11: the UI-SPEC at 143.1-UI-SPEC.md is APPROVED (CLI scope, 2026-06-06). Mark complete-by-reference. Do NOT re-run gsd-ui-phase (AP8).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the end-to-end state-matrix + closeReach round-trip test</name>
  <read_first>
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 7 state matrix, Section 12 the standing verification dimensions V1/V2/V5/V7/V9/V10/V11/V12)
    - the five plan SUMMARYs (the exact exported function names from Plans 01-05)
  </read_first>
  <files>tests/test-dial-end-to-end-states.cjs</files>
  <behavior>
    - Full pipeline per state: orchestrator builds the ReachList -> composer fills labels -> presenter renders -> assert the S1-S5 render contract (1/2/0/0 markers, footer, cold-room framing) end to end
    - closeReach round-trip: render S1, commit the recommended detent -> assert a SELECTED_REACH edge + f_selector_sync_confirmed event; render S1, pivot off -> assert a PIVOTED edge + f_selector_pivot
    - Standing gate V9: MAX_K (f-selector-ranker) is still 3 AND DIAL_REACH_K (orchestrator) is 5 - distinct constants
    - Standing gate V10: a crafted margin>0.15 case yields exactly 1 marker (0.70 floor never lowered)
    - Standing gate V11: the brain_consult/deep_research {framework} egress seam rejects raw user content (re-assert via the composer)
    - Standing gate V12: a grep sweep confirms no module under this phase opens room.db directly (navigation.cjs-only)
  </behavior>
  <action>Create the e2e test wiring all five modules. Drive each state through the full pipeline and assert the render contract + a closeReach round-trip for sync and pivot. Re-assert the four standing fences (V9/V10/V11/V12) so the aggregator owns them as a single gate. Test MUST fail before Task 2 if any wiring is missing (it depends on all prior plans being green).</action>
  <verify>
    <automated>node tests/test-dial-end-to-end-states.cjs</automated>
  </verify>
  <acceptance_criteria>The e2e test drives orchestrator->labels->presenter->closeReach->memory for S1-S5, asserts the render contract + sync/pivot round-trips + the V9/V10/V11/V12 standing gates, and passes against the shipped Plans 01-05.</acceptance_criteria>
  <done>End-to-end test green; the four standing fences asserted in one place.</done>
</task>

<task type="auto">
  <name>Task 2: Build tests/run-all-1431.sh aggregator (mirror run-all-143.sh)</name>
  <read_first>
    - tests/run-all-143.sh (mirror the structure exactly: set -uo pipefail, SHELL_SUITES + CJS_SUITES, per-suite PASS/FAIL header with plan ownership, run-to-completion, non-zero exit on any failure; bash only, no emoji, no em-dashes)
  </read_first>
  <files>tests/run-all-1431.sh</files>
  <action>Create the Phase 143.1 scoped runner mirroring run-all-143.sh byte-for-structure. List all seven CJS_SUITES (test-dial-reach-orchestrator, test-dial-label-bank-drift, test-dial-close-reach, test-dial-render-states, test-dial-memory-renderer, test-dial-graph-relationship-layer, test-dial-end-to-end-states) with a per-suite plan-ownership comment header. The runner runs every suite to completion even when one fails, prints per-suite PASS/FAIL, and exits non-zero if any failed. No em-dashes, no emoji.</action>
  <verify>
    <automated>bash tests/run-all-1431.sh</automated>
  </verify>
  <acceptance_criteria>bash tests/run-all-1431.sh runs all seven suites to completion, prints the summary, and exits 0 with every Phase 143.1 suite green.</acceptance_criteria>
  <done>Aggregator mirrors run-all-143.sh; all 143.1 suites pass.</done>
</task>

<task type="auto">
  <name>Task 3: Record DIALTUI-11 complete-by-reference (no gsd-ui-phase re-run)</name>
  <read_first>
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (the Checker Sign-Off block: approved CLI scope 2026-06-06; V8 deferred)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-CONTEXT.md (the DONE classification for DIALTUI-11; AP8 - do not re-run gsd-ui-phase)
  </read_first>
  <files>.planning/phases/143.1-dial-tui-capability-selector/143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md</files>
  <action>Write a short record stating DIALTUI-11 is satisfied by the approved 143.1-UI-SPEC.md (CLI scope, 2026-06-06): the UI-SPEC was produced and checker-signed; gsd-ui-phase is NOT re-run (AP8). Note the V8 Desktop render proof is DEFERRED past 143.1 as a tracked follow-up per resolved OQ1, and the cold-room zero-marker intentionality (V7) is covered by the Plan 04 render-state test + the Plan 06 e2e test. Cross-reference the UI-SPEC path and the run-all-1431.sh gate.</action>
  <verify>
    <automated>test -f .planning/phases/143.1-dial-tui-capability-selector/143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md && grep -q "complete-by-reference" .planning/phases/143.1-dial-tui-capability-selector/143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>The record exists, cites the approved UI-SPEC, states no gsd-ui-phase re-run, and notes V8 deferred + V7 covered by tests.</acceptance_criteria>
  <done>DIALTUI-11 marked complete-by-reference; gsd-ui-phase not re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregator gate -> CI | The run-all-1431.sh gate is where the standing fences are enforced before merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1431.06-01 | Tampering | the two-K / 0.70-floor fences | mitigate | The e2e test re-asserts V9 (MAX_K=3 vs DIAL_REACH_K=5) + V10 (margin>0.15 -> 1 marker) as a standing gate in the aggregator |
| T-1431.06-02 | Information Disclosure | the Part-8 {framework} seam | mitigate | V11 re-asserted end-to-end; the egress seam blocks raw user content before any consumer lands |
| T-1431.06-03 | Tampering | write-locality | mitigate | V12 grep sweep across the phase modules confirms navigation.cjs-only writes |
| T-1431.06-SC | Tampering | npm installs | accept | No package installs; bash + node test wiring only |
</threat_model>

<verification>
- bash tests/run-all-1431.sh exits 0 with all seven suites green
- node tests/test-dial-end-to-end-states.cjs passes
- the DIALTUI-11 complete-by-reference record exists and cites the approved UI-SPEC
- grep -cP "\x{2014}" tests/run-all-1431.sh returns 0 (no em-dashes)
</verification>

<success_criteria>
The Phase 143.1 aggregator mirrors run-all-143.sh and is green; the end-to-end state matrix + closeReach round-trip prove the full pipeline; the four standing fences (two-K, 0.70 floor, Part-8 seam, write-locality) are enforced in one gate; DIALTUI-11 is complete-by-reference with no gsd-ui-phase re-run.
</success_criteria>

<output>
Create `.planning/phases/143.1-dial-tui-capability-selector/143.1-06-SUMMARY.md` when done.
</output>
