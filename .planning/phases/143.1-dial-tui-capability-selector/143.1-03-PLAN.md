---
phase: 143.1-dial-tui-capability-selector
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/edges.cjs
  - lib/core/navigation/memory-events.cjs
  - lib/workflow/dial-close-reach.cjs
  - tests/test-dial-close-reach.cjs
autonomous: true
requirements: [DIALTUI-06, DIALTUI-07, DIALTUI-08, DIALTUI-09, FILEVAL-01]
canon_parts: [Part 4, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "PIVOTED and SELECTED_REACH are members of ALLOWED_EDGE_TYPES (additive idiom, floor preserved)"
    - "f_selector_pivot and f_selector_sync_confirmed are members of EVENT_TYPES (additive, floor preserved)"
    - "closeReach() handles all four outcomes (sync-commit, pivot-commit, defer/reject, free-text miss) routed exclusively through navigation.cjs"
    - "A resting-detent commit writes a SELECTED_REACH edge + a positive f_selector_sync_confirmed event"
    - "A rotate-to-pivot commit writes a PIVOTED edge (ENUM-only props) + f_selector_pivot event PLUS the SELECTED_REACH edge"
    - "Every committed reach writes SELECTED_REACH FROM active focus TO cmd:<command> with {jtbd, framework, recommended, decision_id} PLUS a STATE.md/TodoWrite next-action"
    - "SELECTED_REACH is system bookkeeping, kept distinct from any confirmNode truth-claim promotion"
  artifacts:
    - path: "lib/workflow/dial-close-reach.cjs"
      provides: "closeReach() unified 4-outcome transaction wrapping the shipped recorders"
      min_lines: 90
    - path: "lib/core/navigation/edges.cjs"
      provides: "PIVOTED + SELECTED_REACH in ALLOWED_EDGE_TYPES"
      contains: "SELECTED_REACH"
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "f_selector_pivot + f_selector_sync_confirmed in EVENT_TYPES"
      contains: "f_selector_sync_confirmed"
  key_links:
    - from: "lib/workflow/dial-close-reach.cjs"
      to: "lib/core/navigation.cjs"
      via: "writeEdge / logMemoryEvent through the chokepoint only - never room.db directly"
      pattern: "require\\('\\.\\./core/navigation\\.cjs'\\)"
    - from: "lib/workflow/dial-close-reach.cjs"
      to: "lib/workflow/selector-decisions.cjs"
      via: "recordSelectorDecision (defer/reject) + recordSelectorMiss (free-text overflow) reused"
      pattern: "recordSelectorDecision|recordSelectorMiss"
---

<objective>
Make every dial decision graph data (DIALTUI-06/07/08/09 + FILEVAL-01): add the PIVOTED + SELECTED_REACH edge types and the f_selector_pivot + f_selector_sync_confirmed event types via the additive idiom, then build closeReach() - the unified 4-outcome transaction routed exclusively through navigation.cjs.

Purpose: "Why not the recommendation" becomes graph data (Canon Part 4). The resting detent IS the in-sync signal (DIALTUI-07). Every committed reach closes the Part 3 Layer-3 gap (DIALTUI-08). FILEVAL-01 is satisfied by the SELECTED_REACH write at the selection moment.

Output: extended edges/events registries + lib/workflow/dial-close-reach.cjs + its transaction test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-CONTEXT.md

<interfaces>
<!-- The additive idioms + recorders to reuse. Do NOT re-explore. -->

From lib/core/navigation/edges.cjs:
  ALLOWED_EDGE_TYPES = Object.freeze(new Set([ 'DEFERRED','REJECTED','DERIVED_FROM','FILED_AS_DECISION','FOLLOWS_FROM','OPERATOR_TRANSITION', ... ]))
  writeEdge(db, params)  (the only door; ENUM-only props on cascade edges per the FOLLOWS_FROM precedent at line 79)
  Additive idiom (line 30-31): tests assert a FLOOR + named membership, never an exact count - so additive extensions cannot regress baseline.

From lib/core/navigation/memory-events.cjs:
  EVENT_TYPES = Object.freeze(new Set([ ...,'selector_presentation','selector_response','tension_surfaced','auto_explore_fired', ... ]))
  Same additive floor-not-count idiom.

From lib/core/navigation.cjs (the chokepoint - import THIS, never the submodules, never room.db):
  writeEdge, logMemoryEvent, getActiveFocus, confirmNode  (re-exported)

From lib/workflow/selector-decisions.cjs (reuse for outcomes 3 + 4):
  recordSelectorDecision({decision:'DEFERRED'|'REJECTED', command, framework, reason?, roomState}) -> {decision_id, edge_id}
  recordSelectorMiss({top_k_offered, user_intent, roomState}) -> {miss_id}

The four closeReach outcomes (UI-SPEC Section 8):
  1. resting-detent commit (no rotate)  -> SELECTED_REACH edge + f_selector_sync_confirmed event
  2. rotate-to-pivot commit             -> PIVOTED edge FROM chosen TO declined-recommended {declined_recommended:'cmd:...', margin, decision_id} + f_selector_pivot event + SELECTED_REACH edge
  3. defer / reject                     -> recordSelectorDecision (DEFERRED +30d / REJECTED)
  4. free-text / none-fit overflow      -> recordSelectorMiss({top_k_offered, user_intent})  (NEVER a 4th explicit Free-Text row - AP5)

Part 9 carve-out: SELECTED_REACH is system bookkeeping (created_by=system, exempt from human-confirm). A truth-claim promotion stays distinct and routes through confirmNode with a human byUser. Keep the two writes separate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the closeReach 4-outcome transaction test</name>
  <read_first>
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 8 the four outcomes + the SELECTED_REACH/PIVOTED edge contracts + Part 9 bookkeeping-vs-truth-claim split)
    - lib/workflow/selector-decisions.cjs (recordSelectorDecision/recordSelectorMiss + the Test-7 grep-audit invariant style at lines 19-29 to mirror)
    - lib/core/navigation/edges.cjs (the floor-not-count assertion idiom at lines 30-31)
  </read_first>
  <files>tests/test-dial-close-reach.cjs</files>
  <behavior>
    - Outcome 1 (sync): closeReach({outcome:'sync',...}) writes one SELECTED_REACH edge + one f_selector_sync_confirmed event; no PIVOTED edge
    - Outcome 2 (pivot): writes a PIVOTED edge FROM chosen TO declined-recommended with ENUM-only props {declined_recommended, margin, decision_id} + one f_selector_pivot event + one SELECTED_REACH edge
    - Outcome 3 (defer/reject): delegates to recordSelectorDecision; a DEFERRED or REJECTED edge lands
    - Outcome 4 (free-text miss): delegates to recordSelectorMiss; no SELECTED_REACH edge, no 4th explicit row
    - SELECTED_REACH props are {jtbd, framework, recommended, decision_id}; edge runs FROM active focus TO cmd:<command>
    - Layer-3: each committed reach also produces a STATE.md Decisions row or TodoWrite next-action (assert the next-action write is invoked)
    - Write locality (Part 9): grep-audit asserts dial-close-reach.cjs imports ONLY lib/core/navigation.cjs (no room.db open, no direct submodule require)
    - Floor preservation: ALLOWED_EDGE_TYPES still has DEFERRED+REJECTED; EVENT_TYPES still has its baseline; sets are still Object.freeze'd
    - PIVOTED props carry zero freeform user-content fields (ENUM-only, Part 8)
  </behavior>
  <action>Create the transaction test against a fixture room.db (mirror the run-all-143.sh CJS-suite style). Drive each of the four outcomes and assert the exact edges/events written. Include the grep-audit tripwire (only navigation.cjs imported) and the ENUM-only PIVOTED-props assertion. Test MUST fail before Tasks 2-4.</action>
  <verify>
    <automated>node tests/test-dial-close-reach.cjs; test $? -ne 0 && echo "RED OK (expected fail)"</automated>
  </verify>
  <acceptance_criteria>Test exercises all four outcomes + SELECTED_REACH/PIVOTED edge shapes + Layer-3 next-action + write-locality grep + floor preservation; fails because closeReach and the new types are absent.</acceptance_criteria>
  <done>Failing transaction test committed; assertions map to UI-SPEC Section 8.</done>
</task>

<task type="auto">
  <name>Task 2: Add PIVOTED + SELECTED_REACH to ALLOWED_EDGE_TYPES (additive)</name>
  <read_first>
    - lib/core/navigation/edges.cjs (the additive block pattern - copy the FOLLOWS_FROM/OPERATOR_TRANSITION comment+entry idiom verbatim; ENUM-only props constraint at line 79)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 8 edge semantics)
  </read_first>
  <files>lib/core/navigation/edges.cjs</files>
  <action>Add 'PIVOTED' and 'SELECTED_REACH' to the ALLOWED_EDGE_TYPES Set using the additive idiom (a comment block mirroring the FOLLOWS_FROM/OPERATOR_TRANSITION precedent, then the two string entries). Note in the comment: PIVOTED carries ENUM-only props {declined_recommended:'cmd:...', margin, decision_id} (no freeform user content, Part 8); SELECTED_REACH is system bookkeeping (Part 9 carve-out, distinct from confirmNode truth-claim promotion). Do NOT change the floor; the existing Set membership stays byte-identical except the two additions.</action>
  <verify>
    <automated>node -e "const{ALLOWED_EDGE_TYPES}=require('./lib/core/navigation/edges.cjs');process.exit(ALLOWED_EDGE_TYPES.has('PIVOTED')&&ALLOWED_EDGE_TYPES.has('SELECTED_REACH')&&ALLOWED_EDGE_TYPES.has('DEFERRED')&&ALLOWED_EDGE_TYPES.has('REJECTED')?0:1)"</automated>
  </verify>
  <acceptance_criteria>PIVOTED + SELECTED_REACH present; DEFERRED + REJECTED floor preserved; Set still frozen.</acceptance_criteria>
  <done>Both edge types added additively; baseline membership intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add f_selector_pivot + f_selector_sync_confirmed to EVENT_TYPES (additive)</name>
  <read_first>
    - lib/core/navigation/memory-events.cjs (the additive EVENT_TYPES block idiom; the floor-not-count note at lines 51-53)
  </read_first>
  <files>lib/core/navigation/memory-events.cjs</files>
  <action>Add 'f_selector_pivot' and 'f_selector_sync_confirmed' to the EVENT_TYPES Set using the additive idiom (comment block mirroring the auto_explore/tension precedent, then the two entries). f_selector_sync_confirmed is the positive resting-detent in-sync signal (DIALTUI-07); f_selector_pivot mirrors the PIVOTED edge (DIALTUI-06). Do NOT change the floor.</action>
  <verify>
    <automated>node -e "const m=require('./lib/core/navigation/memory-events.cjs');const E=m.EVENT_TYPES||m;process.exit(E.has('f_selector_pivot')&&E.has('f_selector_sync_confirmed')?0:1)"</automated>
  </verify>
  <acceptance_criteria>Both event types present; baseline EVENT_TYPES membership intact; Set still frozen.</acceptance_criteria>
  <done>Both event types added additively.</done>
</task>

<task type="auto">
  <name>Task 4: Build closeReach() - the unified 4-outcome transaction</name>
  <read_first>
    - lib/workflow/selector-decisions.cjs (recordSelectorDecision + recordSelectorMiss - reuse for outcomes 3+4; the all-writes-through-navigation.cjs invariant)
    - lib/core/navigation.cjs (writeEdge / logMemoryEvent / getActiveFocus / confirmNode - the chokepoint; import ONLY this)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 8 the four outcomes + the SELECTED_REACH Layer-3 next-action + the Part 9 bookkeeping-vs-truth-claim split)
  </read_first>
  <files>lib/workflow/dial-close-reach.cjs</files>
  <action>Create closeReach({outcome, reach, declinedRecommended?, margin?, roomState, ...}) routing exclusively through lib/core/navigation.cjs (never open room.db; never require a navigation submodule directly). Implement the four outcomes: (1) sync -> writeEdge SELECTED_REACH FROM active focus TO cmd:<command> with props {jtbd, framework, recommended, decision_id} + logMemoryEvent f_selector_sync_confirmed; (2) pivot -> writeEdge PIVOTED FROM chosen TO declined-recommended with ENUM-only props {declined_recommended:'cmd:...', margin, decision_id} + logMemoryEvent f_selector_pivot + ALSO the SELECTED_REACH edge; feed an investment_level-scaled pivot-penalty back to the ranker (so one early pivot does not permanently bury a reach) by recording the penalty signal the ranker's decay/penalty reader consumes; (3) defer/reject -> delegate to recordSelectorDecision; (4) free-text miss -> delegate to recordSelectorMiss({top_k_offered, user_intent}) and write NO 4th explicit row (AP5). For every committed reach (outcomes 1+2) also write the Layer-3 next-action: a STATE.md Decisions row or TodoWrite entry (reuse the wikilink injector). SELECTED_REACH is created_by=system bookkeeping (Part 9 carve-out); if the reach also promotes a truth-claim node, keep THAT promotion distinct via confirmNode with a human byUser - do not fold it into the SELECTED_REACH write.</action>
  <verify>
    <automated>node tests/test-dial-close-reach.cjs</automated>
  </verify>
  <acceptance_criteria>test-dial-close-reach.cjs passes: all four outcomes write the correct edges/events; SELECTED_REACH + Layer-3 next-action on commit; PIVOTED props ENUM-only; only navigation.cjs imported; truth-claim promotion stays distinct.</acceptance_criteria>
  <done>closeReach green; write-locality grep clean; pivot-penalty signal recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closeReach -> room.db | All writes must cross via navigation.cjs; never a direct room.db open |
| PIVOTED props -> graph | Only ENUM/scalar props may land; no freeform user content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1431.03-01 | Tampering | dial-close-reach.cjs write path | mitigate | Grep-audit test asserts only navigation.cjs imported - no direct room.db open, no submodule require (Part 9, AP6) |
| T-1431.03-02 | Information Disclosure | PIVOTED edge props | mitigate | Test asserts ENUM-only props {declined_recommended, margin, decision_id}; no freeform user-content fields (Part 8) |
| T-1431.03-03 | Elevation of Privilege | SELECTED_REACH vs truth-claim | mitigate | SELECTED_REACH is created_by=system bookkeeping; truth-claim promotion stays on the confirmNode human-byUser path (Part 9 carve-out) |
| T-1431.03-04 | Tampering | edge/event registry floor | mitigate | Additive idiom + floor-not-count tests guarantee DEFERRED/REJECTED + baseline events cannot regress |
| T-1431.03-SC | Tampering | npm installs | accept | No package installs; thin glue over shipped recorders + chokepoint |
</threat_model>

<verification>
- node tests/test-dial-close-reach.cjs passes
- ALLOWED_EDGE_TYPES has PIVOTED + SELECTED_REACH + DEFERRED + REJECTED
- EVENT_TYPES has f_selector_pivot + f_selector_sync_confirmed
- grep -E "room\.db|require\('\.\./core/navigation/" lib/workflow/dial-close-reach.cjs returns 0 (only the navigation.cjs chokepoint)
</verification>

<success_criteria>
closeReach() writes the correct typed edges/events for all four outcomes through navigation.cjs only; SELECTED_REACH closes the Layer-3 gap and satisfies FILEVAL-01; PIVOTED makes "why not" graph data; the registry floors are preserved.
</success_criteria>

<output>
Create `.planning/phases/143.1-dial-tui-capability-selector/143.1-03-SUMMARY.md` when done.
</output>
