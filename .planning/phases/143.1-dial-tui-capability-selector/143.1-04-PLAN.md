---
phase: 143.1-dial-tui-capability-selector
plan: 04
type: execute
wave: 2
depends_on: ["143.1-01", "143.1-02"]
files_modified:
  - lib/hmi/dial-presenter.cjs
  - tests/test-dial-render-states.cjs
autonomous: true
requirements: [DIALTUI-03, DIALTUI-10]
canon_parts: [Part 3, Part 7, Part 8]

must_haves:
  truths:
    - "The chooser renders AS Shape F.1 Brain-suggestion variant (applyBrainSuggestionVariant + composeBrainOptionRows + appendAskUserQuestionTrailer) - no bespoke widget"
    - "The chooser is hard 3 rows (MAX_K=3); the preview is 5 (DIAL_REACH_K=5); overflow is a footer 'top-3 of N' stat-strip, never pagination"
    - "The dial is the right-aligned NN% confidence column prefixed by the filled/empty triangle glyph - never the literal '(Recommended)' string"
    - "All five UI-SPEC render states (S1 1-marker, S2 2-marker near-tie, S3 Mode B, S4 Tier-0, S5 partial-slot) render correctly"
    - "Mode B / Tier 0 render with zero filled markers + an explicit framing line so the cold room READS AS INTENTIONAL, not broken"
    - "The presenter consumes the surface-agnostic ReachList; no ANSI is baked into the core (resolve/format split)"
  artifacts:
    - path: "lib/hmi/dial-presenter.cjs"
      provides: "CLI presenter: ReachList -> Shape F.1 Brain-variant rows + confidence column + footer; surface-agnostic core honored"
      min_lines: 90
    - path: "tests/test-dial-render-states.cjs"
      provides: "Render assertions for S1-S5 marker counts, glyph swap, column alignment, footer, cold-room framing"
  key_links:
    - from: "lib/hmi/dial-presenter.cjs"
      to: "lib/hmi/shape-f1-renderer.cjs"
      via: "applyBrainSuggestionVariant + composeBrainOptionRows + appendAskUserQuestionTrailer reused"
      pattern: "applyBrainSuggestionVariant|composeBrainOptionRows"
    - from: "lib/hmi/dial-presenter.cjs"
      to: "lib/hmi/dial-reach-orchestrator.cjs"
      via: "consumes ReachList from Plan 01"
      pattern: "buildReachList|reaches"
    - from: "lib/hmi/dial-presenter.cjs"
      to: "lib/hmi/dial-label-composer.cjs"
      via: "swaps mechanism-verb rows for WHAT-THEY-GET labels from Plan 02"
      pattern: "composeLabel"
---

<objective>
Render the dial (DIALTUI-03) and ship the tri-polar surface-agnostic core + CLI master template (DIALTUI-10). The chooser renders AS Shape F.1 Brain-suggestion variant; the "dial" is the right-aligned confidence column; the cold room must read as intentional.

Purpose: Canon-legal graded ranking without a widget the host will not give us. The presenter consumes the ReachList (Plan 01) and the labels (Plan 02), reuses the shipped Shape F.1 renderer (no bespoke widget, MAX_K=3 untouched), and proves all five UI-SPEC render states. The surface-agnostic core (no ANSI) is the Desktop/Cowork foundation; CLI is the master template.

Output: lib/hmi/dial-presenter.cjs + tests/test-dial-render-states.cjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-CONTEXT.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-01-SUMMARY.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-02-SUMMARY.md

<interfaces>
<!-- The renderer to reuse + the contracts from Wave 1. Do NOT re-explore. -->

From lib/hmi/shape-f1-renderer.cjs:
  MARKER_RECOMMENDED = '▶'  (filled, line 58)
  MARKER_ROW = '▷'          (empty, line 59)
  renderShapeF1({ tier, recommendedVerb?, verbs?, header?, personaContext? }) -> { zones, contract }
  Brain-suggestion variant helpers (per UI-SPEC Section 14 reuse map):
    applyBrainSuggestionVariant, composeBrainOptionRows, appendAskUserQuestionTrailer, aliasToCanonical
  Allowed glyphs only: ▶ ▷ ■ • → . MAX_K=3 host clamp (NEVER raised).

From lib/hmi/dial-reach-orchestrator.cjs (Plan 01): buildReachList(...) -> ReachList; DIAL_REACH_K=5
From lib/hmi/dial-label-composer.cjs (Plan 02): composeLabel(reach_id, slotContext) -> {label, canonical_verb}

The 5 render states (UI-SPEC Section 7 + 13 ASCII mockups):
  S1 mode_a clear leader  -> 1 filled ▶ (reach #1); rows 2-3 ▷; right-aligned NN%; footer 'top-3 of 5'
  S2 mode_a near-tie      -> 2 filled ▶ (reach #1+#2, margin<0.15); row 3 ▷
  S3 mode_b               -> 0 filled; all ▷; framing 'No recommendation - offline. You pick.'; honest low %
  S4 tier_0               -> 0 filled; minimal set; all ▷; confidence '--'; framing 'New room - nothing to rank yet. Start anywhere.'
  S5 partial-slot         -> markers unchanged; one row drops to the generic JTBD one_line (label degrades, marker logic intact)

Tri-polar (UI-SPEC Section 10, DIALTUI-10): core returns the structured ReachList (no ANSI). CLI = master template (this plan). Desktop/Cowork mappings are RECORDED (Desktop render proof DEFERRED per resolved OQ1; Cowork = per-actor {topic} label + shared SELECTED_REACH edge per resolved OQ2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the S1-S5 render-state test</name>
  <read_first>
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 7 state matrix, Section 13 the 5 ASCII mockups, Section 6.4 the looks-intentional mandate, Section 12 V1/V2/V3/V5/V7)
    - lib/hmi/shape-f1-renderer.cjs (MARKER glyphs + the Brain-variant helpers; the {zones,contract} return)
  </read_first>
  <files>tests/test-dial-render-states.cjs</files>
  <behavior>
    - S1: rendered body has exactly 1 '▶' and the rest '▷'; highest % on the ▶ row; footer contains 'top-3 of 5'
    - S2: exactly 2 '▶'; row 3 is '▷'
    - S3 (mode_b): zero '▶'; framing line 'No recommendation - offline. You pick.' present; honest low % (not blank, not fake-high)
    - S4 (tier_0): zero '▶'; confidence column shows '--'; framing 'New room - nothing to rank yet. Start anywhere.'; minimal reach set
    - S5: marker count matches the gate; the degraded row carries the generic JTBD one_line; no raw '{topic}' anywhere
    - V2: the literal string '(Recommended)' never appears in any rendered output (glyph swap only)
    - V5: the chooser body has exactly 3 option rows (MAX_K=3); preview/total reflects 5 (DIAL_REACH_K)
    - V7 (looks-intentional): S3/S4 output is distinguishable from S1 - presence of framing line + even ▷ markers + '--'/honest-low %
    - DIALTUI-10 core: the ReachList consumed by the presenter contains no ANSI escape codes (the core stays surface-agnostic)
  </behavior>
  <action>Create the render-state test driving the presenter with ReachLists for S1-S5 (build them via the Plan 01 orchestrator with crafted scores, or fixtures). Assert glyph counts, the no-'(Recommended)' rule, the 3-row chooser, the footer stat-strip, and the cold-room framing lines. Add an assertion that the ReachList input contains zero ANSI (\x1b) so the surface-agnostic core is proven. Test MUST fail before Task 2.</action>
  <verify>
    <automated>node tests/test-dial-render-states.cjs; test $? -ne 0 && echo "RED OK (expected fail)"</automated>
  </verify>
  <acceptance_criteria>Test asserts all five states' marker/glyph/footer/framing per UI-SPEC Section 13 + the V2/V5/V7 dimensions + ANSI-free core; fails because the presenter is absent.</acceptance_criteria>
  <done>Failing render-state test committed; one assertion block per S1-S5.</done>
</task>

<task type="auto">
  <name>Task 2: Build the CLI presenter (Shape F.1 Brain-variant + confidence column + tri-polar core)</name>
  <read_first>
    - lib/hmi/shape-f1-renderer.cjs (applyBrainSuggestionVariant + composeBrainOptionRows + appendAskUserQuestionTrailer + the MARKER glyphs - reuse, do NOT re-implement a renderer; no bespoke widget per AP1)
    - lib/core/nav-dial.cjs (the resolve/format split - the presenter is the format half; the orchestrator ReachList is the resolve half; no ANSI in the core)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 6.1 component anatomy, Section 13 mockups, Section 10 tri-polar mapping)
  </read_first>
  <files>lib/hmi/dial-presenter.cjs</files>
  <action>Create the presenter that takes a ReachList (Plan 01) and renders the Shape F.1 Brain-suggestion variant via the shipped helpers (applyBrainSuggestionVariant + composeBrainOptionRows + appendAskUserQuestionTrailer) - no bespoke scrollable widget, MAX_K=3 untouched. For each offered reach: prefix MARKER_RECOMMENDED('▶') when reach.recommended else MARKER_ROW('▷'); place the right-aligned NN% confidence column (the "dial"); use composeLabel (Plan 02) to swap the mechanism-verb row for the WHAT-THEY-GET label. Render the footer 'top-3 of N' stat-strip from offered_count/total_count; route overflow to the host free-text row (never in-prompt pagination). For S3/S4 render the explicit framing line and '--'/honest-low % so the cold room reads as intentional (Section 6.4 / V7). NEVER render the literal '(Recommended)' (AP4). Keep the structured ReachList (the core) ANSI-free; ANSI/color belongs only in the CLI format layer (resolve/format split, DIALTUI-10). Record the Desktop mapping (Larry voices reaches conversationally; NN% -> markdown bold) and the Cowork mapping (per-actor {topic} label, shared SELECTED_REACH edge) as a documented block in the module header per the resolved OQs - the Desktop render proof is DEFERRED (V8), not built here.</action>
  <verify>
    <automated>node tests/test-dial-render-states.cjs</automated>
  </verify>
  <acceptance_criteria>test-dial-render-states.cjs passes: S1-S5 render per the mockups; 1/2/0/0 markers; 3-row chooser; footer stat-strip; cold-room framing; no '(Recommended)' literal; ANSI-free core; Desktop+Cowork mappings documented in the header.</acceptance_criteria>
  <done>Presenter green against all five states; Shape F.1 reused (no bespoke widget); tri-polar core surface-agnostic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReachList -> CLI format layer | ANSI/color must live only in the format layer; the core stays surface-agnostic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1431.04-01 | Tampering | the chooser render | mitigate | Reuse Shape F.1 helpers; test asserts exactly 3 option rows (MAX_K untouched) - no bespoke widget, no MAX_K raise (AP1/AP2) |
| T-1431.04-02 | Spoofing | the Recommended marker | mitigate | Test asserts glyph swap only + no '(Recommended)' literal; accent reserved to '▶' (AP4, V2/V3) |
| T-1431.04-03 | Repudiation | cold-room intentionality | mitigate | V7 test: S3/S4 distinguishable from S1 via framing line + even ▷ + '--'/honest-low % |
| T-1431.04-SC | Tampering | npm installs | accept | No package installs; reuses shipped shape-f1-renderer |
</threat_model>

<verification>
- node tests/test-dial-render-states.cjs passes
- grep -c "Recommended)" lib/hmi/dial-presenter.cjs returns 0 (no literal marker text)
- grep -E "applyBrainSuggestionVariant|composeBrainOptionRows" lib/hmi/dial-presenter.cjs returns >=1 (renderer reused)
</verification>

<success_criteria>
All five UI-SPEC render states render correctly via the reused Shape F.1 Brain-variant; the dial is the confidence column; the cold room reads as intentional; the surface-agnostic ReachList core carries no ANSI; Desktop/Cowork mappings recorded (Desktop proof deferred).
</success_criteria>

<output>
Create `.planning/phases/143.1-dial-tui-capability-selector/143.1-04-SUMMARY.md` when done.
</output>
