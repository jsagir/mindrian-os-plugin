---
phase: 143.1-dial-tui-capability-selector
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/hmi/dial-label-composer.cjs
  - lib/hmi/jtbd-taxonomy.json
  - tests/test-dial-label-bank-drift.cjs
autonomous: true
requirements: [DIALTUI-04, DIALTUI-05]
canon_parts: [Part 4, Part 7, Part 8]

must_haves:
  truths:
    - "The composer returns a WHAT-THEY-GET label for each of the 5 reach ids, never a mechanism noun, never a raw {topic} slot"
    - "The canonical verb is preserved alongside the label so the graph edge persists the mechanism term"
    - "The brain_consult and deep_research {framework} slots pass the dual-egress Part-8 audit; the other 3 labels are render-only"
    - "The degradation ladder drops an unresolvable slot to the generic JTBD one_line, never a raw slot"
    - "The label bank contains zero em-dashes, zero banned mechanism nouns, and exactly the 5 canonical reach ids"
    - "jtbd-taxonomy.json contains zero em-dashes"
  artifacts:
    - path: "lib/hmi/dial-label-composer.cjs"
      provides: "Feynman-JTBD hybrid label composer with FNV-1a deterministic pick + Part-8 egress seam"
      min_lines: 80
    - path: "tests/test-dial-label-bank-drift.cjs"
      provides: "Drift test: zero em-dashes, zero mechanism nouns, exactly 5 reach ids, egress-seam adversarial fixtures"
  key_links:
    - from: "lib/hmi/dial-label-composer.cjs"
      to: "navigation.getActiveFocus / getNeighborhood"
      via: "{topic} slot filled from active focus, never room.db directly, never the raw message"
      pattern: "getActiveFocus|getNeighborhood"
    - from: "lib/hmi/dial-label-composer.cjs"
      to: "the Part-8 egress audit"
      via: "brain_consult/deep_research {framework} slot audited before it can egress"
      pattern: "audit|egress|framework"
---

<objective>
Build the Feynman-JTBD label composer (DIALTUI-04) and its drift test (DIALTUI-05), and fix the 7 pre-existing em-dashes in jtbd-taxonomy.json. The composer swaps mechanism-verb rows for WHAT-THEY-GET labels at render time while the canonical verb still persists to the graph edge.

Purpose: The dial table is authored in MECHANISM language - exactly what the navigator must NOT read. The composer is the deterministic render-time alias, modeled on rs-query-to-text.cjs (frozen templates + FNV-1a + dual-egress Part-8 audit). The drift test + the egress seam guard the boundary BEFORE any consumer lands.

Output: lib/hmi/dial-label-composer.cjs + tests/test-dial-label-bank-drift.cjs + clean jtbd-taxonomy.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-CONTEXT.md

<interfaces>
<!-- Model the composer on this shipped architecture. Do NOT re-explore. -->

From lib/core/rs-query-to-text.cjs (the architecture to mirror):
  - Frozen VOICE_TEMPLATES (N kinds x 3-4 templates); deterministic hash-based pick (same input -> same render)
  - SEAM A (input audit) + SEAM B (post-render audit) using rs-egress-prompts.cjs::auditQueryString / auditQueryObject / FORBIDDEN_PATTERNS
  - NEVER imports the Brain client; RENDERS only

From lib/core/navigation.cjs:
  getActiveFocus(...)   (the {topic} slot source - single focus node)
  getNeighborhood(...)  (the two-node {topic_a}/{topic_b} slot source)

The 5 frozen template families (UI-SPEC Section 9 - WHAT-THEY-GET voice, hyphens only):
  context_block  -> "Pull up what we decided about {topic}."                                  slot: getActiveFocus
  contradiction  -> "Show where {topic_a} and {topic_b} pull against each other."             slot: getNeighborhood (two nodes)
  cross_room     -> "Bring in what {room_name} already worked out about {topic}."             slot: room switch + focus
  brain_consult  -> "Suggest the next move for {framework_or_situation}."                     slot: {framework} GENERIC handle (Part 8)
  deep_research  -> "Go find out what the world knows about {topic}, framed through {framework}."  slot: {framework} GENERIC handle (Part 8)

Banned mechanism nouns (drift test, UI-SPEC Section 9):
  'Context Block', 'contradiction surface', 'cross-room', 'Brain consult', 'deep research plan'

jtbd-taxonomy.json em-dash lines to fix: 71, 197, 227, 285, 346, 379, 387 (replace each em-dash with a hyphen, preserve meaning per the no-em-dashes HARD rule).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the label-bank drift test + Part-8 egress fixtures</name>
  <read_first>
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 9 the 5 families + composer rules + the drift-test contract; Section 11.1 the Part-8 egress seam)
    - lib/core/rs-query-to-text.cjs (SEAM A/SEAM B pattern; the rs-egress-prompts.cjs imports)
    - lib/hmi/jtbd-taxonomy.json (the one_line fields used as the degradation fallback; the 7 em-dashes to fix)
  </read_first>
  <files>tests/test-dial-label-bank-drift.cjs</files>
  <behavior>
    - Template bank has exactly the 5 canonical reach ids - no more, no fewer (assert the key set)
    - Zero em-dashes anywhere in the template bank
    - Zero banned mechanism nouns ('Context Block','contradiction surface','cross-room','Brain consult','deep research plan') in any template
    - jtbd-taxonomy.json contains zero em-dashes after the fix (grep -c returns 0)
    - Egress seam: composeLabel('brain_consult', {framework:'<raw user artifact text>'}) is rejected/redacted by the audit - only a GENERIC handle crosses
    - Render-only proof: the 3 non-egress labels (context_block/contradiction/cross_room) never invoke the egress audit path (they cannot egress)
    - Degradation ladder: an unresolvable {topic} slot drops to the generic JTBD one_line; the raw '{topic}' string never appears in any rendered label
  </behavior>
  <action>Create the drift + egress test. Use FORBIDDEN_PATTERNS-style adversarial fixtures for the {framework} slot (raw artifact text, numbers, names) and assert the audit blocks them. Assert the banned-noun sweep and em-dash sweep over both the template bank AND jtbd-taxonomy.json. Test MUST fail before Task 2/3.</action>
  <verify>
    <automated>node tests/test-dial-label-bank-drift.cjs; test $? -ne 0 && echo "RED OK (expected fail)"</automated>
  </verify>
  <acceptance_criteria>Test exercises the 5-id coverage, em-dash sweep (bank + taxonomy), banned-noun sweep, the {framework} egress seam, render-only proof, and the degradation ladder; fails because the composer + the em-dash fix are absent.</acceptance_criteria>
  <done>Failing drift test committed; assertions map to UI-SPEC Section 9 + 11.1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the dial-label-composer + Part-8 egress seam</name>
  <read_first>
    - lib/core/rs-query-to-text.cjs (FNV-1a deterministic pick + SEAM A/SEAM B; reuse the rs-egress-prompts.cjs audit helpers verbatim per Part 7)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 9 composer rules + degradation ladder; Section 11.1 the dual-egress seam scoped to brain_consult + deep_research only)
    - lib/core/navigation.cjs (getActiveFocus / getNeighborhood - the slot sources; never read room.db directly)
  </read_first>
  <files>lib/hmi/dial-label-composer.cjs</files>
  <action>Create the hybrid composer: one frozen template family per reach_id (the 5 from UI-SPEC Section 9), a runtime {topic} slot filled from navigation.getActiveFocus / getNeighborhood (passed in by the caller, never read from room.db here), FNV-1a deterministic pick. Implement composeLabel(reach_id, slotContext) returning {label, canonical_verb} - the canonical_verb is preserved for the graph edge per DIALTUI-04. Scope the dual-egress audit (reuse auditQueryString/auditQueryObject from rs-egress-prompts.cjs) to the brain_consult and deep_research {framework} slot ONLY; the other 3 families are render-only and never invoke the audit. Default on ambiguity: do not egress (Part 8). Implement the degradation ladder: full focus -> fill {topic}; recent message only -> noun from message; unresolvable -> generic JTBD one_line from jtbd-taxonomy.json; NEVER render a raw '{topic}'. Pure render module, zero Brain client import.</action>
  <verify>
    <automated>node tests/test-dial-label-bank-drift.cjs</automated>
  </verify>
  <acceptance_criteria>Drift test passes for the composer assertions: 5-id coverage, zero banned nouns, egress seam blocks raw framework content, render-only labels skip the audit, degradation ladder never emits a raw slot.</acceptance_criteria>
  <done>Composer green; canonical_verb preserved alongside label; egress seam scoped to the 2 Brain/web labels.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the 7 em-dashes in jtbd-taxonomy.json</name>
  <read_first>
    - lib/hmi/jtbd-taxonomy.json (lines 71, 197, 227, 285, 346, 379, 387 - the em-dashes)
    - CLAUDE.md (the no-em-dashes HARD rule - hyphens only)
  </read_first>
  <files>lib/hmi/jtbd-taxonomy.json</files>
  <action>Replace each of the 7 em-dashes (lines 71, 197, 227, 285, 346, 379, 387) with a hyphen, preserving the surrounding copy's meaning and valid JSON. These one_line fields are the degradation-ladder fallback consumed by the composer, so the copy must stay clean and human-readable.</action>
  <verify>
    <automated>grep -cP "\x{2014}" lib/hmi/jtbd-taxonomy.json | grep -qx 0 && node -e "JSON.parse(require('fs').readFileSync('lib/hmi/jtbd-taxonomy.json','utf8'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>grep -c em-dash returns 0; the file still parses as valid JSON; the drift test's taxonomy em-dash assertion passes.</acceptance_criteria>
  <done>Zero em-dashes in jtbd-taxonomy.json; file is valid JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {framework} slot -> Brain/web | brain_consult + deep_research labels can become outbound queries; only generic handles may cross |
| slotContext -> composer | Caller-supplied focus/message text; render-only labels must never egress it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1431.02-01 | Information Disclosure | dial-label-composer {framework} slot | mitigate | Dual-egress audit (reuse rs-egress-prompts auditQueryString) blocks raw user artifacts/numbers/names; default-deny on ambiguity (Part 8, AP7) |
| T-1431.02-02 | Information Disclosure | the 3 render-only labels | mitigate | Drift test proves context_block/contradiction/cross_room never invoke the egress path |
| T-1431.02-03 | Tampering | label bank drift | mitigate | Drift test asserts exactly 5 reach ids + zero banned mechanism nouns + zero em-dashes; CI gate |
| T-1431.02-SC | Tampering | npm installs | accept | No package installs; reuses shipped rs-egress-prompts.cjs |
</threat_model>

<verification>
- node tests/test-dial-label-bank-drift.cjs passes
- grep -cP "\x{2014}" lib/hmi/jtbd-taxonomy.json returns 0
- grep -cP "\x{2014}" lib/hmi/dial-label-composer.cjs returns 0
</verification>

<success_criteria>
Composer renders the 5 WHAT-THEY-GET labels with the canonical verb preserved; the {framework} egress seam blocks user content; the drift test is green; jtbd-taxonomy.json is em-dash-free.
</success_criteria>

<output>
Create `.planning/phases/143.1-dial-tui-capability-selector/143.1-02-SUMMARY.md` when done.
</output>
