---
phase: 143.1-dial-tui-capability-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/hmi/dial-reach-orchestrator.cjs
  - tests/test-dial-reach-orchestrator.cjs
autonomous: true
requirements: [DIALTUI-01, DIALTUI-02]
canon_parts: [Part 3, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "The orchestrator returns a structured ReachList of exactly 5 reaches ranked desc by score (DIAL_REACH_K=5), while MAX_K=3 stays untouched"
    - "Each reach carries provenance {score, source, brain_component, local_component, recommended}"
    - "Reach #1 is recommended IFF tier_mode==mode_a AND score>=0.70; reach #2 also only when score>=0.70 AND margin<0.15"
    - "Mode B and Tier 0 ReachLists carry zero recommended:true reaches"
    - "The orchestrator is pure, synchronous, and makes zero live Brain calls at render time"
  artifacts:
    - path: "lib/hmi/dial-reach-orchestrator.cjs"
      provides: "Dial-reach orchestrator + DIAL_REACH_K constant + frozen Recommended gate"
      contains: "DIAL_REACH_K"
      min_lines: 100
    - path: "tests/test-dial-reach-orchestrator.cjs"
      provides: "Render-state assertions for S1/S2/S3/S4 marker counts + provenance fields + the two-K separation"
  key_links:
    - from: "lib/hmi/dial-reach-orchestrator.cjs"
      to: "lib/workflow/f-selector-ranker.cjs"
      via: "rankForSelector consumed for candidate scoring (D4 blend reused, not re-invented)"
      pattern: "rankForSelector"
    - from: "lib/hmi/dial-reach-orchestrator.cjs"
      to: "the frozen gate"
      via: "MARGIN_THRESHOLD=0.15 + 0.70 floor applied per-reach"
      pattern: "0\\.7|MARGIN_THRESHOLD"
---

<objective>
Build the dial-reach orchestrator (DIALTUI-01): the surface-agnostic core that ranks the 5 canonical reaches with per-reach provenance and applies the frozen Recommended gate (DIALTUI-02). This is the load-bearing artifact every other plan consumes - the structured ReachList contract.

Purpose: One structured object (no ANSI, no host formatting) that every surface (CLI/Desktop/Cowork) reads. The two-K separation (MAX_K=3 chooser vs DIAL_REACH_K=5 preview) is the load-bearing architectural decision per UI-SPEC Section 1.3.

Output: lib/hmi/dial-reach-orchestrator.cjs + its render-state test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-CONTEXT.md

<interfaces>
<!-- Contracts the executor consumes. Extracted from the codebase. Do NOT re-explore. -->

From lib/workflow/f-selector-ranker.cjs:
  rankForSelector({jtbd, problemType, focusNodeId, roomState, packetOptional, k=3}) -> Array<RankedItem>
  RankedItem: { command, jtbd_label, jtbd_summary, teaching, framework, score, why, source, investment_level }
  MAX_K = 3  (the AskUserQuestion chooser clamp - NEVER raised, NEVER imported as the dial cap)
  D4 score = (brain_confidence*0.40 + (1-recency_decay)*0.30*investment_level + problem_type_bind*0.30*investment_level) / (0.40 + 0.30*investment_level + 0.30*investment_level)

The 5 canonical reach ids (frozen, UI-SPEC Section 0), in order:
  context_block, contradiction, cross_room, brain_consult, deep_research

ReachList contract (UI-SPEC Section 6):
  ReachList { reaches: Reach[<=5], tier_mode: 'mode_a'|'mode_b'|'tier_0', offered_count:int<=3, total_count:int }
  Reach { reach_id, canonical_verb, label, score, source:'local'|'registry'|'brain'|'blend', brain_component, local_component, recommended:bool }

The frozen gate (UI-SPEC Section 7, never re-tuned without INTERFACE_VERSION bump):
  reach#1.recommended = (tier_mode=='mode_a') AND (reach#1.score >= 0.70)
  reach#2.recommended = (tier_mode=='mode_a') AND (reach#2.score >= 0.70) AND ((reach#1.score - reach#2.score) < 0.15)
  all others = false
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing render-state test for the orchestrator</name>
  <read_first>
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 6 ReachList contract, Section 7 state matrix S1-S4, the frozen gate)
    - lib/workflow/f-selector-ranker.cjs (rankForSelector signature + MAX_K=3 + the D4 blend; the _setRegistry/_setTaxonomy test seams at lines 105-116)
    - tests/run-all-143.sh (the node-suite assertion style to mirror)
  </read_first>
  <files>tests/test-dial-reach-orchestrator.cjs</files>
  <behavior>
    - S1 (mode_a clear leader): scores like [0.87,0.61,0.54] -> exactly 1 reach.recommended===true (reach #1)
    - S2 (mode_a near-tie): scores [0.79,0.71,0.50], margin 0.08<0.15 -> exactly 2 recommended (reach #1 and #2)
    - S2 negative (margin guard): [0.79,0.55,...], margin 0.24>0.15 -> exactly 1 recommended (never lower 0.70 to force #2)
    - S3 (mode_b): any scores -> zero recommended:true
    - S4 (tier_0): minimal reach set -> zero recommended:true
    - Provenance: every reach exposes score, source, brain_component, local_component, recommended (all present, correct types)
    - Two-K separation: orchestrator returns up to DIAL_REACH_K(5) reaches; assert require('../lib/workflow/f-selector-ranker.cjs') MAX_K is still 3 (untouched)
    - Pure/sync: orchestrator returns synchronously; a grep assertion confirms zero fetch/http/Brain-client require in the orchestrator source
  </behavior>
  <action>Create the test using the f-selector-ranker _test._setRegistry seam to inject deterministic candidate scores. Drive the orchestrator with crafted score vectors per S1-S4. Assert marker counts equal the UI-SPEC state matrix exactly. Include a source-grep tripwire asserting the orchestrator file contains no 'fetch'/'require.*brain'/'http' tokens (Part 8 LOCAL-only). Test MUST fail before Task 2 (orchestrator does not exist yet).</action>
  <verify>
    <automated>node tests/test-dial-reach-orchestrator.cjs; test $? -ne 0 && echo "RED OK (expected fail)"</automated>
  </verify>
  <acceptance_criteria>Test file exists, exercises S1/S2/S2-negative/S3/S4 + provenance + two-K + purity, and fails because the orchestrator module is absent.</acceptance_criteria>
  <done>Failing test committed; assertions map 1:1 to UI-SPEC state matrix marker counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the dial-reach orchestrator + DIAL_REACH_K + frozen gate</name>
  <read_first>
    - lib/workflow/f-selector-ranker.cjs (rankForSelector + the D4 blend; reuse it for candidate scoring per DIALTUI-01 - do NOT invent a second formula)
    - lib/core/nav-dial.cjs (the resolve/format split pattern - the orchestrator returns structured data; formatting lives in the CLI presenter, Plan 04)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (Section 6 scoring rules: registry-only reaches default brain_confidence=0.5 so they can never solo-cross 0.70; Section 7 frozen gate)
  </read_first>
  <files>lib/hmi/dial-reach-orchestrator.cjs</files>
  <action>Create the orchestrator as a pure synchronous module. Declare DIAL_REACH_K=5 as a module constant SEPARATE from f-selector-ranker MAX_K=3 (never import or mutate MAX_K). Compose rankForSelector output (called with k=DIAL_REACH_K), resolveTierMode (mode_a/mode_b/tier_0 from roomState - Brain reachability + BRAIN.md presence), and the offer-resolver margin math into a ranked list of up to 5 Reach objects. For each reach attach provenance {score, source, brain_component (brain_confidence*0.40), local_component (the recency_decay+problem_type_bind terms), recommended}. Apply the FROZEN gate verbatim: MARGIN_THRESHOLD=0.15; reach#1.recommended when mode_a AND score>=0.70; reach#2.recommended when mode_a AND score>=0.70 AND margin<0.15; all others false. Set offered_count (min(reaches, 3)) and total_count (N) for the footer. Registry-only reaches default brain_confidence=0.5 so a cold room yields zero markers by design. Zero live Brain calls - brain_confidence is the pre-baked BRAIN.md-derived prior passed in via roomState. Per Part 9, the orchestrator reads no room.db directly; it consumes what callers pass. Export { buildReachList, DIAL_REACH_K, MARGIN_THRESHOLD, REACH_IDS }.</action>
  <verify>
    <automated>node tests/test-dial-reach-orchestrator.cjs</automated>
  </verify>
  <acceptance_criteria>tests/test-dial-reach-orchestrator.cjs passes; DIAL_REACH_K=5 exported and distinct from MAX_K=3; S1=1 marker, S2=2 markers, S3/S4=0 markers; provenance fields present; source grep confirms zero network tokens.</acceptance_criteria>
  <done>Orchestrator green against the full state-matrix test; the two-K separation holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roomState -> orchestrator | Caller-supplied scoring inputs cross here; the orchestrator must not egress them |
| orchestrator -> Brain | MUST NOT exist; brain_confidence is pre-baked, zero live calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1431.01-01 | Information Disclosure | dial-reach-orchestrator.cjs | mitigate | Source-grep tripwire in the test asserts zero fetch/http/brain-client tokens; orchestrator is pure LOCAL (Part 8) |
| T-1431.01-02 | Tampering | the frozen 0.70 gate | mitigate | Test asserts S2-negative (margin>0.15) yields exactly 1 marker - proves 0.70 is never lowered to force a second marker (AP3) |
| T-1431.01-SC | Tampering | npm installs | accept | No package installs in this plan; pure CJS over shipped modules |
</threat_model>

<verification>
- node tests/test-dial-reach-orchestrator.cjs passes
- grep -c "DIAL_REACH_K" lib/hmi/dial-reach-orchestrator.cjs returns >=1
- grep -E "fetch|require\(.*brain|http" lib/hmi/dial-reach-orchestrator.cjs returns 0 (Part 8 LOCAL-only)
</verification>

<success_criteria>
ReachList with 5 ranked reaches + full provenance; frozen gate yields the UI-SPEC marker counts (S1=1, S2=2, S3/S4=0); DIAL_REACH_K=5 separate from the untouched MAX_K=3.
</success_criteria>

<output>
Create `.planning/phases/143.1-dial-tui-capability-selector/143.1-01-SUMMARY.md` when done.
</output>
