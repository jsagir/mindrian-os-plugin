---
phase: 143.1-dial-tui-capability-selector
plan: 05
type: execute
wave: 2
depends_on: ["143.1-03"]
files_modified:
  - lib/core/feynman/dial-memory-renderer.cjs
  - tests/test-dial-memory-renderer.cjs
  - tests/test-dial-graph-relationship-layer.cjs
autonomous: true
requirements: [MEMDIAL-01, MEMDIAL-02, MEMDIAL-03]
canon_parts: [Part 4, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "The dial memory renderer is a pure function reading ONLY via navigation.cjs - zero filesystem reads, zero Brain calls"
    - "The rendered section is sentinel-bounded and the human-authored body is byte-preserved across regeneration"
    - "The section shows available reaches, last selected, current recommended - rendered FROM the graph, not hand-authored"
    - "Regeneration from the same graph state is idempotent (byte-identical between two runs)"
    - "The dial relationship layer (5 reach ids + SELECTED_REACH + PIVOTED edges from Plan 03) is queryable via navigation.cjs, never a flat log"
  artifacts:
    - path: "lib/core/feynman/dial-memory-renderer.cjs"
      provides: "Pure dial memory renderer on the Phase 124 timeline-renderer pattern"
      min_lines: 70
    - path: "tests/test-dial-memory-renderer.cjs"
      provides: "Idempotence + byte-preservation + reads-only-via-navigation assertions"
    - path: "tests/test-dial-graph-relationship-layer.cjs"
      provides: "MEMDIAL-01 queryability assert + FILEVAL-01 selection-writes-typed-edge lock"
  key_links:
    - from: "lib/core/feynman/dial-memory-renderer.cjs"
      to: "lib/core/navigation.cjs"
      via: "all reads via the chokepoint (getNeighborhood / findRecentChanges); pure renderer per Phase 124 D-03"
      pattern: "require\\('\\.\\./navigation\\.cjs'\\)"
    - from: "tests/test-dial-graph-relationship-layer.cjs"
      to: "the SELECTED_REACH/PIVOTED edges"
      via: "queries the edges written by closeReach (Plan 03) and asserts they are graph-queryable"
      pattern: "SELECTED_REACH|PIVOTED"
---

<objective>
Ship the dial memory layer: a sentinel-bounded MD section rendered FROM the graph (MEMDIAL-02/03), and lock the dial-as-relationship-layer queryability (MEMDIAL-01) + the FILEVAL-01 selection-writes-typed-edge invariant.

Purpose: What the navigator reads in the memory file is exactly what the graph holds (Part 9 - SQL is the source of truth, MD is the legible projection). The SELECTED_REACH/PIVOTED edges from Plan 03 ARE the relationship layer (MEMDIAL-01); this plan asserts they are queryable, not a flat log, and renders the legible projection idempotently on the proven Phase 124 pattern.

Output: lib/core/feynman/dial-memory-renderer.cjs + the renderer test + the relationship-layer/FILEVAL assert test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-CONTEXT.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-03-SUMMARY.md

<interfaces>
<!-- The Phase 124 pattern to mirror + the Plan 03 edges to query. Do NOT re-explore. -->

From lib/core/feynman/timeline-renderer.cjs (the pattern to model on, Phase 124):
  - Pure function (db, sectionSlug, opts) -> { markdown_body, summary_stats }
  - Reads ONLY via lib/core/navigation.cjs (D-03 closed chokepoint); ZERO fs reads, ZERO Brain calls
  - Sentinel-bounded section; the runner writes ONLY inside the sentinels; human body byte-preserved (D-02 hard invariant)
  - Empty-state and frozen output format (D-05)

From lib/core/navigation.cjs (the chokepoint - import THIS):
  getNeighborhood, findRecentChanges, getActiveFocus  (re-exported readers)

From Plan 03 (already shipped this wave's dependency):
  SELECTED_REACH + PIVOTED edges in ALLOWED_EDGE_TYPES; closeReach writes them via navigation.cjs

MEMDIAL section content (MEMDIAL-02): available reaches (the 5 reach ids), last selected (most-recent SELECTED_REACH), current recommended (the gate result), recent research conclusions. Sentinel-bounded; rendered FROM the graph (MEMDIAL-03); idempotent regeneration; no hand-authored dial state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the dial-memory renderer test (idempotence + byte-preservation + reads-only-via-navigation)</name>
  <read_first>
    - lib/core/feynman/timeline-renderer.cjs (the pure-renderer contract + the sentinel/byte-preservation invariant + the empty-state format to mirror)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (the MEMDIAL section content; the Part 9 graph-as-source-of-truth mandate)
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs (the adversarial Part-9 invariant style to mirror, referenced in the Phase 124 map row)
  </read_first>
  <files>tests/test-dial-memory-renderer.cjs</files>
  <behavior>
    - Renders a section listing the 5 available reaches + last-selected (from the most recent SELECTED_REACH edge) + current-recommended
    - Idempotence: rendering twice from the same graph state yields byte-identical markdown
    - Byte-preservation: a human-authored body outside the sentinels survives regeneration unchanged
    - Reads-only-via-navigation: a grep-audit asserts the renderer imports only lib/core/navigation.cjs (no fs read, no Brain client, no direct submodule require) - the Phase 124 D-03 invariant
    - Empty state: a room with zero SELECTED_REACH edges renders a clean 'no dial activity yet' section (not a crash, not a raw template)
  </behavior>
  <action>Create the renderer test against a fixture room.db seeded with SELECTED_REACH/PIVOTED edges (use closeReach from Plan 03 or direct fixture edges through navigation.writeEdge). Assert idempotence (two renders byte-equal), sentinel byte-preservation of a human body, the reads-only grep tripwire, and the empty state. Test MUST fail before Task 2.</action>
  <verify>
    <automated>node tests/test-dial-memory-renderer.cjs; test $? -ne 0 && echo "RED OK (expected fail)"</automated>
  </verify>
  <acceptance_criteria>Test exercises idempotence, byte-preservation, reads-only-via-navigation, and empty state; fails because the renderer is absent.</acceptance_criteria>
  <done>Failing renderer test committed; mirrors the Phase 124 Part-9 invariant pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dial-memory renderer on the Phase 124 pattern</name>
  <read_first>
    - lib/core/feynman/timeline-renderer.cjs (copy the pure-function shape + the navigation.cjs-only import + the sentinel-bounded output contract verbatim per Part 7)
    - lib/core/navigation.cjs (getNeighborhood / findRecentChanges / getActiveFocus - the readers to consume)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (the MEMDIAL section content + Part 9 idempotence requirement)
  </read_first>
  <files>lib/core/feynman/dial-memory-renderer.cjs</files>
  <action>Create the renderer as a pure function (db, sectionSlug, opts) -> { markdown_body, summary_stats }, modeled byte-for-byte on the timeline-renderer architecture. Read ONLY via lib/core/navigation.cjs (no fs, no Brain). Render a sentinel-bounded section FROM the graph: the 5 available reaches, the last-selected reach (most recent SELECTED_REACH edge), the current recommended reach (the gate result), and recent research conclusions. Make regeneration idempotent (same graph state -> byte-identical output; sort deterministically, format ISO timestamps consistently). Hand-authored dial state is forbidden - everything is projected from the graph (MEMDIAL-03). The runner concern (atomic write inside sentinels, human-body byte-preservation) follows the Phase 124 timeline-runner precedent - reuse it; this module is the pure renderer half only.</action>
  <verify>
    <automated>node tests/test-dial-memory-renderer.cjs</automated>
  </verify>
  <acceptance_criteria>test-dial-memory-renderer.cjs passes: idempotent, byte-preserving, reads-only-via-navigation, clean empty state; the section is projected FROM the graph.</acceptance_criteria>
  <done>Renderer green; pure function; navigation.cjs-only; idempotent.</done>
</task>

<task type="auto">
  <name>Task 3: Lock MEMDIAL-01 queryability + FILEVAL-01 selection-writes-typed-edge</name>
  <read_first>
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-03-SUMMARY.md (the SELECTED_REACH/PIVOTED edge shapes closeReach writes)
    - lib/core/navigation.cjs (getNeighborhood - the query path that proves the relationship layer is graph-queryable, not a flat log)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (MEMDIAL-01 relationship-layer + FILEVAL-01 at-the-moment-of-selection)
  </read_first>
  <files>tests/test-dial-graph-relationship-layer.cjs</files>
  <action>Create the assert test that (a) MEMDIAL-01: after a few closeReach commits, the 5 reach ids + the SELECTED_REACH + PIVOTED edges are queryable via navigation.getNeighborhood (a graph traversal returns them as typed edges, proving a relationship layer not a flat log); and (b) FILEVAL-01: a Shape-F selection (a closeReach commit) writes the typed edge to the LOCAL room.db at the moment of selection - assert the edge exists in the graph immediately after closeReach returns, not only in an MD file. This test wires both WIRE/assert requirements over the Plan 03 edges.</action>
  <verify>
    <automated>node tests/test-dial-graph-relationship-layer.cjs</automated>
  </verify>
  <acceptance_criteria>Test passes: SELECTED_REACH/PIVOTED edges are returned by a navigation graph query (MEMDIAL-01); a closeReach commit produces a typed room.db edge at selection time (FILEVAL-01).</acceptance_criteria>
  <done>MEMDIAL-01 + FILEVAL-01 locked by a passing graph-query test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> room.db | All reads via navigation.cjs; renderer never opens room.db or reads the filesystem |
| MD section -> human body | Regeneration must write only inside sentinels; human-authored body byte-preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1431.05-01 | Tampering | dial-memory-renderer reads | mitigate | Grep-audit asserts navigation.cjs-only import, zero fs reads (Phase 124 D-03, Part 9) |
| T-1431.05-02 | Tampering | human-authored MD body | mitigate | Sentinel-bounded write + byte-preservation test (Phase 124 D-02 invariant) |
| T-1431.05-03 | Information Disclosure | dial-memory-renderer | mitigate | Zero Brain calls; LOCAL-only projection of LOCAL graph (Part 8) |
| T-1431.05-SC | Tampering | npm installs | accept | No package installs; reuses the Phase 124 renderer pattern |
</threat_model>

<verification>
- node tests/test-dial-memory-renderer.cjs passes
- node tests/test-dial-graph-relationship-layer.cjs passes
- grep -E "fs\.read|require\(.*brain|room\.db" lib/core/feynman/dial-memory-renderer.cjs returns 0 (navigation.cjs-only)
</verification>

<success_criteria>
The dial memory MD section is rendered idempotently FROM the graph with the human body byte-preserved; the SELECTED_REACH/PIVOTED relationship layer is graph-queryable (MEMDIAL-01); a selection writes a typed edge at selection time (FILEVAL-01).
</success_criteria>

<output>
Create `.planning/phases/143.1-dial-tui-capability-selector/143.1-05-SUMMARY.md` when done.
</output>
