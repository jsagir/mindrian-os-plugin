---
phase: 143.1-dial-tui-capability-selector
plan: 05
type: execute
wave: 2
depends_on: ["143.1-03"]
files_modified:
  - lib/core/feynman/dial-memory-renderer.cjs
  - lib/core/feynman/dial-memory-runner.cjs
  - tests/test-dial-memory-renderer.cjs
  - tests/test-dial-memory-runner.cjs
  - tests/test-dial-graph-relationship-layer.cjs
autonomous: true
requirements: [MEMDIAL-01, MEMDIAL-02, MEMDIAL-03]
canon_parts: [Part 4, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "The dial memory renderer is a pure function reading ONLY via navigation.cjs - zero filesystem reads, zero Brain calls"
    - "The rendered section is sentinel-bounded and the human-authored body is byte-preserved across regeneration"
    - "The section shows available reaches, last selected, current recommended, AND recent research conclusions - rendered FROM the graph, not hand-authored"
    - "The dial-memory runner actually writes the sentinel-bounded section to the target MD in production (not test-only); fired on the same hybrid trigger as the Phase 124 timeline (session-start cascade + a manual refresh command)"
    - "Regeneration from the same graph state is idempotent (byte-identical between two runs); the human-authored body survives the runner round-trip unchanged"
    - "The dial relationship layer (5 reach ids + SELECTED_REACH + PIVOTED edges from Plan 03 + pre-existing DRSCH research-conclusion evidence edges from Phase 141) is queryable via navigation.cjs, never a flat log"
  artifacts:
    - path: "lib/core/feynman/dial-memory-renderer.cjs"
      provides: "Pure dial memory renderer on the Phase 124 timeline-renderer pattern"
      min_lines: 70
    - path: "lib/core/feynman/dial-memory-runner.cjs"
      provides: "Atomic-write orchestrator (thin shim over the Phase 124 timeline-runner) that writes the sentinel-bounded dial-memory section to the target MD in production"
      min_lines: 40
    - path: "tests/test-dial-memory-renderer.cjs"
      provides: "Idempotence + byte-preservation + reads-only-via-navigation + research-conclusions-block assertions"
    - path: "tests/test-dial-memory-runner.cjs"
      provides: "Runner writes the sentinel section to a target MD + byte-preserves the human body across regeneration (idempotent in production)"
    - path: "tests/test-dial-graph-relationship-layer.cjs"
      provides: "MEMDIAL-01 queryability assert (SELECTED_REACH + PIVOTED + DRSCH) + FILEVAL-01 selection-writes-typed-edge lock"
  key_links:
    - from: "lib/core/feynman/dial-memory-renderer.cjs"
      to: "lib/core/navigation.cjs"
      via: "all reads via the chokepoint (getNeighborhood / findRecentChanges); pure renderer per Phase 124 D-03"
      pattern: "require\\('\\.\\./navigation\\.cjs'\\)"
    - from: "lib/core/feynman/dial-memory-runner.cjs"
      to: "lib/core/feynman/timeline-runner.cjs"
      via: "reuses the atomic .tmp+rename + sentinel-merge + byte-preservation idiom (Part 7 reuse over the Phase 124 runner)"
      pattern: "timeline-runner|mergeSentinelSection|atomicWrite"
    - from: "tests/test-dial-graph-relationship-layer.cjs"
      to: "the SELECTED_REACH/PIVOTED/DRSCH edges"
      via: "queries the edges written by closeReach (Plan 03) + the pre-existing DRSCH edges (Phase 141) and asserts they are graph-queryable"
      pattern: "SELECTED_REACH|PIVOTED|DRSCH"
---

<objective>
Ship the dial memory layer: a sentinel-bounded MD section rendered FROM the graph (MEMDIAL-02/03), a production runner that actually writes it (MEMDIAL-02 runner half), and lock the dial-as-relationship-layer queryability (MEMDIAL-01 across all three edge families) + the FILEVAL-01 selection-writes-typed-edge invariant.

Purpose: What the navigator reads in the memory file is exactly what the graph holds (Part 9 - SQL is the source of truth, MD is the legible projection). The SELECTED_REACH/PIVOTED edges from Plan 03 plus the pre-existing DRSCH research-conclusion evidence edges (Phase 141) ARE the relationship layer (MEMDIAL-01); this plan asserts all three families are queryable, not a flat log, renders the legible projection idempotently on the proven Phase 124 pattern, AND wires the runner so the section is written in production - not green-in-tests-but-inert.

Output: lib/core/feynman/dial-memory-renderer.cjs (pure renderer) + lib/core/feynman/dial-memory-runner.cjs (atomic-write orchestrator, thin shim over the Phase 124 timeline-runner) + the renderer test + the runner test + the relationship-layer/FILEVAL assert test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-CONTEXT.md
@.planning/phases/143.1-dial-tui-capability-selector/143.1-03-SUMMARY.md

<interfaces>
<!-- The Phase 124 pattern to mirror + the Plan 03 edges to query + the pre-existing DRSCH edges. Do NOT re-explore. -->

From lib/core/feynman/timeline-renderer.cjs (the renderer pattern to model on, Phase 124):
  - Pure function (db, sectionSlug, opts) -> { markdown_body, summary_stats }
  - Reads ONLY via lib/core/navigation.cjs (D-03 closed chokepoint); ZERO fs reads, ZERO Brain calls
  - Empty-state and frozen output format (D-05)

From lib/core/feynman/timeline-runner.cjs (the RUNNER pattern to shim over, Phase 124 - Part 7 reuse):
  - The impure shell: parseFrontmatter / serializeFrontmatter / mergeSentinelSection / bodyOutsideSentinels / atomicWrite (.tmp + rename) / refreshSection(roomDir, sectionSlug, opts) / refreshAll(roomDir, opts)
  - Sentinel-bounded write: the runner writes ONLY inside the sentinels; the human body byte-preserved (D-02 hard invariant)
  - Fired on the hybrid trigger: session-start cascade + the /mos:feynman-timeline-refresh manual command (D-04 + D-12)
  - SENTINEL_START/END are module-private constants; the dial runner defines its OWN dial sentinels and reuses the merge/atomic-write/byte-preservation idiom

From lib/core/navigation.cjs (the chokepoint - import THIS):
  getNeighborhood, findRecentChanges, getActiveFocus  (re-exported readers)
  - getNeighborhood returns rows carrying edgeTypeIn (the typed edge), so a traversal surfaces SELECTED_REACH / PIVOTED / DRSCH-evidence edges as typed edges, proving a relationship layer (not a flat log)

From Plan 03 (already shipped this wave's dependency):
  SELECTED_REACH + PIVOTED edges in ALLOWED_EDGE_TYPES; closeReach writes them via navigation.cjs

From Phase 141 (Complete - PRE-EXISTING, no build needed here):
  DRSCH research-conclusion evidence edges (FILEVAL-02 substrate, navigation.fileEvidenceWithReadback). These edges already live in room.db from Phase 141; this plan only QUERIES + ASSERTS they are reachable via the dial relationship layer - it does not produce them.

MEMDIAL section content (MEMDIAL-02): available reaches (the 5 reach ids), last selected (most-recent SELECTED_REACH), current recommended (the gate result), AND recent research conclusions (surfaced from the DRSCH research-conclusion evidence edges via navigation.getNeighborhood, rendered as an empty/placeholder block when no relevant DRSCH edges exist). Sentinel-bounded; rendered FROM the graph (MEMDIAL-03); idempotent regeneration; no hand-authored dial state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the dial-memory renderer test (idempotence + byte-preservation + reads-only-via-navigation + research-conclusions block)</name>
  <read_first>
    - lib/core/feynman/timeline-renderer.cjs (the pure-renderer contract + the empty-state format to mirror)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (the MEMDIAL section content; the Part 9 graph-as-source-of-truth mandate)
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs (the adversarial Part-9 invariant style to mirror, referenced in the Phase 124 map row)
  </read_first>
  <files>tests/test-dial-memory-renderer.cjs</files>
  <behavior>
    - Renders a section listing the 5 available reaches + last-selected (from the most recent SELECTED_REACH edge) + current-recommended
    - Recent research conclusions: the rendered section includes a "recent research conclusions" sub-block surfaced from the DRSCH research-conclusion evidence edges via navigation.getNeighborhood traversal (filter the neighborhood on edgeTypeIn DRSCH / evidence-conclusion). When no relevant DRSCH edges exist, the block renders as a clean empty/placeholder block ("no recent research conclusions"), NOT omitted and NOT a crash - the section always carries all 4 components (MEMDIAL-02)
    - Idempotence: rendering twice from the same graph state yields byte-identical markdown
    - Byte-preservation: a human-authored body outside the sentinels survives regeneration unchanged
    - Reads-only-via-navigation: a grep-audit asserts the renderer imports only lib/core/navigation.cjs (no fs read, no Brain client, no direct submodule require) - the Phase 124 D-03 invariant
    - Empty state: a room with zero SELECTED_REACH edges renders a clean 'no dial activity yet' section (not a crash, not a raw template)
  </behavior>
  <action>Create the renderer test against a fixture room.db seeded with SELECTED_REACH/PIVOTED edges (use closeReach from Plan 03 or direct fixture edges through navigation.writeEdge) plus at least one fixture DRSCH research-conclusion evidence edge (and a second fixture with zero DRSCH edges to exercise the empty research-conclusions block). Assert all 4 components render (available reaches, last selected, current recommended, recent research conclusions), idempotence (two renders byte-equal), sentinel byte-preservation of a human body, the reads-only grep tripwire, and the empty state. Name the specific query: the research-conclusions block is surfaced by navigation.getNeighborhood filtered on the DRSCH/evidence-conclusion edgeTypeIn. Test MUST fail before Task 2.</action>
  <verify>
    <automated>node tests/test-dial-memory-renderer.cjs; test $? -ne 0 && echo "RED OK (expected fail)"</automated>
  </verify>
  <acceptance_criteria>Test exercises all 4 MEMDIAL-02 components including the recent-research-conclusions block (populated AND empty/placeholder), idempotence, byte-preservation, reads-only-via-navigation, and empty state; fails because the renderer is absent.</acceptance_criteria>
  <done>Failing renderer test committed; mirrors the Phase 124 Part-9 invariant pattern; asserts the research-conclusions block via the named navigation.getNeighborhood DRSCH query.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dial-memory renderer on the Phase 124 pattern (4 components, FROM the graph)</name>
  <read_first>
    - lib/core/feynman/timeline-renderer.cjs (copy the pure-function shape + the navigation.cjs-only import + the output contract verbatim per Part 7)
    - lib/core/navigation.cjs (getNeighborhood / findRecentChanges / getActiveFocus - the readers to consume; getNeighborhood carries edgeTypeIn for the DRSCH filter)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (the MEMDIAL section content + Part 9 idempotence requirement)
  </read_first>
  <files>lib/core/feynman/dial-memory-renderer.cjs</files>
  <action>Create the renderer as a pure function (db, sectionSlug, opts) -> { markdown_body, summary_stats }, modeled on the timeline-renderer architecture. Read ONLY via lib/core/navigation.cjs (no fs, no Brain). Render a section FROM the graph carrying all 4 MEMDIAL-02 components: (1) the 5 available reaches, (2) the last-selected reach (most recent SELECTED_REACH edge), (3) the current recommended reach (the gate result), and (4) recent research conclusions - surfaced from the DRSCH research-conclusion evidence edges via navigation.getNeighborhood filtered on the DRSCH/evidence-conclusion edgeTypeIn, rendering a clean empty/placeholder block when none exist. Make regeneration idempotent (same graph state -> byte-identical output; sort deterministically, format ISO timestamps consistently). Hand-authored dial state is forbidden - everything is projected from the graph (MEMDIAL-03). This module is the pure renderer half only; the atomic-write/byte-preservation runner concern is Task 3.</action>
  <verify>
    <automated>node tests/test-dial-memory-renderer.cjs</automated>
  </verify>
  <acceptance_criteria>test-dial-memory-renderer.cjs passes: all 4 components render (including the research-conclusions block, populated and empty); idempotent, byte-preserving, reads-only-via-navigation, clean empty state; the section is projected FROM the graph.</acceptance_criteria>
  <done>Renderer green; pure function; navigation.cjs-only; idempotent; all 4 MEMDIAL-02 components present.</done>
</task>

<task type="auto">
  <name>Task 3: Build + wire the dial-memory runner (production write + byte-preservation) as a thin shim over the Phase 124 timeline-runner</name>
  <read_first>
    - lib/core/feynman/timeline-runner.cjs (the atomic-write orchestrator to shim over: mergeSentinelSection / bodyOutsideSentinels / atomicWrite (.tmp+rename) / refreshSection / refreshAll - Part 7 reuse)
    - commands/feynman-timeline-refresh.md (the manual-refresh command surface to mirror for the dial section)
    - scripts/session-start (or the session-start cascade entry that fires the Phase 124 timeline refresh - the hybrid trigger to fire the dial runner on)
  </read_first>
  <files>lib/core/feynman/dial-memory-runner.cjs, tests/test-dial-memory-runner.cjs</files>
  <action>Build the dial-memory runner as the atomic-write orchestrator that writes the sentinel-bounded dial-memory section to the target MD in PRODUCTION - this closes the MEMDIAL-02 runner half so the requirement is not green-in-tests-but-inert. Follow the SHIPPED Phase 124 timeline-runner pattern and keep it a THIN SHIM where possible (Part 7 reuse): define the dial sentinels, then reuse the Phase 124 mergeSentinelSection / bodyOutsideSentinels / atomicWrite (.tmp + rename) idiom to write ONLY inside the sentinels with the human-authored body byte-preserved (D-02 hard invariant). Call dial-memory-renderer (Task 2) for the section body. Expose refreshSection(roomDir, sectionSlug, opts) + refreshAll(roomDir, opts) parity with the timeline-runner. Fire it on the SAME hybrid trigger as the Phase 124 timeline: wire it into the session-start cascade alongside the timeline refresh, AND add a manual refresh command surface mirroring commands/feynman-timeline-refresh.md (a /mos:dial-memory-refresh command, or extend the existing refresh command to also refresh the dial section - whichever is the smaller reuse delta). Write tests/test-dial-memory-runner.cjs that proves the runner WRITES the sentinel-bounded dial-memory section to a target MD on disk AND byte-preserves a human-authored body across a second regeneration (idempotent in production: two runner passes leave the human body and the rendered section byte-identical).</action>
  <verify>
    <automated>node tests/test-dial-memory-runner.cjs</automated>
  </verify>
  <acceptance_criteria>The runner actually writes the sentinel-bounded dial-memory section to the target MD (proven by the runner test reading the file back from disk), byte-preserves the human-authored body across regeneration, and is idempotent (two runner passes byte-identical); it is fired on the Phase 124 hybrid trigger (session-start cascade + a manual refresh command). MEMDIAL-02 is now live in production, not test-only.</acceptance_criteria>
  <done>Dial runner green; writes the section in production; human body byte-preserved; idempotent; wired to the session-start cascade + manual refresh command; a thin shim over the Phase 124 timeline-runner (Part 7).</done>
</task>

<task type="auto">
  <name>Task 4: Lock MEMDIAL-01 queryability (SELECTED_REACH + PIVOTED + DRSCH) + FILEVAL-01 selection-writes-typed-edge</name>
  <read_first>
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-03-SUMMARY.md (the SELECTED_REACH/PIVOTED edge shapes closeReach writes)
    - .planning/phases/141-local-retrieval-spine-and-capability-dial/141-04-SUMMARY.md (the DRSCH / FILEVAL-02 evidence-edge substrate already shipped; navigation.fileEvidenceWithReadback)
    - lib/core/navigation.cjs (getNeighborhood - the query path that proves the relationship layer is graph-queryable, not a flat log; edgeTypeIn carries the typed edge)
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-UI-SPEC.md (MEMDIAL-01 relationship-layer + FILEVAL-01 at-the-moment-of-selection)
  </read_first>
  <files>tests/test-dial-graph-relationship-layer.cjs</files>
  <action>Create the assert test that (a) MEMDIAL-01: after a few closeReach commits, the 5 reach ids + the SELECTED_REACH + PIVOTED edges are queryable via navigation.getNeighborhood (a graph traversal returns them as typed edges via edgeTypeIn, proving a relationship layer not a flat log); AND additionally assert the DRSCH research-conclusion evidence edges are reachable via the same navigation.getNeighborhood traversal as part of the dial relationship layer. NOTE: the DRSCH edges are PRE-EXISTING (Phase 141 Complete, FILEVAL-02 substrate) - this is an ASSERT-ONLY addition (seed a fixture DRSCH evidence edge through the shipped navigation.fileEvidenceWithReadback substrate, then query+assert it is reachable; no DRSCH producer is built here). And (b) FILEVAL-01: a Shape-F selection (a closeReach commit) writes the typed edge to the LOCAL room.db at the moment of selection - assert the edge exists in the graph immediately after closeReach returns, not only in an MD file. REQUIREMENTS.md MEMDIAL-01 names exactly three edge families (SELECTED_REACH, PIVOTED, DRSCH); this test now covers all three.</action>
  <verify>
    <automated>node tests/test-dial-graph-relationship-layer.cjs</automated>
  </verify>
  <acceptance_criteria>Test passes: SELECTED_REACH, PIVOTED, AND DRSCH research-conclusion evidence edges are all returned by a navigation.getNeighborhood graph query (MEMDIAL-01, all three families REQUIREMENTS.md names); a closeReach commit produces a typed room.db edge at selection time (FILEVAL-01). DRSCH coverage is assert-only over the pre-existing Phase 141 substrate.</acceptance_criteria>
  <done>MEMDIAL-01 (all 3 edge families) + FILEVAL-01 locked by a passing graph-query test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> room.db | All reads via navigation.cjs; renderer never opens room.db or reads the filesystem |
| runner -> target MD | Runner writes only inside the dial sentinels; human-authored body byte-preserved across regeneration |
| MD section -> human body | Regeneration must write only inside sentinels; human-authored body byte-preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1431.05-01 | Tampering | dial-memory-renderer reads | mitigate | Grep-audit asserts navigation.cjs-only import, zero fs reads (Phase 124 D-03, Part 9) |
| T-1431.05-02 | Tampering | human-authored MD body | mitigate | Sentinel-bounded write + byte-preservation test at BOTH the renderer (Task 1/2) and the runner (Task 3) layers (Phase 124 D-02 invariant) |
| T-1431.05-03 | Information Disclosure | dial-memory-renderer | mitigate | Zero Brain calls; LOCAL-only projection of LOCAL graph (Part 8) |
| T-1431.05-04 | Tampering | dial-memory-runner atomic write | mitigate | Reuse the Phase 124 .tmp+rename atomicWrite idiom; runner test reads the target MD back from disk to prove the write landed and the human body survived (Part 7 reuse) |
| T-1431.05-SC | Tampering | npm installs | accept | No package installs; reuses the Phase 124 renderer + runner pattern |
</threat_model>

<verification>
- node tests/test-dial-memory-renderer.cjs passes (4 components incl. research-conclusions block)
- node tests/test-dial-memory-runner.cjs passes (production write + byte-preservation + idempotence)
- node tests/test-dial-graph-relationship-layer.cjs passes (SELECTED_REACH + PIVOTED + DRSCH queryable; FILEVAL-01)
- grep -E "fs\.read|require\(.*brain|room\.db" lib/core/feynman/dial-memory-renderer.cjs returns 0 (navigation.cjs-only)
</verification>

<success_criteria>
The dial memory MD section is rendered idempotently FROM the graph with all 4 MEMDIAL-02 components (available reaches, last selected, current recommended, recent research conclusions), WRITTEN in production by the dial runner with the human body byte-preserved on the Phase 124 hybrid trigger; the SELECTED_REACH/PIVOTED/DRSCH relationship layer is graph-queryable (MEMDIAL-01, all three edge families REQUIREMENTS.md names); a selection writes a typed edge at selection time (FILEVAL-01).
</success_criteria>

<output>
Create `.planning/phases/143.1-dial-tui-capability-selector/143.1-05-SUMMARY.md` when done.
</output>
