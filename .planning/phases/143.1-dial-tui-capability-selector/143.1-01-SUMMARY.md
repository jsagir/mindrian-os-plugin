---
phase: 143.1-dial-tui-capability-selector
plan: 01
subsystem: ui
tags: [dial-tui, reach-list, f-selector-ranker, recommended-gate, canon-part-3, canon-part-8, canon-part-9]

# Dependency graph
requires:
  - phase: 125-f-selector-ranker
    provides: rankForSelector + the D4 blend + MAX_K=3 (reused, not modified)
  - phase: 141-local-retrieval-spine
    provides: the 5 canonical reach ids (context_block/contradiction/cross_room/brain_consult/deep_research) committed as doctrine
provides:
  - "lib/hmi/dial-reach-orchestrator.cjs: the surface-agnostic ReachList core (DIALTUI-01)"
  - "DIAL_REACH_K=5 constant, SEPARATE from the untouched ranker MAX_K=3 (the two-K separation, V9)"
  - "the frozen 0.70/0.15 Recommended gate applied per-reach (DIALTUI-02)"
  - "per-reach provenance {score, source, brain_component, local_component, recommended}"
affects: [143.1-02, 143.1-03, 143.1-04, dial-label-composer, cli-presenter, desktop-presenter, cowork-presenter]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "resolve/format split (nav-dial.cjs pattern): orchestrator returns structured data, formatting lives in the presenter"
    - "two-K separation: MAX_K=3 chooser clamp vs DIAL_REACH_K=5 read-only preview"
    - "pre-baked Brain prior via roomState.reachScores: zero live Brain calls at render time (Part 8)"
    - "frozen gate reused verbatim: 0.70 floor + 0.15 MARGIN_THRESHOLD, never re-tuned"

key-files:
  created:
    - lib/hmi/dial-reach-orchestrator.cjs
    - tests/test-dial-reach-orchestrator.cjs
  modified: []

key-decisions:
  - "The per-reach score is the caller-supplied pre-baked LOCAL prior (roomState.reachScores); the orchestrator makes ZERO live Brain calls and reads no room.db (Part 8 + Part 9)"
  - "rankForSelector is CALLED for the D4 blend signal (Canon Part 7 reuse), grounding the registry-only floor; the D4 formula is never re-implemented here"
  - "Registry-only reaches (cross_room) default brain_confidence=0.5 so a cold room yields zero markers by design (UI-SPEC Section 6.4)"
  - "The 0.70 floor and 0.15 margin are exported as RECOMMEND_FLOOR + MARGIN_THRESHOLD constants and applied independently; the floor is NEVER lowered to force a second marker (AP3)"

patterns-established:
  - "ReachList contract: {reaches[<=5], tier_mode, offered_count<=3, total_count} is the shared source of truth every surface consumes"
  - "Frozen-gate enforcement is a per-reach post-sort pass gated on tier_mode==mode_a"

requirements-completed: [DIALTUI-01, DIALTUI-02]

# Metrics
duration: 18min
completed: 2026-06-06
---

# Phase 143.1 Plan 01: Dial-Reach Orchestrator Summary

**The surface-agnostic ReachList core: ranks the 5 canonical reaches with the DIAL_REACH_K=5 preview (distinct from the untouched MAX_K=3 chooser), reuses the shipped f-selector-ranker D4 blend, attaches per-reach provenance, and applies the frozen 0.70/0.15 Recommended gate verbatim with zero live Brain calls at render time.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-06-06T13:25:00Z
- **Completed:** 2026-06-06T13:43:00Z
- **Tasks:** 2 (TDD: RED then GREEN)
- **Files modified:** 2 created

## Accomplishments
- `buildReachList(roomState)` returns a structured ReachList of exactly 5 canonical reaches ranked desc by score, no ANSI, no host formatting (the shared core every surface consumes).
- `DIAL_REACH_K=5` declared as a module constant SEPARATE from the ranker `MAX_K=3`; a test asserts both hold simultaneously (the load-bearing two-K separation, V9).
- The frozen Recommended gate reused verbatim: reach#1 marked IFF `tier_mode==mode_a AND score>=0.70`; reach#2 ALSO only when `score>=0.70 AND (s1-s2)<0.15`; zero markers in Mode B / Tier 0.
- Per-reach provenance `{score, source, brain_component (score*0.40), local_component (residual), recommended}` on every reach.
- Pure / synchronous / LOCAL-only: a source-grep tripwire asserts zero `fetch`/`http`/brain-client-require/`await` tokens (Part 8).

## Task Commits

Each task was committed atomically (TDD):

1. **Task 1: failing render-state test (RED)** - `427ce6c4` (test)
2. **Task 2: build orchestrator + DIAL_REACH_K + frozen gate (GREEN)** - `cc834ac6` (feat)

**Plan metadata:** see final docs commit.

## Files Created/Modified
- `lib/hmi/dial-reach-orchestrator.cjs` - the dial-reach orchestrator: `buildReachList`, `DIAL_REACH_K=5`, `MARGIN_THRESHOLD=0.15`, `RECOMMEND_FLOOR=0.70`, `REACH_IDS`. Reuses `rankForSelector` for the D4 blend; pure/sync/LOCAL-only.
- `tests/test-dial-reach-orchestrator.cjs` - 12 render-state assertions mapping 1:1 to UI-SPEC state matrix S1/S2/S2-negative/S3/S4, the provenance contract, the two-K separation, and the Part-8 purity tripwire.

## Decisions Made
- The dial score is the caller-supplied pre-baked LOCAL prior (`roomState.reachScores`), keeping the orchestrator zero-Brain at render time. The shipped `rankForSelector` is still invoked to exercise the D4 blend (Canon Part 7 reuse) and ground the registry-only floor.
- `cross_room` is classified registry-only and defaults to a 0.5 brain_confidence prior so it can never solo-cross the 0.70 gate (cold-room zero-marker state by design).

## Deviations from Plan

None - plan executed exactly as written. Both TDD tasks confirmed RED (module absent, exit 1) before GREEN (12/12 pass). No Rule 1-4 fixes required.

## Issues Encountered
None. The ranker `MAX_K=3` and its 34/34 test suite were verified untouched after the orchestrator landed.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The ReachList contract is the load-bearing artifact every downstream Plan (02 label composer, 03 F.1 renderer wire, 04 CLI presenter) consumes.
- Two-K separation holds; the frozen gate is centralized and exported for reuse.
- The Desktop/Cowork presenters (DIALTUI-10) and the label composer (DIALTUI-04) read this same `buildReachList` output unchanged.

## Self-Check: PASSED

- FOUND: lib/hmi/dial-reach-orchestrator.cjs
- FOUND: tests/test-dial-reach-orchestrator.cjs
- FOUND: .planning/phases/143.1-dial-tui-capability-selector/143.1-01-SUMMARY.md
- FOUND commit: 427ce6c4 (RED test)
- FOUND commit: cc834ac6 (GREEN orchestrator)

---
*Phase: 143.1-dial-tui-capability-selector*
*Completed: 2026-06-06*
