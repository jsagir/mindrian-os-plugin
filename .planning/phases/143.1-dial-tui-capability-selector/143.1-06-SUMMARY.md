---
phase: 143.1-dial-tui-capability-selector
plan: 06
subsystem: ui
tags: [dial-tui, e2e-gate, aggregator, standing-fences, canon-part-3, canon-part-8, canon-part-9, dialtui-11]

# Dependency graph
requires:
  - phase: 143.1-01
    provides: buildReachList ReachList core + DIAL_REACH_K=5 + frozen 0.70/0.15 gate (S1-S5 + V9/V10)
  - phase: 143.1-02
    provides: composeLabel Feynman-JTBD labels + degradation ladder + Part-8 {framework} egress seam (V11)
  - phase: 143.1-03
    provides: closeReach 4-outcome transaction + SELECTED_REACH/PIVOTED edges + f_selector_sync_confirmed/pivot events (V12 write-locality)
  - phase: 143.1-04
    provides: renderDial CLI presenter (the S1-S5 render contract + cold-room looks-intentional)
  - phase: 143.1-05
    provides: renderDialMemory dial-memory renderer (the pipeline tail projection)
provides:
  - "tests/run-all-1431.sh: the Phase 143.1 scoped aggregator mirroring run-all-143.sh; composes all 7 suites"
  - "tests/test-dial-end-to-end-states.cjs: the full-pipeline S1-S5 + closeReach round-trip + V9/V10/V11/V12 standing-fence gate"
  - "143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md: DIALTUI-11 done-by-reference (UI-SPEC approved; gsd-ui-phase NOT re-run)"
affects: [phase-143.1-closure, phase-144-navigation-engine, phase-146-loop-fires-gate]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "one-gate aggregator: the four standing fences (two-K, 0.70 floor, Part-8 seam, write-locality) re-asserted in a single e2e suite the aggregator owns"
    - "standalone assert-runner with node:sqlite SKIP guard (exit 77) mirroring test-dial-graph-relationship-layer.cjs"
    - "full-pipeline drive: buildReachList -> composeLabel -> renderDial -> closeReach -> renderDialMemory in one test"
    - "complete-by-reference receipt: an approved UI-SPEC satisfies a gsd-ui-phase requirement without re-running gsd-ui-phase (AP8)"

key-files:
  created:
    - tests/test-dial-end-to-end-states.cjs
    - tests/run-all-1431.sh
    - .planning/phases/143.1-dial-tui-capability-selector/143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md
  modified: []

key-decisions:
  - "The e2e suite drives the REAL pipeline modules (no mocks): buildReachList feeds renderDial, closeReach writes to a real openRoomDb fixture, renderDialMemory projects the committed SELECTED_REACH back FROM the graph -- proving the pipeline end to end, not the parts in isolation"
  - "Render states are produced via the pre-baked roomState.reachScores prior (exactly the production contract), so S1/S2/S3/S4/S5 are deterministic and the gate is stable across runs"
  - "V10 uses a crafted both-above-0.70-but-margin>0.15 case to prove the floor is never lowered to force a second marker AND the margin gate independently suppresses reach #2 (exactly 1 marker)"
  - "V11 re-asserts the Part-8 seam through composeLabel: an email-bearing {framework} trips FORBIDDEN_PATTERNS and default-denies (degrades); a generic handle (SWOT) clears; the 3 render-only families never move the audit counter"
  - "V12 greps the 5 dial pipeline modules for any raw-driver/room-db require and asserts the one writer (dial-close-reach) routes through navigation.cjs"
  - "DIALTUI-11 is recorded complete-by-reference; gsd-ui-phase is NOT re-run (AP8); V8 deferred past 143.1 per resolved OQ1; V7 covered by Plan 04 + Plan 06 tests"

patterns-established:
  - "aggregator-owned standing fences: the phase's load-bearing invariants (two-K / frozen gate / egress seam / write-locality) live in one e2e suite so the aggregator is the single merge gate that enforces them"
  - "the FNV-1a deterministic label pick means the e2e asserts on the resolved {topic} substring, not a fixed template string (both family templates fill the slot)"

requirements-completed: [DIALTUI-11]

# Metrics
duration: 22min
completed: 2026-06-06
---

# Phase 143.1 Plan 06: Dial End-to-End Gate + Aggregator Summary

**Closes Phase 143.1: a single end-to-end gate drives the full dial pipeline (orchestrator -> labels -> presenter -> closeReach -> memory) through all five UI-SPEC render states plus a sync/pivot closeReach round-trip, and re-asserts the four standing fences (V9 two-K, V10 frozen 0.70/0.15, V11 Part-8 {framework} seam, V12 navigation.cjs write-locality) in one place; the run-all-1431.sh aggregator mirrors run-all-143.sh and composes all 7 Phase 143.1 suites green; DIALTUI-11 is recorded complete-by-reference without re-running gsd-ui-phase.**

## Performance

- **Duration:** ~22 min
- **Tasks:** 3 (Task 1 TDD-style e2e, Task 2 aggregator, Task 3 record)
- **Files:** 3 created, 0 modified

## Accomplishments

- **The end-to-end gate (Task 1).** `tests/test-dial-end-to-end-states.cjs` (11 checks) drives the REAL five pipeline modules end to end:
  - **S1** Mode A clear leader -> exactly 1 filled `▶` marker; WHAT-THEY-GET labels, no mechanism nouns, no literal "(Recommended)".
  - **S2** Mode A near-tie (margin < 0.15) -> exactly 2 filled markers; row 3 empty.
  - **S3** Mode B -> 0 markers + offline framing line (intentional, V3/V7).
  - **S4** Tier 0 cold-room -> 0 markers + `--` confidence + cold-room framing; distinguishable-as-intentional from S1 (V7).
  - **S5** partial-slot -> the contradiction label degrades to the generic JTBD line (no raw `{topic}`); marker logic unchanged.
  - **closeReach round-trip:** render S1 + commit the detent -> `SELECTED_REACH` edge + `f_selector_sync_confirmed`, then projected into the dial-memory surface via `renderDialMemory`; render S1 + pivot off -> `PIVOTED` edge (ENUM-only props) + `f_selector_pivot` (+ `SELECTED_REACH`).
- **The four standing fences re-asserted in one place:** V9 (`ranker.MAX_K===3` AND `DIAL_REACH_K===5`, distinct), V10 (0.70/0.15 frozen; a margin>0.15 case yields exactly 1 marker), V11 (the `brain_consult`/`deep_research` `{framework}` egress audit default-denies raw user content; the other 3 families are render-only), V12 (a grep sweep across the 5 dial modules confirms no direct `room.db` open; the one writer routes through `navigation.cjs`).
- **The aggregator (Task 2).** `tests/run-all-1431.sh` mirrors `run-all-143.sh` byte-for-structure (`set -uo pipefail`, `SHELL_SUITES` + `CJS_SUITES`, per-suite PASS/FAIL with plan-ownership header, run-to-completion, non-zero exit on any failure). It composes all 7 Phase 143.1 suites: `test-dial-reach-orchestrator` (01), `test-dial-label-bank-drift` (02), `test-dial-close-reach` (03), `test-dial-render-states` (04), `test-dial-memory-renderer` + `test-dial-graph-relationship-layer` (05), `test-dial-end-to-end-states` (06).
- **DIALTUI-11 (Task 3).** `143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md` records the requirement satisfied by the approved `143.1-UI-SPEC.md` (CLI scope, checker-signed 2026-06-06); gsd-ui-phase is NOT re-run (AP8); V8 deferred past 143.1 per resolved OQ1; V7 covered by the Plan 04 + Plan 06 tests; cross-references the UI-SPEC and the aggregator gate.

## Task Commits

1. **Task 1: e2e state-matrix + closeReach round-trip gate** - `cbd3780e` (test)
2. **Task 2: run-all-1431.sh aggregator** - `d5e0058b` (test)
3. **Task 3: DIALTUI-11 complete-by-reference record** - `49ded7f8` (docs)

## Verification

- `node tests/test-dial-end-to-end-states.cjs` -> PASS (11 checks; S1-S5 + sync/pivot round-trip + V9/V10/V11/V12).
- `bash tests/run-all-1431.sh` -> **7/7 PASSED, exit 0** (Total 7, Passed 7, Failed 0).
- `grep -cP "\x{2014}"` on all three new files -> 0 (zero em-dashes).
- `test -f 143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md && grep -q "complete-by-reference"` -> OK.
- `docs/empathy-audit/auto-explore-117-rescore.md` left untouched per the objective fence (not in any commit; git status clean).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] e2e assertion pinned a non-deterministic template string**
- **Found during:** Task 1 (first e2e run).
- **Issue:** The S1 check asserted the literal `'Pull up what we decided about pricing'` for the context_block leader row. The composer carries two FNV-1a templates per family and deterministically picks the second (`'Bring back what we worked out on pricing.'`) for the e2e slot context, so the literal-string assertion failed.
- **Fix:** Re-pinned the assertion to the composer's resolved label substring (`composeLabel(leader.reach_id, FULL_SLOTS).label`) and to the `{topic}` resolution invariant (`'pricing'` present, no raw `{` placeholder) -- the correct contract, since the WHAT-THEY-GET label is what must render, not a specific template family member.
- **Files modified:** tests/test-dial-end-to-end-states.cjs
- **Commit:** cbd3780e

The aggregator (Task 2) and the complete-by-reference record (Task 3) executed exactly as written; no other deviations.

## Known Stubs

None. The e2e suite drives the real pipeline modules against a real `openRoomDb` fixture; the closeReach round-trip writes real typed edges and the dial-memory renderer projects them back from the graph. No mocks, no placeholder data sources.

## Threat Surface

No new threat surface beyond the plan's `<threat_model>`. All three `mitigate` STRIDE dispositions are satisfied by tests in the one e2e gate:
- T-1431.06-01 (Tampering, the two-K / 0.70-floor fences): V9 + V10 re-asserted end-to-end.
- T-1431.06-02 (Info Disclosure, the Part-8 {framework} seam): V11 re-asserted via the composer (email-bearing framework default-denies).
- T-1431.06-03 (Tampering, write-locality): V12 grep sweep across the 5 dial modules + the navigation.cjs-only writer assertion.

## Self-Check: PASSED

- FOUND: tests/test-dial-end-to-end-states.cjs
- FOUND: tests/run-all-1431.sh
- FOUND: .planning/phases/143.1-dial-tui-capability-selector/143.1-DIALTUI-11-COMPLETE-BY-REFERENCE.md
- FOUND commit: cbd3780e (e2e gate)
- FOUND commit: d5e0058b (aggregator)
- FOUND commit: 49ded7f8 (complete-by-reference)

---
*Phase: 143.1-dial-tui-capability-selector*
*Completed: 2026-06-06*
