---
phase: 186-corpus-stats-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/corpus-stats-source.json
  - scripts/build-corpus-stats.cjs
  - docs/CORPUS-STATS.generated.md
  - data/corpus-stats.generated.json
autonomous: true
requirements: [CORPUS-01]
canon_parts: [6, 8]

must_haves:
  truths:
    - "A reader of docs/CORPUS-STATS.generated.md sees the three asserted magnitudes 27,904 nodes / 177 frameworks / 12,485 Pinecone vectors (1024-dim) and no directional sub-count asserted as fact (D1 + D2)."
    - "The artifact header marks it GENERATED / do-not-hand-edit, mirroring how build-connector-registry.cjs stamps its output."
    - "The generator reads its magnitudes from a committed local file and makes ZERO Brain/network calls on the hot path (D5 + T-186-01)."
  artifacts:
    - path: "data/corpus-stats-source.json"
      provides: "The committed single input: the three D1 magnitudes + dim + read-date + directional-only note"
      contains: "27904"
    - path: "scripts/build-corpus-stats.cjs"
      provides: "The local generator (default-write mode this plan; --check tripwire added in Plan 02)"
      min_lines: 80
    - path: "docs/CORPUS-STATS.generated.md"
      provides: "The rendered single source of truth for corpus magnitudes, generated-stamped"
      contains: "27,904"
    - path: "data/corpus-stats.generated.json"
      provides: "Machine-readable sibling of the artifact"
      contains: "12485"
  key_links:
    - from: "scripts/build-corpus-stats.cjs"
      to: "data/corpus-stats-source.json"
      via: "fs.readFileSync of the committed source (never a Brain query)"
      pattern: "corpus-stats-source"
    - from: "scripts/build-corpus-stats.cjs"
      to: "docs/CORPUS-STATS.generated.md"
      via: "deterministic render + fs.writeFileSync"
      pattern: "CORPUS-STATS.generated"
---

<objective>
Build the generated corpus-stats artifact and its local generator: the single source of truth for the three asserted corpus magnitudes (D1). The artifact is rendered, generated-stamped, and never hand-edited - exactly how data/connector-registry.json is rendered from frontmatter by build-connector-registry.cjs.

This plan delivers CORPUS-01: the generated stats artifact (docs/CORPUS-STATS.generated.md + a .json sibling) carrying the three live magnitudes, fed from a committed local source so the generator reads NO live Brain on the hot path (the T-186-01 Part 8 mitigation).

Purpose: stop corpus literals from drifting by giving every number one rendered home. Plan 02 adds the --check tripwire and repoints the stale live-surface literals.
Output: data/corpus-stats-source.json, scripts/build-corpus-stats.cjs, docs/CORPUS-STATS.generated.md, data/corpus-stats.generated.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-corpus-stats-hygiene/186-CONTEXT.md

# The generator + --check idiom to MIRROR (Canon Part 7 reuse). Read the
# default-write path, the deterministic serializer (JSON.stringify(...,2) +
# trailing newline), the three-branch main(), and the Part 8 posture (no
# top-level brain-client require; the ONLY Brain touch is gated behind an
# explicit --refresh-names branch, which THIS generator does not need).
@scripts/build-connector-registry.cjs
</context>

<facts>
The three asserted magnitudes (D1, the ONLY asserted corpus magnitudes):
- neo4j_nodes: 27,904
- frameworks: 177
- pinecone_vectors: 12,485 (Pinecone pws-brain, 1024-dim)

NOT asserted as fact, NEVER rendered as a fact claim (D2): directional sub-counts
(teaching-graph core, MethodologyChunk substrate, per-namespace vector splits,
relationship counts such as 19,987). If the artifact mentions any of these at all,
it labels them "directional only, not a source of truth" - it does not state them
as magnitudes. The recommended lean choice is to OMIT them entirely.

Read date for this magnitude set: 2026-06-27.
</facts>

<tasks>

<task type="auto">
  <name>Task 1: Committed magnitude source + the local generator (default-write)</name>
  <files>data/corpus-stats-source.json, scripts/build-corpus-stats.cjs</files>
  <read_first>
    scripts/build-connector-registry.cjs (lines 46-105 for the REPO_ROOT / path
    constants / GENERATED_NOTE idiom; lines 405-414 serializeRegistry for the
    deterministic JSON.stringify(...,2) + trailing-newline serializer; lines
    786-897 main() for the default-write-vs-flag branch and the fs.mkdirSync +
    fs.writeFileSync write). Note the Part 8 header (lines 25-32): there is NO
    top-level require of brain-client; the only Brain touch is gated inside an
    explicit branch. This generator needs NO Brain touch at all.
  </read_first>
  <action>
    Create data/corpus-stats-source.json as the committed single input. Top-level
    keys: a corpus_stats_note string stating this is the hand-edited source of
    truth for the three D1 magnitudes (edited only when a new live Brain read is
    recorded; the generator renders the artifact from it; NO live Brain query on
    the hot path per Canon Part 8 / T-186-01); a read_date string "2026-06-27"; a
    magnitudes object with exactly neo4j_nodes 27904, frameworks 177,
    pinecone_vectors 12485; a pinecone_dim integer 1024; and a directional_only_note
    string recording that sub-counts (teaching-graph core, MethodologyChunk
    substrate, per-namespace splits, relationship counts) are DIRECTIONAL ONLY
    (D2), zero source-of-truth, admin-gated, NEVER asserted as fact and NEVER
    enforced by the tripwire.

    Create scripts/build-corpus-stats.cjs MIRRORING build-connector-registry.cjs:
    require only node:fs and node:path (NO brain-client, NO fetch, NO http/https -
    this is the T-186-01 mitigation made structural); resolve REPO_ROOT via
    path.resolve(__dirname, '..'); define SOURCE_PATH (data/corpus-stats-source.json),
    MD_PATH (docs/CORPUS-STATS.generated.md), JSON_PATH (data/corpus-stats.generated.json),
    and a GENERATED_NOTE constant reading "GENERATED by scripts/build-corpus-stats.cjs - do not hand-edit".
    Provide loadSource() that reads + JSON.parses the source. Provide renderMarkdown(src)
    that returns a deterministic markdown string whose first body line after the H1
    is the GENERATED_NOTE stamp (an HTML comment plus a visible "do not hand-edit"
    line), then a section asserting EXACTLY the three magnitudes (27,904 nodes /
    177 frameworks / 12,485 Pinecone vectors, 1024-dim), the read_date, and a
    closing "Directional sub-counts are NOT asserted here (Canon D2)" note. Do NOT
    render any directional sub-count as a magnitude. Provide serializeArtifactJson(src)
    that returns JSON.stringify of a clean object {generated_note, read_date,
    magnitudes:{neo4j_nodes, frameworks, pinecone_vectors}, pinecone_dim} with
    (null, 2) + a trailing newline (the connector serializer discipline, so the
    Plan 02 --check byte-compare is deterministic). Provide a three-branch main():
    default run writes BOTH MD_PATH and JSON_PATH (fs.mkdirSync data dir recursive,
    fs.writeFileSync) and logs what it wrote; reserve a --check branch as a stub
    that Plan 02 fills (for now it may simply re-run the render and exit 0, or be
    omitted - Plan 02 owns it). Export the pure functions (loadSource,
    renderMarkdown, serializeArtifactJson) under module.exports for the Plan 02
    tests, guarded by require.main === module. Use hyphens only, never em-dashes.
  </action>
  <verify>
    <automated>node -e "const g=require('./scripts/build-corpus-stats.cjs'); const s=g.loadSource(); if(s.magnitudes.neo4j_nodes!==27904||s.magnitudes.frameworks!==177||s.magnitudes.pinecone_vectors!==12485){process.exit(1)}; if(g.serializeArtifactJson(s).slice(-1)!=='\n'){process.exit(1)}; console.log('source+serializer OK')"</automated>
  </verify>
  <done>data/corpus-stats-source.json carries exactly the three D1 magnitudes; scripts/build-corpus-stats.cjs loads it, renders deterministically, and requires no Brain/network module.</done>
</task>

<task type="auto">
  <name>Task 2: Emit the artifact and prove it is generated-stamped, magnitude-correct, and Brain-free</name>
  <files>docs/CORPUS-STATS.generated.md, data/corpus-stats.generated.json</files>
  <read_first>
    scripts/build-connector-registry.cjs lines 880-896 (the default-write logging
    idiom) so the run output reads the same way.
  </read_first>
  <action>
    Run the generator in default mode to emit docs/CORPUS-STATS.generated.md and
    data/corpus-stats.generated.json. Confirm by inspection that the markdown
    carries the GENERATED stamp at the top, asserts the three magnitudes with
    grouped-thousands formatting (27,904 / 177 / 12,485), and asserts NO directional
    sub-count as fact. These two files are committed as part of this plan (they are
    generated output, but they are tracked, exactly like data/connector-registry.json).
  </action>
  <verify>
    <automated>node scripts/build-corpus-stats.cjs && grep -q "do not hand-edit" docs/CORPUS-STATS.generated.md && grep -q "27,904" docs/CORPUS-STATS.generated.md && grep -q "177" docs/CORPUS-STATS.generated.md && grep -q "12,485" docs/CORPUS-STATS.generated.md && grep -q "12485" data/corpus-stats.generated.json && echo ARTIFACT_OK</automated>
  </verify>
  <done>docs/CORPUS-STATS.generated.md exists, is generated-stamped, and carries the three magnitudes; data/corpus-stats.generated.json carries the machine-readable three; a grep proves the generator source has no brain-client/fetch/http require (run: grep -nE "brain-client|require\\('node:https'\\)|require\\(\"node:https\"\\)|fetch\\(" scripts/build-corpus-stats.cjs returns nothing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator -> Brain MCP | The ONLY place a corpus generator could egress / depend on the remote Brain. Canon Part 8: the Brain is a methodology repository, never a hot-path dependency of a local build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-01 | Tampering / Denial of Service | scripts/build-corpus-stats.cjs | mitigate | The magnitudes come from a COMMITTED local file (data/corpus-stats-source.json), NEVER a live Brain query on the hot path (D5). The generator requires no brain-client and no network module; a build/commit never hangs on Render cold-start and never egresses. Proven by the no-Brain grep in Task 2 done-criteria. |
| T-186-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds ZERO dependencies (node:fs + node:path only, both built-ins). No package install, no legitimacy gate needed. |
</threat_model>

<verification>
- node scripts/build-corpus-stats.cjs emits both artifact files and exits 0.
- docs/CORPUS-STATS.generated.md is generated-stamped and carries 27,904 / 177 / 12,485.
- No directional sub-count (e.g. relationship count, MethodologyChunk substrate) is asserted as a magnitude in the artifact.
- grep -nE "brain-client|fetch\(|node:https|node:http" scripts/build-corpus-stats.cjs returns nothing.
</verification>

<success_criteria>
CORPUS-01 satisfied: a generated, generated-stamped single-source-of-truth artifact carries the three D1 magnitudes, rendered from a committed local source by a Brain-free local generator.
</success_criteria>

<artifacts_this_phase_produces>
- data/corpus-stats-source.json - the committed three-magnitude input (hand-edited only on a new live read).
- scripts/build-corpus-stats.cjs - the local generator (Plan 02 extends it with --check).
- docs/CORPUS-STATS.generated.md - the rendered single source of truth.
- data/corpus-stats.generated.json - the machine-readable sibling.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/186-corpus-stats-hygiene/186-01-SUMMARY.md` when done.
</output>
