---
phase: 186-corpus-stats-hygiene
plan: 02
type: execute
wave: 2
depends_on: [186-01]
files_modified:
  - scripts/build-corpus-stats.cjs
  - scripts/install-pre-commit.sh
  - scripts/release.sh
  - CLAUDE.md
  - .claude/includes/moat.md
  - docs/THE-BRAIN.md
  - docs/brain-setup.md
  - .planning/PROJECT.md
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-corpus-stats-artifact.cjs
  - tests/test-corpus-stats-tripwire.cjs
  - tests/run-all-186.sh
autonomous: true
requirements: [CORPUS-02]
canon_parts: [6, 8]

must_haves:
  truths:
    - "node scripts/build-corpus-stats.cjs --check on a clean repo exits 0."
    - "A stale literal placed on a LIVE surface (e.g. 748 frameworks / 27,804 nodes / 12,413 vectors / 21K nodes) makes --check exit non-zero and names the file + token."
    - "The SAME stale literal inside an excluded historical file (Canon Appendix D entry 13/16) does NOT trip --check (D4 historical provenance is frozen)."
    - "Every named LIVE surface shows 27,904 / 177 / 12,485, not the old figures."
    - "Canon Appendix D entries 13 and 16 still record the OLD numbers verbatim - history is preserved, not rewritten (D3 + D4)."
    - "The --check tripwire is wired into scripts/install-pre-commit.sh and scripts/release.sh alongside the sibling coverage gates."
  artifacts:
    - path: "scripts/build-corpus-stats.cjs"
      provides: "--check mode: artifact STALE byte-compare + LIVE-surface stale-literal scan with a documented historical-provenance exclude list"
      contains: "LIVE_SURFACES"
    - path: "tests/test-corpus-stats-tripwire.cjs"
      provides: "Asserts the tripwire fires on a LIVE-surface stale literal and PASSES on the same literal in an excluded historical file"
      min_lines: 30
    - path: "tests/run-all-186.sh"
      provides: "The one-command phase gate, mirroring tests/run-all-180.sh"
      min_lines: 15
  key_links:
    - from: "scripts/install-pre-commit.sh"
      to: "scripts/build-corpus-stats.cjs --check"
      via: "git diff --cached path-scoped guard on the LIVE surfaces + the source"
      pattern: "build-corpus-stats.cjs --check"
    - from: "scripts/build-corpus-stats.cjs"
      to: "data/corpus-stats-source.json"
      via: "reads the current magnitudes so the scan knows which numbers are current vs stale"
      pattern: "corpus-stats-source"
---

<objective>
Add the --check tripwire to the corpus-stats generator, repoint the stale literals on the LIVE fact surfaces to the three live magnitudes, wire the tripwire into the existing R9 enforcement surfaces (pre-commit + release), and ship the test that proves the historical-vs-live exclusion (D4) actually holds.

This plan delivers CORPUS-02: the --check tripwire with the historical-provenance exclude list, plus the LIVE-surface repoint. The crux is D4: the tripwire and the repoint touch LIVE surfaces ONLY and EXCLUDE historical provenance (Canon Appendix D entries 13/16, version-history rows, .planning dated artifacts). A blind sed across the repo is a defect; the exclude list is explicit and documented in the script.

Purpose: stale corpus literals stop drifting because a commit or a release now fails on a stale number on a live surface, while dated history stays frozen.
Output: the --check mode, repointed live surfaces, pre-commit/release wiring, and tests/run-all-186.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/186-corpus-stats-hygiene/186-CONTEXT.md
@.planning/phases/186-corpus-stats-hygiene/186-01-SUMMARY.md

# The --check STALE byte-compare idiom + the four-validation pattern to MIRROR.
@scripts/build-connector-registry.cjs
# Where the sibling generators wire their --check into pre-commit (lines 89-122
# splice path; lines 188-208 fresh-hook path) and release (Step 2.4).
@scripts/install-pre-commit.sh
# The one-command phase-gate aggregator idiom to MIRROR.
@tests/run-all-180.sh
</context>

<facts>
THE THREE CURRENT MAGNITUDES (D1, allowed on live surfaces): 27,904 nodes / 177 frameworks / 12,485 Pinecone vectors (1024-dim). Read from data/corpus-stats-source.json so the scan has one authority for "current".

THE STALE-LITERAL SET the tripwire fires on (the ROADMAP-named set, the stale forms of the THREE magnitudes, CONTEXT-ANCHORED to their magnitude noun to avoid colliding with directional sub-counts and dated records):
- nodes: 27,804 ; 21K / 21K+ ; 23K ; 15,298   (anchored near "node")
- frameworks: 748 ; 275+ / 275 ; 313           (anchored near "framework" / ":Framework")
- vectors: 12,413 ; 12,401 ; 1,427 ; 12K        (anchored near "vector" / "embedding" / "Pinecone")
Anchoring is mandatory: match "748" only adjacent to a framework noun, "12,401" only adjacent to a vector/embedding noun, etc. A bare "313" or "12,401" must NOT fire on a line-count, an edge-count, or a dated MethodologyChunk substrate mention.

DIRECTIONAL SUB-COUNTS (D2): 176 / 76 / 56 / 383 / 203 / 171, relationship counts (19,987 / 19,713 / 65K), MethodologyChunk substrate (12,401 as a substrate label), per-namespace splits. These are NEVER enforced by the tripwire and NEVER asserted as fact. Leave directional companions alone (the repoint targets the THREE magnitudes only).

LIVE SURFACES (scan AND repoint - the D4 named set; .claude/includes/moat.md is included because it is @included into CLAUDE.md and renders as the CLAUDE.md moat block):
- CLAUDE.md (moat block, Brain table, Project description, Technology Stack rows) - NOTE: CLAUDE.md ALREADY carries the 2026-06-11 set 27,804 / 19,987 / 12,413; this plan moves it to the D1 set 27,904 / 177 / 12,485.
- .claude/includes/moat.md (carries the oldest 15,298 / 19,713 / 12,401 set)
- docs/THE-BRAIN.md (Layer 1 + Layer 2 + Tier-0 lines)
- docs/brain-setup.md (the bullets)
- .planning/PROJECT.md (description line ~5; stack rows ~366, ~384: "21K+ nodes", "1,427 Pinecone embeddings", "21K-node")
- docs/MINDRIAN-CANON.md - LIVE PROSE ONLY (Part 2 Engine 1 figure mentions outside any dated entry, e.g. the "12,401 methodology nodes" in Part 2). Repoint the live-prose figure OR, if the executor judges a canon body edit too sensitive, add a NEW dated Appendix D figures-correction entry per D3 - NEVER rewrite an existing Appendix D entry.
- docs/CANON-PHASE-MAP.md - LIVE ROWS ONLY (e.g. the "Pinecone 1,427 embeddings" shipped row near line 46) - NEVER the Version-history table.

EXCLUDE LIST (documented in the tripwire; historical provenance is frozen, D4):
- Inside docs/MINDRIAN-CANON.md: everything from the "## Appendix D" heading onward (entries 13/16 deliberately record the OLD numbers as dated history).
- Inside docs/CANON-PHASE-MAP.md: the "## Version history" table and the Appendix D rows.
- All .planning/ DATED artifacts: anything matching SESSION / HANDOFF / SUMMARY / VERIFICATION / *-AUDIT / milestones, and debug autopsies. (PROJECT.md / ROADMAP.md / STATE.md are NOT dated artifacts and stay LIVE.)
- docs/autopsies/*, docs/testers/* (dated reads citing 12,401 as the live figure at that date).
- The artifact itself: docs/CORPUS-STATS.generated.md and data/corpus-stats.generated.json.
- Non-authoritative design / marketing / research docs that carry aspirational or dated figures and are NOT live-fact surfaces (document them by name in the exclude block): docs/MOAT-MANDATE.md, docs/MWP-SPECIFICATION.md, docs/IDEA-DOCUMENT.md, docs/BUSINESS-MODEL-AND-MOAT.md, docs/POWERHOUSE-1.6.0-SPEC.md, docs/BACKEND-MAP.md, docs/research/*, docs/superpowers/*. These are out of the enforced LIVE set to keep the phase lean; the exclude block states why (historical/aspirational design docs, not authoritative fact surfaces).
</facts>

<tasks>

<task type="auto">
  <name>Task 1: --check tripwire (artifact STALE byte-compare + LIVE-surface scan with documented exclude list) and the LIVE-surface repoint</name>
  <files>scripts/build-corpus-stats.cjs, CLAUDE.md, .claude/includes/moat.md, docs/THE-BRAIN.md, docs/brain-setup.md, .planning/PROJECT.md, docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    scripts/build-connector-registry.cjs lines 813-878 (the --check branch: the
    serialize-then-byte-compare STALE check, the errs[] accumulation, the
    process.exit(1) on any error, the one-line recovery on stderr) and lines
    616-687 (validateConnectors: the pure, exported, array-of-error-strings shape
    so a test can exercise each bad case without spawning a subprocess). The
    LIVE-surface scanner mirrors this shape: a pure exported function returning an
    array of "file:line stale token" error strings.
  </read_first>
  <action>
    Extend scripts/build-corpus-stats.cjs (NO new dependency; node:fs + node:path
    only). Add three things.

    (a) Define LIVE_SURFACES as an explicit array of the D4-named files (CLAUDE.md,
    .claude/includes/moat.md, docs/THE-BRAIN.md, docs/brain-setup.md,
    .planning/PROJECT.md, docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md). Define
    STALE_PATTERNS as the CONTEXT-ANCHORED regex set from <facts> (each stale token
    bound to its magnitude noun: nodes near "node", frameworks near
    "framework"/":Framework", vectors near "vector"/"embedding"/"Pinecone"). Define
    an in-file EXCLUDE contract: a function excludedRegion(file, lineIndex, lines)
    that returns true for the documented historical regions - in
    docs/MINDRIAN-CANON.md every line at or after the "## Appendix D" heading; in
    docs/CANON-PHASE-MAP.md the "## Version history" table region and the Appendix D
    rows. Add a top-of-file comment block enumerating the FULL exclude list from
    <facts> (the .planning dated artifacts, autopsies, testers, the artifact
    itself, and the named non-authoritative design docs) so the policy is
    self-documenting, exactly as the connector generator documents its Part 8
    posture.

    (b) Add scanLiveSurfaces() - a pure, exported function that reads each
    LIVE_SURFACES file line by line, skips excludedRegion lines, applies
    STALE_PATTERNS, and returns an array of "path:line stale-token" strings (empty
    = clean). It reads the current magnitudes from loadSource() so a future
    magnitude bump never makes the current numbers look stale.

    (c) Fill the --check branch of main() to mirror build-connector-registry.cjs:
    regenerate the artifact in memory (renderMarkdown + serializeArtifactJson) and
    push a STALE error if either on-disk file differs byte-for-byte; then push every
    scanLiveSurfaces() hit; if errs.length, print them + a one-line recovery
    ("repoint the live surface to 27,904 / 177 / 12,485, or run: node
    scripts/build-corpus-stats.cjs") to stderr and process.exit(1); else log
    "corpus-stats: OK". Export scanLiveSurfaces, excludedRegion, LIVE_SURFACES,
    STALE_PATTERNS for the Plan tests.

    THEN repoint the LIVE surfaces so --check goes green. On CLAUDE.md,
    .claude/includes/moat.md, docs/THE-BRAIN.md, docs/brain-setup.md, and
    .planning/PROJECT.md replace the stale node/framework/vector figures with
    27,904 / 177 / 12,485 (and 1024-dim where the dim is stated). Update the
    "live read" date phrasing to 2026-06-27 where a date is attached to the figure.
    Repoint the THREE magnitudes ONLY; leave directional companions (relationship
    counts, MethodologyChunk substrate label) as the executor finds them unless they
    are themselves a stale form of one of the three. On docs/MINDRIAN-CANON.md and
    docs/CANON-PHASE-MAP.md touch ONLY live prose/rows OUTSIDE the excluded
    historical regions; if a canon body edit is too sensitive, add a NEW dated
    Appendix D figures-correction entry per D3 instead of editing in place, and
    NEVER alter Appendix D entries 13 or 16. Hyphens only, never em-dashes.
  </action>
  <verify>
    <automated>node scripts/build-corpus-stats.cjs --check && echo CHECK_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/build-corpus-stats.cjs --check exits 0 on the repointed repo.
    - grep -nE "27,804|748 |12,413|21K|1,427|15,298|12,401" CLAUDE.md .claude/includes/moat.md docs/THE-BRAIN.md docs/brain-setup.md .planning/PROJECT.md returns NO node/framework/vector magnitude hit (directional companions and any non-magnitude coincidence aside).
    - grep -c "27,904" docs/THE-BRAIN.md and grep -c "177" docs/THE-BRAIN.md and grep -c "12,485" docs/THE-BRAIN.md each return at least 1.
    - Canon Appendix D entry 13 still contains "15,298" and entry 16 still contains "27,804" and "12,401" (history preserved): grep -c "15,298" docs/MINDRIAN-CANON.md is unchanged from pre-plan and >= 1.
  </acceptance_criteria>
  <done>The --check tripwire scans the LIVE surfaces with a documented exclude list, exits 0 on the repointed repo, and the five non-canon live surfaces carry 27,904 / 177 / 12,485 while Appendix D entries 13/16 are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the tripwire into pre-commit and release</name>
  <files>scripts/install-pre-commit.sh, scripts/release.sh</files>
  <read_first>
    scripts/install-pre-commit.sh lines 89-122 (the splice path: the
    grep-q idempotency guard + the HOOK_TRAILER heredoc + the path-scoped
    git diff --cached guard) and lines 188-208 (the fresh-hook HOOK_BODY path -
    the SAME guard must be added in BOTH places). scripts/release.sh lines 270-297
    (Step 2.4: the sibling coverage gates as HARD ABORT release steps).
  </read_first>
  <action>
    In scripts/install-pre-commit.sh add a corpus-stats guard MIRRORING the
    build-connector-registry.cjs guard in BOTH the splice path (a new
    grep-q "build-corpus-stats.cjs --check" idempotency check added to the
    top-level installed-guard AND a new HOOK_TRAILER heredoc) and the fresh-hook
    HOOK_BODY path. The guard is path-scoped: when any LIVE surface or the source
    is staged (git diff --cached --name-only matching CLAUDE.md,
    .claude/includes/moat.md, docs/THE-BRAIN.md, docs/brain-setup.md,
    .planning/PROJECT.md, docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md,
    data/corpus-stats-source.json, docs/CORPUS-STATS.generated.md), run
    node scripts/build-corpus-stats.cjs --check and exit 1 on failure with a
    one-line recovery. Update the trailing summary echo lines to mention the
    corpus-stats gate. Mirror the existing comment style (Canon Part 8: zero Brain
    calls).

    In scripts/release.sh, inside the Step 2.4 coverage-gate block (alongside the
    connector / orchestration-projection / render-coverage --checks), add a HARD
    ABORT step: if ! node "$PLUGIN_DIR/scripts/build-corpus-stats.cjs" --check then
    echo a red ABORT (a stale corpus literal on a live surface) + the recovery line
    and exit 1. Keep it lean and consistent with the surrounding three gates.
    Hyphens only, never em-dashes.
  </action>
  <verify>
    <automated>grep -q "build-corpus-stats.cjs --check" scripts/install-pre-commit.sh && grep -q "build-corpus-stats.cjs --check" scripts/release.sh && bash -n scripts/install-pre-commit.sh && bash -n scripts/release.sh && echo WIRED_OK</automated>
  </verify>
  <acceptance_criteria>
    - scripts/install-pre-commit.sh references build-corpus-stats.cjs --check in BOTH the splice path and the fresh HOOK_BODY path (grep -c returns >= 2).
    - scripts/release.sh references build-corpus-stats.cjs --check inside the Step 2.4 coverage-gate region.
    - bash -n passes on both scripts (no syntax error).
  </acceptance_criteria>
  <done>The corpus-stats --check tripwire fires from pre-commit (path-scoped on LIVE surfaces + the source) and is a HARD ABORT release gate, mirroring the sibling generators.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The phase test - artifact carries the three; tripwire fires on a live stale literal; tripwire PASSES on the same literal in an excluded historical file</name>
  <files>tests/test-corpus-stats-artifact.cjs, tests/test-corpus-stats-tripwire.cjs, tests/run-all-186.sh</files>
  <read_first>
    tests/run-all-180.sh (the run/PASS/FAIL aggregator idiom - copy its structure
    verbatim and retarget to 186). scripts/build-corpus-stats.cjs module exports
    (scanLiveSurfaces, excludedRegion, loadSource, renderMarkdown) so the tests
    call the pure functions directly without spawning a subprocess, exactly as
    tests of validateConnectors do.
  </read_first>
  <behavior>
    - test-corpus-stats-artifact.cjs: loadSource() returns the three magnitudes 27904/177/12485; renderMarkdown(src) contains "27,904", "177", "12,485", and the "do not hand-edit" stamp; the rendered markdown asserts NO directional sub-count as a magnitude (a negative assertion: it does not contain a "19,987 relationships" fact claim, etc.).
    - test-corpus-stats-tripwire.cjs FIRE case: write a temp LIVE-style fixture line containing a stale literal in magnitude context ("748 frameworks") and assert scanLiveSurfaces-style detection returns a hit (drive it via the exported pattern matcher on a fixture string, or via a tmp file added to a LIVE_SURFACES-shaped list).
    - test-corpus-stats-tripwire.cjs PASS case (the D4 crux): the SAME stale literal placed inside an excluded historical region (a line at/after "## Appendix D" in a canon-shaped fixture, or a path on the documented exclude list) returns NO hit - excludedRegion returns true for it. This is the test the CONTEXT names: fires on LIVE, does NOT fire on an excluded historical file.
  </behavior>
  <action>
    Write tests/test-corpus-stats-artifact.cjs asserting the artifact behaviors
    above against the exported pure functions (process.exit(1) on any failure, a
    console.log on pass - the plain assertion-script idiom used across tests/). Write
    tests/test-corpus-stats-tripwire.cjs asserting BOTH the FIRE case and the
    excluded-historical PASS case, using temp fixtures under the scratch dir so no
    real file is mutated (or by exercising excludedRegion + the STALE_PATTERNS
    matcher directly on fixture strings). Write tests/run-all-186.sh mirroring
    tests/run-all-180.sh: a run() helper, the two suites, plus a third run() that
    shells node scripts/build-corpus-stats.cjs --check (the live end-to-end green
    on the repointed repo), and the PASS/FAIL summary with a non-zero exit on any
    fail. bash only, no emoji, no em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-186.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-186.sh exits 0 with all suites PASSED.
    - The tripwire suite proves BOTH: a stale literal on a LIVE surface is detected, AND the same literal inside an excluded historical region (Appendix D) is NOT detected.
    - node tests/test-corpus-stats-artifact.cjs exits 0 and asserts the three magnitudes + the generated stamp.
  </acceptance_criteria>
  <done>tests/run-all-186.sh is the one-command phase gate and is green; it proves the artifact carries the three magnitudes, the tripwire fires on a live stale literal, and the tripwire stays silent on the same literal inside frozen historical provenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator --check -> Brain MCP | A --check on every commit/release must never depend on or egress to the Brain. Canon Part 8. |
| repoint sweep -> historical provenance | The dangerous boundary of THIS plan: a too-broad repoint or scan that rewrites a dated provenance record falsifies history (D4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-186-01 | Tampering / Denial of Service | scripts/build-corpus-stats.cjs --check | mitigate | --check reads only LOCAL files (the committed source + the live surfaces); no brain-client, no network module; a commit/release never hangs on or egresses to the Brain (D5). |
| T-186-02 | Repudiation / Tampering | the repoint sweep + the tripwire scan | mitigate | The scan carries an EXPLICIT, documented exclude list (Appendix D, version-history, .planning dated artifacts, autopsies, testers, the artifact, named non-authoritative design docs); excludedRegion is unit-tested to PASS on Appendix D; the done-criteria assert entries 13/16 are byte-unchanged. No blind sed. |
| T-186-SC | Tampering | npm/pip/cargo installs | mitigate | Zero dependencies added (node:fs + node:path + bash only). No legitimacy gate needed. |
</threat_model>

<verification>
- node scripts/build-corpus-stats.cjs --check exits 0 on the repointed repo.
- A deliberately reintroduced stale literal on a LIVE surface makes --check exit 1 (the tripwire suite proves this).
- The same literal inside Appendix D does NOT trip --check (the tripwire suite proves this).
- Canon Appendix D entries 13 and 16 are byte-unchanged (history preserved).
- scripts/install-pre-commit.sh and scripts/release.sh both invoke build-corpus-stats.cjs --check.
- bash tests/run-all-186.sh is green.
</verification>

<success_criteria>
CORPUS-02 satisfied: a --check tripwire with a documented historical-provenance exclude list is wired into pre-commit and release; the LIVE surfaces carry 27,904 / 177 / 12,485; historical provenance is frozen; no directional sub-count is enforced; no em-dashes.
</success_criteria>

<artifacts_this_phase_produces>
- scripts/build-corpus-stats.cjs (extended) - the --check tripwire: artifact STALE byte-compare + LIVE-surface stale-literal scan + documented exclude list.
- Repointed LIVE surfaces: CLAUDE.md, .claude/includes/moat.md, docs/THE-BRAIN.md, docs/brain-setup.md, .planning/PROJECT.md (and bounded live-prose touches on docs/MINDRIAN-CANON.md / docs/CANON-PHASE-MAP.md).
- scripts/install-pre-commit.sh + scripts/release.sh - the tripwire wired into the R9 enforcement surfaces.
- tests/test-corpus-stats-artifact.cjs, tests/test-corpus-stats-tripwire.cjs, tests/run-all-186.sh - the phase gate.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/186-corpus-stats-hygiene/186-02-SUMMARY.md` when done.
</output>
