---
phase: 27.1-hsi-reverse-salient-python-pipeline
plan: 02
subsystem: hooks
tags: [bash, python, hsi, post-write, dependency-check, setup]

requires:
  - phase: 27.1-01
    provides: "HSI Python scripts (compute-hsi.py, detect-reverse-salients.py) and KuzuDB bridge (hsi-to-kuzu.cjs)"
provides:
  - "Post-write cascade extended with background HSI computation step"
  - "check-hsi-deps script for tier detection (0/1/2)"
  - "/mos:setup hsi command for guided HSI installation"
affects: [phase-28, phase-29, phase-30]

tech-stack:
  added: []
  patterns: ["Background subshell for non-blocking Python computation in bash hooks", "Tiered dependency detection with exit codes"]

key-files:
  created:
    - scripts/check-hsi-deps
  modified:
    - scripts/post-write
    - commands/setup.md

key-decisions:
  - "HSI step placed after KuzuDB graph index, before classify-insight exec -- runs in background subshell"
  - "Hoisted PLUGIN_ROOT and room_dir to top-level scope so HSI block can access them outside the graph-index if block"
  - "HSI guard requires .lazygraph directory to exist -- rooms without KuzuDB skip HSI entirely"

patterns-established:
  - "Tiered dependency check: exit codes encode availability level (0=missing, 1=no-python, 2=no-sklearn)"
  - "Background subshell with || true on each step: failures in HSI never propagate to the hook"

requirements-completed: [HSI-04, HSI-05]

duration: 2min
completed: 2026-03-30
---

# Phase 27.1 Plan 02: Post-Write HSI Wiring Summary

**Background HSI computation wired into post-write cascade with 3-tier dependency checker and /mos:setup hsi guided install**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-30T22:50:47Z
- **Completed:** 2026-03-30T22:52:58Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Post-write cascade now fires HSI computation (compute-hsi.py -> detect-reverse-salients.py -> hsi-to-kuzu.cjs) in a non-blocking background subshell after every artifact filing
- check-hsi-deps script reports tier:0/1/2 based on Python + sklearn + sentence-transformers + Pinecone availability
- /mos:setup hsi provides guided install instructions with tier status checking, requirements-hsi.txt install, and Tier 2 upgrade path

## Task Commits

Each task was committed atomically:

1. **Task 1: Create check-hsi-deps and extend post-write cascade** - `f72defb` (feat)
2. **Task 2: Add /mos:setup hsi section to setup command** - `564311a` (feat)

## Files Created/Modified
- `scripts/check-hsi-deps` - Bash script that detects HSI tier (0/1/2) via Python package availability checks
- `scripts/post-write` - Extended with background HSI computation step after KuzuDB graph index
- `commands/setup.md` - Added /mos:setup hsi section with tier reporting and install instructions

## Decisions Made
- Hoisted PLUGIN_ROOT and room_dir variables to top-level scope in post-write so the HSI block can access them (they were previously scoped inside the KuzuDB if block)
- HSI guard checks for .lazygraph directory existence -- rooms without KuzuDB initialized skip HSI entirely (no wasted computation)
- HSI step runs BEFORE classify-insight exec (not after git-ops as plan interface suggested, since actual post-write structure differs from plan's 10-step abstraction)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Adapted post-write insertion point to actual file structure**
- **Found during:** Task 1
- **Issue:** Plan referenced "Step 10 git-ops at line 137" but actual post-write is 59 lines with no numbered cascade steps or git-ops section
- **Fix:** Inserted HSI block after KuzuDB graph index and before classify-insight exec, which is the correct position in the actual cascade
- **Files modified:** scripts/post-write
- **Verification:** grep confirms all three HSI references present
- **Committed in:** f72defb

**2. [Rule 3 - Blocking] Hoisted variables from inner scope**
- **Found during:** Task 1
- **Issue:** PLUGIN_ROOT and room_dir were set inside nested if blocks, unavailable to the HSI block at the outer scope
- **Fix:** Moved PLUGIN_ROOT to top-level and room_dir initialization before the if block, preserving the walk-up logic inside
- **Files modified:** scripts/post-write
- **Verification:** HSI block correctly references both variables
- **Committed in:** f72defb

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes necessary to adapt plan's interface assumptions to actual code structure. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 27.1 complete: HSI Python pipeline + post-write wiring fully in place
- Phase 28 (Binary Asset Filing) can proceed -- HSI only triggers on .md files, binary filing is independent
- Users can run `/mos:setup hsi` to activate HSI computation at any time

---
*Phase: 27.1-hsi-reverse-salient-python-pipeline*
*Completed: 2026-03-30*
