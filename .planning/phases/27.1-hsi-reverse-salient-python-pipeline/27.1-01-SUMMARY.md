---
phase: 27.1-hsi-reverse-salient-python-pipeline
plan: 01
subsystem: intelligence
tags: [hsi, lsa, svd, tfidf, miniml, pinecone, kuzudb, reverse-salient, sklearn, sentence-transformers]

requires:
  - phase: 15-lazygraph-knowledge-graph
    provides: KuzuDB openGraph/closeGraph/queryGraph/initSchema, EDGE_TYPES, open-use-close pattern
provides:
  - HSI computation pipeline (compute-hsi.py) with 3-tier embedding
  - Reverse salient cross-section detection (detect-reverse-salients.py)
  - KuzuDB bridge script (hsi-to-kuzu.cjs) for HSI_CONNECTION and REVERSE_SALIENT edges
  - Extended LazyGraph schema with HSI edge types
  - Python dependency manifest (requirements-hsi.txt)
affects: [27.1-02, analyze-room, post-write hook, presentation views]

tech-stack:
  added: [scikit-learn, numpy, sentence-transformers (requirements only)]
  patterns: [3-tier embedding (Tier0=bash/Tier1=MiniLM/Tier2=Pinecone), OM-HMM integrative scoring, content hash caching]

key-files:
  created:
    - scripts/compute-hsi.py
    - scripts/detect-reverse-salients.py
    - scripts/hsi-to-kuzu.cjs
    - requirements-hsi.txt
  modified:
    - lib/core/lazygraph-ops.cjs

key-decisions:
  - "EDGE_TYPES exported from lazygraph-ops.cjs (was internal-only, needed by verification)"
  - "500 max_features for TF-IDF (lighter than V2's 2000 -- room artifacts smaller than papers)"
  - "Content hash cache (.hsi-cache.json) enables skip on unchanged rooms"

patterns-established:
  - "Python scripts read room/*.md with regex frontmatter parsing (no PyYAML)"
  - "HSI pair output contract: .hsi-results.json with metadata, artifacts, hsi_pairs, reverse_salients"
  - "Tier fallback chain: Tier 2 attempt -> Tier 1 fallback -> error exit"

requirements-completed: [HSI-01, HSI-02, HSI-03, HSI-05]

duration: 3min
completed: 2026-03-30
---

# Phase 27.1 Plan 01: HSI Python Pipeline & KuzuDB Bridge Summary

**Dual-similarity HSI computation (LSA+MiniLM/Pinecone), cross-section reverse salient detection, and KuzuDB edge writer for automated innovation discovery**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-30T22:50:45Z
- **Completed:** 2026-03-30T22:54:28Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- compute-hsi.py (484 lines): reads room/*.md, computes TF-IDF/SVD structural + embedding semantic similarity, outputs .hsi-results.json with top 20 HSI pairs
- detect-reverse-salients.py (222 lines): identifies cross-section innovation opportunities from HSI results, classifies structural_transfer vs semantic_implementation, scores breakthrough potential
- hsi-to-kuzu.cjs (154 lines): reads .hsi-results.json, cleans old edges, writes fresh HSI_CONNECTION (Artifact->Artifact) and REVERSE_SALIENT (Section->Section) edges to KuzuDB
- LazyGraph schema extended with two new REL TABLEs carrying score/metadata properties

## Task Commits

Each task was committed atomically:

1. **Task 1: Create compute-hsi.py with 3-tier embedding and detect-reverse-salients.py** - `a73ebc7` (feat)
2. **Task 2: Create hsi-to-kuzu.cjs KuzuDB bridge and extend LazyGraph schema** - `b09109f` (feat)

## Files Created/Modified
- `scripts/compute-hsi.py` - HSI computation pipeline (LSA + embedding similarity + OM-HMM scoring)
- `scripts/detect-reverse-salients.py` - Cross-section reverse salient detection and classification
- `scripts/hsi-to-kuzu.cjs` - KuzuDB edge writer for HSI_CONNECTION and REVERSE_SALIENT
- `requirements-hsi.txt` - Python dependency manifest (scikit-learn, numpy, sentence-transformers)
- `lib/core/lazygraph-ops.cjs` - Extended EDGE_TYPES, initSchema with 2 new REL TABLEs, graphStats updated, EDGE_TYPES exported

## Decisions Made
- Exported EDGE_TYPES from lazygraph-ops.cjs (was internal-only constant, needed for external verification and tooling)
- Used 500 max_features for TF-IDF vectorizer (lighter than V2's 2000 since room artifacts are smaller than academic papers)
- Content hash caching in .hsi-cache.json enables skipping computation when room artifacts are unchanged

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] EDGE_TYPES not exported from lazygraph-ops.cjs**
- **Found during:** Task 2 (verification step)
- **Issue:** EDGE_TYPES array was defined but not included in module.exports, verification command failed
- **Fix:** Added EDGE_TYPES to module.exports object
- **Files modified:** lib/core/lazygraph-ops.cjs
- **Verification:** `node -e "require('./lib/core/lazygraph-ops.cjs').EDGE_TYPES.includes('HSI_CONNECTION')"` returns true
- **Committed in:** b09109f (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Minor addition to exports -- no scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required. Python deps are optional (scripts degrade gracefully with clear install messages).

## Next Phase Readiness
- .hsi-results.json output contract is ready for Plan 02 (integration with analyze-room and post-write hook)
- KuzuDB schema extended and ready for edge queries from dashboard/wiki views
- All scripts can be invoked standalone for testing

---
*Phase: 27.1-hsi-reverse-salient-python-pipeline*
*Completed: 2026-03-30*
