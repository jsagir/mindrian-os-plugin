---
phase: 182-signal-voice-color-render
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SIGNAL-01, SIGNAL-02]
canon_parts: [3, 10, 12]
files_modified:
  - lib/hmi/voice-color-mark.cjs
  - skills/larry-personality/SKILL.md
  - skills/ui-system/SKILL.md
must_haves:
  truths:
    - "The R15 render-coverage gate (scripts/check-render-coverage.cjs --check) exits 0 -- the Phase 179 GA-4 lean is confirmed, not rebuilt (SIGNAL-01)."
    - "Every Larry CLI turn carries exactly one De Stijl voice-color mark, derived from the pedagogical move (blue=building, red=challenging, yellow=contradiction, black=gate, white=invisibility) (SIGNAL-02, D1)."
    - "A turn with no mark is legible as the native host -- the absence is the signal (Part 12 Voice Signature; D1)."
    - "The mark is ONE of the 5 existing De Stijl Mondrian primaries -- NO new color is minted (D2, D4)."
    - "No frozen contract is touched: MAX_K=3, DIAL_REACH_K=6, the 12-glyph vocabulary, the 0.70/0.15 gate, the 6-reach bank, R15 render coverage all unchanged (D2)."
  artifacts:
    - path: "lib/hmi/voice-color-mark.cjs"
      provides: "The deterministic voice-color-mark module: the frozen 5-color->move map, markForMove(move), and detectVoiceMark(turnText) classifier (Larry vs native-host vs missing-mark)."
      exports: ["VOICE_COLOR_MARKS", "MARK_COLORS", "markForMove", "detectVoiceMark"]
      min_lines: 60
    - path: "skills/larry-personality/SKILL.md"
      provides: "The Voice Signature doctrine block: one De Stijl mark per CLI turn from the move, absence = native host, plus the honest declaration-enforced residual."
      contains: "Voice Signature"
    - path: "skills/ui-system/SKILL.md"
      provides: "The render-contract side: the mark is one of the 5 existing De Stijl colors, additive legibility, alters no frozen render contract, no new color minted."
      contains: "voice-color mark"
  key_links:
    - from: "lib/hmi/voice-color-mark.cjs"
      to: "references/visual/palette.json"
      via: "the 5 MARK_COLORS are the 5 Mondrian primaries (no new color anchor)"
      pattern: "mondrian_(red|blue|yellow|black|white)"
    - from: "skills/larry-personality/SKILL.md"
      to: "lib/hmi/voice-color-mark.cjs"
      via: "the doctrine names the same 5 color->move mappings the module encodes"
      pattern: "blue.*building|red.*challeng"
---

<objective>
Deliver the CLI half of the Part 12 Voice Signature HARD requirement: confirm the render half it leans on still holds (SIGNAL-01, verify), then declare and encode the per-turn De Stijl voice-color mark that makes Larry distinguishable from the native host (SIGNAL-02, build).

This phase LEANS ON shipped Phase 179 surfaces. SIGNAL-01 is a VERIFY, not a rebuild: the F.7 always-renders guarantee shipped via the Phase 179 GA-4 card-fire interceptor (green 2026-06-25) and this plan confirms the R15 gate still passes. SIGNAL-02 attaches the mark at the Phase 179 voice surfaces (D1: larry-personality, ui-system) and adds one small deterministic module, mirroring the Phase 179 check-card-fire.cjs exported-predicate idiom. Roughly 90 percent repoint, minimal net-new.

Purpose: a product the navigator cannot tell apart from the generic host is not a product (Part 10). The mark names the pedagogical move and makes invisibility a state with a color (white at the moment the insight lands).
Output: the voice-color-mark module, the Voice Signature doctrine in the two voice SKILLs, and the live-confirmed R15 gate. The missing-mark test and the run-all-182.sh aggregator land in Plan 182-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-signal-voice-color-render/182-CONTEXT.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md

# The render half SIGNAL-01 verifies (do NOT rebuild)
@scripts/check-render-coverage.cjs
@.planning/phases/179-ignite-b1-starting-point-fix/179-01-SUMMARY.md

# The voice surfaces the mark attaches to (D1 -- reuse, do not invent)
@skills/larry-personality/SKILL.md
@skills/ui-system/SKILL.md

# The deterministic-predicate idiom to mirror + the no-new-color anchor
@scripts/check-card-fire.cjs
@references/visual/palette.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: SIGNAL-01 -- verify the R15 render-coverage gate still passes (lean on 179, do not rebuild)</name>
  <read_first>
    - .planning/phases/182-signal-voice-color-render/182-CONTEXT.md (SIGNAL-01 is VERIFY, not rebuild; the F.7 guarantee shipped via 179 GA-4)
    - scripts/check-render-coverage.cjs (the R15 gate; the --check fail-closed exit contract; do NOT modify it)
    - .planning/phases/179-ignite-b1-starting-point-fix/179-01-SUMMARY.md (the GA-4 Stop-hook card-fire interceptor SIGNAL-01 leans on)
  </read_first>
  <files>(none modified -- this is a verification task; its output is the green gate-run, enshrined permanently as leg (a) of run-all-182.sh in Plan 182-02)</files>
  <action>
    Confirm the Phase 179 render half still holds, per SIGNAL-01 (verify, not rebuild). Run the R15 render-coverage gate via its --check entry point and confirm a zero exit (the gate prints render-coverage: OK). Confirm the GA-4 lean is intact: the Phase 179 GA-4 card-fire interceptor test still passes and the check-card-fire Stop block is still registered in hooks.json. Do NOT modify scripts/check-render-coverage.cjs, hooks.json, or any Phase 179 surface -- if the gate is RED, STOP and report the regression rather than patching the gate (a RED gate here is a 179 regression, out of this phase's build scope per the locked SIGNAL-01 decision). Record the confirmation in the plan SUMMARY; the permanent, repeatable capture of this verify is wired as the first leg of tests/run-all-182.sh in Plan 182-02.
  </action>
  <verify>
    <automated>node scripts/check-render-coverage.cjs --check && node tests/test-ga4-card-fire-interceptor.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-render-coverage.cjs --check` exits 0 and prints `render-coverage: OK` (the R15 gate is green; 179 lean confirmed).
    - `node tests/test-ga4-card-fire-interceptor.cjs` exits 0 (the GA-4 interceptor still passes).
    - No Phase 179 surface (scripts/check-render-coverage.cjs, hooks.json, check-card-fire.cjs) is modified by this task.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SIGNAL-02 -- the voice-color-mark module + the Voice Signature doctrine on the two voice surfaces</name>
  <read_first>
    - skills/ui-system/SKILL.md section 4 (the Color Contract + the Dual De Stijl palette: the Mondrian primaries red/yellow/blue/black/white -- the 5 existing colors the mark MUST be drawn from)
    - skills/larry-personality/SKILL.md "Operating the Dial" + the voice doctrine region (where per-turn behavior is declared; the mark doctrine attaches here, D1)
    - scripts/check-card-fire.cjs (the deterministic exported-predicate idiom: classifyCardFire -- mirror its shape for detectVoiceMark)
    - references/visual/palette.json (the canonical De Stijl source of truth: base.mondrian_red/blue/yellow/black/white -- the no-new-color anchor)
    - docs/MINDRIAN-CANON.md Part 12 (The Voice Signature HARD requirement + the blue/red/yellow/black/white move semantics)
  </read_first>
  <files>lib/hmi/voice-color-mark.cjs, skills/larry-personality/SKILL.md, skills/ui-system/SKILL.md</files>
  <behavior>
    - markForMove('building') returns 'blue'; 'challenging' returns 'red'; 'contradiction' returns 'yellow'; 'gate' returns 'black'; 'invisibility' returns 'white'. Every one of the 5 canonical moves maps to exactly one of the 5 De Stijl colors.
    - MARK_COLORS is a frozen 5-member set equal to the 5 Mondrian primaries in references/visual/palette.json -- no sixth color, no non-palette color.
    - detectVoiceMark on a turn opening with one valid mark returns {hasMark:true, count:1, color:<one of 5>, isNativeHost:false, valid:true}.
    - detectVoiceMark on a turn with NO mark returns {hasMark:false, count:0, isNativeHost:true} -- the absence is the native-host signal.
    - detectVoiceMark on a turn carrying more than one mark returns valid:false (the exactly-one contract).
    - detectVoiceMark rejects a non-De-Stijl color tag (for example a green or purple tag) as not a valid mark (no new color is minted).
  </behavior>
  <action>
    Create lib/hmi/voice-color-mark.cjs as a pure, LOCAL, dependency-free CJS module (mirror the check-card-fire.cjs deterministic-predicate shape; no Brain wire, no network, no user data -- D4, Part 8). Define the mark as a single bracketed De Stijl color tag anchored at turn start, one of BLUE / RED / YELLOW / BLACK / WHITE, rendered with the De Stijl glyph and ANSI color already shipped in lib/core/visual-ops.cjs (reuse, do not mint a new glyph). Export: VOICE_COLOR_MARKS (the frozen move->color map per the Part 12 semantics: building=blue, challenging=red, contradiction=yellow, gate=black, invisibility=white); MARK_COLORS (the frozen 5-color set, drawn from references/visual/palette.json so a sixth color is structurally impossible); markForMove(move); and detectVoiceMark(turnText) returning {hasMark, count, color, isNativeHost, valid} with the classifier behavior above. Anchor the detector at turn start (case-insensitive), accept exactly one mark, and treat zero marks as native-host.

    Attach the doctrine at the two Phase 179 voice surfaces (D1, reuse). In skills/larry-personality/SKILL.md add a "Voice Signature (Part 12 HARD requirement)" doctrine block: Larry opens every CLI turn with exactly ONE De Stijl voice-color mark naming the pedagogical move (blue=building with you, red=challenging, yellow=contradiction surfaced, black=the frame / a Decision Gate, white=getting out of the way / invisibility); a turn with NO mark IS the native host speaking and that absence is itself the signal; invisibility ends the badge on white the moment the insight lands. Name the honest residual explicitly (the same honest-residual framing Phase 178 R15 used for the terminal tool-call): enforcement is doctrine plus a declaration test over the SKILL surfaces and the detector module, NOT a runtime interceptor that recolors every literal model token -- there is no hook that recolors assistant text, so the guarantee is the declared convention plus the missing-mark test, not a per-token runtime guarantee. Cite Canon Part 12 and the Part 3 De Stijl palette.

    In skills/ui-system/SKILL.md, in or adjacent to section 4 (the Color Contract / Dual De Stijl palette), add the render-contract side: the voice-color mark is ONE of the 5 existing De Stijl Mondrian colors (no new color minted, D2/D4); the mark is ADDITIVE legibility and alters NO frozen render contract -- the 12-glyph vocabulary, the 4-zone anatomy, MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate, the 6-reach bank, and R15 render coverage are all unchanged. Keep both edits em-dash-free (D5; the Phase 175/179 self-consistency idiom).
  </action>
  <verify>
    <automated>node -e "const m=require('./lib/hmi/voice-color-mark.cjs'); const a=['blue','red','yellow','black','white']; if(m.markForMove('building')!=='blue'||m.markForMove('invisibility')!=='white') throw new Error('move map'); if([...m.MARK_COLORS].sort().join()!==a.sort().join()) throw new Error('not the 5 Mondrian primaries'); const d=m.detectVoiceMark('[BLUE] building the next node'); if(!d.hasMark||d.count!==1||d.isNativeHost) throw new Error('marked-turn'); const n=m.detectVoiceMark('plain native host line'); if(n.hasMark||!n.isNativeHost) throw new Error('native-host'); const g=m.detectVoiceMark('[GREEN] not a de stijl color'); if(g.valid) throw new Error('rejected new color'); console.log('voice-color-mark OK')"</automated>
  </verify>
  <acceptance_criteria>
    - lib/hmi/voice-color-mark.cjs exports VOICE_COLOR_MARKS, MARK_COLORS, markForMove, detectVoiceMark; the Task 2 verify node -e probe prints `voice-color-mark OK` (exit 0).
    - markForMove maps each of the 5 moves to exactly one of {blue, red, yellow, black, white}; MARK_COLORS equals the 5 Mondrian primaries (no sixth color).
    - detectVoiceMark classifies a marked turn (count 1, not native-host), an unmarked turn (isNativeHost true), a multi-mark turn (valid false), and a non-De-Stijl tag (valid false).
    - `grep -q 'Voice Signature' skills/larry-personality/SKILL.md` succeeds and the block names all 5 color->move mappings + the absence-is-native-host rule + the declaration-enforced residual.
    - `grep -q 'voice-color mark' skills/ui-system/SKILL.md` succeeds and the block states "no new color" + "alters no frozen render contract".
    - No em-dash appears in lib/hmi/voice-color-mark.cjs or the two SKILL edits (D5).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Larry turn text -> detector | The detector reads turn text; it is LOCAL, in-process, no untrusted external input, no network. |
| mark -> Brain | No wire. The mark is a generic render attribute (a color enum derived from the pedagogical move); it carries no user data and never egresses (Part 8, D4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-01 | Tampering | doctrine-vs-code drift (SKILL says X, module encodes Y) | mitigate | the Plan 182-02 missing-mark/declaration test asserts the doctrine prose and the module agree on the 5 color->move mappings; drift fails the test. |
| T-182-02 | Information disclosure | the mark crossing the Part 8 boundary | mitigate | voice-color-mark.cjs is pure/LOCAL/dependency-free; MARK_COLORS is a closed enum; no Brain call, no network, no user-content field -- a build-time read confirms zero egress surface. |
| T-182-03 | Spoofing | native-host text falsely carrying a Larry mark | mitigate | detectVoiceMark accepts ONLY the 5 De Stijl primaries anchored at turn start and rejects any non-palette tag and any multi-mark turn (exactly-one contract). |
| T-182-04 | Elevation of privilege | the mark mutating a frozen render contract | accept | the mark is additive legibility only; D2 keeps MAX_K=3 / DIAL_REACH_K=6 / the glyph vocab / the 0.70/0.15 gate / the 6-reach bank / R15 untouched; the carried drift fences in run-all-182.sh (Plan 02) prove the frozen sets are unchanged. |

No package-manager install occurs in this phase (no npm/pip/cargo), so no supply-chain (T-182-SC) legitimacy checkpoint is required.
</threat_model>

<artifacts>
## Artifacts this phase produces (Plan 182-01)

- `lib/hmi/voice-color-mark.cjs` -- NEW. The deterministic voice-color-mark module: VOICE_COLOR_MARKS (frozen move->color), MARK_COLORS (frozen 5-primary set), markForMove(move), detectVoiceMark(turnText). Pure, LOCAL, no Brain wire.
- `skills/larry-personality/SKILL.md` -- MODIFIED (additive). The Voice Signature doctrine block: one De Stijl mark per CLI turn from the move, absence = native host, the white-at-insight invisibility state, and the named declaration-enforced residual.
- `skills/ui-system/SKILL.md` -- MODIFIED (additive). The render-contract side: the mark is one of the 5 existing De Stijl colors, additive, alters no frozen render contract, no new color minted.

(The missing-mark test and tests/run-all-182.sh aggregator are produced by Plan 182-02.)
</artifacts>

<verification>
- SIGNAL-01: `node scripts/check-render-coverage.cjs --check` exits 0 (R15 gate green; 179 lean confirmed); `node tests/test-ga4-card-fire-interceptor.cjs` exits 0.
- SIGNAL-02: the Task 2 node -e probe prints `voice-color-mark OK`; the doctrine grep succeeds on both SKILL surfaces.
- D2/D4: no frozen-set file is touched; `node tests/test-reach-ids-drift.cjs` and `node tests/test-posture-ids-drift.cjs` stay green (re-run as carried fences in Plan 182-02).
- D5: no em-dash in any file this plan creates or edits.
</verification>

<success_criteria>
- The R15 render-coverage gate passes live (SIGNAL-01 verified, 179 leaned on, not rebuilt).
- lib/hmi/voice-color-mark.cjs exists and its detector correctly classifies Larry / native-host / missing-mark / non-De-Stijl-tag turns.
- The Voice Signature doctrine is declared at both Phase 179 voice surfaces, names the 5 color->move mappings, the absence-is-native-host rule, and the honest declaration-enforced residual.
- No new color is minted; no frozen render contract is altered; no Brain wire is opened; no em-dash is introduced.
</success_criteria>

<output>
Create `.planning/phases/182-signal-voice-color-render/182-01-SUMMARY.md` when done.
</output>
