---
phase: 182-signal-voice-color-render
plan: 02
type: execute
wave: 2
depends_on: [182-01]
autonomous: true
requirements: [SIGNAL-02]
canon_parts: [3, 10, 12]
files_modified:
  - tests/test-larry-voice-mark-182.cjs
  - tests/run-all-182.sh
must_haves:
  truths:
    - "A Larry CLI turn missing its De Stijl voice-color mark is CAUGHT by tests/test-larry-voice-mark-182.cjs (SIGNAL-02 acceptance)."
    - "The doctrine declared in Plan 182-01 (larry-personality + ui-system SKILL.md) is asserted present by the drift test -- doctrine and code cannot silently diverge (D1)."
    - "tests/run-all-182.sh is the single PASS/FAIL phase gate: leg (a) re-runs the R15 gate (SIGNAL-01), leg (b) confirms the 179 GA-4 lean, leg (c) runs the missing-mark test (SIGNAL-02), leg (d) carries the frozen-set drift fences (D2)."
    - "The carried reach-ids (frozen 6) + posture-ids (frozen 3) drift fences stay green -- proof D2/D4 mint no frozen-set member (no new reach, no new color)."
  artifacts:
    - path: "tests/test-larry-voice-mark-182.cjs"
      provides: "The missing-mark + declaration drift test: classifier behavior + no-new-color anchor + doctrine-declared assertions + em-dash self-check, mirroring lib/memory/skill-vs-code-drift.test.cjs."
      min_lines: 90
    - path: "tests/run-all-182.sh"
      provides: "The Phase 182 single PASS/FAIL aggregator (mirrors tests/run-all-180.sh): R15 gate + 179 lean + missing-mark test + carried drift fences."
      min_lines: 30
  key_links:
    - from: "tests/test-larry-voice-mark-182.cjs"
      to: "lib/hmi/voice-color-mark.cjs"
      via: "require + assert markForMove / MARK_COLORS / detectVoiceMark behavior"
      pattern: "require.*voice-color-mark"
    - from: "tests/test-larry-voice-mark-182.cjs"
      to: "references/visual/palette.json"
      via: "assert MARK_COLORS equals the 5 Mondrian primaries (no-new-color anchor)"
      pattern: "palette\\.json"
    - from: "tests/run-all-182.sh"
      to: "scripts/check-render-coverage.cjs"
      via: "leg (a) re-runs the R15 gate (SIGNAL-01 enshrined)"
      pattern: "check-render-coverage"
---

<objective>
Close SIGNAL-02 by catching the failure it names: a Larry CLI turn that is missing its De Stijl voice-color mark. Build the missing-mark/declaration drift test over the Plan 182-01 module and doctrine, then add the run-all-182.sh phase aggregator that welds the SIGNAL-01 verify and the SIGNAL-02 test into one PASS/FAIL gate.

The test mirrors the proven Phase 121.5 lib/memory/skill-vs-code-drift.test.cjs idiom (D1): it both exercises the deterministic detector (Larry / native-host / missing-mark / non-De-Stijl-tag) and asserts the doctrine is DECLARED at the two voice SKILL surfaces, so doctrine and code cannot drift apart at PR time rather than at audit time. The aggregator mirrors tests/run-all-180.sh.

Purpose: make the Voice Signature requirement enforced, not aspirational -- a missing mark fails the build, and the honest residual (declaration-enforced, not a runtime recolor) is itself an asserted line so it stays named.
Output: the missing-mark test and the phase aggregator. Depends on Plan 182-01 (the module + doctrine it tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/182-signal-voice-color-render/182-CONTEXT.md
@.planning/phases/182-signal-voice-color-render/182-01-PLAN.md

# The module + doctrine this plan tests (produced by Plan 182-01)
@lib/hmi/voice-color-mark.cjs
@skills/larry-personality/SKILL.md
@skills/ui-system/SKILL.md

# The drift-test idiom to mirror (D1) + the aggregator template
@lib/memory/skill-vs-code-drift.test.cjs
@tests/run-all-180.sh
@references/visual/palette.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SIGNAL-02 -- the missing-mark + doctrine-declaration drift test</name>
  <read_first>
    - lib/memory/skill-vs-code-drift.test.cjs (the idiom to mirror: read the SKILL source, assert load-bearing prose; deterministic classifier assertions; the assert/test harness shape; no em-dash self-consistency)
    - lib/hmi/voice-color-mark.cjs (the Plan 182-01 module under test: VOICE_COLOR_MARKS, MARK_COLORS, markForMove, detectVoiceMark)
    - skills/larry-personality/SKILL.md + skills/ui-system/SKILL.md (the doctrine the test asserts is declared)
    - references/visual/palette.json (base.mondrian_red/blue/yellow/black/white -- the no-new-color anchor the test reads)
  </read_first>
  <files>tests/test-larry-voice-mark-182.cjs</files>
  <behavior>
    - markForMove maps each of the 5 moves (building/challenging/contradiction/gate/invisibility) to exactly one of {blue,red,yellow,black,white}; a bogus move returns no color.
    - MARK_COLORS equals the 5 Mondrian primaries read from references/visual/palette.json -- a sixth or non-palette color FAILS the test (no new color minted; D2/D4).
    - detectVoiceMark on a marked Larry turn -> {hasMark:true, count:1, isNativeHost:false, valid:true}.
    - detectVoiceMark on a turn with NO mark -> {hasMark:false, isNativeHost:true} (absence is the native-host signal).
    - THE missing-mark catch: a Larry turn that SHOULD carry a mark but omits it is classified as native-host / no-mark, and the test asserts this is the caught condition SIGNAL-02 names.
    - detectVoiceMark on a >1-mark turn -> valid:false; on a non-De-Stijl tag ([GREEN]/[PURPLE]) -> valid:false.
    - The doctrine is DECLARED: larry-personality SKILL.md contains "Voice Signature", the 5 color->move mappings, the absence-is-native-host rule, and the declaration-enforced residual line; ui-system SKILL.md contains the "voice-color mark" + "no new color" + "no frozen render contract" prose.
    - Self-consistency: the test file is itself em-dash-free (Phase 175/179 unicode-escape dash detection idiom).
  </behavior>
  <action>
    Create tests/test-larry-voice-mark-182.cjs mirroring lib/memory/skill-vs-code-drift.test.cjs: the same assert(cond,msg) + test(label,fn) harness, a passed/failed counter, and process.exit(1) on any failure. Require lib/hmi/voice-color-mark.cjs and assert the classifier behavior in the behavior block. Read references/visual/palette.json and assert MARK_COLORS is exactly the set {mondrian_red, mondrian_blue, mondrian_yellow, mondrian_black, mondrian_white} reduced to the bare color names -- the load-bearing no-new-color anchor (D2/D4). Add the missing-mark assertion explicitly: build a Larry turn string that SHOULD wear a mark, strip the mark, run detectVoiceMark, and assert it is caught (isNativeHost true / valid not-Larry) -- this is the SIGNAL-02 acceptance ("a Larry CLI turn missing its mark is caught by a test"). Add the doctrine-declared assertions by reading the two SKILL.md files and grepping the load-bearing prose (mirror skill-vs-code-drift Tests 4-6): "Voice Signature", the 5 mappings, the absence rule, the residual line in larry-personality; "voice-color mark", "no new color", and the frozen-contract-untouched line in ui-system. Add the em-dash self-check via a unicode-escape scan so the test file cannot itself contain an em-dash (D5). Use grep-gate hygiene where you count tokens (strip comment lines before counting; never a bare ==0 on an unfiltered file).
  </action>
  <verify>
    <automated>node tests/test-larry-voice-mark-182.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-larry-voice-mark-182.cjs` exits 0 with every assertion passing.
    - The test FAILS (proven by a scratch mutation, reverted) if MARK_COLORS gains a sixth color, if the missing-mark turn is NOT caught, or if either SKILL doctrine line is removed -- it is a real drift fence, not a tautology.
    - The test file contains zero em-dash characters (its own unicode-escape self-check passes).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: the run-all-182.sh phase aggregator (welds SIGNAL-01 verify + SIGNAL-02 test + carried fences)</name>
  <read_first>
    - tests/run-all-180.sh (the aggregator template to mirror: set -uo pipefail, run() helper, PASS/FAIL counters, file-guarded carried fences, final exit on FAIL count)
    - tests/run-all-179.sh (the run_if file-existence guard idiom for the 179 GA-4 lean leg)
    - scripts/check-render-coverage.cjs (the R15 gate leg (a) invokes with --check)
  </read_first>
  <files>tests/run-all-182.sh</files>
  <action>
    Create tests/run-all-182.sh mirroring tests/run-all-180.sh: bash only, no emoji, no em-dash, set -uo pipefail, cd to repo root from BASH_SOURCE, a run() helper that prints PASSED/FAILED and increments counters, and a final non-zero exit when FAIL > 0. Wire four legs. Leg (a) SIGNAL-01: `node scripts/check-render-coverage.cjs --check` (the R15 gate still green -- enshrines the live verify from Plan 182-01 Task 1 as a permanent, repeatable check). Leg (b) SIGNAL-01 lean: `node tests/test-ga4-card-fire-interceptor.cjs` (the Phase 179 GA-4 interceptor still passes), file-guarded so a missing 179 suite is a SKIP, not a hard FAIL. Leg (c) SIGNAL-02: `node tests/test-larry-voice-mark-182.cjs` (the missing-mark test). Leg (d) carried frozen-set drift fences, each file-guarded: `node tests/test-reach-ids-drift.cjs` (frozen 6) and `node tests/test-posture-ids-drift.cjs` (frozen 3) -- proof D2/D4 mint no frozen-set member. Do not modify any existing aggregator (run-all-179.sh / run-all-180.sh); this is a new sibling file.
  </action>
  <verify>
    <automated>bash tests/run-all-182.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-182.sh` exits 0 with leg (a) render-coverage OK, leg (b) GA-4 interceptor PASSED, leg (c) missing-mark test PASSED, leg (d) both drift fences PASSED.
    - The script is bash-only, em-dash-free, emoji-free, and prints a final Passed/Failed summary; a non-zero FAIL count returns a non-zero exit.
    - No existing aggregator or Phase 179 surface is modified.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> repo files | The test reads LOCAL repo files (the module + the two SKILLs + palette.json); no network, no Brain, no untrusted input. |
| aggregator -> gates | run-all-182.sh shells LOCAL node/bash gates only; it opens no remote wire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-182-05 | Tampering | a tautological test that passes regardless of doctrine/code state | mitigate | the test asserts a real no-new-color anchor against palette.json + a real missing-mark catch + real SKILL-prose greps; the acceptance requires a scratch-mutation proof that the fence trips RED. |
| T-182-06 | Repudiation | a silent SKIP hiding a missing 179 lean leg | accept | the 179 leg is file-guarded and records a visible SKIP (mirrors run-all-179.sh); the R15 gate leg (a) is unguarded and hard-FAILs, so the SIGNAL-01 verify can never be silently skipped. |
| T-182-07 | Information disclosure | a test or aggregator opening a Brain/network wire | mitigate | both files are LOCAL-only (node/bash over repo files); a build-time read confirms no fetch/http/Brain call surface (Part 8, D4). |

No package-manager install occurs in this phase (no npm/pip/cargo), so no supply-chain (T-182-SC) legitimacy checkpoint is required.
</threat_model>

<artifacts>
## Artifacts this phase produces (Plan 182-02)

- `tests/test-larry-voice-mark-182.cjs` -- NEW. The missing-mark + doctrine-declaration drift test (mirrors lib/memory/skill-vs-code-drift.test.cjs): classifier behavior, the no-new-color anchor against palette.json, the explicit missing-mark catch (SIGNAL-02 acceptance), the two-SKILL doctrine-declared assertions, and the em-dash self-check.
- `tests/run-all-182.sh` -- NEW. The Phase 182 single PASS/FAIL aggregator (mirrors tests/run-all-180.sh): leg (a) R15 gate (SIGNAL-01), leg (b) 179 GA-4 lean, leg (c) missing-mark test (SIGNAL-02), leg (d) carried reach-ids/posture-ids drift fences (D2/D4).
</artifacts>

<verification>
- SIGNAL-02: `node tests/test-larry-voice-mark-182.cjs` exits 0; the missing-mark turn is caught; both SKILL doctrine assertions pass.
- Phase gate: `bash tests/run-all-182.sh` exits 0 with all four legs green.
- D2/D4: the carried reach-ids (frozen 6) + posture-ids (frozen 3) drift fences pass inside the aggregator; no frozen-set member is minted.
- D5: neither new file contains an em-dash; the test's own unicode-escape self-check passes.
</verification>

<success_criteria>
- A Larry CLI turn missing its voice-color mark is caught by tests/test-larry-voice-mark-182.cjs (SIGNAL-02 closed).
- The doctrine declared in Plan 182-01 cannot silently drift from the module -- the drift test asserts both.
- tests/run-all-182.sh is the one-command Phase 182 gate and is green: R15 verify + 179 lean + missing-mark + frozen-set fences.
- No frozen-set member minted, no new color, no Brain wire, no em-dash.
</success_criteria>

<output>
Create `.planning/phases/182-signal-voice-color-render/182-02-SUMMARY.md` when done.
</output>
