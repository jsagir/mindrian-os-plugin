---
phase: 341-install-and-update-overhaul-npm-source-plugin-artifact-heavy
plan: 05
type: execute
wave: 5
depends_on: ["341-04"]
files_modified:
  - scripts/release.sh
  - scripts/release-lib/shrinkwrap-gate.sh
  - scripts/doctor.cjs
  - tests/fixtures/310-release-step-block-hashes.txt
  - tests/fixtures/341-release-step-block-hashes.txt
  - tests/run-all-310.sh
  - tests/test-release-bump-tag-and-publish-gates.cjs
  - tests/test-341-release-shrinkwrap-gate.cjs
  - tests/test-341-marketplace-npm-source.cjs
  - tests/test-341-version-of-record-source-version.cjs
autonomous: true
requirements: [D-01, D-04, D-06, D-08, D-13]
canon_parts: [7, 8]

must_haves:
  truths:
    - "Per D-01 release.sh Step 4 writes a marketplace source of exactly {source: npm, package: @mindrian_os/cli, version: <exact>} and DELETES any residual source.ref and source.url keys, with no version prefix and no registry key."
    - "Per D-01 the pin is an exact version, never a range and never a dist-tag: the loader's npm cache-hit test is a literal string equality against the installed version, so only an exact pin can ever produce a cache hit, and a dist-tag would let Step 9.5's @next to @latest promotion reach every user without any version bump."
    - "Per D-06 Step 6.7's vendoring is gone and its slot generates npm-shrinkwrap.json with three assertions, the Commit-B untrack block is gone, MOS_SKIP_VENDOR is gone, and Steps 5.5, 7, 8, 9.6b and 9.8 are untouched."
    - "Per D-06 and D-08 Step 9.5's blanket node_modules ban and its docs-and-release.sh string blacklist become the ceiling assertion, while .planning/, mcp-server-brain/ and tests/ stay blacklisted because they are still correct."
    - "Per D-06 doctor version-of-record-published compares marketplace source.version to the version with no v prefix, and its single npm view network call survives unchanged."
    - "The two release-train blockers are handled in the SAME plan that edits release.sh: tests/run-all-310.sh legs 3 and 4 are rebaselined and their literals updated in the same commit, so the release train is never knowingly red between commits."
    - "Every release-gate test added here is zero-network: no real git push, no npm publish, no registry call, using RELEASE_TEST_MODE, MOS_TEST_DRY_RUN=1 and the verify-tag-push.sh injectable-hook idiom."
  artifacts:
    - path: "scripts/release-lib/shrinkwrap-gate.sh"
      provides: "Injectable, sourceable shrinkwrap generation plus its three assertions, testable without a publish"
      contains: "mos_generate_shrinkwrap"
    - path: "tests/fixtures/341-release-step-block-hashes.txt"
      provides: "Phase 341's own byte-level scope tripwire baseline, with a STEP_BLOCK_COUNT data line"
      contains: "STEP_BLOCK_COUNT"
    - path: "tests/test-341-release-shrinkwrap-gate.cjs"
      provides: "Return-code contract proof for the shrinkwrap gate, hooks injected, zero network"
    - path: "tests/test-341-marketplace-npm-source.cjs"
      provides: "Step 4 writes source.source/package/version and deletes source.ref and source.url"
  key_links:
    - from: "scripts/release.sh"
      to: "scripts/release-lib/shrinkwrap-gate.sh"
      via: "source at the former Step 6.7 slot"
      pattern: "release-lib/shrinkwrap-gate.sh"
    - from: "scripts/release.sh"
      to: "scripts/check-release-payload-ceiling.cjs"
      via: "Step 9.5 payload gate delegates to the D-08 runner"
      pattern: "check-release-payload-ceiling"
    - from: "scripts/doctor.cjs"
      to: "~/mindrian-marketplace/.claude-plugin/marketplace.json"
      via: "version-of-record-published leg (b) reads source.version"
      pattern: "source.version"
---

<objective>
Rewrite the release ceremony so the npm tarball is the distributed artifact, and do it in one plan
so the release train is never red between commits.

Purpose: D-06 is a subtraction. `release.sh` Step 6.7 (`:612-672`) force-adds `node_modules` into
the tagged release commit on a premise that expired on 2026-07-05, and Commit B (`:1286`) strips it
again. Deleting it is the whole fix for the 380 MB clone. But 341-RESEARCH found two blockers that
turn a clean deletion into a red tree: Step 9.5's own payload grep at `:750` matches the unanchored
pattern `release\.sh` and would refuse to publish a tarball whose `scripts/` directory ships, and
`tests/run-all-310.sh` legs 3 and 4 hard-pin release.sh's 28 step blocks and a non-comment
`exit 1` floor of 49 (measured 50 today, and Step 6.7's block holds 3 of them, so the count drops to
47). Both are Phase 310's scope tripwires working as designed. Both must move in the same commit as
the release.sh edit.

Output: the Step 4 / Step 6.7 / Step 9.5 surgery, an injectable shrinkwrap gate in
`scripts/release-lib/`, the doctor `source.version` comparison, the two rebaselined 310 tripwires,
a minted 341 fixture, and four zero-network tests. After this plan the next beta can be cut, which
is D-13 step 1's final requirement and the entry condition for the cold-install proof in plan
341-06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-CONTEXT.md
@.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-RESEARCH.md
@.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-PATTERNS.md
@.claude/includes/release-process.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite release.sh Steps 4, 6.7 and 9.5, and extract the shrinkwrap gate into release-lib</name>

  <files>scripts/release.sh, scripts/release-lib/shrinkwrap-gate.sh, tests/test-341-release-shrinkwrap-gate.cjs</files>

  <read_first>
    - `scripts/release.sh` lines 408-422 (Step 4, the `m.plugins[0].source.ref = 'v$NEW_VERSION'` writer), lines 612-672 (Step 6.7 in full: the expired 2026-05-21 premise comment, the `MOS_SKIP_VENDOR` escape hatch, `npm ci --omit=dev`, the `npm ls --omit=dev --depth=0` integrity probe, the `@modelcontextprotocol/sdk` plus `zod` presence loop, the `git add -f node_modules`, and its 3 non-comment `exit 1` statements), lines 708-795 (Step 9.5: the payload grep at :750, the blanket `node_modules/` ban, `MOS_TEST_DRY_RUN` at :761, the `@next` to `@latest` promotion at :765-775, and the recovery text), and lines 1275-1300 (the Commit-B `git rm -r --cached node_modules` block at approximately :1286).
    - `scripts/release-lib/verify-tag-push.sh` lines 1-52 in full - the Phase 310 injectable-hook idiom: the WHAT block, the exact RETURN CODES contract (0 success, 10 warn-and-continue, 1 fail-closed), the HOOK ENV VARS block resolved by name via `command -v`, and the four RULES (no `set -e`, no top-level side effects, safe to source more than once, safe under `set -u` with every expansion guarded `${VAR:-}`). Copy this contract shape exactly.
    - `scripts/release.sh` lines 93-135 - the Step 0 / Step 0.5 / Step 1 blocks, because Step 1 contains the dry-run preview text that run-all-310.sh leg 3 treats specially. Any preview line you add for the new Step 6.7 changes Step 1's block hash, which Task 2 must then handle as an AUTHORIZED delta.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-RESEARCH.md` sections "Pattern 1: One-line shrinkwrap generation, placed where the vendoring was" (the three assertions and the `NODE_ENV` / `omit` ruling), "Release Ceremony: What Actually Breaks" (the per-step change table), and "Pitfall 3".
    - `scripts/check-release-payload-ceiling.cjs` (created by plan 341-04) - `--check` semantics and exit codes, so Step 9.5 delegates rather than duplicating.
    - `.claude/includes/release-process.md` - the 5-way version consistency rule. Record 5 moves from `source.ref` pinned to the tag, to the marketplace `source.version` pin. This include is a CLAUDE.md `@include`, so update its record-5 wording in this task too if it names `source.ref`.
  </read_first>

  <action>
STEP 4 (`:408-422`). Rewrite the inline `node -e` marketplace writer so it produces D-01's exact
shape and leaves nothing stale behind:

- `m.plugins[0].version = '$NEW_VERSION'` stays.
- Replace the `source` object wholesale rather than mutating it: build a fresh object
  `{ source: 'npm', package: '@mindrian_os/cli', version: '$NEW_VERSION' }` and assign it. Building
  fresh is what guarantees the old `ref` and `url` keys are DELETED rather than left sitting beside
  the new `version`; a partial mutation is the failure mode 341-RESEARCH names explicitly.
- No `v` prefix on the version. npm versions carry none.
- Do NOT add a `registry` key. Omitting it means the user's own default registry is used; naming one
  would be a supply-chain configuration this repo does not need. Record that as a deliberate choice
  in a comment on the line above.
- Update the step header comment and the `echo` line so both name `source.version` instead of
  `source.ref`. Update the Step 4 header text itself: `# --- Step 4: Bump marketplace.json (version
  + npm source pinned to the exact version) ---`. Note that changing the header text changes the
  block KEY that run-all-310.sh leg 3 uses, which Task 2 handles as an authorized delta.

STEP 6.7 (`:612-672`). Delete the entire body and replace it with shrinkwrap generation that
delegates to a new sourceable library. Update the step header to
`# --- Step 6.7: Generate npm-shrinkwrap.json (dependency delivery for the npm source) ---`.
The body sources `scripts/release-lib/shrinkwrap-gate.sh` and calls `mos_generate_shrinkwrap
"$PLUGIN_DIR"`, mapping its return code onto the step's outcome with the same rollback the old step
performed on failure (`git checkout` of `.claude-plugin/plugin.json`, `package.json`, `CHANGELOG.md`
in `$PLUGIN_DIR` and `.claude-plugin/marketplace.json` in `$MARKETPLACE_DIR`, then `exit 1`).
`MOS_SKIP_VENDOR` is deleted with the step; grep the whole repo for it and remove every remaining
reference (it is only meaningful inside Step 6.7).

Create `scripts/release-lib/shrinkwrap-gate.sh` in the `verify-tag-push.sh` idiom:
- Header block: WHAT, the exact RETURN CODES contract (`0` = shrinkwrap generated and all three
  assertions passed; `1` = fail closed, the caller must hard-abort; there is no warn code here,
  because a missing lockfile in the tarball is not a warning), the HOOK ENV VARS block, and the four
  RULES verbatim in spirit (no `set -e`, no top-level side effects, safe to source twice, safe under
  `set -u`).
- Hook env vars, resolved by name via `command -v`, so a test can drive every branch without a real
  npm: `MOS_SHRINKWRAP_HOOK` (defaults to the real `npm shrinkwrap`), `MOS_PACK_PROBE_HOOK`
  (defaults to the real `npm pack --dry-run --json`). Both default to the real thing when unset, so
  production behavior is unchanged when nothing is injected.
- `mos_generate_shrinkwrap <plugin_dir>` does, in order:
  1. Run the shrinkwrap hook. On non-zero, echo `"  x npm shrinkwrap failed. package-lock.json is
     likely out of sync with package.json."` and return 1.
  2. Assert zero dev entries: parse `npm-shrinkwrap.json` with `node -e` and return 1 with the
     message `"  x npm-shrinkwrap.json contains dev-only packages; the loader would install them on
     every user machine."` if any `packages` value has `dev === true`. The loader passes NO `--omit`
     flag, so this is a real exposure, not hygiene.
  3. Assert the lockfile is IN the pack payload: run the pack-probe hook and return 1 with the
     message `"  x npm-shrinkwrap.json is NOT in the pack payload. Add it to package.json \"files\"."`
     plus the consequence line `"    Without it the loader skips the dependency install WITHOUT a
     log entry and both MCP servers fail to load."` This is the single highest-risk assertion in the
     phase and it appears here as well as in the harness policy and the standalone test, on purpose.
  4. `git -C "$plugin_dir" add npm-shrinkwrap.json` and return 0.
- Do NOT set `NODE_ENV=production` anywhere. `npm ci`'s `omit` default is `[]` unless `NODE_ENV` is
  production, the loader controls its own environment, and a shrinkwrap generated under
  `NODE_ENV=production` can omit metadata a later `npm ci` needs. The control is assertion 2, not
  the environment. State that reasoning in a comment.
- Use the release.sh step-body error style verbatim: `echo "  x <what failed>"`, then a recovery
  line, then the return.

COMMIT B (`:1286`). Delete the `git ls-files --error-unmatch node_modules` / `git rm -r --cached
node_modules` block entirely. There is nothing to untrack any more. Keep the rest of Step 7.5
untouched.

STEP 9.5 (`:750-757`). Replace the two greps:
- The `node_modules/` blanket ban is deleted. It is no longer correct: the tarball legitimately has
  no `node_modules`, and the ceiling is now expressed as a measurement rather than a string ban.
- The blacklist grep KEEPS `\.planning/`, `mcp-server-brain/` and `tests/` (all three are still
  correct) and DROPS the `docs/` and `release\.sh` alternatives, because `files` now negates
  `scripts/release.sh` and `scripts/release-lib` directly and `docs/` is simply not in the
  allowlist. Leaving the unanchored `release\.sh` alternative in place would refuse the publish
  outright.
- Add, immediately after the blacklist, a delegation to the D-08 runner:
  `node "$PLUGIN_DIR/scripts/check-release-payload-ceiling.cjs" --check` and `exit 1` on non-zero,
  with the release.sh error style. The runner is offline, so this adds no network to Step 9.5.
- Keep the `npm pack --dry-run` payload echo above it: an operator reviewing the payload before a
  live publish is a real control, not noise.
- Steps 5.5, 7, 8, 9.6b, 9.7, 9.8, 10 and 11 are UNTOUCHED. Step 9.6a is already off by default
  (`NO_MINISITE=1`, "RETIRED 2026-06-09", approximately `scripts/release.sh:98`); do not plan around
  it and do not edit it. Step 9.6b (mindrian-os.com FALLBACK_VERSION sync,
  approximately `:944-1101`) is live, its checkout exists at `$HOME/mindrian-website/website`, and
  it is independent of the artifact channel: leave it exactly as it is.

If `.claude/includes/release-process.md` states record 5 as `source.ref` pinned to the tag, update
that one line to name the marketplace `source.version` pin, and nothing else in that file.

Create `tests/test-341-release-shrinkwrap-gate.cjs` in the
`tests/test-310-verify-tag-push-lib.cjs` idiom: source the library in a bash subshell with hooks
injected, assert the return-code contract, ZERO network and ZERO real npm. Arms:
- shrinkwrap hook returns non-zero -> `mos_generate_shrinkwrap` returns 1 and the output contains
  `npm shrinkwrap failed`.
- shrinkwrap hook succeeds, planted `npm-shrinkwrap.json` contains a `dev: true` entry -> returns 1
  with output containing `dev-only packages`.
- shrinkwrap hook succeeds, pack probe reports a payload WITHOUT `npm-shrinkwrap.json` -> returns 1
  with output containing `NOT in the pack payload`.
- all three pass -> returns 0.
- the library is safe to source twice and safe under `set -u` (source it twice inside `set -u` and
  assert no error).
  </action>

  <verify>
    <automated>bash -n scripts/release.sh &amp;&amp; bash -n scripts/release-lib/shrinkwrap-gate.sh &amp;&amp; node tests/test-341-release-shrinkwrap-gate.cjs &amp;&amp; grep -c 'MOS_SKIP_VENDOR' scripts/release.sh | grep -q '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/release.sh` and `bash -n scripts/release-lib/shrinkwrap-gate.sh` both exit 0.
    - Step 6.7's vendoring is gone: `grep -c 'git add -f node_modules' scripts/release.sh` returns 0, `grep -c 'MOS_SKIP_VENDOR' scripts/release.sh` returns 0, `grep -c 'npm ci --omit=dev' scripts/release.sh` returns 0, and `grep -rc 'MOS_SKIP_VENDOR' scripts/ tests/ docs/ .claude/` shows no remaining live reference (a historical CHANGELOG mention is acceptable and must be named in the SUMMARY).
    - The Commit-B untrack block is gone: `grep -c 'git rm -r --cached' scripts/release.sh` returns 0.
    - The step header count dropped by zero, not by one: `grep -c '^# --- Step' scripts/release.sh` returns 28 (Step 6.7 was REPLACED, not deleted). If your implementation removed the header instead, the count is 27 and Task 2's fixture and literals must reflect that; record which you did and why in the SUMMARY.
    - Step 4 writes the npm source shape: `grep -c "source: 'npm'" scripts/release.sh` is at least 1, `grep -c "@mindrian_os/cli" scripts/release.sh` is at least 1, and `grep -c "source.ref = 'v" scripts/release.sh` returns 0.
    - No registry key is written: `grep -c "registry:" scripts/release.sh` returns 0.
    - Step 9.5's blacklist is correct: `grep -c 'release\\\\.sh' scripts/release.sh` returns 0 inside the payload grep, `grep -c 'node_modules/' scripts/release.sh` returns 0 in the Step 9.5 region, and `grep -c 'check-release-payload-ceiling' scripts/release.sh` is at least 1. `.planning/`, `mcp-server-brain/` and `tests/` remain in the blacklist.
    - The shrinkwrap library honors the injectable contract: `grep -c 'MOS_SHRINKWRAP_HOOK' scripts/release-lib/shrinkwrap-gate.sh` is at least 1, `grep -c 'MOS_PACK_PROBE_HOOK' scripts/release-lib/shrinkwrap-gate.sh` is at least 1, and `grep -c 'set -e' scripts/release-lib/shrinkwrap-gate.sh` returns 0.
    - `node tests/test-341-release-shrinkwrap-gate.cjs` exits 0 with `FAIL=0` and its five arms all run.
    - No test in this task performs a real publish, push or registry call: `grep -Ev '^\s*(#|\*|//)' tests/test-341-release-shrinkwrap-gate.cjs | grep -Ec 'npm publish|git push|npm view|https?://'` returns 0.
    - `NODE_ENV=production` appears nowhere: `grep -rc 'NODE_ENV=production' scripts/release.sh scripts/release-lib/shrinkwrap-gate.sh` reports 0 for each.
    - Steps 9.6a and 9.6b are byte-identical to their pre-edit form: `git diff scripts/release.sh | grep -c '9\.6'` returns 0.
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/release.sh scripts/release-lib/shrinkwrap-gate.sh tests/test-341-release-shrinkwrap-gate.cjs` reports 0 for each file.
  </acceptance_criteria>

  <done>Step 4 writes an exact npm pin with no residual keys, Step 6.7's slot generates and asserts the shrinkwrap through an injectable library with a documented return-code contract, the Commit-B untrack block and MOS_SKIP_VENDOR are gone, Step 9.5 delegates to the ceiling runner, and the shrinkwrap gate's every branch is proved without a single network call.</done>
</task>

<task type="auto">
  <name>Task 2: Rebaseline the Phase 310 tripwires and mint the Phase 341 fixture, in the same commit</name>

  <files>tests/fixtures/310-release-step-block-hashes.txt, tests/fixtures/341-release-step-block-hashes.txt, tests/run-all-310.sh</files>

  <read_first>
    - `tests/fixtures/310-release-step-block-hashes.txt` lines 1-20 - the fixture header's own REPRODUCIBLE generating command (the `mapfile` / `sed` / `sha256sum` loop) and the format contract `<sha256>  <header line text or pinned label>`, plus the `PREAMBLE(1-79)` and `EXIT1_NONCOMMENT_COUNT` pinned-label lines. Use that exact command to regenerate; do not write a new one.
    - `tests/run-all-310.sh` lines 58-175 - leg 3 in full (the authorized-delta comment block naming Step 5.5 and Step 1 and WHY each is allowed to differ, the header-keyed split, the set-equality check in both directions, and the `if [[ "${#cur_headers[@]}" -ne 28 ]]` literal) and leg 4 (`grep -v '^[[:space:]]*#' | grep -c 'exit 1'` against the fixture's `EXIT1_NONCOMMENT_COUNT` baseline, currently 49, measured 50 on today's tree).
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-RESEARCH.md` "Pitfall 4: The scope tripwires nobody remembered" and "Blocker 2" - the measured before-and-after: 28 headers, 50 `exit 1`, Step 6.7's block holding 3 of them, so the count drops to 47 against a floor of 49.
    - `scripts/release.sh` as edited by Task 1 - you are baselining the POST-edit file.
  </read_first>

  <action>
This task exists so the release train is never knowingly red. It must be committed TOGETHER with
Task 1's release.sh edit or in the immediately following commit within the same execution, and the
SUMMARY must state which.

1. Measure first, decide second. Run, on the POST-Task-1 `scripts/release.sh`:
   - `grep -c '^# --- Step' scripts/release.sh` (the header count)
   - `grep -v '^[[:space:]]*#' scripts/release.sh | grep -c 'exit 1'` (the non-comment gate count)
   Record both literally in the SUMMARY next to research's pre-edit measurements (28 and 50).

2. Regenerate `tests/fixtures/310-release-step-block-hashes.txt` using the command documented in
   its own header, verbatim. Preserve the header comment block including the generating command and
   the format contract, update its `Generated:` timestamp, and preserve the `PREAMBLE(1-79)` and
   `EXIT1_NONCOMMENT_COUNT` pinned-label lines with their NEW values. Add a short note to the header
   explaining that Phase 341 rebaselined it, naming the authorized regions (Step 4's header and
   body, Step 6.7's header and body, Step 9.5's payload gate, and the Commit-B untrack removal
   inside Step 7.5).

3. Update `tests/run-all-310.sh`:
   - Leg 3's `-ne 28` literal becomes the measured header count. If the count is unchanged at 28,
     leave the literal and say so in the SUMMARY.
   - Leg 3's authorized-delta comment block gains the Phase 341 regions, written in the same voice
     as the existing Step 5.5 / Step 1 entries: name each block expected to differ and WHY. Since
     the fixture is being regenerated from the post-edit file, those blocks will match the new
     fixture and need no special-casing; the comment is the record of what changed and when, which
     is the thing a future reader needs.
   - Leg 4's floor: the fixture's `EXIT1_NONCOMMENT_COUNT` becomes the newly measured count. If the
     new count is LOWER than 49, that is expected (Step 6.7 held 3 `exit 1` statements and its
     replacement holds fewer); rebaseline honestly to the measured value rather than padding the
     replacement with gates that exist only to satisfy a counter. Record the before and after in
     the SUMMARY and in the fixture header.
   - Do NOT weaken leg 3's set-equality assertion or leg 4's `-lt` comparison direction. The floor
     stays a floor; only its value moves.

4. Mint `tests/fixtures/341-release-step-block-hashes.txt` with the SAME generating command and the
   SAME format, so Phase 341's own authorized regions are pinned the way 310's were. Its header must
   carry: the generating command verbatim, the format contract, the `Generated:` timestamp, a
   `PREAMBLE(1-79)` line, an `EXIT1_NONCOMMENT_COUNT` line, and a `STEP_BLOCK_COUNT` data line
   holding the measured header count. `tests/run-all-341.sh`'s step-block tripwire (written in plan
   341-01) reads `STEP_BLOCK_COUNT` from this file rather than carrying a literal, so this line is
   load-bearing and its label must match exactly.

5. Run both aggregators and confirm green. `bash tests/run-all-310.sh` must report FAIL=0 with its
   documented 2 SKIPs. `bash tests/run-all-341.sh` must report FAIL=0 with EXPECTED-RED=0 and its
   341 step-block leg now PASSING rather than SKIPPED.
  </action>

  <verify>
    <automated>bash tests/run-all-310.sh &amp;&amp; bash tests/run-all-341.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-all-310.sh` exits 0 and its summary reports `FAIL=0`.
    - `bash tests/run-all-341.sh` exits 0 and its summary reports `FAIL=0` and `EXPECTED-RED=0`.
    - `tests/fixtures/341-release-step-block-hashes.txt` exists and carries all five header elements: `for s in 'Generating command' 'PREAMBLE(1-79)' 'EXIT1_NONCOMMENT_COUNT' 'STEP_BLOCK_COUNT' 'Generated:'; do grep -qF "$s" tests/fixtures/341-release-step-block-hashes.txt || echo "MISSING $s"; done` prints nothing.
    - The 341 fixture's `STEP_BLOCK_COUNT` equals the live count: `test "$(grep STEP_BLOCK_COUNT tests/fixtures/341-release-step-block-hashes.txt | awk '{print $1}')" = "$(grep -c '^# --- Step' scripts/release.sh)"`.
    - The 310 fixture was regenerated, not hand-edited: every data line's hash matches a fresh run of the fixture's own generating command against `scripts/release.sh`. Demonstrate by re-running the command into a temp file and `diff`ing the data lines; the diff must be empty.
    - Leg 3's set-equality assertion is intact: `grep -c 'block present in fixture but MISSING' tests/run-all-310.sh` returns 1 and `grep -c 'block added or renamed' tests/run-all-310.sh` returns 1.
    - Leg 4 is still a floor: `grep -c '\-lt "\$baseline"' tests/run-all-310.sh` returns 1.
    - The authorized-delta comment names Phase 341: `grep -c '341' tests/run-all-310.sh` is at least 1.
    - `grep -c "$(printf '\xe2\x80\x94')" tests/fixtures/310-release-step-block-hashes.txt tests/fixtures/341-release-step-block-hashes.txt tests/run-all-310.sh` reports 0 for each file.
  </acceptance_criteria>

  <done>Both scope tripwires are rebaselined from the post-edit release.sh using their own documented generating command, the 341 fixture exists with a machine-read STEP_BLOCK_COUNT, no assertion was weakened, and both aggregators are green in the same execution as the release.sh edit.</done>
</task>

<task type="auto">
  <name>Task 3: Move doctor's version-of-record to source.version and extend the sandboxed release-gate suite</name>

  <files>scripts/doctor.cjs, tests/test-release-bump-tag-and-publish-gates.cjs, tests/test-341-marketplace-npm-source.cjs, tests/test-341-version-of-record-source-version.cjs</files>

  <read_first>
    - `scripts/doctor.cjs` lines 847-895 - the `version-of-record-published` point in full: its `label` string (which names `source.ref` today), the `DOCTOR_TEST_FAIL_POINT` injection at the top, leg (a) the git tag check, leg (b) `mp.plugins[0].source.ref !== 'v' + ver`, and leg (c) the `npm view` call carrying the comment "THIS IS THE ONE NETWORK CALL in this point". Legs (a) and (c) survive unchanged.
    - `tests/test-release-bump-tag-and-publish-gates.cjs` lines 1-70 - the test architecture header: each test scaffolds a sandboxed plugin plus marketplace plus minisite repo with `mkdtempSync`, `git init`, seeds, and a bare clone for a fake origin remote; an HTTP mock for the minisite poll; an `npx` shim on PATH; release.sh invoked with `PLUGIN_DIR` / `MARKETPLACE_DIR` / `MINISITE_DIR` / `MINDRIAN_MINISITE_URL` / `RELEASE_TEST_MODE`. Also find cases 10 and 11, which regex the RETIRED package name `@mindrian_os/install`; both were already red before Phase 310 and were explicitly deferred.
    - `~/mindrian-marketplace/.claude-plugin/marketplace.json` - the LIVE catalog, a separate git repo (`jsagir/mindrian-marketplace`). Nothing in this dev repo can change it; the flip happens only when release.sh runs. Read it to confirm the current `{"source":"url","url":"...","ref":"v..."}` shape, and do NOT edit it.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-RESEARCH.md` "Doctor `version-of-record-published`" and "Runtime State Inventory" row "Live service config" - the code edit versus data migration distinction: the release.sh Step 4 rewrite is a code edit; the actual marketplace.json flip is a data migration that happens on the first post-341 release.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-RESEARCH.md` Open Question 4 - the recommendation to fix cases 10 and 11 here, since this is the phase that makes the npm package name load-bearing.
  </read_first>

  <action>
1. `scripts/doctor.cjs`, `version-of-record-published`. Change leg (b) only:
   - Read `mp.plugins[0].source.version` instead of `source.ref`.
   - Compare it to `ver` with NO `v` prefix.
   - The failure `finding` text must name both the found and the expected value, as it does today.
   - Add a THIRD assertion to leg (b): the source object must have `source === 'npm'` and `package
     === '@mindrian_os/cli'`, and must NOT carry a residual `ref` or `url` key. A stale `ref` sitting
     beside a correct `version` is exactly the half-migrated state D-01 warns about, and this point
     is the only thing that will ever notice it.
   - Update the point's `label` string: it names `source.ref` today and must name `source.version`.
   - Legs (a) and (c) are untouched. Keep the "THIS IS THE ONE NETWORK CALL in this point" comment
     verbatim; it is now doubly meaningful, since the npm registry IS the distribution channel
     rather than a side publish.
   - Keep `applies_to: ['full']` and `severity: 'blocker'` as they are.

2. `tests/test-341-version-of-record-source-version.cjs`, hermetic, zero network. Because leg (c)
   makes a real `npm view` call, do NOT run the whole point. Instead test the comparison logic
   through the doctor's own test seam: run `node scripts/doctor.cjs --acceptance --pre-tag` (which
   excludes this `full`-only point) to prove nothing regressed, and separately assert the source
   shape by parsing a FIXTURE marketplace JSON. Arms:
   - A fixture marketplace with `{source:'npm', package:'@mindrian_os/cli', version:'2.0.0-beta.31'}`
     and `ver = '2.0.0-beta.31'` is accepted.
   - The same with `version: 'v2.0.0-beta.31'` is rejected (the `v` prefix is wrong for npm).
   - A fixture carrying BOTH a correct `version` and a residual `ref` is rejected.
   - A fixture still on `{source:'url', ref:'v...'}` is rejected.
   If the comparison logic is not reachable as an exported function, extract it into a small pure
   helper inside `scripts/doctor.cjs` (exported for hermetic tests, the repo's stated convention)
   and have both the point and the test call it. One authority, not two.

3. `tests/test-341-marketplace-npm-source.cjs`, sandboxed and zero-network, in the
   `test-release-bump-tag-and-publish-gates.cjs` idiom: scaffold a temp marketplace repo with the
   OLD `{source:'url', url:'...', ref:'v2.0.0-beta.29'}` shape, run release.sh's Step 4 logic
   against it under `RELEASE_TEST_MODE` and `MOS_TEST_DRY_RUN=1` with `MARKETPLACE_DIR` pointed at
   the sandbox, and assert the resulting JSON has exactly `source`, `package` and `version` under
   `plugins[0].source`, with no `ref` and no `url` key, no `v` prefix on the version, and no
   `registry` key. If driving the full release.sh is too heavy for one arm, drive it far enough to
   reach Step 4 and abort deliberately after it; state in the test header which approach you used
   and why.

4. `tests/test-release-bump-tag-and-publish-gates.cjs`. Two changes:
   - Fix cases 10 and 11: replace the retired `@mindrian_os/install` regex with `@mindrian_os/cli`.
     341-RESEARCH Open Question 4 recommends fixing them here because this is the phase that makes
     the npm package name load-bearing, and inheriting two known-red cases inside the phase that is
     supposed to PROVE the release train would be dishonest. If after the fix either case still
     fails for a different, genuinely out-of-scope reason, do NOT paper over it: record the failure
     and its root cause in the SUMMARY and in the file's own deferred note.
   - Add cases in the file's existing idiom for: Step 6.7's slot generating a shrinkwrap (drive it
     with the `MOS_SHRINKWRAP_HOOK` and `MOS_PACK_PROBE_HOOK` seams so no real npm runs), and Step
     9.5's payload gate PASSING on the new whitelist under `MOS_TEST_DRY_RUN=1`.
   - Every added case obeys the file's stated architecture: `mkdtempSync` sandboxes, `git init`,
     bare-clone fake origins, `RELEASE_TEST_MODE`, and zero real git push, npm publish or registry
     call.
  </action>

  <verify>
    <automated>node tests/test-341-version-of-record-source-version.cjs &amp;&amp; node tests/test-341-marketplace-npm-source.cjs &amp;&amp; node tests/test-release-bump-tag-and-publish-gates.cjs &amp;&amp; node scripts/doctor.cjs --acceptance --pre-tag</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "source.ref" scripts/doctor.cjs` returns 0 in the `version-of-record-published` region, and `grep -c "source.version" scripts/doctor.cjs` is at least 1.
    - The point's label was updated: `grep -c "marketplace source.ref pinned" scripts/doctor.cjs` returns 0.
    - The residual-key assertion exists: `grep -Ec "source\.(ref|url)" scripts/doctor.cjs` is at least 1 in a rejection context (record the line numbers in the SUMMARY).
    - The one network call survives: `grep -c "THIS IS THE ONE NETWORK CALL" scripts/doctor.cjs` returns 1.
    - `node tests/test-341-version-of-record-source-version.cjs` exits 0 with `FAIL=0` and all four fixture arms run.
    - `node tests/test-341-marketplace-npm-source.cjs` exits 0 with `FAIL=0`, and after the run the LIVE `~/mindrian-marketplace/.claude-plugin/marketplace.json` is unmodified (`cd ~/mindrian-marketplace && git status --porcelain` prints nothing).
    - `node tests/test-release-bump-tag-and-publish-gates.cjs` exits 0, OR its only failures are named, root-caused and recorded in the SUMMARY as genuinely out of scope. `grep -c "@mindrian_os/install" tests/test-release-bump-tag-and-publish-gates.cjs` returns 0.
    - `node scripts/doctor.cjs --acceptance --pre-tag` exits 0.
    - Zero network in the new tests: `grep -Ev '^\s*(#|\*|//)' tests/test-341-marketplace-npm-source.cjs tests/test-341-version-of-record-source-version.cjs | grep -Ec 'npm publish|npm view|git push|https?://[a-z]'` returns 0.
    - `bash tests/run-all-341.sh` reports `FAIL=0` and `EXPECTED-RED=0`; `bash tests/run-all-310.sh` reports `FAIL=0`.
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/doctor.cjs tests/test-341-marketplace-npm-source.cjs tests/test-341-version-of-record-source-version.cjs tests/test-release-bump-tag-and-publish-gates.cjs` reports 0 for each file.
  </acceptance_criteria>

  <done>The doctor compares the marketplace `source.version` with no `v` prefix, rejects a half-migrated source object carrying a residual `ref` or `url`, keeps its single network call, and the sandboxed release-gate suite proves Step 4, the shrinkwrap slot and Step 9.5 without one real push, publish or registry call.</done>
</task>

</tasks>

## Artifacts this phase produces

### This plan

| Artifact | Kind | Notes |
|----------|------|-------|
| `scripts/release-lib/shrinkwrap-gate.sh` | new file | `mos_generate_shrinkwrap <plugin_dir>`; return codes 0 and 1; hooks `MOS_SHRINKWRAP_HOOK`, `MOS_PACK_PROBE_HOOK`. Not shipped in the tarball (`!scripts/release-lib`). |
| `scripts/release.sh` Step 4 | changed step | Writes `{source:'npm', package:'@mindrian_os/cli', version:'<exact>'}`, rebuilt fresh so `ref` and `url` are deleted. No `registry` key. |
| `scripts/release.sh` Step 6.7 | replaced step | Vendoring gone; shrinkwrap generation plus three assertions. |
| `scripts/release.sh` Step 9.5 | changed step | `node_modules/` ban and the `docs/` plus `release.sh` blacklist alternatives removed; delegates to `check-release-payload-ceiling.cjs --check`. |
| `scripts/doctor.cjs` `version-of-record-published` | changed point | Compares `source.version`; rejects residual `ref` or `url`; one network call preserved. |
| `tests/fixtures/341-release-step-block-hashes.txt` | new fixture | Carries `STEP_BLOCK_COUNT`, read by `tests/run-all-341.sh`. |
| `tests/fixtures/310-release-step-block-hashes.txt` | rebaselined | Regenerated by its own documented command. |
| `tests/test-341-release-shrinkwrap-gate.cjs`, `tests/test-341-marketplace-npm-source.cjs`, `tests/test-341-version-of-record-source-version.cjs` | new tests | All zero-network. |
| `MOS_SKIP_VENDOR` | retired env var | Deleted with Step 6.7. |

### Phase-wide (for reference)

New files: `npm-shrinkwrap.json`, `data/harness-policies/release-payload-ceiling.json`,
`data/harness-policies/registry-drift.json`, `scripts/check-release-payload-ceiling.cjs`,
`scripts/check-registry-drift.cjs`, `scripts/release-lib/shrinkwrap-gate.sh`,
`scripts/migrate-legacy-install-location.cjs`, `scripts/collect-cold-install-evidence.cjs`,
`lib/core/eureka-deps-resolver.cjs`, `lib/core/eureka/eureka-enable.cjs`,
`docs/install/COLD-INSTALL-PROOF-341.md`, `docs/install/INSTALL-OVERHAUL-341.md`,
`tests/fixtures/341-release-step-block-hashes.txt`,
`tests/fixtures/341-registry-drift-baseline.json`, `tests/run-all-341.sh`, the
`tests/test-341-*.cjs` set.
New CLI surfaces: `/mos:eureka enable`, `doctor --fix eureka`,
`node scripts/check-release-payload-ceiling.cjs --check`,
`node scripts/check-registry-drift.cjs --check`,
`node scripts/migrate-legacy-install-location.cjs --apply|--dry-run`,
`node scripts/collect-cold-install-evidence.cjs --json`.
New policy ids: `release-payload-ceiling`, `registry-drift`. New doctor layer id: `model_installed`.
New env seams: `CHECK_PAYLOAD_CEILING_ROOT`, `CHECK_REGISTRY_DRIFT_BASELINE`,
`MINDRIAN_EUREKA_DEPS_ROOT`, `MOS_LEGACY_INSTALL_HOME`, `MOS_SHRINKWRAP_HOOK`,
`MOS_PACK_PROBE_HOOK`.
Retired: `MOS_SKIP_VENDOR`, release.sh Step 6.7's vendoring, the Commit-B untrack block,
`SHA_DIFFERS_INVERSION_HOTFIX`, `~/.mindrian/post-update-restart-pending`, `install.sh`, doctor
classes A and H, the legacy-topology halves of classes I and J.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release ceremony -> npm registry | Step 9.5 is the only publish. It runs after Commit A so the working tree reads the release version. |
| marketplace catalog -> every user's loader | `plugins[0].source` is a supply-chain configuration change. It is the single line that decides what code every install fetches. |
| release-gate test -> the world | Every test in this phase must be provably offline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-341-03 | Tampering | marketplace `source` pin | mitigate | Exact version, never a range and never a dist-tag. Measured reasons: on semver 7.8.0 `^2.0.0-beta.30` rejects `2.0.1-beta.1` and `2.1.0-beta.1`, so a caret silently stops resolving; and Step 9.5 promotes `@next` to `@latest`, so a dist-tag would auto-push every publish to every user. Asserted by `tests/test-341-marketplace-npm-source.cjs` and by the doctor point. |
| T-341-07 | Spoofing | dependency confusion via a registry override | mitigate | No `registry` key is written into the marketplace source, so the user's own default registry is used. Stated as a deliberate choice in a code comment and asserted by `grep -c "registry:" scripts/release.sh` returning 0. |
| T-341-17 | Tampering | a half-migrated source object (`version` written, `ref` left behind) | mitigate | Step 4 rebuilds the `source` object wholesale rather than mutating it, and the doctor point rejects any residual `ref` or `url`. |
| T-341-01 | Information disclosure | Step 9.5 publishing a payload containing dev state or secrets | mitigate | The blacklist keeps `.planning/`, `mcp-server-brain/` and `tests/`, and now also delegates to `check-release-payload-ceiling.cjs --check`, which asserts the full denylist plus the ceilings plus the shrinkwrap. Two independent controls, both blocking. |
| T-341-16 | Denial of service | publishing a tarball with no lockfile | mitigate | `mos_generate_shrinkwrap` assertion 3 fails the release BEFORE the tag is pushed. Third independent assertion of the same fact, after the standalone tripwire and the harness policy. |
| T-341-18 | Repudiation | a release-gate test that quietly performs a real publish or push | mitigate | Every new test drives release.sh under `RELEASE_TEST_MODE` plus `MOS_TEST_DRY_RUN=1`, or drives the injectable hooks directly. Acceptance criteria grep the test sources for `npm publish`, `npm view`, `git push` and `https://` outside comments and require 0. |
| T-341-SC | Tampering | npm / pip / cargo installs | accept | This plan installs nothing. `slopcheck check` only, NEVER `slopcheck install`, inside this repo. |
</threat_model>

<verification>
Run from `/home/jsagi/dev/MindrianOS-Plugin` (WORKSPACE GUARD: confirm with `pwd`).

1. `bash -n scripts/release.sh` and `bash -n scripts/release-lib/shrinkwrap-gate.sh` exit 0.
2. `node tests/test-341-release-shrinkwrap-gate.cjs`,
   `node tests/test-341-marketplace-npm-source.cjs` and
   `node tests/test-341-version-of-record-source-version.cjs` each exit 0 with `FAIL=0`.
3. `node tests/test-release-bump-tag-and-publish-gates.cjs` exits 0, or its only failures are
   named and root-caused in the SUMMARY as out of scope.
4. `bash tests/run-all-310.sh` exits 0 with `FAIL=0`.
5. `bash tests/run-all-341.sh` exits 0 with `FAIL=0` and `EXPECTED-RED=0`.
6. `node scripts/doctor.cjs --acceptance --pre-tag` exits 0.
7. `node scripts/check-release-payload-ceiling.cjs --check` exits 0.
8. `bash scripts/release.sh --dry-run` (or the repo's documented dry-run invocation) prints the step
   preview and makes no commit, tag, push or publish. Confirm with `git status --porcelain` clean
   and `git tag --list | tail -3` unchanged.
9. `cd ~/mindrian-marketplace && git status --porcelain` prints nothing: no test touched the live
   catalog.
</verification>

<success_criteria>
- The release ceremony produces an npm-source pin at an exact version with no residual keys (D-01).
- Step 6.7's vendoring is gone and its slot generates and triple-asserts the shrinkwrap through an
  injectable, separately-tested library (D-04, D-06).
- Step 9.5 expresses the ceiling as a measurement via the D-08 runner rather than a string ban,
  while keeping the still-correct blacklist entries (D-06, D-08).
- The doctor's version-of-record leg compares `source.version` and catches a half-migrated source
  object (D-06).
- Both Phase 310 scope tripwires are rebaselined from the post-edit file using their own documented
  generating command, in the same execution as the edit, with no assertion weakened.
- Every new release-gate test is provably offline.
- The tree is ready for the first post-341 release, which is D-13 step 1's exit condition and plan
  341-06's entry condition.
- No em-dash in any file this plan touches.
</success_criteria>

<output>
Create `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-05-SUMMARY.md` when done.

Record in the SUMMARY: the post-edit `^# --- Step` header count and non-comment `exit 1` count next
to research's 28 and 50; whether Step 6.7's header was replaced or removed and why; the before and
after `EXIT1_NONCOMMENT_COUNT`; every remaining `MOS_SKIP_VENDOR` reference in the tree and why each
is acceptable; the outcome of the `test-release-bump-tag-and-publish-gates.cjs` cases 10 and 11 fix;
the line numbers of the doctor's residual-key rejection; and an explicit confirmation that
`~/mindrian-marketplace` is unmodified.

Also record the OPERATOR HANDOFF for the first post-341 release: the marketplace `source` flip is a
DATA migration that happens only when `release.sh` runs; nothing in this repo changes the live
catalog. Before that run, re-verify Claude Code's loader constants against the installed binary if
it has moved past 2.1.266 (the 60,000 ms dependency-install timeout, the lockfile table order, and
the entry and byte ceiling constants), per 341-RESEARCH's "Valid until" note.

Commit convention: one atomic commit per task, never `git add -A`. Tasks 1 and 2 MUST land in the
same execution and their commits must be adjacent, because the release.sh edit and the tripwire
rebaseline are two halves of one change. Every commit message ends with the two trailer lines this
repo uses (`Co-Authored-By:` and `Claude-Session:`).
</output>
