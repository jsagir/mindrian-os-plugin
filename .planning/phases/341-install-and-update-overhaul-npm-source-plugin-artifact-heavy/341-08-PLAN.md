---
phase: 341-install-and-update-overhaul-npm-source-plugin-artifact-heavy
plan: 08
type: execute
wave: 8
depends_on: ["341-07"]
files_modified:
  - commands/update.md
  - scripts/check-version-and-sha.cjs
  - scripts/post-update-activation.cjs
  - scripts/sessionstart-post-update-preflight.cjs
  - scripts/session-verify-probe.cjs
  - scripts/check-session-start-verification.cjs
  - scripts/check-eureka-optin-once.cjs
  - scripts/first-install-router.cjs
  - scripts/context-monitor
  - scripts/doctor.cjs
  - hooks/hooks.json
  - lib/core/session-verify-state.cjs
  - lib/core/eureka/eureka-enable.cjs
  - lib/mcp/tools/gate.cjs
  - lib/mcp/tools/room.cjs
  - lib/statusline/cockpit-signals.cjs
  - data/harness-policies/_schema.json
  - data/harness-policies/session-start-verification.json
  - data/harness-policies/eureka-optin-once.json
  - data/harness-manifest.json
  - tests/test-mos-update-activation-gap.cjs
  - tests/test-update-restart-cue.cjs
  - tests/test-341-update-collapsed.cjs
  - tests/test-341-session-verify-state.cjs
  - tests/test-341-session-start-tier.cjs
  - tests/test-341-session-start-verify-hook.cjs
  - tests/test-341-verify-insight-delivery.cjs
  - tests/test-341-eureka-optin-once.cjs
  - tests/test-341-eureka-optin-card.cjs
  - tests/run-all-341.sh
autonomous: true
requirements: [D-07, D-07a, D-07b, D-13]
canon_parts: [7, 8, 11, 12]

must_haves:
  truths:
    - "Per D-07 /mos:update is four steps: Check, Confirm, loader, Verify. Fetch and Swap have no plugin-side owner any more; the Claude Code loader owns both end to end."
    - "Per D-07 the Step 6 stale-path migrator and the Step 7 atomic activator are deleted, along with the ~/.mindrian post-update touch-file that the activator wrote."
    - "Per D-07 the restart banner fires ONLY on a real swap, because the slash-command registry is built once at session start, and it drops entirely on UP_TO_DATE."
    - "Per D-07 the SHA leg is retired: published npm versions are immutable, so an in-version hotfix is a new version, and the api.github.com tag-ref call goes with it."
    - "Per D-07a (a) the post-update signal is a VERSION CHANGE, not a touch-file: installed_plugins.json's registered version compared against a last-seen marker under ~/.mindrian/. The loader writes installed_plugins.json, so it cannot forget to set the signal. A fresh install with no marker counts as changed, so the FIRST session after an install verifies too."
    - "Per D-07a (b) the first session after an install or update verifies itself with a BOUNDED, OFFLINE subset of doctor acceptance points that already exist, inside the existing 12 second SessionStart hook budget. Full doctor --all stays opt-in."
    - "Per D-07a (c) the statusline carries ONE segment: version plus glyph. A check mark when the bounded set passed, a warning glyph plus the failing count when it did not. The detail is Larry's first line, never the segment."
    - "Per D-07a (d) Theo health is probed AFTER the local set, DETACHED, so session start never blocks on the network, reusing the shipped class M brain-smoke rather than a second probe. A keyless or unreachable session says 'Theo not reachable, verification is local-only' and never imitates a reachable one (Decision #8)."
    - "Per D-07a (e) ONE insight is delivered on the first turn after the verification line, once per install or update and never per session. Only a GENERIC handle goes up (the active room's problem type, or nothing when no room is bound); the nugget comes down; Larry frames it as one question or nudge. Canon Part 8 holds: no room content crosses the wire."
    - "Per D-07a (f) the whole behavior is declared as a harness policy in a NEW session-start tier of data/harness-policies/, rung logged, with an offline runner and a test seam."
    - "Per D-07a the SessionStart hook is CONVERTED, not replaced: scripts/sessionstart-post-update-preflight.cjs stays the single registered host and the hook count does not grow. Its Part 7 F8 stale-registry restart cue survives untouched."
    - "Per D-07b (b) the Eureka opt-in card fires only when all three preconditions hold together: the embedding stack is NOT installed (the wave-2 resolver lib/core/eureka-deps-resolver.cjs finds nothing under ~/.mindrian/eureka-deps AND the class S L5 model_installed advisory is not ok), there is no standing answer for this version, and the D-07a version-change trigger fired."
    - "Per D-07b (a) and (c) the card rides the SAME 267.2 router branch as the D-07a insight and there is no second delivery mechanism. It is a real card on every card-capable surface: AskUserQuestion on the CLI, the gate_render card on Desktop and Cowork, never an ASCII box (SEED-021), never a silent download. The copy is locked verbatim and the three options are Install now, Not now, Never ask again."
    - "Per D-07b (d) the re-ask rule is four-state and locked: installed never asks; declined is silent for the life of that version and re-asks exactly once on the next version change; never is permanent until the navigator runs /mos:eureka enable, which clears it and records installed; and when the 267.2 first-install reward owns the first turn the card defers to the next turn of the SAME session, never a later session."
    - "Per D-07b (e) there is ONE state authority: lib/core/session-verify-state.cjs carries eureka_optin { choice, version, at } inside the same marker under ~/.mindrian/, and no second marker file exists anywhere."
    - "Per D-07b (f) the behavior is declared as an eureka-optin-once harness policy in the session-start tier, rung logged at runtime, with an offline runner that drives the six named fixture states through the one decision function and asserts exact fire or no-fire; the same runner is an always-green leg in tests/run-all-341.sh so the dev side is blocking; and a doctor line in the --session-start tier reports the current opt-in state honestly."
    - "The command keeps its Canon Part 11 frontmatter contract intact: name, connector.excluded with its reason, hitl_shape and hitl_why all survive the collapse unchanged."
    - "Per D-13 step 4 this lands only after the cold-install proof and after the one-install-location work, so the loader is proven to own Fetch and Swap before the plugin stops trying to."
  artifacts:
    - path: "commands/update.md"
      provides: "The collapsed four-step update flow"
      contains: "### Step 4"
    - path: "lib/core/session-verify-state.cjs"
      provides: "The ONE contract for the last-seen version marker, the verification result, the statusline segment string, and (D-07b e) the eureka_optin record with its pure decision function"
      exports: ["readVerifyState", "writeVerifyState", "mergeVerifyState", "verifyDir", "statePath", "detectVersionChange", "buildStatuslineSegment", "shortVersionLabel", "shouldAskEurekaOptin", "recordEurekaOptin", "buildEurekaOptinCard", "buildEurekaOptinInstruction", "resolveOptinVersion", "EUREKA_OPTIN_COPY", "EUREKA_OPTIN_OPTIONS", "EUREKA_OPTIN_CHOICES", "EUREKA_OPTIN_CHOICE_BY_OPTION_ID", "EUREKA_OPTIN_GATE_PREFIX"]
      min_lines: 260
    - path: "scripts/session-verify-probe.cjs"
      provides: "The detached child: full session-start tier including the wire point, Theo verdict, and the one generic-handle nugget"
      min_lines: 160
    - path: "scripts/check-session-start-verification.cjs"
      provides: "The offline harness-policy runner for the session-start tier"
      min_lines: 90
    - path: "data/harness-policies/session-start-verification.json"
      provides: "The D-07a (f) policy declaration, rung logged, applies_to session-start"
      contains: "session-start"
    - path: "scripts/check-eureka-optin-once.cjs"
      provides: "The D-07b (f) offline runner: the six fixture states driven through shouldAskEurekaOptin, exact fire or no-fire asserted, zero network, zero spawn"
      min_lines: 120
    - path: "data/harness-policies/eureka-optin-once.json"
      provides: "The D-07b (f) policy declaration, rung logged, applies_to session-start"
      contains: "eureka-optin-once"
    - path: "tests/test-341-eureka-optin-card.cjs"
      provides: "The D-07b answer round trip on both surfaces plus the enable coupling that clears never"
      contains: "Never ask again"
    - path: "tests/test-341-update-collapsed.cjs"
      provides: "Grep tripwire: exactly four steps, no touch-file reference anywhere, no SHA leg, the surviving points still present"
      contains: "activation-reached-the-wire"
  key_links:
    - from: "commands/update.md"
      to: "scripts/check-version-and-sha.cjs"
      via: "Step 1 parses STATUS= from the first line"
      pattern: "STATUS="
    - from: "commands/update.md"
      to: "claude plugin update mos@mindrian-marketplace"
      via: "Step 3 delegates Fetch and Swap to the loader"
      pattern: "claude plugin update"
    - from: "scripts/sessionstart-post-update-preflight.cjs"
      to: "lib/core/session-verify-state.cjs"
      via: "detectVersionChange gates the whole verification leg"
      pattern: "session-verify-state"
    - from: "scripts/sessionstart-post-update-preflight.cjs"
      to: "scripts/doctor.cjs --acceptance --session-start --json"
      via: "the bounded offline inline leg"
      pattern: "--session-start"
    - from: "scripts/sessionstart-post-update-preflight.cjs"
      to: "scripts/session-verify-probe.cjs"
      via: "detached, unref-ed spawn after the local set (the 267.2 fire pattern)"
      pattern: "detached"
    - from: "scripts/context-monitor"
      to: "lib/core/session-verify-state.cjs"
      via: "buildStatuslineSegment replaces the bare version suffix"
      pattern: "buildStatuslineSegment"
    - from: "scripts/first-install-router.cjs"
      to: "lib/core/session-verify-state.cjs"
      via: "the insight drain reads the capture on a later turn"
      pattern: "session-verify-state"
    - from: "scripts/session-verify-probe.cjs"
      to: "lib/core/brain-client.cjs"
      via: "recommendChain called with a normalized problem-type handle only"
      pattern: "recommendChain"
    - from: "scripts/first-install-router.cjs"
      to: "lib/core/session-verify-state.cjs shouldAskEurekaOptin"
      via: "the SAME insight branch decides whether the D-07b card rides with it (no second mechanism)"
      pattern: "shouldAskEurekaOptin"
    - from: "lib/mcp/tools/room.cjs"
      to: "lib/core/session-verify-state.cjs buildEurekaOptinCard"
      via: "room_list carries the one-time card advisory to Desktop and Cowork, which Larry fires through gate_render"
      pattern: "buildEurekaOptinCard"
    - from: "lib/mcp/tools/gate.cjs"
      to: "lib/core/session-verify-state.cjs recordEurekaOptin"
      via: "the reserved eureka-optin gate id records the answer before any room db is opened"
      pattern: "recordEurekaOptin"
    - from: "lib/core/eureka/eureka-enable.cjs"
      to: "lib/core/session-verify-state.cjs recordEurekaOptin"
      via: "a successful enable clears never and records installed, on both /mos:eureka enable and doctor --fix eureka"
      pattern: "recordEurekaOptin"
    - from: "scripts/check-eureka-optin-once.cjs"
      to: "lib/core/session-verify-state.cjs shouldAskEurekaOptin"
      via: "the six fixture states are driven through the one decision function, never a re-implementation"
      pattern: "shouldAskEurekaOptin"
---

<objective>
Collapse `/mos:update` to Check, Confirm, loader, Verify, delete the second implementation of the
loader that grew up beside it, and replace the retired touch-file with a real post-update signal:
the first session after any install or update verifies itself, says so in one statusline segment,
probes Theo without blocking, and hands the navigator one insight.

Purpose: 341-RESEARCH read the loader out of the shipped Claude Code binary and found it already
does, atomically and with a versioned cache directory, everything `/mos:update` Steps 6 and 7 were
hand-rolling: `Tus` fetches and caches, `E$` copies into the versioned directory, `xje` materializes
dependencies. The navigator's teardown called this root cause C, "update has no transaction", and
proposed a five-step transaction (Check, Confirm, Fetch to staging, Swap, Verify). Three of those
five now have a platform owner. What remains for the plugin is Check, Confirm, delegate, Verify.

D-07a (added 2026-09-10, mid-execution) says what Verify must actually mean. The old Verify was a
sentence the command printed at the end of its own run, backed by a touch-file the plugin wrote to
itself. The new Verify is automatic and it happens where the evidence is: on the NEXT session, when
the swapped bytes are actually the running bytes. The trigger is a version change
(`installed_plugins.json` versus a last-seen marker), which the loader cannot forget to set because
the loader is the thing that writes `installed_plugins.json`. The verification is a bounded, offline
subset of doctor acceptance points that already exist. The result is one statusline segment. Theo is
probed after the local set, detached, honestly. And because a first session after an update is the
first-step surface, it carries one variable reward: a single generic-handle nugget from Theo, framed
by Larry as one question.

D-07b (added the same day, navigator: "hyper critical, needs strong harness") puts ONE more thing on
that same first turn and nothing else: the Eureka opt-in card. Phase 341 cut the 380 MB embedding
stack out of the install payload (plans 341-02 and 341-04), which made the install slim but left the
capability behind a command most navigators will never discover. The card is the discovery moment,
and it is deliberately ONE card, once per install or update, with a locked four-state memory: asked
and installed, asked and declined for this version, asked and never again, or not yet asked. It rides
the SAME 267.2 router branch the insight rides, because a second delivery mechanism for a second
message on the same turn is exactly how a first-step surface turns into a nag. The install it offers
is the wave-2 enable path, never a second installer, and it runs only from a card answer.

The deletion is still bigger than `commands/update.md`. `scripts/post-update-activation.cjs` writes
the touch-file and `scripts/doctor.cjs` carries a `--post-update` flag that spawns it; both go. But
two things that the pre-revision plan proposed to delete now SURVIVE, converted rather than removed,
because D-07a gives them a real job:

- `scripts/sessionstart-post-update-preflight.cjs` stays the registered SessionStart host. Deleting
  it and adding a tenth hook would be the same hook with a different name, and it would throw away
  the Part 7 F8 stale-registry restart cue (`detectStaleRegistry`) that is independent of the
  touch-file and still correct. It is converted in place.
- The `post_update` signal in `lib/statusline/cockpit-signals.cjs` keeps its name and its consumer
  and changes its SOURCE: from a touch-file that only ever meant "a swap happened" to a real
  verification result that means "the post-update check found something".

One doctor point also does NOT go: `activation-reached-the-wire` compares the live MCP server
version to `package.json` and never reads the touch-file, so it survives untouched and keeps proving
the thing that actually matters. It moves onto the DETACHED leg, not the inline one, because it
spawns a real network handshake and the inline leg is offline by contract.

Output: the collapsed command, the retired SHA leg, the removed touch-file mechanism, a session-start
verification tier with two policies and a statusline segment, a detached Theo probe with an honest
refusal, one insight, one Eureka opt-in card with a four-state memory and a six-fixture harness, and
the tripwires that keep all of it true.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-CONTEXT.md
@.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-RESEARCH.md
@.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-PATTERNS.md
@docs/install/COLD-INSTALL-PROOF-341.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse commands/update.md to four steps</name>

  <files>commands/update.md</files>

  <read_first>
    - `commands/update.md` in full (256 lines). Specifically: the frontmatter at lines 1-21 (`name: update`, `body_shape: E`, `hitl_shape: "F.0"`, `hitl_why`, `interactive_first_reward`, `argument-hint: "[check|reapply|force]"`, `serves_jtbd`, `allowed-tools`, and the `connector: excluded: true` block with its reason string). Every one of those keys must survive the collapse; Canon Part 11 R1 and R16 both bind here. Then: Step 1 (:43-56, the version check and STATUS parse), Step 2 (:57-81, the four STATUS branches including the `SHA_DIFFERS_INVERSION_HOTFIX` branch at :66-78), Step 3 (:82-93, what is new), Step 4 (:94-116, confirm with the user), Step 5 (:117-135, the native install with 5a marketplace refresh and 5b plugin update), Step 6 (:136-149, the stale user-settings migrator), Step 7 (:150-181, the atomic activator including the touch-file write and the justification at :168), Step 8 (:182-198, verify plus the LOUD restart banner, and the registry-built-once-at-session-start justification at :196), Force Mode (:199-205), Reapply Flow (:206-221), Voice Rules (:222-229), "What this command does NOT do anymore" (:230-237), and the RESTART BANNER section (:238-256).
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-PATTERNS.md` section "commands/update.md and commands/eureka.md" - the body idiom to preserve: a numbered `### Step N` per action, each with a fenced `bash` block using `node "${CLAUDE_PLUGIN_ROOT}/scripts/..."`, then a `STATUS=` parse, then a per-status quoted Larry line.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-RESEARCH.md` "Loader Ground Truth" F-2 and F-3, and the "Don't Hand-Roll" table row on getting the artifact onto the user's machine: the loader already does fetch, swap and dependency install, so D-07's collapse is deleting a second implementation, not simplifying a feature.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-CONTEXT.md` decision D-07a, sub-points (a) through (f) - specifically that Verify's AUTOMATIC half now lands on the next session, which is what Step 4's closing sentence must point at rather than promising a check the command itself performs.
    - `.claude/includes/release-process.md` - "How Users Upgrade": the two-command manual path, word for word, so the command and the include agree.
  </read_first>

  <action>
Rewrite the BODY of `commands/update.md` to exactly four steps. Preserve the frontmatter block
byte-for-byte except for `argument-hint` if a mode disappears (see below); preserve the Voice Rules
section; preserve and EXTEND the "What this command does NOT do anymore" section.

The four steps:

- `### Step 1: Check`. Unchanged in mechanism: run
  `node "${CLAUDE_PLUGIN_ROOT}/scripts/check-version-and-sha.cjs"` and parse `STATUS=` from the
  first line. The STATUS vocabulary drops to three values: `UP_TO_DATE`, `VERSION_DIFFERS`,
  `NETWORK_ERROR`. Delete the `SHA_DIFFERS_INVERSION_HOTFIX` branch and its `--force` fallback prose
  entirely (Task 2 removes the producing leg). Keep the per-status Larry lines for the three
  survivors.
- `### Step 2: Confirm`. The existing Step 3 (what is new) folds INTO this step as the material the
  confirmation is built on, and the existing Step 4's AskUserQuestion confirmation is the step
  itself. On `UP_TO_DATE`, say so in one line and STOP: no confirmation, no loader call, no banner.
- `### Step 3: Loader`. The existing Step 5, unchanged: `5a` refreshes the catalog
  (`/plugin marketplace update`) and `5b` runs `claude plugin update mos@mindrian-marketplace`.
  Add two sentences naming what the loader now owns, so a future reader does not re-add a staging
  step: it installs the package into a shared `~/.claude/plugins/npm-cache`, copies the package into
  a versioned cache directory, and runs `npm ci --ignore-scripts` there with a 60 second cap.
- `### Step 4: Verify`. The existing Step 8's verification half, WITHOUT the touch-file. Re-run the
  version check, report the new version, and emit the restart banner ONLY when a real swap happened
  (Step 3 ran and the version moved). On `UP_TO_DATE` there is no banner. Keep the existing
  justification for why a banner is needed at all, quoted from :196: the slash-command registry is
  built once at session start. Then add THREE sentences, and no more, describing what D-07a makes
  happen without the user doing anything: the next session detects that the version changed, runs a
  bounded offline self-check, shows the result as one statusline segment beside the version, and
  Larry opens with the detail plus one insight. Say plainly that this is why the command no longer
  writes a file to remind itself to check later. Do NOT document the internals here; the command doc
  states the promise, the hook keeps it.

DELETED entirely: the old Step 6 (stale user-settings migrator), the old Step 7 (atomic activator,
its `doctor --fix --post-update` call, the touch-file write and its preflight-banner prose), and the
`SHA_DIFFERS_INVERSION_HOTFIX` branch. Do NOT leave a "deprecated" stub for any of them.

The RESTART BANNER section at the end survives, but its trigger condition changes from "any
successful update" to "a real swap". Edit its lead sentence accordingly.

Force Mode and Reapply Flow: read both, then decide and STATE the decision in the file itself.
`reapply` exists to re-run the local wiring that `install.sh` used to do; with `install.sh` retired
in plan 341-07 and the loader owning the copy, it may have no remaining job. If it has none, delete
the section AND remove `reapply` from `argument-hint`, and record the removal in the "What this
command does NOT do anymore" section. If it retains a real job, keep it and write one sentence in
the file naming that job. Do not leave the question open.

Extend "What this command does NOT do anymore" with the newly deleted behaviors, each with its one
line reason: it does not stage or swap bytes (the loader's `Tus` and `E$` do); it does not install
dependencies (the loader's `npm ci --ignore-scripts` does); it does not migrate stale settings paths
(the second install location is retired, plan 341-07); it does not write a restart touch-file (the
next session's version-change check is the signal now, per D-07a); and it does not check a tag SHA
(published npm versions are immutable, so an in-version hotfix is a new version).

House rule: hyphens only, no em-dashes. Keep the file's existing voice. Do NOT spell the literal
string `post-update-restart-pending` anywhere in this file, not even historically: Task 3's tripwire
asserts that literal is absent from the whole live tree, and a historical mention would be a
self-invalidating grep.
  </action>

  <verify>
    <automated>test "$(grep -c '^### Step ' commands/update.md)" = "4" &amp;&amp; test "$(grep -c 'post-update-restart-pending' commands/update.md)" = "0" &amp;&amp; test "$(grep -c 'SHA_DIFFERS_INVERSION_HOTFIX' commands/update.md)" = "0" &amp;&amp; node scripts/check-shape-declaration.cjs --check</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^### Step ' commands/update.md` returns 4, and the four headings read Check, Confirm, Loader (or the loader-delegating name you chose), and Verify.
    - `grep -c 'post-update-restart-pending' commands/update.md` returns 0, `grep -c 'SHA_DIFFERS_INVERSION_HOTFIX' commands/update.md` returns 0, and `grep -c 'post-update' commands/update.md` returns 0.
    - The frontmatter contract is intact: `for k in '^name: update' '^body_shape:' '^hitl_shape:' '^hitl_why:' '^serves_jtbd:' 'excluded: true'; do grep -qE "$k" commands/update.md || echo "MISSING $k"; done` prints nothing.
    - `node scripts/check-shape-declaration.cjs --check` exits 0 (Canon Part 11 R16 advisory lint clean).
    - The two documented commands survive: `grep -c 'plugin marketplace update' commands/update.md` is at least 1 and `grep -c 'claude plugin update mos@mindrian-marketplace' commands/update.md` is at least 1.
    - The banner condition is a real swap, not any success: `grep -c 'UP_TO_DATE' commands/update.md` is at least 1 and the UP_TO_DATE branch text explicitly says no banner.
    - Step 4 states the D-07a promise in at most three sentences: `grep -ci 'next session' commands/update.md` is at least 1 and `grep -ci 'statusline' commands/update.md` is at least 1. Quote the three sentences verbatim in the SUMMARY so a reviewer can confirm they promise only what plan tasks 3 to 5 deliver.
    - The reapply decision is recorded: either `grep -c 'reapply' commands/update.md` returns 0 AND `argument-hint` no longer lists it, or the Reapply section carries a one-sentence statement of its remaining job. State which in the SUMMARY.
    - "What this command does NOT do anymore" names all five deleted behaviors: `for s in 'swap' 'dependencies' 'settings' 'touch-file' 'SHA'; do grep -qi "$s" commands/update.md || echo "MISSING $s"; done` prints nothing.
    - `grep -c "$(printf '\xe2\x80\x94')" commands/update.md` returns 0.
  </acceptance_criteria>

  <done>`/mos:update` is Check, Confirm, loader, Verify; the migrator, the activator, the touch-file and the SHA branch are gone with no stubs; the banner fires only on a real swap; Step 4 names the automatic next-session verification instead of promising a check the command performs itself; the CIRS frontmatter contract survives; and the reapply question is answered in the file rather than left open.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the SHA leg and the api.github.com tag-ref call</name>

  <files>scripts/check-version-and-sha.cjs</files>

  <read_first>
    - `scripts/check-version-and-sha.cjs` in full (approximately 370 lines). Specifically: the header at lines 1-45 documenting the four STATUS values and the catalog-pin reasoning; `readLocalSha` at approximately :102; `resolveLatestFromCatalog` at :128-145 (the catalog pin is `plugins[].version` for the entry named `mos`, which 341-RESEARCH confirms keeps working unchanged under an exact npm pin); `fetchLatestVersion` at :195-218; `fetchTagSha` at :220-246 (the `https://api.github.com/repos/.../git/refs/tags/v<version>` call); `main` at :248-310 including the four STATUS emissions at :277, :286, :296, :301 and the `SHA_DIFFERS_INVERSION_HOTFIX` emission at :306; and `compareSemver` at :310.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-CONTEXT.md` decision D-07, the sentence retiring the SHA leg: published npm versions are immutable, so an in-version hotfix is a new version.
    - `commands/update.md` as rewritten in Task 1 - the three STATUS values it now parses. The producer and the consumer must agree exactly.
    - `grep -rn "SHA_DIFFERS_INVERSION_HOTFIX\|check-version-and-sha" --include=*.cjs --include=*.md --include=*.sh --include=*.json . | grep -v '^\./\.planning' | grep -v '^\./node_modules'` - every other consumer of this script's output. Read the list BEFORE editing; a STATUS value removed from the producer while a consumer still branches on it is a silent dead branch.
  </read_first>

  <action>
Retire the SHA half of `scripts/check-version-and-sha.cjs` while leaving the version half exactly as
it is.

1. Delete `fetchTagSha` and every call to it. This removes the ONLY `api.github.com` request in the
   script. Note in the header comment that the call was retired in Phase 341 and why: an npm version
   is immutable, so there is no such thing as an in-version hotfix, so there is nothing a tag SHA
   comparison can detect that a version comparison cannot.
2. Delete the `SHA_DIFFERS_INVERSION_HOTFIX` STATUS emission at approximately :306 and every branch
   that reaches it.
3. Decide `readLocalSha`'s fate by reading its remaining callers. If the local SHA is still worth
   REPORTING (it is useful context in the output even when nothing branches on it), keep it and keep
   emitting `LOCAL_SHA=` in the surviving STATUS lines, with `REMOTE_TAG_SHA` dropped. If it has no
   remaining consumer, delete it too. State which you did and why in the file's header and in the
   SUMMARY.
4. Update the header block's documented STATUS vocabulary from four values to three:
   `UP_TO_DATE`, `VERSION_DIFFERS`, `NETWORK_ERROR` (plus the pre-existing `ERROR` emission if the
   header already documents it; do not silently drop a value the code still emits).
5. Simplify `main`'s tail accordingly: after the version comparison there are now exactly two
   outcomes, differ or match, plus the error paths. The three `UP_TO_DATE` emissions at :286, :296
   and :301 existed only to describe how much SHA information was available; collapse them into one
   honest `UP_TO_DATE` emission with a `REASON` that does not mention a SHA it no longer fetched.
6. Rename NOTHING. The file keeps its name and its path, because `commands/update.md` and any other
   consumer reference it by path and a rename is churn with no benefit. Note in the header that the
   `-and-sha` in the filename is now historical.
7. Update every OTHER consumer the grep found so no branch is left waiting for a STATUS the producer
   can no longer emit. List each one and its disposition in the SUMMARY.
  </action>

  <verify>
    <automated>node scripts/check-version-and-sha.cjs | head -1 | grep -qE '^STATUS=(UP_TO_DATE|VERSION_DIFFERS|NETWORK_ERROR|ERROR)$' &amp;&amp; test "$(grep -c 'api.github.com' scripts/check-version-and-sha.cjs)" = "0" &amp;&amp; test "$(grep -rc 'SHA_DIFFERS_INVERSION_HOTFIX' scripts/check-version-and-sha.cjs commands/update.md | grep -vc ':0$')" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'api.github.com' scripts/check-version-and-sha.cjs` returns 0 and `grep -c 'fetchTagSha' scripts/check-version-and-sha.cjs` returns 0.
    - `grep -c 'SHA_DIFFERS_INVERSION_HOTFIX' scripts/check-version-and-sha.cjs` returns 0.
    - `grep -rn 'SHA_DIFFERS_INVERSION_HOTFIX' --include=*.cjs --include=*.md --include=*.sh . | grep -v '^\./\.planning' | grep -v '^\./CHANGELOG.md' | grep -v '^\./docs/autopsies'` prints nothing (only the historical paper trail may keep it).
    - `node scripts/check-version-and-sha.cjs` runs and its first line matches `^STATUS=(UP_TO_DATE|VERSION_DIFFERS|NETWORK_ERROR|ERROR)$`.
    - The catalog leg is untouched: `grep -c 'resolveLatestFromCatalog' scripts/check-version-and-sha.cjs` is at least 1 and `grep -c "p.name === 'mos'" scripts/check-version-and-sha.cjs` is at least 1.
    - The header documents exactly the STATUS values the code emits: extract them by `grep -o "STATUS=[A-Z_]*" scripts/check-version-and-sha.cjs | sort -u` and confirm every one is documented in the header block, and that the header documents no value the code cannot emit. Record both lists in the SUMMARY.
    - The existing suites still pass: `node tests/test-check-version-semver-prerelease.cjs` exits 0 and `node tests/test-marketplace-cache-prerelease-pick.cjs` exits 0.
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/check-version-and-sha.cjs` returns 0.
  </acceptance_criteria>

  <done>The version check makes no GitHub call, emits three STATUS values that its consumer actually branches on, keeps the catalog-pin leg intact, and no consumer anywhere is left waiting for a status the producer can no longer emit.</done>
</task>

<task type="auto">
  <name>Task 3: Define the session-verify state contract, retire the touch-file mechanism, and pin the collapse with a tripwire</name>

  <files>lib/core/session-verify-state.cjs, scripts/post-update-activation.cjs, scripts/doctor.cjs, lib/statusline/cockpit-signals.cjs, scripts/context-monitor, tests/test-mos-update-activation-gap.cjs, tests/test-341-session-verify-state.cjs, tests/test-341-update-collapsed.cjs, tests/run-all-341.sh</files>

  <read_first>
    - `scripts/post-update-activation.cjs` - the touch-file path construction at approximately :66 and again at approximately :129, and `activatePostUpdate`'s full result envelope. Read the header to understand exactly which compensating behavior is being retired.
    - `scripts/doctor.cjs` lines 194-205 (the `postUpdate: false` flag declaration and its comment), :289 (`else if (arg === '--post-update') flags.postUpdate = true;`), :460-470 (the `--post-update` help text), and :3227-3265 (the dispatch that requires `post-update-activation.cjs` and calls `activatePostUpdate`).
    - `scripts/doctor.cjs` lines 1454-1545 - the `activation-reached-the-wire` acceptance point. Read it and CONFIRM for yourself that it never reads the touch-file: it reads `package.json`, spawns `doctor --brain-smoke --json`, parses the L4 handshake version, and guards on the `store_identity` layer. It SURVIVES this plan untouched. Do not delete it. Note its `DOCTOR_SKIP_ACTIVATION_GATE=1` escape hatch: Task 4 uses it, so read what it returns (`ok: true` with `detail.skipped`).
    - `lib/statusline/cockpit-signals.cjs` header lines 20-30 (the `post-update->` paragraph in the SIGNAL SOURCING map, including its NAMED DEBT wording), `mindrianDir()`, `readPostUpdate()` at :106-117, and the `collectSignals` tail at :265-285 where `post_update` is placed on the returned object.
    - `lib/statusline/cockpit-renderer.cjs` lines 195-230 (`effectiveHealth` and the post-update escalation), :302 (the `post_update boolean` line of the documented state contract), and :385 (the hero integrity corrective). This file is NOT edited by this plan; read it to confirm the `post_update` KEY and its consumer stay exactly as they are while its SOURCE changes.
    - `scripts/context-monitor` lines 460-476 (the lazy requires of the three cockpit modules and the graceful-degradation comment) and lines 908-930 (the `verSuffix` construction and the single `process.stdout.write` that composes the one navigator line). Line 920 is the ONE place the version reaches the statusline; it is the segment D-07a (c) extends.
    - `tests/test-statusline-glyph-isolation.cjs` lines 45-61 - `EXCLUSIVE_GLYPHS = ['chart', 'target', 'gear']` (the three carve-out glyphs, written there as emoji). Confirm that a check mark and a warning glyph are NOT on that list before you place either in `scripts/context-monitor`, and record the confirmation in the SUMMARY.
    - `tests/test-mos-update-activation-gap.cjs` in full (233 lines) - arms 1 and 2 exercise `activatePostUpdate` (deleted here); arms 3a and 3b exercise the preflight's touch-file gate (replaced in Task 5).
    - `tests/test-213-part8-boundary.cjs` - the grep-tripwire idiom (header-declared exclusion list, one arm per asserted fact, a FAIL counter, exit non-zero on any arm).
    - `tests/run-all-341.sh` lines 25-60 (`run`, `run_if`, `run_red_until` and their exact semantics) and lines 139-149 (the last `run_if` legs, immediately before the step-block tripwire block, which is where new legs are appended).
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-PATTERNS.md` "Shared Patterns": "Never throw across a resolver boundary", "Test-seam env override", "Pure core, thin CLI, exported for hermetic tests", and "Header comment as the contract".
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-CONTEXT.md` decision D-07b in full, sub-points (a) through (g). Sub-point (c) carries the LOCKED verbatim copy and the three option labels; sub-point (d) carries the LOCKED four-state re-ask rule; sub-point (e) says this module is the ONE state authority and that no second marker file may exist. Transcribe the copy character for character; it is not paraphrasable.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-02-SUMMARY.md` - `lib/core/eureka-deps-resolver.cjs` exports `eurekaDepInstalled(name)` returning `{ installed, dir, where }` and never throwing. That function is the FIRST of D-07b (b)'s two installed signals; this task's decision function consumes the boolean, it does not re-probe.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-03-SUMMARY.md` - Class S is five layers now, and L5 `model_installed` is ADVISORY (`advisory: true`, excluded from the overall-ok reduction). Its `ok` is the SECOND of D-07b (b)'s two installed signals. Note its exact id string, `model_installed`, because Task 4's doctor line and Task 6's caller both read it by id.
  </read_first>

  <action>
This task defines the contract the next two tasks consume, then removes the mechanism it replaces.
Contract first, deletion second, tripwire third.

1. Create `lib/core/session-verify-state.cjs`, CJS, `'use strict'`, house header contract naming
   Phase 341, D-07a sub-points (a) and (c), D-07b sub-points (b), (d) and (e), Canon Parts 7, 8
   and 12, and the test seam `MOS_SESSION_VERIFY_HOME`. This module is the ONE authority for four
   things: the last-seen version marker, the verification result, the statusline segment string,
   and the Eureka opt-in record. Nothing else computes any of the four, and per D-07b (e) there is
   no second marker file for the opt-in anywhere in the tree.

   Single root rule: every path derives from `process.env.MOS_SESSION_VERIFY_HOME || HOME`
   (`HOME || USERPROFILE || os.homedir()`), exactly as `MOS_LEGACY_INSTALL_HOME` works in plan
   341-07's migrator. There must be no second home resolution anywhere in the module.

   Exports:
   - `verifyDir()` returns `<home>/.mindrian/session-verify` (a directory, mirroring
     `~/.mindrian/first-install/`, so the insight capture files of Task 5 have a home and
     `~/.mindrian` does not accumulate loose files).
   - `statePath()` returns `<verifyDir>/state.json`.
   - `readVerifyState()` returns the parsed state or `null`. Absent file, malformed JSON, or any
     throw returns `null`. Never throws.
   - `writeVerifyState(obj)` writes atomically (write to a `.tmp` sibling then `fs.renameSync`),
     creating the directory with `recursive: true`. Returns `true` on success, `false` on any
     failure. Never throws. This is the RAW overwrite; it is for tests and for the very first
     write only.
   - `mergeVerifyState(patch)` reads the current state, shallow-merges `patch` over its TOP-LEVEL
     keys, and writes the result through `writeVerifyState`. Returns `true` or `false`, never
     throws. Every runtime writer in this plan uses this and NOT `writeVerifyState`, because the
     verification result, the probe result and the opt-in record are written by three different
     processes at three different moments into ONE file: a raw overwrite by any of them would
     silently erase the other two. Say that in the header, and name the concrete loss it prevents:
     a verification write on the next update erasing a `never` the navigator chose (which would
     resurrect a card D-07b (d) says is permanently answered).
   - `detectVersionChange(registeredVersion)` returns
     `{ changed, lastSeen, current }`. `changed` is true when `registeredVersion` is a non-empty
     string AND it differs from `state.last_seen_version`. A null, absent or malformed state counts
     as `lastSeen: null` and therefore `changed: true` whenever `registeredVersion` is present, so
     the FIRST session after a fresh install verifies too (D-07a (a)). A null or empty
     `registeredVersion` returns `changed: false` (unknown is not a trigger).
   - `shortVersionLabel(version)` returns the prerelease tail when the version carries one
     (`2.0.0-beta.30` yields `beta.30`) and the whole version otherwise (`2.1.0` yields `2.1.0`).
     Pure string function, no I/O.
   - `buildStatuslineSegment(state)` returns the D-07a (c) segment string, exactly ONE segment and
     never more: `mos <shortVersionLabel> <check mark>` when `state.local.ok === true`;
     `mos <shortVersionLabel> <warning glyph> <N>` where N is `state.local.failed_count` when
     `state.local.ok === false`; `mos <shortVersionLabel>` with no glyph when the verification has
     not run or is still pending. Returns `''` when `state` is null or carries no version. Pure
     function of its argument, no I/O, never throws. Use the plain check mark U+2713 and the plain
     warning sign U+26A0; do NOT use either of the three carve-out glyphs.

   D-07b exports, in the same module because D-07b (e) names this module as the ONE state
   authority:
   - `EUREKA_OPTIN_COPY`, the frozen question string, transcribed VERBATIM from D-07b (c):
     `Eureka spots connections between your claims by meaning, not keywords. It needs a one-time
     380 MB local model download. Install it now?` One space between sentences, the digits `380`
     and the unit `MB` exactly as written. Do not reflow it, do not re-word it, do not append to
     it. Task 6's test asserts the literal.
   - `EUREKA_OPTIN_OPTIONS`, a frozen three-member array in D-07b (c) order:
     `{ id: 'install_now', label: 'Install now' }`, `{ id: 'not_now', label: 'Not now' }`,
     `{ id: 'never_ask_again', label: 'Never ask again' }`. Each option also carries a one-line
     `description` in the repo's voice naming what the choice does, because the gate superset card
     supports per-option descriptions and a bare label makes a 380 MB decision on no information.
   - `EUREKA_OPTIN_CHOICES`, the frozen state enum `['installed', 'declined', 'never']`. Three
     values, exactly D-07b (e)'s contract.
   - `EUREKA_OPTIN_CHOICE_BY_OPTION_ID`, the frozen map from card option id to state choice:
     `{ install_now: 'installed', not_now: 'declined', never_ask_again: 'never' }`. This exists so
     the recorder in Task 6 spells no option id of its own: renaming an option in
     `EUREKA_OPTIN_OPTIONS` without updating this map is a visible edit in one file, where
     spelling the ids again in a second file would be a silent break.
   - `EUREKA_OPTIN_GATE_PREFIX`, the reserved gate id prefix `eureka-optin-`. Task 6's MCP half
     keys its one guarded branch on this prefix, so the prefix lives here beside the card it
     identifies rather than as a string literal in two files.
   - `buildEurekaOptinCard(version)` returns the gate superset card object
     `{ gate_id: EUREKA_OPTIN_GATE_PREFIX + version, header: EUREKA_OPTIN_COPY, kind: 'general',
     selectMode: 'single', options: EUREKA_OPTIN_OPTIONS }`. Pure, no I/O, never throws. This is
     the ONE card definition both surfaces render from: the CLI instruction reads its `header` and
     its option labels, and `lib/mcp/gate-render.cjs`'s `normalizeCard` accepts this shape as is.
     Confirm that by reading `normalizeCard` at `lib/mcp/gate-render.cjs:150` and record the
     confirmation in the SUMMARY.
   - `shouldAskEurekaOptin({ installedNow, state, currentVersion, firstInstallRewardPending })`
   Null-guard, stated explicitly: `state` may be null or malformed (absent marker, unreadable JSON); treat
   `state.eureka_optin` as `{}` before evaluating any rule, so a fresh install reaches the ask rule and
   never throws (the "fresh" fixture passes `state: null` and expects `{ ask: true, defer: false }`).
     returns `{ ask, defer, reason }`. PURE: no I/O, no probe, no clock read; every input is
     supplied by the caller. It encodes D-07b (b) and (d) and nothing else, in this exact order,
     first match wins:
     1. `currentVersion` is not a non-empty string: `{ ask: false, defer: false, reason:
        'unknown_version' }`. Unknown is never a trigger, exactly as `detectVersionChange` treats
        it.
     2. `installedNow === true`: `{ ask: false, reason: 'already_installed' }`. D-07b (b)
        precondition one and D-07b (d)'s "installed never asks".
     3. `state.eureka_optin.choice === 'never'`: `{ ask: false, reason: 'never' }`. Permanent, and
        the ONLY thing that clears it is `recordEurekaOptin('installed', ...)` from the enable
        path (Task 6).
     4. `state.eureka_optin.choice === 'installed'`: `{ ask: false, reason: 'already_installed' }`.
     5. `state.eureka_optin.choice === 'declined'` AND `state.eureka_optin.version ===
        currentVersion`: `{ ask: false, reason: 'declined_this_version' }`. Silent for the life of
        that version.
     6. `firstInstallRewardPending === true`: `{ ask: false, defer: true, reason:
        'first_install_reward_owns_turn' }`. DEFER is a third answer, distinct from no-fire: Task 6
        re-checks on the next turn of the SAME session and never postpones to a later session.
     7. otherwise `{ ask: true, defer: false, reason: 'no_standing_answer' }`.
     Write the derivation of D-07b (d)'s "re-asks exactly once on the next version change" in a
     comment: it is not a counter. A `declined` record is keyed by the version it was made
     against, so it stops matching rule 5 the moment the version moves, and the next decline
     writes a NEW record keyed to the NEW version. Exactly one ask per version falls out of the
     key, with no state to drift.
     Also write, in a comment, why rules 3 to 5 read the RECORD rather than a version-change
     boolean: the D-07a trigger is consumed by whichever surface saw the session start first, so a
     boolean would make the card's behavior depend on which surface won that race. Keying on "is
     there a standing answer for THIS version" is surface-independent and yields the same verdict
     on the CLI and on Desktop.
   - `recordEurekaOptin(choice, version, extra)` validates `choice` against `EUREKA_OPTIN_CHOICES`
     and `version` as a non-empty string, then `mergeVerifyState({ eureka_optin: { choice,
     version, at: Date.now(), ...extra } })`. Returns `{ ok, reason }`; an invalid choice returns
     `{ ok: false, reason: 'invalid_choice' }` and writes nothing. Never throws.
     The honest failure encoding, which D-07b does not spell out and this plan fixes here rather
     than leaving to the executor: when `Install now` is answered and the install FAILS, the
     caller records `recordEurekaOptin('declined', version, { install_failed: true,
     install_failure_reason: <bounded reason string> })`. `choice` stays inside the locked
     three-value enum, so the contract in D-07b (e) is unchanged, and the two extra fields keep it
     honest: the doctor line reports "declined (install failed: <reason>)" rather than claiming an
     install that did not happen, and the card does not re-fire in a loop against the same broken
     network inside the same version. Say that in the header.
   - `resolveOptinVersion(state)` returns the version string every opt-in caller keys on:
     `state.last_seen_version` when it is a non-empty string, else the value returned by
     `lib/core/repo-version.cjs` (required lazily inside a try), else `null`. ONE fallback chain,
     here, so the three callers in Tasks 5 and 6 cannot disagree about what version an answer was
     made against. Note in the header that this is deliberately NOT the same question as the
     preflight's `readRegisteredVersion()`, which asks what the LOADER registered; this one asks
     what version the marker is keyed to.

   State shape, frozen here and documented in the header as the contract every consumer reads:
   `schema` (the literal `mos-session-verify/1`), `last_seen_version`, `verified_at_ms`,
   `local` (`{ ok, ran, failed_count, failed_points, mode, elapsed_ms, inconclusive_reason }`),
   `wire` (`'pending' | 'ok' | 'failed' | 'skipped'`), `theo`
   (`'pending' | 'reachable' | 'unreachable' | 'refused'`), `theo_reason`,
   `insight` (`{ pending, capture_path, handle, delivered_for_version, drain_retries }`), and
   `eureka_optin` (`{ choice, version, at, install_failed, install_failure_reason }`, absent until
   the navigator answers the card). Tasks 4, 5 and 6 fill these fields; this task defines them and
   ships the readers, the decision function and the recorder.

   Close with `module.exports = { ... }` covering every export above. No CLI: this is a library.

2. `git rm scripts/post-update-activation.cjs`. No stub. It is the touch-file writer and the atomic
   activator; both are the second implementation of a swap the loader now performs.

3. `scripts/doctor.cjs`: remove the `postUpdate` flag declaration and its comment (:194-205), the
   `--post-update` argv branch (:289), the help text (:460-470), and the whole dispatch block
   (:3227-3265). Do NOT touch the `activation-reached-the-wire` acceptance point at :1454; it is
   independent of the touch-file and it is the thing that actually proves an update reached the
   wire. Add a one-line comment immediately above that point's `id:` recording that Phase 341
   retired the touch-file mechanism and that this point is deliberately kept and moves onto the
   D-07a detached leg.

4. `lib/statusline/cockpit-signals.cjs`: re-source the `post_update` signal rather than delete it.
   Rename `readPostUpdate()` to `readPostUpdateVerification()` and have it require
   `lib/core/session-verify-state.cjs` (lazily, inside a try, so a degraded install still renders),
   call `readVerifyState()`, and return `true` only when `state.local.ok === false`. Absent state,
   pending state, or any throw returns `false`. Keep the returned object's `post_update` KEY exactly
   as it is: `lib/statusline/cockpit-renderer.cjs` destructures it for `effectiveHealth` and is NOT
   edited by this plan, and the key name is still accurate because the signal is still "the
   post-update check found something". Rewrite the `post-update->` paragraph in the header SIGNAL
   SOURCING map (lines 25-28) to name the new source and to delete the now-false claim that the
   touch-file is written by `scripts/doctor.cjs --post-update`. Do not spell the retired touch-file
   name in the new text.

5. `scripts/context-monitor`: replace the `verSuffix` construction at approximately :920. Lazily
   require `../lib/core/session-verify-state.cjs` next to the three existing cockpit lazy requires
   at :472-477, in the same try-catch, same graceful-degradation comment style. Build the suffix as
   `' - ' + buildStatuslineSegment(readVerifyState())` when the module loaded AND the segment string
   is non-empty; otherwise fall back to the EXACT pre-existing behavior (`' · v' + current_version`
   when the version is known, `''` otherwise). One segment, never two: the new segment REPLACES the
   bare version suffix, it does not sit beside it. Use a plain middot separator (`' · '`) consistent
   with the existing `jtbdSeg` and `operatorSeg` separators in the same expression. Change nothing
   else about the composed line: `cockpitLine + jtbdSeg + operatorSeg + verSuffix` keeps its order
   and its single `process.stdout.write`.

6. `git rm tests/test-mos-update-activation-gap.cjs`. Arms 1 and 2 test `activatePostUpdate`, which
   no longer exists; arms 3a and 3b test the touch-file gate, which Task 5 replaces with the
   version-change gate and covers with `tests/test-341-session-start-verify-hook.cjs`. Record in the
   SUMMARY, arm by arm, where each of the four arms' coverage now lives, so the deletion is a move
   and not a loss. `tests/test-update-restart-cue.cjs` is NOT touched here; Task 5 owns it.

7. Create `tests/test-341-session-verify-state.cjs`, fully hermetic via `MOS_SESSION_VERIFY_HOME`
   pointed at an `fs.mkdtempSync` tree. Arms:
   - `detectVersionChange` on an empty home returns `changed:true, lastSeen:null` for a real version
     string (the fresh-install case).
   - After `writeVerifyState({ last_seen_version: 'X' })`, `detectVersionChange('X')` returns
     `changed:false` and `detectVersionChange('Y')` returns `changed:true`.
   - `detectVersionChange(null)` and `detectVersionChange('')` return `changed:false`.
   - Malformed `state.json` (unparseable bytes) yields `readVerifyState() === null` and
     `detectVersionChange('X').changed === true`, with no throw.
   - `writeVerifyState` is atomic: after a successful write no `.tmp` sibling remains in
     `verifyDir()`.
   - `shortVersionLabel('2.0.0-beta.30')` is `beta.30`; `shortVersionLabel('2.1.0')` is `2.1.0`.
   - `buildStatuslineSegment` returns exactly one segment for each of the four states (ok, failed
     with a count, pending, null) and the failed form contains the failing count as a digit string.
   - `buildStatuslineSegment(null)` returns `''`.
   - `mergeVerifyState` preserves siblings: write `{ eureka_optin: { choice: 'never', version: 'X'
     } }`, then `mergeVerifyState({ last_seen_version: 'Y', local: { ok: true } })`, then assert
     `readVerifyState().eureka_optin.choice === 'never'`. This is the arm that proves a
     verification write cannot erase a `never`.
   - `EUREKA_OPTIN_COPY` equals the D-07b (c) literal byte for byte (assert with `===` against the
     string written out in the test, not a substring match), and `EUREKA_OPTIN_OPTIONS` has
     exactly three members whose labels are `Install now`, `Not now`, `Never ask again` in that
     order.
   - `shouldAskEurekaOptin` over the six D-07b (f) fixture states, asserting BOTH `ask` and
     `defer` for each: fresh (no state, not installed) fires; installed (`installedNow: true`)
     does not; declined at the SAME version does not; declined at an OLDER version fires (the
     exactly-once re-ask); `never` does not; first-install-reward-pending does not fire and
     returns `defer: true`. Each arm also asserts the `reason` string, so a future refactor that
     returns the right verdict for the wrong reason is caught.
   - `shouldAskEurekaOptin` precedence: `installedNow: true` beats a stored `never` (rule 2 before
     rule 3), and a stored `never` beats `firstInstallRewardPending: true` (rule 3 before rule 6).
     Order-sensitive rules need an order-sensitive arm.
   - `recordEurekaOptin('bogus', 'X')` returns `{ ok: false }` and writes nothing (the state file
     is byte-identical before and after); `recordEurekaOptin('never', 'X')` returns `{ ok: true }`
     and `readVerifyState().eureka_optin` carries `choice`, `version` and a numeric `at`.
   - `buildEurekaOptinCard('2.0.0-beta.31')` returns a card whose `gate_id` starts with
     `EUREKA_OPTIN_GATE_PREFIX`, whose `header` is `EUREKA_OPTIN_COPY`, and which survives
     `require('../lib/mcp/gate-render.cjs').normalizeCard(card)` with all three option ids intact.
     This is the arm that proves the ONE card definition is renderable by the shipped gate ladder
     without a second shape.
   - Canon Part 8: the module's source contains no `http`, `https` or `fetch` token
     (`grep -v '^\s*//' | grep -c` filtered so the header prose cannot self-invalidate the arm).

8. Create `tests/test-341-update-collapsed.cjs`, a grep tripwire in the
   `tests/test-213-part8-boundary.cjs` idiom, with its exclusion list declared in the header. Arms:
   - `commands/update.md` has exactly four `### Step ` headings.
   - The literal `post-update-restart-pending` appears NOWHERE in the live tree. Scan `commands/`,
     `scripts/`, `lib/`, `hooks/`, `bin/`, `data/` and `tests/`; exclude `CHANGELOG.md`,
     `docs/autopsies/` and `.planning/` and name the exclusions in the test's header. This is why no
     file this plan writes may spell that literal, not even in a comment.
   - `scripts/post-update-activation.cjs` does not exist.
   - `scripts/doctor.cjs` has no `--post-update` flag: the literal `'--post-update'` does not appear.
   - `scripts/doctor.cjs` STILL has the `activation-reached-the-wire` point (assert the id string is
     present). This arm is what stops a future cleanup from deleting the wrong thing.
   - `hooks/hooks.json` parses, STILL contains `sessionstart-post-update-preflight` (D-07a converts
     that hook rather than removing it, so its absence is now the defect), and STILL contains
     `sessionstart-npm-reconcile` (341-RESEARCH names it the deliberate 60-second-timeout backstop,
     not dead code).
   - The SessionStart hook count in `hooks/hooks.json` is unchanged at 9 (parse the JSON and count
     the entries under `hooks.SessionStart`). D-07a says do not add a tenth hook; this arm holds the
     line. Record the measured count in the SUMMARY.
   - `scripts/check-version-and-sha.cjs` contains no `api.github.com` and no
     `SHA_DIFFERS_INVERSION_HOTFIX`.

9. `tests/run-all-341.sh`: append two `run_if` legs immediately after the existing
   `341: update-collapsed` leg and before the step-block tripwire block, following the file's exact
   two-line convention (label, guard path, then the command on a continued line):
   `341: session-verify-state` guarded on `tests/test-341-session-verify-state.cjs`, and keep the
   existing `341: update-collapsed` leg as it is. Tasks 4 and 5 append their own legs.
  </action>

  <verify>
    <automated>node tests/test-341-session-verify-state.cjs &amp;&amp; node tests/test-341-update-collapsed.cjs &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" &amp;&amp; node tests/test-statusline-glyph-isolation.cjs &amp;&amp; bash -n tests/run-all-341.sh</automated>
  </verify>

  <acceptance_criteria>
    - `test ! -f scripts/post-update-activation.cjs` and `git ls-files scripts/post-update-activation.cjs` prints nothing.
    - `node -e "const m=require('./lib/core/session-verify-state.cjs'); ['readVerifyState','writeVerifyState','mergeVerifyState','verifyDir','statePath','detectVersionChange','buildStatuslineSegment','shortVersionLabel','shouldAskEurekaOptin','recordEurekaOptin','buildEurekaOptinCard','resolveOptinVersion'].forEach(k=>{if(typeof m[k]!=='function') throw new Error('missing '+k)})"` exits 0.
    - The frozen D-07b constants are exported and shaped: `node -e "const m=require('./lib/core/session-verify-state.cjs'); if(typeof m.EUREKA_OPTIN_COPY!=='string'||m.EUREKA_OPTIN_COPY.length===0) process.exit(1); if(m.EUREKA_OPTIN_CHOICES.join(',')!=='installed,declined,never') process.exit(1); if(m.EUREKA_OPTIN_OPTIONS.map(o=>o.label).join('|')!=='Install now|Not now|Never ask again') process.exit(1); if(m.EUREKA_OPTIN_OPTIONS.map(o=>m.EUREKA_OPTIN_CHOICE_BY_OPTION_ID[o.id]).join('|')!=='installed|declined|never') process.exit(1); if(typeof m.EUREKA_OPTIN_GATE_PREFIX!=='string'||m.EUREKA_OPTIN_GATE_PREFIX.length===0) process.exit(1)"` exits 0. The map arm is deliberately written as a JOIN over the options array rather than as three literal lookups, so an option id renamed in one place and not the other fails here.
    - The locked copy is verbatim: `grep -c 'It needs a one-time 380 MB local model download. Install it now?' lib/core/session-verify-state.cjs` returns 1, and `grep -c 'Eureka spots connections between your claims by meaning, not keywords.' lib/core/session-verify-state.cjs` returns 1. Quote the whole constant in the SUMMARY so a reviewer can diff it against D-07b (c) by eye.
    - D-07b (e) one authority, no second marker: `grep -rEln 'eureka[-_]optin' --include=*.cjs --include=*.json lib/ scripts/ data/ bin/ hooks/ | xargs -r grep -lE "(writeFileSync|renameSync|appendFileSync)" | grep -v 'lib/core/session-verify-state.cjs' | grep -v '^tests/'` prints nothing. Every opt-in write in the tree goes through this module. List whatever it does print in the SUMMARY with a disposition.
    - The module never throws across its boundary: `MOS_SESSION_VERIFY_HOME=/nonexistent-path-$$ node -e "const m=require('./lib/core/session-verify-state.cjs'); if(m.readVerifyState()!==null) process.exit(1); if(m.detectVersionChange('X').changed!==true) process.exit(1)"` exits 0.
    - `grep -c "'--post-update'" scripts/doctor.cjs` returns 0 and `grep -c 'postUpdate' scripts/doctor.cjs` returns 0.
    - `grep -c "activation-reached-the-wire" scripts/doctor.cjs` is at least 1 (the surviving point was NOT deleted).
    - The statusline signal is re-sourced, not deleted: `grep -c 'post_update' lib/statusline/cockpit-signals.cjs` is at least 1, `grep -c 'session-verify-state' lib/statusline/cockpit-signals.cjs` is at least 1, and `grep -c 'existsSync' lib/statusline/cockpit-signals.cjs` returns its pre-edit count minus at least 1 (the touch-file probe is gone). Record the pre-edit and post-edit counts in the SUMMARY.
    - `lib/statusline/cockpit-renderer.cjs` is NOT modified: `git diff --name-only lib/statusline/cockpit-renderer.cjs` prints nothing.
    - The statusline still renders and never blanks: `echo '{}' | node scripts/context-monitor` exits 0 and writes at least one byte to stdout.
    - The one segment is composed through the module: `grep -c 'buildStatuslineSegment' scripts/context-monitor` is at least 1, and `grep -c "' · v'" scripts/context-monitor` is at least 1 (the graceful fallback survives).
    - `node tests/test-statusline-glyph-isolation.cjs` exits 0 (neither new glyph collides with the three carve-out glyphs). State the confirmation in the SUMMARY.
    - `grep -rc 'post-update-restart-pending' commands/ scripts/ lib/ hooks/ bin/ data/ tests/ 2>/dev/null | grep -v ':0$'` prints nothing.
    - `node tests/test-341-session-verify-state.cjs` exits 0 with `FAIL=0` and all fifteen arms run (the nine D-07a arms plus the six D-07b arms: merge-preserves-siblings, the verbatim copy and option labels, the six fixture verdicts, the rule-precedence pair, the recorder's valid and invalid paths, and the card-survives-normalizeCard arm).
    - `node tests/test-341-update-collapsed.cjs` exits 0 with `FAIL=0` and all eight arms run, including the SessionStart-hook-count arm reporting 9.
    - `bash -n tests/run-all-341.sh` exits 0 and `bash tests/run-all-341.sh` reports `FAIL=0` and `EXPECTED-RED=0`.
    - `grep -c "$(printf '\xe2\x80\x94')" lib/core/session-verify-state.cjs lib/statusline/cockpit-signals.cjs scripts/context-monitor tests/test-341-session-verify-state.cjs tests/test-341-update-collapsed.cjs` reports 0 for each file.
  </acceptance_criteria>

  <done>One module owns the post-update signal, the last-seen marker, the statusline segment string and the D-07b opt-in record (its locked copy, its three options, its pure four-state decision function and its recorder), with `mergeVerifyState` making a verification write structurally incapable of erasing a `never`; the touch-file writer and the doctor flag are gone with no orphans; the statusline's post-update signal keeps its key and its consumer while changing to a real source; the version suffix is now one version-plus-glyph segment with a byte-identical fallback; the npm-reconcile backstop, the preflight hook host and the activation-reached-the-wire point are demonstrably still there; and an eight-arm tripwire keeps every one of those facts true.</done>
</task>

<task type="auto">
  <name>Task 4: Add the doctor session-start acceptance tier and declare it as two harness policies</name>

  <files>scripts/doctor.cjs, data/harness-policies/_schema.json, data/harness-policies/session-start-verification.json, data/harness-policies/eureka-optin-once.json, scripts/check-session-start-verification.cjs, scripts/check-eureka-optin-once.cjs, data/harness-manifest.json, tests/test-341-session-start-tier.cjs, tests/test-341-eureka-optin-once.cjs, tests/run-all-341.sh</files>

  <read_first>
    - `scripts/doctor.cjs` lines 165-190 (the flags object and the comment block explaining the `pre-tag` / `pre-flight` tier history, including the Phase 126.1 hotfix reasoning), :280-283 (the `--acceptance` / `--pre-tag` / `--pre-flight` argv branches), :322-330 (`--pre-tag` and `--pre-flight` both imply `--acceptance`, and the stated precedence), :425-440 (the help text for those three flags), and :1845-1875 (`runAcceptance`: it calls `buildAcceptanceChecklist(...)` FIRST and computes `mode` SECOND, then filters on `p.applies_to.indexOf(mode)`).
    - `scripts/doctor.cjs` the five points D-07a (b) names, each with its `applies_to` line, its `severity`, and its `DOCTOR_TEST_FAIL_POINT` guard: `install-state` at :726-745 (`applies_to: ['pre-tag','full']`), `session-start-active-version` at :1089-1140 (`['pre-tag','full']`), `harness-policies` at :1387-1432 (`['pre-tag','full']`, spawns `scripts/run-harness.cjs --check --json` with a 30 s timeout), `activation-reached-the-wire` at :1454-1545 (`['full']` only, with the stated reason that the wire probe needs a live MCP server, and the `DOCTOR_SKIP_ACTIVATION_GATE=1` escape hatch that returns `ok:true` with `detail.skipped`), and `mcp-surface-tool-count` at :1816-1842 (`['pre-tag','full']`, calls the organ in-process, Canon Part 8 zero network, with a `DOCTOR_SKIP_MCP_SURFACE=1` escape hatch).
    - `data/harness-policies/_schema.json` in full - specifically `applies_to_vocabulary` (a CLOSED five-member enum: pre-flight, pre-tag, full, stop-hook, room), `rung_vocabulary`, `kind_vocabulary`, the eleven `policy_fields`, `runner_path_rule`, `promotion_rule_doc`, and `validation_rule` ("an unknown applies_to member ... is a hard --check failure").
    - `data/harness-policies/release-payload-ceiling.json` (written by plan 341-04) - the exact field order and the `notes` voice to copy. This is the closest sibling in age and idiom.
    - `scripts/run-harness.cjs` :140-190 (`loadPolicies` and `classifyPolicy`, including the `unknown applies_to member` rejection at :181-186), :280-290 (`filterByTier`, which is fully generic and needs NO change for a new tier), and :810-870 (the `--tier` argv parsing).
    - `scripts/build-harness-manifest.cjs` :40-55 (the three-branch main: default write, `--check`, `--refresh`) and :990-1040 (`runCheck` regenerating from live sources and failing closed on drift). A new policy requires a manifest REGENERATION, not just a `--check`.
    - `scripts/check-shape-declaration.cjs` lines 30-48 - the `CHECK_SHAPE_DECLARATION_ROOT` seam idiom to copy for the new runner's root override.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-PATTERNS.md` section "data/harness-policies/release-payload-ceiling.json and registry-drift.json" and section "scripts/check-release-payload-ceiling.cjs and scripts/check-registry-drift.cjs" - the policy-file copy-shape and the gate-runner shape (`--check`, argv array spawns, `usage` plus `process.exit(2)` on an unknown flag, `module.exports` exposing the internals a test needs).
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-CONTEXT.md` decision D-07b sub-point (f) in full: the policy id is `eureka-optin-once`, it sits in the `session-start` tier, its rung is `logged` at runtime, its runner exercises SIX named fixture states, the same runner is wired into `tests/run-all-341.sh` as an always-green leg so the dev side is blocking, and a doctor line in the `--session-start` tier reports the current opt-in state honestly.
    - `lib/core/session-verify-state.cjs` as Task 3 wrote it: `shouldAskEurekaOptin`, `recordEurekaOptin`, `readVerifyState`, `EUREKA_OPTIN_CHOICES`. This task's runner and doctor point CALL those; neither re-implements the rule, and a second copy of the four-state logic anywhere is the defect this task exists to prevent.
    - `scripts/doctor.cjs` `runAcceptance` at :1908-1935 one more time, specifically the reduction at :1927 (`if (!r.ok) failed.push(p.id);`). It is severity-BLIND: any point returning `ok:false` fails the tier no matter what its `severity` string says. That is why step 2b's reporter point returns `ok:true` on every path and carries its state in `detail`; write that reasoning into the point's own comment so nobody later "fixes" it into a gate.
    - `lib/core/doctor/class-s-eureka-smoke.cjs` `LAYERS` (five members as of plan 341-03) and the `model_installed` layer id. The reporter point reads that layer's `ok` as one of D-07b (b)'s two installed signals; read how `checkEurekaSmoke` returns its layers array so the point reads the right field.
  </read_first>

  <action>
D-07a (b) says the session-start verification uses "the doctor points that already exist". The
correct reuse of an existing selection mechanism is a new TIER on the existing acceptance checklist,
not a second checklist and not a hand-rolled point list inside a hook. That is what this task builds.

1. `scripts/doctor.cjs`, tier plumbing:
   - Add `sessionStart: false` to the flags object beside `preTag` and `preFlight`, with a comment
     naming Phase 341 D-07a (b) and stating what the tier is for: the bounded, OFFLINE subset the
     SessionStart hook runs once per install or update, inside a 12 second hook budget.
   - Add `else if (arg === '--session-start') flags.sessionStart = true;` to the argv router.
   - Add `if (flags.sessionStart) flags.acceptance = true;` beside the two existing implications,
     and extend the precedence comment: `--pre-tag` still wins, then `--pre-flight`, then
     `--session-start`, then `full`.
   - Add the help text line beside the `--pre-tag` and `--pre-flight` lines.
   - In `runAcceptance`, compute `mode` BEFORE calling `buildAcceptanceChecklist` and pass it in as
     `opts.mode`. This is a reorder of two existing statements, not a rewrite. `mode` becomes
     `flagPreTag ? 'pre-tag' : (flagPreFlight ? 'pre-flight' : (flagSessionStart ? 'session-start' : 'full'))`.
     Thread `flagSessionStart` through `runAcceptance`'s opts from the call site at :3090.

2. `scripts/doctor.cjs`, tier membership. Add the literal `'session-start'` to `applies_to` on
   exactly these four points, leaving every existing member in place:
   `install-state`, `session-start-active-version`, `mcp-surface-tool-count`, `harness-policies`.
   Do NOT add it to `activation-reached-the-wire`. Write a comment at that point saying why, in one
   sentence: it spawns a real MCP handshake over the network, the inline session-start leg is
   offline by contract, and D-07a (d) puts the wire probe on the DETACHED leg instead, which Task 5
   runs by clearing `DOCTOR_SKIP_ACTIVATION_GATE` on a `full`-mode run. Every other point keeps its
   current `applies_to` untouched.

2b. `scripts/doctor.cjs`, the D-07b (f) doctor line. Add ONE new acceptance point,
   `eureka-optin-state`, `severity: 'info'`, `applies_to: ['session-start', 'full']`, placed
   immediately after `install-state` so a reader finds the two install-shaped points together.
   This brings the session-start tier to FIVE points: the four D-07a (b) points above plus this
   one. Say so in the point's comment, and say why the fifth one is cheap enough to sit inside a
   12 second hook budget: it is one JSON read of a file the tier's other points already touched
   the directory of, with no spawn and no network.

   Its `run` reads `readVerifyState()` from `lib/core/session-verify-state.cjs` (lazy require
   inside a try) and returns, on EVERY path, `{ ok: true, finding: null, detail: { eureka_optin:
   <the record or null>, sentence: <one honest sentence> } }`. FOUR answer sentences, one for each
   state the record can be in, and no fifth answer sentence:
   "Eureka opt-in: not yet asked" when there is no record; "Eureka opt-in: installed at <version>";
   "Eureka opt-in: declined for <version>", extended to "declined for <version> (install failed:
   <reason>)" when the record carries `install_failed`; and "Eureka opt-in: never (cleared by
   running /mos:eureka enable)". Plus one non-answer fallback: a read failure returns the same
   envelope with `sentence` set to "Eureka opt-in: state unreadable", which reports the failure
   honestly and is still `ok: true` because an unreadable marker is not the navigator's fault and
   is not a release defect.

   This point is a REPORTER, not a gate, and the plan says so out loud so a checker does not read
   it as a vacuous check: `runAcceptance`'s reduction is severity-blind, so any point that can
   return `ok:false` FAILS the tier, and D-07b (f) says a navigator's machine is never blocked by
   the opt-in behavior. The enforcement for that behavior lives in two other places, both real:
   the `eureka-optin-once` policy at rung `logged` (step 6b) and the always-green blocking leg in
   `tests/run-all-341.sh` (step 9). Put those two pointers in the point's comment.

3. `scripts/doctor.cjs`, the harness point's tier awareness. `harness-policies` currently spawns
   `run-harness.cjs --check --json` with no tier filter, which would run every policy inside the
   hook budget. In `session-start` mode ONLY, append `['--tier', 'session-start']` to that argv
   array so exactly the two session-start policies run (`session-start-verification` from step 6
   and `eureka-optin-once` from step 6b) and nothing else. Every other mode keeps the current
   argv byte-for-byte. Read `opts.mode` from the checklist builder's own opts; do not read a
   global.

4. Extend `data/harness-policies/_schema.json`: add `"session-start"` to `applies_to_vocabulary`,
   making it a six-member closed enum. Add one sentence to `_doc.validation_rule` or a sibling key
   recording that the member was added in Phase 341 for D-07a (f) and what it means: the tier the
   SessionStart hook runs once per install or update, offline and bounded. Change nothing else in
   the schema; the runner's `filterByTier` is already generic, so no `scripts/run-harness.cjs` edit
   is needed. Confirm that by reading `filterByTier` and state the confirmation in the SUMMARY.

5. Create `scripts/check-session-start-verification.cjs`, the policy runner. CJS, executable
   shebang, `'use strict'`, house header contract naming Phase 341, D-07a (f), Canon Parts 7 and 8,
   the seams `CHECK_SESSION_START_ROOT` (repo root override, the `check-shape-declaration.cjs`
   idiom) and `MOS_SESSION_VERIFY_HOME` (the state root, the same seam
   `lib/core/session-verify-state.cjs` uses), and the house rule line. Zero network, zero spawn of
   anything but node on a repo-relative script, every filesystem call wrapped.

   It asserts, each as a separately reported violation with a one-line remedy:
   - `hooks/hooks.json` parses, and `scripts/sessionstart-post-update-preflight.cjs` is registered
     under `hooks.SessionStart` EXACTLY once.
   - That hook entry's `timeout` is present and is at most 12000, which is the budget D-07a (b)
     binds the verification to.
   - The SessionStart hook entry count is 9 (no tenth hook), read from the parsed JSON.
   - `scripts/doctor.cjs` declares the `session-start` tier: the literal `'--session-start'` is
     present, and exactly the four point ids named in step 2 above carry `'session-start'` in their
     `applies_to`. Assert the SET, both directions, so adding a fifth point silently is a violation
     and dropping one is too.
   - `lib/core/session-verify-state.cjs` exists and `require`s cleanly, exporting the seven
     functions of its contract.
   - `scripts/session-verify-probe.cjs` exists (Task 5 creates it; until then this arm reports a
     violation, which is correct and harmless at rung `logged`).
   - The last recorded verification, if any, did not fail: read `readVerifyState()` and report a
     violation only when `state.local.ok === false`. An ABSENT state is NOT a violation; a machine
     that has not verified yet has nothing to answer for. This is the only arm that reads runtime
     state, and it is the reason the policy is `logged` rather than `blocking`: on a navigator's
     machine a failed verification is surfaced, never blocking (D-07a (f)).

   CLI: `--check` runs the assertions and prints a report; `--json` emits the result object;
   an unknown flag prints `usage:` and `process.exit(2)`. Exit 0 when there are no violations,
   1 otherwise. `if (require.main === module) main();` plus a `module.exports` exposing the
   individual assertion functions and a `runChecks(rootDir)` so the test can drive them hermetically.

6. Create `data/harness-policies/session-start-verification.json`, copying
   `release-payload-ceiling.json`'s field order exactly:
   `id: "session-start-verification"`, `kind: "gate"`,
   `runner: "scripts/check-session-start-verification.cjs"`, `args: ["--check"]`,
   `rung: "logged"`, `evidence_log: null`,
   `promotion_rule` with `window_runs`, `max_false_positive_rate` and `min_true_positives` chosen
   and JUSTIFIED in `notes` (do not copy the ceiling policy's numbers without a reason),
   `owner: "doctor"`, `pinned_by: ["tests/test-341-session-start-tier.cjs"]`,
   `applies_to: ["session-start"]`, and a `notes` paragraph, hyphens only, stating in plain prose
   why it enters at `logged` and not `blocking`: D-07a (f) says a failed verification on a
   navigator's machine is surfaced, never blocking, and a `blocking` rung inside a SessionStart hook
   would let a self-check refuse the session it exists to reassure. Name the promotion condition
   that would justify raising it.

6b. Create `scripts/check-eureka-optin-once.cjs`, the D-07b (f) runner, and
   `data/harness-policies/eureka-optin-once.json`, its policy file.

   The runner: CJS, executable shebang, `'use strict'`, house header contract naming Phase 341,
   D-07b (f), Canon Parts 7 and 8, the seams `CHECK_SESSION_START_ROOT` and
   `MOS_SESSION_VERIFY_HOME`, and the house rule line. ZERO network, ZERO spawn of anything at
   all (unlike the step 5 runner, this one spawns nothing: it requires the decision function and
   calls it), every filesystem call wrapped.

   It requires `shouldAskEurekaOptin` from `lib/core/session-verify-state.cjs` and drives it over
   the SIX fixture states D-07b (f) names by literal name, asserting BOTH the boolean verdict and
   the `defer` flag, each mismatch reported as its own violation with a one-line remedy:
   - `fresh`: `{ installedNow: false, state: null, currentVersion: 'V', firstInstallRewardPending:
     false }` MUST fire (`ask: true`, `defer: false`).
   - `installed`: `{ installedNow: true, ... }` MUST NOT fire. Assert `reason` is
     `already_installed`.
   - `declined-same-version`: state carries `eureka_optin { choice: 'declined', version: 'V' }`
     and `currentVersion` is `'V'`. MUST NOT fire.
   - `declined-older-version`: state carries `eureka_optin { choice: 'declined', version: 'U' }`
     and `currentVersion` is `'V'`. MUST fire. This is the D-07b (d) exactly-once re-ask, and it
     is the one fixture whose verdict a naive "already answered, never ask again" implementation
     gets wrong, so it is the fixture that earns the policy.
   - `never`: state carries `eureka_optin { choice: 'never', version: 'U' }` and `currentVersion`
     is `'V'`. MUST NOT fire even though the version moved.
   - `first-install-reward-pending`: fresh state plus `firstInstallRewardPending: true`. MUST NOT
     fire AND MUST return `defer: true`. Assert both; a plain no-fire here would be a different
     and wrong behavior (it would drop the card for the whole session instead of deferring one
     turn).
   The six fixture objects are declared as ONE exported `FIXTURES` array with a `name`, an
   `input` and an `expect` per member, so the test in step 8 can assert the array's length is 6
   and that its names are exactly D-07b (f)'s six, rather than trusting the runner's own report.

   It also asserts two structural facts that keep the rule single-sourced:
   - `lib/core/session-verify-state.cjs` exports `shouldAskEurekaOptin`, `recordEurekaOptin` and
     `EUREKA_OPTIN_COPY` (a missing export is a violation naming the export).
   - `EUREKA_OPTIN_COPY` is non-empty and contains the literal `380 MB`, so a silent re-wording of
     the locked copy is a reported violation rather than a silent drift.

   CLI: `--check` runs the assertions and prints a report; `--json` emits the result object; an
   unknown flag prints `usage:` and `process.exit(2)`. Exit 0 when there are no violations, 1
   otherwise. `if (require.main === module) main();` plus a `module.exports` exposing `FIXTURES`
   and `runChecks(rootDir)`.

   The runner reads NO runtime state. That is deliberate and it is the difference between this
   policy and the step 6 one: a navigator who answered `never` is not in violation of anything,
   so there is nothing about their machine for this policy to judge. What it judges is the RULE,
   which is why it is green on every machine and why it can be a blocking leg on the dev side
   while staying `logged` at runtime. Write that sentence into the runner's header.

   The policy file: copy `session-start-verification.json`'s field order exactly.
   `id: "eureka-optin-once"`, `kind: "gate"`, `runner: "scripts/check-eureka-optin-once.cjs"`,
   `args: ["--check"]`, `rung: "logged"`, `evidence_log: null`, a `promotion_rule` whose three
   numbers are chosen and JUSTIFIED in `notes`, `owner: "doctor"`,
   `pinned_by: ["tests/test-341-eureka-optin-once.cjs", "tests/test-341-eureka-optin-card.cjs"]`,
   `applies_to: ["session-start"]`, and a `notes` paragraph, hyphens only, stating in plain prose
   why it enters at `logged`: D-07b (f) says a navigator's machine is never blocked by the opt-in
   behavior, and the blocking enforcement lives on the dev side in `tests/run-all-341.sh`, which
   is where a rule regression is actually caught before it ships. Name the promotion condition
   that would justify raising it.

7. Regenerate the manifest: run `node scripts/build-harness-manifest.cjs` (the default write branch),
   then `node scripts/build-harness-manifest.cjs --check` must exit 0. Commit the regenerated
   `data/harness-manifest.json` in the same commit as BOTH policy files; a policy without its
   manifest entry fails `--check` and takes the release gate red.

8. Create `tests/test-341-session-start-tier.cjs`, hermetic, zero network. Arms:
   - `node scripts/doctor.cjs --help` output contains `--session-start`.
   - `node scripts/doctor.cjs --acceptance --session-start --json` exits 0 or 1 (either verdict is
     valid) and its JSON parses with `mode === 'session-start'` and a `points` array whose ids are
     EXACTLY the FIVE-member set from steps 2 and 2b, in both directions: the four D-07a (b)
     points plus `eureka-optin-state`. This is the arm that proves the tier selects what D-07a (b)
     and D-07b (f) together say it selects.
   - The wire point is NOT in the session-start set: the returned ids do not contain
     `activation-reached-the-wire`.
   - The tier is bounded: measure the wall-clock elapsed time of that same invocation and assert it
     is under 12000 ms. Record the MEASURED number in the SUMMARY; it is the number Task 5's budget
     is derived from and the first real measurement of it.
   - `DOCTOR_TEST_FAIL_POINT=install-state node scripts/doctor.cjs --acceptance --session-start --json`
     exits non-zero and reports `install-state` in `failed_points`, proving the tier's failure path
     is wired and countable (the statusline glyph depends on that count being real).
   - `node scripts/run-harness.cjs --tier session-start --json` parses and reports exactly TWO
     policies, `session-start-verification` and `eureka-optin-once`, both at the `logged` rung.
   - `node scripts/build-harness-manifest.cjs --check` exits 0.
   - The schema enum grew by exactly one member: parse `data/harness-policies/_schema.json` and
     assert `applies_to_vocabulary` has 6 members and contains `session-start`.
   - `node scripts/check-session-start-verification.cjs --bogus` exits 2 with a `usage:` line.
   - Canon Part 8: `scripts/check-session-start-verification.cjs` source, with comment lines filtered
     out (`grep -v '^\s*[*/#]'`), contains no `http`, `https` or `fetch` token.

   Create `tests/test-341-eureka-optin-once.cjs` as a SEPARATE hermetic suite for the D-07b (f)
   half, so the two policies' pins do not entangle. Arms:
   - `node scripts/check-eureka-optin-once.cjs --check` exits 0 on a clean tree, and
     `--bogus` exits 2 with a `usage:` line.
   - The runner's exported `FIXTURES` array has exactly 6 members and its `name` values are
     exactly `fresh`, `installed`, `declined-same-version`, `declined-older-version`, `never`,
     `first-install-reward-pending` (assert the SET both directions, so a seventh fixture added
     without a decision is caught and a dropped fixture is too).
   - The runner is a caller, not a second implementation: its source contains
     `shouldAskEurekaOptin` and contains NO literal `'declined'` comparison of its own outside
     the `FIXTURES` array (assert by reading the file and checking that every occurrence of
     `'declined'` is inside the fixture block, which the test locates by the exported array's own
     source markers). Record how you drew that boundary in the SUMMARY.
   - A real regression is caught: monkeypatch the required decision function in-process to always
     return `{ ask: true }`, re-run `runChecks`, and assert it reports violations for the three
     no-fire fixtures. Restore in a `finally`. This is the arm that proves the runner is a
     tripwire and not a tautology, in the `tests/test-341-eureka-no-brain-reach.cjs` idiom plan
     341-03 established.
   - `eureka-optin-state` is in the session-start tier and never fails it: run
     `node scripts/doctor.cjs --acceptance --session-start --json` four times with
     `MOS_SESSION_VERIFY_HOME` pointed at four temp trees seeded with no record, `installed`,
     `declined` (with and without `install_failed`) and `never`, and assert in every run that the
     point is present, `ok` is `true`, and `detail.sentence` names the right state. The
     `install_failed` seed must produce a sentence containing both `declined` and `install
     failed`, which is the honest-failure reporting D-07b leaves to this plan.
   - Canon Part 8: `scripts/check-eureka-optin-once.cjs` source, with comment lines filtered out
     (`grep -v '^\s*[*/#]'`), contains no `http`, `https`, `fetch` or `spawn` token. This runner
     spawns nothing at all.

9. `tests/run-all-341.sh`: append three legs immediately after the `341: session-verify-state` leg
   from Task 3, in the file's exact two-line convention:
   - `run_if "341: session-start-tier"` guarded on `tests/test-341-session-start-tier.cjs`.
   - `run_if "341: eureka-optin-once"` guarded on `tests/test-341-eureka-optin-once.cjs`.
   - `run_if "341: eureka-optin-once runner (D-07b f, blocking on the dev side)"` guarded on
     `scripts/check-eureka-optin-once.cjs`, running `node scripts/check-eureka-optin-once.cjs
     --check`. Guarding on the RUNNER's own path (not a test file) is what makes this the
     always-green blocking leg D-07b (f) asks for: once the runner exists, `run_if` behaves
     exactly like `run`, so any six-fixture mismatch is a `FAIL` in the phase aggregator even
     though the runtime rung stays `logged`.
   Also extend the `PHASE_341_SURFACES` array (the em-dash guard's file list, `tests/run-all-341.sh`
   :72-90) with the paths this plan adds or edits that are not already listed:
   `lib/core/session-verify-state.cjs`, `scripts/session-verify-probe.cjs`,
   `scripts/sessionstart-post-update-preflight.cjs`, `scripts/first-install-router.cjs`,
   `scripts/check-session-start-verification.cjs`, `scripts/check-eureka-optin-once.cjs`,
   `lib/mcp/tools/gate.cjs`, `lib/mcp/tools/room.cjs`,
   `data/harness-policies/session-start-verification.json`,
   `data/harness-policies/eureka-optin-once.json`. All ten are em-dash free on HEAD today
   (verified during planning), so adding them cannot turn the guard red on arrival; re-verify
   before adding and report the check in the SUMMARY.
  </action>

  <verify>
    <automated>node tests/test-341-session-start-tier.cjs &amp;&amp; node tests/test-341-eureka-optin-once.cjs &amp;&amp; node scripts/check-eureka-optin-once.cjs --check &amp;&amp; node scripts/run-harness.cjs --tier session-start --json &amp;&amp; node scripts/build-harness-manifest.cjs --check &amp;&amp; node scripts/run-harness.cjs --check &amp;&amp; node scripts/doctor.cjs --acceptance --pre-tag</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/doctor.cjs --help | grep -c -- '--session-start'` is at least 1.
    - `node scripts/doctor.cjs --acceptance --session-start --json` emits parseable JSON with `mode` equal to `session-start`.
    - The tier selects exactly five points: `node scripts/doctor.cjs --acceptance --session-start --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const got=j.points.map(p=>p.id).sort();const want=['eureka-optin-state','harness-policies','install-state','mcp-surface-tool-count','session-start-active-version'];process.exit(JSON.stringify(got)===JSON.stringify(want)?0:1)})"` exits 0.
    - The reporter point never fails the tier: `node scripts/doctor.cjs --acceptance --session-start --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const p=j.points.find(x=>x.id==='eureka-optin-state');process.exit(p&&p.ok===true&&p.detail&&typeof p.detail.sentence==='string'?0:1)})"` exits 0, and `grep -c "severity: 'info'" scripts/doctor.cjs` is at least 1. Quote the four answer sentences plus the unreadable-state fallback in the SUMMARY.
    - The wire point is deliberately absent from the inline tier: the same JSON's point ids do not include `activation-reached-the-wire`, and the comment at that point states why. Record the four edited line numbers in the SUMMARY.
    - The tier is bounded: the measured wall-clock time of `node scripts/doctor.cjs --acceptance --session-start --json` is under 12000 ms. Record the MEASURED milliseconds in the SUMMARY; Task 5's inline budget is derived from this number, so it must be a measurement and never an estimate.
    - The failure path is countable: `DOCTOR_TEST_FAIL_POINT=install-state node scripts/doctor.cjs --acceptance --session-start --json` exits non-zero and its `failed_points` array contains `install-state` with `summary.failed` equal to 1.
    - Every other tier is unchanged: `node scripts/doctor.cjs --acceptance --pre-tag` and `node scripts/doctor.cjs --acceptance --pre-flight` both exit 0, and the pre-tag point count equals its pre-edit value (record both in the SUMMARY).
    - `node scripts/run-harness.cjs --tier session-start --json` reports exactly two policies, with `id` values `session-start-verification` and `eureka-optin-once`, both with `rung` equal to `logged`.
    - The D-07b runner is real and offline: `node scripts/check-eureka-optin-once.cjs --check` exits 0, `node scripts/check-eureka-optin-once.cjs --bogus` exits 2 with a `usage:` line, and `node -e "const m=require('./scripts/check-eureka-optin-once.cjs'); if(!Array.isArray(m.FIXTURES)||m.FIXTURES.length!==6) process.exit(1); if(typeof m.runChecks!=='function') process.exit(1)"` exits 0.
    - The runner spawns nothing and reaches no network: `grep -vE '^\s*[*/#]' scripts/check-eureka-optin-once.cjs | grep -Ec 'http|https|fetch|spawn'` returns 0.
    - The rule is single-sourced: `grep -c 'shouldAskEurekaOptin' scripts/check-eureka-optin-once.cjs` is at least 1, and `grep -c 'shouldAskEurekaOptin' scripts/doctor.cjs` returns 0 (the doctor point REPORTS the record, it does not re-decide).
    - `node scripts/run-harness.cjs --check` exits 0 and `node scripts/build-harness-manifest.cjs --check` exits 0.
    - The schema enum grew by exactly one: `node -e "const s=require('./data/harness-policies/_schema.json'); const v=s._doc.applies_to_vocabulary; if(v.length!==6||v.indexOf('session-start')===-1) process.exit(1)"` exits 0.
    - `scripts/run-harness.cjs` is NOT modified: `git diff --name-only scripts/run-harness.cjs` prints nothing. Record in the SUMMARY the line number of `filterByTier` that proves it needed no change.
    - `node scripts/check-session-start-verification.cjs --check` runs and exits 0 or 1, never throws, and `node scripts/check-session-start-verification.cjs --bogus` exits 2 with a `usage:` line.
    - `node -e "const m=require('./scripts/check-session-start-verification.cjs'); if(typeof m.runChecks!=='function') process.exit(1)"` exits 0.
    - `node tests/test-341-session-start-tier.cjs` exits 0 with `FAIL=0` and all ten arms run.
    - `node tests/test-341-eureka-optin-once.cjs` exits 0 with `FAIL=0` and all six arms run, including the monkeypatched-decision-function regression arm and the four-state reporter arm.
    - The aggregator carries the always-green blocking leg: `grep -c 'check-eureka-optin-once.cjs --check' tests/run-all-341.sh` is at least 1, and `bash tests/run-all-341.sh` reports that leg as `PASSED` (not `SKIPPED`).
    - The em-dash guard covers this plan's surfaces: `node -e "const s=require('fs').readFileSync('tests/run-all-341.sh','utf8'); ['lib/core/session-verify-state.cjs','scripts/check-eureka-optin-once.cjs','data/harness-policies/eureka-optin-once.json','lib/mcp/tools/gate.cjs','lib/mcp/tools/room.cjs'].forEach(p=>{if(s.indexOf(p)===-1){console.error('MISSING '+p);process.exit(1)}})"` exits 0.
    - `bash tests/run-all-341.sh` reports `FAIL=0` and `EXPECTED-RED=0`.
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/doctor.cjs scripts/check-session-start-verification.cjs scripts/check-eureka-optin-once.cjs data/harness-policies/_schema.json data/harness-policies/session-start-verification.json data/harness-policies/eureka-optin-once.json tests/test-341-session-start-tier.cjs tests/test-341-eureka-optin-once.cjs` reports 0 for each file.
  </acceptance_criteria>

  <done>A `session-start` acceptance tier selects exactly five offline points, the four D-07a (b) names plus the D-07b (f) opt-in reporter, measurably inside the 12 second hook budget, with the network wire point deliberately left for the detached leg and the reason written at the point; the harness schema carries a sixth tier member and `run-harness.cjs` needed no edit to honor it; TWO `logged` policies with offline runners and test seams declare the whole behavior, and the opt-in rule is additionally blocking on the dev side through an always-green aggregator leg; and the manifest is regenerated in the same commit so the release gate stays green.</done>
</task>

<task type="auto">
  <name>Task 5: Convert the SessionStart preflight into the version-change verification hook, fire the detached Theo probe, and deliver one insight</name>

  <files>scripts/sessionstart-post-update-preflight.cjs, scripts/session-verify-probe.cjs, scripts/first-install-router.cjs, hooks/hooks.json, tests/test-update-restart-cue.cjs, tests/test-341-session-start-verify-hook.cjs, tests/test-341-verify-insight-delivery.cjs, tests/run-all-341.sh</files>

  <read_first>
    - `scripts/sessionstart-post-update-preflight.cjs` in full. Specifically: the header's Canon discipline block and its "Part 7 (F8, added 2026-07-02)" paragraph; `TOUCH_FILE` and `MINDRIAN_DIR` at approximately :63-66; `INSTALLED_PLUGINS_JSON` and `PLUGIN_KEY` at :68-70; `ENVELOPE_ALLOWED`, `emitEnvelope`, `emitEmpty` and the `uncaughtException` handler at :72-92; the colour helpers at :94-99; `readPackageJsonVersion` at :101-120; `probeBrainSmokeVersion` at :122-148 (which spawns `doctor --brain-smoke --json`, finds the L4 layer by `l.id === 'mcp_stdio_handshake' || l.name === 'L4 MCP stdio handshake'`, and parses `server=mindrian-brain v<VERSION>`); `versionFromInstallPath`, `readSessionVersion`, `readRegisteredVersion` and `detectStaleRegistry` at :150-205; `formatStaleRegistryBanner` at :207-224; `formatBanner` at :226-244; and `main` at :246 onward, where the stale-registry cue runs FIRST and returns, then the touch-file gate, the wire probe, the version-of-record read, the match-and-unlink, and finally the `continue:false` exit-1 drift banner.
    - `lib/core/doctor/class-m-brain-smoke.cjs` lines 50-56 - the frozen `LAYERS` array. The L4 layer's `id` is the literal `stdio_handshake`, NOT `mcp_stdio_handshake`. The preflight's id match at :140 has ALWAYS been dead; only its `name` fallback fires. Record the discrepancy in the SUMMARY as a pre-existing fragility this plan documents rather than silently inherits.
    - `lib/core/brain-client.cjs` :304-306 (`isAvailable()`, a pure key check), :415-437 (`getAutoRegisterFailureReason`), :1879-1889 (`recommendChain(problemType, maxSteps)`: it calls `_normalizeBrainProblemType` FIRST and returns `{error:'invalid_problem_type'}` with zero network and zero key consumption when the handle fails the shape gate), :1802-1811 (`BRAIN_PROBLEM_TYPE_ALIASES_THEO`, where `udp` maps to `UnDefined`, `idp` to `IllDefined`, `wdp` to `WellDefined`), and the `recommend_chain` output contract comment at :1855-1878 (`{ tool, backend, grounded, problem_type, seed_candidates, chain: [{step, framework, ...}], note, execution_hint }`, with an unknown problem type GUARANTEED to return `grounded:false, chain:[]` rather than an error).
    - `lib/core/user-md-ops.cjs` :233-250 (`readUserMd(userMdPath, opts)` and the persona-override seam) and :337-342 (the `problem_type` field, validated against `taxonomy.PROBLEM_TYPES` and defaulted to the literal `unknown`).
    - `lib/core/persona-taxonomy.cjs` :125-137 - `PROBLEM_TYPES` is the frozen four-member enum `UDP`, `IDP`, `WDP`, `unknown`. Three of those four are the ONLY strings this plan is permitted to send to Theo; `unknown` sends nothing.
    - `lib/core/resolve-active-room.cjs` :575-592 - `resolveActiveRoomDir()` returns the active room's absolute path or `null`. `null` is the no-room-bound case and it means no handle and no Theo call.
    - `scripts/first-install-router.cjs` :134-190 (`homeDir`, `firstInstallDir`, `stateFilePath`, the envelope helpers and `ENVELOPE_ALLOWED`), :213-236 (`_readState` and `_atomicWriteState`), :297-330 (`_buildAdditionalContext`), :417-530 (`_fireReward`: the detached, unref-ed `spawn` with `stdio: ['ignore', fd, 'ignore']` writing to a capture file, and the `SKIP_REASONS` honest-skip discipline), :526-660 (`_drainReward`: the later-turn drain, the `drain_retries` counter, the `stableFallback` at `retriesSoFar >= 1`, and the `drain_timeout` terminal path that never wedges), and `main()` at :882-968 (the dispatch chain and its early returns).
    - `hooks/hooks.json` - the SessionStart array. The second entry is the preflight, `timeout: 12000`, `async: false`, `statusMessage: "Verifying post-update activation..."`. Read the surrounding array so an edit leaves valid JSON and disturbs no sibling; `sessionstart-npm-reconcile.cjs` (first entry, 120 s) MUST stay. Also read the `_firstInstallRouterOrdering` metadata block at the top: it records that `first-install-router.cjs` runs after `mva-detect.cjs` and that the ordering is load bearing.
    - `tests/test-update-restart-cue.cjs` in full (168 lines) - it pins `detectStaleRegistry` and the restart-cue envelope, both of which SURVIVE this conversion untouched.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-PATTERNS.md` "Shared Patterns": "spawnSync discipline (Canon Part 8)" (argv array always, `shell` never, `input: ''`, explicit timeout, output capped at 500 characters) and "Test-seam env override".
  </read_first>

  <action>
Three legs land here, in this order: the inline offline verification, the detached probe, the
delivered insight. Every one of them is non-blocking by construction.

LEG A: convert `scripts/sessionstart-post-update-preflight.cjs`.

Keep the filename and the registered path. Renaming would be the same hook with a different name,
it would churn `hooks/hooks.json`, `tests/test-update-restart-cue.cjs` and every doc reference, and
Task 2 already set this plan's precedent by refusing a cosmetic rename. Note in the header that
"preflight" is historical and that the file's job is now the D-07a (a) version-change verification.
Do NOT spell the retired touch-file name anywhere in the rewritten file; Task 3's tripwire scans the
live tree for that literal.

Delete: the `TOUCH_FILE` constant and the whole touch-file gate; the `probeBrainSmokeVersion`
function and its inline call; `formatBanner`; and the `continue:false` / exit-1 drift path. That
path could not distinguish a drifted version from an unreachable Brain and it refused Larry-load on
that ambiguity, which is exactly the failure D-07a (d) forbids. This hook now NEVER blocks: every
path emits `continue:true` and exits 0.

Keep, byte-unchanged where possible: `ENVELOPE_ALLOWED`, `emitEnvelope`, `emitEmpty`, the
`uncaughtException` handler, the colour helpers, `versionFromInstallPath`, `readSessionVersion`,
`readRegisteredVersion`, `detectStaleRegistry`, `formatStaleRegistryBanner`, and the Part 7 F8
stale-registry cue as the FIRST branch of `main()` with its existing early return. That cue answers
a different question ("an update landed under this running session") from the new one ("the running
version is not the version I last verified") and both are correct.

Add, after the F8 branch:

1. `const versionState = require('../lib/core/session-verify-state.cjs')` inside a try, so a degraded
   install still emits an empty envelope rather than throwing.
2. `const registered = readRegisteredVersion()`, then
   `versionState.detectVersionChange(registered)`. When `changed` is false, `emitEmpty()` and return.
   This is the common path on every ordinary session and it costs two JSON reads.
3. When `changed` is true, run the inline offline leg: `spawnSync` node on `scripts/doctor.cjs` with
   the argv array `['--acceptance', '--session-start', '--json']`, `input: ''`,
   `encoding: 'utf8'`, `cwd` the plugin root, and `timeout: SESSION_VERIFY_BUDGET_MS`. Declare
   `SESSION_VERIFY_BUDGET_MS` as a named constant with a comment carrying its provenance: the
   hooks.json entry allows 12000 ms and Task 4 MEASURED the tier at the number recorded in its
   SUMMARY. Set the constant to that measured value plus 25 percent, rounded up to the nearest 500,
   and capped at 10000 so the hook keeps at least 2000 ms of headroom for its own two JSON reads and
   the detached spawn. Do NOT raise the hooks.json timeout and do NOT shrink the point set; if the
   measurement leaves no room, the honest outcome is an `inconclusive` result, not a smaller check.
4. Parse the result. On a clean parse write the verification result through
   `versionState.mergeVerifyState` (NEVER `writeVerifyState`: the same file carries the D-07b
   `eureka_optin` record, and a raw overwrite here would erase a navigator's `never` on the next
   update, resurrecting a card D-07b (d) says is permanently answered) with `schema`,
   `last_seen_version` set to `registered`,
   `verified_at_ms`, `local` filled from the JSON (`ok` from the exit status, `ran: true`,
   `failed_count` from `summary.failed`, `failed_points`, `mode`, `elapsed_ms`),
   `wire: 'pending'`, `theo: 'pending'`, and `insight: { pending: false }` for now. On a timeout, a
   non-parse, or a spawn failure, write the SAME state with
   `local: { ok: null, ran: false, inconclusive_reason: <the reason> }`. Inconclusive is a third
   answer and it is written honestly; it is never coerced to a pass and never to a failure.
   Writing `last_seen_version` here, on every outcome including inconclusive, is deliberate: the
   verification runs once per version change, not once per session, so a machine that cannot verify
   does not re-spend the budget every session. State that in a comment.
5. Fire the detached probe: `spawn(process.execPath, [path.join(__dirname, 'session-verify-probe.cjs')], { detached: true, stdio: ['ignore','ignore','ignore'], env: process.env })`
   then `child.unref()`, exactly the `_fireReward` shape from `scripts/first-install-router.cjs`
   :475-486. Wrap in a try; a throw here degrades to no probe, never to a failed session start. The
   probe writes its own output through the state module, so no capture fd is needed at this level.
6. Emit ONE envelope with `continue: true`, a `systemMessage` carrying the verification line, and
   `hookSpecificOutput.additionalContext` telling Larry to open with that line in his own voice.
   The line states the version, the local verdict, and the failing count when there is one, and it
   says plainly that the Theo check is still running. Three states, three honest sentences: passed,
   failed with N named points, inconclusive with the reason. Keep the colour helpers for the failed
   form only; a pass is quiet.

LEG B: create `scripts/session-verify-probe.cjs`, the detached child.

CJS, executable shebang, `'use strict'`, house header contract naming Phase 341, D-07a (d) and (e),
Canon Parts 7, 8 and 12, the seams `MOS_SESSION_VERIFY_HOME` and `MOS_SESSION_VERIFY_PROBE_FIXTURE`,
and the house rule line. It is spawned detached and unref-ed, so it may take as long as it needs and
it must never write to the terminal: every output goes through
`lib/core/session-verify-state.cjs`'s `mergeVerifyState` (the merging writer, for the same reason
LEG A uses it: this process runs concurrently with the hook that spawned it and with whatever the
navigator answers on the D-07b card, and all three write the same file), and every path is
wrapped so the process exits 0 whatever happens.

Order of operations:

1. The wire leg. Spawn node on `scripts/doctor.cjs` with the argv array
   `['--acceptance', '--json']` (full mode, where `activation-reached-the-wire` lives) with an
   explicit timeout of 60000 ms and `input: ''`. Read the `activation-reached-the-wire` point out of
   the returned `points` array. Set `wire` to `'ok'` when that point is `ok` and its detail is not
   `skipped`, `'skipped'` when `detail.skipped` is true, and `'failed'` otherwise, carrying the
   point's `finding` into `theo_reason` on a failure. This is the Canon Part 7 reuse D-07a (d) asks
   for: the class M brain-smoke runs once, inside the point that already knows how to read it,
   rather than in a second hand-rolled handshake parser.
   Test seam: when `MOS_SESSION_VERIFY_PROBE_FIXTURE` names a readable JSON file, parse THAT instead
   of spawning anything. That is what makes the whole probe testable with zero network.
2. The Theo verdict, derived, not re-probed. `reachable` when the wire leg is `ok`. Otherwise call
   `brainClient.isAvailable()`: false means `refused` with the reason
   "Theo not reachable, verification is local-only" (the honest refusal of Decision #8, and the
   exact phrasing D-07a (d) specifies); true with a failed wire means `unreachable` carrying the
   point's finding. Never write a verdict the evidence does not support and never imitate a
   reachable Theo. Persist `theo` and `theo_reason`.
3. The handle. Only when `theo === 'reachable'`: resolve the active room with
   `resolveActiveRoomDir()`. `null` means no room is bound, so there is no handle, so there is NO
   Theo call and no insight; write `insight: { pending: false, handle: null }` and stop. With a
   room, read `<room>/USER.md` through `readUserMd` and take `problem_type`. The literal `unknown`
   is also a stop: it is not a handle. The ONLY three strings that may leave this process are `UDP`,
   `IDP` and `WDP`, which `recommendChain` normalizes to `UnDefined`, `IllDefined` and `WellDefined`
   before the wire. Assert that in a comment and enforce it in code with an explicit allowlist check
   against `PROBLEM_TYPES` before the call. Canon Part 8 holds by construction: no room name, no
   room path, no room content, no governing thought, no file bytes, nothing but the enum member.
4. The nugget. `await brainClient.recommendChain(handle, 3)`. Take at most the first three
   `chain[].framework` names plus the `note` when present. A result with `grounded === false` or an
   empty `chain` is a legitimate answer and it means NO insight: write `insight: { pending: false }`
   with a recorded reason and stop. Do not manufacture a nugget from a graph that did not give one.
5. The capture. Write the framing instruction to `<verifyDir>/insight-<version>.md`: the handle, the
   framework names, the note, and a one-paragraph instruction for Larry, in the repo's voice, to
   deliver ONE question or nudge and nothing more, GUIDED not delivered, with his De Stijl glyph, no
   lecture and no framework dump (Canon Part 12). Then update the state with
   `insight: { pending: true, capture_path, handle, drain_retries: 0 }`. Write the capture file
   FIRST and the state SECOND, so a reader never sees `pending: true` pointing at a file that does
   not exist yet.

LEG C: deliver the insight, in `scripts/first-install-router.cjs`.

D-07a (e) says reuse the Phase 267.2 detached-spawn delivery pattern rather than minting a second
mechanism, and the SessionStart hook cannot deliver a turn-scoped line. Add ONE new branch to this
hook and do not add a ninth UserPromptSubmit hook.

Place the branch at the TOP of `main()`, before the router's own state machine reads its state.json.
It fires ONLY when all of these hold, and otherwise falls through to the untouched existing chain:
`readVerifyState()` returns a state with `insight.pending === true`; `insight.delivered_for_version`
does not already equal the state's `last_seen_version` (the once-per-install-or-update marker
D-07a (e) requires, never once per session); the capture file exists with non-zero size; AND the
router's own `_readState()` returns null or a state whose `phase` is `done`. That last condition is
the important one: on a fresh install the 267.2 first-install reward already owns the first-turn
moment, and two rewards competing for one turn is worse than one arriving a turn later. When
onboarding is mid-flight the branch DEFERS (leaves `pending` true, increments `drain_retries`, falls
through) rather than competing. Say so in a comment.

When it fires: read the capture, emit `emitContinueWithContext(captureText)` using the router's
existing helper, then update the state with `insight.pending = false` and
`insight.delivered_for_version = <the state's last_seen_version>`. Bound it exactly as
`_drainReward` bounds itself: at `drain_retries >= 3` give up terminally, mark `pending:false` with
a recorded `drain_timeout` reason, and never wedge. Touch NOTHING else in this file: the router's
own state.json, its telemetry, its phase machine and its existing branches stay byte-unchanged, and
the new branch writes only through `lib/core/session-verify-state.cjs`.

Build this branch as a NAMED function (for example `_deliverVerifyInsight(state)`) called from the
top of `main()`, not as inline code in `main()`. Task 6 rides this same branch with the D-07b
opt-in card, and a named function is the difference between Task 6 appending to one place and Task
6 re-deriving the same conditions somewhere else. D-07b (a) is explicit that there is no second
delivery mechanism, and this is the structural decision that makes that true rather than aspirational.

LEG D: `hooks/hooks.json`. Change exactly one field: the preflight entry's `statusMessage`, from
"Verifying post-update activation..." to a sentence that matches what the hook now does. Keep
`timeout: 12000`, keep `async: false`, keep the entry's position in the array, keep every sibling.
The SessionStart entry count stays at 9. Validate with
`node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))"`.

LEG E: tests.

Extend `tests/test-update-restart-cue.cjs` rather than replacing it: its existing arms pin
`detectStaleRegistry` and the restart-cue envelope, both of which survive. Add one arm asserting the
hook NEVER exits non-zero and never emits `continue:false` on any input, which is the contract the
deleted drift path used to violate.

Create `tests/test-341-session-start-verify-hook.cjs`, hermetic via `MOS_SESSION_VERIFY_HOME` plus a
fake `installed_plugins.json` reached through a temp `HOME`. Arms:
- No version change: with a marker equal to the registered version, the hook emits an empty envelope,
  exits 0, and spawns no doctor (assert by wall clock, the run completes in well under a second, and
  by state, `verified_at_ms` is unchanged).
- Fresh install, no marker at all: the hook treats it as changed and runs the verification.
- Version changed and the tier passes: the state records `local.ok === true`, `last_seen_version`
  equals the registered version, `wire` is `pending`, and the emitted `systemMessage` contains the
  short version label.
- Version changed and the tier fails, driven by `DOCTOR_TEST_FAIL_POINT=install-state`: the state
  records `local.ok === false` with `failed_count` 1 and `install-state` in `failed_points`, the
  message names the failing count, and the hook STILL exits 0 with `continue:true`.
- Inconclusive: force a spawn failure (point the plugin root at a temp dir with no `doctor.cjs`) and
  assert `local.ran === false` with a non-empty `inconclusive_reason`, exit 0, and
  `last_seen_version` still written so the budget is not re-spent every session.
- The F8 cue still fires: reproduce the existing restart-cue fixture and assert the cue branch
  returns first and unchanged.
- Never blocks: across every arm above, the process exit code is 0 and the parsed envelope's
  `continue` is `true`.

Create `tests/test-341-verify-insight-delivery.cjs`, hermetic, zero network, driving the probe
through `MOS_SESSION_VERIFY_PROBE_FIXTURE` and the router through `MOS_SESSION_VERIFY_HOME`. Arms:
- Wire fixture `ok` plus a room whose USER.md carries `problem_type: IDP`: the probe records
  `theo: 'reachable'` and `insight.handle === 'IDP'`.
- Wire fixture failed plus `isAvailable()` false: the probe records `theo: 'refused'` and
  `theo_reason` contains the exact string `verification is local-only`, and `insight.pending` is
  false. This is the honest-refusal arm.
- No room bound (`resolveActiveRoomDir()` null): no Theo call is attempted at all and
  `insight.handle` is null. Assert the no-call by fixture, not by inspection.
- `problem_type: unknown`: same as no room. `unknown` is not a handle.
- Canon Part 8 egress: assert that the only room-derived value written anywhere into the state or
  the capture is a member of `['UDP','IDP','WDP']`. Plant a room whose USER.md carries a distinctive
  sentinel string in a non-problem_type field and assert that sentinel appears in NEITHER the state
  file NOR the capture file.
- Delivery once, not per session: with `insight.pending` true and a capture on disk, the router's
  branch emits the capture as additionalContext, sets `delivered_for_version`, and a SECOND
  invocation emits nothing.
- Deferral: with the router's own state.json at a non-`done` phase, the branch does not fire, the
  capture survives, `pending` stays true and `drain_retries` increments.
- Never wedges: at `drain_retries >= 3` the branch gives up, sets `pending:false`, and the router
  still emits its normal envelope.
- The router's own machine is untouched: with no verify state at all, the hook's behavior is
  byte-identical to its pre-edit behavior on the same input (capture both and compare).

`tests/run-all-341.sh`: append two `run_if` legs, `341: session-start-verify-hook` and
`341: verify-insight-delivery`, guarded on their own test files, in the file's exact two-line
convention, after the Task 4 leg.
  </action>

  <verify>
    <automated>node tests/test-341-session-start-verify-hook.cjs &amp;&amp; node tests/test-341-verify-insight-delivery.cjs &amp;&amp; node tests/test-update-restart-cue.cjs &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" &amp;&amp; bash tests/run-all-341.sh</automated>
  </verify>

  <acceptance_criteria>
    - The hook is converted, not replaced: `test -f scripts/sessionstart-post-update-preflight.cjs`, `grep -c 'sessionstart-post-update-preflight' hooks/hooks.json` returns 1, and `node -e "const j=JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')); process.exit(j.hooks.SessionStart.length===9?0:1)"` exits 0.
    - The hook budget is unchanged: `node -e "const j=JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')); const e=j.hooks.SessionStart.find(x=>JSON.stringify(x).includes('sessionstart-post-update-preflight')); process.exit(e.hooks[0].timeout===12000?0:1)"` exits 0.
    - The hook never blocks: `grep -c 'continue: false' scripts/sessionstart-post-update-preflight.cjs` returns 0 and `grep -c 'formatBanner' scripts/sessionstart-post-update-preflight.cjs` returns 0.
    - The F8 cue survives: `grep -c 'detectStaleRegistry' scripts/sessionstart-post-update-preflight.cjs` is at least 1, `grep -c 'formatStaleRegistryBanner' scripts/sessionstart-post-update-preflight.cjs` is at least 1, and `node tests/test-update-restart-cue.cjs` exits 0.
    - The trigger is the version change: `grep -c 'detectVersionChange' scripts/sessionstart-post-update-preflight.cjs` is at least 1 and `grep -c 'readRegisteredVersion' scripts/sessionstart-post-update-preflight.cjs` is at least 1.
    - The budget constant carries its provenance: `grep -c 'SESSION_VERIFY_BUDGET_MS' scripts/sessionstart-post-update-preflight.cjs` is at least 2, and the SUMMARY records the constant's value next to Task 4's measured tier time and shows the arithmetic.
    - The probe is detached: `grep -c 'detached' scripts/sessionstart-post-update-preflight.cjs` is at least 1 and `grep -c 'unref' scripts/sessionstart-post-update-preflight.cjs` is at least 1.
    - The common path is cheap: with the marker equal to the registered version, the hook completes in under 500 ms wall clock. Record the measured number in the SUMMARY.
    - The probe reuses class M rather than re-parsing a handshake: `grep -c 'activation-reached-the-wire' scripts/session-verify-probe.cjs` is at least 1 and `grep -c 'server=mindrian-brain' scripts/session-verify-probe.cjs` returns 0.
    - The honest refusal is verbatim: `grep -c 'verification is local-only' scripts/session-verify-probe.cjs` is at least 1.
    - The egress allowlist is enforced in code, not only in prose: `grep -c 'PROBLEM_TYPES' scripts/session-verify-probe.cjs` is at least 1 and `grep -c 'recommendChain' scripts/session-verify-probe.cjs` is at least 1.
    - The probe never writes to the terminal: `grep -Ec 'console\.(log|error)|process\.stdout\.write|process\.stderr\.write' scripts/session-verify-probe.cjs` returns 0.
    - The router gained one branch and nothing else: `git diff --stat scripts/first-install-router.cjs` shows insertions and at most 2 deletions, and `grep -c 'session-verify-state' scripts/first-install-router.cjs` is at least 1. Record the exact diffstat in the SUMMARY.
    - The branch is a NAMED function Task 6 can ride: `grep -c '_deliverVerifyInsight' scripts/first-install-router.cjs` is at least 2 (the declaration and the call from `main()`). If you named it something else, state the name in the SUMMARY and use that name in the grep.
    - No runtime writer clobbers the shared marker: `grep -c 'writeVerifyState' scripts/sessionstart-post-update-preflight.cjs` returns 0, `grep -c 'writeVerifyState' scripts/session-verify-probe.cjs` returns 0, and `grep -c 'mergeVerifyState' scripts/sessionstart-post-update-preflight.cjs` and `grep -c 'mergeVerifyState' scripts/session-verify-probe.cjs` are each at least 1.
    - The router's own machine is untouched: `grep -c "_fireReward" scripts/first-install-router.cjs`, `grep -c "_drainReward" scripts/first-install-router.cjs` and `grep -c "PHASES" scripts/first-install-router.cjs` all return their pre-edit values (record all three pairs in the SUMMARY).
    - `node tests/test-341-session-start-verify-hook.cjs` exits 0 with `FAIL=0` and all seven arms run.
    - `node tests/test-341-verify-insight-delivery.cjs` exits 0 with `FAIL=0` and all nine arms run, including the Canon Part 8 sentinel arm.
    - Zero network in the whole test leg: `grep -rEc 'theo-mcp|onrender|api\.github|npm view|npm publish' tests/test-341-session-start-verify-hook.cjs tests/test-341-verify-insight-delivery.cjs` reports 0 for each file.
    - A fresh session start does not error with the converted hook in place: `node scripts/sessionstart-coordinator.cjs` exits 0.
    - `node scripts/doctor.cjs --acceptance --pre-flight` and `node scripts/doctor.cjs --acceptance --pre-tag` both exit 0.
    - `bash tests/run-all-341.sh` reports `FAIL=0` and `EXPECTED-RED=0`; `bash tests/run-all-310.sh` reports `FAIL=0`.
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/sessionstart-post-update-preflight.cjs scripts/session-verify-probe.cjs scripts/first-install-router.cjs hooks/hooks.json tests/test-341-session-start-verify-hook.cjs tests/test-341-verify-insight-delivery.cjs` reports 0 for each file.
  </acceptance_criteria>

  <done>The first session after any install or update detects the version change from `installed_plugins.json`, runs a bounded offline self-check inside the existing 12 second budget, writes an honest three-state result, shows it as one statusline segment, fires a detached Theo probe that never blocks session start and refuses honestly when Theo is not reachable, and delivers exactly one generic-handle insight on the following turn, once per install or update, without adding a hook, without competing with the first-install reward, and without a single byte of room content crossing the wire.</done>
</task>

<task type="auto">
  <name>Task 6: Ride the D-07a router branch with the D-07b Eureka opt-in card, record the answer through one verb on both surfaces, and make /mos:eureka enable clear a wedged never</name>

  <files>lib/core/session-verify-state.cjs, scripts/first-install-router.cjs, lib/mcp/tools/room.cjs, lib/mcp/tools/gate.cjs, lib/core/eureka/eureka-enable.cjs, tests/test-341-eureka-optin-card.cjs, tests/run-all-341.sh</files>

  <read_first>
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-CONTEXT.md` decision D-07b, sub-points (a) through (g), in full. (c) carries the LOCKED verbatim copy and the three option labels; (d) carries the LOCKED four-state re-ask rule; (g) is the Canon Part 8 clause: the card and the install carry no room content, and the model download fetches public weights by model id only through the existing `lib/core/eureka/embedding-spine.cjs` contract.
    - `lib/core/session-verify-state.cjs` as Task 3 wrote it: `shouldAskEurekaOptin`, `recordEurekaOptin`, `buildEurekaOptinCard`, `resolveOptinVersion`, `EUREKA_OPTIN_COPY`, `EUREKA_OPTIN_OPTIONS`, `EUREKA_OPTIN_GATE_PREFIX`, `mergeVerifyState`. This task CALLS all of them and re-implements none of them. Any four-state logic written in this task is the defect.
    - `scripts/first-install-router.cjs`: the `_deliverVerifyInsight` branch Task 5 added at the top of `main()`, plus :175-190 (`emitContinueWithContext`), :297-330 (`_buildAdditionalContext`, the house voice for an injected instruction), :417-530 (`_fireReward`, in particular its `SKIP_REASONS` honest-skip discipline), :526-660 (`_drainReward`'s `drain_retries` bound), and `main()` at :882-968.
    - `scripts/auto-explore-drain.cjs` :121-133 - the in-repo precedent for a HOOK telling the model to fire a card: `INSTRUCTION FOR LARRY: ... dispatch ... via AskUserQuestion with verbs = [...]`. Copy its shape, not its content.
    - `lib/hmi/selector-dispatcher.cjs` :553-593 (`appendAskUserQuestionTrailer`) - the self-decoding trailer: the byte-frozen `[AskUserQuestion contract: shape=F.X verbs=N]` marker, the `[FIRE-IF-FORK: ...]` imperative, and the VERBATIM SEED-021 clause `do not reproduce this block as text (SEED-021)`. Read the header's root-cause paragraph (a bare marker with no imperative got rendered as an ASCII box) before writing a single line of instruction text; that failure is exactly what D-07b (c) forbids.
    - `lib/mcp/gate-render.cjs` :77-108 (`detectClientCapabilities`: Claude Code, Desktop and Cowork all land on the AskUserQuestion thin-adapter rung through the `MINDRIAN_MCP_FIRST` surface list, NOT on elicitation), :150-197 (`normalizeCard`, the shape `buildEurekaOptinCard` must satisfy), :198-215 (`normalizeGateAnswer`, the canonical `{ gate_id, chosen, verdict }`), :320-340 (`validateChosenAgainstCard`), :341-381 (`renderViaAskUserQuestion`).
    - `lib/mcp/tools/gate.cjs` :110-166 (the `gate_render` registration and its `_mintLiveGate` call), and :167-232 of `gate_answer` - specifically the ordered guards: `_consumeLiveGate` at :175, `validateChosenAgainstCard` at :191, the `material_step` resume-owner guard at :209, `const answer = gateRender.normalizeGateAnswer(...)` at :218, and then `const roomDir = resolveSessionRoomDir(sessionId, ctx);` at :219. That last line is the FIRST thing in the function that needs a room, and the gap immediately above it is this task's only insertion point.
    - `lib/mcp/tools/room.cjs` :350-357 (the `room_list` registration: `{}` input schema, `return textResponse(listRooms());`) and :420-428 (its declaration block: `tool: 'room_list'`, `hitl_shape: 'none'`, `hitl_why: 'Pure read: enumerates room directories under MINDRIAN_ROOMS_HOME, no fork.'`). Read the plugin's own MCP runtime instructions in `lib/mcp/runtime-instructions.cjs` :24-37, which bind Larry to call `room_list` ONCE at session start on the hookless surfaces. That is why `room_list` is the Desktop and Cowork carrier and `status_read` is not: the runtime instructions guarantee the first, not the second.
    - `lib/core/eureka/eureka-enable.cjs` :131-228 (`enableEureka`) - it has FIVE return sites (three `ok:true`, two `ok:false`) and a `finally` that releases the install lock. Count them yourself before editing; the wrapper below exists precisely so none of the five is touched.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-02-SUMMARY.md` - `enableEureka` is idempotent (a fresh filesystem probe short-circuits with `alreadyInstalled: true` and spawns nothing), and `doctor --fix eureka` already spawns the same path. Both entry points must clear a `never`, which is why the coupling goes in `eureka-enable.cjs` and not in one caller.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-03-SUMMARY.md` - class S `_layer5` resolves its probe THROUGH `eurekaDepInstalled`, the same authority the resolver exposes. D-07b (b)'s two installed signals are therefore two views of one probe; see LEG A step 2.
    - `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-PATTERNS.md` "Shared Patterns": "spawnSync discipline (Canon Part 8)" and "Never throw across a resolver boundary".
  </read_first>

  <action>
D-07b delivers ONE card on the turn D-07a already owns. The whole task is four narrow edits and a
test file; the reason it is a task and not a paragraph inside Task 5 is that it spans two surfaces
and the MCP half has its own guard ordering to respect.

The single most important structural decision, stated up front so no leg drifts from it: there is
ONE card definition (`buildEurekaOptinCard`, Task 3), ONE decision function (`shouldAskEurekaOptin`,
Task 3), and ONE recorder (`gate_answer`'s reserved branch, LEG C). The CLI and the MCP surfaces
differ ONLY in how the card is announced. Anything that duplicates the copy, the option ids, the
four-state rule or the write is wrong even if it works.

LEG A: the CLI half, appended to the branch Task 5 named.

Inside `_deliverVerifyInsight` in `scripts/first-install-router.cjs`, after the insight text is
assembled and BEFORE `emitContinueWithContext` is called, compute the opt-in verdict and, when it
fires, append the card instruction to the SAME string. One `emitContinueWithContext` call, one turn,
two things on it. Do not add a second emit, a second branch, or a second hook.

1. Read the state once (`readVerifyState()`), which the branch already did, and derive
   `currentVersion` with `resolveOptinVersion(state)`.
2. Derive `installedNow`. Call `eurekaDepInstalled('@huggingface/transformers')` from
   `lib/core/eureka-deps-resolver.cjs` (never-throw, no spawn, no network) and take its `installed`
   boolean. D-07b (b) names two installed signals, the resolver and the class S L5
   `model_installed` advisory; plan 341-03 routed L5's probe THROUGH this same `eurekaDepInstalled`,
   so the two signals are two views of one authority and calling it directly is the Canon Part 7
   reuse, not a shortcut. Write that in a comment and record it in the SUMMARY. Do NOT spawn class S
   from a UserPromptSubmit hook to re-derive a boolean you can read in one filesystem probe; the
   doctor reporter point from Task 4 step 2b is where the L5 view is surfaced.
3. Derive `firstInstallRewardPending` from the condition Task 5's branch already computes: the
   router's own `_readState()` is non-null AND its `phase` is not `done`.
4. Call `shouldAskEurekaOptin({ installedNow, state, currentVersion, firstInstallRewardPending })`.
   - `ask: true`: append the card instruction (below).
   - `defer: true`: append nothing, increment `eureka_optin_defer_retries` through
     `mergeVerifyState`, and fall through. The card lands on the NEXT router turn of this same
     session, which is exactly D-07b (a): the reward owns the first turn, the card takes the second.
     There is no timer and nothing is scheduled; the condition resolves itself when the router's own
     phase reaches `done`.
     Bound it: at `eureka_optin_defer_retries >= 3` stop deferring, append nothing, and write NO
     opt-in record. Say plainly in the comment that this bound is for a WEDGED router state (a phase
     that never reaches `done`), that it is the one path on which the card can land in a later
     session, and that landing later is strictly better than firing on top of a competing reward or
     spinning forever. A defer is not an answer, so nothing is recorded and the next session
     re-evaluates from scratch.
   - `ask: false`: append nothing. This is the common path forever after the navigator answers once.

The card instruction. Add ONE new export to `lib/core/session-verify-state.cjs`, the module Task 3
created: `buildEurekaOptinInstruction(card, { surface })`, where `surface` is the literal `'cli'`
or `'mcp'`. It is the ONLY place in the tree the instruction sentence exists, both surfaces call
it, and it is pure (a string in, a string out, no I/O). Adding it here rather than in either
caller is the same decision as the locked copy living there: two copies of a locked instruction
is the same defect as two copies of the locked copy. What it must contain:
   - The question line is `EUREKA_OPTIN_COPY`, interpolated from the module, NEVER retyped in this
     file. `grep -c '380 MB' scripts/first-install-router.cjs` must return 0 at the end of this
     task; that is the acceptance test for "one source of copy".
   - The gate id is `buildEurekaOptinCard(currentVersion).gate_id`, printed in the instruction so
     Larry passes the same id to both MCP calls.
   - The instruction, in the `scripts/auto-explore-drain.cjs` shape: tell Larry to call the
     `gate_render` MCP tool in THIS response with that `gate_id`, that `header`, `select_mode:
     'single'` and those three options with those three exact labels; then, when the navigator picks,
     to call `gate_answer` with the same `gate_id`, the chosen option id and `verdict: 'approve'`.
     Naming `gate_render` rather than AskUserQuestion directly is deliberate and is worth one
     sentence in the comment: `gate-render.cjs`'s ladder already routes Claude Code to the native
     AskUserQuestion thin adapter, so this gets D-07b (c)'s "AskUserQuestion on the CLI" through the
     shipped renderer instead of a second bespoke payload (SEED-020), and the identical instruction
     works on Desktop and Cowork with no branch.
   - On `Install now` ONLY, and only when `surface` is `'cli'` (Claude Code, where a shell exists),
     tell Larry to first run
     `node "${CLAUDE_PLUGIN_ROOT}/scripts/eureka-command.cjs" enable` in Bash so the npm output is
     visible while it runs, and THEN call `gate_answer`. That is what "visible progress" means on
     this surface, and it is honest: the recorder's own probe is idempotent, so the `gate_answer`
     that follows records `installed` without installing a second time. On `surface: 'mcp'` this
     sentence is absent, because there is no shell on Desktop or Cowork and promising one would be
     a lie; the install runs inside the `gate_answer` call instead (LEG C).
   - Close with the SEED-021 clause, copied verbatim from
     `lib/hmi/selector-dispatcher.cjs`'s trailer: `do not reproduce this block as text (SEED-021)`.
     An ASCII box here is the exact failure D-07b (c) names.

LEG B: the enable coupling, in `lib/core/eureka/eureka-enable.cjs`.

D-07b (d) says `never` is permanent UNTIL the navigator runs `/mos:eureka enable`, which clears it.
Both `/mos:eureka enable` and `doctor --fix eureka` reach the same `enableEureka`, so the coupling
goes there and only there.

Rename the current `enableEureka` body to `_enableEurekaInner` and make `enableEureka(opts)` a thin
wrapper that awaits it, records the opt-in, and returns the inner result UNCHANGED. Do this instead
of editing the five return sites: the envelope is already pinned by
`tests/test-341-eureka-enable-argv.cjs` and by `fixEurekaSmoke`, and five edits are five chances to
change one of them.

The wrapper, all of it inside a try so an opt-in write can never fail an install:
   - `const version = resolveOptinVersion(readVerifyState())`. A null version means no record is
     written; an install still succeeds and reports normally. Never invent a version.
   - `result.ok === true`: `recordEurekaOptin('installed', version)`. This is the clear: an
     `installed` record replaces a `never` by definition, so there is no separate clearing step and
     no way to clear without recording the truth.
   - `result.ok === false`: `recordEurekaOptin('declined', version, { install_failed: true,
     install_failure_reason: <result.stderrTail or result.message, capped at 200 characters> })`,
     the honest-failure encoding Task 3 defined. Do NOT record `installed` on a failed install and
     do NOT leave the state untouched: an untouched state re-fires the card against the same broken
     network on the next turn.
   - `opts.skipOptinRecord === true` skips the record entirely. The MCP recorder in LEG C passes
     this, because it writes the record itself with the surface context; without the flag the same
     install would be recorded twice.

LEG C: the answer, one verb, both surfaces, in `lib/mcp/tools/gate.cjs`.

Add ONE guarded branch to `gate_answer`, inserted between `const answer = gateRender.
normalizeGateAnswer(...)` (:218) and `const roomDir = resolveSessionRoomDir(sessionId, ctx);` (:219).
The position is load bearing: the branch runs AFTER the ledger consume, the option validation and the
resume-owner guard, so it inherits every existing guard, and BEFORE the room resolution, so an
opt-in answer works on a machine with no room bound. Say that in the comment.

The branch fires only when `answer.gate_id` starts with `EUREKA_OPTIN_GATE_PREFIX`. It maps the
chosen option id to a state choice through `EUREKA_OPTIN_CHOICE_BY_OPTION_ID` and spells NO option
id of its own; an id absent from the map is the `unknown_optin_choice` path below. Then:
   - `install_now`: call `enableEureka({ skipOptinRecord: true })` and await it. On `ok: true` call
     `recordEurekaOptin('installed', version)`; on `ok: false` call `recordEurekaOptin('declined',
     version, { install_failed: true, install_failure_reason: <bounded> })`. Return
     `textResponse({ ok: true, gate_id, eureka_optin: <the recorded choice>, installed: result.ok,
     message: result.message, duration_ms: result.durationMs })`.
     State the honest limit in the comment rather than implying a progress bar: an MCP tool response
     is one message, so on Desktop and Cowork the navigator sees Larry's line before the call and the
     returned `message` and `duration_ms` after it, and nothing in between. `enableEureka`'s own
     `spawnSync` timeout bounds the call. If the host aborts the tool call first, nothing is
     recorded, the card has no standing answer, and it fires again next session, which is the correct
     degradation and is asserted by a test arm.
   - `not_now`: `recordEurekaOptin('declined', version)`.
   - `never_ask_again`: `recordEurekaOptin('never', version)`.
   - Any other chosen id under this prefix: return `{ ok: false, reason: 'unknown_optin_choice' }`
     and write nothing. `validateChosenAgainstCard` already rejects ids that are not on the card, so
     this is the belt to that suspenders and it exists because a wrong write here is a wedged card.
   - `version` comes from `resolveOptinVersion(readVerifyState())` and a null version returns
     `{ ok: false, reason: 'unknown_version' }` with nothing written.
   - The whole branch is wrapped so a throw returns `{ ok: false, reason: 'optin_record_failed' }`
     and never breaks gate ratification, mirroring the reasoning-write discipline already documented
     at :240-247 of this same function.
   - Everything below the branch stays byte-unchanged, and a non-reserved `gate_id` reaches
     `resolveSessionRoomDir` exactly as it does today.

LEG D: the Desktop and Cowork announcement, in `lib/mcp/tools/room.cjs`.

`room_list` is the one tool the plugin's own MCP runtime instructions bind Larry to call once at
session start on the hookless surfaces, which makes it the D-07b (a) placement on those surfaces.
Extend its handler ONLY: compute the same verdict LEG A computes (same three inputs, same
`shouldAskEurekaOptin` call, `firstInstallRewardPending` is always `false` here because the 267.2
router is a Claude Code hook and does not run on these surfaces; say so in a comment rather than
inventing a signal for it), and when it fires, return
`Object.assign({}, listRooms(), { eureka_optin: { card: buildEurekaOptinCard(version), instruction:
<the same instruction text LEG A appends> } })`. When it does not fire, return `listRooms()`
byte-identically, so the ordinary response shape is untouched on every session after the first.

The `instruction` value is `buildEurekaOptinInstruction(card, { surface: 'mcp' })`, the same helper
LEG A added and LEG A calls with `{ surface: 'cli' }`. This file writes no instruction prose of its
own.

`room_list`'s Canon Part 11 declaration: `hitl_shape` stays `none`. Extend `hitl_why` with one
clause naming why that is still true: the advisory is a POINTER to the gate tools' own declared
fork, `room_list` itself still forks nothing and still returns on one path.
`node scripts/check-shape-declaration.cjs --check` must stay exit 0; run it and record the result.

LEG E: tests and the aggregator.

Create `tests/test-341-eureka-optin-card.cjs`, hermetic via `MOS_SESSION_VERIFY_HOME` plus
`MINDRIAN_EUREKA_DEPS_ROOT` (the plan 341-02 seam) pointed at `fs.mkdtempSync` trees. ZERO network
and ZERO real install anywhere: every arm that would reach `enableEureka`'s spawn either hits the
idempotent probe on a seeded fixture tree or monkeypatches the module's export, restored in a
`finally`, in the `tests/test-341-slim-install-honest-degrade.cjs` idiom. Assert at the end of the
run that `~/.mindrian/eureka-deps/node_modules/@huggingface` was never created, exactly as plan
341-02's suite does. Arms:
   - The copy is locked and single-sourced: the instruction text the router appends contains
     `EUREKA_OPTIN_COPY` character for character, and `scripts/first-install-router.cjs`,
     `lib/mcp/tools/room.cjs` and `lib/mcp/tools/gate.cjs` each contain zero occurrences of the
     literal `380 MB`.
   - Fires once: with a fresh state, no deps installed and the router's own state at `phase: done`,
     the router emits an `additionalContext` containing the copy and all three option labels. A
     SECOND invocation after `recordEurekaOptin('declined', version)` emits no card.
   - Never an ASCII box: the emitted instruction contains the verbatim SEED-021 clause and contains
     no box-drawing character (assert against the ranges U+2500 to U+257F) and no run of three or
     more consecutive `-` or `=` characters outside a fenced block.
   - Defers, does not drop: with the router's own state at a non-`done` phase, no card is emitted,
     `eureka_optin_defer_retries` increments, and no `eureka_optin` record is written. On the next
     invocation with `phase: done`, the card IS emitted. This is the D-07b (a) same-session rule.
   - The defer bound: after three defers the branch stops deferring, emits no card and writes no
     record, and the state still carries no `eureka_optin` key so a later session re-evaluates.
   - `installedNow` short-circuits: seed `MINDRIAN_EUREKA_DEPS_ROOT` with an installed
     `@huggingface/transformers` fixture and assert no card is emitted and no probe of any kind is
     spawned.
   - The three answers, driven through `gate_answer`'s branch directly (require the module and call
     the registered handler, or drive `runChecks`-style exported internals; do not boot a server):
     `not_now` writes `declined` with the current version; `never_ask_again` writes `never`;
     `install_now` with a monkeypatched `enableEureka` returning `ok:true` writes `installed`, and
     with one returning `ok:false` writes `declined` plus `install_failed: true` and a non-empty
     `install_failure_reason`, and NEVER writes `installed`.
   - The aborted-call degradation: with `enableEureka` monkeypatched to throw, `gate_answer` returns
     `{ ok: false, reason: 'optin_record_failed' }`, writes no `eureka_optin` record, and the next
     `shouldAskEurekaOptin` still returns `ask: true`.
   - No room needed: every arm above runs with no room bound and no `room.db` anywhere, proving the
     branch sits above `resolveSessionRoomDir`.
   - A non-reserved gate id is untouched: a `gate_id` that does not carry the prefix takes the
     existing path (assert by monkeypatching `resolveSessionRoomDir` to a sentinel and confirming it
     is reached).
   - `/mos:eureka enable` clears `never`: seed `eureka_optin { choice: 'never' }`, call
     `enableEureka` with a fixture tree where the dep is already present (so the idempotent probe
     returns `ok: true, alreadyInstalled: true` and nothing spawns), and assert the state now reads
     `choice: 'installed'` with the current version and that `shouldAskEurekaOptin` returns
     `ask: false, reason: 'already_installed'`. This is the D-07b (d) escape hatch and it is the arm
     that proves a `never` can never be permanently wedged.
   - `skipOptinRecord` is honored: the same call with `{ skipOptinRecord: true }` leaves the `never`
     record untouched, proving the MCP recorder cannot double-write.
   - Canon Part 8 and (g): assert that neither the card, the instruction, nor anything
     `gate_answer`'s branch writes contains a room name, a room path or any room-derived string.
     Plant a bound room whose `STATE.md` and `ROOM.md` carry a distinctive sentinel and assert the
     sentinel appears in NEITHER the emitted instruction NOR the state file. Also assert that
     `lib/mcp/tools/gate.cjs`'s branch reaches no network of its own: the only outbound call is
     `enableEureka`, whose single npm egress plan 341-03's topology tripwire already names and bounds.
   - `room_list` shape: with no card firing, `room_list`'s response is byte-identical to
     `listRooms()` (capture both and compare); with the card firing, it carries exactly one extra
     top-level key, `eureka_optin`, and the `rooms` and `home` values are unchanged.

`tests/run-all-341.sh`: append a `run_if` leg `341: eureka-optin-card` guarded on
`tests/test-341-eureka-optin-card.cjs`, in the file's exact two-line convention, after the Task 4
legs.
  </action>

  <verify>
    <automated>node tests/test-341-eureka-optin-card.cjs &amp;&amp; node tests/test-341-eureka-optin-once.cjs &amp;&amp; node scripts/check-eureka-optin-once.cjs --check &amp;&amp; node scripts/check-shape-declaration.cjs --check &amp;&amp; node scripts/doctor.cjs --acceptance --pre-tag &amp;&amp; bash tests/run-all-341.sh</automated>
  </verify>

  <acceptance_criteria>
    - The copy has exactly one home: `grep -c '380 MB' lib/core/session-verify-state.cjs` is at least 1, and `grep -rc '380 MB' scripts/first-install-router.cjs lib/mcp/tools/gate.cjs lib/mcp/tools/room.cjs` returns 0 for each of the three.
    - The option ids have exactly one home: `grep -rc "install_now" scripts/first-install-router.cjs lib/mcp/tools/room.cjs` returns 0 for each, and `grep -c 'EUREKA_OPTIN_GATE_PREFIX' lib/mcp/tools/gate.cjs` is at least 1.
    - The four-state rule has exactly one home: `grep -c 'shouldAskEurekaOptin' scripts/first-install-router.cjs` is at least 1, `grep -c 'shouldAskEurekaOptin' lib/mcp/tools/room.cjs` is at least 1, and neither file contains a `declined` or `never` string comparison of its own (`grep -Ec "'declined'|'never'" scripts/first-install-router.cjs lib/mcp/tools/room.cjs` returns 0 for each).
    - The card rides the existing branch, not a new one: `grep -c 'emitContinueWithContext' scripts/first-install-router.cjs` equals its pre-edit value (record the pre-edit and post-edit numbers in the SUMMARY), and `git diff --stat scripts/first-install-router.cjs` for THIS task shows no new top-level dispatch entry in `main()`.
    - No new MCP tool: `node -e "const j=require('./data/command-registry.json')"` still parses, `node scripts/doctor.cjs --acceptance --pre-tag` exits 0, and the `mcp-surface-tool-count` point reports the same per-server tool counts as before this task (record both numbers in the SUMMARY).
    - The `gate_answer` branch sits above the room resolution: `node -e "const s=require('fs').readFileSync('lib/mcp/tools/gate.cjs','utf8'); const a=s.indexOf('EUREKA_OPTIN_GATE_PREFIX'); const b=s.indexOf('resolveSessionRoomDir(sessionId, ctx)'); if(a===-1) process.exit(1); if(b===-1) process.exit(1); process.exit(Math.min(a,b)===a ? 0 : 1)"` exits 0.
    - The recorder spells no option id: `grep -Ec "'install_now'|'not_now'|'never_ask_again'" lib/mcp/tools/gate.cjs` returns 0, and `grep -c 'EUREKA_OPTIN_CHOICE_BY_OPTION_ID' lib/mcp/tools/gate.cjs` is at least 1.
    - The instruction has exactly one home: `grep -c 'buildEurekaOptinInstruction' lib/core/session-verify-state.cjs` is at least 1, `grep -c 'buildEurekaOptinInstruction' scripts/first-install-router.cjs` is at least 1, `grep -c 'buildEurekaOptinInstruction' lib/mcp/tools/room.cjs` is at least 1, and `grep -c 'gate_render' scripts/first-install-router.cjs` equals `grep -c 'gate_render' lib/mcp/tools/room.cjs` equals 0 (the tool names live inside the builder, not in either caller).
    - The enable wrapper did not touch the five returns: `grep -c '_enableEurekaInner' lib/core/eureka/eureka-enable.cjs` is at least 2, `grep -c 'return {' lib/core/eureka/eureka-enable.cjs` equals its pre-edit value of 6 (five inside the installer plus one in `buildEurekaInstallArgv`; verify the pre-edit count yourself and record both in the SUMMARY), and `node tests/test-341-eureka-enable-argv.cjs` exits 0 with `PASS=6 FAIL=0`.
    - `/mos:eureka enable` clears `never`: the dedicated test arm passes, and `grep -c 'recordEurekaOptin' lib/core/eureka/eureka-enable.cjs` is at least 1.
    - `room_list` stays a pure read in its declaration: `grep -c "hitl_shape: 'none'" lib/mcp/tools/room.cjs` equals its pre-edit value, the `room_list` entry still declares `hitl_shape: 'none'`, and `node scripts/check-shape-declaration.cjs --check` exits 0.
    - Zero real install anywhere in this task's verification: after the full test run, `test ! -d "$HOME/.mindrian/eureka-deps/node_modules/@huggingface"` exits 0. Record the check in the SUMMARY.
    - `node tests/test-341-eureka-optin-card.cjs` exits 0 with `FAIL=0` and all fourteen arms run, including the SEED-021 no-ASCII-box arm, the defer-bound arm, the failed-install honest-record arm and the Canon Part 8 sentinel arm.
    - Zero network in the test leg: `grep -rEc 'theo-mcp|onrender|api\.github|registry\.npmjs|npm view|npm publish' tests/test-341-eureka-optin-card.cjs` returns 0.
    - `bash tests/run-all-341.sh` reports `FAIL=0` and `EXPECTED-RED=0`; `bash tests/run-all-310.sh` reports `FAIL=0`.
    - `node scripts/doctor.cjs --acceptance --session-start --json` still reports the five-point set and `eureka-optin-state` now reflects whatever the last test arm left on the real home (or `not yet asked` on a clean machine).
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/first-install-router.cjs lib/mcp/tools/room.cjs lib/mcp/tools/gate.cjs lib/core/eureka/eureka-enable.cjs tests/test-341-eureka-optin-card.cjs` reports 0 for each file.
  </acceptance_criteria>

  <done>The Eureka opt-in card fires exactly once per version on the turn D-07a already owns, from the SAME router branch with no second delivery mechanism; it is a real card through the shipped gate ladder on every surface and never an ASCII box; the locked copy, the three option labels, the four-state rule and the write each live in exactly one file; `Install now` runs the wave-2 enable path and never a second installer, with the honest failure state recorded when it fails; `Not now` and `Never ask again` are recorded against the version they were answered for; `/mos:eureka enable` clears a `never` so the permanent choice is never a wedge; and a fourteen-arm zero-network suite proves every one of those sentences.</done>
</task>

</tasks>

## Artifacts this phase produces

### This plan

| Artifact | Kind | Notes |
|----------|------|-------|
| `commands/update.md` | collapsed | Four steps: Check, Confirm, Loader, Verify. Frontmatter contract preserved. Step 4 names the automatic next-session verification (D-07a). |
| `scripts/check-version-and-sha.cjs` | changed | `fetchTagSha` and the `api.github.com` call removed; STATUS vocabulary drops to three (plus `ERROR`). |
| `scripts/post-update-activation.cjs` | DELETED | The atomic activator and touch-file writer. |
| `lib/core/session-verify-state.cjs` | new file | The ONE contract: last-seen marker, verification result, statusline segment string, and the D-07b opt-in half (`eureka_optin` record, `EUREKA_OPTIN_COPY` locked verbatim, `EUREKA_OPTIN_OPTIONS`, `EUREKA_OPTIN_GATE_PREFIX`, `buildEurekaOptinCard`, the pure `shouldAskEurekaOptin`, `recordEurekaOptin`, `resolveOptinVersion`) plus `mergeVerifyState`, the merging writer every runtime path uses. Seam `MOS_SESSION_VERIFY_HOME`. |
| `scripts/sessionstart-post-update-preflight.cjs` | CONVERTED | Same registered hook, new job: version-change trigger, the bounded offline leg, the detached fire. Never blocks. F8 stale-registry cue kept. |
| `scripts/session-verify-probe.cjs` | new file | Detached child: wire point, Theo verdict with honest refusal, one generic-handle nugget. Seam `MOS_SESSION_VERIFY_PROBE_FIXTURE`. |
| `scripts/check-session-start-verification.cjs` | new file | Offline harness-policy runner. Seams `CHECK_SESSION_START_ROOT`, `MOS_SESSION_VERIFY_HOME`. |
| `data/harness-policies/session-start-verification.json` | new policy | Rung `logged`, `applies_to: ["session-start"]` (D-07a f). |
| `scripts/check-eureka-optin-once.cjs` | new file | The D-07b (f) offline runner. Drives the six fixture states (`fresh`, `installed`, `declined-same-version`, `declined-older-version`, `never`, `first-install-reward-pending`) through `shouldAskEurekaOptin` and asserts exact fire or no-fire. Spawns nothing, reaches nothing, reads no runtime state. Exports `FIXTURES` and `runChecks`. |
| `data/harness-policies/eureka-optin-once.json` | new policy | Rung `logged`, `applies_to: ["session-start"]` (D-07b f). Also an always-green blocking leg in `tests/run-all-341.sh`. |
| `data/harness-policies/_schema.json` | changed | `applies_to_vocabulary` grows to six members with `session-start`. |
| `data/harness-manifest.json` | regenerated | Same commit as the policy. |
| `scripts/doctor.cjs` | changed | `--post-update` flag, help text and dispatch removed. New `--session-start` acceptance tier over four existing points plus ONE new reporter point, `eureka-optin-state` (`severity: 'info'`, always `ok: true`, D-07b f's honest doctor line); `activation-reached-the-wire` deliberately kept and deliberately left off the inline tier. |
| `hooks/hooks.json` | changed | One `statusMessage` reworded. Entry count stays 9; `sessionstart-npm-reconcile.cjs` and the preflight entry both kept. |
| `lib/statusline/cockpit-signals.cjs` | changed | `post_update` signal re-sourced from the verification result; key and consumer unchanged. |
| `scripts/context-monitor` | changed | The single version suffix becomes the one version-plus-glyph segment, byte-identical fallback preserved. |
| `scripts/first-install-router.cjs` | changed | One new top-of-`main()` branch, `_deliverVerifyInsight`: the insight drain, and (D-07b a) the opt-in card appended to the SAME `emitContinueWithContext` call. No second branch, no second hook, no second emit. Existing state machine untouched. |
| `lib/mcp/tools/gate.cjs` | changed | One guarded branch in `gate_answer`, keyed on `EUREKA_OPTIN_GATE_PREFIX`, inserted after the existing guards and BEFORE `resolveSessionRoomDir` so an opt-in answer needs no bound room. The ONE recorder for both surfaces. |
| `lib/mcp/tools/room.cjs` | changed | `room_list` carries the one-time `eureka_optin` advisory (card plus instruction) on Desktop and Cowork, the surfaces where the plugin's own MCP runtime instructions guarantee a session-start call. Byte-identical response on every session where the card does not fire. `hitl_shape` stays `none`. |
| `lib/core/eureka/eureka-enable.cjs` | changed | `enableEureka` becomes a thin wrapper over an untouched `_enableEurekaInner`, recording `installed` on success and the honest failed-install state on failure, so BOTH `/mos:eureka enable` and `doctor --fix eureka` clear a `never` (D-07b d). `skipOptinRecord` lets the MCP recorder avoid a double write. |
| `~/.mindrian/session-verify/` | new runtime path | `state.json` plus `insight-<version>.md`. |
| `~/.mindrian/post-update-restart-pending` | retired runtime path | Nothing writes it, nothing reads it, and a tripwire asserts the literal is absent from the live tree. |
| `SHA_DIFFERS_INVERSION_HOTFIX` | retired STATUS value | |
| `doctor --post-update` | retired flag | |
| `tests/test-mos-update-activation-gap.cjs` | DELETED | Coverage moved; the move is recorded arm by arm in the SUMMARY. |
| `tests/test-341-update-collapsed.cjs` | new test | Eight-arm tripwire. |
| `tests/test-341-session-verify-state.cjs`, `tests/test-341-session-start-tier.cjs`, `tests/test-341-session-start-verify-hook.cjs`, `tests/test-341-verify-insight-delivery.cjs`, `tests/test-341-eureka-optin-once.cjs`, `tests/test-341-eureka-optin-card.cjs` | new tests | All zero-network, and the two D-07b suites additionally prove zero real install. |
| `~/.mindrian/session-verify/state.json` `eureka_optin` key | new runtime record | `{ choice, version, at }` plus the honest `install_failed` / `install_failure_reason` pair. D-07b (e): no second marker file anywhere. |

### Phase-wide (for reference)

The full phase artifact list is enumerated in `341-01-PLAN.md`. This plan's retirements:
`~/.mindrian/post-update-restart-pending`, `SHA_DIFFERS_INVERSION_HOTFIX`, `doctor --post-update`,
`scripts/post-update-activation.cjs`. This plan's additions: the `session-start` acceptance tier, the
sixth `applies_to` member, and the `~/.mindrian/session-verify/` runtime path. Plans 341-09 and
341-10 retire doctor classes A and H and the legacy-topology halves of classes I and J; neither
touches the `session-start` tier or its four points.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/mos:update` -> the Claude Code loader | Fetch and Swap move fully to the platform. The plugin stops holding a second implementation. |
| SessionStart hooks -> every session | The converted hook runs on every user's session start and now decides whether to spend a bounded budget. |
| version check -> the network | The GitHub tag-ref call is deleted; only the marketplace catalog fetch remains. |
| `~/.mindrian/session-verify/state.json` -> the statusline, the hook and the router | A user-writable marker that gates whether verification runs and what the statusline claims. |
| the detached probe -> Theo | The only network reach this plan adds, off the session-start critical path. |
| the active room's USER.md -> Theo | The Canon Part 8 boundary. Exactly one enum member is permitted to cross it. |
| the D-07b card answer -> a 380 MB download | A navigator-facing fork whose approve branch spends bandwidth and disk on the navigator's machine. |
| `eureka_optin` in the shared marker -> the card | A user-writable record that decides whether a card ever fires again. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-341-26 | Denial of service | changing a registered SessionStart hook and orphaning its siblings | mitigate | `hooks/hooks.json` is JSON-parsed as an acceptance criterion; the SessionStart entry count is asserted at 9 by a dedicated tripwire arm (no tenth hook, no removed hook); `sessionstart-npm-reconcile` is asserted still present (the deliberate 60-second-timeout backstop, not dead code); and `scripts/sessionstart-coordinator.cjs` is executed to prove a clean session start. |
| T-341-27 | Repudiation | deleting the wrong verification and losing the ability to detect a phantom version | mitigate | `activation-reached-the-wire` is explicitly kept, asserted present by a dedicated tripwire arm, and given a NEW consumer: the detached probe runs it in `full` mode. It reads `package.json` and the live L4 MCP handshake, never the touch-file, so it is independent of everything being retired. |
| T-341-28 | Tampering | a consumer left branching on a STATUS the producer can no longer emit | mitigate | Task 2 requires a full repo grep of every consumer BEFORE the edit, a documented disposition per consumer, and an acceptance criterion that the header's documented STATUS set equals the set the code emits. |
| T-341-29 | Information disclosure | the retired GitHub tag-ref call | mitigate | Deleting `fetchTagSha` removes the only `api.github.com` request from the update path, shrinking the update flow's network surface to the marketplace catalog fetch alone. |
| T-341-30 | Repudiation | a retired test left passing vacuously | mitigate | `tests/test-mos-update-activation-gap.cjs` is DELETED with its four arms' coverage traced, arm by arm, to the tests that replace them; `tests/test-update-restart-cue.cjs` is EXTENDED, not left as it was, with a new never-blocks arm. A green suite that tests nothing is worse than a deleted one. |
| T-341-31 | Tampering | the `~/.mindrian/session-verify/state.json` marker | mitigate | The marker can only suppress a re-verification or force one; it can never grant a pass. `buildStatuslineSegment` renders the glyph ONLY from `local.ok`, which is written from a doctor exit status the same process just observed, never from a value read back off disk and trusted. A malformed or absent state degrades to `changed: true` (verify again) and to a segment with no glyph (claim nothing), never to a check mark. The `session-start-verification` harness policy re-reads the state offline at release tiers, so a machine sitting on a failed verification is surfaced rather than silent. |
| T-341-32 | Denial of service | a network probe at session start | mitigate | The inline leg is offline by tier construction: `activation-reached-the-wire`, the only point that touches the network, is deliberately excluded from `session-start` and the exclusion is asserted by a test arm. The network leg is a `detached: true` plus `unref()` child the hook never awaits, so session start cannot block on it. The inline spawn carries an explicit `SESSION_VERIFY_BUDGET_MS` timeout derived from a real measurement and capped under the hooks.json 12000 ms budget, and a timeout writes an honest `inconclusive` result rather than a failure. The whole leg runs once per version change, not once per session, so the steady-state cost is two JSON reads. |
| T-341-33 | Information disclosure | egress of anything but a generic handle | mitigate | Canon Part 8, enforced in three places: the probe checks the resolved value against the frozen `PROBLEM_TYPES` allowlist before any call; `recommendChain` runs `_normalizeBrainProblemType` and refuses locally with zero network on anything failing the shape gate; and "no room bound" and `unknown` both short-circuit to no call at all. The test suite plants a sentinel string in the room's USER.md and asserts it appears in neither the state file nor the capture file. No room name, path, content or governing thought is ever passed. |
| T-341-34 | Tampering | the `eureka_optin` record, and specifically a wedged `never` | mitigate | Three-layer mitigation, because a permanent choice with no escape hatch is a permanent defect. (a) `/mos:eureka enable` ALWAYS clears it: the coupling lives in `lib/core/eureka/eureka-enable.cjs`, which is the single path BOTH `/mos:eureka enable` and `doctor --fix eureka` reach, so there is no entry point that installs without clearing, and a dedicated test arm seeds `never` and asserts the state reads `installed` afterwards. (b) The record can only SUPPRESS a card, never grant an install: nothing reads `eureka_optin` to decide whether to download, only whether to ask, and the download runs from a card answer alone (T-341-35). (c) A malformed or absent record degrades to `ask: true` (fire the card and let the navigator answer again), never to a silent permanent `never`, because `shouldAskEurekaOptin` matches on the exact literal `'never'` and falls through on anything else. `mergeVerifyState` is what stops the OTHER direction of tampering: a verification write on the next update can no longer overwrite the file and erase a navigator's answer, which would have resurrected a card D-07b (d) says is permanently answered. |
| T-341-35 | Elevation of privilege | an unattended 380 MB download triggered without a navigator answer | mitigate | The install has exactly two call sites, both of which require an explicit human act: `gate_answer` with a reserved `eureka-optin-` gate id and a `chosen` that passed `validateChosenAgainstCard` (so the id was on a card the server itself minted this session), and the navigator typing `/mos:eureka enable` or `doctor --fix eureka` themselves. The SessionStart hook, the detached probe, the statusline and the doctor reporter point all read state and none of them can reach `enableEureka`; asserted by `grep -c 'enableEureka'` returning 0 in `scripts/sessionstart-post-update-preflight.cjs`, `scripts/session-verify-probe.cjs` and `scripts/context-monitor`. The card itself downloads nothing: D-07b (c) says never a silent download, and the copy states the size (380 MB) and the one-time nature before the fork, so the navigator approves a known cost. `enableEureka`'s own `spawnSync` carries an explicit 120000 ms timeout and Canon Part 8 spawn discipline (argv array, no shell, `input: ''`), and a failed or aborted install records the honest failed state rather than a phantom `installed`. |
| T-341-SC | Tampering | npm / pip / cargo installs | accept | This plan installs no package into this repo. The ONE install it can trigger is the wave-2 `enableEureka` path, whose single package (`@huggingface/transformers`, pinned by `EUREKA_DEP_SPEC`) was audited in plan 341-02 and which installs into `~/.mindrian/eureka-deps`, never into the repo. `slopcheck check` only, NEVER `slopcheck install`, inside this repo. |
</threat_model>

<verification>
Run from `/home/jsagi/dev/MindrianOS-Plugin` (WORKSPACE GUARD: confirm with `pwd`).

1. `node tests/test-341-update-collapsed.cjs`, `node tests/test-341-session-verify-state.cjs`,
   `node tests/test-341-session-start-tier.cjs`, `node tests/test-341-session-start-verify-hook.cjs`,
   `node tests/test-341-verify-insight-delivery.cjs`, `node tests/test-341-eureka-optin-once.cjs`
   and `node tests/test-341-eureka-optin-card.cjs` each exit 0 with `FAIL=0`.
2. `node scripts/check-version-and-sha.cjs` emits one of the three surviving STATUS values (or
   `ERROR`) and makes no GitHub call.
3. `node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))"` exits 0 and the
   SessionStart entry count is 9.
4. `node scripts/check-shape-declaration.cjs --check` exits 0.
5. `node scripts/doctor.cjs --acceptance --session-start --json` parses, reports
   `mode: "session-start"` with exactly the five expected point ids (the four D-07a points plus
   `eureka-optin-state`), and completes in under 12000 ms wall clock (record the measurement).
6. `node scripts/run-harness.cjs --tier session-start --json` reports exactly two `logged`
   policies, `session-start-verification` and `eureka-optin-once`;
   `node scripts/check-eureka-optin-once.cjs --check` exits 0;
   `node scripts/run-harness.cjs --check` and `node scripts/build-harness-manifest.cjs --check` both
   exit 0.
7. `node scripts/doctor.cjs --acceptance --pre-flight` and
   `node scripts/doctor.cjs --acceptance --pre-tag` both exit 0.
8. `echo '{}' | node scripts/context-monitor` exits 0, writes a non-empty line, and
   `node tests/test-statusline-glyph-isolation.cjs` exits 0.
9. `node scripts/sessionstart-coordinator.cjs` exits 0.
10. `bash tests/run-all-341.sh` reports `FAIL=0` and `EXPECTED-RED=0`;
    `bash tests/run-all-310.sh` reports `FAIL=0`.
11. `test ! -d "$HOME/.mindrian/eureka-deps/node_modules/@huggingface"` exits 0: nothing in this
    plan's verification performed a real embedding-stack install.
12. `git status --porcelain` shows only the files this plan declares, including two deletions.
</verification>

<success_criteria>
- `/mos:update` is Check, Confirm, loader, Verify, with no staging, swapping or dependency install of
  its own (D-07).
- The touch-file, its writer and its doctor flag are gone, with no orphaned reference anywhere in the
  live tree (D-07).
- The restart banner fires only on a real swap and never on `UP_TO_DATE` (D-07).
- The SHA leg and the `api.github.com` tag-ref call are retired, with no consumer left branching on
  the removed STATUS (D-07).
- The post-update signal is a version change read from `installed_plugins.json` against a last-seen
  marker, and a fresh install with no marker verifies too (D-07a a).
- The first session after an install or update runs a bounded, offline subset of existing doctor
  points inside the 12 second hook budget, and full `doctor --all` stays opt-in (D-07a b).
- The statusline carries ONE segment: version plus a check mark on a pass, a warning glyph plus the
  failing count on a failure (D-07a c).
- Theo is probed after the local set, detached so session start never blocks, reusing the shipped
  class M brain-smoke, and a keyless or unreachable session says "Theo not reachable, verification is
  local-only" rather than imitating a reachable one (D-07a d).
- ONE insight is delivered on the first turn after the verification line, once per install or update
  and never per session, keyed by a generic handle only, framed by Larry as one question (D-07a e).
- The behavior is declared as a `logged` harness policy in a new `session-start` tier with an offline
  runner and a test seam (D-07a f).
- The Eureka opt-in card fires only when the stack is not installed, there is no standing answer for
  this version, and the version-change trigger fired (D-07b b).
- The card rides the SAME 267.2 router branch as the insight, with no second delivery mechanism, and
  it is a real card through the shipped gate ladder on every surface: never an ASCII box, never a
  silent download, copy locked verbatim, three options locked (D-07b a, c).
- `Install now` runs the wave-2 enable path and never a second installer, recording `installed` on
  success and the honest failed-install state on failure; `Not now` records `declined` against this
  version; `Never ask again` records `never` (D-07b c).
- The four-state re-ask rule holds exactly: installed never asks, declined re-asks exactly once on
  the next version change, never is permanent until `/mos:eureka enable` clears it, and a pending
  first-install reward defers the card to the next turn of the SAME session (D-07b d).
- There is ONE state authority and no second marker file: `eureka_optin { choice, version, at }`
  lives in the same `~/.mindrian/` marker, and `mergeVerifyState` makes a verification write
  structurally incapable of erasing it (D-07b e).
- The opt-in behavior is declared as a second `logged` policy, `eureka-optin-once`, whose offline
  runner asserts exact fire or no-fire over the six named fixture states, is an always-green
  blocking leg in `tests/run-all-341.sh`, and is reported honestly by a `--session-start` doctor
  line that never blocks (D-07b f).
- No room content and no room-derived string reaches the card, the instruction, the record or the
  install; the model download fetches public weights by model id through the existing
  `embedding-spine.cjs` contract (D-07b g, Canon Part 8).
- No hook was added and none was removed: the SessionStart count is still 9, the converted hook
  never blocks on any path, and no MCP tool was added (the per-server tool counts are unchanged).
- The npm-reconcile backstop and the `activation-reached-the-wire` acceptance point are provably
  still in place, the latter with a new detached consumer.
- Every new behavior has a zero-network test, and the aggregator carries a `run_if`-guarded leg for
  each new test file.
- No em-dash in any file this plan touches.
</success_criteria>

<output>
Create `.planning/phases/341-install-and-update-overhaul-npm-source-plugin-artifact-heavy/341-08-SUMMARY.md` when done.

Record in the SUMMARY: the reapply decision and its reason; the three Step 4 sentences verbatim;
`readLocalSha`'s fate and why; the STATUS set the code emits next to the STATUS set the header
documents; the full consumer list found for `check-version-and-sha.cjs` and each one's disposition;
where each of `tests/test-mos-update-activation-gap.cjs`'s four arms' coverage now lives; the
pre-edit and post-edit `existsSync` counts in `lib/statusline/cockpit-signals.cjs`; the confirmation
that the check mark and the warning glyph are not carve-out glyphs; the MEASURED wall-clock time of
`doctor --acceptance --session-start`, the `SESSION_VERIFY_BUDGET_MS` value derived from it and the
arithmetic; the measured no-change fast-path time; the pre-tag point count before and after; the
`filterByTier` line number proving `run-harness.cjs` needed no edit; the `git diff --stat` for
`scripts/first-install-router.cjs` plus the three pre-edit and post-edit grep counts proving its
state machine is untouched; and the `class-m-brain-smoke.cjs` L4 layer-id discrepancy
(`stdio_handshake` versus the `mcp_stdio_handshake` the old preflight matched on) as a pre-existing
fragility this plan documents rather than inherits silently.

Record for D-07b: `EUREKA_OPTIN_COPY` quoted in full so a reviewer can diff it against D-07b (c) by
eye; the confirmation that `buildEurekaOptinCard`'s shape survives `gate-render.cjs`'s
`normalizeCard` (with the line number you read it at); the six fixture names and their asserted
verdicts as a table; the four answer sentences plus the unreadable-state fallback the `eureka-optin-state` reporter point can emit; the
pre-edit and post-edit `emitContinueWithContext` counts in `scripts/first-install-router.cjs` and
the pre-edit and post-edit `return {` count in `lib/core/eureka/eureka-enable.cjs`; the per-server
MCP tool counts before and after (proving no tool was added); the name you gave Task 5's branch
function if it is not `_deliverVerifyInsight`; where you drew the boundary for the
"runner is a caller, not a second implementation" arm; the confirmation that all ten paths added to
`PHASE_341_SURFACES` were em-dash free before they were added; the note that D-07b (b)'s two
installed signals collapse to one authority because class S L5 probes through the same
`eurekaDepInstalled`; and the confirmation that
`~/.mindrian/eureka-deps/node_modules/@huggingface` was never created during verification.

Commit convention: one atomic commit per task, never `git add -A`. Task 3 MUST commit the
`post-update-activation.cjs` deletion together with the `doctor.cjs` flag removal, or a doctor flag
points at a file that no longer exists. Task 4 MUST commit
`data/harness-policies/session-start-verification.json` together with the regenerated
`data/harness-manifest.json`, or `build-harness-manifest.cjs --check` fails closed. Task 5 MUST
commit the `hooks/hooks.json` `statusMessage` edit together with the converted hook. Task 6 MUST
commit the `lib/mcp/tools/gate.cjs` recorder together with the `lib/core/eureka/eureka-enable.cjs`
wrapper, or a card answer records an install through a path that does not yet clear a `never`. Every commit
message ends with the two trailer lines this repo uses (`Co-Authored-By:` and `Claude-Session:`).
</output>
