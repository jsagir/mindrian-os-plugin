---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 13
type: execute
wave: 6
depends_on: ["354-11", "354-12"]
files_modified:
  - tests/test-354-acceptance-diagnostics.cjs
  - scripts/doctor.cjs
  - .planning/debug/sys-07-acceptance-timing.md
autonomous: true
requirements: [SYS-07]
tags: [doctor, acceptance, diagnostics, child-process, tier-5]

must_haves:
  truths:
    - "doctor --acceptance prints a start and a done line with duration for every point on stderr, and its JSON carries duration_ms per point plus summary.duration_ms and summary.slowest"
    - "Every child process an acceptance point spawns has a bounded timeout; a hung child fails its point with detail.child.timed_out true, names the point, and leaves no live child process"
    - "A clean rerun outside the tool sandbox classifies SYS-07 as WORKING, ENV GAP or NEW FAILURE with the exact phase, measured durations, network waits and an orphan-process check, recorded in an RCA file"
    - "stdout JSON stays parseable (progress goes to stderr only) and doctor --all's renderer is untouched (Phase 352 owns it)"
  artifacts:
    - path: "scripts/doctor.cjs"
      provides: "runBoundedChild helper, per-point timing and stderr progress in runAcceptance, test-mode point filter and hang injection"
      contains: "runBoundedChild"
    - path: "tests/test-354-acceptance-diagnostics.cjs"
      provides: "Hermetic regression: timing fields, progress lines, bounded hang, no orphan"
      min_lines: 80
    - path: ".planning/debug/sys-07-acceptance-timing.md"
      provides: "RCA-template record of the clean rerun and its classification"
      contains: "kind: rca"
  key_links:
    - from: "scripts/doctor.cjs runAcceptance"
      to: "process.stderr"
      via: "[acceptance] <id> start / done <ms>ms lines"
      pattern: "\\[acceptance\\]"
---

<objective>
Resolve SYS-07 the way the research requires (CTX-SYS07 in 354-CONTEXT.md: "Do not close this as fixed OR as a defect without first instrumenting phase timing + bounded child-process diagnostics and rerunning clean"; handoff SYS-07: "Reproduce with an explicit time budget and phase diagnostics. Separate normal suite duration, environment restrictions, network waits and child-process hangs. A 20-second timeout alone proves no defect. On a real failure, show the exact phase, bounded termination and actionable status; verify no orphan processes"). Tier 5 of the locked repair sequence.

The seam: the acceptance runner's silence (no progress, no per-point timing, several child spawns without a timeout, for example the git probes near scripts/doctor.cjs:963, :973, :1015 and the node --check at :1054) versus an operator who cannot tell a slow suite from a hang. Instrument first (Tasks 1-2), then rerun and classify (Task 3). Scope boundary with Phase 352 (unplanned, owns doctor --all rendering F2/F3/F9): this plan edits only runAcceptance, buildAcceptanceChecklist's child spawns and new helpers.

This plan has no CONFIRMED finding; Task 1's test is written first and is red because the instrumentation does not exist yet.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@scripts/doctor.cjs
@docs/RCA-TEMPLATE.md
@docs/reviews/2026-09-20-full-system-code-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing regression - timing, progress, bounded hang, no orphan</name>
  <files>tests/test-354-acceptance-diagnostics.cjs</files>
  <read_first>
    - scripts/doctor.cjs lines 733-760 (acceptance header, DOCTOR_TEST_MODE hooks), 815-830 (buildAcceptanceChecklist shape), 900-1100 (child spawns), 2043-2070 (runAcceptance)
    - tests/test-doctor-acceptance-preflight-checks.cjs (existing DOCTOR_TEST_MODE harness usage)
    - docs/reviews/2026-09-20-full-system-code-review.md lines 60-70 (F-07 as observed)
  </read_first>
  <action>
Create tests/test-354-acceptance-diagnostics.cjs. Spawn `node scripts/doctor.cjs --acceptance --pre-tag --json` with env DOCTOR_TEST_MODE=1, DOCTOR_TEST_ONLY_POINTS=install-state,version-of-record-repo (two cheap pre-tag points; read buildAcceptanceChecklist to confirm both ids apply to pre-tag and substitute two that do if not), DOCTOR_TEST_HANG_POINT=version-of-record-repo, DOCTOR_ACCEPTANCE_CHILD_TIMEOUT_MS=800, MINDRIAN_ACCEPTANCE_PROGRESS=1, with an outer spawn timeout of 120000 ms and killSignal SIGKILL so the test itself is bounded.
Assert: (1) the process exits within the outer bound with status 1; (2) stdout parses as JSON; exactly the two filtered points appear; each has a numeric duration_ms; summary.duration_ms is a number and summary.slowest.id is one of the two ids; (3) the hung point has ok false, detail.child.timed_out === true, detail.child.pid a number, and its finding contains 'timed out'; (4) stderr contains '[acceptance] install-state start', '[acceptance] install-state done', '[acceptance] version-of-record-repo start' and a done line for it; (5) after exit, process.kill(detail.child.pid, 0) throws (no orphan); (6) with MINDRIAN_ACCEPTANCE_PROGRESS=0 stderr carries no '[acceptance]' lines.
Run against current doctor.cjs: expected to fail (no filter, no timing, no hang injection).
  </action>
  <verify>
    <automated>node tests/test-354-acceptance-diagnostics.cjs; test $? -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test bounds its own child with a timeout and SIGKILL (grep finds `killSignal`)
    - The test asserts `detail.child.timed_out`, `duration_ms`, `summary.slowest` and the absence of the hung pid after exit
    - Against the current doctor.cjs the test exits 1
  </acceptance_criteria>
  <done>A red regression defines what "diagnosable" means for the acceptance runner before any change.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument runAcceptance and bound every acceptance child spawn</name>
  <files>scripts/doctor.cjs</files>
  <read_first>
    - scripts/doctor.cjs lines 733-2100 (acceptance block in full; note each cp.spawnSync call and whether it has a timeout)
    - scripts/doctor.cjs lines 160-350 (flag parsing; DOCTOR_TEST_MODE conventions)
    - tests/test-doctor-acceptance-preflight-checks.cjs and any tests/test-doctor-acceptance*.cjs (must stay green)
    - tests/test-354-acceptance-diagnostics.cjs
  </read_first>
  <action>
Add function runBoundedChild(cmd, args, opts) near the acceptance block: wraps cp.spawnSync(cmd, args, { encoding: 'utf8', ...opts, timeout: opts.timeout || Number(process.env.DOCTOR_ACCEPTANCE_CHILD_TIMEOUT_MS) || 120000, killSignal: 'SIGKILL' }), measures duration_ms, and returns { status, signal, timed_out: !!(r.error && r.error.code === 'ETIMEDOUT'), error_code: r.error ? r.error.code : null, pid: r.pid, duration_ms, stdout, stderr }. Route every acceptance-point child spawn that has no timeout through it with an explicit bound (git probes 10000 ms, node --check 15000 ms); for spawns that already pass a timeout, route them through it keeping their timeout so every point reports detail.child { cmd, timed_out, signal, status, duration_ms, pid } and a point whose child timed out returns ok false with finding 'point <id>: child <cmd> timed out after <ms> ms'. Do not change what any point checks.
In runAcceptance: record t0 per point and add duration_ms to each results entry; add summary.duration_ms and summary.slowest { id, duration_ms }; unless process.env.MINDRIAN_ACCEPTANCE_PROGRESS === '0', write '[acceptance] <id> start' before and '[acceptance] <id> done <ok|FAIL> <ms>ms' after each point to process.stderr (never stdout).
Test-mode hooks, honored only when DOCTOR_TEST_MODE=1 (same convention as the existing DOCTOR_TEST_FAIL_POINT): DOCTOR_TEST_ONLY_POINTS (comma list) filters the checklist after the tier filter; DOCTOR_TEST_HANG_POINT=<id> replaces that point's run with runBoundedChild(process.execPath, ['-e', 'setInterval(function(){}, 1 << 30)']) and maps its timeout to the point failure described above. Update the acceptance header comment to document the three env vars and the progress lines. Do not touch the --all renderer, the class modules or report.checks rendering (Phase 352 scope).
  </action>
  <verify>
    <automated>node tests/test-354-acceptance-diagnostics.cjs && for t in tests/test-doctor-acceptance*.cjs; do node "$t" || exit 1; done && node --check scripts/doctor.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-acceptance-diagnostics.cjs` exits 0
    - `grep -c "runBoundedChild(" scripts/doctor.cjs` is at least 6 (definition plus the rerouted spawns)
    - Inside buildAcceptanceChecklist, no `cp.spawnSync(` call remains without a timeout (count spawnSync calls between the function start and runAcceptance and compare with the timeout-bearing ones; record the counts in the SUMMARY)
    - Every tests/test-doctor-acceptance*.cjs file exits 0
    - `node scripts/doctor.cjs --acceptance --pre-tag --json 2>/dev/null | node -e "JSON.parse(require('fs').readFileSync(0,'utf8'))"` exits 0 (stdout stays clean JSON)
  </acceptance_criteria>
  <done>Every acceptance point reports how long it took and every child it spawns is bounded, so a hang names its phase.</done>
</task>

<task type="auto">
  <name>Task 3: Clean rerun outside the sandbox and classify SYS-07</name>
  <files>.planning/debug/sys-07-acceptance-timing.md</files>
  <read_first>
    - docs/RCA-TEMPLATE.md (kind: rca frontmatter and required sections)
    - scripts/doctor.cjs acceptance block (after Task 2)
    - docs/reviews/phase-354-disposition-ledger.md SYS-07 row
  </read_first>
  <action>
Outside the tool sandbox (the earlier failures were affected by sandbox mock-server restrictions), run and time: (a) `timeout 900 node scripts/doctor.cjs --acceptance --pre-tag --json > $SCRATCH/pretag.json 2> $SCRATCH/pretag.err; echo $?` and (b) `timeout 1800 node scripts/doctor.cjs --acceptance --json > $SCRATCH/full.json 2> $SCRATCH/full.err; echo $?`, using /usr/bin/time -v when available for wall time and peak memory, with SCRATCH set to the session scratchpad directory. Immediately after each run capture `ps -eo pid,ppid,etimes,cmd | grep -E "doctor.cjs|mos-acceptance|release.sh|npm (view|install)" | grep -v grep` to check for orphans. From the JSON and the stderr progress lines, list every point with duration_ms and ok, the slowest five, which points touched the network (version-of-record-published, npx-roundtrip, activation-reached-the-wire and any other point whose detail shows a network call), and any timed_out child.
Classify SYS-07 per project CLAUDE.md QA classification: WORKING (both runs finish within their bound with every point reporting, and failures are real point findings with actionable text), ENV GAP (the only failures or delays are network or environment restrictions, named per point), or NEW FAILURE (a point hangs to its bound or leaves an orphan; name the exact point and child). Write .planning/debug/sys-07-acceptance-timing.md following docs/RCA-TEMPLATE.md with frontmatter kind: rca, the commands, exit codes, durations, environment (node version, HEAD, sandbox off), the per-point table, the orphan check output, the classification and, for NEW FAILURE, the next action (a gap-closure plan owns any fix; this task does not patch production code). Stage it with git add -f.
  </action>
  <verify>
    <automated>test -f .planning/debug/sys-07-acceptance-timing.md && grep -q "kind: rca" .planning/debug/sys-07-acceptance-timing.md && grep -Eq "WORKING|ENV GAP|NEW FAILURE" .planning/debug/sys-07-acceptance-timing.md && git ls-files --error-unmatch .planning/debug/sys-07-acceptance-timing.md</automated>
  </verify>
  <acceptance_criteria>
    - The RCA file exists, is tracked (git ls-files succeeds), has `kind: rca`, and states exactly one of WORKING, ENV GAP or NEW FAILURE as the SYS-07 classification
    - It contains both run commands with exit codes and wall-clock durations, a per-point duration table, and the orphan-check output (empty output recorded as 'no orphan processes')
    - A NEW FAILURE classification names the point id and child command that hung
  </acceptance_criteria>
  <done>SYS-07 is classified from an instrumented clean rerun instead of a single 20-second timeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor -> child processes | Acceptance spawns git, npm, node and bash children, some touching the network |
| doctor stdout -> release.sh and harness parsers | JSON consumers depend on a clean stdout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-26 | Denial of service | unbounded acceptance children | mitigate | runBoundedChild with SIGKILL timeouts on every acceptance spawn; hang regression with orphan check |
| T-354-26b | Tampering | stdout protocol for JSON consumers | mitigate | Progress only on stderr; test parses stdout JSON |
| T-354-26c | Repudiation | test-mode hooks in production | mitigate | DOCTOR_TEST_ONLY_POINTS and DOCTOR_TEST_HANG_POINT honored only under DOCTOR_TEST_MODE=1, matching existing hooks |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-acceptance-diagnostics PASSED.
- The RCA file classifies SYS-07 from measured data.
</verification>

<success_criteria>
An operator can tell a slow acceptance run from a hang, see which point is responsible, and trust that no child outlives the run.
</success_criteria>

## Artifacts this phase produces

- `runBoundedChild(cmd, args, opts)` (new function in scripts/doctor.cjs)
- Acceptance JSON fields `points[].duration_ms`, `points[].detail.child`, `summary.duration_ms`, `summary.slowest`
- Env vars `MINDRIAN_ACCEPTANCE_PROGRESS`, `DOCTOR_ACCEPTANCE_CHILD_TIMEOUT_MS`, test-mode `DOCTOR_TEST_ONLY_POINTS`, `DOCTOR_TEST_HANG_POINT`
- stderr progress lines `[acceptance] <id> start` / `[acceptance] <id> done <ok|FAIL> <ms>ms`
- `tests/test-354-acceptance-diagnostics.cjs` (new regression); `.planning/debug/sys-07-acceptance-timing.md` (new RCA record)

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-13-SUMMARY.md` when done (stage with `git add -f`).
</output>
