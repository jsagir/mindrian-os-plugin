---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reviews/phase-354-disposition-ledger.md
  - tests/run-all-354.sh
  - tests/helpers/fixture-room-354.cjs
  - tests/helpers/playwright-354.cjs
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03, SYS-04, SYS-05, SYS-06, SYS-07, SYS-08, SYS-09, THEO-01, THEO-02, THEO-03]
tags: [ledger, research-gate, test-infra, phase-354]

must_haves:
  truths:
    - "A tracked disposition ledger exists before any production fix lands, giving every candidate ID (SYS-01..07, THEO-01..03) and both newly discovered findings (SYS-08 gate subject promotion, SYS-09 chain resume identity) a disposition, evidence command, observed output, source reference, impact, confidence and owning plan"
    - "Every probe output in the ledger was re-run at execution time against the recorded HEAD commit, not copied from docs/reviews/2026-09-23-deep-system-research.md"
    - "The ledger carries a coverage map naming every production area as examined (with the plan that covers it) or explicitly UNEXAMINED, and an ownership map against phases 273, 345, 350, 351 and 352"
    - "The ledger states the two planning-time decisions with their impact: D-354-SYS05 (extract_shallow is an honest parser) and D-354-EGR (Brain free-form egress requires a closed-vocabulary typed question)"
    - "tests/run-all-354.sh runs every planned Phase 354 test through run_if guards and reports exit code 77 as SKIPPED (ENV GAP), never as PASSED"
  artifacts:
    - path: "docs/reviews/phase-354-disposition-ledger.md"
      provides: "Pre-implementation disposition ledger, coverage map, root-cause groups, ownership map, decision records"
      contains: "D-354-EGR"
    - path: "tests/run-all-354.sh"
      provides: "Phase 354 aggregator with run/run_if legs and exit-77 SKIP handling"
      contains: "run_if"
    - path: "tests/helpers/fixture-room-354.cjs"
      provides: "makeScratchRoom, captureToolServer shared by every 354 test"
      exports: ["makeScratchRoom", "captureToolServer", "SKIP_EXIT_CODE"]
    - path: "tests/helpers/playwright-354.cjs"
      provides: "resolvePlaywright() resolver that never installs a package"
      exports: ["resolvePlaywright"]
  key_links:
    - from: "tests/run-all-354.sh"
      to: "tests/test-354-*.cjs"
      via: "run_if guard per planned test file"
      pattern: "run_if .*test-354-"
    - from: "docs/reviews/phase-354-disposition-ledger.md"
      to: "docs/reviews/phase-354-probes/"
      via: "evidence command column"
      pattern: "phase-354-probes/"
---

<objective>
Publish the pre-implementation disposition ledger the handoff requires ("Before implementation, publish the research ledger, coverage map, root-cause analysis, ownership/dependency map, and a disposition for every ID") and stand up the shared Phase 354 test infrastructure every later plan uses.

Purpose: CTX-RESEARCH-GATE (354-CONTEXT.md "Research gate (already satisfied -- locked)") says the research is evidence to challenge and convert into regression tests. This plan re-runs every probe at execution time, records the dispositions against the current HEAD, and states the two planning-time decisions (D-354-SYS05, D-354-EGR) so the navigator can veto either before wave 2 changes production code. No production code is touched in this plan.

Output: docs/reviews/phase-354-disposition-ledger.md, tests/run-all-354.sh, tests/helpers/fixture-room-354.cjs, tests/helpers/playwright-354.cjs.

Preconditions (every 354 plan): work only in /home/jsagi/dev/MindrianOS-Plugin (never ~/.claude/plugins/). Run `git status --short -- <this plan's files_modified>` first; if any listed file carries an uncommitted change this plan did not make, stop and report it (never revert or overwrite unowned work).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-RESEARCH.md
@docs/reviews/2026-09-23-deep-system-research.md
@docs/2026-09-20-HANDOFF-phase-354-system-integrity-and-theo.md
@docs/reviews/2026-09-20-full-system-code-review.md
@docs/reviews/2026-09-20-localhost-workspace-review.md
@tests/run-all-353.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the five probes and publish the disposition ledger</name>
  <files>docs/reviews/phase-354-disposition-ledger.md</files>
  <read_first>
    - docs/reviews/2026-09-23-deep-system-research.md (full; every finding and the Corrected or bounded section)
    - docs/2026-09-20-HANDOFF-phase-354-system-integrity-and-theo.md (candidate ledger rows and Acceptance and close-out)
    - docs/reviews/phase-354-probes/persistence.cjs, registration.cjs, resources.cjs, theo.cjs, browser.cjs
    - .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
    - docs/reviews/2026-09-20-localhost-workspace-review.md (Verified baseline findings table, lines 124-143)
    - lib/mcp/gate-ledger.cjs lines 97-106 (consumeGate single-use burn)
    - lib/mcp/tools/dual-path.cjs (extract_shallow description versus connector hitl_why)
    - scripts/generate-presentation.cjs lines 600-790 (where chat-panel.js is inlined; check for any Content-Security-Policy)
  </read_first>
  <action>
Record a baseline block: `git rev-parse HEAD`, `node lib/core/repo-version.cjs`, `node --version`, `uname -a`, and `git status --short` (dirty files listed verbatim). Re-run, from the repo root, `node docs/reviews/phase-354-probes/persistence.cjs`, `node docs/reviews/phase-354-probes/registration.cjs`, `node docs/reviews/phase-354-probes/resources.cjs`, `node docs/reviews/phase-354-probes/theo.cjs`, and `node docs/reviews/phase-354-probes/browser.cjs` (the browser probe requires the Playwright install at /home/jsagi/node_modules/playwright; if it fails to load, record the exact error as ENV GAP for that row, do not drop the row). Capture each probe's JSON lines verbatim into the ledger with the timestamp.

Write docs/reviews/phase-354-disposition-ledger.md with these sections, hyphens only (no em-dashes):
(1) Baseline.
(2) Disposition table with columns ID | Finding | Disposition | Evidence command | Observed output (excerpt) | Source refs | Impact | Confidence | Owner plan. Rows and expected dispositions (overwrite with what the re-run actually shows; a mismatch is itself a finding to report): SYS-01 CONFIRMED-BOUNDED (percent-encoded traversal REFUTED through the SDK matcher; symlink escape CONFIRMED on room://section/{sectionName} and artifact_file; reasoning://section/{name} unchecked name at lib/mcp/resources.cjs:260-263) owner 354-05; SYS-02 CONFIRMED (live-lock takeover at write-lock.cjs:59, non-owner release at :101-104) owner 354-04; SYS-03 CONFIRMED (chat-panel.js:20-44,379,539 innerHTML execution; the Anthropic API key sits in localStorage at chat-panel.js:242-250; record whether the exported presentation sets any CSP) owner 354-07; SYS-04 CONFIRMED P2 owner 354-14; SYS-05 DESCRIPTION-HONESTY DEFECT (tool description and agents/larry-extended.md:198 claim writes, connector hitl_why admits zero writes) owner 354-15; SYS-06 CONFIRMED three sub-findings owners 354-07 (render), 354-08 (save format, origin), 354-11 (journey); SYS-07 UNRESOLVED pending instrumentation owner 354-13; SYS-08 NEW, CONFIRMED gate approval confirms decision:gate:* not the subject claim (gate.cjs:303-304) owner 354-02; SYS-09 NEW, CONFIRMED chain resume by command name (chain-executor.cjs:1367-1377) and lost predecessor output (:1331) owner 354-03; THEO-01 CONFIRMED four parts (classification round trip P1, executable chain P1, FEEDS_INTO provenance P2, taxonomy casing P2) owners 354-09, 354-10, live certification 354-12; THEO-02 BLOCKED cross-repository (Phase 351 open with 0 plans, owned by jsagir/theo; /home/jsagi/Theo/.github/workflows holds ci.yml, theo-liveness.yml, theo-seam-audit.yml and no theo-resync consumer at the inspected commit; confirm with a read-only `ls` and record the Theo commit via `git -C /home/jsagi/Theo log -1 --format=%h`) plugin-side verification 354-12; THEO-03 CONFIRMED false-safe egress path (part8-egress-guard.cjs:521-531) owner 354-06, journey verification 354-12.
(3) Observed, not a defect: gate-ledger.cjs:100 consumeGate deletes the entry before the session check, so a wrong-session attempt burns the gate (probe output wrongSession session_mismatch, ownerAfterward null). Disposition: fail-closed single-use by design (anti-replay comment at :100), no bypass; 354-16 re-verifies the claim stays proposed.
(4) Corrected or bounded earlier findings (graph rebuild destruction REFUTED as current behavior; recommendChain casing NOT affected; URI percent-encoding REFUTED; release notification plugin side exists, consumption unproven) each with the 2026-09-20 review line it corrects.
(5) Legacy localhost review findings (ROOM_DATA empty, file event no refresh, mobile overflow, foreign Host on a read endpoint, chat integration) listed as RECHECK in 354-08 (foreign Host) and 354-16 (the rest), never silently excluded.
(6) Coverage map: one row per production area (hooks, installation/update, commands, skills, agents, pipelines, MCP tools and resources, local graph, Brain client and egress, views/editor and localhost POC, chat panel and export, release and doctor tooling) with the examining plan or UNEXAMINED plus the reason.
(7) Root-cause groups: each seam pair (claim versus confirmed node; command name versus step identity; lock age versus owner liveness; lexical check versus realpath write; typed enum versus natural-language echo; denylist plus keyword versus typed proof; ranked candidates versus FEEDS_INTO label; plugin casing versus provider enum).
(8) Ownership map: 273 (single navigation.cjs write chokepoint, check-substrate stays green), 345 (rung-vocabulary.cjs and taxonomy-climb.cjs are 345 artifacts, fixed in place; strategy-card ratifyGoalProposal in gate.cjs must stay byte-behavior-identical), 350 (supersession gate rides gate.cjs, not touched here), 351 (THEO-02 provider consumer, not duplicated), 352 (doctor --all renderer F2/F3/F9 untouched; 354-13 edits only the --acceptance path).
(9) Decision records, stated with impact: D-354-SYS05 extract_shallow is an honest pure parser (reasons: both documented callers run before a room is bound and lib/mcp/runtime-instructions.cjs forbids writing a room artifact before binding; claim_write already is the governed persistence path; the shipped connector declaration already describes a pure parse). D-354-EGR Brain free-form egress (brain_ask, brain_search) is forwarded only when the whole string is proven to be a closed-vocabulary methodology question; impact: a model-issued brain_ask or brain_search containing any token outside the closed vocabulary (for example a venture name, a date or a possessive such as "our") is refused with the existing honest egress_blocked envelope (bin/mindrian-brain-mcp-client.cjs Phase 257 branch) instead of being forwarded with a disclosure; plugin-internal templated questions keep working because 354-06 proves each template against the vocabulary. State that the navigator may veto either decision before wave 3 runs.
Never paste room content, secrets or keys into the ledger; probes use synthetic text only.
  </action>
  <verify>
    <automated>test -f docs/reviews/phase-354-disposition-ledger.md && for id in SYS-01 SYS-02 SYS-03 SYS-04 SYS-05 SYS-06 SYS-07 SYS-08 SYS-09 THEO-01 THEO-02 THEO-03; do grep -q "| $id " docs/reviews/phase-354-disposition-ledger.md || { echo "missing $id"; exit 1; }; done && grep -q "D-354-SYS05" docs/reviews/phase-354-disposition-ledger.md && grep -q "D-354-EGR" docs/reviews/phase-354-disposition-ledger.md && ! grep -q $'\xe2\x80\x94' docs/reviews/phase-354-disposition-ledger.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/reviews/phase-354-disposition-ledger.md contains a table row starting `| SYS-01 ` through `| SYS-09 ` and `| THEO-01 ` through `| THEO-03 ` (12 rows), each with a non-empty Owner plan cell
    - The Baseline section contains the literal output of `git rev-parse HEAD` at execution time and the `node lib/core/repo-version.cjs` output
    - The ledger contains the re-run JSON line `"probe":"gate-subject-confirmation"` with the claim row still `"review_status":"proposed"` (or, if it no longer reproduces, a REFUTED disposition with the new output)
    - The ledger contains the strings `D-354-SYS05`, `D-354-EGR`, `UNEXAMINED` or an explicit statement that no area is unexamined, and each of `273`, `345`, `350`, `351`, `352` in the ownership section
    - `grep -c $'\xe2\x80\x94' docs/reviews/phase-354-disposition-ledger.md` prints 0
  </acceptance_criteria>
  <done>The ledger is committed, every ID has a disposition backed by an execution-time probe run, and both planning decisions are stated with impact.</done>
</task>

<task type="auto">
  <name>Task 2: Shared Phase 354 test infrastructure (aggregator, scratch-room helper, Playwright resolver)</name>
  <files>tests/run-all-354.sh, tests/helpers/fixture-room-354.cjs, tests/helpers/playwright-354.cjs</files>
  <read_first>
    - tests/run-all-353.sh (run/run_if/counter shape and the targeted em-dash guard; reuse the shape)
    - tests/helpers/fixture-room-348.cjs (existing scratch-room helper convention)
    - docs/reviews/phase-354-probes/persistence.cjs (makeRoom and the captured server.tool map idiom to extract)
    - docs/reviews/phase-354-probes/browser.cjs (the hard-coded Playwright require this resolver replaces)
  </read_first>
  <action>
Create tests/helpers/fixture-room-354.cjs exporting: SKIP_EXIT_CODE = 77; makeScratchRoom(label) which mkdtemps a root under os.tmpdir() named `mos-354-<label>-`, sets process.env.MINDRIAN_ROOMS_HOME to `<root>/rooms-home`, creates `<root>/<label>` and opens then closes its room.db through lib/core/room-db.cjs openRoomDb/closeRoomDb (the persistence.cjs makeRoom idiom), and returns { root, room, cleanup } where cleanup removes root recursively; captureToolServer() returning { server, handlers } where server.tool(name, description, schema, handler) stores handler in the handlers Map and server.registerTool(name, config, handler) does the same (both registration idioms exist in this repo). CJS, no dependencies, hyphens only.

Create tests/helpers/playwright-354.cjs exporting resolvePlaywright(): try require('playwright'), then process.env.MINDRIAN_PLAYWRIGHT_PATH, then path.join(os.homedir(), 'node_modules', 'playwright'); return the module or null. It never installs anything (no npm, no npx); a null return means the calling browser test must print `ENV GAP: playwright unavailable` and exit SKIP_EXIT_CODE.

Create tests/run-all-354.sh modeled on tests/run-all-353.sh: set -uo pipefail, cd to repo root, PASS/FAIL/SKIP counters, run() treating exit 0 as PASSED, exit 77 as SKIPPED (ENV GAP) and anything else as FAILED, run_if(label, guard, cmd...) skipping a missing file. One run_if leg per planned test file, in wave order: test-354-gate-subject-promotion.cjs (SYS-08), test-354-chain-resume-identity.cjs (SYS-09), test-354-write-lock-ownership.cjs (SYS-02), test-354-room-symlink-containment.cjs (SYS-01), test-354-egress-typed-question.cjs (THEO-03), test-354-chat-inert-render.cjs (SYS-03), test-354-poc-save-origin.cjs (SYS-06), test-354-dashboard-host-read.cjs (SYS-06 adjacent), test-354-theo-router-contract.cjs (THEO-01), test-354-taxonomy-ladder-casing.cjs (THEO-01), test-354-poc-room-journey.cjs (SYS-06), test-354-theo-journey.cjs (THEO-03), test-354-theo-live-contract.cjs (THEO-01/THEO-03 live, SKIPs without MINDRIAN_354_LIVE=1), test-354-acceptance-diagnostics.cjs (SYS-07), test-354-registration-diagnostics.cjs (SYS-04), test-354-extract-shallow-contract.cjs (SYS-05), test-354-concurrency-surfaces.cjs (acceptance close-out). Add a targeted em-dash guard over the non-test files Phase 354 plans modify (list them explicitly as a bash array; a missing file is skipped). Final line prints `PASSED=<n> FAILED=<n> SKIPPED=<n>`; exit 1 when FAILED > 0. This aggregator is written once here; later plans add their test file and the run_if leg picks it up without editing this file. chmod +x the script.
  </action>
  <verify>
    <automated>bash -n tests/run-all-354.sh && node -e "const f=require('./tests/helpers/fixture-room-354.cjs');const r=f.makeScratchRoom('selftest');const fs=require('fs');if(!fs.existsSync(r.room))process.exit(1);r.cleanup();if(f.SKIP_EXIT_CODE!==77)process.exit(1);const p=require('./tests/helpers/playwright-354.cjs');console.log('playwright', !!p.resolvePlaywright())" && bash tests/run-all-354.sh; test $? -le 1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/run-all-354.sh` exits 0 and the file is executable
    - `grep -c "run_if" tests/run-all-354.sh` is at least 17 (one leg per planned test file plus the function definition)
    - `grep -q "77" tests/run-all-354.sh` succeeds and the run() function reports exit 77 as SKIPPED
    - `node -e "require('./tests/helpers/fixture-room-354.cjs').makeScratchRoom('x')"` creates a room directory containing `.mindrian/room.db`
    - tests/helpers/playwright-354.cjs contains no `npm` or `npx` invocation (`grep -cE "npm |npx " tests/helpers/playwright-354.cjs` prints 0)
    - Running `bash tests/run-all-354.sh` right after this plan reports every leg SKIPPED (missing file) and exits 0
  </acceptance_criteria>
  <done>Later plans can drop a tests/test-354-*.cjs file and have it run by the aggregator, with scratch rooms and Playwright resolution shared instead of re-implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe output -> tracked doc | Probe stdout is copied into a tracked, public-facing review document |
| test helper -> host filesystem | makeScratchRoom creates and deletes directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-30 | Information disclosure | docs/reviews/phase-354-disposition-ledger.md | mitigate | Probes use synthetic text only; Task 1 forbids pasting room content, keys or secrets; the brain key the theo probe sets is the literal string synthetic-phase-354-key and is never real |
| T-354-31 | Tampering | tests/helpers/fixture-room-354.cjs cleanup | mitigate | cleanup removes only the mkdtemp root it created under os.tmpdir(), never a caller-supplied path |
| T-354-SC | Tampering | package installs | accept | This phase installs no package; playwright-354.cjs resolves an existing install and returns null otherwise (no npm/npx calls, asserted by acceptance criteria) |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` exits 0 with every leg SKIPPED (no 354 test exists yet).
- The ledger lists 12 IDs with owners and both decision records.
</verification>

<success_criteria>
The research gate's publication step is complete and tracked; every later plan has a named owner row in the ledger and a shared test harness.
</success_criteria>

## Artifacts this phase produces

- `docs/reviews/phase-354-disposition-ledger.md` (new document; later updated by 354-12 evidence references and finalized by 354-16)
- `tests/run-all-354.sh` (new aggregator; functions `run`, `run_if`)
- `tests/helpers/fixture-room-354.cjs` (new; exports `makeScratchRoom`, `captureToolServer`, `SKIP_EXIT_CODE`)
- `tests/helpers/playwright-354.cjs` (new; exports `resolvePlaywright`)
- New requirement IDs referenced: `SYS-08` (gate subject promotion), `SYS-09` (chain resume identity), registered in .planning/REQUIREMENTS.md at plan time
- Decision labels: `D-354-SYS05`, `D-354-EGR`

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-01-SUMMARY.md` when done (`.planning/` is gitignored; stage it with `git add -f`).
</output>
