---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 07
type: execute
wave: 3
depends_on: ["354-02", "354-03", "354-04"]
files_modified:
  - tests/test-354-chat-inert-render.cjs
  - lib/chat/chat-panel.js
  - lib/chat/generative-tools.js
autonomous: true
requirements: [SYS-03, SYS-06]
tags: [xss, chat-panel, browser, boundary-integrity, tier-2]

must_haves:
  truths:
    - "An assistant message carrying <img src=x onerror=...> renders as visible text in a real Chromium page and the payload never executes, on both the whole-message path (_renderMessage) and the streamed text_delta path"
    - "Useful formatting survives: bold, italic, inline code, fenced code blocks and list items still render as <strong>, <em>, <code>, <pre> and <li>"
    - "A payload inside inline code, inside a fenced block, inside bold text or inside a tool component title never executes"
    - "A tool component color value that is not a hex color is dropped, so no CSS or markup can be injected through insight_card items"
    - "The visitor API key stored in localStorage is not readable by any rendered payload (no script runs)"
  artifacts:
    - path: "lib/chat/chat-panel.js"
      provides: "Escape-first renderMarkdown with fenced-code extraction; escapeHtml escapes & < > \" '"
      contains: "escapeHtml"
    - path: "lib/chat/generative-tools.js"
      provides: "Hex-validated color, single-quote escaping in component renderers"
      contains: "#[0-9a-fA-F]"
    - path: "tests/test-354-chat-inert-render.cjs"
      provides: "Playwright regression on streamed, whole-message and component paths"
      min_lines: 90
  key_links:
    - from: "lib/chat/chat-panel.js _renderMessage and the content_block_delta handler"
      to: "renderMarkdown"
      via: "innerHTML receives only escape-first output"
      pattern: "innerHTML = renderMarkdown"
---

<objective>
Make assistant rendering inert (CTX-POC sub-finding 1 in 354-CONTEXT.md; handoff SYS-03): lib/chat/chat-panel.js:20-44 builds HTML from raw assistant text with regex replacements and assigns it through innerHTML at :379 and :539, so a synthetic onerror payload executed. The two layers that disagree: the markdown renderer's assumption that model text is trusted versus the page, which holds the visitor's Anthropic API key in localStorage (chat-panel.js:242-250). Traced before asserting exploitability (SYS-03 row): the exported presentation inlines this file (scripts/generate-presentation.cjs:774) with inline scripts and no CSP, so escaping is the control that matters. There is no restored-history path in chat-panel.js (no transcript is persisted; verified by grep at plan time), so "restored" rendering is recorded N/A in the ledger.

Fix pattern: escape first, then apply the fixed set of markdown transforms that only ever insert static tags, so no model byte is ever interpreted as markup.

Test-first (CTX-TESTFIRST): Task 1 red, Task 2 green.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/reviews/phase-354-probes/browser.cjs
@lib/chat/chat-panel.js
@lib/chat/generative-tools.js
@tests/helpers/playwright-354.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing browser regression - inert rendering on every assistant path</name>
  <files>tests/test-354-chat-inert-render.cjs</files>
  <read_first>
    - docs/reviews/phase-354-probes/browser.cjs lines 25-37 (seed: addScriptTag chat-panel.js, Object.create(ChatPanel.prototype), _renderMessage with an onerror payload, waitForFunction on a window flag)
    - lib/chat/chat-panel.js lines 17-45, 370-385, 500-560, 620-665
    - lib/chat/generative-tools.js lines 110-219
    - tests/helpers/playwright-354.cjs
  </read_first>
  <action>
Create tests/test-354-chat-inert-render.cjs. resolvePlaywright(); when null print `ENV GAP: playwright unavailable` and exit 77. Launch chromium, open a blank page served from a tiny local node:http server on 127.0.0.1 (random port) so the page has an http origin with localStorage; addScriptTag with lib/chat/chat-panel.js and lib/chat/generative-tools.js; set localStorage 'mos-chat-api-key' (or the exact KEY_API value from chat-panel.js) to 'SYNTHETIC_KEY'; set window.__pwned = false.
Cases, each rendering through the real prototype methods (panel = Object.create(ChatPanel.prototype) with _msgList a fresh div and _scrollToBottom a no-op, as the probe does):
(P1) _renderMessage('assistant', '<img src=/missing onerror="window.__pwned=true">');
(P2) inline code: 'see `<img src=/m onerror="window.__pwned=true">` here';
(P3) fenced: three backticks, newline, '<script>window.__pwned=true</script>', newline, three backticks;
(P4) bold wrapper: '**<img src=/m onerror="window.__pwned=true">**';
(P5) attribute break: 'x" onmouseover="window.__pwned=true' inside bold and italic markers;
(P6) streamed path: drive the same code the content_block_delta branch runs; if that branch cannot be invoked without the network, call the exact expression the branch uses (renderMarkdown via the element assignment) through a small exported seam only if one exists, otherwise assert through _renderMessage and state in a comment that both sites call the same renderMarkdown;
(P7) component: renderToolComponent / the generative-tools renderComponent for an insight_card with title '<img src=/m onerror="window.__pwned=true">' and an item color 'red;background-image:url(/leak)' and 'x" onmouseover="window.__pwned=true'.
After each case wait 300 ms and assert window.__pwned === false and no element in _msgList has an attribute starting with 'on'. Formatting case (F1): '**bold** *it* `code`' plus a fenced block plus '- item' renders exactly one strong, one em, at least one code, one pre and one li. Close the browser in finally. Run against unfixed code: P1 must fail.
  </action>
  <verify>
    <automated>node tests/test-354-chat-inert-render.cjs; rc=$?; test $rc -eq 1 && echo "RED as expected" || { test $rc -eq 77 && echo "ENV GAP"; }</automated>
  </verify>
  <acceptance_criteria>
    - The test resolves Playwright only through tests/helpers/playwright-354.cjs and exits 77 (not 0) when it is unavailable
    - The test contains cases P1-P7 and F1 and asserts `window.__pwned === false` after each P case
    - Against the pre-fix chat-panel.js the test exits 1 with P1 failing (the payload executes)
  </acceptance_criteria>
  <done>A red browser-level regression proves assistant HTML executes today on the real prototype methods.</done>
</task>

<task type="auto">
  <name>Task 2: Escape-first markdown rendering and hardened component renderers</name>
  <files>lib/chat/chat-panel.js, lib/chat/generative-tools.js</files>
  <read_first>
    - lib/chat/chat-panel.js lines 17-45 (renderMarkdown, escapeHtml), 370-385, 530-545, 645-660
    - lib/chat/generative-tools.js lines 110-219 (escapeHtml, renderInsightCard, renderList, renderTable)
    - scripts/generate-presentation.cjs lines 600-620 and 770-780 (how these files are inlined; keep the var/IIFE shape)
    - tests/test-354-chat-inert-render.cjs
  </read_first>
  <action>
In lib/chat/chat-panel.js: make escapeHtml(str) escape all five characters & < > " ' (String(str) first). Rewrite renderMarkdown(text) as escape-first: (1) extract fenced blocks with the existing fence regex into an array, replacing each with a unique sentinel token that cannot appear in escaped text (for example \u0000FENCE<n>\u0000), storing escapeHtml(code.trim()); (2) escapeHtml the whole remaining text; (3) apply the existing inline code, bold, italic, unordered list, ordered list and newline transforms on the escaped text (their capture groups are already escaped, and each transform inserts only its fixed tag with the existing static style attribute); (4) restore each sentinel with the existing pre/code wrapper around the stored escaped code. Keep the var/IIFE style, zero dependencies, and the existing visual styles. _renderMessage and the text_delta branch keep assigning innerHTML = renderMarkdown(...), which is now inert by construction.
In lib/chat/generative-tools.js: escapeHtml also escapes the single quote (&#39;); add a module-private safeColor(value) returning the value only when it matches /^#[0-9a-fA-F]{3,8}$/ and otherwise null; renderInsightCard renders the color dot only when safeColor returns a value; every interpolation already routed through escapeHtml stays so, and any interpolation that is not (verify table headers, list items, footer, type label) is routed through it.
Do not add a CSP to exported presentations in this task (they inline scripts by design); record in the SUMMARY that escaping is the enforced control and name the inline-script constraint.
  </action>
  <verify>
    <automated>node tests/test-354-chat-inert-render.cjs && node scripts/check-render-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-chat-inert-render.cjs` exits 0 on this machine (Playwright is present at /home/jsagi/node_modules/playwright)
    - `grep -n "replace(/'/g" lib/chat/chat-panel.js lib/chat/generative-tools.js` (or an equivalent single-quote escape) matches in both files
    - `grep -n "safeColor" lib/chat/generative-tools.js` shows the function and its use in renderInsightCard
    - The browser probe's assistant-html line no longer reports `"executed":true` (the probe's waitForFunction times out instead; record that output as the green evidence)
    - F1 formatting assertions pass (strong, em, code, pre, li all present)
  </acceptance_criteria>
  <done>No assistant or tool-component byte is interpreted as markup, and the chat still shows bold, italics, code and lists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output -> DOM | Assistant text and tool_use inputs arrive from the Anthropic API and are rendered into a page |
| page -> localStorage | The page holds the visitor's API key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-14 | Information disclosure / Elevation | chat-panel.js renderMarkdown -> innerHTML | mitigate | Escape-first rendering; fenced code extracted and escaped; browser regression P1-P6 |
| T-354-15 | Tampering | generative-tools.js insight_card color | mitigate | safeColor hex validation; single-quote escaping; regression P7 |
| T-354-14b | Information disclosure | exported presentation without CSP | accept | Inline scripts are the export's design; escaping is the enforced control; recorded in the ledger and close-out as residual |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-chat-inert-render PASSED (or SKIPPED only where Playwright is absent).
</verification>

<success_criteria>
The browser probe's assistant-html execution no longer reproduces, and formatting is preserved.
</success_criteria>

## Artifacts this phase produces

- `tests/test-354-chat-inert-render.cjs` (new Playwright regression; exit 77 on ENV GAP)
- `safeColor(value)` (new module-private in lib/chat/generative-tools.js)
- Rewritten `renderMarkdown(text)` and `escapeHtml(str)` in lib/chat/chat-panel.js (same names, escape-first behavior)

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-07-SUMMARY.md` when done (stage with `git add -f`).
</output>
