---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 18
type: execute
wave: 6
depends_on: ["354-01"]
files_modified:
  - CLAUDE.md
  - docs/GROUNDING-SOURCES.md
  - scripts/check-theo-mcp-exposure.cjs
  - scripts/doctor.cjs
  - tests/test-354-theo-mcp-exposure.cjs
autonomous: true
requirements: [THEO-04]
tags: [theo, mcp, egress, advisory, tier-5]

must_haves:
  truths:
    - "CLAUDE.md's Theo grounding-source entry states explicitly, in words a future session will actually read before calling a tool: any Brain-adjacent question carrying plugin-user or venture-room content MUST go through the guarded mindrian-brain shim (bin/mindrian-brain-mcp-client.cjs), NEVER mcp__theo__* directly; the raw theo server is for questions about Theo's own repository/schema/code only"
    - "scripts/check-theo-mcp-exposure.cjs --check runs fully offline (reads MCP config only, opens no network connection, calls no vendor) and exits 0 always (advisory, never blocks) while printing a named WARN when both a mindrian-brain-shaped entry and a theo-shaped entry are registered for the same session scope"
    - "doctor.cjs --acceptance includes this check in its advisory registry (the check-shape-declaration.cjs pattern: id + script, WARN-level, never a blocker) without changing --acceptance's exit-code contract"
  artifacts:
    - path: "scripts/check-theo-mcp-exposure.cjs"
      provides: "Offline, advisory, WARN-only scan of MCP server registrations for the raw-theo-bypasses-egress-guard exposure"
      contains: "THEO-04"
    - path: "CLAUDE.md"
      provides: "The Theo grounding-source rule a future session actually reads before choosing which tool to call"
      contains: "mindrian-brain"
  key_links:
    - from: "scripts/doctor.cjs --acceptance advisory registry"
      to: "scripts/check-theo-mcp-exposure.cjs"
      via: "{ id: 'theo-mcp-exposure', script: 'check-theo-mcp-exposure.cjs' } entry, same shape as the shape-declaration entry"
      pattern: "theo-mcp-exposure"
---

<objective>
Close THEO-04 (354-CONTEXT.md Addendum 2026-09-23) with documentation and an advisory check, per the navigator's explicit decision: document + procedural discipline, not removal, not a code fix. THEO-04 is not a bug in either MCP server -- it is a structural exposure that exists whenever both this plugin's guarded `mindrian-brain` shim (routes through `lib/core/part8-egress-guard.cjs`, patched by 354-06/D-354-EGR) and the raw `theo` MCP server (`~/.claude.json`, `node /home/jsagi/Theo/dist/index.js`, CLAUDE.md's own ruled standing consult for Theo-repo-own dev work, 2026-09-02) are registered in the same session. `theo-mcp.onrender.com` appears in exactly one place in this repo (`lib/core/brain-client.cjs`); the raw `theo` server never touches that code and never touches `part8-egress-guard.cjs`.

This plan does NOT touch `/home/jsagi/Theo` (out of scope, per the standing "never patch or deploy that repository incidentally from this plugin phase" rule) and does NOT unregister the global `theo` MCP entry (navigator declined that option). It makes the risk visible in the two places a future session or operator will actually encounter it: the grounding-source doc a session reads before calling a Brain-adjacent tool, and an offline advisory doctor check that names the exposure whenever it is live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/GROUNDING-SOURCES.md
@scripts/check-shape-declaration.cjs
@scripts/doctor.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the routing rule where a session actually reads it</name>
  <files>CLAUDE.md, docs/GROUNDING-SOURCES.md</files>
  <read_first>
    - CLAUDE.md "Consult ALL Relevant Grounding Sources During Dev Work (MANDATORY)" section (the existing Theo standing-consult bullet, 2026-09-02 ruling)
    - docs/GROUNDING-SOURCES.md lines 40-56 (the full Theo entry: what it's for, `notes/graph-rulebook.md`, readiness scoring)
    - .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md (THEO-04 addendum, exact wording of the required rule)
  </read_first>
  <action>
In CLAUDE.md's Theo grounding-source bullet, append one explicit sentence (do not soften or paraphrase the CONTEXT.md wording, do not remove the existing standing-consult ruling): "Any Brain-adjacent question that carries plugin-user or venture-room content MUST go through the guarded `mindrian-brain` MCP shim (`bin/mindrian-brain-mcp-client.cjs`), never `mcp__theo__*` directly -- the raw `theo` server (`~/.claude.json`, `node /home/jsagi/Theo/dist/index.js`) is for questions about Theo's own repository, schema or code only, and it never passes through `lib/core/part8-egress-guard.cjs` (THEO-04)."
In docs/GROUNDING-SOURCES.md's Theo entry, add the same rule with one added sentence of mechanism: name that `theo-mcp.onrender.com` appears only in `lib/core/brain-client.cjs` in this repo, so a reader can verify the claim themselves rather than take it on faith.
  </action>
  <verify>
    <automated>grep -c "THEO-04" CLAUDE.md; grep -c "part8-egress-guard" docs/GROUNDING-SOURCES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mcp__theo__\* directly" CLAUDE.md` matches inside the existing Theo grounding-source bullet (not a new, disconnected section)
    - `grep -n "part8-egress-guard" docs/GROUNDING-SOURCES.md` matches inside the existing Theo entry
    - The existing 2026-09-02 standing-consult ruling text is still present, byte-unchanged, in both files (this task adds one rule, it does not rewrite the entry)
  </acceptance_criteria>
  <done>A session reading either file before calling a Brain-adjacent tool sees the routing rule and can verify it against the actual code.</done>
</task>

<task type="auto">
  <name>Task 2: Offline advisory check, wired into doctor.cjs --acceptance</name>
  <files>scripts/check-theo-mcp-exposure.cjs, scripts/doctor.cjs, tests/test-354-theo-mcp-exposure.cjs</files>
  <read_first>
    - scripts/check-shape-declaration.cjs (whole file -- the advisory, WARN-only, `--check`/`--strict` pattern this new script mirrors; it enumerates from disk and never blocks by default)
    - scripts/doctor.cjs lines 1130-1160 (the advisory registry array `{ id, script }` entries; find the exact array name and how `--acceptance` iterates it)
    - ~/.claude.json and this repo's own .mcp.json (read-only, to confirm the exact JSON shape: top-level `mcpServers` map keyed by server name, each entry an object; a `theo`-shaped entry is one whose `args` contains a path ending `/Theo/dist/index.js` OR whose command line matches `/Theo/`; a `mindrian-brain`-shaped entry is one whose `args` contains `mindrian-brain-mcp-client.cjs`)
  </read_first>
  <action>
Create scripts/check-theo-mcp-exposure.cjs: pure offline scan, opens no network connection, requires no key. Read `~/.claude.json` (global) and this repo's own `.mcp.json`, tolerating either being absent or unparseable (treat as "no entries found there", never throw). Walk every `mcpServers` map found in both locations (the global file may nest project-scoped server maps; walk one level of project-scoped nesting the same way CLAUDE.md's own doctor tooling already expects, matching whatever shape the actual file uses -- inspect it in read_first before assuming a shape). Classify each entry: `theo-shaped` when its command/args reference `/Theo/dist/index.js` or similar (`/Theo` path segment), `guarded-shim-shaped` when its args reference `mindrian-brain-mcp-client.cjs`. Exit 0 always (this is advisory, THEO-04's navigator decision was explicitly not-a-blocker). `--check` (default): print a WARN line naming THEO-04, the exact config path(s) where both shapes were found registered (or "no exposure found" when only one or neither is present), and a one-line pointer to CLAUDE.md's Theo bullet. No `--strict` escalation flag for this one (unlike check-shape-declaration.cjs) -- the navigator's decision was documentation, not enforcement; do not invent a stricter mode this decision didn't ask for.
In scripts/doctor.cjs, add this check to the SAME advisory registry array `check-shape-declaration.cjs` is already registered in: `{ id: 'theo-mcp-exposure', script: 'check-theo-mcp-exposure.cjs' }`, positioned as a sibling entry (same array, same iteration, same WARN-collection behavior `--acceptance` already applies to every entry in that array). Do not change `--acceptance`'s exit-code contract, its point count semantics, or any other registry entry.
Create tests/test-354-theo-mcp-exposure.cjs: (T1) with a temp HOME containing a synthetic `.claude.json` carrying both a theo-shaped and a mindrian-brain-shaped entry, assert the script exits 0 and its stdout names THEO-04 and both config paths. (T2) with only a mindrian-brain-shaped entry, assert exit 0 and "no exposure found" (or equivalent) in stdout. (T3) with a malformed/missing `.claude.json`, assert exit 0 and no throw. (T4) confirm zero network: stub `global.fetch`/`http.request` to throw if called, run the script, assert it still exits 0.
  </action>
  <verify>
    <automated>node scripts/check-theo-mcp-exposure.cjs --check; test $? -eq 0 && echo "advisory-exit-0-ok"; node tests/test-354-theo-mcp-exposure.cjs; grep -q "theo-mcp-exposure" scripts/doctor.cjs && echo "wired"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-theo-mcp-exposure.cjs --check` exits 0 regardless of what it finds (advisory, never blocks)
    - `grep -n "THEO-04" scripts/check-theo-mcp-exposure.cjs` matches
    - `grep -n "theo-mcp-exposure" scripts/doctor.cjs` matches inside the same advisory registry array as the `shape-declaration` entry (same array literal, not a new parallel list)
    - tests/test-354-theo-mcp-exposure.cjs T1-T4 all pass, including T4's zero-network assertion
    - `node scripts/doctor.cjs --acceptance` exits with the same status/point-count contract it had before this task (this check is additive, never a new blocker)
  </acceptance_criteria>
  <done>A live theo+mindrian-brain co-registration is named, offline, every time doctor's acceptance checklist runs -- without ever failing the checklist because of it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session MCP configuration -> egress guard applicability | Whether a given Brain-adjacent tool call in a session passes through `part8-egress-guard.cjs` depends entirely on which MCP server the caller chooses, a choice this plan cannot enforce technically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-24 | Information disclosure | raw `theo` MCP server bypassing `part8-egress-guard.cjs` | accept (navigator-ruled, documented) | Not fixable in code from this repo (Theo's own binary, out of scope); CLAUDE.md/GROUNDING-SOURCES.md rule plus a WARN-level offline doctor check make the exposure visible every relevant session instead of silent |
| T-354-25 | Tampering | the advisory check itself becoming a false sense of enforcement | mitigate | Script and CLAUDE.md wording both state plainly this is advisory-only, no `--strict` mode invented, no claim of blocking made anywhere in the plan |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-theo-mcp-exposure PASSED.
- `node scripts/doctor.cjs --acceptance` still exits with its pre-existing status contract.
</verification>

<success_criteria>
THEO-04 is closed the way the navigator directed: documented where a session will read it, and surfaced by an offline, zero-network, non-blocking advisory check every time doctor's acceptance checklist runs -- not silently left as tribal knowledge in a CONTEXT.md only this phase reads.
</success_criteria>

## Artifacts this phase produces

- `scripts/check-theo-mcp-exposure.cjs` (new, offline, advisory, WARN-only, no `--strict`)
- `tests/test-354-theo-mcp-exposure.cjs` (new regression)
- `scripts/doctor.cjs`: one new advisory registry entry `{ id: 'theo-mcp-exposure', script: 'check-theo-mcp-exposure.cjs' }`
- `CLAUDE.md`, `docs/GROUNDING-SOURCES.md`: one added rule sentence each in the existing Theo entry

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-18-SUMMARY.md` when done (stage with `git add -f`).
</output>