---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 11
type: execute
wave: 5
depends_on: ["354-09", "354-10"]
files_modified:
  - tests/test-354-poc-room-journey.cjs
  - docs/reviews/localhost-poc/server.cjs
  - docs/reviews/localhost-poc/app.js
  - docs/reviews/localhost-poc/styles.css
  - docs/reviews/localhost-poc/README.md
autonomous: true
requirements: [SYS-06]
tags: [localhost-poc, journey, governed-write, graph, tier-4]

must_haves:
  truths:
    - "With one temporary room, a real browser can: see which room is bound, edit and save the working file, reload and see the saved bytes, see the file indexed as a node in the room graph through the governed filing path, inspect it on the graph tab, ask the graph a question and get an answer that lists node ids and source references, and see an external edit to the file appear without a manual reload"
    - "The working file is filed through the same governed path artifact_file uses (views.cjs fileArtifact: section containment, artifact_id, memory_event, claim node), not written raw into the room root"
    - "The graph answer panel is labelled as a deterministic graph lookup with no model, visibly distinct from any conversation"
    - "An external edit while the editor has unsaved changes shows a conflict banner and never silently discards either version"
    - "A server bound to room A never reads or writes room B"
  artifacts:
    - path: "tests/test-354-poc-room-journey.cjs"
      provides: "Playwright end-to-end journey on one temporary room plus a room-isolation leg"
      min_lines: 140
    - path: "docs/reviews/localhost-poc/server.cjs"
      provides: "Room mode with explicit binding, governed filing through fileArtifact, grounded graph answers with references, revision polling"
      contains: "fileArtifact"
  key_links:
    - from: "docs/reviews/localhost-poc/server.cjs POST /api/document (room mode)"
      to: "lib/mcp/tools/views.cjs _internal.fileArtifact"
      via: "navigation.openRoomDbForCaller -> fileArtifact(db, ROOM, { section, filename, content }) -> closeRoomDbForCaller"
      pattern: "_internal\\.fileArtifact"
    - from: "docs/reviews/localhost-poc/app.js"
      to: "GET /api/document/revision"
      via: "polling every 2000 ms"
      pattern: "/api/document/revision"
---

<objective>
Prove the complete browser-to-room-to-graph journey the handoff's acceptance requires ("One temporary room supports a real end-to-end journey: select/bind room, edit/save/reopen file, index through governed graph writes, inspect graph, ask a grounded question with references, and observe an external edit. Trace each promised propagation step and expose unsupported ones honestly") and the remaining SYS-06 row items (room isolation, graph refresh, graph-answer provenance, document placement against artifact rules, deterministic lookup visibly distinct from model conversation). This is tier 4 of the locked repair sequence (CTX-SEQ).

Document placement decision (SYS-06 "Research document placement against actual artifact rules"): the committed room mode wrote workspace-poc.md into the room root with a raw fs.writeFileSync, bypassing the governed filing path. In room mode the working file is filed through lib/mcp/tools/views.cjs _internal.fileArtifact (the same implementation the artifact_file MCP tool uses, with 354-05's realpath containment and atomic write) into `<room>/<section>/workspace-poc.md`, section from `--section <slug>` validated by SECTION_RE (default `workspace`). One governed writer; no new writer (Reuse Before Build, Canon Part 9 single chokepoint).

This plan is journey verification plus the room-mode wiring it needs; it has no pre-existing CONFIRMED finding of its own, so its test (Task 1) is written first and is expected red against the committed room mode for the placement, references and external-edit legs.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply; 354-08 is merged (origin, token, revision, lossless editor).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/reviews/localhost-poc/server.cjs
@docs/reviews/localhost-poc/app.js
@lib/mcp/tools/views.cjs
@tests/helpers/playwright-354.cjs
@tests/helpers/fixture-room-354.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Journey test on one temporary room (expected red on placement, references and external edit)</name>
  <files>tests/test-354-poc-room-journey.cjs</files>
  <read_first>
    - docs/reviews/localhost-poc/server.cjs and app.js (after 354-08)
    - tests/test-354-poc-save-origin.cjs (spawn and page helpers to reuse by copying their shape)
    - lib/mcp/tools/views.cjs lines 150-300 (fileArtifact: section gate, artifact id, memory_event, claim:artifact node id)
    - lib/core/navigation.cjs getGraphExport (node data fields available for references, for example source path fields)
    - tests/helpers/playwright-354.cjs, tests/helpers/fixture-room-354.cjs
  </read_first>
  <action>
Create tests/test-354-poc-room-journey.cjs. resolvePlaywright() or exit 77. Create room A and room B with makeScratchRoom (distinct labels) and give room A a ROOM.md. Spawn docs/reviews/localhost-poc/server.cjs with `<port> --room <roomA> --section workspace` on a reserved port. Steps, each asserted:
(J1 bind) the page shows room A's folder name and 'room' mode (for example GET /api/room returns { mode: 'room', name } and the header renders the name); starting the server with --room pointing at a directory with no .mindrian/room.db exits non-zero with a message naming the missing room.db (explicit binding, no silent fallback).
(J2 edit, save, reopen) type a line containing JOURNEY_EDIT_1 into #editor, save, reload the page; the editor contains JOURNEY_EDIT_1; the file exists at <roomA>/workspace/workspace-poc.md; <roomA>/workspace-poc.md does not exist.
(J3 governed index) open room A's room.db with a fresh handle: a node whose id starts with 'claim:artifact:' exists for this file and a memory_event row with label artifact_file and filename workspace-poc.md exists; saving twice more yields exactly one claim:artifact node for this artifact (no duplicate per save).
(J4 inspect graph) the graph tab renders at least one element whose text includes the saved file's first body line (or its node label) after the save, fetched fresh from GET /api/graph.
(J5 grounded ask) ask a question containing a word from JOURNEY_EDIT_1's line; the answer panel heading reads 'Graph lookup (deterministic, no model)', and the references list contains the claim:artifact node id and the relative source path workspace/workspace-poc.md.
(J6 external edit, clean editor) append EXTERNAL_EDIT_2 to the file from Node; within 5 s the editor shows EXTERNAL_EDIT_2 without a manual reload.
(J7 external edit, dirty editor) type unsaved text, then append EXTERNAL_EDIT_3 from Node; within 5 s a conflict banner is visible, the editor still holds the unsaved text, and the file still holds EXTERNAL_EDIT_3.
(J8 isolation) after the journey, room B's directory contains no workspace/ folder and its room.db has no claim:artifact node; starting a second server with `--section ../roomB` against room A exits non-zero before listening (SECTION_RE refusal), and a POST body carrying extra keys such as section or path is ignored (the server only ever files into its bound room and section).
Record each step's pass or fail line; exit 1 if any fails. Against the committed room mode J2 (placement), J3, J5 and J6 are expected to fail.
  </action>
  <verify>
    <automated>node tests/test-354-poc-room-journey.cjs; rc=$?; test $rc -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test covers J1-J8 by name and reads room.db through a fresh handle for J3 and J8
    - The test exits 77 (never 0) when Playwright is unavailable
    - Against the pre-journey server the test exits 1 naming J2 and J5 at least
  </acceptance_criteria>
  <done>The handoff's end-to-end journey is an executable test on a temporary room, red where the POC does not yet propagate.</done>
</task>

<task type="auto">
  <name>Task 2: Room-mode binding, governed filing and grounded answers in the POC server</name>
  <files>docs/reviews/localhost-poc/server.cjs</files>
  <read_first>
    - docs/reviews/localhost-poc/server.cjs (after 354-08)
    - lib/mcp/tools/views.cjs (_internal.fileArtifact signature and return shape)
    - lib/mcp/tool-router.cjs module.exports._test (SECTION_RE)
    - lib/core/navigation.cjs openRoomDbForCaller, closeRoomDbForCaller, getGraphExport
    - lib/core/room-path-containment.cjs (from 354-05)
    - tests/test-354-poc-room-journey.cjs
  </read_first>
  <action>
Room binding: when --room is given, resolve it with fs.realpathSync; refuse to start (stderr message naming the missing file, exit 2) unless `<room>/.mindrian/room.db` exists. Parse --section (default 'workspace') and refuse to start unless it matches SECTION_RE from require(lib/mcp/tool-router.cjs)._test. Add GET /api/room returning { mode: 'room' | 'fixture', name: basename of the room or 'fixture', section }.
Room-mode document: documentPath = path.join(ROOM, SECTION, 'workspace-poc.md'); GET /api/document on a missing file returns markdown '' with revision of the empty string (no write on read). POST /api/document (after 354-08's Host, Origin, token, size and base_revision checks) opens db = navigation.openRoomDbForCaller(ROOM), calls require(lib/mcp/tools/views.cjs)._internal.fileArtifact(db, ROOM, { section: SECTION, filename: 'workspace-poc.md', content: markdown }), closes the db in finally, maps result.ok false to 422 { error: result.reason }, and otherwise reads the file back and returns { ok: true, revision, markdown, artifact_id: result.artifact_id } (the bytes on disk, which may include an injected artifact_id). Fixture mode keeps 354-08's atomic write to data/workspace.md.
Grounded answers: graph() in room mode maps each exported node to { id, type, title, source } where source is the node's source path field from getGraphExport data when present (read the export to find the field; for claim:artifact nodes it is derived from 'artifact:<id>' plus the filed path) and edges as today. graphAnswer returns { mode: 'deterministic_graph_lookup', answer, nodes, edges, references: nodes.map(n => ({ id: n.id, source: n.source || null })) }; no model or network call.
Keep every 354-08 protection unchanged.
  </action>
  <verify>
    <automated>node --check docs/reviews/localhost-poc/server.cjs && node tests/test-354-poc-save-origin.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_internal.fileArtifact" docs/reviews/localhost-poc/server.cjs` matches and `grep -n "workspace-poc.md')" docs/reviews/localhost-poc/server.cjs` shows the path built under the section, not the room root
    - `grep -c "writeFileSync(documentPath" docs/reviews/localhost-poc/server.cjs` prints 0 for room mode (fixture mode uses the atomic writer from 354-08)
    - `grep -n "deterministic_graph_lookup" docs/reviews/localhost-poc/server.cjs` matches
    - tests/test-354-poc-save-origin.cjs still exits 0 (no regression of 354-08)
  </acceptance_criteria>
  <done>Room mode binds explicitly, files through the one governed writer, and answers from the graph with references.</done>
</task>

<task type="auto">
  <name>Task 3: Journey UI - room identity, references, external-edit polling, conflict banner, README</name>
  <files>docs/reviews/localhost-poc/app.js, docs/reviews/localhost-poc/styles.css, docs/reviews/localhost-poc/README.md</files>
  <read_first>
    - docs/reviews/localhost-poc/app.js and styles.css (after 354-08)
    - docs/reviews/localhost-poc/README.md
    - tests/test-354-poc-room-journey.cjs
  </read_first>
  <action>
app.js: fetch GET /api/room at load and render the room name and mode in the existing sidebar room block and header (replace the hard-coded 'Example venture' labels with the fetched values; fixture mode shows 'Fixture'). After a successful save, refetch GET /api/graph and re-render the graph tab and node counts (graph refresh). Rename the talk panel heading to 'Graph lookup (deterministic, no model)', keep its intro sentence stating answers come only from the local graph, and render d.references as a list of '<node id> - <source or "no source path">' items (esc() every value). Poll GET /api/document/revision every 2000 ms; when the revision differs from the loaded one and editor.value === loadedMarkdown, reload the document into the editor and preview and show a short 'Reloaded an external change' status; when the editor is dirty, show the conflict banner from 354-08 with the text 'This file changed on disk while you were editing. Your edits are still in the editor; save to see the conflict or reload to take the disk version.' and never overwrite the editor. Stop polling on page hide (visibilitychange) and resume on show.
styles.css: style the references list and the conflict banner with the existing De Stijl palette and card rules (yellow #e5b52f accent for the banner, black rules), no new fonts, keyboard-focusable controls keep a visible focus ring.
README.md: document room mode (`node docs/reviews/localhost-poc/server.cjs 3196 --room <room dir> [--section <slug>]`), where the working file is filed and that it is filed through the governed artifact path, the Host/Origin/token model from 354-08, and the journey test command `node tests/test-354-poc-room-journey.cjs`. State plainly what the POC does not do: no model conversation, no Claude session bridge, no multi-user locking beyond revision conflicts. Replace the example room path with the generic `<room dir>` form; never name a real person or a real room path.
  </action>
  <verify>
    <automated>node tests/test-354-poc-room-journey.cjs && node tests/test-354-poc-save-origin.cjs && ! grep -q $'\xe2\x80\x94' docs/reviews/localhost-poc/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-poc-room-journey.cjs` exits 0 (J1-J8)
    - `grep -n "/api/document/revision" docs/reviews/localhost-poc/app.js` matches and `grep -n "Graph lookup (deterministic, no model)" docs/reviews/localhost-poc/app.js` matches
    - `grep -c "Example venture" docs/reviews/localhost-poc/app.js` prints 0
    - README.md contains `--room` and `test-354-poc-room-journey.cjs` and no `/home/jsagi/MindrianRooms` path
    - tests/test-354-poc-save-origin.cjs still exits 0
  </acceptance_criteria>
  <done>A real browser completes the whole room journey on a temporary room, and every propagation step is visible to the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> POC server -> room | Browser edits become governed room writes |
| room A server -> other rooms | One server process is bound to one room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-22 | Information disclosure / Tampering | room-mode paths and section | mitigate | fs.realpathSync room binding, SECTION_RE on --section, fileArtifact with 354-05 realpath containment; journey J8 isolation leg |
| T-354-18 | Tampering (lost update) | external edit while dirty | mitigate | Revision polling never overwrites a dirty editor; 409 conflict on save; J7 |
| T-354-22b | Repudiation | graph answers | mitigate | Answers labelled deterministic lookup with node ids and source references; no model call |
| T-354-22c | Tampering | ungoverned writes | mitigate | Room-mode writes go only through fileArtifact (memory_event + claim node), never a raw write into the room root |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-poc-room-journey and test-354-poc-save-origin PASSED.
</verification>

<success_criteria>
The acceptance journey (bind, edit/save/reopen, governed index, inspect, grounded ask with references, external edit) passes in a real browser on a temporary room.
</success_criteria>

## Artifacts this phase produces

- `tests/test-354-poc-room-journey.cjs` (new Playwright journey; steps J1-J8)
- POC server: CLI flag `--section <slug>`, route `GET /api/room`, answer fields `mode: 'deterministic_graph_lookup'` and `references[]`, node field `source`
- POC app: references list, external-edit polling, 'Graph lookup (deterministic, no model)' panel

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-11-SUMMARY.md` when done (stage with `git add -f`).
</output>
