---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 12
type: execute
wave: 5
depends_on: ["354-09", "354-10"]
files_modified:
  - tests/test-354-theo-journey.cjs
  - tests/test-354-theo-live-contract.cjs
  - .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md
autonomous: true
requirements: [THEO-01, THEO-02, THEO-03]
tags: [theo, journey, live-contract, cross-repo, tier-3-4]

must_haves:
  truths:
    - "A hermetic synthetic journey drives generic methodology request -> guarded client -> provider response and provenance -> local application (validated act chain) -> governed room write -> later retrieval through the MCP resource, for healthy, unavailable, invalid-schema and thin-result providers"
    - "A canary string seeded in the room's files and room.db never appears in any captured outbound request in any of the four cases"
    - "Whenever the provider did not supply the recommendation (unavailable, invalid schema, thin result), the output is labelled with a local source and never with source 'brain' or ranked_candidates provenance"
    - "A live synthetic run against theo-mcp.onrender.com records origin, time, request shapes and sanitized responses for recommend_chain and taxonomy_ladder on all four rungs, or records ENV GAP and leaves live certification open (a mock-only pass never certifies the live integration)"
    - "THEO-02 is dispositioned BLOCKED on Phase 351 with plugin-side evidence (dispatch and stamp gate tests) and a read-only inspection of Theo's workflows; no Theo-side change is made"
  artifacts:
    - path: "tests/test-354-theo-journey.cjs"
      provides: "Hermetic four-case teaching-to-action journey with canary egress check"
      min_lines: 150
    - path: "tests/test-354-theo-live-contract.cjs"
      provides: "Opt-in live synthetic contract run (MINDRIAN_354_LIVE=1), exit 77 otherwise"
      min_lines: 80
    - path: ".planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md"
      provides: "Live run record and THEO-02 cross-repository evidence"
      contains: "THEO-02"
  key_links:
    - from: "tests/test-354-theo-journey.cjs"
      to: "tests/helpers/brain-capture-server.cjs"
      via: "every outbound request captured and scanned for the room canary"
      pattern: "CANARY"
    - from: "tests/test-354-theo-live-contract.cjs"
      to: "https://theo-mcp.onrender.com"
      via: "real brain-client recommendChain and taxonomy-climb renderLadder with synthetic handles only"
      pattern: "theo-mcp.onrender.com"
---

<objective>
Prove the complete teaching-to-action loop and the live provider contract (handoff THEO-03, THEO-01 "Require a real Theo-sourced result and visible failure/degradation; local fallback is not proof of successful integration", Acceptance "A separate synthetic Theo journey proves provider contribution and local application, including meaningful failure behavior. A mock-only pass cannot certify the live integration"), and disposition THEO-02 without duplicating Phase 351 (CTX-THEO02).

Phase 351 state at planning time: registered, Goal "[To be planned]", 0 plans, owned by the Theo repository (jsagir/theo) via its own GSD workflow; its plugin-side acceptance number is `bash scripts/release.sh patch --dry-run 2>&1 | grep theo-stamp-gate` printing PASS. Theo at /home/jsagi/Theo (commit 4ae9843) has .github/workflows ci.yml, theo-liveness.yml and theo-seam-audit.yml and no theo-resync consumer. This plan therefore only verifies the plugin side and records the provider-side dependency as open (CTX-THEO-READONLY: no write, deploy, dispatch or workflow change in that repository).

This is verification after the tier-2 and tier-3 fixes (354-06, 354-09, 354-10). A failure found here is a new finding: record it in 354-THEO-EVIDENCE.md with a reproduction; do not patch production code in this plan (gap closure owns new fixes).

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/reviews/phase-354-disposition-ledger.md
@tests/helpers/brain-capture-server.cjs
@tests/helpers/fixture-room-354.cjs
@docs/THEO-NOTIFY-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hermetic four-case teaching-to-action journey with canary egress check</name>
  <files>tests/test-354-theo-journey.cjs</files>
  <read_first>
    - tests/helpers/brain-capture-server.cjs (setToolScript, captured, per-tool failure modes)
    - lib/mcp/brain-router.cjs (after 354-09: recommend, provenance, brain_router_note)
    - lib/mcp/tool-router.cjs act-chain branch (after 354-09) and registerRouterTools
    - lib/mcp/tools/views.cjs artifact_file handler and lib/mcp/resources.cjs room-section resource
    - lib/mcp/pipeline-state.cjs read()
    - tests/test-354-theo-router-contract.cjs (capture-server scripting to reuse)
  </read_first>
  <action>
Create tests/test-354-theo-journey.cjs (plain Node harness). Setup per case: makeScratchRoom; seed CANARY = 'CANARY_354_ROOM_BYTES_' + random hex into a room file (problem-definition/notes.md), into a claim node (via lib/core/navigation.cjs writeClaimNode through openRoomDbForCaller, the governed writer), and into STATE.md body text, while STATE.md also carries `definition_level: ill-defined` and `complexity: complex`; start the capture server per its ordering contract.
Journey per case: (1) request: call the tool-router 'act-chain' handler (captureToolServer + registerRouterTools, as persistence.cjs registers tools) for the room; (2) local application: read pipeline-state; (3) governed room write: call the artifact_file handler (register lib/mcp/tools/views.cjs through registerCoreTools or views.register into the same capture server; set the write-path flags the handler requires for the 'cli' surface) to file a recommendation artifact whose frontmatter records source, chain_type and provenance.method from the act-chain response; (4) later retrieval: read room://section/<section> through an SDK Client over InMemoryTransport with registerResources and assert the artifact content is returned.
Cases: (H healthy) capture server scripts brain_ask structured_rows, recommend_chain with a candidate whose commands include a registry slug; expect source 'brain', chain_type 'ranked_candidates', captured recommend_chain problem_type 'IllDefined', pipeline-state chain equal to the validated chain, retrieval shows provenance. (U unavailable) the server returns HTTP 503 for tools/call (or is stopped); expect a local source, no chain_type ranked_candidates, and a disclosure field (brain_refusal or brain_router_note) present. (S invalid schema) recommend_chain returns { chain: 'not-an-array' } and brain_ask returns an unrecognized object; expect local source and no brain provenance. (T thin result) rows [] and chain []; expect local source with brain_router_note set.
For all four cases: every captured request body is scanned and must not contain CANARY; when source is 'brain' at least one tools/call was actually captured and the chain elements appear in the scripted provider response; when no tools/call succeeded, source is never 'brain'.
Exit 1 on any failure; print one line per case with source, chain_type and the captured problem_type.
  </action>
  <verify>
    <automated>node tests/test-354-theo-journey.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-theo-journey.cjs` exits 0 with four case lines (H, U, S, T)
    - The test asserts the canary is absent from every captured request body (grep finds CANARY and a loop over the capture array)
    - The retrieval step reads through `Client.readResource` over InMemoryTransport, not by reading the file directly
    - If any case fails because of a production defect, the SUMMARY records it as a NEW FINDING with the failing line, and 354-THEO-EVIDENCE.md lists it; production files are not edited in this plan
  </acceptance_criteria>
  <done>The teaching-to-action loop is proven end to end on synthetic data for all four provider conditions, with no room byte on the wire and no fallback posing as Theo.</done>
</task>

<task type="auto">
  <name>Task 2: Opt-in live synthetic contract run against Theo</name>
  <files>tests/test-354-theo-live-contract.cjs, .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md</files>
  <read_first>
    - lib/core/brain-client.cjs (recommendChain, ensureAvailable, getBrainUrl, callTool)
    - lib/core/strategy/taxonomy-climb.cjs renderLadder (after 354-10)
    - tests/test-339-theo-ask-e2e-live.cjs (existing live-test gating pattern to mirror)
    - lib/core/doctor/class-m-brain-smoke.cjs (how the doctor records origin and build for a live Brain check)
  </read_first>
  <action>
Create tests/test-354-theo-live-contract.cjs. Unless process.env.MINDRIAN_354_LIVE === '1', print 'SKIP: live Theo contract not requested (set MINDRIAN_354_LIVE=1)' and exit 77. Otherwise require the real lib/core/brain-client.cjs with the default URL (no override), call ensureAvailable(); if unavailable print 'ENV GAP: Brain unavailable (no key or no network)' and exit 77. Then, with synthetic generic handles only (never room content): recommendChain(rung, 4) for UnDefined, IllDefined, WellDefined, Wicked; renderLadder(id) for the four Theo rung ids; ask('Which framework fits an ill-defined problem at the discovery stage?', { problem_type: 'IllDefined' }). Assert: each recommendChain returns an array chain (possibly empty) or an explicit provider error, never a thrown exception; each renderLadder returns a non-null provider result whose rendered ladder marks exactly the requested rung (read the response shape; fail with the raw sanitized shape if it cannot be found); ask returns a composed envelope with problem_type 'IllDefined' and rung_source 'structured'. Record for each call: origin (getBrainUrl), ISO timestamp, tool name, argument keys and enum values, duration_ms, and a sanitized response digest (top-level keys, array lengths, the marked rung, first framework name). Try to read a provider build or version through a read-only call the client already wraps (for example a contract or stats call); record 'not exposed' if none exists. Write the JSON records to stdout and exit 0 only when every assertion held.
Run it with MINDRIAN_354_LIVE=1 outside the tool sandbox (network required). Create .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md with a 'Live contract run' section: the exact command, exit status, duration, environment (node version, HEAD, origin), and the JSON records, or the ENV GAP line with the statement 'THEO-01 and THEO-03 live certification remains OPEN; hermetic tests do not certify the live integration.' Stage the evidence file with git add -f.
  </action>
  <verify>
    <automated>node tests/test-354-theo-live-contract.cjs; test $? -eq 77 && { MINDRIAN_354_LIVE=1 node tests/test-354-theo-live-contract.cjs; rc=$?; echo "live rc=$rc"; test $rc -eq 0 -o $rc -eq 77; } && grep -q "Live contract run" .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Without MINDRIAN_354_LIVE the test exits 77 (never 0)
    - With MINDRIAN_354_LIVE=1 the test exits 0 with eight rung records plus one ask record, or exits 77 with an ENV GAP line; any other exit is a NEW FINDING recorded in the evidence file
    - 354-THEO-EVIDENCE.md contains the section 'Live contract run' with the command, exit status, duration and origin, and, when live was not reached, the literal sentence that live certification remains OPEN
    - The test's request arguments contain only rung enums, the literal generic question above and numbers (grep the file: no fs.readFileSync of any room path)
  </acceptance_criteria>
  <done>The live integration is either certified by recorded provider evidence or explicitly left open; a mock pass is never counted as live.</done>
</task>

<task type="auto">
  <name>Task 3: THEO-02 plugin-side verification and read-only provider inspection</name>
  <files>.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md</files>
  <read_first>
    - docs/THEO-NOTIFY-CONTRACT.md
    - scripts/release-lib/theo-stamp-gate.sh and scripts/release.sh lines 1470-1500 (Step 5.6)
    - tests/run-all-349.sh and tests/test-343-theo-stamp-gate.cjs
    - .planning/ROADMAP.md Phase 351 card (lines around 883-892)
  </read_first>
  <action>
Run, recording command, exit status and duration for each: `bash tests/run-all-349.sh`; `node tests/test-343-theo-stamp-gate.cjs`; `bash scripts/release.sh patch --dry-run 2>&1 | grep -i theo-stamp` (dry run only; it sends nothing, per tests/test-349-dry-run-never-sends.cjs; if the dry run would mutate the tree, stop and report instead of running it). Read-only provider inspection: `git -C /home/jsagi/Theo log -1 --format='%h %ad'`, `ls /home/jsagi/Theo/.github/workflows/`, `grep -rl "theo-resync" /home/jsagi/Theo/.github /home/jsagi/Theo/scripts 2>/dev/null`; if the gh CLI is authenticated, also `gh api repos/jsagir/theo/actions/workflows --jq '.workflows[].path'` (read-only API call). Capture `git -C /home/jsagi/Theo status --short` before and after and assert they are identical.
Append a 'THEO-02 release registry synchronization' section to 354-THEO-EVIDENCE.md: what the plugin side proves (dispatch payload shape and dry-run behavior, stamp-gate result), what it does not prove (consumption, applied registry stamp, digest verification, retry and idempotency on the Theo side), the provider-side findings, and the disposition 'BLOCKED - external dependency: Phase 351 (owner jsagir/theo). Not marked complete while the provider consumer is absent.' Name the concrete Theo-side task for Theo's own GSD workflow: a repository_dispatch theo-resync workflow that re-emits the command layer for the received version, verifies registryHash, restamps mappedBy, and makes `theo-stamp-gate` print PASS on the plugin side.
  </action>
  <verify>
    <automated>grep -q "THEO-02 release registry synchronization" .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md && grep -q "BLOCKED" .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md && bash tests/run-all-349.sh</automated>
  </verify>
  <acceptance_criteria>
    - 354-THEO-EVIDENCE.md contains the THEO-02 section with the six recorded commands and their exit statuses
    - The section's disposition line contains 'BLOCKED' and 'Phase 351'
    - The before and after outputs of `git -C /home/jsagi/Theo status --short` are recorded and identical
    - `bash tests/run-all-349.sh` exits 0 (or its failure is recorded as a NEW FINDING with output)
  </acceptance_criteria>
  <done>THEO-02 has an evidence-backed BLOCKED disposition that coordinates with Phase 351 instead of duplicating it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LOCAL -> Theo (live) | Synthetic generic requests cross to the production provider |
| plugin repo -> /home/jsagi/Theo and jsagir/theo | Read-only inspection of another repository and its GitHub workflows |
| provider response -> local chain state and room write | Provider output becomes local state and a filed artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-23 | Spoofing | fallback labelled as Theo evidence | mitigate | Four-case journey asserts source 'brain' only when a tools/call was captured and the chain came from it |
| T-354-24 | Information disclosure | live and hermetic requests | mitigate | Canary scan over every captured request; live test uses closed enums and one literal generic question only |
| T-354-25 | Tampering | /home/jsagi/Theo, jsagir/theo | mitigate | Read-only commands only; status before/after compared; no dispatch, deploy or workflow edit |
| T-354-25b | Repudiation | THEO-02 closure | mitigate | Disposition stays BLOCKED with the named Phase 351 dependency; never marked complete from the plugin side |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-theo-journey PASSED and test-354-theo-live-contract SKIPPED or PASSED.
- 354-THEO-EVIDENCE.md holds the live record (or ENV GAP) and the THEO-02 disposition.
</verification>

<success_criteria>
The Theo integration is proven on the hermetic journey for all four provider conditions, live certification is recorded or explicitly open, and THEO-02 is tracked as an external dependency.
</success_criteria>

## Artifacts this phase produces

- `tests/test-354-theo-journey.cjs` (new hermetic journey; cases H, U, S, T)
- `tests/test-354-theo-live-contract.cjs` (new opt-in live test; env `MINDRIAN_354_LIVE=1`; exit 77 otherwise)
- `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md` (new evidence record; sections 'Live contract run', 'THEO-02 release registry synchronization')

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-12-SUMMARY.md` when done (stage with `git add -f`).
</output>
