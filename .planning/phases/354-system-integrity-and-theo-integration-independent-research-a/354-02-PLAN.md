---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 02
type: execute
wave: 2
depends_on: ["354-01"]
files_modified:
  - tests/test-354-gate-subject-promotion.cjs
  - lib/mcp/tools/gate.cjs
autonomous: true
requirements: [SYS-08]
tags: [gate, canon-part-9, trust-integrity, tier-1]

must_haves:
  truths:
    - "After meeting file-meeting writes a claim and gate_answer approves its card, that exact claim node id (the card subject) reads review_status confirmed in room.db after reopening"
    - "reject and defer verdicts leave the subject claim at review_status proposed"
    - "Evidence node ids carried on the card are never promoted by an approve"
    - "A subject that is not a proposed claim node (missing id, decision node, already confirmed) is not promoted and the response names why"
    - "Strategy cards (goalGate.isStrategyCard) and material_step cards keep their existing approve behavior byte-for-byte"
  artifacts:
    - path: "tests/test-354-gate-subject-promotion.cjs"
      provides: "Regression asserting the exact claim id the next layer consumes transitions proposed -> confirmed"
      min_lines: 80
    - path: "lib/mcp/tools/gate.cjs"
      provides: "_promoteCardSubject eligibility + confirmNode on live.card.subjectNodeId"
      contains: "_promoteCardSubject"
  key_links:
    - from: "lib/mcp/tools/gate.cjs"
      to: "navigation.confirmNode"
      via: "confirmNode(db, live.card.subjectNodeId, navigation.resolveByUser(roomDir), ...)"
      pattern: "confirmNode\\(\\s*db,\\s*subjectId"
    - from: "lib/mcp/tool-router.cjs meeting file-meeting card"
      to: "lib/mcp/tools/gate.cjs approve branch"
      via: "card.subject_node_id = writeResult.node_id (tool-router.cjs:1636)"
      pattern: "subject_node_id: writeResult.node_id"
---

<objective>
Repair the P1 seam where human gate approval confirms the newly minted decision node instead of the card's subject claim (CTX-GATE in 354-CONTEXT.md: "Gate approval must promote the card's actual subject claim, not the newly-minted decision node"). The two layers that disagree: the meeting tool's public contract (lib/mcp/tool-router.cjs:1563 and :1653, "approve ... promotes it to confirmed") versus gate.cjs:303-304, which passes writeResult.node_id (decision:gate:*) to confirmNode. After the fix both agree on the subject id carried at live.card.subjectNodeId.

Test-first (CTX-TESTFIRST): Task 1 writes the failing regression that asserts the exact claim id; Task 2 fixes gate.cjs. Task 1 must be committed red before Task 2 starts.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/reviews/phase-354-disposition-ledger.md
@docs/reviews/phase-354-probes/persistence.cjs
@lib/mcp/tools/gate.cjs
@lib/core/navigation/confirm-node.cjs
@tests/helpers/fixture-room-354.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing regression - approve promotes the exact subject claim id</name>
  <files>tests/test-354-gate-subject-promotion.cjs</files>
  <read_first>
    - docs/reviews/phase-354-probes/persistence.cjs (approval() is the seed: registerRouterTools + gate.register into a captured server, meeting file-meeting, gate_answer approve, then SELECT from nodes)
    - tests/test-276-meeting-gate-wiring.cjs lines 190-230 (the existing assertion at :214 only checks that some node is confirmed; do not edit that file)
    - lib/mcp/tool-router.cjs lines 1555-1665 (file-meeting writes the claim and sets card.subject_node_id)
    - lib/mcp/tools/gate.cjs lines 100-140 and 200-340
    - tests/helpers/fixture-room-354.cjs
  </read_first>
  <action>
Create tests/test-354-gate-subject-promotion.cjs (plain Node harness, local check() counter, exit 1 on any failure, hyphens only). Use makeScratchRoom and captureToolServer from tests/helpers/fixture-room-354.cjs; set MINDRIAN_MCP_FIRST=all and delete CLAUDE_ACTIVE_ROOM/CLAUDE_CODE_SESSION_ID exactly as persistence.cjs does; register lib/mcp/tool-router.cjs registerRouterTools(server, room, repoRoot, { full: '' }, 'cli') and lib/mcp/tools/gate.cjs register(server, { fallbackRoomDir: room, pluginRoot: repoRoot, surface: 'cli' }).
Cases, each in a fresh scratch room, each reading room.db through a NEW openRoomDb handle after the handler returns:
(A) approve: meeting { command: 'file-meeting', knowledge_type: 'fact', claim_text: 'Synthetic subject claim.' } -> parse gate_id and the claim node id (the response line "Claim node <id> was written") -> gate_answer { gate_id, chosen: ['approve'], verdict: 'approve' } -> assert the row WHERE id = <that claim id> has review_status 'confirmed' (exact id, not "some node"); assert the response JSON reasoning_node.subject_node_id equals the claim id and reasoning_node.subject_confirmed === true; assert the decision:gate:<gate_id> node still exists.
(B) reject and (C) defer: same setup, the exact claim id stays 'proposed' and subject_confirmed is false.
(D) evidence never promoted: write two claims via file-meeting (first becomes evidence), then call gate_render with subject_node_id = second claim id and evidence_node_ids = [first claim id], kind 'general', options approve/reject; approve it; assert second claim 'confirmed' and first claim still 'proposed'.
(E) ineligible subject: gate_render with subject_node_id = 'claim:does-not-exist' then approve; assert ok response, subject_confirmed false, and a subject_skip_reason string equal to 'subject_not_found'.
Run it against the unfixed code and record in the task commit message that cases A and D fail (the probe shows the claim stays proposed). Wire nothing else.
  </action>
  <verify>
    <automated>node tests/test-354-gate-subject-promotion.cjs; test $? -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-354-gate-subject-promotion.cjs exists and asserts on `review_status` of the exact claim id parsed from the meeting response (grep finds `WHERE id = ?` or equivalent prepared SELECT keyed by that id)
    - Against the pre-fix gate.cjs, `node tests/test-354-gate-subject-promotion.cjs` exits 1 and its output names case A as failing with the claim still `proposed`
    - The test contains the strings `subject_confirmed` and `subject_skip_reason`
    - tests/test-276-meeting-gate-wiring.cjs is not modified (`git diff --quiet tests/test-276-meeting-gate-wiring.cjs`)
  </acceptance_criteria>
  <done>A red regression pins the contract the next layer consumes: the subject claim id itself is confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the card subject in gate_answer's approve branch</name>
  <files>lib/mcp/tools/gate.cjs</files>
  <read_first>
    - lib/mcp/tools/gate.cjs (whole file; the approve branch at 240-340 and the gate_render schema at 103-140)
    - lib/core/navigation/confirm-node.cjs (confirmNode resolves current review_status and delegates to promoteNodeStatus)
    - lib/core/navigation/goal-anchor.cjs header (strategy card subjects are goal anchors, owned by goalGate.ratifyGoalProposal)
    - lib/mcp/gate-render.cjs lines 60-100 and 160-190 (normalizeCard guarantees subjectNodeId and evidenceNodeIds)
    - tests/test-354-gate-subject-promotion.cjs
  </read_first>
  <action>
Add a module-private function _promoteCardSubject(db, roomDir, live) to lib/mcp/tools/gate.cjs returning { subject_node_id, subject_confirmed, subject_skip_reason }. Eligibility, checked in this order, each failure returning subject_confirmed false with the named reason: live.card.subjectNodeId is a non-empty string else 'no_subject'; goalGate.isStrategyCard(live.card) is false else 'strategy_card_owned_by_goal_gate'; live.kind (or live.card.kind) is 'general' else 'kind_not_general' (material_step and binding approvals authorize an action, not a truth claim); a prepared SELECT id, type, review_status FROM nodes WHERE id = ? finds the row else 'subject_not_found'; row.type === 'claim' else 'subject_not_claim'; row.review_status === 'proposed' else 'subject_not_proposed'. When eligible call navigation.confirmNode(db, subjectId, navigation.resolveByUser(roomDir), 'gate_answer approve (card subject)') and set subject_confirmed from confirmResult.ok === true (promoteNodeStatus's own reason is surfaced as subject_skip_reason on failure). Never touch evidenceNodeIds.
Call it inside the existing `if (writeResult && writeResult.ok === true)` approve block, after the existing decision-node confirmNode call, inside its own try/catch so no subject-promotion fault can flip response.ok or clobber reasoningNode (same additive idiom as the 260903-i2x block). Merge the three fields into reasoningNode (reasoningNode = Object.assign({}, writeResult, { confirmed, subject_node_id, subject_confirmed, subject_skip_reason })). The decision-node confirm stays as is: it records the human decision itself. No raw INSERT/UPDATE: all writes go through navigation.confirmNode (Canon Part 9 single chokepoint, Phase 273).
Update the gate_render subject_node_id .describe() text, which currently says "Nothing consumes this yet", to state: on an approve verdict for a kind 'general' card whose subject is a proposed claim node, gate_answer promotes that claim to confirmed through navigation.confirmNode; evidence nodes are never promoted; Canon Part 8: never a Brain identifier or room content. Keep the evidence_node_ids description honest the same way (provenance only, SOURCED_FROM targets, never promoted). Then run `node scripts/build-connector-registry.cjs --check` and `node scripts/check-tool-honesty.cjs --check`; if the connector check reports drift caused only by these description edits, regenerate with `node scripts/build-connector-registry.cjs` and commit the regenerated data files in this task.
  </action>
  <verify>
    <automated>node tests/test-354-gate-subject-promotion.cjs && node tests/test-276-meeting-gate-wiring.cjs && node tests/test-345-gate-ratify.cjs && node tests/test-345-gate-anchor.cjs && node docs/reviews/phase-354-probes/persistence.cjs 2>/dev/null | grep gate-subject-confirmation | grep -v '"type":"claim","review_status":"proposed"' && node scripts/build-connector-registry.cjs --check && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-gate-subject-promotion.cjs` exits 0 (cases A-E pass)
    - `grep -n "_promoteCardSubject" lib/mcp/tools/gate.cjs` shows a definition and exactly one call site inside the approve branch
    - `grep -c "Nothing consumes this yet" lib/mcp/tools/gate.cjs` prints 0
    - The persistence probe's gate-subject-confirmation line now shows the claim row with `"review_status":"confirmed"`
    - `node tests/test-276-meeting-gate-wiring.cjs`, `node tests/test-345-gate-ratify.cjs` and `node tests/test-345-gate-anchor.cjs` exit 0 (strategy-card path unchanged)
    - `node scripts/check-substrate.cjs --diff` exits 0 (no new raw write sites)
  </acceptance_criteria>
  <done>Approving a filed meeting's card confirms that meeting's claim; nothing else is promoted; the strategy and material-step paths are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model -> gate_answer MCP tool | The model relays a human verdict; the ledger entry and the card decide what may be promoted |
| gate.cjs -> room.db | The only promotion door is navigation.confirmNode (Canon Part 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-01 | Tampering | gate.cjs approve branch | mitigate | Promotion target is live.card.subjectNodeId from the minted ledger entry (not caller input at answer time); exact-id regression test |
| T-354-02 | Elevation of privilege | subject eligibility | mitigate | _promoteCardSubject requires kind 'general', non-strategy card, row.type 'claim', review_status 'proposed'; evidence nodes never promoted; material_step/binding approvals never confirm a truth claim |
| T-354-03a | Repudiation | response shape | mitigate | Response carries subject_node_id, subject_confirmed and subject_skip_reason so a skipped promotion is visible, never silent |
| T-354-03b | Spoofing | gate_answer session | accept | Session binding and single-use consumption are unchanged (gate-ledger.cjs consumeGate); re-verified in 354-16 |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-gate-subject-promotion PASSED.
- Existing gate suites (276 meeting wiring, 345 gate ratify and anchor) still pass.
</verification>

<success_criteria>
The persistence probe's gate-subject-confirmation observation flips: the original claim is confirmed after approval, verified by an independent room.db read.
</success_criteria>

## Artifacts this phase produces

- `tests/test-354-gate-subject-promotion.cjs` (new regression)
- `_promoteCardSubject(db, roomDir, live)` (new module-private function in lib/mcp/tools/gate.cjs)
- New gate_answer response fields: `reasoning_node.subject_node_id`, `reasoning_node.subject_confirmed`, `reasoning_node.subject_skip_reason`
- New skip-reason literals: `no_subject`, `strategy_card_owned_by_goal_gate`, `kind_not_general`, `subject_not_found`, `subject_not_claim`, `subject_not_proposed`

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-02-SUMMARY.md` when done (stage with `git add -f`).
</output>
