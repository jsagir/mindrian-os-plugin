---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 15
type: execute
wave: 6
depends_on: ["354-11", "354-12"]
files_modified:
  - tests/test-354-extract-shallow-contract.cjs
  - lib/mcp/tools/dual-path.cjs
  - lib/core/shallow-doc-parser.cjs
  - agents/larry-extended.md
  - data/mcp-tool-connectors.json
  - data/connector-registry.json
autonomous: true
requirements: [SYS-05]
tags: [extract-shallow, tool-honesty, contract, canon-part-11]

must_haves:
  truths:
    - "Calling extract_shallow through the MCP handler on a bound temporary room writes nothing: after reopening room.db the node, edge and memory_event counts are unchanged, and the room directory listing is unchanged"
    - "The extract_shallow tool description and response state the honest contract: a pure parse that writes nothing, persisted false, persist via claim_write after a room is bound"
    - "agents/larry-extended.md no longer claims the parser writes 3-5 nodes; it names claim_write after room_bind as the governed persistence step"
    - "The connector declaration's hitl_why agrees with the new description (no description-versus-behavior mismatch left for check-tool-honesty to miss)"
  artifacts:
    - path: "lib/mcp/tools/dual-path.cjs"
      provides: "Honest extract_shallow description and response fields persisted/persist_via"
      contains: "persisted: false"
    - path: "tests/test-354-extract-shallow-contract.cjs"
      provides: "Handler + disk + database-after-reopen regression for the pure-parse contract"
      min_lines: 70
  key_links:
    - from: "lib/mcp/tools/dual-path.cjs extract_shallow"
      to: "lib/core/shallow-doc-parser.cjs extractShallow(text, sessionId)"
      via: "called with no db handle, so no setFocus and no memory_event write"
      pattern: "extractShallow\\(text, sessionId\\)"
---

<objective>
Settle SYS-05 (CTX-SYS05, Claude's Discretion in 354-CONTEXT.md) with decision D-354-SYS05: extract_shallow's public contract is honest parsing, not governed persistence.

Why parsing, stated with evidence: (1) both documented callers run it before a room exists or is bound: the hookless runtime loop (lib/mcp/runtime-instructions.cjs:31, "If message 1 is a pasted doc, detect_dual_path, then extract_shallow on 'upload'", in the same step that says "Never write a room artifact before binding") and ignite Door 2 (commands/ignite.md:140, skills/ignite/SKILL.md:136, before room birth); persisting at that moment would either need a room that does not exist or break the binding rule. (2) Governed persistence already exists: claim_write writes a typed claim at review_status proposed through writeClaimNode, the single node chokepoint; a second writer would duplicate it (Canon Part 7) and would write CV-derived claims without a gate. (3) The shipped connector declaration (hitl_shape none, layer loop) already describes a pure parse, and its own hitl_why records that the description's write claim is false because navigation.cjs never exported recordMemoryEvent and setFocus only fires with an opts.db the tool never passes. The seam fixed here: the description and agents/larry-extended.md:197-200 promise writes; the code performs none. After the fix every surface says the same thing, and the behavior is tested through the handler and the database after reopening (not response text alone).

Test-first (CTX-TESTFIRST): Task 1 red (the description and agent prose still promise writes), Task 2 green.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@lib/mcp/tools/dual-path.cjs
@lib/core/shallow-doc-parser.cjs
@agents/larry-extended.md
@lib/mcp/runtime-instructions.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing contract regression through the MCP handler, disk and database</name>
  <files>tests/test-354-extract-shallow-contract.cjs</files>
  <read_first>
    - lib/mcp/tools/dual-path.cjs (whole file)
    - lib/core/shallow-doc-parser.cjs lines 130-240 (extractShallow, setFocus guard at :191, the recordMemoryEvent comment at :138-140)
    - agents/larry-extended.md lines 190-205
    - lib/mcp/register-core-tools.cjs (registration path the test uses)
    - tests/helpers/fixture-room-354.cjs
  </read_first>
  <action>
Create tests/test-354-extract-shallow-contract.cjs. makeScratchRoom; snapshot counts from room.db through a fresh openRoomDb handle (SELECT COUNT(*) FROM nodes, FROM edges, and the memory_event count by the column or node type navigation uses for memory events; read navigation to find it) and a recursive directory listing of the room (paths plus sizes). Register the tools on a real McpServer with registerCoreTools(server, { fallbackRoomDir: room, surface: 'cli' }), connect an SDK Client over InMemoryTransport, and call extract_shallow with a synthetic CV paste ('Synthetic Person\nFounder of Synthetic Venture Labs\nBuilding tools for synthetic testing.') and sessionId 'synthetic-354-session'. Assert: the response parses as JSON with persisted === false and persist_via === 'claim_write'; after the call, a NEW openRoomDb handle shows identical node, edge and memory_event counts; the directory listing is identical. From client.listTools() assert the extract_shallow description does not contain 'Routes graph writes' and does contain 'writes nothing'. Read agents/larry-extended.md and assert it does not contain 'The parser writes 3-5 nodes' and does contain 'claim_write'.
Run against current code: the persisted field, description and agent-prose assertions must fail (the no-write assertions pass today, which is the point: behavior is already a parse, the promise is what lies).
  </action>
  <verify>
    <automated>node tests/test-354-extract-shallow-contract.cjs; test $? -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test calls extract_shallow through an SDK Client (not the parser function directly) and re-reads room.db through a new handle
    - The test asserts `persisted === false`, the description text, and the agent prose
    - Against current code the test exits 1 naming the description and prose assertions
  </acceptance_criteria>
  <done>A red regression pins the honest contract at the handler, the disk and the database.</done>
</task>

<task type="auto">
  <name>Task 2: Make every surface state the pure-parse contract</name>
  <files>lib/mcp/tools/dual-path.cjs, lib/core/shallow-doc-parser.cjs, agents/larry-extended.md, data/mcp-tool-connectors.json, data/connector-registry.json</files>
  <read_first>
    - lib/mcp/tools/dual-path.cjs (whole file, including the connectors export and hitl_why)
    - lib/core/shallow-doc-parser.cjs header and lines 130-240
    - agents/larry-extended.md lines 185-205
    - commands/ignite.md line 140 and skills/ignite/SKILL.md line 136 (already consistent with parsing; read to confirm, do not edit)
    - scripts/build-connector-registry.cjs usage (regenerates data/mcp-tool-connectors.json and data/connector-registry.json)
    - tests/test-354-extract-shallow-contract.cjs
  </read_first>
  <action>
lib/mcp/tools/dual-path.cjs: rewrite the extract_shallow description to: parses a CV, memo or pitch paste classified as upload-path into an in-memory { user, venture, claims } object (1 user, 1 venture, 1-3 claims); writes nothing to the room or its graph; to keep any of it, bind a room and file each claim with claim_write (review_status proposed) after the navigator confirms it; falls back to 0 parsed nodes on parse failure. Change the sessionId parameter .describe() to say it scopes the parsed ids only. In the handler return JSON.stringify(Object.assign({}, result, { persisted: false, persist_via: 'claim_write' })). Rewrite the connector hitl_why to state the tool is a pure parse by contract (decision D-354-SYS05), no longer describing a mismatch; keep hitl_shape 'none' and layer 'loop'. Update the file header note to cite D-354-SYS05.
lib/core/shallow-doc-parser.cjs: update only comments that describe the MCP extract_shallow path as writing (the lines around :138-140 and :229) to state that the MCP tool calls extractShallow without a db handle by design and persistence is claim_write's job; no behavior change.
agents/larry-extended.md: replace lines 197-200 so the upload path reads: call extract_shallow (a pure parse that writes nothing); reflect it back ('Got it - you are a [role] working on [venture]. What decision is stuck?'); once a room is bound and the navigator confirms a parsed claim, file it with claim_write (proposed) so it lands in the local graph through the governed writer. Keep D-17's rationale honest: the graph is populated at the first confirmed claim, not silently by the parser. Hyphens only.
Regenerate the connector registries with `node scripts/build-connector-registry.cjs`, then run `node scripts/build-connector-registry.cjs --check`, `node scripts/check-tool-honesty.cjs --check` and `node scripts/build-skill-mirrors.cjs --check` (agents may be mirrored; if the mirror check reports drift from this agent edit, regenerate with the same script and include the regenerated files in this task's commit).
  </action>
  <verify>
    <automated>node tests/test-354-extract-shallow-contract.cjs && node scripts/build-connector-registry.cjs --check && node scripts/check-tool-honesty.cjs --check && node scripts/build-skill-mirrors.cjs --check && node lib/core/dual-path-detector.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-extract-shallow-contract.cjs` exits 0
    - `grep -c "Routes graph writes" lib/mcp/tools/dual-path.cjs` prints 0 and `grep -c "persisted: false" lib/mcp/tools/dual-path.cjs` is at least 1
    - `grep -c "The parser writes 3-5 nodes" agents/larry-extended.md` prints 0 and `grep -c "claim_write" agents/larry-extended.md` is at least 1
    - `node scripts/build-connector-registry.cjs --check`, `node scripts/check-tool-honesty.cjs --check` and `node scripts/build-skill-mirrors.cjs --check` exit 0
    - `grep -c $'\xe2\x80\x94' agents/larry-extended.md lib/mcp/tools/dual-path.cjs` prints 0 for both files
  </acceptance_criteria>
  <done>extract_shallow promises exactly what it does, and the agent tells Larry where persistence actually happens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted CV text -> parser -> model | Personal data is parsed locally and reflected back |
| tool description -> model behavior | The model acts on what the description promises |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-29 | Repudiation | extract_shallow description versus behavior | mitigate | Honest description, persisted:false response field, database-after-reopen regression |
| T-354-29b | Information disclosure | CV-derived claims written without a gate | mitigate | No write path added; persistence only via claim_write at proposed after binding and navigator confirmation |
| T-354-29c | Information disclosure | CV text egress | accept | Unchanged: the parser is local and opens no Brain wire (Canon Part 8); egress is governed by 354-06 |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-extract-shallow-contract PASSED.
- Connector, tool-honesty and skill-mirror gates pass.
</verification>

<success_criteria>
Decision D-354-SYS05 is implemented and tested through the MCP handler and the reopened database.
</success_criteria>

## Artifacts this phase produces

- `tests/test-354-extract-shallow-contract.cjs` (new regression)
- extract_shallow response fields `persisted: false`, `persist_via: 'claim_write'`
- Decision label `D-354-SYS05` cited in lib/mcp/tools/dual-path.cjs
- Regenerated `data/mcp-tool-connectors.json` and `data/connector-registry.json`

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-15-SUMMARY.md` when done (stage with `git add -f`).
</output>
