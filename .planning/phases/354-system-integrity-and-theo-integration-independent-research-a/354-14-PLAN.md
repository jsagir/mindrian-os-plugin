---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 14
type: execute
wave: 6
depends_on: ["354-11", "354-12"]
files_modified:
  - tests/test-354-registration-diagnostics.cjs
  - lib/mcp/register-core-tools.cjs
  - lib/mcp/tools/status.cjs
autonomous: true
requirements: [SYS-04]
tags: [mcp, registration, health, diagnostics, canon-part-11]

must_haves:
  truths:
    - "When a tool module fails to load or to register, the healthy siblings still register (sibling isolation preserved)"
    - "Each failed registration writes one diagnostic line naming the module and phase to stderr, and nothing to stdout (MCP stdio protocol stays clean)"
    - "registerCoreTools returns a report { complete, registered, failed } and getRegistrationHealth() exposes the same report"
    - "status_read's capability_floor.tool_registration reports complete false and lists the failed module whenever registration was partial, so health cannot claim complete capability"
  artifacts:
    - path: "lib/mcp/register-core-tools.cjs"
      provides: "Registration report, stderr diagnostics, getRegistrationHealth"
      exports: ["registerCoreTools", "getRegistrationHealth"]
    - path: "lib/mcp/tools/status.cjs"
      provides: "capability_floor.tool_registration from getRegistrationHealth()"
      contains: "tool_registration"
    - path: "tests/test-354-registration-diagnostics.cjs"
      provides: "Fault-injection regression at the real registration seam"
      min_lines: 70
  key_links:
    - from: "lib/mcp/tools/status.cjs status_read handler"
      to: "lib/mcp/register-core-tools.cjs getRegistrationHealth"
      via: "lazy require at call time (avoids the load-order cycle)"
      pattern: "getRegistrationHealth\\(\\)"
---

<objective>
Make a partial MCP tool surface visible (CTX-REG in 354-CONTEXT.md; handoff SYS-04): lib/mcp/register-core-tools.cjs:39-75 swallows require and register failures with `continue`, returns nothing, and emits no diagnostic, so fault injection removed graph_query, graph_write and memory_event while the server kept starting silently. The seam: the registration loop's isolation (correct, keep it) versus the health surface, which cannot see what the loop dropped. After the fix the loop records what it dropped, says so on stderr, and status_read reports it.

Canon Part 11 (CIRS) note: no tool is added or removed and no connector declaration changes; status_read gains one output field, so the born-wired and connector registries are unaffected (verified by the --check commands).

Test-first (CTX-TESTFIRST): Task 1 red, Task 2 green.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/reviews/phase-354-probes/registration.cjs
@lib/mcp/register-core-tools.cjs
@lib/mcp/tools/status.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing fault-injection regression at the registration seam</name>
  <files>tests/test-354-registration-diagnostics.cjs</files>
  <read_first>
    - docs/reviews/phase-354-probes/registration.cjs (seed: Module._load fault for tools/graph.cjs, healthy vs degraded McpServer _registeredTools comparison)
    - lib/mcp/register-core-tools.cjs (whole file)
    - lib/mcp/tools/status.cjs lines 95-165
    - lib/mcp/tool-router.cjs lines 2195-2212 (the only caller)
  </read_first>
  <action>
Create tests/test-354-registration-diagnostics.cjs. Build a healthy McpServer and call registerCoreTools(healthy, { fallbackRoomDir: <scratch room> }). Then (F1 require fault) monkeypatch Module._load to throw Error('SYNTHETIC_REGISTRATION_FAILURE') for the absolute path of lib/mcp/tools/graph.cjs, clear that path from require.cache, capture process.stdout.write and process.stderr.write, call registerCoreTools on a fresh degraded McpServer, and restore everything in finally. Assert: the returned value is an object with complete === false, failed containing { module: 'graph.cjs', phase: 'require' } whose message contains SYNTHETIC_REGISTRATION_FAILURE, registered listing the other module files; stderr contains 'tool registration failed: graph.cjs (require)'; captured stdout is the empty string; the degraded tool count equals the healthy count minus the tools graph.cjs registers (graph_query, graph_write, memory_event missing, all others present). (F2 register fault) Module._load returns { register() { throw Error('SYNTHETIC_REGISTER_THROW') } } for graph.cjs; assert failed contains phase 'register'. (H1 health) after F1, require lib/mcp/register-core-tools.cjs getRegistrationHealth() and assert it deep-equals the F1 report; call the degraded server's status_read handler (degraded._registeredTools.status_read callback, or through an SDK Client over InMemoryTransport) and assert the parsed response segments.capability_floor.tool_registration.complete === false and its failed list names graph.cjs. (H2) after a clean registration, tool_registration.complete === true and failed is empty.
Run against current code: F1 must fail (returns undefined, no diagnostic).
  </action>
  <verify>
    <automated>node tests/test-354-registration-diagnostics.cjs; test $? -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test captures process.stdout.write during registration and asserts it is empty
    - The test asserts on status_read output, not only on the registerCoreTools return value
    - Against the current code the test exits 1 naming F1
  </acceptance_criteria>
  <done>A red regression proves the surface can shrink silently and health cannot tell.</done>
</task>

<task type="auto">
  <name>Task 2: Registration report, stderr diagnostics and status_read health exposure</name>
  <files>lib/mcp/register-core-tools.cjs, lib/mcp/tools/status.cjs</files>
  <read_first>
    - lib/mcp/register-core-tools.cjs (whole file)
    - lib/mcp/tools/status.cjs (whole file; buildStatusSegments is a pure function and stays pure)
    - bin/mindrian-mcp-server.cjs (confirm the server never prints to stdout outside the protocol)
    - tests/test-354-registration-diagnostics.cjs
  </read_first>
  <action>
lib/mcp/register-core-tools.cjs: keep the sorted discovery and per-module try/catch isolation. Build report = { complete: true, registered: [], failed: [], at: new Date().toISOString() }. On a contract_version failure push { module: 'contract-version.cjs', phase: 'contract_version', message }; on readdir failure push { module: 'tools/', phase: 'discover', message }; on a require failure push { module: f, phase: 'require', message }; on a register() throw push { module: f, phase: 'register', message }; a module without a register function pushes { module: f, phase: 'no_register_export', message: 'module exports no register()' }. message is String(err && err.message || err).slice(0, 200). For every failure write exactly one line to process.stderr: '[mindrian-os] tool registration failed: <module> (<phase>): <message>'. complete = failed.length === 0. Store the report in a module-level _lastReport and return it. Export getRegistrationHealth() returning a structured clone of _lastReport, or { complete: null, registered: [], failed: [], reason: 'not_run' } before any run. Never write to stdout.
lib/mcp/tools/status.cjs: in the status_read handler, lazily require('../register-core-tools.cjs') (inside the handler, after load, to avoid the load-order cycle) and add capabilityFloor.tool_registration = { complete, failed: failed.map(f => ({ module: f.module, phase: f.phase })) } from getRegistrationHealth(); buildStatusSegments stays pure (it already receives capabilityFloor). Add one clause to the status_read description stating that capability_floor reports incomplete tool registration; run `node scripts/build-connector-registry.cjs --check` and regenerate only if this description edit causes drift.
  </action>
  <verify>
    <automated>node tests/test-354-registration-diagnostics.cjs && node docs/reviews/phase-354-probes/registration.cjs 2>/dev/null | grep -q '"returnedFailure":true' && node scripts/build-connector-registry.cjs --check && node scripts/build-orchestration-projection.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-registration-diagnostics.cjs` exits 0 (F1, F2, H1, H2)
    - The registration probe prints `"returnedFailure":true` (its failureDiagnosticPresent stays false only because the probe itself swallows stderr; record that in the SUMMARY)
    - `grep -c "process.stdout" lib/mcp/register-core-tools.cjs` prints 0
    - `grep -n "getRegistrationHealth" lib/mcp/tools/status.cjs` matches inside the handler
    - `node scripts/build-connector-registry.cjs --check` and `node scripts/build-orchestration-projection.cjs --check` exit 0
  </acceptance_criteria>
  <done>A partial tool surface is announced on stderr and reported by status_read, while healthy tools keep working.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server stdout -> host client | stdout carries the JSON-RPC stream; any stray byte corrupts the protocol |
| tool modules -> registration seam | A broken module must not take down its siblings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-27 | Repudiation | silent partial tool surface | mitigate | Report, stderr diagnostic per failure, status_read tool_registration.complete false; regression F1/H1 |
| T-354-28 | Tampering | MCP stdout protocol | mitigate | Diagnostics use stderr only; test asserts empty stdout during registration |
| T-354-28b | Information disclosure | error messages in health output | mitigate | status_read exposes module and phase only; messages capped at 200 chars and only on stderr |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-registration-diagnostics PASSED.
</verification>

<success_criteria>
The registration probe's failure is now visible in the return value, on stderr and through status_read.
</success_criteria>

## Artifacts this phase produces

- `getRegistrationHealth()` (new export of lib/mcp/register-core-tools.cjs); registerCoreTools now returns `{ complete, registered, failed, at }`
- Failure phases `require`, `register`, `contract_version`, `discover`, `no_register_export`
- status_read field `segments.capability_floor.tool_registration`
- `tests/test-354-registration-diagnostics.cjs` (new regression)

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-14-SUMMARY.md` when done (stage with `git add -f`).
</output>
