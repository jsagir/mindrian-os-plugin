---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 10
type: execute
wave: 4
depends_on: ["354-05", "354-06", "354-07", "354-08"]
files_modified:
  - tests/test-354-taxonomy-ladder-casing.cjs
  - lib/core/strategy/rung-vocabulary.cjs
  - lib/core/strategy/taxonomy-climb.cjs
  - lib/core/part8-egress-guard.cjs
  - tests/test-345-rung-mapping.cjs
  - tests/test-345-climb.cjs
  - tests/test-260906-fda-known-tool-shapes.cjs
autonomous: true
requirements: [THEO-01]
tags: [theo, taxonomy-ladder, vocabulary, contract, tier-3]

must_haves:
  truths:
    - "renderLadder sends taxonomy_ladder a rung value that Theo's schema accepts (one of UnDefined, IllDefined, WellDefined, Wicked) for each of the four Theo rung ids"
    - "The in-process Part 8 belt allows { rung: '<Theo id>' } and { rung, question_label } for taxonomy_ladder as known_tool_shape, so the call is not silently degraded by the guard"
    - "The plugin's ladder vocabulary is proven equal to Theo's PROBLEM_TYPE_IDS by reading /home/jsagi/Theo/src/mcp/content/vocabulary.ts read-only (or a recorded ENV GAP when the checkout is absent)"
    - "recommendChain's problem-type alias tables and normalization are byte-unchanged (scope stays on the taxonomy_ladder path)"
  artifacts:
    - path: "lib/core/strategy/rung-vocabulary.cjs"
      provides: "LADDER_RUNG_BY_THEO_ID mapping to Theo's accepted enum"
      contains: "LADDER_RUNG_BY_THEO_ID"
    - path: "lib/core/part8-egress-guard.cjs"
      provides: "TAXONOMY_RUNGS equal to Theo's PROBLEM_TYPE_IDS"
      contains: "TAXONOMY_RUNGS"
    - path: "tests/test-354-taxonomy-ladder-casing.cjs"
      provides: "Provider-compatibility regression over all four rungs through the real client call path"
      min_lines: 70
  key_links:
    - from: "lib/core/strategy/taxonomy-climb.cjs renderLadder"
      to: "brainClient.callTool('taxonomy_ladder', args)"
      via: "args.rung = rungVocabulary.toLadderRung(theoRungId)"
      pattern: "toLadderRung\\("
    - from: "lib/core/strategy/rung-vocabulary.cjs"
      to: "/home/jsagi/Theo/src/mcp/content/vocabulary.ts PROBLEM_TYPE_IDS"
      via: "read-only contract test"
      pattern: "PROBLEM_TYPE_IDS"
---

<objective>
Fix the P2 taxonomy-ladder casing mismatch (CTX-TAXO in 354-CONTEXT.md; handoff THEO-01): lib/core/strategy/rung-vocabulary.cjs:102 maps Theo rung ids to lowercase/hyphenated strings and lib/core/strategy/taxonomy-climb.cjs:145 sends them, while Theo's taxonomy_ladder input schema is z.enum([...PROBLEM_TYPE_IDS]) with PROBLEM_TYPE_IDS = ['UnDefined', 'IllDefined', 'WellDefined', 'Wicked'] (/home/jsagi/Theo/src/mcp/content/vocabulary.ts:53-58, taxonomy-ladder.ts:285-286, inspected read-only at planning time, Theo commit 4ae9843). The two layers that disagree: the plugin's guard vocabulary (lowercase, also pinned in part8-egress-guard.cjs TAXONOMY_RUNGS) versus the provider's actual enum. Existing local tests validated the plugin's own vocabulary, not provider compatibility, which is how this shipped green.

Scope lock (CTX-CORRECTED, CTX-TAXO): only the taxonomy_ladder path. recommendChain's origin-specific normalization in lib/core/brain-client.cjs is already correct and is not touched (brain-client.cjs is not in this plan's files). Rung ids are Phase 345 artifacts; this fixes them in place (no parallel vocabulary). No write to /home/jsagi/Theo.

Test-first (CTX-TESTFIRST): Task 1 red, Task 2 green.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@lib/core/strategy/rung-vocabulary.cjs
@lib/core/strategy/taxonomy-climb.cjs
@lib/core/part8-egress-guard.cjs
@tests/test-345-rung-mapping.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing provider-compatibility regression for the taxonomy ladder</name>
  <files>tests/test-354-taxonomy-ladder-casing.cjs</files>
  <read_first>
    - lib/core/strategy/rung-vocabulary.cjs (whole file)
    - lib/core/strategy/taxonomy-climb.cjs lines 120-175 (renderLadder)
    - lib/core/part8-egress-guard.cjs lines 320-470 (TAXONOMY_RUNGS, _proveKnownToolShape taxonomy_ladder arm)
    - /home/jsagi/Theo/src/mcp/content/vocabulary.ts lines 50-60 and /home/jsagi/Theo/src/mcp/content/taxonomy-ladder.ts lines 275-300 (READ ONLY; never edit, stage or run anything in that repository)
    - tests/helpers/brain-capture-server.cjs
  </read_first>
  <action>
Create tests/test-354-taxonomy-ladder-casing.cjs (plain Node harness). Resolve the provider vocabulary: read `${MINDRIAN_THEO_CHECKOUT || '/home/jsagi/Theo'}/src/mcp/content/vocabulary.ts` with fs.readFileSync, extract the PROBLEM_TYPE_IDS array literal with a regex over the quoted strings between `export const PROBLEM_TYPE_IDS = [` and `] as const`, and record the Theo commit via `git -C <checkout> log -1 --format=%h` (execFileSync, read-only). If the checkout is absent, print `ENV GAP: Theo checkout not found; provider leg skipped` and fall back to the pinned literal ['UnDefined', 'IllDefined', 'WellDefined', 'Wicked'] for the remaining legs (the provider leg itself reports SKIP, never PASS).
(T1) For each of rungVocabulary.THEO_RUNGS, toLadderRung(id) is a member of the provider set.
(T2) Through the real client path: start the capture server per its ordering contract, call taxonomy-climb renderLadder(id, <a valid jtbd slug or undefined>) for all four ids, and assert each captured taxonomy_ladder request's arguments.rung is in the provider set and no call was dropped (four captured tools/call for taxonomy_ladder).
(T3) Egress: part8-egress-guard classify({ rung: 'IllDefined' }, { toolName: 'taxonomy_ladder' }) returns verdict 'allow' class 'known_tool_shape', and classify({ rung: 'ill-defined' }, ...) does not return allow.
(T4) Scope pin: require lib/core/brain-client.cjs and assert its exposed Theo alias table (via _test or the exported selector, whichever exists; read the file to find it) still maps 'ill-defined' -> 'IllDefined', 'undefined' -> 'UnDefined', 'well-defined' -> 'WellDefined' (recommendChain path unchanged).
Run against unfixed code: T1, T2 and T3 must fail.
  </action>
  <verify>
    <automated>node tests/test-354-taxonomy-ladder-casing.cjs; test $? -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test reads vocabulary.ts with fs.readFileSync only (`grep -cE "writeFileSync|git (add|commit|push)" tests/test-354-taxonomy-ladder-casing.cjs` prints 0)
    - The test prints the Theo commit hash it compared against
    - Against the pre-fix code the test exits 1 naming T1 (for example 'ill-defined' not in provider set) and T2
  </acceptance_criteria>
  <done>A red regression compares the plugin ladder against the provider's real enum, not the plugin's own vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Map ladder rungs to Theo's enum and align the guard's known-shape vocabulary</name>
  <files>lib/core/strategy/rung-vocabulary.cjs, lib/core/strategy/taxonomy-climb.cjs, lib/core/part8-egress-guard.cjs, tests/test-345-rung-mapping.cjs, tests/test-345-climb.cjs, tests/test-260906-fda-known-tool-shapes.cjs</files>
  <read_first>
    - lib/core/strategy/rung-vocabulary.cjs (whole file, including its header on PERSISTED_VOCABULARY and Pitfall 4)
    - lib/core/strategy/taxonomy-climb.cjs (whole file; localLadderLine and renderLadder)
    - lib/core/part8-egress-guard.cjs TAXONOMY_RUNGS and the taxonomy_ladder arm of _proveKnownToolShape (after 354-06)
    - tests/test-345-rung-mapping.cjs, tests/test-345-climb.cjs, tests/test-260906-fda-known-tool-shapes.cjs (find assertions pinning lowercase ladder values)
    - tests/test-354-taxonomy-ladder-casing.cjs
  </read_first>
  <action>
In lib/core/strategy/rung-vocabulary.cjs set LADDER_RUNG_BY_THEO_ID to the identity mapping Wicked -> 'Wicked', UnDefined -> 'UnDefined', WellDefined -> 'WellDefined', IllDefined -> 'IllDefined', and rewrite its comment: the wire vocabulary for taxonomy_ladder is Theo's PROBLEM_TYPE_IDS (cite /home/jsagi/Theo/src/mcp/content/vocabulary.ts:53-58 and taxonomy-ladder.ts:285-286 and the commit inspected); toLadderRung keeps its fail-closed contract (an unmapped value returns null). PERSISTED_VOCABULARY and THEO_RUNGS are unchanged.
In lib/core/part8-egress-guard.cjs set TAXONOMY_RUNGS = Object.freeze(new Set(['UnDefined', 'IllDefined', 'WellDefined', 'Wicked'])) and update its comment to name the provider source; the taxonomy_ladder arm of _proveKnownToolShape is otherwise unchanged.
In lib/core/strategy/taxonomy-climb.cjs change only comments or human-readable strings that describe the ladder values as lowercase; renderLadder logic is unchanged (it already sends toLadderRung's value). If localLadderLine renders lowercase ladder labels for humans, leave the human labels as they are.
Update only the existing assertions in tests/test-345-rung-mapping.cjs, tests/test-345-climb.cjs and tests/test-260906-fda-known-tool-shapes.cjs that pin the lowercase taxonomy_ladder wire values or TAXONOMY_RUNGS membership, to Theo's ids; keep the bidirectional equality assertion between LADDER_RUNG_BY_THEO_ID values and TAXONOMY_RUNGS. Do not touch assertions about recommendChain, workflow stages or trigger tiers (tests/test-339-*, test-254-*, test-129-*, test-244-*). List each changed assertion (file:line, old, new) in the SUMMARY.
  </action>
  <verify>
    <automated>node tests/test-354-taxonomy-ladder-casing.cjs && node tests/test-345-rung-mapping.cjs && node tests/test-345-climb.cjs && node tests/test-345-part8.cjs && node tests/test-260906-fda-known-tool-shapes.cjs && node tests/test-339-theo-ask-compose.cjs && node tests/test-254-one-chain-source.cjs && node tests/test-354-egress-typed-question.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-taxonomy-ladder-casing.cjs` exits 0 and prints the compared Theo commit
    - `node -e "const r=require('./lib/core/strategy/rung-vocabulary.cjs');console.log(r.THEO_RUNGS.map(r.toLadderRung).join(','))"` prints `Wicked,UnDefined,WellDefined,IllDefined`
    - tests/test-345-rung-mapping.cjs, test-345-climb.cjs, test-345-part8.cjs, test-260906-fda-known-tool-shapes.cjs, test-339-theo-ask-compose.cjs and test-354-egress-typed-question.cjs exit 0
    - No commit made by this plan touches lib/core/brain-client.cjs (`git show --stat` of each 354-10 commit lists no brain-client.cjs; 354-09 owns that file in this wave), so the recommendChain path is untouched here and T4 pins its alias table
    - `git -C /home/jsagi/Theo status --short` shows the same output before and after this plan (no writes to Theo)
  </acceptance_criteria>
  <done>All four ladder rungs reach Theo in the casing its schema accepts, proven against the provider's source rather than the plugin's own list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin -> Theo taxonomy_ladder | Closed enum crossing to the remote provider |
| plugin repo -> /home/jsagi/Theo | Read-only comparison of a separate repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-21 | Repudiation / Spoofing | renderLadder -> local fallback | mitigate | Provider-enum contract test over all four rungs through the real call path, so a rejected rung cannot hide behind localLadderLine |
| T-354-21b | Information disclosure | taxonomy_ladder args | accept | Only the closed rung enum and an optional closed jtbd slug cross (Part 8 unchanged); the guard arm keeps its exact-key check |
| T-354-25 | Tampering | /home/jsagi/Theo | mitigate | Test reads vocabulary.ts only; acceptance compares `git -C /home/jsagi/Theo status --short` before and after |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-taxonomy-ladder-casing PASSED.
- Phase 345 strategy suites stay green with only the documented casing assertions updated.
</verification>

<success_criteria>
The ladder path is provider-compatible for all four rungs, and the recommendChain path is provably untouched.
</success_criteria>

## Artifacts this phase produces

- `tests/test-354-taxonomy-ladder-casing.cjs` (new provider-compatibility regression; env override `MINDRIAN_THEO_CHECKOUT`)
- LADDER_RUNG_BY_THEO_ID identity mapping and TAXONOMY_RUNGS set of Theo ids (changed values, same symbol names)

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-10-SUMMARY.md` when done (stage with `git add -f`).
</output>
