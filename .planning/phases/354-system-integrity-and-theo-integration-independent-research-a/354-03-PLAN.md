---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 03
type: execute
wave: 2
depends_on: ["354-01"]
files_modified:
  - tests/test-354-chain-resume-identity.cjs
  - lib/core/chain-executor.cjs
autonomous: true
requirements: [SYS-09]
tags: [chain-executor, resume, journal, trust-integrity, tier-1]

must_haves:
  truths:
    - "Resuming research -> validate -> research with only step 1 journaled dispatches steps 2 AND 3, and the durable journal ends at chain_position 2 with suggested_next null"
    - "The first resumed step receives the journaled predecessor output reference (output_path 'a.md'), never null"
    - "A journal whose chain disagrees with the executing step list halts with reason resume_journal_mismatch instead of guessing which steps ran"
    - "When the executor reports completed:true with journaling on, the durable journal agrees (chain_position === steps.length - 1); a disagreement flips completed to false with reason journal_disagreement"
  artifacts:
    - path: "tests/test-354-chain-resume-identity.cjs"
      provides: "Regression on repeated-command resume, predecessor restore, mismatch halt, completion agreement"
      min_lines: 80
    - path: "lib/core/chain-executor.cjs"
      provides: "_computeResumePlan (positional step identity + agreement check) used once before the _runChainResilient loop"
      contains: "_computeResumePlan"
  key_links:
    - from: "lib/core/chain-executor.cjs _runChainResilient"
      to: "lib/mcp/pipeline-state.cjs read()"
      via: "one journal read before the loop, not journal.chain.indexOf(step.command) per iteration"
      pattern: "_computeResumePlan\\("
---

<objective>
Repair the P1 chain-resume seam (CTX-RESUME in 354-CONTEXT.md): lib/core/chain-executor.cjs:1367-1377 identifies a completed step by `journal.chain.indexOf(step.command)`, so a repeated command collapses into one identity, and :1331 dispatches the first resumed step with previousOutput = null although the journal holds output_path. The two layers that disagree: the executor's notion of "already done" (a command name) versus the journal's (a position, lib/mcp/pipeline-state.cjs chain_position, the SOLE chain-state truth per D-166-02). After the fix the executor uses positional step identity validated against the journal's chain, restores the predecessor reference, and checks its own completion claim against the journal.

Test-first (CTX-TESTFIRST): Task 1 red, Task 2 green. pipeline-state.cjs is not modified (it stays the sole truth; no second store).

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/reviews/phase-354-probes/persistence.cjs
@lib/core/chain-executor.cjs
@lib/mcp/pipeline-state.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing regression - repeated-command resume and predecessor restore</name>
  <files>tests/test-354-chain-resume-identity.cjs</files>
  <read_first>
    - docs/reviews/phase-354-probes/persistence.cjs resume() (the seed: initChain research/validate/research, recordStep research a.md, runChain with journal:true resume:true)
    - lib/core/chain-executor.cjs lines 1293-1400 and 1600-1640 (resume skip, recordStep journaling, return shape)
    - lib/mcp/pipeline-state.cjs lines 100-205 (read, initChain, recordStep semantics)
    - tests/helpers/fixture-room-354.cjs
  </read_first>
  <action>
Create tests/test-354-chain-resume-identity.cjs (plain Node harness, exit 1 on failure). Each case uses a fresh makeScratchRoom room and lib/mcp/pipeline-state.cjs plus lib/core/chain-executor.cjs runChain with an onStep that records (step.step, previous) and returns { chain_output: { value: step.step }, quality: 'high' }, gateFn: () => 'run'.
(A) repeated command: initChain(room, ['research','validate','research'], 'manual'); write a.md; recordStep(room,'research','a.md'); runChain([{step:1,command:'research'},{step:2,command:'validate'},{step:3,command:'research'}], { roomDir: room, journal: true, resume: true, gateFn, onStep }). Assert the dispatched step numbers deep-equal [2, 3], result.completed === true, and after the run ps.read(room).chain_position === 2 and suggested_next === null.
(B) predecessor restore: same setup; assert the previous argument onStep received for step 2 is an object with output_path === 'a.md', restored_from === 'journal' and step_index === 0 (not null).
(C) mismatch: initChain(room, ['research','validate'], 'manual') and recordStep research, then resume a list of ['diagnose','validate']; assert no step dispatched, completed false, haltedAt.reason === 'resume_journal_mismatch'.
(D) completion agreement: initChain(room, ['research','validate'], 'manual') first so the journal chain agrees with the list, then journal:true, resume:false, onStep succeeds, but monkeypatch pipeline-state recordStep for this case only to a no-op (restore it after); assert completed === false and haltedAt.reason === 'journal_disagreement'.
(E) floor: a fresh chain with no journal file and resume:true dispatches every step once and completes (unchanged behavior).
Run against unfixed code; A and B must fail (probe shows calls [2], firstResumedInput null).
  </action>
  <verify>
    <automated>node tests/test-354-chain-resume-identity.cjs; test $? -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts `deepStrictEqual(calls, [2, 3])` (or equivalent) for case A and `output_path === 'a.md'` for case B
    - The test contains the strings `resume_journal_mismatch` and `journal_disagreement`
    - Against the pre-fix executor the test exits 1 and names cases A and B as failing
  </acceptance_criteria>
  <done>A red regression pins step identity and predecessor restore at the value onStep actually consumes.</done>
</task>

<task type="auto">
  <name>Task 2: Positional step identity, predecessor restore and completion agreement in _runChainResilient</name>
  <files>lib/core/chain-executor.cjs</files>
  <read_first>
    - lib/core/chain-executor.cjs lines 1293-1640 (_runChainResilient in full) and lines 898-960 (runChain dispatcher)
    - lib/mcp/pipeline-state.cjs (read, recordStep; history entries carry tool and output_path)
    - tests/test-347-resume-nonlinear.cjs header (chain.cjs gate resume is a different path; do not touch lib/mcp/tools/chain.cjs)
    - tests/test-354-chain-resume-identity.cjs
  </read_first>
  <action>
Add a module-private _computeResumePlan(list, ps, roomDir) to lib/core/chain-executor.cjs. It reads the journal once (ps.read(roomDir), fault-tolerant). No journal or no journal.chain array -> { skipThroughIndex: -1, predecessor: null, mismatch: null }. Otherwise the agreement check: the journal's chain must equal list.map(s => s && s.command) element by element with the same length; any difference -> { mismatch: 'resume_journal_mismatch', detail: { journal_chain_length, list_length, first_divergent_index } } (no user content in detail, command slugs only). On agreement: cursor = journal.chain_position (number, else -1); skipThroughIndex = cursor; predecessor = cursor >= 0 ? { restored_from: 'journal', output_path, step_index: cursor, command: journal.chain[cursor] } where output_path is journal.output_path when journal.last_tool === journal.chain[cursor], else the output_path of the last journal.history entry whose tool equals journal.chain[cursor], else null.
In _runChainResilient: when doResume, call _computeResumePlan once before the for-loop. A mismatch returns immediately with trace [], completed false, partial false, haltedAt { step: list[0] || null, reason: 'resume_journal_mismatch', detail }, plus run_id and record_write_failures 0 (close the chain-state db handle first). Otherwise seed previousOutput = plan.predecessor (so the first dispatched step's fan-in input is the restored reference) and keep a local resumePassActive = true. Replace the per-iteration `journal.chain.indexOf(step.command)` block with: if (resumePassActive && i <= plan.skipThroughIndex) { i += 1; continue; }. Set resumePassActive = false immediately before the first real dispatch so a later on_fail back-edge to an earlier index is never silently skipped. Step identity is the list index i, valid because the agreement check proved the list and the journal describe the same chain.
Completion agreement: after the loop, when doJournal and completed === true, re-read the journal; if journal.chain_position !== list.length - 1 set completed = false and haltedAt = { step: null, reason: 'journal_disagreement', journal_position, expected_position: list.length - 1 }. Apply this only when the journal chain agrees with list (same length and commands); a caller that journals a list that was never initChain'd keeps today's behavior.
Leave the synchronous runChain path, lib/mcp/tools/chain.cjs and lib/mcp/pipeline-state.cjs untouched.
  </action>
  <verify>
    <automated>node tests/test-354-chain-resume-identity.cjs && node tests/test-347-resume-nonlinear.cjs && node tests/test-chain-graceful-partial.cjs && node tests/test-chain-executor-loop.cjs && node tests/test-chain-executor-verdict.cjs && node tests/test-chain-executor-gate.cjs && node tests/test-pipeline-state-isnext-gate.cjs && bash tests/run-all-166.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-chain-resume-identity.cjs` exits 0 (cases A-E pass)
    - `grep -c "journal.chain.indexOf(step.command)" lib/core/chain-executor.cjs` prints 0
    - `grep -n "_computeResumePlan" lib/core/chain-executor.cjs` shows one definition and one call inside _runChainResilient
    - The persistence probe line `"probe":"repeated-command-resume"` now prints `"calls":[2,3]` and `"chainPosition":2`, and `"probe":"resume-predecessor-input"` prints a firstResumedInput object containing `"output_path":"a.md"`
    - tests/test-347-resume-nonlinear.cjs, tests/test-chain-graceful-partial.cjs and bash tests/run-all-166.sh exit 0
    - `git diff --quiet lib/mcp/pipeline-state.cjs lib/mcp/tools/chain.cjs` succeeds (neither file touched)
  </acceptance_criteria>
  <done>Resume re-enters at the right step, hands it the real predecessor reference, refuses a mismatched journal, and never reports a completion the journal contradicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room/.mindrian/pipeline-state.json -> executor | The journal is a room file any local process can edit; the executor must not trust it blindly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-03 | Tampering / Repudiation | _runChainResilient resume skip | mitigate | Positional identity plus the journal_disagreement completion check: a skipped step can no longer be reported complete |
| T-354-04 | Tampering | journal from a different chain | mitigate | _computeResumePlan agreement check halts with resume_journal_mismatch; no guessing |
| T-354-04b | Information disclosure | predecessor reference | mitigate | previousOutput carries output_path (a room-relative reference already in the journal), never file bodies (Part 8: bodies never leave onStep) |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-chain-resume-identity PASSED.
- Chain executor suites (166 runner, 347 nonlinear, graceful partial) stay green.
</verification>

<success_criteria>
The repeated-command resume probe dispatches steps 2 and 3, and the journal and the executor agree on completion.
</success_criteria>

## Artifacts this phase produces

- `tests/test-354-chain-resume-identity.cjs` (new regression)
- `_computeResumePlan(list, ps, roomDir)` (new module-private function in lib/core/chain-executor.cjs)
- New haltedAt reasons: `resume_journal_mismatch`, `journal_disagreement`
- New previousOutput shape on resume: `{ restored_from: 'journal', output_path, step_index, command }`

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-03-SUMMARY.md` when done (stage with `git add -f`).
</output>
