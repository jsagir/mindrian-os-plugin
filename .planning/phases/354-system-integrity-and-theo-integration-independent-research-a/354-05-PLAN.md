---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 05
type: execute
wave: 3
depends_on: ["354-02", "354-03", "354-04"]
files_modified:
  - tests/test-354-room-symlink-containment.cjs
  - lib/core/room-path-containment.cjs
  - lib/mcp/tool-router.cjs
  - lib/mcp/tools/views.cjs
  - lib/mcp/resources.cjs
  - lib/core/reasoning-ops.cjs
autonomous: true
requirements: [SYS-01]
tags: [path-containment, symlink, boundary-integrity, tier-2]

must_haves:
  truths:
    - "artifact_file into a section that is a symlink pointing outside the room returns ok:false reason path_escape and writes no byte outside the room"
    - "An existing file inside the room that is a symlink to an outside file is replaced atomically inside the room, never written through"
    - "room://section/{sectionName} read through the real MCP SDK client never returns bytes from a symlink target outside the room, while a normal discovered section still returns its content"
    - "reasoning://section/{name} and every reasoning-ops read and write path refuse a section whose realpath leaves the room"
    - "Percent-encoded traversal and sibling-prefix names (room-evil next to room) stay refused through the SDK URI matcher"
  artifacts:
    - path: "lib/core/room-path-containment.cjs"
      provides: "One realpath-containment helper for every room read/write site"
      exports: ["realRoomRoot", "isRealpathContained", "assertRealpathContained", "writeFileContained"]
    - path: "tests/test-354-room-symlink-containment.cjs"
      provides: "Regression through fileArtifact, the SDK resource client, and reasoning-ops"
      min_lines: 100
  key_links:
    - from: "lib/mcp/tool-router.cjs safeResolveSection"
      to: "lib/core/room-path-containment.cjs assertRealpathContained"
      via: "realpath check after the lexical check, inherited by views.cjs, room.cjs searchRoom and tool-router.cjs:1257"
      pattern: "assertRealpathContained\\("
    - from: "lib/mcp/tools/views.cjs fileArtifact"
      to: "writeFileContained"
      via: "atomic contained write replacing fs.writeFileSync(filePath)"
      pattern: "writeFileContained\\("
---

<objective>
Close the P1 room-boundary seam (CTX-SYMLINK in 354-CONTEXT.md; handoff SYS-01): lib/mcp/tool-router.cjs:145-151 checks containment lexically, then lib/mcp/tools/views.cjs:206-207 writes through the resolved filesystem path, so an existing room/research symlink sends artifact_file's write outside the room; the same gap lets room://section/{sectionName} read outside, and lib/mcp/resources.cjs:260-263 passes an unchecked section name to reasoning-ops. The two layers that disagree: the lexical string check versus the realpath the write actually lands on.

Symlink policy (decided here, per CTX-SYMLINK "define a symlink policy"): a directory symlink inside a room is followed only when its realpath stays inside the room's realpath; any section, destination or existing parent whose realpath leaves the room is refused; the final file write never follows a symlink leaf (atomic temp-file plus rename inside the contained parent replaces the directory entry instead of writing through it). One helper enforces this at every site (Don't Hand-Roll: no per-caller symlink checker).

Bounded, not re-opened (CTX-CORRECTED): percent-encoded traversal did not escape through the SDK matcher; the test keeps it as a regression floor only.

Test-first (CTX-TESTFIRST): Task 1 red; Tasks 2 and 3 green.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/reviews/phase-354-probes/resources.cjs
@docs/reviews/phase-354-probes/persistence.cjs
@lib/mcp/tool-router.cjs
@lib/mcp/tools/views.cjs
@lib/mcp/resources.cjs
@lib/core/reasoning-ops.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing regression - symlink escape through the write tool, the SDK resource path and reasoning-ops</name>
  <files>tests/test-354-room-symlink-containment.cjs</files>
  <read_first>
    - docs/reviews/phase-354-probes/resources.cjs (seed: McpServer + Client over InMemoryTransport, registerResources, readResource for four URIs)
    - docs/reviews/phase-354-probes/persistence.cjs artifact() (seed: room/research symlink to an outside dir, views._internal.fileArtifact)
    - lib/mcp/tool-router.cjs lines 130-152 (SECTION_RE, safeResolveSection)
    - lib/mcp/tools/views.cjs lines 110-240 and 383-420
    - lib/mcp/resources.cjs lines 100-170 and 245-270
    - lib/core/reasoning-ops.cjs lines 390-625
    - tests/helpers/fixture-room-354.cjs
  </read_first>
  <action>
Create tests/test-354-room-symlink-containment.cjs (plain Node harness, exit 1 on failure). Fixture per run: makeScratchRoom('containment') plus sibling dirs `<root>/outside` (note.md = OUTSIDE_SYNTHETIC, REASONING.md = OUTSIDE_REASONING_SYNTHETIC, victim.md = VICTIM_ORIGINAL) and `<root>/<roomName>-evil` (a sibling-prefix directory); inside the room: normal/note.md = INSIDE_SYNTHETIC, a symlink `research` -> outside, a symlink `.reasoning/linked` -> outside, a real section `notes/` containing a file symlink `notes/victim.md` -> outside/victim.md, and an inside-pointing directory symlink `alias` -> `<room>/normal`.
Write-side cases through lib/mcp/tools/views.cjs _internal.fileArtifact(db, room, ...): (W1) section 'research', filename 'probe.md' -> expect ok false, reason 'path_escape', and outside/probe.md absent; (W2) section 'notes', filename 'victim.md', content NEW_INSIDE -> expect outside/victim.md still VICTIM_ORIGINAL and room/notes/victim.md now a regular file containing NEW_INSIDE; (W3) section 'alias' (inside-pointing) -> ok true and the file lands under room/normal.
Read-side cases through a real SDK Client over InMemoryTransport with registerResources(server, { fallbackRoomDir: room }): (R1) room://section/normal contains INSIDE_SYNTHETIC; (R2) room://section/research contains neither OUTSIDE_SYNTHETIC nor OUTSIDE_REASONING_SYNTHETIC; (R3) reasoning://section/linked does not contain OUTSIDE_REASONING_SYNTHETIC; (R4) room://section/..%2Foutside and room://section/..%2F<roomName>-evil return no outside bytes (floor); (R5) a file symlink inside a real section pointing outside (notes/victim.md before W2 runs, so order R5 before W2) is not included in room://section/notes output.
reasoning-ops cases: (O1) getReasoning(room, 'linked') returns an error object and not the outside content; (O2) setReasoningFrontmatter(room, 'linked', 'x', 'y') does not modify outside/REASONING.md (compare bytes before and after).
Run against unfixed code: W1, R2, R3, O1, O2 must fail.
  </action>
  <verify>
    <automated>node tests/test-354-room-symlink-containment.cjs; test $? -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test drives reads through `@modelcontextprotocol/sdk` Client.readResource over InMemoryTransport (grep finds `InMemoryTransport.createLinkedPair`)
    - The test contains `path_escape`, `OUTSIDE_SYNTHETIC`, `VICTIM_ORIGINAL` and a sibling-prefix `-evil` directory case
    - Against the pre-fix code the test exits 1 naming W1, R2, R3, O1 and O2
  </acceptance_criteria>
  <done>A red regression proves both write and read boundaries fail against an existing symlink, while encoded traversal is pinned as a passing floor.</done>
</task>

<task type="auto">
  <name>Task 2: One realpath-containment helper wired into safeResolveSection and fileArtifact</name>
  <files>lib/core/room-path-containment.cjs, lib/mcp/tool-router.cjs, lib/mcp/tools/views.cjs</files>
  <read_first>
    - lib/mcp/tool-router.cjs lines 110-152 and 2235-2250 (the _test export surface that views.cjs and room.cjs import)
    - lib/mcp/tools/views.cjs lines 1-60, 110-240
    - lib/mcp/tools/room.cjs lines 20-30 and 340-347 (searchRoom inherits safeResolveSection)
    - lib/core/recovery/case-file.cjs lines 83-110 (existing lexical assertContained and atomicWrite idioms to mirror, with citation)
    - tests/test-354-room-symlink-containment.cjs
  </read_first>
  <action>
Create lib/core/room-path-containment.cjs (CJS, no dependencies, hyphens only) exporting:
realRoomRoot(roomDir): fs.realpathSync(path.resolve(roomDir)).
isRealpathContained(roomDir, absPath): resolve the realpath of absPath's deepest existing ancestor (walk up with fs.existsSync / lstat until one exists), rejoin the non-existing remainder, and return true when the result equals realRoomRoot(roomDir) or starts with it plus path.sep.
assertRealpathContained(roomDir, absPath, label): throw an Error with code 'ROOM_PATH_ESCAPE' and message `room path escapes room via symlink: <label>` when not contained; return absPath.
writeFileContained(roomDir, filePath, content): assert the parent directory is contained; if filePath exists and fs.lstatSync(filePath).isSymbolicLink() the write still proceeds by replacement, never through; write content to `<parent>/.<basename>.<pid>.<random hex>.tmp` opened with flag 'wx', then fs.renameSync(tmp, filePath) (rename replaces the directory entry, so a symlink leaf is replaced, not followed); unlink the temp on any error; re-assert the final filePath's parent realpath after the rename.
In lib/mcp/tool-router.cjs safeResolveSection: keep the lexical check, then call assertRealpathContained(base, resolved, 'section ' + section) and rethrow its error unchanged; add an import at the top of the file. Every existing caller (views.cjs fileArtifact, room.cjs searchRoom, tool-router.cjs:1257) inherits the check.
In lib/mcp/tools/views.cjs fileArtifact: wrap safeResolveSection in try/catch and return { ok: false, reason: 'path_escape', section } on code ROOM_PATH_ESCAPE (and 'section_traversal' for the lexical error) before any byte, memory_event or node is written; after fs.mkdirSync(scopeDir, { recursive: true }) call assertRealpathContained(roomDir, scopeDir, 'section ' + p.section) again (mkdir can traverse a symlink created in between); replace fs.writeFileSync(filePath, ...) with writeFileContained(roomDir, filePath, content). Update the artifact_file tool description sentence that names the guard so it states the realpath containment and the symlink policy in one sentence; run `node scripts/build-connector-registry.cjs --check` and regenerate only if these description edits cause drift.
  </action>
  <verify>
    <automated>node -e "const c=require('./lib/core/room-path-containment.cjs');for(const k of ['realRoomRoot','isRealpathContained','assertRealpathContained','writeFileContained'])if(typeof c[k]!=='function')process.exit(1)" && node lib/memory/mcp-input-validation.test.cjs && node scripts/build-connector-registry.cjs --check && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/room-path-containment.cjs exports realRoomRoot, isRealpathContained, assertRealpathContained, writeFileContained
    - `grep -n "assertRealpathContained" lib/mcp/tool-router.cjs` matches inside safeResolveSection
    - `grep -c "fs.writeFileSync(filePath" lib/mcp/tools/views.cjs` prints 0 and `grep -n "writeFileContained(" lib/mcp/tools/views.cjs` matches
    - `node lib/memory/mcp-input-validation.test.cjs` exits 0 (existing traversal tests unchanged)
    - Test cases W1, W2, W3 of tests/test-354-room-symlink-containment.cjs pass (run the test; the read-side cases may still fail until Task 3)
  </acceptance_criteria>
  <done>The write boundary is enforced by realpath at the moment of the write, through one helper every section resolver inherits.</done>
</task>

<task type="auto">
  <name>Task 3: Contain the resource read paths and every reasoning-ops read/write</name>
  <files>lib/mcp/resources.cjs, lib/core/reasoning-ops.cjs</files>
  <read_first>
    - lib/mcp/resources.cjs lines 100-170 (room-section handler: path.join(roomDir, sectionName), readdir, safeReadFile) and 245-270 (reasoning-section handler)
    - lib/core/reasoning-ops.cjs lines 390-625 (read sites :432, :483, :568; write sites :416, :549, :597, :622)
    - lib/core/room-path-containment.cjs (from Task 2)
    - tests/test-354-room-symlink-containment.cjs
  </read_first>
  <action>
In lib/mcp/resources.cjs room-section handler: compute sectionDir = path.resolve(roomDir, sectionName) and refuse (return the text `Section "<name>" is outside this Data Room and was not read.`) when it is lexically outside roomDir or isRealpathContained(roomDir, sectionDir) is false. Do not apply SECTION_RE here (discovered section names may legitimately fall outside [a-z0-9-]). For each listed .md file, skip any file whose realpath is not contained (a file symlink to outside is omitted, never read). The reasoning-section handler passes through reasoning-ops, which is contained below.
In lib/core/reasoning-ops.cjs add one module-private _containedReasoningPath(roomDir, section, fileName) that builds path.join(root, '.reasoning', section, fileName), rejects lexical escape and realpath escape (isRealpathContained against the room), and returns null when refused. Route every read site (getReasoning, the listReasoning per-section read, getReasoningFrontmatter) and every write site (the generate write at :416, setReasoningFrontmatter :597, the sibling writer at :622) through it; a refused path returns the module's existing error shape ({ error: 'Section outside room: <section>' }) and writes nothing. The runs directory write (:549) builds its path from a generated run id, not a caller section; assert its parent with assertRealpathContained as well. Keep all function signatures and return shapes unchanged for valid sections.
  </action>
  <verify>
    <automated>node tests/test-354-room-symlink-containment.cjs && node docs/reviews/phase-354-probes/resources.cjs 2>/dev/null | grep 'room://section/linked' | grep -q '"outside":false' && node scripts/check-substrate.cjs --diff && node scripts/check-render-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-room-symlink-containment.cjs` exits 0 (all W, R and O cases)
    - The resources probe prints `"uri":"room://section/linked"` with `"outside":false` and `"outsideReasoning":false`, and room://section/normal still prints `"inside":true`
    - `grep -c "_containedReasoningPath" lib/core/reasoning-ops.cjs` is at least 7 (definition plus six call sites)
    - The persistence probe's artifact-symlink-containment line prints `"writtenOutsideRoom":false`
  </acceptance_criteria>
  <done>No room read or write path, tool or resource, crosses a symlink out of the room; valid discovered sections still read normally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room directory -> host filesystem | A room folder may contain symlinks created by sync tools, users or other processes |
| MCP resource URI -> filesystem read | Section names arrive from the model through the SDK URI template matcher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-09 | Tampering / Information disclosure | fileArtifact, room-section, reasoning-ops | mitigate | Realpath containment via one helper at every read and write site; regression covers write, SDK read, reasoning read and reasoning write |
| T-354-10 | Tampering (TOCTOU) | check-then-write window | mitigate | Re-assert after mkdir, write to a 'wx' temp in the contained parent then rename (no leaf follow); residual race requires a concurrent local attacker with write access to the room and is accepted |
| T-354-10b | Denial of service | legitimate inside-pointing symlinks | mitigate | Policy follows directory symlinks whose realpath stays inside the room (case W3) |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-room-symlink-containment PASSED.
- Both probes (resources, persistence artifact) no longer show outside bytes or outside writes.
</verification>

<success_criteria>
Room boundaries hold against a pre-existing symlink on every read and write path this repo exposes through MCP.
</success_criteria>

## Artifacts this phase produces

- `lib/core/room-path-containment.cjs` (new module; exports `realRoomRoot`, `isRealpathContained`, `assertRealpathContained`, `writeFileContained`)
- `_containedReasoningPath(roomDir, section, fileName)` (new module-private in lib/core/reasoning-ops.cjs)
- Error code `ROOM_PATH_ESCAPE`; fileArtifact return reasons `path_escape`, `section_traversal`
- `tests/test-354-room-symlink-containment.cjs` (new regression)

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-05-SUMMARY.md` when done (stage with `git add -f`).
</output>
