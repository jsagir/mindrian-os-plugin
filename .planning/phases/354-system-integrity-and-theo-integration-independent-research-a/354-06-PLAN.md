---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 06
type: execute
wave: 3
depends_on: ["354-02", "354-03", "354-04"]
files_modified:
  - tests/test-354-egress-typed-question.cjs
  - lib/core/part8-egress-guard.cjs
  - scripts/part8-egress-guard-hook.cjs
  - lib/core/brain-client.cjs
  - tests/test-223-part8-egress.cjs
  - tests/test-245-egress-contentless.cjs
  - tests/test-339-theo-ask-compose.cjs
  - tests/part8-egress-e2e-smoke.test.cjs
  - tests/part8-egress-guard-hook.test.cjs
  - tests/test-260906-fda-known-tool-shapes.cjs
autonomous: true
requirements: [THEO-03]
tags: [canon-part-8, egress, brain-boundary, tier-2]

must_haves:
  truths:
    - "brain.ask('Use the SWOT framework on our confidential plan: cedar will acquire juniper next autumn.') opens no Brain wire: the capture server records zero tools/call requests and the call returns { error: 'egress_blocked', egress_class: 'freeform_unproven' }"
    - "The same sentence sent as a model-issued brain_ask through the mindrian-brain shim renders the honest egress_blocked refusal and never reaches the Brain"
    - "classify() allows a free-form brain_ask/brain_search string only when every token is proven to come from the closed methodology vocabulary (class typed_question); a methodology word next to unproven prose is ambiguous freeform_unproven, never allow"
    - "Every plugin-internal templated Brain question (brain-router, rs-chain-feeder, rs-explain) still passes the proof and is forwarded"
    - "callTool's in-process belt and its Phase 254 D-02 disclose-and-proceed policy for the other wrappers are unchanged"
  artifacts:
    - path: "lib/core/part8-egress-guard.cjs"
      provides: "_proveTypedQuestion closed-vocabulary proof; step 3 allow only via proof"
      contains: "_proveTypedQuestion"
    - path: "lib/core/brain-client.cjs"
      provides: "_typedFreeformGate enforced in ask(), search() and smartSearch() before any wire call"
      contains: "_typedFreeformGate"
    - path: "tests/test-354-egress-typed-question.cjs"
      provides: "Transport-captured regression plus shim end-to-end plus corpus gates"
      min_lines: 120
  key_links:
    - from: "bin/mindrian-brain-mcp-client.cjs brain_ask handler"
      to: "lib/core/brain-client.cjs ask()"
      via: "brainClient.ask(question) returns the egress_blocked sentinel the Phase 257 branch renders"
      pattern: "egress_blocked"
    - from: "lib/core/brain-client.cjs _typedFreeformGate"
      to: "lib/core/part8-egress-guard.cjs classify"
      via: "classify({question},{toolName:'brain_ask'}).class === 'typed_question'"
      pattern: "typed_question"
---

<objective>
Close the P1 free-form Brain egress gap (CTX-EGRESS in 354-CONTEXT.md; handoff THEO-03 "Verify local user bytes never enter outbound requests"): lib/core/part8-egress-guard.cjs:521-531 allows any string that clears the denylist and contains one methodology token, and the real brain.ask() path forwarded a private sentence that mentioned SWOT. The two layers that disagree: the guard's claim "generic" (a keyword was present) versus what the string actually carries (arbitrary prose). After the fix, "generic" means structurally proven: every token of a free-form question comes from a closed methodology vocabulary, and the two natural-language channels (brain_ask, brain_search) refuse anything else before any socket opens.

Decision D-354-EGR (planner, implementing the locked CTX-EGRESS "Use typed fields and canonical methodology identifiers at the egress boundary... do not forward free-form user prose through a 'generic' channel"), stated with its impact per the handoff: enforcement lives in brain-client.cjs ask(), search() and smartSearch(), the single functions both the in-process callers and the model-issued shim (bin/mindrian-brain-mcp-client.cjs) call, so one change covers CLI, Desktop and Cowork (Tri-Polar). Impact: a model-issued brain_ask or brain_search containing any token outside the closed vocabulary (a venture name, a date, "our", "my") is refused with the existing honest egress_blocked envelope (Phase 257 branch) instead of being forwarded with a disclosure. callTool's belt and Phase 254 D-02 Option A (ambiguous discloses and proceeds for the other wrappers) are not changed. brain_query (admin-gated Cypher with its own raw-field guard at brain-client.cjs:601) stays on its current path and is recorded as a bounded residual in the ledger. The navigator may veto D-354-EGR before this plan executes (354-01 ledger section 9).

Test-first (CTX-TESTFIRST): Task 1 red; Tasks 2 and 3 green. No model or vendor call is used in the guard (the spike skill rules the egress guard is not a Jev seat).

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/reviews/phase-354-disposition-ledger.md
@docs/reviews/phase-354-probes/theo.cjs
@lib/core/part8-egress-guard.cjs
@scripts/part8-egress-guard-hook.cjs
@tests/helpers/brain-capture-server.cjs
@bin/mindrian-brain-mcp-client.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing regression - private prose with a methodology word never reaches the Brain wire</name>
  <files>tests/test-354-egress-typed-question.cjs</files>
  <read_first>
    - docs/reviews/phase-354-probes/theo.cjs (seed: false-safe-egress probe; global.fetch capture)
    - tests/helpers/brain-capture-server.cjs (ordering contract: set MINDRIAN_BRAIN_URL/KEY and clear the brain-client require cache before requiring it)
    - lib/core/part8-egress-guard.cjs lines 36-130 and 480-545
    - lib/core/brain-client.cjs lines 600-700 (callTool belt), 1040-1100 (search, smartSearch), 1395-1420 (ask)
    - bin/mindrian-brain-mcp-client.cjs lines 160-280 (brain_ask egress_blocked branch, brain_search handler)
    - lib/mcp/brain-router.cjs lines 312-320, lib/core/rs-chain-feeder.cjs lines 147-157, scripts/rs-explain-command.cjs lines 150-160 (the internal question templates)
    - data/framework-names.json (canonical framework phrases)
  </read_first>
  <action>
Create tests/test-354-egress-typed-question.cjs (plain Node harness, exit 1 on failure). Start the shared capture server (startCaptureServer) and point MINDRIAN_BRAIN_URL and MINDRIAN_BRAIN_KEY (synthetic value) at it per its ordering contract.
(A) transport capture: brain.ask(PRIVATE) where PRIVATE = 'Use the SWOT framework on our confidential plan: cedar will acquire juniper next autumn.' -> assert zero captured requests whose body contains 'cedar' or 'juniper', and the return value has error === 'egress_blocked' and egress_class === 'freeform_unproven'.
(B) search channels: brain.search(PRIVATE) and brain.smartSearch(PRIVATE) -> same zero-capture assertion and egress_blocked return.
(C) classify: classify({ question: PRIVATE }, { toolName: 'brain_ask' }) must not return verdict 'allow'; its class must be 'freeform_unproven'.
(D) private corpus: at least 10 synthetic sentences that each carry one methodology word next to unproven prose (for example a venture-sounding name, a date, a money-free plan description, "our", "my"); every one must be refused by ask() with zero captured bytes of the sentence.
(E) generic corpus: at least 20 questions built only from canonical vocabulary, including every internal template instantiated with canonical inputs: brain-router's 'recommend a framework for a <definition> definition <complexity> problem' for definitions undefined/ill-defined/well-defined and complexities simple/complex/wicked; rs-chain-feeder _buildUpstreamQuestion(problem_type, stage) for problem types undefined/ill-defined/well-defined/wicked and stages discovery/investment/scoping/execution (call the exported builder if exported, else reproduce its exact template string and cite the line); rs-explain's 'what frameworks chain from <name> via FEEDS_INTO?' and 'what methodology chain follows <slug>?' for three names taken from data/framework-names.json and three slugs from data/command-registry.json. Every one must classify as allow with class 'typed_question' and ask() must forward it (captured request present).
(F) shim end to end: spawn `node bin/mindrian-brain-mcp-client.cjs` through the SDK StdioClientTransport with the capture-server env, call tool brain_ask with PRIVATE, assert the returned text contains 'egress_blocked' and the capture server recorded no tools/call for brain_ask carrying 'cedar'.
Run against unfixed code: A, B, C, D and F must fail.
  </action>
  <verify>
    <automated>node tests/test-354-egress-typed-question.cjs; test $? -eq 1 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - The test uses tests/helpers/brain-capture-server.cjs (grep finds `startCaptureServer`) and asserts on captured request bodies, not only on classify() output
    - The test contains at least 10 private sentences and at least 20 generic questions (count the two arrays in the file)
    - The test spawns bin/mindrian-brain-mcp-client.cjs through StdioClientTransport (case F)
    - Against the pre-fix code the test exits 1 naming cases A and F
  </acceptance_criteria>
  <done>A red regression proves the private sentence reaches the wire today, measured at the transport the Brain actually receives.</done>
</task>

<task type="auto">
  <name>Task 2: Closed-vocabulary typed-question proof in classify() and the shim-scope hook class</name>
  <files>lib/core/part8-egress-guard.cjs, scripts/part8-egress-guard-hook.cjs, tests/test-223-part8-egress.cjs, tests/test-245-egress-contentless.cjs, tests/test-339-theo-ask-compose.cjs, tests/part8-egress-e2e-smoke.test.cjs, tests/part8-egress-guard-hook.test.cjs, tests/test-260906-fda-known-tool-shapes.cjs</files>
  <read_first>
    - lib/core/part8-egress-guard.cjs (whole file, including the _isFreeFormTool and _proveKnownToolShape docblocks that explain step ordering)
    - scripts/part8-egress-guard-hook.cjs lines 1-130 (SHIM_BACKED_AMBIGUOUS_ALLOW_CLASSES and the 260917-dgf / 260917-ild rulings)
    - data/framework-names.json, data/command-registry.json (canonical phrases and command slugs)
    - Each test file listed in this task's files (find assertions that pin free-form strings to verdict allow / class move_set)
    - tests/test-354-egress-typed-question.cjs
  </read_first>
  <action>
In lib/core/part8-egress-guard.cjs add module-private, lazily built vocabularies: QUESTION_FUNCTION_WORDS, a frozen Set of closed function words and generic methodology relation words: a, an, the, of, for, to, in, on, at, by, with, from, into, via, and, or, not, is, are, be, does, do, should, can, which, what, when, why, how, where, best, first, next, then, before, after, between, versus, vs, than, most, more, fit, fits, apply, use, recommend, suggest, sequence, order, follow, follows, feed, feeds, precede, precedes, step, steps, stage, stages, rung, level, type, types, kind, problem, problems, question, framework, frameworks, method, methods, methodology, technique, techniques, definition, defined, simple, complex, complexity, chain, chains, reverse, salient, relationship, relationships, edge, edges, command, commands, compare, difference. It must contain no possessive or personal pronoun (our, my, we, us, your, their, his, her) and no content noun outside methodology. METHODOLOGY_TOKEN_SET: the lowercase alternatives already enumerated in METHODOLOGY_VOCAB (split 'problem[- ]type' and 'edge[- ]type' into their hyphen and space forms) plus the Theo rung ids lowercased (undefined, illdefined, welldefined, wicked). COMMAND_SLUG_SET: slugs from data/command-registry.json. CANONICAL_PHRASES: framework_names plus curated_extras from data/framework-names.json, lowercased, sorted longest first. Load the two data files with fs.readFileSync inside a try/catch; on failure the proof returns false (fail closed).
Add _proveTypedQuestion(str): false unless str is a string of 1-240 characters with no CR or LF and _safeAudit(str) is true; lowercase it, strip every '/mos:' prefix, replace each CANONICAL_PHRASES occurrence (whole-phrase, longest first) with a space, split on /[^a-z0-9_-]+/, drop empty tokens; true only when every remaining token is in QUESTION_FUNCTION_WORDS, METHODOLOGY_TOKEN_SET or COMMAND_SLUG_SET, or matches /^[0-9]{1,2}$/. Export it on module.exports next to the other test seams.
Change classify() step 3 only: after the existing _safeAudit block, return { verdict: 'allow', class: 'typed_question', reason: 'closed-vocabulary methodology question' } when _proveTypedQuestion(str); else when METHODOLOGY_VOCAB.test(str) return { verdict: 'ambiguous', class: 'freeform_unproven', reason: 'methodology vocabulary present but free-form tokens unproven' }; else keep the existing freeform_unmatched ambiguous verdict. Step 1, step 1b, step 2, step 3b and the terminal catch-all are byte-unchanged. Update the classify() docblock and the _isFreeFormTool docblock to state that step 3 no longer treats keyword presence as proof.
In scripts/part8-egress-guard-hook.cjs add 'freeform_unproven' to SHIM_BACKED_AMBIGUOUS_ALLOW_CLASSES, with a comment: on shim-backed scopes the call proceeds to bin/mindrian-brain-mcp-client.cjs, whose ask()/search() refuse it with the renderable egress_blocked envelope (Task 3), because a PreToolUse hook cannot render the refusal (260917-dgf); every non-shim Brain-shaped scope keeps exit 2.
Update only the assertions in the listed existing tests that pin a free-form string to verdict 'allow' / class 'move_set' through keyword presence: change the expectation to the new class (typed_question when the string is fully canonical, freeform_unproven otherwise) and add a one-line comment citing 354-06 D-354-EGR. Do not weaken any block assertion. List every changed assertion (file:line, old, new) in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const g=require('./lib/core/part8-egress-guard.cjs');const v=g.classify({question:'Use the SWOT framework on our confidential plan: cedar will acquire juniper next autumn.'},{toolName:'brain_ask'});if(v.verdict==='allow'||v.class!=='freeform_unproven'){console.error(v);process.exit(1)};const ok=g.classify({question:'Which framework fits an ill-defined problem at the discovery stage?'},{toolName:'brain_ask'});if(ok.class!=='typed_question'){console.error(ok);process.exit(1)}" && node tests/test-223-part8-egress.cjs && node tests/test-245-egress-contentless.cjs && node tests/test-339-theo-ask-compose.cjs && node tests/part8-egress-e2e-smoke.test.cjs && node tests/part8-egress-guard-hook.test.cjs && node tests/test-260906-fda-known-tool-shapes.cjs && node tests/test-257-brain-tool-egress-invariant.cjs && node tests/test-239-query-egress-canary.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_proveTypedQuestion" lib/core/part8-egress-guard.cjs` shows the definition, its use in classify() and its export
    - `grep -c "generic methodology vocabulary handle" lib/core/part8-egress-guard.cjs` prints 0 (the keyword-only allow reason is gone)
    - `grep -n "freeform_unproven" scripts/part8-egress-guard-hook.cjs` matches inside SHIM_BACKED_AMBIGUOUS_ALLOW_CLASSES
    - QUESTION_FUNCTION_WORDS contains none of our, my, we, us, your, their (grep on the Set literal returns no match for those quoted words)
    - All listed existing egress suites plus tests/test-257-brain-tool-egress-invariant.cjs and tests/test-239-query-egress-canary.cjs exit 0; the SUMMARY lists every edited assertion
  </acceptance_criteria>
  <done>"Generic" now means structurally proven; keyword presence alone can no longer earn an allow.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce the typed-question gate in ask(), search() and smartSearch() before any wire call</name>
  <files>lib/core/brain-client.cjs</files>
  <read_first>
    - lib/core/brain-client.cjs lines 560-700 (callTool: key gate, belt, egress_blocked sentinel idiom), 1040-1100 (search, smartSearch), 1395-1420 (ask)
    - bin/mindrian-brain-mcp-client.cjs lines 160-280 (the Phase 257 egress_blocked rendering branch for brain_ask; honestRefusal for brain_search)
    - lib/core/part8-egress-guard.cjs (after Task 2)
    - tests/test-354-egress-typed-question.cjs
  </read_first>
  <action>
Add module-private _typedFreeformGate(toolName, text) to lib/core/brain-client.cjs: require('./part8-egress-guard.cjs') lazily, run classify({ [toolName === 'brain_search' ? 'query' : 'question']: text }, { toolName }); return null when verdict is 'allow' and class is 'typed_question'; otherwise return the sentinel { error: 'egress_blocked', tool: toolName, egress_class: verdict === 'block' ? 'content_set' : 'freeform_unproven', reason: 'Brain free-form channels accept closed-vocabulary methodology questions only (framework names, problem types, stages, command slugs); rephrase without room or venture details.' }. If the guard module fails to load, return the same sentinel with egress_class 'guard_unavailable' (fail closed on these two natural-language channels only).
Call it at the top of ask(question) (after the existing empty-string check, before callTool), at the top of search(queryText), and at the top of smartSearch(queryText) before the first search() call and before its Cypher fallback. A non-null result is returned as-is and no callTool runs. Do not edit callTool's belt, the other wrappers, query(), or the no-key null contract. The shim already renders the sentinel for brain_ask (Phase 257 branch); verify brain_search's honestRefusal path renders the error text too, and if it does not, fix it in this task only by passing the sentinel through the existing honestRefusal helper (no new rendering path).
  </action>
  <verify>
    <automated>node tests/test-354-egress-typed-question.cjs && node tests/test-339-theo-ask-compose.cjs && node tests/test-247-contract-client.cjs && node tests/test-257-refusal-egress-kind.cjs && node docs/reviews/phase-354-probes/theo.cjs 2>/dev/null | grep false-safe-egress | grep -q '"capturedToolCalls":\[\]'</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-354-egress-typed-question.cjs` exits 0 (cases A-F)
    - `grep -c "_typedFreeformGate(" lib/core/brain-client.cjs` is at least 4 (definition plus ask, search, smartSearch)
    - The theo probe's false-safe-egress line prints `"capturedToolCalls":[]`
    - tests/test-247-contract-client.cjs and tests/test-257-refusal-egress-kind.cjs exit 0
  </acceptance_criteria>
  <done>Private prose that happens to name a framework never leaves the machine on any surface; generic methodology questions still flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LOCAL -> BRAIN (Canon Part 8) | brain_ask / brain_search strings cross to theo-mcp.onrender.com |
| model -> PreToolUse hook -> shim -> brain-client | Model-issued Brain calls pass the hook, then the stdio shim, then ask()/search() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-11 | Information disclosure | brain-client ask/search/smartSearch | mitigate | _typedFreeformGate refuses any string not proven closed-vocabulary before callTool; transport-captured regression and shim end-to-end test |
| T-354-12 | Denial of service (usability) | legitimate methodology questions | mitigate | 20-question generic corpus including every internal template must pass; honest refusal names how to rephrase |
| T-354-13 | Spoofing | hook versus in-process divergence | mitigate | freeform_unproven proceeds to the shim only on shim-backed scopes, where ask()/search() is the single enforcement point; non-shim scopes still exit 2 |
| T-354-13b | Information disclosure | brain_query free-form Cypher | accept | Admin-gated tool with its own raw-field guard (brain-client.cjs:601) and ambiguous disclosure; recorded as bounded residual in the ledger |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` shows test-354-egress-typed-question PASSED.
- Every existing egress suite passes with only the documented assertion updates.
</verification>

<success_criteria>
The theo probe's false-safe-egress observation flips to zero captured tool calls, and the model-issued shim path refuses the same sentence honestly.
</success_criteria>

## Artifacts this phase produces

- `_proveTypedQuestion(str)` (new, exported test seam in lib/core/part8-egress-guard.cjs) with `QUESTION_FUNCTION_WORDS`, `METHODOLOGY_TOKEN_SET`, `COMMAND_SLUG_SET`, `CANONICAL_PHRASES`
- New classify classes: `typed_question` (allow), `freeform_unproven` (ambiguous)
- `_typedFreeformGate(toolName, text)` (new module-private in lib/core/brain-client.cjs); egress_class values `freeform_unproven`, `guard_unavailable`
- `SHIM_BACKED_AMBIGUOUS_ALLOW_CLASSES` gains `freeform_unproven` (scripts/part8-egress-guard-hook.cjs)
- `tests/test-354-egress-typed-question.cjs` (new regression)
- Decision label `D-354-EGR`

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-06-SUMMARY.md` when done (stage with `git add -f`).
</output>
