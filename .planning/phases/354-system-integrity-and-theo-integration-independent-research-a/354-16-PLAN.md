---
phase: 354-system-integrity-and-theo-integration-independent-research-a
plan: 16
type: execute
wave: 7
depends_on: ["354-13", "354-14", "354-15"]
files_modified:
  - tests/test-354-concurrency-surfaces.cjs
  - docs/reviews/phase-354-disposition-ledger.md
  - docs/reviews/phase-354-close-out.md
  - docs/reviews/2026-09-20-full-system-code-review.md
  - docs/OPEN-HANDOFFS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03, SYS-04, SYS-05, SYS-06, SYS-07, SYS-08, SYS-09, THEO-01, THEO-02, THEO-03]
tags: [close-out, concurrency, tri-polar, acceptance, phase-354]

must_haves:
  truths:
    - "Concurrent CLI, MCP and browser clients on two rooms do not cross rooms, do not lose updates silently, and cannot bypass a required human decision; each surface check is run or labelled HOST UI NOT RUN explicitly"
    - "Every candidate ID and every newly discovered issue has a final evidence-backed disposition, owner, implementation reference and verification command in the ledger; BLOCKED items (THEO-02, and live certification if it was not reached) stay open"
    - "Targeted regressions plus the substrate, connector, orchestration, render and bounded acceptance gates were run, each recorded with command, exit status, duration and environment"
    - "The 2026-09-20 review carries a Corrections section naming each overstatement and its corrected finding, and the legacy localhost review findings are rechecked with dispositions"
    - "A tracked close-out states actual coverage, measured results, residual risks and deployment status (fixed on main, not live until released and picked up)"
  artifacts:
    - path: "tests/test-354-concurrency-surfaces.cjs"
      provides: "Two-room, multi-client concurrency and human-decision bypass checks across CLI, MCP stdio and browser"
      min_lines: 120
    - path: "docs/reviews/phase-354-close-out.md"
      provides: "Tracked close-out: coverage, measured gates, residual risks, deployment status"
      contains: "Residual risks"
    - path: "docs/reviews/phase-354-disposition-ledger.md"
      provides: "Final dispositions with implementation references and verification"
      contains: "Final disposition"
  key_links:
    - from: "docs/reviews/phase-354-close-out.md"
      to: "tests/run-all-354.sh"
      via: "measured gate table"
      pattern: "run-all-354.sh"
    - from: ".planning/REQUIREMENTS.md Phase 354 rows"
      to: "docs/reviews/phase-354-disposition-ledger.md"
      via: "Measured lines citing commands and exit codes"
      pattern: "\\*\\*Measured:\\*\\*"
---

<objective>
Close Phase 354 against the handoff's Acceptance and close-out section, point by point: concurrency across surfaces (Task 1), measured gates (Task 2), and the tracked close-out with dispositions, corrections, requirement rows and the Dev-Research Compositing filing (Task 3). This plan fixes no production code; any failure found becomes a NEW FINDING with a reproduction for gap closure, never a silent pass.

Preconditions: workspace guard and unowned-diff check from 354-01's objective apply. docs/OPEN-HANDOFFS.md carried an uncommitted Phase 354 registration line at planning time; if it is still uncommitted, keep that line intact (edit around it, never revert it) and commit it together with this plan's update, naming it in the commit message.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-CONTEXT.md
@docs/2026-09-20-HANDOFF-phase-354-system-integrity-and-theo.md
@docs/reviews/phase-354-disposition-ledger.md
@.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md
@.planning/debug/sys-07-acceptance-timing.md
@docs/reviews/2026-09-20-localhost-workspace-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Concurrency and Tri-Polar surface checks</name>
  <files>tests/test-354-concurrency-surfaces.cjs</files>
  <read_first>
    - tests/helpers/fixture-room-354.cjs, tests/helpers/write-lock-holder-354.cjs
    - lib/mcp/gate-ledger.cjs lines 90-110 (single-use consumption)
    - lib/mcp/tools/views.cjs artifact_file handler, lib/mcp/tools/gate.cjs gate_answer, lib/mcp/tool-router.cjs meeting file-meeting
    - bin/mindrian-mcp-server.cjs (stdio server entry and how it chooses its room)
    - docs/reviews/localhost-poc/server.cjs (after 354-11)
  </read_first>
  <action>
Create tests/test-354-concurrency-surfaces.cjs with legs, each asserting through fresh room.db handles and file reads:
(K1 Desktop protocol, two rooms) spawn two `node bin/mindrian-mcp-server.cjs` stdio servers through the SDK StdioClientTransport, one bound to room A and one to room B (use the env or argument the server reads for its fallback room; read the entry file to find it), call artifact_file concurrently on both (Promise.all, 10 writes each with distinct filenames); room A holds exactly its 10 files and 10 artifact memory_event rows and none of B's, and vice versa; status_read on each reports tool_registration.complete true.
(K2 CLI plus MCP, one room, no lost update) in room A, run 5 forked CLI-style writers (tests/helpers/write-lock-holder-354.cjs in graph-ops mode writing a distinct node through lib/core/graph-ops.cjs) while the MCP client files 5 artifacts; afterwards every one of the 10 writes is present; any contention surfaced as an explicit held error that the writer retried, never a silent drop (writers report their outcome over IPC).
(K3 human decision cannot be bypassed) file-meeting a claim through the MCP client (session S1), then call gate_answer approve with a different session id (S2): the response is refused (session_mismatch) and the claim stays proposed; a second approve from S1 on the same gate_id is refused as consumed (the observed single-use burn at gate-ledger.cjs:100, recorded as fail-closed by design); the claim still reads proposed. Then a fresh file-meeting plus S1 approve confirms that exact claim (354-02).
(K4 browser plus MCP, same document) start the POC server in room mode on room A; load the document in a Playwright page; file a new version of the same artifact through the MCP artifact_file tool; the page shows the external-change reload (clean editor) and, after typing, a save returns 409 with the conflict banner (no silent overwrite). If Playwright is unavailable, K4 prints ENV GAP and the test exits 77 only if K1-K3 passed.
(K5 Cowork shared state) record as a covered-by statement: Cowork shares the room folder across sessions, which K1-K4 exercise at the filesystem and database layer; print 'HOST UI NOT RUN: Claude Desktop app, Cowork app' so no host UI check is implied.
Exit 1 on any failure.
  </action>
  <verify>
    <automated>node tests/test-354-concurrency-surfaces.cjs; rc=$?; test $rc -eq 0 -o $rc -eq 77</automated>
  </verify>
  <acceptance_criteria>
    - The test spawns bin/mindrian-mcp-server.cjs twice through StdioClientTransport (K1) and forks CLI writers (K2)
    - The output contains the literal line 'HOST UI NOT RUN: Claude Desktop app, Cowork app'
    - `node tests/test-354-concurrency-surfaces.cjs` exits 0 on this machine (Playwright present); any failing leg is recorded as a NEW FINDING in the ledger with its output
  </acceptance_criteria>
  <done>Cross-room isolation, no-lost-update and no-bypass hold across the CLI, MCP stdio and browser clients, with host UI checks labelled as not run.</done>
</task>

<task type="auto">
  <name>Task 2: Measured gate run with command, exit, duration and environment</name>
  <files>docs/reviews/phase-354-close-out.md</files>
  <read_first>
    - project CLAUDE.md Verification section (the gate commands)
    - tests/run-all-354.sh
    - .planning/debug/sys-07-acceptance-timing.md (for the bounded acceptance numbers)
    - tests/audit-localhost-workspace-review.cjs (existing recheck harness for the legacy dashboard findings)
  </read_first>
  <action>
Create docs/reviews/phase-354-close-out.md with a 'Measured gates' table. For each command record the exact command, exit status, wall-clock duration (use /usr/bin/time -f %e or date +%s.%N deltas) and environment (node version, `git rev-parse HEAD`, `node lib/core/repo-version.cjs`, sandbox on or off). Commands: `bash tests/run-all-354.sh`; `node scripts/check-substrate.cjs --diff`; `node scripts/build-connector-registry.cjs --check`; `node scripts/build-orchestration-projection.cjs --check`; `node scripts/check-render-coverage.cjs`; `node scripts/check-tool-honesty.cjs --check`; `node scripts/check-shape-declaration.cjs` (advisory; record WARN count); `node scripts/build-skill-mirrors.cjs --check`; `timeout 900 node scripts/doctor.cjs --acceptance --pre-tag --json` (bounded, with 354-13 progress); the adjacent suites each 354 plan ran (`bash tests/run-all-166.sh`, `bash tests/run-all-349.sh`, `node lib/memory/write-lock-atomic.test.cjs`, every tests/*egress*.cjs and tests/part8-egress*.cjs file, tests/test-345-*.cjs, tests/test-276-meeting-gate-wiring.cjs); `node tests/test-354-concurrency-surfaces.cjs`; and the legacy localhost recheck `node tests/audit-localhost-workspace-review.cjs` (read its output for the ROOM_DATA, file-event refresh, mobile overflow, foreign Host read and chat-integration rows). A failing command is reported as failing with its output excerpt; nothing is re-run until green without saying so.
  </action>
  <verify>
    <automated>grep -q "Measured gates" docs/reviews/phase-354-close-out.md && grep -q "run-all-354.sh" docs/reviews/phase-354-close-out.md && grep -q "check-substrate.cjs --diff" docs/reviews/phase-354-close-out.md && grep -q "audit-localhost-workspace-review.cjs" docs/reviews/phase-354-close-out.md</automated>
  </verify>
  <acceptance_criteria>
    - The 'Measured gates' table has one row per command listed in the action with a numeric exit status and a duration
    - The environment block contains the HEAD commit and the repo-version output
    - The legacy localhost review rows (ROOM_DATA, refresh, mobile overflow, foreign Host read, chat integration) each carry a recheck result: CONFIRMED, REFUTED or ALREADY FIXED (the foreign Host row cites 354-08)
  </acceptance_criteria>
  <done>Every gate the handoff names has a measured, recorded result; existing passes are not used as a substitute for the new behavior tests.</done>
</task>

<task type="auto">
  <name>Task 3: Final dispositions, review corrections, requirement rows, handoff entry and rethinking-room filing</name>
  <files>docs/reviews/phase-354-disposition-ledger.md, docs/reviews/phase-354-close-out.md, docs/reviews/2026-09-20-full-system-code-review.md, docs/OPEN-HANDOFFS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/reviews/phase-354-disposition-ledger.md (from 354-01)
    - every .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-*-SUMMARY.md
    - .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-THEO-EVIDENCE.md
    - .planning/debug/sys-07-acceptance-timing.md
    - docs/reviews/2026-09-20-full-system-code-review.md (whole file)
    - .planning/REQUIREMENTS.md section 'Phase 354 - System Integrity and Theo Integration (SYS and THEO families)' and the Traceability section
    - docs/OPEN-HANDOFFS.md top entries
    - The most recent rethinking-mindrianos research entry for its folder and file convention (`git -C ~ show --stat 6dbaf1089` or `ls ~/MindrianRooms/rethinking-mindrianos/research/ | tail -5`)
  </read_first>
  <action>
Ledger: add a 'Final disposition' column (or section) for all 12 IDs plus every new finding recorded in 354 SUMMARYs: disposition (FIXED-VERIFIED, BLOCKED, ENV GAP, NEW FAILURE OPEN, REFUTED), implementation reference (commit hashes and files), verification command with its exit status from Task 2. THEO-02 stays BLOCKED on Phase 351; live certification stays OPEN if 354-THEO-EVIDENCE.md recorded ENV GAP; SYS-07 carries the 354-13 classification.
Close-out doc: add sections 'Coverage' (per production area, examined by which plan, and what remains unexamined), 'Residual risks' (at least: brain_query free-form Cypher on its existing path; exported presentation without CSP relying on escaping; the check-then-write race window accepted in 354-05; a live but hung lock owner requires breakLock; host UI checks not run; THEO-02 provider consumer absent), 'Decisions' (D-354-SYS05, D-354-EGR with their impact), and 'Deployment status': fixed on main, not live for users until a release is cut and users update (`/plugin marketplace update` then `claude plugin update mos@mindrian-marketplace`); no release was cut by this phase.
Corrections: append a '## Corrections (Phase 354, <date>)' section to docs/reviews/2026-09-20-full-system-code-review.md listing each overstatement with its corrected finding and evidence link: the subjective 5/10 and 6/10 ratings are not measurements; percent-encoded URI traversal did not escape through the SDK matcher (symlink escape did, fixed in 354-05); graph rebuild is scoped and transactional (not blanket destruction); taxonomy casing was limited to taxonomy_ladder (recommendChain was correct); extract_shallow's connector metadata did not all promise writes; the acceptance timeout was an unresolved observation (see the SYS-07 RCA); release notification consumption is a cross-repository gap, not proven drift. Do not edit the original findings text above the new section.
REQUIREMENTS.md: for each of SYS-01..09 and THEO-01..03, flip `- [ ]` to `- [x]` only when its final disposition is FIXED-VERIFIED, adding a '**Measured:**' line (date, command, exit status, pass counts); otherwise leave `- [ ]` with a '**Status:**' line naming the reason (BLOCKED on Phase 351, ENV GAP, NEW FAILURE with the RCA path).
OPEN-HANDOFFS.md: add a dated top entry for Phase 354 close-out linking docs/reviews/phase-354-close-out.md, naming the open items (THEO-02 via Phase 351, any OPEN live certification, any NEW FAILURE). Preserve the existing uncommitted Phase 354 registration line.
Dev-Research Compositing (project CLAUDE.md): file the research trail into the rethinking-mindrianos room as markdown only, following the folder and frontmatter convention of its most recent research entry (a dated folder such as research/<YYYY-MM-DD>-phase-354-system-integrity/ holding a summary that cites docs/reviews/2026-09-23-deep-system-research.md, the ledger and the close-out), cross-linked back to this phase; commit it in the repository that tracks that room using the same message convention as the recent 'rethinking-mindrianos: file ...' commits. No room.db or graph write in that room.
Every doc edited here uses hyphens only; names no real person; contains no secrets.
  </action>
  <verify>
    <automated>grep -q "Final disposition" docs/reviews/phase-354-disposition-ledger.md && grep -q "Residual risks" docs/reviews/phase-354-close-out.md && grep -q "Deployment status" docs/reviews/phase-354-close-out.md && grep -q "## Corrections (Phase 354" docs/reviews/2026-09-20-full-system-code-review.md && test "$(grep -cE '^- \[(x| )\] \*\*(SYS-0[1-9]|THEO-0[1-3])\*\*' .planning/REQUIREMENTS.md)" -eq 12 && ! grep -q $'\xe2\x80\x94' docs/reviews/phase-354-close-out.md docs/reviews/phase-354-disposition-ledger.md</automated>
  </verify>
  <acceptance_criteria>
    - The ledger has a final disposition, implementation reference and verification entry for all 12 IDs; THEO-02 reads BLOCKED with Phase 351
    - docs/reviews/phase-354-close-out.md contains 'Coverage', 'Residual risks' (at least the six named risks), 'Decisions' and 'Deployment status' stating not released
    - docs/reviews/2026-09-20-full-system-code-review.md ends with a '## Corrections (Phase 354' section and `git diff` shows no deletion above it
    - `grep -cE '^- \[(x| )\] \*\*(SYS-0[1-9]|THEO-0[1-3])\*\*' .planning/REQUIREMENTS.md` prints 12, and every `- [x]` row has a '**Measured:**' line while every `- [ ]` row has a '**Status:**' line
    - docs/OPEN-HANDOFFS.md has the new dated entry and still contains the earlier Phase 354 registration line
    - A new dated research entry exists under ~/MindrianRooms/rethinking-mindrianos/research/ citing the ledger and the close-out
  </acceptance_criteria>
  <done>The phase closes with every finding dispositioned from evidence, open dependencies left open, the earlier review corrected, and the reasoning trail filed in both homes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two MCP servers, CLI writers, browser -> shared room folders | Concurrent clients on the same filesystem and databases |
| tracked docs -> public repository | Close-out and ledger are published |
| dev repo -> rethinking-mindrianos room | Research trail filed into a real room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-354-32 | Elevation of privilege | gate_answer across sessions | mitigate | K3 proves a foreign session cannot approve and a burned gate stays fail-closed |
| T-354-33 | Tampering | cross-room writes under concurrency | mitigate | K1 and K2 prove per-room isolation and no lost update with fresh-handle reads |
| T-354-30 | Information disclosure | published close-out and ledger | mitigate | Synthetic data only, no names, no secrets, no room content; hyphen and secret checks in verify |
| T-354-34 | Tampering | rethinking room filing | mitigate | Markdown files only in a new dated folder; no room.db or graph write; follows the room's existing convention |
</threat_model>

<verification>
- `bash tests/run-all-354.sh` reports every leg PASSED or SKIPPED (ENV GAP) with the reason printed.
- The close-out, ledger, corrections and requirement rows are consistent with each other.
</verification>

<success_criteria>
Every bullet of the handoff's Acceptance and close-out section maps to a recorded artifact: dispositions (ledger), room journey (354-11), Theo journey (354-12), concurrency (Task 1), measured gates (Task 2), corrections and close-out (Task 3).
</success_criteria>

## Artifacts this phase produces

- `tests/test-354-concurrency-surfaces.cjs` (new; legs K1-K5)
- `docs/reviews/phase-354-close-out.md` (new tracked close-out)
- Final-disposition section in `docs/reviews/phase-354-disposition-ledger.md`
- `## Corrections (Phase 354, <date>)` section in `docs/reviews/2026-09-20-full-system-code-review.md`
- Finalized SYS-01..09 and THEO-01..03 rows in `.planning/REQUIREMENTS.md`
- New dated entry in `docs/OPEN-HANDOFFS.md` and a new research entry under `~/MindrianRooms/rethinking-mindrianos/research/`

<output>
Create `.planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-16-SUMMARY.md` when done (stage with `git add -f`).
</output>
