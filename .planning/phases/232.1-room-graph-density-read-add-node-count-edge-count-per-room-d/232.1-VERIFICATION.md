---
phase: 232.1-room-graph-density-read-add-node-count-edge-count-per-room-d
verified: 2026-07-25T20:20:16Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 232.1: Room-Graph Density Read Verification Report

**Phase Goal:** Add a density read to `/mos:doctor` and/or `room_state`/`--acceptance` output: node count and edge count per `room.db`, read exclusively through `lib/core/navigation.cjs` (Canon Part 9 substrate guard, no direct `room.db` opens outside that chokepoint). Closes SEED-074's own "Suggested first move." Out of scope: PageRank/Louvain, SAG-style query-time joins, any query-language surface. Hard guard: no "dense"/"at risk" claim anywhere in output.

**Verified:** 2026-07-25T20:20:16Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `/mos:doctor` reports a per-registered-room node/edge count via a doctor module | VERIFIED | `lib/core/doctor/room-graph-density-module.cjs` exists, registered in `data/doctor-modules.json:142-150` (`id: room-graph-density`, `cadence: always`, `flag: null`, `fix_supported: false`). `node scripts/doctor.cjs --json` (real HOME) shows the module correctly listed in the accumulative engine's `deferred[]` because this dev box's local `installed_plugins.json` version (`1.15.3-beta.46`) lags the repo's own version-of-record (`1.15.3-beta.47`, matching `introduced_version`) — this is the documented, intentional versioning-gate behavior (D-08), not a phase defect; the phase's own test suite (`runDoctorJson`) sidesteps it by stamping a scratch HOME at the repo's own version and confirms the module fires and reports correctly under that condition (`node tests/run-all-232.1.sh` scenario "2 rooms... -> ok, counts + totals correct" passes). |
| 2 | Read exclusively through `lib/core/navigation.cjs`, never a direct `room.db` open | VERIFIED | `room-graph-density-module.cjs` imports only `openRoomDbReadOnlyForCaller`/`closeRoomDbForCaller` from `../navigation/spine-events.cjs` (the Part-9 chokepoint submodule); `roomDensityLine` in `lib/mcp/tool-router.cjs` requires `../core/navigation.cjs` and calls `navigation.openRoomDbReadOnlyForCaller`. `node scripts/check-substrate.cjs --diff` staged against all touched files exits 0 (zero new chokepoint-bypass violations introduced by this phase; the 209 pre-existing baseline violations elsewhere in the repo are unrelated legacy debt, confirmed unaffected). |
| 3 | A room with no `room.db` reports 0/0 and never crashes the sweep (D-05) | VERIFIED | `check()`'s per-room branch pushes `{node_count:0, edge_count:0, has_db:false}` when `openRoomDbReadOnlyForCaller` returns `null`; `roomDensityLine` returns `'Local graph: 0 nodes, 0 edges (no room.db yet).'` on the same condition. Confirmed by test scenarios "schema-less room.db -> 0/0, no throw" and "bound room with no room.db yet -> 0/0" — both pass. |
| 4 | A corrupt registry entry soft-fails only that room; sweep completes (T-217-01) | VERIFIED | Per-room `try/catch` wraps the `resolveRoomPath` call itself; corrupt entries push `{unreadable:true}` and the loop continues. Test scenario "corrupt registry entry soft-fails that room only" passes with overall `status: 'ok'`. |
| 5 | No rendered output string ever claims a room graph is dense/sparse/at-risk/healthy (D-06) | VERIFIED | Direct grep of the actual return-value string literals in both `room-graph-density-module.cjs` (`detail:`) and `tool-router.cjs`'s `roomDensityLine` found zero forbidden adjectives — the words `dense`/`healthy`/`risk` appear ONLY inside code comments documenting the guard, never in a returned string. Both test suites carry a dedicated D-06 forbidden-word regression (`grep`-style case-insensitive substring check against `dense, sparse, density, risk, healthy, low, high`) and both pass. |
| 6 | `room_state status`/`get-state` includes the bound room's own node/edge counts, resolved against the LIVE `activeRoomDir`, never the boot-time closure (D-07) | VERIFIED | `case 'status'` (tool-router.cjs:671) and `case 'get-state'` (tool-router.cjs:683) both concatenate `roomDensityLine(activeRoomDir)`, using the variable resolved at line ~611 via `resolveWriteTargetDir`, not the outer `roomDir` parameter. Test scenario "mid-session active-room switch reroutes the density line (activeRoomDir, not boot roomDir)" explicitly exercises a boot-room vs. registry-active-room divergence and asserts the response reflects the ACTIVE room's counts — passes. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/core/navigation/spine-events.cjs` | `openRoomDbReadOnlyForCaller` + `_fileUriPath` | VERIFIED | Both present (lines 110-112, 431-443), exported in `module.exports` (line 455). |
| `lib/core/navigation.cjs` | re-export of the new door | VERIFIED | Line 461: `openRoomDbReadOnlyForCaller: spineEvents.openRoomDbReadOnlyForCaller,` |
| `lib/core/doctor/room-graph-density-module.cjs` | `check(ctx)` doctor module | VERIFIED | Exports `{ check }` only, no `fix` (contract-parity rule 8 requirement). |
| `data/doctor-modules.json` | `room-graph-density` entry | VERIFIED | Present with correct `introduced_version` matching `.claude-plugin/plugin.json`'s live version. |
| `lib/mcp/tool-router.cjs` | `roomDensityLine(activeRoomDir)` | VERIFIED | Defined (line 594), wired into both `status` and `get-state`, exported via `module.exports._test.roomDensityLine`. |
| `tests/test-232.1-room-graph-density.cjs` | door + module regression (plan said 8 scenarios) | VERIFIED (9 scenarios) | An extra scenario ("abs_path-registered room... is counted, not silently skipped") was added post-review to pin the WR-01 fix — a strengthening beyond plan scope, not a gap. |
| `tests/test-232.1-room-state-density.cjs` | room_state fold regression, 6 scenarios | VERIFIED | 6/6 scenarios present and pass. |
| `tests/run-all-232.1.sh` | phase aggregator | VERIFIED | Globs `test-232.1-*.cjs`, discovers and runs both files, exits 0. |
| `commands/doctor.md` | check-inventory bullet | VERIFIED | Line 84, correct `(no flag; bare run + --all; check-only)` format, D-06 guard restated in the doc copy itself. |
| `lib/memory/run-feynman-tests.cjs` | registration lines | VERIFIED | Both test files registered (lines 351, 360). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `room-graph-density-module.cjs` | `spine-events.cjs` | `require('../navigation/spine-events.cjs').openRoomDbReadOnlyForCaller` | WIRED | Confirmed by direct read; no `room-db.cjs` or `node:sqlite` import anywhere in the doctor module. |
| `data/doctor-modules.json` entry | `room-graph-density-module.cjs` | `runner` path resolved by `scripts/doctor.cjs` | WIRED | Confirmed via `node scripts/doctor.cjs --json`: module appears correctly in the accumulative engine's registry-driven dispatch (as `selected` or `deferred` depending on version gate). |
| `tool-router.cjs roomDensityLine` | `lib/core/navigation.cjs` | `require('../core/navigation.cjs').openRoomDbReadOnlyForCaller` | WIRED | Confirmed by direct read (line 595, 598). |
| `room_state case 'status'`/`'get-state'` | `roomDensityLine(activeRoomDir)` | string concatenation into `textResponse(...)` | WIRED | Confirmed at lines 671 and 683; both use the in-scope `activeRoomDir`, not the outer `roomDir` closure parameter. |

### Behavioral Spot-Checks / Re-Run Verification Commands (executed live by this verifier, not trusted from prior docs)

| Command | Result | Status |
|---------|--------|--------|
| `bash tests/run-all-232.1.sh` | `Phase 232.1: PASS=2 FAIL=0` (9 + 6 = 15 scenarios, all `ok`) | PASS |
| `node tests/test-doctor-module-contract-parity.cjs` | `ALL PASS (2 assertions)` — 16 registry modules pass the 9-rule gate | PASS |
| `node tests/test-doctor-doc-parity.cjs` | `ALL PASS (3 assertions)` | PASS |
| `node scripts/check-substrate.cjs --diff` (staged: `lib/core/navigation`, `lib/core/doctor`, `data/doctor-modules.json`, `lib/mcp/tool-router.cjs`) | exit 0, zero new violations | PASS |
| `node scripts/doctor.cjs --acceptance` | `14/15 points passed`; the 1 failure (`verify-release-clean-tree`) is unrelated pre-existing `.planning/debug/*` tracked-file drift, not touched by this phase | PASS (unrelated fail excluded) |
| `node scripts/doctor.cjs --json` (real HOME) | `checks['accumulative-engine'].deferred` includes `room-graph-density` because local `installed_plugins.json` (`beta.46`) lags repo version (`beta.47`) | EXPECTED / matches documented D-08 versioning-gate behavior, exercised correctly by the module's own hermetic tests |

### D-06 Forbidden-Word Guard — Direct Grep (not just test-passing)

`grep -in "dense\|at risk\|healthy" lib/core/doctor/room-graph-density-module.cjs lib/mcp/tool-router.cjs` — every match is inside a `//` or `/* */` comment documenting the guard itself; zero matches inside a returned string literal (`detail:` value or `roomDensityLine`'s return statements).

### Scope Guard — Out-of-Scope Terms

`grep -rniE "pagerank|louvain|SAG-style|query-time-join|query.language|pattern.match"` across all new/modified implementation files (`room-graph-density-module.cjs`, `spine-events.cjs`, `tool-router.cjs`) returns zero hits. Scope boundary honored.

### Anti-Patterns Found

None blocking. No `TBD`/`FIXME`/`XXX` markers, no placeholder returns, no empty handlers in any new file.

## Judgment on the 3 REVIEW.md Warnings

- **WR-01 (`resolveRoomPath` dropping `abs_path`-only entries):** Already fixed in the code as it stands today — `resolveRoomPath` now checks `info.abs_path` first, falls back to `info.path`, and a new 9th test scenario pins the fix (`abs_path-registered room (no path key) is counted, not silently skipped`, verified passing). This warning is resolved, not outstanding. Verdict: **not a blocker — closed.**
- **WR-02 (no Windows path handling in `_fileUriPath`):** Confirmed still present, and confirmed inherited verbatim from 3 pre-existing call sites (`session-presence.cjs`, `coverage-rollup.cjs`, `graph-derivation.cjs`) that share the identical gap — this phase reproduces an existing repo-wide idiom into a 4th site rather than introducing a new defect class. Failure mode on Windows is a silent 0/0 read (degrades gracefully per D-05's own contract, does not crash or corrupt), and this repo's primary dev/deploy targets are documented as Linux/macOS-first with existing separate Windows regression fences elsewhere. Verdict: **legitimate deferred, cross-cutting debt — does not block this specific phase's goal** (the phase's goal is "add the read," not "fix cross-platform path handling repo-wide"). Should be logged as known-debt against the 4 call sites collectively, not against this phase alone.
- **WR-03 (no busy-timeout on the read-only door):** Confirmed still present, confirmed matches all 3 existing read-only precedents (none of which set a timeout either), and confirmed self-correcting (a rare lock collision reads as 0 for one table on one run, not a permanent miscount, and WAL mode makes true contention rare for a reader). Verdict: **acceptable, self-healing, and consistent with existing precedent — does not block.**

**Overall warnings verdict:** 1 of 3 already closed; the remaining 2 are honestly-documented, inherited-idiom debt with graceful (non-crashing) degradation, explicitly called out inline in the source code itself (not swept under the rug). None of the 3 threaten the phase's actual goal (self-reported density census, read-only, no health-adjective claim) — they threaten correctness only in narrow, already-degrading-safely edge cases (non-`path`-keyed registry entries — now fixed; Windows; rare lock contention). Recommend logging WR-02 and WR-03 as known-debt items (a follow-up todo referencing the 4 shared call sites) rather than reopening this phase.

### Human Verification Required

None — every truth is grep/test/read verifiable; no visual, real-time, or external-service behavior in scope.

### Documentation Hygiene Note (not a gap, informational)

`232.1-VALIDATION.md`'s frontmatter (`status: draft`, `wave_0_complete: false`) and its entire Per-Task Verification Map (every row marked "pending") were never updated after execution — this is stale pre-execution planning scaffolding, not a reflection of actual test state. Confirmed by this verifier's own live re-run: every command the VALIDATION map lists as "pending" actually passes. Recommend the VALIDATION.md be updated to close out its own checklist for hygiene, but this does not affect the goal-achievement verdict since the actual evidence (live command re-runs, direct code reads) supersedes the stale doc.

---

_Verified: 2026-07-25T20:20:16Z_
_Verifier: Claude (gsd-verifier)_
