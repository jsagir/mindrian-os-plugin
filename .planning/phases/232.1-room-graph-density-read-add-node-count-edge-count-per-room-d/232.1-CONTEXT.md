# Phase 232.1: Room-Graph Density Read - Context

**Gathered:** 2026-07-25
**Status:** Ready for planning

<domain>
## Phase Boundary

Add a density read (node count, edge count per `room.db`) so SEED-074's own gate
condition -- "measured room.db density crossing a real threshold" -- becomes
something the system reports on its own, instead of something a human has to
remember to go check by hand (as SEED-074's own author had to do on 2026-07-25,
sampling 2 rooms manually, both 0 tables). This phase implements ONLY SEED-074's
"Suggested first move" section. It does not open SEED-074's actual gate.

</domain>

<decisions>
## Implementation Decisions

### Surface placement (the one gray area discussed)
- **D-01:** Both surfaces ship, per navigator choice over three options (doctor-only /
  room_state-only / both). Doctor module is the primary deliverable (system-wide
  census, closes SEED-074's gate); room_state gets a lightweight single-room
  addition for in-conversation visibility.
- **D-02:** Doctor side reuses Phase 217's per-class module registry pattern exactly:
  one new `lib/core/doctor/room-graph-density-module.cjs` + one new entry in
  `data/doctor-modules.json` ("new organ module = ONE entry, no engine code
  change"). `cadence: 'always'` (per-invocation diagnostic, not a one-time heal).
  `fix_supported: false` (a density count is check-only; there is nothing to
  "fix" about a measurement).
- **D-03:** Room iteration reuses `lib/core/doctor/cascade-rooms-module.cjs`'s
  pattern verbatim: `readRegistry()` + `resolveRoomPath()` from
  `lib/core/doctor/shared.cjs`, one try/catch per room so a single bad room
  soft-fails without aborting the sweep (same T-217-01 self-DoS guard cascade-rooms
  already uses).
- **D-04:** `room.db` access goes through `lib/core/navigation.cjs`'s
  `openRoomDbForCaller` / `closeRoomDbForCaller` door (the Phase 135-01
  caller-owned-handle pattern) -- never a direct `room.db` open. Canon Part 9
  substrate guard is non-negotiable; this is the existing sanctioned door for
  exactly this "diagnostic caller needs a live handle" shape.
- **D-05:** Must handle the "0 tables" case as the NORMAL case, not an error --
  that is today's actual measured reality (both sampled rooms in SEED-074 came
  back 0 tables). `room.db` absent or schema-less reports `node_count: 0,
  edge_count: 0`, never throws.
- **D-06:** No risk or density-adjective framing in any output copy -- report raw
  counts only. This is SEED-074's own "Hard guard on any downstream claim"
  section, binding on this phase's own output, not just future artifacts: never
  imply room graphs are "dense" or "at risk" today.
- **D-07:** `room_state` side folds ONE additional line (this bound room's own
  node/edge count) into whatever existing status/get-state assembly point is
  found at research time -- same `navigation.cjs` door, no new query mechanism,
  no new command surface.

### Claude's Discretion
- Exact node/edge counting query shape (which typed node tables count as
  "nodes", whether edge tables are summed separately or reported as one
  combined figure) -- resolved by reading `navigation.cjs`'s schema/table
  definitions at research/plan time, not a navigator-facing choice.
- Exact `data/doctor-modules.json` `id` slug and whether the doctor module
  surfaces under the existing `--acceptance` 7-point checklist or its own
  always-on flag -- note the 7-point `buildAcceptanceChecklist` in
  `scripts/doctor.cjs` is the RELEASE gate (install-state, version-of-record,
  hard-abort, no `--allow` override) and is very likely the WRONG home
  semantically; the per-class module registry (`data/doctor-modules.json`,
  cadence `always`) is almost certainly the right one instead, but confirm
  against `commands/doctor.md`'s documented flag surface before finalizing.

### Reviewed Todos (not folded)
`gsd_run query todo.match-phase 232.1` returned 5 keyword matches, none
substantively about room-graph density -- reviewed and explicitly not folded:
- `2026-07-08-f7-rescope-212-213-against-registercapability.md` (F7 rescope,
  unrelated phases)
- `2026-06-28-ignite-persona-card-under-shows-frozen-role-blend-vocabulary.md`
  (ignite persona card, unrelated)
- `2026-07-03-registry-drift-gate-prevent-silent-command-disappearance-key.md`
  (registry-drift gate, unrelated)
- `2026-07-12-never-git-stash-mid-merge-conflict-resolution-it-drops-merge.md`
  (git hygiene note, unrelated)
- `2026-07-17-ingest-skill-description-insight-to-brain.md` (Brain ingestion,
  unrelated)

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Source of this phase
- `.planning/seeds/SEED-074-local-graph-read-layer-lacks-salience-and-query-time-joins.md`
  -- the phase's entire mandate. Contains the exact "Suggested first move" text
  this phase implements (lines ~103-109) AND the "Hard guard on any downstream
  claim" section (lines ~95-101) that binds this phase's own output copy.
  SEED-074's actual gated target (PageRank/Louvain, SAG-style query-time joins)
  is explicitly OUT of this phase's scope -- do not let planning or execution
  creep into it.

### Canon
- `docs/MINDRIAN-CANON.md` Part 9 (Memory Locality) -- `room.db` is the local
  mind; `navigation.cjs` is the single chokepoint; only a human confirms a
  truth-claim node (not relevant to a read-only count, but the substrate guard
  is).
- `docs/MINDRIAN-CANON.md` Part 4 (every choice is graph data) -- SEED-074's
  own declared `canon_parts: [4, 9]`, carried forward to this phase.
- `CLAUDE.md` Canon Part 7 (Reuse Before Build) -- directly governs D-02/D-03
  above; the doctor module registry and cascade-rooms iteration pattern exist
  precisely so a new check like this needs zero engine changes.

### Doctor module system (Phase 217)
- `data/doctor-modules.json` -- the module registry; per-entry contract
  documented in its own `$schema_note` (id, introduced_version, cadence, flag,
  fix_supported, runner, description).
- `lib/core/doctor/cascade-rooms-module.cjs` -- reference implementation:
  per-registered-room iteration, `check(ctx)` contract shape, per-room
  try/catch soft-fail pattern (T-217-01).
- `lib/core/doctor/shared.cjs` -- `readRegistry()`, used by cascade-rooms and
  reusable here verbatim.
- `.planning/phases/217-doctor-cjs-architecture-rethink-audit-all-14-check-classes-i/`
  -- the phase that established this module-registry architecture; read for
  the full design rationale if the module contract is unclear.

### room.db access
- `lib/core/navigation.cjs` -- `openRoomDbForCaller` / `closeRoomDbForCaller`
  (Phase 135-01 caller-owned handle door); this is the sanctioned way for a
  diagnostic caller (not a hot-path selector) to get a live `room.db` handle
  without a direct open.

### Verification
- `CLAUDE.md` Verification section -- `node scripts/doctor.cjs --acceptance`
  is named as this repo's canonical acceptance roll-up entry point; confirm at
  plan/research time whether the new module rides the existing `--acceptance`
  7-point checklist (scripts/doctor.cjs `buildAcceptanceChecklist`, which is
  the RELEASE gate -- install-state/version-of-record/hard-abort, likely the
  WRONG semantic home) or the separate always-on module-registry path (data/doctor-modules.json,
  almost certainly the RIGHT one) -- see "Claude's Discretion" above.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `lib/core/doctor/shared.cjs` `readRegistry()` -- returns every registered
  room from `~/MindrianRooms/.rooms/registry.json` (`MINDRIAN_ROOMS_HOME`
  override honored). Directly reusable for the system-wide census.
- `lib/core/doctor/cascade-rooms-module.cjs` `resolveRoomPath(roomsHome, info)`
  -- room path resolution (absolute `path` used verbatim, else joined against
  `roomsHome`); directly reusable.
- `lib/core/navigation.cjs` `openRoomDbForCaller` / `closeRoomDbForCaller` --
  the caller-owned room.db handle door (Phase 135-01); the door this phase's
  reads must go through.

### Established Patterns
- Doctor per-class module contract: `check(ctx) -> { status, detail, ... }`,
  optional `fix(ctx)`. `fix_supported` is an explicit boolean (D-03 from Phase
  217) -- this phase's module sets it `false`.
- Per-room try/catch so one bad/corrupt room soft-fails without aborting the
  whole sweep (cascade-rooms-module.cjs's T-217-01 self-DoS guard) -- apply the
  same shape here, since a density read touches every registered room's
  `room.db`.
- `data/doctor-modules.json` is hand-maintained, one entry per module; "new
  organ module = ONE entry here, no engine code change" is the explicit design
  intent (see the file's own `$schema_note`).

### Integration Points
- New file: `lib/core/doctor/room-graph-density-module.cjs` (exact filename
  TBD at plan time), registered as one new entry in `data/doctor-modules.json`.
- `room_state` side: the existing status/get-state assembly point (to be
  located precisely at research/plan time -- likely `lib/core/room-state.cjs`
  or equivalent) gains one additional read via the same `navigation.cjs`
  caller-owned-handle door, for the single currently-bound room only.

</code_context>

<specifics>
## Specific Ideas

No UI/copy specifics beyond D-06 (no risk-adjective framing) and D-05 (0/0 is
the normal, expected case, not an error state) -- both are direct carry-forwards
from SEED-074's own text, not new navigator preferences.

</specifics>

<deferred>
## Deferred Ideas

- SEED-074's actual gated target -- PageRank/Louvain salience + clustering,
  SAG-style query-time-join fallback, any query-language/pattern-match surface.
  All of it stays gated on SEED-074 exactly as written. This phase makes the
  gate's own trigger condition observable; it does not open the gate. Any
  future phase picking up SEED-074's real target should re-measure density
  fresh (using THIS phase's own output) rather than reuse a stale snapshot.

### Reviewed Todos (not folded)
See `<decisions>` above -- 5 `todo.match-phase` hits reviewed, all unrelated
keyword-only matches, none folded.

</deferred>

---

*Phase: 232.1-room-graph-density-read-add-node-count-edge-count-per-room-d*
*Context gathered: 2026-07-25*
