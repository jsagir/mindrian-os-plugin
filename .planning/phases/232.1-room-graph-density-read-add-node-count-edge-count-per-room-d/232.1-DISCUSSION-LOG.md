# Phase 232.1: Room-Graph Density Read - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-07-25
**Phase:** 232.1-room-graph-density-read-add-node-count-edge-count-per-room-d
**Areas discussed:** Surface placement

---

## Surface placement

| Option | Description | Selected |
|--------|-------------|----------|
| Doctor module only (recommended) | New `lib/core/doctor/room-graph-density-module.cjs` registered in `data/doctor-modules.json` (Phase 217's established "one entry, no engine change" pattern), reusing `cascade-rooms-module.cjs`'s per-registered-room iteration. Surfaces under `/mos:doctor --acceptance` as a system-wide census across every registered room -- matches SEED-074's "measured room.db density crossing a real threshold" framing directly. Zero new surface area (Canon Part 7). | |
| room_state tool only | Add density to the `room_state` MCP tool / `room_state_bound`, covering only the currently-bound room. No `--acceptance` flag exists on any room_state surface today, so this is genuinely new surface area, and it can't answer the seed's system-wide gate question on its own (one room per session, not a census). | |
| Both | Doctor module does the real system-wide census (the phase's actual deliverable, closes SEED-074's gate), plus a lightweight single-room density line folded into room_state's existing status output for in-conversation visibility. More surface touched for a small phase. | ✓ |

**User's choice:** Both.
**Notes:** Pre-question research established that "room_state --acceptance" as
named in SEED-074's "Suggested first move" text does not exist as a literal
surface anywhere in the codebase (no CLI script, no MCP flag) -- the seed
author was gesturing loosely at two possible homes, not two equally-real ones.
Doctor's per-class module registry (Phase 217) is the well-precedented,
low-effort extension point; the doctor module is the phase's primary
deliverable per the navigator's choice, with the room_state line as a
secondary, low-cost addition.

---

## Claude's Discretion

- Exact node/edge counting query shape (which typed node tables count as
  "nodes", whether edge tables are reported separately or combined) --
  resolved by reading `navigation.cjs`'s schema/table definitions at
  research/plan time.
- Whether the new doctor module rides the existing `--acceptance` 7-point
  release-gate checklist (`scripts/doctor.cjs` `buildAcceptanceChecklist` --
  install-state/version-of-record/hard-abort, likely the WRONG semantic home
  for a room-graph metric) or the separate always-on module-registry path
  (`data/doctor-modules.json`, almost certainly the RIGHT one). Flagged in
  CONTEXT.md for research/plan-time confirmation, not asked of the navigator
  directly -- this is an implementation-correctness call, not a vision choice.
- Exact `data/doctor-modules.json` `id` slug and new module filename.

## Deferred Ideas

- SEED-074's actual gated target (PageRank/Louvain salience + clustering,
  SAG-style query-time-join fallback, any query-language/pattern-match
  surface) -- explicitly out of scope for this phase, stays gated on SEED-074
  exactly as written. Not raised as a live option during this discussion;
  recorded here only so future readers of this log understand why it wasn't
  on the table.
- Reviewed-but-not-folded todos (5 keyword matches from
  `todo.match-phase 232.1`, all unrelated to room-graph density): F7
  rescope, ignite persona card vocabulary, registry-drift gate, git-stash
  merge-conflict hygiene note, Brain skill-description ingestion insight.
