---
phase: 127.3
plan: 07
type: execute
wave: 4
depends_on: [127.3-00, 127.3-01, 127.3-02, 127.3-03, 127.3-04, 127.3-05, 127.3-06]
files_modified:
  - .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md (move from .planning/debug/)
  - .planning/debug/knowledge-base.md
  - CHANGELOG.md
autonomous: true
requirements:
  - JTBD-AUTO-ANCHOR-127.3-08
canon_parts:
  - Part 6 (product-as-venture / dog-fooding mandate -- the RCA goes through Mindrian's own resolution discipline, becoming a knowledge-base entry that gsd-debugger surfaces as a known pattern for the next investigator)
  - Part 7 (reuse-before-build -- the knowledge-base entry's "pattern lesson" is the canonical record that future contract-drift bugs (N=4 etc.) trigger immediate chokepoint extraction)
parallel_session_awareness: |
  Phase 121.5 (terminal-coherence-capstone) is still actively committed on `main` by a
  parallel session. CHANGELOG.md is a HIGH-COLLISION file -- 121.5 may add its own
  bullets to the same `## [Unreleased]` header. Executor MUST `git fetch origin main`
  IMMEDIATELY BEFORE editing CHANGELOG, AND must use APPEND-ONLY semantics (add Phase 127.3
  bullets to the existing Added/Fixed/Changed sections; do NOT overwrite anything else).
  If a merge conflict surfaces, take BOTH sides of the conflict (union of bullets) and
  re-emit a clean section. The other two files (.planning/debug/resolved/* and
  .planning/debug/knowledge-base.md) are NOT in the 121.5 scope-guard set.
must_haves:
  truths:
    - "The original RCA file `.planning/debug/jtbd-auto-anchor-silent-failure/jtbd-auto-anchor-silent-failure.md` is moved (via `git mv` or equivalent) to `.planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` AT THE SAME PATH STRUCTURE (the parent directory `.planning/debug/jtbd-auto-anchor-silent-failure/` is removed if it becomes empty after the move)."
    - "The moved RCA file's YAML frontmatter is updated to add: `status: resolved`, `resolved: <ISO-8601 date>`, `resolved_by: phase-127.3`, `resolution_summary: \"Chokepoint extraction (lib/core/resolve-active-room.cjs) + jtbd-update.cjs refactor + sibling sweep + room-registry create bootstrap + first-touch nudge. Ships v1.13.0-beta.34.\"`."
    - "A new `## Resolution` section is APPENDED to the end of the moved RCA file, pointing at the Phase 127.3 SUMMARY artifacts (each plan's SUMMARY.md from Plans 00-06) and naming the exact chokepoint file the resolution shipped."
    - ".planning/debug/knowledge-base.md gains a new entry following the canonical 6-line format (Date, Error patterns, Root cause, Fix, Files changed, Pattern lesson) -- the entry's Pattern lesson IS the verbatim 'registry-shape contract drift between sibling scripts produces silent-dead pipelines' lesson from PATTERNS.md."
    - "CHANGELOG.md under the current `## [Unreleased] -- v1.13.0-beta.34` (or matching in-progress header) gains Fixed / Changed bullets that name (a) the RCA path explicitly, (b) all 3 PRIMARY/SECONDARY/TERTIARY defects fixed, (c) the chokepoint helper as the Canon Part 7 extraction. Bullet HEADINGS follow the beta.32 canonical style per iteration-1 plan-check W-04: `### Fixed (JTBD auto-anchor silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34)` and `### Changed (JTBD auto-anchor silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34)` (NOT the older `### Fixed (Phase 127.3 ...):` format)."
    - "The Resolution section + knowledge-base entry + CHANGELOG bullets ALL cross-reference each other so future investigators can navigate any of the three entry points to the others."
    - "ZERO regression: every test from Plan 06 STILL passes after this plan lands (this plan is pure documentation; no code surface changes)."
    - "No em-dashes anywhere in the NEW content (CLAUDE.md hard rule). Acceptance gate per iteration-1 plan-check W-05 uses `git diff` against ADDED lines only so historical em-dashes carried forward from the RCA source file do not falsely fail the test. No new npm dependencies."
  artifacts:
    - path: ".planning/debug/resolved/jtbd-auto-anchor-silent-failure.md"
      provides: "Moved RCA file with status:resolved + Resolution section appended; the source-of-truth for the bug's complete lifecycle"
      contains: "status: resolved"
      contains_2: "resolved_by: phase-127.3"
      contains_3: "## Resolution"
    - path: ".planning/debug/knowledge-base.md"
      provides: "Appended Phase 127.3 entry that gsd-debugger surfaces as a known pattern for future contract-drift investigations"
      contains: "jtbd-auto-anchor-silent-failure"
      contains_2: "registry-shape contract drift"
      contains_3: "chokepoint"
    - path: "CHANGELOG.md"
      provides: "User-readable v1.13.0-beta.34 entry naming all 3 defects + the chokepoint extraction; headings follow beta.32 canonical style"
      contains: "Phase 127.3"
      contains_2: "lib/core/resolve-active-room.cjs"
      contains_3: "v1.13.0-beta.34"
      contains_4: "JTBD auto-anchor silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34"
  key_links:
    - from: ".planning/debug/resolved/jtbd-auto-anchor-silent-failure.md (Resolution section)"
      to: ".planning/phases/127.3-jtbd-auto-anchor-fix/127.3-{00..06}-SUMMARY.md"
      via: "explicit relative-path references in the Resolution prose"
      pattern: "127.3"
    - from: ".planning/debug/knowledge-base.md (new entry)"
      to: ".planning/debug/resolved/jtbd-auto-anchor-silent-failure.md"
      via: "Files changed line names the moved RCA path AND lib/core/resolve-active-room.cjs"
      pattern: "jtbd-auto-anchor-silent-failure"
    - from: "CHANGELOG.md (Fixed bullets under [Unreleased] -- v1.13.0-beta.34)"
      to: ".planning/debug/resolved/jtbd-auto-anchor-silent-failure.md"
      via: "RCA path explicitly cited in at least one bullet"
      pattern: ".planning/debug/resolved/jtbd-auto-anchor-silent-failure"
---

<objective>
Close the RCA loop per the canonical resolution discipline established by the prior
6 resolved debug sessions in `.planning/debug/resolved/` (per the Read of that directory
earlier: brain-ask-contract-mismatch-rename.md, brain-ask-empty-envelope.md,
brain-three-ticket-reconciliation-2026-05-23.md, brain-topk-uncapped-advisory.md,
heal-command-cwd-misfire-scaffolds-spurious-sections.md, mcp-servers-cache-missing-node-modules.md).

Three artifacts to produce:
1. **Move + annotate the RCA file** -- from `.planning/debug/jtbd-auto-anchor-silent-failure/`
   to `.planning/debug/resolved/`, adding `status: resolved` + `resolved_by: phase-127.3`
   to frontmatter and a `## Resolution` section pointing at this phase's per-plan SUMMARY
   artifacts.
2. **Append a knowledge-base entry** -- so `gsd-debugger` surfaces this pattern as a
   known-pattern hypothesis for future investigations. The pattern lesson IS the canonical
   record: "When N scripts read the same file with N different shape expectations and
   N-1 of them are correct, the failure manifests as 'this one downstream layer is
   mysteriously empty forever'. Extract a chokepoint resolver on first occurrence; the
   second occurrence is N=3."
3. **Add CHANGELOG bullets** under `## [Unreleased] -- v1.13.0-beta.34` describing all
   3 defects fixed (PRIMARY: registry-shape mismatch silent-failure; SECONDARY: room-registry
   bootstrap gap; TERTIARY+: first-touch JTBD nudge for fresh rooms) + naming the new
   chokepoint helper as the Canon Part 7 extraction. Headings follow the beta.32
   canonical schema per iteration-1 plan-check W-04: `### Fixed (JTBD auto-anchor
   silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34)` (NOT the older
   `### Fixed (Phase 127.3 JTBD Auto-Anchor Silent-Failure Bundle):` format).

This is the wrap-up plan: zero code surface; pure documentation + state-transition.
The full closed loop ships in v1.13.0-beta.34 with the RCA now resolved, the pattern
indexed, and the user-facing CHANGELOG honest about what shipped.

Purpose: dog-food the Mindrian RCA discipline on Mindrian's own bug; make the lesson
permanent. Honor the canonical CHANGELOG heading style established by Phase 127.2's
beta.32 release so the v1.13.0 endgame entries read coherently.
Output: 1 file moved + 1 file appended + 1 file edited (CHANGELOG with canonical schema).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/127.3-jtbd-auto-anchor-fix/127.3-CONTEXT.md
@.planning/phases/127.3-jtbd-auto-anchor-fix/127.3-PATTERNS.md
@.planning/phases/127.3-jtbd-auto-anchor-fix/127.3-PLAN-CHECK.md
@.planning/debug/jtbd-auto-anchor-silent-failure/jtbd-auto-anchor-silent-failure.md
@.planning/debug/knowledge-base.md
@CLAUDE.md

<interfaces>
<!-- The canonical 6-line knowledge-base entry format used by existing entries. -->

From .planning/debug/knowledge-base.md (existing entries, e.g., heal-command-cwd-misfire-scaffolds-spurious-sections at lines 16-22):
```
## <slug> -- <one-line summary>
- **Date:** <YYYY-MM-DD>
- **Error patterns:** <comma-separated keywords gsd-debugger greps for>
- **Root cause:** <one-paragraph explanation>
- **Fix:** <one-paragraph or bulleted summary of the fix shape>
- **Files changed:** <comma-separated list of files>
- **Pattern lesson:** <one-paragraph generalization for future investigators>
---
```

From .planning/debug/resolved/brain-ask-contract-mismatch-rename.md (existing resolved
entry pattern -- the frontmatter additions + Resolution section template):
```yaml
---
status: resolved
resolved: 2026-05-23
resolved_by: phase-127.2 Plan 127.2-03 (false-positive flagged from stale install cache)
... existing frontmatter ...
---

... existing RCA body ...

## Resolution

[Resolution prose pointing at the phase that closed the issue, the files changed, and
any cross-references like the knowledge-base entry.]
```

PATTERNS.md "RCA Closeout Pattern" section gives the exact 5-step protocol:
1. Move .planning/debug/jtbd-auto-anchor-silent-failure/jtbd-auto-anchor-silent-failure.md -> .planning/debug/resolved/
2. Add to YAML frontmatter: resolved_by, resolved_date, resolution_summary.
3. Add ## Resolution section at end.
4. Append entry to .planning/debug/knowledge-base.md.
5. The knowledge-base entry's "pattern lesson": "A registry-shape contract drift between sibling scripts produces silent-dead pipelines. When N scripts read the same file with N different shape expectations and N-1 of them are correct, the failure manifests as 'this one downstream layer is mysteriously empty forever'. Extract a chokepoint resolver on first occurrence; the second occurrence is N=3."

CHANGELOG.md heading schema (canonical -- iteration-1 plan-check W-04):

Phase 127.2's beta.32 entry established the convention:
```
### Fixed (Windows-tester regression bundle, Phase 127.2 Plan 04 -- ships v1.13.0-beta.32)
```

Phase 127.3 follows the same pattern:
```
### Fixed (JTBD auto-anchor silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34)
### Changed (JTBD auto-anchor silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34)
```

DO NOT use the older non-canonical form `### Fixed (Phase 127.3 JTBD Auto-Anchor Silent-Failure Bundle):`
-- iteration-1 plan-check W-04 flagged this as deviation from the beta.32 style.

CHANGELOG.md under "## [Unreleased]" -- the current in-progress header at top of file
should match `## [Unreleased] -- v1.13.0-beta.34 (in progress)` (or whatever the actual
in-progress beta number is when this plan executes -- verify before editing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move + annotate the RCA file from .planning/debug/ to .planning/debug/resolved/</name>
  <files>
    .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md (NEW path, moved from old)
  </files>
  <read_first>
    - .planning/debug/jtbd-auto-anchor-silent-failure/jtbd-auto-anchor-silent-failure.md (the original RCA file, 298 lines -- understand existing frontmatter + content structure before annotating)
    - .planning/debug/resolved/brain-ask-contract-mismatch-rename.md (canonical existing resolved-entry frontmatter shape -- pattern to mirror)
    - .planning/debug/resolved/heal-command-cwd-misfire-scaffolds-spurious-sections.md (another existing resolved-entry to confirm the Resolution-section convention)
  </read_first>
  <action>
    Run `git mv` to move the file atomically:
    `git mv .planning/debug/jtbd-auto-anchor-silent-failure/jtbd-auto-anchor-silent-failure.md .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md`

    After the move, IF the parent directory `.planning/debug/jtbd-auto-anchor-silent-failure/`
    is empty, remove it: `rmdir .planning/debug/jtbd-auto-anchor-silent-failure/ 2>/dev/null || true`.

    Edit the moved file's YAML frontmatter (between the existing `---` delimiters at the
    top of the file). The existing frontmatter has `status: diagnosed`. CHANGE this to
    `status: resolved` AND ADD these new keys (anywhere within the frontmatter block):
    - `resolved: <today's ISO-8601 date>` (the executor should compute the actual date
      at execution time; format YYYY-MM-DD)
    - `resolved_by: phase-127.3`
    - `resolution_summary: "Chokepoint extraction (lib/core/resolve-active-room.cjs) + jtbd-update.cjs refactor + intent-classifier reroute + sibling-sweep tripwire + room-registry create bootstrap + retro-bootstrap + first-touch nudge. Ships v1.13.0-beta.34."`

    APPEND a new `## Resolution` section at the END of the file (after all existing
    content). The section must contain:
    - A one-paragraph summary of what closed the bug + when (this phase + this beta).
    - A bulleted list of the 7 plans + their roles:
      - Plan 127.3-00: extracted `lib/core/resolve-active-room.cjs` chokepoint helper (Canon Part 7).
      - Plan 127.3-01: refactored `scripts/jtbd-update.cjs` (both registry walks) to use the chokepoint -- THE primary silent-failure fix.
      - Plan 127.3-02: rerouted `scripts/intent-classifier.cjs::resolveActiveRoomDir` through the chokepoint with MINDRIAN_ROOMS_ROOT -> MINDRIAN_ROOMS_HOME env-var bridging.
      - Plan 127.3-03: sibling-sweep tripwire + patched 4 sibling scripts (hmi-compliance-poll, jtbd-command, operator-command, check-onboard-statusline) to consolidate canonical-single-source; memory-completion-detector.cjs deferred to Phase 129 per iteration-1 plan-check B-01.
      - Plan 127.3-04: extended `scripts/room-registry create` to seed USER.md / STATE.md / ROOM.md / .mindrian/ idempotently (D-02 honored: NO jtbd-state.json / operator-state.json seeded); added NEW `bootstrap-missing` subcommand for retro-seeding existing rooms per iteration-1 plan-check B-02.
      - Plan 127.3-05: first-touch JTBD nudge in `scripts/memory-resume-nudge.cjs::contribute()`, priority 3, 7-day ROOM.md mtime gate per D-03 (effective on both new and retro-bootstrapped existing rooms).
      - Plan 127.3-06: empirical reproduction test + sibling-sweep + Plan 00 unit-test aggregated under `tests/run-all-127.3.sh`; Plan 00 unit-test registered in `lib/memory/run-feynman-tests.cjs`.
    - A cross-reference line pointing at the knowledge-base.md entry for future
      investigators: "Pattern indexed at `.planning/debug/knowledge-base.md` as
      `jtbd-auto-anchor-silent-failure`."
    - A cross-reference line pointing at CHANGELOG.md: "User-readable summary in
      CHANGELOG.md under `## [Unreleased] -- v1.13.0-beta.34`."

    DO NOT modify any existing content of the RCA body (Current Focus, Symptoms,
    Eliminated, Evidence, Resolution.root_cause, Proposed Fixes, Reproduction Steps, etc.)
    -- those are historical record of the diagnostic process and stay byte-identical.
    Only frontmatter mutates + new Resolution section appended.

    NO em-dashes IN NEW CONTENT (NEW = frontmatter additions + Resolution section).
    Per iteration-1 plan-check W-05, the acceptance gate uses `git diff` against ADDED
    lines so historical em-dashes carried forward from the RCA source body do NOT
    falsely fail the test. NO new dependencies.
  </action>
  <verify>
    <automated>test -f .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md && grep -q "status: resolved" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md && grep -q "resolved_by: phase-127.3" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md && grep -q "## Resolution" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md && ! test -f .planning/debug/jtbd-auto-anchor-silent-failure/jtbd-auto-anchor-silent-failure.md && ADDED_EMDASHES=$(git diff --unified=0 .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md 2>/dev/null | grep -c "^+.*$'\u2014'" || true); if [ "${ADDED_EMDASHES:-0}" -ne 0 ]; then echo "FAIL: ${ADDED_EMDASHES} em-dashes introduced in ADDED lines of the move-with-annotate (W-05 gate)"; exit 1; fi; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` EXISTS.
    - `.planning/debug/jtbd-auto-anchor-silent-failure/jtbd-auto-anchor-silent-failure.md` DOES NOT EXIST (or the file path is gone).
    - The parent directory `.planning/debug/jtbd-auto-anchor-silent-failure/` either does not exist OR is empty.
    - `grep -c "status: resolved" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` returns at least 1.
    - `grep -c "resolved_by: phase-127.3" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` returns at least 1.
    - `grep -c "resolution_summary:" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` returns at least 1.
    - `grep -c "## Resolution" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` returns at least 1.
    - The Resolution section names all 7 plans (Plan 127.3-00, -01, -02, -03, -04, -05, -06): `for p in 00 01 02 03 04 05 06; do grep -q "Plan 127.3-${p}" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md || echo "FAIL: Plan 127.3-${p} not named"; done | grep -c FAIL` returns 0.
    - Cross-references present: `grep -c "knowledge-base" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` returns at least 1 AND `grep -c "CHANGELOG" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` returns at least 1.
    - Em-dash acceptance gate per iteration-1 plan-check W-05 (added-lines-only): `git diff --unified=0 .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md | grep -c "^+.*$'\u2014'"` returns 0. This intentionally tolerates historical em-dashes in the preserved RCA body (which may have slipped in originally) but BLOCKS introducing new em-dashes in the Resolution section or frontmatter additions made by this task. The older literal `grep -c "$'\u2014'" .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` test was replaced because it failed if any historical em-dash existed even when zero NEW em-dashes were added.
    - The historical RCA body (Symptoms, Evidence, Resolution.root_cause, Proposed Fixes, Reproduction Steps) is preserved byte-identical: `wc -l` before vs after the edits should differ ONLY by the count of new Resolution-section lines + any frontmatter additions.
  </acceptance_criteria>
  <done>The RCA file is moved to the resolved/ directory with status:resolved + Resolution section pointing at this phase's deliverables; the original location no longer exists; cross-references to knowledge-base.md + CHANGELOG.md are in place; em-dash discipline enforced via the W-05 added-lines-only gate (not the older literal-grep tautology).</done>
</task>

<task type="auto">
  <name>Task 2: Append a knowledge-base.md entry for the registry-shape-contract-drift pattern</name>
  <files>.planning/debug/knowledge-base.md</files>
  <read_first>
    - .planning/debug/knowledge-base.md (full file -- understand the entry format; the 6-line bullet shape Date / Error patterns / Root cause / Fix / Files changed / Pattern lesson, separator `---`)
    - .planning/phases/127.3-jtbd-auto-anchor-fix/127.3-PATTERNS.md ("RCA Closeout Pattern" section -- the EXACT pattern-lesson text is provided verbatim)
    - .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md (the just-moved RCA -- pull root cause + fix details from here)
  </read_first>
  <action>
    Locate the END of `.planning/debug/knowledge-base.md` (just before EOF, after the
    last existing entry's closing `---`).

    APPEND a new entry following the canonical 6-line format from existing entries:

    ```markdown
    ## jtbd-auto-anchor-silent-failure -- JTBD memory layer silently dead since v1.11.x due to registry-shape contract drift in scripts/jtbd-update.cjs
    - **Date:** <YYYY-MM-DD>  (the date this plan executes; ISO-8601)
    - **Error patterns:** jtbd-state.json never created, /mos:memory empty, in_flight 0 parked 0 completed 0, jtbd-update.cjs, resolveActiveRoom, reg.active_room, Array.isArray(reg.rooms), MINDRIAN_ROOMS_HOME, registry shape mismatch, Phase 100, Phase 103-05, intent_persona all-null, decide-pursue cue, fresh-room bootstrap, USER.md missing, STATE.md missing, ROOM.md missing, contract drift, sibling scripts, chokepoint, lib/core/resolve-active-room.cjs, Canon Part 7, silent failure, Phase 127.3
    - **Root cause:** scripts/jtbd-update.cjs lines 65-79 (`resolveActiveRoom`) read against an OBSOLETE registry shape (expected `reg.active_room` + Array `reg.rooms`); the actual registry writes `reg.active` + Object `reg.rooms` since at least Phase 100 (commit fcbbcf9a, 2026-04-26). Both checks failed on every call -> function returned null -> main() short-circuited at line 132 -> classifier never ran -> jtbd-state.json never written -> /mos:memory reported 0/0/0 for ~7 months. THREE sibling scripts (intent-classifier.cjs, memory-command.cjs, hmi/across-session-memory.cjs) plus FOUR additional consumers (jtbd-command, operator-command, hmi-compliance-poll, check-onboard-statusline, memory-completion-detector) each carried their own registry-resolution code with different tolerances -- the bug existed in the load-bearing one. Secondary: room-registry create did not seed USER.md / STATE.md / ROOM.md / .mindrian/, so fresh rooms started with intent_persona = all-null in every decision trace; existing rooms predated the seeding gap and required a retro-bootstrap pass. Tertiary: memory-resume-nudge.cjs was RESUMPTION-only, no first-touch prompt for fresh rooms.
    - **Fix:** PRIMARY -- extracted `lib/core/resolve-active-room.cjs` as the Canon Part 7 chokepoint tolerating BOTH legacy (active_room + Array) and current (active + Object) shapes; refactored scripts/jtbd-update.cjs at both broken sites (lines 65-79 + lines 196-214) to import + call the chokepoint. SECONDARY -- rerouted scripts/intent-classifier.cjs::resolveActiveRoomDir through the chokepoint with MINDRIAN_ROOMS_ROOT -> MINDRIAN_ROOMS_HOME env-var bridging; refactored 4 sibling scripts (hmi-compliance-poll, jtbd-command, operator-command, check-onboard-statusline) to consolidate; structural tripwire `tests/test-127.3-sibling-sweep.sh` prevents regrowth; memory-completion-detector.cjs (multi-room iteration) deferred to Phase 129 with a TODO marker in the tripwire ALLOW comment block. TERTIARY -- extended scripts/room-registry `create` to seed USER.md / STATE.md / ROOM.md / .mindrian/ idempotently (per D-02: NOT jtbd-state.json or operator-state.json); added NEW `bootstrap-missing` subcommand + sentinel auto-trigger to retro-seed every existing room on first post-127.3 invocation. QUATERNARY -- first-touch JTBD nudge in scripts/memory-resume-nudge.cjs::contribute() at priority 3, 7-day ROOM.md mtime gate (D-03). Empirical reproduction test `tests/test-jtbd-auto-anchor-empirical.sh` reproduces the RCA Section 'Reproduction Steps' verbatim. Shipped v1.13.0-beta.34.
    - **Files changed:** lib/core/resolve-active-room.cjs (new), tests/test-resolve-active-room-canonical.cjs (new), scripts/jtbd-update.cjs, scripts/intent-classifier.cjs, scripts/hmi-compliance-poll.cjs, scripts/jtbd-command.cjs, scripts/operator-command.cjs, scripts/check-onboard-statusline.cjs, scripts/room-registry, scripts/memory-resume-nudge.cjs, tests/test-127.3-sibling-sweep.sh (new), tests/test-jtbd-auto-anchor-empirical.sh (new), tests/run-all-127.3.sh (new), lib/memory/run-feynman-tests.cjs, .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md (moved), CHANGELOG.md. (Deferred to Phase 129: scripts/memory-completion-detector.cjs absorption onto a future iterateRegisteredRooms chokepoint helper.)
    - **Pattern lesson:** A registry-shape contract drift between sibling scripts produces silent-dead pipelines. When N scripts read the same file with N different shape expectations and N-1 of them are correct, the failure manifests as 'this one downstream layer is mysteriously empty forever'. Extract a chokepoint resolver on first occurrence; the second occurrence is N=3. Phase 129's spine-repair work absorbs the remaining 6 spine scripts (mos-status, suggest-next, act, pipeline, operator, memory) plus the deferred multi-room iteration site (memory-completion-detector.cjs) onto the same chokepoint -- 127.3 lands the infrastructure 129 reuses without double-work.
    ---
    ```

    The new entry follows the EXACT format of existing entries (e.g., the
    heal-command-cwd-misfire-scaffolds-spurious-sections entry at lines 16-22).
    The trailing `---` separator is mandatory.

    CRITICAL: this is APPEND-ONLY. DO NOT modify any existing entries.

    NO em-dashes anywhere in the new content -- the pattern-lesson text in PATTERNS.md
    uses hyphens already; preserve verbatim. Em-dash acceptance gate uses `git diff`
    against ADDED lines per iteration-1 plan-check W-05.

    NO new dependencies.
  </action>
  <verify>
    <automated>grep -q "jtbd-auto-anchor-silent-failure" .planning/debug/knowledge-base.md && grep -q "registry-shape contract drift" .planning/debug/knowledge-base.md && grep -q "lib/core/resolve-active-room.cjs" .planning/debug/knowledge-base.md && ADDED_EMDASHES=$(git diff --unified=0 .planning/debug/knowledge-base.md 2>/dev/null | grep -c "^+.*$'\u2014'" || true); if [ "${ADDED_EMDASHES:-0}" -ne 0 ]; then echo "FAIL: ${ADDED_EMDASHES} em-dashes introduced in ADDED lines of knowledge-base.md (W-05 gate)"; exit 1; fi; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "jtbd-auto-anchor-silent-failure" .planning/debug/knowledge-base.md` returns at least 1.
    - `grep -c "registry-shape contract drift" .planning/debug/knowledge-base.md` returns at least 1 (the pattern-lesson key phrase).
    - `grep -c "lib/core/resolve-active-room.cjs" .planning/debug/knowledge-base.md` returns at least 1 (the chokepoint cross-reference).
    - `grep -c "Phase 127.3" .planning/debug/knowledge-base.md` returns at least 1.
    - The new entry follows the 6-line bullet format: `grep -A 7 "jtbd-auto-anchor-silent-failure" .planning/debug/knowledge-base.md | grep -cE "(Date|Error patterns|Root cause|Fix|Files changed|Pattern lesson):" >= 6`.
    - Existing entries preserved byte-identical (no entries deleted; verify by `grep -c "^## " .planning/debug/knowledge-base.md` is N+1 where N is the pre-edit count).
    - Em-dash acceptance gate per iteration-1 plan-check W-05 (added-lines-only): `git diff --unified=0 .planning/debug/knowledge-base.md | grep -c "^+.*$'\u2014'"` returns 0.
  </acceptance_criteria>
  <done>The knowledge-base.md has a new entry that gsd-debugger surfaces as the canonical "registry-shape contract drift" pattern for future investigators; cross-referenced to lib/core/resolve-active-room.cjs and Phase 127.3; em-dash discipline enforced via the W-05 added-lines-only gate.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 127.3 bullets to CHANGELOG.md under [Unreleased] v1.13.0-beta.34 with canonical W-04 heading schema</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (top 60 lines -- understand the current in-progress header (verify it reads `## [Unreleased] -- v1.13.0-beta.34 (in progress)` or similar; if it reads `beta.32` or `beta.34`, use whatever in-progress header is currently at the top), the existing section structure Added/Fixed/Changed, the style of existing release entries; especially Phase 127.2's beta.32 entry to confirm the W-04 canonical heading style `### Fixed (Windows-tester regression bundle, Phase 127.2 Plan 04 -- ships v1.13.0-beta.32)`)
    - .planning/phases/127.3-jtbd-auto-anchor-fix/127.3-CONTEXT.md (beta_target: v1.13.0-beta.34 -- confirm the target)
    - .planning/phases/127.3-jtbd-auto-anchor-fix/127.3-PLAN-CHECK.md W-04 (the iteration-1 warning that established the canonical heading schema deviation)
    - .planning/debug/resolved/jtbd-auto-anchor-silent-failure.md (the moved RCA -- pull RCA path for the bullet citation)
  </read_first>
  <action>
    PRE-FLIGHT (mandatory for CHANGELOG -- HIGH-COLLISION file):
    - Run `git fetch origin main`.
    - Run `git diff origin/main..HEAD -- CHANGELOG.md`.
    - If there are local edits to CHANGELOG ahead of remote, NOTE them in the summary.
    - If remote has new CHANGELOG bullets in the same `[Unreleased]` block, ensure the
      Phase 127.3 bullets are APPENDED after any 121.5 / other-phase additions, NOT
      overwriting them. If conflict surfaces during commit, take the UNION of both sets
      of bullets.

    Locate the in-progress `## [Unreleased] -- v1.13.0-beta.34` header (or whatever the
    current in-progress version reads). Under that header, ADD the following subsections.

    HEADING SCHEMA (iteration-1 plan-check W-04 -- canonical form, mirrors the beta.32
    convention `### Fixed (Windows-tester regression bundle, Phase 127.2 Plan 04 -- ships v1.13.0-beta.32)`):

    ```
    ### Fixed (JTBD auto-anchor silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34)
    - JTBD memory layer was silently dead in every release since v1.11.x (commit fcbbcf9a, 2026-04-26) because `scripts/jtbd-update.cjs::resolveActiveRoom()` at lines 65-79 read against an obsolete registry shape (expected `reg.active_room` + Array `reg.rooms`; actual `reg.active` + Object `reg.rooms`). Refactored both broken registry walks in jtbd-update.cjs (lines 65-79 + lines 196-214) to use the new `lib/core/resolve-active-room.cjs` chokepoint. `/mos:memory` now populates with real in_flight entries on the 3rd same-cue turn instead of reporting `0/0/0` forever. Full RCA: `.planning/debug/resolved/jtbd-auto-anchor-silent-failure.md`.
    - Fresh-room creation now seeds USER.md (canonical_role: null), STATE.md (empty Decisions section), ROOM.md (per Canon decision 15), and `.mindrian/` directory. Previously every fresh room had `intent_persona = all-null` in every decision trace because intent-classifier.cjs had no USER.md to read from. Per D-02, jtbd-state.json and operator-state.json are explicitly NOT seeded so the legitimate first-write events stay in audit logs.
    - Existing rooms (Jonathan's MindrianRooms canonical rooms + every tester's rooms + every CI fixture) are retro-seeded with USER.md / STATE.md / ROOM.md / .mindrian/ via the new `bash scripts/room-registry bootstrap-missing` subcommand, auto-triggered once on first post-127.3 invocation via a `~/MindrianRooms/.rooms/.bootstrap-127.3-done` sentinel. This closes the silent-failure gap for the ACTUAL population, not just rooms created after this release.
    - First-touch JTBD nudge for fresh-room users: on SessionStart, when an active room exists AND `.mindrian/jtbd-state.json` is absent AND ROOM.md mtime is < 7 days, Larry asks "What are you trying to do here?" with a `/mos:jtbd set <id>` hint. Past 7 days the nudge stays silent (deliberate-blank-room respected). Effective on both newly created rooms AND retro-bootstrapped existing rooms (where ROOM.md mtime reflects the moment the retro-pass stamped the room's identity).
    ```

    ```
    ### Changed (JTBD auto-anchor silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34)
    - Extracted `lib/core/resolve-active-room.cjs` as the chokepoint for active-room registry resolution, mirroring the Canon Part 7 precedent set by `lib/core/resolve-brain-key.cjs` (Phase 123 Plan 07). Tolerates both legacy (`reg.active_room` + Array `reg.rooms`) AND current (`reg.active` + Object `reg.rooms`) shapes; returns `{ slug, abs_path }` or null. Phase 129 (spine-repair-memory-event) will absorb the remaining 6 spine scripts (mos-status, suggest-next, act, pipeline, operator, memory) plus the deferred multi-room iteration site (memory-completion-detector.cjs) onto the same helper in v1.13.1-beta.3 -- 127.3 lands the infrastructure 129 reuses without double-work.
    - Consolidated 6 sibling registry-resolution sites (`scripts/jtbd-update.cjs` x 2 sites, `scripts/intent-classifier.cjs`, `scripts/hmi-compliance-poll.cjs`, `scripts/jtbd-command.cjs`, `scripts/operator-command.cjs`, `scripts/check-onboard-statusline.cjs`) onto the chokepoint. Structural tripwire `tests/test-127.3-sibling-sweep.sh` prevents the broken pattern from regrowing -- any future PR that introduces `reg.active_room` or `Array.isArray(reg.rooms)` outside the two-file allow-list (chokepoint + the Phase-129-deferred memory-completion-detector.cjs with a `# TODO Phase 129` marker) fails CI immediately.
    - Empirical regression test `tests/test-jtbd-auto-anchor-empirical.sh` reproduces the RCA "Reproduction Steps" protocol verbatim and serves as the behavioral gate. Phase 127.3 aggregator `tests/run-all-127.3.sh` runs all 3 Phase 127.3 test suites (chokepoint unit-test + sibling-sweep tripwire + empirical reproduction); `lib/memory/run-feynman-tests.cjs` registers the chokepoint unit-test so every Feynman pass picks it up.
    ```

    ```
    ### USER-FACING NOTE (Phase 127.3)
    If you previously ran a Larry session in a fresh room and `/mos:memory` reported `in_flight: 0` even after multiple JTBD-relevant turns, this fix restores the auto-anchor mechanism. After upgrading, the FIRST time you run any `room-registry` subcommand (or open any room), the system retro-seeds USER.md / STATE.md / ROOM.md / .mindrian/ on every existing room AND fires the first-touch nudge for rooms without a declared JTBD. No manual migration needed.
    ```

    If a `### Fixed` or `### Changed` subsection with the canonical 127.3 heading
    already exists in beta.33 (unlikely but possible if this plan ran partially before),
    APPEND new bullets to it; do NOT delete prior content.

    Verify CHANGELOG.md style consistency with existing release entries. Look at the
    v1.13.0-beta.32 entry for the canonical reference (e.g., `### Fixed (Windows-tester regression bundle, Phase 127.2 Plan 04 -- ships v1.13.0-beta.32)`).

    NO em-dashes anywhere in the NEW bullets. NO new dependencies.
  </action>
  <verify>
    <automated>grep -q "Phase 127.3" CHANGELOG.md && grep -q "lib/core/resolve-active-room.cjs" CHANGELOG.md && grep -q "v1.13.0-beta.34" CHANGELOG.md && grep -q ".planning/debug/resolved/jtbd-auto-anchor-silent-failure.md" CHANGELOG.md && grep -qE "### Fixed \(JTBD auto-anchor silent-failure bundle, Phase 127\.3 -- ships v1\.13\.0-beta\.33\)" CHANGELOG.md && grep -qE "### Changed \(JTBD auto-anchor silent-failure bundle, Phase 127\.3 -- ships v1\.13\.0-beta\.33\)" CHANGELOG.md && ADDED_EMDASHES=$(git diff --unified=0 CHANGELOG.md 2>/dev/null | grep -c "^+.*$'\u2014'" || true); if [ "${ADDED_EMDASHES:-0}" -ne 0 ]; then echo "FAIL: ${ADDED_EMDASHES} em-dashes introduced in ADDED lines of CHANGELOG.md (W-05 gate)"; exit 1; fi; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 127.3" CHANGELOG.md` returns at least 1 (the bundle is named).
    - `grep -c "lib/core/resolve-active-room.cjs" CHANGELOG.md` returns at least 1 (the chokepoint is named as the Canon Part 7 extraction).
    - `grep -c "v1.13.0-beta.34" CHANGELOG.md` returns at least 1 (the beta target).
    - `grep -c "jtbd-auto-anchor-silent-failure" CHANGELOG.md` returns at least 1 (the RCA path is cited).
    - `grep -c "first-touch" CHANGELOG.md` returns at least 1 (Plan 05's user-facing surface named).
    - `grep -c "bootstrap-missing" CHANGELOG.md` returns at least 1 (Plan 04's retro-bootstrap user-facing surface named).
    - `grep -c "USER-FACING NOTE" CHANGELOG.md` returns at least 1 (the migration-friendly note is present).
    - Canonical heading schema per iteration-1 plan-check W-04 (BOTH headings present):
      - `grep -qE "### Fixed \(JTBD auto-anchor silent-failure bundle, Phase 127\.3 -- ships v1\.13\.0-beta\.33\)" CHANGELOG.md` exits 0.
      - `grep -qE "### Changed \(JTBD auto-anchor silent-failure bundle, Phase 127\.3 -- ships v1\.13\.0-beta\.33\)" CHANGELOG.md` exits 0.
    - The older non-canonical form is NOT introduced: `grep -cE "### (Fixed|Changed) \(Phase 127\.3 [JjAa][^,]*[Bb]undle\):" CHANGELOG.md` returns 0.
    - Em-dash acceptance gate per iteration-1 plan-check W-05 (added-lines-only): `git diff --unified=0 CHANGELOG.md | grep -c "^+.*$'\u2014'"` returns 0.
    - Existing CHANGELOG content preserved byte-identical (no entries deleted; verify by `grep -c "^## \\[" CHANGELOG.md` is unchanged from pre-edit count).
    - Pre-flight git-fetch performed (documented in commit message OR summary).
  </acceptance_criteria>
  <done>CHANGELOG.md has user-readable bullets under [Unreleased] -- v1.13.0-beta.34 describing all 3 defects fixed + naming the chokepoint as the Canon Part 7 extraction + citing the RCA path for archival traceability; heading schema matches the W-04 canonical convention `### Fixed (... bundle, Phase 127.3 -- ships v1.13.0-beta.34)`; em-dash discipline enforced via the W-05 added-lines-only gate.</done>
</task>

</tasks>

<verification>
- `.planning/debug/resolved/jtbd-auto-anchor-silent-failure.md` exists with status:resolved + resolved_by:phase-127.3 + ## Resolution section pointing at all 7 plans.
- `.planning/debug/jtbd-auto-anchor-silent-failure/` directory either no longer exists OR is empty (the move was atomic).
- `.planning/debug/knowledge-base.md` has a new entry for the jtbd-auto-anchor-silent-failure pattern with the canonical 6-line format AND the "registry-shape contract drift" pattern lesson.
- `CHANGELOG.md` has Phase 127.3 bullets under the in-progress beta.33 header with the W-04 canonical heading schema `### Fixed (JTBD auto-anchor silent-failure bundle, Phase 127.3 -- ships v1.13.0-beta.34)` (NOT the older form); USER-FACING NOTE present; RCA path cited; chokepoint named; bootstrap-missing + first-touch surfaces called out.
- ZERO regression: re-running `bash tests/run-all-127.3.sh` after this plan lands still returns "3/3 green" (this plan is pure documentation; no code changes).
- ZERO new em-dashes introduced in ADDED lines (W-05 gate applied across all three edited files via `git diff --unified=0 <file> | grep -c "^+.*$'\u2014'"` returns 0 for each).
- ZERO edits to forbidden Phase 121.5 surfaces.
- Pre-flight git-fetch run before CHANGELOG edit; any 121.5 CHANGELOG additions preserved via UNION semantics if conflict surfaced.
</verification>

<success_criteria>
The full Phase 127.3 release is complete. The RCA is resolved (file moved, frontmatter
annotated, Resolution section pointing at the per-plan SUMMARYs). The pattern is indexed
in the knowledge-base so gsd-debugger surfaces it as a known pattern for the next
investigator. The user-facing CHANGELOG honestly names all 3 defects + the chokepoint
extraction + the user-facing migration note, with HEADINGS in the canonical beta.32-style
W-04 schema so the v1.13.0 endgame entries read coherently. Em-dash discipline is
enforced via the W-05 added-lines-only gate so historical em-dashes carried forward
from the RCA source body do not falsely fail the test. The full closed loop ships in
v1.13.0-beta.34: 7-month silent-failure closed, structural regression fence in place,
retro-bootstrap closes the gap for the actual existing-rooms population, Phase 129 ready
to absorb the remaining 6 spine scripts + the deferred multi-room iteration site onto
the same helper without double-work.

The Mindrian RCA discipline is dog-fooded on Mindrian's own bug. Per Canon Part 6
(Product-as-Venture), the plugin honors its own canon: when a bug shipped silently for
~7 months, the closure leaves a permanent indexed lesson + a CI regression fence + a
honest user-facing CHANGELOG entry with canonical heading style. That is the contract
Mindrian sells to its users; that is the contract Mindrian honors for itself.
</success_criteria>

<output>
Create `.planning/phases/127.3-jtbd-auto-anchor-fix/127.3-07-SUMMARY.md` per the GSD
template when done. Document: the 3 documentation surfaces produced, the cross-reference
graph (RCA <-> knowledge-base <-> CHANGELOG triangulation), the canonical W-04 heading
schema applied to both `### Fixed` and `### Changed` blocks, the W-05 added-lines-only
em-dash gate applied across all three files, the pre-flight git-fetch result + any
121.5-CHANGELOG-collision resolution, the absence of em-dashes / new deps /
121.5-surface edits, and the commit SHA(s). This SUMMARY closes Phase 127.3; the next
operator action is `bash scripts/release.sh --prerelease --allow-ahead` to cut
v1.13.0-beta.34 per the release ceremony in `.claude/includes/release-process.md`.
</output>
</content>
</invoke>