---
phase: 215-eureka-portfolio-fusion
plan: 04
type: execute
wave: 2
depends_on: [215-01, 215-02, 215-03]
files_modified:
  - scripts/eureka-portfolio-report.cjs
  - scripts/eureka-room-report.cjs
  - tests/test-215-portfolio-report.cjs
  - tests/run-all-215.sh
autonomous: true
requirements: [215-R2, 215-R3, 215-R4, 215-R5]
must_haves:
  truths:
    - "ONE command turns the room + idea-graph into a ranked, AHP-weighted, provenance-stamped portfolio report with a DISTINCT tail-quadrant section - composition of shipped engines, zero new engine (Canon Part 7)"
    - "The runner never scores stale vectors: idx.embedded === true gates every scoring path (the 211 a1e13182 fix class)"
    - "Counts (nodes/edges/pairs) are read from the graph meta + the run itself at run time - never a hardcoded 481/1362/2117 literal (the graph regenerates; 96d0ce44 already changed the counts once)"
    - "run-all-211.sh stays green after the additive export change to the 211 runner (no dependency regression)"
  artifacts:
    - path: "scripts/eureka-portfolio-report.cjs"
      provides: "the portfolio batch runner: --pairs graph|full, AHP compose, tail section, statements, md + json sibling"
      min_lines: 200
    - path: "tests/test-215-portfolio-report.cjs"
      provides: "offline end-to-end structural proof on a tmp fixture room"
      min_lines: 70
    - path: "tests/run-all-215.sh"
      provides: "the phase aggregator (PASS/FAIL/SKIP, run_if guards, offline preload)"
      min_lines: 40
  key_links:
    - from: "scripts/eureka-portfolio-report.cjs"
      to: "scripts/eureka-room-report.cjs"
      via: "requires buildRootDomainMap + loadIndexVectors + stubEncode (additive export)"
      pattern: "eureka-room-report"
    - from: "scripts/eureka-portfolio-report.cjs"
      to: "lib/core/rs-differential-scorer.cjs"
      via: "scoreMeasured(textA, textB, {vectors}) full-matrix reuse path"
      pattern: "scoreMeasured"
    - from: "scripts/eureka-portfolio-report.cjs"
      to: "lib/core/eureka/ahp-weights.cjs + portfolio-dimensions.cjs + tail-quadrant.cjs + opportunity-statement.cjs"
      via: "the Wave-1 modules composed into the pipeline"
      pattern: "ahp-weights"
---

<objective>
Wire the Wave-1 modules onto the shipped 211 pipeline: scripts/eureka-portfolio-report.cjs,
the single-process batch runner that indexes the room, scores pairs with the reused RS
path, composes the AHP 3-dim score, flags the tail quadrant, emits critic-gated
Opportunity Statements, and writes a markdown report + JSON sibling.

Locked decisions honored: reuse FUSION/RS/harness/211-runner, mint no new engine
(SEED-048 line 20; the runner is COMPOSITION - every scoring primitive already ships).
Tail is a distinct output section, not a sort key. Statements in the canonical shape.

** FLAGGED - Decision Gate DG-2 (215-RESEARCH.md Open Q2, navigator item): full-2117 vs
the lens3 idea-graph subset for the ranking run. This plan encodes the RESEARCHED
RECOMMENDATION as two explicit run modes: --pairs graph (default; the pre-built
evals/eureka/jhtv-idea-graph.json CONVERGES pairs = the fast ranked loop over the cited
substrate) and --pairs full (the 211-style cross-boundary enumeration over ALL room
nodes = the acceptance mode Plan 05 runs against the 2117-tech room, because the
full catalog may hold tail gems the lens3-paired subset omits - 215-RESEARCH.md A3).
The navigator sees both modes' provenance at the Plan 05 checkpoint and can re-point the
default afterward; nothing here forecloses the fork. **

Discretion decision (215-RESEARCH.md A4, documented): single-process runner, NOT harness
fan-out. The 211 runner already proved full-matrix single-process at room scale; fan-out
(chain-executor runChain, 201) stays the reserved path for per-tech DEEP analysis a
navigator elects after triage - the report's next_steps route there (recommend, never
trigger). No runChain call in this batch path.

Purpose: 215-R5 - the one command a portfolio operator (Oliver's 2117-tech room) runs.
Output: the runner + offline structural test + the phase aggregator.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-eureka-portfolio-scale-fusion-eureka-portfolio-fusion/215-RESEARCH.md
@scripts/eureka-room-report.cjs
@tests/run-all-211.sh
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: additive exports on the 211 runner (zero behavior change)</name>
  <files>scripts/eureka-room-report.cjs</files>
  <read_first>
    - scripts/eureka-room-report.cjs lines 145-168 (loadIndexVectors + truncate) and line 419 (module.exports)
  </read_first>
  <action>
    Add loadIndexVectors and truncate to the existing module.exports object of
    scripts/eureka-room-report.cjs. NOTHING else changes - no logic edits, no signature
    changes, no reordering. This is the Canon Part 7 move that lets the portfolio runner
    share the backend-aware vector read (eureka_vec vs eureka_vec_fallback) instead of
    duplicating it. The 211 aggregator must stay green - run it before and after.
  </action>
  <verify>
    <automated>bash tests/run-all-211.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-211.sh` reports FAIL=0 (SKIPs allowed for env-gated legs) and exits 0
    - `node -e "const r=require('./scripts/eureka-room-report.cjs'); if(typeof r.loadIndexVectors!=='function'||typeof r.truncate!=='function') process.exit(1)"` exits 0
    - `git diff --stat scripts/eureka-room-report.cjs` touches exactly 1 file with <= 4 changed lines
  </acceptance_criteria>
  <done>Two helpers exported additively; run-all-211 green; diff minimal.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/eureka-portfolio-report.cjs - the composed batch runner</name>
  <files>scripts/eureka-portfolio-report.cjs</files>
  <read_first>
    - scripts/eureka-room-report.cjs (WHOLE file - the model: argv router, openRoomDb try/finally, indexNodes, idx.embedded gate, figure-guard skip+count, renderReport, Part 8/9 banner)
    - evals/eureka/jhtv-idea-graph.json meta block + 1 node + 1 CONVERGES edge (field contract; note meta.honest_nouns is prose to quote verbatim in provenance, never parsed into asserted counts)
    - the three Wave-1 interface_contract blocks (215-01/02/03-PLAN.md)
  </read_first>
  <action>
    Create scripts/eureka-portfolio-report.cjs modeled clause-for-clause on the 211 runner
    (same BSL header, same Canon Part 8 ZERO-network banner, same Part 9 derived-tables-
    only banner, same argv switch-case router, same try/finally openRoomDb/closeRoomDb).

    Flags: --db <roomDir> (default room), --graph <path> (default
    evals/eureka/jhtv-idea-graph.json), --pairs graph|full (default graph; DG-2 modes),
    --offline (stub encoder via the imported stubEncode), --top <n> (default 25),
    --out <path> (default evals/eureka/215-portfolio-report.md), --json <path> (default
    evals/eureka/215-portfolio-report.json).

    Pipeline (compose, do not re-derive):
    1. Load the graph JSON; build techMap id -> { cnumber, title, primary_tier,
       pair_count, degree, section, primary_problem, problems } from elements.nodes;
       collect CONVERGES edges (dedupe on unordered id pair) with shared_problems.
       Read cohort counts at RUN TIME from what loaded; quote meta.honest_nouns verbatim
       in provenance. NEVER hardcode node/edge counts.
    2. openRoomDb(roomDir, { allowExtension: true }); triModal.indexNodes(db, { encodeFn:
       offline ? stubEncode : undefined, roomDir }); HARD GATE: proceed to scoring only
       when idx.embedded === true, else render the honest encoder-unavailable report
       (mirror the 211 runner's block verbatim in spirit) and still exit 0.
    3. Vectors via the newly exported loadIndexVectors(db, idx.vec_backend); node text via
       triModal.nodeText(row, { roomDir }).
    4. Pair set: mode graph -> the deduped CONVERGES pairs whose BOTH ids exist in the
       room index; mode full -> the 211 cross-boundary enumeration (differ in root-domain
       via imported buildRootDomainMap OR differ in type). Progress log to stderr every
       100000 scored pairs in full mode (the 2117 room is ~2.2M pairs; silence reads as a
       hang).
    5. Score each pair: scoreMeasured(textA, textB, { vectors }) inside try/catch -
       ExternalEgressViolation (the Part 8 K/M/B figure-guard) skips + counts, never
       aborts (the 211 posture).
    6. Dimensions: scoreTechDimensions per side over the loaded cohort, scorePairDimensions
       with the rs fields; weights from loadAhpConfig() (per-run reload; an
       AHP_INCONSISTENT throw aborts the run LOUDLY with exit 1 - a bad matrix must never
       produce a silently mis-weighted report); score = composeScore(dims, weights).
    7. Tail: per-TECH axes - attention = percentileRank(pair_count over cohort), growth =
       percentileRank(numeric cnumber over cohort) (strip the leading 'C', parseInt);
       classifyTail(items) with defaults; thread opts.brokerage ONLY if a future flag
       --brokerage <json> is supplied (accept a JSON file of id -> [0,1]; the DG-1 seam
       surfaced at the CLI so the 212.5 output can plug in without a code change).
    8. Rank pairs by score descending; keep --top. Candidates for statements: the top-N
       PLUS every pair where at least one side is tail-flagged AND complementary(dimsA,
       dimsB) holds (the gem candidates - they ride even when outranked; that is the
       whole point of the tail category). buildOpportunityStatement per candidate with
       rank, tail flag, weak_dimensions.
    9. Render markdown report: provenance table (run mode, pairs mode, encoder model/dtype
       via spine.encoderProvenance, vec backend, AHP weights + CR + matrix source path,
       tail composition + growth_proxy + thresholds, cohort counts read at run time,
       honest_nouns quote, figure-guard skip count, run date); '## Ranked top N' table
       (rank, A id/title, B id/title, score, the 3 dims, weak dims, banked/critic state);
       '## Tail quadrant (weak signals)' as its OWN section (tech-level tail list +
       the tail-driven candidate pairs) - when insufficient_structure or suspect_noise is
       stamped, print that verdict prominently INSTEAD of dressing up noise; '## Opportunity
       Statements' (each statement text + banked/critic state - 'pending' statements
       explicitly labeled 'NOT YET BANKED (critic pending)'); mandatory caveat block
       (necessary-not-sufficient, uncalibrated bands, growth is a recency proxy, real-room
       content never reaches a network judge - human spot-check verifies).
    10. Write the JSON sibling: { provenance, ranked, tail, statements } with full
        per-candidate fields (Plan 05's reproduction test reads THIS file). Same
        deterministic serializer style as the house pattern (JSON.stringify null 2 +
        trailing newline).

    Constraints restated as code comments where they bite: no runFusion in the batch loop
    (A4 decision, see portfolio-dimensions header); no navigation.cjs writes (report
    files + derived eureka_* tables only, Part 9 - graph-node banking of statements is a
    later phase's wiring, not minted here un-governed); zero network; no em-dashes; no
    new npm dependency; CLI surface is a dev script like the 211 runner, NOT a new
    commands/*.md invocable surface (Part 11: no new surface class minted, so no
    hitl_shape declaration is due; the phase's declared HITL is the Plan 05
    checkpoint:human-verify leg, same shape as 211-05).
  </action>
  <verify>
    <automated>node scripts/eureka-portfolio-report.cjs --help && node -e "const m=require('./scripts/eureka-portfolio-report.cjs'); if(typeof m.main!=='function'||typeof m.parseArgv!=='function') process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/eureka-portfolio-report.cjs --help` exits 0 and stdout contains '--pairs'
    - `grep -c "idx.embedded === true" scripts/eureka-portfolio-report.cjs` >= 1 (stale-vector gate)
    - `grep -c "scoreMeasured" scripts/eureka-portfolio-report.cjs` >= 1 && `grep -c "loadAhpConfig" scripts/eureka-portfolio-report.cjs` >= 1 && `grep -c "classifyTail" scripts/eureka-portfolio-report.cjs` >= 1 && `grep -c "buildOpportunityStatement" scripts/eureka-portfolio-report.cjs` >= 1 (all four layers composed)
    - `grep -c "Tail quadrant" scripts/eureka-portfolio-report.cjs` >= 1 (distinct section, not a sort key)
    - `grep -c "runFusion\|runChain" scripts/eureka-portfolio-report.cjs` == 0 (A4/posture held)
    - `grep -cE "\b(481|1362|1629)\b" scripts/eureka-portfolio-report.cjs` == 0 (no hardcoded substrate counts)
    - `grep -c "http" scripts/eureka-portfolio-report.cjs | head -1` == 0 (zero network, Part 8)
  </acceptance_criteria>
  <done>Runner composes all four Wave-1 layers on the reused 211 spine; both DG-2 pair modes implemented; DG-1 brokerage pluggable at the CLI; honest degrades everywhere (encoder, AHP, tail).</done>
</task>

<task type="auto">
  <name>Task 3: offline structural e2e + tests/run-all-215.sh aggregator</name>
  <files>tests/test-215-portfolio-report.cjs, tests/run-all-215.sh</files>
  <read_first>
    - tests/test-211-tri-modal.cjs lines 40-60 (the tmp-room fixture pattern: mkdtempSync + openRoomDb + INSERT INTO nodes(id, type, properties))
    - tests/run-all-211.sh (WHOLE file - the aggregator to model: offline preload NODE_OPTIONS, run/run_if, PASS/FAIL/SKIP summary, nonzero exit on FAIL)
  </read_first>
  <action>
    Write tests/test-215-portfolio-report.cjs: build a tmp fixture room (mkdtempSync +
    openRoomDb + insert ~36 claim nodes with distinct multi-sentence properties.text
    across 2 synthetic root domains - 36 > MIN_COHORT so the tail path classifies; give
    ids C00001..C00036 so the cnumber recency axis is exercised) and a tmp fixture graph
    JSON (nodes with cnumber/primary_tier/pair_count/degree/section/primary_problem +
    a handful of CONVERGES edges including at least one pair constructed to be
    complementary-weak). Invoke the runner via its exported main() with --offline,
    --pairs graph, tmp --out/--json paths. Assert: exit code 0; report file exists and
    contains the literal headings '## Ranked top' and '## Tail quadrant (weak signals)'
    and '## Opportunity Statements'; provenance contains 'CR' and
    'attention-growth-only' and 'cnumber-recency'; every statement block contains
    'NOT YET BANKED (critic pending)' OR a real critic verdict label (fixture tree has no
    critic module, so expect the pending label); the JSON sibling parses and
    ranked.length >= 1. Then a second main() invocation with --pairs full on the same tmp
    room asserts exit 0 (the full-enumeration mode works offline at fixture scale).
    Also assert the OFFLINE caveat text is present (stub semantics are not
    embedding-quality evidence - the 211 honesty rule carried forward).

    Write tests/run-all-215.sh modeled on run-all-211.sh: same set -uo pipefail, same
    NODE_OPTIONS offline preload (tests/eureka-offline-preload.cjs), run/run_if helpers,
    PASS/FAIL/SKIP counters, exit 1 on any FAIL. Legs in order:
    (1) run test-215-ahp-weights.cjs, (2) run test-215-score.cjs, (3) run
    test-215-tail.cjs, (4) run test-215-opp-statement.cjs, (5) run
    test-215-portfolio-report.cjs, (6) run_if guarded on
    evals/eureka/215-jhtv-portfolio-report.json -> node tests/test-215-reproduction.cjs
    (the Plan 05 acceptance leg SKIPs cleanly until the real run lands), (7) run_if
    guarded on tests/run-all-211.sh -> bash tests/run-all-211.sh (dependency
    no-regression). Header comment: the phase gate has two legs - this aggregator green
    AND the Plan 05 navigator spot-check verdict recorded (the 211-05 pattern).
  </action>
  <verify>
    <automated>bash tests/run-all-215.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-215.sh` exits 0 with FAIL=0 (leg 6 SKIPs: the real-room JSON does not exist yet)
    - `bash tests/run-all-215.sh 2>&1 | grep -c "SKIPPED"` >= 1 (the reproduction leg is guarded, not missing)
    - `node tests/test-215-portfolio-report.cjs` exits 0 and stdout contains 'assertions passed'
    - `grep -c "run_if" tests/run-all-215.sh` >= 2
    - `grep -c "eureka-offline-preload" tests/run-all-215.sh` == 1 (zero-network guard inherited)
  </acceptance_criteria>
  <done>Offline e2e proves the whole composed pipeline structurally (both pair modes) on a hermetic tmp room; aggregator green with the acceptance leg cleanly guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db + graph JSON -> report | real portfolio content flows through scoring into a durable report |
| derived eureka_* tables across runs | stale stub vectors can masquerade as LIVE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-09 | Tampering / integrity | vector reuse | mitigate | idx.embedded === true hard gate before any scoring (the 211 a1e13182 fix class); encoder-unavailable renders the honest empty report |
| T-215-10 | Information disclosure | scoreMeasured inputs | mitigate | the existing Part 8 K/M/B figure-guard throws ExternalEgressViolation; runner skips + counts, and the skip count prints in provenance |
| T-215-11 | Information disclosure | runner egress | mitigate | zero network by construction (no URL, no socket, grep-gated); Plurai/network judges never see real-room content - the human spot-check verifies (211 EGRESS RULE carried forward) |
| T-215-12 | Repudiation | report provenance | mitigate | weights, CR, matrix path, pair mode, composition, growth proxy, encoder, skip counts all stamped per run |
| T-215-SC | Tampering | npm/pip/cargo installs | accept | nothing installed in this plan; composition of in-repo modules only |
</threat_model>

<verification>
- bash tests/run-all-215.sh exits 0 with FAIL=0
- bash tests/run-all-211.sh exits 0 with FAIL=0 (dependency unregressed)
- no em-dashes in any new/modified file
</verification>

<success_criteria>
- 215-R5: pipeline is pure composition - RS scoreMeasured, tri-modal index, 211 enumeration/vector helpers, AHP module, classifiers, emitter; zero new engines, zero new deps, zero new invocable command surfaces
- Both DG-2 modes runnable; DG-1 seam reachable from the CLI; all honest-degrade paths render, not throw
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Kind | Name | Where |
|------|------|-------|
| Script | scripts/eureka-portfolio-report.cjs (exports: parseArgv, main) | net-new composition runner (dev script class, like the 211 runner) |
| CLI flags | --db, --graph, --pairs graph|full, --offline, --top, --out, --json, --brokerage | eureka-portfolio-report.cjs |
| Additive exports | loadIndexVectors, truncate | scripts/eureka-room-report.cjs |
| Report headings | '## Ranked top N', '## Tail quadrant (weak signals)', '## Opportunity Statements' | generated md |
| Label | 'NOT YET BANKED (critic pending)' | generated md |
| Default outputs | evals/eureka/215-portfolio-report.md, evals/eureka/215-portfolio-report.json | generated (not committed by this plan) |
| Test | tests/test-215-portfolio-report.cjs | offline e2e |
| Aggregator | tests/run-all-215.sh | phase gate leg 1 |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/215-eureka-portfolio-scale-fusion-eureka-portfolio-fusion/215-04-SUMMARY.md` when done.
</output>
