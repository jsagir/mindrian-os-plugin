---
phase: 215-eureka-portfolio-fusion
plan: 05
type: execute
wave: 3
depends_on: [215-04]
files_modified:
  - tests/test-215-reproduction.cjs
  - evals/eureka/215-jhtv-portfolio-report.md
  - evals/eureka/215-jhtv-portfolio-report.json
autonomous: false
requirements: [215-R6]
must_haves:
  truths:
    - "The REAL MiniLM pass runs end-to-end against the 2117-tech jhtv room (both 212-era blockers fixed on main: c222ff7d embed batching, 73698c73 vec0 probe) - not assumed fixed, PROVEN at full scale (215-RESEARCH.md Pitfall 1, the single most load-bearing gate of the phase)"
    - "The two manual Opportunity Statements REPRODUCE from the automated pipeline: arrhythmias C16796xC03552 and cerebral aneurysm C16742xC05004 both appear as scored candidates with emitted canonical statements"
    - "A navigator (human) spot-checks the automated statements against the manual drafts and the tail section before the phase closes - real-room content is never verified by a network judge (Part 8 EGRESS RULE)"
  artifacts:
    - path: "tests/test-215-reproduction.cjs"
      provides: "the automated acceptance assertion over the real-run JSON"
      min_lines: 40
    - path: "evals/eureka/215-jhtv-portfolio-report.md"
      provides: "the LIVE full-2117 portfolio report (run artifact)"
      contains: "Tail quadrant"
  key_links:
    - from: "tests/test-215-reproduction.cjs"
      to: "evals/eureka/215-jhtv-portfolio-report.json"
      via: "reads the JSON sibling; asserts both acceptance pairs + canonical clauses + LIVE provenance"
      pattern: "215-jhtv-portfolio-report"
---

<objective>
The hard acceptance gate the ROADMAP goal sentence does NOT name (conflict #4, honored
per navigator directive): run the REAL pass against the 2117-tech jhtv-oliver-kuntz room
and reproduce the TWO manual Opportunity Statements as an acceptance test, then hand the
report to the navigator for the human spot-check.

Why this gate exists: the two drafts in the room are MANUAL - built by hand from the CSVs
precisely because the automated tri-modal path was blocked when they were written
(embedding OOM + vec0 offline fail, jhtv-d15 section 2). Both blockers are fixed on main
(c222ff7d, 73698c73) but were verified against a 261-node dogfood room and unit tests,
NOT against 2117 nodes end-to-end with the real encoder. Phase 215's job is to make those
drafts automated AND ranked; if the pipeline cannot surface them, nothing above it means
anything (215-RESEARCH.md Pitfall 1).

DG-2 note (flagged in 215-04): this acceptance run uses --pairs full (the whole catalog,
~2.2M cross-boundary pairs) per the researched recommendation - the full catalog may hold
tail gems the lens3-paired graph subset omits. The fast graph-mode report also runs, so
the navigator sees both substrates side by side at the checkpoint.

Purpose: 215-R6. Output: the LIVE report + JSON, the reproduction test green, the
navigator verdict recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-eureka-portfolio-scale-fusion-eureka-portfolio-fusion/215-RESEARCH.md
@scripts/eureka-portfolio-report.cjs
@tests/run-all-215.sh
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: bring the real encoder into the worktree + run the LIVE full-2117 pass</name>
  <files>evals/eureka/215-jhtv-portfolio-report.md, evals/eureka/215-jhtv-portfolio-report.json, evals/eureka/215-jhtv-portfolio-report.graph-mode.md</files>
  <read_first>
    - 215-RESEARCH.md Environment Availability table (MiniLM declared in package.json but NOT carried into build worktrees; npm install is the same gate 211-05 hit)
    - lib/core/eureka/embedding-spine.cjs header lines 80-90 (MINDRIAN_EMBED_BATCH knob, default 32 - the c222ff7d OOM fix)
  </read_first>
  <action>
    1. Run npm install (installs the ALREADY-DECLARED package.json dependency set into
       this worktree - @huggingface/transformers ^4.2.0 among them; NO new package is
       added to package.json, so the Package Legitimacy gate is N/A per 215-RESEARCH.md.
       If package.json would need a NEW package, STOP - that is out of contract).
    2. Sanity: node -e requiring @huggingface/transformers resolves; free RAM check
       (the pre-fix OOM was ~26.7GB; the batched path needs far less - keep
       MINDRIAN_EMBED_BATCH at default 32, lower to 16 only if the run OOMs, and record
       the value used).
    3. LIVE acceptance run (NO --offline):
       node scripts/eureka-portfolio-report.cjs --db /home/jsagi/MindrianRooms/jhtv-oliver-kuntz --pairs full --top 50 --out evals/eureka/215-jhtv-portfolio-report.md --json evals/eureka/215-jhtv-portfolio-report.json
       Expect a LONG run (2117 nodes embed once batched, then ~2.2M pair scorings with
       in-memory vectors; stderr progress every 100k pairs proves liveness). Do not
       background-and-forget; capture the final stdout summary line.
       If the run fails: this is a GENUINE acceptance failure - diagnose root cause
       (embed OOM vs vec0 vs enumeration), record it in the SUMMARY, do NOT paper over
       with --offline (a stub-vector report is NOT acceptance-grade; the provenance would
       say so anyway).
    4. Also produce the fast-substrate comparison report for the checkpoint:
       node scripts/eureka-portfolio-report.cjs --db /home/jsagi/MindrianRooms/jhtv-oliver-kuntz --pairs graph --top 25 --out evals/eureka/215-jhtv-portfolio-report.graph-mode.md --json evals/eureka/215-jhtv-portfolio-report.graph-mode.json
       (DG-2: both substrates visible to the navigator side by side.)
    5. Confirm provenance in BOTH reports says LIVE (not stub) and the vec backend +
       embed batch used; confirm the room.db was not mutated beyond derived eureka_*
       tables (Part 9: SELECT count(*) FROM nodes unchanged before/after; record both
       counts in the SUMMARY).
  </action>
  <verify>
    <automated>test -s evals/eureka/215-jhtv-portfolio-report.md && test -s evals/eureka/215-jhtv-portfolio-report.json && ! grep -q "OFFLINE (deterministic stub encoder)" evals/eureka/215-jhtv-portfolio-report.md</automated>
  </verify>
  <acceptance_criteria>
    - the LIVE run exits 0 and writes both --out and --json files non-empty
    - `grep -c "Tail quadrant" evals/eureka/215-jhtv-portfolio-report.md` >= 1
    - `grep -c "stub" evals/eureka/215-jhtv-portfolio-report.md | head -1` == 0 (LIVE provenance, no stub encoder anywhere in the report)
    - `node -e "const j=require('./evals/eureka/215-jhtv-portfolio-report.json'); if(!j.provenance||!Array.isArray(j.ranked)||j.ranked.length<1) process.exit(1)"` exits 0
    - nodes count in room.db identical before and after (recorded in SUMMARY)
  </acceptance_criteria>
  <done>The pipeline that was blocked at 2117 scale has now completed at 2117 scale with the real encoder, and the artifacts exist for the reproduction test.</done>
</task>

<task type="auto">
  <name>Task 2: the reproduction assertion - the two manual gems surface automated</name>
  <files>tests/test-215-reproduction.cjs</files>
  <read_first>
    - lib/core/eureka/opportunity-statement.cjs (CLAUSE_LABELS export - assert against the single source of truth, do not restring the clauses)
    - tests/test-215-portfolio-report.cjs (assertion style from Plan 04)
  </read_first>
  <action>
    Write tests/test-215-reproduction.cjs. It reads
    evals/eureka/215-jhtv-portfolio-report.json (exit 2 with a clear
    'reproduction: real-run JSON missing - run Plan 05 Task 1 first' message when absent,
    so the run-all-215 run_if guard and a direct invocation both stay honest) and asserts:

    1. ACCEPTANCE PAIR 1 (arrhythmias): a candidate exists whose unordered id pair is
       {C16796, C03552} - search BOTH j.ranked and j.statements arrays; it must appear in
       statements (top-N or tail-ridden, either path counts - the runner banks tail
       candidates regardless of rank by design).
    2. ACCEPTANCE PAIR 2 (cerebral aneurysm): same for {C16742, C05004}.
    3. Each of the two carries a statement whose text contains EVERY marker in
       CLAUSE_LABELS (require the module and iterate - byte parity with the canonical
       shape, commit 360e4826).
    4. Each statement's fields.unmet_need_a and unmet_need_b are non-empty and its
       critic state is either a real verdict object or the literal 'pending' (and when
       'pending', banked === false - the Pitfall 4 honesty invariant holds on real data).
    5. Provenance: j.provenance run mode is LIVE, pairs mode 'full', and the AHP cr
       value is <= 0.1.
    6. Print the two reproduced statement texts to stdout (the navigator reads them in
       the Task 3 checkpoint transcript) + the standard 'assertions passed' summary.

    If assertion 1 or 2 FAILS: that is the acceptance verdict 'pipeline does not
    reproduce the manual drafts' - do NOT weaken thresholds or switch substrates to force
    a pass; record the failure and stop for the navigator (the checkpoint below becomes a
    failure review). Then re-run bash tests/run-all-215.sh: leg 6's guard file now
    exists, so the reproduction leg must run (not SKIP) and the aggregator must stay
    FAIL=0 overall for the green path.
  </action>
  <verify>
    <automated>node tests/test-215-reproduction.cjs && bash tests/run-all-215.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-215-reproduction.cjs` exits 0 and stdout contains 'C16796' and 'C03552' and 'C16742' and 'C05004' and 'assertions passed'
    - `bash tests/run-all-215.sh` exits 0 with FAIL=0 and the reproduction leg reports PASSED (not SKIPPED)
    - `bash tests/run-all-211.sh` still exits 0 with FAIL=0
  </acceptance_criteria>
  <done>Both manual drafts reproduce as automated, ranked, canonical statements; the full phase aggregator is green with the acceptance leg live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: navigator spot-check - manual drafts vs automated statements + tail sanity</name>
  <what-built>
    The automated portfolio pipeline ran LIVE against the real 2117-tech JHU room
    (--pairs full) and reproduced the two manual Opportunity Statements as ranked,
    canonical, critic-gated candidates. A graph-substrate comparison report was also
    produced (DG-2). The reproduction test is green.
  </what-built>
  <how-to-verify>
    1. Open evals/eureka/215-jhtv-portfolio-report.md.
    2. Compare the two automated statements (also printed in the Task 2 test output)
       against your manual drafts in the room entry:
       ~/MindrianRooms/rethinking-mindrianos/research/2026-07-06-jhtv-d15-real-room-test-and-opportunity-formula/2026-07-06-jhtv-d15-real-room-test-and-opportunity-formula.md (section 4).
       Question: does the automated unmet-need / gap / next-steps framing capture what
       you wrote by hand, or did the deterministic derivation lose the insight?
    3. Read the '## Tail quadrant (weak signals)' section. Question: does it read like
       candidate gems, or a noise fountain? (If it flagged hundreds, the suspect_noise
       stamp should already say so - Pitfall 3.)
    4. Skim the graph-mode comparison report (215-jhtv-portfolio-report.graph-mode.md).
       DECISION GATE DG-2: should the DEFAULT ranking substrate stay graph-pairs (fast,
       lens3-cited) or move to full-catalog? Your call is recorded in the SUMMARY.
    5. DECISION GATE DG-1: the tail ran 'attention-growth-only' (the 212.5 Burt
       brokerage module does not exist yet; the seam is live at --brokerage). Confirm or
       veto the researched recommendation to COMPOSE with structural-hole brokerage when
       212.5 ships (this sets a 215-follow-up wiring item on 212.5, not new 215 code).
    6. Note: statements are stamped 'NOT YET BANKED (critic pending)' unless the Phase
       212 critic module is present in this tree - banking requires the critic pass, by
       design (Pitfall 4).
  </how-to-verify>
  <resume-signal>Type "approved" (records the acceptance verdict + your DG-1/DG-2 calls), or describe what the automated statements got wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real room.db (Oliver's catalog) -> local report | real client portfolio content processed at full scale |
| npm registry -> worktree | model/runtime deps installed for the live run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-13 | Information disclosure | live run egress | mitigate | runner makes zero network calls (grep-gated in 215-04); the only network touch is transformers' one-time model-weight download BY MODEL ID (no room bytes egress - the 211 banner contract); real-room verification is HUMAN, never a network judge |
| T-215-14 | Tampering / integrity | LIVE-labeled report with stale stub vectors | mitigate | idx.embedded gate + Task 1 acceptance grep rejecting any 'stub' provenance in the LIVE report |
| T-215-15 | Repudiation | acceptance verdict | mitigate | reproduction test output + navigator checkpoint verdict + before/after node counts recorded in SUMMARY |
| T-215-SC | Tampering | npm install | mitigate | installs ONLY the committed package.json/package-lock dependency set; adding any NEW package is out of contract for this plan (stop + escalate); 215-RESEARCH.md: Package Legitimacy Audit N/A because no new package enters |
</threat_model>

<verification>
- node tests/test-215-reproduction.cjs exits 0
- bash tests/run-all-215.sh exits 0, FAIL=0, reproduction leg PASSED not SKIPPED
- bash tests/run-all-211.sh exits 0, FAIL=0
- navigator verdict recorded in 215-05-SUMMARY.md (approved / issues verbatim), including the DG-1 and DG-2 calls
</verification>

<success_criteria>
- 215-R6: the real 2117-scale pass completed with the live encoder AND both manual Opportunity Statements reproduced automated + ranked AND the navigator spot-check verdict is recorded
- Both flagged Decision Gates (DG-1 tail x Burt composition, DG-2 default substrate) carry an explicit navigator call in the SUMMARY
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Kind | Name | Where |
|------|------|-------|
| Run artifact | evals/eureka/215-jhtv-portfolio-report.md (LIVE, full-2117) | generated + kept |
| Run artifact | evals/eureka/215-jhtv-portfolio-report.json (acceptance substrate) | generated + kept |
| Run artifact | evals/eureka/215-jhtv-portfolio-report.graph-mode.md/.json (DG-2 comparison) | generated + kept |
| Test | tests/test-215-reproduction.cjs (acceptance pairs C16796xC03552, C16742xC05004) | integration |
| Record | navigator verdict + DG-1/DG-2 calls | 215-05-SUMMARY.md |
| Env note | MINDRIAN_EMBED_BATCH value used for the 2117 run | 215-05-SUMMARY.md |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/215-eureka-portfolio-scale-fusion-eureka-portfolio-fusion/215-05-SUMMARY.md` when done (include the navigator verdict verbatim).
</output>
