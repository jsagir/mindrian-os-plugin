---
phase: 155-ignite-flow
plan: 02
type: execute
wave: 2
depends_on: [155-01]
files_modified:
  - lib/core/navigation/room-birth.cjs
  - lib/core/navigation/memory-events.cjs
  - lib/core/navigation.cjs
  - lib/core/scratchpad-ops.cjs
  - tests/test-room-birth.cjs
  - tests/test-memory-events-birth-floor.cjs
  - tests/run-all-155.sh
autonomous: true
requirements: [GAP-2]
must_haves:
  truths:
    - "room-birth.cjs executes the 7-step Q1 sequence (mkdir + scaffold -> room.db birth -> compute-state -> registry-last -> reconcile -> scratchpad drain) as a single orchestration with the SQLite transaction as the one genuinely ACID block"
    - "room.db is created via room-db.cjs::openRoomDb (NOT openRoomDbForCaller) from inside the lib/core/navigation/ allow-list path"
    - "feynman-seed-writer.seedSection is called per section to close the 4/6 -> 6/6 memory complement gap"
    - "EVENT_TYPES contains both 'room_created' and 'birth_gate_answered' (additive floor-test idiom, never asserting .size)"
    - "drainBirthGateAnswers (Plan 01 stub) is filled: each birth_gate_answers entry becomes a logMemoryEvent('birth_gate_answered') + typed edge (Approve->FILED_AS_DECISION, Defer->DEFERRED, Reject->REJECTED_BECAUSE)"
    - "No CHOSE edge is written anywhere (CHOSE is not in ALLOWED_EDGE_TYPES -- verified; RESEARCH Pitfall 7)"
    - "registry flip is the LAST structural step (STEP 4 in Q1 -- scaffold-then-register order; RESEARCH Q1 'registry-first dangles the active pointer')"
    - "confirmNode batch on B2 Approve uses resolveByUser(roomDir) for byUser attribution (USER.md must be written in STEP 1 before this call)"
    - "scripts/check-substrate.cjs passes: room-birth.cjs is inside the lib/core/navigation/ allow-list"
  artifacts:
    - path: "lib/core/navigation/room-birth.cjs"
      provides: "birthRoom(opts) -- the 7-step birth orchestration; drainBirthGateAnswers body (filling the Plan 01 stub)"
      min_lines: 120
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "EVENT_TYPES extended with room_created and birth_gate_answered (additive, floor-test idiom)"
      contains: "room_created"
    - path: "lib/core/navigation.cjs"
      provides: "additive re-export of birthRoom"
      contains: "birthRoom"
    - path: "tests/test-room-birth.cjs"
      provides: "integration test over a tmp ROOMS_HOME fixture proving scaffold-then-register order, 6/6 files, replay edges, registry-last"
  key_links:
    - from: "lib/core/navigation/room-birth.cjs"
      to: "lib/core/room-db.cjs"
      via: "openRoomDb (the lazy creator; NOT openRoomDbForCaller)"
      pattern: "openRoomDb"
    - from: "lib/core/navigation/room-birth.cjs"
      to: "lib/core/scratchpad-ops.cjs"
      via: "drainBirthGateAnswers called inside STEP 2 transaction before migrateToRoom in STEP 6"
      pattern: "drainBirthGateAnswers"
    - from: "lib/core/navigation/room-birth.cjs"
      to: "lib/core/feynman/feynman-seed-writer.cjs"
      via: "seedSection per section to close 4/6 -> 6/6"
      pattern: "seedSection"
    - from: "lib/core/navigation/room-birth.cjs"
      to: "lib/core/navigation/confirm-node.cjs"
      via: "confirmNode batch from B2 Approve; resolveByUser(roomDir) for byUser"
      pattern: "confirmNode"
---

<objective>
Ship the birth-transaction keystone: lib/core/navigation/room-birth.cjs, a new module inside the substrate-guard allow-list that orchestrates the Q1 7-step birth sequence, fills the drainBirthGateAnswers stub from Plan 01, and extends EVENT_TYPES additively with room_created and birth_gate_answered.

Purpose: Everything in Phase 155 composes this module. The birth transaction is the ACID chokepoint: mkdir + scaffold (idempotent, unregistered) -> single SQLite BEGIN...COMMIT (room.db birth, setFocus, drain gate answers, confirmNode batch, room_created event) -> compute-state -> registry flip (the commit point) -> reconcileMemoryArtifacts -> scratchpad drain. The scaffold-before-registry order is RESEARCH Q1's primary correction -- registry-first would dangle the active pointer during the structurally-incomplete window. The feynman-seed-writer calls close the known 4/6 gap (scaffold writes ROOM/STATE/MINTO/USER; FEYNMAN.md and BRAIN.md have no scaffold templates). The drainBirthGateAnswers body converts Plan 01 journaling into graph edges using only ALLOWED_EDGE_TYPES (no CHOSE edge exists -- RESEARCH Pitfall 7).

Output: lib/core/navigation/room-birth.cjs, additive EVENT_TYPES entries, navigation.cjs re-export, scratchpad-ops.cjs drainBirthGateAnswers body, two test drivers, updated run-all-155.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/155-ignite-flow/155-RESEARCH.md
@.planning/research/new-room-onboarding-20260612/BIRTH-FLOW-BRIEF.md
@lib/core/navigation.cjs
@lib/core/navigation/memory-events.cjs
@lib/core/navigation/confirm-node.cjs
@lib/core/navigation/edges.cjs
@lib/core/navigation/spine-events.cjs
@lib/core/room-skeleton-scaffold.cjs
@lib/core/feynman/feynman-seed-writer.cjs
@lib/core/room-db.cjs
@lib/core/scratchpad-ops.cjs
@scripts/room-registry
@scripts/check-substrate.cjs
@lib/core/memory/reconcile-memory-runner.cjs
@lib/core/breakthrough/schema.cjs
@.planning/phases/155-ignite-flow/155-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols for Plans 04/05/06 to consume:
- `birthRoom(opts)` - exported from room-birth.cjs, re-exported via navigation.cjs
  opts: { slug, roomDir, sessionId, ventureText, jtbd, blueprintFamily, gateAnswers, approvedBy, scratchpadReplay:true }
- EVENT_TYPES now includes 'room_created' and 'birth_gate_answered'
- drainBirthGateAnswers body (fills the Plan 01 stub; also available as navigation.drainBirthGateAnswers)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EVENT_TYPES additive extension + floor tests (GAP-2 event prerequisite)</name>
  <files>lib/core/navigation/memory-events.cjs, tests/test-memory-events-birth-floor.cjs</files>
  <read_first>
    - lib/core/navigation/memory-events.cjs:10-101 -- EVENT_TYPES frozen Set + the additive extension idiom (lines 69, 81, 101 each show the comment block pattern: cite the phase, state "accepted only because they are now IN the Set")
    - lib/core/navigation/memory-events.cjs:125-145 -- the most recent additive block (mirroring Phase 124 +2 entries) to copy verbatim as the 155 block
    - lib/core/navigation/memory-events.cjs:400-405 -- logEvent event_type gate (the SET member check; adding to the Set is how you register)
    - tests/test-memory-events-birth-floor.cjs -- does not exist yet; create it using the FLOOR idiom (assert membership, assert Set contains prior entries, NEVER assert .size)
    - lib/core/navigation/memory-events.cjs:482 -- module.exports line (EVENT_TYPES is already exported; no re-export change needed)
  </read_first>
  <behavior>
    - Test: EVENT_TYPES.has('room_created') === true
    - Test: EVENT_TYPES.has('birth_gate_answered') === true
    - Test: all previously-existing members still pass EVENT_TYPES.has() (FLOOR test -- assert a representative sample including 'memory_synced', 'focus_changed', 'operator_transitioned', 'f_selector_decision'; never call .size)
    - Test: logEvent called with event_type='room_created' on an in-memory db does NOT throw 'invalid_event_type' (the Set gate accepts it)
    - Test: logEvent called with event_type='birth_gate_answered' on an in-memory db does NOT throw 'invalid_event_type'
    - Test: logEvent called with event_type='room_destroyed' (made-up) DOES return/throw invalid_event_type (the negative guard)
  </behavior>
  <action>
    Open lib/core/navigation/memory-events.cjs and append two new entries to the EVENT_TYPES Set using the established additive comment-block idiom. Copy the comment format from the most recent block verbatim: cite Phase 155-02, name the consumer (room-birth.cjs birthRoom), and state "accepted only because they are now IN the Set." Add 'room_created' with comment: emitted once at the end of the STEP 2 SQLite transaction when the room first exists; the canonical signal that birth succeeded. Add 'birth_gate_answered' with comment: emitted per gate answer drained from scratchpad during STEP 2 (B1/B2/U0 gate decisions become audit nodes before the registry flip).

    Write tests/test-memory-events-birth-floor.cjs as a bare node CJS driver. Import EVENT_TYPES and logEvent from lib/core/navigation/memory-events.cjs. Use the check/pass counter pattern (not jest). Build an in-memory db with applySchema from tests/claim-harness/build-fixture-room-db.cjs for the logEvent calls. FLOOR assertions only -- never assert Set.size. The SKIP-77 probe guards node:sqlite availability. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-memory-events-birth-floor.cjs && bash tests/run-all-150.8.sh</automated>
  </verify>
  <done>EVENT_TYPES contains room_created and birth_gate_answered. All prior members preserved. logEvent accepts both new types and rejects made-up ones. run-all-150.8.sh regression holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: room-birth.cjs birth transaction + drainBirthGateAnswers body (GAP-2, GAP-6 replay half)</name>
  <files>lib/core/navigation/room-birth.cjs, lib/core/navigation.cjs, lib/core/scratchpad-ops.cjs, tests/test-room-birth.cjs, tests/run-all-155.sh</files>
  <read_first>
    - lib/core/breakthrough/schema.cjs:1-60 -- the D-20 single-SQLite-transaction precedent (writeBreakthrough; BEGIN/COMMIT with multiple writes inside; this is the exact idiom room-birth mirrors)
    - lib/core/navigation/spine-events.cjs:169-181,340-348 -- logOperatorTransition signature + openRoomDbForCaller REFUSES creation (returns null if room.db file does not exist); room-birth MUST use openRoomDb from room-db.cjs instead
    - lib/core/room-db.cjs -- openRoomDb (the lazy creator; the only legal door for birth)
    - lib/core/room-skeleton-scaffold.cjs:163-183 -- scaffoldRoomSkeleton signature (roomDir must already EXIST); SECTION_NAMES frozen table (what feynman-seed-writer augments)
    - lib/core/feynman/feynman-seed-writer.cjs:1-40,129 -- seedSection(roomDir, sectionSlug, seedBody, opts) signature; idempotent; never clobbers human body
    - lib/core/navigation/confirm-node.cjs:38-44,54-69 -- confirmNode(db, id, byUser, reason) + resolveByUser(roomDir) (reads USER.md; falls back to 'navigator' if USER.md not yet written -- RESEARCH Pitfall 12: write USER.md in STEP 1 BEFORE calling resolveByUser)
    - lib/core/navigation/edges.cjs:32-60,339-369 -- ALLOWED_EDGE_TYPES frozen set + writeEdge signature; DERIVED_FROM at line 52; NO CHOSE edge (RESEARCH Pitfall 7 verified)
    - lib/core/navigation/focus.cjs:13,36-43 -- setFocus(db, sessionId, nodeId, setBy); VALID_SET_BY enum; 'user' is valid; 'auto-from-upload' is NOT (RESEARCH Pitfall 1)
    - lib/core/memory/reconcile-memory-runner.cjs:414-422 -- reconcileMemoryArtifacts(roomDir, opts); opts.db REQUIRED (returns empty report without it)
    - lib/core/navigation/typed-claim.cjs -- writeClaimNode (Plan 02 calls it for venture node and initial extraction claims; proposed status)
    - lib/core/scratchpad-ops.cjs -- drainBirthGateAnswers stub (Plan 01 exported; fill the body here)
    - scripts/room-registry:122-165 -- the registry create command (atomic tmp+mv; parks previous active; flips active pointer; MUST be the last structural step)
    - lib/core/navigation.cjs:122-148 -- the additive re-export comment-block idiom
    - .planning/phases/155-ignite-flow/155-RESEARCH.md (Q1 7-step sequence verbatim)
  </read_first>
  <behavior>
    - Test (integration, tmp ROOMS_HOME): birthRoom({slug:'test-venture', roomDir:tmpDir+'/test-venture', sessionId:'s1', ventureText:'A test venture', jtbd:'When I need X', blueprintFamily:'venture', gateAnswers:[], approvedBy:'test-user'}) creates: the roomDir directory, all 6 memory files (ROOM.md, STATE.md, MINTO.md, USER.md, FEYNMAN.md, BRAIN.md path referenced), room.db file, a registry entry with active=true, and returns {ok:true}
    - Test: room.db contains a node with type matching the venture claim (writeClaimNode was called)
    - Test: room.db contains a memory_event row with event_type='room_created'
    - Test: the registry file was written AFTER room.db was created (scaffold-then-register order -- stat mtimes; or assert registry does not exist mid-transaction by checking at a synthetic pause point)
    - Test: birthRoom called a second time on the same roomDir is idempotent (no crash, returns ok)
    - Test (drain): birthRoom with gateAnswers:[{gate_id:'B2', canonical_verb:'Approve', option_key:'approve', alias_label:'Approve', ts:Date.now()}] writes a memory_event with event_type='birth_gate_answered' and a FILED_AS_DECISION edge (not a CHOSE edge)
    - Test (drain): gateAnswers entry with canonical_verb:'Defer' writes a DEFERRED edge
    - Test (drain): gateAnswers entry with canonical_verb:'Reject' writes a REJECTED_BECAUSE edge
    - Test: bash scripts/check-substrate.cjs passes (room-birth.cjs is inside the allow-list path lib/core/navigation/)
  </behavior>
  <action>
    Create lib/core/navigation/room-birth.cjs inside the substrate-guard allow-list path (lib/core/navigation/). Open with 'use strict' and a header doctrine block citing Canon Parts 8 + 9, SEED-022, SEED-001 partial, and the Phase 155 plan. State the allow-list rationale: "This module opens room.db via room-db.cjs::openRoomDb (the lazy creator). openRoomDbForCaller is FORBIDDEN here (it refuses to create room.db). This module is in lib/core/navigation/ so scripts/check-substrate.cjs accepts it."

    Implement birthRoom(opts) following the Q1 7-step sequence from RESEARCH exactly:

    GATE: opts.approvedBy must be truthy (fail closed if called without B2 Approve; return {ok:false, reason:'no_approval'}).

    STEP 1 -- scaffold (idempotent, unregistered): fs.mkdirSync(roomDir, {recursive:true}); write .room-root sentinel file (the {room, active:true, born} JSON used by resolve-umbilical-target.cjs); call scaffoldRoomSkeleton(roomDir, {blueprintFamily: opts.blueprintFamily||'venture'}); call feynman-seed-writer.seedSection for each section slug in the blueprint family (use SECTION_NAMES as the default list; seedBody is a brief placeholder noting "Seeded at birth -- replace with your content"); write USER.md via writeUserMdAtomic (Plan 03 owns the full schema; here write the minimum: {canonical_role: opts.canonicalRole||'navigator', role_blend: opts.roleBlend||{}, journey_stage: opts.journeyStage||'Ordinary World'} -- this MUST happen before confirmNode so resolveByUser finds identity).

    STEP 2 -- SQLite transaction (the ACID block; mirrors writeBreakthrough D-20): openRoomDb(roomDir) to create room.db; db.exec('BEGIN'); writeClaimNode for the venture node (knowledge_type:'heuristic', text:opts.ventureText, sessionId:opts.sessionId); setFocus(db, opts.sessionId, ventureNodeId, 'user') [NOT 'auto-from-upload' -- RESEARCH Pitfall 1]; call drainBirthGateAnswers(db, roomDir, opts.gateAnswers) (fills the Plan 01 stub -- see below); batch confirmNode on all proposed nodes using resolveByUser(roomDir) as byUser (reversible default per open question 7, flagged in comment: "// OPEN-Q7: batch attribution; revisit for per-claim if navigator decides"); logJtbdTransition(roomDir, {jtbd:opts.jtbd, sessionId:opts.sessionId}) [note: this is roomDir-based, its own handle, dedupe-keyed -- call after db is created but inside the COMMIT sequence by passing the open db]; logMemoryEvent(db, {event_type:'room_created', sessionId:opts.sessionId, data:{slug:opts.slug, blueprint_family:opts.blueprintFamily||'venture'}}); db.exec('COMMIT'). Wrap entire block in try/catch; on error db.exec('ROLLBACK') and return {ok:false, reason:'birth_transaction_failed', detail}.

    STEP 2 post-commit: logOperatorTransition(roomDir, {to:'BUILD_ROOM', write_transition_edge:true, sessionId:opts.sessionId}) -- safe post-commit because room.db now exists.

    STEP 3: child_process.execSync('node scripts/compute-state "'+roomDir+'"', {stdio:'pipe'}) to write STATE.md (STATE.md is NEVER authored; RESEARCH Section 3 note).

    STEP 4 (COMMIT POINT): child_process.execSync('node scripts/room-registry create "'+opts.slug+'" "'+roomDir+'" "'+opts.vname+'" "'+opts.vstage+'"', {stdio:'pipe'}) -- the atomic tmp+mv that flips the active pointer. This is last of the structural steps.

    STEP 5: reconcileMemoryArtifacts(roomDir, {db}) -- cortex + SENS-08 scalars live before next turn. Note: re-open db via openRoomDb after the registry write (or reuse the handle if still open; guard with db.open check).

    STEP 6: call scratchpadOps.migrateToRoom(roomDir) -- drains banked opportunities and highlights, then clearScratchpad(). Gate answers were consumed in STEP 2 via drainBirthGateAnswers; migrateToRoom is safe to call now without losing them.

    STEP 7 (deferred enrichment): emit a log line "BRAIN derivation enqueued for room: {slug}" (the actual Phase 90 enqueue is optional here; the comment makes the intent clear and prevents a future plan from forgetting it).

    Return {ok:true, roomDir, slug, db_created:true}.

    Fill the drainBirthGateAnswers body in lib/core/scratchpad-ops.cjs (the Plan 01 stub): given (db, roomDir, gateAnswers) where gateAnswers is an array of {gate_id, canonical_verb, option_key, alias_label, free_text?, arrival_asset?, ts}, iterate and for each entry: logMemoryEvent(db, {event_type:'birth_gate_answered', data:{gate_id, canonical_verb, option_key}}); then write a typed edge based on canonical_verb: 'Approve' or 'RunMethodology' -> FILED_AS_DECISION; 'Defer' -> DEFERRED; 'Reject' -> REJECTED_BECAUSE (with reason=entry.free_text||'navigator rejected at gate'); anything else -> FILED_AS_DECISION as the safe default. NEVER write a CHOSE edge (not in ALLOWED_EDGE_TYPES). The source_id is the venture node id (passed as opts param or derived from the gate_id); target_id is a synthetic node id for the gate answer artifact.

    Add additive re-export in lib/core/navigation.cjs: require('./navigation/room-birth.cjs'), export birthRoom. Comment block names consumers (ignite.md command, Plan 06).

    Write tests/test-room-birth.cjs as an integration driver: use fs.mkdtempSync for a tmp ROOMS_HOME, call birthRoom with fixture opts, assert all post-conditions (file existence, room.db content, registry, event rows, edge types). Use the SKIP-77 probe. No em-dashes. Extend tests/run-all-155.sh to include test-room-birth.cjs and test-memory-events-birth-floor.cjs in the aggregation.
  </action>
  <verify>
    <automated>node tests/test-memory-events-birth-floor.cjs && node tests/test-room-birth.cjs && bash tests/run-all-155.sh && bash tests/run-all-150.8.sh && node scripts/check-substrate.cjs</automated>
  </verify>
  <done>birthRoom executes the 7-step Q1 sequence. room.db created via openRoomDb (not openRoomDbForCaller). 6/6 memory files exist post-birth. room_created and birth_gate_answered events present. Drain edges use FILED_AS_DECISION/DEFERRED/REJECTED_BECAUSE (never CHOSE). Registry flip is last structural step. check-substrate.cjs passes. run-all-155.sh green. run-all-150.8.sh regression holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opts.ventureText -> room.db claim node | User-supplied venture description crosses into a graph node property |
| gateAnswers[].free_text -> REJECTED_BECAUSE edge reason | User rejection reason crosses into an edge property |
| roomDir path -> file system | Caller-supplied path used for mkdir and subprocess invocations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-02-01 | Tampering | opts.ventureText smuggling SQL via writeClaimNode | mitigate | writeClaimNode uses parameterized statements (Plan 01 established); no string interpolation |
| T-155-02-02 | Tampering | roomDir path traversal in compute-state/room-registry subprocess calls | mitigate | Validate roomDir is under ROOMS_HOME before execSync; use double-quoted shell args; reject paths containing '..' |
| T-155-02-03 | Elevation of Privilege | birthRoom called without B2 Approve (gate bypass) | mitigate | Hard guard on opts.approvedBy at function entry; returns {ok:false, reason:'no_approval'} immediately |
| T-155-02-04 | Tampering | No CHOSE edge (constitutional; ALLOWED_EDGE_TYPES frozen) | mitigate | drainBirthGateAnswers maps all canonical_verbs to allowed types; 'CHOSE' is not in the mapping table; test asserts FILED_AS_DECISION not CHOSE |
| T-155-02-05 | Information Disclosure | gateAnswers or ventureText reaching Brain during STEP 7 BRAIN enqueue | mitigate | STEP 7 is a log line only (no real enqueue in this plan); the Phase 90 enqueue sends only generic handles (Part 8); add a comment citing this |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; all work over node built-ins (node:sqlite via room-db.cjs, node:fs, node:child_process) |
</threat_model>

<verification>
- `node tests/test-memory-events-birth-floor.cjs` -- room_created/birth_gate_answered in EVENT_TYPES; floor preserved
- `node tests/test-room-birth.cjs` -- 7-step sequence, 6/6 files, room.db seeded, drain edges correct, registry-last order
- `bash tests/run-all-155.sh` -- phase gate green (all Wave-1 + Wave-2 drivers)
- `bash tests/run-all-150.8.sh` -- regression floor
- `bash tests/run-all-148.sh` -- regression floor 18/18
- `node scripts/check-substrate.cjs` -- room-birth.cjs in allow-list; passes
- `grep -rn "CHOSE" lib/core/navigation/room-birth.cjs lib/core/scratchpad-ops.cjs` returns zero (no CHOSE edge written)
- `grep -rn "\-\-" lib/core/navigation/room-birth.cjs tests/test-room-birth.cjs | grep -v '//'` returns zero (no em-dashes)
</verification>

<success_criteria>
- lib/core/navigation/room-birth.cjs exists inside the substrate-guard allow-list; exports birthRoom
- 7-step Q1 sequence implemented with scaffold-before-registry order; SQLite transaction as the ACID block
- feynman-seed-writer.seedSection called per section (6/6 complement closed)
- EVENT_TYPES additively extended with room_created and birth_gate_answered (floor-test idiom, no .size assert)
- drainBirthGateAnswers fills the Plan 01 stub: FILED_AS_DECISION/DEFERRED/REJECTED_BECAUSE edges; no CHOSE
- confirmNode batch uses resolveByUser(roomDir) after USER.md is written in STEP 1
- run-all-155.sh green; run-all-150.8.sh + run-all-148.sh regressions hold
</success_criteria>

<output>
Create `.planning/phases/155-ignite-flow/155-02-SUMMARY.md` when done.
</output>
