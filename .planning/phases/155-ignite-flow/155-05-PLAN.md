---
phase: 155-ignite-flow
plan: 05
type: execute
wave: 4
depends_on: [155-02, 155-04]
files_modified:
  - data/room-blueprints.json
  - lib/core/room-skeleton-scaffold.cjs
  - commands/new-project.md
  - scripts/check-room-blueprints.cjs
  - tests/run-all-155.sh
autonomous: true
requirements: [GAP-5, GAP-8]
must_haves:
  truths:
    - "data/room-blueprints.json exists with 8 blueprint families (exploration, solution-first, problem-first, business-first, portfolio, venture, program, case-study) each carrying a sections array and a default_methodologies array"
    - "scripts/check-room-blueprints.cjs --check passes: all 8 families present, all section slugs are valid (no drift from SECTION_NAMES), CI-enforced"
    - "room-skeleton-scaffold.cjs scaffoldRoomSkeleton accepts opts.blueprintFamily and consumes data/room-blueprints.json to set the section list instead of the frozen SECTION_NAMES table when a family is specified"
    - "ROOM.md default_methodologies is populated from the blueprint family's default_methodologies array at birth (not the static table at new-project.md:195-204)"
    - "The B1 starting-point gate in new-project.md maps each arrival_asset option to a blueprint family (A solution->solution-first, A domain->exploration, A venture->venture)"
    - "SECTION_NAMES frozen table in room-skeleton-scaffold.cjs is UNCHANGED for callers that do not pass blueprintFamily (backwards compat; the venture family reproduces the frozen 8 sections)"
  artifacts:
    - path: "data/room-blueprints.json"
      provides: "8 blueprint families with sections + default_methodologies arrays; does not exist today (net-new)"
      min_lines: 80
    - path: "scripts/check-room-blueprints.cjs"
      provides: "CI schema checker: 8 families, valid section slugs, default_methodologies non-empty; --check flag for pre-commit"
      contains: "check-room-blueprints"
    - path: "lib/core/room-skeleton-scaffold.cjs"
      provides: "scaffoldRoomSkeleton extended: accepts opts.blueprintFamily, reads data/room-blueprints.json, uses family sections when provided"
      contains: "blueprintFamily"
  key_links:
    - from: "lib/core/room-skeleton-scaffold.cjs"
      to: "data/room-blueprints.json"
      via: "require (or fs.readFileSync) when opts.blueprintFamily is provided"
      pattern: "blueprintFamily"
    - from: "commands/new-project.md"
      to: "data/room-blueprints.json"
      via: "B1 gate arrival_asset options map to blueprint family names; B2 displays the family's section set"
      pattern: "blueprintFamily"
    - from: "lib/core/navigation/room-birth.cjs"
      to: "lib/core/room-skeleton-scaffold.cjs"
      via: "birthRoom passes opts.blueprintFamily into scaffoldRoomSkeleton (Plan 02 already calls scaffold; this plan makes the family param live)"
      pattern: "blueprintFamily"
---

<objective>
Create data/room-blueprints.json (the 8-family blueprint registry that does not exist today), extend room-skeleton-scaffold.cjs to consume it, wire the B1 gate's arrival_asset selection to blueprint family choice, and populate ROOM.md default_methodologies from the chosen chain.

Purpose: RESEARCH Atlas 3 confirms data/room-blueprints.json does NOT exist (verified ls data/). The SECTION_NAMES frozen table in room-skeleton-scaffold.cjs is a dual-source with new-project.md:195-204 and produces the same 8 sections for every persona -- the root cause of iris2026 hand-rolling custom sections and portfolio-steward rooms lacking financial-model/legal-ip appropriately. Blueprint families are the data layer that makes scaffolding persona-conditional without code changes: exploration for CV-passers, portfolio for TTO/VC/PE/studio/accelerator/CVC/family-office (the BIRTH-FLOW-BRIEF.md Section 3 revised persona class), venture for sole entrepreneurs, etc. The B1 gate (already documented in new-project.md via Plan 01's B2 work) selects arrival_asset; that selection maps to a blueprint family which drives both the section list and the ROOM.md default_methodologies. This plan runs in wave 4, after Plan 04 (serialized because both edit commands/new-project.md -- Plan 04 owns the --express/--from-brief argument preamble; this plan owns the B1 STARTING POINT block; declared ordering eliminates the sequential-overwrite ambiguity flagged at plan-check).

Output: data/room-blueprints.json, scripts/check-room-blueprints.cjs CI check, extended room-skeleton-scaffold.cjs, B1 gate arrival_asset -> family mapping in new-project.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/155-ignite-flow/155-RESEARCH.md
@.planning/research/new-room-onboarding-20260612/BIRTH-FLOW-BRIEF.md
@lib/core/room-skeleton-scaffold.cjs
@commands/new-project.md
@.planning/phases/155-ignite-flow/155-02-SUMMARY.md
@.planning/phases/155-ignite-flow/155-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: data/room-blueprints.json + scripts/check-room-blueprints.cjs CI schema check (GAP-5, GAP-8)</name>
  <files>data/room-blueprints.json, scripts/check-room-blueprints.cjs, tests/run-all-155.sh</files>
  <read_first>
    - lib/core/room-skeleton-scaffold.cjs:36-56 -- SECTION_NAMES frozen table (the 8 canonical section slugs that are the valid values for any blueprint family's sections array; the venture family MUST reproduce this set exactly for backwards compat)
    - lib/core/room-skeleton-scaffold.cjs:36-56 -- SECTION_METADATA (the metadata per section: the frozen table the scaffold uses for descriptions; blueprint families reference these slugs)
    - .planning/research/new-room-onboarding-20260612/BIRTH-FLOW-BRIEF.md:126-136 -- Section 3 persona matrix: the 8 families mapped to their section sets and first framework chains (the authority for what goes in each family)
    - data/ -- verify room-blueprints.json does NOT exist (the data file is net-new)
    - scripts/check-room-blueprints.cjs -- does not exist yet; create it
    - scripts/build-connector-registry.cjs:51-56,408-441 -- the --check pattern to mirror for check-room-blueprints.cjs
  </read_first>
  <action>
    Create data/room-blueprints.json with the 8 blueprint families from BIRTH-FLOW-BRIEF.md Section 3. Each family is a top-level key with this shape:
    { "sections": [list of section slugs from SECTION_NAMES], "default_methodologies": [list of command slugs], "arrival_assets": [list of arrival_asset enum values that map to this family], "description": "one-line description" }

    The 8 families and their section sets (from BIRTH-FLOW-BRIEF.md Section 3 persona matrix):
    - "exploration": sections [problem-definition, opportunity-bank, market-analysis, domain-decomposition (if legal, else problem-definition/domain-analysis)]; default_methodologies: [explore-domains, beautiful-question, map-unknowns]; arrival_assets: ["domain-or-interest", "cv-upload"]
    - "solution-first": sections [solution-design, analogies, market-analysis, problem-definition]; default_methodologies: [find-analogies, user-needs]; arrival_assets: ["solution-looking-for-problem"]
    - "problem-first": sections [problem-definition, market-analysis, opportunity-bank]; default_methodologies: [validate, mullins]; arrival_assets: ["problem-seeking-business"]
    - "business-first": sections [business-model, competitive-analysis, problem-definition, market-analysis]; default_methodologies: [lean-canvas, challenge-assumptions, diagnose]; arrival_assets: ["business-case"]
    - "portfolio": sections [portfolio, assets, connections, opportunity-bank]; default_methodologies: [find-connections, whitespace, explore-domains]; arrival_assets: ["portfolio-steward"]; note: "portfolio steward persona class: TTO / VC / PE / venture studio / accelerator / CVC / family office (Decision 7)"
    - "venture": sections [problem-definition, market-analysis, solution-design, business-model, competitive-analysis, team-execution, legal-ip, financial-model]; default_methodologies: [mva-brief, diagnose, lean-canvas]; arrival_assets: ["defined-venture", "mva-brief"]; note: "reproduces the SECTION_NAMES frozen 8 for backwards compat"
    - "program": sections [problem-definition, market-analysis, opportunity-bank, team-execution]; default_methodologies: [explore-domains, mullins]; arrival_assets: ["program-initiative"]
    - "case-study": sections [problem-definition, solution-design, market-analysis, financial-model]; default_methodologies: [diagnose, lean-canvas]; arrival_assets: ["case-study"]

    IMPORTANT: sections must use only slugs from the SECTION_NAMES frozen table in room-skeleton-scaffold.cjs. Read those slugs first and use them exactly. If a desired section concept (e.g. "domain-decomposition") does not exist as a frozen slug, substitute the closest existing slug (e.g. "problem-definition") and add a comment field explaining the substitution. Do NOT invent new section slugs -- the frozen table is authoritative for the scaffold.

    Create scripts/check-room-blueprints.cjs as a node CJS script mirroring the --check pattern from build-connector-registry.cjs. The checker: reads data/room-blueprints.json; validates exactly 8 families are present; validates each family has sections (non-empty array), default_methodologies (non-empty array), arrival_assets (non-empty array); validates all section slugs appear in a VALID_SECTION_SLUGS set (read from room-skeleton-scaffold.cjs or hardcode the frozen set from SECTION_NAMES); exits 0 on pass, non-zero with error details on fail. Support --check flag for pre-commit use. No em-dashes.

    Extend tests/run-all-155.sh to include node scripts/check-room-blueprints.cjs --check in the aggregation.
  </action>
  <verify>
    <automated>node scripts/check-room-blueprints.cjs --check && bash tests/run-all-155.sh</automated>
  </verify>
  <done>data/room-blueprints.json exists with 8 valid families. check-room-blueprints.cjs --check passes. run-all-155.sh green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: room-skeleton-scaffold.cjs blueprint-family consumption + B1 gate wiring in new-project.md (GAP-8)</name>
  <files>lib/core/room-skeleton-scaffold.cjs, commands/new-project.md</files>
  <read_first>
    - lib/core/room-skeleton-scaffold.cjs:163-183 -- scaffoldRoomSkeleton(roomDir, opts) signature; opts currently has no blueprintFamily key; the section list comes from SECTION_NAMES frozen table
    - lib/core/room-skeleton-scaffold.cjs:36-56 -- SECTION_NAMES + SECTION_METADATA frozen tables (MUST remain unchanged; backwards compat for all callers that do not pass blueprintFamily)
    - lib/core/room-skeleton-scaffold.cjs -- read the section that writes ROOM.md (lines that populate default_methodologies in ROOM.md frontmatter) -- this is what gets parameterized from the family's default_methodologies array
    - commands/new-project.md -- the B1 gate block (added by Plan 01 in the context of new-project's B2; B1 is the arrival_asset gate before B2). Find or locate the B1 section; if Plan 01 did not add B1 to new-project.md (Plan 01 focused on B2 only), add the B1 block here with the arrival_asset -> blueprintFamily mapping
    - data/room-blueprints.json -- just created by Task 1; read the family structures
  </read_first>
  <behavior>
    - Test: scaffoldRoomSkeleton(tmpDir, {blueprintFamily:'exploration'}) creates a section directory set matching the exploration family's sections array (not the frozen SECTION_NAMES 8-section set)
    - Test: scaffoldRoomSkeleton(tmpDir, {blueprintFamily:'venture'}) creates the same section directory set as scaffoldRoomSkeleton(tmpDir, {}) (backwards compat -- venture family reproduces frozen 8)
    - Test: scaffoldRoomSkeleton(tmpDir, {blueprintFamily:'unknown-family'}) falls back to the frozen SECTION_NAMES default (graceful; unknown family is not an error)
    - Test: scaffoldRoomSkeleton(tmpDir, {blueprintFamily:'portfolio'}) writes ROOM.md with default_methodologies containing 'find-connections' (from the portfolio family)
    - Test (static): grep commands/new-project.md for 'blueprintFamily' returns >= 1 (B1 arrival_asset mapping is present)
    - Test (static): grep commands/new-project.md for 'B1' or 'STARTING POINT' returns >= 1 (B1 gate block present)
  </behavior>
  <action>
    Edit lib/core/room-skeleton-scaffold.cjs scaffoldRoomSkeleton function additively:

    At the top of the function, after validating roomDir exists, check opts.blueprintFamily. If provided and not falsy: load data/room-blueprints.json (require or fs.readFileSync + JSON.parse; the data/ path is relative to the module or resolved via __dirname); look up the family; if found, use family.sections as the sectionList instead of SECTION_NAMES. If not found, log a warning and fall back to SECTION_NAMES. If opts.blueprintFamily is not provided, continue using SECTION_NAMES as before (zero behavior change for existing callers).

    When writing ROOM.md frontmatter (the section that sets default_methodologies), populate the list from the blueprint family's default_methodologies array when a family is active. When no family is active (backwards compat path), use the existing static table or leave default_methodologies empty as before.

    FROZEN TABLE CONTRACT: SECTION_NAMES and SECTION_METADATA are never modified by this plan. They remain the canonical slug set. Blueprint families reference slugs FROM this set. The check-room-blueprints.cjs CI check enforces this.

    Edit commands/new-project.md to add the B1 gate block if not already present (Plan 01 added B2; B1 is the arrival_asset gate that precedes B2):

    Add a B1 -- STARTING POINT section in the new-project.md flow (before B2): instruction to call pickShape with payload.verbs for the F.1 shape (B1 uses F.1, not F.0, per BIRTH-FLOW-BRIEF.md Section 2). Display: "What are you arriving with?" with three options (matching the brief's B1 spec):
    - A: "A solution looking for its problem" -> sets arrival_asset='solution-looking-for-problem', blueprintFamily='solution-first'
    - B: "A domain or interest to explore" -> sets arrival_asset='domain-or-interest', blueprintFamily='exploration'
    - C: "A defined venture or business case" -> sets arrival_asset='defined-venture', blueprintFamily='venture'
    Plus Free-Text (F.1 always appends Free-Text last via the trailer).

    After B1 answer is received: journal to scratchpad via writeScratchpadBirthAnswer({gate_id:'B1', option_key: answer.option_key, canonical_verb: answer.canonical_verb, alias_label: answer.alias_label, arrival_asset: mappedArrivalAsset, ts: Date.now()}). Pass blueprintFamily into subsequent B2 display and into the birthRoom call.

    Add the directive / --express bypass note: when --express or --from-brief is active, B1 is pre-answered from context (arrival_asset inferred from material; B1 shown only if ambiguous per BIRTH-FLOW-BRIEF.md Section 2 "directive entry pre-answers and shows B1 only if ambiguous").

    Add Tri-Polar degradation script for B1: for Desktop (no AskUserQuestion card guarantee), render as "Which best describes what you are arriving with? (a) solution, (b) domain, (c) venture -- type a, b, or c."

    No new test file needed for this task -- the behavior tests use an integration fixture with fs.mkdtempSync in the same test file pattern. Add a brief test section in tests/test-room-birth.cjs (Plan 02 created it) or create tests/test-blueprint-scaffold.cjs as a standalone driver testing the scaffoldRoomSkeleton extension. Include in run-all-155.sh.
  </action>
  <verify>
    <automated>node scripts/check-room-blueprints.cjs --check && bash tests/run-all-155.sh && bash tests/run-all-148.sh && bash tests/test-no-bespoke-brain-prompts.sh</automated>
  </verify>
  <done>scaffoldRoomSkeleton consumes blueprintFamily from data/room-blueprints.json. SECTION_NAMES frozen table unchanged for no-family callers. ROOM.md default_methodologies populated from family. B1 gate with arrival_asset -> family mapping present in new-project.md. Tri-Polar degradation script present. All regressions hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| opts.blueprintFamily string -> data/room-blueprints.json lookup | Caller-supplied family name used as a JSON key; must not allow path traversal or arbitrary key injection |
| data/room-blueprints.json -> ROOM.md default_methodologies | Blueprint data crosses into a user-visible configuration file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-05-01 | Tampering | opts.blueprintFamily used as JSON object key (prototype pollution risk) | mitigate | Validate blueprintFamily against the known 8-family set via Object.prototype.hasOwnProperty check; reject any value not in the whitelist |
| T-155-05-02 | Tampering | data/room-blueprints.json section slugs referencing non-existent sections (scaffold creates wrong dirs) | mitigate | check-room-blueprints.cjs --check validates all slugs against VALID_SECTION_SLUGS; CI gate enforces this before any commit that modifies the data file |
| T-155-05-03 | Information Disclosure | blueprint family metadata (default_methodologies) reaching Brain | mitigate | default_methodologies are generic command slugs (framework handles); they are the class of data that IS allowed in Brain packets per Part 8 (generic methodology handles only) |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; node built-ins + existing require paths only |
</threat_model>

<verification>
- `node scripts/check-room-blueprints.cjs --check` -- 8 families, valid section slugs, non-empty default_methodologies
- `bash tests/run-all-155.sh` -- phase gate green (all Wave-1/2/3 drivers)
- `bash tests/run-all-148.sh` -- regression floor 18/18
- `bash tests/test-no-bespoke-brain-prompts.sh` -- bespoke-prompt tripwire green
- `grep -c 'SECTION_NAMES' lib/core/room-skeleton-scaffold.cjs` unchanged from pre-plan value (frozen table not removed; only opts.blueprintFamily branch added)
- `grep -c 'blueprintFamily' lib/core/room-skeleton-scaffold.cjs` returns >= 1
- `grep -c 'B1\|STARTING POINT' commands/new-project.md` returns >= 1
- `grep -rn "\-\-" data/room-blueprints.json scripts/check-room-blueprints.cjs | grep -v '//'` returns zero (no em-dashes)
</verification>

<success_criteria>
- data/room-blueprints.json: 8 families, CI check passing
- room-skeleton-scaffold.cjs: blueprintFamily opts param consumed; SECTION_NAMES unchanged for no-family callers; ROOM.md default_methodologies from family
- new-project.md: B1 gate present with arrival_asset -> blueprintFamily mapping; Tri-Polar degradation script; directive --express bypass note
- run-all-155.sh green; run-all-148.sh 18/18
</success_criteria>

<output>
Create `.planning/phases/155-ignite-flow/155-05-SUMMARY.md` when done.
</output>
