---
phase: 155-ignite-flow
plan: 04
type: execute
wave: 3
depends_on: [155-02, 155-03]
files_modified:
  - commands/new-project.md
  - lib/core/mva-option-router.cjs
  - tests/test-mva-from-brief.cjs
  - tests/run-all-155.sh
autonomous: true
requirements: [GAP-4]
must_haves:
  truths:
    - "new-project.md has an --express branch that skips the 5-8 exchange conversation and goes directly to B2 blueprint from given material"
    - "STUB_MESSAGE_119 behavior is replaced: mva-option-router.cjs option 2 now reads the sha8 brief side-file and returns a structured result instead of the stub narrative"
    - "The --from-brief <sha8> path reads ~/.mindrian/mva/briefs/<sha8>.json and passes its content as the blueprint input to B2; the most-recent-brief auto-discovery also works"
    - "interactive_first_reward: instant_brief is honored: a directive entry that has not yet received a reward gets the MVA brief rendered BEFORE B2 (reward-before-investment per Decision 8)"
    - "venture_classified is verified to fire end-to-end on the f_selector_decision path (the write seam shipped in FIX-05; this plan verifies it and routes the nudge from [/mos:new-project] to [/mos:ignite])"
    - "STUB_MESSAGE_119 string no longer appears as the primary response for option 2 (the stub is replaced, not just wrapped)"
  artifacts:
    - path: "lib/core/mva-option-router.cjs"
      provides: "option 2 unstubbed: reads sha8 brief side-file, returns {action:'ignite_from_brief', brief_content, canon_verb:8, invoke_command:'/mos:ignite --from-brief <sha8>'}"
      contains: "ignite_from_brief"
    - path: "commands/new-project.md"
      provides: "--express branch (directive fast path) and --from-brief <sha8> route documented and wired"
      contains: "express"
    - path: "tests/test-mva-from-brief.cjs"
      provides: "hermetic HOME fixture test proving option 2 reads sha8 brief and returns ignite_from_brief action"
  key_links:
    - from: "lib/core/mva-option-router.cjs"
      to: "~/.mindrian/mva/briefs/<sha8>.json"
      via: "option 2 reads the brief side-file written by the Phase 118 orchestrator"
      pattern: "ignite_from_brief"
    - from: "commands/new-project.md"
      to: "lib/core/mva-option-router.cjs"
      via: "--from-brief <sha8> argument triggers option 2 path"
      pattern: "from-brief"
---

<objective>
Unstub MVA option 2 (replacing STUB_MESSAGE_119) and wire the --express directive fast path in new-project, honoring the reward-before-investment principle (Decision 8) and routing the venture_classified nudge to /mos:ignite.

Purpose: STUB_MESSAGE_119 at mva-option-router.cjs:62-65 has been a known placeholder since Phase 119. The Phase 118 orchestrator already writes brief side-files to ~/.mindrian/mva/briefs/<sha8>.json; the most-recent-brief auto-discovery at mva-option-router.cjs:112-120 already exists. Option 2 should read the brief and invoke /mos:ignite --from-brief <sha8> (not the old /mos:challenge-assumptions). The --express branch in new-project is the directive entry door: when the user says "make me a room for X" with enough material, skip the 5-8 conversation and feed the material directly into B2 as the blueprint input. The reward-before-investment invariant (Decision 8 / BIRTH-FLOW-BRIEF.md constraint 10): a directive entry that has NOT yet received an MVA brief reward gets the instant_brief FIRST, then B2. The venture_classified write seam shipped in FIX-05 (selector-decisions.cjs:179-190); this plan verifies end-to-end fire and repoints the nudge's [/mos:new-project] option to [/mos:ignite].

Output: unstubbed mva-option-router.cjs option 2, --express branch in new-project.md, venture_classified nudge repoint, hermetic test driver.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/155-ignite-flow/155-RESEARCH.md
@lib/core/mva-option-router.cjs
@lib/workflow/selector-decisions.cjs
@lib/core/venture-shape-nudge.cjs
@commands/new-project.md
@.planning/phases/155-ignite-flow/155-02-SUMMARY.md
@.planning/phases/155-ignite-flow/155-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unstub MVA option 2 (--from-brief) + --express branch in new-project.md (GAP-4)</name>
  <files>lib/core/mva-option-router.cjs, commands/new-project.md, tests/test-mva-from-brief.cjs</files>
  <read_first>
    - lib/core/mva-option-router.cjs:1-40 -- module header; understand STUB_MESSAGE_119 placement
    - lib/core/mva-option-router.cjs:62-86 -- STUB_MESSAGE_119 const + option 2 behavior object (action:'phase_119_stub', canon_verb:8); THIS IS WHAT GETS REPLACED
    - lib/core/mva-option-router.cjs:112-120 -- most-recent-brief auto-discovery (already exists; reuse it)
    - lib/core/mva-option-router.cjs:270-295 -- the actual routing logic calling option behaviors; understand how action is consumed
    - lib/workflow/selector-decisions.cjs:104,179-190 -- FIX-05 write seam: recordSelectorDecision stamps venture_classified + classification_source via mva-classifier.classify(); this IS shipped
    - lib/core/venture-shape-nudge.cjs:132-143 -- shouldSurfaceNudge reads venture_classified count; the nudge's command reference that needs repointing from /mos:new-project to /mos:ignite
    - commands/new-project.md -- the argument handling section (if any) and Step 2 / Step 3 conversation flow to understand where --express branches
    - docs/reward-before-investment-rule.md -- the canonical rule; Decision 8 / BIRTH-FLOW-BRIEF.md constraint 10
  </read_first>
  <behavior>
    - Test (hermetic HOME): create a tmp HOME with ~/.mindrian/mva/briefs/abc12345.json containing {venture_summary:'Test venture', domains:['biotech'], jtbd:'When I explore biotech...'}; call the option 2 routing path with sha8='abc12345'; assert result.action === 'ignite_from_brief'
    - Test: result.invoke_command === '/mos:ignite --from-brief abc12345'
    - Test: result.brief_content contains the venture_summary from the side-file
    - Test: STUB_MESSAGE_119 narrative does NOT appear in result (the stub is replaced)
    - Test: when no sha8 is given, the most-recent-brief auto-discovery returns the most recently written brief (reuse existing logic at lines 112-120)
    - Test: when ~/.mindrian/mva/briefs/ is empty or missing, option 2 gracefully falls back with action:'no_brief_available' and a user-facing message explaining how to create a brief via /mos:mva-brief
    - Test (static): grep commands/new-project.md for 'express' returns >= 1 (the --express branch is documented)
    - Test (static): grep lib/core/mva-option-router.cjs for 'STUB_MESSAGE_119' as the primary response returns 0 (replaced)
  </behavior>
  <action>
    Edit lib/core/mva-option-router.cjs option 2 behavior: replace the {action:'phase_119_stub', narrative:STUB_MESSAGE_119} object with a real handler function. The handler: (1) resolves the sha8 from caller args (if provided) or runs the most-recent-brief auto-discovery at lines 112-120; (2) if no brief found, returns {action:'no_brief_available', canon_verb:8, message:'No MVA brief found. Run /mos:mva-brief first to generate one.'}; (3) reads the brief JSON from ~/.mindrian/mva/briefs/<sha8>.json; (4) returns {action:'ignite_from_brief', brief_content: parsed JSON, sha8, canon_verb:8, invoke_command:'/mos:ignite --from-brief '+sha8}. The STUB_MESSAGE_119 const may remain as a code comment or be removed; it must NOT be the returned narrative for option 2. Keep any docstring that explains the Phase 119 contract claim -- "This plan claims the Phase 119 contract per BIRTH-FLOW-BRIEF.md Section 7 Gap 4."

    Reward-before-investment enforcement: in the option 2 routing path, before returning ignite_from_brief, check if the caller has already received an MVA brief reward (look for a flag in the brief JSON or a recent mva_brief_shown memory_event). If no reward has been delivered yet AND interactive_first_reward is set, prepend an instant brief summary to the result (brief_reward_pending:true in the return object). The /mos:ignite command (Plan 06) reads this flag and renders the brief before B2. Comment: "// OPEN-Q8: directive-vs-reward -- instant_brief runs first per Decision 8 until navigator rules otherwise."

    Edit commands/new-project.md to document the --express branch and --from-brief <sha8> argument. Add a section near the top (argument handling, before Step 1) explaining: --express skips the 5-8 conversation and uses the current session context as blueprint input, jumping directly to B2; --from-brief <sha8> reads the specified brief side-file and uses it as blueprint input, also jumping to B2. Both paths still run B2 (no gate is skipped). Both paths honor reward-before-investment: if no MVA brief has been shown yet, render one from the brief/context before B2.

    Repoint the venture_classified nudge: find the string '/mos:new-project' in lib/core/venture-shape-nudge.cjs (the nudge option that fires when shouldSurfaceNudge returns true) and replace it with '/mos:ignite'. Add a comment: "// Repointed from /mos:new-project to /mos:ignite per Phase 155-04 (the Ignite Flow is now the birth front door)."

    Write tests/test-mva-from-brief.cjs as a hermetic driver: use process.env.HOME = tmpDir, create the briefs/ directory structure, write a fixture brief JSON, call the option 2 router, assert all return fields. Test the fallback path (empty briefs dir). Test the reward-before-investment flag. Use the check/pass counter pattern; no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-mva-from-brief.cjs && bash tests/run-all-155.sh && bash tests/run-all-148.sh && bash tests/test-no-bespoke-brain-prompts.sh</automated>
  </verify>
  <done>STUB_MESSAGE_119 is no longer the primary option 2 response. --from-brief reads the sha8 brief and returns ignite_from_brief. --express branch is documented in new-project.md. The venture_classified nudge points to /mos:ignite. Bespoke-prompt tripwire stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.mindrian/mva/briefs/<sha8>.json -> mva-option-router | The brief JSON is written by the Phase 118 orchestrator; reading it back crosses user venture content into the routing path |
| brief_content -> B2 blueprint | Venture content from the brief becomes the prefilled blueprint shown to the user at B2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-04-01 | Tampering | brief JSON path traversal (sha8 arg used to construct file path) | mitigate | Validate sha8 matches /^[0-9a-f]{8}$/ before path construction; reject any value containing '/', '.', or whitespace; brief files are always in the fixed briefs/ directory |
| T-155-04-02 | Information Disclosure | brief_content (venture text) reaching Brain | mitigate | brief_content is LOCAL-only; the ignite_from_brief action feeds into B2 (local gate) and birthRoom (local transaction); Part 8 boundary applies; any Brain call from ignite uses only generic handles extracted from the brief, never the raw text |
| T-155-04-03 | Tampering | malformed brief JSON causing unhandled exception | mitigate | Wrap brief file read+parse in try/catch; on parse error return {action:'brief_parse_error', message} so the caller can surface a helpful message |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages |
</threat_model>

<verification>
- `node tests/test-mva-from-brief.cjs` -- sha8 brief read, ignite_from_brief action, fallback path, reward flag
- `grep -c 'STUB_MESSAGE_119' lib/core/mva-option-router.cjs` should return 0 if fully replaced (or 1 if kept as a comment label only -- assert the narrative is NOT returned)
- `grep -c 'ignite_from_brief' lib/core/mva-option-router.cjs` returns >= 1
- `grep -c 'express' commands/new-project.md` returns >= 1
- `grep -c '/mos:ignite' lib/core/venture-shape-nudge.cjs` returns >= 1 (nudge repointed)
- `bash tests/run-all-155.sh` -- phase gate green
- `bash tests/run-all-148.sh` -- regression floor 18/18
- `bash tests/test-no-bespoke-brain-prompts.sh` -- bespoke-prompt tripwire green
- `grep -rn "\-\-" lib/core/mva-option-router.cjs tests/test-mva-from-brief.cjs | grep -v '//'` returns zero (no em-dashes)
</verification>

<success_criteria>
- STUB_MESSAGE_119 is no longer the primary response for option 2; ignite_from_brief action returned
- --from-brief <sha8> reads the Phase 118 brief side-file; graceful fallback when no brief exists
- --express branch documented in new-project.md
- Reward-before-investment honored (brief_reward_pending flag in result)
- venture_classified nudge repointed to /mos:ignite
- run-all-155.sh green; run-all-148.sh 18/18
</success_criteria>

<output>
Create `.planning/phases/155-ignite-flow/155-04-SUMMARY.md` when done.
</output>
