---
phase: 155-ignite-flow
plan: 06
type: execute
wave: 4
depends_on: [155-01, 155-02, 155-03, 155-04, 155-05]
files_modified:
  - commands/ignite.md
  - commands/new-project.md
  - commands/onboard.md
  - commands/rooms.md
  - data/command-registry.json
  - data/connector-registry.json
  - docs/CONNECTOR-CONTRACT.md
autonomous: true
requirements: [GAP-6]
must_haves:
  truths:
    - "commands/ignite.md exists with valid frontmatter passing both build-connector-registry --check and build-command-registry --check (CONN-03: reach_id in frozen 6, posture in frozen 3)"
    - "ignite.md connector block has reach_id:'context_block', posture:'push_forward', framework:null, filing:none, sensor_triggers:[SENS-01,SENS-06] -- matching the new-project precedent"
    - "ignite.md serves_jtbd and teaching fields are present (required or ranker excludes the command)"
    - "The B3 first-win gate is documented in ignite.md: in-room, full ranker path via closeReach/recordSelectorDecision, Mode A Brain-ranked or Tier-0 slate"
    - "Entry routing is complete: onboard Step 6, rooms-new, discover, and MVA option 2 all route to /mos:ignite (not to /mos:new-project directly)"
    - "CONNECTOR-CONTRACT.md frozen-5 prose is corrected to frozen-6 (doc drift fix per RESEARCH Atlas 7 / Pitfall 9)"
    - "data/command-registry.json and data/connector-registry.json are regenerated and committed; the pre-commit --check tripwires pass"
    - "Tri-Polar degradation scripts are present for B1, B2, and B3 in ignite.md"
    - "tests/test-no-bespoke-brain-prompts.sh stays green: no bespoke patterns in ignite.md or any new code file"
  artifacts:
    - path: "commands/ignite.md"
      provides: "the /mos:ignite front-door orchestrator command; three entry doors; B1/B2/B3 gates; Hooked first-cycle assembly; CONN-03-passing connector block"
      min_lines: 80
    - path: "data/command-registry.json"
      provides: "regenerated registry including ignite with jtbd_label/jtbd_summary derived from serves_jtbd"
      contains: "ignite"
    - path: "data/connector-registry.json"
      provides: "regenerated registry including ignite connector block; --check passes"
      contains: "ignite"
    - path: "docs/CONNECTOR-CONTRACT.md"
      provides: "frozen-5 prose corrected to frozen-6 (doc drift from Phase 148)"
  key_links:
    - from: "commands/ignite.md"
      to: "lib/core/navigation/room-birth.cjs"
      via: "the birth transaction called after B2 Approve"
      pattern: "birthRoom"
    - from: "commands/onboard.md"
      to: "commands/ignite.md"
      via: "Step 6 routes to /mos:ignite instead of proceeding inline"
      pattern: "ignite"
    - from: "commands/rooms.md"
      to: "commands/ignite.md"
      via: "rooms-new path routes to /mos:ignite"
      pattern: "ignite"
    - from: "scripts/build-connector-registry.cjs"
      to: "commands/ignite.md"
      via: "--check validates ignite connector frontmatter against frozen 6 reach_ids and 3 posture_ids"
      pattern: "ignite"
---

<objective>
Ship the /mos:ignite front-door orchestrator command, wire all four entry doors to route into it, add the B3 first-win gate, fix the CONNECTOR-CONTRACT.md frozen-5 doc drift, and regenerate both registries so the pre-commit --check tripwires pass.

Purpose: This is the capstone plan -- it composes all prior plans into the Ignite Flow front door. Commands/ignite.md is the ONE command users (and the routed surfaces) invoke. It must pass CONN-03 (connector frontmatter in frozen 6 reach_ids + frozen 3 posture_ids), the command-registry --check (requires serves_jtbd-derived jtbd_summary AND teaching field), and the bespoke-prompt tripwire (no '(RECOMMENDED)', 'continue', 'Pick one to ' patterns). The B3 first-win gate is now IN-room (room.db exists after Plan 02's birthRoom), so it uses the full ranker path: closeReach/recordSelectorDecision, Mode A Brain-ranked at >= 0.70 threshold or Tier-0 slate, SENS-01 cold-card framing as the S4 newborn-room voice. Entry routing closes the five-overlapping-front-doors problem: onboard Step 6, rooms-new, discover, and MVA option 2 all route to /mos:ignite. CONNECTOR-CONTRACT.md still says "frozen 5" (doc drift from Phase 148 which added 'hats' as the 6th reach_id per Canon Appendix D entry 15); fix it in passing per RESEARCH Pitfall 9.

Output: commands/ignite.md, entry routing edits to onboard/rooms/discover, regenerated registries, CONNECTOR-CONTRACT.md drift fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/155-ignite-flow/155-RESEARCH.md
@.planning/research/new-room-onboarding-20260612/BIRTH-FLOW-BRIEF.md
@commands/new-project.md
@commands/onboard.md
@commands/rooms.md
@commands/discover.md
@lib/hmi/selector-dispatcher.cjs
@lib/workflow/f-selector-ranker.cjs
@lib/workflow/dial-close-reach.cjs
@scripts/build-connector-registry.cjs
@scripts/build-command-registry.cjs
@docs/CONNECTOR-CONTRACT.md
@lib/core/sensors/sensor-types.cjs
@.planning/phases/155-ignite-flow/155-01-SUMMARY.md
@.planning/phases/155-ignite-flow/155-02-SUMMARY.md
@.planning/phases/155-ignite-flow/155-03-SUMMARY.md
@.planning/phases/155-ignite-flow/155-04-SUMMARY.md
@.planning/phases/155-ignite-flow/155-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: commands/ignite.md -- the front-door orchestrator with CONN-03-passing connector block (GAP-6 front door)</name>
  <files>commands/ignite.md, docs/CONNECTOR-CONTRACT.md</files>
  <read_first>
    - commands/new-project.md:17-28 -- the no-framework connector block precedent (framework:null, filing:none, reach_id:context_block, sub_mode:new-project, posture:push_forward, sensor_triggers:[SENS-01,SENS-06]) to copy as the ignite template
    - commands/onboard.md:348-367 -- the F.0 D-12 LOCKED block (the onboard Step 6 gate structure; Step 6 is what routes OUT to ignite)
    - lib/core/sensors/sensor-types.cjs -- REACH_IDS frozen 6 and POSTURE_IDS frozen 3 (the exact values build-connector-registry --check enforces)
    - scripts/build-command-registry.cjs:55-68,200-209 -- how teaching and serves_jtbd -> jtbd_label/jtbd_summary are derived (REQUIRED for ranker inclusion)
    - lib/workflow/f-selector-ranker.cjs:403-411 -- ranker fail-closed: commands missing jtbd_summary or teaching are EXCLUDED from every ranked slate
    - lib/hmi/jtbd-taxonomy.json -- the valid serves_jtbd taxonomy keys ('explore', 'build', 'validate', etc.); ignite serves 'explore' + 'build'
    - docs/CONNECTOR-CONTRACT.md -- find the "frozen 5" prose to correct to "frozen 6"; RESEARCH Pitfall 9
    - .planning/research/new-room-onboarding-20260612/BIRTH-FLOW-BRIEF.md:82-90,110-119 -- B2 (already in new-project via Plan 01) and B3 gate spec
    - tests/test-no-bespoke-brain-prompts.sh:30-50 -- the 5 named consumers + any-new-file scan for '(RECOMMENDED)', 'continue', 'Pick one to '
  </read_first>
  <action>
    Create commands/ignite.md as the /mos:ignite front-door orchestrator. Structure:

    FRONTMATTER (YAML, must pass CONN-03 and command-registry --check):
    ```
    name: ignite
    description: "Start or excavate a room through one front door."
    help_jtbd: "Start (or excavate) a room through one front door."
    body_shape: E
    argument-hint: "[--express | --from-brief <sha8> | --from-opportunity <ref>]"
    serves_jtbd: ["explore", "build"]
    teaching: "Larry walks you through three birth gates -- starting point, blueprint approve, first win -- so every new room begins with a clear JTBD, the right section set, and one bankable opportunity."
    interactive_first_reward: instant_brief
    allowed-tools: [Read, Write, Bash, Glob]
    connector:
      connects_to_spine: true
      sensor_triggers: [SENS-01, SENS-06]
      reach_id: context_block
      sub_mode: ignite
      framework: null
      posture: push_forward
      hierarchy_rank: 10
      filing: none
      plan_gated: false
      web_scope: null
    ```
    Note: reach_id 'context_block' is in the frozen 6 (RESEARCH Q5 confirmed); posture 'push_forward' is in the frozen 3. teaching is 50-300 chars, Larry voice, hyphens only (no em-dashes). serves_jtbd uses valid taxonomy keys. ONE connector block only (Part 7 / MOAT rule). canon_parts goes in 155-CONTEXT.md frontmatter, NOT in the command file (RESEARCH Q5 note).

    BODY (prose orchestration instructions):

    ## Entry Routing
    Three entry doors per BIRTH-FLOW-BRIEF.md Section 2:
    A. Just Talk -- operator JUST_TALK/EXPLORE_CAPTURE; extract conversationally, journal to scratchpad; gates fire after operator transition.
    B. Directive -- --express (skip conversation, go to B2), --from-brief <sha8> (read Phase 118 brief side-file via mva-option-router option 2 per Plan 04), or "make me a room for X" imperative.
    C. Umbilical -- /mos:ignite --from-opportunity <ref> (U0 on-demand path); arrival_asset=excavated-opportunity; B1 skipped; B2 prefilled from parent.

    Reward-before-investment (Decision 8 / constraint 10): if the session has not yet delivered an MVA brief reward, render the instant brief FIRST before B1. Check mva-option-router brief_reward_pending flag (Plan 04). Cite docs/reward-before-investment-rule.md.

    ## Gate B1 -- Starting Point (F.1, pre-room)
    Instruct pickShape with F.1 shape, payload.verbs for the three arrival_asset options (from BIRTH-FLOW-BRIEF.md Section 2):
    - option A alias: "A solution looking for its problem" -> arrival_asset='solution-looking-for-problem', blueprintFamily='solution-first'
    - option B alias: "A domain or interest to explore" -> arrival_asset='domain-or-interest', blueprintFamily='exploration'
    - option C alias: "A defined venture or business case" -> arrival_asset='defined-venture', blueprintFamily='venture'
    F.1 trailer appends Free-Text last automatically. Journal answer via writeScratchpadBirthAnswer({gate_id:'B1',...}).
    Directive / --express bypasses B1 if arrival_asset is determinable from context; shows B1 only if ambiguous.
    Tri-Polar Desktop degradation: "Which best describes what you are arriving with? (a) solution, (b) domain or interest, (c) defined venture -- type a, b, or c."

    ## Gate B2 -- Blueprint (F.0, pre-room -- THE Part 9 promotion moment)
    This gate is already documented in commands/new-project.md (Plan 01 wired it). Ignite delegates to new-project's B2 block by calling the new-project scaffold backend. Reference: "See commands/new-project.md B2 gate (Phase 155-01). Ignite passes blueprintFamily from B1 into new-project's scaffold call."
    B2 Approve = call birthRoom({...opts, approvedBy: resolveByUser(roomDir)}) from lib/core/navigation/room-birth.cjs (Plan 02).
    B2 Reject = capture reason, revise blueprint, re-render B2 (the Adjust loop per RESEARCH Q3 option a).
    B2 Defer = writeScratchpadBirthAnswer({gate_id:'B2', canonical_verb:'Defer',...}); exit gracefully.
    Nugget routing table HARD RULE: B2 must display the nugget | target section | why table BEFORE the Approve/Reject/Defer choice. Nothing files until the table is approved. (Jonathan HARD RULE 2026-06-11, constraint 11.)

    ## Gate B3 -- First Win (F.1, IN-ROOM with room.db + focus -- full ranker path)
    After birthRoom succeeds (room.db exists, focus set, registry flipped):
    Call pickShape with F.1 shape. The dial is now LIVE (room.db exists): use closeReach/recordSelectorDecision for the user's B3 pick -- this writes SELECTED_REACH + a memory_event. Mode A (Brain reachable, confidence >= 0.70): ranked slate with RECOMMENDED marker on the top reach. Tier-0 slate (Brain unreachable): Run Methodology / Reformulate / Free-Text.
    Header: "birth - FIRST MOVE - decision gate / LOCAL context: room seeded with [N] claims, focus set / Choose next reach:"
    Standing footer rows: "File these findings" (verb 8: Bank Opportunity), "Brain review" (Phase 148 pattern).
    SENS-01 cold-card framing is the sanctioned newborn-room voice (S4 dial-presenter state).
    Tri-Polar Desktop degradation: render as text list with "Type the letter of your choice."

    ## Hooked First-Cycle Assembly (Decision 8 standing product rule)
    Cite docs/reward-before-investment-rule.md. The ignite flow IS a complete Hook cycle: trigger (install / U0 nudge) -> minimal action (B1 arrival_asset pick) -> VARIABLE reward (MVA brief / domain insight sweep) -> investment (B2 Approve -> room exists -> B3 loads the next trigger). Every step ends at an F.1 offering to Bank Opportunity so the win persists.

    Fix docs/CONNECTOR-CONTRACT.md: find the "frozen 5" prose (the doc drift from Phase 148 per RESEARCH Pitfall 9 / Atlas 7) and replace with "frozen 6 (hats added Phase 148, Canon Appendix D entry 15)." This is a passing doc-fix only.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/build-command-registry.cjs --check && bash tests/test-no-bespoke-brain-prompts.sh</automated>
  </verify>
  <done>commands/ignite.md exists with CONN-03-passing connector block. build-connector-registry --check passes. build-command-registry --check passes (jtbd_summary and teaching present). Bespoke-prompt tripwire stays green. CONNECTOR-CONTRACT.md frozen-5 corrected to frozen-6.</done>
</task>

<task type="auto">
  <name>Task 2: Entry routing (onboard/rooms/discover -> /mos:ignite) + registry regeneration (GAP-6 routing closure)</name>
  <files>commands/onboard.md, commands/rooms.md, commands/new-project.md, data/command-registry.json, data/connector-registry.json, tests/run-all-155.sh</files>
  <read_first>
    - commands/onboard.md:340-380 -- the onboard Step 6 completion block; currently proceeds inline to new-project; add a routing instruction to /mos:ignite
    - commands/rooms.md:154-228 -- the rooms-new path; currently points back at new-project; add routing to /mos:ignite
    - commands/discover.md:170-190 -- the discover command's new-room exit; add routing to /mos:ignite (BIRTH-FLOW-BRIEF.md: discover routes INTO ignite, not the other way)
    - commands/new-project.md -- add a note at the top (or in the argument section) that /mos:ignite is now the canonical front door; new-project continues to work as the scaffold backend but users should invoke /mos:ignite instead
    - scripts/build-connector-registry.cjs:51-56 -- the REACH_IDS frozen 6 and how --check works
    - scripts/build-command-registry.cjs:200-209 -- the registry write; run after ignite.md exists
    - data/command-registry.json, data/connector-registry.json -- the files to regenerate (committed; stale registry fails pre-commit per CONN-03 and command-registry --check)
  </read_first>
  <action>
    Edit commands/onboard.md Step 6: the current Step 6 F.0 gate (D-12 LOCKED) was already the canonical birth gate pattern. Add a routing note after the Approve path: "On Approve, invoke /mos:ignite with the context gathered so far (persona signals, domain, JTBD). Pass blueprintFamily from B1 if already determined. Ignite takes over the birth transaction (Plan 02 birthRoom)." This replaces any inline new-project invocation in onboard's Step 6 Approve path.

    Edit commands/rooms.md rooms-new path (lines 154-228): replace "points back at new-project" behavior with a direct route to /mos:ignite. The instruction: "Route to /mos:ignite. Pass any name/slug the user provided. Ignite runs B1 (starting-point gate) unless arrival_asset is determinable from context."

    Edit commands/discover.md at the new-room exit point: add "Route to /mos:ignite at the birth moment (after the client discovery session has gathered context). Pass the Discovery Brief content as --from-brief context or as pre-filled B2 blueprint material. Discover is the conversation; ignite is the birth."

    Edit commands/new-project.md (top section / argument preamble): add a note "Note: /mos:ignite is the canonical front door for new room creation. /mos:new-project is the scaffold backend invoked by ignite. Direct invocation of /mos:new-project continues to work but users are encouraged to use /mos:ignite for the full Hooked first-cycle experience."

    Regenerate data/command-registry.json: run node scripts/build-command-registry.cjs (without --check flag; generates the file). Verify ignite appears with jtbd_label and jtbd_summary fields.

    Regenerate data/connector-registry.json: run node scripts/build-connector-registry.cjs (without --check flag; generates the file). Verify ignite's connector block appears with reach_id:context_block, posture:push_forward.

    Run both --check tripwires to confirm the regenerated files pass: node scripts/build-connector-registry.cjs --check && node scripts/build-command-registry.cjs --check.

    Extend tests/run-all-155.sh to include the final phase-gate verification steps: both --check tripwires, the bespoke-prompt scan, and the carried fence aggregators (run-all-148.sh, run-all-150.5.sh, run-all-150.8.sh). This makes run-all-155.sh the complete one-command phase gate for the entire Phase 155.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/build-command-registry.cjs --check && bash tests/test-no-bespoke-brain-prompts.sh && bash tests/run-all-155.sh && bash tests/run-all-148.sh && bash tests/run-all-150.5.sh && bash tests/run-all-150.8.sh</automated>
  </verify>
  <done>onboard Step 6, rooms-new, and discover all route to /mos:ignite. Both registries regenerated and committed; --check tripwires pass. run-all-155.sh is the complete phase gate covering all six plans. All carried regressions hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entry routing (onboard/rooms/discover) -> ignite | Command dispatch crossing surface context into the birth orchestrator |
| ignite.md connector block -> connector-registry | Frontmatter read by the registry generator; malformed values would cause --check failure |
| B3 closeReach -> room.db | The first in-room decision gate writes SELECTED_REACH; room.db must exist before B3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-06-01 | Elevation of Privilege | B3 closeReach called before room.db exists (Part 9 violation) | mitigate | B3 is gated on birthRoom success (ok:true); closeReach is only called after Plan 02's STEP 4 registry flip confirms room is live; if birthRoom returns ok:false, B3 does not render |
| T-155-06-02 | Tampering | connector frontmatter drift (stale registry fails pre-commit) | mitigate | Both --check tripwires run in the same commit that adds ignite.md; regeneration and check are part of this plan's Task 2 |
| T-155-06-03 | Elevation of Privilege | ignite.md importing chain-recommender or rankForSelector directly (bespoke-prompt tripwire) | mitigate | B3 uses pickShape via the selector-dispatcher chokepoint (the SEED-020 single construction door); ignite.md itself does not import ranker directly; test-no-bespoke-brain-prompts.sh scans new files |
| T-155-06-04 | Information Disclosure | Brain egress during B3 ranked slate (Mode A Brain call) | mitigate | Brain receives only generic framework handles + phase identifiers + problem-type enums (Part 8 packet contract, Phase 110); the dial-close-reach path is the established safe channel; no user content crosses the boundary |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages |
</threat_model>

<verification>
- `node scripts/build-connector-registry.cjs --check` -- ignite passes CONN-03 (reach_id in frozen 6, posture in frozen 3)
- `node scripts/build-command-registry.cjs --check` -- ignite has jtbd_summary and teaching (ranker will include it)
- `bash tests/test-no-bespoke-brain-prompts.sh` -- no bespoke patterns in ignite.md or any modified file
- `grep -c 'ignite' commands/onboard.md` returns >= 1 (routing present)
- `grep -c 'ignite' commands/rooms.md` returns >= 1 (routing present)
- `grep -c 'ignite' commands/discover.md` returns >= 1 (routing present)
- `grep -c 'frozen.6\|frozen 6' docs/CONNECTOR-CONTRACT.md` returns >= 1 (drift fixed)
- `grep -c 'frozen.5\|frozen 5' docs/CONNECTOR-CONTRACT.md` returns 0 after fix (old prose gone)
- `grep -c 'ignite' data/command-registry.json` returns >= 1 (registry regenerated)
- `grep -c 'ignite' data/connector-registry.json` returns >= 1 (registry regenerated)
- `bash tests/run-all-155.sh` -- full phase gate green (all six plans)
- `bash tests/run-all-148.sh` -- regression floor 18/18
- `bash tests/run-all-150.5.sh && bash tests/run-all-150.8.sh` -- carried fences green
- `grep -rn "\-\-" commands/ignite.md | grep -v '^#' | grep -v '//'` returns zero (no em-dashes in command file)
</verification>

<success_criteria>
- commands/ignite.md: CONN-03 passing, ranker-eligible (jtbd_summary + teaching), three entry doors, B1/B2/B3 gates, Hooked first-cycle, Tri-Polar degradation for each gate, nugget routing table HARD RULE enforced at B2
- Entry routing: onboard Step 6, rooms-new, discover, MVA option 2 all route to /mos:ignite
- data/command-registry.json and data/connector-registry.json regenerated and committed; both --check tripwires pass
- CONNECTOR-CONTRACT.md frozen-5 -> frozen-6 drift corrected
- tests/run-all-155.sh is the complete one-command phase gate for Phase 155; all six plans' drivers included; carried regressions (148/150.5/150.8) included
</success_criteria>

<output>
Create `.planning/phases/155-ignite-flow/155-06-SUMMARY.md` when done.
</output>
