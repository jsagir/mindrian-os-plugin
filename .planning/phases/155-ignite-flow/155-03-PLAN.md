---
phase: 155-ignite-flow
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/user-md-ops.cjs
  - lib/core/shallow-doc-parser.cjs
  - commands/new-project.md
  - commands/onboard.md
  - tests/test-user-md-convergence.cjs
autonomous: true
requirements: [GAP-3, GAP-7]
must_haves:
  truths:
    - "writeUserMdAtomic is called by both new-project and onboard to write the same machine schema (role_blend, journey_stage, canonical_role) -- it has at least one production call path per surface"
    - "The dead setFocus call at shallow-doc-parser.cjs:149 is fixed: it now passes 4 args (db, sessionId, ventureNodeId, 'user') and 'user' is in VALID_SET_BY"
    - "The /mos:profile-user stub in 3 production room USER.md files is replaced by a clear description of how USER.md is populated (honest surface per RESEARCH Q6)"
    - "role_blend populated at birth flows into the USER.md machine schema via emptyUser() shape (7-axis struct from user-md-ops.cjs:76-102)"
    - "The round-trip test proves writeUserMdAtomic -> readUserMd preserves both frontmatter (machine schema) and human body"
  artifacts:
    - path: "lib/core/user-md-ops.cjs"
      provides: "writeUserMdAtomic production-caller comment updated; emptyUser() shape doc; no behavior change needed -- the function already exists"
      contains: "writeUserMdAtomic"
    - path: "lib/core/shallow-doc-parser.cjs"
      provides: "setFocus call at line 149 fixed: 4-arg signature, valid setBy='user'"
      contains: "setFocus"
    - path: "tests/test-user-md-convergence.cjs"
      provides: "round-trip test for writeUserMdAtomic + readUserMd; setFocus arity check"
  key_links:
    - from: "commands/new-project.md"
      to: "lib/core/user-md-ops.cjs"
      via: "writeUserMdAtomic called in Step 3 (user profile) using the emptyUser() shape, populated from extraction"
      pattern: "writeUserMdAtomic"
    - from: "lib/core/shallow-doc-parser.cjs"
      to: "lib/core/navigation/focus.cjs"
      via: "setFocus(db, sessionId, ventureNodeId, 'user') -- 4 args, valid setBy"
      pattern: "setFocus"
---

<objective>
Converge USER.md to a single machine schema written by writeUserMdAtomic on both new-project and onboard, fix the live dead-code bug at shallow-doc-parser.cjs:149 (wrong-arity setFocus that is silently swallowed), and ship the smallest honest surface for /mos:profile-user.

Purpose: Three problems compose this plan. First, writeUserMdAtomic has ZERO production callers today (RESEARCH Atlas 3 -- verified). The ignite birth transaction (Plan 02 STEP 1) needs USER.md written before confirmNode so resolveByUser finds identity; this plan wires the call in new-project and onboard so the schema is consistent. Second, the setFocus bug at shallow-doc-parser.cjs:149 is load-bearing for Plan 02: the upload path's focus-set is dead code today (3 args against a 4-arg surface, invalid setBy 'auto-from-upload' not in VALID_SET_BY, try/catch swallows it silently -- RESEARCH Pitfall 1). Birth depends on upload-path extraction being real. Third, three production rooms carry a USER.md stub pointing at /mos:profile-user which does not exist; the smallest honest surface is to either ship a minimal /mos:profile-user or replace the stub with an accurate description.

This plan is INDEPENDENT of Plan 02 (wave 1, parallel). Both touch new-project.md but at different sections (Plan 01 touches the gate block at line 103; this plan touches the Step 3 user-profile section). Coordinate: this plan adds the writeUserMdAtomic call in Step 3; Plan 02's birthRoom STEP 1 also writes USER.md. They must write the same schema. The authority is user-md-ops.cjs emptyUser() (7-axis role_blend struct). Plan 02 defers to this plan's schema definition.

Output: fixed shallow-doc-parser.cjs, writeUserMdAtomic wired in both command surfaces, honest /mos:profile-user stub replacement, one convergence test driver.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/155-ignite-flow/155-RESEARCH.md
@lib/core/user-md-ops.cjs
@lib/core/shallow-doc-parser.cjs
@lib/core/navigation/focus.cjs
@commands/new-project.md
@commands/onboard.md
</context>

<artifacts_this_phase_produces>
Schema contract for Plan 02:
- The USER.md machine schema written by writeUserMdAtomic: {canonical_role, role_blend:{founder,researcher,operator,investor,mentor,domain_expert,student}, journey_stage, first_seen, last_updated}
- emptyUser() is the authoritative zero-state; detectPersonaUpdate() populates role_blend from signals
- shallow-doc-parser.cjs now correctly sets focus on the upload path (4-arg setFocus with 'user' setBy)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix dead setFocus call in shallow-doc-parser.cjs + writeUserMdAtomic convergence wiring (GAP-3, GAP-7)</name>
  <files>lib/core/shallow-doc-parser.cjs, lib/core/user-md-ops.cjs, commands/new-project.md, commands/onboard.md, tests/test-user-md-convergence.cjs</files>
  <read_first>
    - lib/core/shallow-doc-parser.cjs:140-155 -- the dead setFocus call (line 149): calls navigation.setFocus(sessionId, ventureNodeId, 'auto-from-upload') -- 3 args against the 4-arg surface (db, sessionId, nodeId, setBy); 'auto-from-upload' not in VALID_SET_BY; try/catch on line 147 swallows the error silently -- RESEARCH Pitfall 1
    - lib/core/navigation/focus.cjs:13,36-43 -- setFocus(db, sessionId, nodeId, setBy) signature; VALID_SET_BY set (must include 'user' -- confirm it does)
    - lib/core/user-md-ops.cjs:76-102 -- emptyUser() returns the 7-axis role_blend struct; keys: founder, researcher, operator, investor, mentor, domain_expert, student (all 0.0 default)
    - lib/core/user-md-ops.cjs:374-400 -- writeUserMdAtomic(userMdPath, data): atomic frontmatter write, preserves human body; ZERO production callers today (comment confirming this is the FIRST production caller)
    - lib/core/user-md-ops.cjs:487-513 -- detectPersonaUpdate() hysteresis and first_detection always-writes rule
    - commands/new-project.md -- Step 3 (user profile section) where the writeUserMdAtomic call is inserted; the exact line range varies; read the full Step 3 section
    - commands/onboard.md:340-370 -- the onboard user-writing section; where the convergent writeUserMdAtomic call is inserted
    - lib/memory/user-md-persona.test.cjs -- existing test to EXTEND (not replace) per RESEARCH Req Map GAP-3
  </read_first>
  <behavior>
    - Test: writeUserMdAtomic called with {canonical_role:'navigator', role_blend:{founder:0.6, researcher:0.4, operator:0.0, investor:0.0, mentor:0.0, domain_expert:0.0, student:0.0}, journey_stage:'Crossing the Threshold', first_seen:isoDate} writes a USER.md whose frontmatter round-trips via readUserMd with all 7 role_blend keys intact
    - Test: writeUserMdAtomic preserves any human-authored body section below the frontmatter (byte-preserving)
    - Test: the fixed extractShallow correctly calls setFocus with 4 arguments (db, sessionId, ventureNodeId, 'user'); calling it with the old 3-arg form returns an error from navigation.setFocus (not silently swallowed)
    - Test: 'user' is in VALID_SET_BY (assert navigation.setFocus with valid args succeeds; with 'auto-from-upload' fails with reason 'invalid_set_by')
    - Test: new-project.md contains the string 'writeUserMdAtomic' (static grep -- confirms the call is wired)
    - Test: onboard.md contains the string 'writeUserMdAtomic' (static grep)
  </behavior>
  <action>
    Fix lib/core/shallow-doc-parser.cjs line 149: change the dead 3-arg call to the correct 4-arg form. The fix requires passing the open db handle. Inspect extractShallow(text, sessionId) to see if it receives a db handle; if not, the db must be opened (use openRoomDbForCaller or accept db as a parameter -- prefer accepting db as an optional parameter to avoid creating a new db dependency inside this module). The fixed call: navigation.setFocus(db, sessionId, ventureNodeId, 'user'). The setBy 'user' is correct for an explicit upload action (the user pasted the document; this IS a user-initiated focus change). Remove or update the comment that previously described the wrong behavior. Ensure the try/catch is kept for genuine errors but add a log line on catch so silent swallowing is eliminated: console.warn('[shallow-doc-parser] setFocus failed:', e.message).

    Update lib/core/user-md-ops.cjs: remove the "ZERO production callers" comment and replace it with "Production callers: commands/new-project.md Step 3 (first production caller, Phase 155-03), commands/onboard.md user-profile section (Phase 155-03), lib/core/navigation/room-birth.cjs STEP 1 (Phase 155-02)." This is documentation only; the function body is unchanged.

    Edit commands/new-project.md Step 3 (the user profile / persona section, which currently extracts role signals conversationally but never writes USER.md): add a prose instruction to call writeUserMdAtomic with the emptyUser() base populated from the conversation's extracted signals. Specify the schema fields exactly: canonical_role (from conversation-mode detection or 'navigator' as default), role_blend (7-axis struct from detectPersonaUpdate or emptyUser() if signals are thin), journey_stage (inferred from context or 'Ordinary World' if unclear), first_seen (ISO date). State explicitly: "Use writeUserMdAtomic from lib/core/user-md-ops.cjs. Do NOT write USER.md as freeform prose. The machine schema must be present before room creation so resolveByUser (called at the Plan 02 STEP 2 confirmNode batch) finds identity."

    Edit commands/onboard.md similarly: locate the section that writes USER.md (the current onboard writes a divergent schema per BIRTH-FLOW-BRIEF.md Section 1). Replace the divergent schema instruction with the same writeUserMdAtomic call with identical field set. The onboard and new-project USER.md writes must be schema-identical.

    For /mos:profile-user: the three production rooms carry a stub pointing at a command that does not exist. RESEARCH Q6 flags this as an open question -- "ship the smallest honest surface." The honest answer for Phase 155: add a comment in user-md-ops.cjs and a short note in commands/new-project.md Step 3 that says "USER.md is populated by writeUserMdAtomic at birth. To update your profile, re-run /mos:ignite or edit USER.md frontmatter directly. The /mos:profile-user command is deferred to a successor phase." Remove the dead stub from production room USER.md files by replacing the stub reference with this explanation. (The three rooms are in ~/.mindrian or ROOMS_HOME -- scan for USER.md files containing '/mos:profile-user' and update them.)

    Write tests/test-user-md-convergence.cjs as a bare node CJS driver: use fs.mkdtempSync; write a fixture USER.md with a known role_blend frontmatter and human body section; call writeUserMdAtomic; call readUserMd; assert round-trip; assert body preserved. Also test the setFocus arity fix via static analysis: read shallow-doc-parser.cjs and assert the 3-arg pattern 'setFocus(sessionId' no longer appears (grep-style string search). No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-user-md-convergence.cjs && bash tests/run-all-155.sh && bash tests/run-all-148.sh</automated>
  </verify>
  <done>writeUserMdAtomic is called by both new-project and onboard. The dead 3-arg setFocus is fixed (4-arg, 'user' setBy, no silent swallow). The /mos:profile-user stub is replaced by an honest description. run-all-155.sh green. run-all-148.sh regression 18/18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user conversation signals -> USER.md frontmatter | Role_blend values inferred from untrusted conversation text cross into structured YAML |
| uploaded document text -> setFocus node selection | The shallow-doc-parser extraction crosses user document content into a graph focus pointer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-03-01 | Tampering | role_blend values smuggling non-numeric data into USER.md frontmatter | mitigate | writeUserMdAtomic validates the data shape; emptyUser() provides the zero-state type contract; detectPersonaUpdate() constrains to 0.0-1.0 float range |
| T-155-03-02 | Tampering | shallow-doc-parser setFocus with user-controlled ventureNodeId (node must already exist) | mitigate | setFocus returns 'unknown_node' if the node does not exist in room.db -- no fabrication possible |
| T-155-03-03 | Information Disclosure | USER.md machine schema (role_blend) reaching Brain | mitigate | Part 8 boundary: writeUserMdAtomic is LOCAL-only; confirmNode in Plan 02 is LOCAL; Brain packets carry only generic handles (Phase 110 contract); role_blend values are never in Brain query payloads |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; node built-ins only |
</threat_model>

<verification>
- `node tests/test-user-md-convergence.cjs` -- writeUserMdAtomic round-trip; body preserved; setFocus arity fix confirmed by static analysis
- `grep -c 'writeUserMdAtomic' commands/new-project.md` returns >= 1
- `grep -c 'writeUserMdAtomic' commands/onboard.md` returns >= 1
- `grep -c 'setFocus(sessionId' lib/core/shallow-doc-parser.cjs` returns 0 (old 3-arg form gone)
- `bash tests/run-all-155.sh` -- phase gate green
- `bash tests/run-all-148.sh` -- regression floor 18/18
- `grep -rn "\-\-" lib/core/shallow-doc-parser.cjs lib/core/user-md-ops.cjs tests/test-user-md-convergence.cjs | grep -v '//'` returns zero (no em-dashes)
</verification>

<success_criteria>
- shallow-doc-parser.cjs:149 fixed: 4-arg setFocus(db, sessionId, ventureNodeId, 'user'); try/catch no longer swallows silently
- writeUserMdAtomic called by both command surfaces with the emptyUser() 7-axis schema
- /mos:profile-user stub replaced by honest deferred description in all three production rooms
- test-user-md-convergence.cjs passes; run-all-155.sh green; run-all-148.sh 18/18
</success_criteria>

<output>
Create `.planning/phases/155-ignite-flow/155-03-SUMMARY.md` when done.
</output>
