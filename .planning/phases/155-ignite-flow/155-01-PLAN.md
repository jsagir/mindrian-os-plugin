---
phase: 155-ignite-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/new-project.md
  - lib/core/scratchpad-ops.cjs
  - tests/test-new-project-b2-gate.cjs
  - tests/test-scratchpad-birth-answers.cjs
  - tests/run-all-155.sh
autonomous: true
requirements: [GAP-1]
must_haves:
  truths:
    - "commands/new-project.md contains a pickShape F.0 blueprint gate that replaces the line-103 bare-prose confirmation; the phrase 'Wait for user confirmation before creating the room.' no longer appears outside a comment"
    - "scratchpad.json accepts a birth_gate_answers key that survives readScratchpad() re-normalization without loss"
    - "writeScratchpadBirthAnswer() journals {gate_id, option_key, canonical_verb, alias_label, free_text?, arrival_asset?, ts} entries under birth_gate_answers"
    - "readScratchpad() returns birth_gate_answers as an array (empty when absent), never silently drops it"
    - "The bespoke-prompt tripwire stays green: no 'continue', '(RECOMMENDED)', 'Pick one to' patterns appear in new-project.md or any new code file"
  artifacts:
    - path: "commands/new-project.md"
      provides: "B2 blueprint gate (F.0 Approve/Reject/Defer) replacing the line-103 prose; nugget routing table embedded in the blueprint display block"
      contains: "pickShape"
    - path: "lib/core/scratchpad-ops.cjs"
      provides: "birth_gate_answers in DEFAULT_SCRATCHPAD, readScratchpad, writeScratchpadBirthAnswer, drainBirthGateAnswers signature stub"
      contains: "birth_gate_answers"
    - path: "tests/run-all-155.sh"
      provides: "one-command phase gate (extended by later plans)"
  key_links:
    - from: "commands/new-project.md"
      to: "lib/hmi/selector-dispatcher.cjs"
      via: "pickShape call with payload.verbs for F.0 Approve/Reject/Defer"
      pattern: "pickShape"
    - from: "lib/core/scratchpad-ops.cjs"
      to: "lib/core/navigation/room-birth.cjs"
      via: "drainBirthGateAnswers called from inside the STEP 2 transaction (Plan 02)"
      pattern: "drainBirthGateAnswers"
---

<objective>
Replace the Canon Part 3 violation at new-project.md:103 with a proper F.0 blueprint gate (B2 per BIRTH-FLOW-BRIEF.md Section 2) and extend scratchpad-ops to journal gate answers that survive re-normalization.

Purpose: The bare-prose "Wait for user confirmation before creating the room" at line 103 is the same violation already fixed in onboard Step 6 (onboard.md:348-367, D-12 LOCKED). B2 is THE Part 9 promotion moment -- Approve = batch confirmNode on all extracted claims, byUser from USER.md. Without a typed gate here nothing downstream can record the navigator's approval as graph data. The scratchpad extension is the journaling half: answers must survive readScratchpad() re-normalization (RESEARCH Pitfall 2 -- the silently-drops-unknown-keys trap requires editing DEFAULT_SCRATCHPAD, readScratchpad, and the writer together).

Output: Updated commands/new-project.md with the B2 block, updated lib/core/scratchpad-ops.cjs with birth_gate_answers support, two unit-test drivers, and the phase-gate aggregator tests/run-all-155.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/155-ignite-flow/155-RESEARCH.md
@.planning/research/new-room-onboarding-20260612/BIRTH-FLOW-BRIEF.md
@commands/new-project.md
@lib/core/scratchpad-ops.cjs
@commands/onboard.md
@lib/hmi/selector-dispatcher.cjs
</context>

<artifacts_this_phase_produces>
New symbols produced for Plan 02 to consume:
- `writeScratchpadBirthAnswer(answer)` - exported from scratchpad-ops.cjs; entry shape: {gate_id:'B1'|'B2'|'U0', option_key, canonical_verb, alias_label, free_text?, arrival_asset?, ts}
- `drainBirthGateAnswers(db, roomDir)` - stub/signature exported now; Plan 02 fills the body (called inside the STEP 2 SQLite transaction)
- `readScratchpad()` now returns birth_gate_answers: [] reliably
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B2 blueprint gate in commands/new-project.md (GAP-1, per D-03 LOCKED)</name>
  <files>commands/new-project.md, tests/test-new-project-b2-gate.cjs</files>
  <read_first>
    - commands/onboard.md:348-367 -- the F.0 D-12 LOCKED block to copy verbatim as the structural template (the "onboard Step 6 LOCKED precedent" the RESEARCH names)
    - commands/new-project.md:95-115 -- the current prose confirmation at line 103 to excise; also read lines 150-175 (Step 4 room creation preamble) for context ordering
    - commands/new-project.md:17-28 -- the connector block (framework:null, filing:none precedent ignite will copy)
    - lib/hmi/selector-dispatcher.cjs:664-690 -- F.0 sub-shape dispatch: closed Approve/Reject/Defer vocab; F.0 IGNORES caller verbs; this is intentional -- the three verbs are exactly Approve, Reject (Adjust channel per RESEARCH Q3 option a), Defer
    - .planning/research/new-room-onboarding-20260612/BIRTH-FLOW-BRIEF.md:82-90 -- the B2 gate spec including the nugget routing table HARD RULE (constraint 11)
  </read_first>
  <behavior>
    - Test: grep -c 'Wait for user confirmation before creating the room' commands/new-project.md returns 0 (the bare-prose violation is gone)
    - Test: grep -c 'pickShape' commands/new-project.md returns >= 1 (the F.0 gate call is present)
    - Test: the B2 block contains the phrase 'ROOM BLUEPRINT' (matching the BIRTH-FLOW-BRIEF.md Section 2 header "birth - ROOM BLUEPRINT - decision gate")
    - Test: the B2 block contains a nugget routing table comment or block labeled 'nugget | target section | why' (HARD RULE constraint 11)
    - Test: bash tests/test-no-bespoke-brain-prompts.sh returns 0 (no '(RECOMMENDED)', 'continue', 'Pick one to ' in the modified file)
  </behavior>
  <action>
    Locate the line-103 prose confirmation block in commands/new-project.md (the paragraph beginning "Here's what I'm hearing..." ending with "Wait for user confirmation before creating the room.") and replace it with the B2 blueprint gate block using the onboard.md:348-367 D-12 pattern as the exact structural template.

    The B2 block must follow this structure (prose instruction, not inline code):
    - Header comment citing BIRTH-FLOW-BRIEF.md Section 2 and Canon Part 9 -- "this is THE promotion moment; Approve = batch confirmNode on extracted claims, byUser from USER.md"
    - Instruction to call pickShape with operator=currentOperator (null pre-room), tier=resolveTier(), payload.header='birth - ROOM BLUEPRINT - decision gate', and the F.0 shape. Note F.0's closed vocab (Approve/Reject/Defer); Reject IS the Adjust channel (user types reason, Larry revises blueprint, re-renders F.0). RESEARCH Q3 option (a) -- document this explicitly.
    - Before the pickShape call, display the room blueprint summary: name/slug, section set (from blueprint family or default 8 sections), venture_stage (honest, not assumed), JTBD, persona summary.
    - HARD RULE: embed the nugget routing table in the blueprint display -- format as a markdown table with columns "nugget | target section | why" covering each extracted fact. This is the Jonathan hard rule from BIRTH-FLOW-BRIEF.md constraint 11 and the memory file feedback_nugget_routing_hitl.md. Nothing files until this table is approved.
    - Approve path: proceed with room creation (Step 4 below); record that this is the Part 9 confirmNode batch moment (the batch byUser attribution -- open question 7, reversible default).
    - Reject path: capture reason (REJECTED_BECAUSE edge will be written at birth), revise the blueprint, re-render B2 (the Adjust loop).
    - Defer path: journal the blueprint to scratchpad via writeScratchpadBirthAnswer({gate_id:'B2', canonical_verb:'Defer', ...}) and exit gracefully.
    - Add Tri-Polar degradation script comment: for Desktop (no AskUserQuestion card guarantee), render the blueprint as a prose summary followed by "Type: approve / reject [reason] / defer" -- the same conversational degradation pattern as onboard.md.

    The rest of new-project.md (Steps 4+) is UNCHANGED. The gate replaces only the prose confirmation paragraph.

    Write tests/test-new-project-b2-gate.cjs as a bare node CJS driver (the check/pass counter pattern from test-typed-claim-writer.cjs). Static-analysis tests only (no LLM invocation): read the commands/new-project.md file, assert the violations are gone, assert required tokens are present. Use the SKIP-77 probe if node:sqlite is needed (it is not for this static test). The test must pass with: node tests/test-new-project-b2-gate.cjs. No em-dashes anywhere in the test file.
  </action>
  <verify>
    <automated>node tests/test-new-project-b2-gate.cjs && bash tests/test-no-bespoke-brain-prompts.sh</automated>
  </verify>
  <done>The bare-prose confirmation is gone from new-project.md. A pickShape F.0 B2 gate with nugget routing table and Tri-Polar degradation script is present. The bespoke-prompt tripwire stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scratchpad birth_gate_answers extension + phase-gate aggregator (GAP-1/6 journal half)</name>
  <files>lib/core/scratchpad-ops.cjs, tests/test-scratchpad-birth-answers.cjs, tests/run-all-155.sh</files>
  <read_first>
    - lib/core/scratchpad-ops.cjs:19-51 -- DEFAULT_SCRATCHPAD (the 5-key struct) + readScratchpad (the re-normalization that silently drops unknown keys -- RESEARCH Pitfall 2, the load-bearing trap)
    - lib/core/scratchpad-ops.cjs:59-100 -- writeScratchpadEntry (knows only opportunity/highlight; the model for writeScratchpadBirthAnswer)
    - lib/core/scratchpad-ops.cjs:131-170 -- migrateToRoom + clearScratchpad (replay must run BEFORE drain; drainBirthGateAnswers is called from Plan 02 STEP 2 BEFORE migrateToRoom in STEP 6)
    - lib/core/scratchpad-ops.cjs:195-200 -- module.exports (add new exports additively)
    - tests/run-all-150.5.sh:1-30 -- the phase-gate aggregator shell pattern to mirror
  </read_first>
  <behavior>
    - Test: readScratchpad() on a scratchpad.json that contains a birth_gate_answers array returns that array intact (the drop-unknown-keys trap is closed)
    - Test: readScratchpad() on a scratchpad.json without birth_gate_answers returns birth_gate_answers: [] (the default)
    - Test: writeScratchpadBirthAnswer({gate_id:'B2', option_key:'approve', canonical_verb:'Approve', alias_label:'Approve', ts:Date.now()}) appends an entry to birth_gate_answers; a second call appends a second entry (not overwrite)
    - Test: writeScratchpadBirthAnswer({gate_id:'B1', option_key:'a', canonical_verb:'RunMethodology', alias_label:'A solution looking for its problem', arrival_asset:'solution-seeking', ts:Date.now()}) round-trips all fields
    - Test: a read-modify-write cycle (readScratchpad -> writeScratchpadBirthAnswer -> readScratchpad) does NOT drop the opportunities or highlights arrays (the original keys must survive alongside birth_gate_answers)
    - Test: drainBirthGateAnswers is exported (function or stub) -- Plan 02 fills the body; its presence lets Plan 02 import it without re-editing this file
    - Test: run-all-155.sh exists, is executable (chmod +x), and exits 0 when the two Wave-1 drivers pass
  </behavior>
  <action>
    Edit lib/core/scratchpad-ops.cjs with three additive changes (never touching the existing opportunity/highlight logic):

    1. Add birth_gate_answers: [] to DEFAULT_SCRATCHPAD (the 5-key struct at lines 19-25). Also add it to the two fallback return objects inside readScratchpad's catch block and the "file missing" path (lines 36, 49) so re-normalization NEVER drops it.

    2. In readScratchpad(), after spreading DEFAULT_SCRATCHPAD, explicitly preserve the birth_gate_answers array from the parsed JSON -- the same idiom used for opportunities (apply Array.isArray check; default to []). This closes RESEARCH Pitfall 2 without touching any other key.

    3. Add writeScratchpadBirthAnswer(answer) function: reads the scratchpad, appends the answer object (with ts defaulted to Date.now() if absent) to the birth_gate_answers array, writes back. Entry shape: {gate_id, option_key, canonical_verb, alias_label, free_text, arrival_asset, ts}. Mirror writeScratchpadEntry's try/catch style.

    4. Add drainBirthGateAnswers(db, roomDir) as a STUB EXPORT (returns { ok:true, drained:0 }) -- the body is written in Plan 02 (lib/core/navigation/room-birth.cjs calls it inside the STEP 2 transaction). Export the stub so Plan 02 can import without re-opening this file. Add a comment: "// body filled by Plan 02 (room-birth.cjs) -- stub allows Plan 01 tests to run in isolation."

    5. Add writeScratchpadBirthAnswer and drainBirthGateAnswers to module.exports additively.

    Write tests/test-scratchpad-birth-answers.cjs as a bare node CJS driver: set process.env.HOME to a tmp dir, write fixture scratchpad.json files with known contents, call the functions, assert round-trip fidelity. The test must be self-contained (no real HOME pollution). Use fs.mkdtempSync for isolation. No em-dashes.

    Create tests/run-all-155.sh mirroring run-all-150.5.sh structure: bash aggregator running test-new-project-b2-gate.cjs and test-scratchpad-birth-answers.cjs with pass/fail tally; chmod +x; non-zero exit on any failure; SKIP-77 tolerated. Header comment states what this gate PROVES (Canon Part 6): "the B2 gate and scratchpad journaling half of the birth contract cannot go green through bare prose or a key-dropping re-read."
  </action>
  <verify>
    <automated>node tests/test-scratchpad-birth-answers.cjs && bash tests/run-all-155.sh && bash tests/run-all-148.sh</automated>
  </verify>
  <done>birth_gate_answers survives readScratchpad() re-normalization. writeScratchpadBirthAnswer appends entries. drainBirthGateAnswers stub is exported. run-all-155.sh is green. run-all-148.sh regression holds (18/18).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user turn text -> scratchpad JSON | Untrusted free-text from B2 Adjust/Reject reason lands in birth_gate_answers[].free_text |
| scratchpad JSON -> room.db (Plan 02) | The drain reads scratchpad and writes graph edges; the entry shape must be validated before SQL binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-01 | Tampering | scratchpad birth_gate_answers free_text (SQL injection via Plan 02 drain) | mitigate | All room.db writes use parameterized statements (Plan 02 responsibility); scratchpad stores raw strings, never interpolated into SQL |
| T-155-02 | Elevation of Privilege | B2 Approve bypassed by skipping the gate (bare prose path) | mitigate | Task 1 removes the bypass text; test-new-project-b2-gate.cjs asserts absence via grep; CI tripwire covers new files |
| T-155-03 | Information Disclosure | birth_gate_answers leaking to Brain via a future query | mitigate | Part 8 boundary: scratchpad is LOCAL-only; drainBirthGateAnswers writes to room.db, never to Brain; the check-brain-boundary scan covers the drain in Plan 02 |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan; Part 3 closed-vocab, no new deps |
</threat_model>

<verification>
- `node tests/test-new-project-b2-gate.cjs` -- B2 gate present, bare-prose violation gone, nugget routing table present
- `bash tests/test-no-bespoke-brain-prompts.sh` -- no '(RECOMMENDED)', 'continue', 'Pick one to ' in modified files
- `node tests/test-scratchpad-birth-answers.cjs` -- birth_gate_answers round-trip survives re-normalization
- `bash tests/run-all-155.sh` -- Wave-1 phase gate green
- `bash tests/run-all-148.sh` -- regression floor 18/18
- `grep -rn "\-\-" commands/new-project.md lib/core/scratchpad-ops.cjs | grep -v '^#' | grep -v '//'` returns zero (no em-dashes in modified files)
</verification>

<success_criteria>
- commands/new-project.md: zero instances of the bare-prose confirmation; F.0 B2 gate with nugget routing table present; pickShape call present; Tri-Polar degradation script present
- lib/core/scratchpad-ops.cjs: birth_gate_answers in DEFAULT_SCRATCHPAD and preserved in readScratchpad(); writeScratchpadBirthAnswer and drainBirthGateAnswers exported
- tests/run-all-155.sh green as the one-command phase gate for this wave
- run-all-148.sh regression holds
</success_criteria>

<output>
Create `.planning/phases/155-ignite-flow/155-01-SUMMARY.md` when done.
</output>
