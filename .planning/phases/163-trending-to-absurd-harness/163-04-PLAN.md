---
phase: 163-trending-to-absurd-harness
plan: 04
type: execute
wave: 4
depends_on: [163-03]
files_modified:
  - lib/core/trending-to-absurd/orchestrator.cjs
  - commands/trending-to-absurd.md
  - skills/trending-to-absurd/SKILL.md
  - tests/test-trending-to-absurd-orchestrator.cjs
  - tests/run-all-163.sh
autonomous: true
requirements: [D-163-04, D-163-05]
canon_parts: [2, 3, 4, 7, 8, 9, 10]

must_haves:
  truths:
    - "the TTA orchestrator clones the futures 5-act harness and seeds Act 1 from getDomainsForTrendExtrapolation (graph-walked domains), not user-typed strings"
    - "the orchestrator writes ONLY to room/opportunity-bank/ (exclusive file ownership)"
    - "commands/trending-to-absurd.md declares a connector: block conforming to the 11-sub-key contract"
    - "every graph write routes through navigation.writeEdge / confirmNode (zero raw edge-table SQL)"
  artifacts:
    - path: "lib/core/trending-to-absurd/orchestrator.cjs"
      provides: "the 5-act trend pipeline (seed-from-domains / extrapolate-to-absurd rings / file+cascade / HSI bridge / bank+handoff)"
      min_lines: 120
    - path: "commands/trending-to-absurd.md"
      provides: "the /mos:trending-to-absurd command + Phase 122 frontmatter + connector block"
      contains: "connector:"
    - path: "skills/trending-to-absurd/SKILL.md"
      provides: "when/how the skill activates + canon_parts"
      min_lines: 30
  key_links:
    - from: "lib/core/trending-to-absurd/orchestrator.cjs"
      to: "lib/core/navigation/get-domains-for-trends.cjs"
      via: "getDomainsForTrendExtrapolation seeds Act 1"
      pattern: "getDomainsForTrendExtrapolation"
    - from: "lib/core/trending-to-absurd/orchestrator.cjs"
      to: "lib/core/navigation.cjs"
      via: "writeEdge / confirmNode / writeDomainNode chokepoint"
      pattern: "navigation\\.(writeEdge|confirmNode)"
    - from: "commands/trending-to-absurd.md"
      to: "data/connector-registry.json"
      via: "connector: block (generated by build-connector-registry.cjs pre-commit)"
      pattern: "connects_to_spine"
---

<objective>
WAVE 4 SURFACE-A: clone+extend lib/core/futures/orchestrator.cjs into
lib/core/trending-to-absurd/orchestrator.cjs (the 5-act trend pipeline), plus commands/
trending-to-absurd.md (with the connector block) and skills/trending-to-absurd/SKILL.md. The clone
NEVER rewrites the futures harness (Part 7 reuse); it extends it with: Act 1 seeded from
getDomainsForTrendExtrapolation (graph-walked domains, not user-typed strings), trend extraction +
selection (the Shape F gate at the trend-selection judgment point per D-163-05), and the
extrapolate-to-absurd ring generation across the 3-10 / 11-30 / 50yr horizons.

Purpose: the agent must be graph-native from its first run (D-163-04). It clones the proven 5-act
guided-by-ring harness (seed grounding -> ring-by-ring generation -> per-ring filing + cascade edges
-> HSI bridge scan -> opportunity banking + handoffs) and swaps the seed source: instead of the
futures seed string, Act 1 reads the connective taxonomy via getDomainsForTrendExtrapolation. The
hybrid default pipeline (D-163-05) auto-runs autonomous_safe stages and gates Shape F at the two
judgment points (trend selection, opportunity pick). Exclusive ownership: the harness writes ONLY to
room/opportunity-bank/ (the futures registerConsequenceArtifacts pattern).

Output: the orchestrator clone, the command + connector + skill, the test suite, and the run-all-163
registration. NO em-dashes (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-trending-to-absurd-harness/163-CONTEXT.md
@lib/core/futures/orchestrator.cjs
@commands/explore-trends.md
@docs/CONNECTOR-CONTRACT.md
@docs/COMMAND-FRONTMATTER.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clone+extend the futures orchestrator into the 5-act trend pipeline</name>
  <files>lib/core/trending-to-absurd/orchestrator.cjs, tests/test-trending-to-absurd-orchestrator.cjs, tests/run-all-163.sh</files>
  <behavior>
    - Test 1: seedFromDomains(roomDir, {db}) calls getDomainsForTrendExtrapolation and returns the graph-walked domains as trend-extraction seeds (tier 2 when domains exist).
    - Test 2: generateAbsurdRings(seed, horizon, parents, opts) clamps to the depth/fan-out caps (reuse FUTURES_DEPTH_CAP / FUTURES_FANOUT_CAP) and stamps each consequence with one of the three horizons (near / mid / long mapped to 3-10 / 11-30 / 50yr).
    - Test 3: registerTrendArtifacts writes ONLY under room/opportunity-bank/ (assert no write path escapes opportunity-bank/) with ROOM.md per folder (ICM Layer 0).
    - Test 4: writeCascadeEdges routes ROOT_CAUSES through navigation.writeEdge (zero raw edge-table INSERT in the file; grep gate).
    - Test 5: surfaceTrendSelectionGate returns a Shape F.1 gate descriptor at the trend-selection judgment point (D-163-05 hybrid gate).
  </behavior>
  <action>Create lib/core/trending-to-absurd/orchestrator.cjs by CLONING lib/core/futures/orchestrator.cjs (Part 7: clone + extend, never rewrite) and extending the 5 acts. Reuse VERBATIM by require where possible: import FUTURES_DEPTH_CAP, FUTURES_FANOUT_CAP, resolveDepthCap, resolveFanoutCap, validateConsequenceFrontmatter, writeCascadeEdges, registerConsequenceArtifacts, runHsiScan, surfaceBridgesAtGate, confirmRingDecisions, bankCandidateWithProvenance from '../futures/orchestrator.cjs' so the harness logic is reused not duplicated. Add the net-new functions: (1) seedFromDomains(roomDir, opts) -- calls navigation.getDomainsForTrendExtrapolation (Wave 3) and projects each walked domain into a trend-extraction seed (Act 1 graph-native, D-163-04); on cold start it accepts the tier-0 fallback domains. (2) extractTrends(domains) -- derives candidate trends from the domain hubs + their related nodes. (3) generateAbsurdRings(seed, horizon, parents, opts) -- the extrapolate-to-absurd expansion: reuse generateRing but map the three spec horizons (3-10yr -> 'near', 11-30yr -> 'mid', 50yr -> 'long') onto the HORIZON_ENUM and push each trend to its absurd extreme. (4) surfaceTrendSelectionGate(roomDir, trends) -- the Shape F.1 gate at the trend-selection judgment point (mirror surfaceBridgesAtGate; D-163-05 hybrid). (5) registerTrendArtifacts -- a thin wrapper over registerConsequenceArtifacts that pins the seed folder under opportunity-bank/trending-to-absurd-<seed>/ (exclusive ownership). Mark seed-from-domains + ring-generation autonomous_safe and the two gates (trend selection, opportunity pick) as HITL judgment points (D-163-05). Part 8: zero Brain egress; the only external leg is the inherited runSignalResearch generic-handle path. Create tests/test-trending-to-absurd-orchestrator.cjs covering behaviors 1-5 (stub getDomainsForTrendExtrapolation; in-memory db). Register in run-all-163.sh + add the orchestrator to the em-dash + Part-8 sweep targets. NO fenced code in this action; follow the cited futures/orchestrator.cjs functions.</action>
  <verify>
    <automated>node tests/test-trending-to-absurd-orchestrator.cjs && grep -cE "INSERT INTO edges" lib/core/trending-to-absurd/orchestrator.cjs | grep -q '^0$' && echo NO_RAW_EDGE_SQL</automated>
  </verify>
  <done>The orchestrator clones the futures harness, seeds Act 1 from graph-walked domains, generates absurd rings across the three horizons, writes only to opportunity-bank/, routes cascade edges through the chokepoint, and surfaces the trend-selection Shape F gate.</done>
</task>

<task type="auto">
  <name>Task 2: The command surface + connector block + skill</name>
  <files>commands/trending-to-absurd.md, skills/trending-to-absurd/SKILL.md, tests/run-all-163.sh</files>
  <action>Create commands/trending-to-absurd.md mirroring commands/explore-trends.md frontmatter VERBATIM in shape: the name / description / help_jtbd / body_shape:"methodology" / serves_jtbd / teaching block, then the Phase 122 workflow-layer block (kind: methodology, frameworks: ["S-Curve Analysis"] -- the EXACT Brain framework name explore-trends uses so the resolver key matches, produces: "room/opportunity-bank/trending-to-absurd/*", inputs: [], autonomous_safe: true, allowed-tools Read/Write/Bash/Glob), then the Phase 143.3 connector block per docs/CONNECTOR-CONTRACT.md section 2 (the 11 sub-keys): connects_to_spine true, sensor_triggers [SENS-04] (the trend sensor explore-trends uses), reach_id context_block, sub_mode trending-to-absurd, framework "S-Curve Analysis" (MUST equal the frameworks: value per contract section 27), posture push_forward, hierarchy_rank a free integer not colliding with explore-trends 32 / explore-domains 34 (use 33), filing fileEvidenceWithReadback, plan_gated false, web_scope null, surface F.1. The command BODY: a Larry-led walk that calls the orchestrator's 5 acts, references references/methodology/explore-trends.md for the absurd-trend reference (zero change to that reference per Part 7 EXTEND posture), and renders the two Shape F gates (trend selection + opportunity pick) per D-163-05. Cite the hybrid default (auto where autonomous_safe, gate at judgment points). Create skills/trending-to-absurd/SKILL.md declaring when/how the skill activates (when the navigator wants to push trends to their absurd extreme to surface disruptive opportunities), its canon_parts [2,3,4,7,8,9,10], and that it CHAINS to /mos:futures at the Stage 5-6 boundary + EXTENDS /mos:explore-trends. Register the command + connector validation in run-all-163.sh: add a grep gate asserting commands/trending-to-absurd.md carries connects_to_spine + a framework that equals its frameworks: value, and add both files to the em-dash sweep. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-163.sh && grep -q "connects_to_spine: true" commands/trending-to-absurd.md && grep -q "framework: \"S-Curve Analysis\"" commands/trending-to-absurd.md && test -f skills/trending-to-absurd/SKILL.md && echo SURFACE_OK</automated>
  </verify>
  <done>The command declares the Phase 122 + connector frontmatter (framework matches frameworks:), the body walks the 5 acts with both Shape F gates, the skill declares its canon_parts + chain/extend posture, and run-all-163.sh validates the connector block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> room/opportunity-bank/ | exclusive file ownership; no write escapes opportunity-bank/ |
| orchestrator -> room.db | navigation.writeEdge / confirmNode chokepoint only |
| connector framework -> Brain | generic framework handle only (build-time validation), never user content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-10 | Tampering | exclusive file ownership | mitigate | registerTrendArtifacts pins under opportunity-bank/; Wave 6 ownership check asserts no escape |
| T-163-11 | Spoofing | confirmNode promotion | mitigate | opportunity-pick gate routes APPROVE through confirmNode with resolveByUser (human byUser, never agent) per the futures confirmRingDecisions pattern |
| T-163-12 | Information disclosure | connector + signal leg | mitigate | connector framework is a generic handle; the only external call is the inherited runSignalResearch generic-handle path; Wave 6 leak scan asserts |
| T-163-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (clones the futures orchestrator + reuses navigation); no install task. The RESEARCH.md Package Legitimacy gate is N/A (no installs) |
</threat_model>

<verification>
- `node tests/test-trending-to-absurd-orchestrator.cjs` passes all five behaviors.
- Zero raw `INSERT INTO edges` in orchestrator.cjs (chokepoint-only).
- commands/trending-to-absurd.md carries connects_to_spine + framework == frameworks:.
- skills/trending-to-absurd/SKILL.md exists with canon_parts.
- `bash tests/run-all-163.sh` green; no em-dashes.
</verification>

<success_criteria>
The TTA agent is graph-native from its first run (D-163-04): Act 1 seeds from getDomainsForTrendExtrapolation,
the 5-act harness is cloned not rewritten (Part 7), it pushes trends to their absurd extreme across the
three horizons, writes only to opportunity-bank/, and surfaces the hybrid Shape F gates at the two
judgment points (D-163-05). The command + connector + skill plug into the reach spine.
</success_criteria>

<output>
Create `.planning/phases/163-trending-to-absurd-harness/163-04-SUMMARY.md` when done.
</output>
