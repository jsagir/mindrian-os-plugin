---
phase: 163-trending-to-absurd-harness
plan: 02
type: execute
wave: 2
depends_on: [163-01]
files_modified:
  - lib/core/navigation/typed-domain.cjs
  - lib/core/navigation.cjs
  - tests/test-typed-domain.cjs
  - tests/run-all-163.sh
autonomous: true
requirements: [D-163-01, D-163-04]
canon_parts: [4, 8, 9, 10]

must_haves:
  truths:
    - "writeDomainNode mints a domain / subdomain / focus_area typed node at review_status 'proposed' for truth-claim domains, never auto-confirmed"
    - "linkDomainToRelated connects a new domain node to related existing nodes via the four Wave-1 edges through navigation.writeEdge ONLY"
    - "every domain write and edge write routes through the navigation.cjs chokepoint (zero direct room-db require in typed-domain.cjs)"
    - "navigation.cjs re-exports writeDomainNode + linkDomainToRelated additively (the logMemoryEvent re-export idiom)"
  artifacts:
    - path: "lib/core/navigation/typed-domain.cjs"
      provides: "writeDomainNode + linkDomainToRelated + DOMAIN_NODE_TYPES frozen Set"
      exports: ["writeDomainNode", "linkDomainToRelated", "DOMAIN_NODE_TYPES"]
      min_lines: 90
    - path: "lib/core/navigation.cjs"
      provides: "additive re-export of the two domain writers on the closed surface"
      contains: "writeDomainNode"
    - path: "tests/test-typed-domain.cjs"
      provides: "node-write + proposed-status + edge-linker + chokepoint-only assertions"
      min_lines: 60
  key_links:
    - from: "lib/core/navigation/typed-domain.cjs"
      to: "lib/core/node-insert.cjs"
      via: "insertNode for the domain node (both-schema safe)"
      pattern: "insertNode"
    - from: "lib/core/navigation/typed-domain.cjs"
      to: "lib/core/navigation/edges.cjs"
      via: "writeEdge with DECOMPOSED_INTO / PART_OF / TAGGED_WITH / RELATED_TO"
      pattern: "DECOMPOSED_INTO|PART_OF|TAGGED_WITH|RELATED_TO"
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/typed-domain.cjs"
      via: "require + additive module.exports"
      pattern: "typed-domain"
---

<objective>
WAVE 2 FOUNDATION-B: domain / subdomain / focus_area as FIRST-CLASS typed nodes (D-163-01) plus the
typed-domain.cjs writer and edge-linker that connects a new domain node to related existing nodes,
all through the navigation.cjs chokepoint. This is the connective-taxonomy substrate: a domain hub is
walkable to everything it touches (claims, assumptions, opportunities, artifacts, sections, trends,
consequences) via the four Wave-1 edges. Tier-2 foundation FIRST (D-163-04): the substrate ships
before the agent.

Purpose: D-163-01 requires domains/subdomains to be first-class typed nodes carrying relevant typed
edges to ALL other node types, so getDomainsForTrendExtrapolation (Wave 3) can WALK a domain hub
instead of re-deriving from prose. typed-domain.cjs mirrors typed-claim.cjs verbatim: it takes a
caller-owned db handle, never opens room.db, stays inside the navigation/* allow-list, and writes
truth-claim domain nodes at review_status 'proposed' per Part 9 role 5 (proposed->confirmed human
gate).

Output: typed-domain.cjs (writer + edge-linker), the navigation.cjs additive re-export, the test
suite, and the run-all-163.sh registration. NO em-dashes (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-trending-to-absurd-harness/163-CONTEXT.md
@lib/core/navigation/typed-claim.cjs
@lib/core/navigation/edges.cjs
@lib/core/node-insert.cjs
@lib/core/navigation.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the domain node-type contract + writeDomainNode writer</name>
  <files>lib/core/navigation/typed-domain.cjs, tests/test-typed-domain.cjs</files>
  <behavior>
    - Test 1: writeDomainNode(db, {domainType:'domain', name:'Quantum Imaging', sessionId}) returns {ok:true, node_id} with node type 'domain' and review_status 'proposed'.
    - Test 2: writeDomainNode rejects an invalid domainType (not in DOMAIN_NODE_TYPES) with {ok:false, reason:'invalid_domain_type'}.
    - Test 3: re-writing the same (name, sessionId) UPSERTs (idempotent id), not a duplicate row.
    - Test 4: a system-bookkeeping taxonomy domain (taxonomy:true) lands review_status 'confirmed' via created_by='system' (Part 9 carve-out), while a truth-claim domain (taxonomy false/absent) stays 'proposed'.
  </behavior>
  <action>Create lib/core/navigation/typed-domain.cjs mirroring lib/core/navigation/typed-claim.cjs structure VERBATIM (the header allow-list note, the isPlainObject helper, the stable 31-multiplier hash id-minter, the additive-JSON-props discipline, the defensive never-throw contract). Define a frozen DOMAIN_NODE_TYPES Set {'domain','subdomain','focus_area'} (D-163-01) mirroring the KNOWLEDGE_TYPES frozen-Set idiom at typed-claim.cjs:48. writeDomainNode(db, params) where params = {domainType, name, sessionId, parentId?, taxonomy?, evidenceTier?}: validate domainType against DOMAIN_NODE_TYPES; mint the node via lib/core/node-insert.cjs insertNode (both-schema safe per node-insert.cjs:95) with type=domainType and created_by='system'. Per D-163-04 + Part 9: a PURE TAXONOMY domain (params.taxonomy === true) is system-bookkeeping and may carry review_status 'confirmed' (the Part 9 v1.5 audit-node carve-out spirit -- a taxonomy label asserts no venture truth); a domain that carries a truth-claim (taxonomy absent/false) lands review_status 'proposed' and is NEVER auto-confirmed (truth-claim node, Part 9 role 5). Properties bag is additive JSON (name, domainType, parentId default '', evidenceTier default 'None'); NEVER DDL columns (the typed-claim.cjs:32-38 D-10 precedent). Node id: 'domain:'+sid+':'+hash(name). Then create tests/test-typed-domain.cjs covering the four behaviors over an in-memory node:sqlite db with the migrated nodes schema. NO fenced code in this action; the writer follows the cited typed-claim.cjs lines.</action>
  <verify>
    <automated>node tests/test-typed-domain.cjs</automated>
  </verify>
  <done>writeDomainNode mints domain/subdomain/focus_area nodes, rejects invalid types, UPSERTs idempotently, and applies the proposed-vs-confirmed split per the taxonomy flag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: linkDomainToRelated edge-linker + navigation.cjs re-export</name>
  <files>lib/core/navigation/typed-domain.cjs, lib/core/navigation.cjs, tests/test-typed-domain.cjs, tests/run-all-163.sh</files>
  <behavior>
    - Test 5: linkDomainToRelated(db, {domainId, relations:[{targetId, edge:'PART_OF'}, {targetId2, edge:'TAGGED_WITH'}]}) writes both edges via navigation.writeEdge and returns {written:2}.
    - Test 6: a DECOMPOSED_INTO relation from a domain to a subdomain is accepted; an edge type NOT in the four Wave-1 domain edges is rejected (returns a failure entry, not a throw).
    - Test 7: navigation.writeDomainNode and navigation.linkDomainToRelated are callable (the additive re-export resolves).
    - Test 8: typed-domain.cjs has zero direct require of room-db.cjs / lazygraph-ops.cjs / node:sqlite (chokepoint-only; grep assertion in the test or run-all-163 sweep).
  </behavior>
  <action>Add linkDomainToRelated(db, params) to typed-domain.cjs where params = {domainId, relations:[{targetId, edge, properties?}]}. It constrains its accepted edge set to the DOMAIN_EDGE_SUBSET {'DECOMPOSED_INTO','PART_OF','TAGGED_WITH','RELATED_TO'} (a frozen subset of the Wave-1 ALLOWED_EDGE_TYPES, mirroring how memory-artifacts.cjs writeCortexLineageEdge constrains to {STATES,SUPPORTS,INFORMS,DESCRIBES} per edges.cjs:271-274). For each relation it calls navigation.writeEdge(db, {source_id, target_id, edge_type, properties}) -- the chokepoint, NEVER raw SQL. Properties stay ENUM/scalar only (a relation enum + the taxonomy node id; never prose, Part 8). The edge DIRECTION follows D-163-03: DECOMPOSED_INTO source=parent-taxonomy target=child; PART_OF source=member target=domain; TAGGED_WITH source=any target=domain; RELATED_TO source/target both taxonomy. Return {written, edges, failures} mirroring writeCascadeEdges in futures/orchestrator.cjs:283-310. Then add the additive re-export to lib/core/navigation.cjs: require('./navigation/typed-domain.cjs') and add writeDomainNode + linkDomainToRelated + DOMAIN_NODE_TYPES to module.exports, following the writeLensFinding / writeClaimNode re-export idiom already in navigation.cjs (the closed surface comment block). Extend tests/test-typed-domain.cjs with behaviors 5-8 and a require-scan asserting typed-domain.cjs carries no forbidden substrate require. Register test-typed-domain.cjs in the CJS_SUITES array of tests/run-all-163.sh and add typed-domain.cjs + navigation.cjs to its em-dash sweep targets. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-typed-domain.cjs && node -e "const n=require('./lib/core/navigation.cjs'); if(typeof n.writeDomainNode!=='function'||typeof n.linkDomainToRelated!=='function') process.exit(1); console.log('REEXPORT_OK')" && bash tests/run-all-163.sh</automated>
  </verify>
  <done>linkDomainToRelated writes the four domain edges through the chokepoint and rejects non-domain edges; navigation.cjs re-exports both writers; typed-domain.cjs has zero substrate bypass; run-all-163.sh stays green with the new suite registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain write -> room.db | through navigation chokepoint only; no direct room-db require |
| domain truth-claim -> confirmed status | Part 9 role 5: only a human byUser via confirmNode (not this writer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-04 | Elevation of privilege | writeDomainNode review_status | mitigate | truth-claim domains land 'proposed'; only taxonomy (system-bookkeeping) domains carry 'confirmed' per the Part 9 carve-out; no auto-confirm of a truth-claim |
| T-163-05 | Tampering | linkDomainToRelated edge set | mitigate | DOMAIN_EDGE_SUBSET constrains accepted edges; writeEdge re-validates against the frozen Set; non-domain edge returns a failure entry |
| T-163-06 | Information disclosure | edge / node properties | mitigate | properties ENUM/scalar only; the domain name + taxonomy id, never prose; zero Brain egress (Wave 6 leak scan asserts) |
| T-163-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (pure Node built-ins reusing insertNode + navigation); no install task |
</threat_model>

<verification>
- `node tests/test-typed-domain.cjs` passes all eight behaviors.
- navigation.cjs re-export resolves (writeDomainNode + linkDomainToRelated callable).
- typed-domain.cjs has zero direct room-db.cjs / lazygraph-ops.cjs / node:sqlite require (chokepoint-only).
- `bash tests/run-all-163.sh` green with test-typed-domain.cjs registered.
- No em-dashes.
</verification>

<success_criteria>
domain / subdomain / focus_area are first-class typed nodes (D-163-01); the writer + edge-linker route
every write through the navigation chokepoint; truth-claim domains honor the proposed->confirmed human
gate (Part 9); the four Wave-1 edges connect a domain hub to related existing nodes; the substrate
ships before the agent (D-163-04).
</success_criteria>

<output>
Create `.planning/phases/163-trending-to-absurd-harness/163-02-SUMMARY.md` when done.
</output>
