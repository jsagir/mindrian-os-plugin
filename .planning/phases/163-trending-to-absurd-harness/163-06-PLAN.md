---
phase: 163-trending-to-absurd-harness
plan: 06
type: execute
wave: 6
depends_on: [163-01, 163-02, 163-03, 163-04, 163-05]
files_modified:
  - tests/test-trending-to-absurd-verdict.cjs
  - tests/test-trending-to-absurd-part8-leak.cjs
  - tests/run-all-163.sh
autonomous: true
requirements: [D-163-01, D-163-03, D-163-05, D-163-06]
canon_parts: [4, 6, 8, 9, 10]

must_haves:
  truths:
    - "the adversarial verdict returns a structured {passed, findings[]} over five checks (ROOM.md/nesting, frozen-edge-set, Part 8 leak, Part 9 proposed-not-confirmed, exclusive ownership)"
    - "the frozen-edge-set check asserts the four domain edges are members AND no non-domain edge leaked into the domain writers"
    - "the Part 8 leak scan over all Phase-163 surfaces finds zero user-content-to-Brain / raw-external-egress tokens"
    - "the Part 9 check asserts truth-claim domain + Stage 7 nodes land proposed, never auto-confirmed"
    - "tests/run-all-163.sh aggregates every Phase-163 suite into one PASS/FAIL phase gate"
  artifacts:
    - path: "tests/test-trending-to-absurd-verdict.cjs"
      provides: "the five-check adversarial structured verdict"
      min_lines: 80
    - path: "tests/test-trending-to-absurd-part8-leak.cjs"
      provides: "the Part 8 leak scan over all Phase-163 lib + command + skill surfaces"
      min_lines: 50
    - path: "tests/run-all-163.sh"
      provides: "the finalized phase aggregator (all suites + sweeps, runs to completion)"
      min_lines: 60
  key_links:
    - from: "tests/test-trending-to-absurd-verdict.cjs"
      to: "lib/core/navigation/edges.cjs"
      via: "frozen-edge-set membership assertion"
      pattern: "ALLOWED_EDGE_TYPES"
    - from: "tests/run-all-163.sh"
      to: "tests/test-trending-to-absurd-verdict.cjs"
      via: "CJS_SUITES registration"
      pattern: "verdict"
---

<objective>
WAVE 6 VERIFY: the adversarial structured verdict (the harness-as-code property 6) over every
Phase-163 surface, plus the finalized tests/run-all-163.sh aggregator that is the single PASS/FAIL
phase gate. The verdict returns {passed, findings[]} over five checks: ROOM.md / nesting, frozen-edge-set,
Part 8 leak scan, Part 9 proposed-not-confirmed, and exclusive file ownership.

Purpose: the harness-as-code 9-property mandate requires an adversarial verify that returns a
structured verdict, not a vibe. This wave proves by instrumentation that the foundation
(Waves 1-3) and the surfaces (Waves 4-5) honor the constitution: the four domain edges are frozen
members (D-163-03), the connective taxonomy nodes + Stage 7 truth-claims land proposed not confirmed
(Part 9), no Phase-163 code path egresses user content to Brain or opens a raw external fetch (Part 8),
the TTA harness writes ONLY to opportunity-bank/ (exclusive ownership), and every artifact folder has
its ICM Layer 0 ROOM.md. run-all-163.sh ties every suite + sweep into one green gate.

Output: the verdict suite, the Part 8 leak suite, and the finalized aggregator. NO em-dashes
(CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-trending-to-absurd-harness/163-CONTEXT.md
@tests/run-all-156.sh
@lib/core/navigation/edges.cjs
@lib/core/trending-to-absurd/orchestrator.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: The five-check adversarial structured verdict + Part 8 leak scan</name>
  <files>tests/test-trending-to-absurd-verdict.cjs, tests/test-trending-to-absurd-part8-leak.cjs</files>
  <action>Create tests/test-trending-to-absurd-verdict.cjs returning a structured {passed, findings[]} over FIVE checks (mirror the futures VERIFICATION 5-check pattern + the run-all-156 sweep philosophy): (1) ROOM.md / nesting -- run a TTA pipeline over a tmp room and assert every created folder under opportunity-bank/ carries a ROOM.md and every artifact sits in its own named folder (CLAUDE.md decisions 15 + 16). (2) frozen-edge-set -- require lib/core/navigation/edges.cjs and assert ALLOWED_EDGE_TYPES has DECOMPOSED_INTO / PART_OF / TAGGED_WITH / RELATED_TO AND the full prior FLOOR is preserved AND the domain edge-linker (typed-domain.cjs DOMAIN_EDGE_SUBSET) rejects a non-domain edge (no leak of an arbitrary type into the domain writers); never assert .size. (3) Part 9 proposed-not-confirmed -- write a truth-claim domain node (taxonomy absent) + a Stage 7 venture-truth step and assert review_status is 'proposed', never 'confirmed'; assert a taxonomy domain (taxonomy:true) is the only path to a system-confirmed status (the carve-out). (4) Part 8 leak -- delegate to the leak-scan suite (assert it passes). (5) exclusive ownership -- run the orchestrator + Stage 7 over a tmp room and assert ZERO writes land outside room/opportunity-bank/ (walk the room tree; any file mtime-touched outside opportunity-bank/ is a finding). Each check pushes a {check, passed, detail} into findings[]; the suite exits non-zero if any check failed and prints the findings. Then create tests/test-trending-to-absurd-part8-leak.cjs: a grep/scan over ALL Phase-163 surfaces (lib/core/navigation/typed-domain.cjs, lib/core/navigation/get-domains-for-trends.cjs, lib/core/synthesizers/domain-hierarchy.cjs, lib/core/trending-to-absurd/*.cjs, commands/trending-to-absurd.md, skills/trending-to-absurd/SKILL.md) for forbidden tokens: no mcp__brain_(write|store|upsert|ingest), no writeBrain/sendToBrain/ingestToBrain, no raw fetch( egress in the lib code, no hardcoded external http(s) endpoint except github.com comment URLs (mirror the run-all-156.sh BRAIN_WRITE + RAW_FETCH regexes). Use `grep -v '^#'` style filtering where a comment could self-invalidate the count; never a bare unfiltered == 0 gate. NO fenced code in this action; mirror the cited run-all-156.sh sweep logic and the futures verification structure. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-trending-to-absurd-verdict.cjs && node tests/test-trending-to-absurd-part8-leak.cjs && echo VERDICT_OK</automated>
  </verify>
  <done>The verdict returns {passed:true, findings:[...]} over all five checks; the Part 8 leak scan finds zero forbidden tokens across every Phase-163 surface; both suites exit zero only when the constitution holds.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize tests/run-all-163.sh as the single phase gate</name>
  <files>tests/run-all-163.sh</files>
  <action>Finalize tests/run-all-163.sh (scaffolded in Wave 1) into the complete phase aggregator, mirroring tests/run-all-156.sh structure end to end. The CJS_SUITES array MUST list every Phase-163 suite in dependency order: test-edges-domain-taxonomy-floor.cjs (Wave 1), test-typed-domain.cjs (Wave 2), test-lens-domain-family.cjs + test-get-domains-for-trends.cjs (Wave 3), test-trending-to-absurd-orchestrator.cjs (Wave 4), test-trending-to-absurd-stage7.cjs + test-trending-to-absurd-variance.cjs (Wave 5), test-trending-to-absurd-verdict.cjs + test-trending-to-absurd-part8-leak.cjs (Wave 6). The per-suite loop runs to completion (a missing file gates to a FAIL line, never a crash). Add the Part-8 grep sweep block over the Phase-163 lib + command + skill surfaces (the same BRAIN_WRITE + RAW_FETCH + external-http regexes as run-all-156.sh, plus the frozen-edge-set assertion: grep the four domain edges are present in edges.cjs and no non-domain edge string leaked into typed-domain.cjs's DOMAIN_EDGE_SUBSET). Add the em-dash sweep (U+2014 codepoint escape so the runner carries no literal em-dash) over all Phase-163 lib files, commands/trending-to-absurd.md, skills/trending-to-absurd/SKILL.md, the test suites, docs/MINDRIAN-CANON.md region touched, and run-all-163.sh itself. Final tally + exit 1 on any failure. set -uo pipefail; bash only; no emoji. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-163.sh && echo PHASE_GATE_GREEN</automated>
  </verify>
  <done>run-all-163.sh runs every Phase-163 suite + the Part 8 sweep + the frozen-edge-set assertion + the em-dash sweep to completion, prints a per-suite PASS/FAIL + final tally, and exits 0 only when the entire phase is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict suite -> all Phase-163 surfaces | adversarial: assumes each surface is hostile until proven compliant |
| run-all-163 -> CI | the single phase gate; a RED here blocks the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-16 | Tampering | frozen-edge-set drift | mitigate | the verdict + run-all sweep assert the four domain edges present + FLOOR preserved + no non-domain edge leaked into the domain writers |
| T-163-17 | Information disclosure | any Phase-163 egress path | mitigate | the Part 8 leak scan over every lib + command + skill surface finds zero Brain-write / raw-fetch / external-http tokens |
| T-163-18 | Elevation of privilege | truth-claim auto-confirm | mitigate | the Part 9 check asserts truth-claim domain + Stage 7 nodes stay proposed; only the taxonomy carve-out reaches system-confirmed |
| T-163-19 | Tampering | exclusive ownership escape | mitigate | the ownership check walks the room tree and flags any write outside opportunity-bank/ |
| T-163-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (pure bash + node:test/assert); no install task across the entire phase |
</threat_model>

<verification>
- `node tests/test-trending-to-absurd-verdict.cjs` returns passed over all five checks.
- `node tests/test-trending-to-absurd-part8-leak.cjs` finds zero forbidden tokens.
- `bash tests/run-all-163.sh` is green (every suite + Part 8 sweep + frozen-edge-set assertion + em-dash sweep).
- No em-dashes anywhere in the phase.
</verification>

<success_criteria>
The adversarial structured verdict proves by instrumentation that the connective taxonomy (D-163-01),
the frozen-edge amendment (D-163-03), the hybrid gates (D-163-05), and the full variance (D-163-06)
all honor the canon: ROOM.md/nesting holds, the frozen edge set is intact, no Part 8 leak, Part 9
proposed-not-confirmed holds, and exclusive ownership is enforced. run-all-163.sh is the single green
phase gate.
</success_criteria>

<output>
Create `.planning/phases/163-trending-to-absurd-harness/163-06-SUMMARY.md` when done.
</output>
