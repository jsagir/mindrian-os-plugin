---
phase: 163-trending-to-absurd-harness
plan: 03
type: execute
wave: 3
depends_on: [163-02]
files_modified:
  - lib/core/lens-engine.cjs
  - lib/core/navigation/get-domains-for-trends.cjs
  - lib/core/navigation.cjs
  - tests/test-lens-domain-family.cjs
  - tests/test-get-domains-for-trends.cjs
  - tests/run-all-163.sh
autonomous: true
requirements: [D-163-01, D-163-04]
canon_parts: [2, 4, 8, 9, 10]

must_haves:
  truths:
    - "the lens-engine domain family is ACTIVE (client_count > 0) with five lens sets + a hierarchy synthesizer"
    - "rotate({lensType:'domain', ...}) runs (no longer returns lens_family_reserved)"
    - "getDomainsForTrendExtrapolation WALKS a domain hub via getNeighborhood and returns related nodes, falling back to BRAIN.md / explore-domains artifacts only on cold start"
    - "the hierarchy synthesizer emits the domain->subdomain->focus_area tree AND the cross-links to existing nodes"
  artifacts:
    - path: "lib/core/lens-engine.cjs"
      provides: "domain family activated in LENS_REGISTRY (was reserved_for v1.14.0)"
      contains: "client_count: 1"
    - path: "lib/core/synthesizers/domain-hierarchy.cjs"
      provides: "the domain->subdomain->focus_area hierarchy synthesizer + cross-links"
      min_lines: 40
    - path: "lib/core/navigation/get-domains-for-trends.cjs"
      provides: "graph-walking domain reader with Tier-0 cold-start fallback"
      exports: ["getDomainsForTrendExtrapolation"]
      min_lines: 70
  key_links:
    - from: "lib/core/navigation/get-domains-for-trends.cjs"
      to: "lib/core/navigation.cjs"
      via: "getNeighborhood walk of the domain hub"
      pattern: "getNeighborhood"
    - from: "lib/core/lens-engine.cjs"
      to: "lib/core/synthesizers/domain-hierarchy.cjs"
      via: "NAMED_SYNTHESIZERS registration"
      pattern: "domain-hierarchy"
---

<objective>
WAVE 3 FOUNDATION-C: activate the lens-engine domain family (the slot reserved_for 'v1.14.0' at
lens-engine.cjs:68) with five lens sets (disciplinary / stakeholder / system / temporal / scale) plus
a hierarchy synthesizer, and ship getDomainsForTrendExtrapolation -- the graph-WALKING reader that
walks a domain hub to its related nodes (the Tier-2 built path per D-163-04), with the
Tier-0 artifact/BRAIN.md derivation as a cold-start fallback ONLY.

Purpose: D-163-01's connective taxonomy is only real if the agent can WALK it. The domain lens family
emits the domain->subdomain->focus_area tree (via the Wave-2 DECOMPOSED_INTO edges) AND the cross-links
to existing claims/assumptions/opportunities (via PART_OF / TAGGED_WITH / RELATED_TO).
getDomainsForTrendExtrapolation is the reader the Wave-4 trend pipeline seeds itself from: it calls
navigation.getNeighborhood on a domain hub instead of re-deriving from prose. Tier-0 (parsing
explore-domains artifacts + BRAIN.md concepts) is the cold-start stopgap, NOT the primary path
(D-163-04).

Output: the activated domain family, the hierarchy synthesizer, the reader, the additive re-export,
the two test suites, and the run-all-163.sh registration. NO em-dashes (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-trending-to-absurd-harness/163-CONTEXT.md
@lib/core/lens-engine.cjs
@lib/core/navigation/typed-domain.cjs
@lib/core/navigation.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate the lens-engine domain family + the hierarchy synthesizer</name>
  <files>lib/core/lens-engine.cjs, lib/core/synthesizers/domain-hierarchy.cjs, tests/test-lens-domain-family.cjs, tests/run-all-163.sh</files>
  <behavior>
    - Test 1: LENS_REGISTRY.domain.client_count > 0 and lens_sets includes the five domain lenses (disciplinary, stakeholder, system, temporal, scale).
    - Test 2: rotate({lensType:'domain', lensSet:'domain-five', input:{...}, perLensFn, synthesize:'domain-hierarchy', surfaceSelector:'F.1'}) returns {ok:true} (no longer {ok:false, reason:'lens_family_reserved'}).
    - Test 3: synthesizeDomainHierarchy(findingNodes) emits a structured tree {domain, subdomains:[{name, focus_areas:[]}], cross_links:[]} from the typed finding nodes.
    - Test 4: the other reserved families (framework, trend) stay client_count 0 (no over-activation; the broader v1.14.0 lens migrations are out of scope per CONTEXT).
  </behavior>
  <action>In lib/core/lens-engine.cjs: flip the domain family in LENS_REGISTRY (line 68) from {client_count:0, reserved_for:'v1.14.0'} to an ACTIVE slot mirroring the source family pattern at lines 76-80: client_count 1, lens_sets ['disciplinary','stakeholder','system','temporal','scale'] (the five Engine 1 decomposition lenses named in Canon Part 2 Engine 1), synthesizers ['domain-hierarchy']. Add a NAMED_LENS_SETS entry 'domain-five' resolving to the five lens names (mirror the 'six-hats' entry at line 96). Add a NAMED_SYNTHESIZERS entry 'domain-hierarchy' -> require('./synthesizers/domain-hierarchy.cjs').synthesizeDomainHierarchy (mirror the 'source-comparison' registration at line 106). Leave framework + trend reserved (CONTEXT out-of-scope: the broader lens-family migrations are separate phases). Then create lib/core/synthesizers/domain-hierarchy.cjs exporting synthesizeDomainHierarchy(findingNodes): a PURE function (mirror lib/core/synthesizers/source-comparison.cjs) that reads the typed lens_finding node array the engine passes (per lens-engine.cjs:363 the synthesizer receives typed nodes, not raw arrays) and emits the domain->subdomain->focus_area tree plus a cross_links list naming the related existing-node ids each lens surfaced. NO graph writes in the synthesizer (the engine owns writes via writeLensFinding/writeEdge). Create tests/test-lens-domain-family.cjs covering behaviors 1-4 (use a stub perLensFn + an in-memory db). Register it in run-all-163.sh CJS_SUITES + em-dash targets. NO fenced code in this action; follow the cited lens-engine.cjs + source-comparison.cjs lines.</action>
  <verify>
    <automated>node tests/test-lens-domain-family.cjs && node -e "const {LENS_REGISTRY}=require('./lib/core/lens-engine.cjs'); if(LENS_REGISTRY.domain.client_count<1) process.exit(1); console.log('DOMAIN_ACTIVE')"</automated>
  </verify>
  <done>The domain family is active with the five lenses + hierarchy synthesizer; rotate runs for lensType 'domain'; framework + trend stay reserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getDomainsForTrendExtrapolation graph-walking reader + cold-start fallback</name>
  <files>lib/core/navigation/get-domains-for-trends.cjs, lib/core/navigation.cjs, tests/test-get-domains-for-trends.cjs, tests/run-all-163.sh</files>
  <behavior>
    - Test 5: with domain nodes + PART_OF/TAGGED_WITH edges in room.db, getDomainsForTrendExtrapolation(roomDir, {db}) returns the domains WITH their related node ids (walked via getNeighborhood), tier:2.
    - Test 6: with NO domain nodes (cold start), it falls back to parsing explore-domains artifacts / BRAIN.md concepts and returns tier:0 (the documented stopgap), never crashing.
    - Test 7: the reader makes zero Brain calls and zero raw room-db require (chokepoint-only; getNeighborhood is the walk primitive).
    - Test 8: navigation.getDomainsForTrendExtrapolation is callable (additive re-export resolves).
  </behavior>
  <action>Create lib/core/navigation/get-domains-for-trends.cjs exporting getDomainsForTrendExtrapolation(roomDir, opts). Tier-2 PRIMARY path (D-163-04): when opts.db is supplied, SELECT domain-type nodes and for each call navigation.getNeighborhood(db, domainNodeId, opts) (signature getNeighborhood(db, focusNodeId, opts) per neighborhood.cjs:48) to walk the hub to its related nodes (the PART_OF / TAGGED_WITH / DECOMPOSED_INTO / RELATED_TO neighbors), returning {tier:2, domains:[{id, name, domainType, related:[...]}]}. Tier-0 COLD-START fallback ONLY (when no domain nodes exist): parse recent explore-domains artifacts under room/problem-definition/domain-decomposition/ + BRAIN.md concept handles (reuse the shipped parseFrontmatter from opportunity-ops.cjs; do NOT hand-roll), returning {tier:0, domains:[...]} -- the documented stopgap that does NOT satisfy D-163-01 but keeps a brand-new room moving. Defensive: never throw on a missing db / missing artifacts (mirror the futures orchestrator degrade contract). The module reaches room.db ONLY via navigation.getNeighborhood (caller-owned db handle, zero direct room-db require -- mirror lens-engine.cjs:13-26 substrate contract). Add the additive re-export to navigation.cjs (require + module.exports getDomainsForTrendExtrapolation, the logMemoryEvent idiom). Create tests/test-get-domains-for-trends.cjs covering behaviors 5-8. Register in run-all-163.sh. Part 8: zero Brain egress; the cold-start BRAIN.md read is a LOCAL file read of an already-derived local cache, never a live Brain call. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-get-domains-for-trends.cjs && node -e "const n=require('./lib/core/navigation.cjs'); if(typeof n.getDomainsForTrendExtrapolation!=='function') process.exit(1); console.log('READER_OK')" && bash tests/run-all-163.sh</automated>
  </verify>
  <done>getDomainsForTrendExtrapolation walks domain hubs (tier 2) and falls back to artifacts/BRAIN.md on cold start (tier 0); zero Brain egress and zero substrate bypass; navigation.cjs re-exports it; run-all-163.sh green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader -> room.db | through getNeighborhood chokepoint only |
| cold-start fallback -> BRAIN.md | LOCAL file read of a derived cache; never a live Brain call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-07 | Information disclosure | getDomainsForTrendExtrapolation | mitigate | LOCAL only: getNeighborhood walk + LOCAL BRAIN.md file read; zero live Brain call; Wave 6 leak scan asserts |
| T-163-08 | Denial of service | cold-start artifact parse | accept | bounded to recent domain-decomposition artifacts; defensive never-throw; low-value local parse |
| T-163-09 | Tampering | domain family activation | mitigate | framework + trend stay reserved (no over-activation); test asserts client_count for the reserved families |
| T-163-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (reuses parseFrontmatter + getNeighborhood); no install task |
</threat_model>

<verification>
- `node tests/test-lens-domain-family.cjs` and `node tests/test-get-domains-for-trends.cjs` pass.
- LENS_REGISTRY.domain.client_count > 0; framework + trend stay 0.
- getDomainsForTrendExtrapolation returns tier 2 with domain nodes, tier 0 on cold start; zero Brain egress.
- navigation.cjs re-exports the reader.
- `bash tests/run-all-163.sh` green; no em-dashes.
</verification>

<success_criteria>
The domain lens family is active (five lenses + hierarchy synthesizer emitting the tree + cross-links);
getDomainsForTrendExtrapolation walks a domain hub as the Tier-2 built path (D-163-04) with a Tier-0
cold-start fallback; the connective taxonomy (D-163-01) is now READABLE end to end through the
chokepoint, ready for the Wave-4 agent.
</success_criteria>

<output>
Create `.planning/phases/163-trending-to-absurd-harness/163-03-SUMMARY.md` when done.
</output>
