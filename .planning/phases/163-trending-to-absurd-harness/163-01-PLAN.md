---
phase: 163-trending-to-absurd-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/edges.cjs
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-edges-domain-taxonomy-floor.cjs
  - tests/run-all-163.sh
autonomous: false
requirements: [D-163-03]
canon_parts: [4, 6, 8, 9, 10]

must_haves:
  truths:
    - "writeEdge accepts DECOMPOSED_INTO / PART_OF / TAGGED_WITH / RELATED_TO and rejects a made-up domain edge type"
    - "the four new edges appear in the Part 4 typed-edge list AND in a new Appendix D entry in docs/MINDRIAN-CANON.md"
    - "the canon version line bumps and a CANON-PHASE-MAP version-history row + Phase 163 map row record the amendment"
    - "the floor test asserts every prior frozen edge type still passes AND the four new types are members"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "ALLOWED_EDGE_TYPES extended with the four domain edges via one additive block"
      contains: "DECOMPOSED_INTO"
    - path: "tests/test-edges-domain-taxonomy-floor.cjs"
      provides: "membership + FLOOR + frozen-Set + made-up-type-negative assertions (never .size)"
      min_lines: 40
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Part 4 edge list + Appendix D entry + version bump"
      contains: "DECOMPOSED_INTO"
    - path: "tests/run-all-163.sh"
      provides: "phase aggregator scaffold (Wave 1 registers the floor test)"
      min_lines: 30
  key_links:
    - from: "lib/core/navigation/edges.cjs"
      to: "tests/test-edges-domain-taxonomy-floor.cjs"
      via: "require ALLOWED_EDGE_TYPES + writeEdge"
      pattern: "ALLOWED_EDGE_TYPES"
    - from: "docs/MINDRIAN-CANON.md"
      to: "docs/CANON-PHASE-MAP.md"
      via: "version-history row references the same Appendix D entry"
      pattern: "163"
---

<objective>
WAVE 1 FOUNDATION-A: the four-edge frozen-vocabulary canon amendment, landed as ONE atomic
lockstep wave so CI never goes RED mid-phase (per D-163-03). This mints DECOMPOSED_INTO, PART_OF,
TAGGED_WITH, and RELATED_TO into the frozen ALLOWED_EDGE_TYPES closed set in
lib/core/navigation/edges.cjs:32, and moves every lockstep surface together: the edges.cjs additive
block, the docs/MINDRIAN-CANON.md Part 4 edge list + a new Appendix D entry + the header/footer
version bump, the docs/CANON-PHASE-MAP.md Phase 163 row + a version-history row, and a canonical
floor test. This is a navigator-GATED frozen-set move, mirroring the Phase 150.8 procedure (Appendix
D entry 18 added three edges; this adds four) and the Phase 148 reach-count amendment procedure.

Purpose: D-163-03 demands a FULL domain-edge set added via a navigator-LOCKED canon amendment. The
typed-edge vocabulary is a frozen constitutional property (Part 4 renders against it, the Phase 108
frozen-taxonomy contract froze it, Part 6 dog-fooding requires the plugin to honor its own canon), so
moving the set is a canon amendment, not a per-phase edit. Wave 2's edge-linker writes these edges;
this wave is the load-bearing constitutional prerequisite that must ship first.

Output: an extended ALLOWED_EDGE_TYPES Set, the amended canon + phase map, the floor test, and the
phase test-runner scaffold. NO em-dashes anywhere (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-trending-to-absurd-harness/163-CONTEXT.md
@lib/core/navigation/edges.cjs
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ALLOWED_EDGE_TYPES with the four domain edges + ship the floor test</name>
  <files>lib/core/navigation/edges.cjs, tests/test-edges-domain-taxonomy-floor.cjs</files>
  <behavior>
    - Test 1: ALLOWED_EDGE_TYPES.has('DECOMPOSED_INTO') === true (and PART_OF, TAGGED_WITH, RELATED_TO).
    - Test 2: every prior frozen type still present (FLOOR): DEFERRED, REJECTED, DERIVED_FROM, FILED_AS_DECISION, FOLLOWS_FROM, OPERATOR_TRANSITION, INFORMS, REJECTED_BECAUSE, CONTRADICTS, SUPERSEDES, AFFILIATED_WITH, PIVOTED, SELECTED_REACH, FEEDS_INTO, VALIDATES, STATES, SUPPORTS, DESCRIBES, REFINES, ROOT_CAUSES, INSTANTIATES.
    - Test 3: ALLOWED_EDGE_TYPES is a frozen Set instance (Object.isFrozen true).
    - Test 4: writeEdge(db, {edge_type:'DECOMPOSED_INTO', source_id, target_id}) returns ok on an in-memory node:sqlite db with an edges table; writeEdge(db, {edge_type:'MADE_UP_DOMAIN_EDGE', ...}) returns {ok:false, reason:'invalid_edge_type'}.
    - NEVER assert .size (additive extensions must not regress the baseline; mirror the edges.cjs FLOOR doctrine at lines 30-31, 183-184).
  </behavior>
  <action>Append ONE additive block to the ALLOWED_EDGE_TYPES Set in lib/core/navigation/edges.cjs (after the Phase 150.8 INSTANTIATES member at line 321), mirroring the Phase 150.8 additive idiom VERBATIM (the comment block at lines 278-321 is the template). Add four members: DECOMPOSED_INTO, PART_OF, TAGGED_WITH, RELATED_TO. The comment block MUST define each edge per D-163-03: (1) DECOMPOSED_INTO -- the hierarchy edge, source=parent taxonomy node, target=child; legal endpoints domain->subdomain and subdomain->focus_area ONLY. (2) PART_OF -- structural membership, source=member node, target=the domain/subdomain it belongs to; legal source endpoints any node type (claim/assumption/opportunity/Artifact/Section/trend/CausalClaim), legal target endpoints domain/subdomain/focus_area. (3) TAGGED_WITH -- a lightweight categorization edge, source=any node, target=domain/subdomain (the connective taxonomy tag). (4) RELATED_TO -- the symmetric cross-domain relatedness edge between two taxonomy nodes (domain<->domain, subdomain<->subdomain) when a theme spans them. Mark the block NAVIGATOR-GATED (per D-163-03) exactly as the Phase 150.8 block at lines 287-293 marks its trio. State Part 8 ENUM/scalar-only props (the taxonomy node id + a relation enum; never prose) and that these never cross to Brain. Then create tests/test-edges-domain-taxonomy-floor.cjs mirroring the structure named in the edges.cjs header (membership + FLOOR + frozen-Set + made-up-type negative); use node:test or a plain assert harness matching tests/test-edges-refines-rootcauses-instantiates-floor.cjs. NO fenced code in this action; the implementation lives in edges.cjs following the cited template lines.</action>
  <verify>
    <automated>node tests/test-edges-domain-taxonomy-floor.cjs</automated>
  </verify>
  <done>The floor test passes: the four new types are members, every prior type is preserved, the Set is frozen, and a made-up domain edge is rejected by writeEdge.</done>
</task>

<task type="auto">
  <name>Task 2: Land the canon amendment text + phase-map rows (the docs lockstep)</name>
  <files>docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md, tests/run-all-163.sh</files>
  <action>In docs/MINDRIAN-CANON.md: (a) extend the Part 4 typed-edge vocabulary paragraph to name the four new edges as the domain-taxonomy relationship edges (mirror how the Phase 150.8 trio REFINES/ROOT_CAUSES/INSTANTIATES is woven into the same paragraph). (b) Add Appendix D entry 21 (the next number after entry 20) recording the four-edge amendment, written in the voice of entry 18 (the three-edge precedent): name each edge + its semantics + direction + legal endpoints, cite navigator-LOCKED D-163-03, cite the Part 6 dog-fooding canon-amendment-on-itself mechanism mirroring entries 14/15/18/19/20, and state the floor test as the fence. (c) Bump the header Version line (currently 1.9) to 1.10 and the footer "_Mindrian Canon v1.9_" line to v1.10. In docs/CANON-PHASE-MAP.md: (a) update the "Canon reference" line to v1.10. (b) Add a Phase 163 row under the "v1.13.1 Larry Reaches connector spine + engine flip" section (or a new v1.14.0 section header if cleaner) with canon_parts and a one-line description of the amendment + the foundation it gates. (c) Add a v1.10 version-history row dated 2026-06-18 recording the amendment, mirroring the v1.7 entry-18 row format. Finally create tests/run-all-163.sh as the phase aggregator scaffold, mirroring tests/run-all-156.sh structure: a CJS_SUITES array (Wave 1 registers test-edges-domain-taxonomy-floor.cjs; later waves append their suites), a per-suite PASS/FAIL loop that runs to completion, an em-dash sweep over the phase artifacts written via the U+2014 codepoint escape, and a final tally with exit 1 on any failure. Reference D-163-03 in the amendment text for traceability. NO em-dashes in any edited or created file.</action>
  <verify>
    <automated>bash tests/run-all-163.sh && grep -c "DECOMPOSED_INTO" docs/MINDRIAN-CANON.md | grep -qv '^0$' && grep -q "v1.10" docs/MINDRIAN-CANON.md && echo CANON_OK</automated>
  </verify>
  <done>run-all-163.sh runs green (floor test passes, em-dash sweep clean), MINDRIAN-CANON.md Part 4 + Appendix D entry 21 name all four edges, the version is 1.10 in header and footer, and CANON-PHASE-MAP.md carries the Phase 163 row + v1.10 version-history row.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The four-edge frozen-vocabulary amendment: edges.cjs additive block (DECOMPOSED_INTO / PART_OF / TAGGED_WITH / RELATED_TO), the floor test, the MINDRIAN-CANON.md Part 4 + Appendix D entry 21 + version bump to 1.10, the CANON-PHASE-MAP.md rows, and the run-all-163.sh scaffold.</what-built>
  <how-to-verify>
    This is a NAVIGATOR-GATED frozen-constitutional-set move (D-163-03); it cannot auto-advance. Confirm:
    1. `node tests/test-edges-domain-taxonomy-floor.cjs` passes (four new + all prior + frozen + negative).
    2. `git diff docs/MINDRIAN-CANON.md` shows ONLY the Part 4 edge-list addition, Appendix D entry 21, and the two version lines (1.9 -> 1.10). No doctrine drift.
    3. The four edge semantics + directions + legal endpoints in Appendix D entry 21 match your intent for the connective taxonomy layer (D-163-01).
    4. `bash tests/run-all-163.sh` is green.
  </how-to-verify>
  <resume-signal>Type "approved" to ratify the amendment, or describe the edge-semantic corrections needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge-write -> room.db | writeEdge validates edge_type against the frozen Set before any INSERT |
| canon doc -> code | the amendment text must match the shipped Set (drift = a Part 6 self-CONTRADICTS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | Tampering | ALLOWED_EDGE_TYPES Set | mitigate | floor test asserts FLOOR + frozen Set + made-up-type negative; additive-only block, never a rewrite |
| T-163-02 | Repudiation | canon amendment | mitigate | navigator-gated blocking checkpoint; Appendix D entry 21 records D-163-03 provenance |
| T-163-03 | Information disclosure | new edge properties | accept | props are ENUM/scalar only (taxonomy node id + relation enum); never prose; never cross to Brain (asserted by the Wave 6 frozen-edge-set + leak scan) |
| T-163-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages in this wave (pure Node built-ins + doc edits); no install task |
</threat_model>

<verification>
- `node tests/test-edges-domain-taxonomy-floor.cjs` passes.
- `bash tests/run-all-163.sh` is green.
- MINDRIAN-CANON.md version is 1.10 (header + footer); Appendix D entry 21 names all four edges with direction + legal endpoints.
- CANON-PHASE-MAP.md carries the Phase 163 row + the v1.10 version-history row; canon reference updated to v1.10.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The four domain edges are frozen members writeEdge accepts; a non-member is rejected; the canon and
phase map record the amendment in lockstep; the floor test and run-all-163.sh are green; the
navigator has ratified the frozen-set move at the blocking checkpoint.
</success_criteria>

<output>
Create `.planning/phases/163-trending-to-absurd-harness/163-01-SUMMARY.md` when done.
</output>
