---
phase: 163-trending-to-absurd-harness
plan: 05
type: execute
wave: 5
depends_on: [163-04]
files_modified:
  - lib/core/trending-to-absurd/stage7-roadmap.cjs
  - lib/core/trending-to-absurd/variance.cjs
  - commands/trending-to-absurd.md
  - tests/test-trending-to-absurd-stage7.cjs
  - tests/test-trending-to-absurd-variance.cjs
  - tests/run-all-163.sh
autonomous: true
requirements: [D-163-05, D-163-06]
canon_parts: [2, 3, 4, 5, 8, 9, 10]

must_haves:
  truths:
    - "Stage 7 emits a mitigation / innovation roadmap section mapping each opportunity to UDP/IDP/WDP, filed under opportunity-bank/ as a nested artifact"
    - "all four persona lenses (Founder / Researcher / Investor / Analyst) are selectable via a Shape F gate"
    - "all three paths (Quick / Full / Expert) are selectable and gate Shape F at the judgment points per the hybrid default"
    - "persona + path selection writes a typed edge (the selection becomes graph data, Part 4)"
  artifacts:
    - path: "lib/core/trending-to-absurd/stage7-roadmap.cjs"
      provides: "Stage 7 mitigation/innovation roadmap generator + UDP/IDP/WDP mapping"
      exports: ["generateStage7Roadmap"]
      min_lines: 60
    - path: "lib/core/trending-to-absurd/variance.cjs"
      provides: "the 4-persona x 3-path variance matrix + Shape F gate descriptors"
      exports: ["PERSONA_LENSES", "PATH_VARIANTS", "surfacePersonaPathGate"]
      min_lines: 60
  key_links:
    - from: "lib/core/trending-to-absurd/stage7-roadmap.cjs"
      to: "room/opportunity-bank/"
      via: "nested artifact write with ROOM.md (ICM Layer 0)"
      pattern: "opportunity-bank"
    - from: "lib/core/trending-to-absurd/variance.cjs"
      to: "lib/core/navigation.cjs"
      via: "writeEdge records the persona/path selection as graph data"
      pattern: "writeEdge|SELECTED_REACH"
---

<objective>
WAVE 5 SURFACE-B: Stage 7 (the mitigation / innovation roadmap absent from explore-trends) plus the
FULL variance surface (D-163-06): all four persona lenses (Founder / Researcher / Investor / Analyst)
and all three paths (Quick / Full / Expert), surfaced through Shape F hybrid gates (D-163-05). This
completes the agent to the spec.

Purpose: D-163-06 ships FULL variance in v1 -- not a subset. Stage 7 is the net-new output section the
7-stage spec adds beyond explore-trends's 6 stages: each surfaced opportunity maps to UDP/IDP/WDP
(the /mos:diagnose problem-type taxonomy) and gets a mitigation/innovation roadmap. The persona lens
reshapes the framing (a Founder reads the absurd extrapolation differently than an Investor); the path
variant sets the depth (Quick = fewer rings + auto, Full = all rings + hybrid gates, Expert = all
rings + multi-agent refinement). All variance rides the SAME hybrid rails (D-163-05): auto where
autonomous_safe, Shape F gate at the judgment points.

Output: the Stage 7 generator, the variance matrix + gate, the command-body wiring, the two test
suites, and the run-all-163 registration. NO em-dashes (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-trending-to-absurd-harness/163-CONTEXT.md
@lib/core/trending-to-absurd/orchestrator.cjs
@lib/core/futures/orchestrator.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage 7 mitigation / innovation roadmap generator</name>
  <files>lib/core/trending-to-absurd/stage7-roadmap.cjs, tests/test-trending-to-absurd-stage7.cjs, tests/run-all-163.sh</files>
  <behavior>
    - Test 1: generateStage7Roadmap(roomDir, opportunities, opts) maps each opportunity to a UDP / IDP / WDP problem type and returns a structured roadmap {opportunity_id, problem_type, mitigation_steps:[], innovation_steps:[]}.
    - Test 2: it files the roadmap as a nested artifact under room/opportunity-bank/trending-to-absurd-<seed>/stage7-roadmap/ WITH a ROOM.md (ICM Layer 0), reusing the futures registerConsequenceArtifacts folder discipline.
    - Test 3: a roadmap step that asserts a venture truth lands review_status 'proposed' (Part 9 role 5), never auto-confirmed.
    - Test 4: an empty opportunity set returns an empty roadmap without crashing (defensive).
  </behavior>
  <action>Create lib/core/trending-to-absurd/stage7-roadmap.cjs exporting generateStage7Roadmap(roomDir, opportunities, opts). For each banked opportunity, classify it into the UDP / IDP / WDP problem-type taxonomy (the /mos:diagnose vocabulary named in CONTEXT scope item 3 + the futures FORESIGHT_WEB_PARTNERS diagnose handle) and emit a mitigation/innovation roadmap (mitigation_steps = how to defend against the absurd-trend risk; innovation_steps = how to seize the disruptive opportunity). File the roadmap as a NESTED Obsidian artifact under opportunity-bank/ reusing the futures writeRoomMd + nested-folder discipline (registerConsequenceArtifacts pattern at futures/orchestrator.cjs:327-377) -- exclusive ownership, ICM Layer 0 ROOM.md per folder (CLAUDE.md decisions 15 + 16). Evidence tier per Part 5: tag each roadmap step with an evidence tier; a step asserting a venture truth is a truth-claim and stays proposed (Part 9). Defensive: empty set -> empty roadmap, never throw. Part 8: zero Brain egress; pure LOCAL generation + file write. Create tests/test-trending-to-absurd-stage7.cjs covering behaviors 1-4 (use a tmp room dir). Register in run-all-163.sh + em-dash sweep. NO fenced code; follow the cited futures functions.</action>
  <verify>
    <automated>node tests/test-trending-to-absurd-stage7.cjs</automated>
  </verify>
  <done>Stage 7 maps each opportunity to UDP/IDP/WDP and files a mitigation/innovation roadmap as a nested artifact with ROOM.md; truth-claim steps stay proposed; empty input is handled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The 4-persona x 3-path variance matrix + Shape F hybrid gate</name>
  <files>lib/core/trending-to-absurd/variance.cjs, commands/trending-to-absurd.md, tests/test-trending-to-absurd-variance.cjs, tests/run-all-163.sh</files>
  <behavior>
    - Test 1: PERSONA_LENSES is a frozen array of exactly the four lenses Founder / Researcher / Investor / Analyst (D-163-06).
    - Test 2: PATH_VARIANTS is exactly Quick / Full / Expert, each carrying a depth + gate-policy descriptor (Quick = fewer rings + auto; Full = all rings + hybrid gates; Expert = all rings + multi-agent refinement).
    - Test 3: surfacePersonaPathGate(roomDir, opts) returns a Shape F gate descriptor offering the four personas and three paths (the hybrid judgment-point selector, D-163-05).
    - Test 4: recordPersonaPathSelection(db, {persona, path, focusNodeId}) writes a typed edge via navigation.writeEdge so the selection is graph data (Part 4) with enum-only props (Part 8).
  </behavior>
  <action>Create lib/core/trending-to-absurd/variance.cjs exporting PERSONA_LENSES (frozen ['Founder','Researcher','Investor','Analyst'] -- the four per D-163-06, aligned with the Canon Appendix E SME archetypes), PATH_VARIANTS (frozen {Quick:{rings:1, gate_policy:'auto'}, Full:{rings:FUTURES_DEPTH_CAP, gate_policy:'hybrid'}, Expert:{rings:FUTURES_DEPTH_CAP, gate_policy:'hybrid', multi_agent:true}} -- reuse FUTURES_DEPTH_CAP from the futures orchestrator), surfacePersonaPathGate(roomDir, opts) returning a Shape F gate descriptor (F.2 path-control for the path choice, F.1 next-move for the persona) offering the personas + paths (mirror the futures surfaceBridgesAtGate descriptor shape; D-163-05 hybrid), and recordPersonaPathSelection(db, params) writing the selection as a typed edge via navigation.writeEdge (use TAGGED_WITH or SELECTED_REACH carrying enum-only props {persona, path}; the selection becomes graph data per Part 4; properties ENUM/scalar only per Part 8). The multi-agent Expert refinement is a descriptor flag the command body dispatches (the economic/tech/social/environmental sub-agents named in the spec ride the existing Canon Part 2 SUB-AGENT SPAWN affordance, NOT a new mechanism). Then wire commands/trending-to-absurd.md: add the persona + path gate to the command body (it fires surfacePersonaPathGate before the trend-selection gate; the chosen path sets the ring depth + gate policy for the orchestrator's hybrid run; the chosen persona reshapes the Larry framing). Create tests/test-trending-to-absurd-variance.cjs covering behaviors 1-4. Register in run-all-163.sh + em-dash sweep. NO fenced code; follow the cited futures descriptor shape.</action>
  <verify>
    <automated>node tests/test-trending-to-absurd-variance.cjs && node -e "const {PERSONA_LENSES,PATH_VARIANTS}=require('./lib/core/trending-to-absurd/variance.cjs'); if(PERSONA_LENSES.length!==4) process.exit(1); if(!PATH_VARIANTS.Quick||!PATH_VARIANTS.Full||!PATH_VARIANTS.Expert) process.exit(1); console.log('VARIANCE_OK')" && bash tests/run-all-163.sh</automated>
  </verify>
  <done>All four personas + all three paths are selectable via Shape F gates (D-163-06); the path sets depth + gate policy on the hybrid rails (D-163-05); the persona reshapes the framing; the selection is recorded as a typed edge (Part 4) with enum-only props; run-all-163.sh green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 7 -> room/opportunity-bank/ | exclusive file ownership; nested artifact + ROOM.md only |
| persona/path selection -> room.db | navigation.writeEdge chokepoint; enum-only props |
| Expert multi-agent refinement | rides the existing Part 2 SUB-AGENT SPAWN affordance, not a new egress path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-13 | Tampering | Stage 7 file write | mitigate | pinned under opportunity-bank/; Wave 6 ownership check asserts no escape |
| T-163-14 | Elevation of privilege | Stage 7 truth-claim steps | mitigate | venture-truth steps land 'proposed'; only confirmNode human byUser promotes (Part 9) |
| T-163-15 | Information disclosure | persona/path edge + Expert sub-agents | mitigate | edge props enum-only (persona/path); Expert sub-agents inherit the Part 8 boundary of the existing SUB-AGENT SPAWN affordance; Wave 6 leak scan asserts |
| T-163-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (pure Node built-ins + reuse of the futures orchestrator + navigation); no install task |
</threat_model>

<verification>
- `node tests/test-trending-to-absurd-stage7.cjs` and `node tests/test-trending-to-absurd-variance.cjs` pass.
- PERSONA_LENSES has the four personas; PATH_VARIANTS has Quick / Full / Expert with depth + gate policy.
- Stage 7 files a nested roadmap with ROOM.md under opportunity-bank/; truth-claim steps stay proposed.
- The persona/path selection writes a typed edge with enum-only props.
- `bash tests/run-all-163.sh` green; no em-dashes.
</verification>

<success_criteria>
The agent is complete to the spec (D-163-06 FULL variance): all four personas + all three paths ship
in v1, Stage 7 maps each opportunity to UDP/IDP/WDP with a mitigation/innovation roadmap, and every
variance rides the hybrid Shape F rails (D-163-05) with the selection recorded as graph data (Part 4).
</success_criteria>

<output>
Create `.planning/phases/163-trending-to-absurd-harness/163-05-SUMMARY.md` when done.
</output>
