---
phase: 243-voice-glyph
plan: 02
type: execute
wave: 2
depends_on: ["243-01"]
canon_parts: [11, 12]
files_modified:
  - tests/test-243-rca-routing.cjs
  - .planning/debug/voice-signature-dark-runtime.md
autonomous: true
requirements: [GLYPH-01]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: []
  gate_impact: "none. This plan creates one RCA document under .planning/debug/ and one doc-presence test. It adds, modifies, and removes zero invocable surfaces, mints no reach/edge/node, and opens no Brain wire. All three CIRS gates (build-connector-registry.cjs --check, build-orchestration-projection.cjs --check, check-render-coverage.cjs) are unaffected."
  explanation: "Phase 243 plan 02 neither uses nor is used by CIRS. It writes a markdown incident report and a test that greps it. The R12 trigger (files under commands/|skills/|agents/, OR a non-empty spine_consumed) is NOT met, so this block is filed voluntarily for completeness, matching the 177-05-PLAN.md precedent. Per the R12 auto-derivation rule, declaring the block puts 11 in canon_parts; 12 is declared because the RCA's subject is Canon Part 12 (The Voice Signature) doctrine, including a sub-finding that records an unresolved Part 10 versus Part 12 conflict for the navigator."

must_haves:
  truths:
    - "The RCA at .planning/debug/voice-signature-dark-runtime.md EXISTS and can be read - the six documents that cite it no longer point at a phantom."
    - "Reading that RCA shows V-2 and V-3 as OPEN, cross-referenced sub-findings with their sites and their blockers named."
    - "Reading that RCA shows V-1 as resolved-history, closed by Phase 243, with the fix site named."
    - "Reading that RCA shows the F5 residual as its own named OPEN item: the Tier-1 voice glyph is dark in production, permanently, by design, until a session-keyed writer lands."
    - "The RCA carries a fourth OPEN sub-finding for the who: 'larry' default, filed for a navigator ruling rather than fixed, with the Part 10 versus Part 12 conflict quoted from its three primary sources."
    - "Exactly ONE voice-signature RCA file exists repo-wide - no SECOND RCA was spawned (SC2's intent)."
    - "The RCA contains no fabricated date, quote, attribution, or reproduction step - every claim traces to something verified in 243-RESEARCH.md."
  artifacts:
    - path: ".planning/debug/voice-signature-dark-runtime.md"
      provides: "The RCA the repo has cited since 2026-06-28, carrying V-1 resolved-history plus V-2/V-3/who-default/F5-residual as open items"
      contains: "kind: rca"
      min_lines: 110
    - path: "tests/test-243-rca-routing.cjs"
      provides: "Doc-presence gate for SC2: frontmatter keys, required headings, V-1/V-2/V-3 and GLYPH-01 citations, no-em-dash check. Structure only, never prose wording."
      min_lines: 45
  key_links:
    - from: "tests/test-243-rca-routing.cjs"
      to: ".planning/debug/voice-signature-dark-runtime.md"
      via: "fs.readFileSync on a path.join'd repo-relative path, then structural assert.match calls"
      pattern: "voice-signature-dark-runtime"
    - from: "tests/run-all-243.sh"
      to: "tests/test-243-rca-routing.cjs"
      via: "glob-discovery of tests/test-243-*.cjs, created by plan 243-01 - this plan edits no harness"
      pattern: "test-243-\\*"
    - from: ".planning/debug/voice-signature-dark-runtime.md"
      to: ".planning/REQUIREMENTS.md GLYPH-01"
      via: "an explicit citation back to the requirement that ordered the filing"
      pattern: "GLYPH-01"
---

<objective>
GLYPH-01 half 2 (Success Criterion 2): route V-2 and V-3 into the `voice-signature-dark-runtime.md` RCA as cross-referenced entries, "verifiable by reading that RCA".

The complication, and the whole reason this is a plan rather than a footnote: **the file does not exist and never has.** Six documents cite it as the existing open RCA. All three verification commands come back empty - `git log --all --oneline -- .planning/debug/voice-signature-dark-runtime.md` prints nothing, `find . -name "voice-signature*"` prints nothing, and the file is absent from both `.planning/debug/` and `.planning/debug/resolved/`. Phase 182.1 ran inline, wrote the RCA citation into four artifacts as a forward reference, and never authored the file. The audit that produced V-1/V-2/V-3 then read those citations and concluded the RCA was "still open", which is how the phantom propagated into REQUIREMENTS.md and ROADMAP.md.

Purpose: give the repo's oldest dangling citation a real referent, and consolidate a voice-signature debt that five phases each recorded in a different document into one readable file.
Output: the RCA, plus a structural gate that keeps it honest without ossifying its prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-voice-glyph/243-RESEARCH.md
@.planning/phases/243-voice-glyph/243-PATTERNS.md
@.planning/phases/243-voice-glyph/243-VALIDATION.md
</context>

## The F1 decision fork, resolved here in writing (do NOT let the executor improvise)

Success Criterion 2 says "no new RCA file created". Read literally against a file that does not exist, SC2 is unsatisfiable. Three options were assessed in RESEARCH.md Finding F1:

| Option | Verdict |
|--------|---------|
| **A - CREATE `.planning/debug/voice-signature-dark-runtime.md` per `docs/RCA-TEMPLATE.md`, carrying V-1 as resolved-history and V-2/V-3 as open cross-referenced sub-findings** | **CHOSEN.** Satisfies SC2's intent (one RCA, cross-referenced entries, readable) and repairs six dangling citations at once. |
| B - Re-point all six citations at a different existing RCA | REJECTED. No existing RCA covers voice-signature. Would fabricate a relationship. |
| C - Amend REQUIREMENTS.md / ROADMAP.md to drop the RCA requirement | REJECTED. Loses V-2/V-3 entirely; the audit explicitly wanted them recorded. |

**The reading this plan adopts, stated so nobody has to guess later:** SC2's "no new RCA file created" means "no SECOND RCA beside the one the repo already names". That is what the audit's own rethink verdict said - "Findings 2 and 3 belong as additions to the still-open file". Creating the file the repo has been citing for a month is not spawning a competitor; it is giving six existing citations their referent. The acceptance criteria enforce the "no SECOND RCA" reading literally, by counting matching files.

## The no-fabrication constraint (a hard rule, not a style note)

Per this repo's QA/RCA standard and the standing no-fabrication rule, the RCA may contain ONLY claims verifiable at authoring time.

- Do NOT invent a `created:` date in June to make the "still-open since 182.1" story tidy. The file is created 2026-07-28 and `Meta` says so plainly.
- Do NOT attribute any quote to the navigator that is not already quoted in `243-RESEARCH.md`.
- Do NOT write reproduction steps that were never run. The reproduction steps that WERE run are in RESEARCH.md F2 and F1 verbatim; reuse those exact commands and no others.
- Do NOT re-derive facts. Every fact needed is already verified in RESEARCH.md Findings F1, F2, F4, F5, F7 and the State of the Art table. If something you want to write is not in there, it does not go in.

## Artifacts this phase produces

New symbols created across the whole of Phase 243 (both plans), listed here so no downstream reviewer flags them as unexplained references:

| Artifact | Plan | Kind |
|----------|------|------|
| `tests/test-243-voice-glyph-honest.cjs` | 243-01 | NEW test (fixture + mutation gate, SC1a/b/c) |
| `tests/run-all-243.sh` | 243-01 | NEW phase aggregator (glob-discovering) |
| `.planning/debug/voice-signature-dark-runtime.md` | 243-02 | NEW RCA document (SC2) |
| `tests/test-243-rca-routing.cjs` | 243-02 | NEW test (doc-presence, SC2) |

This plan's own contributions: the RCA document and its doc-presence gate.

## Why this plan is Wave 2 and not parallel with 243-01

Content-wise the two halves are independent - the RCA touches no source file and plan 01 touches no document. The dependency is a HARNESS dependency, and it is real: this plan's full-suite verification is `bash tests/run-all-243.sh`, and that aggregator is created by plan 01. Running the two plans concurrently would also make each one's aggregator run observe the other's half-landed files - plan 01's `run-all-243.sh` glob-discovers `tests/test-243-rca-routing.cjs` the moment it appears, and would go red on a missing RCA that plan 01 has nothing to do with. Sequencing removes a genuine flake at zero cost, because the phase is two plans either way.

## File-handling note (do NOT copy the `git add -f` habit here)

`.gitignore` lines 79-80 are `.planning/*` followed by `!.planning/debug/`, so `.planning/debug/` is UN-ignored and its files are tracked by a plain `git add`. Verified at planning time: `git check-ignore -v .planning/debug/voice-signature-dark-runtime.md` exits 1 (not ignored), and `git ls-files .planning/debug/` already lists 38 tracked RCA files.

This is worth stating because every OTHER phase-243 planning document (`243-RESEARCH.md`, `243-PATTERNS.md`, `243-VALIDATION.md`, both PLAN files) DOES need `git add -f`, and `docs/RCA-TEMPLATE.md` itself says `.planning/` files are committed with `git add -f`. An executor copying that habit here wastes a step. `git add -f` on an already-unignored path is harmless, so this is a clarity note, not a failure mode.

<tasks>

<task type="auto">
  <name>Task 1: Land the RCA doc-presence gate (RED until the RCA exists)</name>
  <files>tests/test-243-rca-routing.cjs</files>
  <read_first>
    docs/RCA-TEMPLATE.md (sections 2.5 and 3 - section 2.5 is the MANDATORY Source-of-Truth Preamble and section 3 is the copy-paste markdown template carrying the exact frontmatter key list (status, kind, trigger, issue_id, severity, surfaces, brain_mode, canon_parts, created, updated) and the exact heading order. The test asserts against THAT list, so read it before writing a single assertion. Note the hard rule restated in the template intro: NO em-dashes anywhere in an RCA file.),
    tests/test-stance-voice-glyph-override.cjs (the doc-presence idiom this repo already uses - RESEARCH.md names it "explicitly a DOCUMENTATION-presence test". Copy its structure of reading a file and asserting on presence, not on prose.),
    tests/run-all-243.sh (created by plan 243-01. Confirm its glob is `tests/test-243-*.cjs` so this new file is auto-discovered and NO harness edit is needed.),
    .planning/phases/243-voice-glyph/243-PATTERNS.md (the `tests/test-243-rca-routing.cjs` section carries a ready-to-adapt sketch),
    .planning/phases/243-voice-glyph/243-RESEARCH.md Validation Architecture, the SC2 row (defines exactly what this test may and may not assert)
  </read_first>
  <action>
    Create `tests/test-243-rca-routing.cjs`, a standalone CJS assertion script using `node:assert/strict`, `node:fs`, and `node:path`. Resolve `RCA_PATH` as `path.join(__dirname, '..', '.planning', 'debug', 'voice-signature-dark-runtime.md')`.

    Assert EXISTENCE first with a failure message that names the full path, so a red run tells the reader exactly which file is missing.

    Then read the file with `fs.readFileSync(RCA_PATH, 'utf8')` and assert STRUCTURE ONLY:

    Frontmatter keys: `kind: rca` present; `slug: voice-signature-dark-runtime` present; `trigger: "voice-signature-dark-runtime"` present; a `status:` key present whose value is NOT `resolved` (use `assert.doesNotMatch` against a `status:\s*resolved` pattern - V-2, V-3, the who-default finding, and the F5 residual all stay open, so this file must never be moved to `.planning/debug/resolved/` by this phase); `canon_parts:` containing 12; `surfaces:` containing `cli` (the statusline is CLI-only).

    Required headings, each asserted as a line-anchored `## ` match: `## Source-of-Truth Preamble`, `## Current Focus`, `## Meta`, `## Problem Statement`, `## Symptoms`, `## Scope and Impact`, `## Evidence`, `## Technical Root Cause`.

    Required cross-references: the strings `V-1`, `V-2`, `V-3`, and `GLYPH-01` each appear at least once, and `REQUIREMENTS.md` appears at least once (the citation back to the requirement that ordered the filing).

    Em-dash gate: assert the file contains no em-dash. CRITICAL implementation detail - express the em-dash in the TEST SOURCE as a unicode escape (a `/\u2014/` regex literal, or `String.fromCharCode(0x2014)`), never as a literal em-dash byte. A literal-em-dash-byte detector that self-trips is a real bug this repo has already hit and fixed once: `.planning/STATE.md` records it under Phase 179 as "a literal-em-dash-byte detector that self-tripped, rewritten with unicode escapes".

    Do NOT assert any full sentence, any prose wording, any word count, or any specific date value. Asserting prose would ossify a document that is meant to keep accumulating Evidence and Eliminated entries. Add a header comment saying exactly that, so a future maintainer does not "helpfully" tighten it.

    Print a pass/fail count and exit non-zero on failure, matching the harness idiom in the sibling `tests/test-243-voice-glyph-honest.cjs`. Hyphens only in every message you write.

    At the end of this task the test is RED because the RCA does not exist yet. That is correct and intended.
  </action>
  <verify>
    <automated>node --check tests/test-243-rca-routing.cjs && ! node tests/test-243-rca-routing.cjs && git diff --quiet tests/run-all-243.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node --check tests/test-243-rca-routing.cjs` exits 0.
    - `node tests/test-243-rca-routing.cjs` exits NON-ZERO, and its first failure message contains the literal path `.planning/debug/voice-signature-dark-runtime.md` so a reader knows immediately what is missing.
    - Glob-discovery works with ZERO harness edits: `bash tests/run-all-243.sh` prints the filename `test-243-rca-routing.cjs` in its output, and `git diff --quiet tests/run-all-243.sh` exits 0 (the aggregator was not touched).
    - The em-dash detector uses a unicode escape, not a literal byte: `grep -c 'u2014\|0x2014' tests/test-243-rca-routing.cjs` returns 1 or more, AND `grep -vE '^\s*//' tests/test-243-rca-routing.cjs | grep -cF "$(printf '\u2014')"` returns 0 (comments stripped first so header prose cannot self-trip the check).
    - The test asserts structure, not prose: `grep -c 'assert' tests/test-243-rca-routing.cjs` returns 15 or more, and no assertion argument contains a full sentence quoted from the RCA.
    - `grep -c 'doesNotMatch' tests/test-243-rca-routing.cjs` returns 1 or more (the status-not-resolved assertion is present).
  </acceptance_criteria>
  <done>The SC2 gate exists, is auto-discovered by the phase aggregator, and is RED for exactly one reason: the RCA has not been written yet.</done>
</task>

<task type="auto">
  <name>Task 2: Author the RCA the repo has been citing since 2026-06-28</name>
  <files>.planning/debug/voice-signature-dark-runtime.md</files>
  <read_first>
    docs/RCA-TEMPLATE.md (FULL sections 2.5 and 3 - the mandatory Source-of-Truth Preamble block and the complete copy-paste markdown template including the sections after Technical Root Cause: Required Code Changes, Tests to Add or Update, Non-Code Follow-ups, Resolution. Follow its fixed section order exactly; agents key off the headings.),
    .planning/debug/resolved/hedge-fold-has-no-production-trigger.md (the WORKED EXAMPLE, named by RESEARCH.md as the closest analog because it has the same shape - a fully built layer with no production trigger. Read its frontmatter, Source-of-Truth Preamble, Current Focus, Meta, Problem Statement, and Symptoms to copy the register and the level of specificity. Note especially how its Symptoms section says "errors: none. There is no error to observe" - the same silent-degrade shape applies here.),
    .planning/phases/243-voice-glyph/243-RESEARCH.md (FULL. This is the ONLY permitted source of content. Findings F1 (the six citing documents table with line numbers, the three verification commands, the decision fork), F2 (the fabrication site plus the live reproduction command and its verbatim before/after output), F4 (the who default, its three re-assertion sites, and the Part 10 versus Part 12 conflict quoted from four primary sources), F5 (the residual and its three supporting citations), F7 (the V-4 exclusions and the false provenance comment), and the State of the Art table (the 182 -> 182.1 -> 187 -> 187.1 -> 192 -> 210 -> 243 through-line). Do not add any fact that is not in this file.),
    .planning/phases/243-voice-glyph/243-PATTERNS.md (the RCA section's frontmatter values and heading-order sketch),
    docs/STATUSLINE-CONTRACT.md around line 219 (open item 1, "The voice-mark WRITE-side hook ... the shared prerequisite with Phase 182.1" - the RCA CITES this rather than re-deriving the debt),
    .planning/phases/243-voice-glyph/243-01-SUMMARY.md (plan 01's outcome - the RCA records V-1 as resolved by it, so the fix commit and the mutation-probe result belong in the Resolution and Evidence sections)
  </read_first>
  <action>
    Create `.planning/debug/voice-signature-dark-runtime.md` following `docs/RCA-TEMPLATE.md` section 3's markdown template in its exact section order.

    Frontmatter: `status: investigating` (NOT resolved - four items stay open), `kind: rca`, `slug: voice-signature-dark-runtime`, `trigger: "voice-signature-dark-runtime"`, `issue_id: ""`, `severity: medium`, `surfaces: [cli]` (the statusline is CLI-only; Desktop and Cowork have no statusline, and Part 12's Voice Signature rides the turn-text glyph there instead), `brain_mode: local-only`, `canon_parts: [12]`, `created:` and `updated:` both 2026-07-28 in the template's ISO form.

    Source-of-Truth Preamble (MANDATORY, template section 2.5): CODE claims read against branch `main` at the commit this plan executes on, naming the dev workspace path. WIRE claims probe against none - this is a pure LOCAL rendering finding with no Brain call, no network probe, no deployed server. Date of audit 2026-07-28. Re-verification rule stating that every source claim was produced by a grep or a node run against the working tree at that commit, with the exact commands reproduced in Evidence.

    Meta: state PLAINLY that this file was authored on 2026-07-28 during Phase 243 to back six citations that predate it, and that it did not exist before - verified three ways (`git log --all --oneline` on the path returned nothing, `find . -name "voice-signature*"` returned nothing, and the file was absent from both `.planning/debug/` and `.planning/debug/resolved/`). Name Phase 182.1's inline execution mode as the root cause of the phantom: the citation was written into four artifacts as a forward reference and the file was never authored. Do NOT backdate anything.

    Problem Statement: one or two sentences. The Voice Signature has been dark at runtime since Phase 182, and the statusline covered the darkness with a fabricated glyph rather than showing it.

    Symptoms: expected versus actual in the template's key-value form. Note, in the hedge-fold register, that there is no error to observe - the fabricated glyph looked exactly like a working feature, which is the whole difficulty.

    Scope and Impact: affected surfaces cli; affected commands the statusline pipeline (`scripts/statusline-mos` -> `scripts/context-monitor` -> `cockpit-signals` -> `cockpit-renderer`) and `/mos:stance`; affected users all installs; blast radius every statusline render on every install.

    Evidence (APPEND-only): the three F1 verification commands with their empty output; the F2 live reproduction command with its verbatim before output `⬡ 🟥 · [redteam] · 📂 Demo ✅ · Next: x` and its after output `⬡ · [redteam] · 📂 Demo ✅ · Next: x`; the grep proving exactly two `~/.mindrian/voice-mark.json` references repo-wide and both are readers; the Phase 243 mutation-probe run from plan 01 Task 3 with its exit code and failing row names.

    Technical Root Cause: the write side of the voice-mark side-channel was never built. `readVoiceGlyph` at `lib/statusline/cockpit-signals.cjs` reads `~/.mindrian/voice-mark.json`; zero writers exist repo-wide; therefore `resolveVoiceGlyph` returns null on 100% of production turns, and every downstream consumer either goes dark or fabricates.

    Then FIVE clearly headed finding blocks. Each names its site by file plus symbol, never by line number alone (RESEARCH.md Pitfall 4 - the source audit's line numbers had already drifted).

    1. **V-1, RESOLVED by Phase 243.** The fabricated glyph. Site: `lib/statusline/cockpit-renderer.cjs` `_render`, the branch that defined `stanceDefaultGlyph`. It substituted the active stance's default De Stijl color when natural detection returned null, and because null is the permanent state, it was the only thing that ever painted a glyph on a live statusline. Fix: branch deleted; the three assertions in `tests/test-voice-glyph-advisory.cjs` leg 3 and `tests/test-192-statusline-stance-chip.cjs` cases (b) and (c) INVERTED rather than deleted; `tests/test-243-voice-glyph-honest.cjs` added as an 18-row gate; mutation probe executed. Record that Phase 243 SUPERSEDES the second half of Phase 210 item B (natural-detection-wins survives, stance-fills-the-default dies) and why: Phase 210 assumed silence was occasional when it is permanent.

    2. **V-2, OPEN.** No writer exists for `~/.mindrian/voice-mark.json`. Exactly two references repo-wide, `lib/statusline/cockpit-signals.cjs` at both its read sites, plus one comment in `lib/core/voice-transition-detector.cjs`. Blocked not on effort but on concurrency: the checkpoint schema in `voice-transition-detector.cjs` (`current_who`, `session_has_switched`, `last_fire_turn`, `turn_index`) and `lib/core/stance-state.cjs` are both HOME-global with no session key, because `mindrianDir()` resolves to `~/.mindrian` rather than a per-session or per-room path, so N concurrent worktree agents would share one file. Cite `docs/STATUSLINE-CONTRACT.md:219` open item 1 and `.planning/STATE.md:829` (the Phase 187 named debt) rather than re-deriving the debt.

    3. **V-3, OPEN.** The pure transition detector `lib/core/voice-transition-detector.cjs` has zero production callers and the F.7 recalibration dial is unwired. Record the false provenance claim at `lib/statusline/cockpit-signals.cjs` (the comment asserting the schema is "written by the 187.1-02 checkpoint hook" when no such writer exists) as a finding, and state explicitly that Phase 243 chose to LEAVE that comment unedited - it is V-4 territory, REQUIREMENTS.md's GLYPH-01 names only V-1/V-2/V-3, and adding a second unrequired file diff contradicts the phase's "smallest blast radius" justification. Recording the false claim here preserves it for the phase that owns the write side.

    4. **The `who: 'larry'` default, OPEN, FILED FOR A NAVIGATOR RULING, NOT FIXED.** A SEPARATE defect from the glyph. Site: `scripts/context-monitor` (locate by the `data.agent` name ternary), independently re-asserted at `lib/statusline/cockpit-signals.cjs` and again at `lib/statusline/cockpit-renderer.cjs`. Claude Code's own statusline documentation states that `agent` appears only when running with the `--agent` flag or agent settings configured, so in an ordinary session `who` evaluates to `larry` unconditionally. The doctrinal conflict, with all four sources named: `docs/MINDRIAN-CANON.md:661` (Part 12, HARD, navigator-LOCKED 2026-06-25) says a turn with no Larry mark IS the native host speaking and that absence is itself legible; `agents/larry-extended.md:48` says a turn with no glyph reads as the raw host, not Larry; `lib/core/voice-transition-detector.cjs` `deriveWho` implements exactly that; while the inline comments at `scripts/context-monitor` and `cockpit-signals.cjs` say "default larry - the conversational surface IS Larry (Part 10)". Why Phase 243 did not fix it: flipping the default to `claude` with no writer present would make every statusline on every install permanently render the host marker and permanently suppress the Brain chip (which is gated on `isLarry`) - honest, and also a severe universally-visible UX regression on a phase justified as smallest blast radius. Reconciling Part 10 and Part 12 is a navigator call, not an executor call. State the ask plainly: which reading governs the statusline.

    5. **The F5 RESIDUAL, OPEN, its own heading so it cannot be skimmed past.** After Phase 243 the Tier-1 voice glyph is DARK in production - on every turn, on every install, permanently - until a session-keyed writer lands. This is the correct honest state and it satisfies GLYPH-01 SC1. It is ALSO, on its own, indistinguishable from the failure that created Phase 182.1: a green suite certifying a feature that does nothing. The difference between honest and quietly dead is entirely whether it is written down, which is what this section is for. State that a navigator with a stance set today will see their glyph disappear after this ships, and that a placeholder glyph must NEVER be invented to fill the gap because that recreates the defect.

    Then a **named non-goals** block: V-4 in full (spoof-fence gaps, the mixed-mark gap, the checkbox special case, the false provenance comment, the stale `agents/larry-extended.md:56` line) is knowingly out of GLYPH-01's scope because REQUIREMENTS.md's GLYPH-01 text names only V-1/V-2/V-3 and the audit's own rethink verdict for V-4 was "no RCA, just the batch fix". Recorded so a future reader does not read the omission as an oversight.

    Then a **citation repair** block listing the six documents that cite this file with their line numbers - `.planning/REQUIREMENTS.md:52`, `.planning/ROADMAP.md:128`, `.planning/phases/182.1-signal-voice-glyph-repair/182.1-CONTEXT.md:26`, the matching `182.1-SUMMARY.md:21`, `.planning/milestones/v1.15.0-ROADMAP.md:3945`, and `docs/CANON-PHASE-MAP.md:373` - and stating that all six now resolve. Also state the F1 fork resolution: SC2's "no new RCA file created" is read as "no SECOND RCA beside the one the repo already names", which is what the audit's rethink verdict said.

    Then the template's remaining sections: **Required Code Changes** (V-1's change, marked done; the write-side change, marked blocked on the session-keying design decision; the `who` change, marked blocked on a navigator ruling), **Tests to Add or Update** (`tests/test-243-voice-glyph-honest.cjs` and `tests/test-243-rca-routing.cjs`, both registered via `tests/run-all-243.sh` glob-discovery), **Non-Code Follow-ups** (a CHANGELOG Fixed entry saying the glyph now appears only when a real mark is detected; the release-lockstep note that this fix is not live until a release ships AND is picked up; the reminder that when this RCA eventually resolves it moves to `.planning/debug/resolved/` and gets a `knowledge-base.md` block - and that this phase must NOT move it), and **Resolution** (partial: V-1 only, with the Phase 243 commit hashes and the verification commands run, as evidence rather than assertion).

    Hyphens only, no em-dashes anywhere in the file - `docs/RCA-TEMPLATE.md` restates this as a hard rule for RCA files specifically, and Task 1's gate enforces it.

    Commit with a plain `git add` - see the File-handling note above; `.planning/debug/` is un-ignored.
  </action>
  <verify>
    <automated>node tests/test-243-rca-routing.cjs && bash tests/run-all-243.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-243-rca-routing.cjs` exits 0 (every frontmatter key, every required heading, every cross-reference, and the no-em-dash gate).
    - `bash tests/run-all-243.sh` exits 0 with FAIL=0 - this now runs the fixture suite, the doc-presence test, and both mandatory regression legs (`run-all-192.sh`, `run-all-210.sh`).
    - Exactly ONE voice-signature RCA exists repo-wide (SC2's "no SECOND RCA" reading, enforced literally): `ls .planning/debug/*voice-signature* .planning/debug/resolved/*voice-signature* 2>/dev/null | wc -l` returns 1.
    - The file is genuinely new, and that is recorded rather than claimed: re-run `git log --all --oneline -- .planning/debug/voice-signature-dark-runtime.md` BEFORE staging and confirm it is empty; paste that command and its empty result into the RCA's Evidence section.
    - It stays open: `grep -c 'status: resolved' .planning/debug/voice-signature-dark-runtime.md` returns 0, and the file is NOT in `.planning/debug/resolved/`.
    - All five finding blocks are present and findable by heading: `grep -c '^#\+ .*V-1\|^#\+ .*V-2\|^#\+ .*V-3' .planning/debug/voice-signature-dark-runtime.md` returns 3 or more, and the file contains a heading for the who-default finding and a heading for the residual.
    - The residual is explicit, not implied: the file contains the words "dark" and "production" in the residual section, and states the permanence.
    - No fabricated dates: `grep -cE 'created: 2026-0[123456]' .planning/debug/voice-signature-dark-runtime.md` returns 0. The created date is 2026-07-28.
    - Git tracking behaves as documented: `git check-ignore -v .planning/debug/voice-signature-dark-runtime.md` exits 1 (not ignored) and a plain `git add` stages it.
    - `grep -c 'GLYPH-01' .planning/debug/voice-signature-dark-runtime.md` returns 1 or more AND `grep -c 'REQUIREMENTS.md' .planning/debug/voice-signature-dark-runtime.md` returns 1 or more.
  </acceptance_criteria>
  <done>Six dangling citations resolve, V-2 and V-3 are readable as open cross-referenced entries, the who-default conflict is queued for a navigator ruling, and the permanent-dark residual is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/debug/*.md` -> `/gsd:debug` and `gsd-debugger` | An RCA is a machine-readable incident spec consumed by an agent. A false claim inside it becomes an agent's premise. |
| RCA prose -> the navigator's decision | The who-default sub-finding asks for a canon ruling. A fabricated quote or attribution would corrupt a constitutional decision. |

## STRIDE Threat Register

Reused from RESEARCH.md's Security Domain analysis. This plan writes documentation and a test that greps it; it ships no executable behavior change.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-05 | Repudiation / Tampering (integrity of the record) | `.planning/debug/voice-signature-dark-runtime.md` | mitigate | A fabricated date, quote, attribution, or never-run reproduction step inside an RCA is a truth-claim violation under this repo's QA/RCA rule, and it propagates because agents consume the file as a premise. Mitigations: the mandatory Source-of-Truth Preamble naming the exact commit and workspace; a `Meta` statement that the file was authored 2026-07-28 to back citations that predate it; the RESEARCH.md-only content constraint stated in this plan; and the acceptance criterion that greps for a backdated `created:` value. |
| T-243-06 | Repudiation (silent-loss of a known defect) | The four OPEN findings | mitigate | The exact failure this phase exists to stop is a debt recorded in five different documents and therefore findable in none. Mitigation: one file, five headed finding blocks, a doc-presence gate that fails if V-1/V-2/V-3 or GLYPH-01 stop being cited, and a `status` assertion that fails if anyone marks it resolved while items remain open. |
| T-243-04 | Tampering / Repudiation | HOME-global unkeyed state (`~/.mindrian/voice-mark.json`, `~/.mindrian/stance-state.json`) | accept and document | Carried over from plan 243-01's register. This plan is where the acceptance is DOCUMENTED: V-2's finding block states that any writer without a session key would let one worktree agent's turn state be read as another's identity, which is why no writer is built in this phase. |
| T-243-02 | Spoofing | `lib/hmi/voice-color-mark.cjs` spoof fence (`NON_DESTIJL_GLYPHS`) | accept | The V-4 spoof-fence gaps are out of GLYPH-01's scope. This plan records them in the named non-goals block so the acceptance is explicit and dated rather than silent. No code change. |
| T-243-SC | Tampering | npm / pip / cargo installs | not applicable | Zero packages installed, zero dependencies added. Node built-ins only. RESEARCH.md emits no Package Legitimacy Audit for that reason and the slopcheck gate is not triggered. |

ASVS categories V2, V3, V4, V5, and V6 are not applicable to this plan: it writes a markdown file and a test that reads it. No auth surface, no sessions, no access decisions, no untrusted input parsing, no crypto.
</threat_model>

<verification>
Full gate for this plan, in order:

1. `node tests/test-243-rca-routing.cjs` exits 0.
2. `bash tests/run-all-243.sh` exits 0 with FAIL=0 (fixture suite + doc-presence test + the 192 and 210 regression legs).
3. `ls .planning/debug/*voice-signature* .planning/debug/resolved/*voice-signature* 2>/dev/null | wc -l` returns 1.
4. `git diff --quiet tests/run-all-243.sh` exits 0 (glob-discovery meant no harness edit was needed).
5. `node scripts/build-connector-registry.cjs --check` exits 0, gap=0 (no invocable surface added).
</verification>

<success_criteria>
- `.planning/debug/voice-signature-dark-runtime.md` exists, is `kind: rca`, is NOT resolved, and follows `docs/RCA-TEMPLATE.md`'s section order including the mandatory Source-of-Truth Preamble.
- V-2 and V-3 are readable in it as open, cross-referenced sub-findings with their sites named by symbol and their blockers stated.
- V-1 appears as resolved-history closed by Phase 243, including the Phase 210 item B supersession and its reason.
- The `who: 'larry'` default appears as a fourth open sub-finding, filed for a navigator ruling, with the Part 10 versus Part 12 conflict quoted from its named primary sources.
- The F5 residual appears under its own heading: the Tier-1 glyph is dark in production, permanently, by design, until a session-keyed writer lands.
- V-4 appears as a named non-goal with its reason, so the omission does not read as an oversight.
- The six citing documents are listed and all six now resolve; exactly one voice-signature RCA exists.
- Nothing in the file is fabricated. Every claim traces to `243-RESEARCH.md`.
- No em-dashes anywhere.
</success_criteria>

<output>
Create `.planning/phases/243-voice-glyph/243-02-SUMMARY.md` when done.

The SUMMARY MUST state:
- That the RCA did NOT exist before this plan and was created, with the F1 fork resolution spelled out: SC2's "no new RCA file created" is read as "no SECOND RCA beside the one the repo already names". Anyone auditing SC2 literally will otherwise see a contradiction.
- That four items remain OPEN in it: V-2, V-3, the `who: 'larry'` default, and the permanent-dark residual. The file must NOT be moved to `.planning/debug/resolved/` and gets no `knowledge-base.md` block while items remain open.
- The one thing this RCA asks a human for: which reading governs the statusline's `who` signal, Canon Part 10 ("the conversational surface IS Larry") or Canon Part 12 ("a turn with no Larry mark IS the native host speaking"). That is a navigator call and it is now queued in one findable place.
- The Dev-Research Compositing obligation: the F1 phantom-RCA finding (a forward-referenced artifact that was never authored, propagating into four downstream documents) is a durable process lesson and belongs in `~/MindrianRooms/rethinking-mindrianos/research/<dated-entry>/`, cross-linked back to this phase, not only in this phase folder.
</output>
