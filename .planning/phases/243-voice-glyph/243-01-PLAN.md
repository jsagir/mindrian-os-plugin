---
phase: 243-voice-glyph
plan: 01
type: execute
wave: 1
depends_on: []
canon_parts: [11, 12]
files_modified:
  - lib/statusline/cockpit-renderer.cjs
  - tests/test-voice-glyph-advisory.cjs
  - tests/test-192-statusline-stance-chip.cjs
  - tests/test-243-voice-glyph-honest.cjs
  - tests/run-all-243.sh
autonomous: true
requirements: [GLYPH-01]
cirs_relationship:
  surfaces_added: []
  surfaces_modified: []
  surfaces_removed: []
  spine_consumed: []
  gate_impact: "none. This plan edits one pure rendering library plus test files. It adds, modifies, and removes zero invocable surfaces (nothing under commands/, skills/, or agents/), mints no reach/edge/node, and opens no Brain wire. build-connector-registry.cjs --check, build-orchestration-projection.cjs --check, and check-render-coverage.cjs are all unaffected and must stay at their current gap=0 state."
  explanation: "Phase 243 neither uses nor is used by CIRS. lib/statusline/cockpit-renderer.cjs is a render-only transform (a plain state object in, a string out) reached by scripts/context-monitor, which is a host adapter, not an invocable surface under R1's unit-of-coverage. The R12 trigger (files under commands/|skills/|agents/, OR a non-empty spine_consumed) is NOT met, so this block is filed voluntarily for completeness, matching the 177-05-PLAN.md precedent. Per the R12 auto-derivation rule, declaring the block puts 11 in canon_parts; 12 is declared because this phase is an APPLICATION of Canon Part 12 (The Voice Signature), not an amendment to it."

must_haves:
  truths:
    - "A turn opened with any of the 5 De Stijl glyphs (blue/red/yellow/black/white) renders that exact glyph in the statusline, through all three input shapes (voice_glyph, voice_color, voice_move)."
    - "A turn opened with NO voice mark renders NO glyph, even when a stance (redteam or tell-act) is active - the honest empty state."
    - "Restoring the deleted stanceDefaultGlyph branch turns the honest-empty fixture rows RED, proven by running it, not by asserting it."
    - "The [redteam] / [tell-act] chip still renders, and a null stance still renders byte-identically to the no-stance state."
    - "Natural voice-mark detection still beats the stance preference - the surviving half of Phase 210 item B."
    - "The frozen producers (lib/hmi/voice-color-mark.cjs, lib/core/stance-state.cjs) and the out-of-scope who default (scripts/context-monitor, lib/statusline/cockpit-signals.cjs) are byte-identical after this plan."
  artifacts:
    - path: "lib/statusline/cockpit-renderer.cjs"
      provides: "Single-source voice glyph: resolveVoiceGlyph is the ONLY producer; the stance-default fabrication branch is gone"
      contains: "const voiceGlyph = isLarry ? resolveVoiceGlyph(s) : null"
    - path: "tests/test-243-voice-glyph-honest.cjs"
      provides: "5-glyph x 3-input-shape vocabulary fixtures (15 rows) + 3 honest-empty mutation-gate rows"
      min_lines: 55
    - path: "tests/run-all-243.sh"
      provides: "Glob-discovering phase aggregator with hard-fail-on-zero, plus the two mandatory 192/210 regression legs"
      min_lines: 30
    - path: "tests/test-voice-glyph-advisory.cjs"
      provides: "Leg 3 inverted from PRESERVE FLOOR to SUPERSEDED BY PHASE 243 (assertion count unchanged at 4 legs)"
    - path: "tests/test-192-statusline-stance-chip.cjs"
      provides: "Cases (b)/(c) glyph assertions inverted; the chip and natural-wins assertions untouched (assertion count unchanged at 27)"
  key_links:
    - from: "lib/statusline/cockpit-renderer.cjs"
      to: "lib/hmi/voice-color-mark.cjs"
      via: "resolveVoiceGlyph's MARK_GLYPHS / COLOR_GLYPHS allow-list lookups (read only, producer stays byte-frozen)"
      pattern: "resolveVoiceGlyph"
    - from: "tests/test-243-voice-glyph-honest.cjs"
      to: "lib/hmi/voice-color-mark.cjs"
      via: "glyphForColor / COLOR_GLYPHS / VOICE_COLOR_MARKS - never a hard-coded glyph literal"
      pattern: "voiceMark\\.(glyphForColor|COLOR_GLYPHS|VOICE_COLOR_MARKS)"
    - from: "tests/run-all-243.sh"
      to: "tests/run-all-192.sh and tests/run-all-210.sh"
      via: "mandatory regression legs inside the aggregator (RESEARCH.md Pitfall 1)"
      pattern: "run-all-(192|210)\\.sh"
    - from: "lib/statusline/cockpit-renderer.cjs"
      to: "NOTHING (negative link)"
      via: "the deleted fabrication branch must not exist anywhere in the file"
      pattern: "stanceDefaultGlyph"
---

<objective>
GLYPH-01 half 1 (Success Criterion 1): make the De Stijl voice glyph tell the truth. Delete the four-line branch in `lib/statusline/cockpit-renderer.cjs` that, when natural voice-mark detection returns null, substitutes the active stance's default De Stijl color as the glyph. Because nothing repo-wide writes `~/.mindrian/voice-mark.json`, natural detection is null on 100% of production turns, so that branch is not a rare fallback - it is the ONLY thing that ever paints a glyph on a live statusline, and it paints a Larry identity mark onto turns that carried no mark at all (audit finding V-1).

The code change is four deleted lines. The care is entirely in the three currently-GREEN assertions that pin the behavior being removed, and in proving the new gate actually bites.

Purpose: the statusline stops making an unevidenced identity claim to the navigator. Honest-empty over plausible-default, the same rule the file already applies at Ruling 3c ("--" for Next:, never the lying word "continue").
Output: a fabrication-free renderer, three inverted contract assertions, a 18-row fixture suite, a phase aggregator, and a mutation probe run for real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/243-voice-glyph/243-RESEARCH.md
@.planning/phases/243-voice-glyph/243-PATTERNS.md
@.planning/phases/243-voice-glyph/243-VALIDATION.md
</context>

## Supersession notice (state this out loud, do not let it read as a regression)

Phase 243 REVERSES the second half of Phase 210 item B, a navigator-directed softening recorded in `.planning/STATE.md:2964`: *"210-03: voice-glyph precedence flipped at the consumer (cockpit-renderer) - natural detection wins, stance color fills the default."*

- **What survives:** natural detection wins. Unchanged, and now the only rule.
- **What dies:** the stance color filling the default when detection is silent.
- **Why:** Phase 210 made that call on the assumption that "natural detection yields nothing" was an OCCASIONAL state. It is the PERMANENT state - zero writers exist for `~/.mindrian/voice-mark.json`, verified by grep (exactly two references repo-wide, both readers, plus one comment). A reasonable default over an occasional gap becomes a permanent lie over a permanent gap.
- **Direction:** this is a further SOFTENING, the same direction Phase 210 was travelling. It removes a signal the system was asserting without evidence.
- **Governance:** the three assertions encoding the old contract are INVERTED, never deleted. An inverted assertion IS the mutation gate SC1 requires; a deleted assertion proves nothing and erases the record of what changed.

## The F5 residual (MUST be carried into the SUMMARY, not left implicit)

After this plan lands, the Tier-1 voice glyph is DARK in production - on every turn, on every install, permanently - until a session-keyed writer for `~/.mindrian/voice-mark.json` exists. That is the correct honest state and it satisfies SC1. It is also, on its own, indistinguishable from the failure that created Phase 182.1: a green suite certifying a feature that does nothing.

The difference between "honest" and "quietly dead" is entirely whether it is written down. The SUMMARY for this plan MUST say plainly: *the voice glyph now reflects the actual turn IN FIXTURES; production is dark by design pending the write side.* Plan 243-02 files the same residual as a named OPEN item in the RCA. Do NOT invent a placeholder glyph to fill the gap - that recreates the defect.

## UI design contract (asked and answered, not skipped)

This phase touches VISUAL rendering (a CLI statusline glyph) but introduces NO new visual design. It removes a fabricated rendering path and restores honest display of an already-frozen 5-color De Stijl palette (`lib/hmi/voice-color-mark.cjs`, explicitly out of scope and byte-frozen). There is no new design decision for a UI-SPEC.md to contract, which is why none was required. The statusline is CLI-only; Desktop and Cowork have no statusline, so this is a deliberate CLI-scoped change, not a Tri-Polar oversight - Part 12's Voice Signature on those two surfaces rides the turn-text glyph in `agents/larry-extended.md`, which this phase does not touch.

## Artifacts this phase produces

New symbols created across the whole of Phase 243 (both plans), listed here so no downstream reviewer flags them as unexplained references:

| Artifact | Plan | Kind |
|----------|------|------|
| `tests/test-243-voice-glyph-honest.cjs` | 243-01 | NEW test (fixture + mutation gate, SC1a/b/c) |
| `tests/run-all-243.sh` | 243-01 | NEW phase aggregator (glob-discovering) |
| `.planning/debug/voice-signature-dark-runtime.md` | 243-02 | NEW RCA document (SC2) |
| `tests/test-243-rca-routing.cjs` | 243-02 | NEW test (doc-presence, SC2) |

This plan's own contributions: the fixture suite, the aggregator, the fabrication-free renderer, and the three inverted contract assertions.

## Out of scope for this plan (named, with reasons, so omission does not read as oversight)

- **`scripts/context-monitor`'s `who: 'larry'` default.** A SEPARATE defect (RESEARCH.md F4). SC1's text is entirely about glyph rendering. Flipping it would make every statusline on every install render "Claude" and suppress the Brain chip permanently, on a phase whose ROADMAP justification is "smallest blast radius". It is also doctrinally undecided between Canon Part 10 and Part 12 and needs a navigator ruling. Plan 243-02 files it in the RCA as a fourth sub-finding.
- **Any writer for `~/.mindrian/voice-mark.json`.** This is V-2's exact documented hazard: `mindrianDir()` resolves to `~/.mindrian` with no session key, so N concurrent worktree agents would share one file. Routed to the RCA, not to build work.
- **The F.7 recalibration dial / `detectedVoiceTransition` production callers.** V-3's downstream consumer, same disposition.
- **All of V-4** (spoof-fence gaps, mixed-mark gap, the `⬜` checkbox special case, the false provenance comment at `cockpit-signals.cjs:216`, the stale `agents/larry-extended.md:56` line). REQUIREMENTS.md's GLYPH-01 text names only V-1/V-2/V-3. Planner decision on `cockpit-signals.cjs:216`: LEAVE the comment unedited and record the false claim in the RCA instead. Reason: it is V-4 territory, and adding a second unrequired file diff contradicts the "smallest blast radius" justification; the RCA preserves the finding for the phase that owns the write side.
- **`lib/hmi/voice-color-mark.cjs` and `lib/core/stance-state.cjs`.** Both stay byte-identical. The defect is in the renderer CONSUMER, not either producer. Phase 210-03 made the same call deliberately.

## File-handling note (drift you will see, and should ignore)

`.planning/STATE.md` records 7 pre-existing tracked-file drifts from a concurrent statusline session, including `scripts/context-monitor`, which can make `doctor --acceptance` report 14/15 with `verify-release-clean-tree` FAILing. This plan does not touch `scripts/context-monitor` at all, so that drift is unrelated to Phase 243. Run `git status --porcelain` before starting and expect that pre-existing noise rather than mis-attributing it here. Zero file overlap with Phase 236 (room.db) or Phase 242 (The Moat), and zero logic overlap with `scripts/statusline-mos` (it resolves which `context-monitor` to exec and carries no `who` or glyph field) - all three verified in RESEARCH.md's Overlap Check, restated here rather than re-derived.

<tasks>

<task type="auto">
  <name>Task 1: Land the honest-glyph fixture suite and the phase aggregator (RED on exactly the fabrication rows)</name>
  <files>tests/test-243-voice-glyph-honest.cjs, tests/run-all-243.sh</files>
  <read_first>
    lib/hmi/voice-color-mark.cjs (FULL - the frozen 5-color classifier. Confirm the live export list before writing the loop: VOICE_COLOR_MARKS maps move to color as building/blue, challenging/red, contradiction/yellow, gate/black, invisibility/white; COLOR_GLYPHS maps color to glyph; glyphForColor and glyphForMove are functions. This file is READ ONLY and must stay byte-identical.),
    lib/statusline/cockpit-renderer.cjs (the module.exports block near the end - confirm renderCockpit and resolveVoiceGlyph are exported - AND the _render glyph seam. LOCATE THE SEAM BY GREPPING THE SYMBOL stanceDefaultGlyph, never by line number: RESEARCH.md Pitfall 4 records that the source audit's line numbers had already drifted by the time research ran.),
    tests/test-voice-glyph-advisory.cjs lines 1-45 (the 'use strict' plus path.join require idiom, and the leg() / pass / fail counter harness this new suite copies),
    tests/run-all-233.sh (the glob-discovery, hard-fail-on-zero, and trailing [ "$FAIL" -eq 0 ] aggregator skeleton),
    .planning/phases/243-voice-glyph/243-PATTERNS.md (the "tests/test-243-voice-glyph-honest.cjs" and "tests/run-all-243.sh" sections carry ready-to-adapt skeletons validated against the live exports)
  </read_first>
  <action>
    Create `tests/test-243-voice-glyph-honest.cjs`, a standalone CJS assertion script using `node:assert/strict` and `node:path` (no framework - this repo has none). Require `lib/statusline/cockpit-renderer.cjs` as `renderer` and `lib/hmi/voice-color-mark.cjs` as `voiceMark` through a `REPO = path.join(__dirname, '..')` join, matching `tests/test-voice-glyph-advisory.cjs`.

    Define a `base()` factory returning a fresh object with `who: 'larry'`, `room: 'Demo'`, `ctx_pct: 10`, `next_move: 'x'`. Return a NEW object each call so `Object.assign` mutation in one row cannot leak into the next.

    Block 1 - the vocabulary, 15 rows. Iterate `Object.entries(voiceMark.VOICE_COLOR_MARKS)` to get each move/color pair, resolve the glyph with `voiceMark.glyphForColor(color)`, and for each of the three input shapes `{voice_glyph: glyph}`, `{voice_color: color}`, `{voice_move: move}` assert that `renderer.renderCockpit(Object.assign(base(), shape))` includes that glyph. Name each row with the move and the shape key so a failure message identifies the exact cell. NEVER hard-code a glyph literal such as the red or blue square character - a hard-coded literal cannot detect a re-pointed palette, which is why the existing tests import the module for exactly this purpose.

    Block 2 - the honest empty state, 3 rows, and these are the mutation-gate rows. Row 2a: `base()` with no voice field and no stance - assert NONE of `Object.values(voiceMark.COLOR_GLYPHS)` appears in the rendered line. Rows 2b and 2c: `base()` plus `{stance: 'redteam', stance_forced_color: 'red'}` and `{stance: 'tell-act', stance_forced_color: 'blue'}` - assert NONE of the five COLOR_GLYPHS appears, AND separately assert the `[redteam]` / `[tell-act]` chip DOES still appear (the chip is a different segment and is not the defect). Include the offending rendered line in every failure message.

    Print a trailing pass/fail count and `process.exit(fail > 0 ? 1 : 0)`, mirroring `test-voice-glyph-advisory.cjs`. Hyphens only, no em-dashes anywhere in the file.

    Create `tests/run-all-243.sh` from the `tests/run-all-233.sh` skeleton: `#!/usr/bin/env bash`, `set -uo pipefail`, resolve ROOT from BASH_SOURCE and cd there, a `run()` helper that echoes a label and increments PASS or FAIL, `shopt -s nullglob` glob-discovery over `tests/test-243-*.cjs` (run with `node`) and `tests/test-243-*.sh` (run with `bash`) setting a `found` flag, a hard `exit 1` when `found` is 0, then TWO mandatory regression legs `bash tests/run-all-192.sh` and `bash tests/run-all-210.sh` (RESEARCH.md Pitfall 1: these carry the three assertions Phase 243 inverts, and they live in files named for OTHER phases so a 243-scoped grep will never find them), a `Phase 243: PASS=$PASS FAIL=$FAIL` summary, and a final `[ "$FAIL" -eq 0 ]`. Glob-discovery is load-bearing: plan 243-02 adds `tests/test-243-rca-routing.cjs` and must not have to edit this harness. Skip the Part 8 egress sweep leg - it is nearly vacuous for pure local rendering with zero network, and a self-testing sweep is the only honest form; note the skip in a header comment rather than shipping a gate that cannot bite.

    At the end of this task the suite is RED. That is correct and intended: the renderer still fabricates. Do not touch the renderer in this task.
  </action>
  <verify>
    <automated>node --check tests/test-243-voice-glyph-honest.cjs && bash -n tests/run-all-243.sh && ! node tests/test-243-voice-glyph-honest.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check tests/test-243-voice-glyph-honest.cjs` exits 0 and `bash -n tests/run-all-243.sh` exits 0.
    - `node tests/test-243-voice-glyph-honest.cjs` exits NON-ZERO, and its failure output names ONLY the honest-empty rows. All 15 vocabulary rows pass BEFORE the renderer is touched - this proves the read path was never the defect (RESEARCH.md Pitfall 5: do not spend effort "fixing" resolveVoiceGlyph).
    - Verified live at planning time against this exact working tree: the two failing lines are `⬡ 🟥 · [redteam] · 📂 Demo ✅ · Next: x · 🟢` and `⬡ 🟦 · [tell-act] · 📂 Demo ✅ · Next: x · 🟢`, each carrying exactly one fabricated glyph. If a THIRD row fails, or a vocabulary row fails, stop and re-read the exports - the fixture is wrong, not the renderer.
    - No hard-coded glyph literals: `grep -vE '^\s*//' tests/test-243-voice-glyph-honest.cjs | grep -cF '🟥'` returns 0, and `grep -c 'glyphForColor\|COLOR_GLYPHS\|VOICE_COLOR_MARKS' tests/test-243-voice-glyph-honest.cjs` returns 3 or more.
    - `bash tests/run-all-243.sh` prints the discovered filename `test-243-voice-glyph-honest.cjs` and exits NON-ZERO (the fixture rows are still red). It must never exit 0 with zero tests discovered.
    - `grep -c 'run-all-192.sh' tests/run-all-243.sh` returns 1 or more AND `grep -c 'run-all-210.sh' tests/run-all-243.sh` returns 1 or more.
    - `grep -vE '^\s*(#|//)' tests/run-all-243.sh tests/test-243-voice-glyph-honest.cjs | grep -cF "$(printf '\u2014')"` returns 0 (no em-dashes on executable lines; comments are stripped first so header prose cannot self-trip the check).
  </acceptance_criteria>
  <done>The SC1 gate exists, is discovered by the aggregator, and is RED for exactly the two fabricated-glyph rows and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the fabrication branch and invert the three superseded assertions (one atomic change)</name>
  <files>lib/statusline/cockpit-renderer.cjs, tests/test-voice-glyph-advisory.cjs, tests/test-192-statusline-stance-chip.cjs</files>
  <read_first>
    lib/statusline/cockpit-renderer.cjs (the _render glyph seam IN ITS CURRENT LIVE STATE. Grep for `stanceDefaultGlyph` to find it. Read the whole comment block above it - the one that opens "Tier-1 voice glyph. SEED-042 (192-04), precedence softened by Phase 210 item B" - because the comment is being replaced, not just the code. Also read resolveVoiceGlyph and the `if (voiceGlyph) identity += ' ' + voiceGlyph;` consumer so you can confirm a falsy glyph already renders nothing and no new "unknown" placeholder needs inventing.),
    tests/test-voice-glyph-advisory.cjs (FULL, 120 lines. Confirm the EXACT current text of leg 3 and of the file header comment block before editing either - RESEARCH.md's own Pitfall 4 warns that quoted locations drift, so locate by the quoted string, not by line number. Legs 1, 2, and 4 stay untouched.),
    tests/test-192-statusline-stance-chip.cjs (the header comment block items (b) and (c), and the two test bodies. Confirm the exact current assertion text before editing. Cases (a), (d), (e), (f), (g) stay untouched.),
    .planning/phases/243-voice-glyph/243-PATTERNS.md (the before/after excerpts for all three files, written against the live tree),
    .planning/phases/243-voice-glyph/243-RESEARCH.md Finding F3 (the governance argument and the baseline test counts)
  </read_first>
  <action>
    All three edits land in THIS ONE TASK. Splitting them turns `tests/run-all-210.sh` and `tests/run-all-192.sh` red and makes an intended supersession look like a regression (RESEARCH.md Pitfall 1). The warning sign of a bad split is a change set containing `cockpit-renderer.cjs` but not `test-192-statusline-stance-chip.cjs`.

    Edit 1, `lib/statusline/cockpit-renderer.cjs`. Delete the four-line branch that opens `if (isLarry && !voiceGlyph && voiceMark && typeof s.stance_forced_color === 'string' && s.stance_forced_color)`, including its `stanceDefaultGlyph` const and the inner `if (stanceDefaultGlyph) voiceGlyph = stanceDefaultGlyph;`. Change the surviving line from `let voiceGlyph = ...` to `const voiceGlyph = isLarry ? resolveVoiceGlyph(s) : null;` - it is no longer reassigned. Replace the entire comment block above it (the SEED-042 / Phase 210 item B precedence paragraph) with a new block stating: Phase 243 (GLYPH-01) supersedes the second half of Phase 210 item B; natural voice-mark detection is the ONLY source of the glyph; the stance color no longer fills the default when detection is silent, because "silent" is not an occasional state - no writer for `~/.mindrian/voice-mark.json` exists, so detection is silent on every production turn and the stance default was painting a Larry glyph over turns that carried no mark at all (audit finding V-1); honest-empty over plausible-default, the same rule as Ruling 3c's "--" for Next:; the stance still shows as its own `[stance]` chip and only the fabricated glyph is gone; the stance-to-color mapping in `lib/core/stance-state.cjs` is UNCHANGED capability. Touch nothing else in the function - not `isLarry`, not `brainOn`, not `nextMove`, not `hostLabel`, not `resolveVoiceGlyph`, not the `[stance]` chip segment.

    Edit 2, `tests/test-voice-glyph-advisory.cjs` leg 3. INVERT, do not delete. Change the leg label from "leg 3 PRESERVE FLOOR: with no natural signal the stance color stays the default glyph" to "leg 3 SUPERSEDED BY PHASE 243: with no natural signal the stance color must NOT render a glyph". Change the assertion's expected value from `true` to `false` so it reads `assert.equal(line.indexOf(RED_GLYPH) !== -1, false, ...)`, and change the message from "the stance default color renders when natural detection yields nothing" to a GLYPH-01 message stating the stance default no longer fabricates a glyph when natural detection is silent. Update the file's header comment block, where leg 3 is currently described as "(PRESERVE FLOOR, green now and after): with NO natural voice signal the stance color still applies as the default glyph (the capability survives)" - it must now say leg 3 is SUPERSEDED by Phase 243. The header doc and the assertion must not contradict each other. Leave legs 1, 2, and 4 exactly as they are.

    Edit 3, `tests/test-192-statusline-stance-chip.cjs` cases (b) and (c). INVERT ONLY THE GLYPH ASSERTION IN EACH. In case (b), flip `assert(line.indexOf(RED_SQUARE) !== -1, 'redteam render must carry the default red square when natural detection is silent')` to `=== -1` with a GLYPH-01 message stating the render must NOT carry a fabricated red square. In case (c), apply the symmetric flip to `BLUE_SQUARE`. In BOTH cases leave the `[redteam]` / `[tell-act]` chip assertion untouched and leave the trailing natural-yellow-wins assertion untouched - that is the surviving half of Phase 210 item B and it must stay green. Rename the two test titles from "chip + DEFAULT red/blue glyph when natural detection is silent (Phase 210 re-point)" to reflect that no glyph is fabricated and that Phase 243 supersedes the 210 re-point. Update the file's header comment items (b) and (c) the same way. Cases (a), (d), (e), (f), and (g) stay untouched.

    Hyphens only, no em-dashes, in every comment and message you write. Case (g) of the 192 suite already asserts no em-dash in any rendered stance line and will catch a violation in the renderer.
  </action>
  <verify>
    <automated>node --check lib/statusline/cockpit-renderer.cjs && node tests/test-243-voice-glyph-honest.cjs && node tests/test-voice-glyph-advisory.cjs && node tests/test-192-statusline-stance-chip.cjs && bash tests/run-all-192.sh && bash tests/run-all-210.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c stanceDefaultGlyph lib/statusline/cockpit-renderer.cjs` returns 0.
    - `grep -c 'let voiceGlyph' lib/statusline/cockpit-renderer.cjs` returns 0, and `grep -cF 'const voiceGlyph = isLarry ? resolveVoiceGlyph(s) : null' lib/statusline/cockpit-renderer.cjs` returns 1.
    - Live before/after proof, run verbatim (this is RESEARCH.md's own reproduction command): `node -e "const r=require('./lib/statusline/cockpit-renderer.cjs'); console.log(r.renderCockpit({room:'Demo',next_move:'x',stance:'redteam',stance_forced_color:'red'}));"` printed `⬡ 🟥 · [redteam] · 📂 Demo ✅ · Next: x` before this task (confirmed live at planning time) and MUST print `⬡ · [redteam] · 📂 Demo ✅ · Next: x` after it. The load-bearing change is the absence of the red square; the `[redteam]` chip must still be there.
    - `node tests/test-243-voice-glyph-honest.cjs` exits 0 with all 18 rows green (15 vocabulary + 3 honest-empty).
    - `bash tests/run-all-192.sh` exits 0 AND `bash tests/run-all-210.sh` exits 0.
    - Assertion counts UNCHANGED (inverted, not deleted), matching the RESEARCH.md F3 baselines exactly: `node tests/test-voice-glyph-advisory.cjs` still prints "4 passed, 0 failed" and `node tests/test-192-statusline-stance-chip.cjs` still prints "PASSED: 27" with "FAILED: 0".
    - The frozen producers and the out-of-scope surfaces are byte-identical: `git diff --stat lib/core/stance-state.cjs lib/hmi/voice-color-mark.cjs lib/statusline/cockpit-signals.cjs scripts/context-monitor` produces EMPTY output.
    - `grep -vE '^\s*(//|\*)' lib/statusline/cockpit-renderer.cjs | grep -cF "$(printf '\u2014')"` returns 0, and the same comment-stripped check returns 0 for both edited test files.
    - The new renderer comment block names Phase 243, GLYPH-01, Phase 210 item B, and Ruling 3c: `grep -c 'Phase 243\|GLYPH-01' lib/statusline/cockpit-renderer.cjs` returns 2 or more.
  </acceptance_criteria>
  <done>The renderer has exactly one glyph source (resolveVoiceGlyph), the three superseded assertions are inverted with their counts intact, and both sibling aggregators are green.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the mutation gate bites, then prove nothing else moved</name>
  <files>lib/statusline/cockpit-renderer.cjs (mutated and restored within this task; net diff must be zero)</files>
  <read_first>
    lib/statusline/cockpit-renderer.cjs (its post-Task-2 state, so you know exactly what you are restoring to),
    tests/test-243-voice-glyph-honest.cjs (which rows are the mutation-gate rows - blocks 2a/2b/2c - so you can confirm the mutation turns exactly those red and leaves the 15 vocabulary rows green),
    .planning/phases/243-voice-glyph/243-RESEARCH.md Pattern 2 (the milestone's mutation-proven-gate standard: prove it by running, never by reasoning)
  </read_first>
  <action>
    SC1's text is "a mutation restoring the fabricated default turns the gate red". That is a claim that must be executed, not asserted. Run it as a real, reversible sequence.

    Step 1, snapshot. Copy `lib/statusline/cockpit-renderer.cjs` to a scratch path outside the repo (use the session scratchpad directory, not /tmp directly). Restoring by file copy rather than by `git checkout --` is deliberate: `git checkout` would silently destroy Task 2's work if it has not been committed yet.

    Step 2, mutate. Re-insert the deleted four-line branch verbatim above the `const hostLabel` line: the `if (isLarry && !voiceGlyph && voiceMark && typeof s.stance_forced_color === 'string' && s.stance_forced_color)` guard, the `const stanceDefaultGlyph = voiceMark.glyphForColor(s.stance_forced_color);` lookup, and the `if (stanceDefaultGlyph) voiceGlyph = stanceDefaultGlyph;` assignment. Change `const voiceGlyph` back to `let voiceGlyph` so the reassignment is legal.

    Step 3, observe. Run `node tests/test-243-voice-glyph-honest.cjs` and capture the output. Record in the SUMMARY: the exit code, the pass/fail counts, and WHICH row names failed.

    Step 4, restore. Copy the snapshot back over the file.

    Step 5, confirm the restore is exact and complete.

    Then run the phase-gate roll-up: the full aggregator (which now also runs the 192 and 210 regression legs) plus the connector-registry check, because this phase adds no invocable surface and the born-wired ledger must be provably unchanged - a diff there means something unintended was added.
  </action>
  <verify>
    <automated>node tests/test-243-voice-glyph-honest.cjs && bash tests/run-all-243.sh && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - MUTATED state (step 3): `node tests/test-243-voice-glyph-honest.cjs` exits NON-ZERO, and the failing rows are EXACTLY the honest-empty rows 2b (redteam) and 2c (tell-act). All 15 vocabulary rows and row 2a (no stance at all) stay green under the mutation - the fabrication only fires when `stance_forced_color` is set, so row 2a going red would mean the fixture is testing the wrong thing.
    - RESTORED state (step 5): `grep -c stanceDefaultGlyph lib/statusline/cockpit-renderer.cjs` returns 0 AND `grep -c 'let voiceGlyph' lib/statusline/cockpit-renderer.cjs` returns 0 AND `node tests/test-243-voice-glyph-honest.cjs` exits 0. The restore left no residue.
    - Net-zero mutation diff: after restore, `git diff --stat lib/statusline/cockpit-renderer.cjs` shows only Task 2's intended change (or empty output if Task 2 was already committed). The mutation contributed nothing.
    - `bash tests/run-all-243.sh` exits 0 with PASS >= 3 and FAIL=0 (the fixture suite plus both regression legs).
    - `node scripts/build-connector-registry.cjs --check` exits 0 with gap=0. This phase adds no invocable surface, so the ledger must be unchanged.
    - No unexpected working-tree movement: `git status --porcelain` lists no file outside this plan's `files_modified` set, apart from the pre-existing concurrent-session drift in `scripts/context-monitor` and its siblings noted in the File-handling section above.
    - The mutation run is recorded in the SUMMARY as evidence (exit code + failing row names), not as a claim that it was done.
  </acceptance_criteria>
  <done>The gate is proven to bite by execution, the tree is back to the intended post-fix state, and the full phase-01 gate plus the CIRS ledger are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code host stdin -> `scripts/context-monitor` | Untrusted-shaped JSON payload (model, workspace, context_window, optional agent) enters the statusline pipeline. |
| `~/.mindrian/*.json` (HOME-global) -> `cockpit-signals.readVoiceGlyph` / `readStanceState` | On-disk state files, unkeyed by session, read by every concurrent agent on the machine. |
| `cockpit-renderer._render(state)` -> the rendered line the navigator reads | The point where the system makes an identity claim ("this turn is Larry, and this is the pedagogical move") to a human who will act on it. |

## STRIDE Threat Register

Reused from RESEARCH.md's Security Domain analysis rather than re-derived. `security_enforcement` is unset in `.planning/config.json` and therefore treated as enabled; ASVS L1 applies.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-01 | Spoofing (self-inflicted) | `lib/statusline/cockpit-renderer.cjs` `_render`, the `stanceDefaultGlyph` branch | mitigate | THIS IS V-1 AND THIS PLAN IS THE MITIGATION. The statusline currently asserts a Larry identity mark with zero supporting evidence on 100% of production turns. Task 2 deletes the branch; Task 1 gates it with the honest-empty fixture rows; Task 3 proves the gate bites by executing the mutation. |
| T-243-02 | Spoofing | `lib/hmi/voice-color-mark.cjs` `detectVoiceMark` (spoof fence) | accept | A non-Larry turn opening with a De Stijl glyph to impersonate Larry is partially fenced by `NON_DESTIJL_GLYPHS` in the frozen classifier. The remaining gaps are V-4, which REQUIREMENTS.md's GLYPH-01 text does not name. This plan does not widen the surface - it removes an input path and adds none. Filed as a named non-goal in the 243-02 RCA. |
| T-243-03 | Tampering | Untrusted JSON at `~/.mindrian/voice-mark.json` -> `cockpit-signals.readVoiceGlyph` | transfer (already-mitigated existing control) | ASVS V5 input validation is already satisfied and is UNWEAKENED by this change: `readVoiceGlyph` wraps `JSON.parse` in try/catch and type-checks every field, and `resolveVoiceGlyph` validates every value against the frozen `MARK_GLYPHS` / `COLOR_GLYPHS` allow-lists via hasOwnProperty before use. Deleting the fabrication branch REMOVES a value-producing path; it introduces none. No new control needed. |
| T-243-04 | Tampering / Repudiation | HOME-global unkeyed state (`~/.mindrian/voice-mark.json`, `~/.mindrian/stance-state.json`); `mindrianDir()` resolves to `~/.mindrian` with no session or room key | accept and document | N concurrent worktree agents share one file, so any writer would let agent A's turn state be read as agent B's identity. This is precisely why NO writer is built in this phase. Routed to the 243-02 RCA as open sub-finding V-2, citing `docs/STATUSLINE-CONTRACT.md:219` open item 1 and `.planning/STATE.md:829`. |
| T-243-SC | Tampering | npm / pip / cargo installs | not applicable | This phase installs zero external packages and adds zero dependencies (Node built-ins plus existing repo modules only, per the CJS-only convention). RESEARCH.md emits no Package Legitimacy Audit for that reason, the slopcheck gate is not triggered, and no legitimacy checkpoint task is warranted. If any task in execution finds itself reaching for a package manager, that is a scope breach - stop and escalate. |

ASVS categories V2 (authentication), V3 (session management), V4 (access control), and V6 (cryptography) are not applicable: no auth surface, no sessions, no access decisions, no crypto. The absence of session keying in the HOME-global files is tracked as T-243-04, a correctness and multi-tenancy concern, not an ASVS session-management control.
</threat_model>

<verification>
Full gate for this plan, in order:

1. `node tests/test-243-voice-glyph-honest.cjs` exits 0 (18 rows: 15 vocabulary + 3 honest-empty).
2. `bash tests/run-all-192.sh` exits 0 (27 passed / 0 failed).
3. `bash tests/run-all-210.sh` exits 0 (`test-voice-glyph-advisory.cjs`: 4 passed / 0 failed).
4. `bash tests/run-all-243.sh` exits 0 with FAIL=0.
5. `node scripts/build-connector-registry.cjs --check` exits 0, gap=0.
6. The mutation probe was RUN and its output recorded in the SUMMARY.
7. `git diff --stat lib/core/stance-state.cjs lib/hmi/voice-color-mark.cjs lib/statusline/cockpit-signals.cjs scripts/context-monitor` is empty.
</verification>

<success_criteria>
- `lib/statusline/cockpit-renderer.cjs` contains zero occurrences of `stanceDefaultGlyph` and exactly one glyph source, `resolveVoiceGlyph`.
- All 5 De Stijl glyphs render through all 3 input shapes; a turn with no mark renders no glyph, with or without an active stance.
- The mutation restoring the fabricated default was executed and turned exactly the honest-empty stance rows red; the run is recorded as evidence in the SUMMARY.
- The three superseded assertions are INVERTED with their counts unchanged, and both sibling aggregators (192, 210) are green.
- The SUMMARY states BOTH governance facts explicitly: (a) Phase 243 supersedes the second half of Phase 210 item B, with the reason (silent is permanent, not occasional); (b) the F5 residual - the voice glyph now reflects the actual turn IN FIXTURES, and production is DARK by design, permanently, pending a session-keyed write side.
- No em-dashes anywhere in the changed files.
- Zero dependencies added; zero invocable surfaces added, modified, or removed; the CIRS ledger unchanged.
</success_criteria>

<output>
Create `.planning/phases/243-voice-glyph/243-01-SUMMARY.md` when done.

The SUMMARY MUST carry, as named sections and not as passing asides:
- The Phase 210 item B supersession, with its reason.
- The F5 residual: production glyph is dark, forever, until a session-keyed writer lands. Never write "the voice glyph now reflects the actual turn" without the qualifier "in fixtures; production is dark pending the writer".
- The mutation-probe run: the command, the exit code, and which rows went red.
- A note that a navigator with a stance set today WILL see their statusline glyph disappear after this ships. That is the intended honest state, not a regression. Consider a one-line CHANGELOG entry saying the glyph now appears only when a real mark is detected.
- The release-lockstep reminder: per the standing rule, this fix is NOT live for any running session after commit, and not after a release either until that session picks it up. v1.16.0 work ships as `v1.16.0-beta.N` and no v1.16.0 cut happens before ROADMAP Gate 0 (the stable v1.15.0 close-out).
</output>
