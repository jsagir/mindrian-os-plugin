---
phase: 95.6
plan: 05
type: execute
wave: 1
depends_on: ["95.6-02"]
files_modified:
  - install.sh
  - scripts/doctor.cjs
  - scripts/check-onboard-statusline.cjs  # extend to auto-run /mos:doctor on a fresh first session (D-09 self-verify)
autonomous: true
tier: 1
requirements_addressed: [D-09]
canon_parts: [Part 6, Part 7]
must_haves:
  truths:
    - "install.sh writes the canonical statusLine block into ~/.claude/settings.json via an idempotent `register_statusline()` that runs FIRST in install order (after node+git preflight, before any cp loops), so it survives even if a later step fails"
    - "install.sh writes ~/.claude/plugins/mindrian-os/.install-receipt.json listing every step that ran with its exit code; a halted install produces a receipt missing the tail steps"
    - "/mos:doctor detects a missing or stale statusLine block as a drift class (class H -- 'install incomplete'); `/mos:doctor --statusline-visibility` returns ok on a complete install and missing (offering --fix) when the block is absent; `/mos:doctor --fix` writes the canonical block idempotently"
    - "On a fresh first session after install, the SessionStart path runs `/mos:doctor` automatically and reports all-green OR names exactly what is missing and how to finish -- there is no silent install-incomplete state"
  artifacts:
    - path: "install.sh"
      provides: "`register_statusline()` function called first in install order; `.install-receipt.json` written incrementally with per-step exit codes"
      contains: "register_statusline"
    - path: "scripts/doctor.cjs"
      provides: "Class H detection (install-incomplete: missing statusLine block and/or .install-receipt.json shows a halted install) + `--fix` writes the canonical statusLine block idempotently; class H subsumes the missing-statusLine case and is activated by `--statusline-visibility` and `--all`"
      contains: "class H"
    - path: "scripts/check-onboard-statusline.cjs"
      provides: "On a fresh first session (no touch-file), additionally spawns `node scripts/doctor.cjs --all --json` and surfaces a one-line all-green-or-here-is-what-is-missing summary via additionalContext (defensive: never blocks the hook chain)"
      contains: "doctor"
  key_links:
    - from: "install.sh"
      to: "~/.claude/settings.json (.statusLine)"
      via: "register_statusline() runs before any cp loop, writes the block idempotently"
      pattern: "register_statusline"
    - from: "scripts/doctor.cjs"
      to: "~/.claude/plugins/mindrian-os/.install-receipt.json"
      via: "class H reads the receipt and names missing tail steps"
      pattern: ".install-receipt.json"
    - from: "scripts/check-onboard-statusline.cjs"
      to: "scripts/doctor.cjs"
      via: "spawnSync node scripts/doctor.cjs --all --json on a fresh first session"
      pattern: "doctor.cjs"
---

<objective>
D-09: Fix the "silent install-incomplete" failure mode -- two confirmed sites (the Wave-2 tester's machine after 2026-05-08; Jonathan's "other machine") where install completed enough to have commands and Larry but the bottom-of-terminal statusline (`(hexagon) MindrianOS-Plugin <room> <context%>`) never renders, because install.sh wrote the `statusLine` block AFTER the skill-loop that halted (D-03 bug), and the manual recovery never re-stamped it. Fix in four sequenced parts: (1) make statusline registration idempotent and FIRST in install order via `register_statusline()`; (2) have install.sh write an `.install-receipt.json` so a halted install is detectable; (3) extend `/mos:doctor` with a "class H" (install-incomplete) that detects a missing statusLine block + reads the receipt + offers `--fix` to write the block idempotently; (4) have the SessionStart first-session path auto-run `/mos:doctor` and surface all-green-or-here-is-what-is-missing -- so there is no silent gap.

Purpose: 95.6-VALIDATION.md ranks D-09 HIGH coverage need ("the 'silent install-incomplete' fix, a failure mode with no error message, observed on two separate machines"). It is the SEED-008 "make the protective layer self-verify" pattern at the install boundary. The class-H suite + receipt + auto-run together close the loop: install either reports it succeeded, or it tells you what is missing.

Output: a reordered + receipt-writing `install.sh`; a class-H-extended `scripts/doctor.cjs`; an auto-doctor-running `scripts/check-onboard-statusline.cjs`. Turns `tests/test-doctor-class-h.cjs`, `tests/test-doctor-class-h-fix.cjs`, `tests/test-install-receipt.cjs` (all created RED in Wave 0) GREEN, and turns the `.statusLine`-present assertion in `tests/test-install-sh-skill-loop.sh` GREEN.

Per Canon Part 7 (reuse before build): the class-H detection extends the existing `scripts/doctor.cjs` class taxonomy (classes A-G already exist; lines 900-1110 are the class-G statusLine logic this builds on -- the `scripts/statusline-mos` wrapper, the `statusLine.command` shape, the `--statusline-visibility` flag all already exist). The `--fix` block-write mirrors the SessionStart hook-registration idempotent-write pattern from Phase 95.2. `register_statusline()` in install.sh writes the same `{ "type": "command", "command": "bash \"$INSTALL_DIR/scripts/statusline-mos\"" }` shape `doctor.cjs` already expects. `check-onboard-statusline.cjs` already spawns logic (Phase 106-05); this adds the `doctor.cjs --json` spawn alongside. Per Canon Part 6 (dog-fooding): the maintainer's own machine has this exact gap; the recovery procedure must restamp the statusline -- this plan makes that automatic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-RESEARCH.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-VALIDATION.md
@.planning/seeds/SEED-008-intelligence-layer-activation-gap-close-the-loop.md
@install.sh
@scripts/doctor.cjs

<reference_data>
Current `install.sh` settings.json write (Step 7, lines 124-170) -- this is where the statusLine block needs to MOVE FROM (or rather: a statusLine block is NOT currently written here at all; D-09 says "Verify install.sh actually writes the statusLine block. If not, add it"). The current Step 7 node block writes `.agent`, `.hooks.SessionStart`, `.env.MINDRIAN_OS_ROOT` -- but NO `.statusLine`. So D-09 part (1) is: ADD a `register_statusline()` that writes `.statusLine`, and call it FIRST (after node+git preflight, before Step 4's command-symlink loop). The reference shape (from `scripts/doctor.cjs` class-G logic, lines ~1003-1085, which checks for `statusLine.command` resolving to a real file and to `scripts/statusline-mos`):
```json
"statusLine": { "type": "command", "command": "bash \"$INSTALL_DIR/scripts/statusline-mos\"" }
```
(`$INSTALL_DIR` expands to `${HOME}/.claude/plugins/mindrian-os` at install time.) The session-start script (`scripts/session-start` lines ~1087-1103) ALSO rewrites `statusLine` under some conditions -- read those lines to make sure `register_statusline()` writes a block that session-start will not immediately clobber or vice versa; align on the same wrapper path `scripts/statusline-mos`.

Existing `scripts/doctor.cjs` structure: flags parsed at lines 60-115; class flags `--cascade-rooms` (B+C), `--verify-surface` (D), `--room-md` (E), `--ui-compliance` (F), `--statusline-visibility` (G); `--all` activates all; `--fix` attempts recovery for classes that support it; `--json` machine-readable. Class G (lines ~900-1110) does THREE checks: (1) stale user-settings statusLine path, (2) plugin's own settings.json statusLine resolves, (3) statusline-mos isolated execution produces the brand prefix. Class H is NEW and orthogonal: "is the install structurally complete?" -- specifically (a) does `~/.claude/settings.json` HAVE a `.statusLine` block at all (G assumes one exists and checks it resolves; H checks it EXISTS); (b) does `~/.claude/plugins/mindrian-os/.install-receipt.json` exist and show all steps ran with exit 0; if a halted receipt, name the missing tail steps. Class H is activated by `--statusline-visibility` (so the existing first-session check `/mos:doctor --statusline-visibility` surfaces it) AND by `--all`. Class H's `--fix`: write the canonical `.statusLine` block into `~/.claude/settings.json` idempotently (no-op if already present and well-formed); it does NOT re-run the whole install -- it just restamps the statusline (the missing-tail-steps case is reported, not auto-fixed, because re-running install.sh is the user's call). Pick a status-code convention consistent with class G: `ok` / `warn` (recoverable, offers `--fix`) / `error` / `skip` (on Desktop, mirror class G's `CLAUDE_DESKTOP=1` carve-out).

Hermetic test target shape (from `tests/test-doctor-class-h.cjs`, created Wave 0): scratch HOME / `MINDRIAN_PLUGIN_HOME` tree; `settings.json` with vs without `.statusLine`; `.install-receipt.json` complete vs halted; assert `node scripts/doctor.cjs --statusline-visibility --json` returns `report.checks.classH` (or whatever key name -- match what the Wave 0 test expects; if the test expects `checks.classH`, use that) with `status` `ok` / `warn`; assert `--fix` writes the block + is idempotent; assert `--all` activates the key.

`.install-receipt.json` shape (canonical, from 95.6-02 Task 2's `tests/test-install-receipt.cjs` header):
```json
{
  "version": "<plugin.json version>",
  "completed_at": "<ISO timestamp or null if halted>",
  "steps": [
    { "name": "preflight", "exit": 0 },
    { "name": "longpaths", "exit": 0 },
    { "name": "clone", "exit": 0 },
    { "name": "npm_install", "exit": 0 },
    { "name": "register_statusline", "exit": 0 },
    { "name": "register_commands", "exit": 0 },
    { "name": "register_skills", "exit": 0 },
    { "name": "register_agents", "exit": 0 },
    { "name": "configure_hooks", "exit": 0 }
  ]
}
```
install.sh writes this incrementally: start with an empty `steps: []` and `completed_at: null`, append `{name, exit}` after each step, and set `completed_at` at the very end. If install.sh halts mid-loop (which it should not anymore post-95.6-03, but defense in depth), the receipt on disk is the partial list -- which is exactly what class H reads. (Note: post-95.6-03 the skill-loop no longer halts, but the receipt is still valuable: it makes "did the install actually finish" inspectable, and it is the artifact the cold-machine manual gate captures.) Writing JSON incrementally from bash: use a `node -e` one-liner per step to read-modify-write the receipt (the same idiom Step 7 already uses for settings.json), or accumulate in a bash array and write once at each checkpoint -- the read-modify-write per step is more robust against a halt.

Coordination with 95.6-03 (Step 5 skill-loop) and 95.6-04 (Step 1/Step 2 preflight): all three modify `install.sh`, same wave. THIS plan owns: the new `register_statusline()` function, the call-order reshuffle (statusline FIRST, after preflight, before the command-symlink loop), and the `.install-receipt.json` write. Do NOT re-do 95.6-03's skill-loop pre-filter or 95.6-04's longpath preflight -- but DO add the per-step receipt-append calls around all steps (including the ones those plans touched). If 95.6-03 / 95.6-04 have not landed yet, that is fine -- add the receipt-append calls around the steps as they currently are; the step names (`longpaths`, `register_skills`, etc.) are stable regardless. If they HAVE landed, slot the receipt-append calls in around their changes. execute-phase sequences within-wave on the file overlap; this plan is the most invasive install.sh change, so it likely lands LAST within the wave -- the executor should expect 95.6-03 + 95.6-04's changes to already be present and build on them.

`scripts/check-onboard-statusline.cjs` (Phase 106-05): currently a SessionStart hook that fires once per fresh install (no touch-file at `~/.mindrian/onboarding/statusline-onboarded.json`) and once per upgrade, surfacing an `additionalContext` gate asking the user to confirm they see the statusline. It does NOT write the touch-file. This plan ADDS: on the fire path, also `spawnSync('node', [path.join(pluginRoot, 'scripts', 'doctor.cjs'), '--all', '--json'], { timeout: 1500 })`, parse the JSON, and append to the `additionalContext` a one-line summary: if all checks `ok` -> "MindrianOS install check: all green." ; else -> "MindrianOS install check: <N> item(s) need attention. Run /mos:doctor --fix to resolve, or /mos:doctor --all to see details." Defensive: wrap in try/catch; on ANY error (doctor missing, timeout, JSON parse fail) emit nothing extra and `{ continue: true }` -- NEVER block the hook chain (the existing 106-05 invariant). Keep the existing gate behavior; this is additive. Pure CJS, node built-ins only, zero npm deps (Phase 87 invariant). Canon Part 8: doctor.cjs is purely LOCAL (verified -- `scripts/preflight-doctor.cjs` already spawns it; zero network surface), so this adds no egress.

No emoji (skills/ui-system -- and the statusline brand glyph is the hexagon, written as `(hexagon)` in any prose, never an emoji). No em-dashes (hyphens only).
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add register_statusline() first in install.sh order + write .install-receipt.json incrementally</name>
  <files>install.sh</files>
  <behavior>
    - After `bash install.sh`, `~/.claude/settings.json` has `.statusLine.command` set to `bash "<INSTALL_DIR>/scripts/statusline-mos"`.
    - `register_statusline()` runs after the node+git preflight and BEFORE the command-symlink loop (Step 4), so even if a later step fails, the statusLine block is already written.
    - `register_statusline()` is idempotent: a second `bash install.sh` does not duplicate or corrupt the block.
    - After a clean run, `~/.claude/plugins/mindrian-os/.install-receipt.json` exists with `completed_at` non-null and all 9 step names present with `exit: 0`.
    - If install.sh is interrupted partway, the receipt on disk lists only the steps that completed (and `completed_at` is null).
  </behavior>
  <read_first>
    - install.sh (whole file; especially Step 7 settings.json node block lines 124-170, the summary block lines 172-189, and wherever 95.6-03/04 have landed their changes)
    - scripts/session-start (lines ~1087-1103 -- the existing statusLine rewrite, to align the block shape so the two do not fight)
    - scripts/doctor.cjs (lines ~1003-1085 -- the class-G statusLine.command shape this must match)
    - tests/test-install-sh-skill-loop.sh (asserts `.statusLine.command` present after a run)
    - tests/test-install-receipt.cjs (the receipt shape + the canonical 9-step list)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md (D-09 parts 1+2)
  </read_first>
  <action>
    1. Define `register_statusline()` near the other functions (after the OS-detection block if 95.6-04 landed; else near the top). Body:
       ```bash
       # Phase 95.6 D-09: write the MindrianOS statusLine block into the user's
       # ~/.claude/settings.json. Runs FIRST in install order (right after the
       # node+git preflight) so it survives even if a later step fails -- the
       # "silent install-incomplete" fix. Idempotent.
       register_statusline() {
         echo "  . Registering statusline..."
         if [ ! -f "$SETTINGS_FILE" ]; then echo '{}' > "$SETTINGS_FILE"; fi
         node -e "
       const fs = require('fs');
       const p = '$SETTINGS_FILE';
       let s = {};
       try { s = JSON.parse(fs.readFileSync(p, 'utf8')); } catch(e) {}
       const want = { type: 'command', command: 'bash \"$INSTALL_DIR/scripts/statusline-mos\"' };
       const cur = s.statusLine;
       if (!cur || typeof cur !== 'object' || cur.command !== want.command) {
         s.statusLine = want;
         fs.writeFileSync(p, JSON.stringify(s, null, 2));
         console.log('    statusLine block written');
       } else {
         console.log('    statusLine block already present');
       }
       "
       }
       ```
       Note: `SETTINGS_FILE` is currently defined inside Step 7 (`SETTINGS_FILE="${CLAUDE_DIR}/settings.json"`). Hoist that assignment up near the top (next to `CLAUDE_DIR`) so `register_statusline()` can use it. Also ensure `mkdir -p "$CLAUDE_DIR"` happens before `register_statusline()` is called (currently `mkdir -p "$COMMANDS_DIR"` etc. handle subdirs; add `mkdir -p "$CLAUDE_DIR"` early).

    2. Add a receipt helper near the top:
       ```bash
       RECEIPT_FILE="$INSTALL_DIR/.install-receipt.json"
       PLUGIN_VERSION=$(node -e "try{process.stdout.write(require('$INSTALL_DIR/.claude-plugin/plugin.json').version)}catch(e){process.stdout.write('unknown')}" 2>/dev/null || echo unknown)
       receipt_init() {
         mkdir -p "$INSTALL_DIR" 2>/dev/null || true
         node -e "require('fs').writeFileSync('$RECEIPT_FILE', JSON.stringify({version:'$PLUGIN_VERSION',completed_at:null,steps:[]},null,2))" 2>/dev/null || true
       }
       receipt_step() {  # $1 = step name, $2 = exit code
         node -e "
       const fs=require('fs'); const p='$RECEIPT_FILE';
       let r={version:'$PLUGIN_VERSION',completed_at:null,steps:[]};
       try{r=JSON.parse(fs.readFileSync(p,'utf8'))}catch(e){}
       r.steps.push({name:'$1',exit:$2});
       fs.writeFileSync(p,JSON.stringify(r,null,2));
       " 2>/dev/null || true
       }
       receipt_done() {
         node -e "
       const fs=require('fs'); const p='$RECEIPT_FILE';
       let r={version:'$PLUGIN_VERSION',completed_at:null,steps:[]};
       try{r=JSON.parse(fs.readFileSync(p,'utf8'))}catch(e){}
       r.completed_at=new Date().toISOString();
       fs.writeFileSync(p,JSON.stringify(r,null,2));
       " 2>/dev/null || true
       }
       ```
       Caveat: `receipt_init()` writes the receipt INTO `$INSTALL_DIR`, which only exists after the clone (Step 2). So call `receipt_init` right after Step 2 succeeds, and call `receipt_step preflight 0`, `receipt_step longpaths 0`, `receipt_step clone 0` retroactively right after `receipt_init` (those steps already completed by then). Then `receipt_step npm_install $?` after Step 3, `receipt_step register_statusline 0` after `register_statusline` (which now runs after Step 2 and before Step 4 -- adjust order: preflight -> clone -> receipt_init + backfill -> npm_install + receipt_step -> register_statusline + receipt_step -> register_commands + receipt_step -> register_skills + receipt_step -> register_agents + receipt_step -> configure_hooks + receipt_step -> receipt_done). The `2>/dev/null || true` on every receipt call ensures a receipt-write failure NEVER aborts the install (the receipt is diagnostic, not load-bearing).

    3. Reorder install.sh's body so the sequence is: Step 1 prerequisites (incl. 95.6-04's `enable_longpaths_on_windows` first) -> Step 2 clone (incl. 95.6-04's stale-clone cleanup) -> `receipt_init` + backfill `preflight`/`longpaths`/`clone` steps -> Step 3 npm install + `receipt_step npm_install` -> `register_statusline` + `receipt_step register_statusline` -> Step 4 register commands + `receipt_step register_commands` -> Step 5 register skills (95.6-03's hardened loop) + `receipt_step register_skills` -> Step 6 register agents + `receipt_step register_agents` -> Step 7 configure hooks (the existing node block; remove the now-redundant statusLine concern -- it never wrote one anyway) + `receipt_step configure_hooks` -> Step 8 summary -> `receipt_done`. Renumber the echo'd "Step N" labels if you like, or leave them; the order is what matters. The KEY constraint from D-09: `register_statusline` runs before any cp loop (Step 4+). Do NOT move it after Step 4.

    4. Do NOT touch `set -euo pipefail`. Do NOT undo 95.6-03's skill-loop hardening or 95.6-04's preflight -- build around them.

    Per D-09 parts (1)+(2): "(1) Verify install.sh actually writes the statusLine block. If not, add it. (2) Make statusline registration idempotent and independent ... A function `register_statusline()` that runs FIRST in the install order, after node + git preflight, before any cp loops." Plus R-01: the install receipt.
  </action>
  <verify>
    <automated>bash -n /home/jsagi/MindrianOS-Plugin/install.sh && node /home/jsagi/MindrianOS-Plugin/tests/test-install-receipt.cjs && bash /home/jsagi/MindrianOS-Plugin/tests/test-install-sh-skill-loop.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` exits 0
    - `grep -q "register_statusline" install.sh` succeeds and the function is CALLED (`grep -c "register_statusline" install.sh` returns >= 3: definition + invocation + receipt_step) and the invocation precedes the first `cp`/`ln` loop in source order (verify by reading: `register_statusline` call line number < the first `for cmd in "$INSTALL_DIR"/commands/*.md` line number)
    - `grep -q 'statusLine' install.sh && grep -q 'statusline-mos' install.sh` succeeds (writes the canonical block shape)
    - `grep -q ".install-receipt.json" install.sh && grep -q "receipt_init\|receipt_step\|receipt_done" install.sh` succeeds; every `receipt_*` call has a `2>/dev/null || true` tail (a receipt write must never abort the install): `grep -n "receipt_step\|receipt_init\|receipt_done" install.sh | head` and confirm
    - `grep -c "set -euo pipefail" install.sh` returns 1 (strict mode untouched)
    - `node tests/test-install-receipt.cjs` exits 0 (complete-install fixture -> receipt with all 9 steps + completed_at; halted-install fixture -> receipt missing tail steps; doctor class H reads it -- the doctor side lands in Task 2, so if this test asserts the doctor read AND Task 2 has not landed, run it AFTER Task 2; sequence Task 1 then Task 2 then re-run)
    - `bash tests/test-install-sh-skill-loop.sh` -- the `.statusLine.command` assertion now PASSES (along with 95.6-03's WARN-and-continue assertions, assuming 95.6-03 landed)
    - No em-dashes in the diff: `git diff install.sh | grep "^+" | grep -c $'—'` returns 0
  </acceptance_criteria>
  <done>
    `install.sh` writes the canonical statusLine block via an idempotent `register_statusline()` that runs before any cp loop, and writes `.install-receipt.json` incrementally with per-step exit codes. A halted install is now detectable on disk. `set -euo pipefail` untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add /mos:doctor class H (install-incomplete) detection + --fix statusline restamp</name>
  <files>scripts/doctor.cjs</files>
  <behavior>
    - `node scripts/doctor.cjs --statusline-visibility --json` on a clean fixture (settings.json WITH a well-formed `.statusLine`, receipt complete) -> class-H check `status: ok`.
    - On a fixture where `~/.claude/settings.json` has NO `.statusLine` key -> class-H `status: warn`, `recoverable: true`, detail names "statusLine block missing".
    - On a fixture where `.install-receipt.json` shows a halted install (missing tail steps) -> class-H reports the missing step names; `recoverable` reflects whether `--fix` can help (statusLine: yes; re-run-install: no -> report-only).
    - `node scripts/doctor.cjs --statusline-visibility --fix` on the no-statusLine fixture -> writes the canonical block; re-running `--fix` is a byte-identical no-op (idempotent); afterward `--json` reports class-H `status: ok`.
    - `--all` activates the class-H key in `report.checks`.
    - On Desktop (`CLAUDE_DESKTOP=1`) class H returns `status: skip` (mirror class G's carve-out).
  </behavior>
  <read_first>
    - scripts/doctor.cjs (whole file -- flag parsing lines 60-115; the class-G block lines ~900-1110 for the statusLine.command shape, the report.checks structure, the status conventions ok/warn/error/skip, the `--fix` dispatch, the `CLAUDE_DESKTOP` carve-out)
    - tests/test-doctor-class-h.cjs and tests/test-doctor-class-h-fix.cjs (the exact assertions + the exact report key name expected -- match it)
    - tests/test-install-receipt.cjs (the `.install-receipt.json` shape class H reads)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md (D-09 parts 3+4)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-VALIDATION.md (the D-09 behavioral contract -- "the audit calls it class H -- install incomplete -- which subsumes the missing-statusLine case")
  </read_first>
  <action>
    1. In the flag parser: nothing new to add as a flag -- class H is activated by the EXISTING `--statusline-visibility` (so the v1.12.5.1 first-session check `/mos:doctor --statusline-visibility` surfaces it) AND by `--all`. Find where class G is gated and add class H to the same gate.

    2. Add a `runClassH()` function (mirror `runClassG()`'s structure):
       - `CLAUDE_DESKTOP=1` -> return `{ status: 'skip', detail: 'class H (install-incomplete) is CLI-only' }`.
       - Read `~/.claude/settings.json` (use the existing `PLUGIN_HOME`/HOME-aware path logic doctor.cjs already has). If it has no `.statusLine` (or `.statusLine` is not an object, or `.statusLine.command` is empty) -> this is the "install incomplete: statusLine never registered" case -> `status: 'warn'`, `recoverable: true`, `detail: 'statusLine block missing from ~/.claude/settings.json (install.sh halted before register_statusline, or a manual recovery skipped it)'`, `evidence: [...]`.
       - Read `~/.claude/plugins/mindrian-os/.install-receipt.json` (the canonical path; use `INSTALL_DIR` doctor.cjs already defines). If absent -> `status: 'warn'`, `detail: 'no .install-receipt.json -- cannot confirm the install completed; re-run install.sh'`, `recoverable: false` (report-only -- doctor does not re-run install). If present and `completed_at` is null OR `steps[]` is missing any of the canonical 9 names -> `status: 'warn'`, `detail: 'install halted -- missing steps: ' + missing.join(', ')`, list the missing steps in `evidence`, `recoverable: <true only if the ONLY missing step is register_statusline; else false>`. If present + complete + statusLine present -> `status: 'ok'`.
       - Return the standard report-check object shape doctor.cjs uses (match class G's keys: `status`, `recoverable`, `detail`, `evidence`, plus an `action` string describing what `--fix` does, like class G's `action: 'removes stale user-settings.json statusLine override ...'` -> here `action: 'writes the canonical MindrianOS statusLine block into ~/.claude/settings.json'`).

    3. `--fix` for class H: when class H is `warn` + `recoverable: true` (statusLine missing or only-register_statusline-missing), write the canonical block:
       ```js
       const want = { type: 'command', command: 'bash "' + path.join(INSTALL_DIR, 'scripts', 'statusline-mos') + '"' };
       ```
       Read settings.json, set `.statusLine = want` only if not already byte-equal, write back with `JSON.stringify(s, null, 2)`. Idempotent: a second `--fix` reads, finds `.statusLine.command === want.command`, writes nothing (or writes the identical bytes -- either way byte-stable). Wire this into doctor.cjs's existing `--fix` dispatch so it runs when class H needs it, and reflect it in the exit code (class G uses exit 2 for "drift recovered"; reuse that convention).

    4. Register the class-H check key in `report.checks` (whatever the existing key naming is -- `report.checks.classG` or `report.checks['statusline-visibility']` -- add `report.checks.classH` or the parallel name; MATCH what `tests/test-doctor-class-h.cjs` expects -- read that test first and use its key name).

    5. Update the `--help` / usage text: add a line under the class flags describing what `--statusline-visibility` now also covers (class H install-incomplete), and under `--fix` note "class H: re-stamps the statusLine block when missing".

    Do NOT touch the existing classes A-G logic. Class H is purely additive.

    Per D-09 parts (3)+(4): "(3) Extend `/mos:doctor` to detect missing statusLine block as a class of drift ... verify it actually surfaces and offers --fix. (4) `/mos:doctor --fix` should write the statusLine block when missing, idempotently. Same pattern as the SessionStart hook registration in Phase 95.2."
  </action>
  <verify>
    <automated>node -c /home/jsagi/MindrianOS-Plugin/scripts/doctor.cjs && node /home/jsagi/MindrianOS-Plugin/tests/test-doctor-class-h.cjs && node /home/jsagi/MindrianOS-Plugin/tests/test-doctor-class-h-fix.cjs && node /home/jsagi/MindrianOS-Plugin/tests/test-install-receipt.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/doctor.cjs` exits 0 (syntax valid)
    - `grep -qi "class H\|classH" scripts/doctor.cjs` succeeds; `grep -q "runClassH\|class.?H" scripts/doctor.cjs` (a class-H handler exists)
    - `grep -q ".install-receipt.json" scripts/doctor.cjs` succeeds (class H reads the receipt)
    - `grep -q "statusline-mos" scripts/doctor.cjs` (already present for class G; class H reuses the same wrapper path in its `--fix` write -- confirm the `--fix` block writes `path.join(INSTALL_DIR, 'scripts', 'statusline-mos')`)
    - `node tests/test-doctor-class-h.cjs` exits 0 (detection: ok on clean, warn on missing-statusLine, warn+named-steps on halted-receipt, --all activates the key)
    - `node tests/test-doctor-class-h-fix.cjs` exits 0 (--fix writes the block, idempotent on re-run, --json reports ok afterward)
    - `node tests/test-install-receipt.cjs` exits 0 (now that class H reads the receipt, the doctor-reads-receipt assertions pass)
    - `node scripts/doctor.cjs --statusline-visibility --json | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d);process.exit(j.checks&&(j.checks.classH||j.checks['statusline-visibility'])?0:1)})"` -- exits 0 on this dev machine (the check key is present in the JSON output; status may be warn/ok/skip depending on the dev machine's state -- that is fine, we just assert the key exists)
    - `grep -c $'—' scripts/doctor.cjs` -- if 0, good; if pre-existing em-dashes exist outside this change, weaken to `git diff scripts/doctor.cjs | grep "^+" | grep -c $'—'` returns 0
  </acceptance_criteria>
  <done>
    `/mos:doctor` has class H: it detects a missing/empty statusLine block, reads `.install-receipt.json` to name missing install steps, and `--fix` re-stamps the statusLine block idempotently. `--statusline-visibility` and `--all` activate it. The three Wave 0 doctor/receipt tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Auto-run /mos:doctor on a fresh first session and surface the result</name>
  <files>scripts/check-onboard-statusline.cjs</files>
  <read_first>
    - scripts/check-onboard-statusline.cjs (whole file -- the touch-file path `~/.mindrian/onboarding/statusline-onboarded.json`, the fire condition, the `additionalContext` gate it surfaces, the defensive `{ continue: true }` + uncaughtException handler, the "pure CJS, node built-ins only, zero npm deps" invariant, the existing spawn logic if any)
    - scripts/preflight-doctor.cjs (the existing pattern for spawning `node scripts/doctor.cjs --json` with a timeout and parsing the result; Canon Part 8 zero-network note)
    - scripts/doctor.cjs (the `--all --json` output shape -- `report.checks.*` with `status` per check)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md (D-09 -- "On a first session after install, /mos:doctor reports all-green or names exactly what is missing")
    - .planning/seeds/SEED-008-intelligence-layer-activation-gap-close-the-loop.md (the "make the protective layer self-verify" pattern this instantiates)
  </read_first>
  <action>
    In `scripts/check-onboard-statusline.cjs`, on the fire path (the branch that surfaces the statusline-confirmation gate), ADD a doctor self-check:
    ```js
    // Phase 95.6 D-09: on a fresh first session, also run /mos:doctor and
    // surface a one-line all-green-or-here-is-what-is-missing summary, so a
    // silently-incomplete install gets caught and named instead of staying
    // quiet. Canon Part 8: doctor.cjs is purely LOCAL (no network).
    let doctorLine = '';
    try {
      const { spawnSync } = require('node:child_process');
      const doctorPath = require('node:path').join(pluginRoot, 'scripts', 'doctor.cjs');
      const r = spawnSync(process.execPath, [doctorPath, '--all', '--json'], { timeout: 1500, encoding: 'utf8' });
      if (r && r.stdout) {
        const report = JSON.parse(r.stdout);
        const checks = (report && report.checks) ? Object.values(report.checks) : [];
        const bad = checks.filter(c => c && c.status && c.status !== 'ok' && c.status !== 'skip');
        if (bad.length === 0) {
          doctorLine = 'MindrianOS install check: all green.';
        } else {
          doctorLine = 'MindrianOS install check: ' + bad.length + ' item(s) need attention. Run /mos:doctor --fix to resolve, or /mos:doctor --all for details.';
        }
      }
    } catch (e) { /* never block the hook chain; doctorLine stays '' */ }
    ```
    Then append `doctorLine` (if non-empty) to the `additionalContext` string the hook already builds (a newline + the line). Keep ALL existing behavior: the touch-file logic, the statusline-confirmation gate, the `{ continue: true }` on any error, the `uncaughtException` handler. This is strictly additive. If `pluginRoot` is not already a variable in this file, derive it the same way the file already resolves the plugin install dir (or use `process.env.CLAUDE_PLUGIN_ROOT` / the `INSTALL_DIR` convention -- match what the file does).

    Defensive contract (unchanged invariant, Phase 106-05): on ANY internal error -- doctor.cjs missing, spawn timeout, JSON parse failure, anything -- the hook emits `{ continue: true }` and exits 0. The doctor self-check is best-effort decoration on the gate, never a blocker.

    Per D-09 part (4) context + SEED-008: the protective layer self-verifies. The first session is the moment to surface "is this install actually complete?" -- and to name what is missing if not.
  </action>
  <verify>
    <automated>node -c /home/jsagi/MindrianOS-Plugin/scripts/check-onboard-statusline.cjs && echo "{}" | node /home/jsagi/MindrianOS-Plugin/scripts/check-onboard-statusline.cjs >/dev/null 2>&1; echo "exit: $? (0 expected -- hook must never crash)"
</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/check-onboard-statusline.cjs` exits 0 (syntax valid)
    - `echo "{}" | node scripts/check-onboard-statusline.cjs` exits 0 (the hook never crashes, even with junk-ish stdin) -- this is the Phase 106-05 invariant, preserved
    - `grep -q "doctor.cjs" scripts/check-onboard-statusline.cjs` succeeds (the doctor self-check is wired in)
    - `grep -q "spawnSync\|spawn" scripts/check-onboard-statusline.cjs` succeeds and the doctor spawn has a `timeout` (`grep -q "timeout: 1500\|timeout:1500\|timeout" scripts/check-onboard-statusline.cjs`)
    - `grep -q "all green\|need attention" scripts/check-onboard-statusline.cjs` succeeds (the two summary-line variants)
    - `grep -q "continue: true\|continue:true" scripts/check-onboard-statusline.cjs` still present (defensive contract intact)
    - `grep -q "fetch\|http\|curl\|brain.mindrian\|tavily" scripts/check-onboard-statusline.cjs` returns nothing new in the diff (Canon Part 8: zero network surface added; doctor.cjs is purely LOCAL): `git diff scripts/check-onboard-statusline.cjs | grep "^+" | grep -E "fetch|http|curl|brain\.mindrian|tavily"` returns nothing
    - `grep -c $'—' scripts/check-onboard-statusline.cjs` -- if 0, good; else weaken to `git diff scripts/check-onboard-statusline.cjs | grep "^+" | grep -c $'—'` returns 0
  </acceptance_criteria>
  <done>
    On a fresh first session, `check-onboard-statusline.cjs` additionally runs `/mos:doctor --all --json`, parses it, and appends a one-line "all green" or "N items need attention -- run /mos:doctor --fix" summary to the additionalContext gate. Defensive: any error and the hook emits `{ continue: true }` and exits 0. Zero network surface added.
  </done>
</task>

</tasks>

<verification>
After all three tasks (sequence: Task 1 install.sh -> Task 2 doctor.cjs -> Task 3 check-onboard; re-run the receipt test after Task 2 since it exercises the doctor side):

1. `bash -n install.sh && node -c scripts/doctor.cjs && node -c scripts/check-onboard-statusline.cjs` -> all exit 0.
2. `node tests/test-doctor-class-h.cjs && node tests/test-doctor-class-h-fix.cjs && node tests/test-install-receipt.cjs` -> all exit 0 (the three Wave 0 D-09 suites now GREEN).
3. `bash tests/test-install-sh-skill-loop.sh` -> exits 0, INCLUDING the `.statusLine.command` assertion (and 95.6-03's WARN-and-continue assertions if 95.6-03 landed; if 95.6-03 has NOT landed, the WARN assertion is RED -- but the `.statusLine` assertion this plan owns is GREEN).
4. `bash tests/run-all-956.sh` -> after 95.6-03/04/05 all land, the only RED suite is `test-release-npm-gate.sh` (owned by 95.6-06); after 95.6-06 + 95.6-01, the scoped runner is fully GREEN.
5. `node scripts/doctor.cjs --statusline-visibility --json` on this dev box -> emits JSON with a class-H key present (status may be warn here since this dev workspace is not a fresh install -- that is expected and fine).
6. `echo "{}" | node scripts/check-onboard-statusline.cjs; echo "exit $?"` -> exit 0 (hook never crashes).
7. `grep -c "set -euo pipefail" install.sh` -> 1. No em-dashes in any diff.

Files touched: `install.sh` (new `register_statusline()` + receipt helpers + call-order reshuffle), `scripts/doctor.cjs` (new class H, additive), `scripts/check-onboard-statusline.cjs` (doctor self-check, additive). Overlaps `install.sh` with 95.6-03 (Step 5) and 95.6-04 (Step 1/2) -- THIS plan is the most invasive install.sh change and should land LAST within Wave 1's install.sh sequence; build on 95.6-03 + 95.6-04's changes, do not undo them.
</verification>

<success_criteria>
- [ ] `install.sh` has `register_statusline()` called before any cp/ln loop (after node+git preflight), writing the canonical `statusLine` block idempotently
- [ ] `install.sh` writes `~/.claude/plugins/mindrian-os/.install-receipt.json` incrementally with per-step exit codes + `completed_at`; every receipt call has `2>/dev/null || true` so it never aborts the install
- [ ] `scripts/doctor.cjs` has class H: detects missing/empty statusLine block, reads `.install-receipt.json` to name missing install steps, `--fix` re-stamps the statusLine block idempotently; activated by `--statusline-visibility` and `--all`; `skip` on Desktop
- [ ] `scripts/check-onboard-statusline.cjs` runs `/mos:doctor --all --json` on a fresh first session and appends a one-line summary to the gate's additionalContext; defensive `{ continue: true }` on any error preserved; zero network surface added (Canon Part 8)
- [ ] `tests/test-doctor-class-h.cjs`, `tests/test-doctor-class-h-fix.cjs`, `tests/test-install-receipt.cjs` all pass; `.statusLine` assertion in `tests/test-install-sh-skill-loop.sh` passes
- [ ] `set -euo pipefail` untouched; classes A-G in doctor.cjs untouched; existing 106-05 hook behavior preserved
- [ ] No emoji (hexagon written as `(hexagon)` in prose, never an emoji glyph), no em-dashes anywhere in the edits
</success_criteria>

<output>
After completion, create `.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-05-SUMMARY.md` documenting:
- The `register_statusline()` function + where it is called in the new install order (confirm it precedes the first cp/ln loop)
- The `.install-receipt.json` shape written + the per-step calls + confirmation every receipt call is best-effort (`|| true`)
- The class-H detection logic in doctor.cjs + the `--fix` statusline restamp + which report key name was used (matching the Wave 0 test)
- The check-onboard-statusline.cjs doctor self-check addition + confirmation the defensive contract is intact + no network surface added
- Which Wave 0 tests flipped GREEN
- Whether 95.6-03 / 95.6-04 had already landed and how the install.sh reshuffle built on them
- Confirmation `set -euo pipefail`, classes A-G, and the existing 106-05 gate behavior are untouched
</output>
</invoke>
