---
phase: 95.6
slug: install-cache-windows-hardening-and-skill-loop-resilience
type: gsd-recommendations
status: complete
created: 2026-05-10
companion: 95.6-INSIGHTS.md
classification: LOCAL (not for Brain egress per Canon Part 8)
intended_for: gsd-planner agent + human reviewer (GSD process orchestrator)
---

# Phase 95.6 GSD Recommendations

This file translates the review findings in `95.6-INSIGHTS.md` into actions GSD should sequence and own. The user directive on 2026-05-10 was: "give insights MD and recommendations MD for GSD. It will manage the process."

GSD reads the recommendations below and routes them into plans, todos, follow-ups, or process changes. Each recommendation maps to a specific GSD command path so the orchestrator can act without further human disambiguation.

---

## 0. LEADING APPROACH NOTE (read first)

A second user directive landed 2026-05-10 after the first version of this file was written: **"I want your approach to be the leading in the phase we are planning."**

That approach is captured in the new file `95.6-PACKAGING-RESEARCH.md`. It is the canonical packaging architecture for Phase 95.6 and supersedes the simpler framing of REC-04 below. The packaging research expands the original D-05 (single npm publish step) into four sub-decisions D-05a/b/c/d, adds two new decisions D-10 (subagent capability parity) and D-11 (reserved names + Deferred Tool Loading awareness), and revises Wave-1 from 5 plans to 10 plans (with a documented minimum-viable subset for beta.9).

**When GSD picks Phase 95.6 work back up, the canonical reading order is:**

1. CONTEXT.md (locked decisions, including the new D-10 / D-11 stubs)
2. RESEARCH.md (live call findings + NATO timing)
3. **95.6-PACKAGING-RESEARCH.md (LEADING APPROACH for D-05 / R-06 / distribution -- read BEFORE generating plans for those decisions)**
4. 95.6-FAMILY-AUDIT.md
5. 95.6-INSIGHTS.md
6. 95.6-RECOMMENDATIONS.md (this file)
7. 95.6-01-PLAN.md (existing partial plan)

Plans that diverge from the packaging research must justify the deviation in their frontmatter or `<rationale>` block.

REC-04 below stays in this file for traceability of the review chain, but its scope is now expanded by the packaging research. Treat REC-04 as REC-04a (publish gate, locked) with REC-04b/c/d implied by the packaging research's D-05b/c/d. See 95.6-PACKAGING-RESEARCH.md Section 7 for the full mapping.

---

## 1. Pre-plan-phase remediation (must run before /gsd:plan-phase 95.6)

Five Critical issues block plan-phase from consuming the current artifacts as authoritative input. Each maps to a single Edit operation against a specific file. GSD should sequence these as a single quick-fix sweep before the next planner spawn.

### REC-01: Patch the Phase 92 leaf char count (174 to 189)

- **Action type:** /gsd:fast (trivial inline patch, no agent needed)
- **Files:** 95.6-CONTEXT.md, 95.6-FAMILY-AUDIT.md, 95.6-01-PLAN.md
- **Source verified:** 189 chars at the leaf (PLAN-01 line 64 already shows correct value)
- **Edit pattern:** Replace `174` with `189` in three files. PLAN-01 line 33 also says "(174-char leaf)" in its objective text; update to match line 64.
- **Acceptance:** `grep -rln "174.char\|174 char" .planning/phases/95.6-*/` returns 0 hits.
- **Estimated time:** 2 minutes.

### REC-02: Distinguish pre-95.2 (cp -aT) from current (fs.cpSync) in audit

- **Action type:** /gsd:fast (one paragraph rewrite in audit Pattern C section)
- **File:** 95.6-FAMILY-AUDIT.md (Pattern C section + Section 8 implications)
- **Source verified:** `scripts/doctor.cjs:256` uses `fs.cpSync`. Pre-95.2 used `cp -aT`. The 2026-05-06 autopsy line 36 confirms.
- **Edit pattern:** In Pattern C, replace mentions of `cp -aT` with explicit "pre-95.2 cp -aT, post-95.2 fs.cpSync". Update Pattern C headline per IMP-03 below to be reframed correctly.
- **Acceptance:** `grep "cp -aT" 95.6-FAMILY-AUDIT.md` returns 0 hits without context, OR all hits are in historical references.
- **Estimated time:** 5 minutes.

### REC-03: Re-ground D-06 on supported source

- **Action type:** /gsd:fast OR additive-edit on FEEDBACK.md
- **Files:** 95.6-CONTEXT.md (D-06, line 103) AND 95.6-FAMILY-AUDIT.md (R-06)
- **Source verified:** FEEDBACK.md has 0 hits for "BSL" or "open[- ]source". The "two sessions" claim is unsupported.
- **Edit pattern (recommended):** Re-ground D-06 on RESEARCH.md F-06 ("NATO Defense College institutional trust audit will catch BSL-1.1 mislabel"). The institutional-trust framing is the actual urgency driver.
- **Edit pattern (alternate):** If the BSL/open-source flag really did surface in the tester's CC sessions, add the missing FEEDBACK.md entry from the actual transcript. Otherwise stay with re-grounded version.
- **Acceptance:** `grep "two separate sessions" 95.6-CONTEXT.md` returns 0 hits.
- **Estimated time:** 10 minutes.

### REC-04: Reframe D-05 from "fix existing" to "add missing"

- **Action type:** /gsd:fast (D-05 step (1) rewrite)
- **Files:** 95.6-CONTEXT.md (D-05) AND 95.6-FAMILY-AUDIT.md (R-06)
- **Source verified:** `scripts/release.sh` has Steps 1-11; no Step 9.5. `grep "npm publish" scripts/release.sh` returns 0 hits.
- **Edit pattern:** Replace "Verify scripts/release.sh Step 9.5 actually exists and what it does. Run a dry-run to see the failure mode." with "Step 9.5 does not exist in scripts/release.sh. The npm publish gate documented in feedback_release_lockstep_npm.md was never implemented despite the memory entry. ADD Step 9.5 to release.sh as the canonical npm publish gate."
- **Acceptance:** D-05 step (1) reads as an additive operation, not a repair operation.
- **Estimated time:** 5 minutes.

### REC-05: Move live Brain key UUIDs out of FEEDBACK.md (key-handling decision)

**This is the user-decision item. The user said "TELL GSD ABOUT IT" for the key handling.**

- **Action type:** /gsd:fast OR /gsd:add-todo
- **Files:** [private tester archive], docs/testers/outbox/2026-05-07-gary-laben-welcome.md (gitignored)
- **Source verified:** FEEDBACK.md 2026-05-09 entry currently contains both UUIDs as plaintext. outbox welcome is correctly gitignored per docs/testers/.gitignore.
- **Edit pattern (canonical decision per docs/testers/STYLE-GUIDE.md): MOVE BOTH UUIDS TO GITIGNORED WELCOME EMAIL.**
   1. Append to docs/testers/outbox/2026-05-07-gary-laben-welcome.md:
      ```
      ## 2026-05-09 key rotation

      Old key (REVOKED 2026-05-09): 04ffcdae-17cb-4bca-a286-2ac32bda3d56
      New key (ACTIVE 2026-05-09, expires 2026-07-08): 7a69abf8-65e3-4dfa-b5f0-a8b9597a17de

      Both keys here ONLY because outbox/*-welcome.md is gitignored.
      Reference from FEEDBACK.md without disclosing UUIDs.
      ```
   2. Edit FEEDBACK.md 2026-05-09 entry: replace both UUIDs with redacted form.
      - "Old key `04ffcdae-...` revoked." -> "Old key (`04ffcdae-...` redacted; full value in gitignored welcome email) revoked."
      - "New key issued: `7a69abf8-...`, valid 60 days, expires 2026-07-08, free plan." -> "New key issued (UUID redacted from this committed file; full value in gitignored welcome email at docs/testers/outbox/2026-05-07-gary-laben-welcome.md), valid 60 days, expires 2026-07-08, free plan."
- **Acceptance:** `grep -E "04ffcdae|7a69abf8" [private tester archive]` returns 0 hits. Both UUIDs present only in gitignored outbox file.
- **Estimated time:** 5 minutes.

### REC-06: Add Plan 95.6-09 for the case #4 autopsy (R-07 plan slot)

- **Action type:** Add to plan-phase scope OR /gsd:add-phase scoped to autopsy-only
- **File to create:** [private case archive]
- **Source pattern:** Mirror docs/autopsies/2026-05-06-install-dir-missing-incident.md structure (Summary, Root Causes, Detection, Fix, Generalization).
- **Why this is mandatory:** every prior case in the install-cache failure family has an autopsy. Case #4 should not ship as v1.13.0-beta.9 without one. The audit's R-07 already named this gap.
- **Sequencing:** can be parallel with other Wave-2 plans; depends only on having FEEDBACK.md content as source material.
- **Estimated time:** 60-90 minutes (writing new autopsy from FEEDBACK.md + RESEARCH.md transcript synthesis).

---

## 2. Plan-phase scope adjustments

Three structural changes that the next /gsd:plan-phase 95.6 invocation should embed. These are not file patches; they are scope-decision notes for the planner.

### REC-07: Reframe Pattern C in audit (IMP-03)

The audit's Pattern C ("scripts can't reach upstream causes") is overreached. R-03 (cross-shell wrapper) and R-05 (settings.json allowlist) directly contradict the pattern.

**Action:** Edit FAMILY-AUDIT.md Section 3 Pattern C headline to: "the cure-via-install.sh paradigm has a ceiling; scripts can reach upstream causes only by operating OUTSIDE the install.sh boundary."

**Why this matters for plan-phase:** without this reframe, the planner may treat R-03 and R-05 as out-of-scope (per the original Pattern C claim) when they are actually the in-scope mechanisms.

### REC-08: Distinguish R-02 from 95.2 SessionStart preflight (IMP-02)

R-02 (pre-install dependency audit) operates BEFORE install.sh. Phase 95.2 SessionStart preflight operates AFTER install completes. Two different surfaces, both required.

**Action:** Add to FAMILY-AUDIT.md R-02 the sentence: "Pre-install audit runs BEFORE install.sh executes its work; SessionStart preflight (95.2) runs AFTER install completes. Two different surfaces, both required."

**Why this matters for plan-phase:** without this clarification, a planner may unintentionally re-architect what 95.2 already shipped.

### REC-09: Add `replies/.gitkeep` to tester folder canon

REGISTRY.md "Per-tester folder structure" mandates `replies/` but git silently dropped the tester's empty directory.

**Action:** Add `[private tester archive]/replies/.gitkeep`. Update REGISTRY.md "Per-tester folder structure" canon to explicitly mention the `.gitkeep` requirement.

**Why this matters for GSD:** future tester onboards will keep dropping the `replies/` directory until canon is updated. Pattern propagates.

---

## 3. Pre-merge fixes (must fix before 95.6 ships as v1.13.0-beta.9)

These are Important issues from INSIGHTS.md that should be resolved before the beta.9 release tag.

### REC-10: PLAN-01 frontmatter `files_modified` understates (IMP-04)

Add `.planning/phases/92-trust-layer-refactor/*-CONTEXT.md` to PLAN-01 `files_modified`. Add a note: "potentially additional files if dangling references found in Task 2."

### REC-11: PLAN-01 introduces `full_slug` without canon registration (IMP-05)

Either accept as one-off and document inline, OR move searchable name to file body `## Note` section instead of frontmatter.

**Recommendation:** move to file body. Avoids canon precedent for a field that solves a one-time naming problem.

### REC-12: CONTEXT.md off-by-one (IMP-06)

Change CONTEXT.md line 191 from "Phase 94" to "Phase 93".

### REC-13: SEED-007 self-violation patches (IMP-07, IMP-08)

- IMP-07: Replace literal U+2014 + U+2013 in SEED-007 line 88 with codepoint references.
- IMP-08: Tighten SEED-007 session-start breadcrumb to clarify current source uses double-hyphens (not em-dashes); the v1.12.0-era stale install was the em-dash source.

### REC-14: PLAN-01 grep stability hardening (MIN-04, MIN-05)

- Add `--color=never` to grep invocations in Task 2.
- Add `-M50%` to git rename-detection OR weaken acceptance to file-count check.

---

## 4. Defer-friendly polish (file as todos, do not block ship)

These minor issues should be captured as todos but should NOT block 95.6 ship for the NATO June deadline.

### REC-15: REGISTRY.md the Wave-2 tester row too long (MIN-01)

Trim to "Critical tester. Head of advisory board at Hopkins, intro via Lawrence. See FEEDBACK.md for install-friction signal." Capture as /gsd:add-todo.

### REC-16: Phone number in PERSONA.md (MIN-02)

Verify with the Wave-2 tester whether he consents to phone in committed file. If not, move to gitignored welcome. Capture as /gsd:add-todo with `awaiting_user_input: true`.

### REC-17: SEED-007 scope casing (MIN-03)

Stylistic drift only. Capture as a backlog item for next SEED canon review (out of 95.6 scope).

### REC-18: PERSONA.md cross-reference path qualifier (MIN-07)

Add "(will be created when he replies)" to the replies/ path reference. Capture as /gsd:add-todo.

---

## 5. Process changes (canon-level, future-facing)

These are PROC findings from the reviewers. They affect how future audits / seeds / tester onboards are done. Each is canon-level and should be ratified before being applied broadly.

### REC-19: Future family audits must self-verify factual claims

The two reviewer subagents collectively found 4 Critical factual errors in 95.6-FAMILY-AUDIT.md that simple file reads at write-time would have caught. The audit treated memory-entry text and CONTEXT.md as authoritative when they are not always.

**Action:** Update audit-writing process (informally now, formally in a docs/ entry later):
- Each major factual claim in a family audit must include `[verified by: <file>:<line>]` citation
- Audits must include a closing claim-vs-source table

**Owner:** GSD (when next family audit is written, enforce this convention via the gsd-phase-researcher prompt)

### REC-20: SEED template extension fields should be canonized

SEED-002, SEED-006, SEED-007 all extend frontmatter with `canon_parts`, `binds_to_phase`, `binds_to_milestone`. These fields are not documented in `~/.claude/get-shit-done/workflows/plant-seed.md`.

**Action:** Update plant-seed.md to document the canonical extension set so seed authors converge.

**Owner:** /gsd:plant-seed workflow maintainer (out-of-scope for 95.6; capture as `/gsd:add-todo` against superpowers/get-shit-done plugin)

### REC-21: Tester folder canon must include `.gitkeep` mandate

REGISTRY.md "Per-tester folder structure" canon should include: "create `replies/.gitkeep` so the empty directory survives git."

**Action:** Already covered by REC-09 above. This is the canon-level companion.

### REC-22: the tester's "scope clarity" gap needs a canonical surface

The question "does MindrianOS run in every Claude Code project, or only Mindrian-associated ones?" appears in 3 places (REGISTRY Notes, FEEDBACK action item, ROOM cross-reference) but no canonical surface owns the answer.

**Action:** Add one paragraph to STYLE-GUIDE.md OR install-site copy. Capture as /gsd:add-todo against the install-site repo.

---

## 6. Sequencing -- recommended GSD execution order

When GSD picks this work up, run in this order:

```
Step 1 (REC-01..REC-04): Pre-plan-phase Critical patches.
        Time budget: 25 minutes total.
        Single /gsd:fast invocation for all four. Touches 3 files.

Step 2 (REC-05): Move Brain key UUIDs out of FEEDBACK.md.
        Time budget: 5 minutes.
        Touches 2 files (FEEDBACK.md, gitignored outbox welcome).

Step 3 (REC-07, REC-08, REC-09): Plan-phase scope adjustments.
        Time budget: 15 minutes.
        Single /gsd:fast invocation. Touches 1-2 files.

Step 4: Re-run /gsd:plan-phase 95.6 with updated artifacts.
        Time budget: 15-20 minutes.
        Spawns gsd-planner with updated CONTEXT, RESEARCH, AUDIT, INSIGHTS, RECOMMENDATIONS.

Step 5 (REC-06): Add the case #4 autopsy.
        Time budget: 60-90 minutes (depending on transcript-synthesis depth).
        Can run parallel with Wave-1 plans from Step 4.

Step 6 (REC-10..REC-14): Pre-merge Important issues.
        Time budget: 30-45 minutes total.
        Run during /gsd:execute-phase 95.6 for the affected plans.

Step 7 (REC-15..REC-18): Defer to /gsd:add-todo.
        Time budget: 5 minutes total to file as todos.
        Resolve in 1-2 milestones, NOT blocking on NATO June.

Step 8 (REC-19..REC-22): Process / canon changes.
        Time budget: variable.
        File as backlog. Most are out-of-95.6-scope.
```

**Time-to-plan-phase-ready: ~45 minutes (Steps 1-3).**

**Time-to-beta.9-ship-ready: 95.6 plan-phase execution + Step 5 autopsy + Step 6 fixes. NATO June deadline (2026-06-01) gives ~3 weeks of buffer.**

---

## 7. What GSD owns vs what humans own

To avoid GSD trying to make decisions outside its scope, this section lists which RECs need human input.

### GSD owns end-to-end (no human prompt needed)

- REC-01, REC-02, REC-04, REC-05 (mechanical edits with verified targets)
- REC-07, REC-08 (paragraph rewrites with explicit text provided above)
- REC-09 (add .gitkeep file)
- REC-10, REC-12, REC-14 (mechanical edits)
- REC-13 (codepoint substitution + breadcrumb tightening)
- REC-15, REC-18 (text trims)

### GSD prompts user before executing

- REC-03 (D-06 re-ground vs add to FEEDBACK -- human decides which path)
- REC-06 (Plan 95.6-09 vs fold-into-95.6-06 -- human decides plan structure)
- REC-11 (one-off accept vs move-to-body -- human decides canon precedent)
- REC-16 (phone consent verification -- requires direct user input)

### Out of GSD's scope (filed as todos, owned elsewhere)

- REC-17 (SEED canon casing review -- next SEED canon iteration)
- REC-19 (audit-writing convention -- gsd-phase-researcher prompt update)
- REC-20 (plant-seed.md update -- superpowers plugin maintenance)
- REC-21 (REGISTRY.md canon update -- separate canon-update phase)
- REC-22 (STYLE-GUIDE.md update -- install-site repo + canon)

---

## 8. Companion file index

```
.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/
├─ 95.6-CONTEXT.md          locked decisions D-01..D-09 (NEEDS CRIT-01, CRIT-03, CRIT-04 patches)
├─ 95.6-RESEARCH.md         live call findings + NATO timing (NEEDS CRIT-01 patch)
├─ 95.6-FAMILY-AUDIT.md     cross-phase audit (NEEDS CRIT-01, CRIT-02, CRIT-04, IMP-03 patches)
├─ 95.6-INSIGHTS.md         review findings (this audit cycle's deliverable)
├─ 95.6-RECOMMENDATIONS.md  this file -- GSD action sequence
└─ 95.6-01-PLAN.md          partial plan from interrupted spawn (NEEDS CRIT-01, IMP-04, IMP-05 patches)
```

When /gsd:plan-phase 95.6 next runs, it should read all six artifacts in the order shown above. INSIGHTS and RECOMMENDATIONS are the most authoritative sources for "what changed since the audit was written."

The two reviewer subagent agentIds are preserved if follow-up questions arise:
- Review #1 agentId: `a755190628d0d191a` (commits review)
- Review #2 agentId: `aa14b641776d5083d` (install artifacts review)
