---
phase: 95.6
plan: 10
type: execute
wave: 3
depends_on: ["95.6-01", "95.6-02", "95.6-03", "95.6-04", "95.6-05", "95.6-06"]
files_modified:
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - package.json
  - .planning/STATE.md  # update milestone position / beta target after the ship
autonomous: false
tier: 1
defer_to_beta: null
requirements_addressed: [D-08]
canon_parts: [Part 6, Part 7]
must_haves:
  truths:
    - "The full automated test surface is green: `bash tests/run-all-956.sh` exits 0 (all 8 Phase 95.6 suites pass) -- the precondition for cutting the release"
    - "The Windows cold-install manual gate (tests/manual/95.6-windows-cold-install-acceptance.md) has been run by a real Windows tester and signed off -- this is a BLOCKING checkpoint before the release commit"
    - "v1.13.0-beta.9 is cut per CLAUDE.md's MANDATORY release process: CHANGELOG entry + plugin.json + package.json bumped + git tag v1.13.0-beta.9 + marketplace.json ref pinned + `@mindrian/os@1.13.0-beta.9` published to npm with the @next dist-tag (via scripts/release.sh Step 9.5)"
    - "STATE.md reflects the ship: phase 95.6 done, beta.9 shipped, the install-cache failure family case #4 closed"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "The `## [1.13.0-beta.9]` entry: Added/Fixed/Changed for the Tier 1 work (D-02 rename, D-03 skill-loop hardening, D-01 long-path preflight, D-09 statusline + class H + install receipt, D-05a npm publish gate) + the legacy beta.1-beta.8 npm-gap note folded in"
      contains: "1.13.0-beta.9"
    - path: ".claude-plugin/plugin.json"
      provides: "version bumped to 1.13.0-beta.9"
      contains: "1.13.0-beta.9"
    - path: "package.json"
      provides: "version bumped to 1.13.0-beta.9 (in lockstep with plugin.json)"
      contains: "1.13.0-beta.9"
  key_links:
    - from: "tests/manual/95.6-windows-cold-install-acceptance.md"
      to: "the v1.13.0-beta.9 release commit"
      via: "the cold-install gate is a BLOCKING precondition -- the release does not happen until it is signed off"
      pattern: "windows-cold-install-acceptance"
    - from: "scripts/release.sh (Step 9.5)"
      to: "npm registry (@mindrian/os@1.13.0-beta.9 @next)"
      via: "the release script's npm publish gate publishes the package in lockstep"
      pattern: "Step 9.5"
---

<objective>
TIER 1 RELEASE GATE (the last Tier 1 plan; Wave 3). Bundle the Phase 95.6 Tier 1 work into v1.13.0-beta.9 per CLAUDE.md's MANDATORY release process, gated on (a) the full automated test surface being green and (b) the Windows cold-install manual gate being signed off by a real Windows tester. D-08: beta target is locked at v1.13.0-beta.9 (beta.8 has the bugs, beta.9 ships the fixes); NATO Defense College Rome June 2026-06-01 is the hard deadline, soft target 2026-05-23, maintainer's stated personal commitment 2026-05-11 for the Tier 1 hotfix subset.

Purpose: the Tier 1 plans (95.6-01 D-02 rename, 95.6-03 D-03 skill-loop, 95.6-04 D-01 long-path preflight, 95.6-05 D-09 statusline + class H + receipt, 95.6-06 D-05a npm publish gate + autopsy) produce the fixes; this plan ships them as a coherent release. Release infrastructure ALWAYS ships as a beta first (CLAUDE.md) -- v1.13.0-beta.9 is correct (it includes the new `scripts/release.sh` Step 9.5, which is itself release infra). The cold-install manual gate is the empirical Dimension-8 gate (95.6-VALIDATION.md): a green CI run with a failed cold-machine gate means the phase is NOT done.

Output: a `## [1.13.0-beta.9]` CHANGELOG entry; `plugin.json` + `package.json` bumped to `1.13.0-beta.9`; the release executed via `scripts/release.sh` (which now includes Step 9.5 npm publish); the marketplace.json `ref` pinned to `v1.13.0-beta.9` (in the separate `~/mindrian-marketplace/` repo -- a manual step if not locally checked out); `npm view @mindrian/os@next version` returning `1.13.0-beta.9`; STATE.md updated; the install-site `npx @mindrian/os@next` block updated (or removed if not yet working) -- a manual action in the separate install-site repo, noted in the checkpoint.

This plan has a BLOCKING human-verify checkpoint (the cold-install gate) before the release commit. It does NOT include Tier 2 (95.6-07, 95.6-08) or Tier 3 (95.6-09) in the `depends_on` -- those are not blockers for beta.9; if they have landed by the time this runs, fold their CHANGELOG lines in; if not, they roll into beta.10. Per CLAUDE.md: never skip the release process; every push that changes user-facing functionality MUST bump the version and sync all five gates (CHANGELOG, plugin.json, package.json, git tag, marketplace.json ref).

Per Canon Part 7 (reuse before build): the release runs through the existing `scripts/release.sh` (which 95.6-06 extended with Step 9.5); this plan does not invent a release procedure. Per Canon Part 6 (dog-fooding): the release process the product team uses IS the release process; the cold-install gate is the dog-fooding mandate at the ship boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-VALIDATION.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-RESEARCH.md
@CHANGELOG.md
@.claude-plugin/plugin.json
@scripts/release.sh
@CLAUDE.md

<reference_data>
CLAUDE.md MANDATORY release process (the five gates): (1) CHANGELOG.md entry at the top; (2) `.claude-plugin/plugin.json` `version` matches; (3) `package.json` `version` matches; (4) `git tag v<version>` at the release commit; (5) `~/mindrian-marketplace/.claude-plugin/marketplace.json` updated with the new version AND its `source.ref` pinned to the tag. PLUS (memory `feedback_release_lockstep_npm.md` + 95.6-06's Step 9.5): `npm publish` `@mindrian/os@<version>` with `--tag next` for the `-beta.` suffix. `scripts/release.sh` is the ONLY way to release -- run it, do not bump versions by hand. (Note: there is NO `packages/npm-installer/package.json` in this repo despite the memory entry mentioning it; only the root `package.json`.)

Current state: `plugin.json` `version` = `1.13.0-beta.8`; `package.json` `version` = `1.13.0-beta.8`; `~/mindrian-marketplace/.claude-plugin/marketplace.json` has `mos` at `1.13.0-beta.8` with `source.ref: v1.13.0-beta.8`. Target: `1.13.0-beta.9` everywhere + `ref: v1.13.0-beta.9` + npm publish at `@next`.

The CHANGELOG `## [1.13.0-beta.9] - <YYYY-MM-DD>` entry should cover (group as Added / Fixed / Changed):
- Fixed: install.sh skill-loop no longer aborts on a skill dir missing SKILL.md -- it warns and continues (D-03; the bug that broke Gary Laben's install live on 2026-05-09). Backfilled `skills/mullins-scaffold/SKILL.md`.
- Fixed: Windows long-path failures during `git clone` -- install.sh now enables `core.longpaths` on Windows before cloning, cleans a stale partial clone before retrying, and fails with a clear "Git for Windows" error if git is missing on Windows (D-01).
- Changed: renamed `.planning/phases/92-...` (189-char leaf) to `.planning/phases/92-trust-layer-refactor/` -- removes the Windows MAX_PATH failure class for that directory (D-02).
- Fixed: the "silent install-incomplete" failure -- install.sh now registers the statusline FIRST (idempotently), writes an `.install-receipt.json`, and `/mos:doctor` gained a class H that detects a missing statusLine block + reads the receipt + `--fix` re-stamps it; a fresh first session auto-runs `/mos:doctor` (D-09).
- Added: `scripts/release.sh` Step 9.5 -- publishes `@mindrian/os` to npm in lockstep with every plugin release (`@next` for betas, `@latest` for releases) (D-05a).
- Notes: `@mindrian/os` versions 1.13.0-beta.1 through 1.13.0-beta.8 were never published to npm (the lockstep publish gate did not exist until this release); those versions remain unpublished; the first npm-published version is 1.13.0-beta.9. (Fold in the 95.6-06 npm-gap note here if it landed in CHANGELOG already; else add it.)
- (If 95.6-07/08/09 landed: add their lines -- bin/cli.js + the four-paths doc + the BSL-1.1 source-available correction + the Manual Recovery README section + the reserved-names release gate + the SessionStart npm-reconcile hook + the subagent parity frontmatter + the Deferred Tool Loading note. If not, do not mention them.)

The cold-install manual gate: `tests/manual/95.6-windows-cold-install-acceptance.md` (created in 95.6-02). It must be RUN by a real Windows tester (Gary Laben or a Wave-3 equivalent) and SIGNED OFF before the release commit. The gate's 8-step checklist: cold Windows 11 box, Git for Windows, no prior MindrianOS state; run the canonical install path (CMD); `git clone` completes with no MAX_PATH error + no manual `core.longpaths`; `install.sh` exits 0 (WARN-and-continue on incomplete skills); statusline renders; `/mos:doctor --all` all-green or names what is missing + `--fix` resolves; reach `/mos:onboard` -> Larry greets, under 5 min, no maintainer help; `npm view @mindrian/os@next version` returns `1.13.0-beta.9` (after the publish) OR the install site does not advertise npx (before).

The install site (`~/mindrianos-install-site/`) is a SEPARATE repo. After the npm publish lands, the `npx @mindrian/os@next` block there should be updated to advertise the now-working path (or, if the publish fails for some reason, removed). This is a manual action in that repo, noted in the checkpoint -- NOT a file edit in THIS repo.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-release gate -- full automated test surface green + CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the top -- where the new entry goes; check whether 95.6-06 already added an `## [1.13.0-beta.9]` or `## [Unreleased]` section + the npm-gap note)
    - tests/run-all-956.sh (the scoped runner -- must be all-green before this plan can proceed)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-VALIDATION.md (the sampling/gate contract)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md (the Tier 1 decision list for the CHANGELOG entry)
  </read_first>
  <action>
    1. Run `bash tests/run-all-956.sh`. ALL 8 Phase 95.6 suites MUST pass: `test-skills-have-skill-md.cjs`, `test-92-rename-no-long-leaves.cjs`, `test-install-sh-skill-loop.sh`, `test-install-preflight-longpath.sh`, `test-doctor-class-h.cjs`, `test-doctor-class-h-fix.cjs`, `test-install-receipt.cjs`, `test-release-npm-gate.sh`. If any is RED, STOP -- the corresponding Tier 1 plan (95.6-01/03/04/05/06) is not complete; do not proceed to the release. Also run `bash -n install.sh && node -c scripts/doctor.cjs && bash -n scripts/release.sh` (syntax sanity).
    2. Write the `## [1.13.0-beta.9] - <today's date>` CHANGELOG entry at the top of `CHANGELOG.md` per the content in `<reference_data>` (Added / Fixed / Changed / Notes). If 95.6-06 already created a stub `## [1.13.0-beta.9]` entry or put the npm-gap note under `## [Unreleased]`, MERGE -- fold the npm-gap note into the beta.9 entry's Notes; do not leave two entries. If 95.6-07/08/09 have landed, add their lines; if not, do not mention them (they go in beta.10).
    3. Do NOT bump `plugin.json` / `package.json` here -- that is Task 3 (via `scripts/release.sh`). This task only verifies the gate and writes the CHANGELOG. Do NOT tag or push here.
  </action>
  <verify>
    <automated>bash /home/jsagi/MindrianOS-Plugin/tests/run-all-956.sh && grep -q "1.13.0-beta.9" /home/jsagi/MindrianOS-Plugin/CHANGELOG.md && bash -n /home/jsagi/MindrianOS-Plugin/install.sh && node -c /home/jsagi/MindrianOS-Plugin/scripts/doctor.cjs && bash -n /home/jsagi/MindrianOS-Plugin/scripts/release.sh
</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-956.sh` exits 0 -- all 8 Phase 95.6 suites PASS
    - `bash -n install.sh && node -c scripts/doctor.cjs && bash -n scripts/release.sh` all exit 0
    - `CHANGELOG.md` has a `## [1.13.0-beta.9]` entry at (or near) the top with a date; it covers D-02, D-03, D-01, D-09, D-05a (grep each decision area's keywords: `grep -q "skill-loop\|SKILL.md" CHANGELOG.md && grep -q "core.longpaths\|long-path" CHANGELOG.md && grep -q "92-trust-layer-refactor" CHANGELOG.md && grep -q "statusLine\|statusline\|class H" CHANGELOG.md && grep -q "npm publish\|Step 9.5" CHANGELOG.md`)
    - The legacy beta.1-beta.8 npm-gap note is present exactly once in CHANGELOG (`grep -c "1.13.0-beta.8" CHANGELOG.md` >= 1; not duplicated)
    - `plugin.json` / `package.json` versions are still `1.13.0-beta.8` at the end of THIS task (the bump is Task 3)
    - No em-dashes in the CHANGELOG diff: `git diff CHANGELOG.md | grep "^+" | grep -c $'—'` returns 0
  </acceptance_criteria>
  <done>
    The full Phase 95.6 automated test surface is green; the `## [1.13.0-beta.9]` CHANGELOG entry is written covering the Tier 1 work + the npm-gap note. Versions not yet bumped (Task 3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING -- Windows cold-install acceptance gate</name>
  <action>This is a blocking human-verify checkpoint. The executor does NOT run anything here beyond confirming the automated gate is green (`bash tests/run-all-956.sh` exits 0) and then PAUSES for the human to run the cold-install acceptance gate (see how-to-verify). Resume only on the sign-off described in resume-signal. If the gate fails, do NOT proceed to Task 3; file the failure as a 95.6 gap.</action>
  <verify>
    <automated>bash /home/jsagi/MindrianOS-Plugin/tests/run-all-956.sh</automated>
  </verify>
  <what-built>
    Phase 95.6 Tier 1: install.sh skill-loop hardening (D-03), Windows long-path preflight + stale-clone cleanup (D-01), the 189-char phase-92 dir renamed (D-02), statusline registered first + /mos:doctor class H + .install-receipt.json + first-session auto-doctor (D-09), scripts/release.sh Step 9.5 npm publish gate (D-05a). The automated test surface (tests/run-all-956.sh) is green.
  </what-built>
  <how-to-verify>
    Run `tests/manual/95.6-windows-cold-install-acceptance.md` end to end on a fresh Windows 11 machine (Surface-class touch device preferred, with an external keyboard for the permission prompts), with ZERO AI hand-holding -- no Claude Code improvising missing symlinks or settings.json fragments:
    1. Fresh Windows 11, Git for Windows installed, no prior MindrianOS state.
    2. Run the canonical install path exactly as the install site documents it (CMD shell; note any PowerShell deviation).
    3. `git clone` completes with no MAX_PATH error and no manual `core.longpaths` intervention (D-01 + D-02).
    4. `install.sh` exits 0; if any skill dir is incomplete it prints a WARN and continues; agents, hooks, larry-extended default, MINDRIAN_OS_ROOT all registered (D-03).
    5. Open a fresh `claude` session: the statusline `(hexagon) MindrianOS-Plugin <room> <context%>` renders at the bottom within ~1s (D-09).
    6. `/mos:doctor --all` (or `--statusline-visibility`): all-green, OR it names exactly what is missing and `--fix` resolves it.
    7. Reach `/mos:onboard` -> Larry greets, under 5 minutes total, no maintainer help.
    8. After the release (Task 3) lands the npm publish: `npm view @mindrian/os@next version` returns `1.13.0-beta.9`; before that, confirm the install site does not advertise the npx path. (This step can be re-checked after Task 3; the cold-install gate itself, steps 1-7, must pass BEFORE Task 3.)
    Capture the `/mos:doctor` first-session output and attach it to the SUMMARY. Also: this run produces the source material for / validates the case #4 autopsy (`docs/autopsies/2026-05-09-gary-laben-install-failure.md`, written in 95.6-06).
    If the gate fails at any step: STOP. Do NOT cut the release. File the failure as a 95.6 gap (it would trigger a `--gaps` re-plan) -- a green CI run with a failed cold-machine gate means the phase is not done.
  </how-to-verify>
  <resume-signal>Type "approved -- cold install gate passed, /mos:doctor first-session output attached" (and attach it), or describe exactly which step failed so it can be filed as a gap.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Cut v1.13.0-beta.9 via scripts/release.sh (after the cold-install gate is signed off)</name>
  <files>.claude-plugin/plugin.json, package.json, CHANGELOG.md (the release commit), .planning/STATE.md</files>
  <read_first>
    - scripts/release.sh (the full 11-step + Step 9.5 sequence -- read it before running it; confirm Step 9.5 from 95.6-06 is present, confirm Step 5b from 95.6-07 is present if that landed)
    - CLAUDE.md (the MANDATORY release process -- the five gates + the npm lockstep rule + "release infra ships as beta first")
    - CHANGELOG.md (the `## [1.13.0-beta.9]` entry written in Task 1 -- `scripts/release.sh` Step 6 checks for it)
    - .planning/STATE.md (the milestone-position section to update after the ship)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-VALIDATION.md (the post-publish manual check: `npm view @mindrian/os@next version` returns the shipped version)
  </read_first>
  <action>
    PRECONDITION: Task 2 (the cold-install gate) must be signed off. Do NOT run this task before that approval.
    1. Run `scripts/release.sh` to cut `1.13.0-beta.9`. The script: bumps `plugin.json` + `package.json` to `1.13.0-beta.9`; bumps + validates `~/mindrian-marketplace/.claude-plugin/marketplace.json` (pins `source.ref: v1.13.0-beta.9`); checks the CHANGELOG entry exists (Task 1 wrote it); commits both repos (specific files only, never `git add -A`); pushes both; Step 9.5 runs `npm publish --tag next` for `@mindrian/os@1.13.0-beta.9`; updates the local marketplace cache; post-release verification. Pass whatever bump argument the script expects to produce `1.13.0-beta.9` from `1.13.0-beta.8` (read the script -- it may need a `prerelease`/`beta` mode or you may need to set the target version explicitly; if `scripts/release.sh` does not support pre-release bumps directly, that is a gap -- in that case bump `plugin.json` + `package.json` + CHANGELOG by hand to `1.13.0-beta.9`, then `git tag v1.13.0-beta.9`, push with `--tags`, and run JUST the Step 9.5 publish manually: `npm publish --tag next`; document this in the SUMMARY). If `~/mindrian-marketplace/` is not checked out locally, the marketplace.json bump is a MANUAL step -- note it in the SUMMARY with the exact target shape (`version: 1.13.0-beta.9`, `source.ref: v1.13.0-beta.9`).
    2. Verify the five gates + npm: `grep -q '"version": "1.13.0-beta.9"' .claude-plugin/plugin.json && grep -q '"version": "1.13.0-beta.9"' package.json && grep -q "1.13.0-beta.9" CHANGELOG.md && git tag -l v1.13.0-beta.9 (non-empty) && (cd ~/mindrian-marketplace 2>/dev/null && grep -q "v1.13.0-beta.9" .claude-plugin/marketplace.json || echo "marketplace.json is a manual step -- see SUMMARY")`. Then `npm view @mindrian/os@next version` -> should return `1.13.0-beta.9` (if the publish failed, the script halted with the recovery message -- follow it: `npm whoami`, ensure publish rights on `@mindrian`, re-run `npm publish --tag next`, re-verify).
    3. Update `.planning/STATE.md`: mark phase 95.6 as done / shipped at v1.13.0-beta.9; note the install-cache failure family case #4 is closed; update the milestone position. Keep STATE.md's existing structure.
    4. Note in the SUMMARY (as a follow-up, NOT a file edit in this repo): update the install site (`~/mindrianos-install-site/`) `npx @mindrian/os@next` block to advertise the now-working path (or remove it if the publish failed).
    Per CLAUDE.md: this is the ONLY way to release; never bump versions by hand unless `scripts/release.sh` genuinely cannot do a pre-release bump (then do it carefully + document). Release infra ships as beta first -- `1.13.0-beta.9` is correct.
  </action>
  <verify>
    <automated>grep -q '"version": "1.13.0-beta.9"' /home/jsagi/MindrianOS-Plugin/.claude-plugin/plugin.json && grep -q '"version": "1.13.0-beta.9"' /home/jsagi/MindrianOS-Plugin/package.json && git -C /home/jsagi/MindrianOS-Plugin tag -l v1.13.0-beta.9 | grep -q v1.13.0-beta.9
</automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/plugin.json` `version` is `1.13.0-beta.9`; `package.json` `version` is `1.13.0-beta.9` (the two in lockstep)
    - `git tag -l v1.13.0-beta.9` is non-empty (the tag points at the release commit)
    - `CHANGELOG.md` has the `## [1.13.0-beta.9]` entry (written in Task 1, committed in the release commit)
    - `~/mindrian-marketplace/.claude-plugin/marketplace.json` has `mos` at `1.13.0-beta.9` with `source.ref: v1.13.0-beta.9` -- OR the SUMMARY documents this as a manual step (if the marketplace repo is not locally checked out)
    - `npm view @mindrian/os@next version` returns `1.13.0-beta.9` -- OR the SUMMARY documents the publish recovery path that was followed (if Step 9.5's publish initially failed)
    - `.planning/STATE.md` reflects: phase 95.6 done, beta.9 shipped, install-cache failure family case #4 closed
    - No em-dashes in any diff: `git diff CHANGELOG.md .planning/STATE.md .claude-plugin/plugin.json package.json | grep "^+" | grep -c $'—'` returns 0
    - The install-site follow-up (update/remove the `npx @mindrian/os@next` block in `~/mindrianos-install-site/`) is noted in the SUMMARY (it is a separate repo; not a file edit here)
  </acceptance_criteria>
  <done>
    v1.13.0-beta.9 is cut: CHANGELOG entry + plugin.json + package.json bumped + git tag v1.13.0-beta.9 + marketplace.json ref pinned + `@mindrian/os@1.13.0-beta.9` published to npm at `@next` (via scripts/release.sh Step 9.5). STATE.md updated. The install-cache failure family case #4 is closed. The install-site update is the only remaining follow-up (separate repo).
  </done>
</task>

</tasks>

<verification>
After all three tasks (in order; Task 3 is gated on Task 2's signed-off checkpoint):
1. `bash tests/run-all-956.sh` -> exits 0 (the precondition for Task 1 -> Task 3).
2. The cold-install manual gate (`tests/manual/95.6-windows-cold-install-acceptance.md`) is signed off by a real Windows tester (Task 2 checkpoint approved).
3. `grep -q '"version": "1.13.0-beta.9"' .claude-plugin/plugin.json package.json` -> succeeds; `git tag -l v1.13.0-beta.9` -> non-empty; `grep -q "1.13.0-beta.9" CHANGELOG.md` -> succeeds.
4. `npm view @mindrian/os@next version` -> `1.13.0-beta.9` (or the SUMMARY documents the recovery path).
5. `~/mindrian-marketplace/.claude-plugin/marketplace.json` -> `mos` at `1.13.0-beta.9`, `ref: v1.13.0-beta.9` (or noted as a manual step).
6. `.planning/STATE.md` -> phase 95.6 done, beta.9 shipped.
7. No em-dashes in any diff.

Files touched: `CHANGELOG.md`, `.claude-plugin/plugin.json`, `package.json` (all via the release process), `.planning/STATE.md`. The release also touches `~/mindrian-marketplace/.claude-plugin/marketplace.json` (separate repo, via `scripts/release.sh` or manually). `CHANGELOG.md` overlaps with 95.6-06 (npm-gap note) -- Task 1 merges them. `.claude-plugin/plugin.json` overlaps with 95.6-09 (license confirm -- disjoint field) and 95.6-07 (no overlap). `package.json` overlaps with 95.6-06 (name/private -- disjoint field) and 95.6-07 (bin -- disjoint field). This plan is Wave 3 (after the Tier 1 plans 95.6-01/02/03/04/05/06); it depends on those, NOT on the Tier 2/3 plans (95.6-07/08/09) -- those fold into beta.10 if not done by ship time.
</verification>

<success_criteria>
- [ ] `bash tests/run-all-956.sh` exits 0 (all 8 Phase 95.6 suites pass) -- the precondition gate
- [ ] The Windows cold-install manual gate is run by a real Windows tester and signed off (BLOCKING checkpoint) before the release commit
- [ ] `## [1.13.0-beta.9]` CHANGELOG entry covers D-02/D-03/D-01/D-09/D-05a + the npm-gap note (folded in once, not duplicated)
- [ ] `plugin.json` + `package.json` bumped to `1.13.0-beta.9`; `git tag v1.13.0-beta.9` exists; `~/mindrian-marketplace/marketplace.json` has `mos` at `1.13.0-beta.9` with `source.ref: v1.13.0-beta.9` (or documented as a manual step)
- [ ] `@mindrian/os@1.13.0-beta.9` published to npm at `@next` (via `scripts/release.sh` Step 9.5); `npm view @mindrian/os@next version` returns `1.13.0-beta.9` (or the recovery path is documented)
- [ ] `.planning/STATE.md` updated: phase 95.6 done, beta.9 shipped, install-cache failure family case #4 closed
- [ ] The install-site `npx @mindrian/os@next` follow-up is noted (separate repo)
- [ ] No emoji, no em-dashes; the release ran through `scripts/release.sh` (not by-hand bumps) unless the script genuinely cannot do a pre-release bump (then documented); release infra shipped as beta (`1.13.0-beta.9`)
</success_criteria>

<output>
After completion, create `.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-10-SUMMARY.md` documenting:
- The `bash tests/run-all-956.sh` result (all 8 PASS)
- The cold-install gate sign-off: who ran it, which Windows machine, the `/mos:doctor` first-session output (attach it), any deviations noted
- The CHANGELOG entry (the decisions covered; whether 95.6-07/08/09 lines were folded in or deferred to beta.10)
- The release: the `scripts/release.sh` invocation (or the by-hand bump if the script could not do a pre-release bump, with the exact steps taken); the five gates' final state; the marketplace.json edit (in-repo via the script, or manual)
- `npm view @mindrian/os@next version` output (and the publish recovery path if Step 9.5 failed)
- The STATE.md update
- The install-site follow-up (the exact change needed in `~/mindrianos-install-site/`)
- Confirmation the install-cache failure family case #4 is closed; pointer to the autopsy (`docs/autopsies/2026-05-09-gary-laben-install-failure.md`)
</output>
