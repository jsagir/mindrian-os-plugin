---
phase: 95.6-install-cache-windows-hardening-and-skill-loop-resilience
plan: 01
subsystem: planning-hygiene
tags: [install, windows, max-path, phase-rename, d-02, dog-fooding]

# Dependency graph
requires:
  - phase: 95.6-CONTEXT
    provides: D-02 spec + REC-11 (descriptive form lives in a body Searchability Note, not frontmatter)
  - phase: 95.6-02
    provides: tests/test-92-rename-no-long-leaves.cjs (Wave 0 scaffold; SKIPped until this plan ran)
provides:
  - ".planning/phases/92-trust-layer-refactor/ -- renamed phase-92 directory (was a 189-char leaf that broke Windows MAX_PATH on git clone); git history preserved via git mv (R100)"
  - ".planning/phases/92-trust-layer-refactor/92-CONTEXT.md -- new CONTEXT carrying the original 189-char descriptive name as a `## Searchability Note` body section (NOT a frontmatter field, per REC-11) + slug: 92-trust-layer-refactor"
  - "tests/test-92-rename-no-long-leaves.cjs now GREEN (Check 1/2/3 all pass) -- Rule-1 bug fix made it actually runnable"
affects: [95.6-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "git mv (not plain mv) for phase-directory renames so git tracks R100 and `git log --follow` keeps history"
    - "When a long-but-descriptive name is shortened for OS-path-length hygiene, preserve the original as a `## Searchability Note` body section in the directory's CONTEXT.md (one-off; not a `full_slug:` frontmatter precedent -- REC-11)"
    - "Repo-scan test self-reference avoidance: assemble the grep needle from string fragments so the test file does not match its own search"

key-files:
  created:
    - .planning/phases/92-trust-layer-refactor/92-CONTEXT.md
  modified:
    - tests/test-92-rename-no-long-leaves.cjs
    - .planning/milestones/v1.11.0-MILESTONE-AUDIT.md
    - .planning/v1.11.0-MILESTONE-AUDIT.md
  renamed:
    - ".planning/phases/92-refactor-constitution-and-trust-layer-formalizes-audit-driven-refactor-work-constitution-v1-1-directive-1-validation-directive-2-consolidation-directive-3-unidirectional-flow-trust-layer/ -> .planning/phases/92-trust-layer-refactor/ (.gitkeep + SKIP-NOTE.md, R100)"

key-decisions:
  - "Phase-92 directory had only .gitkeep + SKIP-NOTE.md (it is a skipped, unscoped placeholder) -- no pre-existing CONTEXT.md. Created 92-CONTEXT.md as the canonical home for the `## Searchability Note` (the search target the plan + the rename test expect at .../92-trust-layer-refactor/*.md)."
  - "No `full_slug:` frontmatter field added anywhere (REC-11). slug: 92-trust-layer-refactor lives in the new 92-CONTEXT.md frontmatter; SKIP-NOTE.md has no slug field so it needed no change."
  - "FEEDBACK.md, docs/autopsies/2026-05-09-gary-laben-install-failure.md, .planning/STATE.md line 273 (bug-narrative log entry), and the 95.6 phase artifacts (95.6-CONTEXT/RESEARCH/01-PLAN/02-PLAN) all quote the old 189-char name as a verbatim historical / spec record -- left as-is per the plan (a quote of what broke is not a dangling link)."
  - "tests/test-92-rename-no-long-leaves.cjs (shipped by 95.6-02) could never go GREEN: its Check-2 `git grep` matched its own source AND every file that quotes the old name. Rule-1 fix: needle assembled from fragments so the test does not self-match; Check-2 whitelist widened to the historical-record files + the 95.6 phase directory."

patterns-established:
  - "Phase-rename hygiene checklist: git mv; shorten slug in the directory's CONTEXT.md; add `## Searchability Note` body section with the old name + keywords + date + reason; repoint identifier-style refs (e.g. `phase: <slug>` audit fields); leave verbatim historical quotes (tester transcripts, autopsies, frozen log entries) alone."

requirements-completed: [D-02]

# Metrics
duration: ~20min
completed: 2026-05-11
---

# Phase 95.6 Plan 01: Rename 92-trust-layer-refactor (D-02) Summary

Renamed the 189-char phase-92 directory leaf to `.planning/phases/92-trust-layer-refactor/` via `git mv` (history preserved, R100), removing the Windows MAX_PATH (260-char) failure class that broke Gary Laben's `git clone` twice during the 2026-05-08 live install call. The original descriptive name is preserved as a `## Searchability Note` body section in a new `92-CONTEXT.md` (not a frontmatter field, per REC-11). All identifier-style references repointed; verbatim historical quotes left intact; the Wave-0 rename test is now GREEN.

## What shipped

### Task 1 -- rename + Searchability Note (commit `be2046c`)

- `git mv ".planning/phases/92-refactor-constitution-and-trust-layer-formalizes-audit-driven-refactor-work-constitution-v1-1-directive-1-validation-directive-2-consolidation-directive-3-unidirectional-flow-trust-layer" ".planning/phases/92-trust-layer-refactor"` -- both files in the directory (`.gitkeep`, `SKIP-NOTE.md`) moved as R100 renames; `git log --follow` confirms history continuity past the rename.
- Created `.planning/phases/92-trust-layer-refactor/92-CONTEXT.md` with:
  - frontmatter `slug: 92-trust-layer-refactor` (the short form; **no `full_slug:` field** -- REC-11);
  - a `## Searchability Note` body section containing the original 189-char name verbatim, the rename date (2026-05-10 per the plan), the reason (Windows MAX_PATH), a pointer to the autopsy, and the descriptive keyword fragments ("refactor-constitution", "trust-layer-formalizes-audit", "directive-1-validation", "directive-2-consolidation", "directive-3-unidirectional-flow") so future grep still resolves to this phase;
  - a `## Status` section restating that phase 92 is a skipped, unscoped placeholder (cross-refs `SKIP-NOTE.md`), a `## Canon Parts` section (Part 6 + Part 7), and the `## Resume Path` (`/gsd:discuss-phase 92` ...).
- The directory previously held **only** `.gitkeep` + `SKIP-NOTE.md` (no pre-existing CONTEXT.md), so creating `92-CONTEXT.md` was the cleanest place for the Searchability Note rather than wedging it into `SKIP-NOTE.md`. `SKIP-NOTE.md` has no `slug:` frontmatter field, so it required no edit.

### Task 2 -- repoint references + make the rename test GREEN (commit `0dd37ea`)

- **Identifier-style references repointed:** `.planning/milestones/v1.11.0-MILESTONE-AUDIT.md` and its identical copy `.planning/v1.11.0-MILESTONE-AUDIT.md` both had `phase: 92-refactor-constitution-and-trust-layer` (a truncated slug used as a lookup key) -> changed to `phase: 92-trust-layer-refactor`.
- **`tests/test-92-rename-no-long-leaves.cjs` (Rule-1 bug fix):** the test shipped by 95.6-02 could never reach GREEN -- its Check-2 `git grep -l "92-refactor-constitution-and-trust-layer"` matched (a) its own `OLD_92_LEAF` constant + header comment and (b) every file that legitimately quotes the old name (the autopsy, `STATE.md` line 273, the 95.6 plan/context/research). Fixed by: assembling `OLD_92_NEEDLE` / `OLD_92_LEAF` from string fragments so the test source no longer contains the contiguous token it searches for; widening the Check-2 whitelist to the historical-record set (`docs/testers/gary-laben/FEEDBACK.md`, `docs/autopsies/2026-05-09-gary-laben-install-failure.md`, `.planning/STATE.md`, the test file itself) plus a prefix match for `.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/`; and clarifying the header comment to define "dangling reference" as "uses the old name as a live path you would follow", which a bug-autopsy quote is not.

## Verification

- `node tests/test-92-rename-no-long-leaves.cjs` -> **GREEN** (`All 3 check(s) PASS`): Check 1 (no `92-*` leaf > 60 chars; exactly one `92-*` leaf and it is `92-trust-layer-refactor`), Check 2 (no non-whitelisted file references the old path), Check 3 (`git log --follow` on the renamed dir is non-empty -- rename history preserved). Exit 0.
- `bash tests/run-all-956.sh` -> 2/8 PASS (`test-skills-have-skill-md.cjs` from 95.6-02 + `test-92-rename-no-long-leaves.cjs` from this plan); the other 6 are RED-by-design (owning plans 95.6-03/04/05/06 not yet landed). This matches the expected Wave-0/Wave-1 state documented in the runner header and `95.6-VALIDATION.md`.
- Long-leaf check `find .planning/phases -mindepth 1 -maxdepth 1 -type d | awk -F/ '{print length($NF), $NF}' | sort -rn | head -3` -> `80 03.1-data-room-dashboard-localhost-de-stijl-viewer-with-knowledge-graph-and-chat`, `73 93-v1-11-1-hotfix-install-cache-drift-recovery-brain-telemetry-visibility`, `62 95.6-install-cache-windows-hardening-and-skill-loop-resilience`. The 189-char line is gone. New global champion leaf: **`03.1-data-room-dashboard-localhost-de-stijl-viewer-with-knowledge-graph-and-chat` at 80 chars** -- explicitly out of 95.6 scope (D-02 only locks the 92 rename); filed as a future hygiene candidate (the plan's `<verification>` already notes 80 chars + a typical home path stays under MAX_PATH).
- `git -c diff.renames=true status` / `git diff --cached --name-status -M` on the Task-1 commit confirms both directory files moved as `R100` (rename, 100% similarity), so `git blame` / `git log --follow` history is intact.
- Cross-reference grep `git grep -l "92-refactor-constitution-and-trust-layer"` post-change returns only: `docs/testers/gary-laben/FEEDBACK.md` (whitelisted), `docs/autopsies/2026-05-09-gary-laben-install-failure.md`, `.planning/STATE.md`, and the four 95.6 phase artifacts (`95.6-01-PLAN.md`, `95.6-02-PLAN.md`, `95.6-CONTEXT.md`, `95.6-RESEARCH.md`). Zero genuine dangling path references. The test file itself no longer matches.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] No pre-existing 92-CONTEXT.md to edit**
- **Found during:** Task 1.
- **Issue:** The plan's Task 1 step 3 assumed a CONTEXT.md exists inside the directory ("locate the CONTEXT.md inside the renamed directory ... likely `92-CONTEXT.md`"). It does not -- the directory only ever held `.gitkeep` + `SKIP-NOTE.md` (phase 92 is a skipped, unscoped placeholder).
- **Fix:** Created `.planning/phases/92-trust-layer-refactor/92-CONTEXT.md` with the `slug: 92-trust-layer-refactor` frontmatter, the `## Searchability Note` body section (the canonical search target the plan + the rename test's acceptance criteria reference via `.../92-trust-layer-refactor/*.md`), plus Status / Canon Parts / Resume Path sections that mirror `SKIP-NOTE.md`. No `full_slug:` frontmatter (REC-11). `SKIP-NOTE.md` was left unchanged (no `slug:` field present).
- **Files modified:** `.planning/phases/92-trust-layer-refactor/92-CONTEXT.md` (new).
- **Commit:** `be2046c`.

**2. [Rule 1 - Bug] `tests/test-92-rename-no-long-leaves.cjs` could never reach GREEN**
- **Found during:** Task 2 (and the plan's success criterion + this execution's stated goal: "`node tests/test-92-rename-no-long-leaves.cjs` is GREEN after the rename").
- **Issue:** The test (shipped by 95.6-02) runs `git grep -l "92-refactor-constitution-and-trust-layer"` and asserts the only hit is `docs/testers/gary-laben/FEEDBACK.md`. But the test's own source contains that token (`OLD_92_LEAF` constant + header comment), and so do the install-failure autopsy, `STATE.md` line 273, and the 95.6 plan/context/research artifacts -- all legitimate historical/spec records of the old name, none of them dangling links. So even a perfect rename leaves the test RED.
- **Fix:** (a) assemble the search needle from string fragments so the test file no longer contains the contiguous token it greps for; (b) widen the Check-2 whitelist to the historical-record files + a prefix match for the 95.6 phase directory; (c) clarify the header comment's definition of "dangling reference". Test now GREEN (3/3).
- **Files modified:** `tests/test-92-rename-no-long-leaves.cjs`.
- **Commit:** `0dd37ea`.

### Intentionally NOT changed (per plan)

- `docs/testers/gary-laben/FEEDBACK.md` -- verbatim quote of what Gary's git client said on 2026-05-08; the plan is explicit it stays as-is (whitelisted).
- `docs/autopsies/2026-05-09-gary-laben-install-failure.md` -- the autopsy's root-cause statement ("`.planning/phases/92-...-trust-layer/` has a 189-character leaf name ... exceeds Windows MAX_PATH") is a precise historical record; rewriting it to the new 23-char name would falsify it.
- `.planning/STATE.md` line 273 -- a frozen log entry describing what Bug 1 was; same logic as the autopsy. STATE.md was otherwise updated via `state advance-plan` / `update-progress` at plan close.
- `.planning/phases/95.6-.../95.6-01-PLAN.md`, `95.6-02-PLAN.md`, `95.6-CONTEXT.md`, `95.6-RESEARCH.md` -- these quote the old name as the spec of the rename; they are this phase's own planning artifacts and the quote is intentional (and whitelisted in the test).

## Commits

- `be2046c` -- `chore(95.6-01): rename phase 92 dir to 92-trust-layer-refactor (D-02)` (git mv R100 of `.gitkeep` + `SKIP-NOTE.md`; new `92-CONTEXT.md` with `## Searchability Note`)
- `0dd37ea` -- `fix(95.6-01): repoint phase-92 refs to new short slug; make rename test GREEN` (both v1.11.0 milestone-audit copies; Rule-1 fix of `tests/test-92-rename-no-long-leaves.cjs`)
- (plus the docs metadata commit closing this plan: SUMMARY.md + STATE.md + ROADMAP.md)

## Self-Check: PASSED

- FOUND: `.planning/phases/92-trust-layer-refactor/92-CONTEXT.md`
- FOUND: `.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-01-SUMMARY.md`
- FOUND: `.planning/phases/92-trust-layer-refactor/` (directory); old 189-char directory confirmed GONE
- FOUND: commit `be2046c` (Task 1: rename + Searchability Note)
- FOUND: commit `0dd37ea` (Task 2: repoint refs + test fix)
- `node tests/test-92-rename-no-long-leaves.cjs` exits 0, all 3 checks PASS
