---
phase: 95.6
plan: 10
status: partial
reason: "Tasks 1 + 2 (waived) + Task 3 complete EXCEPT the npm publish (needs `npm login` -- no npm auth in env). Pushed to GitHub (main + tag v1.13.0-beta.9) and the marketplace.json ref is pinned + pushed on 2026-05-11. Remaining: `npm login && npm publish --tag next` from MindrianOS-Plugin/, then `gsd-tools phase complete 95.6`."
date: 2026-05-11
---

# 95.6-10 SUMMARY -- Tier 1 Release Gate (v1.13.0-beta.9)

## Status: PARTIAL -- shipped to GitHub + marketplace 2026-05-11; npm publish pending `npm login`

Tasks 1 and 3 are done EXCEPT the npm publish step. Task 2 (the BLOCKING Windows cold-install human-verify checkpoint) was **waived by the maintainer** on 2026-05-11. On 2026-05-11 (maintainer instruction "I want this pushed across all install types") the release was pushed:
- `git push origin main --tags` -> `origin/main` at `60fe434`, tag `v1.13.0-beta.9` (-> `9ed8280`) on GitHub. DONE.
- `~/mindrian-marketplace/.claude-plugin/marketplace.json`: `mos` bumped to `1.13.0-beta.9`, `source.ref` pinned to `v1.13.0-beta.9`; committed (`a41964f`) and pushed to `origin/master`. DONE.
- `npm publish --tag next`: **NOT done** -- `npm whoami` -> ENEEDAUTH (no `~/.npmrc`, no token, can't do interactive `npm login` from this environment). `npm publish --dry-run --tag next` confirms the package is publishable: `@mindrian/os@1.13.0-beta.9`, 590 files, 1.8 MB packed / 6.3 MB unpacked, sha `f43ce6ff...`. The maintainer must run `npm login` then `npm publish --tag next` from `/home/jsagi/MindrianOS-Plugin/`.

**Install paths now live:** `claude plugin install mos@mindrian-marketplace --version 1.13.0-beta.9` (and `claude plugin update ... --version 1.13.0-beta.9` for existing users) -- WORKS (marketplace -> GitHub tag). Direct `install.sh` from the `v1.13.0-beta.9` tag or `main` -- WORKS. `npx @mindrian/os@next` / `npm i -g @mindrian/os@next` -- NOT YET (waiting on the npm publish). The ZIP/URL + CI-Docker pre-bake paths are documented (`docs/install/PACKAGING-PATHS.md`) but the `npm run pack-zip` build step + the Dockerfile are future deliverables (per 95.6-08).

## Task 1 -- pre-release gate + CHANGELOG entry: DONE

- `bash tests/run-all-956.sh` -> **8/8 PASS** (Total 8, Passed 8, Failed 0). All Phase 95.6 suites green: `test-skills-have-skill-md.cjs`, `test-92-rename-no-long-leaves.cjs`, `test-install-sh-skill-loop.sh`, `test-install-preflight-longpath.sh`, `test-doctor-class-h.cjs`, `test-doctor-class-h-fix.cjs`, `test-install-receipt.cjs`, `test-release-npm-gate.sh`.
- Syntax sanity: `bash -n install.sh` OK, `node -c scripts/doctor.cjs` OK, `bash -n scripts/release.sh` OK.
- `CHANGELOG.md`: the `## [Unreleased]` block (Step 9.5 npm gate entry + case #4 autopsy entry + npm-gap note) was folded into a dated `## [1.13.0-beta.9] - 2026-05-11` entry, and the rest of the Tier 1 + Tier 2/3 work was added: D-01 long-path preflight, D-02 phase-92 rename, D-03 skill-loop hardening + mullins-scaffold backfill, D-09 statusline-first + class H + receipt + first-session auto-doctor, D-05a Step 9.5, D-11a Step 5b reserved-names gate, D-05c bin/cli.js, D-05d sessionstart-npm-reconcile hook, D-10 subagent parity, D-11b Deferred Tool Loading note, D-05e/f PACKAGING-PATHS.md, D-04 README Manual Recovery section, D-07 first-touch-drift scanner, D-06 BSL-1.1 source-available correction, plus the test1_enumCount floor fix. The npm-gap note (beta.1..beta.8 never published; first npm-published version is beta.9) is in the entry's Notes exactly once. A Note records the maintainer's waiver of the Windows cold-install checkpoint. A Note lists the separate-repo follow-ups. No em-dashes. Committed: `577a668 docs(95.6-10): write [1.13.0-beta.9] CHANGELOG entry (Task 1; test gate 8/8 green)`.
- Versions NOT bumped in Task 1 (that is Task 3).

## Task 2 -- Windows cold-install acceptance gate: WAIVED by maintainer

`tests/manual/95.6-windows-cold-install-acceptance.md` (the 8-step cold-machine acceptance gate, created by 95.6-02) was NOT run by an external Windows tester. On 2026-05-11 the maintainer waived the blocking checkpoint ("I will not reach out to the Wave-2 tester to try; we will do it anyway") to ship the Tier 1 hotfix subset on the maintainer's stated 2026-05-11 commitment / ahead of the NATO Defense College Rome 2026-06-01 hard deadline.

Consequence: v1.13.0-beta.9 ships **unverified on real Windows**. Per the project beta-first policy this is acceptable for a beta (opt-in only -- only users who run `claude plugin update mos@mindrian-marketplace --version 1.13.0-beta.9` get it). The beta IS the test artifact. **Promotion to a clean `1.13.0` (no suffix) should still wait on a Windows cold-install confirmation from some Windows tester** (the Wave-2 tester or a Wave-3 equivalent). The 8-step gate doc remains the acceptance contract for that promotion. No `/mos:doctor` first-session output is attached because the gate was not run.

## Task 3 -- cut v1.13.0-beta.9: LOCAL PART DONE, push/publish/pin PENDING

`scripts/release.sh` only takes `[patch|minor|major]` -- it has no path that produces `1.13.0-beta.9` from `1.13.0-beta.8` (a `patch` bump would parse `0-beta` as the patch segment and drop the suffix toward `1.13.1`). Per the plan's contingency, the version bump was done by hand:

- `.claude-plugin/plugin.json` `version`: `1.13.0-beta.8` -> `1.13.0-beta.9` (version line only; JSON re-parses).
- `package.json` `version`: `1.13.0-beta.8` -> `1.13.0-beta.9` (version line only; JSON re-parses; `name` stays `@mindrian/os`, `files` allowlist + `bin` field untouched).
- Committed: `9ed8280 release: v1.13.0-beta.9 -- install-cache Windows hardening + skill-loop resilience (Phase 95.6 Tier 1)`.
- Tag created: `git tag v1.13.0-beta.9` (points at `9ed8280`; the CHANGELOG entry is one commit earlier at `577a668`).
- **DONE 2026-05-11 (maintainer instruction "I want this pushed across all install types"):**
  1. `git push origin main --tags` -- pushed 39 commits (the 2 graph-on-graph P0 fixes + 33 from this session's 95.6 work + the test1_enumCount fix + the CHANGELOG commit + the release commit + the docs commit) and the `v1.13.0-beta.9` tag. `origin/main` = `60fe434`; `refs/tags/v1.13.0-beta.9` -> `9ed8280` on GitHub. Verified via `git ls-remote origin`.
  2. `~/mindrian-marketplace/.claude-plugin/marketplace.json`: `mos` bumped to `1.13.0-beta.9`, `source.ref` pinned to `v1.13.0-beta.9`; committed (`a41964f mos: ref-pin v1.13.0-beta.8 -> v1.13.0-beta.9 (Phase 95.6 install-cache Windows hardening)`) and pushed to `origin/master`. Done AFTER the tag was on GitHub. The `description` field was left as-is (slightly stale "73 commands ... 13 hooks" -- not updated in this pass).
  - Pre-publish payload review: `npm pack --dry-run` -> tarball clean, no `.planning/` / `docs/` / `mcp-server-brain/` / `tests/` / `release.sh` (the `files` allowlist works). 590 files, 1.8 MB packed / 6.3 MB unpacked, sha `f43ce6ff...`. `CHANGELOG.md` is ~317 kB in the tarball (long history; bloat-not-leak; could be trimmed in a future release). `bin/cli.js` mentions "Brain" only as setup instructions, not credentials.
- **STILL PENDING (needs `npm login`):**
  - `npm publish --tag next` from `/home/jsagi/MindrianOS-Plugin/` -- publishes `@mindrian/os@1.13.0-beta.9` at the `@next` dist-tag. `npm whoami` -> ENEEDAUTH in this environment (no `~/.npmrc`, no token, no interactive login available). The maintainer must `npm login` (with publish rights on the `@mindrian` scope), then `npm publish --tag next`, then verify `npm view @mindrian/os@next version` returns `1.13.0-beta.9`. `npm publish --dry-run --tag next` succeeded, so the only blocker is auth.
- `npm view @mindrian/os@next version` -> currently `E404` (not yet on npm; beta.9 will be the first npm publish for `@mindrian/os` once the maintainer runs the publish).
- After the npm publish lands: `gsd-tools phase complete 95.6` (the gsd-verifier checks the npm-publish must_have); update `~/mindrianos-install-site/` to advertise `npx @mindrian/os@next`.
- `.planning/STATE.md` updated: see below.

## STATE.md update

Phase 95.6: all 10 plans complete. v1.13.0-beta.9 SHIPPED to GitHub + marketplace 2026-05-11: `origin/main` at `60fe434`, tag `v1.13.0-beta.9` -> `9ed8280`, CHANGELOG `[1.13.0-beta.9]` at `577a668`, `~/mindrian-marketplace` `master` at `a41964f` with `mos` at `1.13.0-beta.9` / `source.ref: v1.13.0-beta.9`. Windows cold-install blocking checkpoint waived by maintainer 2026-05-11. STILL PENDING: `npm publish --tag next` (needs `npm login` -- no npm auth in env), then `gsd-tools phase complete 95.6`, then update `~/mindrianos-install-site/` for `npx @mindrian/os@next`. Install-cache failure family case #4: fixes shipped via the marketplace/install.sh paths; the npm path goes live with the publish.

## Follow-ups (separate repos -- NOT file edits in this repo)

- `~/mindrianos-install-site/`: update the `npx @mindrian/os@next` block to advertise the now-working path once the npm publish lands (or remove it if publish is deferred).
- `~/mindrianos-install-site/` and the gitignored `docs/testers/outbox/2026-05-07-gary-laben-welcome.md` still carry "open source" adjacent to "BSL-1.1" -- manual sweep needed (D-06 cleaned the shipped surfaces; these two are out of this repo's tarball).

## Pointers

- Case #4 autopsy: `[private case archive]` (written/augmented in 95.6-06).
- Cold-install acceptance gate: `tests/manual/95.6-windows-cold-install-acceptance.md` (the contract for promoting beta.9 -> 1.13.0).
- Scoped test runner: `tests/run-all-956.sh` (8 suites; 8/8 green).
