---
phase: 95.6-install-cache-windows-hardening-and-skill-loop-resilience
plan: 02
subsystem: testing
tags: [install, windows, skill-loop, doctor, release-gate, hermetic-fixtures, manual-gate, nyquist]

# Dependency graph
requires:
  - phase: 95.6-CONTEXT
    provides: D-01..D-11 decisions + Priority Hierarchy (Tier 1 = D-02 + D-03 + D-01 + D-05a + D-09)
  - phase: 95.6-VALIDATION
    provides: per-decision verification map + Wave 0 requirements list
provides:
  - skills/mullins-scaffold/SKILL.md backfilled (the missing data file that aborted Gary Laben's install.sh skill-loop, D-03 root cause)
  - 7 Wave 0 test files (3 shell fixtures, 4 CJS suites); 6 RED-by-design until their owning Tier 1 plan lands, 1 GREEN-or-SKIP
  - tests/manual/95.6-windows-cold-install-acceptance.md (the empirical Dimension-8 cold-machine gate; authoritative over the automated suites)
  - tests/run-all.sh extended to aggregate test-*.cjs suites; tests/run-all-956.sh scoped 95.6 runner added
affects: [95.6-03, 95.6-04, 95.6-05, 95.6-06, 95.6-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Hermetic CJS doctor-class test via fs.mkdtempSync scratch HOME + USERPROFILE + MINDRIAN_STATUSLINE_SURFACE=CLI (mirrors tests/test-doctor-class-g.cjs)"
    - "Hermetic CJS install-receipt test via MINDRIAN_PLUGIN_HOME scratch ~/.claude/plugins tree (mirrors tests/test-doctor-atomic-swap.cjs)"
    - "Hermetic shell install.sh fixture: scratch repo placed at $HOME/.claude/plugins/mindrian-os + fake ~/.claude + PATH-shimmed git/npm + MOS_TEST_FORCE_OS injection"
    - "RED-by-design Wave 0 tests: each names its owning plan + RED/GREEN expectation in the header; runs to completion even when the assertion fails"
    - "Scoped phase runner pattern: tests/run-all-956.sh runs only the phase-relevant suites with the same TOTAL/PASSED/FAILED accounting"

key-files:
  created:
    - skills/mullins-scaffold/SKILL.md
    - tests/test-skills-have-skill-md.cjs
    - tests/test-doctor-class-h.cjs
    - tests/test-doctor-class-h-fix.cjs
    - tests/test-install-receipt.cjs
    - tests/test-92-rename-no-long-leaves.cjs
    - tests/test-install-sh-skill-loop.sh
    - tests/test-install-preflight-longpath.sh
    - tests/test-release-npm-gate.sh
    - tests/manual/95.6-windows-cold-install-acceptance.md
    - tests/run-all-956.sh
  modified:
    - tests/run-all.sh

key-decisions:
  - "skills/mullins-scaffold/SKILL.md indexes the existing scaffold.json (Mullins Seven Domains) -- JSON is source of truth, SKILL.md is the human/agent index; no activation:/paths: (passive data-and-index skill)"
  - "Class-H doctor key name kept flexible (accepts install-incomplete / class-h / classH / install-receipt) so 95.6-05 can slot it into the taxonomy without renaming the test"
  - "Canonical .install-receipt.json step list documented in tests/test-install-receipt.cjs so 95.6-03/05 implement install.sh to match: preflight, clone, register_statusline, register_commands, register_skills, register_agents, configure_hooks"
  - "test-92-rename-no-long-leaves.cjs SKIPs gracefully (exit 0) when 95.6-01 has not run -- it must never block Wave 0; release plan 95.6-10 re-runs it GREEN"

patterns-established:
  - "Wave 0 RED-by-design test headers: name the owning plan + the GREEN-when condition; the RED state is the contract, not a bug"
  - "Shell install.sh fixtures stub git + npm on PATH so they stay offline and the scratch .git does not need to be a real repo"

requirements-completed: [D-03]

# Metrics
duration: ~75min
completed: 2026-05-11
---

# Phase 95.6 Plan 02: Wave 0 Scaffold Summary

**Backfilled the missing skills/mullins-scaffold/SKILL.md (the file whose absence aborted Gary Laben's install.sh skill-loop), wrote the 7 missing Wave 0 test files (3 shell fixtures + 4 CJS suites, 6 RED-by-design until their owning Tier 1 plan lands), authored the 8-step cold-machine Windows acceptance gate, and wired tests/run-all.sh to aggregate the CJS suites plus a scoped tests/run-all-956.sh runner -- the Nyquist coverage surface that 95.6-03..06 and 95.6-10 depend on.**

## Performance

- **Duration:** ~75 min
- **Started:** 2026-05-11T16:00Z (approx)
- **Completed:** 2026-05-11T17:15Z
- **Tasks:** 3
- **Files modified:** 12 (11 created, 1 modified)

## Accomplishments

- **D-03 data bug fixed.** `skills/mullins-scaffold/SKILL.md` now exists (61 lines, valid `name: mullins-scaffold` frontmatter, references `scaffold.json`). This is the file whose absence aborted Gary Laben's install on 2026-05-08/09 under `set -euo pipefail`. The *contract* fix (install.sh pre-filter) is 95.6-03; this plan fixed the data.
- **Release-gate data-integrity test is GREEN.** `tests/test-skills-have-skill-md.cjs` enumerates `skills/*/` and asserts every directory has a `SKILL.md`. It reports `PASS: all 10 skills have SKILL.md`. (Note: the plan estimated "11"; the actual count is 10 -- `mullins-scaffold` was one of the 10 existing directories, and the other 9 already carried SKILL.md.)
- **Six RED-by-design Wave 0 test scaffolds created**, each with a header naming its owning plan and the GREEN-when condition:
  - `tests/test-install-sh-skill-loop.sh` -> GREEN when **95.6-03** lands the skill-loop pre-filter (+ 95.6-05 register_statusline first)
  - `tests/test-install-preflight-longpath.sh` -> GREEN when **95.6-04** lands the Windows long-path preflight
  - `tests/test-doctor-class-h.cjs` -> GREEN when **95.6-05** lands doctor class H
  - `tests/test-doctor-class-h-fix.cjs` -> GREEN when **95.6-05** lands doctor class H `--fix` (idempotent)
  - `tests/test-install-receipt.cjs` -> GREEN when **95.6-05** lands `.install-receipt.json` + the doctor reader (R-01)
  - `tests/test-release-npm-gate.sh` -> GREEN when **95.6-06** lands `scripts/release.sh` Step 9.5
- **One GREEN-or-SKIP test:** `tests/test-92-rename-no-long-leaves.cjs` (D-02). It SKIPs (exit 0) until 95.6-01 has run, then asserts no `92-*` phase leaf exceeds 60 chars + no dangling old-path refs + rename history preserved. It never blocks Wave 0.
- **Cold-machine manual gate** at `tests/manual/95.6-windows-cold-install-acceptance.md`: the 8-step checklist copied from `95.6-RESEARCH.md`, marked "authoritative over the automated suites", with sign-off, capture/attach, and a pointer to the case #4 autopsy.
- **Runner wiring:** `tests/run-all.sh` now runs the `test-*.cjs` suites in the same TOTAL/PASSED/FAILED accounting; `tests/run-all-956.sh` is a thin scoped runner for the 8 phase-relevant suites.

## Task Commits

Each task was committed atomically (normal commits, pre-commit hook ran):

1. **Task 1: Backfill mullins-scaffold SKILL.md + write data-integrity test** - `6dafc4c` (feat)
2. **Task 2: Four CJS Wave 0 test scaffolds (class H, class H fix, receipt, 92-rename)** - `7ec5385` (test)
3. **Task 3: Three shell fixtures + cold-machine manual gate + wire run-all.sh + run-all-956.sh** - `7e6cb57` (test)

**Plan metadata commit:** (this SUMMARY + STATE.md + ROADMAP.md) -- see final commit.

## Files Created/Modified

- `skills/mullins-scaffold/SKILL.md` - NEW. The missing data file; indexes `scaffold.json` (Mullins Seven Domains). 61 lines.
- `tests/test-skills-have-skill-md.cjs` - NEW. GREEN. Asserts every `skills/*/` dir has a SKILL.md.
- `tests/test-doctor-class-h.cjs` - NEW. RED until 95.6-05. Hermetic mkdtempSync HOME; asserts doctor class H detects missing statusLine.
- `tests/test-doctor-class-h-fix.cjs` - NEW. RED until 95.6-05. Asserts `doctor --fix` writes the canonical statusLine block idempotently.
- `tests/test-install-receipt.cjs` - NEW. RED until 95.6-05. MINDRIAN_PLUGIN_HOME scratch tree; complete vs halted receipt fixtures; documents the canonical step list.
- `tests/test-92-rename-no-long-leaves.cjs` - NEW. GREEN-or-SKIP. Leaf-length cap + dangling-ref grep + rename-history check.
- `tests/test-install-sh-skill-loop.sh` - NEW. RED until 95.6-03 + 95.6-05. Hermetic install.sh fixture with a SKILL.md-less skill dir injected; PATH-shimmed git/npm.
- `tests/test-install-preflight-longpath.sh` - NEW. RED until 95.6-04. OS-detection-injected (`MOS_TEST_FORCE_OS`); fake git logging; core.longpaths assertion + named-error-when-Git-for-Windows-absent.
- `tests/test-release-npm-gate.sh` - NEW. RED until 95.6-06. Structural assertion on `scripts/release.sh` Step 9.5 ordering, dist-tag logic, dry-run hatch, recovery message.
- `tests/manual/95.6-windows-cold-install-acceptance.md` - NEW. 8-step cold-machine gate; 8 numbered items; mentions core.longpaths / install.sh / statusline / /mos:doctor / /mos:onboard / npm view @mindrian/os; "authoritative over the automated suites".
- `tests/run-all-956.sh` - NEW. Scoped runner for the 8 phase-relevant suites; runs to completion even when RED suites fail; exits non-zero (expected at Wave 0).
- `tests/run-all.sh` - MODIFIED. Added a `test-*.cjs` loop before the summary block (kept `set -uo pipefail`, no `-e`, so a failing CJS test does not abort the loop).

## `bash tests/run-all-956.sh` output at Wave 0

Ran to completion, no crash, 8 per-suite lines, exit 1 (expected):

```
--- Running: test-install-sh-skill-loop.sh ---        FAILED   (RED until 95.6-03 + 95.6-05)
--- Running: test-install-preflight-longpath.sh ---   FAILED   (RED until 95.6-04)
--- Running: test-release-npm-gate.sh ---             FAILED   (RED until 95.6-06)
--- Running: test-skills-have-skill-md.cjs ---        PASSED   (GREEN -- this plan landed it)
--- Running: test-doctor-class-h.cjs ---              FAILED   (RED until 95.6-05)
--- Running: test-doctor-class-h-fix.cjs ---          FAILED   (RED until 95.6-05; Test 2 idempotency PASSES)
--- Running: test-install-receipt.cjs ---             FAILED   (RED until 95.6-05; Test 0 receipt-shape PASSES)
--- Running: test-92-rename-no-long-leaves.cjs ---    PASSED   (SKIP -- 95.6-01 has not run yet)
  Total:  8   Passed: 2   Failed: 6
```

This is the intended Wave 0 state: the only suites that PASS are `test-skills-have-skill-md.cjs` (because Task 1 landed the backfill) and `test-92-rename-no-long-leaves.cjs` (which SKIPs cleanly since Plan 95.6-01 has not executed yet). The other six are RED-by-design and turn GREEN as their owning Tier 1 plans land.

**Is `tests/run-all-956.sh` GREEN at the end of this plan?** No -- and it should not be. It is RED by design (6 of 8 suites have not had their owning plans land). It runs to completion without a crash and prints a per-suite line for each of the 8. It goes fully GREEN only after 95.6-01 + 95.6-03 + 95.6-04 + 95.6-05 + 95.6-06 have all landed (the release-gate plan 95.6-10 re-runs the full suite as its contract).

## Pre-existing `tests/test-*.cjs` / `tests/test-*.sh` failures under the extended `run-all.sh`

`tests/run-all.sh` now also picks up the CJS suites and ran to completion (it returns exit 1 because failures exist; the runner itself did not crash). A full run is slow in a CI-less environment because several `test-114-*.sh` / `test-115-*.sh` shell suites invoke `claude --print` / `/mos:*` commands that time out (60s each) when no interactive Claude Code session is available. The 8 new 95.6 suites all ran: `test-skills-have-skill-md` PASSED, `test-92-rename-no-long-leaves` PASSED (SKIP), and the 6 RED-by-design suites FAILED as expected.

Pre-existing failures observed (other phases' suites, NOT in 95.6 scope, NOT modified -- logged for awareness only): `test-114-commands-regression`, `test-114-larry-default-activation`, `test-116-00-scaffold`, `test-117-00-scaffold`, `test-89-07-00-scaffold`, `test-phase-15`, `test-phase-27-meetings`, `test-phase-53-causal-extract`, `test-brain-ingestion-part-9-invariant`, `test-canon-part-9-ratification`, `test-causal-seed`, `test-command-jtbd-declarations`, `test-navigation-acceptance`, `test-navigation-chokepoint-hook`, `test-navigation-focus`, `test-navigation-insights`, `test-navigation-memory-events`, `test-navigation-neighborhood`, `test-navigation-packet-builder`, `test-navigation-packet-part8-leak`, `test-navigation-perf-10k`, `test-room-home-vs-brain-derivation-regression`, `test-self-update-platform`, `test-sqlite-concurrent`, `test-sqlite-ops`, `test-statusline-glyph-isolation`, `test-tension-hook-detection`. (Most of the `claude --print`-dependent and SQLite-binding-dependent ones fail only in a CI-less / binary-less environment; the navigation cluster and part-9 cluster carry their own phase RED states.) The new scoped runner `tests/run-all-956.sh` exists precisely so the 95.6 signal is not drowned by that noise.

## Decisions Made

- Kept the SKILL.md lean (~61 lines) and passive (no `activation:`/`paths:`) per the plan's reference-data guidance -- it is a data-and-index skill consumed by `/mos:scaffold`-style flows, not an auto-activating one.
- Made the doctor class-H key lookup tolerant of several names (`install-incomplete` / `class-h` / `classH` / `install-receipt`) so 95.6-05 can pick whatever fits the taxonomy without forcing a test rewrite.
- Stubbed `git` and `npm` on PATH in the install.sh shell fixtures so they stay offline and the scratch `.git` directory does not need to be a real repository.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Stubbed git + npm on PATH in `tests/test-install-sh-skill-loop.sh`**
- **Found during:** Task 3 (shell fixtures)
- **Issue:** install.sh's "existing install" branch runs `git pull --quiet origin main`, which exits 128 against a scratch (non-real) `.git` directory and the `npm install` step would hit the network -- the fixture would fail at the wrong step (a runtime crash, not the intended RED assertion).
- **Fix:** Placed no-op `git` and `npm` shims early on PATH (mirroring the pattern already used in `tests/test-install-preflight-longpath.sh`), keeping the real `node` + coreutils available. The fixture now reaches the skill-loop and produces the intended RED state (no `WARN: skipping skill` line yet, because the pre-filter is 95.6-03's job).
- **Files modified:** tests/test-install-sh-skill-loop.sh
- **Verification:** `bash tests/test-install-sh-skill-loop.sh` runs to completion, exits 1 with `FAIL: expected a 'WARN: skipping skill ...' line` (the RED-by-design assertion), not a `fatal: not a git repository` crash.
- **Committed in:** `7e6cb57` (Task 3 commit)

**2. [Rule 1 - Bug] Fixed a wrapped header phrase in `tests/test-release-npm-gate.sh`**
- **Found during:** Task 3 acceptance check (`grep -l "RED until Plan 95.6-06"`)
- **Issue:** The header comment wrapped "RED until Plan" onto one line and "95.6-06 ..." onto the next, so the single-string grep in the acceptance criteria did not match.
- **Fix:** Re-flowed the header so "RED until Plan 95.6-06 (release.sh Step 9.5 npm publish gate) lands." sits on one line.
- **Files modified:** tests/test-release-npm-gate.sh
- **Verification:** `grep -l "RED until Plan 95.6-06" tests/test-release-npm-gate.sh` returns the file; `bash -n` still passes.
- **Committed in:** `7e6cb57` (Task 3 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug). Both are within this plan's own test files -- no scope creep, no change to install.sh / doctor.cjs / release.sh (those are 95.6-03/04/05/06).
**Impact on plan:** Necessary for the fixtures to reach their intended RED state and for the acceptance grep to match. No scope creep.

## Issues Encountered

- Plan estimated "11 skills" but the repo has 10 skill directories (one of which, `mullins-scaffold`, was the missing-SKILL.md one). The test reports the real count (`PASS: all 10 skills have SKILL.md`), which is correct -- the "11" in the plan's acceptance criteria was an estimate. Adjusted the expectation in this SUMMARY.
- A full `bash tests/run-all.sh` run is slow in a CI-less environment (many `test-114-*.sh`/`test-115-*.sh` suites hit 60s `claude --print` timeouts). This pre-exists 95.6 and is out of scope; the scoped `tests/run-all-956.sh` is the fast path for phase work.

## Known Stubs

None that block this plan's goal. The seven new test files are deliberately RED-by-design "stubs" in the sense that their assertions encode contracts that downstream Tier 1 plans (95.6-03/04/05/06) and the Wave 1 rename plan (95.6-01) will satisfy -- this is the Nyquist Wave 0 pattern, documented in `95.6-VALIDATION.md`, not an unintentional stub. `skills/mullins-scaffold/SKILL.md` is real content (no placeholders) backed by the real `scaffold.json`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The Wave 0 surface is complete: every Tier 1 plan downstream now has an `<automated>` command to verify against (`95.6-VALIDATION.md` Per-Decision Verification Map rows all have their test files on disk).
- `95.6-VALIDATION.md` frontmatter `nyquist_compliant: false` / `wave_0_complete: false` can flip true once this plan + the Tier 1 plans land (this plan provides the test files; the Tier 1 plans turn them green). This plan does not flip those flags itself -- the release-gate plan (95.6-10) or whoever closes the phase does.
- 95.6-01 (D-02 rename) is the only plan that turns `test-92-rename-no-long-leaves.cjs` from SKIP to PASS; it is Wave 1 and independent of this plan.
- No blockers. This plan touched disjoint files from 95.6-01 and from 95.6-03/04/05/06, so Wave 0 + Wave 1 parallelism holds.

## Self-Check: PASSED

All 11 created files verified present on disk. All 3 task commits (`6dafc4c`, `7ec5385`, `7e6cb57`) verified present in git history.

---
*Phase: 95.6-install-cache-windows-hardening-and-skill-loop-resilience*
*Completed: 2026-05-11*
