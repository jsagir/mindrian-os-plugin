---
phase: 95.6
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - skills/mullins-scaffold/SKILL.md
  - tests/test-install-sh-skill-loop.sh
  - tests/test-skills-have-skill-md.cjs
  - tests/test-install-preflight-longpath.sh
  - tests/test-doctor-class-h.cjs
  - tests/test-doctor-class-h-fix.cjs
  - tests/test-install-receipt.cjs
  - tests/test-release-npm-gate.sh
  - tests/test-92-rename-no-long-leaves.cjs
  - tests/manual/95.6-windows-cold-install-acceptance.md
  - tests/run-all.sh
  - tests/run-all-956.sh
autonomous: true
tier: 1
requirements_addressed: [D-03]
canon_parts: [Part 6, Part 7]
must_haves:
  truths:
    - "Every skills/*/ directory in the repo contains a SKILL.md (no skill is structurally incomplete)"
    - "The seven missing Wave 0 test files exist as runnable hand-rolled assertion scripts, each red against current code (RED state) until its owning Tier 1 plan lands the fix"
    - "tests/run-all.sh aggregates the new shell-fixture suites AND the new CJS suites; a single command surfaces all of them"
    - "tests/manual/95.6-windows-cold-install-acceptance.md exists as the empirical Dimension-8 cold-machine gate checklist"
  artifacts:
    - path: "skills/mullins-scaffold/SKILL.md"
      provides: "The missing data file whose absence aborted Gary Laben's install.sh skill-loop (D-03 root cause). Wraps the existing skills/mullins-scaffold/scaffold.json (Mullins Seven Domains scaffold)."
      contains: "name: mullins-scaffold"
      min_lines: 20
    - path: "tests/test-skills-have-skill-md.cjs"
      provides: "Release-gate data-integrity assertion: find skills -mindepth 1 -maxdepth 1 -type d -exec test ! -f {}/SKILL.md returns empty"
      contains: "skills"
    - path: "tests/test-install-sh-skill-loop.sh"
      provides: "Hermetic install.sh-completion fixture (RED until 95.6-03 lands the pre-filter): scratch repo with a SKILL.md-less skill dir injected, run install.sh against /tmp fake ~/.claude, assert exit 0 + WARN line + settings.json fully populated"
      contains: "WARN: skipping skill"
    - path: "tests/test-install-preflight-longpath.sh"
      provides: "Hermetic OS-detection-injected preflight fixture (RED until 95.6-04 lands the preflight): forced-Windows asserts git config --global core.longpaths true is called + banner emitted; forced-non-Windows asserts it is not; missing-Git-for-Windows branch exits non-zero with a named error"
      contains: "core.longpaths"
    - path: "tests/test-doctor-class-h.cjs"
      provides: "Hermetic doctor class-H detection suite (RED until 95.6-05 lands class H), mirrors tests/test-doctor-class-g.cjs: MINDRIAN_PLUGIN_HOME scratch tree with settings.json missing statusLine, assert doctor.cjs --json flags it"
      contains: "MINDRIAN_PLUGIN_HOME"
    - path: "tests/test-doctor-class-h-fix.cjs"
      provides: "Hermetic doctor class-H remediation suite (RED until 95.6-05): assert --fix writes the canonical statusLine block + is a no-op on re-run (idempotent) + --statusline-visibility returns ok/missing correctly"
      contains: "idempotent"
    - path: "tests/test-install-receipt.cjs"
      provides: "Install-receipt unit (RED until 95.6-05 lands R-01): complete-install fixture produces ~/.claude/plugins/mindrian-os/.install-receipt.json with all steps; halted-install fixture produces a receipt missing tail steps; doctor class H reads it and names the gap"
      contains: ".install-receipt.json"
    - path: "tests/test-release-npm-gate.sh"
      provides: "Script-shape assertion on scripts/release.sh (RED until 95.6-06 lands Step 9.5): grep npm publish returns a match positioned between Step 9 (git push) and Step 10 (cache update); dist-tag logic branches on version suffix; a MOS_TEST_DRY_RUN=1 path exists"
      contains: "npm publish"
    - path: "tests/test-92-rename-no-long-leaves.cjs"
      provides: "Repo-state assertion (GREEN once 95.6-01 has run): no .planning/phases/ leaf name exceeds 60 chars; grep for the old 189-char path returns 0 hits outside the rename's own Searchability Note; git log --follow on the renamed dir is non-empty"
      contains: "60"
    - path: "tests/manual/95.6-windows-cold-install-acceptance.md"
      provides: "The cold-machine manual gate checklist (8 steps from 95.6-RESEARCH.md Validation Architecture). Gates v1.13.0-beta.9 ship. Empirical Dimension-8 gate, authoritative over the automated suites."
      contains: "cold-machine"
  key_links:
    - from: "tests/run-all.sh"
      to: "tests/test-skills-have-skill-md.cjs"
      via: "explicit invocation appended after the test-*.sh loop"
      pattern: "test-skills-have-skill-md"
    - from: "skills/mullins-scaffold/SKILL.md"
      to: "skills/mullins-scaffold/scaffold.json"
      via: "SKILL.md body references the scaffold JSON data file"
      pattern: "scaffold.json"
---

<objective>
WAVE 0 -- create the seven missing test files, backfill the known-missing `skills/mullins-scaffold/SKILL.md` data file (the file whose absence aborted Gary Laben's install.sh skill-loop on 2026-05-08/09), write the cold-machine manual acceptance gate, and wire all the new automated suites into `tests/run-all.sh`. This plan unblocks the Nyquist coverage that every Tier 1 plan (95.6-03 through 95.6-06) and the release-gate plan (95.6-10) depend on.

Purpose: 95.6-VALIDATION.md lists nine Wave 0 requirements. None of the test files exist yet, and `skills/mullins-scaffold/` has only `scaffold.json` (no `SKILL.md`). Without this plan, the Tier 1 plans have nothing to verify against and the data bug that broke Gary stays unfixed. Per Nyquist: every later task needs an `<automated>` command OR a Wave 0 dependency -- this plan provides the former.

Output: 1 backfilled SKILL.md, 7 new test files (6 RED against current code by design, 1 GREEN once 95.6-01 has run), 1 manual gate checklist, run-all.sh wired.

Per Canon Part 7 (reuse before build): all test files follow existing repo patterns -- CJS suites are hermetic via `MINDRIAN_PLUGIN_HOME` (mirroring `tests/test-doctor-class-g.cjs`, `tests/test-doctor-atomic-swap.cjs`); shell suites use a scratch repo + `/tmp` fake `~/.claude` with `PATH`-shimmed `git`/`node`/`npm` (mirroring `tests/test-session-start-preflight.sh`); manual gates are `tests/manual/*.md` checklists (mirroring `tests/manual/114-acceptance.md`). No new test framework. The SKILL.md follows the existing frontmatter shape used by `skills/larry-personality/SKILL.md` etc. (`name:` + `description:` block scalar; optional `activation:`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-VALIDATION.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-RESEARCH.md

<reference_data>
Existing test patterns to mirror (read these before writing the new ones):
- `tests/test-doctor-class-g.cjs` + `tests/test-doctor-class-g-fix.cjs` -- the hermetic doctor-class CJS pattern; uses `fs.mkdtempSync` + HOME/USERPROFILE override; spawns `node scripts/doctor.cjs --json`; the class-H suite is a near-copy with the class-G assertions swapped for statusLine-block assertions.
- `tests/test-doctor-atomic-swap.cjs` -- the `MINDRIAN_PLUGIN_HOME` scratch-tree pattern (Phase 95.2).
- `tests/test-session-start-preflight.sh` -- the shell-fixture style: scratch dir, fake `~/.claude`, real-or-stubbed `git`/`node`/`npm`.
- `tests/test-89-07-pattern-doc.sh` -- the simplest `set -euo pipefail` + `fail()` + `grep -q` structural-assertion shell test (good template for the release-gate shape assertion and the install-sh fixtures).
- `tests/run-all.sh` -- currently iterates `test-*.sh` only; the CJS suites are NOT picked up. This plan must extend it.
- `tests/fixtures/cmd-shim.sh` -- existing PATH-shim helper for faking CLIs in shell fixtures.

Existing SKILL.md frontmatter shape (from `skills/larry-personality/SKILL.md`, `skills/pws-methodology/SKILL.md`):
  (frontmatter block delimited by triple-dash lines; fields:)
  name: <skill-name>
  description: >
    <2-4 line block scalar describing when the skill is relevant>
  (then close the frontmatter; no other required fields)
Optional fields seen elsewhere: `activation:` (e.g. `no_room`, `resolve_room:active`, `env:MINDRIAN_BRAIN_KEY`), `paths:` (glob list). mullins-scaffold does not need `activation`/`paths` -- it is a passive data-and-prompt scaffold consumed by `/mos:scaffold` / room creation; describe it as relevant when scaffolding a room around the Mullins Seven Domains of Attractive Opportunities.

`skills/mullins-scaffold/scaffold.json` already exists: `{ "version": 1, "generated_from": "Mullins Seven Domains of Attractive Opportunities (Phase 84-04, v1.10.8)", "sections": [ ... 7 domains: market, industry, sustainable advantage, mission, ability to execute on CSFs, connectedness up/down the value chain ... ] }`. The SKILL.md is the human/agent-facing index that points at it.

The 92-rename test (`tests/test-92-rename-no-long-leaves.cjs`) is GREEN if and only if 95.6-01 has already executed (Wave 1, no dependency on this plan -- it can run before OR after Wave 0). State that explicitly in the test header: "This suite passes only after Plan 95.6-01 (D-02 rename) has run; if the 189-char dir still exists, this is expected RED until then." The release-gate plan (95.6-10) re-runs the full suite, so green-by-then is the contract.

The other six new tests are RED-by-design until their owning plan lands:
| Test file | Owning plan | Goes GREEN when |
|-----------|-------------|-----------------|
| `tests/test-install-sh-skill-loop.sh` | 95.6-03 | install.sh pre-filters the skill-loop + registers statusLine first |
| `tests/test-install-preflight-longpath.sh` | 95.6-04 | install.sh has the OS-detection longpath preflight |
| `tests/test-doctor-class-h.cjs` | 95.6-05 | doctor.cjs has class H detection |
| `tests/test-doctor-class-h-fix.cjs` | 95.6-05 | doctor.cjs --fix writes the statusLine block idempotently |
| `tests/test-install-receipt.cjs` | 95.6-05 | install.sh writes .install-receipt.json + doctor reads it |
| `tests/test-release-npm-gate.sh` | 95.6-06 | release.sh has Step 9.5 |

No-emoji rule (skills/ui-system). No em-dashes anywhere (use hyphens). REC-13 / SEED-007: do not introduce em-dashes or version literals into first-touch surfaces.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill skills/mullins-scaffold/SKILL.md and write the data-integrity test</name>
  <files>
    - skills/mullins-scaffold/SKILL.md (NEW -- the missing data file)
    - tests/test-skills-have-skill-md.cjs (NEW)
  </files>
  <read_first>
    - skills/mullins-scaffold/scaffold.json (the data file the SKILL.md indexes)
    - skills/larry-personality/SKILL.md (frontmatter shape reference)
    - skills/pws-methodology/SKILL.md (frontmatter shape reference)
    - skills/ui-system/SKILL.md (no-emoji / 12-glyph / body-shape conventions to honor in any user-facing text)
    - tests/test-89-07-pattern-doc.sh (simple structural-assertion test template, for the cjs equivalent)
  </read_first>
  <action>
    1. Create `skills/mullins-scaffold/SKILL.md`. Use this exact frontmatter shape (block-scalar description, no emoji, no em-dashes):
       ```yaml
       ---
       name: mullins-scaffold
       description: >
         Room scaffolding around the Mullins Seven Domains of Attractive Opportunities
         (Market, Industry, Sustainable Advantage, Mission/Aspirations, Ability to
         Execute on Critical Success Factors, Connectedness Up the Value Chain,
         Connectedness Down the Value Chain). Relevant when creating a new venture
         room, running an opportunity assessment, or structuring a Data Room around
         a market-attractiveness lens. Backed by skills/mullins-scaffold/scaffold.json
         (the section definitions and prompts).
       ---
       ```
       Then a body with at least these sections (keep it lean, ~30-50 lines total):
       - `# Mullins Scaffold -- Seven Domains of Attractive Opportunities`
       - `## What this scaffold is` -- one paragraph: the Seven Domains framework (Mullins, "The New Business Road Test"), what each domain asks, why it is a useful Data Room skeleton for the early/opportunity stage. Cite the source: "Generated from Mullins Seven Domains (Phase 84-04, v1.10.8)."
       - `## The data file` -- explain that `skills/mullins-scaffold/scaffold.json` carries the canonical section list: `version`, `generated_from`, and `sections[]` where each section has `id`, `domain`, `title`, `prompt`, `required`. Note that room-scaffolding code (and `/mos:scaffold`-style flows) reads this JSON to build the folder skeleton; the JSON is the source of truth, this SKILL.md is the index.
       - `## When Larry should reach for this` -- 3-5 bullets: new venture room, opportunity-bank deep-dive, market-attractiveness review, comparing two ventures on the same axes.
       - `## Relationship to other scaffolds` -- one line: this is one of several room scaffolds; it is the market-attractiveness lens, complementary to the PWS section structure (`skills/pws-methodology`).
       Do NOT add `activation:` or `paths:` -- this is a passive data-and-index skill, not an auto-activating one.

    2. Create `tests/test-skills-have-skill-md.cjs` -- a hand-rolled CJS assertion (mirror the `assert`-driven style of `tests/test-doctor-class-g.cjs`, but much simpler -- no fixtures needed, it scans the repo). Behavior:
       ```js
       #!/usr/bin/env node
       'use strict';
       // Phase 95.6 D-03(a) -- release-gate data-integrity assertion.
       // Every skills/*/ directory MUST contain a SKILL.md, else install.sh's
       // skill-loop (pre-95.6-03) aborts under set -euo pipefail. (The bug that
       // broke Gary Laben's install 2026-05-08/09; see docs/testers/gary-laben/FEEDBACK.md.)
       const assert = require('node:assert');
       const fs = require('node:fs');
       const path = require('node:path');
       const REPO_ROOT = path.resolve(__dirname, '..');
       const SKILLS_DIR = path.join(REPO_ROOT, 'skills');
       const dirs = fs.readdirSync(SKILLS_DIR, { withFileTypes: true })
         .filter(d => d.isDirectory())
         .map(d => d.name);
       const missing = dirs.filter(name => !fs.existsSync(path.join(SKILLS_DIR, name, 'SKILL.md')));
       assert.deepStrictEqual(missing, [],
         'skills/ directories missing SKILL.md (will abort install.sh skill-loop): ' + JSON.stringify(missing));
       console.log('PASS: all ' + dirs.length + ' skills have SKILL.md');
       ```
       Make the file executable (`chmod +x`). This test goes GREEN immediately once Task 1 step 1 lands.

    Per D-03(a): backfill the canonical skills. `skills/mullins-scaffold/` was the known-broken one; this test enumerates and asserts there are no others (none currently expected -- the other 10 skill dirs already have SKILL.md per the 2026-05-09 `ls`).
  </action>
  <verify>
    <automated>node /home/jsagi/MindrianOS-Plugin/tests/test-skills-have-skill-md.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/jsagi/MindrianOS-Plugin/skills/mullins-scaffold/SKILL.md` succeeds
    - `head -1 /home/jsagi/MindrianOS-Plugin/skills/mullins-scaffold/SKILL.md` is `---` and `grep -q "^name: mullins-scaffold" /home/jsagi/MindrianOS-Plugin/skills/mullins-scaffold/SKILL.md` succeeds
    - `grep -q "scaffold.json" /home/jsagi/MindrianOS-Plugin/skills/mullins-scaffold/SKILL.md` succeeds (it references its data file)
    - `wc -l < /home/jsagi/MindrianOS-Plugin/skills/mullins-scaffold/SKILL.md` returns >= 20
    - `grep -c $'—' /home/jsagi/MindrianOS-Plugin/skills/mullins-scaffold/SKILL.md` returns 0 (no em-dashes)
    - `node /home/jsagi/MindrianOS-Plugin/tests/test-skills-have-skill-md.cjs` exits 0 and prints `PASS: all 11 skills have SKILL.md`
    - `find /home/jsagi/MindrianOS-Plugin/skills -mindepth 1 -maxdepth 1 -type d -exec test '!' -f {}/SKILL.md ';' -print` produces no output
  </acceptance_criteria>
  <done>
    `skills/mullins-scaffold/SKILL.md` exists with valid frontmatter and references its scaffold.json. `tests/test-skills-have-skill-md.cjs` exists, is executable, and passes. The data bug that broke Gary's install is fixed (the contract fix is 95.6-03).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the four CJS Wave 0 test scaffolds (doctor class H, class H fix, install receipt, 92-rename)</name>
  <files>
    - tests/test-doctor-class-h.cjs (NEW -- RED until 95.6-05)
    - tests/test-doctor-class-h-fix.cjs (NEW -- RED until 95.6-05)
    - tests/test-install-receipt.cjs (NEW -- RED until 95.6-05)
    - tests/test-92-rename-no-long-leaves.cjs (NEW -- GREEN once 95.6-01 ran)
  </files>
  <read_first>
    - tests/test-doctor-class-g.cjs (the hermetic doctor-class detection pattern to mirror)
    - tests/test-doctor-class-g-fix.cjs (the remediation pattern to mirror)
    - tests/test-doctor-atomic-swap.cjs (the MINDRIAN_PLUGIN_HOME scratch-tree pattern)
    - scripts/doctor.cjs (lines 60-115 for flag parsing; lines 900-1110 for the class-G statusLine logic that class H will extend; the `--statusline-visibility` flag already exists)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-VALIDATION.md (the D-09 and D-02 behavioral contracts the assertions encode)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-01-PLAN.md (the D-02 rename contract -- the 92-rename test asserts its acceptance criteria)
  </read_first>
  <action>
    For each file, write a hand-rolled `node:assert`-driven test. Each MUST: (a) have a header comment naming the owning plan and stating "RED until <plan> lands the fix" (except the 92-rename test which states "GREEN once 95.6-01 has run"); (b) exit 0 on pass with a `PASS:` line, non-zero on fail with a clear assertion message; (c) be `chmod +x`.

    1. `tests/test-doctor-class-h.cjs` (RED until 95.6-05): mirror `tests/test-doctor-class-g.cjs`'s hermetic envelope (`fs.mkdtempSync` scratch HOME, or `MINDRIAN_PLUGIN_HOME` scratch tree -- match whichever the class-G suite uses). Tests:
       - clean fixture (settings.json WITH a canonical `statusLine` block) -> `node scripts/doctor.cjs --statusline-visibility --json` reports the class-H key as `status: ok`.
       - settings.json present but WITHOUT a `statusLine` key (the "install incomplete / halted before statusline step" case -- Gary's and Jonathan's-other-machine symptom) -> reports class H as `status: warn` (or `error`), `recoverable: true`, with a detail string naming "statusLine block missing".
       - `--all` activates the class-H key in `report.checks`.
       Since class H does not exist in `doctor.cjs` yet, these assertions WILL fail until 95.6-05 lands -- that is the RED-by-design contract. Write the assertions against the contract in 95.6-VALIDATION.md D-09 ("the audit calls it class H -- install incomplete -- which subsumes the missing-statusLine case").

    2. `tests/test-doctor-class-h-fix.cjs` (RED until 95.6-05): mirror `tests/test-doctor-class-g-fix.cjs`. Tests:
       - given a scratch settings.json missing `statusLine`, `node scripts/doctor.cjs --statusline-visibility --fix` writes the canonical MindrianOS statusLine block (the one `install.sh`'s `register_statusline()` writes -- a `{ "type": "command", "command": "bash \"$INSTALL_DIR/scripts/statusline-mos\"" }` shape, matching the existing wrapper at `scripts/statusline-mos` referenced in `doctor.cjs` class G).
       - re-running `--fix` is a no-op (idempotent): the file is byte-identical the second time.
       - after `--fix`, `node scripts/doctor.cjs --statusline-visibility --json` reports `status: ok`.

    3. `tests/test-install-receipt.cjs` (RED until 95.6-05): tests the `.install-receipt.json` contract (R-01). Use a scratch `~/.claude/plugins/mindrian-os/` via `MINDRIAN_PLUGIN_HOME`. Tests:
       - a complete-install fixture (a `.install-receipt.json` with every expected step name and exit code 0) -- assert `doctor.cjs` (class H) reads it and reports no gap.
       - a halted-install fixture (`.install-receipt.json` listing only the steps before the skill-loop) -- assert `doctor.cjs` reads it and reports the missing tail steps BY NAME (e.g. "register_statusline did not run").
       - the receipt JSON shape: `{ "version": "<plugin version>", "completed_at": "<ISO or null>", "steps": [ { "name": "preflight", "exit": 0 }, { "name": "clone", "exit": 0 }, { "name": "register_statusline", "exit": 0 }, { "name": "register_commands", "exit": 0 }, { "name": "register_skills", "exit": 0 }, { "name": "register_agents", "exit": 0 }, { "name": "configure_hooks", "exit": 0 } ] }` -- assert the test fixtures conform and document the canonical step list in the test header so 95.6-03/05 implement install.sh to match.

    4. `tests/test-92-rename-no-long-leaves.cjs` (GREEN once 95.6-01 ran -- header says so): no fixtures. Tests (mirror the `tests/test-canon-crossref-completeness.cjs` repo-scan style):
       - `fs.readdirSync('.planning/phases')` -> for each entry that is a directory, `entry.length <= 60`. Assert the max is <= 60. (Note in the header: the next-longest leaf after the rename is `03.1-data-room-dashboard-...` at 80 chars, which is out of 95.6 scope and OK -- so the assertion is specifically that no `92-*` leaf exceeds 60, AND for completeness assert the overall max is reported; if the executor wants a hard global cap they may set it to 81 to allow the 03.1 dir, but the 92 leaf MUST be <= 60. Pick the global-max-reported approach with a specific `92-` assertion to avoid coupling to the 03.1 dir.)
       - `git grep -l --no-color "92-refactor-constitution-and-trust-layer"` (via `child_process.execSync`) -> 0 files OUTSIDE `.planning/phases/92-trust-layer-refactor/` and `docs/testers/gary-laben/FEEDBACK.md` (the whitelisted historical quote). Skip the assertion gracefully (print SKIP) if `git` is unavailable.
       - `child_process.execSync('git log --follow --oneline -- .planning/phases/92-trust-layer-refactor/')` -> non-empty output (rename history preserved). Skip gracefully if `git` unavailable or the dir does not exist yet (print "SKIP: 95.6-01 has not run yet" and exit 0 -- do NOT fail; this test must not block Wave 0).

    Per Nyquist coverage ranking (D-03 > D-09 ~ D-01 > D-05a > D-02): the class-H + receipt suites get the densest assertion sets; the 92-rename test is a single repo-state assertion. Match that weighting in how many test cases each file carries.
  </action>
  <verify>
    <automated>node /home/jsagi/MindrianOS-Plugin/tests/test-92-rename-no-long-leaves.cjs; test -f /home/jsagi/MindrianOS-Plugin/tests/test-doctor-class-h.cjs && test -f /home/jsagi/MindrianOS-Plugin/tests/test-doctor-class-h-fix.cjs && test -f /home/jsagi/MindrianOS-Plugin/tests/test-install-receipt.cjs && node -c /home/jsagi/MindrianOS-Plugin/tests/test-doctor-class-h.cjs && node -c /home/jsagi/MindrianOS-Plugin/tests/test-doctor-class-h-fix.cjs && node -c /home/jsagi/MindrianOS-Plugin/tests/test-install-receipt.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under `tests/`: `test-doctor-class-h.cjs`, `test-doctor-class-h-fix.cjs`, `test-install-receipt.cjs`, `test-92-rename-no-long-leaves.cjs`
    - `node -c tests/test-doctor-class-h.cjs && node -c tests/test-doctor-class-h-fix.cjs && node -c tests/test-install-receipt.cjs && node -c tests/test-92-rename-no-long-leaves.cjs` all exit 0 (valid syntax)
    - Each CJS file's header comment names its owning plan and its RED/GREEN expectation (`grep -l "RED until 95.6-05" tests/test-doctor-class-h.cjs tests/test-doctor-class-h-fix.cjs tests/test-install-receipt.cjs` returns all three; `grep -q "95.6-01" tests/test-92-rename-no-long-leaves.cjs` succeeds)
    - `node tests/test-92-rename-no-long-leaves.cjs` exits 0 -- either because 95.6-01 already ran (real PASS) or because it printed `SKIP` (95.6-01 not yet run). It MUST NOT hard-fail Wave 0.
    - `grep -q "MINDRIAN_PLUGIN_HOME\|mkdtempSync" tests/test-doctor-class-h.cjs` succeeds (hermetic envelope present)
    - `grep -q ".install-receipt.json" tests/test-install-receipt.cjs` succeeds and the canonical step list (`register_statusline`, `register_commands`, etc.) appears in the file
    - `grep -c $'—' tests/test-doctor-class-h.cjs tests/test-doctor-class-h-fix.cjs tests/test-install-receipt.cjs tests/test-92-rename-no-long-leaves.cjs | grep -v ':0$'` returns nothing (no em-dashes in any of the four)
    - Files are executable: `test -x tests/test-doctor-class-h.cjs && test -x tests/test-doctor-class-h-fix.cjs && test -x tests/test-install-receipt.cjs && test -x tests/test-92-rename-no-long-leaves.cjs`
  </acceptance_criteria>
  <done>
    Four CJS test scaffolds exist. Three are RED-by-design (they encode the D-09 / R-01 contracts and pass once 95.6-05 lands). The 92-rename test is GREEN-or-SKIP and never blocks Wave 0. All have headers naming their owning plan.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the three shell-fixture Wave 0 tests, the cold-machine manual gate, and wire run-all.sh</name>
  <files>
    - tests/test-install-sh-skill-loop.sh (NEW -- RED until 95.6-03)
    - tests/test-install-preflight-longpath.sh (NEW -- RED until 95.6-04)
    - tests/test-release-npm-gate.sh (NEW -- RED until 95.6-06)
    - tests/manual/95.6-windows-cold-install-acceptance.md (NEW)
    - tests/run-all.sh (MODIFY -- aggregate the new CJS suites)
  </files>
  <read_first>
    - tests/test-session-start-preflight.sh (the shell-fixture style: scratch dir + fake ~/.claude + stubbed CLIs)
    - tests/test-89-07-pattern-doc.sh (the simple `set -euo pipefail` + `fail()` + `grep -q` template)
    - tests/fixtures/cmd-shim.sh (the PATH-shim helper for faking CLIs)
    - tests/run-all.sh (the current runner -- only picks up `test-*.sh`, not `test-*.cjs`)
    - install.sh (the script under test for the skill-loop + preflight fixtures -- note `set -euo pipefail` at line 10, the skill-loop at lines 97-109, the settings.json node block at lines 134-170)
    - scripts/release.sh (the script under test for the npm-gate shape assertion -- Steps 1-11, `git push` is "Step 9" at line ~141, "Step 10: Update local cache" at line ~147)
    - tests/manual/114-acceptance.md (the manual-gate checklist format to mirror)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-RESEARCH.md (the "cold-machine manual gate" 8-step checklist in the Validation Architecture section -- copy it)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-VALIDATION.md ("Manual-Only Verifications" table)
  </read_first>
  <action>
    1. `tests/test-install-sh-skill-loop.sh` (RED until 95.6-03). `set -euo pipefail`; `cd "$(git rev-parse --show-toplevel)"`; `fail() { echo "FAIL: $1" >&2; exit 1; }`. Fixture:
       - `WORK=$(mktemp -d)`; build a scratch "repo" inside it: copy (or symlink) `install.sh`, `scripts/`, `commands/`, `agents/`, and a `skills/` dir containing the real skill dirs PLUS one deliberately broken skill dir `skills/_test-broken-skill/` with NO `SKILL.md`.
       - Fake `~/.claude`: `HOME="$WORK/fakehome"`; `mkdir -p "$HOME/.claude"`.
       - Run `bash install.sh` with `INSTALL_DIR` pointed at the scratch repo (install.sh reads `INSTALL_DIR="${HOME}/.claude/plugins/mindrian-os"` -- so set up the scratch repo AT that path, or export an override if 95.6-03 adds one; for the fixture, place the scratch repo at `$HOME/.claude/plugins/mindrian-os` and pre-populate `.git` so the "existing install" branch is taken and the clone is skipped).
       - Assertions (these FAIL until 95.6-03 lands the pre-filter, by design): exit code 0; stderr contains `WARN: skipping skill _test-broken-skill: no SKILL.md`; after the run, `$HOME/.claude/settings.json` parses as JSON and has `.agent == "larry-extended"`, `.env.MINDRIAN_OS_ROOT` set, a `SessionStart` hook entry containing `mindrian-os`, AND `.statusLine.command` set (this overlaps D-09 by design -- a halt before the statusLine step is exactly what broke Gary).
       - Header comment: "RED until Plan 95.6-03 (skill-loop pre-filter) and 95.6-05 (register_statusline first) land."
       - Cleanup: `trap 'rm -rf "$WORK"' EXIT`.

    2. `tests/test-install-preflight-longpath.sh` (RED until 95.6-04). Same harness shape. Fixture stubs OS detection (95.6-04 will introduce a `MOS_TEST_FORCE_OS` env var or equivalent -- write the test against `MOS_TEST_FORCE_OS=windows` / `MOS_TEST_FORCE_OS=linux`) and puts a fake `git` early on `PATH` (a shell script that appends its args to `$WORK/git-calls.log` and exits 0). Assertions (FAIL until 95.6-04 lands the preflight):
       - `MOS_TEST_FORCE_OS=windows bash install.sh` (with the fake git) -> `git-calls.log` contains a line `config --global core.longpaths true`, AND install.sh stdout contains a banner line explaining the global git-config change (grep for `core.longpaths` in the captured stdout).
       - `MOS_TEST_FORCE_OS=linux bash install.sh` -> `git-calls.log` does NOT contain `config --global core.longpaths true`.
       - `MOS_TEST_FORCE_OS=windows` with NO `git` on PATH at all -> install.sh exits non-zero and prints a named error mentioning "Git for Windows" (grep the captured output).
       Header: "RED until Plan 95.6-04 (Windows long-path preflight + OS detection) lands."

    3. `tests/test-release-npm-gate.sh` (RED until 95.6-06). Pure structural assertion on `scripts/release.sh` -- no fixture, mirror `tests/test-89-07-pattern-doc.sh`. Assertions (FAIL until 95.6-06 lands Step 9.5):
       - `grep -q "npm publish" scripts/release.sh` (the gate exists).
       - The `npm publish` line sits between the `git push` block and the `# --- Step 10` block: use `awk` to capture the line numbers of `git push` (or `Step 9`), `npm publish`, and `Step 10`, and assert `line(git push) < line(npm publish) < line(Step 10)`.
       - The dist-tag selection branches on the version suffix: `grep -Eq "beta\.|alpha\.|rc\.|next\.|--tag next|--tag latest" scripts/release.sh` AND a conditional (`if .*beta` or a `case` on the version) precedes the `npm publish`.
       - A dry-run escape hatch exists: `grep -q "MOS_TEST_DRY_RUN" scripts/release.sh` (so the gate is exercisable without touching the live registry).
       - A recovery message surfaces on publish failure: `grep -q "npm publish failed\|publish failed\|recover" scripts/release.sh` near the `npm publish` invocation (the gate must halt loudly, not silently ship unpublished).
       Header: "RED until Plan 95.6-06 (release.sh Step 9.5 npm publish gate) lands."

    4. `tests/manual/95.6-windows-cold-install-acceptance.md` -- mirror `tests/manual/114-acceptance.md` structure (title, "When to run", "Tester", "Preconditions", numbered checklist, "Pass criteria", "Capture / attach", "Sign-off" line). Copy the 8-step checklist VERBATIM from 95.6-RESEARCH.md's "The cold-machine manual gate (the empirical Dimension-8 gate)" section:
       1. Fresh Windows 11 machine, Git for Windows installed, no prior MindrianOS state (Surface-class touch device preferred -- matches Gary's Surface Pro 7; bring an external keyboard for the permission prompts).
       2. Run the canonical install path exactly as the install site documents it (CMD shell -- what worked for Gary live; note any PowerShell deviation).
       3. Observe: `git clone` completes with no MAX_PATH error and no manual `core.longpaths` intervention required (D-01 + D-02).
       4. Observe: `install.sh` exits 0; if any skill dir is incomplete it prints a `WARN` and continues; agents, hooks, larry-extended default, `MINDRIAN_OS_ROOT` all registered (D-03).
       5. Open a fresh `claude` session: the statusline `(hexagon) MindrianOS-Plugin <room> <context%>` renders at the bottom within ~1s (D-09).
       6. Run `/mos:doctor --all` (or `/mos:doctor --statusline-visibility`): all-green, OR it names exactly what is missing and `--fix` resolves it.
       7. Reach `/mos:onboard` -> Larry greets, in under 5 minutes total, with no maintainer help.
       8. (D-05a, if recovery publish has landed) `npm view @mindrian/os@next version` returns the shipped beta version; if not yet landed, confirm the install site does not advertise the npx path.
       Add at the top: "This gate is authoritative over the automated suites. A green CI run with a failed cold-machine gate means Phase 95.6 is NOT done and v1.13.0-beta.9 does NOT ship. Scheduled tester: Gary Laben (or a Wave-3 equivalent). It also produces the source material for the case #4 autopsy at `docs/autopsies/2026-05-09-gary-laben-install-failure.md` (Plan 95.6-06)." Use `(hexagon)` as written here -- do NOT use an actual emoji or unicode hexagon glyph in the markdown body; describe it in words.

    5. Modify `tests/run-all.sh`: after the existing `for test_script in "$SCRIPT_DIR"/test-*.sh` loop completes, add a second loop that runs the CJS suites and folds them into the same TOTAL/PASSED/FAILED accounting. Concretely, before the final summary block, insert:
       ```bash
       # Phase 95.6: also run the hand-rolled CJS assertion suites.
       for test_cjs in "$SCRIPT_DIR"/test-*.cjs; do
         [[ -f "$test_cjs" ]] || continue
         test_name=$(basename "$test_cjs" .cjs)
         ((TOTAL++))
         echo "--- Running: $test_name ---"
         if node "$test_cjs"; then
           ((PASSED++)); echo ">>> $test_name: PASSED"
         else
           ((FAILED++)); FAILED_TESTS+=("$test_name"); echo ">>> $test_name: FAILED"
         fi
         echo ""
       done
       ```
       Keep `set -uo pipefail` (the existing header) -- do NOT add `-e` (the loop must continue past a failing test). NOTE: many pre-existing CJS suites in `tests/` may currently fail (they belong to other phases and may have their own RED states). To avoid drowning 95.6 signal, ALSO add a `tests/run-all-956.sh` that runs ONLY the 95.6-relevant suites (`test-install-sh-skill-loop.sh`, `test-install-preflight-longpath.sh`, `test-release-npm-gate.sh`, `test-skills-have-skill-md.cjs`, `test-doctor-class-h.cjs`, `test-doctor-class-h-fix.cjs`, `test-install-receipt.cjs`, `test-92-rename-no-long-leaves.cjs`) -- same accounting, scoped. Header comment: "Phase 95.6 scoped runner -- run before each 95.6 task commit. Expect RED on the suites whose owning plan has not landed yet (see 95.6-VALIDATION.md)."

    Per Canon Part 7: all of this reuses existing test idioms. The only new infrastructure is the scoped runner, which is a thin wrapper.
  </action>
  <verify>
    <automated>bash -n /home/jsagi/MindrianOS-Plugin/tests/test-install-sh-skill-loop.sh && bash -n /home/jsagi/MindrianOS-Plugin/tests/test-install-preflight-longpath.sh && bash -n /home/jsagi/MindrianOS-Plugin/tests/test-release-npm-gate.sh && bash -n /home/jsagi/MindrianOS-Plugin/tests/run-all.sh && bash -n /home/jsagi/MindrianOS-Plugin/tests/run-all-956.sh && test -f /home/jsagi/MindrianOS-Plugin/tests/manual/95.6-windows-cold-install-acceptance.md</automated>
  </verify>
  <acceptance_criteria>
    - All three shell tests exist: `tests/test-install-sh-skill-loop.sh`, `tests/test-install-preflight-longpath.sh`, `tests/test-release-npm-gate.sh`
    - `bash -n tests/test-install-sh-skill-loop.sh && bash -n tests/test-install-preflight-longpath.sh && bash -n tests/test-release-npm-gate.sh` all exit 0 (valid syntax)
    - Each shell test has `set -euo pipefail` (or `set -uo pipefail` for the runner) and a `fail()` helper and a header comment naming its owning plan: `grep -l "RED until Plan 95.6-03" tests/test-install-sh-skill-loop.sh`, `grep -l "RED until Plan 95.6-04" tests/test-install-preflight-longpath.sh`, `grep -l "RED until Plan 95.6-06" tests/test-release-npm-gate.sh` each return the file
    - `test -x tests/test-install-sh-skill-loop.sh && test -x tests/test-install-preflight-longpath.sh && test -x tests/test-release-npm-gate.sh` (executable)
    - `tests/manual/95.6-windows-cold-install-acceptance.md` exists, has 8 numbered checklist items (`grep -c "^[0-9]\." tests/manual/95.6-windows-cold-install-acceptance.md` returns >= 8), mentions `core.longpaths`, `install.sh`, `statusline`, `/mos:doctor`, `/mos:onboard`, and `npm view @mindrian/os`, and contains the line "authoritative over the automated suites"
    - `grep -c $'—' tests/manual/95.6-windows-cold-install-acceptance.md tests/test-install-sh-skill-loop.sh tests/test-install-preflight-longpath.sh tests/test-release-npm-gate.sh | grep -v ':0$'` returns nothing (no em-dashes)
    - `tests/run-all.sh` now contains a `test-*.cjs` loop: `grep -q 'test-\*.cjs' tests/run-all.sh` succeeds; `bash -n tests/run-all.sh` exits 0
    - `tests/run-all-956.sh` exists, is executable, names all 8 of the 95.6 suites, and `bash -n tests/run-all-956.sh` exits 0
    - Running `bash tests/run-all-956.sh` exits non-zero IS EXPECTED at Wave 0 time (the 6 RED-by-design suites have not had their owning plans land yet) -- but it MUST run to completion without a syntax/runtime crash, printing a per-suite PASS/FAIL line for each of the 8. The only suites that should PASS at Wave 0: `test-skills-have-skill-md.cjs` (after Task 1) and `test-92-rename-no-long-leaves.cjs` (PASS if 95.6-01 ran, else SKIP).
  </acceptance_criteria>
  <done>
    Three shell-fixture tests + the cold-machine manual gate exist. `tests/run-all.sh` aggregates CJS suites. `tests/run-all-956.sh` gives a scoped 95.6-only runner. The Wave 0 surface is complete: every Tier 1 plan downstream now has an `<automated>` command to verify against.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. `node tests/test-skills-have-skill-md.cjs` -> PASS (all 11 skills have SKILL.md).
2. `bash tests/run-all-956.sh` -> runs to completion, prints 8 per-suite lines. At Wave 0: 1-2 PASS (skills-have-skill-md, and 92-rename if 95.6-01 ran), the rest RED-by-design. No crash.
3. `for f in tests/test-install-sh-skill-loop.sh tests/test-install-preflight-longpath.sh tests/test-release-npm-gate.sh; do bash -n "$f" || echo "SYNTAX FAIL: $f"; done` -> no output.
4. `for f in tests/test-doctor-class-h.cjs tests/test-doctor-class-h-fix.cjs tests/test-install-receipt.cjs tests/test-92-rename-no-long-leaves.cjs; do node -c "$f" || echo "SYNTAX FAIL: $f"; done` -> no output.
5. `test -f tests/manual/95.6-windows-cold-install-acceptance.md && grep -c "^[0-9]\." tests/manual/95.6-windows-cold-install-acceptance.md` -> >= 8.
6. `grep -q 'test-\*.cjs' tests/run-all.sh` -> succeeds (run-all.sh wired).
7. No em-dashes in any new file: `grep -rl $'—' skills/mullins-scaffold/SKILL.md tests/test-*-h.cjs tests/test-install-*.* tests/test-release-npm-gate.sh tests/test-92-rename-no-long-leaves.cjs tests/manual/95.6-windows-cold-install-acceptance.md tests/run-all-956.sh 2>/dev/null` -> no output.

This plan does NOT modify install.sh, doctor.cjs, or release.sh -- those are 95.6-03 / 95.6-04 / 95.6-05 / 95.6-06. It only creates test scaffolds + the SKILL.md data file + the manual gate + the runner wiring. So it touches disjoint files from every other Wave 1 plan and from 95.6-01.
</verification>

<success_criteria>
- [ ] `skills/mullins-scaffold/SKILL.md` backfilled (the data file whose absence broke Gary's install) with valid frontmatter referencing `scaffold.json`
- [ ] `tests/test-skills-have-skill-md.cjs` exists and PASSES (release-gate data-integrity assertion)
- [ ] All 7 missing Wave 0 test files created (3 shell, 4 CJS), each with a header naming its owning plan and RED/GREEN expectation
- [ ] `tests/manual/95.6-windows-cold-install-acceptance.md` created with the 8-step cold-machine gate checklist (verbatim from 95.6-RESEARCH.md), marked authoritative over the automated suites
- [ ] `tests/run-all.sh` extended to aggregate `test-*.cjs` suites; `tests/run-all-956.sh` scoped runner added
- [ ] No emoji, no em-dashes anywhere in the new files
- [ ] Plan touches disjoint files from 95.6-01 and from 95.6-03/04/05/06 (so Wave 0 + Wave 1 parallelism holds)
- [ ] `nyquist_compliant: true` becomes settable in 95.6-VALIDATION.md frontmatter once this plan + the Tier 1 plans land (this plan provides the test files; the Tier 1 plans turn them green)
</success_criteria>

<output>
After completion, create `.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-02-SUMMARY.md` documenting:
- The SKILL.md backfilled (line count, what it indexes)
- The 7 test files created, and which 6 are RED-by-design + which plan turns each one green
- The cold-machine manual gate file path
- The run-all.sh change + the new scoped runner
- Output of `bash tests/run-all-956.sh` at Wave 0 (expect: 1-2 PASS, the rest RED, no crash) -- attach the per-suite line summary
- Any pre-existing `tests/test-*.cjs` suites that now fail under the extended `run-all.sh` (noted for awareness; not in 95.6 scope to fix)
</output>
