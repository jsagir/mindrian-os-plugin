---
phase: 95.6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/92-trust-layer-refactor/
  - .planning/phases/92-trust-layer-refactor/92-CONTEXT.md
  - docs/testers/gary-laben/FEEDBACK.md  # NOT EDITED -- historical quote; listed for completeness
  # Plus: any file Task 2 grep finds with a dangling old-path reference (potentially 0; no current dangling refs known per 2026-05-09 grep)
autonomous: true
requirements_addressed: [D-02]
canon_parts: [Part 6, Part 7]
must_haves:
  truths:
    - "No phase directory leaf name exceeds 60 characters"
    - "Windows tester clone of MindrianOS-Plugin completes without long-path error caused by phase 92 directory"
    - "All in-repo references to the old phase 92 path resolve to the new path"
  artifacts:
    - path: ".planning/phases/92-trust-layer-refactor/"
      provides: "Renamed phase 92 directory (was 189-char leaf name)"
      contains: "all original files preserved (PLAN, CONTEXT, SUMMARY, etc.)"
    - path: ".planning/phases/92-trust-layer-refactor/92-CONTEXT.md"
      provides: "CONTEXT.md retains the original 189-char descriptive name as a body-section Searchability Note (NOT a frontmatter field; per REC-11 in 95.6-RECOMMENDATIONS.md, frontmatter precedent should not be set for one-off naming problems)"
      contains: "## Searchability Note"
  key_links:
    - from: "docs/testers/gary-laben/FEEDBACK.md"
      to: ".planning/phases/92-trust-layer-refactor/"
      via: "verbatim path quote in 2026-05-09 entry"
      pattern: "92-trust-layer-refactor"
---

<objective>
D-02: Rename `.planning/phases/92-refactor-constitution-and-trust-layer-formalizes-audit-driven-refactor-work-constitution-v1-1-directive-1-validation-directive-2-consolidation-directive-3-unidirectional-flow-trust-layer/` (189-char leaf) to `.planning/phases/92-trust-layer-refactor/` (24-char leaf). Update every in-repo reference to the old path. Preserve the original 189-char descriptive name as a `## Searchability Note` body section in CONTEXT.md (NOT a frontmatter field -- per REC-11, avoid setting a canon precedent for a one-off naming problem).

Purpose: Single highest-leverage Wave-1 fix in the phase. Removes the entire Windows MAX_PATH (260 char) failure class that broke Gary Laben's clone twice during the 2026-05-09 live install call. Fixes the bug for every future Windows tester whether or not `core.longpaths` is enabled.

Output: short-leaf phase directory, updated cross-references, FEEDBACK.md path quote left verbatim (it is a historical record of what Gary actually saw).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md
@.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-RESEARCH.md
@docs/testers/gary-laben/FEEDBACK.md

<reference_data>
Confirmed by grep at planning time (2026-05-09):
- Only one in-repo file references the literal old path string outside the directory itself:
  - `docs/testers/gary-laben/FEEDBACK.md` (verbatim quote of Gary's CC error message)

The FEEDBACK.md reference is a historical quote of what Gary's git client said. It MUST stay verbatim. The renamed directory does not invalidate that record because the quote is timestamped 2026-05-08 and describes a clone that hit the old name. Do not edit this file in this plan.

Inside the directory itself, all relative cross-links keep working after rename. The CONTEXT.md `slug:` frontmatter inside the directory currently encodes the 189-char string as the slug; that needs to change to `92-trust-layer-refactor`; the descriptive form is preserved as a `## Searchability Note` body section in CONTEXT.md, NOT as a frontmatter field (per REC-11).

Long-leaf check:
  find .planning/phases -mindepth 1 -maxdepth 1 -type d | awk -F/ '{print length($NF), $NF}' | sort -rn | head -3
Currently returns 189 / 80 / 73 (top three). Post-rename, the 189 line drops out and 24 (the new 92 leaf) stays well below 60.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename phase 92 directory and update its frontmatter slug</name>
  <files>
    - .planning/phases/92-trust-layer-refactor/ (renamed from 189-char path)
    - .planning/phases/92-trust-layer-refactor/92-CONTEXT.md (slug frontmatter shortened + `## Searchability Note` body section added)
  </files>
  <read_first>
    - .planning/phases/92-refactor-constitution-and-trust-layer-formalizes-audit-driven-refactor-work-constitution-v1-1-directive-1-validation-directive-2-consolidation-directive-3-unidirectional-flow-trust-layer/ (list contents to confirm files preserved)
    - .planning/phases/92-refactor-constitution-and-trust-layer-formalizes-audit-driven-refactor-work-constitution-v1-1-directive-1-validation-directive-2-consolidation-directive-3-unidirectional-flow-trust-layer/92-CONTEXT.md (or whatever CONTEXT.md filename actually exists; ls the directory first)
    - .planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-CONTEXT.md (D-02 spec)
  </read_first>
  <action>
    1. Run `ls -la /home/jsagi/MindrianOS-Plugin/.planning/phases/92-refactor-constitution-and-trust-layer-formalizes-audit-driven-refactor-work-constitution-v1-1-directive-1-validation-directive-2-consolidation-directive-3-unidirectional-flow-trust-layer/` to confirm the directory exists and capture its file list.

    2. Use `git mv` (NOT plain `mv`) so git tracks the rename. Exact command:
       ```
       cd /home/jsagi/MindrianOS-Plugin
       git mv ".planning/phases/92-refactor-constitution-and-trust-layer-formalizes-audit-driven-refactor-work-constitution-v1-1-directive-1-validation-directive-2-consolidation-directive-3-unidirectional-flow-trust-layer" ".planning/phases/92-trust-layer-refactor"
       ```
       If `git mv` fails because the path is too long even on Linux (unlikely but possible), fall back to `git mv` invoked from inside `.planning/phases/` with relative paths.

    3. After rename, locate the CONTEXT.md inside the renamed directory (whatever filename it has -- likely `92-CONTEXT.md`). Apply two changes:

       (a) Edit frontmatter: Change `slug: 92-refactor-constitution-and-trust-layer-...` (the long form) to `slug: 92-trust-layer-refactor`. Do NOT add a `full_slug` field to frontmatter (per REC-11: avoid setting canon precedent for a one-off naming problem).

       (b) Add a body section at the top of the file titled `## Searchability Note` containing the original 189-char descriptive name. Suggested format:
       ```markdown
       ## Searchability Note

       This phase's original directory name was:

           92-refactor-constitution-and-trust-layer-formalizes-audit-driven-refactor-work-constitution-v1-1-directive-1-validation-directive-2-consolidation-directive-3-unidirectional-flow-trust-layer

       Renamed to `92-trust-layer-refactor/` 2026-05-10 per Phase 95.6 D-02 (the 189-char leaf exceeded Windows MAX_PATH and broke clones for Wave-2 testers). The original name is preserved here so future grep operations searching for the descriptive content keywords ("refactor-constitution", "trust-layer-formalizes-audit", etc.) still resolve to this phase.
       ```

       (c) If the directory has multiple files (PLAN, SUMMARY) with frontmatter that carry the long slug, apply the same `slug` -> short rewrite to each. Do NOT add `full_slug` or duplicate the Searchability Note in those files; the CONTEXT.md note is the canonical search target.

    4. If any file inside the renamed directory references `92-refactor-constitution-and-trust-layer-formalizes-...` (the long path) as a relative or absolute path string, leave those alone -- they are historical records of the old name. The `## Searchability Note` body section in CONTEXT.md carries the search hit forward.

    5. Do NOT edit `docs/testers/gary-laben/FEEDBACK.md`. The path quote in the 2026-05-09 entry is a verbatim record of what Gary saw and stays as-is.

    Per D-02: rename is hygiene + bug fix in one move. The 189-char leaf was the proximate cause of the long-path failure independent of `core.longpaths`. Even with longpaths enabled, the leaf name is a UX bug because cd-ing into the directory in any shell is awful.
  </action>
  <verify>
    <automated>find /home/jsagi/MindrianOS-Plugin/.planning/phases -mindepth 1 -maxdepth 1 -type d | awk -F/ '{print length($NF), $NF}' | sort -rn | head -1 | awk '{exit ($1 > 60)}'</automated>
  </verify>
  <acceptance_criteria>
    - `find /home/jsagi/MindrianOS-Plugin/.planning/phases -mindepth 1 -maxdepth 1 -type d -name "92-*"` returns exactly one path: `.planning/phases/92-trust-layer-refactor`
    - `find /home/jsagi/MindrianOS-Plugin/.planning/phases -mindepth 1 -maxdepth 1 -type d | awk -F/ '{print length($NF), $NF}' | sort -rn | head -1 | awk '{print $1}'` returns a number < 60
    - `ls /home/jsagi/MindrianOS-Plugin/.planning/phases/92-trust-layer-refactor/` lists the same files that existed in the old directory (no file loss)
    - `grep -l "## Searchability Note" /home/jsagi/MindrianOS-Plugin/.planning/phases/92-trust-layer-refactor/*.md` returns at least one file (the CONTEXT.md)
    - `grep -A 5 "## Searchability Note" /home/jsagi/MindrianOS-Plugin/.planning/phases/92-trust-layer-refactor/*.md` shows the body contains `refactor-constitution-and-trust-layer-formalizes`
    - `grep -l "^full_slug:" /home/jsagi/MindrianOS-Plugin/.planning/phases/92-trust-layer-refactor/*.md` returns ZERO files (frontmatter MUST NOT contain full_slug per REC-11)
    - `cd /home/jsagi/MindrianOS-Plugin && git -c diff.renames=true status --short .planning/phases/92-trust-layer-refactor/` shows the rename is staged (R lines preferred; if D + A pair appears due to slug edits exceeding 50% similarity threshold, weaken acceptance to: same file count before/after, AND `find` against old leaf path returns "No such file" while find against new leaf returns the directory)
  </acceptance_criteria>
  <done>
    Directory renamed, frontmatter `slug` shortened, a `## Searchability Note` body section in CONTEXT.md preserves the descriptive form, git tracks it as a rename. Long-leaf grep returns nothing above 60 chars. The Windows MAX_PATH failure class for phase 92 is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no in-repo path references to old leaf are dangling</name>
  <files>
    - (read-only verification; no file edits expected unless dangling references found)
  </files>
  <read_first>
    - (none - this task is verification by grep across the whole tree)
  </read_first>
  <action>
    1. Run a wide grep for the old long path string across the repo (excluding the renamed directory itself, which has historical references in its own files):
       ```
       cd /home/jsagi/MindrianOS-Plugin
       grep -rln --color=never "92-refactor-constitution-and-trust-layer" \
         --exclude-dir=node_modules \
         --exclude-dir=.git \
         --exclude-dir=.planning/phases/92-trust-layer-refactor \
         --exclude-dir=.claude/worktrees \
         . 2>/dev/null
       ```
       (`--color=never` is mandatory: without it, ANSI escape codes from a user's `~/.gitconfig` or shell color settings can leak into the output and break the downstream verify chain. Per REC-14.)

    2. Expected hits (whitelist, do NOT edit these):
       - `docs/testers/gary-laben/FEEDBACK.md` (verbatim historical quote)

    3. Any OTHER hit found must be evaluated:
       - If it is a path reference (e.g. `cat .planning/phases/92-refactor-...`), update it to the new short path.
       - If it is a historical quote inside a SUMMARY.md or research artifact, leave it alone but note it in the task SUMMARY for awareness.

    4. Re-run the grep after any edits. The only remaining hit MUST be `docs/testers/gary-laben/FEEDBACK.md`.

    5. Bonus check (optional but recommended): grep for partial fragments that would also indicate dangling references:
       ```
       grep -rln --color=never "92-refactor-constitution\|trust-layer-formalizes-audit-driven\|directive-1-validation-directive-2-consolidation" \
         --exclude-dir=node_modules \
         --exclude-dir=.git \
         --exclude-dir=.planning/phases/92-trust-layer-refactor \
         --exclude-dir=.claude/worktrees \
         . 2>/dev/null
       ```
       Same whitelist applies.

    Per D-02 acceptance: rename should leave no broken cross-links. The grep is the proof.
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && [ "$(grep -rln --color=never "92-refactor-constitution-and-trust-layer" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning/phases/92-trust-layer-refactor --exclude-dir=.claude/worktrees . 2>/dev/null | grep -v --color=never "docs/testers/gary-laben/FEEDBACK.md" | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns ONLY `docs/testers/gary-laben/FEEDBACK.md` (or zero lines if even that has been moved). No other file references the old long-form path.
    - If any non-whitelisted file is found, it has been edited in this task to use the new short path, AND a re-run of the grep confirms zero non-whitelisted hits.
    - The renamed directory `.planning/phases/92-trust-layer-refactor/` is excluded from the grep (its own CONTEXT.md `## Searchability Note` body section contains the old descriptive name, which is intentional).
  </acceptance_criteria>
  <done>
    Zero broken cross-references. The only file in the repo that still mentions the long-form path is `docs/testers/gary-laben/FEEDBACK.md`, which is a historical record. All other code paths and docs resolve to the new short path.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Run the long-leaf check:
   ```
   find /home/jsagi/MindrianOS-Plugin/.planning/phases -mindepth 1 -maxdepth 1 -type d | awk -F/ '{print length($NF), $NF}' | sort -rn | head -3
   ```
   Top three should all be < 80 (the next-longest is `03.1-data-room-dashboard-localhost-de-stijl-viewer-with-knowledge-graph-and-chat` at 80, which is OUT OF SCOPE for this plan -- it's a separate hygiene task that does not block Windows install since 80 chars + typical home path stays under MAX_PATH).

2. Run the cross-reference grep from Task 2 and confirm only the whitelisted FEEDBACK.md remains.

3. `git status --short .planning/phases/` shows the rename as `R  old -> new` (single rename line per file, not delete + add pairs).

Out-of-scope for this plan but worth noting: the `03.1-data-room-dashboard-localhost-de-stijl-viewer-with-knowledge-graph-and-chat` directory at 80 chars is the second-longest leaf. It is NOT in 95.6 scope (D-02 only locks the 92 rename). Filed as a future hygiene candidate.
</verification>

<success_criteria>
- [ ] Phase 92 directory renamed from 189-char leaf to `92-trust-layer-refactor` (24 chars)
- [ ] CONTEXT.md frontmatter `slug` shortened to `92-trust-layer-refactor`; original 189-char descriptive name preserved as a body `## Searchability Note` section (NOT as a frontmatter field, per REC-11 -- avoids canon precedent for one-off naming problems)
- [ ] Git tracks the rename (single `R` line per file, not delete + add)
- [ ] Long-leaf grep returns no path > 60 chars in `.planning/phases/`
- [ ] Cross-reference grep returns only `docs/testers/gary-laben/FEEDBACK.md` (historical quote)
- [ ] Plan 95.6-02, 95.6-03, 95.6-04 (all Wave 1) can run in parallel with this plan because they touch disjoint files
</success_criteria>

<output>
After completion, create `.planning/phases/95.6-install-cache-windows-hardening-and-skill-loop-resilience/95.6-01-SUMMARY.md` documenting:
- Files renamed (count of files inside the directory)
- Frontmatter changes (which files got `slug` shortened) + the `## Searchability Note` body section added to CONTEXT.md
- Any non-whitelisted dangling references found and fixed (expected: zero)
- The new long-leaf champion in `.planning/phases/` (expected: `03.1-data-room-dashboard-...` at 80 chars, filed as future hygiene)
</output>
