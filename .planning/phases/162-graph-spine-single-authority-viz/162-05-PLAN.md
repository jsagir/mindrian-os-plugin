---
phase: 162-graph-spine-single-authority-viz
plan: 05
type: execute
wave: 5
depends_on: [162-04]
files_modified:
  - lib/core/navigation/cooccurrence.cjs
  - lib/core/lazygraph-ops.cjs
  - lib/core/migrations/phase-162-cooccurrence-backfill.cjs
  - lib/core/navigation.cjs
  - tests/test-cooccurrence-rehome.cjs
  - tests/run-all-162.sh
autonomous: true
requirements: [R8]
milestone: v1.14.0
canon_parts: [4, 7, 8, 9]

must_haves:
  truths:
    - "The cross-file co-occurrence signal (a concept/term appearing in 2+ files) is written into room.db as typed edges, reusing DESCRIBES, not re-derived at render time (R8/D-A)"
    - "No new edge type is minted and no Canon Part 4 amendment is made (D-A LOCKED reuse); the REFERENCES member named in D-A is absent from the frozen set, so DESCRIBES is the reuse target (discrepancy flagged, not silently re-minted)"
    - "A one-time backfill migration writes existing rooms' co-occurrence edges through the migrate-* chokepoint (R8 two-clock: backfill)"
    - "An on-write hook keeps co-occurrence edges current as new artifacts are filed (R8 two-clock: on-write)"
    - "The signal survives in room.db as durable typed memory BEFORE W6 retires the build-graph node path (no signal loss mid-flight)"
  artifacts:
    - path: "lib/core/navigation/cooccurrence.cjs"
      provides: "computeCooccurrence + writeCooccurrenceEdges; the re-home primitive routed through writeEdge"
      exports: ["computeCooccurrence", "writeCooccurrenceEdges"]
    - path: "lib/core/migrations/phase-162-cooccurrence-backfill.cjs"
      provides: "idempotent one-time backfill of co-occurrence edges for existing rooms"
      exports: ["backfillCooccurrence"]
    - path: "tests/test-cooccurrence-rehome.cjs"
      provides: "2+-file term -> DESCRIBES edge; <2 files -> no edge; backfill idempotent; on-write updates"
  key_links:
    - from: "lib/core/navigation/cooccurrence.cjs"
      to: "lib/core/navigation/edges.cjs writeEdge"
      via: "writes DESCRIBES edges through the frozen-vocabulary chokepoint"
      pattern: "writeEdge"
    - from: "lib/core/lazygraph-ops.cjs artifact ingest"
      to: "writeCooccurrenceEdges"
      via: "on-write hook recomputes affected co-occurrence edges"
      pattern: "writeCooccurrenceEdges|cooccurrence"
---

<objective>
W5 of Phase 162: re-home the cross-file concept signal (a term/concept appearing in
2+ files -- currently computed only at render time by scripts/build-graph Phase 2b)
into room.db as durable typed edges, reusing the frozen DESCRIBES edge per LOCKED
decision D-A, with a two-clock migration (one-time backfill + on-write hook). This
MUST complete before W6 retires the build-graph node path, so the signal is never
lost mid-flight (the Yellow-hat signal-loss risk).

Purpose: the cross-file concept signal is trapped in the disposable meaning layer
and re-derived each render. Canon Part 9 says room.db is the authoritative memory;
this makes the concept signal first-class graph memory, not a render-time artifact,
so retiring the build-graph scanner (W6) loses nothing.

Output: the co-occurrence re-home primitive, the backfill migration, the on-write
hook, and tests. Decision D-A reuse target: DESCRIBES (REFERENCES is NOT in the
frozen set; flag the D-A naming discrepancy, do not mint a new type).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-graph-spine-single-authority-viz/162-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
@.planning/phases/162-graph-spine-single-authority-viz/162-04-SUMMARY.md

# The signal to re-home + the frozen edge vocabulary + the write chokepoint:
@scripts/build-graph
@lib/core/navigation/edges.cjs
@lib/core/lazygraph-ops.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Co-occurrence re-home primitive - compute + write DESCRIBES edges via the chokepoint (R8/D-A)</name>
  <files>lib/core/navigation/cooccurrence.cjs, lib/core/navigation.cjs, tests/test-cooccurrence-rehome.cjs, tests/run-all-162.sh</files>
  <read_first>
    - scripts/build-graph lines 456-515 (the Phase 2b concept-node algorithm: a wikilink concept appearing in 2+ DISTINCT files becomes a concept node with ref_count; this is the signal to re-home)
    - lib/core/navigation/edges.cjs lines 32-372 (ALLOWED_EDGE_TYPES frozen set + writeEdge; DESCRIBES is present, REFERENCES is NOT)
    - lib/core/lazygraph-ops.cjs lines 230-245 (getArtifactId) + 355-397 (the wikilink-extraction idiom this reuses)
    - lib/core/navigation.cjs lines 340-369 (the additive re-export idiom for the new primitive)
  </read_first>
  <behavior>
    - Test 1: a concept/term appearing in 2+ DISTINCT artifact files produces a
      Concept node (type 'Concept', already in NODE_TYPE_RENDER from W1) plus a
      DESCRIBES edge from each referencing Artifact to that Concept node, written
      through writeEdge (frozen vocabulary).
    - Test 2: a concept appearing in only 1 file produces NO Concept node and NO edge
      (the 2+-file threshold, mirroring build-graph ref_count >= 2).
    - Test 3: re-running computeCooccurrence + writeCooccurrenceEdges is idempotent
      (writeEdge ON CONFLICT upserts; no duplicate edges; ref_count refreshed).
    - Test 4: writeCooccurrenceEdges rejects a non-frozen edge type (proves no new type
      is minted; only DESCRIBES is used) -- if a future caller passes REFERENCES,
      writeEdge throws because REFERENCES is not in ALLOWED_EDGE_TYPES.
    - Test 5: the Concept node + DESCRIBES edges render through getGraphExport (W1) with
      no dangling and Concept colored per NODE_TYPE_RENDER.
  </behavior>
  <action>
    Create lib/core/navigation/cooccurrence.cjs exporting computeCooccurrence(roomDir)
    and writeCooccurrenceEdges(db, roomDir). computeCooccurrence ports the build-graph
    Phase 2b algorithm to CJS: scan artifact .md files (skip transcript.md), extract
    [[wikilinks]], slugify, count DISTINCT source files per concept, return the set of
    concepts with ref_count >= 2 and their source artifact ids (resolved to getArtifactId,
    reusing the W4 artifact resolver so the source ids are real Artifact node ids).
    writeCooccurrenceEdges upserts a Concept node per qualifying concept (type 'Concept',
    properties { ref_count, knowledge_type:'mental_model' or null }) via insertNode, and
    writes a DESCRIBES edge from each source Artifact to the Concept node via writeEdge
    (the frozen-vocabulary chokepoint -- Part 4). DESCRIBES is the D-A reuse target:
    REFERENCES (named in D-A) is NOT in ALLOWED_EDGE_TYPES, so reusing it is impossible
    without a Part 4 amendment, which D-A forbids; DESCRIBES ("describes or annotates the
    target") fits the artifact-describes-concept semantic. RECORD this REFERENCES-absent
    discrepancy in the SUMMARY (D-A said "REFERENCES/DESCRIBES, existing frozen members"
    but REFERENCES is not a frozen member; honor the LOCKED reuse intent with DESCRIBES;
    do NOT mint a new type, do NOT amend Part 4). Re-export computeCooccurrence +
    writeCooccurrenceEdges through navigation.cjs with the additive idiom + a
    surface-growth note. Add tests/test-cooccurrence-rehome.cjs; register in run-all-162.sh.

    Canon Part 8: the Concept node + DESCRIBES edge carry only the concept slug +
    scalar ref_count -- no artifact body, no user content; the existing getGraphExport
    whitelist + the W3 leak test cover the render payload.
  </action>
  <verify>
    <automated>node tests/test-cooccurrence-rehome.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-cooccurrence-rehome.cjs` prints all-pass, exits 0.
    - A 2+-file concept yields a Concept node + one DESCRIBES edge per source artifact, written via writeEdge.
    - A 1-file concept yields zero Concept nodes and zero edges.
    - `node -e "const e=require('./lib/core/navigation/edges.cjs'); if(e.ALLOWED_EDGE_TYPES.has('REFERENCES'))throw new Error('REFERENCES unexpectedly present'); if(!e.ALLOWED_EDGE_TYPES.has('DESCRIBES'))throw new Error('DESCRIBES missing'); console.log('OK reuse target')"` exits 0 (confirms the reuse target reality).
    - `grep -c "writeEdge" lib/core/navigation/cooccurrence.cjs` returns >= 1 (edges routed through the chokepoint).
    - `grep -cE "ALLOWED_EDGE_TYPES.add|new edge type|CO_OCCURS" lib/core/navigation/cooccurrence.cjs` returns 0 (no new type minted).
    - getGraphExport on a re-homed room renders Concept nodes with DESCRIBES edges, no dangling.
  </acceptance_criteria>
  <done>The cross-file concept signal is durable room.db typed memory via DESCRIBES; no new edge type; no Part 4 amendment; D-A discrepancy flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Two-clock migration - one-time backfill + on-write hook (R8)</name>
  <files>lib/core/migrations/phase-162-cooccurrence-backfill.cjs, lib/core/lazygraph-ops.cjs, tests/test-cooccurrence-rehome.cjs</files>
  <read_first>
    - lib/core/migrations/phase-162-section-nodes.cjs (the W2 migration shape: alreadyMigrated + sentinel memory_event + idempotent + chokepoint-routed)
    - lib/core/lazygraph-ops.cjs lines 340-405 (the artifact ingest path where the on-write hook attaches)
    - scripts/migrate-hats-to-roomdb.cjs lines 120-167 (alreadyMigrated + stampSentinel idiom)
  </read_first>
  <action>
    BACKFILL CLOCK: create lib/core/migrations/phase-162-cooccurrence-backfill.cjs
    exporting backfillCooccurrence(roomDir): mirror phase-162-section-nodes.cjs --
    alreadyMigrated guard (a cooccurrence_backfill sentinel memory_event), open through
    the allow-listed migrate path, call computeCooccurrence + writeCooccurrenceEdges
    inside a transaction, stamp a cooccurrence_backfill memory_event (created_by=system).
    Idempotent; never deletes; routes through navigation.cjs / the migrate chokepoint
    (no forbidden direct open).

    ON-WRITE CLOCK: in lib/core/lazygraph-ops.cjs's artifact ingest path (after the
    BELONGS_TO + wikilink-edge writes from W4), recompute the co-occurrence edges for
    the concepts touched by the newly-written artifact (call writeCooccurrenceEdges
    scoped to the affected concepts, or a cheap incremental update). This keeps the
    signal current as new artifacts file without a full rescan. Guard it so it never
    throws on the ingest hot path (best-effort, mirror the birthRoom tolerate idiom).

    Add migration + on-write coverage to tests/test-cooccurrence-rehome.cjs: (a)
    backfill on an existing room with no Concept nodes creates them and is idempotent;
    (b) filing a new artifact that pushes a concept to its 2nd file mints the DESCRIBES
    edges via the on-write hook without a manual backfill call.
  </action>
  <verify>
    <automated>node tests/test-cooccurrence-rehome.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-cooccurrence-rehome.cjs` (now including migration + on-write cases) prints all-pass, exits 0.
    - backfillCooccurrence on an existing room creates the co-occurrence edges; a second call reports already_migrated and adds zero edges.
    - Filing a new artifact that brings a concept to 2 files triggers the on-write hook and the DESCRIBES edges appear without calling backfill.
    - A cooccurrence_backfill memory_event (created_by=system) is stamped.
    - `grep -c "openRoomDbForCaller\|new Database(" lib/core/migrations/phase-162-cooccurrence-backfill.cjs` returns 0 (chokepoint-routed).
    - The on-write hook is wrapped so an exception never aborts artifact ingest.
  </acceptance_criteria>
  <done>Both clocks work: existing rooms are backfilled idempotently and new writes keep the signal current; the signal is durable in room.db before W6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .md concept text -> room.db Concept node + DESCRIBES edge | human-meaning layer feeds the authoritative graph (one-way candidate-edge feed, demoted authority) |
| migration / on-write -> room.db | write paths that must be idempotent and chokepoint-routed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-11 | Tampering | minting a new edge type bypasses the frozen Part 4 vocabulary | mitigate | reuse DESCRIBES via writeEdge (rejects non-frozen types by construction); D-A LOCKED no-amendment honored; test asserts no new type |
| T-162-12 | Denial of Service | on-write full-rescan on every artifact write | mitigate | on-write hook is scoped/incremental + best-effort wrapped; never aborts ingest |
| T-162-13 | Information Disclosure | Concept node leaks artifact body | mitigate | Concept node carries only the concept slug + scalar ref_count; getGraphExport whitelist + W3 leak test cover the render |
</threat_model>

<verification>
- node tests/test-cooccurrence-rehome.cjs -> all pass (compute + write + backfill + on-write).
- bash tests/run-all-162.sh -> 0 (W1-W4 regression fence holds).
- The reuse target is DESCRIBES (REFERENCES absent); no Part 4 amendment; no new type.
- Tri-Polar: the re-home is a room.db write (surface-agnostic); the Concept nodes +
  DESCRIBES edges then render identically on CLI canvas-graph, Desktop/Cowork dashboard,
  and any spine reader. Backfill covers existing rooms across all surfaces; the on-write
  hook fires wherever artifact ingest runs (CLI hooks + MCP write path).
</verification>

<success_criteria>
- The cross-file concept signal lives as DESCRIBES edges in room.db (durable typed memory).
- Existing rooms backfilled; new writes kept current (two-clock).
- No new edge type; no Canon Part 4 amendment (D-A honored); REFERENCES discrepancy flagged.
- The signal is proven present in room.db BEFORE W6 retires the build-graph scanner.
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols/files introduced by this plan (W5):
- `lib/core/navigation/cooccurrence.cjs` (new module)
- `computeCooccurrence(roomDir)` + `writeCooccurrenceEdges(db, roomDir)` (new functions, re-exported through navigation.cjs)
- `lib/core/migrations/phase-162-cooccurrence-backfill.cjs` + `backfillCooccurrence(roomDir)` (new migration)
- `tests/test-cooccurrence-rehome.cjs` (new test file)
- `cooccurrence_backfill` memory_event type (additive enum entry)
- the on-write co-occurrence hook in lib/core/lazygraph-ops.cjs
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/162-graph-spine-single-authority-viz/162-05-SUMMARY.md` when done.
Record the D-A reuse-target decision (DESCRIBES) and the REFERENCES-absent discrepancy.
</output>
