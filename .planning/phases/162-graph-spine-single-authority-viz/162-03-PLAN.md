---
phase: 162-graph-spine-single-authority-viz
plan: 03
type: execute
wave: 3
depends_on: [162-01, 162-02]
files_modified:
  - tests/test-graph-export-part8-leak.cjs
  - tests/test-graph-export-golden-room.cjs
  - tests/fixtures/graph-export-golden-room/.mindrian/room.db
  - scripts/check-graph-export-typemap.cjs
  - tests/run-all-162.sh
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - package.json
autonomous: false
requirements: [R5, R6]
canon_parts: [7, 8]

must_haves:
  truths:
    - "A brain-boundary-scan runs over the getGraphExport payload and proves zero correlation_id/Brain bytes by construction (R5)"
    - "An adversarial leak test (mirroring the navigation-packet Part 8 leak test) seeds hostile properties and asserts none reach the export (R5)"
    - "A golden-room snapshot test asserts unmapped_types is empty for the fixture (R5)"
    - "A node-type-map completeness gate fails CI when room.db carries a node type graph-export.cjs does not map (R5)"
    - "SEED-026 (W1+W2+W3) ships on the v1.13.x beta train via the release lockstep, no push without navigator go (R6/D-C)"
  artifacts:
    - path: "tests/test-graph-export-part8-leak.cjs"
      provides: "adversarial Part 8 leak test over the export payload (boundary-scan)"
    - path: "tests/test-graph-export-golden-room.cjs"
      provides: "golden-room snapshot asserting unmapped_types empty + stable counts"
    - path: "scripts/check-graph-export-typemap.cjs"
      provides: "node-type-map completeness gate (CI tripwire)"
      exports: ["checkTypeMap"]
    - path: "tests/run-all-162.sh"
      provides: "one-command phase gate aggregating all 162 tests"
  key_links:
    - from: "tests/test-graph-export-part8-leak.cjs"
      to: "lib/core/navigation/graph-export.cjs"
      via: "seeds hostile properties, serializes export, asserts no leak"
      pattern: "getGraphExport"
    - from: "scripts/check-graph-export-typemap.cjs"
      to: "lib/core/navigation/graph-export.cjs NODE_TYPE_RENDER + EXCLUDE_TYPES"
      via: "diffs live room.db node types against the mapped+excluded set"
      pattern: "NODE_TYPE_RENDER"
---

<objective>
W3 of Phase 162: harden the Canon Part 8 boundary over the getGraphExport BROAD
READ (it can land in a deployable present/ HTML artifact), add the CI completeness
and golden-room gates, then ship SEED-026 (W1+W2+W3) on the v1.13.x beta train per
the LOCKED decision D-C.

Purpose: getGraphExport is a whole-graph export that publishes; per the Black-hat
HIGH risk and Canon Part 8 it MUST be brain-boundary-scanned with an adversarial
leak test, a golden-room snapshot proving unmapped_types stays empty, and a
node-type-map completeness gate that fails loud on an unmapped type. Then the beta
release makes the orphan fix real for users.

Output: the leak test, the golden-room snapshot test, the type-map completeness
gate, the phase test runner, and the beta release lockstep. Route the PR through
Canon Custodian review (Part 8 PR gate).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-graph-spine-single-authority-viz/162-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
@.claude/includes/release-process.md
@.planning/phases/162-graph-spine-single-authority-viz/162-01-SUMMARY.md
@.planning/phases/162-graph-spine-single-authority-viz/162-02-SUMMARY.md

# The Part 8 leak-test pattern to mirror + the export to harden:
@tests/test-navigation-packet-part8-leak.cjs
@lib/core/navigation/graph-export.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adversarial Part 8 leak test + brain-boundary-scan over the export payload (R5)</name>
  <files>tests/test-graph-export-part8-leak.cjs</files>
  <read_first>
    - tests/test-navigation-packet-part8-leak.cjs (the 9-tripwire adversarial pattern to mirror: seed hostile values, serialize, assert absent)
    - lib/core/navigation/graph-export.cjs lines 18-24 + 184-196 (the Part 8 whitelist construction the test attacks)
    - tests/test-graph-export.cjs lines 127-144 (the existing Part 8 whitelist test -- extend, do not duplicate)
  </read_first>
  <action>
    Create tests/test-graph-export-part8-leak.cjs mirroring the
    test-navigation-packet-part8-leak.cjs tripwire idiom. Seed a tmp room.db with nodes
    whose properties carry hostile bytes across every leak vector: correlation_id,
    brain_id, a full source_path, a meeting-transcript-shaped prose blob, a personal
    identifier, a proprietary number string, and a raw secret. Then call getGraphExport,
    JSON.stringify the entire result, and assert NONE of the seeded hostile strings
    appear anywhere in the serialized payload (the boundary-scan). Also assert per-node
    that node.data has EXACTLY the whitelist keys (id, label, type, knowledge_type,
    color, degree, review_status) and no others -- a structural assertion that catches a
    future field-addition leak. Add a negative test: a node whose ALLOWED label/title is
    a benign string DOES survive (proves the scan is not over-broad). Register this in
    the phase runner.
  </action>
  <verify>
    <automated>node tests/test-graph-export-part8-leak.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-graph-export-part8-leak.cjs` prints all-pass, exits 0.
    - The test seeds at least 6 distinct hostile vectors (correlation_id, brain_id, source_path, transcript-prose, personal-identifier, proprietary-number) and asserts each is absent from JSON.stringify(result).
    - The test asserts node.data key set equals exactly {id,label,type,knowledge_type,color,degree,review_status} (no extra keys) for every node.
    - A benign title string is present in the export (negative control passes).
  </acceptance_criteria>
  <done>The export payload is adversarially proven Part 8-clean; a future field-addition leak is caught by the exact-key-set assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Golden-room snapshot + node-type-map completeness gate (R5)</name>
  <files>tests/test-graph-export-golden-room.cjs, tests/fixtures/graph-export-golden-room/.mindrian/room.db, scripts/check-graph-export-typemap.cjs, tests/run-all-162.sh</files>
  <read_first>
    - lib/core/navigation/graph-export.cjs lines 46-72 (NODE_TYPE_RENDER + EXCLUDE_TYPES -- the mapped + excluded sets the completeness gate diffs against)
    - lib/core/lazygraph-ops.cjs lines 1-60 (the nodes/edges schema for building the fixture room.db)
    - tests/run-all-158.sh (a recent phase runner shape to mirror for tests/run-all-162.sh)
  </read_first>
  <action>
    Build a committed golden-room fixture at
    tests/fixtures/graph-export-golden-room/.mindrian/room.db seeded with one node of
    EVERY type in NODE_TYPE_RENDER (claim with each knowledge_type, CausalClaim,
    assumption, decision, opportunity, governing_thought, EvidenceClaim,
    WhitespaceZone, RSDiscovery, Breakthrough, lens_finding, Artifact, Section,
    Concept) plus one of each EXCLUDE_TYPE and a representative set of typed edges.
    Create tests/test-graph-export-golden-room.cjs that runs getGraphExport on the
    fixture and asserts: unmapped_types is EMPTY (every live type is mapped); the
    excluded count equals the number of seeded bookkeeping nodes; the node + edge
    counts match a committed snapshot (stable golden numbers); zero dangling.

    Create scripts/check-graph-export-typemap.cjs exporting checkTypeMap(roomDir):
    open room.db, SELECT DISTINCT type FROM nodes, and assert every distinct type is
    either in NODE_TYPE_RENDER or in EXCLUDE_TYPES; print and exit non-zero on any
    unmapped type (the FAIL-LOUD completeness gate). Make it runnable as a CLI
    (`node scripts/check-graph-export-typemap.cjs <roomDir>`) and importable. Point it
    at the golden-room fixture in the phase runner so CI fails when a new schema phase
    adds a node type the export does not classify.

    Create tests/run-all-162.sh aggregating: test-graph-export.cjs,
    test-section-nodes-birth-and-migration.cjs, test-dashboard-graph-feed.cjs,
    test-graph-export-part8-leak.cjs, test-graph-export-golden-room.cjs, and
    `node scripts/check-graph-export-typemap.cjs tests/fixtures/graph-export-golden-room`.
    Mirror the run-all-158.sh exit-code aggregation idiom.
  </action>
  <verify>
    <automated>bash tests/run-all-162.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-162.sh` runs every 162 test + the type-map gate and exits 0.
    - The golden-room fixture room.db exists and is committed (git add -f if under a gitignored path).
    - getGraphExport on the fixture returns unmapped_types.length === 0.
    - `node scripts/check-graph-export-typemap.cjs tests/fixtures/graph-export-golden-room` exits 0; injecting a fake unmapped type into a throwaway copy makes it exit non-zero (verified in the test).
    - The golden snapshot asserts stable node/edge/excluded counts (a count change requires an intentional snapshot update).
  </acceptance_criteria>
  <done>unmapped_types is gated empty on the golden room; the completeness gate fails loud on any unmapped type; one-command phase runner green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Canon Custodian Part 8 review + ship SEED-026 on the v1.13.x beta train (R6/D-C)</name>
  <what-built>
    W1 (spine primitive + CLI repoint), W2 (dashboard repoint + D-B Section nodes +
    migration), and W3 (Part 8 leak test + golden-room snapshot + type-map gate). All
    automated gates green: bash tests/run-all-162.sh exits 0. The getGraphExport BROAD
    READ is whitelist-constructed and adversarially leak-tested.
    The release lockstep has been PREPARED but NOT pushed: CHANGELOG.md entry drafted,
    plugin.json + package.json beta version bump staged, tag prepared. No git push has
    run.
  </what-built>
  <how-to-verify>
    1. Confirm the brain-boundary-scan / leak test is sufficient for a publishing BROAD
       READ: review tests/test-graph-export-part8-leak.cjs and confirm it covers the
       vectors that matter for THIS room's data (correlation_id is the load-bearing one
       per Canon Part 8 + the intended local-vs-Brain correlation that must never
       merge). This is the Canon Custodian review the Part 8 PR gate requires for any
       change touching a Brain-adjacent broad read.
    2. Confirm the dashboard + CLI both render connected (no orphan soup) on a real room.
    3. Approve the beta version number for the v1.13.x train (SEED-026 = W1+W2+W3
       decoupled per D-C; W4-W7 are v1.14.0).
    4. Give explicit go/no-go for the push (release lockstep: CHANGELOG + plugin.json +
       package.json + git tag + marketplace ref; ships as beta first; NEVER push
       without the navigator's explicit go - CLAUDE.md hard rule).
  </how-to-verify>
  <resume-signal>Type the approved beta version + "push approved", or describe blocking issues. On approval, run the release lockstep (CHANGELOG + plugin.json + package.json + tag + marketplace ref) and push --tags.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db -> getGraphExport -> present/ HTML (published) | the broad read can publish user-correlated bytes if the whitelist regresses |
| local room.db -> Brain | the intended local-vs-Brain correlation must never merge; correlation_id must never egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-07 | Information Disclosure | getGraphExport BROAD READ published to HTML | mitigate | adversarial leak test (6+ vectors) + exact-key-set structural assertion + golden-room snapshot; Canon Custodian Part 8 review gate (Task 3 blocking-human) |
| T-162-08 | Tampering | new schema phase adds an unmapped node type that renders as 'unknown' silently in prod | mitigate | check-graph-export-typemap.cjs CI gate fails loud on any type not in NODE_TYPE_RENDER or EXCLUDE_TYPES |
| T-162-SC | Tampering | release lockstep skips a surface (version drift) | mitigate | the 5-surface release lockstep (CHANGELOG + plugin.json + package.json + tag + marketplace ref); beta-first; blocking-human go (no auto-advance) |
</threat_model>

<verification>
- bash tests/run-all-162.sh -> 0 (all tests + type-map gate).
- The golden-room fixture is committed and self-contained (no machine-specific path).
- The leak test asserts zero hostile bytes in the serialized export.
- Tri-Polar: the Part 8 boundary holds identically on CLI (W1) and Desktop/Cowork
  dashboard (W2) because both publish the SAME getGraphExport payload; the leak test
  attacks that single payload, so all three surfaces are covered by one gate.
- Release: beta-first, 5-surface lockstep, navigator go required (no push without it).
</verification>

<success_criteria>
- getGraphExport is adversarially proven Part 8-clean and Canon-Custodian-reviewed.
- The node-type-map completeness gate is wired into CI (the phase runner).
- SEED-026 (W1+W2+W3) ships on the v1.13.x beta train with the full release lockstep
  and explicit navigator approval.
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols/files introduced by this plan (W3):
- `tests/test-graph-export-part8-leak.cjs` (new adversarial leak test)
- `tests/test-graph-export-golden-room.cjs` (new golden-room snapshot test)
- `tests/fixtures/graph-export-golden-room/.mindrian/room.db` (new committed fixture)
- `scripts/check-graph-export-typemap.cjs` + `checkTypeMap(roomDir)` (new CI completeness gate)
- `tests/run-all-162.sh` (new one-command phase runner)
- A v1.13.x beta release (CHANGELOG entry + plugin.json/package.json bump + tag + marketplace ref)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/162-graph-spine-single-authority-viz/162-03-SUMMARY.md` when done.
Record the shipped beta version and the Canon Custodian review outcome.
</output>
