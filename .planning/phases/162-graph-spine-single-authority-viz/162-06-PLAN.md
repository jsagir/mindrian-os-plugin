---
phase: 162-graph-spine-single-authority-viz
plan: 06
type: execute
wave: 6
depends_on: [162-05]
files_modified:
  - scripts/build-graph
  - scripts/generate-presentation.cjs
  - scripts/serve-dashboard
  - scripts/generate-standalone
  - tests/test-build-graph-node-path-retired.cjs
  - tests/run-all-162.sh
autonomous: true
requirements: [R9]
milestone: v1.14.0
canon_parts: [6, 7]

must_haves:
  truths:
    - "The build-graph node-discovery path (filesystem/wikilink node scan + Phase 2b concept nodes) is retired AFTER W5 proved the signal survives in room.db (R9)"
    - "No viz surface (CLI canvas-graph, Desktop/Cowork dashboard) calls scripts/build-graph for nodes anymore; all source from getGraphExport (R9)"
    - "Net surface area goes DOWN (the ~700-line scan is removed or reduced to a thin shim), Canon Part 7 (R9)"
    - "The cross-file concept signal still renders (now sourced from W5 room.db DESCRIBES edges, not the retired scan)"
  artifacts:
    - path: "tests/test-build-graph-node-path-retired.cjs"
      provides: "asserts no viz path invokes build-graph for nodes; concept signal still renders from room.db"
  key_links:
    - from: "scripts/serve-dashboard + scripts/generate-standalone"
      to: "build-graph-from-sqlite.cjs (the spine writer)"
      via: "the only graph.json producer; build-graph node scan removed from the path"
      pattern: "build-graph-from-sqlite"
---

<objective>
W6 of Phase 162: retire the build-graph node-discovery path now that W5 has proven
the cross-file concept signal lives durably in room.db as DESCRIBES edges. This is
the net-surface-reduction step (Canon Part 7) that completes the single-authority
goal: the wikilink/filesystem node scanner is gone, and every viz sources nodes
from getGraphExport.

Purpose: the two-authority orphan generator was the build-graph node scan; W1-W3
repointed the viz off it, W4 fixed the ingest, W5 re-homed the only unique signal
the scan still produced. With nothing left that only build-graph provides, deleting
its node-discovery path removes drift-class surface (Canon Part 7: reuse compounds
the moat, net-new surface dilutes it). W6 MUST come after W5 (never lose the signal
mid-flight - the Yellow-hat risk).

Output: the retired build-graph node path (deleted or reduced to a thin
deprecation shim), confirmation no viz calls it for nodes, and a retirement test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-graph-spine-single-authority-viz/162-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
@.planning/phases/162-graph-spine-single-authority-viz/162-05-SUMMARY.md

# The scanner to retire + the spine writer that replaces it + the remaining callers:
@scripts/build-graph
@scripts/build-graph-from-sqlite.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit every build-graph caller + confirm the W5 signal is in room.db (R9 precondition)</name>
  <files>tests/test-build-graph-node-path-retired.cjs</files>
  <read_first>
    - scripts/build-graph (the full scanner -- the node-discovery sections: section-group ~165, artifact ~197, meeting ~254, speaker ~288, Phase 2b concept ~456-515)
    - scripts/generate-presentation.cjs lines 466-483 (collectGraph -- the dead/legacy build-graph caller; W1 took it off the render path but the function may still exist)
    - scripts/serve-dashboard line 25 + scripts/generate-standalone lines 29-35 (the dashboard graph.json producers W2 repointed)
    - lib/core/navigation/cooccurrence.cjs (the W5 re-home -- confirm the concept signal source of truth is now room.db)
  </read_first>
  <action>
    Enumerate EVERY caller of scripts/build-graph across the repo (grep -rn
    "build-graph\b" scripts/ lib/ bin/ excluding build-graph-from-sqlite). For each,
    classify: (a) viz-node path that W1/W2 already repointed (safe to retire), (b)
    non-viz consumer that needs the scan for a different reason (must be preserved or
    migrated). Confirm via a test that the cross-file concept signal now renders from
    room.db (W5 DESCRIBES edges through getGraphExport) so retiring build-graph's
    Phase-2b concept nodes loses nothing. Create
    tests/test-build-graph-node-path-retired.cjs that: (1) builds a tmp room whose
    concepts have been re-homed via W5, runs getGraphExport, and asserts the Concept
    nodes + DESCRIBES edges are present (the signal survives WITHOUT build-graph); (2)
    asserts no remaining viz producer shells out to scripts/build-graph for nodes (grep
    assertion baked into the test). Register in run-all-162.sh.
  </action>
  <verify>
    <automated>node tests/test-build-graph-node-path-retired.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-build-graph-node-path-retired.cjs` prints all-pass, exits 0.
    - The test enumerates build-graph callers and asserts every viz-node caller is repointed.
    - getGraphExport on a W5-re-homed room shows Concept nodes + DESCRIBES edges (signal present without build-graph).
    - The SUMMARY records the caller audit table (caller -> classification -> disposition).
  </acceptance_criteria>
  <done>Every build-graph caller is classified; the concept signal is confirmed durable in room.db; retirement is safe.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the build-graph node-discovery path (net surface down) (R9)</name>
  <files>scripts/build-graph, scripts/generate-presentation.cjs, scripts/serve-dashboard, scripts/generate-standalone, tests/run-all-162.sh</files>
  <read_first>
    - the Task 1 caller audit (in the 162-06-SUMMARY draft)
    - scripts/generate-presentation.cjs lines 466-485 (collectGraph + collectGraphData -- the dead two-authority functions to delete now that nothing calls them)
    - scripts/build-graph (the ~700-line scanner to remove or shim)
  </read_first>
  <action>
    Retire the build-graph node-discovery path. Per the Task 1 audit: if NO consumer
    needs the scan, delete scripts/build-graph's node-discovery body (or the whole
    script) and remove its remaining references in serve-dashboard / generate-standalone
    (those already route through build-graph-from-sqlite.cjs after W2; remove any dead
    bash build-graph fallback). If a non-viz consumer still needs it, reduce build-graph
    to a thin shim that prints a deprecation notice and exits, OR preserve only the
    minimal non-viz path -- whichever the audit dictates; in all cases the FILESYSTEM/
    WIKILINK NODE-DISCOVERY for the viz is removed. Delete the now-dead collectGraph +
    collectGraphData functions from generate-presentation.cjs (W1 took them off the
    render path; W5 made the concept signal durable; they are now fully dead). Measure
    and record the net line-count reduction (Part 7: surface goes DOWN). Confirm the
    full phase gate still passes.
  </action>
  <verify>
    <automated>bash tests/run-all-162.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-162.sh` exits 0 (all 162 tests including the retirement test).
    - `grep -rn "scripts/build-graph\b" scripts/serve-dashboard scripts/generate-standalone` returns 0 matches for the viz graph.json path (the dashboard feed is build-graph-from-sqlite only).
    - `grep -c "function collectGraph(" scripts/generate-presentation.cjs` returns 0 (dead two-authority function deleted) AND `grep -c "function collectGraphData(" scripts/generate-presentation.cjs` returns 0.
    - Net line count is lower: record `git diff --stat` showing more deletions than additions for this plan (Part 7 net-surface-down).
    - The CLI presentation graph + the dashboard both still render connected on a real room (concept signal sourced from room.db).
  </acceptance_criteria>
  <done>The build-graph node-discovery path is retired; dead two-authority functions deleted; net surface area is down; all viz sources from the spine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (removal of a scanner) | retiring code that other consumers may silently depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-14 | Denial of Service | a non-viz consumer breaks when build-graph is removed | mitigate | Task 1 caller audit enumerates every caller before deletion; non-viz needs preserved or shimmed; full phase gate re-run |
| T-162-15 | Tampering | the concept signal is lost when the scan is deleted | mitigate | W6 depends_on W5; Task 1 asserts the signal renders from room.db DESCRIBES edges WITHOUT build-graph before deletion |
</threat_model>

<verification>
- node tests/test-build-graph-node-path-retired.cjs -> all pass (signal survives; no viz caller).
- bash tests/run-all-162.sh -> 0 (full regression including W5 co-occurrence).
- git diff --stat shows net deletion (Part 7).
- Tri-Polar: CLI canvas-graph (generate-presentation) and Desktop/Cowork dashboard
  (serve-dashboard + generate-standalone) both confirmed off the build-graph node scan
  and on the spine; the retirement is verified on both viz-producing surfaces.
</verification>

<success_criteria>
- The build-graph node-discovery path is retired; net surface area decreases.
- No viz surface depends on the wikilink/filesystem node scan.
- The cross-file concept signal still renders, sourced from room.db (W5).
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols/files introduced by this plan (W6):
- `tests/test-build-graph-node-path-retired.cjs` (new retirement test)
- (DELETIONS, not additions) build-graph node-discovery body; collectGraph + collectGraphData in generate-presentation.cjs; dead bash build-graph fallbacks in serve-dashboard / generate-standalone
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/162-graph-spine-single-authority-viz/162-06-SUMMARY.md` when done.
Record the caller-audit table and the net line-count reduction.
</output>
