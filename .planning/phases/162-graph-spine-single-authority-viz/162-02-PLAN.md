---
phase: 162-graph-spine-single-authority-viz
plan: 02
type: execute
wave: 2
depends_on: [162-01]
files_modified:
  - scripts/build-graph-from-sqlite.cjs
  - scripts/serve-dashboard
  - scripts/generate-standalone
  - dashboard/index.html
  - lib/core/navigation/room-birth.cjs
  - lib/core/migrations/phase-162-section-nodes.cjs
  - tests/test-section-nodes-birth-and-migration.cjs
  - tests/test-dashboard-graph-feed.cjs
autonomous: true
requirements: [R4, R11]
canon_parts: [4, 7, 8, 9]

must_haves:
  truths:
    - "The dashboard graph.json writer sources from getGraphExport, not scripts/build-graph wikilink/filesystem scan (R4)"
    - "dashboard/index.html styles nodes by knowledge_type color + degree size and shows an edge-type gloss on tap (R4)"
    - "The >30-node cluster grouping path still works after the repoint (R4)"
    - "birthRoom writes empty Section nodes into room.db at room creation, inside the birth ACID transaction (R11/D-B)"
    - "A migration backfills Section nodes for existing rooms via the navigation chokepoint (R11/D-B)"
    - "getGraphExport export-time Section anchors demote to a Tier-0 fallback now that room.db carries real Section nodes (R11/D-B)"
  artifacts:
    - path: "scripts/build-graph-from-sqlite.cjs"
      provides: "the dashboard graph.json writer, repointed onto getGraphExport"
      contains: "getGraphExport"
    - path: "lib/core/migrations/phase-162-section-nodes.cjs"
      provides: "idempotent Section-node backfill for existing rooms, routed through navigation.cjs"
      exports: ["migrateSectionNodes"]
    - path: "tests/test-section-nodes-birth-and-migration.cjs"
      provides: "birth writes Section nodes; migration backfills; both idempotent"
    - path: "tests/test-dashboard-graph-feed.cjs"
      provides: "dashboard graph.json node/edge counts equal getGraphExport counts (no orphan-producing wikilink path)"
  key_links:
    - from: "scripts/build-graph-from-sqlite.cjs"
      to: "navigation.getGraphExport"
      via: "the dashboard graph.json writer reads the spine"
      pattern: "getGraphExport"
    - from: "lib/core/navigation/room-birth.cjs"
      to: "room.db Section nodes"
      via: "insertNode Section inside the birth BEGIN..COMMIT transaction"
      pattern: "Section"
    - from: "lib/core/migrations/phase-162-section-nodes.cjs"
      to: "navigation.cjs"
      via: "writes through the chokepoint, never a direct room.db open outside the allow-listed migrate path"
      pattern: "navigation"
---

<objective>
W2 of Phase 162: bring the Desktop/Cowork dashboard (the Cytoscape second surface)
onto the same getGraphExport spine the CLI canvas-graph already uses (W1), and
realize D-B: write REAL Section nodes into room.db at room birth plus a migration
that backfills existing rooms, so the export-time Section anchors can demote from a
primary cold-start mechanism to a Tier-0 fallback.

Purpose: close the Tri-Polar gap (CLI shipped in W1; Desktop/Cowork dashboard is
the second viz surface) and make room.db genuinely complete (Canon Part 9) so the
section scaffold is durable graph memory, not an export-time synthesis.

Output: the repointed dashboard graph.json writer + dashboard styling/gloss, the
birthRoom Section-node write, the existing-rooms migration, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-graph-spine-single-authority-viz/162-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md
@.planning/phases/162-graph-spine-single-authority-viz/162-01-SUMMARY.md

# The W2 targets and the patterns to mirror:
@lib/core/navigation/graph-export.cjs
@scripts/build-graph-from-sqlite.cjs
@lib/core/navigation/room-birth.cjs
@lib/core/migrations/phase-160-nodes-bitemporal.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: birthRoom writes Section nodes + migration backfills existing rooms (R11/D-B)</name>
  <files>lib/core/navigation/room-birth.cjs, lib/core/migrations/phase-162-section-nodes.cjs, tests/test-section-nodes-birth-and-migration.cjs</files>
  <read_first>
    - lib/core/navigation/room-birth.cjs lines 90-100 (SECTION_NAMES array) and lines 370-460 (the STEP 2 BEGIN..COMMIT ACID transaction)
    - lib/core/lazygraph-ops.cjs lines 355-361 (insertNode upsert helper for Section nodes, the NOT-NULL-safe idiom)
    - lib/core/migrations/phase-160-nodes-bitemporal.cjs (the migration module shape to mirror: idempotent, sentinel-stamped)
    - scripts/migrate-hats-to-roomdb.cjs lines 120-167 (the alreadyMigrated + stampSentinel via navigation.logMemoryEvent chokepoint idiom)
  </read_first>
  <behavior>
    - Test 1: after birthRoom on a fresh room, room.db has one Section node per canonical section (the 8 SECTION_NAMES), type='Section', review_status confirmed-by-system (system-bookkeeping carve-out, Part 9), zero artifacts required.
    - Test 2: birthRoom is idempotent on Section nodes (re-running insertNode upserts, never duplicates a Section id).
    - Test 3: migrateSectionNodes(roomDir) on an existing room that has artifacts but no Section nodes backfills exactly the missing Section nodes and is idempotent (second run is a no-op, reports already_migrated).
    - Test 4: migrateSectionNodes routes through navigation.cjs / the allow-listed migrate path and stamps a section_nodes_migration memory_event (created_by=system); it never opens room.db via a forbidden direct path.
    - Test 5: getGraphExport on a migrated room renders the Section nodes from room.db (real nodes), and the cold-start export-time anchors only fire when room.db is absent or has zero knowledge+section nodes (the Tier-0 demotion).
  </behavior>
  <action>
    In room-birth.cjs STEP 2 (inside the existing BEGIN..COMMIT, before the
    room_created event), iterate SECTION_NAMES and insertNode(conn, sectionSlug,
    'Section', { section: sectionSlug, created_by: 'system' }) using the same
    NOT-NULL-safe insertNode upsert lazygraph-ops uses at line 357-360. Section nodes
    are system-bookkeeping structural anchors, NOT truth-claim nodes, so they do not
    require a human byUser (Canon Part 9 audit-node carve-out applies to the structural
    Section type by the same reasoning as focus/audit). birthRoom MUST reach room.db
    only through the existing room-birth transaction (Canon Part 9 chokepoint), never a
    new direct open.

    Create lib/core/migrations/phase-162-section-nodes.cjs exporting
    migrateSectionNodes(roomDir): mirror phase-160-nodes-bitemporal.cjs and
    migrate-hats-to-roomdb.cjs -- check alreadyMigrated (any Section node present OR a
    section_nodes_migration sentinel event), open through the allow-listed migrate path,
    discover the room's sections (reuse section-registry.cjs discoverSections, the same
    source graph-export.cjs cold-start uses), insertNode the missing Section nodes, and
    stamp a section_nodes_migration memory_event via navigation.logMemoryEvent
    (created_by=system) inside a transaction. Idempotent; never deletes; never
    auto-runs destructively. Add section_nodes_migration to the EVENT_TYPES set if the
    schema enumerates event types (additive only; mirror the Phase 124 +2 idiom).

    In graph-export.cjs, demote the export-time Section anchors: the in-DB path already
    renders real Section nodes (NODE_TYPE_RENDER.Section exists), so no code change is
    required there beyond confirming coldStart() fires ONLY when room.db is
    absent or has zero included nodes. Add a one-line comment marking the export-time
    anchors as the Tier-0 fallback now that D-B writes real Section nodes. Do NOT
    remove the cold-start path (un-migrated rooms / no room.db still need it).
  </action>
  <verify>
    <automated>node tests/test-section-nodes-birth-and-migration.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-section-nodes-birth-and-migration.cjs` prints all-pass, exits 0.
    - After birthRoom on a fresh tmp room, `SELECT count(*) FROM nodes WHERE type='Section'` >= 8.
    - migrateSectionNodes on a room with artifacts but no Section nodes adds the missing Section nodes; a second call reports already_migrated and adds zero rows.
    - `grep -c "openRoomDbForCaller\|new Database(" lib/core/migrations/phase-162-section-nodes.cjs` returns 0 (no forbidden direct open; routes through the allow-listed migrate/navigation path).
    - `grep -c "Section" lib/core/navigation/room-birth.cjs` returns >= 1 (the birth-time Section write exists inside STEP 2).
    - A section_nodes_migration memory_event is written with created_by=system.
  </acceptance_criteria>
  <done>room.db carries real Section nodes at birth and after migration; the export-time anchors are the Tier-0 fallback; all tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the dashboard graph.json writer onto getGraphExport (R4)</name>
  <files>scripts/build-graph-from-sqlite.cjs, scripts/serve-dashboard, scripts/generate-standalone, tests/test-dashboard-graph-feed.cjs</files>
  <read_first>
    - scripts/build-graph-from-sqlite.cjs (the existing room.db-reading graph.json writer -- read in full; it already reads room.db but in its own node-builder, NOT through getGraphExport)
    - scripts/serve-dashboard line 25 (the bash build-graph call that writes dashboard/graph.json)
    - scripts/generate-standalone lines 29-35 (the build-graph call into a temp graph.json)
    - lib/core/navigation/graph-export.cjs (the export shape: elements.{nodes,edges} + counts + cold_start)
  </read_first>
  <action>
    Make build-graph-from-sqlite.cjs emit its graph.json by calling
    navigation.getGraphExport(roomDir) and writing { elements } in the Cytoscape shape
    the dashboard expects, instead of running its own node-builder. This collapses the
    dashboard onto the SAME single spine the CLI uses (Canon Part 7 reuse; Canon Part 9
    single authority). Preserve the existing CLI signature (roomDir, outputPath) and
    the stderr "N nodes, M edges -> path" line. Repoint scripts/serve-dashboard line 25
    and scripts/generate-standalone to call build-graph-from-sqlite.cjs (the spine
    writer) instead of the bash scripts/build-graph wikilink scanner for the
    dashboard/graph.json feed. Do NOT delete scripts/build-graph (W6/R9 retires the
    node-discovery path, and it depends on W5). The Part 8 whitelist rides along
    automatically because the payload IS the getGraphExport payload.

    Add tests/test-dashboard-graph-feed.cjs: build a tmp room with seeded room.db
    nodes/edges, run build-graph-from-sqlite.cjs, parse the written graph.json, and
    assert its node/edge counts equal getGraphExport(roomDir).counts and that every
    edge endpoint is present in the node set (the F1 test from the QA session:
    "assert the generated graph.html node/edge counts equal getGraphExport counts").
  </action>
  <verify>
    <automated>node tests/test-dashboard-graph-feed.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-dashboard-graph-feed.cjs` prints all-pass, exits 0.
    - `grep -c "getGraphExport" scripts/build-graph-from-sqlite.cjs` returns >= 1.
    - `grep -c "build-graph-from-sqlite" scripts/serve-dashboard` returns >= 1 (repointed) AND the bash `scripts/build-graph` call for graph.json is no longer the dashboard feed path.
    - The written graph.json parses as JSON with elements.nodes and elements.edges arrays; node count and edge count equal getGraphExport(roomDir).counts.nodes / .edges.
    - Every edge endpoint id is present in the node id set (no dangling in the dashboard feed).
  </acceptance_criteria>
  <done>The dashboard graph.json feed is spine-sourced; counts match getGraphExport; no dangling; the wikilink scanner is off the dashboard path (deletion deferred to W6).</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard styling - knowledge_type color + degree size + edge gloss + preserve >30 cluster path (R4)</name>
  <files>dashboard/index.html</files>
  <read_first>
    - dashboard/index.html lines 844-927 (initGraph + the >30-node cluster-edge synthesis path + layoutConfig)
    - dashboard/index.html lines 1402-1416 (loadGraph fetch('graph.json'))
    - lib/core/navigation/graph-export.cjs lines 36-93 (KNOWLEDGE_TYPE_COLOR + NODE_TYPE_RENDER + EDGE_GLOSS -- the values the dashboard styles against; node.data already carries color, degree, and each edge carries gloss)
  </read_first>
  <action>
    Style the Cytoscape graph from the fields getGraphExport already emits on each
    element: node color comes from node.data.color (already knowledge_type-derived for
    claims), node size scales with node.data.degree (hubs read at a glance), and on
    edge tap show edge.data.gloss (the plain-English typed-edge explanation) plus
    edge.data.type. Use node.data.color directly in the Cytoscape style mapper rather
    than re-deriving a color in the dashboard (single source of truth: the spine
    already colored it). PRESERVE the existing >30-node cluster grouping path (lines
    872-895): it strips node.data.parent and synthesizes cluster- edges for cose
    layout; this must keep working against the new spine-sourced feed (the spine emits
    no `parent` field, so confirm the cluster path degrades cleanly -- if it relied on
    parent it now no-ops, which is acceptable since spine nodes are not compound; if
    section grouping is desired at >30 nodes, synthesize cluster- edges from each
    node's Section membership using the BELONGS_TO edges already in the feed). Do NOT
    introduce a second color/degree computation; read what the spine emits.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('dashboard/index.html','utf8'); if(!/data\(['\"]?color['\"]?\)|data\.color/.test(h)) throw new Error('no color mapper'); if(!/gloss/.test(h)) throw new Error('no edge gloss surface'); if(!/totalNodes\s*>\s*30/.test(h)) throw new Error('>30 cluster path lost'); console.log('OK dashboard styling')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gloss" dashboard/index.html` returns >= 1 (edge gloss surfaced on tap).
    - The Cytoscape node style reads node.data.color (knowledge_type-derived) - verified by grep for `data('color')` or `data.color` mapper.
    - Node size maps to node.data.degree (grep for `degree` in a style/size context).
    - `grep -c "totalNodes > 30" dashboard/index.html` returns >= 1 (the cluster path is preserved).
    - Loading a spine-sourced graph.json in the dashboard renders colored, degree-sized nodes with no orphan soup (Tri-Polar Desktop/Cowork surface now matches CLI).
  </acceptance_criteria>
  <done>Dashboard styles from the spine-emitted color/degree/gloss; the >30 cluster path is intact; Desktop/Cowork viz matches the CLI viz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db -> dashboard/graph.json -> served HTML | the dashboard graph.json is a deployable artifact (generate-standalone bundles it) |
| migration -> room.db | a write path into existing rooms; must be idempotent and chokepoint-routed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-04 | Information Disclosure | dashboard graph.json payload | mitigate | inherits the getGraphExport Part 8 whitelist; no new fields added in the dashboard writer; W3/R5 adds the adversarial golden-room snapshot over the same payload |
| T-162-05 | Tampering | migration corrupts existing room.db | mitigate | idempotent insertNode upsert; alreadyMigrated guard; transaction-wrapped; never deletes; routes through the allow-listed migrate path (no forbidden direct open) |
| T-162-06 | Repudiation | silent migration with no audit | mitigate | section_nodes_migration memory_event (created_by=system) stamps every successful pass |
</threat_model>

<verification>
- node tests/test-section-nodes-birth-and-migration.cjs -> all pass.
- node tests/test-dashboard-graph-feed.cjs -> all pass (counts equal getGraphExport).
- node tests/test-graph-export.cjs still 5/5 (W1 regression fence; cold-start demotion did not break the existing tests).
- Tri-Polar: CLI (W1) + Desktop/Cowork dashboard (this plan) now both read getGraphExport.
  The migration is surface-agnostic (room.db write); the dashboard is the Desktop/Cowork
  render surface; serve-dashboard + generate-standalone cover the local + bundled cases.
</verification>

<success_criteria>
- The dashboard Cytoscape feed is spine-sourced; no dangling edges; colored by
  knowledge_type, sized by degree, gloss on tap; >30 cluster path preserved.
- room.db carries real Section nodes at birth and after migrating existing rooms.
- The export-time Section anchors are demoted to the Tier-0 fallback.
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols/files introduced by this plan (W2):
- `lib/core/migrations/phase-162-section-nodes.cjs` (new migration module)
- `migrateSectionNodes(roomDir)` (new exported function)
- `tests/test-section-nodes-birth-and-migration.cjs` (new test file)
- `tests/test-dashboard-graph-feed.cjs` (new test file)
- birthRoom Section-node write block (new code inside lib/core/navigation/room-birth.cjs STEP 2)
- `section_nodes_migration` memory_event type (additive enum entry)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/162-graph-spine-single-authority-viz/162-02-SUMMARY.md` when done.
</output>
