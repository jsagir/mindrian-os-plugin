---
phase: 162-graph-spine-single-authority-viz
plan: 04
type: execute
wave: 4
depends_on: [162-03]
files_modified:
  - lib/core/lazygraph-ops.cjs
  - .planning/seeds/SEED-031-lossy-wikilink-to-section-join.md
  - tests/test-wikilink-resolves-to-artifact.cjs
  - tests/run-all-162.sh
autonomous: true
requirements: [R7]
milestone: v1.14.0
canon_parts: [4, 9]

must_haves:
  truths:
    - "SEED-031 is filed as a seed artifact documenting the lossy section-name join debt (R7/D-D)"
    - "The [[wikilink]] edge resolves to the artifact node, not the whole section, at lazygraph-ops.cjs ingest (R7/D-D)"
    - "An unresolved wikilink fails loud (surfaced, logged, or skipped-with-signal), never mints a mis-targeted whole-section edge silently (R7/D-D)"
    - "After this fix, room.db wikilink edges target Artifact ids, so authoritative-room.db is fully true before the viz is called spine-sourced (R7)"
  artifacts:
    - path: ".planning/seeds/SEED-031-lossy-wikilink-to-section-join.md"
      provides: "the filed SEED-031 debt artifact"
    - path: "tests/test-wikilink-resolves-to-artifact.cjs"
      provides: "wikilink resolves to artifact node; unresolved fails loud; no whole-section edge minted"
  key_links:
    - from: "lib/core/lazygraph-ops.cjs wikilink ingest"
      to: "Artifact node id"
      via: "resolve [[link]] to the artifact, not json_extract section match"
      pattern: "Artifact"
---

<objective>
W4 of Phase 162 (first v1.14.0 plan): fix SEED-031, the lossy wikilink-to-section
edge join. room.db ITSELF currently mints INFORMS/CONTRADICTS edges from
[[wikilinks]] via a section-name join (lazygraph-ops.cjs line 379) that targets
EVERY artifact in a whole section, not the linked artifact. Resolve the wikilink to
the specific artifact node; fail loud on an unresolved link instead of minting a
mis-targeted edge.

Purpose: per LOCKED decision D-D, "authoritative room.db" must be fully true before
the viz is called spine-sourced. The W1-W3 spine fix removed orphans but the
underlying ingest still mis-targets edges; this closes that debt so the spine reads
correct edges.

Output: the SEED-031 artifact, the resolver fix in lazygraph-ops.cjs, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-graph-spine-single-authority-viz/162-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md

# The lossy join to fix + the artifact-id derivation it must use:
@lib/core/lazygraph-ops.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: File SEED-031 (the lossy section-name join debt) (R7/D-D)</name>
  <files>.planning/seeds/SEED-031-lossy-wikilink-to-section-join.md</files>
  <read_first>
    - .planning/seeds/SEED-026-graph-viz-from-roomdb-typed-edges.md (the seed format to mirror)
    - lib/core/lazygraph-ops.cjs lines 367-397 (the lossy join: line 379 SELECT id FROM nodes WHERE type='Artifact' AND json_extract(properties,'$.section')=target, then loops ALL matched artifacts minting INFORMS/CONTRADICTS)
    - .planning/research/local-graph-spine-vs-wikilink-authority-research.md Section 7b (the MEDIUM NEW DEFECT bullet describing this)
  </read_first>
  <action>
    Write .planning/seeds/SEED-031-lossy-wikilink-to-section-join.md mirroring the
    SEED-026 format (frontmatter kind:seed, status:open, canon_parts:[4,9], severity,
    proving_case, source). Document the defect precisely: at lazygraph-ops.cjs:379 the
    [[wikilink]] resolver does `SELECT id FROM nodes WHERE type='Artifact' AND
    json_extract(properties,'$.section') = <slugified-link>` -- this matches the link
    text against the SECTION property, so a link to one artifact mints INFORMS (and
    CONTRADICTS when nearby contradiction terms appear) to EVERY artifact in that
    section, not the linked artifact. Record the required acceptance (resolve to the
    specific artifact; fail loud on unresolved) and that D-D LOCKED the fix to W4.
    Commit with git add -f (.planning is gitignored, the established pattern).
  </action>
  <verify>
    <automated>test -f .planning/seeds/SEED-031-lossy-wikilink-to-section-join.md && grep -c "lazygraph-ops.cjs" .planning/seeds/SEED-031-lossy-wikilink-to-section-join.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/seeds/SEED-031-lossy-wikilink-to-section-join.md exists.
    - It cites lazygraph-ops.cjs line 379 and the json_extract section-name join as the defect.
    - Frontmatter has kind:seed, status:open, canon_parts including 4 and 9, severity.
    - It states the D-D LOCKED fix scope (resolve to artifact, fail loud on unresolved).
  </acceptance_criteria>
  <done>SEED-031 is filed and committed; the debt is documented before the fix lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve wikilink edges to the artifact node, fail loud on unresolved (R7/D-D)</name>
  <files>lib/core/lazygraph-ops.cjs, tests/test-wikilink-resolves-to-artifact.cjs, tests/run-all-162.sh</files>
  <read_first>
    - lib/core/lazygraph-ops.cjs lines 230-245 (getArtifactId -- the slash-preserving nested artifact id derivation; the resolver must target THIS id space)
    - lib/core/lazygraph-ops.cjs lines 355-397 (the BELONGS_TO write + the wikilink loop to fix)
    - CLAUDE.md decision 16 (nested artifact folder structure: section/artifact-name/artifact-name.md -- so a wikilink names an artifact, resolvable to a getArtifactId)
  </read_first>
  <behavior>
    - Test 1: a [[link]] that names a real artifact mints an INFORMS edge from the
      source artifact to THAT artifact's node id only (not to sibling artifacts in the
      same section).
    - Test 2: with two artifacts in one section and a [[link]] to one of them, the
      OTHER artifact receives NO edge (the lossy whole-section fan-out is gone).
    - Test 3: a [[link]] that resolves to no artifact node fails loud -- it does NOT
      mint any edge, and it surfaces the unresolved link (return/collect an
      unresolved[] list and/or log a warning), never a silent mis-target.
    - Test 4: the CONTRADICTS branch (nearby contradiction terms) also targets only
      the resolved artifact, not the whole section.
    - Test 5: a benign resolved link still mints exactly one INFORMS edge (no regression
      in the happy path).
  </behavior>
  <action>
    Replace the section-name join at lazygraph-ops.cjs:378-396. Resolve each
    [[wikilink]] to a specific Artifact node: slugify the link text and match it
    against the artifact id space (getArtifactId / the trailing artifact-name segment),
    NOT against the section property. Prefer an exact id/last-segment match; if multiple
    artifacts share a name across sections, that is an ambiguous-resolution case -- per
    the research "start exact-match + fail-loud" -- treat ambiguous as unresolved
    (fail loud), do not fan out. When resolved, mint INFORMS (and CONTRADICTS when the
    nearby-contradiction-term heuristic fires) to that single artifact id only. When
    unresolved (no match or ambiguous), mint NO edge and record the link in an
    unresolved collection that the caller surfaces (the "fail loud" requirement);
    optionally console.warn the unresolved link. Preserve the BELONGS_TO write and the
    ENABLES branch unchanged. Add tests/test-wikilink-resolves-to-artifact.cjs and
    register it in tests/run-all-162.sh.

    Canon note: this tightens room.db's own ingest so wikilink-derived edges target
    artifacts (Part 9 authoritative room.db) using the frozen edge vocabulary
    (INFORMS/CONTRADICTS already in ALLOWED_EDGE_TYPES -- Part 4, no new type).
  </action>
  <verify>
    <automated>node tests/test-wikilink-resolves-to-artifact.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-wikilink-resolves-to-artifact.cjs` prints all-pass, exits 0.
    - `grep -c "json_extract(properties, '\$.section') = ?" lib/core/lazygraph-ops.cjs` returns 0 for the WIKILINK resolver (the section-name join is removed from the wikilink path; the BELONGS_TO section upsert is unaffected).
    - A link to one artifact in a 2-artifact section mints an edge to exactly one node; the sibling gets zero edges.
    - An unresolved link mints zero edges and is surfaced (unresolved list non-empty or a warning logged).
    - `bash tests/run-all-162.sh` still exits 0 (no regression in W1-W3 gates).
  </acceptance_criteria>
  <done>Wikilink edges target the resolved artifact; the lossy whole-section fan-out is gone; unresolved links fail loud; SEED-031 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .md wikilink text -> room.db typed edge | untrusted human authoring crosses into the authoritative graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-09 | Tampering | mis-targeted edges pollute the authoritative graph | mitigate | resolve to a single artifact id; ambiguous/unresolved fails loud (no edge), never fans out across a section |
| T-162-10 | Repudiation | silent unresolved link loses the human-meaning signal | mitigate | unresolved links are collected/surfaced (fail loud), so a missing edge is visible, not lost |
</threat_model>

<verification>
- node tests/test-wikilink-resolves-to-artifact.cjs -> all pass.
- bash tests/run-all-162.sh -> 0 (W1-W3 regression fence holds).
- Tri-Polar: this is an ingest-layer (room.db write) fix, surface-agnostic; it corrects
  the edges that all three viz surfaces (CLI canvas-graph, Desktop/Cowork dashboard,
  and any spine reader) consume. The fix lands once at the chokepoint and all surfaces
  inherit correct edges.
</verification>

<success_criteria>
- room.db wikilink-derived edges target artifacts, not sections.
- Unresolved wikilinks fail loud; no mis-targeted whole-section edges are minted.
- SEED-031 is filed and closed; authoritative room.db is fully true.
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols/files introduced by this plan (W4):
- `.planning/seeds/SEED-031-lossy-wikilink-to-section-join.md` (new seed artifact)
- `tests/test-wikilink-resolves-to-artifact.cjs` (new test file)
- the rewritten wikilink resolver in lib/core/lazygraph-ops.cjs (artifact-targeted, fail-loud)
- an unresolved-links surface (return field / warning) from the ingest path
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/162-graph-spine-single-authority-viz/162-04-SUMMARY.md` when done.
</output>
