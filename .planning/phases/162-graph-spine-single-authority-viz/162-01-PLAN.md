---
phase: 162-graph-spine-single-authority-viz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/graph-export.cjs
  - lib/core/navigation.cjs
  - scripts/generate-presentation.cjs
  - tests/test-graph-export.cjs
autonomous: true
requirements: [R1, R2, R3]
status: implemented-pending-commit
branch: fix/seed-026-graph-export-spine
canon_parts: [4, 7, 8, 9]

must_haves:
  truths:
    - "getGraphExport(roomDir) sources nodes AND edges from room.db in ONE id space (R1)"
    - "An edge ships only when BOTH endpoints are in the included node set (no dangling, structurally) (R1)"
    - "memory_event / focus / audit bookkeeping types are excluded and counted, never silently dropped (R1)"
    - "Unknown node types render loud (default color) + flagged in unmapped_types, never thrown, never dropped (R1)"
    - "Cold-start (no room.db, or zero knowledge nodes) emits Section anchors so a Tier-0 room never renders blank (R1)"
    - "The export payload is a strict whitelist (no raw properties blob, no correlation_id, no source_path) - Canon Part 8 (R1)"
    - "generate-presentation.cjs canvas-graph sources from getGraphExport, not scripts/build-graph + room.db edge bolt-on (R2)"
    - "5/5 graph-export tests pass (R3)"
  artifacts:
    - path: "lib/core/navigation/graph-export.cjs"
      provides: "getGraphExport spine primitive + KNOWLEDGE_TYPE_COLOR + NODE_TYPE_RENDER + EXCLUDE_TYPES + EDGE_GLOSS"
      exports: ["getGraphExport", "KNOWLEDGE_TYPE_COLOR", "NODE_TYPE_RENDER", "EXCLUDE_TYPES", "EDGE_GLOSS"]
      contains: "async function getGraphExport"
    - path: "tests/test-graph-export.cjs"
      provides: "5 tests: no-orphan invariant, cold-start, unknown-type, Part 8 whitelist, real-room integration"
      contains: "assertNoDangling"
  key_links:
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/graph-export.cjs"
      via: "getGraphExport: graphExport.getGraphExport re-export"
      pattern: "getGraphExport: graphExport.getGraphExport"
    - from: "scripts/generate-presentation.cjs"
      to: "navigation.getGraphExport"
      via: "collectGraphExport child-process call"
      pattern: "nav.getGraphExport"
    - from: "lib/core/navigation/graph-export.cjs"
      to: "lib/core/lazygraph-ops.cjs"
      via: "openGraph + queryGraph read primitives (reuse, never a second scanner)"
      pattern: "lgOps.(openGraph|queryGraph)"
---

<objective>
W1 of Phase 162: ship the single whole-graph spine read primitive that kills the
two-authority orphan generator, and repoint the CLI presentation canvas-graph
onto it.

THIS PLAN IS AN AS-BUILT RETROFIT. W1 was implemented and verified this session
on branch `fix/seed-026-graph-export-spine` (NOT yet committed). The tasks below
describe what already shipped; each acceptance criterion matches a real artifact
and verification evidence already gathered. Do NOT re-design W1. Reconcile the
plan to the code, then commit.

Purpose: room.db (Canon Part 9, the local mind) becomes the single node-identity
authority for the viz, so dangling edges and orphan soup are structurally
impossible. This is the SEED-026 critical-path fix.

Output: lib/core/navigation/graph-export.cjs (new spine primitive), the
navigation.cjs re-export with the Part 6 surface-growth note, the
generate-presentation.cjs repoint, and 5/5 passing tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-graph-spine-single-authority-viz/162-CONTEXT.md
@.planning/research/local-graph-spine-vs-wikilink-authority-research.md
@.planning/seeds/SEED-026-graph-viz-from-roomdb-typed-edges.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md

# As-built code to reconcile against (already on the working tree):
@lib/core/navigation/graph-export.cjs
@tests/test-graph-export.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile graph-export.cjs spine primitive (AS-BUILT)</name>
  <files>lib/core/navigation/graph-export.cjs</files>
  <read_first>
    - lib/core/navigation/graph-export.cjs (the shipped module - read in full)
    - lib/core/lazygraph-ops.cjs lines 1-60 (openGraph/queryGraph/closeGraph + schema; the read primitives this reuses per Part 7)
    - lib/core/lazygraph-ops.cjs lines 355-397 (the wikilink-to-section INFORMS/CONTRADICTS minting -- DO NOT touch in W1; it is W4/SEED-031)
  </read_first>
  <action>
    Confirm the shipped lib/core/navigation/graph-export.cjs matches the R1 contract;
    do NOT rewrite it. The module exports getGraphExport(roomDir, opts) which: (a)
    opens room.db through lgOps.openGraph (reuse, never a second scanner - Canon Part
    9 + Part 7); (b) reads `SELECT * FROM nodes LIMIT nodeCap+1` (SELECT * so it is
    robust to schema variance across migration phases - review_status may be absent on
    unmigrated rooms); (c) excludes EXCLUDE_TYPES (memory_event, focus, audit,
    memory_artifact, navigator_persona, HatState, requirement, planning_artifact),
    counting them in counts.excluded; (d) maps node.type via NODE_TYPE_RENDER and
    colors claim nodes by knowledge_type via KNOWLEDGE_TYPE_COLOR; (e) flags unknown
    types into unmapped_types and renders them with DEFAULT_COLOR + classes 'unknown'
    (loud-but-graceful, never thrown, never dropped); (f) emits an edge ONLY when both
    endpoints are in the included nodeIndex (the no-dangling guarantee), deriving
    degree from edge incidence; (g) returns the strict whitelist payload per node
    (id, label, type, knowledge_type, color, degree, review_status) - NEVER the raw
    properties blob, source paths, or correlation_id (Canon Part 8 structural
    boundary); (h) cold-starts to Section anchors when room.db is absent OR has zero
    knowledge nodes; (i) caps at DEFAULT_NODE_CAP (5000) with a 15s child-process
    timeout enforced by the caller, and degrades gracefully (ok:false, empty
    elements) on any throw rather than crashing the hot /mos:present path.
    Verify the module never opens room.db directly (only via lgOps) and never imports
    correlation.cjs or any Brain wire. If a discrepancy with this contract exists,
    fix the code to match; otherwise leave byte-unchanged.
  </action>
  <verify>
    <automated>node -e "const g=require('./lib/core/navigation/graph-export.cjs'); const ks=Object.keys(g); if(!ks.includes('getGraphExport')||!ks.includes('EXCLUDE_TYPES')||!ks.includes('KNOWLEDGE_TYPE_COLOR')||!ks.includes('EDGE_GLOSS')) throw new Error('missing export'); if(typeof g.getGraphExport!=='function') throw new Error('getGraphExport not a function'); console.log('OK exports')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./lib/core/navigation/graph-export.cjs')"` loads with no error.
    - Exports include getGraphExport, KNOWLEDGE_TYPE_COLOR, NODE_TYPE_RENDER, EXCLUDE_TYPES, EDGE_GLOSS.
    - `grep -c "require('../correlation" lib/core/navigation/graph-export.cjs` returns 0 (no Brain/correlation wire).
    - `grep -c "properties:" lib/core/navigation/graph-export.cjs` shows the raw properties blob is never placed on node.data (only id/label/type/knowledge_type/color/degree/review_status are emitted).
    - EXCLUDE_TYPES contains memory_event, focus, audit (verified by: `node -e "const g=require('./lib/core/navigation/graph-export.cjs'); ['memory_event','focus','audit'].forEach(t=>{if(!g.EXCLUDE_TYPES.has(t))throw new Error('missing '+t)}); console.log('OK excludes')"`).
  </acceptance_criteria>
  <done>graph-export.cjs matches the R1 contract; loads clean; Part 8 whitelist + Part 9 spine-only reads confirmed by grep.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile navigation.cjs re-export + Part 6 surface-growth note (AS-BUILT)</name>
  <files>lib/core/navigation.cjs</files>
  <read_first>
    - lib/core/navigation.cjs lines 340-369 (the getGraphExport re-export block + the SURFACE-GROWTH NOTE)
    - lib/core/navigation.cjs lines 1-5 (the "closed 13-function surface" header that the note reconciles)
  </read_first>
  <action>
    Confirm navigation.cjs re-exports getGraphExport: graphExport.getGraphExport
    (line ~368) with the additive-re-export idiom matching writeEdge / logMemoryEvent
    / getRoomContext. Confirm the SURFACE-GROWTH NOTE is present and explains that
    navigation.cjs documents a "closed 13-function" API but has grown additively past
    it, that getGraphExport adds NO new taxonomy and opens NO new wire, and that a
    future canon pass should reconcile the stated count (this is the Canon Part 6
    dog-fooding tracking note -- the count drift is flagged, not silent, so the Part 6
    dog-fooding scan does not raise a CONTRADICTS against the canon). Leave
    byte-unchanged if already correct.
  </action>
  <verify>
    <automated>node -e "const nav=require('./lib/core/navigation.cjs'); if(typeof nav.getGraphExport!=='function') throw new Error('getGraphExport not re-exported'); console.log('OK re-export')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const nav=require('./lib/core/navigation.cjs'); if(typeof nav.getGraphExport!=='function')throw new Error('x')"` exits 0.
    - `grep -c "SURFACE-GROWTH NOTE" lib/core/navigation.cjs` returns >= 1.
    - `grep -c "getGraphExport: graphExport.getGraphExport" lib/core/navigation.cjs` returns 1.
  </acceptance_criteria>
  <done>navigation.getGraphExport resolves to the spine primitive; the surface-growth tracking note is present (Part 6 dog-fooding honored).</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile generate-presentation.cjs repoint + run the 5 tests (AS-BUILT, R2 + R3)</name>
  <files>scripts/generate-presentation.cjs, tests/test-graph-export.cjs</files>
  <read_first>
    - scripts/generate-presentation.cjs lines 556-590 (collectGraphExport child-process helper)
    - scripts/generate-presentation.cjs lines 710-730 (the repointed call site replacing collectGraph + collectGraphData)
    - tests/test-graph-export.cjs (all 5 tests, read in full)
  </read_first>
  <action>
    Confirm collectGraphExport(roomDir) (line ~564) runs navigation.getGraphExport in
    a 15s child process (the same synchronous-generator idiom the old collectGraphData
    used) and that the main() call site (line ~718) sources `graph` and `graphData`
    from getGraphExport rather than from collectGraph (the scripts/build-graph
    wikilink/filesystem scan) + collectGraphData (the room.db edge bolt-on). The old
    two-authority functions (collectGraph at ~466, collectGraphData at ~485) may remain
    defined as dead code for this plan but MUST NOT be called on the graph path; W1's
    scope is the repoint, not deletion (build-graph node-path retirement is W6/R9, which
    depends on W5). Then run the test suite and confirm 5/5 pass. On a machine with the
    AION room at ~/MindrianRooms/aion-eureka-synergy the integration test reports the
    real numbers; on a machine without it the integration test self-skips (still 5/5
    reported, one skipped) -- both are acceptable per the as-built evidence.
  </action>
  <verify>
    <automated>node tests/test-graph-export.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-graph-export.cjs` prints "5 passed, 0 failed" and exits 0.
    - `grep -c "nav.getGraphExport" scripts/generate-presentation.cjs` returns >= 1.
    - `grep -c "collectGraphExport(roomDir)" scripts/generate-presentation.cjs` returns >= 1 (the repointed call site).
    - The graph path no longer calls collectGraph on the main render path: confirm `collectGraphExport` (not `collectGraph(`) feeds the `graph` variable at the main() call site (line ~718).
    - As-built evidence to record in SUMMARY: on the real AION room the export reported "93 nodes, 81 edges, zero dangling, unmapped_types=[]"; intelligence smoke 6/0; navigation acceptance 1/1; chokepoint hook 7/7; substrate guard no new violation.
  </acceptance_criteria>
  <done>generate-presentation canvas-graph sources from the spine; 5/5 tests pass; the old wikilink node path is no longer on the render path (deletion deferred to W6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db -> present/ HTML | the export payload can land in a deployable, publicly-served HTML artifact |
| room.db -> Brain | MUST NOT cross; getGraphExport is a LOCAL read only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01 | Information Disclosure | getGraphExport payload in present/ HTML | mitigate | strict whitelist payload (id/label/type/knowledge_type/color/degree/review_status); raw properties blob, source_path, correlation_id never emitted; Test 4 (Part 8 whitelist) asserts no LEAK-123 / do-not-egress / properties / source_path / correlation_id in serialized output. Full adversarial hardening is W3/R5. |
| T-162-02 | Denial of Service | unbounded SELECT * on hot /mos:present path | mitigate | DEFAULT_NODE_CAP 5000 with LIMIT nodeCap+1 + capped flag; 15s child-process timeout in collectGraphExport; graceful ok:false on throw |
| T-162-03 | Tampering | unknown node type silently dropped | mitigate | unknown types render loud (default color) + unmapped_types flag; never dropped, never thrown |
</threat_model>

<verification>
- node tests/test-graph-export.cjs -> 5 passed, 0 failed.
- node -e require checks on graph-export.cjs and navigation.cjs load clean.
- No correlation/Brain import in graph-export.cjs (grep == 0).
- Tri-Polar: this plan is the CLI surface (generate-presentation canvas-graph). The
  Desktop/Cowork dashboard Cytoscape surface is W2 (162-02); both read the SAME
  getGraphExport spine, so the export is surface-agnostic by construction.
</verification>

<success_criteria>
- getGraphExport is the single whole-graph viz feed over room.db in one id space.
- The CLI presentation graph renders connected (zero structural orphans) on a real room.
- 5/5 tests green.
- Plan reconciled to as-built code and committed on fix/seed-026-graph-export-spine.
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols/files introduced by this plan (W1):
- `lib/core/navigation/graph-export.cjs` (new file)
- `getGraphExport(roomDir, opts)` (new exported function, re-exported through navigation.cjs)
- `KNOWLEDGE_TYPE_COLOR`, `NODE_TYPE_RENDER`, `EXCLUDE_TYPES`, `EDGE_GLOSS` (new exported maps)
- `tests/test-graph-export.cjs` (new test file, 5 tests)
- `collectGraphExport(roomDir)` (new helper in scripts/generate-presentation.cjs)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/162-graph-spine-single-authority-viz/162-01-SUMMARY.md` when done.
Note in the SUMMARY: this plan is implemented-pending-commit; the execution work is
the reconcile + commit on branch fix/seed-026-graph-export-spine.
</output>
