---
phase: 212-eureka-substrate-grounding-guard
plan: 04
type: execute
wave: 3
depends_on: [212-02]
files_modified:
  - evals/eureka/opportunity-drafts/pair-1-arrhythmias.md
  - evals/eureka/opportunity-drafts/pair-2-cerebral-aneurysm.md
  - tests/test-212-gold-cards.cjs
  - tests/test-212-plurai-leg.cjs
  - tests/run-all-212.sh
autonomous: true
requirements: [212-D3, 212-D6, 212-D7]
tags: [eureka, critic, gold-set, plurai, gate]
must_haves:
  truths:
    - "All 6 SEED-050 gold cards run through classifyCandidate (stub-judge derived from gold_label) and land on their expected verdicts; archimedes-sterling (lean_checkable) is asserted FIRST as the objective anchor (D3)"
    - "The two JHU Opportunity Statements exist as read-only fixtures under evals/eureka/opportunity-drafts/, personal names stripped, quantities marked catalog-sourced - read as fixtures, NEVER written to any room"
    - "The deployed Plurai classifier leg degrades to SKIP + baseline_deferred when the key is missing or the endpoint 404s - never a red CI failure (navigator Q3 lock)"
    - "bash tests/run-all-212.sh composes D7 legs (a) fixture suite, (b) negative corpus, (c) Part 8 scan, (d) D5 check, with run_if file guards so a partially-landed phase SKIPs instead of failing"
  artifacts:
    - path: "evals/eureka/opportunity-drafts/pair-1-arrhythmias.md"
      provides: "First real acceptance fixture (C16796 x C03552, room entry 2026-07-06 section 4)"
    - path: "evals/eureka/opportunity-drafts/pair-2-cerebral-aneurysm.md"
      provides: "Second real acceptance fixture (C16742 x C05004)"
    - path: "tests/test-212-gold-cards.cjs"
      provides: "D7(a) fixture suite: 6 cards vs expected verdicts"
    - path: "tests/test-212-plurai-leg.cjs"
      provides: "Optional deployed-classifier leg with SKIP degrade"
    - path: "tests/run-all-212.sh"
      provides: "The D7 phase gate aggregator (run-all-211.sh model)"
  key_links:
    - from: "tests/test-212-gold-cards.cjs"
      to: "evals/eureka/cases/"
      via: "gray-matter frontmatter parse (house stack)"
      pattern: "gray-matter"
    - from: "tests/run-all-212.sh"
      to: "tests/test-212-*.cjs"
      via: "run_if file-guarded legs"
      pattern: "run_if"
    - from: "tests/test-212-plurai-leg.cjs"
      to: "lab/eval/report-from-transcript.cjs"
      via: "callJudge reuse (Part 7, Don't Hand-Roll)"
      pattern: "callJudge"
---

<objective>
Ship the phase's acceptance substrate: the D7(a) gold-card fixture suite against the 6 SEED-050 cards (D3: calibration base is the existing gold set, NOT a new corpus), the two real JHU Opportunity Statement fixtures (the critic's first real-world acceptance workload per 212-RESEARCH - currently flagged "not yet critic-verified" in the room entry), the OPTIONAL Plurai deployed-classifier leg (navigator Q3 lock: the local two-stage critic is the core; Plurai is a separately-gated leg that degrades to SKIP - the endpoint 404s today), and the run-all-212.sh gate aggregator composing all D7 legs.

Decision coverage: D3 (6 gold cards, sterling as the lean-checkable anchor validated first), D6 (negative corpus wired as a gate leg), D7 (the full aggregator: legs a-d), plus the ROADMAP goal's Plurai evaluator line honored as the optional leg (upload/Optimize deferred with the pending 211 human-labeled corpus - a data dependency, not a build gap).

Scope note (navigator Q1 lock): critic-only phase; the substrate/whitespace half of the ROADMAP goal moves to Phase 212.5 (orchestrator-registered stub). Runs in Wave 3 parallel with plan 03 (zero file overlap; run_if guards make the aggregator safe if legs land in either order).

Purpose: the phase cannot claim a working Grounding Guard without scoring it against its ground truth and its first real workload.
Output: 2 fixtures + 3 test surfaces + the phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-RESEARCH.md
@evals/eureka/README.md
@lib/core/eureka-critic.cjs
@tests/run-all-211.sh
@tests/test-211-judge-gate.cjs
@lab/eval/report-from-transcript.cjs

The two draft Opportunity Statements live in the dev-research room: ~/MindrianRooms/rethinking-mindrianos/research/2026-07-06-jhtv-d15-real-room-test-and-opportunity-formula/2026-07-06-jhtv-d15-real-room-test-and-opportunity-formula.md section 4. Fixtures are LAB-SIDE ONLY (never ship to users, never egress); this plan READS the room entry and writes repo fixtures - it never writes to any room.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The two JHU Opportunity Statement fixtures (the critic's first real acceptance workload)</name>
  <files>evals/eureka/opportunity-drafts/pair-1-arrhythmias.md, evals/eureka/opportunity-drafts/pair-2-cerebral-aneurysm.md</files>
  <read_first>
    - ~/MindrianRooms/rethinking-mindrianos/research/2026-07-06-jhtv-d15-real-room-test-and-opportunity-formula/2026-07-06-jhtv-d15-real-room-test-and-opportunity-formula.md section 4 (the two draft statements, verbatim source) and the Cross-references block (the "treat both as unverified Eurekas, not yet bankable" flag this fixture set exists to replace)
    - evals/eureka/README.md (the no-real-names HARD RULE and its hash-deny-list enforcement in tests/test-211-case-cards.cjs Test 4; the card frontmatter house style)
    - evals/eureka/cases/archimedes-sterling.md (frontmatter idiom to mirror where sensible)
  </read_first>
  <action>
    Create evals/eureka/opportunity-drafts/ with the two fixtures. Each file: YAML frontmatter + the statement body copied from room entry section 4 with these transforms: (1) STRIP all personal inventor surnames to role descriptors ("the two originating JHU labs" / "the originating lab") per the repo's no-real-names discipline - institution names (JHU Technology Ventures), public catalog IDs (C16796, C03552, C16742, C05004), technology titles, and manufacturer-class mentions stay (public catalog data, lab-side fixture); (2) no em-dashes (convert to hyphens). Frontmatter keys: draft_id (pair-1-arrhythmias / pair-2-cerebral-aneurysm), source_pair (the CxxxxxXCxxxxx ids), source_entry (the room entry path + section), quantities_sourced true (every figure traces to the JHU catalog descriptions - this feeds stageA opts.sourcedQuantities so the unsourced-quantity gate does not auto-kill a catalog-sourced figure; D2/D6 discipline preserved for genuinely unsourced figures), expected_verdict pending_human_review (these are EXACTLY the unverified Eurekas the phase exists to rule on - no gold label is invented here; plan 05's checkpoint produces the human ruling), status unverified_eureka. A header comment in each body: fixtures are read-only acceptance inputs; the critic reads them, nothing writes them back to any room.
  </action>
  <verify>
    <automated>node -e "const m=require('gray-matter'); const fs=require('fs'); for (const f of ['evals/eureka/opportunity-drafts/pair-1-arrhythmias.md','evals/eureka/opportunity-drafts/pair-2-cerebral-aneurysm.md']) { const d=m(fs.readFileSync(f,'utf8')); if(d.data.quantities_sourced!==true) throw new Error(f+': quantities_sourced'); if(d.data.expected_verdict!=='pending_human_review') throw new Error(f+': expected_verdict'); if(d.content.length<400) throw new Error(f+': body too thin'); } console.log('fixtures ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both fixture files parse via gray-matter with quantities_sourced true and expected_verdict pending_human_review (the verify one-liner exits 0)
    - Zero personal surnames from the room entry survive: grep for the inventor surnames listed in room entry section 4 returns 0 matches across evals/eureka/opportunity-drafts/ (run the grep with the names read from the room entry; do not copy the names into the test or the SUMMARY)
    - `node tests/test-211-case-cards.cjs` still exits 0 (the hash deny-list untouched by the new directory)
    - `grep -rcP '\x{2014}' evals/eureka/opportunity-drafts/*.md` returns 0 per file (no em-dashes)
  </acceptance_criteria>
  <done>The critic's first real workload is pinned as two read-only lab fixtures with honest pending_human_review status, name-stripped and source-stamped.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-212-gold-cards.cjs - the D7(a) fixture suite (D3)</name>
  <files>tests/test-212-gold-cards.cjs</files>
  <read_first>
    - evals/eureka/cases/ all 6 cards' frontmatter (gold_label.salient values, distractors lists, dials.critic_available - derive the expected-verdict table from disk, do not invent labels)
    - evals/eureka/README.md (card schema table; the label vocabulary)
    - lib/core/eureka-critic.cjs (classifyCandidate signature, stub seams: opts.encodeFn, opts.judgeFn, opts.knnFn, opts.sourcedQuantities)
    - tests/test-211-case-cards.cjs (gray-matter loading idiom for the cards + the house assert style)
  </read_first>
  <action>
    Write tests/test-212-gold-cards.cjs: load all 6 cards from evals/eureka/cases/ via gray-matter. For each card build a candidate from hypothesis_in + destination (mechanismText = destination; the card's gold_label.salient is the expected verdict when it is one of the four critic verdicts). Judge stub: a deterministic judgeFn derived from the card's gold_label (a transferable gold answers rubric items consistently positive with specific evidence; a restatement/general_shallow gold answers the matching failing item) - the suite verifies the PIPELINE ROUTING (verdict-by-code, disagreement handling, Stage A ordering), while plan 05 verifies real-judge accuracy. Assertions: (1) archimedes-sterling FIRST, standalone, with an explicit comment that its lean_checkable ground truth is the objective anchor (D3: validate the judge HERE before trusting it on human-judged cases); (2) each remaining card's routed verdict equals its expected verdict; (3) card distractors labeled general_shallow route to general_shallow or pseudoscience under an honest-generic stub judge; distractor labels outside the four-verdict enum (e.g. knowledge_gap_question - Phase 213 question-type scope) are explicitly SKIPPED with a printed note, not silently dropped; (4) cards still marked validated candidate are surfaced in the summary line as calibration-pending (Q2 honesty: labels await the 211-04/211-05 human checkpoints). Zero network, zero model load, exit non-zero on any mismatch.
  </action>
  <verify>
    <automated>node tests/test-212-gold-cards.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-212-gold-cards.cjs` exits 0; output names all 6 cards with routed vs expected verdicts and prints sterling first
    - `grep -c "sterling" tests/test-212-gold-cards.cjs` >= 2 (anchor-first logic is explicit)
    - `grep -c "gray-matter" tests/test-212-gold-cards.cjs` >= 1 (house parser, not a hand-rolled frontmatter reader)
    - `grep -c "https\?://" tests/test-212-gold-cards.cjs` returns 0 (offline)
  </acceptance_criteria>
  <done>D7 leg (a) exists: the 6-card fixture suite scores the pipeline against expected verdicts with the lean-checkable anchor validated first.</done>
</task>

<task type="auto">
  <name>Task 3: Optional Plurai leg + the run-all-212.sh gate aggregator (D7, navigator Q3 lock)</name>
  <files>tests/test-212-plurai-leg.cjs, tests/run-all-212.sh</files>
  <read_first>
    - tests/test-211-judge-gate.cjs (the SKIP + baseline_deferred degrade pattern to mirror exactly: missing key OR endpoint error never red-fails)
    - lab/eval/report-from-transcript.cjs (callJudge, JUDGES, endpointUrl exports - the Part 7 reuse; do not hand-roll a REST client)
    - tests/run-all-211.sh (the aggregator model: set -uo pipefail, run/run_if helpers, PASS/FAIL/SKIP counters, the NODE_OPTIONS offline-preload guard, the EGRESS RULE header comment)
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md section D7
  </read_first>
  <action>
    Write tests/test-212-plurai-leg.cjs: the OPTIONAL deployed-classifier signal (navigator Q3 lock: local two-stage critic is the core; this leg is separately gated). Reuse callJudge/JUDGES from lab/eval/report-from-transcript.cjs on SYNTHETIC gold-card text ONLY (Part 8 EGRESS RULE: Plurai is build/CI only, never runtime, never real-room content). When PLURAI_API_KEY is absent OR the endpoint errors/404s (its state today per evals/plurai/211-baseline.json): print SKIPPED, write/refresh evals/plurai/212-baseline.json with status baseline_deferred, exit 0 - the sanctioned degrade path from test-211-judge-gate.cjs, never a red failure.

    Write tests/run-all-212.sh modeled line-for-line on run-all-211.sh (set -uo pipefail, run/run_if, PASS/FAIL/SKIP, the NODE_OPTIONS eureka-offline-preload guard, no em-dashes). Legs, each run_if-guarded on its test file and labeled with its D7 letter: (a) test-212-gold-cards.cjs [fixture suite vs expected verdicts]; (b) test-212-negative-corpus.cjs [D6 junk rejected]; (c) test-212-part8-boundary.cjs [Part 8 boundary scan - also carries leg (d), the D5 per-call-resolution CHECK 4; comment maps both letters to this file]; plus supporting legs: test-212-critic-stage-a.cjs, test-212-critic-rubric.cjs, test-212-plurai-leg.cjs [optional, SKIP-degrading]. Footer echoes PASS/FAIL/SKIP totals and exits non-zero when FAIL > 0. Header comment restates: the phase gate is run-all-212 green + the plan 05 human calibration checkpoint (Q2 lock: the >=0.85 accuracy bar is a human-verify leg, not an automated assertion).
  </action>
  <verify>
    <automated>bash tests/run-all-212.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/run-all-212.sh` exits 0 with FAIL=0 (SKIPs allowed for the Plurai leg and any plan-03 legs not yet landed in this wave)
    - `grep -v '^#' tests/run-all-212.sh | grep -c "run_if"` >= 5 (file-guarded legs, not hard-wired paths)
    - `grep -c "eureka-offline-preload" tests/run-all-212.sh` >= 1 (zero-network guard inherited)
    - `node tests/test-212-plurai-leg.cjs` exits 0 on this machine today (endpoint 404s -> SKIP + baseline_deferred written)
    - `bash tests/run-all-211.sh` still green (no generator regression)
  </acceptance_criteria>
  <done>The D7 gate aggregator composes all legs with honest SKIP degradation; the Plurai leg exists as an optional signal that cannot block the phase.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `evals/eureka/opportunity-drafts/pair-1-arrhythmias.md` | acceptance fixture | frontmatter: draft_id, source_pair, quantities_sourced, expected_verdict, status |
| `evals/eureka/opportunity-drafts/pair-2-cerebral-aneurysm.md` | acceptance fixture | same schema |
| `tests/test-212-gold-cards.cjs` | D7(a) fixture suite | sterling-first anchor assertion, 6-card verdict table |
| `tests/test-212-plurai-leg.cjs` | optional deployed-judge leg | SKIP degrade, writes evals/plurai/212-baseline.json (baseline_deferred) |
| `tests/run-all-212.sh` | the D7 phase gate | run/run_if legs a-d + supporting suites, PASS/FAIL/SKIP contract |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room research entry -> repo fixtures | real names must not cross; only role descriptors + public catalog data land in the tracked repo |
| synthetic gold text -> Plurai endpoint | the only sanctioned network egress, build/CI only, synthetic only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-14 | Info disclosure | fixture files (real names) | mitigate | surname strip at authoring + test-211-case-cards deny-list stays green |
| T-212-15 | Info disclosure | Plurai leg payload | mitigate | synthetic gold-card text only; EGRESS RULE header restated in the aggregator (run-all-211 precedent) |
| T-212-16 | DoS (CI fragility) | deployed-judge dependency | mitigate | SKIP + baseline_deferred degrade; endpoint 404 can never fail the gate |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (212-RESEARCH audit table); no install task |
</threat_model>

<verification>
- `bash tests/run-all-212.sh` exit 0, FAIL=0
- `node tests/test-212-gold-cards.cjs` exit 0 with sterling asserted first
- `node tests/test-211-case-cards.cjs` exit 0 (no-real-names deny-list green)
- `bash tests/run-all-211.sh` exit 0 (no regression)
</verification>

<success_criteria>
- D7 legs a-d all composed under one aggregator with the run-all-211 SKIP semantics
- The two real JHU drafts are staged for plan 05's human ruling without any invented gold label
- The optional Plurai leg cannot block shipping (Q3 lock honored)
</success_criteria>

<output>
Create `.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-04-SUMMARY.md` when done.
</output>
