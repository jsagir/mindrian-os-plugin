---
phase: 212-eureka-substrate-grounding-guard
plan: 02
type: execute
wave: 2
depends_on: [212-01]
files_modified:
  - lib/core/eureka-critic.cjs
  - evals/eureka/212-critic-baseline.json
  - tests/test-212-critic-rubric.cjs
  - tests/test-212-negative-corpus.cjs
autonomous: true
requirements: [212-D2, 212-D3, 212-D6]
tags: [eureka, critic, rubric, llm-judge]
must_haves:
  truths:
    - "The verdict enum is computed BY CODE from the rubric item pattern; the LLM never picks the class (D2 item 2)"
    - "Exactly 2 judge calls per candidate (neutral + adversarial); ANY item disagreement routes to general_shallow with tag rubric_disagreement (D2 item 3)"
    - "Confidence is calibration-derived from evals/eureka/212-critic-baseline.json buckets; an unseen rubric pattern returns confidence unknown (D2 item 4)"
    - "tahini x blockchain 0.825, wind turbines 0.985, and Molecular Casino $2-5B ALL classify as pseudoscience or general_shallow end-to-end (D6, explicit acceptance test)"
    - "criticRule is a pure function of the D1 payload: no fs writes, no roomDir, no content"
  artifacts:
    - path: "lib/core/eureka-critic.cjs"
      provides: "Stage B rubric + verdict-by-code + classifyCandidate + criticRule"
      exports: ["RUBRIC_ITEMS", "runRubric", "verdictFromRubric", "classifyCandidate", "criticRule"]
    - path: "evals/eureka/212-critic-baseline.json"
      provides: "Calibration buckets keyed by rubric pattern, embedding_model-stamped"
      contains: "baseline_deferred"
    - path: "tests/test-212-critic-rubric.cjs"
      provides: "Stubbed-judge rubric/verdict/confidence unit suite"
    - path: "tests/test-212-negative-corpus.cjs"
      provides: "D6 negative corpus acceptance test (the shipping bar)"
  key_links:
    - from: "lib/core/eureka-critic.cjs"
      to: "evals/eureka/212-critic-baseline.json"
      via: "criticRule bucket lookup"
      pattern: "212-critic-baseline"
    - from: "tests/test-212-negative-corpus.cjs"
      to: "lib/core/eureka-critic.cjs"
      via: "classifyCandidate with crafted stub encodeFn + stub judgeFn"
      pattern: "classifyCandidate"
---

<objective>
Build Stage B of the Grounding Guard: the 6-item binary rubric (rubric-not-Likert, per D2 item 2 and the Rubrics-as-Rewards evidence), the two-pass judge protocol (neutral + adversarial, exactly 2 calls, D2 item 3), verdict-by-code (`verdictFromRubric`), the composed `classifyCandidate` pipeline (Stage A -> Stage B -> verdict), and `criticRule` - the calibration ruling that is the ONLY logic the Phase-03 MCP tool will wrap. Ship the D6 negative corpus as an explicit acceptance test: the three confirmed-junk generator outputs on record must be rejected.

Decision coverage: D2 items 2-5 (rubric, 2 calls, calibration-derived confidence, never expose persuasive framing), D3 (calibration base = the 6 SEED-050 gold cards; baseline file stamped with embedding_model per navigator Q4 lock so a future embedder swap invalidates cleanly - the eureka_meta precedent), D3b item 3 (coarse confidence return), D6 (negative corpus as acceptance test, the minimum shipping bar).

The rubric LLM step runs LOCALLY (it needs content; content egress would breach Canon Part 8 - 212-RESEARCH Architectural Responsibility Map). Only abstracted features + the rubric boolean pattern ever reach criticRule. In tests the judge is a stub judgeFn (the 211 encodeFn seam precedent); the real judge is the local Claude session, exercised in plan 05.

Purpose: this is the core of the phase - the verifier that separates a REAL transferable salient from confident noise.
Output: complete local critic pipeline + calibration skeleton + D6 acceptance suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-RESEARCH.md
@lib/core/eureka-critic.cjs
@data/eureka-critic-tags.json
@evals/eureka/README.md
@.planning/seeds/SEED-050-eureka-eval-salient-verifier-judge-synthetic-trust.md

Boundary: do NOT modify lib/core/eureka/vector-store.cjs (parallel quick 260706-5b7) or lib/mcp/tool-router.cjs (plan 03's exclusive file).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RUBRIC_ITEMS + runRubric two-pass protocol + verdictFromRubric + classifyCandidate (D2 items 2-3, 5)</name>
  <files>lib/core/eureka-critic.cjs, tests/test-212-critic-rubric.cjs</files>
  <read_first>
    - lib/core/eureka-critic.cjs (plan 01 output - the file being extended; note stageA return shape and tags loading)
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-RESEARCH.md "Pattern 2" (the verdictFromRubric mapping to implement) and "Anti-Patterns to Avoid"
    - .planning/seeds/SEED-050-eureka-eval-salient-verifier-judge-synthetic-trust.md (expert grounding for rubric item wording - items are expert-grounded, not model-invented, per Claude's Discretion note in 212-RESEARCH user_constraints)
    - tests/test-212-critic-stage-a.cjs (house test idiom + the stub seams already established)
  </read_first>
  <behavior>
    - Test 1: verdictFromRubric is a pure total function over 6-item patterns: !f or !c -> pseudoscience; else !e -> restatement; else !d -> general_shallow; else a and b and c -> transferable; else general_shallow (bias to reject on ambiguity - Pitfall 1)
    - Test 2: runRubric calls the injected judgeFn EXACTLY twice (call-count assertion) - one neutral framing, one adversarial framing ("argue this is generic filler; complete a counter-mapping")
    - Test 3: identical item answers across both passes -> the agreed pattern flows to verdictFromRubric
    - Test 4: ANY per-item disagreement between passes -> verdict general_shallow, reasoning_tag rubric_disagreement, rubric_pattern records 'x' at each disagreed position
    - Test 5: the prompt strings runRubric builds contain the mechanism/mapping material but NEVER the differential score, band, or any excitement/framing metadata (D2 item 5: never expose the candidate's persuasive framing; assert the neutral prompt string does not contain the digits of differential_score)
    - Test 6: classifyCandidate short-circuits: a Stage-A-failing candidate never invokes judgeFn (call-count 0)
  </behavior>
  <action>
    Extend lib/core/eureka-critic.cjs:
    - `RUBRIC_ITEMS` - frozen array of 6 binary items keyed a-f, wording grounded in SEED-050's documented failure modes: (a) shared relational schema statable WITHOUT domain nouns; (b) one-to-one element mapping with no orphans; (c) mechanism yields a checkable consequence; (d) adds something over the nearest graph edge / prior knowledge; (e) NOT a restatement (claim differs from the source restated in target vocabulary - the #1 job, Pitfall 2); (f) no unsourced quantities or statistics in the claim. Each item carries {id, question, evidence_instruction:'one sentence of evidence'} - the LLM answers items, code computes the class (D2 item 2).
    - `runRubric(candidate, opts)` - async; opts.judgeFn(promptString) -> parsed {a..f booleans, evidence strings} (parsing helper included, tolerant of markdown formatting); builds TWO prompts via internal buildNeutralPrompt/buildAdversarialPrompt helpers from candidate.mechanismText + the mapping statement ONLY (no scores, no band, no provenance - D2 item 5). Exactly 2 calls, never more (anti-pattern: panels give 2.2 effective votes). Compare item-by-item; disagreement handling per behavior Test 4.
    - `verdictFromRubric(items)` - the Pattern 2 mapping, exported pure.
    - `classifyCandidate(candidate, opts)` - the composed pipeline: await stageA; on fail return its route/tag immediately with rubric_pattern 'xxxxxx' (judge never reached); on pass, runRubric then verdictFromRubric; returns {verdict, reasoning_tag, rubric_pattern, features, stage:'A'|'B'}.
    Write tests/test-212-critic-rubric.cjs per the behavior list (stub judgeFn everywhere; zero network; zero model load).
  </action>
  <verify>
    <automated>node tests/test-212-critic-rubric.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-212-critic-rubric.cjs` exits 0 with a "passed" summary line
    - `grep -c "RUBRIC_ITEMS" lib/core/eureka-critic.cjs` >= 1 and item count assertion in test pins exactly 6 items
    - `grep -c "adversarial" lib/core/eureka-critic.cjs` >= 1 (second pass exists)
    - judgeFn call-count assertions present: `grep -c "callCount\|calls" tests/test-212-critic-rubric.cjs` >= 2
    - `grep -c "modelcontextprotocol\|server\.tool" lib/core/eureka-critic.cjs` returns 0 (D4 still holds after extension)
  </acceptance_criteria>
  <done>The full local two-stage pipeline exists: Stage A gates, two-pass rubric with disagreement-to-general_shallow, verdict computed by code, judge never sees scores or framing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: criticRule calibration ruling + evals/eureka/212-critic-baseline.json (D2 item 4, D3, D3b item 3)</name>
  <files>lib/core/eureka-critic.cjs, evals/eureka/212-critic-baseline.json, tests/test-212-critic-rubric.cjs</files>
  <read_first>
    - lib/core/eureka-critic.cjs (current state after Task 1)
    - evals/eureka/README.md (gold-set provenance, card schema, COMPRESSION formula context)
    - evals/eureka/cases/archimedes-sterling.md frontmatter (gold_label shape; sterling is critic_available lean_checkable - the objective anchor per D3)
    - 211-04/211-05 SUMMARY sections on baseline_deferred (the honest-deferral precedent this file follows): .planning/phases/211-eureka-generator-mvp-tri-modal-room-db-sqlite-vec-xenova-all/211-04-SUMMARY.md and 211-05-SUMMARY.md
  </read_first>
  <behavior>
    - Test 7 (appended to test-212-critic-rubric.cjs): criticRule with a known bucket pattern returns {verdict, confidence in ['low','medium','high'], reasoning_tag} - confidence COARSE, never a raw float (D3b item 3)
    - Test 8: an unseen rubric_pattern returns confidence 'unknown' + reasoning_tag 'calibration_unknown' (routes to human review, D2 item 4)
    - Test 9: criticRule validates the payload through the same closed-key discipline as assembleCriticPayload (rejects unknown keys, rejects any string field not in the closed enums) and never reads process.cwd(), fs paths outside evals/eureka, or roomDir
    - Test 10: a payload whose schema_version mismatches the tags file returns confidence 'unknown' (versioned enums, Canon Part 9)
  </behavior>
  <action>
    Create `evals/eureka/212-critic-baseline.json`: { schema_version: 1, status: 'baseline_deferred', embedding_model: 'MongoDB/mdbr-leaf-ir', rubric_version: 1, buckets: {}, note } - buckets keyed by rubric_pattern string -> {n, correct, verdict_observed}. Status stays baseline_deferred until plan 05's human checkpoint approves (the 211 honest-deferral pattern). embedding_model is stamped per navigator Q4 lock: Stage A thresholds and calibration lock against mdbr-leaf-ir; a future embedder swap must invalidate this file cleanly (the eureka_meta precedent from quick 260706-13z).

    Extend lib/core/eureka-critic.cjs with `criticRule(payload, opts)` - the ONE function plan 03's MCP wrapper calls (D4 thin-wrapper contract). Pure function of the payload: validate shape (closed keys: the D1 six + rubric_pattern + schema_version; TypeError on anything else), load buckets from evals/eureka/212-critic-baseline.json (opts.baselinePath injectable for tests), map rubric_pattern -> bucket -> coarse confidence banding (bucket accuracy >=0.9 high, >=0.7 medium, else low; document the bands in a comment as calibration-derived, never model-self-reported - D2 item 4). Stage-A-forced patterns ('xxxxxx' with a deterministic reasoning_tag) return confidence 'high' by construction with a comment justifying it (programmatic gates are code-certain, not judge-estimated). Local calibration validity guard: classifyCandidate (not the wire) checks encoderProvenance().model === baseline.embedding_model before trusting buckets; mismatch -> confidence 'unknown' (Pitfall 8).
  </action>
  <verify>
    <automated>node tests/test-212-critic-rubric.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const b=JSON.parse(require('fs').readFileSync('evals/eureka/212-critic-baseline.json','utf8')); if(b.status!=='baseline_deferred'||b.embedding_model!=='MongoDB/mdbr-leaf-ir') process.exit(1)"` exits 0
    - `node tests/test-212-critic-rubric.cjs` exits 0 (Tests 1-10 all green)
    - `grep -c "confidence" lib/core/eureka-critic.cjs` >= 3 and `grep -c "'unknown'" lib/core/eureka-critic.cjs` >= 1
    - criticRule returns no float confidence: test asserts typeof confidence === 'string'
  </acceptance_criteria>
  <done>criticRule is the complete, pure, payload-only ruling function with calibration-derived coarse confidence and honest unknown routing; the baseline file exists in its deferred state stamped with the locked embedder.</done>
</task>

<task type="auto">
  <name>Task 3: D6 negative-corpus acceptance test - the minimum shipping bar</name>
  <files>tests/test-212-negative-corpus.cjs</files>
  <read_first>
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md section D6 (the three confirmed-junk examples, verbatim)
    - .planning/seeds/SEED-050-eureka-eval-salient-verifier-judge-synthetic-trust.md (the recorded junk output text: tahini x blockchain 0.825, wind turbines as living weather algorithms 0.985, Molecular Casino $2-5B exit - pull the exact recorded phrasings)
    - lib/core/eureka-critic.cjs (classifyCandidate signature + seams)
    - tests/test-212-critic-stage-a.cjs (the crafted stub encodeFn technique for swap-invariance)
  </read_first>
  <action>
    Write tests/test-212-negative-corpus.cjs: three named candidates built from the SEED-050 recorded failures (synthetic/recorded text only - these are generator outputs already on record, not room content):
    1. "Molecular Casino... $2-5B exit" with differential metadata and sourcedQuantities false -> MUST route pseudoscience via the fabricated-quantity gate ("5B" matches the KMB regex), judgeFn call-count 0.
    2. "tahini x blockchain" differential 0.825 -> under a crafted stub encodeFn that embeds noun-stripped text (so noun swaps produce near-zero shift), MUST route general_shallow via domain_swap_invariant.
    3. "wind turbines as living weather algorithms" differential 0.985 -> MUST route pseudoscience or general_shallow; if it survives Stage A under the stub, the stub judgeFn answers items honestly-generic (no checkable consequence, no one-to-one mapping) and verdictFromRubric MUST NOT return transferable.
    Final assertion: ALL THREE verdicts are in the set {pseudoscience, general_shallow} - this is the explicit D6 shipping bar, written as an acceptance test, not a manual spot-check. Zero network, zero model load, exit non-zero on any junk candidate classified transferable or restatement.
  </action>
  <verify>
    <automated>node tests/test-212-negative-corpus.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-212-negative-corpus.cjs` exits 0 and the output names all three candidates with their routed verdicts
    - `grep -c "tahini\|turbine\|Casino" tests/test-212-negative-corpus.cjs` >= 3
    - `grep -c "transferable" tests/test-212-negative-corpus.cjs` >= 1 (the not-transferable assertion is explicit)
  </acceptance_criteria>
  <done>The three real observed failures are pinned as automated regressions; the critic cannot ship while blessing any of them.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `lib/core/eureka-critic.cjs` (extended) | critic core Stage B + ruling | `RUBRIC_ITEMS` (6 items a-f), `runRubric()`, `verdictFromRubric()`, `classifyCandidate()`, `criticRule()` |
| `evals/eureka/212-critic-baseline.json` | calibration bucket store | schema_version, status baseline_deferred, embedding_model 'MongoDB/mdbr-leaf-ir', rubric_version, buckets{} |
| `tests/test-212-critic-rubric.cjs` | stubbed-judge unit suite | Tests 1-10 incl. exactly-2-calls and coarse-confidence pins |
| `tests/test-212-negative-corpus.cjs` | D6 acceptance suite | 3 named junk candidates, not-transferable bar |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate content -> rubric prompts | judge sees mechanism/mapping ONLY, never scores or framing (sycophancy channel) |
| rubric pattern -> criticRule | abstracted booleans + D1 scalars; the only material eligible to cross the wire later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-05 | Spoofing (sycophancy) | runRubric prompts | mitigate | prompts built from mechanism text only; Test 5 asserts differential digits absent; adversarial second pass |
| T-212-06 | Info disclosure | criticRule confidence return | mitigate | coarse string bands only (D3b3); Test asserts non-float |
| T-212-07 | Repudiation (silent misgrade) | verdictFromRubric | mitigate | verdict by code, total function, bias-to-reject default; D6 suite pins the observed failure classes |
| T-212-08 | Elevation (unseen pattern trusted) | bucket lookup | mitigate | unseen pattern -> confidence 'unknown' -> human review route (D2 item 4) |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (212-RESEARCH audit table); no install task |
</threat_model>

<verification>
- `node tests/test-212-critic-rubric.cjs` exit 0
- `node tests/test-212-negative-corpus.cjs` exit 0
- `node tests/test-212-critic-stage-a.cjs` still exit 0 (no Stage A regression from the extension)
- `bash tests/run-all-211.sh` still green
</verification>

<success_criteria>
- classifyCandidate implements the complete D2 design: gates -> rubric -> code-verdict -> calibration confidence
- criticRule is provably pure and payload-only (plan 03 wraps it without modification)
- The D6 bar is an automated test that fails the build on regression
</success_criteria>

<output>
Create `.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-02-SUMMARY.md` when done.
</output>
