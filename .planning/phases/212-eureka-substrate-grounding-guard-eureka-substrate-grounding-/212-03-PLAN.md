---
phase: 212-eureka-substrate-grounding-guard
plan: 03
type: execute
wave: 3
depends_on: [212-02]
files_modified:
  - lib/mcp/tool-router.cjs
  - tests/test-212-part8-boundary.cjs
autonomous: true
requirements: [212-D1, 212-D3B, 212-D4, 212-D5, 212-D7]
tags: [eureka, critic, mcp, part8, cirs]
must_haves:
  truths:
    - "A new eureka_critic MCP tool is registered on the EXISTING tool-router surface and returns {verdict, confidence(coarse), reasoning_tag} for a valid D1 payload (D4)"
    - "The tool's zod schema accepts ONLY the closed D1 wire shape: bounded numbers + closed enums + rubric_pattern; any free-text or unknown key is rejected at the schema (D1, D3b)"
    - "The eureka_critic handler takes ZERO dependency on the registration-time roomDir closure (D5) - grep-assertable"
    - "Near-identical consecutive payloads are deduped and calls are rate-limited per process (D3b item 4)"
    - "tests/test-212-part8-boundary.cjs reuses the connector-part8 scan idiom and covers D7 legs (c) and (d)"
  artifacts:
    - path: "lib/mcp/tool-router.cjs"
      provides: "Thin eureka_critic tool wrapper (10th server.tool)"
      contains: "eureka_critic"
    - path: "tests/test-212-part8-boundary.cjs"
      provides: "Part 8 boundary scan + D5 per-call-resolution check"
      min_lines: 80
  key_links:
    - from: "lib/mcp/tool-router.cjs"
      to: "lib/core/eureka-critic.cjs"
      via: "require + criticRule call (wrapper stays thin, logic stays portable)"
      pattern: "eureka-critic\\.cjs"
    - from: "tests/test-212-part8-boundary.cjs"
      to: "lib/mcp/tool-router.cjs"
      via: "source-scan assertions (the test-connector-part8-boundary.cjs grep idiom)"
      pattern: "tool-router"
---

<objective>
Expose the Grounding Guard ruling as a thin MCP tool on the EXISTING `bin/mindrian-mcp-server.cjs` / `lib/mcp/tool-router.cjs` surface (D4: plugin-local first, architected for the SEED-014 Brain-repo lift - the wrapper is thin, `criticRule` stays portable in lib/core/eureka-critic.cjs). Ship the phase's Part 8 boundary scan and the D5 per-call-resolution check as an automated test (D7 legs c and d).

Decision coverage: D1 (wire schema = scalars + closed enums only), D3b item 4 (rate-limit + dedupe on the wrapper), D4 (thin wrapper on the existing surface, zero interface rewrite needed for the SEED-014 lift), D5 (zero registration-time roomDir closure - the exact stale-room bug the 2026-07-05 mcp-first addendum documented on the existing tools must not recur on this new surface), D7c (Part 8 boundary scan reusing the test-connector-part8-boundary.cjs pattern, not a parallel scanner), D7d (the D5 check).

Canon Part 11 (born WIRED or EXCLUDED): registration through `registerRouterTools` on the one governed MCP path IS the wiring; the tool carries an explicit hitl_shape:none declaration comment (pure ruling function with no Decision-Gate fork of its own - the human gate on eureka verdicts lives downstream in Phase 213's eureka-reach wiring, which is exactly where CONTEXT scopes it). The declaration + registry gates are run as no-drift proofs.

Scope note (navigator Q1 lock): critic-only phase; substrate/whitespace work is Phase 212.5 (orchestrator-registered stub), not here. The rubric LLM never sits behind this boundary - only the abstracted ruling call does (212-RESEARCH load-bearing rule).

Purpose: the correct moat boundary per the 02-moat-embedding-audit verdict - generator local/open, critic ruling MCP-side, only 20-60 bits cross.
Output: eureka_critic tool + Part 8/D5 boundary test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-RESEARCH.md
@lib/mcp/tool-router.cjs
@bin/mindrian-mcp-server.cjs
@lib/core/eureka-critic.cjs
@tests/test-connector-part8-boundary.cjs
@tests/test-205-surface-fence.cjs

Load-bearing coupling discovered at planning time: `tests/test-205-surface-fence.cjs` pins `ALL_TOOL_COMMANDS` unique membership at exactly 65 ("mints no new tool"). `ALL_TOOL_COMMANDS` exists for CLI<->MCP command parity of the 64 routed CLI commands; eureka_critic has NO CLI command twin (its CLI surface is scripts/eureka-critic-run.cjs, plan 05; its programmatic consumer is Phase 213). Therefore: do NOT add eureka_critic to ALL_TOOL_COMMANDS and do NOT touch the 65 pin - register the tool directly via server.tool and document why in a comment at the registration site. If any other fence unexpectedly trips, STOP and surface it in the SUMMARY rather than silently widening a pin.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the eureka_critic tool - thin wrapper, hardened schema, rate-limit + dedupe (D1, D3b4, D4, D5)</name>
  <files>lib/mcp/tool-router.cjs</files>
  <read_first>
    - lib/mcp/tool-router.cjs lines 90-200 (command enums + ALL_TOOL_COMMANDS composition + its parity docstring) and lines 343-400 (registerRouterTools signature + the room_state server.tool registration idiom: zod shape, async handler, textResponse)
    - lib/core/eureka-critic.cjs (criticRule signature + loadCriticTags - the tags file supplies the closed enums for the zod schema)
    - data/eureka-critic-tags.json (domain_tags, verdicts - the enum sources)
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md sections D1, D3b, D4, D5
    - tests/test-205-surface-fence.cjs lines 135-141 (the 65-pin you must NOT trip)
  </read_first>
  <action>
    Inside registerRouterTools in lib/mcp/tool-router.cjs, append a 10th registration: server.tool('eureka_critic', description string naming it the Grounding Guard calibration ruling (scalars + enums in, coarse ruling out; no content ever), with a zod shape built from the closed enums:
    - differential_score: z.number().min(-1).max(1)
    - semantic_similarity: z.number().min(-1).max(1)
    - lsa_similarity: z.number().min(0).max(1)
    - surprise_type: z.enum(['structural_transfer','semantic_implementation'])
    - source_domain_tag / target_domain_tag: z.enum(tags.domain_tags) loaded once from require('../core/eureka-critic.cjs').loadCriticTags()
    - rubric_pattern: z.string().regex(/^[01x]{6}$/)
    - schema_version: z.number().int()
    No z.string() free-text field of any kind exists on this schema (D1; the sycophancy channel D2 item 5 closed stays closed). The handler: (1) dedupe - sha256 of the canonical-JSON payload against a module-level Map (payloadHash -> {ruling, ts}); identical payload within EUREKA_CRITIC_DEDUPE_TTL_MS (default 60000) returns the cached ruling without recompute (D3b item 4); (2) rate-limit - simple in-process token count per rolling minute, EUREKA_CRITIC_RATE_LIMIT (default 30); over-limit returns a textResponse error naming the limit, isError true; (3) call require('../core/eureka-critic.cjs').criticRule(payload) and return its {verdict, confidence, reasoning_tag} as JSON text via textResponse. The handler body references NEITHER roomDir NOR pluginRoot NOR larryContext (D5: pure function of the payload; the addendum's stale-roomDir bug cannot recur on a surface that never touches it). Registration-site comment block: (a) hitl_shape: none rationale (pure ruling, no Decision-Gate fork; downstream human gate = Phase 213 eureka-reach wiring); (b) why eureka_critic is NOT in ALL_TOOL_COMMANDS (no CLI command twin; the 65-pin in test-205-surface-fence.cjs guards CLI parity, not this tool); (c) SEED-014 lift note (wrapper is disposable, criticRule moves); (d) rate-limit scope note: D3b item 4's "per API key" rate-limit is DEFERRED to the SEED-014 Brain-repo lift - the plugin-local tool has no per-key identity yet, so this phase ships the per-process token count as the honest local equivalent; the per-key upgrade lands with the key surface, not here.
  </action>
  <verify>
    <automated>node -e "const tr=require('./lib/mcp/tool-router.cjs'); const src=require('fs').readFileSync('lib/mcp/tool-router.cjs','utf8'); if(!/server\.tool\(\s*'eureka_critic'/.test(src)) throw new Error('tool not registered'); if(tr.ALL_TOOL_COMMANDS.includes('eureka_critic')||tr.ALL_TOOL_COMMANDS.includes('eureka-critic')) throw new Error('parity array polluted'); console.log('registration ok')" &amp;&amp; node tests/test-205-surface-fence.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "eureka_critic" lib/mcp/tool-router.cjs` >= 2 (registration + comment block)
    - `node tests/test-205-surface-fence.cjs` exits 0 (the 65-pin untouched)
    - The eureka_critic handler block contains no roomDir token: extracting the handler source region and grepping roomDir yields 0 matches (automated properly in Task 2's scan; smoke via reading the diff)
    - `grep -c "EUREKA_CRITIC_RATE_LIMIT\|EUREKA_CRITIC_DEDUPE_TTL_MS" lib/mcp/tool-router.cjs` >= 2
    - `grep -c "hitl_shape" lib/mcp/tool-router.cjs` >= 1 (the declaration comment)
    - `node scripts/build-connector-registry.cjs --check` exits 0 and `node scripts/check-shape-declaration.cjs` exits 0 (no drift on the Part 11 gates)
    - `node -e "require('./bin/mindrian-mcp-server.cjs')"` is NOT run (it starts a server); instead `node --check bin/mindrian-mcp-server.cjs` exits 0 and `node --check lib/mcp/tool-router.cjs` exits 0
  </acceptance_criteria>
  <done>eureka_critic is live on the governed MCP path with a closed scalar/enum schema, dedupe + rate-limit, zero room coupling, documented HITL shape, and every existing fence/gate still green.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-212-part8-boundary.cjs - the D7(c) boundary scan + D7(d) per-call-resolution check</name>
  <files>tests/test-212-part8-boundary.cjs</files>
  <read_first>
    - tests/test-connector-part8-boundary.cjs (the grep-based multi-check scan idiom to reuse - named IIFE checks, ok/fail counters, source-region extraction; D7 says reuse this pattern, do not write a parallel scanner)
    - lib/mcp/tool-router.cjs (Task 1 output - the scan target; locate the eureka_critic registration region)
    - lib/core/eureka-critic.cjs (second scan target)
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md sections D1, D3b, D5, D7
  </read_first>
  <action>
    Write tests/test-212-part8-boundary.cjs modeled structurally on test-connector-part8-boundary.cjs (named check IIFEs, plain assert, summary line, exit non-zero on failure). Checks:
    - CHECK 1 (D1 wire discipline): extract the eureka_critic zod schema region from tool-router.cjs source; assert NO bare z.string() without a .regex() constraint exists in it (rubric_pattern's regex-bound string is the only string allowed); assert the six D1 field names + rubric_pattern + schema_version are all present and no other field is.
    - CHECK 2 (D3b quantization + audit at the assembly site): eureka-critic.cjs source contains quantize applied to differential_score/semantic_similarity/lsa_similarity inside assembleCriticPayload, and auditQueryObject called on the assembled payload (the rs-egress-prompts reuse, not a second auditor - assert require of rs-egress-prompts.cjs exists and no local function named audit* is DEFINED in eureka-critic.cjs).
    - CHECK 3 (D4 portability): eureka-critic.cjs contains no modelcontextprotocol/server.tool/tool-router references (grep the module source; comments excluded via the grep -v hygiene rule - filter comment lines before counting).
    - CHECK 4 (D5 per-call resolution, D7d): extract the eureka_critic handler region (from its server.tool line to the matching close - line-window extraction like check1 of the connector scan does with function boundaries); assert zero occurrences of roomDir, pluginRoot, loadRoomState in that region.
    - CHECK 5 (D3b3 coarse return): criticRule's return path yields string confidence - assert via requiring eureka-critic.cjs and calling criticRule with a stubbed baselinePath fixture, typeof ruling.confidence === 'string'.
    - CHECK 6 (D3b4): dedupe map + rate-limit tokens present in the handler region (grep for the env tunable names).
  </action>
  <verify>
    <automated>node tests/test-212-part8-boundary.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-212-part8-boundary.cjs` exits 0 printing 6 named PASS checks and a summary line
    - Deleting the quantize call from a scratch copy of assembleCriticPayload makes CHECK 2 fail (verify once locally by temporary mutation in a scratchpad copy, not the working tree - document in SUMMARY)
    - `grep -c "roomDir" tests/test-212-part8-boundary.cjs` >= 1 (the D5 assertion is real)
    - Zero network and zero model load: `grep -c "https\?://\|embedTexts" tests/test-212-part8-boundary.cjs` returns 0
  </acceptance_criteria>
  <done>D7 legs (c) and (d) are automated: the wire cannot silently grow a content channel, the module cannot silently couple to MCP or roomDir, and the return path cannot silently become a raw float.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| MCP tool `eureka_critic` | 10th server.tool on lib/mcp/tool-router.cjs | zod schema (6 D1 fields + rubric_pattern + schema_version), dedupe map, rate limiter, hitl_shape:none declaration comment |
| Env tunables (new) | call-time env vars | `EUREKA_CRITIC_RATE_LIMIT` (default 30/min), `EUREKA_CRITIC_DEDUPE_TTL_MS` (default 60000) |
| `tests/test-212-part8-boundary.cjs` | boundary scan suite | CHECK 1-6 covering D1/D3b/D4/D5 (D7 legs c+d) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> eureka_critic tool | the ONLY place phase-212 data crosses a process/network boundary; 20-60 bits by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-09 | Info disclosure | zod schema drift (smuggled text field) | mitigate | no unconstrained z.string(); CHECK 1 fails the build on schema growth |
| T-212-10 | Linkability | full-precision floats / stable IDs on the wire | mitigate | quantize + closed keys asserted (CHECK 2); no ID field exists in the schema |
| T-212-11 | Info disclosure | membership inference via returned confidence | mitigate | coarse string bands only (CHECK 5) |
| T-212-12 | DoS / shadow-model probing | query-stream against the judge | mitigate | rate-limit + dedupe (D3b4, CHECK 6) |
| T-212-13 | Tampering | stale roomDir closure on the new tool | mitigate | handler region grep-clean of roomDir/pluginRoot (CHECK 4, the D5 mandate) |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (212-RESEARCH audit table); no install task |
</threat_model>

<verification>
- `node tests/test-212-part8-boundary.cjs` exit 0
- `node tests/test-205-surface-fence.cjs` exit 0 (65-pin intact)
- `node scripts/build-connector-registry.cjs --check` exit 0; `node scripts/check-shape-declaration.cjs` exit 0
- `node tests/test-212-critic-rubric.cjs` and `node tests/test-212-critic-stage-a.cjs` still exit 0
</verification>

<success_criteria>
- The Grounding Guard ruling is reachable on Desktop/Cowork via MCP (tri-polar: CLI surface arrives with plan 05's runner; programmatic consumer is Phase 213)
- The wrapper is provably thin: deleting it and re-wrapping criticRule elsewhere (SEED-014) requires zero changes to lib/core/eureka-critic.cjs
- Part 8 boundary is enforced by automated scan, not review discipline
</success_criteria>

<output>
Create `.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-03-SUMMARY.md` when done.
</output>
