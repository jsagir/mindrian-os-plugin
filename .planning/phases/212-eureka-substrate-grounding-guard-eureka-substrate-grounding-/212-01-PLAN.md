---
phase: 212-eureka-substrate-grounding-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/eureka-critic-tags.json
  - lib/core/eureka-critic.cjs
  - tests/test-212-critic-stage-a.cjs
autonomous: true
requirements: [212-D1, 212-D2, 212-D3B]
tags: [eureka, critic, part8, mcp]
must_haves:
  truths:
    - "A candidate carrying an unsourced K/M/B or dollar figure routes to pseudoscience deterministically, with zero LLM calls (D2 Stage A)"
    - "A generic-filler mechanism whose embedding barely moves under noun-swap routes to general_shallow with reasoning_tag domain_swap_invariant (D2, D6 failure signature)"
    - "assembleCriticPayload emits ONLY quantized-2dp scalars and closed enums, and throws ExternalEgressViolation on any smuggled content string (D1, D3b)"
    - "lib/core/eureka-critic.cjs loads with zero MCP-framework imports and zero roomDir knowledge (D4, D5)"
    - "Stage A degrades (records gate_skipped) instead of throwing when the vector backend is unavailable"
  artifacts:
    - path: "data/eureka-critic-tags.json"
      provides: "Closed versioned reasoning_tag + verdict + domain_tag enums"
      contains: "schema_version"
    - path: "lib/core/eureka-critic.cjs"
      provides: "Portable critic core: Stage A gates + payload assembly"
      exports: ["stageA", "assembleCriticPayload", "quantize", "loadCriticTags", "VERDICTS"]
      min_lines: 120
    - path: "tests/test-212-critic-stage-a.cjs"
      provides: "Offline Stage A unit suite (no model, no network)"
  key_links:
    - from: "lib/core/eureka-critic.cjs"
      to: "lib/core/rs-egress-prompts.cjs"
      via: "require + auditQueryObject at payload assembly"
      pattern: "auditQueryObject\\("
    - from: "lib/core/eureka-critic.cjs"
      to: "data/eureka-critic-tags.json"
      via: "loadCriticTags fs read"
      pattern: "eureka-critic-tags\\.json"
    - from: "tests/test-212-critic-stage-a.cjs"
      to: "lib/core/eureka-critic.cjs"
      via: "require + injected encodeFn stub (211 encodeFn seam precedent)"
      pattern: "encodeFn"
---

<objective>
Build the contract layer and Stage A (deterministic, no-LLM) half of the Eureka Grounding Guard: the closed versioned tag/verdict enums (`data/eureka-critic-tags.json`), the portable critic module (`lib/core/eureka-critic.cjs`), the four programmatic gates (fabricated-quantity, domain-swap invariance, nearest-neighbor novelty delta, entity-specificity), and the D1/D3b-hardened payload assembler that is the ONLY thing allowed to shape what later crosses the MCP boundary.

Decision coverage: implements CONTEXT D1 (scalars + closed enums only, auditQueryString/auditQueryObject reuse), D2 item 1 (Stage A gates before any LLM), D3b items 1-2 (quantize to 2dp, no stable identifiers), D4 (pure portable module, zero MCP imports), D5 (zero roomDir dependency). Q4 navigator lock: Stage A thresholds are calibrated against MongoDB/mdbr-leaf-ir (the current embedding-spine.cjs DEFAULT_MODEL); provenance travels with features so a future embedder swap invalidates cleanly.

Scope note (navigator Q1 lock): Phase 212 is CRITIC-ONLY. The graph-framed substrate + whitespace/bridge detection named in the ROADMAP goal is NOT here - it moves to the new Phase 212.5 stub the orchestrator registers. Nothing is dropped; it is split.

Purpose: candidates failing Stage A never reach the LLM judge - this alone kills the "$2-5B exit" and "tahini x blockchain" classes already observed live (D6).
Output: tags enum file, critic module (Stage A half), offline unit suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-RESEARCH.md
@lib/core/rs-differential-scorer.cjs
@lib/core/rs-egress-prompts.cjs
@lib/core/eureka/embedding-spine.cjs
@data/dispatch-framework-map.json
@CLAUDE.md

Hard boundary for this plan: do NOT modify `lib/core/eureka/vector-store.cjs` - the vec0 offline-load bug (infer-backend at vector-store.cjs:160) is being fixed in the parallel quick task 260706-5b7 and is a declared dependency, not 212 work. Stage A consumes kNN only through an injected `knnFn` seam and degrades when absent. Blocker 1 (embedding OOM) is ALREADY FIXED (quick 260706-4yl, commits c222ff7d/7ec75b5e, batchSlices + MINDRIAN_EMBED_BATCH default 32) - reference the batching seam, never re-plan it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint data/eureka-critic-tags.json + the eureka-critic.cjs contract skeleton (D1, D3b, D4)</name>
  <files>data/eureka-critic-tags.json, lib/core/eureka-critic.cjs</files>
  <read_first>
    - data/dispatch-framework-map.json (the closed-lookup-table precedent D2 names; note the _note header style)
    - data/hitl-shape-declaration-schema.json (schema_version-from-day-one precedent)
    - lib/core/rs-egress-prompts.cjs (auditQueryString, auditQueryObject exports at lines 52/79; module.exports at 109)
    - lib/core/rs-differential-scorer.cjs lines 480-545 (the scoreMeasured Part 8 audit precedent D1 says reuse: auditQueryString on inputs, auditQueryObject on the output record)
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md sections D1, D2, D3b
  </read_first>
  <action>
    Create `data/eureka-critic-tags.json` - the closed, versioned enum file (per D2 and the Specifics section: schema_version from day one, Canon Part 9 typed-packet discipline). Keys: `schema_version` (integer 1), `_note` (one-paragraph purpose string in the dispatch-framework-map.json style, no em-dashes), `verdicts` (exactly: transferable, restatement, pseudoscience, general_shallow), `reasoning_tags` (object of tag -> one-line description; minimum set: unsourced_quantity, domain_swap_invariant, low_novelty_delta, entity_nonspecific, rubric_disagreement, restatement_vocab_swap, no_checkable_consequence, schema_orphan, passes_all_gates, calibration_unknown), `domain_tags` (closed generic-domain enum for source_domain_tag/target_domain_tag per D1 - generic fields only, e.g. engineering, biology, medicine, computing, materials, energy, mathematics, finance, social, food, unknown; NEVER room artifact vocabulary), `rubric_pattern_len` (6), `rubric_version` (1).

    Create `lib/core/eureka-critic.cjs` (D4 location decision: top-level lib/core sibling of rs-differential-scorer.cjs, NOT inside lib/core/eureka/ - the eureka/ subdir is the local generator spine that stays; eureka-critic.cjs is the portable module SEED-014 later lifts into the Brain repo, so the lift boundary is one file). CJS only, no MCP-framework imports of any kind (D4 - grep-assertable: the strings modelcontextprotocol, server.tool, tool-router must never appear). Header comment documents the two-stage architecture and the LOCAL/REMOTE split from 212-RESEARCH.md's Architectural Responsibility Map.

    Implement in this task (Stage A itself is Task 2):
    - `loadCriticTags()` - reads data/eureka-critic-tags.json relative to the module (path.join(__dirname, '..', '..', 'data', ...)), validates schema_version === 1, caches.
    - `VERDICTS` frozen array re-exported from the tags file.
    - `quantize(x)` - null-safe 2-decimal rounding (D3b item 1, non-negotiable; Math.round(x*100)/100; returns null for non-finite input).
    - `assembleCriticPayload(features)` - builds the D1 wire object with EXACTLY these keys: differential_score, semantic_similarity, lsa_similarity (all through quantize), surprise_type (enum passthrough), source_domain_tag, target_domain_tag (must be members of tags.domain_tags, else throw TypeError), rubric_pattern (string matching /^[01x]{6}$/, default 'xxxxxx' when Stage B never ran), schema_version (from tags file). Then call auditQueryObject(payload, 'eureka-critic-mcp') from rs-egress-prompts.cjs exactly as rs-differential-scorer.cjs:487 area does with auditQueryString (D1 says reuse that precedent, do not write a second auditor). No artifact IDs, no free text, no raw floats survive past this function (D3b items 1-2).
    - Env tunable name constants (RS_SEMANTIC_FLOOR precedent, values read at call time not module load): EUREKA_SWAP_INVARIANCE_FLOOR (default 0.05), EUREKA_SWAP_K (default 3), EUREKA_NN_DELTA_FLOOR (default 0.10), EUREKA_ENTITY_MIN (default 2).
    - module.exports: loadCriticTags, VERDICTS, quantize, assembleCriticPayload (Task 2 appends stageA).
  </action>
  <verify>
    <automated>node -e "const c=require('./lib/core/eureka-critic.cjs'); const t=c.loadCriticTags(); if(t.schema_version!==1) throw new Error('schema_version'); if(c.VERDICTS.length!==4) throw new Error('verdicts'); if(c.quantize(0.82537)!==0.83) throw new Error('quantize'); const p=c.assembleCriticPayload({differential_score:0.825371,semantic_similarity:0.91,lsa_similarity:0.08,surprise_type:'structural_transfer',source_domain_tag:'food',target_domain_tag:'computing'}); if(p.differential_score!==0.83) throw new Error('not quantized'); console.log('contract ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('data/eureka-critic-tags.json','utf8'))"` exits 0
    - `grep -c "schema_version" data/eureka-critic-tags.json` >= 1
    - `grep -c "modelcontextprotocol\|server\.tool\|tool-router" lib/core/eureka-critic.cjs` returns 0 (D4 portability, exit code 1 from grep is the pass)
    - `grep -c "auditQueryObject" lib/core/eureka-critic.cjs` >= 1 (D1 reuse, not a hand-rolled auditor)
    - `grep -rn "roomDir" lib/core/eureka-critic.cjs | grep -v '^\s*//' | wc -l` returns 0 (D5)
    - assembleCriticPayload throws on a payload feature object carrying a text/content/id-shaped string field (asserted properly in Task 2's test file; smoke here via the verify one-liner)
    - No em-dashes in either file: `grep -cP '\x{2014}' lib/core/eureka-critic.cjs data/eureka-critic-tags.json` returns 0 per file
  </acceptance_criteria>
  <done>Tags enum file exists with schema_version 1 and all four verdicts; eureka-critic.cjs loads standalone, quantizes to 2dp, audits its payload through rs-egress-prompts, and contains zero MCP/roomDir coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage A deterministic gates + offline unit suite (D2 item 1, D6 failure signatures)</name>
  <files>lib/core/eureka-critic.cjs, tests/test-212-critic-stage-a.cjs</files>
  <read_first>
    - lib/core/eureka-critic.cjs (Task 1 output - the file being extended)
    - lib/core/eureka/embedding-spine.cjs header + lines 350-400 (embedTexts contract, encodeFn injection seam, batchSlices, encoderProvenance - the 211 test-seam precedent Stage A mirrors)
    - lib/core/rs-differential-scorer.cjs section around line 483 (scoreMeasured: the per-pair record shape the critic consumes - semantic, lexical, signed_diff, abs_diff, direction, passes, band, provenance)
    - tests/test-211-embedding-spine.cjs (house offline-test idiom: plain assert, ok/fail counters, exit non-zero on failure)
    - .planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-RESEARCH.md sections "Pattern 1" and "Common Pitfalls" 1, 2, 6, 8
  </read_first>
  <behavior>
    - Test 1: candidate text containing "5B" or "$2" with sourcedQuantities false routes {pass:false, route:'pseudoscience', tag:'unsourced_quantity'} and the injected encodeFn is NEVER called (call-count 0, the scoreMeasured Test 8 precedent)
    - Test 2: same quantity text with opts.sourcedQuantities true does NOT trip the quantity gate (the two JHU Opportunity Statement fixtures carry catalog-sourced figures and must be able to pass Stage A)
    - Test 3: a mechanism whose noun-swapped variants embed to (near-)identical vectors under a crafted stub encodeFn yields shift < EUREKA_SWAP_INVARIANCE_FLOOR and routes {pass:false, route:'general_shallow', tag:'domain_swap_invariant'}
    - Test 4: with opts.knnFn returning a very-near neighbor (similarity 0.97), novelty delta < EUREKA_NN_DELTA_FLOOR routes {pass:false, route:'restatement', tag:'low_novelty_delta'} (Pitfall 2: restatement is the #1 job)
    - Test 5: with opts.knnFn absent, the gate records gate_skipped 'nn_unavailable' in features and does NOT throw (degrade path; vec0 fix 260706-5b7 is a parallel dependency, never assumed)
    - Test 6: a specific, entity-rich mechanism passing all gates returns {pass:true, features:{...}} where features carries quantized shift, nn_delta, entity_count, embedder (from encoderProvenance or the injected stub marker)
    - Test 7: EUREKA_SWAP_INVARIANCE_FLOOR env override changes gate behavior at call time (RS_SEMANTIC_FLOOR precedent)
  </behavior>
  <action>
    Append `stageA(candidate, opts)` to lib/core/eureka-critic.cjs. `candidate` = { text, mechanismText, sourceDomainTag, targetDomainTag, differential } where differential is the scoreMeasured record (or the D3 gold-card equivalents). `opts` = { encodeFn, knnFn, sourcedQuantities, env-tunable overrides }; async (encodeFn may be async). Gates in fixed order, first failure returns immediately:

    1. Fabricated-quantity flag (D2): the SAME regex class the 211-05 Part 8 guard uses, /\b\d+(?:\.\d+)?[KMB]\b/, plus a dollar-figure pattern /\$\s?\d/. Applied to candidate.text unless opts.sourcedQuantities === true (the fixture-declared sourced-figures escape hatch; document it in a comment citing the JHU catalog-sourced drafts). Route pseudoscience / unsourced_quantity. Runs BEFORE any embedding work so forbidden candidates never reach an encoder.
    2. Domain-swap invariance (D2, the computable D6 signature): generate EUREKA_SWAP_K noun-swapped variants of mechanismText via a deterministic internal nounSwap helper (replace domain-noun tokens with a fixed generic replacement list; deterministic seeding so tests are stable), embed original + variants via opts.encodeFn (tests) or lazy-required embedding-spine embedTexts (production; lazy INSIDE the function per Canon Decision 8 exactly like rs-differential-scorer.cjs does), shift = 1 - mean cosine(original, variants) using the cosineSimilarity already exported by the spine (Don't Hand-Roll). shift < floor routes general_shallow / domain_swap_invariant.
    3. Nearest-neighbor novelty delta: if opts.knnFn is a function, call knnFn(vectorOfOriginal, k) -> [{similarity}]; delta = 1 - max similarity; delta < EUREKA_NN_DELTA_FLOOR routes restatement / low_novelty_delta. If knnFn absent or throws, set features.gate_skipped='nn_unavailable' and continue (degrade, never die - Blocker 2 lesson, Pitfall 6). Do NOT require vector-store.cjs directly in this phase.
    4. Entity-specificity: deterministic token heuristic counting specific entities in mechanismText (proper-noun-shaped tokens + number-with-unit tokens); count < EUREKA_ENTITY_MIN routes general_shallow / entity_nonspecific.

    On pass: return { pass:true, features } with every float pre-quantized and embedder provenance recorded (Q4 lock: thresholds are mdbr-leaf-ir-calibrated; provenance makes a swap detectable). Never throws on encoder failure - mirror embedTexts' never-throws envelope: encoder failure returns { pass:false, route:'general_shallow', tag:'calibration_unknown', degraded:true }.

    Write tests/test-212-critic-stage-a.cjs implementing the behavior list above: plain assert, zero network, zero model load (every embedding via crafted stub encodeFn), exit non-zero on any failure, PASS/FAIL summary line (house idiom from test-211-embedding-spine.cjs).
  </action>
  <verify>
    <automated>node tests/test-212-critic-stage-a.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-212-critic-stage-a.cjs` exits 0 and prints a summary line containing "passed"
    - `grep -c "gate_skipped" lib/core/eureka-critic.cjs` >= 1 (degrade path exists)
    - `grep -c "require(.\+vector-store" lib/core/eureka-critic.cjs` returns 0 (kNN only via injected seam; grep exit 1 is the pass)
    - `grep -c "KMB\|\[KMB\]" lib/core/eureka-critic.cjs` >= 1 (the shared fabricated-quantity regex class present)
    - Test file makes zero network calls: `grep -c "https\?://" tests/test-212-critic-stage-a.cjs` returns 0
    - `bash tests/run-all-211.sh` still green (no generator regression; legs PASS with existing SKIPs allowed)
  </acceptance_criteria>
  <done>stageA routes all four gate classes deterministically with injected seams, degrades on missing kNN backend, and the offline suite proves each route plus the sourced-quantities escape hatch without loading a model.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `data/eureka-critic-tags.json` | closed versioned enum file | schema_version, verdicts[4], reasoning_tags{10}, domain_tags, rubric_pattern_len, rubric_version |
| `lib/core/eureka-critic.cjs` | portable critic core (Stage A half) | `loadCriticTags()`, `VERDICTS`, `quantize()`, `assembleCriticPayload()`, `stageA()` |
| Env tunables (new) | call-time env vars | `EUREKA_SWAP_INVARIANCE_FLOOR`, `EUREKA_SWAP_K`, `EUREKA_NN_DELTA_FLOOR`, `EUREKA_ENTITY_MIN` |
| `tests/test-212-critic-stage-a.cjs` | offline unit suite | 7 behaviors incl. encodeFn call-count-0 proof |

Phase-wide artifact map (for the verifier; produced across plans 01-05): `lib/core/eureka-critic.cjs` (this plan + 02: adds `RUBRIC_ITEMS`, `runRubric()`, `verdictFromRubric()`, `classifyCandidate()`, `criticRule()`), `data/eureka-critic-tags.json` (this plan), `evals/eureka/212-critic-baseline.json` (02, updated 05), MCP tool `eureka_critic` on `lib/mcp/tool-router.cjs` (03), `tests/test-212-critic-stage-a.cjs` (this plan), `tests/test-212-critic-rubric.cjs` + `tests/test-212-negative-corpus.cjs` (02), `tests/test-212-part8-boundary.cjs` (03), `tests/test-212-gold-cards.cjs` + `tests/test-212-plurai-leg.cjs` + `tests/run-all-212.sh` (04), `evals/eureka/opportunity-drafts/pair-1-arrhythmias.md` + `pair-2-cerebral-aneurysm.md` (04), `scripts/eureka-critic-run.cjs` + `evals/eureka/212-calibration-report.md` (05), env tunables `EUREKA_SWAP_INVARIANCE_FLOOR`, `EUREKA_SWAP_K`, `EUREKA_NN_DELTA_FLOOR`, `EUREKA_ENTITY_MIN` (this plan), `EUREKA_CRITIC_RATE_LIMIT`, `EUREKA_CRITIC_DEDUPE_TTL_MS` (03).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate text -> Stage A | raw room content enters the LOCAL gates; must never leak into the payload |
| features -> assembleCriticPayload | the only choke point shaping what may later cross the MCP wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-01 | Info disclosure | assembleCriticPayload | mitigate | auditQueryObject (rs-egress-prompts reuse) throws ExternalEgressViolation on smuggled strings; closed-key wire object (D1) |
| T-212-02 | Linkability | float fields on the payload | mitigate | quantize() to 2dp before any field leaves the function (D3b1); asserted in Task 1 verify |
| T-212-03 | Spoofing (sycophancy) | Stage A gates | mitigate | deterministic gates, no judge in the loop; fabricated-quantity gate runs before any embedding |
| T-212-04 | Tampering | stale roomDir closure | mitigate | module has zero roomDir token (D5); grep-asserted |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages this phase (212-RESEARCH Package Legitimacy Audit: reuse only, all deps navigator-approved in 211-01); no install task exists |
</threat_model>

<verification>
- `node tests/test-212-critic-stage-a.cjs` exit 0
- `node -e "require('./lib/core/eureka-critic.cjs')"` exit 0 (loads with heavy deps untouched)
- `bash tests/run-all-211.sh` no regression (generator untouched)
- grep gates from acceptance_criteria all hold
</verification>

<success_criteria>
- All must_haves truths hold; Stage A is the complete D2-item-1 gate set with injected seams and degrade paths
- The wire-shaping function exists in exactly one place and is Part 8 audited
- Zero files under lib/core/eureka/ or lib/mcp/ modified
</success_criteria>

<output>
Create `.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-01-SUMMARY.md` when done.
</output>
