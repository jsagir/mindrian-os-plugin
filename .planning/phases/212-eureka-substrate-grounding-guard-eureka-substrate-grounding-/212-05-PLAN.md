---
phase: 212-eureka-substrate-grounding-guard
plan: 05
type: execute
wave: 4
depends_on: [212-03, 212-04]
files_modified:
  - scripts/eureka-critic-run.cjs
  - evals/eureka/212-calibration-report.md
  - evals/eureka/212-critic-baseline.json
autonomous: false
requirements: [212-D2, 212-D3, 212-D7]
tags: [eureka, critic, calibration, human-verify]
must_haves:
  truths:
    - "The full critic pipeline runs against all 6 gold cards + 2 JHU Opportunity Statements with the REAL local embedder (mdbr-leaf-ir) and the REAL local judge (the Claude session performing the 2 rubric passes), producing a written calibration report"
    - "The >=0.85 gold-card accuracy + high-pseudoscience-recall bar is judged by a HUMAN at a blocking checkpoint, never self-certified (navigator Q2 lock, the 211 honest-deferral pattern)"
    - "The two JHU drafts receive their first critic ruling with calibrated coarse confidence, replacing the manual not-yet-critic-verified stopgap - read as fixtures, nothing written to any room"
    - "212-critic-baseline.json buckets are populated from the run; status flips from baseline_deferred to calibrated ONLY after explicit human approval"
  artifacts:
    - path: "scripts/eureka-critic-run.cjs"
      provides: "CLI runner (tri-polar CLI surface): cards + drafts -> features, rubric prompts, verdicts, report"
      min_lines: 100
    - path: "evals/eureka/212-calibration-report.md"
      provides: "Per-candidate features, rubric answers (neutral + adversarial), verdict vs gold, accuracy + recall summary"
    - path: "evals/eureka/212-critic-baseline.json"
      provides: "Populated calibration buckets, embedding_model-stamped, human-gated status"
  key_links:
    - from: "scripts/eureka-critic-run.cjs"
      to: "lib/core/eureka-critic.cjs"
      via: "classifyCandidate + assembleCriticPayload + criticRule"
      pattern: "eureka-critic\\.cjs"
    - from: "scripts/eureka-critic-run.cjs"
      to: "lib/core/eureka/embedding-spine.cjs"
      via: "real embedTexts (batched per quick 260706-4yl; MINDRIAN_EMBED_BATCH default 32)"
      pattern: "embedding-spine"
---

<objective>
Close the phase: run the Grounding Guard for real. A CLI runner (scripts/eureka-critic-run.cjs - the tri-polar CLI surface; Desktop/Cowork get the plan-03 MCP tool) executes the full pipeline on the 6 SEED-050 gold cards (D3) plus the 2 JHU Opportunity Statement fixtures with the REAL local embedder (MongoDB/mdbr-leaf-ir, the navigator Q4 lock - Stage A features computed for real, no stubs) and the REAL local judge (the executing Claude session performs the neutral + adversarial rubric passes per D2 - the rubric LLM step runs LOCALLY, content never egresses). The output is a written calibration report + populated confidence buckets.

Then the honest-deferral gate (navigator Q2 lock, the 211 pattern): the >=0.85 gold-card accuracy + high pseudoscience recall bar from the ROADMAP goal is a blocking checkpoint:human-verify leg. The navigator also reviews the critic's first two REAL rulings (the JHU drafts, until now flagged "not yet critic-verified" in the room entry). Only after approval does 212-critic-baseline.json flip from baseline_deferred to calibrated.

Decision coverage: D2 item 4 (calibration buckets from measured accuracy, unseen pattern stays unknown), D3 (gold-set as the calibration base; sterling anchor first), D7 (the phase gate = run-all-212 green + this human leg). Note honestly at the checkpoint: the 6 cards still carry validated: candidate pending the 211-04/211-05 human checkpoints, so this calibration is a structural baseline over N=6+2, not a validated corpus (SEED-050 validate-before-trust).

Scope note (navigator Q1 lock): critic-only phase; substrate/whitespace detection is Phase 212.5 (orchestrator-registered stub) - nothing here is dropped, it is split.

Purpose: a critic that has never ruled on real workload is not a Grounding Guard; this plan produces its first calibrated rulings under human sign-off.
Output: runner script, calibration report, human-approved (or honestly-deferred) baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-CONTEXT.md
@.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-RESEARCH.md
@lib/core/eureka-critic.cjs
@evals/eureka/212-critic-baseline.json
@evals/eureka/README.md
@scripts/eureka-room-report.cjs
@lib/core/eureka/embedding-spine.cjs

Runtime notes: the embedding OOM is FIXED (quick 260706-4yl, commits c222ff7d/7ec75b5e - batchSlices loop, MINDRIAN_EMBED_BATCH default 32); this 8-candidate run is tiny anyway. The vec0 offline-load fix (quick 260706-5b7, vector-store.cjs infer-backend) is a declared parallel dependency: if it has landed, the runner MAY pass a knnFn backed by the room-scale vector store; if not, run with knnFn absent and let Stage A record gate_skipped nn_unavailable (the degrade path plan 01 built). Do NOT modify vector-store.cjs either way.
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/eureka-critic-run.cjs + the real calibration run (D2, D3, Q4 lock)</name>
  <files>scripts/eureka-critic-run.cjs, evals/eureka/212-calibration-report.md, evals/eureka/212-critic-baseline.json</files>
  <read_first>
    - lib/core/eureka-critic.cjs (the complete pipeline exports after plans 01-02: classifyCandidate, runRubric, RUBRIC_ITEMS, assembleCriticPayload, criticRule)
    - scripts/eureka-room-report.cjs (the house CLI-runner precedent: process.argv switch-case router, no Commander/yargs; offline/live mode handling)
    - evals/eureka/cases/ all 6 cards + evals/eureka/opportunity-drafts/ both fixtures (the run inputs; note quantities_sourced on the drafts)
    - evals/eureka/212-critic-baseline.json (the bucket schema plan 02 defined)
    - lib/core/eureka/embedding-spine.cjs header (resolveModel/encoderProvenance - confirm DEFAULT_MODEL is MongoDB/mdbr-leaf-ir before locking any threshold reading)
  </read_first>
  <action>
    Write scripts/eureka-critic-run.cjs (CJS, process.argv switch-case per house convention). Modes:
    - --emit-prompts: load the 6 cards (gray-matter) + 2 drafts, run stageA with the REAL embedding spine (no encodeFn stub; embedder provenance recorded per candidate - the Q4 lock), write per-candidate neutral + adversarial rubric prompt files to a working dir given by --workdir (default a scratch path printed to stdout). Stage-A-failed candidates are recorded with their deterministic route and get no prompts.
    - --score --answers (json path): read a judge-answers JSON (per candidate, per pass, items a-f + one-line evidence), feed runRubric's comparison path, compute verdicts by code, assemble the D1 payload per candidate (quantize + audit), call criticRule, and write evals/eureka/212-calibration-report.md: a table per candidate (name, stage reached, quantized features, rubric_pattern per pass, final verdict, reasoning_tag, confidence, gold/expected label, match), then summary lines: gold-card accuracy (matches/6), pseudoscience-recall statement referencing the automated D6 suite (3/3), the two draft rulings marked FIRST REAL RULINGS, and an honesty block noting validated: candidate status on the cards. Update 212-critic-baseline.json buckets (rubric_pattern -> n/correct/verdict_observed) while KEEPING status baseline_deferred (the flip is human-gated in Task 2).
    THEN execute the run in this task: run --emit-prompts; the executor (this Claude session) IS the local judge per the 212-RESEARCH Architectural Responsibility Map - answer each candidate's neutral pass and adversarial pass faithfully per RUBRIC_ITEMS (one sentence of evidence per item, no peeking at scores - the prompts contain none by construction), save the answers JSON, run --score. Commit nothing to any room; all writes are repo-side eval artifacts.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('evals/eureka/212-calibration-report.md','utf8'); if(!/accuracy/i.test(r)) throw new Error('no accuracy line'); if(!/pair-1-arrhythmias/.test(r)||!/pair-2-cerebral-aneurysm/.test(r)) throw new Error('draft rulings missing'); if(!/sterling/.test(r)) throw new Error('anchor missing'); const b=JSON.parse(fs.readFileSync('evals/eureka/212-critic-baseline.json','utf8')); if(Object.keys(b.buckets).length<1) throw new Error('buckets empty'); if(b.status!=='baseline_deferred') throw new Error('status flipped without human approval'); console.log('calibration artifacts ok')"</automated>
  </verify>
  <acceptance_criteria>
    - evals/eureka/212-calibration-report.md exists, names all 8 candidates, carries an accuracy line and both draft rulings (the verify one-liner exits 0)
    - 212-critic-baseline.json has >= 1 populated bucket AND status still baseline_deferred pre-checkpoint
    - `grep -c "mdbr-leaf-ir" evals/eureka/212-calibration-report.md` >= 1 (embedder provenance recorded - Q4 lock)
    - `bash tests/run-all-212.sh` exits 0 with FAIL=0 after the run (no regression from the report/baseline writes)
    - `grep -rcP '\x{2014}' evals/eureka/212-calibration-report.md scripts/eureka-critic-run.cjs` returns 0 per file (no em-dashes)
  </acceptance_criteria>
  <done>The critic has ruled on all 8 real candidates with real embeddings and a real local judge; the report and buckets exist; nothing is self-certified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator calibration sign-off - the >=0.85 bar + the first two real rulings (Q2 lock)</name>
  <files>evals/eureka/212-critic-baseline.json, evals/eureka/212-calibration-report.md</files>
  <what-built>The complete Grounding Guard (Stage A gates + two-pass rubric + verdict-by-code + calibration ruling + MCP tool) ran end-to-end on the 6 SEED-050 gold cards and the 2 JHU Opportunity Statements. Results are in evals/eureka/212-calibration-report.md; confidence buckets in evals/eureka/212-critic-baseline.json (status baseline_deferred, awaiting this checkpoint).</what-built>
  <action>
    Present the calibration evidence to the navigator and pause (blocking). After the navigator responds: on "approved", make exactly ONE edit - flip evals/eureka/212-critic-baseline.json status from baseline_deferred to calibrated, adding approved_at (ISO date) and gold_accuracy (the measured number) - then re-run bash tests/run-all-212.sh and confirm green. On "deferred", keep status baseline_deferred and append the navigator's stated reason to the report's honesty block (the 211 honest-deferral posture; the phase still completes and the deferral is recorded for /gsd-verify-work). On specific misruling feedback, log the misrulings in the report and stop for a gap-closure pass rather than silently re-tuning thresholds (Pitfall 7: criteria drift requires re-calibration, not quiet edits).
  </action>
  <how-to-verify>
    1. Open evals/eureka/212-calibration-report.md.
    2. Check the gold-card accuracy line: is it >= 0.85 (at least 5/6 with archimedes-sterling, the lean-checkable anchor, correct)? Sterling correct is non-negotiable - it is the one objective ground truth (D3).
    3. Check pseudoscience recall: the D6 negative corpus is 3/3 by automated test (tests/test-212-negative-corpus.cjs); confirm no gold card was WRONGLY routed to pseudoscience (precision check).
    4. Read the two JHU draft rulings (pair-1-arrhythmias, pair-2-cerebral-aneurysm): do the verdict + reasoning_tag + coarse confidence match your own judgment of these two statements? These replace the manual "not yet critic-verified" stopgap from the 2026-07-06 room entry.
    5. Note the honesty block: the 6 cards are still validated: candidate (211-04/211-05 human checkpoints pending), so this is a structural baseline, not a validated corpus.
  </how-to-verify>
  <verify>
    <automated>node -e "const b=JSON.parse(require('fs').readFileSync('evals/eureka/212-critic-baseline.json','utf8')); if(b.status==='calibrated' &amp;&amp; !b.approved_at) throw new Error('calibrated without approved_at'); if(!['calibrated','baseline_deferred'].includes(b.status)) throw new Error('invalid status'); console.log('checkpoint state ok: '+b.status)"</automated>
  </verify>
  <resume-signal>Reply "approved" to flip status to calibrated, "deferred" to keep baseline_deferred with your reason logged, or describe specific misrulings to fix.</resume-signal>
  <done>The navigator has ruled on the calibration bar; baseline status reflects that ruling exactly (calibrated with approved_at, or baseline_deferred with a logged reason); run-all-212 green either way.</done>
</task>

</tasks>

## Artifacts this plan produces

| Artifact | Kind | Symbols |
|----------|------|---------|
| `scripts/eureka-critic-run.cjs` | CLI runner (tri-polar CLI surface) | modes `--emit-prompts`, `--score --answers <json>`, `--workdir` |
| `evals/eureka/212-calibration-report.md` | calibration evidence | per-candidate table, accuracy line, FIRST REAL RULINGS block, honesty block |
| `evals/eureka/212-critic-baseline.json` (updated) | populated buckets | buckets{rubric_pattern -> n/correct/verdict_observed}, status transition baseline_deferred -> calibrated (human-gated), approved_at, gold_accuracy |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| card/draft content -> local judge session | content stays in the local Claude session (Part 8: the rubric LLM step never sits behind the MCP boundary) |
| features -> criticRule | only quantized scalars + rubric booleans; same choke point as plans 01-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-17 | Info disclosure | calibration run egress | mitigate | zero network in the runner (local model cache; no Plurai, no Brain calls); report + buckets are repo-side only |
| T-212-18 | Spoofing (self-certification) | accuracy bar | mitigate | blocking human checkpoint; status flip is human-gated; deferral path is first-class (Q2 lock) |
| T-212-19 | Repudiation | ruling provenance | mitigate | report records embedder provenance, rubric_pattern per pass, and approved_at on flip |
| T-212-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (212-RESEARCH audit table); no install task |
</threat_model>

<verification>
- `bash tests/run-all-212.sh` exit 0, FAIL=0 (the D7 automated gate)
- `bash tests/run-all-211.sh` exit 0 (generator no-regression)
- `node scripts/doctor.cjs --acceptance` green before /gsd-verify-work (house phase-gate roll-up)
- Checkpoint resolved: approved (status calibrated) or deferred (recorded reason)
</verification>

<success_criteria>
- The Grounding Guard has real, human-reviewed rulings on its gold set and its first real workload
- The >=0.85 bar was judged by the navigator, not asserted by the build
- The two JHU Opportunity Statements are no longer "not yet critic-verified" by stopgap - they carry critic verdicts with calibrated coarse confidence
</success_criteria>

<output>
Create `.planning/phases/212-eureka-substrate-grounding-guard-eureka-substrate-grounding-/212-05-SUMMARY.md` when done.
</output>
