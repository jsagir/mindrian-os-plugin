---
phase: 275-enlarge-room-schema-by-icm-layer
plan: 07
type: execute
wave: 3
depends_on: ["275-02"]
files_modified:
  - templates/room-skeleton/references/SECTION-SCHEMA.md
  - templates/room-skeleton/references/SUB-SCHEMAS.md
autonomous: true
canon_parts: [7, 8, 9]
requirements: [ICML-12, ICML-13]
must_haves:
  truths:
    - "A room owner can read one file and learn which venture-stage values are legal and what each means"
    - "The three coexisting stage vocabularies are named as three separate axes, with each one's ICM layer stated"
    - "Both grains of default_methodologies are documented, and the rule for which one wins is stated with its code grounding"
    - "funding's Stage and Outcome dimensions, opportunity-bank's Knight position, and team-execution's Mentor-Profiles each have a written schema"
    - "Neither reference document contains any room-specific value"
  artifacts:
    - path: "templates/room-skeleton/references/SECTION-SCHEMA.md"
      provides: "venture_stage axis schema, three-axes ruling, both methodology grains, the precedence rule, the command tiering"
      contains: "Pre-Opportunity"
    - path: "templates/room-skeleton/references/SUB-SCHEMAS.md"
      provides: "funding Stage/Outcome, opportunity-bank Knight position, team-execution Mentor-Profiles"
      contains: "Knight"
  key_links:
    - from: "templates/room-skeleton/references/SECTION-SCHEMA.md"
      to: "lib/core/model-profiles.cjs VALID_STAGES"
      via: "the venture_stage enum documented from its live code source"
      pattern: "VALID_STAGES"
    - from: "templates/room-skeleton/references/SECTION-SCHEMA.md"
      to: "lib/core/room-skeleton-scaffold.cjs resolveBlueprint"
      via: "the family-wins precedence rule and its line-level grounding"
      pattern: "familyActive"
    - from: "lib/core/room-skeleton-scaffold.cjs"
      to: "templates/room-skeleton/references/"
      via: "writeReferenceDocs copying the REFERENCE_DOCS allowlist"
      pattern: "REFERENCE_DOCS"
---

## Decision map

See `275-01-PLAN.md`'s Decision map for the full D-01 through D-13 table. This plan implements
**D-04** (promote `stage_relevance` and `default_methodologies` into L3 at BOTH grains, with the
precedence rule documented), **D-07**'s sub-schema half, **D-12** (`team-execution` Mentor-Profiles
schema), and the L3 record of **D-03**, **D-05** and **D-06**. It is the ICM Layer 3 factory layer
that today exists nowhere in any room on disk.

<objective>
Write the two L3 reference documents every room gets: the section schema, and the per-section
nested sub-schemas.

Purpose: SEED-084's core structural claim is that `MindrianRooms/CLAUDE.md` has claimed an L0-to-L4
hierarchy for months while L3 exists in no room at all. This plan is the file that makes L3 real.
Its second job is a forcing function: promoting a hardcoded table into a document a human can read
is what caught five dead command citations in the first place. A stale reference a reader can
immediately falsify cannot ship quietly.

Output: two static markdown documents copied verbatim into every room's `references/` directory.
</objective>

## Artifacts this phase produces

Files THIS PLAN creates. The phase-wide index lives in `275-01-PLAN.md` under this same heading.

### Created by 275-07

Two new files under `templates/room-skeleton/references/`, the exact two filenames named by the
frozen `REFERENCE_DOCS` allowlist plan 275-02 added to the scaffold (a filename mismatch means the
document silently never ships):

- `SECTION-SCHEMA.md`
- `SUB-SCHEMAS.md`

No new code symbol. The writer that consumes these files, `writeReferenceDocs`, was built in plan
275-02.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-CONTEXT.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-PATTERNS.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-02-SUMMARY.md
</context>

<mandatory_consults>
- **icm-architect** (`~/.claude/skills/icm-architect/`). Read `references/core.md`'s L3 row before
  writing: "What rules apply?", role "factory (stable)"; "L3 = the recipe... L4 = the ingredients
  and the dish"; "reference material... kept structurally separate from working artifacts, because
  they ask different things of the model". Also read `assets/templates/CLAUDE.md`'s `_shared/`
  convention. That doctrine is the direct grounding for why a per-room stage VALUE stays at L0 and
  only the axis SCHEMA comes to L3.
- **langtalks-graph-expert**, per the ROADMAP navigator ruling 2026-08-31. This is the phase's
  deepest layer-design work. Run a live consult before Task 1 on "stable reference layer separate
  from working state in an agent workspace" and on "declaring a per-section tool subset as schema
  data". Record the verbatim result including an honest `edges: []` outcome. **Planner-time status:
  the tool was not present in the planning session. SEED-084's `## ADDENDUM 2026-09-04` records a
  prior live run of the second question returning `edges: []` against a reachable server (9,260
  nodes / 21,492 edges); if the new run returns the same, say so and cite the prior result rather
  than presenting it as new corroboration.**
</mandatory_consults>

<tasks>

<task type="auto">
  <name>Task 1: Write SECTION-SCHEMA.md, the venture-stage axis plus both methodology grains plus the precedence rule</name>

  <read_first>
    - `lib/core/model-profiles.cjs` lines 32-46 - `STAGE_HINTS` and
      `VALID_STAGES = ['Pre-Opportunity', 'Discovery', 'Validation', 'Design', 'Investment']`.
      This is the live, in-code enum and it is the authority for the documented value list. Do not
      take the value list from SEED-084's prose, which quotes a looser six-value set observed in
      `INDEX.md` including a `QA` value that is NOT in `VALID_STAGES`.
    - `templates/room-skeleton/STATE.md.tmpl` line 6 - `venture_stage: Pre-Opportunity`, the L0
      default every room is born with
    - `lib/core/room-skeleton-scaffold.cjs` `SECTION_METADATA` after plan 275-01 - the section-grain
      `stage_relevance` and `default_methodologies` for all 11 sections, quoted into the document
    - `lib/core/room-skeleton-scaffold.cjs` lines 253-260 and 366-380 - `resolveBlueprint`'s return
      shape and the `familyActive && defaultMethodologies.length > 0` ternary. That ternary IS the
      precedence rule; document what the code does, do not invent a rule the code does not follow.
    - `data/room-blueprints.json` - the family-grain `default_methodologies` for all 9 families,
      and the `_sections_note` convention for documenting a substitution in place
    - `.planning/seeds/SEED-084-....md` `## ADDENDUM 2026-09-02` in full - the three-axes ruling,
      its file-line anchors, and its "what would overturn this ruling" list
    - `.planning/seeds/SEED-084-....md` `## ADDENDUM 2026-09-04e` - the full three-tier command
      sweep, the corrected dead-slug table, and the two structural gaps it surfaced
    - `data/command-registry.json` via `node -e` query - re-verify every command named
    - `~/.claude/skills/icm-architect/references/core.md` L3 row
    - langtalks-graph-expert consult per `<mandatory_consults>`
  </read_first>

  <files>templates/room-skeleton/references/SECTION-SCHEMA.md</files>

  <action>
One static markdown document, no YAML frontmatter, no em-dashes, Feynman-simplified prose. It ships
verbatim into every room, so it must read as generic methodology reference with zero room-specific
values. Sections, in this order:

**Header.** Title, one line saying what this file is: the stable schema behind every section in this
room. Then one line saying what it is NOT: it holds no value belonging to this particular venture.
This room's own current stage lives in `STATE.md` at the room root; this file only says which values
are legal and what each one means. State that this is ICM Layer 3, the factory layer: read every
run, edited rarely, never a destination for work product.

**Section 1: The three stage vocabularies are three separate axes, not one taxonomy.** This is the
2026-09-02 ruling and it must be stated plainly, because three vocabularies that sound alike coexist
in this system and conflating them bakes ambiguity into every room. Give a three-row table:

| Axis | Answers | Values | ICM layer | Where it lives |
| Problem type | What KIND of problem is on the table | Un-Defined, Ill-Defined, Well-Defined, with Wicked as a score-triggered escalation rather than a co-equal fourth value | L1 routing | Shared plugin routing code, not a room file |
| Venture stage | How far along is THIS venture | the five `VALID_STAGES` values | L0 identity, with its schema at L3 | The room's `STATE.md`; this file holds only the schema |
| Brain innovation stage | Where in the teaching curriculum a methodology sits | Discovery, Focus, Proof, Creation, Launch Zone | Not a room concept | The Brain's own graph; zero runtime consumers in a room today, deliberately out of scope |

Add one sentence naming the disconfirming test that was actually run: a search for any code path
that reads one vocabulary and derives or writes another found nothing. Add one sentence naming what
would overturn the ruling: a real code path that reads `venture_stage` to set `problem_type` or the
reverse, or a runtime consumer of Brain innovation stage appearing inside a room.

**Section 2: The venture_stage axis schema.** A table with one row per value from `VALID_STAGES`,
in order: `Pre-Opportunity`, `Discovery`, `Validation`, `Design`, `Investment`. For each, one plain
sentence on what being at that stage means for the venture. State that a room is born at
`Pre-Opportunity`. State that progression is bidirectional: a venture can regress, and the history
is preserved rather than overwritten (this repo's Key Decision 14). Add a short honesty note: a
sixth value, `QA`, has been observed written into room index files; it is NOT in the code's own
valid-stage list, so treat it as drift rather than as a legal value.

**Section 3: stage_relevance, per section.** A table with one row per section slug and its
`stage_relevance` array, transcribed from `SECTION_METADATA`. One introductory sentence: this says
when a section is most likely to matter, not when it is forbidden. A room owner working out of order
is not making an error.

**Section 4: default_methodologies at two grains, and which one wins.** This is D-04's core and the
part that is undocumented today. Structure it in three parts:

- *Section grain.* A table: one row per section slug, its `default_methodologies` array, transcribed
  from `SECTION_METADATA`. Static, the same for every room.
- *Family grain.* A table: one row per blueprint family (all 9), its `default_methodologies` array,
  transcribed from `data/room-blueprints.json`. Chosen once, at room birth, from the navigator's
  arrival shape.
- *The precedence rule, stated as what the code actually does.* When a blueprint family is active
  AND its `default_methodologies` array is non-empty, the FAMILY value is written into every
  section's `ROOM.md` frontmatter at scaffold time, and the section-grain value is not used. When no
  family is active, or the family's array is empty, the section-grain value from `SECTION_METADATA`
  is used. Ground it: `lib/core/room-skeleton-scaffold.cjs`, the `familyActive && defaultMethodologies.length > 0`
  ternary in the section write loop. Add one honest sentence: this rule was undocumented before
  Phase 275 and the two grains were a colliding source of truth for the same fact, with nothing
  reconciling them. Documenting it here is the reconciliation.

**Section 5: Which commands write into which section.** Three tiers, and label the confidence of
each honestly, because they are not equal:

- *Ground truth.* The command's own `produces` path names the section. A table: one row per section
  slug listing its ground-truth commands. Two sections have NONE, and the table must say so rather
  than leaving a blank that reads as an oversight: `legal-ip` and `funding`. `financial-model` has
  none directly and relies on a wildcard command.
- *Framework-matched.* A wildcard command whose named framework fits the section's job. A judgment
  call, flagged as such, not equal certainty to ground truth. One row per section.
- *Room-wide infrastructure.* Roughly sixty commands with no `room/` produces path at all. They
  operate on the room as a whole (navigation, export, admin) rather than producing section content,
  so they have no section affinity. Name the category and a handful of examples; do not enumerate
  all of them, it is the same pool for every section.

Re-verify every command named in this section against `data/command-registry.json` before writing.
Do not transcribe the tiering from SEED-084 without re-checking: `275-CONTEXT.md` explicitly warns
it may be stale, and shipping a stale reference into every room is the exact failure this document
is supposed to make impossible.

**Section 6: What changed in Phase 275, and what did not.** Short. The section set grew from 8 to
11 (`opportunity-bank`, `funding`, `strategy`). Five dead or misfiled command citations were
corrected. `value-proposition` is sub-structure inside `business-model`, not a section, and "Value
Proposition" stays reserved for the venture-level Triple Validation gate. `meetings` is a structural
directory that feeds sections, not a section itself, and needed no change. `marketing-sales` and
`research-documents` are deliberately deferred: the 2026-04-14 source shows real intended content
for `marketing-sales` (Marketing Strategies, Sales Strategies and Pipelines) that was simply never
built, so it is deferred rather than rejected.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'),assert=require('node:assert'); const p='templates/room-skeleton/references/SECTION-SCHEMA.md'; const c=fs.readFileSync(p,'utf8'); const s=require('./lib/core/room-skeleton-scaffold.cjs'); const b=require('./data/room-blueprints.json'); const mp=require('./lib/core/model-profiles.cjs'); const reg=require('./data/command-registry.json'); const live=new Set((reg.commands||[]).map(x=>x.command)); assert.ok(!c.startsWith('---'),'no frontmatter'); assert.ok(!c.includes(String.fromCharCode(8212)),'em-dash'); const stages=(mp.VALID_STAGES||['Pre-Opportunity','Discovery','Validation','Design','Investment']); for(const st of stages) assert.ok(c.includes(st),'missing stage '+st); for(const slug of s.SECTION_NAMES) assert.ok(c.includes(slug),'missing section '+slug); for(const fam of Object.keys(b).filter(k=>!k.startsWith('_'))) assert.ok(c.includes(fam),'missing family '+fam); for(const t of ['Un-Defined','Ill-Defined','Well-Defined','Wicked','Pre-Opportunity','familyActive','Layer 3','QA']) assert.ok(c.includes(t),'missing concept '+t); for(const m of c.matchAll(/\/mos:[a-z0-9-]+/g)) assert.ok(live.has(m[0]),'DEAD command '+m[0]+' cited in SECTION-SCHEMA.md'); assert.ok(!/domain-explorer|scenario-analysis/.test(c),'dead slug named as live'); console.log('OK SECTION-SCHEMA.md');"</automated>
  </verify>

  <acceptance_criteria>
    - The file exists at `templates/room-skeleton/references/SECTION-SCHEMA.md`, has no YAML
      frontmatter and no em-dashes
    - All five `VALID_STAGES` values appear, and `QA` appears explicitly labelled as observed drift
      rather than a legal value
    - All 11 section slugs appear
    - All 9 blueprint family names appear
    - The three-axes table names all four problem-type values and states the ICM layer for each axis
    - The precedence rule is stated and grounded in `familyActive` by name
    - Every `/mos:<name>` token in the file resolves to a live entry in `data/command-registry.json`
    - The strings `domain-explorer` and `scenario-analysis` do not appear as live commands anywhere
      in the file (they may appear only inside Section 6's "what changed" note, described as
      corrected; if the automated check's blanket rejection conflicts with that, name the correction
      without using the dead slugs as bare command references)
    - `legal-ip` and `funding` are shown with an explicit "none today" in the ground-truth tier,
      not a blank cell
    - The file contains no venture name, no room name, and no `venture_stage` VALUE presented as
      this room's own: grep for a `venture_stage:` assignment line returns nothing
  </acceptance_criteria>

  <done>
    A room owner can open one file and learn which stage values are legal, what each means, when
    each section matters, which methodologies each section and each family defaults to, which of
    those two wins and why, and which commands actually write where, with every command name
    verified live.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SUB-SCHEMAS.md and prove both reference documents land in a scaffolded room</name>

  <read_first>
    - `commands/funding.md` lines 55-110 - the four-stage machine, the enforcement wording ("No
      skipping stages. No going backward. Each transition is recorded in `transition_history`."),
      the `outcome` subcommand's three values with their stage gating, and the Design Note at line
      105 that separates stage from outcome
    - `commands/opportunities.md` lines 55-120 - the Knight-position definition at line 60 verbatim,
      the two source kinds at 105-106 (grant opportunities carry `risk` because grants are known
      risk; cascade opportunities carry a position from their own schema), and the filter flags at
      113-116 (`--knight`, `--min-confidence` with its 0.0 to 1.0 range)
    - `templates/room-skeleton/section-contracts/funding.md`, `opportunity-bank.md` and
      `team-execution.md` as written by plans 275-05 and 275-06 - all three point AT this document
      for the field lists. Read what they actually say so this file answers exactly the question
      they defer to it, with no contradiction and no gap.
    - `.planning/ROADMAP.md` Phase 275 entry - the `team-execution` Mentor-Profiles half of OQ-7
      (D-12): `SECTION_METADATA`'s prose is thin against real Mentor-Profiles usage
    - `.planning/seeds/SEED-084-....md` `## ADDENDUM 2026-09-04i` in full
    - `lib/core/room-skeleton-scaffold.cjs` `REFERENCE_DOCS` after plan 275-02 - the exact two
      filenames the allowlist expects; a filename mismatch means the document silently never ships
  </read_first>

  <files>templates/room-skeleton/references/SUB-SCHEMAS.md</files>

  <action>
One static markdown document, same rules: no frontmatter, no em-dashes, generic reference only, ships
verbatim to every room. It answers the questions the L2 contracts defer to it.

**Header.** One line: the nested schemas inside individual sections. ICM Layer 3, same as its
sibling `SECTION-SCHEMA.md`. State that a section whose contract says "the full field list lives in
SUB-SCHEMAS.md" is pointing here.

**Section 1: funding, two orthogonal dimensions.** State up front why this is two dimensions and not
one, because collapsing them is the error the command's own Design Note exists to prevent: stage
answers WHERE in the process an entry is; outcome answers WHAT the result was. An entry at Submitted
can carry any of the three outcomes.

- *Stage*, sequential and enforced: `Discovered`, `Researched`, `Applying`, `Submitted`. Give one
  plain sentence per stage on what being there means. State the enforcement verbatim: no skipping
  stages, no going backward, each transition recorded in `transition_history`.
- *Outcome*, orthogonal, not a stage: `awarded` (funder approved; only valid at Submitted),
  `rejected` (funder declined; only valid at Submitted), `withdrawn` (the room owner withdrew; valid
  at any stage).
- *Funding type.* Two types per the 2026-04-14 primary source: `dilutive` (equity and venture
  capital; ownership is given up) and `non-dilutive` (grants and similar; ownership is retained).
  State honestly that the command surface implements the non-dilutive half today and that dilutive
  tracking is a named Phase 275 deferral (D-11), so a dilutive entry is recorded as an ordinary
  dated entry folder without a stage machine until that is built. This must match
  `funding.md`'s contract wording; read it first and agree with it rather than paraphrasing loosely.

**Section 2: opportunity-bank, Knight position plus confidence.** Quote the definition verbatim from
`commands/opportunities.md`: every opportunity carries a Knight position (risk vs uncertainty vs
mixed) and a confidence score; risk is a known problem with quantifiable odds, uncertainty is an
unknown problem requiring exploration, mixed is a contradiction that could go either way. Then state
the field shape: `knight_position` is one of `risk`, `uncertainty`, `mixed`; `confidence` is a
number from 0.0 to 1.0. Then the two source kinds and how each is populated: a grant-sourced
opportunity gets `risk` (grants are known risk) with confidence taken from its relevance score; a
cascade-sourced opportunity takes both from the framework that surfaced it. Name the filter flags
that consume these fields so a reader knows the schema is load-bearing, not decorative.

Explain WHY the distinction earns its place, in one sentence, because a reader who does not
understand it will collapse both into "opportunity": risk and uncertainty are different problems
requiring different next moves, and a bank that does not distinguish them cannot tell you which
opportunities need a decision and which need exploration.

**Section 3: team-execution, Mentor-Profiles.** This is D-12 and it is where this document does work
no existing file does. Define the record. Required fields: `name`, `domain` (what they actually
know), `answers` (the one question they are the right person to answer, stated as a question), and
`last_consulted` (a date). Optional fields: `relationship` (how they came to be in this venture's
orbit), `access` (how to reach them and how much of their time is realistically available), and
`open_asks` (what is currently outstanding with them).

State the filing convention, matching the L4 rule every section contract carries: one folder per
mentor at `team-execution/mentors/<mentor-slug>/<mentor-slug>.md`, never a list inlined into
`ROOM.md`. State the human check that makes the schema worth having: if the `answers` field is
empty or generic, the profile is a contact record, not a mentor profile, and it will not help anyone
decide who to call.

Also state the directory distinction, because three spellings are live and `team-execution.md`'s
contract already names it: `team-execution/` is the ICM section; `team/` is a structural directory
holding the people layer, including AI personas at `team/ai-personas/`; `meetings/` is the other
structural directory, a source that feeds sections rather than a destination.

**Then prove both documents ship.** Scaffold a fresh temporary room and assert:
- `references/ROOM.md` exists (written by the identity loop)
- both `references/SECTION-SCHEMA.md` and `references/SUB-SCHEMAS.md` exist
- `result.warnings` contains zero `reference_doc_missing:` entries
- `result.reference_docs_created.length` equals `REFERENCE_DOCS.length`
- each file is byte-identical to its template (verbatim copy, no substitution pass)
- neither file contains the temp room's own directory name, nor a `venture_stage:` assignment
Run the icm-architect walk test against that room and record the result.
  </action>

  <verify>
    <automated>node -e "const os=require('os'),fs=require('fs'),path=require('path'),assert=require('node:assert'); const s=require('./lib/core/room-skeleton-scaffold.cjs'); const P='templates/room-skeleton/references/SUB-SCHEMAS.md'; const c=fs.readFileSync(P,'utf8'); assert.ok(!c.startsWith('---')); assert.ok(!c.includes(String.fromCharCode(8212))); for(const t of ['Discovered','Researched','Applying','Submitted','awarded','rejected','withdrawn','transition_history','Knight','risk','uncertainty','mixed','confidence','dilutive','non-dilutive','Mentor','last_consulted','ai-personas']) assert.ok(c.includes(t),'SUB-SCHEMAS missing '+t); const d=fs.mkdtempSync(path.join(os.tmpdir(),'m275-l3docs-')); const r=s.scaffoldRoomSkeleton(d,{}); assert.strictEqual(r.ok,true,JSON.stringify(r.errors)); assert.strictEqual(r.warnings.filter(w=>w.startsWith('reference_doc_missing')).length,0,'missing reference docs: '+r.warnings.join(',')); assert.strictEqual(r.reference_docs_created.length,s.REFERENCE_DOCS.length); assert.ok(fs.existsSync(path.join(d,'references','ROOM.md'))); for(const n of s.REFERENCE_DOCS){const a=fs.readFileSync(path.join(d,'references',n),'utf8'); const b=fs.readFileSync(path.join('templates/room-skeleton/references',n),'utf8'); assert.strictEqual(a,b,'not verbatim: '+n); assert.ok(!a.includes(path.basename(d)),'room name leaked into '+n); assert.ok(!/^venture_stage:/m.test(a),'room stage value leaked into '+n);} fs.rmSync(d,{recursive:true,force:true}); console.log('OK SUB-SCHEMAS.md + both L3 docs ship verbatim');"</automated>
  </verify>

  <acceptance_criteria>
    - The file exists at `templates/room-skeleton/references/SUB-SCHEMAS.md` with no frontmatter and
      no em-dashes, and its filename matches `REFERENCE_DOCS` exactly (a mismatch means it silently
      never ships)
    - It contains the four funding stage names in order, the three outcome values, and
      `transition_history`
    - It contains both funding types, `dilutive` and `non-dilutive`, and states which half is
      implemented today in wording consistent with `section-contracts/funding.md`
    - It contains the verbatim Knight-position definition and names `risk`, `uncertainty`, `mixed`
      and the 0.0 to 1.0 confidence range
    - It defines the Mentor-Profiles record with all four required fields including
      `last_consulted`, names the `team-execution/mentors/<slug>/<slug>.md` filing path, and
      distinguishes `team-execution/` from `team/` and `meetings/`
    - Scaffolding a fresh room yields `reference_docs_created.length === REFERENCE_DOCS.length` and
      zero `reference_doc_missing:` warnings
    - Both L3 documents in the scaffolded room are byte-identical to their templates
    - Neither shipped document contains the room's own directory name or a `venture_stage:`
      assignment line (Canon Part 8, and the 2026-09-02 ruling that L3 holds schema and L0 holds
      value)
    - The icm-architect walk test passes against the scaffolded room
  </acceptance_criteria>

  <done>
    Both L3 reference documents exist, ship verbatim into every scaffolded room alongside a
    `references/ROOM.md` identity file, answer exactly the questions the L2 contracts defer to them,
    and provably carry schema without carrying any room's state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `templates/room-skeleton/references/**` -> every user room | Reference text ships verbatim to every room ever scaffolded |
| L3 factory layer -> L0 room state | The layer split only holds if no per-room value leaks into a factory document |
| documented schema -> reader belief | A room owner will act on what this document says; a stale claim here is worse than no document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-275-26 | Information disclosure | Room-specific value leaking into the L3 factory layer | mitigate | Plan 275-02 copies these files verbatim with no substitution pass, so nothing CAN be interpolated. Acceptance criteria additionally grep a scaffolded room's copies for the room directory name and for any `venture_stage:` assignment and require zero hits. Canon Part 8 plus SEED-084's 2026-09-02 ruling. |
| T-275-27 | Spoofing | A documented command or stage value that does not exist in code | mitigate | Every `/mos:<name>` token is asserted against the live registry. The stage value list is taken from `lib/core/model-profiles.cjs` `VALID_STAGES`, not from SEED-084's prose, which quotes a looser observed set including a `QA` value the code does not accept. |
| T-275-28 | Repudiation | The precedence rule documenting an invented rule instead of observed behavior | mitigate | The rule must be derived by reading the `familyActive && defaultMethodologies.length > 0` ternary in the scaffold's section write loop, and the acceptance criteria require `familyActive` to be named by identifier in the document. Documenting an aspirational rule the code does not follow would be a fresh instance of the drift this phase closes. |
| T-275-29 | Tampering | Filename mismatch against the `REFERENCE_DOCS` allowlist | mitigate | The allowlist is explicit, not a glob, so a misnamed file ships nothing and produces a `reference_doc_missing:` warning. Acceptance criteria assert zero such warnings and assert `reference_docs_created.length === REFERENCE_DOCS.length`. |
| T-275-30 | Tampering | Substitution injection via `{{` or `${` in reference prose | mitigate | Copied verbatim, no `renderTemplate` pass. Avoid `{{` and `${` sequences in the prose so a future writer who wrongly adds a substitution pass cannot corrupt a room file. |
| T-275-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. Markdown only. |
</threat_model>

<verification>
1. Both tasks' automated verify commands exit 0
2. Scaffold a fresh temp room: `references/ROOM.md` plus both L3 documents present, zero warnings,
   every file byte-identical to its template
3. Re-scaffold the same room; a sentinel string written into `references/SECTION-SCHEMA.md` survives
   byte-for-byte (Canon Part 9)
4. Every `/mos:` token across both documents resolves in the live command registry
5. Cross-check: every section slug named in `SECTION-SCHEMA.md` exists in `SECTION_NAMES`, and every
   family named exists in `data/room-blueprints.json`
6. `grep -rnP '\\x{2014}' templates/room-skeleton/references/` returns nothing
7. `node scripts/check-room-blueprints.cjs --check` and `node tests/test-blueprint-scaffold.cjs` both exit 0
8. icm-architect walk test against the scaffolded room; result recorded in the SUMMARY
</verification>

<success_criteria>
- ICM Layer 3 exists in a scaffolded room for the first time: a `references/` directory with an
  identity file and two schema documents
- The three stage vocabularies are documented as three axes, each with its layer, per the
  2026-09-02 ruling
- The venture_stage enum is documented from its live code source, with the observed `QA` drift
  named as drift
- Both methodology grains are documented and the precedence rule between them is stated with its
  code grounding
- Every sub-schema the L2 contracts defer to this document is answered here with no gap and no
  contradiction
- Zero room-specific values in either document, proven by grep against a scaffolded room
</success_criteria>

<output>
Create `.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-07-SUMMARY.md` when done.

Record: the verbatim langtalks-graph-expert consult result, explicitly noting whether it matches
SEED-084 `## ADDENDUM 2026-09-04`'s prior `edges: []` result rather than presenting a repeat as new
corroboration; the icm-architect walk-test result; and any place where the live code disagreed with
SEED-084's prose (the `QA` stage value and any stale command tier are the two already expected), so
each disagreement is recorded as a fresh instance of the propagation-gap class rather than silently
resolved in favour of the document.
</output>
