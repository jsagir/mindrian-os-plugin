---
phase: 275-enlarge-room-schema-by-icm-layer
plan: 04
type: execute
wave: 3
depends_on: ["275-01", "275-02", "275-03"]
files_modified:
  - lib/core/room-skeleton-scaffold.test.cjs
  - tests/test-room-skeleton-scaffold-integration.cjs
  - tests/test-119-02-scaffold.sh
  - tests/test-hypothesis-family-and-claim.cjs
autonomous: true
canon_parts: [6, 9]
requirements: [ICML-06]
must_haves:
  truths:
    - "The whole pre-existing scaffold test surface is green again against the 11-section, 6-identity-directory schema"
    - "The exactly-N count literal appears at exactly one sanctioned location in the repo"
    - "Every other count assertion derives its expectation from the table instead of restating a number"
    - "The hypothesis-family test asserts the new truth about opportunity-bank, not a weakened tautology"
  artifacts:
    - path: "lib/core/room-skeleton-scaffold.test.cjs"
      provides: "the single legitimate home of the exactly-N count assertions, updated to 11 and 6"
      contains: "SECTION_NAMES.length, 11"
    - path: "tests/test-119-02-scaffold.sh"
      provides: "Gate 6 rewritten as a drift invariant instead of a restated count"
    - path: "tests/test-hypothesis-family-and-claim.cjs"
      provides: "assumptions still non-frozen; opportunity-bank asserted promoted"
  key_links:
    - from: "tests/test-room-skeleton-scaffold-integration.cjs"
      to: "lib/core/room-skeleton-scaffold.cjs"
      via: "expectations derived from SECTION_NAMES.length rather than a literal"
      pattern: "SECTION_NAMES\\.length"
    - from: "lib/core/room-skeleton-scaffold.test.cjs"
      to: "tests/test-270-baseline-schema-driven.cjs"
      via: "the single-legitimate-home rule that test names by file and line"
      pattern: "270-baseline"
---

## Decision map

See `275-01-PLAN.md`'s Decision map for the full D-01 through D-13 table. This plan carries the
test-surface consequences of **D-01** (8 to 11 sections) and **D-10** (the `references/` L3
directory that takes `IDENTITY_DIRECTORIES` from 5 to 6). It introduces no new decision.

<objective>
Turn the seven documented-RED assertion sites green, without creating a second copy of the count
literal that `tests/test-270-baseline-schema-driven.cjs` explicitly warns about.

Purpose: Phase 270 left a rule in the tree, in a test's own failure message:
`lib/core/room-skeleton-scaffold.test.cjs:251` is "the ONE legitimate home of the exactly-8/exactly-5
assertions; a second copy silently diverges when OQ-7 lands". OQ-7 has now landed. This plan is
where that rule gets honoured rather than quietly broken.

Output: a green scaffold test surface, one count literal, and three sites that derive instead of
restate.
</objective>

<artifacts_this_phase_produces>
See `275-01-PLAN.md`'s `<artifacts_this_phase_produces>` for the phase-wide list. This plan creates
no new symbol; it updates assertions in four existing test files.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-CONTEXT.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-01-SUMMARY.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-02-SUMMARY.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-03-SUMMARY.md
</context>

<mandatory_consults>
The **langtalks-graph-expert** consult binds every plan in this phase (ROADMAP navigator ruling
2026-08-31). This plan is test reconciliation, not layer design, so citing the result recorded by
plan 275-02's executor is sufficient. The applicable **icm-architect** invariant is the same
single-source-of-truth rule: a restated count is a duplicated table wearing a different hat.
</mandatory_consults>

<tasks>

<task type="auto">
  <name>Task 1: Update the sanctioned count home and derive the two scaffold-suite sites</name>

  <read_first>
    - `lib/core/room-skeleton-scaffold.test.cjs` (the file being modified; read in full. Note the
      import line pulling in `SECTION_NAMES` / `SECTION_METADATA` / `IDENTITY_DIRECTORIES`, Test 1's
      hardcoded eight-slug `expectedSections` array at lines 48-60, Test 7 at 133-142, Bonus Test 15
      at 251-254 with its two literals, and Bonus Test 19 at 284-292)
    - `tests/test-room-skeleton-scaffold-integration.cjs` (the file being modified; read in full.
      Note the two `sections_created.length === 8` assertions at lines 53 and 141, the two
      `identity_files_created.length === 5` assertions at 54 and 142, the test names at 41 and the
      second integration test, the "All 8 sections" inline comment at 57, and confirm whether the
      file already imports `SECTION_NAMES` before relying on it)
    - `tests/test-270-baseline-schema-driven.cjs` lines 95-140 - read the `OPERATOR_FILES` constant
      at the top and the baseline.3 rule body. Its failure message names
      `lib/core/room-skeleton-scaffold.test.cjs:251` by file and line as the single legitimate home.
      That is the constraint this task must satisfy, not merely avoid tripping.
    - `275-01-SUMMARY.md` and `275-02-SUMMARY.md` - the two RED baselines this task closes
  </read_first>

  <files>lib/core/room-skeleton-scaffold.test.cjs, tests/test-room-skeleton-scaffold-integration.cjs</files>

  <action>
One principle governs both files: the counts `11` and `6` may appear as literals in exactly ONE
place, Bonus Test 15 of `room-skeleton-scaffold.test.cjs`. Every other site derives.

**1a. `lib/core/room-skeleton-scaffold.test.cjs`, Bonus Test 15 (the sanctioned home).** Change
`assert.strictEqual(SECTION_NAMES.length, 8, 'SECTION_NAMES not 8')` to assert `11` with message
`SECTION_NAMES not 11`, and `assert.strictEqual(Object.keys(IDENTITY_DIRECTORIES).length, 5, 'IDENTITY_DIRECTORIES not 5')`
to assert `6` with message `IDENTITY_DIRECTORIES not 6`. Rename the test title from
"SECTION_NAMES has exactly 8 entries; IDENTITY_DIRECTORIES has exactly 5" to state 11 and 6.

Add a comment block directly above Bonus Test 15 stating: this is the sanctioned single home of the
frozen-count assertions; `tests/test-270-baseline-schema-driven.cjs` baseline.3 names this file and
line in its own failure message and will fail if a second copy appears; Phase 155-05 froze 8,
Phase 179-04 moved the family count 8 to 9, Phase 275 moved the section count 8 to 11 and the
identity count 5 to 6. That comment is what stops the next editor from copying the literal.

**1b. `lib/core/room-skeleton-scaffold.test.cjs`, Test 1.** It hardcodes an eight-slug
`expectedSections` array. Delete the array and iterate `SECTION_NAMES` directly (Test 7 at line 137
already does exactly this, so copy that idiom). Rename the test from "8 ICM sections present after
scaffolding" to "all SECTION_NAMES sections present after scaffolding". Do not change the assertion
bodies.

**1c. `lib/core/room-skeleton-scaffold.test.cjs`, Test 7 and Bonus Test 19.** Bodies already iterate
`SECTION_NAMES` and need no logic change. Only drop "8" from their titles so the file carries no
stale count in prose.

**1d. `tests/test-room-skeleton-scaffold-integration.cjs`.** Replace both
`assert.strictEqual(r.sections_created.length, 8)` with
`assert.strictEqual(r.sections_created.length, SECTION_NAMES.length)`, and both
`assert.strictEqual(r.identity_files_created.length, 5)` with
`assert.strictEqual(r.identity_files_created.length, Object.keys(IDENTITY_DIRECTORIES).length)`.
Import `IDENTITY_DIRECTORIES` from the scaffold if the file does not already; `SECTION_NAMES` is
already imported (the file iterates it at line 57).

Do NOT write the literals `11` or `6` into this file. That is the specific failure
`test-270-baseline-schema-driven.cjs` predicted and it is the whole point of this task.

Update the test-name string at line 41 ("8 sections + 5 identity dirs + 3 root files") and the "All
8 sections" inline comment at line 57 so neither states a count.

**1e. Add one new assertion to the integration suite**, in the fresh-scaffold test: assert that
`r.warnings` is an array and that `r.ok` is `true` even when it is non-empty. Plan 275-02 introduced
the warnings channel precisely so a not-yet-authored template cannot turn the scaffold red; that
invariant currently has no test and would regress invisibly.
  </action>

  <verify>
    <automated>node --test lib/core/room-skeleton-scaffold.test.cjs && node tests/test-room-skeleton-scaffold-integration.cjs && node tests/test-270-baseline-schema-driven.cjs && node -e "const fs=require('fs'),assert=require('node:assert'); const a=fs.readFileSync('lib/core/room-skeleton-scaffold.test.cjs','utf8'); assert.strictEqual((a.match(/SECTION_NAMES\.length, 11/g)||[]).length,1,'count literal must appear exactly once'); assert.strictEqual((a.match(/IDENTITY_DIRECTORIES\)\.length, 6/g)||[]).length,1,'identity literal must appear exactly once'); const b=fs.readFileSync('tests/test-room-skeleton-scaffold-integration.cjs','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*)/.test(l)).join('\n'); assert.ok(!/length,\s*11\b/.test(b)&&!/length,\s*6\b/.test(b),'no second copy of the count literal allowed'); console.log('OK single-home rule holds');"</automated>
  </verify>

  <acceptance_criteria>
    - `node --test lib/core/room-skeleton-scaffold.test.cjs` exits 0 with zero failing subtests
    - `node tests/test-room-skeleton-scaffold-integration.cjs` exits 0
    - `node tests/test-270-baseline-schema-driven.cjs` exits 0
    - `grep -c "SECTION_NAMES.length, 11" lib/core/room-skeleton-scaffold.test.cjs` returns `1`
    - `grep -c "IDENTITY_DIRECTORIES).length, 6" lib/core/room-skeleton-scaffold.test.cjs` returns `1`
    - After stripping comment lines, `tests/test-room-skeleton-scaffold-integration.cjs` contains
      neither `length, 11` nor `length, 6`
    - `grep -v '^\s*//' lib/core/room-skeleton-scaffold.test.cjs | grep -c "'problem-definition', 'market-analysis'"`
      returns `0` (Test 1's hardcoded slug array is gone)
    - The integration suite contains an assertion that `r.warnings` is an array and `r.ok` is true
  </acceptance_criteria>

  <done>
    The two scaffold suites are green against 11 sections and 6 identity directories, the count
    literal lives at exactly one sanctioned location with a comment saying so, and the warnings
    channel introduced by plan 275-02 has a regression test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the 119-02 shell gate as a drift invariant and flip the hypothesis-family assertion</name>

  <read_first>
    - `tests/test-119-02-scaffold.sh` (the file being modified; read in full. Note the Gate 6 header
      comment at line 10, the inline `node -e` check at lines 56-60 with its two `!==` guards, the
      `fail` helper's signature, and the closing echo at line 62 which also states "8 ICM sections")
    - `tests/test-hypothesis-family-and-claim.cjs` (the file being modified; read in full. Note the
      header comment at lines 7-11 describing the hypothesis family's `assumptions` and
      `opportunity-bank` non-frozen slugs, the check at line 82-84 asserting
      `check-room-blueprints.cjs` still references `assumptions`, and the two assertions at lines
      104-110)
    - `data/room-blueprints.json` `hypothesis` family (lines 136-151) and its `_sections_note`,
      which still describes `opportunity-bank` as non-frozen; note whether that note is now stale
    - `scripts/check-room-blueprints.cjs` after plan 275-01 - `EXTENDED_VALID_SLUGS_FOR_CHECK` now
      holds only `assumptions`
    - `275-01-SUMMARY.md`
  </read_first>

  <files>tests/test-119-02-scaffold.sh, tests/test-hypothesis-family-and-claim.cjs</files>

  <action>
**2a. `tests/test-119-02-scaffold.sh`, Gate 6.** The gate currently asserts two literal counts
(`SECTION_NAMES.length !== 8`, `Object.keys(IDENTITY_DIRECTORIES).length !== 5`). Both are the
"second copy" pattern. Rewrite the inline `node -e` block to assert the real invariant this gate
exists to protect, which is table drift rather than any particular number:

- `SECTION_NAMES.length` is greater than 0
- `Object.keys(SECTION_METADATA).length` equals `SECTION_NAMES.length`
- every entry in `SECTION_NAMES` has a corresponding `SECTION_METADATA` key (exit non-zero naming
  the offending slug otherwise)
- every `SECTION_METADATA` entry has a non-empty `purpose`, a non-empty `statement`, and array
  `stage_relevance` and `default_methodologies` fields
- `Object.keys(IDENTITY_DIRECTORIES).length` is greater than 0 and every entry has a
  `directory_type` and a `purpose`

Keep the existing `fail 6 "..."` call shape and exit conventions; only the assertion body changes.
Update the Gate 6 header comment at line 10 and the closing echo at line 62 so neither states a
count. This gate becomes strictly stronger than it was: it previously proved a number, it now proves
the tables cannot drift apart.

**2b. `tests/test-hypothesis-family-and-claim.cjs`.** The assertion at lines 109-110
(`SECTION_NAMES.includes('opportunity-bank') === false`) is now factually inverted by D-01. Flip it
to assert `true`, with a message naming Phase 275 and D-01 and pointing at
`SEED-084`'s `## RULING 2026-09-04`. Do NOT delete it and do NOT weaken it to a tautology: its job
is to pin which slugs are frozen, and that job is still live.

Keep the `assumptions` assertion at lines 107-108 exactly as it is. `assumptions` remains a
non-frozen, intent-only slug (`EXTENDED_VALID_SLUGS_FOR_CHECK` still carries it and the
`hypothesis` family still uses it), so that assertion is still true and still load-bearing.

Rename the test title from "the frozen SECTION_NAMES table is untouched by the family add (no
assumptions slug)" to something accurate, for example "assumptions stays non-frozen; Phase 275
promoted opportunity-bank into SECTION_NAMES". Update the file header comment at lines 7-11 which
describes both slugs as non-frozen; only `assumptions` is now.

Check `data/room-blueprints.json`'s `hypothesis` family `_sections_note`, which still says
"assumptions + opportunity-bank are non-frozen slugs ... the scaffold skips them gracefully". That
sentence is now half false. Correcting it belongs to whoever owns that file; plan 275-01 owns it.
If the note is still stale when this task runs, record it in the SUMMARY as a named follow-up
rather than editing a file outside this plan's `files_modified`.
  </action>

  <verify>
    <automated>bash tests/test-119-02-scaffold.sh && node tests/test-hypothesis-family-and-claim.cjs && node -e "const fs=require('fs'),assert=require('node:assert'); const sh=fs.readFileSync('tests/test-119-02-scaffold.sh','utf8').split('\n').filter(l=>!/^\s*#/.test(l)).join('\n'); assert.ok(!/!== 8|!== 5|!== 11|!== 6/.test(sh),'Gate 6 must assert drift, not a literal count'); assert.ok(/SECTION_METADATA/.test(sh),'Gate 6 must cross-check SECTION_METADATA'); const hy=fs.readFileSync('tests/test-hypothesis-family-and-claim.cjs','utf8'); assert.ok(/includes\('opportunity-bank'\),\s*true/.test(hy)||/includes\('opportunity-bank'\), true/.test(hy),'opportunity-bank assertion must be flipped to true'); assert.ok(/includes\('assumptions'\),\s*false/.test(hy),'assumptions assertion must remain false'); console.log('OK gate + hypothesis');"</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/test-119-02-scaffold.sh` exits 0 and its final echo line states no section or
      identity count
    - After stripping `#` comment lines, `tests/test-119-02-scaffold.sh` matches none of
      `!== 8`, `!== 5`, `!== 11`, `!== 6`
    - Gate 6's body references `SECTION_METADATA` and fails non-zero if a `SECTION_NAMES` entry has
      no matching metadata key. Prove it by temporarily deleting one `SECTION_METADATA` key in a
      scratch copy of the scaffold and confirming the gate exits non-zero, then discard the scratch
      copy. Record the result of that negative test in the SUMMARY.
    - `node tests/test-hypothesis-family-and-claim.cjs` exits 0
    - `tests/test-hypothesis-family-and-claim.cjs` asserts
      `SECTION_NAMES.includes('opportunity-bank')` is `true` and
      `SECTION_NAMES.includes('assumptions')` is `false`
    - `node scripts/check-room-blueprints.cjs --check` exits 0 (do-not-regress)
  </acceptance_criteria>

  <done>
    Gate 6 proves a drift invariant instead of restating a number and has been negative-tested,
    the hypothesis-family suite asserts the post-D-01 truth without being weakened, and both files
    are free of count literals.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> release gate | These suites feed `scripts/verify-release` and `doctor --acceptance`; a weakened assertion silently lowers the release bar |
| scratch copy of `room-skeleton-scaffold.cjs` (negative test) | Task 2's negative test temporarily mutates a copy of a shipped module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-275-14 | Tampering | Assertion weakening to make a red test green | mitigate | Every rewritten assertion must be strictly equal or stronger. Acceptance criteria pin the exact post-state of each: the count home keeps a literal, the derived sites gain a cross-table check, Gate 6 gains a metadata cross-check and must be negative-tested to prove it can still fail. |
| T-275-15 | Repudiation | A silently-skipped subtest reported as a pass | mitigate | `node --test` reports skipped subtests separately from passes; the acceptance criterion is "zero failing subtests" AND a green exit code, and the SUMMARY records the pass/skip counts, not just the exit status. This repo's own history includes false-success and silent-skip incidents; do not trust a runner's summary line without reading its counts. |
| T-275-16 | Tampering | Task 2's negative test mutating a shipped module | mitigate | Perform the negative test on a scratch copy in a temp directory, never by editing `lib/core/room-skeleton-scaffold.cjs` in place. Confirm `git status` shows that file unmodified after the negative test. |
| T-275-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. No dependency change. |
</threat_model>

<verification>
1. `node --test lib/core/room-skeleton-scaffold.test.cjs` exits 0, zero failing subtests
2. `node tests/test-room-skeleton-scaffold-integration.cjs` exits 0
3. `bash tests/test-119-02-scaffold.sh` exits 0
4. `node tests/test-hypothesis-family-and-claim.cjs` exits 0
5. `node tests/test-270-baseline-schema-driven.cjs` exits 0
6. `node tests/test-blueprint-scaffold.cjs` exits 0
7. `node tests/test-section-nodes-birth-and-migration.cjs` exits 0
8. `node scripts/check-room-blueprints.cjs --check` exits 0
9. `git status --porcelain lib/core/room-skeleton-scaffold.cjs` is empty (the negative test did not mutate the shipped module)
10. No em-dashes in any of the four modified files
</verification>

<success_criteria>
- All seven previously-RED assertion sites (four section-count from 275-01, three identity-count
  from 275-02) are green
- The count literal exists at exactly one sanctioned location, with a comment naming why
- Three sites derive their expectation from the table instead of restating a number
- Gate 6 is strictly stronger than before and has been proven able to fail
- The hypothesis-family assertion states the new truth, unweakened
</success_criteria>

<output>
Create `.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-04-SUMMARY.md` when done.

Record: the pass/skip/fail counts from `node --test` (not just the exit code); the result of Gate
6's negative test; and whether the `hypothesis` family's stale `_sections_note` in
`data/room-blueprints.json` was already corrected by plan 275-01 or is being carried as a follow-up.
</output>
