---
phase: 275-enlarge-room-schema-by-icm-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/room-skeleton-scaffold.cjs
  - lib/core/section-registry.cjs
  - data/room-blueprints.json
  - scripts/check-room-blueprints.cjs
autonomous: true
canon_parts: [7, 9, 11]
requirements: [ICML-01, ICML-02, ICML-03, ICML-04]
must_haves:
  truths:
    - "A room scaffolded with no blueprint family gets 11 section directories, not 8"
    - "Every default_methodologies slug in SECTION_METADATA names a command that exists in data/command-registry.json"
    - "Every default_methodologies slug is filed under the section its command actually produces into"
    - "opportunity-bank, funding and strategy each carry a registered De Stijl label and color as CORE sections"
    - "The blueprint CI gate passes against the grown vocabulary"
  artifacts:
    - path: "lib/core/room-skeleton-scaffold.cjs"
      provides: "SECTION_NAMES 11 entries, SECTION_METADATA 11 keys each with statement/purpose/stage_relevance/default_methodologies"
      contains: "'strategy'"
    - path: "lib/core/section-registry.cjs"
      provides: "CORE_SECTIONS 11 entries, STRUCTURAL_DIRS includes references, isIndexableArtifactFile excludes CONTEXT.md"
      contains: "'strategy'"
    - path: "data/room-blueprints.json"
      provides: "venture family reproduces all 11 slugs; program family carries funding"
      contains: "\"strategy\""
    - path: "scripts/check-room-blueprints.cjs"
      provides: "fallback slug set and EXTENDED_VALID_SLUGS_FOR_CHECK reconciled to the 11-slug vocabulary"
      contains: "Phase 275"
  key_links:
    - from: "scripts/check-room-blueprints.cjs"
      to: "lib/core/room-skeleton-scaffold.cjs"
      via: "require() of SECTION_NAMES at loadValidSectionSlugs"
      pattern: "scaffold\\.SECTION_NAMES"
    - from: "lib/core/room-skeleton-scaffold.cjs"
      to: "data/room-blueprints.json"
      via: "resolveBlueprint frozenSet filter"
      pattern: "frozenSet\\.has"
---

## Decision map (D-NN ids minted at plan time from `275-CONTEXT.md`'s `<decisions>` headings)

`275-CONTEXT.md` states its decisions as prose headings, not numbered ids. These ids are minted
here so every task can cite the exact decision it implements, and so the same ids are stable
across all six plans in this phase.

| ID | Decision (verbatim source heading in `275-CONTEXT.md`) |
|---|---|
| D-01 | Section-set grows 8 to 11: add `opportunity-bank`, `funding`, `strategy` |
| D-02 | `value-proposition` is NOT a new section; sub-structure inside `business-model`; no per-section `value_proposition` field |
| D-03 | `meetings` NO SCHEMA CHANGE (already `STRUCTURAL_DIRS`) |
| D-04 | L3 promotions: `stage_relevance` + `default_methodologies` at BOTH grains, precedence documented |
| D-05 | Propagation-gap citation fixes (`domain-explorer` dead, `scenario-analysis` retargets to `strategy`, `trending-to-absurd` refiles to `opportunity-bank`, `analyze-needs` refiles to `market-analysis`, `/mos:persona` team-slug note) |
| D-06 | `strategy` section Tier 1 command set |
| D-07 | `opportunity-bank` to `funding` pipeline relationship named in both L2 contracts, sub-schemas in L3 |
| D-08 | `funding` L2 contract names BOTH Dilutive and Non-Dilutive explicitly |
| D-09 | `solution-design` moat/defensibility Human-check cross-linked to `competitive-analysis` |
| D-10 | Discretion RESOLVED: L1 `STATEMENT` is a YAML frontmatter key in each section's `ROOM.md`, not a separate `STATEMENT.md` |
| D-11 | Discretion RESOLVED: dilutive/equity tracking is DOCUMENTED in the L2 contract, NOT BUILT this phase |
| D-12 | Discretion RESOLVED: `team-execution` Mentor-Profiles thickening is purpose text + L3 sub-schema + L2 contract naming, no new code path |
| D-13 | Discretion RESOLVED: migration is additive and idempotent; L4 inline-content drift is report-only, never rewritten |

<objective>
Grow the room section vocabulary from 8 to 11 (D-01) and correct every propagation-gap citation
found while grounding it (D-05), in the four data/config files that own the vocabulary.

Purpose: this is the L0/L4 consequence of the ICM layering work the rest of the phase does. Every
later plan in this phase reads the grown table; nothing else can start until the table is right.

Output: `SECTION_NAMES` at 11 entries, `SECTION_METADATA` at 11 keys each carrying a new
`statement` field (D-10) plus a corrected `default_methodologies` array, `CORE_SECTIONS` at 11
entries, `data/room-blueprints.json` reconciled, and the blueprint CI gate green.
</objective>

<artifacts_this_phase_produces>
Symbols and files this PHASE creates (not pre-existing; downstream drift checks should treat
these as new, not as unacknowledged references to existing code):

- `SECTION_NAMES` new entries: `opportunity-bank`, `funding`, `strategy`
- `SECTION_METADATA` new keys: `opportunity-bank`, `funding`, `strategy`
- `SECTION_METADATA` new field on all 11 entries: `statement`
- `CORE_SECTIONS` new entries: `opportunity-bank`, `funding`, `strategy` (color `#8B2942` for `strategy`)
- `STRUCTURAL_DIRS` new entry: `references`
- `IDENTITY_DIRECTORIES` new key: `references` (plan 275-02)
- Template substitution key `{{STATEMENT}}` and ROOM.md frontmatter key `statement` (plan 275-02)
- Scaffold functions `writeSectionContracts()` and `writeReferenceDocs()`; result fields
  `contracts_created` and `reference_docs_created`; constants `SECTION_CONTRACTS_DIR` and
  `REFERENCE_TEMPLATES_DIR` (plan 275-02)
- New template directory `templates/room-skeleton/section-contracts/` with 11 files, one per
  section slug (plans 275-05 and 275-06)
- New template directory `templates/room-skeleton/references/` with `SECTION-SCHEMA.md` and
  `SUB-SCHEMAS.md` (plan 275-07)
- New script `scripts/migrate-room-sections-v275.cjs` with flags `--dry-run`, `--help`,
  `--report-drift` (plan 275-08)
- New tests `tests/test-275-section-schema.cjs` and `tests/run-all-275.sh` (plan 275-08)
- Requirement ids `ICML-01` through `ICML-16` (plan 275-08 writes them into REQUIREMENTS.md)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-CONTEXT.md
@.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-PATTERNS.md
</context>

<mandatory_consults>
Two standing consults bind every plan in this phase (ROADMAP Phase 275, navigator ruling
2026-08-31, and `feedback_mindrianos_dev_consult_icm_architect.md`):

- **langtalks-graph-expert** (`mcp__langtalks-graph-expert__*`), continuously, at every stage.
  **Planner-time status, recorded honestly:** the `mcp__langtalks-graph-expert__*` tool was NOT
  present in the planning session, so no live consult backs this plan's layer design. This is the
  same honest-unavailability record SEED-084's own `## ADDENDUM 2026-09-02` made, not a skipped
  step papered over. **Executor obligation:** attempt a live `relationship_path` or
  `query_relationship` call on "layered context hierarchy for agent workspaces" and
  "section-scoped tool subsets vs one global toolset" before starting Task 1, and record the
  result (including an honest `edges: []` or an unreachable server) in the plan SUMMARY. Do NOT
  fabricate a consult result; do NOT block on it either.
- **icm-architect** (`~/.claude/skills/icm-architect/`) - the ten invariants, six-forms taxonomy
  and walk test are the checklist this schema change must clear. Its `references/core.md` L3
  doctrine ("L3 = the recipe... L4 = the ingredients and the dish") is the grounding for the
  whole layer split.
</mandatory_consults>

<tasks>

<task type="auto">
  <name>Task 1: Grow SECTION_NAMES to 11, add the three metadata entries, correct all seven citations, add the statement field</name>

  <read_first>
    - `lib/core/room-skeleton-scaffold.cjs` (the file being modified; read all 437 lines, notably
      the `SECTION_NAMES` table at 36-45, `SECTION_METADATA` at 47-56, the FROZEN TABLE CONTRACT
      comment at 350-354, the silent-skip comment at 239-243, and the `sectionSubs` call site at
      374-380)
    - `.planning/phases/275-.../275-PATTERNS.md` sections "lib/core/room-skeleton-scaffold.cjs"
      and "Frozen-table extension is versioned, not truly immutable" - the Phase 179-04 precedent
      that makes this a documented extension rather than a contract violation
    - `data/command-registry.json` - do NOT read whole (it is large). Query it with
      `node -e` against the `commands[].command` and `commands[].produces` fields to re-verify
      every slug named in the action below. `275-CONTEXT.md` explicitly warns the tiering in
      SEED-084 may be stale; the registry is ground truth.
    - `.planning/seeds/SEED-084-....md` sections `## ADDENDUM 2026-09-04e` (the full command
      tiering) and `## ADDENDUM 2026-09-04g` (the `strategy` Tier 1 set)
    - langtalks-graph-expert consult per `<mandatory_consults>` above (layer-design task)
  </read_first>

  <files>lib/core/room-skeleton-scaffold.cjs</files>

  <action>
Extend the two frozen tables in place. Do NOT create a new module, a new registry, or a second
source of truth: Canon Part 7 and `275-CONTEXT.md`'s canonical_refs both say this phase EXTENDS
this mechanism.

**1a. `SECTION_NAMES`** - append exactly three slugs in this order after `financial-model`:
`opportunity-bank`, `funding`, `strategy`. Update the JSDoc above the table ("The 8 ICM sections")
to say 11, and update the module header comment at line 6 ("canonical 8-section ICM structure")
to say 11. Add a versioned extension comment in the Phase-179-04 house style directly above the
table: `// Phase 275 (D-01): grew SECTION_NAMES 8 -> 11 (opportunity-bank, funding, strategy).`
plus `// See 275-CONTEXT.md. Phase 155-05 froze 8; Phase 179-04 moved EXPECTED_FAMILY_COUNT 8 -> 9;`
plus `// the table is versioned, not immutable.`

**1b. FROZEN TABLE CONTRACT comment (line ~351)** - it currently reads "SECTION_NAMES +
SECTION_METADATA are never modified". Rewrite it to state the real contract: the tables are never
modified SILENTLY or by a drive-by edit; a deliberate, versioned, phase-cited extension is the
established move. Name Phase 179-04 and Phase 275 as the two precedents. Do not delete the comment.

**1c. Add a `statement` field (D-10) to all 11 `SECTION_METADATA` entries.** This is the ICM L1
"one sentence always true, always visible". Use exactly these strings (no em-dashes anywhere):

- `problem-definition`: `This section holds the problem this venture is actually trying to solve, stated so a stranger can restate it.`
- `market-analysis`: `This section holds who has this problem, how many of them there are, and what they do about it today.`
- `solution-design`: `This section holds the solution and the technical choices behind it, and why each choice was made.`
- `business-model`: `This section holds how this venture makes money, and the value proposition that claim rests on.`
- `competitive-analysis`: `This section holds who else is solving this, and what would still be true if a competitor copied us.`
- `team-execution`: `This section holds who does the work, who advises it, and what happens next.`
- `legal-ip`: `This section holds the legal structure, the agreements, and what is actually protected.`
- `financial-model`: `This section holds the numbers, the assumptions under them, and what breaks them.`
- `opportunity-bank`: `This section banks the opportunities found but not yet chosen, each with its Knight position and confidence.`
- `funding`: `This section tracks funding paths, dilutive and non-dilutive, from discovery through submission to outcome.`
- `strategy`: `This section holds where this venture could go and what is holding it back: the scenarios and the reverse salients.`

Note on `business-model`'s statement: it carries the value-proposition claim deliberately, per
D-02. Do NOT add a `value_proposition` key to this table or anywhere in
`lib/core/frontmatter-schemas.cjs`; "Value Proposition" is the reserved PWS Triple-Validation
third gate. `/mos:validate-proposition` already produces to `room/business-model/value-proposition/*`
in `data/command-registry.json`, which is the live-code corroboration of D-02.

**1d. Correct the `default_methodologies` citations on the existing 8 (D-05).** Every slug below
was re-verified against `data/command-registry.json`'s `produces` field at plan time; re-verify
before writing. Target arrays:

- `problem-definition`: `['explore-domains', 'beautiful-question', 'diagnose']`
  (`domain-explorer` is DEAD, live replacement is `explore-domains` which produces
  `room/problem-definition/domain-decomposition/*`; `trending-to-absurd` is LIVE but MISFILED and
  moves to `opportunity-bank`; `diagnose` produces `room/problem-definition/diagnosis/*`.)
- `market-analysis`: `['analyze-needs', 'user-needs']`
  (`domain-explorer` DEAD; `scenario-analysis` DEAD and retargets to `strategy`; `analyze-needs`
  is LIVE but MISFILED under `team-execution` and produces `room/market-analysis/jtbd-analysis/*`;
  `user-needs` produces `room/market-analysis/user-needs/*`.)
- `solution-design`: `['bono', 'structure-argument', 'think-hats']`
  (`bono` produces `room/solution-design/*`, the only Tier 1 command for this section; the two
  existing wildcard entries are live and stay.)
- `business-model`: `['lean-canvas', 'validate-proposition', 'structure-argument']`
  (`scenario-analysis` DEAD, retargets to `strategy`; `lean-canvas` produces
  `room/business-model/lean-canvas/*`; `validate-proposition` produces
  `room/business-model/value-proposition/*`.)
- `competitive-analysis`: `['compare-ventures', 'challenge-assumptions']`
  (`find-bottlenecks` retargets to `strategy` where it is Tier 1 per D-06; `compare-ventures`
  produces `room/competitive-analysis/comparison/*`.)
- `team-execution`: `['leadership', 'think-hats']`
  (`analyze-needs` refiles to `market-analysis`; `leadership` produces
  `room/team-execution/leadership/*`.)
- `legal-ip`: `['structure-argument', 'challenge-assumptions']`
  (unchanged plus the one framework-matched addition; `legal-ip` has zero Tier 1 commands today,
  a real structural gap named in SEED-084 `## ADDENDUM 2026-09-04e`, not fixed here.)
- `financial-model`: `['build-thesis', 'grade']`
  (`scenario-analysis` DEAD, retargets to `strategy`; `grade` produces `room/**/grades/*`.)

**1e. Add the three new `SECTION_METADATA` entries**, same object shape as the existing eight
(`purpose`, `stage_relevance`, `default_methodologies`, plus the new `statement`):

- `opportunity-bank`: purpose `Bank the opportunities discovered before any one is chosen.`;
  stage_relevance `['Pre-Opportunity', 'Discovery']`;
  default_methodologies `['trending-to-absurd', 'whitespace', 'futures']`
  (all three produce into `room/opportunity-bank/...`, Tier 1 ground truth).
- `funding`: purpose `Track funding paths, dilutive and non-dilutive, from discovery to submission.`;
  stage_relevance `['Validation', 'Investment']`;
  default_methodologies `['mullins', 'grade']`
  (this section has ZERO Tier 1 commands today; these two are the only indirect touches it gets,
  a documented gap per SEED-084 `## ADDENDUM 2026-09-04e`, not a claim of coverage).
- `strategy`: purpose `Work the futures and the bottlenecks: where this venture could go and what is holding it back.`;
  stage_relevance `['Discovery', 'Design']`;
  default_methodologies `['scenario-plan', 'find-bottlenecks']`
  (the D-06 Tier 1 set is `scenario-plan`, `find-bottlenecks`, `rs-experts`, `rs-explain`,
  `rs-fetch`, `rs-thesis`; the two named here are the defaults, the full six live in the L3
  reference document plan 275-07 writes).

**1f. Comment the reversal at the silent-skip site (line ~239-243).** The comment currently reads
`(e.g. "opportunity-bank" is not a scaffold section; skip it silently)`. That example is now
false. Replace the example with `assumptions` (still a non-frozen slug used by the `hypothesis`
family) and add: `// Phase 275 (D-01): opportunity-bank joined SECTION_NAMES, so this filter no`
plus `// longer drops it. The filter logic itself is unchanged; only the table grew.`

Leave `resolveBlueprint`, `scaffoldRoomSkeleton`, the writer loops, and `module.exports`
untouched in this task. Rendering the new `statement` field is plan 275-02's job.
  </action>

  <verify>
    <automated>node -e "const s=require('./lib/core/room-skeleton-scaffold.cjs'); const r=require('./data/command-registry.json'); const live=new Set((r.commands||[]).map(c=>c.command)); const assert=require('node:assert'); assert.strictEqual(s.SECTION_NAMES.length,11,'SECTION_NAMES must be 11'); for(const k of ['opportunity-bank','funding','strategy']) assert.ok(s.SECTION_NAMES.includes(k),'missing '+k); assert.deepStrictEqual(Object.keys(s.SECTION_METADATA).sort(), s.SECTION_NAMES.slice().sort(), 'METADATA/NAMES drift'); for(const k of s.SECTION_NAMES){const m=s.SECTION_METADATA[k]; assert.ok(typeof m.statement==='string'&&m.statement.length>20,'no statement for '+k); assert.ok(!m.statement.includes(String.fromCharCode(8212)),'em-dash in statement for '+k); for(const slug of m.default_methodologies) assert.ok(live.has('/mos:'+slug), 'DEAD citation '+slug+' under '+k);} console.log('OK 11 sections, 0 dead citations');"</automated>
  </verify>

  <acceptance_criteria>
    - `node -e "console.log(require('./lib/core/room-skeleton-scaffold.cjs').SECTION_NAMES.length)"` prints `11`
    - Every slug in every `SECTION_METADATA[*].default_methodologies` array resolves to a
      `/mos:<slug>` entry in `data/command-registry.json` (the verify command asserts this and
      exits non-zero naming the offending slug otherwise)
    - `grep -c "domain-explorer\|scenario-analysis" lib/core/room-skeleton-scaffold.cjs` returns
      `0` (both dead slugs fully removed, not just one occurrence)
    - `grep -n "trending-to-absurd" lib/core/room-skeleton-scaffold.cjs` shows the slug only on
      the `opportunity-bank` line, never on `problem-definition`
    - `grep -n "analyze-needs" lib/core/room-skeleton-scaffold.cjs` shows the slug only on the
      `market-analysis` line, never on `team-execution`
    - `grep -v '^\s*//' lib/core/room-skeleton-scaffold.cjs | grep -c "value_proposition"` returns
      `0` (D-02: the reserved PWS term is not introduced as a section field)
    - All 11 `SECTION_METADATA` entries carry a non-empty `statement` string, verified by the
      automated command above
    - `node -e "const fs=require('fs');const s=fs.readFileSync('lib/core/room-skeleton-scaffold.cjs','utf8');process.exit(s.includes(String.fromCharCode(8212))?1:0)"` exits `0` (no em-dashes, CLAUDE.md conventions)
  </acceptance_criteria>

  <done>
    `SECTION_NAMES` has 11 entries, `SECTION_METADATA` has 11 matching keys each with a
    `statement`, zero dead command citations remain, the two misfiled citations sit under the
    section their command actually produces into, and the FROZEN TABLE CONTRACT comment states the
    real (versioned-extension) contract rather than an aspirational one.
  </done>
</task>

<task type="auto">
  <name>Task 2: Promote opportunity-bank and funding into CORE_SECTIONS, register strategy, and keep the new L2/L3 files out of section discovery</name>

  <read_first>
    - `lib/core/section-registry.cjs` (the file being modified; all 160 lines. Note
      `CORE_SECTIONS` at 16-25, `EXTENDED_SECTION_META` at 30-34 where `opportunity-bank` and
      `funding` already carry a registered label and color, `STRUCTURAL_DIRS` at 39,
      `isIndexableArtifactFile` at 48-53, and the `discoverSections` qualification pass at 63-158)
    - `lib/core/icm-forest.cjs` lines 110-155 - `classifyDirectory` gives `STRUCTURAL_DIRS`
      precedence over `IDENTITY_DIRECTORIES`, which is exactly the precedence the new `references`
      directory needs (reference material kept structurally separate from working artifacts)
    - `tests/test-270-baseline-schema-driven.cjs` lines 95-130 - the baseline rule that
      `STRUCTURAL_DIRS.includes(name)` is the sanctioned escape hatch and a bare literal is not
    - `.planning/phases/275-.../275-PATTERNS.md` section "lib/core/section-registry.cjs"
  </read_first>

  <files>lib/core/section-registry.cjs</files>

  <action>
Four edits in one file, all in service of D-01.

**2a. Move `opportunity-bank` and `funding` from `EXTENDED_SECTION_META` into `CORE_SECTIONS`,
byte-identical** (label `OPPORTUNITY BANK` / color `#8B6914`, label `FUNDING` / color `#1A5276`).
This is a copy-paste move, not a re-authoring: both already carry registered De Stijl identities.
Leave `personas` in `EXTENDED_SECTION_META` (it is not part of D-01).

**2b. Add a `strategy` entry to `CORE_SECTIONS`**: label `STRATEGY`, color `#8B2942`. That hex is
chosen because it collides with none of the eleven colors already in use across both tables
(`#A63D2F`, `#C8A43C`, `#5C5A56`, `#2D6B4A`, `#B5602A`, `#1E3A6E`, `#6B4E8B`, `#2A6B5E`,
`#8B6914`, `#1A5276`, `#6C3483`). Verify no collision before writing.

Update the JSDoc above `CORE_SECTIONS` ("8 DD-aligned core sections") to 11 and add
`// Phase 275 (D-01): promoted opportunity-bank + funding from EXTENDED_SECTION_META; minted strategy.`

**2c. Add `references` to `STRUCTURAL_DIRS`** so the new ICM L3 factory directory (created by plan
275-02) is never mis-discovered as a scored section. `STRUCTURAL_DIRS` becomes
`['meetings', 'team', 'references']`. Add a comment naming why: L3 reference material is a stable
factory recipe, not a destination that accumulates methodology content, the same distinction that
already keeps `meetings` structural (D-03 confirms `meetings` itself needs no change). Do NOT
remove `meetings` or `team`; `tests/test-270-baseline-schema-driven.cjs` self-checks that
`STRUCTURAL_DIRS` contains `team`.

**2d. Exclude `CONTEXT.md` from `isIndexableArtifactFile`.** The predicate currently excludes
`STATE.md` and `ROOM.md` only. Plans 275-05 and 275-06 put a `CONTEXT.md` in every section
directory; without this edit each one would be counted as a real artifact by
`lazygraph-ops.cjs` (which shares this single predicate) and by the nested-artifact qualification
pass in `discoverSections`, inflating every room's artifact count by 11 the moment the migration
runs. Add `CONTEXT.md` to the same identity-file exclusion line and comment it as an ICM L2
contract file, a sibling of the L0 `ROOM.md` identity file, not an L4 artifact.
  </action>

  <verify>
    <automated>node -e "const r=require('./lib/core/section-registry.cjs'); const assert=require('node:assert'); const s=require('./lib/core/room-skeleton-scaffold.cjs'); assert.deepStrictEqual(Object.keys(r.CORE_SECTIONS).sort(), s.SECTION_NAMES.slice().sort(), 'CORE_SECTIONS must match SECTION_NAMES'); assert.strictEqual(r.CORE_SECTIONS['strategy'].color,'#8B2942'); const colors=[...Object.values(r.CORE_SECTIONS),...Object.values(r.EXTENDED_SECTION_META)].map(v=>v.color); assert.strictEqual(new Set(colors).size, colors.length, 'duplicate color'); assert.ok(r.STRUCTURAL_DIRS.includes('references')&&r.STRUCTURAL_DIRS.includes('team')&&r.STRUCTURAL_DIRS.includes('meetings')); assert.strictEqual(r.isIndexableArtifactFile('CONTEXT.md'), false); assert.strictEqual(r.isIndexableArtifactFile('ROOM.md'), false); assert.strictEqual(r.isIndexableArtifactFile('my-entry.md'), true); console.log('OK section-registry');"</automated>
  </verify>

  <acceptance_criteria>
    - `Object.keys(CORE_SECTIONS)` is set-equal to `SECTION_NAMES` (11 entries, zero drift),
      asserted by the automated command
    - `CORE_SECTIONS['strategy']` is exactly `{ label: 'STRATEGY', color: '#8B2942' }`
    - No color value appears twice across `CORE_SECTIONS` plus `EXTENDED_SECTION_META`
    - `EXTENDED_SECTION_META` retains exactly one key, `personas`
    - `STRUCTURAL_DIRS` equals `['meetings', 'team', 'references']`
    - `isIndexableArtifactFile('CONTEXT.md') === false` and `isIndexableArtifactFile('note.md') === true`
    - `node tests/test-270-baseline-schema-driven.cjs` exits 0 (the schema-driven baseline rules
      still hold after the registry grows)
  </acceptance_criteria>

  <done>
    All 11 sections carry a registered core label and color, `references` is structural so the L3
    directory never registers as a section, and `CONTEXT.md` is excluded from artifact indexing
    before any CONTEXT.md file exists on disk.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the blueprint data file and the blueprint CI gate to the 11-slug vocabulary</name>

  <read_first>
    - `data/room-blueprints.json` (the file being modified; all 152 lines. Note the `venture`
      family at 84-103 whose `_sections_note` explicitly promises "Exact reproduction of the
      SECTION_NAMES frozen table", and the `program` family at 104-119)
    - `scripts/check-room-blueprints.cjs` (the file being modified; all 221 lines. Note
      `EXPECTED_FAMILY_COUNT` at 46, `loadValidSectionSlugs` at 51-72 with its hardcoded fallback
      set at 62-71, and `EXTENDED_VALID_SLUGS_FOR_CHECK` at 103 with its long explanatory comment
      at 74-102)
    - `tests/test-hypothesis-family-and-claim.cjs` line 84 - asserts
      `check-room-blueprints.cjs` must still reference the `assumptions` slug, so that entry
      cannot be removed
    - `.planning/phases/275-.../275-PATTERNS.md` sections "data/room-blueprints.json" and
      "scripts/check-room-blueprints.cjs"
  </read_first>

  <files>data/room-blueprints.json, scripts/check-room-blueprints.cjs</files>

  <action>
**3a. `data/room-blueprints.json`, `venture` family.** Its `_sections_note` states the family is an
exact reproduction of the frozen table and that a caller passing `blueprintFamily: 'venture'` gets
the same sections as a caller passing no family. Growing `SECTION_NAMES` to 11 breaks that promise
unless the family grows with it. Append `opportunity-bank`, `funding`, `strategy` to `venture`'s
`sections` array so it holds all 11, and extend `_sections_note` with
`Phase 275 (D-01): grew 8 -> 11 alongside SECTION_NAMES; the exact-reproduction contract above is what forces this.`

**3b. `data/room-blueprints.json`, `program` family.** Its own `description` names "grant
initiative" as the persona, and the confirmed-empty `funding/` shell that motivated D-01's
`funding` promotion was found in exactly that kind of room (`launchpad-02`, the icm-architect
audit leg). Append `funding` to `program`'s `sections` array and extend its `_sections_note` with
`Phase 275 (D-01): funding added; this family's own description names grant initiatives, and the funding-as-empty-shell audit finding that promoted the section came from a room of this shape.`
Leave the other seven families' section arrays untouched: no source in this phase evidences a
change to them, and changing what a persona scaffolds is a Canon Part 3 weight decision.

**3c. `data/room-blueprints.json`, `_comment` header.** It says all section slugs MUST appear in
`SECTION_NAMES`. That is now true of `opportunity-bank` (previously it was not, which is why the
CI gate needed an extension set). Leave the sentence, append
`Phase 275 grew SECTION_NAMES 8 -> 11; assumptions remains the only non-frozen slug still in use (hypothesis family).`

**3d. `scripts/check-room-blueprints.cjs`, hardcoded fallback.** `loadValidSectionSlugs` requires
the scaffold and only falls back to a hardcoded literal if that require throws. Update the
fallback set to the same 11 slugs, and update its comment from
`(from room-skeleton-scaffold.cjs:36-45 as of Phase 155-05)` to name Phase 275 and the 11-slug
table. Leave the require-first strategy exactly as it is: it is the anti-drift mechanism and must
not be replaced by a literal.

**3e. `scripts/check-room-blueprints.cjs`, `EXTENDED_VALID_SLUGS_FOR_CHECK`.** It currently holds
`opportunity-bank` and `assumptions`. Remove `opportunity-bank` (it is now a real frozen slug, so
the extension is dead weight that would hide a future regression) and KEEP `assumptions`
(`tests/test-hypothesis-family-and-claim.cjs:84` asserts this file still references it, and the
`hypothesis` family still uses it). Rewrite the long comment block above it: the
`opportunity-bank` half of the rationale is now historical, so state that Phase 275 promoted it
into `SECTION_NAMES` and that `assumptions` is the sole remaining intent-only slug.

**3f. Do NOT change `EXPECTED_FAMILY_COUNT` or `EXPECTED_FAMILIES`.** This plan adds no blueprint
family; it only changes two families' section arrays. The count stays 9.
  </action>

  <verify>
    <automated>node scripts/check-room-blueprints.cjs --check && node -e "const b=require('./data/room-blueprints.json'); const s=require('./lib/core/room-skeleton-scaffold.cjs'); const assert=require('node:assert'); assert.deepStrictEqual(b.venture.sections.slice().sort(), s.SECTION_NAMES.slice().sort(), 'venture family must reproduce SECTION_NAMES exactly'); assert.ok(b.program.sections.includes('funding')); const src=require('fs').readFileSync('scripts/check-room-blueprints.cjs','utf8'); assert.ok(src.includes('assumptions'),'assumptions slug reference required by test-hypothesis-family-and-claim.cjs:84'); assert.ok(!/EXTENDED_VALID_SLUGS_FOR_CHECK = new Set\(\[[^\]]*opportunity-bank/.test(src),'opportunity-bank must be out of the extension set'); console.log('OK blueprints');"</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/check-room-blueprints.cjs --check` exits `0` and prints
      `PASS: 9 families, all section slugs valid, all arrays non-empty.`
    - `venture.sections` is set-equal to `SECTION_NAMES` (11 slugs)
    - `program.sections` contains `funding`
    - The other seven families' `sections` arrays are byte-identical to their pre-change values
      (verifiable with `git diff data/room-blueprints.json`: only `venture`, `program`, the
      `_comment` header and the two `_sections_note` strings should appear as changed)
    - `EXTENDED_VALID_SLUGS_FOR_CHECK` contains `assumptions` and does NOT contain
      `opportunity-bank`
    - `grep -c "EXPECTED_FAMILY_COUNT = 9" scripts/check-room-blueprints.cjs` returns `1`
    - `node tests/test-hypothesis-family-and-claim.cjs` still passes its
      "check accepts the assumptions slug" assertion (its two frozen-table assertions at lines
      104-110 are expected to FAIL here; plan 275-04 owns that fix, see `<known_red>`)
  </acceptance_criteria>

  <done>
    The blueprint data file and its CI gate both speak the 11-slug vocabulary, the `venture`
    family honours its own exact-reproduction contract, the CI extension set carries only the one
    slug that is still genuinely intent-only, and `--check` is green.
  </done>
</task>

</tasks>

<known_red>
This plan deliberately leaves four assertion sites RED. They are hardcoded copies of the "exactly
8" fact that plan 275-04 owns, not regressions caused by this plan. Do NOT soften them here and do
NOT skip them; record their RED state in the SUMMARY as this phase's documented baseline (the same
discipline `tests/run-all-274.sh` records for its own `--check-scripts` arm).

| Site | Assertion that goes RED | Fixed by |
|---|---|---|
| `lib/core/room-skeleton-scaffold.test.cjs:251-252` | `SECTION_NAMES.length === 8` | 275-04 |
| `tests/test-room-skeleton-scaffold-integration.cjs:53,141` | `sections_created.length === 8` | 275-04 |
| `tests/test-119-02-scaffold.sh:58` | `SECTION_NAMES.length !== 8` guard | 275-04 |
| `tests/test-hypothesis-family-and-claim.cjs:109-110` | `SECTION_NAMES.includes('opportunity-bank') === false` | 275-04 |

`tests/test-blueprint-scaffold.cjs` Section 2 (venture family reproduces the frozen set) is
expected to stay GREEN because Task 3a grows the `venture` family in lockstep. If it goes red,
Task 3a was not completed.
</known_red>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blueprint family name -> JSON object key | A caller-supplied string is used as a lookup key in `resolveBlueprint`; untrusted input crosses here |
| section slug -> filesystem path | Section slugs become directory names under a room directory |
| `data/*.json` -> module state | Data files are parsed and their contents drive scaffold behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-275-01 | Tampering | `resolveBlueprint` family-key lookup (`room-skeleton-scaffold.cjs:222`) | mitigate | The existing `Object.prototype.hasOwnProperty.call` guard (T-155-05-01) stays untouched by this plan; verify it is still present after the edit. Do not introduce a bare `blueprints[family]` access. |
| T-275-02 | Tampering | New `SECTION_METADATA[section]` lookups for the 3 new slugs | mitigate | Slugs are only ever read from the frozen `SECTION_NAMES` array or from a `frozenSet.has()`-filtered blueprint list, never from user input. Keep the `frozenSet` filter at line 243 intact; it is the guard that makes every downstream `SECTION_METADATA[section]` lookup safe. |
| T-275-03 | Elevation of Privilege | New section slug becomes a directory name | accept | All three new slugs are compile-time literals in a frozen array; no path component is caller-supplied. No traversal surface added. |
| T-275-04 | Information disclosure | `default_methodologies` / `statement` strings | accept | Both are generic methodology vocabulary with zero room-specific content; Canon Part 8 LOCAL-to-BRAIN boundary is not touched (this plan makes no network call of any kind). |
| T-275-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. No `package.json` change, no new dependency. The Package Legitimacy Gate does not apply; if any task in execution proposes an install, stop and escalate. |
</threat_model>

<verification>
Run in order after all three tasks:

1. `node -e "const s=require('./lib/core/room-skeleton-scaffold.cjs');const r=require('./lib/core/section-registry.cjs');console.log(s.SECTION_NAMES.length, Object.keys(s.SECTION_METADATA).length, Object.keys(r.CORE_SECTIONS).length)"` prints `11 11 11`
2. `node scripts/check-room-blueprints.cjs --check` exits 0
3. `node tests/test-blueprint-scaffold.cjs` exits 0 (must stay green; Task 3a is what keeps it green)
4. `node tests/test-270-baseline-schema-driven.cjs` exits 0
5. `node scripts/build-connector-registry.cjs --check` exits 0 (no command frontmatter was touched, so this is a do-not-regress arm)
6. `grep -rnP '\\x{2014}' lib/core/room-skeleton-scaffold.cjs lib/core/section-registry.cjs data/room-blueprints.json scripts/check-room-blueprints.cjs` returns nothing (no em-dashes)
</verification>

<success_criteria>
- `SECTION_NAMES`, `SECTION_METADATA` and `CORE_SECTIONS` all carry the identical 11-slug set
- Zero dead command citations remain in `SECTION_METADATA` (asserted against the live registry)
- The two misfiled citations sit under the section their command actually produces into
- Every section carries an L1 `statement` string
- `references` is structural and `CONTEXT.md` is not artifact-indexable, both before any such file exists
- `check-room-blueprints --check` is green with `EXPECTED_FAMILY_COUNT` unchanged at 9
- The four `<known_red>` sites are recorded as this phase's documented baseline, not silently softened
</success_criteria>

<output>
Create `.planning/phases/275-enlarge-room-schema-by-icm-layer-notion-gap-close-icm-archit/275-01-SUMMARY.md` when done.

The SUMMARY must record: (a) the langtalks-graph-expert consult result verbatim, including an
honest "unreachable" or `edges: []`; (b) the exact before/after `default_methodologies` arrays for
all 11 sections; (c) the RED state of the four `<known_red>` sites, confirmed by running them.
</output>
