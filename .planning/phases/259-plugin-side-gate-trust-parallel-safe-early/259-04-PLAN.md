---
phase: 259-plugin-side-gate-trust-parallel-safe-early
plan: 04
type: execute
wave: 2
depends_on: [259-01, 259-02, 259-03]
files_modified:
  - tests/run-all-259.sh
  - .planning/debug/brain-client-429-maps-to-unreachable-zero-retry.md
  - .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md
  - .planning/debug/knowledge-base.md
autonomous: false
requirements: [TRUST-01, TRUST-02]
canon_parts: [6, 8]
user_setup: []

must_haves:
  truths:
    - "One command, bash tests/run-all-259.sh, proves the whole phase"
    - "The aggregator FAILS when it discovers nothing (the found-eq-0 guard is intact)"
    - "The aggregator fences every file this phase touched against the em-dash glyph"
    - "The open 429 RCA is closed with both needs-source-reverify tags explicitly discharged"
    - "A human has observed a live floor run and confirmed the new output reads honestly"
    - "RESEARCH.md assumption A1 is discharged or explicitly re-opened as a blocker for FLOOR-01"
  artifacts:
    - path: "tests/run-all-259.sh"
      provides: "phase verification aggregator with the TEST_259_PREFIX hook and the no-em-dash fence"
      min_lines: 90
    - path: ".planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md"
      provides: "the closed RCA, with the wire claim and the code claim both discharged by name"
      contains: "resolved"
    - path: ".planning/debug/knowledge-base.md"
      provides: "the pattern-lesson block a future gsd-debugger session surfaces"
      contains: "429"
  key_links:
    - from: "tests/run-all-259.sh"
      to: "tests/test-259-*"
      via: "glob discovery, not a hard-coded list"
      pattern: "TEST_259_PREFIX"
    - from: "tests/run-all-259.sh em-dash fence"
      to: "every file Phase 259 touched"
      via: "EMDASH_TARGETS array"
      pattern: "EMDASH_TARGETS"
---

<objective>
Close Phase 259 as a phase: one command that proves both requirement legs, the open RCA
retired with its two `needs-source-reverify` tags explicitly discharged, the reasoning trail
filed in both homes the repo's compositing rule requires, and one human observation of the new
output against the live Brain.

Purpose: the phase's two code legs (259-01/259-02 for TRUST-01, 259-03 for TRUST-02) are
independently green by the time this plan runs. What is still missing is the single gate a
future session runs, the record that says WHY the 429 hypothesis was ever confirmed, and the
one thing no hermetic test can give: an operator looking at a real 56-probe run and saying
"yes, that reads honestly."

Output: `tests/run-all-259.sh`, a resolved RCA, a knowledge-base block, a cross-linked research
trail, and a signed-off live observation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/259-plugin-side-gate-trust-parallel-safe-early/259-CONTEXT.md
@.planning/phases/259-plugin-side-gate-trust-parallel-safe-early/259-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Every symbol Phase 259 creates, across all four plans. This plan runs in Wave 2, after all
three Wave 1 plans have landed, so every row below already exists on disk by the time these
tasks run. Use the table to build the aggregator's em-dash target list and the RCA's
"what shipped" section.

| Symbol / path | Kind | Owner plan | Shape |
|---|---|---|---|
| `RATE_LIMIT_RETRY_MAX_DEFAULT` / `RATE_LIMIT_BASE_MS_DEFAULT` | consts, `lib/core/brain-client.cjs` | 259-01 | `3` / `500` |
| `MINDRIAN_BRAIN_RATELIMIT_RETRY_MAX` / `MINDRIAN_BRAIN_RATELIMIT_BASE_MS` / `MINDRIAN_BRAIN_RATELIMIT_MAX_WAIT_MS` | env vars | 259-01 | last one unset by default = no cap (D-01 literal) |
| `_rateLimitRetryMax()` / `_rateLimitBaseMs()` / `_rateLimitMaxWaitMs()` | fns | 259-01 | env readers, reusing `_envNonNegativeInt` |
| `_parseRetryAfterMs(raw)` / `_rateLimitWaitMs(i, hdr, baseMs)` | pure fns on `module.exports._test` | 259-01 | milliseconds |
| `rate_limited` sentinel | return value of `callTool()` | 259-01 | `{ error: 'rate_limited', tool, retry_after_s, attempts, message }` |
| `setToolScript` / `getToolCallCount` / `resetToolScript` | fns, `tests/helpers/brain-capture-server.cjs` | 259-01 | default-off scripted responses |
| `tests/test-259-brain-client-429.cjs` | new file | 259-01 | forced-429 suite |
| `'rate_limited'` in `REFUSAL_KINDS` | 5th enum member, `lib/core/refusal-messaging.cjs` | 259-02 | appended last |
| `KIND_STATUS.rate_limited` | wire status | 259-02 | `'BRAIN_RATE_LIMITED'` |
| `NEXT_MOVES.rate_limited` | handles | 259-02 | `['retry_after_wait', 'continue_without']` |
| `'BRAIN_RATE_LIMITED'` in `STRUCTURED_REFUSAL_STATUSES` | `lib/core/doctor/class-m-brain-smoke.cjs` | 259-02 | 4 members becomes 5 |
| `tests/test-259-refusal-rate-limited.cjs` | new file | 259-02 | refusal-rail suite |
| `errorKind` on `brainCall()` failure returns | field, `scripts/build-brain-census.cjs` | 259-03 | `'hard_error'` \| `'timeout'` \| `'malformed'` |
| `_classifyThrown(e)` | fn, `scripts/build-brain-census.cjs` | 259-03 | `=> 'timeout' \| 'hard_error'` |
| `failures[]` on `probeFramework()` | field, `scripts/check-flagship-floor.cjs` | 259-03 | `[{ probe, kind, httpStatus, detail, retryAfterS }]` |
| `VOID` verdict + `voidCount` + exit code `3` | `evaluateFloor` | 259-03 | VOID outranks PASS and MISS |
| `renderVoidDetailLines` / `renderFloorSummaryLines` | pure exported fns | 259-03 | `=> string[]` |
| `tests/test-259-floor-void.cjs` / `tests/test-259-brain-call-errorkind.cjs` | new files | 259-03 | TRUST-02 suites |
| `tests/run-all-259.sh` | new file | **259-04** | glob aggregator + `TEST_259_PREFIX` hook + no-em-dash fence |
| `.planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md` | moved file | **259-04** | the closed RCA |

<tasks>

<task type="auto">
  <name>Task 1: Build the phase verification aggregator</name>
  <files>tests/run-all-259.sh</files>
  <read_first>
    - `tests/run-all-250.sh` in full: the header block naming what the phase must prove in one
      sentence each, the DISCOVERY-IS-BY-GLOB note, the enumerated mandatory-test list that
      makes a missing suite visible by READING the header, the `found -eq 0` guard note, and
      the NO-EM-DASH FENCE note. Then the body: `set -uo pipefail`, the `ROOT` resolution and
      `cd "$ROOT"`, the `PREFIX="${TEST_250_PREFIX:-tests/test-250-}"` override hook, the
      `run` and `run_may_skip` helpers, the `shopt -s nullglob` discovery loops for `*.cjs`
      (via `node --test`) and `*.sh`, the self-exclusion guard that stops the runner re-running
      itself, the `found -eq 0` bail, the `EMDASH_TARGETS` array with its
      `LC_ALL=C.UTF-8 grep -lP '\x{2014}'` scan and the `rc -ge 2` SCAN BROKE branch, and the
      final PASS/FAIL/SKIP summary with `[ "$FAIL" -eq 0 ]`.
    - `.planning/phases/259-.../259-PATTERNS.md` section "`tests/run-all-259.sh` (NEW; config,
      batch)" for the verbatim excerpts and the exact rename list.
    - `.planning/phases/259-.../259-VALIDATION.md` "Wave 0 Requirements" for the four items
      this runner must cover.
  </read_first>
  <action>
    Copy `tests/run-all-250.sh` to `tests/run-all-259.sh` and re-point three things: the
    discovery prefix, the mandatory-test list, and the em-dash target list. Change nothing
    structural.

    Renames: `TEST_250_PREFIX` becomes `TEST_259_PREFIX`; the default prefix
    `tests/test-250-` becomes `tests/test-259-`; every `Phase 250` label becomes `Phase 259`.

    Header block: state in one sentence each what this phase has to prove.
    1. A 429 from the Brain retries up to 3 times honoring `Retry-After` and then reports
       `rate_limited`, never `BRAIN_UNREACHABLE` with zero retries (TRUST-01).
    2. The refusal rail can name a rate limit honestly instead of coercing it to
       `unreachable` (TRUST-01, F-09 Option B).
    3. A floor run containing any probe-failure row reports VOID with exit code 3 and names
       every failed row plus its trigger type, never a false MISS or RED (TRUST-02).

    Mandatory-test list, enumerated by filename so a missing one is visible by reading the
    header even though the glob does the discovery:
    - `259-01  tests/test-259-brain-client-429.cjs`
    - `259-02  tests/test-259-refusal-rate-limited.cjs`
    - `259-03  tests/test-259-brain-call-errorkind.cjs`
    - `259-03  tests/test-259-floor-void.cjs`

    `EMDASH_TARGETS` array, exactly this phase's nine files:
    `lib/core/brain-client.cjs`, `lib/core/refusal-messaging.cjs`,
    `lib/core/doctor/class-m-brain-smoke.cjs`, `scripts/build-brain-census.cjs`,
    `scripts/check-flagship-floor.cjs`, `tests/helpers/brain-capture-server.cjs`,
    `tests/test-259-brain-client-429.cjs`, `tests/test-259-refusal-rate-limited.cjs`,
    `tests/test-259-brain-call-errorkind.cjs`, `tests/test-259-floor-void.cjs`,
    `tests/run-all-259.sh`.

    Keep the `found -eq 0` guard exactly as it is. A harness that discovers nothing must FAIL,
    not print green. Keep the `rc -ge 2` SCAN BROKE branch: a broken grep must fail, not pass
    silently. Keep the self-exclusion guard in the `*.sh` loop so the runner never re-runs
    itself. Keep `node --test "$t"` as the `.cjs` invocation; both new 259 suites and both
    Wave 1 suites are `node:test` files.

    The runner itself must carry no literal em-dash, or it trips its own fence.

    After writing, run the guard proof:
    `TEST_259_PREFIX=tests/test-259-nonexistent- bash tests/run-all-259.sh` must exit non-zero.
    Production runs never set that variable.

    Also run the two regression suites this phase puts most at risk, as a wave gate:
    `node --test tests/test-250-transport-retry.cjs` and
    `node --test tests/test-249-floor-gate.cjs`.
  </action>
  <acceptance_criteria>
    - `bash tests/run-all-259.sh` exits 0 and its summary line reports `FAIL=0` with at least 4 discovered `.cjs` suites plus the em-dash fence.
    - `TEST_259_PREFIX=tests/test-259-nonexistent- bash tests/run-all-259.sh` exits NON-ZERO and prints the `no ... files discovered` message (the found-eq-0 guard is load-bearing and provable without editing the file).
    - `node --test tests/test-250-transport-retry.cjs` exits 0.
    - `node --test tests/test-249-floor-gate.cjs` exits 0.
    - `node scripts/doctor.cjs --acceptance` exits 0.
    - Source assertion: `grep -c 'EMDASH_TARGETS' tests/run-all-259.sh` is at least 2, and `grep -c 'found -eq 0' tests/run-all-259.sh` is at least 1.
    - Source assertion: `grep -c 'tests/test-250-' tests/run-all-259.sh` returns 0 (no leftover 250 prefix in the discovery path).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/run-all-259.sh` returns 0 hits.
  </acceptance_criteria>
  <verify>
    <automated>bash tests/run-all-259.sh &amp;&amp; ! TEST_259_PREFIX=tests/test-259-nonexistent- bash tests/run-all-259.sh &amp;&amp; node --test tests/test-250-transport-retry.cjs &amp;&amp; node --test tests/test-249-floor-gate.cjs</automated>
  </verify>
  <done>One command proves both requirement legs; the found-eq-0 guard is provable via the prefix override; the em-dash fence covers all eleven files this phase touched; both regression suites and `doctor --acceptance` are green.</done>
</task>

<task type="auto">
  <name>Task 2: Close the 429 RCA and file the reasoning trail in both homes</name>
  <files>.planning/debug/brain-client-429-maps-to-unreachable-zero-retry.md, .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md, .planning/debug/knowledge-base.md</files>
  <read_first>
    - `.planning/debug/brain-client-429-maps-to-unreachable-zero-retry.md` in full: its
      frontmatter (`status: investigating`, `kind: rca`, `severity: medium`,
      `surfaces: [cli, desktop, cowork]`, `canon_parts: [8]`), its Source-of-Truth Preamble
      with the two `needs-source-reverify` obligations, its Current Focus hypothesis, its
      three eliminated competing hypotheses, and its `next_action` line.
    - `.planning/debug/knowledge-base.md` lines 1-40: the exact per-entry block format
      (`## <slug> - <one-sentence framing>`, then `- **Date:**`, `- **Error patterns:**`,
      `- **Root cause:**`, `- **Fix:**`, `- **Verification:**`, `- **Files changed:**`,
      `- **Pattern lesson:**`, then a `---` separator). Newest entries sit at the top.
    - `CLAUDE.md` sections "QA and RCA Reporting" (the on-resolve rule) and "Dev-Research
      Compositing (Rethinking Room)" (the both-homes rule).
    - The three Wave 1 SUMMARY files (`259-01-SUMMARY.md`, `259-02-SUMMARY.md`,
      `259-03-SUMMARY.md`) for what actually shipped, including the final sentinel shape, the
      final VOID line format, and any recorded deviations.
  </read_first>
  <action>
    (1) Update the RCA in place BEFORE moving it. Set frontmatter `status: resolved` and bump
    `updated` to today. Add a `## Resolution` section that does four things:
    - Names the shipped fix per requirement: the 429 branch and `rate_limited` sentinel in
      `lib/core/brain-client.cjs` (TRUST-01), the fifth refusal kind in
      `lib/core/refusal-messaging.cjs` (TRUST-01, F-09 Option B, which is what the RCA's own
      `next_action` asked for when it said "so refusal-messaging can say the true reason"), and
      the VOID verdict in `scripts/check-flagship-floor.cjs` (TRUST-02).
    - **Discharges the WIRE claim's `needs-source-reverify` tag explicitly**, citing
      `jsagir/ProblemsWorthSolving-Brain src/http/rate-limit.mjs` lines 112-131,
      `perKeyRateLimit`, mounted on `/mcp` per `src/http/app.mjs:7,23`: a fixed-window counter
      at `BRAIN_HTTP_RATE_WINDOW_MS = 60000` and `BRAIN_HTTP_RATE_MAX = 120` per key that
      always sets `Retry-After` as integer delay-seconds floored at 1 and bounded by the
      window, with a JSON body `{"error":{"code":-32005,"message":"Rate limit exceeded"}}`,
      not SSE. State that the wire-side trigger the original filing could not capture is now
      confirmed by source read.
    - **Discharges the CODE claim's re-verification rule explicitly**: the original citation
      ("~line 442-515", read against the install cache at 2.0.0-beta.5) is superseded by a read
      of the dev checkout at `origin/main`, where the same ladder sits at
      `lib/core/brain-client.cjs` :511 (`if (!toolRes.ok)`), :520-538 (the 403 zero-retry
      sentinel), :547-550 (the 5xx retry within budget), and **:551 (`return null`, the bug
      site)**. Same defect, current line numbers.
    - Names the verification: `bash tests/run-all-259.sh` green, plus
      `node --test tests/test-250-transport-retry.cjs` and
      `node --test tests/test-249-floor-gate.cjs` green as regressions, and the live-run
      checkpoint result from Task 3.

    (2) `git mv` the file to `.planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md`.
    Note the repo's gitignore trap: `.planning/*` is gitignored except `.planning/debug/`, so
    `.planning/debug/` paths ARE trackable normally, but confirm with `git status` after the
    move rather than assuming.

    (3) Add a knowledge-base block at the TOP of `.planning/debug/knowledge-base.md` (newest
    first), following the existing eight-field format exactly. Slug:
    `brain-client-429-maps-to-unreachable-zero-retry`. `Error patterns` must include the tokens
    a future `gsd-debugger` would grep on: `429`, `Retry-After`, `rate_limited`,
    `BRAIN_UNREACHABLE`, `callTool`, `tools/call`, `REFUSAL_KINDS`, `BRAIN_RATE_LIMITED`,
    `evaluateFloor`, `VOID`, `check-flagship-floor`, `errorKind`, `perKeyRateLimit`.
    The `Pattern lesson` field must carry the two transferable lessons, not a restatement of
    the fix:
    - A status ladder whose final `return null` is the sole transport-failure signal quietly
      absorbs every status nobody wrote a branch for, and the refusal copy attached to that
      signal ("after the bounded retry budget") becomes a lie for exactly the statuses that
      never reached the budget. Audit what falls through a catch-all before trusting the
      message attached to it.
    - A new sentinel kind is only half a fix while the renderer holds a frozen enum that
      coerces unknown kinds to a default. Closing the taxonomy hole in the producer without
      closing it in the renderer relocates the bug one layer up, where no test is looking.

    (4) Dev-research compositing, per CLAUDE.md. Create
    `~/MindrianRooms/rethinking-mindrianos/research/2026-08-20-plugin-gate-trust-429-void/`
    with an entry that carries the durable reasoning: the two-layer taxonomy-hole pattern, the
    F-09 Option A versus Option B cost table and why Option B was taken, the OQ-1 through OQ-5
    dispositions, the exit-code-space finding (exit 3 was free because zero automation call
    sites exist), and the A1 carried risk. Mirror the same file to
    `~/MindrianOS/research/`. Cross-link both back to
    `.planning/phases/259-plugin-side-gate-trust-parallel-safe-early/`, and add a line in the
    room entry pointing at the resolved RCA path. Neither home substitutes for the other: the
    repo gets the executable decision, the room gets the evidence and reasoning.

    No em-dashes in any file written by this task.
  </action>
  <acceptance_criteria>
    - `test -f .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md` succeeds and `test -f .planning/debug/brain-client-429-maps-to-unreachable-zero-retry.md` FAILS (the file moved, it was not copied).
    - `head -3 .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md | grep -c 'status: resolved'` returns 1.
    - `grep -c 'rate-limit.mjs' .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md` is at least 1 (the WIRE discharge is cited by source path).
    - `grep -c 'needs-source-reverify' .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md` is at least 1 AND the Resolution section states both tags are discharged (verify by reading, not by count alone).
    - `head -20 .planning/debug/knowledge-base.md | grep -c 'brain-client-429'` returns at least 1 (the new block is at the top).
    - `grep -c 'Pattern lesson' .planning/debug/knowledge-base.md` increased by exactly 1 from its pre-edit value. Capture the pre-edit count before editing.
    - `test -d ~/MindrianRooms/rethinking-mindrianos/research/2026-08-20-plugin-gate-trust-429-void` succeeds, and the mirrored copy exists under `~/MindrianOS/research/`.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md .planning/debug/knowledge-base.md` returns 0 hits for the lines this task added (the knowledge base may contain pre-existing violations in older blocks; only the new block must be clean).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md &amp;&amp; ! test -f .planning/debug/brain-client-429-maps-to-unreachable-zero-retry.md &amp;&amp; grep -q 'status: resolved' .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md &amp;&amp; grep -q 'rate-limit.mjs' .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md &amp;&amp; head -20 .planning/debug/knowledge-base.md | grep -q 'brain-client-429' &amp;&amp; test -d ~/MindrianRooms/rethinking-mindrianos/research/2026-08-20-plugin-gate-trust-429-void</automated>
  </verify>
  <done>The RCA is resolved and moved, both `needs-source-reverify` obligations are discharged by named source citation, the knowledge base carries a transferable pattern lesson, and the reasoning trail exists in both homes and is cross-linked back to this phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live floor run observation (also discharges assumption A1)</name>
  <files>none (observation only)</files>
  <read_first>
    - `259-03-SUMMARY.md` for the exact VOID line format and banner strings as shipped, so the
      expected output is known before the run.
    - This plan's Decisions-adjacent note below and `259-03-PLAN.md`'s Decisions section on
      OQ-3 and assumption A1.
  </read_first>
  <action>
    This is an observation checkpoint, not an edit task. Do NOT modify any file.

    Confirm all of Wave 1 and Tasks 1 and 2 of this plan are complete and
    `bash tests/run-all-259.sh` is green, then PAUSE and present the what-built and
    how-to-verify blocks below to the operator verbatim. Wait for the resume signal.

    Record the operator's verbatim response in `259-04-SUMMARY.md`, including the observed exit
    code and the explicit A1 verdict (holds / falsified / not observable). If the operator
    reports a defect in the output, do NOT patch it here: record it as a gap for
    `/gsd-plan-phase --gaps` and stop.
  </action>
  <what-built>
    Phase 259 shipped, all hermetically tested:
    - `lib/core/brain-client.cjs` now retries a 429 up to 3 times honoring `Retry-After`, then
      returns `{ error: 'rate_limited', tool, retry_after_s, attempts, message }` instead of
      `null`. It no longer renders as `BRAIN_UNREACHABLE` with copy claiming a retry budget was
      spent when zero retries ran.
    - `lib/core/refusal-messaging.cjs` gained a fifth refusal kind, `rate_limited`, mapping to
      status `BRAIN_RATE_LIMITED`, so a rate limit can be named honestly instead of coerced to
      `unreachable`.
    - `scripts/check-flagship-floor.cjs` gained a third verdict, `VOID`, triggered by any hard
      error, timeout, or malformed response on either probe of a row. It exits 3 on a VOID run,
      names every failed row with its probe and trigger type, and labels the miss count a lower
      bound whenever any row is VOID.
    Everything above is proven by `bash tests/run-all-259.sh` with zero network. This checkpoint
    is the one thing no hermetic test can supply: a human reading real output.
  </what-built>
  <how-to-verify>
    1. Confirm a read-tier Brain key is available (`~/.mindrian.env` or `MINDRIAN_BRAIN_KEY`).
       If none is available, mark this checkpoint SKIPPED with that reason rather than guessing.
    2. Wait for a window-fresh minute if an interactive Brain session just ran. The floor issues
       56 requests against a 120-per-60-seconds per-key budget, so a partly-burned window will
       VOID the run. A VOID run is itself a valid observation, just a different one.
    3. Run: `node scripts/check-flagship-floor.cjs; echo "EXIT=$?"`
    4. Read the output and answer four questions:
       a. **Does the verdict column read honestly?** Rows should be `[PASS]`, `[MISS]`, or
          `[VOID]`. **The floor is EXPECTED to be RED with roughly two dozen misses.** Phases
          260 through 262 own turning it green. You are verifying the OUTPUT, not the verdict.
       b. **If any row is `[VOID]`:** does the detail block name that row, which probe failed
          (`normalize` or `readiness`), which of the three trigger types (`hard-error`,
          `timeout`, `malformed`), the HTTP status, and a readable excerpt? Is the banner
          clearly NOT the RED banner, and does it say the run is not a floor verdict and needs
          a re-run? Is `EXIT=3`?
       c. **If zero rows are `[VOID]`:** is `EXIT` either `0` or `1`, and are the green and red
          banners byte-identical to what they were before this phase?
       d. **ASSUMPTION A1 DISCHARGE, the load-bearing one.** Count the `[VOID]` rows whose
          trigger detail contains a JSON-RPC error rather than an HTTP failure. If many rows
          (roughly the count of frameworks known to be absent from the graph) come back VOID
          this way, then `orchestration_readiness` errors on unknown framework names, A1 is
          FALSIFIED, and OQ-3's classification must be re-decided before FLOOR-01 can ever go
          green. Say so explicitly. If absent frameworks come back as clean `[MISS]` rows with
          `score=n/a`, A1 holds and this is the discharge.
    5. Optional, only if you want to see the rate-limited path live: run the script twice
       back-to-back inside the same 60-second window. The second run should produce VOID rows
       with `hard-error HTTP 429` and a `retry_after=<n>s` hint. Do not do this if you need a
       clean window for other work.
  </how-to-verify>
  <acceptance_criteria>
    - The operator reports the observed exit code and pastes or paraphrases the summary block and, when present, the VOID detail block.
    - The operator states explicitly whether assumption A1 HOLDS, is FALSIFIED, or was NOT OBSERVABLE in this run.
    - If A1 is falsified, a follow-on blocker is recorded in the SUMMARY naming OQ-3 and FLOOR-01, and the phase does not silently proceed.
    - No code change is made by this task. If the output reveals a defect, that is a new gap for `/gsd-plan-phase --gaps`, not an in-checkpoint edit.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms the floor output reads honestly and reports the A1 verdict.</human-check>
  </verify>
  <resume-signal>Type "approved" plus the A1 verdict (holds / falsified / not observable), or describe what looked wrong.</resume-signal>
  <done>A human has observed a live floor run, confirmed the new VOID and verdict output reads honestly rather than as a false MISS or RED, and recorded whether assumption A1 holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the live Brain -> `check-flagship-floor.cjs` (Task 3 only) | The only task in this plan that touches the network. Read-tier key, 56 outbound requests carrying framework NAMES only. |
| `tests/run-all-259.sh` -> CI / future sessions | The aggregator's exit status is the phase's machine-readable verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-259-18 | Repudiation | `tests/run-all-259.sh` | mitigate | A harness that discovers nothing and prints green is a false verification record. The `found -eq 0` guard is preserved verbatim and its liveness is PROVEN in this plan's own acceptance criteria via the `TEST_259_PREFIX` override, not merely asserted in prose. |
| T-259-19 | Tampering | the em-dash fence | mitigate | The `rc -ge 2` SCAN BROKE branch is preserved, so a `grep -P` that is unavailable or errors FAILS the fence rather than passing silently. The runner itself carries no literal em-dash or it trips its own scan. |
| T-259-20 | Information Disclosure | Task 3 live run | mitigate | Canon Part 8: `check-flagship-floor.cjs` sends only generic framework NAMES, which is exactly what Part 8 permits, and this phase added zero outbound fields. Read-tier key only; no admin key is used or required anywhere in Phase 259. The operator must not paste key values into the checkpoint response. |
| T-259-21 | Denial of Service (self-inflicted) | Task 3 live run | accept | 56 requests against a 120-per-60-seconds budget. The instructions tell the operator to run on a window-fresh minute and make the optional back-to-back run explicitly opt-in with a stated cost. |
| T-259-22 | Repudiation | assumption A1 | mitigate | A1 was never discharged at research time and would silently prevent FLOOR-01 from ever going green. Task 3 names the falsification signal explicitly and requires a stated verdict, so A1 cannot be carried forward as quietly-assumed-true. |
| T-259-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed by this phase (RESEARCH.md "Package Legitimacy Audit: Not applicable"). If any task proposes an install, stop and run the Package Legitimacy Gate first. |
</threat_model>

<verification>
```bash
# Phase gate. All of these must pass before /gsd-verify-work.
bash tests/run-all-259.sh
! TEST_259_PREFIX=tests/test-259-nonexistent- bash tests/run-all-259.sh   # guard liveness
node --test tests/test-250-transport-retry.cjs
node --test tests/test-249-floor-gate.cjs
node scripts/doctor.cjs --acceptance

# Records.
test -f .planning/debug/resolved/brain-client-429-maps-to-unreachable-zero-retry.md
head -20 .planning/debug/knowledge-base.md | grep -q 'brain-client-429'
test -d ~/MindrianRooms/rethinking-mindrianos/research/2026-08-20-plugin-gate-trust-429-void
```

Manual (Task 3 checkpoint, not a gate): `node scripts/check-flagship-floor.cjs` with a
read-tier key. The floor verdict itself is expected RED; Phases 260 through 262 own turning it
green. What is being verified is the honesty of the output and the A1 discharge.
</verification>

<success_criteria>
- `bash tests/run-all-259.sh` exits 0 and discovers at least four suites plus the fence.
- The found-eq-0 guard is provably live via the `TEST_259_PREFIX` override.
- Both regression suites and `doctor --acceptance` are green.
- The 429 RCA is resolved, moved, and both `needs-source-reverify` obligations are discharged by name.
- The knowledge base carries a transferable pattern lesson, not a restatement of the fix.
- The reasoning trail exists in `rethinking-mindrianos/research/` and its `MindrianOS/research/` mirror, cross-linked back to this phase.
- A human has observed a live floor run and recorded the A1 verdict.
- Zero em-dashes in every file this phase touched.
</success_criteria>

<output>
Create `.planning/phases/259-plugin-side-gate-trust-parallel-safe-early/259-04-SUMMARY.md` when done.
Record: the aggregator's PASS/FAIL/SKIP line, the guard-liveness proof output, the resolved-RCA
path, the knowledge-base slug, both research-trail paths, and the operator's verbatim A1 verdict
from the Task 3 checkpoint. If A1 was falsified, record the follow-on blocker against OQ-3 and
FLOOR-01 in the SUMMARY's own header, not buried in prose.
</output>
