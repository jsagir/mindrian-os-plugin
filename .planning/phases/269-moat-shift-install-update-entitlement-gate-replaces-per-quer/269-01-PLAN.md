---
phase: 269-moat-shift-install-update-entitlement-gate-replaces-per-quer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/269-doctrine-reconcile.test.cjs
  - tests/run-all-269.sh
autonomous: true
requirements: [MOAT-01, MOAT-02, MOAT-03, MOAT-04, MOAT-05]
canon_parts: [6, 7]

must_haves:
  truths:
    - "Running the Phase 269 doctrine test BEFORE any doctrine edit lands produces a non-zero exit and names which of MOAT-01 through MOAT-05 is unmet (the can-fail proof)"
    - "The Phase 269 aggregator discovers and runs the doctrine test even though that test's filename does not match the tests/test-269- glob"
    - "The aggregator re-runs both Phase 250 tests that already pin decisions.md rows 1 and 5, so a doctrine edit that regresses them fails loudly instead of silently"
    - "The aggregator's discovery guard is provable: pointing it at a non-existent anchor and a non-existent glob prefix makes it exit non-zero with a discovery message, not an assertion failure"
  artifacts:
    - path: "tests/269-doctrine-reconcile.test.cjs"
      provides: "Text assertions for MOAT-01 through MOAT-05 against decisions.md, moat.md, the amendment doc, and bin/cli.js"
      contains: "MOAT-01"
      min_lines: 120
    - path: "tests/run-all-269.sh"
      provides: "The Phase 269 verification aggregator, glob discovery plus explicit anchor and gate legs"
      contains: "no Phase 269 test files discovered"
      min_lines: 120
  key_links:
    - from: "tests/run-all-269.sh"
      to: "tests/269-doctrine-reconcile.test.cjs"
      via: "explicit anchor leg, because the tests/test-269- glob cannot see this filename"
      pattern: "269-doctrine-reconcile\\.test\\.cjs"
    - from: "tests/run-all-269.sh"
      to: "tests/test-250-amendment-unit.cjs"
      via: "explicit gate leg guarding the decisions.md rows 1/5 lockstep this phase is most likely to break"
      pattern: "test-250-amendment-unit\\.cjs"
---

<objective>
Build the Phase 269 Wave 0 test infrastructure before any doctrine text moves.

Phase 269 edits four markdown surfaces that two existing Phase 250 tests already pin by
exact substring. Without a test written first, the doctrine reconciliation is an unverifiable
prose diff and the Phase 250 coupling breaks silently. This plan creates the assertion
harness and the phase aggregator, runs them RED (the can-fail proof), and hands a green
target to plan 02.

Purpose: make the doctrine edits in plan 02 and plan 03 machine-verifiable, and make the
Phase 250 regression arm impossible to miss.
Output: `tests/269-doctrine-reconcile.test.cjs` and `tests/run-all-269.sh`, both committed
RED, with the RED output recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-PATTERNS.md
@.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-VALIDATION.md
@tests/run-all-266.sh
@tests/test-250-amendment-unit.cjs
@tests/test-250-doctrine-fence.cjs
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (they do not exist upstream; drift verification must
exclude them from "unexplained new symbol" checks):

| Symbol | Kind | File |
|--------|------|------|
| `tests/269-doctrine-reconcile.test.cjs` | new file | itself |
| `tests/run-all-269.sh` | new file | itself |
| `DECISIONS_PATH`, `MOAT_PATH`, `AMENDMENT_PATH`, `CLI_PATH` | new constants | `tests/269-doctrine-reconcile.test.cjs` |
| `OLD_ROW_1`, `PRESERVED_ROW_1_SUBSTRINGS`, `PRESERVED_ROW_5_SUBSTRING`, `FLAG_ANCHORS` | new constants | `tests/269-doctrine-reconcile.test.cjs` |
| `extractParagraph` | new function | `tests/269-doctrine-reconcile.test.cjs` |
| `TEST_269_PREFIX`, `TEST_269_ANCHOR`, `TEST_269_ALLOW_MISSING` | new env vars | `tests/run-all-269.sh` |
| `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` | referenced-but-not-yet-created path | asserted by this plan, created by plan 02 |

Symbols this plan REFERENCES but does not create (they already exist on disk):
`tests/test-250-amendment-unit.cjs`, `tests/test-250-doctrine-fence.cjs`,
`scripts/build-connector-registry.cjs`, `.claude/includes/decisions.md`,
`.claude/includes/moat.md`, `bin/cli.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/269-doctrine-reconcile.test.cjs (MOAT-01 through MOAT-05 assertions)</name>

  <files>tests/269-doctrine-reconcile.test.cjs</files>

  <read_first>
    - `tests/test-250-amendment-unit.cjs` lines 20-125 (the path-constant and positive/negative assertion patterns, and the three substrings it already pins in decisions.md)
    - `tests/test-250-doctrine-fence.cjs` lines 60-100 and 225-270 (the header idiom, the imports block, the content-anchored `extractParagraph` shape, and the closed-set self-check)
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-PATTERNS.md` section "tests/269-doctrine-reconcile.test.cjs"
    - `.claude/includes/decisions.md` (rows 1 and 5 as they stand today)
    - `.claude/includes/moat.md` (all 7 lines)
  </read_first>

  <action>
Create `tests/269-doctrine-reconcile.test.cjs`. CJS, `node:test` + `node:assert/strict` + `node:fs` + `node:path` only, zero external dependencies, no em-dashes anywhere in the file.

Header block (JSDoc, following the `tests/test-250-doctrine-fence.cjs` header idiom) states, one sentence per requirement, what MOAT-01 through MOAT-05 prove, and carries the can-fail note: run before the doctrine edits land, this test demonstrably FAILS, and that RED run is recorded in the plan SUMMARY. Also carry the written-reason idiom for two deliberate divergences: (1) this file keeps the name `269-doctrine-reconcile.test.cjs` from `269-RESEARCH.md` and `269-VALIDATION.md` rather than the repo's usual `test-<phase>-<slug>.cjs` shape, so `tests/run-all-269.sh` reaches it through an explicit anchor leg instead of the `tests/test-269-` glob; (2) the no-em-dash scan is NOT duplicated here because `tests/run-all-269.sh` owns it as a fence over the full target list.

Constants, using `path.join` with split segments (never a raw slash string), with `REPO_ROOT` resolved as `path.resolve(__dirname, '..')`:
- `DECISIONS_PATH` for `.claude/includes/decisions.md`
- `MOAT_PATH` for `.claude/includes/moat.md`
- `AMENDMENT_PATH` for `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md`
- `CLI_PATH` for `bin/cli.js`
- `OLD_ROW_1`, the exact pre-phase row as a single string literal: `| 1 | One-command install; the Brain is part of what installs. | Larry's methodology comes from the Brain and says so; a keyless session gets an honest refusal and a visible path to a key, never an imitation. |`
- `PRESERVED_ROW_1_SUBSTRINGS`, an array of the two substrings `tests/test-250-amendment-unit.cjs` already pins: `the Brain is part of what installs` and `a keyless session gets an honest refusal and a visible path to a key`
- `PRESERVED_ROW_5_SUBSTRING` set to `The Brain is remote by design, not optional by default`
- `FLAG_ANCHORS`, an array of exactly these four strings: `FLAG 1: docs/BUSINESS-MODEL-AND-MOAT.md`, `FLAG 2: project_mindrianos_business_model.md`, `FLAG 3: LICENSE BSL 1.1 Additional Use Grant (d)`, `FLAG 4: Gaurav Thorat double sign-in RCA gap`

Helper `extractParagraph(text, anchor)`: return the blank-line-delimited block that starts at the first index of `anchor`, or `null` when the anchor is absent. Clone the content-anchored shape from `tests/test-250-doctrine-fence.cjs`, anchored on content rather than line number so the extraction survives the edit it guards.

Five `test(...)` blocks plus one self-check, each assertion message stating WHY the assertion exists rather than what it checks:

1. `MOAT-01`: read `DECISIONS_PATH`. Assert it includes `checked at install and update time`. Assert it includes `no separate per-query key check`. Assert both `PRESERVED_ROW_1_SUBSTRINGS` are still present, with a message naming `tests/test-250-amendment-unit.cjs` as the reason the substrings are load-bearing. Assert it does NOT include `OLD_ROW_1`, with a message saying an unreconciled row 1 must red this test. Assert it does NOT include `Zero config; Larry works immediately.` (the pre-amendment regression arm carried over from Phase 250).

2. `MOAT-02`: read `DECISIONS_PATH`. Assert it includes `PRESERVED_ROW_5_SUBSTRING` verbatim, with a message saying the moat shift moves the enforcement point and must never delete the remote-by-design principle. Assert it includes `Per-query Brain keys are gone`. Assert it includes `the entitlement moved to install and update time`.

3. `MOAT-03`: read `MOAT_PATH`. Call `extractParagraph` with anchor `Commercial boundary:` and assert the result is not null. Assert the extracted paragraph includes `INSTALLING and UPDATING`. Assert the extracted paragraph includes `never checked per query`. Assert the whole file still includes `served via MCP, never distributed`, with a message saying the boundary clause is an ADD and the existing moat sentence survives verbatim. Assert the last non-empty line of the file still starts with `Deep dive:`.

4. `MOAT-04`: assert `AMENDMENT_PATH` exists, with a message naming plan 02 as its producer. Read it and assert it includes each of the four `FLAG_ANCHORS`, one assertion per anchor so a missing flag names itself in the failure output.

5. `MOAT-05`: assert `fs.existsSync` is false for `lib/core/entitlement.cjs`, with a message stating Phase 269 ships zero entitlement-check code and any such file means the deferred engineering family was executed against an interim Theo cutover state. Assert `bin/cli.js` still contains the verbatim string `Optional -- connect the Brain for enriched intelligence:`, with a message stating the CLI key ceremony has not moved yet and its wording is the anchor that proves it.

6. Self-check: assert `FLAG_ANCHORS.length` equals 4 and that `FLAG_ANCHORS[0]` equals `FLAG 1: docs/BUSINESS-MODEL-AND-MOAT.md`, so the closed four-flag set cannot be silently widened or narrowed.

Do NOT modify `tests/test-250-amendment-unit.cjs` or `tests/test-250-doctrine-fence.cjs` in this plan or any Phase 269 plan. The reconciled doctrine text in plan 02 is designed to keep both green by ADDING alongside the pinned substrings rather than replacing them.
  </action>

  <verify>
    <automated>node tests/269-doctrine-reconcile.test.cjs; test $? -ne 0 &amp;&amp; node tests/269-doctrine-reconcile.test.cjs 2>&amp;1 | grep -q 'MOAT-01'</automated>
  </verify>

  <acceptance_criteria>
    - `tests/269-doctrine-reconcile.test.cjs` exists and is valid CJS: `node --check tests/269-doctrine-reconcile.test.cjs` exits 0.
    - Run against the current unedited repo the file exits NON-ZERO (the can-fail proof), and its output names MOAT-01, MOAT-03 and MOAT-04 as failing.
    - The MOAT-05 leg PASSES already on the unedited repo, because no entitlement code exists and `bin/cli.js:206` still reads `Optional -- connect the Brain for enriched intelligence:`.
    - `grep -c 'require(' tests/269-doctrine-reconcile.test.cjs` shows only `node:` built-in requires: `grep -n "require(" tests/269-doctrine-reconcile.test.cjs | grep -v "node:" | grep -v "^\s*\*"` returns nothing.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/269-doctrine-reconcile.test.cjs` returns nothing.
    - `FLAG_ANCHORS` contains exactly the four strings listed in the action, byte for byte.
    - The RED output is pasted into the plan SUMMARY.
  </acceptance_criteria>

  <done>The doctrine test exists, is syntactically valid, fails RED on the unedited repo naming the unmet MOAT requirements, and its RED output is recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/run-all-269.sh (aggregator with explicit anchor leg and Phase 250 gate legs)</name>

  <files>tests/run-all-269.sh</files>

  <read_first>
    - `tests/run-all-266.sh` in full (the donor; it is itself a clone of `tests/run-all-264.sh`)
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-PATTERNS.md` section "tests/run-all-269.sh", including the NAMING CONFLICT block and the four Planner Warnings
    - `tests/269-doctrine-reconcile.test.cjs` (created in task 1, the anchor this runner must reach)
  </read_first>

  <action>
Create `tests/run-all-269.sh` by cloning `tests/run-all-266.sh` and substituting the Phase 269 specifics. Bash only, no em-dashes, every structural comment in the donor carried over with Phase 269 content.

Header: enumerate what this phase has to prove, one sentence each, for MOAT-01 (decisions.md row 1 names the install/update enforcement point), MOAT-02 (row 5 keeps the remote-by-design phrase verbatim and adds the per-query-keys-are-gone clause), MOAT-03 (moat.md carries a commercial-boundary paragraph), MOAT-04 (the amendment doc records all four cross-cutting flags), MOAT-05 (zero entitlement-check code shipped).

Carry the written-reason idiom, since `.planning/` is gitignored and this tracked file is the only place the reasoning survives. Write two reasons in the header: (1) discovery is by glob over `tests/test-269-` AND by an explicit anchor leg, because `269-VALIDATION.md` fixes the doctrine test's name as `tests/269-doctrine-reconcile.test.cjs`, which the `tests/test-269-` glob cannot see; (2) `tests/test-250-amendment-unit.cjs` and `tests/test-250-doctrine-fence.cjs` are explicit legs because they already pin decisions.md rows 1 and 5 by exact substring and are the coupling this phase is most likely to break, and no Phase 269 glob will ever discover them.

Preamble: `set -uo pipefail`, resolve `ROOT` from `BASH_SOURCE`, `cd "$ROOT"`.

Two override variables so both discovery legs are provable without editing the file:
- `PREFIX="${TEST_269_PREFIX:-tests/test-269-}"`
- `ANCHOR="${TEST_269_ANCHOR:-tests/269-doctrine-reconcile.test.cjs}"`

Copy `PASS`/`FAIL`/`SKIP` counters and the `run` and `run_may_skip` functions verbatim from the donor.

Discovery: `shopt -s nullglob`; glob `"$PREFIX"*.cjs` running each with `run "$(basename "$t")" node "$t"`; glob `"$PREFIX"*.sh` running each with `run_may_skip`, skipping any file whose basename equals this runner's own basename; increment `found` for each. Then the explicit anchor leg: if `[ -f "$ANCHOR" ]`, increment `found` and `run "269 doctrine reconcile (MOAT-01 through MOAT-05)" node "$ANCHOR"`. `shopt -u nullglob`.

Discovery guard: if `found` equals 0, echo the exact line `!!! no Phase 269 test files discovered` and `exit 1`. Otherwise echo `discovered $found test file(s)`. This message string is the guard's proof surface, so keep it byte-exact.

Explicit gate legs, in this order, after discovery:
- `run "250 amendment unit (decisions.md rows 1/5/8 lockstep)" node tests/test-250-amendment-unit.cjs`
- `run "250 doctrine fence (living-docs forbidden phrases)" node tests/test-250-doctrine-fence.cjs`
- `run "connector registry born-wired --check" node scripts/build-connector-registry.cjs --check`

No-em-dash fence: copy the donor's block including the `rc -ge 2` SCAN BROKE arm so a broken `grep -P` FAILS rather than silently passing. Set `EMDASH_TARGETS` to exactly these five paths: `.claude/includes/decisions.md`, `.claude/includes/moat.md`, `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md`, `tests/269-doctrine-reconcile.test.cjs`, `tests/run-all-269.sh`. Use `TEST_269_ALLOW_MISSING` as the missing-target escape, and write a comment stating it is set on wave 1 runs (the amendment doc does not exist yet) and MUST be unset on the phase's final gate run, because that unset run is what proves every listed file exists.

Footer: copy the donor's three echo lines with `Phase 269: PASS=$PASS FAIL=$FAIL SKIP=$SKIP`, ending with the bare test `[ "$FAIL" -eq 0 ]` rather than an `exit`, so the script's exit status is the fence.

Make the file executable (`chmod +x`).
  </action>

  <verify>
    <automated>TEST_269_ANCHOR=tests/269-nonexistent.test.cjs TEST_269_PREFIX=tests/test-269-nonexistent- bash tests/run-all-269.sh 2>&amp;1 | grep -q 'no Phase 269 test files discovered'</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n tests/run-all-269.sh` exits 0 (syntax valid).
    - `TEST_269_ANCHOR=tests/269-nonexistent.test.cjs TEST_269_PREFIX=tests/test-269-nonexistent- bash tests/run-all-269.sh` exits NON-ZERO and prints `!!! no Phase 269 test files discovered`, proving the discovery guard is live rather than decorative.
    - A default `TEST_269_ALLOW_MISSING=1 bash tests/run-all-269.sh` run reaches the doctrine test through the anchor leg: its output contains `269 doctrine reconcile`.
    - That same run FAILS overall on wave 1 (the doctrine test is RED by design) and its footer reads `Phase 269: PASS=` with a non-zero `FAIL=` count.
    - Both Phase 250 legs run and PASS on the unedited repo: output contains `250 amendment unit` followed by `PASSED` and `250 doctrine fence` followed by `PASSED`.
    - `grep -v '^#' tests/run-all-269.sh | grep -c 'test-250-amendment-unit.cjs'` is at least 1, proving the gate leg is real code and not only a header comment.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/run-all-269.sh` returns nothing.
    - `test -x tests/run-all-269.sh` succeeds.
  </acceptance_criteria>

  <done>The aggregator exists, its discovery guard is provably live via the anchor and prefix overrides, it reaches the doctrine test by explicit leg, it runs both Phase 250 coupling tests green, and it fails overall on wave 1 because the doctrine test is still RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan writes two local test scripts that read repo-tracked files. No network call, no credential read, no user input, no new invocable surface, no package install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-269-01 | Tampering | `tests/run-all-269.sh` discovery guard | mitigate | A fence that silently discovers zero files is worse than no fence. The `found`-equals-zero guard exits non-zero, and the `TEST_269_ANCHOR` / `TEST_269_PREFIX` overrides make that guard provable in the acceptance criteria rather than assumed. |
| T-269-02 | Tampering | no-em-dash scan | mitigate | The `rc -ge 2` SCAN BROKE arm is copied from the donor so an unavailable `grep -P` FAILS the fence instead of passing it. |
| T-269-03 | Repudiation | Phase 250 coupling | mitigate | `tests/test-250-amendment-unit.cjs` and `tests/test-250-doctrine-fence.cjs` run as explicit legs, so a doctrine edit that breaks the pinned substrings is visible in this phase's own aggregator instead of only in a later release gate. |
| T-269-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs zero packages. `269-RESEARCH.md` records the Package Legitimacy Audit as not applicable for the same reason. No slopcheck run is required and none was performed. |

**Note on scope:** these are test-infrastructure integrity threats, not application security threats. This plan adds no attack surface. The real credential threat model belongs to the deferred engineering family and is addressed in `269-04-PLAN.md`.
</threat_model>

<verification>
1. `node --check tests/269-doctrine-reconcile.test.cjs` exits 0.
2. `bash -n tests/run-all-269.sh` exits 0.
3. `node tests/269-doctrine-reconcile.test.cjs` exits NON-ZERO and names MOAT-01, MOAT-03, MOAT-04 (the can-fail proof; RED is the expected wave 1 state).
4. `TEST_269_ANCHOR=tests/269-nonexistent.test.cjs TEST_269_PREFIX=tests/test-269-nonexistent- bash tests/run-all-269.sh` exits NON-ZERO printing `!!! no Phase 269 test files discovered`.
5. `node tests/test-250-amendment-unit.cjs` and `node tests/test-250-doctrine-fence.cjs` both still exit 0 (this plan changes no doctrine text).
6. `LC_ALL=C.UTF-8 grep -lP '\x{2014}' tests/269-doctrine-reconcile.test.cjs tests/run-all-269.sh` returns nothing.
</verification>

<success_criteria>
- Both files exist, are syntactically valid, and are committed.
- The doctrine test is RED against the unedited repo and its RED output is pasted into the SUMMARY.
- The aggregator's discovery guard is proven live by the override run, not asserted.
- Zero existing test files were modified.
- Zero files under `lib/`, `bin/`, or `scripts/` were modified.
</success_criteria>

<output>
Create `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-01-SUMMARY.md` when done. Include the verbatim RED output from `node tests/269-doctrine-reconcile.test.cjs`.
</output>
