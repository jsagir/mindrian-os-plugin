---
phase: 269-moat-shift-install-update-entitlement-gate-replaces-per-quer
plan: 05
type: execute
wave: 5
depends_on: ["269-04"]
files_modified:
  - docs/269-ENTITLEMENT-GATE-SPEC.md
  - docs/copy/269-website-entitlement-issuance.md
autonomous: false
requirements: [MOAT-05]
canon_parts: [5, 7, 8, 11]

must_haves:
  truths:
    - "No entitlement-check code is written by Phase 269; the gate task stops execution until a human confirms the external precondition"
    - "The external precondition is concrete enough to check by reading one file: Theo Phase 9 must no longer read 'Plans: TBD'"
    - "When the gate clears, the plugin-side enforcement points and the website-side issuance change are specified rather than rediscovered"
    - "No task edits any file under /home/jsagi/dev/mindrian-website/; the cross-repo work is a spec file in this repo plus a named coordination action"
  artifacts:
    - path: "docs/269-ENTITLEMENT-GATE-SPEC.md"
      provides: "Plugin-side entitlement-gate spec: enforcement seams, reuse targets, Tri-Polar call, retroactivity limit"
      contains: "sessionstart-post-update-preflight.cjs"
      min_lines: 70
    - path: "docs/copy/269-website-entitlement-issuance.md"
      provides: "The out-of-repo deliverable specifying the mindrian-website issuance change, applied by hand post-merge"
      contains: "type: out-of-repo-deliverable"
      min_lines: 40
  key_links:
    - from: "docs/269-ENTITLEMENT-GATE-SPEC.md"
      to: "docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md"
      via: "the spec builds on the credential model decided in plan 04"
      pattern: "Credential model DECIDED"
    - from: "docs/copy/269-website-entitlement-issuance.md"
      to: "mindrian-website provisionTrialKey"
      via: "out-of-repo deliverable, applied by hand post-merge, never edited from this repo"
      pattern: "provisionTrialKey"
---

<objective>
This is the DEFERRED half of Phase 269. It is not ready to execute and must not run until a
human confirms the external precondition.

ROADMAP.md sequences the entitlement-check ENGINEERING after Theo's own Phase 9 gets a firmer
timeline. `269-RESEARCH.md` verified the chain by reading Theo's ROADMAP and STATE on
2026-08-27 and found it blocked harder than the roadmap implies: Phase 9 is blocked on Phase
8, Phase 8 on Phase 7, Phase 7 is mid-execution at plan 07-02 of 12, and Phases 8 and 9 both
literally read `Plans: TBD (not yet planned)`. The "firmer timeline" does not exist and is at
least two unplanned phases away. Building a check now means building against an interim
cutover state, which is exactly the throwaway-work risk ROADMAP.md names.

There is no `[DEFERRED]` or `[BLOCKED]` marker in GSD or in this repo's 1,118 PLAN.md files,
so this plan uses only real mechanisms: a separate plan family, `autonomous: false`, and a
leading `checkpoint:human-action gate="blocking"`. Per the GSD checkpoints reference,
`human-action` is the only checkpoint kind that still stops when auto-mode is on, which
matters because `.planning/config.json` has `mode: "yolo"`.

Purpose: hold the engineering behind a real gate, and have the specification ready the day
the gate clears.
Output: nothing at all until the gate clears. Then two specification documents, and still
zero lines of entitlement-check code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md
@docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md
@docs/BRAIN-IDENTITY-DESIGN.md
@docs/INSTALL-LIFECYCLE-HARNESS.md
</context>

<artifacts_this_phase_produces>
| Symbol | Kind | File |
|--------|------|------|
| `docs/269-ENTITLEMENT-GATE-SPEC.md` | new file (post-gate only) | itself |
| `docs/copy/269-website-entitlement-issuance.md` | new file (post-gate only) | itself |

Symbols this plan explicitly does NOT create, by design: `lib/core/entitlement.cjs`, any
`requireEntitlement` function, any new refusal kind in `lib/core/refusal-messaging.cjs`, any
new SessionStart hook entry, and any edit to `install.sh` or `bin/cli.js`. MOAT-05 in
`tests/269-doctrine-reconcile.test.cjs` asserts their absence. A `files_modified` entry under
`lib/`, `bin/`, or `scripts/` in this plan is the failure mode `269-RESEARCH.md` Pitfall 1
names by name.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: BLOCKING GATE - confirm Theo readiness against three real legs, not a roadmap snapshot (rewritten 2026-09-03, Phase 339 D-14)</name>

  <read_first>
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md` lines 295 to 470 (READ ONLY), the `### Coverage re-measurement, 2026-09-03, and the ruling on it` subsection and its ruling
    - `/home/jsagi/Theo/.planning/phases/08.4-remote-hosting-mcp-server/08.4-MOS-LEARNING.md` (READ ONLY), the `POST /register` compat-route record
    - `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-11-SUMMARY.md` and the flip record's `### The remote ledger verdict (09-11, 2026-09-03)` subsection (READ ONLY)
    - `ls /home/jsagi/Theo/.planning/phases/06.2-*/06.2-*-SUMMARY.md` and `/home/jsagi/Theo/.planning/phases/06.3-*` (READ ONLY, the latter to confirm it still has 0 plans)
    - `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md` Design 6 IN FULL, and DRIFT-3
    - `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` credential section (written by plan 04)
  </read_first>

  <action>
STOP. Do not run task 2 or task 3 until a human confirms every item below.

This checklist is a REWRITE (Phase 339, D-14, 2026-09-03). The original six items read
3-of-3 PASS on the first three from 2026-08-27 onward for a fact that was never in question
(Theo Phase 9 having plans, Phases 7 and 8 being closed), while the real content and
infrastructure legs went untested. A checklist that can read green while its real leg is
untested is worse than no checklist, because it converts an unknown into false confidence.
The three items below replace that false-green surface with three real legs, each with a
live source and an automated check.

Present this checklist and require an explicit confirmation for each:

1. `Leg (a): coverage re-measured live against a PINNED Brain count.` Source:
   `/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md`, the
   subsection headed exactly `### Coverage re-measurement, 2026-09-03, and the ruling on it`,
   or any later dated re-measurement recorded in the same section. Explicitly NOT SEED-004:
   SEED-004's latest dated `## UPDATE` (2026-09-02) reads "not yet" on both legs and is
   superseded on leg one by this subsection's ruling clause 1. This is a navigator RULING
   recorded in writing, not a mechanical threshold. The named-Frameworks RATIO is RETIRED as
   a coverage measure by ruling clause 4: coverage is now a SET question, meaning of the
   Brain's 258 names, how many resolve in Theo by name. Current reading, verbatim:
   `Covered: 228 of 258, 88.4%`, `Uncovered: 30 of 258`, pinned against `Brain@56bf75a`.
2. `Leg (b): Theo Phase 06.2 live, meaning summaries on disk.` Automated check:
   `ls /home/jsagi/Theo/.planning/phases/06.2-*/06.2-*-SUMMARY.md | wc -l` is at least 1.
   Current state per the flip record: 4 of 4 summaries, closed 2026-09-02, so this leg passes
   on its own terms. Stated with the honesty caveat the flip record itself carries, or this
   reads greener than the evidence: the mechanism that would keep the coverage table CURRENT
   is Theo Phase 06.3 (the bidirectional drift detector, SEED-005), which has 0 plans and 0
   summaries. Leg (b) as originally specified passes; the part of leg (b) that bears on DRIFT
   does not.
3. `Leg (c): 09-12 infrastructure legs.` Three sub-checks, each with a source:
   - Theo Phase 08.4 closed. Check: `08.4-MOS-LEARNING.md` exists and is non-empty.
   - 09-11 remote parity, 0 mismatches. Check: `09-11-SUMMARY.md` exists, and the flip
     record's `### The remote ledger verdict (09-11, 2026-09-03)` subsection carries the
     verdict (0 `mismatch` rows).
   - `/register` compat route live. Check: `08.4-MOS-LEARNING.md` records that a
     `POST /register` route exists and hands an opaque token to anybody who asks, against
     `theo-mcp.onrender.com` (retargeted from the incumbent, Retired-as-a-check `pws-brain-mcp.onrender.com` host).
     Do NOT probe it live from the gate: `POST /register` is a WRITE-shaped call and this
     gate is contractually zero-write, same as every other leg here.
4. The credential model recorded by plan 04 is still the intended one. Read the
   `Credential model DECIDED:` line in `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md`.
   The original A2 precondition text named the now-Retired `pws-brain-mcp.onrender.com` for a live
   `POST /register` probe; that host is now the wrong host post-flip and that live-probe
   shape is superseded by leg (c)'s third sub-check above, which reads the RECORD in
   `08.4-MOS-LEARNING.md` against `theo-mcp.onrender.com` instead of probing live.
5. The `Q2 distribution reading:` line is answered and does not read `still open`.
   Privatising distribution and refusing-to-operate produce different specs, and task 2
   cannot be written against an unanswered Q2. Unaffected by the Theo cutover; unchanged
   from the original item.
6. This phase's own research governs readiness, not `269-RESEARCH.md`'s validity window.
   `269-RESEARCH.md` states `Valid until: 2026-09-10`; that window is retired as this gate's
   authority. Read `.planning/phases/339-brain-to-theo-cutover-release-flip-brain-client-default-orig/339-RESEARCH.md`
   instead: Theo's own readiness, established by legs (a), (b) and (c) above, is what gates
   this task now, not the entitlement-gate research's own expiry.

Retired 2026-09-03 (Phase 339, D-14). Items 1, 2 and 3 as originally written (Theo Phase 9
Retired `Plans: TBD` line, Theo Phase 8 Retired `Plans: TBD` line, Theo Phase 7 completion)
read PASS from 2026-08-27 onward while the real content and infrastructure legs went
unchecked. Superseded by legs (a), (b) and (c) above.

If ANY of legs (a), (b), (c) or items 4, 5, 6 fails, HALT and report which one failed. Do not
partially execute. Do not write a spec against an interim state. Reporting the block IS the
correct outcome on any run before the precondition is met.

Perform no repository writes in this task.
  </action>

  <verify>
    <automated>
FR="/home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-FLIP-RECORD.md"
awk '/^### Coverage re-measurement, 2026-09-03, and the ruling on it$/{f=1;next} f&&/^#{2,3} /{exit} f' "$FR" | grep -Fq 'Coverage does NOT block Task 2, the flip' && echo LEG_A_RULING_OK
awk '/^### Coverage re-measurement, 2026-09-03, and the ruling on it$/{f=1;next} f&&/^#{2,3} /{exit} f' "$FR" | grep -Fq 'Brain@56bf75a' && echo LEG_A_PIN_OK
ls /home/jsagi/Theo/.planning/phases/06.2-*/06.2-*-SUMMARY.md 2>/dev/null | wc -l
test -s /home/jsagi/Theo/.planning/phases/08.4-remote-hosting-mcp-server/08.4-MOS-LEARNING.md && echo LEG_C_08_4_OK
test -f /home/jsagi/Theo/.planning/phases/09-brain-contract-cutover/09-11-SUMMARY.md && echo LEG_C_09_11_OK
grep -Fq 'POST /register' /home/jsagi/Theo/.planning/phases/08.4-remote-hosting-mcp-server/08.4-MOS-LEARNING.md && echo LEG_C_REGISTER_OK
grep -Fq 'Credential model DECIDED:' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md && echo ITEM_4_OK
    </automated>
  </verify>

  <acceptance_criteria>
    - A human explicitly confirmed legs (a), (b), (c) and items 4, 5, 6, leg by leg and item
      by item, and the confirmation is recorded in the SUMMARY.
    - Leg (a)'s coverage reading, quoted verbatim in the SUMMARY, is `Covered: 228 of 258,
      88.4%` (or a later dated re-measurement's figures, if this task runs after a newer one
      lands in `09-FLIP-RECORD.md`), pinned against `Brain@56bf75a`.
    - Leg (b)'s SUMMARY entry records both the summary count (>= 1) and the 06.3 drift-detector
      caveat verbatim, so leg (b) cannot be read as a clean pass.
    - Leg (c)'s three sub-checks are each confirmed against `08.4-MOS-LEARNING.md` and
      `09-11-SUMMARY.md` / the flip record's remote ledger verdict subsection, and the
      `/register` host recorded in the SUMMARY reads `theo-mcp.onrender.com`, never the
      incumbent host, and was read from the record rather than probed live.
    - `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` contains a `Credential
      model DECIDED:` line, and its `Q2 distribution reading:` line does not contain `still
      open`.
    - Zero files were written by this task: `git status --porcelain` is byte-identical before
      and after it.
    - If the gate did NOT clear, the SUMMARY records which leg or item failed and tasks 2 and
      3 are left untouched. That is a successful outcome for this task, not a failure.
  </acceptance_criteria>

  <resume-signal>Type "gate cleared" plus leg (a)'s coverage reading verbatim and confirmation that legs (b) and (c) and items 4-6 hold, or "still blocked" plus which leg or item failed.</resume-signal>

  <done>Either legs (a), (b), (c) and items 4, 5, 6 are all confirmed by a human and tasks 2 and 3 may proceed, or the block is recorded with its failing leg or item and the plan halts.</done>
</task>

<task type="auto">
  <name>Task 2: POST-GATE ONLY - write the plugin-side entitlement-gate specification</name>

  <files>docs/269-ENTITLEMENT-GATE-SPEC.md</files>

  <read_first>
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md` "Research Question 2", "Distribution reality", "Don't Hand-Roll", and the Security Domain section
    - `scripts/sessionstart-post-update-preflight.cjs` header (the existing blocking-refusal precedent)
    - `scripts/post-update-activation.cjs` header (the atomic-swap and touch-file protocol)
    - `lib/core/resolve-brain-key.cjs` (the single credential-resolution chokepoint and its SEC-02 mode-0600 gate)
    - `lib/core/refusal-messaging.cjs` header (the byte-locked wire contract and its four refusal kinds)
    - `install.sh` lines 137-202 and `bin/cli.js` lines 95-120 and 195-215 (the two seams this repo controls)
    - `docs/INSTALL-LIFECYCLE-HARNESS.md` section 3 (doctor drift classes)
    - `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` (the decided credential model and the Q2 answer)
  </read_first>

  <action>
Run only after task 1's gate cleared. This task writes a SPECIFICATION, not code. Do not create `lib/core/entitlement.cjs`. Do not edit `install.sh`, `bin/cli.js`, `lib/core/refusal-messaging.cjs`, `lib/core/resolve-brain-key.cjs`, `scripts/sessionstart-post-update-preflight.cjs`, `scripts/post-update-activation.cjs`, or `hooks/hooks.json`.

Create `docs/269-ENTITLEMENT-GATE-SPEC.md` with these sections, written against the Q2 reading the navigator chose, not both:

`## Credential model` - restate the decided model verbatim from the amendment doc's `Credential model DECIDED:` line, with its preconditions.

`## What an install gate can and cannot mean` - the verified constraint: `jsagir/mindrian-os-plugin` and `jsagir/mindrian-marketplace` are both PUBLIC, `@mindrian_os/cli` publishes to public npm, and Claude Code has NO plugin lifecycle hook (no `PluginInstall`, `PluginUpdate`, `PluginLoad`, `PostInstall`, `PluginActivation`; the `Setup` event fires only under init or maintenance modes and cannot block). The gate cannot refuse to deliver the bytes without a distribution change.

`## Enforcement seams` - a table with columns Seam, File, Trigger, Blocking, Notes and exactly four rows: `install.sh` (preflight after the prereq check at line 156 and BEFORE the clone at line 158, because after the clone the bytes are on disk and the gate is theatre); `bin/cli.js install` and `update` (a `requireEntitlement()` sibling to `requireClaudeCli()` at line 101, same shape: boolean return, actionable stderr, zero deps); the SessionStart preflight `scripts/sessionstart-post-update-preflight.cjs` (the ONLY seam catching `claude plugin install` and `/plugin update`, already shipping a blocking red-banner refusal for version drift, same registration point and posture with a different predicate); and `scripts/post-update-activation.cjs` (the update-time atomic swap). Carry that preflight's own defensive rule into the spec: never block the hook chain on internal error, exit 0 silently when the state file is absent.

`## Reuse targets (Canon Part 7)` - state that this is a relocation of existing checks, not greenfield, and forbid each fork: extend `lib/core/resolve-brain-key.cjs` rather than adding a fifth key guesser (Phase 123-07 collapsed three into one because two disagreeing guessers made a working Brain print a Tier-0 warning); add a FIFTH refusal kind alongside the existing four in `lib/core/refusal-messaging.cjs` rather than mutating any existing value, because the `DIRECTOR_NOT_AVAILABLE` shape and the `reason: 'MINDRIAN_BRAIN_KEY not set'` string are byte-locked and a mutation is a declared phase amendment; reuse `POST /register` plus `~/.mindrian-install.json` rather than inventing a token scheme; do not resurrect SEED-011 Option B (an embedded HMAC key), formally rejected as extractable from shipped CJS by design.

`## Tri-Polar call (Canon Part 5)` - state explicitly which of CLI, Desktop and Cowork the design gates and which it does not. SessionStart hooks fire in Claude Code CLI; Desktop and Cowork do not run them the same way, so a SessionStart-only gate covers one of three surfaces. Per CLAUDE.md's Tri-Polar rule a skip must be a deliberate stated call, so state it as one and either name the compensating seam or state plainly that the other two surfaces are ungated in v1.

`## What this gate cannot do` - every npm version already published and every git tag already pushed stays installable forever with no credential, so the gate is prospective only. Use the exact phrase `prospective only` so nobody expects retroactive coverage.

`## Canon Part 8 review items` - any field added to an outbound entitlement payload beyond the closed `{"install_id": "<UUIDv4>"}` schema is a Part 8 review item, not a routine change, and is asserted by `tests/test-250-silent-registration.cjs` Test 5.

`## Canon Part 11 obligations` - any new invocable surface the design adds (for example an `/mos:activate` command) is born WIRED or EXCLUDED with a declared `hitl_shape` or `hitl_why` and must pass `node scripts/build-connector-registry.cjs --check`. `commands/update.md` is the template.

`## Doctor drift class` - PROPOSE a new drift class letter for entitlement drift in `docs/INSTALL-LIFECYCLE-HARNESS.md` section 3. Propose it in this spec; do not add it to that file.

`## Threat model` - the real credential threat model for the engineering, seeded from `269-RESEARCH.md`'s Security Domain section (ASVS V2/V3/V4/V5/V6/V8/V11/V13 rows and the known threat-pattern table). This is the correct home for it, because it can only be written against a firm target.

No em-dashes.
  </action>

  <verify>
    <automated>test -f docs/269-ENTITLEMENT-GATE-SPEC.md &amp;&amp; grep -Fq 'sessionstart-post-update-preflight.cjs' docs/269-ENTITLEMENT-GATE-SPEC.md &amp;&amp; grep -Fq 'prospective only' docs/269-ENTITLEMENT-GATE-SPEC.md &amp;&amp; test ! -f lib/core/entitlement.cjs &amp;&amp; node tests/269-doctrine-reconcile.test.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `docs/269-ENTITLEMENT-GATE-SPEC.md` exists and contains all ten headings named in the action.
    - The Enforcement seams table has exactly four data rows and each names a real existing file path.
    - The spec contains the literal phrase `prospective only`.
    - The Tri-Polar section names CLI, Desktop and Cowork individually and makes an explicit gated-or-ungated call for each.
    - Zero code files were touched: `git status --porcelain | grep -Ev '^\?\? docs/|^ M docs/'` returns nothing.
    - `test ! -f lib/core/entitlement.cjs` succeeds and `node tests/269-doctrine-reconcile.test.cjs` exits 0, MOAT-05 included.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' docs/269-ENTITLEMENT-GATE-SPEC.md` returns nothing.
  </acceptance_criteria>

  <done>The plugin-side gate is fully specified against the decided credential model and the answered Q2 reading, with zero entitlement-check code written.</done>
</task>

<task type="auto">
  <name>Task 3: POST-GATE ONLY - write the mindrian-website out-of-repo deliverable</name>

  <files>docs/copy/269-website-entitlement-issuance.md</files>

  <read_first>
    - `.planning/phases/115-owned-emotion-dual-path-first-touch/115-00-PLAN.md` lines 795-820 (the `type: out-of-repo-deliverable` precedent this task copies)
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md` "Research Question 3: mindrian-website auth code, actual state" in full
    - `docs/269-ENTITLEMENT-GATE-SPEC.md` as written in task 2
    - `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` FLAG 4 (the double sign-in root cause)
  </read_first>

  <action>
Run only after task 1's gate cleared. NO task in this plan may edit any file under `/home/jsagi/dev/mindrian-website/`. The website source is an independent repo with its own git state, and an executor editing it from here would write outside this repo's GSD state. The mechanism is this repo's own convention: write a spec file HERE and name a coordination action.

Create `docs/copy/269-website-entitlement-issuance.md` with frontmatter carrying exactly these keys, matching the `115-00` precedent: `type: out-of-repo-deliverable`, `phase: 269`, `target_repo: /home/jsagi/dev/mindrian-website/`, `target_url: https://mindrian-os.com`, `applied_post_merge: pending (manual action)`, `spec_source: docs/269-ENTITLEMENT-GATE-SPEC.md`.

Immediately under the frontmatter, a blockquote stating: the website source lives in a separate repo, Phase 269 ships the SPEC here as a deliverable, and the actual edit lands in the website repo post-merge as a manual coordination action.

Body sections:

`## Current state (verified 2026-08-27, read only)` - `brain-access/page.tsx` is a 16-line server component gating on `supabase.auth.getUser()`. `components/brain/AuthButton.tsx` calls `signInWithOAuth` with provider google and `redirectTo` built from `window.location.origin`. `app/auth/callback/route.ts` is 17 lines, the only `?code=` consumer, exchanging the code and redirecting to a request-derived `${origin}/brain-access`. `getDashboardData()` in `actions.ts` runs behind `getUser()` and calls `provisionTrialKey()` inline when the user has no row, so a `mbr_` 30-day 50-per-day trial key exists the moment a Google sign-in completes. `next.config.ts` is 14 lines whose only redirects are `/how-it-works` to `/docs`; there is no canonical-domain redirect and no `NEXT_PUBLIC_SITE_URL` pin.

`## Requested change` - specify the issuance delta against the decided credential model. State plainly that `provisionTrialKey` is the single mint site, so this is a small delta on one function rather than a new auth flow. Specify what the function issues, what authorizes it, and what the plugin then reads.

`## Coupled fix: the canonical-domain seam` - specify the canonical-domain redirect plus the `NEXT_PUBLIC_SITE_URL` pin, and state why it is coupled rather than separate: `AuthButton` and `route.ts` both derive origin from the request, so a hop between `mindrian-os.com` and the raw Vercel alias sets the session cookie on one origin while `/brain-access` renders on the other, producing the second Google sign-in the tester hit. Building the entitlement gate on top of that seam without fixing it ships the bug into the paid path.

`## Application steps` - numbered steps the human runs in the website repo. Reference file paths for orientation only; this document is the instruction, not the edit.

`## Coordination and verification` - name the operator action that applies this deliverable, and how to verify it landed (a single Google sign-in producing both a session and the entitlement credential, with no second sign-in prompt).

Constraints: never write a live key value or any `mbr_` string into this file. No em-dashes. Do not add any `mindrian-website` path to this plan's `files_modified`.
  </action>

  <verify>
    <automated>test -f docs/copy/269-website-entitlement-issuance.md &amp;&amp; grep -Fq 'type: out-of-repo-deliverable' docs/copy/269-website-entitlement-issuance.md &amp;&amp; grep -Fq 'provisionTrialKey' docs/copy/269-website-entitlement-issuance.md &amp;&amp; ! grep -q 'mbr_' docs/copy/269-website-entitlement-issuance.md &amp;&amp; test -z "$(git status --porcelain /home/jsagi/dev/mindrian-website 2>/dev/null)" &amp;&amp; node tests/269-doctrine-reconcile.test.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `docs/copy/269-website-entitlement-issuance.md` exists with all six frontmatter keys named in the action, including `type: out-of-repo-deliverable`.
    - It contains the four body headings: current state, requested change, coupled canonical-domain fix, application steps, and coordination.
    - It names `provisionTrialKey` as the single mint site.
    - It contains no `mbr_` string: `grep -c 'mbr_' docs/copy/269-website-entitlement-issuance.md` returns 0.
    - No file under `/home/jsagi/dev/mindrian-website/` was created, modified, or deleted.
    - This plan's `files_modified` frontmatter contains zero paths outside `/home/jsagi/dev/MindrianOS-Plugin/`.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' docs/copy/269-website-entitlement-issuance.md` returns nothing.
  </acceptance_criteria>

  <done>The website-side change is specified as an in-repo out-of-repo-deliverable with a named coordination action, and no foreign repo file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deferred | The real trust boundaries of this work (browser to website, website to Supabase, plugin to Brain/Theo, install-time credential on disk) belong to the entitlement gate itself, which this plan does not build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-269-13 | (meta) | the entitlement-gate threat model itself | defer | The real threat model is NOT fabricated here. Writing it now would mean modelling an interim Theo cutover state that is two unplanned phases away, producing a document that reads authoritative and is wrong. `269-RESEARCH.md`'s Security Domain section already carries the seed list (ASVS V2, V3, V4, V5, V6, V8, V11, V13 plus seven named threat patterns). Task 2 of this plan carries it into `docs/269-ENTITLEMENT-GATE-SPEC.md` only AFTER the gate clears against a firm target. |
| T-269-14 | Tampering | premature execution of tasks 2 and 3 | mitigate | `autonomous: false` plus a leading `checkpoint:human-action gate="blocking"`, the only checkpoint kind that still stops under `mode: "yolo"`. MOAT-05 in `tests/269-doctrine-reconcile.test.cjs` independently asserts that no entitlement code exists and that `bin/cli.js` still carries its unchanged `Optional -- connect the Brain for enriched intelligence:` line. |
| T-269-15 | Tampering | cross-repo write from this repo's executor | mitigate | Task 3 uses the `type: out-of-repo-deliverable` mechanism. Its acceptance criteria assert `git status --porcelain` on the website repo is empty and that this plan's `files_modified` contains zero paths outside this repo. |
| T-269-16 | Information disclosure | spec docs in a PUBLIC repo | mitigate | No key value, no `mbr_` string, no Supabase URL, and no service-role secret is written into either spec document. Asserted by grep in task 3's verify. The one known compromised key is referenced by description in FLAG 4 of the amendment doc, never by value. |
| T-269-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs zero packages. `269-RESEARCH.md` records the Package Legitimacy Audit as not applicable for the same reason; no slopcheck run is required and none was performed. |

**Explicit statement for the ASVS L1 gate:** Phase 269 ships markdown only. Its Family 1
plans (01 through 04) introduce no attack surface, which is stated rather than papered over
with a fabricated register. This plan, Family 2, is a checkpoint-gated placeholder, so its
real credential threat model is deferred to `docs/269-ENTITLEMENT-GATE-SPEC.md` and is
written against a firm Theo target, not an interim one.
</threat_model>

<verification>
1. On any run before the gate clears: task 1 halts, tasks 2 and 3 do not execute, and `git status --porcelain` is unchanged.
2. `test ! -f lib/core/entitlement.cjs` succeeds.
3. `grep -Fq 'Optional -- connect the Brain for enriched intelligence:' bin/cli.js` succeeds (the CLI key ceremony has not moved).
4. `node tests/269-doctrine-reconcile.test.cjs` exits 0 with MOAT-05 green.
5. `git status --porcelain /home/jsagi/dev/mindrian-website` is empty.
6. `git diff --name-only` lists zero paths under `lib/`, `bin/`, `scripts/`, `hooks/`, or `commands/`.
7. `node scripts/build-connector-registry.cjs --check` exits 0 (no invocable surface minted).
</verification>

<success_criteria>
- Before the gate clears: nothing executed, nothing written, and the block reported with its failing checklist item.
- After the gate clears: two specification documents exist and still zero lines of entitlement-check code were written.
- No file under `/home/jsagi/dev/mindrian-website/` was touched at any point.
- MOAT-05 stays green throughout.
</success_criteria>

<output>
Create `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-05-SUMMARY.md` when done, INCLUDING on a blocked run. On a blocked run the SUMMARY records the failing checklist item, the verbatim Theo Phase 9 `Plans:` line, and the date the gate was last checked.
</output>
