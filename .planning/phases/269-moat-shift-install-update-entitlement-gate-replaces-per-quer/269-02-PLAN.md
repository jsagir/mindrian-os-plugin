---
phase: 269-moat-shift-install-update-entitlement-gate-replaces-per-quer
plan: 02
type: execute
wave: 2
depends_on: ["269-01"]
files_modified:
  - .claude/includes/decisions.md
  - .claude/includes/moat.md
autonomous: true
requirements: [MOAT-01, MOAT-02, MOAT-03]
canon_parts: [6, 7]

must_haves:
  truths:
    - "Key Decision #1 now states the entitlement is checked at install and update time, and that methodology queries carry no separate per-query key check"
    - "Key Decision #5 still contains 'The Brain is remote by design, not optional by default' byte for byte, and now also states per-query Brain keys are gone"
    - "moat.md now names where the commercial boundary sits, which it never did before, and its existing 'served via MCP, never distributed' sentence is untouched"
    - "Both Phase 250 tests that pin decisions.md rows 1 and 5 stay green after the reconciliation"
  artifacts:
    - path: ".claude/includes/decisions.md"
      provides: "Reconciled Key Decision rows 1 and 5, install/update enforcement point"
      contains: "checked at install and update time"
    - path: ".claude/includes/moat.md"
      provides: "The commercial-boundary paragraph naming install and update as the paid gate"
      contains: "Commercial boundary:"
  key_links:
    - from: "tests/269-doctrine-reconcile.test.cjs"
      to: ".claude/includes/decisions.md"
      via: "text assertions for MOAT-01 and MOAT-02"
      pattern: "checked at install and update time"
    - from: "tests/269-doctrine-reconcile.test.cjs"
      to: ".claude/includes/moat.md"
      via: "content-anchored paragraph extraction for MOAT-03"
      pattern: "Commercial boundary:"
---

<objective>
Reconcile the two doctrine surfaces the moat shift changes: `.claude/includes/decisions.md`
rows 1 and 5, and `.claude/includes/moat.md`.

The navigator locked the shift on 2026-08-27: the key gates installing and updating
MindrianOS, not each Brain query. ROADMAP.md's own Phase 269 text says this "requires
reconciling, not silently editing." `269-RESEARCH.md` proves by exhaustive grep that the
roadmap's paraphrase "pay per graph query" appears nowhere on disk, so a find-and-replace
task would fail. Every edit here is an amendment or an addition, never a deletion of a
principle.

Purpose: make the plugin's own constitution surface match the decision.
Output: reconciled `decisions.md` rows 1 and 5, and a new commercial-boundary paragraph in
`moat.md`. The reasoning and the cross-cutting flags land in plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md
@.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-PATTERNS.md
@.claude/includes/decisions.md
@.claude/includes/moat.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (drift verification must exclude them from
"unexplained new symbol" checks):

| Symbol | Kind | File |
|--------|------|------|
| `Commercial boundary:` paragraph | new markdown paragraph | `.claude/includes/moat.md` |
| `checked at install and update time` | new clause in row 1 | `.claude/includes/decisions.md` |
| `no separate per-query key check` | new clause in row 1 | `.claude/includes/decisions.md` |
| `Per-query Brain keys are gone` | new clause in row 5 | `.claude/includes/decisions.md` |
| `the entitlement moved to install and update time` | new clause in row 5 | `.claude/includes/decisions.md` |

This plan creates NO new files. It modifies two existing includes and nothing else.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile decisions.md Key Decision rows 1 and 5 (MOAT-01, MOAT-02)</name>

  <files>.claude/includes/decisions.md</files>

  <read_first>
    - `.claude/includes/decisions.md` in full (16 rows, one physical line each)
    - `tests/test-250-amendment-unit.cjs` lines 96-123 (the three substrings it pins in this file and the one it forbids)
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-PATTERNS.md` section ".claude/includes/decisions.md", including Planner Warning 1 and the CRITICAL COUPLING block
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md` sections "Half 1: DECISION-RECORDING" and "Pattern 1: Doctrine reconciliation, not silent edit"
    - `.planning/ROADMAP.md` Phase 269 section, both Goal paragraphs (these ARE the locked decisions for this phase; no CONTEXT.md exists)
  </read_first>

  <action>
Replace exactly two lines in `.claude/includes/decisions.md`. Touch nothing else: no reflow, no renumbering, no new column, no change to rows 2, 3, 4, 6 through 16, no change to the header or separator lines.

Replace line 5 (the current row 1) with this exact single physical line:

`| 1 | One-command install; the Brain is part of what installs, and the key gates installing and updating rather than each query. | Larry's methodology comes from the Brain and says so; the entitlement is checked at install and update time, so a keyless session gets an honest refusal and a visible path to a key at that moment, never an imitation; once an install is entitled, methodology queries carry no separate per-query key check. |`

Replace line 9 (the current row 5) with this exact single physical line:

`| 5 | Brain as remote MCP | IP never distributed; users get intelligence, not data. The Brain is remote by design, not optional by default; a keyless session gets an honest refusal, never a silent local substitute. Per-query Brain keys are gone: the entitlement moved to install and update time, and moving the check does not make the Brain local or optional. |`

Why the text is shaped this way, so the executor does not improve it into a regression:

- `The Brain is remote by design, not optional by default` is preserved byte for byte. ROADMAP.md says Decision #5 stays true and only needs a clause added, and `tests/test-250-amendment-unit.cjs:107` asserts the phrase.
- `the Brain is part of what installs` and `a keyless session gets an honest refusal and a visible path to a key` are preserved because `tests/test-250-amendment-unit.cjs:98-105` pins them. This is the ADD-alongside resolution from `269-PATTERNS.md` Planner Warning 1. Rewriting either substring reds an existing test. Do not take that path, and do not amend the Phase 250 test to make room.
- Row 1's column 2 keeps a terminal period, matching the row as it stands today. Row 5's column 2 (`Brain as remote MCP`) keeps no period, also matching. `269-PATTERNS.md` records that punctuation here is per-row, not global.
- Each replacement is ONE physical line. Do not wrap.
- No em-dashes. Hyphens only (project rule C6).
- Neither line may contain `silent fallback`, `never mention failures`, `graceful degradation everywhere`, or `never tell the user about degradation`. This file is inside the `LIVING_DOCS_FILES` scope of `tests/test-250-doctrine-fence.cjs:113`.
- `Zero config; Larry works immediately.` must not reappear anywhere in the file; it is the pre-amendment row 1 rationale and Phase 250 forbids its return.
  </action>

  <verify>
    <automated>grep -Fq 'the entitlement is checked at install and update time' .claude/includes/decisions.md &amp;&amp; grep -Fq 'no separate per-query key check' .claude/includes/decisions.md &amp;&amp; grep -Fq 'The Brain is remote by design, not optional by default' .claude/includes/decisions.md &amp;&amp; grep -Fq 'Per-query Brain keys are gone' .claude/includes/decisions.md &amp;&amp; node tests/test-250-amendment-unit.cjs &amp;&amp; node tests/test-250-doctrine-fence.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `.claude/includes/decisions.md` contains the exact substring `the entitlement is checked at install and update time`.
    - It contains the exact substring `once an install is entitled, methodology queries carry no separate per-query key check`.
    - It still contains, byte for byte, `The Brain is remote by design, not optional by default`.
    - It still contains, byte for byte, `the Brain is part of what installs` and `a keyless session gets an honest refusal and a visible path to a key`.
    - It contains the exact substring `Per-query Brain keys are gone: the entitlement moved to install and update time`.
    - It no longer contains the pre-phase row 1 tail: `grep -Fq 'the Brain is part of what installs. | Larry' .claude/includes/decisions.md` exits non-zero.
    - It does NOT contain `Zero config; Larry works immediately.`
    - `node tests/test-250-amendment-unit.cjs` exits 0 and `node tests/test-250-doctrine-fence.cjs` exits 0.
    - The table still has exactly 16 data rows: `grep -c '^| [0-9]' .claude/includes/decisions.md` returns 16.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' .claude/includes/decisions.md` returns nothing.
    - `git diff --numstat .claude/includes/decisions.md` shows 2 insertions and 2 deletions, no more.
  </acceptance_criteria>

  <done>Rows 1 and 5 name install and update time as the enforcement point, the two principles ROADMAP.md said stay true are preserved verbatim, both Phase 250 tests are green, and the diff is exactly two lines.</done>
</task>

<task type="auto">
  <name>Task 2: Add the commercial-boundary paragraph to moat.md (MOAT-03)</name>

  <files>.claude/includes/moat.md</files>

  <read_first>
    - `.claude/includes/moat.md` in full (7 lines, the entire file)
    - `.claude/includes/architecture.md` (the sibling include, for the `Deep dive:` trailer convention)
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-PATTERNS.md` section ".claude/includes/moat.md", including the precision constraint
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md` "Precision note the planner needs" and Common Pitfall 4
  </read_first>

  <action>
This is an ADD, not a replace. `269-RESEARCH.md` verified by exhaustive grep across `.claude/includes/*.md`, `docs/MOAT-MANDATE.md`, and `docs/BUSINESS-MODEL-AND-MOAT.md` that the strings `pay per`, `per graph query`, and `per-query` appear in none of them. The roadmap's phrase "pay per graph query" is a paraphrase of the de facto model, not a quotation. There is nothing to find and replace. What is actually missing is that `moat.md` never states where the commercial boundary sits at all, and this task adds that statement.

Insert exactly one new paragraph into `.claude/includes/moat.md`, placed AFTER the `MWP deepening mandate:` paragraph and BEFORE the `Deep dive:` line, separated by a blank line on each side so the `Deep dive:` line remains the last non-empty line of the file:

`Commercial boundary: the paid gate is INSTALLING and UPDATING MindrianOS, not querying the graph. An entitled install queries methodology freely, the entitlement is checked when the plugin is installed and when it is updated, and it is never checked per query. The moat is the graph plus the right to run it, not a metered query counter.`

Structural rules carried from the file's own shape:
- One paragraph on ONE physical line, matching the existing two body paragraphs. Do not wrap.
- The paragraph opens with a named concept followed by the rule, matching `Prompts can be copied.` and `MWP deepening mandate:`.
- SCREAMING-CAPS is reserved in this file for load-bearing words (`WHEN`, `WHICH`, `SEQUENCE`, `REAL`). The new paragraph uses it exactly once, on `INSTALLING and UPDATING`, which is the boundary's load-bearing term.
- No sub-headers, no bullets, no tables. This file has none.
- No em-dashes.
- Change nothing else. The first paragraph, including `served via MCP, never distributed`, survives verbatim; that sentence is still true and the MOAT-03 test asserts it.
  </action>

  <verify>
    <automated>grep -Fq 'Commercial boundary: the paid gate is INSTALLING and UPDATING MindrianOS' .claude/includes/moat.md &amp;&amp; grep -Fq 'it is never checked per query' .claude/includes/moat.md &amp;&amp; grep -Fq 'served via MCP, never distributed' .claude/includes/moat.md &amp;&amp; test "$(grep -v '^[[:space:]]*$' .claude/includes/moat.md | tail -1 | cut -c1-10)" = "Deep dive:" &amp;&amp; node tests/test-250-doctrine-fence.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `.claude/includes/moat.md` contains the exact substring `Commercial boundary: the paid gate is INSTALLING and UPDATING MindrianOS, not querying the graph.`
    - It contains the exact substring `it is never checked per query`.
    - The pre-existing sentence `served via MCP, never distributed` is still present byte for byte.
    - The last non-empty line of the file still begins with `Deep dive:`.
    - The new paragraph is exactly one physical line: `grep -c '^Commercial boundary:' .claude/includes/moat.md` returns 1.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' .claude/includes/moat.md` returns nothing.
    - `git diff --numstat .claude/includes/moat.md` shows 2 insertions (the paragraph plus its blank separator) and 0 deletions.
    - `node scripts/doctor.cjs --acceptance` still completes without a new failure, proving CLAUDE.md still loads its four includes cleanly.
  </acceptance_criteria>

  <done>moat.md names install and update as the paid gate, the existing moat sentences are untouched, and the Deep dive trailer is still the last line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan edits two repo-tracked markdown includes. No code path changes, no network call, no credential handling, no new invocable surface, no package install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-269-04 | Repudiation | `.claude/includes/decisions.md` rows 1 and 5 | mitigate | A doctrine row whose text no longer matches any code and whose history is one unexplained commit diff is the anti-pattern `269-RESEARCH.md` Pattern 1 names. The reasoning, ruling citation, and before/after text are filed in plan 03's dated amendment doc, not left inside the table diff. |
| T-269-05 | Tampering | Phase 250 pinned substrings | mitigate | Both preserved substrings are asserted in this plan's own verify command by running `tests/test-250-amendment-unit.cjs` and `tests/test-250-doctrine-fence.cjs` inline, so a regression fails the task rather than surfacing at a later release gate. |
| T-269-06 | Information disclosure | doctrine text | accept | The new clauses describe where the commercial gate sits. They name no credential, no endpoint, no secret, and no implementation detail an attacker could use. Public repo already, so this discloses nothing not already inferable. |
| T-269-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs zero packages. `269-RESEARCH.md` records the Package Legitimacy Audit as not applicable for the same reason; no slopcheck run is required and none was performed. |

**Note on scope:** these are doctrine-integrity threats, not application security threats.
This plan adds no attack surface. The credential threat model belongs to the deferred
engineering family and is addressed in `269-05-PLAN.md`.
</threat_model>

<verification>
1. `node tests/test-250-amendment-unit.cjs` exits 0 (the rows 1/5/8 lockstep survived).
2. `node tests/test-250-doctrine-fence.cjs` exits 0 (no forbidden doctrine phrase entered the living-docs set).
3. `LC_ALL=C.UTF-8 grep -lP '\x{2014}' .claude/includes/decisions.md .claude/includes/moat.md` returns nothing.
4. `node scripts/doctor.cjs --acceptance` shows no new failure versus the pre-edit run.
5. `node scripts/build-connector-registry.cjs --check` exits 0 (no invocable surface was minted).
6. `node tests/269-doctrine-reconcile.test.cjs` now passes its MOAT-01, MOAT-02, MOAT-03 and MOAT-05 legs, and still fails only MOAT-04 (the amendment doc is plan 03's deliverable).
</verification>

<success_criteria>
- decisions.md rows 1 and 5 name install and update time as the enforcement point.
- The two principle phrases ROADMAP.md said stay true are byte-identical to before.
- moat.md states where the commercial boundary sits, for the first time.
- Exactly two files changed; zero files under `lib/`, `bin/`, `scripts/`, or `tests/` modified.
- Both Phase 250 coupling tests are green.
</success_criteria>

<output>
Create `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-02-SUMMARY.md` when done. Record the `git diff` of both includes verbatim, and the MOAT-04 leg that is still RED with the reason (plan 03 owns it).
</output>
