---
phase: 269-moat-shift-install-update-entitlement-gate-replaces-per-quer
plan: 03
type: execute
wave: 3
depends_on: ["269-02"]
files_modified:
  - docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md
autonomous: true
requirements: [MOAT-04]
canon_parts: [6, 7, 8]

must_haves:
  truths:
    - "The navigator ruling, the before/after doctrine text, and the reasoning behind the moat shift live in a dated citable record, not only in a one-line table diff"
    - "All four cross-cutting surfaces the roadmap did not name are recorded as explicit flags with an owner and a recommended action, none of them silently edited"
    - "A reader who lands on decisions.md row 1 can trace why it changed without reading git history"
    - "The record states plainly that the entitlement-check engineering is deferred and why, so a later reader does not mistake the doctrine change for a shipped gate"
  artifacts:
    - path: "docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md"
      provides: "The Phase 269 amendment record: ruling, before/after, credential option space, enforcement reality, four cross-cutting flags, deferral note"
      contains: "FLAG 1: docs/BUSINESS-MODEL-AND-MOAT.md"
      min_lines: 90
  key_links:
    - from: "docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md"
      to: ".claude/includes/decisions.md"
      via: "verbatim before/after quotation of rows 1 and 5"
      pattern: "One-command install"
    - from: "tests/269-doctrine-reconcile.test.cjs"
      to: "docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md"
      via: "MOAT-04 four-flag presence assertions"
      pattern: "FLAG [1-4]:"
---

<objective>
File the reasoning behind the moat shift where it survives, and record the four
cross-cutting surfaces the roadmap never named.

`269-RESEARCH.md` Pattern 1 states the in-repo convention: amend the one-line doctrine row,
and file the ruling, the rejected framings, and the threat model in a dated standalone doc
that quotes the decision. `docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md` and
`docs/BRAIN-IDENTITY-DESIGN.md` are the two precedents. Plan 02 amended the rows; this plan
writes the record.

Research also found four surfaces the roadmap's own scope paragraph omits, one of which
(`docs/BUSINESS-MODEL-AND-MOAT.md`) is the single most affected document in the repo. None
of the four is edited here. Each is FLAGGED with its current state, why the shift affects
it, a recommended action, and an owner.

Purpose: make the doctrine change traceable and make the unnamed blast radius visible.
Output: `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md
@.claude/includes/decisions.md
@.claude/includes/moat.md
@docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (drift verification must exclude them from
"unexplained new symbol" checks):

| Symbol | Kind | File |
|--------|------|------|
| `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` | new file | itself |
| `FLAG 1: docs/BUSINESS-MODEL-AND-MOAT.md` | new heading | the amendment doc |
| `FLAG 2: project_mindrianos_business_model.md` | new heading | the amendment doc |
| `FLAG 3: LICENSE BSL 1.1 Additional Use Grant (d)` | new heading | the amendment doc |
| `FLAG 4: Gaurav Thorat double sign-in RCA gap` | new heading | the amendment doc |

This plan modifies NO existing file. It adds one document. It does not add the new document
to `LIVING_DOCS_FILES` in `tests/test-250-doctrine-fence.cjs`; that fence scans `skills/`,
`commands/`, `agents/`, `dist/` plus an explicit living-docs list, so an unlisted `docs/`
file is out of its scan scope. Do not edit that test.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the amendment record (ruling, before/after, credential option space, enforcement reality)</name>

  <files>docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md</files>

  <read_first>
    - `docs/AMENDMENT-2026-08-DECISIONS-1-AND-8.md` in full (the shape precedent for this file)
    - `docs/BRAIN-IDENTITY-DESIGN.md` (the second precedent: opens by quoting the decision it amends, then carries rejected options with reasons and a threat model)
    - `.planning/ROADMAP.md` Phase 269 section, both Goal paragraphs and the `Depends on` line, for the verbatim ruling text
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md` sections "Research Question 1: Credential Unification", "Research Question 2: The install/update code path today", "Distribution reality", and Open Questions Q1 and Q2
    - `.claude/includes/decisions.md` and `.claude/includes/moat.md` as reconciled by plan 02
  </read_first>

  <action>
Create `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md`. Markdown, no em-dashes, hyphens only.

Frontmatter keys: `status: applied`, `date: 2026-08-27`, `phase: 269`, `amends: .claude/includes/decisions.md rows 1 and 5; .claude/includes/moat.md`, `ruling_source: .planning/ROADMAP.md Phase 269 (navigator-locked 2026-08-27)`, `engineering_status: deferred`.

Write these sections in this order:

Section `## The ruling`. Quote verbatim, as a blockquote, the navigator sentence from ROADMAP.md Phase 269: `the key will be required to install and update mindrian... there will be no dependency on key to access [Theo]... it will be accessible to any mindrianOS user... we are shifting the moat.` Then quote the concrete-mechanism refinement paragraph's core claim: the install page's existing Google-auth flow at `/brain-access` (Supabase `signInWithOAuth` with provider google) is repurposed so the same gate issues the install/update entitlement credential. Cite ROADMAP.md as the source, and state that no CONTEXT.md exists for this phase because `.planning/config.json` sets `skip_discuss: true`, so ROADMAP.md's two Goal paragraphs ARE the locked decision record.

Section `## What changed in decisions.md`. A before/after pair for row 1 and a before/after pair for row 5, each quoted as a fenced markdown block containing the exact table line. The BEFORE for row 1 is `| 1 | One-command install; the Brain is part of what installs. | Larry's methodology comes from the Brain and says so; a keyless session gets an honest refusal and a visible path to a key, never an imitation. |`. The BEFORE for row 5 is `| 5 | Brain as remote MCP | IP never distributed; users get intelligence, not data. The Brain is remote by design, not optional by default; a keyless session gets an honest refusal, never a silent local substitute. |`. The AFTER lines are copied verbatim from the reconciled file on disk.

Section `## What changed in moat.md`. State that the roadmap's paraphrase `pay per graph query` was verified by exhaustive grep to appear nowhere in `.claude/includes/*.md`, `docs/MOAT-MANDATE.md`, or `docs/BUSINESS-MODEL-AND-MOAT.md`, so nothing was replaced. What was missing is that moat.md never stated where the commercial boundary sits. Quote the added `Commercial boundary:` paragraph verbatim.

Section `## What did NOT change`. Four bullets, each naming a preserved phrase and why: `The Brain is remote by design, not optional by default` (Decision #5's principle survives, only the check location moved); `served via MCP, never distributed` (Theo stays remote and is never bundled, per Theo's own Phase 9 resolution); the honest-refusal rail from Phase 250 (a non-entitled install still refuses honestly, it just refuses at a different moment); and the moat formula itself (WHEN, WHICH, SEQUENCE), which `docs/MOAT-MANDATE.md` carries unchanged.

Section `## Credential option space (decision pending, see plan 04)`. Reproduce the three options from `269-RESEARCH.md` Research Question 1 as a table with columns Option, What changes, Pros, Cons: Option A replace outright, Option B unify into one credential with two authorized uses, Option C promote the per-install registration token. Record the research finding that Option C's plumbing is roughly 80 percent built (per-install identity, idempotency per `install_id`, mode-0600 storage at `~/.mindrian-install.json`, revocation by flipping one `brain_api_keys` row to `revoked`, a documented threat model, an opt-out via `MINDRIAN_DISABLE_AUTO_REGISTER`, and a register-specific rate cap), AND record explicitly that this is a research finding and not a decision, because ROADMAP.md defers the call to this phase's own planning. State that assumption A2 from `269-RESEARCH.md` must be probed before Option C can be locked: `POST /register` on `pws-brain-mcp.onrender.com` is documented as shipping but its production deploy was not verified. State that the chosen option gets recorded into this same section by plan 04.

Section `## Enforcement reality`. Four findings, stated plainly so nobody later expects more than the design can deliver: (1) `jsagir/mindrian-os-plugin` and `jsagir/mindrian-marketplace` are both PUBLIC and `@mindrian_os/cli` publishes to public npm with no auth, verified live 2026-08-27, so an install gate cannot mean refusing to deliver the bytes without a distribution change. (2) Claude Code has NO plugin lifecycle hook: no `PluginInstall`, `PluginUpdate`, `PluginLoad`, `PostInstall`, or `PluginActivation` event exists, verified against the live hooks documentation, and the `Setup` event fires only under init or maintenance modes and cannot block. The only seam that catches `claude plugin install` and `/plugin update` is the next `SessionStart`, and `scripts/sessionstart-post-update-preflight.cjs` already ships exactly that blocking shape for version drift. (3) Every npm version already published and every git tag already pushed stays installable forever with no credential, so any gate is prospective only and cannot be retroactive. (4) Tri-Polar gap: SessionStart hooks fire in Claude Code CLI; Desktop and Cowork do not run them the same way, so a SessionStart-only gate covers one of three surfaces and that must be a stated call, not an oversight.
  </action>

  <verify>
    <automated>test -f docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md &amp;&amp; grep -Fq '## The ruling' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md &amp;&amp; grep -Fq 'we are shifting the moat' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md &amp;&amp; grep -Fq '## What did NOT change' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md &amp;&amp; grep -Fq '## Enforcement reality' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md &amp;&amp; grep -Fq 'The Brain is remote by design, not optional by default' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md</automated>
  </verify>

  <acceptance_criteria>
    - The file exists at exactly `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md`.
    - It contains the headings `## The ruling`, `## What changed in decisions.md`, `## What changed in moat.md`, `## What did NOT change`, `## Credential option space (decision pending, see plan 04)`, `## Enforcement reality`.
    - It quotes the navigator string `we are shifting the moat` verbatim.
    - It quotes the pre-phase row 1 line and the pre-phase row 5 line verbatim, so a reader can diff without git.
    - Its AFTER quotes match the live file: `grep -Fq "$(grep '^| 1 |' .claude/includes/decisions.md)" docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` exits 0, and the same holds for `^| 5 |`.
    - The credential section names all three options A, B and C and explicitly states the decision is pending, not made.
    - The enforcement section states all four findings: public distribution, no plugin lifecycle hook, prospective-only gating, and the Tri-Polar gap.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` returns nothing.
    - No file other than this one is created or modified: `git status --porcelain` lists exactly one path for this task.
  </acceptance_criteria>

  <done>The amendment record exists, quotes the ruling and the before/after doctrine text verbatim, maps the credential option space without deciding it, and states the four enforcement-reality findings.</done>
</task>

<task type="auto">
  <name>Task 2: Add the four cross-cutting flags and the compositing note (MOAT-04)</name>

  <files>docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md</files>

  <read_first>
    - `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` as written in task 1
    - `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-RESEARCH.md` "Cross-cutting flags to file (NOT to edit)", Open Questions Q3, Q4 and Q5, Pitfall 7, and the "Rethinking Room" section
    - `docs/BUSINESS-MODEL-AND-MOAT.md` frontmatter and its tier table only (do not edit it)
    - `LICENSE` Additional Use Grant clauses (a), (b) and (d) (do not edit it)
    - `docs/testers/gaurav-thorat/FEEDBACK.md` 2026-08-25 entry (do not edit it)
    - `tests/269-doctrine-reconcile.test.cjs` `FLAG_ANCHORS` constant (the four exact strings this task must produce)
  </read_first>

  <action>
Append two sections to `docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md`. Edit no other file. These are FLAGS, not edits: none of the four surfaces below is modified by this phase.

Section `## Cross-cutting flags`. Open with one sentence stating that ROADMAP.md's Phase 269 scope paragraph names decisions.md, moat.md and the personal-memory note, and that research surfaced two more surfaces the roadmap does not mention. Then four sub-sections, each an H3 whose heading text is byte-exact so the MOAT-04 assertions can find it:

`### FLAG 1: docs/BUSINESS-MODEL-AND-MOAT.md` -- the single most affected document in the repo and entirely unnamed by the phase scope. Record: its whole tier ladder is priced on Brain access being the paid thing (Free tier explicitly lists "What's missing: Brain intelligence"; the Brain tier is 19 dollars per month whose first bullet is "Brain API key"; the University tier is priced as Brain access for all enrolled students), all of which becomes incoherent once Theo access is unconditional for any installed user. Record that it is 233 lines with `status: Draft for review` frontmatter, and that its stack figures (23K Neo4j nodes, 12K Pinecone embeddings) are already stale independent of this shift, since the backend moved to Memgraph with local e5 embeddings on 2026-07-22 and Pinecone is retired. Recommended action: amend the frontmatter status and add a superseding note pointing at this amendment. Do NOT rewrite the pricing model. Owner: navigator. Rewriting a full pricing model is its own phase.

`### FLAG 2: project_mindrianos_business_model.md` -- lives OUTSIDE this repo at `~/.claude/projects/-home-jsagi/memory/`, outside git and outside this phase's tracked scope. Record its current claim verbatim: "Free tier = prompt-Larry + Brain MCP; paid = trained Lawrence model", and that it is now inconsistent with a model where the paid thing is the install and update right. Recommended action: the human updates it. Explicitly record that no plan task edits it, per `269-RESEARCH.md` Pitfall 7. Owner: human, outside GSD.

`### FLAG 3: LICENSE BSL 1.1 Additional Use Grant (d)` -- research-surfaced, not in the roadmap at all. Record that grant (d) currently permits "Using the Licensed Work as an installed plugin in Claude Code, Claude Desktop, or Cowork for your own projects, even if those projects are commercial", which is the exact right this phase proposes to charge for, and that the license draws its commercial line at reselling or hosting (a "Commercial Offering"), not at installing. Record that this is the legal counterpart of the enforcement-reality finding that a client-side check in a public repo is strippable: a user who forks and strips the check is arguably doing what grant (d) permits. Recommended action: the licensor (who is also the navigator) makes the call before any gate ships. Record that this is a reading and not a legal opinion. Owner: licensor.

`### FLAG 4: Gaurav Thorat double sign-in RCA gap` -- root-caused twice and written down twice, in `docs/testers/gaurav-thorat/FEEDBACK.md` (2026-08-25 entry) and in the `rethinking-mindrianos` research trail at `research/2026-08-26-trial-install-testimonial/`, yet with zero `.planning/debug/` file in either repo. Record the root cause in one paragraph: `AuthButton.tsx` sends `redirectTo` built from `window.location.origin`, `auth/callback/route.ts` redirects back to a request-derived `origin`, and `next.config.ts` carries no canonical-domain redirect and no `NEXT_PUBLIC_SITE_URL` pin, so a hop between `mindrian-os.com` and the raw Vercel alias sets the session cookie on one origin and renders `/brain-access` on the other, producing a second Google sign-in. Record that CLAUDE.md's QA and RCA section requires a NEW FAILURE to get `.planning/debug/<slug>.md` at the `docs/RCA-TEMPLATE.md` standard so `/gsd:debug <slug>` can resume it, and that this phase FLAGS the gap rather than closing it because both existing write-ups say the fix waits for the Theo swap to land. Also record the adjacent live security item from the same FEEDBACK.md entry: a Brain API key was printed unmasked into an emailed install-report PDF with two or more recipients and rotation is still pending; that item is independent of this phase but a credential-model change is the natural moment to resolve it. Owner: human plus a future `/gsd-debug` session.

Section `## Dev-Research Compositing action (CLAUDE.md C8)`. Record that `~/MindrianRooms/rethinking-mindrianos/research/` already holds the directly relevant prior art at `2026-08-26-trial-install-testimonial/`, whose closing "Deliberately not acted on yet" section independently corroborates this phase's defer posture a day before the navigator decision, and that no room entry covers the moat shift itself. State the required post-execution action: a dated entry in that room cross-linked back to this amendment and to `.planning/phases/269-.../269-RESEARCH.md`. State explicitly that filing it is NOT a MindrianOS-Plugin repo file edit and therefore not a task in any Phase 269 plan; it is a compositing action for the operator after execution.

Formatting rules: no em-dashes; the four H3 heading strings must be byte-identical to the `FLAG_ANCHORS` entries in `tests/269-doctrine-reconcile.test.cjs`; do not modify `docs/BUSINESS-MODEL-AND-MOAT.md`, `LICENSE`, `docs/testers/gaurav-thorat/FEEDBACK.md`, or anything under `~/.claude/projects/` or `~/MindrianRooms/`.
  </action>

  <verify>
    <automated>node tests/269-doctrine-reconcile.test.cjs &amp;&amp; TEST_269_ALLOW_MISSING=0 bash tests/run-all-269.sh</automated>
  </verify>

  <acceptance_criteria>
    - The amendment doc contains all four heading lines byte-exact: `### FLAG 1: docs/BUSINESS-MODEL-AND-MOAT.md`, `### FLAG 2: project_mindrianos_business_model.md`, `### FLAG 3: LICENSE BSL 1.1 Additional Use Grant (d)`, `### FLAG 4: Gaurav Thorat double sign-in RCA gap`.
    - Each flag sub-section names a current state, a reason the shift affects it, a recommended action, and an owner. `grep -c '^Owner:' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` returns 4.
    - It contains the heading `## Dev-Research Compositing action (CLAUDE.md C8)` and names `2026-08-26-trial-install-testimonial`.
    - `node tests/269-doctrine-reconcile.test.cjs` now exits 0 with all five legs green (MOAT-01 through MOAT-05).
    - `bash tests/run-all-269.sh` run WITHOUT `TEST_269_ALLOW_MISSING` set exits 0, which is the run that proves every file in `EMDASH_TARGETS` now exists.
    - `git status --porcelain` shows zero modifications to `docs/BUSINESS-MODEL-AND-MOAT.md`, `LICENSE`, and `docs/testers/gaurav-thorat/FEEDBACK.md`.
    - `LC_ALL=C.UTF-8 grep -lP '\x{2014}' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` returns nothing.
  </acceptance_criteria>

  <done>All four cross-cutting flags are recorded with owner and recommended action, none of the four surfaces is edited, the C8 compositing action is named, and the Phase 269 aggregator is fully green with the missing-target escape unset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan adds one markdown document. No code path, no network call, no credential handling, no new invocable surface, no package install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-269-07 | Information disclosure | FLAG 4 text in a PUBLIC repo | mitigate | FLAG 4 describes the double sign-in root cause and references a compromised key incident. Record the incident by DESCRIPTION only. Never write the key value `mbr_...` or any key body into this file. Reference `docs/testers/gaurav-thorat/FEEDBACK.md` for detail rather than restating a credential. |
| T-269-08 | Repudiation | unnamed blast radius | mitigate | Four surfaces the roadmap never named are recorded with an owner each, so a later reader cannot mistake omission for a considered decision. MOAT-04 asserts all four are present. |
| T-269-09 | Tampering | scope creep into flagged files | mitigate | The acceptance criteria assert `git status --porcelain` shows zero modifications to `docs/BUSINESS-MODEL-AND-MOAT.md`, `LICENSE`, and the tester FEEDBACK file. Flagging is the deliverable; editing them is out of scope by design. |
| T-269-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs zero packages. `269-RESEARCH.md` records the Package Legitimacy Audit as not applicable for the same reason; no slopcheck run is required and none was performed. |

**Note on scope:** these are record-integrity and disclosure-hygiene threats, not application
security threats. This plan adds no attack surface. The credential threat model belongs to
the deferred engineering family and is addressed in `269-05-PLAN.md`.
</threat_model>

<verification>
1. `node tests/269-doctrine-reconcile.test.cjs` exits 0 with MOAT-01 through MOAT-05 all green.
2. `bash tests/run-all-269.sh` with `TEST_269_ALLOW_MISSING` unset exits 0.
3. `node tests/test-250-amendment-unit.cjs` and `node tests/test-250-doctrine-fence.cjs` both exit 0.
4. `node scripts/doctor.cjs --acceptance` shows no new failure.
5. `node scripts/build-connector-registry.cjs --check` exits 0.
6. `git status --porcelain` shows exactly one new file for this plan and zero modifications to the four flagged surfaces.
7. `grep -rn 'mbr_' docs/AMENDMENT-2026-08-27-DECISIONS-1-AND-5-MOAT-SHIFT.md` returns nothing (T-269-07).
</verification>

<success_criteria>
- The amendment record exists and is citable from decisions.md row 1's history.
- All four cross-cutting flags are recorded with current state, impact, recommended action, and owner.
- Zero flagged surfaces were edited.
- The Phase 269 aggregator is fully green with no escape variable set.
- Zero files under `lib/`, `bin/`, or `scripts/` were modified.
</success_criteria>

<output>
Create `.planning/phases/269-moat-shift-install-update-entitlement-gate-replaces-per-quer/269-03-SUMMARY.md` when done. Include the full GREEN output of `bash tests/run-all-269.sh` and a one-line status for each of the four flags.
</output>
