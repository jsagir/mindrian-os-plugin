---
phase: 132-dual-graph-correlation-hypergraph-reformat
plan: 02
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - scripts/curation-132-02-hypergraph-reify.cjs
  - lib/memory/curation-132-02-hypergraph-reify.test.cjs
  - tests/run-all-132.sh
autonomous: true
requirements:
  - REFORMAT-02   # at least 5 reified event-node types exist with at least 20 instances each (Authorship/Illustration/Contradiction/Motivation/Evolution) -- audit acceptance criterion 4
  - REVERSE-01    # every reify write carries created_by='phase-132-curation-batch-2'; reversible via DELETE-by-selector
  - REVERSE-02    # the batch carries an asserted rollback that restores the pre-batch event-node count
  - ADDITIVE-07   # existing binary AUTHORED/ILLUSTRATES/CONTEXTUALIZES/MOTIVATES edges UNTOUCHED (Canon Part 7); a known caller (chain-recommender recommendFrameworkChain) still resolves non-empty
user_setup:
  - service: mindrian-brain
    why: "This batch reifies the 5 event-node types into the Brain teaching graph via the 132-01 curation-batch runner. --dry-run (default) prints the forward + rollback Cypher and touches the Brain zero times; --execute is gated by NEO4J creds and routes through makeBatch.execute() (the admin-brain-write neo4j-driver transport). No new dependency; the driver is reused from mcp-server-brain/node_modules."
    env_vars:
      - name: NEO4J_URI
        source: "Brain Aura instance (plugin-root .env; same creds scripts/admin-brain-write.cjs reads)"
      - name: NEO4J_USER
        source: "Brain Aura instance (.env)"
      - name: NEO4J_PASSWORD
        source: "Brain Aura instance (.env)"

must_haves:
  truths:
    - "At least 5 reified event-node types (AuthorshipEvent, IllustrationEvent, ContradictionEvent, MotivationEvent, EvolutionEvent) exist in the Brain, each with at least 20 instances, after --execute."
    - "The reify batch is purely additive: no pre-existing binary AUTHORED/ILLUSTRATES/CONTEXTUALIZES/MOTIVATES edge is deleted, removed, or detached (Canon Part 7)."
    - "A known binary-edge caller -- chain-recommender.recommendFrameworkChain -- still returns a non-empty chain for a known framework seed after the reify batch (the additive contract is asserted against a real caller, not just a grep)."
    - "Every reified event node and every event-to-participant edge carries created_by = 'phase-132-curation-batch-2'; the paired rollback restores the pre-batch event-node count by that selector."
    - "The reify batch writes only participant handles + role enums + scalar properties (year/status/severity/outcome/industry) + provenance toward the Brain; scanBatchForUserContent returns []."
  artifacts:
    - path: "scripts/curation-132-02-hypergraph-reify.cjs"
      provides: "the Plan-02 reify driver: builds the 5-event-type forward Cyphers via buildReifyCypher from the source data in the 2026-05-17 audit (cluster A illustration examples, Cluster B authorship pairs, the C3 contradiction/evolution candidates), feeds them to makeBatch as batch 'phase-132-curation-batch-2', emits the paired rollback, defaults to --dry-run, --execute gated by creds"
      min_lines: 120
      exports: ["buildHypergraphBatch", "EVENT_SEED_DATA"]
    - path: "lib/memory/curation-132-02-hypergraph-reify.test.cjs"
      provides: "asserts the batch yields >=5 event types with >=20 seed instances each, zero destructive Cypher (additive), created_by stamping, Part 8 clean scan, and the known-caller still-resolves assertion (recommendFrameworkChain non-empty)"
      min_lines: 70
  key_links:
    - from: "scripts/curation-132-02-hypergraph-reify.cjs"
      to: "lib/brain/hypergraph-event-schema.cjs buildReifyCypher + EVENT_INSTANCE_MIN"
      via: "buildReifyCypher(eventType, participants, by) per seed row; the >=20 floor read from EVENT_INSTANCE_MIN, not a magic number"
      pattern: "buildReifyCypher|EVENT_INSTANCE_MIN"
    - from: "scripts/curation-132-02-hypergraph-reify.cjs"
      to: "lib/brain/curation-batch.cjs makeBatch + assertRollbackPath + scanBatchForUserContent"
      via: "makeBatch({batchId:'phase-132-curation-batch-2', forwardCyphers, rollbackCyphers}); assertRollbackPath before any execute"
      pattern: "makeBatch|assertRollbackPath|scanBatchForUserContent"
    - from: "lib/memory/curation-132-02-hypergraph-reify.test.cjs"
      to: "lib/brain/chain-recommender.cjs recommendFrameworkChain"
      via: "require + call with a known framework seed; assert non-empty chain (the additive Part 7 contract asserted against a real caller)"
      pattern: "recommendFrameworkChain"
---

<objective>
Reify the 5 hypergraph event-node types into the Brain teaching graph, routing every write through the 132-01 curation-batch runner as batch `phase-132-curation-batch-2`. This is pillar 1 of Phase 132 (the hypergraph reformat) and satisfies audit acceptance criterion 4: at least 5 reified event-node types with at least 20 instances each.

The event-node seed data is drawn from the already-enumerated content in the 2026-05-17 curation audit (no live Brain read): the Cluster B authorship pairs (Section 12 -- 25+ AUTHORED edges, e.g. Ackoff/Idealized Design, Ben Wiener/HEART, Anthony Ulwick/ODI), the Cluster A illustration examples (Section 18 -- 31 ILLUSTRATES, e.g. Kodak/Disruptive Innovation, M-Pesa/Platform Thinking, Heinz Ketchup/JTBD), and the C3 contradiction + evolution candidates (Section 20 -- Disruptive vs Sustaining; Lean Canvas from Business Model Canvas). Each row becomes one buildReifyCypher call.

Purpose: Neo4j is a binary-edge substrate; an n-ary fact ("Ackoff authored Idealized Design in 1981 at Wharton") loses context when shattered into N separate binary edges. The reified event node captures the whole n-ary context as ONE node + binary edges to participants. Canon Part 7 is the hard invariant: the existing binary AUTHORED/ILLUSTRATES/CONTEXTUALIZES/MOTIVATES edges that chain-recommender and dashboard-graph-neighborhood read STAY -- reification is additive, never a replacement that breaks a caller.

Output: scripts/curation-132-02-hypergraph-reify.cjs (the reify driver, --dry-run default), its test, and the run-all-132.sh append.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-CONTEXT.md
@.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-01-PLAN.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Extracted from 132-01 + the codebase. Executor: use these directly, no exploration, no live Brain read. -->

From lib/brain/hypergraph-event-schema.cjs (132-01 Task 2 -- the frozen contract):
  EVENT_NODE_TYPES -- frozen object keyed by the 5 labels (AuthorshipEvent / IllustrationEvent / ContradictionEvent / MotivationEvent / EvolutionEvent), each with roles[] + the event-to-participant edge labels (named so they coexist with legacy binary AUTHORED/ILLUSTRATES, never collide).
  buildReifyCypher(eventType, participants, by) -> { cypher, params } -- MERGEs the event node (id deterministic from participants, idempotent) + one binary edge per participant; SETs created_by=by on node + edges; embeds scalar role values; contains ZERO DELETE/REMOVE/DETACH/DROP.
  EVENT_INSTANCE_MIN === 20 -- the per-type instance floor. Read the floor from here, never hardcode 20.

From lib/brain/curation-batch.cjs (132-01 Task 1 -- the runner):
  makeBatch({batchId, forwardCyphers, rollbackCyphers, by}) -> { dryRun(), execute(), rollback() }. batchId must match /^phase-132-curation-batch-\d+$/. Rejects empty rollbackCyphers. Every forward params must carry created_by===batchId.
  assertRollbackPath(batch) -- string-asserts each rollback names the forward created_by selector.
  scanBatchForUserContent(batch) -> [] for handles-only batches; non-empty if a Cypher embeds artifact/MINTO/meeting/transcript/roomDir/MindrianRooms tokens.
  BATCH_PROVENANCE_PREFIX (''). execute()/rollback() reuse the admin-brain-write neo4j-driver transport from mcp-server-brain/node_modules; dryRun() is creds-free + opens NO session.

From lib/brain/chain-recommender.cjs (the known binary-edge caller -- the Part 7 still-resolves assertion target):
  recommendFrameworkChain(opts) -> array of framework-name strings (filters to non-empty strings; degrades to [seed] when no FEEDS_INTO edge / no Brain). It reads FEEDS_INTO (binary edges), NOT event nodes. Calling it after the reify batch (which never touches FEEDS_INTO) must still return a non-empty chain for a known seed -- this proves reification did not break the binary-edge reader.

The 2026-05-17 audit is the seed-data authority (Section 12 = AUTHORED pairs; Section 18 = ILLUSTRATES examples; Section 20 = C3 contradiction/evolution candidates). The audit Section 17 rollback shape for edges: MATCH ()-[r {created_by:$by}]-() DELETE r; for nodes: MATCH (n {created_by:$by}) DETACH DELETE n.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hypergraph reify driver -- 5 event types x >=20 instances via buildReifyCypher + makeBatch batch-2</name>
  <read_first>
    - ~/MindrianRooms/mindrianOS/methodology/2026-05-17-brain-curation-audit.md Section 12 (Cluster B AUTHORED pairs -> AuthorshipEvent seed rows), Section 18 (Cluster A ILLUSTRATES examples -> IllustrationEvent seed rows, e.g. A1 Disruptive Innovation 10 examples, A2 Platform Thinking 9, A3 JTBD 5, A4 Beautiful Question 7), Section 20 (C3 contradiction + evolution candidates -> ContradictionEvent / EvolutionEvent seed rows), Section 14 + Section 23 (the MOTIVATES + INSTANCE_OF problem rows from A5 -> MotivationEvent seed rows)
    - lib/brain/hypergraph-event-schema.cjs (buildReifyCypher signature + EVENT_NODE_TYPES roles + EVENT_INSTANCE_MIN -- the contract this driver reifies against)
    - lib/brain/curation-batch.cjs (makeBatch + assertRollbackPath + scanBatchForUserContent -- the single write chokepoint; do NOT hand-roll a neo4j-driver call here)
    - docs/MINDRIAN-CANON.md Part 7 (existing binary edges STAY; reification is ADDITIVE) + Part 8 (handles + enums + scalars only toward Brain)
  </read_first>
  <behavior>
    - Test 1 (>=5 types, >=20 instances each): buildHypergraphBatch() returns a batch whose forward Cyphers cover all 5 EVENT_NODE_TYPES keys, with at least EVENT_INSTANCE_MIN (20) seed instances per type. Assert via grouping the forward Cyphers by event label and counting >= 20 per group; assert the type set equals the 5 frozen keys.
    - Test 2 (additive -- zero destructive Cypher): no forward Cypher in the batch contains DELETE / REMOVE / DETACH / DROP (Canon Part 7 -- the legacy binary edges are never touched). Grep-gate over every forward cypher body, filtering comment lines first (grep -v '^#'-equivalent in JS: skip lines starting with '//').
    - Test 3 (provenance + rollback pairing): every forward params carries created_by==='phase-132-curation-batch-2'; rollbackCyphers is non-empty; assertRollbackPath(batch) does not throw (the rollback selector matches the forward created_by).
    - Test 4 (Part 8 clean): scanBatchForUserContent(batch) returns [] -- the reify batch binds only participant handles (framework + person + example names), role enums, and year/status/severity/outcome/industry scalars + provenance.
    - Test 5 (known caller still resolves -- the additive contract against a real reader): require lib/brain/chain-recommender.cjs and call recommendFrameworkChain with a known framework seed (e.g. {currentFramework:'HEART Framework'} or a problemType that routes); assert the returned chain is a non-empty array of strings. This proves the binary-edge reader is unaffected by the reify batch construction (no Brain write occurs in the test -- dry-run only).
  </behavior>
  <action>
    Create scripts/curation-132-02-hypergraph-reify.cjs. Export EVENT_SEED_DATA (a frozen object keyed by the 5 event labels; each value is an array of participant-objects transcribed from the 2026-05-17 audit -- AuthorshipEvent rows from Section 12 Cluster B pairs, IllustrationEvent rows from Section 18 Cluster A batches A1-A4, ContradictionEvent + EvolutionEvent rows from Section 20, MotivationEvent rows from Section 14/23 A5; pad each type to at least EVENT_INSTANCE_MIN rows using the audit's enumerated examples so the >=20 floor is met from real curation data, not invented filler). Export buildHypergraphBatch() that: maps every EVENT_SEED_DATA row through buildReifyCypher(eventType, participants, 'phase-132-curation-batch-2') to produce forwardCyphers; builds rollbackCyphers as the audit Section 17 node+edge delete-by-selector pair (DETACH DELETE the event nodes created_by='phase-132-curation-batch-2'; DELETE the event-to-participant edges created_by='phase-132-curation-batch-2'); calls makeBatch({batchId:'phase-132-curation-batch-2', forwardCyphers, rollbackCyphers}); calls assertRollbackPath on the result before returning. The script main() routes argv: default --dry-run prints batch.dryRun() forward + rollback + a per-type count summary and touches the Brain zero times; --execute calls batch.execute() (creds-gated via the 132-01 runner) then prints the post-count summary; --rollback calls batch.rollback(). Add lib/memory/curation-132-02-hypergraph-reify.test.cjs implementing the five behaviors with NEO4J_* unset (dry-run + pure assertions only; the suite never touches a live Brain). Zero em-dashes; no fenced code blocks beyond Cypher template strings.
  </action>
  <verify>
    <automated>node lib/memory/curation-132-02-hypergraph-reify.test.cjs && node -e "const m=require('./scripts/curation-132-02-hypergraph-reify.cjs'); const s=require('./lib/brain/hypergraph-event-schema.cjs'); const b=require('./lib/brain/curation-batch.cjs'); const batch=m.buildHypergraphBatch(); b.assertRollbackPath(batch); const d=batch.dryRun(); const byType={}; d.forward.forEach(function(f){var mch=f.cypher.match(/MERGE \(\w*:(\w+Event)/); if(mch){byType[mch[1]]=(byType[mch[1]]||0)+1;}}); const types=Object.keys(byType).sort(); if(types.length<5){console.error('only '+types.length+' event types');process.exit(1);} const floor=s.EVENT_INSTANCE_MIN; for(const t of types){ if(byType[t]<floor){console.error(t+' has '+byType[t]+' < '+floor);process.exit(2);} } if(b.scanBatchForUserContent(batch).length){console.error('Part 8 violation');process.exit(3);} const destructive=d.forward.some(function(f){return f.cypher.split('\n').filter(function(l){return !l.trim().startsWith('//');}).join('\n').match(/\b(DELETE|REMOVE|DETACH|DROP)\b/);}); if(destructive){console.error('non-additive forward Cypher');process.exit(4);} console.log('OK '+types.length+' types, floor '+floor+' met per type');" && bash tests/run-all-132.sh</automated>
  </verify>
  <acceptance_criteria>
    - The batch covers exactly the 5 frozen EVENT_NODE_TYPES keys, each with >= EVENT_INSTANCE_MIN (20) seed instances (audit acceptance criterion 4).
    - Zero forward Cypher contains DELETE/REMOVE/DETACH/DROP (additive -- Canon Part 7; legacy binary edges untouched).
    - recommendFrameworkChain returns a non-empty array for a known seed after batch construction (the known caller still resolves -- the additive contract asserted against a real reader).
    - Every forward write carries created_by='phase-132-curation-batch-2'; assertRollbackPath passes; rollback restores the pre-batch event-node count by that selector.
    - scanBatchForUserContent(batch) returns [] (Part 8 clean -- handles + enums + scalars + provenance only).
    - run-all-132.sh appends this suite and stays GREEN.
  </acceptance_criteria>
  <done>The 5 reified event-node types are built as one reversible, additive, Part 8-clean batch routed through the 132-01 runner; >=20 instances per type; a known binary-edge caller still resolves; dry-run is the default and touches the Brain zero times.</done>
</task>

<task type="auto">
  <name>Task 2: register Plan-02 suite in run-all-132.sh; zero-regression gate</name>
  <read_first>
    - tests/run-all-132.sh (132-01 Task 3 shipped this; it is structured for Plans 02-05 to append their suites)
    - lib/memory/run-feynman-tests.cjs (the additive Phase 132 block 132-01 added; append the Plan-02 suite, every prior line byte-unchanged)
  </read_first>
  <action>
    Append lib/memory/curation-132-02-hypergraph-reify.test.cjs to tests/run-all-132.sh (mirroring how 132-01's two suites were registered) and to the Phase 132 block in lib/memory/run-feynman-tests.cjs (additive only -- every prior block byte-unchanged). Run the prior-phase aggregators (run-all-130.sh, run-all-132.sh, test-navigation-acceptance.cjs) to prove zero regression from the additive script + test + runner edits. Zero em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-132.sh && bash tests/run-all-130.sh && node tests/test-navigation-acceptance.cjs && git diff --unified=0 lib/memory/run-feynman-tests.cjs | grep -E '^-[^-]' && echo "REGRESSION: deleted lines in runner" && exit 1 || echo "runner additive-only OK"</automated>
  </verify>
  <acceptance_criteria>
    - run-all-132.sh runs the Plan-02 suite GREEN alongside the 132-01 suites.
    - The Phase 132 block in run-feynman-tests.cjs is additive: `git diff` shows only added lines.
    - Zero regression: run-all-130.sh + test-navigation-acceptance.cjs still pass.
  </acceptance_criteria>
  <done>The Plan-02 reify suite is wired into the phase aggregator + Feynman runner additively, with zero regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reify batch execute() -> Brain (Neo4j, remote, WRITE) | Adds 5 event-node types + event-to-participant edges. Default path is --dry-run (creds-free, zero writes); --execute is creds-gated through the 132-01 runner. |
| seed data (audit doc) -> reify Cypher | The audit-doc participant names are methodology handles (frameworks, public authors, public example companies), never user artifacts. The Part 8 scan is the structural guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-02-01 | Tampering (irreversible Brain write) | reify batch | mitigate | makeBatch rejects a batch without rollbackCyphers; the rollback is the audit Section-17 DETACH-DELETE-by-created_by pair; assertRollbackPath ties rollback to the forward created_by='phase-132-curation-batch-2'. Rollback restores the pre-batch event-node count. |
| T-132-02-02 | Tampering (broken caller -- Canon Part 7) | reified events vs legacy binary edges | mitigate | every forward Cypher is grep-gated to zero DELETE/REMOVE/DETACH/DROP (additive); the test calls chain-recommender.recommendFrameworkChain for a known seed and asserts a non-empty chain -- proving the binary-edge reader still resolves after the reify batch is built. |
| T-132-02-03 | Information Disclosure (Canon Part 8) | reify Cypher payloads | mitigate | scanBatchForUserContent over the whole batch returns []; the batch binds only participant handles + role enums + year/status/severity/outcome/industry scalars + provenance. LOCAL->BRAIN: NO. |
| T-132-02-04 | Spoofing (wrong-batch rollback) | rollback selector | mitigate | batchId regex-validated /^phase-132-curation-batch-2$/ by makeBatch; assertRollbackPath proves the rollback created_by selector equals the forward writes' -- a rollback cannot delete another batch's events. |
| T-132-02-05 | Elevation of Privilege | execute() write gate | mitigate | --dry-run is the default and opens no session; --execute requires NEO4J creds via the 132-01 runner's getNeo4jCreds. No write without explicit creds + --execute. |
| T-132-02-SC | Tampering | npm installs | mitigate | ZERO new packages: reuses lib/brain/* (132-01) + chain-recommender + node built-ins; the neo4j-driver is the mcp-server-brain/node_modules one. `git diff package.json` shows no dependency change; no install task, legitimacy gate N/A. |
</threat_model>

<verification>
- `node lib/memory/curation-132-02-hypergraph-reify.test.cjs` -- 5 types x >=20 instances, additive (no destructive Cypher), provenance + rollback, Part 8 clean, known-caller still-resolves all pass.
- `bash tests/run-all-132.sh` GREEN (132-01 + 132-02 suites); prior-phase aggregators GREEN (zero regression).
- `git diff package.json` shows no new dependency.
</verification>

<success_criteria>
- 5 reified event-node types with >=20 instances each are built as one reversible, additive, Part 8-clean batch through the 132-01 runner (audit acceptance criterion 4).
- The existing binary AUTHORED/ILLUSTRATES/CONTEXTUALIZES/MOTIVATES edges are untouched; chain-recommender still resolves (Canon Part 7).
- Default path is --dry-run (creds-free, zero Brain writes); --execute is creds-gated; rollback restores the pre-batch count.
</success_criteria>

<output>
Create `.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
