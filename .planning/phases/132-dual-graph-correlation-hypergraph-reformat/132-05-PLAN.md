---
phase: 132-dual-graph-correlation-hypergraph-reformat
plan: 05
type: execute
wave: 5
depends_on: ["132-02", "132-03", "132-04"]
files_modified:
  - scripts/curation-132-05-pseudonymize.cjs
  - lib/memory/curation-132-05-pseudonymize.test.cjs
  - scripts/verify-phase-132.cjs
  - tests/run-all-132.sh
autonomous: true
requirements:
  - PSEUDO-08     # pseudonymize the 6 internal-team :Person nodes (keep mindrian_internal:true) -- CONTEXT resolved decision 5, the Brain MCP ships to the testers tier
  - VERIFY-09     # run the Phase 130.7 CI dual-graph health gate as the Phase 132 release gate -- the reformat moved the metrics in the right direction (or at least no regression)
  - VERIFY-10     # the brain-boundary-scan is clean across the new code paths (Canon Part 8)
  - REVERSE-01    # the pseudonymize write carries created_by='phase-132-curation-batch-5'; reversible
  - REVERSE-02    # the batch carries an asserted rollback that restores the pre-batch :Person name properties
user_setup:
  - service: mindrian-brain
    why: "This batch pseudonymizes the 6 internal-team :Person nodes (replaces real names with pseudonyms, keeps mindrian_internal:true) via the 132-01 curation-batch runner as batch 'phase-132-curation-batch-5', then runs the Phase 130.7 CI dual-graph health gate to MEASURE the whole reformat (improvement / no-regression) as the phase release gate. --dry-run (default) prints the pseudonymize Cypher + reads the gate without mutating; --execute is creds-gated. The health gate is a READ-only Cypher health check shipped by Phase 130.7 -- this plan INVOKES it, never rebuilds it."
    env_vars:
      - name: NEO4J_URI
        source: "Brain Aura instance (.env)"
      - name: NEO4J_USER
        source: "Brain Aura instance (.env)"
      - name: NEO4J_PASSWORD
        source: "Brain Aura instance (.env)"

must_haves:
  truths:
    - "The 6 internal-team :Person nodes carry pseudonyms (real names replaced) AND keep the mindrian_internal:true flag (CONTEXT resolved decision 5; the no-real-names-in-tracked-surfaces rule applies because the Brain MCP ships to the testers tier)."
    - "No real internal-team name appears in scripts/curation-132-05-pseudonymize.cjs or its test (the pseudonym map keys are stable opaque handles; real names live only in-graph pre-pseudonymization, never in the tracked plugin repo) -- consistent with the no-real-names-in-tracked-files hard rule."
    - "The Phase 130.7 CI dual-graph health gate runs against the reformatted graph and reports that the reformat moved the 4 health metrics in the right direction OR at least did not regress them (the phase release gate)."
    - "The brain-boundary-scan passes on every new Phase 132 code path (Canon Part 8): no curation script writes user bytes to the Brain."
    - "The pseudonymize write carries created_by='phase-132-curation-batch-5'; the paired rollback restores the pre-batch :Person name properties by that selector."
  artifacts:
    - path: "scripts/curation-132-05-pseudonymize.cjs"
      provides: "the Plan-05 pseudonymize driver: SET each of the 6 internal-team :Person node names to a pseudonym (keyed by stable opaque handle, NOT by real name in the repo), keep mindrian_internal:true, route through makeBatch as batch-5 with paired name-restore rollback; --dry-run default"
      min_lines: 90
      exports: ["buildPseudonymizeBatch", "PSEUDONYM_HANDLES"]
    - path: "scripts/verify-phase-132.cjs"
      provides: "the phase release-gate runner: INVOKES the Phase 130.7 CI dual-graph health gate (the 4-metric health check) + the brain-boundary-scan over the new Phase 132 code paths, prints PASS/FAIL with the metric deltas; does NOT rebuild the gate (130.7 owns it)"
      min_lines: 70
      exports: ["runPhase132ReleaseGate"]
    - path: "lib/memory/curation-132-05-pseudonymize.test.cjs"
      provides: "asserts 6 pseudonym handles + mindrian_internal:true preserved, no real name in the script body, created_by stamping + rollback pairing (name-restore), Part 8 clean; asserts verify-phase-132 invokes (not reimplements) the 130.7 gate"
      min_lines: 80
  key_links:
    - from: "scripts/curation-132-05-pseudonymize.cjs"
      to: "lib/brain/curation-batch.cjs makeBatch + assertRollbackPath + scanBatchForUserContent"
      via: "makeBatch({batchId:'phase-132-curation-batch-5', forwardCyphers, rollbackCyphers}); assertRollbackPath before execute"
      pattern: "makeBatch|assertRollbackPath|scanBatchForUserContent"
    - from: "scripts/verify-phase-132.cjs"
      to: "the Phase 130.7 CI dual-graph health gate (the 4-metric health check) + brain-boundary-scan"
      via: "spawn/require the 130.7-shipped gate runner; parse its PASS/FAIL + metric output; do NOT reimplement the metrics"
      pattern: "health|boundary|130.7"
---

<objective>
Pseudonymize the 6 internal-team :Person nodes (pillar 3b), then run the Phase 130.7 CI dual-graph health gate + brain-boundary-scan as the Phase 132 release gate that MEASURES the whole reformat. The pseudonymize write routes through the 132-01 curation-batch runner as batch `phase-132-curation-batch-5`; the verification INVOKES the 130.7-shipped gate, it does NOT rebuild it (the CONTEXT scope fence: the CI gate + curation surfaces are Phase 130.7, 132 is MEASURED by them).

Pseudonymization is CONTEXT resolved decision 5: the Brain MCP now ships to the testers tier (a shared surface), so the spirit of the no-real-names-in-tracked-files hard rule applies to it. The 6 internal-team :Person nodes (re-labeled in the dogfooding session per audit Section 12 with mindrian_internal:true, names deliberately NOT listed in the audit) get pseudonyms while keeping the mindrian_internal:true flag. CRITICAL: the real names must NOT enter the tracked plugin repo -- the pseudonym map is keyed by stable opaque handles (e.g. internal-person-1), and the real-name->handle binding lives only in the live graph pre-pseudonymization, matched at execute time by the existing mindrian_internal:true flag, never by a real-name string literal in the script.

Purpose: this is the last wave because it both finishes the content (the 6 pseudonyms) and proves the phase: the 130.7 health gate measures whether the hypergraph reify (batch-2), cross-label dedup (batch-3), and wire-it (batch-4) moved the 4 health metrics in the right direction (JTBD cohort >= 3 frameworks; REVIEW_REQUIRED count down; one canonical target per query across labels; orphan-rate per label down). The phase ships only if the gate shows improvement or at least no regression.

Output: scripts/curation-132-05-pseudonymize.cjs (pseudonymize driver, --dry-run default), scripts/verify-phase-132.cjs (the release-gate runner), their test, the run-all-132.sh append.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-CONTEXT.md
@.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-01-PLAN.md
@.planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-CONTEXT.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Extracted from 132-01 + 130.7 + the audit. Executor: use these directly, no live Brain read. -->

From lib/brain/curation-batch.cjs (132-01):
  makeBatch({batchId, forwardCyphers, rollbackCyphers}) -> {dryRun(), execute(), rollback()}; batchId /^phase-132-curation-batch-\d+$/; rejects empty rollbackCyphers; created_by===batchId on every forward params.
  assertRollbackPath(batch); scanBatchForUserContent(batch) -> [] for handles-only.

The 6 internal-team :Person nodes (audit Section 12): re-labeled :Person with mindrian_internal:true in the dogfooding session; names deliberately NOT in the audit doc or any tracked file. Match them in-graph by the mindrian_internal:true flag, NEVER by a real-name string literal in the script. The Aronhime node is the one PUBLIC exception (Larry-persona namesake) and is NOT pseudonymized (it is wired AUTHORED to 7 PWS framework variants).

The Phase 130.7 CI dual-graph health gate (130.7-CONTEXT scope -- 132 INVOKES, never rebuilds):
  The 4-metric release-candidate Cypher health check: fails the build if ANY of -- a JTBD cohort < 3 frameworks OR REVIEW_REQUIRED count > 10 OR any framework has > 1 target across labels for the same canonical name OR orphan-rate per label > 5%. This is shipped by Phase 130.7 (the dependency). verify-phase-132 spawns/requires the 130.7 gate runner and parses its PASS/FAIL + the metric values; it must NOT reimplement the metrics. If the 130.7 gate is not present on disk (130.7 not yet executed in this checkout), verify-phase-132 must report a clear blocked-on-130.7 status, not silently pass.

The brain-boundary-scan (Canon Part 8 PR gate): scans the new Phase 132 code paths for any user-data egress to the Brain. verify-phase-132 runs it over scripts/curation-132-*.cjs + lib/brain/* and asserts clean. (If the repo-level scan tool is not yet scaffolded, fall back to scanBatchForUserContent over every batch builder the phase ships -- a structural equivalent.)

The audit Section 17 relabel/property rollback shape: MATCH (p {pseudonymized_by:$by}) SET p.name = p.name_pre_pseudonym REMOVE p.pseudonymized_by, p.name_pre_pseudonym -- store the pre-name in a temp property at execute time (in-graph only, never in the repo) so the rollback restores it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pseudonymize driver -- 6 internal :Person nodes by mindrian_internal flag, no real name in repo, via batch-5</name>
  <read_first>
    - ~/MindrianRooms/mindrianOS/methodology/2026-05-17-brain-curation-audit.md Section 12 (the 6 internal-team :Person re-labels with mindrian_internal:true; the Aronhime public exception) + Section 17 (the relabel/property rollback shape)
    - .planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-CONTEXT.md resolved decision 5 (pseudonymize the 6, keep mindrian_internal:true, because the Brain MCP ships to the testers tier)
    - lib/brain/curation-batch.cjs (makeBatch + assertRollbackPath + scanBatchForUserContent -- the write chokepoint)
    - docs/MINDRIAN-CANON.md Part 8 (no user/identifying bytes; the pseudonym is the de-identification) + the no-real-names-in-tracked-files hard rule (no real internal name in the script or test)
  </read_first>
  <behavior>
    - Test 1 (6 pseudonym handles): PSEUDONYM_HANDLES is a frozen array of exactly 6 stable opaque handles (e.g. internal-person-1..6, each mapped to a pseudonym string); none of the 6 handles or pseudonyms is a real internal-team name.
    - Test 2 (no real name in repo): the script body + test body contain no real internal-team name (assert structurally -- the match is by mindrian_internal:true flag in the Cypher, and the forward Cypher binds only the pseudonym + the handle, never a real-name string; a grep for a real-name token returns nothing because the binding is flag-based).
    - Test 3 (mindrian_internal preserved + Aronhime excluded): the pseudonymize Cypher matches (p:Person {mindrian_internal:true}) and SETs name=pseudonym WITHOUT removing mindrian_internal; it explicitly excludes the Aronhime public-exception node (WHERE p.name <> 'Aronhime' or an is_public_persona flag).
    - Test 4 (provenance + rollback): every forward params carries created_by==='phase-132-curation-batch-5'; the forward Cypher stores the pre-name in a temp property (name_pre_pseudonym) at execute time so the rollback can restore it; rollbackCyphers restores name from name_pre_pseudonym by pseudonymized_by selector; assertRollbackPath passes.
    - Test 5 (Part 8 clean): scanBatchForUserContent(batch) returns [] -- the batch binds only the mindrian_internal flag, pseudonym handles, and provenance.
  </behavior>
  <action>
    Create scripts/curation-132-05-pseudonymize.cjs. Export PSEUDONYM_HANDLES (a frozen array of 6 { handle, pseudonym } objects -- opaque handles + neutral pseudonym strings, ZERO real names). Export buildPseudonymizeBatch() that builds a forward Cypher matching (p:Person {mindrian_internal:true}) excluding the public Aronhime persona, assigning each matched node a pseudonym from PSEUDONYM_HANDLES by a stable in-graph ordinal (e.g. ordered by elementId so re-runs are idempotent), SET p.name_pre_pseudonym = p.name BEFORE overwriting p.name = pseudonym, SET p.pseudonymized_by='phase-132-curation-batch-5', and KEEP mindrian_internal:true; builds rollbackCyphers as the audit Section-17 restore shape (SET p.name = p.name_pre_pseudonym, REMOVE p.pseudonymized_by + p.name_pre_pseudonym by the selector); feeds to makeBatch({batchId:'phase-132-curation-batch-5', ...}); calls assertRollbackPath before returning. main() routes --dry-run (default, creds-free) / --execute (creds-gated) / --rollback. Add lib/memory/curation-132-05-pseudonymize.test.cjs (NEO4J_* unset; dry-run + pure assertions; Test 2 asserts no real-name token in the script source by reading the file and confirming the match is flag-based). Zero em-dashes; no fenced code blocks beyond Cypher template strings.
  </action>
  <verify>
    <automated>node lib/memory/curation-132-05-pseudonymize.test.cjs && node -e "const m=require('./scripts/curation-132-05-pseudonymize.cjs'); const b=require('./lib/brain/curation-batch.cjs'); if(m.PSEUDONYM_HANDLES.length!==6){console.error('need exactly 6 handles');process.exit(1);} const batch=m.buildPseudonymizeBatch(); b.assertRollbackPath(batch); const d=batch.dryRun(); const fwd=d.forward.map(function(f){return f.cypher;}).join(' '); if(!/mindrian_internal\s*:\s*true/.test(fwd)){console.error('match must be by mindrian_internal flag');process.exit(2);} if(!/name_pre_pseudonym/.test(fwd)){console.error('must store pre-name for rollback');process.exit(3);} if(b.scanBatchForUserContent(batch).length){console.error('Part 8 violation');process.exit(4);} console.log('OK 6 pseudonyms, flag-matched, reversible');"</automated>
  </verify>
  <acceptance_criteria>
    - PSEUDONYM_HANDLES has exactly 6 opaque handles; no real internal-team name in the script or test.
    - The pseudonymize Cypher matches by mindrian_internal:true (not real names), keeps mindrian_internal:true, excludes the public Aronhime node.
    - The pre-name is stored at execute time so the rollback restores it; created_by='phase-132-curation-batch-5'; assertRollbackPath passes; scanBatchForUserContent returns [].
  </acceptance_criteria>
  <done>The 6 internal-team :Person nodes are pseudonymized (mindrian_internal:true preserved, Aronhime excluded) via a reversible flag-matched batch-5 with zero real names in the tracked repo.</done>
</task>

<task type="auto">
  <name>Task 2: phase release-gate runner -- invoke the 130.7 CI health gate + brain-boundary-scan; register + zero-regression</name>
  <read_first>
    - .planning/phases/130.7-correlation-id-contract-dual-graph-ci-gates/130.7-CONTEXT.md (the 4-metric CI dual-graph health gate this runner INVOKES -- 132 is MEASURED by it, never rebuilds it)
    - docs/MINDRIAN-CANON.md Part 8 (the brain-boundary-scan PR gate -- no user-data egress) + the CANON-PHASE-MAP Part 8 row (the pending check-brain-boundary.cjs gate; fall back to scanBatchForUserContent if the repo-level tool is absent)
    - lib/brain/curation-batch.cjs scanBatchForUserContent (the structural Part 8 equivalent over the phase's batch builders)
    - tests/run-all-132.sh + lib/memory/run-feynman-tests.cjs (the additive registration surfaces)
  </read_first>
  <action>
    Create scripts/verify-phase-132.cjs exporting runPhase132ReleaseGate(). It (1) locates the Phase 130.7 CI dual-graph health gate runner (the 4-metric health check 130.7 ships); if present, spawns/requires it and parses PASS/FAIL + the 4 metric values, asserting the reformat moved them in the right direction OR did not regress (compare against a baseline snapshot captured before batches 2-4, or assert all 4 metrics within threshold); if the 130.7 gate is NOT on disk, return a clear blocked-on-130.7 status (do NOT silently pass); (2) runs the brain-boundary-scan over scripts/curation-132-*.cjs + lib/brain/* -- if the repo-level check-brain-boundary tool exists, invoke it; else fall back to requiring each Phase 132 batch builder (buildHypergraphBatch / buildDedupBatch / buildWireItBatch / buildPseudonymizeBatch) and asserting scanBatchForUserContent returns [] for each; (3) prints a PASS/FAIL summary with metric deltas. main() runs the gate and exits non-zero on FAIL or blocked. Append lib/memory/curation-132-05-pseudonymize.test.cjs to tests/run-all-132.sh + the Feynman runner Phase 132 block (additive; the test also asserts verify-phase-132 INVOKES rather than reimplements the 130.7 metrics -- e.g. by asserting the metric computation is not defined in verify-phase-132.cjs). Run run-all-130.sh, run-all-132.sh, test-navigation-acceptance.cjs for zero-regression. Zero em-dashes.
  </action>
  <verify>
    <automated>node -e "const v=require('./scripts/verify-phase-132.cjs'); if(typeof v.runPhase132ReleaseGate!=='function'){console.error('missing runPhase132ReleaseGate');process.exit(1);} const src=require('fs').readFileSync('./scripts/verify-phase-132.cjs','utf8'); if(/REVIEW_REQUIRED\s*>\s*10|orphan-rate|JTBD cohort\s*<\s*3/.test(src.replace(/\/\/.*/g,''))){console.error('verify-phase-132 must INVOKE the 130.7 gate, not reimplement the metrics');process.exit(2);} console.log('OK release-gate runner invokes 130.7, does not reimplement');" && bash tests/run-all-132.sh && bash tests/run-all-130.sh && node tests/test-navigation-acceptance.cjs && git diff --unified=0 lib/memory/run-feynman-tests.cjs | grep -E '^-[^-]' && echo "REGRESSION" && exit 1 || echo "runner additive-only OK"</automated>
  </verify>
  <acceptance_criteria>
    - verify-phase-132.cjs INVOKES the Phase 130.7 CI dual-graph health gate (does NOT reimplement the 4 metrics -- grep-gated); reports the reformat moved the metrics right or did not regress; reports blocked-on-130.7 if the gate is absent (never silent-pass).
    - The brain-boundary-scan is clean over the new Phase 132 code paths (or the scanBatchForUserContent fallback returns [] for all 4 batch builders).
    - run-all-132.sh runs the Plan-05 suite GREEN alongside 132-01..04; the Phase 132 Feynman block is additive; zero regression on run-all-130.sh + test-navigation-acceptance.cjs.
  </acceptance_criteria>
  <done>The Phase 132 release gate INVOKES the 130.7 CI health gate + brain-boundary-scan to MEASURE the reformat (improvement / no-regression), wired into the aggregator additively with zero regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pseudonymize batch execute() -> Brain (Neo4j, WRITE) | Overwrites 6 :Person name properties with pseudonyms. Default --dry-run is creds-free + zero writes; --execute is creds-gated through the 132-01 runner. |
| real internal-team names -> tracked plugin repo | The hard rule: real names must NEVER enter the tracked repo. The match is flag-based (mindrian_internal:true), not name-based; the script binds only pseudonyms + handles. |
| 130.7 CI health gate -> Phase 132 verification (READ-only) | verify-phase-132 INVOKES the gate; it never rebuilds the metrics (CONTEXT scope fence: the gate is 130.7's). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-05-01 | Information Disclosure (real names in repo) | pseudonymize script | mitigate | the script matches by mindrian_internal:true and binds only opaque handles + pseudonyms; Test 2 asserts no real-name token in the source; the real-name->pseudonym binding happens in-graph at execute time, never in a tracked file (the no-real-names-in-tracked-files hard rule). |
| T-132-05-02 | Tampering (irreversible name overwrite) | pseudonymize write | mitigate | the forward Cypher stores name_pre_pseudonym BEFORE overwriting name; makeBatch rejects no-rollback batches; the rollback restores name from name_pre_pseudonym by pseudonymized_by='phase-132-curation-batch-5'. |
| T-132-05-03 | Tampering (broken caller) | name overwrite vs AUTHORED edges | mitigate | only the name property changes; mindrian_internal:true and all AUTHORED edges are preserved; the Aronhime public-persona node (wired AUTHORED to 7 PWS variants) is explicitly excluded so its public namesake stays intact. |
| T-132-05-04 | Spoofing (gate reimplementation drift) | verify-phase-132 | mitigate | the runner INVOKES the 130.7-shipped gate and is grep-gated to NOT contain the 4-metric thresholds itself -- so the phase is measured by 130.7's gate, not a divergent copy that could pass falsely. |
| T-132-05-05 | Information Disclosure (Canon Part 8) | the whole phase | mitigate | verify-phase-132 runs the brain-boundary-scan / scanBatchForUserContent over all 4 Phase 132 batch builders; clean is the release-gate precondition. |
| T-132-05-SC | Tampering | npm installs | mitigate | ZERO new packages: lib/brain/* (132-01) + node built-ins + the 130.7-shipped gate; neo4j-driver reused from mcp-server-brain/node_modules. `git diff package.json` shows no change; no install task. |
</threat_model>

<verification>
- `node lib/memory/curation-132-05-pseudonymize.test.cjs` -- 6 pseudonym handles, flag-matched, no real name, mindrian_internal preserved, Aronhime excluded, reversible, Part 8 clean.
- `node scripts/verify-phase-132.cjs` -- the 130.7 CI health gate reports improvement/no-regression (or blocked-on-130.7); brain-boundary-scan clean; the runner invokes (not reimplements) the gate.
- `bash tests/run-all-132.sh` GREEN (132-01..05); prior aggregators GREEN; `git diff package.json` shows no new dependency.
</verification>

<success_criteria>
- The 6 internal-team :Person nodes are pseudonymized (mindrian_internal:true preserved, Aronhime excluded) with zero real names in the tracked repo, fully reversible by created_by='phase-132-curation-batch-5'.
- The Phase 130.7 CI dual-graph health gate (INVOKED, not rebuilt) shows the reformat moved the 4 metrics in the right direction or did not regress -- the phase release gate.
- brain-boundary-scan clean across all new Phase 132 code paths (Canon Part 8).
</success_criteria>

<output>
Create `.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-05-SUMMARY.md` when done.
</output>
</content>
