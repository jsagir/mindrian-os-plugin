---
phase: 132-dual-graph-correlation-hypergraph-reformat
plan: 04
type: execute
wave: 4
depends_on: ["132-03"]
files_modified:
  - scripts/curation-132-04-content-wire-it.cjs
  - lib/memory/curation-132-04-content-wire-it.test.cjs
  - tests/run-all-132.sh
autonomous: true
requirements:
  - WIRE-05       # the ~278-node wire-it backlog (clusters A/C/D/E/F/G) is wired with typed edges per the 2026-05-17 audit Section 15/24 backlog -- the Wire-It Contract (audit Section 10)
  - WIRE-06       # Ackoff-class fragmentation cleanup across the other shattered methodology authors (Drucker, Christensen, Senge, Meadows, de Bono)
  - REVERSE-01    # every wire-it write carries created_by='phase-132-curation-batch-4'; reversible
  - REVERSE-02    # the batch carries an asserted rollback that restores the pre-batch edge + label state
  - ADDITIVE-07   # wire-it ADDS typed edges (ILLUSTRATES/CONTEXTUALIZES/USES_CONCEPT/INSTANCE_OF/MOTIVATES) + reversible re-labels; case-difference dedup archives via :Archived+REPLACED_BY, never DETACH DELETE
user_setup:
  - service: mindrian-brain
    why: "This batch wires the ~278-node support-cast backlog with typed edges to the frameworks they support (the Wire-It Contract), routed through the 132-01 curation-batch runner as batch 'phase-132-curation-batch-4'. Breadth-first whole-cluster batches per cluster (A/C/D/E/F/G + shattered-author cleanup). --dry-run (default) prints the per-cluster edge + relabel Cypher and touches the Brain zero times; --execute is creds-gated."
    env_vars:
      - name: NEO4J_URI
        source: "Brain Aura instance (.env)"
      - name: NEO4J_USER
        source: "Brain Aura instance (.env)"
      - name: NEO4J_PASSWORD
        source: "Brain Aura instance (.env)"

must_haves:
  truths:
    - "Per-cluster, the wire-it edge count matches the 2026-05-17 audit backlog: Cluster A ~55 examples ILLUSTRATES frameworks; Cluster C2/C3 ~30 dedups + framework promotions; Cluster D ~80 concepts CONTEXTUALIZES/USES_CONCEPT; Cluster E ~20 problems INSTANCE_OF ProblemType + MOTIVATES framework; Cluster F ~25 case-difference dedup pairs; Cluster G ~15 manual one-by-one."
    - "Ackoff-class fragmentation cleanup is applied across the other shattered authors (Drucker, Christensen, Senge, Meadows, de Bono) -- their fragmented nodes are consolidated to a canonical with the support cast wired."
    - "Wiring is additive: typed edges (ILLUSTRATES/CONTEXTUALIZES/USES_CONCEPT/INSTANCE_OF/MOTIVATES) are ADDED and re-labels are reversible; case-difference dedup (Cluster F) archives the lowercase variant via :Archived + REPLACED_BY, never DETACH DELETE (audit Section 9 + Canon Part 7)."
    - "Every edge, re-label, and archive carries created_by = 'phase-132-curation-batch-4'; the paired rollback (DELETE edges / restore label / REMOVE :Archived) restores the pre-batch state by that selector."
    - "The wire-it batch writes only node handles, edge-type enums, and provenance toward the Brain; scanBatchForUserContent returns []."
  artifacts:
    - path: "scripts/curation-132-04-content-wire-it.cjs"
      provides: "the Plan-04 wire-it driver: breadth-first whole-cluster batches for A (ILLUSTRATES), C2/C3 (dedup + promotion), D (CONTEXTUALIZES/USES_CONCEPT), E (INSTANCE_OF+MOTIVATES), F (case-difference :Archived+REPLACED_BY), G (manual one-by-one), + shattered-author consolidation; all fed to makeBatch as batch-4 with the paired rollback; --dry-run default"
      min_lines: 150
      exports: ["buildWireItBatch", "CLUSTER_BACKLOG", "SHATTERED_AUTHORS"]
    - path: "lib/memory/curation-132-04-content-wire-it.test.cjs"
      provides: "asserts per-cluster wire-it counts match the audit backlog, F dedup uses :Archived+REPLACED_BY not DETACH DELETE, shattered-author consolidation present, created_by stamping + rollback pairing, Part 8 clean"
      min_lines: 90
  key_links:
    - from: "scripts/curation-132-04-content-wire-it.cjs"
      to: "lib/brain/curation-batch.cjs makeBatch + assertRollbackPath + scanBatchForUserContent"
      via: "makeBatch({batchId:'phase-132-curation-batch-4', forwardCyphers, rollbackCyphers}); assertRollbackPath before execute"
      pattern: "makeBatch|assertRollbackPath|scanBatchForUserContent"
    - from: "scripts/curation-132-04-content-wire-it.cjs"
      to: "the audit Section 15/24 cluster backlog counts"
      via: "CLUSTER_BACKLOG encodes the per-cluster node counts + edge types the audit enumerated"
      pattern: "ILLUSTRATES|CONTEXTUALIZES|USES_CONCEPT|INSTANCE_OF|MOTIVATES"
---

<objective>
Finish the Wire-It content: wire the ~278-node support-cast backlog (clusters A/C/D/E/F/G) with the typed edges the 2026-05-17 audit enumerated, plus the Ackoff-class fragmentation cleanup across the other shattered methodology authors. This is pillar 3a of Phase 132. Every mutation routes through the 132-01 curation-batch runner as batch `phase-132-curation-batch-4`, in breadth-first whole-cluster batches (CONTEXT resolved decision 4: 4.8 holds the full cluster map in context, so larger reversible per-cluster batches are viable).

The backlog is fully enumerated in the audit (no live Brain read): Section 15 (the original 6-cluster backlog with node counts), Section 18 (Cluster A executed examples -- 37 done, A6's 17 deferred to G), Section 19 (Cluster F 18 case-difference dedups done), Section 20 (Cluster C 10 dedups + 10 promotions), Section 21 (Cluster D1 521 concepts cleaned of former_label), Section 24 (the remaining-work table: D2 ~521 CONTEXTUALIZES wiring NOT STARTED, G ~37 NOT STARTED, cross-label NOT STARTED, shattered-author partially started). This plan covers the wire-it remainder: the A/C/D/E/F/G edge wiring that the audit marks as the support-cast contract, framed as whole-cluster batches.

Purpose: the audit Section 10 Wire-It Contract reframe -- the demoted Concepts are NOT broken Frameworks to relabel; they are the support cast (examples + authors + factors + motivations) that makes frameworks teachable. Without ILLUSTRATES/CONTEXTUALIZES/USES_CONCEPT/INSTANCE_OF/MOTIVATES edges, frameworks have no proof, no context, no motivation. With them, every framework becomes teachable and the dashboard-graph-neighborhood agent surfaces cross-domain analogies. Cluster F case-difference dedup uses the reversible-delete archive contract (:Archived + REPLACED_BY), never DETACH DELETE.

Output: scripts/curation-132-04-content-wire-it.cjs (the wire-it driver, --dry-run default), its test, the run-all-132.sh append.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-CONTEXT.md
@.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-01-PLAN.md
@docs/MINDRIAN-CANON.md

<interfaces>
<!-- Extracted from 132-01 + the audit. Executor: use these directly, no live Brain read. -->

From lib/brain/curation-batch.cjs (132-01):
  makeBatch({batchId, forwardCyphers, rollbackCyphers}) -> {dryRun(), execute(), rollback()}; batchId /^phase-132-curation-batch-\d+$/; rejects empty rollbackCyphers; created_by===batchId on every forward params.
  assertRollbackPath(batch); scanBatchForUserContent(batch) -> [] for handles-only.

The Wire-It Contract edge types (audit Section 10 + 15 -- the typed edges per cluster):
  Cluster A (~55 examples): (Example)-[:ILLUSTRATES]->(Framework). Section 18: A1 Disruptive Innovation 10, A2 Platform Thinking 9, A3 JTBD 5, A4 Beautiful Question 7 already executed (31 ILLUSTRATES); A6 17 deferred to G. The remaining A edges + any not-yet-wired are the A backlog.
  Cluster C2/C3 (~30): C2 = duplicates archived into live :Framework canonicals; C3 = promotions Concept->Framework with anchors. Section 20 lists the 10 dedups + 10 promotions.
  Cluster D (~80 of the ~521 cleaned): (Concept)-[:CONTEXTUALIZES]->(Framework) / (Framework)-[:USES_CONCEPT]->(Concept). Section 21 cleaned former_label on 521; D2 wiring is the remaining work.
  Cluster E (~20 problems): (Problem)-[:INSTANCE_OF]->(ProblemType) + (Problem)-[:MOTIVATES]->(Framework). Section 18 A5 wired 7 inline; the rest are the E backlog.
  Cluster F (~25 case-difference dedup pairs): title-case canonical wins; lowercase variant :Archived + REPLACED_BY. Section 19 did 18; remainder is the F backlog.
  Cluster G (~15-37 manual one-by-one): per-node triage; A6's 17 + B's 3 (Uriel/Yaakov/Yishai) + suspects (ECE, Shavuot Break, B2B, B2C, Oil & Gas, ARINC Model, Workarounds, Cognitive Framework, Three-Part Framework).

Shattered-author cleanup (audit Section 24 + CONTEXT): Ackoff first-pass done; Drucker, Christensen, Senge, Meadows, de Bono fragmented across multiple nodes -- consolidate to a canonical author + wire AUTHORED to their frameworks.

The audit Section 17 rollback shapes (edge / archive / relabel) -- same as Plan 03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wire-it driver -- per-cluster breadth-first edge wiring (A/C/D/E/F/G) + shattered-author consolidation via batch-4</name>
  <read_first>
    - ~/MindrianRooms/mindrianOS/methodology/2026-05-17-brain-curation-audit.md Section 10 (the Wire-It Contract 7-cluster table -- the typed edge per cluster), Section 15 (the original backlog with node counts), Section 18-21 (what was already executed -- subtract from the backlog so this batch covers the remainder), Section 24 (the remaining-work table -- D2 / G / shattered-author), Section 9 + 17 (archive contract + rollback shapes)
    - lib/brain/curation-batch.cjs (makeBatch + assertRollbackPath + scanBatchForUserContent -- the write chokepoint; do NOT hand-roll the driver)
    - scripts/brain-normalize-targeted.cypher (the MERGE (a)-[:EDGE]->(b) wiring idiom + the :Archived + REPLACED_BY dedup idiom for Cluster F)
    - docs/MINDRIAN-CANON.md Part 7 (additive edges + reversible relabel; reversible-delete for dedup) + Part 8 (handles + edge enums only)
  </read_first>
  <behavior>
    - Test 1 (per-cluster backlog counts): CLUSTER_BACKLOG has the 6 clusters A/C/D/E/F/G, each declaring its edge type + a node/edge count matching the audit backlog (A ~55 ILLUSTRATES, C ~30, D ~80 CONTEXTUALIZES/USES_CONCEPT, E ~20 INSTANCE_OF+MOTIVATES, F ~25 dedup pairs, G ~15+ manual). buildWireItBatch() yields forward Cyphers whose per-cluster count matches CLUSTER_BACKLOG (assert by grouping forward Cyphers by a cluster tag and counting).
    - Test 2 (correct edge type per cluster): every Cluster A forward Cypher MERGEs an ILLUSTRATES edge; Cluster D uses CONTEXTUALIZES or USES_CONCEPT; Cluster E uses INSTANCE_OF + MOTIVATES. Assert the edge label per cluster matches the Wire-It Contract table.
    - Test 3 (Cluster F reversible-delete): every Cluster F forward Cypher archives the lowercase variant via SET n:Archived + MERGE REPLACED_BY; no Cluster F Cypher contains DETACH DELETE (audit Section 9). Grep-gate skipping comment lines.
    - Test 4 (shattered-author consolidation present): SHATTERED_AUTHORS includes Drucker, Christensen, Senge, Meadows, de Bono; buildWireItBatch() yields a consolidation Cypher per shattered author (canonical author node + AUTHORED edges to their frameworks, archive of fragment variants via :Archived+REPLACED_BY).
    - Test 5 (provenance + rollback + Part 8): every forward params carries created_by==='phase-132-curation-batch-4'; rollbackCyphers covers DELETE edges + restore label + REMOVE :Archived; assertRollbackPath passes; scanBatchForUserContent(batch) returns [].
  </behavior>
  <action>
    Create scripts/curation-132-04-content-wire-it.cjs. Export CLUSTER_BACKLOG (a frozen object keyed by cluster A/C/D/E/F/G; each declares { edgeType, items:[{source,target}|{name,labels}], count } transcribed from audit Section 10/15/24, with already-executed items from Sections 18-21 subtracted so this batch covers the remainder; where the audit enumerates specific pairs use them, otherwise encode the named backlog items). Export SHATTERED_AUTHORS (frozen list: Drucker, Christensen, Senge, Meadows, de Bono, each with their fragment node names + their canonical + the frameworks to AUTHORED-wire, from CONTEXT). Export buildWireItBatch() that, per cluster, emits the cluster's typed-edge MERGE Cyphers (ILLUSTRATES for A; CONTEXTUALIZES + USES_CONCEPT for D; INSTANCE_OF + MOTIVATES for E; promotion SET label + anchor for C3; :Archived + REPLACED_BY for C2 + F dedups; per-node triage Cyphers for G), and per shattered author emits a consolidation Cypher (canonical + AUTHORED edges + :Archived+REPLACED_BY for fragments); SET created_by='phase-132-curation-batch-4' on every edge/label/archive; builds rollbackCyphers from the audit Section 17 shapes (DELETE edges by created_by, restore promoted labels, REMOVE :Archived by archived_by); feeds all to makeBatch({batchId:'phase-132-curation-batch-4', ...}); calls assertRollbackPath before returning. main() routes --dry-run (default, creds-free, prints the per-cluster count summary + shattered-author tally) / --execute (creds-gated) / --rollback. Breadth-first: one batch covers all clusters (CONTEXT decision 4). Add lib/memory/curation-132-04-content-wire-it.test.cjs (NEO4J_* unset; dry-run + pure assertions). Zero em-dashes; no fenced code blocks beyond Cypher template strings.
  </action>
  <verify>
    <automated>node lib/memory/curation-132-04-content-wire-it.test.cjs && node -e "const m=require('./scripts/curation-132-04-content-wire-it.cjs'); const b=require('./lib/brain/curation-batch.cjs'); const clusters=['A','C','D','E','F','G']; const miss=clusters.filter(function(c){return !m.CLUSTER_BACKLOG[c];}); if(miss.length){console.error('missing clusters: '+miss.join(','));process.exit(1);} const authors=['Drucker','Christensen','Senge','Meadows','de Bono']; const haveAuthors=authors.filter(function(a){return m.SHATTERED_AUTHORS.some(function(s){return JSON.stringify(s).indexOf(a)>=0;});}); if(haveAuthors.length<5){console.error('missing shattered authors: '+authors.filter(function(a){return haveAuthors.indexOf(a)<0;}).join(','));process.exit(2);} const batch=m.buildWireItBatch(); b.assertRollbackPath(batch); const d=batch.dryRun(); const hasIllustrates=d.forward.some(function(f){return /ILLUSTRATES/.test(f.cypher);}); const hasContextualizes=d.forward.some(function(f){return /CONTEXTUALIZES|USES_CONCEPT/.test(f.cypher);}); const hasInstanceOf=d.forward.some(function(f){return /INSTANCE_OF/.test(f.cypher)&&/MOTIVATES/.test(d.forward.map(function(x){return x.cypher;}).join(' '));}); if(!hasIllustrates||!hasContextualizes||!hasInstanceOf){console.error('missing wire-it edge type');process.exit(3);} if(b.scanBatchForUserContent(batch).length){console.error('Part 8 violation');process.exit(4);} console.log('OK 6 clusters + 5 shattered authors wired');" && bash tests/run-all-132.sh</automated>
  </verify>
  <acceptance_criteria>
    - CLUSTER_BACKLOG covers all 6 clusters A/C/D/E/F/G; per-cluster wire-it count matches the audit backlog.
    - The correct typed edge per cluster (ILLUSTRATES / CONTEXTUALIZES+USES_CONCEPT / INSTANCE_OF+MOTIVATES / promotion+anchor / :Archived+REPLACED_BY).
    - Cluster F + C2 dedup uses :Archived + REPLACED_BY only; zero DETACH DELETE (audit Section 9 + Canon Part 7).
    - Shattered-author consolidation present for Drucker, Christensen, Senge, Meadows, de Bono.
    - created_by='phase-132-curation-batch-4' on every write; assertRollbackPath passes; scanBatchForUserContent returns []; run-all-132.sh GREEN.
  </acceptance_criteria>
  <done>The ~278-node wire-it backlog (A/C/D/E/F/G) + shattered-author consolidation is wired with the correct typed edges as one reversible, additive, Part 8-clean breadth-first batch-4 through the 132-01 runner.</done>
</task>

<task type="auto">
  <name>Task 2: register Plan-04 suite in run-all-132.sh + Feynman runner; zero-regression gate</name>
  <read_first>
    - tests/run-all-132.sh (appended through Plan 03; append the Plan-04 suite the same way)
    - lib/memory/run-feynman-tests.cjs (the additive Phase 132 block; append Plan-04, prior lines byte-unchanged)
  </read_first>
  <action>
    Append lib/memory/curation-132-04-content-wire-it.test.cjs to tests/run-all-132.sh and to the Phase 132 block in lib/memory/run-feynman-tests.cjs (additive only). Run run-all-130.sh, run-all-132.sh, test-navigation-acceptance.cjs for zero-regression proof. Zero em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-132.sh && bash tests/run-all-130.sh && node tests/test-navigation-acceptance.cjs && git diff --unified=0 lib/memory/run-feynman-tests.cjs | grep -E '^-[^-]' && echo "REGRESSION" && exit 1 || echo "runner additive-only OK"</automated>
  </verify>
  <acceptance_criteria>
    - run-all-132.sh runs the Plan-04 suite GREEN alongside 132-01..03.
    - The Phase 132 block in run-feynman-tests.cjs is additive (git diff shows only added lines).
    - Zero regression: run-all-130.sh + test-navigation-acceptance.cjs still pass.
  </acceptance_criteria>
  <done>The Plan-04 wire-it suite is wired into the aggregator + Feynman runner additively, with zero regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire-it batch execute() -> Brain (Neo4j, WRITE) | Adds ~278 typed edges + re-labels + Cluster F/C2 archives. Default --dry-run is creds-free + zero writes; --execute is creds-gated through the 132-01 runner. |
| audit backlog -> wire-it edges | The source/target names are methodology handles (frameworks, public example companies, public authors). The Part 8 scan is the structural guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-04-01 | Tampering (irreversible Brain write) | Cluster F/C2 dedup + relabels | mitigate | dedup archives via :Archived + REPLACED_BY (audit Section 9), never DETACH DELETE -- grep-gated in Test 3; makeBatch rejects no-rollback batches; rollback = DELETE edges + restore label + REMOVE :Archived by created_by='phase-132-curation-batch-4'. |
| T-132-04-02 | Tampering (broken caller -- Canon Part 7) | typed-edge wiring | mitigate | wire-it ADDS edges (ILLUSTRATES/CONTEXTUALIZES/etc.); it never deletes a pre-existing binary edge; C3 promotions SET a label (reversible), and the rollback restores it. Existing callers reading the prior graph are unaffected. |
| T-132-04-03 | Information Disclosure (Canon Part 8) | wire-it Cypher payloads | mitigate | scanBatchForUserContent returns []; the batch binds only node-name handles, edge-type enums, and provenance. LOCAL->BRAIN: NO. |
| T-132-04-04 | Spoofing (wrong wire target) | edge source/target | mitigate | targets are the canonical correlation_id nodes produced by batch-3 (132-04 depends on 132-03, so dedup lands first); a mis-wired edge is fully reversible (DELETE by created_by). |
| T-132-04-05 | Elevation of Privilege | execute() write gate | mitigate | --dry-run default opens no session; --execute requires NEO4J creds via the 132-01 runner. |
| T-132-04-SC | Tampering | npm installs | mitigate | ZERO new packages: lib/brain/* (132-01) + node built-ins; neo4j-driver reused from mcp-server-brain/node_modules. `git diff package.json` shows no change; no install task. |
</threat_model>

<verification>
- `node lib/memory/curation-132-04-content-wire-it.test.cjs` -- 6 clusters with correct edge types, F reversible-delete, 5 shattered authors, provenance + rollback + Part 8 all pass.
- `bash tests/run-all-132.sh` GREEN (132-01..04); prior aggregators GREEN.
- `git diff package.json` shows no new dependency.
</verification>

<success_criteria>
- The ~278-node wire-it backlog (A/C/D/E/F/G) is wired with the correct typed edges per the audit; per-cluster counts match the backlog.
- Ackoff-class fragmentation cleanup is applied across Drucker/Christensen/Senge/Meadows/de Bono.
- Breadth-first whole-cluster batch routed through the 132-01 runner, Part 8 clean, fully reversible by created_by='phase-132-curation-batch-4'.
</success_criteria>

<output>
Create `.planning/phases/132-dual-graph-correlation-hypergraph-reformat/132-04-SUMMARY.md` when done.
</output>
</content>
