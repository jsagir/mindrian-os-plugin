---
phase: 150.6-drift-fix-sweep-and-human-gate-quick-closers
plan: 150.6-01
status: complete
requirements-completed: [DRIFT-01, DRIFT-10]
subsystem: prose-truth + TUI-hygiene
one-liner: "Larry's agent + skill prose corrected to shipped facts (6 reach-ids, SENS-01..08, Phase 144 engine flip SHIPPED) and five command files brought to glyph + Action-Footer + Part-8-help compliance, zero em-dashes."
files-modified:
  - agents/larry-extended.md
  - skills/larry-personality/SKILL.md
  - commands/doctor.md
  - commands/memory.md
  - commands/room.md
commits:
  - d317eb49: "fix(150.6-01): FIX-01 rewrite Larry reach/sensor/engine-flip prose to shipped facts"
  - d30347ff: "fix(150.6-01): FIX-10 Action Footers + glyph hygiene + memory.md M8 help line"
metrics:
  tasks: 2
  files: 5
  duration: ~12m
  completed: 2026-06-11
---

# Phase 150.6 Plan 01: Drift-Fix Prose-Truth Half (FIX-01 + FIX-10) Summary

Executed the prose-truth half of the 150.6 drift sweep -- no code, no Brain numbers touched.
Larry's runtime self-knowledge now reflects shipped reality, and five command surfaces honor
the sanctioned-glyph + no-em-dash + Part-8 hard rules.

## Task 1 (FIX-01): Larry's reach/sensor/engine-flip prose -> shipped facts

Ground truth confirmed against code before writing:
- `lib/hmi/dial-reach-orchestrator.cjs:56` `DIAL_REACH_K = 6`; `:74-81` REACH_DEFS = 6 entries (hats is the 6th).
- `lib/core/insight-sensors.cjs:351-363` SENSOR_REGISTRY = 8 entries (SENS-01..08, incl. sensorMemoryCortex SENS-08).

Edits:
- `agents/larry-extended.md` body line 42 ("## Operating the machinery"): "5 reach-ids ... 7 insight
  sensors ... engine flip ... executing in Phase 144" -> "6 reach-ids are LIVE (Phase 141 getRoomContext
  + Phase 148 minted hats as the 6th), 8 insight sensors are LIVE (Phase 143: SENS-01..08) ... the engine
  flip that auto-fires the dial SHIPPED (Phase 144: lib/core/navigation-engine.cjs decide() flips
  routing_source legacy to engine on a fired reach)." The "you DRIVE these surfaces" framing kept intact.
- `skills/larry-personality/SKILL.md`:
  - Section header "### Scope -- shipped sensors + dial, engine flip executing" -> "... engine flip SHIPPED".
  - Scope body (line 113 region): "SENS-01..07" -> "SENS-01..08"; "Phase 144 wires ... is executing now ...
    Until 144 lands the engine does not route live" -> shipped tense ("Phase 144 wired dispatchSensors INTO
    decide() ... SHIPPED (lib/core/navigation-engine.cjs; tests/run-all-144.sh 5/5). ... The engine routes live.").
  - Operating-the-Dial behavior 1 (line 119): "The seven sensors (SENS-01..07)" -> "The eight sensors
    (SENS-01..08)". The already-correct "frozen six reach-ids (context_block, contradiction, cross_room,
    brain_consult, deep_research, hats)" left UNCHANGED.
  - Reading-routing_source (line 129): "until Phase 144 lands, routing_source stays legacy; the engine routes
    once 144 lands, not now" -> "Phase 144 SHIPPED: when the engine populates fire_skill, routing_source flips
    legacy to engine; the engine routes live now." The Part 8 floor sentence after it left UNCHANGED.

Frozen larry-extended frontmatter: byte-identical. Captured sha256 of lines 1-26 BEFORE editing
(`b7bf4128f906a5181e696ab361350a838c4fb3136706656af7f3caa186e8a25c`) and re-confirmed the identical hash
AFTER the body edit. Frontmatter untouched per Phase 143.2 freeze.

Verify output (plan automated gate): PASS (6 reach-ids present / 5 absent; SENS-01..08 present / SENS-01..07
absent; SHIPPED present; "eight sensors" present / "seven sensors" absent; "until Phase 144 lands" + "is
executing now" both absent; zero em-dashes across both files).

## Task 2 (FIX-10): Action Footers + glyph hygiene + memory.md M8 help line

(a) `commands/doctor.md` -- added "## Zone 4 (Action Footer)" at end (graph.md:125-130 shape):
    "> Want to repair what we found? -> /mos:doctor --fix" / "> Want the full picture? -> /mos:status".
(b) `commands/memory.md` -- added "## Zone 4 (Action Footer)":
    "> Want to pick up where you left off? -> /mos:memory resume" / "> Want what other rooms learned? -> /mos:memory cross-room".
(c) `commands/memory.md` help_jtbd: "Inspect what the Brain remembers about your sessions." ->
    "Inspect what the room remembers about your sessions." (Canon Part 8 -- the Brain never holds session
    data; the room does). Renders in /mos:help via scripts/help-renderer.cjs.
(d) Glyph hygiene:
    - `commands/doctor.md`: all 8 em-dash (U+2014) occurrences -> "--" (frontmatter line 3 description + body
      lines 17, 23-25, 45-47). Sanctioned "->" arrows left intact.
    - `commands/room.md`: the two box-drawing header lines (`╭─ ... ──╮` + the `│ ... │` border row at the
      old lines 86-87 / 165-166) collapsed to plain-text header lines (`-- [Room Name] -- ... --`); the two
      `✗` error glyphs (lines 150, 230) -> literal `x` (graph.md:32 idiom). Sanctioned tree glyphs `├─` / `└─`
      / `│` inside Zone-2 semantic-tree renders left intact (in the 12-glyph vocabulary).
    - `commands/memory.md` `§` at line 77: not flagged by --ui-compliance, left as-is per the plan's
      low-priority instruction.
    - `commands/jtbd.md` + `commands/update.md`: confirmed clean (no em-dash, no box chars); not touched.

Verify output (plan automated gate): UI-COMPLIANCE CLEAN + PASS.
- `node scripts/doctor.cjs --ui-compliance --scan-commands=./commands --json`: zero violations name any of
  doctor.md / memory.md / room.md / jtbd.md / update.md.
- doctor.md + memory.md each contain "## Zone 4 (Action Footer)".
- memory.md contains "room remembers about your sessions"; stale "Brain remembers" line removed.
- zero em-dashes in doctor.md (and across all 7 surfaces touched in this plan).

## Regression fences

- `bash tests/run-all-148.sh`: 18/18 PASS (exit 0) -- frozen reach contracts intact.
- `bash tests/run-all-150.sh`: 15/15 PASS (exit 0) -- cortex bridge + 148 constitution held.

## Deviations

1. **--ui-compliance scope clarification (not a defect; informational).** The plan's verify command checks
   whether the five command files are *named as violations* in the class F scanner output. At baseline AND
   after edits, zero violations name those five files: the class F scanner flags `scripts/*.cjs` renderers
   (missing Zone 1/4 patterns, box chars) but does NOT scan command-`.md` em-dashes. The binding em-dash
   gate is therefore the separate no-em-dash HARD-RULE grep (which passes). No action needed; the plan's
   acceptance criteria are satisfied as written. The 129 pre-existing `scripts/*.cjs` class F violations are
   out of scope (not introduced by this plan) and were left untouched.

2. **SKILL.md "### Scope" section header also corrected.** The plan's edit list named lines 113/119/129 but
   the section header on line 111 ("engine flip executing") carried the same stale tense. Corrected to
   "engine flip SHIPPED" for internal consistency (Rule 1 -- the prose-truth fix would have been incomplete
   otherwise). Body frontmatter and all other lines unchanged.

3. **room.md box-drawing replacement chose plain-text headers** rather than tree glyphs. The sanctioned
   12-glyph vocabulary has no top-corner box characters (`╭ ╮`), and the header rows are Zone-1 panel
   decoration, not semantic-tree rows, so `├─`/`└─` would be semantically wrong there. Collapsed each
   2-line box header to a single plain-text `-- ... --` header line, matching the sanctioned `--` separator.

## Self-Check: PASSED

- agents/larry-extended.md: FOUND, frontmatter sha256 byte-identical (b7bf4128...).
- skills/larry-personality/SKILL.md: FOUND, SENS-01..08 + eight sensors + 144 SHIPPED present.
- commands/doctor.md: FOUND, Zone 4 present, zero em-dashes.
- commands/memory.md: FOUND, Zone 4 present, Part-8-correct help_jtbd.
- commands/room.md: FOUND, no box-drawing headers, no ✗.
- Commit d317eb49: FOUND in git log.
- Commit d30347ff: FOUND in git log.
