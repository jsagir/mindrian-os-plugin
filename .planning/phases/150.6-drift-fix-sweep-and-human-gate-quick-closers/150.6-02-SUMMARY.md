---
phase: 150.6-drift-fix-sweep-and-human-gate-quick-closers
plan: 02
subsystem: docs
tags: [supply-chain, npm-deprecate, brain-mcp, corpus-figures, workspace-guard, pre-commit-hook, canon-amendment]

requires:
  - phase: 148-larryreach-selector-re-wire
    provides: "DIAL_REACH_K=6 frozen reach bank that the 148/150 regression fences assert (held green through these doc edits)"
  - phase: 127.1-graphrag-collapse
    provides: "the MethodologyChunk substrate move into Neo4j that the 27,804-node total now reflects"
provides:
  - "package.json + docs swept to the live @mindrian_os/cli package; zero npx-install-instruction strings to the dead @mindrian_os/install"
  - "@mindrian_os/install deprecated live on npm (Retired -- use @mindrian_os/cli)"
  - "THE-BRAIN.md honest 6-tool Brain MCP table (brain_query/schema/write/search/stats/ask); brain_ask flagged as the flagship GUIDED DirectiveEnvelope tool"
  - "brain-setup.md Six Brain tools + brain_ask row"
  - "ONE live-read Brain number set (27,804 / 19,987 / 12,413) across THE-BRAIN.md, brain-setup.md, CLAUDE.md, MINDRIAN-CANON.md Appendix D"
  - "MINDRIAN-CANON.md Appendix D entry 16 + CANON-PHASE-MAP version-history row (figures correction; canon stays v1.6)"
  - "CLAUDE.md + release-process.md repointed to /home/jsagi/dev/MindrianOS-Plugin"
  - "install-pre-commit.sh fixed so appended guards splice BEFORE the terminal exit 0; refreshed installed hook fires both the substrate-diff and schema-alias guards"
affects: [release-process, brain-docs, future-tester-emails, canon-provenance]

tech-stack:
  added: []
  patterns:
    - "Appendix-D entry-13 corpus-figures-corrected procedure reused (one live read normalizes all surfaces; provenance entry records, doctrine body untouched; canon version unchanged)"
    - "pre-commit guard splice-before-terminal-exit-0 (installer inserts missing guards ahead of the last bare exit 0 instead of blind-appending)"

key-files:
  created:
    - .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/deferred-items.md
  modified:
    - package.json
    - docs/INSTALL-LIFECYCLE-HARNESS.md
    - docs/CANON-PHASE-MAP.md
    - docs/THE-BRAIN.md
    - docs/brain-setup.md
    - CLAUDE.md
    - docs/MINDRIAN-CANON.md
    - .claude/includes/release-process.md
    - scripts/install-pre-commit.sh

key-decisions:
  - "Navigator-pre-resolved checkpoint (Task 2a): FRESH LIVE READ chosen over the entry-13 RATIFIED set; orchestrator ran it 2026-06-11 against production Aura (my-neo4j MCP) + Pinecone brain_stats."
  - "State the MethodologyChunk substrate nuance everywhere the node total appears (27,804 total incl. 12,401 MethodologyChunk; ~15.4K teaching-graph core) so the substrate is never hidden."
  - "Keep the dead-name mentions of @mindrian_os/install in INSTALL-LIFECYCLE-HARNESS.md prose (they correctly identify it as the dead/swept package); the must-have is zero npx-RUN-instruction strings, which is satisfied."
  - "npm deprecate attempted AND succeeded (npm authed as jsagir); supply-chain squat window closed at the registry layer."

patterns-established:
  - "Cherry-pick (never branch-merge) for single-commit doc corrections, then apply superseding live-read numbers ON TOP in a separate commit."

requirements-completed: [DRIFT-02, DRIFT-03, DRIFT-06]

duration: 38min
completed: 2026-06-11
---

# Phase 150.6 Plan 02: Drift-Fix Sweep (Supply-Chain + Brain-Truth + Workspace-Path) Summary

**Swept the dead `@mindrian_os/install` package to the live `@mindrian_os/cli` (and deprecated the dead name on npm), rewrote THE-BRAIN.md's fictional 7-tool table to the real 6 with `brain_ask` as flagship, normalized all four Brain-number surfaces onto one fresh live-read set (27,804 / 19,987 / 12,413), repointed the workspace guard to `~/dev/MindrianOS-Plugin`, and fixed a pre-commit installer bug that had left the substrate-diff guard stranded after a terminal `exit 0`.**

## Performance

- **Duration:** ~38 min
- **Started:** 2026-06-11
- **Completed:** 2026-06-11
- **Tasks:** 3/3 (Task 1 auto, Task 2a checkpoint pre-resolved, Task 2b auto)
- **Files modified:** 9 (8 doc/config + 1 installer); 1 created (deferred-items.md)

## Navigator Decision (Task 2a checkpoint -- pre-resolved, recorded here)

The Task-2a `checkpoint:human-action` (live Brain corpus read) was **pre-resolved by the navigator** and executed by the orchestrator before this run; I did not pause. Decision: **FRESH LIVE READ** (not the entry-13 RATIFIED set). Production Aura via `my-neo4j` MCP + Pinecone `brain_stats`, 2026-06-11:

- **Neo4j:** 27,804 nodes / 19,987 relationships.
  - Composition (stated in the docs, not hidden): 12,401 of those nodes are MethodologyChunk substrate (Phase 127.1 GraphRAG collapse moved the chunk corpus into Neo4j); the teaching-graph core is ~15.4K nodes.
  - Top labels: MethodologyChunk 12,401; Concept 9,131; __Entity__ 4,357; Product 1,289; Chunk 1,167; Event 1,013; ProcessStep 650; Person 624; Framework 748.
- **Pinecone pws-brain:** 12,413 vectors, 1024-dim. Namespaces: core 8,555; materials 1,775; reference 1,690; tools 242; graphrag 144; books 7.

This set supersedes the three conflicting sets the docs carried (notably the entry-13 ratified 15,298 / 19,713 / 12,401). Published figures are corpus aggregates only (Part 8 permits); no user content, no `:Person` names, no artifact text.

## Accomplishments

### Task 1 -- FIX-02 dead-package sweep + FIX-06 workspace-path sweep + worktree prune + pre-commit refresh (commit a3c86775)

- **FIX-02:** `package.json` description `npx @mindrian_os/install` -> `npx @mindrian_os/cli` (verified the bare-`npx @mindrian_os/cli` idiom against README:64). `docs/INSTALL-LIFECYCLE-HARNESS.md`: flipped the inverted rename at line 100 and fixed three further install-string/arrow refs (line 91 live round-trip; lines 113/133 historical rename arrows) so every direction reads `@mindrian_os/install -> @mindrian_os/cli`. `docs/CANON-PHASE-MAP.md`: both "Step 9.5 @mindrian_os/install" rows -> `@mindrian_os/cli`.
- **npm deprecate:** ATTEMPTED and SUCCEEDED. `npm whoami` = jsagir; `npm deprecate @mindrian_os/install "Retired -- use @mindrian_os/cli"` exit 0; `npm view @mindrian_os/install deprecated` now returns "Retired -- use @mindrian_os/cli". Squat window closed at the registry layer.
- **FIX-06 paths:** `CLAUDE.md` (Working-directory header, WORKSPACE GUARD `cd` instruction, "THE ONLY DEV WORKSPACE" text) and `.claude/includes/release-process.md` ("canonical development workspace", `cd ~/MindrianOS-Plugin`) repointed `/home/jsagi/MindrianOS-Plugin` -> `/home/jsagi/dev/MindrianOS-Plugin` and `~/MindrianOS-Plugin` -> `~/dev/MindrianOS-Plugin`. WORKSPACE GUARD prose left intact; the different repos `/home/jsagi/MindrianOS/` and `/home/jsagi/MindrianV2/` left untouched.
- **FIX-06 worktree prune:** `git worktree prune` ran clean. Count 48 before / 48 after / **0 pruned** -- the 47 agent worktrees are all `locked`, so prune (which only removes unlocked + missing-path entries) is a correct no-op here. The audit's "47 orphans" expectation assumed unlocked entries; on this box they are locked, not orphaned, so nothing was removed (and nothing should have been).
- **FIX-06 pre-commit refresh + bug fix:** `grep -ln "hooks/pre-commit"` found the installers `scripts/setup-hooks.sh` + `scripts/install-pre-commit.sh`. Re-running them surfaced a real ordering bug (see Deviations) -- the substrate-diff guard had been appended AFTER a terminal `exit 0` and was unreachable. Fixed the tracked installer to splice missing guards before the last bare `exit 0`, rebuilt the installed hook (234 -> 241 lines). Verified end-to-end: green path (harmless staged change) runs both guards and exits 0; red path (staged `require('better-sqlite3')` chokepoint bypass) -> substrate guard FAILs exit 1; red path (bare `sendPacket(` outside allow-list) -> schema-alias guard FAILs exit 1.

### Task 2a -- live Brain read checkpoint (pre-resolved; see Navigator Decision above)

### Task 2b -- FIX-03 cherry-pick + 6-tool table + brain_ask + four-surface normalization (commits 8bfbf9c7 + 8ea1702e)

- **Cherry-pick a3c422fe (commit 8bfbf9c7):** `git cherry-pick a3c422fe` applied CLEAN (Auto-merging, +11/-8, no conflict -- matching the PATTERNS merge-tree simulation). The abort-and-hand-apply fallback was NOT needed. Brought the qualitative corrections: CO_OCCURS 0-in-graph sidecar note, FEEDS_INTO as the load-bearing first moat edge, Tier-0 "curated subset (~275) of the live graph's 748 :Framework nodes" framing.
- **THE-BRAIN.md 6-tool table (commit 8ea1702e):** replaced the fictional 7-tool table (enrich_context / suggest_chain / suggest_methodology / grade_room / find_connections / get_chaining_rules / get_teaching_context -- none registered) with the real 6: brain_query, brain_schema, brain_write, brain_search, brain_stats, brain_ask. `brain_ask` described as the flagship GUIDED DirectiveEnvelope tool (mcp-server-brain/lib/brain-ask.cjs). brain_api_keys / brain_usage_log (Supabase tables) NOT listed.
- **brain-setup.md:** "Five Brain tools" -> "Six Brain tools"; added the `brain_ask` row; replaced the stale "21K+ nodes / 1,427 embeddings" and the "1,427 PWS knowledge vectors" with the live set.
- **Four-surface normalization:** THE-BRAIN.md (Layer 1 header + node-type label list + Layer 2 Pinecone header/namespaces), brain-setup.md (the three-bullet count block + brain_search row), CLAUDE.md (Three Layers Brain row + moat teaching-graph header + curriculum-graph line + the PROJECT/STACK Brain-number prose), and MINDRIAN-CANON.md Appendix D all now carry ONE set: 27,804 / 19,987 / 12,413 with the MethodologyChunk-substrate + ~15.4K-core nuance stated wherever the total appears.
- **Canon + map:** MINDRIAN-CANON.md Appendix D entry 16 ("Corpus figures corrected (2026-06-11)") in the entry-13 voice; canon header Version stays **1.6** (figures correction, no doctrine change). CANON-PHASE-MAP.md gained the matching v1.6-(kept) Version-history row (canon_parts 4/6/7/8) per the "update this map in the same commit" rule.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Pre-commit substrate guard stranded after a terminal `exit 0` (never fired)**
- **Found during:** Task 1 (FIX-06 pre-commit refresh).
- **Issue:** `scripts/setup-hooks.sh` writes a hook body ending in a bare `exit 0`. `scripts/install-pre-commit.sh` then `cat >>`-appended its substrate-diff and schema-alias guards AFTER that `exit 0`, making them unreachable dead code. The FIX-06(d) acceptance ("the refreshed hook fires both guards on a scratch commit") could not be met as the hook stood.
- **Fix:** Rewrote the installer's append path to build the missing-guard snippet in a temp file and splice it in BEFORE the LAST bare `exit 0` line (falling back to append only when no terminal `exit 0` exists). Repaired the already-installed hook by stripping the stranded tail and re-running the fixed installer. Verified both guards now run before `exit 0` and actively reject on their triggers.
- **Files modified:** scripts/install-pre-commit.sh (tracked); .git/hooks/pre-commit (untracked install artifact).
- **Commit:** a3c86775.

**2. [Rule 2 - Missing critical correctness] Extra inverted `@mindrian_os/install` references in INSTALL-LIFECYCLE-HARNESS.md beyond the named line 100**
- **Found during:** Task 1 verify gate.
- **Issue:** The plan's read_first named only line 100, but the verify gate exposed three more references (line 91 live round-trip string; lines 113/133 historical rename arrows pointing the wrong way). Leaving them would keep a shipped install-string steering toward the dead package.
- **Fix:** Line 91 -> `npx @mindrian_os/cli`; lines 113/133 arrows flipped to `@mindrian_os/install -> @mindrian_os/cli`.
- **Files modified:** docs/INSTALL-LIFECYCLE-HARNESS.md.
- **Commit:** a3c86775.

## Authentication Gates

None blocked execution. npm was already authenticated (`npm whoami` = jsagir), so the `user_setup` npm-deprecate step completed inline rather than being deferred -- recorded above as done.

## Deferred Issues

See `deferred-items.md` in this phase dir:
- **DI-150.6-02-01:** four `npx @mindrian_os/install` strings remain in `docs/testers/outbox/` sent-mail archives (out of FIX-02's named scope; editing archived sent mail would falsify the record; the npm deprecate redirects them at the registry layer regardless).
- **DI-150.6-02-02:** ~52 pre-existing em-dashes remain in CLAUDE.md sections unrelated to FIX-03 (header, WORKSPACE GUARD, decisions, architecture, release process, PROJECT/STACK blocks). Every line THIS plan edited was converted to hyphens; THE-BRAIN.md / brain-setup.md / MINDRIAN-CANON.md are fully em-dash clean (0 each). A whole-file CLAUDE.md em-dash hygiene pass is the recommended follow-up.

## Verification Evidence

- `node -e "require('./package.json')"` -> VALID JSON.
- `grep "@mindrian_os/install"` over the FIX-02 files: only the intentional dead-name mentions remain (no `npx @mindrian_os/install` RUN-instruction strings in shipped install docs/config).
- Four-surface number agreement confirmed: 27,804 / 19,987 / 12,413 present in all of THE-BRAIN.md, brain-setup.md, CLAUDE.md, MINDRIAN-CANON.md; zero stale 15,298 / 19,713 / 21K / 65K on the three doc surfaces.
- THE-BRAIN.md: 6 real tools present; zero fictional tools (enrich_context/suggest_chain/etc).
- brain-setup.md: "Six Brain tools" present, "Five" gone, brain_ask row present, "1,427" gone.
- Canon header Version = 1.6; Appendix D entry 16 present; CANON-PHASE-MAP row present.
- Em-dash gate: THE-BRAIN.md / brain-setup.md / MINDRIAN-CANON.md = 0; all CLAUDE.md lines edited by this plan = 0 (pre-existing remainder deferred).
- Pre-commit hook: green path exits 0 running both guards; red paths block (substrate exit 1, schema-alias exit 1).
- **Regression fences GREEN:** `bash tests/run-all-148.sh` 18/18 exit 0; `bash tests/run-all-150.sh` 15/15 exit 0.

## Known Stubs

None. No empty-value stubs, placeholders, or unwired data sources introduced -- this is a doc/config correction plan.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern, or schema change introduced beyond the plan's `<threat_model>` (the two mitigated boundaries -- npm package reference and live-Brain-read-to-docs -- were addressed exactly as the register prescribed: dead-name swept + deprecated; only corpus aggregates published).

## Self-Check: PASSED

- FOUND: .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-02-SUMMARY.md
- FOUND: .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/deferred-items.md
- FOUND commit a3c86775 (Task 1)
- FOUND commit 8bfbf9c7 (cherry-pick a3c422fe)
- FOUND commit 8ea1702e (Task 2b)
