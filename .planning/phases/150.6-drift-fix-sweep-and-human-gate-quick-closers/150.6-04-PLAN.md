---
phase: 150.6-drift-fix-sweep-and-human-gate-quick-closers
plan: 04
type: execute
wave: 2
depends_on: [150.6-01, 150.6-02]
files_modified:
  - .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md
  - .planning/phases/88-feynman-minto-memory-layer/88-VERIFICATION.md
  - .planning/phases/126-install-lifecycle-harness-gaps/126-VERIFICATION.md
  - skills/ui-system/SKILL.md
  - lib/hmi/dial-presenter.cjs
  - docs/MINDRIAN-CANON.md
autonomous: false
requirements: [DRIFT-07, DRIFT-08, DRIFT-09, DRIFT-11]
must_haves:
  truths:
    - "A written GATE-0 verdict states whether intelligence-cascade fires at SEGMENT granularity on meeting artifacts E2E, consumable by the Phase 150.8 plan 01 type guard"
    - "Phase 88 88-VERIFICATION.md is flipped off human_needed (passed, or a follow-up opened) per the 88-HUMAN-GATE-CLOSER.md script, each citation build-version-tagged"
    - "126-VERIFICATION.md H1 + H3 are annotated SUPERSEDED (install minisite retired 2026-06-09; canonical surface mindrian-os.com), not attempted"
    - "The F.7 dial header fork is RESOLVED by a navigator AskUserQuestion decision -- either dial-presenter renders the declared tri-context header OR SKILL.md is amended to bless the shipped header; never pre-resolved by the planner"
  artifacts:
    - path: ".planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md"
      provides: "The SEED-023 precondition verdict consumed by Phase 150.8 plan 01"
      contains: "segment"
    - path: ".planning/phases/88-feynman-minto-memory-layer/88-VERIFICATION.md"
      provides: "Closed (or follow-up-opened) Phase 88 human gate"
      contains: "build"
    - path: ".planning/phases/126-install-lifecycle-harness-gaps/126-VERIFICATION.md"
      provides: "H1/H3 superseded annotation"
      contains: "SUPERSEDED"
  key_links:
    - from: "150.6-GATE-0-CASCADE-VERDICT.md"
      to: "Phase 150.8 plan 01 (type guard decision)"
      via: "verdict consumed as the SEED-023 precondition"
      pattern: "segment"
---

<objective>
Execute the gate closures that gate downstream phases: GATE-0 (a read-only segment-level-cascade-on-meetings audit producing a written verdict the Phase 150.8 plan 01 consumes as its SEED-023 precondition), the Phase 88 human-gate closer (run the staged 88-HUMAN-GATE-CLOSER.md script, flip 88-VERIFICATION off human_needed), the 126-H1/H3 stale-gate re-scope (annotate SUPERSEDED -- the install-minisite machinery retired 2026-06-09), and FIX-09 (the F.7 dial-header fork, surfaced as a navigator AskUserQuestion decision, NOT pre-resolved).

Purpose: SEED-023 / Phase 150.8 gets its precondition verdict; two human_needed phases stop blocking the v1.13.1 gate where evidence already exists or the gate is stale; the F.7 header contradiction is resolved by the navigator, the only authority that can.
Output: one verdict doc, two flipped/annotated VERIFICATION files, and ONE of {dial-presenter header change} OR {SKILL.md canon-amendment} per the navigator's choice.

NOTE: this plan is NOT autonomous. It contains a blocking AskUserQuestion checkpoint (FIX-09) and a human-action checkpoint (the Phase 88 two-session live run). It runs in wave 2 (after Plan 01, because the FIX-09 SKILL.md / dial-presenter reconciliation must land against the FIX-01 honest-claim prose already in place; and gate verdicts should be written against the fixed build).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-CONTEXT.md
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-PATTERNS.md
@.planning/phases/88-feynman-minto-memory-layer/88-HUMAN-GATE-CLOSER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-0 -- segment-level cascade-on-meetings audit (read-only) + write the verdict doc</name>
  <files>.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md</files>
  <read_first>
    - lib/core/intelligence-cascade.cjs around line 196 (the meetings/ routing -- routes meetings/ at FILE level; the audit question is whether cascade fires per-SEGMENT on meeting artifacts E2E)
    - lib/core/intelligence-cascade.cjs the sectionForPath / KNOWN_SECTIONS region (lines ~190-205, the meetings/ branch)
    - test/fixtures/cascade-surface-e2e/ (the doctor class D --verify-surface harness -- the production-shaped cascade input analog)
    - scripts/doctor.cjs class D (--verify-surface) for the harness invocation idiom
    - 150.6-PATTERNS.md "GATE-0" section (verification, not construction; findings file to the phase dir, NOT to lib/)
    - .planning/phases/150.8-meeting-micro-knowledge-dikw-filing-v1-any-transcript-files-/150.8-CONTEXT.md (Section 10 -- HOW 150.8 plan 01 consumes this verdict / the type-guard rule)
  </read_first>
  <action>
    Read-only audit. Do NOT modify lib/. Determine, with file:line + E2E evidence, whether intelligence-cascade fires at SEGMENT granularity on meeting artifacts, or only at FILE granularity (the CONTEXT note that intelligence-cascade.cjs:196 routes meetings/ at file level).

    Procedure:
    1. Trace the cascade entry path for a meeting artifact: where a filed meetings/ file enters the cascade, whether segmentation happens before or after the cross-relationship scan, and whether each segment (not just the whole file) gets a cascade pass.
    2. Drive a production-shaped input through the class D --verify-surface harness (or the test/fixtures/cascade-surface-e2e/ fixture) with a multi-segment meeting artifact; observe whether multiple segment-level cascade events fire or a single file-level event.
    3. Write the verdict to .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md with: (a) the question, (b) the file:line evidence, (c) the E2E observation, (d) a clear VERDICT -- one of "SEGMENT-LEVEL (cascade fires per segment E2E)" or "FILE-LEVEL ONLY (segment granularity NOT present; Phase 150.8 plan 01 must fix the type guard first per 150.8 Section 10)", and (e) the build-version tag of the observation.

    The verdict doc is the deliverable; it is consumed by Phase 150.8 plan 01. Use hyphens, never em-dashes.
  </action>
  <verify>
    <automated>test -f .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md && grep -qiE "SEGMENT-LEVEL|FILE-LEVEL ONLY" .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md && grep -qi "segment" .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md && ! grep -qP '\x{2014}' .planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 150.6-GATE-0-CASCADE-VERDICT.md exists in the phase dir
    - It contains a single explicit VERDICT line matching "SEGMENT-LEVEL" or "FILE-LEVEL ONLY"
    - It cites lib/core/intelligence-cascade.cjs with a line number as evidence
    - It records an E2E observation (the class D / cascade-surface-e2e harness run) with a build-version tag
    - It states the consequence for Phase 150.8 plan 01 (proceed vs fix-type-guard-first)
    - lib/ is unmodified by this task: `git diff --quiet lib/`
    - Zero em-dashes
  </acceptance_criteria>
  <done>A build-version-tagged GATE-0 verdict states segment-vs-file cascade granularity with file:line + E2E evidence and the 150.8 consequence; no lib/ changes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 (CHECKPOINT): Phase 88 two-session live run + quiet-env timing run</name>
  <what-built>The Phase 88 memory layer is shipped and live-proven cross-session in beta.34 room evidence; what remains per 88-HUMAN-GATE-CLOSER.md is the scripted defer->reference two-session acceptance sequence (88-H1) and the quiet-env timing run (88-H2). The closer script is staged; an executor cannot run two sequential live Claude sessions or a quiet-env timing benchmark autonomously.</what-built>
  <how-to-verify>
    Follow 88-HUMAN-GATE-CLOSER.md verbatim:
    1. 88-H1: SESSION A -- in a live claude session against a real room (mindrianOS dog-food room or a scratch room in ~/MindrianRooms), raise a decision and DEFER it (Decision Gate verb 9 or "let's defer this until next time"). Confirm the on-stop hook wrote the defer record (check .room-graph after session end). SESSION B (new session, same room, cold start) -- PASS if Larry references the deferred item unprompted in the greeting/first turns or via the tension/defer resurface path. Capture both transcript excerpts; file them as a room artifact (sub-rooms/qa/ or product-evolution/), build-version tagged.
    2. 88-H2: quiet env (native Linux fs, no /mnt/c, no parallel load) -- run the Phase 88 memory snapshot suite (node lib/memory/debouncer-drain-at-prompt.test.cjs) 10x; record the test-5 distribution. p95 < 1500ms quiet -> environmental flake (annotate test with WSL2 caveat, close). p95 >= 1500ms -> real regression (open a debug session instead of closing).
    3. Report both verdicts + the build version observed.
  </how-to-verify>
  <resume-signal>Paste the two-session transcript excerpts (or the room-artifact path) + the test-5 p95 distribution, and state the verdict for each (88-H1 pass/fail, 88-H2 flake/regression). Then Task 3 records the flip.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip 88-VERIFICATION + annotate 126-H1/H3 SUPERSEDED</name>
  <files>.planning/phases/88-feynman-minto-memory-layer/88-VERIFICATION.md, .planning/phases/126-install-lifecycle-harness-gaps/126-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/88-feynman-minto-memory-layer/88-VERIFICATION.md frontmatter (status: human_needed + the two human_verification items)
    - .planning/phases/126-install-lifecycle-harness-gaps/126-VERIFICATION.md frontmatter (status: human_needed + the three human_verification items H1/H2/H3) + the Observable Truths table row 3 (the "VERIFIED (automated); HUMAN NEEDED (live end-to-end)" cell)
    - .planning/phases/117-.../117-VERIFICATION.md line 104 (the "VERIFIED (superseded)" annotation idiom -- keep the original gate text, change the verdict cell to SUPERSEDED, put WHY + superseding authority in the evidence cell)
    - the Task-2 verdicts (88-H1 + 88-H2 outcomes + build version)
    - feedback memory hard rule: install minisite retired 2026-06-09; canonical surface = mindrian-os.com; release.sh NO_MINISITE defaults 1; Step 9.6b repointed
  </read_first>
  <action>
    (a) Phase 88: per Task-2 verdicts, update 88-VERIFICATION.md frontmatter status from human_needed to passed (if both 88-H1 passed and 88-H2 is an environmental flake) OR keep human_needed scoped to the one open follow-up (if 88-H2 is a real regression -> note the opened debug session). For each closed item, cite 88-HUMAN-GATE-CLOSER.md + the room-artifact path + the build version observed (per the harvest rule: every citation carries the build version). If 88-H2 closed as a flake, annotate the timing test with the WSL2 caveat reference.

    (b) Phase 126: annotate 126-VERIFICATION.md H1 and H3 as SUPERSEDED using the 117-VERIFICATION.md:104 idiom. In the frontmatter human_verification items for H1 (the beta.15 Step 9.6 minisite-lockstep release) and H3 (the mindrianos-install-site.vercel.app live-poll): add a superseded annotation (date 2026-06-11 + reason: install minisite retired 2026-06-09, canonical surface mindrian-os.com, release.sh NO_MINISITE defaults 1, Step 9.6b repointed) rather than deleting them. In the Observable Truths table row that asserts the minisite lockstep (row 3), change the verdict cell to SUPERSEDED and put the WHY + superseding authority in the evidence cell, keeping the original gate text. H2 (tester upgrade migration smoke) is NOT superseded -- leave it; if it is now the only live item, status stays human_needed scoped to H2 only (its closure rides the FIX-07 tester round, out of this phase's scope).

    Use hyphens, never em-dashes.
  </action>
  <verify>
    <automated>grep -qi "SUPERSEDED" .planning/phases/126-install-lifecycle-harness-gaps/126-VERIFICATION.md && grep -qiE "passed|follow-up|debug session" .planning/phases/88-feynman-minto-memory-layer/88-VERIFICATION.md && grep -qiE "mindrian-os.com|minisite retired|2026-06-09" .planning/phases/126-install-lifecycle-harness-gaps/126-VERIFICATION.md && ! grep -qP '\x{2014}' .planning/phases/88-feynman-minto-memory-layer/88-VERIFICATION.md .planning/phases/126-install-lifecycle-harness-gaps/126-VERIFICATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 88-VERIFICATION.md status reflects the Task-2 verdict (passed, or human_needed scoped to an opened follow-up); each closed item cites 88-HUMAN-GATE-CLOSER.md + room-artifact path + build version
    - 126-VERIFICATION.md H1 and H3 frontmatter items carry a SUPERSEDED annotation (2026-06-11 + the minisite-retirement reason); they are NOT deleted
    - 126-VERIFICATION.md Observable Truths minisite-lockstep row verdict cell reads SUPERSEDED with the WHY + superseding authority in the evidence cell; original gate text preserved
    - 126-VERIFICATION.md H2 is left intact (not superseded)
    - Zero em-dashes in either file
  </acceptance_criteria>
  <done>Phase 88 gate flipped per the live verdict (build-version-cited); 126-H1/H3 annotated SUPERSEDED via the 117 idiom; H2 untouched; zero em-dashes.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4a (CHECKPOINT): FIX-09 -- the F.7 dial-header fork (navigator decision, NOT pre-resolved)</name>
  <decision>The F.7 dial header drifted: Canon declares a tri-context header "[right-triangle-filled] Choose next reach:" (skills/ui-system/SKILL.md:257); the shipped renderer emits "Larry can reach for:" (lib/hmi/dial-presenter.cjs:221,251,258). One must move to the other. The planner has NO authority to pick; this is a canon-vs-code fork the navigator resolves.</decision>
  <context>
    Both sides are verified on disk. Option A is a render-contract change (touch three dial-presenter literals); it re-runs the 150.5 dial render tests + run-all-150.sh. Option B is a canon-amendment-on-itself (Part 6): amend SKILL.md to bless the shipped header, recording the decision in MINDRIAN-CANON.md Appendix D in the entries 14/15 voice. This mirrors the D-09 / entry-15 mechanism. The frozen contracts (MAX_K=3, 0.70/0.15, DIAL_REACH_K=6, the 6-reach bank) are NOT touched by either option.
  </context>
  <options>
    <option id="option-a">
      <name>Render the declared tri-context header (change the code)</name>
      <pros>Code conforms to canon as-written; no canon text changes; the tri-context "Choose next reach:" header is the originally-designed UX.</pros>
      <cons>Render-contract change: must re-run the 150.5 dial render tests + run-all-150.sh; risk of perturbing the atomic card trailer contract (intent-classifier.cjs); three literals to change in dial-presenter.cjs.</cons>
    </option>
    <option id="option-b">
      <name>Bless the shipped header (amend the canon)</name>
      <pros>Zero code risk; the shipped "Larry can reach for:" header stays; uses the established canon-amendment-on-itself mechanism (Part 6, entries 14/15); fast.</pros>
      <cons>Requires a SKILL.md edit + an Appendix D entry; the tri-context framing in the canon is softened to match shipped reality; canon version may bump (navigator decides if it is a doctrine change or a figures-class correction).</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (render the declared header in dial-presenter.cjs) or option-b (amend SKILL.md + Appendix D to bless "Larry can reach for:").</resume-signal>
</task>

<task type="auto">
  <name>Task 4b: Execute the navigator's FIX-09 choice</name>
  <files>lib/hmi/dial-presenter.cjs, skills/ui-system/SKILL.md, docs/MINDRIAN-CANON.md</files>
  <read_first>
    - lib/hmi/dial-presenter.cjs lines 163, 221, 251, 258 (the four "Larry can reach for:" literals -- one in a doc comment, three in render paths)
    - skills/ui-system/SKILL.md lines 253-258 (the declared tri-context header + "Choose next reach:")
    - docs/MINDRIAN-CANON.md Appendix D entries 14 + 15 (the canon-amendment-on-itself voice for recording the decision, who locked it, and the mechanism)
    - tests/run-all-150.sh + the 150.5 dial render test names (the regression fence for Option A)
    - the Task-4a navigator selection
  </read_first>
  <action>
    Execute ONLY the option the navigator selected in Task 4a. Do NOT do both.

    If OPTION A (render the declared header): change the render-path literals in lib/hmi/dial-presenter.cjs (lines 221, 251, 258 and the doc-comment at 163) from "Larry can reach for:" to the declared tri-context header per skills/ui-system/SKILL.md:257 ("Choose next reach:" with the filled-right-triangle glyph). Do NOT change MAX_K, the 0.70/0.15 gate, DIAL_REACH_K, or the reach bank. Re-run the 150.5 dial render tests + run-all-150.sh; if the atomic card trailer assertions reference the header string, update those assertions to the new header in the same change.

    If OPTION B (bless the shipped header): edit skills/ui-system/SKILL.md (the lines 253-258 region) so the canon-side header text matches the shipped "Larry can reach for:" (record that the shipped header is the blessed form). Append a MINDRIAN-CANON.md Appendix D entry in the entries 14/15 voice recording: the F.7 header decision, that it was a navigator-locked decision (date 2026-06-11), the canon-amendment-on-itself mechanism (Part 6), and that frozen contracts are unchanged. Bump the canon Version line ONLY if the navigator deemed it a doctrine change; otherwise keep 1.6 and say "presentation reconciliation, no doctrine change."

    Use hyphens, never em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-150.sh && bash tests/run-all-148.sh && ! grep -qP '\x{2014}' lib/hmi/dial-presenter.cjs skills/ui-system/SKILL.md docs/MINDRIAN-CANON.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - EXACTLY ONE of the two options is executed (not both): either dial-presenter.cjs render literals changed to the declared header AND SKILL.md/Canon unchanged for the header text, OR SKILL.md header text matches the shipped "Larry can reach for:" + a new Appendix D entry exists AND dial-presenter.cjs is unchanged
    - The canon and the code now AGREE on the F.7 header string (no remaining drift between SKILL.md:253-258 and dial-presenter.cjs render paths)
    - Frozen contracts unchanged: `bash tests/run-all-148.sh` exits 0 (MAX_K=3 / 0.70/0.15 / DIAL_REACH_K=6 / 6-reach bank intact)
    - `bash tests/run-all-150.sh` green (dial render + atomic card trailer assertions pass against the chosen header)
    - If Option B: docs/MINDRIAN-CANON.md has a new Appendix D entry recording the navigator-locked F.7 decision in the entries 14/15 voice
    - Zero em-dashes in all three files
  </acceptance_criteria>
  <done>The navigator's FIX-09 choice is executed; canon and code agree on the F.7 header; frozen contracts intact; 148 + 150 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meeting artifact -> cascade engine | GATE-0 only READS this boundary (read-only audit). The verdict informs whether Phase 150.8 may file segment-level meeting knowledge; mis-stating it could let 150.8 build on a false precondition. |
| canon doctrine -> shipped code | FIX-09 reconciles a declared-vs-shipped header. Either side moves; the risk is silent doctrine softening (Option B) or a render-contract regression (Option A). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.6.04-REP | Repudiation | GATE-0 verdict + 88/126 gate flips | mitigate | Every verdict + flip carries a build-version tag and a cited evidence path (88-HUMAN-GATE-CLOSER.md, room artifact, 117 idiom). No silent status change; the harvest rule (build-version on every citation) is enforced as an acceptance criterion. |
| T-150.6.04-TAMP | Tampering | dial-presenter render contract (Option A) | mitigate | run-all-150.sh + the 150.5 dial render tests are the regression fence; the atomic card trailer assertions are updated in the same change if they pin the header string. Frozen contracts re-asserted by run-all-148.sh. |
| T-150.6.04-INFO | Information disclosure | GATE-0 read-only audit | accept | Audit reads cascade routing + a fixture meeting artifact; no user-content egress, no Brain call, no write to lib/. |
</threat_model>

<verification>
- GATE-0 verdict doc exists with an explicit SEGMENT-LEVEL / FILE-LEVEL ONLY verdict + file:line + build-version tag; `git diff --quiet lib/` for Task 1.
- 88-VERIFICATION flipped per the live verdict; 126-H1/H3 annotated SUPERSEDED via the 117 idiom; H2 intact.
- FIX-09: canon and code agree on the F.7 header; exactly one option executed.
- Regression fences: `bash tests/run-all-148.sh` 18/18 (frozen contracts), `bash tests/run-all-150.sh` green.
- Zero em-dashes across all edited files.
</verification>

<success_criteria>
- Phase 150.8 has its GATE-0 precondition verdict in hand.
- Phase 88 + 126 stop blocking the v1.13.1 gate (closed or correctly re-scoped).
- The F.7 header drift is resolved by the navigator; canon and code agree.
- Frozen contracts and the 148/150 suites stay green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

| Symbol | Kind | Surface |
|--------|------|---------|
| 150.6-GATE-0-CASCADE-VERDICT.md | NEW FILE (verdict doc, consumed by Phase 150.8 plan 01) | .planning/phases/150.6-.../ |
| 88-VERIFICATION status flip | changed frontmatter + citations | .planning/phases/88-feynman-minto-memory-layer/88-VERIFICATION.md |
| 126-H1/H3 SUPERSEDED annotations | changed frontmatter + table cell | .planning/phases/126-install-lifecycle-harness-gaps/126-VERIFICATION.md |
| F.7 header reconciliation (ONE of) | changed literals OR changed canon prose | lib/hmi/dial-presenter.cjs OR skills/ui-system/SKILL.md + docs/MINDRIAN-CANON.md Appendix D |

The GATE-0 verdict doc path (.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-GATE-0-CASCADE-VERDICT.md) is the load-bearing cross-phase artifact: Phase 150.8 plan 01 reads it as the SEED-023 precondition.
</artifacts_produced>

<output>
Create `.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-04-SUMMARY.md` when done.
</output>
