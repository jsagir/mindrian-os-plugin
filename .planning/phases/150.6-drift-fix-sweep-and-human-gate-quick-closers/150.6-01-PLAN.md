---
phase: 150.6-drift-fix-sweep-and-human-gate-quick-closers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/larry-extended.md
  - skills/larry-personality/SKILL.md
  - commands/doctor.md
  - commands/memory.md
  - commands/room.md
  - commands/jtbd.md
  - commands/update.md
autonomous: true
requirements: [DRIFT-01, DRIFT-10]
must_haves:
  truths:
    - "Larry's agent + skill prose claims 6 reach-ids, 8 sensors (SENS-01..08), and Phase 144 SHIPPED -- never 5 reach-ids, 7 sensors, or 144 executing"
    - "doctor.md and memory.md each declare a Zone 4 (Action Footer) section"
    - "memory.md help_jtbd no longer implies the Brain holds session data"
    - "No em-dash (U+2014) survives in any edited command file"
    - "scripts/doctor.cjs --ui-compliance reports zero new violations on the edited command files"
  artifacts:
    - path: "agents/larry-extended.md"
      provides: "Honest reach-machinery prose (body line 42 region only; frontmatter byte-unchanged)"
      contains: "6 reach-ids"
    - path: "skills/larry-personality/SKILL.md"
      provides: "Honest sensor/engine-flip prose (SENS-01..08, 144 shipped)"
      contains: "SENS-01..08"
    - path: "commands/doctor.md"
      provides: "Zone 4 Action Footer + em-dash-free frontmatter"
      contains: "Zone 4 (Action Footer)"
    - path: "commands/memory.md"
      provides: "Zone 4 Action Footer + Part-8-correct help_jtbd"
      contains: "Zone 4 (Action Footer)"
  key_links:
    - from: "skills/larry-personality/SKILL.md"
      to: "lib/core/insight-sensors.cjs SENSOR_REGISTRY"
      via: "prose cites the shipped 8-sensor registry"
      pattern: "SENS-01\\.\\.08"
---

<objective>
Execute the prose-truth half of the 150.6 drift sweep that touches NO code and NO Brain numbers: FIX-01 (Larry's stale reach/sensor/engine-flip prose in the agent + skill) and FIX-10 (TUI minors -- Action Footer templates, glyph hygiene, the memory.md M8 Part-8 help line). These surfaces load every session or render in /mos:help, so the drift is user-visible and runtime-behavioral (Larry under-claims his own shipped engine).

Purpose: Larry stops lying about his own machinery; the help surface stops contradicting Canon Part 8; command files honor the sanctioned-glyph + no-em-dash hard rules.
Output: 7 edited markdown surfaces, all passing scripts/doctor.cjs --ui-compliance and the no-em-dash gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-CONTEXT.md
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-01 -- rewrite Larry's reach/sensor/engine-flip prose to shipped facts</name>
  <files>agents/larry-extended.md, skills/larry-personality/SKILL.md</files>
  <read_first>
    - agents/larry-extended.md (read the "## Operating the machinery" section around line 42 -- the stale claim)
    - skills/larry-personality/SKILL.md lines 108-135 (the "Scope", "Operating the Dial", and "Reading routing_source" sections -- the stale claims at 113, 119, 129)
    - 150.6-PATTERNS.md "FIX-01" section (the ground-truth file:line citations and exact stale-claim list)
    - lib/hmi/dial-reach-orchestrator.cjs lines 74-83 + line 56 (REACH_DEFS = 6 entries; DIAL_REACH_K = 6) -- confirm before writing
    - lib/core/insight-sensors.cjs lines 351-362 (SENSOR_REGISTRY = 8 entries SENS-01..08) -- confirm before writing
  </read_first>
  <action>
    Rewrite the stale prose in the agent body and the skill body to match shipped code. Do NOT touch agents/larry-extended.md frontmatter (FROZEN per Phase 143.2 -- edit only the body prose).

    In agents/larry-extended.md, the "## Operating the machinery" paragraph: replace "5 reach-ids are LIVE (Phase 141 getRoomContext), 7 insight sensors are LIVE (Phase 143), and the dial-TUI capability selector is LIVE (Phase 143.1, Shape F.7); the engine flip that auto-fires the dial is executing in Phase 144 (skill-activation-router.cjs Precedence Rule 1)" with prose asserting: 6 reach-ids are LIVE (Phase 141 getRoomContext + Phase 148 minted hats as the 6th), 8 insight sensors are LIVE (Phase 143: SENS-01..08), the dial-TUI capability selector is LIVE (Phase 143.1, Shape F.7), and the engine flip that auto-fires the dial SHIPPED (Phase 144: lib/core/navigation-engine.cjs decide() flips routing_source legacy to engine on a fired reach). Keep the rest of the paragraph (the "you DRIVE these surfaces" framing) intact.

    In skills/larry-personality/SKILL.md "### Scope" section (line 113 region): replace "SENS-01..07 + SENSOR_REGISTRY" with "SENS-01..08 + SENSOR_REGISTRY"; replace "Phase 144 wires dispatchSensors INTO decide() -- the engine flip -- and is executing now ... Until 144 lands the engine does not route live -- the sensors and dial are shipped, the routing flip is the remaining step" with shipped-tense prose: "Phase 144 wired dispatchSensors INTO decide() -- the engine flip -- and SHIPPED (lib/core/navigation-engine.cjs; tests/run-all-144.sh 5/5). decide() populates fire_skill from real sensor reaches and the router returns source=engine. The engine routes live." Keep the file:line-citing honest-claim voice.

    In SKILL.md "### Operating the Dial" behavior 1 (line 119 region): replace "The seven sensors (SENS-01..07)" with "The eight sensors (SENS-01..08)". Leave the already-correct "frozen six reach-ids (context_block, contradiction, cross_room, brain_consult, deep_research, hats)" UNCHANGED.

    In SKILL.md "### Reading routing_source" (line 129 region): replace "Honesty floor (Reach rule 7): until Phase 144 lands, routing_source stays legacy; the engine routes once 144 lands, not now." with shipped-tense: "Phase 144 SHIPPED: when the engine populates fire_skill, routing_source flips legacy to engine; the engine routes live now." Keep the Part 8 floor sentence that follows it UNCHANGED.

    Use hyphens, never em-dashes. Cite phase + file path in the honest-claim voice already modeled in the file.
  </action>
  <verify>
    <automated>grep -c "6 reach-ids" agents/larry-extended.md | grep -qv '^0$' && grep -c "5 reach-ids" agents/larry-extended.md | grep -q '^0$' && grep -c "SENS-01..08" skills/larry-personality/SKILL.md | grep -qv '^0$' && grep -c "SENS-01..07" skills/larry-personality/SKILL.md | grep -q '^0$' && ! grep -qP '\x{2014}' agents/larry-extended.md skills/larry-personality/SKILL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - agents/larry-extended.md contains "6 reach-ids" and does NOT contain "5 reach-ids"
    - agents/larry-extended.md contains "Phase 144" with "SHIPPED" (not "executing in Phase 144")
    - skills/larry-personality/SKILL.md contains "SENS-01..08" and does NOT contain "SENS-01..07"
    - skills/larry-personality/SKILL.md contains "eight sensors" and does NOT contain "seven sensors (SENS-01..07)"
    - skills/larry-personality/SKILL.md does NOT contain "until Phase 144 lands" or "is executing now"
    - skills/larry-personality/SKILL.md still contains "frozen six reach-ids (context_block, contradiction, cross_room, brain_consult, deep_research, hats)" (the already-correct line, unchanged)
    - agents/larry-extended.md frontmatter (everything between the opening and closing `---`) is byte-identical to before this task
    - `grep -P '\x{2014}' agents/larry-extended.md skills/larry-personality/SKILL.md` returns no matches (zero em-dashes)
  </acceptance_criteria>
  <done>Larry's agent + skill prose asserts 6 reach-ids, 8 sensors (SENS-01..08), Phase 144 SHIPPED, engine routes live; the frozen larry-extended frontmatter is unchanged; zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 2: FIX-10 -- Action Footer templates + glyph hygiene + memory.md M8 help line</name>
  <files>commands/doctor.md, commands/memory.md, commands/room.md, commands/jtbd.md, commands/update.md</files>
  <read_first>
    - commands/graph.md lines 125-130 (the "## Zone 4 (Action Footer)" template to copy)
    - commands/doctor.md (the frontmatter em-dashes at line 3 + body; the missing Zone 4)
    - commands/memory.md (help_jtbd line 4 -- the M8 Part-8 contradiction; body line ~88 Part-8 section; line ~77 the `§` reference; the missing Zone 4)
    - commands/room.md (the box-drawing `╭ ╮ ──` at lines ~86/165 and `✗` at lines ~150/230)
    - 150.6-PATTERNS.md "FIX-10" section (the glyph inventory + sanctioned 12-glyph vocabulary + the exact footer targets)
    - commands/update.md (the sanctioned-glyph rule line, to confirm the vocabulary)
  </read_first>
  <action>
    Four edits, each acceptance-gated by scripts/doctor.cjs --ui-compliance (run it before and after).

    (a) Action Footer for doctor.md: add a "## Zone 4 (Action Footer)" section near the end, copying the graph.md:125-130 shape. Point at the natural next moves: "> Want to repair what we found? -> /mos:doctor --fix" and "> Want the full picture? -> /mos:status". Use the sanctioned `->` arrow glyph (written as the two-char arrow per the file's existing convention), never an em-dash.

    (b) Action Footer for memory.md: add the same "## Zone 4 (Action Footer)" section pointing at "> Want to pick up where you left off? -> /mos:memory resume" and "> Want what other rooms learned? -> /mos:memory cross-room".

    (c) memory.md M8 help line: rewrite the frontmatter help_jtbd from "Inspect what the Brain remembers about your sessions." to "Inspect what the room remembers about your sessions." (the Brain NEVER holds session data per Canon Part 8; the room does -- matches the file's own body framing of within-session / across-session / cross-room locally-stored memory). This single line renders in /mos:help.

    (d) Glyph hygiene:
      - doctor.md: replace every em-dash (U+2014) with a double hyphen `--`, including the frontmatter description at line 3 and any body lines. Leave the sanctioned `->` and any `[before -> after]` arrows alone.
      - room.md: replace the box-drawing `╭ ╮ ── ` decoration with sanctioned tree glyphs (`├─` / `└─`) or plain text per the surrounding render; replace the `✗` error glyph with the literal `x` error idiom (graph.md:32 convention). Verify against the sanctioned 12-glyph vocabulary: `■ ▼ ▶ ▷ ├─ └─ ✓ • ⚠ ⚡ ⬜ ->`.
      - memory.md: leave the internal `§` doc-reference at line ~77 (low priority, internal reference) UNLESS --ui-compliance flags it; if flagged, replace "§8" with "section 8".
      - jtbd.md + update.md: confirmed clean in the audit scan; touch only if --ui-compliance flags them.

    Run `node scripts/doctor.cjs --ui-compliance --scan-commands=./commands` after edits and confirm it reports no violations on these five files.
  </action>
  <verify>
    <automated>node scripts/doctor.cjs --ui-compliance --scan-commands=./commands >/tmp/uic.txt 2>&1; grep -qi "doctor.md\|memory.md\|room.md\|jtbd.md\|update.md" /tmp/uic.txt && { echo "violations remain"; cat /tmp/uic.txt; exit 1; } || echo "UI-COMPLIANCE CLEAN"; grep -c "Zone 4 (Action Footer)" commands/doctor.md | grep -qv '^0$' && grep -c "Zone 4 (Action Footer)" commands/memory.md | grep -qv '^0$' && grep -q "room remembers about your sessions" commands/memory.md && ! grep -qP '\x{2014}' commands/doctor.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - commands/doctor.md contains "## Zone 4 (Action Footer)"
    - commands/memory.md contains "## Zone 4 (Action Footer)"
    - commands/memory.md help_jtbd reads "Inspect what the room remembers about your sessions." and does NOT contain "what the Brain remembers about your sessions"
    - `grep -P '\x{2014}' commands/doctor.md` returns no matches (zero em-dashes; including line 3 frontmatter)
    - commands/room.md contains no `✗` and no `╭` and no `╮`
    - `node scripts/doctor.cjs --ui-compliance --scan-commands=./commands` output names none of doctor.md / memory.md / room.md / jtbd.md / update.md as a violation
  </acceptance_criteria>
  <done>doctor.md + memory.md declare Zone 4; memory.md M8 help line is Part-8-correct; all five command files pass --ui-compliance with zero em-dashes and only sanctioned glyphs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure prose/markdown edits. No new input crosses any trust boundary; no code path, no Brain query, no event emission added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.6.01-01 | Information disclosure | memory.md help_jtbd prose | mitigate | The fix REMOVES a Part-8-contradicting claim ("Brain remembers your sessions"); the corrected line asserts local-room memory only. Net Part-8 posture improves. |
| T-150.6.01-02 | Tampering | larry-extended frontmatter | mitigate | Frozen frontmatter (Phase 143.2) protected by acceptance assertion (byte-identical check); edits confined to body prose. |
</threat_model>

<verification>
- `node scripts/doctor.cjs --ui-compliance --scan-commands=./commands` reports zero violations on the five edited command files.
- `grep -rP '\x{2014}' agents/larry-extended.md skills/larry-personality/SKILL.md commands/doctor.md commands/memory.md commands/room.md commands/jtbd.md commands/update.md` returns no matches.
- Regression fence: `bash tests/run-all-148.sh` exits 0 (frozen reach contracts intact -- prose edits must not perturb the 148 suite). `bash tests/run-all-150.sh` green.
</verification>

<success_criteria>
- Larry's runtime self-knowledge (agent + skill) reflects 6 reach-ids / 8 sensors / Phase 144 SHIPPED.
- doctor.md + memory.md declare Action Footers; memory.md help line honors Part 8.
- Every edited file passes --ui-compliance and the no-em-dash gate.
- Frozen larry-extended frontmatter unchanged; 148 + 150 suites green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

| Symbol | Kind | Surface |
|--------|------|---------|
| "6 reach-ids" / "SENS-01..08" / "Phase 144 SHIPPED" prose | changed strings | agents/larry-extended.md (body), skills/larry-personality/SKILL.md |
| "## Zone 4 (Action Footer)" section | new section | commands/doctor.md, commands/memory.md |
| help_jtbd "Inspect what the room remembers about your sessions." | changed string | commands/memory.md |
| glyph + em-dash cleanup | changed strings | commands/doctor.md, commands/room.md |

No new files, no new constants, no GATE-0 verdict (that is Plan 04).
</artifacts_produced>

<output>
Create `.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-01-SUMMARY.md` when done.
</output>
