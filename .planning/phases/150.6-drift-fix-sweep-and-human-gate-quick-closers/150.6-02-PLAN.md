---
phase: 150.6-drift-fix-sweep-and-human-gate-quick-closers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/INSTALL-LIFECYCLE-HARNESS.md
  - docs/CANON-PHASE-MAP.md
  - docs/THE-BRAIN.md
  - docs/brain-setup.md
  - CLAUDE.md
  - docs/MINDRIAN-CANON.md
  - .claude/includes/release-process.md
autonomous: false
requirements: [DRIFT-02, DRIFT-03, DRIFT-06]
user_setup:
  - service: npm
    why: "Deprecate the dead @mindrian_os/install package so npx users are redirected to @mindrian_os/cli"
    env_vars: []
    dashboard_config:
      - task: "Run `npm deprecate @mindrian_os/install \"Renamed to @mindrian_os/cli -- npx @mindrian_os/cli\"` (requires npm publish auth for the @mindrian_os scope)"
        location: "local shell with npm login to the @mindrian_os org"
must_haves:
  truths:
    - "No shipped doc or config string tells a user to run `npx @mindrian_os/install`; the live package @mindrian_os/cli is named instead"
    - "docs/THE-BRAIN.md lists exactly the 6 real Brain MCP tools (brain_query/schema/write/search/stats/ask) and zero fictional tools"
    - "docs/brain-setup.md says Six Brain tools and includes a brain_ask row"
    - "THE-BRAIN.md, brain-setup.md, CLAUDE.md, and MINDRIAN-CANON.md Appendix D agree on ONE Brain number set (the live-read figures)"
    - "CLAUDE.md + .claude/includes/release-process.md reference ~/dev/MindrianOS-Plugin, never the old ~/MindrianOS-Plugin workspace path"
    - "git worktree prune has run; orphan worktree warnings cleared"
  artifacts:
    - path: "package.json"
      provides: "Correct npx package name in description"
      contains: "@mindrian_os/cli"
    - path: "docs/THE-BRAIN.md"
      provides: "Real 6-tool Brain MCP table"
      contains: "brain_ask"
    - path: "docs/brain-setup.md"
      provides: "Six-tool table including brain_ask"
      contains: "brain_ask"
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Appendix D number-reconciliation entry in the entry-13 voice"
      contains: "Appendix D"
  key_links:
    - from: "docs/THE-BRAIN.md tool table"
      to: "mcp-server-brain real tool surface"
      via: "table lists only registered MCP tools"
      pattern: "brain_(query|schema|write|search|stats|ask)"
---

<objective>
Execute the supply-chain + Brain-truth + workspace-path doc sweep: FIX-02 (the dead @mindrian_os/install package reference, a supply-chain surface), FIX-03 (cherry-pick a3c422fe for THE-BRAIN.md qualitative corrections, rewrite the fictional 7-tool table to the real 6, add brain_ask to brain-setup.md, and normalize ALL FOUR Brain-number surfaces to ONE live-read set via the Appendix D entry-13 procedure), and FIX-06 (the stale workspace-path sweep + git worktree prune + pre-commit hook refresh). This is the highest-trust-surface plan in the phase: an npm install string and the production Brain corpus figures.

Purpose: users reach the live package; THE-BRAIN.md stops advertising tools that do not exist; the four Brain-number surfaces stop disagreeing; the anti-drift workspace guard stops pointing at a path that no longer exists.
Output: 8 edited doc/config surfaces normalized to ground truth; a clean worktree list; a refreshed pre-commit hook.

NOTE: this plan is NOT autonomous. Task 2 contains a `checkpoint:human-action` for the live Brain read (the my-neo4j + pinecone MCP read can only be authorized/run by the navigator) before the four-surface number normalization.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-CONTEXT.md
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-PATTERNS.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-02 -- @mindrian_os/install to @mindrian_os/cli sweep + FIX-06 workspace-path sweep + worktree prune + pre-commit refresh</name>
  <files>package.json, docs/INSTALL-LIFECYCLE-HARNESS.md, docs/CANON-PHASE-MAP.md, CLAUDE.md, .claude/includes/release-process.md</files>
  <read_first>
    - package.json lines 1-10 (line 2 name is already "@mindrian_os/cli"; line 4 description still says install)
    - README install line (grep "npx @mindrian_os" README.md -- confirm the canonical idiom: bare `npx @mindrian_os/cli` vs `npx @mindrian_os/cli@latest install`)
    - docs/INSTALL-LIFECYCLE-HARNESS.md line 100 (it INVERTS the rename -- flip the direction)
    - docs/CANON-PHASE-MAP.md (the two "Step 9.5 @mindrian_os/install" rows in the Part 6 + Part 7 Phase 123 entries)
    - CLAUDE.md (the WORKSPACE GUARD header + "THE ONLY DEV WORKSPACE" + related-repo pointers using /home/jsagi/MindrianOS-Plugin)
    - .claude/includes/release-process.md (the "canonical development workspace is /home/jsagi/MindrianOS-Plugin/" + `cd ~/MindrianOS-Plugin` lines)
    - 150.6-PATTERNS.md "FIX-02" + "FIX-06" sections (exact current-state strings + the mechanical sweep command)
  </read_first>
  <action>
    (a) FIX-02 package sweep. In package.json line 4 description: replace `npx @mindrian_os/install` with the live idiom (verify against README; if README uses bare `npx @mindrian_os/cli`, use that; if it uses `npx @mindrian_os/cli@latest install`, mirror it exactly). In docs/INSTALL-LIFECYCLE-HARNESS.md line 100: flip the inverted rename so the prose reads that the live package is `@mindrian_os/cli` and `@mindrian_os/install` is the dead one (abandoned at beta.36). In docs/CANON-PHASE-MAP.md: change both "Step 9.5 @mindrian_os/install" mentions (the Part 6 Phase 123 row and the Part 7 Phase 123 row) to "Step 9.5 @mindrian_os/cli".

    (b) FIX-06 workspace-path sweep. Mechanically replace `/home/jsagi/MindrianOS-Plugin` with `/home/jsagi/dev/MindrianOS-Plugin` and `~/MindrianOS-Plugin` with `~/dev/MindrianOS-Plugin` across CLAUDE.md and .claude/includes/release-process.md ONLY. Do NOT touch `/home/jsagi/mindrianos-install-site` (a different repo). Update the CLAUDE.md WORKSPACE GUARD header "Working directory:" line, the "THE ONLY DEV WORKSPACE" text, and the related-repo pointers. Update release-process.md "The canonical development workspace is ..." and the `cd ~/MindrianOS-Plugin` instruction.

    (c) FIX-06 worktree prune. Run `git worktree prune` then `git worktree list` and record the resulting count in the SUMMARY (the audit found 47 orphans pointing at the old path).

    (d) FIX-06 pre-commit refresh. Find every hook installer: `grep -ln "hooks/pre-commit" scripts/*.sh scripts/*.cjs`. Re-run them (at minimum `bash scripts/install-pre-commit.sh`). Then make a scratch commit on a throwaway change to confirm BOTH the navigation-chokepoint guard (`check-substrate.cjs --diff`) and the schema-alias guard fire; abandon the scratch change after. Record the refreshed hook line count.

    Use hyphens, never em-dashes, in any edited prose.
  </action>
  <verify>
    <automated>! grep -rn "@mindrian_os/install" package.json docs/INSTALL-LIFECYCLE-HARNESS.md docs/CANON-PHASE-MAP.md && grep -q "@mindrian_os/cli" package.json && ! grep -rn "/home/jsagi/MindrianOS-Plugin\|~/MindrianOS-Plugin\b" CLAUDE.md .claude/includes/release-process.md && git worktree list && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "@mindrian_os/install" package.json docs/INSTALL-LIFECYCLE-HARNESS.md docs/CANON-PHASE-MAP.md` returns no matches
    - package.json description contains "@mindrian_os/cli"
    - `grep -rn "/home/jsagi/MindrianOS-Plugin\b" CLAUDE.md .claude/includes/release-process.md` returns no matches (note the word boundary: the new `/home/jsagi/dev/MindrianOS-Plugin` is allowed)
    - `grep -rn "~/MindrianOS-Plugin\b" CLAUDE.md .claude/includes/release-process.md` returns no matches
    - `git worktree list` runs clean after `git worktree prune` (orphan count reduced; record before/after in SUMMARY)
    - the refreshed installed pre-commit hook fires both the substrate-diff guard and the schema-alias guard on a scratch commit (record evidence in SUMMARY)
    - SUMMARY documents the npm-deprecate step as user_setup (navigator must run `npm deprecate @mindrian_os/install ...`)
  </acceptance_criteria>
  <done>Zero shipped `@mindrian_os/install` install strings; CLAUDE.md + release-process.md point at ~/dev/MindrianOS-Plugin; worktrees pruned; pre-commit hook refreshed and verified; npm-deprecate surfaced as user_setup.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2a (CHECKPOINT): live Brain corpus read for the four-surface number normalization</name>
  <what-built>The four Brain-number surfaces (THE-BRAIN.md, brain-setup.md, CLAUDE.md moat block, MINDRIAN-CANON.md Appendix D) currently carry THREE conflicting number sets (per 150.6-PATTERNS.md FIX-03). Two of them both claim "live read" and disagree. The clean play (per the Appendix D entry-13 procedure) is ONE fresh live read of the production substrates, then normalize all four surfaces to it.</what-built>
  <how-to-verify>
    1. Run the live read using the EXACT procedure of MINDRIAN-CANON.md Appendix D entry 13:
       - Neo4j via the `my-neo4j` MCP: total node count + total relationship count (and, if cheap, the Framework / Book / Tool / Technique label counts).
       - Pinecone via the `pinecone` MCP: the `pws-brain` index vector count + dimension + embedding model.
    2. Record the figures here as the canonical set Task 2b will write to all four surfaces.
    3. Confirm: are these figures safe to publish in the (shipped) docs? (They are corpus aggregates, not user data -- Part 8 permits corpus figures. Confirm no user-identifying content is included.)
  </how-to-verify>
  <resume-signal>Paste the live node/relationship/vector figures (and label breakdown if available), or type "use the Appendix-D-entry-13 RATIFIED set" to normalize to the most recent ratified figures (15,298 nodes / 19,713 rels / Pinecone pws-brain 12,401 vectors, 1024-dim multilingual-e5-large) without a fresh read.</resume-signal>
</task>

<task type="auto">
  <name>Task 2b: FIX-03 -- cherry-pick a3c422fe + rewrite THE-BRAIN.md 6-tool table + add brain_ask + normalize four surfaces + Appendix D entry</name>
  <files>docs/THE-BRAIN.md, docs/brain-setup.md, CLAUDE.md, docs/MINDRIAN-CANON.md</files>
  <read_first>
    - 150.6-PATTERNS.md "FIX-03" section (the verified clean cherry-pick of a3c422fe; the real 6-tool surface; the three conflicting number sets; the Appendix-D entry-13 amendment voice)
    - docs/THE-BRAIN.md lines 48-64 (the fictional 7-tool table: enrich_context / suggest_chain / suggest_methodology / grade_room / find_connections / get_chaining_rules / get_teaching_context -- NONE exist)
    - docs/brain-setup.md lines 28-44 (the "Five Brain tools" line at ~30 + the 5-row tool table missing brain_ask + the stale "1,427 PWS knowledge vectors" at ~41 + the "21K+ nodes / 1,427 embeddings" at lines 6-8)
    - docs/MINDRIAN-CANON.md Appendix D entry 13 (the EXACT template for a corpus-figures-corrected entry)
    - CLAUDE.md moat block ("Neo4j 15,298 nodes ... Pinecone 12,401 embeddings" + the Three Layers table + the moat formula section)
    - the figures recorded in Task 2a
  </read_first>
  <action>
    (a) Cherry-pick a3c422fe (NOT a full-branch merge -- the full merge conflicts in mcp-server-brain/lib/brain-ask.cjs). Run `git cherry-pick a3c422fe`. It touches ONLY docs/THE-BRAIN.md (+11/-8) and applies clean (merge-tree simulated 2026-06-11). It brings the qualitative corrections (CO_OCCURS not populated, FEEDS_INTO first moat edge, Tier-0 "curated subset of 748" framing). If the cherry-pick surfaces an unexpected conflict, abort it (`git cherry-pick --abort`) and apply those three qualitative corrections by hand instead; note which path was taken in the SUMMARY.

    (b) THE-BRAIN.md tool table (lines 50-62): replace the fictional 7-tool table with the real 6, keeping the existing `| Tool | Returns | User Experiences |` shape. The six rows are brain_query, brain_schema, brain_write, brain_search, brain_stats, brain_ask (the real surface registered at mcp-server-brain/lib/brain-ask.cjs:515). Do NOT list brain_api_keys or brain_usage_log -- those are Supabase table names, not MCP tools.

    (c) brain-setup.md: change "Five Brain tools will appear" to "Six Brain tools will appear"; add a `brain_ask` row to the tool table (lines 36-43); update the stale numbers (line ~41 "1,427 PWS knowledge vectors" and lines 6-8 "21K+ nodes / 1,427 embeddings") to the Task-2a figures.

    (d) Normalize ALL FOUR surfaces to the SINGLE Task-2a number set in this same commit: THE-BRAIN.md, brain-setup.md, CLAUDE.md (the moat block + the Three Layers "Neo4j N nodes + Pinecone M embeddings" line + the moat "15,298 nodes, 19,713 relationships" / "12,401 embeddings" lines), and MINDRIAN-CANON.md Appendix D.

    (e) Append a new MINDRIAN-CANON.md Appendix D entry in the entry-13 voice ("Corpus figures corrected (2026-06-11). A live read of the production Brain substrates -- Neo4j via the my-neo4j MCP, Pinecone via the pinecone MCP -- normalized the four number surfaces ... Factual correction only -- no change to canon doctrine; version stays 1.6."). Canon version stays 1.6 (a figures correction is not a doctrine change). Add the corresponding row to docs/CANON-PHASE-MAP.md Version history (canon_parts 4/6/7/8 per this phase) in the same commit per the CANON-PHASE-MAP "update this map in the same commit" rule.

    Use hyphens, never em-dashes.
  </action>
  <verify>
    <automated>grep -c "brain_ask" docs/THE-BRAIN.md | grep -qv '^0$' && grep -c "brain_ask" docs/brain-setup.md | grep -qv '^0$' && ! grep -qE "enrich_context|suggest_chain|suggest_methodology|grade_room|get_chaining_rules|get_teaching_context" docs/THE-BRAIN.md && grep -qi "Six Brain tools" docs/brain-setup.md && ! grep -qP '\x{2014}' docs/THE-BRAIN.md docs/brain-setup.md docs/MINDRIAN-CANON.md CLAUDE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - docs/THE-BRAIN.md tool table contains all of: brain_query, brain_schema, brain_write, brain_search, brain_stats, brain_ask
    - docs/THE-BRAIN.md does NOT contain any of: enrich_context, suggest_chain, suggest_methodology, grade_room, get_chaining_rules, get_teaching_context
    - docs/brain-setup.md contains "Six Brain tools" and a "brain_ask" table row and does NOT contain "Five Brain tools"
    - docs/brain-setup.md does NOT contain "1,427" (the stale embedding count)
    - the SAME node count, relationship count, and Pinecone vector count appear in THE-BRAIN.md, brain-setup.md, CLAUDE.md, and MINDRIAN-CANON.md Appendix D (one number set across four surfaces -- the Task-2a figures)
    - docs/MINDRIAN-CANON.md Appendix D has a new "Corpus figures corrected (2026-06-11)" entry in the entry-13 voice; the header `Version:` line still reads 1.6
    - docs/CANON-PHASE-MAP.md Version history has a matching row for this figures correction
    - `grep -P '\x{2014}' docs/THE-BRAIN.md docs/brain-setup.md docs/MINDRIAN-CANON.md CLAUDE.md` returns no matches
  </acceptance_criteria>
  <done>a3c422fe applied; THE-BRAIN.md lists the real 6 tools; brain-setup.md says Six + has brain_ask; all four surfaces carry ONE number set; Appendix D + the phase map record the correction at canon v1.6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> end user | The package.json description string and docs tell users which package to `npx`. A wrong/dead name is a supply-chain misdirection surface (users could be steered to an abandoned/squattable name). |
| production Brain substrate -> shipped docs | Corpus figures are read from the live Neo4j + Pinecone and published in shipped docs. Must be corpus aggregates only, never user-identifying content (Part 8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.6.02-SC | Tampering / Spoofing | npm package reference (@mindrian_os/install) | mitigate | Sweep all shipped references to the live @mindrian_os/cli; user_setup deprecates the dead @mindrian_os/install so an attacker cannot republish it under the abandoned name and intercept `npx` traffic. Per the package-legitimacy gate: @mindrian_os/cli is the project's own first-party package (verified live name at package.json:2), not a third-party install -- no external legitimacy checkpoint required, but the dead-name deprecation closes the squat window. |
| T-150.6.02-INFO | Information disclosure | live Brain read -> shipped docs | mitigate | Task 2a checkpoint confirms only corpus aggregates (counts, dimension, model) are published; no user content, no :Person names, no artifact text. Part 8 permits corpus figures. |
| T-150.6.02-TAMP | Tampering | cherry-pick a3c422fe | accept | Single-commit cherry-pick verified clean by merge-tree simulation (150.6-PATTERNS.md); abort-and-hand-apply fallback documented if conflict surfaces. |
</threat_model>

<verification>
- `grep -rn "@mindrian_os/install" package.json docs/ CLAUDE.md` returns no matches.
- One Brain number set across THE-BRAIN.md / brain-setup.md / CLAUDE.md / MINDRIAN-CANON.md Appendix D.
- `git worktree list` clean post-prune.
- Regression fence: `bash tests/run-all-148.sh` exits 0 and `bash tests/run-all-150.sh` green (doc edits must not perturb frozen contracts).
- The refreshed pre-commit hook fires the substrate + schema-alias guards on a scratch commit.
</verification>

<success_criteria>
- Users reach the live package; zero dead-package install strings ship.
- THE-BRAIN.md is honest about its 6-tool surface; brain-setup.md includes brain_ask.
- Four Brain-number surfaces agree on one live-read set; Appendix D records it at v1.6.
- Workspace guard points at the real path; worktrees pruned; pre-commit refreshed.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

| Symbol | Kind | Surface |
|--------|------|---------|
| "@mindrian_os/cli" install idiom | changed string | package.json:4, docs/INSTALL-LIFECYCLE-HARNESS.md:100, docs/CANON-PHASE-MAP.md (2 rows) |
| real 6-tool Brain MCP table | rewritten table | docs/THE-BRAIN.md |
| brain_ask row + "Six Brain tools" | new row + changed string | docs/brain-setup.md |
| ONE normalized Brain number set | changed numbers | docs/THE-BRAIN.md, docs/brain-setup.md, CLAUDE.md, docs/MINDRIAN-CANON.md |
| Appendix D "Corpus figures corrected (2026-06-11)" entry | new canon entry | docs/MINDRIAN-CANON.md |
| matching Version-history row | new row | docs/CANON-PHASE-MAP.md |
| ~/dev/MindrianOS-Plugin workspace paths | changed strings | CLAUDE.md, .claude/includes/release-process.md |
| pruned worktree list + refreshed pre-commit hook | operational state | git worktree / .git/hooks/pre-commit (untracked) |
| npm-deprecate @mindrian_os/install | user_setup action (navigator-run) | npm registry |
</artifacts_produced>

<output>
Create `.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-02-SUMMARY.md` when done.
</output>
