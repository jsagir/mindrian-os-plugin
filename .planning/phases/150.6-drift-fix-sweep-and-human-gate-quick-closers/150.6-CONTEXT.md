---
phase: 150.6
slug: drift-fix-sweep-and-human-gate-quick-closers
inserted: 2026-06-11
milestone: v1.13.1
ships: next beta after v1.13.1-beta.15
canon_parts: [4, 6, 7, 8]
status: scoped
---

# Phase 150.6 CONTEXT: Drift-Fix Sweep + Human-Gate Quick Closers

## Why this phase exists

The 2026-06-11 full drift audit (Claude Fable 5, 6 subagent tracks; report at `.planning/v1.13.1-DRIFT-AUDIT.md`) found the engine real and wired but the PROSE drifting: agent/skill/doc surfaces lying about shipped code, two dead-package references, stranded Brain numbers, help-surface gaps, one one-line wiring no-op, and stale workspace paths. All evidence is pre-gathered; this phase is execution, not investigation. Doc-heavy, low-risk, gates 150.7 (testers must receive the FIXED build).

## Scope (the Q1 queue, verbatim from the audit Section 8)

1. **FIX-01 (HIGH, runtime-behavioral):** rewrite `agents/larry-extended.md:42` + `skills/larry-personality/SKILL.md:113,119,129` -- 6 reach-ids, 8 sensors (SENS-01..08), Phase 144 SHIPPED (engine live). Larry currently under-claims his own engine every session.
2. **FIX-02:** `@mindrian_os/install` -> `@mindrian_os/cli` sweep: `package.json:4` description, `docs/INSTALL-LIFECYCLE-HARNESS.md:100`, `docs/CANON-PHASE-MAP.md` Phase 123/126 Step 9.5 rows. Consider npm deprecate on the dead package.
3. **FIX-03:** merge/cherry-pick commit `a3c422fe` (workspace/brain-cleanup branch -- THE-BRAIN.md live numbers); rewrite `docs/THE-BRAIN.md:50-62` 7-tool table to the real 6 (brain_query/schema/write/search/stats/ask); add brain_ask to `docs/brain-setup.md`; reconcile CLAUDE.md + MINDRIAN-CANON Appendix D to ONE Brain number set.
4. **FIX-04:** `data/help-groups.json`: remove `visualize` (deprecated), add `discover` + `memory-cortex-reach` (shipped, invisible). Or teach `scripts/help-renderer.cjs` to skip `deprecated: true`.
5. **FIX-05:** emit `venture_classified` from `lib/core/mva-classifier.cjs` into memory_events -- the one-line gap that makes `lib/core/venture-shape-nudge.cjs:152` a permanent no-op (Phase 119 receipt nudge).
6. **FIX-06:** workspace-path sweep: CLAUDE.md + `.claude/includes/release-process.md` say `/home/jsagi/MindrianOS-Plugin`; repo lives at `~/dev/MindrianOS-Plugin`. Plus `git worktree prune` (47 orphans) + refresh installed pre-commit hook from source (234 vs 272 lines).
7. **GATE-0 (SEED-023 precondition):** read-only audit -- confirm segment-level cascade firing on meeting artifacts (`lib/core/intelligence-cascade.cjs:196` routes meetings/ at file level; verify segment granularity E2E).
8. **Phase 88 closer:** execute `.planning/phases/88-feynman-minto-memory-layer/88-HUMAN-GATE-CLOSER.md` (two-session defer->reference + quiet-env timing run); flip 88-VERIFICATION from human_needed.
9. **126 re-scope:** mark 126-H1/H3 superseded in 126-VERIFICATION (install minisite retired 2026-06-09; canonical surface mindrian-os.com).
10. **TUI minors (from the TUI slippage track):** Action Footer templates for `commands/doctor.md` + `commands/memory.md`; strip emoji from 5 command files (memory, jtbd, room, doctor, update); fix `/mos:memory` help_jtbd Brain-implication line (M8). F.7 header decision (FIX-09) needs a navigator call: render the declared tri-context header OR amend SKILL.md to bless the shipped header (canon-amendment-on-itself).

## Out of scope

Tester round (150.7), SEED-022/023 builds (v1.14.0), Q3 process fixes except where free.

## Evidence sources

`.planning/v1.13.1-DRIFT-AUDIT.md` (fix queue + findings), product-claims sweep report (session transcript 2026-06-11), room-side `product-evolution/audits/2026-06-11-human-gate-harvest.md` (via .umbilical).
