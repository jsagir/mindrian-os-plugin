---
phase: 150.6-drift-fix-sweep-and-human-gate-quick-closers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - data/help-groups.json
  - scripts/help-renderer.cjs
  - lib/workflow/selector-decisions.cjs
  - lib/workflow/offer-closer.cjs
  - scripts/intent-classifier.cjs
  - lib/core/venture-shape-nudge.test.cjs
autonomous: true
requirements: [DRIFT-04, DRIFT-05]
must_haves:
  truths:
    - "The deprecated /mos:visualize no longer renders in /mos:help"
    - "The shipped /mos:discover and /mos:memory-cortex-reach appear in /mos:help groups"
    - "A real f_selector_decision emission carries a scalar boolean venture_classified + a classification_source enum"
    - "venture-shape-nudge.shouldSurfaceNudge returns a turn_count > 0 (not skip_reason: venture_classification_unavailable) when production emissions carry the field"
    - "Only the scalar boolean + source enum reach the event payload -- never the user sentence or any substring of it (Canon Part 8)"
  artifacts:
    - path: "data/help-groups.json"
      provides: "discover + memory-cortex-reach visible; visualize removed"
      contains: "memory-cortex-reach"
    - path: "lib/workflow/selector-decisions.cjs"
      provides: "venture_classified + classification_source emitted at the f_selector_decision write site"
      contains: "venture_classified"
    - path: "scripts/help-renderer.cjs"
      provides: "skips deprecated:true command entries when walking groups"
      contains: "deprecated"
  key_links:
    - from: "lib/workflow/selector-decisions.cjs (emit site :180)"
      to: "lib/core/venture-shape-nudge.cjs (read site :138-154)"
      via: "f_selector_decision.properties.venture_classified scalar"
      pattern: "venture_classified"
    - from: "scripts/intent-classifier.cjs (user-text seam)"
      to: "lib/core/mva-classifier.cjs classify()"
      via: "classify(sentence) -> {venture, source}; only scalars thread down"
      pattern: "classify\\("
---

<objective>
Execute the two CODE fixes in the 150.6 queue: FIX-04 (the help-surface gaps -- drop the deprecated visualize, surface the shipped-but-invisible discover + memory-cortex-reach, and teach the renderer to skip deprecated entries so the CLASS is killed not just the instance) and FIX-05 (THE precision fix: emit the scalar boolean venture_classified + classification_source enum at the f_selector_decision write site so the Phase 119 receipt nudge stops being a permanent no-op).

Purpose: help tells the truth about what ships; the Phase 119 receipt nudge can finally fire.
Output: data + renderer changes that make help honest; a one-seam classification emission that drives the production-shaped path the nudge reads, under the Canon Part 8 scalar-only fence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-CONTEXT.md
@.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-PATTERNS.md
@lib/core/venture-shape-nudge.cjs
@lib/workflow/selector-decisions.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-04 -- help-groups data fix + renderer deprecated-skip</name>
  <files>data/help-groups.json, scripts/help-renderer.cjs</files>
  <read_first>
    - data/help-groups.json (the getting-started group lines 16-29, the intelligence-brain group lines 76+, the hub group lines 165-174 with "visualize" at line 172, the deprecated_aliases block lines 189-196)
    - commands/visualize.md lines 8-10 (the `deprecated: true` / `deprecated_redirect` / `deprecated_removal` frontmatter shape the renderer must read)
    - commands/discover.md lines 1-8 (help_jtbd present; serves_jtbd ["explore"])
    - commands/memory-cortex-reach.md lines 1-8 (help_jtbd present; serves_jtbd ["navigate"])
    - scripts/help-renderer.cjs lines 117, 125, 191 (the frontmatter reader + the two group walks)
    - 150.6-PATTERNS.md "FIX-04" section (the entry-shape analog + the concrete edits + the renderer durability note)
  </read_first>
  <action>
    Data fix (data/help-groups.json):
    1. Remove "visualize" from the "hub" group commands array (it is deprecated -- commands/visualize.md:8 deprecated:true, redirect dashboard --mermaid, removal v1.14.0).
    2. Add "discover" to the "getting-started" group commands array (lane start; it scaffolds a room; help_jtbd present so it joins cleanly).
    3. Add "memory-cortex-reach" to the "intelligence-brain" group commands array (lane explore, where "memory" already lives).

    Renderer fix (scripts/help-renderer.cjs) -- the durability layer that kills the class:
    4. In the group walk (lines 125 and 191), where each command slug's frontmatter is already parsed (the help_jtbd reader at line 117), add a skip when the parsed frontmatter has `deprecated: true`. Read the flag exactly as commands/visualize.md:8 declares it. After this, even if a future deprecated command is left in a group, it does not render.

    Do BOTH the data fix and the renderer skip (kill the class, not just the instance). Use hyphens, never em-dashes, in any comment added.
  </action>
  <verify>
    <automated>node scripts/help-renderer.cjs 2>/dev/null | grep -c "visualize" | grep -q '^0$' && node scripts/help-renderer.cjs 2>/dev/null | grep -q "discover" && node scripts/help-renderer.cjs 2>/dev/null | grep -q "memory-cortex-reach" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/help-renderer.cjs 2>/dev/null | grep -c visualize` returns 0
    - `node scripts/help-renderer.cjs 2>/dev/null | grep -c discover` returns a value greater than 0
    - `node scripts/help-renderer.cjs 2>/dev/null | grep -c memory-cortex-reach` returns a value greater than 0
    - data/help-groups.json "hub" group commands array does NOT contain "visualize"
    - data/help-groups.json "getting-started" group contains "discover"; "intelligence-brain" group contains "memory-cortex-reach"
    - scripts/help-renderer.cjs reads the `deprecated` frontmatter flag and skips deprecated:true entries during the group walk (verify by temporarily marking a non-visualize command deprecated in a scratch fixture if needed, then reverting)
    - data/help-groups.json remains valid JSON: `node -e "JSON.parse(require('fs').readFileSync('data/help-groups.json','utf8'))"` exits 0
  </acceptance_criteria>
  <done>visualize gone from help; discover + memory-cortex-reach visible; the renderer skips deprecated:true entries; help-groups.json is valid JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX-05 -- emit venture_classified + classification_source at the f_selector_decision write site</name>
  <files>lib/workflow/selector-decisions.cjs, lib/workflow/offer-closer.cjs, scripts/intent-classifier.cjs</files>
  <behavior>
    - Test 1 (production-shaped emission): when recordSelectorDecision is called with a sentence in hand, the f_selector_decision memory_event it writes carries properties.venture_classified (boolean) AND properties.classification_source (enum from classify().source).
    - Test 2 (nudge un-deadens): after such an emission, venture-shape-nudge.shouldSurfaceNudge over the recent window returns surface/turn_count derived from the field (turn_count > 0 when a venture-true emission exists), NOT skip_reason: 'venture_classification_unavailable'.
    - Test 3 (Part 8 fence): the f_selector_decision payload contains NO substring of the input sentence; only the scalar boolean + the source enum are added. classify() is called but its input sentence is never stored.
    - Test 4 (no-sentence path unchanged): when no sentence is available, the emission omits venture_classified entirely (the nudge's existing safe-default still applies); no crash.
  </behavior>
  <read_first>
    - lib/workflow/selector-decisions.cjs lines 90-135 (recordSelectorDecision signature -- args.sentence is the new optional thread-through) and lines 175-200 (the navigation.logMemoryEvent('f_selector_decision', {...}) emission site at :180 -- the exact call to extend)
    - lib/core/mva-classifier.cjs lines 283-328 (classify(sentence) -> {venture:boolean, source:enum, ...}; synchronous, cached, length-guarded, Hebrew-guarded, zero-network on the sync path -- safe to call inline; DO NOT modify it)
    - lib/core/venture-shape-nudge.cjs lines 20-40 (the scalar-only contract) and 138-154 (the read site: filters f_selector_decision, counts venture_classified === true, degrades to skip_reason when the field is structurally absent)
    - lib/core/venture-shape-nudge.test.cjs Tests 2/5 (the target shape the fix must produce via a REAL emission, not a hand-built fixture)
    - scripts/intent-classifier.cjs around line 1655-1700 (the offer-closer.closeOffer seam) and line 1387/1422 (conversationSeed -- the user sentence already in hand at this seam)
    - lib/workflow/offer-closer.cjs lines 248-330 (closeOffer builds decisionArgs and calls decisions.recordSelectorDecision -- the thread-through path)
    - 150.6-PATTERNS.md "FIX-05" section (the precise contract: scalar boolean PROPERTY on existing f_selector_decision events, NOT a new event type; f_selector_decision is already in the frozen EVENT_TYPES allowlist so NO allowlist change)
  </read_first>
  <action>
    The shipped contract (per 150.6-PATTERNS.md and venture-shape-nudge.cjs:27-38): the nudge reads a scalar boolean PROPERTY on EXISTING f_selector_decision events. It does NOT need a new event type. f_selector_decision is already in the frozen EVENT_TYPES allowlist -- make NO EVENT_TYPES change.

    Step 1 -- selector-decisions.cjs. Add an optional `sentence` to the recordSelectorDecision args (o.sentence). At the navigation.logMemoryEvent('f_selector_decision', {...}) call at line 180, conditionally add two payload keys ONLY when a sentence is present:
      - require lib/core/mva-classifier.cjs (lazy require at top or inline), call `const cls = classify(o.sentence)`.
      - add `venture_classified: cls.venture` (boolean) and `classification_source: cls.source` (enum) to the payload object.
      When o.sentence is absent or empty, add NEITHER key (preserves the no-sentence path; the nudge's safe-default still applies). NEVER add the sentence itself or any substring of it to the payload (Canon Part 8 -- the memory_event log is queried by cross-room aggregators). Route ONLY through navigation.logMemoryEvent (the chokepoint); never raw SQL.

    Step 2 -- thread the sentence from the user-text seam. In lib/workflow/offer-closer.cjs closeOffer, forward an optional `sentence` from its args into the decisionArgs object it builds (the decisionArgs near lines 290-300). In scripts/intent-classifier.cjs, where conversationSeed (the user sentence already in hand) flows into the offer-closer.closeOffer call (around lines 1655-1700), pass `sentence: conversationSeed` into closeOffer. This is the seam where user text is already present, so classification happens where the text lives and ONLY the scalars travel onward. (dial-close-reach.cjs also calls recordSelectorDecision for defer/reject; it has no user sentence in hand, so it stays on the no-sentence path -- do not synthesize one there.)

    Step 3 -- acceptance leg (production-shaped, per the 150.5 lesson FIX-11): add or extend a test that drives a REAL recordSelectorDecision emission with a sentence, then asserts venture-shape-nudge.shouldSurfaceNudge over the produced events returns a field-derived result (not skip_reason: 'venture_classification_unavailable'). Drive the PRODUCTION path, not a hand-built fixture. Reuse the venture-shape-nudge.test.cjs harness shape.

    Use hyphens, never em-dashes.
  </action>
  <verify>
    <automated>node lib/core/venture-shape-nudge.test.cjs && node lib/memory/selector-decisions.test.cjs && grep -q "venture_classified" lib/workflow/selector-decisions.cjs && git diff --quiet lib/core/mva-classifier.cjs lib/core/navigation/memory-events.cjs && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - lib/workflow/selector-decisions.cjs contains "venture_classified" and "classification_source" added to the f_selector_decision payload, conditional on a sentence being present
    - lib/workflow/selector-decisions.cjs calls classify() from lib/core/mva-classifier.cjs and does NOT modify mva-classifier.cjs
    - The f_selector_decision payload NEVER includes o.sentence or any substring of it: sentence is used ONLY as the classify() input, never as a stored payload value (assert by inspection + the Part-8 test leg)
    - lib/core/mva-classifier.cjs is byte-unchanged: `git diff --quiet lib/core/mva-classifier.cjs`
    - No change to the EVENT_TYPES allowlist: `git diff --quiet lib/core/navigation/memory-events.cjs`
    - lib/workflow/offer-closer.cjs forwards the optional `sentence` arg into decisionArgs (the thread-through), with no sentence ever stored in any payload it builds
    - The production-shaped acceptance leg drives a REAL emission and asserts shouldSurfaceNudge returns turn_count > 0 (or surface true) when a venture-true emission exists -- NOT skip_reason: 'venture_classification_unavailable'
    - `node lib/core/venture-shape-nudge.test.cjs` exits 0
    - `bash tests/run-all-148.sh` exits 0 (frozen contracts intact); `bash tests/run-all-150.sh` green
  </acceptance_criteria>
  <done>A real f_selector_decision emission carries venture_classified + classification_source; the Phase 119 nudge fires on the production path; the sentence never enters the payload; mva-classifier + EVENT_TYPES unchanged; 148 + 150 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user sentence -> memory_event log | The user's conversational sentence is classified at the write seam. The memory_event log is queried by cross-room aggregators, so any user-text byte that lands here is a Canon Part 8 breach. Only the scalar boolean + source enum may cross. |
| selector decision -> local graph | recordSelectorDecision writes a typed edge through the navigation.cjs chokepoint (Part 9). The added scalars ride that same sanctioned path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.6.03-INFO | Information disclosure | venture_classified emission (Canon Part 8 scalar-only fence) | mitigate | Store ONLY `venture_classified` (boolean from classify().venture) + `classification_source` (enum from classify().source). The sentence is the classify() INPUT only, never a stored value. Dedicated Part-8 test leg asserts no sentence substring appears in the f_selector_decision payload. classify() is itself Hebrew-guarded + length-guarded + zero-network on the sync path. |
| T-150.6.03-TAMP | Tampering | memory_event write path | mitigate | Emission routes ONLY through navigation.logMemoryEvent (the single chokepoint); no raw SQL; f_selector_decision already in the frozen EVENT_TYPES allowlist (no allowlist widening). |
| T-150.6.03-ELEV | Elevation | help-renderer deprecated-skip | accept | Pure presentation filter over local command frontmatter; no privilege surface; no network. |
</threat_model>

<verification>
- `node scripts/help-renderer.cjs` shows discover + memory-cortex-reach, hides visualize.
- `node lib/core/venture-shape-nudge.test.cjs` exits 0; the production-shaped leg proves the nudge fires.
- Part 8 fence: no user-sentence substring in the f_selector_decision payload (test leg + grep inspection).
- Regression fences: `bash tests/run-all-148.sh` 18/18 (frozen MAX_K=3 / 0.70/0.15 / DIAL_REACH_K=6 intact); `bash tests/run-all-150.sh` green.
- `git diff --quiet lib/core/navigation/memory-events.cjs` (EVENT_TYPES untouched) and `git diff --quiet lib/core/mva-classifier.cjs` (classifier untouched).
</verification>

<success_criteria>
- /mos:help is honest: deprecated hidden, shipped surfaced, the class killed at the renderer.
- The Phase 119 receipt nudge fires on a real production emission.
- The Part 8 scalar-only fence holds; classifier + EVENT_TYPES + frozen contracts unchanged.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Symbol | Kind | Surface |
|--------|------|---------|
| discover + memory-cortex-reach group membership | changed JSON | data/help-groups.json |
| visualize removed from hub group | changed JSON | data/help-groups.json |
| deprecated:true skip in group walk | new logic | scripts/help-renderer.cjs |
| `venture_classified` (boolean) payload key | new event property | lib/workflow/selector-decisions.cjs (f_selector_decision emission) |
| `classification_source` (enum) payload key | new event property | lib/workflow/selector-decisions.cjs |
| optional `sentence` arg thread-through | new arg | lib/workflow/selector-decisions.cjs, lib/workflow/offer-closer.cjs, scripts/intent-classifier.cjs |
| production-shaped nudge acceptance leg | new/extended test | lib/core/venture-shape-nudge.test.cjs (or sibling) |
</artifacts_produced>

<output>
Create `.planning/phases/150.6-drift-fix-sweep-and-human-gate-quick-closers/150.6-03-SUMMARY.md` when done.
</output>
