---
phase: 121.5-terminal-coherence-capstone
plan: "10"
type: execute
wave: 3
depends_on: ["121.5-01", "121.5-02"]
files_modified:
  - mcp-server-brain/lib/brain-ask.cjs
  - lib/hmi/selector-dispatcher.cjs
  - lib/workflow/f-selector-ranker.cjs
  - lib/agents/tension-hook-agent.cjs
  - lib/agents/auto-explore-agent.cjs
  - lib/agents/reverse-salient-agent.cjs
  - scripts/suggest-next-command.cjs
  - scripts/act-command.cjs
  - commands/suggest-next.md
  - skills/ui-system/SKILL.md
  - docs/F-SELECTOR-CONSUMER-GUIDE.md
  - lib/hmi/jtbd-taxonomy.json
  - lib/memory/brain-suggestion-template.test.cjs
  - lib/memory/selector-alias-map.test.cjs
  - tests/test-no-bespoke-brain-prompts.sh
  - tests/run-all-121.5.sh
  - CHANGELOG.md
autonomous: true
requirements: ["COHERENCE-121.5-K-01", "COHERENCE-121.5-K-02", "COHERENCE-121.5-K-03", "COHERENCE-121.5-K-04", "COHERENCE-121.5-K-05"]
canon_parts: ["Part 3", "Part 4", "Part 7", "Part 8"]

must_haves:
  truths:
    - "All 5 Brain-suggestion consumers (/mos:suggest-next, /mos:act --chain, tension-hook-agent, auto-explore-agent, reverse-salient-agent) render the SAME locked template per audit Section 5.2"
    - "Header chip is exactly `[■ BRAIN]` (filled-square + 5-char brand word, 9 chars including brackets) across all 5 consumers"
    - "Question line is exactly `Choose next move:` (verb-first imperative, 3 words, no parenthetical) for F.1 variants"
    - "Option rows are two-line dense: Row 1 `<glyph> <N>. <Run Verb>` left + `<conf>%` right-aligned to 80-col; Row 2 indented `<category> · <graph relationship>`"
    - "Option label glyph is `▶` for the 0.7+ confidence top pick (recommended) and `▷` for sub-0.7 alternatives (alternative). One-glyph hierarchy replaces the verbose `(RECOMMENDED)` tag"
    - "Zone 1 left-rail color is `cyan` default; yellow-on-cascade for CONTRADICTS edges deferred to v1.13.2 hotfix per LOCKED decision 3"
    - "Footer is a stat-strip line: `▶ Brain · top-<K> of <N> ranked · <color> = informing`"
    - "F-variant routing: F.1 default; F.6 when JTBD set (per selector-dispatcher.cjs lines 313-345 JTBD lookup); F.0 for pre-gate recommendation confirmations; F.5 for branch-resolution surfaces"
    - "Verb labels: registered aliases ALLOWED per LOCKED decision 1; dispatcher carries `alias_map`; canonical stays in graph edges via collapse-on-write"
    - "All 5 consumers route through `lib/hmi/selector-dispatcher.cjs pickShape` (today, 3 of 5 do; this plan converts the remaining 2: suggest-next + act --chain)"
    - "CI tripwire `tests/test-no-bespoke-brain-prompts.sh` exits 0 against the live repo today; exits 1 if any future commit reintroduces `Ready to ___?` / `Pick one` / `[continue] / [stop]` / bespoke `[GATE]` text patterns in commands/*.md or in consumers of chain-recommender.cjs / rankForSelector"
    - "`docs/F-SELECTOR-CONSUMER-GUIDE.md` Section 4 publishes the locked template (slot table + visual mockup) so future consumers discover it before inventing a new pattern"
  artifacts:
    - path: "lib/hmi/selector-dispatcher.cjs"
      provides: "Brain-suggestion-variant template support (optionRows + footer slots) + alias_map plumbing for verb-label aliases per LOCKED decision 1"
      contains: "alias_map"
    - path: "lib/workflow/f-selector-ranker.cjs"
      provides: "optionRows + footer payload emitter; MAX_K constant for top-K cap; ranking metadata fields (category + graph_relationship)"
      contains: "optionRows"
    - path: "lib/hmi/jtbd-taxonomy.json"
      provides: "Canonical verb alias_map registry per LOCKED decision 1"
      contains: "alias_map"
    - path: "lib/memory/brain-suggestion-template.test.cjs"
      provides: "Integration test asserting all 5 consumers render the locked template (chip + question + option-row shape + footer)"
    - path: "lib/memory/selector-alias-map.test.cjs"
      provides: "Unit test for alias_map round-trip (alias label rendered, canonical verb persisted to graph edge)"
    - path: "tests/test-no-bespoke-brain-prompts.sh"
      provides: "CI tripwire grep test per audit punch list item 6; exits 1 on bespoke-pattern reintroduction"
    - path: "docs/F-SELECTOR-CONSUMER-GUIDE.md"
      provides: "Section 4 (NEW): The Locked Template for Brain-Suggested Selectors; publishes slot-value table + visual mockup from audit Section 5.2"
    - path: "skills/ui-system/SKILL.md"
      provides: "Shape F.1 Brain-suggestion variant subsection citing the locked template (chip + score-right two-line rows + stat-strip footer); body_shape vs F-shape orthogonality note per LOCKED decision 4"
  key_links:
    - from: "scripts/suggest-next-command.cjs"
      to: "lib/workflow/f-selector-ranker.cjs rankForSelector + lib/hmi/selector-dispatcher.cjs pickShape"
      via: "promote NONE -> F.1 (audit Section 5.3 highest-leverage adoption)"
      pattern: "rankForSelector.*pickShape"
    - from: "scripts/act-command.cjs"
      to: "lib/hmi/selector-dispatcher.cjs pickShape (F.0 variant)"
      via: "promote BESPOKE [continue] / [stop] bracket text -> F.0 Mini Decision Gate (audit Section 5.3)"
      pattern: "pickShape.*F\\.0"
    - from: "lib/agents/tension-hook-agent.cjs"
      to: "the locked [■ BRAIN] chip"
      via: "rename F1_HEADER (line 52) from `-- mindrianOS -- pending tension -- pick a verb --` to `[■ BRAIN]`; surface tension context in question-line slot"
      pattern: "\\[■ BRAIN\\]"
    - from: "lib/agents/auto-explore-agent.cjs"
      to: "the locked [■ BRAIN] chip"
      via: "replace BQ-anchored header (Brain Section 8.5) with [■ BRAIN] chip; move BQ anchor to question-line slot (two-row format preserves BQ moat)"
      pattern: "\\[■ BRAIN\\]"
    - from: "lib/agents/reverse-salient-agent.cjs"
      to: "the locked [■ BRAIN] chip"
      via: "replace header composition (line 506) with [■ BRAIN] chip; move persona suffix (Phase 89-07 extension) to question-line slot"
      pattern: "\\[■ BRAIN\\]"
    - from: "lib/hmi/jtbd-taxonomy.json"
      to: "the verb alias_map"
      via: "register Resolve/Later/Skip + Explore/Skip/Later aliases against canonical Run Methodology / Defer / Free-Text per LOCKED decision 1"
      pattern: "alias_map"
---

<objective>
Close Sub-plan K from 121.5-CONTEXT.md absorbed_2026_05_23 (the Bundle A bundle of the Phase 121.5 Selector Coverage Audit). The single source of truth is `.planning/121.5-selector-coverage-audit.md` Section 5 (the locked Brain-suggestion content template), Section 5.3 (per-consumer adoption diffs), and Section 6 punch list items 1, 2, 3, 6, 7 (which together comprise Bundle A).

Purpose: today 5 surfaces consume Brain-ranked next moves and all 5 render differently. The capstone locks ONE shape across all consumers; the lock propagates to every Brain-touching selector simultaneously and converts the two NONE/BESPOKE CLI scripts (suggest-next + act --chain) in the same pass. This is THE single highest-leverage promotion in Phase 121.5 per audit Executive Summary.

5 audit items this plan delivers (Bundle A):
- Item 1: Lock the Brain-suggestion content template across all 5 consumers (Section 5)
- Item 2: Promote `/mos:suggest-next` from NONE to F.1 via `rankForSelector` + `pickShape` (Section 5.3 highest-leverage)
- Item 3: Promote `/mos:act --chain` from BESPOKE to F.0 via `pickShape` (Section 5.3 second-highest)
- Item 6: Add CI tripwire to enforce the lock (`tests/test-no-bespoke-brain-prompts.sh`)
- Item 7: Update `docs/F-SELECTOR-CONSUMER-GUIDE.md` to publish the locked template as Section 4

5 LOCKED design decisions (encoded into this plan; navigator decided 2026-05-23):
1. Verb labels: ALLOW registered aliases with mapping (NOT force-canonical). Dispatcher carries `alias_map`; canonical stays in graph edges via collapse-on-write.
2. F.4 vs F.0 for `/mos:operator reset`: F.0 (architectural fit).
3. CONTRADICTS color flip (cyan -> yellow): DEFER past Bundle A. Ship cyan default. Yellow-on-cascade lands as v1.13.2 hotfix if testers report missed warnings.
4. `body_shape: B` vs `F.1` for `/mos:suggest-next`: KEEP B, document orthogonality in SKILL.md (body_shape is layout discipline; F-shape is selector contract; the two are orthogonal axes).
5. 71 N/A methodology commands' close-out F.1: OUT OF SCOPE for 121.5. Defer to v1.14.0.

Output: the 5 consumer files migrate to the locked template; the dispatcher + ranker grow optionRows + footer + alias_map plumbing; SKILL.md gains the variant subsection + orthogonality note; F-SELECTOR-CONSUMER-GUIDE.md publishes the lock; 3 new test files lock the contract; CHANGELOG flags the consolidation under v1.13.0 final.

Estimate: ~2-2.5 person-days (per audit Section 5.3 total `0.5 + 0.5 + 0.25 + 0.25 + 0.25 + tests`; +0.5 for SKILL.md + docs + CHANGELOG + CI tripwire).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/121.5-selector-coverage-audit.md
@.planning/phases/121.5-terminal-coherence-capstone/121.5-CONTEXT.md
@.planning/phases/121.5-terminal-coherence-capstone/121.5-01-PLAN.md
@.planning/phases/121.5-terminal-coherence-capstone/121.5-02-PLAN.md
@docs/F-SELECTOR-CONSUMER-GUIDE.md
@skills/ui-system/SKILL.md
@lib/hmi/selector-dispatcher.cjs
@lib/workflow/f-selector-ranker.cjs
@scripts/suggest-next-command.cjs
@scripts/act-command.cjs
@lib/agents/tension-hook-agent.cjs
@lib/agents/auto-explore-agent.cjs
@lib/agents/reverse-salient-agent.cjs
@lib/hmi/jtbd-taxonomy.json

<interfaces>
<!-- LOCKED slot values from audit Section 5.2 (the canonical lock; do NOT renegotiate at execute-time): -->
<!--   Header chip (<= 12 chars): [■ BRAIN]                                                             -->
<!--   Question line:             Choose next move:                                                     -->
<!--   Glyph (recommended top):   ▶ (right-triangle-filled) for >= 0.7 confidence                       -->
<!--   Glyph (alternatives):      ▷ (right-triangle-empty)  for < 0.7 confidence                        -->
<!--   Option row format:         Row 1: `<glyph> <N>. <Run Verb>` left + `<conf>%` right-aligned 80c   -->
<!--                              Row 2: 5-space indent, `<framework category> · <graph relationship>`  -->
<!--   Zone 1 left-rail color:    cyan default (yellow-on-cascade DEFERRED to v1.13.2 per decision 3)   -->
<!--   Footer caption:            `▶ Brain · top-<K> of <N> ranked · <color> = informing`               -->
<!--   F-variant routing:         F.1 default; F.6 when JTBD set; F.0 pre-gate; F.5 branch resolution   -->

<!-- Wave-3 dependency chain (per 121.5-CONTEXT.md): -->
<!--   This plan is wave 3, parallel to Plans 121.5-04 + 121.5-08.                                      -->
<!--   Depends on Plan 121.5-01 (SKILL.md v2 reconciliation -- F.0 + F.6 documented; we extend Sec 2).  -->
<!--   Depends on Plan 121.5-02 (body_shape sweep -- the orthogonality note we add requires that sweep  -->
<!--     to land first so body_shape: B references in /mos:suggest-next are stable).                    -->
<!--   Plan 121.5-09 (coherence smoke test, wave 4) depends_on includes 121.5-10 once this plan ships.  -->

<!-- 5 LOCKED design decisions (audit Section 7 Open Questions, navigator answered 2026-05-23): -->
<!--   1. Verb labels: ALLOW registered aliases (NOT force canonical).                                  -->
<!--      Dispatcher carries alias_map; canonical stays in graph edges (collapse-on-write).             -->
<!--   2. F.4 vs F.0 for `/mos:operator reset`: F.0 (architectural fit per audit Section 4.1).          -->
<!--      Note: this decision is recorded for Plan 121.5-08 Sub-plan J D-12; not implemented in 121.5-10. -->
<!--   3. CONTRADICTS color flip (cyan -> yellow): DEFER past Bundle A. Ship cyan default.              -->
<!--      Yellow-on-cascade lands as v1.13.2 hotfix if testers report missed warnings.                  -->
<!--   4. `body_shape: B` for /mos:suggest-next: KEEP B; document orthogonality in SKILL.md.            -->
<!--      body_shape is layout discipline (a tree, a card, a list); F-shape is selector contract        -->
<!--      (the verb-pick gate that fires AT THE END of the body). Orthogonal axes.                      -->
<!--   5. 71 N/A methodology commands' close-out F.1: OUT OF SCOPE for 121.5. Defer to v1.14.0.         -->

<!-- alias_map shape (per LOCKED decision 1): -->
<!--   {                                                                                                -->
<!--     "verb_aliases": {                                                                              -->
<!--       "Resolve": "Run Methodology",                                                                -->
<!--       "Explore": "Run Methodology",                                                                -->
<!--       "Later":   "Defer",                                                                          -->
<!--       "Skip":    "Free-Text"                                                                       -->
<!--     }                                                                                              -->
<!--   }                                                                                                -->
<!--   Render alias-labeled verb to user; persist canonical verb to graph edge (CHOSE / CHOSE_MINI).    -->

<!-- Audit re-verification: file paths cited in audit (rev 5bdb7667) verified 2026-05-23 still accurate: -->
<!--   - mcp-server-brain/lib/brain-ask.cjs                                            (28,989 bytes)   -->
<!--   - lib/hmi/selector-dispatcher.cjs                                               (28,300 bytes)   -->
<!--   - lib/workflow/f-selector-ranker.cjs                                            (18,661 bytes)   -->
<!--   - lib/agents/tension-hook-agent.cjs    F1_HEADER line 52, F1_VERBS line 51      (21,428 bytes)   -->
<!--   - lib/agents/auto-explore-agent.cjs    BQ_TEMPLATE_REGISTRY line 121            (48,321 bytes)   -->
<!--   - lib/agents/reverse-salient-agent.cjs surfaceFinding line 463                  (27,570 bytes)   -->
<!--   - scripts/suggest-next-command.cjs     renderSequence line 123 + actionFooter   ( 9,550 bytes)   -->
<!--   - scripts/act-command.cjs              renderChainReport line 131 + [GATE] line 156 (11,138 bytes) -->

<!-- Canon Part 7 audit: 0 net-new commands. 3 net-new infrastructure files (test x2 + tripwire). -->
<!-- The plan EXTENDS shipped substrate (Phase 88.2 renderers, Phase 101-01 dispatcher, Phase 125 ranker)  -->
<!-- and CONSOLIDATES 5 divergent consumers into 1 locked template. Per Canon Part 7 reuse-before-build,   -->
<!-- every modified file's purpose is reconciliation of N existing things, not the N+1th.                  -->

<!-- Canon Part 8 audit: ZERO new Brain queries. The locked template is a RENDER contract on outputs       -->
<!-- already produced by shipped Brain query paths. lib/brain/chain-recommender.cjs is unchanged.          -->
<!-- alias_map lives LOCAL in jtbd-taxonomy.json. Verb_chosen persisted to graph edges via                 -->
<!-- navigation.cjs (already routes through Phase 109 chokepoint). No user content egresses.               -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Substrate -- wire optionRows + footer + alias_map into dispatcher + ranker + taxonomy</name>
  <files>
    lib/hmi/selector-dispatcher.cjs,
    lib/workflow/f-selector-ranker.cjs,
    lib/hmi/jtbd-taxonomy.json,
    lib/memory/selector-alias-map.test.cjs,
    tests/run-all-121.5.sh
  </files>
  <read_first>
    lib/hmi/selector-dispatcher.cjs (the pickShape entry point at line 513; the payload signature at lines 307-308 + 357-358; the zone composition at lines 293-299 -- understand how `rendered.zones.footer` is currently appended);
    lib/workflow/f-selector-ranker.cjs (the rankForSelector signature at line 307; the return shape produced for items[]; identify where category + graph_relationship can be sourced for the meta row);
    lib/hmi/jtbd-taxonomy.json (today: no alias_map; the next_move_verbs section + F.6 JTBD-aware variant routing);
    docs/F-SELECTOR-CONSUMER-GUIDE.md (the typical-wiring pseudocode at lines 57-109 -- this is the substrate the audit references);
    .planning/121.5-selector-coverage-audit.md Section 5.2 (the slot-value table) + 5.3 (per-consumer adoption diffs)
  </read_first>
  <behavior>
    Test 1: pickShape with `payload.brain_suggestion_variant === true` AND `payload.optionRows` set renders the option rows verbatim into `rendered.zones.body` with two-line format per row (`<glyph> <N>. <verb>` line + 5-space-indent `<meta>` line).
    Test 2: pickShape with `payload.brain_suggestion_variant === true` AND `payload.footer` set appends the stat-strip footer to `rendered.zones.footer` AFTER any existing trailer.
    Test 3: pickShape with `payload.header === '[■ BRAIN]'` renders the literal chip in `rendered.zones.header` (the chip-vocabulary regex matches: `^\[■ BRAIN\]$`).
    Test 4: pickShape with `payload.alias_map: { "Resolve": "Run Methodology" }` AND `payload.verbs: ["Resolve", "Defer", "Free-Text"]` renders "Resolve" to the user; on accept, the returned `result.selection.verb_canonical` is "Run Methodology" (the alias collapses for graph-edge persistence).
    Test 5: rankForSelector returns items[] with optional `category` + `graph_relationship` meta fields (NEW; non-breaking) populated when packetOptional carries them.
    Test 6: rankForSelector exposes a MAX_K constant (3) and clamps the user-passed k arg to MAX_K (caller asking k=20 receives k=3 to fit the visual mockup row budget per audit Section 5.2.3 anti-pattern "More than 3 lines per option").
    Test 7: jtbd-taxonomy.json parses with NEW top-level key `alias_map.verb_aliases` containing at least the four entries: Resolve, Explore, Later, Skip mapped to their canonical verbs per LOCKED decision 1.
    Test 8: A round-trip helper (in lib/hmi/jtbd-taxonomy-helpers.cjs OR inline in selector-dispatcher.cjs) collapses an alias to canonical: `aliasToCanonical("Resolve") === "Run Methodology"`; `aliasToCanonical("Run Methodology") === "Run Methodology"` (no-op for already-canonical).
  </behavior>
  <action>
    Step 1: Extend `lib/hmi/jtbd-taxonomy.json` with a top-level `alias_map` block per LOCKED decision 1. Shape:
    ```
    "alias_map": {
      "verb_aliases": {
        "Resolve": "Run Methodology",
        "Explore": "Run Methodology",
        "Later":   "Defer",
        "Skip":    "Free-Text"
      },
      "provenance": "Phase 121.5-10 Sub-plan K (audit Section 5.3 verb-label alias decision; LOCKED decision 1)"
    }
    ```
    The block is additive; no existing keys touched. Future agentic surfaces register additional aliases here (NOT in agent source code); the dispatcher reads from one place.

    Step 2: Extend `lib/hmi/selector-dispatcher.cjs` `pickShape` (line 513) to accept a `payload.brain_suggestion_variant: true` flag. When set:
    - Read `payload.optionRows: Array<{glyph: '▶'|'▷', verb: string, confPct: number, meta: string}>` -- the ranker's pre-computed render rows. Compose into `rendered.zones.body` as two-line dense rows per audit Section 5.2.1.
    - Read `payload.footer: string` (the stat-strip caption) and append to `rendered.zones.footer` AFTER the AskUserQuestion structural-marker trailer (which the dispatcher already appends at lines 293-299).
    - Read `payload.alias_map: { [alias: string]: canonical_verb }` (defaults to the alias_map.verb_aliases from jtbd-taxonomy.json loaded once at module init). On selection, attach `result.selection.verb_canonical = alias_map[verb_chosen] || verb_chosen` so consumers persist the canonical verb to graph edges via navigation.cjs.

    Step 3: Add a small helper `aliasToCanonical(verb)` exported from `lib/hmi/selector-dispatcher.cjs` so consumers can perform the alias collapse outside the dispatch flow when they need it (e.g., when reading historical decisions back out).

    Step 4: Extend `lib/workflow/f-selector-ranker.cjs rankForSelector` (line 307) return items[] with NEW optional fields:
    - `category: string` -- the framework category (sourced from packetOptional.local_graph_summary or from a per-command lookup in command-registry.json; falls back to '' when unknown).
    - `graph_relationship: string` -- one-line edge-description ("feeds Porter Five Forces" / "chains to JTBD" / "diverges from current path"). Sourced from packetOptional.local_graph_summary.framework_chain_hint or computed from the local navigation chokepoint; falls back to '' when unknown.
    These fields are OPTIONAL; consumers that do not need the two-line dense row may ignore them. Non-breaking.

    Step 5: Add a `MAX_K = 3` constant at the top of `lib/workflow/f-selector-ranker.cjs` (with a top-of-file comment citing audit Section 5.2.3 anti-pattern "More than 3 lines per option" + the AskUserQuestion auto-injected "Type something" / "Chat about this" row constraint). In rankForSelector clamp `k = Math.min(k, MAX_K)` per the user-facing visual budget.

    Step 6: Create `lib/memory/selector-alias-map.test.cjs`:
    - Test 1: jtbd-taxonomy.json parses; alias_map.verb_aliases present; the 4 LOCKED aliases (Resolve, Explore, Later, Skip) present.
    - Test 2: aliasToCanonical round-trip per behavior Test 8.
    - Test 3: pickShape with payload.alias_map honors LOCKED decision 1 per behavior Test 4.
    - Test 4: Render-vs-persist invariant: alias label appears in rendered.zones.body; canonical verb appears in result.selection.verb_canonical.
    - Test 5: Unknown alias passes through unchanged (`aliasToCanonical("NotAnAlias") === "NotAnAlias"`).

    Step 7: Update `tests/run-all-121.5.sh` CJS_SUITES: append `../lib/memory/selector-alias-map.test.cjs` (mirror the existing entry style with a per-line plan ownership comment in the header block).

    Note: This task is SUBSTRATE only -- the 5 consumer files (Task 2) ride on top. The substrate is built first so each consumer adoption is a payload-shape change, not a contract change.
  </action>
  <verify>
    <automated>node -e "const j=require('./lib/hmi/jtbd-taxonomy.json'); if(!j.alias_map||!j.alias_map.verb_aliases||!j.alias_map.verb_aliases.Resolve) process.exit(1)" && node lib/memory/selector-alias-map.test.cjs && bash tests/run-all-121.5.sh</automated>
    jtbd-taxonomy.json carries alias_map; selector-alias-map suite GREEN; cumulative 121.5 suites unbroken.
  </verify>
  <acceptance_criteria>
    - File lib/hmi/jtbd-taxonomy.json carries top-level alias_map.verb_aliases with the 4 LOCKED entries (Resolve, Explore, Later, Skip).
    - lib/hmi/selector-dispatcher.cjs pickShape accepts payload.brain_suggestion_variant + optionRows + footer + alias_map; aliasToCanonical exported.
    - lib/workflow/f-selector-ranker.cjs rankForSelector returns optional category + graph_relationship fields; MAX_K = 3 clamps caller k arg.
    - lib/memory/selector-alias-map.test.cjs runs 5/5 GREEN.
    - bash tests/run-all-121.5.sh exits 0 with all cumulative suites GREEN.
  </acceptance_criteria>
  <done>
    Substrate ready for the 5 consumer adoptions in Task 2. Alias_map LOCKED per decision 1. MAX_K constant prevents future consumers from over-stuffing rows past the AskUserQuestion budget.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adopt the locked template in all 5 Brain-suggestion consumers</name>
  <files>
    scripts/suggest-next-command.cjs,
    scripts/act-command.cjs,
    lib/agents/tension-hook-agent.cjs,
    lib/agents/auto-explore-agent.cjs,
    lib/agents/reverse-salient-agent.cjs,
    commands/suggest-next.md,
    lib/memory/brain-suggestion-template.test.cjs,
    tests/run-all-121.5.sh,
    mcp-server-brain/lib/brain-ask.cjs
  </files>
  <read_first>
    .planning/121.5-selector-coverage-audit.md Section 5.3 per-consumer adoption diffs (the verbatim lines + payload shapes; do not paraphrase);
    scripts/suggest-next-command.cjs (renderSequence at line 123; actionFooter at line 138 -- both will be replaced by the pickShape call);
    scripts/act-command.cjs (renderChainReport at line 131; the [GATE] bracket text block at lines 152-167 -- this is the BESPOKE pattern being retired);
    lib/agents/tension-hook-agent.cjs (F1_HEADER at line 52 + F1_VERBS at line 51 -- locked decision 1 ALLOWS keeping Resolve/Later/Skip as aliases, mapped to canonical);
    lib/agents/auto-explore-agent.cjs (BQ_TEMPLATE_REGISTRY at line 121 -- BQ anchor MOVES from header to question-line slot to preserve Phase 117 RESEARCH 8.5 moat without violating the 12-char chip rule);
    lib/agents/reverse-salient-agent.cjs (surfaceFinding at line 463 -- persona suffix MOVES from header to question-line slot per Phase 89-07 Sub-plan I);
    commands/suggest-next.md (today body_shape: B at line 5 -- KEPT per LOCKED decision 4; only frontmatter docs note added)
  </read_first>
  <behavior>
    Test 1: scripts/suggest-next-command.cjs invokes rankForSelector + pickShape with payload `{ brain_suggestion_variant: true, header: '[■ BRAIN]', optionRows: [...], footer: '▶ Brain · top-<K> of <N> ranked · cyan = informing' }`. Captured-stdout assertion: `[■ BRAIN]` chip appears once; `Choose next move:` question line appears once; the footer `▶ Brain · top-` substring appears once.
    Test 2: scripts/act-command.cjs `--chain` mode invokes pickShape with `requestedShape: 'F.0'`, payload `{ header: '[■ BRAIN]', body: gateReason }`. Captured-stdout assertion: NO `[continue]` bracket text; NO `[stop]` bracket text; F.0 Mini Gate header (filled-square + chip) appears.
    Test 3: lib/agents/tension-hook-agent.cjs F1_HEADER === '[■ BRAIN]'; F1_VERBS unchanged at ['Resolve', 'Later', 'Skip'] (LOCKED decision 1 allows alias retention); the surfaceFinding pickShape call includes payload.alias_map referencing jtbd-taxonomy.json.verb_aliases.
    Test 4: lib/agents/auto-explore-agent.cjs surfaceFinding emits payload.header === '[■ BRAIN]'; the BQ anchor from BQ_TEMPLATE_REGISTRY is placed in the question-line slot beneath the chip (not in the header itself); the F.1 verbs include alias_map collapse so Explore -> Run Methodology persists to the graph.
    Test 5: lib/agents/reverse-salient-agent.cjs surfaceFinding emits payload.header === '[■ BRAIN]'; persona suffix appears in the question-line slot beneath the chip; F.0 closed vocab (Approve / Reject / Defer) verbatim per audit Section 5.2.
    Test 6: commands/suggest-next.md frontmatter `body_shape: B` is UNCHANGED (LOCKED decision 4); a new docs comment in the body cites the orthogonality (body_shape = layout, F.1 = selector contract).
    Test 7: Smoke-end-to-end: a single invocation of each consumer with synthetic inputs renders a string containing the chip + question line + footer triplet; the triplet is byte-identical-by-shape across all 5 consumers (only the meta payload differs).
    Test 8: mcp-server-brain/lib/brain-ask.cjs has any docstring/comment mentioning Section 5 chip refs aligned with the locked `[■ BRAIN]` chip (docstring sweep only; ZERO behavior change in brain-ask.cjs).
  </behavior>
  <action>
    Step 1 (consumer #1 -- scripts/suggest-next-command.cjs, audit Section 5.3 highest-leverage):
    - Read lines 119 (suggestHeader composition) + 123-198 (renderSequence + actionFooter + main flow).
    - Replace renderSequence + actionFooter text-block emission with a wired call to `lib/workflow/f-selector-ranker.cjs rankForSelector` (gives top-K items[] with category + graph_relationship + investment_level fields from Task 1) then `lib/hmi/selector-dispatcher.cjs pickShape` with payload:
      ```
      {
        brain_suggestion_variant: true,
        requestedShape: 'F.1',
        header: '[■ BRAIN]',
        questionLine: 'Choose next move:',
        verbs: items.map(i => i.command_or_alias),
        alias_map: jtbdTaxonomy.alias_map.verb_aliases,
        recommendedVerb: items[0].score >= 0.7 ? items[0].command_or_alias : null,
        optionRows: items.map((i, idx) => ({
          glyph: i.score >= 0.7 ? '▶' : '▷',
          number: idx + 1,
          verb: i.command_or_alias,
          confPct: Math.round(i.score * 100),
          meta: `${i.category} · ${i.graph_relationship}`
        })),
        footer: `▶ Brain · top-${items.length} of ${totalRanked} ranked · cyan = informing`
      }
      ```
    - On the dispatch result, route F.0 accept -> recordSelectorDecision (already in lib/workflow/selector-decisions.cjs); none-fit -> recordSelectorMiss + route to `/mos:do` with user_intent.
    - Preserve the existing `actionFooter` Zone 4 ONLY in non-Brain fallback (Tier 0; when rankForSelector returns zero items).

    Step 2 (consumer #2 -- scripts/act-command.cjs, audit Section 5.3 second-highest):
    - Read line 131 renderChainReport + lines 152-167 [GATE] bracket text block.
    - Replace lines 152-167 with a wired call to `pickShape({ requestedShape: 'F.0', payload: { header: '[■ BRAIN]', questionLine: `[GATE] Chain step ${plan.stopAt.step}: ${cmd} for ${fw}`, body: 'This step is not autonomous_safe -- it needs your eyes.' } })`.
    - Wire result.selection.verb (or verb_canonical via alias_map): Approve -> continue chain; Reject -> stop with REJECTED_BECAUSE edge (captured via the F.0 reject_reason field); Defer -> milestone-audit row + halt chain.
    - Preserve every other line of renderChainReport (the per-step status table at lines 132-150) verbatim.

    Step 3 (consumer #3 -- lib/agents/tension-hook-agent.cjs, audit Section 5.3 minor alignment):
    - Line 52 F1_HEADER: rename `-- mindrianOS -- pending tension -- pick a verb --` -> `[■ BRAIN]` (literal const string).
    - Line 51 F1_VERBS: KEEP `['Resolve', 'Later', 'Skip']` (LOCKED decision 1; aliases allowed; alias_map collapses to canonical for graph-edge persistence).
    - Inside surfaceFinding (line 134), populate payload.alias_map from jtbd-taxonomy.json.alias_map.verb_aliases (loaded once at module init), add a new payload.questionLine carrying the tension context ("Resolve pending tension:" + brief tension summary).
    - Inside surfaceFinding, populate payload.brain_suggestion_variant = true + payload.optionRows from the ranker (if available; Phase 116 explicitly omits the RECOMMENDED gate per the agent's design note -- preserve that; all three verbs render with `▷` empty-triangle).
    - The remaining surfaceFinding logic (telemetry, memory_event mirror, selector_presentation edge) is unchanged.

    Step 4 (consumer #4 -- lib/agents/auto-explore-agent.cjs, audit Section 5.3 minor alignment):
    - Find the surfaceFinding call site (function defined near line 116 area, BQ-anchored header composition).
    - Replace the BQ-anchored header with the literal `[■ BRAIN]` chip in payload.header.
    - Move the verbatim Brain BQ name (sourced via composeBQAnchoredLarryVoice and BQ_TEMPLATE_REGISTRY at line 121) into payload.questionLine (the two-row chip+question format preserves the Phase 117 RESEARCH 8.5 BQ moat without violating the 12-char chip rule).
    - Verb-label aliases (Explore / Skip / Later) KEPT per LOCKED decision 1; alias_map collapses on selection (Explore -> Run Methodology, Skip -> Free-Text, Later -> Defer).
    - Phase 88.2 confidence-gate (>= 0.7) preserved -- it determines `▶` vs `▷` glyph in payload.optionRows[0].

    Step 5 (consumer #5 -- lib/agents/reverse-salient-agent.cjs, audit Section 5.3 minor alignment):
    - Line 463 surfaceFinding: replace header composition with payload.header = '[■ BRAIN]'.
    - Move persona suffix (Phase 89-07 extension via resolvePersonaSuffix at line 386) into payload.questionLine slot beneath the chip. Two-row pattern matches the Phase 117 adoption (Step 4 above).
    - F.0 closed vocabulary (Approve / Reject / Defer) STAYS verbatim per audit Section 5.2 + F.0 spec (no RECOMMENDED in F.0; the shape itself is the recommendation surface).

    Step 6 (commands/suggest-next.md, LOCKED decision 4):
    - Frontmatter `body_shape: B` at line 5 UNCHANGED.
    - Add a new docs comment in the body (after the frontmatter, before the first user-facing line) explaining the orthogonality per LOCKED decision 4: body_shape is layout discipline (Shape B = Semantic Tree); F.1 is the selector contract (the dispatcher render around the verb-pick fork) that fires at the close of the tree render. The two axes are orthogonal; this command is "F.1 over Shape B."

    Step 7 (mcp-server-brain/lib/brain-ask.cjs, docstring sweep only):
    - Grep for any comment / docstring mentioning the audit Section 5 chip references; if any docstring mentions the chip pattern, ensure it cites `[■ BRAIN]` not a legacy pattern.
    - ZERO BEHAVIOR CHANGE. This step is a docstring lint; if no mentions exist, the file is unchanged.

    Step 8: Create `lib/memory/brain-suggestion-template.test.cjs`:
    - Test 1: scripts/suggest-next-command.cjs --json invocation contains the chip + question + footer triplet per behavior Test 1.
    - Test 2: scripts/act-command.cjs --chain --dry-run output (with synthetic non-autonomous_safe step) contains F.0 Mini Gate + chip; NO bracket-text fallback.
    - Test 3: tension-hook-agent surfaceFinding returns object with header === '[■ BRAIN]'; questionLine starts with "Resolve pending tension:".
    - Test 4: auto-explore-agent surfaceFinding returns object with header === '[■ BRAIN]'; questionLine carries verbatim BQ name from BQ_TEMPLATE_REGISTRY.
    - Test 5: reverse-salient-agent surfaceFinding returns object with header === '[■ BRAIN]'; questionLine carries persona suffix.
    - Test 6: Shape isomorphism: the 5 captured outputs share the SAME chip header + question-line shape + footer shape (only the meta payload differs across consumers).

    Step 9: Update `tests/run-all-121.5.sh` CJS_SUITES: append `../lib/memory/brain-suggestion-template.test.cjs` (with a header-line comment crediting Plan 121.5-10 Task 2).
  </action>
  <verify>
    <automated>node lib/memory/brain-suggestion-template.test.cjs && bash tests/run-all-121.5.sh && grep -c "\[■ BRAIN\]" scripts/suggest-next-command.cjs scripts/act-command.cjs lib/agents/tension-hook-agent.cjs lib/agents/auto-explore-agent.cjs lib/agents/reverse-salient-agent.cjs</automated>
    All 5 consumers emit the chip; all tests GREEN.
  </verify>
  <acceptance_criteria>
    - All 5 consumer files contain the literal chip `[■ BRAIN]` (verified via grep returning >= 1 per file).
    - scripts/suggest-next-command.cjs renderSequence + actionFooter replaced by rankForSelector + pickShape (NONE -> F.1 per audit Section 6 item 2).
    - scripts/act-command.cjs renderChainReport bracket-text block (lines 152-167) replaced by pickShape F.0 (BESPOKE -> F.0 per audit Section 6 item 3).
    - Phase 116 tension hook + Phase 117 auto-explore + Phase 89-07 reverse-salient agents all carry [■ BRAIN] chip; per-agent context (tension summary / BQ anchor / persona suffix) moved to question-line slot per audit Section 5.3 minor-alignment adoption pattern.
    - commands/suggest-next.md body_shape: B UNCHANGED; orthogonality docs comment added per LOCKED decision 4.
    - mcp-server-brain/lib/brain-ask.cjs docstring sweep complete; zero behavior change.
    - lib/memory/brain-suggestion-template.test.cjs runs 6/6 GREEN.
    - bash tests/run-all-121.5.sh exits 0 with all cumulative suites GREEN.
  </acceptance_criteria>
  <done>
    All 5 Brain-suggestion consumers render the locked template. The two NONE/BESPOKE CLI scripts converted in one pass; the three agentic surfaces aligned. Per audit Executive Summary: "the lock propagates to every Brain-touching selector simultaneously."
  </done>
</task>

<task type="auto">
  <name>Task 3: Publish the lock -- SKILL.md variant subsection + F-SELECTOR-CONSUMER-GUIDE Section 4 + CI tripwire + CHANGELOG</name>
  <files>
    skills/ui-system/SKILL.md,
    docs/F-SELECTOR-CONSUMER-GUIDE.md,
    tests/test-no-bespoke-brain-prompts.sh,
    tests/run-all-121.5.sh,
    CHANGELOG.md
  </files>
  <read_first>
    skills/ui-system/SKILL.md Section 2 (the Shape F subsections; F.1 starts at line 100 -- we add a Brain-suggestion variant subsection beneath F.1; the body_shape vs F-shape orthogonality note lands as a new note paragraph in Section 2's opening prose);
    docs/F-SELECTOR-CONSUMER-GUIDE.md (today: 3 sections + "What each consumer surface owns" table + "Filter helper" + "Canon Part 8 invariants" + "D11 fallback" + "See also"; we add a new Section 4 after the wiring pseudocode);
    .planning/121.5-selector-coverage-audit.md Section 5.2 (the slot-value table) + Section 5.2.2 (visual mockup) + Section 5.2.3 (anti-patterns rejected) -- the verbatim content to publish;
    CHANGELOG.md (find the v1.13.0 final entry top -- this plan lands under the in-progress v1.13.0-beta.x train; the entry goes under v1.13.0 final per the Phase 121.5 ship plan)
  </read_first>
  <action>
    Step 1: Extend `skills/ui-system/SKILL.md` Section 2 with a Brain-suggestion variant note under F.1.
    Location: insert a new subsection AFTER the "State-update hook:" line of "Shape F.1 - Next Move" (around line 119) and BEFORE the "#### Shape F.2 - Path Control" heading. Title:

    ##### Brain-suggestion variant (Plan 121.5-10 LOCKED)

    Content (drawing from audit Section 5.2):
    - When the F.1 selector consumes Brain-ranked next moves (the 5 surfaces: /mos:suggest-next, /mos:act --chain pre-gate, tension-hook-agent, auto-explore-agent, reverse-salient-agent), the renderer applies a LOCKED visual variant per the Phase 121.5 selector audit Section 5.2.
    - Header chip: `[■ BRAIN]` (literal). Distinct from `[GATE]` / `[CONTEXT]` / `[NEXT MOVE]` chips by the leading filled-square glyph.
    - Question line: `Choose next move:` (or per-surface variant per Section 5.3 adoption diffs -- tension surfaces use "Resolve pending tension:", BQ surfaces use the verbatim Brain BQ name).
    - Option rows: TWO LINES per row. Row 1 = `<glyph> <N>. <Run Verb>` left-padded + `<conf>%` right-aligned to 80-col boundary. Row 2 = 5-space indent, `<framework category> · <graph relationship>`. Glyphs: `▶` (right-triangle-filled) for >= 0.7 confidence top pick; `▷` (right-triangle-empty) for sub-0.7 alternatives.
    - Footer: stat-strip line `▶ Brain · top-<K> of <N> ranked · <color> = informing`.
    - Zone 1 left-rail color: cyan default. Yellow-on-cascade for CONTRADICTS edges DEFERRED to v1.13.2 hotfix.
    - Full template + slot-value table + anti-patterns rejected: `docs/F-SELECTOR-CONSUMER-GUIDE.md` Section 4.

    Step 2: Add the body_shape vs F-shape orthogonality note to `skills/ui-system/SKILL.md` Section 2.
    Location: insert a new paragraph after the existing opening note (the "> This section documents the current Shape F sub-shape catalog..." block at line 40). Content per LOCKED decision 4:

    > Orthogonality note (Plan 121.5-10 LOCKED decision 4): `body_shape:` frontmatter and Shape F sub-shape are ORTHOGONAL axes. body_shape describes the LAYOUT discipline of the command body (Shape A Mondrian Board, Shape B Semantic Tree, Shape C Room Card, Shape D Document View, Shape E Action Report). Shape F.x describes the SELECTOR CONTRACT that fires at the close of the body to capture the navigator's next-move decision. A command can carry body_shape: B (Semantic Tree layout) AND surface an F.1 selector at its close; these are not competing values -- they describe different surfaces of the same render. Example: `/mos:suggest-next` body_shape: B (renders the ranked list as a tree) plus F.1 selector (the verb-pick gate beneath the tree). The /mos:hmi-status doctor check enforces body_shape coverage; the F-selector ranker enforces F-shape contracts. The two are not double-counted.

    Step 3: Extend `docs/F-SELECTOR-CONSUMER-GUIDE.md` with a NEW Section 4 titled "The Locked Template for Brain-Suggested Selectors" per audit punch list item 7.
    Location: insert AFTER the "Typical wiring (pseudocode)" section (after line 109 today, before "What each consumer surface owns" -- renumber subsequent sections downstream).
    Content: republish the audit Section 5.2 slot-value table + the Section 5.2.2 visual mockup + the Section 5.2.3 anti-patterns-rejected list verbatim. Add a cross-reference to `.planning/121.5-selector-coverage-audit.md` Section 5 (the source of truth) and to Plan 121.5-10 (the implementing plan). Note that this template is LOCKED -- any future Brain-suggestion consumer MUST follow this shape; any divergence triggers the `tests/test-no-bespoke-brain-prompts.sh` CI tripwire.

    Step 4: Create `tests/test-no-bespoke-brain-prompts.sh` per audit punch list item 6. Bash shell script (per Mindrian repo convention; no em-dashes). Behavior:
    - Greps `commands/*.md` AND `lib/agents/*.cjs` AND `scripts/*.cjs` for known bespoke-prompt patterns: `Ready to`, `Want to`, `Should I`, `Pick one`, `continue / stop`, bare `[GATE]` text (NOT inside an F.0 pickShape call), bare `[continue]`, bare `[stop]`.
    - For each match, prints file + line + matched pattern to stderr.
    - Exit 0 if no matches (after subtracting the legitimate F.0 chain-step header that uses the literal `[GATE]` chip per audit Section 5.3 act --chain adoption -- which is the F.0 pickShape call, NOT bespoke prose).
    - Exit 1 if any match.
    - Allowlist: shell comments inside the test itself; the 121.5-10 plan and audit doc paths.

    Implementation outline:
    ```
    #!/usr/bin/env bash
    # Plan 121.5-10 Sub-plan K CI tripwire -- audit punch list item 6.
    # Exits 1 if any future commit reintroduces a bespoke Brain-suggestion prompt pattern.
    # Bash only. No em-dashes. No emoji.
    set -uo pipefail
    REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")"/.. && pwd)"
    PATTERNS=( "Ready to" "Want to" "Should I" "Pick one" "continue / stop" )
    SCANS=( "$REPO/commands" "$REPO/lib/agents" "$REPO/scripts" )
    EXCLUDES=( "tests/test-no-bespoke-brain-prompts.sh" ".planning/121.5-selector-coverage-audit.md" ".planning/phases/121.5-terminal-coherence-capstone/121.5-10-PLAN.md" )
    # ... grep, dedupe against allowlist, exit-code by hit count
    ```
    First run against the post-Task-2 tree MUST exit 0 (zero hits) -- proving the lock is live.

    Step 5: Update `tests/run-all-121.5.sh` SHELL_SUITES (currently empty array): add `test-no-bespoke-brain-prompts.sh` with a per-line plan-ownership comment.

    Step 6: Add a `CHANGELOG.md` entry under v1.13.0 final (the in-progress entry, not a new top-level version block). Entry structure:

    ### Added (Phase 121.5 Sub-plan K -- Plan 121.5-10)
    - Locked Brain-suggestion content template across all 5 consumers (/mos:suggest-next, /mos:act --chain, Phase 116 tension-hook-agent, Phase 117 auto-explore-agent, Phase 89-07 reverse-salient-agent): canonical `[■ BRAIN]` chip + score-right two-line option rows + stat-strip footer. Source: `.planning/121.5-selector-coverage-audit.md` Section 5.
    - Promoted `/mos:suggest-next` from NONE (silent dispatch) to F.1 via rankForSelector + pickShape (audit Section 6 item 2).
    - Promoted `/mos:act --chain` from BESPOKE bracket text (`[continue]` / `[stop]`) to F.0 Mini Decision Gate via pickShape (audit Section 6 item 3).
    - Selector verb-label aliases: dispatcher carries alias_map (Resolve -> Run Methodology, Explore -> Run Methodology, Later -> Defer, Skip -> Free-Text) per LOCKED decision 1; aliases render to the user, canonical verbs persist to graph edges.
    - CI tripwire `tests/test-no-bespoke-brain-prompts.sh` enforces the lock; any future bespoke-prompt reintroduction in commands/*.md, lib/agents/*.cjs, or scripts/*.cjs triggers a CI fail (audit Section 6 item 6).
    - `docs/F-SELECTOR-CONSUMER-GUIDE.md` Section 4 (NEW): publishes the locked template + slot-value table + visual mockup + anti-patterns rejected, so future Brain-suggestion consumers discover the lock before inventing a new pattern (audit Section 6 item 7).
    - `skills/ui-system/SKILL.md` Section 2: added the Shape F.1 Brain-suggestion variant subsection + body_shape vs F-shape orthogonality note (LOCKED decision 4).

    ### Deferred to v1.13.2
    - CONTRADICTS color flip (cyan -> yellow) for Brain-suggestion selectors when any candidate carries a CONTRADICTS edge. LOCKED decision 3: ship cyan default; yellow-on-cascade lands as v1.13.2 hotfix if testers report missed warnings.

    ### Deferred to v1.14.0
    - F.1 close-out adoption on the 71 N/A methodology commands (think-hats, structure-argument, grade, deep-grade, validate, etc.). LOCKED decision 5: out of scope for Phase 121.5; methodology-body conversational close stays acceptable per Phase 88.2 design.
  </action>
  <verify>
    <automated>grep -q "Brain-suggestion variant" skills/ui-system/SKILL.md && grep -q "The Locked Template for Brain-Suggested Selectors" docs/F-SELECTOR-CONSUMER-GUIDE.md && test -x tests/test-no-bespoke-brain-prompts.sh && bash tests/test-no-bespoke-brain-prompts.sh && bash tests/run-all-121.5.sh && grep -q "Plan 121.5-10" CHANGELOG.md</automated>
    SKILL.md variant subsection + orthogonality note added; F-SELECTOR-CONSUMER-GUIDE Section 4 published; tripwire exits 0 today; cumulative tests GREEN; CHANGELOG cites the plan.
  </verify>
  <acceptance_criteria>
    - skills/ui-system/SKILL.md Section 2 carries the Shape F.1 "Brain-suggestion variant (Plan 121.5-10 LOCKED)" subsection AND the body_shape vs F-shape orthogonality note (LOCKED decision 4).
    - docs/F-SELECTOR-CONSUMER-GUIDE.md has a NEW Section 4 titled "The Locked Template for Brain-Suggested Selectors" with the slot-value table + visual mockup + anti-patterns from audit Section 5.2 verbatim.
    - tests/test-no-bespoke-brain-prompts.sh exists, is executable, exits 0 against today's tree (proving the lock is live post-Task-2).
    - tests/run-all-121.5.sh SHELL_SUITES carries test-no-bespoke-brain-prompts.sh.
    - CHANGELOG.md carries the Plan 121.5-10 entry under v1.13.0 final with the Added + Deferred-to-v1.13.2 + Deferred-to-v1.14.0 subsections.
    - bash tests/run-all-121.5.sh exits 0 with all cumulative suites GREEN (selector-alias-map + brain-suggestion-template + test-no-bespoke-brain-prompts plus all earlier suites).
  </acceptance_criteria>
  <done>
    Lock published in three surfaces: SKILL.md (the canon doc), F-SELECTOR-CONSUMER-GUIDE.md (the integration guide), CHANGELOG.md (the release surface). CI tripwire prevents regression. Bundle A items 1+2+3+6+7 from the audit punch list all delivered.
  </done>
</task>

</tasks>

<verification>
- Plan 121.5-10 closes Sub-plan K of Phase 121.5 + 5 audit items (Bundle A: 1, 2, 3, 6, 7).
- All 5 Brain-suggestion consumers render the locked template: chip `[■ BRAIN]` + question line + two-line dense option rows + stat-strip footer.
- `/mos:suggest-next` promoted from NONE to F.1 (audit Section 6 item 2 -- THE highest-leverage promotion per audit Executive Summary).
- `/mos:act --chain` promoted from BESPOKE to F.0 (audit Section 6 item 3 -- second-highest leverage).
- Verb-label aliases LOCKED per decision 1: dispatcher.alias_map carries the 4 LOCKED entries; aliases render, canonical verbs persist to graph edges.
- CI tripwire `tests/test-no-bespoke-brain-prompts.sh` exits 0 today; exits 1 on bespoke-pattern reintroduction.
- `docs/F-SELECTOR-CONSUMER-GUIDE.md` Section 4 publishes the lock per audit Section 5.2.
- `skills/ui-system/SKILL.md` carries the F.1 Brain-suggestion variant subsection + body_shape vs F-shape orthogonality note (decision 4).
- bash tests/run-all-121.5.sh exits 0 with all cumulative suites GREEN (3 new suites: selector-alias-map, brain-suggestion-template, test-no-bespoke-brain-prompts).
- Canon Part 3 audit: the Brain-suggestion variant IS the F.1 selector contract honored across 5 consumers; one chip + one question shape + one footer = the Decision Gate as a coherent surface.
- Canon Part 4 audit: every locked-template selection produces a typed graph edge with verb_canonical (alias_map collapsed); rejections capture REJECTED_BECAUSE; misses capture user_intent LOCALLY. Per Part 4 every choice is graph data.
- Canon Part 7 audit: ZERO new commands. 3 new test files + 1 new jtbd-taxonomy.json block + locked-template publication. The plan EXTENDS shipped substrate (Phase 88.2 renderers + Phase 101-01 dispatcher + Phase 125 ranker) and CONSOLIDATES 5 divergent consumers into 1 shape. Per Part 7 reuse-before-build.
- Canon Part 8 audit: ZERO new Brain queries. The template is a RENDER contract on outputs already produced by shipped Brain query paths. alias_map LOCAL in jtbd-taxonomy.json. Verb_chosen via navigation.cjs chokepoint (Phase 109). No user content egresses.
</verification>

<success_criteria>
- All 5 Brain-suggestion consumers render `[■ BRAIN]` chip + locked template.
- /mos:suggest-next NONE -> F.1; /mos:act --chain BESPOKE -> F.0.
- selector-dispatcher carries alias_map plumbing; jtbd-taxonomy carries the 4 LOCKED aliases.
- F-SELECTOR-CONSUMER-GUIDE Section 4 publishes the lock.
- SKILL.md F.1 variant subsection + orthogonality note (decision 4) added.
- 3 new tests GREEN; CI tripwire enforces the lock.
- CHANGELOG flags v1.13.0 Added + v1.13.2 deferred (decision 3) + v1.14.0 deferred (decision 5).
- Sub-plan K of Phase 121.5 closes; Bundle A items 1+2+3+6+7 from audit punch list delivered.
- Plan 121.5-09 wave 4 coherence smoke test re-runs cleanly with Plan 121.5-10 in its depends_on (verified by 121.5-09-PLAN.md frontmatter update).
</success_criteria>

<notes>
The 5 LOCKED design decisions are recorded verbatim here so future maintainers reading this plan understand the navigator's reasoning (audit Section 7 Open Questions, answered 2026-05-23):

1. **Verb labels: ALLOW registered aliases with mapping (NOT force-canonical).** The agents argue for aliases (Resolve / Later / Skip in Phase 116 tension hook; Explore / Skip / Later in Phase 117 auto-explore) because the contextual verbs read more pedagogically in their specific surfaces than the canonical `Run Methodology / Defer / Free-Text`. The dispatcher argues for canonical because the graph edges should carry one stable vocabulary. Decision: BOTH. alias_map carries the mapping; aliases render to the user; canonical verbs persist to the graph. The render-vs-persist split honors both arguments without forcing one.

2. **F.4 vs F.0 for `/mos:operator reset` confirmation.** F.0 (architectural fit). F.4 Insight Extraction has a Back option that maps to Cancel, which gives a semantic fit for "extract my reset intent." But F.0 has Approve / Reject / Defer which maps Approve = Confirm reset, Reject = Cancel, Defer = silent dismiss with memory_event -- and F.0 is the canonical "binary-with-escape" gate per Canon Part 3. F.0 wins. This decision is recorded for Plan 121.5-08 Sub-plan J D-12 (it's actually executed there, not in this plan); the decision is captured here so the audit trail lives next to the rest of Bundle A.

3. **CONTRADICTS color flip (cyan -> yellow): DEFER past Bundle A.** The locked template proposes yellow-on-cascade when any candidate carries a CONTRADICTS edge against an existing assumption. This requires the consumer to walk the cascade graph BEFORE rendering -- adding a SQL call to the rank path. We chose to ship cyan default in v1.13.0 and let yellow-on-cascade land as a v1.13.2 hotfix IF testers report missed warnings. Ship simple, observe, then layer in if the data says it matters.

4. **`body_shape: B` vs `F.1` for `/mos:suggest-next`: KEEP B.** body_shape and Shape F.x are orthogonal axes. body_shape describes the LAYOUT discipline (Shape B Semantic Tree is the visual rendering of a ranked list as a tree). Shape F.1 describes the SELECTOR CONTRACT (the verb-pick fork that fires at the close of the tree). The two can coexist; the doctor / hmi-status compliance check counts body_shape; the F-selector ranker counts F-shape. They are not double-counted. The orthogonality note in SKILL.md Section 2 documents this so future readers don't try to unify them.

5. **71 N/A methodology commands' close-out F.1: OUT OF SCOPE for 121.5. Defer to v1.14.0.** Five methodology commands (think-hats, structure-argument, grade, deep-grade, validate) produce rich enough output that an explicit F.1 close-out would be load-bearing. But the Phase 88.2 design legitimately delegates the methodology close to Larry's conversational follow-up. We accept that design for v1.13.0 final and queue the F.1 close-out adoption as a v1.14.0 task. Scope discipline.

Cross-reference: this plan completes Bundle A of `.planning/121.5-selector-coverage-audit.md` Section 6 punch list (items 1, 2, 3, 6, 7). Items 4 + 5 (PARTIAL sweep + BESPOKE non-Brain promotions) live in Plan 121.5-08 Sub-plan J already (the naming-drift sweep) and are out of scope here.
</notes>

<output>
After completion, the executor produces:
- `.planning/phases/121.5-terminal-coherence-capstone/121.5-10-SUMMARY.md` (per the standard plan summary pattern; one section per task with the verify-block evidence inline)
- Grep-verified evidence that the chip `[■ BRAIN]` appears in all 5 consumer files (counter output >= 5).
- bash tests/run-all-121.5.sh exit-0 transcript with the 3 new suites listed (selector-alias-map, brain-suggestion-template, test-no-bespoke-brain-prompts).
- CHANGELOG.md preview of the Plan 121.5-10 entry under v1.13.0 final.
</output>
</content>
</invoke>