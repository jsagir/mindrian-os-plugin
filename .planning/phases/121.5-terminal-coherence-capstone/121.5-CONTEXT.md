---
phase: 121.5
slug: terminal-coherence-capstone
status: scoped (ready for /gsd:plan-phase 121.5)
created: 2026-05-10
updated: 2026-05-16
milestone: v1.13.0
beta_target: final (runs AFTER 118/119/120/121, immediately BEFORE the v1.13.0 FINAL RELEASE GATE)
absorbed_2026_05_16:
  - Bulletproof ASCII /mos:help renderer (NEW Sub-plan I)
  - Naming-drift sweep + lifecycle cleanup -- 8 commands (NEW Sub-plan J)
  - 16 implementation decisions captured (D-01 through D-16)
absorbed_2026_05_23:
  - Brain-suggestion content template lock + selector audit Bundle A (NEW Sub-plan K, Plan 121.5-10)
  - Source artifact: .planning/121.5-selector-coverage-audit.md (5,396 words, 7-item punch list, Bundle A scope locked)
  - 5 additional design decisions LOCKED (D-Bundle-A-01 verb-label registered-alias mapping; D-Bundle-A-02 F.0 for /mos:operator reset; D-Bundle-A-03 defer CONTRADICTS color-flip past Bundle A; D-Bundle-A-04 keep body_shape B + document orthogonality; D-Bundle-A-05 71 N/A methodology close-out F.1 out-of-scope, defer v1.14.0)
  - Phase 121.5-VERIFICATION.md reopened (status complete -> amended) per re-seal flow; target re-seal as complete / 11/11 after Plan 10 lands + coherence smoke re-runs GREEN
  - Drift reconciliation at .planning/phases/121.5-terminal-coherence-capstone/121.5-DRIFT-RECONCILIATION.md confirms Plans 00..09 substrate unchanged and safe to remain as shipped; only Sub-plan K is net-new work
canon_parts: [Part 3, Part 4, Part 7, Part 8, Part 10]
depends_on:
  - Phase 88.1 (UI/UX polish + MINTO surface plumbing) -- shipped
  - Phase 88.2 (UIUX selector block, Shape F.0-F.6) -- shipped
  - Phase 102 (context-aware rendering, lib/render/render-v2.cjs) -- shipped, VERIFICATION pending
  - Phase 104 (per-command JTBD declarations) -- shipped
  - Phase 105 (HMI compliance polling) -- shipped
  - Phase 106 (statusline visibility + context-window broadcast) -- shipped
  - Phase 114 (larry-default-activation) -- human-needed gate pending
  - Phase 115 (owned-emotion dual-path first touch) -- human-needed gate pending
  - Phase 116 (unresolved-tension hook) -- human-needed gate pending
  - Phase 117 (auto-explore-domains on first material) -- shipped
  - Phase 119 (room-as-receipt invariant) -- final wave
  - Phase 121 (trajectory telemetry) -- spans beta.1->final
dependents:
  - v1.13.0 FINAL RELEASE GATE (this phase is the last substantive work before the gate)
  - Canon Part 10 ratification (the terminal must coherently express "conversation as the product")
estimated_days: 3-4
related_seeds: [SEED-007 (version-dynamic first-touch greeting -- absorbed as a sub-concern here), SEED-003 A-items (Claude Code 2.1.x adoption -- output-style + force-for-plugin land here)]
reference_docs:
  - 121.5-REFERENCE-destijl-guide-annotated.md (the "De Stijl Full UI/UX Implementation Guide" + verification annotations -- TREAT AS REFERENCE, NOT SPEC)
---

# Phase 121.5 -- Terminal Coherence Capstone (the JTBD product, made coherent in the terminal)

## ✓ VERDICT LOGGED (2026-05-16) -- Hold-flag retracted

**Architectural review of the dual-graph proposal (DGEKT-inspired ASSOCIATION_LENS + TRANSITION_LENS lens-engine extension) concluded 2026-05-16. Three independent reviewers were run: Agent A (canon compliance), Agent B (execution-plan contract), Agent C (adversarial framing). Their deliverables are filed at `.planning/research/2026-05-16-dual-graph-review-A-canon-compliance.md`, `.../review-B-execution-plan-contract.md`, and `.../review-C-adversarial.md`. The proposal itself is at `.planning/research/2026-05-16-dual-graph-architectural-proposal.md`.**

**Synthesized verdict:** REJECT the architectural framing. APPROVE two minimal additive primitives in v1.13.1. DEFER the rest to v1.14.0 as a plant-seed. The DGEKT-inspired lens-class taxonomy is overspecified relative to what the three gaps actually require; the canonical workflow's "current vs final" language discipline (preserved across Sub-plans D / E / F / G / H / I / J) is sufficient protection without the hold-flag's forcing function. Per Canon Part 4, the rejection is itself graph data: this decision will land as an `architectural_proposal_decided` memory_event when the v1.13.1 primitives ship.

**What survives in v1.13.1 (additive primitives, NOT lens-class taxonomy):**
- **FOLLOWS_FROM cascade edge type** rides Phase 129 (spine-repair memory_event emission). Extends the existing 7-type cascade vocabulary (INFORMS / CONTRADICTS / CONVERGES / INVALIDATES / ENABLES / REJECTED / DEFERRED) without introducing parallel lens classes. ~30 lines, additive to `lib/core/navigation/memory-events.cjs`.
- **`local-chain-recommender.cjs`** rides Phase 127 as a plain Tier-LOCAL helper. Closes the Phase 127 Tier-LOCAL predictive-surface gap that Phase 127 itself opens; walks local `memory_event` aggregates only. ~80 lines, sibling to `lib/brain/chain-recommender.cjs`.

**What gets deferred to v1.14.0 (plant-seed at `.planning/seeds/2026-05-16-learned-ranker-weights.md`):**
- F-selector ranker feedback-loop with adaptive weight updates from outcome edges. Trigger: cohort >= 30 testers AND F-selector outcome edge count (REJECTED + DEFERRED + accepted) >= 1000 events accumulated. Until then, the 0.40 / 0.30 / 0.30 hardcoded weights in `lib/workflow/f-selector-ranker.cjs:47-52` remain canonical.

**What gets rejected entirely:**
- ASSOCIATION_LENS / TRANSITION_LENS as first-class lens classes in Phase 130's lens-engine skeleton. Phase 130 ships the cognitive-family migration scope as the execution plan locks; no new lens classes named in v1.13.1.
- "Graph-native" redefinition in Phase 131 to mean tri-lens ensemble (source + association + transition). Phase 131's "graph-native" stays defined by Phase 130's actual v1.13.1 deliverables (cognitive lens only).
- Any cross-phase amendment to 127.1 / 128 / 129 / 130 / 131 CONTEXT.md files that names lens-class vocabulary. The minimal primitives above ride Phase 127 and Phase 129 as additive scope expansions, NOT as architectural-naming amendments.

**Effect on this phase (121.5):**
- ZERO sub-plan rollback. Plans 121.5-00 through 121.5-09 continue as authored.
- Decisions D-01 through D-16 stay locked and canonical.
- The "current vs final" language discipline this hold-flag imposed STAYS as a Canon-Part-7 consolidation invariant -- not because of an in-flight review, but because consolidation phases (per Canon Part 7) preserve extensibility headroom by convention. Sub-plans D / E / F / G / H / I / J should still use "current scoring signals" / "current statusline segments" / "current Shape F catalog" language as a permanent linguistic discipline.

**Authority for this verdict:**
- Room session 2026-05-16 (Jonathan + Larry).
- Three review deliverables cited above.
- Canon Parts 3 (Tri-Context Decision Gate -- this verdict IS the gate), 4 (the rejection becomes graph data via `architectural_proposal_decided` event), 7 (Reuse-Before-Build -- the additive primitives extend shipped substrate; lens-class taxonomy is rejected as net-new ceremony), 8 (Graph Boundary preserved by enum-only projections from memory_event), 9 (Memory Locality -- SQL remembers; this proposal asked SQL to remember its own movement, and we accepted the minimal version of that ask via FOLLOWS_FROM).

---

**STATUS:** SCOPED 2026-05-16. 16 decisions locked (D-01..D-16) via discuss-phase pass on top of the 2026-05-10 stub; Sub-plans I + J absorbed; ready for `/gsd:plan-phase 121.5`. Body sweeps applied 2026-05-16 (post-ultrareview Bug 1) to reconcile previously-binding requirements with the locked decisions -- see annotations in Sub-plan D and Acceptance Criterion 5. Plans 121.5-00 through 121.5-09 written; remaining plans land at plan-phase time.

**This is the last phase before the v1.13.0 FINAL RELEASE GATE.** It does not
introduce a new product surface. It takes everything v1.13.0 already built --
Larry-by-default, the owned emotion, the tension hook, the auto-explore reward,
the selector library, the context-aware renderer, the statusline broadcast, the
JTBD engine -- and makes the **Claude Code terminal** express it as ONE coherent
product instead of N independently-shipped surfaces. Acknowledge first. Plan
second. Ship third. Then the gate.

## The single design statement

> The MindrianOS terminal IS the product surface. Every glyph, every status
> segment, every cold-start line, every injected directive, every command's
> output shape -- they all encode the same thing: *you are navigating a wicked
> problem with Larry, and here is the job you are doing right now.* If a user
> reads the terminal and can't tell what job they're in, who they're talking to,
> how much runway they have, or what the next move is -- the product is leaking.

This phase closes the leaks.

## Why this phase exists (the accumulated debt)

Two parallel audits + one external reference doc + one foundation review across
this milestone surfaced a consistent picture: the terminal-UI story is
**fragmented across ~18 phases of independent shipping**, each one correct in
isolation, none of them reconciled. The capstone exists to pay that down in one
deliberate pass before v1.13.0 final, rather than carrying it into v1.14.0.

Concrete debt this phase acknowledges and resolves:

1. **SessionStart injection flood.** ~11 SessionStart hook entries, ~9 capable of
   injecting `additionalContext` into Larry's turn-1, ~4 composing competing
   "Larry-voice directives" -- with **no coordinator and no combined budget**.
   This is the direct cause of Larry occasionally opening a session incoherent
   ("lost in the middle"). The repair is a single SessionStart Coordinator with a
   precedence ladder and a hard token budget. (Note: the "10,000 chars per hook"
   figure cited in the parallel research is illustrative, not a confirmed
   documented cap -- VERIFY before citing externally; the architecture is wrong
   regardless of the exact number.)

2. **The ruling system is ~45% enforced.** `skills/ui-system/SKILL.md` is canon,
   but only ~38 of ~85 command files carry `body_shape:` frontmatter. Phase 105's
   `/mos:hmi-status` can flag the other ~47 as "missing body shape" forever. The
   `body_shape:` sweep makes the compliance signal honest for the first time.

3. **`force-for-plugin` output style is unused.** Claude Code supports plugin
   output styles with `force-for-plugin: true` (auto-applies, overrides user
   setting) and `keep-coding-instructions: true` (preserves the coding system
   prompt). This is the strongest UI-enforcement lever Claude Code offers and the
   plugin doesn't use it. Shipping `output-styles/destijl.md` makes the 4-zone
   format system-prompt-enforced, not skill-instruction-suggested. This is the
   multiplier on the `body_shape:` sweep.

4. **SKILL.md has drifted from its own implementation.** SKILL.md §2 documents 5
   Shape-F sub-shapes; Phase 88.2 shipped 7 (added F.0 Mini Gate, F.6 Plan
   Review). SKILL.md does not mention Phase 102's dual De Stijl palette
   (`lib/render/JTBD-PALETTES.md`) even though Phase 102 D-06b explicitly queued
   the amendment. Reconcile SKILL.md to reality.

5. **`lib/render/render-v2.cjs` is built, tested, and almost unused.** Only
   `selector-dispatcher.cjs` and the agentic surfaces route through it; ordinary
   Larry prose still follows SKILL.md by instruction. Decide explicitly: wire it
   into the real prose path, or document it as agent-surface-only and let SKILL.md
   remain the canonical prose contract. Also: Phase 102 never got a VERIFICATION.md
   (`102-VALIDATION.md` still `status: draft`) -- close it.

6. **The statusline is one unreadably-long line.** Split into Row 1 (identity:
   Larry / Brain / context %) + Row 2 (situation: room / section / stage /
   governing thought / JTBD). Multi-line statuslines are a real, documented Claude
   Code feature. (`refreshInterval` is NOT real -- do not add it.)

7. **De Stijl palette + glyph set have no single source of truth.** Hex values
   live in `scripts/banner`, `lib/core/visual-ops.cjs` (`DS_HEX`),
   `templates/destijl-base.css`, `templates/shared.css`,
   `references/vault-kit/snippets/mindrian-destijl.css` -- and the external
   reference doc proposes a *different* (brighter, more literal-Mondrian) palette.
   Pick one. Create `references/visual/palette.json` as canon; every surface
   derives from it; CI asserts agreement.

8. **`🎯` is overloaded three ways** (JTBD broadcast / Phase 109 active-focus
   prefix / problem-definition stage emoji) and "JTBD" the word names two
   unrelated systems (Phase 37 bash nudge templates vs Phase 100 typed engine).
   Either disambiguate the glyphs/terms or document the overload as an explicit
   exception in SKILL.md §3 alongside the emoji carve-out.

9. **Stale version copy in first-touch surfaces (SEED-007).** A v1.12.0 "Three
   ways to do this" greeting was still rendering on a stale Windows install with
   no version-of-record visible. Absorbed here: every first-touch surface stamps
   the running version; a scanner catches stale version-tied copy.

10. **The Phase 88.7 / `/mos:power-demo` ROADMAP lie.** ROADMAP says "1/1 plans
    complete"; only `scripts/power-demo-prompt.md` exists, `commands/power-demo.md`
    does not. And the stray empty directory `.planning/phases/40-hook-expansion/`
    actively corrupts `scripts/context-monitor`'s active-phase mtime scan. Both
    are truth-telling / cleanup items folded into this capstone's housekeeping
    sub-plan.

## Explicitly REJECTED (do not build these)

The external "De Stijl Full UI/UX Implementation Guide" (filed as
`121.5-REFERENCE-destijl-guide-annotated.md`) contains good ideas wrapped around
fabricated or wrong ones. Verified against Claude Code docs:

- **Custom theme JSON files** (`~/.claude/themes/destijl.json`, `/theme
  mindrian-destijl`, hot-reload, configurable token list) -- **NOT REAL.** No
  user-facing custom-theme-file mechanism exists. Do not build against it.
- **Room/Stage state via `export MINDRIAN_ROOM` in a `UserPromptSubmit` hook** --
  **ARCHITECTURALLY WRONG.** Hook subprocess `export` dies with the subprocess.
  Env-var persistence works only in `SessionStart` / `Setup` / `CwdChanged` /
  `FileChanged` hooks, and only by writing to `$CLAUDE_ENV_FILE`. The codebase
  already solved room/stage state correctly with the bridge file
  (`~/.mindrian/bridge/{md5}.json`, Phase 54-02) + `statusline-mos`. The doc's
  approach is a regression. NOTE: `$CLAUDE_ENV_FILE` + `CwdChanged`/`FileChanged`
  IS a real mechanism the codebase may not be using -- worth evaluating, but not
  as a replacement for the bridge file.
- **`SessionStart` hook declared in a SKILL.md frontmatter** -- **NOT REAL** for
  session-level hooks. Skill `hooks:` frontmatter is skill-scoped only;
  session-level hooks must live in `hooks/hooks.json`.
- **`statusLine.refreshInterval`** -- **NOT REAL.** The statusline is event-driven.
- **Silently adopting the doc's brighter palette / new glyph set** -- that is a
  rebrand, not an alignment. It requires an explicit design decision (see scope
  item below), not a copy-paste.

## Explicitly ADOPTED from the reference doc (verified real)

- **`output-styles/destijl.md` with `force-for-plugin: true` + `keep-coding-instructions: true`** -- real, the best idea in the doc, the enforcement multiplier.
- **Two-row statusline** -- real, buildable, fixes readability.
- **The consolidated semantic vocabulary** -- 4-zone anatomy, 12-glyph table with
  one-meaning-each, 5-color semantics (RED=identity/Larry/error, BLUE=Brain/data/
  plan, YELLOW=context/memory/stage/caution, WHITE=default, BLACK=structure),
  anti-patterns list (no emoji, no padding phrases, no decorative dividers,
  no center-align) -- adopt as the SKILL.md v2 reconciliation, mapped onto the
  EXISTING glyph/palette values unless the design decision below says otherwise.
- **`PostCompact` as the correct event** for post-compaction context re-injection
  (vs the current SessionStart-with-`compact`-matcher, which may not be a real
  matcher) -- evaluate migrating the half-wired Phase 95.5 re-injection here.
- **`Notification` hook** for ANSI/desktop alerts on idle + permission -- real
  event; verify the payload var name (`CLAUDE_NOTIFICATION_MESSAGE` is not in the
  documented env-var table) before relying on it. Low priority.

## Scope (provisional, to be refined in 121.5-00)

### IN SCOPE

**Sub-plan A -- SessionStart Coordinator (the unlock).**
- Single script `scripts/sessionstart-coordinator.cjs` (or analogous) that owns
  the SessionStart `additionalContext` composition. Every current injector
  (room-context, sealed-room guardrails, MINTO banner, JTBD nudges, operator
  state, memory-resume-nudge, tension hook, auto-explore preflight,
  post-compact restore, onboarding gate, install-drift / preflight-doctor
  warnings, statusline-fallback-echo) becomes a *contributor* that returns a
  scored fragment; the coordinator ranks by a **precedence ladder** (the ladder
  is a design decision for Jonathan -- see "Open Design Decisions"), enforces a
  **hard combined token budget** (default ~2,000 chars; tune in 121.5-00), and
  emits one consolidated `additionalContext` block.
- Contributors that don't make the budget are dropped or compressed to a
  one-line pointer ("1 unresolved tension pending -- ask Larry") rather than
  injecting their full payload.
- Telemetry: every coordinator decision (which contributors fired, which were
  dropped, total bytes) mirrors to Phase 121's trajectory store.

**Sub-plan B -- `body_shape:` frontmatter sweep + output-style enforcement.**
- One agent pass adds `body_shape:` to the ~47 commands missing it, using the
  fixed command-to-shape mapping table from Phase 21 (extended for any new
  commands).
- Ship `output-styles/destijl.md` with `force-for-plugin: true` +
  `keep-coding-instructions: true` -- the 4-zone format becomes system-prompt
  enforced. `/mos:doctor` self-checks that its OWN output follows the 4-zone
  format (if it doesn't, the output style isn't loaded).
- After this, `/mos:hmi-status` reports a clean baseline for the first time.

**Sub-plan C -- SKILL.md v2 reconciliation.**
- Add Shape F.0 (Mini Decision Gate) and F.6 (Plan Review Round) to SKILL.md §2.
- Fold in Phase 102's dual-palette rule (the D-06b amendment that never landed).
- Document the `🎯` overload + "JTBD" word collision as explicit exceptions, OR
  disambiguate them (design decision -- see below).
- Cross-reference Canon Part 3 (UI Ruling System) + Part 10 (Conversation as
  Product). Result is the single source of truth for the terminal, accurate to
  the shipped code.

**Sub-plan D -- Statusline RE-DESIGN (co-design mandatory, JTBD-driven) + canonical palette token file.**

> **BINDING REQUIREMENT (added 2026-05-14 by Jonathan; SUPERSEDED 2026-05-16 by Decisions Locked D-01..D-04 captured in the discuss-phase pass and recorded in 121.5-DISCUSSION-LOG.md Area 1).** Historical text preserved for audit: the statusline shape is NOT pre-decided by this CONTEXT.md. The current `🏠 MindrianOS-Plugin ▶ 📂 product-evolution │ 🔍 PRODUCT EVOLUTION │ 🧠 MindrianOS v1.13.0-beta.13 🔄 /mos:update │ 🧬 BRAIN │ Opus 4.7 (1M context) 📊 ████░░░░░░ 47%` line is one accumulated artifact across ~8 phases (Phase 5, 17, 23, 54, 83, 88.1, 94, 106, 109), each adding a segment in isolation. Phase 121.5 must **co-design** the new shape with the navigator (Jonathan) in the discuss-phase pass before any code lands. The framing question was: **for each JTBD, what does the user need to see at a glance, and what is noise?** Co-design completed 2026-05-16; the resolved decisions are: D-01 JTBD-invariant single shape (the co-design concluded JTBD-adaptive shape adds complexity without earning its read; one spec wins); D-02 two-row layout (identity row / situation row split); D-03 Row 1 = Larry/version + Brain tier + context % (update glyph conditional); D-04 Row 2 = room ▶ section │ JTBD │ STAGE │ governing thought (truncated) │ operator. The HARD RULE that the statusline shape is navigator-owned is preserved -- it was navigator-decided in the 2026-05-16 co-design pass, not orchestrator-decided. Steps below have been rewritten to match the locked decisions; ultrareview 2026-05-16 Bug 1 reconciliation applied.

- **Step 1 (discuss-phase work, COMPLETED 2026-05-16):** the discuss-phase pass for 121.5 included the live co-design session with Jonathan on the statusline. Inputs that informed the locked decisions:
  - The 13-JTBD taxonomy from `lib/hmi/jtbd-taxonomy.json` (shipped Phase 100-01)
  - The `serves_jtbd:` frontmatter coverage from Phase 104 (every command declares its job)
  - The operator state machine from Phase 105 (JUST_TALK / EXPLORE_CAPTURE / BUILD_ROOM / METHODOLOGY / DECISION_GATE)
  - Audit of the current line's segments + which JTBD each segment serves (or fails to serve)
  - The accumulated debt from this CONTEXT.md item 6 (the line is unreadable as one row)
- **Step 2 (output of co-design, LOCKED via D-01..D-04):** a written layout spec in `121.5-STATUSLINE-DESIGN.md` capturing the JTBD-invariant single-shape contract:
  - Row 1 (identity) load-bearing segments: Larry/version + Brain tier + context % (update glyph conditional)
  - Row 2 (situation) load-bearing segments: room ▶ section │ JTBD │ STAGE │ governing thought (truncated) │ operator
  - The shape does NOT vary by JTBD (D-01); JTBD becomes one segment in Row 2 rather than the dimension the whole layout adapts on
  - Transition behavior: Row 2's JTBD segment updates in place when JTBD changes mid-session; the row shape itself stays fixed
  - Surface adaptation: CLI vs Desktop vs Cowork (Desktop has no statusline -- prose echo per the existing pattern; both rows compress to one prose line)
  - Palette + glyph choices apply uniformly across the single shape
- **Step 3 (implementation):** `scripts/context-monitor` is refactored per the locked spec. Two-row identity/situation split (D-02), JTBD-invariant (D-01). Preserve the `📊` context-budget glyph + compaction-imminent blink-red at >=80% behavior. Preserve the emoji carve-out (SKILL.md §3) for surfaces where it applies; the design spec confirms or revises this.
- **Step 4 (palette).** Create `references/visual/palette.json` as the canonical De Stijl palette per Open Design Decision 3. `lib/core/visual-ops.cjs` (`DS_HEX`), `scripts/banner`, `templates/destijl-base.css`, `templates/shared.css`, and `references/vault-kit/snippets/mindrian-destijl.css` all derive from it (or a CI check asserts they agree).
- **Step 5 (wrapper migration).** Wire `statusline-mos` (the version-resolving wrapper, Phase 83) as the canonical `settings.json` `statusLine.command` so plugin updates don't strand users on a stale path. `migrate-stale-user-settings.cjs` migrates toward it.

**Why this was co-design and not orchestrator-decided:** the statusline is the user's primary peripheral-vision interface for "what job am I in, who am I talking to, how much runway, what's next" (Canon Part 10). Asking an agent to redesign that surface in isolation would reproduce the exact failure mode this phase exists to fix: N independent decisions, none reconciled to the user's actual job. The navigator decided in the 2026-05-16 co-design pass; the implementation steps above execute that decision. Per Canon Part 3 (the Decision Gate) + Canon Part 1 (the user is the wicked navigator), the statusline shape is a navigator-owned design call -- and the navigator made it.

**Sub-plan E -- render-v2 disposition + Phase 102 closure.**
- Decide: wire `lib/render/render-v2.cjs` into the actual Larry prose path, OR
  document it (in `lib/render/ROOM.md`) as agent-surface-only by design with
  SKILL.md as the canonical prose contract.
- Run `/gsd:validate-phase 102` -- produce `102-VERIFICATION.md`, flip
  `102-VALIDATION.md` out of `status: draft / nyquist_compliant: false`.

**Sub-plan F -- version-of-record on first-touch (SEED-007 absorption).**
- Every first-touch surface (banner, `/mos:splash`, cold-start greeting,
  onboarding) stamps the running plugin version. A scanner (extend `doctor.cjs`)
  flags version-tied marketing copy that has gone stale. Em-dash hard-rule check
  on greeting strings.

**Sub-plan G -- truth-telling + housekeeping.**
- Delete the stray empty `.planning/phases/40-hook-expansion/` directory.
- Fix the Phase 88.7 ROADMAP status line ("1/1 plans complete" -> actual: 1
  prompt doc shipped, command + 5 sub-plans unbuilt; mark deferred or schedule).
- Audit `context-monitor`'s `detectActiveWorkflow()` mtime scan for other
  empty/abandoned phase dirs that could mis-report the active phase.
- Optional: evaluate `PostCompact` migration of the Phase 95.5 re-injection;
  evaluate `Notification` hook adoption; evaluate `$CLAUDE_ENV_FILE` use in
  `CwdChanged`/`FileChanged`.

**Sub-plan H -- acceptance: the coherence smoke test.**
- A fresh-install, fresh-session walk: cold start (banner + version + owned
  emotion) -> first material upload (room-as-receipt fires, ONE coherent
  greeting, not a flood) -> a `/mos:*` command (4-zone output, correct body
  shape) -> a decision gate (Shape F renders) -> a tension re-surfaces next
  session (coordinator ranks it, single clean directive). Byte-identical across
  CLI; degraded-but-coherent on Desktop/Cowork (prose echo). A human reads the
  terminal at each step and can answer: who am I talking to, what job am I in,
  how much runway, what's next. If they can't -> the phase isn't done.

**Sub-plan I -- Bulletproof ASCII `/mos:help` (ABSORBED 2026-05-16).**
Current `commands/help.md` uses ANSI 24-bit truecolor escapes
(e.g. `\033[38;2;166;61;47m`) that break on terminals without truecolor
support, on Desktop chat surface, and in any non-TTY pipe. The renderer
must work bulletproof across CLI / Desktop / Cowork and surface ALL
export/publish/hub commands with explicit JTBD "what's in it for me"
framing.

- `scripts/help-renderer.cjs` (new) -- truecolor-vs-ASCII dispatch via
  new `lib/core/terminal-capability.cjs` (probes `process.stdout.isTTY`
  AND `$COLORTERM=truecolor`/`256color`; reusable for /mos:status,
  /mos:doctor, /mos:splash). Color path when both pass; ASCII path
  (12-glyph vocabulary only, no `\033` escapes) otherwise. (Decision D-05)
- `commands/help.md` rewritten to delegate rendering to
  `scripts/help-renderer.cjs`. The 4-zone format becomes deterministic
  regardless of terminal capability.
- `data/help-groups.json` (new) -- declares the 10 Zone-2 groups (7 of
  the existing 8 + the new Export/Publish/Hub trio that replaces the
  legacy "Output + Export" group: 7 + 3 = 10). Each group entry lists
  the commands that belong to it. (Decision D-06)
- `commands/<name>.md` frontmatter gains a `help_jtbd:` field -- a
  one-line "what's in it for me" outcome description. Each command
  owns its own JTBD line (distributed maintenance); the renderer
  composes the help page by joining groups + per-command JTBD lines.
- Three new groups replace the single "Output + Export": **Export**
  (PDF/vault: `/mos:export`, `/mos:room linkify`), **Publish** (live URL:
  `/mos:publish`, `/mos:present --share`), **Hub** (browser/cron:
  `/mos:dashboard`, `/mos:wiki`, `/mos:scheduled-tasks`,
  `/mos:rooms new --github`). Destination-first mental model:
  "I want my work to end up at X." (Decision D-07)
- `scripts/check-help-coverage.cjs` (new) -- CI guard. Hard-fails
  pre-commit AND CI on (a) any command without `help_jtbd:` frontmatter,
  (b) any visibility:non-admin command missing from
  `data/help-groups.json`. Closes the Cluster 5 audit finding: 3 commands
  (doctor / heal / hmi-status) silently absent from current help.md.
  (Decision D-08)
- Tests: `tests/test-help-renderer-bulletproof.cjs` -- 3 torture tests:
  (a) pipe through `tr -d $'\033'`, assert sentinel glyphs in each group;
  (b) capture in non-TTY (heredoc / file redirect), assert ASCII-only;
  (c) render in Desktop-simulator (no ANSI), assert human-readable.

**Sub-plan J -- Naming-drift sweep + lifecycle cleanup (ABSORBED 2026-05-16).**
Cluster 5 audit (2026-05-15) found 4 confusingly-named diagnostic
commands, 3 invisible in /mos:help, a `/mos:mos` declared in
plugin.json with no backing file, and 2 Canon Part 3 violations
(onboard / diagnose recommendation surfaces without F.1 selectors).
This sub-plan ships the cleanup as zero-logic-risk soft-alias work
that fits the capstone's truth-telling pillar.

- **Soft-alias deprecation policy** (Decision D-09): old commands stay
  as 1-line stubs that print a deprecation note + invoke the new
  command transparently. Zero tester breakage. Removal scheduled
  for v1.14.0. CHANGELOG flags all renames.
- `/mos:heal` -> fold into `/mos:doctor --heal-room` (heal's 10-step
  orchestrator becomes doctor's class-E fix engine). `commands/heal.md`
  becomes soft-alias stub.
- `/mos:query` -> alias for `/mos:graph "<question>"`. The terminal
  Q&A pattern collapses into one surface. `commands/query.md` becomes
  soft-alias stub.
- `/mos:organize` -> fold into `/mos:rooms organize <verb>`. Portfolio
  grouping moves under multi-room management. `commands/organize.md`
  becomes soft-alias stub.
- `/mos:visualize` -> retire or fold into `/mos:dashboard --mermaid`
  (decision deferred to plan-phase; 5 other commands already cover
  graph visualization).
- `/mos:hmi-status` -> fold into `/mos:doctor --ui-compliance --json`
  (Decision D-11). The standalone command goes away; functionality
  lives in doctor. Any scripts/hooks calling `/mos:hmi-status`
  migrate to the new invocation. Soft-alias for one release; remove
  v1.14.0.
- `/mos:diagnostics` -> rename to `/mos:fingerprint`. Kills the
  diagnose/diagnostics naming ambiguity. Soft-alias stub for one
  release.
- `commands/mos.md` -- CREATE. State-aware router (Decision D-10):
  if no room exists -> `/mos:onboard`; if room mostly empty -> `/mos:status`
  with next-move suggestion; if room populated -> `/mos:suggest-next`.
  Meets the navigator where they are.
- F.1 selectors wired on `commands/onboard.md` Step 6 + `commands/diagnose.md`
  recommendation (Decision D-12). Standard 3-verb F.1: Run Methodology
  (the primary recommendation) / Defer (look around first) /
  Free-Text (tell Larry what you want). Closes the Canon Part 3
  violation flagged in the Cluster 5 audit.
- `commands/help.md` Infrastructure group adds doctor / hmi-status
  (admin-only after D-11) -- previously invisible per Cluster 5 audit.
- /mos:doctor adds a "deprecated commands you still use" surface
  starting beta.5 (post-Sub-plan-I ship). Proactive migration nudge.

### OUT OF SCOPE

- Any NEW product surface or command (this is consolidation, not expansion --
  Canon Part 7).
- The HTML export / presentation / snapshot generator consolidation (that's a
  v1.14.0 "Visible Room" concern -- the wiki+SnapshotHub fusion -- not terminal).
- Running the empathy audits that unpin Phase 114/115/116/95.2 (those are
  separate release-gate gates; this phase makes the terminal coherent, the
  audits validate the emotional through-line -- both must pass before final, but
  they are not the same work).
- Custom themes, env-var-room-state, skill-frontmatter-SessionStart-hooks (see
  REJECTED above).
- Phase 113 WASM work (deferred to v3.0 -- do not pull forward).

## Open Design Decisions (status as of 2026-05-16)

1. **SessionStart precedence ladder** -- ✅ **LOCKED via D-13**: install-drift > sealed-room > tension-hook > memory-resume > auto-explore > onboarding > MINTO > JTBD > operator > post-compact > statusline-fallback.
2. **Combined token budget** -- ✅ **LOCKED via D-14**: ~2,000 chars hard cap. ✅ **D-15** Iterative compression: walk bottom-up, compress lowest-priority to one-line pointer until total ≤ cap. ✅ **D-16** Isolate failed contributors: try/catch + telemetry + memory_event, never crash turn-1.
3. **Palette: keep or rebrand** -- ⏳ STILL OPEN. Deferred to plan-phase / brand decision. Lean: keep existing muted/earthy values.
4. **🎯 overload + "JTBD" word collision** -- ⏳ STILL OPEN. Disambiguate or document as explicit exception. Defer to plan-phase.
5. **render-v2 disposition** -- ⏳ STILL OPEN. Wire into prose path, or freeze as agent-only by design. Defer to plan-phase.
6. **Statusline shape co-design** -- ✅ **LOCKED via D-01 through D-04**: JTBD-invariant single shape (NOT per-JTBD adaptive); two-row identity/situation split; Row 1 = Larry/version + Brain tier + context % (update glyph conditional); Row 2 = room ▶ section │ JTBD │ STAGE │ governing thought (truncated) │ operator. `121.5-STATUSLINE-DESIGN.md` documents this shape; no per-JTBD variation; transition behavior (re-render on JTBD change) becomes plan-phase detail.

## Decisions Locked (Captured 2026-05-16 discuss-phase pass)

16 implementation decisions captured. Downstream agents (planner, executor) act on these without re-asking.

### Statusline (Sub-plan D)
- **D-01** JTBD-invariant single shape. One spec, not 13 per-JTBD subsections. (Eliminates the "13 layouts" complexity flagged in the 2026-05-14 hard rule.)
- **D-02** Two-row layout, identity/situation split.
- **D-03** Row 1 (identity) = `⬡ MindrianOS v<version> │ 🧠 BRAIN│LOCAL │ 📊 ███░░░░░ N%`. Update glyph `🔄` appears CONDITIONALLY only when /mos:update detects new version. SEED-007 version-of-record anchored here.
- **D-04** Row 2 (situation) = `🏠 room ▶ 📂 section │ 🎯 JTBD │ 🔍 STAGE │ <governing thought, truncated to ~40 chars> │ <operator>`. All 6 segments; truncation policy on governing thought + room name for narrow terminals = planner detail.

### Bulletproof /mos:help (Sub-plan I)
- **D-05** Render dispatch via `lib/core/terminal-capability.cjs` (NEW). Probes `process.stdout.isTTY` AND `$COLORTERM=truecolor` (or 256color fallback). Color path when BOTH pass; ASCII path otherwise. Reusable across /mos:status, /mos:doctor, /mos:splash.
- **D-06** Hybrid data source: `data/help-groups.json` (NEW) declares groups + which commands belong; each `commands/<name>.md` frontmatter gains `help_jtbd:` field (the "what's in it for me" line). Distributed ownership of JTBD lines; centralized grouping. Build-time validation: every command in help-groups.json exists; every command has help_jtbd.
- **D-07** Split "Output + Export" into 3 groups: **Export** (PDF/vault: `/mos:export`, `/mos:room linkify`) / **Publish** (live URL: `/mos:publish`, `/mos:present --share`) / **Hub** (browser/cron: `/mos:dashboard`, `/mos:wiki`, `/mos:scheduled-tasks`, `/mos:rooms new --github`). Destination-first mental model.
- **D-08** Hard-fail pre-commit + CI on coverage gaps (missing `help_jtbd:` frontmatter OR missing entry in `data/help-groups.json`). `scripts/check-help-coverage.cjs` (NEW).

### Naming-drift sweep (Sub-plan J)
- **D-09** Soft-alias deprecation: old commands stay as 1-line stubs (print deprecation note + invoke new command transparently). Zero tester breakage. Removal scheduled for v1.14.0. CHANGELOG flags all renames. `/mos:doctor` adds a "deprecated commands you still use" surface starting beta.5+.
- **D-10** `/mos:mos` (CREATE -- declared in plugin.json, file absent) = state-aware router. No room exists -> route to `/mos:onboard`. Room mostly empty (Pre-Opportunity stage) -> `/mos:status` + next-move suggestion. Room populated -> `/mos:suggest-next`. Meets navigator where they are.
- **D-11** `/mos:hmi-status` -> fold into `/mos:doctor --ui-compliance --json`. Standalone command goes away. Soft-alias for one release; remove v1.14.0. Migrate scripts/hooks calling old form.
- **D-12** F.1 selectors wired on `/mos:onboard` Step 6 + `/mos:diagnose` recommendation. Standard 3-verb F.1: Run Methodology / Defer / Free-Text. Closes the Canon Part 3 violation (Cluster 5 audit).

### SessionStart Coordinator (Sub-plan A)
- **D-13** Precedence ladder: install-drift > sealed-room > tension-hook > memory-resume > auto-explore > onboarding > MINTO > JTBD > operator > post-compact > statusline-fallback. Blocking warnings first; continuity (tension hook + memory resume) before novelty (auto-explore / onboarding); ambient state last.
- **D-14** ~2,000 chars combined token budget. Hard cap. Per-surface uniform (not different CLI vs Desktop -- one budget, one mental model).
- **D-15** Iterative compression on overflow: walk precedence ladder bottom-up, compress lowest-priority contributor to one-line pointer until total ≤ 2,000 chars. Top 2-3 priorities always full; bottom degrades gracefully.
- **D-16** Failed contributors isolated: try/catch per contributor; log to `~/.mindrian/telemetry/sessionstart-errors.jsonl`; emit `memory_event: sessionstart_contributor_failed`; drop the failed contributor; continue with the rest. Coordinator NEVER crashes turn-1.

### Claude's Discretion (planner-level)
- Statusline overflow truncation policy (governing thought + room name when narrow terminal width)
- JTBD-change transition behavior (re-render immediately vs next prompt vs sticky for session)
- Desktop/Cowork prose echo format for statusline segments
- Specific glyph choices per new group (Export/Publish/Hub icons)
- Per-entry JTBD wording for the ~9 Ship+Share lines
- CHANGELOG.md announcement structure for the naming sweep
- Deprecation note exact wording in soft-alias stubs

### Folded Todos
None this round -- the discuss-phase scope was the four selected gray areas. Existing 8 sub-plans + 2 new sub-plans (I, J) cover the locked decisions.

### Still-Open ODDs (deferred to plan-phase)
- ODD 3 Palette: keep muted/earthy or rebrand to brighter primaries (brand call, not code call)
- ODD 4 🎯 overload + "JTBD" word collision: disambiguate or document
- ODD 5 render-v2 prose-path wiring vs agent-only freeze

## Canon Compliance

- **Part 3 (UI Ruling System):** this phase IS the Part 3 enforcement pass --
  every command obeys; SKILL.md is accurate; the output style enforces.
- **Part 4 (Every Choice Is Graph Data):** coordinator decisions + body-shape
  compliance signals mirror to telemetry (Phase 121) and, where applicable,
  emit typed graph events. No side-channel printing of decisions.
- **Part 7 (Reuse Before Build):** consolidation only. Any NEW file (the
  coordinator, the output style, palette.json) must justify itself against the
  audit's "N parallel implementations" finding -- the bar is "this REPLACES /
  reconciles N existing things," not "this is the N+1th."
- **Part 8 (Graph Boundary):** coordinator telemetry is local-only, scalar /
  enum / hash payloads only -- no injected-context bytes egressed.
- **Part 10 (Conversation as Product):** the terminal must coherently express
  "you are in a conversation with Larry, navigating a wicked problem." This
  phase is the precondition for the Part 10 ratification at the final gate --
  if the terminal is incoherent, the thesis isn't demonstrated.

## Acceptance Criteria

1. SessionStart Coordinator exists; all current injectors are contributors; a
   precedence ladder + hard token budget are enforced; coordinator decisions
   mirror to Phase 121 telemetry.
2. 100% of `commands/*.md` declare `body_shape:`; `/mos:hmi-status` reports a
   clean baseline (zero "missing body shape" violations).
3. `output-styles/destijl.md` ships with `force-for-plugin: true`; `/mos:doctor`
   verifies its own output is 4-zone compliant.
4. SKILL.md documents F.0 + F.6, the dual palette, and the `🎯`/"JTBD"
   resolutions; SKILL.md matches shipped code (a doc-vs-code diff check passes).
5. Statusline shape was co-designed with Jonathan in the 2026-05-16 discuss-phase pass and locked via D-01..D-04: JTBD-invariant single shape, two-row identity/situation split. The layout spec lives in `121.5-STATUSLINE-DESIGN.md`; `scripts/context-monitor` implements that spec exactly (no agent-side reinterpretation); shape is two-row identity/situation as locked (D-02), JTBD is one segment in Row 2 (D-04) rather than the dimension the layout adapts on. `references/visual/palette.json` is canonical and all surfaces derive from it (CI check passes); `settings.json` `statusLine.command` points at `statusline-mos`.
6. `102-VERIFICATION.md` exists; `102-VALIDATION.md` is out of `draft`;
   render-v2's disposition is documented in `lib/render/ROOM.md`.
7. Every first-touch surface stamps the running version; the stale-copy scanner
   runs in `/mos:doctor`; greeting strings pass the em-dash check.
8. `.planning/phases/40-hook-expansion/` is deleted; Phase 88.7's ROADMAP status
   is truthful; the active-phase mtime scan is audited.
9. The coherence smoke test (sub-plan H) passes with a human reader at each step.
10. **(Sub-plan I)** `/mos:help` renders bulletproof across CLI / Desktop / Cowork. `lib/core/terminal-capability.cjs` (NEW) probes TTY + truecolor; color path when both pass, ASCII-only path (12-glyph vocabulary, zero `\033` escapes) otherwise. Torture tests pass: piped (`tr -d $'\033'`), non-TTY (file redirect / heredoc), Desktop-simulator -- all produce human-readable output.
11. **(Sub-plan I)** `data/help-groups.json` (NEW) is canonical. Every `commands/*.md` declares `help_jtbd:` frontmatter. `scripts/check-help-coverage.cjs` (NEW) hard-fails pre-commit + CI on missing `help_jtbd:` OR missing group entry. Closes the audit finding of doctor/heal/hmi-status silently absent from help.md. "Output + Export" group replaced by 3 groups: Export / Publish / Hub.
12. **(Sub-plan J)** Soft-alias deprecation shipped for `/mos:heal` (→ doctor --heal-room), `/mos:query` (→ graph), `/mos:organize` (→ rooms organize), `/mos:diagnostics` (→ fingerprint), `/mos:hmi-status` (→ doctor --ui-compliance). Old commands print deprecation note + invoke new transparently. `/mos:visualize` retired or folded into `/mos:dashboard --mermaid` (plan-phase decides). CHANGELOG announces all renames; `/mos:doctor` adds "deprecated commands you still use" surface starting beta.5+.
13. **(Sub-plan J)** `commands/mos.md` exists and ships as state-aware router (no room → onboard; mostly empty → status; populated → suggest-next). `/mos:onboard` Step 6 + `/mos:diagnose` recommendation both render F.1 selector (Run Methodology / Defer / Free-Text) -- closes Canon Part 3 violations. help.md Infrastructure group surfaces doctor (and admin-gated hmi-status).

## Verification (post-execution)

- Run the coherence smoke test on a fresh install (CLI) -- human confirms
  who/job/runway/next at every step.
- Run `/mos:hmi-status` -- zero structural violations.
- Run the SessionStart Coordinator under a synthetic worst case (every
  contributor has a payload) -- confirm output stays under budget and the
  precedence ladder holds.
- Diff SKILL.md against the shipped renderer/glyph/shape code -- zero drift.
- Confirm the v1.13.0 FINAL RELEASE GATE checklist can now cite "terminal
  coherence verified" as a precondition for Canon Part 10 ratification.

## Cross-References

- Roadmap: `.planning/ROADMAP.md` (Phase 121.5 entry) +
  `.planning/milestones/v1.13.0-CLOSED-LOOP-ROADMAP.md` (final wave, before the
  FINAL RELEASE GATE)
- Reference (annotated): `121.5-REFERENCE-destijl-guide-annotated.md`
- Canon: `docs/MINDRIAN-CANON.md` Parts 3, 4, 7, 8, 10
- Audit inputs: the two UI/UX audits + the HUD/De Stijl audit + the parallel
  research note (all 2026-05-10, this conversation) -- summarized inline above;
  file the raw audit outputs into this dir as `121.5-AUDIT-INPUTS.md` when
  `/gsd:plan-phase 121.5` runs
- Predecessor surfaces this phase reconciles: Phases 17, 21, 37, 88.1, 88.2, 95,
  95.2, 102, 104, 105, 106, 114, 115, 116, 117, 119, 121; SEED-007
- Claude Code feature ground-truth: see "Explicitly REJECTED" / "Explicitly
  ADOPTED" above (verified against CC docs 2026-05-10)

## Provenance

Phase 121.5 was scoped 2026-05-10 by Jonathan Sagir + Claude-as-Larry after a
three-part audit of every UI/UX-touching phase in the plugin, a verification
pass on an external "De Stijl Full UI/UX Implementation Guide" reference doc
(found to be ~1/3 buildable, ~1/3 regression, ~1/3 fabricated -- annotated copy
filed alongside this CONTEXT), and a parallel-research note identifying the
uncoordinated SessionStart injection flood as the root cause of Larry's
occasional session-start incoherence.

Jonathan's framing (verbatim intent): "the last phase that will acknowledge all
else before planning, just before issuing 1.13.0 -- an accumulated representation
of the Mindrian JTBD as a product in the relevant UI/UX in terminal."

CONTEXT.md scaffolded 2026-05-10; promoted to scoped 2026-05-16 with 16 decisions
locked (D-01..D-16) via the discuss-phase pass + Sub-plans I + J absorbed. Three
Open Design Decisions (3, 4, 5) remain open and are deferred to plan-phase per the
body table. Plans 121.5-00 through 121.5-09 written; remaining plans land at
plan-phase time. Dual-graph architectural review concluded 2026-05-16 (verdict
logged in the top section of this file); hold-flag retracted.
