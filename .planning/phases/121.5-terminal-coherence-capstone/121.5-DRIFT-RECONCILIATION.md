---
phase: 121.5-terminal-coherence-capstone
type: drift-reconciliation
date: 2026-05-23
auditor: gsd-debugger sibling pass (drift-vs-substrate audit)
plugin_version_at_audit: 1.13.0-beta.29
head_at_audit: c49dcf39dbb8f4faa51508c77209024412e9dc94
status: read-only survey; produces this one file; no plan or source modified
---

# Phase 121.5 Drift Reconciliation -- pre-execute pass

## 1. Source-of-Truth Preamble (per docs/RCA-TEMPLATE.md Section 2.5)

- CODE claims read against: `origin/main` HEAD @ `c49dcf39` (plugin 1.13.0-beta.29).
  Note: the originating prompt cited `1025a2fc` (the selector-audit commit). That commit is the
  immediate parent of the v1.13.0-beta.29 bump and the v1.13.0-beta.28 release; the audit
  surveys the latest tip, not the audit-scoping commit, because the drift question is
  "what is in main now relative to what the plans assumed when they were scoped."
- WIRE claims probe against: none. This is a plan-vs-source-tree audit only.
- Date of audit: 2026-05-23.
- Re-verification rule: every drift claim is verified by two reads -- (a) `git log --since="2026-05-16" -- <file>`, (b) the current state of the file at origin/main HEAD (via Read or grep). Both must agree before a claim is filed.
- Scope: 11 plans in `.planning/phases/121.5-terminal-coherence-capstone/` (Plans 121.5-00 through 121.5-10) plus their declared substrate.

## 2. Executive Summary

11 plans surveyed. The picture is unusual: **the phase is almost entirely SHIPPED ALREADY**.

| Verdict | Count | Plans |
|---------|------:|-------|
| ALREADY-SHIPPED (plan was executed; SUMMARY.md exists; substrate is in main) | 10 | 121.5-00, 01, 02, 03, 04, 05, 06, 07, 08, 09 |
| READY-TO-EXECUTE (plan is fresh against current substrate; no drift) | 1 | 121.5-10 |
| CLEAN-AS-WRITTEN | 0 |  |
| NEEDS-EDIT | 0 |  |
| CONFLICTS-WITH-SHIPPED | 0 |  |

There is **no drift inside the plans themselves**. The drift is at the orchestrator level: 10 of 11 plans have been silently absorbed into shipped main with full SUMMARY.md artifacts, a coherence smoke report, and a phase VERIFICATION.md marking the phase `status: complete / nyquist_compliant: true / completed: 2026-05-16` -- yet the orchestrator's task list still says "Execute Phase 121.5 (plans 00-07 + 10, then 09 coherence smoke)" as if all of them were still pending. Plans 00 through 09 ran on 2026-05-16; Plan 10 was added 2026-05-23.

### Top 3 highest-risk findings

1. **Plans 00 through 09 are SHIPPED.** Every plan in 00-09 has a SUMMARY.md (15-25 KB each) and the substrate files exist on disk. Re-executing them would either (a) noop the verifications, (b) double-commit work, or (c) overwrite the actual shipped behavior with the plan's idealized version, undoing 7 days of post-2026-05-16 betas (beta.19 through beta.29). **Do not run `/gsd:execute-phase 121.5` against plans 00-09. Run it against Plan 10 only.**

2. **Plan 09's `depends_on` lists `121.5-10` but Plan 09 ALREADY shipped on 2026-05-16.** Plan 09's frontmatter line 6 reads `depends_on: ["121.5-00", ..., "121.5-08", "121.5-10"]`. Plan 10 was written 2026-05-23 -- after Plan 09 completed. The dependency edge is true in dependency-order-of-concerns terms (the smoke test should re-run against Plan 10's substrate before Phase 121.5 is "really done"), but it is FALSE in execution-chronology terms (Plan 09 ran without Plan 10 existing). The 121.5-VERIFICATION.md was written under the 10-sub-plan model and does not enumerate Plan 10 (Sub-plan K) in its closure table. After Plan 10 executes, the smoke test should re-run AND the VERIFICATION.md should be amended.

3. **Plan 06's "create lib/core/active-phase-resolver.cjs" was satisfied by a DIFFERENT file path.** Plan 06 declares `files_modified: lib/core/active-phase-resolver.cjs` and asserts grep `"active-phase-resolver" scripts/context-monitor` returns >= 1 hit. The actual shipped artifact (per `121.5-06-SUMMARY.md`) is `scripts/audit-active-phase-resolver.cjs` -- a different filename, a different location (scripts/ not lib/core/), and `scripts/context-monitor` was not refactored. The Plan 06 SUMMARY.md transparently documents this deviation as a worktree-agent-timeout finish, with the refined logic ("healthy = HAS canonical files, not LACKS them"). For Plan 10 execution this drift is benign; for an idealized Plan 06 re-execution it would be a divergence.

### One-paragraph thesis

The drift state today is: **the orchestrator's task list lies; the plan files are silent witnesses to a phase that already shipped**. Pre-execution edits required: **zero edits to plans 00-09 (they shipped as-written or with documented deviations); zero blocking edits to Plan 10 (it is fresh against current substrate)**. The single action required before `/gsd:execute-phase 121.5` is to **scope the invocation to Plan 10 only** -- the other 10 plans need no re-execution, no re-verification, and no surgical edits. After Plan 10 ships, optionally amend the existing 121.5-VERIFICATION.md to add the Plan 10 (Sub-plan K) row and optionally re-run the coherence smoke test against the Brain-suggestion lock-in.

## 3. Per-Plan Verdicts

### Plan 121.5-00 (Sub-plan A: SessionStart Coordinator)

- **Verdict:** ALREADY-SHIPPED
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (15.4 KB). All 5 declared substrate files exist (`scripts/sessionstart-coordinator.cjs`, `lib/sessionstart/precedence-ladder.cjs`, `lib/sessionstart/budget-compressor.cjs`, `lib/sessionstart/contributor-isolator.cjs`, `tests/run-all-121.5.sh`). Two git commits in main: `cb3ca332 test(121.5-00): add RED tests + GREEN impl` and `3b8c30c6 feat(121.5-00): wire SessionStart Coordinator + refactor 9 injectors`.
- **Specific edits required:** None. Do not re-execute.
- **Evidence:**
  - `git log --since="2026-05-16" -- scripts/sessionstart-coordinator.cjs` returns 2 commits (`3b8c30c6`, `cb3ca332`).
  - `ls .planning/phases/121.5-terminal-coherence-capstone/121.5-00-SUMMARY.md` returns the file with frontmatter `requirements: ["COHERENCE-121.5-A-01"..04]`.
- **Recommendation:** Do not re-execute. Skip in any execute-phase invocation.

### Plan 121.5-01 (Sub-plan B: body_shape sweep + output-styles/destijl.md)

- **Verdict:** ALREADY-SHIPPED
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (17.7 KB). `output-styles/destijl.md` exists; `scripts/audit-body-shape-coverage.cjs` exists. Commit `ff063757 docs(121.5-01): complete body_shape sweep + De Stijl output style plan`.
- **Specific edits required:** None.
- **Evidence:**
  - `ls output-styles/destijl.md` returns the file.
  - `ls scripts/audit-body-shape-coverage.cjs` returns the file.
  - `git log --since="2026-05-16" -- output-styles/destijl.md scripts/audit-body-shape-coverage.cjs` returns the merge commit + content commit.
- **Recommendation:** Do not re-execute.

### Plan 121.5-02 (Sub-plan C: SKILL.md v2 reconciliation)

- **Verdict:** ALREADY-SHIPPED
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (20.4 KB). The three sidecar rules files exist: `skills/ui-system/rules/{dual-palette,shape-f-zero-and-six,glyph-disambiguation}.md`. `scripts/check-skill-vs-code-drift.cjs` exists. Commit `065ca73a merge(121.5-02): SKILL.md v2 reconciliation + skill-vs-code drift checker`.
- **Specific edits required:** None.
- **Evidence:**
  - `ls skills/ui-system/rules/` returns all three rules files.
  - `grep -c "Brain-suggestion variant" skills/ui-system/SKILL.md` returns 0 (Plan 10's SKILL.md amendment has NOT landed yet; expected, that is Plan 10's work, not a 121.5-02 drift).
- **Recommendation:** Do not re-execute. Plan 10 will further extend SKILL.md.

### Plan 121.5-03 (Sub-plan D: Statusline + canonical palette.json)

- **Verdict:** ALREADY-SHIPPED
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (24.3 KB). `references/visual/palette.json` exists; `references/visual/ROOM.md` exists; `lib/statusline/{two-row-renderer,version-resolver,governing-thought-truncator}.cjs` all exist; `scripts/check-palette-consistency.cjs` exists; `scripts/statusline-mos` wrapper exists. `121.5-STATUSLINE-DESIGN.md` is on disk in the phase directory. Commit `06f21a4a merge(121.5-03): two-row statusline + canonical palette.json + statusline-mos wrapper`.
- **Specific edits required:** None.
- **Evidence:** All 5 substrate files exist; the merge commit lands on 2026-05-16.
- **Recommendation:** Do not re-execute.

### Plan 121.5-04 (Sub-plan E: render-v2 disposition + Phase 102 closure)

- **Verdict:** ALREADY-SHIPPED
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (14.2 KB). `lib/render/ROOM.md` exists; `scripts/disposition-render-v2.cjs` exists; `.planning/phases/102-context-aware-rendering/102-VERIFICATION.md` exists; `102-VALIDATION.md` was flipped to `status: complete`. Commits `1fb6f241 docs(121.5-04): document render-v2 as agent-surface-only` and `401826cf merge(121.5-04)`.
- **Specific edits required:** None.
- **Evidence:** Both merge commits in `git log --since="2026-05-16" -- lib/render/render-v2.cjs lib/render/ROOM.md`.
- **Recommendation:** Do not re-execute.

### Plan 121.5-05 (Sub-plan F: version-of-record first-touch + SEED-007)

- **Verdict:** ALREADY-SHIPPED
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (22.3 KB). `data/first-touch-surfaces.json` exists; `lib/core/first-touch-version-stamper.cjs` exists; `lib/core/stale-copy-scanner.cjs` exists; `scripts/doctor.cjs` carries class K (the implementing scanner class). Plan declares this as "class H" but the actual shipped class letter is **K** (see Cross-Plan Pattern below). Commits `27d346f9 docs(121.5-05): complete version-of-record on first touch plan` and `b6814fb1 feat(121.5-05): stale-first-touch-copy class K scanner + /mos:doctor wiring`.
- **Specific edits required:** None functional. Plan body says "class H"; shipped reality is "class K"; the deviation is documented in the SUMMARY.md and in `scripts/doctor.cjs:1187` ("CONTEXT D4 text reads 'K' but K is taken" comment). Benign.
- **Evidence:** `grep -c "class K\|--stale-first-touch" scripts/doctor.cjs` returns 29 hits; the K-not-H pivot is recorded inline in the doctor source.
- **Recommendation:** Do not re-execute. The letter-pivot is intentional and documented.

### Plan 121.5-06 (Sub-plan G: truth-telling + housekeeping)

- **Verdict:** ALREADY-SHIPPED (with documented deviation)
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (3.9 KB, the smallest summary). `.planning/phases/40-hook-expansion/` is GONE. Phase 88.7 ROADMAP entry is updated to "INSERTED, INCOMPLETE" (truthful; from "1/1 plans complete"). `scripts/audit-active-phase-resolver.cjs` exists (NOT `lib/core/active-phase-resolver.cjs` as the plan declared). `scripts/context-monitor` was NOT refactored to require the resolver -- the plan-declared key-link `"active-phase-resolver" scripts/context-monitor` evaluates to 0 grep hits. The SUMMARY.md transparently documents both deviations as worktree-agent-timeout finish artifacts. Commit `5b51d620 merge(121.5-06): truth-telling housekeeping (Phase 88.7 status fix)`.
- **Specific edits required:** None. The deviation is documented in the SUMMARY.md and is benign for Plan 10 execution.
- **Evidence:**
  - `ls .planning/phases/40-hook-expansion/` returns "No such file or directory".
  - `grep -A2 "Phase 88.7" .planning/ROADMAP.md` returns the updated text "INSERTED, INCOMPLETE".
  - `find . -name "active-phase*"` returns only `.claude/worktrees/agent-a732546586e585a5f/lib/memory/active-phase-resolver.test.cjs` (the abandoned worktree artifact).
  - `grep -c "active-phase-resolver" scripts/context-monitor` returns 0 (the plan-declared wiring did not happen; the existing mtime-scan was left in place).
- **Recommendation:** Do not re-execute. Optional v1.14.0 carry-forward: complete the resolver-as-lib-module refactor or remove the deviation note from CONTEXT.md item 10.

### Plan 121.5-07 (Sub-plan I: Bulletproof ASCII /mos:help)

- **Verdict:** ALREADY-SHIPPED
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (21.6 KB). `lib/core/terminal-capability.cjs` exists; `scripts/help-renderer.cjs` exists; `data/help-groups.json` exists; `scripts/check-help-coverage.cjs` exists. Every `commands/*.md` has `help_jtbd:` frontmatter. Commit `8edeef13 docs(121.5-07): complete bulletproof /mos:help renderer plan`.
- **Specific edits required:** None.
- **Evidence:** All 4 declared substrate files exist; the SUMMARY.md cites 21/21 test pass.
- **Recommendation:** Do not re-execute.

### Plan 121.5-08 (Sub-plan J: naming-drift sweep + /mos:mos + F.1 selectors)

- **Verdict:** ALREADY-SHIPPED (with letter-shift in doctor class assignment)
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (18.0 KB). `scripts/soft-alias-runner.cjs` exists; `lib/core/state-aware-router.cjs` exists; `commands/mos.md` exists; the 5 soft-alias stubs (heal/query/organize/hmi-status/visualize) exist with `deprecated: true` markers in their bodies. Plan body says "doctor class I" (--deprecated-usage); shipped reality is **class L** (because K was taken by Plan 121.5-05 -- documented in the doctor source comment). `commands/fingerprint.md` does NOT exist yet -- per Plan 121.5-08 Task 1 Step 5, the diagnostics-to-fingerprint rename only deprecation-notes; the actual `commands/fingerprint.md` create is deferred to v1.14.0 by design. Commits `4ced5f51 feat(121.5-08): Task 1 -- soft-alias runner + state-aware router + 5 stubs + /mos:mos`, `340fadb7 feat(121.5-08): Task 2 -- F.1 selectors + doctor class L`, `3826c99e merge(121.5-08)`.
- **Specific edits required:** None functional. The K-vs-L pivot is documented in `scripts/doctor.cjs` source comment ("class L (deprecated-commands-you-still-use)" with explicit note that K was claimed by Plan 121.5-05).
- **Evidence:**
  - `ls commands/{heal,query,organize,hmi-status,visualize,diagnostics,mos}.md` returns all 7 files.
  - `grep -c "checkDeprecatedUsage\|--deprecated-usage" scripts/doctor.cjs` returns 36 hits (class L is wired in).
  - `ls commands/fingerprint.md` returns "No such file or directory" (expected; deferred to v1.14.0).
- **Recommendation:** Do not re-execute.

### Plan 121.5-09 (Sub-plan H: coherence smoke test + 121.5-VERIFICATION.md)

- **Verdict:** ALREADY-SHIPPED (with downstream amendment expected after Plan 10 ships)
- **Drift summary:** SHIPPED 2026-05-16. SUMMARY.md exists (18.0 KB). `scripts/coherence-smoke-test.cjs` exists; `tests/test-coherence-smoke.cjs` exists; `.planning/phases/121.5-terminal-coherence-capstone/121.5-COHERENCE-SMOKE-REPORT.md` exists with the full transcript; `.planning/phases/121.5-terminal-coherence-capstone/121.5-VERIFICATION.md` exists with frontmatter `status: complete / nyquist_compliant: true / completed: 2026-05-16`.
  - **Real drift here (the only meaningful drift in the audit):** Plan 09's frontmatter `depends_on:` line 6 lists `121.5-10` -- but Plan 10 was written 2026-05-23, AFTER 09 shipped on 2026-05-16. The dependency edge was retro-fitted into Plan 09's frontmatter when Plan 10 was added (per the 121.5-CONTEXT.md `absorbed_2026_05_23:` block). The shipped 121.5-VERIFICATION.md does NOT enumerate Plan 10 in its closure table (the table has 10 rows: 121.5-00..09). After Plan 10 ships, the 121.5-VERIFICATION.md SHOULD be amended (one new row, Plan 121.5-10 = Sub-plan K = SHIPPED) AND the coherence smoke test SHOULD be re-run against the Brain-suggestion lock-in.
- **Specific edits required after Plan 10 ships (not before):**
  - File `.planning/phases/121.5-terminal-coherence-capstone/121.5-VERIFICATION.md` -- add one row to the "Plan-by-plan closure" table for 121.5-10 (Sub-plan K Brain-suggestion template + selector audit Bundle A); update plan count from 10 to 11. Effort: 5 min.
  - Optional: re-run `node scripts/coherence-smoke-test.cjs` to confirm Plan 10's Brain-suggestion template surfaces did not regress the existing smoke walk. Effort: 1 min.
- **Evidence:** 
  - The shipped 121.5-VERIFICATION.md plan-closure table has 10 rows (121.5-00..09); no 121.5-10 row.
  - Plan 09's frontmatter line 6 was retro-edited to include 121.5-10 (the file mtime predates Plan 10's existence; the line is a textual edit not a verification dependency).
- **Recommendation:** Do not re-execute. After Plan 10 ships, do the 5-minute VERIFICATION amendment + re-smoke (or accept the deviation in a follow-up SUMMARY note).

### Plan 121.5-10 (Sub-plan K: Brain-suggestion content template lock + audit Bundle A)

- **Verdict:** READY-TO-EXECUTE (CLEAN-AS-WRITTEN against current substrate; no drift)
- **Drift summary:** Written 2026-05-23 (today) against `origin/main` at HEAD `5bdb7667` (the audit's stated probe SHA in the plan's interfaces block). The current main is `c49dcf39` -- two commits ahead of the plan's probe, both Windows-tester QA closeout work (`00e19f5a` Plan 02 SUMMARY + `c49dcf39` find-bottlenecks transcript). Neither touches any of Plan 10's `files_modified` list. The plan is fresh against current substrate.
- **Specific edits required:** None. The plan is clean.
- **Evidence:**
  - Plan 10's `files_modified` list intersected with `git log --since="2026-05-21" -- <each file>` returns zero overlap (the recent betas touched `mcp-server-brain/lib/brain-ask.cjs` for the curated-op fix and `mcp-server-brain/lib/pinecone-tools.cjs` for the topK clamp; neither is in Plan 10's render-template work).
  - `grep -c "alias_map\|brain_suggestion_variant" lib/hmi/selector-dispatcher.cjs` returns 0 -- the dispatcher does not yet carry the Plan 10 plumbing; the plan's "ADD payload.brain_suggestion_variant + payload.optionRows + payload.alias_map" is genuine net-new work.
  - `grep -c "MAX_K" lib/workflow/f-selector-ranker.cjs` returns 0 -- Plan 10 Task 1's MAX_K constant has not been added; genuine net-new work.
  - `grep -c "alias_map" lib/hmi/jtbd-taxonomy.json` returns 0 -- Plan 10 Task 1's alias_map block has not been added; genuine net-new work.
  - `ls tests/test-no-bespoke-brain-prompts.sh` returns "No such file or directory" -- Plan 10 Task 3's CI tripwire is genuinely missing.
  - All 5 Brain-suggestion consumers (`scripts/suggest-next-command.cjs`, `scripts/act-command.cjs`, `lib/agents/tension-hook-agent.cjs`, `lib/agents/auto-explore-agent.cjs`, `lib/agents/reverse-salient-agent.cjs`) return zero hits for `[■ BRAIN]` -- the Plan 10 consumer adoption is genuinely pending across all five.
  - Plan 10's depends_on `["121.5-01", "121.5-02"]` is satisfied (both shipped 2026-05-16).
- **Recommendation:** Execute as written. This is the ONLY plan in Phase 121.5 that should be invoked by `/gsd:execute-phase 121.5` now.

## 4. Cross-Plan Patterns

### Pattern A: Doctor class letter pivots (benign)

Plan 121.5-05 declared its scanner as "class H" + Plan 121.5-08 declared its scanner as "class I". Shipped reality is **class K** (Plan 121.5-05's stale-first-touch-copy) + **class L** (Plan 121.5-08's deprecated-commands-you-still-use). The pivot happened because the doctor class roster had already grown to G/H/I/J during the v1.13.0-beta train (Phase 123 install-lifecycle-harness shipped classes A-J in beta.13; the SUMMARY-K letter clash was resolved by skipping ahead). Documented inline in `scripts/doctor.cjs` source comments. Future plan drafting should source doctor's free-class-letter from `scripts/doctor.cjs --list-classes` or similar live query rather than from a CONTEXT.md target letter.

### Pattern B: SUMMARY.md is the source of truth for shipped reality; PLAN.md is the source of truth for intent

For all 10 shipped plans, the SUMMARY.md transparently documents deviations from the PLAN.md (file rename, class letter pivot, worktree-agent-timeout finish, deferred sub-step). This is canon-compliant per the gsd-tools "Rule 3: document deviations, don't hide them" mantra. The PLAN.md files were NOT retroactively edited to match shipped reality; the deviation lives in SUMMARY.md. This means a future audit reading only PLAN.md without SUMMARY.md will see false drift. Always read SUMMARY.md alongside PLAN.md.

### Pattern C: Phase 121.5 dependency phases all SHIPPED -- the "human-needed gate pending" frontmatter is stale

121.5-CONTEXT.md depends_on lists Phase 114 / 115 / 116 as "human-needed gate pending". Reality at 2026-05-23:
- Phase 114 verification: `status: human_needed`, score 4/4, verified 2026-05-05.
- Phase 115 verification: `status: human_needed`, score 4/4, verified 2026-05-05.
- Phase 116 verification: `status: human_needed`, plans 5/5 complete, must-haves 7/8, release gates 4/5, verified 2026-05-06.
- Phase 117: shipped per CONTEXT.md.
- Phase 119: VERIFICATION.md exists.
- Phase 121: `status: passed`, 12/12 must-haves verified 2026-05-19.

All five "pending" phases shipped their CODE; the "human_needed" gate marker means the empathy / tester-rubric synthesis step still pends (per phase 115's verification document). This does not block Plan 10 execution -- Plan 10's substrate dependencies are Phase 88.2 renderers + Phase 101-01 dispatcher + Phase 125 ranker, all unambiguously shipped.

### Pattern D: The retro-fitted Plan 09 depends_on line

Plan 09's frontmatter `depends_on: ["121.5-00", ..., "121.5-08", "121.5-10"]` was retro-edited on 2026-05-23 to add `121.5-10` after Plan 10 was authored. This is a logical dependency (the smoke test should validate Plan 10's Brain-suggestion lock-in) but not an execution dependency (Plan 09 shipped without Plan 10 existing). After Plan 10 ships, either re-run the smoke harness or accept the dependency as logical-only with a SUMMARY annotation.

## 5. Pre-Execution Punch List

Ordered by leverage:

1. **Scope `/gsd:execute-phase 121.5` to Plan 10 only.** Edit the orchestrator's task list (#4) to say "Execute Phase 121.5 PLAN 10 ONLY (plans 00-09 shipped 2026-05-16)" or invoke executor with explicit plan selection. **Effort: 1 min. Highest leverage by far -- prevents re-shipping 10 plans and clobbering 7 days of betas.**

2. **(Post-Plan-10) Amend `121.5-VERIFICATION.md`** to add the Plan 121.5-10 closure row. The current VERIFICATION lists 10 plans; after Plan 10 ships, add row 11 (Sub-plan K) + bump plan count from 10 to 11 in the totals line + add Plan 10 SUMMARY cross-reference. Effort: 5 min.

3. **(Post-Plan-10, optional) Re-run `node scripts/coherence-smoke-test.cjs`** to confirm Plan 10's Brain-suggestion lock-in did not regress the smoke walk. The harness already runs; this is a 1-min sanity check. If GREEN, append a note to 121.5-COHERENCE-SMOKE-REPORT.md ("re-verified post-Plan-10 on YYYY-MM-DD"). Effort: 2 min including note.

4. **(Optional, low-leverage) Update Plan 06 PLAN.md or CONTEXT.md item 10** to reflect the actual shipped artifact location (`scripts/audit-active-phase-resolver.cjs`, not `lib/core/active-phase-resolver.cjs`). This is documentation cleanup; the SUMMARY.md already records the deviation. Effort: 3 min. Defer if not blocking.

5. **(Optional, no-effort) Note the K-vs-H + L-vs-I doctor letter pivots** in a Phase 121.5 retrospective if one is written. Documented inline in source; further surfacing is optional.

**Total pre-execution effort: 1 minute (just punch-list item 1)**. Items 2 + 3 are post-execution; items 4 + 5 are optional cleanup.

## 6. Phase 121.5 Dependencies State

Per 121.5-CONTEXT.md frontmatter `depends_on:`. State verified 2026-05-23 against each phase's VERIFICATION.md:

| Phase | CONTEXT.md said (2026-05-16) | Current state (2026-05-23) | Blocks Plan 10? |
|------|------------------------------|----------------------------|------------------|
| 88.1 | shipped | shipped (no change) | NO |
| 88.2 | shipped | shipped (no change) | NO |
| 102 | shipped, VERIFICATION pending | SHIPPED (VERIFICATION created by Plan 121.5-04 on 2026-05-16) | NO |
| 104 | shipped | shipped | NO |
| 105 | shipped | shipped | NO |
| 106 | shipped | shipped | NO |
| 114 | human-needed gate pending | code SHIPPED; human gate still pending (verified 2026-05-05) | NO |
| 115 | human-needed gate pending | code SHIPPED; human gate still pending (verified 2026-05-05) | NO |
| 116 | human-needed gate pending | code SHIPPED; human gate still pending (verified 2026-05-06) | NO |
| 117 | shipped | shipped | NO |
| 119 | final wave | code SHIPPED; VERIFICATION.md exists | NO |
| 121 | spans beta.1->final | SHIPPED (status: passed, 12/12 must-haves verified 2026-05-19) | NO |

Additionally relevant (not in CONTEXT depends_on but load-bearing for Plan 10):
- Phase 88.2 selector-dispatcher (lib/hmi/selector-dispatcher.cjs): shipped, ready for Plan 10's brain_suggestion_variant extension.
- Phase 125 f-selector-ranker (lib/workflow/f-selector-ranker.cjs): shipped, ready for Plan 10's MAX_K + category + graph_relationship extension.
- Phase 127 brain-mcp-local-stdio-shim: shipped in v1.13.0-beta.13. Does not change Plan 10's surface; Plan 10's brain-ask.cjs touch is docstring-only.
- Phase 127.1 D-MOAT-1/D-MOAT-2 (admin gate on brain_query): shipped in v1.13.0-beta.21. Does not interact with Plan 10's render-template work.
- Phase 127.2 Brain Edge Bundle (beta.26): shipped (BRAIN_MAX_TOPK env var + version-of-record + npm/GitHub priority swap). No interaction with Plan 10.

**Conclusion: zero blocking dependencies for Plan 10. Execute as written.**

## 7. Open Questions for Navigator

1. **Should `121.5-VERIFICATION.md` be amended in the same execution as Plan 10**, or as a separate follow-up commit? AskUserQuestion candidate: `[Amend in same commit / Separate follow-up / Defer to v1.13.0 final-release work]`.

2. **Should the coherence smoke test be re-run after Plan 10 ships?** The smoke harness covers 5 steps + tri-polar + tripwires + four-questions; Plan 10's Brain-suggestion template lock-in could plausibly regress step 4 (the F.1 decision-gate frame fixture). Re-running takes 1 min. AskUserQuestion candidate: `[Re-run (default) / Skip (Plan 10's own tests are sufficient) / Re-run only if step 4 explicitly verifies]`.

3. **Should the doctor class-letter drift (K-instead-of-H, L-instead-of-I) be retroactively edited into Plans 121.5-05 and 121.5-08?** The PLAN.md files still say H + I; the SUMMARY.md files document the pivot to K + L. Future audits reading only PLAN.md will see false drift. AskUserQuestion candidate: `[Edit PLANs to match shipped letters / Leave PLANs as intent-of-record + rely on SUMMARYs / Add a single CONTEXT.md note explaining the pattern]`.

4. **Should Plan 121.5-06's CONTEXT.md item 10 deviation (active-phase-resolver shipped as `scripts/audit-active-phase-resolver.cjs` not `lib/core/active-phase-resolver.cjs`) be reconciled as a v1.14.0 follow-up?** The deviation is documented in 121.5-06-SUMMARY.md but not in CONTEXT.md. AskUserQuestion candidate: `[v1.14.0 carry-forward / Accept-as-is via CONTEXT.md note / No action]`.

5. **Is the Phase 121.5 phase itself "really done" if Plan 10 is the 11th sub-plan but the VERIFICATION.md was sealed as 10/10 on 2026-05-16?** The strict reading is "no -- Plan 10 reopens the phase"; the pragmatic reading is "yes -- the phase shipped its core acceptance, Sub-plan K is a tracked late-addition Bundle A item." AskUserQuestion candidate: `[Reopen phase formally / Treat Plan 10 as in-phase addendum / Spin Plan 10 into a Phase 121.6 mini-phase]`.

---

End of reconciliation. The single executable action is item 1 of the Pre-Execution Punch List: scope `/gsd:execute-phase 121.5` to Plan 10 only.
