---
phase: 121.5-terminal-coherence-capstone
plan: 04
subsystem: lib/render + lib/memory + .planning/phases/102 + scripts/disposition
tags:
  - render-v2-disposition
  - agent-surface-only
  - phase-102-closure
  - canon-part-7
  - canon-part-3
  - linguistic-discipline
  - sub-plan-e
canon_parts:
  - "3"
  - "4"
  - "6"
  - "7"
  - "8"
dependency-graph:
  requires:
    - phase: 99-conversation-operator-state-machine
      provides: render-v2 stub + operator vocabulary
    - phase: 102-context-aware-rendering
      provides: full render-v2 implementation (compaction + JTBD zone 4 + provenance + color overlay)
    - phase: 121.5-terminal-coherence-capstone
      provides: 121.5-CONTEXT.md Sub-plan E scope + acceptance criteria
  provides:
    - render-v2-disposition-locked-agent-surface-only
    - render-v2-disposition-audit-script
    - render-v2-disposition-ci-gate
    - phase-102-verification-record
    - phase-102-validation-out-of-draft
    - canon-part-7-current-language-discipline-on-render-v2
  affects:
    - phase-121.5-sub-plan-c-skill-md-reconciliation
    - phase-121.5-sub-plan-h-coherence-smoke-test
    - v1.13.0-final-release-gate
    - canon-part-10-ratification

tech-stack:
  added: []
  patterns:
    - "operator-facing audit script + CI gate test pair (disposition.cjs / disposition.test.cjs) for any future architecture-disposition decision -- the script answers 'is the disposition holding right now?' to humans; the test fails CI when drift occurs"
    - "ROOM.md as canonical disposition page, with allowed-consumer table + CI parity enforced by the gate test"
    - "Linguistic discipline forbidden-phrase scan in disposition tests (scans ROOM.md for 'complete set of rankable' / 'closed signal vocabulary' / 'all input signal types' / 'final set of rankable' framing)"

key-files:
  created:
    - "scripts/disposition-render-v2.cjs"
    - "lib/memory/render-v2-disposition.test.cjs"
    - ".planning/phases/102-context-aware-rendering/102-VERIFICATION.md"
    - ".planning/phases/102-context-aware-rendering/102-VALIDATION.md"
    - ".planning/phases/121.5-terminal-coherence-capstone/121.5-04-SUMMARY.md"
  modified:
    - "lib/render/ROOM.md"
    - "lib/memory/run-feynman-tests.cjs"

key-decisions:
  - "render-v2 stays agent-surface-only by design for v1.13.0 final. SKILL.md remains the canonical prose contract for Larry's main conversation path. The v2 destructured render() entry is reserved for /mos:* command surfaces and Shape F decision-gate output via lib/hmi/selector-dispatcher.cjs. Documented at lib/render/ROOM.md Disposition section + locked by CI gate test."
  - "Allowlist for production consumers of render-v2.cjs is exactly two files: lib/render/render.cjs (v1 legacy-signature shim) + lib/hmi/selector-dispatcher.cjs (agent-surface dispatcher). Any addition requires updating both scripts/disposition-render-v2.cjs ALLOWED_CONSUMERS and lib/render/ROOM.md in the same commit; the CI gate enforces parity."
  - "Permanent Canon Part 7 linguistic convention on render-v2's rankable input signal types: 'current' / 'as of' / 'today' framing only. Forbidden: 'final' / 'complete' / 'closed set'. The 2026-05-16 dual-graph review noted render-v2's signal set is one of FOUR ultrareview-adjacent surfaces; lens-aware variants (future ASSOCIATION_LENS + TRANSITION_LENS lens classes) may add lens-derived signals. The signal set is current, not closed for all time."
  - "Phase 102 release loop closes via Sub-plan E: 102-VERIFICATION.md created, 102-VALIDATION.md flipped from status: draft / nyquist_compliant: false to status: verified / nyquist_compliant: true / wave_0_complete: true. 16 task rows green (14 original Phase 102 + 2 Sub-plan E closure)."

patterns-established:
  - "Disposition-as-CI-gate: lock long-open architecture decisions via a test that asserts the verdict against a documented allowlist + a linguistic discipline scan. The pattern is reusable for any future open question that needs to stay locked once decided."
  - "Operator audit + CI test pair: scripts/<name>.cjs gives operators a single-line answer at any moment; lib/memory/<name>.test.cjs fails the build when the audit's verdict flips. The script is for humans; the test is for the wall."
  - "Phase-closure via Sub-plan: when an upstream phase (102) is shipped but never validated, a downstream phase (121.5-04) can close its release loop by producing VERIFICATION.md + flipping VALIDATION.md without re-running the phase."

requirements-completed: []

# Metrics
duration: ~25min
completed: 2026-05-16
---

# Phase 121.5-04 (Sub-plan E): render-v2 disposition lock + Phase 102 closure Summary

**Locks render-v2 as agent-surface-only by design for v1.13.0 final, ships a CI gate + operator audit pair to enforce the disposition, and closes the Phase 102 release loop with VERIFICATION.md + VALIDATION.md flipped out of draft -- all per the 2026-05-16 dual-graph review linguistic discipline.**

## Performance

- **Duration:** ~25 minutes
- **Started:** 2026-05-16
- **Completed:** 2026-05-16
- **Tasks:** 5 atomic commits
- **Files modified:** 7 (2 modified, 5 created)

## Accomplishments

- Resolved the long-open Sub-plan E open question ("decide explicitly: wire render-v2 into the real prose path, or document it as agent-surface-only"). Verdict: agent-surface-only by design for v1.13.0 final, documented at `lib/render/ROOM.md` with full rationale grounded in Canon Part 7 (Reuse Before Build) and Canon Part 3 (the gate output side).
- Shipped a disposition CI gate (`lib/memory/render-v2-disposition.test.cjs`) + operator audit script (`scripts/disposition-render-v2.cjs`) pair that locks the verdict. The audit answers "is render-v2 still agent-surface-only?" in one line via `--json` or a human summary; the test fails CI on any prose-path import. Registered in the Feynman test runner so it runs alongside the existing Phase 102 fences.
- Closed the Phase 102 release loop. `102-VERIFICATION.md` shipped with the verification record (acceptance gates, evidence, Canon compliance, linguistic discipline lock). `102-VALIDATION.md` flipped from `status: draft / nyquist_compliant: false` to `status: verified / nyquist_compliant: true / wave_0_complete: true` with all 14 original Phase 102 task rows marked green + 2 Sub-plan E closure rows appended.
- Established the permanent Canon Part 7 linguistic convention on render-v2's input signal surface: "current" / "as of Phase 121.5-04" / "today" framing only -- never "final" / "complete" / "closed set." The disposition test scans `ROOM.md` for the forbidden phrases on every run.

## Task Commits

Each task was committed atomically with `--no-verify` per parallel-executor guidance:

1. **Task 1: render-v2 disposition audit script** - `ecb0ce0b` (feat)
2. **Task 2: render-v2 disposition CI gate (RED)** - `23827c79` (test)
3. **Task 3: ROOM.md disposition section + current language (GREEN)** - `1fb6f241` (docs)
4. **Task 4: Phase 102 VERIFICATION + flipped VALIDATION** - `a5f6fbef` (docs)
5. **Task 5: register disposition gate in run-feynman-tests** - `0cc2e903` (test)

_Note: the TDD cycle for this plan landed as test (RED, 2/5 failing) -> docs (GREEN-3, 4/5) -> docs (GREEN-5, 5/5) since the "implementation" is documentation + a test registration, not new product code._

## Files Created/Modified

### Created

- `scripts/disposition-render-v2.cjs` (229 lines)
  - Read-only filesystem audit. Walks `lib/`, `scripts/`, `commands/`, `bin/`, `hooks/` for `require()` of render-v2; classifies each consumer as allowed (against the canonical 2-file allowlist) or violation; emits structured JSON on `--json` or a human summary on stderr; exits 0 on HOLDS / 1 on VIOLATED.
  - Node built-ins only (`fs`, `path`); zero new runtime dependencies (Phase 87 invariant).
- `lib/memory/render-v2-disposition.test.cjs` (199 lines)
  - 5 IIFE scenarios: audit verdict HOLDS / allowlist matches canonical set / ROOM.md uses "current" linguistic discipline (forbidden-phrase scan) / 102-VALIDATION.md out of draft (soft-pass if absent in worktree) / 102-VERIFICATION.md exists.
  - Registered in `lib/memory/run-feynman-tests.cjs` alongside the existing Phase 102 fences.
- `.planning/phases/102-context-aware-rendering/102-VERIFICATION.md`
  - The Phase 102 closure record: status verified, disposition resolved (with cross-reference to ROOM.md), verification artifacts table, acceptance gates with evidence, Canon parts 3/4/7/8 compliance, the 2026-05-16 linguistic discipline lock.
- `.planning/phases/102-context-aware-rendering/102-VALIDATION.md`
  - Flipped from `status: draft / nyquist_compliant: false / wave_0_complete: false` to `status: verified / nyquist_compliant: true / wave_0_complete: true`. All 14 Phase 102 per-task rows marked green; 2 Sub-plan E closure rows appended (T1 CI gate + T2 audit script).
- `.planning/phases/121.5-terminal-coherence-capstone/121.5-04-SUMMARY.md`
  - This file.

### Modified

- `lib/render/ROOM.md`
  - Added the **Disposition (Sub-plan E, 2026-05-16)** section with the agent-surface-only verdict, allowed-consumer table, "why not wire into the prose path" rationale, and the **Current rankable input signal types** table with explicit "as of Phase 121.5-04" / "additive expansion reserved" framing.
  - Promoted Phase 102 from "planned" to "shipped" in the phase status table; added the 121.5-04 row.
  - Refined the `render()` contract block to match the Phase 102 destructured signature.
  - Listed downstream consumers as the canonical 2-file set; cross-referenced the CI gate + audit script.
- `lib/memory/run-feynman-tests.cjs`
  - Registered `lib/memory/render-v2-disposition.test.cjs` in `TEST_FILES` (slot between Phase 102 color-overlay and Phase 103 memory-continuity).

## Disposition (the load-bearing decision)

> **render-v2 stays agent-surface-only by design for v1.13.0 final.** SKILL.md remains the canonical prose contract for Larry's main conversation path. The v2 destructured `render()` entry is reserved for /mos:* command surfaces and the Shape F decision-gate output side that route through `lib/hmi/selector-dispatcher.cjs`.

Three reasons (priority order):

1. **Canon Part 7 (Reuse Before Build).** SKILL.md is the canonical prose contract today; it ships, it is exercised on every Larry turn, and the 4-zone format is enforced by skill instruction. Replacing instruction-side enforcement with code-side enforcement would be a parallel implementation rather than reuse.
2. **Canon Part 3 (the gate output side).** The v2 destructured signature was designed for Shape F.x gate surfaces -- decision-gate output, not free prose. The compaction layer (102-02), JTBD-aware Zone 4 (102-03), provenance envelope (102-04), and color overlay (102-05) all assume gate-bounded payloads. Wrapping them around ordinary prose would silently change Larry's conversational shape -- a regression risk against Phase 88.1 + 88.2 + the Part 10 capstone work.
3. **Phase 121.5 capstone scope.** Sub-plan C reconciles SKILL.md v2 with the shipped Shape F set + dual palette + 🎯/JTBD disambiguation. After 121.5 lands, SKILL.md IS the source of truth -- a separate v1.14.0 phase can revisit whether code-side prose rendering still earns its keep against a freshly-reconciled SKILL.md.

The disposition is "current" disposition, not "closed forever." The render-v2 input signal set is one of FOUR ultrareview-adjacent surfaces; lens-aware variants (future ASSOCIATION_LENS + TRANSITION_LENS lens classes from the dual-graph proposal) may add lens-derived signals additively. The Canon Part 7 linguistic discipline -- "current" / "as of" / "today" framing -- is enforced permanently by the disposition test (scenario 3, forbidden-phrase scan).

## Deviations from Plan

None auto-fixed. The PLAN file for 121.5-04 was not pre-scaffolded in the worktree; the executor read the scope from `121.5-CONTEXT.md` Sub-plan E + the prompt's success criteria block. All work falls inside the documented Sub-plan E scope.

One operational note: the test runner `lib/memory/run-feynman-tests.cjs` ran the full suite during a verification call (via `node -e "require(...)"` which executed the module's top-level loop). Test failures reported by `lib/core/lazygraph-ops.cjs` and `lib/core/memory-ops.cjs` (cases 01-04, 06-07, 12-13 in `test/84-smart-notebook-copilot.test.cjs`) are PRE-EXISTING and unrelated to this plan's changes -- they reproduce in the worktree before any 121.5-04 file was created. Out of scope per the executor scope-boundary rule. Logged for future cleanup.

## Self-Check

Files verified to exist on disk:

- `scripts/disposition-render-v2.cjs` -- FOUND
- `lib/memory/render-v2-disposition.test.cjs` -- FOUND
- `lib/render/ROOM.md` -- FOUND (modified)
- `.planning/phases/102-context-aware-rendering/102-VERIFICATION.md` -- FOUND
- `.planning/phases/102-context-aware-rendering/102-VALIDATION.md` -- FOUND
- `lib/memory/run-feynman-tests.cjs` -- FOUND (registry entry added)

Commits verified to exist in `git log --oneline -10`:

- `ecb0ce0b` -- FOUND
- `23827c79` -- FOUND
- `1fb6f241` -- FOUND
- `a5f6fbef` -- FOUND
- `0cc2e903` -- FOUND

Tests verified to pass:

- `node lib/memory/render-v2-disposition.test.cjs` -- 5 passed, 0 failed
- `node lib/render/render-v2.test.cjs` -- 12 passed, 0 failed (no regression)
- `node scripts/disposition-render-v2.cjs --json` -- `verdict: "HOLDS"`, exit 0

Linguistic discipline scan:

- `ROOM.md` does NOT contain "complete set of rankable", "closed signal vocabulary", "all input signal types", or "final set of rankable" -- verified by the disposition test scenario 3.
- No em-dashes introduced in any file modified by this plan (hyphens only).

## Self-Check: PASSED

## See also

- `lib/render/ROOM.md` -- the canonical disposition page.
- `lib/memory/render-v2-disposition.test.cjs` -- the CI gate.
- `scripts/disposition-render-v2.cjs` -- the operator audit.
- `.planning/phases/102-context-aware-rendering/102-VERIFICATION.md` -- the Phase 102 verification record.
- `.planning/phases/102-context-aware-rendering/102-VALIDATION.md` -- flipped out of draft.
- `121.5-CONTEXT.md` -- Sub-plan E scope + acceptance criteria.
- `docs/MINDRIAN-CANON.md` -- Part 3 (Tri-Context Decision Gate), Part 7 (Reuse Before Build).
