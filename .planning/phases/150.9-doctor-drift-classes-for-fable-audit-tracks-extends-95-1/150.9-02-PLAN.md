---
phase: 150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1
plan: 02
type: execute
wave: 2
depends_on: ["150.9-01"]
files_modified:
  - scripts/doctor.cjs
  - tests/test-doctor-class-p.cjs
  - tests/test-doctor-class-q.cjs
autonomous: true
canon_parts: [6, 7, 8]
requirements: [DDC-01, DDC-02, DDC-04, DDC-05]
must_haves:
  truths:
    - "doctor --drift runs Class P (prose-vs-code, report-only) + Class Q (gsd-record drift) + the DRIFT.md writer, and NOTHING else new fires on a bare doctor run (D-04 opt-in)"
    - "doctor --drift exits 0 even when drift is present (classFlagsActive invariant) and never touches Class A install/version logic, so the marketplace-cache-drift-deadlock carve-out is preserved"
    - "Class P aggregates check-skill-vs-code-drift (require) + check-first-touch-drift (subprocess, Pitfall 1) and NEVER edits prose (D-03 report-only)"
    - "Class Q shells out to gsd-tools validate health --raw with cwd=repo-root, parses W007 + I001, and degrades to status:error on shape drift rather than false-clean (Pitfall 2/3)"
    - "doctor --drift --fix auto-writes DRIFT.md + stubs missing SUMMARYs but makes ZERO edits to any prose .md (D-03 heal-where-safe split)"
  artifacts:
    - path: "scripts/doctor.cjs"
      provides: "--drift argv flag, classFlagsActive OR-term, Class P dispatch (report-only), Class Q dispatch (gsd-health shell-out), --drift --fix heal arm wiring lib/core/drift-baseline.cjs"
      contains: "flags.drift"
    - path: "tests/test-doctor-class-p.cjs"
      provides: "Class P wrap-both-checkers + no-prose-edit assertions (mtime compare)"
      min_lines: 40
    - path: "tests/test-doctor-class-q.cjs"
      provides: "Canned gsd-health JSON injection: W007/I001 extraction + shape-drift safety + exit-0 invariant + deadlock-carve-out untouched"
      min_lines: 50
  key_links:
    - from: "scripts/doctor.cjs (Class Q)"
      to: "gsd-tools validate health --raw"
      via: "cp.spawnSync('node', [gsdToolsPath,'validate','health','--raw'], {cwd: repoRoot})"
      pattern: "spawnSync"
    - from: "scripts/doctor.cjs (--drift --fix heal arm)"
      to: "lib/core/drift-baseline.cjs"
      via: "require + writeDriftBaseline + stubMissingSummary"
      pattern: "drift-baseline"
---

<objective>
Wire the two new lettered classes into the doctor.cjs class-engine and the `--drift` activation flag, conforming EXACTLY to the shipped A-N class contract. Class P (prose-vs-code, report-only) wraps the two existing checkers. Class Q (GSD execution-record drift) shells out to gsd-health and parses W007 (ROADMAP drift) + I001 (missing SUMMARYs). The `--drift --fix` heal arm calls the Plan-01 writer to auto-write DRIFT.md and stub missing SUMMARYs, while Class P stays report-only (D-03). All of this is opt-in under `--drift` only (D-04) and registered into classFlagsActive so the run honors the graceful-degradation exit-0 invariant, leaving Class A's marketplace-cache-drift-deadlock carve-out untouched.

Purpose: turn the one-shot Fable v1.13.1 audit (tracks 1 product-claims + the GSD-health track) into standing doctor classes so every release self-audits.
Output: doctor.cjs argv+dispatch+heal edits + tests/test-doctor-class-p.cjs + tests/test-doctor-class-q.cjs (RED-first).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-CONTEXT.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-RESEARCH.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-VALIDATION.md

# The host pattern: argv parse (line ~210), classFlagsActive (line ~3988),
# class K/L dispatch (line ~4288/4320), Class M subprocess (line ~2987),
# _finalizeAndExit + deadlock carve-out (line ~4564), checkInstallVersion
# carve-out (line ~428):
@scripts/doctor.cjs
# The two checkers Class P wraps:
@scripts/check-skill-vs-code-drift.cjs
@scripts/check-first-touch-drift.cjs
# The Plan-01 writer this heal arm consumes:
@lib/core/drift-baseline.cjs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan:
- CLI flag `--drift` (sets flags.drift) on scripts/doctor.cjs.
- `flags.drift` added to the classFlagsActive OR-chain (graceful-degradation exit-0 invariant).
- Class P dispatch block writing `report.checks['prose-vs-code']` (class:'P', status ok|warn|error; NEVER edits prose).
- Class Q dispatch block writing `report.checks['gsd-record-drift']` (class:'Q', status ok|warn|error|skip; carries w007[] + i001[] counts).
- The `--drift --fix` heal arm: writeDriftBaseline(findings from P+Q) + stubMissingSummary(per I001) via lib/core/drift-baseline.cjs.
- A gsd-tools locator helper mirroring workflows/health.md:52 _GSD_TOOLS resolver order (no hardcoded path).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for Class P (report-only wrap) and Class Q (gsd-health parse + exit invariant)</name>
  <files>tests/test-doctor-class-p.cjs, tests/test-doctor-class-q.cjs</files>
  <read_first>
    - .planning/phases/150.9-.../150.9-VALIDATION.md RQ1/RQ2/RQ3/D-03/D-04 rows (the exact assertions to attach)
    - .planning/phases/150.9-.../150.9-RESEARCH.md Pitfall 1 (first-touch CLI-only self-exit), Pitfall 2 (gsd-health shape drift), Pitfall 3 (CWD/home guard), Pitfall 4 (do NOT arm the default roster), and the LIVE-captured gsd-health JSON shape
    - tests/test-doctor-class-i.cjs + tests/test-doctor-class-j.cjs (the hermetic mkdtempSync HOME envelope + plain-node assert idiom)
  </read_first>
  <behavior>
    test-doctor-class-p.cjs:
    - P-1 (wrap, no reimplement): Class P invokes check-skill-vs-code-drift via require().check() and check-first-touch-drift via subprocess; on a fixture with injected drift, report.checks['prose-vs-code'].status === 'warn'.
    - P-2 (report-only, D-03): after `doctor --drift` over a fixture, the mtimes of all fixture skill/agent .md files are UNCHANGED (no prose edit), even under --fix.
    - P-3 (first-touch subprocess, Pitfall 1): Class P captures first-touch exit code via spawnSync and NEVER require()s it (a require would self-exit and kill doctor).
    test-doctor-class-q.cjs:
    - Q-1 (parse): feed a canned gsd-health JSON {status:'degraded',warnings:[{code:'W007',...}],info:[{code:'I001',...}]} -> Class Q reports the expected W007 + I001 counts.
    - Q-2 (shape-drift safety, Pitfall 2): feed a shape-shifted JSON (W007 moved, or warnings not an array) -> status === 'error' "shape unrecognized", NOT a false clean.
    - Q-3 (exit-0 invariant, RQ1): `doctor --drift --json` with drift present exits 0; report.checks carries both prose-vs-code and gsd-record-drift.
    - Q-4 (deadlock carve-out, RQ1): a marketplace-cache-topology fixture run under `--drift` does NOT invoke Class A install-version drift exit-1; assert exit stays 0 and Class A path not deadlocked.
    - Q-5 (opt-in, D-04): a BARE `doctor` (no --drift) run produces NO prose-vs-code and NO gsd-record-drift keys in report.checks.
  </behavior>
  <action>Create both test files as standalone node tests using the mkdtempSync HOME envelope from test-doctor-class-i.cjs. For Class Q tests, inject the canned gsd-health JSON by stubbing the gsd-tools locator/subprocess (e.g. point the locator at a tiny fixture node script that prints the canned JSON, OR set an env override the Class Q code reads for the tool path) so the test is hermetic and does not depend on the live gsd-core install. Run doctor.cjs via cp.spawnSync('node', ['scripts/doctor.cjs','--drift','--json']) and JSON.parse stdout. Assert exit codes with the spawn result status. NO em-dashes anywhere (hyphens only). Both files are RED until Task 2 + 3 land.</action>
  <verify>
    <automated>node tests/test-doctor-class-p.cjs; node tests/test-doctor-class-q.cjs; echo "expect both RED (non-zero) pre-implementation"</automated>
  </verify>
  <done>Both test files exist, encode the behaviors above, and fail (RED) because Class P/Q + --drift do not yet exist in doctor.cjs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire --drift flag, classFlagsActive, Class P (report-only) into doctor.cjs</name>
  <files>scripts/doctor.cjs</files>
  <read_first>
    - scripts/doctor.cjs argv parse loop (line ~210-230), classFlagsActive block (line ~3988-3992), Class K dispatch template (line ~4288)
    - scripts/check-skill-vs-code-drift.cjs (exports {check} at line 178; check() returns {valid, missing_in_skill, missing_in_code, missing_crossrefs, missing_rules_files} with no process.exit)
    - .planning/phases/150.9-.../150.9-RESEARCH.md Pattern 1 (status enum ok|warn|error to match the renderer stage logic), Pattern 2 (try/catch soft-fail wrapper), Anti-Patterns (never process.exit in a class fn; never auto-edit prose)
  </read_first>
  <action>Add `else if (arg === '--drift') flags.drift = true;` to the argv loop following the shipped idiom. Add `|| flags.drift` to the classFlagsActive OR-chain so the run honors the exit-0 graceful-degradation invariant (this is what preserves the marketplace-cache-drift-deadlock carve-out automatically, since that carve-out lives in Class A's checkInstallVersion which --drift never touches -- per D-04 and the CONTEXT discretion ruling). Add the Class P dispatch block gated strictly behind `if (flags.drift)` (NEVER behind !classFlagsActive -- Pitfall 4), wrapped in try/catch that records a class:'P' status:'error' finding on throw. Inside: require check-skill-vs-code-drift.cjs and call check(); shell out to check-first-touch-drift.cjs via cp.spawnSync('node',[path,...],{encoding:'utf8'}) capturing exit code (1=drift, 0=clean) and parsing its DRIFT: stdout lines (Pitfall 1 -- do NOT require it). Aggregate both verdicts into report.checks['prose-vs-code'] = {class:'P', status: anyDrift?'warn':'ok', detail, skill_findings, first_touch_hits}. Class P is REPORT-ONLY: it MUST NOT edit any file (D-03). Use status strings ok|warn|error only (Pattern 1 / A3). Per Canon Part 7 reuse justification: this REPOINTS the two shipped checkers into the class roster -- it does NOT reimplement either; that is the explicit Part-7 answer. NO em-dashes (hyphens only). Do NOT add Class Q or the heal arm in this task (Task 3 owns them) -- but you may add the gsd-tools locator helper now if convenient. Keep this task's doctor.cjs edits scoped to argv + classFlagsActive + Class P.</action>
  <verify>
    <automated>node tests/test-doctor-class-p.cjs</automated>
  </verify>
  <done>test-doctor-class-p.cjs passes; `node scripts/doctor.cjs --drift --json` runs Class P and emits report.checks['prose-vs-code']; a bare `doctor` does not; no fixture prose file mtime changes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Class Q (gsd-health shell-out) + the --drift --fix heal arm into doctor.cjs</name>
  <files>scripts/doctor.cjs</files>
  <read_first>
    - scripts/doctor.cjs Class M subprocess idiom (line ~2987 cp.spawnSync('node',[...])), the class-E/G/H --fix dispatch idiom, _finalizeAndExit (line ~4564)
    - .planning/phases/150.9-.../150.9-RESEARCH.md "## gsd-health JSON shape" (W007 in warnings[], I001 in info[], record shape {code,message,fix,repairable}, top-level status enum), Pitfall 2 (assert shape before reading; status:error not false-clean), Pitfall 3 (spawn with cwd=repo-root; treat broken/error status as indeterminate), the workflows/health.md:52 _GSD_TOOLS resolver order
    - lib/core/drift-baseline.cjs (writeDriftBaseline + stubMissingSummary signatures from Plan 01)
  </read_first>
  <action>Add the Class Q dispatch block gated behind `if (flags.drift)`, wrapped in try/catch (class:'Q', status:'error' on throw). Resolve the gsd-tools path via a locator helper mirroring the _GSD_TOOLS resolver order (workflows/health.md:52) -- NEVER hardcode ~/.claude/gsd-core/bin/gsd-tools.cjs; if the locator fails, emit class:'Q' status:'skip' "gsd-health unavailable" (never crash -- Environment Availability fallback). Spawn `cp.spawnSync('node',[gsdToolsPath,'validate','health','--raw'],{cwd: <repo root resolved from the workspace, NOT process.cwd()>, encoding:'utf8'})` (Pitfall 3 CWD guard) with a FIXED arg array (no shell string -- T-150.9-03 injection mitigation). JSON.parse(stdout) inside a try/catch. BEFORE reading .warnings/.info, assert the shape: typeof json==='object' && Array.isArray(json.warnings) && Array.isArray(json.info); if not, status:'error' "gsd-health output shape unrecognized -- W007/I001 parse skipped" (Pitfall 2). If json.status is broken/error, treat as indeterminate and surface it, do NOT report clean. Extract w007 = json.warnings.filter(w=>w.code==='W007'), i001 = json.info.filter(i=>i.code==='I001'); report.checks['gsd-record-drift'] = {class:'Q', status: (w007.length||i001.length)?'warn':'ok', w007, i001}. Then add the `--drift --fix` HEAL ARM (D-03 heal-where-safe): when flags.drift && flags.fix, require lib/core/drift-baseline.cjs, build the finding set from Class P + Class Q results, call writeDriftBaseline(...) to auto-write the root + per-folder DRIFT.md, and call stubMissingSummary(...) for each I001 finding. The heal arm MUST NOT edit any prose (Class P findings are recorded, never auto-fixed). Class Q and the heal arm MUST NOT call process.exit (exit is _finalizeAndExit-only -- preserves the exit-0 invariant + deadlock carve-out). NO em-dashes (hyphens only).</action>
  <verify>
    <automated>node tests/test-doctor-class-q.cjs</automated>
  </verify>
  <done>test-doctor-class-q.cjs passes; Class Q parses W007/I001 from canned JSON, degrades to error on shape drift, exits 0 with drift present, leaves Class A deadlock carve-out untouched; `doctor --drift --fix` writes DRIFT.md + SUMMARY stubs and edits no prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gsd-tools subprocess <-> doctor.cjs | doctor spawns an external CLI and parses its stdout; output-shape and arg-injection are the surfaces |
| Class A version logic <-> --drift run | a wrong dispatch guard could re-arm the marketplace-cache deadlock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.9-03 | Tampering | Class Q gsd-health subprocess | mitigate | spawnSync('node', [FIXED arg array], {cwd, encoding}) -- never a shell string, no interpolation of untrusted input. Tested by test-doctor-class-q.cjs (the spawn uses a fixed array). |
| T-150.9-04 | Denial of Service | --drift re-introducing the marketplace-cache-drift-deadlock | mitigate | --drift registered into classFlagsActive -> exit 0; Class P/Q never touch checkInstallVersion; never call process.exit. Q-4 test asserts exit stays 0 and Class A path is not deadlocked. EXPLICIT acceptance criterion (CONTEXT D-04 discretion ruling). |
| T-150.9-05 | Information Disclosure | Class P/Q (Canon Part 8 floor) | mitigate | P reads local files + spawns a LOCAL first-touch subprocess; Q spawns a LOCAL gsd-tools subprocess (not network). PR-diff-scoped grep `fetch|http|curl|brain|tavily` over the new doctor.cjs lines returns 0 (Plan 03 phase gate, mirrors 95.2 proof). |
| T-150.9-06 | Spoofing/Tampering | gsd-health output-shape drift -> false clean | mitigate | Shape-assert before parse; status:error not false-clean; broken/error health status treated as indeterminate (Pitfall 2/3). Q-2 test. |
| T-150.9-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Node built-ins + existing repo modules only; RESEARCH Package Legitimacy Audit empty-set case). |
</threat_model>

<verification>
- `node tests/test-doctor-class-p.cjs` GREEN; `node tests/test-doctor-class-q.cjs` GREEN.
- `node scripts/doctor.cjs --drift --json; echo $?` -> 0 (exit invariant) with both report.checks keys present.
- `node scripts/doctor.cjs --json; echo $?` -> NO prose-vs-code / gsd-record-drift keys (D-04 opt-in).
- PR-diff grep `git diff origin/main -- scripts/doctor.cjs | grep '^+' | grep -E "fetch|http|curl|brain|tavily"` returns 0 for the new class lines (Q's spawnSync('node',['gsd-tools','validate','health']) is a LOCAL subprocess, not matched).
</verification>

<success_criteria>
- Class P + Class Q conform to the lettered-class contract (return object, dispatch registration, status enum, 0/1/2 exit owned by _finalizeAndExit).
- --drift is opt-in only and never re-arms the deadlock; exit stays 0 with drift present.
- --drift --fix heals where safe (DRIFT.md + SUMMARY stubs) and never edits prose.
- No new dependencies; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-02-SUMMARY.md` when done.
</output>
