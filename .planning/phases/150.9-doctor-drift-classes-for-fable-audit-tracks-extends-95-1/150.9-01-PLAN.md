---
phase: 150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/drift-baseline.cjs
  - tests/test-drift-baseline.cjs
autonomous: true
canon_parts: [6, 7, 8]
requirements: [DDC-03, DDC-08]
must_haves:
  truths:
    - "Running the writer produces a root .planning/DRIFT.md index and per-phase .planning/phases/<NN>/DRIFT.md detail files (D-01 both granularities)"
    - "Re-running the writer on an unchanged finding set produces a byte-identical body except last_seen (diff-stable, FIX-13)"
    - "The writer writes ONLY under .planning/ and refuses any path that resolves outside it (path-traversal guard)"
    - "The writer has zero network surface (Canon Part 8 floor)"
  artifacts:
    - path: "lib/core/drift-baseline.cjs"
      provides: "Pure DRIFT.md schema writer (root index + per-folder) + SUMMARY-stub helper; require-clean, no process.exit"
      exports: ["writeDriftBaseline", "stubMissingSummary", "renderRootIndex", "renderPerFolder"]
      min_lines: 80
    - path: "tests/test-drift-baseline.cjs"
      provides: "Schema round-trip + diff-stability + path-traversal-refusal assertions"
      min_lines: 40
  key_links:
    - from: "lib/core/drift-baseline.cjs"
      to: ".planning/DRIFT.md + .planning/phases/<NN>/DRIFT.md"
      via: "fs.writeFileSync under an asserted-.planning resolved path"
      pattern: "writeFileSync"
---

<objective>
Ship the ONE genuinely net-new piece of Phase 150.9: `lib/core/drift-baseline.cjs`, a pure, hermetic, require-clean DRIFT.md writer that emits the root rolled-up index (`.planning/DRIFT.md`) AND per-phase detail files (`.planning/phases/<NN>/DRIFT.md`) per locked decision D-01, plus the SUMMARY-stub helper the heal arm uses. This is contract-first: Plan 02 (the doctor.cjs Class P/Q dispatch + `--drift` heal arm) consumes the functions defined here, so they ship first with a frozen signature.

Purpose: FIX-13 from the v1.13.1 drift audit -- give the NEXT audit a baseline to DIFF against instead of rediscovering ~600k tokens of drift. The diff-stability rules (stable finding_id, first_seen set-once, last_seen bumped, closed-with-history) are the FIX-13 contract.
Output: `lib/core/drift-baseline.cjs` (the writer module) + `tests/test-drift-baseline.cjs` (the RED-first schema/diff-stability/traversal test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-CONTEXT.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-RESEARCH.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-VALIDATION.md

# The DRIFT.md schema + diff-stability rules live in RESEARCH.md "## DRIFT.md Schema"
# The class-K externalization precedent this mirrors:
@lib/core/stale-copy-scanner.cjs
# The audit being codified (FIX-13 intent, finding clusters):
@.planning/v1.13.1-DRIFT-AUDIT.md
</context>

<artifacts_this_phase_produces>
This plan introduces these NEW symbols (Plan 02 + 03 consume them):
- `lib/core/drift-baseline.cjs` module with exports:
  - `renderRootIndex(meta, findings) -> string` -- pure renderer for `.planning/DRIFT.md` (frontmatter + rolled-up table per RESEARCH schema).
  - `renderPerFolder(phase, meta, findings) -> string` -- pure renderer for `.planning/phases/<NN>/DRIFT.md`.
  - `writeDriftBaseline({ planningDir, findings, meta, noTouch }) -> { rootPath, folderPaths[], wrote[] }` -- the orchestrator that writes both granularities atomically under `.planning/`.
  - `stubMissingSummary({ phaseDir, planFile }) -> { path, created }` -- writes a minimal `<plan>-SUMMARY.md` stub for an I001 missing-SUMMARY finding (heal arm helper).
- The `finding` record contract (consumed by Plan 02 Class P/Q dispatch): `{ finding_id, track, severity, status, source_class, detail, phase, plan, per_folder }`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing test for the DRIFT.md writer (schema + diff-stability + traversal refusal)</name>
  <files>tests/test-drift-baseline.cjs</files>
  <read_first>
    - .planning/phases/150.9-.../150.9-RESEARCH.md "## DRIFT.md Schema" (the root + per-folder frontmatter, the diff-stability rules, the finding_id key formats)
    - .planning/phases/150.9-.../150.9-VALIDATION.md RQ4 row (re-run yields stable diff; closed findings retain history) and the Security Domain V12 file-resources control (write ONLY under .planning/)
    - tests/test-doctor-class-i.cjs (the hermetic mkdtempSync envelope + plain-node assert + process.exit(code) idiom this repo uses; NO jest/mocha)
  </read_first>
  <behavior>
    - Test 1 (schema round-trip): writeDriftBaseline into a mkdtempSync scratch .planning with a fixture finding set -> assert `.planning/DRIFT.md` exists, parses YAML frontmatter with kind=drift-baseline-index, has a counts block, and the body table carries one row per finding with a per_folder pointer column.
    - Test 2 (per-folder locality): a Q-track finding with phase=103 produces `.planning/phases/103/DRIFT.md` carrying ONLY phase-103 findings, with finding_ids matching the root.
    - Test 3 (diff-stability): write baseline, then write the SAME finding set again with noTouch=true -> assert the per-folder body is byte-identical (true idempotence); without noTouch -> assert ONLY last_seen lines differ and first_seen is unchanged.
    - Test 4 (closed-with-history): a finding flipped open->closed keeps its row plus a closed_date (mirrors CLAUDE.md decision 14 history preservation).
    - Test 5 (path-traversal refusal): writeDriftBaseline with a finding whose phase contains "../../etc" -> assert it throws or skips that write and NO file is created outside the scratch .planning/ (V12 control).
    - Test 6 (Part 8 floor on this module): assert grep of the module source for fetch|http|curl|brain|tavily returns 0.
  </behavior>
  <action>Create tests/test-drift-baseline.cjs as a standalone node test (no runner). Use mkdtempSync to build a scratch .planning tree, require lib/core/drift-baseline.cjs, and assert the six behaviors above. Use plain `assert` and `process.exit(failures ? 1 : 0)` matching the test-doctor-class-i.cjs idiom. The fixture finding set MUST be obviously-synthetic (e.g. phase 999, finding_id W007-999-test) so it can never be confused with a real audit baseline. This test is RED until Task 2 lands the module. NO em-dashes anywhere in the file (hyphens only).</action>
  <verify>
    <automated>node tests/test-drift-baseline.cjs; test $? -ne 0 && echo "RED as expected (module not yet built)" || echo "UNEXPECTED GREEN"</automated>
  </verify>
  <done>tests/test-drift-baseline.cjs exists, encodes the six behaviors, and fails (RED) because lib/core/drift-baseline.cjs does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lib/core/drift-baseline.cjs (pure writer + SUMMARY stub, require-clean)</name>
  <files>lib/core/drift-baseline.cjs</files>
  <read_first>
    - lib/core/stale-copy-scanner.cjs (the class-K externalization pattern: a pure lib/core module with NO process.exit, returning structured objects, that doctor.cjs requires and wraps)
    - .planning/phases/150.9-.../150.9-RESEARCH.md "## DRIFT.md Schema" (exact frontmatter keys + table columns + diff-stability rules) and the Security Domain block (V12 write-under-.planning control; V5 no untrusted shell interpolation)
  </read_first>
  <action>Implement the module using ONLY Node built-ins (fs, path) -- zero new packages, zero network (Canon Part 8). Implement renderRootIndex, renderPerFolder, writeDriftBaseline, and stubMissingSummary per the artifacts contract above. Key behaviors: (1) finding_id is the deterministic stable key per RESEARCH (W007-<phase> / I001-<phase>-<plan> for Q; P-<surface>-<token> for P) so persistent findings keep their id across audits. (2) first_seen is set once and never rewritten; last_seen bumps each run unless noTouch is true (true idempotence for tests). (3) closed findings keep their row with a closed_date. (4) BEFORE every write, resolve the target path and assert it is inside the passed planningDir via a path.resolve + startsWith(planningDir + path.sep) guard; throw a clear error if not (V12 path-traversal mitigation -- the DRIFT.md per-folder writer must never traverse outside .planning/). (5) stubMissingSummary writes a minimal SUMMARY stub ONLY at <phaseDir>/<plan>-SUMMARY.md, applying the same containment guard. The module MUST NOT call process.exit and MUST be require-clean (no top-level side effects). Add a header comment naming the FIX-13 diff-stability contract and the Fable 600s-watchdog process note (carry it into the root index process_note field per RESEARCH/CONTEXT specifics). NO em-dashes anywhere (hyphens only) -- the output DRIFT.md content and the source code both.</action>
  <verify>
    <automated>node tests/test-drift-baseline.cjs</automated>
  </verify>
  <done>tests/test-drift-baseline.cjs passes (GREEN); the module exports the four functions, is require-clean (require does not run or exit), writes both granularities under .planning/ only, refuses traversal, and `grep -nE "fetch|http|curl|brain|tavily" lib/core/drift-baseline.cjs` returns 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding record -> filesystem path | A finding's phase/plan fields flow into a write path; untrusted-shaped values could traverse outside .planning/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.9-01 | Tampering | drift-baseline.cjs per-folder + SUMMARY-stub write path | mitigate | Resolve every target with path.resolve and assert startsWith(planningDir + sep) before write; throw on violation. Tested by test-drift-baseline.cjs Test 5. |
| T-150.9-02 | Information Disclosure | drift-baseline.cjs (Canon Part 8 floor) | mitigate | Module uses only fs/path; zero network calls. Acceptance grep `fetch|http|curl|brain|tavily` returns 0 (Test 6 + Plan 03 phase gate). |
| T-150.9-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (Node built-ins only; RESEARCH Package Legitimacy Audit = empty-set case). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `node tests/test-drift-baseline.cjs` GREEN (schema round-trip + diff-stability + closed-with-history + traversal refusal + Part 8 floor).
- `grep -nE "fetch|http|curl|brain|tavily" lib/core/drift-baseline.cjs` returns 0.
- `node -e "require('./lib/core/drift-baseline.cjs')"` exits 0 and prints nothing (require-clean, no side effects, no process.exit).
</verification>

<success_criteria>
- lib/core/drift-baseline.cjs ships with the four exported functions and a frozen signature Plan 02 can wire.
- Root index + per-folder detail both written under .planning/ only; diff-stable across re-runs; traversal refused.
- Zero network surface; zero new dependencies; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-01-SUMMARY.md` when done.
</output>
