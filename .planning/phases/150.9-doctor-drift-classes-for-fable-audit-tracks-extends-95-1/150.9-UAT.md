---
phase: 150.9
slug: doctor-drift-classes-for-fable-audit-tracks-extends-95-1
status: passed
tested: 2026-06-14
method: live CLI demonstration (no UI; deterministic command output verified directly)
result: 6/6 passed, 0 issues
---

# Phase 150.9 - UAT Results

User-perspective validation of the doctor drift-classes. All behaviors run live as real `doctor` invocations; expected vs actual confirmed directly from command output.

## Test Results

| # | Behavior (must_have) | Expected | Actual | Verdict |
|---|----------------------|----------|--------|---------|
| UAT-1 | `doctor --drift` fires Class P + Class Q | both classes run, drift reported | Class P present + Class Q present in `--drift --json` | PASS |
| UAT-2 | Opt-in gating (D-04) | bare `doctor` does NOT fire P/Q | P/Q absent on bare `doctor --json` | PASS |
| UAT-3 | Deadlock carve-out (D-04 invariant) | `doctor --drift` exits 0 with drift present | exit 0 | PASS |
| UAT-4 | Heal-where-safe (D-03) | `--drift --fix` writes DRIFT.md, ZERO prose edits | root + 109 per-folder DRIFT.md written; 0 prose .md modified | PASS |
| UAT-5 | DRIFT.md is machine-diffable (FIX-13) | frontmatter counts + finding table + per-folder pointers | schema landed: counts{w007:96,i001:9,prose high:1}, finding table, Fable process-note embedded | PASS |
| UAT-6 | One-command phase gate | `bash tests/run-all-150.9.sh` green | 6/6 GREEN, exit 0 | PASS |

## Live Evidence

- **Opt-in honored:** `doctor --json` (bare) shows no Class P/Q; `doctor --drift --json` shows both. D-04 satisfied.
- **Carve-out preserved:** `doctor --drift` and `doctor --drift --fix` both exit 0 even with 105 findings present. `/mos:update` cannot deadlock.
- **Report-only proven:** after `--drift --fix`, `git status` shows 0 modified files under skills/ agents/ commands/ docs/. Only `.planning/` DRIFT.md artifacts written (gitignored).
- **Real drift caught:** the engine independently rediscovered the Fable audit findings - 96 W007 (ROADMAP gaps, including the `118-30` parser artifact Fable logged as W006), 9 I001 (missing SUMMARYs), 1 HIGH prose-vs-code.

## Issues

None. No gaps to plan.

## Verdict

PASS - all 6 must_have behaviors verified live. Phase 150.9 delivers the scoped capability and honors all four locked decisions (D-01..D-04). No `/gsd-plan-phase --gaps` needed.
