---
phase: 150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1
plan: 03
type: execute
wave: 3
depends_on: ["150.9-01", "150.9-02"]
files_modified:
  - tests/run-all-150.9.sh
  - .planning/ROADMAP.md
autonomous: true
canon_parts: [6, 7, 8]
requirements: [DDC-06, DDC-07]
must_haves:
  truths:
    - "bash tests/run-all-150.9.sh is the one-command phase gate: it runs the three CJS tests AND the two acceptance greps (Part 8 floor + deadlock carve-out) and reports a single pass/fail count"
    - "The Part 8 floor grep over the new class code (scripts/doctor.cjs new lines + lib/core/drift-baseline.cjs) returns 0 network-surface matches (mirrors the 95.2 proof)"
    - "The marketplace-cache-drift-deadlock carve-out is proven preserved: doctor --drift exits 0 with drift present"
    - "ROADMAP.md Phase 150.9 entry carries the real Goal, the minted DDC-01..DDC-08 Requirements line, and the plan list (no remaining [To be planned] / TBD placeholders for this phase)"
  artifacts:
    - path: "tests/run-all-150.9.sh"
      provides: "Phase gate aggregator mirroring run-all-150.5.sh: 3 CJS tests + Part 8 grep + deadlock-exit-0 grep"
      min_lines: 40
  key_links:
    - from: "tests/run-all-150.9.sh"
      to: "tests/test-doctor-class-p.cjs + test-doctor-class-q.cjs + test-drift-baseline.cjs"
      via: "node invocation with pass/fail tally"
      pattern: "test-doctor-class-p"
---

<objective>
Close the phase: ship the one-command phase gate `tests/run-all-150.9.sh` (mirroring run-all-150.5.sh) that aggregates the three CJS tests from Plans 01-02 plus the two acceptance greps the audit mandates -- the Canon Part 8 zero-egress floor proof (mirroring the 95.2 grep precedent) and the marketplace-cache-drift-deadlock-preserved proof (doctor --drift exits 0 with drift present). Then backfill the ROADMAP 150.9 entry: real Goal, the minted DDC-01..DDC-08 Requirements line (following the sibling 150.6 DRIFT-01..11 idiom), and the plan list -- removing the [To be planned] / TBD placeholders.

Purpose: make the phase verifiable by one green command and reflect the plan-local requirement IDs back into the roadmap so every minted ID has a home (the GSD requirement-coverage contract).
Output: tests/run-all-150.9.sh + the ROADMAP.md 150.9 backfill.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-CONTEXT.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-RESEARCH.md
@.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-VALIDATION.md

# The phase-gate aggregator pattern to mirror (two-group, single tally, named per VALIDATION):
@tests/run-all-150.5.sh
# The exact Part 8 grep precedent (95.2 proof) lives in RESEARCH.md Security Domain block.
# The sibling requirement-line idiom (DRIFT-01..11 minted 1:1 from the fix queue):
# see ROADMAP.md Phase 150.6 **Requirements** line.
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan:
- `tests/run-all-150.9.sh` -- the phase gate (3 CJS tests + Part 8 grep + deadlock-exit-0 grep, single pass/fail tally).
- ROADMAP.md Phase 150.9 backfill: Goal line, `**Requirements**: DDC-01..DDC-08` line (with the 1:1 mapping), `**Depends on:** Phase 95.1, Phase 150.6`, and the plan list rows for 150.9-01/02/03.

Minted requirement IDs (the phase's full set, reflected into ROADMAP):
- DDC-01 Class P (prose-vs-code, report-only; wraps the two checkers)
- DDC-02 Class Q (GSD execution-record drift; gsd-health shell-out, W007/I001 parse)
- DDC-03 DRIFT.md root + per-folder writer (FIX-13 diff-stable baseline)
- DDC-04 --drift opt-in activation flag (D-04; classFlagsActive registration)
- DDC-05 heal-where-safe split (D-03; auto-write DRIFT.md + stub SUMMARYs; prose report-only)
- DDC-06 Part 8 zero-egress floor proof (95.2-mirrored grep) + deadlock-carve-out-preserved proof
- DDC-07 phase-gate test suite (run-all-150.9.sh + the 3 CJS test files)
- DDC-08 SUMMARY-stub helper for I001 missing-SUMMARY heal (part of DDC-03/05 substrate)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Ship tests/run-all-150.9.sh phase gate (CJS tests + Part 8 grep + deadlock proof)</name>
  <files>tests/run-all-150.9.sh</files>
  <read_first>
    - tests/run-all-150.5.sh (the two-group single-tally aggregator pattern: pass/fail counters, named legs, one-command gate; mirror its structure)
    - .planning/phases/150.9-.../150.9-RESEARCH.md Security Domain block (the EXACT Part 8 grep + the PR-diff-scoped form) and Validation Architecture "Sampling Rate" (phase gate = full suite green + the two acceptance greps RQ1/RQ5)
    - .planning/phases/150.9-.../150.9-VALIDATION.md (the Wave 0 list this gate aggregates)
  </read_first>
  <action>Create tests/run-all-150.9.sh as a bash aggregator mirroring run-all-150.5.sh. It MUST: (1) run `node tests/test-doctor-class-p.cjs`, `node tests/test-doctor-class-q.cjs`, `node tests/test-drift-baseline.cjs`, tallying pass/fail. (2) Run the Part 8 floor grep leg (RQ5): `grep -nE "fetch|http|curl|brain|tavily" lib/core/drift-baseline.cjs` MUST return 0 matches (fail the gate on any match); for scripts/doctor.cjs use the PR-diff-scoped form from RESEARCH (`git diff origin/main -- scripts/doctor.cjs | grep '^+' | grep -E "fetch|http|curl|brain|tavily"` returns 0) so pre-existing class-M brain-smoke matches are out of scope -- guard for the no-upstream case by falling back to grepping only the new Class P/Q line range if origin/main is unavailable. (3) Run the deadlock-carve-out-preserved leg (RQ1): `node scripts/doctor.cjs --drift --json` exits 0 even with drift present (capture $?; fail if non-zero). (4) Print a final `N passed / M failed` line and exit non-zero if any leg failed. Use a clear header comment naming what the gate PROVES by instrumentation (Canon Part 6 dog-fooding), in the run-all-150.5.sh voice. NO em-dashes anywhere (hyphens only). chmod +x is not required (invoked via `bash`).</action>
  <verify>
    <automated>bash tests/run-all-150.9.sh</automated>
  </verify>
  <done>bash tests/run-all-150.9.sh runs all three CJS tests + the two greps + the deadlock-exit-0 proof, prints a single pass/fail tally, and exits 0 when everything is green.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill ROADMAP.md Phase 150.9 (Goal + DDC requirements line + plan list)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 150.9 entry (the [To be planned] / TBD placeholders to replace) and the Phase 150.6 entry (the **Requirements**: DRIFT-01..11 minted-1:1 idiom to mirror exactly)
  </read_first>
  <action>Replace the Phase 150.9 placeholders in ROADMAP.md. Set **Goal:** to a concrete one-paragraph statement derived from CONTEXT (extend the 95.1 doctor class-engine with Class P prose-vs-code report-only + Class Q GSD-record drift via gsd-health shell-out + a FIX-13 DRIFT.md root+per-folder baseline, all opt-in under `doctor --drift`, heal-where-safe, Canon Part 8 zero-egress, deadlock carve-out preserved). Set the **Requirements**: line to the minted DDC-01..DDC-08 list with the 1:1 mapping (mirror the 150.6 DRIFT-01..11 format exactly: ID then short description, comma-separated). Set **Depends on:** Phase 95.1 (the class-engine host), Phase 150.6 (the audit fix-queue context). Set **Plans:** 3 plans. Replace the `- [ ] TBD` plan-list row with three rows: `150.9-01-PLAN.md -- lib/core/drift-baseline.cjs DRIFT.md root+per-folder writer + SUMMARY stub (DDC-03, DDC-08)`, `150.9-02-PLAN.md -- Class P + Class Q + --drift flag + heal-where-safe arm in doctor.cjs (DDC-01, DDC-02, DDC-04, DDC-05)`, `150.9-03-PLAN.md -- run-all-150.9.sh phase gate + Part 8/deadlock proofs + ROADMAP backfill (DDC-06, DDC-07)`. NO em-dashes anywhere (hyphens only -- the ` -- ` separators in the existing roadmap rows are double-hyphens, keep that form). Make edits surgically; do not disturb neighboring phase entries (150.8, 150.10).</action>
  <verify>
    <automated>grep -q "DDC-01" .planning/ROADMAP.md && ! grep -A6 "Phase 150.9:" .planning/ROADMAP.md | grep -q "To be planned" && echo OK</automated>
  </verify>
  <done>ROADMAP.md Phase 150.9 carries a real Goal, the DDC-01..DDC-08 Requirements line (1:1 mapping, 150.6 idiom), Depends-on 95.1+150.6, Plans: 3, and three plan rows; no [To be planned]/TBD remain in the 150.9 block; every minted DDC-NN appears in at least one plan's frontmatter requirements (cross-checked against Plans 01/02/03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase gate <-> shipped code | the gate is the enforcement surface that proves the Part 8 boundary + deadlock invariant hold; a weak gate is the real risk here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.9-07 | Repudiation | run-all-150.9.sh false-green (gate passes while Part 8 floor or deadlock invariant is actually broken) | mitigate | The gate runs the literal greps + the literal `doctor --drift; echo $?` exit check and fails the tally on any non-zero/any match; it cannot pass on a substring (the FIX-11 production-shaped-input lesson). |
| T-150.9-08 | Information Disclosure | residual network surface in new code escaping the grep | mitigate | Grep covers BOTH new-code files (lib/core/drift-baseline.cjs full + scripts/doctor.cjs PR-diff new lines); Q's gsd-health call is a LOCAL spawnSync('node',[...]) not matched by the network token set (documented in RESEARCH Security Domain). |
| T-150.9-SC | Tampering | npm/pip/cargo installs | accept | No package installs (bash + node built-ins only). |
</threat_model>

<verification>
- `bash tests/run-all-150.9.sh` -> all legs green, single pass/fail tally, exit 0.
- `grep -nE "fetch|http|curl|brain|tavily" lib/core/drift-baseline.cjs` -> 0 matches.
- `node scripts/doctor.cjs --drift --json; echo $?` -> 0 (deadlock carve-out preserved).
- ROADMAP.md 150.9 block: real Goal, DDC-01..DDC-08 Requirements, 3 plan rows, no placeholders.
</verification>

<success_criteria>
- One green command (`bash tests/run-all-150.9.sh`) is the phase gate and includes the two acceptance greps + the deadlock proof.
- Every minted DDC requirement ID is reflected in ROADMAP and covered by at least one plan's frontmatter requirements.
- No new dependencies; no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/150.9-doctor-drift-classes-for-fable-audit-tracks-extends-95-1/150.9-03-SUMMARY.md` when done.
</output>
