---
phase: 149-gsd-planning-artifacts-as-local-graph-members
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/planning-artifacts.cjs
  - lib/core/navigation.cjs
  - tests/test-149-artifact-nodes.cjs
  - tests/test-149-lineage-edges.cjs
  - tests/test-149-idempotent-upsert.cjs
  - tests/run-all-149.sh
autonomous: true
requirements: [GAM-01, GAM-03, GAM-04]
canon_parts: [Part 6, Part 8, Part 9]

must_haves:
  truths:
    - "After a GSD doc is written, querying room.db via navigation.cjs returns a planning_artifact node with the correct artifact_type and path"
    - "Writing the same artifact node twice yields exactly one node (idempotent)"
    - "SPEC FEEDS_INTO CONTEXT FEEDS_INTO PLAN lineage edges exist and resolve only from the shipped ALLOWED_EDGE_TYPES taxonomy"
    - "The writer module carries zero direct room.db opens (substrate guard passes)"
  artifacts:
    - path: "lib/core/navigation/planning-artifacts.cjs"
      provides: "writePlanningArtifactNode + writeRequirementNode + writeLineageEdge node/edge-write chokepoint over a caller-owned db handle"
      exports: ["writePlanningArtifactNode", "writeRequirementNode", "writeLineageEdge", "ARTIFACT_TYPES", "ARTIFACT_NODE_ID", "REQUIREMENT_NODE_ID"]
      min_lines: 120
    - path: "lib/core/navigation.cjs"
      provides: "Additive re-export of the planning-artifact writers on the closed surface"
      contains: "writePlanningArtifactNode"
    - path: "tests/run-all-149.sh"
      provides: "Phase 149 scoped aggregator (clone of run-all-124.sh shape)"
      contains: "test-149-artifact-nodes.cjs"
  key_links:
    - from: "lib/core/navigation/planning-artifacts.cjs"
      to: "lib/core/navigation/edges.cjs ALLOWED_EDGE_TYPES"
      via: "writeLineageEdge delegates to writeEdge for FEEDS_INTO / VALIDATES / INFORMS"
      pattern: "FEEDS_INTO|VALIDATES|INFORMS"
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/planning-artifacts.cjs"
      via: "require + additive re-export"
      pattern: "planning-artifacts\\.cjs"
---

<objective>
Build the foundation wave: a `planning_artifact` node type, a per-requirement node type, and a typed-lineage-edge writer, all as a new navigation submodule (`lib/core/navigation/planning-artifacts.cjs`) that takes a caller-owned db handle and routes every write through the Phase 109 chokepoint. Reuse the shipped edge taxonomy (FEEDS_INTO / VALIDATES / INFORMS already in `ALLOWED_EDGE_TYPES`) with zero bespoke edge type. This is the net-new substrate the reconcile (Plan 02) and hook (Plan 03) ride on.

Purpose: Without a typed node/edge writer that honors the substrate guard (zero direct room.db opens) and the Part 9 audit-node carve-out, the reconcile and hook have nothing to call. Interface-first: this plan defines the contracts the later plans consume.
Output: `lib/core/navigation/planning-artifacts.cjs`, its additive re-export on `navigation.cjs`, the Phase 149 aggregator `tests/run-all-149.sh`, and three CJS test suites (artifact-nodes, lineage-edges, idempotent-upsert).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-SPEC.md
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-CONTEXT.md
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-VALIDATION.md

# The node-write chokepoint pattern to clone verbatim (caller-owned db handle, zero room.db open, Part 9 audit-node carve-out):
@lib/core/navigation/lens-nodes.cjs
# The edge-write primitive + the ALLOWED_EDGE_TYPES Set (FEEDS_INTO / VALIDATES / INFORMS confirmed present):
@lib/core/navigation/edges.cjs
# The audit-node carve-out reference (created_by=system review_status=confirmed for system-bookkeeping nodes):
@lib/core/navigation/focus.cjs
# The closed navigation surface + the additive-re-export idiom to mirror:
@lib/core/navigation.cjs
# The aggregator shape to clone:
@tests/run-all-124.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold + aggregator (RED-by-design)</name>
  <files>tests/run-all-149.sh, tests/test-149-artifact-nodes.cjs, tests/test-149-lineage-edges.cjs, tests/test-149-idempotent-upsert.cjs</files>
  <read_first>
    - tests/run-all-124.sh (the aggregator shape: SHELL_SUITES + CJS_SUITES arrays, per-suite PASS/FAIL, exit non-zero if any failed, RED-by-design header)
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs (the node:sqlite SKIP-on-unavailable guard at exit 77, the applySchema(db) nodes+edges in-memory fixture, the seed helper shape)
    - lib/core/migrations/phase-109-nodes-provenance.cjs lines 293-310 (the real nodes column set + the created_by CHECK constraint including 'system', + the review_status enum) so the in-memory fixture matches production
    - lib/core/lazygraph-ops.cjs lines 39-53 (the real edges table: PRIMARY KEY (source, target, type) -- this is why ON CONFLICT upsert is idempotent)
  </read_first>
  <behavior>
    - test-149-artifact-nodes.cjs: builds an in-memory DatabaseSync with the nodes+edges schema, calls writePlanningArtifactNode for a SPEC, asserts exactly one row of type 'planning_artifact' with properties.{phase, artifact_type, path, status} correct; asserts created_by='system' review_status='confirmed' (audit-node carve-out); asserts a grep of the writer source matches NO direct room.db open (the substrate-guard floor).
    - test-149-lineage-edges.cjs: seeds two artifact nodes, calls writeLineageEdge(db, {source_id, target_id, edge_type:'FEEDS_INTO'}), asserts the edge row exists; asserts writeLineageEdge rejects a non-taxonomy edge_type (e.g. 'BOGUS_LINK') with ok:false; asserts a SPEC->CONTEXT->PLAN chain is traversable.
    - test-149-idempotent-upsert.cjs: calls writePlanningArtifactNode twice for the same artifact and asserts exactly one node row after the second write; calls writeLineageEdge twice for the same triple and asserts exactly one edge row.
  </behavior>
  <action>Create tests/run-all-149.sh by cloning tests/run-all-124.sh: set CJS_SUITES to the seven Phase 149 suites (test-149-artifact-nodes.cjs, test-149-requirement-nodes.cjs, test-149-lineage-edges.cjs, test-149-idempotent-upsert.cjs, test-149-navigable.cjs, test-149-brain-egress.cjs, test-149-backfill.cjs) plus test-149-navigation-only-invariant.cjs; keep the RED-by-design header explaining each suite goes GREEN when its owning plan lands. Create the three CJS suites this plan owns (artifact-nodes, lineage-edges, idempotent-upsert) implementing the behaviors above. Each suite opens a node:sqlite in-memory db, applies an applySchema(db) that creates nodes + edges with the production column set (mirror phase-109-nodes-provenance.cjs and lazygraph-ops.cjs), guards node:sqlite-unavailable with exit 77 SKIP like the 124 invariant test, and uses node:assert/strict. NO em-dashes anywhere; the suite that asserts the no-em-dash rule must reference the em-dash and en-dash codepoints via String.fromCharCode(0x2014) and String.fromCharCode(0x2013) so the file stays greppably clean. The four not-yet-owned suites (requirement-nodes, navigable, brain-egress, backfill, navigation-only-invariant) are created by their owning plans (02/03) -- run-all-149.sh tolerates them MISSING by printing a MISSING line, exactly like run-all-124.sh.</action>
  <verify>
    <automated>bash tests/run-all-149.sh; test $? -ge 0</automated>
  </verify>
  <done>tests/run-all-149.sh runs to completion, prints per-suite PASS/FAIL/MISSING, and the three owned suites are RED (they fail because planning-artifacts.cjs does not exist yet) -- RED-by-design before Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: planning-artifacts.cjs writer submodule + navigation.cjs re-export</name>
  <files>lib/core/navigation/planning-artifacts.cjs, lib/core/navigation.cjs</files>
  <read_first>
    - lib/core/navigation/lens-nodes.cjs (clone its EXACT shape: takes db owned by caller, never requires node:sqlite, never opens room.db; INSERT ... ON CONFLICT(id) DO UPDATE SET properties=excluded.properties, last_seen_at=excluded.last_seen_at; defensive returns {ok,node_id} | {ok:false,reason})
    - lib/core/navigation/edges.cjs (writeEdge(db, {source_id, target_id, edge_type, properties}) gated on ALLOWED_EDGE_TYPES; FEEDS_INTO, VALIDATES, INFORMS are present members; the ON CONFLICT(source,target,type) DO UPDATE upsert is the idempotency mechanism)
    - lib/core/navigation/focus.cjs lines 61-74 (the CANON PART 9 AUDIT-NODE CARVE-OUT comment block + the created_by='system' review_status='confirmed' INSERT to copy verbatim for planning_artifact + requirement nodes)
    - lib/core/navigation.cjs lines 80-99 (the additive-re-export idiom and the header comment style for documenting a new re-export)
  </read_first>
  <action>Create lib/core/navigation/planning-artifacts.cjs mirroring lens-nodes.cjs structure. Export ARTIFACT_TYPES = Object.freeze(['SPEC','CONTEXT','RESEARCH','VALIDATION','PLAN','VERIFICATION','DISCUSSION-LOG']) and an ARTIFACT_TYPE_SET. Export ARTIFACT_NODE_ID(phase, artifactType) -> 'planning_artifact:'+phase+':'+artifactType and REQUIREMENT_NODE_ID(reqId) -> 'requirement:'+reqId (stable ids so the ON CONFLICT upsert deduplicates per the GAM-04 idempotence contract). writePlanningArtifactNode(db, {phase, artifactType, path, status}) validates artifactType against ARTIFACT_TYPE_SET, JSON.stringify the properties {phase, artifact_type, path, status}, and runs INSERT INTO nodes (id, type, properties, source_path, created_by, confidence, review_status, created_at, last_seen_at) VALUES (?, 'planning_artifact', ?, ?, 'system', 1.0, 'confirmed', ?, ?) ON CONFLICT(id) DO UPDATE SET properties=excluded.properties, last_seen_at=excluded.last_seen_at -- the source_path is 'planning:'+phase+':'+artifactType (a LOCAL handle, never user prose). writeRequirementNode(db, {reqId, phase}) is the same shape with type 'requirement', id REQUIREMENT_NODE_ID(reqId), properties {req_id, phase}. Document via a header comment block (copy from focus.cjs) that planning_artifact + requirement are SYSTEM-BOOKKEEPING nodes under the Part 9 v1.5 audit-node carve-out -- created_by='system' review_status='confirmed' is canon-legal, they are NOT truth-claim nodes, never need a human byUser. writeLineageEdge(db, {source_id, target_id, edge_type, properties}) is a thin pass-through to edges.writeEdge that additionally asserts edge_type is one of the LINEAGE_EDGE_TYPES it supports (FEEDS_INTO, VALIDATES, INFORMS) -- it adds NO new taxonomy member, it constrains to the existing ones, so writeEdge's ALLOWED_EDGE_TYPES gate is the single source of truth. The module requires ./edges.cjs only; it requires NEITHER node:sqlite NOR room-db.cjs (the substrate-guard floor for GAM-01). Then in lib/core/navigation.cjs add an additive re-export block (mirror the firstCapturedLastTouchedBySection / writeEdge precedent comment style) requiring ./navigation/planning-artifacts.cjs and exporting writePlanningArtifactNode, writeRequirementNode, writeLineageEdge, ARTIFACT_TYPES, ARTIFACT_NODE_ID, REQUIREMENT_NODE_ID. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-149-artifact-nodes.cjs && node tests/test-149-lineage-edges.cjs && node tests/test-149-idempotent-upsert.cjs</automated>
  </verify>
  <done>All three owned suites GREEN: a planning_artifact node lands with correct artifact_type+path+status as a system-bookkeeping node, the FEEDS_INTO/VALIDATES/INFORMS lineage edges write and a non-taxonomy edge_type is rejected, and a second write of the same node or edge does not duplicate. navigation.cjs re-exports the three writers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> planning-artifacts.cjs | the db handle and the {phase, artifactType, path, status} payload are caller-supplied; only generic LOCAL handles, never Brain-bound |
| planning-artifacts.cjs -> room.db | every write routes through the navigation allow-listed submodule; zero direct room.db open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Tampering | writeLineageEdge | mitigate | constrain edge_type to LINEAGE_EDGE_TYPES which are a subset of edges.cjs ALLOWED_EDGE_TYPES; no bespoke type can bypass the taxonomy gate |
| T-149-02 | Elevation of Privilege | planning_artifact node status | mitigate | created_by='system', review_status='confirmed' is the audit-node carve-out (Part 9 v1.5); it is NOT a truth-claim promotion and never reaches the confirmNode human-byUser path |
| T-149-03 | Tampering | direct room.db open in writer | mitigate | module requires only ./edges.cjs; substrate guard (check-substrate.cjs allow-list /^lib\/core\/navigation\//) covers it; test-149-artifact-nodes grep-asserts no room.db open |
| T-149-SC | Tampering | npm/pip/cargo installs | accept | this plan installs zero packages; pure node:sqlite + existing submodules |
</threat_model>

<verification>
- `node tests/test-149-artifact-nodes.cjs` -- planning_artifact node correct + grep-audit no room.db open
- `node tests/test-149-lineage-edges.cjs` -- FEEDS_INTO/VALIDATES/INFORMS write; non-taxonomy rejected
- `node tests/test-149-idempotent-upsert.cjs` -- second write does not duplicate node or edge
- `node scripts/check-substrate.cjs --diff` (when staged) -- planning-artifacts.cjs adds no navigation bypass
</verification>

<success_criteria>
- planning-artifacts.cjs exports the three writers + the id helpers + ARTIFACT_TYPES
- A planning_artifact node carries {phase, artifact_type, path, status} and is a system-bookkeeping node
- Lineage edges resolve only from the shipped taxonomy; idempotent on re-run
- navigation.cjs re-exports the writers; substrate guard passes
- run-all-149.sh exists and tolerates not-yet-created suites as MISSING
- Zero em-dashes in shipped output
</success_criteria>

<output>
Create `.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-01-SUMMARY.md` when done
</output>
