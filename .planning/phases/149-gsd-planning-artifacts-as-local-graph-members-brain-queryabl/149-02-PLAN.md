---
phase: 149-gsd-planning-artifacts-as-local-graph-members
plan: 02
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - lib/core/planning/reconcile-runner.cjs
  - scripts/session-start
  - tests/test-149-requirement-nodes.cjs
  - tests/test-149-backfill.cjs
  - tests/test-149-navigable.cjs
  - tests/test-149-navigation-only-invariant.cjs
autonomous: true
requirements: [GAM-02, GAM-05, GAM-07]
canon_parts: [Part 6, Part 8, Part 9]

must_haves:
  truths:
    - "First run of the reconcile backfills every existing .planning/ artifact as a planning_artifact node"
    - "Re-running the reconcile over an unchanged .planning/ tree leaves the node and edge count unchanged (idempotent backfill = sync)"
    - "Each requirement id parsed from a SPEC/ROADMAP is a requirement node; the query which artifacts touch a requirement returns the SPEC plus the owning plan"
    - "planning_artifact nodes are returned by a /mos:graph-style read and live in the active room's room.db"
    - "The reconcile reads room data ONLY via navigation.cjs (zero non-SQLite fs reads of room data)"
  artifacts:
    - path: "lib/core/planning/reconcile-runner.cjs"
      provides: "reconcilePlanningArtifacts(roomDir, opts) -- the ONE idempotent function that does backfill, sync, requirement parsing, lineage edges, and navigability; the spine"
      exports: ["reconcilePlanningArtifacts", "parseRequirementIds", "discoverPlanningArtifacts", "classifyArtifactType"]
      min_lines: 150
    - path: "scripts/session-start"
      provides: "Best-effort reconcile cascade slot (clone of the Phase 124 timeline-refresh slot)"
      contains: "reconcile-runner.cjs"
  key_links:
    - from: "lib/core/planning/reconcile-runner.cjs"
      to: "lib/core/navigation.cjs"
      via: "writePlanningArtifactNode / writeRequirementNode / writeLineageEdge"
      pattern: "writePlanningArtifactNode|writeRequirementNode|writeLineageEdge"
    - from: "scripts/session-start"
      to: "lib/core/planning/reconcile-runner.cjs"
      via: "best-effort node -e cascade slot resolving the active room"
      pattern: "reconcile-runner\\.cjs"
---

<objective>
Build the spine: ONE idempotent `reconcilePlanningArtifacts(roomDir, opts)` function that walks `.planning/`, classifies each artifact by filename, upserts its `planning_artifact` node (D-01/D-02: backfill on first run, diff-and-upsert on every session-start), parses requirement ids into `requirement` nodes (GAM-02), writes the FEEDS_INTO / VALIDATES / INFORMS lineage edges (GAM-07), and proves navigability (GAM-05). Wire it into the `scripts/session-start` cascade as a best-effort slot, exactly cloning the Phase 124 timeline-refresh slot. This is the universal mechanism that lands FIRST and covers all three surfaces (CLI / Desktop / Cowork) via the session-start reconcile.

Purpose: Per D-02 the reconcile is the single code path for backfill, ongoing sync, and tri-polar coverage. It is the load-bearing deliverable; the hook (Plan 03) is just a CLI immediacy optimization on top of it.
Output: `lib/core/planning/reconcile-runner.cjs`, the session-start cascade slot, and four CJS suites (requirement-nodes, backfill, navigable, navigation-only-invariant).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-SPEC.md
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-CONTEXT.md
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-VALIDATION.md
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-01-PLAN.md

# The runner/orchestrator analog to mirror (atomic, idempotent, reads only via navigation.cjs, best-effort on session-start):
@lib/core/feynman/timeline-runner.cjs
# The session-start cascade slot to clone verbatim (best-effort node -e, registry resolver, db open/close, || true):
# (read scripts/session-start lines 1383-1430 -- the Phase 124 timeline refresh slot)
# The navigation surface this calls (writers added by Plan 01):
@lib/core/navigation.cjs
# The Part-9 invariant test pattern to clone for the navigation-only test:
@tests/test-feynman-timeline-canon-part-9-invariant.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests for the reconcile spine (RED-by-design)</name>
  <files>tests/test-149-requirement-nodes.cjs, tests/test-149-backfill.cjs, tests/test-149-navigable.cjs, tests/test-149-navigation-only-invariant.cjs</files>
  <read_first>
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs (clone the FILES_TO_SCAN forbidden-require/forbidden-call grep sweep + the fs-instrument allow-list + the node:sqlite SKIP-at-77 guard + the applySchema fixture)
    - tests/helpers/fs-instrument.cjs (the install/calls/uninstall allow-list helper the invariant test uses)
    - lib/core/feynman/timeline-runner.cjs (the refreshAll signature shape -- the reconcile mirrors {refreshed, skipped, failed} or a {upserted, edges, pruned, unchanged} report)
  </read_first>
  <behavior>
    - test-149-requirement-nodes.cjs: builds a fixture .planning/ tree with a SPEC containing a numbered Requirements list (IRW-01..IRW-08 style) and a PLAN whose frontmatter requirements field cites a subset; runs the reconcile against an in-memory db; asserts a requirement node exists per id; asserts the query which artifacts touch <reqId> (a navigation.cjs traversal over the INFORMS/VALIDATES edges) returns the SPEC node + the owning PLAN node.
    - test-149-backfill.cjs: builds a fixture .planning/ tree with N artifacts across 2 phases; runs reconcile; asserts the expected planning_artifact node count and lineage edge count; runs reconcile AGAIN on the unchanged tree; asserts node count AND edge count are identical (idempotent backfill = sync).
    - test-149-navigable.cjs: after a reconcile, asserts a /mos:graph-style read (the same getRoomHomeView / getNeighborhood read path, or a direct type='planning_artifact' query via navigation) returns the planning_artifact nodes, proving they are reachable from the navigable surface in the active room's room.db.
    - test-149-navigation-only-invariant.cjs: greps reconcile-runner.cjs for forbidden requires (brain-client/http/https) and forbidden calls (fetch/http.request); runs the reconcile under fs-instrument and asserts every fs read is allow-listed (the .planning/ markdown it is ALLOWED to read for classification + the room.db family) and zero room-data read bypasses navigation.cjs.
  </behavior>
  <action>Create the four CJS suites implementing the behaviors above. Each builds a temp fixture .planning/ tree via fs.mkdtempSync (SPEC with a numbered requirements list and a few CONTEXT/PLAN/VERIFICATION files), opens an in-memory or temp-file DatabaseSync with the nodes+edges production schema, guards node:sqlite-unavailable with exit 77, and uses node:assert/strict. The navigation-only-invariant suite clones tests/test-feynman-timeline-canon-part-9-invariant.cjs structure but its allow-list ADDITIONALLY permits reads under the fixture .planning/ dir (the reconcile legitimately reads the planning markdown to classify + parse requirement ids -- that is the source-of-meaning read, NOT a room-data bypass; room DATA still flows only through navigation.cjs). Document that distinction in a header comment. NO em-dashes; reference forbidden dash codepoints via String.fromCharCode where asserted. These suites are RED until Task 2 lands reconcile-runner.cjs.</action>
  <verify>
    <automated>bash tests/run-all-149.sh; test $? -ge 0</automated>
  </verify>
  <done>The four suites exist and are RED-by-design (reconcile-runner.cjs absent). run-all-149.sh now finds them (no longer MISSING) and reports them FAILED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconcile-runner.cjs (the idempotent backfill = sync spine)</name>
  <files>lib/core/planning/reconcile-runner.cjs</files>
  <read_first>
    - lib/core/feynman/timeline-runner.cjs (mirror: the section walk findFeynmanSections, the safeIsDir/safeIsFile guards, the {refreshed, skipped, failed} report shape, the best-effort try/catch-per-item, the memory_event log on each pass)
    - lib/core/navigation.cjs (the writers from Plan 01: writePlanningArtifactNode, writeRequirementNode, writeLineageEdge, ARTIFACT_TYPES, ARTIFACT_NODE_ID, REQUIREMENT_NODE_ID -- call these, never open room.db)
    - .planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-SPEC.md requirement 3 (the lineage: SPEC FEEDS_INTO CONTEXT FEEDS_INTO PLAN; VERIFICATION VALIDATES requirement; PLAN -> requirement via INFORMS)
  </read_first>
  <action>Create lib/core/planning/reconcile-runner.cjs. classifyArtifactType(filename) maps a .planning filename to one of ARTIFACT_TYPES by suffix (-SPEC.md -> SPEC, -CONTEXT.md -> CONTEXT, -RESEARCH.md -> RESEARCH, -VALIDATION.md -> VALIDATION, -PLAN.md -> PLAN, -VERIFICATION.md -> VERIFICATION, -DISCUSSION-LOG.md -> DISCUSSION-LOG) and returns null for non-artifact files. discoverPlanningArtifacts(planningDir) walks .planning/phases/<phase>/*.md (and the top-level .planning/*.md for ROADMAP-style), returning [{phase, artifactType, path, status}] -- status defaults to 'present' (the frontmatter status field if parsed). parseRequirementIds(specOrRoadmapText) extracts requirement ids via the established token regex (the SPEC numbered list bolds them; ROADMAP uses **Requirements**: ID, ID -- match the /\b[A-Z]{2,}-\d{1,3}(\.\d+)?\b/ family and the bracketed-list form) and returns a deduped array. reconcilePlanningArtifacts(roomDir, opts) -- the spine: resolves the planning dir (roomDir/.planning by default, overridable via opts.planningDir for the plugin's own .planning), takes opts.db (a caller-owned handle, mirror timeline-runner), and for EACH discovered artifact calls navigation.writePlanningArtifactNode; parses requirement ids from each SPEC and the ROADMAP and calls navigation.writeRequirementNode per id; writes the lineage via navigation.writeLineageEdge -- SPEC FEEDS_INTO CONTEXT, CONTEXT FEEDS_INTO PLAN (per phase), VERIFICATION VALIDATES each requirement, PLAN INFORMS each requirement it cites in its frontmatter requirements field. Idempotence is FREE: every write is an upsert keyed on the stable node/edge id (Plan 01 contract), so a second pass over an unchanged tree changes no counts -- this is D-02 (backfill and sync are the same code path). Lifecycle discretion (CONTEXT D-02 prune-on-archive): implement create+update; for prune, detect planning_artifact nodes whose path no longer exists on disk and DELETE them via a navigation prune helper IF a clean delete path exists -- if delete-detection proves costly, SHIP create+update only and record the orphan risk in the SUMMARY (the CONTEXT explicitly permits this deferral). Return a {upserted, requirement_nodes, edges, pruned, unchanged} report. The module requires ONLY lib/core/navigation.cjs and node:fs/node:path -- NEVER brain-client, NEVER node:http/https, NEVER fetch (the GAM-06 / Part-8 floor). NO em-dashes.</action>
  <verify>
    <automated>node tests/test-149-requirement-nodes.cjs && node tests/test-149-backfill.cjs && node tests/test-149-navigable.cjs && node tests/test-149-navigation-only-invariant.cjs</automated>
  </verify>
  <done>All four suites GREEN: requirement nodes land and the touch-query resolves SPEC+plan; the backfill is idempotent across two passes; planning_artifact nodes are navigable; the reconcile reads room data only via navigation.cjs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the reconcile into the session-start cascade</name>
  <files>scripts/session-start</files>
  <read_first>
    - scripts/session-start lines 1383-1430 (the Phase 124 feynman timeline-refresh slot: the env node -e block, the registry resolver fallback to find the active room, the room.db open/close, the best-effort `|| true` that never blocks startup)
    - scripts/session-start lines 1432-1472 (the Phase 143.1-05 dial-memory slot -- a second example of the SAME slot shape, confirming the clone pattern)
  </read_first>
  <action>Add a new best-effort cascade slot to scripts/session-start immediately AFTER the dial-memory-refresh slot (after line ~1472), bounded by `# --- BEGIN gsd planning-artifact reconcile (Phase 149, best-effort) ---` / `# --- END ...`. Clone the timeline-refresh slot verbatim: an `env PLUGIN_ROOT_FOR_GSD="$PLUGIN_ROOT" ACTIVE_ROOM_FOR_GSD="${ROOM_DIR:-}" node -e '...'` block that resolves the active room (prefer ROOM_DIR, else the MindrianRooms registry resolver), opens room.db if present (node:sqlite DatabaseSync at roomDir/.mindrian/room.db), requires lib/core/planning/reconcile-runner.cjs, calls reconcilePlanningArtifacts(roomDir, {db}), closes db, writes a one-line stderr summary when upserted>0, and ends with `' 2>/dev/null || true` so a failure never blocks startup. This is D-01 (the reconcile is the universal safety net) + D-02 (session-start IS the reconcile trigger) + tri-polar (Desktop/Cowork land here since they have no PostToolUse hook). The plugin dog-foods its own .planning by passing opts.planningDir when roomDir is the mindrianOS room -- reuse the existing registry resolver; do NOT add a new resolver. NO em-dashes; bash only in the shell, node -e for the logic.</action>
  <verify>
    <automated>bash -n scripts/session-start && grep -q "reconcile-runner.cjs" scripts/session-start && ! grep -qP "$(printf '\\u2014')" scripts/session-start</automated>
  </verify>
  <done>session-start parses (bash -n), contains the reconcile slot referencing reconcile-runner.cjs, the slot is best-effort (`|| true`), and there are no em-dashes in the added block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/ markdown -> reconcile-runner | the reconcile READS the planning markdown to classify + parse requirement ids; this is the source-of-meaning read, LOCAL only, never egressed |
| reconcile-runner -> room.db | every node/edge write routes through navigation.cjs (Plan 01 writers); zero direct room.db open |
| session-start cascade -> reconcile | best-effort slot; a failure must never block Claude Code startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-04 | Information Disclosure | reconcile reading .planning prose | mitigate | the prose is read LOCALLY to derive generic handles (phase id, artifact_type, requirement id, path) only; the NODE properties carry generic handles, never artifact body prose; the navigation-only-invariant test asserts no Brain require and no network call |
| T-149-05 | Denial of Service | session-start slot blocking startup | mitigate | the slot is `2>/dev/null || true`, cloned from the proven Phase 124 timeline slot; a reconcile failure is swallowed |
| T-149-06 | Tampering | duplicate nodes on re-run | mitigate | every write is an upsert on a stable id (Plan 01); test-149-backfill asserts count stability across two passes |
| T-149-07 | Elevation of Privilege | prune deleting wrong nodes | mitigate | prune only targets planning_artifact nodes whose path is absent on disk; if delete-detection is not reliably scoped, defer prune to a follow-up (CONTEXT D-02 permits) and record orphan risk |
| T-149-SC | Tampering | npm/pip/cargo installs | accept | zero package installs; node:fs/node:path/node:sqlite + existing navigation submodule |
</threat_model>

<verification>
- `node tests/test-149-requirement-nodes.cjs` -- requirement nodes + touch-query
- `node tests/test-149-backfill.cjs` -- idempotent backfill across two passes
- `node tests/test-149-navigable.cjs` -- /mos:graph-style read returns planning_artifact nodes
- `node tests/test-149-navigation-only-invariant.cjs` -- reconcile reads room data only via navigation.cjs
- `bash -n scripts/session-start` -- the cascade slot parses
</verification>

<success_criteria>
- reconcilePlanningArtifacts is the ONE function doing backfill + sync + requirement parse + lineage + navigability
- First run backfills; second run over unchanged tree changes no counts (D-02 idempotence)
- Requirement nodes exist; which-artifacts-touch-<req> resolves SPEC + owning plan
- planning_artifact nodes are navigable in the active room's room.db
- The reconcile rides the session-start cascade best-effort (D-01 universal net, tri-polar)
- Reconcile reads room data only via navigation.cjs; zero Brain/network surface
- Zero em-dashes
</success_criteria>

<output>
Create `.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-02-SUMMARY.md` when done
</output>
