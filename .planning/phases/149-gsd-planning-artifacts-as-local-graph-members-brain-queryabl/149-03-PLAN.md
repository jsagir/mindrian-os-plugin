---
phase: 149-gsd-planning-artifacts-as-local-graph-members
plan: 03
type: execute
wave: 3
depends_on: ["149-02"]
files_modified:
  - scripts/gsd-artifact-graph-hook.cjs
  - hooks/hooks.json
  - tests/test-149-brain-egress.cjs
  - lib/core/planning/artifact-brain-packet.cjs
autonomous: true
requirements: [GAM-04, GAM-06]
canon_parts: [Part 6, Part 8, Part 9]

must_haves:
  truths:
    - "Writing a .planning/*.md file on CLI fires a PostToolUse hook that upserts the artifact node immediately (CLI immediacy on top of the reconcile)"
    - "The hook is idempotent with the reconcile -- a write the hook caught and a later session-start reconcile produce exactly one node and one edge set"
    - "Any Brain query about a planning artifact carries the Part 9 typed packet only (generic handles); zero artifact prose reaches a Brain packet"
    - "The brain-boundary scan passes for the new code; an adversarial test asserts zero raw-egress sites"
  artifacts:
    - path: "scripts/gsd-artifact-graph-hook.cjs"
      provides: "PostToolUse hook on .planning/*.md that calls the SAME reconcile (D-01 immediacy optimization), scoped to the touched file"
      min_lines: 60
    - path: "lib/core/planning/artifact-brain-packet.cjs"
      provides: "buildArtifactBrainPacket -- the typed-packet projection (generic handles only) for any Brain query about planning artifacts"
      exports: ["buildArtifactBrainPacket"]
      min_lines: 50
    - path: "hooks/hooks.json"
      provides: "Registers the gsd-artifact-graph-hook on Write|Edit|MultiEdit"
      contains: "gsd-artifact-graph-hook.cjs"
  key_links:
    - from: "scripts/gsd-artifact-graph-hook.cjs"
      to: "lib/core/planning/reconcile-runner.cjs"
      via: "calls reconcilePlanningArtifacts (same code path as session-start; D-01 belt-and-suspenders)"
      pattern: "reconcilePlanningArtifacts|reconcile-runner"
    - from: "lib/core/planning/artifact-brain-packet.cjs"
      to: "generic handles only"
      via: "phase id, requirement ids, test names, framework names, status enums -- never artifact prose"
      pattern: "phase|requirement|status"
---

<objective>
Add the CLI immediacy optimization (D-01) on top of the reconcile spine: a PostToolUse hook on `.planning/*.md` writes that calls the SAME `reconcilePlanningArtifacts` function (scoped to the touched file) so CLI users get immediate landing, while the session-start reconcile remains the universal net for Desktop/Cowork. Because the reconcile is idempotent (Plan 02), the hook and the reconcile never duplicate. Then lock the GAM-06 Brain boundary: a typed-packet projection (`buildArtifactBrainPacket`) that emits ONLY generic handles, plus the adversarial egress test proving zero artifact prose reaches a Brain packet.

Purpose: D-01 is a hybrid trigger. The hook is the CLI immediacy half; the reconcile is the universal half. This plan adds the hook half and seals the constitutional Part 8/9 boundary (GAM-06) that makes the whole bridge Brain-safe.
Output: `scripts/gsd-artifact-graph-hook.cjs`, its hooks.json registration, `lib/core/planning/artifact-brain-packet.cjs`, and the adversarial `tests/test-149-brain-egress.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-SPEC.md
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-CONTEXT.md
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-VALIDATION.md
@.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-02-PLAN.md

# The PostToolUse hook pattern to clone (reads CLAUDE tool payload, resolves room, best-effort, exits 0 always):
@scripts/memory-completion-detector.cjs
# The typed-packet contract + the sha256-by-default projection (the Part 8/9 wire; projectText hashes prose):
@lib/core/navigation/packet.cjs
# The brain-boundary sanitizer the SPEC calls check-brain-boundary (Phase 117-04):
@lib/core/brain-response-sanitize.cjs
# The reconcile this hook reuses (Plan 02): lib/core/planning/reconcile-runner.cjs -- exports reconcilePlanningArtifacts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 adversarial Brain-egress test (RED-by-design)</name>
  <files>tests/test-149-brain-egress.cjs</files>
  <read_first>
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs (the FORBIDDEN_SUBSTRINGS adversarial-seed pattern: seed the graph with a node whose properties carry a poison string, then assert the packet does NOT contain it)
    - lib/core/navigation/packet.cjs lines 99-127 (projectText hashing prose to sha256 by default; the leak-safe path)
    - lib/core/brain-response-sanitize.cjs (the sanitizer the SPEC names check-brain-boundary; how it scans for raw egress)
  </read_first>
  <behavior>
    - Seed an in-memory graph with a planning_artifact node and a requirement node whose path and properties contain FORBIDDEN_SUBSTRINGS (e.g. 'SECRET ARTIFACT PROSE', 'leak@example.com', an absolute /home/jsagi/secret/ path).
    - Call buildArtifactBrainPacket for that artifact and assert the serialized packet (JSON.stringify) contains NONE of the FORBIDDEN_SUBSTRINGS -- only generic handles (phase id, requirement ids, artifact_type enum, test names, status enum) survive.
    - Grep buildArtifactBrainPacket + reconcile-runner source for forbidden requires (brain-client/http/https) and forbidden calls (fetch/http.request) -- the structural floor.
    - Assert no em-dash / en-dash in the packet output (reference codepoints via String.fromCharCode).
  </behavior>
  <action>Create tests/test-149-brain-egress.cjs cloning the adversarial-seed structure of tests/test-feynman-timeline-canon-part-9-invariant.cjs. Build the nodes+edges in-memory schema, seed a poisoned planning_artifact node (path set to a /home/jsagi/secret/ string, properties carrying 'SECRET ARTIFACT PROSE'), call buildArtifactBrainPacket(db, {phase, artifactType}), and assert the JSON.stringify of the packet contains none of the FORBIDDEN_SUBSTRINGS. Add the forbidden-require + forbidden-call grep sweep over lib/core/planning/artifact-brain-packet.cjs AND lib/core/planning/reconcile-runner.cjs. Guard node:sqlite-unavailable with exit 77. NO em-dashes; reference dash codepoints via String.fromCharCode. RED until Task 2 lands artifact-brain-packet.cjs.</action>
  <verify>
    <automated>bash tests/run-all-149.sh; test $? -ge 0</automated>
  </verify>
  <done>tests/test-149-brain-egress.cjs exists and is RED-by-design (artifact-brain-packet.cjs absent). run-all-149.sh finds it and reports FAILED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: artifact-brain-packet.cjs (typed-packet projection, generic handles only)</name>
  <files>lib/core/planning/artifact-brain-packet.cjs</files>
  <read_first>
    - lib/core/navigation/packet.cjs (the buildBrainPacket contract + projectText sha256-by-default; mirror the generic-handles-only discipline; do NOT include node properties prose, only enums + ids + hashes)
    - the 149-SPEC.md requirement 6 (the allowed handles: phase id, requirement ids, test names, framework names, status enums; never the artifact body, filenames-as-content, or any user prose)
  </read_first>
  <action>Create lib/core/planning/artifact-brain-packet.cjs exporting buildArtifactBrainPacket(db, {phase, artifactType}). It reads the planning_artifact + requirement nodes for the phase VIA navigation.cjs (never opens room.db) and projects a typed packet containing ONLY: phase (a generic phase id handle), artifact_types (the enum set present for the phase), requirement_ids (the parsed requirement id handles), status (the status enum), and OPTIONALLY a sha256 hash of any path/prose field IF a hash is needed for dedup (reuse the packet.cjs projectText sha256 helper; never emit the raw path). It MUST NOT include: the artifact body, the raw file path, filenames-as-content, or any node properties prose. The packet is a flat object of generic scalars plus arrays of id handles. The module requires lib/core/navigation.cjs (plus optionally the packet.cjs sha256 helper) and NEVER brain-client / node:http / node:https / fetch (the Part 8 floor; this builds a packet, it does NOT send it; sending stays the existing Brain-client path which already passes the sanitizer). NO em-dashes.</action>
  <verify>
    <automated>node tests/test-149-brain-egress.cjs</automated>
  </verify>
  <done>test-149-brain-egress.cjs GREEN: the packet carries only generic handles; zero FORBIDDEN_SUBSTRINGS survive; zero forbidden requires/calls; no em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: PostToolUse hook + hooks.json registration (CLI immediacy, D-01)</name>
  <files>scripts/gsd-artifact-graph-hook.cjs, hooks/hooks.json</files>
  <read_first>
    - scripts/memory-completion-detector.cjs (the PostToolUse hook shape: read the tool payload, resolve the room, try/catch wrapping the whole body, exit 0 ALWAYS, never throw upward, never block)
    - hooks/hooks.json lines 139-200 (the PostToolUse Write|Edit|MultiEdit matcher blocks; copy one block's structure to register the new hook with a 3000ms timeout)
    - lib/core/planning/reconcile-runner.cjs (reconcilePlanningArtifacts -- the SAME function session-start calls; the hook calls it scoped to the touched file via opts)
  </read_first>
  <action>Create scripts/gsd-artifact-graph-hook.cjs as a PostToolUse hook: read the tool-call payload (the written file_path) from stdin/env the same way memory-completion-detector.cjs does, gate to ONLY fire when the path matches the planning-markdown pattern (a regex for .planning/ ending in .md); skip otherwise and exit 0 fast. When it matches, resolve the active room (reuse the same registry resolver the Plan 02 session-start slot uses), open room.db if present, and call reconcilePlanningArtifacts(roomDir, {db, planningDir, onlyPath: the_touched_file}) so the hook does a SCOPED upsert of just the touched artifact (the reconcile already upserts idempotently, so onlyPath is an optimization, not a new code path; if reconcile-runner does not accept onlyPath, call the full reconcile -- correctness over speed). Wrap the whole body in try/catch, close the db, and exit 0 ALWAYS (a hook failure must never block the user's write). This is D-01's CLI immediacy half: the hook lands the node on CLI immediately; the session-start reconcile (Plan 02) remains the universal net; because both call the idempotent reconcile, they never duplicate. Then register it in hooks/hooks.json under PostToolUse with matcher "Write|Edit|MultiEdit" and timeout 3000, cloning the structure of the existing memory-completion-detector block. NO em-dashes.</action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" && grep -q "gsd-artifact-graph-hook.cjs" hooks/hooks.json && node -c scripts/gsd-artifact-graph-hook.cjs</automated>
  </verify>
  <done>hooks.json is valid JSON and registers the hook; the hook script parses (node -c); the hook gates to .planning/*.md, reuses the idempotent reconcile, and exits 0 always.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE tool payload -> hook | the written file_path is attacker-influenceable; the hook gates on a strict .planning/*.md regex before acting |
| buildArtifactBrainPacket -> Brain wire | the packet is the ONLY thing a Brain query about artifacts may carry; it must hold generic handles only |
| hook -> reconcile -> room.db | the hook reuses the idempotent reconcile (Plan 02), which writes only via navigation.cjs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-08 | Information Disclosure | buildArtifactBrainPacket | mitigate | the packet emits ONLY phase id, requirement id handles, artifact_type enum, status enum, and sha256 hashes; the adversarial test-149-brain-egress seeds poison prose and asserts none survives in JSON.stringify(packet) |
| T-149-09 | Denial of Service | hook blocking the user's write | mitigate | the hook wraps its body in try/catch and exits 0 always; 3000ms timeout; cloned from memory-completion-detector.cjs |
| T-149-10 | Tampering | hook + reconcile double-writing | mitigate | both call the idempotent reconcile keyed on stable node/edge ids; no duplication by construction |
| T-149-11 | Elevation of Privilege | hook firing on non-planning files | mitigate | strict .planning/*.md path gate; the hook is a no-op (exit 0) for any other path |
| T-149-SC | Tampering | npm/pip/cargo installs | accept | zero package installs; node built-ins plus existing navigation submodule + reconcile |
</threat_model>

<verification>
- `node tests/test-149-brain-egress.cjs` -- zero artifact prose in the packet; zero forbidden requires/calls
- `node -c scripts/gsd-artifact-graph-hook.cjs` -- hook parses
- `node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))"` + grep -- hooks.json valid and registers the hook
- `bash tests/run-all-149.sh` -- full Phase 149 suite GREEN (all 8 suites)
</verification>

<success_criteria>
- The PostToolUse hook fires on .planning/*.md and reuses the idempotent reconcile (D-01 CLI immediacy)
- The hook never blocks a write (exit 0 always); never duplicates with the session-start reconcile
- buildArtifactBrainPacket emits generic handles only; the adversarial egress test passes (GAM-06)
- The brain-boundary scan finds zero raw-egress sites for the new code
- Zero em-dashes
</success_criteria>

<output>
Create `.planning/phases/149-gsd-planning-artifacts-as-local-graph-members-brain-queryabl/149-03-SUMMARY.md` when done
</output>
