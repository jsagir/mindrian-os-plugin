---
phase: 226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la
plan: 04
type: execute
wave: 4
depends_on: [226-01, 226-02, 226-03]
files_modified:
  - tests/test-226-posture.cjs
  - tests/test-226-pair-cap.cjs
  - tests/test-226-rejection-replay.cjs
  - tests/run-all-226.sh
  - lib/memory/run-feynman-tests.cjs
  - docs/ENV-TUNING.md
  - docs/CANON-PHASE-MAP.md
autonomous: false
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5, REQ-6, REQ-7, REQ-8]
canon_parts: [3, 8, 9]
must_haves:
  truths:
    - "banked === false (literal) on every reasoning statement, zero opportunity nodes written on the reasoning path, and a reasoning-then-embedded re-run surfaces the delta instead of silently replacing the old result (D5/G-3, REQ-4)"
    - "A synthetic 200-entry room sends pairs_sent <= MINDRIAN_EUREKA_REASONING_MAX_PAIRS to the rubric pass (D8/G-6, REQ-7)"
    - "The Phase 212 negative-corpus junk classes STAY rejected on the encoder-free reasoning path - proof the bar was not relaxed (D3, REQ-2)"
    - "bash tests/run-all-226.sh is the single PASS/FAIL/SKIP phase gate, green, exiting non-zero on any FAIL, running every leg under the zero-network preload"
    - "The navigator has verified the David proving case live: a 30-entry room with no encoder yields a real short ranked list with an honest cause and a caveat that actually prevents over-trust (the AI-SPEC human calibration checkpoint)"
  artifacts:
    - path: "tests/run-all-226.sh"
      provides: "The phase aggregator (run/run_if counters, offline preload, non-zero exit on FAIL)"
      min_lines: 40
    - path: "tests/test-226-posture.cjs"
      provides: "D5 working-diagnosis assertions (banked literal, zero graph writes, upgrade delta)"
    - path: "tests/test-226-pair-cap.cjs"
      provides: "D8 end-to-end cap assertion on a synthetic 200-entry room"
    - path: "tests/test-226-rejection-replay.cjs"
      provides: "D3 negative-corpus replay through the reasoning path"
    - path: "docs/ENV-TUNING.md"
      provides: "MINDRIAN_EUREKA_REASONING_MAX_PAIRS floor documentation"
      contains: "MINDRIAN_EUREKA_REASONING_MAX_PAIRS"
  key_links:
    - from: "tests/run-all-226.sh"
      to: "tests/eureka-offline-preload.cjs"
      via: "NODE_OPTIONS --require (the zero-network guard)"
      pattern: "eureka-offline-preload"
    - from: "lib/memory/run-feynman-tests.cjs"
      to: "tests/test-226-*.cjs"
      via: "TEST_FILES registration (the 224-VALIDATION test-infra contract precedent)"
      pattern: "test-226-"
---

<objective>
Close the phase: the remaining automated dimensions (D5 posture, D8 end-to-end cap, D3
negative-corpus replay), the single-command phase gate (tests/run-all-226.sh), test
registration, tuning/canon docs, and the mandatory HUMAN calibration checkpoint (the
AI-SPEC Section 5 rule: real-judge accuracy and caveat-wording honesty are a human
checkpoint, never an automated assertion - the Phase 212 plan-05 precedent).

Purpose: REQ-4 and REQ-7 get their end-to-end proofs; every REQ gets rolled into one
gate; the navigator (the sole Part 9 confirmer) calibrates that the caveat wording
actually prevents over-trust rather than legally covering it.

Output: three test legs, the aggregator, registration + docs, and an approved (or
issue-listed) human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-AI-SPEC.md
@.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-VALIDATION.md
@tests/run-all-212.sh
@tests/test-212-negative-corpus.cjs
@docs/ENV-TUNING.md
</context>

## Artifacts this phase produces (plan 04 subset)

New symbols created by this plan (excluded from drift verification):

- `tests/test-226-posture.cjs`, `tests/test-226-pair-cap.cjs`,
  `tests/test-226-rejection-replay.cjs`, `tests/run-all-226.sh` (new test files).
- `test-226-*` entries appended to `lib/memory/run-feynman-tests.cjs` TEST_FILES.
- `docs/ENV-TUNING.md` Phase 226 floors section; `docs/CANON-PHASE-MAP.md` Phase 226 row.

<tasks>

<task type="auto">
  <name>Task 1: D5 posture, D8 end-to-end cap, and D3 negative-corpus replay test legs</name>
  <files>tests/test-226-posture.cjs, tests/test-226-pair-cap.cjs, tests/test-226-rejection-replay.cjs</files>
  <read_first>
    - tests/test-226-field-contract.cjs (plan 02: the hermetic three-stage run pattern all three legs reuse)
    - tests/test-212-negative-corpus.cjs (the recorded junk classes and their expected verdicts)
    - tests/test-219-banking.cjs (how opportunity-node writes are counted in room.db - the zero-write assertion reuses this query idiom)
    - scripts/eureka-portfolio-report.cjs (buildUpgradeDelta and the provenance.upgrade shape from plan 02)
    - tests/fixtures/226-reasoning-pairs.cjs (plan 01 stub answers)
  </read_first>
  <action>
    tests/test-226-posture.cjs (D5/G-3, REQ-4): hermetic temp room; run the full
    reasoning flow (the field-contract pattern with --force-encoder-unavailable). Assert:
    (1) every statements[] row in the written JSON has banked === false via
    assert.strictEqual (literal, not truthiness);
    (2) zero opportunity nodes exist in room.db after the run (SELECT COUNT(*) FROM nodes
    WHERE type = 'opportunity' returns 0 - the banking hard-skip proof; use the
    test-219-banking query idiom);
    (3) provenance.banking names the skip reason string;
    (4) upgrade delta: with the reasoning JSON in place, run the embedded path over the
    same room with --offline (stub encoder, idx.embedded true); assert the new JSON
    carries provenance.upgrade with previous_run_mode 'reasoning', previous_run_date, a
    previous_top array, and survived + demoted_or_absent counts; assert the md contains
    the upgrade section heading; assert the OLD reasoning ranked rows do NOT appear
    inside the new embedded ranked table (never merged - D6);
    (5) the ranked list is SHORT: ranked.length <= provenance.reasoning_cap.
    tests/test-226-pair-cap.cjs (D8/G-6, REQ-7): generate a synthetic 200-entry room
    (two sections, programmatic small md files - up to ~19900 raw pairs). Run stage 1
    with --force-encoder-unavailable and assert: pairs.json candidates.length <= 25
    (default cap); provenance.pairs_considered > 1000 (the fan-out existed);
    provenance.pairs_sent <= provenance.reasoning_cap after a stubbed score run over the
    capped set; with MINDRIAN_EUREKA_REASONING_MAX_PAIRS=7 in the child env the cap
    becomes 7. Keep runtime bounded: entry bodies tiny (Jaccard over 200 entries is pure
    CJS math; target under 30 seconds).
    tests/test-226-rejection-replay.cjs (D3, REQ-2): take the three rejection-class
    candidates from the 212 negative corpus (reuse tests/test-212-negative-corpus.cjs's
    recorded junk texts, or byte-copy the class exemplars into this test with an
    attribution comment if that module does not export them) and drive them through
    reasoningMode.scoreReasoningPairs with rejecting stub answers derived from each
    class's failing items. Assert each routes to its expected rejecting verdict and NONE
    reaches ranked (the ranked filter excludes them - only 'transferable' ranks). Also
    assert the gate-1 exemplar (the '$2-5B exit' class) is rejected with ZERO judgeFn
    calls (Stage A Gate 1 killed it before any prompt existed).
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require ./tests/eureka-offline-preload.cjs" node tests/test-226-posture.cjs && NODE_OPTIONS="--require ./tests/eureka-offline-preload.cjs" node tests/test-226-pair-cap.cjs && NODE_OPTIONS="--require ./tests/eureka-offline-preload.cjs" node tests/test-226-rejection-replay.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All three legs exit 0 standalone under the offline preload
    - The zero-opportunity-node assertion queries the REAL room.db written by the run (behavior assertion, not source grep)
    - The 200-entry cap leg proves pairs_sent is bounded by the cap, not by room size, and the env override works in a child process
    - The rejection replay proves all three junk classes stay rejected encoder-free and the gate-1 class dies with zero judge calls
  </acceptance_criteria>
  <done>D5, D8, and the D3 negative half are proven end to end against the real flow.</done>
</task>

<task type="auto">
  <name>Task 2: tests/run-all-226.sh aggregator, TEST_FILES registration, ENV-TUNING + CANON-PHASE-MAP docs</name>
  <files>tests/run-all-226.sh, lib/memory/run-feynman-tests.cjs, docs/ENV-TUNING.md, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - tests/run-all-212.sh (FULL file, 88 lines: the run/run_if PASS/FAIL/SKIP counter shape, the NODE_OPTIONS preload export, the non-zero-exit-on-FAIL convention - reuse verbatim, do not invent a new counter/exit convention)
    - lib/memory/run-feynman-tests.cjs (TEST_FILES array at line 33, the test-224 append precedent at lines 1899-1901)
    - docs/ENV-TUNING.md (the Phase 224 floors section shape: default value, byte-matching source header, rationale, provenance)
    - docs/CANON-PHASE-MAP.md (the per-phase canon_parts row format)
  </read_first>
  <action>
    (A) tests/run-all-226.sh: bash, set -uo pipefail, cd to repo root, export
    NODE_OPTIONS="${NODE_OPTIONS:-} --require ${ROOT}/tests/eureka-offline-preload.cjs"
    (the run-all-212 zero-network guard), run/run_if functions with PASS/FAIL/SKIP
    counters, final exit tests [ FAIL -eq 0 ]. Legs, each run_if-guarded on its test
    file (partial-landing safe): test-226-null-legs (D1 - list FIRST and label it in the
    header comment as the phase's hardest gate), test-226-rubric-parity (D3),
    test-226-field-contract (D4), test-226-degrade-cause (D7), test-226-mode-disclosure
    (D6), test-226-posture (D5), test-226-pair-cap (D8), test-226-rejection-replay (D3
    negative). Plus SEED-req-7 regression legs: run the field-contract legs of the
    embedded suites directly (node tests/test-215-field-contract.cjs and
    node tests/test-216-field-contract.cjs) rather than nesting whole suites - state
    this choice and its runtime rationale in the header. Header comment maps every leg
    to its D-dimension and REQ id. No em-dashes.
    (B) lib/memory/run-feynman-tests.cjs: append the eight test-226-*.cjs paths to
    TEST_FILES (the 224-VALIDATION test-infra contract precedent, mirroring the
    lines 1899-1901 path.join shape).
    (C) docs/ENV-TUNING.md: add a Phase 226 section documenting
    MINDRIAN_EUREKA_REASONING_MAX_PAIRS (default 25, chosen to match embedded's --top 25;
    the D8 bounded-fan-out rationale: a 200-entry room has ~20k raw pairs and the cap
    bounds the paid rubric pass by the cap, never by room size; the Jaccard pre-filter
    selects the low-overlap eureka band). Byte-match the stated default against the
    reasoning-mode.cjs source constant. Note the reasoning path deliberately computes no
    AHP composite (no new AHP floor).
    (D) docs/CANON-PHASE-MAP.md: add the Phase 226 row with canon_parts [3, 8, 9] and a
    one-line description (reasoning-mode fallback: Part 8 local-only judge, Part 9
    banked-never-true on the fallback path, Part 3 gate unaffected).
  </action>
  <verify>
    <automated>bash tests/run-all-226.sh && grep -q "test-226-" lib/memory/run-feynman-tests.cjs && grep -q "MINDRIAN_EUREKA_REASONING_MAX_PAIRS" docs/ENV-TUNING.md && grep -qi "phase 226\|^| 226" docs/CANON-PHASE-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - bash tests/run-all-226.sh prints PASS/FAIL/SKIP totals with FAIL=0 and exits 0; deleting one test file yields SKIP not FAIL (run_if guard proven); a deliberately-failing leg flips the exit to non-zero (tripwire-plant proof, then reverted byte-clean - the run-all-224 idiom)
    - The eight test-226 paths appear in TEST_FILES
    - The ENV-TUNING default (25) byte-matches the reasoning-mode.cjs source
    - node scripts/doctor.cjs --acceptance shows NO NEW regression versus the documented environmental baseline {coverage-gate, verify-release-clean-tree} (the run-all-217 written-reason idiom)
  </acceptance_criteria>
  <done>One command proves the whole phase green; tests are registered; tuning and canon docs land.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Navigator calibration checkpoint - the David proving case + caveat-wording honesty</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-AI-SPEC.md (Section 5 note: real-judge accuracy and caveat wording are a HUMAN checkpoint, never an automated assertion; Section 1b domain-expert roles)
    - .planning/seeds/SEED-058-eureka-reasoning-mode-fallback.md (the David proving case: 30-entry room, encoder never cached, the old 'not enough entries' misdiagnosis)
  </read_first>
  <action>
    Pause for the navigator (blocking checkpoint). Automate nothing further: present
    what-built and the verification steps below verbatim, then wait for the resume
    signal. Any issue reported becomes a gap for /gsd-plan-phase 226 --gaps.
  </action>
  <what-built>
    The full reasoning-mode fallback: /mos:eureka on an encoder-less room now seeds
    candidate pairs, walks Larry through the faithful-judge protocol (mappings, two-pass
    rubric answers), and writes a real, short, honestly-labeled ranked report (md + json
    + html) with the caveat up front, banked always false, and an upgrade delta on a
    later embedded re-run. All eight automated dimensions (D1-D8) are green via
    bash tests/run-all-226.sh.
  </what-built>
  <how-to-verify>
    1. Build (or reuse from tests) a ~30-entry fixture room mirroring the David case
       (two or more sections, real-ish prose). If ~/MindrianRooms/david-innovation-studio
       is available and the navigator prefers the true proving case, use it read-only.
    2. Run: node scripts/eureka-portfolio-report.cjs --db <room> --pairs room
       --force-encoder-unavailable (simulates the cold machine deterministically).
       Confirm the output NAMES the cause (encoder_unavailable), not 'not enough
       entries', and points at the reasoning next step (D7 - the exact David fix).
    3. Drive the reasoning loop as Larry would (per commands/eureka.md): write
       mappings.json for the seeded pairs, run reasoning-prompts, answer the emitted
       neutral + adversarial prompt files faithfully, run reasoning-score.
    4. Open the md report and the html export (/mos:eureka html path or
       node scripts/eureka-command.cjs <room> html). Judge, as the sole Part 9
       confirmer: does the up-front caveat wording ACTUALLY stop you from over-trusting
       the ranked pairs (ICD-203 lens: is trust-in-basis clearly separate from pair
       plausibility), or does it merely legally cover it? Would a second reader of the
       html alone mistake this for a full-confidence embedded result?
    5. Re-run the embedded path over the same room with --offline and confirm the
       upgrade section reads honestly (previous reasoning result visible as a delta,
       not silently replaced).
    6. Spot-check 2-3 ranked pairs with the Gentner lens: did the rubric bar hold
       (genuine structure-mapping, not generic filler that slipped through)?
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe the caveat-wording / ranking issues found (each becomes a gap for /gsd-plan-phase 226 --gaps)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> phase gate | The aggregator's verdict is only as honest as its legs; a silently-skipped leg is a false green (the memory-filed skipped-gates bug class) |
| Navigator judgment -> Part 9 promotion | The human checkpoint is the ONLY authority that may bless the caveat wording; automation must not claim it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-01 | Tampering (integrity - THE primary phase threat) | A regression reintroducing a fabricated differential_score / semantic_similarity after ship | mitigate | test-226-null-legs runs FIRST in run-all-226.sh and is registered in TEST_FILES, making the D1 deterministic null assertion a permanent tripwire, not a one-time review (G-1's offline twin: any firing is a Critical bug, never noise) |
| T-226-02 | Elevation of privilege (Canon Part 9) | banked:true or an opportunity-node write appearing on the reasoning path later | mitigate | test-226-posture's literal banked assertion + the zero-opportunity-node room.db count run on every phase-gate invocation (G-3 made permanent) |
| T-226-12 | Repudiation (false green) | An aggregator leg silently skipping and the phase gate reading green | mitigate | run_if prints SKIP with a counter (never silent); the tripwire-plant proof in acceptance criteria demonstrates a FAIL actually flips the exit code |
| T-226-13 | Denial of service | The 200-entry cap test itself unbounded | mitigate | Tiny fixture bodies, pure-CJS Jaccard, stated under-30s target; the cap leg asserts the production bound (G-6) |
| T-226-14 | Spoofing (automation claiming human authority) | An automated leg asserting caveat-wording honesty or real-judge accuracy | mitigate | Task 3 is a blocking human-verify checkpoint (autonomous: false); the aggregator asserts STRUCTURE only, mirroring the 212 plan-05 human bar |
| T-226-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero new dependencies; no install tasks |
</threat_model>

<verification>
- bash tests/run-all-226.sh: FAIL=0, exit 0 (all D1-D8 legs + embedded regression legs)
- node scripts/doctor.cjs --acceptance: no NEW regression vs the documented baseline
- Human checkpoint resolved: navigator approved or gaps filed
</verification>

<success_criteria>
- One command (bash tests/run-all-226.sh) proves every automated dimension of the phase
- The D1 guard and the Part 9 posture checks are permanent tripwires, registered and aggregated
- The navigator has confirmed, on the David case shape, that the honest degrade actually lands: real short list, true cause, caveat that prevents over-trust, honest upgrade delta
</success_criteria>

<output>
Create `.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-04-SUMMARY.md` when done.
</output>
