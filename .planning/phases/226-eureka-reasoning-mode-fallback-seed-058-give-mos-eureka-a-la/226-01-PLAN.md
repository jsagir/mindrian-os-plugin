---
phase: 226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/eureka/reasoning-mode.cjs
  - lib/core/eureka-critic.cjs
  - lib/core/eureka/tri-modal-index.cjs
  - tests/test-226-null-legs.cjs
  - tests/test-226-rubric-parity.cjs
  - tests/fixtures/226-reasoning-pairs.cjs
autonomous: true
requirements: [REQ-1, REQ-2, REQ-7]
canon_parts: [3, 8, 9]
must_haves:
  truths:
    - "Every reasoning-mode statement object carries differential_score === null AND semantic_similarity === null, with only lsa_similarity (jaccard-v1, [0,1]) holding a real number (D1/G-1)"
    - "The reasoning-mode rubric pass calls judgeFn exactly twice per pair (neutral then adversarial), the verdict is exactly verdictFromRubric's output, and the prompts are byte-equal to critic.buildNeutralPrompt/buildAdversarialPrompt (D3/G-2)"
    - "Candidate-pair fan-out is capped by MINDRIAN_EUREKA_REASONING_MAX_PAIRS (default 25) selected by the free Jaccard pre-filter, so cost is bounded by the cap, never by room size (D8/G-6)"
    - "A garbled or missing judge answer defaults every rubric item to false (bias-to-reject) - it never silently blesses a pair"
  artifacts:
    - path: "lib/core/eureka/reasoning-mode.cjs"
      provides: "readRoomMarkdown, proposeCandidatePairs, validateMappings, emitReasoningPrompts, scoreReasoningPairs, buildReasoningStatement, assertReasoningInvariants"
      min_lines: 200
    - path: "tests/test-226-null-legs.cjs"
      provides: "The D1 null-legs regression guard (this phase's single highest-stakes correctness property)"
      min_lines: 60
    - path: "tests/test-226-rubric-parity.cjs"
      provides: "D3 critic-bar-parity assertions (call-count, verdict-by-code, prompt byte-equality)"
    - path: "tests/fixtures/226-reasoning-pairs.cjs"
      provides: "The >= 12 fixture reference dataset (AI-SPEC Section 5 composition)"
  key_links:
    - from: "lib/core/eureka/reasoning-mode.cjs"
      to: "lib/core/eureka-critic.cjs"
      via: "critic.runRubric / critic.verdictFromRubric / critic.buildNeutralPrompt / critic.buildAdversarialPrompt"
      pattern: "critic\\.runRubric"
    - from: "lib/core/eureka/reasoning-mode.cjs"
      to: "lib/core/eureka/lexical-overlap.cjs"
      via: "lexicalOverlap as the ONLY numeric anchor"
      pattern: "lexicalOverlap\\("
    - from: "lib/core/eureka/reasoning-mode.cjs"
      to: "lib/core/eureka/opportunity-statement.cjs"
      via: "oppmod.buildOpportunityStatement (the byte-parity output contract)"
      pattern: "buildOpportunityStatement"
---

<objective>
Build the reasoning-mode core module: the encoder-free scoring path that reads raw room
markdown, pre-filters candidate pairs with the free Jaccard anchor, runs the SAME Grounding
Guard Stage B two-pass rubric at full rigor via the Claude-session-as-judge seam, and emits
statements that structurally CANNOT carry a fabricated encoder number.

Purpose: Phase 226 (SEED-058) gives /mos:eureka a labeled, lower-confidence mode:reasoning
path instead of the pairs_scored:0 hard stop. This plan owns the phase's single
highest-stakes correctness property: REQ-1 / D1 / G-1, the fabricated-number prohibition.
Per the phase planning directive, the D1 null-legs test is written FIRST (Task 1), before
any implementation, and is the load-bearing regression guard for the whole phase.

Also covers REQ-2 (critic-bar parity, D3/G-2) and REQ-7 (bounded fan-out, D8/G-6 cap logic;
the cap's end-to-end provenance assertion lands in plan 04).

Output: lib/core/eureka/reasoning-mode.cjs (new), two additive exports in eureka-critic.cjs,
a 2-line _forceUnavailable forwarding seam in tri-modal-index.cjs, the >= 12 pair fixture
set, and the D1 + D3 test legs, all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-AI-SPEC.md
@.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-PATTERNS.md
@.planning/seeds/SEED-058-eureka-reasoning-mode-fallback.md
@lib/core/eureka-critic.cjs
@lib/core/eureka/lexical-overlap.cjs
@lib/core/eureka/opportunity-statement.cjs
@scripts/eureka-critic-run.cjs
</context>

## Artifacts this phase produces (plan 01 subset)

New symbols created by this plan (the plan-review source-grounding pass excludes these from
drift verification because they do not exist yet):

- `lib/core/eureka/reasoning-mode.cjs` (new module) exporting: `readRoomMarkdown`,
  `proposeCandidatePairs`, `validateMappings`, `emitReasoningPrompts`, `scoreReasoningPairs`,
  `buildReasoningStatement`, `assertReasoningInvariants`, `reasoningMaxPairs`,
  `REASONING_FORMULA_VERSION` (value `'eureka-reasoning-v1'`), and a `_test` block.
- New exports ADDED to `lib/core/eureka-critic.cjs` module.exports: `buildNeutralPrompt`,
  `buildAdversarialPrompt`, `parseRubricResponse`, and `_gate1` (an object exposing the
  existing private `FABRICATED_QUANTITY` and `DOLLAR_FIGURE` regex constants).
- `_forceUnavailable` option forwarding in `lib/core/eureka/tri-modal-index.cjs::indexNodes`.
- `tests/test-226-null-legs.cjs`, `tests/test-226-rubric-parity.cjs`,
  `tests/fixtures/226-reasoning-pairs.cjs` (new test files / fixture module).
- New env var: `MINDRIAN_EUREKA_REASONING_MAX_PAIRS` (default 25).

<tasks>

<task type="auto">
  <name>Task 1: Write the D1 null-legs regression test FIRST, plus the >= 12 pair fixture reference set</name>
  <files>tests/test-226-null-legs.cjs, tests/fixtures/226-reasoning-pairs.cjs</files>
  <read_first>
    - .planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-AI-SPEC.md (Section 5 Reference Dataset composition; D1 rubric; the zod ReasoningStatement field list in Section 4b)
    - tests/test-215-field-contract.cjs (the node:assert + hermetic-fixture idiom to imitate)
    - tests/test-212-gold-cards.cjs (the fixture-pair-with-expected-verdict idiom)
    - lib/core/eureka/lexical-overlap.cjs (lexicalOverlap signature + LEXICAL_METHOD, used by fixture expectations)
  </read_first>
  <action>
    Create tests/fixtures/226-reasoning-pairs.cjs: a pure CJS module (lexical-overlap.cjs
    module shape: 'use strict', frozen data, module.exports, no side effects) exporting
    FIXTURE_PAIRS, an array of at least 12 fixture candidate pairs. Each fixture is
    { id, textA, textB, mechanismText, mappingStatement, expected_verdict,
    neutral: {a..f}, adversarial: {a..f} } where neutral/adversarial hold the stub judge
    answers ('yes'/'no' strings, the coerceBool-compatible form) that produce the expected
    verdict through the REAL critic.runRubric. Composition per AI-SPEC Section 5:
    (a) 3-4 critical-path pairs: genuinely cross-domain, LOW Jaccard overlap between textA
    and textB, all six rubric items yes on both passes, expected_verdict 'transferable';
    (b) at least 1 fabricated-number trap: mechanismText prose that tempts a numeric
    estimate (mentions of scale, growth, valuation IN WORDS, no bare $-figures so Gate 1
    does not trip), expected_verdict 'transferable' - its purpose is the null-legs
    assertion, not rejection;
    (c) 3 rejection pairs mirroring the Phase 212 negative-corpus junk classes: one
    expected 'pseudoscience' (rubric item f no: unsourced quantity in the mapping), one
    'restatement' (item e no), one 'general_shallow' (items a/b no);
    (d) at least 1 adversarial-flip pair: neutral all-yes but adversarial answers flip at
    least one item to no, expected_verdict 'general_shallow' with reasoning_tag
    'rubric_disagreement' (the two-pass disagreement route).
    Create tests/test-226-null-legs.cjs (node:assert, exit non-zero on failure, runnable
    standalone via node): it requires lib/core/eureka/reasoning-mode.cjs and drives
    scoreReasoningPairs + buildReasoningStatement over EVERY fixture pair, then asserts,
    for every emitted statement object and every emitted ranked row:
    (1) assert.strictEqual(x.differential_score, null);
    (2) assert.strictEqual(x.semantic_similarity, null);
    (3) typeof x.lsa_similarity === 'number' && x.lsa_similarity >= 0 && x.lsa_similarity <= 1;
    (4) x.lexical_method === 'jaccard-v1';
    (5) x.banked === false (strict, literal);
    (6) x.mode === 'reasoning';
    (7) assertReasoningInvariants THROWS when handed a deep-cloned result where one
    statement's differential_score is mutated to 0.42 (the G-1 refuse-to-emit guard), and
    THROWS when one statement's banked is mutated to true (the G-3 guard), and returns
    cleanly on the unmutated result.
    This test MUST FAIL at this point (reasoning-mode.cjs does not exist yet) - run it,
    capture the require-failure, and note RED status in the commit message
    (test(226-01): add failing D1 null-legs guard). This is the phase's Wave 0 scaffold.
  </action>
  <verify>
    <automated>node tests/test-226-null-legs.cjs 2>&1 | grep -q "Cannot find module.*reasoning-mode" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/226-reasoning-pairs.cjs exports FIXTURE_PAIRS with length >= 12
      (source assertion: node -e "const f=require('./tests/fixtures/226-reasoning-pairs.cjs'); if(f.FIXTURE_PAIRS.length < 12) process.exit(1)")
    - Fixture composition includes all four AI-SPEC Section 5 classes: >= 3 expected 'transferable', >= 1 fabricated-number trap, exactly 3 rejection pairs covering pseudoscience/restatement/general_shallow, >= 1 adversarial-flip pair
    - tests/test-226-null-legs.cjs exists, uses node:assert strict equality on both null legs (grep -c "strictEqual" tests/test-226-null-legs.cjs returns >= 4)
    - The test currently fails on require of the not-yet-written module (RED proven, D1 test-first directive honored)
    - No em-dashes in either file (grep -c $'—' returns 0 on both)
  </acceptance_criteria>
  <done>The D1 regression guard exists and provably fails before implementation; the >= 12 pair reference dataset exists with the AI-SPEC composition.</done>
</task>

<task type="auto">
  <name>Task 2: Build lib/core/eureka/reasoning-mode.cjs plus the two small additive seams (critic exports, indexNodes forwarding)</name>
  <files>lib/core/eureka/reasoning-mode.cjs, lib/core/eureka-critic.cjs, lib/core/eureka/tri-modal-index.cjs</files>
  <read_first>
    - lib/core/eureka-critic.cjs (RUBRIC_ITEMS lines 342-349, verdictFromRubric 358-364, parseRubricResponse 391-409, buildNeutralPrompt/buildAdversarialPrompt 430-460, runRubric 469+, FABRICATED_QUANTITY/DOLLAR_FIGURE constants at lines 167-168, module.exports at 701)
    - scripts/eureka-critic-run.cjs (loadCandidate 128-154 for the frontmatter-strip idiom; emitPrompts 207-267 for the workdir + manifest shape; judgeFromAnswers 282-290; the score replay loop 301-338)
    - lib/core/eureka/lexical-overlap.cjs (full file, 98 lines: the module-shape template - frozen versioned constant, pure never-throw functions, _test export)
    - lib/core/eureka/opportunity-statement.cjs (buildOpportunityStatement at 488, validateCandidate's required candidate shape, the honesty-rule comment block at 221-227, runCriticGate's fail-closed {critic:'pending', banked:false} default at 275-318)
    - lib/core/eureka/tri-modal-index.cjs (indexNodes lines 355-385: the embedTexts call that forwards only options.encodeFn today)
    - lib/core/eureka/embedding-spine.cjs (embedTexts at 382 and the _forceUnavailable short-circuit at 262-268 - the existing test-hook contract being forwarded)
    - scripts/generate-export.cjs (SKIP_DIRS/SKIP_FILES sets at lines 43-44, the room-walk exclusion precedent)
  </read_first>
  <action>
    (A) lib/core/eureka-critic.cjs - ADDITIVE exports only, zero logic changes (SEED req 7):
    add to module.exports: buildNeutralPrompt, buildAdversarialPrompt, parseRubricResponse,
    and _gate1: { FABRICATED_QUANTITY: FABRICATED_QUANTITY, DOLLAR_FIGURE: DOLLAR_FIGURE }.
    Rationale comment: reasoning-mode (Phase 226) must reuse these builders byte-identically
    instead of keeping a third prompt copy (D3 prompt discipline); Gate 1 regexes are shared
    so the encoder-free fabricated-quantity gate is the SAME class, not a fork.
    (B) lib/core/eureka/tri-modal-index.cjs - forward the existing embedding-spine test
    hook: change the embedTexts call at line 365 to pass
    { encodeFn: options.encodeFn, _forceUnavailable: options._forceUnavailable }.
    Behavior when unset is byte-identical (embedTexts already treats undefined as absent);
    this is the deterministic seam plan 02's degrade tests use to force idx.embedded !== true
    on a machine where the real model IS cached. Mirror the hybrid-retrieve.cjs line 126-139
    forwarding precedent verbatim in a comment.
    (C) Create lib/core/eureka/reasoning-mode.cjs, CJS, 'use strict', node built-ins +
    in-repo requires only (zero new deps), following lexical-overlap.cjs module shape
    (pure functions, never-throw where documented, _test export, versioned frozen constant
    REASONING_FORMULA_VERSION = 'eureka-reasoning-v1'). Exports:
    (1) reasoningMaxPairs(): envInt('MINDRIAN_EUREKA_REASONING_MAX_PAIRS', 25), the
    eureka-critic.cjs lines 62-65 env-tunable idiom (parseInt, NaN/negative falls back to
    default).
    (2) readRoomMarkdown(roomDir): synchronous recursive walk of roomDir for *.md files.
    Skip directories: any dot-directory (.mindrian, .lazygraph, .git, .room-graph),
    node_modules, exports (the generate-export.cjs SKIP_DIRS precedent). Skip files:
    ROOM.md, STATE.md, MINTO.md, USER.md, ROOM-INTELLIGENCE.md, MEETINGS-INTELLIGENCE.md
    (identity scaffolding, generate-export.cjs SKIP_FILES precedent). Per file: strip YAML
    frontmatter and HTML comments (the loadCandidate idiom: /<!--[\s\S]*?-->/g), trim; skip
    entries whose remaining body is under 40 chars (noise floor). Return
    [{ id, section, title, text }] where id = path relative to roomDir, section = first
    path segment (or '(root)'), title = first '# ' heading line or the filename stem.
    Never throws on unreadable files (skip + count internally); returns [] on a missing dir.
    (3) proposeCandidatePairs(entries, opts): mechanical cross-SECTION enumeration - all
    pairs (i<j) where entries[i].section !== entries[j].section; if the room has fewer than
    2 distinct sections, fall back to all i<j pairs. For every pair compute
    lexicalOverlap(a.text, b.text) (the free pure-CJS anchor - this pre-filter is exactly
    the AI-SPEC 4b cost-routing move). Sort ascending by lsa_similarity (LOW lexical overlap
    on a still-plausible pair is the eureka signature) and cap at
    opts.maxPairs || reasoningMaxPairs(). Return { pairs_considered, cap, candidates } where
    candidates = [{ id: 'P' + zero-padded index, a, b, section_a, section_b, title_a,
    title_b, lsa_similarity, lexical_method: LEXICAL_METHOD, text_a, text_b }]. Pure,
    deterministic, never throws.
    (4) validateMappings(candidates, mappings): mappings is the session-written object
    keyed by candidate id: { mechanismText, mappingStatement }, both non-empty strings
    (bound each to 2000 chars, truncate honestly). A candidate with a missing/invalid
    mapping is EXCLUDED fail-closed and counted (never guessed). Returns
    { valid: [{...candidate, mechanismText, mappingStatement}], excluded_count }.
    (5) emitReasoningPrompts(workdir, validCandidates): per candidate run the encoder-free
    Gate 1 ONLY (critic._gate1.FABRICATED_QUANTITY.test(mechanismText + ' ' +
    mappingStatement) || DOLLAR_FIGURE.test(same)) - a tripped gate routes the candidate
    { stage_a_pass: false, route: 'pseudoscience', tag: 'unsourced_quantity' } with NO
    prompt files (the AI-SPEC Pitfall 3 rule: Stage A's swap gate needs the absent encoder
    and is SKIPPED, never faked). For passing candidates write
    <workdir>/<id>.neutral.txt = critic.buildNeutralPrompt(candidate) and
    <workdir>/<id>.adversarial.txt = critic.buildAdversarialPrompt(candidate) - call the
    newly exported builders, NEVER a local copy (byte-parity by construction). Write
    <workdir>/manifest.json mirroring eureka-critic-run.cjs's emitPrompts manifest shape
    (generated_at, formula_version, candidates: [{ id, stage_a_pass, route, tag,
    prompt_files }]).
    (6) scoreReasoningPairs(validCandidates, answers, opts): per candidate with
    stage_a_pass true, build judgeFn from answers[id] = { neutral: {a..f},
    adversarial: {a..f} } using the eureka-critic-run.cjs judgeFromAnswers pattern (the
    /Argue this proposed analogy is generic filler/ marker dispatch, replicated here or
    imported - replicate with an attribution comment since eureka-critic-run.cjs does not
    export it). Missing/garbled answers for a candidate: do NOT guess - default all items
    false via critic.parseRubricResponse's bias-to-reject (pass an empty object), record
    judge_answer_missing: true on the row, max 1 re-emit is the CALLER's concern (plan 02).
    Call await critic.runRubric({ mechanismText, mappingStatement }, { judgeFn }) - CODE
    picks the verdict. Return rows [{ ...candidate, verdict, reasoning_tag, rubric_pattern,
    judge_answer_missing }]. The loop is bounded by the already-capped candidate list (D8).
    (7) buildReasoningStatement(row, rank): assemble the candidate shape
    oppmod.buildOpportunityStatement expects ({ a: { title: title_a, primary_problem:
    'unclassified problem for ' + a, section: section_a, weak_dimensions: [] }, b: {...},
    shared_problems: [], score: 0, rank, tail: false }), call it for the canonical text,
    then OVERRIDE the honesty fields unconditionally (the runCriticGate fail-closed
    precedent): banked: false (never delegated to anything), critic: row.verdict,
    mode: 'reasoning', lsa_similarity: row.lsa_similarity, lexical_method: 'jaccard-v1',
    differential_score: null, semantic_similarity: null. Return the flat statement-row
    object matching the embedded statements[] field names (rank, a, b, tail_flag: false,
    text, banked, critic, weak_dimensions, potential_tier) plus the reasoning extensions.
    (8) assertReasoningInvariants(result): the G-1/G-3 production guard - iterates
    result.statements and result.ranked; node:assert.strictEqual(differential_score, null),
    strictEqual(semantic_similarity, null), strictEqual(banked, false),
    strictEqual(mode, 'reasoning') on every row, and
    strictEqual(result.provenance.run_mode, 'reasoning'). THROWS on any violation (refuse
    to emit - the writer in plan 02 calls this immediately before fs.writeFileSync). This
    is a deterministic code assertion, never a judge call.
    Ranking rule (documented in the module header): only verdict === 'transferable' rows
    enter ranked, ordered ascending by lsa_similarity, rank = index + 1; rejected rows are
    returned separately as rejected: [{ id, verdict, reasoning_tag }] for provenance
    counting. No AHP composite is computed - a blended quality score would fuse
    trust-in-evidence with plausibility (D2's explicit Bad case).
  </action>
  <verify>
    <automated>node -e "const rm=require('./lib/core/eureka/reasoning-mode.cjs'); const c=require('./lib/core/eureka-critic.cjs'); if(typeof rm.assertReasoningInvariants!=='function'||typeof c.buildNeutralPrompt!=='function'||typeof c._gate1.FABRICATED_QUANTITY.test!=='function') process.exit(1); console.log('exports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - reasoning-mode.cjs exports all 8 symbols plus REASONING_FORMULA_VERSION === 'eureka-reasoning-v1' and a _test block
    - eureka-critic.cjs diff is ADDITIVE-ONLY in the module.exports block plus comments (git diff shows no changed existing lines outside module.exports; behavior assertion: node tests/test-212-critic-rubric.cjs still passes)
    - tri-modal-index.cjs diff touches only the embedTexts options object (+ comment); bash tests/run-all-215.sh still fully green (embedded path byte-untouched, SEED req 7)
    - grep -c "require(" lib/core/eureka/reasoning-mode.cjs shows only node built-ins and in-repo paths (zero new npm deps)
    - Part 8: grep -nE "fetch\(|https?://|require\('node:http" lib/core/eureka/reasoning-mode.cjs returns nothing (zero egress, the test-218-extractor grep-gate idiom)
    - No em-dashes in any touched file
  </acceptance_criteria>
  <done>The reasoning-mode core module exists with the full emit/score/assemble/assert surface; both seams are in place; embedded-path suites still green.</done>
</task>

<task type="auto">
  <name>Task 3: Make the D1 test green and add the D3 critic-bar-parity test</name>
  <files>tests/test-226-rubric-parity.cjs, tests/test-226-null-legs.cjs</files>
  <read_first>
    - tests/test-212-critic-rubric.cjs (the Test 5 pattern AI-SPEC D3 names: judgeFn call-count and verdict-by-code assertions)
    - lib/core/eureka/reasoning-mode.cjs (as just written - Task 2)
    - tests/fixtures/226-reasoning-pairs.cjs (Task 1)
  </read_first>
  <action>
    Run node tests/test-226-null-legs.cjs; fix reasoning-mode.cjs (never the test's
    assertions) until green. Then create tests/test-226-rubric-parity.cjs (node:assert,
    standalone) asserting over the fixture set:
    (1) Call-count: wrap each fixture's judgeFn in a counting closure; after
    scoreReasoningPairs, every stage-A-passing candidate's count === 2 exactly (neutral
    then adversarial - G-2's "exactly twice per pair");
    (2) Verdict-by-code: for every fixture, the row verdict strictly equals
    critic.verdictFromRubric applied to the agreed items (recompute independently in the
    test from the fixture's stub answers; for the adversarial-flip fixture assert verdict
    'general_shallow' with reasoning_tag 'rubric_disagreement');
    (3) Prompt byte-equality: emitReasoningPrompts into a temp dir (fs.mkdtempSync under
    os.tmpdir()); read back <id>.neutral.txt and assert
    content === critic.buildNeutralPrompt({ mechanismText, mappingStatement }) byte-for-byte
    (same for adversarial) - proving no third prompt variant exists;
    (4) Prompt discipline: assert the prompt files contain the MECHANISM: and PROPOSED
    MAPPING: sections and do NOT contain the strings 'score', 'band', 'differential',
    'similarity', 'excitement' outside the fixed rubric text (mechanism + mapping ONLY, the
    sycophancy-channel rule);
    (5) Rejection set: the three negative-class fixtures route to their expected verdicts
    (pseudoscience / restatement / general_shallow) through the REAL runRubric - the bar
    did not relax encoder-free;
    (6) Gate 1: a candidate whose mappingStatement contains '$2-5B exit' routes
    { stage_a_pass: false, route: 'pseudoscience', tag: 'unsourced_quantity' } with zero
    prompt files written;
    (7) Cap: proposeCandidatePairs over a synthetic 40-entry two-section room returns
    candidates.length <= 25 with pairs_considered > 25 (the D8 pre-filter cap, unit level);
    with MINDRIAN_EUREKA_REASONING_MAX_PAIRS=5 in env, length <= 5;
    (8) Bias-to-reject: a candidate with answers missing entirely gets
    judge_answer_missing true and a rejecting verdict (all-false items route via
    verdictFromRubric, never 'transferable').
  </action>
  <verify>
    <automated>node tests/test-226-null-legs.cjs && node tests/test-226-rubric-parity.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-226-null-legs.cjs exits 0: every statement and ranked row across all >= 12 fixtures carries the two strict nulls, the [0,1] lsa_similarity, banked === false, mode === 'reasoning'; assertReasoningInvariants throws on both mutations (D1/G-1, REQ-1)
    - node tests/test-226-rubric-parity.cjs exits 0 with all 8 legs (D3/G-2, REQ-2; D8 cap unit leg, REQ-7)
    - bash tests/run-all-212.sh and bash tests/run-all-215.sh remain fully green (no regression to the shipped critic or embedded path)
    - Commits follow the phase convention: test(226-01) RED first, feat(226-01) module, test(226-01) parity green
  </acceptance_criteria>
  <done>Both plan-01 test legs green standalone; embedded-path and critic suites unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session-judge answers -> CJS scorer | The Claude session's rubric answers (answers.json) are untrusted free-form input crossing into deterministic scoring |
| Raw room markdown -> reasoning context | Local user content read from disk; stays room-local (Part 8 holds; zero new network surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-01 | Tampering (integrity - THE primary phase threat) | buildReasoningStatement / any writer emitting a non-null differential_score or semantic_similarity on a reasoning result | mitigate | Deterministic code assertion, not a judge call: assertReasoningInvariants (node:assert.strictEqual on both null legs) + tests/test-226-null-legs.cjs, written FIRST in this plan as the load-bearing regression guard (D1/G-1). A judge can be wrong; an assertion on a null field cannot |
| T-226-02 | Elevation of privilege (Canon Part 9 breach) | banked flipped true on the reasoning path (auto-confirmation of an unverified truth claim) | mitigate | banked: false hard-coded in buildReasoningStatement (never delegated), assertReasoningInvariants strictEqual(banked, false), null-legs test mutation leg (G-3) |
| T-226-03 | Tampering (bar-softening) | A shortened/single-pass rubric or a third freer prompt variant hiding behind the reasoning label | mitigate | test-226-rubric-parity.cjs: judgeFn call-count === 2, verdict === verdictFromRubric exactly, prompt files byte-equal to the exported critic builders (D3/G-2) |
| T-226-04 | Denial of service (cost) | Unbounded O(N^2) rubric fan-out on a large room | mitigate | Jaccard pre-filter + MINDRIAN_EUREKA_REASONING_MAX_PAIRS cap (default 25) enforced in proposeCandidatePairs, unit-asserted here, provenance-asserted in plan 04 (D8/G-6) |
| T-226-05 | Spoofing (garbled judge input) | A malformed answers.json silently blessing a pair | mitigate | parseRubricResponse bias-to-reject (unparseable -> false), judge_answer_missing recorded, never guessed |
| T-226-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero new dependencies this phase (AI-SPEC Section 2 locked); no install tasks exist, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- node tests/test-226-null-legs.cjs exits 0 (D1 - the phase's hardest gate)
- node tests/test-226-rubric-parity.cjs exits 0 (D3 + D8 unit cap)
- bash tests/run-all-212.sh green (critic untouched in behavior)
- bash tests/run-all-215.sh green (embedded path untouched - SEED req 7)
- grep -nE "fetch\(|https?://" lib/core/eureka/reasoning-mode.cjs returns nothing (Part 8)
</verification>

<success_criteria>
- The D1 null-legs guard was demonstrably RED before implementation and is green after
- reasoning-mode.cjs exposes the full documented surface with zero new deps and zero egress
- Critic-bar parity is proven by code (call-count, verdict-by-code, prompt byte-equality)
- The fixture reference set (>= 12, four composition classes) exists for every later plan
</success_criteria>

<output>
Create `.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-01-SUMMARY.md` when done.
</output>
