---
phase: 226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la
plan: 02
type: execute
wave: 2
depends_on: [226-01]
files_modified:
  - scripts/eureka-portfolio-report.cjs
  - tests/test-226-field-contract.cjs
  - tests/test-226-degrade-cause.cjs
autonomous: true
requirements: [REQ-1, REQ-3, REQ-4, REQ-6, REQ-8]
canon_parts: [3, 8, 9]
must_haves:
  truths:
    - "The reasoning branch is entered ONLY after the real embedded attempt proved idx.embedded !== true, or the scored-pairs floor was unmet after a genuine attempt - never speculatively (SEED req 2, REQ-8)"
    - "The embedded-mode scoring logic is byte-untouched: run-all-215/216/219 stay green (SEED req 7, REQ-8)"
    - "A reasoning-mode run writes the SAME { provenance, ranked, tail, statements } md+json pair with the exact embedded statements[] field names plus mode:'reasoning' (D4/G-5, REQ-3)"
    - "Degrade provenance names the CAUSE (encoder_unavailable and/or below_floor) and the render says WHY, never the bare not-enough-entries symptom (D7, REQ-6)"
    - "The banking pass is hard-skipped on the reasoning path: zero opportunity nodes written, banked can never become true (G-3, REQ-4 write-side)"
    - "A later embedded run over a room previously scored in reasoning mode surfaces the reasoning-to-embedded delta instead of silently replacing the old result (SEED req 5, D5)"
  artifacts:
    - path: "scripts/eureka-portfolio-report.cjs"
      provides: "The mode:reasoning branch inside the existing async main: stage dispatch (--reasoning-emit/--reasoning-score), degrade seeding, reasoning provenance + render caveat zone, G-1 assertion before write, upgrade delta"
      contains: "run_mode: 'reasoning'"
    - path: "tests/test-226-field-contract.cjs"
      provides: "D4 byte-parity assertions against the REAL emitter in a hermetic temp room"
    - path: "tests/test-226-degrade-cause.cjs"
      provides: "D7 honest-cause assertions (encoder_unavailable / below_floor, never the bare symptom)"
  key_links:
    - from: "scripts/eureka-portfolio-report.cjs"
      to: "lib/core/eureka/reasoning-mode.cjs"
      via: "require + the emit/score stage functions"
      pattern: "reasoning-mode\\.cjs"
    - from: "scripts/eureka-portfolio-report.cjs"
      to: "lib/core/eureka/reasoning-mode.cjs::assertReasoningInvariants"
      via: "called immediately before fs.writeFileSync of the reasoning JSON (G-1 refuse-to-emit)"
      pattern: "assertReasoningInvariants"
---

<objective>
Wire the reasoning-mode branch into scripts/eureka-portfolio-report.cjs's existing async
main, gated on the negation of the SAME idx.embedded boolean the embedded path already
computes (PATTERNS.md hard-gate rule: no second gate variable). The branch turns today's
pairs_scored:0 dead end into a three-stage flow (seed pairs -> session proposes mappings ->
emit prompts -> session answers -> score + write), producing a REAL, short, honestly
labeled ranked list in the SAME output shape embedded mode writes.

Purpose: REQ-8 (additive entry condition), REQ-3 (output-shape byte-parity, the ROADMAP
hard requirement), REQ-6 (honest degrade cause - the David proving case), REQ-4's
write-side (banking hard-skip + upgrade delta), and REQ-1's enforcement point (the G-1
assertion guards the writer, not just the tests).

Output: the modified runner plus two hermetic test legs (field-contract, degrade-cause).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-AI-SPEC.md
@.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-PATTERNS.md
@scripts/eureka-portfolio-report.cjs
@lib/core/eureka/reasoning-mode.cjs
@tests/test-215-field-contract.cjs
@tests/test-216-field-contract.cjs
</context>

## Artifacts this phase produces (plan 02 subset)

New symbols created by this plan (excluded from drift verification):

- In `scripts/eureka-portfolio-report.cjs`: new parseArgv flags `--reasoning-emit`,
  `--reasoning-score`, `--mappings <path>`, `--answers <path>`, `--reasoning-workdir <dir>`,
  `--force-encoder-unavailable`; new functions `reasoningStageSeed`, `reasoningStageEmit`,
  `reasoningStageScore`, `renderReasoningReport`, `buildUpgradeDelta` (all added to
  module.exports for hermetic tests); new provenance keys `degrade_cause`,
  `formula_version`, `reasoning_cap`, `pairs_considered`, `pairs_sent`,
  `pairs_rejected_by_rubric`, `entries_read`, `banking`, `upgrade`; `run_mode: 'reasoning'`
  as a new run_mode value on the fallback path only; `mode` field on statements[] rows
  (both paths).
- `tests/test-226-field-contract.cjs`, `tests/test-226-degrade-cause.cjs` (new test files).
- New on-disk state layout under `<roomDir>/.mindrian/eureka/reasoning/` (pairs.json,
  mappings.json, prompts/, answers.json).

<tasks>

<task type="auto">
  <name>Task 1: Stage dispatch + degrade seeding + reasoning score/write path in eureka-portfolio-report.cjs</name>
  <files>scripts/eureka-portfolio-report.cjs</files>
  <read_first>
    - scripts/eureka-portfolio-report.cjs (READ THE WHOLE async main, lines 598-1085, before touching anything - it is the byte-parity contract source; also parseArgv 137-165, the provenance object 975-1012, the jsonOut statements map 1055-1070, bankStatements 1195+, module.exports 1276)
    - .planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-PATTERNS.md (the hard-gate, provenance-extension, and report-write pattern excerpts with line anchors)
    - lib/core/eureka/reasoning-mode.cjs (the plan-01 module surface this branch calls)
    - scripts/eureka-critic-run.cjs (the --emit-prompts / --score CLI split being mirrored)
  </read_first>
  <action>
    All changes are ADDITIVE to main + parseArgv + module.exports; the embedded scoring
    block (lines 656-1085 as shipped) keeps byte-identical behavior when no reasoning flag
    is set and no degrade occurs (SEED req 7 / REQ-8; the run-all-215/216/219 suites are
    the regression oracle).
    (A) parseArgv: add cases --reasoning-emit (bool), --reasoning-score (bool),
    --mappings <path>, --answers <path>, --reasoning-workdir <path> (default
    path.join(roomDir, '.mindrian', 'eureka', 'reasoning')), --force-encoder-unavailable
    (bool, the deterministic test seam). Update HELP text honestly for all six.
    (B) Encoder-unavailable seam: where main calls triModal.indexNodes (line 656), pass
    _forceUnavailable: opts.forceEncoderUnavailable (undefined when unset - byte-identical
    default), riding the plan-01 forwarding.
    (C) Stage dispatch at the TOP of main (after parseArgv, before the AHP load): if
    opts.reasoningEmit, run reasoningStageEmit(opts) and return; if opts.reasoningScore,
    run reasoningStageScore(opts) and return. Neither stage opens the encoder or re-runs
    the embedded pipeline.
    (D) reasoningStageSeed (called from the NORMAL run, inside main, AFTER the scoring
    loop and BEFORE provenance assembly): compute degradeCause exactly once from the
    booleans the embedded path already computed - idx.embedded !== true ->
    'encoder_unavailable'; else scored.length === 0 -> 'below_floor'; else null. When
    degradeCause is non-null: call reasoningMode.readRoomMarkdown(roomDir) +
    proposeCandidatePairs, write <workdir>/pairs.json ({ generated_at, degrade_cause, cap,
    entries_read, pairs_considered, candidates }), and extend the run's provenance with
    degrade_cause plus reasoning: { state: 'await_mappings', workdir, pairs_selected,
    next_step: 'write mappings.json then run --reasoning-emit' }. The transitional
    degrade report still writes (unchanged flow) - it simply now names the CAUSE (task 2
    render) instead of dead-ending. NOTE the entry condition is evaluated only after the
    genuine embedded attempt ran (SEED req 2: never speculative).
    (E) reasoningStageEmit(opts): read pairs.json + the session-written mappings.json
    (--mappings, default <workdir>/mappings.json); reasoningMode.validateMappings;
    reasoningMode.emitReasoningPrompts into <workdir>/prompts/; print the
    eureka-critic-run.cjs-style next-step instruction (answer each *.neutral.txt and
    *.adversarial.txt faithfully into answers.json, then --reasoning-score). Exit 1 with a
    3-line error if pairs.json is missing (no genuine degrade ran first - the SEED req 2
    guard, stated in the error text).
    (F) reasoningStageScore(opts): read pairs.json, mappings.json, prompts/manifest.json,
    and answers.json (--answers, default <workdir>/answers.json). Per candidate with
    missing/garbled answers allow exactly ONE re-emit instruction path: if ANY candidate
    has judge_answer_missing after scoring AND the manifest does not yet carry
    retry_used: true, rewrite manifest with retry_used: true, print which pair ids need
    faithful re-answers, exit 2 (a distinct retriable code); on a second failure proceed
    with the bias-to-reject default (AI-SPEC 4b retry rule: max 1 retry, never guess).
    Call reasoningMode.scoreReasoningPairs, split rows into ranked (verdict
    'transferable', ascending lsa_similarity, rank = index + 1, capped by --top) and
    rejected. Build statements via reasoningMode.buildReasoningStatement. Assemble
    jsonOut with the SAME four top-level keys and the SAME field names as the embedded
    writer (read the shipped jsonOut map at lines 1039-1070 and mirror it):
    ranked rows carry rank, a, b, a_title, b_title, score: null, dims: null, weak_a: [],
    weak_b: [], complementary: false, tail_flag: false, banked: false PLUS mode:
    'reasoning', lsa_similarity, verdict, reasoning_tag, differential_score: null,
    semantic_similarity: null (honest-null parity: the field NAMES survive, no number is
    fabricated - D2's no-blended-score rule);
    tail carries the same six keys with honest empties (composition: 'not computed
    (reasoning mode: no cohort axes without the embedded index)', growth_proxy: 'none',
    insufficient_structure: true, suspect_noise: false, thresholds: null, items: []);
    statements rows carry the exact embedded field names (rank, a, b, tail_flag, text,
    banked, critic, weak_dimensions, potential_tier) plus mode, lsa_similarity,
    lexical_method, differential_score: null, semantic_similarity: null;
    provenance carries EVERY key the embedded provenance object has (975-1012) with honest
    values (run_mode: 'reasoning'; encoder_model: 'none (reasoning mode)'; encoder_dtype:
    'none'; vec_backend: 'none'; ahp_weights: null; ahp_cr: null; ahp_matrix_source: 'not
    used (reasoning mode)'; tail_* honest defaults; cohort_techs: 0; graph_nodes: 0;
    converges_pairs: 0; pairs_scored: <rubric-scored count>; scaffold_pairs_excluded: 0;
    figure_guard_skipped: <gate-1 trip count>; critic_resolution: 'reasoning rubric
    (verdict-by-code)'; honest_nouns: '(none: raw-markdown substrate)'; encoder_unavailable:
    true; run_date) PLUS degrade_cause, formula_version: reasoningMode
    .REASONING_FORMULA_VERSION, reasoning_cap, pairs_considered, pairs_sent (candidates
    actually rubric-scored), pairs_rejected_by_rubric: { pseudoscience, restatement,
    general_shallow }, entries_read, banking: 'skipped (reasoning mode: Part 9 human-only
    promotion)'.
    BANKING HARD-SKIP: the reasoning path NEVER calls bankStatements - not even with the
    default predicate - because MINDRIAN_OPPORTUNITY_BANK_PREDICATE=all would otherwise
    bank a non-pending reasoning verdict (the exact G-3 breach vector). State this in a
    comment at the skip site.
    G-1 ENFORCEMENT AT THE WRITER: immediately before BOTH fs.writeFileSync calls, call
    reasoningMode.assertReasoningInvariants(jsonOut) - a throw refuses the emit and exits
    1 with the assertion message (the online guardrail, deterministic, never a judge call).
    Write to the SAME default outPath/jsonPath the embedded run uses (byte-parity of
    location: eureka-command's report subcommand reads them unchanged).
    (G) buildUpgradeDelta (called on the EMBEDDED success path, just before the jsonPath
    write): try-read the existing jsonPath; if it parses and its provenance.run_mode ===
    'reasoning', set provenance.upgrade = { previous_run_mode: 'reasoning',
    previous_run_date: <old provenance.run_date>, previous_top: <up to 5 {a, b} from old
    statements>, survived: <count present in new ranked>, demoted_or_absent: <count not
    present> }. Any read/parse failure -> no upgrade key, byte-identical behavior (never
    throw). This is SEED req 5: the delta surfaces, the old result is never silently
    replaced.
    (H) module.exports: add reasoningStageSeed, reasoningStageEmit, reasoningStageScore,
    buildUpgradeDelta (renderReasoningReport lands in Task 2) so the tests drive stages
    hermetically without a full CLI spawn.
  </action>
  <verify>
    <automated>node -e "const r=require('./scripts/eureka-portfolio-report.cjs'); ['reasoningStageEmit','reasoningStageScore','buildUpgradeDelta'].forEach(function(k){ if(typeof r[k]!=='function'){console.error('missing '+k);process.exit(1);} }); console.log('stage exports ok')" && bash tests/run-all-215.sh</automated>
  </verify>
  <acceptance_criteria>
    - Stage dispatch works: node scripts/eureka-portfolio-report.cjs --reasoning-emit --db <tmp> exits 1 with the 3-line no-pairs.json error naming the SEED req 2 entry condition
    - The entry guard branches on the existing idx.embedded boolean and scored.length only (source assertion: no new gate variable computed from different inputs; grep shows degradeCause derived from idx.embedded and scored.length)
    - grep -n "bankStatements" scripts/eureka-portfolio-report.cjs shows NO call site reachable from the reasoning score stage (banking hard-skip with the G-3 comment)
    - grep -c "assertReasoningInvariants" scripts/eureka-portfolio-report.cjs >= 2 (required + called before write)
    - bash tests/run-all-215.sh AND bash tests/run-all-216.sh AND bash tests/run-all-219.sh all fully green (embedded path untouched - REQ-8/SEED req 7 regression oracle)
    - No em-dashes introduced
  </acceptance_criteria>
  <done>The three-stage reasoning flow runs end to end against a temp room with stubbed answers, writing md+json at the canonical paths; embedded suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Reasoning render caveat zone, mode field on every statement, honest-cause degrade message, upgrade render section</name>
  <files>scripts/eureka-portfolio-report.cjs</files>
  <read_first>
    - scripts/eureka-portfolio-report.cjs (renderReport 382-595, especially the Run mode line at 397 and the encoder_unavailable block at 426-433; the statements jsonOut map 1055-1070)
    - .planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-AI-SPEC.md (Section 1b Working-diagnosis dimension: caveat stated ONCE, prominently, up front; D6 never-merged rule; D7 symptom-vs-cause)
  </read_first>
  <action>
    (A) renderReasoningReport(ctx): a sibling render function for the reasoning score
    stage (extending renderReport's conventions, not duplicating the embedded tables).
    Structure, in order: title line naming Phase 226 reasoning mode; then the PROMINENT
    caveat block FIRST, before provenance (the D5 anti-overconfidence antidote - stated
    once, up front, never a footer). The caveat block MUST name, in Feynman-plain
    language: (1) this is a REASONING-MODE result - lower confidence BY BASIS, not a
    verdict; (2) WHY the basis is weak: the embedding encoder was unavailable
    (degrade_cause verbatim), so 2 of the critic's 3 numeric legs (differential_score,
    semantic_similarity) are structurally null and only lexical overlap (Jaccard) is a
    real number; (3) the analogy bar was NOT lowered: the same 6-item rubric ran twice
    (neutral + adversarial) with the verdict computed by code; (4) nothing here is banked
    or confirmed - a human decision is required to act (Part 9); (5) re-running after the
    encoder arrives upgrades this room to embedded mode and shows the delta.
    Then the provenance table (reuse the renderReport table idiom) including Run mode
    ('reasoning'), Degrade cause, Formula version, Entries read, Pairs considered / sent /
    cap, Rejected by rubric (per class), Banking (the skipped line).
    Then '## Ranked top N (reasoning mode)': columns rank | pair (a_title x b_title) |
    lsa_similarity | verdict | mode. NEVER a differential/semantic column, NEVER an AHP
    score column (a fabricated number column is the D1 lie in render form). Sub-note under
    the table: LOW lexical overlap on a pair the rubric still cleared is the eureka
    signature (shared meaning the vocabulary hides).
    Then '## Statements' rendering each statement text with its mode:'reasoning' tag and
    banked: false visible.
    D6 never-merge rule: this render NEVER includes embedded-mode rows; assert in code
    (one line) that every row it renders has mode === 'reasoning'.
    (B) D7 honest-cause fix in the EXISTING renderReport degrade block (lines 426-433):
    rewrite the encoder-unavailable block to name the CAUSE from
    provenance.degrade_cause ('encoder_unavailable': the embedding model is not
    installed/cached - fetch it or run reasoning mode; 'below_floor': the room's scored
    pairs came back empty after a genuine attempt). It must NOT say 'not enough entries'
    when degrade_cause is encoder_unavailable (the exact David misdiagnosis: sending the
    navigator to add content when the blocker is infrastructural). When
    provenance.reasoning exists, print the next-step line (reasoning workdir + the
    await_mappings state). Keep the block's graceful-degradation tone (Canon Decision 8).
    (C) mode field on EVERY statement (SEED req 1): in the embedded jsonOut statements
    map (lines 1055-1070) add mode: 'embedded' to each row; the reasoning writer already
    adds mode: 'reasoning'. Also append a '| Mode | ' + mode row per statement in the
    embedded markdown statements render so the label is render-visible on both paths.
    Embedded ranked rows and provenance are otherwise untouched (test-215-reproduction
    pins the live run_mode string - do NOT change embedded run_mode values).
    (D) '## Reasoning to embedded upgrade' render section on the embedded path: when
    provenance.upgrade exists, render previous_run_date, the previous top pairs, and per
    pair whether it survived into the new embedded ranked or was demoted/absent - in its
    OWN clearly-labeled section, never merged into the ranked table (D6).
    (E) Export renderReasoningReport in module.exports.
  </action>
  <verify>
    <automated>node -e "const r=require('./scripts/eureka-portfolio-report.cjs'); if(typeof r.renderReasoningReport!=='function')process.exit(1); const s=r.renderReasoningReport({provenance:{run_mode:'reasoning',degrade_cause:'encoder_unavailable',formula_version:'eureka-reasoning-v1',reasoning_cap:25,pairs_considered:3,pairs_sent:1,pairs_rejected_by_rubric:{pseudoscience:0,restatement:0,general_shallow:0},entries_read:2,banking:'skipped (reasoning mode: Part 9 human-only promotion)',run_date:'2026-07-15'},top:25,ranked:[{rank:1,a:'x',b:'y',a_title:'X',b_title:'Y',lsa_similarity:0.08,verdict:'transferable',mode:'reasoning'}],statements:[{rank:1,text:'t',mode:'reasoning',banked:false,critic:'transferable'}]}); if(!/reasoning/i.test(s)||!/encoder_unavailable/.test(s))process.exit(1); console.log('render ok')"</automated>
  </verify>
  <acceptance_criteria>
    - renderReasoningReport output places the caveat block BEFORE the provenance table (string index of the caveat heading < index of '## Provenance' equivalent) and names all five caveat elements
    - The reasoning render contains no 'differential' or 'semantic_similarity' column header and no AHP score column (grep of the rendered string)
    - renderReport's degrade block names degrade_cause and does NOT emit the bare 'not enough entries' class message when the cause is encoder_unavailable (D7)
    - Embedded statements JSON rows now carry mode: 'embedded'; reasoning rows mode: 'reasoning' (SEED req 1)
    - tests/test-215-reproduction.cjs still passes (embedded run_mode string untouched)
    - No em-dashes introduced
  </acceptance_criteria>
  <done>Both renders are honest: reasoning-mode reports lead with the caveat and never show a fabricated numeric column; degrade messages name the cause; every statement carries its mode.</done>
</task>

<task type="auto">
  <name>Task 3: Hermetic D4 field-contract and D7 degrade-cause test legs</name>
  <files>tests/test-226-field-contract.cjs, tests/test-226-degrade-cause.cjs</files>
  <read_first>
    - tests/test-215-field-contract.cjs and tests/test-216-field-contract.cjs (the hermetic temp-room + REAL-emitter + field-name assertion idiom to follow)
    - scripts/eureka-portfolio-report.cjs (as modified in Tasks 1-2: the stage exports and jsonOut shapes)
    - tests/fixtures/226-reasoning-pairs.cjs (plan 01 fixture answers reused as stub session answers)
    - tests/eureka-offline-preload.cjs (the zero-network guard both tests run under)
  </read_first>
  <action>
    Both tests are hermetic (fs.mkdtempSync temp room, real emitter functions, no network;
    document that the aggregator runs them under
    NODE_OPTIONS=--require tests/eureka-offline-preload.cjs).
    tests/test-226-field-contract.cjs (D4/G-5, REQ-3): build a temp room with 4-6 small
    markdown entries across 2 sections; drive the REAL flow: main([...normal args,
    '--force-encoder-unavailable']) -> assert pairs.json seeded + provenance.degrade_cause
    === 'encoder_unavailable'; write mappings.json + answers.json from the fixture module;
    run reasoningStageEmit then reasoningStageScore; parse the written portfolio-report
    JSON and assert:
    (1) top-level keys are exactly ['provenance', 'ranked', 'tail', 'statements'];
    (2) every statements[] row carries EXACTLY the embedded field names (rank, a, b,
    tail_flag, text, banked, critic, weak_dimensions, potential_tier) plus the reasoning
    extensions (mode, lsa_similarity, lexical_method, differential_score,
    semantic_similarity) - assert both presence and the null/false/label values;
    (3) provenance carries every key the embedded provenance emits (assert against a
    literal list copied from the shipped 975-1012 object) plus the reasoning keys
    (degrade_cause, formula_version === 'eureka-reasoning-v1', reasoning_cap, pairs_sent
    <= reasoning_cap, pairs_rejected_by_rubric, banking);
    (4) tail carries the same six keys with items.length === 0 and thresholds === null;
    (5) provenance.run_mode === 'reasoning' (D6's JSON leg);
    (6) the md report file contains the caveat block text and 'reasoning' before the
    provenance table.
    tests/test-226-degrade-cause.cjs (D7, REQ-6): three legs.
    Leg 1 (encoder_unavailable): temp room with entries, run main with
    --force-encoder-unavailable; assert provenance.degrade_cause === 'encoder_unavailable',
    pairs.json exists in the reasoning workdir, and the md degrade block names the cause
    and does NOT contain 'not enough entries'.
    Leg 2 (below_floor): temp room whose substrate yields zero scoreable pairs with the
    encoder PRESENT (use --offline so the stub encoder runs: a room with a single entry
    gives scored.length === 0); assert degrade_cause === 'below_floor' and the render
    names it.
    Leg 3 (no false trigger): a normal --offline run on a healthy multi-entry room ends
    with degrade_cause null/absent and NO reasoning workdir seeded (SEED req 2: never
    speculative).
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require ./tests/eureka-offline-preload.cjs" node tests/test-226-field-contract.cjs && NODE_OPTIONS="--require ./tests/eureka-offline-preload.cjs" node tests/test-226-degrade-cause.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both tests exit 0 standalone under the offline preload
    - test-226-field-contract drives the REAL emitter (requires scripts/eureka-portfolio-report.cjs, never a reimplementation) in a hermetic temp room
    - The field-name lists asserted are literal copies from the shipped embedded writer, not derived at runtime (a drift in either path fails the test)
    - test-226-degrade-cause leg 3 proves the reasoning branch never fires on a healthy run (REQ-8)
    - node tests/test-226-null-legs.cjs still green (D1 unchanged by the wiring)
  </acceptance_criteria>
  <done>Byte-parity and honest-cause are proven against the real emitter end to end; the entry condition is proven non-speculative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session-written mappings.json / answers.json -> runner | Untrusted free-form session output crossing into the deterministic scorer and writer |
| Prior report JSON on disk -> upgrade delta reader | A stale/corrupt previous report must never crash or skew an embedded run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-01 | Tampering (integrity - THE primary phase threat) | The reasoning writer emitting a non-null differential_score / semantic_similarity (a fabricated confidence number presented as real) | mitigate | reasoningMode.assertReasoningInvariants called immediately before BOTH fs.writeFileSync calls - a deterministic node:assert on the null fields that refuses the emit (G-1 Block). The plan-01 test-226-null-legs.cjs is the standing regression guard; test-226-field-contract re-asserts the nulls on the real emitter output |
| T-226-02 | Elevation of privilege (Canon Part 9) | banked:true reaching disk or the graph on the reasoning path | mitigate | bankStatements is UNREACHABLE from the reasoning stage (hard-skip, comment-documented against the predicate=all vector); assertReasoningInvariants strictEqual(banked, false) before write (G-3) |
| T-226-06 | Repudiation (label loss) | run_mode/mode absent from the md while present in JSON | mitigate | renderReasoningReport leads with the caveat; test-226-field-contract asserts the md caveat AND json run_mode on the same run (G-4's md+json legs; the html leg is plan 03) |
| T-226-07 | Tampering (shape drift) | A second/divergent output shape breaking every existing consumer | mitigate | test-226-field-contract literal field-name assertions against the real emitter (G-5 Block); run-all-215/216/219 as the embedded regression oracle |
| T-226-08 | Denial of service | Reasoning stages triggered speculatively or repeatedly re-emitting | mitigate | Entry only after genuine attempt (degradeCause from idx.embedded/scored.length); max-1-retry manifest latch (retry_used) in reasoningStageScore |
| T-226-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero new dependencies; no install tasks |
</threat_model>

<verification>
- NODE_OPTIONS preload + node tests/test-226-field-contract.cjs exits 0 (D4)
- NODE_OPTIONS preload + node tests/test-226-degrade-cause.cjs exits 0 (D7 + REQ-8 leg 3)
- node tests/test-226-null-legs.cjs exits 0 (D1 held through the wiring)
- bash tests/run-all-215.sh && bash tests/run-all-216.sh && bash tests/run-all-219.sh all green (SEED req 7)
</verification>

<success_criteria>
- A cold-machine room (David case shape) now produces a real, short, labeled ranked list through the three-stage flow instead of a pairs_scored:0 dead end
- The output is byte-parity with embedded mode in keys and field names, with honest nulls where the encoder legs would be
- The degrade message names the cause, never the bare symptom
- banked cannot become true and no opportunity node is written on this path
</success_criteria>

<output>
Create `.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-02-SUMMARY.md` when done.
</output>
