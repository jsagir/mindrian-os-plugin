---
phase: 226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la
plan: 03
type: execute
wave: 3
depends_on: [226-02]
files_modified:
  - lib/core/eureka/report-html.cjs
  - scripts/eureka-command.cjs
  - commands/eureka.md
  - tests/test-226-mode-disclosure.cjs
autonomous: true
requirements: [REQ-5]
canon_parts: [3, 8, 9]
must_haves:
  truths:
    - "The mode label and its caveat ride with the result at every surface: the Shape E render caveat zone, JSON provenance.run_mode, AND the /mos:eureka html export - a downstream reader of a shared report cannot mistake a reasoning-mode result for an embedded one (D6/G-4, SEED reqs 1/4/6)"
    - "The html export is self-contained, zero-network (no CDN, inline CSS only), and reads the existing portfolio-report.json - it invents no second data shape"
    - "One command, one mental model: the reasoning flow is orchestrated behind /mos:eureka via the existing governed door (eureka-command.cjs), never a separate fallback command (SEED's 'silent' framing)"
    - "Larry's judge protocol is written down: answer the emitted rubric prompts faithfully (yes/no + one sentence of evidence), NEVER estimate a similarity or differential score, never invent numbers"
  artifacts:
    - path: "lib/core/eureka/report-html.cjs"
      provides: "renderReportHtml(json) - pure De Stijl html renderer with the mandatory mode banner + reasoning caveat"
      min_lines: 80
    - path: "scripts/eureka-command.cjs"
      provides: "html, reasoning-prompts, reasoning-score subcommands + reasoning status states"
      contains: "case 'html'"
    - path: "commands/eureka.md"
      provides: "Reasoning-mode orchestration doc (Larry's faithful-judge protocol, caveat-zone render, html subcommand)"
    - path: "tests/test-226-mode-disclosure.cjs"
      provides: "D6 string assertions across md + json + html for the same run"
  key_links:
    - from: "scripts/eureka-command.cjs"
      to: "lib/core/eureka/report-html.cjs"
      via: "require + renderReportHtml on the html subcommand"
      pattern: "report-html\\.cjs"
    - from: "scripts/eureka-command.cjs"
      to: "scripts/eureka-portfolio-report.cjs"
      via: "RUNNER.main with --reasoning-emit / --reasoning-score pass-through"
      pattern: "reasoning-(emit|score)"
    - from: "commands/eureka.md"
      to: "scripts/eureka-command.cjs"
      via: "the documented shell invocations Larry runs"
      pattern: "eureka-command\\.cjs"
---

<objective>
Make the mode label impossible to lose (REQ-5 / D6 / G-4): add the /mos:eureka html export
surface (SEED req 6 names it verbatim; no eureka html surface exists in the repo today -
verified: eureka-command.cjs ships run|start|status|report only and generate-export.cjs
SKIP_DIRS excludes .mindrian/), wire the reasoning-stage subcommands through the one
governed door, and write the command-doc orchestration so Larry runs the reasoning flow
(propose mappings, answer prompts faithfully, score) with the caveat zone rendered
prominently on every surface.

Purpose: a shared report reaching a second reader who did not run the scan is the exact
over-trust vector this phase exists to prevent. The label + caveat must survive md, JSON,
and html identically.

Output: report-html.cjs (new pure renderer), three new eureka-command subcommands, the
updated commands/eureka.md, and the D6 disclosure test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-AI-SPEC.md
@scripts/eureka-command.cjs
@commands/eureka.md
@lib/core/eureka/lexical-overlap.cjs
@dashboard/export-template.html
</context>

## Artifacts this phase produces (plan 03 subset)

New symbols created by this plan (excluded from drift verification):

- `lib/core/eureka/report-html.cjs` (new module) exporting `renderReportHtml` and `_test`.
- In `scripts/eureka-command.cjs`: subcommand cases `'html'`, `'reasoning-prompts'`,
  `'reasoning-score'`; helper `outHtml(roomDir)`; status.json states
  `'reasoning_await_mappings'` and `'reasoning_await_answers'`.
- `commands/eureka.md`: new sections "Reasoning mode (lower-confidence fallback)" and
  "Subcommand: html"; argument-hint extended to `[run|status|report|html]`.
- `tests/test-226-mode-disclosure.cjs` (new test file).
- New on-disk artifact: `<roomDir>/.mindrian/eureka/portfolio-report.html`.

<tasks>

<task type="auto">
  <name>Task 1: report-html.cjs - the pure De Stijl html renderer with a mandatory mode banner</name>
  <files>lib/core/eureka/report-html.cjs</files>
  <read_first>
    - lib/core/eureka/lexical-overlap.cjs (the pure-CJS module shape template: frozen constants, never-throws, _test export)
    - dashboard/export-template.html (the existing De Stijl export look: Mondrian palette blocks, section colors - imitate the visual language with INLINE css only, do NOT reuse its CDN/script machinery)
    - scripts/eureka-portfolio-report.cjs (the jsonOut shape the renderer consumes: provenance/ranked/tail/statements field names, both modes)
  </read_first>
  <action>
    Create lib/core/eureka/report-html.cjs: 'use strict', zero deps beyond node built-ins,
    export renderReportHtml(json, opts) returning a COMPLETE self-contained html string
    (doctype, inline <style> only, no external URL of any kind - Part 8: a shared export
    must never phone home; LTR, no em-dashes, De Stijl palette: red #A63D2F-class blocks,
    yellow, blue, black rules on white, matching the export-template.html visual language).
    Structure: (1) a full-width MODE BANNER at the very top reading the mode from
    provenance.run_mode: when 'reasoning', a red-background banner 'REASONING MODE -
    LOWER-CONFIDENCE RESULT' followed by the caveat paragraph (same five elements as the
    md caveat: basis-weakness reason via degrade_cause, the two structurally-null legs,
    bar-held-at-parity, nothing banked, upgrade path); for any other run_mode a neutral
    black-on-yellow banner naming the mode string verbatim (SEED req 1: never defaulted,
    never hidden). The banner is NOT a footer and NOT collapsible. (2) a provenance table
    (key/value rows straight from the provenance object, escaped). (3) the ranked table:
    for reasoning mode the columns are rank / pair / lsa_similarity / verdict / mode; for
    embedded mode rank / pair / score / banked / mode - the renderer NEVER prints
    differential_score or semantic_similarity for reasoning rows (they are null; render
    'n/a (no encoder)' if the column would appear). (4) statements as cards, each carrying
    its mode tag and banked value visibly. (5) when provenance.upgrade exists, an 'upgrade'
    section mirroring the md render, clearly separated (D6 never-merge). HTML-escape every
    interpolated value (a room title must not inject markup). Never throws: malformed json
    returns a minimal error page naming what was missing. Include _test exports for the
    escape helper and banner builder.
  </action>
  <verify>
    <automated>node -e "const h=require('./lib/core/eureka/report-html.cjs'); const s=h.renderReportHtml({provenance:{run_mode:'reasoning',degrade_cause:'encoder_unavailable',run_date:'2026-07-15'},ranked:[],tail:{items:[]},statements:[]}); if(!/REASONING MODE/.test(s)||!/encoder_unavailable/.test(s)||/https?:\\/\\//.test(s)) process.exit(1); console.log('html ok')"</automated>
  </verify>
  <acceptance_criteria>
    - renderReportHtml output for a reasoning fixture contains the banner text, the caveat, and provenance.run_mode; for an embedded fixture contains the mode banner naming the embedded run_mode string
    - grep of the rendered string finds no external URL (no http://, https://, //cdn) - fully self-contained (Part 8)
    - The reasoning ranked table contains no differential_score / semantic_similarity numeric cells
    - Interpolated values are escaped (test with a title containing '<script>' - the output contains no executable tag)
    - Module follows the lexical-overlap shape (pure, never-throws, _test block); no em-dashes
  </acceptance_criteria>
  <done>A pure, zero-egress, De Stijl html renderer exists whose mode banner cannot be lost or defaulted.</done>
</task>

<task type="auto">
  <name>Task 2: eureka-command.cjs subcommands (html, reasoning-prompts, reasoning-score) + reasoning status states</name>
  <files>scripts/eureka-command.cjs</files>
  <read_first>
    - scripts/eureka-command.cjs (FULL file: the switch router at 435-442, runnerArgv/args construction at 282, outMd/outJson/statusPath helpers at 71-74, cmdReport's no-report 3-line error idiom at 415, the status.json state machine)
    - scripts/eureka-portfolio-report.cjs (the plan-02 stage flags and exit codes: 0 done, 1 error, 2 retriable re-answer)
    - lib/core/eureka/report-html.cjs (Task 1 renderer signature)
  </read_first>
  <action>
    (A) Add outHtml(roomDir) = path.join(reportDir(roomDir), 'portfolio-report.html').
    (B) case 'html': read outJson(roomDir); if absent print the cmdReport-style 3-line
    error ('no eureka report yet' / 'run /mos:eureka' idiom); else
    renderReportHtml(parsedJson), write to outHtml(roomDir), print the path plus one line
    naming the mode from provenance.run_mode (so even the CLI confirmation discloses it).
    (C) case 'reasoning-prompts': pass-through to RUNNER.main(['--db', roomDir,
    '--reasoning-emit', '--reasoning-workdir', <default workdir>, ...optional --mappings
    from argv]); on exit 0 write status.json { state: 'reasoning_await_answers',
    workdir, started_at }; propagate nonzero exit codes unchanged.
    (D) case 'reasoning-score': pass-through to RUNNER.main(['--db', roomDir,
    '--reasoning-score', ...optional --answers]); on exit 0 write status.json
    { state: 'done', run_mode: 'reasoning', finished_at } (the existing done-state shape
    plus run_mode); on exit 2 write { state: 'reasoning_await_answers', retry: true } and
    print the runner's re-answer instruction verbatim; on exit 1 the existing failed-state
    idiom.
    (E) In cmdRun/start completion handling: when the runner finished but the report
    JSON's provenance carries reasoning.state === 'await_mappings' (the plan-02 seed),
    write status.json { state: 'reasoning_await_mappings', workdir, pairs_selected } so
    /mos:eureka status tells Larry (and the navigator) exactly where the flow stands -
    the SEED's 'silent' promise: the navigator never manages the degrade manually.
    (F) Update USAGE/HELP text for the three subcommands and the unknown-subcommand hint
    list ('run one of: run | start | status | report | html | reasoning-prompts |
    reasoning-score | help'). Router stays the plain switch-case (no Commander/yargs,
    repo convention).
  </action>
  <verify>
    <automated>node -e "const src=require('node:fs').readFileSync('scripts/eureka-command.cjs','utf8'); ['case \'html\'','reasoning-prompts','reasoning-score','reasoning_await_mappings','reasoning_await_answers'].forEach(function(t){ if(src.indexOf(t)===-1){console.error('missing '+t);process.exit(1);} }); console.log('wiring present')" && node scripts/eureka-command.cjs /tmp/nonexistent-room-226 html 2>&1 | grep -qi "no eureka report" && echo "html no-report path ok"</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/eureka-command.cjs <temp room with a fixture portfolio-report.json> html writes portfolio-report.html and prints its path plus the mode line
    - reasoning-prompts / reasoning-score are thin pass-throughs (no scoring logic in the dispatcher - one governed path, the SEED-034 one-door rule stated in a comment)
    - Exit code 2 from the score stage round-trips as the retriable state, never swallowed
    - tests/test-216-eureka-command.cjs still passes (existing subcommands untouched)
    - No em-dashes introduced
  </acceptance_criteria>
  <done>All three subcommands work through the one governed door with honest status states.</done>
</task>

<task type="auto">
  <name>Task 3: commands/eureka.md orchestration doc + the D6 mode-disclosure test</name>
  <files>commands/eureka.md, tests/test-226-mode-disclosure.cjs</files>
  <read_first>
    - commands/eureka.md (FULL file: frontmatter incl. argument-hint, the Shape E 4-zone render anatomy at the Zone 2 block, the Step 1-3 run flow, the existing degrade guidance)
    - .planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-AI-SPEC.md (Section 4 Prompt Engineering Discipline - what the reasoning pass must and must not be asked; D6's three-surface rule)
    - tests/test-226-field-contract.cjs (plan 02: the hermetic run pattern this test extends to html)
  </read_first>
  <action>
    (A) commands/eureka.md: extend argument-hint to "[run|status|report|html]". Add a
    'Reasoning mode (lower-confidence fallback)' section documenting the full loop Larry
    drives when status reads reasoning_await_mappings: (1) read
    .mindrian/eureka/reasoning/pairs.json; (2) for each candidate write mappings.json:
    a one-line mappingStatement naming the shared relational schema WITHOUT either
    domain's nouns, and mechanismText selected from the pair's own entry prose - never
    invented; (3) run reasoning-prompts; (4) answer EVERY *.neutral.txt and
    *.adversarial.txt FAITHFULLY into answers.json ({ '<id>': { neutral: {a..f: 'yes'|'no'},
    adversarial: {...} } }) - the faithful-judge protocol block MUST state verbatim rules:
    answer each item yes or no with one sentence of evidence; take the skeptical reading
    when unsure on the adversarial pass; NEVER estimate a semantic-similarity or
    differential score; NEVER invent a number (the fabricated-number prohibition D1 -
    those fields are structurally null in this mode and the writer will refuse to emit
    otherwise); (5) run reasoning-score; (6) render the report through Shape E with the
    caveat in the TOP zone (Zone 1), stated once, prominently - never a footer (SEED
    req 4). Update the Zone 2 anatomy so the Mode field renders on EVERY result
    (embedded or reasoning) and reasoning results show lsa_similarity + verdict, never a
    differential column. Add the 'Subcommand: html' section (shareable De Stijl export;
    tell the navigator the mode banner rides with it). Update the existing
    encoder-degrade paragraph (the '--offline degrades to an honest empty report' line)
    to name the new honest flow: the degrade now yields a REAL short reasoning-mode list,
    cause named, upgrade-on-re-run. Frontmatter keys other than argument-hint untouched.
    (B) tests/test-226-mode-disclosure.cjs (D6/G-4, REQ-5): hermetic; reuse the plan-02
    field-contract flow to produce a reasoning-mode md+json in a temp room, then call the
    eureka-command html path (require scripts/eureka-command.cjs is main-guarded, so
    spawn node scripts/eureka-command.cjs <room> html) and assert on the SAME run:
    (1) md contains the caveat block and the word 'reasoning' before the provenance table;
    (2) json provenance.run_mode === 'reasoning';
    (3) html file exists, contains 'REASONING MODE', the caveat, and degrade_cause;
    (4) html contains no external URL (self-contained);
    (5) an embedded-shaped fixture json written to the same room and re-exported yields
    an html whose banner names the embedded run_mode (the label is never reasoning-only);
    (6) commands/eureka.md contains the strings 'html', 'reasoning_await_mappings' or
    'reasoning mode', and the faithful-judge NEVER-estimate rule (doc-parity leg, the
    Phase 217 D-04 doc-regression idiom: the doc must not rot).
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require ./tests/eureka-offline-preload.cjs" node tests/test-226-mode-disclosure.cjs</automated>
  </verify>
  <acceptance_criteria>
    - test-226-mode-disclosure exits 0: label + caveat asserted present in md AND json AND html for one and the same reasoning run (the G-4 three-surface check)
    - commands/eureka.md names the faithful-judge protocol including the never-estimate-a-score rule and the Zone 1 caveat placement
    - argument-hint includes html
    - grep -c $'—' commands/eureka.md returns 0 (no em-dashes)
  </acceptance_criteria>
  <done>The mode label provably survives all three artifacts of a single run, and the command doc teaches Larry the exact honest protocol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portfolio-report.json -> html export | Room-derived strings interpolated into shareable html (escape or a room title injects markup into a report a second reader opens) |
| Shared html -> second reader | A reader who did not run the scan sees ONLY this artifact; the mode label is their entire calibration signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-01 | Tampering (integrity - THE primary phase threat) | The html surface re-printing a fabricated differential/semantic number for a reasoning result | mitigate | renderReportHtml renders no differential/semantic cell for reasoning rows (the nulls stay null; 'n/a (no encoder)' if the column exists); test-226-mode-disclosure asserts the html carries the caveat, and the upstream writer already refused any non-null leg (plan 02 G-1 assertion) |
| T-226-09 | Repudiation / Information disclosure (label loss - G-4) | mode present in JSON but absent from html or md for the same run, letting a shared report read as embedded | mitigate | Mandatory non-collapsible mode banner in renderReportHtml (never defaulted: unknown mode renders the verbatim string, never blank); test-226-mode-disclosure string-asserts all three surfaces on one run |
| T-226-10 | Injection (XSS-class in a local artifact) | Room content (titles, statement text) injected unescaped into the html export | mitigate | HTML-escape helper applied to every interpolated value, unit-tested with a script-tag fixture |
| T-226-11 | Information disclosure (egress) | The html export loading remote assets (CDN fonts/scripts) leaking read-signals from a shared machine | mitigate | Self-contained rule: inline CSS only; test asserts no http(s):// URL in the output (Part 8 posture extended to the artifact) |
| T-226-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero new dependencies; no install tasks |
</threat_model>

<verification>
- NODE_OPTIONS preload + node tests/test-226-mode-disclosure.cjs exits 0 (D6 across md+json+html)
- node tests/test-216-eureka-command.cjs green (existing dispatcher behavior untouched)
- node scripts/check-render-coverage.cjs exits 0 (render-surface gate unaffected)
- node scripts/build-connector-registry.cjs --check exits 0 (plan-checker Info fix: this plan edits commands/eureka.md's argument-hint frontmatter, so the connector registry gate belongs here, not deferred to plan 04's doctor --acceptance rollup alone)
</verification>

<success_criteria>
- /mos:eureka html exists, is self-contained, and its mode banner cannot be lost
- The reasoning orchestration is fully documented behind the one existing command (no separate fallback command, no second mental model)
- A second reader of the shared html cannot mistake a reasoning result for an embedded one
</success_criteria>

<output>
Create `.planning/phases/226-eureka-reasoning-mode-fallback-seed-058-give-mos-eureka-a-la/226-03-SUMMARY.md` when done.
</output>
