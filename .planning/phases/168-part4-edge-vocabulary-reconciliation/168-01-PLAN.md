---
phase: 168-part4-edge-vocabulary-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/edges.cjs
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-edges-part4-cascade-floor.cjs
  - tests/run-all-168.sh
autonomous: false
requirements: [EDGE-01, EDGE-02, EDGE-03, EDGE-04]
canon_parts: [4, 6, 8, 9]

must_haves:
  truths:
    - "writeEdge accepts CONVERGES / INVALIDATES / ENABLES and still rejects a made-up edge type"
    - "the three edges are added to ALLOWED_EDGE_TYPES via ONE additive block; every prior frozen type still present; the Set stays frozen"
    - "Canon Part 4 prose ALREADY lists the three (no prose change for them); the amendment is recorded as a code-to-canon RECONCILIATION in a new Appendix D entry + version bump"
    - "the floor test asserts the three new members + the FULL prior FLOOR + frozen Set + a made-up-type negative, never .size"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "ALLOWED_EDGE_TYPES extended with CONVERGES / INVALIDATES / ENABLES via one additive block"
      contains: "CONVERGES"
    - path: "tests/test-edges-part4-cascade-floor.cjs"
      provides: "membership + FULL FLOOR + frozen-Set + made-up-type negative (never .size)"
      min_lines: 40
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Appendix D reconciliation entry + version bump (Part 4 prose for the three unchanged)"
      contains: "CONVERGES"
    - path: "tests/run-all-168.sh"
      provides: "phase aggregator (registers the floor test + em-dash sweep)"
      min_lines: 30
  key_links:
    - from: "lib/core/navigation/edges.cjs"
      to: "tests/test-edges-part4-cascade-floor.cjs"
      via: "require ALLOWED_EDGE_TYPES + writeEdge"
      pattern: "ALLOWED_EDGE_TYPES"
    - from: "docs/MINDRIAN-CANON.md"
      to: "docs/CANON-PHASE-MAP.md"
      via: "version-history row references the same Appendix D entry"
      pattern: "168"
---

<objective>
WAVE 1 (the only wave): reconcile the Part 9 frozen edge set with Canon Part 4 by adding the three
Part-4-blessed-but-code-missing cascade edges CONVERGES / INVALIDATES / ENABLES to
`ALLOWED_EDGE_TYPES` in lib/core/navigation/edges.cjs, landed as ONE atomic lockstep wave (mirror the
Phase 150.8 / 163-01 amendment procedure). This is a navigator-GATED frozen-constitutional-set move.

Purpose: Canon Part 4 prose already declares INFORMS/CONTRADICTS/CONVERGES/INVALIDATES/ENABLES, and the
old lazygraph-ops.cjs path already writes them, but the Part 9 writeEdge chokepoint frozen set never
caught up (verified: CONVERGES/INVALIDATES/ENABLES MISSING). Phase 164's issue-tree must emit
INVALIDATES/ENABLES via the chokepoint, so this drift is a blocking prerequisite. This is a
RECONCILIATION (bring code into line with the already-blessed prose), not a net-new vocabulary
expansion.

Output: an extended ALLOWED_EDGE_TYPES Set, the canon reconciliation record + phase map, the floor
test, the phase runner. NO em-dashes (CLAUDE.md HARD RULE). BELONGS_TO is NOT added (the 164 issue-tree
remaps it to PART_OF); the lazygraph legacy-array unification is OUT of scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-part4-edge-vocabulary-reconciliation/168-SPEC.md
@.planning/phases/164-bono-research-debate-engine/164-RESEARCH.md
@lib/core/navigation/edges.cjs
@docs/MINDRIAN-CANON.md
@tests/test-edges-domain-taxonomy-floor.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ALLOWED_EDGE_TYPES with the three cascade edges + ship the floor test</name>
  <files>lib/core/navigation/edges.cjs, tests/test-edges-part4-cascade-floor.cjs</files>
  <behavior>
    - Test 1: ALLOWED_EDGE_TYPES.has('CONVERGES') === true (and INVALIDATES, ENABLES).
    - Test 2: FULL FLOOR -- every prior frozen type still present (DEFERRED, REJECTED, DERIVED_FROM, FILED_AS_DECISION, FOLLOWS_FROM, OPERATOR_TRANSITION, INFORMS, REJECTED_BECAUSE, CONTRADICTS, SUPERSEDES, AFFILIATED_WITH, PIVOTED, SELECTED_REACH, FEEDS_INTO, VALIDATES, STATES, SUPPORTS, DESCRIBES, REFINES, ROOT_CAUSES, INSTANTIATES, DECOMPOSED_INTO, PART_OF, TAGGED_WITH, RELATED_TO).
    - Test 3: ALLOWED_EDGE_TYPES is a frozen Set instance (Object.isFrozen true).
    - Test 4: writeEdge(db, {edge_type:'CONVERGES', source_id, target_id}) returns ok on an in-memory node:sqlite db with an edges table (and INVALIDATES, ENABLES); writeEdge with a made-up type returns {ok:false, reason:'invalid_edge_type'}.
    - NEVER assert .size (additive extension; mirror the edges.cjs FLOOR doctrine).
  </behavior>
  <action>Append ONE additive block to the ALLOWED_EDGE_TYPES Set in lib/core/navigation/edges.cjs (after the last current member), mirroring the Phase 150.8 / 163-01 additive idiom VERBATIM (reuse the existing comment-block template). Add three members: CONVERGES, INVALIDATES, ENABLES. The comment block MUST note these are the Part-4-blessed cascade edges being RECONCILED into the Part 9 frozen set (already declared in Part 4 prose + already written by the legacy lazygraph-ops path), mark it NAVIGATOR-GATED, and state Part 8 ENUM/scalar-only props (never prose, never cross to Brain). Then create tests/test-edges-part4-cascade-floor.cjs mirroring tests/test-edges-domain-taxonomy-floor.cjs structure (membership + FULL FLOOR + frozen-Set + made-up-type negative). NO fenced code in this action.</action>
  <verify>
    <automated>node tests/test-edges-part4-cascade-floor.cjs</automated>
  </verify>
  <done>The floor test passes: the three new types are members, every prior type preserved, the Set is frozen, a made-up edge is rejected by writeEdge.</done>
</task>

<task type="auto">
  <name>Task 2: Land the canon reconciliation record + phase-map rows + the phase runner</name>
  <files>docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md, tests/run-all-168.sh</files>
  <action>In docs/MINDRIAN-CANON.md: (a) Part 4 prose for the three is ALREADY correct -- do NOT change the cascade-edge sentence; verify it still lists CONVERGES/INVALIDATES/ENABLES. (b) Add the next Appendix D entry (number after the current highest) recording this as a CODE-TO-CANON RECONCILIATION: the Part 9 writeEdge frozen set was missing CONVERGES/INVALIDATES/ENABLES though Part 4 prose + the legacy lazygraph-ops path already carried them; this entry records the code being brought into line, cites the navigator split-out decision 2026-06-18, the Part 6 dog-fooding mechanism, and names the deferred lazygraph two-vocabulary unification + the BELONGS_TO->PART_OF remap as out of scope. (c) Bump the header Version line + the footer version line by one minor. In docs/CANON-PHASE-MAP.md: (a) update the Canon reference version. (b) Add a Phase 168 row with canon_parts + a one-line description. (c) Add a version-history row dated 2026-06-18. Finally create tests/run-all-168.sh mirroring tests/run-all-163.sh structure (a CJS_SUITES array registering test-edges-part4-cascade-floor.cjs, a per-suite PASS/FAIL loop, an em-dash sweep over the phase artifacts via the U+2014 codepoint escape, a final tally with exit 1 on any failure). NO em-dashes in any edited or created file.</action>
  <verify>
    <automated>bash tests/run-all-168.sh && grep -c "CONVERGES" docs/MINDRIAN-CANON.md | grep -qv '^0$' && echo CANON_OK</automated>
  </verify>
  <done>run-all-168.sh green (floor test + em-dash sweep), MINDRIAN-CANON.md Appendix D reconciliation entry added + version bumped, Part 4 prose unchanged, CANON-PHASE-MAP.md carries the Phase 168 + version-history rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The three-edge reconciliation: edges.cjs additive block (CONVERGES / INVALIDATES / ENABLES), the floor test, the MINDRIAN-CANON.md Appendix D reconciliation entry + version bump, the CANON-PHASE-MAP.md rows, the run-all-168.sh runner.</what-built>
  <how-to-verify>
    NAVIGATOR-GATED frozen-constitutional-set move; cannot auto-advance. Confirm:
    1. `node tests/test-edges-part4-cascade-floor.cjs` passes (three new + full FLOOR + frozen + negative).
    2. `git diff docs/MINDRIAN-CANON.md` shows ONLY the Appendix D reconciliation entry + the two version lines (Part 4 prose for the three UNCHANGED). No doctrine drift.
    3. The reconciliation framing is correct: code brought into line with already-blessed Part 4 prose; BELONGS_TO deferred-to-PART_OF; lazygraph unification out of scope.
    4. `bash tests/run-all-168.sh` is green.
  </how-to-verify>
  <resume-signal>Type "approved" to ratify the reconciliation, or describe corrections needed.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-168-01 | Tampering | ALLOWED_EDGE_TYPES Set | mitigate | floor test asserts FULL FLOOR + frozen Set + made-up-type negative; additive-only block |
| T-168-02 | Repudiation | canon reconciliation | mitigate | navigator-gated blocking checkpoint; Appendix D entry records the split-out decision provenance |
| T-168-03 | Information disclosure | new edge properties | accept | props ENUM/scalar only; never prose; never cross to Brain |
| T-168-SC | Tampering | npm/pip installs | mitigate | zero new packages (pure Node built-ins + doc edits) |
</threat_model>

<verification>
- `node tests/test-edges-part4-cascade-floor.cjs` passes.
- `bash tests/run-all-168.sh` green.
- MINDRIAN-CANON.md version bumped; Appendix D reconciliation entry names the three edges; Part 4 prose unchanged.
- CANON-PHASE-MAP.md carries the Phase 168 + version-history rows.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
CONVERGES / INVALIDATES / ENABLES are frozen members writeEdge accepts; a non-member is rejected; the
canon + phase map record the reconciliation in lockstep; the floor test + run-all-168.sh are green; the
navigator has ratified the frozen-set move at the blocking checkpoint.
</success_criteria>

<output>
Create `.planning/phases/168-part4-edge-vocabulary-reconciliation/168-01-SUMMARY.md` when done.
</output>
