---
phase: 95.1-mos-doctor-drift-detection-and-self-heal
plan: 02
type: execute
wave: 0
depends_on: [95.1-00, 95.1-01]
files_modified:
  - tests/test-doctor-class-b.cjs
  - tests/test-doctor-class-c.cjs
  - tests/test-cascade-surface-e2e.cjs
  - tests/test-doctor-class-e.cjs
  - tests/test-doctor-class-f.cjs
  - tests/test-generate-section-intelligence.cjs
  - tests/test-doctor-ui-self-compliant.cjs
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements:
  - DOCTOR-95.1-01
  - DOCTOR-95.1-02
  - DOCTOR-95.1-03
  - DOCTOR-95.1-04
  - DOCTOR-95.1-05
  - DOCTOR-95.1-06
  - DOCTOR-95.1-08
must_haves:
  truths:
    - "All 7 new test files exist as RED skeletons (they fail until Wave 1 implements the production code, but their structure / labels / sandbox helpers are complete)"
    - "Each test file follows the IIFE harness pattern from tests/test-cascade-side-channel.cjs (passed/failed counters, ok/fail helpers, process.exit at end)"
    - "Every test file carries the BSL 1.1 header"
    - "lib/memory/run-feynman-tests.cjs registers all 7 new tests in its TEST_FILES array"
    - "Each test imports MINDRIAN_ROOMS_HOME-based isolation pattern from test-cascade-side-channel.cjs (no test runs against the user's real active room)"
  artifacts:
    - path: "tests/test-doctor-class-b.cjs"
      provides: "Class B (.room-root sentinel detection) RED skeleton"
    - path: "tests/test-doctor-class-c.cjs"
      provides: "Class C (active-room guard silence) RED skeleton"
    - path: "tests/test-cascade-surface-e2e.cjs"
      provides: "Class D (live cascade end-to-end + 8-key shape assertion) RED skeleton"
    - path: "tests/test-doctor-class-e.cjs"
      provides: "Class E (ROOM.md/MINTO.md cascade) RED skeleton"
    - path: "tests/test-doctor-class-f.cjs"
      provides: "Class F (UI compliance scanner) RED skeleton"
    - path: "tests/test-generate-section-intelligence.cjs"
      provides: "Generator behavior (single-dir / --recursive / skip-if-exists / --force) RED skeleton"
    - path: "tests/test-doctor-ui-self-compliant.cjs"
      provides: "Doctor self-retrofit verification (Shape E body / glyph cleanup / 4-zone anatomy) RED skeleton"
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "Test runner registration of all 7 new tests"
  key_links:
    - from: "tests/test-cascade-surface-e2e.cjs"
      to: "test/fixtures/cascade-surface-e2e/ (from Plan 95.1-01)"
      via: "spawnSync('bash', [POST_WRITE], { env: { MINDRIAN_ROOMS_HOME: <fixturePath> }})"
      pattern: "MINDRIAN_ROOMS_HOME"
    - from: "lib/memory/run-feynman-tests.cjs TEST_FILES array"
      to: "all 7 new test files"
      via: "path.join(REPO_ROOT, 'tests', 'test-X.cjs')"
      pattern: "tests/test-(doctor|cascade-surface|generate)"
---

<objective>
Create 7 RED test skeletons + register them in the test runner. RED skeletons mean: the test file exists, has the correct structure, has all sandbox helpers wired, has the assertions written — but the asserts FAIL because the production code (Wave 1) hasn't been written yet. This is the validation-architecture-first wave per 95.1-VALIDATION.md.

Purpose: Per Nyquist-sampling rule, no implementation lands without a test that fails. By writing the test FIRST in Wave 0, Wave 1 implementations land as RED -> GREEN transitions, not as blind shipping. Phase 87 invariant + 95-02 + 95-03 patterns enforce this.
Output: 7 new .cjs test files + updated run-feynman-tests.cjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-CONTEXT.md
@.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-RESEARCH.md
@.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-VALIDATION.md
@tests/test-cascade-side-channel.cjs
@tests/test-room-proactive-side-channel.cjs
@lib/memory/run-feynman-tests.cjs

<interfaces>
<!-- IIFE harness contract from tests/test-cascade-side-channel.cjs -->

```javascript
// File header (every test file MUST start with):
#!/usr/bin/env node
'use strict';

/*
 * Copyright (c) 2026 Mindrian. BSL 1.1.
 * Phase 95.1 -- <test description>.
 * <REQ-IDs covered>
 */

const assert = require('node:assert/strict');
const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');
const { spawnSync } = require('node:child_process');

const REPO = path.resolve(__dirname, '..');

let passed = 0;
let failed = 0;

function ok(name) {
  passed += 1;
  process.stdout.write('  ok ' + name + '\n');
}

function fail(name, err) {
  failed += 1;
  process.stdout.write('  FAIL ' + name + '\n');
  if (err) process.stdout.write('    ' + (err.message || String(err)) + '\n');
}

// Sandbox helpers (cloned from test-cascade-side-channel.cjs as needed)
function makeScratchDir(suffix) { /* ... */ }
function rmrf(p) { /* ... */ }

// IIFE test scenarios:
(function test1_<name>() {
  const label = '<test-name>';
  const scratch = makeScratchDir('<suffix>');
  try {
    // setup
    // assertions
    ok(label);
  } catch (e) { fail(label, e); }
  finally { rmrf(scratch); }
})();

// Footer (every test file MUST end with):
process.stdout.write('\n');
process.stdout.write('<suite name>: ' + passed + ' passed, ' + failed + ' failed\n');
process.exit(failed === 0 ? 0 : 1);
```

From lib/memory/run-feynman-tests.cjs (line 286, 299 — existing registration pattern):
```javascript
const TEST_FILES = [
  // ...
  path.join(REPO_ROOT, 'tests', 'test-cascade-side-channel.cjs'),
  // ...
  path.join(REPO_ROOT, 'tests', 'test-room-proactive-side-channel.cjs'),
  // ...
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 7 RED test skeletons + register in run-feynman-tests.cjs</name>
  <read_first>
    - tests/test-cascade-side-channel.cjs (FULL FILE — exact pattern source: file header / IIFE structure / makeScratchDir / makeRoomRoot / runBashHook / assertEnvelopeShape / process.exit footer)
    - tests/test-room-proactive-side-channel.cjs (sibling pattern for SKILL.md contract tests; per 95.1-CONTEXT line 119 this is the 6-test pattern reference)
    - lib/memory/run-feynman-tests.cjs (lines 280-310 for the TEST_FILES array; need to add 7 new entries)
    - .planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-VALIDATION.md (Wave 0 Requirements section — confirms exact 7 file names)
    - .planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-RESEARCH.md (Code Examples section — `detect_room_section` ported to CJS, the spawnSync pattern, the 8-key assertion)
  </read_first>
  <behavior>
    Per RED -> GREEN -> REFACTOR cycle, the tests below MUST fail today (Wave 1 hasn't written the production code). Each test asserts the EXACT shape Wave 1 will produce, so when Wave 1 lands, these tests flip from RED to GREEN with no further test edits.

    **Test 1 (test-doctor-class-b.cjs) — DOCTOR-95.1-01 partial (B half):**
    - Scenario 1: registry has 2 rooms, only 1 has `.room-root` -> checkCascadeRooms returns `{status: 'warn', missingSentinels: ['<roomName>']}` for the unsentineled room
    - Scenario 2: registry has 1 room, has `.room-root` -> returns `{status: 'ok', missingSentinels: []}`
    - Scenario 3: registry empty / missing -> returns `{status: 'skip', detail: 'no registry'}`
    - Spawns `node scripts/doctor.cjs --cascade-rooms --json` with `MINDRIAN_ROOMS_HOME=<scratch>`; parses stdout as JSON; asserts the `checks['cascade-rooms']` field shape

    **Test 2 (test-doctor-class-c.cjs) — DOCTOR-95.1-01 partial (C half):**
    - Scenario 1: registry's active room is roomA, simulate a write to roomB (different room) -> doctor reports `{status: 'warn', activeMismatch: true, activeRoom: 'roomA', writeRoom: 'roomB'}` for the silenced write path
    - Scenario 2: registry's active room is roomA, simulate write inside roomA -> `{status: 'ok'}`
    - Scenario 3: no registry -> `{status: 'skip'}`

    **Test 3 (test-cascade-surface-e2e.cjs) — DOCTOR-95.1-02 + DOCTOR-95.1-08:**
    - This is the headline new test. Per D-04, D-05, D-06.
    - Setup: copy `test/fixtures/cascade-surface-e2e/` to a tmpdir (so the test doesn't dirty the committed fixture); set `MINDRIAN_ROOMS_HOME=<tmpdir>`; write to `<tmpdir>/surface-e2e-room/problem-definition/seed-artifact/seed-artifact.md` via spawnSync('bash', [POST_WRITE], { input: JSON.stringify(envelope), env: ... })
    - Assert exit 0
    - Assert `<tmpdir>/surface-e2e-room/.mindrian/last-cascade.json` exists
    - Assert it parses as JSON
    - Assert it contains all 8 keys: `cascade_status`, `classification`, `file_path`, `git_commit`, `graph_index`, `proactive_intelligence`, `section`, `timestamp`
    - Defensive assertion: at the time of spawn, fail if `process.env.MINDRIAN_ROOMS_HOME` is not set to the tmpdir (prevents Pitfall 2 from 95.1-RESEARCH.md)

    **Test 4 (test-doctor-class-e.cjs) — DOCTOR-95.1-03:**
    - Scenario 1: scratch room with `.room-root` + STATE.md + 3 sections, none have ROOM.md/MINTO.md -> `checkRoomMd` returns `{status: 'warn', missing: [{section: 'X', files: ['ROOM.md','MINTO.md']}, ...]}` with 3 entries
    - Scenario 2: same setup but all 3 sections have both files -> `{status: 'ok', missing: []}`
    - Scenario 3: --fix dispatches to `scripts/generate-section-intelligence.cjs --recursive` and re-runs the check, expecting `missing: []` after generation

    **Test 5 (test-doctor-class-f.cjs) — DOCTOR-95.1-04:**
    - Scenario 1: Build a fake commands/ dir under scratch with one .md file containing `body_shape: E (Action Report)` and one without; run `checkUIRulingCompliance` against that scratch — assert it flags only the missing-body_shape file
    - Scenario 2: Build a fake scripts/ dir with a .cjs file containing `╭─` -> assert it's flagged as box-char violation
    - Scenario 3: Build a fake scripts/ dir with `✗` -> assert it's flagged as glyph-vocabulary violation
    - Scenario 4: Approved glyphs only (`■ ✓ ⚠ ⬜ ⚡ ▶ ▷ →`) -> no flags
    - Note: Class F detector has `--fix` deferred; tests assert detection only (D-13)

    **Test 6 (test-generate-section-intelligence.cjs) — DOCTOR-95.1-05:**
    - Scenario 1: scratch dir, no ROOM.md/MINTO.md present -> `node scripts/generate-section-intelligence.cjs <dir>` exits 0 and creates BOTH files; both have `BSL-1.1` license frontmatter
    - Scenario 2: ROOM.md already exists with custom user content -> default invocation skips (file unchanged); `--force` overwrites
    - Scenario 3: `--recursive` against scratch with 3 nested dirs -> creates 6 files (3 ROOM.md + 3 MINTO.md), excluding `.room-root` sentinel dir if present at root
    - Scenario 4: invocation against non-existent dir -> exits non-zero with clear error
    - Scenario 5: atomic write — interrupt simulation (skip; just assert files have full content via fs.readFileSync, no partial writes)

    **Test 7 (test-doctor-ui-self-compliant.cjs) — DOCTOR-95.1-06:**
    - Scenario 1: `commands/doctor.md` frontmatter contains `body_shape: E (Action Report)` (exact string match)
    - Scenario 2: `scripts/doctor.cjs` source code contains zero matches for the regex `[╭╮╰╯┌┐└┘│─━]` and zero matches for `✗`
    - Scenario 3: `scripts/doctor.cjs` `renderHumanReport` source contains the literal string `'-- MindrianOS -- doctor'` (Zone 1 header)
    - Scenario 4: `scripts/doctor.cjs` `renderHumanReport` source contains `▶ /mos:` (Zone 4 action footer primary action glyph)
    - Note: this test is a SOURCE-CODE scan, not a runtime test; uses fs.readFileSync + regex
  </behavior>
  <files>
    tests/test-doctor-class-b.cjs,
    tests/test-doctor-class-c.cjs,
    tests/test-cascade-surface-e2e.cjs,
    tests/test-doctor-class-e.cjs,
    tests/test-doctor-class-f.cjs,
    tests/test-generate-section-intelligence.cjs,
    tests/test-doctor-ui-self-compliant.cjs,
    lib/memory/run-feynman-tests.cjs
  </files>
  <action>
**For each of 7 test files**, write a CJS file following the IIFE harness pattern shown in `<interfaces>`. EVERY file MUST include:
1. `#!/usr/bin/env node` shebang
2. `'use strict';`
3. BSL 1.1 copyright header with phase reference
4. The exact REQ-IDs covered (commented in header)
5. Imports from `node:assert/strict`, `node:fs`, `node:os`, `node:path`, `node:child_process`
6. `passed`/`failed` counters
7. `ok()` / `fail()` helpers (verbatim copy from test-cascade-side-channel.cjs lines 83-94)
8. Sandbox helpers as needed (`makeScratchDir`, `rmrf`, `makeRoomRoot` for tests 3-4)
9. IIFE-style scenario blocks (one per `behavior` item)
10. Footer: `process.stdout.write('\n'); process.stdout.write('<suite>: ' + passed + ' passed, ' + failed + ' failed\n'); process.exit(failed === 0 ? 0 : 1);`

**For tests 3-4 specifically (those that exercise the bash hook):**
Include the `runBashHook` helper verbatim from test-cascade-side-channel.cjs lines 175-189. Include this defensive assertion (per Pitfall 2 from 95.1-RESEARCH.md) before each spawnSync call:
```javascript
assert.equal(typeof opts.env.MINDRIAN_ROOMS_HOME, 'string',
  'Test must set MINDRIAN_ROOMS_HOME to scratch dir to avoid leaking into real active room');
```

**For test 3 (test-cascade-surface-e2e.cjs) specifically:**
Use `fs.cpSync(repoFixturePath, scratchPath, { recursive: true })` to copy the committed fixture into the scratch dir before running. This isolates the test from the committed fixture state.

**For tests that assert behavior of unimplemented production code:**
The assertions WILL fail today. That's RED state. Wave 1 plans (03, 04, 05, 06) flip them to GREEN. Document this in a one-line comment at the top of each scenario:
```javascript
// RED: asserts behavior of <function> which Wave 1 Plan 95.1-XX will implement
```

**For lib/memory/run-feynman-tests.cjs:**
Use Edit tool. Find the existing `TEST_FILES` array (the line `path.join(REPO_ROOT, 'tests', 'test-cascade-side-channel.cjs')` is at line 286). Add the 7 new entries IMMEDIATELY after the test-room-proactive-side-channel.cjs entry (line 299):
```javascript
  path.join(REPO_ROOT, 'tests', 'test-doctor-class-b.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-doctor-class-c.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-cascade-surface-e2e.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-doctor-class-e.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-doctor-class-f.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-generate-section-intelligence.cjs'),
  path.join(REPO_ROOT, 'tests', 'test-doctor-ui-self-compliant.cjs'),
```

**Acceptance behavior at end of Wave 0:**
- All 7 test files exist
- Running `node tests/test-doctor-class-b.cjs` exits non-zero (RED — production code not yet written)
- Running `node tests/test-doctor-ui-self-compliant.cjs` MAY exit zero or non-zero — for some scenarios (file existence) the assertion holds even pre-implementation; the renderer-string-match scenarios will fail
- Running `node lib/memory/run-feynman-tests.cjs` discovers all 7 new tests
- The existing 27 envelope tests (test-cascade-side-channel + test-room-proactive-side-channel + test-hook-envelope-shape) STILL pass (no regression)
  </behavior>
  <verify>
    <automated>test -f tests/test-doctor-class-b.cjs && test -f tests/test-doctor-class-c.cjs && test -f tests/test-cascade-surface-e2e.cjs && test -f tests/test-doctor-class-e.cjs && test -f tests/test-doctor-class-f.cjs && test -f tests/test-generate-section-intelligence.cjs && test -f tests/test-doctor-ui-self-compliant.cjs && grep -c "tests/test-doctor-class-\|tests/test-cascade-surface-e2e\|tests/test-generate-section-intelligence\|tests/test-doctor-ui-self-compliant" lib/memory/run-feynman-tests.cjs</automated>
  </verify>
  <done>
    - All 7 test files exist under `tests/`
    - All 7 files contain `Copyright (c) 2026 Mindrian. BSL 1.1.`
    - All 7 files contain `process.exit(failed === 0 ? 0 : 1);` footer
    - All 7 files contain at least one `(function test` IIFE block (RED scenarios written)
    - lib/memory/run-feynman-tests.cjs grep returns 7 (one match per new test path)
    - Tests 3 and 4 contain the defensive `MINDRIAN_ROOMS_HOME` typecheck assertion before spawnSync
    - Existing 3 envelope tests (test-cascade-side-channel.cjs, test-room-proactive-side-channel.cjs, test-hook-envelope-shape.cjs) still pass: `node tests/test-cascade-side-channel.cjs` exits 0
  </done>
</task>

</tasks>

<verification>
- 7 new files exist + run-feynman-tests.cjs has 7 new entries
- `node tests/test-cascade-side-channel.cjs` exits 0 (regression: existing tests still pass)
- New tests are RED (expected); they flip GREEN as Wave 1 lands
</verification>

<success_criteria>
- 7 RED test skeletons in place, structurally complete, ready for Wave 1 implementation
- Test runner discovers all 7
- No regressions to existing 27 envelope-shape passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-02-SUMMARY.md`
</output>
