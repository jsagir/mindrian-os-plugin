---
phase: 95.1
slug: mos-doctor-drift-detection-and-self-heal
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-30
---

# Phase 95.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

This validation strategy is derived from the Validation Architecture section in `95.1-RESEARCH.md`. The planner will fill the Per-Task Verification Map below as plans are written. The checker enforces nyquist compliance before approving plans.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js IIFE harness (existing pattern from `tests/test-cascade-side-channel.cjs`) — NOT node:test |
| **Config file** | None — tests are self-contained `.cjs` files run directly via `node tests/test-X.cjs` |
| **Quick run command** | `node tests/test-cascade-surface-e2e.cjs` (per-test) |
| **Full suite command** | `cd /home/jsagi/MindrianOS-Plugin && for t in tests/test-*.cjs; do node "$t" || break; done` |
| **Estimated runtime** | ~5-15s for full suite (class D test takes longest at ~2-3s due to live hook fork) |
| **Isolation** | `MINDRIAN_ROOMS_HOME` env override per D-05 (NOT `MINDRIAN_ROOMS_ROOT` per researcher's correction) |
| **Registry** | `lib/memory/run-feynman-tests.cjs` (per researcher's note — new tests must register here) |

---

## Sampling Rate

- **After every task commit:** Run the test for that task (e.g., after class B detector lands, run `node tests/test-doctor-class-b.cjs`)
- **After every plan wave:** Run the full suite via `for t in tests/test-*.cjs; do node "$t" || break; done`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds (full suite)

---

## Per-Task Verification Map

> The planner fills this table as plans are written. Each task's `<automated>` field maps to a row here. The checker verifies no 3 consecutive tasks lack automated verification (Nyquist sampling rule).

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| _to be filled by planner_ | | | | | | | |

*Status: ⬜ pending · ✓ green · ⚠ red · ⚡ flaky*

---

## Wave 0 Requirements

Per researcher's Validation Architecture analysis, the following test stubs MUST exist before any implementation begins. Wave 0 is the test-infrastructure-first wave.

- [ ] `tests/test-doctor-class-b.cjs` — class B (.room-root sentinel) detector test
- [ ] `tests/test-doctor-class-c.cjs` — class C (active-room guard) detector test
- [ ] `tests/test-cascade-surface-e2e.cjs` — class D live-cascade end-to-end test (the headline new test per D-04..D-06)
- [ ] `tests/test-doctor-class-e.cjs` — class E (ROOM.md/MINTO.md cascade) detector test
- [ ] `tests/test-doctor-class-f.cjs` — class F (UI compliance) detector test
- [ ] `tests/test-generate-section-intelligence.cjs` — generator behavior (single-dir, --recursive, skip-if-exists, --force) per D-01..D-03
- [ ] `tests/test-doctor-ui-compliance.cjs` — /mos:doctor self-retrofit verification (Shape E body, glyph cleanup, 4-zone anatomy, F.1 selector) per D-10..D-19
- [ ] Registration in `lib/memory/run-feynman-tests.cjs` for all 7 new tests

*Existing test infrastructure (`tests/test-cascade-side-channel.cjs`, `tests/test-room-proactive-side-channel.cjs`, `tests/test-hook-envelope-shape.cjs`) covers Phase 95's surface but does NOT cover 95.1's new drift checks. All 7 tests above are net-new for 95.1.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| /mos:doctor visual output (Shape E rendering, 4-zone anatomy at terminal) | D-10..D-12 | Visual conformance to SKILL.md is hard to assert programmatically beyond glyph allowlist + zone presence | After class F automated test passes, run `node scripts/doctor.cjs --cascade-rooms --verify-surface --room-md --ui-compliance` against the dogfood room and visually verify: (1) Zone 1 header `-- MindrianOS -- doctor -- {stage} --`, (2) Zone 2 per-class status rows, (3) Zone 3 only if room-proactive signals exist, (4) Zone 4 always present with 2-3 grounded /mos: commands |
| F.1 Next Move selector interactive flow | D-18 | AskUserQuestion is a TUI interaction not amenable to scripted test | After class F automated test passes, run `node scripts/doctor.cjs` (no --fix) against a drift-positive room. Verify F.1 selector appears with options: "Run /mos:doctor --fix" / "Defer" / "Free-Text". Pick "Defer" and verify the choice writes to STATE.md Decisions section per Canon Part 3 Layer 3 state hook |
| Cross-surface degradation (Larry conversational rendering on Desktop) | D-15, D-19 | Desktop-side rendering is Larry's conversational output, not the doctor renderer | Once /mos:doctor is shipped, in a Desktop session ask Larry "run /mos:doctor on this room" and verify: (1) no box chars in Larry's response, (2) glyphs degrade to bullets/bold per SKILL.md §9, (3) no ANSI color codes leak |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all 7 MISSING test files listed above
- [ ] No watch-mode flags (Phase 87 invariant: zero new runtime dependencies, no watchexec/nodemon)
- [ ] Feedback latency < 15s for full suite
- [ ] `nyquist_compliant: true` set in frontmatter (planner sets after writing all plans)

**Approval:** pending — to be set by gsd-plan-checker on PASS verdict
