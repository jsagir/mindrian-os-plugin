---
phase: 95.1-mos-doctor-drift-detection-and-self-heal
plan: 08
type: execute
wave: 2
depends_on: [95.1-07]
files_modified:
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - package.json
autonomous: false
requirements:
  - DOCTOR-95.1-01
  - DOCTOR-95.1-02
  - DOCTOR-95.1-03
  - DOCTOR-95.1-04
  - DOCTOR-95.1-05
  - DOCTOR-95.1-06
  - DOCTOR-95.1-07
  - DOCTOR-95.1-08
must_haves:
  truths:
    - "CHANGELOG.md has [1.12.1-beta.1] entry at top with Added / Fixed / Changed sections enumerating all class B/C/D/E/F closures"
    - "plugin.json version field == 1.12.1-beta.1"
    - "package.json version field == 1.12.1-beta.1"
    - "git tag v1.12.1-beta.1 exists pointing at the release commit"
    - "marketplace.json source.ref pinned to v1.12.1-beta.1 (in ~/mindrian-marketplace; this lands as a separate user action)"
    - "Anthropic upstream bug report draft filed (per Phase 93 D5 deferred item — held until /mos:doctor exists with classes A-F)"
    - "Beta-gate per release-process.md mandate: release infrastructure (this includes /mos:doctor extension) ALWAYS ships as beta first"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Versioned release notes for v1.12.1-beta.1 with 8 closed REQ-IDs"
      contains: "[1.12.1-beta.1]"
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin version manifest"
      contains: "1.12.1-beta.1"
    - path: "package.json"
      provides: "npm version manifest"
      contains: "1.12.1-beta.1"
  key_links:
    - from: ".claude-plugin/plugin.json version"
      to: "package.json version"
      via: "string equality"
      pattern: "\"version\": \"1.12.1-beta.1\""
    - from: "git tag v1.12.1-beta.1"
      to: "the release commit on main"
      via: "git tag/git push --tags"
      pattern: "v1\\.12\\.1-beta\\.1"
---

<objective>
Final release plan. Wraps Wave 0/1/2 deliverables into a single beta-gated release per CLAUDE.md release-process.md mandate ("release infrastructure ALWAYS ships as a beta first"). The /mos:doctor extension touches the live install path (it offers --fix recovery actions); per the mandate, qualifies as release infrastructure.

Per D-07: ships as v1.12.1 patch. Per release-process.md beta-gate: ships as v1.12.1-beta.1 first (this plan); promoted to v1.12.1 after Lawrence (or another external user) confirms it works. Promotion to stable v1.12.1 is a SEPARATE follow-up (not part of this phase).

Five release gates per release-process.md Version Consistency Rule:
1. CHANGELOG.md — entry at top
2. .claude-plugin/plugin.json — version field
3. package.json — version field
4. git tag v1.12.1-beta.1 — annotated tag
5. ~/mindrian-marketplace/.claude-plugin/marketplace.json — ref pinned to v1.12.1-beta.1 (separate repo; user-side action since the marketplace lives outside this workspace)

This plan handles gates 1-4. Gate 5 is documented as a follow-up to be done by the user after the commit lands.

Plan also includes the deferred Anthropic upstream bug report (Phase 93 D5; held until /mos:doctor existed at full strength — now does).

Per Workspace Guard: ALL git operations happen in `/home/jsagi/MindrianOS-Plugin/`. Verify cwd before every git command.

Purpose: Ships Phase 95.1 as v1.12.1-beta.1 with full beta-gate semantics. Closes the 8 DOCTOR-95.1-* requirements. Delivers the deferred Anthropic upstream bug report draft.
Output: 3 file edits (CHANGELOG, plugin.json, package.json) + git tag + Anthropic bug report draft + commit + push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-CONTEXT.md
@.claude/includes/release-process.md
@CHANGELOG.md
@.claude-plugin/plugin.json
@package.json

<interfaces>
<!-- Release-process.md mandate (excerpt — already read in <execution_context>) -->

> Release infrastructure ALWAYS ships as a beta first. /mos:doctor, release.sh, pre-push hooks, session-start guards, migration scripts — all of these go out as X.Y.Z-beta.N and get promoted after at least one external user (currently Lawrence) confirms they work. Bugs in release infrastructure are the hardest to recover from — a broken release script can prevent you from shipping its own fix. Beta gating is the only safe path.

<!-- Five release gates -->

| Gate | File | Required state for v1.12.1-beta.1 |
|------|------|------------------------------------|
| 1 | CHANGELOG.md | `## [1.12.1-beta.1] - 2026-04-30` entry at top with Added / Fixed / Changed sections |
| 2 | .claude-plugin/plugin.json | `"version": "1.12.1-beta.1"` |
| 3 | package.json | `"version": "1.12.1-beta.1"` |
| 4 | git tag | `git tag -a v1.12.1-beta.1 -m '...'` pointing at the release commit |
| 5 | ~/mindrian-marketplace/.claude-plugin/marketplace.json | `"source": { ..., "ref": "v1.12.1-beta.1" }` — DEFERRED to user (separate repo) |

<!-- Beta installation path (for the user / Lawrence) -->
```bash
claude plugin update mos@mindrian-marketplace --version 1.12.1-beta.1
```

<!-- CHANGELOG.md current state -->
Existing entries follow this format (verified via Read). Insert new entry at the very top after `# Changelog` heading, before the [1.12.0] entry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CHANGELOG.md, plugin.json, package.json — three release gates</name>
  <read_first>
    - CHANGELOG.md (FULL FILE — need to see top entry format and where to insert v1.12.1-beta.1)
    - .claude-plugin/plugin.json (current version field)
    - package.json (current version field)
    - .claude/includes/release-process.md (5-gate enforcement; beta-gate mandate)
    - .planning/REQUIREMENTS.md (the 8 DOCTOR-95.1-* IDs to enumerate in CHANGELOG)
    - .planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-CONTEXT.md (D-07 milestone target; six drift classes A-F)
  </read_first>
  <files>CHANGELOG.md, .claude-plugin/plugin.json, package.json</files>
  <action>
**Three surgical edits using Edit tool. Workspace cwd must be `/home/jsagi/MindrianOS-Plugin/`.**

**Edit 1: CHANGELOG.md** — Insert NEW entry at the very top, AFTER the `# Changelog` heading and BEFORE the existing `## [1.12.0]` entry.

Insert this block verbatim:

```markdown
## [1.12.1-beta.1] - 2026-04-30

### Added
- `/mos:doctor --cascade-rooms` flag detects rooms missing `.room-root` sentinel (drift class B) and active-room guard silence (drift class C) per DOCTOR-95.1-01.
- `/mos:doctor --verify-surface` flag executes a live cascade end-to-end against `test/fixtures/cascade-surface-e2e/`, asserts the 8-key side-channel shape per DOCTOR-95.1-02 + DOCTOR-95.1-08.
- `/mos:doctor --room-md` flag detects directories under `.room-root` subtrees missing ROOM.md or MINTO.md (drift class E); `--fix --room-md` invokes the new generator per DOCTOR-95.1-03.
- `/mos:doctor --ui-compliance` flag detects UI Ruling System violations across `commands/*.md` and `scripts/*.cjs` (drift class F): missing `body_shape` frontmatter, unauthorized box chars, unauthorized glyphs, unauthorized ANSI colors, missing Zone 1/4 patterns per DOCTOR-95.1-04.
- `/mos:doctor --all` flag activates all class detectors A-F in one invocation.
- `scripts/generate-section-intelligence.cjs` — the missing generator that Phase 87-01a's pre-commit hook has been pointing at since 2026-04-19. Single-dir + `--recursive` + `--force` per D-01..D-03. Hand-rolled minimal frontmatter. Atomic writes via mktemp + rename. Closes DOCTOR-95.1-05.
- `test/fixtures/cascade-surface-e2e/` — sibling of `cascade-e2e/seed-room/` for surface-layer (envelope -> render) verification per DOCTOR-95.1-08.
- 7 new test files: `tests/test-doctor-class-b.cjs`, `test-doctor-class-c.cjs`, `test-cascade-surface-e2e.cjs`, `test-doctor-class-e.cjs`, `test-doctor-class-f.cjs`, `test-generate-section-intelligence.cjs`, `test-doctor-ui-self-compliant.cjs`. Registered in `lib/memory/run-feynman-tests.cjs`.
- F.1 Next Move structural marker block in `/mos:doctor` output when drift detected without `--fix`. Canonical AskUserQuestion-based F.1 deferred to Phase 88.2 per OQ2 (see `f1-selector-deferred.md`).
- Dogfood `room/.room-root` sentinel + ROOM.md + MINTO.md across all 9 subdirectories. Closes Decision #15 violation in the plugin's own dogfood room.

### Fixed
- `/mos:doctor` itself was non-compliant with `skills/ui-system/SKILL.md` (mandatory since Phase 80) — used `╭─ ╮ ╰─ ╯` box chars and `✗` glyph (not in the 12-glyph vocabulary), missing 4-zone anatomy, missing `body_shape` frontmatter, missing Zone 4 action footer. Retrofitted to Shape E (Action Report) compliance: `-- MindrianOS -- doctor -- {stage} --` Zone 1 header, per-class status rows in Zone 2, Zone 4 footer with `▶ /mos:` primary action, density-aware compact header when output > 30 lines per D-10..D-12, D-16. Closes DOCTOR-95.1-06.
- `commands/doctor.md` frontmatter missing `body_shape`. Added `body_shape: E (Action Report)` per D-10 (canonical form across 33/80 shipped commands).
- Smoke session debris cleaned: removed duplicate nested `room/decisions/v1-12-0-smoke-fresh-session-2/v1-12-0-smoke-fresh-session-2/` per <specifics> in CONTEXT.md.

### Changed
- `scripts/doctor.cjs` extended from 335 to ~750 lines. Architecture preserved: parseArgs / checkX() / performRecovery / renderHumanReport. Five new check functions registered behind flag selectors per D-09. Each check is graceful-degradation-wrapped (try/catch around each invocation; one failure does not abort the run).
- `commands/doctor.md` examples updated to match new Shape E renderer output.
- `commands/doctor.md` argument-hint extended to enumerate the new flags.

### Beta gate
Per `release-process.md` mandate ("release infrastructure ALWAYS ships as a beta first"), this release ships as `v1.12.1-beta.1`. Promotion to stable `v1.12.1` requires confirmation from at least one external user (Lawrence). Beta install path:

```bash
claude plugin update mos@mindrian-marketplace --version 1.12.1-beta.1
```

### Five release gates status
- [x] CHANGELOG.md updated (this entry)
- [x] .claude-plugin/plugin.json bumped to 1.12.1-beta.1
- [x] package.json bumped to 1.12.1-beta.1
- [x] git tag v1.12.1-beta.1
- [ ] ~/mindrian-marketplace/.claude-plugin/marketplace.json source.ref pinned to v1.12.1-beta.1 (separate repo; user-side step after this commit lands)

```

**Edit 2: .claude-plugin/plugin.json** — Update the `version` field. Current value is `"1.12.0"`; new value `"1.12.1-beta.1"`. Use Edit tool — surgical replacement of the version line only. DO NOT reformat or touch any other field.

**Edit 3: package.json** — Update the `version` field same way. Current value is `"1.12.0"`; new value `"1.12.1-beta.1"`. Use Edit tool — surgical replacement. DO NOT touch any other field, dependency, or script entry.

**Verification before proceeding to Task 2:**
```bash
grep "\"version\":" /home/jsagi/MindrianOS-Plugin/.claude-plugin/plugin.json
grep "\"version\":" /home/jsagi/MindrianOS-Plugin/package.json
head -10 /home/jsagi/MindrianOS-Plugin/CHANGELOG.md | grep -A 1 "1.12.1-beta.1"
```

All three should show `1.12.1-beta.1`.
  </action>
  <verify>
    <automated>grep '"version": "1.12.1-beta.1"' /home/jsagi/MindrianOS-Plugin/.claude-plugin/plugin.json && grep '"version": "1.12.1-beta.1"' /home/jsagi/MindrianOS-Plugin/package.json && grep "## \[1.12.1-beta.1\]" /home/jsagi/MindrianOS-Plugin/CHANGELOG.md</automated>
  </verify>
  <done>
    - CHANGELOG.md has `## [1.12.1-beta.1] - 2026-04-30` entry at top
    - CHANGELOG.md entry contains Added, Fixed, Changed sections AND enumerates all 8 DOCTOR-95.1-* REQ-IDs
    - .claude-plugin/plugin.json `"version"` field == `"1.12.1-beta.1"`
    - package.json `"version"` field == `"1.12.1-beta.1"`
    - All three files parse correctly (json files valid, markdown loads)
    - No other fields modified in plugin.json or package.json (verify diff is single-line per file)
  </done>
</task>

<task type="auto">
  <name>Task 2: Draft Anthropic upstream bug report (Phase 93 D5 deferred item)</name>
  <read_first>
    - .planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-CONTEXT.md (Deferred section: "Anthropic upstream bug report (Phase 93 D5): held until /mos:doctor exists. /mos:doctor exists, but 95.1 is the right time to draft once class A is no longer the only check.")
    - docs/autopsies/2026-04-13-wrong-workspace-incident.md (incident #1 driving the bug report)
    - docs/autopsies/2026-04-28-install-cache-drift-incident.md (incident #2)
  </read_first>
  <files>docs/anthropic-upstream-install-cache-drift.md</files>
  <action>
Create a draft bug report for filing with Anthropic. Content describes the install-cache drift class A behavior at the Claude Code plugin manager level. The report does NOT propose code changes to Claude Code itself; it describes the symptom and the workaround (/mos:doctor) that MindrianOS implements locally.

Write content (verbatim):

```markdown
# Claude Code Plugin Manager — Install Cache Drift (Bug Report Draft)

**Status:** Draft for Anthropic submission
**Filed:** 2026-04-30 (held since Phase 93; released after /mos:doctor classes A-F shipped in 95.1)
**Affected versions:** Claude Code 1.x — 2.x (multiple users observed)
**Reporter:** Jonathan Sagir (MindrianOS Plugin maintainer)

## Symptom

Two confirmed incidents (2026-04-13 + 2026-04-28) where the live plugin install at `~/.claude/plugins/<plugin>/` was a stale older version while the marketplace cache at `~/.claude/plugins/cache/<marketplace>/<plugin>/` had a newer version cached and ready. The plugin manager reported "already at latest" while `plugin.json` in the install dir said an older version.

In incident 1: `~/.claude/plugins/mindrian-os/.claude-plugin/plugin.json` reported `1.10.10`. The marketplace cache had `1.10.12, 1.10.17, 1.11.0` cached. Claude Code reported "already up to date" and `plugin marketplace update` did not refresh the install dir.

In incident 2: same pattern at v1.11.x.

The state was indistinguishable from a working install until commands silently failed (referenced files that existed in the source repo but not in the install).

## Reproduction

Difficult to reproduce intentionally; appears to be a state where the marketplace cache fetch succeeds but the install-dir replacement step is skipped or interrupted. Both incidents were observed only on Linux WSL2 hosts; macOS / native Linux not yet tested.

## Workaround (MindrianOS-side, not asking for upstream change)

MindrianOS Plugin v1.11.1+ ships `/mos:doctor` (Phase 93, extended in Phase 95.1):

- Reads `~/.claude/plugins/mindrian-os/.claude-plugin/plugin.json` for the install version
- Enumerates `~/.claude/plugins/cache/mindrian-marketplace/mos/*` directory names as semver candidates
- Compares; reports drift
- `/mos:doctor --fix` backs up the stale install (rename to `.stale-<old>-<timestamp>`) and copies the latest marketplace cache to the install location

## What we'd find useful from Anthropic

1. **Diagnostic visibility:** When `claude plugin update <plugin>@<marketplace>` runs, expose the actual install version vs marketplace version comparison, even when the manager believes there's no work to do.
2. **Forced-refresh flag:** A `--force` or `--reinstall` flag on `claude plugin update` that bypasses the "already up to date" early return and re-executes the cache-to-install copy step.
3. **Auto-detection at session start:** Optional. The session-start could observe install-cache drift and surface a one-line advisory.

## What we are NOT asking for

- We don't need a fix for this specific transition path; our local /mos:doctor handles it.
- We don't want this filed as a security issue; it's a UX observability issue.
- We don't want behavior changes to plugin auto-update; the existing "stable by default, opt-in to latest" model is correct.

## Pointers

- `MindrianOS-Plugin/scripts/doctor.cjs` (the workaround implementation)
- `MindrianOS-Plugin/docs/autopsies/2026-04-13-wrong-workspace-incident.md` (incident 1)
- `MindrianOS-Plugin/docs/autopsies/2026-04-28-install-cache-drift-incident.md` (incident 2)

## Submission target

Anthropic feedback channel for Claude Code (TBD — confirm correct channel before sending).
```

This file is a DRAFT. The user can review, refine, and send it through the appropriate Anthropic channel. Closes the Phase 93 D5 deferred item.
  </action>
  <verify>
    <automated>test -f /home/jsagi/MindrianOS-Plugin/docs/anthropic-upstream-install-cache-drift.md && grep "1.10.10\|1.11.0\|/mos:doctor" /home/jsagi/MindrianOS-Plugin/docs/anthropic-upstream-install-cache-drift.md</automated>
  </verify>
  <done>
    - File exists at `docs/anthropic-upstream-install-cache-drift.md`
    - Content describes both incidents (2026-04-13, 2026-04-28)
    - Content describes the /mos:doctor workaround
    - Content lists 3 specific asks (diagnostic visibility, forced-refresh flag, optional session-start advisory)
    - Content explicitly DOES NOT ask for upstream behavior changes that would conflict with the "stable by default" plugin auto-update model
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human reviews release artifacts before commit + tag</name>
  <read_first>
    - CHANGELOG.md (top entry — verify accuracy)
    - .claude-plugin/plugin.json (verify version)
    - package.json (verify version)
    - docs/anthropic-upstream-install-cache-drift.md (verify content)
    - .planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-CONTEXT.md (D-07 milestone, beta-gate mandate)
  </read_first>
  <what-built>
After Wave 1 + Wave 2 Tasks 1-2 (and Plan 95.1-07 Task 3 already approved):
- All 8 DOCTOR-95.1-* requirements closed
- /mos:doctor extended with class A-F detection
- scripts/generate-section-intelligence.cjs built
- Dogfood room compliant
- Three release gate files updated (CHANGELOG, plugin.json, package.json)
- Anthropic upstream bug report draft filed
- Test suite GREEN (existing 27 + new 25 = 52 scenarios)
  </what-built>
  <how-to-verify>
1. **Inspect the release files:**
   ```bash
   head -60 /home/jsagi/MindrianOS-Plugin/CHANGELOG.md
   cat /home/jsagi/MindrianOS-Plugin/.claude-plugin/plugin.json | head -10
   cat /home/jsagi/MindrianOS-Plugin/package.json | head -10
   ```
   Verify all three show `1.12.1-beta.1`.

2. **Inspect the Anthropic draft:**
   ```bash
   cat /home/jsagi/MindrianOS-Plugin/docs/anthropic-upstream-install-cache-drift.md
   ```
   Verify it accurately describes both incidents and the workaround. Edit if anything is wrong before approving.

3. **Inspect git status:**
   ```bash
   cd /home/jsagi/MindrianOS-Plugin
   git status --short
   ```
   Should show: 3 modified release files (CHANGELOG, plugin.json, package.json), 1 new docs file (anthropic-upstream...), modified scripts/doctor.cjs, modified commands/doctor.md, NEW scripts/generate-section-intelligence.cjs, NEW tests/test-doctor-class-*.cjs (5 files), NEW tests/test-cascade-surface-e2e.cjs, NEW tests/test-generate-section-intelligence.cjs, NEW tests/test-doctor-ui-self-compliant.cjs, modified lib/memory/run-feynman-tests.cjs, NEW test/fixtures/cascade-surface-e2e/* (9 files), NEW room/.room-root, NEW room/ROOM.md + MINTO.md across 9 subdirs (~19 files), modified .planning/REQUIREMENTS.md, NEW .planning/phases/95.1-.../95.1-NN-PLAN.md (8 files), NEW .planning/phases/95.1-.../f1-selector-deferred.md.
   Total: ~70 files.

4. **Confirm beta-gate is the right call:**
   This release touches the live install path (--fix). Per release-process.md, beta-gate is mandatory. If you disagree (e.g., "this is more like a hotfix, ship as v1.12.1 stable directly"), flag now BEFORE the commit lands. The default per the mandate is beta.

5. **Final approval:** Type "approved" — Plan continues to commit + tag + push. Or describe issues to defer.
  </how-to-verify>
  <resume-signal>Type "approved" to commit + tag + push, or describe issues for follow-up before commit.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit, tag v1.12.1-beta.1, push (gates 4 and the orchestration of 1-3)</name>
  <read_first>
    - .claude/includes/release-process.md (5-gate enforcement; beta-gate; commit message format)
    - All files modified across Plans 95.1-00..07 (run git status to confirm)
  </read_first>
  <files>(no file edits — git operations)</files>
  <action>
**Git operations from `/home/jsagi/MindrianOS-Plugin/`. Verify cwd at every step. NEVER skip hooks. NEVER use --no-verify.**

**Step A: Confirm cwd + workspace guard.**
```bash
cd /home/jsagi/MindrianOS-Plugin
pwd  # must be /home/jsagi/MindrianOS-Plugin
test -f .claude-plugin/plugin.json || { echo "WRONG WORKSPACE"; exit 1; }
```

**Step B: Confirm git state.**
```bash
git fetch origin main
git status --short
git log origin/main..HEAD  # should be empty if first commit on this branch since last sync
git log HEAD..origin/main  # should be empty (we're not behind); if not, abort and resolve
```

**Step C: Stage all phase outputs.** Avoid `git add .` (per CLAUDE.md guidance). Add specific files:
```bash
git add .planning/REQUIREMENTS.md
git add .planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/
git add tests/test-doctor-class-b.cjs tests/test-doctor-class-c.cjs tests/test-cascade-surface-e2e.cjs tests/test-doctor-class-e.cjs tests/test-doctor-class-f.cjs tests/test-generate-section-intelligence.cjs tests/test-doctor-ui-self-compliant.cjs
git add lib/memory/run-feynman-tests.cjs
git add test/fixtures/cascade-surface-e2e/
git add scripts/doctor.cjs scripts/generate-section-intelligence.cjs
git add commands/doctor.md
git add room/.room-root room/ROOM.md room/MINTO.md
git add room/decisions/ room/product-evolution/
git add CHANGELOG.md .claude-plugin/plugin.json package.json
git add docs/anthropic-upstream-install-cache-drift.md
```

**Step D: Verify staging.**
```bash
git status --short
git diff --cached --stat | tail -5
```

Expected: ~70 files staged. NO unstaged changes that should have been included.

**Step E: Commit. Use HEREDOC for proper formatting per CLAUDE.md rules.**
```bash
git commit -m "$(cat <<'EOF'
release: v1.12.1-beta.1 -- /mos:doctor drift classes B/C/D/E/F + section-intelligence generator

Phase 95.1 closes 5 silent-failure drift classes surfaced by the v1.12.0
fresh-session smoke (2026-04-30):

B: missing .room-root sentinel -- cascade silently skips
C: active-room guard silence -- cross-room writes exit 0 before side-channel
D: surface-layer verification gap -- envelope-to-render path now tested e2e
E: ROOM.md/MINTO.md cascade -- generator referenced since Phase 87-01a now exists
F: UI Ruling System compliance gap -- /mos:doctor itself retrofitted to Shape E

Eight new requirement IDs DOCTOR-95.1-01..08 added to REQUIREMENTS.md.
Seven new tests (52 total scenarios) registered. Dogfood room/ now
Decision-#15 compliant. Anthropic upstream bug report draft filed
(Phase 93 D5 deferred item).

Beta-gated per release-process.md mandate (release infrastructure
ALWAYS ships as a beta first). Install: claude plugin update
mos@mindrian-marketplace --version 1.12.1-beta.1

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**Step F: Verify commit landed.**
```bash
git log -1 --oneline
git status --short  # should be clean
```

**Step G: Tag the release commit (gate 4).**
```bash
git tag -a v1.12.1-beta.1 -m "v1.12.1-beta.1 -- /mos:doctor drift classes B/C/D/E/F + generator"
git tag --list "v1.12.1-beta.1"  # confirm
```

**Step H: Push commit + tag.**
```bash
git push origin main
git push origin v1.12.1-beta.1
```

**Step I: Report gate 5 to user.** (Marketplace ref pin lives in `~/mindrian-marketplace`, a SEPARATE repo. Cannot do from this workspace per Workspace Guard.)

Print this message verbatim to the user:

```
Gates 1-4 landed:
- [x] CHANGELOG.md
- [x] .claude-plugin/plugin.json -> 1.12.1-beta.1
- [x] package.json -> 1.12.1-beta.1
- [x] git tag v1.12.1-beta.1 pushed

Gate 5 (REMAINING — user-side action in separate repo):
- [ ] ~/mindrian-marketplace/.claude-plugin/marketplace.json source.ref pinned to v1.12.1-beta.1

To complete gate 5:
  cd ~/mindrian-marketplace
  # edit .claude-plugin/marketplace.json — change source.ref of mos entry
  # to "v1.12.1-beta.1"
  git add .claude-plugin/marketplace.json
  git commit -m "ref: pin mos to v1.12.1-beta.1"
  git push origin main

Beta install path for testers (Lawrence):
  claude plugin update mos@mindrian-marketplace --version 1.12.1-beta.1

After Lawrence confirms it works, promote by re-releasing as v1.12.1
(without -beta.N suffix) per release-process.md.
```

**CRITICAL — DO NOT:**
- Skip pre-commit hooks (`--no-verify`). If the hook fails, fix and re-stage; do NOT amend or bypass.
- Force push (`--force`).
- Push to anything other than `origin main` and the specific tag.
- Touch the marketplace repo from this workspace (Workspace Guard violation).
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && git tag --list "v1.12.1-beta.1" && git log -1 --oneline | head -1</automated>
  </verify>
  <done>
    - `git tag --list "v1.12.1-beta.1"` returns `v1.12.1-beta.1` (tag exists locally)
    - `git log -1 --oneline` shows the release commit with subject starting `release: v1.12.1-beta.1`
    - `git status --short` is clean (no uncommitted leftovers)
    - `git ls-remote origin v1.12.1-beta.1` shows the tag pushed (verify after push)
    - Pre-commit hook accepted (no `--no-verify` was used)
    - User received the gate-5 follow-up message
  </done>
</task>

</tasks>

<verification>
- All 5 release gates accounted for (4 done, gate 5 user-action documented)
- `node lib/memory/run-feynman-tests.cjs` reports all tests GREEN
- `node scripts/doctor.cjs --all` runs without crashing
- `git tag --list "v*"` includes v1.12.1-beta.1
- Workspace guard honored throughout (cwd = /home/jsagi/MindrianOS-Plugin/ at every step)
</verification>

<success_criteria>
- v1.12.1-beta.1 shipped as beta per release-process.md mandate
- All 8 DOCTOR-95.1-* requirements closed
- All 6 drift classes A-F detectable
- Anthropic upstream bug report draft filed
- User has clear instructions for gate 5 (marketplace ref pin)
- Phase 95.1 closure complete; STATE.md update is a follow-up by /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-08-SUMMARY.md`
</output>
