---
phase: 95.1-mos-doctor-drift-detection-and-self-heal
plan: 04
subsystem: tooling
tags: [doctor, ui-system, shape-e, glyph-vocabulary, 4-zone-anatomy, retrofit, drift-detection]

# Dependency graph
requires:
  - phase: 95.1-02
    provides: tests/test-doctor-ui-self-compliant.cjs RED skeleton (4 scenarios) flipped GREEN by this plan
  - phase: 93
    provides: scripts/doctor.cjs original parseArgs/checkX/performRecovery/renderHumanReport architecture (extension target)
  - phase: 80
    provides: skills/ui-system/SKILL.md governing spec (4-zone, 5+1 body shapes, 12 glyphs, 5 colors)
provides:
  - 4-zone Shape E (Action Report) compliant renderHumanReport in scripts/doctor.cjs
  - body_shape E (Action Report) frontmatter on commands/doctor.md
  - F.1 Next Move structural marker block (canonical AskUserQuestion deferred to Phase 88.2)
  - Density rule (compact header when projected lines > 30) per D-16
  - Zero box-drawing chars or unauthorized glyphs in either target file
  - F.1 selector deferral note documenting Phase 88.2 re-trigger condition
  - computeSummary() helper aggregating healthy/drift/warning totals from class A + future class B-F rows
affects:
  - 95.1-05 (class B/C/E checks extend report.checks slot already wired in renderHumanReport)
  - 95.1-06 (class F detector can now scan scripts/doctor.cjs without flagging it)
  - 88.2 (uiux-selector-block; canonical Shape F.1 implementation will replace structural marker block)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "4-zone Shape E renderer: Zone 1 header + Zone 2 body + Zone 3 (reserved) + Zone 4 action footer (always present)"
    - "Density rule branching on bodyRows.length BEFORE Zone 1 header emission (D-16)"
    - "Structural F.1 marker block as stepping stone for canonical AskUserQuestion (D-18 + Phase 88.2 deferral)"
    - "12-glyph vocabulary mapping: filled-square (■) for class header rows, checkmark (✓) for pass, warning (⚠) for warn, gap (⬜) for missing, U+25B6 (▶) for primary action, U+25B7 (▷) for alternative action, arrow (→) for [before → after]"
    - "ASCII-double-dash (--) replaces all U+2500 (─) decorative comment dividers in scripts/*.cjs (Plan 06 class F detector base)"

key-files:
  created:
    - .planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/f1-selector-deferred.md
  modified:
    - scripts/doctor.cjs (renderHumanReport rewritten + computeSummary helper added + comment dividers cleaned)
    - commands/doctor.md (frontmatter body_shape + argument-hint + Step 1/3 + 3 example output blocks)

key-decisions:
  - "Used 'body_shape: E (Action Report)' canonical form (matches 31+ shipped commands per RESEARCH Pitfall 6); D-10 CONTEXT phrasing 'action-report' was paraphrastic"
  - "Zone 3 stays empty in 95.1 per D-17 (room-proactive integration deferred); inline comment documents this"
  - "F.1 selector ships as STRUCTURAL marker block; canonical AskUserQuestion deferred to Phase 88.2 with explicit re-trigger note"
  - "Comment dividers throughout scripts/doctor.cjs converted from U+2500 (─) to ASCII -- to satisfy Plan 06 class F detector regex (Rule 3 deviation: blocking issue auto-fixed)"
  - "Renderer stays purely structural per D-19; no Larry-voice prose injection"

patterns-established:
  - "Pattern 1: bodyRows accumulator -> density branch -> emit Zone 1 (compact or standard) -> emit body -> emit footer (allows D-16 line-count introspection without re-rendering)"
  - "Pattern 2: report.checks slot in renderHumanReport for Plans 05+06 to register class B/C/D/E/F rows in same Shape-E format without re-architecting"
  - "Pattern 3: F.1 marker block as deferred-canonical bridge (D-18) — verbs (Run / Defer / Free-Text) match Phase 88.2 contract verbatim, so swap-in is mechanical"

requirements-completed:
  - DOCTOR-95.1-06
  - DOCTOR-95.1-07

# Metrics
duration: 16min
completed: 2026-04-30
---

# Phase 95.1 Plan 04: scripts/doctor.cjs UI Ruling System Retrofit Summary

**4-zone Shape E (Action Report) renderer landed in scripts/doctor.cjs with 12-glyph vocabulary + F.1 structural marker block, flipping tests/test-doctor-ui-self-compliant.cjs (4/4) from RED to GREEN and clearing the path for Plan 95.1-06 class F detector to scan scripts/*.cjs without self-flagging.**

## Performance

- **Duration:** ~16 min
- **Started:** 2026-04-30T10:24:59Z
- **Completed:** 2026-04-30T10:40:46Z
- **Tasks:** 3
- **Files modified:** 2 (scripts/doctor.cjs, commands/doctor.md) + 1 created (f1-selector-deferred.md)

## Accomplishments

- scripts/doctor.cjs `renderHumanReport` rewritten as 4-zone Shape E renderer per skills/ui-system/SKILL.md §1-§4
- Zero forbidden box-drawing chars (U+2500 - U+2501, U+2502, U+250C - U+251C, U+2570 - U+256F) anywhere in scripts/doctor.cjs OR commands/doctor.md
- Zero unauthorized cross-marks (U+2717) — replaced with U+26A0 (warning) and U+2B1C (gap) per 12-glyph vocabulary
- Zone 1 header literal `-- MindrianOS -- doctor -- {stage} --` emitted with stage values (no-drift / drift-detected / recovering / recovered) per D-12
- Zone 4 action footer always present (D-12 mandate) with primary action U+25B6 + alternatives U+25B7
- F.1 Next Move structural marker block emitted before Zone 4 when drift detected without --fix (D-18); canonical AskUserQuestion deferred to Phase 88.2 with explicit re-trigger note filed
- Density rule (D-16): when projected lines > 30, Zone 1 collapses to compact `-- doctor: {N} drift classes checked --` header
- Zone 3 (Intelligence Strip) reserved per D-17; default empty (room-proactive integration deferred to future phase)
- commands/doctor.md frontmatter carries `body_shape: E (Action Report)` (canonical form matching 31+ shipped commands per RESEARCH Pitfall 6)
- 3 example output blocks in commands/doctor.md replaced with retrofitted Shape E examples (healthy / drift-detected with F.1 marker / recovered)
- argument-hint expanded to enumerate Plan 95.1-05 + 95.1-06 flags (--cascade-rooms / --verify-surface / --room-md / --ui-compliance / --all / --json)
- All 4 RED tests in tests/test-doctor-ui-self-compliant.cjs flipped to GREEN
- Pre-commit hook ran successfully on every commit; no regression introduced into 111 passing tests

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite scripts/doctor.cjs renderHumanReport for 4-zone Shape E compliance** — `182436d` (refactor)
2. **Task 2: Update commands/doctor.md frontmatter + examples for Shape E compliance** — `e5d8ac3` (docs)
3. **Task 3: File F.1 selector deferral note** — `1d1a7bb` (docs)

## Files Created/Modified

- `scripts/doctor.cjs` — Replaced renderHumanReport (53 lines → 119 lines) with 4-zone Shape E renderer; added computeSummary() helper; converted 9 comment dividers from U+2500 (─) to ASCII --; preserved all parseArgs / checkInstallVersion / checkMarketplaceCache / checkDevSourceConsistency / performRecovery / parseVersion / cmpVersion / copyRecursive / shellQuote / main logic verbatim
- `commands/doctor.md` — Frontmatter gained `body_shape: E (Action Report)` + `body_shape_detail` description; argument-hint expanded to 7 flags; Step 1 enumerates all flag interpretations; Step 3 wording updated to reference SKILL.md; 3 example output blocks replaced with retrofitted Shape E examples; D-19 voice-dna note appended
- `.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/f1-selector-deferred.md` — Created (50 lines) documenting Phase 88.2 deferral, re-trigger condition, and references

## Decisions Made

- **D-10 phrasing reconciliation:** Used `body_shape: E (Action Report)` (canonical shipped form) over CONTEXT D-10's paraphrastic `body_shape: action-report`. RESEARCH Pitfall 6 + Plan 02's RED test both pin on the canonical phrase.
- **Comment-divider cleanup scope:** Plan 04 scope only required retrofitting `renderHumanReport`, but the test regex `/[╭╮╰╯┌┐└┘│─━]/` scans the WHOLE file. Pre-existing decorative dividers (lines 32, 39, 51, 82, 110, 182, 224, 281) all contained forbidden U+2500 (`─`). Auto-fixed under Rule 3 (blocking issue) — converted all to ASCII `--` so test passes AND Plan 06 class F detector can safely scan this file.
- **F.1 selector deferral filed as standalone note:** RESEARCH OQ2 recommended a deferred-items.md entry; Task 3 explicitly created `f1-selector-deferred.md` per plan instruction. Single-purpose file is clearer than burying the deferral in mixed deferred-items.md.
- **Zone 3 implementation:** Hard-coded as ALWAYS empty in Phase 95.1 per D-17 — there's no room-proactive integration in this plan; the renderer simply doesn't emit a Zone 3 block. Inline comment documents this.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Cleaned 9 forbidden box-char comment dividers from scripts/doctor.cjs**
- **Found during:** Task 1 (Rewrite renderHumanReport)
- **Issue:** Plan 04 scope only specified retrofitting renderHumanReport (lines 226-279), but tests/test-doctor-ui-self-compliant.cjs Scenario 2 scans the WHOLE file with regex `[╭╮╰╯┌┐└┘│─━]`. The pre-existing decorative dividers at lines 32, 39, 51, 82, 110, 182, 224, 281 all used U+2500 (─) which fails that test. Plan 06 class F detector will also scan all of `scripts/*.cjs` for forbidden glyphs.
- **Fix:** Replaced all 9 `── HEADING ──...──` dividers with ASCII `-- HEADING --...--`. Visually equivalent for the developer reader; regex-safe for both tests.
- **Files modified:** scripts/doctor.cjs (8 comment-divider edits + 1 box-char header + 1 box-char footer in renderHumanReport)
- **Verification:** `grep -E '╭|╮|╰|╯|✗|┌|┐|└|┘|│|─|━' scripts/doctor.cjs` returns 0 matches; `node tests/test-doctor-ui-self-compliant.cjs` Scenario 2 passes
- **Committed in:** 182436d (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Auto-fix necessary to satisfy Plan 02's existing RED test scope (whole-file regex) and to clear the path for Plan 06's class F detector. No scope creep — this is exactly the kind of base-cleanup that Plan 04 had to do before Plans 05+06 could safely extend the file. Plan 06 explicitly relies on this cleanup per its <sequential_execution> note.

## Issues Encountered

- `.planning/` directory is gitignored at repo root. Task 3 commit required `git add -f` per existing convention (verified by `git log -- .planning/` showing prior plan commits used the same pattern). Documented as routine, not blocking.

## User Setup Required

None — no external service configuration required. Pure CJS retrofit + markdown frontmatter edit + planning-folder note.

## Next Phase Readiness

- Plan 95.1-05 (Wave 1, depends on 95.1-04) can now extend `scripts/doctor.cjs` with class B/C/E check functions; the `report.checks` iteration slot is wired in renderHumanReport, computeSummary handles class B/E rows additively.
- Plan 95.1-06 (Wave 1, depends on 95.1-05) can scan `scripts/doctor.cjs` for class F violations without self-flagging — the file now contains zero unauthorized box chars or glyphs.
- Phase 88.2 (uiux-selector-block, planned) re-trigger condition documented in `f1-selector-deferred.md`: when 88.2 ships, replace the F.1 marker block in renderHumanReport with the canonical AskUserQuestion call.
- DOCTOR-95.1-06 (UI Ruling System self-compliance) and DOCTOR-95.1-07 (F.1 selector renders when no --fix) requirements satisfied.

---
*Phase: 95.1-mos-doctor-drift-detection-and-self-heal*
*Completed: 2026-04-30*

## Self-Check: PASSED

- Files exist: scripts/doctor.cjs, commands/doctor.md, .planning/phases/95.1-.../f1-selector-deferred.md, .planning/phases/95.1-.../95.1-04-SUMMARY.md (all FOUND)
- Commits exist: 182436d (Task 1), e5d8ac3 (Task 2), 1d1a7bb (Task 3) (all FOUND in git log)
- RED test flipped: tests/test-doctor-ui-self-compliant.cjs 4/4 GREEN
- Forbidden chars: zero across scripts/doctor.cjs + commands/doctor.md (regex `[╭╮╰╯┌┐└┘│─━]` returns 0 matches)
- Vocabulary glyphs in use: 6 distinct glyphs from 12-glyph vocabulary (■ ✓ ⚠ ▶ ▷ →) across 22 occurrences in scripts/doctor.cjs
- No regression: 111/118 tests still pass (the 7 failures are pre-existing class B/C/E/F RED scaffolds for Plans 05+06, plus 1 pre-existing 84-smart-notebook + 5 pre-existing self-update failures unrelated to this retrofit)
