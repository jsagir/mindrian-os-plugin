# Phase 95.1: mos:doctor drift detection and self-heal - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 95.1-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-30
**Phase:** 95.1-mos-doctor-drift-detection-and-self-heal
**Areas presented:** Scope split, /mos:doctor extension architecture, Generator design (class E), Class D verification fixture
**Areas selected for discussion:** Generator design, Class D verification fixture
**Mode:** discuss (interactive AskUserQuestion, not advisor mode, not text mode, not auto)

---

## Gray-area selection (multi-select)

**Question:** Which gray areas do you want to discuss for Phase 95.1?

| Area | Description | Selected |
|------|-------------|----------|
| Scope split (Recommended) | A-E in 95.1, or split A/B/C in 95.1 + D/E in 95.2? | |
| /mos:doctor extension architecture | Flag selectors vs subcommands vs always-run-all | |
| Generator design (class E) | scripts/generate-section-intelligence.cjs API + schema source | ✓ |
| Class D verification fixture | Where the live-cascade end-to-end test lives | ✓ |

**Notes:** User skipped "Scope split" (recommended) and "Doctor architecture" — both deferred to Claude's discretion. Decisions taken: D-08 (no split, all A-E in 95.1), D-09 (flag selectors on existing single command). Both rationales documented in CONTEXT.md `<decisions>` § Claude's Discretion.

---

## Generator design (class E)

### Q1: Generator scope per invocation

| Option | Description | Selected |
|--------|-------------|----------|
| Single-dir + --recursive flag (Recommended) | Default = one dir; --recursive walks subtree | ✓ |
| Always whole-room | One command, generates for every dir under closest .room-root | |
| Subcommands (section / room) | Explicit verbs at call site | |

**User's choice:** Single-dir + --recursive flag (Recommended)
**Captured as:** D-01

### Q2: Idempotency

| Option | Description | Selected |
|--------|-------------|----------|
| Skip if exists (Recommended) | Default = leave existing files alone; --force overrides | ✓ |
| Always overwrite | Regenerate every run | |
| Refuse + error | Hard-fail if files exist | |

**User's choice:** Skip if exists (Recommended)
**Captured as:** D-02

### Q3: Schema source

| Option | Description | Selected |
|--------|-------------|----------|
| Hand-rolled minimal frontmatter (Recommended) | Templates baked into the script as strings | ✓ |
| Use test fixture as template | Read cascade-e2e/seed-room/'s files and copy structure | |
| Introspect existing room patterns | Walk the room, infer schema from siblings | |

**User's choice:** Hand-rolled minimal frontmatter (Recommended)
**Captured as:** D-03

---

## Class D verification fixture

### Q1: Fixture location

| Option | Description | Selected |
|--------|-------------|----------|
| test/fixtures/cascade-surface-e2e/ (Recommended) | Mirror existing cascade-e2e pattern, sibling fixture | ✓ |
| Use the dogfood room post-generator | Run test against MindrianOS-Plugin/room/ after generator fills it | |
| Ephemeral tmpdir per run | mktemp -d, build, register, run, teardown | |

**User's choice:** test/fixtures/cascade-surface-e2e/ (Recommended)
**Captured as:** D-04

### Q2: Registry registration in test

| Option | Description | Selected |
|--------|-------------|----------|
| MINDRIAN_ROOMS_ROOT env override (Recommended) | Test process sets env var; resolve-room honors first per Phase 84-03 | ✓ |
| Mock resolveActiveRoomDir at module boundary | require-cache mock | |
| Real registry edit + restore | Mutate ~/MindrianRooms/.rooms/registry.json before/after test | |

**User's choice:** MINDRIAN_ROOMS_ROOT env override (Recommended)
**Captured as:** D-05

### Q3: PASS criteria

| Option | Description | Selected |
|--------|-------------|----------|
| Exists + 8-key shape (Recommended) | File exists + parses + has 8 expected root keys | ✓ |
| Full zod schema validation | Define zod schema, validate exhaustively | |
| Just existence | File exists + non-empty | |

**User's choice:** Exists + 8-key shape (Recommended)
**Captured as:** D-06

---

## Milestone target (closing question)

| Option | Description | Selected |
|--------|-------------|----------|
| v1.12.1 patch (Recommended) | 95.1 ships as Phase 95 follow-up; bug-fix-shaped | ✓ |
| v1.13.0 minor | New code surface counts as semver-minor addition | |
| v1.12.0.1 hotfix-style | Strict no-new-features hotfix discipline | |

**User's choice:** v1.12.1 patch (Recommended)
**Captured as:** D-07

---

## Done question

| Option | Description | Selected |
|--------|-------------|----------|
| Write CONTEXT.md (Recommended) | All decisions captured; ready for downstream | ✓ |
| Explore scope-split as a real area | | |
| Explore /mos:doctor architecture | | |

**User's choice:** Write CONTEXT.md (Recommended)

---

## Claude's Discretion

Two areas the user explicitly skipped in the gray-area multi-select:

- **Scope split** — Claude decision: bundle A-E in 95.1, no 95.2. Rationale: B/C/D/E share the cascade-surface contract and the /mos:doctor + generator surface; splitting forces relitigation.
- **/mos:doctor extension architecture** — Claude decision: flag selectors on the existing single command. Rationale: matches Phase 93's existing `--fix` flag pattern, doesn't fragment the user-facing surface, allows opt-in to expensive checks.

Both fully documented in CONTEXT.md `<decisions>` § Claude's Discretion as D-08 and D-09.

---

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section:

- Active-room switch UX (touches /mos:rooms)
- Cross-room cascade promotion (architectural change to side-channel ownership)
- Stale side-channel cleanup (session-start concern, not /mos:doctor)
- /mos:admin command (Phase 93 deferral, still out)
- Session-start drift detection extension (Phase 93 deferral, still out)
- Anthropic upstream bug report draft (Phase 93 D5, held until /mos:doctor exists — now eligible but out of 95.1)

---

## Late-stage TUI audit (post-write_context, pre-commit)

After CONTEXT.md was first written and the auto-advance gate surfaced, the user asked: "what about all TUI design factors in mindrian?"

Audit ran. Findings:

- Current `scripts/doctor.cjs renderHumanReport` uses `╭─ ╰─ ✗` — none of those are in `skills/ui-system/SKILL.md` §3 12-glyph vocabulary (`■ ▼ ▶ ▷ ├─ └─ ✓ • ⚠ ⚡ ⬜ →`).
- `commands/doctor.md` frontmatter has no `body_shape` field — required by SKILL.md §2.
- No Zone 1 header, no Zone 4 action footer — required by SKILL.md §1 ("NEVER omit").
- Phase 93 (which shipped /mos:doctor on 2026-04-28) was AFTER UI Ruling System became mandatory at Phase 80. So /mos:doctor has been non-compliant since it shipped.
- Phase 93's hotfix discipline ("no feature additions, only bug fixes + safety net") explains why UI compliance was never retrofitted at the time, but the gap remained.

Surfaced as **drift class F** — UI Ruling System compliance gap. /mos:doctor is the most ironic case: the command we extend to detect drift is itself a drift victim.

### User decision: lock all in 95.1

**Question:** Do D-10..D-13 ship in 95.1, split into 95.2, or defer entirely?

| Option | Description | Selected |
|--------|-------------|----------|
| All in 95.1 (Recommended) | Drift class F detected by the same /mos:doctor extension that surfaces it. Whole drift roster in one release. | ✓ |
| D-10..D-12 in 95.1, D-13 in 95.2 | Make new command compliant from birth; class F detector ships separately | |
| All defer to 95.2 | 95.1 stays tight on B/C/D/E | |

**User's response:** "all"
**Interpreted as:** lock D-10..D-13 in 95.1 PLUS the broader cross-surface TUI factors I flagged at the end of the audit reply (color contract, cross-surface adaptation, density, intelligence strip ownership, Shape F selector for --fix, voice-dna integration).

**Captured as:** D-10..D-19 (10 new decisions). drift class F added to roster table in `<domain>`. canonical_refs extended with `skills/ui-system/SKILL.md` and `references/personality/voice-dna.md`.

### Audit-driven decisions (no AskUserQuestion — locked by "all")

| ID | Decision | Source |
|----|----------|--------|
| D-10 | body_shape: action-report (Shape E) | SKILL.md §2 mapping |
| D-11 | Glyph cleanup (replace ╭ ╰ ✗ with vocab) | SKILL.md §3 |
| D-12 | 4-zone retrofit (Header + Footer) | SKILL.md §1 |
| D-13 | Class F detector (commands/, scripts/, renderers) | New scope |
| D-14 | Color contract verify-only | SKILL.md §4 (existing already compliant) |
| D-15 | CLI master, Desktop degrades via Larry, Cowork = CLI | SKILL.md §9 |
| D-16 | Density: compact header at >30 lines, signal cap 2 | SKILL.md §1 |
| D-17 | Drift findings = Zone 2 body; Zone 3 reserved for room-proactive externals | SKILL.md §1 |
| D-18 | F.1 Next Move selector when drift detected without --fix | SKILL.md §2 Shape F + Canon Part 3 |
| D-19 | Renderer stays structural; voice-dna applies only when Larry narrates | SKILL.md §6 + voice-dna.md |

---

## Notable session events

### Live cascade trigger fired during discuss-phase

While editing `.planning/STATE.md` mid-discussion to add the Roadmap Evolution entry for Phase 95.1, the live PostToolUse hook fired:

```
post-write: cascade complete for STATE.md
```

Then again on writing `95.1-CONTEXT.md` itself:

```
post-write: cascade complete for 95.1-CONTEXT.md
```

Both match the v1.12.0 envelope advisory contract exactly. Side-channel files did not refresh in either case (both edits were outside `.room-root` subtrees), but the trigger pattern firing is independent evidence that the v1.12.0 envelope hygiene work is alive at the production hook layer. Folded into CONTEXT.md `<specifics>` as a baseline assertion target for the Phase 95.1 class D test design.

### No matching todos

`gsd-tools.cjs todo match-phase 95.1` returned `{ "matches": [], "todo_count": 0 }`. Nothing to fold from backlog.

---

*Discuss completed: 2026-04-30*
