---
phase: 95.1-mos-doctor-drift-detection-and-self-heal
plan: 01
type: execute
wave: 0
depends_on: [95.1-00]
files_modified:
  - test/fixtures/cascade-surface-e2e/.room-root
  - test/fixtures/cascade-surface-e2e/STATE.md
  - test/fixtures/cascade-surface-e2e/ROOM.md
  - test/fixtures/cascade-surface-e2e/MINTO.md
  - test/fixtures/cascade-surface-e2e/.rooms/registry.json
  - test/fixtures/cascade-surface-e2e/problem-definition/ROOM.md
  - test/fixtures/cascade-surface-e2e/problem-definition/MINTO.md
  - test/fixtures/cascade-surface-e2e/problem-definition/seed-artifact/seed-artifact.md
  - test/fixtures/cascade-surface-e2e/README.md
autonomous: true
requirements:
  - DOCTOR-95.1-08
must_haves:
  truths:
    - "test/fixtures/cascade-surface-e2e/ exists as a SIBLING of test/fixtures/cascade-e2e/, not a subdirectory"
    - "Fixture has .room-root + STATE.md so post-write detect_room_section AND STATE.md walk-up both succeed"
    - "Fixture has .rooms/registry.json so MINDRIAN_ROOMS_HOME=<scratch> resolution works without scripts/resolve-room falling through to ~/MindrianRooms/"
    - "Decision #16 nested artifact layout: seed-artifact lives at problem-definition/seed-artifact/seed-artifact.md"
  artifacts:
    - path: "test/fixtures/cascade-surface-e2e/.room-root"
      provides: "Phase 87-01a sentinel to satisfy detect_room_section walk-up"
    - path: "test/fixtures/cascade-surface-e2e/STATE.md"
      provides: "Cascade walk-up target at scripts/post-write lines 232-238"
    - path: "test/fixtures/cascade-surface-e2e/.rooms/registry.json"
      provides: "Strategy 0 active-room resolution under MINDRIAN_ROOMS_HOME override"
    - path: "test/fixtures/cascade-surface-e2e/README.md"
      provides: "Documentation of pipeline-vs-surface contrast with cascade-e2e/seed-room/"
  key_links:
    - from: "test/fixtures/cascade-surface-e2e/.rooms/registry.json"
      to: "scripts/resolve-room Strategy 0"
      via: "active field + rooms.<name>.path resolution"
      pattern: "\"active\":"
---

<objective>
Seed the cascade-surface-e2e fixture as a SIBLING (not subdirectory) of the existing cascade-e2e/seed-room fixture. This is the test target for class D end-to-end verification (DOCTOR-95.1-02 / DOCTOR-95.1-08). The fixture must satisfy four independent constraints simultaneously: (1) Phase 87-01a `.room-root` sentinel for detect_room_section, (2) STATE.md at room root for the cascade walk-up, (3) `.rooms/registry.json` so MINDRIAN_ROOMS_HOME override resolves, (4) Decision #16 nested artifact layout.

Purpose: Phase 87-03 uses cascade-e2e/seed-room/ to test pipeline output (edge counts). Class D needs a NEW fixture for surface output (envelope -> side-channel -> reader contract). The two fixtures must coexist without sharing state.
Output: 9 new files at `test/fixtures/cascade-surface-e2e/` ready to be exercised by Wave 1's `tests/test-cascade-surface-e2e.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-CONTEXT.md
@.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-RESEARCH.md
@test/fixtures/cascade-e2e/seed-room/ROOM.md
@test/fixtures/cascade-e2e/seed-room/STATE.md
@tests/test-cascade-side-channel.cjs

<interfaces>
<!-- Reference patterns extracted from existing siblings -->

From scripts/resolve-room (Strategy 0, line 34):
```bash
ROOMS_HOME="${MINDRIAN_ROOMS_HOME:-$HOME/MindrianRooms}"
# Strategy 0 reads $ROOMS_HOME/.rooms/registry.json and uses {.active} field
# + rooms.<name>.path to compute roomDir = $ROOMS_HOME/<rel-path>
```

From tests/test-cascade-side-channel.cjs makeRoomRoot() lines 107-150:
```javascript
// Layout that works:
//   <scratch>/                    <- ROOMS_HOME
//     .rooms/registry.json        <- declares "test-room" as active
//     test-room/                  <- the room (= roomDir)
//       .room-root
//       STATE.md
//       problem-definition/      <- the section (= sectionDir)
const registry = {
  active: roomName,
  rooms: { [roomName]: { path: roomName } },
};
```

From scripts/post-write detect_room_section() lines 25-47:
- Walks up from file_path looking for .room-root sentinel (max 12 hops)
- Returns "<roomDir>|<section>" where section = first path component under roomDir

NOTE: cascade-surface-e2e differs from test-cascade-side-channel.cjs scratch layout in ONE way:
the test fixture is a COMMITTED, persistent directory tree under the repo, not a tmpdir scratch.
The Wave 1 test will set MINDRIAN_ROOMS_HOME to test/fixtures/cascade-surface-e2e/ (the OUTER dir
that contains .rooms/) and the room name is the inner sibling. See structure below.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cascade-surface-e2e fixture file tree (9 files)</name>
  <read_first>
    - test/fixtures/cascade-e2e/seed-room/.room-root (verify it's empty / sentinel-only)
    - test/fixtures/cascade-e2e/seed-room/STATE.md (frontmatter shape to mirror)
    - test/fixtures/cascade-e2e/seed-room/ROOM.md (frontmatter shape to mirror)
    - tests/test-cascade-side-channel.cjs (lines 107-150 makeRoomRoot pattern; lines 291-336 test2 envelope-shape pattern — fixture must satisfy these expectations)
    - scripts/resolve-room (lines 30-50 Strategy 0 registry parsing — fixture's registry.json must conform)
  </read_first>
  <files>
    test/fixtures/cascade-surface-e2e/.room-root,
    test/fixtures/cascade-surface-e2e/STATE.md,
    test/fixtures/cascade-surface-e2e/ROOM.md,
    test/fixtures/cascade-surface-e2e/MINTO.md,
    test/fixtures/cascade-surface-e2e/.rooms/registry.json,
    test/fixtures/cascade-surface-e2e/problem-definition/ROOM.md,
    test/fixtures/cascade-surface-e2e/problem-definition/MINTO.md,
    test/fixtures/cascade-surface-e2e/problem-definition/seed-artifact/seed-artifact.md,
    test/fixtures/cascade-surface-e2e/README.md
  </files>
  <action>
**Layout — outer dir `test/fixtures/cascade-surface-e2e/` is BOTH the fixture root AND the MINDRIAN_ROOMS_HOME target.** The fixture is a self-contained mini-rooms-home: it has a `.rooms/registry.json` at its root, which declares the room as "." (the same dir, named via the registry's `active` field — NOT a subdirectory).

Wait — that conflicts with the makeRoomRoot pattern from tests/test-cascade-side-channel.cjs which has the room as a SUBDIR of the scratch (ROOMS_HOME). Re-resolve:

**Final structure (matching the scratch pattern exactly so the Wave 1 test can mirror runBashHook(POST_WRITE, envelope, { env: { MINDRIAN_ROOMS_HOME: <fixturePath> }})):**

```
test/fixtures/cascade-surface-e2e/                <- this is MINDRIAN_ROOMS_HOME (outer)
├── .rooms/
│   └── registry.json                             <- declares "surface-e2e-room" as active
├── surface-e2e-room/                             <- this is the actual room (= roomDir)
│   ├── .room-root                                <- empty sentinel file
│   ├── STATE.md                                  <- room root state for cascade walk-up
│   ├── ROOM.md                                   <- room identity (Layer 0)
│   ├── MINTO.md                                  <- room reasoning (Layer 0)
│   └── problem-definition/                       <- the section
│       ├── ROOM.md                               <- section identity
│       ├── MINTO.md                              <- section reasoning
│       └── seed-artifact/                        <- Decision #16 nested
│           └── seed-artifact.md
└── README.md                                     <- contrast vs cascade-e2e/seed-room/
```

WAIT — files_modified above lists 9 files but does NOT include the `surface-e2e-room/` intermediate directory. Update the file_path layout: every path that says `test/fixtures/cascade-surface-e2e/` (without `surface-e2e-room/` prefix) refers to either README or the registry. Every other file lives under `surface-e2e-room/`. The exact path list, with corrections:

```
test/fixtures/cascade-surface-e2e/.rooms/registry.json
test/fixtures/cascade-surface-e2e/README.md
test/fixtures/cascade-surface-e2e/surface-e2e-room/.room-root
test/fixtures/cascade-surface-e2e/surface-e2e-room/STATE.md
test/fixtures/cascade-surface-e2e/surface-e2e-room/ROOM.md
test/fixtures/cascade-surface-e2e/surface-e2e-room/MINTO.md
test/fixtures/cascade-surface-e2e/surface-e2e-room/problem-definition/ROOM.md
test/fixtures/cascade-surface-e2e/surface-e2e-room/problem-definition/MINTO.md
test/fixtures/cascade-surface-e2e/surface-e2e-room/problem-definition/seed-artifact/seed-artifact.md
```

**File contents (exact bytes):**

`test/fixtures/cascade-surface-e2e/.rooms/registry.json`:
```json
{
  "active": "surface-e2e-room",
  "rooms": {
    "surface-e2e-room": {
      "path": "surface-e2e-room"
    }
  }
}
```

`test/fixtures/cascade-surface-e2e/surface-e2e-room/.room-root`: empty file (0 bytes; just the sentinel).

`test/fixtures/cascade-surface-e2e/surface-e2e-room/STATE.md`:
```markdown
---
room: surface-e2e-fixture
stage: frozen-test-fixture
created: 2026-04-30
license: BSL-1.1
---

# STATE.md

This STATE.md exists so that the cascade walk-up at `scripts/post-write` lines
232-238 finds a room root. Do not edit. See `../README.md` for the
pipeline-vs-surface contrast with `cascade-e2e/seed-room/`.
```

`test/fixtures/cascade-surface-e2e/surface-e2e-room/ROOM.md`:
```markdown
---
room: surface-e2e-fixture
type: test-fixture
created: 2026-04-30
purpose: Class D end-to-end test fixture (DOCTOR-95.1-02 / DOCTOR-95.1-08)
sections:
  - problem-definition
license: BSL-1.1
---

# Surface E2E Fixture Room

Sibling to `cascade-e2e/seed-room/`. This fixture tests the SURFACE path
(envelope -> Claude Code schema validator -> side-channel write -> reader
contract). The seed-room sibling tests the PIPELINE path (edge counts). They
must coexist without sharing state.

The acceptance gate is `<roomDir>/.mindrian/last-cascade.json` exists with the
8 root keys per `tests/test-cascade-side-channel.cjs` Test 3. See
`../README.md` for the rollback policy.
```

`test/fixtures/cascade-surface-e2e/surface-e2e-room/MINTO.md`:
```markdown
---
type: room-minto
room: surface-e2e-fixture
governing_thought: This fixture exists to gate the envelope-to-render surface path against shape regressions.
created: 2026-04-30
license: BSL-1.1
---

# Governing Thought

This fixture exists to gate the envelope-to-render surface path against shape regressions.

## MECE Support

1. detect_room_section walks up to .room-root and returns "<roomDir>|<section>".
2. The cascade pipeline writes <roomDir>/.mindrian/last-cascade.json with 8 root keys.
3. The bash post-write hook emits a Claude Code 2.x compliant envelope with hookSpecificOutput.additionalContext matching one of two recognized prefixes.
```

`test/fixtures/cascade-surface-e2e/surface-e2e-room/problem-definition/ROOM.md`:
```markdown
---
section: problem-definition
parent: surface-e2e-room
created: 2026-04-30
license: BSL-1.1
---

# problem-definition

Section identity for the surface-e2e-room fixture. Holds one seed artifact.
```

`test/fixtures/cascade-surface-e2e/surface-e2e-room/problem-definition/MINTO.md`:
```markdown
---
type: section-minto
section: problem-definition
governing_thought: A single seed artifact is sufficient to fire the cascade end-to-end.
created: 2026-04-30
license: BSL-1.1
---

# Governing Thought

A single seed artifact is sufficient to fire the cascade end-to-end.

## MECE Support

1. The cascade pipeline accepts any markdown file under a section as a trigger.
2. Edge-count assertions are NOT this fixture's job (see cascade-e2e/seed-room/).
3. Surface-shape assertions are this fixture's only job.
```

`test/fixtures/cascade-surface-e2e/surface-e2e-room/problem-definition/seed-artifact/seed-artifact.md`:
```markdown
---
name: seed-artifact
section: problem-definition
created: 2026-04-30
license: BSL-1.1
---

# Seed Artifact

A minimal artifact body. The Wave 1 test will Write to this file via the
PostToolUse hook simulator. The cascade fires; the side-channel JSON
materializes; the test asserts the 8-key shape.
```

`test/fixtures/cascade-surface-e2e/README.md`:
```markdown
# cascade-surface-e2e fixture

**Sibling of:** `test/fixtures/cascade-e2e/seed-room/`
**Purpose:** Class D surface verification per DOCTOR-95.1-02 + DOCTOR-95.1-08.
**Anchor REQ:** see `.planning/REQUIREMENTS.md` "Plugin Self-Healing Diagnostics (DOCTOR-95.1)".

## Pipeline vs Surface

| | cascade-e2e/seed-room/ | cascade-surface-e2e/ |
|---|---|---|
| **Tests** | Pipeline (edge counts) | Surface (envelope + side-channel + reader contract) |
| **Acceptance gate** | `expected-edges.json` exact match | `.mindrian/last-cascade.json` 8-key shape |
| **Rollback policy** | Refactor must preserve edge counts | Surface change must preserve envelope shape |
| **Owns** | Phase 87-03 | Phase 95.1 |

The two fixtures must coexist without sharing state. Do not consolidate.

## Layout

```
cascade-surface-e2e/
├── .rooms/registry.json          # active = "surface-e2e-room"
├── surface-e2e-room/             # the room (resolves under MINDRIAN_ROOMS_HOME=cascade-surface-e2e)
│   ├── .room-root                # Phase 87-01a sentinel
│   ├── STATE.md                  # cascade walk-up target
│   ├── ROOM.md / MINTO.md        # Layer 0
│   └── problem-definition/
│       ├── ROOM.md / MINTO.md
│       └── seed-artifact/seed-artifact.md   # Decision #16 nested
└── README.md (this file)
```

## Hermeticity contract

The Wave 1 test (`tests/test-cascade-surface-e2e.cjs`) MUST set
`env: { MINDRIAN_ROOMS_HOME: '<repo>/test/fixtures/cascade-surface-e2e' }` on
every `runBashHook` call. Forgetting this leaks side-effects into the user's
real active room (drift class C trap). See `95.1-RESEARCH.md` Pitfall 2.

## License

BSL 1.1 (covered by repo LICENSE).
```

**Use Write tool for each file. Use mkdir -p via Bash for the intermediate directories before writing the leaf files. Do NOT add the empty .room-root via heredoc — use `touch`-equivalent (write empty content via Write tool or `> file` via Bash).**
  </action>
  <verify>
    <automated>test -f test/fixtures/cascade-surface-e2e/surface-e2e-room/.room-root && test -f test/fixtures/cascade-surface-e2e/surface-e2e-room/STATE.md && test -f test/fixtures/cascade-surface-e2e/.rooms/registry.json && test -f test/fixtures/cascade-surface-e2e/surface-e2e-room/problem-definition/seed-artifact/seed-artifact.md && jq -e '.active == "surface-e2e-room"' test/fixtures/cascade-surface-e2e/.rooms/registry.json && echo OK</automated>
  </verify>
  <done>
    - All 9 files exist at correct paths
    - `.room-root` is exactly 0 bytes
    - `.rooms/registry.json` parses as JSON and `.active == "surface-e2e-room"`
    - All `.md` files have YAML frontmatter with `license: BSL-1.1`
    - Decision #16 nested layout: seed-artifact lives at `problem-definition/seed-artifact/seed-artifact.md`, NOT at `problem-definition/seed-artifact.md`
    - README.md contains the comparison table contrasting pipeline-level (cascade-e2e) vs surface-level (cascade-surface-e2e)
  </done>
</task>

</tasks>

<verification>
- `find test/fixtures/cascade-surface-e2e -type f | wc -l` returns 9
- `cat test/fixtures/cascade-surface-e2e/.rooms/registry.json | jq -e '.active == "surface-e2e-room" and .rooms."surface-e2e-room".path == "surface-e2e-room"'` returns true
- `wc -c test/fixtures/cascade-surface-e2e/surface-e2e-room/.room-root` returns 0
- `grep -l "BSL-1.1" test/fixtures/cascade-surface-e2e -r` lists all 7 .md files
</verification>

<success_criteria>
- 9 files committed under `test/fixtures/cascade-surface-e2e/`
- Fixture is structurally identical to the makeRoomRoot() scratch pattern from tests/test-cascade-side-channel.cjs (so Wave 1 test can spawn the bash hook with MINDRIAN_ROOMS_HOME pointing at the outer dir)
- README.md documents the pipeline-vs-surface contrast and the hermeticity contract (MINDRIAN_ROOMS_HOME requirement)
</success_criteria>

<output>
After completion, create `.planning/phases/95.1-mos-doctor-drift-detection-and-self-heal/95.1-01-SUMMARY.md`
</output>
