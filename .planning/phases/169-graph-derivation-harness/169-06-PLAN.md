---
phase: 169-graph-derivation-harness
plan: 06
type: execute
wave: 6
depends_on: ["169-05"]
files_modified:
  - tests/test-graph-derivation-verdict.cjs
  - tests/test-169-brain-boundary.cjs
  - tests/test-depth2-full-citizen.cjs
  - tests/test-sqlite-battle.cjs
  - tests/run-all-169.sh
autonomous: true
requirements: [GDH-01, GDH-02, GDH-03, GDH-04, GDH-05, GDH-06, GDH-07, GDH-08, GDH-09, D-169-11]
canon_parts: [3, 4, 6, 8, 9]

must_haves:
  truths:
    - "tests/test-graph-derivation-verdict.cjs is an ADVERSARIAL structured verdict returning {passed:bool, findings:[...]} via a record(check, passed, detail) accumulator that proves GDH-01..09 + the canon guards BY INSTRUMENTATION (not prose), mirroring Phase 167 W5 / Phase 166 W8 / Phase 163 W6"
    - "the verdict proves the FULL-CITIZEN depth>=2 acceptance (D-169-11): it builds the 3-level topology motj-ecosystem -> jonathan-contractor-motj -> b2-journey (real fixture when present, synthetic temp topology otherwise) and asserts EACH nested level is a FULL CITIZEN -- ROOM.md present + own .mindrian/room.db + at least one per-section FEYNMAN.md + a `## Timeline (auto)` section in that FEYNMAN + a NESTED_WITHIN edge UP to its parent + the parent rollup SEES that level's edge DOWN (visible-from-parent); and it asserts a SUB-SUB-room's edge reaches the TOP-level rollup (arbitrary-depth transitivity). The prior verdict had ZERO depth/full-citizen/parent-linkage/FEYNMAN assertions -- all are added"
    - "the verdict proves the b2-journey acceptance BY COUNT against the REAL fixture HEAL-FIRST: when the b2 path (~/MindrianRooms/motj-ecosystem/sub-rooms/jonathan-contractor-motj/b2-journey) is PRESENT the self-heal runs as STEP 0 (GDH-08) THEN the 0 -> N check runs NON-SKIPPABLY and records the actual N + the derived edge types + the healed-room full-citizen markers in the SUMMARY (D-169-08 + D-169-09, HIGH-2); a synthetic sentinel-less-folder topology is the CI-determinism fallback ONLY when the real fixture is absent, never the sole evidence"
    - "the verdict proves the GDH-08/09 heal: a sentinel-less folder is detected, healRoom WITHOUT approvedBy refuses, healRoom WITH approvedBy produces a full citizen (ROOM.md + own db + per-section FEYNMAN + ## Timeline (auto)) registered with a parent + a sentinel parent field + a NESTED_WITHIN edge, and a healed room is byte-indistinguishable from a born room on those markers"
    - "the verdict includes the MEDIUM-4 check: NO derivation-owned cascade edge type (CONTRADICTS / INFORMS / ENABLES / INVALIDATES) is written by a raw INSERT INTO edges in the indexer path (lazygraph-ops.cjs _indexArtifactBody) -- derivation via navigation.writeEdge is the sole cascade writer (D-169-08)"
    - "tests/test-sqlite-battle.cjs is MIGRATED to the post-MEDIUM-4 reality (BATTLE-03/04): indexArtifact on a [[wikilink]] artifact creates the structural BELONGS_TO edge and does NOT create the legacy cascade edges INFORMS/CONTRADICTS (those now come ONLY from the derivation path per D-169-08); the migrated test is REGISTERED in tests/run-all-169.sh as a CARRIED regression fence so the migration is proven GREEN by the phase gate and the break is no longer invisible"
    - "tests/test-169-brain-boundary.cjs is the Part 8 forbidden-substring sweep (the Phase 90 5-tripwire pattern) over graph-derivation.cjs + graph-candidate-producer.cjs + graph-self-heal.cjs + the Stop sweep hook + the SessionStart drain hook + brain-derive-command.cjs, returning 0 forbidden matches, with a sharp-scan self-test (RED then GREEN on a planted artifact-body token)"
    - "the verdict proves the canon guards: every derived edge_type is in the frozen ALLOWED_EDGE_TYPES set (Part 4), the lineage edge is the legal NESTED_WITHIN (not a PART_OF room target, not BELONGS_TO), review_status lands on the NODE not the edge (Part 9 Pitfall 1), the producer emits BOTH CONTRADICTS AND CONVERGES (D-169-06), fable-mode rejects a bad CONTRADICTS (167), the rollup never writes the parent db (Part 8 room isolation), the heal halts at the gate (approvedBy), and re-run is a no-op (GDH-07)"
    - "tests/run-all-169.sh is FINALIZED as the single PASS/FAIL phase gate printing 'Total N, Passed N, Failed N' and exiting non-zero on any failure: every 169 suite + the verdict + the depth2 full-citizen test + the Part 8 sweep + the carried Phase 168 cascade floor test + the carried Phase 169-00 room-lineage floor test + the carried migrated tests/test-sqlite-battle.cjs regression fence + an em-dash sweep, all green"
    - "the carried Phase 168 cascade floor test AND the carried Phase 169-00 room-lineage floor test stay GREEN (the frozen edge set is intact; NESTED_WITHIN was the only addition this phase, minted in Plan 00)"
  artifacts:
    - path: "tests/test-graph-derivation-verdict.cjs"
      provides: "the adversarial {passed, findings[]} verdict over GDH-01..09 + the canon guards + the MEDIUM-4 sole-cascade-writer check + the REAL-b2 heal-first 0 -> N acceptance + the depth>=2 full-citizen + parent-linkage + FEYNMAN assertions, by instrumentation"
      min_lines: 200
    - path: "tests/test-169-brain-boundary.cjs"
      provides: "the finalized Part 8 forbidden-substring sweep over the six surfaces (graph-derivation + candidate-producer + self-heal + sweep + drain + brain-derive) + a sharp-scan self-test"
      min_lines: 60
    - path: "tests/test-depth2-full-citizen.cjs"
      provides: "the depth>=2 full-citizen acceptance turned GREEN (3-level topology; each level a full citizen; sub-sub-room edge reaches the top rollup)"
      min_lines: 50
    - path: "tests/test-sqlite-battle.cjs"
      provides: "the migrated battle test: BATTLE-03/04 assert indexArtifact writes BELONGS_TO and NO legacy cascade edges (INFORMS/CONTRADICTS) post-MEDIUM-4; the carried regression fence"
      contains: "BELONGS_TO"
    - path: "tests/run-all-169.sh"
      provides: "the finalized single PASS/FAIL phase gate (all suites + verdict + depth2 + Part 8 + carried 168 cascade floor + carried 169-00 room-lineage floor + carried migrated sqlite-battle fence + em-dash sweep)"
      min_lines: 70
  key_links:
    - from: "tests/test-graph-derivation-verdict.cjs"
      to: "lib/core/graph-derivation.cjs + lib/core/graph-candidate-producer.cjs + lib/core/graph-self-heal.cjs + lib/core/room-root.cjs + lib/core/doc-text-extractor.cjs + commands/graph.md"
      via: "drives the SHIPPED surfaces through every GDH acceptance scenario + the heal-first REAL b2 0 -> N + the depth>=2 full-citizen topology"
      pattern: "runDerivation|resolveRoomRoot|extractDocText|produceCandidates|healRoom"
    - from: "tests/test-graph-derivation-verdict.cjs"
      to: "lib/core/navigation/edges.cjs NESTED_WITHIN + lib/core/feynman/timeline-runner.cjs"
      via: "asserts the lineage edge is the legal NESTED_WITHIN + each healed level has a ## Timeline (auto) section"
      pattern: "NESTED_WITHIN|Timeline"
    - from: "tests/test-graph-derivation-verdict.cjs"
      to: "lib/core/lazygraph-ops.cjs _indexArtifactBody"
      via: "the MEDIUM-4 check asserts no derivation-owned cascade type is raw-INSERTed in the indexer"
      pattern: "INSERT INTO edges|CONTRADICTS"
    - from: "tests/test-sqlite-battle.cjs"
      to: "lib/core/lazygraph-ops.cjs indexArtifact"
      via: "BATTLE-03/04 assert the structural index writes BELONGS_TO but not the disabled cascade edges post-D-169-08"
      pattern: "BELONGS_TO|INFORMS|CONTRADICTS"
    - from: "tests/test-169-brain-boundary.cjs"
      to: "lib/core/graph-candidate-producer.cjs + lib/core/graph-derivation.cjs + lib/core/graph-self-heal.cjs + scripts/gsd-graph-derive-sweep.cjs + scripts/gsd-graph-derive-drain.cjs + scripts/brain-derive-command.cjs"
      via: "forbidden-substring sweep proving zero user-content reaches a Brain query"
      pattern: "brain-derive-command|graph-candidate-producer|graph-self-heal"
    - from: "tests/run-all-169.sh"
      to: "tests/test-graph-derivation-verdict.cjs + tests/test-edges-part4-cascade-floor.cjs + tests/test-edges-room-lineage-floor.cjs + tests/test-sqlite-battle.cjs"
      via: "the phase gate runs the verdict + BOTH carried floor tests + the carried migrated sqlite-battle fence"
      pattern: "test-graph-derivation-verdict|test-edges-room-lineage-floor|test-sqlite-battle"
---

<objective>
WAVE 6 VERIFY (harness-as-code property 6). Ship the adversarial structured {passed, findings[]} verdict
that proves GDH-01..09 + the canon guards BY INSTRUMENTATION, including the previously-MISSING depth>=2
full-citizen + parent-linkage + FEYNMAN-temporal assertions the 8-agent fan-out found absent. Finalize the
Part 8 boundary sweep over the SIX relevant surfaces (now incl. graph-self-heal.cjs), prove the b2-journey
acceptance (typed-edge count 0 -> N) by count AGAINST THE REAL FIXTURE HEAL-FIRST (non-skippable when
present, recording N + edge types + the healed-room full-citizen markers in the SUMMARY), include the
MEDIUM-4 sole-cascade-writer check, MIGRATE tests/test-sqlite-battle.cjs to the post-MEDIUM-4 reality and
carry it as a regression fence, and finalize tests/run-all-169.sh as the single PASS/FAIL phase gate (all
suites + the verdict + the depth2 full-citizen test + Part 8 + BOTH carried floor tests + the carried
migrated battle fence + an em-dash sweep).

The depth>=2 full-citizen task closes the core fan-out finding: the prior verdict asserted NO depth, NO
full-citizen markers, NO parent-linkage, NO FEYNMAN temporal. This wave builds the 3-level topology
motj-ecosystem -> jonathan-contractor-motj -> b2-journey and asserts EACH level is a full citizen (ROOM.md +
own db + per-section FEYNMAN + ## Timeline (auto) + NESTED_WITHIN up + visible-from-parent rollup down), and
asserts a sub-sub-room's edges reach the TOP-level rollup. The migrate-and-fence task closes the BLOCKER the
plan-check found (the cascade-disable would silently break the un-aggregated battle test). The Verify wave
does not start until Plan 05's barrier passes.

Purpose: turn test-169-brain-boundary.cjs + test-depth2-full-citizen.cjs (RED from Plan 01) GREEN, add the
verdict, prove the real b2 moat goes 0 -> N heal-first, prove the fractal holds at depth>=2, migrate + carry
the battle test, and lock the gate.
Output: tests/test-graph-derivation-verdict.cjs + the finalized boundary sweep + the GREEN depth2 test + the
migrated tests/test-sqlite-battle.cjs + the finalized run-all-169.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-CONTEXT.md
@.planning/phases/169-graph-derivation-harness/169-VALIDATION.md
@.planning/phases/169-graph-derivation-harness/169-00-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-04-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-05-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-07-PLAN.md
@tests/test-harness-167-verdict.cjs
@tests/test-harness-167-part8-leak.cjs
@tests/test-sqlite-battle.cjs
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract. This plan CONSUMES every shipped surface (resolveRoomRoot,
extractDocText, produceCandidates, candidateToFinding, runDerivation, rollupSubRooms RECURSIVE,
detectUnsentineledArtifactFolder, healRoom, the NESTED_WITHIN lineage edge, the --derive heal-first backfill,
the Stop sweep hook, the SessionStart drain hook) and proves them, including the depth>=2 full-citizen
acceptance.
</shared_iface_contract>

<rules>
- Harness-as-code property 6: the verdict proves BY INSTRUMENTATION (a record(check, passed, detail)
  accumulator returning {passed, findings[]}), never by prose. Mirror tests/test-harness-167-verdict.cjs.
- D-169-11 (the core add): the verdict MUST include depth>=2 full-citizen assertions -- build a 3-level
  topology, assert EACH nested level is a full citizen (ROOM.md + own room.db + per-section FEYNMAN +
  ## Timeline (auto) + NESTED_WITHIN up + visible-from-parent rollup down), and assert a sub-sub-room edge
  reaches the TOP rollup. The lineage edge asserted is the legal NESTED_WITHIN (NOT PART_OF, NOT BELONGS_TO).
- D-169-08/09 (HIGH-2): the b2 acceptance runs HEAL-FIRST against the REAL fixture when present
  (non-skippable), recording N + edge types + the healed full-citizen markers in the SUMMARY; the synthetic
  topology is the fallback ONLY when the fixture is absent.
- D-169-08 (MEDIUM-4): the verdict asserts NO derivation-owned cascade edge type is raw-INSERTed in the
  indexer path; derivation via navigation.writeEdge is the sole cascade writer.
- D-169-08 (MEDIUM-4 migration): tests/test-sqlite-battle.cjs BATTLE-03/04 are MIGRATED to the post-disable
  reality (BELONGS_TO yes, INFORMS/CONTRADICTS from the structural index no); carried in run-all-169.sh. Do
  NOT re-enable the cascade; migrate the assertions.
- Part 8: the boundary sweep is comment-filtered (grep -v '^#') with a sharp-scan self-test; never a bare
  ==0 on an unfiltered file. The sweep covers graph-self-heal.cjs (the heal reads room artifacts + writes the
  lineage edge; it must not leak to Brain) AND graph-candidate-producer.cjs.
- Part 4/9: the verdict asserts edge_type in the frozen set + the NESTED_WITHIN lineage representation +
  review_status on the NODE + the producer emits both edge families; BOTH carried floor tests stay green; NO
  edge type other than the Plan-00 NESTED_WITHIN minted this phase.
- The phase gate prints Total/Passed/Failed and exits non-zero on any failure.
- A self-defect the verdict catches in its own first draft is fixed in-wave and reported as a finding (the
  FINDING-167-05-01 / FINDING-163-06-01 pattern).
- NO em-dashes. CJS / bash only. No new deps.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: the adversarial structured verdict (GDH-01..09 + real-b2 heal-first + depth>=2 + MEDIUM-4) + the finalized Part 8 sweep</name>
  <read_first>
    - tests/test-harness-167-verdict.cjs (the record(check,passed,detail) accumulator shape to mirror) +
      tests/test-harness-167-part8-leak.cjs (the surface-sweep + sharp-scan self-test pattern)
    - lib/core/graph-derivation.cjs (runDerivation + rollupSubRooms RECURSIVE + candidateToFinding),
      lib/core/graph-candidate-producer.cjs (produceCandidates), lib/core/graph-self-heal.cjs
      (detectUnsentineledArtifactFolder + healRoom), lib/core/room-root.cjs, lib/core/doc-text-extractor.cjs,
      commands/graph.md --derive (the surfaces to drive)
    - lib/core/navigation/edges.cjs NESTED_WITHIN (Plan 00) + lib/core/feynman/timeline-runner.cjs (the
      ## Timeline (auto) section the depth2 check asserts)
    - lib/core/lazygraph-ops.cjs _indexArtifactBody (the MEDIUM-4 target: assert no raw cascade INSERT)
    - 169-RESEARCH.md Pitfall 1 (review_status node column) + Pitfall 4 (brain-derive boundary) + the
      Acceptance section (b2 0 -> N) + 169-CONTEXT.md D-169-06/07/08/09/10/11 + 169-VALIDATION.md Part 8 row +
      the real-b2 manual-verify gate row
    - the b2 fixture path: ~/MindrianRooms/motj-ecosystem/sub-rooms/jonathan-contractor-motj/b2-journey
  </read_first>
  <action>
    Create tests/test-graph-derivation-verdict.cjs: a record(check, passed, detail) accumulator returning
    {passed, checks, failed, findings} that drives the SHIPPED surfaces and proves --
    GDH-01 (resolveRoomRoot returns the sub-room dir for a sub-room file while the active room is the parent);
    GDH-03 (rollupSubRooms ATTACH UNION sees a sub-room edge; the parent db row count is unchanged) AND the
    RECURSIVE depth-transitivity (a sub-sub-room edge reaches the TOP rollup);
    GDH-04 (extractDocText > 0 runs on the fixture, source bytes unchanged);
    GDH-05 (produceCandidates emits BOTH a CONTRADICTS AND a CONVERGES candidate; runDerivation bridges via
    candidateToFinding and lands a proposed NODE + typed edge; fable-mode drops an unjustified CONTRADICTS;
    NO edge.properties.review_status);
    GDH-06 (the HEAL-FIRST backfill drives typed-edge count 0 -> N: when the REAL b2 path exists run STEP 0
    self-heal THEN derive NON-SKIPPABLY and record the actual N + derived edge types + the healed full-citizen
    markers via a finding detail string the SUMMARY captures; otherwise the synthetic sentinel-less topology);
    GDH-07 (a second runDerivation mints no duplicate proposed node; a confirmed node untouched; the heal
    re-run is a no-op);
    GDH-08 (detectUnsentineledArtifactFolder finds a sentinel-less folder; healRoom WITHOUT approvedBy
    refuses; WITH approvedBy heals);
    GDH-09 (the healed room is a full citizen: ROOM.md + own db + per-section FEYNMAN + ## Timeline (auto) +
    a NESTED_WITHIN edge to its parent + a registry/sentinel parent field; byte-indistinguishable-on-markers
    from a born room);
    the DEPTH>=2 FULL-CITIZEN acceptance (D-169-11): build the 3-level topology
    motj-ecosystem -> jonathan-contractor-motj -> b2-journey (real fixture when present, synthetic temp
    topology otherwise) and for EACH nested level assert the full-citizen markers above + the parent rollup
    SEES that level's edge; assert a sub-sub-room edge reaches the TOP rollup;
    the lineage-legality check (the lineage edge is NESTED_WITHIN, NOT a PART_OF room target, NOT BELONGS_TO);
    the MEDIUM-4 sole-cascade-writer check (grep lazygraph-ops.cjs _indexArtifactBody for a raw INSERT INTO
    edges of CONTRADICTS/INFORMS/ENABLES/INVALIDATES -> assert NONE remain);
    Part 4 (every derived edge_type in the frozen ALLOWED_EDGE_TYPES set);
    Part 9 (review_status on the NODE);
    Part 3 (the heal halts at the gate -- approvedBy required);
    and a no-em-dash self-check. Finalize tests/test-169-brain-boundary.cjs as the Part 8 forbidden-substring
    sweep over graph-derivation.cjs + graph-candidate-producer.cjs + graph-self-heal.cjs +
    scripts/gsd-graph-derive-sweep.cjs + scripts/gsd-graph-derive-drain.cjs + scripts/brain-derive-command.cjs
    (comment-filtered, with a sharp-scan self-test that plants an artifact-body token and proves the sweep
    goes RED then GREEN). Turn tests/test-depth2-full-citizen.cjs GREEN against the shipped surfaces. No
    em-dashes.
  </action>
  <verify>
    <automated>node tests/test-graph-derivation-verdict.cjs && node tests/test-169-brain-boundary.cjs && node tests/test-depth2-full-citizen.cjs</automated>
  </verify>
  <done>The verdict returns {passed:true} proving GDH-01..09 + the canon guards + the MEDIUM-4
  sole-cascade-writer check + the b2 heal-first 0 -> N acceptance (against the REAL fixture when present,
  recording N + edge types + the healed full-citizen markers in the SUMMARY) + the depth>=2 full-citizen +
  parent-linkage + FEYNMAN-temporal assertions + the NESTED_WITHIN lineage-legality, all by instrumentation;
  the Part 8 sweep returns 0 forbidden matches over all six surfaces with a working sharp-scan self-test;
  the depth2 full-citizen test is green.</done>
</task>

<task type="auto">
  <name>Task 2: migrate test-sqlite-battle.cjs to the post-MEDIUM-4 reality (carried regression fence)</name>
  <read_first>
    - tests/test-sqlite-battle.cjs BATTLE-03 :79-87 (asserts indexArtifact creates an INFORMS edge from a
      [[wikilink]]) + BATTLE-04 :89-97 (asserts a CONTRADICTS edge near a wikilink) -- both assert the legacy
      cascade behavior that D-169-08 / Plan 04 Task 1e disables; BATTLE-07 :126-133 (rebuildGraph
      consistency) for the BELONGS_TO structural expectation
    - lib/core/lazygraph-ops.cjs _indexArtifactBody :363-365 (the BELONGS_TO write that STAYS) + :386-413
      (the disabled cascade writes of CONTRADICTS/INFORMS/ENABLES/INVALIDATES per Plan 04)
    - 169-CONTEXT.md D-169-08 (the legacy raw cascade is disabled so derivation is the sole cascade writer)
    - tests/run-all-169.sh (the carried floor-test registrations are the pattern to mirror for the carried
      battle fence)
  </read_first>
  <behavior>
    - BATTLE-03 asserts indexArtifact on a [[wikilink]] artifact creates the structural BELONGS_TO edge and
      does NOT create an INFORMS edge (the structural index no longer writes the cascade; derivation does)
    - BATTLE-04 asserts indexArtifact on a contradiction-near-wikilink artifact does NOT create a
      CONTRADICTS edge from the structural index (derivation is now the sole cascade source)
    - the rest of the battle suite (BATTLE-01/02/05..12) stays unchanged and green
    - test-sqlite-battle.cjs is registered in run-all-169.sh as a carried regression fence
  </behavior>
  <action>
    Migrate tests/test-sqlite-battle.cjs BATTLE-03 and BATTLE-04 to the post-disable reality. For BATTLE-03:
    after indexArtifact on the [[wikilink]] artifact, assert the structural BELONGS_TO edge exists for that
    artifact (SELECT FROM edges WHERE source = <artifact id> AND type = 'BELONGS_TO') and assert NO INFORMS
    edge was created by the structural index (the INFORMS-from-wikilink count is now 0). For BATTLE-04: after
    indexArtifact on the contradiction-near-wikilink artifact, assert NO CONTRADICTS edge was created by the
    structural index (count 0). Update the describe/it labels + assertion messages to name the new reality
    (structural index writes membership, derivation writes the cascade). Do NOT re-enable the legacy cascade
    to satisfy the old assertions; migrate the assertions. Then REGISTER tests/test-sqlite-battle.cjs in
    tests/run-all-169.sh as a CARRIED regression fence next to the carried floor tests. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-sqlite-battle.cjs && grep -q "test-sqlite-battle.cjs" tests/run-all-169.sh && echo BATTLE_MIGRATED_AND_CARRIED</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-sqlite-battle.cjs` exits 0 (BATTLE-03 asserts BELONGS_TO present + no INFORMS from the
      structural index; BATTLE-04 asserts no CONTRADICTS from the structural index; the other battle cases
      stay green).
    - `tests/run-all-169.sh` includes test-sqlite-battle.cjs as a carried regression fence (grep finds the
      reference and the suite runs inside the aggregator's PASS/FAIL accounting).
  </acceptance_criteria>
  <done>BATTLE-03/04 assert the post-MEDIUM-4 reality (BELONGS_TO yes, INFORMS/CONTRADICTS from the
  structural index no), the rest of the battle suite is unchanged and green, and test-sqlite-battle.cjs is a
  carried regression fence inside run-all-169.sh.</done>
</task>

<task type="auto">
  <name>Task 3: finalize run-all-169.sh as the single phase gate</name>
  <read_first>
    - tests/run-all-169.sh (the scaffold from Plan 01 / 00 to finalize)
    - tests/run-all-167.sh (the finalized-gate shape: per-suite invoke, Total/Passed/Failed, exit code,
      Part 8 sweep + em-dash sweep)
    - tests/test-edges-part4-cascade-floor.cjs (the carried Phase 168 cascade floor test) +
      tests/test-edges-room-lineage-floor.cjs (the carried Phase 169-00 room-lineage floor test)
    - tests/test-sqlite-battle.cjs (the carried migrated regression fence from Task 2)
  </read_first>
  <action>
    Finalize tests/run-all-169.sh: register every 169 suite (room-root-resolver, doc-text-extractor,
    subroom-rollup, recursive-rollup, candidate-producer, graph-derivation-loop, derive-idempotence,
    derive-backfill-acceptance, graph-derive-sweep, sentinel-self-heal, room-lineage-edge,
    depth2-full-citizen, graph-derivation-verdict, 169-brain-boundary) plus the carried
    tests/test-edges-part4-cascade-floor.cjs AND tests/test-edges-room-lineage-floor.cjs AND the carried
    migrated tests/test-sqlite-battle.cjs regression fence, the Part 8 grep sweep (incl. graph-self-heal.cjs),
    and the em-dash sweep over the 169 surfaces; accumulate PASS/FAIL; print "Total N, Passed N, Failed N";
    exit non-zero on any failure. Confirm BOTH carried floor tests pass (frozen set intact: NESTED_WITHIN was
    the only addition, minted in Plan 00) and the carried migrated battle fence passes. No em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-169.sh && node tests/test-edges-part4-cascade-floor.cjs && node tests/test-edges-room-lineage-floor.cjs && node tests/test-sqlite-battle.cjs && echo PHASE_GATE_GREEN</automated>
  </verify>
  <done>bash tests/run-all-169.sh prints Total/Passed/Failed with Failed 0 and exit 0; BOTH carried floor
  tests are green; the carried migrated test-sqlite-battle.cjs fence is green; the gate is the single
  PASS/FAIL phase contract for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| derivation + heal surfaces -> Brain | the verdict + sweep prove zero user-content egress (incl. the LLM producer + the self-heal) |
| verdict harness -> shipped code | the verdict drives real surfaces, not mocks; the b2 proof is the REAL fixture heal-first |
| nested levels -> rollup | the depth2 check proves the rollup sees each level read-only at arbitrary depth, never merging |
| indexer -> cascade edges | the verdict asserts derivation is the sole cascade writer (MEDIUM-4); the carried battle fence proves the structural index no longer writes the cascade |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-14 | Information Disclosure | a .docx body string / dossier name reaching a Brain query in brain-derive, the LLM producer, or the self-heal | mitigate | the Part 8 sweep over all six surfaces returns 0 forbidden matches; the LLM producer + the heal + the LOCAL derivers never touch the wire (Pitfall 4) |
| T-169-15 | Tampering | a vacuously-passing verdict (false green), or the synthetic topology masking a real b2 failure | mitigate | the verdict drives the SHIPPED surfaces by instrumentation + the REAL b2 heal-first 0 -> N (non-skippable when present, N + edge types + full-citizen markers in the SUMMARY); the sharp-scan self-test proves the Part 8 sweep can go RED |
| T-169-16 | Tampering | a new edge type silently minted beyond NESTED_WITHIN, breaking the frozen-set floors | mitigate | BOTH carried floor tests stay registered + green; the verdict asserts edge_type membership in ALLOWED_EDGE_TYPES |
| T-169-18 | Tampering | two cascade writers (legacy raw + derivation) both writing CONTRADICTS / INFORMS | mitigate | the verdict's MEDIUM-4 check + the carried migrated battle fence assert derivation is the sole cascade writer |
| T-169-19 | Tampering | the cascade-disable silently breaking a committed test outside any aggregator | mitigate | test-sqlite-battle.cjs migrated + carried in run-all-169.sh so the phase gate proves it green |
| T-169-27 | Tampering | the verdict passing without the depth>=2 / full-citizen / parent-linkage / FEYNMAN assertions (the original fan-out gap) | mitigate | the depth2 full-citizen test is GREEN + registered; the verdict records a finding per level (ROOM.md, own db, per-section FEYNMAN, ## Timeline (auto), NESTED_WITHIN up, rollup-visible down) + the sub-sub-room-reaches-top check |
| T-169-SC | Tampering | npm installs | mitigate | zero new packages; no install task |
</threat_model>

<verification>
- node tests/test-graph-derivation-verdict.cjs returns {passed:true} (GDH-01..09 + canon guards + MEDIUM-4 +
  the REAL b2 heal-first 0 -> N + the depth>=2 full-citizen + parent-linkage + FEYNMAN + NESTED_WITHIN
  lineage-legality, recording N + edge types + markers in the SUMMARY).
- node tests/test-169-brain-boundary.cjs returns 0 forbidden matches over all six surfaces with a working
  self-test; node tests/test-depth2-full-citizen.cjs GREEN.
- node tests/test-sqlite-battle.cjs exits 0 (BATTLE-03/04 migrated to the post-MEDIUM-4 reality).
- bash tests/run-all-169.sh prints Total/Passed/Failed, Failed 0, exit 0; BOTH carried floor tests green;
  the carried migrated sqlite-battle fence green.
- No em-dashes anywhere.
</verification>

<success_criteria>
The harness is proven end to end: the adversarial structured verdict shows GDH-01..09 + the canon guards +
the sole-cascade-writer disposition + the NESTED_WITHIN lineage-legality honored by instrumentation, the
REAL b2-journey is healed-first into a FULL CITIZEN and its moat goes 0 -> N (N + edge types + full-citizen
markers in the SUMMARY), the fractal holds at depth>=2 (each of three levels a full citizen + parent-linked
up + rollup-visible down + a sub-sub-room reaching the top), Part 8 is clean across all six surfaces, the
committed test-sqlite-battle.cjs is migrated + carried (the invisible break closed), and run-all-169.sh is
the single PASS/FAIL phase gate. The big claim holds: the nested folder hierarchy carrying memory + typed
edges at every level IS the wicked-problem substrate, self-wiring at arbitrary depth.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-06-SUMMARY.md` when done. Record the actual b2
0 -> N typed-edge count + the derived edge types + the healed-room full-citizen markers (incl. the depth>=2
topology proof) in the SUMMARY (D-169-08 + D-169-09 + D-169-11, HIGH-2).
</output>
