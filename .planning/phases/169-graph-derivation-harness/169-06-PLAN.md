---
phase: 169-graph-derivation-harness
plan: 06
type: execute
wave: 5
depends_on: ["169-05"]
files_modified:
  - tests/test-graph-derivation-verdict.cjs
  - tests/test-169-brain-boundary.cjs
  - tests/test-sqlite-battle.cjs
  - tests/run-all-169.sh
autonomous: true
requirements: [GDH-01, GDH-02, GDH-03, GDH-04, GDH-05, GDH-06, GDH-07]
canon_parts: [4, 6, 8, 9]

must_haves:
  truths:
    - "tests/test-graph-derivation-verdict.cjs is an ADVERSARIAL structured verdict returning {passed:bool, findings:[...]} via a record(check, passed, detail) accumulator that proves GDH-01..07 + the canon guards BY INSTRUMENTATION (not prose), mirroring Phase 167 W5 / Phase 166 W8 / Phase 163 W6"
    - "the verdict proves the b2-journey acceptance BY COUNT against the REAL fixture: when the b2 path (~/MindrianRooms/motj-ecosystem/sub-rooms/jonathan-contractor-motj/b2-journey) is PRESENT (the dogfood/maintainer box) the 0 -> N check runs NON-SKIPPABLY and records the actual N + the derived edge types in the SUMMARY (D-169-08, HIGH-2); a synthetic two-artifact room is the CI-determinism fallback ONLY when the real fixture is absent, never the sole evidence"
    - "the verdict includes the MEDIUM-4 check: NO derivation-owned cascade edge type (CONTRADICTS / INFORMS / ENABLES / INVALIDATES) is written by a raw INSERT INTO edges in the indexer path (lazygraph-ops.cjs _indexArtifactBody) -- derivation via navigation.writeEdge is the sole cascade writer (D-169-08)"
    - "tests/test-sqlite-battle.cjs is MIGRATED to the post-MEDIUM-4 reality (BATTLE-03/04): indexArtifact on a [[wikilink]] artifact creates the structural BELONGS_TO edge and does NOT create the legacy cascade edges INFORMS/CONTRADICTS (those now come ONLY from the derivation path, never the structural index, per D-169-08); the migrated test is REGISTERED in tests/run-all-169.sh as a CARRIED regression fence (mirroring the carried Phase 168 floor test) so the migration is proven GREEN by the phase gate and the break is no longer invisible"
    - "tests/test-169-brain-boundary.cjs is the Part 8 forbidden-substring sweep (the Phase 90 5-tripwire pattern) over graph-derivation.cjs + graph-candidate-producer.cjs + the Stop sweep hook + the SessionStart drain hook + brain-derive-command.cjs, returning 0 forbidden matches, with a sharp-scan self-test (RED then GREEN on a planted artifact-body token)"
    - "the verdict proves the canon guards: every derived edge_type is in the frozen ALLOWED_EDGE_TYPES set (Part 4), review_status lands on the NODE not the edge (Part 9 Pitfall 1), the producer emits BOTH CONTRADICTS AND CONVERGES (D-169-06), fable-mode rejects a bad CONTRADICTS (167), the rollup never writes the parent db (Part 8 room isolation), and re-run is a no-op (GDH-07)"
    - "tests/run-all-169.sh is FINALIZED as the single PASS/FAIL phase gate printing 'Total N, Passed N, Failed N' and exiting non-zero on any failure: every 169 suite + the verdict + the Part 8 sweep + the carried Phase 168 floor test (tests/test-edges-part4-cascade-floor.cjs) + the carried migrated tests/test-sqlite-battle.cjs regression fence + an em-dash sweep, all green"
    - "the carried Phase 168 floor test stays GREEN (the frozen edge set is untouched; no new edge type minted this phase)"
  artifacts:
    - path: "tests/test-graph-derivation-verdict.cjs"
      provides: "the adversarial {passed, findings[]} verdict over GDH-01..07 + the canon guards + the MEDIUM-4 sole-cascade-writer check + the REAL-b2 0 -> N acceptance (non-skippable when present), by instrumentation"
      min_lines: 160
    - path: "tests/test-169-brain-boundary.cjs"
      provides: "the finalized Part 8 forbidden-substring sweep over the 5 surfaces (graph-derivation + candidate-producer + sweep + drain + brain-derive) + a sharp-scan self-test"
      min_lines: 60
    - path: "tests/test-sqlite-battle.cjs"
      provides: "the migrated battle test: BATTLE-03/04 assert indexArtifact writes BELONGS_TO and NO legacy cascade edges (INFORMS/CONTRADICTS) post-MEDIUM-4; the carried regression fence proving the cascade-disable did not silently break the committed structural-index suite"
      contains: "BELONGS_TO"
    - path: "tests/run-all-169.sh"
      provides: "the finalized single PASS/FAIL phase gate (all suites + verdict + Part 8 + carried 168 floor + carried migrated sqlite-battle fence + em-dash sweep)"
      min_lines: 60
  key_links:
    - from: "tests/test-graph-derivation-verdict.cjs"
      to: "lib/core/graph-derivation.cjs + lib/core/graph-candidate-producer.cjs + lib/core/room-root.cjs + lib/core/doc-text-extractor.cjs + commands/graph.md"
      via: "drives the SHIPPED surfaces through every GDH acceptance scenario + the REAL b2 0 -> N count"
      pattern: "runDerivation|resolveRoomRoot|extractDocText|produceCandidates"
    - from: "tests/test-graph-derivation-verdict.cjs"
      to: "lib/core/lazygraph-ops.cjs _indexArtifactBody"
      via: "the MEDIUM-4 check asserts no derivation-owned cascade type is raw-INSERTed in the indexer"
      pattern: "INSERT INTO edges|CONTRADICTS"
    - from: "tests/test-sqlite-battle.cjs"
      to: "lib/core/lazygraph-ops.cjs indexArtifact"
      via: "BATTLE-03/04 assert the structural index writes BELONGS_TO but not the disabled cascade edges (INFORMS/CONTRADICTS) post-D-169-08"
      pattern: "BELONGS_TO|INFORMS|CONTRADICTS"
    - from: "tests/test-169-brain-boundary.cjs"
      to: "lib/core/graph-candidate-producer.cjs + lib/core/graph-derivation.cjs + scripts/gsd-graph-derive-sweep.cjs + scripts/gsd-graph-derive-drain.cjs + scripts/brain-derive-command.cjs"
      via: "forbidden-substring sweep proving zero user-content reaches a Brain query"
      pattern: "brain-derive-command|graph-candidate-producer"
    - from: "tests/run-all-169.sh"
      to: "tests/test-graph-derivation-verdict.cjs + tests/test-edges-part4-cascade-floor.cjs + tests/test-sqlite-battle.cjs"
      via: "the phase gate runs the verdict + the carried 168 floor test + the carried migrated sqlite-battle regression fence"
      pattern: "test-graph-derivation-verdict|test-edges-part4-cascade-floor|test-sqlite-battle"
---

<objective>
WAVE 5 VERIFY (harness-as-code property 6). Ship the adversarial structured {passed, findings[]} verdict
that proves GDH-01..07 + the canon guards BY INSTRUMENTATION, finalize the Part 8 boundary sweep over the
five relevant surfaces, prove the b2-journey acceptance (typed-edge count 0 -> N) by count AGAINST THE REAL
FIXTURE (non-skippable when present, recording the actual N + edge types in the SUMMARY -- HIGH-2), include
the MEDIUM-4 sole-cascade-writer check, MIGRATE the committed tests/test-sqlite-battle.cjs to the
post-MEDIUM-4 reality and carry it as a regression fence, and finalize tests/run-all-169.sh as the single
PASS/FAIL phase gate (all suites + the verdict + Part 8 + the carried Phase 168 floor test + the carried
migrated battle fence + an em-dash sweep). The Verify wave must not start until Plan 05's barrier (the
backfill + idempotence + sweep/drain legs green) passes; the orchestrator inspects this barrier last.

The migrate-and-fence task closes the BLOCKER the plan-check found: disabling the legacy _indexArtifactBody
cascade in Plan 04 (Wave 3) makes test-sqlite-battle.cjs BATTLE-03 (indexArtifact creates INFORMS from a
[[wikilink]]) and BATTLE-04 (CONTRADICTS near a wikilink) assert behavior that no longer exists, and that
committed test was in NO run-all aggregator so the break was invisible to the phase gate. This plan owns
run-all-169.sh finalization and runs AFTER the Wave-3 disable, so the migration + the carried-fence
registration belong here (clean file ownership; test-sqlite-battle.cjs is touched by no other plan).

Purpose: turn test-169-brain-boundary.cjs (RED from Plan 01) GREEN, add the verdict, prove the real b2
moat goes 0 -> N, migrate + carry the battle test, and lock the gate.
Output: tests/test-graph-derivation-verdict.cjs + the finalized boundary sweep + the migrated
tests/test-sqlite-battle.cjs + the finalized run-all-169.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-CONTEXT.md
@.planning/phases/169-graph-derivation-harness/169-VALIDATION.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-04-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-05-PLAN.md
@tests/test-harness-167-verdict.cjs
@tests/test-harness-167-part8-leak.cjs
@tests/test-sqlite-battle.cjs
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract. This plan CONSUMES every shipped surface (resolveRoomRoot,
extractDocText, produceCandidates, candidateToFinding, runDerivation, rollupSubRooms, the --derive backfill,
the Stop sweep hook, the SessionStart drain hook) and proves them.
</shared_iface_contract>

<rules>
- Harness-as-code property 6: the verdict proves BY INSTRUMENTATION (a record(check, passed, detail)
  accumulator returning {passed, findings[]}), never by prose. Mirror tests/test-harness-167-verdict.cjs.
- D-169-08 (HIGH-2): the b2 acceptance proof runs against the REAL fixture. When the b2 path is PRESENT the
  0 -> N check is NON-SKIPPABLE and the actual N + derived edge types are recorded in the SUMMARY. The
  synthetic two-artifact room is the CI-determinism fallback ONLY when the real fixture is absent -- never
  the sole evidence for GDH-06.
- D-169-08 (MEDIUM-4): the verdict asserts NO derivation-owned cascade edge type (CONTRADICTS / INFORMS /
  ENABLES / INVALIDATES) is written by a raw INSERT in the indexer path (lazygraph-ops.cjs); derivation via
  navigation.writeEdge is the sole cascade writer.
- D-169-08 (MEDIUM-4 migration): tests/test-sqlite-battle.cjs BATTLE-03/04 are MIGRATED to assert the
  post-disable reality (indexArtifact writes BELONGS_TO, does NOT write the legacy cascade edges
  INFORMS/CONTRADICTS); the migrated test is CARRIED in run-all-169.sh as a regression fence (mirroring the
  carried Phase 168 floor test). Do NOT re-enable the cascade to make the old assertions pass; migrate the
  assertions to the new reality.
- Part 8: the boundary sweep is comment-filtered (grep -v '^#') with a sharp-scan self-test (a planted
  artifact-body token must make it go RED then GREEN); never a bare ==0 on an unfiltered file. The sweep
  covers graph-candidate-producer.cjs (the LLM producer reads LOCAL text and must not leak it to Brain).
- Part 4/9: the verdict asserts edge_type in the frozen set + review_status on the NODE (Pitfall 1) + the
  producer emits both CONTRADICTS and CONVERGES; the carried 168 floor test stays green; NO new edge type
  minted this phase.
- The phase gate prints Total/Passed/Failed and exits non-zero on any failure.
- A self-defect the verdict catches in its own first draft (e.g. a literal em-dash in a detector constant)
  is fixed in-wave and reported as a finding (the FINDING-167-05-01 / FINDING-163-06-01 pattern).
- NO em-dashes. CJS / bash only. No new deps.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: the adversarial structured verdict (real b2 + MEDIUM-4) + the finalized Part 8 sweep</name>
  <read_first>
    - tests/test-harness-167-verdict.cjs (the record(check,passed,detail) accumulator shape to mirror) +
      tests/test-harness-167-part8-leak.cjs (the surface-sweep + sharp-scan self-test pattern)
    - lib/core/graph-derivation.cjs (runDerivation + rollupSubRooms + candidateToFinding),
      lib/core/graph-candidate-producer.cjs (produceCandidates), lib/core/room-root.cjs,
      lib/core/doc-text-extractor.cjs, commands/graph.md --derive (the surfaces to drive)
    - lib/core/lazygraph-ops.cjs _indexArtifactBody (the MEDIUM-4 target: assert no raw cascade INSERT)
    - 169-RESEARCH.md Pitfall 1 (review_status node column) + Pitfall 4 (brain-derive boundary) + the
      Acceptance section (b2 0 -> N) + 169-CONTEXT.md D-169-06/07/08 + 169-VALIDATION.md Part 8 row + the
      real-b2 manual-verify gate row + the manual-only verifications
    - the b2 fixture path: ~/MindrianRooms/motj-ecosystem/sub-rooms/jonathan-contractor-motj/b2-journey
  </read_first>
  <action>
    Create tests/test-graph-derivation-verdict.cjs: a record(check, passed, detail) accumulator returning
    {passed, checks, failed, findings} that drives the SHIPPED surfaces and proves -- GDH-01 (resolveRoomRoot
    returns the sub-room dir for a sub-room file while the active room is the parent), GDH-03 (rollupSubRooms
    ATTACH UNION sees a sub-room edge; the parent db row count is unchanged), GDH-04 (extractDocText > 0 runs
    on the fixture, source bytes unchanged), GDH-05 (produceCandidates emits BOTH a CONTRADICTS AND a
    CONVERGES candidate; runDerivation bridges via candidateToFinding and lands a proposed NODE + typed edge;
    fable-mode drops an unjustified CONTRADICTS; NO edge.properties.review_status), GDH-06 (the backfill
    drives typed-edge count 0 -> N: when the REAL b2 path exists run it NON-SKIPPABLY and record the actual N
    + the derived edge types via a finding detail string the SUMMARY captures; otherwise run the synthetic
    two-artifact fallback room), GDH-07 (a second runDerivation mints no duplicate proposed node; a confirmed
    node untouched), the MEDIUM-4 sole-cascade-writer check (grep lazygraph-ops.cjs _indexArtifactBody for a
    raw INSERT INTO edges of CONTRADICTS/INFORMS/ENABLES/INVALIDATES -> assert NONE remain), Part 4 (every
    derived edge_type is in the frozen ALLOWED_EDGE_TYPES set), Part 9 (review_status on the NODE), and a
    no-em-dash self-check. Finalize tests/test-169-brain-boundary.cjs as the Part 8 forbidden-substring sweep
    over graph-derivation.cjs + graph-candidate-producer.cjs + scripts/gsd-graph-derive-sweep.cjs +
    scripts/gsd-graph-derive-drain.cjs + scripts/brain-derive-command.cjs (comment-filtered, with a
    sharp-scan self-test that plants an artifact-body token and proves the sweep goes RED then GREEN). No
    em-dashes.
  </action>
  <verify>
    <automated>node tests/test-graph-derivation-verdict.cjs && node tests/test-169-brain-boundary.cjs</automated>
  </verify>
  <done>The verdict returns {passed:true} proving GDH-01..07 + the canon guards + the MEDIUM-4
  sole-cascade-writer check + the b2 0 -> N acceptance by instrumentation (against the REAL fixture when
  present, recording N + edge types in the SUMMARY); the Part 8 sweep returns 0 forbidden matches over all
  five surfaces with a working sharp-scan self-test.</done>
</task>

<task type="auto">
  <name>Task 2: migrate test-sqlite-battle.cjs to the post-MEDIUM-4 reality (carried regression fence)</name>
  <read_first>
    - tests/test-sqlite-battle.cjs BATTLE-03 :79-87 (asserts indexArtifact creates an INFORMS edge from a
      [[wikilink]]) + BATTLE-04 :89-97 (asserts a CONTRADICTS edge near a wikilink) -- both assert the legacy
      cascade behavior that D-169-08 / Plan 04 Task 1e disables; BATTLE-07 :126-133 (rebuildGraph
      consistency) for the BELONGS_TO structural expectation
    - lib/core/lazygraph-ops.cjs _indexArtifactBody :363-365 (the BELONGS_TO write that STAYS) + :386-413
      (the disabled cascade writes of CONTRADICTS/INFORMS/ENABLES/INVALIDATES per Plan 04)
    - 169-CONTEXT.md D-169-08 (the legacy raw cascade is disabled so derivation is the sole cascade writer)
    - tests/run-all-169.sh (the carried Phase 168 floor test registration is the pattern to mirror for the
      carried battle fence)
  </read_first>
  <behavior>
    - BATTLE-03 asserts indexArtifact on a [[wikilink]] artifact creates the structural BELONGS_TO edge and
      does NOT create an INFORMS edge (the structural index no longer writes the cascade; derivation does)
    - BATTLE-04 asserts indexArtifact on a contradiction-near-wikilink artifact does NOT create a
      CONTRADICTS edge from the structural index (derivation is now the sole cascade source)
    - the rest of the battle suite (BATTLE-01/02/05..12) stays unchanged and green
    - test-sqlite-battle.cjs is registered in run-all-169.sh as a carried regression fence
  </behavior>
  <action>
    Migrate tests/test-sqlite-battle.cjs BATTLE-03 and BATTLE-04 to the post-disable reality. For BATTLE-03:
    after indexArtifact on the [[wikilink]] artifact, assert the structural BELONGS_TO edge exists for that
    artifact (SELECT FROM edges WHERE source = <artifact id> AND type = 'BELONGS_TO') and assert NO INFORMS
    edge was created by the structural index (the INFORMS-from-wikilink count is now 0 -- the cascade moved
    to derivation per D-169-08). For BATTLE-04: after indexArtifact on the contradiction-near-wikilink
    artifact, assert NO CONTRADICTS edge was created by the structural index (count 0). Update the describe/it
    labels and assertion messages to name the new reality (structural index writes membership, derivation
    writes the cascade) and optionally add a one-line note that the derivation path is now the cascade
    source. Do NOT re-enable the legacy cascade to satisfy the old assertions; migrate the assertions. Then
    REGISTER tests/test-sqlite-battle.cjs in tests/run-all-169.sh as a CARRIED regression fence next to the
    carried Phase 168 floor test (same per-suite invoke + PASS/FAIL accumulation), so the phase gate proves
    the migration green and the break is no longer invisible. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-sqlite-battle.cjs && grep -q "test-sqlite-battle.cjs" tests/run-all-169.sh && echo BATTLE_MIGRATED_AND_CARRIED</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-sqlite-battle.cjs` exits 0 (BATTLE-03 asserts BELONGS_TO present + no INFORMS from the
      structural index; BATTLE-04 asserts no CONTRADICTS from the structural index; the other battle cases
      stay green).
    - `tests/run-all-169.sh` includes test-sqlite-battle.cjs as a carried regression fence (grep finds the
      reference and the suite runs inside the aggregator's PASS/FAIL accounting).
  </acceptance_criteria>
  <done>BATTLE-03/04 assert the post-MEDIUM-4 reality (BELONGS_TO yes, INFORMS/CONTRADICTS from the
  structural index no), the rest of the battle suite is unchanged and green, and test-sqlite-battle.cjs is a
  carried regression fence inside run-all-169.sh.</done>
</task>

<task type="auto">
  <name>Task 3: finalize run-all-169.sh as the single phase gate</name>
  <read_first>
    - tests/run-all-169.sh (the Wave-0 scaffold from Plan 01 to finalize)
    - tests/run-all-167.sh (the finalized-gate shape: per-suite invoke, Total/Passed/Failed, exit code,
      Part 8 sweep + em-dash sweep)
    - tests/test-edges-part4-cascade-floor.cjs (the carried Phase 168 floor test to keep registered + green)
    - tests/test-sqlite-battle.cjs (the carried migrated regression fence from Task 2 to keep registered)
  </read_first>
  <action>
    Finalize tests/run-all-169.sh: register every 169 suite (room-root-resolver, doc-text-extractor,
    subroom-rollup, candidate-producer, graph-derivation-loop, derive-idempotence, derive-backfill-
    acceptance, graph-derive-sweep, graph-derivation-verdict, 169-brain-boundary) plus the carried
    tests/test-edges-part4-cascade-floor.cjs AND the carried migrated tests/test-sqlite-battle.cjs regression
    fence, the Part 8 grep sweep, and the em-dash sweep over the 169 surfaces; accumulate PASS/FAIL; print
    "Total N, Passed N, Failed N"; exit non-zero on any failure. Confirm the carried 168 floor test passes
    (frozen set untouched) and the carried migrated battle fence passes. No em-dashes.
  </action>
  <verify>
    <automated>bash tests/run-all-169.sh && node tests/test-edges-part4-cascade-floor.cjs && node tests/test-sqlite-battle.cjs && echo PHASE_GATE_GREEN</automated>
  </verify>
  <done>bash tests/run-all-169.sh prints Total/Passed/Failed with Failed 0 and exit 0; the carried 168
  floor test is green; the carried migrated test-sqlite-battle.cjs fence is green; the gate is the single
  PASS/FAIL phase contract for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| derivation surfaces -> Brain | the verdict + sweep prove zero user-content egress (incl. the LLM producer) |
| verdict harness -> shipped code | the verdict drives real surfaces, not mocks; the b2 proof is the REAL fixture |
| indexer -> cascade edges | the verdict asserts derivation is the sole cascade writer (MEDIUM-4); the carried battle fence proves the structural index no longer writes the cascade |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-14 | Information Disclosure | a .docx body string / dossier name reaching a Brain query in brain-derive or the LLM producer | mitigate | the Part 8 sweep over graph-derivation.cjs + graph-candidate-producer.cjs + the sweep + drain hooks + brain-derive-command.cjs returns 0 forbidden matches; the LLM producer + LOCAL derivers never touch the wire (Pitfall 4) |
| T-169-15 | Tampering | a vacuously-passing verdict (false green), or the synthetic room masking a real b2 failure | mitigate | the verdict drives the SHIPPED surfaces by instrumentation + the REAL b2 0 -> N count (non-skippable when present, N + edge types recorded in the SUMMARY); the sharp-scan self-test proves the Part 8 sweep can go RED |
| T-169-16 | Tampering | a new edge type silently minted, breaking the frozen-set floor | mitigate | the carried Phase 168 floor test stays registered + green; the verdict asserts edge_type membership in ALLOWED_EDGE_TYPES |
| T-169-18 | Tampering | two cascade writers (legacy raw + derivation) both writing CONTRADICTS / INFORMS | mitigate | the verdict's MEDIUM-4 check asserts no derivation-owned cascade type is raw-INSERTed in the indexer path; the carried migrated battle fence asserts indexArtifact writes BELONGS_TO but no INFORMS/CONTRADICTS; derivation is the sole cascade writer (D-169-08) |
| T-169-19 | Tampering | the cascade-disable silently breaking a committed test outside any aggregator (the invisible-break the plan-check found) | mitigate | test-sqlite-battle.cjs is migrated to the post-disable reality AND carried in run-all-169.sh so the phase gate proves it green; the break can no longer hide |
| T-169-SC | Tampering | npm installs | mitigate | zero new packages; no install task |
</threat_model>

<verification>
- node tests/test-graph-derivation-verdict.cjs returns {passed:true} (GDH-01..07 + canon guards + MEDIUM-4 +
  the REAL b2 0 -> N, recording N + edge types in the SUMMARY).
- node tests/test-169-brain-boundary.cjs returns 0 forbidden matches over all five surfaces with a working
  self-test.
- node tests/test-sqlite-battle.cjs exits 0 (BATTLE-03/04 migrated to the post-MEDIUM-4 reality).
- bash tests/run-all-169.sh prints Total/Passed/Failed, Failed 0, exit 0; the carried 168 floor test green;
  the carried migrated sqlite-battle fence green.
- No em-dashes anywhere.
</verification>

<success_criteria>
The harness is proven end to end: the adversarial structured verdict shows GDH-01..07 + the canon guards +
the sole-cascade-writer disposition honored by instrumentation, the REAL b2-journey moat goes 0 -> N (with
N + edge types recorded in the SUMMARY), Part 8 is clean across all five surfaces, the committed
test-sqlite-battle.cjs is migrated to the post-MEDIUM-4 reality and carried as a regression fence (the
invisible break closed), and run-all-169.sh is the single PASS/FAIL phase gate. The typed-edge moat is
self-wiring.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-06-SUMMARY.md` when done. Record the actual b2
0 -> N typed-edge count + the derived edge types in the SUMMARY (D-169-08, HIGH-2).
</output>
</output>
