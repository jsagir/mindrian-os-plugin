---
phase: 169-graph-derivation-harness
plan: 04
type: execute
wave: 3
depends_on: ["169-02", "169-03"]
files_modified:
  - lib/core/graph-derivation.cjs
  - lib/core/lazygraph-ops.cjs
autonomous: true
requirements: [GDH-02, GDH-03, GDH-05]
canon_parts: [4, 6, 8, 9]

must_haves:
  truths:
    - "lib/core/graph-derivation.cjs exports runDerivation({roomDir, runChain, selfCritiqueFn, deriveFn}) -- a runChain composer where each step is material:true (so fable-mode always critiques), invokes a LOCAL deriver (findings-wirer primary), and writes ONLY critique-passed candidates as a PROPOSED truth-claim NODE (review_status='proposed' on the NODE via the findings-wirer precedent) plus a typed EDGE via navigation.writeEdge (GDH-05, D-169-04)"
    - "the derived edge carries review_status on the NODE, NEVER as an edge column (Pitfall 1); the edge properties are enum/scalar only (Part 8); the edge_type is drawn ONLY from the frozen ALLOWED_EDGE_TYPES set (Part 4, complete post-168)"
    - "fable-mode (167 selfCritiqueFn) rejects an unjustified candidate (e.g. a bad CONTRADICTS) so it never lands -- a {passed:false} verdict on a material step drops the candidate, mirroring the Phase 167 seam"
    - "lazygraph-ops.cjs rebuildGraph (:462) RECURSES into sub-rooms: it walks each sub-room (detected by a child carrying `.room-root`, the heal-command:909-921 precedent) and indexes each into THAT sub-room's own room.db -- per-sub-room db, never a merged parent db (GDH-03, D-169-02, Part 8 isolation)"
    - "graph-derivation.cjs exports rollupSubRooms(parentRoomDir) -- a READ-SIDE ATTACH (node:sqlite ATTACH DATABASE read-only) UNION across each registered sub-room's room.db so the parent SEES sub-room edges WITHOUT merging rows; the parent db is never written sub-room rows (GDH-03 parent rollup)"
    - "lazygraph-ops.cjs _indexArtifactBody (:341, fs.readFileSync :342) and the .md-only filter (:487-489) are widened to route .docx/.html through doc-text-extractor.extractDocText so non-.md content is indexed (GDH-04 reach into the index path); the .md-only readdir filter now includes .docx/.html"
    - "every derived proposed node carries a STABLE content/source-hash id (the Artifact content_hash from _indexArtifactBody:348-352 is the dedupe seed) so a re-run does not re-mint it (sets up GDH-07, finished in Plan 05)"
  artifacts:
    - path: "lib/core/graph-derivation.cjs"
      provides: "runDerivation runChain composer (the 4-deriver wiring) + rollupSubRooms read-side ATTACH; writes proposed node + typed edge via navigation.writeEdge"
      min_lines: 120
      exports: ["runDerivation", "rollupSubRooms"]
    - path: "lib/core/lazygraph-ops.cjs"
      provides: "rebuildGraph sub-room recursion + the non-.md reach (extractDocText branch in _indexArtifactBody + widened .md-only filter)"
      contains: "extractDocText"
  key_links:
    - from: "lib/core/graph-derivation.cjs"
      to: "lib/core/navigation.cjs writeEdge + lib/core/findings-wirer.cjs"
      via: "proposed truth-claim NODE then typed EDGE; the findings-wirer proposed-node precedent, not a fork"
      pattern: "writeEdge|findings-wirer"
    - from: "lib/core/graph-derivation.cjs"
      to: "lib/core/chain-executor.cjs runChain selfCritiqueFn"
      via: "material:true steps + fable-mode critique gating each candidate before it lands"
      pattern: "runChain|selfCritiqueFn"
    - from: "lib/core/lazygraph-ops.cjs"
      to: "lib/core/doc-text-extractor.cjs"
      via: "_indexArtifactBody routes .docx/.html through extractDocText"
      pattern: "extractDocText"
    - from: "lib/core/graph-derivation.cjs rollupSubRooms"
      to: "sub-room room.db files"
      via: "node:sqlite ATTACH DATABASE read-only UNION; never merge rows into the parent"
      pattern: "ATTACH"
---

<objective>
WAVE 3 SURFACES (GDH-02/03/05). Land the runChain derivation composer and the sub-room + non-.md reach
extensions to lazygraph-ops. This is the core of the harness: lib/core/graph-derivation.cjs wires the four
SHIPPED derivers into runChain (D-169-04, no fork), fable-mode-critiques each candidate (167), and writes
critique-passed candidates as a PROPOSED truth-claim NODE plus a typed EDGE via the navigation.cjs
chokepoint (Part 9, the findings-wirer proposed-node precedent -- NOT an edge review_status column).
rebuildGraph gains sub-room recursion (per-sub-room db) + a parent read-side ATTACH rollup (GDH-03), and
_indexArtifactBody routes .docx/.html through extractDocText (GDH-04 reach into the index). This closes
root causes #2, #3, and #4.

Purpose: turn test-graph-derivation-loop.cjs + test-subroom-rollup.cjs (RED from Plan 01) GREEN.
Output: lib/core/graph-derivation.cjs + the lazygraph-ops sub-room/non-.md extensions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-02-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-03-PLAN.md
@lib/core/lazygraph-ops.cjs
@lib/core/findings-wirer.cjs
@lib/core/chain-executor.cjs
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract. This plan IMPLEMENTS:
`runDerivation({roomDir, runChain, selfCritiqueFn, deriveFn}) -> {proposedNodes, edges, trace}` and
`rollupSubRooms(parentRoomDir) -> {edges}`. It CONSUMES resolveRoomRoot (Plan 02) and extractDocText
(Plan 03).
</shared_iface_contract>

<rules>
- Part 9: derived edges land via a PROPOSED truth-claim NODE (review_status on the NODE). NEVER write
  review_status onto the edge (Pitfall 1 -- no insights query reads edge properties). Reuse
  findings-wirer.cjs wireAccept/wireReject/wireDefer (D-169-04); do not fork the proposed-node lifecycle.
- Part 8: derivation is LOCAL. The 3 LOCAL derivers (findings-wirer, proactive-intelligence,
  cross-room-detect-within-room) never touch the wire. brain-derive is wired in Plan 05/06 boundary-scan
  scope only; this composer adds NO new Brain wire. cross-room TYPED edges stay DEFERRED (Phase 83) -- use
  cross-room-detect only for within-room convergence signals or exclude it (RESEARCH Open Q2).
- Part 4: edge_type ONLY from the frozen ALLOWED_EDGE_TYPES set; edge properties enum/scalar only.
- Part 3/9 gate: derivation HALTS at a Decision Gate -- it lands proposed and NEVER auto-confirms.
- GDH-03: read-side ATTACH only; NEVER copy sub-room rows into the parent db (Part 8 room isolation).
- All graph writes via navigation.cjs (Phase 109 pre-commit substrate guard); NO raw INSERT INTO edges.
- fable-mode: mark derivation steps material:true so the critic always fires; a thrown critic fails OPEN.
- NO em-dashes. CJS only. No new deps.
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lazygraph-ops sub-room recursion + non-.md reach</name>
  <read_first>
    - lib/core/lazygraph-ops.cjs rebuildGraph :462 (sections-only walk :474-499), _indexArtifactBody :341
      (fs.readFileSync :342, content_hash :348-352), the .md-only filter :487-489
    - scripts/heal-command.cjs:909-921 (the sub-rooms-container detection precedent: each child carrying
      `.room-root`)
    - lib/core/doc-text-extractor.cjs extractDocText (Plan 03)
    - tests/test-subroom-rollup.cjs (the RED stub for rollup; rebuild recursion is exercised via the
      acceptance test in Plan 05/06)
  </read_first>
  <behavior>
    - rebuildGraph on a room with sub-rooms indexes each sub-room into ITS OWN room.db (not the parent)
    - the readdir filter now includes .docx and .html (alongside .md, still excluding STATE.md/ROOM.md)
    - _indexArtifactBody on a .docx/.html file reads its text via extractDocText, not fs.readFileSync
  </behavior>
  <action>
    In lib/core/lazygraph-ops.cjs: (a) widen the .md-only readdir filter (:487-489) to also accept
    `.docx`/`.html` (keep the STATE.md/ROOM.md exclusions). (b) In _indexArtifactBody, before the
    fs.readFileSync content read, branch on path.extname: for .docx/.html call
    require('./doc-text-extractor.cjs').extractDocText(filePath) and use that as the content (preserve the
    content_hash computation over the extracted text); .md stays the existing readFileSync path. (c) In
    rebuildGraph, after the sections walk, detect sub-rooms (children of roomDir carrying a `.room-root`
    file, the heal-command precedent) and recurse: open each sub-room's OWN room.db via room-db.openRoomDb
    and index that sub-room's sections into it -- never into the parent conn. Keep all existing rebuild
    behavior for the .md sections path. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-subroom-rollup.cjs && grep -q extractDocText lib/core/lazygraph-ops.cjs && node -e "require('./lib/core/lazygraph-ops.cjs')" && echo LAZYGRAPH_OK</automated>
  </verify>
  <done>The readdir filter includes .docx/.html, _indexArtifactBody routes non-.md through extractDocText,
  rebuildGraph recurses sub-rooms into per-sub-room dbs, and lazygraph-ops still loads. The rollup stub
  passes (rollupSubRooms is delivered in Task 2; this task delivers the per-sub-room index side).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: graph-derivation.cjs composer (runDerivation + rollupSubRooms)</name>
  <read_first>
    - lib/core/chain-executor.cjs runChain :295 (six-callback contract) + _applySelfCritique :213
      (material-step fable-mode seam) + RESEARCH Code Example 3 (the derivation-loop shape)
    - lib/core/findings-wirer.cjs wireAccept/wireReject/wireDefer (proposed EvidenceClaim NODE + typed edge
      via navigation.cjs, caller-owned db handle, LOCAL-only)
    - lib/core/navigation.cjs writeEdge (:113) + confirmNode + RESEARCH Code Example 2 (the writeEdge
      contract: source_id/target_id/edge_type/properties, ON CONFLICT idempotent)
    - lib/core/navigation/edges.cjs frozen ALLOWED_EDGE_TYPES (the 7 phase edges present)
    - 169-RESEARCH.md Pitfall 1 (review_status is a NODE column) + Pitfall 3 (stable proposed-node id) +
      Open Q2 (deriver classification)
    - tests/test-graph-derivation-loop.cjs (the RED stub this turns GREEN)
  </read_first>
  <behavior>
    - runDerivation writes a proposed NODE (review_status='proposed') + a typed EDGE for each
      critique-passed candidate
    - a selfCritiqueFn returning {passed:false} for an unjustified candidate drops it (no node, no edge)
    - the proposed-node id is a stable content/source-hash (re-run does not duplicate it)
    - rollupSubRooms ATTACHes each sub-room db read-only and returns the UNION of sub-room edges without
      writing the parent db
  </behavior>
  <action>
    Create lib/core/graph-derivation.cjs. runDerivation({roomDir, runChain, selfCritiqueFn, deriveFn}):
    build runChain steps (one per section/deriver pass) each `material:true`; onStep invokes the LOCAL
    deriver (default deriveFn delegating to findings-wirer's candidate-building, injectable for tests) and
    returns {chain_output: candidates, quality}; selfCritiqueFn is the fable-mode critic (defaults to a
    no-op {passed:true}). After a passing critique, for each candidate write a PROPOSED truth-claim NODE via
    the findings-wirer / navigation typed-claim writer (review_status='proposed', id = a stable
    sha256 over the source artifact content_hash + edge semantics) THEN a typed EDGE via
    navigation.writeEdge (edge_type from the frozen set, properties enum/scalar only). Return
    {proposedNodes, edges, trace}. rollupSubRooms(parentRoomDir): enumerate registered sub-rooms (the
    `.rooms/registry.json` + the `.room-root` walk via resolveRoomRoot), open the parent room.db, ATTACH
    each sub-room's room.db read-only, UNION-select edges across them, DETACH, and return {edges} WITHOUT
    writing the parent db. Use cross-room-detect only for within-room convergence signals (RESEARCH Open
    Q2); cross-room typed edges stay deferred. No raw edge SQL. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-graph-derivation-loop.cjs && node tests/test-subroom-rollup.cjs && bash tests/run-all-169.sh 2>&1 | tail -5</automated>
  </verify>
  <done>test-graph-derivation-loop.cjs passes: a proposed NODE + typed edge land for a good candidate, an
  unjustified candidate is dropped by fable-mode, no edge carries review_status, and re-mint uses a stable
  id. test-subroom-rollup.cjs passes (ATTACH UNION, parent rows unchanged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| deriver candidate -> graph write | a derived edge crosses into the room graph; must be critiqued + proposed |
| sub-room room.db -> parent rollup | a read crosses the room boundary; must stay read-only ATTACH |
| brain-derive deriver -> Brain | the one Brain-touching deriver; scoped to Plan 06 boundary scan |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-07 | Tampering | a bad CONTRADICTS edge misleads the navigator | mitigate | fable-mode selfCritiqueFn rejects unjustified candidates (material:true so it always fires); every edge lands PROPOSED, human confirms (Part 3/9) |
| T-169-08 | Information Disclosure | review_status written on the edge so it never surfaces for confirm | mitigate | proposed NODE pattern (findings-wirer); the test asserts no edge.properties.review_status |
| T-169-09 | Information Disclosure | the rollup copying sub-room rows into the parent db (room-boundary blur) | mitigate | read-side ATTACH only; rollupSubRooms never writes the parent; cross-room typed edges deferred (Phase 83) |
| T-169-10 | Tampering | a raw INSERT INTO edges bypassing the chokepoint | mitigate | all writes via navigation.writeEdge; the Phase 109 pre-commit substrate guard rejects a new direct room-db require |
| T-169-SC | Tampering | npm installs | mitigate | zero new packages; no install task |
</threat_model>

<verification>
- node tests/test-graph-derivation-loop.cjs GREEN; node tests/test-subroom-rollup.cjs GREEN.
- grep confirms graph-derivation.cjs writes via navigation.writeEdge (no raw `INSERT INTO edges`).
- bash tests/run-all-169.sh shows the derivation + rollup legs GREEN (backfill/idempotence/boundary still
  RED until Plan 05/06).
</verification>

<success_criteria>
The runChain derivation composer writes fable-mode-critiqued PROPOSED typed edges via the chokepoint; the
indexer reaches .docx/.html; rebuild recurses sub-rooms into per-sub-room dbs; the parent rollup is a
read-side ATTACH. Root causes #2, #3, #4 closed at the composition layer.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-04-SUMMARY.md` when done.
</output>
