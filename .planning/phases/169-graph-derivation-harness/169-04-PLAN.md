---
phase: 169-graph-derivation-harness
plan: 04
type: execute
wave: 4
depends_on: ["169-00", "169-02", "169-03"]
files_modified:
  - lib/core/graph-candidate-producer.cjs
  - lib/core/graph-derivation.cjs
  - lib/core/lazygraph-ops.cjs
autonomous: true
requirements: [GDH-02, GDH-03, GDH-04, GDH-05, D-169-11]
canon_parts: [4, 6, 8, 9]

must_haves:
  truths:
    - "lib/core/graph-candidate-producer.cjs exports produceCandidates({roomDir, artifactPair, llm}) -- the LLM DERIVATION PRODUCER that reads artifact-pair text (incl. extracted .docx/.html) and PROPOSES candidate {source, target, edge_type, reason} tuples drawn ONLY from the frozen cascade subset, and MUST be able to emit BOTH CONTRADICTS AND CONVERGES (D-169-06: not CONTRADICTS-only). This is the missing FUEL the four shipped derivers never generate. llm is injectable (stub in tests). It reads LOCAL text + returns LOCAL tuples and NEVER calls the Brain (Part 8)"
    - "lib/core/graph-derivation.cjs exports runDerivation({roomDir, runChain, selfCritiqueFn, deriveFn}) -- a runChain composer where deriveFn IS the produceCandidates producer (default, injectable), each step is material:true (so fable-mode critiques EACH candidate), and critique-passed candidates flow through the named candidate->finding adapter candidateToFinding(candidate) into findings-wirer, which writes a PROPOSED truth-claim NODE (review_status='proposed' on the NODE) plus a typed EDGE via navigation.writeEdge (GDH-05, D-169-04 writer + D-169-06 producer/adapter split)"
    - "the derived edge carries review_status on the NODE, NEVER as an edge column (Pitfall 1); the edge properties are enum/scalar only (Part 8); the edge_type is drawn ONLY from the frozen ALLOWED_EDGE_TYPES set (Part 4, complete post-168)"
    - "CONVERGES within-room is produced by the LLM producer (D-169-06, LOW-6 DECIDED here, not deferred to the executor); cross-room-detect is EXCLUDED (cross-room typed edges stay deferred to Phase 83)"
    - "fable-mode (167 selfCritiqueFn) rejects an unjustified candidate (e.g. a bad CONTRADICTS) so it never lands -- a {passed:false} verdict on a material step drops the candidate, mirroring the Phase 167 seam"
    - "lazygraph-ops.cjs rebuildGraph (:462) gains a ROOT-FILES pass: alongside the discoverSections(roomDir).all section walk it ALSO sweeps the room ROOT for top-level .md/.docx/.html artifacts and indexes them, so a flat (sub-)room whose artifacts sit in the ROOT (the b2 sub-room: 39 artifacts in the root, not in section subfolders) is reached (GDH-03/04, D-169-07 -- general fix, not fixture-prep)"
    - "lazygraph-ops.cjs rebuildGraph ALSO recurses TRANSITIVELY into sub-rooms AT ARBITRARY DEPTH (children carrying `.room-root`, then their children, the heal-command:909-921 precedent) indexing each into THAT sub-room's own room.db -- per-sub-room db, never a merged parent db (GDH-03, D-169-02, D-169-11 fractal teeth, Part 8 isolation); a sub-sub-room is rebuilt into its OWN db, not the grandparent's"
    - "graph-derivation.cjs exports rollupSubRooms(parentRoomDir) -- a RECURSIVE, TRANSITIVE read-side ATTACH (node:sqlite ATTACH DATABASE read-only) that walks the NESTED_WITHIN child links (Plan 00) of parentRoomDir AND recurses into each child's own sub-rooms, so the parent SEES edges from sub-rooms AND sub-sub-rooms (arbitrary depth, D-169-11) WITHOUT merging rows; the parent db is never written sub-room rows; the read is idempotently re-applied at each nesting level (GDH-03 parent rollup made fractal)"
    - "lazygraph-ops.cjs _indexArtifactBody (:341) is widened to route .docx/.html through doc-text-extractor.extractDocText (GDH-04 reach into the index path); the legacy raw-SQL cascade in _indexArtifactBody (:386-413, the raw INSERT INTO edges writing CONTRADICTS/INFORMS/ENABLES/INVALIDATES, bypassing the chokepoint and auto-confirming) is DISABLED so derivation (via navigation.writeEdge + proposed nodes) is the SOLE cascade writer (MEDIUM-4 disposition, D-169-08)"
    - "every derived proposed node carries a STABLE content/source-hash id (the Artifact content_hash from _indexArtifactBody:348-352 is the dedupe seed) so a re-run does not re-mint it (sets up GDH-07, finished in Plan 05)"
  artifacts:
    - path: "lib/core/graph-candidate-producer.cjs"
      provides: "produceCandidates -- the LLM candidate-edge producer (the missing fuel); emits both CONTRADICTS + CONVERGES; frozen-cascade-subset only; LOCAL text in, LOCAL tuples out, never the Brain"
      min_lines: 60
      exports: ["produceCandidates"]
    - path: "lib/core/graph-derivation.cjs"
      provides: "runDerivation runChain composer (deriveFn = the producer; candidateToFinding adapter; findings-wirer writer) + rollupSubRooms RECURSIVE read-side ATTACH walking NESTED_WITHIN at arbitrary depth; writes proposed node + typed edge via navigation.writeEdge"
      min_lines: 130
      exports: ["runDerivation", "rollupSubRooms", "candidateToFinding"]
    - path: "lib/core/lazygraph-ops.cjs"
      provides: "rebuildGraph ROOT-FILES pass + TRANSITIVE sub-room recursion (arbitrary depth) + the non-.md reach (extractDocText branch in _indexArtifactBody + widened filter) + the disabled legacy raw cascade"
      contains: "extractDocText"
  key_links:
    - from: "lib/core/graph-derivation.cjs"
      to: "lib/core/graph-candidate-producer.cjs produceCandidates"
      via: "deriveFn defaults to the LLM producer; onStep invokes it on an artifact pair"
      pattern: "produceCandidates"
    - from: "lib/core/graph-derivation.cjs candidateToFinding"
      to: "lib/core/findings-wirer.cjs + lib/core/navigation.cjs writeEdge"
      via: "the adapter turns a candidate tuple into a wireAccept-shaped finding; findings-wirer writes the proposed NODE then the typed EDGE; not a fork"
      pattern: "candidateToFinding|writeEdge|findings-wirer"
    - from: "lib/core/graph-derivation.cjs"
      to: "lib/core/chain-executor.cjs runChain selfCritiqueFn"
      via: "material:true steps + fable-mode critique gating each candidate before it lands"
      pattern: "runChain|selfCritiqueFn"
    - from: "lib/core/lazygraph-ops.cjs rebuildGraph"
      to: "the room ROOT + sub-room dirs (transitive)"
      via: "ROOT-FILES pass indexes top-level .md/.docx/.html; sub-room recursion indexes each level into its own db at arbitrary depth"
      pattern: "extractDocText|room-root"
    - from: "lib/core/graph-derivation.cjs rollupSubRooms"
      to: "sub-room + sub-sub-room room.db files (via NESTED_WITHIN walk)"
      via: "node:sqlite ATTACH DATABASE read-only UNION, recursing the NESTED_WITHIN child links at arbitrary depth; never merge rows into the parent"
      pattern: "ATTACH|NESTED_WITHIN"
---

<objective>
WAVE 4 SURFACES (GDH-02/03/04/05 + D-169-11 fractal teeth). Land the LLM candidate PRODUCER (the missing
fuel), the runChain derivation composer, the RECURSIVE sub-room + ROOT-FILES + non-.md reach extensions to
lazygraph-ops, and the TRANSITIVE rollupSubRooms that walks the NESTED_WITHIN lineage edge (Plan 00) at
arbitrary depth. This is the core of the harness.

The plan-check REVISE verdict found the engine had no fuel: the four shipped derivers only WRITE
pre-structured findings; none GENERATE candidate edges from raw artifact text, so the b2 fixture (flat-root
Hebrew .docx, no wikilinks) would never reach 0 -> N. A NEW lib/core/graph-candidate-producer.cjs is the LLM
derivation step that READS artifact-pair text and PROPOSES candidate {source, target, edge_type, reason}
tuples (BOTH CONTRADICTS AND CONVERGES, D-169-06), wired in as runChain's deriveFn; fable-mode (167)
critiques each candidate; survivors flow through the explicit named candidate->finding adapter
candidateToFinding into findings-wirer, which writes the PROPOSED truth-claim NODE + typed EDGE via the
navigation.cjs chokepoint (Part 9; findings-wirer WRITES, the LLM step PRODUCES, the adapter joins them --
D-169-04 still holds, no fork).

The 8-agent fan-out found that rollup was single-hop and nothing composed at arbitrary depth. This plan
makes the fractal hold (D-169-11): rebuildGraph recurses TRANSITIVELY (a sub-room's own sub-rooms each get
their own db), and rollupSubRooms walks the NESTED_WITHIN child links RECURSIVELY so a sub-sub-room's edges
reach the TOP-level parent rollup. rebuildGraph gains a ROOT-FILES pass (HIGH-3, D-169-07: index the room
ROOT's flat .md/.docx/.html so the b2 39-artifact flat root is reached) plus the non-.md reach
(_indexArtifactBody routes .docx/.html through extractDocText). The legacy raw-SQL cascade in
_indexArtifactBody is DISABLED so derivation is the sole cascade writer (MEDIUM-4, D-169-08). This closes
root causes #2, #3, #4 and gives the fractal its arbitrary-depth teeth at the index + rollup layer.

Purpose: turn test-candidate-producer.cjs + test-graph-derivation-loop.cjs + test-subroom-rollup.cjs +
test-recursive-rollup.cjs (RED from Plan 01) GREEN.
Output: lib/core/graph-candidate-producer.cjs + lib/core/graph-derivation.cjs (with TRANSITIVE
rollupSubRooms) + the lazygraph-ops ROOT-FILES / transitive-sub-room / non-.md extensions + the disabled
legacy raw cascade.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-CONTEXT.md
@.planning/phases/169-graph-derivation-harness/169-00-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-02-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-03-PLAN.md
@lib/core/lazygraph-ops.cjs
@lib/core/findings-wirer.cjs
@lib/core/chain-executor.cjs
@lib/core/llm-name-suggester.cjs
@lib/core/navigation/edges.cjs
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract. This plan IMPLEMENTS:
`produceCandidates({roomDir, artifactPair, llm}) -> [{source, target, edge_type, reason}]` (the LLM
producer),
`runDerivation({roomDir, runChain, selfCritiqueFn, deriveFn}) -> {proposedNodes, edges, trace}`,
`candidateToFinding(candidate)` (the adapter), and
`rollupSubRooms(parentRoomDir) -> {edges}` (RECURSIVE / TRANSITIVE, walking the NESTED_WITHIN lineage edge
from Plan 00). It CONSUMES resolveRoomRoot (Plan 02), extractDocText (Plan 03), and the NESTED_WITHIN frozen
edge type (Plan 00).
</shared_iface_contract>

<rules>
- the producer emits CONTRADICTS + CONVERGES, frozen-set only; the LLM derivation reads LOCAL text + writes
  LOCAL edges, never calls the Brain.
- the producer's default `llm` transport is the SHIPPED Part-8-legal LOCAL LLM pattern, mirroring
  lib/core/llm-name-suggester.cjs: a direct fetch to api.anthropic.com/v1/messages (the standard plugin LLM
  transport), NOT the Mindrian-owned Brain MCP. The posture is LOCAL artifact text in, anthropic-transport
  out, NEVER the Brain (Part 8). Tests inject a deterministic stub so no live model is touched.
- Part 9: derived edges land via a PROPOSED truth-claim NODE (review_status on the NODE). NEVER write
  review_status onto the edge (Pitfall 1). Reuse findings-wirer.cjs wireAccept/wireReject/wireDefer
  (D-169-04) as the WRITER; candidateToFinding is the adapter from a producer tuple to a finding; do not fork.
- Part 8: derivation is LOCAL. The LLM candidate producer + the LOCAL derivers never touch the Brain wire.
  cross-room-detect is EXCLUDED (cross-room TYPED edges stay DEFERRED, Phase 83); CONVERGES within-room comes
  from the LLM producer (LOW-6). brain-derive is boundary-scanned in Plan 06 only; this composer adds NO new
  Brain wire.
- Part 4: cascade edge_type ONLY from the frozen ALLOWED_EDGE_TYPES cascade subset; the rollup walks the
  frozen NESTED_WITHIN lineage edge (Plan 00); edge properties enum/scalar only.
- Part 3/9 gate: derivation HALTS at a Decision Gate -- it lands proposed and NEVER auto-confirms.
- GDH-03 / D-169-11: the rebuild recursion AND the rollup are TRANSITIVE at ARBITRARY DEPTH (a sub-sub-room
  rebuilds into its own db and rolls up to the top); read-side ATTACH only; NEVER copy sub-room rows into a
  parent db (Part 8 room isolation); idempotent re-apply at each level.
- D-169-07: the ROOT-FILES pass is a general fix for any flat room, NOT a b2-fixture hack.
- D-169-08 (MEDIUM-4): the legacy raw-SQL cascade in _indexArtifactBody (:386-413) is DISABLED so derivation
  via navigation.writeEdge is the SOLE writer of the cascade edge types (CONTRADICTS / INFORMS / ENABLES /
  INVALIDATES); the INFORMS wikilink write (:395) is part of the disabled set. BELONGS_TO (:363-365) stays.
- All graph writes via navigation.cjs (Phase 109 pre-commit substrate guard); NO raw INSERT INTO edges in
  the new modules.
- fable-mode: mark derivation steps material:true so the critic always fires; a thrown critic fails OPEN.
- NO em-dashes. CJS only. No new deps.
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lazygraph-ops ROOT-FILES pass + TRANSITIVE sub-room recursion + non-.md reach + disabled legacy cascade</name>
  <read_first>
    - lib/core/lazygraph-ops.cjs rebuildGraph :462 (sections-only walk :474-499), _indexArtifactBody :341
      (fs.readFileSync :342, content_hash :348-352), the .md-only filter :487-489, and the legacy raw-SQL
      cascade :386-413 (raw INSERT INTO edges writing CONTRADICTS at :390, INFORMS at :395, ENABLES at :403,
      INVALIDATES at :412, bypassing the chokepoint, auto-confirming -- the MEDIUM-4 target; all four cascade
      writes are disabled, the BELONGS_TO write at :363-365 stays)
    - scripts/heal-command.cjs:909-921 (the sub-rooms-container detection precedent: each child carrying
      `.room-root`)
    - lib/core/room-root.cjs resolveRoomRoot + findRoomRootSentinels (Plan 02)
    - lib/core/doc-text-extractor.cjs extractDocText (Plan 03)
    - lib/core/room-db.cjs openRoomDb (the per-sub-room db opener for recursion)
    - tests/test-subroom-rollup.cjs + tests/test-recursive-rollup.cjs (the RED stubs; the ROOT-FILES +
      recursion paths are exercised via the acceptance test in Plan 05/06 too)
  </read_first>
  <behavior>
    - rebuildGraph indexes top-level .md/.docx/.html artifacts that sit in the room ROOT (flat room), not
      only files inside section subfolders (D-169-07)
    - rebuildGraph on a room with sub-rooms indexes each sub-room into ITS OWN room.db, and recurses into a
      sub-room's OWN sub-rooms (arbitrary depth: a sub-sub-room is rebuilt into its own db, D-169-11)
    - the readdir filter (root pass and section pass) includes .docx and .html (alongside .md, still
      excluding STATE.md/ROOM.md)
    - _indexArtifactBody on a .docx/.html file reads its text via extractDocText, not fs.readFileSync
    - the legacy raw-SQL cascade in _indexArtifactBody no longer writes CONTRADICTS/INFORMS/ENABLES/
      INVALIDATES edges (derivation is the sole cascade writer); INFORMS is part of the disabled set
  </behavior>
  <action>
    In lib/core/lazygraph-ops.cjs: (a) widen the .md-only readdir filter (:487-489) to also accept
    `.docx`/`.html` (keep the STATE.md/ROOM.md exclusions). (b) In _indexArtifactBody, before the
    fs.readFileSync content read, branch on path.extname: for .docx/.html call
    require('./doc-text-extractor.cjs').extractDocText(filePath) and use that as the content (preserve the
    content_hash computation over the extracted text); .md stays the existing readFileSync path. (c) In
    rebuildGraph, ADD a ROOT-FILES pass: read roomDir's top-level entries and index each top-level
    .md/.docx/.html artifact (same exclusions) into the room's own conn, so a flat room whose artifacts sit
    in the ROOT is reached (D-169-07). Keep the existing discoverSections section walk. (d) In rebuildGraph,
    after the sections + root walk, detect sub-rooms (children of roomDir carrying a `.room-root` file, the
    heal-command precedent) and recurse TRANSITIVELY: open each sub-room's OWN room.db via room-db.openRoomDb,
    rebuild that sub-room's sections + root files into it, AND recurse into THAT sub-room's own sub-rooms
    (arbitrary depth, D-169-11) -- never into a parent conn. Guard against cycles (a visited-set of resolved
    absolute paths). (e) DISABLE the legacy raw-SQL cascade at :386-413: remove (or guard off) the raw
    `INSERT INTO edges` writes of CONTRADICTS (:390), INFORMS (:395), ENABLES (:403), and INVALIDATES (:412)
    from _indexArtifactBody; ALL FOUR cascade types are disabled, including the INFORMS wikilink write. Leave
    the BELONGS_TO structural edge (:363-365) intact. Add a short comment naming D-169-08 as the reason. No
    em-dashes.
  </action>
  <verify>
    <automated>node tests/test-subroom-rollup.cjs && node tests/test-recursive-rollup.cjs && grep -q extractDocText lib/core/lazygraph-ops.cjs && node -e "require('./lib/core/lazygraph-ops.cjs')" && ! grep -E "run\(id, *target(Id)?, *'(CONTRADICTS|INFORMS|ENABLES|INVALIDATES)'" lib/core/lazygraph-ops.cjs && echo LAZYGRAPH_OK</automated>
  </verify>
  <done>The readdir filter includes .docx/.html, _indexArtifactBody routes non-.md through extractDocText,
  rebuildGraph has a ROOT-FILES pass + recurses sub-rooms TRANSITIVELY into per-sub-room dbs at arbitrary
  depth (cycle-guarded), the legacy raw cascade writes of CONTRADICTS/INFORMS/ENABLES/INVALIDATES are all
  gone (BELONGS_TO intact), and lazygraph-ops still loads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: graph-candidate-producer.cjs (the LLM producer, the missing fuel)</name>
  <read_first>
    - 169-CONTEXT.md D-169-06 (the candidate-edge PRODUCER is an LLM derivation step; emits both edge
      families; reads LOCAL text; never calls the Brain)
    - lib/core/llm-name-suggester.cjs (THE shipped Part-8-legal LOCAL LLM transport precedent the default
      `llm` mirrors: _resolveProductionLlmClient :142-186 fetches api.anthropic.com/v1/messages directly with
      the x-api-key header + anthropic-version + AbortController timeout, the injectable opts.llmClient seam
      :64-68, and the header docstring :16-31 stating api.anthropic.com is the LOCAL transport, NOT the
      Mindrian-owned Brain)
    - lib/core/navigation/edges.cjs frozen ALLOWED_EDGE_TYPES (the cascade subset: CONTRADICTS, CONVERGES,
      INFORMS, INVALIDATES, ENABLES, REFINES, ROOT_CAUSES)
    - lib/core/doc-text-extractor.cjs extractDocText (Plan 03 -- the .docx/.html text source)
    - 169-RESEARCH.md Open Q2 (deriver classification; the producer is the net-new SOURCE the shipped
      derivers lack) + Pitfall 4 (Brain boundary)
    - tests/test-candidate-producer.cjs (the RED stub this turns GREEN)
  </read_first>
  <behavior>
    - produceCandidates reads the artifact-pair text (extractDocText for non-.md) and returns candidate
      {source, target, edge_type, reason} tuples
    - the producer can emit BOTH a CONTRADICTS AND a CONVERGES candidate (not CONTRADICTS-only)
    - every emitted edge_type is in the frozen cascade subset; anything else is dropped
    - the injectable `llm` stub is the only model surface; no Brain call is made
    - the default (non-injected) `llm` transport is the anthropic-transport pattern from
      llm-name-suggester.cjs, never the Brain
  </behavior>
  <action>
    Create lib/core/graph-candidate-producer.cjs. produceCandidates({roomDir, artifactPair, llm}): gather
    the text of each artifact in the pair (route .docx/.html through extractDocText), build a LOCAL
    derivation prompt that asks the injectable `llm` to identify typed relationships between the two
    artifacts drawn ONLY from the frozen cascade subset, parse the model's structured reply into
    {source, target, edge_type, reason} tuples, filter out any edge_type not in the frozen cascade subset,
    and return the surviving tuples. The function MUST be capable of returning BOTH a CONTRADICTS AND a
    CONVERGES tuple (within-room convergence is the LLM producer's job, LOW-6 -- cross-room-detect is
    excluded). `llm` is injectable; when not injected it defaults to the shipped LOCAL LLM transport that
    lib/core/llm-name-suggester.cjs establishes: a direct fetch to api.anthropic.com/v1/messages (the
    standard plugin LLM transport, Part-8-legal), NOT the Mindrian-owned Brain MCP -- mirror that module's
    _resolveProductionLlmClient idiom (x-api-key header, anthropic-version, AbortController timeout) and its
    injectable-client seam so tests pass a deterministic stub. Read LOCAL artifact text only; make NO Brain
    call (Part 8 -- LOCAL artifact text in, anthropic-transport out, never the Brain; the boundary scan in
    Plan 06 asserts zero artifact-body tokens reach a Brain query in this file). No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-candidate-producer.cjs && grep -q CONVERGES lib/core/graph-candidate-producer.cjs && node -e "require('./lib/core/graph-candidate-producer.cjs')" && echo PRODUCER_OK</automated>
  </verify>
  <done>test-candidate-producer.cjs passes: a stub llm yields BOTH a CONTRADICTS and a CONVERGES candidate,
  non-cascade edge types are filtered out, the default transport mirrors the llm-name-suggester.cjs
  anthropic-transport pattern (not the Brain), and the producer makes no Brain call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: graph-derivation.cjs composer (runDerivation + candidateToFinding + RECURSIVE rollupSubRooms)</name>
  <read_first>
    - lib/core/chain-executor.cjs runChain :295 (six-callback contract) + _applySelfCritique :213
      (material-step fable-mode seam) + RESEARCH Code Example 3 (the derivation-loop shape)
    - lib/core/graph-candidate-producer.cjs produceCandidates (Task 2 -- the deriveFn default)
    - lib/core/findings-wirer.cjs wireAccept/wireReject/wireDefer (proposed EvidenceClaim NODE + typed edge
      via navigation.cjs, caller-owned db handle, LOCAL-only -- the WRITER)
    - lib/core/navigation.cjs writeEdge (:113) + confirmNode + RESEARCH Code Example 2 (the writeEdge
      contract: source_id/target_id/edge_type/properties, ON CONFLICT idempotent)
    - lib/core/navigation/edges.cjs frozen ALLOWED_EDGE_TYPES (the cascade subset + NESTED_WITHIN from Plan 00)
    - lib/core/room-root.cjs resolveRoomRoot (Plan 02 -- to enumerate the child chain)
    - 169-RESEARCH.md Pitfall 1 (review_status is a NODE column) + Pitfall 3 (stable proposed-node id) +
      169-CONTEXT.md D-169-06 (producer/writer/adapter split) + D-169-11 (the NESTED_WITHIN lineage edge +
      arbitrary-depth recursion the rollup walks)
    - tests/test-graph-derivation-loop.cjs + tests/test-recursive-rollup.cjs (the RED stubs this turns GREEN)
  </read_first>
  <behavior>
    - runDerivation drives deriveFn (= produceCandidates) per artifact-pair step (material:true)
    - each critique-passed candidate is bridged by candidateToFinding into a findings-wirer finding, which
      writes a proposed NODE (review_status='proposed') + a typed EDGE
    - a selfCritiqueFn returning {passed:false} for an unjustified candidate drops it (no node, no edge)
    - the proposed-node id is a stable content/source-hash (re-run does not duplicate it)
    - rollupSubRooms TRANSITIVELY walks the NESTED_WITHIN child links: it ATTACHes each child room db
      read-only, recurses into the child's own children (arbitrary depth, cycle-guarded), and returns the
      UNION of edges across ALL nested levels without writing the parent db (a sub-sub-room edge appears in
      the top rollup)
  </behavior>
  <action>
    Create lib/core/graph-derivation.cjs. candidateToFinding(candidate): the explicit adapter that turns a
    {source, target, edge_type, reason} producer tuple into the finding shape findings-wirer.wireAccept
    expects (the proposed truth-claim NODE + typed edge intent). runDerivation({roomDir, runChain,
    selfCritiqueFn, deriveFn}): default deriveFn to graph-candidate-producer.produceCandidates (injectable
    for tests); build runChain steps (one per artifact pair) each `material:true`; onStep invokes deriveFn
    to get candidates and returns {chain_output: candidates, quality}; selfCritiqueFn is the fable-mode
    critic (defaults to a no-op {passed:true}). After a passing critique, for each surviving candidate call
    candidateToFinding then write via findings-wirer / the navigation typed-claim writer a PROPOSED
    truth-claim NODE (review_status='proposed', id = a stable sha256 over the source artifact content_hash +
    edge semantics) THEN a typed EDGE via navigation.writeEdge (cascade edge_type from the frozen set,
    properties enum/scalar only). Return {proposedNodes, edges, trace}. rollupSubRooms(parentRoomDir): open
    the parent room.db; query its NESTED_WITHIN child edges (room:<child> -> room:<parent>, the Plan-00
    lineage edge) to enumerate direct children; for each child, ATTACH its room.db read-only, UNION-select
    edges, then RECURSE into that child's own NESTED_WITHIN children (arbitrary depth, D-169-11) keeping a
    visited-set of resolved paths to guard cycles; DETACH each; return {edges} across ALL nested levels
    WITHOUT writing the parent db. No cross-room-detect; cross-room typed edges stay deferred. No raw edge
    SQL. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-graph-derivation-loop.cjs && node tests/test-subroom-rollup.cjs && node tests/test-recursive-rollup.cjs && ! grep -E "INSERT INTO edges" lib/core/graph-derivation.cjs && bash tests/run-all-169.sh 2>&1 | tail -5</automated>
  </verify>
  <done>test-graph-derivation-loop.cjs passes (candidate bridged by candidateToFinding, proposed NODE + typed
  edge for a good candidate, unjustified candidate dropped by fable-mode, no edge review_status, stable id,
  no raw edge SQL). test-subroom-rollup.cjs + test-recursive-rollup.cjs pass: the rollup walks NESTED_WITHIN
  TRANSITIVELY so a sub-sub-room edge reaches the top rollup, parent rows unchanged (ATTACH read-only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| artifact-pair text -> LLM producer | LOCAL text feeds the injectable model (default anthropic-transport, never the Brain); must never reach the Brain wire |
| producer candidate -> graph write | a derived edge crosses into the room graph; must be critiqued + proposed |
| sub-room / sub-sub-room room.db -> parent rollup | a read crosses the room boundary at arbitrary depth; must stay read-only ATTACH |
| structural index -> cascade edges | the legacy raw cascade auto-confirmed; disabled so derivation is the sole writer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-07 | Tampering | a bad CONTRADICTS edge misleads the navigator | mitigate | fable-mode selfCritiqueFn rejects unjustified candidates (material:true so it always fires); every edge lands PROPOSED, human confirms (Part 3/9) |
| T-169-08 | Information Disclosure | review_status written on the edge so it never surfaces for confirm | mitigate | proposed NODE pattern (findings-wirer via candidateToFinding); the test asserts no edge.properties.review_status |
| T-169-09 | Information Disclosure | the rollup copying sub-room rows into the parent db (room-boundary blur), worsened by recursion | mitigate | read-side ATTACH only at EVERY level; rollupSubRooms never writes the parent; cycle-guarded; cross-room typed edges deferred (Phase 83) |
| T-169-10 | Tampering | a raw INSERT INTO edges bypassing the chokepoint | mitigate | all writes via navigation.writeEdge; the legacy raw cascade in _indexArtifactBody is DISABLED (D-169-08); the Phase 109 pre-commit substrate guard rejects a new direct room-db require |
| T-169-17 | Information Disclosure | the LLM producer carrying .docx body text into a Brain query | mitigate | the producer reads LOCAL text + uses the injectable local llm (default anthropic-transport) only; makes NO Brain call; Plan 06 boundary-scans graph-candidate-producer.cjs |
| T-169-18 | Tampering | two cascade writers (legacy raw + derivation) both writing CONTRADICTS or INFORMS | mitigate | the legacy raw cascade (all four types) is disabled; the Plan 06 verdict asserts no derivation-owned edge type is also raw-INSERTed in the indexer |
| T-169-20 | Denial of Service | the transitive recursion looping on a cyclic NESTED_WITHIN graph or a symlink cycle | mitigate | a visited-set of resolved absolute paths guards both rebuildGraph recursion and rollupSubRooms recursion |
| T-169-SC | Tampering | npm installs | mitigate | zero new packages; no install task |
</threat_model>

<verification>
- node tests/test-candidate-producer.cjs GREEN (both CONTRADICTS + CONVERGES emit; default transport mirrors
  the llm-name-suggester.cjs anthropic pattern, not the Brain).
- node tests/test-graph-derivation-loop.cjs GREEN; node tests/test-subroom-rollup.cjs GREEN;
  node tests/test-recursive-rollup.cjs GREEN (transitive arbitrary-depth rollup via NESTED_WITHIN).
- grep confirms graph-derivation.cjs writes via navigation.writeEdge (no raw `INSERT INTO edges`) and the
  legacy cascade writes of CONTRADICTS/INFORMS/ENABLES/INVALIDATES are all gone from lazygraph-ops.cjs.
- bash tests/run-all-169.sh shows the producer + derivation + rollup + recursive-rollup legs GREEN
  (backfill/idempotence/heal/boundary still RED until Plans 05/07/06).
</verification>

<success_criteria>
The missing fuel exists (an LLM candidate producer emitting both CONTRADICTS and CONVERGES, wired into
runChain as deriveFn, fable-mode-critiqued, bridged by candidateToFinding into findings-wirer, written as
PROPOSED typed edges via the chokepoint). The indexer reaches ROOT-LEVEL flat artifacts + .docx/.html;
rebuild recurses sub-rooms TRANSITIVELY into per-sub-room dbs at arbitrary depth; the parent rollup is a
RECURSIVE read-side ATTACH walking the NESTED_WITHIN lineage edge so a sub-sub-room reaches the top; the
legacy raw cascade is disabled so derivation is the sole cascade writer. Root causes #2, #3, #4 closed at
the composition layer and the fractal now has arbitrary-depth teeth at the index + rollup layer.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-04-SUMMARY.md` when done.
</output>
