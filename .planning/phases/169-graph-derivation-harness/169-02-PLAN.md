---
phase: 169-graph-derivation-harness
plan: 02
type: execute
wave: 3
depends_on: ["169-01"]
files_modified:
  - lib/core/room-root.cjs
  - scripts/query-efficiency-telemetry.cjs
  - scripts/auto-explore-fingerprint.cjs
  - scripts/async-artifact-auto-commit.cjs
  - scripts/gsd-artifact-graph-hook.cjs
autonomous: true
requirements: [GDH-01]
canon_parts: [6, 8, 9]

must_haves:
  truths:
    - "lib/core/room-root.cjs exports resolveRoomRoot(filePath) that walks up from filePath to the nearest ancestor dir containing the SHIPPED `.room-root` FILE sentinel and returns that dir ('' when none), consolidating the 3x-duplicated walk-up (Part 7 CONSOLIDATION, not net-new -- RESEARCH Orchestrator note)"
    - "the auto-graph hook (gsd-artifact-graph-hook.cjs resolveRoomDir :80-100) resolves the WRITE-INDEX room by the written file's `.room-root` via resolveRoomRoot, NOT the registry active room; a write into a sub-room indexes into THAT sub-room's db regardless of the active room (GDH-01, D-169-05)"
    - "the three scripts that hand-rolled the walk-up (query-efficiency-telemetry.cjs:205, auto-explore-fingerprint.cjs:82, async-artifact-auto-commit.cjs:130) are repointed at resolveRoomRoot so there is ONE resolver, removing drift"
    - "dashboard-helpers.cjs::detectActiveRoom is LEFT INTACT for genuine 'what is the active room' callers (the unify switches only the WRITE-INDEX path to file-rooted resolution, per the RESEARCH Anchor-1 refinement)"
    - "the resolver fails safe: a file outside any room returns '' and the hook degrades to its existing exit-0 no-op (never throws, never blocks a Write)"
  artifacts:
    - path: "lib/core/room-root.cjs"
      provides: "the ONE `.room-root` walk-up resolver: resolveRoomRoot(filePath) + findRoomRootSentinels()"
      min_lines: 40
      exports: ["resolveRoomRoot"]
    - path: "scripts/gsd-artifact-graph-hook.cjs"
      provides: "resolveRoomDir threading filePath through resolveRoomRoot for the write-index path"
      contains: "resolveRoomRoot"
    - path: "scripts/query-efficiency-telemetry.cjs"
      provides: "walk-up repointed at resolveRoomRoot"
      contains: "room-root"
  key_links:
    - from: "scripts/gsd-artifact-graph-hook.cjs"
      to: "lib/core/room-root.cjs"
      via: "resolveRoomDir calls resolveRoomRoot(filePath) before any registry fallback"
      pattern: "resolveRoomRoot"
    - from: "lib/core/room-root.cjs"
      to: ".room-root sentinel file"
      via: "fs.existsSync(path.join(dir, '.room-root')) walk-up"
      pattern: "\\.room-root"
---

<objective>
WAVE 2 FOUNDATION (GDH-01). Land lib/core/room-root.cjs as the ONE `.room-root` walk-up resolver and
repoint the per-write auto-graph hook (and the three scripts that already hand-rolled the same walk-up) at
it. This closes root cause #1 (two resolvers disagree): a sub-room write indexes into the sub-room's own
db regardless of the registry active room. This is a Part 7 CONSOLIDATION of a SHIPPED convention, not a
net-new invention (RESEARCH Orchestrator note: the `.room-root` file is already written at room birth and
the walk-up is already duplicated in three scripts).

Purpose: turn tests/test-room-root-resolver.cjs (RED from Plan 01) GREEN and unify the write-index path.
Output: lib/core/room-root.cjs + the hook + three scripts repointed at it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
@scripts/gsd-artifact-graph-hook.cjs
@scripts/heal-command.cjs
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract for resolveRoomRoot. This plan IMPLEMENTS that signature:
`resolveRoomRoot(filePath) -> roomDir|''` (walk up to the nearest `.room-root` FILE) +
`findRoomRootSentinels() -> ['.room-root']`.
</shared_iface_contract>

<rules>
- Part 7: this is a CONSOLIDATION. Extract the existing 3x-duplicated walk-up into ONE resolver and repoint
  the duplicates; do not invent a new sentinel convention. Sentinel = the `.room-root` FILE (Q4 CLOSED).
- Part 8: the resolver is a pure LOCAL filesystem walk; zero network, zero Brain.
- D-169-05: only the WRITE-INDEX path switches to file-rooted resolution; detectActiveRoom stays for
  active-room callers.
- Fail safe: '' when no sentinel; the hook keeps its exit-0-always degrade pattern; never throw on a Write.
- NO em-dashes. CJS only. No new deps.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: lib/core/room-root.cjs resolver (consolidation)</name>
  <read_first>
    - scripts/query-efficiency-telemetry.cjs:205 (the existing walk-up idiom to extract)
    - scripts/heal-command.cjs:890 + :909-921 (the `['.room-root','STATE.md','ROOM.md']` sentinel set + the
      sub-rooms-container detection precedent for GDH-03)
    - tests/test-room-root-resolver.cjs (the RED stub this turns GREEN)
  </read_first>
  <action>
    Create lib/core/room-root.cjs exporting resolveRoomRoot(filePath): normalize to an absolute dir (if
    filePath is a file, start from its dirname), then walk up parent by parent checking
    fs.existsSync(path.join(dir, '.room-root')); return the first dir that has it, or '' at filesystem root.
    Export findRoomRootSentinels() returning ['.room-root'] (the canonical primary sentinel; the
    heal-command broader set ['.room-root','STATE.md','ROOM.md'] is referenced in a comment as the
    belt-and-suspenders fallback but the PRIMARY sentinel is `.room-root`). Pure built-ins (node:fs,
    node:path). No throw: a missing/relative path resolves against process.cwd and returns '' if nothing is
    found. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-room-root-resolver.cjs</automated>
  </verify>
  <done>tests/test-room-root-resolver.cjs passes: resolveRoomRoot returns the sub-room dir for a file
  inside a temp sub-room carrying `.room-root`, '' for a file outside any room.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the hook + the three scripts at the one resolver</name>
  <read_first>
    - scripts/gsd-artifact-graph-hook.cjs resolveRoomDir :80-100 (the registry-active fallback to demote
      for the write-index path; RESEARCH Anchor 1 verbatim lines 87-96)
    - scripts/auto-explore-fingerprint.cjs:82, scripts/async-artifact-auto-commit.cjs:130 (the duplicated
      walk-ups to repoint)
  </read_first>
  <action>
    In gsd-artifact-graph-hook.cjs: thread the written filePath into resolveRoomDir and call
    resolveRoomRoot(filePath) FIRST; only if it returns '' fall back to the existing env/registry-active
    resolution (so the path is file-rooted for sub-room writes but still degrades for path-less calls). In
    query-efficiency-telemetry.cjs, auto-explore-fingerprint.cjs, and async-artifact-auto-commit.cjs:
    replace the inline `.room-root` walk-up with require('../lib/core/room-root.cjs').resolveRoomRoot (adjust
    the relative require path per each script's depth), preserving each script's existing behavior on a ''
    result. Keep dashboard-helpers.cjs::detectActiveRoom untouched. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-room-root-resolver.cjs && grep -q resolveRoomRoot scripts/gsd-artifact-graph-hook.cjs && grep -q room-root scripts/auto-explore-fingerprint.cjs && grep -q room-root scripts/async-artifact-auto-commit.cjs && echo REPOINTED && node -e "require('./scripts/gsd-artifact-graph-hook.cjs')" 2>/dev/null; echo HOOK_LOADS</automated>
  </verify>
  <done>The hook resolves the write-index room by `.room-root` first; all three scripts require the shared
  resolver; detectActiveRoom is unchanged; the hook still loads and degrades safely on ''.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| written file path -> resolver | a Write/Edit path crosses into room resolution; must not escape the room tree |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-02 | Information Disclosure | a sub-room write indexing into the parent db (root cause #1) | mitigate | resolveRoomRoot walks to the file's own `.room-root`, so the write lands in the sub-room db regardless of active room; asserted by test-room-root-resolver.cjs |
| T-169-03 | Denial of Service | a resolver that throws on a malformed path and breaks every Write | mitigate | fail-safe '' return + the hook's exit-0-always degrade; no throw on missing/relative path |
</threat_model>

<verification>
- node tests/test-room-root-resolver.cjs GREEN.
- The hook + three scripts require/call resolveRoomRoot; detectActiveRoom untouched.
- bash tests/run-all-169.sh shows the room-root suite GREEN (others still RED until later waves).
</verification>

<success_criteria>
ONE `.room-root` resolver exists and the write-index path is file-rooted; root cause #1 closed. The
Wave-2 barrier for the resolver leg is green.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-02-SUMMARY.md` when done.
</output>
