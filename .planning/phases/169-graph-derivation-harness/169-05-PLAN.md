---
phase: 169-graph-derivation-harness
plan: 05
type: execute
wave: 4
depends_on: ["169-04"]
files_modified:
  - commands/graph.md
  - scripts/gsd-graph-derive-sweep.cjs
  - scripts/gsd-graph-derive-drain.cjs
  - hooks/hooks.json
  - lib/core/graph-derivation.cjs
  - tests/test-graph-derive-sweep.cjs
autonomous: true
requirements: [GDH-02, GDH-06, GDH-07]
canon_parts: [3, 6, 8, 9]

must_haves:
  truths:
    - "commands/graph.md gains a `--derive` branch that resolves the room by resolveRoomRoot, rebuilds it sub-room-recursively (ROOT-FILES-aware, non-.md-aware), calls runDerivation once per room+sub-room, and reports the typed-edge delta (0 -> N); it is the UNIVERSAL net that works on Desktop/Cowork (no hooks) too (GDH-06, three-surface rule)"
    - "scripts/gsd-graph-derive-sweep.cjs is a NEW Stop hook that ENQUEUES a derive request (cheap, within the Stop timeout budget) into a queue file; scripts/gsd-graph-derive-drain.cjs is the DRAIN that runs the expensive runDerivation pass and is REGISTERED on the SessionStart event in hooks.json, mirroring the SHIPPED scripts/brain-derivation-drain.cjs (already SessionStart-registered) -- the named drain trigger, NOT enqueue-and-never-drain (GDH-02 trigger, D-169-01, D-169-08)"
    - "the new Stop sweep hook + the SessionStart drain hook are registered in hooks/hooks.json so they ship with the plugin to every install; the per-write structural index hook (gsd-artifact-graph-hook.cjs) is UNCHANGED (the two-trigger split, Pattern 3)"
    - "a unit test asserts the round-trip: an enqueued request written by the sweep is actually DRAINED by the drain (the drain reads the queue, calls runDerivation, and clears the entry) -- not merely that the hook file loads (MEDIUM-5)"
    - "runDerivation is idempotent (GDH-07): re-running on a wired room mints NO duplicate proposed node (the stable content-hash id from Plan 04 + a pre-propose check that no-ops when the node is already proposed OR confirmed) and leaves confirmed edges untouched"
    - "the derivation HALTS at the Decision Gate (Part 3): derived edges land proposed; the hook/command NEVER auto-confirm; the human confirms via the existing confirmNode path"
    - "the sweep hook + the drain hook + the --derive command open ZERO Brain wire (Part 8); brain-derive (the one Brain-touching deriver) is boundary-scanned in Plan 06"
  artifacts:
    - path: "commands/graph.md"
      provides: "the --derive backfill branch (resolve -> rebuild ROOT-FILES + sub-room recursive -> runDerivation per room+sub-room -> report 0 -> N)"
      contains: "--derive"
    - path: "scripts/gsd-graph-derive-sweep.cjs"
      provides: "the NEW debounced Stop sweep hook: ENQUEUE a derive request; cheap; exit-0-always; never per-keystroke"
      min_lines: 40
    - path: "scripts/gsd-graph-derive-drain.cjs"
      provides: "the SessionStart DRAIN hook: read the queue, run runDerivation, clear the entry (mirrors brain-derivation-drain.cjs)"
      min_lines: 40
    - path: "hooks/hooks.json"
      provides: "registration of the Stop sweep hook AND the SessionStart drain hook (per-write structural hook unchanged)"
      contains: "gsd-graph-derive"
    - path: "lib/core/graph-derivation.cjs"
      provides: "the idempotence guard (pre-propose no-op when node already proposed/confirmed) finishing GDH-07"
      contains: "proposed"
    - path: "tests/test-graph-derive-sweep.cjs"
      provides: "the enqueue-then-drain round-trip assertion: a sweep-enqueued request is drained by the drain (MEDIUM-5)"
      min_lines: 35
  key_links:
    - from: "commands/graph.md --derive"
      to: "lib/core/graph-derivation.cjs runDerivation + rollupSubRooms"
      via: "the backfill resolves by resolveRoomRoot, rebuilds, derives per room+sub-room"
      pattern: "runDerivation"
    - from: "scripts/gsd-graph-derive-sweep.cjs"
      to: "the queue file"
      via: "the Stop hook enqueues a derive request (cheap)"
      pattern: "queue|enqueue"
    - from: "scripts/gsd-graph-derive-drain.cjs"
      to: "lib/core/graph-derivation.cjs runDerivation"
      via: "the SessionStart drain reads the queue and runs the expensive composer"
      pattern: "runDerivation"
    - from: "hooks/hooks.json"
      to: "scripts/gsd-graph-derive-sweep.cjs + scripts/gsd-graph-derive-drain.cjs"
      via: "Stop event registers the sweep; SessionStart event registers the drain (mirror brain-derivation-drain)"
      pattern: "Stop|SessionStart"
---

<objective>
WAVE 4 SURFACES (GDH-02 trigger / GDH-06 / GDH-07). Land the two triggers and close idempotence. The
`/mos:graph --derive` backfill (commands/graph.md) is the universal net that wires an EXISTING room incl.
its sub-rooms in one pass and works on the hook-less surfaces (Desktop/Cowork). The CLI trigger is the
two-part enqueue-then-drain debounce that mirrors the SHIPPED brain-derivation precedent (D-169-08): the
NEW Stop sweep hook (scripts/gsd-graph-derive-sweep.cjs) ENQUEUES a derive request (cheap), and the NEW
SessionStart drain hook (scripts/gsd-graph-derive-drain.cjs, registered on SessionStart just like
scripts/brain-derivation-drain.cjs) DRAINS the queue and runs the expensive runDerivation pass. This is the
named drain trigger -- not enqueue-and-never-drain. The per-write structural index hook is left untouched
(the two-trigger split). The idempotence guard in graph-derivation.cjs makes a re-run a no-op. The
derivation halts at the Decision Gate; nothing auto-confirms.

Purpose: turn test-derive-backfill-acceptance.cjs + test-derive-idempotence.cjs GREEN, and add
test-graph-derive-sweep.cjs proving the enqueue-then-drain round-trip (MEDIUM-5).
Output: commands/graph.md --derive + the Stop sweep hook + the SessionStart drain hook + their hooks.json
registrations + the idempotence guard + the sweep/drain round-trip test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-CONTEXT.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-04-PLAN.md
@commands/graph.md
@hooks/hooks.json
@scripts/brain-derivation-drain.cjs
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract. This plan IMPLEMENTS the `/mos:graph --derive` backfill entry,
the Stop sweep hook, and the SessionStart drain hook, and CONSUMES runDerivation + rollupSubRooms (Plan 04)
and resolveRoomRoot (Plan 02).
</shared_iface_contract>

<rules>
- D-169-01: the trigger is a DEBOUNCED Stop sweep (enqueue) + a SessionStart drain (run) + the explicit
  backfill. NEVER per-keystroke. The per-write structural index hook stays cheap and UNCHANGED (Pattern 3,
  two-trigger split).
- D-169-08 (MEDIUM-5): the drain trigger MUST be named and registered -- mirror the SHIPPED
  scripts/brain-derivation-drain.cjs which is registered on SessionStart in hooks.json. Enqueue on Stop,
  drain on SessionStart. A unit test asserts the enqueued request is actually drained by that trigger
  (not merely that the hook file loads).
- Three-surface rule: the backfill is the universal net for Desktop/Cowork (no PostToolUse/Stop hooks);
  mirror the Phase 149 session-start-reconcile-as-universal-net posture.
- Part 3/9: derivation HALTS at the Decision Gate; derived edges land proposed; NEVER auto-confirm.
- GDH-07: re-run is a no-op -- a pre-propose check no-ops when the node is already proposed OR confirmed;
  confirmed edges are untouched (Pitfall 3).
- Part 8: the sweep hook + the drain hook + the command open ZERO Brain wire; brain-derive is
  boundary-scanned in Plan 06.
- The Stop hook is exit-0-always (never block session end); it only enqueues so it stays within the Stop
  timeout. The drain runs the expensive pass on the next SessionStart.
- NO em-dashes. CJS / markdown / JSON only. No new deps.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: idempotence guard + the Stop sweep hook + the SessionStart drain hook</name>
  <read_first>
    - lib/core/graph-derivation.cjs runDerivation (Plan 04: the stable proposed-node id seed)
    - scripts/brain-derivation-drain.cjs (the SHIPPED enqueue-then-drain precedent + its queue-file idiom;
      it is registered on SessionStart in hooks.json) + 169-RESEARCH.md Open Q5 (mirror this idiom)
    - 169-RESEARCH.md Pitfall 3 (idempotence on proposed nodes: stable id + skip already-confirmed)
    - hooks/hooks.json (the existing Stop + SessionStart hook blocks; the PostToolUse structural hook to
      leave untouched)
    - tests/test-derive-idempotence.cjs (the RED stub this turns GREEN) + tests/test-graph-derive-sweep.cjs
      (created in Task 2 -- the round-trip assertion)
  </read_first>
  <action>
    In lib/core/graph-derivation.cjs: add the idempotence guard -- before proposing a node, query its
    stable-id existence + review_status via navigation; if it is already `proposed` or `confirmed`, no-op
    (do not re-mint, do not re-propose); leave confirmed edges untouched. Create
    scripts/gsd-graph-derive-sweep.cjs: a Stop hook that ENQUEUES a derive request for the current room
    (resolveRoomRoot on the room dir) into a cheap queue file (mirror the brain-derivation queue idiom);
    the Stop handler returns exit 0 always and stays within the timeout by ONLY enqueueing. Create
    scripts/gsd-graph-derive-drain.cjs: the DRAIN, structured exactly like scripts/brain-derivation-drain.cjs
    -- read the queue file, for each queued room run runDerivation, then clear the drained entry; exit-0.
    No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-derive-idempotence.cjs && node -e "require('./scripts/gsd-graph-derive-sweep.cjs')" 2>/dev/null; node -e "require('./scripts/gsd-graph-derive-drain.cjs')" 2>/dev/null; echo SWEEP_AND_DRAIN_LOAD</automated>
  </verify>
  <done>test-derive-idempotence.cjs passes: a second runDerivation mints no duplicate proposed node and
  leaves a confirmed node untouched; the Stop sweep hook (enqueue-only) and the SessionStart drain hook
  (read-queue-run-clear) both load.</done>
</task>

<task type="auto">
  <name>Task 2: /mos:graph --derive backfill + hook registration + the drain round-trip test</name>
  <read_first>
    - commands/graph.md (the existing graph command surface to extend with a --derive branch)
    - hooks/hooks.json (the Stop-event block + the SessionStart block where brain-derivation-drain is
      already registered -- register the new drain alongside it)
    - lib/core/graph-derivation.cjs runDerivation + rollupSubRooms (Plan 04)
    - tests/test-derive-backfill-acceptance.cjs (the RED acceptance stub: b2 0 -> N)
  </read_first>
  <action>
    In commands/graph.md: add a `--derive` branch documenting the backfill flow -- resolve the room by
    resolveRoomRoot, rebuild it ROOT-FILES-aware + sub-room-recursively (the Plan-04 rebuildGraph,
    non-.md-aware), call runDerivation once per room and per sub-room, surface the proposed edges at the
    Decision Gate (Part 3, never auto-confirm), and report the typed-edge delta (count before -> count
    after). State that on Desktop/Cowork (no hooks) this command is the universal net. In hooks/hooks.json:
    register scripts/gsd-graph-derive-sweep.cjs on the Stop event AND scripts/gsd-graph-derive-drain.cjs on
    the SessionStart event (alongside brain-derivation-drain.cjs; leave the PostToolUse structural hook
    unchanged). Create tests/test-graph-derive-sweep.cjs: write a temp queue entry via the sweep's enqueue
    path, run the drain, and ASSERT the drain read the entry, called runDerivation (a stub/spy or the real
    composer on a synthetic two-artifact room), and cleared the entry -- proving the enqueue-then-drain
    round-trip, not merely that the hook loads (MEDIUM-5). No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-derive-backfill-acceptance.cjs && node tests/test-graph-derive-sweep.cjs && grep -q -- "--derive" commands/graph.md && grep -q gsd-graph-derive-sweep hooks/hooks.json && grep -q gsd-graph-derive-drain hooks/hooks.json && node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" && echo HOOKS_JSON_VALID && bash tests/run-all-169.sh 2>&1 | tail -5</automated>
  </verify>
  <done>test-derive-backfill-acceptance.cjs passes (b2 0 -> N with skip-if-absent fallback);
  test-graph-derive-sweep.cjs proves the enqueue-then-drain round-trip; commands/graph.md carries the
  --derive branch; hooks.json is valid JSON registering BOTH the Stop sweep hook AND the SessionStart drain
  hook; the structural per-write hook is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Stop event -> sweep hook | session-end fires the hook; must stay cheap + exit-0 (enqueue only) |
| SessionStart event -> drain hook | session-start drains the queue and runs the expensive pass |
| backfill command -> room graph | the universal net writes proposed edges; must halt at the gate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-11 | Denial of Service | the Stop hook running the expensive derivation inline + blowing the timeout | mitigate | enqueue-then-drain debounce: the Stop handler only enqueues (cheap), the expensive pass runs on the SessionStart drain; exit-0-always |
| T-169-12 | Tampering | a re-run re-proposing the same finding (duplicate gate-fatigue) | mitigate | stable content-hash node id + pre-propose no-op when already proposed/confirmed (GDH-07, Pitfall 3) |
| T-169-13 | Elevation of Privilege | the hook/command auto-confirming a derived edge | mitigate | derivation halts at the Decision Gate; lands proposed only; confirm is the existing human confirmNode path (Part 3/9) |
| T-169-19 | Reliability | enqueue-and-never-drain (the queue silently grows, derivation never runs) | mitigate | the drain is a named SessionStart hook mirroring brain-derivation-drain.cjs; the round-trip test asserts an enqueued request is actually drained (MEDIUM-5) |
| T-169-SC | Tampering | npm installs | mitigate | zero new packages; no install task |
</threat_model>

<verification>
- node tests/test-derive-backfill-acceptance.cjs GREEN; node tests/test-derive-idempotence.cjs GREEN;
  node tests/test-graph-derive-sweep.cjs GREEN (the enqueue-then-drain round-trip).
- hooks/hooks.json is valid JSON and registers BOTH the Stop sweep hook AND the SessionStart drain hook;
  the per-write structural hook is unchanged.
- bash tests/run-all-169.sh shows backfill + idempotence + sweep/drain legs GREEN (only the adversarial
  verdict + Part 8 sweep remain for Plan 06).
</verification>

<success_criteria>
Both triggers exist (debounced Stop sweep that enqueues + a named SessionStart drain that runs + the
universal-net backfill), the queue actually drains, idempotence holds, and the derivation halts at the
Decision Gate. Root cause #4 fully closed end to end. Wave-4 barrier green.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-05-SUMMARY.md` when done.
</output>
