---
phase: 169-graph-derivation-harness
plan: 05
type: execute
wave: 4
depends_on: ["169-04"]
files_modified:
  - commands/graph.md
  - scripts/gsd-graph-derive-sweep.cjs
  - hooks/hooks.json
  - lib/core/graph-derivation.cjs
autonomous: true
requirements: [GDH-02, GDH-06, GDH-07]
canon_parts: [3, 6, 8, 9]

must_haves:
  truths:
    - "commands/graph.md gains a `--derive` branch that resolves the room by resolveRoomRoot, rebuilds it sub-room-recursively (non-.md-aware), calls runDerivation once per room+sub-room, and reports the typed-edge delta (0 -> N); it is the UNIVERSAL net that works on Desktop/Cowork (no hooks) too (GDH-06, three-surface rule)"
    - "scripts/gsd-graph-derive-sweep.cjs is a NEW Stop hook that ENQUEUES a derive request (cheap, within the Stop timeout budget) and a drain runs the expensive runDerivation pass, mirroring the SHIPPED brain-derivation enqueue-then-drain debounce idiom (GDH-02 trigger, D-169-01); it is NOT per-keystroke"
    - "the new Stop hook is registered in hooks/hooks.json so it ships with the plugin to every install; the per-write structural index hook (gsd-artifact-graph-hook.cjs) is UNCHANGED (the two-trigger split, Pattern 3)"
    - "runDerivation is idempotent (GDH-07): re-running on a wired room mints NO duplicate proposed node (the stable content-hash id from Plan 04 + a pre-propose check that no-ops when the node is already proposed OR confirmed) and leaves confirmed edges untouched"
    - "the derivation HALTS at the Decision Gate (Part 3): derived edges land proposed; the hook/command NEVER auto-confirm; the human confirms via the existing confirmNode path"
    - "the sweep hook + the --derive command open ZERO Brain wire (Part 8); brain-derive (the one Brain-touching deriver) is boundary-scanned in Plan 06"
  artifacts:
    - path: "commands/graph.md"
      provides: "the --derive backfill branch (resolve -> rebuild -> runDerivation per room+sub-room -> report 0 -> N)"
      contains: "--derive"
    - path: "scripts/gsd-graph-derive-sweep.cjs"
      provides: "the NEW debounced Stop sweep hook: enqueue-then-drain calling runDerivation; never per-keystroke; exit-0-always"
      min_lines: 60
    - path: "hooks/hooks.json"
      provides: "registration of the Stop sweep hook (per-write structural hook unchanged)"
      contains: "gsd-graph-derive-sweep"
    - path: "lib/core/graph-derivation.cjs"
      provides: "the idempotence guard (pre-propose no-op when node already proposed/confirmed) finishing GDH-07"
      contains: "proposed"
  key_links:
    - from: "commands/graph.md --derive"
      to: "lib/core/graph-derivation.cjs runDerivation + rollupSubRooms"
      via: "the backfill resolves by resolveRoomRoot, rebuilds, derives per room+sub-room"
      pattern: "runDerivation"
    - from: "scripts/gsd-graph-derive-sweep.cjs"
      to: "lib/core/graph-derivation.cjs runDerivation"
      via: "enqueue-then-drain debounce calls the same composer the backfill calls"
      pattern: "runDerivation"
    - from: "hooks/hooks.json"
      to: "scripts/gsd-graph-derive-sweep.cjs"
      via: "Stop event registration"
      pattern: "Stop"
---

<objective>
WAVE 4 SURFACES (GDH-02 trigger / GDH-06 / GDH-07). Land the two triggers and close idempotence. The
`/mos:graph --derive` backfill (commands/graph.md) is the universal net that wires an EXISTING room incl.
its sub-rooms in one pass and works on the hook-less surfaces (Desktop/Cowork). The NEW Stop sweep hook
(scripts/gsd-graph-derive-sweep.cjs) is the CLI debounced trigger -- enqueue-then-drain, never
per-keystroke, the per-write structural index hook left untouched (the two-trigger split). The
idempotence guard in graph-derivation.cjs makes a re-run a no-op. The derivation halts at the Decision
Gate; nothing auto-confirms.

Purpose: turn test-derive-backfill-acceptance.cjs + test-derive-idempotence.cjs + the sweep behavior
(RED from Plan 01) GREEN.
Output: commands/graph.md --derive + the Stop sweep hook + its hooks.json registration + the idempotence
guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-04-PLAN.md
@commands/graph.md
@hooks/hooks.json
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract. This plan IMPLEMENTS the `/mos:graph --derive` backfill entry and
the Stop sweep hook, and CONSUMES runDerivation + rollupSubRooms (Plan 04) and resolveRoomRoot (Plan 02).
</shared_iface_contract>

<rules>
- D-169-01: the trigger is a DEBOUNCED Stop sweep + the explicit backfill. NEVER per-keystroke. The
  per-write structural index hook stays cheap and UNCHANGED (Pattern 3, two-trigger split).
- Three-surface rule: the backfill is the universal net for Desktop/Cowork (no PostToolUse/Stop hooks);
  mirror the Phase 149 session-start-reconcile-as-universal-net posture.
- Part 3/9: derivation HALTS at the Decision Gate; derived edges land proposed; NEVER auto-confirm.
- GDH-07: re-run is a no-op -- a pre-propose check no-ops when the node is already proposed OR confirmed;
  confirmed edges are untouched (Pitfall 3).
- Part 8: the hook + command open ZERO Brain wire; brain-derive is boundary-scanned in Plan 06.
- The Stop hook is exit-0-always (never block session end); debounce keeps it within the Stop timeout.
- NO em-dashes. CJS / markdown / JSON only. No new deps.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: idempotence guard + the Stop sweep hook</name>
  <read_first>
    - lib/core/graph-derivation.cjs runDerivation (Plan 04: the stable proposed-node id seed)
    - 169-RESEARCH.md Pitfall 3 (idempotence on proposed nodes: stable id + skip already-confirmed) + Open
      Q5 (the brain-derivation enqueue-then-drain debounce idiom to mirror)
    - hooks/hooks.json (the existing Stop hooks + the PostToolUse structural hook to leave untouched)
    - tests/test-derive-idempotence.cjs (the RED stub this turns GREEN)
  </read_first>
  <action>
    In lib/core/graph-derivation.cjs: add the idempotence guard -- before proposing a node, query its
    stable-id existence + review_status via navigation; if it is already `proposed` or `confirmed`, no-op
    (do not re-mint, do not re-propose); leave confirmed edges untouched. Create
    scripts/gsd-graph-derive-sweep.cjs: a Stop hook that ENQUEUES a derive request for the current room
    (resolveRoomRoot on the room dir) into a cheap queue file (mirror the brain-derivation queue+drain
    idiom referenced in RESEARCH Open Q5), and a drain path that runs runDerivation; the Stop handler
    returns exit 0 always and stays within the timeout by only enqueueing (the expensive pass runs on
    drain). No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-derive-idempotence.cjs && node -e "require('./scripts/gsd-graph-derive-sweep.cjs')" 2>/dev/null; echo SWEEP_LOADS</automated>
  </verify>
  <done>test-derive-idempotence.cjs passes: a second runDerivation mints no duplicate proposed node and
  leaves a confirmed node untouched; the sweep hook loads and is enqueue-then-drain (not per-keystroke).</done>
</task>

<task type="auto">
  <name>Task 2: /mos:graph --derive backfill + hook registration</name>
  <read_first>
    - commands/graph.md (the existing graph command surface to extend with a --derive branch)
    - hooks/hooks.json (the Stop-event block to register the new hook in)
    - lib/core/graph-derivation.cjs runDerivation + rollupSubRooms (Plan 04)
    - tests/test-derive-backfill-acceptance.cjs (the RED acceptance stub: b2 0 -> N)
  </read_first>
  <action>
    In commands/graph.md: add a `--derive` branch documenting the backfill flow -- resolve the room by
    resolveRoomRoot, rebuild it sub-room-recursively (the Plan-04 rebuildGraph, non-.md-aware), call
    runDerivation once per room and per sub-room, surface the proposed edges at the Decision Gate (Part 3,
    never auto-confirm), and report the typed-edge delta (count before -> count after). State that on
    Desktop/Cowork (no hooks) this command is the universal net. In hooks/hooks.json: register
    scripts/gsd-graph-derive-sweep.cjs on the Stop event (leave the PostToolUse structural hook unchanged).
    No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-derive-backfill-acceptance.cjs && grep -q -- "--derive" commands/graph.md && grep -q gsd-graph-derive-sweep hooks/hooks.json && node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" && echo HOOKS_JSON_VALID && bash tests/run-all-169.sh 2>&1 | tail -5</automated>
  </verify>
  <done>test-derive-backfill-acceptance.cjs passes (b2 0 -> N with skip-if-absent fallback); commands/graph.md
  carries the --derive branch; hooks.json is valid JSON registering the Stop sweep hook; the structural
  per-write hook is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Stop event -> sweep hook | session-end fires the hook; must stay cheap + exit-0 |
| backfill command -> room graph | the universal net writes proposed edges; must halt at the gate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-11 | Denial of Service | the Stop hook running the expensive derivation inline + blowing the timeout | mitigate | enqueue-then-drain debounce: the Stop handler only enqueues (cheap), the expensive pass runs on drain; exit-0-always |
| T-169-12 | Tampering | a re-run re-proposing the same finding (duplicate gate-fatigue) | mitigate | stable content-hash node id + pre-propose no-op when already proposed/confirmed (GDH-07, Pitfall 3) |
| T-169-13 | Elevation of Privilege | the hook/command auto-confirming a derived edge | mitigate | derivation halts at the Decision Gate; lands proposed only; confirm is the existing human confirmNode path (Part 3/9) |
| T-169-SC | Tampering | npm installs | mitigate | zero new packages; no install task |
</threat_model>

<verification>
- node tests/test-derive-backfill-acceptance.cjs GREEN; node tests/test-derive-idempotence.cjs GREEN.
- hooks/hooks.json is valid JSON and registers the Stop sweep hook; the per-write structural hook is
  unchanged.
- bash tests/run-all-169.sh shows backfill + idempotence + sweep legs GREEN (only the adversarial verdict
  + Part 8 sweep remain for Plan 06).
</verification>

<success_criteria>
Both triggers exist (debounced Stop sweep + universal-net backfill), idempotence holds, and the derivation
halts at the Decision Gate. Root cause #4 fully closed end to end. Wave-4 barrier green.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-05-SUMMARY.md` when done.
</output>
