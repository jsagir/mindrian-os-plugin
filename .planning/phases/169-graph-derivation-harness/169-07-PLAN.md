---
phase: 169-graph-derivation-harness
plan: 07
type: execute
wave: 4
depends_on: ["169-00", "169-02"]
files_modified:
  - lib/core/graph-self-heal.cjs
  - lib/core/navigation/room-birth.cjs
  - tests/run-all-169.sh
autonomous: true
requirements: [GDH-08, GDH-09, D-169-11]
canon_parts: [3, 4, 6, 8, 9]

must_haves:
  truths:
    - "lib/core/graph-self-heal.cjs exports detectUnsentineledArtifactFolder(roomDir) that finds artifact-bearing folders UNDER a room that lack their OWN `.room-root` sentinel (reusing lib/core/room-root.cjs resolveRoomRoot + the heal-command.cjs:909-921 sub-rooms-container precedent), so the GDH-01 walk-up would otherwise silently roll their artifacts up into the parent's db (GDH-08, D-169-09; the b2-journey live case)"
    - "lib/core/graph-self-heal.cjs exports healRoom({folder, parentRoomDir, slug, approvedBy, runTimeline}) that REFUSES without approvedBy (no silent room creation; the Part 3 Decision Gate APPROVE attribution, room-birth.cjs:316-318) and on APPROVE INVOKES lib/core/navigation/room-birth.cjs birthRoom threading opts.approvedBy for the FULL SEED-001 wiring (ROOM.md + STATE.md + MINTO.md + USER.md + per-section FEYNMAN.md + BRAIN enqueue + room_created memory_event + own .mindrian/room.db) -- a healed room is byte-indistinguishable from a born room (GDH-09, D-169-10; REUSE birthRoom, do not hand-roll the scaffold, Part 7)"
    - "the heal writes the OPERATIONAL joint (D-169-11 Option C, operational half): it registers the healed sub-room in `.rooms/registry.json` with parent=<parent-slug> (the registry already supports a parent field -- polygon/mindrianOS carry one) AND it adds a `parent` field to the `.room-root` sentinel JSON, which room-birth.cjs:357-367 currently writes as {room,active,born} with NO parent -- the sentinel now carries the parent so the rollup + a hierarchy query can see the lineage operationally"
    - "the heal writes the GRAPH-NAVIGABLE joint (D-169-11 Option C, graph half): a NESTED_WITHIN typed edge from room:<child-slug> to room:<parent-slug> via navigation.writeEdge (the Plan 00 frozen edge), so a graph walk traverses the nested hierarchy UP to the parent and (read-side, via rollupSubRooms) DOWN to children; PART_OF is NOT used (illegal for a room target); BELONGS_TO is NOT used (absent from the frozen set); the edge properties are enum/scalar only"
    - "the heal invokes lib/core/feynman/timeline-runner.cjs refreshAll(healedRoomDir) so the healed room's per-section FEYNMAN.md gains its `## Timeline (auto)` section regenerated from the room_created memory_event (GDH-09 temporal, D-169-10; REUSE the Phase 124 runner, do not hand-roll the timeline)"
    - "room-birth.cjs:357-367 is amended so the `.room-root` sentinel JSON carries an optional `parent` field when opts.parent is provided (additive, default omitted so existing born-room behavior is byte-unchanged); birthRoom accepts opts.parent and threads it to both the sentinel write AND the registry create (so a NEW sub-room born via the SEED-001 contract ALSO gets the parent joint, not just the backfill heal)"
    - "the heal is ordered: (0) detect, (1) birthRoom full wiring, (2) operational joint (registry parent + sentinel parent), (3) graph joint (NESTED_WITHIN edge), (4) FEYNMAN timeline refresh, THEN it RETURNS so the backfill (Plan 05) indexes + derives into the healed db -- the heal itself never indexes/derives (clean separation; Plan 05 owns the index+derive)"
    - "the heal composes at ARBITRARY DEPTH (D-169-11): healRoom on a folder whose parent is itself a healed/born sub-room writes the NESTED_WITHIN edge to THAT sub-room (not only to a top-level room), so a 3-level chain motj-ecosystem -> jonathan-contractor-motj -> b2-journey links each level UP one hop"
    - "Part 8: detect + heal are LOCAL fs + navigation.cjs only; ZERO Brain wire (BRAIN.md is enqueued, derived locally per Phase 90); Part 3/9: nothing auto-confirms -- the heal lands the room + the proposed-graph state and the human confirmed the heal itself at the gate (approvedBy)"
  artifacts:
    - path: "lib/core/graph-self-heal.cjs"
      provides: "detectUnsentineledArtifactFolder + healRoom: the GDH-08 detect + the GDH-09 full-citizen heal (birthRoom reuse + operational joint + NESTED_WITHIN graph joint + FEYNMAN timeline), approvedBy-gated, arbitrary-depth"
      min_lines: 130
      exports: ["detectUnsentineledArtifactFolder", "healRoom"]
    - path: "lib/core/navigation/room-birth.cjs"
      provides: "the `.room-root` sentinel JSON + the registry create gain an optional parent field (opts.parent), additive, byte-unchanged when parent is omitted"
      contains: "parent"
    - path: "tests/run-all-169.sh"
      provides: "registers test-sentinel-self-heal.cjs + test-room-lineage-edge.cjs + test-depth2-full-citizen.cjs in the phase gate"
      contains: "sentinel-self-heal"
  key_links:
    - from: "lib/core/graph-self-heal.cjs healRoom"
      to: "lib/core/navigation/room-birth.cjs birthRoom"
      via: "healRoom threads approvedBy + parent into birthRoom for full SEED-001 wiring (no hand-rolled scaffold)"
      pattern: "birthRoom|approvedBy"
    - from: "lib/core/graph-self-heal.cjs healRoom"
      to: "lib/core/navigation.cjs writeEdge NESTED_WITHIN"
      via: "the graph-navigable joint: a NESTED_WITHIN edge room:<child> -> room:<parent>"
      pattern: "NESTED_WITHIN|writeEdge"
    - from: "lib/core/graph-self-heal.cjs healRoom"
      to: "lib/core/feynman/timeline-runner.cjs refreshAll"
      via: "the GDH-09 temporal joint: refresh the healed room's ## Timeline (auto) from the room_created memory_event"
      pattern: "refreshAll|timeline-runner"
    - from: "lib/core/graph-self-heal.cjs detectUnsentineledArtifactFolder"
      to: "lib/core/room-root.cjs resolveRoomRoot + scripts/heal-command.cjs"
      via: "reuse the walk-up resolver + the sub-rooms-container precedent to find sentinel-less artifact folders"
      pattern: "resolveRoomRoot|room-root"
    - from: "lib/core/navigation/room-birth.cjs"
      to: ".room-root sentinel JSON + .rooms/registry.json"
      via: "opts.parent threads to BOTH the sentinel write and the registry create"
      pattern: "parent"
---

<objective>
WAVE 4 SURFACES, THE CONSTITUTIONAL CORE (GDH-08 + GDH-09 + the D-169-11 fractal joint). Land
lib/core/graph-self-heal.cjs: the detect + the full-citizen self-heal that makes a hand-built,
sentinel-less artifact folder (the live b2-journey) into a FIRST-CLASS room byte-indistinguishable from a
born room, joined to its parent BOTH operationally (registry parent + sentinel parent) AND graph-navigably
(the NESTED_WITHIN typed lineage edge from Plan 00). This is the plan the 8-agent fan-out found MISSING:
GDH-08/09 were in no plan, the b2 acceptance was unreachable, the typed lineage edge had no legal home, and
nothing composed at depth.

The heal is REUSE, not net-new scaffold (Part 7, D-169-10): healRoom INVOKES birthRoom (threading the
approvedBy from the Part 3 Decision Gate, the room-birth.cjs:316-318 hard gate) for the full SEED-001
wiring, then invokes the Phase 124 timeline-runner for the `## Timeline (auto)` temporal section. The
net-new is small and exactly what the fan-out named: (a) the detect of a sentinel-less artifact folder;
(b) the parent pointer in the `.room-root` sentinel JSON (room-birth.cjs:357-367 omits it today) + the
registry parent; (c) the NESTED_WITHIN graph-navigable edge; (d) the composition + the arbitrary-depth
linkage. The heal HALTS at the gate (approvedBy required; "why-not" captured on reject); it never indexes or
derives (Plan 05 owns the index+derive AFTER the heal -- the heal-first sequence).

Purpose: turn test-sentinel-self-heal.cjs + test-room-lineage-edge.cjs (RED from Plan 01) GREEN and provide
the heal the depth>=2 acceptance (Plan 06) + the backfill heal-first step (Plan 05) consume.
Output: lib/core/graph-self-heal.cjs + the room-birth.cjs parent-field amendment + the run-all-169.sh
registration of the heal/lineage tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-SPEC.md
@.planning/phases/169-graph-derivation-harness/169-CONTEXT.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-00-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
@.planning/phases/169-graph-derivation-harness/169-02-PLAN.md
@lib/core/navigation/room-birth.cjs
@lib/core/feynman/timeline-runner.cjs
@scripts/heal-command.cjs
@lib/core/navigation/edges.cjs
@scripts/room-registry
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract. This plan IMPLEMENTS:
`detectUnsentineledArtifactFolder(roomDir) -> [{folder, artifactCount}]` and
`healRoom({folder, parentRoomDir, slug, approvedBy, runTimeline}) -> {ok, roomDir, lineageEdge, ...}`.
It CONSUMES resolveRoomRoot (Plan 02), the NESTED_WITHIN frozen edge (Plan 00), birthRoom
(room-birth.cjs, amended here for opts.parent), and timeline-runner.refreshAll (Phase 124).
</shared_iface_contract>

<rules>
- D-169-10 / Part 7 REUSE: the heal INVOKES birthRoom for the scaffold + the Phase 124 timeline-runner for
  the temporal section. Do NOT hand-roll ROOM.md/STATE.md/MINTO.md/FEYNMAN scaffolding or the timeline
  render. Net-new is the detect + the parent pointer + the NESTED_WITHIN edge + the composition.
- D-169-11 (Option C, BOTH joints): the heal writes the OPERATIONAL joint (registry parent + sentinel
  parent) AND the GRAPH-NAVIGABLE joint (the NESTED_WITHIN edge). The graph edge is the frozen Plan-00 type;
  it is NOT PART_OF (illegal for a room target) and NOT BELONGS_TO (absent from the frozen navigation set).
- Part 3/9: healRoom REQUIRES approvedBy (the room-birth.cjs:316-318 gate); without it, refuse (no silent
  room creation). Nothing auto-confirms; the human approved the heal at the gate.
- D-169-11 arbitrary depth: the NESTED_WITHIN edge links the child to ITS parent (which may itself be a
  healed/born sub-room), so a 3-level chain links each level up one hop.
- Heal-first separation: healRoom does the detect+birth+joints+timeline and RETURNS; it never indexes or
  derives (Plan 05 runs rebuild + runDerivation into the healed db AFTER the heal).
- room-birth.cjs amendment is ADDITIVE: opts.parent default-omitted so existing born-room sentinels are
  byte-unchanged; when provided it threads to the sentinel JSON AND the registry create.
- Part 8: detect + heal are LOCAL fs + navigation.cjs only; ZERO Brain wire (BRAIN.md is enqueued, derived
  locally per Phase 90). The Plan 06 boundary scan covers graph-self-heal.cjs.
- NO em-dashes. CJS only. No new deps.
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: room-birth.cjs parent-field amendment (sentinel + registry)</name>
  <read_first>
    - lib/core/navigation/room-birth.cjs birthRoom :312 (the opts contract :294-307), the approvedBy gate
      :316-318, the `.room-root` sentinel JSON write :357-367 (writes {room,active,born}, NO parent), the
      registry create execSync :550-577 (passes slug/roomDir/vname/vstage, NO parent)
    - scripts/room-registry create stanza (the python create block; the registry already stores a parent
      field for some rooms -- polygon/mindrianOS -- so the data shape supports it; determine whether the
      create verb needs a parent arg OR a follow-on set-parent verb)
    - the live ~/MindrianRooms/.rooms/registry.json (polygon.parent / mindrianOS.parent confirm the field)
  </read_first>
  <behavior>
    - birthRoom({..., parent:'<slug>'}) writes the `.room-root` sentinel JSON with a `parent` field
    - birthRoom WITHOUT opts.parent writes the sentinel byte-identically to today ({room,active,born})
    - birthRoom with opts.parent records the parent in the registry entry (via the registry create/set-parent
      path)
    - the approvedBy gate is unchanged (still required)
  </behavior>
  <action>
    In lib/core/navigation/room-birth.cjs: (a) add `parent` to the opts contract comment + read
    `const parent = typeof options.parent === 'string' && options.parent.length > 0 ? options.parent : ''`.
    (b) At the sentinel write (:357-367), when parent is non-empty add `parent` to the sentinel object (keep
    {room,active,born} and append parent only when set, so the omitted case is byte-unchanged). (c) At the
    registry create (:550-577), when parent is non-empty record it: if scripts/room-registry already accepts
    a parent arg on create, pass it; otherwise add a minimal `set-parent <slug> <parent>` verb to
    scripts/room-registry (mirroring the create python idiom) and call it after create. Keep the create
    failure tolerated (the half-born guard). No behavior change when parent is omitted. No em-dashes.
  </action>
  <verify>
    <automated>node -e "require('./lib/core/navigation/room-birth.cjs')" && grep -q "parent" lib/core/navigation/room-birth.cjs && grep -q "options.parent\|opts.parent" lib/core/navigation/room-birth.cjs && echo ROOMBIRTH_PARENT_OK</automated>
  </verify>
  <done>birthRoom accepts opts.parent, threads it to the sentinel JSON + the registry, leaves the
  no-parent case byte-unchanged, the approvedBy gate is intact, and room-birth.cjs loads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: graph-self-heal.cjs -- detect + the full-citizen heal (birthRoom + joints + timeline)</name>
  <read_first>
    - lib/core/room-root.cjs resolveRoomRoot + findRoomRootSentinels (Plan 02 -- the walk-up to reuse)
    - scripts/heal-command.cjs:885-925 (assertIsRoom sentinel set + isContainerDir: the precedent for
      detecting a folder that holds artifacts but lacks `.room-root`)
    - lib/core/navigation/room-birth.cjs birthRoom (Task 1: the approvedBy + parent-threaded full wiring)
    - lib/core/navigation.cjs writeEdge (:113) + lib/core/navigation/edges.cjs NESTED_WITHIN (Plan 00) +
      lib/core/room-db.cjs openRoomDb (the healed room's db handle for the edge write)
    - lib/core/feynman/timeline-runner.cjs refreshAll(roomDir) (Phase 124 -- the temporal section)
    - 169-CONTEXT.md D-169-09 (the self-heal) + D-169-10 (full-citizen birthRoom reuse + net-new = parent edge
      + temporal) + D-169-11 (the operational + graph joints + arbitrary depth)
    - tests/test-sentinel-self-heal.cjs + tests/test-room-lineage-edge.cjs (the RED stubs this turns GREEN)
  </read_first>
  <behavior>
    - detectUnsentineledArtifactFolder(roomDir) returns folders under roomDir that hold artifacts
      (.md/.docx/.html) but have NO `.room-root` of their own
    - healRoom without approvedBy returns {ok:false, reason:'no_approval'} (no fs writes, no room created)
    - healRoom with approvedBy: the folder gains a `.room-root` (with a parent field) + own .mindrian/room.db
      + ROOM.md + STATE.md + MINTO.md + per-section FEYNMAN.md (via birthRoom), is registered with
      parent=<parent-slug>, gains a NESTED_WITHIN edge room:<child> -> room:<parent> in its room.db, and its
      FEYNMAN gains a `## Timeline (auto)` section (via timeline-runner)
    - healRoom returns the healed roomDir + the lineage edge result; it does NOT index/derive
    - healRoom on a folder whose parent is a sub-room links NESTED_WITHIN to THAT sub-room (arbitrary depth)
  </behavior>
  <action>
    Create lib/core/graph-self-heal.cjs. detectUnsentineledArtifactFolder(roomDir): read direct child
    directories of roomDir; for each, if it contains at least one .md/.docx/.html artifact but
    fs.existsSync(path.join(child,'.room-root')) is false, include {folder, artifactCount}; reuse the
    heal-command isContainerDir/assertIsRoom precedent for the sentinel check. healRoom({folder,
    parentRoomDir, slug, approvedBy, runTimeline}): (0) if !approvedBy return {ok:false, reason:'no_approval'}
    (the Part 3 gate). (1) Resolve the parent slug from parentRoomDir's `.room-root` (resolveRoomRoot +
    read the sentinel) and call birthRoom({roomDir:folder, slug, approvedBy, parent:<parent-slug>, ...the
    minimal venture defaults}) for the FULL SEED-001 wiring (REUSE -- do not hand-roll). (2) The operational
    joint is now done by birthRoom (sentinel parent + registry parent via Task 1). (3) Open the healed
    room.db via room-db.openRoomDb and write the GRAPH joint: navigation.writeEdge(db, {source_id:
    'room:'+slug, target_id:'room:'+parentSlug, edge_type:'NESTED_WITHIN', properties:{relation:'nested',
    parent:parentSlug}}); close the db. (4) If runTimeline !== false, invoke
    require('../feynman/timeline-runner.cjs').refreshAll(folder) so the healed room's FEYNMAN gains
    ## Timeline (auto) from the room_created memory_event. (5) return {ok:true, roomDir:folder, slug,
    parentSlug, lineageEdge:<the writeEdge result>}. Do NOT index or derive here (Plan 05 does that after).
    All LOCAL fs + navigation.cjs; zero Brain. Register tests/test-sentinel-self-heal.cjs +
    tests/test-room-lineage-edge.cjs + tests/test-depth2-full-citizen.cjs in tests/run-all-169.sh. No
    em-dashes.
  </action>
  <verify>
    <automated>node tests/test-sentinel-self-heal.cjs && node tests/test-room-lineage-edge.cjs && node -e "require('./lib/core/graph-self-heal.cjs')" && grep -q NESTED_WITHIN lib/core/graph-self-heal.cjs && grep -q refreshAll lib/core/graph-self-heal.cjs && grep -q "no_approval" lib/core/graph-self-heal.cjs && grep -q sentinel-self-heal tests/run-all-169.sh && echo SELF_HEAL_OK</automated>
  </verify>
  <done>test-sentinel-self-heal.cjs + test-room-lineage-edge.cjs pass: a sentinel-less folder is detected,
  healRoom refuses without approvedBy and on approve births a full-citizen room (ROOM.md + per-section
  FEYNMAN + own db) with a registry parent + a sentinel parent + a NESTED_WITHIN edge to its parent + a
  ## Timeline (auto) section, links to a sub-room parent at depth, never indexes/derives, and touches no
  Brain wire; the heal/lineage/depth2 tests are registered in run-all-169.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| sentinel-less folder -> heal | the heal writes a sentinel + births a room; gated by approvedBy (Part 3) |
| healed room node -> parent room node | the NESTED_WITHIN edge crosses the lineage joint; must be the legal frozen type, written via the chokepoint |
| heal -> Brain | the heal is LOCAL fs + navigation.cjs; BRAIN.md is enqueued, derived locally; zero Brain wire |
| parent slug -> sentinel/registry | the parent pointer crosses into the operational joint; must be a slug handle, never prose |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-21 | Elevation of Privilege | the heal creating a room silently without human approval | mitigate | healRoom REQUIRES approvedBy (the room-birth.cjs:316-318 gate); without it returns no_approval and writes nothing; the test asserts the refusal |
| T-169-22 | Tampering | the lineage edge written as an illegal type (PART_OF room target / BELONGS_TO bypass) | mitigate | the heal writes the frozen NESTED_WITHIN type (Plan 00) via navigation.writeEdge only; PART_OF + the legacy lazygraph path are not used |
| T-169-23 | Information Disclosure | a room/artifact body landing on the NESTED_WITHIN edge or in a Brain query | mitigate | edge properties enum/scalar only (relation enum + parent slug); the heal makes no Brain call; Plan 06 boundary-scans graph-self-heal.cjs |
| T-169-24 | Reliability | a healed room left as a bare sentinel + empty db (the GDH-09 anti-pattern) | mitigate | healRoom INVOKES birthRoom for the full SEED-001 wiring + timeline-runner for the temporal section; the depth2 test asserts the full-citizen markers |
| T-169-25 | Tampering | the parent-field amendment changing existing born-room sentinel bytes | mitigate | opts.parent is additive + default-omitted; the no-parent sentinel is byte-unchanged (asserted in Task 1) |
| T-169-SC | Tampering | npm installs | mitigate | zero new packages; no install task |
</threat_model>

<verification>
- node tests/test-sentinel-self-heal.cjs GREEN (detect + approvedBy gate + birthRoom reuse + registry/sentinel
  parent + ## Timeline (auto)).
- node tests/test-room-lineage-edge.cjs GREEN (NESTED_WITHIN edge present + walkable + enum/scalar props).
- room-birth.cjs accepts opts.parent (sentinel + registry), no-parent case byte-unchanged, loads.
- graph-self-heal.cjs makes zero Brain calls (Plan 06 boundary scan confirms).
- bash tests/run-all-169.sh registers + runs the heal/lineage/depth2 tests.
- No em-dashes anywhere.
</verification>

<success_criteria>
The constitutional core ships: a hand-built sentinel-less folder (the live b2-journey) is DETECTED and, on
the navigator's APPROVE at the Decision Gate, SELF-HEALED into a full-citizen room byte-indistinguishable
from a born room (birthRoom reuse), joined to its parent BOTH operationally (registry parent + sentinel
parent) AND graph-navigably (the NESTED_WITHIN lineage edge), with a ## Timeline (auto) temporal section
(timeline-runner reuse), composing at arbitrary depth, halting at the gate, touching no Brain wire. GDH-08 +
GDH-09 are now in a plan, the lineage edge has a legal home, and the fractal joint is wired both ways.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-07-SUMMARY.md` when done.
</output>
