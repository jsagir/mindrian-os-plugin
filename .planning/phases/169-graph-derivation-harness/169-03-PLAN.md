---
phase: 169-graph-derivation-harness
plan: 03
type: execute
wave: 3
depends_on: ["169-01"]
files_modified:
  - lib/core/doc-text-extractor.cjs
autonomous: true
requirements: [GDH-04]
canon_parts: [6, 8]

must_haves:
  truths:
    - "lib/core/doc-text-extractor.cjs exports extractDocText(absPath) that returns the body text of a .docx via node:zlib inflateRawSync over the zip word/document.xml w:t runs (RESEARCH Code Example 1, PROVEN against the b2 fixture: 216 Hebrew runs) and the body text of a .html via cheerio, '' for any other extension"
    - "the source file is NEVER mutated: extractDocText only reads; no sidecar .md is written; D-169-03 (non-destructive) holds and is asserted by the test (source bytes + mtime unchanged before/after)"
    - "the extractor is zip-bomb hardened (V5): it caps the ZIP central-directory entry count and caps the inflated output size, aborting over a sane cap (~10 MB of text), and returns '' on any parse fault (exit-safe, the hook's existing degrade pattern)"
    - "the extractor uses ONLY Node built-ins (node:fs, node:zlib) plus the already-vendored cheerio; ZERO new dependency (RESEARCH Package Legitimacy Audit)"
    - "method-0 (stored) and method-8 (deflate) zip entries are both handled (inflateRawSync only for method 8); Hebrew/UTF-8 decodes correctly via toString('utf8')"
  artifacts:
    - path: "lib/core/doc-text-extractor.cjs"
      provides: "extractDocText(absPath) pure-JS .docx (zip+inflate+w:t) + .html (cheerio) -> text; non-destructive; zip-bomb capped"
      min_lines: 60
      exports: ["extractDocText"]
  key_links:
    - from: "lib/core/doc-text-extractor.cjs"
      to: "node:zlib inflateRawSync"
      via: "method-8 entries inflated; method-0 read raw; output-size cap"
      pattern: "inflateRawSync"
    - from: "lib/core/doc-text-extractor.cjs"
      to: "cheerio"
      via: ".html body text extraction"
      pattern: "cheerio"
---

<objective>
WAVE 2 FOUNDATION (GDH-04). Land lib/core/doc-text-extractor.cjs, the pure-JS non-destructive .docx/.html
text extractor that makes the dense moat content (the 9 b2-journey .docx dossiers) reachable to the
indexer and the derivation. This closes root cause #3 (the indexer is .md-only). The .docx path is the
zip+inflate+w:t-run walk PROVEN this session against the real b2 fixture (216 Hebrew runs); the .html path
is cheerio (already a dep). Zero new dependency.

Purpose: turn tests/test-doc-text-extractor.cjs (RED from Plan 01) GREEN.
Output: lib/core/doc-text-extractor.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/169-graph-derivation-harness/169-01-PLAN.md
</context>

<shared_iface_contract>
See 169-01-PLAN.md shared_iface_contract for extractDocText. This plan IMPLEMENTS that signature:
`extractDocText(absPath) -> string` (.docx via zlib inflateRawSync over word/document.xml w:t runs with a
method-0 stored fallback; .html via cheerio body text; '' otherwise; never mutates the source; zip-bomb
capped).
</shared_iface_contract>

<rules>
- D-169-03: NON-DESTRUCTIVE. The extractor only READS. No sidecar .md, no source edit, no conversion. The
  test asserts source bytes + mtime unchanged.
- Part 8: pure LOCAL file read; the extracted text is DATA only (never eval/exec'd); zero network.
- V5 input validation: cap the central-directory entry count and the inflated output size (zip-bomb guard);
  return '' on any parse fault.
- No new dependency (built-ins + existing cheerio only). CJS only. NO em-dashes.
</rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extractDocText (.docx + .html), non-destructive, zip-bomb capped</name>
  <read_first>
    - 169-RESEARCH.md Code Example 1 (the verified EOCD + central-directory walk + inflateRawSync + w:t
      regex; method-0 stored fallback) and Pitfall 5 (inflateRawSync not inflateSync; method 0 vs 8; UTF-8)
    - tests/test-doc-text-extractor.cjs (the RED stub: b2 fixture skip-if-absent + synthetic stored-method
      fixture + .html via cheerio + source-bytes-unchanged assertion)
    - tests/fixtures/169/stored-method.docx + sample.html (the synthetic fixtures from Plan 01)
  </read_first>
  <behavior>
    - .docx (method 8): returns the joined w:t runs (non-empty for the b2 fixture / synthetic fixture)
    - .docx (method 0 stored): returns the w:t runs without inflating
    - .html: returns $('body').text() via cheerio, entities decoded
    - other extension: returns ''
    - malformed/huge zip: returns '' (no throw); inflated output capped at the sane limit
    - source file: bytes + mtime identical before and after the call
  </behavior>
  <action>
    Create lib/core/doc-text-extractor.cjs exporting extractDocText(absPath): branch on path.extname. For
    .docx, implement the RESEARCH Code Example 1 walk verbatim in spirit (find EOCD 0x06054b50, walk the
    central directory to locate word/document.xml, read the local header to find the data start, inflate
    method-8 via zlib.inflateRawSync with a maxOutputLength size cap, read method-0 raw, regex-pull
    `<w:t[^>]*>([^<]*)</w:t>` runs, join with spaces); cap the central-directory loop at a sane entry count.
    For .html, load with cheerio and return $('body').text() (fall back to $.root().text() if no body). Wrap
    the whole body in try/catch returning '' on any fault. Never open the file for write. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-doc-text-extractor.cjs</automated>
  </verify>
  <done>tests/test-doc-text-extractor.cjs passes: > 0 runs for the synthetic stored-method fixture (and the
  b2 fixture when present), .html text via cheerio, source bytes/mtime unchanged, '' on a malformed input,
  and no new dependency added (verify package.json unchanged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| .docx/.html file bytes -> extractor | attacker-influenceable file content parsed in-process |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-04 | Denial of Service | a zip-bomb .docx (tiny compressed, huge inflated) | mitigate | inflateRawSync maxOutputLength size cap + central-directory entry-count cap; abort over ~10 MB; return '' |
| T-169-05 | Denial of Service | a malformed zip / missing document.xml | mitigate | try/catch wrapping the whole body returns '' (the hook's exit-safe degrade) |
| T-169-06 | Tampering | the extractor mutating the navigator's authored .docx | mitigate | read-only; no write/open-for-write; the test asserts source bytes + mtime unchanged (D-169-03) |
| T-169-SC | Tampering | a new docx npm dep (mammoth/adm-zip) | mitigate | REJECTED in RESEARCH; built-ins + existing cheerio only; verify package.json unchanged |
</threat_model>

<verification>
- node tests/test-doc-text-extractor.cjs GREEN.
- package.json dependencies unchanged (zero new deps).
- bash tests/run-all-169.sh shows the extractor suite GREEN.
</verification>

<success_criteria>
The dense .docx/.html moat content is reachable to the indexer/derivation, non-destructively, with a
zip-bomb guard and zero new deps. Root cause #3 closed. Wave-2 extractor barrier green.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-03-SUMMARY.md` when done.
</output>
