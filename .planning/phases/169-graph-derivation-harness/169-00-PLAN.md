---
phase: 169-graph-derivation-harness
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/edges.cjs
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-edges-room-lineage-floor.cjs
  - tests/run-all-169.sh
autonomous: false
requirements: [GDH-09, D-169-11]
canon_parts: [4, 6, 8, 9]

must_haves:
  truths:
    - "writeEdge accepts a new room-lineage edge type NESTED_WITHIN (child-room-node -> parent-room-node) and still rejects a made-up edge type, so the D-169-11 graph-navigable joint has a LEGAL frozen-set representation that the rollup/hierarchy query can walk (this is the constitutional prerequisite the 8-agent fan-out found MISSING: PART_OF is illegal for a room target, BELONGS_TO is not in the navigation frozen set at all)"
    - "NESTED_WITHIN is added ADDITIVELY to the frozen ALLOWED_EDGE_TYPES closed set in lib/core/navigation/edges.cjs, mirroring the Phase 163-01 / 150.8 / 168-01 additive-block idiom verbatim; every prior frozen edge type still passes (FLOOR) and the Set stays Object.frozen"
    - "the choice is NESTED_WITHIN (a NEW type), NOT a widening of PART_OF endpoints: PART_OF is consumed by typed-domain.cjs + get-domains-for-trends.cjs as the domain-taxonomy structural edge whose targets are domain/subdomain/focus_area; adding room->room PART_OF would pollute those taxonomy traversals with non-taxonomy endpoints (a Part 4 self-CONTRADICTS), so a dedicated lineage type is the clean, navigable, non-colliding representation"
    - "NESTED_WITHIN appears in the Part 4 typed-edge list in docs/MINDRIAN-CANON.md AND in a new Appendix D entry 23, the canon header/footer version line bumps 1.11 -> 1.12, and a CANON-PHASE-MAP version-history row + the Phase 169 map row record the amendment (the canon-amendment-on-itself mechanism, Part 6 dog-fooding)"
    - "edge properties stay ENUM/scalar ONLY (a relation enum + the parent slug handle); no prose, no room/artifact BODY ever lands on the edge; NESTED_WITHIN never crosses to Brain (Part 8)"
    - "the amendment is RATIFIED at a BLOCKING navigator checkpoint BEFORE the bytes land (D-169-11 navigator-gated), and lands as ONE atomic lockstep wave (edges.cjs + canon + phase-map + floor test + run-all registration) so CI never goes RED mid-phase"
    - "the new floor test asserts membership + the full prior FLOOR + frozen-Set + a writeEdge round-trip on a room->room NESTED_WITHIN + a made-up-type negative, never asserting .size"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "ALLOWED_EDGE_TYPES extended with NESTED_WITHIN via one additive block (the room-lineage edge)"
      contains: "NESTED_WITHIN"
    - path: "tests/test-edges-room-lineage-floor.cjs"
      provides: "membership + FLOOR + frozen-Set + room->room writeEdge round-trip + made-up-type-negative (never .size)"
      min_lines: 45
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Part 4 edge list gains NESTED_WITHIN + Appendix D entry 23 + version 1.11 -> 1.12"
      contains: "NESTED_WITHIN"
    - path: "docs/CANON-PHASE-MAP.md"
      provides: "Phase 169 row + a v1.12 version-history row recording the amendment"
      contains: "NESTED_WITHIN"
    - path: "tests/run-all-169.sh"
      provides: "the phase aggregator registers the room-lineage floor test alongside the carried 168 floor test"
      contains: "test-edges-room-lineage-floor"
  key_links:
    - from: "lib/core/navigation/edges.cjs"
      to: "tests/test-edges-room-lineage-floor.cjs"
      via: "require ALLOWED_EDGE_TYPES + writeEdge; assert NESTED_WITHIN membership + round-trip"
      pattern: "NESTED_WITHIN"
    - from: "docs/MINDRIAN-CANON.md"
      to: "docs/CANON-PHASE-MAP.md"
      via: "version-history row references the same Appendix D entry 23"
      pattern: "169"
    - from: "tests/run-all-169.sh"
      to: "tests/test-edges-room-lineage-floor.cjs + tests/test-edges-part4-cascade-floor.cjs"
      via: "the phase gate runs the new room-lineage floor + the carried 168 cascade floor"
      pattern: "room-lineage-floor"
---

<objective>
WAVE 1 FOUNDATION-A (the constitutional prerequisite). Mint the room-lineage edge NESTED_WITHIN into the
frozen ALLOWED_EDGE_TYPES closed set so the D-169-11 fractal joint has a LEGAL, graph-navigable
representation. The 8-agent ICM/fractal fan-out (verdict MISSING) proved the joint has no legal home today:
PART_OF (edges.cjs:368, comment :343-348) allows ONLY domain/subdomain/focus_area targets so a room is an
illegal PART_OF target (writeEdge:451 checks type-membership but NOT endpoints, so a room->room PART_OF
writes SILENTLY but breaches the frozen-endpoint contract -- a Part 4 self-CONTRADICTS); and BELONGS_TO is
NOT in the navigation frozen set at all (it lives only in the legacy lazygraph EDGE_TYPES array
:26, written via raw SQL not the chokepoint), so a child-room BELONGS_TO parent-room via
navigation.writeEdge would be REJECTED (invalid_edge_type).

The cleanest LEGAL representation is a NEW dedicated lineage type, NESTED_WITHIN (child-room-node ->
parent-room-node), NOT a widening of PART_OF endpoints. PART_OF is consumed by typed-domain.cjs (:64, :208)
and get-domains-for-trends.cjs as the domain-taxonomy structural edge expecting a taxonomy node on the far
end; bending it to also mean room-lineage would pollute every domain-walk traversal with non-taxonomy
endpoints. NESTED_WITHIN is unambiguous, walkable by the rollup + a hierarchy query, and expresses the
ICM/Simon nested-near-decomposable-hierarchy claim directly (the nested folder hierarchy IS the graph).

This MOVES a frozen constitutional set, so it is a NAVIGATOR-GATED canon amendment (D-169-11), ratified at a
BLOCKING checkpoint BEFORE the bytes land, mirroring Appendix D entries 18 (Phase 150.8) / 21 (Phase 163) /
22 (Phase 168). It lands as ONE atomic lockstep wave (edges.cjs additive block + canon Part 4 list +
Appendix D entry 23 + version 1.11 -> 1.12 + CANON-PHASE-MAP rows + the canonical floor test + the run-all
registration) so CI never goes RED mid-phase. The healed-room lineage edge (Plan 07) and the rollup walk
(Plan 04) BOTH consume this type, so it is Wave 1 -- before any consumer.

Purpose: give D-169-11's graph-navigable joint a legal frozen-set home; unblock Plans 04 + 07.
Output: an extended ALLOWED_EDGE_TYPES Set, the amended canon + phase map, the floor test, and the
run-all-169.sh registration of the floor test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-graph-derivation-harness/169-SPEC.md
@.planning/phases/169-graph-derivation-harness/169-CONTEXT.md
@.planning/phases/169-graph-derivation-harness/169-RESEARCH.md
@.planning/phases/163-trending-to-absurd-harness/163-01-PLAN.md
@lib/core/navigation/edges.cjs
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
@tests/test-edges-domain-taxonomy-floor.cjs
</context>

<shared_iface_contract>
This plan MINTS the room-lineage edge the shared IFACE (169-01) and Plans 04/07 cite:
- `NESTED_WITHIN` is a member of ALLOWED_EDGE_TYPES (edges.cjs). Semantics: source = a healed/born CHILD
  room node id (`room:<child-slug>`), target = its PARENT room node id (`room:<parent-slug>`). It is the
  graph-navigable fractal joint (D-169-11): a graph walk traverses the nested hierarchy up to the parent and
  (read-side, via the rollup) down to children. Properties are ENUM/scalar ONLY: `{ relation:'nested',
  parent: '<parent-slug>' }` -- never prose, never a room/artifact body.
- It is written through `navigation.writeEdge` (the Part 9 chokepoint) ONLY -- never the legacy lazygraph
  raw-SQL path, never typed-domain.cjs (which is domain-taxonomy-scoped).
</shared_iface_contract>

<rules>
- D-169-11: NESTED_WITHIN is a navigator-GATED frozen-set move; ratify at the blocking checkpoint BEFORE the
  bytes land. ONE atomic lockstep wave (code + canon + phase-map + floor test + run-all) so CI never goes RED.
- Choice rationale is LOCKED: a NEW type (NESTED_WITHIN), NOT a PART_OF endpoint widening (PART_OF stays the
  domain-taxonomy edge; do NOT touch its documented endpoints or typed-domain.cjs).
- Part 4: edges only from the frozen set; the additive block mirrors the 163-01 / 150.8 / 168-01 idiom
  verbatim (comment block naming the navigator-gated provenance + the ENUM-only property constraint).
- Part 8: edge properties ENUM/scalar ONLY (relation enum + parent slug handle); NESTED_WITHIN never crosses
  to Brain; cross-room aggregation of the edge is forbidden (it is a LOCAL room.db edge in the child's db).
- The floor test asserts membership + the full prior FLOOR + frozen-Set + a room->room writeEdge round-trip
  + a made-up-type negative; NEVER asserts .size (the edges.cjs FLOOR doctrine, :30-31).
- NO em-dashes (hyphens only). CJS / markdown only. No new deps.
</rules>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Ratify the room-lineage edge representation before any bytes land (D-169-11 navigator-gated).</decision>
  <context>
    The D-169-11 fractal joint needs a LEGAL, graph-navigable typed edge between a child room node and its
    parent room node. The 8-agent fan-out found no legal home exists today. The planner investigated all
    three options the directive named and recommends OPTION A (mint NESTED_WITHIN). This moves the frozen
    constitutional ALLOWED_EDGE_TYPES set, so per Part 4 + the Phase 108 frozen-taxonomy contract it requires
    navigator ratification BEFORE the edges.cjs bytes land (the same gate run for Appendix D entries
    18/21/22).
  </context>
  <options>
    <option id="option-a">
      <name>Mint NESTED_WITHIN (a new room-lineage edge type) -- RECOMMENDED</name>
      <pros>Unambiguous; cannot collide with the domain-taxonomy PART_OF traversals (typed-domain.cjs / get-domains-for-trends.cjs); expresses the ICM/Simon nested-hierarchy claim directly; written cleanly through the navigation.writeEdge chokepoint; mirrors the well-trodden additive-amendment idiom (entries 18/21/22).</pros>
      <cons>Adds one type to the frozen set (a navigator-gated amendment is required -- this checkpoint).</cons>
    </option>
    <option id="option-b">
      <name>Widen PART_OF endpoints to also accept a room/sub-room target</name>
      <pros>No new type; PART_OF is already "structural membership from any node."</pros>
      <cons>PART_OF is consumed as the domain-taxonomy edge (targets domain/subdomain/focus_area); room->room PART_OF would pollute every domain-walk query with non-taxonomy endpoints -- a Part 4 self-CONTRADICTS. Still a frozen-set (endpoint-contract) move, so it needs the same gate with worse semantics.</cons>
    </option>
    <option id="option-c">
      <name>Reuse BELONGS_TO (the legacy structural artifact-to-section edge)</name>
      <pros>BELONGS_TO already means structural membership in lazygraph.</pros>
      <cons>BELONGS_TO is NOT in the navigation frozen set -- writeEdge would REJECT it; writing it would require the legacy lazygraph raw-SQL path, BYPASSING the Part 9 chokepoint. Illegal via the canonical door.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (mint NESTED_WITHIN), option-b (widen PART_OF), or option-c (BELONGS_TO). The planner recommends option-a; the rest of this plan assumes it.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Extend ALLOWED_EDGE_TYPES with NESTED_WITHIN + ship the room-lineage floor test</name>
  <files>lib/core/navigation/edges.cjs, tests/test-edges-room-lineage-floor.cjs, tests/run-all-169.sh</files>
  <read_first>
    - lib/core/navigation/edges.cjs ALLOWED_EDGE_TYPES (:32-423) + the 168-01 CONVERGES/INVALIDATES/ENABLES
      additive block (:371-422, the most recent additive idiom to mirror) + writeEdge (:440-471, type-only
      gate, no endpoint check) + the FLOOR doctrine comment (:30-31)
    - tests/test-edges-domain-taxonomy-floor.cjs + tests/test-edges-part4-cascade-floor.cjs (the canonical
      floor-test shape: membership + FLOOR + frozen-Set + per-edge writeEdge round-trip on an in-memory
      node:sqlite db + made-up-type negative; never .size)
    - 169-CONTEXT.md D-169-11 (the fractal joint + the navigator-gated representation)
  </read_first>
  <behavior>
    - Test 1: ALLOWED_EDGE_TYPES.has('NESTED_WITHIN') === true.
    - Test 2: every prior frozen type still present (FLOOR): DEFERRED, REJECTED, DERIVED_FROM,
      FILED_AS_DECISION, FOLLOWS_FROM, OPERATOR_TRANSITION, INFORMS, REJECTED_BECAUSE, CONTRADICTS, SUPERSEDES,
      AFFILIATED_WITH, PIVOTED, SELECTED_REACH, FEEDS_INTO, VALIDATES, STATES, SUPPORTS, DESCRIBES, REFINES,
      ROOT_CAUSES, INSTANTIATES, DECOMPOSED_INTO, PART_OF, TAGGED_WITH, RELATED_TO, CONVERGES, INVALIDATES,
      ENABLES.
    - Test 3: ALLOWED_EDGE_TYPES is a frozen Set instance (Object.isFrozen true).
    - Test 4: writeEdge(db, {source_id:'room:child', target_id:'room:parent', edge_type:'NESTED_WITHIN',
      properties:{relation:'nested', parent:'parent'}}) returns ok on an in-memory node:sqlite db with an
      edges table; writeEdge with edge_type 'MADE_UP_LINEAGE_EDGE' returns {ok:false,
      reason:'invalid_edge_type'}.
    - NEVER assert .size.
  </behavior>
  <action>
    In lib/core/navigation/edges.cjs, append ONE additive block after the Phase 168-01 ENABLES entry (after
    :422), mirroring the 168-01 comment idiom verbatim: a header naming Phase 169-00, D-169-11, the
    navigator-gated provenance, the ENUM/scalar-only property constraint (relation enum + parent slug handle,
    never prose, never crosses to Brain), the rationale that this is the room-lineage joint (a NEW type, NOT a
    PART_OF widening, since PART_OF is domain-taxonomy-scoped), and the source/target semantics
    (room:<child> -> room:<parent>), then add `'NESTED_WITHIN',` to the frozen Set. Do NOT touch PART_OF or
    typed-domain.cjs. Create tests/test-edges-room-lineage-floor.cjs implementing the four behaviors above
    against an in-memory node:sqlite db (mirror test-edges-part4-cascade-floor.cjs's db setup + round-trip).
    Register tests/test-edges-room-lineage-floor.cjs in tests/run-all-169.sh next to the carried
    test-edges-part4-cascade-floor.cjs (if run-all-169.sh does not yet exist because Plan 01 has not run,
    create the minimal aggregator scaffold here and Plan 01 finalizes it; otherwise add the one registration
    line). No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-edges-room-lineage-floor.cjs && node tests/test-edges-part4-cascade-floor.cjs && grep -q "'NESTED_WITHIN'" lib/core/navigation/edges.cjs && ! grep -q "domain/subdomain/focus_area ONLY.*room" lib/core/navigation/edges.cjs && grep -q room-lineage-floor tests/run-all-169.sh && echo LINEAGE_EDGE_OK</automated>
  </verify>
  <done>NESTED_WITHIN is a frozen-set member, the room-lineage floor test passes (membership + FLOOR +
  frozen-Set + room->room round-trip + made-up-type negative, no .size), the carried 168 floor test still
  passes (frozen prior vocabulary untouched), PART_OF + typed-domain.cjs are untouched, and the floor test is
  registered in run-all-169.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Record the amendment in the canon + the phase map (the docs lockstep)</name>
  <files>docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - docs/MINDRIAN-CANON.md Part 4 typed-edge list (the closed-set prose) + Appendix D entry 22 (the most
      recent, Phase 168, the entry voice to mirror) + the header/footer Version line (currently 1.11)
    - docs/CANON-PHASE-MAP.md the "v1.14.0 execution order + phase additions" section + the Version history
      table (the v1.11 row to mirror)
  </read_first>
  <action>
    In docs/MINDRIAN-CANON.md: (a) add NESTED_WITHIN to the Part 4 typed-edge vocabulary prose (the
    structural-and-lineage edges clause) naming it the room-lineage edge (child room PART of nested parent
    room) added by navigator-gated amendment (Appendix D entry 23). (b) Add Appendix D entry 23 in the entry
    voice of entries 21/22: Phase 169 minted NESTED_WITHIN to give the D-169-11 fractal joint a legal
    graph-navigable representation (PART_OF illegal for a room target, BELONGS_TO absent from the frozen set);
    a NEW type, not a PART_OF widening, to avoid colliding with the domain-taxonomy traversals; navigator-
    gated (D-169-11) ratified at a blocking checkpoint before the bytes landed via the Part 6
    dog-fooding canon-amendment-on-itself mechanism (mirroring entries 14/15/18/19/21/22); ENUM/scalar-only
    props, never prose, never crosses to Brain; ONE atomic lockstep wave; floor test fence green. (c) Bump the
    header AND footer Version line 1.11 -> 1.12. In docs/CANON-PHASE-MAP.md: update the canon reference line
    to v1.12, add a Phase 169 row to the "v1.14.0 execution order + phase additions" section (canon_parts
    4/6/8/9; the NESTED_WITHIN lineage amendment + the self-heal harness), and add a v1.12 version-history row
    recording the amendment in the v1.11-row voice. No em-dashes.
  </action>
  <verify>
    <automated>grep -q NESTED_WITHIN docs/MINDRIAN-CANON.md && grep -q "entry 23\|23\." docs/MINDRIAN-CANON.md && grep -qE "Version: 1\.12|v1\.12" docs/MINDRIAN-CANON.md && grep -q NESTED_WITHIN docs/CANON-PHASE-MAP.md && grep -qE "v1\.12|1\.12" docs/CANON-PHASE-MAP.md && echo CANON_RECORDED</automated>
  </verify>
  <done>NESTED_WITHIN is in the Part 4 edge list + a new Appendix D entry 23; the canon version is 1.12 in
  both header and footer; CANON-PHASE-MAP carries the Phase 169 row + a v1.12 version-history row; no
  em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| frozen edge set -> writeEdge gate | a new edge type crosses the constitutional vocabulary; navigator-gated |
| NESTED_WITHIN edge -> Brain | the lineage edge is a LOCAL room.db edge; must never cross to Brain (Part 8) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-00-01 | Tampering | a room->room edge written via an illegal type (PART_OF endpoint breach or BELONGS_TO bypass) | mitigate | mint the legal NESTED_WITHIN type; PART_OF + typed-domain untouched; the lineage edge is written only through navigation.writeEdge (the chokepoint), proven by the floor-test round-trip |
| T-169-00-02 | Information Disclosure | prose / a room body landing on the lineage edge and leaking | mitigate | ENUM/scalar-only properties (relation enum + parent slug); the additive comment block names the constraint; Part 8 boundary scan in Plan 06 covers the writer |
| T-169-00-03 | Tampering | the frozen-set move skipping navigator ratification | mitigate | the blocking checkpoint:decision task gates the edges.cjs bytes; the docs lockstep records the navigator-gated provenance |
| T-169-SC | Tampering | npm installs | mitigate | zero new packages; no install task |
</threat_model>

<verification>
- node tests/test-edges-room-lineage-floor.cjs GREEN (membership + FLOOR + frozen-Set + room->room round-trip
  + made-up-type negative).
- node tests/test-edges-part4-cascade-floor.cjs GREEN (the frozen prior vocabulary is untouched).
- NESTED_WITHIN is in edges.cjs + canon Part 4 + Appendix D entry 23; canon version 1.12; phase-map rows
  present; PART_OF + typed-domain.cjs untouched.
- No em-dashes anywhere.
</verification>

<success_criteria>
The D-169-11 fractal joint has a LEGAL, graph-navigable frozen-set representation: NESTED_WITHIN, minted via
a navigator-gated atomic lockstep amendment, written only through the Part 9 chokepoint, floor-fenced, and
recorded in the canon + phase map. Plans 04 (rollup walk) and 07 (the heal lineage edge) can now write it.
</success_criteria>

<output>
Create `.planning/phases/169-graph-derivation-harness/169-00-SUMMARY.md` when done.
</output>
