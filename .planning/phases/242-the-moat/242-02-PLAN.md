---
phase: 242-the-moat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-kuzu-reintroduction.cjs
  - tests/test-242-kuzu-reintroduction-gate.cjs
  - scripts/verify-release
  - docs/MOAT-MANDATE.md
autonomous: true
requirements: [MOAT-02]
canon_parts: [7]

must_haves:
  truths:
    - "Seeding one live kuzu require or one kuzu dependency line makes node scripts/check-kuzu-reintroduction.cjs exit 1"
    - "The current, unmodified tree makes the same command exit 0"
    - "scripts/verify-release runs the gate and fails the release on exit 1"
    - "The stale 'Does this work without KuzuDB edges?' checklist line is gone from docs/MOAT-MANDATE.md, replaced by a machine-checked warning-sign item that keeps the list's checked-means-bad polarity"
    - "The gate cannot be silently neutered by growing its allowlist"
  artifacts:
    - path: "scripts/check-kuzu-reintroduction.cjs"
      provides: "Release-gate scan for kuzu dependency-manifest entries and live require/import statements; exit 0/1/2 contract"
      min_lines: 90
      contains: "ALLOWLIST"
    - path: "tests/test-242-kuzu-reintroduction-gate.cjs"
      provides: "Hermetic scratch-root fixtures (clean, seeded-require, seeded-dep, seeded-lock, missing-root), live-tree pass, allowlist-size fence, doc-replacement assertion, verify-release wiring assertion, Part 8 sweep of the new script"
      min_lines: 120
    - path: "scripts/verify-release"
      provides: "Section 17 gate wiring immediately after the section 16 Stop-hook schema gate"
      contains: "check-kuzu-reintroduction.cjs"
    - path: "docs/MOAT-MANDATE.md"
      provides: "Machine-checked warning-sign item replacing the dead KuzuDB prose at line 96"
      contains: "check-kuzu-reintroduction.cjs"
  key_links:
    - from: "scripts/verify-release"
      to: "scripts/check-kuzu-reintroduction.cjs"
      via: "node invocation with exit-code capture, mirroring the STOP_SCHEMA_OUT block"
      pattern: "check-kuzu-reintroduction\\.cjs"
    - from: "docs/MOAT-MANDATE.md"
      to: "scripts/check-kuzu-reintroduction.cjs"
      via: "PR checklist pointer replacing the dead prose"
      pattern: "check-kuzu-reintroduction\\.cjs"
    - from: "tests/test-242-kuzu-reintroduction-gate.cjs"
      to: "scripts/check-kuzu-reintroduction.cjs"
      via: "spawnSync with --root against scratch fixtures and against the real tree"
      pattern: "--root"
---

<objective>
MOAT-02: replace the PR checklist's dead KuzuDB warning sign with a real, machine-checked assertion.

`docs/MOAT-MANDATE.md` line 96 still asks reviewers "Does this work without KuzuDB edges?" even though the same file's own correction banner at line 3 records that the local graph moved off KuzuDB to `node:sqlite` on 2026-06-14. That is doctrine rot: a checklist item asking a human to judge against a database this repo no longer has. The audit's rethink verdict scoped it as a light doc fix, not an RCA cycle.

Purpose: turn one unfalsifiable prose question into a deterministic gate that fails a release if a retired graph engine re-enters the dependency surface, and passes on the tree as it stands today.
Output: a new release-gate script, its wiring into `scripts/verify-release`, the corrected checklist line, and a test that proves the gate both fires and passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/242-the-moat/242-RESEARCH.md
@.planning/phases/242-the-moat/242-PATTERNS.md
@.planning/phases/242-the-moat/242-VALIDATION.md
</context>

<planner_resolutions>
All three RESEARCH.md Open Questions are resolved. Do not re-open them during execution.

**Open Question 2 (gate placement): CONFIRMED as `scripts/verify-release` only, not folded into `scripts/doctor.cjs`'s coverage-gate organ.**
Reason: the closest sibling precedent, `scripts/check-hook-schema-compatibility.cjs`, lives only in `verify-release`; doctor's coverage-gate organ reports coverage of DECLARED surfaces (connector, projection, render, skill-mirrors, shape-declaration), which is a different question from a forbidden-dependency scan; and a kuzu reintroduction can only arrive through a commit, so release time is the moment it must be caught. Folding it into doctor later is additive and non-breaking if a reason to want it in the day-to-day report ever appears.

**Open Question 1 (shared `withTransaction` helper): DECLINED** (Plan 01 owns this; restated here so both plans agree). Reason: Phase 236 is concurrently editing `lib/core/lazygraph-ops.cjs` in a sibling worktree, so extracting a shared helper into that file would manufacture a merge collision. Phase 242 touches no file under `lib/`.

**Open Question 3 (crash-injection seam): RESOLVED as `MINDRIAN_HSI_CRASH_TEST_DELAY_MS`** (Plan 01 owns this; no bearing on Plan 02).

**Assumption A1 from RESEARCH.md is now VERIFIED, not assumed.** `npm view kuzu` returns `name = 'kuzu'`, `version = '0.11.3'`, and `npm view kuzu-wasm` returns `name = 'kuzu-wasm'`, `version = '0.11.3'` (checked live at plan time). The real package family is a bare `kuzu` plus `kuzu-`-prefixed siblings, so the regexes below are correct as written; the scoped `@kuzudb/*` alternative is kept as cheap forward insurance.

**Census confirming the gate passes green on day one** (checked live at plan time, so the executor does not have to rediscover it): `git grep -nE "(require\(\s*['\"]kuzu|from\s+['\"]kuzu|import\s+['\"]kuzu)" -- '*.cjs' '*.js' '*.mjs'` returns ZERO hits; `grep -c kuzu package.json package-lock.json` returns 0 and 0; `node_modules` is not tracked. The six tracked JS files that DO contain the string kuzu are all legitimate and must NOT trip the gate: `bin/mindrian-tools.cjs` (the `case 'build-kuzu':` back-compat subcommand label), `lib/core/graph-ops.cjs` (the `buildGraphFromKuzu` back-compat export alias whose body is SQLite), and `scripts/causal-to-graph.cjs`, `scripts/hsi-to-graph.cjs`, `scripts/migrate-lazygraph.cjs`, `scripts/whitespace-to-graph.cjs` (historical "Migrated from X-to-kuzu.cjs" header comments). That is exactly why the scan is scoped to require/import statements and manifest dependency keys rather than a blanket string search (RESEARCH.md Pitfall 3).

**Polarity correction (planner catch, overriding the replacement text RESEARCH.md and PATTERNS.md propose).** Both propose replacing line 96 with "`scripts/check-kuzu-reintroduction.cjs` passes ...". That is wrong for its location: line 96 sits inside `### Surface Area Warning Signs`, a list whose items at lines 93-97 all mean checked-equals-bad, and whose rule at line 99 reads "If 3+ warning signs are checked, the PR adds surface area." A "the gate passes" item would mean checked-equals-good and silently corrupt that rule's arithmetic. The replacement in Task 2 keeps warning-sign polarity while still being machine-checked.

**Naming reconciliation (shared with Plan 01):** VALIDATION.md names the test `tests/test-kuzu-reintroduction-gate.cjs` while also specifying a `tests/run-all-242.sh` aggregator that glob-discovers `tests/test-242-*`. The canonical name is `tests/test-242-kuzu-reintroduction-gate.cjs`, because the glob contract is what lets plans add coverage without editing the aggregator.

**Wave-1 parallelism note:** `tests/run-all-242.sh` is created by Plan 01 and discovers this plan's test file by glob with zero edits here, so the two plans share no file and can execute in the same wave. This plan's per-task verification therefore runs its own test file directly; the aggregator run is the post-wave phase gate.
</planner_resolutions>

<artifacts_this_phase_produces>
Every symbol Phase 242 creates. Nothing below exists in the tree today, so drift verification must not treat any of these as pre-existing.

**New files (both plans):**
- `scripts/check-kuzu-reintroduction.cjs` (Plan 02)
- `tests/test-242-kuzu-reintroduction-gate.cjs` (Plan 02)
- `tests/test-242-hsi-to-graph-transaction.cjs` (Plan 01)
- `tests/run-all-242.sh` (Plan 01)

**New env vars:**
- `MINDRIAN_HSI_CRASH_TEST_DELAY_MS` (Plan 01, test-only seam in `scripts/hsi-to-graph.cjs`)

**New literal protocol strings:**
- `HSI-CRASH-SEAM: in-transaction` (Plan 01, one-shot stderr marker)

**New CLI surface / exit-code contracts:**
- `node scripts/check-kuzu-reintroduction.cjs [--root <dir>]`; exit 0 = clean, exit 1 = forbidden kuzu dependency or require/import found (release blocked), exit 2 = scanner failure (root missing, package.json missing or unparseable, or zero scannable files found)

**New identifiers inside new files:**
- `scripts/check-kuzu-reintroduction.cjs`: `KUZU_DEP_RE`, `KUZU_LOCK_PATH_RE`, `KUZU_REQUIRE_RE`, `ALLOWLIST`, `SKIP_DIRS`, `SCAN_EXTENSIONS`, `resolveRoot`, `walkSourceFiles`, `scanManifest`, `scanSourceFile`
- `tests/test-242-kuzu-reintroduction-gate.cjs`: `makeScratchRoot`, `runGate`, `seedRequireFixture`, `seedDependencyFixture`, `seedLockFixture`
- `tests/test-242-hsi-to-graph-transaction.cjs` (Plan 01): `createSeededRoom`, `writeHsiFixture`, `snapshotScoringEdges`, `spawnUntilSeam`, `buildMutantScript`
- `tests/run-all-242.sh` (Plan 01): `run`, `strip_comments`, `PART8_RE`, `PART8_TARGETS`
- `scripts/verify-release`: section heading `17. Kuzu Reintroduction Gate`, shell vars `KUZU_GATE_OUT`, `KUZU_GATE_CODE`

**New doc line:** the replacement warning-sign bullet at `docs/MOAT-MANDATE.md` line 96.

**Explicitly NOT produced:** no `withTransaction` helper, no new npm dependency, no `scripts/doctor.cjs` change, no change to any file under `lib/`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the kuzu-reintroduction release gate</name>
  <files>scripts/check-kuzu-reintroduction.cjs</files>
  <read_first>
    - `scripts/check-hook-schema-compatibility.cjs` (whole file, 232 lines) - the exact scaffold to copy: the exit-code doc block at lines 39-43, the `node:fs` / `node:path` / `ROOT` imports at lines 45-51, the self-identifying forbidden regex at line 61, the ALLOWLIST Set with its written reason at lines 63-68, the scanner-failure exit-2 path at lines 70-78, the per-line `scanFile` at lines 170-192, and the enumerate-every-violation `main` at lines 194-231 (it never short-circuits on the first hit).
    - `docs/MOAT-MANDATE.md` lines 1-10 (the 2026-06-14 KuzuDB-drift correction banner) and lines 85-100 (the checklist whose prose this gate replaces).
    - `.planning/phases/242-the-moat/242-RESEARCH.md`, section "MOAT-02: Concrete Target and Assertion Design" (the census table and the two-check design) and "Common Pitfalls" Pitfall 3 (too broad versus too narrow).
    - This plan's `<planner_resolutions>` census block, which already lists the six legitimate kuzu-mentioning files the gate must not flag.
  </read_first>
  <action>
Create `scripts/check-kuzu-reintroduction.cjs`, `'use strict'`, requiring only `node:fs` and `node:path`. Structure it identically to `scripts/check-hook-schema-compatibility.cjs`.

**Header doc block.** State what the gate does; why it exists (it replaces the dead `docs/MOAT-MANDATE.md` line 96 prose question about KuzuDB, retired 2026-06-14 per that file's own correction banner); what it deliberately does NOT do (it is not a comment scrub: historical "Migrated from hsi-to-kuzu.cjs" header comments, the `buildGraphFromKuzu` back-compat export alias in `lib/core/graph-ops.cjs`, and the `case 'build-kuzu':` back-compat subcommand label in `bin/mindrian-tools.cjs` are legitimate and must keep passing); and the exit contract: 0 clean, 1 forbidden pattern found so the release must be blocked, 2 scanner failure.

**Root resolution.** `resolveRoot()` parses `process.argv` for `--root <dir>`, defaulting to `path.resolve(__dirname, '..')`, using the repo's `process.argv` switch-style convention (no Commander, no yargs). If the resolved root does not exist or is not a directory, print a named error and `process.exit(2)`. Document in one comment line that `--root` exists so the test suite can drive real fixtures hermetically instead of mutating the live tree.

**Check 1 - dependency manifests.** Read `<root>/package.json`; a missing or unparseable manifest is a scanner failure (exit 2). Read `<root>/package-lock.json` if present; a missing lock is skipped with a printed note, not a failure. Scan each manifest LINE BY LINE (so violations report a line number, matching the sibling script's reporting) against two regexes:
  - `KUZU_DEP_RE` matching a dependency key: optional leading whitespace, a double quote, then `kuzu` optionally followed by a hyphen and further name characters, OR `@kuzudb/` followed by name characters, then a closing double quote and a colon.
  - `KUZU_LOCK_PATH_RE` matching an npm lockfile v3 package path key: the same name alternation but preceded by an optional `node_modules/` prefix inside the quotes, so a transitive reintroduction that only shows up in the lock is still caught.

**Check 2 - live require/import in source.** `walkSourceFiles(root)` walks recursively from the root, collecting files whose extension is in `SCAN_EXTENSIONS` (`.cjs`, `.js`, `.mjs`) and skipping any directory whose basename is in `SKIP_DIRS`: `node_modules`, `.git`, `.planning`, `worktrees`. Write the reason for each skip in a comment: `node_modules` is untracked install output and would be enormous, `.git` is object storage, `.planning` is gitignored planning prose that quotes forbidden patterns as examples, and `worktrees` prevents `.claude/worktrees/agent-*` sibling checkouts from being scanned as if they were this tree. Because only three code extensions are scanned, every `.md` file (docs/, references/, pipelines/) is excluded automatically, which is the whole reason the roughly 38 historical prose mentions cannot trip this gate.

`KUZU_REQUIRE_RE` matches a live module reference only: a `require(` with optional whitespace, or a `from ` , or an `import(` with optional whitespace, immediately followed by a quote (single or double) and then `kuzu`, optionally a hyphen plus further name characters, or `@kuzudb/` plus name characters, then the closing quote. It must NOT match the bare word kuzu in a comment, in a `case 'build-kuzu':` label, or inside an identifier such as `buildGraphFromKuzu`. Scan per line, record `{ file (repo-relative), line, snippet }` for every hit, and never stop at the first one.

**ALLOWLIST.** A `Set` with exactly two entries, each carrying its written reason on the same line:
  - `scripts/check-kuzu-reintroduction.cjs` - self; this file's own regexes contain the literal patterns.
  - `tests/test-242-kuzu-reintroduction-gate.cjs` - it authors seeded forbidden-pattern fixtures into scratch dirs, so its source necessarily contains the literal strings.
Add a comment stating the allowlist is size-fenced by the test: growing an allowlist is how a gate like this gets silently neutered, so any third entry must be a deliberate, reviewed act.

**Failure guard.** If `walkSourceFiles` returns zero files, print a named error and exit 2. A scanner that scanned nothing must never report PASS (same reasoning as the sibling script's zero-Stop-hook-files exit 2 at lines 197-200).

**Output.** On clean: print a PASS line plus the count of manifests and source files scanned. On violation: print a FAIL banner, then every violation as `  <file>:<line>` followed by the trimmed snippet, then a short remediation paragraph stating that the local graph has been `node:sqlite` since 2026-06-14 (see `docs/MOAT-MANDATE.md`) and that historical comments and back-compat alias names are exempt by design, then `process.exit(1)`.

No em-dashes anywhere. CJS only. No new dependency.
  </action>
  <verify>
    <automated>node scripts/check-kuzu-reintroduction.cjs &amp;&amp; node -e "const s=require('fs').readFileSync('scripts/check-kuzu-reintroduction.cjs','utf8'); for (const t of ['KUZU_DEP_RE','KUZU_LOCK_PATH_RE','KUZU_REQUIRE_RE','ALLOWLIST','SKIP_DIRS','--root']) if(!s.includes(t)) throw new Error('missing '+t); console.log('gate structure OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-kuzu-reintroduction.cjs` on the current, unmodified tree exits 0 and prints a PASS line naming a non-zero count of scanned source files. A first run that is not clean means the scope is miscalibrated (RESEARCH.md Pitfall 3).
    - Mutation proof (require leg), run by the executor and recorded in the SUMMARY: create `lib/core/__kuzu-mutation-probe.cjs` containing a single line that requires the package literally named kuzu, run `node scripts/check-kuzu-reintroduction.cjs`, and confirm exit code 1 with the probe file named in the output; then `rm lib/core/__kuzu-mutation-probe.cjs`, re-run, and confirm exit code 0. Record both exit codes.
    - Mutation proof (dependency leg), same shape: add a `"kuzu": "^0.11.3"` line to `package.json`'s `dependencies`, run the gate, confirm exit 1 with `package.json` and the line number named; then `git checkout -- package.json`, re-run, confirm exit 0. Record both exit codes and confirm `git status --porcelain package.json` is empty afterwards.
    - False-positive fence: the gate exits 0 while `bin/mindrian-tools.cjs`, `lib/core/graph-ops.cjs`, `scripts/hsi-to-graph.cjs`, `scripts/causal-to-graph.cjs`, `scripts/migrate-lazygraph.cjs`, and `scripts/whitespace-to-graph.cjs` all still contain the string kuzu unchanged. Confirm with `git diff --name-only` listing none of those six files.
    - `node scripts/check-kuzu-reintroduction.cjs --root /nonexistent-path-xyz` exits 2, not 1 and not 0.
    - `ALLOWLIST` has exactly two entries.
    - `grep -cP '\x{2014}' scripts/check-kuzu-reintroduction.cjs` returns 0.
  </acceptance_criteria>
  <done>The gate exists, passes clean on today's tree, exits 1 on both a seeded require and a seeded dependency line, exits 2 on a bad root, and does not false-positive on any of the six legitimate historical kuzu mentions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the gate into verify-release and replace the dead checklist line</name>
  <files>scripts/verify-release, docs/MOAT-MANDATE.md</files>
  <read_first>
    - `scripts/verify-release` lines 425-455 - the section 16 Stop-hook schema gate: its `# ====` banner comment block, the `echo -e "\n${BOLD}16. ..."` heading at line 442, the `STOP_SCHEMA_OUT=$(node ... 2>&1) && STOP_SCHEMA_CODE=0 || STOP_SCHEMA_CODE=$?` capture at line 444, the pass/fail branch at lines 445-450, and the unnumbered PACKAGE-LOCK SYNC banner that starts at line 452.
    - `docs/MOAT-MANDATE.md` lines 1-10 and lines 85-100 - the correction banner, the `### Surface Area Warning Signs` list, the exact target at line 96, and the 3+ rule at line 99 that the replacement must not corrupt.
    - `.planning/phases/242-the-moat/242-PATTERNS.md`, sections "scripts/verify-release (edit)" and "docs/MOAT-MANDATE.md (edit)".
    - This plan's `<planner_resolutions>` polarity correction, which overrides the replacement text PATTERNS.md proposes.
  </read_first>
  <action>
**Edit A - `scripts/verify-release`.** Insert one new numbered section immediately after the section 16 block's closing `fi` (currently line 450) and immediately before the `# ====` banner that opens PACKAGE-LOCK SYNC (currently line 452). Do not renumber or otherwise modify section 16, and do not touch the package-lock block.

The new section is a banner comment, a heading, and the capture block:
  - Banner comment in the `# ====` bar style used by section 16, explaining: the local graph moved off KuzuDB to `node:sqlite` on 2026-06-14; `docs/MOAT-MANDATE.md`'s PR checklist asked reviewers to judge that by eye against a database the repo no longer has; this gate replaces that prose with a deterministic scan of dependency manifests and live require/import statements; historical comments and back-compat alias names are exempt by design.
  - Heading: `echo -e "\n${BOLD}17. Kuzu Reintroduction Gate${NC}"`.
  - Capture using the identical idiom as line 444, with variables `KUZU_GATE_OUT` and `KUZU_GATE_CODE`: run `node "$PLUGIN_ROOT/scripts/check-kuzu-reintroduction.cjs" 2>&1`, capture, set the code to 0 on success or `$?` on failure.
  - Branch: on code 0 call `pass` with a message that no live kuzu dependency or require re-entered the tree; otherwise call `fail` with a message naming the gate, then `echo "$KUZU_GATE_OUT"`, exactly mirroring lines 445-450. Use the existing `pass` / `fail` helpers; do not invent a new reporting path.

**Edit B - `docs/MOAT-MANDATE.md` line 96.** Replace exactly one line. The current line 96 reads `- [ ] Does this work without KuzuDB edges?`. Leave the surrounding checklist items and the 3+ rule at line 99 byte-identical.

The replacement must preserve the list's checked-means-bad polarity (see planner_resolutions). Write it as one checkbox line asking whether this PR reintroduces a retired graph engine, followed by a parenthetical stating that it is machine-checked by `node scripts/check-kuzu-reintroduction.cjs` (which exits non-zero if a live kuzu dependency or require/import re-entered the tree), and that historical comments and back-compat alias names are exempt. One line, no em-dashes, hyphens only.

After the edit, the exact string `Does this work without KuzuDB edges` must appear zero times in the file and `check-kuzu-reintroduction.cjs` must appear exactly once. Do not touch the 2026-06-14 correction banner at line 3, which is legitimate history.
  </action>
  <verify>
    <automated>bash -n scripts/verify-release &amp;&amp; test "$(grep -vE '^[[:space:]]*#' scripts/verify-release | grep -c 'check-kuzu-reintroduction\.cjs')" -eq 1 &amp;&amp; test "$(grep -c '17\. Kuzu Reintroduction Gate' scripts/verify-release)" -eq 1 &amp;&amp; test "$(grep -c 'Does this work without KuzuDB edges' docs/MOAT-MANDATE.md)" -eq 0 &amp;&amp; test "$(grep -c 'check-kuzu-reintroduction\.cjs' docs/MOAT-MANDATE.md)" -eq 1 &amp;&amp; echo "wiring OK"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify-release` exits 0 (no syntax break introduced into the release gate).
    - The wiring assertion is comment-stripped and two-sided: comment-stripped `scripts/verify-release` contains `check-kuzu-reintroduction.cjs` exactly once (a banner comment mentioning the script cannot satisfy the gate), and the heading `17. Kuzu Reintroduction Gate` appears exactly once.
    - The doc assertion is two-sided so it cannot pass vacuously on an emptied file: `grep -c 'Does this work without KuzuDB edges' docs/MOAT-MANDATE.md` returns 0 AND `grep -c 'check-kuzu-reintroduction\.cjs' docs/MOAT-MANDATE.md` returns exactly 1.
    - Polarity preserved: the replacement line still reads as a warning sign (checked means a problem), so the "If 3+ warning signs are checked" rule at line 99 keeps its meaning. Confirm by reading lines 92-99 after the edit and stating in the SUMMARY that all items in the list share one polarity.
    - Blast radius contained: `git diff --stat docs/MOAT-MANDATE.md` reports 1 insertion and 1 deletion, and `wc -l docs/MOAT-MANDATE.md` is unchanged from before the edit.
    - End-to-end wiring proof, run by the executor and recorded in the SUMMARY: with `lib/core/__kuzu-mutation-probe.cjs` seeded again, run the section 17 block's own command line (`node scripts/check-kuzu-reintroduction.cjs`) as `scripts/verify-release` invokes it and confirm exit 1; remove the probe and confirm exit 0. Record both exit codes.
    - `grep -cP '\x{2014}' scripts/verify-release docs/MOAT-MANDATE.md` returns 0 for both files.
  </acceptance_criteria>
  <done>Section 17 runs the gate through the same capture-and-branch idiom as section 16, and the dead KuzuDB prose is replaced by a same-polarity, machine-checked warning-sign item.</done>
</task>

<task type="auto">
  <name>Task 3: Build the hermetic gate test with seeded-fixture and allowlist fences</name>
  <files>tests/test-242-kuzu-reintroduction-gate.cjs</files>
  <read_first>
    - `scripts/check-kuzu-reintroduction.cjs` after Task 1 - the `--root` contract, the exit codes, and the ALLOWLIST entries this test fences.
    - `scripts/verify-release` after Task 2 - the section 17 block this test asserts is wired.
    - `docs/MOAT-MANDATE.md` after Task 2 - the replaced line this test asserts.
    - `tests/test-sqlite-concurrent.cjs` lines 1-40 - the `node:test` + `node:assert/strict` + `fs.mkdtempSync` hermetic-fixture convention every test in this repo follows.
    - `.planning/phases/242-the-moat/242-PATTERNS.md`, section "tests/test-242-kuzu-reintroduction-gate.cjs (NEW test, unit)" - the `spawnSync` exit-code-assertion shape (no direct sibling test exists for `check-hook-schema-compatibility.cjs`, so this is a synthesized shape, not a copied analog).
    - `.planning/phases/242-the-moat/242-VALIDATION.md` - the per-task verification map row 242-02-01 this file satisfies.
  </read_first>
  <action>
Create `tests/test-242-kuzu-reintroduction-gate.cjs` using `node:test` (`describe`/`it`) and `node:assert/strict`, matching every other test in `tests/`. Fully hermetic: every fixture under `fs.mkdtempSync(path.join(os.tmpdir(), 'mindrian-242-kuzu-'))`, zero network reach, and NOTHING written inside the repository. The live tree is exercised read-only.

**Helpers (module scope):**
  - `makeScratchRoot()` - mkdtemp a scratch root, write a minimal valid `package.json` with no kuzu anywhere, and create a `lib/` subdirectory holding one clean `.cjs` file that requires `node:path` (so the walk finds at least one scannable file and the zero-files exit-2 guard is not triggered). Return the root path.
  - `runGate(args)` - `spawnSync(process.execPath, [path.join(__dirname, '..', 'scripts', 'check-kuzu-reintroduction.cjs'), ...args], { encoding: 'utf8' })`, returning `{ status, stdout, stderr }`.
  - `seedRequireFixture(root)` - append one line to the scratch `lib/` file that requires the package literally named kuzu. Build the literal with ordinary source text; this test file is one of the two ALLOWLIST entries precisely so its own source may contain that string.
  - `seedDependencyFixture(root)` - rewrite the scratch `package.json` with a `dependencies` entry keyed on the bare kuzu name.
  - `seedLockFixture(root)` - write a scratch `package-lock.json` containing a lockfile v3 style `"node_modules/kuzu"` package path key.

**Legs, each its own `it`:**
  1. `passes (exit 0) on the current, unmodified tree` - `runGate([])` with no `--root`, assert `status === 0`. This exercises the DEFAULT root path, which is what `scripts/verify-release` actually invokes, so a `--root`-only test cannot vouch for the production path.
  2. `passes (exit 0) on a clean scratch root` - `makeScratchRoot()` then `runGate(['--root', root])`, assert `status === 0`.
  3. `fails (exit 1) on a seeded live require` - seed then re-run, assert `status === 1` and that stdout or stderr names the seeded file.
  4. `fails (exit 1) on a seeded package.json dependency` - fresh scratch root, seed, assert `status === 1` and that the output names `package.json` with a line number.
  5. `fails (exit 1) on a seeded package-lock node_modules path` - fresh scratch root, seed, assert `status === 1`.
  6. `exits 2 on a missing root` - `runGate(['--root', path.join(os.tmpdir(), 'definitely-not-a-real-root-242')])`, assert `status === 2`, distinguishing scanner failure from both clean and violation.
  7. `exits 2 rather than passing when there is nothing to scan` - a scratch root holding only a `package.json` and no source files at all, assert `status === 2`. A scanner that scanned nothing must never report PASS.
  8. `does not false-positive on the six legitimate historical kuzu mentions` - assert with `fs.readFileSync` that `bin/mindrian-tools.cjs`, `lib/core/graph-ops.cjs`, `scripts/hsi-to-graph.cjs`, `scripts/causal-to-graph.cjs`, `scripts/migrate-lazygraph.cjs`, and `scripts/whitespace-to-graph.cjs` each still contain the string kuzu, AND that leg 1 passed. The two facts together are the false-positive fence; asserting either alone proves nothing.
  9. `allowlist is size-fenced at exactly two documented entries` - read `scripts/check-kuzu-reintroduction.cjs` source, extract the ALLOWLIST literal, and assert it contains exactly two entries and that they are exactly the self path and this test's own path. This is what stops the gate from being neutered by quietly adding files to the allowlist instead of removing the dependency.
  10. `the dead KuzuDB checklist prose is gone and replaced` - read `docs/MOAT-MANDATE.md`, assert zero occurrences of `Does this work without KuzuDB edges` and exactly one occurrence of `check-kuzu-reintroduction.cjs`. Two-sided so an emptied file cannot pass.
  11. `the gate is wired into verify-release, not merely present on disk` - read `scripts/verify-release`, strip lines whose first non-whitespace character is `#`, and assert the remaining executable lines contain `check-kuzu-reintroduction.cjs` exactly once. Comment stripping is mandatory: a banner comment mentioning the script must not be able to satisfy a production-wiring gate.
  12. `Part 8 sweep: the new script makes zero network reach` - read `scripts/check-kuzu-reintroduction.cjs`, strip comment lines (`//`, `*`, `/*`), and assert the remaining executable lines match none of `https?://`, `fetch(`, `axios`, `WebFetch`, `mindrian-brain.onrender.com`, `api.anthropic.com`. Precede it with a negative self-test: run the same pattern against a synthetic string containing each forbidden token on an executable line plus one token on a comment line, and assert every executable token is caught and the comment line is ignored. A grep gate that quietly stopped matching looks exactly like a clean file.

Every leg cleans up its scratch dir. `git status --porcelain` must be clean after a full run apart from this new file. No em-dashes.
  </action>
  <verify>
    <automated>node tests/test-242-kuzu-reintroduction-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-242-kuzu-reintroduction-gate.cjs` exits 0 with all twelve legs passing.
    - Seed-and-fail is proven at three independent shapes (live require, `package.json` dependency, `package-lock.json` path key), each asserting exit exactly 1, so the gate cannot pass by only covering the manifest or only covering source.
    - Exit 2 is asserted at two independent shapes (missing root, zero scannable files), so a scanner failure can never be mistaken for a clean pass.
    - Leg 12's negative self-test fails the suite if the Part 8 pattern stops matching, so the sweep proves itself before it is trusted.
    - The suite is hermetic: after a full run, `git status --porcelain` shows only the new test file, and no scratch artifact remains under the repo.
    - Cross-plan discovery check, run once and recorded in the SUMMARY: after Plan 01's `tests/run-all-242.sh` exists, run `bash tests/run-all-242.sh` and confirm both `test-242-hsi-to-graph-transaction.cjs` and `test-242-kuzu-reintroduction-gate.cjs` appear as discovered PASSED legs with no edit to the aggregator.
    - `grep -cP '\x{2014}' tests/test-242-kuzu-reintroduction-gate.cjs` returns 0.
  </acceptance_criteria>
  <done>All twelve legs pass, the gate is proven to fire on three seeded reintroduction shapes and to pass on the live tree, and the doc replacement plus the verify-release wiring are both machine-asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future commit -> the dependency surface | Untrusted-by-default change crosses here: a reintroduced graph-engine dependency arrives as a `package.json` edit or a new require in source, and today nothing but a human's memory stops it. |
| `scripts/verify-release` -> release artifact | The gate's exit code decides whether a release proceeds, so a gate that silently stops working is worse than no gate. |
| repository filesystem -> scanner walk | The scanner reads arbitrary tree contents, including sibling worktrees and untracked directories if not bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-242-05 | Tampering | supply chain: reintroduction of the retired kuzu graph engine | mitigate | `scripts/check-kuzu-reintroduction.cjs` scans `package.json` and `package-lock.json` dependency keys plus live require/import statements across `.cjs`/`.js`/`.mjs`, wired into `scripts/verify-release` section 17 as a release-blocking exit-1 gate (Tasks 1 and 2). |
| T-242-06 | Repudiation | the ALLOWLIST inside the scanner | mitigate | The allowlist holds exactly two entries with written reasons, and leg 9 of `tests/test-242-kuzu-reintroduction-gate.cjs` asserts both the size and the exact members, so neutering the gate by adding files becomes a test failure rather than a quiet commit. |
| T-242-07 | Denial of Service | the scanner's directory walk | mitigate | `SKIP_DIRS` excludes `node_modules`, `.git`, `.planning`, and `worktrees`, and only three code extensions are read, bounding the walk to roughly 1900 files; a walk that finds zero files exits 2 rather than reporting a fast, empty PASS. |
| T-242-08 | Spoofing | a banner comment satisfying the wiring gate | mitigate | Leg 11 strips comment lines before asserting that `scripts/verify-release` references the script, so prose about the gate cannot stand in for the gate being wired (the repo's own comment-stripped census convention). |
| T-242-09 | Information disclosure | scanner output | accept | The scanner prints repo-relative paths, line numbers, and trimmed source snippets of violating lines only. It reads no `.env`, no credentials, and no room data, and it makes zero network reach (asserted by leg 12). Canon Part 8 is unaffected: nothing egresses. |
| T-242-SC | Tampering | npm/pip/cargo installs | accept | Zero package-manager installs in this phase. RESEARCH.md's Package Legitimacy Audit is N/A by inspection: the new script uses only the `node:fs` and `node:path` builtins, and no `package.json` line changes. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package exists to gate. Note the inversion: this plan's deliverable IS a supply-chain control. |
</threat_model>

<verification>
1. `node scripts/check-kuzu-reintroduction.cjs` exits 0 on the current tree.
2. `node tests/test-242-kuzu-reintroduction-gate.cjs` exits 0 with all twelve legs passing.
3. `bash -n scripts/verify-release` exits 0, and comment-stripped `verify-release` references `check-kuzu-reintroduction.cjs` exactly once.
4. `grep -c 'Does this work without KuzuDB edges' docs/MOAT-MANDATE.md` returns 0 and `grep -c 'check-kuzu-reintroduction\.cjs' docs/MOAT-MANDATE.md` returns 1.
5. Both live mutation re-checks (seeded require probe; seeded `package.json` dependency) have been run and all four exit codes recorded in the SUMMARY, with `git status --porcelain` clean afterwards.
6. After Plan 01 lands, `bash tests/run-all-242.sh` discovers both phase test files with no edit to the aggregator.
</verification>

<success_criteria>
ROADMAP Phase 242 success criterion 2 is met and demonstrated by command, not by claim:
- The PR checklist's KuzuDB warning prose is gone from `docs/MOAT-MANDATE.md` and replaced by a machine-checked, same-polarity warning-sign item.
- The assertion fails when a kuzu reference re-enters the tree, proven at three independent seeded shapes plus a live probe on the real tree, and passes on the current tree.
- The assertion actually runs at release time: `scripts/verify-release` section 17 invokes it and fails the release on a non-zero exit.
</success_criteria>

<output>
Create `.planning/phases/242-the-moat/242-02-SUMMARY.md` when done. It must record: the four exit codes from the two live mutation re-checks (seeded require probe on and off; seeded `package.json` dependency on and off), confirmation that `git status --porcelain package.json` was clean afterwards, the `git diff --stat docs/MOAT-MANDATE.md` line counts (must be 1 insertion / 1 deletion), a one-line statement that Open Question 2 resolved to verify-release only and `scripts/doctor.cjs` was not touched, and the result of the cross-plan `bash tests/run-all-242.sh` discovery check.
</output>
