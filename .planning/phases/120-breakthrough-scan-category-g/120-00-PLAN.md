---
phase: 120-breakthrough-scan-category-g
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/memory-events.cjs
  - lib/core/navigation/edges.cjs
  - lib/core/breakthrough/detectors.cjs
  - lib/core/breakthrough/detectors.test.cjs
  - lib/core/breakthrough/schema.cjs
  - lib/core/breakthrough/schema.test.cjs
  - tests/test-breakthrough-event-types.cjs
  - tests/test-breakthrough-edge-types.cjs
  - tests/test-120-00-scaffold.sh
  - lib/memory/run-feynman-tests.cjs
autonomous: true
requirements:
  - BREAKTHROUGH-120-01  # 4 breakthrough pattern detectors (convergence + contradiction-resolved + cross-domain analogy + reverse-salient closed)
  - BREAKTHROUGH-120-02  # Two-tier firing thresholds (soft buffer + hard surface) per D-01..D-06
  - BREAKTHROUGH-120-06  # Hybrid 4-tier ethics fence: HARD FLOOR Cypher provenance enforcement at the detector layer (D-18)
canon_parts:
  - Part 2 Engine 1     # Act 1 intelligence; pattern detection over local graph
  - Part 4              # Every choice is graph data -- detectors emit Breakthrough nodes + DERIVED_FROM edges as first-class graph signal
  - Part 8              # Graph boundary -- detectors read room.db only; zero Brain MCP coupling; zero cross-room aggregation
  - Part 9              # Memory locality -- ALL reads + writes through navigation.cjs chokepoint per Phase 109 D-06 invariant
  - Part 10             # sub-claim 5 -- variable reward fires automatically; the math IS the surface
must_haves:
  truths:
    - "Five new memory_event types (breakthrough_detected_soft, breakthrough_surfaced, breakthrough_confirmed, breakthrough_dismissed, breakthrough_filed_as_decision) plus one canary event (breakthrough_throttled) are accepted by lib/core/navigation/memory-events.cjs::logEvent without runtime errors."
    - "One new edge type (DERIVED_FROM) is accepted by lib/core/navigation/edges.cjs::writeEdge so every surfaced Breakthrough node has a graph-provable provenance link to one or more Artifact nodes (D-20 Cypher-provable principle)."
    - "Four pure detector functions (detectConvergence, detectContradictionResolved, detectCrossDomainAnalogy, detectReverseSalientClosed) each take a roomState handle that wraps navigation.cjs and return {hits: Breakthrough[], soft_fires: SoftFire[]} where every hit carries provenance.artifact_ids[] (>= 1 entry) so D-20 is enforceable at write time."
    - "Two-tier firing thresholds are encoded as named constants (SOFT_FIRE_MIN_ARTIFACTS=3 + SOFT_FIRE_MIN_CONFIDENCE=0.25 + HARD_FIRE_MIN_ARTIFACTS=4 + HARD_FIRE_CROSS_SECTION_BYPASS=3 + HARD_FIRE_MIN_CONFIDENCE=0.35 + SEMANTIC_SIMILARITY_THRESHOLD=0.40 + WINDOW_DAYS_DEFAULT=14) frozen via Object.freeze and exported so Plan 120-02 can read them at scanner time."
    - "Detectors read Phase 117 math-layer output (whitespace.gaps + rs.pairs + analogy.zones) via the same composeAutoExploreFinding-side files that auto-explore-fire.cjs writes (.mindrian/whitespace-results.json + .mindrian/.rs-engine-results.json + .mindrian/discovery-cycle-results.json) -- NO recomputation of the math."
    - "Detectors NEVER require lib/core/brain-client or any module under mcp-server-brain/ (Canon Part 8 boundary enforced by source-grep test)."
  artifacts:
    - path: "lib/core/navigation/memory-events.cjs"
      contains: "'breakthrough_detected_soft'"
      contains_also: "'breakthrough_surfaced'"
      contains_also_2: "'breakthrough_confirmed'"
      contains_also_3: "'breakthrough_dismissed'"
      contains_also_4: "'breakthrough_filed_as_decision'"
      contains_also_5: "'breakthrough_throttled'"
      provides: "EVENT_TYPES Set extended additively (+6 strings; size N -> N+6); Object.freeze invariant preserved"
    - path: "lib/core/navigation/edges.cjs"
      contains: "'DERIVED_FROM'"
      provides: "ALLOWED_EDGE_TYPES Set extended additively (+1 string; size N -> N+1); Object.freeze invariant preserved; writeEdge accepts edge_type='DERIVED_FROM' params"
    - path: "lib/core/breakthrough/detectors.cjs"
      provides: "Pure detector functions for the 4 pattern types + the two-tier firing classifier + the named threshold constants; reads only via navigation.cjs chokepoint + the existing Phase 117 math JSON files"
      exports: ["detectConvergence", "detectContradictionResolved", "detectCrossDomainAnalogy", "detectReverseSalientClosed", "classifyFireTier", "DETECTOR_THRESHOLDS", "DETECTOR_TYPES"]
    - path: "lib/core/breakthrough/schema.cjs"
      provides: "Breakthrough node shape + DERIVED_FROM edge writer + provenance validator (D-20 HARD FLOOR enforcement: refuses to write a Breakthrough without at least one DERIVED_FROM target)"
      exports: ["writeBreakthrough", "validateProvenance", "BREAKTHROUGH_NODE_TYPE", "BREAKTHROUGH_KIND"]
    - path: "tests/test-120-00-scaffold.sh"
      provides: "Wave-0 acceptance harness asserting the 6 event types + 1 edge type + zero Brain client require + zero em-dash + provenance HARD FLOOR enforcement"
  key_links:
    - from: "lib/core/breakthrough/detectors.cjs"
      to: "lib/core/navigation.cjs::findRecentChanges"
      via: "all queries over the breakthrough trailing window route through the navigation chokepoint (Canon Part 9 D-06)"
      pattern: "navigation\\.findRecentChanges\\("
    - from: "lib/core/breakthrough/schema.cjs::writeBreakthrough"
      to: "lib/core/navigation.cjs::writeEdge"
      via: "every Breakthrough node write is followed (in same SQLite transaction) by N writeEdge calls with edge_type='DERIVED_FROM' linking the Breakthrough to its source Artifact ids -- D-20 HARD FLOOR enforced structurally"
      pattern: "navigation\\.writeEdge.+'DERIVED_FROM'"
    - from: "lib/core/breakthrough/detectors.cjs"
      to: "auto-explore JSON outputs"
      via: "fs.readFileSync of .mindrian/whitespace-results.json + .mindrian/.rs-engine-results.json + .mindrian/discovery-cycle-results.json (the Phase 117 math layer output files)"
      pattern: "whitespace-results\\.json|rs-engine-results\\.json|discovery-cycle-results\\.json"
---

<objective>
**Wave 1 (Plan 120-00) -- the four pattern detectors + Breakthrough graph schema + EVENT_TYPES + edge-type extensions.**

This plan implements the math-to-detection layer for Phase 120. It ships:
1. The four pure pattern detectors (convergence / contradiction-resolved / cross-domain analogy / reverse-salient closed) per CONTEXT.md scope and BREAKTHROUGH-120-01.
2. The two-tier firing classifier (soft buffer + hard surface) with the verbatim thresholds locked by D-01..D-06.
3. The Breakthrough node schema + DERIVED_FROM edge type so D-20 (every breakthrough Cypher-provable) is enforceable at write time, not just audited.
4. The six new memory_event types (5 lifecycle + 1 canary) for Phase 120 surfacing telemetry, all additively appended to the EVENT_TYPES Set per the Phase 88.2-00 / 89-07-00 / 116-00 / 117-00 / 110-02 / 124-02 / 125-01 / 125-06 / 125-07 / 119-00 / 119-01 precedent.

Per CONTEXT.md D-01 + D-20: the detectors are PURE functions over local graph state. Every Breakthrough they emit carries provenance.artifact_ids[] (>= 1 entry). The HARD FLOOR provenance test (D-18 / D-20) is enforced in `lib/core/breakthrough/schema.cjs::writeBreakthrough` -- a `Breakthrough` node CANNOT land in room.db without at least one accompanying `DERIVED_FROM` edge to an Artifact. The Cypher invariant `MATCH (b:Breakthrough)-[:DERIVED_FROM]->(a:Artifact) WHERE b.id = $id RETURN count(a)` is guaranteed >= 1 by construction.

Plan 120-00 does NOT ship the F.7 Breakthrough Surface selector (that's Plan 120-01) or the session-start scanner integration (that's Plan 120-02) or the voice scaffold + full ethics-fence enforcement (that's Plan 120-03). Plan 120-00 owns the detector layer + the graph schema substrate that all three downstream plans build on.

**Purpose:**
- Implements Canon Part 10 sub-claim 5 ("variable reward fires automatically; the math IS the surface") at the detector layer.
- Implements Canon Part 4 ("every choice is graph data"): Breakthroughs become first-class typed graph nodes with provenance edges; "File as decision" (Plan 120-01) will further promote them via Phase 88 decision-log machinery.
- Implements Canon Part 8 + Part 9: ALL reads route through navigation.cjs; ZERO Brain MCP coupling; ZERO cross-room aggregation; provenance edges make D-20 a structural property of the graph, not a procedural promise.

**Output:**
- New file `lib/core/breakthrough/detectors.cjs` (~280 LOC) -- the four detector functions + classifyFireTier + the frozen threshold constants.
- New file `lib/core/breakthrough/schema.cjs` (~140 LOC) -- writeBreakthrough + validateProvenance + the Breakthrough node-type constants.
- Modified `lib/core/navigation/memory-events.cjs` -- 6 string additions to EVENT_TYPES per the additive tail-append precedent.
- Modified `lib/core/navigation/edges.cjs` -- 1 string addition to ALLOWED_EDGE_TYPES (DERIVED_FROM).
- Tests proving: EVENT_TYPES grows by exactly 6, ALLOWED_EDGE_TYPES grows by exactly 1, each detector returns the canonical {hits, soft_fires} shape, every emitted Breakthrough has provenance.artifact_ids.length >= 1, writeBreakthrough rejects provenance-less inputs, NO Brain MCP require, NO em-dashes.

Per CONTEXT.md "Claude's Discretion" item 2 -- the Cypher schema for `Breakthrough` node + `DERIVED_FROM` edge is materialized in this plan as the additive extension (Phase 110-03 / 119-00 / 124 / 125-00 precedent). The `Breakthrough` node uses the existing `nodes` table with `type='breakthrough'`; the `DERIVED_FROM` edge uses the existing `edges` table with edge_type='DERIVED_FROM'. No schema migration needed.

**Per CONTEXT.md threshold lock (D-01..D-06) -- VERBATIM CONSTANTS:**
```
SOFT_FIRE_MIN_ARTIFACTS = 3
SOFT_FIRE_MIN_CONFIDENCE = 0.25
HARD_FIRE_MIN_ARTIFACTS = 4
HARD_FIRE_CROSS_SECTION_BYPASS = 3   // 3 artifacts with cross-section linkage = hard-fire
HARD_FIRE_MIN_CONFIDENCE = 0.35
SEMANTIC_SIMILARITY_THRESHOLD = 0.40
WINDOW_DAYS_DEFAULT = 14
```

**Per CONTEXT.md D-05 (reverse-salient closed requires BOTH signals):**
- Graph-level proof: an edge type or property combination that did not exist before now exists (Cypher-provable).
- Score delta: the lagging-component score crosses its baseline within the window.
Single-signal closure returns soft_fire only; both-signal closure returns a hard-fire-eligible hit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/120-breakthrough-scan-category-g/120-CONTEXT.md
@.planning/phases/120-breakthrough-scan-category-g/120-DISCUSSION-LOG.md
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
@CLAUDE.md

# Phase 117 (the math layer this plan reads -- the 4 detectors consume its JSON output, never recompute)
@.planning/phases/117-auto-explore-domains-on-first-material/117-CONTEXT.md
@lib/agents/auto-explore-agent.cjs
@scripts/auto-explore-fire.cjs

# Phase 109 SQL chokepoint (Canon Part 9 -- ALL reads + writes route through this)
@lib/core/navigation.cjs
@lib/core/navigation/memory-events.cjs
@lib/core/navigation/edges.cjs

# Phase 119-00 EVENT_TYPES additive-extension precedent (the exact pattern Phase 120-00 mirrors for the 6 new strings)
@.planning/phases/119-room-as-receipt-invariant/119-00-PLAN.md

# Phase 125-00 writeEdge + ALLOWED_EDGE_TYPES precedent (the pattern Phase 120-00 mirrors for the DERIVED_FROM addition)
@lib/core/navigation/edges.cjs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase.
     Executor should use these directly -- no codebase exploration needed. -->

From lib/core/navigation/memory-events.cjs (current EVENT_TYPES Set size baseline = 42 post-Phase-119-01):
```javascript
const EVENT_TYPES = Object.freeze(new Set([
  // ... 42 existing strings (DO NOT REORDER, DO NOT MUTATE) ...
  'room_discard_partial_failure',
  // Phase 120-00 Wave 1 extension (Breakthrough Scan / Category G; D-01..D-06 detector tier
  //   + D-09 file-as-decision bridge + D-10 mandatory dismiss + D-19 canary auto-throttle).
  //   Six strings; mirrors the Phase 119-00 3-string + 117-00 6-string additive idiom.
  'breakthrough_detected_soft',        // D-02 soft-fire writes to buffer; does NOT surface
  'breakthrough_surfaced',             // D-03 hard-fire surfaces via F.7
  'breakthrough_confirmed',            // D-08 [Confirm] verb selected; positive training signal
  'breakthrough_dismissed',            // D-08 [Dismiss] verb selected; negative training signal + canary input
  'breakthrough_filed_as_decision',    // D-09 [File as decision] verb selected; Canon Part 4 bridge to decision-log
  'breakthrough_throttled',            // D-19 per-detector dismissal-rate canary auto-throttle fired
]));
```
Set size grows by 6 (was 42 baseline; now 48 minimum). Floor-not-exact-count test idiom applies.

From lib/core/navigation/edges.cjs (current ALLOWED_EDGE_TYPES Set; Phase 125-00 baseline = {DEFERRED, REJECTED}):
```javascript
const ALLOWED_EDGE_TYPES = Object.freeze(new Set([
  'DEFERRED',
  'REJECTED',
  // Phase 120-00 Wave 1 extension (Breakthrough Scan / Category G; D-18 HARD FLOOR + D-20
  //   Cypher-provable principle). DERIVED_FROM is the structural enforcement: a Breakthrough
  //   node CANNOT surface without at least one DERIVED_FROM edge to an Artifact node.
  //   Mirrors the Phase 125-00 DEFERRED + REJECTED additive idiom.
  'DERIVED_FROM',
]));
```

From lib/core/navigation.cjs::writeEdge (Phase 125-00 thin re-export):
```javascript
// Signature:
navigation.writeEdge(db, { source_id, target_id, edge_type, properties })
//   -> { ok: true, edgeId } | { ok: false, reason: string }
// edge_type MUST be a member of ALLOWED_EDGE_TYPES; properties is a plain object (JSON-stringified).
```

From lib/core/navigation.cjs::logMemoryEvent (Phase 110-03 thin re-export):
```javascript
// Signature:
navigation.logMemoryEvent(db, eventType, payload)
//   -> { ok: true, eventId } | { ok: false, reason: string }
// Payload MUST be a plain object; source_path + created_by extracted as top-level columns.
```

From lib/agents/auto-explore-agent.cjs::composeAutoExploreFinding (Phase 117 math-layer output shape):
```javascript
// composeAutoExploreFinding({material_id, whitespace, rs, analogy}) -> Finding
// whitespace.gaps -- array of {gap_id, theme, artifacts: [string], differential: number, ...}
// rs.pairs        -- array of {pair_id, source_artifact_id, target_artifact_id,
//                              direction, signed_diff: number, source_section, target_section, ...}
// analogy.zones   -- array of {zone_id, source_artifact_id, target_artifact_id,
//                              similarity: number, source_section, target_section, ...}
// File locations within roomDir/.mindrian/:
//   whitespace-results.json     -- {gaps: [...]}
//   .rs-engine-results.json     -- {pairs: [...]}
//   discovery-cycle-results.json -- {analogy_whitespace: {zones: [...]}}
```

From lib/core/navigation.cjs::findRecentChanges (the closed-surface chokepoint Phase 120 detectors use for trailing-window scans):
```javascript
// Signature:
navigation.findRecentChanges(db, sinceEpochMs, {limit?: 100, eventType?: string})
//   -> Array<{id, eventType, targetNodeId, createdAt, sourcePath, properties}>
// Phase 120-00 uses sinceEpochMs = (Date.now() - WINDOW_DAYS_DEFAULT * 24*3600*1000).
```

Detector return shape (canonical, exported from breakthrough/detectors.cjs):
```javascript
// Each of the 4 detectors returns:
{
  hits: Array<{
    id: string,                              // breakthrough:{detector_kind}:{material_hash}:{nowMs}
    kind: 'convergence' | 'contradiction_resolved' | 'cross_domain_analogy' | 'reverse_salient_closed',
    confidence: number,                      // [0, 1]; used by HARD CEILING (>=0.50) + SOFT BAND (0.35..0.50) ethics fence (Plan 120-03)
    artifact_ids: string[],                  // >= 1; the DERIVED_FROM targets; HARD FLOOR D-20 enforced here
    theme: string,                           // human-readable theme label (sanitized; Canon Part 8 boundary)
    differential: number,                    // [0, 1]; how much this stands out vs room baseline (feeds scoring formula D-12)
    cross_section_linked: boolean,           // D-03 bypass condition: 3 artifacts + cross-section = hard-fire
    detected_at: number,                     // epoch ms
    window_start: number,                    // epoch ms
    window_end: number,                      // epoch ms
  }>,
  soft_fires: Array<{...same shape, confidence < HARD_FIRE_MIN_CONFIDENCE...}>,
}
```

classifyFireTier signature:
```javascript
// classifyFireTier(candidate) -> 'hard' | 'soft' | 'below_floor'
//   'hard'        -- meets D-03 hard-fire thresholds (artifact_count >= 4 OR (>= 3 AND cross_section_linked))
//                    AND confidence >= HARD_FIRE_MIN_CONFIDENCE (0.35)
//   'soft'        -- meets D-02 soft-fire thresholds (artifact_count >= 3 AND confidence >= 0.25)
//                    AND below hard tier
//   'below_floor' -- everything else; never surfaces, never logs to buffer
```

writeBreakthrough signature (the D-20 HARD FLOOR structural enforcement):
```javascript
// writeBreakthrough(db, breakthrough) -> {ok: true, eventId, edgeIds: string[]} | {ok: false, reason: string}
//   1. validateProvenance(breakthrough) -- rejects if artifact_ids.length === 0 OR any id is empty
//   2. Opens SQLite transaction.
//   3. Inserts Breakthrough node row (type='breakthrough', properties.kind, .confidence, .theme, .differential,
//      .cross_section_linked, .detected_at, .window_start, .window_end, .surfaced=false initially).
//   4. For each artifact_id in breakthrough.artifact_ids: writeEdge(db, {source_id: breakthrough.id,
//      target_id: artifact_id, edge_type: 'DERIVED_FROM', properties: {detected_at, detector_kind}}).
//   5. If ANY step fails, the transaction rolls back -- partial Breakthrough state CANNOT land.
//   6. Returns the breakthrough.id + the array of created DERIVED_FROM edge ids.
//
// CONSTITUTIONAL: this function is the ONLY way breakthroughs land in room.db. Plan 120-02
// session-start scanner MUST route through this function. Bypassing it = Canon Part 4 + D-20 violation.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend EVENT_TYPES Set (+6 strings) + ALLOWED_EDGE_TYPES Set (+1 string) + scaffold harness</name>
  <files>lib/core/navigation/memory-events.cjs, lib/core/navigation/edges.cjs, tests/test-breakthrough-event-types.cjs, tests/test-breakthrough-edge-types.cjs, tests/test-120-00-scaffold.sh, lib/memory/run-feynman-tests.cjs</files>
  <read_first>
    - lib/core/navigation/memory-events.cjs (current Set contents, current size 42 post-Phase-119-01; understand the additive tail-append precedent from Phase 88.2-00 + 89-07-00 + 116-00 + 117-00 + 110-02 + 124-02 + 125-01 + 125-06 + 125-07 + 119-00 + 119-01)
    - lib/core/navigation/edges.cjs (current ALLOWED_EDGE_TYPES Set baseline {DEFERRED, REJECTED}; understand the Phase 125-00 additive tail-append precedent)
    - lib/core/navigation.cjs (the chokepoint -- understand logMemoryEvent + writeEdge thin re-exports)
    - .planning/phases/119-room-as-receipt-invariant/119-00-PLAN.md lines 260-340 (the verbatim EVENT_TYPES extension recipe Phase 120-00 mirrors)
    - .planning/phases/117-auto-explore-domains-on-first-material/117-00-SUMMARY.md (the additive-extension scaffold harness pattern)
  </read_first>
  <behavior>
    - Test 1 (EVENT_TYPES grows by exactly 6): `require('lib/core/navigation/memory-events.cjs').EVENT_TYPES.size` returns >= 48 (baseline 42 + 6 new; floor-not-exact-count idiom for forward compatibility).
    - Test 2 (named membership x6): all six strings `'breakthrough_detected_soft'`, `'breakthrough_surfaced'`, `'breakthrough_confirmed'`, `'breakthrough_dismissed'`, `'breakthrough_filed_as_decision'`, `'breakthrough_throttled'` return `.has() === true` on the Set.
    - Test 3 (ALLOWED_EDGE_TYPES grows by exactly 1): `require('lib/core/navigation/edges.cjs').ALLOWED_EDGE_TYPES.size` returns >= 3 (baseline 2 + 1 new; floor-not-exact-count idiom).
    - Test 4 (DERIVED_FROM membership): `.has('DERIVED_FROM') === true`.
    - Test 5 (logEvent round-trip for each new string): `navigation.logMemoryEvent(db, 'breakthrough_surfaced', {breakthrough_id: 'bk:1', kind: 'convergence', confidence: 0.55, source_path: 'system:breakthrough-detector', created_by: 'phase-120-scanner'})` returns `{ok:true, eventId: /^memory_event:breakthrough_surfaced:\d+:[0-9a-f]{8}$/}`. Repeat for all 6 new strings.
    - Test 6 (writeEdge round-trip for DERIVED_FROM): given a seeded db with two nodes id='bk:1' (type='breakthrough') + id='art:1' (type='artifact'), `navigation.writeEdge(db, {source_id: 'bk:1', target_id: 'art:1', edge_type: 'DERIVED_FROM', properties: {detected_at: Date.now(), detector_kind: 'convergence'}})` returns `{ok: true, edgeId: /^edge:DERIVED_FROM:/}`.
    - Test 7 (Object.freeze invariants preserved): attempting `EVENT_TYPES.add('foo')` AND `ALLOWED_EDGE_TYPES.add('FOO')` both throw TypeError in strict mode (or silently no-op in sloppy mode); both Sets remain frozen.
    - Test 8 (scaffold harness): `bash tests/test-120-00-scaffold.sh` exits 0 with the literal line `OK: 120-00 scaffold complete (6 EVENT_TYPES + 1 ALLOWED_EDGE_TYPES + zero Brain client require + zero em-dashes)`.
  </behavior>
  <action>
    **Step 1: Extend EVENT_TYPES additively in `lib/core/navigation/memory-events.cjs`.** Open the file. Find the existing `'room_discard_partial_failure'` entry (the Phase 119-01 tail, around line 157). Append the Phase 120-00 block IMMEDIATELY AFTER it, BEFORE the closing `]));` of the `Object.freeze(new Set([` call. Verbatim block to append:

    ```javascript
      // Phase 120-00 Wave 1 extension (Breakthrough Scan / Category G; D-01..D-06 detector tier
      // + D-09 file-as-decision bridge + D-10 mandatory dismiss + D-19 canary auto-throttle).
      //
      // Six strings; mirrors the Phase 119-00 3-string + 117-00 6-string + 124-02 2-string
      // additive idiom. logEvent already rejects event_type values outside EVENT_TYPES -- so
      // these are accepted only because they are now IN the Set. Set size grows by 6 (was 42
      // baseline after Phase 119-01; now 48 baseline).
      //
      // Canon Part 10 sub-claim 5: variable reward fires automatically; the math IS the surface.
      //   These six events are the canonical telemetry mirror for the breakthrough lifecycle:
      //   soft-fire (buffer-only) -> hard-fire (surfaced) -> user response (confirm/dismiss/file)
      //   -> canary auto-throttle (D-19 30%-over-100-fire-window static threshold).
      //
      // Canon Part 4: every choice is graph data. The breakthrough_filed_as_decision event is
      //   the bridge to Phase 88 decision-log machinery -- promotes a breakthrough to a
      //   first-class decision with audit trail.
      //
      // Canon Part 8: payloads carry scalar enums + framework names + sha256 hashes only;
      //   theme strings are sanitized at write time per the Phase 90-06 sanitizeDetailScalar
      //   precedent. Raw artifact content never lands in a memory_event.
      'breakthrough_detected_soft',
      'breakthrough_surfaced',
      'breakthrough_confirmed',
      'breakthrough_dismissed',
      'breakthrough_filed_as_decision',
      'breakthrough_throttled',
    ```

    Use the `Edit` tool. Do NOT reorder, mutate, or remove any prior entry. The provenance comment block is REQUIRED (mirrors the 117-00 + 119-00 + 124-02 + 110-02 precedent).

    **Step 2: Extend ALLOWED_EDGE_TYPES additively in `lib/core/navigation/edges.cjs`.** Open the file. Find the existing `'REJECTED'` entry (the Phase 125-00 tail). Append the Phase 120-00 block IMMEDIATELY AFTER it, BEFORE the closing `]));`. Verbatim block to append:

    ```javascript
      // Phase 120-00 Wave 1 extension (Breakthrough Scan / Category G; D-18 HARD FLOOR enforcement
      //   + D-20 Cypher-provable principle). DERIVED_FROM is the structural enforcement: a
      //   Breakthrough node CANNOT surface without at least one DERIVED_FROM edge to an
      //   Artifact node. Mirrors the Phase 125-00 DEFERRED + REJECTED additive idiom.
      //
      // Canon Part 4: every choice is graph data. The Breakthrough node + its DERIVED_FROM
      //   edges are the graph-native artifact of pattern detection.
      //
      // Canon Part 8: writeEdge takes (db, params) over a LOCAL room.db handle; DERIVED_FROM
      //   never crosses to Brain. Cross-room aggregation forbidden (Phase 8 cross-room fence).
      //
      // D-20 enforcement: lib/core/breakthrough/schema.cjs::writeBreakthrough wraps the
      //   Breakthrough node insert + N DERIVED_FROM edge inserts in a single SQLite transaction.
      //   If any step fails, the transaction rolls back -- partial Breakthrough state CANNOT
      //   land. The Cypher invariant `MATCH (b:Breakthrough)-[:DERIVED_FROM]->(a:Artifact)
      //   RETURN count(a)` is guaranteed >= 1 by construction.
      'DERIVED_FROM',
    ```

    **Step 3: Create `tests/test-breakthrough-event-types.cjs` (~80 LOC).** Use `node --test`. Mirror `tests/test-room-auto-create-event-types.cjs` from Phase 119-00 verbatim in structure. The 6 named-membership checks + the 6 logEvent round-trip tests + the Object.freeze invariant test. Each logEvent round-trip seeds an in-memory better-sqlite3 db with the minimal `nodes` table DDL, calls navigation.logMemoryEvent, asserts the eventId regex, and asserts the row landed in nodes table with type='memory_event'.

    **Step 4: Create `tests/test-breakthrough-edge-types.cjs` (~60 LOC).** Use `node --test`. Mirror `lib/memory/edges-write.test.cjs` (the Phase 125-00 writeEdge test) for the DERIVED_FROM addition. Tests: DERIVED_FROM membership, ALLOWED_EDGE_TYPES floor >= 3, writeEdge round-trip with a seeded db (two nodes id='bk:1' + 'art:1'), writeEdge rejection with edge_type='UNKNOWN' returns `{ok: false, reason: /invalid_edge_type/}`, Object.freeze invariant.

    **Step 5: Create `tests/test-120-00-scaffold.sh` (~70 LOC).** A 9-gate shell acceptance harness mirroring `tests/test-119-00-scaffold.sh`:
      - Gate 1: `grep -c "'breakthrough_detected_soft'" lib/core/navigation/memory-events.cjs` returns 1
      - Gate 2: `grep -c "'breakthrough_surfaced'" lib/core/navigation/memory-events.cjs` returns 1
      - Gate 3: `grep -c "'breakthrough_confirmed'" lib/core/navigation/memory-events.cjs` returns 1
      - Gate 4: `grep -c "'breakthrough_dismissed'" lib/core/navigation/memory-events.cjs` returns 1
      - Gate 5: `grep -c "'breakthrough_filed_as_decision'" lib/core/navigation/memory-events.cjs` returns 1
      - Gate 6: `grep -c "'breakthrough_throttled'" lib/core/navigation/memory-events.cjs` returns 1
      - Gate 7: `grep -c "'DERIVED_FROM'" lib/core/navigation/edges.cjs` returns 1
      - Gate 8: `grep -rE "require.+brain-client|fetch.+brain\.mindrian" lib/core/breakthrough/ tests/test-breakthrough-event-types.cjs tests/test-breakthrough-edge-types.cjs` returns 0 (Canon Part 8 invariant)
      - Gate 9: `grep -rc "—" lib/core/breakthrough/ tests/test-breakthrough-event-types.cjs tests/test-breakthrough-edge-types.cjs tests/test-120-00-scaffold.sh 2>/dev/null | grep -v ":0$" | wc -l` returns 0 (em-dash HARD RULE per CLAUDE.md)
      - On success: exit 0 with the literal line `OK: 120-00 scaffold complete (6 EVENT_TYPES + 1 ALLOWED_EDGE_TYPES + zero Brain client require + zero em-dashes)`.

    **Step 6: Wire the tests into the Feynman runner.** Edit `lib/memory/run-feynman-tests.cjs` -- append a `// Phase 120-00 Wave 1` block with paths to the 4 test files (event types + edge types + detectors test from Task 2 + schema test from Task 3) at the tail of the CJS_SUITES array immediately after the Phase 125 block. Comment block REQUIRED with provenance ("Phase 120-00 Wave 1: 4 breakthrough pattern detectors + Breakthrough graph schema").

    Per D-01..D-06 lock: every threshold string + threshold number in the comment block references the CONTEXT.md decision ID it implements (D-01 / D-02 / D-03 / D-09 / D-10 / D-18 / D-19 / D-20). Cross-references back to CONTEXT.md decisions by ID so the plan-checker can verify locked-decision coverage.
  </action>
  <verify>
    <automated>node -e "const e=require('./lib/core/navigation/memory-events.cjs').EVENT_TYPES; if(!e.has('breakthrough_detected_soft')||!e.has('breakthrough_surfaced')||!e.has('breakthrough_confirmed')||!e.has('breakthrough_dismissed')||!e.has('breakthrough_filed_as_decision')||!e.has('breakthrough_throttled')){process.exit(1)}" && node -e "const ed=require('./lib/core/navigation/edges.cjs').ALLOWED_EDGE_TYPES; if(!ed.has('DERIVED_FROM')){process.exit(1)}" && node --test tests/test-breakthrough-event-types.cjs tests/test-breakthrough-edge-types.cjs && bash tests/test-120-00-scaffold.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./lib/core/navigation/memory-events.cjs').EVENT_TYPES.size)"` returns an integer >= 48 (baseline 42 + 6 new).
    - All 6 named-membership checks (`.has('breakthrough_detected_soft')`, `'breakthrough_surfaced'`, `'breakthrough_confirmed'`, `'breakthrough_dismissed'`, `'breakthrough_filed_as_decision'`, `'breakthrough_throttled'`) return `true`.
    - `node -e "console.log(require('./lib/core/navigation/edges.cjs').ALLOWED_EDGE_TYPES.size)"` returns an integer >= 3 (baseline 2 + 1 new).
    - `node -e "console.log(require('./lib/core/navigation/edges.cjs').ALLOWED_EDGE_TYPES.has('DERIVED_FROM'))"` returns `true`.
    - `node --test tests/test-breakthrough-event-types.cjs tests/test-breakthrough-edge-types.cjs` reports 0 failures with at least 14 tests passing (6 named-membership + 6 logEvent round-trip + 1 DERIVED_FROM round-trip + 1 frozen invariant).
    - `bash tests/test-120-00-scaffold.sh` exits 0 with stdout containing the literal `OK: 120-00 scaffold complete`.
    - `grep -c "Phase 120-00 Wave 1 extension" lib/core/navigation/memory-events.cjs` returns 1 (provenance comment landed).
    - `grep -c "Phase 120-00 Wave 1 extension" lib/core/navigation/edges.cjs` returns 1 (provenance comment landed).
    - `grep -rc "—" lib/core/breakthrough/ tests/test-breakthrough-event-types.cjs tests/test-breakthrough-edge-types.cjs tests/test-120-00-scaffold.sh 2>/dev/null | grep -v ":0$"` returns no output (em-dash invariant).
    - **D-20 surface check** (covered fully in Task 3): for every breakthrough_surfaced memory_event emitted in any subsequent test in this phase, the Cypher invariant `MATCH (b:Breakthrough)-[:DERIVED_FROM]->(a:Artifact) WHERE b.id = $surfaced_id RETURN count(a)` returns >= 1. This is a CONSTITUTIONAL acceptance criterion: if Task 3 ever lands a breakthrough_surfaced event without the corresponding DERIVED_FROM provenance, that surfacing is a Canon Part 8 + D-20 violation and the test fails. The Task 3 `validateProvenance` function is the structural guard.
  </acceptance_criteria>
  <done>
    EVENT_TYPES Set extended by exactly 6 strings (`breakthrough_detected_soft`, `breakthrough_surfaced`, `breakthrough_confirmed`, `breakthrough_dismissed`, `breakthrough_filed_as_decision`, `breakthrough_throttled`) and ALLOWED_EDGE_TYPES Set extended by exactly 1 string (`DERIVED_FROM`) via the additive tail-append precedent; 14+ tests passing across the 2 test files; scaffold harness passes all 9 gates including the no-em-dash invariant; Feynman runner registers the new tests so they fire on every release flight; provenance comment blocks land in both extended files with cross-references to CONTEXT.md D-01..D-20.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the 4 pattern detectors + classifyFireTier + DETECTOR_THRESHOLDS in lib/core/breakthrough/detectors.cjs</name>
  <files>lib/core/breakthrough/detectors.cjs, lib/core/breakthrough/detectors.test.cjs</files>
  <read_first>
    - lib/agents/auto-explore-agent.cjs lines 260-340 (composeAutoExploreFinding signature -- understand whitespace.gaps, rs.pairs, analogy.zones shapes Phase 120 detectors consume)
    - scripts/auto-explore-fire.cjs lines 200-260 (the JSON file paths .mindrian/whitespace-results.json + .mindrian/.rs-engine-results.json + .mindrian/discovery-cycle-results.json the detectors read)
    - lib/core/navigation.cjs (the chokepoint surface; understand findRecentChanges -- detectors use this for the trailing-window scan)
    - lib/core/navigation/memory-events.cjs (the EVENT_TYPES Set extended in Task 1; understand which event types detectors will read via findRecentChanges)
    - .planning/phases/119-room-as-receipt-invariant/119-00-PLAN.md Task 2 (the room-auto-create.cjs structural pattern; mirror its defensive invariant + return shape style + Canon Part 8 source-grep audit pattern)
  </read_first>
  <behavior>
    - Test 1 (DETECTOR_THRESHOLDS verbatim): the 7 named constants exist, are frozen, and have the locked numeric values: `SOFT_FIRE_MIN_ARTIFACTS=3`, `SOFT_FIRE_MIN_CONFIDENCE=0.25`, `HARD_FIRE_MIN_ARTIFACTS=4`, `HARD_FIRE_CROSS_SECTION_BYPASS=3`, `HARD_FIRE_MIN_CONFIDENCE=0.35`, `SEMANTIC_SIMILARITY_THRESHOLD=0.40`, `WINDOW_DAYS_DEFAULT=14`. Object.freeze invariant: `DETECTOR_THRESHOLDS.SOFT_FIRE_MIN_ARTIFACTS = 99` throws / no-ops.
    - Test 2 (DETECTOR_TYPES verbatim): the 4 kind strings exist as frozen array `['convergence', 'contradiction_resolved', 'cross_domain_analogy', 'reverse_salient_closed']`.
    - Test 3 (detectConvergence happy path): given a roomState with whitespace.gaps = [{theme: 'X', artifacts: ['a1','a2','a3','a4'], differential: 0.7}], returns `{hits: [{kind:'convergence', artifact_ids:['a1','a2','a3','a4'], confidence: >=0.35, theme:'X'}], soft_fires: []}`. Confidence is a function of artifact_count + differential (formula documented in source as `Math.min(0.95, 0.25 + 0.10*count + 0.30*differential)`).
    - Test 4 (detectConvergence soft-fire branch): given whitespace.gaps = [{theme:'Y', artifacts:['a1','a2','a3'], differential: 0.3}], returns `{hits: [], soft_fires: [{kind:'convergence', artifact_ids:['a1','a2','a3'], confidence: 0.25..0.349, theme:'Y'}]}`. Sub-confidence-0.35 fires soft only.
    - Test 5 (detectConvergence below-floor): given whitespace.gaps = [{theme:'Z', artifacts:['a1','a2'], differential: 0.1}], returns `{hits: [], soft_fires: []}`. < 3 artifacts = below floor; nothing fires.
    - Test 6 (detectContradictionResolved): given a seeded room.db with two CONTRADICTS edges in the window where the older one has properties.resolved=true, returns 1 hit (or soft_fire depending on confidence) with kind='contradiction_resolved' + artifact_ids = [source, target].
    - Test 7 (detectCrossDomainAnalogy semantic threshold): given analogy.zones = [{similarity: 0.42, source_section:'a', target_section:'b', source_artifact_id:'art1', target_artifact_id:'art2'}], returns 1 hit (cross-section linked = true via different source_section vs target_section). Same shape with similarity=0.38 returns 0 hits (below SEMANTIC_SIMILARITY_THRESHOLD).
    - Test 8 (detectReverseSalientClosed both-signals invariant per D-05): given rs.pairs = [{pair_id:'p1', signed_diff: -0.6, source_artifact_id:'a1', target_artifact_id:'a2', closure_edge_now_exists: true}], returns 1 hit. With closure_edge_now_exists=false (only score-delta signal), returns soft_fire only. With signed_diff=0.1 (no score-delta) and closure_edge_now_exists=true (only graph-level signal), returns soft_fire only. Both signals required = hard hit.
    - Test 9 (cross_section_linked bypass per D-03): a 3-artifact convergence where the 3 artifacts span at least 2 sections (cross_section_linked=true) AT confidence>=0.35 fires HARD (not soft). The 3-artifact-with-bypass branch covers the D-03 "OR 3 artifacts with cross-section linkage" clause.
    - Test 10 (window enforcement): given a candidate with detected_at older than `Date.now() - WINDOW_DAYS_DEFAULT * 24*3600*1000`, the detector returns 0 hits. Configurable via `opts.window_days` (D-06 ethical fence; capped at 14).
    - Test 11 (classifyFireTier): the function returns 'hard' / 'soft' / 'below_floor' per the threshold matrix. Hand-coded matrix in test (8 cases covering each axis).
    - Test 12 (Canon Part 8 invariant -- source-grep): `grep -E "require.+brain-client|fetch.+brain\.mindrian|cross.+user|cross.+room.+aggregat" lib/core/breakthrough/detectors.cjs` returns 0.
    - Test 13 (no recomputation of math): the detector reads `whitespace.gaps[]` etc. directly -- it does NOT require `sentence-transformers`, `scripts/hsi-*`, `compute-disruption-index.py`, or any Python script. Source-grep returns 0 for `child_process.execSync.+hsi|child_process.execFileSync.+\.py`.
  </behavior>
  <action>
    Create `lib/core/breakthrough/detectors.cjs` (~280 LOC) implementing the four detectors + classifier:

    **Module structure:**
    ```javascript
    'use strict';
    // Phase 120-00 Wave 1 -- the 4 breakthrough pattern detectors + classifyFireTier + the
    // frozen threshold constants. Per CONTEXT.md D-01..D-06 + D-18 + D-20:
    //   - PURE functions over local graph state (Canon Part 8 -- no Brain coupling)
    //   - ALL reads via lib/core/navigation.cjs chokepoint (Canon Part 9 D-06)
    //   - Every emitted Breakthrough carries provenance.artifact_ids (D-20 HARD FLOOR)
    //   - Reads Phase 117 math-layer JSON output; NEVER recomputes the math

    const path = require('node:path');
    const fs = require('node:fs');
    const crypto = require('node:crypto');
    const navigation = require('../navigation.cjs');

    const DETECTOR_THRESHOLDS = Object.freeze({
      SOFT_FIRE_MIN_ARTIFACTS: 3,             // D-02
      SOFT_FIRE_MIN_CONFIDENCE: 0.25,         // D-02
      HARD_FIRE_MIN_ARTIFACTS: 4,             // D-03
      HARD_FIRE_CROSS_SECTION_BYPASS: 3,      // D-03 OR clause
      HARD_FIRE_MIN_CONFIDENCE: 0.35,         // D-03
      SEMANTIC_SIMILARITY_THRESHOLD: 0.40,    // D-04 (within-room noise correlation pushes threshold up)
      WINDOW_DAYS_DEFAULT: 14,                // D-06 ethical fence (no surface for material > 14 days old)
    });

    const DETECTOR_TYPES = Object.freeze([
      'convergence',
      'contradiction_resolved',
      'cross_domain_analogy',
      'reverse_salient_closed',
    ]);

    function buildBreakthroughId(kind, artifactIds, nowMs) {
      const seed = kind + ':' + artifactIds.sort().join(',') + ':' + nowMs;
      const hash = crypto.createHash('sha256').update(seed).digest('hex').slice(0, 16);
      return 'breakthrough:' + kind + ':' + hash;
    }

    function buildCandidate(kind, artifactIds, theme, differential, crossSectionLinked, nowMs, windowMs) {
      // Confidence formula (kind-specific; defaults to artifact_count + differential blend):
      const baseConf = 0.25 + 0.10 * artifactIds.length + 0.30 * differential;
      const confidence = Math.min(0.95, Math.max(0, baseConf));
      return {
        id: buildBreakthroughId(kind, artifactIds, nowMs),
        kind: kind,
        confidence: confidence,
        artifact_ids: artifactIds.slice(),
        theme: typeof theme === 'string' ? theme.slice(0, 200) : '',  // Canon Part 8 sanitize
        differential: differential,
        cross_section_linked: !!crossSectionLinked,
        detected_at: nowMs,
        window_start: nowMs - windowMs,
        window_end: nowMs,
      };
    }

    function classifyFireTier(candidate) {
      const T = DETECTOR_THRESHOLDS;
      const count = candidate.artifact_ids.length;
      const conf = candidate.confidence;
      const hard_eligible =
        ((count >= T.HARD_FIRE_MIN_ARTIFACTS) ||
         (count >= T.HARD_FIRE_CROSS_SECTION_BYPASS && candidate.cross_section_linked))
        && conf >= T.HARD_FIRE_MIN_CONFIDENCE;
      if (hard_eligible) return 'hard';
      const soft_eligible = count >= T.SOFT_FIRE_MIN_ARTIFACTS && conf >= T.SOFT_FIRE_MIN_CONFIDENCE;
      if (soft_eligible) return 'soft';
      return 'below_floor';
    }

    function partitionByTier(candidates) {
      const hits = [];
      const soft_fires = [];
      for (const c of candidates) {
        const tier = classifyFireTier(c);
        if (tier === 'hard') hits.push(c);
        else if (tier === 'soft') soft_fires.push(c);
      }
      return { hits, soft_fires };
    }

    // --- detectConvergence (D-01 detector 1) ---
    // Reads whitespace.gaps[] from .mindrian/whitespace-results.json (Phase 117 math output).
    // Each gap: {theme, artifacts: string[], differential: number, sections?: string[]}.
    // A gap with N artifacts in the window where N >= 3 + confidence formula passes the soft floor.
    function detectConvergence(roomState, opts) {
      // ... implementation per behavior tests 3-5 + cross_section bypass per behavior test 9
    }

    // --- detectContradictionResolved (D-01 detector 2) ---
    // Reads CONTRADICTS edges from room.db via navigation.cjs query (a getNeighborhood or
    // findRecentChanges variant). When a CONTRADICTS edge in the window now has properties.resolved=true
    // AND the corresponding pair of artifacts still exists, that's a candidate. The "resolved" flag is
    // assumed to be set by Phase 116 unresolved-tension-hook closure logic when a tension is resolved
    // by a new artifact.
    function detectContradictionResolved(roomState, opts) {
      // ... implementation per behavior test 6
    }

    // --- detectCrossDomainAnalogy (D-01 detector 3) ---
    // Reads analogy.zones[] from .mindrian/discovery-cycle-results.json (Phase 117 math output).
    // Per Canon Part 8 + D-CONTEXT IN SCOPE clarification: "cross-domain means cross-section within
    // the same room, NEVER cross-user or cross-room aggregation". Cross-section is encoded by
    // source_section !== target_section AND similarity >= SEMANTIC_SIMILARITY_THRESHOLD (D-04).
    function detectCrossDomainAnalogy(roomState, opts) {
      // ... implementation per behavior test 7
    }

    // --- detectReverseSalientClosed (D-01 detector 4) ---
    // Reads rs.pairs[] from .mindrian/.rs-engine-results.json (Phase 117 math output).
    // Per CONTEXT.md D-05: BOTH signals required for hard-fire:
    //   (a) graph-level proof -- closure_edge_now_exists === true (a structural Cypher-provable
    //       transition that did not hold at window_start but holds now), AND
    //   (b) score-delta -- |signed_diff| >= a threshold (~0.5; the lagging-component score crossed
    //       its baseline within the window).
    // Single-signal candidates return soft_fire only -- single-signal closure is where false
    // positives live (CONTEXT.md D-05 verbatim).
    function detectReverseSalientClosed(roomState, opts) {
      // ... implementation per behavior test 8
    }

    module.exports = {
      detectConvergence,
      detectContradictionResolved,
      detectCrossDomainAnalogy,
      detectReverseSalientClosed,
      classifyFireTier,
      partitionByTier,
      buildCandidate,
      buildBreakthroughId,
      DETECTOR_THRESHOLDS,
      DETECTOR_TYPES,
    };
    ```

    **Reading the math-layer files (D-01 mandate -- detectors NEVER recompute):**
    Each of the 4 detectors takes a `roomState` object with shape `{roomDir, db, now?, window_days?}`. The detector function:
    1. Computes `windowMs = (opts.window_days || DETECTOR_THRESHOLDS.WINDOW_DAYS_DEFAULT) * 24*3600*1000` capped at 14 days.
    2. Computes `windowStart = nowMs - windowMs`.
    3. Resolves the math-layer JSON file path within `roomDir/.mindrian/`. If the file does not exist, returns `{hits: [], soft_fires: []}` (graceful degradation: Phase 117 may not have fired yet on a fresh room).
    4. Parses the JSON via `JSON.parse(fs.readFileSync(...))` wrapped in try/catch -- malformed JSON returns the same empty result with NO throw.
    5. Filters records by their `detected_at` / `mtime_seconds` field >= windowStart. Records older than the window are excluded (D-06 ethical fence).
    6. Builds candidates via `buildCandidate(kind, artifact_ids, theme, differential, cross_section_linked, nowMs, windowMs)`.
    7. Calls `partitionByTier(candidates)` and returns `{hits, soft_fires}`.

    For `detectContradictionResolved`, step 3 instead reads the room.db CONTRADICTS edges via `navigation.getNeighborhood` (or `navigation.findRecentChanges` with an event filter for edge_added with edge_type=CONTRADICTS, then filter by properties.resolved). Falls back to empty result on chokepoint error.

    **Canon Part 8 defensive invariants:**
    - The `theme` field is sliced to 200 chars maximum (sanitization per Phase 90-06 sanitizeDetailScalar precedent). Raw artifact text is NEVER assigned to theme; only the Phase 117 math layer's pre-computed theme label.
    - NEVER require `lib/core/brain-client.cjs` or any module under `mcp-server-brain/`.
    - NEVER call `child_process.execSync` with a Python script path (no math recomputation).
    - The `artifact_ids` array is filtered to exclude empty strings + non-string entries before being passed to `buildCandidate`. This is the upstream of the D-20 HARD FLOOR (validateProvenance in Task 3 enforces it again at write time).
    - Every comment + log line uses `--` not `—` (em-dash HARD RULE).

    Create `lib/core/breakthrough/detectors.test.cjs` (~250 LOC) implementing the 13 behavior tests. Use `node --test`. Mock `roomState` via `os.tmpdir() + '/phase120-test-' + crypto.randomBytes(4).toString('hex')` + seed `.mindrian/*.json` files inline.
  </action>
  <verify>
    <automated>node --test lib/core/breakthrough/detectors.test.cjs && grep -c "require.*brain-client" lib/core/breakthrough/detectors.cjs && grep -c "child_process.execSync.*\.py\|child_process.execFileSync.*\.py" lib/core/breakthrough/detectors.cjs && node -e "const d=require('./lib/core/breakthrough/detectors.cjs'); if(d.DETECTOR_THRESHOLDS.SOFT_FIRE_MIN_ARTIFACTS!==3||d.DETECTOR_THRESHOLDS.HARD_FIRE_MIN_ARTIFACTS!==4||d.DETECTOR_THRESHOLDS.SEMANTIC_SIMILARITY_THRESHOLD!==0.40||d.DETECTOR_THRESHOLDS.WINDOW_DAYS_DEFAULT!==14){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test lib/core/breakthrough/detectors.test.cjs` reports 0 failures with at least 13 tests passing (DETECTOR_THRESHOLDS verbatim, DETECTOR_TYPES verbatim, 4 detector happy-paths, 2-tier classifier, cross_section bypass, window enforcement, both-signals invariant for reverse-salient closed, Canon Part 8 source-grep, no math recomputation).
    - `grep -c "require.*brain-client" lib/core/breakthrough/detectors.cjs` returns 0 (Canon Part 8 invariant).
    - `grep -c "child_process.exec.+\.py" lib/core/breakthrough/detectors.cjs` returns 0 (no math recomputation invariant).
    - `node -e "const d=require('./lib/core/breakthrough/detectors.cjs'); console.log(d.DETECTOR_THRESHOLDS.SOFT_FIRE_MIN_ARTIFACTS)"` prints exactly `3`.
    - `node -e "const d=require('./lib/core/breakthrough/detectors.cjs'); console.log(d.DETECTOR_THRESHOLDS.HARD_FIRE_MIN_ARTIFACTS)"` prints exactly `4`.
    - `node -e "const d=require('./lib/core/breakthrough/detectors.cjs'); console.log(d.DETECTOR_THRESHOLDS.SEMANTIC_SIMILARITY_THRESHOLD)"` prints exactly `0.4`.
    - `node -e "const d=require('./lib/core/breakthrough/detectors.cjs'); console.log(d.DETECTOR_THRESHOLDS.WINDOW_DAYS_DEFAULT)"` prints exactly `14`.
    - `node -e "const d=require('./lib/core/breakthrough/detectors.cjs'); console.log(JSON.stringify(d.DETECTOR_TYPES))"` prints exactly `["convergence","contradiction_resolved","cross_domain_analogy","reverse_salient_closed"]`.
    - `grep -c "—" lib/core/breakthrough/detectors.cjs` returns 0 (em-dash invariant).
    - `grep -c "D-01\|D-02\|D-03\|D-04\|D-05\|D-06" lib/core/breakthrough/detectors.cjs` returns at least 6 (decision-ID provenance comments).
  </acceptance_criteria>
  <done>
    `lib/core/breakthrough/detectors.cjs` exports the 4 detector functions (detectConvergence, detectContradictionResolved, detectCrossDomainAnalogy, detectReverseSalientClosed) + classifyFireTier + partitionByTier + buildCandidate + buildBreakthroughId + the frozen DETECTOR_THRESHOLDS and DETECTOR_TYPES constants. All detectors read Phase 117 math output via JSON files (no recomputation); ALL chokepoint reads route through navigation.cjs; ZERO Brain MCP coupling; D-05 both-signals invariant enforced for reverse-salient closed; D-04 0.40 semantic threshold enforced for cross-domain analogy; D-03 cross-section bypass implemented; D-06 14-day window cap enforced. 13+ tests passing via node --test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build writeBreakthrough + validateProvenance (the D-20 HARD FLOOR structural enforcement) in lib/core/breakthrough/schema.cjs</name>
  <files>lib/core/breakthrough/schema.cjs, lib/core/breakthrough/schema.test.cjs</files>
  <read_first>
    - lib/core/navigation.cjs (the chokepoint -- writeBreakthrough routes ALL writes through navigation.logMemoryEvent + navigation.writeEdge)
    - lib/core/navigation/edges.cjs (the ALLOWED_EDGE_TYPES Set extended in Task 1; writeEdge accepts edge_type='DERIVED_FROM' params)
    - lib/core/breakthrough/detectors.cjs (created in Task 2; understand the breakthrough.id + artifact_ids shape writeBreakthrough consumes)
    - lib/core/room-discard-cascade.cjs lines 1-50 + 160-210 (Phase 119-01 atomic-transaction precedent -- SQLite BEGIN / COMMIT / ROLLBACK pattern for multi-step writes; mirror this for the writeBreakthrough transaction)
    - .planning/phases/125-investment-gradient/125-00-PLAN.md (the writeEdge thin-export precedent + the structural-invariant test pattern)
  </read_first>
  <behavior>
    - Test 1 (validateProvenance happy): `validateProvenance({id:'bk:1', artifact_ids:['a1','a2']})` returns `{ok: true}`.
    - Test 2 (validateProvenance D-20 HARD FLOOR rejection -- empty artifact_ids): `validateProvenance({id:'bk:1', artifact_ids:[]})` returns `{ok: false, reason: 'provenance_required'}`. Per D-20: every breakthrough MUST be Cypher-provable from graph state alone; a breakthrough with zero DERIVED_FROM targets is a CONSTITUTIONAL VIOLATION.
    - Test 3 (validateProvenance D-20 HARD FLOOR rejection -- empty-string artifact ids): `validateProvenance({id:'bk:1', artifact_ids:['']})` returns `{ok: false, reason: 'provenance_required'}` (empty strings filtered out).
    - Test 4 (validateProvenance D-20 HARD FLOOR rejection -- missing id): `validateProvenance({artifact_ids:['a1']})` returns `{ok: false, reason: 'breakthrough_id_required'}`.
    - Test 5 (writeBreakthrough happy path -- transaction completes): given a seeded room.db with two `artifact` nodes ('a1', 'a2') + a candidate `{id:'bk:happy', kind:'convergence', confidence:0.55, artifact_ids:['a1','a2'], theme:'X', differential:0.6, cross_section_linked:false, detected_at:nowMs, window_start:earlier, window_end:nowMs}`, calling `writeBreakthrough(db, candidate)` returns `{ok: true, eventId: /^memory_event:.../, edgeIds: ['edge:DERIVED_FROM:...', 'edge:DERIVED_FROM:...']}` AND afterwards: (a) a `breakthrough` type node exists in `nodes` table with the breakthrough.id, (b) 2 DERIVED_FROM edges exist in `edges` table linking bk:happy -> a1 and bk:happy -> a2.
    - Test 6 (writeBreakthrough rejects provenance-less input): calling `writeBreakthrough(db, {id:'bk:bad', artifact_ids:[]})` returns `{ok: false, reason: 'provenance_required'}` AND afterwards: NO `breakthrough` node row landed AND NO DERIVED_FROM edge row landed (transaction rollback).
    - Test 7 (D-20 CONSTITUTIONAL TEST -- Cypher invariant): after Test 5's successful write, the SQL query `SELECT COUNT(*) AS c FROM edges WHERE source_id='bk:happy' AND edge_type='DERIVED_FROM'` returns `>= 1`. This is the SQL equivalent of the canonical Cypher invariant `MATCH (b:Breakthrough)-[:DERIVED_FROM]->(a:Artifact) WHERE b.id = 'bk:happy' RETURN count(a)`.
    - Test 8 (atomic rollback on writeEdge failure): mock the db so the SECOND writeEdge call throws (simulated FK constraint violation -- target_id 'a-missing' doesn't exist as a node). Calling `writeBreakthrough(db, {id:'bk:partial', artifact_ids:['a1','a-missing']})` returns `{ok: false, reason: /writeEdge_failed/}` AND afterwards: NO `breakthrough` node row landed (the transaction rolled back the node insert too).
    - Test 9 (initial `surfaced` flag is false): the inserted `breakthrough` node has `properties.surfaced === false` initially. Plan 120-02 scanner flips this to true when it surfaces via F.7.
    - Test 10 (D-20 batch invariant -- every Breakthrough in test db has provenance): after running Tests 5-8, the SQL query `SELECT b.id FROM nodes b WHERE b.type='breakthrough' AND NOT EXISTS (SELECT 1 FROM edges e WHERE e.source_id=b.id AND e.edge_type='DERIVED_FROM')` returns ZERO rows. The constitutional invariant holds.
    - Test 11 (Canon Part 8 source-grep): `grep -E "require.+brain-client|fetch.+brain\.mindrian" lib/core/breakthrough/schema.cjs` returns 0.
    - Test 12 (memory_event emission shape): the `breakthrough_surfaced` event is NOT emitted by writeBreakthrough; only the `breakthrough_detected_soft` event is emitted for soft fires (Plan 120-02 scanner emits `breakthrough_surfaced` AT SURFACE TIME after the F.7 selector renders). writeBreakthrough is shape-agnostic to surfacing decisions -- it lands the graph state; the scanner promotes via the surfacing event.
  </behavior>
  <action>
    Create `lib/core/breakthrough/schema.cjs` (~140 LOC):

    ```javascript
    'use strict';
    // Phase 120-00 Wave 1 -- Breakthrough node schema + writeBreakthrough atomic-transaction
    // writer. Per CONTEXT.md D-18 HARD FLOOR + D-20 Cypher-provable principle: every
    // Breakthrough node that lands in room.db MUST have at least one DERIVED_FROM edge to an
    // Artifact node. The Cypher invariant
    //   MATCH (b:Breakthrough)-[:DERIVED_FROM]->(a:Artifact) WHERE b.id = $id RETURN count(a)
    // is guaranteed >= 1 BY CONSTRUCTION (this function refuses to land a breakthrough without
    // provenance) AND BY TRANSACTION (the node insert + N edge inserts are wrapped in a single
    // SQLite transaction; partial state cannot land).
    //
    // Canon Part 4: every choice is graph data. The Breakthrough node is the typed graph
    //   artifact of pattern detection.
    // Canon Part 8: writes are LOCAL only; no Brain coupling.
    // Canon Part 9: ALL writes route through navigation.cjs chokepoint.
    //
    // CONSTITUTIONAL: this function is the ONLY way breakthroughs land in room.db. Plan 120-02
    // session-start scanner MUST route through this function. Bypassing it = Canon Part 4 + D-20
    // violation.

    const crypto = require('node:crypto');
    const navigation = require('../navigation.cjs');

    const BREAKTHROUGH_NODE_TYPE = 'breakthrough';
    const BREAKTHROUGH_KIND = Object.freeze({
      CONVERGENCE: 'convergence',
      CONTRADICTION_RESOLVED: 'contradiction_resolved',
      CROSS_DOMAIN_ANALOGY: 'cross_domain_analogy',
      REVERSE_SALIENT_CLOSED: 'reverse_salient_closed',
    });

    function validateProvenance(breakthrough) {
      if (!breakthrough || typeof breakthrough !== 'object') {
        return { ok: false, reason: 'invalid_input' };
      }
      if (typeof breakthrough.id !== 'string' || breakthrough.id.length === 0) {
        return { ok: false, reason: 'breakthrough_id_required' };
      }
      const ids = Array.isArray(breakthrough.artifact_ids)
        ? breakthrough.artifact_ids.filter(s => typeof s === 'string' && s.length > 0)
        : [];
      if (ids.length === 0) {
        // D-20 HARD FLOOR: every breakthrough must be Cypher-provable. Provenance-less
        // breakthroughs are a CONSTITUTIONAL VIOLATION; the write is refused at the gate.
        return { ok: false, reason: 'provenance_required' };
      }
      return { ok: true, sanitized_artifact_ids: ids };
    }

    function writeBreakthrough(db, breakthrough) {
      const validation = validateProvenance(breakthrough);
      if (!validation.ok) {
        return { ok: false, reason: validation.reason };
      }
      const artifactIds = validation.sanitized_artifact_ids;
      const nowMs = Date.now();
      const breakthroughId = breakthrough.id;

      // Atomic transaction: node insert + N DERIVED_FROM edge inserts. If any step fails,
      // the entire transaction rolls back -- partial Breakthrough state cannot land.
      const tx = db.transaction(() => {
        // 1. Insert the Breakthrough node directly into the `nodes` table. The properties JSON
        //    carries kind / confidence / theme / differential / cross_section_linked /
        //    detected_at / window_start / window_end / surfaced (initially false).
        const props = {
          kind: breakthrough.kind,
          confidence: breakthrough.confidence,
          theme: typeof breakthrough.theme === 'string' ? breakthrough.theme.slice(0, 200) : '',
          differential: breakthrough.differential,
          cross_section_linked: !!breakthrough.cross_section_linked,
          detected_at: breakthrough.detected_at || nowMs,
          window_start: breakthrough.window_start || (nowMs - 14*24*3600*1000),
          window_end: breakthrough.window_end || nowMs,
          surfaced: false,  // Plan 120-02 scanner flips this to true at F.7 surface time.
        };
        db.prepare(
          "INSERT INTO nodes (id, type, properties, source_path, created_by, confidence, review_status, created_at, last_seen_at) " +
          "VALUES (?, 'breakthrough', ?, 'system:breakthrough-schema', 'phase-120-detectors', ?, 'proposed', ?, ?)"
        ).run(breakthroughId, JSON.stringify(props), breakthrough.confidence, nowMs, nowMs);

        // 2. Insert N DERIVED_FROM edges via the navigation.cjs writeEdge chokepoint.
        //    Each edge: source_id = breakthroughId, target_id = artifactId, edge_type =
        //    'DERIVED_FROM', properties = {detected_at, detector_kind}.
        const edgeIds = [];
        for (const artifactId of artifactIds) {
          const result = navigation.writeEdge(db, {
            source_id: breakthroughId,
            target_id: artifactId,
            edge_type: 'DERIVED_FROM',
            properties: { detected_at: nowMs, detector_kind: breakthrough.kind },
          });
          if (!result.ok) {
            // Throw to trigger transaction rollback. Better-sqlite3 transactions rollback on
            // any throw inside the wrapped function (the standard pattern).
            throw new Error('writeEdge_failed:' + result.reason);
          }
          edgeIds.push(result.edgeId);
        }

        return { edgeIds };
      });

      let result;
      try {
        result = tx();
      } catch (err) {
        return { ok: false, reason: err.message || 'transaction_failed' };
      }

      return {
        ok: true,
        breakthroughId: breakthroughId,
        edgeIds: result.edgeIds,
      };
    }

    module.exports = {
      writeBreakthrough,
      validateProvenance,
      BREAKTHROUGH_NODE_TYPE,
      BREAKTHROUGH_KIND,
    };
    ```

    **Canon Part 8 defensive invariants:**
    - Theme sliced to 200 chars (sanitization).
    - NEVER require `lib/core/brain-client.cjs`.
    - NEVER bypass navigation.cjs for edge writes (all DERIVED_FROM edges go through navigation.writeEdge).
    - Em-dash HARD RULE: every comment + log uses `--`.

    **Canon Part 9 defensive invariants:**
    - Direct `db.prepare(...).run(...)` for the `nodes` table insert is the ONLY non-navigation-routed write in this module. This is the SAME pattern used by `lib/core/navigation/memory-events.cjs::logEvent` (which itself does a direct nodes insert because it IS the chokepoint helper). Plan 120-00 mirrors this precedent: schema.cjs lives in `lib/core/breakthrough/` not in `lib/core/navigation/` but operates as the per-type insert helper, similar to how memory-events.cjs operates as the per-type insert helper for memory_event nodes. The DERIVED_FROM edges DO route through navigation.writeEdge -- that's the chokepoint-respecting half of the contract.

    **D-20 STRUCTURAL ENFORCEMENT (the CONSTITUTIONAL load-bearing acceptance criterion):**
    The transaction wrapper guarantees that EITHER (a) the Breakthrough node AND all its DERIVED_FROM edges land together, OR (b) NEITHER lands. There is no partial state where a Breakthrough node exists without provenance. This is the structural enforcement of D-20 ("if you cannot write a Cypher query that proves a breakthrough is real, it shouldn't surface") -- not a procedural promise, an SQL-transaction-level guarantee.

    Create `lib/core/breakthrough/schema.test.cjs` (~200 LOC) implementing all 12 behavior tests. Use `node --test`. Seed a fresh better-sqlite3 in-memory db with the `nodes` + `edges` table DDL per the Phase 109 schema. Test 7 (the D-20 Cypher invariant SQL query) is THE LOAD-BEARING acceptance criterion of the whole plan.
  </action>
  <verify>
    <automated>node --test lib/core/breakthrough/schema.test.cjs && grep -c "require.*brain-client" lib/core/breakthrough/schema.cjs && node -e "const s=require('./lib/core/breakthrough/schema.cjs'); const v=s.validateProvenance({id:'bk:x', artifact_ids:[]}); if(v.ok||v.reason!=='provenance_required'){process.exit(1)}; const v2=s.validateProvenance({id:'bk:x', artifact_ids:['a1']}); if(!v2.ok){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test lib/core/breakthrough/schema.test.cjs` reports 0 failures with at least 12 tests passing.
    - `grep -c "require.*brain-client" lib/core/breakthrough/schema.cjs` returns 0 (Canon Part 8 invariant).
    - **D-20 CONSTITUTIONAL TEST -- the load-bearing acceptance criterion of the whole phase:** after running the schema.test.cjs Test 5 (writeBreakthrough happy path), the SQL query `SELECT COUNT(*) AS c FROM edges WHERE source_id='bk:happy' AND edge_type='DERIVED_FROM'` returns >= 1. This is the SQL equivalent of the canonical Cypher invariant `MATCH (b:Breakthrough)-[:DERIVED_FROM]->(a:Artifact) WHERE b.id = 'bk:happy' RETURN count(a)`. If this query returns 0 against a successfully-landed breakthrough, the write was a CONSTITUTIONAL VIOLATION and Phase 120 D-20 has failed.
    - **D-20 BATCH INVARIANT:** the SQL query `SELECT b.id FROM nodes b WHERE b.type='breakthrough' AND NOT EXISTS (SELECT 1 FROM edges e WHERE e.source_id=b.id AND e.edge_type='DERIVED_FROM')` returns ZERO rows after any sequence of writeBreakthrough calls. Every breakthrough in the db has provenance by construction.
    - `node -e "const s=require('./lib/core/breakthrough/schema.cjs'); console.log(s.validateProvenance({id:'bk:x',artifact_ids:[]}).reason)"` prints exactly `provenance_required`.
    - `node -e "const s=require('./lib/core/breakthrough/schema.cjs'); console.log(s.validateProvenance({id:'bk:x',artifact_ids:['a1']}).ok)"` prints exactly `true`.
    - `grep -c "—" lib/core/breakthrough/schema.cjs` returns 0 (em-dash invariant).
    - `grep -c "D-18\|D-20" lib/core/breakthrough/schema.cjs` returns at least 2 (decision-ID provenance comments).
    - `grep -c "transaction" lib/core/breakthrough/schema.cjs` returns at least 2 (atomic-transaction pattern documented + used).
  </acceptance_criteria>
  <done>
    `lib/core/breakthrough/schema.cjs` exports `writeBreakthrough` + `validateProvenance` + `BREAKTHROUGH_NODE_TYPE` + `BREAKTHROUGH_KIND` constants. `writeBreakthrough` is the ONLY way Breakthrough nodes land in room.db (CONSTITUTIONAL); it wraps the node insert + N DERIVED_FROM edge inserts in a single SQLite transaction so partial Breakthrough state cannot land. The D-20 Cypher-provable invariant is structurally enforced -- `validateProvenance` refuses provenance-less inputs, the transaction guarantees atomic both-or-neither writes. 12+ tests passing including the load-bearing D-20 Cypher invariant SQL query and the D-20 batch invariant.
  </done>
</task>

</tasks>

<verification>
**Overall phase acceptance for Plan 120-00:**
1. `bash tests/test-120-00-scaffold.sh` exits 0.
2. `node --test tests/test-breakthrough-event-types.cjs tests/test-breakthrough-edge-types.cjs lib/core/breakthrough/detectors.test.cjs lib/core/breakthrough/schema.test.cjs` reports 0 failures across all 4 suites (>= 35 tests total).
3. The 6 new EVENT_TYPES strings + the 1 new ALLOWED_EDGE_TYPES string are all reachable via navigation.cjs::logMemoryEvent / writeEdge round-trip tests.
4. **D-20 load-bearing test:** every Breakthrough node landed by writeBreakthrough has >= 1 DERIVED_FROM edge to an Artifact (SQL invariant verified in schema.test.cjs Test 7 + Test 10).
5. Source-grep audits pass: zero Brain client require, zero math recomputation, zero em-dashes across all new files.
6. Feynman runner registers the 4 new test files so they fire on every release flight.
</verification>

<success_criteria>
**Plan 120-00 ships when:**
- [ ] EVENT_TYPES Set extended by exactly 6 strings (the 6 breakthrough_* + breakthrough_throttled) per the additive tail-append precedent
- [ ] ALLOWED_EDGE_TYPES Set extended by exactly 1 string (DERIVED_FROM) per the additive tail-append precedent
- [ ] `lib/core/breakthrough/detectors.cjs` exports the 4 detectors + classifyFireTier + DETECTOR_THRESHOLDS + DETECTOR_TYPES
- [ ] `lib/core/breakthrough/schema.cjs` exports writeBreakthrough + validateProvenance with D-20 HARD FLOOR structural enforcement (provenance-less inputs refused; atomic transaction ensures both-or-neither)
- [ ] All 7 named threshold constants match CONTEXT.md D-01..D-06 verbatim (3, 0.25, 4, 3, 0.35, 0.40, 14)
- [ ] All detectors read Phase 117 math-layer JSON files; ZERO math recomputation (no child_process.exec on Python scripts)
- [ ] ZERO Brain MCP coupling across all new files (source-grep verified)
- [ ] ZERO em-dashes across all new files (em-dash HARD RULE per CLAUDE.md)
- [ ] D-20 Cypher-provable invariant SQL-enforced: every Breakthrough node has >= 1 DERIVED_FROM edge by construction (atomic transaction)
- [ ] 35+ tests passing across 4 new test files; scaffold harness passes all 9 gates
- [ ] Feynman runner registered with all 4 new test files
- [ ] Provenance comment blocks landed in both extended core files with cross-references to CONTEXT.md D-01..D-20
</success_criteria>

<output>
After completion, create `.planning/phases/120-breakthrough-scan-category-g/120-00-SUMMARY.md` documenting:
- The 6 new EVENT_TYPES strings + the 1 new ALLOWED_EDGE_TYPES string with cross-references to CONTEXT.md decisions
- The 4 detector functions' signatures + the math-layer JSON files they consume
- The 7 frozen DETECTOR_THRESHOLDS constants verbatim
- The D-20 structural enforcement pattern (atomic transaction + validateProvenance) with the load-bearing Cypher invariant SQL query verbatim
- Test count totals + scaffold harness pass evidence
- Any deviations from the plan (per the Phase 89.5-style deviation reporting precedent)
</output>
