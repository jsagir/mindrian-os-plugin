---
phase: 233-graph-derive-drain-residual-seed-037
plan: 02
type: execute
wave: 2
depends_on: ["233-01"]  # priority-sequencing per 233-CONTEXT.md (4c/4d land+verify first), not a file/functional dependency
files_modified:
  - lib/core/graph-derivation.cjs
  - scripts/gsd-graph-derive-drain.cjs
  - tests/test-233-derivation-default-gate.cjs
  - tests/test-233-drain-backfill-producer-parity.cjs
  - CHANGELOG.md
autonomous: true
requirements: ["4b", "4e"]

must_haves:
  truths:
    - "Calling runDerivation({roomDir}) with NO deriveFn injected and no explicit hosted-derive opt-in throws a named, synchronous error (deriveFn_required_no_hosted_default) instead of silently attempting a network call that would 400 against the dead Anthropic account"
    - "Calling runDerivation({roomDir}) with MINDRIAN_ALLOW_HOSTED_DERIVE=1 set AND a non-empty ANTHROPIC_API_KEY still resolves to the existing hosted producer (graph-candidate-producer.cjs produceCandidates), so a deliberately-configured deployment is not broken"
    - "The headless drain (scripts/gsd-graph-derive-drain.cjs) and the in-session backfill (lib/core/graph-backfill.cjs) both default to the IDENTICAL classifier.scoreBasedDeriveFn producer when no deriveFn is injected -- proven by a regression test, not by re-reading the source and trusting it"
    - "scripts/gsd-graph-derive-drain.cjs's header comment states the CURRENT division of labor (headless enqueues/preserves/retries via the local score-based producer; in-session /mos:graph --derive backfill is the universal net + heal-first sequence) and no longer implies the SessionStart drain silently clears entries on failure"
  artifacts:
    - path: "lib/core/graph-derivation.cjs"
      provides: "runDerivation's default deriveFn resolution is gated behind an explicit opt-in + funded-key check instead of a silent dead-API default"
      exports: ["runDerivation", "rollupSubRooms", "candidateToFinding", "CASCADE_SUBSET"]
  key_links:
    - from: "lib/core/graph-derivation.cjs runDerivation"
      to: "lib/core/graph-candidate-producer.cjs produceCandidates"
      via: "ONLY reached when MINDRIAN_ALLOW_HOSTED_DERIVE=1 AND ANTHROPIC_API_KEY is set"
      pattern: "MINDRIAN_ALLOW_HOSTED_DERIVE"
    - from: "scripts/gsd-graph-derive-drain.cjs drainScoreBased"
      to: "lib/core/graph-derive-classifier.cjs scoreBasedDeriveFn"
      via: "the default producer, unchanged by this plan, pinned by the new parity test"
      pattern: "classifier\\.scoreBasedDeriveFn"
    - from: "lib/core/graph-backfill.cjs runDeriveBackfill"
      to: "lib/core/graph-derive-classifier.cjs scoreBasedDeriveFn"
      via: "the default producer, unchanged by this plan, pinned by the new parity test"
      pattern: "classifier\\.scoreBasedDeriveFn"
---

<objective>
Close RCA items 4b (retire/gate the dead hosted-API default of `runDerivation`) and 4e
(reconcile the drain's doctrine comments) from `.planning/debug/graph-derive-silent-clear-dead-api-derivation.md`
Section 10 "Still OPEN". Per `233-CONTEXT.md`: Phase 224-02 already answered MOST of 4b's
"runChain-only vs funded key" question by making both the headless drain AND the in-session
backfill default to the LOCAL score-based producer (`graph-derive-classifier.cjs`
`scoreBasedDeriveFn`) -- what remains is `runDerivation`'s OWN internal fallback (reached only
when a caller forgets to inject a `deriveFn`), which still silently resolves to the dead
`api.anthropic.com` transport.

Purpose: a caller that forgets to inject `deriveFn` today gets a silent, live 400 against a
known-dead Anthropic account -- the exact "reachable by accident" footgun class this whole
phase exists to close. Live code audit confirms every real caller in this repo (the drain, the
backfill, every test) already injects `deriveFn` explicitly, so gating the default is safe:
nothing in the shipped tree depends on the silent fallback.

Output: `runDerivation`'s default is gated behind an explicit opt-in env var + a present API key
(Claude's Discretion per 233-CONTEXT.md: gate, not remove, so a deliberately-configured
deployment is not broken); a regression proving the drain and the backfill share one producer;
a reconciled doctrine comment in the drain header.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-graph-derive-drain-residual-seed-037-heal-already-damaged-ro/233-CONTEXT.md
@.planning/debug/graph-derive-silent-clear-dead-api-derivation.md

# Existing code this plan reuses/reconciles (Canon Part 7)
@lib/core/graph-derivation.cjs
@lib/core/graph-candidate-producer.cjs
@scripts/gsd-graph-derive-drain.cjs
@lib/core/graph-backfill.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate runDerivation's dead hosted-API default (4b)</name>
  <files>lib/core/graph-derivation.cjs, tests/test-233-derivation-default-gate.cjs</files>
  <read_first>
    lib/core/graph-derivation.cjs lines 165-200 (the exact `deriveFn` default resolution line:
    `const deriveFn = (typeof opts.deriveFn === 'function') ? opts.deriveFn :
    require('./graph-candidate-producer.cjs').produceCandidates;`); lib/core/graph-candidate-producer.cjs
    lines 52-95 (`_resolveDefaultLlm` -- its own existing `anthropic_api_key_missing` throw when
    `ANTHROPIC_API_KEY` is absent, the pattern to mirror for the NEW gate's error shape and
    naming); the RCA's Section 3 item 1 (the dead-API-account evidence: a live probe returned
    `400 "credit balance is too low"`) and 233-CONTEXT.md's 4b decision text (gate vs retire is
    Claude's Discretion; only that "the dead default stops being reachable by accident"). Confirm
    via `grep -rn "runDerivation(" lib scripts tests --include=*.cjs` (excluding
    `.claude/worktrees/`) that every current caller already injects `deriveFn` explicitly before
    changing the default -- this is the evidence that gating is safe, not a re-derivation of
    that fact from scratch.
  </read_first>
  <behavior>
    - runDerivation({roomDir: '<any>'}) with no deriveFn, no MINDRIAN_ALLOW_HOSTED_DERIVE env var
      set -> throws synchronously with message/name identifying `deriveFn_required_no_hosted_default`,
      BEFORE any network attempt
    - runDerivation({roomDir: '<any>'}) with no deriveFn, MINDRIAN_ALLOW_HOSTED_DERIVE='1' set but
      ANTHROPIC_API_KEY absent/empty -> still throws the same gated error (the funded-key half of
      the check failed)
    - runDerivation({roomDir: '<any>'}) with no deriveFn, MINDRIAN_ALLOW_HOSTED_DERIVE='1' AND a
      non-empty ANTHROPIC_API_KEY -> resolves deriveFn to
      require('./graph-candidate-producer.cjs').produceCandidates (identity-checked, not just
      "did not throw")
    - runDerivation({roomDir, deriveFn: <injected>}) behavior is COMPLETELY unchanged regardless
      of either env var (existing tests test-graph-derivation-verdict.cjs / test-graph-derivation-loop.cjs
      / test-derive-idempotence.cjs / test-224-proposed-only.cjs / test-224-migration.cjs must all
      still pass unmodified)
  </behavior>
  <action>
    In lib/core/graph-derivation.cjs, replace the unconditional default-deriveFn line with a
    small named helper `_resolveDefaultDeriveFn()` called only when `opts.deriveFn` is absent: it
    checks `process.env.MINDRIAN_ALLOW_HOSTED_DERIVE === '1'` AND a non-empty
    `process.env.ANTHROPIC_API_KEY` (the same funded-key proxy `_resolveDefaultLlm` already
    checks -- presence, not a live balance probe, since a balance probe would be a network call
    this synchronous composer cannot make); when both hold, return
    `require('./graph-candidate-producer.cjs').produceCandidates` (byte-identical to today's
    behavior, so an operator who deliberately opts in sees no change); otherwise throw a
    `new Error('deriveFn_required_no_hosted_default: runDerivation was called with no deriveFn
    injected. The dead hosted-API default is gated; inject the local score-based producer
    (graph-derive-classifier.cjs scoreBasedDeriveFn, see scripts/gsd-graph-derive-drain.cjs or
    lib/core/graph-backfill.cjs for the pattern), or set MINDRIAN_ALLOW_HOSTED_DERIVE=1 with a
    funded ANTHROPIC_API_KEY to opt into the hosted transport explicitly.')` -- named so a test
    can assert on `err.message.indexOf('deriveFn_required_no_hosted_default')`. This throw must
    happen synchronously at the top of `runDerivation`, before the room.db is opened and before
    any pair is processed (fail fast, fail loud, never a live 400).
  </action>
  <acceptance_criteria>
    Ground truth via direct function calls (not mocked): call `runDerivation({roomDir: os.tmpdir()})`
    with `MINDRIAN_ALLOW_HOSTED_DERIVE` deleted from `process.env` and assert it throws
    synchronously (wrap in try/catch, assert `err.message` contains
    `deriveFn_required_no_hosted_default`), and assert `require('./graph-candidate-producer.cjs').produceCandidates`
    was NEVER invoked (spy/stub it and assert call count 0 -- proving no network attempt was
    even reached). Then set `process.env.MINDRIAN_ALLOW_HOSTED_DERIVE='1'` and
    `process.env.ANTHROPIC_API_KEY='test-key-value'`, call runDerivation with no deriveFn and no
    artifactPairs, and assert the SAME producer stub above WAS invoked (proving the gate opens
    correctly on explicit opt-in), then restore both env vars to their original state. Run the
    FULL existing derivation test files (test-graph-derivation-verdict.cjs,
    test-graph-derivation-loop.cjs, test-derive-idempotence.cjs, test-224-proposed-only.cjs,
    test-224-migration.cjs) unmodified and confirm all still pass (proving zero regression to
    every injected-deriveFn caller).
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-233-derivation-default-gate.cjs && node tests/test-graph-derivation-verdict.cjs && node tests/test-graph-derivation-loop.cjs && node tests/test-derive-idempotence.cjs && node tests/test-224-proposed-only.cjs && node tests/test-224-migration.cjs</automated>
  </verify>
  <done>runDerivation's default deriveFn is gated behind MINDRIAN_ALLOW_HOSTED_DERIVE + a funded key check; every existing injected-deriveFn caller is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm drain/backfill producer parity + reconcile drain doctrine comment (4e)</name>
  <files>scripts/gsd-graph-derive-drain.cjs, tests/test-233-drain-backfill-producer-parity.cjs, CHANGELOG.md</files>
  <read_first>
    scripts/gsd-graph-derive-drain.cjs lines 1-45 (the file header doctrine comment, specifically
    the phrase "runs runDerivation once per queued room, and CLEARS the drained entry" in the
    intro paragraph -- this is stale: since Phase 224-02's `reconcileQueue`, only a SUCCEEDED
    entry clears; a FAILED entry is KEPT with a bumped `failures` counter up to MAX_DERIVE_ATTEMPTS.
    The "Failure discipline" paragraph immediately below it was already corrected by 224-02 and
    must NOT be touched again -- only the intro paragraph's stale "and CLEARS the drained entry"
    framing needs reconciling); scripts/gsd-graph-derive-drain.cjs line 356
    (`const deriveFn = injectedDerive ? options.deriveFn : classifier.scoreBasedDeriveFn;` -- the
    headless default producer); lib/core/graph-backfill.cjs line 510
    (`const deriveFn = injected ? opts.deriveFn : classifier.scoreBasedDeriveFn;` -- the in-session
    default producer). Both already point at the SAME classifier export today; this task proves
    that fact with a regression test rather than leaving it as an assertion in a comment (233-CONTEXT.md
    4b: "Confirm the in-session /mos:graph --derive backfill path wires the SAME local
    score-based producer... do not let the two paths diverge").
  </read_first>
  <action>
    In scripts/gsd-graph-derive-drain.cjs's header (the intro paragraph around lines 9-13, NOT
    the already-corrected "Failure discipline" block below it), replace "runs runDerivation once
    per queued room, and CLEARS the drained entry" with language stating the real, current
    behavior: the drain runs runDerivation once per queued entry via the LOCAL score-based
    producer, and reconciles the queue afterward -- a SUCCEEDED entry clears, a FAILED entry is
    KEPT and retried up to MAX_DERIVE_ATTEMPTS, never silently dropped. State explicitly, in one
    sentence, the division of labor RCA item 4e demands: headless (this file) = enqueue-drain +
    preserve-on-failure + score locally; in-session `/mos:graph --derive` backfill
    (lib/core/graph-backfill.cjs) = the heal-first universal net for anything the headless path
    missed (sentinel-less folders, rooms that never had a Stop-hook fire). Do not touch the
    "Failure discipline" paragraph itself; it is already correct. Write
    tests/test-233-drain-backfill-producer-parity.cjs: require both
    `scripts/gsd-graph-derive-drain.cjs` and `lib/core/graph-backfill.cjs`, require
    `lib/core/graph-derive-classifier.cjs` directly, and assert (via a controlled fixture room
    with `MOS_NO_DETACHED_DERIVE=1` so the drain runs in-process) that neither module's default
    path resolves to any function OTHER than `classifier.scoreBasedDeriveFn` -- the simplest
    reliable proof is a source-level assertion (read both files' text, comment-stripped per the
    run-all-158.sh grep-gate-hygiene idiom, and assert each contains the literal
    `classifier.scoreBasedDeriveFn` token as its default-path assignment, and assert NEITHER
    contains a live default reference to `graph-candidate-producer` outside of a
    gated/documented context) combined with one live behavioral check: drive a tiny fixture pair
    through each module's default (non-injected) path with a stubbed/forced-available encoder
    probe and confirm both produce the identical candidate shape for the identical input pair
    (proving they are not just textually similar but functionally identical). Add one CHANGELOG.md
    `## [Unreleased]` `### Added`/`### Fixed` bullet (repo voice, no em-dashes) noting the gated
    runDerivation default and the reconciled drain doctrine comment, citing RCA items 4b/4e.
  </action>
  <acceptance_criteria>
    `grep -c 'classifier.scoreBasedDeriveFn' scripts/gsd-graph-derive-drain.cjs lib/core/graph-backfill.cjs`
    (comment-stripped) shows the token present in BOTH files. The new parity test asserts this
    via source AND via one live default-path invocation per file, not source-reading alone.
    `grep -n "and CLEARS the drained entry" scripts/gsd-graph-derive-drain.cjs` returns NO match
    after this task (the stale phrase is gone); the existing, already-correct "Failure
    discipline" paragraph is byte-unchanged (diff the file against the pre-task version and
    confirm only the intro paragraph changed).
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-233-drain-backfill-producer-parity.cjs</automated>
  </verify>
  <done>The drain's header doctrine matches current reconcileQueue behavior; a regression proves the drain and backfill share one default producer; CHANGELOG documents both fixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var opt-in (MINDRIAN_ALLOW_HOSTED_DERIVE + ANTHROPIC_API_KEY) -> hosted LLM transport | Local operator-set only; not reachable from any room artifact or user input |
| runDerivation caller contract | Any future caller that forgets deriveFn now fails loud and synchronous instead of silent-then-network-400 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-06 | Repudiation (silent footgun, this is 4b itself) | lib/core/graph-derivation.cjs runDerivation default | mitigate | Gate throws synchronously before any network attempt; pinned by a test asserting the producer stub is never invoked on the ungated path |
| T-233-07 | Tampering (Supply Chain) | this plan's changes | accept (n/a) | Zero new npm/pip packages; no package-legitimacy gate applies |
| T-233-08 | Information Disclosure | the new gate's thrown error message | mitigate | Message is a fixed, hardcoded developer-facing string; never includes room content, API key value, or any runtime data |

</threat_model>

<verification>
All Task 1 and Task 2 automated commands pass. `grep -rn "runDerivation(" lib scripts tests
--include=*.cjs | grep -v worktrees` shows every live caller still passing tests (zero
regressions from the gate). The drain header no longer claims a blanket clear-on-drain.
</verification>

<success_criteria>
- runDerivation's dead hosted-API default is gated behind an explicit, documented opt-in.
- A regression test proves the headless drain and the in-session backfill share one producer.
- The drain's doctrine comment matches its actual reconcileQueue behavior.
- Zero regressions in the existing derivation test suite.
</success_criteria>

<output>
Create `.planning/phases/233-graph-derive-drain-residual-seed-037-heal-already-damaged-ro/233-02-SUMMARY.md` when done
</output>
