---
phase: 233-graph-derive-drain-residual-seed-037
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/doctor/graph-derive-health-module.cjs
  - lib/core/doctor/graph-derive-heal-retrofit-module.cjs
  - data/doctor-modules.json
  - scripts/doctor.cjs
  - scripts/preflight-doctor.cjs
  - tests/test-233-graph-derive-health.cjs
  - tests/test-233-graph-derive-heal-retrofit.cjs
  - CHANGELOG.md
autonomous: true
requirements: ["4c", "4d"]

must_haves:
  truths:
    - "Running `node scripts/doctor.cjs --graph-derive-health --json` against a real fixture room whose room.db has BELONGS_TO edges and ZERO cascade edges reports that room's status as 'fail', never 'ok' or silently absent"
    - "Running `node scripts/doctor.cjs --heal-room` against that same fixture writes a new entry into `<room>/.mindrian/graph-derive-queue.json` (a real file read proves it, not a mocked return value); running it a second time in a row does not add a duplicate entry (idempotent)"
    - "A room that predates this phase (BELONGS_TO present, zero cascade edges, no queue entry, no failure log -- the ~16-damaged-room shape) gets a derive queue entry written automatically the FIRST time `doctor.cjs` runs after this code ships, with no explicit --heal-room needed (the cadence:'once' watermark-gated retrofit fires unconditionally within its version window)"
    - "Re-running the retrofit a second time (same version, watermark already advanced) enqueues nothing new for that room (the once-pass window is empty; sweep enqueue is also itself dedup-by-roomDir)"
    - "On the SessionStart Coordinator path (Desktop/Cowork), a room needing heal surfaces as a short plain-language sentence inside the existing install-drift fragment's payload, never a raw JSON table"
  artifacts:
    - path: "lib/core/doctor/graph-derive-health-module.cjs"
      provides: "detectRoomHealth(roomDir) the ONE shared detection signal, plus check(ctx) (class graph-derive-health report) and fix(ctx) (re-enqueue WARN/FAIL rooms)"
      exports: ["detectRoomHealth", "check", "fix"]
    - path: "lib/core/doctor/graph-derive-heal-retrofit-module.cjs"
      provides: "cadence:'once' auto-heal-on-update sweep across every registered room, reusing detectRoomHealth"
      exports: ["check"]
    - path: "data/doctor-modules.json"
      provides: "two new registry entries: id graph-derive-health (cadence always) and id graph-derive-heal-retrofit (cadence once)"
  key_links:
    - from: "lib/core/doctor/graph-derive-heal-retrofit-module.cjs"
      to: "lib/core/doctor/graph-derive-health-module.cjs"
      via: "require('./graph-derive-health-module.cjs').detectRoomHealth"
      pattern: "require\\(.\\/graph-derive-health-module\\.cjs.\\)"
    - from: "lib/core/doctor/graph-derive-health-module.cjs (fix)"
      to: "scripts/gsd-graph-derive-sweep.cjs"
      via: "require('../../../scripts/gsd-graph-derive-sweep.cjs').enqueueDerive(roomPath)"
      pattern: "enqueueDerive\\("
    - from: "scripts/preflight-doctor.cjs (contribute)"
      to: "doctor.cjs --json report.checks['graph-derive-health']"
      via: "runDoctor() JSON parse, existing install-drift SessionStart Coordinator slot"
      pattern: "report\\.checks\\['graph-derive-health'\\]"
---

<objective>
Close RCA items 4c (heal already-damaged rooms) and 4d (doctor graph-derive-health check +
heal class) from `.planning/debug/graph-derive-silent-clear-dead-api-derivation.md` Section 10
"Still OPEN", per `233-CONTEXT.md` decisions. These two are planned together in one plan
because 4d's detection signal IS 4c's detection signal: one function, two consumers (a
per-invocation CLI report, and a one-time automatic retrofit), never two copies.

Purpose: ~16 live rooms had their derive queues silently cleared before Phase 224-02's fix and
still carry zero semantic cascade edges today (independently corroborated by SEED-074's
2026-07-25 spot-check finding 0 tables in 2 sampled rooms). This plan repairs those rooms'
retry signal automatically, and makes sure the underlying defect class (a room silently never
deriving) can never again go unnoticed, closing the exact gap this whole phase exists to
prevent: a "verification: pending" fix that looks done but isn't (per the check-card-fire.cjs
lesson cited in this phase's planning context).

Output: a new doctor check class `graph-derive-health` (CHECK + `--heal-room` HEAL, Tri-Polar
aware) and a new one-time `graph-derive-heal-retrofit` module that auto-repairs every
already-damaged room the first time doctor runs post-update. Canon Part 8: every new code path
in this plan is LOCAL room.db reads (through the existing read-only door) + LOCAL JSON queue
writes; zero Brain egress, zero network.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/233-graph-derive-drain-residual-seed-037-heal-already-damaged-ro/233-CONTEXT.md
@.planning/debug/graph-derive-silent-clear-dead-api-derivation.md

# Existing code this plan reuses (Canon Part 7 -- read before writing anything new)
@lib/core/doctor/room-graph-density-module.cjs
@lib/core/doctor/cascade-rooms-active-module.cjs
@lib/core/doctor/cascade-rooms-module.cjs
@lib/core/doctor/shared.cjs
@scripts/gsd-graph-derive-drain.cjs
@scripts/gsd-graph-derive-sweep.cjs
@lib/core/graph-derivation.cjs
@data/doctor-modules.json
@scripts/preflight-doctor.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared detection signal + class graph-derive-health CHECK/HEAL (4d)</name>
  <files>lib/core/doctor/graph-derive-health-module.cjs, data/doctor-modules.json, scripts/doctor.cjs</files>
  <read_first>
    lib/core/doctor/room-graph-density-module.cjs (the read-only room.db door pattern:
    openRoomDbReadOnlyForCaller / closeRoomDbForCaller from ../navigation/spine-events.cjs, the
    per-room try/catch self-DoS guard, the D-05 "no db yet is Tier 0 normal, not an error"
    convention); lib/core/doctor/cascade-rooms-active-module.cjs (the registry active-room
    resolution: reg.registry.active name + roomsHome join, EXACT pattern to reuse for "active
    room" default scope); lib/core/doctor/cascade-rooms-module.cjs (resolveRoomPath helper
    shape, check(ctx)/fix(ctx) contract, per-path non-empty `detail` rule D-03); lib/core/doctor/shared.cjs
    (readRegistry() exact return shape: {roomsHome, registry:{rooms, active}}); scripts/gsd-graph-derive-drain.cjs
    lines 106-153 (MAX_DERIVE_ATTEMPTS=5, failureLogPath(resolved), the scalarError 200-char cap
    -- reuse both exports, do not reimplement the failure-log reader); scripts/gsd-graph-derive-sweep.cjs
    (readQueue(roomDir) shape: {entries:[{roomDir, filePath?, enqueued_at}]}, enqueueDerive(roomDir)
    dedup-by-roomDir contract); lib/core/graph-derivation.cjs line ~69 CASCADE_SUBSET export (the
    frozen 7-item cascade edge_type Set -- reuse this exact Set for cascade-edge counting, never
    hardcode a shorter 5-item list that can drift from the composer's own set); data/doctor-modules.json
    (its own $schema_note field documents the full per-entry contract: id, introduced_version,
    cadence, flag, fix_supported, runner, description -- follow it exactly); scripts/doctor.cjs
    lines 140-330 (flags object defaults, parseArgs branches, the classFlagsActive expression
    near line 3001, the --all activation block near line 290, usageText() class-flags section
    near line 330) and lines 142/lib/core/doctor/deprecated-usage-module.cjs line 40 (the
    EXISTING but never-implemented rename-table entry 'heal' -> 'doctor --heal-room' -- this
    task makes that documented mapping real for the first time, do not invent a different flag
    name).
  </read_first>
  <behavior>
    - detectRoomHealth(roomDir) given a fixture room.db with a BELONGS_TO edge and zero rows
      whose type is in CASCADE_SUBSET, and an empty derive queue with no failure log -> returns
      { hasDb:true, hasBelongsTo:true, cascadeEdgeCount:0, needsHeal:true, status:'fail', reasons: [...] }
    - detectRoomHealth(roomDir) given the same room but WITH at least one cascade edge ->
      needsHeal:false, status:'ok'
    - detectRoomHealth(roomDir) given a room with a queue entry whose enqueued_at is more than
      QUEUE_STUCK_DAYS (3) days in the past -> status:'warn', reasons includes a queue-stuck note
    - detectRoomHealth(roomDir) given a room with no `.mindrian/room.db` file at all -> status:'skip'
      (Tier 0 normal, never an error)
    - check(ctx) with no ctx.flags.cascadeRooms -> scopes to ONLY the registry's active room
    - check(ctx) with ctx.flags.cascadeRooms:true -> scopes to every room in the registry
    - fix(ctx) given a check_result whose rooms[] contains a needsHeal:true or queue-stuck entry
      -> calls sweep.enqueueDerive on that room's resolved path and reports it in recoveries[]
  </behavior>
  <action>
    Create lib/core/doctor/graph-derive-health-module.cjs. Export a pure `detectRoomHealth(roomDir)`
    function: open room.db read-only via `openRoomDbReadOnlyForCaller` (from
    ../navigation/spine-events.cjs, the exact door room-graph-density-module.cjs uses); when it
    returns null, return `{hasDb:false, status:'skip', reasons:['no room.db yet (Tier 0)']}`.
    Otherwise: `SELECT count(*) FROM edges WHERE type='BELONGS_TO'` for hasBelongsTo; `SELECT count(*)
    FROM edges WHERE type IN (...CASCADE_SUBSET)` (build the placeholder list from
    `require('../graph-derivation.cjs').CASCADE_SUBSET`, never a hand-typed literal list) for
    cascadeEdgeCount; wrap both counts in the same try/catch-returns-0 idiom room-graph-density-module.cjs's
    countTable() uses (a room.db with no edges table yet is normal, not an error). Read the derive
    queue via `require('../../../scripts/gsd-graph-derive-sweep.cjs').readQueue(roomDir)` for
    queueEntries and compute queueStuckCount (entries whose `enqueued_at` parses more than
    QUEUE_STUCK_DAYS=3 days before Date.now(), a module-level const documented as Claude's
    Discretion per 233-CONTEXT.md, sibling to the existing MAX_DERIVE_ATTEMPTS=5 constant). Check
    failure-log presence via `require('../../../scripts/gsd-graph-derive-drain.cjs').failureLogPath(roomDir)`
    + fs.existsSync + a bounded JSON parse for failureLogCount (cap the read the same way
    appendFailureLog bounds writes -- never trust an attacker-sized file, wrap in try/catch,
    default to 0 on any parse error). Compute `needsHeal = hasDb && hasBelongsTo && cascadeEdgeCount === 0`.
    Derive `status` in priority order: !hasDb -> 'skip'; needsHeal -> 'fail'; queueStuckCount>0 ->
    'warn'; failureLogCount>0 -> 'warn'; else 'ok'. Populate a human-readable `reasons: string[]`
    array (each reason a short scalar sentence, never artifact body text -- truncate any embedded
    error string to 200 chars via the same cap pattern as scalarError in gsd-graph-derive-drain.cjs).
    Export `check(ctx)`: resolve scope via readRegistry() (from ./shared.cjs) -- default to
    `[reg.registry.active]` resolved through roomsHome exactly as cascade-rooms-active-module.cjs
    resolves activePath; when `ctx.flags && ctx.flags.cascadeRooms` is true, scope to every name in
    `reg.registry.rooms`. Run detectRoomHealth per room inside a per-room try/catch (T-217-01
    self-DoS: one bad room never aborts the sweep), assemble `{status, detail, scope, rooms:[{room,
    roomPath, ...detectRoomHealth fields}]}` where the overall `status` is the worst of the
    per-room statuses (fail > warn > ok > skip), and `detail` is a one-line scalar summary (e.g.
    "1 of 3 room(s) need a derive re-enqueue"). Export `fix(ctx)`: read `ctx.check_result.rooms`,
    for each entry where `needsHeal || queueStuckCount > 0`, call
    `require('../../../scripts/gsd-graph-derive-sweep.cjs').enqueueDerive(entry.roomPath)` (honor
    `ctx.dryRun`: count as projected, write nothing), collect recoveries, return
    `{status, healed, recoveries, detail}` per the umbilical fix(ctx) contract shape. Register a
    new entry in data/doctor-modules.json's `modules` array: `id:"graph-derive-health"`,
    `introduced_version` set to the CURRENT in-progress version read from
    .claude-plugin/plugin.json (byte-match it, do not guess), `cadence:"always"`,
    `flag:"graphDeriveHealth"`, `fix_supported:true`, `runner:"lib/core/doctor/graph-derive-health-module.cjs"`,
    a one-line description mirroring the style of the existing entries. In scripts/doctor.cjs: add
    `graphDeriveHealth: false` to the flags default object (near the other class flags, e.g. beside
    installState); add `else if (arg === '--graph-derive-health') flags.graphDeriveHealth = true;`
    and, immediately after it, `else if (arg === '--heal-room') { flags.graphDeriveHealth = true;
    flags.fix = true; }` to parseArgs (this is the literal `--heal-room` flag the RCA's item 4 and
    the CONTEXT decisions name, and it closes the existing but never-wired
    deprecated-usage-module.cjs rename-table entry `'heal': 'doctor --heal-room'`); add
    `flags.graphDeriveHealth = true;` to the `--all` activation block; add `|| flags.graphDeriveHealth`
    to the `classFlagsActive` expression near line 3001; add one usageText() line documenting both
    flags next to the existing `--cascade-rooms` line, stating that `--heal-room` re-enqueues
    WARN/FAIL rooms and is sugar for `--graph-derive-health --fix`.
  </action>
  <acceptance_criteria>
    Ground truth, not code-shape: build a real temp room dir with a real `.mindrian/room.db`
    (via the repo's existing `openRoomDb`, writing one BELONGS_TO edge and zero cascade edges
    through `navigation.writeEdge`, not a mocked object). Call `detectRoomHealth(roomDir)`
    directly and assert the RETURNED object has `status === 'fail'` and `needsHeal === true` by
    reading its real fields. Spawn `node scripts/doctor.cjs --graph-derive-health --json` against
    a registry fixture pointing at that room (MINDRIAN_ROOMS_HOME override) and assert the
    parsed stdout JSON's `report.checks['graph-derive-health'].rooms[0].status === 'fail'`.
    Spawn `node scripts/doctor.cjs --heal-room` against the same fixture, then `fs.readFileSync`
    the room's actual `.mindrian/graph-derive-queue.json` from disk and assert it now contains an
    entry for that roomDir (a real file read, never an assertion on an in-memory mock). Run the
    same `--heal-room` spawn a second time and assert the queue file's entry count did not grow
    (idempotent, byte-compared before/after).
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-233-graph-derive-health.cjs</automated>
  </verify>
  <done>lib/core/doctor/graph-derive-health-module.cjs exists exporting detectRoomHealth/check/fix; data/doctor-modules.json carries the new graph-derive-health entry; doctor.cjs supports --graph-derive-health and --heal-room end-to-end against a real fixture room.db.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Heal-on-update auto-retrofit (4c) + Tri-Polar Desktop/Cowork nudge</name>
  <files>lib/core/doctor/graph-derive-heal-retrofit-module.cjs, data/doctor-modules.json, scripts/preflight-doctor.cjs</files>
  <read_first>
    lib/core/doctor/umbilical-module.cjs (the ONLY existing precedent for a cadence:'once'
    module whose check() itself performs the write/heal, not gated behind --fix -- confirm this
    pattern before writing Task 2's module so its behavior matches the engine's actual dispatch,
    not an assumed one); scripts/doctor.cjs lines 2088-2156 (`runAccumulativeEngine`'s ONCE pass:
    a selected module's check() runs UNCONDITIONALLY within its (applied_through, running]
    version window regardless of the --fix flag, and the watermark then advances -- this is why
    Task 2's module must do its real work inside check(), never wait for an external --fix);
    Task 1's lib/core/doctor/graph-derive-health-module.cjs (the detectRoomHealth export this
    module imports -- do not re-derive the detection logic, require it); scripts/sessionstart-coordinator.cjs
    lines 57-71 (the CONTRIBUTORS map) and lib/sessionstart/precedence-ladder.cjs (the ladder is
    FROZEN, "Adding/removing/reordering requires a canon amendment" -- this is why Task 2 extends
    the EXISTING 'install-drift' slot rather than registering a new ladder entry); scripts/preflight-doctor.cjs
    `contribute()` function in full (it already composes an install-drift warning + a
    release-drift sub-finding into one ContributorFragment via `fi.makeFragment` -- follow that
    exact composition pattern, appending a third piece rather than replacing the function's
    existing behavior).
  </read_first>
  <behavior>
    - graph-derive-heal-retrofit-module.cjs's check(ctx) walks every room in the registry, calls
      the imported detectRoomHealth per room, and for every room where needsHeal is true calls
      sweep.enqueueDerive on it (unless ctx.dryRun) -- returns {status:'ok', healed:N, rooms_scanned:N}
    - re-invoking check(ctx) a second time against the same fixture (simulating the watermark
      already having advanced, i.e. calling the exported function directly twice) enqueues no
      duplicate entries (enqueueDerive's own dedup-by-roomDir contract)
    - preflight-doctor.cjs's contribute(), given a doctor --json report whose
      report.checks['graph-derive-health'].status is 'warn' or 'fail', returns a fragment whose
      full_payload contains a plain-language sentence (no raw JSON, no table characters) about
      the graph needing a re-derive
  </behavior>
  <action>
    Create lib/core/doctor/graph-derive-heal-retrofit-module.cjs. Require
    `{ detectRoomHealth } = require('./graph-derive-health-module.cjs')` and
    `sweep = require('../../../scripts/gsd-graph-derive-sweep.cjs')` and `{ readRegistry } =
    require('./shared.cjs')`. Export ONLY `check(ctx)` (no `fix` export -- fix_supported will be
    false, and the contract-parity gate fails a fix_supported:false module that exports one):
    read the registry; when absent return `{status:'skip', detail:'no registry to retrofit',
    healed:0, rooms_scanned:0}`; otherwise for every registered room resolve its path (mirror
    resolveRoomPath from cascade-rooms-module.cjs, do not re-derive), call `detectRoomHealth(roomPath)`
    inside a per-room try/catch, and when `needsHeal` is true call `sweep.enqueueDerive(roomPath)`
    unless `ctx.dryRun` (in dry-run, count as projected without writing). Return
    `{status:'ok', healed, rooms_scanned, detail}` where `detail` states how many of how many
    rooms were retrofitted. Register a second data/doctor-modules.json entry:
    `id:"graph-derive-heal-retrofit"`, the SAME `introduced_version` as Task 1's entry (both ship
    together), `cadence:"once"`, `flag:null`, `fix_supported:false`,
    `runner:"lib/core/doctor/graph-derive-heal-retrofit-module.cjs"`, a description stating this
    is the one-time automatic retrofit for rooms whose derive queues were silently cleared before
    Phase 224-02's fix (RCA 4c). In scripts/preflight-doctor.cjs's `contribute()` function: after
    the existing `drift`/`releaseDriftLine` computation, additionally read
    `report.checks && report.checks['graph-derive-health']` from the SAME already-parsed `report`
    (no second doctor spawn); when its `status` is `'warn'` or `'fail'`, build one additional
    plain-language line (e.g. "Your graph needs a re-derive -- run /mos:doctor --heal-room to fix
    it.") and push it onto the SAME `pieces` array the function already assembles before joining
    into `fullPayload`, so it rides the existing 'install-drift' ladder slot rather than
    requesting a new frozen-ladder entry. Update the CLI `main()` function's already-empty
    behavior only if needed to keep it byte-compatible (main() itself does not need the new
    line; only `contribute()`, the SessionStart Coordinator path, does -- CLI already gets the
    full report via `--graph-derive-health --json` from Task 1).
  </action>
  <acceptance_criteria>
    Ground truth: construct a registry fixture with 2 rooms, one healthy (has a cascade edge) and
    one damaged (BELONGS_TO only, zero cascade edges, matching the ~16-room RCA shape exactly).
    Call `graph-derive-heal-retrofit-module.cjs`'s `check({home, running, dryRun:false})` directly,
    then `fs.readFileSync` the damaged room's actual `.mindrian/graph-derive-queue.json` and
    assert a new entry exists; assert the healthy room's queue file was NOT modified (read its
    mtime/content before and after). Call `check()` a second time and assert the damaged room's
    queue entry count did not grow. Separately, call `preflight-doctor.cjs`'s `contribute()` with
    a stubbed `runDoctor()` return whose `checks['graph-derive-health'].status` is `'fail'`, and
    assert the returned fragment's `full_payload` string contains a re-derive nudge sentence and
    contains NO `{` or `|` table/JSON characters.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-233-graph-derive-heal-retrofit.cjs</automated>
  </verify>
  <done>graph-derive-heal-retrofit-module.cjs exists, registered as cadence:once in data/doctor-modules.json, auto-heals a fixture registry's damaged rooms via a direct check() call, and preflight-doctor.cjs's contribute() surfaces a plain-language nudge when the health check is warn/fail.</done>
</task>

<task type="auto">
  <name>Task 3: Regression suite, existing contract-parity gates, CHANGELOG</name>
  <files>tests/test-233-graph-derive-health.cjs, tests/test-233-graph-derive-heal-retrofit.cjs, CHANGELOG.md</files>
  <read_first>
    tests/test-doctor-module-contract-parity.cjs and tests/test-doctor-doc-parity.cjs (these
    EXISTING generic suites already iterate every entry in data/doctor-modules.json and assert
    the check/fix export parity + doc coverage -- run them, do not write a new duplicate
    contract test); tests/test-232.1-room-graph-density.cjs (a recent, structurally similar
    doctor-module test file to mirror for fixture-room construction helpers); CHANGELOG.md's
    `## [Unreleased]` section header (top of file) for the exact bullet format this repo uses.
  </read_first>
  <action>
    Write tests/test-233-graph-derive-health.cjs and tests/test-233-graph-derive-heal-retrofit.cjs
    implementing every scenario named in Task 1's and Task 2's `<acceptance_criteria>` verbatim
    (this task is where those criteria become runnable code, not a restatement). Both files build
    real temp room dirs with real room.db files (via `openRoomDb` + `navigation.writeEdge`, never
    a mocked db object) and real registry.json fixtures pointed at via `MINDRIAN_ROOMS_HOME`.
    After both test files pass, run `node tests/test-doctor-module-contract-parity.cjs` and
    `node tests/test-doctor-doc-parity.cjs` unmodified against the two new data/doctor-modules.json
    entries and confirm both stay green (the new entries must satisfy the SAME generic contract
    every other class already does -- non-empty detail, fix_supported boolean present, no
    fix_supported:false module exporting a fix). Add one bullet under CHANGELOG.md's `## [Unreleased]`
    `### Added` section describing the new graph-derive-health doctor class + heal-room flag +
    the automatic heal-on-update retrofit for already-damaged rooms, in this repo's Feynman/no-em-dash
    voice, citing RCA items 4c/4d.
  </action>
  <acceptance_criteria>
    `node tests/test-233-graph-derive-health.cjs` exits 0. `node tests/test-233-graph-derive-heal-retrofit.cjs`
    exits 0. `node tests/test-doctor-module-contract-parity.cjs` and `node tests/test-doctor-doc-parity.cjs`
    both exit 0 with the two new registry entries present (grep data/doctor-modules.json for both
    new `id` values to confirm they are actually registered before trusting the parity result).
    CHANGELOG.md's `## [Unreleased]` section contains a new non-empty bullet mentioning
    graph-derive-health.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-233-graph-derive-health.cjs && node tests/test-233-graph-derive-heal-retrofit.cjs && node tests/test-doctor-module-contract-parity.cjs && node tests/test-doctor-doc-parity.cjs</automated>
  </verify>
  <done>All four commands above exit 0; CHANGELOG.md documents the new class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry.json -> doctor module | A room's registered `path`/`abs_path` is filesystem-local, not remote-attacker-controlled, but a corrupt entry must never crash the sweep |
| doctor JSON output -> SessionStart Coordinator fragment | Detail/reason strings must stay scalar-only; never leak artifact body text into a conversational nudge |
| new CLI flags (--graph-derive-health, --heal-room) | Local-user-invoked only; no new external input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-01 | Tampering | graph-derive-health-module.cjs check()/graph-derive-heal-retrofit-module.cjs | mitigate | Per-room try/catch (T-217-01 self-DoS pattern already used by cascade-rooms-module.cjs); a malformed registry entry soft-fails that one room, never aborts the sweep |
| T-233-02 | Information Disclosure | detectRoomHealth `reasons[]` / failure-log read | mitigate | Every string is scalar and capped (reuse gsd-graph-derive-drain.cjs's scalarError 200-char truncation idiom); never includes artifact body text |
| T-233-03 | Tampering (Supply Chain) | this plan's new files | accept (n/a) | Zero new npm/pip packages introduced; both new modules require only existing repo files and node built-ins -- package legitimacy gate does not apply |
| T-233-04 | Repudiation (false-success) | graph-derive-health-module.cjs check() status derivation | mitigate | The whole phase exists because a prior fix's own status nearly shipped unverified; Task 3's acceptance criteria assert the REAL room.db/queue-file state, never a mocked return value, so a status:'ok' claim can never mask a real zero-cascade-edge room |
| T-233-05 | Denial of Service | reading an oversized/corrupt graph-derive-failures.json | mitigate | Wrapped in try/catch with a default-to-0 fallback on any parse error, mirroring appendFailureLog's own bounded-log discipline |

</threat_model>

<verification>
All three tasks' automated commands pass. `node scripts/doctor.cjs --graph-derive-health --json`
and `node scripts/doctor.cjs --heal-room` both run cleanly against a real fixture without
throwing. `grep -c 'graph-derive-health\|graph-derive-heal-retrofit' data/doctor-modules.json`
returns at least 2 (both entries present).
</verification>

<success_criteria>
- lib/core/doctor/graph-derive-health-module.cjs and lib/core/doctor/graph-derive-heal-retrofit-module.cjs
  exist, exporting the contracts documented above.
- data/doctor-modules.json registers both new modules with correct cadence/flag/fix_supported.
- scripts/doctor.cjs supports --graph-derive-health and --heal-room end to end.
- scripts/preflight-doctor.cjs surfaces a Tri-Polar plain-language nudge, not a raw table.
- Existing contract-parity gates (test-doctor-module-contract-parity.cjs, test-doctor-doc-parity.cjs)
  stay green with the two new entries present.
- Zero Brain egress: every new code path is LOCAL room.db reads (via the existing read-only door)
  and LOCAL JSON queue writes only.
</success_criteria>

<output>
Create `.planning/phases/233-graph-derive-drain-residual-seed-037-heal-already-damaged-ro/233-01-SUMMARY.md` when done
</output>
