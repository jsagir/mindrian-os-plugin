---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/356-no-network-preload.cjs
  - tests/run-all-356.sh
  - tests/test-353-tripwires.cjs
  - tests/test-356-tripwires.cjs
  - tests/test-356-larry-contract.cjs
autonomous: true
requirements: [R356-05, R356-07]
tags: [jev, tripwire, test-scaffolding, larry-contract, part8]

must_haves:
  truths:
    - "No non-comment line under lib/ or hooks/ references api.typesafe.ai, TYPESAFE_API_KEY, jev-devtime-client, build-command-irreversibility-ledger or irreversibility-answer-key, proven by a scan that also catches a planted negative control (R356-05 d)"
    - "tests/test-353-tripwires.cjs leg 2 bans dev-time ledger scripts from hooks/ through one named, append-only list constant HOOKS_BANNED_LEDGER_SCRIPTS, never an edited regex (D-19, agreed with jsagi-e0/357 and jsagi-25/354-17)"
    - "The Larry post-gate contract (runChain halts at the first material step) is asserted from agents/larry-extended.md without editing that file, and nothing there or in the larry-personality skill redefines material as irreversible (D-12)"
    - "Every spawned 356 child process can run with TYPESAFE_API_KEY removed and fetch replaced by a thrower through one shared preload file (the key IS set in the developer shell)"
    - "bash tests/run-all-356.sh runs every planned 356 test under run_if guards, the chain suites with the default ledger and with MINDRIAN_IRREVERSIBILITY_LEDGER pointed at a missing file, and an em-dash guard over every 356 surface"
  artifacts:
    - path: "tests/fixtures/356-no-network-preload.cjs"
      provides: "NODE_OPTIONS --require preload: deletes TYPESAFE_API_KEY, replaces globalThis.fetch with a thrower that prints NETWORK_ATTEMPT_356"
      contains: "NETWORK_ATTEMPT_356"
    - path: "tests/run-all-356.sh"
      provides: "Phase 356 aggregator, written once, run/run_if legs, ledger-absent legs, em-dash guard"
      contains: "MINDRIAN_IRREVERSIBILITY_LEDGER=/nonexistent"
    - path: "tests/test-353-tripwires.cjs"
      provides: "Leg 2 driven by the append-only HOOKS_BANNED_LEDGER_SCRIPTS list"
      contains: "HOOKS_BANNED_LEDGER_SCRIPTS"
    - path: "tests/test-356-tripwires.cjs"
      provides: "lib/ + hooks/ vendor-token scan with a negative control; runtime-module raw-fetch sweep"
      min_lines: 80
    - path: "tests/test-356-larry-contract.cjs"
      provides: "D-12 doc scan of larry-extended Post-Gate Handoff plus the handoff-seam suite run with and without a ledger"
      min_lines: 80
  key_links:
    - from: "tests/run-all-356.sh"
      to: "tests/test-356-*.cjs"
      via: "run_if guard per planned test file"
      pattern: "run_if \"356:"
    - from: "tests/test-356-larry-contract.cjs"
      to: "agents/larry-extended.md"
      via: "section extraction from '## Post-Gate Handoff' to the next '## ' heading"
      pattern: "Post-Gate Handoff"
---

<objective>
Lay the Phase 356 test scaffolding that every later plan leans on, and land the two cheap guards that must exist before any vendor-adjacent code does.

Three things: (1) the shared no-network preload and the `tests/run-all-356.sh` aggregator (written once, like `tests/run-all-354.sh`); (2) the tripwires: `tests/test-356-tripwires.cjs` (no Jev token under `lib/` or `hooks/`, with a negative control) and the D-19 conversion of `tests/test-353-tripwires.cjs` leg 2 from a regex into the named list constant `HOOKS_BANNED_LEDGER_SCRIPTS` that 357 and 354-17 append to; (3) the D-12 Larry-contract test, which reads `agents/larry-extended.md` and never edits it.

Purpose: SPEC R5(d) (no vendor reference in `lib/` or `hooks/`), R7 support (tests never touch the live key), D-12 (Larry contract calibration), D-19 (shared tripwire list).
Output: five test files. No production code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-VALIDATION.md
@tests/run-all-354.sh
@tests/test-353-tripwires.cjs

Parallel-session rules for EVERY task in this plan (D-11): commit only by explicit path (`git add <path>`; `git add -f <path>` for anything under `.planning/`), never `git add -A`, never `git stash`, never revert or checkout another session's diff. Re-read any shared file from disk right before editing it. Unowned dirty files that this plan must never edit: `scripts/eval-icm-writers.cjs`, `tests/test-353-grader-agreement.cjs`, `tests/test-353-ledger-shape.cjs` (D-18). Wave-1 siblings 356-02 (353 builder, client) and 356-03 (chain-executor) edit code that this plan's existing-suite legs exercise; if one of those legs fails while a sibling is mid-edit, re-run it after the sibling's next commit and record both runs in the SUMMARY, never patch the sibling's files.

Test hygiene contract used by every 356 test file (state it in each file's header comment): the file starts with an inline scrub block, before any other require of repo code: `delete process.env.TYPESAFE_API_KEY;`, a module-level counter `let NET_ATTEMPTS = 0;`, and `globalThis.fetch = function noNetwork356() { NET_ATTEMPTS += 1; throw new Error('no network in 356 tests'); };`. It asserts `NET_ATTEMPTS === 0` as its last check. Every spawned child gets an env copy with `TYPESAFE_API_KEY` deleted, `HOME` set to a fresh `fs.mkdtempSync` directory (so `~/.secrets/typesafe.env` is unreachable), and `NODE_OPTIONS` = `--require <absolute path of tests/fixtures/356-no-network-preload.cjs>`; the test fails if the child's stderr contains `NETWORK_ATTEMPT_356`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: No-network preload and the run-all-356.sh aggregator</name>
  <files>tests/fixtures/356-no-network-preload.cjs, tests/run-all-356.sh</files>
  <read_first>
    - tests/run-all-354.sh (whole file: run/run_if shape, em-dash guard with the printf '\xe2\x80\x94' byte grep, exit semantics PASS/FAIL/SKIP and `exit $(( FAIL > 0 ? 1 : 0 ))`)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-VALIDATION.md (Wave 0 list and per-requirement test names)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md ("Validation Architecture" and Pitfall 2)
  </read_first>
  <action>
    1. Create `tests/fixtures/356-no-network-preload.cjs`: `'use strict'`; header comment (hyphens only) saying it is loaded with `NODE_OPTIONS=--require <abs path>` by every 356 test that spawns a child, because `TYPESAFE_API_KEY` is exported in the developer shell and a fixture-path bug would otherwise make a real, billable vendor call (356-RESEARCH Pitfall 2). Body: `delete process.env.TYPESAFE_API_KEY;` then assign `globalThis.fetch` a function named `noNetwork356` that writes the exact line `NETWORK_ATTEMPT_356` plus newline to `process.stderr` and throws `new Error('no network in 356 tests')`. Nothing else, no exports needed.
    2. Create `tests/run-all-356.sh` modeled on `tests/run-all-354.sh`: shebang, `set -uo pipefail`, ROOT resolution and `cd "$ROOT"`, the same `run` and `run_if` functions, PASS/FAIL/SKIP counters, header comment stating: written ONCE in 356-01, no later 356 plan edits it, later plans only add the test files the legs already name; exit 0 PASSED, 77 SKIPPED (ENV GAP), else FAILED; hyphens only.
    3. Guarded legs, in this order, each `run_if "356: <label> (<req>)" tests/<file> node tests/<file>`: test-356-jev-client.cjs (R356-08), test-356-egress.cjs (R356-07), test-356-tripwires.cjs (R356-05 d), test-356-larry-contract.cjs (D-12), test-356-runtime.cjs (R356-05, R356-06), test-356-label-sheet.cjs (R356-02, D-04, D-16), test-356-policy.cjs (R356-01), test-356-ledger-build.cjs (R356-03, R356-04, D-14, D-15), test-356-check.cjs (R356-06), test-356-answer-key.cjs (R356-02).
    4. Existing-suite legs (plain `run`, the files exist): tests/test-264-b3-frozen.cjs, tests/test-353-tripwires.cjs, tests/test-larry-handoff-seam.cjs, tests/test-chain-executor-gate.cjs, tests/test-chain-executor-loop.cjs, tests/test-chain-executor-verdict.cjs, tests/test-chain-executor-fable-mode.cjs, tests/test-chain-executor-part8-leak.cjs. Do NOT add tests/test-353-grader-agreement.cjs or tests/test-353-ledger-shape.cjs (unowned dirty files; ledger-shape is red at baseline 13/6 by D-18); later plans check those against their recorded baseline explicitly.
    5. Ledger-absent legs (SPEC R5 c): for each of the six suites test-larry-handoff-seam.cjs and the five test-chain-executor-*.cjs, one `run "356: ledger absent: <file>" env MINDRIAN_IRREVERSIBILITY_LEDGER=/nonexistent/356-no-ledger.json node tests/<file>`.
    6. Em-dash guard exactly in the 354 style: array `PHASE_356_SURFACES` = scripts/jev-devtime-client.cjs, scripts/build-command-irreversibility-ledger.cjs, scripts/irreversibility-answer-key.cjs, scripts/build-section-command-ledger.cjs, lib/core/irreversibility-ledger.cjs, lib/core/chain-executor.cjs, lib/workflow/command-resolver.cjs, data/jev-policies/command-irreversibility.json, data/jev-labels/command-irreversibility.json, data/command-irreversibility-ledger.json, data/ROOM.md, tests/test-264-b3-frozen.cjs, tests/test-353-tripwires.cjs; plus this aggregator; plus `find tests -maxdepth 1 -name 'test-356-*.cjs'`, `find tests/fixtures -maxdepth 1 -name '356-*'`, and `find .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at- -maxdepth 1 \( -name '356-*.md' -o -name '356-*.json' \)`. A missing file is skipped, never an error.
    7. `chmod +x tests/run-all-356.sh`. Commit both files by explicit path: `test(356-01): no-network preload and run-all-356 aggregator`.
  </action>
  <verify>
    <automated>bash -n tests/run-all-356.sh && bash tests/run-all-356.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/run-all-356.sh` exits 0
    - `bash tests/run-all-356.sh` exits 0 (unlanded 356 legs print `SKIPPED (missing ...)`, existing suites PASS)
    - `grep -v '^#' tests/run-all-356.sh | grep -c 'run_if "356:'` is 10
    - `grep -v '^#' tests/run-all-356.sh | grep -c 'MINDRIAN_IRREVERSIBILITY_LEDGER=/nonexistent'` is 6
    - `grep -v '^#' tests/run-all-356.sh | grep -c 'test-353-ledger-shape\|test-353-grader-agreement'` is 0
    - `node -r ./tests/fixtures/356-no-network-preload.cjs -e "fetch('https://example.invalid')"` exits non-zero and its stderr contains `NETWORK_ATTEMPT_356`
    - `grep -c "$(printf '\xe2\x80\x94')" tests/run-all-356.sh tests/fixtures/356-no-network-preload.cjs` reports 0 for both files
    - `test -x tests/run-all-356.sh`
  </acceptance_criteria>
  <done>The preload refuses any fetch loudly, and the aggregator runs green with every unlanded leg reported as SKIPPED.</done>
</task>

<task type="auto">
  <name>Task 2: HOOKS_BANNED_LEDGER_SCRIPTS list constant (D-19) and test-356-tripwires.cjs</name>
  <files>tests/test-353-tripwires.cjs, tests/test-356-tripwires.cjs</files>
  <read_first>
    - tests/test-353-tripwires.cjs (whole file; leg2 is the function with `const bannedRe = /eval-icm-writers|build-section-command-ledger/;`)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md (grep -n "named list constant" to see 357's side of the agreement; read-only, never edit 357 files)
    - tests/test-chain-executor-part8-leak.cjs (the RAW_FETCH, EXTERNAL_HTTP and BRAIN_WRITE regex constants only)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-08 last sentence, D-19)
  </read_first>
  <action>
    1. Preflight, recorded in the SUMMARY: `git status --short -- tests/test-353-tripwires.cjs` and `git log -3 --format='%h %s' -- tests/test-353-tripwires.cjs`; run `node tests/test-353-tripwires.cjs` and record its PASS/FAIL counts (expected 5 PASS / 0 FAIL). If the file carries another session's uncommitted diff, do not edit it: record that in the SUMMARY, message the owner (D-11), and do only step 3.
    2. Leg-2 conversion (D-19). If jsagi-e0 (357) already introduced a named list constant in leg 2, append `'jev-devtime-client'`, `'build-command-irreversibility-ledger'` and `'irreversibility-answer-key'` to it (skip any already present) and change nothing else. Otherwise 356 is first: declare above `function leg2()` a module-level `const HOOKS_BANNED_LEDGER_SCRIPTS = Object.freeze(['eval-icm-writers', 'build-section-command-ledger', 'jev-devtime-client', 'build-command-irreversibility-ledger', 'irreversibility-answer-key']);` with a comment: append-only list agreed across 356, 357 and 354-17 (D-19); one entry per dev-time ledger script; never fold it back into a hand-edited regex. Add a local helper `escapeRe(s)` that escapes regex metacharacters, and inside leg2 replace the literal regex with `const bannedRe = new RegExp(HOOKS_BANNED_LEDGER_SCRIPTS.map(escapeRe).join('|'));`. Update leg 2's console heading and check label to name the list and its length (for example `no non-comment hooks/ line references any HOOKS_BANNED_LEDGER_SCRIPTS entry (5 names)`). Legs 1 and 3, `isPureLineComment`, `nonCommentContains` and `listFilesRecursive` stay byte-identical.
    3. Create `tests/test-356-tripwires.cjs` with the inline scrub block from the context section. Copy (do not require) the two helper functions `isPureLineComment` and the URL-safe comment strip used by `nonCommentContains` from test-353-tripwires.cjs (test helpers, not the Jev client). Legs:
       - Leg A: list every file under `lib/` and `hooks/` recursively, print `files scanned: <n>` and fail if n is 0; `BANNED_356 = ['api.typesafe.ai', 'TYPESAFE_API_KEY', 'jev-devtime-client', 'build-command-irreversibility-ledger', 'irreversibility-answer-key']`; fail on the first non-comment line containing any entry and print the relative path and the token.
       - Leg B, negative control: write `lib/core/__scratch-356-tripwire-negctl.cjs` whose code line reads the key env var by name, run the same scan on it, assert it is caught, and `fs.unlinkSync` it in a `finally`.
       - Leg C: if `lib/core/irreversibility-ledger.cjs` exists, its non-comment lines match none of RAW_FETCH `/[^a-zA-Z_]fetch[\s]*\(/`, EXTERNAL_HTTP `/https?:\/\//`, or the BRAIN_WRITE regex copied from test-chain-executor-part8-leak.cjs, and contain no case-insensitive `jev` or `typesafe`; if absent, print `PENDING: lib/core/irreversibility-ledger.cjs not landed yet` and count the leg as passing.
       - Leg D: non-comment lines of `lib/core/chain-executor.cjs` contain no case-insensitive whole word `jev` and no `typesafe`.
       - Final check `NET_ATTEMPTS === 0`. Exit 1 on any failure, print `test-356-tripwires: PASS (<n> checks)` otherwise.
    4. Commit both files by explicit path: `test(356-01): HOOKS_BANNED_LEDGER_SCRIPTS list (D-19) and 356 vendor tripwires`.
  </action>
  <verify>
    <automated>node tests/test-353-tripwires.cjs && node tests/test-356-tripwires.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-353-tripwires.cjs` exits 0 and prints the same number of PASS lines as the preflight run
    - `grep -c "HOOKS_BANNED_LEDGER_SCRIPTS" tests/test-353-tripwires.cjs` is at least 2
    - `grep -v '^\s*//' tests/test-353-tripwires.cjs | grep -c "/eval-icm-writers|build-section-command-ledger/"` is 0
    - `grep -c "'jev-devtime-client'" tests/test-353-tripwires.cjs` is 1 and `grep -c "'build-command-irreversibility-ledger'" tests/test-353-tripwires.cjs` is 1
    - `node tests/test-356-tripwires.cjs` exits 0, prints `files scanned:` followed by a number greater than 0, and reports the negative control as caught
    - `test ! -e lib/core/__scratch-356-tripwire-negctl.cjs` (scratch file removed)
    - `git show $(git log -1 --format=%H -- tests/test-353-tripwires.cjs) -- tests/test-353-tripwires.cjs | grep -c '^-.*function leg1\|^-.*function leg3'` is 0 (legs 1 and 3 untouched; the commit is located by path, not HEAD~1, because peer sessions commit in parallel)
  </acceptance_criteria>
  <done>Leg 2 is list-driven and append-only; the 356 tripwire proves no vendor token sits in lib/ or hooks/ and that the scan can actually catch one.</done>
</task>

<task type="auto">
  <name>Task 3: D-12 Larry contract test (tests/test-356-larry-contract.cjs)</name>
  <files>tests/test-356-larry-contract.cjs</files>
  <read_first>
    - agents/larry-extended.md (run `grep -n '^## ' agents/larry-extended.md`, then read the "## Post-Gate Handoff" section and the "## Decision Gates" section in full; READ ONLY, this phase never edits it)
    - skills/larry-personality/SKILL.md (`grep -n -i 'material\|irreversible' skills/larry-personality/SKILL.md` and read those lines)
    - tests/test-larry-handoff-seam.cjs (whole file)
    - lib/core/chain-executor.cjs (`grep -n "forced_material" lib/core/chain-executor.cjs`)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-03, D-12)
    - `git log -5 --format='%h %ad %s' --date=iso -- agents/larry-extended.md` (records whether 357's gate-prose shrink has landed)
  </read_first>
  <action>
    Create `tests/test-356-larry-contract.cjs` with the inline scrub block. Legs:
    1. Section extraction: find the line starting with `## Post-Gate Handoff`, take every line up to (not including) the next line starting with `## `. FAIL if the heading is missing (357 D-16 keeps it verbatim; a missing heading means the contract moved and must be re-checked by hand). Assert the section matches `/halts at the first material/i` and contains `runChain`.
    2. No redefinition (D-12): over non-empty lines of agents/larry-extended.md and skills/larry-personality/SKILL.md, no line matches `REDEFINE_MATERIAL_RE = /\bmaterial\b[^.\n]{0,40}\b(means|is defined as|equals|only means)\b[^.\n]{0,40}\birreversible\b/i` or `REDEFINE_IRREVERSIBLE_RE = /\birreversible\b[^.\n]{0,20}\b(means|is defined as|equals)\b[^.\n]{0,20}\bmaterial\b/i`. On a hit print file:line and fail.
    3. Reason-code consistency: non-comment lines of lib/core/chain-executor.cjs contain the literal `'forced_material'` at least 2 times (the two halt-reason sites). Print, without asserting, whether larry-extended.md mentions `forced_material`.
    4. D-03 independence, only when `data/jev-policies/command-irreversibility.json` exists (else print `PENDING: policy not drafted yet`): parse it; its `instructions` string matches `/irreversible is narrower than material/i`, and the file text contains no `larry-extended` substring.
    5. Handoff seam runs: spawn `node tests/test-larry-handoff-seam.cjs` twice with the child-env rules from the context section: (a) default env; (b) plus `MINDRIAN_IRREVERSIBILITY_LEDGER=/nonexistent/356-no-ledger.json`. Both must exit 0 and neither stderr may contain `NETWORK_ATTEMPT_356`. When `data/command-irreversibility-ledger.json` exists and (a) passed, print exactly `handoff seam with shipped ledger: PASS`; otherwise print `handoff seam with shipped ledger: PENDING (ledger not shipped yet)`.
    6. No 356 edit of the contract file: `git log --format=%s -- agents/larry-extended.md` via `child_process.spawnSync`; fail if any subject matches `/\(356-\d\d\)|phase-356|\b356-\d\d\b/`.
    7. Final `NET_ATTEMPTS === 0`; exit 1 on any failure.
    Commit by explicit path: `test(356-01): D-12 Larry post-gate contract check`.
  </action>
  <verify>
    <automated>node tests/test-356-larry-contract.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-larry-contract.cjs` exits 0
    - its stdout contains `handoff seam with shipped ledger: PENDING` (no ledger ships yet) and no `FAIL`
    - `grep -c "Post-Gate Handoff" tests/test-356-larry-contract.cjs` is at least 1
    - `git log -1 --format=%H -- agents/larry-extended.md` is unchanged by this plan (compare with the value recorded in read_first)
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-356-larry-contract.cjs tests/test-356-tripwires.cjs` reports 0 for both
  </acceptance_criteria>
  <done>The D-12 contract is machine-checked from the Larry file without touching it, and the handoff seam passes with and without a ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> TypeSafe API | the developer shell exports TYPESAFE_API_KEY; any unscrubbed test could make a real vendor call |
| dev-time scripts -> user runtime (lib/, hooks/) | vendor-touching code must never reach a user's turn loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-01 | Information disclosure | every 356 test | mitigate | inline scrub block deletes TYPESAFE_API_KEY and replaces fetch with a counting thrower; spawned children use tests/fixtures/356-no-network-preload.cjs and a temp HOME; tests assert zero attempts |
| T-356-02 | Elevation of privilege | lib/, hooks/ | mitigate | test-356-tripwires scans every file for five vendor tokens with a planted negative control; leg C sweeps the runtime ledger reader for fetch, URLs and vendor names |
| T-356-03 | Tampering | tests/test-353-tripwires.cjs leg 2 | mitigate | the list keeps both original names, legs 1 and 3 stay byte-identical, the PASS count is compared with the preflight run |
| T-356-06 | Tampering | peer sessions' working-tree diffs | mitigate | explicit-path commits only; preflight git status on the shared tripwire file; unowned 353 files never opened for write |
| T-356-SC | Tampering | package installs | accept | this phase installs no packages (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `bash tests/run-all-356.sh` exits 0
- `node tests/test-353-tripwires.cjs` exits 0 with the preflight PASS count
- `node tests/test-356-tripwires.cjs && node tests/test-356-larry-contract.cjs` both exit 0
</verification>

<success_criteria>
The phase has a green aggregator, a list-driven hooks/ ban that 357 and 354-17 can append to, a lib/ and hooks/ vendor tripwire with a working negative control, and a D-12 contract test that reads the Larry file without editing it.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `tests/fixtures/356-no-network-preload.cjs` (new)
- `tests/run-all-356.sh` (new, written once)
- `tests/test-353-tripwires.cjs` (leg 2 converted to `HOOKS_BANNED_LEDGER_SCRIPTS`)
- `tests/test-356-tripwires.cjs` (new)
- `tests/test-356-larry-contract.cjs` (new)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-01-SUMMARY.md` when done (commit it with `git add -f` by explicit path). Record: the preflight test-353-tripwires counts, whether 357 had already introduced the list constant, and the larry-extended git log lines read in Task 3.
</output>
