---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/irreversibility-ledger.cjs
  - lib/workflow/command-resolver.cjs
  - lib/core/chain-executor.cjs
  - tests/test-264-b3-frozen.cjs
  - tests/test-356-runtime.cjs
autonomous: true
requirements: [R356-05, R356-06]
tags: [chain-executor, runtime, add-only, frozen-pin, canon-part3]

must_haves:
  truths:
    - "isIrreversibleStep(step) = step.irreversible === true OR keyword substring OR a FRESH ledger entry for step.command with flag true; the ledger clause is reached only after both older signals returned nothing, so it can force a halt and never clear one (SPEC R5, add-only)"
    - "A ledger entry counts only when its text_hash equals sha256 hex of JSON.stringify([command, teaching, jtbd_summary]) over the CURRENT registry row (non-strings as ''); a stale, unknown or malformed entry is ignored silently (SPEC R6)"
    - "The ledger is read from local disk at most once per process with zero network calls; a missing or unreadable ledger degrades to exactly the pre-356 predicate for every registry command"
    - "A synthetic fresh flag:true entry on an autonomous_safe command makes runChain halt there with haltedAt.reason === 'forced_material' (R5 a); /mos:publish with flag:false still halts forced_material (R5 b)"
    - "Only isIrreversibleStep's final `return false;` changed inside the six pinned functions; tests/test-264-b3-frozen.cjs re-pins exactly that one hash with a written reason and the other five pins are unchanged (D-17)"
  artifacts:
    - path: "lib/core/irreversibility-ledger.cjs"
      provides: "Pure local ledger reader: DEFAULT_LEDGER_PATH, TEXT_HASH_BASIS, commandTextHash, forcesIrreversible, __reset"
      exports: ["DEFAULT_LEDGER_PATH", "TEXT_HASH_BASIS", "commandTextHash", "forcesIrreversible", "__reset"]
    - path: "lib/workflow/command-resolver.cjs"
      provides: "Additive commandRow(cmd) accessor over the existing cached registry read"
      contains: "function commandRow"
    - path: "lib/core/chain-executor.cjs"
      provides: "Lazy _loadIrreversibilityLedger / _ledgerForcesIrreversible helpers and the single changed return line"
      contains: "return _ledgerForcesIrreversible(step.command);"
    - path: "tests/test-356-runtime.cjs"
      provides: "Unit + runChain integration legs for R5 a/b/c, add-only, staleness, one-read-per-process"
      min_lines: 150
  key_links:
    - from: "lib/core/chain-executor.cjs isIrreversibleStep"
      to: "lib/core/irreversibility-ledger.cjs forcesIrreversible"
      via: "lazy require in _loadIrreversibilityLedger, called from the final return line"
      pattern: "_ledgerForcesIrreversible\\(step\\.command\\)"
    - from: "lib/core/irreversibility-ledger.cjs"
      to: "lib/workflow/command-resolver.cjs commandRow"
      via: "lazy require inside _build; the registry text the hash is checked against"
      pattern: "commandRow"
---

<objective>
Land the runtime half of the ledger against synthetic test ledgers, before any real ledger exists: a pure local reader (`lib/core/irreversibility-ledger.cjs`), an additive `commandRow` accessor on the command resolver, and a one-line, add-only change to `isIrreversibleStep` in `lib/core/chain-executor.cjs`. Re-pin the one frozen hash that change breaks, with a written reason (D-17).

Purpose: SPEC R5 (add-only runtime integration, local-only, silent degradation) and R6 (runtime staleness fallback). Every caller of `isIrreversibleStep` (the default gate in `makeGateFn`, `_isMaterialStep`, both `forced_material` halt-reason sites) inherits the change with no edits.
Output: the reader module, the accessor, the seam, the re-pin, and `tests/test-356-runtime.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md

Parallel-session rules (D-11): commit only by explicit path, never `git add -A`, never stash, never revert or checkout another session's diff. 354-03 (jsagi-25) landed edits in chain-executor.cjs today (`_runChainResilient`); anchor every edit by FUNCTION NAME found with grep, never by line number (RESEARCH Pitfall 8). Test hygiene contract: see 356-01-PLAN.md context (inline scrub block, NET_ATTEMPTS === 0). Wave 1: tests/fixtures/356-no-network-preload.cjs may not exist yet, so this plan's test runs everything in-process.

Runtime contracts this plan creates (signatures, not code):
- `commandTextHash(command, teaching, jtbdSummary)` -> 64-char lowercase hex: sha256 of the UTF-8 bytes of `JSON.stringify([norm(command), norm(teaching), norm(jtbdSummary)])`, where norm(v) = v when v is a string, else ''. This is THE staleness hash; the builder (356-07) imports it, and Theo would port it from `TEXT_HASH_BASIS`.
- `TEXT_HASH_BASIS` = 'sha256 hex of UTF-8 JSON.stringify([command, teaching, jtbd_summary]), non-string -> ""'
- `forcesIrreversible(command)` -> boolean, never throws; true only for a command with a fresh `flag: true` entry.
- Ledger path: env `MINDRIAN_IRREVERSIBILITY_LEDGER` when non-empty, else `data/command-irreversibility-ledger.json` (resolved from `__dirname`). Ledger shape read: `{ entries: [{ command, p_irreversible, flag, text_hash }], ... }`; only `entries` is read at runtime.
- `commandRow(cmd)` on command-resolver -> frozen `{ command, teaching, jtbd_summary }` (raw values) or null.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baselines, commandRow accessor, lib/core/irreversibility-ledger.cjs, and the reader unit legs</name>
  <files>lib/workflow/command-resolver.cjs, lib/core/irreversibility-ledger.cjs, tests/test-356-runtime.cjs</files>
  <read_first>
    - lib/workflow/command-resolver.cjs (whole file: `_registryPath`, `_load`, `EMPTY_REGISTRY`, `__reset`, the export object)
    - lib/core/chain-executor.cjs (`grep -n "function _loadRecipeMaps\|IRREVERSIBLE_HINTS\|function isIrreversibleStep\|^module.exports" lib/core/chain-executor.cjs`; read `_loadRecipeMaps` and `isIrreversibleStep`)
    - lib/core/recipe-maps.cjs (`grep -n "function postureForCommand" lib/core/recipe-maps.cjs` and read that function: the exact-match rule on `command`)
    - tests/test-chain-executor-part8-leak.cjs (RAW_FETCH, EXTERNAL_HTTP, BRAIN_WRITE and CODE_SURFACES)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 2)
  </read_first>
  <action>
    1. Baselines BEFORE any edit (write to the SUMMARY under "## Baselines (pre-edit)", each run with `env -u TYPESAFE_API_KEY`, record exit code and pass/fail counts): tests/test-264-b3-frozen.cjs (expected PASS, 29 checks), tests/test-larry-handoff-seam.cjs, tests/test-chain-executor-gate.cjs, -loop, -verdict, -fable-mode, -part8-leak, tests/test-bch-09-forced-material.cjs, tests/test-ignite-on-runchain.cjs, tests/test-201-bounded-retry.cjs, tests/test-264-flagship-ralph.cjs, tests/test-354-chain-resume-identity.cjs, tests/test-act-on-runchain.cjs, tests/test-pipeline-on-runchain.cjs, tests/test-harness-167-verdict.cjs, lib/workflow/command-resolver.test.cjs, `bash tests/run-all-166.sh` (expected one pre-existing FAIL: test-act-prebehavior-snapshot), `bash tests/run-all-264.sh` (expected two pre-existing FAIL arms: frozen-166 passthrough and the whole-file zero-diff arm). Also `git status --short -- lib/core/chain-executor.cjs lib/workflow/command-resolver.cjs` (both must be clean; if either carries another session's diff, stop and message jsagi-25).
    2. `lib/workflow/command-resolver.cjs`: add `function commandRow(cmd)` next to `validateChainAutonomy`: return null unless `cmd` is a non-empty string; find the entry in `_load().commands` whose `command === cmd`; return `Object.freeze({ command: c.command, teaching: c.teaching, jtbd_summary: c.jtbd_summary })` or null. Add `commandRow: commandRow` to module.exports. Nothing else in the file changes.
    3. Create `lib/core/irreversibility-ledger.cjs`: `'use strict'`; requires only `node:fs`, `node:path`, `node:crypto`; header comment (hyphens only): Phase 356 R5/R6; reads a dev-time-scored ledger shipped as data; add-only (can force a halt, never clear one); zero network; read at most once per process; lib/ must never require the dev-time scoring client (tripwires). Contents: `DEFAULT_LEDGER_PATH` (path.join(__dirname, '..', '..', 'data', 'command-irreversibility-ledger.json')); `_ledgerPath()` honoring `MINDRIAN_IRREVERSIBILITY_LEDGER`; `TEXT_HASH_BASIS`; `commandTextHash` per the context contract; a module cache `_fresh` (null until built); `_build()` returning a Set: read and JSON.parse the ledger (any error -> empty Set); `entries` must be an array (else empty Set); lazily require `../workflow/command-resolver.cjs` (failure or missing `commandRow` -> empty Set); for each entry keep it only when `flag === true`, `command` is a string, `text_hash` matches `/^[0-9a-f]{64}$/`, `commandRow(command)` is non-null, and `commandTextHash(command, row.teaching, row.jtbd_summary) === text_hash`; `forcesIrreversible(command)` builds `_fresh` on first call inside try/catch (any throw -> false) and returns `typeof command === 'string' && _fresh.has(command)`; `__reset()` sets `_fresh = null`. No identifier or string may contain `fetch`, `http`, `jev` or `typesafe` (the part8-leak and 356 tripwire scans).
    4. Create `tests/test-356-runtime.cjs` (inline scrub block) with the READER legs; Task 2 appends the integration legs. Helpers: `writeLedger(obj)` writes JSON to a fresh temp file and sets `process.env.MINDRIAN_IRREVERSIBILITY_LEDGER`; `resetAll()` calls the ledger module's `__reset()` and command-resolver's `__reset()`. Real registry text comes from `commandRow`. Legs:
       - hash: 64-char lowercase hex; deterministic; `commandTextHash(c, null, x) === commandTextHash(c, '', x)`; all registry commands (enumerate `require('../data/command-registry.json').commands`, never hard-code 113) hash to distinct values.
       - fresh flag:true entry for `/mos:find-analogies` -> `forcesIrreversible('/mos:find-analogies') === true`, and `'/mos:find-connections'` -> false.
       - stale entry (text_hash computed over an edited teaching) -> false.
       - flag:false -> false; unknown command `/mos:zz-356-unknown` -> false; `text_hash` not 64-hex -> false.
       - malformed JSON file -> false for every registry command; missing file -> false; `entries` not an array -> false; non-string argument -> false.
       - registry override: point `MINDRIAN_COMMAND_REGISTRY` at a temp copy of the registry whose `/mos:find-analogies` teaching differs; the fresh-for-real-registry entry is now ignored (false); restore the env and reset.
       - one read per process: wrap `fs.readFileSync` with a counter for calls whose first argument equals the ledger path, call `forcesIrreversible` 200 times across registry commands, assert the count is 1; after `__reset()` one more call reads again (count 2); restore the original function.
    5. Commit by explicit path: `feat(356-03): local irreversibility ledger reader and commandRow accessor (R5, R6)`.
  </action>
  <verify>
    <automated>node tests/test-356-runtime.cjs && node lib/workflow/command-resolver.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-runtime.cjs` exits 0 with every reader leg passing
    - `node lib/workflow/command-resolver.test.cjs` exit code equals its recorded baseline
    - `node -e "const r=require('./lib/workflow/command-resolver.cjs');const x=r.commandRow('/mos:publish');process.exit(x&&x.command==='/mos:publish'&&Object.isFrozen(x)&&r.commandRow('')===null?0:1)"` exits 0
    - `grep -v '^\s*//\|^\s*\*' lib/core/irreversibility-ledger.cjs | grep -ciE "fetch|https?:|jev|typesafe"` is 0
    - `grep -v '^\s*//\|^\s*\*' lib/core/irreversibility-ledger.cjs | grep 'require(' | grep -v "require('node:" | grep -vc "command-resolver"` is 0
    - `git diff -U0 -- lib/workflow/command-resolver.cjs | grep '^-' | grep -vc '^---'` is at most 1 (only the export object's closing context may change; no existing function body removed)
    - The SUMMARY has "## Baselines (pre-edit)" with an exit code for every suite listed in step 1
  </acceptance_criteria>
  <done>A pure, network-free, once-per-process ledger reader exists and is proven against synthetic ledgers, including stale and malformed ones.</done>
</task>

<task type="auto">
  <name>Task 2: The add-only seam in isIrreversibleStep, the single documented re-pin (D-17), and runChain integration legs</name>
  <files>lib/core/chain-executor.cjs, tests/test-264-b3-frozen.cjs, tests/test-356-runtime.cjs</files>
  <read_first>
    - lib/core/chain-executor.cjs (RE-READ FROM DISK NOW: `git log -3 --format='%h %s' -- lib/core/chain-executor.cjs`, `git status --short -- lib/core/chain-executor.cjs`; then `grep -n "function _loadRecipeMaps\|function isIrreversibleStep\|function _isMaterialStep\|function makeGateFn\|'forced_material'\|^function runChain" lib/core/chain-executor.cjs` and read each of those functions plus the comment block above `IRREVERSIBLE_HINTS`)
    - tests/test-264-b3-frozen.cjs (whole file: `extractFunction`, `PINNED_HASHES`, header)
    - tests/test-larry-handoff-seam.cjs (the `executor.runChain(steps, { postureFn, onStep, onHalt, decideFn })` call shape and the `result.haltedAt` fields)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-17)
  </read_first>
  <action>
    1. If `git status` shows another session's uncommitted diff in chain-executor.cjs, STOP: message jsagi-25 and wait for their commit; never edit on top of a peer's uncommitted diff.
    2. In `lib/core/chain-executor.cjs`, directly after the `_loadRecipeMaps` function (anchor by name), add: `let _irreversibilityLedger = null;`, `function _loadIrreversibilityLedger()` mirroring `_loadRecipeMaps` (lazy `require(path.join(__dirname, 'irreversibility-ledger.cjs'))`, `false` on failure, cached), and `function _ledgerForcesIrreversible(command)` (returns false when the module is unavailable or `forcesIrreversible` is not a function; otherwise returns `m.forcesIrreversible(command) === true` inside try/catch, false on throw). Comment above them: Phase 356 R5, a dev-time-scored ledger shipped as data, add-only, zero network, degrades silently to the two older signals.
    3. Per D-17, inside `isIrreversibleStep` change ONLY its final line `  return false;` to `  return _ledgerForcesIrreversible(step.command);`. No other byte inside `isIrreversibleStep`, `_isMaterialStep`, `makeGateFn`, `_defaultPostureFn`, `_critiqueFailed` or `_ralphSafeRetry` changes. Outside the pinned bodies, append one sentence to the comment block above `IRREVERSIBLE_HINTS` naming the third signal (a fresh Phase 356 ledger entry; it can force a halt, never clear one). Do not name the vendor in chain-executor.cjs.
    4. Re-pin (D-17): compute the new hash with the test's own rule (slice from the line matching `^function isIrreversibleStep\(` through the next line that is exactly `}`, sha256 hex) using a one-off `node -e` that requires nothing but fs and crypto. In `tests/test-264-b3-frozen.cjs` replace ONLY the `isIrreversibleStep` literal in `PINNED_HASHES`. Add directly above `const PINNED_HASHES` a comment: `Re-pin (Phase 356 R5, D-17, <YYYY-MM-DD>): isIrreversibleStep's final "return false;" became "return _ledgerForcesIrreversible(step.command);", an add-only third signal (a fresh dev-time ledger entry can force a halt, never clear one). Previous pin 037f9515aeff1b5123956b4dfda515ee3076f20fd4423f5cc0e92f65b4c3bc3d. The other five pins are unchanged. First re-pin of a Canon Part 3 gate surface.` Add one line to the file's header comment: `Phase 356 re-pinned isIrreversibleStep only; see the ARM 1 comment.` Nothing else in the test changes (hyphens only).
    5. Append integration legs to `tests/test-356-runtime.cjs` (use `resetAll()` after each env change; the executor caches the ledger MODULE, and the module's `__reset` clears its Set):
       - R5(a): assert from the registry that `/mos:find-analogies` has `autonomous_safe: true`; synthetic ledger with a fresh flag:true entry for it; `runChain([{ step: 1, command: '/mos:find-analogies' }, { step: 2, command: '/mos:find-connections' }], { postureFn: () => ({ autonomous_safe: true, posture: 'run' }), onStep: recorder, onHalt: () => 'defer', decideFn: () => ({ decision_trace: null }) })` -> `completed === false`, `haltedAt.step.command === '/mos:find-analogies'`, `haltedAt.reason === 'forced_material'`, and the recorder saw no step.
       - R5(b): synthetic ledger with a FRESH `flag: false` entry for `/mos:publish`; `isIrreversibleStep({ command: '/mos:publish' }) === true` and a one-step runChain halts with `forced_material`.
       - R5(c): `MINDRIAN_IRREVERSIBILITY_LEDGER=/nonexistent/356.json`: for every registry command `c`, `isIrreversibleStep({ command: c })` equals `preRule(c)`, where `preRule` is the keyword-only predicate re-implemented in the test from `executor.IRREVERSIBLE_HINTS` (lowercased substring); `isIrreversibleStep({ irreversible: true })` stays true; repeat with a malformed ledger file: same equality.
       - add-only property: with a ledger where every registry command has a fresh `flag: false` entry, and again with every entry stale, no command where `preRule(c)` is true returns false.
       - export surface unchanged: `Object.keys(require('../lib/core/chain-executor.cjs')).sort()` equals a constant list copied into the test from the pre-edit `module.exports` (the seam adds no export; `_isMaterialStep` and the new helpers stay private).
       - final `NET_ATTEMPTS === 0`.
    6. Regression reruns, each with `env -u TYPESAFE_API_KEY`, twice: default env (no shipped ledger exists yet) and `MINDRIAN_IRREVERSIBILITY_LEDGER=/nonexistent/356.json`: every suite from Task 1 step 1. Each must match its recorded baseline exactly (same pre-existing fails, no new fail). Record both runs in the SUMMARY.
    7. Commit by explicit path (`git add lib/core/chain-executor.cjs tests/test-264-b3-frozen.cjs tests/test-356-runtime.cjs`): `feat(356-03): add-only ledger clause in isIrreversibleStep; re-pin its frozen hash (D-17)`.
  </action>
  <verify>
    <automated>node tests/test-264-b3-frozen.cjs && node tests/test-356-runtime.cjs && node tests/test-chain-executor-part8-leak.cjs && node tests/test-larry-handoff-seam.cjs && node tests/test-chain-executor-gate.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-264-b3-frozen.cjs` exits 0 and prints `PASS (29 checks)`
    - In the commit that changed tests/test-264-b3-frozen.cjs (`C=$(git log -1 --format=%H -- tests/test-264-b3-frozen.cjs)`): `git show -U0 $C -- tests/test-264-b3-frozen.cjs | grep '^-' | grep -v '^---' | grep -c 037f9515aeff1b5123956b4dfda515ee3076f20fd4423f5cc0e92f65b4c3bc3d` is 1, and `git show -U0 $C -- tests/test-264-b3-frozen.cjs | grep '^-' | grep -v '^---' | grep -cE "43a27cb8|c644e445|6f356837|da7cbfed|38192499"` is 0
    - In the commit that changed lib/core/chain-executor.cjs: the removed lines are exactly one line, `  return false;` (`git show -U0 <sha> -- lib/core/chain-executor.cjs | grep '^-' | grep -v '^---'` prints only that line)
    - `grep -c "return _ledgerForcesIrreversible(step.command);" lib/core/chain-executor.cjs` is 1
    - `node tests/test-356-runtime.cjs` exits 0 with legs R5(a), R5(b), R5(c), add-only and export-surface passing
    - `node tests/test-chain-executor-part8-leak.cjs` exits 0
    - Every regression suite matches its pre-edit baseline in both runs (default env and `MINDRIAN_IRREVERSIBILITY_LEDGER=/nonexistent/356.json`), recorded in the SUMMARY
    - `grep -c "$(printf '\xe2\x80\x94')" lib/core/irreversibility-ledger.cjs lib/core/chain-executor.cjs tests/test-356-runtime.cjs tests/test-264-b3-frozen.cjs` reports 0 for each
  </acceptance_criteria>
  <done>isIrreversibleStep gains an add-only, hash-checked, local-only third signal through one changed line; the frozen pin moves by exactly one documented hash; every chain suite is at its baseline with and without a ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/command-irreversibility-ledger.json -> runChain gate | a local data file influences whether a step halts |
| registry text -> ledger verdict | a verdict scored on old text must not apply to changed text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-08 | Tampering | isIrreversibleStep | mitigate | the ledger clause is the final return, reached only after the explicit flag and keyword checks return nothing; the add-only property test runs every registry command against all-false and all-stale ledgers |
| T-356-09 | Tampering | irreversibility-ledger.cjs _build | mitigate | per-entry text_hash compared with commandTextHash over the CURRENT registry row; stale, unknown and malformed entries ignored; a registry override makes every entry stale |
| T-356-10 | Denial of service | runChain | mitigate | lazy require and forcesIrreversible both wrapped in try/catch returning false; missing or malformed ledger legs prove the pre-356 predicate holds |
| T-356-11 | Repudiation | tests/test-264-b3-frozen.cjs | mitigate | exactly one hash literal changes, with a written reason naming D-17 and the previous pin; the other five pins verified unchanged in the commit diff |
| T-356-02 | Elevation of privilege | lib/core/irreversibility-ledger.cjs | mitigate | requires only node built-ins and the resolver; no fetch, URL or vendor name (part8-leak scan and 356 tripwire leg C) |
| T-356-SC | Tampering | package installs | accept | no packages installed |
</threat_model>

<verification>
- `node tests/test-356-runtime.cjs && node tests/test-264-b3-frozen.cjs` both exit 0
- The chain suites in the SUMMARY match their pre-edit baselines with and without `MINDRIAN_IRREVERSIBILITY_LEDGER=/nonexistent/356.json`
- `bash tests/run-all-166.sh` and `bash tests/run-all-264.sh` show only their recorded pre-existing fails
</verification>

<success_criteria>
The runtime reads a hash-checked ledger locally and add-only through one changed line, degrades to today's behavior when the ledger is missing or bad, and the frozen gate pin is moved by one documented hash.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `lib/core/irreversibility-ledger.cjs` (new)
- `lib/workflow/command-resolver.cjs` (additive `commandRow`)
- `lib/core/chain-executor.cjs` (lazy loader, helper, one changed return line)
- `tests/test-264-b3-frozen.cjs` (one hash re-pinned with a written reason)
- `tests/test-356-runtime.cjs` (new)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-03-SUMMARY.md` when done (`git add -f`, explicit path). It must contain "## Baselines (pre-edit)" and "## Regression reruns" (default env and ledger-absent), the old and new isIrreversibleStep hashes, and a note for the navigator that this is the first re-pin of a Canon Part 3 gate surface.
</output>
