---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 10
type: execute
wave: 4
depends_on: ["356-09", "356-08"]
files_modified:
  - scripts/build-command-irreversibility-ledger.cjs
  - tests/test-356-check.cjs
  - tests/test-356-answer-key.cjs
autonomous: true
requirements: [R356-06, R356-02]
tags: [check, staleness, answer-key, d-16, zero-network]

must_haves:
  truths:
    - "`--check` is key-free and zero-network: it lists stale entries (text_hash mismatch), unscored commands (in the registry, not the ledger), removed commands, policy and answer-key drift, label-set mismatch, flag inconsistencies, a false-alarm recount mismatch, and a shipped fixture build, each as a WARN line, and always exits 0 (SPEC R6: never a release blocker, never needs the key)"
    - "Editing one command's scored text in a test registry makes the runtime ignore its entry and makes --check print it STALE; adding a command makes --check print it UNSCORED (R6 acceptance)"
    - "The committed answer key covers exactly the live registry's command set, carries reviewed_by 'navigator' and reviewed_at, a valid label_source on every row, and a failing diff names any new command that needs a label (R2 acceptance)"
    - "Git order proves the labeling sequence (D-16): the pre-label seal precedes the blind sheet, the filled blind sheet precedes the policy's first commit, and the blind sheet's commit time precedes the ledger's built_at"
  artifacts:
    - path: "scripts/build-command-irreversibility-ledger.cjs"
      provides: "runCheck and the --check branch (first branch of main)"
      contains: "runCheck"
    - path: "tests/test-356-check.cjs"
      provides: "Stale, unscored, removed, missing-ledger, flag-inconsistency legs; zero-network proof in process and spawned; exit 0 always"
      min_lines: 120
    - path: "tests/test-356-answer-key.cjs"
      provides: "Build-time refusal legs plus shipped answer-key legs: set equality, header, label_source, seal, disagreement recount, D-16 git order"
      min_lines: 150
  key_links:
    - from: "runCheck"
      to: "lib/core/irreversibility-ledger.cjs commandTextHash"
      via: "the same staleness hash the runtime uses"
      pattern: "commandTextHash"
    - from: "tests/test-356-answer-key.cjs"
      to: "356-BLIND-LABEL-SHEET.md / 356-CLAUDE-PRELABELS.json git history"
      via: "git log / git merge-base --is-ancestor"
      pattern: "merge-base"
---

<objective>
Add the key-free `--check` mode to the builder and the two remaining 356 tests: `tests/test-356-check.cjs` (staleness and zero-network) and `tests/test-356-answer-key.cjs` (answer-key integrity and the D-16 labeling order proven from git).

Purpose: SPEC R6 (staleness fallback, key-free zero-network `--check` that exits 0 with WARN), R2 acceptance (answer key equals the registry set, reviewer and date recorded, checked by a test), D-16.
Output: `runCheck` plus the `--check` branch, and two test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-08-SUMMARY.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-09-SUMMARY.md

Do not wire `--check` into scripts/release.sh (tests/test-353-release-wiring.cjs counts the 353 builder's call sites only, and release wiring is outside the SPEC). Do not copy the 353 `--check`'s plugin_version comparison (it would turn every version bump into a failure, against R6). Explicit-path commits only. Test hygiene contract: see 356-01-PLAN.md context.
</context>

<tasks>

<task type="auto">
  <name>Task 1: runCheck, the --check branch, and tests/test-356-check.cjs</name>
  <files>scripts/build-command-irreversibility-ledger.cjs, tests/test-356-check.cjs</files>
  <read_first>
    - scripts/build-command-irreversibility-ledger.cjs (whole file: `main`, `loadInputs`, `readPolicy`, constants)
    - lib/core/irreversibility-ledger.cjs (`commandTextHash`, `forcesIrreversible`, `__reset`, the env override)
    - lib/workflow/command-resolver.cjs (`__reset`, `MINDRIAN_COMMAND_REGISTRY`)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 7)
  </read_first>
  <action>
    1. Add `runCheck({ ledgerPath, registryPath, root, labelsPath })` -> `{ warnings: [{ code, detail }], summary }` that never throws (wrap every read). It never calls `client.loadKey` or `client.jev`. Checks, each producing a `{ code, detail }`: `LEDGER_MISSING` or `LEDGER_UNPARSEABLE` (then skip the ledger-dependent checks but still run the label checks); `BUILD_MODE_FIXTURE` when the ledger's build_mode is `jev-fixture`; `STALE <command>` when an entry's text_hash differs from `commandTextHash` over the current registry row; `UNSCORED <command>` for each registry command without an entry; `REMOVED <command>` for each entry whose command left the registry; `POLICY_MISSING` or `POLICY_DRIFT` (ledger policy_hash vs sha256 of the current policy bytes); `LABELS_MISSING` or `ANSWER_KEY_DRIFT` (ledger answer_key_hash vs sha256 of the label bytes); `LABEL_SET_MISMATCH <missing/extra>` (label rows vs registry); `FLAG_INCONSISTENT <command>` when `flag !== (p_irreversible >= threshold)`; `FALSE_ALARM_COUNT_MISMATCH` when a recount from entries and current labels differs from `false_alarm_count`. Paths: `--ledger` flag, else env `MINDRIAN_IRREVERSIBILITY_LEDGER`, else the default; `--registry` flag, else env `MINDRIAN_COMMAND_REGISTRY`, else the default; `--root` and `--labels` as in 356-09.
    2. Make `--check` the FIRST branch of `main`: print each warning as `WARN: <CODE> <detail>`, then `command-irreversibility-ledger --check: OK (<n> entries, T=<T>, mode=<build_mode>)` when there are none, else `command-irreversibility-ledger --check: WARN <count>`; always `process.exit(0)`.
    3. Create `tests/test-356-check.cjs` (inline scrub block). Build a fresh fixture ledger into a temp dir with the 356-09 CLI (temp root policy, synthetic answer key over the real registry, `tests/fixtures/356-jev-noul-responses.json`, `--out <tmp>/ledger.json`), copy the real registry to `<tmp>/registry.json`. Legs (spawned with the child-env rules, plus one in-process `runCheck` call):
       - clean: `--check --ledger <tmp ledger> --registry <tmp registry> --root <tmp root> --labels <tmp labels>` exits 0 and prints `OK`, except the expected `BUILD_MODE_FIXTURE` warning (assert it is the only one).
       - stale (R6 acceptance): edit `/mos:find-analogies`'s teaching in the temp registry; `--check` exits 0 and prints `WARN: STALE /mos:find-analogies`; and at runtime, with `MINDRIAN_COMMAND_REGISTRY` and `MINDRIAN_IRREVERSIBILITY_LEDGER` pointed at the temp files, a copy of the ledger whose `/mos:find-analogies` entry has `flag: true` makes `forcesIrreversible('/mos:find-analogies')` return false (after both modules' `__reset`).
       - unscored: add a command `/mos:zz-356-new` to the temp registry; `--check` prints `WARN: UNSCORED /mos:zz-356-new` (and `LABEL_SET_MISMATCH`), exits 0.
       - removed: drop a command from the temp registry; `WARN: REMOVED <that command>`.
       - flag inconsistency and recount: flip one entry's `flag` in a copy; `WARN: FLAG_INCONSISTENT <command>`; change `false_alarm_count`; `WARN: FALSE_ALARM_COUNT_MISMATCH`.
       - drift: change one byte of the temp policy -> `WARN: POLICY_DRIFT`; of the temp labels -> `WARN: ANSWER_KEY_DRIFT`.
       - missing ledger: `--ledger /nonexistent/356.json` exits 0 with `WARN: LEDGER_MISSING`.
       - zero network and key-free: the in-process `runCheck` leaves `NET_ATTEMPTS` at 0; every spawned run's stderr lacks `NETWORK_ATTEMPT_356`; one spawned run with `HOME` a fresh temp dir and no key in env still exits 0.
       - the real shipped state, only when data/command-irreversibility-ledger.json exists: `--check` with default paths exits 0 (warnings allowed, printed to the test log); else print `PENDING`.
       - final `NET_ATTEMPTS === 0`.
    4. Commit by explicit path: `feat(356-10): key-free zero-network --check for the irreversibility ledger (R6)`.
  </action>
  <verify>
    <automated>node tests/test-356-check.cjs && node tests/test-356-ledger-build.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-check.cjs` exits 0 with the stale, unscored, removed, drift, missing-ledger and zero-network legs passing
    - `env -u TYPESAFE_API_KEY HOME=$(mktemp -d) node scripts/build-command-irreversibility-ledger.cjs --check --ledger /nonexistent/356.json; echo $?` prints a `WARN: LEDGER_MISSING` line and then 0
    - `grep -c "plugin_version" scripts/build-command-irreversibility-ledger.cjs` is 0
    - `grep -c "build-command-irreversibility-ledger" scripts/release.sh` is 0
    - `node tests/test-356-ledger-build.cjs` still exits 0
  </acceptance_criteria>
  <done>A key-free, zero-network --check reports every staleness and drift signal as a WARN and never fails the release.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-356-answer-key.cjs (integrity and the D-16 labeling order)</name>
  <files>tests/test-356-answer-key.cjs</files>
  <read_first>
    - data/jev-labels/command-irreversibility.json (header fields and a few rows)
    - scripts/irreversibility-answer-key.cjs (`parseBlindSheet`, `parseReviewSheet`, `sha256Hex`, `LABEL_SOURCES`)
    - scripts/build-command-irreversibility-ledger.cjs (`loadInputs` refusals)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-VALIDATION.md (R356-02 rows)
  </read_first>
  <action>
    Create `tests/test-356-answer-key.cjs` (inline scrub block). Run all unit legs first, then the shipped legs.
    Unit legs (temp files over the real registry command set): `loadInputs` refuses with `INPUT_REFUSED` a key missing one command (message names it and says a new command needs a label), a key with one extra command, a key without `reviewed_by`, without `reviewed_at`, a row with `label_source: 'claude'`, a row with an empty reason, an `appeal_rulings` entry for an unknown command, and one with ruling `maybe`; a well-formed key loads.
    Shipped legs over `data/jev-labels/command-irreversibility.json` and the phase-dir files:
    - set equality with the LIVE registry; on mismatch FAIL with the exact missing and extra commands and the line "a new command needs a label before the next rebuild".
    - header: `schema === 'jev-answer-key/v1'`, `labels_for === 'data/command-registry.json'`, 64-hex `registry_hash`, `reviewed_by === 'navigator'`, `reviewed_at` a YYYY-MM-DD date, non-empty `method`, `blind_sheet_ref` of the form `<path>@<40-hex sha>`, `appeal_rulings` an array.
    - rows: boolean `irreversible`, non-empty `reason`, `label_source` in LABEL_SOURCES; every blind-sheet command has `label_source === 'navigator-blind'` unless `appeal_rulings` has a `relabel` for it (then `navigator-corrected`); every review-sheet command is `claude-prelabel/navigator-confirmed` or `navigator-corrected`, consistent with the ruling in the committed review sheet.
    - seal: sha256 of 356-CLAUDE-PRELABELS.json bytes equals the blind sheet's `prelabels_sha256` and the key's `prelabels_sha256`.
    - disagreement recount: recomputed from the committed blind sheet and the sealed pre-labels (blind rows only) equals `blind_vs_claude_disagreement`.
    - D-16 git order (via `child_process.spawnSync('git', ...)`): the pre-label file's first commit is an ancestor of the blind sheet's first commit; the sha in `blind_sheet_ref` is the blind sheet's latest commit; that commit is an ancestor of the first commit of data/jev-policies/command-irreversibility.json (`git merge-base --is-ancestor`), and they differ; when data/command-irreversibility-ledger.json exists, the blind sheet commit's committer time (`git log -1 --format=%ct <sha>`) is less than `Date.parse(ledger.built_at) / 1000`, and less than `Date.parse(raw.scored_at) / 1000` when the phase dir holds 356-RAW-SCORES.json.
    - If the blind sheet is not in git at all, print `ENV GAP: blind sheet not committed` and exit 77 after the unit legs (unit-leg failures still exit 1).
    - final `NET_ATTEMPTS === 0`.
    Commit by explicit path: `test(356-10): answer-key integrity and D-16 labeling-order legs`.
  </action>
  <verify>
    <automated>node tests/test-356-answer-key.cjs && bash tests/run-all-356.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-answer-key.cjs` exits 0 (the answer key and both sheets exist after 356-08)
    - `grep -c "merge-base" tests/test-356-answer-key.cjs` is at least 1
    - `grep -c "113" tests/test-356-answer-key.cjs` is 0
    - `bash tests/run-all-356.sh` reports FAILED=0, with test-356-check and test-356-answer-key PASSED
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-356-check.cjs tests/test-356-answer-key.cjs` reports 0 for both
  </acceptance_criteria>
  <done>The answer key's coverage, provenance and the blind-first labeling order are machine-checked from files and git history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / release box (no key) -> ledger check | --check must run anywhere without a key or a network |
| git history -> labeling claims | the claim "labels were blind" rests on commit order |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-09 | Tampering | stale ledger entries | mitigate | --check prints STALE and UNSCORED; the runtime ignores the same entries (both use commandTextHash); proven with an edited temp registry |
| T-356-05 | Information disclosure | --check | mitigate | never loads the key or calls jev; spawned with a temp HOME and no key it still exits 0; zero network asserted in and out of process |
| T-356-18 | Repudiation | labeling order | mitigate | git ancestry and commit times prove seal -> blind sheet -> policy draft -> ledger; seal hash checked three ways |
| T-356-17 | Denial of service | release train | accept | --check always exits 0 (R6); it is informative, never a release blocker, and not wired into release.sh |
| T-356-SC | Tampering | package installs | accept | no packages installed |
</threat_model>

<verification>
- `node tests/test-356-check.cjs && node tests/test-356-answer-key.cjs` both exit 0
- `bash tests/run-all-356.sh` reports FAILED=0
</verification>

<success_criteria>
Staleness is visible without a key or a network, and the answer key's completeness and blind-first provenance are proven by tests before the live build.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `scripts/build-command-irreversibility-ledger.cjs` (`runCheck`, `--check`)
- `tests/test-356-check.cjs` (new)
- `tests/test-356-answer-key.cjs` (new)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-10-SUMMARY.md` when done (`git add -f`, explicit path).
</output>
