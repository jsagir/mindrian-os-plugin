---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 09
type: execute
wave: 3
depends_on: ["356-07"]
files_modified:
  - scripts/build-command-irreversibility-ledger.cjs
  - tests/test-356-ledger-build.cjs
autonomous: true
requirements: [R356-03, R356-04, R356-02]
tags: [builder, threshold, zero-miss, appeal-gate, d-14, d-15]

must_haves:
  truths:
    - "The threshold T is the minimum stored p over labeled-irreversible rows (the highest T with zero misses); every entry's flag = p >= T for every command (Jev judges, D-14); ties at T count as flagged; p is stored exactly as returned, never re-rounded (SPEC R4, D-15)"
    - "The build fails loudly and writes nothing when: a row lacks a finite p in [0,1], a label is not boolean, no row is labeled irreversible, T <= 0, or T would flag every command (D-15)"
    - "Appeal gate (D-14 final): the build fails, writes nothing, exits 4 and prints each false alarm (labeled reversible, p >= T) that lands on a chain-run command, with its p, T, label source and reason; chain-run = curated_chains endpoints resolved to registry slugs plus a declared CHAIN_SUITE_COMMANDS list in the builder; an 'accept' ruling in the answer key's appeal_rulings lets that command ship flagged"
    - "The ledger has one entry per registry command with command, p_irreversible, flag, text_hash, and top-level threshold, false_alarm_count, false_alarms, built_at, policy_hash, build_mode (plus answer_key_hash, registry_hash, text_hash_basis, jev_model, usage and cost) (SPEC R3)"
    - "The builder refuses (exit 2) an answer key whose command set differs from the registry's or that lacks reviewed_by/reviewed_at (SPEC R2 at build time); live mode with no key exits 3 and never prints the key"
    - "--raw-out records scores before thresholding; --from-raw rebuilds a ledger from recorded scores with zero network calls, refusing if the policy or any command's text changed since scoring"
  artifacts:
    - path: "scripts/build-command-irreversibility-ledger.cjs"
      provides: "computeThreshold, chainRunCommands, CHAIN_SUITE_COMMANDS, CHAIN_SUITE_FILES, appealGate, loadInputs, assembleLedger, serializeLedger, runBuild, main (live, --jev-fixture, --from-raw, --raw-out, --out, --root, --registry, --labels)"
      contains: "CHAIN_SUITE_COMMANDS"
    - path: "tests/test-356-ledger-build.cjs"
      provides: "Threshold cases, appeal gate, chain-run drift guard, CLI e2e in fixture mode, failed builds write nothing, shipped-ledger legs"
      min_lines: 200
  key_links:
    - from: "scripts/build-command-irreversibility-ledger.cjs appealGate"
      to: "data/jev-labels/command-irreversibility.json appeal_rulings"
      via: "loadInputs reads appeal_rulings; 'accept' entries exempt that command"
      pattern: "appeal_rulings"
    - from: "tests/test-356-ledger-build.cjs drift guard"
      to: "CHAIN_SUITE_COMMANDS"
      via: "parses the chain suite files at TEST time (never at build time, per D-14)"
      pattern: "CHAIN_SUITE_FILES"
---

<objective>
Finish the builder: the zero-miss threshold (D-15), the D-14 appeal gate, ledger assembly and serialization, and the CLI modes (default live, `--jev-fixture`, `--from-raw`, `--raw-out`). Prove every failure path writes nothing, in keyless fixture mode.

Purpose: SPEC R3 (the ledger file and its fields), R4 (zero misses, false alarms recorded, fail loud), R2 at build time (set equality), D-14 (Jev judges, the navigator audits, chain-run false alarms need a ruling), D-15.
Output: the completed builder (except `--check`, which 356-10 adds) and `tests/test-356-ledger-build.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-07-SUMMARY.md

Parallel-session rules (D-11): explicit-path commits, never `git add -A`. Plan 356-08 runs in the same wave and only requires this file's exports; keep every intermediate save loadable (make each edit a complete, syntactically valid change) and commit promptly. Test hygiene contract: see 356-01-PLAN.md context; spawned children use tests/fixtures/356-no-network-preload.cjs, an env without TYPESAFE_API_KEY, and a temp HOME.

Ledger shape (fields in this order; entries sorted by command; `JSON.stringify(obj, null, 2) + '\n'`):
`{ schema: 'command-irreversibility-ledger/v1', built_at, build_mode, built_from_raw, raw_scored_at, jev_model, policy_hash, answer_key_hash, registry_hash, text_hash_basis, threshold, threshold_set_by, margin, labeled_irreversible, false_alarm_count, false_alarms, chain_run_false_alarms_accepted, jev_calls, input_tokens, output_tokens, estimated_cost_usd, cost_basis: 'vendor-claimed rate, unverified', entries: [{ command, p_irreversible, flag, text_hash }] }`. `build_mode` is `'jev-live'` or `'jev-fixture'` (distinct, so a shipped fixture build is detectable); a `--from-raw` rebuild keeps the raw file's mode and sets `built_from_raw: true`. All hashes are bare 64-char lowercase hex.

Raw scores file: `{ schema: 'command-irreversibility-raw/v1', build_mode, scored_at, jev_model, policy_hash, registry_hash, entries: [{ command, p_irreversible, text_hash }], usage: { jev_calls, input_tokens, output_tokens } }`. It never contains the key.

Exit codes: 0 ok; 1 threshold failure or scoring failure; 2 refused input (answer key, registry, raw file, policy); 3 no key in live mode; 4 appeal gate.

Chain-run seed (measured at planning time by grepping the chain suites for registry slugs tagged autonomous_safe; the executor re-derives it): `/mos:find-analogies`, `/mos:find-connections` (tests/test-larry-handoff-seam.cjs), `/mos:lean-canvas` (tests/test-chain-executor-gate.cjs), `/mos:explore-domains`, `/mos:grade` (tests/test-bch-09-forced-material.cjs, tests/test-pipeline-on-runchain.cjs), `/mos:think-hats`, `/mos:scenario-plan` (tests/test-act-on-runchain.cjs), `/mos:find-bottlenecks` (tests/test-264-flagship-ralph.cjs). D-14 names the handoff seam and the chain-executor suites; the other runChain suites above are included so no chain suite can go red silently (the navigator can narrow this at the appeal).
</context>

<tasks>

<task type="auto">
  <name>Task 1: computeThreshold, chain-run set, appeal gate, input loading, assembly and the CLI modes</name>
  <files>scripts/build-command-irreversibility-ledger.cjs</files>
  <read_first>
    - scripts/build-command-irreversibility-ledger.cjs (the scoring core from 356-07)
    - scripts/irreversibility-answer-key.cjs (`LABEL_SOURCES`, `sha256Hex`, `readRegistryRows`)
    - data/command-registry.json (`curated_chains` entries: `from`/`to` strings are framework names, `framework:<name>`, or `command:/mos:<slug>`; `framework_index` maps a framework name to command slugs)
    - tests/test-larry-handoff-seam.cjs, tests/test-act-on-runchain.cjs, tests/test-bch-09-forced-material.cjs, tests/test-pipeline-on-runchain.cjs, tests/test-264-flagship-ralph.cjs, tests/test-chain-executor-gate.cjs (grep -n "/mos:" in each and read which commands each suite expects to auto-run rather than halt; confirm the role of `/mos:jtbd` in test-act-on-runchain.cjs)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 4, Pattern 6, Pitfalls 3 and 11)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-14 final, D-15)
  </read_first>
  <action>
    Extend `scripts/build-command-irreversibility-ledger.cjs` (add; do not restructure the 356-07 scoring core):
    1. `computeThreshold(rows)` with rows `[{ command, p, irreversible }]`, per D-15: throw `code = 'THRESHOLD_FAIL'` naming the command when any `irreversible` is not boolean, or any `p` is not a finite number in [0, 1]; when no row is labeled true; when `T = Math.min(...)` over labeled-true p is not greater than 0 (name the labeled-true commands that scored T); when every row has `p >= T` (degenerate: flags every command). Otherwise return `{ threshold: T, threshold_set_by: sorted labeled-true commands with p === T, false_alarms: sorted labeled-false commands with p >= T, false_alarm_count, margin: T minus the highest labeled-false p below T (null if none), labeled_irreversible: count of true rows }`. Use `>=` on the stored values; never subtract an epsilon, never re-round.
    2. `CHAIN_SUITE_FILES = Object.freeze([...])`: tests/test-larry-handoff-seam.cjs, every tests/test-chain-executor-*.cjs present, tests/test-bch-09-forced-material.cjs, tests/test-act-on-runchain.cjs, tests/test-pipeline-on-runchain.cjs, tests/test-264-flagship-ralph.cjs, tests/test-ignite-on-runchain.cjs, tests/test-201-bounded-retry.cjs, tests/test-354-chain-resume-identity.cjs (names only; the builder never reads them at build time). `CHAIN_SUITE_COMMANDS = Object.freeze([...])`: the seed list from the context, re-derived from your read of those suites (every registry command a suite expects to auto-run as autonomous_safe), each with a trailing comment naming its suite.
    3. `chainRunCommands(registry)` -> sorted unique slugs: for each `curated_chains` entry, resolve `from` and `to`: a `command:` prefix gives the slug directly; a `framework:` prefix is stripped and looked up in `framework_index`; a bare name is looked up in `framework_index`; keep only slugs present in `registry.commands`; union with `CHAIN_SUITE_COMMANDS`.
    4. `appealGate({ falseAlarms, chainRun, appealRulings, pByCommand, threshold, labelsByCommand })` -> array of `{ command, p_irreversible, threshold, label: false, label_source, reason }` for every false alarm in the chain-run set that has no `appeal_rulings` entry with `ruling === 'accept'`.
    5. `loadInputs({ registryPath, labelsPath, root })` -> `{ registry, rows (readRegistryRows), registryHash (sha256 of registry bytes), policy (readPolicy(root)), labels, labelsHash (sha256 of label file bytes), labelsByCommand }`. Refuse with `code = 'INPUT_REFUSED'` (exit 2), naming everything wrong: label command set differs from the registry's (list missing and extra; add the line "a new command needs a label before the next rebuild"); `reviewed_by` or `reviewed_at` missing; a row with a non-boolean `irreversible`, empty `reason`, or `label_source` not in LABEL_SOURCES; an `appeal_rulings` entry naming an unknown command or a ruling other than `relabel` or `accept`. A `registry_hash` in the label file that differs from the current registry is a printed WARN, not a refusal (text edits do not change what a command does; set equality is the gate).
    6. `assembleLedger({ scored, thresholdResult, inputs, mode, usage, builtAt, rawMeta, acceptedChainRun })` -> the ledger object in the context's field order; `entries[i].flag = entries[i].p_irreversible >= threshold`; `estimated_cost_usd = input_tokens * VENDOR_USD_PER_INPUT_TOKEN`; `text_hash_basis = TEXT_HASH_BASIS`. `serializeLedger(ledger)`. `writeFileAtomic(target, text)` writes a temp file in the same directory, then renames.
    7. `runBuild(opts)` with `opts = { mode: 'live' | 'fixture' | 'from-raw', root, registryPath, labelsPath, out, rawOut, rawIn, fixturePath, env }`, returning `{ exitCode, ledger?, appeal?, error? }` and never calling `process.exit` itself: load inputs; score (live: `key = client.loadKey({ env: opts.env, secretsPath: path.join(os.homedir(), '.secrets', 'typesafe.env') })`, no key -> exitCode 3 with message "no TYPESAFE_API_KEY (checked the environment and ~/.secrets/typesafe.env). Writing nothing."; fixture: `makeFixtureFetch(require(fixturePath), [])` with key `'fixture-no-key'`; from-raw: read the raw file and refuse (2) unless its `policy_hash` equals the current policy hash, its command set equals the registry's, and every `text_hash` equals the current `commandTextHash`); when `rawOut` is set and mode is not from-raw, write the raw file BEFORE thresholding; computeThreshold (THRESHOLD_FAIL -> exitCode 1, print the reason, write nothing to `out`); appealGate (non-empty -> exitCode 4, print one line per item `APPEAL: <command> p=<p> T=<T> label=false (<label_source>): <reason>` then `APPEAL: rule on each: relabel (irreversible: true) or accept (ship flagged; affected chain-test expectations updated in a follow-up)`, write nothing to `out`); otherwise assemble, write atomically to `out`, print `command-irreversibility-ledger: wrote <n> entries, T=<T>, false alarms=<k>, mode=<build_mode>, model=<jev_model>`.
    8. `main(argv)`: parse `--jev-fixture <path>`, `--from-raw <path>`, `--raw-out <path>`, `--out <path>` (default data/command-irreversibility-ledger.json), `--root <dir>` (default repo root; the policy is read from `<root>/<POLICY_REL>`), `--registry <path>` (default: env `MINDRIAN_COMMAND_REGISTRY` when set, else data/command-registry.json), `--labels <path>` (default data/jev-labels/command-irreversibility.json); no mode flag means live. Call `runBuild`, then `process.exit(result.exitCode)`. Add `if (require.main === module) main(process.argv.slice(2));` and keep exporting everything. Leave `--check` unhandled here (356-10 adds it as the first branch of `main`). The key is never printed; error messages never include it.
    Commit by explicit path: `feat(356-09): zero-miss threshold, D-14 appeal gate, ledger assembly and build modes`.
  </action>
  <verify>
    <automated>node -e "const b=require('./scripts/build-command-irreversibility-ledger.cjs');for(const k of ['computeThreshold','chainRunCommands','CHAIN_SUITE_COMMANDS','CHAIN_SUITE_FILES','appealGate','loadInputs','assembleLedger','serializeLedger','runBuild','main'])if(!(k in b))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify one-liner exits 0
    - `grep -v '^\s*//\|^\s*\*' scripts/build-command-irreversibility-ledger.cjs | grep -c "fetch(\|function assertEgressCeiling\|EGRESS_ALLOWED_"` is 0
    - `grep -v '^\s*//\|^\s*\*' scripts/build-command-irreversibility-ledger.cjs | grep -c "epsilon"` is 0, and no rounding is applied to any p value or to T (toFixed or Math.round may appear only in printed cost or summary text, never on p_irreversible or threshold; Task 2's exact-value leg proves stored p values equal the fixture values)
    - `node -e "const b=require('./scripts/build-command-irreversibility-ledger.cjs');const s=b.chainRunCommands(require('./data/command-registry.json'));process.exit(s.includes('/mos:find-bottlenecks')&&s.includes('/mos:find-analogies')?0:1)"` exits 0
    - With `T=$(mktemp -d)`: `env -u TYPESAFE_API_KEY HOME=$T node scripts/build-command-irreversibility-ledger.cjs --out $T/nokey.json; echo $?` prints 3 and `$T/nokey.json` does not exist (when 356-08 has not yet committed the answer key, exit 2 with an INPUT_REFUSED message naming the missing label file is the expected result instead)
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/build-command-irreversibility-ledger.cjs` is 0
  </acceptance_criteria>
  <done>The builder turns scores plus the answer key into a zero-miss ledger or a loud, write-nothing failure, and stops for a ruling when a false alarm hits a chain-run command.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-356-ledger-build.cjs</name>
  <files>tests/test-356-ledger-build.cjs</files>
  <read_first>
    - scripts/build-command-irreversibility-ledger.cjs (as completed in Task 1)
    - tests/fixtures/356-jev-noul-responses.json
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-VALIDATION.md (R356-03, R356-04, D-14 rows)
  </read_first>
  <action>
    Create `tests/test-356-ledger-build.cjs` (inline scrub block). Helper `makeWorld({ labelsTrue, fixtureP, appealRulings })` builds a temp root holding a D-01-shaped policy at `data/jev-policies/command-irreversibility.json`, a synthetic answer key over the REAL registry command set (all false except `labelsTrue`, `reviewed_by: 'navigator'`, `reviewed_at`, `method: 'synthetic test'`, `label_source: 'navigator-blind'`, reasons filled, `appeal_rulings`), and a temp fixture file (`default_p` plus overrides). CLI runs use `spawnSync(process.execPath, [builder, ...])` with the child-env rules; each asserts no `NETWORK_ATTEMPT_356` in stderr. Legs:
    - computeThreshold: normal case (T = min labeled-true p, correct false_alarms, sorted, margin); a labeled-false row with p exactly T is flagged and counted; all p equal -> THRESHOLD_FAIL (degenerate); a labeled-true row with p 0 -> THRESHOLD_FAIL naming it; zero labeled-true rows -> THRESHOLD_FAIL; a missing or NaN p -> THRESHOLD_FAIL; a non-boolean label -> THRESHOLD_FAIL; values like 0.41 are compared exactly (no rounding drift).
    - appealGate: a false alarm on `/mos:find-analogies` is returned; the same with an `accept` ruling is not; a false alarm on a command outside the chain-run set is not returned.
    - chainRunCommands on the real registry contains the resolved `command:` endpoint `/mos:find-bottlenecks` and every CHAIN_SUITE_COMMANDS entry.
    - drift guard (D-14, parsed at TEST time): for every file in CHAIN_SUITE_FILES that exists, every `/mos:<slug>` it mentions that is a registry command with `autonomous_safe: true` is in CHAIN_SUITE_COMMANDS; on failure print the file and slug with the fix ("add it to CHAIN_SUITE_COMMANDS").
    - CLI fixture build: labels true only for `/mos:publish`, fixture default 0.05 and `/mos:publish` 0.97, `--root <tmp> --labels <tmp> --jev-fixture <tmp fixture> --out <tmp>/ledger.json --raw-out <tmp>/raw.json` -> exit 0; one entry per registry command (count from the registry, never 113 hard-coded), each with exactly the four entry fields; every top-level field from the context present; `build_mode === 'jev-fixture'`; `threshold === 0.97`; `false_alarm_count` equals a recount from entries and labels; every `flag === (p_irreversible >= threshold)`; `policy_hash` equals sha256 of the temp policy bytes; `answer_key_hash` equals sha256 of the temp label bytes; the raw file exists without the key string.
    - failures write nothing: a fixture giving a labeled-true command p 0 -> exit 1 and no ledger file; an answer key missing one registry command -> exit 2 naming it and no ledger; a key without `reviewed_at` -> exit 2.
    - appeal gate end to end: labels true for `/mos:publish` (0.97), fixture p 0.99 for `/mos:find-analogies` labeled false -> exit 4, stdout or stderr contains `APPEAL: /mos:find-analogies`, no ledger file, raw file written; add `appeal_rulings: [{ command: '/mos:find-analogies', ruling: 'accept', ... }]` -> exit 0, the ledger lists it in `false_alarms` and `chain_run_false_alarms_accepted`, its entry has `flag: true`.
    - from-raw: rebuild from the raw file -> exit 0, identical entries, `built_from_raw: true`; the same after one byte of the temp policy changes -> exit 2; after one registry teaching changes (temp registry via `--registry`) -> exit 2.
    - live with no key: `HOME` temp, env without the key, no mode flag -> exit 3, no ledger file, output contains no `sk-`.
    - shipped-ledger legs, only when data/command-irreversibility-ledger.json exists (else print `PENDING: shipped ledger not built yet`): `build_mode === 'jev-live'`; entry set equals the committed answer key's row set; every entry has the four fields; recall 1.0 (every labeled-true row in data/jev-labels/command-irreversibility.json has `p_irreversible >= threshold` and `flag: true`); `false_alarm_count` recount matches; every flag consistent with the threshold; `policy_hash` equals sha256 of data/jev-policies/command-irreversibility.json bytes; `answer_key_hash` equals sha256 of the answer key bytes.
    - final `NET_ATTEMPTS === 0`.
    Commit by explicit path: `test(356-09): threshold, appeal gate and build-mode legs`.
  </action>
  <verify>
    <automated>node tests/test-356-ledger-build.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-ledger-build.cjs` exits 0 with the shipped legs printing PENDING
    - `grep -c "113" tests/test-356-ledger-build.cjs` is 0 (the command count is always read from the registry)
    - `grep -c "CHAIN_SUITE_FILES" tests/test-356-ledger-build.cjs` is at least 1
    - `bash tests/run-all-356.sh` reports FAILED=0
    - `git status --short -- data/command-irreversibility-ledger.json` prints nothing (tests never write into data/)
  </acceptance_criteria>
  <done>Every threshold rule, the appeal gate, each build mode and every write-nothing failure is proven keyless in fixture mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| answer key -> threshold | labels set the threshold; the threshold decides which commands halt in users' chains |
| builder output -> shipped data | whatever lands in data/command-irreversibility-ledger.json runs on every user machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-16 | Tampering | computeThreshold | mitigate | T = min labeled-true p guarantees zero misses; degenerate and missing-score cases throw and write nothing; exact comparisons, no rounding; recount legs |
| T-356-17 | Denial of service | Larry's chains (false alarms) | mitigate | appeal gate fails the build on any chain-run false alarm and prints it for a navigator ruling; a test-time drift guard keeps CHAIN_SUITE_COMMANDS complete |
| T-356-18 | Repudiation | appeal rulings | mitigate | an accept is data in the answer key's appeal_rulings, reflected in the ledger's chain_run_false_alarms_accepted and in answer_key_hash |
| T-356-09 | Tampering | --from-raw | mitigate | refuses when the policy hash, the command set or any text_hash changed since scoring |
| T-356-05 | Information disclosure | live mode | mitigate | no-key path exits 3 without printing; the raw file and ledger never include the key (legs check for 'sk-') |
| T-356-SC | Tampering | package installs | accept | no packages installed |
</threat_model>

<verification>
- `node tests/test-356-ledger-build.cjs` exits 0
- `bash tests/run-all-356.sh` reports FAILED=0
</verification>

<success_criteria>
The builder is complete apart from --check: zero-miss thresholding, the D-14 appeal gate, atomic writes, raw-score capture and replay, and every failure path proven to write nothing.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `scripts/build-command-irreversibility-ledger.cjs` (threshold, appeal gate, assembly, CLI modes)
- `tests/test-356-ledger-build.cjs` (new)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-09-SUMMARY.md` when done (`git add -f`, explicit path). Record the final CHAIN_SUITE_COMMANDS list with the suite behind each entry, and the chainRunCommands size on the real registry.
</output>
