---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 06
type: execute
wave: 2
depends_on: ["356-04", "356-05"]
files_modified:
  - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md
  - data/jev-policies/command-irreversibility.json
  - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md
autonomous: false
requirements: [R356-02, R356-01]
tags: [human-checkpoint, sitting-1, blind-labels, policy-draft, d-16]

must_haves:
  truths:
    - "The navigator labels the registry-picked risk subset blind, BEFORE seeing any policy draft, any Claude pre-label, or any Jev output (D-04, D-16)"
    - "The filled blind sheet is committed by explicit path before the policy draft file is first committed, so git order proves the labels were blind"
    - "The policy draft exists at data/jev-policies/command-irreversibility.json with policy_id, version, instructions, criteria as a {true, false} object, and boundary_cases naming every export and share command in the registry (D-01 amended, D-02), including the D-03 sentence that irreversible is narrower than material, and no em-dash"
    - "The review sheet reveals Claude's sealed pre-labels only for the remainder, and only after the blind sheet is committed"
  artifacts:
    - path: ".planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md"
      provides: "The navigator's blind labels on the D-04 risk subset, with the pre-label seal in its header"
      contains: "prelabels_sha256:"
    - path: "data/jev-policies/command-irreversibility.json"
      provides: "Draft irreversibility policy (locked by the navigator in 356-08)"
      contains: "\"criteria\""
    - path: ".planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md"
      provides: "Claude's pre-labels for the remainder, laid out for row-by-row review in sitting 2"
      contains: "blind_sheet_sha256:"
  key_links:
    - from: "356-BLIND-LABEL-SHEET.md"
      to: "356-CLAUDE-PRELABELS.json"
      via: "prelabels_sha256 header line equals sha256 of the sealed file"
      pattern: "prelabels_sha256"
---

<objective>
SITTING 1 of 3 with the navigator: the blind labels (Checkpoint A). Generate the blind sheet from the registry and the sealed pre-labels, have the navigator fill it without seeing anything Claude wrote, commit it, and only then draft the irreversibility policy and lay out the pre-label review sheet for sitting 2.

The ordering is the point (D-16): the policy's boundary cases are effectively Claude's labels for the riskiest commands, so the navigator must not see them before the blind sheet is in git.

Purpose: SPEC R2 (human ground truth), R1 (policy text drafted, D-02), D-04, D-16.
Output: the committed blind sheet, the committed policy draft, the committed review sheet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-04-SUMMARY.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-05-SUMMARY.md

Order inside this plan is mandatory: generate sheet -> commit blank sheet -> navigator fills -> commit filled sheet -> ONLY THEN draft the policy and the review sheet. Until the filled sheet is committed, do not create or print `data/jev-policies/command-irreversibility.json`, do not print any pre-label, and do not open 356-CLAUDE-PRELABELS.json except to hash it. Parallel plan 356-07 (same wave) edits the scoring builder and the client; it never creates the policy file. Explicit-path commits only, `git add -f` for `.planning/` files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit the blank blind sheet</name>
  <files>.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md</files>
  <read_first>
    - scripts/irreversibility-answer-key.cjs (the `--label-sheet` mode)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-04-SUMMARY.md (the recorded seal digest)
  </read_first>
  <action>
    1. Guards: `git ls-files --error-unmatch` on 356-CLAUDE-PRELABELS.json exits 0 (sealed and committed); its current `sha256sum` equals the digest in 356-04-SUMMARY.md; `test ! -e data/jev-policies/command-irreversibility.json`; `test ! -e data/command-irreversibility-ledger.json`. If any guard fails, stop and report; do not improvise around it.
    2. Run `node scripts/irreversibility-answer-key.cjs --label-sheet .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md --prelabels .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json`. Record in the SUMMARY only the row count it prints.
    3. Run `node tests/test-356-label-sheet.cjs` (its shipped legs now check the blank sheet's hygiene and seal).
    4. Commit the blank sheet by explicit path: `git add -f <sheet>`; message `docs(356-06): blank blind irreversibility label sheet (D-04 subset, seal in header)`.
  </action>
  <verify>
    <automated>node tests/test-356-label-sheet.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-label-sheet.cjs` exits 0 and its blind-sheet shipped leg is no longer PENDING
    - `grep -c "autonomous_safe" .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md` is 0 and `grep -ciE "boundary[_ -]*cases?" .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md` is 0 (narrowed from bare "boundary" 2026-09-23, navigator-approved: the registry's own Canon Part 8 wording is command text, not the policy's `boundary_cases` field)
    - `test ! -e data/jev-policies/command-irreversibility.json`
    - The sheet is tracked: `git ls-files --error-unmatch .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md` exits 0
  </acceptance_criteria>
  <done>A clean blind sheet, committed, carrying the seal digest and nothing Claude concluded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CHECKPOINT A (sitting 1) - the navigator labels the risk subset blind</name>
  <files>.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md</files>
  <read_first>
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md (to state its path and row count to the navigator; do not summarize its contents)
  </read_first>
  <action>Present the checkpoint below to the navigator. Show only the sheet path, the row count, the rubric sentence already in the sheet, and the instructions. Show no pre-label, no policy text and no autonomous_safe tag.</action>
  <what-built>A blind label sheet: one row per command in the registry-picked risk subset (D-04), with the command's slug, its registry teaching line and its job summary, and two blank cells. It hides the autonomous_safe tag, Claude's labels and any policy text on purpose, so your labels stay independent.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md`.
    2. Until this sheet is committed, please do not open `356-CLAUDE-PRELABELS.json`, anything under `data/jev-policies/`, or the 356 plan files: they contain Claude's answers.
    3. For each row, fill `irreversible (y/n)` with y or n, and `reason` with one line. Judge what running the command actually does. You may open `commands/<name>.md` to check. The rubric at the top of the sheet is the only definition in play: an effect outside the machine or outside your control that the room's own history cannot undo is irreversible; local room writes are not.
    4. Either save your edits in the file, or reply in chat with one line per row in the form `/mos:<name>: y - <reason>` (or `n`). Your words are transcribed exactly as written.
    Expected time: about 20 to 30 minutes for roughly 66 rows.
  </how-to-verify>
  <resume-signal>Type "labeled" after saving the sheet, or paste your per-row labels</resume-signal>
  <acceptance_criteria>
    - The navigator's resume signal is recorded verbatim in the SUMMARY (either the word "labeled" or the pasted labels)
  </acceptance_criteria>
  <verify>
    <automated>git ls-files --error-unmatch .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md</automated>
  </verify>
  <done>The navigator has labeled every row of the blind sheet.</done>
</task>

<task type="auto">
  <name>Task 3: Transcribe and commit the blind labels, then draft the policy and the review sheet</name>
  <files>.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md, data/jev-policies/command-irreversibility.json, .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md</files>
  <read_first>
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md (as saved by the navigator)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md (Requirement 1)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-01 amended, D-02, D-03, "Specific Ideas")
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 3: how instructions and criteria are sent)
    - data/command-registry.json (via node: every command whose slug or teaching/jtbd_summary matches `/export|share|snapshot|vault|present|publish|deploy|send|email|upload/i`, with its teaching line)
    - commands/export.md, commands/snapshot.md, commands/vault.md, commands/present.md, commands/publish.md (grep -n -i for deploy, publish, upload, send, vercel, and read the hit lines with context)
  </read_first>
  <action>
    1. Transcribe: if the navigator pasted labels, write each into its row's two cells exactly as given (no paraphrase; `yes`/`no` kept as written). Parse the result with `parseBlindSheet` from scripts/irreversibility-answer-key.cjs and require every row to have a boolean and a non-empty reason; if any row is incomplete, list those rows back to the navigator and wait (this is still Checkpoint A). Append the footer lines `filled_by: navigator` and `filled_at: <YYYY-MM-DD>`. The header lines (registry_hash, prelabels_sha256, rubric) must be byte-unchanged.
    2. Confirm that no Jev call has happened: `test ! -e data/command-irreversibility-ledger.json` and no `356-RAW-SCORES.json` in the phase dir. Commit the filled sheet by explicit path (`git add -f`): `docs(356-06): navigator blind irreversibility labels (committed before any policy draft or Jev call, D-16)`. Record the commit sha and its committer timestamp (`git log -1 --format='%H %cI'`) in the SUMMARY.
    3. Only now draft `data/jev-policies/command-irreversibility.json` (per D-01 amended and D-02), pretty JSON (2 spaces) with a trailing newline, plain language, hyphens only, and no reference to agents/larry-extended.md (D-03 independence):
       - `policy_id`: "command-irreversibility"; `version`: 1.
       - `instructions`: one string that states the SPEC R1 definition verbatim in meaning (running the command takes an effect outside the machine or outside the user's control that the room's own history cannot undo: send, email, publish, deploy, share or upload to a third party, external write); says local room writes and local file exports are not irreversible; says to judge what running the command itself does, not advice it gives the user to do later; and contains the D-03 sentence exactly: "Irreversible is narrower than material: every irreversible step is material, but not every material step is irreversible."
       - `criteria`: the Noul-native OBJECT (never an array): `{ "true": "<running this command itself sends, emails, publishes, deploys, shares or uploads to a third party, or writes to a system outside this machine, so the room's own history cannot undo it>", "false": "<running this command only reads, analyzes, or writes inside the user's room or local files, including local exports, or only advises the user to do something external later>" }`. Keep instructions and criteria saying the same thing (mismatched wording confuses the model).
       - `boundary_cases`: an array of strings, one per export or share style command found in the registry scan above, each naming the slug, what running it does, and the verdict with its reason. It must include `/mos:export`, `/mos:snapshot`, `/mos:vault`, `/mos:present` as reversible (they write locally, even when their descriptions suggest deploying or sending later) and `/mos:publish` as irreversible (it deploys to a public third-party URL), plus two general cases: writing or deleting files inside the room is reversible; advice to deploy or send later is not an effect of running the command. Ground every case on the command bodies read above.
       Validate with node: parses; the five fields exist; `criteria` is a plain object with string `true` and `false`; `boundary_cases` is a non-empty array of strings mentioning all five named slugs; the file contains no em-dash and no `larry-extended`; the file is at most 8000 characters (the egress cap in 356-07).
    4. Generate the review sheet: `node scripts/irreversibility-answer-key.cjs --review-sheet .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md --blind .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md --prelabels .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json`.
    5. Commit the draft policy and the review sheet together by explicit path: `git add data/jev-policies/command-irreversibility.json` and `git add -f <review sheet>`; message `docs(356-06): draft irreversibility policy and pre-label review sheet (pending navigator lock in 356-08)`.
    6. Run `node tests/test-356-label-sheet.cjs` and `node tests/test-356-larry-contract.cjs` (its D-03 leg now reads the draft). Tell the navigator at the end of this sitting: sitting 2 is the policy lock plus the row-by-row pre-label review (about 15 to 20 minutes).
  </action>
  <verify>
    <automated>node tests/test-356-label-sheet.cjs && node tests/test-356-larry-contract.cjs && node -e "const p=JSON.parse(require('fs').readFileSync('data/jev-policies/command-irreversibility.json','utf8'));const t=require('fs').readFileSync('data/jev-policies/command-irreversibility.json','utf8');const ok=p.policy_id==='command-irreversibility'&&p.criteria&&!Array.isArray(p.criteria)&&typeof p.criteria.true==='string'&&typeof p.criteria.false==='string'&&Array.isArray(p.boundary_cases)&&['/mos:export','/mos:snapshot','/mos:vault','/mos:present','/mos:publish'].every(s=>p.boundary_cases.some(b=>b.indexOf(s)!==-1))&&/irreversible is narrower than material/i.test(p.instructions)&&t.indexOf('\u2014')===-1&&t.indexOf('larry-extended')===-1&&t.length<=8000;process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Every blind-sheet row parses to a boolean with a non-empty reason; the footer carries `filled_by: navigator`
    - Git order (D-16): `git log --format=%ct -1 -- .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md` is less than or equal to `git log --diff-filter=A --format=%ct -- data/jev-policies/command-irreversibility.json`, and the sheet's latest commit is a strict ancestor of the policy's first commit (`git merge-base --is-ancestor <sheet sha> <policy first sha>` exits 0 and the shas differ)
    - The policy verify one-liner exits 0
    - `node tests/test-356-label-sheet.cjs` exits 0 with the review-sheet shipped leg no longer PENDING
    - `test ! -e data/command-irreversibility-ledger.json` (no Jev call yet)
  </acceptance_criteria>
  <done>The blind labels are in git first; the draft policy and the pre-label review sheet follow, ready for sitting 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude's outputs -> navigator's blind judgment | contamination here would bias the answer key and hide LLM bias |
| navigator's chat reply -> sheet file | transcription must not alter the navigator's labels or words |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-13 | Information disclosure | blind labeling order | mitigate | guards refuse to start if a policy file or ledger already exists; the checkpoint shows only path, count and rubric; git ancestry proves the sheet commit precedes the policy's first commit |
| T-356-12 | Tampering | pre-label seal | mitigate | the seal digest is re-hashed before the sheet is generated and is carried in the sheet header; the review sheet refuses on mismatch |
| T-356-18 | Repudiation | transcription | mitigate | verbatim transcription, the resume signal recorded in the SUMMARY, footer filled_by and filled_at, the commit sha and timestamp recorded |
| T-356-14 | Information disclosure | policy text (crosses to Jev later) | mitigate | plugin-authored generic text only; no room content, no names, no larry-extended quotes; 8000-character cap checked here and enforced by the egress guard |
| T-356-SC | Tampering | package installs | accept | no packages installed |
</threat_model>

<verification>
- `node tests/test-356-label-sheet.cjs` exits 0 with all shipped legs live
- The policy verify one-liner in Task 3 exits 0
- The blind sheet's latest commit is an ancestor of the policy file's first commit
</verification>

<success_criteria>
The navigator's blind labels exist in git before any Claude answer was visible to him, and the policy draft plus the review sheet are ready for sitting 2.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `.planning/phases/356-.../356-BLIND-LABEL-SHEET.md` (new; blank then filled, two commits)
- `data/jev-policies/command-irreversibility.json` (new, draft; locked in 356-08)
- `.planning/phases/356-.../356-PRELABEL-REVIEW-SHEET.md` (new)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-06-SUMMARY.md` when done (`git add -f`, explicit path). Record: subset row count, the navigator's resume signal verbatim, the filled-sheet commit sha and timestamp, the policy-draft commit sha, and the review-sheet row count.
</output>
