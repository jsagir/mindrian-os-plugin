---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/irreversibility-answer-key.cjs
  - tests/test-356-label-sheet.cjs
autonomous: true
requirements: [R356-02]
tags: [answer-key, labeling, d-04, d-05, d-16]

must_haves:
  truths:
    - "A zero-network dev-time script picks the D-04 blind subset from the registry alone: every command whose slug matches an IRREVERSIBLE_HINTS keyword, OR is not autonomous_safe, OR has send/share/export/upload/publish/deploy in teaching or jtbd_summary, OR that Claude's sealed pre-label calls irreversible"
    - "The blind sheet shows only slug, teaching, jtbd_summary, the SPEC R1 definition, and blank label cells: no autonomous_safe tag, no policy boundary cases, no Claude label, no picking reason (D-16, RESEARCH Pitfall 4 and A3)"
    - "The blind sheet header carries prelabels_sha256, a commitment to the sealed pre-label file, so the reveal can be proven unchanged"
    - "The review sheet reveals Claude's pre-labels only for the remainder (registry minus blind subset), and the merge produces the D-05 answer key with a label_source per row and the blind-vs-Claude disagreement rate computed on the blind subset only"
    - "The script refuses (exit 2, naming the rows) on unfilled blind rows, unresolved review rows, a pre-label seal mismatch, or a command-set mismatch with the registry"
  artifacts:
    - path: "scripts/irreversibility-answer-key.cjs"
      provides: "--label-sheet, --review-sheet, --merge modes plus pure exports: sha256Hex, readRegistryRows, pickBlindSubset, renderBlindSheet, parseBlindSheet, renderReviewSheet, parseReviewSheet, mergeAnswerKey, R1_DEFINITION, LABEL_SOURCES"
      min_lines: 200
    - path: "tests/test-356-label-sheet.cjs"
      provides: "Unit legs for picking, rendering hygiene, parsing, merging; shipped legs for the committed sheets and the seal"
      min_lines: 150
  key_links:
    - from: "scripts/irreversibility-answer-key.cjs pickBlindSubset"
      to: "lib/core/chain-executor.cjs IRREVERSIBLE_HINTS"
      via: "lazy require of the exported frozen keyword list (one source of truth)"
      pattern: "IRREVERSIBLE_HINTS"
    - from: "scripts/irreversibility-answer-key.cjs mergeAnswerKey"
      to: "data/jev-labels/command-irreversibility.json"
      via: "--merge --out"
      pattern: "jev-answer-key/v1"
---

<objective>
Build the zero-network labeling tooling the human checkpoints run: `scripts/irreversibility-answer-key.cjs` with three CLI modes (`--label-sheet`, `--review-sheet`, `--merge`) over pure, exported functions, plus `tests/test-356-label-sheet.cjs`.

Kept separate from the scoring builder on purpose: it never imports the Jev client, it can run while the builder is still being written (no shared file across waves), and it is the "script that reads only the registry" D-04 describes.

Purpose: SPEC R2 (answer key covering exactly the registry, reviewer and date recorded), D-04 (hybrid subset rule), D-05 (label file shape, label_source, disagreement rate), D-16 (blind before any reveal).
Output: the script and its test. It does not generate the real sheet (356-06 does, after the seal from 356-04 exists).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md

Parallel-session rules (D-11): explicit-path commits, never `git add -A`. Test hygiene contract: see 356-01-PLAN.md context (inline scrub block, NET_ATTEMPTS === 0; spawned children get an env without TYPESAFE_API_KEY and a temp HOME; add NODE_OPTIONS with the preload only when tests/fixtures/356-no-network-preload.cjs exists, since 356-01 lands it in the same wave). The script has no network code at all.

File formats this plan fixes (the checkpoints and 356-10's test rely on them):
- Blind sheet (markdown): title `# Phase 356 blind irreversibility labels (navigator)`; header lines `registry_hash: <64 hex>`, `prelabels_sha256: <64 hex>`, `rubric: <R1_DEFINITION>`; an instructions paragraph; one table `| # | command | teaching | jtbd_summary | irreversible (y/n) | reason |` with the last two cells blank; rows alphabetical by command; `|` inside text escaped as `\|`; newlines in text replaced by a space; after the navigator fills it, footer lines `filled_by: navigator` and `filled_at: <YYYY-MM-DD>` are appended by the executor.
- Review sheet (markdown): title `# Phase 356 pre-label review (navigator)`; header lines `registry_hash`, `prelabels_sha256`, `blind_sheet_sha256`; table `| # | command | teaching | jtbd_summary | claude label | claude reason | your ruling (ok/y/n) | your reason |`, remainder rows only.
- Answer key (JSON, D-05 plus additive fields): `{ schema: 'jev-answer-key/v1', labels_for: 'data/command-registry.json', registry_hash, reviewed_by: 'navigator', reviewed_at: 'YYYY-MM-DD', method, blind_subset_size, blind_sheet_ref, prelabels_sha256, blind_vs_claude_disagreement: { compared, disagreed, rate, commands }, appeal_rulings: [], rows: [{ command, irreversible, reason, label_source }] }`, rows sorted by command, `label_source` one of `navigator-blind`, `claude-prelabel/navigator-confirmed`, `navigator-corrected`. `reviewed_by` is the role handle `navigator`, never a person's name (data/ ships in the npm tarball; no real names in tracked files).
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/irreversibility-answer-key.cjs (pure functions and three CLI modes)</name>
  <files>scripts/irreversibility-answer-key.cjs</files>
  <read_first>
    - scripts/build-section-command-ledger.cjs (header-comment style and the argv switch-case dispatch in `main()`; read only those parts)
    - lib/core/chain-executor.cjs (`grep -n "IRREVERSIBLE_HINTS" lib/core/chain-executor.cjs`: confirm it is a frozen, exported array)
    - data/command-registry.json (shape of one `commands` entry; `node -e` print one entry)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-04, D-05, D-16)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 5)
  </read_first>
  <action>
    Create `scripts/irreversibility-answer-key.cjs`: `'use strict'`; requires only `node:fs`, `node:path`, `node:crypto`, plus a lazy `require(path.join(ROOT, 'lib', 'core', 'chain-executor.cjs')).IRREVERSIBLE_HINTS` inside the picking path (a scripts/ file may require lib/; lib/ may never require this file). Header comment (hyphens only): Phase 356 dev-time labeling tooling, zero network, never required from lib/ or hooks/, the order it enforces (seal, blind sheet, commit, reveal, merge; D-16), exit codes 0 ok, 1 unexpected, 2 refused input.
    Constants: `ROOT`; `DEFAULT_REGISTRY_PATH` (env `MINDRIAN_COMMAND_REGISTRY` when non-empty, else data/command-registry.json); `EXTERNAL_VERB_RE = /\b(send|share|export|upload|publish|deploy)/i`; `R1_DEFINITION` = the SPEC R1 definition as one plain sentence ("Irreversible means running the command takes an effect outside the machine or outside the user's control that the room's own history cannot undo: send, email, publish, deploy, share or upload to a third party, or an external write. Local room writes are not irreversible."); `LABEL_SOURCES = Object.freeze(['navigator-blind', 'claude-prelabel/navigator-confirmed', 'navigator-corrected'])`.
    Pure exported functions:
    - `sha256Hex(bufOrString)`.
    - `readRegistryRows(registryPath)` -> rows sorted by command `{ command, teaching, jtbd_summary, autonomous_safe }`, non-string teaching/jtbd_summary normalized to '' (same rule as commandTextHash in 356-03), `autonomous_safe` strictly `=== true`.
    - `pickBlindSubset(rows, prelabelRows, hints)` -> sorted `[{ command, why }]`, where `why` lists the rules that fired (`keyword`, `not_autonomous_safe`, `external_verb`, `claude_prelabel_true`); `hints` defaults to the chain-executor list; the keyword rule is a lowercase substring match on the slug.
    - `renderBlindSheet(subsetRows, { registryHash, prelabelsSha256 })` -> markdown per the context format. Never render `autonomous_safe`, `why`, a Claude label, or the word "boundary".
    - `parseTable(markdown)` -> header meta (`key: value` lines before the table) and table rows as arrays of unescaped, trimmed cells (split on `|` not preceded by a backslash; skip the header and the `---` separator rows).
    - `parseBlindSheet(markdown)` -> `{ meta, rows: [{ command, irreversible, reason }] }`; `y`, `yes`, `true` -> true; `n`, `no`, `false` -> false (case-insensitive); anything else, including blank -> null.
    - `renderReviewSheet(remainderRows, prelabelByCommand, { registryHash, prelabelsSha256, blindSheetSha256 })` -> markdown per the context format; `claude label` rendered `y` or `n`.
    - `parseReviewSheet(markdown)` -> `{ meta, rows: [{ command, claude_irreversible, claude_reason, ruling, navigator_reason }] }`; `ruling` is `ok`, `y`, `n`, or null.
    - `mergeAnswerKey({ registryRows, registryHash, blind, prelabels, review, prelabelsSha256, reviewedAt, blindSheetRef })` -> the answer key object. Blind rows -> `navigator-blind` with the navigator's reason. Review rows: `ok` -> `claude-prelabel/navigator-confirmed` with Claude's label and reason; `y` or `n` equal to Claude's label -> `claude-prelabel/navigator-confirmed`; `y` or `n` different from Claude's -> `navigator-corrected` with the navigator's label and reason (a correction requires a non-empty navigator reason). `blind_vs_claude_disagreement` compares the navigator's blind label with Claude's pre-label for blind-subset rows ONLY: `{ compared, disagreed, rate: disagreed / compared rounded to 4 decimals, commands: sorted disagreeing slugs }`. `method` = "hybrid (D-04): the navigator labeled the registry-picked risk subset blind, before seeing any policy draft or Claude label; the navigator then reviewed Claude's pre-labels row by row for the remainder". Throw an `Error` with `code = 'ANSWER_KEY_REFUSED'` naming the rows when: a blind row is null or has an empty reason; a review row has a null ruling; blind commands and review commands overlap or together differ from the registry command set (list missing and extra); the prelabels file's sha256 differs from the blind sheet's `prelabels_sha256`.
    CLI in `main(argv)` (switch-case on the first mode flag; `if (require.main === module) process.exitCode = main(process.argv.slice(2));` and always export):
    - `--label-sheet <out> --prelabels <path> [--registry <path>]`: refuse (exit 2) when the prelabels file is missing or its command set differs from the registry's; write the sheet; print `label-sheet: wrote <n> rows to <out>` (never print `why` or labels).
    - `--review-sheet <out> --blind <sheet> --prelabels <path> [--registry <path>]`: refuse when any blind row is unfilled, or the seal mismatches; remainder = registry commands not in the blind sheet; write the sheet; print the row count only.
    - `--merge --blind <sheet> --review <sheet> --prelabels <path> --out <json> --blind-sheet-ref <string> [--reviewed-at <YYYY-MM-DD>] [--registry <path>]`: build via `mergeAnswerKey`, write pretty JSON with a trailing newline, print `merge: <rows> rows, blind <k>, disagreement <disagreed>/<compared>`; on `ANSWER_KEY_REFUSED` print the message and exit 2.
    Commit by explicit path: `feat(356-05): zero-network irreversibility answer-key tooling (D-04, D-05, D-16)`.
  </action>
  <verify>
    <automated>node -e "const m=require('./scripts/irreversibility-answer-key.cjs');for(const k of ['sha256Hex','readRegistryRows','pickBlindSubset','renderBlindSheet','parseBlindSheet','renderReviewSheet','parseReviewSheet','mergeAnswerKey','R1_DEFINITION','LABEL_SOURCES'])if(!(k in m))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify one-liner exits 0
    - `grep -v '^\s*//\|^\s*\*' scripts/irreversibility-answer-key.cjs | grep -ciE "fetch|https?:|typesafe|jev-devtime-client"` is 0
    - `grep -c "autonomous_safe" scripts/irreversibility-answer-key.cjs` is at least 1 (read for picking) and the rendered blind sheet never contains it (proven in Task 2)
    - `node scripts/irreversibility-answer-key.cjs --label-sheet /tmp/x.md --prelabels /nonexistent.json; echo $?` prints 2
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/irreversibility-answer-key.cjs` is 0
  </acceptance_criteria>
  <done>The labeling tooling picks, renders, parses and merges with refusals that name the offending rows, and has no network path.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-356-label-sheet.cjs</name>
  <files>tests/test-356-label-sheet.cjs</files>
  <read_first>
    - scripts/irreversibility-answer-key.cjs (as written in Task 1)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-VALIDATION.md (R356-02 row)
  </read_first>
  <action>
    Create `tests/test-356-label-sheet.cjs` with the inline scrub block. Unit legs (synthetic rows, no repo data files):
    - pickBlindSubset: each of the four rules alone includes a command; a safe-tagged command with no keyword, no verb and a false pre-label is excluded; `why` lists every rule that fired; output is sorted.
    - renderBlindSheet hygiene: the markdown contains `R1_DEFINITION`, a `prelabels_sha256:` line with the given hex, one table row per subset command; contains no `autonomous_safe`, no case-insensitive `boundary`, no `why`; a teaching containing `|` and a newline renders escaped and on one line.
    - round trip: fill a rendered sheet programmatically (mixed `y`, `Yes`, `n`, `NO`), `parseBlindSheet` returns the booleans and reasons; a blank cell parses as null.
    - review sheet: `renderReviewSheet` includes only the remainder, shows Claude's label as y/n; `parseReviewSheet` reads `ok`, `y`, `n` and null.
    - mergeAnswerKey: label_source assignment for blind, confirmed (`ok`), confirmed (same letter), corrected (different letter); the correction keeps the navigator's reason; disagreement computed on blind rows only (a synthetic case with 2 of 5 blind rows disagreeing gives `rate` 0.4 and the two slugs); every `ANSWER_KEY_REFUSED` case (null blind row, empty blind reason, null review ruling, overlap, missing registry command, extra command, seal mismatch, correction without a reason) throws with `code === 'ANSWER_KEY_REFUSED'` and names the row; the output has `reviewed_by === 'navigator'`, `schema === 'jev-answer-key/v1'`, `appeal_rulings` an empty array, and rows sorted.
    - CLI: spawn the three modes against temp files built from a small synthetic registry passed with `--registry` (child env per the hygiene contract); refused inputs exit 2; successful runs exit 0 and print no label.
    Shipped legs, each only when its file exists (else print `PENDING: <file>` and pass):
    - with the real registry and the committed `356-CLAUDE-PRELABELS.json`: the picked subset contains every command whose registry `autonomous_safe` is not true and contains `/mos:publish`; its size is at least the count of non-autonomous_safe commands (computed, never hard-coded).
    - with the committed `356-BLIND-LABEL-SHEET.md`: its table rows equal the picked subset exactly; its `prelabels_sha256` equals `sha256Hex` of the pre-label file bytes (seal intact); it contains no `autonomous_safe` and no case-insensitive `boundary`.
    - with the committed `356-PRELABEL-REVIEW-SHEET.md`: its rows equal registry minus the blind subset; its `blind_sheet_sha256` equals the blind sheet's hash.
    Final `NET_ATTEMPTS === 0`. Commit by explicit path: `test(356-05): answer-key tooling legs`.
  </action>
  <verify>
    <automated>node tests/test-356-label-sheet.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-label-sheet.cjs` exits 0, with the shipped legs printing `PENDING` at this point (no sheet exists yet)
    - `grep -c "ANSWER_KEY_REFUSED" tests/test-356-label-sheet.cjs` is at least 1
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-356-label-sheet.cjs` is 0
    - `git log -1 --format=%s -- tests/test-356-label-sheet.cjs` starts with `test(356-05)`
  </acceptance_criteria>
  <done>The labeling tooling is proven to keep the blind sheet clean, parse what the navigator writes, and refuse anything incomplete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude's labels -> blind sheet | the blind sheet must not carry Claude's labels, the audited tag, or policy boundary cases |
| navigator's answers -> answer key | transcription and merge must preserve the navigator's words and labels exactly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-13 | Information disclosure | renderBlindSheet | mitigate | no autonomous_safe, why, Claude label or boundary text is rendered; unit and shipped legs grep for them |
| T-356-12 | Tampering | pre-label reveal | mitigate | review-sheet and merge refuse when sha256 of the pre-label file differs from the blind sheet's prelabels_sha256 |
| T-356-18 | Spoofing | answer key provenance | mitigate | label_source per row; corrections require the navigator's own reason; reviewed_by is the role handle; disagreement computed only where both labels exist independently |
| T-356-19 | Information disclosure | data/jev-labels (ships in npm) | mitigate | reviewed_by fixed to 'navigator'; no person names |
| T-356-SC | Tampering | package installs | accept | no packages installed |
</threat_model>

<verification>
- `node tests/test-356-label-sheet.cjs` exits 0
- `node scripts/irreversibility-answer-key.cjs --label-sheet /tmp/x.md --prelabels /nonexistent.json` exits 2
</verification>

<success_criteria>
A zero-network tool exists that turns the sealed pre-labels and the registry into a clean blind sheet, a remainder review sheet, and a D-05 answer key, refusing loudly on any gap.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `scripts/irreversibility-answer-key.cjs` (new)
- `tests/test-356-label-sheet.cjs` (new)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-05-SUMMARY.md` when done (`git add -f`, explicit path).
</output>
