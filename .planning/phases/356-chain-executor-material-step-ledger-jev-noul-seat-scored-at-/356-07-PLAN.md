---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 07
type: execute
wave: 2
depends_on: ["356-02", "356-03", "356-05"]
files_modified:
  - scripts/jev-devtime-client.cjs
  - tests/test-356-egress.cjs
  - scripts/build-command-irreversibility-ledger.cjs
  - tests/fixtures/356-jev-noul-responses.json
  - tests/test-356-policy.cjs
autonomous: true
requirements: [R356-07, R356-01, R356-03, R356-08]
tags: [jev, noul, egress-guard, part8, builder, fixture]

must_haves:
  truths:
    - "EGRESS_PROFILES.material_step_ledger allows exactly top keys {model, state, questions}, model 'jev-latest', state keys {slug, teaching, jtbd_summary, policy} (all strings), one Noul question 'irreversible' with keys {type, instructions, criteria}, instructions keys {question, rule, boundary_cases}, criteria keys {true, false}; per-key caps via max_len_by_key; and must_equal_file {policy: 'data/jev-policies/command-irreversibility.json'} so state.policy must be byte-identical to the policy file (D-08, orchestrator addendum)"
    - "Every string in the question except the fixed question sentence must be a string value of the policy file itself, so no text other than the registry blurb and the policy can cross to Jev (SPEC R7)"
    - "Any disallowed key, over-cap value, policy mismatch or foreign question text throws EGRESS_REFUSED naming the key before fetchImpl is ever called; a test that injects room_path into state proves the builder throws with zero fetches (R7 acceptance)"
    - "The scoring core sends one Noul per registry command through the shared client (no local fetch wrapper or guard, R8), carrying the policy text byte-identical to the file in state.policy and its instructions, boundary cases and criteria verbatim in the question (R1 acceptance, proven by a captured-body fixture test)"
    - "The fixture path drives the real payload, guard and response parsing through a fake fetchImpl, unlike 353's fixture shortcut (RESEARCH Pitfall 9)"
    - "No key material appears in data/ or in any scored output (sentinel key test, plus the real key compared silently when loadable)"
  artifacts:
    - path: "scripts/jev-devtime-client.cjs"
      provides: "EGRESS_PROFILES.material_step_ledger and the exact_state_v1 optional fields string_keys, question_max_len, question_strings_from_file_key"
      contains: "material_step_ledger"
    - path: "scripts/build-command-irreversibility-ledger.cjs"
      provides: "Scoring core: POLICY_REL, readPolicy, buildPayload, makeGuard, parseNoulAnswer, makeFixtureFetch, scoreAll"
      contains: "jev-devtime-client"
    - path: "tests/fixtures/356-jev-noul-responses.json"
      provides: "SYNTHETIC per-command p values for fixture builds (not Jev output, not the answer key)"
      contains: "default_p"
    - path: "tests/test-356-egress.cjs"
      provides: "Refusal legs (room_path, extra question key, policy mismatch, over-cap, foreign question text), builder injection leg, sentinel and real-key absence legs"
      min_lines: 120
    - path: "tests/test-356-policy.cjs"
      provides: "Byte-identical policy in every outgoing payload; policy_hash over file bytes; shipped-policy shape legs"
      min_lines: 100
  key_links:
    - from: "scripts/build-command-irreversibility-ledger.cjs scoreAll"
      to: "scripts/jev-devtime-client.cjs jev + makeEgressGuard(EGRESS_PROFILES.material_step_ledger, {root})"
      via: "require('./jev-devtime-client.cjs')"
      pattern: "EGRESS_PROFILES\\.material_step_ledger"
    - from: "scripts/build-command-irreversibility-ledger.cjs"
      to: "lib/core/irreversibility-ledger.cjs commandTextHash"
      via: "one staleness-hash definition shared by builder and runtime"
      pattern: "commandTextHash"
---

<objective>
Add 356's own egress profile to the shared client and build the scoring core of `scripts/build-command-irreversibility-ledger.cjs`: one Noul per registry command, sent through the shared client, with the policy text byte-identical to the policy file. Prove the refusals and the byte identity in keyless fixture mode.

Purpose: SPEC R7 (egress allow-list before every fetch; key discipline), R1 acceptance (policy byte-identical in the outgoing payload), R3 (the scoring half of the builder), R8 (the builder imports the client, no copies).
Output: the `material_step_ledger` profile, the builder's scoring functions, a synthetic fixture, `tests/test-356-egress.cjs`, `tests/test-356-policy.cjs`. Thresholding, ledger assembly and the CLI land in 356-09; `--check` in 356-10.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-02-SUMMARY.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-03-SUMMARY.md
@.claude/skills/spike-findings-MindrianOS-Plugin/SKILL.md

Parallel-session rules (D-11): explicit-path commits, never `git add -A`. Re-read scripts/jev-devtime-client.cjs from disk first: 354-17 (jsagi-25) or 357 (jsagi-e0) may have appended their own profiles; touch only `material_step_ledger` and the exact_state_v1 kind's new optional fields, never another profile. Do NOT create `data/jev-policies/command-irreversibility.json` in this plan (356-06 drafts it after the blind sheet is committed, D-16); tests use temp roots. Test hygiene contract: see 356-01-PLAN.md context; tests/fixtures/356-no-network-preload.cjs exists from wave 1, so spawned children use it.

Noul contract (docs.typesafe.ai primitives/noul.md, verified in RESEARCH): question `{ type: 'noul', instructions: string | object, criteria: { true, false } }`; answer `{ type: 'noul', noul: <number 0..1> }`, no confidence field, typically two decimals. Request body: `{ model: 'jev-latest', state: {...}, questions: { irreversible: {...} } }`; response JSON `{ model, answers: { irreversible: { type: 'noul', noul } }, usage: { input_tokens, output_tokens } }`.

The fixed question sentence (builder constant `QUESTION_TEXT`): "Under the policy in `policy`, does running the command named in `slug`, as described by `teaching` and `jtbd_summary`, take an effect the policy calls irreversible?"
</context>

<tasks>

<task type="auto">
  <name>Task 1: EGRESS_PROFILES.material_step_ledger (must_equal_file for policy) and tests/test-356-egress.cjs refusal legs</name>
  <files>scripts/jev-devtime-client.cjs, tests/test-356-egress.cjs</files>
  <read_first>
    - scripts/jev-devtime-client.cjs (whole file, re-read from disk now; note every profile present and the exact_state_v1 checker)
    - tests/test-356-jev-client.cjs (the parity and optional-field legs, which must stay green)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-08, D-09)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 3, "material_step_ledger guard profile")
    - data/command-registry.json (measure with node: the max length of any `teaching` and any `jtbd_summary`, and the longest slug; record them in the SUMMARY)
  </read_first>
  <action>
    1. Extend the `exact_state_v1` kind in `scripts/jev-devtime-client.cjs` with three OPTIONAL, additive profile fields (a profile without them behaves as before; the 353 profile is a different kind and is untouched): `string_keys` (array: each named state key must be a string, else refuse naming it); `question_max_len` (number: `instructions.question` of every question must be a string no longer than this); `question_strings_from_file_key` (a key of `must_equal_file`, for example `'policy'`: parse that file's bytes as JSON once at guard construction, collect every string value anywhere in it, and require `instructions.rule`, every element of `instructions.boundary_cases`, `criteria.true` and `criteria.false` of every question to be one of those strings; refuse naming the offending field, for example `questions.irreversible.instructions.rule`). Every refusal throws with `code = 'EGRESS_REFUSED'`, `profile`, `key`.
    2. Add to the frozen `EGRESS_PROFILES`, touching no other entry: `material_step_ledger: Object.freeze({ id: 'material_step_ledger', kind: 'exact_state_v1', top_keys: ['model','state','questions'], model: 'jev-latest', state_keys: ['slug','teaching','jtbd_summary','policy'], string_keys: ['slug','teaching','jtbd_summary','policy'], max_len_by_key: { slug: 64, teaching: 800, jtbd_summary: 200, policy: 8000 }, must_equal_file: { policy: 'data/jev-policies/command-irreversibility.json' }, question_ids: ['irreversible'], question_keys: ['type','instructions','criteria'], question_type: 'noul', instructions_keys: ['question','rule','boundary_cases'], criteria_keys: ['true','false'], question_max_len: 400, question_strings_from_file_key: 'policy' })`, with every nested array and object frozen. If the measured registry maxima from read_first exceed 80 percent of a cap, record it in the SUMMARY (a longer future blurb makes the build refuse; raising a cap is a deliberate edit, never a strip).
    3. Create `tests/test-356-egress.cjs` (inline scrub block). Helper `tempRoot(policyText)` makes a temp dir with `data/jev-policies/command-irreversibility.json` holding the given text; a valid D-01-shaped policy object is defined in the test. Helper `validPayload(policyText, policyObj)` builds a correct body for `/mos:export` using the real registry blurb. Legs, each asserting that a counting `fetchImpl` passed to `client.jev` was called 0 times, that `err.code === 'EGRESS_REFUSED'`, and that `err.key` and `err.message` name the offending key:
       - valid payload passes (`guard(payload) === true`, and `jev` then calls fetchImpl exactly once)
       - `state.room_path` added -> refused, key `room_path`
       - `state` missing `jtbd_summary` -> refused
       - `teaching` of 801 characters -> refused, key `teaching`
       - `teaching` null -> refused (string_keys)
       - `state.policy` with one changed byte, and with a trailing newline removed -> refused, key `policy`
       - a second question id `examples` -> refused
       - an extra question key `examples` inside `irreversible` -> refused
       - `criteria` given as an array -> refused
       - `instructions.rule` changed to text not in the policy file -> refused, key names `instructions.rule`
       - a boundary case string not in the policy file -> refused
       - `instructions.question` of 401 characters -> refused
       - top key `metadata` -> refused; `model` other than `jev-latest` -> refused
       - guard constructed without `opts.root` -> throws at construction
       - the caller's payload object is unchanged after every refusal (refuse, never strip)
       - `EGRESS_PROFILES.section_command_ledger` still replays the 356-02 parity corpus identically (require the fixture and replay it)
       - final `NET_ATTEMPTS === 0`.
    4. Re-run `node tests/test-356-jev-client.cjs`. Commit by explicit path: `feat(356-07): material_step_ledger egress profile (must_equal_file policy, question text from the policy only)`.
  </action>
  <verify>
    <automated>node tests/test-356-egress.cjs && node tests/test-356-jev-client.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-egress.cjs` exits 0 with every refusal leg passing and zero fetch calls on refusals
    - `node tests/test-356-jev-client.cjs` still exits 0 (section parity untouched)
    - `node -e "const c=require('./scripts/jev-devtime-client.cjs');const p=c.EGRESS_PROFILES.material_step_ledger;process.exit(p&&Object.isFrozen(p)&&p.must_equal_file.policy==='data/jev-policies/command-irreversibility.json'&&p.max_len_by_key.policy===8000?0:1)"` exits 0
    - `git show --format= -U0 $(git log -1 --format=%H -- scripts/jev-devtime-client.cjs) -- scripts/jev-devtime-client.cjs | grep '^-' | grep -v '^---' | grep -c "section_command_ledger\|framework_command_ledger\|card_fire_replay"` is 0 (no other profile touched)
    - `git log --format=%s -- data/jev-policies/command-irreversibility.json | grep -c "356-07"` is 0 (this plan never writes the policy file; tests use temp roots)
  </acceptance_criteria>
  <done>356's profile refuses every out-of-contract payload before the network, naming the key, and only registry blurb plus policy text can cross.</done>
</task>

<task type="auto">
  <name>Task 2: Scoring core of scripts/build-command-irreversibility-ledger.cjs, the synthetic fixture, tests/test-356-policy.cjs, and the no-key-material legs</name>
  <files>scripts/build-command-irreversibility-ledger.cjs, tests/fixtures/356-jev-noul-responses.json, tests/test-356-policy.cjs, tests/test-356-egress.cjs</files>
  <read_first>
    - scripts/jev-devtime-client.cjs (the interface and the new profile)
    - lib/core/irreversibility-ledger.cjs (`commandTextHash`, `TEXT_HASH_BASIS`)
    - scripts/irreversibility-answer-key.cjs (`readRegistryRows`, `sha256Hex` exports)
    - scripts/build-section-command-ledger.cjs (header comment and constants only, for house style; `VENDOR_USD_PER_INPUT_TOKEN`)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 3 request body, Pattern 4 builder shape, "Code Examples" fixture fetch, Pitfall 9)
  </read_first>
  <action>
    1. Create `scripts/build-command-irreversibility-ledger.cjs`: `'use strict'`; header comment (hyphens only): Phase 356 dev-time builder; scores every registry command once with one Jev Noul under the written policy and ships the result as data; the key is read only here, from the environment or `~/.secrets/typesafe.env`, never printed, logged or written; no file under lib/ or hooks/ may require this script or the client (tripwires); exit codes (0 ok, 1 threshold or scoring failure, 2 refused input, 3 no key, 4 appeal gate) with modes added by 356-09 and 356-10. Requires: node:fs, node:path, node:os, node:crypto; `const client = require('./jev-devtime-client.cjs');`; `commandTextHash` and `TEXT_HASH_BASIS` from `../lib/core/irreversibility-ledger.cjs`; `readRegistryRows` and `sha256Hex` from `./irreversibility-answer-key.cjs`. Constants: `ROOT`, `DATA_DIR`, `POLICY_REL = client.EGRESS_PROFILES.material_step_ledger.must_equal_file.policy` (single source for the policy path), `DEFAULT_LABELS_PATH` (data/jev-labels/command-irreversibility.json), `DEFAULT_LEDGER_PATH` (data/command-irreversibility-ledger.json), `MODEL = 'jev-latest'`, `QUESTION_ID = 'irreversible'`, `QUESTION_TEXT` (from the context section), `CONC = 4`, `VENDOR_USD_PER_INPUT_TOKEN = 42 / 1e9`.
       Exported pure functions: `readPolicy(root)` -> `{ text, obj, hash, path }` (text read as utf8 from `path.join(root, POLICY_REL)`, `hash = sha256Hex(raw bytes)`; throw `code = 'INPUT_REFUSED'` unless obj has string `policy_id`, `version`, string `instructions`, `criteria` a plain object with string `true`/`false`, and `boundary_cases` a non-empty array of strings); `makeGuard(root)` -> `client.makeEgressGuard(client.EGRESS_PROFILES.material_step_ledger, { root })`; `buildPayload(row, policy)` -> `{ model: MODEL, state: { slug: row.command, teaching: row.teaching, jtbd_summary: row.jtbd_summary, policy: policy.text }, questions: { irreversible: { type: 'noul', instructions: { question: QUESTION_TEXT, rule: policy.obj.instructions, boundary_cases: policy.obj.boundary_cases }, criteria: { true: policy.obj.criteria.true, false: policy.obj.criteria.false } } } }` (row text already normalized to '' by readRegistryRows); `parseNoulAnswer(json, command)` -> the number, throwing unless `json.answers.irreversible.type === 'noul'` and `noul` is a finite number in [0, 1] (store it exactly as returned, never rounded); `makeFixtureFetch(fixture, sink)` -> an async function `(url, init)` that pushes `{ url, headers: init.headers, body: JSON.parse(init.body) }` into `sink` and resolves `{ status: 200, text: async () => JSON.stringify({ model: fixture.model, answers: { irreversible: { type: 'noul', noul: p } }, usage: { input_tokens: 1500, output_tokens: 5 } }) }` with `p = fixture.p[slug]` when defined, else `fixture.default_p`; `scoreAll(rows, policy, opts)` with `opts = { key, root, fetchImpl, sleepImpl, concurrency, buildPayloadImpl }`: construct the guard once with `makeGuard(opts.root || ROOT)`, then `client.pool(rows, concurrency || CONC, ...)` calling `client.jev((buildPayloadImpl || buildPayload)(row, policy), { key, guard, fetchImpl, sleepImpl })`; a result with status other than 200 (including 0 after retries) or a bad answer shape throws `Error('score: <command> HTTP <status>')` with `code = 'SCORE_FAILED'` (abort, never ship a null); returns `{ entries: [{ command, p_irreversible, text_hash: commandTextHash(command, teaching, jtbd_summary) }], usage: { jev_calls, input_tokens, output_tokens, jev_model } }` with entries sorted by command and `jev_model` the response `model` (if calls report different models, a sorted array of them).
       No `require.main` block yet (356-09 adds `main()`); export the constants and functions. The builder defines no fetch wrapper, no guard, no `EGRESS_ALLOWED_` sets, and no call to a bare `fetch(`.
    2. Create `tests/fixtures/356-jev-noul-responses.json`: `{ "_note": "SYNTHETIC fixture for Phase 356 tests. Not Jev output. Not the answer key.", "model": "jev-fixture-356", "default_p": 0.05, "p": { "/mos:publish": 0.97, "/mos:export": 0.31, "/mos:snapshot": 0.22, "/mos:vault": 0.18, "/mos:present": 0.12 } }` with a trailing newline.
    3. Create `tests/test-356-policy.cjs` (inline scrub block). Legs:
       - byte identity (R1 acceptance): temp root with a D-01-shaped policy text; `rows = readRegistryRows(<real registry>)`; `scoreAll(rows, readPolicy(tmpRoot), { key: 'sk-356-SENTINEL-DO-NOT-SHIP', root: tmpRoot, fetchImpl: makeFixtureFetch(fixture, sink) })`; assert `sink.length === rows.length`; for every captured body, `body.state.policy === fs.readFileSync(<tmp policy>, 'utf8')` and `Buffer.from(body.state.policy, 'utf8').equals(fs.readFileSync(<tmp policy>))`; `instructions.rule === policy.obj.instructions`; `boundary_cases` deep-equals; `criteria` deep-equals; `instructions.question === QUESTION_TEXT`; every captured Authorization header equals `Bearer sk-356-SENTINEL-DO-NOT-SHIP`.
       - hash: `readPolicy(tmpRoot).hash` equals sha256 hex of the raw file bytes.
       - empty-field normalization: the four commands whose registry `jtbd_summary` is null (enumerate at run time) are sent with `jtbd_summary === ''` and their `text_hash` equals `commandTextHash(command, teaching, '')`.
       - strict parsing: a fixture fetch that answers `{ noul: 1.2 }`, or `type: 'choice'`, or HTTP 500 makes `scoreAll` reject with `SCORE_FAILED` naming the command.
       - shipped policy legs, only when `data/jev-policies/command-irreversibility.json` exists (else print `PENDING`): fields present with `criteria` an object (never an array); `instructions` matches `/irreversible is narrower than material/i` (D-03); `boundary_cases` mention `/mos:export`, `/mos:snapshot`, `/mos:vault`, `/mos:present`, `/mos:publish`; the file has no em-dash and no `larry-extended`; `buildPayload(row, readPolicy(ROOT))` passes `makeGuard(ROOT)` for every registry row.
       - final `NET_ATTEMPTS === 0`.
    4. Append to `tests/test-356-egress.cjs`: (a) builder injection (R7 acceptance): `scoreAll(rows.slice(0, 3), policy, { key: 'k', root: tmpRoot, fetchImpl: counting, buildPayloadImpl: (r, p) => { const b = buildPayload(r, p); b.state.room_path = '/home/someone/room'; return b; } })` rejects with `EGRESS_REFUSED`, `err.key === 'room_path'`, and the counter is 0; (b) no key material: after a fixture `scoreAll` with the sentinel key, `JSON.stringify(result)` does not contain the sentinel, and no file under `data/` (recursive) contains it; (c) the real key, only if `client.loadKey({ env: {}, secretsPath: path.join(os.homedir(), '.secrets', 'typesafe.env') })` returns a value: no file under `data/` contains it; print only `real key compared against <n> data files: absent` or `real key unavailable: leg skipped`; never print the key or put it in a check label.
    5. Commit by explicit path: `feat(356-07): irreversibility scoring core through the shared client, synthetic Noul fixture, policy byte-identity tests`.
  </action>
  <verify>
    <automated>node tests/test-356-policy.cjs && node tests/test-356-egress.cjs && node tests/test-356-jev-client.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-356-policy.cjs` exits 0 with the byte-identity leg reporting one captured payload per registry command
    - `node tests/test-356-egress.cjs` exits 0 including the injection leg (EGRESS_REFUSED, key room_path, zero fetches) and the sentinel leg
    - `node tests/test-356-jev-client.cjs` exits 0 and its no-local-copies leg now checks scripts/build-command-irreversibility-ledger.cjs
    - `grep -v '^\s*//\|^\s*\*' scripts/build-command-irreversibility-ledger.cjs | grep -c "fetch(\|function assertEgressCeiling\|EGRESS_ALLOWED_"` is 0
    - `grep -c "require('./jev-devtime-client.cjs')" scripts/build-command-irreversibility-ledger.cjs` is 1
    - `grep -rl "sk-356-SENTINEL-DO-NOT-SHIP" data/ | wc -l` is 0
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/build-command-irreversibility-ledger.cjs tests/test-356-policy.cjs tests/test-356-egress.cjs tests/fixtures/356-jev-noul-responses.json scripts/jev-devtime-client.cjs` reports 0 for each
  </acceptance_criteria>
  <done>The builder can score every registry command through the shared client in fixture mode, and tests prove the outgoing policy is byte-identical to the file and that nothing else, and no key, crosses or lands in data/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder -> api.typesafe.ai | one payload per command crosses; only plugin-authored text may cross (Part 8) |
| key (env / secrets file) -> request header | the key may appear only in the Authorization header of a live request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-14 | Information disclosure | material_step_ledger guard | mitigate | exact state keys {slug, teaching, jtbd_summary, policy}; every question string must come from the policy file; room_path injection test shows refusal with zero fetches |
| T-356-15 | Tampering | state.policy | mitigate | must_equal_file compares the payload bytes with the file bytes read once at guard construction; one-byte and trailing-newline mismatches refused |
| T-356-05 | Information disclosure | key handling | mitigate | the key is passed only to client.jev as a header; sentinel test shows it absent from results and data/; the real key compared silently against data/ |
| T-356-07 | Elevation of privilege | builder | mitigate | no local fetch or guard; every call goes through client.jev with a required guard (no-local-copies leg) |
| T-356-SC | Tampering | package installs | accept | no packages installed; the TypeSafe SDK is deliberately not used |
</threat_model>

<verification>
- `node tests/test-356-egress.cjs && node tests/test-356-policy.cjs && node tests/test-356-jev-client.cjs` all exit 0
- `bash tests/run-all-356.sh` reports FAILED=0
</verification>

<success_criteria>
356's egress profile refuses anything beyond the registry blurb and the policy, the policy crosses byte-identical, and the scoring core runs keyless in fixture mode through the shared client.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `scripts/jev-devtime-client.cjs` (adds `material_step_ledger` and three optional exact_state_v1 fields)
- `scripts/build-command-irreversibility-ledger.cjs` (new: scoring core)
- `tests/fixtures/356-jev-noul-responses.json` (new, synthetic)
- `tests/test-356-egress.cjs` (new)
- `tests/test-356-policy.cjs` (new)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-07-SUMMARY.md` when done (`git add -f`, explicit path). Record the measured registry maxima (slug, teaching, jtbd_summary), the final EGRESS_PROFILES key list, and the three new optional exact_state_v1 fields (so 357 and 354-17 can be told in 356-13).
</output>
