---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/jev-devtime-client.cjs
  - scripts/build-section-command-ledger.cjs
  - tests/fixtures/356-section-guard-parity.json
  - tests/test-356-jev-client.cjs
  - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/deferred-items.md
autonomous: true
requirements: [R356-08, R356-07]
tags: [jev, typesafe, egress-guard, shared-client, part8, cross-session]

must_haves:
  truths:
    - "scripts/jev-devtime-client.cjs exists (extracted by 356 if absent, imported if a peer extracted it first, D-06) and exports the D-07 interface: loadKey({env, secretsPath}), makeEgressGuard(profile, opts), jev(body, {key, guard, endpoint, fetchImpl, sleepImpl}), pool(items, n, fn), EGRESS_PROFILES (frozen, one profile per builder, never a merged union, D-08)"
    - "EGRESS_PROFILES.section_command_ledger accepts and refuses exactly what the pre-extraction assertEgressCeiling in build-section-command-ledger.cjs did, with identical error messages, proven against a recorded parity corpus"
    - "makeEgressGuard honors two OPTIONAL, additive profile fields (cross-session agreement with jsagi-e0/357, orchestrator addendum 2026-09-23): max_len_by_key {key: N} caps state[key] length, and must_equal_file {key: 'data/jev-policies/<ledger>.json'} requires state[key] to be byte-identical to that file's bytes; a profile without them behaves exactly as before; a violation throws (refuse, never strip) with an error naming the key"
    - "jev() refuses to run without a guard function, runs the guard before any fetch, resolves fetchImpl at call time, and keeps the 353 retry semantics exactly"
    - "The 353 builder is refactored to import and re-export loadKey, assertEgressCeiling, jev(key, body) and pool ONLY if the unowned diffs in eval-icm-writers.cjs and the two test-353 files are gone; otherwise it stays byte-identical and the refactor is logged to deferred-items.md (same guard as 354-17, commit 873363114)"
    - "The 353 tests keep their recorded baselines (ledger-shape 13 pass / 6 fail, grader-agreement 30/0, release-wiring 8/0, tripwires green) and the three unowned files are never modified by 356"
  artifacts:
    - path: "scripts/jev-devtime-client.cjs"
      provides: "Shared dev-time Jev client and per-profile egress guard, pure (node:fs and node:path only), Theo-portable (D-13)"
      exports: ["DEFAULT_ENDPOINT", "loadKey", "makeEgressGuard", "jev", "pool", "EGRESS_PROFILES"]
    - path: "tests/fixtures/356-section-guard-parity.json"
      provides: "Recorded accept/throw outcomes of the pre-extraction 353 assertEgressCeiling over at least 16 payloads"
      contains: "356-section-guard-parity/v1"
    - path: "tests/test-356-jev-client.cjs"
      provides: "Interface, parity, optional-field (max_len_by_key, must_equal_file), retry, guard-required and no-local-copy legs"
      min_lines: 150
  key_links:
    - from: "scripts/build-section-command-ledger.cjs"
      to: "scripts/jev-devtime-client.cjs"
      via: "require + re-export of loadKey/assertEgressCeiling/jev/pool (only when the refactor is not deferred)"
      pattern: "jev-devtime-client"
    - from: "tests/test-356-jev-client.cjs"
      to: "tests/fixtures/356-section-guard-parity.json"
      via: "every recorded case replayed against EGRESS_PROFILES.section_command_ledger and the 353 export"
      pattern: "356-section-guard-parity"
---

<objective>
Extract the one shared dev-time Jev client (SPEC R8; D-06..D-09, D-18, D-19) so 356, 354-17 and 357 import one fetch wrapper and one per-profile egress guard instead of three copies, and extend its profile schema with the two optional fields agreed with jsagi-e0 (orchestrator addendum): `max_len_by_key` and `must_equal_file`.

Extract-if-missing: if `scripts/jev-devtime-client.cjs` already exists (a peer got there first), import it and add only what the D-07 interface and the addendum still lack. The 353 builder is refactored to import and re-export only when the unowned uncommitted diffs are gone; otherwise it stays untouched and the refactor is deferred, exactly as jsagi-25 amended 354-17 to do.

Purpose: R8 (one client, no local copies), R7 (refuse-before-fetch egress guard), D-09 (Theo-compatible error shape), D-13 (pure, portable).
Output: the client, a parity fixture recorded from the pre-extraction guard, the client test, and either a byte-compatible 353 refactor or a deferred-items entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md
@.claude/skills/spike-findings-MindrianOS-Plugin/SKILL.md

Parallel-session rules (D-11): commit only by explicit path, never `git add -A`, never stash, never revert or checkout another session's diff. NEVER edit `scripts/eval-icm-writers.cjs`, `tests/test-353-grader-agreement.cjs`, `tests/test-353-ledger-shape.cjs` (unowned, D-18). Run every test with `env -u TYPESAFE_API_KEY`. The test hygiene contract (inline scrub block, NET_ATTEMPTS === 0, spawned-child env rules) is stated in 356-01-PLAN.md's context section and applies here; tests/fixtures/356-no-network-preload.cjs may not exist yet in wave 1, so this plan's test does not spawn children.

Interface this plan must deliver (D-07 plus the addendum), stated as signatures, not code:
- `DEFAULT_ENDPOINT` = 'https://api.typesafe.ai/v1/systemone'
- `loadKey({ env, secretsPath })` -> string or null; env.TYPESAFE_API_KEY first, else the `TYPESAFE_API_KEY=` line of secretsPath (trimmed, surrounding quotes stripped); never logs, never throws with the value
- `makeEgressGuard(profile, opts)` -> `guard(payload)` returning `true` or throwing; `opts.root` is required only when the profile declares `must_equal_file`
- `jev(body, { key, guard, endpoint, fetchImpl, sleepImpl })` -> Promise of `{ status, ms, json, text }`
- `pool(items, n, fn)` -> Promise of results in input order
- `EGRESS_PROFILES` -> frozen object of frozen profile objects keyed by builder id
- Guard error shape (D-09): an `Error` whose message names the offending key, with `err.code = 'EGRESS_REFUSED'`, `err.profile = <profile id>`, `err.key = <offending key>`. The section_command_ledger profile keeps its historical message text (prefix `assertEgressCeiling:`) byte-for-byte.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight, parity corpus from the pre-extraction guard, and scripts/jev-devtime-client.cjs (extract-if-missing, with max_len_by_key and must_equal_file)</name>
  <files>scripts/jev-devtime-client.cjs, tests/fixtures/356-section-guard-parity.json</files>
  <read_first>
    - scripts/build-section-command-ledger.cjs (the key-handling, egress-ceiling and Jev-client block: `grep -n "function loadKey\|EGRESS_ALLOWED\|function assertEgressCeiling\|const sleep\|async function jev\|async function pool\|^  module.exports\|ENDPOINT\|MODEL" scripts/build-section-command-ledger.cjs`, then read from `function loadKey` through the end of `pool`, and the export object at the bottom)
    - scripts/eval-icm-writers.cjs (READ ONLY: `grep -n "build-section-command-ledger\|jev(\|assertEgressCeiling\|loadKey\|pool(" scripts/eval-icm-writers.cjs` to confirm which names and call orders it uses)
    - tests/test-353-grader-agreement.cjs (READ ONLY: `grep -n "assertEgressCeiling" tests/test-353-grader-agreement.cjs`)
    - tests/test-353-ledger-shape.cjs (READ ONLY: `grep -n "global.fetch\|require(" tests/test-353-ledger-shape.cjs`)
    - .planning/phases/354-system-integrity-and-theo-integration-independent-research-a/354-17-PLAN.md (READ ONLY: Task 1 STEP 0, the sibling's extract-if-missing branch and its framework_command_ledger profile, so the interface agrees)
    - .planning/phases/357-gate-triad-ledger-jev-scored-at-dev-time-is-fork-answered-re/357-CONTEXT.md (READ ONLY: `grep -n "must_equal_file\|max_len_by_key\|card_fire_replay\|profile" 357-CONTEXT.md` to confirm 357's use of the two optional fields)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 1 and "Code Examples")
  </read_first>
  <action>
    STEP 0, preflight (write every result into the SUMMARY under "## Preflight"):
    (a) `git status --short -- scripts/eval-icm-writers.cjs tests/test-353-grader-agreement.cjs tests/test-353-ledger-shape.cjs scripts/build-section-command-ledger.cjs scripts/jev-devtime-client.cjs`, and `git diff -- scripts/eval-icm-writers.cjs tests/test-353-grader-agreement.cjs tests/test-353-ledger-shape.cjs | md5sum` (record the digest; Task 2 compares it).
    (b) Record 353 baselines before any edit, each with `env -u TYPESAFE_API_KEY`: `node tests/test-353-ledger-shape.cjs` (expected PASS=13 FAIL=6, D-18; record the actual counts, never "fix" them), `node tests/test-353-grader-agreement.cjs` (expected 30/0), `node tests/test-353-release-wiring.cjs` (expected 8/0), `node tests/test-353-tripwires.cjs` (green; 356-01 may be converting leg 2 in parallel, record what you see).
    (c) Peer notice (do not block on replies): list peer sessions (ListAgents or the runtime's equivalent) and message jsagi-25 (354-17) and jsagi-e0 (357): "356 (jsagi-a7) is running extract-if-missing for scripts/jev-devtime-client.cjs now, with the optional max_len_by_key and must_equal_file profile fields. If you already extracted it, say so and 356 will import yours." If no messaging tool is available, write the message text into the SUMMARY under "## Peer messages (to relay)".
    (d) Immediately before writing, `test -f scripts/jev-devtime-client.cjs` and branch.

    STEP 1, parity corpus (both branches). Pick the pre-extraction source commit: if the client is ABSENT, `git log -1 --format=%H -- scripts/build-section-command-ledger.cjs`; if PRESENT, the parent of the first commit that added the client (`git log --diff-filter=A --format=%H -- scripts/jev-devtime-client.cjs | tail -1`, then `<sha>^`). Write `git show <commit>:scripts/build-section-command-ledger.cjs` to a file in the session scratchpad and require it from there (its guard needs no repo paths). Run its exported `assertEgressCeiling` over at least 16 cases and record each as `{ name, payload, expect }` where `expect` is `"ok"` or `{ "throws": "<exact message>" }`. Cases: minimal valid payload (model, state {section, problem_type, stage, candidates {c1 {name, jtbd, glossary, description}}}, questions {q {type, instructions {judge}, criteria}}); disallowed top key `room_path`; state given as an array (accepted: state checks skipped); disallowed state key `room_path`; disallowed candidate key `body`; candidate string of 141 chars; candidate string of exactly 140 chars (ok); `instructions.judge` of 401 chars; judge of exactly 400 (ok); `criteria` with arbitrary keys (ok, unchecked); payload `null`; payload a string; state absent (ok); candidates absent (ok); numeric candidate value (ok); two candidates where the second carries a bad key (message names the second candidate id). Write `tests/fixtures/356-section-guard-parity.json` as `{ "schema": "356-section-guard-parity/v1", "recorded_from": "scripts/build-section-command-ledger.cjs assertEgressCeiling at <commit sha>", "cases": [...] }`, pretty-printed with a trailing newline. The generator stays in the scratchpad; only the fixture is committed.

    STEP 2, branch ABSENT (356 extracts): create `scripts/jev-devtime-client.cjs`. `'use strict'`; requires only `node:fs` and `node:path`; header comment (hyphens only): dev-time only, never required from lib/ or hooks/ (tripwires enforce it), pure and path-free so Theo can vendor or port it (D-13), the key is never printed, logged or returned in an error, one profile per builder and never a merged union (D-08), refuse and never strip (D-09). Contents:
    - `DEFAULT_ENDPOINT`, `loadKey`, `pool` (verbatim behavior of the 353 versions), and `jev` per the interface in the context section: throw `new Error('jev: a guard is required (no unguarded egress)')` when `opts.guard` is not a function; call `opts.guard(body)` before any network; resolve `const doFetch = opts.fetchImpl || globalThis.fetch` at call time (353 tests swap `global.fetch` after requiring modules); `sleepImpl` defaults to a setTimeout promise; retry semantics identical to the 353 `jev`: a thrown fetch retries while `attempt++ < 3` sleeping `500 * 2 ** attempt`, then returns `{ status: 0, ms, json: null }`; a 429 or 529 response retries while `attempt++ < 4` sleeping `400 * 2 ** attempt`; otherwise returns `{ status, ms, json, text: text.slice(0, 200) }` with `json` null when the body is not JSON; one shared attempt counter.
    - `makeEgressGuard(profile, opts)`: dispatch on `profile.kind`. Kind `'candidates_v1'` reproduces the 353 `assertEgressCeiling` semantics exactly from profile data (`top_keys`, `state_keys`, `candidate_keys`, `candidate_max_len`, `judge_max_len`, `message_prefix`): top-level keys checked; state keys checked only when state is a non-array object; candidate keys and candidate string caps checked; `questions[qid].instructions.judge` capped; `criteria` and question keys NOT checked; returns `true`; messages byte-identical to 353 when `message_prefix` is `'assertEgressCeiling'`. Kind `'exact_state_v1'` is the strict kind 356 and 357 use: implement it here as a generic checker driven by profile data (`top_keys` exact, optional `model` exact value, `state_keys` exact set with every key required, optional `question_ids` exact set, optional `question_keys` exact set per question, optional `question_type`, optional `instructions_keys`, optional `criteria_keys`), each violation throwing with the key named. Unknown kind throws at construction.
    - The two optional fields (orchestrator addendum), applied by BOTH kinds after the kind checks, only when present: `max_len_by_key` `{ key: N }`: for each key present in `payload.state` whose value is a string longer than N, throw `egress refused (<profile id>): state.<key> exceeds <N> chars`. `must_equal_file` `{ key: 'relative/path.json' }`: at guard construction require `opts.root` (throw `makeEgressGuard: must_equal_file requires opts.root` if missing), resolve each path with `path.resolve(opts.root, rel)`, refuse a path that escapes the root, and read the file's bytes once with `fs.readFileSync` (throw at construction if unreadable); at each call, throw `egress refused (<profile id>): state.<key> is not byte-identical to <rel>` unless `Buffer.from(payload.state[key], 'utf8').equals(fileBytes)` (a missing or non-string value also refuses). Every thrown guard error carries `code = 'EGRESS_REFUSED'`, `profile`, `key` (D-09).
    - `EGRESS_PROFILES = Object.freeze({ section_command_ledger: Object.freeze({ id: 'section_command_ledger', kind: 'candidates_v1', top_keys: [...], state_keys: ['section','problem_type','stage','candidates'], candidate_keys: ['name','jtbd','glossary','description'], candidate_max_len: 140, judge_max_len: 400, message_prefix: 'assertEgressCeiling' }) })` with frozen arrays. Do NOT add `material_step_ledger` here (356-07 adds it, with `must_equal_file` for `policy`), and do NOT add profiles for 354-17 or 357 (they add their own).
    - `module.exports = { DEFAULT_ENDPOINT, loadKey, makeEgressGuard, jev, pool, EGRESS_PROFILES }`.

    STEP 2, branch PRESENT (a peer extracted it): read the file as the authoritative interface. Add, additively and without changing any existing profile's behavior, only what is missing from the interface above: a missing export, the `exact_state_v1` kind if absent, and the `max_len_by_key` / `must_equal_file` fields if absent (message the peer who owns the file before editing, per D-06). Do not rename or restructure their code. Record in the SUMMARY exactly what was added.

    STEP 3: commit by explicit path (`git add scripts/jev-devtime-client.cjs tests/fixtures/356-section-guard-parity.json`): `feat(356-02): shared dev-time Jev client with per-profile egress guard (D-06..D-09, max_len_by_key, must_equal_file)`.
  </action>
  <verify>
    <automated>node -e "const c=require('./scripts/jev-devtime-client.cjs');for(const k of ['DEFAULT_ENDPOINT','loadKey','makeEgressGuard','jev','pool','EGRESS_PROFILES'])if(!(k in c))process.exit(1);if(!Object.isFrozen(c.EGRESS_PROFILES)||!c.EGRESS_PROFILES.section_command_ledger)process.exit(2);if(require('./tests/fixtures/356-section-guard-parity.json').cases.length<16)process.exit(3)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/jev-devtime-client.cjs` and the verify one-liner exits 0
    - `grep -v '^\s*//\|^\s*\*' scripts/jev-devtime-client.cjs | grep 'require(' | grep -vc "require('node:"` is 0 (no repo or lib/ requires)
    - `grep -c "EGRESS_REFUSED" scripts/jev-devtime-client.cjs` is at least 1; `grep -c "must_equal_file" scripts/jev-devtime-client.cjs` is at least 2; `grep -c "max_len_by_key" scripts/jev-devtime-client.cjs` is at least 1
    - `grep -v '^\s*//\|^\s*\*' scripts/jev-devtime-client.cjs | grep -c "material_step_ledger"` is 0 (356-07 adds that profile)
    - `node -e "process.exit(require('./tests/fixtures/356-section-guard-parity.json').recorded_from.indexOf('assertEgressCeiling at ')===0?0:1)"` exits 0
    - The SUMMARY "## Preflight" section lists the git status lines, the md5 digest of the three unowned diffs, and the four 353 baseline counts
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/jev-devtime-client.cjs tests/fixtures/356-section-guard-parity.json` reports 0 for both
  </acceptance_criteria>
  <done>One client exists with both profile kinds and both optional fields, and a parity corpus records what the old 353 guard did before anything changed.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test-356-jev-client.cjs and the guarded 353 builder refactor (or its deferral)</name>
  <files>tests/test-356-jev-client.cjs, scripts/build-section-command-ledger.cjs, .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/deferred-items.md</files>
  <read_first>
    - scripts/jev-devtime-client.cjs (as written in Task 1)
    - tests/fixtures/356-section-guard-parity.json
    - scripts/build-section-command-ledger.cjs (the same block as Task 1 plus `main()` and the export object)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-RESEARCH.md (Pattern 1 "Byte-compatible re-export recipe")
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/deferred-items.md (if it exists; append, never rewrite)
  </read_first>
  <action>
    1. Create `tests/test-356-jev-client.cjs` with the inline scrub block (no child spawns). Legs:
       (a) interface: the six exports exist; `EGRESS_PROFILES` and every profile object and its arrays are frozen.
       (b) parity: for every fixture case, `makeEgressGuard(EGRESS_PROFILES.section_command_ledger)(payload)` returns exactly `true` when `expect` is `"ok"`, else throws an error whose `message` equals `expect.throws` byte-for-byte; AND `require('../scripts/build-section-command-ledger.cjs').assertEgressCeiling` gives the identical outcome (holds in both branches: refactored or deferred).
       (c) guard required: `jev({}, { key: 'k', fetchImpl: counting fake })` rejects with `/guard is required/` and the fake was never called.
       (d) guard before fetch: a guard that throws means `fetchImpl` is never called and the rejection is the guard's error.
       (e) retry semantics with a counting `fetchImpl` and a recording `sleepImpl` (no real sleeps): always-throwing fetch gives 4 calls, sleeps `[1000, 2000, 4000]`, result `status === 0` and `json === null`; always-429 gives 5 calls, sleeps `[800, 1600, 3200, 6400]`, result `status === 429`; a 200 JSON body gives 1 call, parsed `json`, `text.length <= 200`.
       (f) call-time fetch: after requiring the client, set `globalThis.fetch` to a counting fake, call `jev` without `fetchImpl` and a pass-through guard, assert the fake was called once, then restore the thrower.
       (g) loadKey: env value wins over the file; a temp secrets file with `TYPESAFE_API_KEY="sk-356-TEST"` yields `sk-356-TEST` with quotes stripped; missing env and missing file yields `null`; wrap `console.log`, `console.error` and `process.stdout.write` during the calls and assert zero writes.
       (h) pool keeps input order and never runs more than n tasks at once.
       (i) optional field `max_len_by_key` (orchestrator addendum): build a synthetic `exact_state_v1` profile in-test (not added to EGRESS_PROFILES) with state keys `{a, b}` and `max_len_by_key: { a: 5 }`; a 5-char `a` passes; a 6-char `a` throws with `err.code === 'EGRESS_REFUSED'`, `err.key === 'a'` and the message containing `state.a`; `b` has no cap.
       (j) optional field `must_equal_file`: write a temp root containing `data/jev-policies/test-policy.json`; a profile with `must_equal_file: { p: 'data/jev-policies/test-policy.json' }`; the exact file text passes; one changed byte throws with `err.key === 'p'` and the message naming the relative path; a trailing newline difference throws (byte identity, not trimmed equality); a missing `p` throws; `makeEgressGuard(profile)` without `opts.root` throws at construction; a rel path `../outside.json` throws at construction; editing the file AFTER guard construction does not change what the guard compares against (bytes read once).
       (k) additivity: adding the two optional fields to a copy of the section profile with neither field present changes no parity outcome (replay the corpus against `Object.assign({}, section profile)`).
       (l) refuse, never strip: after a refusal the caller's payload object is deep-equal to its pre-call snapshot.
       (m) no local copies (R8): for `scripts/build-section-command-ledger.cjs` only when its non-comment lines contain `jev-devtime-client` (refactored branch): no non-comment line contains `fetch(`, `function assertEgressCeiling`, or `EGRESS_ALLOWED_`. When not refactored, assert `deferred-items.md` in the phase dir contains `build-section-command-ledger.cjs refactor DEFERRED` and print `353 builder refactor: DEFERRED (see deferred-items.md)`. For `scripts/build-command-irreversibility-ledger.cjs`, only if it exists: the same three bans, and it contains `jev-devtime-client`.
       (n) final `NET_ATTEMPTS === 0`.
    2. Refactor decision. Re-run `git status --short -- scripts/eval-icm-writers.cjs tests/test-353-grader-agreement.cjs tests/test-353-ledger-shape.cjs scripts/build-section-command-ledger.cjs`.
       - DEFER when any of the three unowned files still shows a diff, or when build-section-command-ledger.cjs itself is dirty: leave build-section-command-ledger.cjs byte-identical. Append to `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/deferred-items.md` (create it if absent) a section headed `## 356-02: build-section-command-ledger.cjs refactor DEFERRED` with the date, the git status lines, the reason (unowned uncommitted diffs in eval-icm-writers.cjs and test-353-*; D-18; the same guard jsagi-25 put in 354-17, commit 873363114), and the recipe to apply later: delete the local loadKey, EGRESS_ALLOWED_* sets, assertEgressCeiling, sleep, jev and pool; require the client; `assertEgressCeiling = client.makeEgressGuard(client.EGRESS_PROFILES.section_command_ledger)`; a `loadKey()` wrapper passing `{ env: process.env, secretsPath: path.join(os.homedir(), '.secrets', 'typesafe.env') }`; a `jev(key, body)` wrapper calling `client.jev(body, { key, guard: assertEgressCeiling, endpoint: ENDPOINT })`; `pool = client.pool`; module.exports names unchanged.
       - REFACTOR when all four paths are clean: message jsagi-25 and jsagi-e0 first (D-06: the 353 builder is a shared file), then apply exactly that recipe. Keep `ENDPOINT` and `MODEL` in the builder. Re-run the four 353 suites with `env -u TYPESAFE_API_KEY` and compare with the Task 1 baselines; if any count differs, restore ONLY this file with `git checkout -- scripts/build-section-command-ledger.cjs` (it was clean before this task, so this restores 356's own edit and nothing else) and take the DEFER path instead.
    3. Confirm the unowned files were not touched: `git diff -- scripts/eval-icm-writers.cjs tests/test-353-grader-agreement.cjs tests/test-353-ledger-shape.cjs | md5sum` equals the Task 1 digest (their owners may commit in parallel; if the digest changed, check `git log -3` on them and record that another session committed, not 356).
    4. Commit by explicit path only the files this task changed (`tests/test-356-jev-client.cjs`, plus `scripts/build-section-command-ledger.cjs` on REFACTOR, plus `git add -f` the deferred-items.md on DEFER): `test(356-02): shared Jev client parity and optional-field tests; 353 builder refactor <applied|deferred>`.
  </action>
  <verify>
    <automated>env -u TYPESAFE_API_KEY node tests/test-356-jev-client.cjs && env -u TYPESAFE_API_KEY node tests/test-353-grader-agreement.cjs && env -u TYPESAFE_API_KEY node tests/test-353-release-wiring.cjs && env -u TYPESAFE_API_KEY node tests/test-353-tripwires.cjs; env -u TYPESAFE_API_KEY node tests/test-353-ledger-shape.cjs | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `env -u TYPESAFE_API_KEY node tests/test-356-jev-client.cjs` exits 0 with legs (a) through (n) passing
    - `env -u TYPESAFE_API_KEY node tests/test-353-ledger-shape.cjs` reports the same PASS and FAIL counts as the Task 1 baseline (expected PASS=13 FAIL=6)
    - `test-353-grader-agreement`, `test-353-release-wiring` and `test-353-tripwires` exit 0 with their baseline counts
    - Exactly one holds: (REFACTOR) `grep -c "jev-devtime-client" scripts/build-section-command-ledger.cjs` is at least 1 AND `grep -v '^\s*//' scripts/build-section-command-ledger.cjs | grep -c "fetch("` is 0 AND `node -e "const b=require('./scripts/build-section-command-ledger.cjs');for(const k of ['loadKey','assertEgressCeiling','jev','pool'])if(typeof b[k]!=='function')process.exit(1)"` exits 0; or (DEFER) `git diff --quiet -- scripts/build-section-command-ledger.cjs` exits 0 AND `grep -c "build-section-command-ledger.cjs refactor DEFERRED" .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/deferred-items.md` is at least 1
    - The md5 of `git diff` over the three unowned files equals the Task 1 digest, or the SUMMARY names the peer commit that changed it
    - `git show --stat --format= $(git log -1 --format=%H -- tests/test-356-jev-client.cjs)` lists only paths from this plan's files_modified
  </acceptance_criteria>
  <done>The shared client is proven equivalent to the old 353 guard, the two optional fields work and refuse by naming the key, and the 353 builder is either a byte-compatible re-exporter or untouched with a written deferral.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-time builder -> api.typesafe.ai | every payload crosses here; the guard is the last check before the network |
| 356 working tree -> peer sessions' uncommitted diffs | three unowned dirty files depend on the 353 builder's exports |
| secrets file / env -> process memory | the key is read here and must never be written or printed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-04 | Tampering | EGRESS_PROFILES.section_command_ledger | mitigate | per-profile objects, never a union (D-08); the parity corpus recorded from the pre-extraction guard must replay identically, messages included |
| T-356-05 | Information disclosure | loadKey | mitigate | returns the value only; test leg (g) captures console and stdout writes during loadKey and asserts none; errors never carry the value |
| T-356-06 | Tampering | eval-icm-writers.cjs, test-353-grader-agreement.cjs, test-353-ledger-shape.cjs | mitigate | never opened for write; refactor only when they are clean, else deferred; md5 of their diffs compared before and after |
| T-356-07 | Elevation of privilege | jev() | mitigate | jev throws when no guard function is supplied; the guard runs before fetchImpl (legs c and d) |
| T-356-14 | Information disclosure | must_equal_file / max_len_by_key | mitigate | refuse, never strip; the error names the key; byte comparison against bytes read once at construction; a rel path escaping opts.root is refused |
| T-356-SC | Tampering | package installs | accept | no packages installed; only node:fs and node:path are required |
</threat_model>

<verification>
- `env -u TYPESAFE_API_KEY node tests/test-356-jev-client.cjs` exits 0
- The four 353 suites report their Task 1 baseline counts
- `git status --short -- scripts/eval-icm-writers.cjs tests/test-353-grader-agreement.cjs tests/test-353-ledger-shape.cjs` shows the same lines as the preflight (or the SUMMARY names the peer commit)
</verification>

<success_criteria>
One shared dev-time Jev client exists in scripts/, with the D-07 interface, the D-09 error shape and the two optional profile fields agreed with 357; the section_command_ledger profile is provably identical to 353's guard; the 353 builder is refactored only if its unowned consumers are clean, otherwise the refactor is deferred in writing.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `scripts/jev-devtime-client.cjs` (new, or extended additively if a peer extracted it first)
- `tests/fixtures/356-section-guard-parity.json` (new)
- `tests/test-356-jev-client.cjs` (new)
- `scripts/build-section-command-ledger.cjs` (modified only on the REFACTOR branch)
- `.planning/phases/356-.../deferred-items.md` (appended only on the DEFER branch)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-02-SUMMARY.md` when done (`git add -f`, explicit path). It must contain "## Preflight" (git status lines, unowned-diff md5, 353 baselines), the branch taken (ABSENT/PRESENT), the refactor outcome (REFACTOR/DEFER), the final profile names in EGRESS_PROFILES, and "## Peer messages" with the exact text sent or to relay.
</output>
