---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 08
type: execute
wave: 3
depends_on: ["356-06"]
files_modified:
  - data/jev-policies/command-irreversibility.json
  - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md
  - data/jev-labels/command-irreversibility.json
  - tests/test-356-policy.cjs
  - tests/test-356-larry-contract.cjs
autonomous: false
requirements: [R356-01, R356-02]
tags: [human-checkpoint, sitting-2, policy-lock, prelabel-review, answer-key]

must_haves:
  truths:
    - "The navigator has locked the irreversibility policy wording (D-02); the committed policy keeps criteria as a {true, false} object (D-01 amended), the D-03 narrower-than-material sentence, boundary cases for every export and share command, and no em-dash"
    - "The navigator has ruled on every remainder row of Claude's pre-labels (confirm, or correct with a reason), and a bulk confirmation, if given, is recorded as such"
    - "data/jev-labels/command-irreversibility.json exists in the D-05 shape: command set equal to the registry's, reviewed_by 'navigator', reviewed_at, method, blind_sheet_ref, prelabels_sha256, the blind-vs-Claude disagreement rate, an empty appeal_rulings list, and a label_source per row"
    - "Policy and answer key are committed before any live Jev call"
  artifacts:
    - path: "data/jev-policies/command-irreversibility.json"
      provides: "Navigator-locked irreversibility policy, version 1"
      contains: "\"boundary_cases\""
    - path: "data/jev-labels/command-irreversibility.json"
      provides: "The navigator-reviewed answer key for every registry command"
      contains: "jev-answer-key/v1"
    - path: ".planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md"
      provides: "The navigator's per-row rulings on Claude's remainder pre-labels"
      contains: "your ruling"
  key_links:
    - from: "data/jev-labels/command-irreversibility.json blind_sheet_ref"
      to: "356-BLIND-LABEL-SHEET.md at its filled commit"
      via: "path@sha reference written by --merge"
      pattern: "356-BLIND-LABEL-SHEET.md@"
---

<objective>
SITTING 2 of 3 with the navigator: lock the policy wording (Checkpoint B) and review Claude's pre-labels for the remainder row by row (Checkpoint C), back to back in one sitting, then commit the locked policy and the merged answer key.

Purpose: SPEC R1 (policy text, navigator-locked), R2 (answer key covering exactly the registry, reviewer and date recorded), D-02, D-04, D-05.
Output: the locked policy, the filled review sheet, and `data/jev-labels/command-irreversibility.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-06-SUMMARY.md

One sitting: present Checkpoint B, then Checkpoint C immediately after B resumes; tell the navigator at the start that this sitting has two parts (about 15 to 20 minutes). Parallel plan 356-09 (same wave) edits the scoring builder; this plan never edits it, and only requires its `buildPayload`, `readPolicy` and `makeGuard` exports for a validation run (if the require fails because 356-09 is mid-edit, wait for 356-09's next commit to that file and retry; never patch the builder here). Explicit-path commits only; `git add -f` for `.planning/` files. No Jev call happens in this plan.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: CHECKPOINT B (sitting 2, part 1) - the navigator locks the policy wording</name>
  <files>data/jev-policies/command-irreversibility.json</files>
  <read_first>
    - data/jev-policies/command-irreversibility.json (the draft from 356-06)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-01 amended, D-02, D-03, D-15)
  </read_first>
  <action>Show the navigator the draft policy file in full (it is short), then present the decision below.</action>
  <decision>Lock the wording of the irreversibility policy that Jev reads for every command</decision>
  <context>The policy is part of the data contract: the Jev docs name literal reading as the first failure mode, so the boundary cases carry most of the accuracy (D-02). Three things are fixed by earlier rulings and should stay: criteria is the Noul-native object with a "true" and a "false" sentence (D-01 amended); the sentence "Irreversible is narrower than material: every irreversible step is material, but not every material step is irreversible." is a tested contract line (D-03; if you reword it, the two tests that look for it are updated to your wording); the threshold rule is already locked (D-15: fail loud if a labeled-irreversible command scores 0, if nothing is labeled irreversible, or if the threshold would flag every command). Plain language, no em-dashes. The file is capped at 8000 characters because it travels with every call.</context>
  <options>
    <option id="lock-as-drafted">
      <name>Lock as drafted</name>
      <pros>No edits; sitting moves straight to the pre-label review</pros>
      <cons>Boundary cases are Claude's wording</cons>
    </option>
    <option id="lock-with-edits">
      <name>Lock with my edits</name>
      <pros>Your wording becomes the contract; edit the file in place or paste replacement text in chat and it is applied verbatim</pros>
      <cons>A few minutes more</cons>
    </option>
    <option id="redraft">
      <name>Redraft first</name>
      <pros>Use when the framing is wrong, not just the words; say what to change and Claude redrafts before you lock</pros>
      <cons>Adds a round inside this sitting</cons>
    </option>
  </options>
  <resume-signal>Select lock-as-drafted, lock-with-edits (then edit or paste), or redraft (say what to change)</resume-signal>
  <acceptance_criteria>
    - The navigator's selection and any pasted wording are recorded verbatim in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('data/jev-policies/command-irreversibility.json','utf8'))"</automated>
  </verify>
  <done>The navigator has chosen how the policy is locked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT C (sitting 2, part 2) - the navigator reviews Claude's pre-labels for the remainder</name>
  <files>.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md</files>
  <read_first>
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md (row count only; the navigator reads the rows)
  </read_first>
  <action>Present the checkpoint below right after Checkpoint B resumes, in the same sitting. If B was redraft, first produce the redraft, show it, and get an explicit lock before moving to this checkpoint. Record B's final wording choice; Task 3 applies, validates and commits it.</action>
  <what-built>A review sheet with Claude's sealed pre-labels for every command outside your blind subset (the commands tagged autonomous_safe that no rule picked). These are exactly the commands the SPEC names as the real exposure: tagged safe, possibly irreversible in behavior.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md`.
    2. For each row, fill `your ruling`: `ok` to confirm Claude's label, or `y` / `n` to set the label yourself, with a one-line reason in `your reason` whenever you change it.
    3. Save the file, or reply in chat with one line per row (`/mos:<name>: ok`, or `/mos:<name>: y - <reason>`). If you want to confirm every remaining row without reading each one, say so in those words; it is recorded as a bulk confirmation in the answer key's method field.
  </how-to-verify>
  <resume-signal>Type "reviewed" after saving the sheet, or paste your per-row rulings</resume-signal>
  <acceptance_criteria>
    - The navigator's resume signal is recorded verbatim in the SUMMARY, including whether a bulk confirmation was given
  </acceptance_criteria>
  <verify>
    <automated>git ls-files --error-unmatch .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md</automated>
  </verify>
  <done>Every remainder row has the navigator's ruling.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the locked policy, transcribe the review, merge and commit the answer key</name>
  <files>data/jev-policies/command-irreversibility.json, .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md, data/jev-labels/command-irreversibility.json, tests/test-356-policy.cjs, tests/test-356-larry-contract.cjs</files>
  <read_first>
    - data/jev-policies/command-irreversibility.json
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-06-SUMMARY.md (the filled blind-sheet commit sha)
    - scripts/irreversibility-answer-key.cjs (the `--merge` mode)
    - tests/test-356-policy.cjs and tests/test-356-larry-contract.cjs (`grep -n "narrower than material"` in each)
  </read_first>
  <action>
    1. Policy: apply the navigator's wording verbatim (from the file as he saved it, or from the pasted text). Keep `version: 1`. Validate with node: the five fields exist; `criteria` is a plain object with string `true` and `false`; `boundary_cases` is a non-empty array of strings that still names `/mos:export`, `/mos:snapshot`, `/mos:vault`, `/mos:present`, `/mos:publish`; no em-dash; no `larry-extended`; at most 8000 characters; every registry payload still passes the egress guard (`node -e` using `buildPayload`, `readPolicy` and `makeGuard` from scripts/build-command-irreversibility-ledger.cjs with the repo root). If the navigator removed a named slug or broke a rule, show him the specific failure and ask; never silently restore Claude's text. If he reworded the D-03 sentence, update the `/irreversible is narrower than material/i` regex in tests/test-356-policy.cjs and tests/test-356-larry-contract.cjs to a regex over his locked phrase, and record the change. Commit by explicit path: `feat(356-08): lock irreversibility policy v1 (navigator, D-02)` (include the two test files only if they changed).
    2. Review: transcribe the navigator's rulings into the sheet exactly as given (bulk confirmation: write `ok` in every empty ruling cell and add the header line `bulk_confirmed: true`). Parse with `parseReviewSheet`; every row needs a ruling, and every `y`/`n` that differs from Claude's label needs a reason; if not, ask the navigator for the missing rows. Commit by explicit path (`git add -f`): `docs(356-08): navigator rulings on Claude pre-labels (remainder)`.
    3. Merge: `node scripts/irreversibility-answer-key.cjs --merge --blind .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md --review .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-PRELABEL-REVIEW-SHEET.md --prelabels .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json --out data/jev-labels/command-irreversibility.json --blind-sheet-ref ".planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md@<filled-sheet commit sha from 356-06-SUMMARY>" --reviewed-at <YYYY-MM-DD>`. If the review sheet has `bulk_confirmed: true`, append " (remainder confirmed in bulk by the navigator)" to `method` in the written file. If the merge refuses with ANSWER_KEY_REFUSED because the registry gained or lost a command since the pre-labels were sealed, stop and report the exact commands (a new command needs a sealed addendum pre-label and a navigator ruling; do not improvise one).
    4. Validate the answer key with node: row set equals the registry command set; `reviewed_by === 'navigator'`; every row has a boolean, a non-empty reason, a valid `label_source`; at least one row is labeled true (D-15 needs positives; `/mos:publish` is expected to be one); `appeal_rulings` is an empty array. Commit by explicit path: `feat(356-08): navigator-reviewed irreversibility answer key (D-05)`.
    5. Run `node tests/test-356-label-sheet.cjs`, `node tests/test-356-policy.cjs`, `node tests/test-356-larry-contract.cjs`. Tell the navigator the blind-vs-Claude disagreement (`disagreed` of `compared`, and the rate) now that both label sets are in git, and that sitting 3 is the live-build authorization once the builder's fixture suite is green.
  </action>
  <verify>
    <automated>node tests/test-356-label-sheet.cjs && node tests/test-356-policy.cjs && node tests/test-356-larry-contract.cjs && node -e "const k=require('./data/jev-labels/command-irreversibility.json');const r=require('./data/command-registry.json');const a=new Set(k.rows.map(x=>x.command)),b=new Set(r.commands.map(x=>x.command));const S=['navigator-blind','claude-prelabel/navigator-confirmed','navigator-corrected'];const ok=k.schema==='jev-answer-key/v1'&&k.reviewed_by==='navigator'&&typeof k.reviewed_at==='string'&&a.size===b.size&&[...b].every(x=>a.has(x))&&k.rows.every(x=>typeof x.irreversible==='boolean'&&x.reason&&S.includes(x.label_source))&&k.rows.some(x=>x.irreversible)&&Array.isArray(k.appeal_rulings)&&k.appeal_rulings.length===0;process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The answer-key verify one-liner exits 0
    - `node tests/test-356-policy.cjs` exits 0 with its shipped-policy legs live (not PENDING)
    - `node tests/test-356-label-sheet.cjs` and `node tests/test-356-larry-contract.cjs` exit 0
    - `git log -1 --format=%s -- data/jev-labels/command-irreversibility.json` starts with `feat(356-08)`
    - `test ! -e data/command-irreversibility-ledger.json` (still no Jev call)
    - `grep -c "$(printf '\xe2\x80\x94')" data/jev-policies/command-irreversibility.json data/jev-labels/command-irreversibility.json` reports 0 for both
    - The SUMMARY records the policy decision, the review resume signal, the bulk flag, and the disagreement numbers
  </acceptance_criteria>
  <done>The policy is locked and the answer key is in git, both navigator-owned, before any Jev call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator's words -> policy and answer key | the navigator owns both; transcription must be exact |
| answer key -> threshold | the key sets the threshold; a key shaped to fit Jev would hide misses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-18 | Spoofing | answer key provenance | mitigate | label_source per row, reviewer handle, blind_sheet_ref pinned to a commit, bulk confirmation recorded in method; committed before any Jev call |
| T-356-12 | Tampering | pre-label reveal | mitigate | --merge refuses unless sha256 of the pre-label file equals the blind sheet's prelabels_sha256 |
| T-356-14 | Information disclosure | locked policy text | mitigate | re-validated against the egress guard for every registry row; no names, no room content, 8000-character cap |
| T-356-19 | Information disclosure | data/ in the npm tarball | mitigate | reviewed_by is the role handle 'navigator'; reasons are about commands, not people |
| T-356-SC | Tampering | package installs | accept | no packages installed |
</threat_model>

<verification>
- The answer-key one-liner, `node tests/test-356-policy.cjs` and `node tests/test-356-label-sheet.cjs` all exit 0
- The policy lock commit and the answer-key commit both precede any ledger or raw-score file
</verification>

<success_criteria>
One sitting produces a navigator-locked policy and a navigator-reviewed answer key covering every registry command, both committed before Jev is ever called.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `data/jev-policies/command-irreversibility.json` (locked, version 1)
- `.planning/phases/356-.../356-PRELABEL-REVIEW-SHEET.md` (filled)
- `data/jev-labels/command-irreversibility.json` (new, the answer key)
- `tests/test-356-policy.cjs`, `tests/test-356-larry-contract.cjs` (only if the D-03 sentence was reworded)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-08-SUMMARY.md` when done (`git add -f`, explicit path). Record the policy decision and final hash, the review resume signal, the bulk flag, the answer-key commit sha, the count of labeled-irreversible rows, and the blind-vs-Claude disagreement.
</output>
