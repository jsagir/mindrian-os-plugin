---
phase: 356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json
autonomous: true
requirements: [R356-02]
tags: [answer-key, labeling, sealed, d-16]

must_haves:
  truths:
    - "Claude has labeled every registry command irreversible true/false with a one-line reason, judged only against the SPEC R1 definition (no policy draft exists yet), before the navigator sees anything (D-04, D-16)"
    - "The pre-label file is SEALED: committed by explicit path before any blind sheet exists, its sha256 recorded, and its labels never printed into any output the navigator reads until the blind sheet is committed"
    - "The pre-label command set equals the registry command set exactly, enumerated at run time, never a hard-coded 113"
  artifacts:
    - path: ".planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json"
      provides: "Sealed Claude pre-labels: {schema, written_by, written_at, registry_hash, rubric, rows:[{command, irreversible, reason}]}"
      contains: "claude-prelabels/v1"
  key_links:
    - from: "356-CLAUDE-PRELABELS.json"
      to: "356-BLIND-LABEL-SHEET.md (356-06)"
      via: "sha256 commitment written into the sheet header as prelabels_sha256"
      pattern: "prelabels_sha256"
---

<objective>
Write Claude's irreversibility pre-labels for every registry command and seal them in git before the navigator labels anything (D-04 hybrid labeling, D-16 labeling order).

Why sealed and first: D-04's blind subset includes every command Claude calls irreversible, so the pre-labels must exist before the sheet is picked; D-16 requires they stay unseen until the navigator's blind sheet is committed, so the blind-vs-Claude disagreement rate (D-05) measures real LLM bias. The seal is a sha256 commitment that 356-06 writes into the blind sheet header, so the pre-labels cannot be quietly changed after the navigator's labels are visible.

Purpose: SPEC R2 (hand-labeled answer key), D-04, D-05, D-16.
Output: `356-CLAUDE-PRELABELS.json`, committed. No code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md
@.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md

SEALING RULE for this whole plan: never print any row's label (true/false) or reason to stdout, the SUMMARY, or the plan-completion message. Report only the row count, the file's sha256 and the commit sha. The navigator must be able to read this plan's SUMMARY without learning a single pre-label.

The only rubric allowed here is the SPEC R1 definition, verbatim: irreversible means "running the command takes an effect outside the machine or outside the user's control that the room's own history cannot undo": send, email, publish, deploy, share or upload to a third party, external write. Local room writes are NOT irreversible. Boundary: export to a local file is reversible; export that uploads is irreversible. Do not read or write any `data/jev-policies/` file in this plan (the policy is drafted only after the blind sheet is committed, D-16).

Parallel-session rules (D-11): explicit-path commits, `git add -f` for `.planning/` files, never `git add -A`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evidence scan over every registry command (blurb plus targeted body grep)</name>
  <files>(scratchpad only; nothing in the repo)</files>
  <read_first>
    - data/command-registry.json (via a node one-liner that prints, for every entry in `commands`, the `command`, `teaching` and `jtbd_summary`; read all of them; do NOT print `autonomous_safe`, so the audited tag does not anchor your judgment)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-SPEC.md (Requirement 1 definition and the Background section)
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-04, D-10, the "Specific Ideas" boundary cases)
  </read_first>
  <action>
    1. Enumerate `require('./data/command-registry.json').commands` at run time. For each entry, derive `name` = the slug without the `/mos:` prefix and run `grep -n -i -E "vercel|netlify|gh-pages|git push|npm publish|curl |https?://|webhook|smtp|gmail|slack|upload|deploy|publish|send|email|share" commands/<name>.md` (skip silently when the file does not exist).
    2. For every command with at least one hit, read the hit lines with 3 lines of context and classify each hit as ACTION (the command itself performs the effect, for example it runs a deploy script or posts to a service) or ADVICE (prose telling the user they could do something themselves later, for example "deploy this to Vercel"). D-10 notes the export, snapshot and vault bodies contain advice lines; advice is not an effect of running the command.
    3. Write the evidence to a file in the session scratchpad (never in the repo): one line per command with slug, a one-phrase summary of what running it does, and the ACTION/ADVICE classification of each hit. This evidence is Claude's working notes only; it is not committed and not shown to the navigator.
  </action>
  <verify>
    <automated>node -e "const r=require('./data/command-registry.json');if(!Array.isArray(r.commands)||r.commands.length===0)process.exit(1);console.log('registry commands: '+r.commands.length)"</automated>
  </verify>
  <acceptance_criteria>
    - The scratchpad evidence file has exactly one line per registry command (count equals `node -e "console.log(require('./data/command-registry.json').commands.length)"`)
    - `git status --short` shows no new repo file from this task
  </acceptance_criteria>
  <done>Every command has a grounded one-phrase description of what it does, with advice lines separated from real effects.</done>
</task>

<task type="auto">
  <name>Task 2: Write, validate and seal 356-CLAUDE-PRELABELS.json</name>
  <files>.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json</files>
  <read_first>
    - the scratchpad evidence file from Task 1
    - .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CONTEXT.md (D-05 row shape, D-16)
  </read_first>
  <action>
    1. Label every command under the SPEC R1 definition only: `irreversible: true` when running it takes an effect outside the machine or outside the user's control that the room's own history cannot undo (send, email, publish, deploy, share or upload to a third party, external write); otherwise `false`. Local room writes, local file exports and advice prose are `false`. Reason: one line, at most 160 characters, plain language, hyphens only, naming the concrete effect (or its absence).
    2. Write `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json`, pretty-printed (2 spaces) with a trailing newline: `{ "schema": "claude-prelabels/v1", "written_by": "claude (356-04 executor)", "written_at": "<ISO timestamp>", "registry_hash": "<sha256 hex of the bytes of data/command-registry.json>", "rubric": "SPEC R1 definition sentence only; no policy draft existed when these labels were written", "rows": [ { "command", "irreversible", "reason" } ] }`, rows sorted by `command`.
    3. Validate with a node one-liner (print only PASS/FAIL and counts, never labels): row command set equals the registry command set (no missing, no extra, no duplicates); every `irreversible` is boolean; every `reason` is a non-empty string of at most 160 characters with no em-dash; `registry_hash` equals the current registry file hash.
    4. Seal: `sha256sum` the file and record the digest. Commit by explicit path: `git add -f .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json` then `git commit -m "docs(356-04): seal Claude irreversibility pre-labels (hidden from the navigator until the blind sheet is committed, D-16)"`. Record the commit sha and the digest in the SUMMARY.
    5. The SUMMARY states: row count, sha256, commit sha, and the sentence "Labels are sealed; do not open this file until 356-06 commits the navigator's blind sheet." It contains no label and no reason.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),c=require('crypto');const P='.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json';const p=JSON.parse(fs.readFileSync(P,'utf8'));const r=require('./data/command-registry.json');const a=new Set(p.rows.map(x=>x.command)),b=new Set(r.commands.map(x=>x.command));const ok=p.schema==='claude-prelabels/v1'&&a.size===p.rows.length&&a.size===b.size&&[...b].every(x=>a.has(x))&&p.rows.every(x=>typeof x.irreversible==='boolean'&&typeof x.reason==='string'&&x.reason.length>0&&x.reason.length<=160&&x.reason.indexOf('\u2014')===-1)&&p.registry_hash===c.createHash('sha256').update(fs.readFileSync('data/command-registry.json')).digest('hex');console.log(ok?'PRELABELS PASS rows='+p.rows.length:'PRELABELS FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify one-liner prints `PRELABELS PASS` and exits 0
    - `git ls-files --error-unmatch .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json` exits 0 (tracked despite .gitignore)
    - `git log -1 --format=%s -- .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-CLAUDE-PRELABELS.json` starts with `docs(356-04): seal`
    - `test ! -e .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-BLIND-LABEL-SHEET.md` at the time of the seal commit (the seal predates the sheet)
    - `grep -c '"irreversible"' .planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-04-SUMMARY.md` is 0 (the SUMMARY leaks no label)
  </acceptance_criteria>
  <done>Claude's pre-labels exist, cover the registry exactly, and are sealed in git with a recorded digest before any blind sheet exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude's labels -> navigator's blind labels | Claude's judgment must not reach the navigator before the blind sheet is committed |
| sealed file -> later reveal | the revealed pre-labels must be the ones written before the blind labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-356-12 | Tampering | 356-CLAUDE-PRELABELS.json | mitigate | committed before any sheet exists; sha256 recorded here and written into the blind sheet header by 356-06; 356-10's answer-key test re-hashes the file against that header |
| T-356-13 | Information disclosure | pre-label labels and reasons | mitigate | never printed; the SUMMARY carries only count, digest and commit sha; acceptance greps the SUMMARY for label keys |
| T-356-18 | Spoofing | label provenance | mitigate | written_by names the executor role, rubric states no policy existed; the navigator, not Claude, is the reviewer of record (D-05) |
| T-356-SC | Tampering | package installs | accept | no packages installed |
</threat_model>

<verification>
- The Task 2 verify one-liner prints `PRELABELS PASS`
- The pre-label file is tracked and its commit precedes any 356-BLIND-LABEL-SHEET.md commit
</verification>

<success_criteria>
Sealed, complete, registry-exact Claude pre-labels exist in git with a recorded digest, and nothing the navigator reads reveals them.
</success_criteria>

## Artifacts this phase produces

This plan's share of the Phase 356 artifacts:
- `.planning/phases/356-.../356-CLAUDE-PRELABELS.json` (new, sealed; revealed in 356-08)

<output>
Create `.planning/phases/356-chain-executor-material-step-ledger-jev-noul-seat-scored-at-/356-04-SUMMARY.md` when done (`git add -f`, explicit path) with ONLY: row count, sha256 digest, seal commit sha, and the sealing sentence. No labels, no reasons.
</output>
