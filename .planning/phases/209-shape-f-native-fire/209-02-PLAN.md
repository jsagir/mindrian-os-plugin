---
phase: 209-shape-f-native-fire
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/stamp-firing-block.cjs
  - commands/*.md (the 99 hitl_shape-declaring command bodies; futures.md frontmatter is NOT touched here - plan 03 owns the drift fix)
  - tests/test-209-stamp-firing-block.cjs
autonomous: true
requirements: [B1]
must_haves:
  truths:
    - "Every hitl_shape-declaring command body carries the canonical firing block or a pre-existing AskUserQuestion dispatch instruction"
    - "Every declaring command whose allowed-tools list exists and is restrictive now grants AskUserQuestion"
    - "Re-running the stamp is byte-identical (idempotent); the 18 bodies that already mention AskUserQuestion are not double-stamped"
    - "The stamp marker is machine-detectable (it doubles as the B3 wired predicate)"
  artifacts:
    - path: "scripts/stamp-firing-block.cjs"
      provides: "Idempotent two-part stamp: canonical firing block + allowed-tools grant"
      exports: ["stampBody", "grantTool", "runStamp"]
      min_lines: 120
    - path: "tests/test-209-stamp-firing-block.cjs"
      provides: "Idempotency + skip-list + grant-semantics proof"
      min_lines: 60
  key_links:
    - from: "scripts/stamp-firing-block.cjs"
      to: "commands/rooms.md"
      via: "canonical firing paragraph extracted as the stamp text"
      pattern: "appendAskUserQuestionTrailer"
    - from: "commands/*.md"
      to: "scripts/build-render-coverage.cjs"
      via: "the stamp marker consumed by plan 03's B3 wired predicate"
      pattern: "mos:firing-block"
---

<objective>
Fix ID B1. RC-4: the Phase 190 backfill was frontmatter-only by contract, so 99 commands declare hitl_shape but their bodies never instruct the fire and zero grant the tool. Write an idempotent stamp script (modeled on scripts/backfill-hitl-shape.cjs) that inserts the canonical firing block into every declared-but-unwired command body and adds AskUserQuestion to restrictive allowed-tools lists in the same pass (the two-part delta, verdict 20). Run it.

Live counts (verified 2026-07-02, 209-PATTERNS.md): 99 declaring commands, 18 bodies already mention AskUserQuestion (skip the body stamp, still fix the grant), 0 grant the tool in an existing allowed-tools list.

Purpose: flips the command plane to wired so plan 03's B3 gate lands green, not red-flooding CI (LOCKED sequencing).
Output: new stamp script, ~99 patched command files, test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-shape-f-native-fire/209-CONTEXT.md
@.planning/phases/209-shape-f-native-fire/209-PATTERNS.md
@scripts/backfill-hitl-shape.cjs
@commands/rooms.md
@commands/think-hats.md
</context>

## Artifacts this phase produces (this plan's share)

- `scripts/stamp-firing-block.cjs` - new script; exports `stampBody(content, opts)`, `grantTool(content)`, `runStamp(opts)`, and the constants `STAMP_MARKER` and `CANONICAL_FIRING_BLOCK`
- `STAMP_MARKER` - the HTML-comment sentinel `<!-- mos:firing-block v1 -->` (the machine-detectable line plan 03's B3 "wired" predicate keys on)
- CLI flags on the new script: `--check` (dry-run, exit non-zero on pending deltas) and default apply mode
- ~99 modified `commands/*.md` files (body stamp and/or allowed-tools grant)
- `tests/test-209-stamp-firing-block.cjs` - new test file

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write scripts/stamp-firing-block.cjs (idempotent two-part stamp)</name>
  <files>scripts/stamp-firing-block.cjs, tests/test-209-stamp-firing-block.cjs</files>
  <read_first>
    - scripts/backfill-hitl-shape.cjs :56 (renderShapeLines), :84 (stripShapeLines), :113-148 (patchSurface - the strip-then-reinsert byte-stable idiom and the "already-conformant file returns byte-identical" guarantee), :157 (runBackfill map-driven iterate + --check), :211 (main)
    - commands/rooms.md :24-27, :119-126, :466-468 (the thrice-repeated canonical firing paragraph - extract ONE canonical text from these; it names renderShapeF1 / appendAskUserQuestionTrailer / "no bespoke widget")
    - commands/think-hats.md :21-25 (allowed-tools is a MULTI-LINE YAML list - the grant is a list-item append, not a scalar edit)
    - .planning/phases/209-shape-f-native-fire/209-PATTERNS.md ("B1" pattern assignment + Verification Duties table)
  </read_first>
  <behavior>
    - Test 1 (idempotency): stamping an already-stamped body returns byte-identical content; running runStamp twice produces zero second-pass deltas
    - Test 2 (skip list): a body already containing the token 'AskUserQuestion' is NOT body-stamped (no marker inserted) but its restrictive allowed-tools list still gains the grant
    - Test 3 (grant semantics): a file with an existing allowed-tools list lacking AskUserQuestion gains exactly one new list item; a file with NO allowed-tools key is left WITHOUT one (absent means unrestricted - adding a list would RESTRICT the command, a regression); a list already granting the tool is untouched
    - Test 4 (frontmatter safety): the frontmatter block is preserved byte-for-byte except the grant line; a file with no frontmatter or no hitl_shape declaration is skipped entirely; hitl_shape 'none' is skipped
    - Test 5 (--check): dry-run mode writes nothing and exits non-zero while deltas are pending, zero when clean
  </behavior>
  <action>
    Create scripts/stamp-firing-block.cjs following the backfill-hitl-shape.cjs skeleton (REPO_ROOT + path constants, pure functions exported, require.main-guarded main, --check vs apply):

    1. `CANONICAL_FIRING_BLOCK`: one paragraph extracted from the rooms.md triplet, prefixed by the sentinel line `STAMP_MARKER = '<!-- mos:firing-block v1 -->'`. The block states: at this command's Decision Gate, fire the AskUserQuestion card composed by lib/hmi/shape-f1-renderer.cjs (renderShapeF1) and fired via lib/hmi/selector-dispatcher.cjs (appendAskUserQuestionTrailer); never reproduce the selector as text (SEED-021); no bespoke widget. Keep it shape-generic (the declared shape varies; reference "the declared hitl_shape" rather than hard-coding F.1).
    2. `stampBody(content, opts)`: parse frontmatter with the same `/^---\r?\n([\s\S]*?)\r?\n---/` regex patchSurface uses. Skip when: no frontmatter, no hitl_shape key, hitl_shape is 'none', body already contains STAMP_MARKER, or body already contains 'AskUserQuestion' (the 18 pre-wired bodies). Otherwise insert the marker + block immediately after the frontmatter close (before the first body heading), separated by blank lines. Strip-then-reinsert on the marker for byte-stable re-runs.
    3. `grantTool(content)`: only when the frontmatter contains an `allowed-tools:` key. Handle BOTH YAML dialects present in commands/ (multi-line list per think-hats.md:21-25, and inline scalar/array if any exist - inspect 3-4 declaring commands to confirm which dialects are live before coding). Append `AskUserQuestion` as a list item if absent. NEVER create the key when missing (absent = unrestricted; creating it would restrict).
    4. `runStamp({ check })`: walk commands/*.md (readdir + .md filter + sort, mirroring backfill's iterate), apply stampBody then grantTool per file, collect { file, bodyStamped, toolGranted, skippedReason }. In --check mode print the pending table and exit 1 if any delta pending; in apply mode write files and print the summary counts.
    5. main(): argv switch-case router (gsd-tools.cjs pattern; no Commander). No em-dashes in the block text or comments (double hyphen ok).

    Test file: run stampBody/grantTool against in-memory fixtures for behaviors 1-4 (never against live commands/), and runStamp with a cwd-scoped temp dir fixture for behavior 5.
  </action>
  <verify>
    <automated>node tests/test-209-stamp-firing-block.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-209-stamp-firing-block.cjs` exits 0
    - `grep -c "mos:firing-block v1" scripts/stamp-firing-block.cjs` returns >= 1
    - `node -e "const s=require('./scripts/stamp-firing-block.cjs'); if(typeof s.stampBody!=='function'||typeof s.grantTool!=='function') process.exit(1)"` exits 0
    - stampBody applied twice to the same fixture returns byte-identical output (asserted in the test)
  </acceptance_criteria>
  <done>Stamp script exists, idempotent, two-part delta, grant semantics correct for both YAML dialects and the absent-key case; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Run the stamp across commands/ and verify the live counts</name>
  <files>commands/*.md (the 99 declaring commands)</files>
  <read_first>
    - .planning/phases/209-shape-f-native-fire/209-PATTERNS.md (Verification Duties: 99 declare / 18 body-mention / 0 grant - your before-state ground truth)
    - scripts/stamp-firing-block.cjs (the script Task 1 produced)
  </read_first>
  <action>
    1. `node scripts/stamp-firing-block.cjs --check` - capture the pending table; expect ~81 body stamps pending (99 declaring minus 18 pre-wired) and grants pending only for files with an existing restrictive allowed-tools list.
    2. `node scripts/stamp-firing-block.cjs` (apply).
    3. `node scripts/stamp-firing-block.cjs --check` again - MUST exit 0 with zero pending (idempotency on the live tree).
    4. Spot-verify with git diff that: futures.md frontmatter hitl_shape line is UNCHANGED (plan 03 owns that drift fix); rooms.md and ignite.md bodies got NO new firing block (they mention AskUserQuestion already); think-hats.md allowed-tools gained the AskUserQuestion list item.
    5. Run the shape-declaration gate as it exists TODAY (pre-B2): `node scripts/check-shape-declaration.cjs` must still exit 0 (the stamp must not break the current validator).
  </action>
  <verify>
    <automated>node scripts/stamp-firing-block.cjs --check && node scripts/check-shape-declaration.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "mos:firing-block v1" commands/ | wc -l` returns approximately 81 (99 declaring minus the 18 pre-wired bodies; record the exact number in the SUMMARY)
    - `grep -c "AskUserQuestion" commands/think-hats.md` returns >= 1 (grant landed on the confirmed anti-exemplar)
    - `node scripts/stamp-firing-block.cjs --check` exits 0 after apply (zero pending deltas)
    - `node scripts/check-shape-declaration.cjs` exits 0
    - `git diff --stat commands/futures.md` shows body/grant changes only if applicable, with the `hitl_shape: "F.2"` frontmatter line byte-unchanged
  </acceptance_criteria>
  <done>Live tree stamped; counts recorded; re-run is a no-op; existing gates green; futures.md drift left for plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stamp script -> 99 command files in place | a bug corrupts the command plane wholesale (frontmatter breakage, double-stamp, YAML damage) |
| allowed-tools grant | frontmatter edits change what tools a command may invoke |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-05 | Tampering | stampBody frontmatter parse | mitigate | reuse patchSurface's proven frontmatter regex; frontmatter is byte-preserved except the grant line (Test 4); --check dry-run before apply; git provides full-file rollback |
| T-209-06 | Tampering | double-stamp / drift on re-run | mitigate | STAMP_MARKER sentinel + strip-then-reinsert; Test 1 asserts byte-identical re-run; post-apply --check exits 0 |
| T-209-07 | Elevation of Privilege | grantTool | mitigate | grant appended ONLY to files declaring hitl_shape AND already carrying a restrictive allowed-tools list; the absent-key case is left absent (never creates a list, never widens beyond AskUserQuestion - a render-only UI tool) |
| T-209-08 | Denial of Service | restricting unrestricted commands | mitigate | the absent-allowed-tools rule (Test 3): the script never CREATES an allowed-tools key, so no command loses tool access as a side effect |
| T-209-09 | Repudiation | which files changed and why | mitigate | runStamp prints a per-file { bodyStamped, toolGranted, skippedReason } table; SUMMARY records exact counts; single atomic commit |
</threat_model>

<verification>
- `node tests/test-209-stamp-firing-block.cjs` exits 0
- `node scripts/stamp-firing-block.cjs --check` exits 0 on the stamped tree
- `node scripts/check-shape-declaration.cjs` exits 0
- `node scripts/check-render-coverage.cjs` exits 0 (the .cjs keyspace untouched)
- No em-dashes: `grep -rP '\x{2014}' scripts/stamp-firing-block.cjs` returns nothing; the stamped block itself carries none
</verification>

<success_criteria>
- All 99 declaring commands satisfy the (body instruction OR marker) half of the wired predicate; grants added everywhere a restrictive list exists
- The stamp marker is in place as plan 03's B3 predicate token
- Re-run idempotent; zero collateral frontmatter damage
</success_criteria>

<output>
Create `.planning/phases/209-shape-f-native-fire/209-02-SUMMARY.md` when done. Record the exact stamped/granted/skipped counts.
</output>
