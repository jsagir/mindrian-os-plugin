---
phase: 209-shape-f-native-fire
plan: 05
type: execute
wave: 2
depends_on: [209-01]
files_modified:
  - lib/core/sensors/sensor-room-pick.cjs
  - lib/core/insight-sensors.cjs
  - tests/test-209-room-pick-sensor.cjs
autonomous: true
requirements: [E5]
must_haves:
  truths:
    - "A mid-dialogue room resume/switch fork (the incident's actual transport-less fork) now carries a rendered room-chooser card envelope + the imperative trailer BEFORE emission"
    - "The card is minted through renderRoomChooserCard -> pickShape (SEED-020 single door); no new renderer, no new dispatcher branch"
    - "The sensor rides the SENS spine (dispatchSensors chokepoint) exactly like the shipped Phase 203/205 sensors; a sensor fault never breaks the turn"
  artifacts:
    - path: "lib/core/sensors/sensor-room-pick.cjs"
      provides: "room-pick fork detector, sensor-gate-approach signature"
      exports: ["sensorRoomPick"]
      min_lines: 60
    - path: "lib/core/insight-sensors.cjs"
      provides: "sensorRoomPick registered in SENSOR_REGISTRY"
      contains: "sensorRoomPick"
    - path: "tests/test-209-room-pick-sensor.cjs"
      provides: "fire/no-fire/fault-degrade/envelope-content proof"
      min_lines: 60
  key_links:
    - from: "lib/core/sensors/sensor-room-pick.cjs"
      to: "lib/core/room-chooser.cjs"
      via: "renderRoomChooserCard(rooms)"
      pattern: "renderRoomChooserCard"
    - from: "lib/core/insight-sensors.cjs"
      to: "lib/core/sensors/sensor-room-pick.cjs"
      via: "SENSOR_REGISTRY entry"
      pattern: "sensor-room-pick"
---

<objective>
Fix ID E5 (Wave 3 of the research plan; L-size, the incident's actual fork). A conversationally-reached room pick (resume/switch mid-dialogue, no command, no engine dial) today carries NEITHER card-fire transport (RC-1 verdict 23) - the exact shape of the incident transcript. Detect the fork pre-emission and inject the renderRoomChooserCard envelope plus the Wave-1 imperative trailer, exactly as ignite Gate B0 does by script.

Placement decision (CONTEXT grants discretion between an intent-classifier arm and a SENS-spine sensor; 209-PATTERNS.md discretion note resolves it): the SENSOR path reuses the most shipped machinery - the sensor-gate-approach signature, the dispatchSensors chokepoint (the single governed reach path per CLAUDE.md architecture), and the Phase 203-03 context_block ride. Build the sensor.

E6 (trailer gate-likelihood enrichment) is explicitly OUT OF SCOPE (CONTEXT Deferred Ideas).

Purpose: puts a transport on the one fork that had none; the backstop stops being the mechanism users experience at room resume.
Output: new sensor + registration + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-shape-f-native-fire/209-CONTEXT.md
@.planning/phases/209-shape-f-native-fire/209-PATTERNS.md
@lib/core/insight-sensors.cjs
@lib/core/sensors/sensor-gate-approach.cjs
@lib/core/room-chooser.cjs
@scripts/room-naming-selector.cjs
</context>

## Artifacts this phase produces (this plan's share)

- `lib/core/sensors/sensor-room-pick.cjs` - new sensor module; exports `sensorRoomPick(_turn, tuple, ctx)`
- New `SENSOR_REGISTRY` entry in `lib/core/insight-sensors.cjs`
- The injected envelope content: room-chooser rendered text + `askuserquestion_marker` + `askuserquestion_binding` + an "INSTRUCTION FOR LARRY" imperative block (room-naming-selector pattern)
- `tests/test-209-room-pick-sensor.cjs` - new test file

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sensor-room-pick.cjs - detect the mid-dialogue room resume/switch fork</name>
  <files>lib/core/sensors/sensor-room-pick.cjs, tests/test-209-room-pick-sensor.cjs</files>
  <read_first>
    - lib/core/sensors/sensor-gate-approach.cjs :78 onward (the signature + null-guard idiom to COPY: sensorGateApproach(_turn, tuple, ctx) returns makeReach({...}) or null; never throws)
    - lib/core/insight-sensors.cjs :1-80 (module doctrine + makeReach), :572-654 (SENSOR_REGISTRY + dispatchSensors iteration order and what a sensor may return), and the Phase 203-03 expert-skill sensor entry (registered by commit 809ac89b - the most recent registration precedent, incl. how its output rides context_block)
    - lib/core/room-chooser.cjs :198-216 (renderRoomChooserCard(rooms) - already routes through pickShape('F.1'), so the trailer marker + binding attach at the SEED-020 door automatically) and the rooms-registry adapter in the same file (Phase 204-01 built it - reuse it to enumerate prior rooms; do NOT hand-roll a registry read)
    - scripts/room-naming-selector.cjs :154-166 (the shipped 'INSTRUCTION FOR LARRY: ... dispatch ... via AskUserQuestion' imperative block to mirror)
  </read_first>
  <behavior>
    - Fires (returns a reach/context payload): the current user message signals a room resume/switch (phrases like resuming work on / switch to / back to my <room> / which room, or a named prior room referenced while the session is bound elsewhere or unbound) AND at least one prior room exists in the registry
    - Does NOT fire: no prior rooms; the turn is already inside a command flow (ignite/rooms own their own gates); the message has no room-pick signal
    - Fault degrade: a throwing registry read or malformed ctx returns null (never throws into dispatchSensors)
    - Envelope content when fired: contains the rendered room-chooser text, the '[AskUserQuestion contract:' marker, the '[BINDING:' line, and an 'INSTRUCTION FOR LARRY' block naming AskUserQuestion
  </behavior>
  <action>
    Create lib/core/sensors/sensor-room-pick.cjs:
    1. `sensorRoomPick(_turn, tuple, ctx)` copying the sensor-gate-approach signature and null-guard idiom. Detection: a conservative keyword/pattern check on the current message text in tuple/ctx (read how sensor-gate-approach extracts the message) for resume/switch/room-pick intent, plus a prior-rooms-exist check via the room-chooser registry adapter (lazy require, try/catch, null on fault). Deliberately HIGH-PRECISION over high-recall: false fires inject a card where none belongs (the inverse anti-pattern); when unsure return null - the H3 backstop still covers misses.
    2. On fire: call `renderRoomChooserCard(rooms)`; compose the injected block as rendered.text + '\n' + rendered.askuserquestion_marker + '\n' + rendered.askuserquestion_binding + '\n' + an INSTRUCTION FOR LARRY paragraph modeled verbatim-adjacent on room-naming-selector.cjs:158-165 ('dispatch the room chooser via AskUserQuestion with the options above; when the user picks, resume/switch to that room; never reproduce this block as text (SEED-021)'). Return it in the shape dispatchSensors expects (mirror the 203-03 sensor's return contract exactly - reach signal + context_block ride).
    3. Part 8: registry read is LOCAL; room names render locally; zero Brain requires. No em-dashes.
    Tests: in-memory tuple/ctx fixtures + a stubbed registry for all four behaviors; assert the envelope substrings.
  </action>
  <verify>
    <automated>node tests/test-209-room-pick-sensor.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-209-room-pick-sensor.cjs` exits 0
    - The fired envelope contains ALL of: '[AskUserQuestion contract:', '[BINDING:', 'INSTRUCTION FOR LARRY' (source assertions in the test)
    - `grep -c "renderRoomChooserCard" lib/core/sensors/sensor-room-pick.cjs` returns >= 1 and `grep -c "renderShapeF" lib/core/sensors/sensor-room-pick.cjs` returns 0 (SEED-020: no direct renderer call)
    - A throwing registry stub yields null, not an exception (asserted)
  </acceptance_criteria>
  <done>Sensor detects the incident fork with high precision, mints only through the room-chooser/pickShape door, degrades on any fault.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register sensorRoomPick in SENSOR_REGISTRY and prove the spine ride</name>
  <files>lib/core/insight-sensors.cjs, tests/test-209-room-pick-sensor.cjs</files>
  <read_first>
    - lib/core/insight-sensors.cjs :572 (SENSOR_REGISTRY array - append exactly like the Phase 203-03/205 sensors did; note the canonical-order comment), :637-654 (dispatchSensors loop + fault isolation), :658 (module.exports incl. SENSOR_REGISTRY)
  </read_first>
  <behavior>
    - dispatchSensors over a room-pick fixture turn includes the room-pick signal in its collected output; a non-room-pick turn's dispatch output is unchanged from before this plan
    - A sensorRoomPick throw (forced via stub) is isolated by dispatchSensors (other sensors still run)
  </behavior>
  <action>
    Append the lazy-required sensorRoomPick to SENSOR_REGISTRY (:572) following the exact registration idiom of the most recently added sensor (203-03). Do not reorder existing sensors. Extend the test file with the two spine behaviors, driving dispatchSensors directly with fixtures.
  </action>
  <verify>
    <automated>node tests/test-209-room-pick-sensor.cjs && node -e "const s=require('./lib/core/insight-sensors.cjs'); if(!s.SENSOR_REGISTRY.some(f=>String(f.name||f).includes('RoomPick')||String(f).includes('room-pick')))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sensor-room-pick" lib/core/insight-sensors.cjs` returns >= 1
    - `node tests/test-209-room-pick-sensor.cjs` exits 0 including the dispatch-spine assertions
    - `bash tests/run-all-209.sh` shows the room-pick leg PASS (leg pre-declared by 209-04)
    - Existing sensor suites still green: run the newest existing sensor test (the 203-03 / 205-03 sensor tests found under tests/) and confirm exit 0
  </acceptance_criteria>
  <done>The sensor rides the one governed reach path; no second selection brain; existing sensors unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user message text -> sensor detection | untrusted free text drives whether a card envelope is injected into the turn |
| rooms registry -> rendered card labels | registry content renders into the chooser options |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-19 | Spoofing | fork detection on free text | mitigate | high-precision detection with an explicit prior-rooms-exist precondition; a false fire only offers a room chooser (reversible UI), never binds or writes; when unsure return null |
| T-209-20 | Denial of Service | sensor fault breaking dispatchSensors | mitigate | copy the null-guard/never-throw idiom; fault-isolation asserted in Task 2; lazy requires inside try/catch |
| T-209-21 | Tampering | bespoke card construction | mitigate | mint ONLY via renderRoomChooserCard -> pickShape (SEED-020); grep-gate asserts zero direct renderShapeF* calls in the sensor |
| T-209-22 | Information Disclosure | room names in the rendered turn | accept | room names are the navigator's own LOCAL data rendered back to the navigator; zero Brain egress (no Brain require in the sensor, grep-provable) |
</threat_model>

<verification>
- `node tests/test-209-room-pick-sensor.cjs` exits 0
- `bash tests/run-all-209.sh` exits 0 (room-pick leg PASS)
- `grep -rn "brain" lib/core/sensors/sensor-room-pick.cjs` returns nothing (Part 8)
- No em-dashes in touched files
</verification>

<success_criteria>
- The incident's fork carries a transport: rendered card + marker + binding + imperative, minted through the single door
- Zero impact on non-room-pick turns and on existing sensors
</success_criteria>

<output>
Create `.planning/phases/209-shape-f-native-fire/209-05-SUMMARY.md` when done. Record the detection patterns chosen and the precision rationale.
</output>
