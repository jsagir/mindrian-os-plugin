---
phase: 209-shape-f-native-fire
plan: 07
type: execute
wave: 3
depends_on: [209-05, 209-06]
files_modified:
  - scripts/check-card-fire.cjs
  - tests/test-209-backstop-tuning.cjs
  - tests/test-209-incident-replay.cjs
autonomous: true
requirements: [H1, H2]
must_haves:
  truths:
    - "A legit U+25A0-bearing non-gate turn passes without a block (the false-positive full block is gone)"
    - "A multiline labeled-box gate and a numbered-prose gate are intercepted (the incident-class false negatives are gone)"
    - "A turn that fired the card in an EARLIER assistant message of the CURRENT turn is not intercepted for a later U+25A0 (WR-06 window fixed)"
    - "The incident transcript shape (mid-dialogue room resume) replays clean: card fires natively turn-1, zero intercepts, render-coverage green"
    - "The constitutional floor is byte-untouched: Stop-block re-prompt, degrade envelope, MAX_FORCE_RETRIES=3, MAX_SESSION_INTERCEPTS=12"
  artifacts:
    - path: "scripts/check-card-fire.cjs"
      provides: "H1 tuned ASCII_BOX_GLYPH_RE + H2 current-turn askFired window"
    - path: "tests/test-209-backstop-tuning.cjs"
      provides: "FP/FN regex matrix + window regression"
      min_lines: 80
    - path: "tests/test-209-incident-replay.cjs"
      provides: "the adversarial phase verification (RESEARCH section 3)"
      min_lines: 60
  key_links:
    - from: "tests/test-209-incident-replay.cjs"
      to: "scripts/check-card-fire.cjs"
      via: "replayed Stop-transcript fixtures through the classify path"
      pattern: "check-card-fire"
---

<objective>
Fix IDs H1 + H2 - deliberately LAST (LOCKED wave order): tune the backstop floor only after the native path (waves 1-2 of this phase) is live, so the Wave 2-3 intercept telemetry validates which regex branches still fire. Plus the phase's adversarial verification: replay the incident transcript shape end to end.

H1: ASCII_BOX_GLYPH_RE (:203) currently is `/\[\s*1\s*\]\s*.*\[\s*2\s*\]|type\s+1\s*,\s*2\s*,\s*or\s+3|■/i`. The bare ■ alternative false-positive-blocks sanctioned UI turns (U+25A0 is plugin vocabulary: selector-dispatcher.cjs:256, ui-system SKILL.md:131, dial-presenter.cjs:134); prose gates and multiline labeled boxes slip through (false negatives, incl. the incident's shape, verdict 25).
H2: WR-06 evaluates askFired on the LAST assistant message only (:681-716); a card fired earlier in the CURRENT turn followed by a glyph-bearing message classifies as no-card.

Constitutional floor (DO NOT TOUCH): :468-481 Stop-block re-prompt, :462-467 degrade envelope, :159 MAX_FORCE_RETRIES=3, :179 MAX_SESSION_INTERCEPTS=12. Detection regex/window ONLY. SECONDARY (regex) detection is never removed even with H3 PRIMARY live.

Purpose: the backstop's intercept counters become clean telemetry (target zero on wired surfaces); false blocks stop costing navigator turns.
Output: tuned hook + two test files, one of which is the phase's adversarial gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-shape-f-native-fire/209-CONTEXT.md
@.planning/phases/209-shape-f-native-fire/209-PATTERNS.md
@.planning/phases/209-shape-f-native-fire/209-RESEARCH.md
@scripts/check-card-fire.cjs
</context>

## Artifacts this phase produces (this plan's share)

- Tuned `ASCII_BOX_GLYPH_RE` (drops the bare `■` alternative; adds the multiline labeled-box form and a numbered-prose-gate form)
- Current-turn `askFired` accumulation window in the transcript walk (assistant messages since the last user message)
- `tests/test-209-backstop-tuning.cjs`, `tests/test-209-incident-replay.cjs` - new test files

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: H1 - tune ASCII_BOX_GLYPH_RE (drop bare U+25A0, add labeled-box + numbered-prose forms)</name>
  <files>scripts/check-card-fire.cjs, tests/test-209-backstop-tuning.cjs</files>
  <read_first>
    - scripts/check-card-fire.cjs :195-210 (the regex + its doctrine comment) and the gateSignature function (the CR-03/WR-07 retry-key anchor derives from the MATCHED GLYPH SPAN - a regex change shifts what the signature sees; the existing 179-wave e2e suites are the stability net)
    - .planning/phases/209-shape-f-native-fire/209-PATTERNS.md ("H1" pattern assignment - the exact multiline form to add)
    - The shipped check-card-fire fixture/test suites under tests/ (the 179-wave unit + e2e files - your regression baseline; find them via `grep -rl "check-card-fire" tests/`)
  </read_first>
  <behavior>
    FP matrix (must PASS, no match):
    - A bare U+25A0 in a dial/status row with no option structure
    - A U+25A0-bearing Voice Signature line in an ordinary prose turn
    FN matrix (must MATCH):
    - Same-line labeled box: '[1] resume [2] new room' (existing branch, unchanged)
    - Multiline labeled box: '[1] resume the room\n   details\n[2] start fresh' (the new `\[\s*1\s*\][^\n]*\n[\s\S]*?\[\s*2\s*\]` form)
    - 'type 1, 2, or 3' (existing branch, unchanged)
    - Numbered-prose gate: a line-anchored '1. <option>' followed by '2. <option>' on a subsequent line within the same message
    Stability:
    - The 179-wave suites still pass (gate signatures for same-line labeled boxes unchanged)
  </behavior>
  <action>
    Replace the regex at :203: keep the same-line `\[\s*1\s*\]\s*.*\[\s*2\s*\]` and `type\s+1\s*,\s*2\s*,\s*or\s+3` alternatives; DELETE the bare `■` alternative; ADD the multiline labeled-box alternative `\[\s*1\s*\][^\n]*\n[\s\S]*?\[\s*2\s*\]` and a numbered-prose-gate alternative anchored to line starts, e.g. `(?:^|\n)\s{0,3}1[.)]\s+\S[^\n]*\n(?:[^\n]*\n)?\s{0,3}2[.)]\s+\S` (tune against the FP matrix - a numbered list in ordinary explanatory prose is the FP risk; require both markers within a 3-line span and keep the /i flag). Update the doctrine comment: name the sanctioned-glyph rationale (three citation sites) and the H1 fix ID. Check every OTHER use of the matched span (gateSignature glyph-span extraction) still behaves for the new alternatives - two distinct multiline gates must yield distinct signatures (WR-07 idiom).

    tests/test-209-backstop-tuning.cjs: drive the exported regex/classify seam with the full FP/FN matrix above; plus a WR-07 assertion that two different multiline gates produce different gate signatures.
  </action>
  <verify>
    <automated>node tests/test-209-backstop-tuning.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const s=require('fs').readFileSync('scripts/check-card-fire.cjs','utf8'); if(/\|\\\\u25A0|■\//.test(s.match(/ASCII_BOX_GLYPH_RE = .*/)[0])) process.exit(1)"` exits 0 (bare U+25A0 alternative gone from the regex line)
    - `node tests/test-209-backstop-tuning.cjs` exits 0 (full FP/FN matrix)
    - The 179-wave check-card-fire suites exit 0 unchanged
  </acceptance_criteria>
  <done>Sanctioned glyph never blocks; labeled-box and numbered-prose gates intercept; signatures stable per gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: H2 - askFired ORs across the CURRENT turn's assistant messages</name>
  <files>scripts/check-card-fire.cjs, tests/test-209-backstop-tuning.cjs</files>
  <read_first>
    - scripts/check-card-fire.cjs :680-720 (the transcript walk: lastAssistantContent tracking, the WR-06 comment, the :716 askFired decision, and the CR-03 note at :710-713 explaining why user messages are NOT tracked for the retry KEY - your window boundary must not re-anchor the key)
  </read_first>
  <behavior>
    - Fired-then-glyph within one turn: assistant message 1 fires AskUserQuestion, assistant message 2 (same turn, no user message between) carries a labeled box -> askFired true, NO intercept
    - Stale-card across turns: assistant fires in turn N, user speaks, turn N+1 renders a box with no card -> intercepted (the original WR-06 cross-turn bleed stays fixed)
    - Retry-key stability: the gate_signature and turnContextHash for a given gate are IDENTICAL before and after this change (user-boundary tracking bounds the window only, never feeds the key) - assert by comparing hashes across the two code paths on a fixed fixture
  </behavior>
  <action>
    In the transcript walk: track a currentTurnAssistantContents array that RESETS to empty whenever a role:user record is encountered and appends each assistant message's content otherwise. After the walk, `askFired = currentTurnAssistantContents.some(scanContentForAskUserQuestion)`. Keep lastAssistantContent/lastAssistantText exactly as-is for output_text and gateSignature (the key path is untouched). Rewrite the WR-06 comment block to describe the turn-window semantics and explicitly restate the CR-03 invariant (user records bound the WINDOW, never anchor the KEY). Extend tests/test-209-backstop-tuning.cjs with the three behaviors using JSONL transcript fixtures (mirror the 179-wave fixture style, incl. a no-role:user compaction-lead fixture to prove the degenerate path still works: no user record means the whole transcript tail is one window).
  </action>
  <verify>
    <automated>node tests/test-209-backstop-tuning.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-209-backstop-tuning.cjs` exits 0 including the three H2 behaviors
    - `grep -n "MAX_FORCE_RETRIES = 3" scripts/check-card-fire.cjs` and `grep -n "MAX_SESSION_INTERCEPTS = 12" scripts/check-card-fire.cjs` still match; `git diff` shows zero edits in :159, :179, :462-481 regions
    - The 179-wave suites (incl. the CR-03 no-role:user fixtures) exit 0 unchanged
  </acceptance_criteria>
  <done>Window widened to the current turn, cross-turn bleed still fixed, retry key provably unmoved, floor untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Adversarial phase verification - replay the incident</name>
  <files>tests/test-209-incident-replay.cjs</files>
  <read_first>
    - .planning/phases/209-shape-f-native-fire/209-RESEARCH.md section 3 (the four replay assertions - this task implements them verbatim)
    - lib/core/sensors/sensor-room-pick.cjs and lib/core/insight-sensors.cjs (from 209-05 - the native-fire transport the replay proves)
    - scripts/check-card-fire.cjs (post Tasks 1-2)
  </read_first>
  <action>
    Build tests/test-209-incident-replay.cjs reproducing the incident shape (mid-dialogue room resume in free conversation) and asserting the four RESEARCH-mandated outcomes:
    (a) Card fires natively turn-1: drive dispatchSensors with a room-resume message fixture + a stubbed registry of prior rooms; assert the collected output carries the room-chooser rendered text, the '[AskUserQuestion contract:' marker, and the '[BINDING:' line (the transport exists BEFORE emission).
    (b) Zero check-card-fire intercepts: feed a Stop-transcript fixture whose current turn contains that envelope AND an AskUserQuestion tool-use record through the hook's classify path; assert no block decision and no retry-side-file growth (hermetic side-file path).
    (c) Render-coverage gate green: shell out to `node scripts/check-render-coverage.cjs`; assert exit 0 and assert the registry carries 0 unwired declared .md entries (parse data/render-coverage-registry.json).
    (d) Sanctioned glyph passes: a non-gate turn fixture whose text carries U+25A0 (a dial row) and no card; assert no intercept.
    Print each lettered assertion distinctly so the SUMMARY can cite them.
  </action>
  <verify>
    <automated>node tests/test-209-incident-replay.cjs && bash tests/run-all-209.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-209-incident-replay.cjs` exits 0 with all four lettered assertions printed PASS
    - `bash tests/run-all-209.sh` exits 0 with ALL nine legs PASS and 0 SKIP (the phase-complete condition)
    - `node scripts/check-render-coverage.cjs` exits 0
  </acceptance_criteria>
  <done>The incident cannot recur silently: native fire proven, backstop quiet, coverage gate green, sanctioned glyph safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detection regex/window -> block decisions | loosening detection (dropping U+25A0) risks false negatives; widening the window risks masking real misses |
| retry-key derivation | any drift re-opens the CR-02/CR-03/CR-04 livelock class |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-28 | Denial of Service (inverse) | dropping bare U+25A0 creates FNs | mitigate | sequenced LAST by design: H3 PRIMARY (209-06) is live and registry-keyed before the regex loosens, and the two ADDED alternatives (multiline box, numbered prose) net-tighten coverage; the FP/FN matrix is the proof |
| T-209-29 | Denial of Service | H2 window masking a real miss | mitigate | the window is ONE turn (reset at every user record); the stale-card cross-turn fixture stays a REQUIRED intercept; degenerate no-user path fixture required |
| T-209-30 | Tampering | retry-key drift via the walk refactor | mitigate | gateSignature/turnContextHash inputs untouched (lastAssistantText path preserved); hash-equality assertion on a fixed fixture; 179-wave CR-03/CR-04 suites must stay green |
| T-209-31 | Tampering | constitutional floor edits | mitigate | grep + git-diff acceptance criteria pin :159/:179/:462-481 byte-untouched; detection-only edit surface |
</threat_model>

<verification>
- `node tests/test-209-backstop-tuning.cjs` and `node tests/test-209-incident-replay.cjs` exit 0
- `bash tests/run-all-209.sh` exits 0 with 9 PASS / 0 FAIL / 0 SKIP (phase gate)
- All 179-wave check-card-fire suites exit 0 (floor + livelock guarantees intact)
- `node scripts/check-render-coverage.cjs` and `node scripts/check-shape-declaration.cjs` exit 0
- No em-dashes in touched files
</verification>

<success_criteria>
- Backstop detection: fewer false positives (sanctioned glyph), fewer false negatives (multiline/prose gates), current-turn window correct
- The backstop's role is now telemetry: the incident replay proves the native path fires first and the hook stays silent
- Constitutional floor provably byte-untouched
</success_criteria>

<output>
Create `.planning/phases/209-shape-f-native-fire/209-07-SUMMARY.md` when done. Record the final regex, the four replay assertion results, and the run-all-209 tally.
</output>
