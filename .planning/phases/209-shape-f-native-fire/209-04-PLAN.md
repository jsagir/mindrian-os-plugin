---
phase: 209-shape-f-native-fire
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/card-fire-gate.cjs
  - evals/plurai/13-native-fire.csv
  - evals/plurai/209-baseline.json
  - tests/test-209-card-fire-gate.cjs
  - tests/run-all-209.sh
autonomous: true
requirements: [EVAL-GATE]
must_haves:
  truths:
    - "A frozen-invariant offline classifier reproduces the card-fired-vs-prose judge deterministically (100% parity with the hand-labeled CSV)"
    - "The baseline rides the sanctioned baseline_deferred degrade path (Plurai's interactive flow cannot run non-interactively; Part 8 - Plurai is BUILD/CI only, zero runtime egress)"
    - "bash tests/run-all-209.sh aggregates every 209 test leg with run_if SKIP guards so later-wave plans only create their test files, never edit the runner"
  artifacts:
    - path: "lib/core/card-fire-gate.cjs"
      provides: "frozen HARD_VIOLATIONS-style invariants + classifyCardFireTrace"
      exports: ["classifyCardFireTrace", "HARD_VIOLATIONS"]
      min_lines: 60
    - path: "evals/plurai/13-native-fire.csv"
      provides: "hand-labeled synthetic rows: card-fired vs prose-at-gate"
      min_lines: 15
    - path: "evals/plurai/209-baseline.json"
      provides: "baseline_deferred:true CI baseline (196/201 pattern)"
      contains: "baseline_deferred"
    - path: "tests/test-209-card-fire-gate.cjs"
      provides: "local parity: gate reproduces every CSV label"
      min_lines: 40
    - path: "tests/run-all-209.sh"
      provides: "phase aggregator with pre-declared run_if legs"
      min_lines: 20
  key_links:
    - from: "tests/test-209-card-fire-gate.cjs"
      to: "evals/plurai/13-native-fire.csv"
      via: "row-by-row parity assertion"
      pattern: "13-native-fire"
    - from: "evals/plurai/209-baseline.json"
      to: "evals/plurai/13-native-fire.csv"
      via: "source_csv field"
      pattern: "source_csv"
---

<objective>
The phase's mandatory eval GATE (milestone ground rule; CONTEXT "Eval (GATE)"). A Plurai eval for card-fired-vs-prose fidelity plus the local parity gate, following the Phase 196/201 pattern EXACTLY (frozen invariants, synthetic CSV, baseline_deferred degrade path, parity test, run-all aggregator).

Self-contained new file set - no shared files with any other 209 plan, so it rides wave 1 in parallel. The runner it ships pre-declares run_if legs for ALL 209 test files (waves 1-3), so no later plan ever edits it.

Purpose: the phase's success claim ("card fires natively, prose-at-gate is a violation") becomes a deterministic, offline, CI-enforced classifier, not an assertion.
Output: 5 new files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-shape-f-native-fire/209-CONTEXT.md
@.planning/phases/209-shape-f-native-fire/209-PATTERNS.md
@lib/core/ralph-loop-gate.cjs
@evals/plurai/201-baseline.json
@evals/plurai/08-ralph-loop-behavior.csv
@tests/test-201-ralph-loop-gate.cjs
@tests/run-all-201.sh
</context>

## Artifacts this phase produces (this plan's share)

- `lib/core/card-fire-gate.cjs` - new module; exports `classifyCardFireTrace(trace, _optsIgnored)` and the frozen `HARD_VIOLATIONS` array with invariant names: `rendered_prose_instead_of_card`, `ascii_box_at_gate`, `no_card_at_declared_gate`, `trailer_present_card_absent`
- `evals/plurai/13-native-fire.csv` - new synthetic eval CSV (numbering: 12 is the current highest)
- `evals/plurai/209-baseline.json` - new CI baseline, `baseline_deferred: true`
- `tests/test-209-card-fire-gate.cjs` - new parity test
- `tests/run-all-209.sh` - new phase aggregator with pre-declared legs for: test-209-card-fire-gate.cjs, test-209-engine-arm-contract.cjs, test-209-stamp-firing-block.cjs, test-209-declared-implies-wired.cjs, test-209-room-pick-sensor.cjs, test-209-primary-sidechannel.cjs, test-209-session-start-exemplar.cjs, test-209-backstop-tuning.cjs, test-209-incident-replay.cjs

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: card-fire-gate.cjs frozen invariants + the 13-native-fire.csv synthetic rows</name>
  <files>lib/core/card-fire-gate.cjs, evals/plurai/13-native-fire.csv</files>
  <read_first>
    - lib/core/ralph-loop-gate.cjs :29-38 (the EXACT structural analog: Object.freeze HARD_VIOLATIONS array of { name, hit(t) } predicates; classifyLoopTrace(trace, _optsIgnored) accepts but IGNORES the second arg - invariants are the law, not a knob)
    - evals/plurai/08-ralph-loop-behavior.csv (row/column dialect incl. the Label column that drives hand-labeling)
    - evals/plurai/09-apo-output-voice.csv (the quoted-JSON dialect used when a cell carries structured content - pick whichever dialect your trace cells need and stay consistent)
    - scripts/check-card-fire.cjs :195-210 (ASCII_BOX_GLYPH_RE - the anti-pattern vocabulary the ascii_box_at_gate invariant mirrors; note plan 209-07's H1 will drop the bare U+25A0 alternative from the hook - the GATE invariant must NOT treat a bare U+25A0 as a violation either, it is sanctioned UI vocabulary per selector-dispatcher.cjs:256 / ui-system SKILL.md:131 / dial-presenter.cjs:134)
  </read_first>
  <behavior>
    - A trace { declared_gate:true, card_fired:false, output_text:'[1] a [2] b', trailer_present:true } classifies violation carrying ascii_box_at_gate, no_card_at_declared_gate, and trailer_present_card_absent
    - A trace { declared_gate:false, card_fired:false, output_text:'the U+25A0 glyph in a status row', trailer_present:false } classifies pass (sanctioned-glyph case)
    - A trace { declared_gate:true, card_fired:true, ... } classifies pass regardless of output text
    - HARD_VIOLATIONS is frozen; the second classifier arg is ignored (passing { disable:true } changes nothing)
  </behavior>
  <action>
    1. lib/core/card-fire-gate.cjs: copy the ralph-loop-gate.cjs skeleton. Trace shape (document in the header): { declared_gate: bool, card_fired: bool, output_text: string, trailer_present: bool }. Frozen invariants, each { name, hit(t) }:
       - rendered_prose_instead_of_card: declared_gate && !card_fired && output_text carries an option-list pattern (numbered '1.'/'1)' options or bracket-labeled options)
       - ascii_box_at_gate: !card_fired && output_text matches the labeled-box glyph forms ('[1]...[2]' same-line or multiline, 'type 1, 2, or 3') - deliberately EXCLUDING a bare U+25A0
       - no_card_at_declared_gate: declared_gate && !card_fired (the floor invariant)
       - trailer_present_card_absent: trailer_present && !card_fired (the BINDING line was in context and ignored)
       `classifyCardFireTrace(trace, _optsIgnored)` returns { classification: 'pass'|'violation', violations: [names] }; second arg accepted and IGNORED. Node built-ins only, no network, no Brain (Part 8 - BUILD/CI vocabulary only). No em-dashes.
    2. evals/plurai/13-native-fire.csv: 16-24 synthetic rows covering: card fired at declared gate (pass), prose numbered list at gate (violation), ASCII labeled box at gate (violation), multiline labeled box at gate (violation), trailer present + card absent (violation), bare U+25A0 in a NON-gate turn (pass - the sanctioned-glyph case), card fired with trailing prose recap (pass), no gate declared + plain prose (pass). Columns: id, the four trace fields, Label, note. Hand-label every row deterministically so the local gate reproduces 100%.
  </action>
  <verify>
    <automated>node -e "const g=require('./lib/core/card-fire-gate.cjs'); if(!Object.isFrozen(g.HARD_VIOLATIONS))process.exit(1); const r=g.classifyCardFireTrace({declared_gate:true,card_fired:false,output_text:'[1] a [2] b',trailer_present:true},{disable:true}); if(r.classification!=='violation')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./lib/core/card-fire-gate.cjs')"` exits 0
    - HARD_VIOLATIONS is Object.frozen and exports exactly the four named invariants (asserted in Task 2's parity test)
    - `wc -l evals/plurai/13-native-fire.csv` returns >= 17 (header + 16 rows minimum)
    - The bare-U+25A0-non-gate row is Labeled pass in the CSV
  </acceptance_criteria>
  <done>Gate module + CSV exist; invariants frozen and options-arg-proof; sanctioned-glyph case encoded as pass.</done>
</task>

<task type="auto">
  <name>Task 2: baseline_deferred baseline + parity test + run-all-209.sh aggregator</name>
  <files>evals/plurai/209-baseline.json, tests/test-209-card-fire-gate.cjs, tests/run-all-209.sh</files>
  <read_first>
    - evals/plurai/201-baseline.json (the EXACT baseline shape: schema_version, artifact_kind ci-baseline, runtime_loaded false, method hand-labeled, baseline_deferred true, deferred_note, source_csv, judge_prompt)
    - tests/test-201-ralph-loop-gate.cjs (parity idiom: parse the CSV, run every row's trace through the classifier, assert Label match on every row; count assertions)
    - tests/run-all-201.sh (set -uo pipefail + run / run_if guards; each leg SKIPs on a missing file - copy verbatim, swap leg files)
  </read_first>
  <action>
    1. evals/plurai/209-baseline.json: copy the 201 structure. source_csv 'evals/plurai/13-native-fire.csv'; judge_prompt worded for card-fired-vs-prose fidelity (a Decision Gate turn must fire the AskUserQuestion card; prose or an ASCII frame at a gate is a violation; a bare U+25A0 outside a gate is sanctioned); deferred_note explains the Plurai interactive multi-turn MCP flow cannot run non-interactively and the local gate reproduces every hand label (the sanctioned Phase 196/201 degrade path).
    2. tests/test-209-card-fire-gate.cjs: parse 13-native-fire.csv (reuse the 201 test's CSV parsing idiom incl. its quoted-cell handling), run classifyCardFireTrace per row, assert 100% Label parity, plus the frozen/ignored-arg assertions from Task 1's behavior list. Print PASS/FAIL counts; exit non-zero on any mismatch.
    3. tests/run-all-209.sh: copy run-all-201.sh verbatim, swap in run_if legs for ALL NINE 209 test files listed in the Artifacts section above (only test-209-card-fire-gate.cjs exists at this point; the other eight SKIP until their plans land - by phase end all nine report PASS). Make it executable.
  </action>
  <verify>
    <automated>node tests/test-209-card-fire-gate.cjs && bash tests/run-all-209.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-209-card-fire-gate.cjs` exits 0 with 100% CSV parity (every row)
    - `bash tests/run-all-209.sh` exits 0 reporting the card-fire-gate leg PASS and the eight not-yet-landed legs SKIP
    - `grep -c "baseline_deferred" evals/plurai/209-baseline.json` returns 1 and its value is true
    - `grep -c "run_if" tests/run-all-209.sh` returns >= 8 (pre-declared legs)
  </acceptance_criteria>
  <done>Baseline, parity test, and aggregator shipped; aggregator pre-declares all phase legs so no later plan edits it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval gate -> CI verdict | a weakened or knob-tunable invariant lets prose-at-gate regressions ship green |
| baseline_deferred | a degrade path that could be abused to skip evaluation entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-15 | Tampering | invariant weakening via options | mitigate | HARD_VIOLATIONS Object.freeze + second classifier arg accepted-but-IGNORED (the 201 idiom), both asserted in the parity test |
| T-209-16 | Repudiation | baseline honesty | mitigate | baseline_deferred:true with an explicit deferred_note naming WHY (interactive Plurai flow) and the parity proof (100% local reproduction) - the sanctioned 196/201 path, never a silent skip |
| T-209-17 | Information Disclosure | eval content egress | mitigate | CSV rows are fully synthetic (no room content, no real transcripts); gate module has zero network/Brain requires (Part 8); grep-provable |
| T-209-18 | Denial of Service | false-positive gate blocking CI on sanctioned glyphs | mitigate | the bare-U+25A0-non-gate row is a REQUIRED pass row in the CSV, locking the sanctioned-glyph doctrine into the gate itself |
</threat_model>

<verification>
- `node tests/test-209-card-fire-gate.cjs` exits 0 (100% parity)
- `bash tests/run-all-209.sh` exits 0 (1 PASS, 8 SKIP at this wave)
- No em-dashes in any of the 5 files
- `grep -rn "require(.*http\|require(.*net\|brain" lib/core/card-fire-gate.cjs` returns nothing (Part 8 proof)
</verification>

<success_criteria>
- The phase has a deterministic offline judge for its core claim, wired into a phase aggregator that every subsequent plan's tests slot into without runner edits
- Pattern fidelity with 196/201 is verifiable side-by-side
</success_criteria>

<output>
Create `.planning/phases/209-shape-f-native-fire/209-04-SUMMARY.md` when done.
</output>
