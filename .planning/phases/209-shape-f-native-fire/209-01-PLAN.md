---
phase: 209-shape-f-native-fire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/intent-classifier.cjs
  - tests/test-209-engine-arm-contract.cjs
autonomous: true
requirements: [E3, E4]
must_haves:
  truths:
    - "The engine-arm dial block carries the BINDING line and a machine-readable AskUserQuestion contract, not just the scalar marker"
    - "Dial row labels resolve {topic}/{room_name} from live room context instead of always degrading to the elevation fallback"
    - "The F.8 binding gate names AskUserQuestion in its guidance and carries the trailer (marker + binding)"
    - "A render fault still degrades to the base engine block; no turn is ever blocked by these changes"
  artifacts:
    - path: "scripts/intent-classifier.cjs"
      provides: "E3 slotContext threading + E2 contract serialization at the engine arm; E4 trailer + footer + AskUserQuestion guidance in emitBindingGate"
      contains: "askuserquestion_binding"
    - path: "tests/test-209-engine-arm-contract.cjs"
      provides: "Regression proof for E2/E3/E4 seams"
      min_lines: 60
  key_links:
    - from: "scripts/intent-classifier.cjs"
      to: "lib/hmi/selector-dispatcher.cjs"
      via: "appendAskUserQuestionTrailer envelope fields"
      pattern: "askuserquestion_binding"
    - from: "scripts/intent-classifier.cjs"
      to: "lib/hmi/dial-presenter.cjs"
      via: "renderDial(reachList, { slotContext })"
      pattern: "slotContext"
---

<objective>
Close RC-3 at the engine emission seam. Fix ID E3 (fold in E2, which did NOT ship in Wave 1 despite CONTEXT/RESEARCH claiming it did - see the 209-PATTERNS.md Wave 1 Drift Report) plus fix ID E4.

Three defects at two seams in scripts/intent-classifier.cjs:
1. `renderDial(reachList, {})` at :993 passes empty opts, so slotContext defaults to {} and every dial row degrades to the ELEVATION_DEFAULTS fallback (E3).
2. The concat at :1007-1010 returns `base + rendered.text + marker` only - it drops E1's `rendered.askuserquestion_binding` line (shipped in selector-dispatcher.cjs:551-566 but never read here) and never serializes `rendered.contract` (E2).
3. `emitBindingGate` (:1925-2010) calls renderShapeF8 directly, excludes zones.footer (where the trailer lives), and never names AskUserQuestion in its guidance (E4).

Purpose: the trigger that reaches Larry on the engine arm becomes self-decoding and machine-readable, so the card fires natively instead of via the check-card-fire backstop.
Output: patched intent-classifier.cjs + a regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-shape-f-native-fire/209-CONTEXT.md
@.planning/phases/209-shape-f-native-fire/209-PATTERNS.md
@scripts/intent-classifier.cjs
@lib/hmi/selector-dispatcher.cjs
@lib/hmi/dial-presenter.cjs
@lib/hmi/dial-label-composer.cjs
</context>

## Artifacts this phase produces (this plan's share)

- `buildDialSlotContext(roomContext, roomDir)` - new pure helper function in scripts/intent-classifier.cjs (derives the composeLabel slot map from the live roomContext)
- Engine-arm concat now emits two new lines: the BINDING line (from `rendered.askuserquestion_binding`) and a serialized contract line of the form `[AskUserQuestion payload: {...json...}]`
- `emitBindingGate` envelope now carries `zones.footer`, the trailer marker + binding lines, and guidance text naming AskUserQuestion
- `tests/test-209-engine-arm-contract.cjs` - new test file

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E3 + E2 - thread slotContext into renderDial and emit binding + contract at the engine-arm concat</name>
  <files>scripts/intent-classifier.cjs, tests/test-209-engine-arm-contract.cjs</files>
  <read_first>
    - .planning/phases/209-shape-f-native-fire/209-PATTERNS.md (Wave 1 Drift Report + "E2+E3" pattern assignment - the authoritative live line numbers)
    - scripts/intent-classifier.cjs :930-1012 (renderEngineDecisionWithDial - the function you are editing; note the SEED-020 pickShape-exemption comment at :939-942, which stays true) and :2425-2455 (the single call site that builds ctx)
    - lib/hmi/dial-presenter.cjs :336-360 (renderDial opts.slotContext consumption) and :325-352 (the return-shape comment listing `contract`)
    - lib/hmi/dial-label-composer.cjs :245-337 (pickTemplate/resolveSlots - slotContext is a FLAT map of slot name to resolved noun string: topic, topic_a, topic_b, room_name, framework, message_noun; missing slots degrade per-row, non-fatal)
    - lib/hmi/selector-dispatcher.cjs :540-570 (appendAskUserQuestionTrailer sets BOTH askuserquestion_marker and askuserquestion_binding on the envelope)
    - lib/core/navigation/room-context.cjs :280-310 (getRoomContext return: summary, recentMessages, relevantNodes, cortexNodes; relevantNodes items are RAW-LOCAL node rows from getNeighborhood - read that function to confirm the display-name field before using it)
  </read_first>
  <behavior>
    - Test 1 (E3): calling renderEngineDecisionWithDial with routing.source='engine' and a ctx whose roomContext carries at least one relevantNodes entry results in renderDial being invoked with a non-empty opts.slotContext (assert via a stubbed presenter or by asserting the rendered row label is NOT one of the three ELEVATION_DEFAULTS strings when slots resolve)
    - Test 2 (E2): the returned block contains the BINDING line (substring '[BINDING:' per selector-dispatcher.cjs:565) AND a contract line matching /\[AskUserQuestion payload: \{.*\}\]/ whose JSON body parses via JSON.parse and carries a verbs array
    - Test 3 (degrade): ctx with roomContext null still returns a rendered block (slotContext omitted or empty; no throw); a presenter fault still returns the base block unchanged (existing try/catch preserved)
    - Test 4 (DIAL-ATOM-01): a tier_0 reachList still carries the marker + binding (the trailer is never gated off)
  </behavior>
  <action>
    In scripts/intent-classifier.cjs:

    1. Add a pure helper `buildDialSlotContext(roomContext, roomDir)` near renderEngineDecisionWithDial. It returns a flat object with only the slots it can resolve: `room_name` from `path.basename(roomDir)` when roomDir is a non-empty string; `topic` from the top entry of `roomContext.relevantNodes` (use the node display-name field confirmed from getNeighborhood; fall back to the first cortexNodes entry's name; omit the key entirely if neither resolves). Do NOT set `framework` (the egress-audited slot stays owned by the composer, Part 8). Never throw; return {} on any malformed input.
    2. At the call site (:2441-2451), extend the ctx object with `roomContext: context.roomContext` and `roomDir: roomDir` (both already in scope; roomContext is attached at :1509 attachRoomContextAndDecide).
    3. Inside renderEngineDecisionWithDial, replace `presenter.renderDial(reachList, {})` at :993 with `presenter.renderDial(reachList, { slotContext: buildDialSlotContext(ctx && ctx.roomContext, ctx && ctx.roomDir) })`.
    4. At the concat (:1007-1010): after `dispatcher.appendAskUserQuestionTrailer(rendered, 'F.1')`, read BOTH `rendered.askuserquestion_marker` AND `rendered.askuserquestion_binding`. Build a compact contract line ONLY when `rendered.contract` is a non-null object: extract verbs (labels only), recommended, and shape/mode into a small plain object and emit `'[AskUserQuestion payload: ' + JSON.stringify(compact) + ']'`. Return `base + '\n\n' + rendered.text + '\n' + marker + '\n' + binding + '\n' + payloadLine`, guarding each appended line on its own presence check so a missing field degrades to the prior behavior (marker-only) rather than emitting 'undefined'. All of this stays inside the existing try/catch so any fault returns base unchanged.
    5. Update the DIAL-ATOM-01 comment block (:995-1006) to state the caller now reads marker + binding + contract positionally (it still never composes the trailer strings - dispatcher-only construction, SEED-020).

    Test file tests/test-209-engine-arm-contract.cjs: require intent-classifier.cjs (it exports under require.main guard - if renderEngineDecisionWithDial is not exported, add it to the module.exports test-surface block the file already has for other helpers; check the existing exports before adding). Implement the four behaviors above with an in-memory reachList fixture (no room.db, no network).
  </action>
  <verify>
    <automated>node tests/test-209-engine-arm-contract.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "askuserquestion_binding" scripts/intent-classifier.cjs` returns >= 1
    - `grep -c "AskUserQuestion payload:" scripts/intent-classifier.cjs` returns >= 1
    - `grep -n "renderDial(reachList, {})" scripts/intent-classifier.cjs` returns no matches (empty-opts call replaced)
    - `node tests/test-209-engine-arm-contract.cjs` exits 0
    - `node -e "require('./scripts/intent-classifier.cjs')"` exits 0 (no syntax/load fault)
  </acceptance_criteria>
  <done>Engine-arm block carries marker + BINDING + parseable JSON contract; dial labels resolve slots from live roomContext; all faults degrade to base block; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E4 - emitBindingGate carries the trailer, includes zones.footer, and names AskUserQuestion</name>
  <files>scripts/intent-classifier.cjs, tests/test-209-engine-arm-contract.cjs</files>
  <read_first>
    - scripts/intent-classifier.cjs :1925-2010 (emitBindingGate - current body; the three defects are: renderShapeF8 called outside the pickShape trailer door, zones.footer excluded from bodyLines, guidance never names AskUserQuestion)
    - lib/hmi/selector-dispatcher.cjs :961-1060 (pickShape and its :1047-1050 unconditional trailer attach for F-shapes - the SEED-020 single construction door)
    - .planning/phases/209-shape-f-native-fire/209-PATTERNS.md ("E4" pattern assignment)
  </read_first>
  <behavior>
    - Test 5: emitBindingGate's additionalContext contains zones.footer content, the '[AskUserQuestion contract:' marker, the '[BINDING:' line, and the literal string 'AskUserQuestion' inside the guidance sentence
    - Test 6: a renderShapeF8 fault still returns false (legacy advisory degrade unchanged)
  </behavior>
  <action>
    In emitBindingGate: PREFERRED route is `dispatcher.pickShape({ requestedShape: 'F.8', roomDir: roomDir, operator: null, payload: { options: options, header: '-- mindrianOS -- bind session -- select rooms --' } })` so the trailer attaches at the SEED-020 door (:1047-1050). FIRST verify pickShape's payload contract passes the F.8 options/header through to renderShapeF8 without needing a new dispatcher branch (read the pickShape body :961-1050 and the F.8 resolve path). If it maps cleanly, use it. If it would require ANY new dispatcher branch (forbidden - Canon Part 7, CONTEXT "no new dispatcher branches"), keep the direct renderShapeF8 call and instead call `dispatcher.appendAskUserQuestionTrailer(rendered, 'F.8')` immediately after the render (mirroring the engine-arm idiom and its documented SEED-020 exemption comment at :939-942 - copy that justification into a comment here).

    Either way, three mandatory deltas:
    1. Include `zones.footer` in bodyLines (after zones.signals).
    2. Append `rendered.askuserquestion_marker` and `rendered.askuserquestion_binding` to additionalContext (presence-guarded, same idiom as Task 1).
    3. Rewrite the guidance string to open with an imperative that names the tool, e.g. 'Fire the AskUserQuestion card with these rooms as a multi-select (toggle every room this session may write to, pick ONE primary write target, and offer a "remember for this session" toggle); never reproduce this block as text (SEED-021).' Keep the existing NO_ROOM_LABEL and confirm-before-write sentences.

    Do not touch persistDecisionTrace, the binding_gate_payload shape, or the envelope keys. Extend tests/test-209-engine-arm-contract.cjs with behaviors 5-6 (emitBindingGate must be reachable from the test - export it under the same test-surface block if not already exported).
  </action>
  <verify>
    <automated>node tests/test-209-engine-arm-contract.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "zones.footer" scripts/intent-classifier.cjs` shows a push into bodyLines inside emitBindingGate
    - The guidance string in emitBindingGate contains the literal token 'AskUserQuestion' (grep the function region)
    - `node tests/test-209-engine-arm-contract.cjs` exits 0 with the E4 assertions included
  </acceptance_criteria>
  <done>emitBindingGate emits footer + marker + binding + AskUserQuestion-naming guidance through a Part-7-compliant construction path; degrade path unchanged; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db content -> rendered turn text | relevantNodes/cortexNodes names flow into dial labels and the serialized contract line |
| engine render seam -> user turn | a fault here could block or corrupt every engine-arm turn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-01 | Tampering | contract line serialization (:1010 concat) | mitigate | build the compact object field-by-field from rendered.contract and JSON.stringify it; never string-concat unescaped label text into the bracket line; presence-guard every field so 'undefined' never renders |
| T-209-02 | Denial of Service | renderEngineDecisionWithDial / emitBindingGate | mitigate | all new logic stays inside the existing try/catch degrade paths (return base / return false); buildDialSlotContext never throws; verified by Test 3 and Test 6 |
| T-209-03 | Information Disclosure | slotContext {topic} from room nodes | accept | LOCAL render-only (Part 8); the egress-audited {framework} slot is deliberately NOT set by buildDialSlotContext - the composer's auditFrameworkSlot chokepoint stays the only framework door |
| T-209-04 | Elevation of Privilege | trailer construction | mitigate | this caller only READS envelope fields; trailer strings are composed exclusively by selector-dispatcher.cjs (SEED-020); no local marker/binding composition permitted |
</threat_model>

<verification>
- `node tests/test-209-engine-arm-contract.cjs` exits 0
- `bash tests/run-all-209.sh` shows the engine-arm leg PASS (leg pre-declared by plan 209-04; SKIP acceptable if 209-04 has not landed yet in this wave)
- `node scripts/check-render-coverage.cjs` exits 0 (no regression on the .cjs keyspace)
- Existing dial regression: `node tests/test-gate-native-fire-w1.cjs` still exits 0 (Wave 1 acceptance unbroken)
- No em-dashes introduced: `grep -P '\x{2014}' scripts/intent-classifier.cjs` returns nothing
</verification>

<success_criteria>
- E2/E3/E4 all closed at their verified live seams (:993, :1007-1010, :1925-2010)
- The BINDING line and machine-readable contract reach the engine-arm transport (the Drift Report gap is closed)
- Zero behavior change on non-engine arms and on every fault path
</success_criteria>

<output>
Create `.planning/phases/209-shape-f-native-fire/209-01-SUMMARY.md` when done.
</output>
