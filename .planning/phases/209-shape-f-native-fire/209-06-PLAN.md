---
phase: 209-shape-f-native-fire
plan: 06
type: execute
wave: 2
depends_on: [209-01]
files_modified:
  - lib/core/card-fire-sidechannel.cjs
  - lib/hmi/selector-dispatcher.cjs
  - scripts/intent-classifier.cjs
  - scripts/check-card-fire.cjs
  - scripts/session-start
  - tests/test-209-primary-sidechannel.cjs
  - tests/test-209-session-start-exemplar.cjs
autonomous: true
requirements: [H3, H4]
must_haves:
  truths:
    - "The three gate-envelope mints (pickShape door, engine arm, emitBindingGate) emit reached-gate side-channel records; check-card-fire's shipped-but-INERT PRIMARY consumer goes live"
    - "Regex detection stays SECONDARY and is never removed (the only detector when the writer fails)"
    - "session-start no longer teaches the anti-pattern: the MODE_MENU 'Type 1, 2, or 3' injection and the 'Other rooms:' prose list are replaced with card-fire instructions"
    - "A malformed or oversized side-channel file can never block a turn or crash the hook"
  artifacts:
    - path: "lib/core/card-fire-sidechannel.cjs"
      provides: "the one side-channel writer + reader (TTL-pruned, size-capped, never-throw)"
      exports: ["recordReachedGate", "readReachedGates"]
      min_lines: 60
    - path: "scripts/check-card-fire.cjs"
      provides: "PRIMARY consumer wired: deriveTurnSignals populates ran_entries/reached_gate_entries from the side file"
      contains: "readReachedGates"
    - path: "scripts/session-start"
      provides: "H4: card-fire instruction replaces the MODE_MENU exemplar and the prose room list"
    - path: "tests/test-209-primary-sidechannel.cjs"
      provides: "producer/consumer round-trip + malformed-file degrade proof"
      min_lines: 60
    - path: "tests/test-209-session-start-exemplar.cjs"
      provides: "anti-exemplar absence + instruction presence + bash syntax proof"
      min_lines: 30
  key_links:
    - from: "lib/hmi/selector-dispatcher.cjs"
      to: "lib/core/card-fire-sidechannel.cjs"
      via: "recordReachedGate at the pickShape trailer door"
      pattern: "recordReachedGate"
    - from: "scripts/check-card-fire.cjs"
      to: "lib/core/card-fire-sidechannel.cjs"
      via: "readReachedGates in deriveTurnSignals"
      pattern: "readReachedGates"
---

<objective>
Fix IDs H3 + H4. check-card-fire.cjs's PRIMARY (registry-keyed) detection is documented INERT: "a repo-wide grep finds ZERO producers of ran_entries / reached_gate_entries" (:46-54); the consumer at :347 has been correct-and-waiting since Phase 179. H3 lands the producers at the three places that mint gate envelopes, plus the consumer wire. H4 removes the session-opening anti-exemplar: scripts/session-start:675/:677 injects a literal 'Type 1, 2, or 3' MODE_MENU (the exact pattern ignite.md forbids and the backstop regex hunts), and :585 injects a prose 'Other rooms:' list.

Constitutional floor (DO NOT TOUCH): Stop-block re-prompt :468-481, degrade envelope :462-467, MAX_FORCE_RETRIES=3 :159, MAX_SESSION_INTERCEPTS=12 :179. This plan wires detection INPUT only.

Purpose: the backstop becomes registry-aware (PRIMARY live, regex secondary) and the session's first exemplar teaches the card, not the box.
Output: new side-channel module, three producer edits, one consumer wire, session-start fix, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-shape-f-native-fire/209-CONTEXT.md
@.planning/phases/209-shape-f-native-fire/209-PATTERNS.md
@scripts/check-card-fire.cjs
@lib/hmi/selector-dispatcher.cjs
@scripts/intent-classifier.cjs
@scripts/session-start
@lib/core/room-chooser.cjs
</context>

## Artifacts this phase produces (this plan's share)

- `lib/core/card-fire-sidechannel.cjs` - new module; exports `recordReachedGate({ sessionId, surface, shape })` and `readReachedGates(sessionId)`; side file `~/.mindrian/card-fire-reached.json` (same directory family as card-fire-retries.json), TTL-pruned, size-capped
- Producer calls at: selector-dispatcher.cjs pickShape door (inside the `payload.emitTelemetry === true` guard), intent-classifier.cjs engine arm (post-trailer), intent-classifier.cjs emitBindingGate (post-E4)
- `deriveTurnSignals` in check-card-fire.cjs now populates `ran_entries` / `reached_gate_entries` from the side file
- Rewritten `MODE_MENU` here-string + rewritten 'Other rooms:' injection in `scripts/session-start`
- `tests/test-209-primary-sidechannel.cjs`, `tests/test-209-session-start-exemplar.cjs` - new test files

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: H3 - side-channel writer module + the three producers</name>
  <files>lib/core/card-fire-sidechannel.cjs, lib/hmi/selector-dispatcher.cjs, scripts/intent-classifier.cjs, tests/test-209-primary-sidechannel.cjs</files>
  <read_first>
    - scripts/check-card-fire.cjs :36-58 (PRIMARY doctrine - what the consumer expects), :330-365 (turnContextHash: t.ran_entries is a STRING ARRAY, sorted+joined for the retry key - your schema must satisfy exactly this), and the retry side-file idiom around card-fire-retries.json (TTL prune, { count, ts } shape, ~/.mindrian location) to mirror
    - lib/hmi/selector-dispatcher.cjs :1040-1062 (the pickShape trailer door; CRITICAL: FS side-effects here are gated on `payload.emitTelemetry === true` because render-v2 Zone 4 invokes pickShape with a zero-FS-side-effect contract, Canon Part 8 fs_scope - the producer call MUST sit inside that same guard, next to emitPresentationTelemetry)
    - scripts/intent-classifier.cjs :1007-1014 (engine-arm seam post-209-01: trailer + binding + contract lines) and the emitBindingGate envelope build (post-209-01 E4 state)
    - .planning/phases/209-shape-f-native-fire/209-PATTERNS.md ("H3" pattern assignment; schema discretion note)
  </read_first>
  <behavior>
    - Round trip: recordReachedGate({sessionId:'s1', surface:'lib/hmi/selector-dispatcher.cjs', shape:'F.1'}) then readReachedGates('s1') returns an array containing a 'lib/hmi/selector-dispatcher.cjs#F.1' style entry string; a different sessionId returns []
    - Degrade: a hand-corrupted side file (invalid JSON), a missing file, and a file over the size cap all make readReachedGates return [] and recordReachedGate a silent no-op; NEITHER ever throws
    - TTL: entries older than the TTL are pruned on write and ignored on read
    - Producers: driving pickShape with emitTelemetry:true writes a record; with the flag absent writes NOTHING (fs_scope proof); the engine arm and emitBindingGate paths write their records (exercised via the exported functions with fixtures)
  </behavior>
  <action>
    1. lib/core/card-fire-sidechannel.cjs: side file `~/.mindrian/card-fire-reached.json` shaped `{ [sessionId]: [{ entry, ts }] }` where entry = `surface + '#' + shape` (registry-keyed: surface strings match data/render-coverage-registry.json surface names). TTL ~10 minutes (a turn-scoped signal, generous for slow turns), size cap ~64KB (truncate-oldest on breach), every fs op inside try/catch, writes atomic (tmp + rename). Support a test override of the side-file path via an options/env seam so tests never touch the real ~/.mindrian (mirror how existing tests stage hermetic dirs). Never throws, never logs to stdout (hook stdout is protocol).
    2. Producers (all lazy-require the module inside try/catch; a writer fault never affects the turn):
       - selector-dispatcher.cjs: inside the :1052 `payloadObj.emitTelemetry === true` block, alongside emitPresentationTelemetry, call recordReachedGate with the sessionId available in the payload/opts (thread it via payload if absent - inspect what emitPresentationTelemetry already receives; if no session id reaches this seam, record under a 'no-session' key that the consumer maps to the degenerate floor, and document it).
       - intent-classifier.cjs engine arm: immediately after the appendAskUserQuestionTrailer call (:1007 region, post-209-01), record { sessionId, surface: 'scripts/intent-classifier.cjs', shape: 'F.1' } - sessionId is in scope at the caller (:2441 region threads it; pass through ctx like roomDir was in 209-01).
       - intent-classifier.cjs emitBindingGate: after the envelope is built (sessionId is a direct arg), record shape 'F.8'.
    3. Do NOT touch check-card-fire.cjs in this task (Task 2 owns the consumer).
    Tests: hermetic tmp side-file for all behaviors; the fs_scope no-write assertion drives pickShape directly with and without the flag.
  </action>
  <verify>
    <automated>node tests/test-209-primary-sidechannel.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-209-primary-sidechannel.cjs` exits 0
    - `grep -c "recordReachedGate" lib/hmi/selector-dispatcher.cjs` returns 1 and the call sits inside the emitTelemetry guard (source assertion in the test)
    - `grep -c "recordReachedGate" scripts/intent-classifier.cjs` returns 2 (engine arm + emitBindingGate)
    - Corrupt-file fixture: readReachedGates returns [] with no throw (asserted)
  </acceptance_criteria>
  <done>Three producers live, writer never-throw, fs_scope contract preserved at the dispatcher, hermetic tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: H3 - wire the PRIMARY consumer in check-card-fire.cjs</name>
  <files>scripts/check-card-fire.cjs, tests/test-209-primary-sidechannel.cjs</files>
  <read_first>
    - scripts/check-card-fire.cjs :36-58 (doctrine header - UPDATE it: PRIMARY is no longer deferred; name the producer module), the deriveTurnSignals function (find it; it builds the turn object the classifier consumes), :330-365 (turnContextHash consumes t.ran_entries first in the key precedence - already shipped, do not change), :159/:179/:462-481 (the constitutional floor - byte-untouched)
  </read_first>
  <behavior>
    - A turn whose session has side-channel records and NO AskUserQuestion tool-use classifies as a reached-gate-no-card intercept even when the output text carries no ASCII-box glyphs (the incident's free-conversation false-negative shape, verdict 25 - now caught by PRIMARY)
    - The same turn WITH a fired card classifies pass
    - An empty/missing/corrupt side file leaves behavior byte-identical to today (regex SECONDARY still the detector; asserted against existing fixtures)
  </behavior>
  <action>
    In deriveTurnSignals (or the envelope-normalization seam where the turn object is assembled): lazy-require card-fire-sidechannel.cjs, call readReachedGates(session_id), and populate `t.ran_entries` / `t.reached_gate_entries` with the entry strings when non-empty. The already-shipped downstream logic (PRIMARY classification + the ran_entries retry-key precedence at :347) activates untouched. Direct-field envelopes (the unit-test shape) keep precedence: only populate from the side file when the envelope did not already carry the fields. Update the WR-04 doctrine comment (:46-54) to record that the producer now exists (name the module and the three sites) and that regex stays SECONDARY permanently. Extend the test file: drive the hook's exported classify path with a staged side file + transcript fixtures for the three behaviors.
  </action>
  <verify>
    <automated>node tests/test-209-primary-sidechannel.cjs && node scripts/check-card-fire.cjs --help 2>/dev/null; node -e "require('./scripts/check-card-fire.cjs')" 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/test-209-primary-sidechannel.cjs` exits 0 including the PRIMARY-live behaviors
    - `grep -c "readReachedGates" scripts/check-card-fire.cjs` returns >= 1
    - The floor is byte-untouched: `grep -n "MAX_FORCE_RETRIES = 3" scripts/check-card-fire.cjs` and `grep -n "MAX_SESSION_INTERCEPTS = 12" scripts/check-card-fire.cjs` both still match; git diff shows no edits within :462-481
    - Existing check-card-fire test suites (the 179-wave e2e/unit files under tests/) still exit 0
  </acceptance_criteria>
  <done>PRIMARY detection live and registry-keyed; regex secondary; floor untouched; all existing suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: H4 - session-start stops teaching the anti-pattern</name>
  <files>scripts/session-start, tests/test-209-session-start-exemplar.cjs</files>
  <read_first>
    - scripts/session-start :583-587 (the 'Other rooms:' prose injection) and :673-679 (MODE_MENU here-string at :675, injection at :677) - NOTE this is a BASH script with very long single-line strings; the edit is a here-string content rewrite, not a JS edit
    - commands/ignite.md :98-106 (the shipped fire-mandate wording to mirror in the instruction text)
    - lib/core/room-chooser.cjs :198-216 (renderRoomChooserCard - session-start is bash and cannot call it directly; the fix is an INSTRUCTED fire, the research's 'or instruct the fire explicitly' arm)
  </read_first>
  <behavior>
    - scripts/session-start no longer contains the literal 'Type 1, 2, or 3' anywhere
    - The no-room injection instructs a card fire: the MODE_MENU replacement keeps the three mode descriptions as option content but wraps them in an explicit instruction ('Fire the AskUserQuestion card with these three modes as the options ... never render this menu as text (SEED-021)')
    - The 'Other rooms:' block becomes an instruction to offer the rooms via an AskUserQuestion card (naming the rooms as option content), not a prose list ending in a bare command hint
    - bash -n scripts/session-start exits 0
  </behavior>
  <action>
    1. Rewrite the MODE_MENU here-string (:675): keep the three mode titles + one-line descriptions as structured option content; remove the box-art framing ('======') and the closing 'Type 1, 2, or 3 -- or just start talking' line; prepend/append the imperative instruction naming AskUserQuestion and SEED-021 (mirror ignite.md:98-106 wording). Update the :677 context line from 'Present the three modes below' to 'Fire the AskUserQuestion card with the three modes below as options; default to Just Talk if the navigator starts talking instead of picking'.
    2. Rewrite the :585 'Other rooms:' injection: instruct Larry to offer the listed rooms through an AskUserQuestion card (the room-chooser fire), keeping the '/mos:rooms' pointer as a non-option footer line.
    3. Preserve bash escaping exactly (the strings are \n-escaped single-line assignments); verify with bash -n.
    Tests (tests/test-209-session-start-exemplar.cjs): source-assert absence of 'Type 1, 2, or 3', presence of 'AskUserQuestion' in both edited regions, presence of 'SEED-021', and shell out to `bash -n scripts/session-start`.
  </action>
  <verify>
    <automated>node tests/test-209-session-start-exemplar.cjs && bash -n scripts/session-start</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Type 1, 2, or 3" scripts/session-start` returns 0
    - `grep -c "AskUserQuestion" scripts/session-start` returns >= 2 (both regions)
    - `bash -n scripts/session-start` exits 0
    - `node tests/test-209-session-start-exemplar.cjs` exits 0
  </acceptance_criteria>
  <done>The session's opening exemplar teaches the card; the forbidden literal is gone; script still parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| side-channel file (~/.mindrian) -> Stop-hook classification | a world-writable-adjacent local file feeds detection input; malformed or injected records could distort intercept decisions |
| session-start here-strings -> every session's opening context | a bash quoting mistake corrupts every session start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-23 | Tampering | injected/malformed side-channel records | mitigate | reader validates shape (string entries only, sessionId-keyed), size cap + TTL prune, JSON parse in try/catch returning []; a poisoned record can at worst cause ONE bounded intercept (the un-flappable session ceiling MAX_SESSION_INTERCEPTS=12 stays untouched) and never weakens blocking (records ADD detection, never suppress it - card_fired comes from the transcript, not the side file) |
| T-209-24 | Denial of Service | writer/reader fault in the 3000ms hook | mitigate | never-throw contract on both ends, atomic tmp+rename writes, size-capped reads; corrupt-file degrade asserted |
| T-209-25 | Information Disclosure | side-file content | mitigate | records carry only { surface path, shape, ts } - registry enums, zero user text, LOCAL only (Part 8); grep-provable no room content in the writer |
| T-209-26 | Tampering | fs_scope breach at pickShape | mitigate | producer sits INSIDE the emitTelemetry===true guard; the no-flag-no-write behavior is a required test assertion (render-v2 Zone 4 stays zero-FS) |
| T-209-27 | Denial of Service | session-start corruption | mitigate | bash -n gate in the test + minimal-diff here-string edit; the forbidden-literal grep is the regression lock |
</threat_model>

<verification>
- `node tests/test-209-primary-sidechannel.cjs` and `node tests/test-209-session-start-exemplar.cjs` exit 0
- `bash tests/run-all-209.sh` exits 0 (both legs PASS)
- Existing 179-wave check-card-fire suites still green (run the run-all-179.sh aggregator if present)
- Constitutional floor grep-verified untouched (:159, :179, :462-481)
- No em-dashes in touched files
</verification>

<success_criteria>
- PRIMARY detection live at all three mint sites; regex demoted to secondary but never removed
- The incident's free-conversation false-negative shape is now detectable post-hoc
- Session start models the fire, not the box
</success_criteria>

<output>
Create `.planning/phases/209-shape-f-native-fire/209-06-SUMMARY.md` when done. Record the side-channel schema and the sessionId threading decision at the dispatcher.
</output>
