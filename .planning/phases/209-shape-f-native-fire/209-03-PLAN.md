---
phase: 209-shape-f-native-fire
plan: 03
type: execute
wave: 2
depends_on: [209-02]
files_modified:
  - scripts/check-shape-declaration.cjs
  - scripts/build-render-coverage.cjs
  - scripts/check-render-coverage.cjs
  - data/render-coverage-registry.json
  - commands/futures.md
  - tests/test-209-declared-implies-wired.cjs
autonomous: true
requirements: [B2, B3]
must_haves:
  truths:
    - "A command that declares hitl_shape but neither carries the firing block nor mentions AskUserQuestion fails the build closed"
    - "A declaring command whose restrictive allowed-tools list omits AskUserQuestion fails the build closed"
    - "A declared shape contradicting the body shape (the futures.md F.2 vs F.1 case) fails the build closed - and futures.md itself is reconciled"
    - "The render-coverage registry now carries the commands/*.md keyspace with { surface, declared_shape, wired } entries and the gate lands GREEN on the B1-stamped tree"
  artifacts:
    - path: "scripts/check-shape-declaration.cjs"
      provides: "B2: three new declared-implies-wired predicates"
      contains: "firing block"
    - path: "scripts/build-render-coverage.cjs"
      provides: "B3: parallel .md keyspace walk over commands/*.md with hitl_shape frontmatter"
      contains: "declared_shape"
    - path: "scripts/check-render-coverage.cjs"
      provides: "B3: fail-closed .md wired predicate with excluded+reason escape hatch"
    - path: "data/render-coverage-registry.json"
      provides: "regenerated registry carrying both keyspaces"
      contains: "declared_shape"
    - path: "tests/test-209-declared-implies-wired.cjs"
      provides: "gate proof incl. adversarial unwired fixture + futures drift regression"
      min_lines: 80
  key_links:
    - from: "scripts/check-render-coverage.cjs"
      to: "data/render-coverage-registry.json"
      via: "wired:false entries fail the gate"
      pattern: "wired"
    - from: "scripts/build-render-coverage.cjs"
      to: "commands/*.md"
      via: "STAMP_MARKER / AskUserQuestion-mention wired predicate (B1 marker designed with B3)"
      pattern: "mos:firing-block"
---

<objective>
Fix IDs B2 + B3. Make "declared implies wired" enforceable and enforced - the R16 pattern one plane up. B2 extends scripts/check-shape-declaration.cjs with three predicates on declaring commands; B3 extends the render-coverage build+check pair to a second keyspace (commands/*.md), failing closed. The gate turns ON in this plan, AFTER plan 209-02's stamp flipped the tree to wired (LOCKED sequencing: green, not red-flooding CI).

First caught case: commands/futures.md declares `hitl_shape: "F.2"` at line 6 while its body specifies Shape F.1 at lines 69 and 77 (verified live). This plan reconciles it.

Purpose: an unwired declared command can never ship again.
Output: extended validator, extended build+check pair, regenerated registry, reconciled futures.md, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-shape-f-native-fire/209-CONTEXT.md
@.planning/phases/209-shape-f-native-fire/209-PATTERNS.md
@scripts/check-shape-declaration.cjs
@scripts/build-render-coverage.cjs
@scripts/check-render-coverage.cjs
@scripts/stamp-firing-block.cjs
@commands/futures.md
</context>

## Artifacts this phase produces (this plan's share)

- Three new B2 predicates in `scripts/check-shape-declaration.cjs` (body-carries-fire, tool-granted, declared-matches-body)
- `listDeclaringCommands()` (or equivalent) - new .md keyspace walk in `scripts/build-render-coverage.cjs`
- New registry entry shape `{ surface, declared_shape, wired }` (plus optional `excluded: true` + `reason`) in `data/render-coverage-registry.json`
- New .md wired predicate in `scripts/check-render-coverage.cjs`
- `commands/futures.md` frontmatter reconciled (`hitl_shape` aligned with its body)
- `tests/test-209-declared-implies-wired.cjs` - new test file

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B2 - three declared-implies-wired predicates in check-shape-declaration.cjs + reconcile futures.md</name>
  <files>scripts/check-shape-declaration.cjs, commands/futures.md, tests/test-209-declared-implies-wired.cjs</files>
  <read_first>
    - scripts/check-shape-declaration.cjs :120-180 (check() validator skeleton - checks 2/3/4/5 live here; you are adding 6/7/8), :410 (frontmatterToFixture - already lifts hitl_shape/allowed-tools; extend it to also carry the body flags the new predicates need)
    - scripts/stamp-firing-block.cjs (STAMP_MARKER + CANONICAL_FIRING_BLOCK from plan 209-02 - the predicate MUST import/reuse the exported STAMP_MARKER constant, never a second literal)
    - commands/futures.md :1-10 and :60-85 (the F.2-vs-F.1 drift; read the body's gate description to decide the correct shape before reconciling)
    - .planning/phases/209-shape-f-native-fire/209-PATTERNS.md ("B2" pattern assignment; grant predicate semantics from plan 02: absent allowed-tools key counts as GRANTED)
  </read_first>
  <behavior>
    - Test 1: a fixture declaring hitl_shape with a body carrying neither STAMP_MARKER nor 'AskUserQuestion' produces a violation naming the surface and the missing firing block
    - Test 2: a fixture with a restrictive allowed-tools list lacking AskUserQuestion produces a grant violation; an ABSENT allowed-tools key produces NO violation (absent = unrestricted)
    - Test 3: a fixture declaring "F.2" whose body text contains 'Shape F.1' produces a shape-contradiction violation; matching shapes pass; a body with no shape mention passes (no contradiction detectable)
    - Test 4: the exclusion escape hatch (connector excluded:true + non-empty reason) exempts all three predicates; excluded:true with an EMPTY reason still fails
    - Test 5: post-reconcile, running the extended checker over the live commands/ tree exits 0
  </behavior>
  <action>
    1. Extend frontmatterToFixture (:410 region) so command fixtures also carry: `body_has_firing_block` (body contains the imported STAMP_MARKER), `body_mentions_tool` (body contains 'AskUserQuestion'), `body_shape_mentions` (array of distinct F.N tokens matched by /Shape\s+F\.(\d)/g plus /hitl_shape[^F]*F\.(\d)/ style mentions in the BODY only, never the frontmatter), and `allowed_tools` (null when the key is absent, else the parsed list).
    2. Add three predicates to check() for surfaces that declare hitl_shape (skip 'none' and staged-only declarations):
       (6) wired-body: body_has_firing_block OR body_mentions_tool, else violation 'declares <shape> but the body neither carries the canonical firing block nor mentions AskUserQuestion'.
       (7) tool-grant: allowed_tools === null passes; a non-null list must include 'AskUserQuestion'.
       (8) declared-matches-body: if body_shape_mentions is non-empty and none equals the declared shape, violation naming both (the futures.md case).
       All three honor the existing excluded:true + non-empty reason escape hatch (check 5's idiom at :177+).
    3. Reconcile commands/futures.md: read the body's actual gate at :69/:77; the body says Shape F.1 twice, so flip the frontmatter `hitl_shape: "F.2"` to `"F.1"` UNLESS the body reading shows the gate is genuinely a multi-select (then fix the two body lines instead) - record which way and why in the SUMMARY.
    4. Tests: in-memory fixtures for behaviors 1-4; behavior 5 shells out to the real checker.
  </action>
  <verify>
    <automated>node tests/test-209-declared-implies-wired.cjs && node scripts/check-shape-declaration.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-shape-declaration.cjs` exits 0 on the live tree AFTER the futures.md reconcile
    - Reverting the futures.md reconcile in a temp copy makes the checker report a shape-contradiction violation naming futures (asserted in the test via a fixture, not by mutating the live file)
    - `grep -c "AskUserQuestion" scripts/check-shape-declaration.cjs` returns >= 1
    - `node tests/test-209-declared-implies-wired.cjs` exits 0
  </acceptance_criteria>
  <done>Three predicates live and fail-closed with the escape hatch; futures.md drift reconciled and regression-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: B3 - extend render coverage to the .md keyspace and turn the gate on</name>
  <files>scripts/build-render-coverage.cjs, scripts/check-render-coverage.cjs, data/render-coverage-registry.json, tests/test-209-declared-implies-wired.cjs</files>
  <read_first>
    - scripts/build-render-coverage.cjs :19-23 (header contract), :71-83 (render-only-excluded table - the excluded:true + reason model), :91-111 (listSourceFiles .cjs walk - PARALLEL it, do not repurpose it)
    - scripts/check-render-coverage.cjs :29 and :185-191 (the .cjs call-site wired predicate - your .md analogue mirrors this shape)
    - data/render-coverage-registry.json (current object shape: { generated_note, dispatcher, render_counts, entries } - 16 .cjs entries; the .md keyspace must be ADDITIVE, existing entries byte-stable)
    - scripts/stamp-firing-block.cjs (import STAMP_MARKER - one shared constant across B1/B2/B3, never three literals)
  </read_first>
  <behavior>
    - Test 6: build regenerates the registry with every hitl_shape-declaring command as an entry { surface: 'commands/<name>.md', declared_shape, wired: true|false }; the 16 existing .cjs entries are unchanged
    - Test 7: check fails (non-zero exit, names the surface) on a registry entry with declared_shape set and wired:false; an entry with excluded:true + non-empty reason passes; excluded:true without a reason fails
    - Test 8 (adversarial, fail-closed proof): synthesize a temp declaring command with no firing block, no AskUserQuestion mention, and a restrictive allowed-tools list; build over a fixture dir containing it yields wired:false and check exits non-zero
    - Test 9 (green on live): on the real B1-stamped tree, build then check exits 0 with 0 unwired declared .md entries
  </behavior>
  <action>
    1. build-render-coverage.cjs: add a parallel walk over commands/*.md reading frontmatter; for each file with a hitl_shape key (excluding 'none'), emit `{ surface, declared_shape, wired }` where wired = (body contains STAMP_MARKER OR body contains 'AskUserQuestion') AND (allowed-tools key absent OR list includes 'AskUserQuestion') - the exact B1/B2 predicate, one shared semantics. Carry an md-keyspace exclusion table mirroring :71-83 (empty at ship; the escape hatch exists structurally). Keep the .cjs walk and its entries byte-identical; extend generated_note to name both keyspaces.
    2. check-render-coverage.cjs: after the existing .cjs checks, iterate the .md entries; any declared_shape-set entry with wired:false and no excluded+reason fails closed with a message naming the surface and which half failed (body vs grant). Exit codes and output style mirror the existing checks.
    3. Regenerate data/render-coverage-registry.json on the live tree and commit the regenerated file.
    4. Tests: fixture-dir based for behaviors 6-8 (never mutate live commands/); behavior 9 shells out to the real build + check.
  </action>
  <verify>
    <automated>node scripts/build-render-coverage.cjs && node scripts/check-render-coverage.cjs && node tests/test-209-declared-implies-wired.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-render-coverage.cjs` exits 0 with 0 unwired declared .md entries on the live tree
    - `node -e "const r=require('./data/render-coverage-registry.json'); const md=r.entries.filter(e=>String(e.surface||'').endsWith('.md')); if(md.length<90) process.exit(1)"` exits 0 (the ~99 declaring commands are enumerated; exact count recorded in SUMMARY)
    - The adversarial unwired fixture makes the check exit non-zero (asserted in test)
    - `node tests/test-209-declared-implies-wired.cjs` exits 0
  </acceptance_criteria>
  <done>Declared-implies-rendered fails closed on the .md keyspace, lands green on the stamped tree, escape hatch modeled, registry regenerated additively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build gate -> what ships | a false NEGATIVE (unwired command scored wired) lets a declared-but-unwired surface ship silently - the exact RC-4 failure this phase exists to kill |
| exclusion escape hatch | excluded:true is a policy bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-209-10 | Tampering | wired predicate divergence | mitigate | ONE shared STAMP_MARKER constant imported from stamp-firing-block.cjs across B1/B2/B3; a drifted second literal would silently green the gate - forbidden by construction and asserted by grepping for a single definition site in the test |
| T-209-11 | Spoofing | gate bypass via excluded:true | mitigate | escape hatch requires a NON-EMPTY reason (Test 4/7); the md exclusion table ships EMPTY; any future entry is a reviewed diff on a committed file |
| T-209-12 | Repudiation | which surface failed and why | mitigate | check output names the surface and the failing half (body vs grant vs shape-contradiction), never a bare exit code |
| T-209-13 | Denial of Service | red-flooding CI | mitigate | LOCKED sequencing - this plan runs in wave 2 after 209-02's stamp; Test 9 proves green-on-live before the gate is committed |
| T-209-14 | Tampering | registry regeneration clobbering .cjs entries | mitigate | Test 6 asserts the 16 existing entries byte-stable; the md walk is parallel and additive |
</threat_model>

<verification>
- `node scripts/check-shape-declaration.cjs` exits 0
- `node scripts/build-render-coverage.cjs && node scripts/check-render-coverage.cjs` exits 0 with 0 unwired declared .md entries
- `node tests/test-209-declared-implies-wired.cjs` exits 0
- Pre-commit still green: `node scripts/build-connector-registry.cjs --check` and `node scripts/build-orchestration-projection.cjs --check` exit 0 (no collateral)
- No em-dashes in any touched file
</verification>

<success_criteria>
- The futures.md drift is both fixed and impossible to reintroduce silently
- "Declared implies rendered" is a closed loop: B1 stamp marker = B2 body predicate = B3 wired predicate, one constant
- Gate is ON, green, and fail-closed with a reviewable escape hatch
</success_criteria>

<output>
Create `.planning/phases/209-shape-f-native-fire/209-03-SUMMARY.md` when done. Record the md-entry count and the futures.md reconcile direction.
</output>
