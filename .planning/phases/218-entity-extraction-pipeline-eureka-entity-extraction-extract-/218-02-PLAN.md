---
phase: 218-entity-extraction-pipeline-eureka-entity-extraction-extract-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/room-db.cjs
  - lib/core/eureka/entity-extractor.cjs
  - tests/test-218-write-safety.cjs
  - tests/test-218-extractor.cjs
autonomous: true
requirements: [REQ-1, D-05]
user_setup: []

must_haves:
  truths:
    - "openRoomDb passes {timeout:5000} to DatabaseSync on both construction branches"
    - "openRoomDb sets PRAGMA synchronous=NORMAL in addition to WAL + foreign_keys"
    - "A freshly opened room.db reports busy_timeout=5000 and synchronous=1 (NORMAL)"
    - "extractEntities reads markdown prose and returns {entities, relations} with zero network calls"
    - "extractEntities never throws on malformed input and caps output per artifact"
  artifacts:
    - path: "lib/core/room-db.cjs"
      provides: "D-05 write-safety: timeout:5000 on both DatabaseSync branches + synchronous=NORMAL"
      contains: "timeout: 5000"
    - path: "lib/core/eureka/entity-extractor.cjs"
      provides: "tier-1 regex/heading/capitalization extractor returning {entities, relations}"
      exports: ["extractEntities"]
    - path: "tests/test-218-write-safety.cjs"
      provides: "PRAGMA busy_timeout/synchronous probe + rollback-on-error leg"
  key_links:
    - from: "lib/core/room-db.cjs::openRoomDb"
      to: "node:sqlite DatabaseSync constructor"
      via: "options object carrying timeout:5000 on both branches"
      pattern: "timeout: 5000"
    - from: "lib/core/eureka/entity-extractor.cjs"
      to: "artifact markdown text"
      via: "text.split(/\\r?\\n/) regex heuristics, no fs/network from the pure extractor"
      pattern: "split\\("
---

<objective>
Two independent Wave-1 foundations the dispatcher (Plan 03) consumes: (1) the D-05 SQLite write-safety edit to `openRoomDb()` so concurrent extraction-vs-live-conversation writes wait instead of failing in 0ms, and (2) the pure tier-1 prose extractor that reads artifact markdown and returns typed entity/relation candidates with zero network egress (Canon Part 8).

Purpose: the write-safety change protects the extraction-job-vs-live-write scenario this pipeline introduces (verified live: no timeout = 0ms SQLITE_BUSY; {timeout:5000} = full wait window). The extractor is the read/parse half of REQ-1's "reads artifact markdown text and writes new graph nodes."
Output: an additive edit to `room-db.cjs`, a new `lib/core/eureka/entity-extractor.cjs`, and a write-safety test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-SPEC.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-CONTEXT.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-RESEARCH.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-05 write-safety edit to openRoomDb + PRAGMA-probe / rollback test</name>
  <read_first>
    - lib/core/room-db.cjs (lines 92-141, especially the two-branch construction at 106-110: the allowExtension branch and the bare branch, then the WAL + foreign_keys pragmas)
    - .planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-RESEARCH.md (the D-05 file:line sheet + Pitfall 2 "timeout MUST reach BOTH branches" + Pitfall 3 "DatabaseSync has no .transaction() helper")
  </read_first>
  <behavior>
    - Test: after openRoomDb(tmpRoom), db.prepare('PRAGMA busy_timeout').get() returns value 5000
    - Test: after openRoomDb(tmpRoom), db.prepare('PRAGMA synchronous').get() returns value 1 (NORMAL)
    - Test: a BEGIN; insert-then-force-error; ROLLBACK sequence leaves zero partial rows (explicit-transaction rollback works with node:sqlite's exec-based BEGIN/COMMIT/ROLLBACK, since DatabaseSync has no .transaction() helper)
  </behavior>
  <action>Per D-05, edit `lib/core/room-db.cjs::openRoomDb` (construction at lines 106-110). Fold `timeout: 5000` into the `DatabaseSync` options object on BOTH branches (Pitfall 2): the extension branch becomes `new DatabaseSync(dbPath, { allowExtension: true, timeout: 5000 })` and the bare branch becomes `new DatabaseSync(dbPath, { timeout: 5000 })` - the bare branch currently passes no options object, so the edit must add one. After the existing `PRAGMA journal_mode = WAL` and `PRAGMA foreign_keys = ON` execs, add `db.exec('PRAGMA synchronous = NORMAL')` (no constructor equivalent exists for synchronous - it stays a db.exec call). This is a GLOBAL change to every openRoomDb caller and is strictly more forgiving (a longer busy wait, never a new failure mode). Per RESEARCH Assumption A2, re-grep the tests/ tree first to confirm no existing test asserts on the current 0ms-fail-under-contention behavior before editing. Write `tests/test-218-write-safety.cjs` (plain-node assert, temp room dir) covering the three <behavior> legs, using an explicit `db.exec('BEGIN')` / `db.exec('ROLLBACK')` for the rollback leg (NOT a .transaction() helper - node:sqlite lacks it).</action>
  <verify>
    <automated>node tests/test-218-write-safety.cjs</automated>
  </verify>
  <done>test-218-write-safety.cjs passes; `grep -c "timeout: 5000" lib/core/room-db.cjs` >= 2 (both branches); `grep -c "synchronous = NORMAL" lib/core/room-db.cjs` >= 1; a freshly opened db reports busy_timeout=5000 and synchronous=1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tier-1 entity extractor (pure regex/heading parser, zero egress)</name>
  <read_first>
    - lib/core/shallow-doc-parser.cjs (whole file - the four disciplines to inherit: regex over text.split(/\r?\n/) at ~116; bounded output / parseClaims(text,max) cap at 113-127; graceful degradation top-level try/catch returning empty at 207-209; zero egress, no fetch/http/Brain at 13-16; stripPii TitleCase/year/email/URL idioms at 267-286)
    - .planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-RESEARCH.md (Pitfall 4 "over-extraction noise" - strip code spans, skip heading-only tokens, use heading context to type; the regexes are Claude's discretion tuned to real prose)
    - .planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-CONTEXT.md (Claude's Discretion block: exact regex/heading-heuristic rules; MISC-label disambiguation is OUT of scope)
  </read_first>
  <behavior>
    - Test: extractEntities("## Competitors\nProdrive is a rival of Xtrac.", {sourceArtifactId:'a1'}) returns an object with `entities` and `relations` arrays (both present, entities non-empty for capitalized proper nouns in body prose)
    - Test: each entity has {entityType in {company,technology,market}, name (non-empty string), sourceArtifactId}
    - Test: extractEntities("", {}) and extractEntities(null, {}) return {entities:[], relations:[]} without throwing
    - Test: entities-per-artifact is bounded (a greedy Title-Case flood does not return an unbounded list)
  </behavior>
  <action>Create `lib/core/eureka/entity-extractor.cjs` (home per CONTEXT: follow lib/core/eureka/*.cjs conventions), a PURE parser that touches NO database and makes NO network call. Export `extractEntities(markdown, opts)` returning `{ entities: [{ entityType, name, sourceArtifactId }], relations: [{ source, target, edge_type }] }` where `source`/`target` are entity names (the dispatcher resolves them to node ids and routes writes through navigation). Inherit the four `shallow-doc-parser.cjs` disciplines verbatim: (1) operate over `markdown.split(/\r?\n/)`, no markdown-AST dependency; (2) bound output with a `maxPerArtifact` cap (default e.g. 25) so a greedy capitalization regex cannot re-flood the graph (Pitfall 4) - precedent is parseClaims's `if (out.length >= limit) break`; (3) wrap the whole body in a top-level `try { ... } catch (_) { return { entities: [], relations: [] }; }` - never throw on caller input; (4) zero egress - no `require('node:http')`, no `fetch`, no Brain call. Extraction heuristics are Claude's discretion tuned against aion-eureka-synergy prose: strip backtick code spans before scanning, skip heading-only tokens unless they recur in body prose, use heading context to type (a "## Competitors"/"## Market" section leans the capitalized tokens toward company/market), reuse the stripPii TitleCase/year/URL regex idioms to isolate proper-noun runs. Do NOT build entity-type disambiguation beyond capitalization + heading-context (MISC-label logic is tier-2, out of scope per CONTEXT). Relation candidates: when two entity names co-occur under a rivalry/supply/component cue in the same line/section, emit a COMPETES_WITH / SUPPLIES_TO / USES_COMPONENT relation (representable-of-the-Prodrive-cross-classification is the motivating case). Create `tests/test-218-extractor.cjs` (named test file, plain-node `assert/strict` + `check()` counter per house convention) asserting: (a) the `maxPerArtifact` bounded-output cap actually caps a synthetic greedy-Title-Case-flood input at the configured limit, (b) every returned entity has the exact `{entityType, name, sourceArtifactId}` shape with `entityType` in `{company,technology,market}`, (c) the null/empty-input never-throw contract, (d) the Part-8 zero-egress grep gate. This test file is also added to Plan 218-03's aggregator leg (a) alongside test-218-edge-vocab.cjs/test-218-entity-writer.cjs/test-218-write-safety.cjs.</action>
  <verify>
    <automated>node -e "const {extractEntities}=require('./lib/core/eureka/entity-extractor.cjs'); const r=extractEntities('## Competitors\nProdrive is a rival of Xtrac.',{sourceArtifactId:'a1'}); const a=require('node:assert/strict'); a.ok(Array.isArray(r.entities)&&Array.isArray(r.relations)); a.deepEqual(extractEntities(null,{}),{entities:[],relations:[]}); console.log('ok - extractor smoke');" &amp;&amp; node tests/test-218-extractor.cjs</automated>
  </verify>
  <done>entity-extractor.cjs exports extractEntities returning {entities,relations}; returns empty on null/empty without throwing; output is bounded; `! grep -rnE "fetch|https?\\.|require\\('node:http|require\\('node:https" lib/core/eureka/entity-extractor.cjs` is clean (zero network, Canon Part 8); tests/test-218-extractor.cjs exists and passes, covering the bounded-cap and entity-shape behavioral legs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact prose -> extractor | Untrusted markdown body text is parsed by regex heuristics |
| concurrent writers -> room.db | Extraction worker and a live conversation may both hold write intent on the same WAL file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-218-04 | Denial of Service (of signal) | extractEntities output volume | mitigate | Bounded output via `maxPerArtifact` cap (parseClaims precedent); type only well-signaled capitalized tokens; strip code spans. Prevents re-flooding the graph with junk. |
| T-218-05 | Information Disclosure (Canon Part 8 breach) | extractor egress surface | mitigate | Tier-1 makes zero network calls by construction; grep gate asserts no fetch/http/https import. No Brain call. |
| T-218-06 | Denial of Service | concurrent write contention | mitigate | D-05: openRoomDb {timeout:5000} on both branches gives a ~5s busy-wait window instead of 0ms fail; WAL readers never block writers (writer-vs-writer only). |
| T-218-07 | Availability / data integrity | malformed prose input | mitigate | Top-level try/catch returns {entities:[],relations:[]} - extractor never throws on caller input. |
| T-218-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan; slopcheck N/A per RESEARCH Package Legitimacy Audit. |
</threat_model>

## Artifacts this phase produces (Plan 02)

New files:
- `lib/core/eureka/entity-extractor.cjs`
- `tests/test-218-write-safety.cjs`

New symbols:
- `extractEntities(markdown, opts)` (exported from entity-extractor.cjs) returning `{ entities: [{ entityType, name, sourceArtifactId }], relations: [{ source, target, edge_type }] }`

Modified (no new symbol, additive behavior only):
- `lib/core/room-db.cjs::openRoomDb` gains `{ timeout: 5000 }` on both DatabaseSync branches + `PRAGMA synchronous = NORMAL`

<verification>
- `node tests/test-218-write-safety.cjs` green (busy_timeout=5000 on both branches, synchronous=1, rollback leaves no partial rows).
- extractor smoke asserts `{entities,relations}` arrays and empty-on-null.
- `! grep -rnE "fetch|https?\.|require\('node:http" lib/core/eureka/entity-extractor.cjs` (Canon Part 8 zero egress).
- `git grep -n "openRoomDb(" -- tests/` re-checked: no existing test asserts on the pre-D-05 0ms-fail behavior (Assumption A2).
</verification>

<success_criteria>
openRoomDb sets timeout:5000 on both DatabaseSync branches plus synchronous=NORMAL, verifiable via PRAGMA probe; the tier-1 extractor parses markdown into bounded, typed {entities,relations} candidates with zero network egress and never throws.
</success_criteria>

<output>
Create `.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-02-SUMMARY.md` when done.
</output>
