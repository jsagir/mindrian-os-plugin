---
phase: 218-entity-extraction-pipeline-eureka-entity-extraction-extract-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/edges.cjs
  - lib/core/navigation/typed-entity.cjs
  - lib/core/navigation.cjs
  - tests/test-218-edge-vocab.cjs
  - tests/test-218-entity-writer.cjs
autonomous: true
requirements: [REQ-1, REQ-2]
user_setup: []

must_haves:
  truths:
    - "ALLOWED_EDGE_TYPES contains COMPETES_WITH, USES_COMPONENT, SUPPLIES_TO and still contains all 37 prior members"
    - "writeEntityNode mints a company/technology/market node with review_status='proposed'"
    - "writeEntityNode rejects any type outside {company,technology,market} without throwing"
    - "An entity node can be linked to its source memory_artifact node via an ALLOWED_EDGE_TYPES edge"
    - "navigation.cjs re-exports writeEntityNode"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "3 additive domain-relationship edge types after line 632"
      contains: "COMPETES_WITH"
    - path: "lib/core/navigation/typed-entity.cjs"
      provides: "writeEntityNode + linkEntityRelations + frozen ENTITY_NODE_TYPES/ENTITY_EDGE_SUBSET"
      exports: ["writeEntityNode", "linkEntityRelations", "ENTITY_NODE_TYPES", "ENTITY_EDGE_SUBSET", "ENTITY_NODE_ID"]
    - path: "lib/core/navigation.cjs"
      provides: "re-export of writeEntityNode/linkEntityRelations"
      contains: "typed-entity.cjs"
    - path: "tests/test-218-edge-vocab.cjs"
      provides: "floor test for the 3 new edge types"
    - path: "tests/test-218-entity-writer.cjs"
      provides: "writeEntityNode proposed-status + invalid-type + artifact-link test"
  key_links:
    - from: "lib/core/navigation/typed-entity.cjs"
      to: "lib/core/node-insert.cjs::insertNode"
      via: "node write chokepoint (no review_status arg -> DEFAULT 'proposed')"
      pattern: "insertNode\\("
    - from: "lib/core/navigation/typed-entity.cjs"
      to: "lib/core/navigation/edges.cjs::writeEdge"
      via: "edge write chokepoint validated against ALLOWED_EDGE_TYPES"
      pattern: "writeEdge\\("
    - from: "lib/core/navigation.cjs"
      to: "lib/core/navigation/typed-entity.cjs"
      via: "require + re-export"
      pattern: "typed-entity"
---

<objective>
Add the domain-relationship edge vocabulary and the typed entity-node writer that this phase's extractor will use. This is the central chokepoint slice: a verbatim structural clone of `typed-domain.cjs` producing `company`/`technology`/`market` nodes (D-01), plus an additive 3-type extension of `ALLOWED_EDGE_TYPES` (D-02). Every write routes through the existing `insertNode`/`writeEdge` chokepoints (Canon Part 7/9) and every entity node is born `review_status='proposed'` and never auto-confirmed.

Purpose: gives the graph real domain-entity node types and typed relationship edges so the shipped Eureka engine (211-216) and the shared room.db readers reason over content, not one-node-per-file scaffolding.
Output: `lib/core/navigation/typed-entity.cjs`, an additive edit to `edges.cjs`, a re-export in `navigation.cjs`, and two Wave-0 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-SPEC.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-CONTEXT.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-RESEARCH.md
@.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add COMPETES_WITH / USES_COMPONENT / SUPPLIES_TO to ALLOWED_EDGE_TYPES + floor test</name>
  <read_first>
    - lib/core/navigation/edges.cjs (lines 32-633 for the frozen ALLOWED_EDGE_TYPES Set close at 632-633; lines 541-632 for the Phase 200-02 / 189-01 additive comment-block idiom; lines 650-681 for the writeEdge signature and its ALLOWED_EDGE_TYPES.has validation)
    - tests/test-200-02-rs-edge-vocab.cjs (the floor-test pattern to clone verbatim: named-membership + prior-floor survival + frozen, NEVER .size)
  </read_first>
  <behavior>
    - Test: ALLOWED_EDGE_TYPES.has('COMPETES_WITH') === true (and USES_COMPONENT, SUPPLIES_TO)
    - Test: prior floor survives - has('DEFERRED'), has('REJECTED'), has('FEEDS_INTO'), has('SHARES_JOB'), has('NESTED_WITHIN'), has('NOT_REMEMBERED_BECAUSE') all true
    - Test: ALLOWED_EDGE_TYPES instanceof Set && Object.isFrozen(ALLOWED_EDGE_TYPES)
    - Test: has('COMPETES_WITH_MAGIC') === false (arbitrary non-member still rejected)
    - Test: writeEdge(db, {source_id, target_id, edge_type:'COMPETES_WITH', properties:{}}) returns ok:true on a fixture DatabaseSync; an unlisted edge_type returns ok:false
  </behavior>
  <action>Per D-02, additively insert exactly three new string members - `COMPETES_WITH`, `USES_COMPONENT`, `SUPPLIES_TO` - into the frozen `ALLOWED_EDGE_TYPES` Set in `lib/core/navigation/edges.cjs`, placed AFTER the existing `'NOT_REMEMBERED_BECAUSE',` at line 632 and BEFORE the closing `]))` at line 633. The existing 37 members stay byte-untouched (post-phase count = 40, satisfying REQ-2's ">=40 entries"). Precede the three members with a Phase 218 comment block mirroring the lighter Phase 189-01 additive-comment idiom (NOT the navigator-gated 200-02 ceremony): cite Phase 218 + D-02, name each type with source->target direction (COMPETES_WITH = company/technology vs the rival it competes with; USES_COMPONENT = company/technology to the technology/component it uses; SUPPLIES_TO = supplier company to the company it supplies), restate Canon Part 8 (edge properties are ENUM/scalar only, never prose, LOCAL room.db never egressed to Brain), and note the floor-test contract (assert named membership, never `.size`). Do NOT modify `writeEdge` - it validates via `ALLOWED_EDGE_TYPES.has(edge_type)` and accepts the new types automatically. Then create `tests/test-218-edge-vocab.cjs` cloning `tests/test-200-02-rs-edge-vocab.cjs` verbatim (plain-node `node:assert/strict` + `check(desc, fn)` counter, no jest), swapping in the three new members and the writeEdge-acceptance leg described in <behavior>. Write the failing test first, then apply the edges.cjs edit to make it pass.</action>
  <verify>
    <automated>node tests/test-218-edge-vocab.cjs</automated>
  </verify>
  <done>test-218-edge-vocab.cjs passes; `grep -c "COMPETES_WITH\|USES_COMPONENT\|SUPPLIES_TO" lib/core/navigation/edges.cjs` >= 3; the 37 prior members are unchanged (git diff shows only additive lines after 632).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create typed-entity.cjs writeEntityNode/linkEntityRelations + navigation.cjs re-export + writer test</name>
  <read_first>
    - lib/core/navigation/typed-domain.cjs (whole file, ~246 lines - the verbatim structural template: imports 47-48, frozen node-type Set 55-57, frozen edge subset 63-65, isPlainObject 67-69, 31-multiplier id minter 75-83, writeDomainNode writer 98-161, linkDomainToRelated 182-238, exports 240-246)
    - lib/core/node-insert.cjs (lines 90-122 - insertNode(db, id, type, propsJson, {source_path, created_by}); it deliberately omits review_status so the column DEFAULT 'proposed' applies; dual-schema via isMigratedSchema)
    - lib/core/navigation.cjs (lines 43-47 typed-* requires; lines 240-256 the writeDomainNode re-export block to mirror)
    - lib/core/navigation/typed-claim.cjs (lines 100-149 - the proposed-node UPSERT + no-downgrade ON CONFLICT idiom, for the never-auto-confirm guarantee)
  </read_first>
  <behavior>
    - Test: writeEntityNode(db, {entityType:'company', name:'Prodrive', sessionId}) returns ok:true; SELECT review_status FROM nodes WHERE id=<returned id> === 'proposed'
    - Test: same for entityType:'technology' and entityType:'market'
    - Test: writeEntityNode(db, {entityType:'person', name:'X', sessionId}) returns {ok:false, reason:'invalid_entity_type'} and writes NO row
    - Test: writeEntityNode(db, {entityType:'company', name:'', sessionId}) returns {ok:false, reason:'invalid_name'}
    - Test: linkEntityRelations linking an entity node to a memory_artifact node produces a row in edges with a member type (e.g. DESCRIBES) and rejects an off-subset type as a failure entry (never throws)
    - Test: require('lib/core/navigation.cjs').writeEntityNode is a function
  </behavior>
  <action>Per D-01, create `lib/core/navigation/typed-entity.cjs` as a verbatim structural clone of `lib/core/navigation/typed-domain.cjs`. Define `ENTITY_NODE_TYPES = Object.freeze(new Set(['company','technology','market']))` and `ENTITY_EDGE_SUBSET = Object.freeze(new Set(['COMPETES_WITH','USES_COMPONENT','SUPPLIES_TO']))`. Copy `isPlainObject` verbatim. Rename the id minter to `ENTITY_NODE_ID(sessionId, name)` using the same 31-multiplier hash, with prefix `'entity:'`. Implement `writeEntityNode(db, params)` mirroring `writeDomainNode` with the defensive never-throw contract (invalid params -> {ok:false, reason:'invalid_params'}; type not in ENTITY_NODE_TYPES -> {ok:false, reason:'invalid_entity_type'}; empty name -> {ok:false, reason:'invalid_name'}; insertNode wrapped in try/catch -> {ok:false, reason:'entity_write_failed'}). Call `insertNode(db, nodeId, entityType, JSON.stringify(props), { source_path: 'entity:'+sessionId+':'+name, created_by: 'system' })` - do NOT pass review_status (the DEFAULT 'proposed' applies). CRITICAL DELETION: omit the entire `taxonomy === true -> 'confirmed'` promotion branch present in typed-domain (entity nodes are pure truth-claims and stay 'proposed' forever per REQ-1 + the CONTEXT HITL constraint). Implement `linkEntityRelations(db, params)` mirroring `linkDomainToRelated`: route EVERY edge through `writeEdge` (imported from `./edges.cjs`), accepting the three ENTITY_EDGE_SUBSET types plus the already-existing `DESCRIBES` artifact-link type, rejecting anything else as a failure entry (never a throw); return {written, edges, failures}. Export `{ writeEntityNode, linkEntityRelations, ENTITY_NODE_TYPES, ENTITY_EDGE_SUBSET, ENTITY_NODE_ID }`. Then wire the re-export in `lib/core/navigation.cjs`: add `const typedEntity = require('./navigation/typed-entity.cjs');` alongside the line-46 typedDomain require, and add `writeEntityNode: typedEntity.writeEntityNode,` + `linkEntityRelations: typedEntity.linkEntityRelations,` (+ the three frozen exports) in the exports object alongside line 252, reproducing the "mints a node ... routes edges through navigation.writeEdge, never raw SQL" comment block. Write `tests/test-218-entity-writer.cjs` first (plain-node assert, open a fixture via `openRoomDb` on a temp room dir or an in-memory DatabaseSync, seed a memory_artifact node to link to) covering the <behavior> cases, then implement to green.</action>
  <verify>
    <automated>node tests/test-218-entity-writer.cjs</automated>
  </verify>
  <done>test-218-entity-writer.cjs passes; entity nodes land with review_status='proposed'; invalid types are rejected without a row written; require('./lib/core/navigation.cjs').writeEntityNode is callable; no raw `INSERT INTO nodes` or `INSERT INTO edges` appears in typed-entity.cjs (grep clean).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extracted entity name -> SQL | Entity `name`/`properties` strings (later sourced from artifact prose) cross into room.db writes |
| new node/edge type -> closed vocabulary | New node-type and edge-type tokens cross into the frozen allow-lists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-218-01 | Tampering | writeEntityNode -> insertNode | mitigate | Route through `insertNode`'s parameterized `db.prepare(...).run(...)`; never interpolate `name`/`properties` into SQL text. JSON.stringify props only. |
| T-218-02 | Tampering | writeEdge type validation | mitigate | Frozen `Object.freeze(new Set(...))` + `ALLOWED_EDGE_TYPES.has()`; floor test asserts a non-member is rejected. No ad-hoc `if (type===...)`. |
| T-218-03 | Elevation of Privilege | review_status auto-confirm | mitigate | writeEntityNode never passes review_status; DEFAULT 'proposed' applies; the taxonomy->'confirmed' branch is deliberately omitted; UPSERT excludes review_status (no-downgrade). |
| T-218-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this plan (Node built-ins + already-pinned deps only); slopcheck N/A per RESEARCH Package Legitimacy Audit. |
</threat_model>

## Artifacts this phase produces (Plan 01)

New files:
- `lib/core/navigation/typed-entity.cjs`
- `tests/test-218-edge-vocab.cjs`
- `tests/test-218-entity-writer.cjs`

New symbols (exported from typed-entity.cjs and re-exported through navigation.cjs):
- `writeEntityNode(db, params)`
- `linkEntityRelations(db, params)`
- `ENTITY_NODE_TYPES` (frozen Set: company, technology, market)
- `ENTITY_EDGE_SUBSET` (frozen Set: COMPETES_WITH, USES_COMPONENT, SUPPLIES_TO)
- `ENTITY_NODE_ID(sessionId, name)`

New edge-type string members added to `ALLOWED_EDGE_TYPES` in edges.cjs:
- `COMPETES_WITH`, `USES_COMPONENT`, `SUPPLIES_TO`

New entity node `type` values (ride the existing `nodes.type` column, no DDL): `company`, `technology`, `market`.

<verification>
- `node tests/test-218-edge-vocab.cjs` green (3 new members, 37-member floor survives, frozen, non-member rejected, writeEdge accepts/rejects).
- `node tests/test-218-entity-writer.cjs` green (proposed status, invalid-type rejection, artifact-link edge, navigation re-export).
- `git diff lib/core/navigation/edges.cjs` shows only additive lines after 632 (no member removed/reordered).
- `! grep -rnE "INSERT INTO (nodes|edges)" lib/core/navigation/typed-entity.cjs` (no raw SQL from the new writer).
</verification>

<success_criteria>
ALLOWED_EDGE_TYPES has >=40 members additively; writeEntityNode mints proposed-only company/technology/market nodes, rejects other types, links to a source artifact through writeEdge; navigation.cjs re-exports the writer. Both tests pass.
</success_criteria>

<output>
Create `.planning/phases/218-entity-extraction-pipeline-eureka-entity-extraction-extract-/218-01-SUMMARY.md` when done.
</output>
