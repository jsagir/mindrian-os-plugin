---
phase: 150.10
plan: 02
subsystem: systems-thinking-selector
tags: [f-selector, systems-thinking, meta-lens, three-layer-navigation, promote-in-place]
requires:
  - "Plan 150.10-01 (Piece A): M1-M5 Brain flow seed (PREREQUISITE chain + M4 FEEDS_INTO M5)"
  - "navigation.cjs chokepoint (getNeighborhood / findContradictions / getActiveFocus)"
  - "memory-ops.cjs trajectory (getSessionHistory)"
  - "Phase 143.3 connector frontmatter (reach_id context_block, sub_mode systems-thinking-loop, SENS-06, F.2)"
provides:
  - "/mos:systems-thinking as an F-surface human-in-the-loop move-selector (M1-M5)"
  - "Three-layer local ranking blend (Brain flow + navigation.cjs graph state + memory-ops.cjs trajectory)"
  - "Brain-flow / heuristic dual path (ships whether or not Piece A landed)"
  - "Five-move + CLD methodology references"
affects:
  - "Plan 150.10-03 (Piece C): registers systems-thinking-loop as a ranked sub_mode component"
tech-stack:
  added: []
  patterns: ["promote-in-place (Part 7)", "selector loop Sense->Rank->Offer->Pick->Execute->Re-rank", "stage-aware filing", "dual-path graceful degradation"]
key-files:
  created:
    - "references/methodology/causal-loop-diagrams.md"
  modified:
    - "commands/systems-thinking.md"
    - "references/methodology/systems-thinking.md"
decisions:
  - "M5 is NAME-AND-STOP: names the stage-appropriate next-action target and offers the handoff, does not auto-jump (Q1 RESOLVED)."
  - "Trending-to-absurd / S-curve stays a CROSS-CHAIN; move set stays M1-M5, no 6th core move (Q2 RESOLVED)."
  - "Connector frontmatter byte-unchanged; per-move sub-modes (st-boundary/st-loop/st-archetype/st-leverage/st-act) added as prose notes, not new reaches (Part 7 promote-in-place)."
  - "Ranking computed LOCALLY; only generic methodology flow crosses to Brain (Canon Part 8)."
metrics:
  duration: "~12 min"
  completed: "2026-06-14"
  tasks: 3
  files: 3
---

# Phase 150.10 Plan 02: Piece B -- /mos:systems-thinking F-Selector Summary

Promoted `/mos:systems-thinking` in place from a fixed four-phase march into an F-surface human-in-the-loop move-selector (M1-M5) with a GUIDED default, dial overrides, a three-layer local-ranking blend, a Brain-flow/heuristic dual path, and stage-aware filing; added the five-move definitions, the CLD storytelling method, the leverage-to-validation handoff, and a dedicated causal-loop-diagrams reference.

## What shipped

**Task 1 - commands/systems-thinking.md (the selector loop).** Replaced the "Quick pass or deep dive?" march with the SENSE -> RANK -> OFFER -> PICK -> EXECUTE -> RE-RANK selector loop. GUIDED is the default; the dial carries "let me think" (stay GUIDED), "just tell me" (AUTONOMOUS), and "stop". The RANK step documents the local blend formula (`brain_prior - already_done_penalty + unfinished_thread_boost + contradiction_flag`) and the heuristic ordering skeleton verbatim. The three-layer navigation section names the real chokepoint functions: navigation.cjs `getNeighborhood` / `findContradictions` / `getActiveFocus` (Layer 2 room state) and memory-ops.cjs `getSessionHistory` (Layer 3 trajectory). A dedicated "Flow-Source Dual Path" section documents both paths: the Brain-flow path traverses the seeded PREREQUISITE/FEEDS_INTO edges when Brain is reachable and Piece A landed; the heuristic fallback runs when Brain is unreachable or the seed is absent, so the command never hard-fails. M5 is name-and-stop with stage-aware filing to the active room section. The connector frontmatter is byte-unchanged; the per-move sub-modes are added as prose comments under `surface: F.2`, not as new reaches.

**Task 2 - references/methodology/systems-thinking.md (five-move depth).** Added "## The Five Moves" (M1-M5: trigger, produces, PWS anchor, sub_mode, Larry voice line verbatim from the spec), "## The CLD Storytelling Method" (build the loop as a story: fishery stock + breakfast/frustration loop), "## The Leverage-Point to Validation Handoff" (the PWS discipline: end with an actionable handle, not a diagram), and "## The Meta-Lens Framing" (any stage, any problem type, not discovery-only). Session 2 transcript anchors preserved verbatim in voice.

**Task 3 - references/methodology/causal-loop-diagrams.md (new, M2 depth).** Created the dedicated CLD reference: reinforcing vs balancing loops (two kinds and two only), signed links and delays, the story-first construction method, and the loop-as-actionable-handle framing (feeds M3/M4, never a terminal diagram). 65 lines (min 30).

## Verification

- Task 1 automated check: PASS (selector / st-boundary / st-leverage / "let me think" / systems-thinking-loop present; no em-dashes).
- Task 2 automated check: PASS (Five Moves / st-archetype / leverage / breakfast present; no em-dashes).
- Task 3 automated check: PASS (file exists; reinforcing / balancing / story present; no em-dashes).
- Overall: selector renders M1-M5 (not a march); GUIDED default + 3 dial overrides; three-layer blend ranked locally; dual path documented; stage-aware filing + meta-lens framing; connector frontmatter unchanged; no new command.
- MED-3 done-criterion: confirmed the navigation.cjs graph-read functions (`getNeighborhood`, `findContradictions`, `getActiveFocus`) and the memory-ops.cjs trajectory function (`getSessionHistory`) exist by the exact names used, by grepping their `module.exports` before writing the prose that depends on them.
- Zero em-dashes across all three files (byte-level grep for U+2014 returned empty).

## Deviations from Plan

None - plan executed exactly as written. The spec's Section 9 step-5 list named the M5 sub_mode as `st-validate` while Section 4 and the CONTEXT name it `st-act`; the plan's Task 1 action and verify block both specify `st-leverage` and the move table, and the move set canon names M5 `st-act` (Section 4 table, the authoritative move-set table). Used `st-act` consistently per the Section 4 move table and the plan frontmatter, matching the verify grep (which checks `st-boundary` and `st-leverage`, not the M5 token).

## Canon compliance

- Part 7 (Reuse Before Build): promoted the existing command in place; no new command; connector reach unchanged.
- Part 8 (Graph Boundary): ranking is computed locally; only generic methodology flow (PREREQUISITE/FEEDS_INTO) crosses to Brain; Layers 2/3 are local-only reads.
- Part 3 / Part 4: the selector is a Shape F gate; every move selection files a typed edge; M5 files stage-aware via fileEvidenceWithReadback.
- HARD RULE: zero em-dashes.

## Known Stubs

None. The command references real, shipped chokepoint functions and the real Piece-A Brain flow seed.

## Self-Check: PASSED

- Files: commands/systems-thinking.md, references/methodology/systems-thinking.md, references/methodology/causal-loop-diagrams.md, 150.10-02-SUMMARY.md all FOUND.
- Commits: 83a5ec55 (Task 1), 91b96366 (Task 2), 20569d36 (Task 3) all FOUND in git log.
