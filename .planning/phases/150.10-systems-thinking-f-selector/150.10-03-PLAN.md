---
phase: 150.10
plan: 03
type: execute
wave: 2
depends_on: []
autonomous: true
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 8]
requirements: [ST-10, ST-11, ST-12]
files_modified:
  - lib/hmi/reach-component-map.json
  - tests/test-150.10-systems-thinking-repertoire.cjs
  - tests/run-all-150.10.sh

must_haves:
  truths:
    - "systems-thinking-loop is registered as a ranked sub_mode component in lib/hmi/reach-component-map.json, alongside the existing 6 sub_mode components."
    - "The dispatcher's resolveArchetype('systems-thinking-loop') returns a valid archetype (not the default-on-miss), proving it is in the repertoire."
    - "FROZEN-6 INVARIANT holds: REACH_IDS length is still exactly 6; the 6-reach bank is byte-unchanged; systems-thinking is NOT a 7th reach_id."
    - "DIAL_REACH_K stays 6, MAX_K stays 3, RECOMMEND_FLOOR stays 0.70, MARGIN_THRESHOLD stays 0.15 -- all byte-unchanged."
    - "The connector-registry --check tripwire stays green (the connector frontmatter already carries sub_mode systems-thinking-loop under reach_id context_block; no registry regeneration needed for the reach bank)."
  artifacts:
    - path: "lib/hmi/reach-component-map.json"
      provides: "The systems-thinking-loop sub_mode row in the ranked component repertoire."
      contains: "systems-thinking-loop"
    - path: "tests/test-150.10-systems-thinking-repertoire.cjs"
      provides: "Falsifiable test: systems-thinking-loop resolves via the dispatcher AND the frozen-6 invariant + frozen constants are byte-unchanged."
      contains: "REACH_IDS"
    - path: "tests/run-all-150.10.sh"
      provides: "The one-command phase gate aggregating the repertoire test + the carried frozen-6/148 fences."
      min_lines: 20
  key_links:
    - from: "lib/hmi/reach-component-map.json sub_modes block"
      to: "lib/hmi/selector-dispatcher.cjs resolveArchetype"
      via: "the sub_modes namespace fold (registry-is-the-table)"
      pattern: "systems-thinking-loop"
    - from: "tests/test-150.10-systems-thinking-repertoire.cjs"
      to: "lib/core/sensors/sensor-types.cjs REACH_IDS + lib/hmi/dial-reach-orchestrator.cjs DIAL_REACH_K"
      via: "frozen-6 + frozen-constant assertions"
      pattern: "REACH_IDS|DIAL_REACH_K"
---

<objective>
Register `systems-thinking-loop` as a ranked sub_mode component in `lib/hmi/reach-component-map.json`, alongside the existing 6 sub_mode components (reverse-salient, whitespace, cross-domain-analogy, cross-domain-connect, dominant-design, six-hats), so the intelligence-orchestrator surfaces systems-thinking in the ranked reach set next to the intelligence engines -- making it RANKED/repertoire-visible, not merely flat-connected. This is the navigator directive of 2026-06-14 (CONTEXT "Piece C").

Purpose: Part 7 reuse -- this mirrors the Phase 148 component-registration idiom exactly (a registry add + the dispatcher's resolveArchetype ranking surface), not a new reach mechanism. The connector is ALREADY wired (connector-registry reach_id=context_block, sub_mode=systems-thinking-loop, SENS-06, hierarchy_rank=44); this plan makes that sub_mode a first-class ranked component in the repertoire registry.

HARD CONSTRAINT -- FROZEN-6 INVARIANT: systems-thinking is NOT a 7th reach_id. It rides as a sub_mode COMPONENT under context_block (where it is already connected). The 6 reach_ids (context_block, contradiction, cross_room, brain_consult, deep_research, hats) are a frozen constitutional set (Canon Appendix D entry 15, "NEVER a 7th"). DIAL_REACH_K (6), MAX_K (3), and the 0.70/0.15 gate stay byte-UNCHANGED. The plan's own test asserts REACH_IDS length is still 6 and the frozen bank is unchanged.

Output: one row added to the reach-component-map sub_modes block, a falsifiable repertoire+frozen-6 test, and the phase-gate aggregator. This plan is PURE-LOCAL registry work, fully independent of Brain (Plan 01) and of the command rewrite (Plan 02).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/specs/systems-thinking-f-selector-design.md
@.planning/phases/150.10-systems-thinking-f-selector/150.10-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md

# The repertoire registry this plan edits + the dispatcher that reads it
@lib/hmi/reach-component-map.json
@lib/hmi/selector-dispatcher.cjs

# The frozen-6 idiom to MIRROR (Phase 148 component-map + frozen-contracts tests)
@tests/test-148-component-map.cjs
@tests/test-148-frozen-contracts.cjs

# The frozen banks + constants this plan must NOT move
@lib/core/sensors/sensor-types.cjs
@lib/hmi/dial-reach-orchestrator.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the systems-thinking-loop sub_mode row to reach-component-map.json (repertoire registration)</name>
  <files>lib/hmi/reach-component-map.json</files>
  <action>
  Per CONTEXT Piece C + design spec (Part 7 reuse of the Phase 148 idiom). In the `sub_modes` block of lib/hmi/reach-component-map.json, add one row alongside the existing six:
    "systems-thinking-loop": { "archetype": "multiSelect" }
  Choose `multiSelect` to match the existing intelligence-engine sub_modes (reverse-salient, whitespace, cross-domain-analogy, cross-domain-connect, dominant-design all carry multiSelect) -- systems-thinking-loop is an engine-class move-selector component ranked next to them, so it shares their interaction archetype. Do NOT touch the `reaches` block (the frozen 6). Do NOT add a 7th reach. Do NOT touch `standing_options`. The dispatcher's resolveArchetype() folds the `sub_modes` namespace, so this single row makes systems-thinking-loop repertoire-visible with ZERO dispatcher code change (registry-is-the-table). Keep valid JSON; the archetype must be one of the documented archetype_vocabulary values. No em-dashes in any added comment.
  </action>
  <verify>
  <automated>node -e "const m=require('./lib/hmi/reach-component-map.json'); const d=require('./lib/hmi/selector-dispatcher.cjs'); if(!m.sub_modes['systems-thinking-loop']) throw new Error('row missing'); if(Object.keys(m.reaches).length!==6) throw new Error('reaches not 6'); if(d.resolveArchetype('systems-thinking-loop')==='select') throw new Error('not in repertoire (resolves to default)'); console.log('OK', d.resolveArchetype('systems-thinking-loop'));"</automated>
  </verify>
  <done>systems-thinking-loop is a sub_mode row with a non-default archetype; resolveArchetype('systems-thinking-loop') returns that archetype (not the default 'select'); the reaches block still has exactly 6 entries; JSON is valid.</done>
</task>

<task type="auto">
  <read_first>lib/hmi/dial-reach-orchestrator.cjs (confirm DIAL_REACH_K/RECOMMEND_FLOOR/MARGIN_THRESHOLD export names before asserting them), lib/core/sensors/sensor-types.cjs (REACH_IDS), lib/workflow/f-selector-ranker.cjs (MAX_K)</read_first>
  <name>Task 2: Falsifiable repertoire + frozen-6 invariant test</name>
  <files>tests/test-150.10-systems-thinking-repertoire.cjs</files>
  <behavior>
    - Test 1 (repertoire): dispatcher.resolveArchetype('systems-thinking-loop') returns a valid archetype that is NOT the default-on-miss 'select' (proving it is a registered ranked component, mirroring test-148-component-map.cjs).
    - Test 2 (frozen-6 invariant, the hard one): sensor-types.REACH_IDS has length === 6 AND equals exactly [context_block, contradiction, cross_room, brain_consult, deep_research, hats] in order; the orchestrator SKILL.md reach_ids line is still the frozen 6; systems-thinking-loop is NOT present in REACH_IDS (it is a sub_mode, never a reach).
    - Test 3 (frozen constants): dial-reach-orchestrator.DIAL_REACH_K === 6, ranker.MAX_K === 3, orchestrator.RECOMMEND_FLOOR === 0.70, orchestrator.MARGIN_THRESHOLD === 0.15 -- byte-unchanged (mirrors test-148-frozen-contracts.cjs).
    - Test 4 (no-7th-reach in the registry): reach-component-map.json `reaches` block still has exactly 6 keys and they equal the frozen bank; systems-thinking-loop lives ONLY under `sub_modes`, never under `reaches`.
  </behavior>
  <action>
  Mirror tests/test-148-component-map.cjs (repertoire resolution) + tests/test-148-frozen-contracts.cjs (frozen constants) idiom verbatim in style. Plain CJS node:assert/strict; print a PASS line; exit non-zero on any failure. Require sensor-types.cjs for REACH_IDS, dial-reach-orchestrator.cjs for DIAL_REACH_K/RECOMMEND_FLOOR/MARGIN_THRESHOLD, f-selector-ranker.cjs for MAX_K, selector-dispatcher.cjs for resolveArchetype, and reach-component-map.json directly. Assert the exact frozen reach order. NO em-dashes anywhere in the file (HARD RULE: hyphens only).
  This is the acceptance criterion the CONTEXT/downstream-consumer demands: "assert REACH_IDS length is still 6 and the frozen bank is unchanged."
  </action>
  <verify>
  <automated>node tests/test-150.10-systems-thinking-repertoire.cjs</automated>
  </verify>
  <done>The test passes: systems-thinking-loop resolves as a ranked component; REACH_IDS length === 6 and the bank is byte-unchanged; DIAL_REACH_K=6 / MAX_K=3 / 0.70 / 0.15 unchanged; systems-thinking-loop is never a reach_id.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-gate aggregator run-all-150.10.sh (repertoire test + carried frozen fences + connector --check)</name>
  <files>tests/run-all-150.10.sh</files>
  <action>
  Per the run-all-148.sh / run-all-150.5.sh convention (the single PASS/FAIL phase gate). Create tests/run-all-150.10.sh as a bash runner (set -uo pipefail; no emoji; no em-dashes) that runs each constituent to completion and exits non-zero if any failed, printing a per-suite PASS/FAIL line and a final tally:
    (a) the connector-registry --check tripwire: `node scripts/build-connector-registry.cjs --check` (proves the connector frontmatter -> generated registry stays consistent; the systems-thinking connector already carries sub_mode systems-thinking-loop, so this stays green with no regeneration).
    (b) tests/test-150.10-systems-thinking-repertoire.cjs (the new repertoire + frozen-6 test).
    (c) the CARRIED frozen fences proving Piece C did not regress the constitution: tests/test-148-frozen-contracts.cjs and tests/test-148-component-map.cjs (they must still pass -- DIAL_REACH_K=6, MAX_K=3, the 6-reach bank, the existing sub_mode rows all intact).
    (d) a Part-8 grep sweep over the Phase 150.10 local artifacts (reach-component-map.json + the new test) for forbidden user-content-to-Brain tokens -- this is a pure-local registry change, so the sweep asserts zero network/egress surface was added.
  The runner MUST run to completion (no crash) even when a suite fails.
  </action>
  <verify>
  <automated>bash tests/run-all-150.10.sh</automated>
  </verify>
  <done>run-all-150.10.sh runs the repertoire test + carried 148 frozen fences + connector --check + the Part-8 sweep, prints per-suite PASS/FAIL + a tally, and exits 0 with all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reach-component-map.json -> the frozen reach bank | A constitutional surface: adding a sub_mode must NOT move the 6-reach bank. |
| repertoire registration -> egress | Pure-local registry edit; must add zero network/Brain surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.10-08 | Tampering | frozen-6 constitutional regression (a 7th reach minted) | mitigate | Task 2 asserts REACH_IDS length === 6 and the bank is byte-unchanged; Task 1 touches ONLY the sub_modes block; Task 3 carries the 148 frozen-contracts fence. A 7th reach (or a moved DIAL_REACH_K/MAX_K/gate) fails the gate. This is the constitutional-regression risk the downstream consumer named. |
| T-150.10-09 | Tampering | connector-registry drift | mitigate | Task 3 runs the --check tripwire; the systems-thinking connector frontmatter is unchanged, so the generated registry stays consistent. |
| T-150.10-10 | Information Disclosure | egress added by the registry change | accept | Pure-local JSON + test edits; no Brain/web/network surface added. Task 3's Part-8 sweep asserts this. |
| T-150.10-SC | Tampering | npm/pip/cargo installs | accept | No package installs (JSON + CJS test + bash runner only). |
</threat_model>

<verification>
- resolveArchetype('systems-thinking-loop') returns a non-default archetype (it is in the ranked repertoire).
- REACH_IDS length === 6; the bank equals [context_block, contradiction, cross_room, brain_consult, deep_research, hats]; systems-thinking-loop is NOT a reach_id.
- DIAL_REACH_K === 6, MAX_K === 3, RECOMMEND_FLOOR === 0.70, MARGIN_THRESHOLD === 0.15 -- all unchanged.
- The reaches block has exactly 6 keys; systems-thinking-loop lives only under sub_modes.
- connector-registry --check is green; carried 148 frozen fences pass.
- No em-dashes; no egress surface added.
</verification>

<success_criteria>
systems-thinking-loop is a first-class ranked sub_mode component in the repertoire (resolvable via the dispatcher), the frozen-6 constitutional invariant is provably intact (REACH_IDS still 6, frozen constants unchanged, no 7th reach), and the phase gate is one green command.
</success_criteria>

<output>
Create `.planning/phases/150.10-systems-thinking-f-selector/150.10-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `tests/test-150.10-systems-thinking-repertoire.cjs` (new file) - falsifiable test: systems-thinking-loop resolves via the dispatcher AND the frozen-6 invariant holds (REACH_IDS length still 6, frozen constants byte-unchanged, no 7th reach).
- `tests/run-all-150.10.sh` (new file) - one-command phase gate aggregating the repertoire test + the carried frozen-6/148 fences.
- `lib/hmi/reach-component-map.json` (modified) - new `systems-thinking-loop` sub_mode row in the ranked component repertoire (a sub_mode key, NOT a new reach_id).

## Reach-chaining note (navigator directive 2026-06-14)

systems-thinking-loop and reverse-salient are SIBLINGS: both ride reach_id context_block in the component repertoire being extended here. They are mutually invocable (leverage points analyzed as reverse salients and vice versa). This registration does NOT change that wiring; the chaining lives in the Brain (M4 <-> Reverse Salient Concept CROSS_DOMAIN_ANALOGUE) + the Piece D scanner (reads rs-engine REVERSE_SALIENT edges). Recorded here so the repertoire registration is read alongside the chain.
