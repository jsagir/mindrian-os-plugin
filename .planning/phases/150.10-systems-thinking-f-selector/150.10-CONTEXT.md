---
phase: 150.10
slug: systems-thinking-f-selector
title: Systems Thinking F-selector + additive Session 2 Brain ingestion
status: scaffolded
canon_parts: [Part 1, Part 2, Part 3, Part 4, Part 5, Part 7, Part 8, Part 9]
depends_on: [150, 143, 144, 148, 109]
design_spec: .planning/specs/systems-thinking-f-selector-design.md
created: 2026-06-13
created_by: navigator (Jonathan) + Larry
---

# Phase 150.10 Context

## What this phase delivers

Two load-bearing pieces, one phase:

- **Piece A (LOAD-BEARING): Additive Brain ingestion of IRIS Session 2.** Ingest Session 2's generic PWS methodology into the CURRENT Brain graph. Invariant: **ADDITIVE + DEDUPED + CONNECTED, ZERO orphans.** Merge into existing nodes (causal loops / leverage points / wicked problems / systems thinking / Meadows already exist); add genuinely-new (Cynefin, Futures Wheel, S-curve/dominant design, Mullins, trending-to-absurd, examples) each wired on creation; one "IRIS 2026 Session 2" Lecture anchor; orphan-scan acceptance gate. Also seeds the M1-M5 move-nodes with PREREQUISITE/FEEDS_INTO edges (the graph-native flow Piece B reads).

- **Piece B: Promote /mos:systems-thinking to an F-surface human-in-the-loop move-selector.** Five ranked moves (M1 boundary -> M2 causal loop -> M3 archetype -> M4 leverage point -> M5 stage-aware next action) + Brain cross-chains. GUIDED default. Three-layer navigation (Brain flow + local graph state + memory trajectory), ranked locally. Promote-in-place; connector already wired (reach_id context_block, SENS-06, F.2).

## Locked decisions (from design spec Section 11)

- Build shape: promote existing command in place (no new command).
- Default posture: GUIDED.
- Flow source: Brain PREREQUISITE/FEEDS_INTO edges, graph-native.
- **Systems thinking is a META-LENS**: any stage, any problem type, any case -- NOT discovery-only (navigator correction 2026-06-13).
- Filing is STAGE-AWARE: files to the active room section, not a fixed default.
- Piece A (Brain ingestion) is additive/deduped/connected with a zero-orphan acceptance gate.
- License: source from Session 2 transcript (own IP); CC BY-SA third-party skill sidestepped.

## Navigator addition (2026-06-14): systems-thinking joins the reach REPERTOIRE

Navigator directive: the systems-thinking move-selector must live in Larry's reach repertoire as a first-class RANKED component, alongside the intelligence-orchestrator's engine set and the other reach factors -- not merely connected as a flat command.

- **Piece C (NEW, load-bearing for the directive): register `systems-thinking-loop` as a ranked sub_mode component in `lib/hmi/reach-component-map.json`**, alongside the existing ranked components (reverse-salient, whitespace, cross-domain-analogy, cross-domain-connect, dominant-design, six-hats -- the Phase 148 set). The intelligence-orchestrator (skills/intelligence-orchestrator/SKILL.md) reads the registry and surfaces it in the ranked reach set, so when Larry reaches, systems-thinking appears as a selectable move ranked next to the intelligence engines.
- **FROZEN-6 INVARIANT (hard constraint):** systems-thinking is NOT a 7th reach_id. The 6 reach_ids (context_block, contradiction, cross_room, brain_consult, deep_research, hats) are a frozen constitutional set (Canon Appendix D entry 15, "NEVER a 7th"). systems-thinking-loop rides as a `sub_mode` COMPONENT under `context_block` (where it is already connected: connector-registry reach_id=context_block, sub_mode=systems-thinking-loop, SENS-06, hierarchy_rank=44). DIAL_REACH_K, MAX_K=3, and the 0.70/0.15 gate stay UNCHANGED.
- **Reuse (Part 7):** this is a registry add + orchestrator ranking surface, mirroring the Phase 148 component-registration idiom -- not a new reach mechanism. The connector is already wired; this makes it RANKED/repertoire-visible.

## Hard dependency / blocker

Piece A requires a write-capable `brain_write` admin key live in the build session. If absent: Piece B falls back to the hardcoded ranking heuristic and Piece A (ingestion + flow seeding) becomes a follow-up.

## Carried defaults (overrule at plan time)

- M5 names the next-action target + OFFERS (does not auto-jump to) the stage-appropriate handoff.
- Trending-to-absurd / S-curve stays a cross-chain (no 6th core move).

## Canon obligations

- Part 8: only generic methodology crosses to Brain. Session 2 PWS frameworks are generic; zero venture/room/personal content ingested. The orphan-scan and dedup run on generic-methodology nodes only.
- Part 9: local graph (navigation.cjs) + room.db memory are the LOCAL mind; Brain is the external cortex supplying flow. Ranking computed locally.
- Part 3 / Part 4: the selector is a Shape F gate; every move selection files a typed edge.
- Part 7: reuse -- promote the existing command, do not build a new one.

## Next step

Run `/gsd-plan-phase 150.10` to break this into plans (expect a wave split: Piece A ingestion + flow-seed, then Piece B selector build + verification).
