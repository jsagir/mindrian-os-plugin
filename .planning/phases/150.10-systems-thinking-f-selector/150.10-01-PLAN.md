---
phase: 150.10
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
canon_parts: [Part 7, Part 8, Part 9]
requirements: [ST-01, ST-02, ST-03, ST-04, ST-05]
files_modified:
  - .planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md
user_setup:
  - service: brain-mcp
    why: "Piece A writes generic PWS methodology nodes into the live Brain graph via the brain_write MCP tool. This is a write-capable admin key; the read-only Brain key cannot create nodes."
    env_vars:
      - name: MINDRIAN_BRAIN_KEY
        source: "A write-capable brain_write admin key, set in MINDRIAN_BRAIN_KEY or ~/.mindrian.env (resolved by lib/core/resolve-brain-key.cjs). The standard read-only install key is INSUFFICIENT."

must_haves:
  truths:
    - "Every Session 2 concept already in Brain (causal loops, leverage points, Meadows, wicked problems, systems thinking, nested hierarchies) is MERGED, never duplicated."
    - "Each genuinely-new framework (Cynefin, Futures Wheel, S-curve/dominant design, Mullins 7-domains, trending-to-absurd, Rittel-Webber) is created WITH at least one edge in the same write."
    - "Each example node (Nautilus, Ely 1910, Benz 1885, fishery CLD, breakfast loop) has an ILLUSTRATES/HAS_EXAMPLE edge to its framework."
    - "One 'IRIS 2026 Session 2' Lecture node exists; every framework taught in it carries TEACHES/MENTIONED_IN."
    - "The five move-nodes M1-M5 exist with PREREQUISITE and FEEDS_INTO edges (the graph-native flow Piece B traverses)."
    - "The orphan-scan returns 0: no node tagged source_doc='iris-2026-session-2' lacks an edge."
    - "Only generic methodology crossed to Brain; zero venture/room/personal content (Canon Part 8)."
  artifacts:
    - path: ".planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md"
      provides: "The audit trail: dedup decisions, new-node list, example wiring, move-node edges, and the verbatim orphan-scan result (0)."
      min_lines: 40
  key_links:
    - from: "M1-M5 move nodes"
      to: "Systems Thinking / Causal Loop Diagrams anchor"
      via: "PREREQUISITE + FEEDS_INTO edges"
      pattern: "PREREQUISITE|FEEDS_INTO"
    - from: "every source_doc='iris-2026-session-2' node"
      to: "at least one other node"
      via: "any edge (orphan backstop = the Lecture node)"
      pattern: "MENTIONED_IN|TEACHES|ILLUSTRATES|HAS_EXAMPLE|PREREQUISITE|FEEDS_INTO|RELATED_TO|AUTHORED_BY|PART_OF"
---

<objective>
Ingest IRIS 2026 Session 2's generic PWS methodology into the LIVE Brain graph, ADDITIVELY: dedup-first (merge what exists, never duplicate), add the genuinely-new each wired on creation, anchor everything to one "IRIS 2026 Session 2" Lecture node, and seed the M1-M5 move-nodes with PREREQUISITE/FEEDS_INTO edges. The governing invariant is ADDITIVE + DEDUPED + CONNECTED, ZERO ORPHANS, proven by an orphan-scan acceptance gate that must return 0.

Purpose: this is LOAD-BEARING. The M1-M5 PREREQUISITE/FEEDS_INTO edges seeded here ARE the graph-native intra-move flow that Piece B (Plan 02) traverses (design spec Section 6, Layer 1). Without these edges, Piece B falls back to the hardcoded heuristic.

This plan is a CHECKPOINT (autonomous: false). Piece A REQUIRES a write-capable brain_write admin key live in the session. Task 1 verifies the key is present and write-capable BEFORE any write. If it is absent, the executor STOPS at the checkpoint and asks the navigator -- it never proceeds blind and never fails mid-write. When deferred, Plan 02 (Piece B) still ships on its heuristic fallback and Plan 03 (Piece C) is fully independent.

Output: the Session 2 methodology live in Brain (deduped, connected), and INGESTION-LOG.md recording every dedup decision, every new node + its edges, and the verbatim orphan-scan result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/specs/systems-thinking-f-selector-design.md
@.planning/phases/150.10-systems-thinking-f-selector/150.10-CONTEXT.md
@CLAUDE.md
@docs/MINDRIAN-CANON.md

# The source material (own IP; CC BY-SA sidestepped per design spec Section 8)
# ~/MindrianRooms/iris2026/sessions/session-2/transcript.md  (read for concept/example extraction)

# How the write key is resolved on this machine
@lib/core/resolve-brain-key.cjs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify a write-capable brain_write admin key is live (HARD BLOCKER gate)</name>
  <what-built>
  Before any Brain write, prove the session can write. This is the brain_write
  blocker the CONTEXT.md and design spec Section 9 step 7 flag as the hard
  dependency. Steps the executor runs FIRST:
    1. Run `node lib/core/resolve-brain-key.cjs` (or require resolveBrainKey())
       to confirm a key resolves (available=true, reason=null).
    2. Probe write capability via the live Brain MCP: attempt a SAFE no-op write
       through the `brain_write` MCP tool (e.g. MERGE a sentinel node
       `(:_IngestionProbe {phase:'150.10'})` then DETACH DELETE it), OR call
       `brain_stats` / `brain_schema` to confirm the connected key's role allows
       writes. A read-only key will reject the write -- that rejection is the
       signal to STOP.
  </what-built>
  <how-to-verify>
  1. The resolver reports available=true.
  2. The probe write succeeded and the sentinel was cleaned up (or the role
     check confirms write capability).
  IF the key is ABSENT or READ-ONLY: STOP here. Report to the navigator:
  "Piece A (Brain ingestion) requires a write-capable brain_write admin key.
  The current key is {absent|read-only}. Defer Piece A as a follow-up; Plan 02
  (Piece B) ships on its heuristic fallback and Plan 03 (Piece C) is independent.
  Proceed with 02 + 03, or supply a write key to run Piece A now?"
  Do NOT proceed to Task 2 without an approved, write-capable key.
  </how-to-verify>
  <resume-signal>Type "write-key confirmed" to proceed, or "defer Piece A" to skip to Plans 02/03.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Dedup-first pass, then additive ingest of Session 2 (every new node wired on creation)</name>
  <files>.planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md</files>
  <action>
  Per D-30/design-spec-Section-9 ADDITIVE+DEDUPED+CONNECTED invariant. Source ONLY from the IRIS Session 2 transcript (own IP; no third-party prose copied, CC BY-SA sidestepped). Egress GENERIC methodology only (Canon Part 8): framework names, author names, concept labels, example names, move definitions. NEVER any venture/room/personal/navigator content. If the transcript contains any specific person's venture detail, it does NOT cross.

  Step A -- DEDUP PASS FIRST. For each Session 2 concept, query the live graph (brain_search / brain_query) before writing. Brain ALREADY holds (per design spec Section 9 step 1): causal loops, leverage points (Meadows), wicked problems, systems thinking, nested hierarchies, Meadows as author. For each: MERGE onto the existing node (MERGE by canonical name), never CREATE a second node. Record each dedup decision (concept -> matched existing node | new) in INGESTION-LOG.md.

  Step B -- ADD THE GENUINELY-NEW, each wired on creation. Weakly/not covered (design spec Section 9 step 2): Cynefin (Snowden), Futures Wheel (Glenn), S-curve + dominant design (Anderson-Tushman), Mullins 7-domains, trending-to-absurd, Rittel-Webber as named authors. Create each Framework/Method/Author/Book node WITH its edges in the SAME write (RELATED_TO / AUTHORED_BY / PART_OF the Systems Thinking anchor or the Session 2 Lecture node). No bare node lands without an edge. Tag every created node `source_doc:'iris-2026-session-2'`.

  Step C -- EXAMPLES (highest-value, lowest-duplication). Create Example/CaseStudy nodes for: USS Nautilus, Ely's 1910 carrier takeoff, Benz 1885, the fishery CLD, the breakfast/frustration loop. Wire each via ILLUSTRATES / HAS_EXAMPLE to its framework in the same write. An example with no ILLUSTRATES edge is an orphan -> forbidden.

  Step D -- THE LECTURE ANCHOR. Create ONE Lecture node "IRIS 2026 Session 2" (source_doc:'iris-2026-session-2'). For every framework taught in it, add TEACHES / MENTIONED_IN. This makes the session navigable AND is the orphan backstop: every ingested node gets at least the lecture edge.

  Record in INGESTION-LOG.md: the dedup table (concept | matched-existing | merged/created), the new-node list with their edges, the example wiring, and the framework->Lecture edges. Use hyphens only (HARD RULE: no em-dashes).
  </action>
  <verify>
  <automated>MISSING -- live-Brain ingest is verified by Task 4's orphan-scan against the live graph (no local test harness writes to production Brain). INGESTION-LOG.md presence + dedup-table + zero-"v1"/"placeholder"/"static" scope-reduction language is the local gate: grep -vE '^#' .planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md | grep -ciE 'placeholder|simplified|hardcoded for now|static for now' must be 0.</automated>
  </verify>
  <done>Every existing concept merged (not duplicated); every new framework + example + the Lecture node created WITH edges; INGESTION-LOG.md records the dedup table and new-node/edge list; no scope-reduction language present.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the M1-M5 move-nodes with PREREQUISITE + FEEDS_INTO edges (the graph-native flow)</name>
  <files>.planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md</files>
  <action>
  Per design spec Section 9 step 5 and Section 6 Layer 1. Create five Method/ProcessStep nodes, one per move, tagged source_doc:'iris-2026-session-2':
    M1 st-boundary (Draw/challenge the boundary)
    M2 st-loop (Build the causal loop)
    M3 st-archetype (Name the archetype)
    M4 st-leverage (Locate the leverage point)
    M5 st-act (Route to next action, stage-aware)
  Wire the flow as edges (NOT hardcoded order): PREREQUISITE chain M1->M2->M3, M2->M4 (M3 and M4 both reachable once the loop exists per the heuristic in design spec Section 5), and FEEDS_INTO M4->M5 (leverage feeds the payoff move). Link each move node to the deduped Systems Thinking / Causal Loop Diagrams anchor (PART_OF / RELATED_TO) so no move node is an orphan. These PREREQUISITE/FEEDS_INTO edges are exactly what Plan 02's selector traverses to drive intra-move ranking; the edge directions here ARE the contract Piece B reads.
  Record the five move nodes and every edge (source -> type -> target) in INGESTION-LOG.md.
  </action>
  <verify>
  <automated>MISSING -- live-Brain edges verified by Task 4 orphan-scan + a flow-traversal spot check (Task 4). Local gate: grep -c 'PREREQUISITE' .planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md returns >= 1 AND grep -c 'FEEDS_INTO' returns >= 1 (the move-edge ledger is recorded).</automated>
  </verify>
  <done>M1-M5 exist as nodes; PREREQUISITE chain (M1->M2->M3, M2->M4) and FEEDS_INTO (M4->M5) edges written and linked to the Systems Thinking anchor; the move-edge ledger is in INGESTION-LOG.md.</done>
</task>

<task type="auto">
  <name>Task 4: Orphan-scan acceptance gate + flow-traversal spot check</name>
  <files>.planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md</files>
  <action>
  Run the orphan-scan against the LIVE graph via brain_query (design spec Section 9 step 6, the acceptance gate):
    MATCH (n) WHERE n.source_doc = 'iris-2026-session-2' AND NOT (n)--() RETURN count(n)
  The result MUST be 0. A non-zero result means at least one ingested node landed without an edge -> the phase fails; go back and wire the orphan(s), then re-scan. Paste the verbatim query and its 0-result into INGESTION-LOG.md as the acceptance receipt.
  Then run a flow-traversal spot check proving Piece B can read the seeded flow:
    MATCH (a:Method {source_doc:'iris-2026-session-2'})-[r:PREREQUISITE|FEEDS_INTO]->(b) RETURN a.name, type(r), b.name
  Confirm M1->M2->M3, M2->M4, M4->M5 appear. Record the traversal result in INGESTION-LOG.md.
  Final Part 8 self-audit: scan INGESTION-LOG.md and the egress you sent for any user-specific strings (venture names, navigator identifiers, room artifact bodies). There must be ZERO. Record "Part 8: zero user-content egress" with the basis.
  </action>
  <verify>
  <automated>grep -c 'count(n)' .planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md returns >= 1 AND the recorded orphan-scan result is 0 (grep -A2 'AND NOT (n)--()' INGESTION-LOG.md shows a 0); the flow-traversal block records M4->M5 FEEDS_INTO.</automated>
  </verify>
  <done>Orphan-scan returns 0 and the verbatim receipt is logged; the flow-traversal spot check shows the M1-M5 edges; Part 8 zero-egress self-audit recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local executor -> Brain (brain_write) | Generic methodology egress crossing to the shared external cortex. The ONLY place user content could leak. |
| read-only key -> write attempt | A wrong/insufficient key attempting a constitutional write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.10-01 | Information Disclosure | brain_write egress (Task 2/3) | mitigate | Canon Part 8 floor: egress GENERIC methodology only (framework/author/concept/example/move labels). Task 4 final self-audit scans for any user-specific string; zero tolerance. Source is the transcript's GENERIC teaching content only -- any venture/personal detail in the transcript does NOT cross. |
| T-150.10-02 | Tampering | duplicate-node creation | mitigate | Dedup-first pass (Task 2 Step A) MERGEs by canonical name before any CREATE; INGESTION-LOG records each dedup decision. No second "Causal Loop" node. |
| T-150.10-03 | Tampering | orphan node (CONNECTED invariant breach) | mitigate | Every new node wired in the same write; the Lecture node is the orphan backstop; Task 4 orphan-scan gate must return 0 or the phase fails. |
| T-150.10-04 | Elevation of Privilege | read-only key writing | mitigate | Task 1 blocking-human checkpoint probes write capability BEFORE any write; a read-only key STOPS the plan rather than half-writing. |
| T-150.10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure Brain MCP calls + one markdown log). |
</threat_model>

<verification>
- Task 1 checkpoint passed with a confirmed write-capable key (or the plan was deferred cleanly).
- Orphan-scan `MATCH (n) WHERE n.source_doc='iris-2026-session-2' AND NOT (n)--() RETURN count(n)` returns 0.
- Flow-traversal shows M1->M2->M3 PREREQUISITE + M2->M4 PREREQUISITE + M4->M5 FEEDS_INTO.
- No duplicate of any pre-existing framework/concept node (dedup table proves merges).
- INGESTION-LOG.md records the dedup table, new-node/edge ledger, the verbatim orphan-scan 0-result, and the Part 8 zero-egress self-audit.
</verification>

<success_criteria>
Session 2 methodology is live in Brain, deduped and fully connected (zero orphans), with the M1-M5 flow edges seeded for Piece B to traverse, and only generic methodology crossed the boundary. OR: the plan deferred cleanly at the Task 1 checkpoint with the navigator informed, leaving Plans 02/03 to proceed.
</success_criteria>

<output>
Create `.planning/phases/150.10-systems-thinking-f-selector/150.10-01-SUMMARY.md` when done. If deferred at Task 1, the SUMMARY records the deferral reason (no write key) and confirms Plans 02/03 proceed.
</output>

## Artifacts this phase produces

- `.planning/phases/150.10-systems-thinking-f-selector/INGESTION-LOG.md` (new file) - audit trail: dedup decisions, new-node list, example wiring, move-node edges, verbatim orphan-scan result (0).
- Brain graph nodes (generic PWS methodology ONLY, Part 8 - zero venture/room/personal content): one `Lecture` node "IRIS 2026 Session 2"; new `Framework`/`Method`/`Author`/`Book` nodes (Cynefin, Futures Wheel, S-curve/dominant design, Mullins 7-domains, trending-to-absurd, Rittel-Webber) each wired on creation; `Example`/`CaseStudy` nodes (USS Nautilus, Ely 1910, Benz 1885, fishery CLD, breakfast loop) via HAS_EXAMPLE/ILLUSTRATES; the M1-M5 move-nodes (`Method`/`ProcessStep`) with PREREQUISITE/FEEDS_INTO edges.
- No new repo code symbols (writes Brain-graph nodes + one local log file only).
