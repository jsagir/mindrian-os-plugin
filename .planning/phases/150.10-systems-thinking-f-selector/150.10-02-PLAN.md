---
phase: 150.10
plan: 02
type: execute
wave: 2
depends_on: []
autonomous: true
canon_parts: [Part 1, Part 3, Part 4, Part 7, Part 8, Part 9]
requirements: [ST-06, ST-07, ST-08, ST-09]
files_modified:
  - commands/systems-thinking.md
  - references/methodology/systems-thinking.md
  - references/methodology/causal-loop-diagrams.md

must_haves:
  truths:
    - "Invoking /mos:systems-thinking offers a ranked move set (M1-M5), not a fixed phase-walk march."
    - "The selector defaults to GUIDED ('let me think'): it offers the next single ranked move, with 'just tell me' (AUTONOMOUS) and 'stop' dial overrides."
    - "Each move (M1-M5) is a selectable state with a sub_mode (st-boundary/st-loop/st-archetype/st-leverage/st-act) producing an actionable handle, not a diagram."
    - "Ranking is computed LOCALLY by blending Brain flow (Layer 1) + local graph state (Layer 2, navigation.cjs) + memory trajectory (Layer 3, memory-ops.cjs)."
    - "Filing is STAGE-AWARE: the artifact files to the ACTIVE room section, never a fixed default. Systems thinking is a META-LENS usable at any stage."
    - "The command is promoted IN PLACE: no new command, connector frontmatter unchanged (reach_id context_block, sub_mode systems-thinking-loop, SENS-06, F.2)."
  artifacts:
    - path: "commands/systems-thinking.md"
      provides: "The selector-loop command (Sense->Rank->Offer->Pick->Execute->Re-rank), GUIDED default, dial overrides, stage-aware filing."
      contains: "Selector"
    - path: "references/methodology/systems-thinking.md"
      provides: "The 5-move definitions, the CLD storytelling method, the leverage-point->validation handoff, Session 2 examples (fishery, breakfast loop)."
      min_lines: 60
    - path: "references/methodology/causal-loop-diagrams.md"
      provides: "Dedicated CLD-building reference for M2 (reinforcing/balancing loops, signed links, story-first construction)."
      min_lines: 30
  key_links:
    - from: "commands/systems-thinking.md selector ranking"
      to: "Brain PREREQUISITE/FEEDS_INTO flow + navigation.cjs local state + memory-ops.cjs trajectory"
      via: "three-layer local blend (Section 6 contract)"
      pattern: "navigation\\.cjs|memory-ops|PREREQUISITE|Layer"
    - from: "commands/systems-thinking.md frontmatter"
      to: "the SENS-06 reach"
      via: "unchanged connector block (reach_id context_block, sub_mode systems-thinking-loop)"
      pattern: "systems-thinking-loop"
---

<objective>
Promote /mos:systems-thinking from a fixed phase-walk into an F-surface human-in-the-loop MOVE-SELECTOR (design spec Sections 1, 4, 5, 6, 7). Every turn the navigator sits at a dial and is offered RANKED moves; they pick the lens, the tool executes it, then it re-ranks. Five within-systems moves (M1 boundary -> M2 causal loop -> M3 archetype -> M4 leverage point -> M5 stage-aware next action) plus the Brain cross-chains (Root Cause 0.85, Scenario 0.80, Six Hats 0.70). GUIDED default. Three-layer navigation (Brain flow + local graph state + memory trajectory), ranked LOCALLY. Stage-aware filing. Systems thinking is a META-LENS: any stage, any problem type, any case -- NOT discovery-only.

Purpose: Part 7 reuse -- promote the EXISTING command in place, no new command. The connector is already wired (SENS-06, reach_id context_block, sub_mode systems-thinking-loop). This makes it a selector, not a march.

Output: the rewritten command + the move-definition references. This plan is INDEPENDENT of Plan 01: if Piece A deferred (no write key), the selector falls back to the hardcoded ranking heuristic (design spec Section 5) instead of traversing the Brain PREREQUISITE/FEEDS_INTO edges. The command MUST document both paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/specs/systems-thinking-f-selector-design.md
@.planning/phases/150.10-systems-thinking-f-selector/150.10-CONTEXT.md
@CLAUDE.md
@commands/systems-thinking.md
@references/methodology/systems-thinking.md

# The local-mind chokepoint the selector reads (Layer 2 + Layer 3)
@lib/core/navigation.cjs

# How the dial / Shape F selector renders (the F.1 gate the selector surfaces moves through)
@skills/intelligence-orchestrator/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite commands/systems-thinking.md into the selector loop (GUIDED default, dial overrides, three-layer blend)</name>
  <files>commands/systems-thinking.md</files>
  <action>
  Per design spec Sections 4-7 and CONTEXT locked decisions. Promote IN PLACE (Part 7): keep the entire `connector:` frontmatter block byte-unchanged (reach_id context_block, sub_mode systems-thinking-loop, sensor_triggers [SENS-06], framework "Systems Thinking", posture hold, hierarchy_rank 44, filing fileEvidenceWithReadback, surface F.2). Do NOT mint a new command, do NOT add a 6th reach. Extend the frontmatter `sub_mode` NOTES (prose, not a new reach) to enumerate the per-move sub-modes: st-boundary, st-loop, st-archetype, st-leverage, st-act.

  Replace the "## Session Flow" section (the fixed "Quick pass or deep dive?" march) with the SELECTOR LOOP (design spec Section 5):
    1. SENSE -- read all three layers: Brain flow (Layer 1), local graph state via navigation.cjs (Layer 2), trajectory via memory-ops.cjs (Layer 3), plus the SENS-06 signal.
    2. RANK -- compute rank(move) LOCALLY (design spec Section 6 formula: brain_prior - already_done_penalty + unfinished_thread_boost + contradiction_flag). Document the heuristic skeleton verbatim from Section 5: no boundary -> M1 first; boundary but no loop -> M2; loop but no archetype/leverage -> M3 then M4; leverage located -> M5 first (the payoff), chains second; prior-session leverage with no validation -> M5 first on return; a local contradiction -> flag in the intelligence strip, do not auto-advance; chains capped at top 1-2 by Brain confidence.
    3. OFFER -- render the F.1 gate: top move highlighted, 2-3 alternatives, plus the dial.
    4. PICK -- navigator chooses one move (or a dial override).
    5. EXECUTE -- run that move's sub_mode, one question/observation per turn (lens-at-a-time discipline; never dump all five moves as prose).
    6. RE-RANK -- back to step 1.

  Dial overrides (design spec Section 5): "just tell me" -> AUTONOMOUS (run remaining moves to leverage point + validation target); "let me think" -> stay GUIDED; "stop" -> halt, return control, offer to file. GUIDED is the DEFAULT.

  FLOW-SOURCE DUAL PATH (load-bearing): the intra-move ranking reads the Brain PREREQUISITE/FEEDS_INTO edges (Layer 1) WHEN Brain is reachable and the Plan-01 seed landed. WHEN Brain is unreachable OR the seed was deferred, FALL BACK to the hardcoded heuristic skeleton above (design spec Section 5 / dependency note in Section 9). Document both paths explicitly so the command works regardless of Plan 01's outcome.

  Terminal condition: M5 produces a stage-appropriate next-action target -> propose filing (read-back before write, per filing: fileEvidenceWithReadback).

  STAGE-AWARE FILING (CONTEXT locked decision; design spec Section 2 + Q4): the artifact files to the ACTIVE room section (the section the navigator invoked from / the current venture stage), NOT a fixed default and NOT always problem-definition. M5's next-action target is stage-shaped: discovery -> validate variable + who to talk to; design -> intervention to prototype; investment -> loop the thesis rests on; operations -> leverage point to instrument. State that systems thinking is a META-LENS: any stage, any problem type, any case, never discovery-only.

  Render contract (design spec Section 7): glyphs only from the allowed set (right-triangle-filled active, right-triangle-hollow available, tree branches, arrow chain). No emoji. Action footer always carries the dial line. NO em-dashes anywhere (HARD RULE: hyphens only).
  </action>
  <verify>
  <automated>grep -qi 'selector' commands/systems-thinking.md && grep -q 'st-boundary' commands/systems-thinking.md && grep -q 'st-leverage' commands/systems-thinking.md && grep -qi 'let me think' commands/systems-thinking.md && grep -q 'systems-thinking-loop' commands/systems-thinking.md && ! grep -q '\xe2\x80\x94' commands/systems-thinking.md && echo OK</automated>
  </verify>
  <done>The command renders a ranked move selector (M1-M5) with GUIDED default + dial overrides + the three-layer blend + the Brain-flow/heuristic dual path + stage-aware filing; connector frontmatter unchanged; no new command; no em-dashes. CONFIRMED: the navigation.cjs graph-read function and the memory-ops.cjs session-trajectory function referenced by the selector prose exist by the exact names used (grep their exports before writing prose that depends on them).</done>
</task>

<task type="auto">
  <name>Task 2: Extend references/methodology/systems-thinking.md with the 5-move definitions + leverage->validation handoff + Session 2 examples</name>
  <files>references/methodology/systems-thinking.md</files>
  <action>
  Per design spec Section 9 (Piece B step 6). Add a "## The Five Moves" section defining M1-M5 (design spec Section 4 table): for each move give its trigger, what it produces, the PWS anchor, the sub_mode, and the Larry voice line verbatim from the spec (e.g. M2: "Every causal map has two kinds of loops and two only."). Add the CLD storytelling method (build a loop as a STORY -- fishery stock; breakfast/frustration loop -- not just read one). Add the leverage-point -> validation handoff: the PWS discipline that a generic tool ends with a pretty diagram but the PWS selector ends with an ACTIONABLE HANDLE (a leverage-point hypothesis + a stage-appropriate next-action target). Preserve the transcript anchors verbatim in voice (design spec Section 2): "There is no solving the systems problem. None..."; "Reinforcing is not positive or negative..."; "Wherever those leverage points are, are opportunities to make the problem better." Fold in the Session 2 examples (fishery stock CLD, the breakfast loop). State the meta-lens framing (any stage, not discovery-only). No em-dashes.
  </action>
  <verify>
  <automated>grep -qi 'Five Moves' references/methodology/systems-thinking.md && grep -q 'st-archetype' references/methodology/systems-thinking.md && grep -qi 'leverage' references/methodology/systems-thinking.md && grep -qi 'breakfast' references/methodology/systems-thinking.md && ! grep -q '\xe2\x80\x94' references/methodology/systems-thinking.md && echo OK</automated>
  </verify>
  <done>The reference defines M1-M5 with voice lines, the CLD storytelling method, the leverage->validation handoff, the meta-lens framing, and the Session 2 examples; no em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Create references/methodology/causal-loop-diagrams.md (dedicated M2/CLD depth)</name>
  <files>references/methodology/causal-loop-diagrams.md</files>
  <action>
  Per design spec Section 9 (Piece B step 7, the optional dedicated CLD reference -- warranted because M2 is the spine move). Create the file: reinforcing vs balancing loops (the "two kinds and two only" rule), signed links, delays, and the STORY-FIRST construction method (build the loop by telling the story of the system, e.g. the fishery stock and the breakfast/frustration loop, then read the loops off the story). Anchor every concept in the navigator's specific problem (never abstract systems theory). Keep the Larry voice. Add the ROOM.md-pattern note that the produced CLD is an actionable handle feeding M3/M4, not a terminal diagram. No em-dashes (hyphens only).
  </action>
  <verify>
  <automated>test -f references/methodology/causal-loop-diagrams.md && grep -qi 'reinforcing' references/methodology/causal-loop-diagrams.md && grep -qi 'balancing' references/methodology/causal-loop-diagrams.md && grep -qi 'story' references/methodology/causal-loop-diagrams.md && ! grep -q '\xe2\x80\x94' references/methodology/causal-loop-diagrams.md && echo OK</automated>
  </verify>
  <done>The CLD reference exists with reinforcing/balancing loops, signed links, the story-first construction method, and the actionable-handle framing; no em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selector ranking -> Brain (Layer 1 flow) | The selector reads generic PREREQUISITE/FEEDS_INTO flow; egress must stay generic. |
| selector -> local graph (navigation.cjs) | Layers 2/3 are LOCAL-only reads; never egress to Brain. |
| connector frontmatter | A frozen-constitutional surface (SENS-06 reach, no 7th reach). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150.10-05 | Information Disclosure | Layer-1 Brain flow read | mitigate | The selector reads GENERIC flow edges only (PREREQUISITE/FEEDS_INTO on methodology nodes); room state (Layers 2/3) is read LOCALLY via navigation.cjs / memory-ops.cjs and NEVER crosses to Brain (Canon Part 8; design spec Section 6 "the blend happens locally"). Ranking is computed locally. |
| T-150.10-06 | Tampering | connector frontmatter drift | mitigate | The connector block stays byte-unchanged (no 6th reach minted, reach_id stays context_block, sub_mode stays systems-thinking-loop). Promote-in-place (Part 7). Plan 03 owns the repertoire registration; this plan does not touch reach banks. |
| T-150.10-07 | Denial of Service | Brain-unreachable / Plan-01-deferred | accept | The selector degrades to the hardcoded heuristic (design spec Section 5) when the Brain flow is unavailable; the command documents both paths so it never hard-fails. |
| T-150.10-SC | Tampering | npm/pip/cargo installs | accept | No package installs (markdown edits only). |
</threat_model>

<verification>
- /mos:systems-thinking renders a ranked move selector (M1-M5), not a fixed march.
- GUIDED is the default; "let me think" / "just tell me" / "stop" dial overrides are present.
- The three-layer blend (Brain flow + navigation.cjs local state + memory-ops.cjs trajectory) is documented, ranked locally.
- Both flow-source paths (Brain PREREQUISITE/FEEDS_INTO traversal AND the heuristic fallback) are documented, so the command works whether or not Plan 01 ran.
- Filing is stage-aware (active section); meta-lens framing stated.
- Connector frontmatter unchanged; no new command; no em-dashes anywhere.
</verification>

<success_criteria>
/mos:systems-thinking is a working F-surface move-selector with GUIDED default, dial overrides, three-layer local ranking, stage-aware filing, and the meta-lens framing, promoted in place with the connector unchanged, and it ships whether or not Piece A landed.
</success_criteria>

<output>
Create `.planning/phases/150.10-systems-thinking-f-selector/150.10-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `references/methodology/causal-loop-diagrams.md` (new file) - dedicated CLD-building reference for M2 (reinforcing/balancing loops, signed links, story-first construction).
- `commands/systems-thinking.md` (modified) - the Sense->Rank->Offer->Pick->Execute->Re-rank selector loop, GUIDED default, dial overrides, stage-aware filing; new sub_mode notes: st-boundary, st-loop, st-archetype, st-leverage, st-validate.
- `references/methodology/systems-thinking.md` (modified) - 5-move definitions, CLD storytelling method, leverage-point to validation handoff, Session 2 examples (fishery stock, breakfast loop).
- No new code symbols (command + reference content; ranking reuses navigation.cjs + memory-ops.cjs).
