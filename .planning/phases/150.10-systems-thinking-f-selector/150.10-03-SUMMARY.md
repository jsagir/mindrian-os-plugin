---
phase: 150.10
plan: 03
subsystem: systems-thinking-selector
tags: [reach-repertoire, systems-thinking, frozen-6-invariant, registry-is-the-table, part-7-reuse]
requires:
  - "Plan 150.10-02 (Piece B): /mos:systems-thinking promoted to F-surface move-selector; sub_mode systems-thinking-loop wired"
  - "Phase 148-03 (IRW-04): reach-component-map.json + dispatcher resolveArchetype (registry-is-the-table)"
  - "Phase 143.3 connector frontmatter (reach_id context_block, sub_mode systems-thinking-loop, SENS-06, hierarchy_rank 44)"
  - "lib/core/sensors/sensor-types.cjs REACH_IDS (the frozen 6)"
  - "lib/hmi/dial-reach-orchestrator.cjs (DIAL_REACH_K / RECOMMEND_FLOOR / MARGIN_THRESHOLD)"
  - "lib/workflow/f-selector-ranker.cjs (MAX_K)"
provides:
  - "systems-thinking-loop as a first-class RANKED sub_mode component in the reach repertoire (resolvable via dispatcher.resolveArchetype)"
  - "tests/test-150.10-systems-thinking-repertoire.cjs (repertoire resolution + frozen-6 invariant + frozen constants, 16 assertions)"
  - "tests/run-all-150.10.sh (the one-command Phase 150.10 phase gate)"
affects:
  - "skills/intelligence-orchestrator/SKILL.md (reads the registry; surfaces systems-thinking ranked next to the intelligence engines)"
tech-stack:
  added: []
  patterns: ["registry-is-the-table (Part 7 component-registration idiom, mirrors Phase 148-03)", "frozen-6 constitutional invariant fence (mirrors test-148-frozen-contracts)", "one-command phase gate aggregator (mirrors run-all-148.sh)"]
key-files:
  created:
    - "tests/test-150.10-systems-thinking-repertoire.cjs"
    - "tests/run-all-150.10.sh"
  modified: []
decisions:
  - "Task 1 (the registry row) was ALREADY committed in HEAD by the Plan 02 wave; reach-component-map.json sub_modes already carries the systems-thinking-loop multiSelect row. No file edit needed; the row was verified present and resolvable (Rule 3: blocking work already done upstream)."
  - "systems-thinking-loop carries the multiSelect engine-class archetype (sibling of reverse-salient / whitespace / cross-domain-analogy / cross-domain-connect / dominant-design), so it ranks next to the intelligence engines."
  - "FROZEN-6 INVARIANT proven by test, not assumed: REACH_IDS length exactly 6, byte-equal to [context_block, contradiction, cross_room, brain_consult, deep_research, hats]; systems-thinking-loop never a reach_id; DIAL_REACH_K=6 / MAX_K=3 / 0.70 / 0.15 byte-unchanged."
  - "The runner's own em-dash sweep matches the forbidden glyph via its bash codepoint escape ($'\\u2014') so the runner carries no literal em-dash to trip its own gate."
metrics:
  duration: "~15 min"
  completed: "2026-06-14"
  tasks: 3
  files: 2
---

# Phase 150.10 Plan 03: Piece C -- systems-thinking-loop Ranked Reach Repertoire Summary

Registered `systems-thinking-loop` as a first-class RANKED sub_mode component in Larry's reach repertoire (`lib/hmi/reach-component-map.json`), resolvable via the dispatcher next to the Phase 148 intelligence engines, and proved by a falsifiable 16-assertion test that the registration did NOT move the frozen-6 constitutional reach bank or the frozen dial constants; bundled into a one-command phase gate that carries the 148 frozen fences and a Part-8 egress sweep.

## What shipped

**Task 1 - the repertoire registration (already landed, verified).** The `systems-thinking-loop` row already exists in `lib/hmi/reach-component-map.json` under the `sub_modes` block (`"systems-thinking-loop": { "archetype": "multiSelect" }`), committed in HEAD by the Plan 02 wave and mirrored in `data/connector-registry.json` (reach_id context_block, sub_mode systems-thinking-loop, SENS-06). The plan's Task 1 verify command passed verbatim: the row is present, `resolveArchetype('systems-thinking-loop')` returns `multiSelect` (NOT the default-on-miss `select`, proving repertoire-visibility), and the `reaches` block still has exactly 6 entries. No file edit was required; the registration is the registry-is-the-table component-registration idiom from Phase 148-03, and the connector was already wired. Recorded as a Rule 3 deviation (blocking work already done upstream).

**Task 2 - tests/test-150.10-systems-thinking-repertoire.cjs (the falsifiable fence).** A new plain-CJS `node:assert/strict` test, 16 assertions, mirroring the `test-148-component-map.cjs` (repertoire resolution) + `test-148-frozen-contracts.cjs` (frozen constants) idiom verbatim in style. Four behavior groups: (1) REPERTOIRE -- `resolveArchetype('systems-thinking-loop')` returns a valid non-default archetype, the sub_mode row carries multiSelect, and it ranks as a sibling of the five intelligence engines with reverse-salient (its named sibling) undisturbed; (2) FROZEN-6 INVARIANT -- `sensor-types.REACH_IDS` length is exactly 6, byte-equal to the canonical bank in order, systems-thinking-loop never appears in REACH_IDS, and the orchestrator SKILL.md `reach_ids:` line still declares the frozen 6; (3) FROZEN CONSTANTS -- `DIAL_REACH_K === 6`, `MAX_K === 3`, `RECOMMEND_FLOOR === 0.70`, `MARGIN_THRESHOLD === 0.15`, and the two caps stay distinct; (4) NO-7TH-IN-REGISTRY -- the `reaches` block has exactly 6 keys equal to the frozen bank, and systems-thinking-loop lives ONLY under `sub_modes` (never `reaches`, never `standing_options`). The real export names were confirmed by reading dial-reach-orchestrator.cjs / sensor-types.cjs / f-selector-ranker.cjs before asserting them.

**Task 3 - tests/run-all-150.10.sh (the phase gate).** A new bash runner (`set -uo pipefail`, no emoji, no em-dashes), 158 lines (min 20), mirroring the run-all-148.sh convention. It composes: (a) the `connector-registry --check` CI tripwire (stays green, the systems-thinking connector frontmatter is unchanged, no regeneration); (b) the new repertoire + frozen-6 test; (c) the CARRIED 148 frozen fences (test-148-frozen-contracts.cjs + test-148-component-map.cjs) proving Piece C did not regress the constitution; (d) a Part-8 grep sweep over the 150.10 local artifacts asserting zero egress-projection token, no free-text body channel toward Brain, no raw network egress in the test, and zero em-dashes. The runner runs to completion on any failure, prints per-suite PASS/FAIL + a tally, and exits 1 if any failed.

## Verification

- Task 1 automated check: PASS (`node -e` row-present / reaches-6 / non-default-archetype check: `OK multiSelect`).
- Task 2 automated check: PASS (`node tests/test-150.10-systems-thinking-repertoire.cjs`: 16/16 assertions green, exit 0).
- Task 3 automated check: PASS (`bash tests/run-all-150.10.sh`: 5/5 suites green, exit 0).
- Overall: systems-thinking-loop resolves as a ranked component; REACH_IDS length exactly 6 and byte-unchanged; DIAL_REACH_K=6 / MAX_K=3 / 0.70 / 0.15 unchanged; systems-thinking-loop never a reach_id; connector-registry --check green; carried 148 fences pass.
- Zero em-dashes across both new files (byte-level grep for U+2014 returned empty; the runner's self-sweep also confirms it).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue resolved upstream] Task 1 registry row already present in HEAD**
- **Found during:** Task 1
- **Issue:** The plan's Task 1 instructs adding the `systems-thinking-loop` sub_mode row to reach-component-map.json, but the row was already committed in HEAD (landed by the Plan 02 wave) and already present in data/connector-registry.json.
- **Fix:** No edit made (an empty/duplicate edit would be a no-op and a spurious commit). Verified the row is present, resolvable to a non-default archetype, and that the reaches block still has exactly 6 entries. The plan's Task 1 verify command passed verbatim.
- **Files modified:** none (lib/hmi/reach-component-map.json was already correct).
- **Commit:** n/a (no Task 1 file change; the registration is captured by the prior wave + proven by the Task 2 test).

**2. [Rule 1 - Bug] Runner self-tripped its own em-dash sweep**
- **Found during:** Task 3
- **Issue:** The Part-8 em-dash sweep originally matched the forbidden glyph with a literal em-dash in the grep pattern, so the runner flagged itself.
- **Fix:** Replaced the literal glyph with the bash ANSI-C codepoint escape (the `EMDASH` variable set to the U+2014 codepoint via the `$'...u2014...'` form), so the runner source carries no literal em-dash while the sweep still matches the glyph at runtime (verified the escape resolves to bytes e2 80 94).
- **Files modified:** tests/run-all-150.10.sh
- **Commit:** 75e70f8c (folded into the Task 3 commit before it landed green).

## Canon compliance

- Part 7 (Reuse Before Build): a registry add + the orchestrator ranking surface, mirroring the Phase 148 component-registration idiom; not a new reach mechanism. The connector was already wired.
- Part 3 / Part 4: systems-thinking-loop is a ranked sub_mode under the Shape F selector repertoire; its archetype routes the AskUserQuestion mode the dispatcher constructs.
- Part 8 (Graph Boundary): pure-local registry + test edits; the Part-8 sweep asserts zero network/Brain surface was added.
- FROZEN-6 INVARIANT (Canon Appendix D entry 15, "NEVER a 7th"): proven intact by Test 2 + Test 4; systems-thinking-loop rides as a sub_mode under context_block, never a 7th reach_id.
- HARD RULE: zero em-dashes.

## Known Stubs

None. The test references real, shipped exports (REACH_IDS, DIAL_REACH_K, MAX_K, RECOMMEND_FLOOR, MARGIN_THRESHOLD, resolveArchetype) and the real registry rows. systems-thinking-loop is genuinely resolvable via the dispatcher, not a placeholder.

## Self-Check: PASSED

- Files: tests/test-150.10-systems-thinking-repertoire.cjs, tests/run-all-150.10.sh, 150.10-03-SUMMARY.md all FOUND.
- Commits: 421bb861 (Task 2 test), 75e70f8c (Task 3 aggregator) both FOUND in git log.
