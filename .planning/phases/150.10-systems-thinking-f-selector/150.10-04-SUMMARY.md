---
phase: 150.10
plan: 04
subsystem: systems-thinking-selector
tags: [leverage-points, meadows, local-graph-excavation, reverse-salient-reuse, part-8-boundary, part-9-read-path, tdd-red-first]
requires:
  - "Plan 150.10-01 (Piece A): the M1-M5 move-flow seed + the systems-thinking Brain nodes"
  - "Plan 150.10-02 (Piece B): /mos:systems-thinking promoted to F-surface move-selector; M4 st-leverage move"
  - "Plan 150.10-03 (Piece C): tests/run-all-150.10.sh (the one-command phase gate)"
  - "lib/core/navigation.cjs (the room.db chokepoint: openRoomDbForCaller / closeRoomDbForCaller)"
  - "scripts/rs-engine.py (writes typed REVERSE_SALIENT edges into room.db carrying signed_diff)"
provides:
  - "lib/core/leverage-scan.cjs scanLeveragePoints(db): runs the 12 Meadows signature queries over room.db, returns candidates ranked highest-leverage-first"
  - "references/methodology/leverage-scan-signatures.md: the local Tier-0 source of truth (12-level Meadows -> room.db-signature mapping)"
  - "tests/test-leverage-scan.cjs: RED-first fixture test for the scanner ranking + the level tags"
  - "M4 in commands/systems-thinking.md invokes the scanner + renders a Shape-F.1 Decision Gate + /mos:find-bottlenecks handoff"
  - "M3 in commands/systems-thinking.md chains to /mos:find-analogies + /mos:research (ST-18)"
affects:
  - "tests/run-all-150.10.sh (the phase gate gains the leverage-scan leg + the Part 8 floor grep)"
  - "the M4 leverage move stops being taxonomic and becomes operational over the local graph"
tech-stack:
  added: []
  patterns:
    - "caller-owned db handle from the navigation chokepoint (the scanner has ZERO require: no direct sqlite open, no room-db require, no fs scan)"
    - "RED-first TDD (failing test committed, then the GREEN implementation)"
    - "reuse-before-build: levels 6-8 READ the shipped rs-engine REVERSE_SALIENT edges rather than reimplement bottleneck detection (ST-17, Canon Part 7)"
    - "Part 8 floor grep (fetch|http|curl|brain|tavily returns 0 over the scanner)"
key-files:
  created:
    - "lib/core/leverage-scan.cjs"
    - "references/methodology/leverage-scan-signatures.md"
    - "tests/test-leverage-scan.cjs"
  modified:
    - "commands/systems-thinking.md"
    - "tests/run-all-150.10.sh"
decisions:
  - "Task 1 (the Brain Method node 'Leverage Point Local-Graph Excavation' + its PART_OF/RELATED_TO/MENTIONED_IN edges) was written DIRECTLY to Neo4j by the orchestrator, orphan-scan verified 0. This executor did NOT touch Neo4j/Brain; it executed ONLY the local Tasks 2/3/4."
  - "The scanner reads room.db through a CALLER-OWNED db handle issued by navigation.openRoomDbForCaller. It has ZERO require() calls of its own, so it CANNOT bypass the chokepoint (no direct sqlite open, no room-db require, no fs scan). The caller (the test, and the M4 prose) owns the open/close. This is the strongest possible Part 9 read-path discipline."
  - "Part 8 floor grep matched once on an explanatory COMMENT that quoted the grep pattern verbatim; the comment was reworded so the literal grep over lib/core/leverage-scan.cjs returns 0 matches, satisfying the plan must_have exactly."
  - "Levels 6-8 (information flows / reinforcing / balancing) READ the existing rs-engine REVERSE_SALIENT edges (source=rs-engine, signed_diff). Negative signed_diff (semantic_implementation) reads as a balancing lag -> level 8; non-negative (structural_transfer) as an information-flow connector -> level 6. The scanner does NOT recompute reverse salients (ST-17)."
  - "Ranking is strictly Meadows-level-first (lower number = higher leverage); a per-signature signal boost is capped at sub-band (1/24) so it never crosses an adjacent level."
metrics:
  duration: "~5 min"
  completed: "2026-06-14"
  tasks: 3
  files: 5
---

# Phase 150.10 Plan 04: Piece D -- Leverage Point Local-Graph Excavation Summary

Made the M4 leverage move OPERATIONAL instead of taxonomic: `lib/core/leverage-scan.cjs` runs Donella Meadows' 12 leverage-level signatures over a room's local graph (room.db, read ONLY via the navigation.cjs chokepoint) and returns candidate leverage points ranked highest-leverage-first, reusing the shipped rs-engine REVERSE_SALIENT edges for the mid-leverage band (ST-17) and keeping every room candidate LOCAL (Canon Part 8). The selector's M4 now invokes the scanner and surfaces ranked candidates as a Shape-F.1 Decision Gate with a /mos:find-bottlenecks chain handoff; M3 chains to /mos:find-analogies + /mos:research (ST-18).

## What shipped

**Task 1 (ST-13) - done externally, recorded here.** The Brain Method node "Leverage Point Local-Graph Excavation" (generic methodology, scan_pattern = the 12-level signature mapping) plus its PART_OF -> M4, RELATED_TO -> Leverage Points, MENTIONED_IN -> IRIS 2026 Session 2 Lecture edges were written DIRECTLY to Neo4j by the orchestrator before this executor ran; the orphan-scan gate returned 0. This executor did NOT touch Neo4j/Brain and executed only the local Tasks 2/3/4.

**Task 2 (ST-13..ST-14) - the local mirror (Tier-0 resilience).** `references/methodology/leverage-scan-signatures.md` holds the same 12-level Meadows -> room.db-signature mapping as the LOCAL source of truth, so the scanner runs with Brain offline (Canon Part 1). Every level lists its room.db signature plus the node/edge types each query uses (node types claim/assumption/decision/opportunity/CausalClaim/governing_thought; edge types INFORMS/CONTRADICTS/CONVERGES/INVALIDATES/ENABLES/REFINES/ROOT_CAUSES/FEEDS_INTO/DEFERRED plus the rs-engine REVERSE_SALIENT). Levels 6-8 carry the explicit rs-engine reuse note (signed_diff = lag = leverage signal). Zero em-dashes.

**Task 3 (ST-15..ST-17) - the scanner, RED-first.** `tests/test-leverage-scan.cjs` was committed FIRST and proven RED (MODULE_NOT_FOUND). It builds a scratch room.db fixture with a known structure (an assumption node, a CONVERGES 2-cycle, a high-degree hub, a leaf scalar claim, a root governing_thought, and an rs-engine REVERSE_SALIENT edge), then asserts `scanLeveragePoints(db)` returns candidates tagged with their Meadows level and ranked lower-number-first, with the assumption flagged level-5 (Rules), the CONVERGES cycle level-7, the leaf scalar level-12, the governing_thought level-3, and the REVERSE_SALIENT node in the 6-8 band. Then `lib/core/leverage-scan.cjs` was implemented GREEN (9/9 checks): it runs the 12 signature queries over a caller-owned db handle, returns `{nodeId, label, meadows_level, signature, score}` ranked highest-leverage-first. The mid-leverage band READS the existing rs-engine REVERSE_SALIENT edges (no reimplementation, ST-17). The scanner has ZERO require() calls, so it cannot open sqlite directly or fs-scan; the navigation chokepoint is the sole read path (Canon Part 9). The Part 8 floor grep (`fetch|http|curl|brain|tavily`) returns 0 matches over the module.

**Task 4 (ST-15..ST-18) - wire M4 + M3 into the selector + extend the gate.** In `commands/systems-thinking.md`: (a) a new "M4 (st-leverage)" section makes M4 open the active room.db through the navigation chokepoint, run `scanLeveragePoints`, and surface the top ranked candidates as a Shape-F.1 Decision Gate (name-and-offer per the M5 name-and-stop discipline; NEVER auto-apply), with a chain handoff to /mos:find-bottlenecks for the rs-engine reverse-salient band; (b) a new "M3 (st-archetype)" section adds name-and-offer chain handoffs to /mos:find-analogies (analogous systems for ideation) and /mos:research (web evidence) per ST-18. Both handoffs name-and-offer, never auto-invoke; candidates stay LOCAL. `tests/run-all-150.10.sh` gained the `test-leverage-scan.cjs` leg, the Part 8 floor grep over `lib/core/leverage-scan.cjs`, and an em-dash sweep over the new artifacts. `bash tests/run-all-150.10.sh` is green 6/6.

## Verification

- `node tests/test-leverage-scan.cjs` -> 9/9 GREEN (was RED before the module landed).
- `grep -nE "fetch|http|curl|brain|tavily" lib/core/leverage-scan.cjs` -> 0 matches (Part 8 floor).
- The scanner has zero `require()` calls -> no direct sqlite open, no room-db require, no fs scan (Part 9 read-path: navigation.cjs is the sole door).
- `node scripts/check-substrate.cjs --diff` + `node scripts/check-schema-aliases.cjs` -> both exit 0 on the staged scanner.
- `bash tests/run-all-150.10.sh` -> 6/6 PASS (repertoire + the new leverage-scan leg + 148 frozen fences + Part-8 sweep).
- Zero em-dashes across all five artifacts (verified by grep over U+2014 and the runner's own sweep).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Part 8 floor grep matched on an explanatory comment.**
- **Found during:** Task 3 (after first GREEN).
- **Issue:** The plan must_have requires `grep -nE 'fetch|http|curl|brain|tavily' lib/core/leverage-scan.cjs` to return 0. The first draft's header comment quoted that grep pattern verbatim and used the lowercase token, so the literal grep matched once (a comment, not a code path -- no actual egress surface).
- **Fix:** Reworded the comment to describe the contract without the literal forbidden tokens. The grep now returns 0 matches; no behavior change.
- **Files modified:** lib/core/leverage-scan.cjs
- **Commit:** 21e0b30a

No other deviations. Task 1 was executed externally by the orchestrator (Brain write + orphan-scan 0) and is out of this executor's scope by design.

## Known Stubs

None. The scanner is fully wired: it reads real room.db structure via the chokepoint and the level-6-8 band reads real rs-engine edges. Tier-0 fallback (null db -> empty array) is a documented graceful-degradation path, not a stub. The M4/M3 prose describes invocation the runtime selector performs; no placeholder data flows to any UI.

## Commits

- 17661b09 docs(150.10-04): local mirror of the 12-level Meadows leverage-scan signature mapping (Task 2)
- d3e16c1c test(150.10-04): add failing test for the leverage-point scanner (Task 3 RED)
- 21e0b30a feat(150.10-04): implement the local leverage-point scanner (Task 3 GREEN)
- 4f7c9b51 feat(150.10-04): wire M4 scanner invocation + M3 chain handoffs into the selector (Task 4)

## Self-Check: PASSED

- Created files all present: lib/core/leverage-scan.cjs, references/methodology/leverage-scan-signatures.md, tests/test-leverage-scan.cjs, 150.10-04-SUMMARY.md.
- All four per-task commits present in git log: 17661b09, d3e16c1c, 21e0b30a, 4f7c9b51.
- Zero em-dashes in the summary and all artifacts.
