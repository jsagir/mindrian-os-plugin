---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 07
type: execute
wave: 3
depends_on: ["134-03"]
files_modified:
  - lib/core/topic-forest.cjs
  - lib/core/fingerprint-math.cjs
  - tests/test-fingerprint-cjs-parity.cjs
autonomous: true
requirements: [CJS-134-FOREST, CJS-134-FINGERPRINT, CJS-134-FP-PARITY, CJS-134-WSMATH]
tags: [topic-forest, hdbscan, diagnostics, fingerprint, parity]
user_setup: []

must_haves:
  truths:
    - "topic-forest.cjs replaces compute_topic_forest.py in-process (HDBSCAN/taxonomy/topicforest strategy by corpus size, via the Wave-0 clustering strategy + rs-embed.cjs)"
    - "fingerprint-math.cjs replaces the 4 /mos:fingerprint (diagnostics) analyzers: compute-disruption-index.py + compute-blindspot-mass.py + compute-element-novelty.py + compute-bayesian-surprise.py"
    - "The CJS fingerprint produces the SAME scores as the Python on the COMMITTED fixture (byte-identical OR documented drift) -- the FINGERPRINT PARITY GATE against committed fixtures"
  artifacts:
    - path: "lib/core/topic-forest.cjs"
      provides: "In-process topic-forest clustering (replaces scripts/compute_topic_forest.py)"
      min_lines: 90
    - path: "lib/core/fingerprint-math.cjs"
      provides: "In-process CD-index + Good-Turing coverage + centroid novelty + Bayesian surprise (replaces the 4 diagnostics scripts)"
      min_lines: 100
    - path: "tests/test-fingerprint-cjs-parity.cjs"
      provides: "The fingerprint + topic-forest parity gate against committed Python golden fixtures"
      min_lines: 45
  key_links:
    - from: "lib/core/topic-forest.cjs"
      to: "lib/core/rs-embed.cjs + rs-math.cjs (PCA via powerIteration) + the Wave-0 clustering strategy"
      via: "in-process require"
      pattern: "rs-embed|hdbscan|cluster"
    - from: "lib/core/fingerprint-math.cjs"
      to: "lib/core/rs-embed.cjs (centroid novelty + Bayesian surprise)"
      via: "in-process require"
      pattern: "rs-embed"
---

<objective>
Port the topic-forest clustering + the `/mos:fingerprint` (formerly `/mos:diagnostics`) analyzer surface to in-process CJS:
- `scripts/compute_topic_forest.py` -> `lib/core/topic-forest.cjs` (HDBSCAN flat clustering + PCA + cosine, with the taxonomy/topicforest fallbacks by corpus size),
- `scripts/compute-disruption-index.py` (Funk & Owen-Smith 2017 CD index) + `scripts/compute-blindspot-mass.py` (Good-Turing coverage) + `scripts/compute-element-novelty.py` (centroid-distance novelty) + `scripts/compute-bayesian-surprise.py` (leave-one-out cosine shift) -> `lib/core/fingerprint-math.cjs`,
all in-process via `rs-embed.cjs` (Plan 03) + the Wave-0 clustering strategy.

Purpose: This is the rest of Family B the original plan set never touched -- the `/mos:whitespace` topic-forest sub-path (`whitespace-command.cjs` line ~384) and the four `/mos:fingerprint` analyzers (`diagnostics-command.cjs`). The diagnostics scripts that embed (element-novelty, bayesian-surprise) import `ensure_ml_deps.py` and auto-pip sentence-transformers -- the install-fragility class. CD-index and Good-Turing are pure frequency/graph arithmetic (no embedding). topic-forest's HDBSCAN is the Wave-0 clustering question (pure-JS lib if audited clean, else the taxonomy/topicforest fallbacks the Python already uses for common corpus sizes). This plan is file-disjoint from Plan 06 (topic-forest/fingerprint vs whitespace/brain-baseline) so they run in parallel within Wave 3.
Output: `topic-forest.cjs` + `fingerprint-math.cjs` + the committed-fixture fingerprint parity gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md

<read_first>
- scripts/compute_topic_forest.py (684 lines: choose_strategy by artifact count (20-50 -> hdbscan; <20 -> taxonomy; >50 -> topicforest), build_hdbscan_tree (sklearn HDBSCAN + PCA + cosine), build_taxonomy_tree, build_topicforest_tree; output the topic-forest tree + whitespace_branches. Use the Wave-0 clustering strategy for the hdbscan branch.)
- scripts/compute-disruption-index.py (211 lines: Funk & Owen-Smith 2017 CD index over the room's citation/reference graph -- pure arithmetic, no embedding, no sklearn; the interpretation strings diagnostics-command.cjs expects.)
- scripts/compute-blindspot-mass.py (209 lines: Good-Turing coverage estimation over term/element frequency -- Counter-based frequency arithmetic, no embedding.)
- scripts/compute-element-novelty.py (254 lines: centroid-distance novelty -- embeds elements via sentence-transformers (-> rs-embed.cjs), computes distance from the corpus centroid.)
- scripts/compute-bayesian-surprise.py (175 lines: leave-one-out cosine shift -- embeds via sentence-transformers (-> rs-embed.cjs), measures how much each element shifts the centroid.)
- scripts/diagnostics-command.cjs (lines 36, 109, 193-218: execSync python3 each compute-*.py + the GROUND-TRUTH interpretation thresholds it hardcodes -- read these so the CJS output matches the interpretation-string contract. NOTE: this command is renaming to /mos:fingerprint this milestone; keep the output contract stable.)
- scripts/whitespace-command.cjs (line ~376-384: the compute_topic_forest.py spawn Plan 08 rewires onto topic-forest.cjs.)
</read_first>

<interfaces>
Wave-0 LOCKED decisions this plan consumes (134-SPIKE.md):
- The clustering strategy for the topic-forest hdbscan branch: a pure-JS density-clustering lib (if it passed the vendoring audit) OR the taxonomy/topicforest fallbacks the Python already implements for the common corpus sizes. Build against whatever was locked.
- The model-unify disposition (element-novelty + bayesian-surprise embed via the unified rs-embed.cjs e5-large).

The CJS surface (consumed by Plan 08 callers):
- topic-forest.cjs: buildTopicForest({ roomDir }) -> the tree + whitespace_branches matching compute_topic_forest.py output.
- fingerprint-math.cjs: computeDisruptionIndex(roomDir), computeBlindspotMass(roomDir), computeElementNovelty(roomDir), computeBayesianSurprise(roomDir) -> each matching its Python script's output JSON + the diagnostics-command.cjs interpretation contract.
- Embedding-based metrics via rs-embed.cjs; PCA via rs-math.cjs powerIteration. Zero Python.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: topic-forest.cjs (Wave-0 clustering strategy) + parity</name>
  <files>lib/core/topic-forest.cjs, tests/test-fingerprint-cjs-parity.cjs</files>
  <behavior>
    - buildTopicForest picks the strategy by corpus size (taxonomy / hdbscan / topicforest) exactly as compute_topic_forest.py, and produces the same tree + whitespace_branches shape on the committed fixture within the Wave-0 drift bar.
    - The hdbscan branch uses the Wave-0 clustering strategy (pure-JS lib or the documented fallback); PCA via rs-math.cjs powerIteration; embeddings via rs-embed.cjs.
    - Zero Python / child_process.
  </behavior>
  <action>Implement lib/core/topic-forest.cjs porting compute_topic_forest.py. Reproduce choose_strategy (by artifact count), build_taxonomy_tree, build_topicforest_tree, and build_hdbscan_tree using the Wave-0 clustering strategy (pure-JS density-clustering lib if audited clean in Plan 01, else the taxonomy/topicforest fallback for the relevant corpus sizes). Use rs-embed.cjs for embeddings and rs-math.cjs powerIteration for PCA. Preserve the output tree + whitespace_branches shape so whitespace-command.cjs (rewired in Plan 08) consumes it unchanged. Begin tests/test-fingerprint-cjs-parity.cjs asserting buildTopicForest against the committed Wave-0 topic-forest golden fixture (Task 2 adds the 4 diagnostics analyzers to the same gate).</action>
  <verify>
    <automated>node -e "require('./lib/core/topic-forest.cjs')" && node --test tests/test-fingerprint-cjs-parity.cjs</automated>
  </verify>
  <done>topic-forest.cjs reproduces the 3-strategy clustering in-process; the topic-forest parity assertion passes against the committed fixture; grep confirms zero child_process/.py. The diagnostics half completes in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fingerprint-math.cjs (CD-index + Good-Turing + element-novelty + Bayesian-surprise) + the FINGERPRINT PARITY GATE</name>
  <files>lib/core/fingerprint-math.cjs, tests/test-fingerprint-cjs-parity.cjs</files>
  <behavior>
    - computeDisruptionIndex + computeBlindspotMass reproduce the Funk-CD and Good-Turing arithmetic exactly (pure arithmetic, no embedding) -- byte-parity with the committed fixtures.
    - computeElementNovelty + computeBayesianSurprise embed via rs-embed.cjs and reproduce the centroid-distance / leave-one-out cosine-shift scores within the Wave-0 drift bar.
    - Each output matches its Python script's JSON shape + the diagnostics-command.cjs interpretation-threshold contract.
    - THE FINGERPRINT PARITY GATE asserts all four metrics + topic-forest against COMMITTED Python golden fixtures (not a python3-at-test-time skip).
    - Zero Python / child_process.
  </behavior>
  <action>Implement lib/core/fingerprint-math.cjs porting the four diagnostics scripts. computeDisruptionIndex: port the Funk & Owen-Smith 2017 CD index arithmetic over the room reference graph (no embedding). computeBlindspotMass: port the Good-Turing coverage estimation (Counter-based frequency arithmetic; reproduce the same smoothing). computeElementNovelty: embed elements via rs-embed.cjs, compute centroid distance. computeBayesianSurprise: embed via rs-embed.cjs, compute the leave-one-out centroid shift. Preserve each output JSON shape + the GROUND-TRUTH interpretation thresholds diagnostics-command.cjs hardcodes (lines ~193-218) so the /mos:fingerprint interpretation strings stay stable across the rename. Complete tests/test-fingerprint-cjs-parity.cjs as THE FINGERPRINT PARITY GATE: assert topic-forest + all four diagnostics against COMMITTED Wave-0 Python golden fixtures byte-for-byte OR within the locked drift bound (max delta). Per WARNING-7: committed fixtures are the default + only parity path; do NOT gate on python3-at-test-time.</action>
  <verify>
    <automated>node --test tests/test-fingerprint-cjs-parity.cjs</automated>
  </verify>
  <done>fingerprint-math.cjs reproduces all four diagnostics in-process; the fingerprint parity gate passes against committed fixtures (topic-forest + CD + coverage + novelty + surprise) within the Wave-0 bar and runs when python3 is absent; grep confirms zero child_process/.py in topic-forest.cjs + fingerprint-math.cjs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room corpus -> in-process clustering/embedding | LOCAL room artifacts processed in-process; never egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-25 | Tampering | HDBSCAN / CD-index / Good-Turing / novelty drift vs sklearn | mitigate | The fingerprint parity gate runs against committed golden fixtures; drift beyond the Wave-0 bar fails the gate. |
| T-134-26 | Information Disclosure | element-novelty / bayesian-surprise embed room content | accept | Embedding runs in-process via rs-embed.cjs; Canon Part 8 preserved (no room text egress). |
| T-134-27 | Repudiation | interpretation-string threshold drift breaks /mos:fingerprint output | mitigate | The interpretation thresholds diagnostics-command.cjs hardcodes are preserved + asserted against the committed fixture. |
</threat_model>

<verification>
- `node --test tests/test-fingerprint-cjs-parity.cjs` passes against committed fixtures (does NOT skip when python3 absent).
- `grep -nE "child_process|spawn|execFile|\\.py\\b|sentence_transformers" lib/core/topic-forest.cjs lib/core/fingerprint-math.cjs` returns nothing.
</verification>

<success_criteria>
The topic-forest + the four /mos:fingerprint diagnostics analyzers run entirely in-process via the unified embedder + the Wave-0 clustering strategy, and pass the fingerprint parity gate against committed Python golden fixtures.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-07-SUMMARY.md` when done.
</output>
