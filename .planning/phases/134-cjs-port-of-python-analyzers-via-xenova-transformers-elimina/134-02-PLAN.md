---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 02
type: tdd
wave: 1
depends_on: ["134-01"]
files_modified:
  - lib/core/rs-math.cjs
  - tests/test-rs-math-cjs.cjs
autonomous: true
requirements: [CJS-134-SVD, CJS-134-MATH]
tags: [svd, lsa, cosine, math, transformers-js]
user_setup: []

must_haves:
  truths:
    - "rs-math.cjs computes cosine similarity identically to rs_math.py for the same input vectors"
    - "rs-math.cjs computes an LSA topic-membership matrix that produces the same ranked reverse-salient findings as rs_math.py on the fixture room (per the Wave-0 correctness bar)"
    - "rs-math.cjs has zero Python and zero child_process"
  artifacts:
    - path: "lib/core/rs-math.cjs"
      provides: "Pure-JS port of rs_math.py: cosine, abs_diff_topk, classify_direction, build_lsa_matrix (via the Wave-0-chosen SVD strategy)"
      min_lines: 120
    - path: "tests/test-rs-math-cjs.cjs"
      provides: "Parity tests vs rs_math.py reference outputs on fixtures"
      min_lines: 40
  key_links:
    - from: "lib/core/rs-math.cjs"
      to: "the Wave-0 SVD strategy in 134-SPIKE.md"
      via: "the chosen truncated-SVD implementation"
      pattern: "svd|SVD|lsa|LSA"
---

<objective>
Port `lib/core/rs_math.py` to `lib/core/rs-math.cjs` in pure JavaScript: the trivial parts (cosine similarity, abs_diff_topk, classify_direction, the L1-similarity normalization) and the one hard part (the TF-IDF + truncated-SVD LSA matrix), using the SVD strategy LOCKED in Wave 0 (`134-SPIKE.md`).

Purpose: rs-math is the foundation every other CJS module sits on. It has no model surface and no I/O, so it ports first and independently. The correctness bar from Wave 0 is the gate: the CJS LSA must produce the SAME ranked reverse-salient findings as the Python on the fixture room.
Output: `lib/core/rs-math.cjs` + parity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md

<read_first>
- lib/core/rs_math.py (the source of truth for the port). Public API per its __all__: build_tfidf_svd, extract_topic_keywords, count_topic_membership, normalize_and_l1_similarity, abs_diff_topk, classify_direction, build_lsa_matrix. The corpus-level entry is build_lsa_matrix(texts, n_components=80, top_k=7) returning an np.ndarray topic-count matrix; classify_direction(signed_diff)->str maps to the reverse-salient direction enum consumed by bridge-writer.cjs.
- lib/core/rs-differential-scorer.cjs (lines ~115-136 document WHY rs_math.py is corpus-level only: build_tfidf_svd expects Sequence[str] >= 3 docs; the pair-wise scorer uses a SEPARATE embedded LSA bridge. The CJS port must preserve this corpus-level contract -- it is the rs-engine internal-mode path, NOT the pair-wise scorer path.)
</read_first>

<interfaces>
The downstream consumer contract (what rs-math.cjs must satisfy):
- classify_direction(signedDiff: number) -> 'upstream'|'downstream'|'balanced' (read the exact enum strings from rs_math.py classify_direction; bridge-writer.cjs maps `direction` to cascade-edge type).
- The LSA matrix feeds count_topic_membership -> normalize_and_l1_similarity -> abs_diff_topk, which produces the (signed_diff, abs_diff) pairs rs-engine writes as findings.
- Export shape: CJS module.exports with the same function names in camelCase (buildLsaMatrix, cosineSimilarity, absDiffTopk, classifyDirection, normalizeAndL1Similarity, etc.). No default export.
</interfaces>
</context>

<feature>
  <name>rs-math.cjs pure-JS math port</name>
  <files>lib/core/rs-math.cjs, tests/test-rs-math-cjs.cjs</files>
  <behavior>
    - cosineSimilarity([1,0,0],[1,0,0]) === 1; cosineSimilarity([1,0],[0,1]) === 0 (within float tolerance).
    - classifyDirection returns the exact same enum strings as rs_math.py classify_direction for the same signed_diff thresholds (test the boundary values).
    - absDiffTopk over a known topic-count matrix returns the same top-k (signed_diff, abs_diff) pairs as the Python reference (captured as a fixture in Wave 0 / regenerated here).
    - buildLsaMatrix(fixtureCorpus) produces a topic-membership matrix that, when run through the full internal-mode pipeline, yields the SAME ranked findings as rs_math.py on the fixture room -- byte-identical ranked order OR within the documented drift bar from 134-SPIKE.md.
    - The module requires zero Python and contains no child_process / spawn / execFile.
  </behavior>
  <action>Implement lib/core/rs-math.cjs as a pure-JS CJS module. Cosine, L1-normalization, abs_diff_topk, and classify_direction are direct arithmetic ports of rs_math.py (preserve the exact direction-enum thresholds). build_tfidf_svd + build_lsa_matrix use the SVD strategy LOCKED in 134-SPIKE.md (the chosen truncated-SVD: hand-rolled randomized-range-finder + power iteration, or the chosen library at the pinned version). Implement TF-IDF (term-frequency * inverse-document-frequency with the same smoothing rs_math.py's TfidfVectorizer uses -- check sublinear_tf / smooth_idf settings in the Python) and the n_components=80 truncated SVD over it. Keep build_lsa_matrix corpus-level (>=3 docs) -- do NOT fold in the pair-wise scorer path (that stays in rs-differential-scorer.cjs, migrated in Plan 06). Write tests/test-rs-math-cjs.cjs asserting parity against reference vectors captured from rs_math.py (regenerate the Python reference inside the test setup via a one-shot python3 invocation guarded so it skips with a clear message if python3 is absent, OR commit the reference outputs as a JSON fixture). Use the node:test convention matching tests/test-reverse-salient-agent.cjs.</action>
  <verify>
    <automated>node --test tests/test-rs-math-cjs.cjs</automated>
  </verify>
  <done>tests/test-rs-math-cjs.cjs passes: cosine + classify_direction + abs_diff_topk are byte-parity with rs_math.py, and buildLsaMatrix meets the Wave-0 correctness bar on the fixture. grep confirms zero child_process/spawn/.py in rs-math.cjs.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | rs-math.cjs is pure in-process arithmetic; no I/O, no network, no Python |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-04 | Tampering | SVD numerical drift vs scipy/sklearn | mitigate | The Wave-0 correctness bar (same ranked findings on fixture) is the parity test gate; drift beyond the documented bar fails the test. |
| T-134-05 | Repudiation | direction-enum mismatch breaks bridge-writer cascade edges | mitigate | classify_direction enum-string parity is an explicit test assertion against the Python boundary values. |
</threat_model>

<verification>
- `node --test tests/test-rs-math-cjs.cjs` passes.
- `grep -nE "child_process|spawn|execFile|require\\(.python|\\.py" lib/core/rs-math.cjs` returns nothing.
</verification>

<success_criteria>
rs_math.py is fully ported to pure-JS rs-math.cjs, parity-tested, with the LSA path meeting the Wave-0 correctness bar. No Python, no child_process.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-02-SUMMARY.md` when done.
</output>
