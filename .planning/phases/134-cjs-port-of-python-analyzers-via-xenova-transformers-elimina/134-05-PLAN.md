---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 05
type: execute
wave: 2
depends_on: ["134-02", "134-03"]
files_modified:
  - lib/core/hsi-compute.cjs
  - lib/core/hsi-whitespace.cjs
  - tests/test-hsi-cjs-parity.cjs
autonomous: true
requirements: [CJS-134-HSI, CJS-134-HSI-PARITY]
tags: [hsi, whitespace, transformers-js, parity]
user_setup: []

must_haves:
  truths:
    - "hsi-compute.cjs computes HSI scores in-process via rs-embed.cjs + rs-math.cjs, replacing compute-hsi.py"
    - "hsi-whitespace.cjs computes the HSI / RS / Analogy whitespace zones in-process, replacing discover-hsi-whitespace.py + discover-rs-whitespace.py + discover-analogy-whitespace.py"
    - "The CJS HSI produces the SAME ranked findings as the Python HSI on the fixture room (byte-identical OR documented drift) -- the HSI PARITY GATE"
    - "This is the CJS HSI Phase 131 deferred; the v1.14.0 source-lens fan-out turns it on"
  artifacts:
    - path: "lib/core/hsi-compute.cjs"
      provides: "In-process HSI scoring (replaces scripts/compute-hsi.py)"
      min_lines: 80
    - path: "lib/core/hsi-whitespace.cjs"
      provides: "In-process whitespace discovery: HSI + RS + Analogy zones (replaces the 3 discover-*-whitespace.py)"
      min_lines: 80
    - path: "tests/test-hsi-cjs-parity.cjs"
      provides: "The HSI parity gate: CJS HSI == Python HSI ranked findings on a fixture room"
      min_lines: 40
  key_links:
    - from: "lib/core/hsi-compute.cjs"
      to: "lib/core/rs-embed.cjs + lib/core/rs-math.cjs"
      via: "in-process require"
      pattern: "rs-embed|rs-math"
---

<objective>
Port the HSI computation surface to CJS: `scripts/compute-hsi.py` -> `lib/core/hsi-compute.cjs`, and the three whitespace-discovery scripts (`discover-hsi-whitespace.py`, `discover-rs-whitespace.py`, `discover-analogy-whitespace.py`) -> `lib/core/hsi-whitespace.cjs`, all in-process via `rs-embed.cjs` (Plan 03) + `rs-math.cjs` (Plan 02). This is the CJS HSI that Phase 131 explicitly DEFERRED to the v1.14.0 fan-out behind this phase's transformers.js HSI.

Purpose: HSI scoring (sentence-transformers + LSA) is the last Python compute surface. compute-hsi.py is "pure arithmetic" over embeddings + LSA (CONTEXT: "trivial port") once rs-embed + rs-math exist. The whitespace-discovery scripts are the /mos:whitespace `discover` cycle. The HSI PARITY GATE -- CJS HSI == Python HSI ranked findings on a fixture -- is the non-negotiable acceptance bar from the CONTEXT and the quality gate. This plan is independent of Plan 04 (different files: hsi-* vs rs-engine) so they run in parallel within Wave 2.
Output: `hsi-compute.cjs` + `hsi-whitespace.cjs` + the parity-gate test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md

<read_first>
- scripts/compute-hsi.py (the HSI scorer. Tier 1: sklearn + MiniLM; Tier 2: sklearn + Pinecone. The HSI math is arithmetic over the embedding/LSA outputs -- the CJS port computes the same scores using rs-embed.cjs embeddings + rs-math.cjs LSA. Read the score formula + the output JSON shape it writes to room/opportunity-bank/hsi-scores/.)
- scripts/discover-hsi-whitespace.py + scripts/discover-rs-whitespace.py + scripts/discover-analogy-whitespace.py (the 3 whitespace-discovery steps scripts/discovery-cycle.cjs spawns -- read discovery-cycle.cjs lines ~40-60 for how each is invoked: `python3 "${scriptPath}" "${roomDir}"`. Each writes a whitespace-results.json fragment.)
- scripts/discovery-cycle.cjs (the CJS orchestrator that currently spawns the 3 python whitespace scripts -- this plan builds the in-process replacement; Plan 06 rewires discovery-cycle.cjs to call hsi-whitespace.cjs instead of spawning python.)
- tests/test-finding-hsi-schema.cjs (the existing finding/HSI schema contract: finding shape extends HSIAnalysis with top_differential / semantic_surprise / category_errors_identified / top_differential_score in [0,1]. The CJS HSI output must satisfy this same schema.)
</read_first>

<interfaces>
The CJS HSI surface (consumed by Plan 06 callers + the v1.14.0 fan-out):
- computeHsi({ roomDir, ... }) -> Promise<{ scores: [...], ... }> matching the room/opportunity-bank/hsi-scores/ JSON shape compute-hsi.py wrote.
- discoverWhitespace({ roomDir, steps }) -> Promise<{ hsi: [...], rs: [...], analogy: [...] }> matching the whitespace-results.json shape discovery-cycle.cjs reads.
- All scores via rs-embed.cjs (embeddings) + rs-math.cjs (LSA / cosine). Zero Python.
- The HSI finding shape must satisfy tests/test-finding-hsi-schema.cjs (top_differential_score numeric in [0,1], etc.).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hsi-compute.cjs in-process HSI scorer</name>
  <files>lib/core/hsi-compute.cjs, tests/test-hsi-cjs-parity.cjs</files>
  <behavior>
    - computeHsi on the fixture room produces HSI scores matching the existing finding schema (test-finding-hsi-schema.cjs: top_differential_score in [0,1], semantic_surprise present, etc.).
    - The output JSON shape matches room/opportunity-bank/hsi-scores/ that compute-hsi.py wrote (so downstream readers like hsi-to-graph.cjs keep working).
    - Zero Python / child_process in the module.
  </behavior>
  <action>Implement lib/core/hsi-compute.cjs. Port the compute-hsi.py score formula (the HSI arithmetic over embeddings + LSA topic-membership). Use rs-embed.cjs.embed() for embeddings (replacing MiniLM/Pinecone) and rs-math.cjs for LSA + cosine. Preserve the output JSON shape compute-hsi.py wrote to room/opportunity-bank/hsi-scores/ and the finding fields test-finding-hsi-schema.cjs asserts (top_differential, semantic_surprise, category_errors_identified, top_differential_score clamped to [0,1]). Zero Python, zero child_process.</action>
  <verify>
    <automated>node -e "require('./lib/core/hsi-compute.cjs')" && node --test tests/test-finding-hsi-schema.cjs</automated>
  </verify>
  <done>hsi-compute.cjs computes HSI in-process, output satisfies the finding-HSI schema, no Python. (The full ranked-parity assertion lands in Task 2's parity gate.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hsi-whitespace.cjs (3 discover steps) + the HSI PARITY GATE</name>
  <files>lib/core/hsi-whitespace.cjs, tests/test-hsi-cjs-parity.cjs</files>
  <behavior>
    - discoverWhitespace({roomDir, steps:'all'}) produces hsi/rs/analogy whitespace zones matching the whitespace-results.json shape discovery-cycle.cjs reads.
    - THE HSI PARITY GATE: on the fixture room, the CJS HSI ranked findings === the Python HSI ranked findings (byte-identical ranked order) OR within the documented drift bar from 134-SPIKE.md with the max rank-position delta recorded.
    - Zero Python / child_process.
  </behavior>
  <action>Implement lib/core/hsi-whitespace.cjs porting the three discover-*-whitespace.py scripts: discoverWhitespace runs the HSI-between, RS-downstream, and Analogy-transfer whitespace steps in-process via rs-embed.cjs + rs-math.cjs + (for the RS step) rs-engine.cjs internal mode. Preserve the whitespace-results.json fragment shape each Python script wrote so discovery-cycle.cjs (rewired in Plan 06) consumes them unchanged. Write tests/test-hsi-cjs-parity.cjs as THE HSI PARITY GATE: run both the Python HSI (python3 scripts/compute-hsi.py + the 3 discover scripts on the fixture room) and the CJS HSI (computeHsi + discoverWhitespace), assert the ranked finding order matches byte-for-byte OR document the drift within the Wave-0 bar (assert max rank-position delta <= the locked bound). Guard the Python reference generation so the test skips with an explicit "python3 unavailable -- parity gate not run" message rather than passing silently if python3 is absent (the gate MUST run in CI where python3 exists during the both-paths beta).</action>
  <verify>
    <automated>node --test tests/test-hsi-cjs-parity.cjs</automated>
  </verify>
  <done>hsi-whitespace.cjs produces the 3 whitespace zone sets in-process; the HSI parity gate test passes (CJS == Python ranked findings on the fixture, or drift within the documented Wave-0 bar). grep confirms zero child_process/.py in both hsi-*.cjs modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room corpus -> in-process HSI | LOCAL room artifacts scored in-process; never egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-13 | Tampering | HSI ranked-finding drift vs Python | mitigate | The HSI parity gate (Task 2) is the explicit acceptance test; drift beyond the Wave-0 bar fails the gate. This is the CONTEXT's non-negotiable bar. |
| T-134-14 | Information Disclosure | HSI scoring of room content | accept | Scoring runs in-process via rs-embed.cjs; Canon Part 8 preserved (no room text egress). |
| T-134-15 | Repudiation | whitespace-results.json shape drift breaks discovery-cycle readers | mitigate | Output shape parity asserted against the Python-written fragment shape. |
</threat_model>

<verification>
- `node --test tests/test-hsi-cjs-parity.cjs` passes (the HSI parity gate).
- `node --test tests/test-finding-hsi-schema.cjs` passes against the CJS HSI output.
- `grep -nE "child_process|spawn|execFile|\\.py\\b|sentence_transformers" lib/core/hsi-compute.cjs lib/core/hsi-whitespace.cjs` returns nothing.
</verification>

<success_criteria>
The CJS HSI layer Phase 131 deferred is shipped in-process, satisfies the finding-HSI schema, and passes the HSI parity gate against the Python HSI on the fixture room.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-05-SUMMARY.md` when done.
</output>
