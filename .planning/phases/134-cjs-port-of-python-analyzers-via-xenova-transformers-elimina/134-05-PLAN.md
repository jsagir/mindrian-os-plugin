---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 05
type: execute
wave: 2
depends_on: ["134-02", "134-03"]
files_modified:
  - lib/core/hsi-compute.cjs
  - lib/core/reverse-salients.cjs
  - lib/core/hsi-whitespace.cjs
  - tests/test-hsi-cjs-parity.cjs
autonomous: true
requirements: [CJS-134-HSI, CJS-134-RS-DETECT, CJS-134-DISCOVER-WS, CJS-134-HSI-PARITY]
tags: [hsi, spectral-gap, reverse-salients, whitespace, parity]
user_setup: []

must_haves:
  truths:
    - "hsi-compute.cjs computes HSI scores in-process via rs-embed.cjs + rs-math.cjs, replacing compute-hsi.py INCLUDING the OM-HMM Markov spectral-gap math"
    - "reverse-salients.cjs replaces detect-reverse-salients.py: it consumes .hsi-results.json + spectral metadata and produces the cross-section reverse-salient findings (this is the auto-fire RCA surface from intelligence-cascade.cjs)"
    - "hsi-whitespace.cjs computes the HSI / RS / Analogy whitespace zones in-process, replacing discover-hsi-whitespace.py + discover-rs-whitespace.py + discover-analogy-whitespace.py"
    - "The CJS HSI produces the SAME ranked findings as the Python HSI on the COMMITTED fixture (byte-identical OR documented drift) -- the HSI PARITY GATE, run against committed golden fixtures (cannot pass-by-skip when python3 is absent)"
  artifacts:
    - path: "lib/core/hsi-compute.cjs"
      provides: "In-process HSI scoring incl. OM-HMM transition-matrix spectral gap (replaces scripts/compute-hsi.py)"
      min_lines: 100
    - path: "lib/core/reverse-salients.cjs"
      provides: "Cross-section reverse-salient detection over .hsi-results.json + spectral metadata (replaces scripts/detect-reverse-salients.py)"
      min_lines: 60
    - path: "lib/core/hsi-whitespace.cjs"
      provides: "In-process whitespace discovery: HSI + RS + Analogy zones (replaces the 3 discover-*-whitespace.py)"
      min_lines: 80
    - path: "tests/test-hsi-cjs-parity.cjs"
      provides: "The HSI parity gate against committed Python golden fixtures"
      min_lines: 50
  key_links:
    - from: "lib/core/hsi-compute.cjs"
      to: "lib/core/rs-embed.cjs + lib/core/rs-math.cjs (powerIteration for spectral gap)"
      via: "in-process require"
      pattern: "rs-embed|rs-math"
    - from: "lib/core/reverse-salients.cjs"
      to: ".hsi-results.json + spectral metadata"
      via: "in-process read of the HSI output"
      pattern: "hsi-results|spectral"
---

<objective>
Port the HSI + reverse-salient-detection + whitespace-discovery surface to CJS:
- `scripts/compute-hsi.py` -> `lib/core/hsi-compute.cjs` (INCLUDING the OM-HMM Markov-chain transition-matrix spectral-gap math, Seabrook & Wiskott 2022 -- a non-trivial sub-port that reuses rs-math.cjs's exported power-iteration eigen primitive),
- `scripts/detect-reverse-salients.py` -> `lib/core/reverse-salients.cjs` (the cross-section detector that consumes .hsi-results.json + spectral metadata),
- the three whitespace-discovery scripts (`discover-hsi-whitespace.py`, `discover-rs-whitespace.py`, `discover-analogy-whitespace.py`) -> `lib/core/hsi-whitespace.cjs`,
all in-process via `rs-embed.cjs` (Plan 03) + `rs-math.cjs` (Plan 02). This is the CJS HSI that Phase 131 explicitly DEFERRED to the v1.14.0 fan-out.

Purpose (corrected scope): The original plan ported compute-hsi + the discover trio but MISSED `detect-reverse-salients.py` -- which is the EXACT auto-fire surface that produced the Windows `ModuleNotFoundError` RCA (intelligence-cascade.cjs runs `python3 compute-hsi.py` THEN `python3 detect-reverse-salients.py` on every artifact write). Both must be ported here so Plan 08 can rewire the auto-fire path. The HSI PARITY GATE runs against COMMITTED golden fixtures (not a python3-at-test-time skip), so it cannot pass-by-skip where python3 is missing.
Output: `hsi-compute.cjs` + `reverse-salients.cjs` + `hsi-whitespace.cjs` + the committed-fixture parity gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md

<read_first>
- scripts/compute-hsi.py (the HSI scorer + the OM-HMM spectral block. Read: compute_lsa_similarity, compute_semantic_similarity_tier1/tier2, classify_sentence_mode, build_transition_matrix, compute_spectral_gap (spectral_gap = 1 - |second-largest eigenvalue|), compute_stationary_distribution, detect_absorbing_tendency, compute_omhmm_score. The CJS port computes the same scores using rs-embed.cjs embeddings + rs-math.cjs LSA + rs-math.cjs's exported power-iteration for the eigenvalue. Read the output JSON shape it writes -- .hsi-results.json -- since detect-reverse-salients.py consumes it.)
- scripts/detect-reverse-salients.py (242 lines, ported from V2's 287-line script -- the goal explicitly names this. Reads .hsi-results.json, groups by section, runs cross-section analysis, classify_opportunity (lsa_sim vs semantic_sim -> structural_transfer | semantic_implementation), scores with the spectral bonus from compute-hsi's OM-HMM metadata, generates thesis, updates the JSON. This is the auto-fire RCA surface.)
- scripts/discover-hsi-whitespace.py + scripts/discover-rs-whitespace.py + scripts/discover-analogy-whitespace.py (the 3 whitespace-discovery steps scripts/discovery-cycle.cjs spawns via `python3 "${scriptPath}" "${roomDir}"`; each writes a whitespace-results.json fragment).
- scripts/discovery-cycle.cjs (the CJS orchestrator that currently spawns the 3 python whitespace scripts -- Plan 08 rewires it to call hsi-whitespace.cjs instead of spawning python).
- lib/core/intelligence-cascade.cjs (lines ~309-340: the auto-fire that runs `execSync python3 compute-hsi.py` THEN `execSync python3 detect-reverse-salients.py` on every md write with room.db present -- the surface Plan 08 rewires onto hsi-compute.cjs + reverse-salients.cjs).
- tests/test-finding-hsi-schema.cjs (the existing finding/HSI schema contract: finding extends HSIAnalysis with top_differential / semantic_surprise / category_errors_identified / top_differential_score in [0,1]).
</read_first>

<interfaces>
The CJS HSI surface (consumed by Plan 08 callers + the v1.14.0 fan-out):
- computeHsi({ roomDir, ... }) -> Promise<{ scores:[...], ... }> matching the .hsi-results.json shape compute-hsi.py wrote (so detect-reverse-salients + hsi-to-graph.cjs keep working).
- detectReverseSalients({ roomDir }) -> Promise<{ findings:[...] }> reading the .hsi-results.json + spectral metadata, matching detect-reverse-salients.py's updated-JSON shape (so bridge-writer + the cascade keep working).
- discoverWhitespace({ roomDir, steps }) -> Promise<{ hsi:[...], rs:[...], analogy:[...] }> matching the whitespace-results.json shape discovery-cycle.cjs reads.
- All scores via rs-embed.cjs (embeddings) + rs-math.cjs (LSA / cosine / power-iteration eigen). Zero Python.
- The HSI finding shape must satisfy tests/test-finding-hsi-schema.cjs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hsi-compute.cjs (incl. OM-HMM spectral gap) + reverse-salients.cjs</name>
  <files>lib/core/hsi-compute.cjs, lib/core/reverse-salients.cjs, tests/test-hsi-cjs-parity.cjs</files>
  <behavior>
    - computeHsi on the fixture room produces HSI scores matching the finding schema (test-finding-hsi-schema.cjs: top_differential_score in [0,1], semantic_surprise present, etc.) and the .hsi-results.json shape compute-hsi.py wrote.
    - The OM-HMM spectral gap matches the Python: build the thinking-mode transition matrix, compute spectral_gap = 1 - |second eigenvalue| via rs-math.cjs's exported power-iteration, within the Wave-0 drift bar of the committed fixture.
    - detectReverseSalients reads .hsi-results.json + spectral metadata and produces the same cross-section findings (classify_opportunity + spectral bonus) as detect-reverse-salients.py on the committed fixture.
    - Zero Python / child_process in both modules.
  </behavior>
  <action>Implement lib/core/hsi-compute.cjs: port the compute-hsi.py score formula (HSI arithmetic over embeddings + LSA topic-membership) using rs-embed.cjs.embed() (replacing MiniLM/Pinecone) and rs-math.cjs for LSA + cosine. Port the OM-HMM block: classify_sentence_mode, build_transition_matrix, compute_spectral_gap (reuse rs-math.cjs's exported powerIterationTopK for the second eigenvalue -- do NOT re-implement an eigen solver), compute_stationary_distribution, detect_absorbing_tendency, compute_omhmm_score. Preserve the .hsi-results.json output shape + the finding fields test-finding-hsi-schema.cjs asserts. Then implement lib/core/reverse-salients.cjs porting detect-reverse-salients.py: read .hsi-results.json, group by section, run the cross-section analysis, classify_opportunity (lsa_sim vs semantic_sim), apply the spectral bonus from the OM-HMM metadata, generate the thesis string, and produce the findings in detect-reverse-salients.py's updated-JSON shape. Both modules: zero Python, zero child_process. Begin the parity test file tests/test-hsi-cjs-parity.cjs asserting computeHsi + detectReverseSalients against the COMMITTED Wave-0 golden fixtures (Task 2 completes the whitespace half of the gate).</action>
  <verify>
    <automated>node -e "require('./lib/core/hsi-compute.cjs');require('./lib/core/reverse-salients.cjs')" && node --test tests/test-finding-hsi-schema.cjs</automated>
  </verify>
  <done>hsi-compute.cjs computes HSI + OM-HMM spectral gap in-process; reverse-salients.cjs reproduces detect-reverse-salients.py's cross-section findings; output satisfies the finding-HSI schema; no Python. grep confirms hsi-compute reuses rs-math powerIteration (no second eigen solver). The full ranked-parity assertion completes in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hsi-whitespace.cjs (3 discover steps) + the HSI PARITY GATE against committed fixtures</name>
  <files>lib/core/hsi-whitespace.cjs, tests/test-hsi-cjs-parity.cjs</files>
  <behavior>
    - discoverWhitespace({roomDir, steps:'all'}) produces hsi/rs/analogy whitespace zones matching the whitespace-results.json shape discovery-cycle.cjs reads.
    - THE HSI PARITY GATE: on the committed fixture, the CJS HSI + reverse-salients + whitespace ranked findings === the Python golden fixtures (byte-identical ranked order) OR within the documented Wave-0 drift bar (assert max rank-position delta <= the locked bound). The gate reads COMMITTED JSON fixtures -- it does NOT shell out to python3, so it CANNOT pass-by-skip when python3 is absent.
    - Zero Python / child_process.
  </behavior>
  <action>Implement lib/core/hsi-whitespace.cjs porting the three discover-*-whitespace.py scripts: discoverWhitespace runs the HSI-between, RS-downstream (via reverse-salients.cjs / rs-engine internal), and Analogy-transfer whitespace steps in-process via rs-embed.cjs + rs-math.cjs. Preserve the whitespace-results.json fragment shape each Python script wrote so discovery-cycle.cjs (rewired in Plan 08) consumes them unchanged. Complete tests/test-hsi-cjs-parity.cjs as THE HSI PARITY GATE: assert the CJS HSI (computeHsi) + reverse-salients (detectReverseSalients) + whitespace (discoverWhitespace) ranked outputs match the COMMITTED Wave-0 Python golden fixtures byte-for-byte OR within the locked drift bound (assert max rank-position delta). Per checker WARNING-7: committed fixtures are the DEFAULT and ONLY parity path here -- do NOT write a `skip if python3 absent` branch as the primary gate; if you additionally add a live-python cross-check, gate it so its absence never makes the suite green.</action>
  <verify>
    <automated>node --test tests/test-hsi-cjs-parity.cjs</automated>
  </verify>
  <done>hsi-whitespace.cjs produces the 3 whitespace zone sets in-process; the HSI parity gate passes against committed golden fixtures (CJS == Python ranked findings, or drift within the documented Wave-0 bar) and runs even when python3 is absent. grep confirms zero child_process/.py in hsi-compute.cjs, reverse-salients.cjs, hsi-whitespace.cjs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room corpus -> in-process HSI | LOCAL room artifacts scored in-process; never egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-13 | Tampering | HSI / spectral-gap / RS-detect ranked-finding drift vs Python | mitigate | The HSI parity gate (Task 2) runs against COMMITTED golden fixtures; drift beyond the Wave-0 bar fails the gate; cannot pass by skipping when python3 absent. |
| T-134-14 | Information Disclosure | HSI scoring of room content | accept | Scoring runs in-process via rs-embed.cjs; Canon Part 8 preserved (no room text egress). |
| T-134-15 | Repudiation | whitespace-results.json / .hsi-results.json shape drift breaks downstream readers | mitigate | Output shape parity asserted against the committed Python-written fragment shapes. |
| T-134-21 | Repudiation | detect-reverse-salients spectral-bonus mismatch breaks cross-section findings | mitigate | reverse-salients.cjs parity asserted against the committed detect-reverse-salients.py golden fixture. |
</threat_model>

<verification>
- `node --test tests/test-hsi-cjs-parity.cjs` passes against committed fixtures (does NOT skip when python3 absent).
- `node --test tests/test-finding-hsi-schema.cjs` passes against the CJS HSI output.
- `grep -nE "child_process|spawn|execFile|\\.py\\b|sentence_transformers" lib/core/hsi-compute.cjs lib/core/reverse-salients.cjs lib/core/hsi-whitespace.cjs` returns nothing.
</verification>

<success_criteria>
The CJS HSI layer Phase 131 deferred is shipped in-process INCLUDING the OM-HMM spectral gap and the detect-reverse-salients port (the auto-fire RCA surface), satisfies the finding-HSI schema, and passes the HSI parity gate against committed Python golden fixtures.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-05-SUMMARY.md` when done.
</output>
