---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 04b
type: execute
wave: 2
depends_on: ["134-02", "134-03", "130.5"]
files_modified:
  - lib/core/rs-engine.cjs
  - tests/test-rs-engine-cjs.cjs
autonomous: true
requirements: [CJS-134-ENGINE, CJS-134-REUSE-130.5]
tags: [rs-engine, orchestrator, transformers-js, parity]
user_setup: []

must_haves:
  truths:
    - "rs-engine.cjs runs the three modes (internal / external / hybrid) in-process, producing the same .rs-engine-results.json finding shape rs-engine.py produced"
    - "rs-engine.cjs produces the SAME ranked findings as rs-engine.py on the fixture room (internal mode), per the committed Wave-0 golden fixture"
    - "rs-engine.cjs contains zero child_process / spawn / execFile / .py"
  artifacts:
    - path: "lib/core/rs-engine.cjs"
      provides: "In-process 3-mode reverse-salient engine orchestrator (replaces scripts/rs-engine.py)"
      min_lines: 200
    - path: "tests/test-rs-engine-cjs.cjs"
      provides: "Internal-mode ranked-parity vs the committed rs-engine.py golden fixture + bridge-writer output-shape contract"
      min_lines: 40
  key_links:
    - from: "lib/core/rs-engine.cjs"
      to: "lib/core/rs-rooms.cjs + rs-cache.cjs + rs-hybrid.cjs + rs-math.cjs + rs-embed.cjs"
      via: "in-process require"
      pattern: "rs-rooms|rs-hybrid|rs-math|rs-embed"
    - from: "lib/core/rs-engine.cjs"
      to: ".rs-engine-results.json (bridge-writer.cjs contract)"
      via: "byte-preserved finding output"
      pattern: "source_artifact_id|signed_diff|direction"
---

<objective>
Port the `scripts/rs-engine.py` orchestrator (1755 lines, 3 modes: internal / external / hybrid) to in-process CJS, consuming the helpers from Plan 04a (`rs-rooms.cjs` / `rs-cache.cjs` / `rs-hybrid.cjs`) + `rs-math.cjs` (Plan 02) + `rs-embed.cjs` (Plan 03), and REUSING the Phase 130.5 shared fetcher. This is the file-disjoint companion to Plan 04a (split per checker WARNING-6); 04a owns the helper files + rs_corpus deletion, 04b owns ONLY rs-engine.cjs + its parity test, so the two run in parallel within Wave 2 against the documented helper contracts.

Purpose: This is the engine spine. After this plan, the reverse-salient computation runs entirely in Node. The output `.rs-engine-results.json` shape must be preserved byte-for-byte so every existing consumer (bridge-writer.cjs, hsi-to-graph.cjs, auto-explore-fire.cjs, the readers found in the survey) keeps working without change. The actual cutover of callers (replacing child_process spawn with require) is Plan 08 -- this plan builds the in-process engine and proves parity; it does not yet rewire the agents.
Output: `rs-engine.cjs`; internal-mode parity test against the committed Wave-0 fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-CONTEXT.md

<read_first>
- scripts/rs-engine.py (the orchestrator. The 3 modes: --mode internal (room-only reverse-salient over the artifact corpus), --mode external (topic -> external corpus via fetcher), --mode hybrid (room + external unified corpus, --topk). The output payload shape: the `result` dict with `meta.mode` + `findings[]` each carrying source_artifact_id / target_artifact_id / signed_diff / abs_diff / direction (lines ~470-550). The internal-mode findings are written to .rs-engine-results.json.)
- lib/core/bridge-writer.cjs (the PRIMARY consumer of .rs-engine-results.json -- read lines ~30-130 for the exact pair field names it reads: source_artifact_id, source_section, source_room, source_artifact, target_artifact_id, signed_diff, abs_diff, direction. The CJS output MUST preserve these.)
- lib/agents/reverse-salient-agent.cjs (lines ~160-320: the execFileSync invocation of rs-engine.py + the .rs-engine-results.json reader. This is the caller Plan 08 rewires; read it to learn the exact CLI args + result-file path/contract the new engine must honor -- confirm the exact .rs-engine-results.json path the agent reads.)
- The Plan 04a helper contracts (rs-rooms.readRoomCorpus, rs-cache embed-once + fetchNamespaceVectors, rs-hybrid.buildUnifiedCorpus) -- build against these documented interfaces; 04a and 04b are the same wave and file-disjoint.
</read_first>

<interfaces>
130.5 forward contract (HARD dependency -- ships in v1.13.1 before 134): research-corpus.cjs fetchCorpus({source,query,limit}); research-cache.cjs shared cache. Plan 04a already added the pre-flight 130.5 assertion in the helper layer; rs-engine.cjs consumes the helpers, so it inherits the blocker.

rs-engine.cjs public surface (consumed by Plan 08 callers in-process):
- runInternal({ roomDir, sessionId, topk }) -> Promise<Result>
- runExternal({ topic, roomDir, ... }) -> Promise<Result>
- runHybrid({ topic, roomDir, topk }) -> Promise<Result>
- A thin CLI shim is OPTIONAL (the migration moves callers to require(); a CLI shim is only for backward-compat during the both-paths beta). The Result shape === the rs-engine.py payload (meta.mode + findings[] with the bridge-writer field names).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rs-engine.cjs 3-mode orchestrator with byte-preserved finding output + internal-mode parity</name>
  <files>lib/core/rs-engine.cjs, tests/test-rs-engine-cjs.cjs</files>
  <behavior>
    - runInternal({roomDir, topk}) on the fixture room produces the SAME ranked findings (source/target/signed_diff/abs_diff/direction) as the committed rs-engine.py golden fixture -- byte-identical ranked order OR within the Wave-0 drift bar.
    - The output object serializes to the .rs-engine-results.json shape bridge-writer.cjs reads (the exact field names from bridge-writer.cjs lines ~30-130).
    - runExternal + runHybrid resolve without spawning Python and without re-implementing the corpus fetch (they call the 04a helpers, which call 130.5 fetchCorpus).
    - The engine contains zero child_process / spawn / execFile / .py references.
  </behavior>
  <action>Implement lib/core/rs-engine.cjs as the in-process orchestrator. runInternal: read room corpus via rs-rooms.cjs, build the LSA topic matrix via rs-math.cjs buildLsaMatrix, compute abs_diff_topk + classify_direction, emit findings in the rs-engine.py payload shape (meta.mode='internal' + findings[] with bridge-writer field names), and write .rs-engine-results.json to the same path reverse-salient-agent.cjs reads (confirm the exact path in the agent, e.g. roomDir/.mindrian/.rs-engine-results.json). runExternal: fetch via the 04a rs-hybrid/rs-cache helpers (which use 130.5 fetchCorpus), embed via rs-embed.cjs, score. runHybrid: build the unified corpus via rs-hybrid.cjs buildUnifiedCorpus, embed, score, honor --topk. Preserve the direction-enum -> cascade-edge mapping bridge-writer.cjs depends on. Provide the runInternal/runExternal/runHybrid exports for Plan 08's in-process callers. Write tests/test-rs-engine-cjs.cjs asserting internal-mode parity vs the COMMITTED rs-engine.py golden fixture (Wave-0) -- the test reads the committed JSON fixture, it does NOT shell out to python3 -- and asserting the output shape matches the bridge-writer contract field-for-field.</action>
  <verify>
    <automated>node --test tests/test-rs-engine-cjs.cjs && node -e "require('./lib/core/rs-engine.cjs')"</automated>
  </verify>
  <done>rs-engine.cjs runs all 3 modes in-process; internal-mode findings match the committed rs-engine.py golden fixture per the Wave-0 bar; the .rs-engine-results.json shape is byte-preserved for bridge-writer.cjs; grep confirms zero child_process/.py in rs-engine.cjs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rs-engine.cjs -> 04a helpers -> 130.5 fetchCorpus -> external APIs | External corpus fetch crosses the egress boundary (Part 8 audit inherited transitively) |
| room corpus -> in-process embedder/LSA | LOCAL room text processed in-process; never egresses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-10 | Information Disclosure | external fetch query string leaks room content | mitigate | Inherited 130.5 Part 8 pre-egress audit (via the 04a helpers); rs-engine.cjs passes only the topic string, never artifact bodies. |
| T-134-11 | Repudiation | output-shape drift breaks bridge-writer cascade edges | mitigate | Test asserts the .rs-engine-results.json field names match bridge-writer.cjs verbatim. |
</threat_model>

<verification>
- `node --test tests/test-rs-engine-cjs.cjs` passes (internal parity vs committed fixture + output shape).
- `grep -nE "child_process|spawn|execFile|\\.py\\b" lib/core/rs-engine.cjs` returns nothing.
- `grep -nE "rs-rooms|rs-hybrid|rs-math|rs-embed" lib/core/rs-engine.cjs` matches (consumes the helpers + math + embedder).
</verification>

<success_criteria>
The reverse-salient engine orchestrator runs entirely in-process across all 3 modes, reuses the 04a helpers + 130.5 fetcher, and produces byte-compatible findings proven against the committed rs-engine.py golden fixture on the fixture room.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-04b-SUMMARY.md` when done.
</output>
