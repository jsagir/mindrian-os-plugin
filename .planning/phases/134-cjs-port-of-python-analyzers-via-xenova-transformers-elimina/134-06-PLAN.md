---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 06
type: execute
wave: 3
depends_on: ["134-04", "134-05"]
files_modified:
  - lib/agents/reverse-salient-agent.cjs
  - lib/core/rs-differential-scorer.cjs
  - scripts/discovery-cycle.cjs
  - scripts/auto-explore-fire.cjs
  - scripts/doctor.cjs
  - commands/find-bottlenecks.md
  - commands/whitespace.md
  - tests/test-134-windows-replay.cjs
  - tests/run-all-134.sh
autonomous: false
requirements: [CJS-134-MIGRATE, CJS-134-ENVFLAG, CJS-134-WINREPLAY, CJS-134-RETIRE-PREFLIGHT]
tags: [migration, env-flag, windows-replay, cutover, no-spawn]
user_setup: []

must_haves:
  truths:
    - "reverse-salient-agent.cjs requires rs-engine.cjs in-process -- no child_process spawn of rs-engine.py"
    - "The 5 Engine-1 command paths spawn zero child_process / .py when MINDRIAN_RS_ENGINE=cjs (default in v1.14.0-beta.1)"
    - "The env-flag MINDRIAN_RS_ENGINE=python falls back to the Python path for one beta (both paths selectable)"
    - "doctor.cjs --check-rs-engine is retired (no-op message or removed) because the Python-deps failure class no longer exists on the CJS path"
    - "A Windows-tester-replay test confirms zero Python errors across the full Engine 1 Act 1 workflow on the CJS path"
  artifacts:
    - path: "lib/agents/reverse-salient-agent.cjs"
      provides: "In-process rs-engine.cjs require (no spawn) on the CJS path; env-flag fallback to rs-engine.py"
      contains: "rs-engine.cjs"
    - path: "tests/test-134-windows-replay.cjs"
      provides: "The Windows-tester-replay acceptance: full Engine 1 Act 1 flow with zero Python on the CJS path"
      min_lines: 40
    - path: "tests/run-all-134.sh"
      provides: "Phase 134 aggregate test runner"
      min_lines: 10
  key_links:
    - from: "lib/agents/reverse-salient-agent.cjs"
      to: "lib/core/rs-engine.cjs"
      via: "in-process require (CJS path)"
      pattern: "require\\(.*rs-engine\\.cjs"
    - from: "all 5 Engine-1 command paths"
      to: "the env-flag MINDRIAN_RS_ENGINE"
      via: "cjs default / python fallback branch"
      pattern: "MINDRIAN_RS_ENGINE"
---

<objective>
Cut the live callers over from spawning Python to requiring the in-process CJS engine + HSI, behind the migration env-flag `MINDRIAN_RS_ENGINE` (cjs default in v1.14.0-beta.1; python fallback for one beta). Retire the `--check-rs-engine` pre-flight (the Python-deps failure class is gone on the CJS path). Prove the Windows-tester-replay acceptance: the full Engine 1 Act 1 workflow runs with zero Python errors.

Purpose: This is the cutover that delivers the phase's whole point -- eliminating Python from the user-machine surface that broke the Windows tester (the `ModuleNotFoundError: requests` RCA). Plans 02-05 built and proved the CJS substrate; this plan rewires the agents/scripts/commands that currently `child_process.spawn('python3', ['scripts/rs-engine.py', ...])` to call `require('./lib/core/rs-engine.cjs')` in-process. The env-flag ships BOTH paths for one beta so a regression can fall back without a re-release.
Output: rewired callers; env-flag; retired pre-flight; Windows-replay acceptance test; phase test runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md

<read_first>
- lib/agents/reverse-salient-agent.cjs (THE primary caller. Lines ~160-320: const { execFileSync } = require('node:child_process'); const py = process.env.MINDRIAN_PYTHON || 'python3'; const script = .../scripts/rs-engine.py; execFileSync(py, [script].concat(args), ...); then reads roomDir/.rs-engine-results.json. This is the spawn to replace with an in-process require of rs-engine.cjs on the cjs path. NOTE: detectAndSurface is currently SYNC (execFileSync) but rs-engine.cjs is async -- the migration must handle the sync->async shift; check every caller of detectAndSurface and make the call chain async, or provide a sync wrapper that awaits internally.)
- lib/core/rs-differential-scorer.cjs (lines ~90-210: spawnSync(PYTHON_BIN, ['-c', lsaBridgeScript]) -- the embedded pair-wise LSA bridge. Replace with rs-math.cjs's pair-wise cosine/LSA on the cjs path; keep the python bridge behind the env-flag fallback.)
- scripts/discovery-cycle.cjs (lines ~40-60 + ~245: spawns the 3 discover-*-whitespace.py via `python3 "${scriptPath}" "${roomDir}"`. Replace with hsi-whitespace.cjs discoverWhitespace on the cjs path.)
- scripts/auto-explore-fire.cjs (lines ~175-213: spawnAsync('python3', [rsScript, '--mode', 'hybrid', ...]) + reads .rs-engine-results.json. Replace the python spawn with rs-engine.cjs runHybrid on the cjs path.)
- scripts/doctor.cjs (lines ~145, ~182, ~261, ~3271-3295, ~3732: the --check-rs-engine pre-flight that probes Python deps + emits "Run: pip install -r requirements-hsi.txt --user". Retire it: on the cjs path, --check-rs-engine prints a no-op message that the Python surface is gone, OR is removed entirely per the CONTEXT acceptance sketch.)
- commands/find-bottlenecks.md (lines ~32, ~45-52: the `node -e "...reverse-salient-agent..."` invocation + the "missing Python deps (pip install -r requirements-hsi.txt)" error hint. Update the error hint -- on the cjs default the Python-deps failure class no longer exists; remove the pip hint, keep the agent-failure disambiguation.)
- commands/whitespace.md (the discover cycle invocation -- ensure it routes through the cjs discovery path.)
</read_first>

<interfaces>
The migration env-flag (LOCKED in 134-SPIKE.md Wave 0):
- MINDRIAN_RS_ENGINE = 'cjs' (DEFAULT in v1.14.0-beta.1) | 'python' (fallback, one beta).
- The existing MINDRIAN_PYTHON env var stays as the python-bin override for the fallback path only.
- Rollout: beta.1 default cjs + python fallback; beta.2 deprecate python (warn on use); beta.3 remove python + delete scripts/rs-engine.py + the rs_*.py siblings + requirements-hsi.txt (NOT in this plan -- this plan ships beta.1 both-paths; the beta.3 removal is a follow-on).

In-process surfaces this plan wires (from Plans 04 + 05):
- rs-engine.cjs runInternal / runExternal / runHybrid
- hsi-whitespace.cjs discoverWhitespace ; hsi-compute.cjs computeHsi
- rs-math.cjs pair-wise cosine/LSA (for rs-differential-scorer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire callers in-process behind MINDRIAN_RS_ENGINE (cjs default, python fallback)</name>
  <files>lib/agents/reverse-salient-agent.cjs, lib/core/rs-differential-scorer.cjs, scripts/discovery-cycle.cjs, scripts/auto-explore-fire.cjs, commands/find-bottlenecks.md, commands/whitespace.md</files>
  <action>Add the MINDRIAN_RS_ENGINE branch to each caller. cjs path (default): reverse-salient-agent.cjs requires rs-engine.cjs and calls runInternal/runHybrid in-process instead of execFileSync(python3, rs-engine.py) -- handle the sync->async shift (make detectAndSurface async and update its callers: tension-hook-agent.cjs, doctor.cjs, find-bottlenecks.md's node -e, the test harnesses; or provide an async-internally sync facade). rs-differential-scorer.cjs uses rs-math.cjs pair-wise scoring instead of spawnSync python lsaBridge. discovery-cycle.cjs calls hsi-whitespace.cjs discoverWhitespace instead of spawning the 3 discover-*.py. auto-explore-fire.cjs calls rs-engine.cjs runHybrid instead of spawning rs-engine.py. python path (MINDRIAN_RS_ENGINE=python): the existing spawn code stays as the fallback branch. Update commands/find-bottlenecks.md + commands/whitespace.md: remove the "pip install -r requirements-hsi.txt" fix hint on the default cjs path (the failure class is gone), keep the agent-failure disambiguation. Reference D-flag MINDRIAN_RS_ENGINE per 134-SPIKE.md.</action>
  <verify>
    <automated>MINDRIAN_RS_ENGINE=cjs node -e "const a=require('./lib/agents/reverse-salient-agent.cjs'); process.exit(typeof a.detectAndSurface==='function'?0:1)" && grep -q "rs-engine.cjs" lib/agents/reverse-salient-agent.cjs && grep -q "MINDRIAN_RS_ENGINE" lib/agents/reverse-salient-agent.cjs lib/core/rs-differential-scorer.cjs scripts/discovery-cycle.cjs scripts/auto-explore-fire.cjs</automated>
  </verify>
  <done>All 4 callers branch on MINDRIAN_RS_ENGINE; the cjs default requires the in-process engine/HSI (no spawn); the python fallback branch is preserved; the command error hints no longer point to pip on the cjs path.</done>
</task>

<task type="auto">
  <name>Task 2: Retire --check-rs-engine + Windows-replay acceptance + phase test runner</name>
  <files>scripts/doctor.cjs, tests/test-134-windows-replay.cjs, tests/run-all-134.sh</files>
  <action>Retire the --check-rs-engine pre-flight in doctor.cjs: on the cjs default, make it print a no-op message ("Engine 1 Act 1 now runs in-process via @huggingface/transformers -- no Python deps required") instead of probing python imports + emitting the pip-install fix line. Keep the probe ONLY behind MINDRIAN_RS_ENGINE=python (the fallback path still needs it). Write tests/test-134-windows-replay.cjs: the Windows-tester-replay acceptance -- simulate the Aryeh machine class (no Python deps / python3 absent OR MINDRIAN_RS_ENGINE=cjs forced) and run the full Engine 1 Act 1 flow: detectAndSurface (find-bottlenecks), discoverWhitespace (whitespace discover), computeHsi (score-innovation). Assert: zero child_process spawn of python in the cjs path (intercept/spy on child_process.spawn/execFileSync to fail the test if a .py is spawned), the flow produces findings, and no ModuleNotFoundError-class error surfaces. This is the structural proof the RCA failure class is eliminated. Write tests/run-all-134.sh aggregating test-rs-math-cjs + test-rs-embed-cjs + test-rs-engine-cjs + test-hsi-cjs-parity + test-134-windows-replay (mirror tests/run-all-124.sh structure). Register it in the test aggregator per the project convention.</action>
  <verify>
    <automated>MINDRIAN_RS_ENGINE=cjs node --test tests/test-134-windows-replay.cjs && bash tests/run-all-134.sh</automated>
  </verify>
  <done>--check-rs-engine no longer emits the pip hint on the cjs path; the Windows-replay test proves zero Python spawn + no ModuleNotFoundError across the full Engine 1 Act 1 flow; run-all-134.sh aggregates the phase tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Engine 1 Act 1 surface (the 5 commands: /mos:find-bottlenecks, /mos:whitespace, /mos:find-connections, /mos:find-analogies, /mos:score-innovation) now runs in-process via @huggingface/transformers on the default MINDRIAN_RS_ENGINE=cjs path. The Windows-replay test proves zero Python spawn. The python fallback is one env-flag away for one beta.</what-built>
  <how-to-verify>
    1. Run `MINDRIAN_RS_ENGINE=cjs bash tests/run-all-134.sh` -- all phase tests green (including the HSI parity gate + Windows-replay).
    2. Run `/mos:find-bottlenecks` on a real room with python3 UNINSTALLED or PATH-masked -- confirm it produces findings with no Python error (this is the actual RCA replay).
    3. Run `/mos:whitespace discover` and `/mos:score-innovation` on the same room -- confirm in-process completion, model cold-load within the Wave-0 budget on first run.
    4. Confirm `MINDRIAN_RS_ENGINE=python /mos:find-bottlenecks` still works as the fallback (with python deps present).
    5. Confirm the model-download first-run UX (progress + size) behaves per the Wave-0 decision and the cold load fits the hook budget.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the Windows-fragility class is eliminated on the cjs path, or describe regressions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> engine | The spawn boundary (child_process -> python) is removed on the cjs path; replaced by in-process require |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-16 | Denial of Service | sync->async shift in detectAndSurface breaks a synchronous caller | mitigate | Audit every detectAndSurface caller (tension-hook-agent, doctor, find-bottlenecks node -e, tests); make the chain async or provide a facade; covered by run-all-134.sh. |
| T-134-17 | Tampering | a residual python spawn slips through on the cjs path | mitigate | The Windows-replay test spies on child_process and FAILS if any .py is spawned on the cjs path -- structural proof, not promise. |
| T-134-18 | Denial of Service | first-run model cold-load overruns the hook timeout in production | mitigate | Human-verify checkpoint step 5 confirms cold-load fits the Wave-0 budget on a real room; ensureModel() warm-up off the hot path if needed. |
| T-134-19 | Repudiation | removing the pip hint hides a real fallback failure | accept | The pip hint is retained behind MINDRIAN_RS_ENGINE=python (the only path that still needs Python). |
</threat_model>

<verification>
- `MINDRIAN_RS_ENGINE=cjs bash tests/run-all-134.sh` -- all phase tests pass (rs-math, rs-embed, rs-engine, HSI parity, Windows-replay).
- `grep -rnE "spawn|execFileSync|execSync" lib/agents/reverse-salient-agent.cjs | grep -iE "python|rs-engine\\.py"` returns nothing on the cjs default branch (python spawn lives only behind the env-flag fallback).
- `grep -q "MINDRIAN_RS_ENGINE" lib/agents/reverse-salient-agent.cjs lib/core/rs-differential-scorer.cjs scripts/discovery-cycle.cjs scripts/auto-explore-fire.cjs scripts/doctor.cjs` matches all.
- Human-verify checkpoint approved (real-room RCA replay with python3 absent).
</verification>

<success_criteria>
The 5 Engine-1 commands run in-process with zero Python on the default cjs path; the env-flag gives a one-beta python fallback; --check-rs-engine is retired; the Windows-tester-replay acceptance proves the install-fragility class is eliminated.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-06-SUMMARY.md` when done.
</output>
