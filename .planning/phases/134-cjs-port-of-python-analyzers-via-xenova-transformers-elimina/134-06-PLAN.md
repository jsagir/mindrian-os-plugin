---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 06
type: execute
wave: 3
depends_on: ["134-03", "130.5"]
files_modified:
  - lib/core/whitespace-math.cjs
  - lib/core/brain-baseline.cjs
  - tests/test-whitespace-cjs-parity.cjs
autonomous: true
requirements: [CJS-134-WS-PORT, CJS-134-WSMATH, CJS-134-WS-PARITY, CJS-134-MODEL-UNIFY]
tags: [whitespace, umap, kde, brain-baseline, parity]
user_setup: []

must_haves:
  truths:
    - "whitespace-math.cjs replaces compute-whitespace-embeddings.py + compute-whitespace-gaps.py + compute-external-whitespace.py in-process via rs-embed.cjs + the Wave-0 whitespace-math strategy (UMAP/PCA reduce + gaussian KDE + NearestNeighbors)"
    - "brain-baseline.cjs replaces fetch-brain-baseline.py: it embeds Brain framework DESCRIPTIONS (generic methodology, not user data) via rs-embed.cjs into brain-baseline.json"
    - "external whitespace fetches via the Phase 130.5 research-corpus.cjs (no re-ported fetcher)"
    - "The CJS whitespace produces the SAME ranked gaps/novelty as the Python on the COMMITTED fixture (byte-identical OR documented drift) -- the WHITESPACE PARITY GATE against committed fixtures"
  artifacts:
    - path: "lib/core/whitespace-math.cjs"
      provides: "In-process whitespace embeddings + gap/novelty detection (replaces 3 compute-whitespace-*.py) using the Wave-0 UMAP/KDE strategy"
      min_lines: 120
    - path: "lib/core/brain-baseline.cjs"
      provides: "In-process Brain-baseline embedder (replaces fetch-brain-baseline.py)"
      min_lines: 50
    - path: "tests/test-whitespace-cjs-parity.cjs"
      provides: "The whitespace parity gate against committed Python golden fixtures"
      min_lines: 45
  key_links:
    - from: "lib/core/whitespace-math.cjs"
      to: "lib/core/rs-embed.cjs + rs-math.cjs (PCA via powerIteration) + the Wave-0 UMAP/KDE strategy"
      via: "in-process require"
      pattern: "rs-embed|umap|kde|KernelDensity"
    - from: "lib/core/whitespace-math.cjs"
      to: "lib/core/research-corpus.cjs (Phase 130.5)"
      via: "require fetchCorpus for external whitespace"
      pattern: "research-corpus|fetchCorpus"
---

<objective>
Port the Family-B whitespace surface to in-process CJS -- the half the original plan set never touched:
- `scripts/compute-whitespace-embeddings.py` (BAAI/llm-embedder 768-dim) + `scripts/compute-whitespace-gaps.py` (UMAP reduce + gaussian KernelDensity + NearestNeighbors + PCA) + `scripts/compute-external-whitespace.py` -> `lib/core/whitespace-math.cjs`,
- `scripts/fetch-brain-baseline.py` -> `lib/core/brain-baseline.cjs`,
all in-process via `rs-embed.cjs` (Plan 03, the unified embedder) + the Wave-0 whitespace-math strategy + the Phase 130.5 fetcher.

Purpose: This is the `/mos:whitespace` non-discover surface that `whitespace-command.cjs` spawns (`compute-whitespace-embeddings.py`, `compute-whitespace-gaps.py`, `compute-external-whitespace.py`) PLUS the brain-baseline embedder that `ensure-brain-baseline.cjs` spawns (and which discovery-cycle / whitespace-command / auto-explore-fire all depend on). Every one of these imports `ensure_ml_deps.py` and auto-pip-installs sentence-transformers + sklearn -- they ARE the install-fragility class. They carry the heavier UMAP/KDE/PCA math (vs the rs path's LSA-SVD); the Wave-0 spike locked the CJS strategy for each. The model-unify disposition decides whether the 768-dim embeddings collapse onto the unified e5-large 1024-dim with re-calibrated thresholds.
Output: `whitespace-math.cjs` + `brain-baseline.cjs` + the committed-fixture whitespace parity gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md
@.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-CONTEXT.md

<read_first>
- scripts/compute-whitespace-embeddings.py (BAAI/llm-embedder 768-dim primary, all-MiniLM-L6-v2 384-dim fallback; the embedding cache check on content-hash + model-name; output whitespace-embeddings.json with embeddings[].vector. Unify onto rs-embed.cjs per the Wave-0 model-unify disposition.)
- scripts/compute-whitespace-gaps.py (the heaviest math: load_embeddings + load_baselines, umap_reduce to 15-dim density + 2-dim viz, KernelDensity(kernel='gaussian'), NearestNeighbors knn, PCA fallback when UMAP unavailable; output whitespace-results.json with gaps + novelty_scores + umap_2d. Use the Wave-0 UMAP/KDE strategy -- umap-js if audited clean, else the PCA-only path this script already falls back to, else hand-rolled per the SPIKE.)
- scripts/compute-external-whitespace.py (external corpus whitespace; sentence-transformers + sklearn. External docs via 130.5 fetchCorpus, embeddings via rs-embed.cjs.)
- scripts/fetch-brain-baseline.py (embeds Brain framework DESCRIPTIONS -- generic methodology text, Part 8 allowed -- with the same model as room artifacts; output brain-baseline.json. Per the model-unify disposition this embeds via rs-embed.cjs e5-large.)
- scripts/ensure-brain-baseline.cjs (the CJS caller that spawns fetch-brain-baseline.py at line ~88; Plan 08 rewires it to call brain-baseline.cjs. fetch-brain-baseline.cjs (the existing CJS half) produces brain-data.json from the Brain MCP; brain-baseline.cjs consumes THAT and embeds it -- do NOT touch the Brain MCP query, only the local embedding step.)
- scripts/whitespace-command.cjs (lines 132-166, 384, 600-604: the runScript python3 spawns Plan 08 rewires onto whitespace-math.cjs.)
- scripts/lib/ensure_ml_deps.py (the auto-pip trigger this port eliminates -- no CJS equivalent; npm @huggingface/transformers replaces it).
</read_first>

<interfaces>
Wave-0 LOCKED decisions this plan consumes (134-SPIKE.md):
- The whitespace-math strategy: UMAP replacement (umap-js / PCA-only / hand-rolled), KDE (hand-rolled gaussian vs lib), NearestNeighbors approach. Build against whatever was locked.
- The model-unify disposition + the Family-B threshold-recalibration note (1024-dim e5 vs the Python's 768-dim BAAI thresholds for gap/novelty cutoffs).

The CJS whitespace surface (consumed by Plan 08 callers):
- computeWhitespaceEmbeddings({ roomDir }) -> writes whitespace-embeddings.json (via rs-embed.cjs)
- computeWhitespaceGaps({ roomDir, umapDim, kdeBandwidth, knnK }) -> writes whitespace-results.json (gaps + novelty + viz)
- computeExternalWhitespace({ roomDir, topic }) -> external whitespace (130.5 fetchCorpus + rs-embed)
- brain-baseline.cjs: embedBrainBaseline({ roomDir, brainDataPath }) -> writes brain-baseline.json (via rs-embed.cjs)
- All via rs-embed.cjs + rs-math.cjs (PCA via the exported powerIteration). Zero Python.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: whitespace-math.cjs (embeddings + gaps via Wave-0 UMAP/KDE) + brain-baseline.cjs</name>
  <files>lib/core/whitespace-math.cjs, lib/core/brain-baseline.cjs, tests/test-whitespace-cjs-parity.cjs</files>
  <behavior>
    - computeWhitespaceEmbeddings writes whitespace-embeddings.json with the same shape (embeddings[].vector + content-hash + model cache fields) the Python wrote, vectors from rs-embed.cjs.
    - computeWhitespaceGaps reduces via the Wave-0 strategy (UMAP-js or PCA-only), fits a gaussian KDE, runs knn, and writes whitespace-results.json with gaps + novelty_scores + viz matching the Python shape within the Wave-0 drift bar.
    - brain-baseline.cjs embedBrainBaseline reads brain-data.json (from the existing fetch-brain-baseline.cjs Brain MCP half) and writes brain-baseline.json with e5-large vectors; it embeds GENERIC framework descriptions only (Part 8 allowed).
    - A pre-flight HARD-FAILS if research-corpus.cjs is absent (for the external-whitespace path).
    - Zero Python / child_process in both modules.
  </behavior>
  <action>Implement lib/core/whitespace-math.cjs. computeWhitespaceEmbeddings: embed room artifacts via rs-embed.cjs (replacing BAAI/MiniLM per the Wave-0 model-unify disposition), preserve the content-hash + model-name cache check + the whitespace-embeddings.json output shape. computeWhitespaceGaps: port compute-whitespace-gaps.py using the Wave-0 dimensionality-reduction strategy (umap-js if it passed the vendoring audit, else the PCA-only fallback path the Python already implements via rs-math.cjs's exported powerIteration, else hand-rolled per the SPIKE), the gaussian KernelDensity (hand-rolled ~40-line gaussian kernel unless a lib was approved), and the NearestNeighbors knn; re-calibrate the gap/novelty thresholds for 1024-dim per the SPIKE note; write whitespace-results.json in the Python shape. computeExternalWhitespace: fetch external docs via 130.5 research-corpus.cjs fetchCorpus (add the same pre-flight 130.5 assertion as Plan 04a), embed via rs-embed.cjs, score. Implement lib/core/brain-baseline.cjs embedBrainBaseline: read brain-data.json, embed the framework descriptions via rs-embed.cjs, write brain-baseline.json (Per D-06/D-07/D-11 in fetch-brain-baseline.py: cache path, refresh-on-brain-version-hash, include model version + timestamp). Both modules: zero Python, zero child_process. Write tests/test-whitespace-cjs-parity.cjs asserting embeddings + gaps + brain-baseline against the COMMITTED Wave-0 Python golden fixtures (the default + only parity path; not a python3-at-test-time skip).</action>
  <verify>
    <automated>node -e "require('./lib/core/whitespace-math.cjs');require('./lib/core/brain-baseline.cjs')" && node --test tests/test-whitespace-cjs-parity.cjs</automated>
  </verify>
  <done>whitespace-math.cjs computes embeddings + gaps + external whitespace in-process via the Wave-0 UMAP/KDE strategy; brain-baseline.cjs embeds the Brain baseline via rs-embed.cjs; the whitespace parity gate passes against committed fixtures within the Wave-0 drift bar; the 130.5 pre-flight blocker is asserted; grep confirms zero child_process/.py in both modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whitespace-math.cjs -> 130.5 fetchCorpus -> external APIs | External whitespace corpus fetch (Part 8 audit inherited from 130.5) |
| room corpus -> in-process embedder | LOCAL room artifacts embedded in-process; never egress |
| Brain framework descriptions -> embedder | brain-baseline embeds GENERIC methodology (not user data) -- Part 8 allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-22 | Tampering | UMAP/KDE numerical drift vs sklearn/umap-learn | mitigate | The whitespace parity gate runs against committed golden fixtures; drift beyond the Wave-0 bar fails the gate. Threshold recalibration for 1024-dim is locked in the SPIKE. |
| T-134-23 | Information Disclosure | brain-baseline accidentally embeds user data instead of generic descriptions | mitigate | brain-baseline.cjs reads ONLY brain-data.json (generic framework descriptions from the Brain MCP half); a test asserts the input contains no room-artifact bodies. Canon Part 8. |
| T-134-24 | Information Disclosure | external-whitespace query leaks room content | mitigate | Inherited 130.5 Part 8 pre-egress audit; only the topic string is passed. |
</threat_model>

<verification>
- `node --test tests/test-whitespace-cjs-parity.cjs` passes against committed fixtures (does NOT skip when python3 absent).
- `grep -nE "child_process|spawn|execFile|\\.py\\b|sentence_transformers|BAAI" lib/core/whitespace-math.cjs lib/core/brain-baseline.cjs` returns nothing.
- `grep -q "research-corpus\|fetchCorpus" lib/core/whitespace-math.cjs` matches (130.5 reuse for external whitespace).
</verification>

<success_criteria>
The Family-B whitespace surface (embeddings + gaps + external + brain-baseline) runs entirely in-process via the unified embedder + the Wave-0 UMAP/KDE strategy, reuses the 130.5 fetcher, and passes the whitespace parity gate against committed Python golden fixtures.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-06-SUMMARY.md` when done.
</output>
