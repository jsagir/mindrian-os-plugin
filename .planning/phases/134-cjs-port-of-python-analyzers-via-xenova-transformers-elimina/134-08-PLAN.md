---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 08
type: execute
wave: 4
depends_on: ["134-04a", "134-04b", "134-05", "134-06", "134-07"]
files_modified:
  - lib/core/intelligence-cascade.cjs
  - lib/agents/reverse-salient-agent.cjs
  - lib/core/rs-differential-scorer.cjs
  - scripts/discovery-cycle.cjs
  - scripts/auto-explore-fire.cjs
  - scripts/whitespace-command.cjs
  - scripts/diagnostics-command.cjs
  - scripts/ensure-brain-baseline.cjs
  - scripts/doctor.cjs
  - commands/find-bottlenecks.md
  - commands/whitespace.md
  - commands/diagnostics.md
  - tests/test-134-windows-replay.cjs
  - tests/run-all-134.sh
autonomous: false
requirements: [CJS-134-MIGRATE, CJS-134-CASCADE, CJS-134-ENVFLAG, CJS-134-WINREPLAY, CJS-134-RETIRE-PREFLIGHT]
tags: [migration, env-flag, windows-replay, cutover, no-spawn, full-surface]
user_setup: []

must_haves:
  truths:
    - "intelligence-cascade.cjs auto-fire requires hsi-compute.cjs + reverse-salients.cjs in-process -- no execSync python3 of compute-hsi.py / detect-reverse-salients.py (the RCA surface, BLOCKER-1)"
    - "reverse-salient-agent.cjs requires rs-engine.cjs in-process -- no child_process spawn of rs-engine.py"
    - "ALL Engine-1 + auto-fire command paths spawn zero analyzer Python when MINDRIAN_RS_ENGINE=cjs (default in v1.14.0-beta.1) -- across the FULL surface (rs + hsi + whitespace + fingerprint + brain-baseline + cascade)"
    - "The env-flag MINDRIAN_RS_ENGINE=python falls back to the Python path for one beta (both paths selectable)"
    - "doctor.cjs --check-rs-engine + the check-hsi-deps gate are retired on the cjs path (the Python-deps failure class no longer exists there)"
    - "A Windows-tester-replay test confirms zero analyzer-Python spawn across the FULL Engine 1 Act 1 workflow + the post-write auto-fire on the CJS path"
  artifacts:
    - path: "lib/core/intelligence-cascade.cjs"
      provides: "Auto-fire HSI + reverse-salient in-process (no python3 execSync) on the cjs path; env-flag fallback"
      contains: "hsi-compute"
    - path: "lib/agents/reverse-salient-agent.cjs"
      provides: "In-process rs-engine.cjs require (no spawn) on the cjs path; env-flag fallback to rs-engine.py"
      contains: "rs-engine.cjs"
    - path: "tests/test-134-windows-replay.cjs"
      provides: "The full-surface Windows-tester-replay acceptance: zero analyzer-Python spawn across rs + hsi + whitespace + fingerprint + cascade"
      min_lines: 60
    - path: "tests/run-all-134.sh"
      provides: "Phase 134 aggregate test runner (all parity gates + windows-replay)"
      min_lines: 12
  key_links:
    - from: "lib/core/intelligence-cascade.cjs"
      to: "lib/core/hsi-compute.cjs + lib/core/reverse-salients.cjs"
      via: "in-process require (cjs path)"
      pattern: "hsi-compute|reverse-salients"
    - from: "all caller paths"
      to: "the env-flag MINDRIAN_RS_ENGINE"
      via: "cjs default / python fallback branch"
      pattern: "MINDRIAN_RS_ENGINE"
---

<objective>
Cut EVERY live caller over from spawning Python to requiring the in-process CJS modules, behind the migration env-flag `MINDRIAN_RS_ENGINE` (cjs default in v1.14.0-beta.1; python fallback for one beta). This is the cutover across the FULL user-machine analyzer surface -- including the auto-fire RCA surface (BLOCKER-1) the original plan set missed. Retire the `--check-rs-engine` pre-flight + the `check-hsi-deps` gate on the cjs path. Prove the Windows-tester-replay acceptance across the full surface.

Purpose: This delivers the phase's whole point -- eliminating the analyzer-Python class from the user machine that broke the Windows tester (`ModuleNotFoundError: requests`). Plans 02-07 built and proved the CJS substrate for BOTH math families; this plan rewires every caller that spawns analyzer Python:
- intelligence-cascade.cjs (post-write auto-fire: compute-hsi.py + detect-reverse-salients.py) -- THE RCA surface, BLOCKER-1
- reverse-salient-agent.cjs (rs-engine.py)
- rs-differential-scorer.cjs (python3 -c LSA/pinecone bridge -> rs-cache.cjs in-process)
- discovery-cycle.cjs (3 discover-*-whitespace.py -> hsi-whitespace.cjs)
- auto-explore-fire.cjs (rs-engine.py --mode hybrid -> rs-engine.cjs runHybrid)
- whitespace-command.cjs (4 compute-whitespace-*.py + compute_topic_forest.py -> whitespace-math.cjs + topic-forest.cjs)
- diagnostics-command.cjs (4 compute-{disruption,blindspot,element,bayesian}.py -> fingerprint-math.cjs)
- ensure-brain-baseline.cjs (fetch-brain-baseline.py -> brain-baseline.cjs)
- doctor.cjs (--check-rs-engine retire)
The env-flag ships BOTH paths for one beta so a regression can fall back without a re-release.
Output: ALL callers rewired; env-flag; retired pre-flights; full-surface Windows-replay acceptance; phase test runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SOURCE-AUDIT.md

<read_first>
- lib/core/intelligence-cascade.cjs (lines ~309-345: the post-write auto-fire. execSync(bash check-hsi-deps) gate, THEN execSync(python3 compute-hsi.py), THEN execSync(python3 detect-reverse-salients.py), THEN node hsi-to-graph.cjs. THIS is the EXACT surface that produced the Windows ModuleNotFoundError RCA -- BLOCKER-1. On the cjs path: require hsi-compute.cjs.computeHsi + reverse-salients.cjs.detectReverseSalients in-process; drop the check-hsi-deps gate (no python deps); keep hsi-to-graph.cjs (already node). Handle the sync->async shift: these modules are async; make the cascade step async / await within the existing structure -- audit every caller of the cascade step. Mind the post-write hot-path cold-load budget -- ensureModel() should already be warmed off-path per Plan 03; do not pay cold load inline.)
- lib/agents/reverse-salient-agent.cjs (lines ~160-320: execFileSync(python3, rs-engine.py); reads .rs-engine-results.json. detectAndSurface is SYNC but rs-engine.cjs is async -- make detectAndSurface async and update its callers (tension-hook-agent.cjs, doctor.cjs, find-bottlenecks.md node -e, test harnesses), or provide an async-internally facade.)
- lib/core/rs-differential-scorer.cjs (lines ~90-210: spawnSync(PYTHON_BIN, ['-c', lsaBridge]) via rs-pinecone-bridge. Replace with rs-cache.cjs fetchNamespaceVectors + rs-math.cjs pair-wise cosine/LSA on the cjs path; keep the python bridge behind the env-flag fallback.)
- scripts/discovery-cycle.cjs (lines ~40-60 + ~245: spawns the 3 discover-*-whitespace.py. Replace with hsi-whitespace.cjs discoverWhitespace.)
- scripts/auto-explore-fire.cjs (lines ~175-213: spawnAsync(python3, [rsScript, '--mode', 'hybrid', ...]). Replace with rs-engine.cjs runHybrid.)
- scripts/whitespace-command.cjs (lines 132-166, 376-384, 600-604: runScript python3 compute-whitespace-embeddings/gaps/external + compute_topic_forest. Replace with whitespace-math.cjs + topic-forest.cjs.)
- scripts/diagnostics-command.cjs (lines 109, 193-196: execSync python3 the 4 compute-*.py. Replace with fingerprint-math.cjs. Honor the /mos:diagnostics -> /mos:fingerprint rename if it lands this milestone -- keep the output contract stable either way.)
- scripts/ensure-brain-baseline.cjs (line ~88: runCmd python3 fetch-brain-baseline.py. Replace with brain-baseline.cjs embedBrainBaseline; leave the Brain MCP query half (fetch-brain-baseline.cjs) untouched.)
- scripts/doctor.cjs (lines ~145, ~182, ~261, ~3271-3295, ~3732-3799: --check-rs-engine probes python deps + emits pip-install hints. Retire on the cjs path (no-op message); keep behind MINDRIAN_RS_ENGINE=python.)
- commands/find-bottlenecks.md, commands/whitespace.md, commands/diagnostics.md (pip-install error hints to remove on the cjs path).
- tests/run-all-124.sh (the aggregate-runner structure to mirror for run-all-134.sh).
</read_first>

<interfaces>
The migration env-flag (LOCKED in 134-SPIKE.md Wave 0):
- MINDRIAN_RS_ENGINE = 'cjs' (DEFAULT in v1.14.0-beta.1) | 'python' (fallback, one beta).
- The existing MINDRIAN_PYTHON env var stays as the python-bin override for the fallback path only.
- Rollout: beta.1 default cjs + python fallback; beta.2 deprecate python (warn on use); beta.3 DELETE the full python surface (rs-engine.py + compute-hsi.py + detect-reverse-salients.py + discover-*.py + compute-whitespace-*.py + compute_topic_forest.py + the diagnostics compute-*.py + fetch-brain-baseline.py + the rs_*.py siblings + scripts/lib/ensure_ml_deps.py + requirements-hsi.txt). The beta.3 deletion is a FOLLOW-ON, NOT this plan (this plan ships beta.1 both-paths).

In-process surfaces this plan wires (from Plans 04a/04b/05/06/07):
- rs-engine.cjs runInternal / runExternal / runHybrid
- hsi-compute.cjs computeHsi ; reverse-salients.cjs detectReverseSalients
- hsi-whitespace.cjs discoverWhitespace
- whitespace-math.cjs computeWhitespaceEmbeddings/Gaps/External ; topic-forest.cjs buildTopicForest
- fingerprint-math.cjs computeDisruptionIndex/BlindspotMass/ElementNovelty/BayesianSurprise
- brain-baseline.cjs embedBrainBaseline
- rs-cache.cjs fetchNamespaceVectors + rs-math.cjs pair-wise cosine/LSA (for rs-differential-scorer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the auto-fire RCA surface (BLOCKER-1) + the rs callers behind MINDRIAN_RS_ENGINE</name>
  <files>lib/core/intelligence-cascade.cjs, lib/agents/reverse-salient-agent.cjs, lib/core/rs-differential-scorer.cjs, scripts/discovery-cycle.cjs, scripts/auto-explore-fire.cjs</files>
  <action>Add the MINDRIAN_RS_ENGINE branch to each caller; cjs is the default. intelligence-cascade.cjs (BLOCKER-1): on the cjs path, replace the execSync(bash check-hsi-deps) + execSync(python3 compute-hsi.py) + execSync(python3 detect-reverse-salients.py) sequence with in-process `await computeHsi({roomDir})` then `await detectReverseSalients({roomDir})`; drop the check-hsi-deps gate on the cjs path; keep hsi-to-graph.cjs (already node). Handle the sync->async shift in the cascade step (make it async / await within the existing flow; audit callers of the cascade step). reverse-salient-agent.cjs: require rs-engine.cjs and call runInternal/runHybrid in-process instead of execFileSync(python3, rs-engine.py); make detectAndSurface async and update its callers (tension-hook-agent, doctor, find-bottlenecks node -e, test harnesses) or provide an async-internally facade. rs-differential-scorer.cjs: use rs-cache.cjs fetchNamespaceVectors + rs-math.cjs pair-wise scoring instead of spawnSync python lsaBridge. discovery-cycle.cjs: call hsi-whitespace.cjs discoverWhitespace instead of spawning the 3 discover-*.py. auto-explore-fire.cjs: call rs-engine.cjs runHybrid instead of spawning rs-engine.py. For EACH: the python path (MINDRIAN_RS_ENGINE=python) keeps the existing spawn code as the fallback branch. Reference the env-flag per 134-SPIKE.md.</action>
  <verify>
    <automated>MINDRIAN_RS_ENGINE=cjs node -e "const a=require('./lib/agents/reverse-salient-agent.cjs'); process.exit(typeof a.detectAndSurface==='function'?0:1)" && grep -q "hsi-compute" lib/core/intelligence-cascade.cjs && grep -q "reverse-salients" lib/core/intelligence-cascade.cjs && grep -q "rs-engine.cjs" lib/agents/reverse-salient-agent.cjs && grep -q "MINDRIAN_RS_ENGINE" lib/core/intelligence-cascade.cjs lib/agents/reverse-salient-agent.cjs lib/core/rs-differential-scorer.cjs scripts/discovery-cycle.cjs scripts/auto-explore-fire.cjs</automated>
  </verify>
  <done>The auto-fire (intelligence-cascade) + the 4 rs callers branch on MINDRIAN_RS_ENGINE; the cjs default requires the in-process engine/HSI/reverse-salients (no spawn); the python fallback branch is preserved; the check-hsi-deps gate is dropped on the cjs path.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the whitespace + fingerprint + brain-baseline command callers; retire the pre-flights</name>
  <files>scripts/whitespace-command.cjs, scripts/diagnostics-command.cjs, scripts/ensure-brain-baseline.cjs, scripts/doctor.cjs, commands/find-bottlenecks.md, commands/whitespace.md, commands/diagnostics.md</files>
  <action>Add the MINDRIAN_RS_ENGINE branch (cjs default) to each command caller. whitespace-command.cjs: replace the python3 compute-whitespace-embeddings/gaps/external spawns with whitespace-math.cjs calls, and the python3 compute_topic_forest spawn with topic-forest.cjs buildTopicForest. diagnostics-command.cjs: replace the 4 execSync python3 compute-*.py with fingerprint-math.cjs calls; preserve the interpretation-threshold contract; honor the /mos:fingerprint rename if it lands (keep output stable). ensure-brain-baseline.cjs: replace runCmd(python3 fetch-brain-baseline.py) with brain-baseline.cjs embedBrainBaseline (leave the Brain MCP query half untouched). doctor.cjs: retire --check-rs-engine on the cjs path -- print a no-op message ("Engine 1 Act 1 now runs in-process via @huggingface/transformers -- no Python deps required") instead of probing python imports + the pip-install line; keep the probe ONLY behind MINDRIAN_RS_ENGINE=python. Update commands/find-bottlenecks.md + commands/whitespace.md + commands/diagnostics.md: remove the "pip install -r requirements-hsi.txt" fix hints on the cjs default path (the failure class is gone), keep the agent-failure disambiguation. For each caller: the python path keeps the existing spawn as the fallback branch.</action>
  <verify>
    <automated>grep -q "whitespace-math\|topic-forest" scripts/whitespace-command.cjs && grep -q "fingerprint-math" scripts/diagnostics-command.cjs && grep -q "brain-baseline" scripts/ensure-brain-baseline.cjs && grep -q "MINDRIAN_RS_ENGINE" scripts/whitespace-command.cjs scripts/diagnostics-command.cjs scripts/ensure-brain-baseline.cjs scripts/doctor.cjs && ! grep -q "requirements-hsi.txt" commands/find-bottlenecks.md commands/whitespace.md commands/diagnostics.md</automated>
  </verify>
  <done>The whitespace/fingerprint/brain-baseline command callers branch on MINDRIAN_RS_ENGINE; the cjs default requires the in-process modules (no spawn); --check-rs-engine + the pip hints are retired on the cjs path (kept behind =python); the python fallback branch is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Full-surface Windows-replay acceptance + phase test runner</name>
  <files>tests/test-134-windows-replay.cjs, tests/run-all-134.sh</files>
  <action>Write tests/test-134-windows-replay.cjs: the Windows-tester-replay acceptance simulating the Aryeh machine class (python3 absent OR MINDRIAN_RS_ENGINE=cjs forced) and running the FULL Engine 1 Act 1 + auto-fire surface: (1) the post-write auto-fire path (intelligence-cascade computeHsi + detectReverseSalients), (2) detectAndSurface (find-bottlenecks), (3) discoverWhitespace (whitespace discover), (4) computeWhitespaceGaps + buildTopicForest (whitespace non-discover), (5) the 4 fingerprint metrics (score-innovation / diagnostics), (6) brain-baseline embedding. Assert: ZERO child_process spawn of python in the cjs path -- intercept/spy on child_process.spawn / spawnSync / execSync / execFileSync and FAIL the test if ANY analyzer .py (rs-engine / compute-hsi / detect-reverse-salients / discover-* / compute-whitespace-* / compute_topic_forest / compute-disruption / compute-blindspot / compute-element / compute-bayesian / fetch-brain-baseline) is spawned; assert each path produces output; assert no ModuleNotFoundError-class error surfaces. This is the structural proof the RCA failure class is eliminated across the FULL surface. Write tests/run-all-134.sh aggregating test-rs-math-cjs + test-rs-embed-cjs + test-rs-helpers-cjs + test-rs-engine-cjs + test-hsi-cjs-parity + test-whitespace-cjs-parity + test-fingerprint-cjs-parity + test-134-windows-replay (mirror tests/run-all-124.sh). Register it in the test aggregator per the project convention.</action>
  <verify>
    <automated>MINDRIAN_RS_ENGINE=cjs node --test tests/test-134-windows-replay.cjs && bash tests/run-all-134.sh</automated>
  </verify>
  <done>The Windows-replay test proves zero analyzer-Python spawn + no ModuleNotFoundError across the FULL Engine 1 Act 1 + post-write auto-fire flow; run-all-134.sh aggregates every phase parity gate + the replay green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The FULL user-machine analyzer surface now runs in-process via @huggingface/transformers on the default MINDRIAN_RS_ENGINE=cjs path: the post-write auto-fire (intelligence-cascade HSI + reverse-salient -- the RCA surface), the 5 Engine-1 commands (/mos:find-bottlenecks, /mos:whitespace, /mos:find-connections, /mos:find-analogies, /mos:score-innovation), the /mos:fingerprint diagnostics, and the brain-baseline embedder. The Windows-replay test proves zero analyzer-Python spawn across all of it. The python fallback is one env-flag away for one beta.</what-built>
  <how-to-verify>
    1. Run `MINDRIAN_RS_ENGINE=cjs bash tests/run-all-134.sh` -- all phase tests green (every parity gate + Windows-replay).
    2. On a real room with python3 UNINSTALLED or PATH-masked: write an artifact and confirm the post-write auto-fire (HSI + reverse-salient cascade) completes with NO Python error (the actual RCA replay -- this is the BLOCKER-1 surface).
    3. Run /mos:find-bottlenecks, /mos:whitespace discover, /mos:whitespace (non-discover gaps + topic-forest), and /mos:score-innovation (/mos:fingerprint) on the same python3-absent room -- confirm in-process completion, model cold-load within the Wave-0 budget on first run.
    4. Confirm `MINDRIAN_RS_ENGINE=python` still works as the fallback (with python deps present) for the full surface.
    5. Confirm the model-download first-run UX (progress + size) behaves per the Wave-0 decision and the post-write auto-fire does NOT pay the cold load inline (warm-up off the hot path).
    6. Re-run the honest coverage grep from 134-SOURCE-AUDIT.md and confirm ZERO analyzer python3 spawn remains on the cjs path.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the Windows-fragility class is eliminated across the FULL surface on the cjs path, or describe regressions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> engine | The spawn boundary (child_process -> python) is removed on the cjs path across the FULL analyzer surface; replaced by in-process require |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-16 | Denial of Service | sync->async shift in detectAndSurface / the cascade step breaks a synchronous caller | mitigate | Audit every detectAndSurface + cascade-step caller (tension-hook-agent, doctor, find-bottlenecks node -e, tests); make the chain async or provide a facade; covered by run-all-134.sh. |
| T-134-17 | Tampering | a residual analyzer-python spawn slips through on the cjs path | mitigate | The Windows-replay test spies on child_process across the FULL surface and FAILS if ANY analyzer .py is spawned on the cjs path -- structural proof, not promise. |
| T-134-18 | Denial of Service | first-run model cold-load overruns the post-write hook timeout in production | mitigate | Human-verify checkpoint step 5 confirms the auto-fire does not pay cold load inline (warm-up off-path per Plan 03 ensureModel); cold-load fits the Wave-0 budget. |
| T-134-19 | Repudiation | removing the pip hint hides a real fallback failure | accept | The pip hint is retained behind MINDRIAN_RS_ENGINE=python (the only path that still needs Python). |
| T-134-28 | Information Disclosure | a rewired caller drops the Part 8 egress audit | mitigate | rs-differential-scorer -> rs-cache.cjs preserves the rs-egress audit (Plan 04a); external paths inherit the 130.5 audit; the Windows-replay + existing egress tests cover it. |
</threat_model>

<verification>
- `MINDRIAN_RS_ENGINE=cjs bash tests/run-all-134.sh` -- all phase tests pass (rs-math, rs-embed, rs-helpers, rs-engine, HSI parity, whitespace parity, fingerprint parity, Windows-replay).
- The honest coverage grep from 134-SOURCE-AUDIT.md returns ZERO analyzer python3 spawn on the cjs path.
- `grep -q "MINDRIAN_RS_ENGINE" lib/core/intelligence-cascade.cjs lib/agents/reverse-salient-agent.cjs lib/core/rs-differential-scorer.cjs scripts/discovery-cycle.cjs scripts/auto-explore-fire.cjs scripts/whitespace-command.cjs scripts/diagnostics-command.cjs scripts/ensure-brain-baseline.cjs scripts/doctor.cjs` matches all.
- Human-verify checkpoint approved (real-room RCA replay across the full surface with python3 absent).
</verification>

<success_criteria>
The FULL Engine-1 + auto-fire analyzer surface runs in-process with zero analyzer-Python on the default cjs path; the env-flag gives a one-beta python fallback; --check-rs-engine + check-hsi-deps are retired on the cjs path; the full-surface Windows-tester-replay acceptance proves the install-fragility class is eliminated.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-08-SUMMARY.md` when done.
</output>
