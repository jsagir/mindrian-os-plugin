---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 04
type: execute
wave: 2
depends_on: ["134-02", "134-03"]
files_modified:
  - lib/core/rs-engine.cjs
  - lib/core/rs-rooms.cjs
  - lib/core/rs-cache.cjs
  - lib/core/rs-hybrid.cjs
  - lib/core/rs_corpus.py
  - tests/test-rs-engine-cjs.cjs
autonomous: true
requirements: [CJS-134-ENGINE, CJS-134-REUSE-130.5, CJS-134-DELETE-CORPUS]
tags: [rs-engine, corpus-reuse, transformers-js, port]
user_setup: []

must_haves:
  truths:
    - "rs-engine.cjs runs the three modes (internal / external / hybrid) in-process, producing the same .rs-engine-results.json finding shape rs-engine.py produced"
    - "lib/core/rs_corpus.py is DELETED and the fetcher is the Phase 130.5 lib/core/research-corpus.cjs + research-cache.cjs (consumed, not re-ported)"
    - "rs_rooms.py / rs_cache.py / rs_hybrid.py are ported to CJS, consuming rs-embed.cjs for vectors"
    - "rs-engine.cjs produces the SAME ranked findings as rs-engine.py on the fixture room (internal mode), per the Wave-0 correctness bar"
  artifacts:
    - path: "lib/core/rs-engine.cjs"
      provides: "In-process 3-mode reverse-salient engine orchestrator (replaces scripts/rs-engine.py)"
      min_lines: 200
    - path: "lib/core/rs-cache.cjs"
      provides: "CJS corpus/vector cache consuming rs-embed.cjs (replaces rs_cache.py Pinecone integrated-embedding)"
      min_lines: 60
    - path: "lib/core/rs-rooms.cjs"
      provides: "CJS room-corpus reader (replaces rs_rooms.py)"
      min_lines: 40
    - path: "lib/core/rs-hybrid.cjs"
      provides: "CJS unified-corpus builder (replaces rs_hybrid.py)"
      min_lines: 60
  key_links:
    - from: "lib/core/rs-engine.cjs"
      to: "lib/core/research-corpus.cjs (Phase 130.5)"
      via: "require fetchCorpus + the shared cache"
      pattern: "research-corpus|fetchCorpus"
    - from: "lib/core/rs-engine.cjs"
      to: "lib/core/rs-math.cjs + lib/core/rs-embed.cjs"
      via: "in-process require"
      pattern: "rs-math|rs-embed"
---

<objective>
Port the `scripts/rs-engine.py` orchestrator (1755 lines, 3 modes: internal / external / hybrid) and its remaining helper modules (`rs_rooms.py`, `rs_cache.py`, `rs_hybrid.py`) to in-process CJS, consuming `rs-math.cjs` (Plan 02) + `rs-embed.cjs` (Plan 03), and REUSING the Phase 130.5 shared fetcher. DELETE `lib/core/rs_corpus.py` -- it is replaced by 130.5's `research-corpus.cjs` + `research-cache.cjs`, NOT re-ported.

Purpose: This is the engine spine. After this plan, the reverse-salient computation runs entirely in Node. The output `.rs-engine-results.json` shape must be preserved byte-for-byte so every existing consumer (bridge-writer.cjs, hsi-to-graph.cjs, auto-explore-fire.cjs, the 24 readers found in the survey) keeps working without change. The actual cutover of callers (replacing child_process spawn with require) is Plan 06 -- this plan builds the in-process engine and proves parity; it does not yet rewire the agents.
Output: `rs-engine.cjs` + `rs-rooms.cjs` + `rs-cache.cjs` + `rs-hybrid.cjs`; `rs_corpus.py` deleted; parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/130.5-CONTEXT.md

<read_first>
- scripts/rs-engine.py (the orchestrator. The 3 modes: --mode internal (room-only reverse-salient over the artifact corpus), --mode external (topic -> external corpus via fetcher), --mode hybrid (room + external unified corpus, --topk). The output payload shape: the `result` dict with `meta.mode` + `findings[]` each carrying source_artifact_id / target_artifact_id / signed_diff / abs_diff / direction (lines ~470-550). The internal-mode findings are written to .rs-engine-results.json.)
- lib/core/rs_rooms.py (room artifact corpus reader -- 193 lines; produces the [{id, text, section}] list rs-engine consumes).
- lib/core/rs_cache.py (Pinecone embed-once cache -- REPLACED by rs-cache.cjs consuming rs-embed.cjs; the on-disk cache shape can mirror 130.5 research-cache or stay rs-local).
- lib/core/rs_hybrid.py (unified-corpus builder for hybrid mode -- 586 lines; merges room corpus + external corpus, attaches _vector; the bypass path).
- lib/core/rs_corpus.py (TO DELETE -- its fetch_openalex / fetch_arxiv / fetch_tavily / fetch_corpus interface is now provided by 130.5 research-corpus.cjs fetchCorpus({source, query, limit})).
- lib/core/bridge-writer.cjs (the PRIMARY consumer of .rs-engine-results.json -- read lines ~30-130 for the exact pair field names it reads: source_artifact_id, source_section, source_room, source_artifact, target_artifact_id, signed_diff, abs_diff, direction. The CJS output MUST preserve these.)
- lib/agents/reverse-salient-agent.cjs (lines ~160-320: the execFileSync invocation of rs-engine.py + the .rs-engine-results.json reader. This is the caller Plan 06 rewires; read it to learn the exact CLI args + result-file contract the new engine must honor.)
</read_first>

<interfaces>
130.5 forward contract (the dependency -- 130.5 ships BEFORE 134 in v1.13.1):
- lib/core/research-corpus.cjs exports fetchCorpus({ source, query, limit }) -> Promise<Doc[]> over OpenAlex / arXiv / PubMed / Tavily, native fetch, with the Part 8 pre-egress audit baked in.
- lib/core/research-cache.cjs exports the shared TTL source-keyed cache.
- If 130.5 is not yet executed at plan-execution time, the executor must treat these as the hard contract and surface a blocker (do NOT re-port rs_corpus.py -- that would violate the reuse mandate).

rs-engine.cjs public surface (consumed by Plan 06 callers in-process):
- runInternal({ roomDir, sessionId, topk }) -> Promise<Result>
- runExternal({ topic, roomDir, ... }) -> Promise<Result>
- runHybrid({ topic, roomDir, topk }) -> Promise<Result>
- A thin CLI shim is OPTIONAL (the migration moves callers to require(); a CLI shim is only for backward-compat during the both-paths beta). The Result shape === the rs-engine.py payload (meta.mode + findings[] with the bridge-writer field names).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port rs_rooms.py + rs_cache.py + rs_hybrid.py to CJS; DELETE rs_corpus.py</name>
  <files>lib/core/rs-rooms.cjs, lib/core/rs-cache.cjs, lib/core/rs-hybrid.cjs, lib/core/rs_corpus.py</files>
  <behavior>
    - rs-rooms.cjs reads a room dir and returns the same [{id, text, section}] artifact list rs_rooms.py produced for the fixture room.
    - rs-cache.cjs embeds via rs-embed.cjs (not Pinecone server-side, not MiniLM) and caches embed-once on disk; a repeat embed of the same text hits the cache.
    - rs-hybrid.cjs builds the unified room+external corpus with _vector attached from rs-embed.cjs, preserving the rs_hybrid.py merge order and the bypass path.
    - lib/core/rs_corpus.py no longer exists; external fetching is via the 130.5 research-corpus.cjs fetchCorpus.
  </behavior>
  <action>Port rs_rooms.py -> rs-rooms.cjs (room artifact corpus reader; pure file/SQL reads -- if it reads room.db, route through navigation.cjs per Canon Part 9, do NOT open room.db directly). Port rs_cache.py -> rs-cache.cjs: replace the Pinecone integrated-embedding with rs-embed.cjs.embed(); keep the embed-once-query-forever semantics as an on-disk cache (mirror 130.5 research-cache.cjs's atomic-write pattern, or reuse it). Port rs_hybrid.py -> rs-hybrid.cjs: the unified-corpus builder; attach _vector via rs-embed.cjs; preserve the merge order (room corpus in discover order, then external docs) and the bypass path. For external fetching, consume 130.5 research-corpus.cjs fetchCorpus -- do NOT re-implement OpenAlex/arXiv/Tavily fetchers. DELETE lib/core/rs_corpus.py (git rm). Write tests asserting the fixture-room corpus + cache + hybrid-merge parity against the Python reference.</action>
  <verify>
    <automated>test ! -f lib/core/rs_corpus.py && node --test tests/test-rs-engine-cjs.cjs 2>/dev/null; node -e "require('./lib/core/rs-rooms.cjs');require('./lib/core/rs-cache.cjs');require('./lib/core/rs-hybrid.cjs')"</automated>
  </verify>
  <done>rs_corpus.py is deleted; rs-rooms/rs-cache/rs-hybrid.cjs load and pass fixture-parity tests; external fetch routes through 130.5 research-corpus.cjs (grep confirms `require` of research-corpus, not a re-ported fetcher).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rs-engine.cjs 3-mode orchestrator with byte-preserved finding output</name>
  <files>lib/core/rs-engine.cjs, tests/test-rs-engine-cjs.cjs</files>
  <behavior>
    - runInternal({roomDir, topk}) on the fixture room produces the SAME ranked findings (source/target/signed_diff/abs_diff/direction) as `python3 scripts/rs-engine.py --mode internal` -- byte-identical ranked order OR within the Wave-0 drift bar.
    - The output object serializes to the .rs-engine-results.json shape bridge-writer.cjs reads (the exact field names from bridge-writer.cjs lines ~30-130).
    - runExternal + runHybrid resolve without spawning Python and without re-implementing the corpus fetch (they call 130.5 fetchCorpus).
    - The engine contains zero child_process / spawn / execFile / .py references.
  </behavior>
  <action>Implement lib/core/rs-engine.cjs as the in-process orchestrator. runInternal: read room corpus via rs-rooms.cjs, build the LSA topic matrix via rs-math.cjs buildLsaMatrix, compute abs_diff_topk + classify_direction, emit findings in the rs-engine.py payload shape (meta.mode='internal' + findings[] with bridge-writer field names), and write .rs-engine-results.json to the same path reverse-salient-agent.cjs reads (roomDir/.mindrian/.rs-engine-results.json -- confirm the exact path in the agent). runExternal: fetch via 130.5 fetchCorpus, embed via rs-embed.cjs, score. runHybrid: build the unified corpus via rs-hybrid.cjs, embed, score, honor --topk. Preserve the direction-enum -> cascade-edge mapping bridge-writer.cjs depends on. Provide the runInternal/runExternal/runHybrid exports for Plan 06's in-process callers. Write tests/test-rs-engine-cjs.cjs asserting internal-mode parity vs rs-engine.py on the fixture (regenerate the Python reference in test setup, skipping with a clear message if python3 is absent, or commit a JSON fixture) and asserting the output shape matches the bridge-writer contract.</action>
  <verify>
    <automated>node --test tests/test-rs-engine-cjs.cjs</automated>
  </verify>
  <done>rs-engine.cjs runs all 3 modes in-process; internal-mode findings match rs-engine.py on the fixture per the Wave-0 bar; the .rs-engine-results.json shape is byte-preserved for bridge-writer.cjs; grep confirms zero child_process/.py in rs-engine.cjs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rs-engine.cjs -> 130.5 fetchCorpus -> external APIs | External corpus fetch crosses the egress boundary (Part 8 audit inherited from 130.5) |
| room corpus -> in-process embedder/LSA | LOCAL room text processed in-process; never egresses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-09 | Tampering | deleting rs_corpus.py breaks a hidden caller | mitigate | grep-confirm no CJS/py caller imports rs_corpus before delete (the survey found only the rs_* python siblings + rs-engine.py reference it); external fetch repointed to 130.5 fetchCorpus. |
| T-134-10 | Information Disclosure | external fetch query string leaks room content | mitigate | Inherited 130.5 Part 8 pre-egress audit; rs-engine.cjs passes only the topic string, never artifact bodies. |
| T-134-11 | Repudiation | output-shape drift breaks bridge-writer cascade edges | mitigate | Test asserts the .rs-engine-results.json field names match bridge-writer.cjs verbatim. |
| T-134-12 | Tampering | room.db opened outside navigation.cjs (Part 9 breach) | mitigate | rs-rooms.cjs routes any room.db read through navigation.cjs (Canon Part 9 -- the only door). |
</threat_model>

<verification>
- `node --test tests/test-rs-engine-cjs.cjs` passes (internal parity + output shape).
- `test ! -f lib/core/rs_corpus.py` (deleted).
- `grep -rln "research-corpus\|fetchCorpus" lib/core/rs-engine.cjs lib/core/rs-hybrid.cjs` matches (130.5 reuse).
- `grep -nE "child_process|spawn|execFile|\\.py\\b" lib/core/rs-engine.cjs` returns nothing.
</verification>

<success_criteria>
The reverse-salient engine runs entirely in-process across all 3 modes, reuses the 130.5 shared fetcher (rs_corpus.py deleted), and produces byte-compatible findings proven against the Python on the fixture room.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-04-SUMMARY.md` when done.
</output>
