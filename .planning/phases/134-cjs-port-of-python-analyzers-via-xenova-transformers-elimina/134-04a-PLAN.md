---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 04a
type: execute
wave: 2
depends_on: ["134-02", "134-03", "130.5"]
files_modified:
  - lib/core/rs-rooms.cjs
  - lib/core/rs-cache.cjs
  - lib/core/rs-hybrid.cjs
  - lib/core/rs_corpus.py
  - tests/test-rs-helpers-cjs.cjs
autonomous: true
requirements: [CJS-134-RS-PORT, CJS-134-REUSE-130.5, CJS-134-DELETE-CORPUS]
tags: [rs-helpers, corpus-reuse, pinecone-bridge, transformers-js, port]
user_setup: []

must_haves:
  truths:
    - "rs-rooms.cjs / rs-cache.cjs / rs-hybrid.cjs are ported to CJS, consuming rs-embed.cjs for vectors"
    - "rs-cache.cjs absorbs the rs-pinecone-bridge.cjs embedded `python3 -c` LSA/Pinecone bridge -- in-process, no spawn"
    - "lib/core/rs_corpus.py is DELETED and external fetching is the Phase 130.5 lib/core/research-corpus.cjs + research-cache.cjs (consumed, not re-ported)"
    - "A pre-flight assertion HARD-FAILS if lib/core/research-corpus.cjs is absent (so the executor blocks instead of re-porting rs_corpus.py)"
  artifacts:
    - path: "lib/core/rs-cache.cjs"
      provides: "CJS corpus/vector cache consuming rs-embed.cjs (replaces rs_cache.py Pinecone integrated-embedding + the rs-pinecone-bridge embedded Python)"
      min_lines: 70
    - path: "lib/core/rs-rooms.cjs"
      provides: "CJS room-corpus reader (replaces rs_rooms.py)"
      min_lines: 40
    - path: "lib/core/rs-hybrid.cjs"
      provides: "CJS unified-corpus builder (replaces rs_hybrid.py)"
      min_lines: 60
  key_links:
    - from: "lib/core/rs-hybrid.cjs"
      to: "lib/core/research-corpus.cjs (Phase 130.5)"
      via: "require fetchCorpus + the shared cache"
      pattern: "research-corpus|fetchCorpus"
    - from: "lib/core/rs-cache.cjs"
      to: "lib/core/rs-embed.cjs"
      via: "in-process require (replaces Pinecone server-side + the python3 -c bridge)"
      pattern: "rs-embed"
---

<objective>
Port the reverse-salient HELPER modules to in-process CJS: `rs_rooms.py` -> `rs-rooms.cjs`, `rs_cache.py` -> `rs-cache.cjs` (ALSO absorbing the `rs-pinecone-bridge.cjs` embedded `python3 -c` LSA/Pinecone bridge), `rs_hybrid.py` -> `rs-hybrid.cjs`, consuming `rs-embed.cjs` (Plan 03). DELETE `lib/core/rs_corpus.py` -- external fetching is REUSED from Phase 130.5's `research-corpus.cjs` + `research-cache.cjs`, NOT re-ported. This is the file-disjoint half of the former heavy Plan 04 (split per checker WARNING-6); Plan 04b builds the rs-engine.cjs orchestrator on top of these helpers.

Purpose: These three helpers + the corpus fetcher are everything rs-engine.cjs (Plan 04b) needs underneath it. Splitting them out keeps each plan within budget and lets 04a and 04b stay file-disjoint within Wave 2. The rs-pinecone-bridge embedded Python (`spawnSync python3 -c`) is part of the user-machine surface the original Plan 04 missed -- it is absorbed here into rs-cache.cjs's in-process path.
Output: `rs-rooms.cjs` + `rs-cache.cjs` + `rs-hybrid.cjs`; `rs_corpus.py` deleted; helper parity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/130.5-shared-corpus-cache-cjs-fetcher-substrate/130.5-CONTEXT.md

<read_first>
- lib/core/rs_rooms.py (room artifact corpus reader -- 193 lines; produces the [{id, text, section}] list rs-engine consumes).
- lib/core/rs_cache.py (Pinecone embed-once cache -- 479 lines; REPLACED by rs-cache.cjs consuming rs-embed.cjs; the on-disk cache shape can mirror 130.5 research-cache or stay rs-local. Note fetch_all_from_namespace -- this is what the pinecone bridge wraps.)
- lib/core/rs-pinecone-bridge.cjs (lines 71-130: the embedded `python3 -c` bridgeScript that imports rs_cache.fetch_all_from_namespace to retrieve 1024-dim Pinecone vectors over stdio. ABSORB this into rs-cache.cjs as an in-process function -- the cjs path no longer spawns python3 -c. Preserve the {success, hits:[{id,score,values,metadata}]} envelope queryPineconeWithVectors returns, AND the rs-egress-prompts.cjs / rs-egress-violations.cjs audit calls it wraps the namespace+query through -- Part 8 egress audit MUST be preserved.)
- lib/core/rs_hybrid.py (unified-corpus builder for hybrid mode -- 586 lines; merges room corpus + external corpus, attaches _vector; the bypass path).
- lib/core/rs_corpus.py (TO DELETE -- its fetch_openalex / fetch_arxiv / fetch_tavily / fetch_corpus interface is now provided by 130.5 research-corpus.cjs fetchCorpus({source, query, limit})).
- lib/core/rs-differential-scorer.cjs (the consumer of the pinecone bridge -- read lines ~90-210 to learn the bridge envelope it expects; Plan 08 rewires it to call rs-cache.cjs in-process).
</read_first>

<interfaces>
130.5 forward contract (HARD dependency -- 130.5 ships in v1.13.1 BEFORE 134 in v1.14.0):
- lib/core/research-corpus.cjs exports fetchCorpus({ source, query, limit }) -> Promise<Doc[]> over OpenAlex / arXiv / PubMed / Tavily, native fetch, with the Part 8 pre-egress audit baked in.
- lib/core/research-cache.cjs exports the shared TTL source-keyed cache.
- Phase 110 (Brain Context Packet Contract) is consumed TRANSITIVELY via 130.5's pre-egress audit.
- If 130.5 is not yet executed at plan-execution time, Task 1's pre-flight assertion HARD-FAILS and the executor surfaces a blocker -- do NOT re-port rs_corpus.py (that violates the reuse mandate).

rs-helper public surfaces (consumed by Plan 04b rs-engine.cjs):
- rs-rooms.cjs: readRoomCorpus(roomDir) -> [{id, text, section}]
- rs-cache.cjs: embed-once cache over rs-embed.cjs; fetchNamespaceVectors(namespace, query, topK) -> {success, hits:[...]} (the in-process replacement for the pinecone bridge, with the Part 8 egress audit preserved)
- rs-hybrid.cjs: buildUnifiedCorpus({roomDir, topic, topk}) -> the merged room+external corpus with _vector attached
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-flight 130.5 assertion + port rs_rooms/rs_cache/rs_hybrid (absorb pinecone-bridge) + DELETE rs_corpus.py</name>
  <files>lib/core/rs-rooms.cjs, lib/core/rs-cache.cjs, lib/core/rs-hybrid.cjs, lib/core/rs_corpus.py, tests/test-rs-helpers-cjs.cjs</files>
  <behavior>
    - Pre-flight: if lib/core/research-corpus.cjs is ABSENT, the test (and any require of rs-hybrid.cjs) HARD-FAILS with a clear "Phase 130.5 research-corpus.cjs missing -- do NOT re-port rs_corpus.py" message rather than silently re-implementing a fetcher.
    - rs-rooms.cjs reads a room dir and returns the same [{id, text, section}] artifact list rs_rooms.py produced for the fixture room.
    - rs-cache.cjs embeds via rs-embed.cjs (not Pinecone server-side, not MiniLM) and caches embed-once on disk; a repeat embed of the same text hits the cache. Its fetchNamespaceVectors runs IN-PROCESS (no python3 -c) and preserves the {success, hits} envelope + the Part 8 egress audit the old bridge wrapped.
    - rs-hybrid.cjs builds the unified room+external corpus with _vector attached from rs-embed.cjs, preserving the rs_hybrid.py merge order and the bypass path, fetching external docs via 130.5 fetchCorpus.
    - lib/core/rs_corpus.py no longer exists.
  </behavior>
  <action>FIRST: add a pre-flight assertion at the top of Task 1's test and inside rs-hybrid.cjs's module init -- check `require.resolve('./research-corpus.cjs')` (or fs.existsSync of lib/core/research-corpus.cjs); if absent, throw/fail with the explicit 130.5-missing blocker message. Then port: rs_rooms.py -> rs-rooms.cjs (room artifact corpus reader; pure file/SQL reads -- if it reads room.db, route through navigation.cjs per Canon Part 9, do NOT open room.db directly). Port rs_cache.py -> rs-cache.cjs: replace the Pinecone integrated-embedding with rs-embed.cjs.embed(); keep embed-once-query-forever as an on-disk cache (mirror 130.5 research-cache.cjs's atomic-write pattern, or reuse it). ABSORB the rs-pinecone-bridge.cjs embedded python3 -c bridge into an in-process rs-cache.cjs function (fetchNamespaceVectors) that returns the same {success, hits:[{id,score,values,metadata}]} envelope and routes namespace+query through the SAME rs-egress-prompts.cjs / rs-egress-violations.cjs audit (Part 8 preserved -- the bridge's auditQueryString/auditQueryObject calls must survive the port). Port rs_hybrid.py -> rs-hybrid.cjs: the unified-corpus builder; attach _vector via rs-embed.cjs; preserve the merge order (room corpus in discover order, then external docs) and the bypass path; fetch external docs via 130.5 research-corpus.cjs fetchCorpus -- do NOT re-implement OpenAlex/arXiv/Tavily fetchers. DELETE lib/core/rs_corpus.py (git rm). Write tests/test-rs-helpers-cjs.cjs asserting the pre-flight blocker behavior + fixture-room corpus + cache-hit + hybrid-merge parity against the committed Wave-0 golden fixtures.</action>
  <verify>
    <automated>test ! -f lib/core/rs_corpus.py && node -e "require('./lib/core/rs-rooms.cjs');require('./lib/core/rs-cache.cjs')" && node --test tests/test-rs-helpers-cjs.cjs</automated>
  </verify>
  <done>rs_corpus.py is deleted; rs-rooms/rs-cache/rs-hybrid.cjs load and pass fixture-parity tests; rs-cache.cjs's fetchNamespaceVectors runs in-process with the Part 8 egress audit preserved (no python3 -c); external fetch routes through 130.5 research-corpus.cjs (grep confirms `require` of research-corpus, not a re-ported fetcher); the 130.5-missing pre-flight blocker is asserted by a test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rs-hybrid.cjs -> 130.5 fetchCorpus -> external APIs | External corpus fetch crosses the egress boundary (Part 8 audit inherited from 130.5) |
| rs-cache.cjs fetchNamespaceVectors -> Pinecone | Namespace query crosses to Pinecone; the Part 8 egress audit (rs-egress-prompts) must survive the bridge absorption |
| room corpus -> in-process embedder | LOCAL room text processed in-process; never egresses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-09 | Tampering | deleting rs_corpus.py breaks a hidden caller | mitigate | grep-confirm no CJS/py caller imports rs_corpus before delete; external fetch repointed to 130.5 fetchCorpus; pre-flight asserts 130.5 present. |
| T-134-10 | Information Disclosure | namespace/query leaks room content via the absorbed pinecone path | mitigate | The rs-egress-prompts.cjs / rs-egress-violations.cjs audit the old python bridge wrapped is preserved in the in-process fetchNamespaceVectors; Part 8 boundary intact. |
| T-134-12 | Tampering | room.db opened outside navigation.cjs (Part 9 breach) | mitigate | rs-rooms.cjs routes any room.db read through navigation.cjs (the only door). |
| T-134-20 | Denial of Service | 130.5 not yet executed when 04a runs | mitigate | Task 1 pre-flight HARD-FAILS with an explicit blocker rather than re-porting rs_corpus.py. |
</threat_model>

<verification>
- `node --test tests/test-rs-helpers-cjs.cjs` passes (helper parity + pre-flight blocker).
- `test ! -f lib/core/rs_corpus.py` (deleted).
- `grep -rln "research-corpus\|fetchCorpus" lib/core/rs-hybrid.cjs` matches (130.5 reuse).
- `grep -nE "child_process|spawn|execFile|python3 -c|\\.py\\b" lib/core/rs-cache.cjs lib/core/rs-rooms.cjs lib/core/rs-hybrid.cjs` returns nothing (the pinecone python3 -c bridge is gone).
- `grep -q "rs-egress" lib/core/rs-cache.cjs` matches (Part 8 audit preserved through the bridge absorption).
</verification>

<success_criteria>
The reverse-salient helper layer (rooms/cache/hybrid) runs entirely in-process, absorbs the rs-pinecone-bridge embedded Python with the Part 8 egress audit intact, reuses the 130.5 shared fetcher (rs_corpus.py deleted), and blocks (not re-ports) if 130.5 is absent.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-04a-SUMMARY.md` when done.
</output>
