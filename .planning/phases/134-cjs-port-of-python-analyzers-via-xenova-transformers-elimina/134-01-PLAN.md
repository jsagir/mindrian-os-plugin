---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md
autonomous: false
requirements: [CJS-134-VENDOR, CJS-134-SVD, CJS-134-BYTECOMPAT, CJS-134-MODELUX]
tags: [transformers-js, onnx, embeddings, svd, vendoring]
user_setup: []

must_haves:
  truths:
    - "@huggingface/transformers installs and require()-loads under Node >=22.5 CJS"
    - "The vendored node_modules tree after the install carries zero native addons (the pure-JS vendoring rule holds)"
    - "One SVD/LSA port strategy is chosen with a stated correctness bar"
    - "The transformers.js multilingual-e5-large embedding output is measured against the current Pinecone-indexed vectors and a drift disposition is recorded"
    - "The first-run model-download UX (size pre-check + progress + hook-timeout budget) has a chosen approach"
  artifacts:
    - path: ".planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md"
      provides: "The four locked Wave-0 decisions (vendoring verdict, SVD strategy, byte-compat disposition, model-download UX) that Waves 1-3 consume"
      min_lines: 60
    - path: "package.json"
      provides: "@huggingface/transformers in dependencies (only after the vendoring checkpoint approves)"
      contains: "@huggingface/transformers"
  key_links:
    - from: "scripts/release.sh Step 6.7 vendoring rule"
      to: "node_modules/@huggingface/transformers + onnxruntime-web"
      via: "find node_modules native-addon grep"
      pattern: "find node_modules -iregex"
---

<objective>
De-risk the four genuinely-uncertain questions BEFORE any port code is written, and gate the dependency commit behind a vendoring re-audit. This is the Wave-0 spike plan: it produces a single authority document (`134-SPIKE.md`) with four LOCKED decisions that every downstream wave reads instead of re-deciding.

Purpose: The CONTEXT explicitly defers four open questions to plan-phase (SVD port strategy, model-weight byte-compat, model-download UX, migration shape). Baking in an answer would violate the planner-authority limits. Instead, we measure, decide, and record. The vendoring re-audit is a HARD GATE mirroring Phase 136 Plan 05's ink-install checkpoint: if `@huggingface/transformers` + its ONNX runtime drag a native addon into the vendored tree, the pure-JS vendoring rule (release.sh Step 6.7) breaks for ALL platforms, and that is a blocker to surface, not paper over.

Output: `134-SPIKE.md` (the four locked decisions); `@huggingface/transformers` added to `package.json` + `package-lock.json` ONLY after the human-verify vendoring checkpoint approves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@CLAUDE.md

<read_first>
The Python being ported / measured:
- lib/core/rs_math.py (the SVD math: build_tfidf_svd uses sklearn TruncatedSVD, n_components=80, plus TfidfVectorizer; build_lsa_matrix is the corpus-level entry; classify_direction + abs_diff_topk are the downstream consumers of the LSA matrix)
- lib/core/rs_cache.py (the Pinecone integrated-embedding path: multilingual-e5-large, 1024-dim, text field 'abstract', namespace 'external:{slug}')
- lib/core/rs_hybrid.py (the local MiniLM fallback embedding path + the _vector attach contract)

The vendoring rule being protected:
- scripts/release.sh Step 6.7 (npm ci --omit=dev; integrity gate; git add -f node_modules into Commit A). The audited invariant is: "every production dependency is pure JS -- zero .node addons, zero binding.gyp, zero prebuilds, zero lifecycle scripts" (release.sh comment at line ~435).
- .claude/includes/release-process.md "Vendored node_modules Rule" -- "Vendoring is only cross-platform-safe because every production dependency is pure JavaScript. Re-audit when adding a dependency."
</read_first>

<interfaces>
Tavily-validated facts (CONTEXT re-baseline 2026-06-01 + transformersjs-v3 blog):
- Package: @huggingface/transformers (Transformers.js v3+). Supports BOTH `require()` (CJS) and `import` (ESM). Official CJS example exists.
- ONNX runtime: collaborates with onnxruntime-web (WASM). Native-addon status is NOT documented and MUST be measured here.
- Embedding pipeline API: `const extractor = await pipeline('feature-extraction', 'Xenova/multilingual-e5-large'); const out = await extractor(texts, { pooling: 'mean', normalize: true }); out.tolist()`.
- Model id stays `Xenova/multilingual-e5-large` (HF model namespace unchanged; only the PACKAGE was renamed from @xenova/transformers).
- ModelRegistry API (get_pipeline_files / get_file_metadata / is_pipeline_cached / progress_callback) is asserted by the CONTEXT but NOT in the public blog -- treat as a spike question, do not assume it exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install probe + native-addon vendoring audit</name>
  <files>package.json, package-lock.json</files>
  <action>Run a real install of @huggingface/transformers into a scratch tree (do NOT yet add to package.json: install into a temp dir or use a throwaway branch, OR install then run the audit before committing). Pin the latest v3.x. After install, run the vendoring audit grep the release script depends on: `find node_modules -iregex '.*\.(node|gyp)$'` MUST be empty, AND scan for binding.gyp, prebuilt platform binaries under onnxruntime-web/bin, and install lifecycle scripts (`npm ls --omit=dev` + inspect the transformers + onnxruntime-web package.json for `scripts.install`/`scripts.postinstall`). Verify a CJS smoke load: `node -e "const t = require('@huggingface/transformers'); console.log(typeof t.pipeline)"` prints `function`. Record EVERY measured fact (tree size, addon count, lifecycle scripts present/absent, the exact onnxruntime-web sub-dependency) into the Task 2 audit section of 134-SPIKE.md. If the lib is ESM-only at runtime despite the require() claim, mirror the Phase 136 ink lazy-import pattern (`await import()`) and record that as the load mechanism. Do NOT commit @huggingface/transformers to package.json yet -- the next task is the human gate.</action>
  <verify>
    <automated>find node_modules -iregex '.*\.\(node\|gyp\)$' | grep -v '^$' | wc -l | grep -qx 0 && node -e "const t=require('@huggingface/transformers');process.exit(typeof t.pipeline==='function'?0:1)"</automated>
  </verify>
  <done>The native-addon grep is empty AND the CJS require smoke load returns a function, OR the audit FAILED and the failure (which addon, from which sub-dependency) is recorded verbatim in 134-SPIKE.md as a blocker. Either way the measured facts are in the SPIKE doc.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Task 1 installed @huggingface/transformers + onnxruntime-web into a scratch node_modules tree and ran the pure-JS vendoring audit (the same `find node_modules -iregex '.*\.(node|gyp)$'` grep that scripts/release.sh Step 6.7 implicitly relies on, plus a lifecycle-script and prebuilt-binary scan). The measured result is recorded in 134-SPIKE.md.</what-built>
  <how-to-verify>
    1. Read the "Vendoring Audit" section of 134-SPIKE.md.
    2. Confirm the native-addon grep count is 0, no binding.gyp, no prebuilt platform .so/.dll/.dylib under onnxruntime-web, and no install/postinstall lifecycle scripts in the added dependency tree.
    3. Confirm the vendored-tree size delta is acceptable for the marketplace artifact (the CONTEXT estimates ~200MB optional ONNX dep; confirm the actual measured size and whether it ships in `files` or is runtime-downloaded).
    4. If the audit PASSED: approve committing @huggingface/transformers to package.json + package-lock.json.
    5. If the audit FAILED (any native addon, gyp, prebuilt binary, or lifecycle script): DO NOT approve. The pure-JS vendoring rule breaks for all platforms. Decide: (a) drop vendoring for this dep and runtime-install, (b) per-platform trees, or (c) reject the dep and re-open the phase scope. This is a phase blocker per the CONTEXT.
  </how-to-verify>
  <resume-signal>Type "approved -- commit the dep" to add @huggingface/transformers to package.json, or describe the audit failure and the chosen disposition.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Three measurement spikes -> SVD strategy, byte-compat disposition, model-download UX (lock all four decisions in 134-SPIKE.md)</name>
  <files>.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md, package.json, package-lock.json</files>
  <action>Only after the checkpoint approves: add @huggingface/transformers to package.json dependencies + run `npm install` to update package-lock.json. Then run three measurement spikes and write the verdicts into 134-SPIKE.md (the Wave-0 authority doc downstream waves consume):

(A) SVD/LSA port strategy. The only non-trivial math port: rs_math.py uses sklearn TruncatedSVD (n_components=80) over a TfidfVectorizer corpus matrix, consumed by build_lsa_matrix -> extract_topic_keywords -> count_topic_membership -> normalize_and_l1_similarity. Evaluate the CONTEXT's four options: (1) hand-rolled truncated SVD via randomized-range-finder + power iteration (~200 lines), (2) numeric.js (flag staleness), (3) WASM math lib, (4) random-projection + power-iteration approximation. Build a fixture: pick a real room with >=20 artifacts (or the mindrianOS dogfood room). Run rs_math.py build_lsa_matrix on the fixture corpus to capture the Python reference topic-membership matrix. Prototype the leading candidate in throwaway CJS and compare. LOCK ONE option with the correctness bar: "the CJS LSA path produces the SAME ranked reverse-salient findings as the Python path on the fixture room -- byte-identical ranked order, OR a documented small drift with justification and the max rank-position delta." Record the chosen algorithm, the library (if any) + its version, and the correctness bar in 134-SPIKE.md.

(B) Model-weight byte-compat. Run the transformers.js feature-extraction pipeline (Xenova/multilingual-e5-large, pooling: mean, normalize: true) on a small fixture set of strings. Compare the output vectors against the current Pinecone pws-brain index vectors for the SAME input text (query the pinecone MCP for a known record's stored vector, or embed a known abstract and cosine-compare against its Pinecone neighbor structure). Measure cosine drift. LOCK the disposition: if cosine similarity is preserved closely enough that ranked retrieval order is unchanged -> "compat: no re-vectorization." If it drifts -> "compat: drift measured at X; re-vectorization of pws-brain is OUT OF SCOPE for 134, scoped to Phase 127.1 disposition" (per CONTEXT Out-of-scope + the 130.7 embedding-independent correlation_id de-risking). Record the measured cosine drift number.

(C) Model-download UX. Determine whether the ModelRegistry API (get_pipeline_files / is_pipeline_cached / progress_callback) actually exists in the installed v3.x (inspect the package exports; the blog did not document it). LOCK the first-run approach: (i) size pre-check mechanism (ModelRegistry if present, else a documented fallback), (ii) progress surface, (iii) the hook-timeout budget -- cold model load must fit the existing ~30s hook timeout OR the load must be moved off the hot hook path (lazy first-use with a one-time warm-up message). Record the chosen approach + the measured cold-load time on this machine.

(D) Migration shape (confirm, not measure). Lock the env-flag name and the both-paths-one-beta plan: env-flag `MINDRIAN_RS_ENGINE` with values `cjs` (default in v1.14.0-beta.1) | `python` (deprecation fallback beta.1-beta.2, removed beta.3). The existing `MINDRIAN_PYTHON` env var (used by reverse-salient-agent.cjs + rs-differential-scorer.cjs) stays as the python-bin override for the fallback path. Record in 134-SPIKE.md.</action>
  <verify>
    <automated>test -f .planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md && grep -qE "SVD|LSA" .planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md && grep -q "@huggingface/transformers" package.json && node -e "require('./package.json').dependencies['@huggingface/transformers']||process.exit(1)"</automated>
  </verify>
  <done>134-SPIKE.md records all four LOCKED decisions (SVD strategy + correctness bar; byte-compat disposition + measured cosine drift; model-download UX + measured cold-load; env-flag name + migration shape). @huggingface/transformers is in package.json dependencies and package-lock.json is in sync (`npm ci` would succeed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> vendored node_modules | A new dependency tree crosses into the shipped marketplace artifact |
| transformers.js -> HF model CDN | First-run downloads ~200MB+ ONNX weights to the user cache (external fetch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Tampering | @huggingface/transformers + onnxruntime-web install | mitigate | Native-addon audit grep + lifecycle-script scan + blocking human checkpoint BEFORE the dep is committed (Task 1 + checkpoint). If a native addon enters the tree, the dep is rejected or vendoring is dropped. |
| T-134-02 | Information Disclosure | transformers.js embedding (runs LOCALLY) | accept | Embeddings run in-process on the user machine; no LOCAL text egresses (Canon Part 8 unchanged -- the model fetches WEIGHTS from the CDN, never sends room text). Recorded in SPIKE. |
| T-134-SC | Tampering | npm install of @huggingface/transformers | mitigate | Package is Tavily-validated (official HF org, https://huggingface.co/blog/transformersjs-v3). Legitimacy confirmed in CONTEXT re-baseline; checkpoint is non-auto-approvable. |
| T-134-03 | Denial of Service | first-run model download on the hook path | mitigate | Spike C measures cold-load vs the ~30s hook timeout; if it overruns, the load moves off the hot hook to lazy first-use with a warm-up message. |
</threat_model>

<verification>
- The pure-JS vendoring grep `find node_modules -iregex '.*\.(node|gyp)$'` is empty after the @huggingface/transformers install.
- `node -e "const t=require('@huggingface/transformers'); console.log(typeof t.pipeline)"` prints `function` (or the await import() fallback is recorded).
- 134-SPIKE.md exists with all four locked decisions.
- package-lock.json is in sync (`npm ci --omit=dev` would not fail).
</verification>

<success_criteria>
Wave-0 de-risking complete: the dependency passed the vendoring gate behind a human checkpoint, and the four open questions (SVD, byte-compat, model UX, migration shape) are measured and locked in 134-SPIKE.md so Waves 1-3 build against decisions, not assumptions.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-01-SUMMARY.md` when done.
</output>
