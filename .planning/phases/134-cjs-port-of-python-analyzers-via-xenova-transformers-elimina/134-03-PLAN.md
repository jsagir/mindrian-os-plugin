---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 03
type: execute
wave: 1
depends_on: ["134-01"]
files_modified:
  - lib/core/rs-embed.cjs
  - tests/test-rs-embed-cjs.cjs
autonomous: true
requirements: [CJS-134-EMBED, CJS-134-BYTECOMPAT, CJS-134-MODELUX, CJS-134-MODEL-UNIFY]
tags: [transformers-js, onnx, embeddings, multilingual-e5-large]
user_setup: []

must_haves:
  truths:
    - "rs-embed.cjs produces 1024-dim normalized embeddings in-process from @huggingface/transformers (Xenova/multilingual-e5-large)"
    - "rs-embed.cjs is the SINGLE in-process embedder for BOTH math families: it replaces the rs Pinecone path (rs_cache.py), the rs local MiniLM fallback (rs_hybrid.py), AND the Family-B 768-dim BAAI/llm-embedder (compute-whitespace-embeddings.py) + brain-baseline embedder (fetch-brain-baseline.py), per the Wave-0 model-unify disposition"
    - "First-run model download surfaces progress and fits the hook-timeout budget per the Wave-0 UX decision; the singleton is warm-able off the post-write auto-fire hot path"
    - "rs-embed.cjs runs zero Python and never egresses room text (Canon Part 8)"
  artifacts:
    - path: "lib/core/rs-embed.cjs"
      provides: "The ONE in-process feature-extraction embedder (1024-dim e5-large) for the whole phase + first-run download UX + warm-cache check"
      min_lines: 90
    - path: "tests/test-rs-embed-cjs.cjs"
      provides: "Dimension + normalization + cosine-self tests; byte-compat assertion per the committed Wave-0 fixture"
      min_lines: 35
  key_links:
    - from: "lib/core/rs-embed.cjs"
      to: "@huggingface/transformers pipeline('feature-extraction','Xenova/multilingual-e5-large')"
      via: "require or await import per Wave-0 load mechanism"
      pattern: "feature-extraction|multilingual-e5-large"
---

<objective>
Build `lib/core/rs-embed.cjs`: the SINGLE in-process embedding layer using `@huggingface/transformers` (ONNX `Xenova/multilingual-e5-large`, 1024-dim) that replaces EVERY Python embedding path on the user machine -- the rs Pinecone server-side integrated-embedding (rs_cache.py), the rs local MiniLM fallback (rs_hybrid.py), the Family-B BAAI/llm-embedder (compute-whitespace-embeddings.py), and the brain-baseline local embedder (fetch-brain-baseline.py) -- per the Wave-0 model-unify disposition. This is the net-new heart of the phase: the one embedding substrate that both math families consume.

Purpose: The original plan scoped rs-embed to only the rs/HSI half. The corrected full-surface audit shows the whitespace + topic-forest + diagnostics + brain-baseline scripts ALSO embed (with a 768-dim BAAI model + a MiniLM fallback). Wave 0 decides whether to unify all of those onto the in-process e5-large (the strong default: one model download, one cache, one code path) and re-calibrate the Family-B thresholds, or keep a second 768-dim model. This plan implements that decision so Plans 04a/05/06/07 all call ONE embedder. It honors the Wave-0 byte-compat disposition (1024-dim e5-large preserves cosine similarity with the Pinecone index, or documented drift is accepted with re-vectorization scoped OUT to Phase 127.1).
Output: `lib/core/rs-embed.cjs` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md

<read_first>
- lib/core/rs_cache.py (the Pinecone integrated-embedding contract this replaces: multilingual-e5-large, 1024-dim, text field 'abstract', namespace 'external:{slug}', DEFAULT_EMBED_MODEL = "multilingual-e5-large"; the embed-once-query-forever cache shape).
- lib/core/rs_hybrid.py (the local MiniLM fallback + the `_vector` attach contract).
- scripts/compute-whitespace-embeddings.py (the Family-B embedder: BAAI/llm-embedder 768-dim PRIMARY, all-MiniLM-L6-v2 384-dim fallback; the whitespace-embeddings.json output shape with embeddings[].vector. The Wave-0 model-unify disposition decides whether rs-embed replaces this with e5-large 1024-dim + re-calibrated thresholds.)
- scripts/fetch-brain-baseline.py (embeds Brain FRAMEWORK DESCRIPTIONS -- generic methodology text, NOT user data per Canon Part 8 -- with "the same model used for room artifacts"; produces brain-baseline.json. Unifying on e5-large means brain-baseline embeds with e5-large too, consistent with the room artifacts.)
- lib/core/lazygraph-ops.cjs (an existing CJS module using `await import()` -- the dynamic-import precedent to mirror if transformers.js loads as ESM at runtime; see also lib/mcp/resources.cjs).
</read_first>

<interfaces>
Decisions LOCKED in 134-SPIKE.md (Wave 0) that this plan consumes:
- The load mechanism (CJS require vs await import) for @huggingface/transformers.
- The model-unify disposition: unify all embeddings on e5-large (with Family-B threshold recalibration noted for Plans 06/07), or keep a second 768-dim model. This plan implements whichever was locked.
- The model-download UX: size pre-check mechanism, progress surface, and whether the load stays on the hook path or moves to lazy first-use (note: the intelligence-cascade auto-fire path is the tightest budget).
- The byte-compat disposition + measured cosine drift vs the Pinecone index.

Embedding API (Tavily-validated):
  const extractor = await pipeline('feature-extraction', 'Xenova/multilingual-e5-large');
  const out = await extractor(texts, { pooling: 'mean', normalize: true });
  out.tolist();  // -> number[][] of 1024-dim normalized vectors

The module's public surface (consumed by Plans 04a rs-cache + 05 hsi-* + 06 whitespace-math + 07 fingerprint):
- embed(texts: string[]) -> Promise<number[][]>  (1024-dim, mean-pooled, normalized)
- isModelCached() -> Promise<boolean>  (warm-cache pre-check)
- ensureModel(progressCb?) -> Promise<void>  (idempotent warm-up; surfaces download progress; callable off the hot hook path)
Export as CJS module.exports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rs-embed.cjs the single in-process embedder with warm-cache + progress</name>
  <files>lib/core/rs-embed.cjs, tests/test-rs-embed-cjs.cjs</files>
  <behavior>
    - embed(["query: test"]) resolves to a [1][1024] array of finite numbers.
    - Each returned vector is L2-normalized (its dot-product with itself is ~1.0).
    - cosineSimilarity(embed(A), embed(A)) === 1 (self-similarity); two clearly-related strings score higher than two unrelated strings.
    - The pipeline is created ONCE and memoized (a second embed() call does not re-instantiate the extractor) -- this is the unification: ONE model serves rs AND whitespace AND fingerprint AND brain-baseline.
    - isModelCached() returns a boolean without throwing; ensureModel(cb) calls cb with progress events during a cold download and is a no-op when warm; ensureModel is callable from a non-hot surface so the post-write auto-fire never pays the cold load inline.
  </behavior>
  <action>Implement lib/core/rs-embed.cjs. Load @huggingface/transformers via the Wave-0 load mechanism (require, or `await import()` mirroring lazygraph-ops.cjs if ESM-only). Create the feature-extraction pipeline for `Xenova/multilingual-e5-large` lazily and memoize it in a module-level singleton (the first call pays the cold-load cost; subsequent calls reuse). embed(texts) calls the extractor with { pooling: 'mean', normalize: true } and returns out.tolist(). Apply the e5 prefix convention if the Python used one (check whether rs_cache.py / compute-whitespace-embeddings.py prepend "query: " / "passage: " -- e5 models are prefix-sensitive; preserve whatever the Python did so vectors stay compatible). Implement isModelCached() and ensureModel(progressCb) per the Wave-0 UX decision: use the ModelRegistry API if the installed version exposes it, else the documented fallback (cache-dir stat or guarded warm-up). Expose ensureModel() so the migration plan (08) can warm it from session-start (off the post-write auto-fire hot path). Zero Python, zero child_process. If the Wave-0 model-unify disposition kept a SECOND 768-dim model, also expose a second memoized pipeline behind a clearly-named export; if it unified on e5-large (the default), document in a header comment that the Family-B 768-dim thresholds are re-calibrated for 1024-dim in Plans 06/07. Write tests/test-rs-embed-cjs.cjs (node:test) asserting dimension=1024, normalization, self-cosine=1, related>unrelated, and singleton memoization. Gate the byte-compat assertion against the COMMITTED Wave-0 Pinecone reference fixture: if the disposition was "no re-vectorization," assert cosine drift vs the committed reference vector is within the locked bar; if "drift accepted / out of scope," assert only dimension + normalization and reference the SPIKE disposition in a test comment.</action>
  <verify>
    <automated>node --test tests/test-rs-embed-cjs.cjs</automated>
  </verify>
  <done>tests pass: 1024-dim normalized embeddings produced in-process, singleton memoized as the ONE phase-wide embedder, warm-cache + progress surface working per Wave-0 UX, ensureModel callable off the hot path. grep confirms zero child_process/.py in rs-embed.cjs. Byte-compat assertion matches the committed SPIKE fixture disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rs-embed.cjs -> HF model CDN | First-run downloads ONNX weights (model weights only, never room text) |
| LOCAL room text -> embedder | Room text is embedded IN-PROCESS and never leaves the machine |
| Brain framework descriptions -> embedder | brain-baseline embeds GENERIC methodology descriptions (not user data) -- Part 8 allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-06 | Information Disclosure | in-process embedding of room text | accept | Canon Part 8 preserved: embeddings run LOCALLY in-process; only model WEIGHTS are fetched from the CDN, room text never egresses. Asserted by a test that the embedder makes no outbound request carrying input text. |
| T-134-07 | Denial of Service | cold model load (~10-15s, ~200MB+) on the post-write auto-fire hot path | mitigate | Singleton memoization + ensureModel() warm-up from session-start (off the post-write hot path) per the Wave-0 budget; the auto-fire never pays the cold load inline. |
| T-134-08 | Tampering | embedding drift breaks similarity vs Pinecone index | mitigate | Byte-compat test gated on the committed Wave-0 drift fixture; re-vectorization scoped OUT to Phase 127.1 if drift exceeds the bar (CONTEXT Out-of-scope). |
</threat_model>

<verification>
- `node --test tests/test-rs-embed-cjs.cjs` passes (against committed fixtures).
- `grep -nE "child_process|spawn|execFile|sentence_transformers|\\.py" lib/core/rs-embed.cjs` returns nothing.
- `grep -nE "feature-extraction|multilingual-e5-large" lib/core/rs-embed.cjs` matches.
</verification>

<success_criteria>
ONE in-process e5-large embedder replaces ALL four Python embedding paths (rs Pinecone, rs MiniLM fallback, Family-B BAAI, brain-baseline), produces 1024-dim normalized vectors, handles first-run download UX per the Wave-0 decision with warm-up off the auto-fire hot path, and preserves Canon Part 8.
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-03-SUMMARY.md` when done.
</output>
