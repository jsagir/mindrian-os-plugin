---
phase: 134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina
plan: 03
type: execute
wave: 1
depends_on: ["134-01"]
files_modified:
  - lib/core/rs-embed.cjs
  - tests/test-rs-embed-cjs.cjs
autonomous: true
requirements: [CJS-134-EMBED, CJS-134-BYTECOMPAT, CJS-134-MODELUX]
tags: [transformers-js, onnx, embeddings, multilingual-e5-large]
user_setup: []

must_haves:
  truths:
    - "rs-embed.cjs produces 1024-dim normalized embeddings in-process from @huggingface/transformers (Xenova/multilingual-e5-large)"
    - "rs-embed.cjs replaces both the Pinecone server-side embedding path (rs_cache.py) and the local MiniLM fallback (rs_hybrid.py) with one in-process embedder"
    - "First-run model download surfaces progress and fits the hook-timeout budget per the Wave-0 UX decision"
    - "rs-embed.cjs runs zero Python and never egresses room text (Canon Part 8)"
  artifacts:
    - path: "lib/core/rs-embed.cjs"
      provides: "In-process feature-extraction embedder (1024-dim e5-large) + first-run download UX + warm-cache check"
      min_lines: 90
    - path: "tests/test-rs-embed-cjs.cjs"
      provides: "Dimension + normalization + cosine-self tests; byte-compat assertion per Wave-0 bar"
      min_lines: 35
  key_links:
    - from: "lib/core/rs-embed.cjs"
      to: "@huggingface/transformers pipeline('feature-extraction','Xenova/multilingual-e5-large')"
      via: "require or await import per Wave-0 load mechanism"
      pattern: "feature-extraction|multilingual-e5-large"
---

<objective>
Build `lib/core/rs-embed.cjs`: the in-process embedding layer using `@huggingface/transformers` (ONNX `Xenova/multilingual-e5-large`, 1024-dim) that replaces BOTH Python embedding paths -- the Pinecone server-side integrated-embedding (rs_cache.py) and the local MiniLM fallback (rs_hybrid.py) -- with one Node module. This is the net-new heart of the phase: the embedding/HSI substrate that 130.5 deliberately left out.

Purpose: The 5 Engine-1 commands and the HSI layer need embeddings. Today they come from Python (sentence-transformers MiniLM) or Pinecone server-side. This module brings embeddings in-process so the user machine needs zero Python. It honors the Wave-0 byte-compat disposition (1024-dim e5-large preserves cosine similarity with the Pinecone index, or the documented drift is accepted with re-vectorization scoped OUT to Phase 127.1).
Output: `lib/core/rs-embed.cjs` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-CONTEXT.md
@.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-SPIKE.md

<read_first>
- lib/core/rs_cache.py (the Pinecone integrated-embedding contract this replaces: multilingual-e5-large, 1024-dim, text field 'abstract', namespace 'external:{slug}', DEFAULT_EMBED_MODEL = "multilingual-e5-large"; the embed-once-query-forever cache shape).
- lib/core/rs_hybrid.py (the local MiniLM fallback + the `_vector` attach contract: attach_vectors / the bypass path embeds locally via MiniLM; rs-engine prefers cached e5-large over local MiniLM. The CJS embedder UNIFIES these -- one e5-large embedder, no MiniLM/e5 split.)
- lib/core/lazygraph-ops.cjs (an existing CJS module using `await import()` -- the dynamic-import precedent to mirror if transformers.js loads as ESM at runtime; see also lib/mcp/resources.cjs).
</read_first>

<interfaces>
Decisions LOCKED in 134-SPIKE.md (Wave 0) that this plan consumes:
- The load mechanism (CJS require vs await import) for @huggingface/transformers.
- The model-download UX: size pre-check mechanism (ModelRegistry if it exists in the installed version, else the documented fallback), the progress surface, and whether the load stays on the hook path or moves to lazy first-use.
- The byte-compat disposition + measured cosine drift vs the Pinecone index.

Embedding API (Tavily-validated):
  const extractor = await pipeline('feature-extraction', 'Xenova/multilingual-e5-large');
  const out = await extractor(texts, { pooling: 'mean', normalize: true });
  out.tolist();  // -> number[][] of 1024-dim normalized vectors

The module's public surface (consumed by Plan 04 rs-engine.cjs + Plan 05 hsi-*.cjs):
- embed(texts: string[]) -> Promise<number[][]>  (1024-dim, mean-pooled, normalized)
- isModelCached() -> Promise<boolean>  (warm-cache pre-check)
- ensureModel(progressCb?) -> Promise<void>  (idempotent warm-up; surfaces download progress)
Export as CJS module.exports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rs-embed.cjs in-process embedder with warm-cache + progress</name>
  <files>lib/core/rs-embed.cjs, tests/test-rs-embed-cjs.cjs</files>
  <behavior>
    - embed(["query: test"]) resolves to a [1][1024] array of finite numbers.
    - Each returned vector is L2-normalized (its dot-product with itself is ~1.0).
    - cosineSimilarity(embed(A), embed(A)) === 1 (self-similarity); two clearly-related strings score higher than two unrelated strings.
    - The pipeline is created ONCE and memoized (a second embed() call does not re-instantiate the extractor).
    - isModelCached() returns a boolean without throwing; ensureModel(cb) calls cb with progress events during a cold download and is a no-op when warm.
  </behavior>
  <action>Implement lib/core/rs-embed.cjs. Load @huggingface/transformers via the Wave-0 load mechanism (require, or `await import()` mirroring lazygraph-ops.cjs if ESM-only). Create the feature-extraction pipeline for `Xenova/multilingual-e5-large` lazily and memoize it in a module-level singleton (the first call pays the cold-load cost; subsequent calls reuse). embed(texts) calls the extractor with { pooling: 'mean', normalize: true } and returns out.tolist(). Apply the e5 prefix convention if rs_cache.py used one (check whether the Python prepends "query: " / "passage: " -- e5 models are prefix-sensitive; preserve whatever rs_cache.py / Pinecone integrated-embedding did so vectors stay compatible). Implement isModelCached() and ensureModel(progressCb) per the Wave-0 UX decision: use the ModelRegistry API if the installed version exposes it, else the documented fallback (e.g. a cache-dir stat or a guarded warm-up). If the Wave-0 decision moved cold-load OFF the hook path, expose ensureModel() so the migration plan (06) can warm it from a non-hot surface. Zero Python, zero child_process. Write tests/test-rs-embed-cjs.cjs (node:test) asserting dimension=1024, normalization, self-cosine=1, related>unrelated, and singleton memoization. Gate the byte-compat assertion: if the Wave-0 disposition was "no re-vectorization," assert cosine drift vs a committed Pinecone reference vector is within the locked bar; if it was "drift accepted / out of scope," assert only dimension + normalization and reference the SPIKE disposition in a test comment.</action>
  <verify>
    <automated>node --test tests/test-rs-embed-cjs.cjs</automated>
  </verify>
  <done>tests pass: 1024-dim normalized embeddings produced in-process, singleton memoized, warm-cache + progress surface working per Wave-0 UX. grep confirms zero child_process/.py in rs-embed.cjs. Byte-compat assertion matches the SPIKE disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rs-embed.cjs -> HF model CDN | First-run downloads ONNX weights (model weights only, never room text) |
| LOCAL room text -> embedder | Room text is embedded IN-PROCESS and never leaves the machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-06 | Information Disclosure | in-process embedding of room text | accept | Canon Part 8 preserved: embeddings run LOCALLY in-process; only the model WEIGHTS are fetched from the CDN, room text never egresses. Asserted by a test that the embedder makes no outbound request carrying input text. |
| T-134-07 | Denial of Service | cold model load (~10-15s, ~200MB+) | mitigate | Singleton memoization + the Wave-0 hook-timeout budget; ensureModel() warm-up off the hot path if the cold load overruns ~30s. |
| T-134-08 | Tampering | embedding drift breaks similarity vs Pinecone index | mitigate | Byte-compat test gated on the Wave-0 measured drift; re-vectorization scoped OUT to Phase 127.1 if drift exceeds the bar (CONTEXT Out-of-scope). |
</threat_model>

<verification>
- `node --test tests/test-rs-embed-cjs.cjs` passes.
- `grep -nE "child_process|spawn|execFile|sentence_transformers|\\.py" lib/core/rs-embed.cjs` returns nothing.
- `grep -nE "feature-extraction|multilingual-e5-large" lib/core/rs-embed.cjs` matches.
</verification>

<success_criteria>
The in-process e5-large embedder replaces both Python embedding paths, produces 1024-dim normalized vectors, handles first-run download UX per the Wave-0 decision, and preserves Canon Part 8 (room text never egresses).
</success_criteria>

<output>
Create `.planning/phases/134-cjs-port-of-python-analyzers-via-xenova-transformers-elimina/134-03-SUMMARY.md` when done.
</output>
