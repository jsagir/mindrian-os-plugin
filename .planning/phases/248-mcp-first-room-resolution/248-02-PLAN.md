---
phase: 248-mcp-first-room-resolution
plan: 02
type: execute
wave: 2
depends_on: [248-01]
files_modified:
  - tests/test-248-room-bind-honest-return.cjs
  - lib/mcp/tool-router.cjs
  - tests/test-248-surface-probes.cjs
  - tests/run-all-248.sh
  - tests/test-room-bind-stdio-session-fallback.cjs
  - tests/test-room-bind-health-signal.cjs
  - CHANGELOG.md
  - docs/ENV-TUNING.md
  - docs/CANON-PHASE-MAP.md
  - .planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md
  - .planning/debug/knowledge-base.md
autonomous: false
requirements: [CTX-02, CTX-03]
notes: >
  langtalks grounding status (honest record, per the phase constraint): unavailable to the
  researcher AND to the planner (re-attempted at plan time 2026-08-10; no ToolSearch
  primitive and no mcp__langtalks-graph-expert__* functions exposed to the planning
  session). Recorded here rather than skipped silently. Executor MAY attempt
  relationship_path ("session state" -> "context binding") if the tools are live in its
  session; "not in corpus yet" is a valid outcome. Grounding rests on first-party RCA +
  source evidence, re-verified at plan time.
  Task-count note: this plan carries 4 tasks instead of the standard 2-3 because the phase
  is capped at 2 plans (navigator constraint) and the close-out (Task 4) may only run AFTER
  the human checkpoint (Task 3) approves the live CLI after-leg; Tasks 3 and 4 are each
  near-zero context cost, so the plan stays inside the ~50% budget.
must_haves:
  truths:
    - "room_bind to a real room returns effective:true plus the exact resolved_dir subsequent reads in that session will use"
    - "room_bind to a slug with no directory on disk returns effective:false with reason room_not_on_disk - the unqualified {ok:true, bound:true} about an inert effect cannot be reproduced"
    - "A second room_bind in the same session takes effect for subsequent reads (the 2026-07-29 first-bind-remnant repro is dead)"
    - "room_bind never moves the machine-wide registry active pointer (the rejected write-through cannot regress in)"
    - "Two concurrent sessions each resolve their OWN binding (isolation)"
    - "The carried defect closes with a live CLI before/after plus scripted Desktop/Cowork surface-equivalents; real-host Desktop/Cowork confirmation is a stated deferral to v2.0.0-beta release pickup, per the release-liveness hard rule"
  artifacts:
    - path: "tests/test-248-room-bind-honest-return.cjs"
      provides: "Honest-return contract: effective / resolved_dir / resolved_source / reason, two-bind sequence, two-session isolation, registry-untouched assert"
    - path: "tests/test-248-surface-probes.cjs"
      provides: "Scripted five-step probe on CLI-, Desktop-, and Cowork-equivalent transports (the CTX-03 merge gate)"
    - path: ".planning/debug/resolved/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md"
      provides: "The carried defect, resolved, with code-vs-wire preamble and per-surface evidence"
  key_links:
    - from: "lib/mcp/tool-router.cjs room_bind handler"
      to: "lib/mcp/session-room.cjs"
      via: "post-write round-trip through the shared resolver (write end and read end verified against each other per call)"
      pattern: "resolveMcpSessionRoom\\("
    - from: "tests/test-248-surface-probes.cjs"
      to: "bin/mindrian-mcp-server.cjs"
      via: "fresh spawn per probe (release-liveness rule: never a long-lived process)"
      pattern: "mindrian-mcp-server"
---

<objective>
Make room_bind HONEST (the return half of CTX-02) and close the carried defect with the
CTX-03 before/after (live on CLI; scripted surface-equivalents for Desktop/Cowork as the
merge gate; real-host confirmation deferred to release pickup, stated, per the
fix-not-live-until-released hard rule).

After plan 248-01, the binding actually applies - but room_bind still returns an
unconditional {ok:true, bound:true} even when the bound slug has no directory on disk
(writeSessionBinding accepts any traversal-safe slug; core registryRoomPath's existsSync
gate then fails Leg A and reads fall to reg.active). The fix: after writing, round-trip
through the SAME shared resolver with the SAME sessionId and report what a subsequent read
will actually see. This round-trip doubles as an in-process seam-liveness proof - the write
end and the read end are verified against each other on every call.

Rejected alternative (recorded per phase constraint, research pitfall 6): the RCA's
short-term patch - room_bind writing through to the global registry `active` field - is
REJECTED. One session's bind would clobber every concurrent session's Leg B, reintroducing
the machine-wide race fixed by 0bec81b9/PSB. The structural fix (plan 248-01) makes it
unnecessary. This plan additionally asserts, in test, that room_bind leaves registry.json
`active` untouched, so the rejected patch cannot regress in unnoticed.

Out-of-scope flag carried from the RCA (do NOT absorb): the STATE.md null-bytes wrinkle
(suspect: state-ops.cjs:44 non-atomic computeState write). UNCONFIRMED; if the CTX-03
probes trip it again, it earns its OWN debug file - note it, never fold it in.

Output: honest room_bind return, honest-return + isolation tests, scripted three-surface
probe, live CLI after-leg via human checkpoint, defect file moved to resolved/ with
code-vs-wire preamble, CHANGELOG/ENV-TUNING/CANON-PHASE-MAP updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/248-mcp-first-room-resolution/248-RESEARCH.md
@.planning/phases/248-mcp-first-room-resolution/248-01-SUMMARY.md
@.planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md
@lib/mcp/tool-router.cjs
@lib/mcp/session-room.cjs
@lib/core/session-binding.cjs
@tests/test-room-bind-stdio-session-fallback.cjs
@docs/RCA-TEMPLATE.md

## Interface contracts (verified against local HEAD 2026-08-10)

room_bind handler: lib/mcp/tool-router.cjs:1643-1700 (registration comment block from 1611).
Three return paths: explicit bind (line 1678), cwd auto-bind (line 1690), ambiguity signal
{ok:true, bound:false, needs_binding_card:true} (line 1698). Session id already resolved at
1669 via resolveEffectiveSessionId(sessionId, extra). The registerRouterTools closure has
`roomDir` (boot fallback) and `surface` in scope - use them for the round-trip ctx.
persistBindHealth (1632-1641) stays as-is, before the round-trip.

Born-wired connector + F.8 HITL declaration: tool-router.cjs:1840-1848 (tool 'room_bind',
hitl_shape F.8). The new response fields are ADDITIVE, so the CIRS declaration needs no
shape change - but verify with `node scripts/build-connector-registry.cjs --check`.

Per-surface transport mechanics (research CTX-03 table):
| Surface | Transport | Session id source |
|---------|-----------|-------------------|
| CLI | stdio | CLAUDE_CODE_SESSION_ID env (SDK never populates extra.sessionId on stdio - RCA-confirmed) |
| Desktop | stdio, non-TTY child | same env var; surface via CLAUDE_SURFACE=desktop |
| Cowork | Streamable HTTP (MINDRIAN_TRANSPORT=http) | SDK extra.sessionId per connection |

Raw stdio JSON-RPC drive precedent: tests/test-room-bind-stdio-session-fallback.cjs.
Release-liveness (personal-memory hard rule + 4 proven occurrences): a main commit is NOT
live until a release ships AND is picked up; a running session never hot-reloads. Every
"after" probe spawns a FRESH server from this repo's fixed code.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Honest room_bind return via post-write round-trip</name>
  <files>tests/test-248-room-bind-honest-return.cjs, lib/mcp/tool-router.cjs, tests/test-room-bind-stdio-session-fallback.cjs, tests/test-room-bind-health-signal.cjs, tests/run-all-248.sh</files>
  <behavior>
    tests/test-248-room-bind-honest-return.cjs (hermetic fixture per the 248-01 pattern:
    tmp MINDRIAN_ROOMS_HOME, flag + CLAUDE_ACTIVE_ROOM cleared, registry active='room-c',
    rooms room-c/room-x/room-y on disk; drive room_bind through the same in-process seam
    test-room-bind-stdio-session-fallback.cjs uses):
    - Test 1 (honest success): bind room-x with sessionId s1 -> response has ok:true,
      bound:true, primary:'room-x', effective:true, resolved_dir === room-x abs path,
      resolved_source:'session.primary', no reason field.
    - Test 2 (the lie is dead): bind 'no-such-room' (safe slug, no dir on disk) with s2 ->
      ok:true, bound:true (the write DID happen) BUT effective:false,
      reason:'room_not_on_disk', resolved_dir === what reads will actually use (room-c via
      reg.active), resolved_source:'reg.active'.
    - Test 3 (two-bind sequence, the 2026-07-29 repro): s3 binds room-x then room-y ->
      second response effective:true with resolved_dir === room-y; a subsequent read
      through session-room.cjs with s3 resolves room-y, not the first bind's remnant.
    - Test 4 (two-session isolation): s4 binds room-x, s5 binds room-y -> each session's
      round-trip and subsequent reads resolve its OWN room.
    - Test 5 (rejected write-through cannot regress in): after all binds, registry.json
      `active` still equals 'room-c' byte-identically.
    - Test 6 (ambiguity branch untouched): no room arg, no .room-root above cwd, sessionId
      s6 -> {ok:true, bound:false, needs_binding_card:true} EXACTLY as before (no new
      fields on this branch).
  </behavior>
  <action>
    Author the test first; confirm red (the current handler returns no effective field).
    Then modify ONLY the room_bind handler in lib/mcp/tool-router.cjs (1643-1700): after
    each writeSessionBinding + persistBindHealth on the explicit (1675-1679) and cwd
    (1686-1691) paths, round-trip:
    `const check = require('./session-room.cjs').resolveMcpSessionRoom({ sessionId:
    effectiveSessionId, ctx: { fallbackRoomDir: roomDir, surface } });`
    then `const effective = check.source === 'session.primary' && check.slug === boundSlug;`
    and extend the returned JSON additively with: effective, resolved_dir: check.dir,
    resolved_source: check.source, and (only when !effective) reason - 'room_not_on_disk'
    when the bound slug has no resolvable directory (check.slug !== boundSlug or
    check.source !== 'session.primary' after a successful write of a safe slug means
    registryRoomPath's existsSync gate failed), else 'binding_not_effective' as the honest
    catch-all (e.g. writeSessionBinding silently no-oped on an unsafe slug). Keep the
    needs_binding_card branch (1698) and the F.8 connector descriptor (1840-1848)
    byte-unchanged. Do NOT write to the global registry anywhere in this handler (the
    rejected write-through; Test 5 is the tripwire). No em-dashes in any added comment.
    Then run tests/test-room-bind-stdio-session-fallback.cjs and
    tests/test-room-bind-health-signal.cjs: the new fields are additive, so these should
    stay green; if either does exact-match response asserts, re-point it to include the new
    fields, preserving its original defect coverage (never delete asserts). Flip the
    honest-return leg in tests/run-all-248.sh from run_if-SKIP to live (it now exists).
  </action>
  <verify>
    <automated>node tests/test-248-room-bind-honest-return.cjs && node tests/test-room-bind-stdio-session-fallback.cjs && node tests/test-room-bind-health-signal.cjs && node scripts/build-connector-registry.cjs --check && bash tests/run-all-248.sh</automated>
  </verify>
  <done>All six behaviors green. ROADMAP SC2's bar met in-process: the unqualified {ok:true, bound:true} about an inert effect cannot be reproduced. Connector registry check green (additive shape, no descriptor change). Commits: test(248-02) red first, then feat(248-02).</done>
</task>

<task type="auto">
  <name>Task 2: CTX-03 scripted surface-equivalent probes + doc updates</name>
  <files>tests/test-248-surface-probes.cjs, tests/run-all-248.sh, CHANGELOG.md, docs/ENV-TUNING.md, docs/CANON-PHASE-MAP.md</files>
  <action>
    Author tests/test-248-surface-probes.cjs - the scripted CTX-03 merge gate. Fixture per
    the research recipe: tmp MINDRIAN_ROOMS_HOME with two real rooms (room-x, room-y) plus
    a third (room-other); registry `active` pointed at room-other (the stale-pointer
    fixture); MINDRIAN_MCP_FIRST unset. Each probe spawns a FRESH
    bin/mindrian-mcp-server.cjs process (release-liveness rule) and drives the five-step
    sequence over raw JSON-RPC (clone the transport plumbing from
    tests/test-room-bind-stdio-session-fallback.cjs):
      1. room_bind({room:'room-x'}) -> ok:true AND effective:true AND resolved_dir ends
         with /room-x.
      2. room_state_bound in the SAME session -> room_dir === room-x path (before the fix
         this resolved the stale global/boot fallback - the defect's exact signature).
      3. room_bind({room:'room-y'}) then room_state_bound -> room-y (first-bind-remnant
         regression case).
      4. Fresh UNBOUND session control -> resolves room-other (reg.active, byte-identical
         legacy for the never-bound user).
      5. Assert registry.json active still 'room-other' (no write-through).
    Three surface-equivalent legs:
    - CLI-equivalent: stdio spawn with CLAUDE_CODE_SESSION_ID set in the child env.
    - Desktop-equivalent: stdio spawn with CLAUDE_SURFACE=desktop + CLAUDE_CODE_SESSION_ID
      (spawned pipes are already non-TTY, matching surface-detect rung 4).
    - Cowork-equivalent: MINDRIAN_TRANSPORT=http daemon spawn; open TWO concurrent HTTP
      sessions, bind each to a different room, prove each session's reads resolve its OWN
      binding (extra.sessionId per connection - the isolation proof stdio cannot give).
    Kill every spawned process in finally; generous-but-bounded timeouts; skip-with-loud-
    message (not silent pass) if the http transport cannot start in the sandbox, so a
    skipped leg is a stated call, never an oversight (Tri-Polar rule). Wire the leg into
    tests/run-all-248.sh.
    Doc updates (the release-independent close-out prep):
    - CHANGELOG.md: Fixed entry under the v2.0.0-beta Unreleased heading naming the RCA
      slug, the nine-copy collapse, the honest room_bind return, and the doctrine change
      (bound sessions escape flag-off legacy parity; unbound sessions keep it).
    - docs/ENV-TUNING.md: amend the MINDRIAN_MCP_FIRST entry - the room-RESOLUTION path no
      longer consults it (Phase 248); its remaining live consumers are daemon lifecycle /
      registration in bin/mindrian-mcp-server.cjs (verified at plan time: lines 130, 226,
      234, 237). Re-grep at execution (`grep -rn "isMcpFirst(" lib/ bin/ --include=*.cjs`)
      and list the survivors in the doc - a flag whose documented meaning silently changed
      is this repo's known bug class (research Open Question 2 / D-07 amendment).
    - docs/CANON-PHASE-MAP.md: Phase 248 row, canon_parts [7, 8, 9, 11] - reuse of the core
      ladder, LOCAL-only resolution, no room.db in the resolver, room_bind's honest-success
      contract under CIRS.
  </action>
  <verify>
    <automated>node tests/test-248-surface-probes.cjs && bash tests/run-all-248.sh && node scripts/doctor.cjs --acceptance && grep -q "Phase 248" docs/CANON-PHASE-MAP.md && grep -qi "no longer" docs/ENV-TUNING.md</automated>
  </verify>
  <done>All three surface-equivalent probe legs pass the five-step sequence against freshly spawned servers (or a leg SKIPs loudly with the environment reason recorded). run-all-248.sh fully live, doctor --acceptance green, all three docs updated. Commit as feat(248-02): CTX-03 scripted gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live CLI before/after (the CTX-03 human leg)</name>
  <what-built>Plans 248-01 + 248-02 tasks 1-2: the nine-copy collapse, unconditional binding reads, honest room_bind return, and scripted probes green on CLI-, Desktop-, and Cowork-equivalent transports. The BEFORE leg is already on the record in the RCA (Evidence 2026-07-28 03:40-03:55: bind reported success, reads resolved /home/jsagi/room; plus the 2026-07-29 two-bind remnant repro) - you do not need to re-run broken code.</what-built>
  <how-to-verify>
    1. Start a FRESH Claude Code CLI session in /home/jsagi/dev/MindrianOS-Plugin (fresh
       session = the MCP server spawns fresh from the fixed dev-repo code via .mcp.json;
       this satisfies the release-liveness rule without waiting on the v2.0.0-beta train.
       Your CURRENT session's server predates the fix and will NOT show it - do not test
       here).
    2. Fixture: `bash scripts/room-registry set-active <some-room-that-is-NOT-mindrianOS>`
       (a real room, e.g. jonathan-sagir), so the global pointer is deliberately stale
       relative to the bind target.
    3. In the fresh session call room_bind({room: "mindrianOS"}) -> expect ok:true AND
       effective:true AND resolved_dir "/home/jsagi/MindrianRooms/mindrianOS".
    4. Call room_state_bound (or suggest_next / reach_candidates for the sensor-spine leg)
       in the SAME session -> expect room_dir "/home/jsagi/MindrianRooms/mindrianOS", NOT
       the room you set active in step 2 (the before-behavior).
    5. Re-bind: room_bind({room: "jonathan-sagir"}), then room_state_bound -> expect
       jonathan-sagir's path (the 2026-07-29 first-bind-remnant case, now dead).
    6. Optional negative: room_bind({room: "no-such-room"}) -> expect effective:false,
       reason "room_not_on_disk".
    If STATE.md content looks corrupted (null bytes) during step 4, that is the separate
    UNCONFIRMED state-ops wrinkle - report it, it gets its OWN debug file, it does not
    block this checkpoint.
  </how-to-verify>
  <resume-signal>Type "approved" (all steps matched) or describe exactly which step diverged and what you saw.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Close the carried defect on the record</name>
  <files>.planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md, .planning/debug/knowledge-base.md</files>
  <action>
    Runs ONLY after Task 3's "approved". Update the RCA file per docs/RCA-TEMPLATE.md:
    - frontmatter status: diagnosed -> resolved; updated timestamp.
    - Resolution block: root_cause stands as written; fix = Phase 248 plans 01+02 (nine-copy
      collapse into lib/mcp/session-room.cjs with UNCONDITIONAL session-binding reads +
      honest room_bind round-trip return); note the census correction (nine copies, not
      eight - stop-gate-handler.cjs:78 was the missed ninth; tools/stop-gate.cjs has no
      copy) and that the short-term registry write-through patch was REJECTED (machine-wide
      race; test-248-room-bind-honest-return.cjs Test 5 is the standing tripwire);
      files_changed and commit hashes from plans 01+02.
    - verification: Source-of-Truth Preamble discipline (the RCA's own template): CODE =
      dev repo local HEAD at the fix commits; WIRE = (a) scripted five-step probes against
      fresh spawns on CLI-, Desktop-, and Cowork-equivalent transports
      (tests/test-248-surface-probes.cjs), (b) the live CLI after-leg from Task 3 with the
      navigator's approval and observed payloads. Desktop and Cowork REAL-HOST legs:
      recorded as a STATED DEFERRAL (Tri-Polar rule - a skip is a stated call) to v2.0.0-beta
      release pickup per the fix-not-live-until-released hard rule; scripted equivalents
      stand as the merge evidence. Add a "release pickup TODO" line naming the two deferred
      legs so verify-work and the release checklist can find them.
    - Move the file to .planning/debug/resolved/ (git mv; .planning/ is gitignored so stage
      with git add -f, per the repo's QA/RCA convention).
    - Append the knowledge-base.md summary block (slug, one-paragraph root cause, fix
      locus, the nine-copy census lesson: "count function definitions, not the
      fallbackRoomDir token").
    - Out-of-repo follow-up, recorded in the SUMMARY as a handoff note rather than done
      here if the directory is unavailable: the rethinking-mindrianos room entry
      (~/MindrianRooms/rethinking-mindrianos/research/2026-07-28-room-bind-session-scope-
      ignored-mcp-first-off/) still says Phase 237 owns the structural fix; it needs the
      Phase 248 correction per the Dev-Research Compositing rule.
  </action>
  <verify>
    <automated>test -f .planning/debug/resolved/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md && ! test -f .planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md && grep -q "status: resolved" .planning/debug/resolved/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md && grep -q "room-bind-mcp-first-off" .planning/debug/knowledge-base.md</automated>
  </verify>
  <done>Defect file resolved and moved with code-vs-wire preamble, deferred real-host legs named as a release-pickup TODO, knowledge-base block appended, room-side correction done or handed off explicitly. Commit as docs(248-02): close the carried defect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> room_bind handler | untrusted room slug + sessionId params cross here |
| MCP server -> machine-wide registry.json | shared mutable state concurrent sessions race on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-248-04 | Tampering | room_bind write-through to global registry (rejected patch regressing in) | mitigate | handler never touches registry.json; test-248-room-bind-honest-return.cjs Test 5 + surface-probe step 5 assert `active` unchanged after binds |
| T-248-05 | Information disclosure | cross-session bleed under concurrent http sessions | mitigate | Cowork-equivalent probe: two concurrent sessions, each resolves its OWN binding |
| T-248-06 | Repudiation | "fixed" claimed against a stale running server | mitigate | every probe spawns fresh; live leg runs in a fresh CLI session; real-host Desktop/Cowork recorded as stated deferral to release pickup |
| T-248-SC | Tampering | npm/pip/cargo installs | accept | zero package installs this phase (research Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- tests/test-248-room-bind-honest-return.cjs green (six behaviors incl. registry-untouched
  and isolation).
- tests/test-248-surface-probes.cjs green on all three surface-equivalent legs (or loud
  stated SKIP with reason).
- bash tests/run-all-248.sh fully live and green; node scripts/build-connector-registry.cjs
  --check green; node scripts/doctor.cjs --acceptance green.
- Task 3 checkpoint approved with the live CLI after-leg observed.
- Defect file in .planning/debug/resolved/ with status: resolved and the deferral TODO.
</verification>

<success_criteria>
- ROADMAP SC2 fully met: room_bind is authoritative regardless of MINDRIAN_MCP_FIRST state
  and returns an honest result, including the room-not-on-disk edge; the unqualified
  {ok:true, bound:true} about an inert effect cannot be reproduced.
- ROADMAP SC3 met at merge scope: live before/after on CLI plus scripted Desktop/Cowork
  surface-equivalents against fresh spawns; real-host Desktop/Cowork confirmation is a
  named, tracked deferral to v2.0.0-beta release pickup - never an implied "done".
- The carried defect is closed on the record: resolved file, knowledge-base block,
  CHANGELOG, ENV-TUNING flag semantics, CANON-PHASE-MAP parts 7/8/9/11.
</success_criteria>

<output>
Create `.planning/phases/248-mcp-first-room-resolution/248-02-SUMMARY.md` when done.
Record: the honest-return field semantics as shipped, any re-pointed response-shape tests,
per-leg probe transcripts (or stated SKIP reasons), the checkpoint outcome verbatim, the
deferred real-host legs, and the room-side compositing handoff status.
</output>
