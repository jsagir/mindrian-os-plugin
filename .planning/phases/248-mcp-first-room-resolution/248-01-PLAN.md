---
phase: 248-mcp-first-room-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-248-resolver-census.cjs
  - tests/test-248-room-bind-session-authoritative.cjs
  - tests/run-all-248.sh
  - lib/mcp/session-room.cjs
  - lib/mcp/tools/room.cjs
  - lib/mcp/tools/gate.cjs
  - lib/mcp/tools/sensors.cjs
  - lib/mcp/tools/status.cjs
  - lib/mcp/tools/graph.cjs
  - lib/mcp/tools/views.cjs
  - lib/mcp/tools/chain.cjs
  - lib/mcp/stop-gate-handler.cjs
  - lib/mcp/tool-router.cjs
  - tests/test-198-flag-off-parity.test.cjs
autonomous: true
requirements: [CTX-01, CTX-02]
notes: >
  langtalks grounding status (honest record, per the phase constraint): the researcher's
  toolset lacked mcp__langtalks-graph-expert__*; the PLANNER re-attempted at plan time
  (2026-08-10) and the tools are absent from the planner's toolset as well (no ToolSearch
  primitive, no langtalks MCP functions exposed to the planning session). Not skipped by
  judgment - tool unavailable twice. Grounding therefore rests on first-party RCA + direct
  source evidence, which is strong here (every claim in this plan re-verified against local
  HEAD at plan time). If the executor's session HAS the langtalks tools live, it MAY fire
  relationship_path point-to-point ("session state" -> "context binding" and "session state"
  -> "multi-agent orchestration") before Task 2 and record the outcome in the SUMMARY;
  "not in corpus yet" is a valid outcome and changes nothing in this plan.
must_haves:
  truths:
    - "With MINDRIAN_MCP_FIRST unset, a session that called room_bind resolves every subsequent MCP read and write in that session to the bound room"
    - "An unbound session resolves byte-identically to legacy: reg.active, then the boot-time fallback"
    - "Reintroducing an independent gate-then-fallthrough resolver copy anywhere under lib/mcp/ turns tests/test-248-resolver-census.cjs red"
  artifacts:
    - path: "lib/mcp/session-room.cjs"
      provides: "The ONE shared MCP room resolver (read + forWrite), flag-free, structured {dir, slug, source} return"
      exports: ["resolveMcpSessionRoom", "resolveSessionRoomDir"]
      min_lines: 60
    - path: "tests/test-248-resolver-census.cjs"
      provides: "Source-grep tripwire: red on any tenth copy (rar.11 precedent)"
    - path: "tests/test-248-room-bind-session-authoritative.cjs"
      provides: "Flag-off bound-session authority proof through both seam families"
    - path: "tests/run-all-248.sh"
      provides: "Phase 248 aggregator (Wave 0 run_if contract, run-all-237.sh precedent)"
  key_links:
    - from: "lib/mcp/tools/*.cjs (7 files)"
      to: "lib/mcp/session-room.cjs"
      via: "require of the shared resolver"
      pattern: "require\\(['\"]\\.\\./session-room\\.cjs['\"]\\)"
    - from: "lib/mcp/tool-router.cjs and lib/mcp/stop-gate-handler.cjs"
      to: "lib/mcp/session-room.cjs"
      via: "require of the shared resolver"
      pattern: "require\\(['\"]\\./session-room\\.cjs['\"]\\)"
    - from: "lib/mcp/session-room.cjs"
      to: "lib/core/resolve-active-room.cjs"
      via: "UNCONDITIONAL resolveSessionRoom call (reads) / resolveWriteRoom call (forWrite)"
      pattern: "resolveSessionRoom\\(|resolveWriteRoom\\("
---

<objective>
Collapse the NINE independent gate-then-fallthrough room-resolver copies in lib/mcp/ into
ONE shared module (lib/mcp/session-room.cjs) whose read path calls core resolveSessionRoom
UNCONDITIONALLY. That un-gating IS the CTX-02 fix: today every copy only consults the
session binding inside `if (isMcpFirst(surface))`, and MINDRIAN_MCP_FIRST is unset on every
install, so room_bind's write is never read. Unbound sessions stay byte-identical legacy
(core Leg B IS resolveActiveRoom). A source-grep census test turns red if a tenth copy ever
appears.

Purpose: CTX-01 (one ladder, census-gated) and the mechanism half of CTX-02 (bind is
authoritative for its session regardless of flag state). The honest room_bind return and
CTX-03 live verification are plan 248-02.

Census correction honored (research finding, re-verified at plan time 2026-08-10): NINE
copies, not the ROADMAP's eight. `lib/mcp/tools/stop-gate.cjs` has NO copy (it delegates);
the ninth copy is `lib/mcp/stop-gate-handler.cjs:78` (null floor, missed by the RCA's
fallbackRoomDir grep). Plans touching exactly 8 files are the research's named warning sign.

Doctrine change, named explicitly (research pitfall 3, never slipped in): this plan
deliberately REPEALS the Phase 198 SPEC-7 "flag-off ignores sessionId entirely" contract
for BOUND sessions. Flag-off + UNBOUND stays byte-identical legacy. Flag-off + BOUND now
resolves the binding. tests/test-198-flag-off-parity.test.cjs is re-pointed in the same
commit with this reasoning written into the test file itself.

Rejected alternative (recorded per phase constraint): the RCA's short-term patch (room_bind
write-through to the global registry `active` field) is REJECTED. It would make one
session's bind clobber every concurrent session's Leg B, reintroducing the machine-wide
race the concurrent-session-collision RCA fixed via 0bec81b9/PSB. The structural collapse
makes it unnecessary. Do not ship both; do not ship it at all.

Output: session-room.cjs, nine re-routed files, three new test artifacts, re-pointed parity
test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/248-mcp-first-room-resolution/248-RESEARCH.md
@.planning/debug/room-bind-mcp-first-off-falls-back-to-stale-global-active-room.md
@lib/core/resolve-active-room.cjs
@lib/core/session-binding.cjs
@lib/mcp/mcp-first-flag.cjs
@lib/mcp/tool-router.cjs
@lib/mcp/tools/room.cjs
@lib/mcp/stop-gate-handler.cjs
@tests/test-198-flag-off-parity.test.cjs
@tests/test-224-resolver-fallback.cjs
@tests/run-all-237.sh

## Interface contracts (verified against local HEAD 2026-08-10 - do not re-derive)

Core ladder (lib/core/resolve-active-room.cjs, UNTOUCHED by this plan):
- `resolveSessionRoom({sessionId, home, requireOwnership})` at line 481. Leg A:
  session.primary via readSessionBinding -> registryRoomPath (exists-on-disk gated,
  sub-room safe). Leg B: reg.active via resolveActiveRoom (demoted, honors
  CLAUDE_ACTIVE_ROOM env + sealed/exists gates). Returns {slug, abs_path, source} or null.
  Never throws. requireOwnership stays OPT-IN OFF this phase (research Open Question 1).
- `resolveWriteRoom({filePath, sessionId, home})` at line 416. Leg 1: `.room-root` cwd
  walk-up (source 'room-root'), then delegates to resolveSessionRoom for legs A/B.

Session id: `resolveEffectiveSessionId(explicit, extra)` (lib/core/session-binding.cjs:139).
Already called at every MCP tool call site; this plan does not touch session-id handling.

The NINE copies (definition line / call sites, re-verified at plan time; chain.cjs drifted
from the research's 452 to 511, and tool-router gained a FIFTH call site at 1233):

| # | File | Def | Call sites | Variant |
|---|------|-----|-----------|---------|
| 1 | lib/mcp/tools/room.cjs | 47 | 245, 271 | canonical; floor ctx.fallbackRoomDir \|\| cwd |
| 2 | lib/mcp/tools/gate.cjs | 56 | 232 | canonical |
| 3 | lib/mcp/tools/sensors.cjs | 58 | 197, 225, 248, 281, 315 | canonical |
| 4 | lib/mcp/tools/status.cjs | 71 | 165 | canonical |
| 5 | lib/mcp/tools/graph.cjs | 112 | 216, 250, 285 | canonical |
| 6 | lib/mcp/tools/views.cjs | 102 | 236, 260 | canonical |
| 7 | lib/mcp/tools/chain.cjs | 126 | 511 | canonical |
| 8 | lib/mcp/tool-router.cjs resolveWriteTargetDir | 116-136 | 693 (room_state READS), 801, 823, 840 (room_content writes), 1233 (eureka compute write target) | (sessionId, fallbackRoomDir, surface) signature; MCP_FIRST_DEPRECATED_ACTIVE_WRITE stderr token (const at 102, emit at 125) |
| 9 | lib/mcp/stop-gate-handler.cjs | 78 | 452 | floors to NULL (no fallback); surface defaults 'cli' |

Test/export seams to KEEP as thin delegates (re-point, never delete): room.cjs:315,
status.cjs:204, graph.cjs:337, views.cjs:302, sensors.cjs:425, chain.cjs:570, gate.cjs:286
(_internal.resolveSessionRoomDir), stop-gate-handler.cjs:585 (top-level export),
tool-router.cjs _test.resolveWriteTargetDir (block at 1854).

Imports that become dead after re-routing (drop ONLY the unused names; graph/status/views
keep isWritePathEnabled): room.cjs:24+26, gate.cjs:36+38, sensors.cjs:39+41, status.cjs:36+38,
graph.cjs:44+46, views.cjs:49+51, chain.cjs:105+107, stop-gate-handler.cjs:57+58,
tool-router.cjs:55+57. Grep each file for remaining uses before dropping any import.

Not copies - do NOT touch as part of the collapse: lib/mcp/tools/stop-gate.cjs (delegates
to stop-gate-handler), scripts/intent-classifier.cjs:950 (hook-side, already composes core
resolveSessionRoom correctly; census scopes to lib/mcp/ so it is auto-whitelisted).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 red gates - census tripwire, authority test, phase runner</name>
  <files>tests/test-248-resolver-census.cjs, tests/test-248-room-bind-session-authoritative.cjs, tests/run-all-248.sh</files>
  <behavior>
    test-248-resolver-census.cjs (source-grep tripwire, rar.11 precedent from
    tests/test-resolve-active-room-canonical.cjs; comment-stripping via the stripComments
    helper pattern at tests/test-224-resolver-fallback.cjs:39 - grep gate hygiene: NEVER
    grep raw source, header prose self-invalidates):
    - Census rule 1: `function resolveSessionRoomDir` appears in exactly ONE file under
      lib/mcp/ (recursive, *.cjs): lib/mcp/session-room.cjs.
    - Census rule 2: zero executable-line occurrences of `isMcpFirst(` in any file under
      lib/mcp/ EXCEPT lib/mcp/mcp-first-flag.cjs (its own definition + the internal
      isWritePathEnabled call at line 118). Verified at plan time: the only current
      executable consumers inside lib/mcp/ are the nine copies themselves, so post-collapse
      this rule is exact. If the executor finds a genuinely legitimate new consumer, it is
      whitelisted IN THE TEST with a dated comment, never silently.
    - Census rule 3: zero executable-line occurrences of `resolveWriteRoom(` or
      `resolveActiveRoom(` in any file under lib/mcp/ EXCEPT lib/mcp/session-room.cjs.
    - Seam-liveness leg (phase constraint: use the Phase 235 helper where it fits): read
      lib/core/seam-liveness.cjs first; register a claim set where claims = the nine former
      call-site files and isLive = comment-stripped source contains the session-room.cjs
      require token. If the helper's API does not fit a source-grep probe cleanly, keep the
      plain grep asserts and record the fallback decision in a test-file comment - the grep
      is sufficient; the helper is preferred, not mandatory.
    test-248-room-bind-session-authoritative.cjs (the RCA's Test 1, driven through the two
    EXISTING seam families so it needs zero new code to run red today):
    - Hermetic fixture cloned from tests/test-198-flag-off-parity.test.cjs lines 26-42:
      tmpdir MINDRIAN_ROOMS_HOME; delete MINDRIAN_MCP_FIRST and CLAUDE_ACTIVE_ROOM;
      registry.json {active: 'room-c', rooms: {'room-c': {abs_path}, 'room-x': {abs_path}}};
      both room dirs really on disk; a separate fallback dir.
    - writeSessionBinding('sess-a', {primary: 'room-x', bound: ['room-x']}) via
      lib/core/session-binding.cjs.
    - Assert 1 (grouped-router family): toolRouter._test.resolveWriteTargetDir('sess-a',
      fallback, 'cli') === room-x abs path.
    - Assert 2 (tools/ family): require('../lib/mcp/tools/sensors.cjs')
      ._internal.resolveSessionRoomDir('sess-a', {fallbackRoomDir: fallback, surface: 'cli'})
      === room-x abs path.
    - Assert 3 (unbound legacy parity): same calls with 'sess-b' (no binding written)
      resolve room-c (reg.active), byte-identical legacy.
    - Assert 4 (stop-gate-handler family): require('../lib/mcp/stop-gate-handler.cjs')
      .resolveSessionRoomDir('sess-a', {surface: 'cli'}) === room-x abs path, and with a
      sessionId that has no binding AND an empty registry it returns null (the null-floor
      compat contract).
    - Restore all env vars in finally (the parity test demonstrates the pattern).
  </behavior>
  <action>
    Author both tests with node:assert/strict, no runner dependency (repo convention), no
    em-dashes. Author tests/run-all-248.sh cloned from tests/run-all-237.sh (run/run_if
    helpers byte-identical, Wave 0 contract): every 248 leg is run_if gated on
    lib/mcp/session-room.cjs existing, so at THIS commit the runner exits clean with SKIPs
    while the two tests, run directly, are RED. Add run_if legs now for: the two tests
    above, tests/test-248-room-bind-honest-return.cjs and tests/test-248-surface-probes.cjs
    (authored in plan 248-02; run_if on their own file paths), plus always-run legacy legs:
    tests/test-198-flag-off-parity.test.cjs, tests/test-tool-router-active-room-misroute.cjs,
    tests/test-room-state-active-room-misroute.cjs,
    tests/test-room-state-no-registry-regression.cjs,
    tests/test-resolve-active-room-canonical.cjs. chmod +x the runner.
  </action>
  <verify>
    <automated>node tests/test-248-resolver-census.cjs; test $? -ne 0 && node tests/test-248-room-bind-session-authoritative.cjs; test $? -ne 0 && bash tests/run-all-248.sh</automated>
  </verify>
  <done>Both new tests exist and FAIL red at this commit for the right reasons (census counts nine copies and no session-room.cjs; authority test proves the binding is ignored flag-off). run-all-248.sh exits 0 with SKIPs for the 248 legs (Wave 0 contract) and green legacy legs. Commit as test(248-01): red gates first.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/mcp/session-room.cjs - the ONE shared resolver</name>
  <files>lib/mcp/session-room.cjs</files>
  <action>
    Create lib/mcp/session-room.cjs as a PEER of mcp-first-flag.cjs (never under tools/ -
    the register-core-tools.cjs disjoint-file contract forbids tools/ siblings requiring
    each other; requiring a lib/mcp parent-level module is the established pattern).

    Export resolveMcpSessionRoom(opts) where opts = {sessionId, ctx, forWrite, noFloor}:
    - Reads (forWrite falsy): call core resolveSessionRoom({sessionId, home:
      process.env.MINDRIAN_ROOMS_HOME}) UNCONDITIONALLY - no isMcpFirst import anywhere in
      this module, no surface consultation; the flag and the surface are simply not inputs
      to room resolution anymore. This single call replaces BOTH the flag-gated branch AND
      the global fallthrough of every copy, because core Leg B IS resolveActiveRoom.
    - forWrite true: call core resolveWriteRoom({sessionId, home:
      process.env.MINDRIAN_ROOMS_HOME}) instead, preserving the `.room-root` leg-1 cwd
      walk-up for writes exactly as the flag-ON path does today (phase constraint). The
      room-root leg must NEVER leak into reads - core resolveSessionRoom exists precisely
      to exclude it (its own header, lines 459-464).
    - Deprecation token: when forWrite is true AND the resolved source is 'reg.active',
      write to stderr the BYTE-IDENTICAL line the 198-02 acceptance greps:
      'MCP_FIRST_DEPRECATED_ACTIVE_WRITE' + ' session=' + String(sessionId) + ' room=' +
      slug + '\n', inside try/catch (stderr failure never blocks). Define the constant in
      this module; current source of the format: tool-router.cjs:102 and :125. Never rename
      the token.
    - Floor: on core miss, dir = (ctx && ctx.fallbackRoomDir) with source 'boot-fallback',
      else process.cwd() with source 'cwd'. When noFloor is true, return
      {dir: null, slug: null, source: 'none'} instead (stop-gate-handler compat).
    - Return {dir, slug, source} always; source is one of 'session.primary', 'room-root'
      (forWrite only), 'reg.active', 'boot-fallback', 'cwd', 'none'. Never throws: wrap the
      core calls in try/catch and fall to the floor on any error (mirrors every copy's
      discipline today).
    - Options-object signature ONLY, no positional variant internally (the 2026-07-11
      positional-args regression on the tool-router:693 call site made sessionId land where
      fallback belonged; make it unrepeatable).

    Export resolveSessionRoomDir(sessionId, ctx) as the back-compat string wrapper: returns
    resolveMcpSessionRoom({sessionId, ctx}).dir, floored exactly as the nine copies floor
    today. This is the drop-in the tools/ modules re-export.

    Header comment must state: what this module is (the ONE MCP room resolver, Phase 248
    CTX-01/CTX-02), the doctrine change (binding reads are unconditional; MINDRIAN_MCP_FIRST
    is no longer consulted on the resolution path; its remaining consumers are daemon
    lifecycle/registration in bin/mindrian-mcp-server.cjs), Canon Parts 7/8/9 (reuse of the
    core ladder; LOCAL fs only, zero network; never opens room.db), and the census contract
    (test-248-resolver-census.cjs turns red on any second ladder). No em-dashes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),os=require('os'),path=require('path');const home=fs.mkdtempSync(path.join(os.tmpdir(),'sr248-'));const rx=path.join(home,'room-x');fs.mkdirSync(rx,{recursive:true});fs.mkdirSync(path.join(home,'.rooms'),{recursive:true});fs.writeFileSync(path.join(home,'.rooms','registry.json'),JSON.stringify({active:'',rooms:{'room-x':{abs_path:rx}}}));process.env.MINDRIAN_ROOMS_HOME=home;delete process.env.MINDRIAN_MCP_FIRST;delete process.env.CLAUDE_ACTIVE_ROOM;const sb=require('./lib/core/session-binding.cjs');sb.writeSessionBinding('s1',{primary:'room-x',bound:['room-x']});const sr=require('./lib/mcp/session-room.cjs');const a=sr.resolveMcpSessionRoom({sessionId:'s1',ctx:{}});const b=sr.resolveMcpSessionRoom({sessionId:'s2',ctx:{fallbackRoomDir:'/tmp/fb'}});const c=sr.resolveMcpSessionRoom({sessionId:'s2',noFloor:true});const assert=require('assert');assert.strictEqual(a.dir,rx);assert.strictEqual(a.source,'session.primary');assert.strictEqual(b.dir,'/tmp/fb');assert.strictEqual(b.source,'boot-fallback');assert.strictEqual(c.dir,null);assert.strictEqual(c.source,'none');console.log('session-room sanity OK')"</automated>
  </verify>
  <done>Module loads; bound session resolves session.primary with the real dir; unbound session floors honestly with source 'boot-fallback'; noFloor returns null/'none'; zero isMcpFirst references in the file. Commit as feat(248-01): the shared resolver.</done>
</task>

<task type="auto">
  <name>Task 3: Route all nine copies through session-room.cjs and re-point the parity doctrine</name>
  <files>lib/mcp/tools/room.cjs, lib/mcp/tools/gate.cjs, lib/mcp/tools/sensors.cjs, lib/mcp/tools/status.cjs, lib/mcp/tools/graph.cjs, lib/mcp/tools/views.cjs, lib/mcp/tools/chain.cjs, lib/mcp/stop-gate-handler.cjs, lib/mcp/tool-router.cjs, tests/test-198-flag-off-parity.test.cjs</files>
  <action>
    Seven tools/ modules (room 47, gate 56, sensors 58, status 71, graph 112, views 102,
    chain 126): delete the local `function resolveSessionRoomDir` body; add
    `const { resolveSessionRoomDir } = require('../session-room.cjs');` at the top. Every
    call site (room 245+271, gate 232, sensors 197/225/248/281/315, status 165, graph
    216/250/285, views 236/260, chain 511) and every _internal export (room 315, gate 286,
    sensors 425, status 204, graph 337, views 302, chain 570) then works UNCHANGED - the
    imported name IS the seam, so existing test consumers stay green without edits. Do not
    rename call sites; do not touch stop-gate.cjs (it has no copy).

    stop-gate-handler.cjs: delete the local function at 78-91; replace with a thin delegate
    preserving its two variant behaviors: `const sessionRoom = require('./session-room.cjs');
    const resolveSessionRoomDir = (sessionId, ctx) => sessionRoom.resolveMcpSessionRoom({
    sessionId, ctx, noFloor: true }).dir;` (null floor kept; the old surface-defaults-'cli'
    line is dead weight now that resolution is surface-free - drop it, note it in the
    SUMMARY). Keep the module.exports entry at 585 pointing at the delegate. Call site 452
    unchanged.

    tool-router.cjs: rewrite resolveWriteTargetDir (116-136) as a thin delegate KEEPING its
    exact signature so all call sites and the _test export survive:
    `function resolveWriteTargetDir(sessionId, fallbackRoomDir, surface) { return
    require('./session-room.cjs').resolveMcpSessionRoom({ sessionId, ctx: { fallbackRoomDir,
    surface }, forWrite: true }).dir; }`. The MCP_FIRST_DEPRECATED_ACTIVE_WRITE constant and
    emission now live in session-room.cjs (Task 2); remove the local const at 102 and the
    emit at 125 ONLY after grepping the repo for other consumers of the constant
    (`grep -rn MCP_FIRST_DEPRECATED_ACTIVE_WRITE lib/ tests/ scripts/ bin/`); if anything
    imports it from tool-router, re-export it from session-room.cjs instead.
    Then split reads from writes per research pitfall 9: call site 693 serves room_state's
    five READ branches - change it to
    `require('./session-room.cjs').resolveSessionRoomDir(sessionId, { fallbackRoomDir:
    roomDir })` so reads stop inheriting the room-root write leg. Call sites 801, 823, 840
    (room_content writes) and 1233 (eureka compute write target) stay on
    resolveWriteTargetDir (forWrite semantics). Keep _test.resolveWriteTargetDir exported.

    Import hygiene, per file: after re-routing, grep the file for remaining uses of
    isMcpFirst / resolveWriteRoom / resolveActiveRoom; drop each import name that is now
    unused (room 24+26, gate 36+38, sensors 39+41, chain 105+107, stop-gate-handler 57+58,
    tool-router 55+57 candidates; status 38, graph 46, views 51 KEEP isWritePathEnabled and
    drop only isMcpFirst; status 36, graph 44, views 49 drop the core-resolver import if
    unused). The census test (rules 2+3) is the arbiter: it must go green with zero
    whitelist additions.

    Re-point tests/test-198-flag-off-parity.test.cjs - the named doctrine change, in the
    test file itself, not a commit message aside: rewrite the header comment to state that
    Phase 248 (CTX-02) deliberately repeals SPEC-7's "flag-off ignores sessionId entirely"
    for BOUND sessions, while flag-off + UNBOUND (and no .room-root above cwd) remains
    byte-identical legacy - reference this plan and the RCA slug. Update the assertions:
    keep every unbound-session parity assert as-is; flip the "a bound sessionId must NOT
    change the outcome" asserts to assert the bound session DOES resolve its binding
    (expect the bound room's abs path). Do not delete the test; coverage is kept, the
    contract is amended.
  </action>
  <verify>
    <automated>node tests/test-248-resolver-census.cjs && node tests/test-248-room-bind-session-authoritative.cjs && node tests/test-198-flag-off-parity.test.cjs && node tests/test-tool-router-active-room-misroute.cjs && node tests/test-room-state-active-room-misroute.cjs && node tests/test-room-state-no-registry-regression.cjs && node tests/test-resolve-active-room-canonical.cjs && bash tests/run-all-248.sh</automated>
  </verify>
  <done>Census red gates from Task 1 are GREEN (one ladder, nine call-site families routed). Authority test green through all three seam families. Re-pointed parity test green with the doctrine change written in-file. Misroute/no-registry regression suites green (re-point an assert ONLY if it enforces the old flag-gated contract, preserving each test's original defect coverage; record any such re-point in the SUMMARY). rar.* canonical tripwires green. Commit as feat(248-01): nine-copy collapse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> tool handler | untrusted room slug + sessionId params cross here |
| Session binding files on disk | $MINDRIAN_ROOMS_HOME/.rooms/sessions/*.json keyed by sessionId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-248-01 | Tampering | bound slug -> path traversal | mitigate | no new path construction: session-room.cjs resolves ONLY through core registryRoomPath containment; isSafeSlug already gates every slug at write (session-binding.cjs, Security V5) - census rule 3 prevents any bypass ladder |
| T-248-02 | Information disclosure | cross-session room bleed | mitigate | session-keyed binding files + core Leg A precedence; asserted by test-248-room-bind-session-authoritative.cjs (sess-a bound vs sess-b unbound) and extended two-bound-session isolation in plan 248-02 |
| T-248-03 | Spoofing | CLAUDE_CODE_SESSION_ID env-trusted on stdio | accept | local trust boundary, same trust class as CLI stdout (research Security Domain); out of scope by design |
| T-248-SC | Tampering | npm/pip/cargo installs | accept | zero package installs this phase (research Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- tests/test-248-resolver-census.cjs green: exactly one ladder under lib/mcp/, zero
  isMcpFirst on the resolution path, zero direct core-resolver imports outside
  session-room.cjs.
- tests/test-248-room-bind-session-authoritative.cjs green: bound session wins flag-off
  through tool-router._test, tools/_internal, and stop-gate-handler seams; unbound session
  is byte-identical legacy; stop-gate-handler null-floor preserved.
- Re-pointed tests/test-198-flag-off-parity.test.cjs green with the doctrine amendment in
  the file header.
- bash tests/run-all-248.sh green (plan-02 legs still SKIP by run_if - expected).
- grep -rn "MCP_FIRST_DEPRECATED_ACTIVE_WRITE" lib/mcp/session-room.cjs returns the
  byte-identical token.
</verification>

<success_criteria>
- ROADMAP SC1 met with the corrected census: ONE shared ladder is the only resolver; all
  NINE former copies (7x tools/*, tool-router resolveWriteTargetDir, stop-gate-handler)
  route through lib/mcp/session-room.cjs; reintroducing a copy turns
  test-248-resolver-census.cjs red.
- CTX-02 mechanism half: with MINDRIAN_MCP_FIRST unset, an explicit room_bind is
  authoritative for every subsequent read and write in its session; unbound sessions are
  byte-identical legacy.
- The doctrine change is on the record inside test-198-flag-off-parity.test.cjs, and the
  registry write-through alternative is rejected on the record in this plan.
</success_criteria>

<output>
Create `.planning/phases/248-mcp-first-room-resolution/248-01-SUMMARY.md` when done.
Record: the nine-file diff map, any census whitelist entries (should be zero), any
misroute-suite re-points, the langtalks attempt outcome if the executor's session had the
tools, and the seam-liveness helper decision (used or grep-fallback, with reason).
</output>
