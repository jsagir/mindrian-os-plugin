---
phase: 204-ignite-room-chooser-persona-entry
plan: 04
type: execute
wave: 2
depends_on: [204-01, 204-02]
files_modified:
  - lib/core/ignite-branch-gate.cjs
  - evals/plurai/09-ignite-branch-routing.csv
  - evals/plurai/README.md
  - evals/plurai/204-baseline.json
  - tests/test-204-ignite-branch-gate.cjs
autonomous: true
requirements: [IGNT-01, IGNT-02, IGNT-03, IGNT-04]
canon_parts: [3, 8]

must_haves:
  truths:
    - "The four ignite branches (room-chooser skip/fire, Just Talk, resume-with-register, render-through-188) have a deterministic, offline-checkable correctness contract, not just prose"
    - "A trace that resumes a room without applying its stored persona register is caught as incorrect"
    - "A trace that forces a room birth from the Just Talk pick is caught as incorrect"
    - "A trace that fires Gate B0 against an empty (or all-archived) registry is caught as incorrect"
    - "A trace that renders a Gate B0 card without going through the pickShape SEED-020 door is caught as incorrect"
  artifacts:
    - path: "lib/core/ignite-branch-gate.cjs"
      provides: "Local deterministic classifier reproducing the Plurai judge verdict offline (mirrors lib/core/ralph-loop-gate.cjs)"
      exports: ["classifyIgniteBranch", "HARD_VIOLATIONS"]
    - path: "evals/plurai/09-ignite-branch-routing.csv"
      provides: "Plurai AI-Judge training/eval samples for the Phase 204 branch-routing contract"
    - path: "evals/plurai/204-baseline.json"
      provides: "Deferred hand-labeled CI baseline (mirrors evals/plurai/196-baseline.json and 201-baseline.json)"
    - path: "tests/test-204-ignite-branch-gate.cjs"
      provides: "Parity test: local gate label matches the CSV Label column on every row"
  key_links:
    - from: "tests/test-204-ignite-branch-gate.cjs"
      to: "lib/core/ignite-branch-gate.cjs"
      via: "classifyIgniteBranch(JSON.parse(row.sample)).label === row.label on every CSV row"
      pattern: "classifyIgniteBranch"
    - from: "evals/plurai/README.md"
      to: "evals/plurai/09-ignite-branch-routing.csv"
      via: "new table row 09 naming the CSV, the canon contract, and the judge prompt"
      pattern: "09-ignite-branch-routing"
---

<objective>
Ship the Plurai eval GATE leg for Phase 204: a hand-labeled Plurai AI-Judge CSV plus a LOCAL,
deterministic parity classifier that reproduces the same correct/incorrect verdict offline (Canon
Part 8: Plurai itself is build/CI-time only, never a runtime dependency). This mirrors the shipped
pattern in `lib/core/ralph-loop-gate.cjs` + `evals/plurai/08-ralph-loop-behavior.csv` +
`evals/plurai/201-baseline.json` + `tests/test-201-ralph-loop-gate.cjs` (Phase 201), and
`evals/plurai/196-baseline.json` (Phase 196) for the deferred-baseline shape. No CONTEXT.md exists
for this phase; the ROADMAP.md Phase 204 entry IS the spec -- this plan turns its four numbered
branches into a deterministic, offline-checkable correctness contract rather than leaving them as
prose-only claims.

Purpose: Plans 204-01/204-02/204-03 WIRE the four branches; this plan proves, with a reproducible
CI gate, that the four hard invariants those branches must never violate are ENFORCED, not merely
described. The four invariants (see Task 1 behavior spec) map 1:1 onto the four ROADMAP.md bullets:
skip Gate B0 with no prior rooms (branch 1), never force a birth from Just Talk (branch 2), never
resume a room without its stored persona register (branch 3), never render a Gate B0 card outside
the pickShape SEED-020 door (branch 4).
Output: `lib/core/ignite-branch-gate.cjs`, `evals/plurai/09-ignite-branch-routing.csv`, an updated
`evals/plurai/README.md`, `evals/plurai/204-baseline.json`, and
`tests/test-204-ignite-branch-gate.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@lib/core/ralph-loop-gate.cjs
@evals/plurai/08-ralph-loop-behavior.csv
@evals/plurai/README.md
@evals/plurai/201-baseline.json
@tests/test-201-ralph-loop-gate.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/core/ignite-branch-gate.cjs -- local deterministic branch-routing classifier</name>
  <files>lib/core/ignite-branch-gate.cjs, tests/test-204-ignite-branch-gate.cjs</files>
  <read_first>
    lib/core/ralph-loop-gate.cjs in full (the exact structural pattern to mirror: a frozen
    HARD_VIOLATIONS array of name/hit(trace) predicate objects, a classify function that collects
    every violation name and returns label correct when zero violations else incorrect plus the
    violations array, pure and total, never throws on a malformed trace)
    tests/test-201-ralph-loop-gate.cjs in full (the exact CSV-parity test shape: a hand-rolled
    quoted-CSV parser respecting double-quote-escaped JSON cells, an assertion that both labels are
    present and row count is at least 16, and a loop asserting the local gate's label matches the
    CSV row's label for every row)
  </read_first>
  <behavior>
    - Trace shape: has_resumable_rooms (bool), gate_b0_fired (bool), pick (one of 'room',
      'just_talk', 'free_text', or null), birthed_new_room (bool), resumed_without_register (bool),
      rendered_via_pickshape (bool).
    - HARD_VIOLATIONS (frozen array, five entries, mirroring the ralph-loop-gate.cjs shape):
      1. gate_fired_with_no_rooms -- hits when has_resumable_rooms is false AND gate_b0_fired is
         true (ROADMAP branch 1: if no prior rooms, skip straight to the birth/talk branches).
      2. resume_without_register -- hits when pick is 'room' AND resumed_without_register is true
         (ROADMAP branch 3: resuming a room must apply its own stored persona register).
      3. resume_births_new_room -- hits when pick is 'room' AND birthed_new_room is true (resuming
         an existing room must never re-run B1/B2/B3).
      4. just_talk_births_room -- hits when pick is 'just_talk' AND birthed_new_room is true
         (ROADMAP branch 2: capture/build is offered later via an F-gate, never forced).
      5. not_rendered_via_pickshape -- hits when gate_b0_fired is true AND rendered_via_pickshape is
         false (ROADMAP branch 4: every branch renders through the 188 selector-dispatcher door,
         never a bespoke widget).
    - classifyIgniteBranch(trace, optsIgnored): pure and total (never throws; a non-object trace
      degrades to an all-fields-false shape before evaluation). Returns an object with label
      ('correct' when zero violations, else 'incorrect') and violations (the array of hit names).
  </behavior>
  <action>
    Write tests/test-204-ignite-branch-gate.cjs FIRST (RED), parsing
    evals/plurai/09-ignite-branch-routing.csv (Task 2 of this plan authors that CSV; write the test
    against the row shape documented in this task's behavior spec so the two tasks compose) with the
    same quoted-CSV parser test-201-ralph-loop-gate.cjs uses, asserting row count is at least 16,
    both labels are present, and parity between classifyIgniteBranch and every row's Label column.
    Then implement lib/core/ignite-branch-gate.cjs per the behavior spec (GREEN), as a pure CJS
    module with zero requires (mirrors ralph-loop-gate.cjs exactly: no dependencies at all, just the
    five-predicate array and the classify function). No em-dashes in either file.
  </action>
  <verify>
    <automated>node tests/test-204-ignite-branch-gate.cjs</automated>
  </verify>
  <done>tests/test-204-ignite-branch-gate.cjs exits 0 once evals/plurai/09-ignite-branch-routing.csv exists (Task 2); lib/core/ignite-branch-gate.cjs exports exactly classifyIgniteBranch and HARD_VIOLATIONS.</done>
</task>

<task type="auto">
  <name>Task 2: evals/plurai/09-ignite-branch-routing.csv + README row + 204-baseline.json</name>
  <files>evals/plurai/09-ignite-branch-routing.csv, evals/plurai/README.md, evals/plurai/204-baseline.json</files>
  <read_first>
    evals/plurai/08-ralph-loop-behavior.csv in full (the exact CSV format: header Sample, Label,
    Reasoning; Sample is a JSON-encoded object wrapped in double quotes with internal double quotes
    doubled per RFC 4180; Label is correct or incorrect; Reasoning is one sentence)
    evals/plurai/README.md in full (the table row format to append a row 09 in; the Judge model and
    Canon Part 8 rule sections at the top are UNCHANGED, do not touch them)
    evals/plurai/201-baseline.json and evals/plurai/196-baseline.json in full (the exact deferred
    hand-labeled baseline JSON shape: schema_version, artifact_kind, runtime_loaded, method,
    baseline_deferred, deferred_note, source_csv, judge_prompt, canon_contract, synthetic_only,
    precision/recall/accuracy/f1, row_count, correct_count, incorrect_count, local_gate,
    local_gate_accuracy, label_semantics)
  </read_first>
  <action>
    Create evals/plurai/09-ignite-branch-routing.csv with the header Sample,Label,Reasoning
    followed by 18 rows over the trace shape from Task 1's behavior spec: 8 correct rows (a
    no-prior-rooms skip, a clean room resume with its register applied, a Just Talk pick with no
    birth, a Free-Text pick with no birth, a second clean resume, an all-archived-registry skip, a
    second Just Talk trace, and a closing clean resume) and 10 incorrect rows, one per
    HARD_VIOLATIONS predicate plus combinations (Gate B0 fired against an empty registry; a resume
    that skips the stored register; a resume that re-births the room; a Just Talk pick that forces a
    birth; a Gate B0 render that bypasses pickShape; two double-violation rows; a triple-violation
    row; and two repeats varying the Reasoning text). Every row's Sample cell is a JSON object with
    exactly the six trace fields, double-quote-escaped per RFC 4180 exactly like
    08-ralph-loop-behavior.csv, and every row's Reasoning names which ROADMAP.md Phase 204 branch
    (1-4) the row is proving.

    Append one new row to the table in evals/plurai/README.md: row 09, file
    09-ignite-branch-routing.csv, use case "Behavior / Loop Correctness" mirroring row 08's use case,
    canon contract "ROADMAP.md Phase 204 (ignite front door branches 1-4); Canon Part 3, Part 8, Part
    10, Part 12", and a judge prompt reading: "Given an ignite front-door branch-routing trace,
    classify it correct (Gate B0 only fires when prior resumable rooms exist, a resumed room applies
    its own stored persona register and is never re-birthed, Just Talk never forces a room birth, and
    every fired gate renders through the pickShape SEED-020 door) or incorrect (any of those
    invariants breached). Local parity gate: lib/core/ignite-branch-gate.cjs."

    Create evals/plurai/204-baseline.json mirroring the exact shape of 201-baseline.json: method
    hand-labeled, baseline_deferred true, a deferred_note citing the same reasoning (Plurai's
    interactive multi-turn MCP-tool flow cannot run non-interactively in the sequential executor;
    rows are hand-labeled deterministically from the Label column; the local gate
    lib/core/ignite-branch-gate.cjs classifyIgniteBranch reproduces every label exactly, parity
    proven by tests/test-204-ignite-branch-gate.cjs; re-run /evals:eval interactively after
    /reload-plugins to replace this with a hosted Plurai baseline), source_csv
    evals/plurai/09-ignite-branch-routing.csv, the same judge_prompt as the README row, canon_contract
    "ROADMAP.md Phase 204; Canon Part 3, Part 8, Part 10, Part 12", synthetic_only true,
    precision/recall/accuracy/f1 all 1, row_count 18, correct_count 8, incorrect_count 10, local_gate
    lib/core/ignite-branch-gate.cjs, local_gate_accuracy 1, and the same label_semantics object
    (correct maps to pass, incorrect maps to flag).

    No em-dashes anywhere in any of the three files.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); JSON.parse(fs.readFileSync('evals/plurai/204-baseline.json','utf8')); const csv=fs.readFileSync('evals/plurai/09-ignite-branch-routing.csv','utf8').trim().split(String.fromCharCode(10)); if (csv.length < 17) { throw new Error('expected at least 16 data rows, got ' + (csv.length-1)); } console.log('OK ' + (csv.length-1) + ' rows');"</automated>
  </verify>
  <done>evals/plurai/09-ignite-branch-routing.csv has 18 data rows with both labels present; evals/plurai/204-baseline.json parses as valid JSON with the documented fields; evals/plurai/README.md's table has a row 09; tests/test-204-ignite-branch-gate.cjs (Task 1) now passes against this CSV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| evals/plurai/*.csv -> Plurai hosted service (future, interactive, out of scope here) | Only synthetic/dogfood samples ever leave the machine when a human later runs /evals:eval interactively; this plan never calls the hosted service |
| CSV Label column -> local gate parity assertion | The CSV is hand-authored input to a test; a mislabeled row would silently certify a wrong classifier if the parity test itself were malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-204-08 | Tampering | evals/plurai/09-ignite-branch-routing.csv content | mitigate | Every sample is a synthetic trace of boolean/enum scalars only (Canon Part 8: "every sample in these files is synthetic or dogfood"); no real room content, no real navigator artifacts, no personal identifiers |
| T-204-09 | Repudiation | The deferred 204-baseline.json overstates confidence | accept | deferred_note explicitly states the Plurai baseline was NOT taken from the live hosted judge, mirroring the honest disclosure already shipped in 196-baseline.json / 201-baseline.json |
| T-204-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan (a pure CJS classifier + a CSV + two JSON/markdown edits) |
</threat_model>

<verification>
- `node tests/test-204-ignite-branch-gate.cjs` exits 0.
- `bash tests/run-all-204.sh` shows 4 PASS, 0 SKIP (the full Phase 204 aggregator, once all four plans have landed).
- Grep sweep: zero em-dash matches across lib/core/ignite-branch-gate.cjs, evals/plurai/09-ignite-branch-routing.csv, evals/plurai/README.md, evals/plurai/204-baseline.json, tests/test-204-ignite-branch-gate.cjs.
- `node -e "JSON.parse(require('node:fs').readFileSync('evals/plurai/204-baseline.json','utf8'))"` exits 0 (valid JSON).
</verification>

<success_criteria>
lib/core/ignite-branch-gate.cjs exists and is proven, via tests/test-204-ignite-branch-gate.cjs, to
reproduce the Plurai judge's correct/incorrect verdict on every row of
evals/plurai/09-ignite-branch-routing.csv. evals/plurai/README.md and evals/plurai/204-baseline.json
carry the Phase 204 eval leg in the exact shape established by Phases 196/201. No new Brain wire, no
new npm dependency, no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/204-ignite-room-chooser-persona-entry/204-04-SUMMARY.md` when done
</output>
