---
phase: 204-ignite-room-chooser-persona-entry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/session-register.cjs
  - tests/test-204-session-register.cjs
autonomous: true
requirements: [IGNT-03]
canon_parts: [8, 9, 12]

must_haves:
  truths:
    - "The functional persona captured at ignite (role_blend) resolves to a single top-weight register the rest of the session can read"
    - "A cold-start session (no role_blend, or all weights zero) resolves to no register (null), never a fabricated default persona"
    - "The register resolver never re-hardcodes the 7-key role vocabulary or the persona_variants copy; it reuses the frozen sources of truth"
  artifacts:
    - path: "lib/core/session-register.cjs"
      provides: "role_blend -> session-register resolver (Phase 204 roadmap branch 3, persona-differentiated dialogue)"
      exports: ["resolveTopRole", "displayNameForRole", "composePersonaContext", "resolveSessionRegister"]
    - path: "tests/test-204-session-register.cjs"
      provides: "Unit tests for the resolver, including cold-start and tie-break cases"
  key_links:
    - from: "lib/core/session-register.cjs"
      to: "lib/core/persona-override.cjs"
      via: "reuses the frozen ROLE_BLEND_KEYS array; never re-declares the 7-key role list"
      pattern: "ROLE_BLEND_KEYS"
    - from: "lib/core/session-register.cjs"
      to: "lib/core/persona-taxonomy.cjs"
      via: "reuses ROLE_BLEND_AXES (positionally aligned with ROLE_BLEND_KEYS) for the display-form string"
      pattern: "ROLE_BLEND_AXES"
---

<objective>
Build the pure resolver for Phase 204 roadmap branch (3): "the persona chosen at ignite (Phase 115
role_blend -> persona_variants: founder / researcher / investor / operator / mentor / domain_expert
/ student) SETS THE CONVERSATION REGISTER for the session". No CONTEXT.md exists for this phase;
the ROADMAP.md Phase 204 entry IS the spec (requirement ID IGNT-03, one of four IDs this plan set
derives 1:1 from the roadmap's four numbered bullets, since no explicit `**Requirements:**` line
exists).

Purpose: Phase 115 already threads `role_blend` end-to-end into `USER.md` at `birthRoom` time
(lib/core/navigation/room-birth.cjs), and Phase 88.2-03 already shipped an UNWIRED `personaContext`
parameter on `renderShapeF1` (lib/hmi/shape-f1-renderer.cjs: "the dispatcher supplies the
pre-computed string from readUserMd().role_blend per Phase 115 D-12") -- but no caller in the repo
has ever computed that string. This plan builds exactly that missing resolver, reusing the frozen
`ROLE_BLEND_KEYS` (lib/core/persona-override.cjs) and `ROLE_BLEND_AXES` (lib/core/persona-taxonomy.cjs)
vocabularies rather than re-declaring the 7-role list a third time. This is the FUNCTIONAL-role axis
(founder/researcher/operator/investor/mentor/domain_expert/student, picked explicitly at ignite's
B1 Door 1/2/3) -- it is deliberately orthogonal to and does NOT touch the separate COMPETENCE-level
axis (student/practitioner/researcher/professor) that Phase 205-05 already ships in
persona-taxonomy.cjs (`ROLE_LEVELS` / `resolveElevationLean`). Building a second elevation-ratio
table here would duplicate that shipped axis; this resolver stays scoped to the functional-role
register only.
Output: `lib/core/session-register.cjs` (pure resolver) and its unit tests. Plan 204-03 (Wave 2)
wires this resolver's output into `commands/ignite.md`'s B1/B3 gates; this plan does not touch
ignite.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/MINDRIAN-CANON.md

@lib/core/persona-override.cjs
@lib/core/persona-taxonomy.cjs
@lib/core/lens-engine.cjs
@lib/hmi/shape-f1-renderer.cjs
@agents/larry-extended.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/core/session-register.cjs -- role_blend session-register resolver</name>
  <files>lib/core/session-register.cjs, tests/test-204-session-register.cjs</files>
  <read_first>
    lib/core/persona-override.cjs (the frozen `ROLE_BLEND_KEYS` array, exactly 7 entries: founder,
    researcher, operator, investor, mentor, domain_expert, student -- import and reuse this array
    verbatim, do not copy the string literals into the new module)
    lib/core/persona-taxonomy.cjs (the frozen `ROLE_BLEND_AXES` array: Founder, Researcher, Operator,
    Investor, Mentor, Domain Expert, Student -- POSITIONALLY ALIGNED, same index order, with
    ROLE_BLEND_KEYS; also read the separate `ROLE_LEVELS` / `ROLE_LEVEL_ELEVATION_LEAN` /
    `resolveElevationLean` section and its header comment -- that is the Phase 205-05 COMPETENCE
    axis; this plan must NOT touch or duplicate it)
    lib/core/lens-engine.cjs `readRoleBlend(roomDir)` (the existing precedent for reading role_blend
    off USER.md via user-md-ops -- reuse this exact defensive-null pattern for any USER.md read in
    this module's tests/fixtures, but this module itself takes a role_blend OBJECT as input, it does
    not read USER.md directly -- keep it a pure function so callers can feed it either a live
    readUserMd() result or a fixture)
    agents/larry-extended.md frontmatter `persona_variants:` (the 10-key map: default + founder +
    researcher + researcher_ind + founder_grant + investor + operator + mentor + domain_expert +
    student -- this module's `resolveSessionRegister` return value carries the `persona_variants`
    KEY only, e.g. `'founder'`, never the copy string itself; the copy stays single-sourced in
    larry-extended.md, read by the caller at render time, not duplicated here)
  </read_first>
  <behavior>
    - `resolveTopRole(role_blend)`: pure. `role_blend` is expected to be an object keyed by the 7
      ROLE_BLEND_KEYS with numeric weights (the exact shape `emptyUser().role_blend` in
      user-md-ops.cjs produces). Returns the key (from ROLE_BLEND_KEYS, in ROLE_BLEND_KEYS
      declaration order for tie-breaking) with the strictly-greatest numeric weight. Returns `null`
      when `role_blend` is not a plain object, has no numeric weights, or every weight is `<= 0`
      (cold start -- never fabricate a default persona).
    - `displayNameForRole(roleKey)`: pure. Looks up `roleKey`'s index in `ROLE_BLEND_KEYS` and
      returns the SAME index of `ROLE_BLEND_AXES` (e.g. `'domain_expert'` -> `'Domain Expert'`).
      Returns `null` on an unknown/absent key. Do not hardcode a second key->display mapping table;
      derive it from the two frozen arrays' positional alignment.
    - `composePersonaContext(role_blend)`: pure. Returns `displayNameForRole(resolveTopRole(role_blend))`
      -- i.e. `null` on cold start, else the display-form string ready to hand straight into
      `renderShapeF1`'s `personaContext` parameter (which itself no-ops on `null`/empty, per its own
      "Cold-start... preserves the prior behavior byte-for-byte" contract -- this function's cold
      -start `null` is exactly what that parameter expects).
    - `resolveSessionRegister(role_blend)`: pure. Returns `null` on cold start (`resolveTopRole` is
      `null`). Otherwise returns `{ role_key: <the ROLE_BLEND_KEYS member>, display_name:
      <displayNameForRole output>, persona_variant_key: <role_key, identity-mapped -- the 7
      ROLE_BLEND_KEYS values are already 7 of the 10 keys in larry-extended.md's persona_variants
      map>, weight: <the winning numeric weight> }`. This is the full "session register" object
      Plan 204-03 threads into ignite.md's B1/B3 wiring and into the room-resume greeting.
  </behavior>
  <action>
    Implement lib/core/session-register.cjs per the behavior spec as a pure CJS module: two
    requires only (`./persona-override.cjs` for ROLE_BLEND_KEYS, `./persona-taxonomy.cjs` for
    ROLE_BLEND_AXES), zero I/O, zero npm deps. Write tests/test-204-session-register.cjs first
    (RED): cover a clear single-winner role_blend, a tie between two roles (asserts the
    ROLE_BLEND_KEYS declaration-order tie-break), an all-zero role_blend (null), a missing/undefined
    role_blend (null), a malformed role_blend (a string, an array -- null, never throws),
    displayNameForRole on every one of the 7 valid keys plus one unknown key (null), and
    resolveSessionRegister's full shape on a representative role_blend (asserting all four returned
    fields). Then implement the module (GREEN). No em-dashes anywhere in either file.
  </action>
  <verify>
    <automated>node tests/test-204-session-register.cjs</automated>
  </verify>
  <done>tests/test-204-session-register.cjs exits 0; lib/core/session-register.cjs exports exactly the four documented functions; a manual `node -e` smoke call with role_blend `{founder:0.2, investor:0.8, ...}` returns `resolveSessionRegister` with `role_key:'investor'`, `display_name:'Investor'`, `persona_variant_key:'investor'`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| USER.md role_blend -> in-memory register | role_blend is navigator-set (via ignite's B1 pick, or the persona-override sentinel file) LOCAL data; this module treats it as untrusted-shape input (may be malformed) but never untrusted-CONTENT input (it is a 7-key numeric weight map, not free text) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-204-04 | Denial of Service | resolveTopRole / resolveSessionRegister on a malformed role_blend | mitigate | Every function returns `null` on any non-plain-object, missing-key, or non-numeric input rather than throwing; asserted directly by the RED test cases |
| T-204-05 | Information Disclosure | composePersonaContext output surfaced in a rendered card header | accept | The display string is a generic role label ("Investor", "Founder"...) already chosen by the navigator themselves; it renders LOCAL-only (Part 8) and is never queried against Brain |
| T-204-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan (pure CJS, two in-repo requires only) |
</threat_model>

<verification>
- `node tests/test-204-session-register.cjs` exits 0.
- Grep sweep: `grep -n '—' lib/core/session-register.cjs tests/test-204-session-register.cjs` returns no matches.
- `grep -n "ROLE_BLEND_KEYS\|ROLE_BLEND_AXES" lib/core/session-register.cjs` shows both are `require`-imported, not re-declared as a new literal array.
</verification>

<success_criteria>
lib/core/session-register.cjs exists, exports the four documented functions, reuses (never
duplicates) the frozen ROLE_BLEND_KEYS/ROLE_BLEND_AXES vocabularies, and is fully covered by
tests/test-204-session-register.cjs. No new Brain wire, no new npm dependency, no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/204-ignite-room-chooser-persona-entry/204-02-SUMMARY.md` when done
</output>
