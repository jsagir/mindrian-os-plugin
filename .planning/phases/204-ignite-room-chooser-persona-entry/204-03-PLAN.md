---
phase: 204-ignite-room-chooser-persona-entry
plan: 03
type: execute
wave: 2
depends_on: [204-01, 204-02]
files_modified:
  - commands/ignite.md
  - skills/conversation-mode/SKILL.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-204-ignite-wiring-grep.cjs
autonomous: true
requirements: [IGNT-01, IGNT-02, IGNT-03, IGNT-04]
canon_parts: [3, 7, 8, 10, 11, 12]
cirs_relationship:
  surfaces_added: []
  surfaces_modified:
    - ignite (new pre-birth Gate B0 room-chooser branch; persona-register wiring threaded into the
      existing B1 Door 1 and B3 gates)
  surfaces_removed: []
  spine_consumed:
    - lib/hmi/selector-dispatcher.cjs (pickShape SEED-020 door, via lib/core/room-chooser.cjs)
    - lib/core/room-chooser.cjs (Plan 204-01)
    - lib/core/session-register.cjs (Plan 204-02)
  gate_impact: "ignite becomes the first user-facing consumer of the Phase 188 selector-dispatcher door for a pre-birth, no-room decision (Gate B0). No new reach, no new edge type, no new Brain wire; the existing born-wired ignite connector block (sensor_triggers SENS-01/SENS-06, reach_id context_block) is unchanged because Gate B0 is an internal gate on the SAME already-wired surface, not a new invocable surface."

must_haves:
  truths:
    - "A navigator with prior (non-archived) rooms sees a rendered Shape-F.1 room-chooser card on /mos:ignite instead of jumping straight into a new birth flow"
    - "Picking a prior room resumes it (registry set-active) and greets the navigator using that room's own stored persona register, without re-running the B1 persona pick"
    - "Picking 'Just talk (no room)' hands off to the existing conversation-mode Lane Picker instead of forcing a room birth"
    - "A navigator with zero prior (non-archived) rooms sees ignite's existing B1 four-door flow completely unchanged (Gate B0 is a no-op skip)"
    - "The B3 first-move dial for a freshly birthed room carries the resolved persona lens in its header"
  artifacts:
    - path: "commands/ignite.md"
      provides: "Gate B0 room-chooser section (before Entry Routing) + session-register wiring threaded into Door 1 and Gate B3"
      contains: "Gate B0"
    - path: "skills/conversation-mode/SKILL.md"
      provides: "Cross-reference from ignite's Gate B0 'Just talk (no room)' pick into the existing Lane Picker (no new lane-picker logic)"
      contains: "ignite"
    - path: "docs/CANON-PHASE-MAP.md"
      provides: "Phase 204 ledger row (status, canon_parts, one-line note), per this file's own 'If a phase implements a canon part that this map does not list, update this map in the same commit' rule"
    - path: "tests/test-204-ignite-wiring-grep.cjs"
      provides: "Prose-assertion proof the wiring landed (mirrors the 115/179 grep-test pattern)"
  key_links:
    - from: "commands/ignite.md"
      to: "lib/core/room-chooser.cjs"
      via: "Gate B0 calls renderRoomChooserCard via a node -e snippet, mirroring the existing scratchpad-ops.cjs node -e call convention"
      pattern: "room-chooser.cjs"
    - from: "commands/ignite.md"
      to: "lib/core/session-register.cjs"
      via: "resolveSessionRegister(role_blend) threaded into both Door 1's recorded answer and Gate B3's pickShape personaContext payload"
      pattern: "session-register.cjs"
    - from: "commands/ignite.md"
      to: "skills/conversation-mode/SKILL.md"
      via: "the Just Talk pick explicitly hands off to the Lane Picker, never re-implementing it"
      pattern: "conversation-mode"
---

<objective>
Wire Plans 204-01 (room-chooser.cjs) and 204-02 (session-register.cjs) into the live `/mos:ignite`
front door, delivering all four ROADMAP.md Phase 204 branches end-to-end: (1) the room-chooser
branch, (2) the "Just talk" no-room branch, (3) persona-differentiated dialogue (the session
register), and (4) every branch rendered through the Phase 188 selector-dispatcher door. No
CONTEXT.md exists for this phase; the ROADMAP.md Phase 204 entry IS the spec, and this plan is
where its prose becomes the actual front-door behavior.

Purpose: today `/mos:ignite` always starts at "## Entry Routing" (the three-door B1 birth flow) --
a navigator with five prior rooms gets no chance to resume one; they are dropped straight into a
NEW-room conversation. This plan inserts a new Gate B0 immediately ahead of Entry Routing that
reads the rooms registry (via lib/core/room-chooser.cjs), and only proceeds into the unchanged B1
flow when there is nothing to resume. It also closes the dangling Phase 88.2-03 `personaContext`
seam on `renderShapeF1` -- shipped but never called by anything in this repo -- by threading the
Plan 204-02 resolver's output into it for the first time.
Output: an ignite front door that is a live Shape-F entry per Canon Part 10, not prose; a
conversation-mode cross-reference; a CANON-PHASE-MAP.md ledger row; and a prose-assertion test
proving the wiring landed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/MINDRIAN-CANON.md

@commands/ignite.md
@commands/new-project.md
@commands/rooms.md
@skills/conversation-mode/SKILL.md
@lib/core/room-chooser.cjs
@lib/core/session-register.cjs
@lib/core/user-md-ops.cjs
@docs/CANON-PHASE-MAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Gate B0 (room-chooser) + persona-register threading into commands/ignite.md</name>
  <files>commands/ignite.md</files>
  <read_first>
    commands/ignite.md in full (187 lines: the existing Runtime, Reward-before-investment, Entry
    Routing, Gate B1 Starting Point, Gate B2 Blueprint, Gate B3 First Win, and Hooked First-Cycle
    Assembly sections -- every anchor referenced below is an exact existing heading or paragraph in
    this file)
    commands/rooms.md Subcommand: open, Step 3 (`bash scripts/room-registry set-active NAME` --
    reuse this EXACT primitive; do not invent a second way to switch the active room)
    commands/new-project.md line ~229 (the `PLUGIN_ROOT="$(dirname "$(dirname "$(readlink -f
    "$0")")")"` resolution convention) and its `node -e "const sp =
    require('$PLUGIN_ROOT/lib/core/scratchpad-ops.cjs'); ..."` call-site pattern (the exact
    node-invocation convention Gate B0 should follow for room-chooser.cjs and session-register.cjs)
    lib/core/room-chooser.cjs (Plan 204-01: listRegisteredRooms, hasResumableRooms,
    renderRoomChooserCard, resolvePick, JUST_TALK_LABEL)
    lib/core/session-register.cjs (Plan 204-02: resolveSessionRegister, composePersonaContext)
    lib/core/user-md-ops.cjs readUserMd(path) (used to pull a RESUMED room's already-stored
    role_blend off its own USER.md -- do not re-run the B1 persona pick for a resumed room)
  </read_first>
  <action>
    Insert a new "Gate B0 -- Room Chooser (F.1, pre-birth -- FIRST CONSUMER OF Phase 188)" section
    between the existing Reward-before-investment section and the existing Entry Routing heading.
    Document, in the same directive-prose style the rest of this file already uses: on every
    /mos:ignite invocation, BEFORE any Entry Routing door fires, resolve PLUGIN_ROOT (the
    new-project.md convention) and invoke lib/core/room-chooser.cjs's listRegisteredRooms plus
    hasResumableRooms via a node -e call mirroring the scratchpad-ops.cjs convention. When
    hasResumableRooms is false, SKIP Gate B0 entirely and proceed straight into the UNCHANGED Entry
    Routing section below (byte-identical current behavior; state this explicitly so a reader knows
    nothing else in this file changed for the no-prior-rooms case). When true, call
    renderRoomChooserCard(rooms) and FIRE the returned AskUserQuestion contract exactly as Door 1
    does today -- cite this file's own existing FIRE THE CARD mandatory doctrine and the SEED-021
    no-card-no-picture rule by reference rather than re-explaining them. After the navigator
    answers, call resolvePick(selectedVerb, roomByVerb) and branch three ways: (a) action resume --
    run bash scripts/room-registry set-active NAME (the exact commands/rooms.md Step 3 primitive),
    then readUserMd that room's USER.md, call resolveSessionRegister(role_blend) on the result,
    greet the navigator back into the room (name plus venture_stage plus the resolved persona
    register), and STOP -- ignite does NOT re-run B1/B2/B3 for a resumed room; the room's own
    ongoing reach machinery takes the next turn; (b) action just_talk -- do NOT birth a room;
    explicitly hand off to the conversation-mode skill's Lane Picker (cite
    skills/conversation-mode/SKILL.md by name; do not re-describe its logic here); (c) action
    free_text -- reuse this file's existing Door 4 free-text interpretation doctrine (Larry
    interprets and routes to one of resume-by-name, Just Talk, or falling through into Entry
    Routing).

    Then amend the existing Record the answer paragraph under Gate B1 (the one ending "the B1
    signal survives to B2"): immediately after it, add a short paragraph stating that once
    role_blend is captured by ANY door (1, 2, or 3), call resolveSessionRegister(role_blend)
    (lib/core/session-register.cjs) and carry its role_key forward as the SESSION's persona register
    for the rest of the conversation -- citing Canon Part 12 (each persona talked-with differently
    later: tone, depth, which reaches fire, which frameworks surface, is this exact roadmap branch 3
    claim) and naming agents/larry-extended.md's existing persona_variants[role_key] string as the
    register's voice reference (no new copy is authored; the existing 10-key map is reused
    verbatim).

    Finally amend the existing Gate B3 paragraph "After birthRoom succeeds, call pickShape('F.1')
    with the dial now LIVE" to also pass personaContext: composePersonaContext(roleBlend)
    (lib/core/session-register.cjs) into that call -- this is the Phase 88.2-03 personaContext seam
    on renderShapeF1 (shipped, never wired) getting its first real caller; note this explicitly in
    the added prose so a future reader understands why this line matters.

    No em-dashes anywhere in the new prose (CLAUDE.md hard rule; the existing file already honors
    this; keep it that way).
  </action>
  <verify>
    <automated>test "$(grep -c 'Gate B0' commands/ignite.md)" -ge 1 && test "$(grep -c 'room-chooser.cjs' commands/ignite.md)" -ge 1 && test "$(grep -c 'session-register.cjs' commands/ignite.md)" -ge 1 && echo OK</automated>
  </verify>
  <done>commands/ignite.md contains the new Gate B0 section ahead of Entry Routing, the amended Door 1 Record-the-answer paragraph citing session-register.cjs, and the amended Gate B3 pickShape call carrying personaContext; the file has zero em-dash characters.</done>
</task>

<task type="auto">
  <name>Task 2: conversation-mode cross-reference + CANON-PHASE-MAP.md row + wiring proof test</name>
  <files>skills/conversation-mode/SKILL.md, docs/CANON-PHASE-MAP.md, tests/test-204-ignite-wiring-grep.cjs</files>
  <read_first>
    skills/conversation-mode/SKILL.md "## Lane Picker (Shape F.1 -- the explicit traffic cop)"
    section (the exact re-surfacing rules: "This re-uses the existing surfaces, it does not replace
    them" -- the ignite Gate B0 handoff is one more re-surface of this SAME picker, documented the
    same way the Mode 1 upgrade-offer re-surface already is)
    docs/CANON-PHASE-MAP.md header ("Every new phase MUST declare canon_parts: frontmatter before
    plan approval... If a phase implements a canon part that this map does not list, update this map
    in the same commit") and the most recent rows (Phase 205 canon-wave row, Phase 182 row) for the
    exact `| status | phase description | canon_parts | note |` column shape to match
    tests/test-115-surfaces-grep.sh and tests/test-179-*.sh (or any grep-based prose-assertion test
    in tests/) for the house style of a grep-based wiring proof test in this repo (assert exact
    substrings exist in specific files; assert zero em-dash matches)
  </read_first>
  <action>
    In skills/conversation-mode/SKILL.md, add one short sentence under the existing "## Lane Picker"
    section's "This re-uses the existing surfaces..." paragraph, naming /mos:ignite's new Gate B0
    "Just talk (no room)" pick as another re-surface of this same Lane Picker (not a new lane, not a
    new selector) -- purely a cross-reference addition, zero logic change to this file's existing
    Mode 1/2/3 behavior.

    In docs/CANON-PHASE-MAP.md, add one new row for Phase 204 following the exact column shape of
    the existing rows (status "shipped", the phase description, canon_parts "3, 7, 8, 10, 11, 12",
    and a one-line note summarizing: ignite's pre-birth Gate B0 room-chooser + Just Talk handoff +
    the session-register personaContext wiring; mints no new reach, no new edge type, opens no Brain
    wire; implementing plans 204-01/204-02/204-03/204-04). Insert it in the position this file's own
    convention uses for phase rows (chronological / adjacent to the Phase 205 row already present).

    Create tests/test-204-ignite-wiring-grep.cjs: a grep-based prose-assertion test (Node,
    node:assert/strict, no npm deps) asserting: commands/ignite.md contains the literal substrings
    "Gate B0", "room-chooser.cjs", "session-register.cjs", "resolvePick", and "personaContext";
    skills/conversation-mode/SKILL.md contains the literal substring "ignite"; docs/CANON-PHASE-MAP.md
    contains the literal substring "204"; and all three files contain zero em-dash characters
    (U+2014). Follow the parse/assert style already used by this repo's grep-based tests (read file
    with node:fs, plain string includes() checks, a running PASS counter, non-zero exit on any
    failure).
  </action>
  <verify>
    <automated>node tests/test-204-ignite-wiring-grep.cjs</automated>
  </verify>
  <done>tests/test-204-ignite-wiring-grep.cjs exits 0; skills/conversation-mode/SKILL.md names ignite's Gate B0 handoff under its Lane Picker section; docs/CANON-PHASE-MAP.md carries a Phase 204 row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Gate B0 navigator pick -> `room-registry set-active` | The resolved room NAME crosses from an AskUserQuestion answer into a shell-invoked registry mutation |
| Resumed room's USER.md -> session-register resolver | A prior session's stored role_blend is read back and re-applied to a new conversation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-204-06 | Tampering / Elevation of Privilege | Gate B0 "resume" branch calling `room-registry set-active` | mitigate | Only ever call set-active with a NAME that came back out of `resolvePick`'s `room` entry (itself sourced from the SAME `listRegisteredRooms()` call), never with raw navigator free text -- documented explicitly in the Task 1 action so the executor cannot wire it to accept arbitrary Free-Text input |
| T-204-07 | Spoofing | A resumed room's USER.md role_blend used to greet the navigator | accept | role_blend is LOCAL data the navigator themselves set in a prior session of their OWN room; no cross-room or cross-user read is introduced |
| T-204-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan (markdown prose edits + one pure Node test file) |
</threat_model>

<verification>
- `node tests/test-204-ignite-wiring-grep.cjs` exits 0.
- `bash tests/run-all-204.sh` shows 3 PASS + 1 SKIP (test-204-ignite-branch-gate.cjs, Plan 204-04, still pending in this wave's execution order) or 4 PASS if 204-04 has already landed.
- Grep sweep: zero em-dash matches across commands/ignite.md, skills/conversation-mode/SKILL.md, docs/CANON-PHASE-MAP.md, tests/test-204-ignite-wiring-grep.cjs.
- `node scripts/check-render-coverage.cjs --check` still exits 0.
</verification>

<success_criteria>
`/mos:ignite` renders a Shape-F.1 room-chooser card the first time a navigator with prior rooms
invokes it, resuming a room applies that room's stored persona register without re-running B1, the
Just Talk pick hands off to the existing conversation-mode Lane Picker, and the B3 first-move dial
carries the resolved persona lens. No new Brain wire, no new npm dependency, no em-dashes.
</success_criteria>

<output>
Create `.planning/phases/204-ignite-room-chooser-persona-entry/204-03-SUMMARY.md` when done
</output>
