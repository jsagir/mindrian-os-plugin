---
phase: 148-larryreach-selector-re-wire-intelligence-toggleable-components
plan: 03
type: execute
wave: 2
depends_on: ["148-01"]
files_modified:
  - lib/hmi/reach-component-map.json
  - lib/hmi/selector-dispatcher.cjs
  - lib/hmi/shape-f1-renderer.cjs
  - tests/test-148-component-map.cjs
  - tests/test-148-standing-options.cjs
  - tests/run-all-148.sh
autonomous: true
requirements: [IRW-03, IRW-04, IRW-07]
must_haves:
  truths:
    - "each reach/sub_mode resolves to a toggleable component archetype via reach-component-map.json read by the dispatcher"
    - "at least 3 distinct components emit across a representative render set"
    - "a non-intelligence reach (File) carries a non-default archetype (multiSelect)"
    - "File + Brain review + Free-Text render at every selector render OUTSIDE the MAX_K=3 cap, independent of ranking"
    - "Free-Text is always the last entry"
    - "no bespoke AskUserQuestion construction exists outside selector-dispatcher.cjs in the edited files"
  artifacts:
    - path: "lib/hmi/reach-component-map.json"
      provides: "reach_id/sub_mode -> archetype component map (NEW data, registry-is-the-table)"
      contains: "multiSelect"
    - path: "tests/run-all-148.sh"
      provides: "The IRW-01..08 aggregator cloning run-all-1433.sh structure"
      min_lines: 40
  key_links:
    - from: "lib/hmi/selector-dispatcher.cjs"
      to: "lib/hmi/reach-component-map.json"
      via: "dispatcher reads the map to route each reach to its archetype"
      pattern: "reach-component-map"
    - from: "lib/hmi/shape-f1-renderer.cjs"
      to: "the standing trio"
      via: "File + Brain review appended after the capped set, before Free-Text-last"
      pattern: "Brain review|File"
---

<objective>
Build the one genuine net-new artifact of Phase 148 (reach-component-map.json) and wire per-option toggleable component routing across the whole selector surface (IRW-04), plus make File + Brain review always-open standing options outside the MAX_K=3 cap with Free-Text always-last (IRW-03), while preserving the frozen no-bespoke-widget rail (IRW-07). Today every selector row shares one uniform shape and brain_consult can rank out; this plan fixes both through the dispatcher (SEED-020, no bespoke widget).

Purpose: This is the toggleable-component surface the navigator asked for. Engines = multiSelect (batch), compose = ordered, File = multiSelect, Brain review = auto, Hats = confirm (D-03). The dispatcher reads the map; a new reach joins by adding a row, never by editing code (registry-is-the-table, mirroring the connector-registry pattern).
Output: reach-component-map.json + dispatcher read + standing-trio rendering + the run-all-148.sh aggregator + two falsifiable tests (IRW-03, IRW-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-SPEC.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-CONTEXT.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-RESEARCH.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reach-component-map.json and wire the dispatcher to route each reach to its archetype (IRW-04, D-03)</name>
  <files>lib/hmi/reach-component-map.json, lib/hmi/selector-dispatcher.cjs</files>
  <read_first>
    - lib/hmi/selector-dispatcher.cjs lines 26, 55-58, 498-617, 657 (pickShape({requestedShape, roomDir, operator, tier, payload}); dispatchShapeFSubShape routing F.0-F.7; ensureFreeTextLast; the AskUserQuestion trailer construction site -- the ONLY sanctioned construction site per SEED-020)
    - lib/hmi/dial-reach-orchestrator.cjs lines 70-78, 214-256 (the 6 REACH_DEFS after Plan 01: context_block, contradiction, cross_room, brain_consult, deep_research, hats; the buildReachList return shape)
    - data/connector-registry.json (the sub_mode values per reach: reverse-salient, whitespace, cross-domain-analogy under context_block; cross-domain-connect under brain_consult; dominant-design under context_block; six-hats now under hats after Plan 01)
    - skills/intelligence-orchestrator/SKILL.md ORCH-01 (the registry-is-the-table pattern: read the data file, never a hardcoded switch)
    - 148-RESEARCH.md "Reach -> component resolution (IRW-04, D-03)" code example (the map shape keyed by reach_id/sub_mode -> { archetype })
  </read_first>
  <action>
    Create lib/hmi/reach-component-map.json as a hand-maintained data file (precedent: dispatch-framework-map.json) keyed by reach_id and sub_mode to an archetype. Per D-03: the engine sub_modes (reverse-salient, whitespace, cross-domain-analogy, cross-domain-connect, dominant-design) map to "multiSelect" (batch, D-01); the compose-chain pseudo-reach "_compose" maps to "ordered" (D-02); the File standing option "_file" maps to "multiSelect" (D-05); the Brain review standing option "_brain_review" maps to "auto" (D-04, no pick); the hats reach and its six-hats sub_mode map to "confirm" (D-06 confirm-gated); deep_research maps to "confirm" (plan-gated); the remaining frozen base reaches (context_block, contradiction, cross_room base) map to "select" (the default). Include a top-level documentation field naming the archetype vocabulary {select, multiSelect, ordered, group, confirm, auto, text} and stating every archetype resolves through the dispatcher (SEED-020).

    In selector-dispatcher.cjs add a thin reader that loads reach-component-map.json once and exposes resolveArchetype(reachIdOrSubMode) returning the mapped archetype (default 'select' on miss). Route pickShape so the resolved archetype drives which AskUserQuestion mode the dispatcher constructs: multiSelect:true for multiSelect; a confirm follow-up for confirm; the existing select path for select; an ordering follow-up prompt for ordered (per D-02 / the TTY wall -- NOT a live ordered widget). The AskUserQuestion construction stays INSIDE the dispatcher (the only door). Do NOT build any AskUserQuestion payload outside this file. Keep tier gating, JUST_TALK refuse, and the telemetry trailer intact.
  </action>
  <verify>
    <automated>node -e "const m=require('./lib/hmi/reach-component-map.json'); const d=require('./lib/hmi/selector-dispatcher.cjs'); if(typeof d.resolveArchetype!=='function') throw new Error('resolveArchetype missing'); const a=['reverse-salient','six-hats','_file','_brain_review','_compose'].map(k=>d.resolveArchetype(k)); if(new Set(a).size<3) throw new Error('fewer than 3 distinct archetypes'); if(d.resolveArchetype('_file')!=='multiSelect') throw new Error('File archetype wrong'); console.log('PASS map+dispatcher');"</automated>
  </verify>
  <done>reach-component-map.json exists with the D-03 archetype assignments; selector-dispatcher.resolveArchetype reads it; the dispatcher routes the archetype into the AskUserQuestion mode it constructs; no bespoke construction outside the dispatcher.</done>
</task>

<task type="auto">
  <name>Task 2: Render File + Brain review as standing options outside the MAX_K=3 cap, Free-Text always-last (IRW-03)</name>
  <files>lib/hmi/shape-f1-renderer.cjs</files>
  <read_first>
    - lib/hmi/shape-f1-renderer.cjs lines 8-16, 53-71 (Free-Text-always-last invariant; FREE_TEXT const; USER_VERB_CAP=5; normalizeVerbs: cap, drop all Free-Text, append exactly one trailing Free-Text)
    - lib/hmi/dial-reach-orchestrator.cjs lines 194-256 (total_count vs offered_count = min(total, OFFERED_CAP=MAX_K=3); the standing trio rides outside offered_count)
    - 148-RESEARCH.md Pitfall 3 (standing options must NOT be ranker candidates -- they are appended by the render host after ranking+capping, independent of score) and "The standing trio appended outside the cap (IRW-03)" code example
    - 148-CONTEXT.md Claude's Discretion (the standing trio renders at every selector render regardless of mode/tier)
  </read_first>
  <action>
    Extend the F.1 render host so that AFTER the ranked+capped reaches (the <= offered_count = MAX_K=3 chooser set) and BEFORE the trailing Free-Text it appends two standing rows: "File these findings" (archetype multiSelect via the map _file key) and "Brain review" (archetype auto via _brain_review). These are appended by the render host independent of reachScores -- never passed through the ranker, never able to rank out (the exact bug IRW-03 forbids: brain_consult could previously rank out). Keep the existing normalizeVerbs Free-Text-always-last behavior so Free-Text remains the final entry after the standing trio. The standing trio renders identically across mode_a / mode_b / tier_0 / cold-room (it does not depend on tier). Add a standing-options slot to normalizeVerbs (or compose the trio in the host that calls the F.1 verbs) so the renderer stays a pure function. Do not raise USER_VERB_CAP or MAX_K; the standing options ride OUTSIDE the cap as appended rows, not as chooser candidates.
  </action>
  <verify>
    <automated>node -e "const r=require('./lib/hmi/shape-f1-renderer.cjs'); if(typeof r.normalizeVerbs!=='function') throw new Error('normalizeVerbs missing'); console.log('PASS renderer surface');" && node tests/test-148-standing-options.cjs</automated>
  </verify>
  <done>File + Brain review render at every selector render outside the MAX_K=3 cap; Free-Text stays last; presence is independent of reachScores (zero-scored reaches still leave the trio present).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write IRW-03 + IRW-04 tests and the run-all-148.sh aggregator (Wave 0 infra)</name>
  <files>tests/test-148-component-map.cjs, tests/test-148-standing-options.cjs, tests/run-all-148.sh</files>
  <read_first>
    - tests/run-all-1433.sh (the full aggregator structure to clone: CJS_SUITES array; the PASS/FAIL loop that runs each suite to completion and exits non-zero on any failure; the carried drift fences section; the standalone Part-8 grep sweep step d)
    - 148-VALIDATION.md lines 43-44 (IRW-03 test-148-standing-options behavior; IRW-04 test-148-component-map behavior) and lines 49, 57 (the A1 lockstep: run-all-148.sh re-runs the carried drift fences now expecting 6)
    - tests/test-dial-reach-orchestrator.cjs (the node:assert/strict PASS-line style)
  </read_first>
  <behavior>
    - IRW-04 test: run a representative render set through the dispatcher; new Set(emittedArchetypes).size >= 3; File (non-intelligence) emits multiSelect (non-default)
    - IRW-03 test: for each tier in {mode_a, mode_b, tier_0, cold-room} the rendered verb list contains "File", "Brain review", "Free-Text"; Free-Text is the LAST entry; with all reachScores zeroed the trio is still present (presence independent of ranking)
  </behavior>
  <action>
    Write tests/test-148-component-map.cjs (IRW-04) and tests/test-148-standing-options.cjs (IRW-03) as plain CJS node:assert/strict suites mirroring the existing test style, asserting the behaviors above.

    Create tests/run-all-148.sh by cloning tests/run-all-1433.sh: a bash PASS/FAIL loop (set -uo pipefail, no emoji, no em-dashes) that composes (a) node scripts/build-connector-registry.cjs --check; (b) the CJS_SUITES: test-148-engine-reaches, test-148-hats-sixth-reach, test-148-standing-options, test-148-component-map, test-148-unified-host, test-148-real-invocation, test-148-frozen-contracts, test-148-brain-review-egress (the suites land across plans 01/03/04/05; the aggregator lists all of them so the phase gate is one command); (c) the CARRIED drift fences NOW EXPECTING 6: test-reach-ids-drift, test-dial-label-bank-drift, test-dial-graph-relationship-layer, test-sensor-spine-dispatch, test-orchestrator-doctrine-presence, test-dial-reach-orchestrator, test-dial-end-to-end-states, plus test-posture-ids-drift (still 3); (d) a standalone Part-8 grep sweep over the new artifacts (reach-component-map.json + the new lib edits) for forbidden user-content-to-Brain tokens and free-text body fields (summary|content|body|text|note|description heading toward a Brain packet), mirroring run-all-1433.sh step d. The runner must run to completion even when a suite fails, print a per-suite PASS/FAIL line and a final tally, and exit 1 on any failure. Suites not yet created (plans 04/05) may be listed; gate a missing file to a FAIL line (not a crash) so the phase gate flags incompleteness.
  </action>
  <verify>
    <automated>node tests/test-148-component-map.cjs && node tests/test-148-standing-options.cjs && bash -n tests/run-all-148.sh && grep -q "test-reach-ids-drift" tests/run-all-148.sh && grep -q "test-posture-ids-drift" tests/run-all-148.sh && ! grep -P "\xE2\x80\x94" tests/run-all-148.sh && echo "PASS tests+aggregator"</automated>
  </verify>
  <done>test-148-component-map.cjs (IRW-04) and test-148-standing-options.cjs (IRW-03) green; run-all-148.sh parses, lists every 148 suite + the carried drift fences (expecting 6) + posture fence (3) + a standalone Part-8 grep sweep; no em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reach row -> AskUserQuestion construction | Every component must be built INSIDE the dispatcher (SEED-020); a bespoke construction is an untrusted render path |
| standing option -> ranker | File/Brain review must never enter the ranker candidate set or they can rank out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-03-01 | Tampering | bespoke AskUserQuestion outside the dispatcher | mitigate | the IRW-07 grep audit (Plan 04) asserts no AskUserQuestion construction outside selector-dispatcher.cjs; this plan keeps all construction in the dispatcher |
| T-148-03-02 | Denial of Service | standing options leaking into the ranker | mitigate | the trio is appended by the render host independent of reachScores; the IRW-03 test zeroes all scores and asserts the trio survives |
| T-148-03-03 | Information Disclosure | reach-component-map.json content | accept | the map holds only reach_id/sub_mode -> archetype enum scalars; no user content, no Brain egress; the run-all-148.sh step-d grep sweep covers it |
| T-148-03-SC | Tampering | npm/pip/cargo installs | mitigate | zero installs (one new JSON data file + CJS edits + tests); no package surface |
</threat_model>

<verification>
- node tests/test-148-component-map.cjs (>=3 distinct archetypes; File=multiSelect)
- node tests/test-148-standing-options.cjs (trio present every tier, outside cap, Free-Text last, independent of score)
- bash -n tests/run-all-148.sh + grep confirms the carried drift fences are listed
- no em-dashes in run-all-148.sh
</verification>

<success_criteria>
- reach-component-map.json exists with the D-03 archetype assignments; dispatcher reads it; >=3 distinct components emit; File carries multiSelect (non-default, non-intelligence)
- File + Brain review render outside the MAX_K=3 cap at every render; Free-Text always last; presence independent of ranking
- no bespoke AskUserQuestion construction outside the dispatcher
- run-all-148.sh aggregator parses and lists all 148 suites + carried drift fences (6) + posture fence (3) + Part-8 grep sweep
</success_criteria>

<output>
Create `.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-03-SUMMARY.md` when done
</output>
