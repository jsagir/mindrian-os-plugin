---
phase: 148-larryreach-selector-re-wire-intelligence-toggleable-components
plan: 04
type: execute
wave: 2
depends_on: ["148-01"]
files_modified:
  - lib/workflow/dial-close-reach.cjs
  - tests/test-148-engine-reaches.cjs
  - tests/test-148-real-invocation.cjs
  - tests/test-148-frozen-contracts.cjs
autonomous: true
requirements: [IRW-01, IRW-06, IRW-07]
must_haves:
  truths:
    - "each of the 5 engine frameworks resolves to a real command via commandsForFramework and is rankable by f-selector-ranker"
    - "committing a reach resolves reach.framework to its command and FIRES the real command (not a stub)"
    - "the commit writes a SELECTED_REACH typed edge via navigation.cjs and lands the engine artifact"
    - "no command resolution fabricates a slug; an empty resolution degrades to a manual-run instruction"
    - "MAX_K===3, RECOMMEND_FLOOR===0.70, MARGIN_THRESHOLD===0.15 unchanged; no bespoke widget outside the dispatcher"
  artifacts:
    - path: "lib/workflow/dial-close-reach.cjs"
      provides: "closeReach sync/pivot path resolves framework->command and fires it (IRW-06)"
      contains: "commandsForFramework"
    - path: "tests/test-148-real-invocation.cjs"
      provides: "IRW-06 integration test: select reverse-salient -> command fires -> edge -> artifact"
      min_lines: 30
  key_links:
    - from: "lib/workflow/dial-close-reach.cjs"
      to: "lib/workflow/command-resolver.cjs"
      via: "closeReach resolves reach.framework through commandsForFramework on the sync path"
      pattern: "commandsForFramework"
    - from: "lib/workflow/dial-close-reach.cjs"
      to: "lib/core/navigation.cjs"
      via: "writeEdge SELECTED_REACH on commit"
      pattern: "SELECTED_REACH"
---

<objective>
Close the IRW-06 gap: today closeReach writes a SELECTED_REACH edge to a plumbing label (target_id 'cmd:'+reach.command) but never RESOLVES or FIRES a real command. Wire the sync/pivot commit path to resolve reach.framework through command-resolver.commandsForFramework() and fire the real /mos:<engine> command, then land the artifact via the existing filing path. Confirm the 5 engine frameworks are resolvable + rankable (IRW-01), and audit that the frozen constants and the no-bespoke-widget rail survived (IRW-07).

Purpose: This is the navigator's core complaint ("plumbing behind the menu"). Selecting reverse-salient must run find-bottlenecks/rs-fetch, write the edge, and land the artifact. The resolver is the ONLY sanctioned framework->command door (degrade-don't-fabricate built in) and the intelligence-orchestrator STEP 5 already proves the firing path.
Output: closeReach resolve+fire on commit + the IRW-01/IRW-06/IRW-07 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-SPEC.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-CONTEXT.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-RESEARCH.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve framework->command and FIRE the real engine on the closeReach commit path (IRW-06)</name>
  <files>lib/workflow/dial-close-reach.cjs</files>
  <read_first>
    - lib/workflow/dial-close-reach.cjs lines 60-105 (require navigation.cjs; the SELECTED_REACH writer: target_id 'cmd:'+reach.command, edge_type SELECTED_REACH, props framework) and lines 130-230 (closeReach the 4-outcome transaction: miss/defer-reject/pivot/sync; the sync path validating reach.command; the SELECTED_REACH + pivot writes)
    - lib/workflow/command-resolver.cjs lines 85-148 (commandsForFramework(name); frameworksForCommand(cmd); composeWorkflow(frameworkChain); module.exports)
    - skills/intelligence-orchestrator/SKILL.md STEP 5 (the resolve+fire loop: framework = reach.framework; cmds = commandsForFramework(framework); if empty -> tell the navigator to run <framework> manually, DEGRADE never fabricate; else fire cmds[0]; then fileEvidenceWithReadback lands the artifact)
    - lib/core/navigation/file-evidence-readback.cjs (fileEvidenceWithReadback signature) and lib/core/findings-wirer.cjs (wireAccept fallback)
    - 148-RESEARCH.md "Resolving + firing the real engine on commit (IRW-06)" code example + Runtime State Inventory (no backfill of historical plumbing-label edges -- state it explicitly)
  </read_first>
  <action>
    On the closeReach sync/pivot (committed-reach) path, after the existing SELECTED_REACH edge write, add the resolve+fire step. Resolve reach.framework through command-resolver.commandsForFramework(reach.framework). If the resolution is empty, DEGRADE: return a structured result instructing the navigator to run <framework> manually (never fabricate a slug). If non-empty, fire cmds[0] (the real /mos:<engine> command) and then land the artifact via fileEvidenceWithReadback (fallback wireAccept on readback error), surfacing the FILEVAL read-back honesty result. Keep the SELECTED_REACH edge pointed at the resolved real command (cmd:<resolved-command>) rather than a plumbing label, but do NOT migrate historical edges -- state in the SUMMARY that old plumbing-label edges are harmless historical system-bookkeeping (A4, no backfill). The SELECTED_REACH edge stays SYSTEM BOOKKEEPING (created_by=system, Part 9 carve-out); never fold a truth-claim confirmNode promotion into this write. Keep the miss/defer-reject paths unchanged (they write no SELECTED_REACH). All writes route through navigation.cjs; do not open room.db directly.
  </action>
  <verify>
    <automated>node tests/test-148-real-invocation.cjs</automated>
  </verify>
  <done>closeReach sync/pivot resolves reach.framework via commandsForFramework, fires the real command, writes the SELECTED_REACH edge through navigation.cjs, and lands the artifact via fileEvidenceWithReadback; empty resolution degrades to a manual-run instruction; miss/defer paths unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IRW-01 engine-reaches-resolvable test + IRW-06 real-invocation integration test</name>
  <files>tests/test-148-engine-reaches.cjs, tests/test-148-real-invocation.cjs</files>
  <read_first>
    - lib/workflow/command-resolver.cjs lines 85-148 (commandsForFramework return shape)
    - lib/workflow/f-selector-ranker.cjs (rankForSelector candidate set; MAX_K=3; reads data/command-registry.json)
    - data/dispatch-framework-map.json lines 5-16 (the engine framework names: "Reverse Salient Analysis", "Usher's Model of Cumulative Synthesis", "Four Lenses of Innovation", "HSI Semantic Surprise Analysis Assistant", "Dominant Design", "Six Thinking Hats")
    - tests/test-dial-close-reach.cjs (the precedent that opens a temp room.db for closeReach -- reuse its db-fixture setup)
    - 148-VALIDATION.md lines 41, 46 (IRW-01 + IRW-06 test behaviors)
  </read_first>
  <behavior>
    - IRW-01: for each of {Reverse Salient Analysis (find-bottlenecks/rs-fetch), HSI Semantic Surprise Analysis Assistant (whitespace), Four Lenses of Innovation (find-analogies), Usher's Model of Cumulative Synthesis (find-connections), Dominant Design (dominant-designs)}: commandsForFramework(framework).length > 0 AND the resolved command appears in rankForSelector(...) candidate output
    - IRW-06: closeReach({outcome:'sync', reach:{framework:'Reverse Salient Analysis', command:'/mos:find-bottlenecks'}, ...}) on a temp room.db -> resolver returned a non-empty command -> a SELECTED_REACH edge exists (read via navigation getNeighborhood/edge read) -> the engine artifact row/file landed (fileEvidenceWithReadback readback)
  </behavior>
  <action>
    Write tests/test-148-engine-reaches.cjs (IRW-01) asserting the five engine frameworks resolve to a non-empty command and are rankable. Write tests/test-148-real-invocation.cjs (IRW-06) as an integration test using a temp room.db fixture (clone the db setup from test-dial-close-reach.cjs): drive closeReach with a sync outcome for reverse-salient, assert the resolver returned a real command, assert a SELECTED_REACH edge landed via navigation.cjs, and assert the artifact landed via the readback. Both are plain CJS node:assert/strict with a PASS line and non-zero exit on failure. Register both in run-all-148.sh (already listed by Plan 03).
  </action>
  <verify>
    <automated>node tests/test-148-engine-reaches.cjs && node tests/test-148-real-invocation.cjs</automated>
  </verify>
  <done>test-148-engine-reaches.cjs proves the 5 engine frameworks resolve + rank; test-148-real-invocation.cjs proves select reverse-salient fires the real command, writes the edge, lands the artifact. Both green.</done>
</task>

<task type="auto">
  <name>Task 3: IRW-07 frozen-contracts + no-bespoke-widget audit test</name>
  <files>tests/test-148-frozen-contracts.cjs</files>
  <read_first>
    - lib/workflow/f-selector-ranker.cjs (MAX_K export) and lib/hmi/dial-reach-orchestrator.cjs lines 53-62, 254-256 (DIAL_REACH_K, RECOMMEND_FLOOR=0.70, MARGIN_THRESHOLD=0.15, OFFERED_CAP; the export block)
    - lib/hmi/selector-dispatcher.cjs (the only sanctioned AskUserQuestion construction site)
    - 148-VALIDATION.md line 47 (IRW-07: MAX_K===3, RECOMMEND_FLOOR===0.70, MARGIN_THRESHOLD===0.15 unchanged; grep-audit no bespoke widget outside the dispatcher)
    - 148-RESEARCH.md Pitfall 2 + Pitfall 5 (the three-cap separation; no AskUserQuestion construction outside selector-dispatcher.cjs)
  </read_first>
  <action>
    Write tests/test-148-frozen-contracts.cjs (IRW-07) asserting ranker.MAX_K===3, orchestrator.RECOMMEND_FLOOR===0.70, orchestrator.MARGIN_THRESHOLD===0.15, and orchestrator.DIAL_REACH_K===6 (the only constant that moved). Add a grep-style audit (using node fs over the Phase-148-edited files: dial-close-reach.cjs, shape-f1-renderer.cjs, reach-component-map.json, and any new helpers) asserting that no AskUserQuestion construction appears outside lib/hmi/selector-dispatcher.cjs. Plain CJS node:assert/strict with a PASS line. Register in run-all-148.sh.
  </action>
  <verify>
    <automated>node tests/test-148-frozen-contracts.cjs</automated>
  </verify>
  <done>test-148-frozen-contracts.cjs asserts MAX_K===3, the 0.70/0.15 gate, DIAL_REACH_K===6, and no bespoke AskUserQuestion construction outside the dispatcher. Green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reach.framework -> command resolution | A fabricated slug would run an unintended command; only the resolver may map framework->command |
| commit -> room.db write | All edges/artifacts route through navigation.cjs (Part 9 chokepoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-04-01 | Spoofing | fabricated /mos: slug from a reach | mitigate | resolve ONLY through commandsForFramework (the sanctioned door); empty resolution degrades to a manual-run instruction, never a guessed slug |
| T-148-04-02 | Tampering | direct room.db write bypassing the chokepoint | mitigate | closeReach writes exclusively through navigation.cjs writeEdge; the integration test reads the edge back via navigation |
| T-148-04-03 | Elevation of Privilege | SELECTED_REACH folded into a truth-claim confirm | mitigate | SELECTED_REACH stays system-bookkeeping (Part 9 carve-out); closeReach never calls confirmNode |
| T-148-04-04 | Tampering | a frozen constant silently drifting | mitigate | IRW-07 test asserts MAX_K===3 + 0.70/0.15 gate; only DIAL_REACH_K moved (to 6) |
| T-148-04-SC | Tampering | npm/pip/cargo installs | mitigate | zero installs (CJS edits + tests only); no package surface |
</threat_model>

<verification>
- node tests/test-148-engine-reaches.cjs (5 engine frameworks resolve + rank)
- node tests/test-148-real-invocation.cjs (select reverse-salient fires real command + edge + artifact)
- node tests/test-148-frozen-contracts.cjs (MAX_K===3, 0.70/0.15, DIAL_REACH_K===6, no bespoke widget)
</verification>

<success_criteria>
- closeReach sync/pivot resolves reach.framework -> real command and fires it; SELECTED_REACH edge written via navigation.cjs; artifact lands; empty resolution degrades
- the 5 engine frameworks resolve to a non-empty command and are rankable (IRW-01)
- MAX_K===3, RECOMMEND_FLOOR===0.70, MARGIN_THRESHOLD===0.15 unchanged; DIAL_REACH_K===6; no bespoke widget outside the dispatcher (IRW-07)
</success_criteria>

<output>
Create `.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-04-SUMMARY.md` when done
</output>
