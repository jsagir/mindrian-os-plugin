---
phase: 148-larryreach-selector-re-wire-intelligence-toggleable-components
plan: 05
type: execute
wave: 3
depends_on: ["148-03", "148-04"]
files_modified:
  - lib/core/navigation-engine-offer.cjs
  - lib/hmi/brain-review-packet.cjs
  - lib/hmi/hats-persona-cache.cjs
  - tests/test-148-unified-host.cjs
  - tests/test-148-brain-review-egress.cjs
autonomous: true
requirements: [IRW-02, IRW-05, IRW-08]
must_haves:
  truths:
    - "offer-resolver and suggest-next both route through selector-dispatcher.pickShape (single code path, no second bespoke renderer)"
    - "the cold-room tier_0 entry leads with 'what can I help you with' and matches typed intent via command-resolver + JTBD rank"
    - "Brain review builds a typed methodology packet only; no user-content string reaches the packet"
    - "check-brain-boundary passes for the Brain-review path"
    - "the Hats persona cache rebuilds on first call and reads from cache on the second for the same room"
  artifacts:
    - path: "lib/hmi/brain-review-packet.cjs"
      provides: "Typed methodology packet builder for the Brain-review standing option (D-04, IRW-08)"
      contains: "auditQueryString"
    - path: "lib/hmi/hats-persona-cache.cjs"
      provides: "Per-room Hats persona cache (read-then-rebuild, D-06)"
      min_lines: 25
  key_links:
    - from: "lib/core/navigation-engine-offer.cjs"
      to: "lib/hmi/selector-dispatcher.cjs"
      via: "resolveOffer output renders through pickShape (the one F.1 host)"
      pattern: "pickShape"
    - from: "lib/hmi/brain-review-packet.cjs"
      to: "lib/core/rs-egress-prompts.cjs"
      via: "auditQueryString default-deny gate on the framework handle"
      pattern: "auditQueryString"
---

<objective>
Unify the three suggest surfaces onto the one component-routed F.1 host (IRW-05): F.1 Next Move, the offer-resolver (resolveOfferNextStep, Phase 135), and suggest-next all render through selector-dispatcher.pickShape, not three bespoke renderers. Wire the cold-room "what can I help you with" lead with command-resolver intent matching (D-07/D-08). Make the Brain-review standing option boundary-safe: a typed methodology packet only, zero user-content egress (IRW-08, D-04). Land the per-room Hats persona cache (D-06, the IRW-02 cache path) so the Plan 01 cache assertion goes hard.

Purpose: One ranking, one render path, no bespoke renderers; Brain review is the "outside review" that surfaces contradictions without leaking a single finding body across the Part 8 boundary; cold rooms get a help-led entry; Hats personas are cached per room and rebuilt on demand.
Output: offer-resolver + suggest-next unified onto pickShape + the cold-room help lead + the typed Brain-review packet + the Hats persona cache + the IRW-05/IRW-08 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-SPEC.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-CONTEXT.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-RESEARCH.md
@.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify offer-resolver + suggest-next onto the one pickShape F.1 host + cold-room help lead (IRW-05, D-07/D-08)</name>
  <files>lib/core/navigation-engine-offer.cjs</files>
  <read_first>
    - lib/core/navigation-engine-offer.cjs lines 103-429 (resolveOffer(context) returns ONE calibrated offer or null; the module.exports)
    - lib/core/navigation-engine.cjs lines 526-843, 1039 (resolveOfferNextStep(context, _brain); where decision.offer_next_step is set; exports)
    - lib/core/research-filing-selector.cjs lines 8-41, 145-166 (the proof-pattern: produce the gate by routing through dispatcher.pickShape({requestedShape:'F.1', ...}), NOT a bespoke selector)
    - lib/hmi/dial-reach-orchestrator.cjs lines 90-107 (_resolveTierMode: mode_a/mode_b/tier_0) for the cold-room lead branch
    - lib/workflow/command-resolver.cjs lines 85-110 (commandsForFramework / frameworksForCommand for D-08 intent matching) and lib/workflow/f-selector-ranker.cjs (JTBD rank)
    - 148-CONTEXT.md D-07/D-08 + Claude's Discretion (cold-room leads with "what can I help you with"; tier_0 base set keeps Run Methodology / Reformulate / Free-Text beneath the lead, plus the standing trio; once JTBD signal exists, the six reaches lead)
  </read_first>
  <action>
    Re-point resolveOffer (and the resolveOfferNextStep caller, plus the suggest-next surface) so the offer is RENDERED through selector-dispatcher.pickShape({requestedShape:'F.1', ...}) -- the identical pattern research-filing-selector.cjs uses -- rather than any second bespoke renderer. The three surfaces (F.1 Next Move, offer-resolver, suggest-next) must converge on this single code path. Keep resolveOffer's calibration logic (the ONE offer it computes) intact; only the rendering door changes.

    Add the cold-room lead: when the resolved tier is tier_0 (or the room has no JTBD signal), the host leads with a "what can I help you with" free-text intent capture row; the typed intent resolves through command-resolver (commandsForFramework / the resolver door, Phase 122) plus f-selector-ranker JTBD rank to produce matched reaches with their components. Beneath the lead, keep the tier_0 canon fallback (Run Methodology / Reformulate / Free-Text) plus the standing trio (Plan 03). Once JTBD signal is present, the six intelligence reaches lead instead. Only the resolved generic framework name reaches commandsForFramework; the raw intent text NEVER crosses into a Brain or web query (Part 8 doctrine, intelligence-orchestrator). Do not build a bespoke AskUserQuestion; the free-text capture is the dispatcher's text archetype.
  </action>
  <verify>
    <automated>node tests/test-148-unified-host.cjs</automated>
  </verify>
  <done>resolveOffer + suggest-next render through pickShape (single F.1 path, no second bespoke renderer); the cold-room tier_0 lead is "what can I help you with" with command-resolver + JTBD matching; raw intent text never crosses to a Brain/web query.</done>
</task>

<task type="auto">
  <name>Task 2: Build the typed Brain-review packet (zero egress) and the per-room Hats persona cache (IRW-08, D-04, D-06, IRW-02 cache path)</name>
  <files>lib/hmi/brain-review-packet.cjs, lib/hmi/hats-persona-cache.cjs</files>
  <read_first>
    - lib/core/rs-egress-prompts.cjs::auditQueryString (throws on forbidden-pattern hit, default-deny; already used by dial-label-composer for the {framework} slot)
    - lib/core/brain-response-sanitize.cjs (the A3 PII-redaction sanitizer, Phase 117-04)
    - lib/hmi/dial-label-composer.cjs lines 54-58, 142-152 (the {framework} egress audit pattern; the 2-egress-family discipline -- ONLY generic framework handles cross)
    - lib/core/navigation.cjs (the chokepoint: getNeighborhood / writeEdge / findRecentChanges -- contradictions are surfaced LOCALLY from room.db, never sent to Brain)
    - lib/core/feynman/dial-memory-renderer.cjs (the per-room cache idiom for the Hats persona cache; A5: a room-local file via the chokepoint, rebuilt on demand)
    - 148-CONTEXT.md D-04 (Brain review auto-reviews latest findings; surfaces contradictions + next framework chain; NO scope prompt; typed packet; zero egress) + D-06 (Hats confirms before running; personas cached per room, rebuilt on demand)
    - 148-RESEARCH.md Pitfall 4 (Brain review must not form a freeform query) + the IRW-08 threat patterns
  </read_first>
  <action>
    Create lib/hmi/brain-review-packet.cjs: the Brain-review standing option builds a TYPED methodology packet only (Phase 110 contract). It surfaces contradictions LOCALLY from room.db via navigation.cjs (getNeighborhood / the contradiction edges) and composes ONLY a generic methodology question ("what chains from <framework>?") carrying generic framework handles + phase ids + enum scalars -- never a finding body, artifact text, number, or name. Gate the outbound handle through rs-egress-prompts.auditQueryString (default-deny) and pass any Brain response through brain-response-sanitize. On Brain-unreachable (mode_b / tier_0) the option degrades to a local-only answer (omit the Brain line), never blocks. All writes route through navigation.cjs.

    Create lib/hmi/hats-persona-cache.cjs: a per-room Hats research-persona cache mirroring the dial-memory per-room idiom (a room-local file via the chokepoint). Expose getOrBuildPersonas(roomDir) that rebuilds on a cache miss and reads from cache on a hit (rebuilt on demand, D-06), and an invalidation entry so personas can be rebuilt explicitly. The Hats reach is confirm-gated (the reach-component-map 'confirm' archetype from Plan 03 + the D-06 confirm copy "Build research personas + run the hats? research spin, ~1 min") and carries the "go deep" marker from the existing 12-glyph UI-ruling vocabulary (the lightning glyph used for heavy/convergence, A2) -- no new glyph. This cache is what makes the Plan 01 test-148-hats-sixth-reach read-then-rebuild assertion go hard.
  </action>
  <verify>
    <automated>node tests/test-148-brain-review-egress.cjs && node tests/test-148-hats-sixth-reach.cjs</automated>
  </verify>
  <done>brain-review-packet.cjs builds a typed methodology packet only (auditQueryString-gated, sanitized, contradictions surfaced locally, degrades when Brain unreachable); hats-persona-cache.cjs rebuilds-then-reads per room; the Plan 01 persona-cache assertion now passes hard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IRW-05 unified-host test + IRW-08 Brain-review adversarial egress test</name>
  <files>tests/test-148-unified-host.cjs, tests/test-148-brain-review-egress.cjs</files>
  <read_first>
    - lib/core/research-filing-selector.cjs (the pickShape single-path precedent to spy/seam on)
    - tests/test-navigation-packet-part8-leak.cjs (the 9-tripwire adversarial pattern: feed forbidden user strings, assert only generic handles + phase ids + enum scalars survive into the packet)
    - tests/run-all-1433.sh step d (the standalone Part-8 grep sweep -- already carried into run-all-148.sh by Plan 03)
    - 148-VALIDATION.md lines 45, 48 (IRW-05 unified-host behavior; IRW-08 Brain-review egress behavior)
  </read_first>
  <behavior>
    - IRW-05: resolveOfferNextStep output AND the suggest-next output both pass through selector-dispatcher.pickShape (single code path; spy/seam asserts no second bespoke renderer is invoked)
    - IRW-08: feed the Brain-review path findings containing forbidden user strings (artifact body, numbers, names); assert the built packet contains ONLY generic framework handles + phase ids + enum scalars; assert auditQueryString rejects a smuggled body; mirror the 9-tripwire pattern
  </behavior>
  <action>
    Write tests/test-148-unified-host.cjs (IRW-05): seam on selector-dispatcher.pickShape and assert both the offer-resolver and suggest-next surfaces route through it (single path), with no second bespoke renderer invoked. Write tests/test-148-brain-review-egress.cjs (IRW-08) mirroring test-navigation-packet-part8-leak.cjs: feed the Brain-review packet builder findings carrying forbidden user-content strings and assert none reach the packet (only generic handles + phase ids + enum scalars), and that auditQueryString default-denies a smuggled body. Both plain CJS node:assert/strict with PASS lines. Confirm both are listed in run-all-148.sh (Plan 03), then run the full aggregator including the check-brain-boundary scan over the new Brain-review path.
  </action>
  <verify>
    <automated>node tests/test-148-unified-host.cjs && node tests/test-148-brain-review-egress.cjs && bash tests/run-all-148.sh</automated>
  </verify>
  <done>test-148-unified-host.cjs proves the single pickShape path; test-148-brain-review-egress.cjs proves zero user-content egress; the full run-all-148.sh aggregator (all IRW suites + carried drift fences at 6 + posture at 3 + Part-8 grep sweep + check-brain-boundary) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding body -> Brain-review packet | The single highest-risk path in the phase; only generic handles may cross (Part 8) |
| free-text intent (D-08) -> Brain/web query | The resolved generic framework name may cross; the raw intent text never does |
| suggest surface -> renderer | Three surfaces must converge on ONE pickShape path; a second renderer is an untrusted divergence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-05-01 | Information Disclosure | finding body egressed via Brain review | mitigate | typed packet only (Phase 110); auditQueryString default-deny; brain-response-sanitize; the IRW-08 9-tripwire adversarial test + run-all-148.sh grep sweep + check-brain-boundary scan |
| T-148-05-02 | Information Disclosure | free-text intent smuggled into a Brain/web query | mitigate | only the resolved generic framework name reaches commandsForFramework; raw intent never crosses (intelligence-orchestrator Part-8 doctrine) |
| T-148-05-03 | Tampering | a second bespoke renderer diverging from pickShape | mitigate | the IRW-05 seam test asserts a single pickShape code path; no bespoke renderer invoked |
| T-148-05-04 | Information Disclosure | Hats persona cache content | accept | the persona cache is room-local via the chokepoint; never egresses (Part 8); rebuilt on demand |
| T-148-05-SC | Tampering | npm/pip/cargo installs | mitigate | zero installs (CJS files + tests only); no package surface |
</threat_model>

<verification>
- node tests/test-148-unified-host.cjs (single pickShape path)
- node tests/test-148-brain-review-egress.cjs (zero user-content egress; adversarial 9-tripwire)
- node tests/test-148-hats-sixth-reach.cjs (persona cache read-then-rebuild now hard)
- bash tests/run-all-148.sh (full phase gate: IRW suites + drift fences at 6 + posture at 3 + Part-8 sweep + check-brain-boundary)
</verification>

<success_criteria>
- offer-resolver + suggest-next + F.1 Next Move route through one pickShape host (IRW-05)
- cold-room tier_0 leads with "what can I help you with" matched via command-resolver + JTBD (D-07/D-08); raw intent never crosses to Brain/web
- Brain review = typed methodology packet only; zero user-content egress; check-brain-boundary passes (IRW-08, D-04)
- Hats persona cache rebuilds-then-reads per room (D-06, IRW-02 cache path)
- the full run-all-148.sh aggregator is green
</success_criteria>

<output>
Create `.planning/phases/148-larryreach-selector-re-wire-intelligence-toggleable-componen/148-05-SUMMARY.md` when done
</output>
