---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 06
type: execute
wave: 3
depends_on: ["136-02", "136-03", "136-04"]
files_modified:
  - lib/core/navigation/edges.cjs
  - docs/architecture/SUBSTRATE-CONTRACT.md
  - lib/tui/gate.cjs
  - tests/136-gate-write-node.test.cjs
autonomous: true
requirements: ["Req 6"]
must_haves:
  truths:
    - "DECISION and FREE_TEXT are members of the ALLOWED_EDGE_TYPES allow-list"
    - "Toggling 2 options + free-text commits exactly 3 edges (2 DECISION + 1 FREE_TEXT) through navigation.cjs writeEdge"
    - "A live 'would write to room.db' preview reflects pending edges before confirm; cancel writes zero"
    - "Confirm fans one memory_event to inline + web + tui surfaces"
    - "A rejection-with-reason captures the reason as a REJECTED_BECAUSE edge property"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "DECISION + FREE_TEXT added to ALLOWED_EDGE_TYPES (additive)"
      contains: "DECISION"
    - path: "lib/tui/gate.cjs"
      provides: "richer multi-select + free-text gate widget (mirrors selector-dispatcher)"
      min_lines: 30
  key_links:
    - from: "lib/tui/gate.cjs"
      to: "lib/core/navigation/edges.cjs writeEdge"
      via: "each toggled option commits a typed DECISION edge; free-text a FREE_TEXT edge"
      pattern: "writeEdge"
    - from: "lib/tui/gate.cjs"
      to: "lib/hmi/selector-dispatcher.cjs"
      via: "mirrors the Shape F selector contract (no bespoke selector)"
      pattern: "verbs"
---

<objective>
Build the gate as the write node (Req 6, D-13): add DECISION + FREE_TEXT to the edge allow-list (additive), and build the richer multi-select + always-present free-text gate widget (mirroring the Shape F selector) where arrow + Space toggles, each toggled option commits its own typed DECISION edge, free-text commits a FREE_TEXT edge, a live "would write to room.db" preview updates as you toggle, and confirm fans one memory_event to every surface. Parallel with Plan 05 (owns edges.cjs + lib/tui/gate.cjs only). Depends on Plan 04 (engine) + Plan 02 (core) + Plan 03 (token core).

Purpose: D-13 / Canon Part 3+4 make the gate the point where browsing becomes a typed graph mutation; reuse the selector-dispatcher contract rather than inventing a bespoke widget (Part 7).
Output: DECISION + FREE_TEXT edge types; lib/tui/gate.cjs; a gate-write-node test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-PATTERNS.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-UI-SPEC.md
@docs/MINDRIAN-CANON.md
@lib/core/navigation/edges.cjs

<interfaces>
From lib/core/navigation/edges.cjs (the additive allow-list - DECISION + FREE_TEXT are NOT yet members):
  const ALLOWED_EDGE_TYPES = Object.freeze(new Set([ ... ]))  // line 32; current members include DEFERRED, REJECTED, DERIVED_FROM, FILED_AS_DECISION, FOLLOWS_FROM, OPERATOR_TRANSITION, INFORMS, REJECTED_BECAUSE (line 128)
  function writeEdge(db, params)  // line 146; rejects any edge_type not in the Set (lines 157-179); properties are ENUM-ONLY per Canon Part 8
  module.exports = { ALLOWED_EDGE_TYPES, writeEdge }  // line 179
  The file's history is additive edge-type extension with a per-type comment block citing Canon Part 4 + Part 8 (the Phase 120/125/129 idiom - copy it).

From lib/hmi/selector-dispatcher.cjs (the contract to MIRROR, do NOT reinvent):
  pickShape(...) returns { shape, rendered: { zones, contract } }  // lines 657-758; contract.verbs is the option list
  ensureFreeTextLast(...)  // lines 216-227; Free-Text is ALWAYS the last verb (Part 3), except contract.freeTextOffered===false
  aliasToCanonical(verb, aliasMap)  // lines 121-129; aliases render, canonical verbs persist
  The 10 MindrianOS-native verbs (Canon Part 3) are the closed vocabulary; each toggled option -> DECISION edge, open-text -> FREE_TEXT edge (D-13).

From navigation.cjs: writeEdge (re-exported, line 94), confirmNode (line 147, human-confirms-truth if a truth-claim node is confirmed at the gate).

From 136-UI-SPEC.md "Gate widget states": Option OFF (☐) / ON (☑, pending DECISION) / Free-text focused (pending FREE_TEXT) / Live preview ("would write to room.db") / Commit (Enter -> each pick a typed edge, one fan-out memory_event) / Cancel (Esc -> zero writes) / Reject-with-reason -> REJECTED_BECAUSE edge property (Part 4). Inline variant: <=4 options + non-persistent -> native AskUserQuestion; >4/persistent -> the ink widget.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DECISION + FREE_TEXT to the edge allow-list (additive)</name>
  <files>lib/core/navigation/edges.cjs, docs/architecture/SUBSTRATE-CONTRACT.md</files>
  <behavior>
    - Test 1: writeEdge(db, {edge_type:'DECISION', ...}) succeeds (DECISION is now an allowed type).
    - Test 2: writeEdge(db, {edge_type:'FREE_TEXT', ...}) succeeds.
    - Test 3: writeEdge with a bogus edge_type still rejects (the allow-list is still closed).
    - Test 4: the ALLOWED_EDGE_TYPES delta is exactly +2 (named-membership assertion, never a brittle absolute Set size).
  </behavior>
  <read_first>
    - lib/core/navigation/edges.cjs lines 32-128 (ALLOWED_EDGE_TYPES Set + the per-type comment-block idiom; INFORMS/REJECTED_BECAUSE at lines 106-128 are the most recent additive precedent to copy)
    - lib/core/navigation/edges.cjs lines 146-179 (writeEdge: the edge_type allow-list check + the ENUM-ONLY properties rule per Canon Part 8)
    - docs/architecture/SUBSTRATE-CONTRACT.md (the M11 amendment rule for any allow-list/chokepoint change)
    - docs/MINDRIAN-CANON.md Part 3 (the 10 verbs -> DECISION edge) + Part 4 (every choice is graph data) + Part 8 (enum-only edge properties)
  </read_first>
  <action>Add DECISION and FREE_TEXT to ALLOWED_EDGE_TYPES in lib/core/navigation/edges.cjs as an additive block (mirror the INFORMS/REJECTED_BECAUSE idiom verbatim) with a per-type comment block citing Canon Part 3 (DECISION = the typed edge a Decision-Gate verb pick produces) + Part 4 (every choice is graph data) + Part 8 (properties ENUM-ONLY - the FREE_TEXT edge carries the routed-intent enum/handle, never the raw user prose as a leakable field; Larry routes the free text). Do NOT change writeEdge's logic; the additive Set membership is the only change. Add a SUBSTRATE-CONTRACT.md Phase 136-06 amendment paragraph noting the +2 edge types and the consumer (Plan 06 gate.cjs). No em-dashes. Write the DECISION/FREE_TEXT half of tests/136-gate-write-node.test.cjs (Tests 1-4 above): assert the delta is exactly +2 by named membership, never an absolute size.</action>
  <verify>
    <automated>node --test tests/136-gate-write-node.test.cjs && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - writeEdge accepts DECISION and FREE_TEXT; a bogus type still rejects; the allow-list delta is exactly +2 by named membership.
    - edges.cjs carries a per-type comment block citing Part 3/4/8 for each new type (matching the INFORMS/REJECTED_BECAUSE idiom).
    - SUBSTRATE-CONTRACT.md gains the 136-06 amendment; node scripts/check-substrate.cjs --diff finds no new violation.
    - No em-dashes in the touched files.
  </acceptance_criteria>
  <done>DECISION and FREE_TEXT are canon-legal edge types on the closed allow-list with the chokepoint contract amended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The richer multi-select + free-text gate widget (write node)</name>
  <files>lib/tui/gate.cjs, tests/136-gate-write-node.test.cjs</files>
  <behavior>
    - Test 1 (Req 6): toggling 2 options + entering free text commits EXACTLY 3 edges through navigation.cjs writeEdge - 2 DECISION + 1 FREE_TEXT.
    - Test 2: the live "would write to room.db" preview reflects the pending edges BEFORE commit (it lists 2 pending DECISION + 1 FREE_TEXT while still uncommitted).
    - Test 3: cancel (Esc) before commit writes ZERO edges (silent-dismiss allowed only pre-commit).
    - Test 4: confirm (Enter) fans ONE memory_event to the inline + web + tui surfaces (a single fan-out event, asserted once).
    - Test 5: a reject-with-reason path captures the reason as a REJECTED_BECAUSE edge property (Canon Part 4 rejection-is-data).
  </behavior>
  <read_first>
    - lib/hmi/selector-dispatcher.cjs lines 121-129, 216-227, 657-758 (aliasToCanonical, ensureFreeTextLast, pickShape contract - the {shape, rendered:{zones, contract:{verbs}}} shape to mirror; Free-Text always last)
    - lib/core/navigation/edges.cjs writeEdge (Task 1 - the commit primitive; DECISION/FREE_TEXT now allowed)
    - lib/core/navigation/focus.cjs setFocus + navigation.cjs confirmNode (if a truth-claim node is confirmed at the gate, route through confirmNode with a human byUser per Part 9)
    - 136-UI-SPEC.md "Gate widget states" + "Copywriting Contract" (the preview label "would write to room.db", the commit confirmation copy, the reject-with-reason -> REJECTED_BECAUSE edge)
    - 136-PATTERNS.md "lib/tui/gate.cjs ... MIRROR" (do not invent a bespoke selector)
  </read_first>
  <action>Build lib/tui/gate.cjs as the >4-options / persistent ink variant of the Part 3 Shape F selector, MIRRORING selector-dispatcher.cjs's contract (consume the same {contract:{verbs}} shape; Free-Text always last via the ensureFreeTextLast invariant; aliasToCanonical so aliases render and canonical verbs persist). Render via React.createElement (no JSX). Arrow moves, Space toggles (☐ -> ☑), Tab jumps to the free-text slot, Enter commits the SET, Esc cancels. On every toggle/keystroke, update a live "would write to room.db" preview listing the pending edges (no write yet). On commit: each toggled option -> navigation.writeEdge(db, {edge_type:'DECISION', ...}) carrying the tri-context (LOCAL+BRAIN+SIGNAL) enum/handle properties (Part 4/8, no raw user bytes); the free-text -> navigation.writeEdge(db, {edge_type:'FREE_TEXT', ...}) Larry routes; fan ONE memory_event to inline + web + tui. A reject path captures the reason as a REJECTED_BECAUSE edge property (Part 4). On Esc before commit: zero writes. Consume the Plan 03 token core (no hardcoded hex/glyph). Inline variant note: <=4 options + non-persistent uses native AskUserQuestion in the conversation pane; this widget is the >4/persistent surface. gate.cjs reaches room.db ONLY through navigation.cjs (no direct sqlite). Write the gate half of tests/136-gate-write-node.test.cjs (Tests 1-5 above).</action>
  <verify>
    <automated>node --test tests/136-gate-write-node.test.cjs && node scripts/check-tokens.cjs --diff && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - Toggling 2 options + free-text commits exactly 3 edges (2 DECISION + 1 FREE_TEXT) through navigation.cjs writeEdge.
    - The preview reflects the pending edges before commit; Esc before commit writes zero edges.
    - Confirm fans exactly one memory_event to all surfaces; a reject-with-reason writes a REJECTED_BECAUSE edge carrying the reason property.
    - gate.cjs uses React.createElement (no JSX), consumes the token core (check-tokens --diff passes), and opens no sqlite directly (check-substrate --diff clean).
  </acceptance_criteria>
  <done>The gate widget turns a multi-select + free-text choice into the exact typed edges with a live preview, a single fan-out event, and rejection-as-data, mirroring the selector contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate free-text -> edge property | raw user prose must not become a leakable edge field; only the routed enum/handle persists (Part 8) |
| gate -> room.db | the gate must write only through navigation.cjs writeEdge (Part 9) |
| pre-commit preview -> graph | nothing is written until Enter; cancel must leave zero edges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-06-LEAK | Information Disclosure | FREE_TEXT edge properties | mitigate | edge properties ENUM-ONLY per Part 8; the free text is routed by Larry, not stored as a leakable field; check-substrate on edges.cjs (Task 1/2) |
| T-136-06-PHANTOM | Tampering | a write before confirm / on cancel | mitigate | nothing writes until Enter; the preview is read-only; Esc writes zero; test asserts zero edges on cancel (Task 2) |
| T-136-06-BYPASS | Tampering | the gate opening room.db directly | mitigate | gate.cjs writes only via navigation.cjs writeEdge; check-substrate --diff flags any direct open (Task 2) |
| T-136-06-CONFIRM | Elevation of Privilege | agent confirming a truth-claim node at the gate | mitigate | a truth-claim confirm routes through confirmNode with a human byUser (Part 9 v1.5); agent-attributed confirm is rejected by the existing guard |
</threat_model>

<verification>
- node --test tests/136-gate-write-node.test.cjs passes (3 edges on toggle, preview, zero-on-cancel, one fan-out event, REJECTED_BECAUSE on reject).
- node scripts/check-tokens.cjs --diff + node scripts/check-substrate.cjs --diff pass.
- ALLOWED_EDGE_TYPES delta is exactly +2 by named membership.
- SUBSTRATE-CONTRACT.md carries the 136-06 amendment; no em-dashes.
</verification>

<success_criteria>
The gate is the write node: DECISION + FREE_TEXT are canon-legal edge types, the multi-select + free-text widget commits exactly the toggled set as typed edges through the chokepoint with a live preview, zero-on-cancel, a single fan-out event, and rejection-as-data.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-06-SUMMARY.md` when done
</output>
