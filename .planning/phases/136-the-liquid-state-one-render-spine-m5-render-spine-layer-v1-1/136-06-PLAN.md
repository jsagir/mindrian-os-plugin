---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 06
type: execute
wave: 3
depends_on: ["136-02", "136-03"]
files_modified:
  - lib/core/navigation/edges.cjs
  - docs/architecture/SUBSTRATE-CONTRACT.md
  - lib/hmi/gate-write.cjs
  - tests/136-gate-write-node.test.cjs
autonomous: true
requirements: ["Req 6"]
must_haves:
  truths:
    - "DECISION and FREE_TEXT are members of the ALLOWED_EDGE_TYPES allow-list"
    - "Toggling 2 options + free-text commits exactly 3 edges (2 DECISION + 1 FREE_TEXT) through navigation.cjs writeEdge"
    - "A live 'would write to room.db' preview reflects pending edges before confirm; cancel writes zero"
    - "Confirm fans one memory_event to inline + web + tui surfaces"
    - "A rejection-with-reason captures the reason as a REJECTED_BECAUSE edge property"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "DECISION + FREE_TEXT added to ALLOWED_EDGE_TYPES (additive)"
      contains: "DECISION"
    - path: "lib/hmi/gate-write.cjs"
      provides: "pure ink-free decision-gate WRITE module (build pending edges, preview, commit set, cancel, reject-with-reason)"
      min_lines: 30
  key_links:
    - from: "lib/hmi/gate-write.cjs"
      to: "lib/core/navigation/edges.cjs writeEdge"
      via: "each pending toggled option commits a typed DECISION edge; free-text a FREE_TEXT edge"
      pattern: "writeEdge"
    - from: "lib/hmi/gate-write.cjs"
      to: "lib/hmi/selector-dispatcher.cjs"
      via: "consumes the Shape F selector contract verbs (no bespoke selector)"
      pattern: "verbs"
---

<objective>
Build the decision-gate WRITE node as a PURE, ink-free CJS module (Req 6, D-13): add DECISION + FREE_TEXT to the edge allow-list (additive), and build the pure write-logic the gate's behavior rests on - building the pending-edge set from a toggled multi-select + always-present free-text, a live "would write to room.db" preview reflecting the pending edges before confirm, committing exactly the toggled set as typed edges (2 DECISION + 1 FREE_TEXT) through navigation.cjs writeEdge, zero writes on cancel, one fan-out memory_event on confirm, and rejection-with-reason captured as a REJECTED_BECAUSE edge property. This module lives in lib/hmi/ (NOT lib/tui/), so it is outside the seamless-no-ink guard and carries zero ink/react dependency. The ink presentation shell that renders this pure module (the >4-option / persistent widget) is owned by Plan 05's tui wave work, where ink is installed and the lazy-import discipline lives. The inline gate path (native AskUserQuestion, <=4 options, the seamless default) has NO ink dependency and is the seamless surface for this same contract.

Purpose: D-13 / Canon Part 3+4 make the gate the point where browsing becomes a typed graph mutation; reuse the selector-dispatcher contract rather than inventing a bespoke widget (Part 7). Keeping the write logic pure (no ink) lets the Req 6 test run with ink physically absent and keeps this plan parallel with Plan 05 in Wave 3.
Output: DECISION + FREE_TEXT edge types; lib/hmi/gate-write.cjs (pure); a gate-write-node test importing ONLY the pure module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-PATTERNS.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-UI-SPEC.md
@docs/MINDRIAN-CANON.md
@lib/core/navigation/edges.cjs

<interfaces>
From lib/core/navigation/edges.cjs (the additive allow-list - DECISION + FREE_TEXT are NOT yet members):
  const ALLOWED_EDGE_TYPES = Object.freeze(new Set([ ... ]))  // line 32; current members include DEFERRED, REJECTED, DERIVED_FROM, FILED_AS_DECISION, FOLLOWS_FROM, OPERATOR_TRANSITION, INFORMS, REJECTED_BECAUSE (line 128)
  function writeEdge(db, params)  // line 146; rejects any edge_type not in the Set (lines 157-179); properties are ENUM-ONLY per Canon Part 8
  module.exports = { ALLOWED_EDGE_TYPES, writeEdge }  // line 179
  The file's history is additive edge-type extension with a per-type comment block citing Canon Part 4 + Part 8 (the Phase 120/125/129 idiom - copy it).

From lib/hmi/selector-dispatcher.cjs (the contract to MIRROR, do NOT reinvent):
  pickShape(...) returns { shape, rendered: { zones, contract } }  // lines 657-758; contract.verbs is the option list
  ensureFreeTextLast(...)  // lines 216-227; Free-Text is ALWAYS the last verb (Part 3), except contract.freeTextOffered===false
  aliasToCanonical(verb, aliasMap)  // lines 121-129; aliases render, canonical verbs persist
  The 10 MindrianOS-native verbs (Canon Part 3) are the closed vocabulary; each toggled option -> DECISION edge, open-text -> FREE_TEXT edge (D-13).

From navigation.cjs: writeEdge (re-exported, line 94) - the ONLY door to room.db for the commit path (Part 9 chokepoint); confirmNode (line 147, human-confirms-truth if a truth-claim node is confirmed at the gate).

From 136-UI-SPEC.md "Gate widget states" (the behavior the pure module models, surface-agnostic): Option OFF (pending none) / ON (pending DECISION) / Free-text present (pending FREE_TEXT) / Live preview ("would write to room.db") / Commit (each pick -> a typed edge, one fan-out memory_event) / Cancel (zero writes) / Reject-with-reason -> REJECTED_BECAUSE edge property (Part 4). The ink rendering of these states is Plan 05's gate widget surface; this plan owns ONLY the pure write logic those states drive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DECISION + FREE_TEXT to the edge allow-list (additive)</name>
  <files>lib/core/navigation/edges.cjs, docs/architecture/SUBSTRATE-CONTRACT.md</files>
  <behavior>
    - Test 1: writeEdge(db, {edge_type:'DECISION', ...}) succeeds (DECISION is now an allowed type).
    - Test 2: writeEdge(db, {edge_type:'FREE_TEXT', ...}) succeeds.
    - Test 3: writeEdge with a bogus edge_type still rejects (the allow-list is still closed).
    - Test 4: the ALLOWED_EDGE_TYPES delta is exactly +2 (named-membership assertion, never a brittle absolute Set size).
  </behavior>
  <read_first>
    - lib/core/navigation/edges.cjs lines 32-128 (ALLOWED_EDGE_TYPES Set + the per-type comment-block idiom; INFORMS/REJECTED_BECAUSE at lines 106-128 are the most recent additive precedent to copy)
    - lib/core/navigation/edges.cjs lines 146-179 (writeEdge: the edge_type allow-list check + the ENUM-ONLY properties rule per Canon Part 8)
    - 136-PATTERNS.md "Additive edge-type allow-list extension" (the Phase 120/125/129 additive idiom this task mirrors)
    - docs/architecture/SUBSTRATE-CONTRACT.md (the M11 amendment rule for any allow-list/chokepoint change)
    - docs/MINDRIAN-CANON.md Part 3 (the 10 verbs -> DECISION edge) + Part 4 (every choice is graph data) + Part 8 (enum-only edge properties)
  </read_first>
  <action>Add DECISION and FREE_TEXT to ALLOWED_EDGE_TYPES in lib/core/navigation/edges.cjs as an additive block (mirror the INFORMS/REJECTED_BECAUSE idiom verbatim) with a per-type comment block citing Canon Part 3 (DECISION = the typed edge a Decision-Gate verb pick produces) + Part 4 (every choice is graph data) + Part 8 (properties ENUM-ONLY - the FREE_TEXT edge carries the routed-intent enum/handle, never the raw user prose as a leakable field; Larry routes the free text). Do NOT change writeEdge's logic; the additive Set membership is the only change. Add a SUBSTRATE-CONTRACT.md Phase 136-06 amendment paragraph noting the +2 edge types and the consumer (Plan 06 lib/hmi/gate-write.cjs as the pure write module; Plan 05 gate.cjs as the ink shell that calls it). No em-dashes. Write the DECISION/FREE_TEXT half of tests/136-gate-write-node.test.cjs (Tests 1-4 above): assert the delta is exactly +2 by named membership, never an absolute size.</action>
  <verify>
    <automated>node --test tests/136-gate-write-node.test.cjs && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - writeEdge accepts DECISION and FREE_TEXT; a bogus type still rejects; the allow-list delta is exactly +2 by named membership.
    - edges.cjs carries a per-type comment block citing Part 3/4/8 for each new type (matching the INFORMS/REJECTED_BECAUSE idiom).
    - SUBSTRATE-CONTRACT.md gains the 136-06 amendment naming lib/hmi/gate-write.cjs (Plan 06) + gate.cjs (Plan 05) as the consumers; node scripts/check-substrate.cjs --diff finds no new violation.
    - No em-dashes in the touched files.
    - Maps to 136-VALIDATION Req 6 (the writeEdge allow-list half of tests/136-gate-write-node.test.cjs).
  </acceptance_criteria>
  <done>DECISION and FREE_TEXT are canon-legal edge types on the closed allow-list with the chokepoint contract amended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The pure ink-free decision-gate WRITE module (write node)</name>
  <files>lib/hmi/gate-write.cjs, tests/136-gate-write-node.test.cjs</files>
  <behavior>
    - Test 1 (Req 6): building a pending set from 2 toggled options + a free-text entry, then committing, calls navigation.cjs writeEdge EXACTLY 3 times - 2 DECISION + 1 FREE_TEXT.
    - Test 2: a preview function returns the pending edges (2 pending DECISION + 1 pending FREE_TEXT) BEFORE any commit, and zero edges have been written to room.db at preview time (assert the edges table count is unchanged).
    - Test 3: cancel before commit writes ZERO edges (the pending set is discarded; the edges table count is unchanged).
    - Test 4: confirm (commit) fans ONE memory_event flagged for the inline + web + tui surfaces (a single fan-out event, asserted once, not three rows).
    - Test 5: a reject-with-reason path writes a REJECTED_BECAUSE edge carrying the reason as a property (Canon Part 4 rejection-is-data).
  </behavior>
  <read_first>
    - lib/hmi/selector-dispatcher.cjs lines 121-129, 216-227, 657-758 (aliasToCanonical, ensureFreeTextLast, pickShape contract - the {shape, rendered:{zones, contract:{verbs}}} shape to consume; Free-Text always last)
    - lib/core/navigation/edges.cjs writeEdge (Task 1 - the commit primitive; DECISION/FREE_TEXT now allowed)
    - lib/core/navigation.cjs writeEdge re-export (line 94) - the chokepoint the module commits through; confirmNode (line 147) if a truth-claim node is confirmed at the gate (route through confirmNode with a human byUser per Part 9)
    - 136-PATTERNS.md "lib/tui/gate.cjs ... MIRROR" + "Single room.db door" (consume the selector contract; reach room.db ONLY through navigation.cjs - lib/hmi/ is not allow-listed for direct sqlite)
    - 136-UI-SPEC.md "Gate widget states" + "Copywriting Contract" (the preview label "would write to room.db", the commit confirmation copy, the reject-with-reason -> REJECTED_BECAUSE edge) - the pure module exposes these as functions/return values; the ink rendering is Plan 05
  </read_first>
  <action>Build lib/hmi/gate-write.cjs as a PURE, ink-free CJS module (require('react') and import('ink') are FORBIDDEN here - this file lives in lib/hmi/, outside lib/tui/, and must carry zero ink/react dependency so the Req 6 test runs with ink absent). It MIRRORS lib/hmi/selector-dispatcher.cjs's contract: consume the same {contract:{verbs}} shape, apply ensureFreeTextLast so Free-Text is always the last offered option, and aliasToCanonical so aliases render but canonical verbs persist. Expose pure functions (no terminal I/O, no React): one that builds the pending-edge set from a toggled-option list + an optional free-text string (each toggled option -> a pending DECISION edge descriptor carrying the tri-context LOCAL+BRAIN+SIGNAL enum/handle properties per Part 4/8, no raw user bytes; the free-text -> a pending FREE_TEXT edge descriptor whose property is the routed-intent enum/handle, never the raw prose - Larry routes the free text); one that returns the live "would write to room.db" preview from the pending set without writing (read-only); one commit function that takes a db handle + the pending set and calls navigation.writeEdge once per pending edge (2 DECISION + 1 FREE_TEXT for the canonical case) then fans ONE memory_event flagged for inline + web + tui (a single event, not one per surface); one cancel path that discards the pending set with zero writes; and a reject-with-reason path that calls navigation.writeEdge with edge_type REJECTED_BECAUSE carrying the reason property (Part 4). The module reaches room.db ONLY through navigation.cjs (no direct sqlite open; lib/hmi/ is not substrate-allow-listed). If a truth-claim node is confirmed at the gate, route through navigation.confirmNode with a human byUser (Part 9), never a direct confirmed-status write. No em-dashes. Write the gate-write half of tests/136-gate-write-node.test.cjs (Tests 1-5 above) importing ONLY lib/hmi/gate-write.cjs (NOT lib/tui/gate.cjs) so the suite has zero ink dependency.</action>
  <verify>
    <automated>node --test tests/136-gate-write-node.test.cjs && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - Building a pending set from 2 toggled options + free-text and committing calls navigation.cjs writeEdge exactly 3 times (2 DECISION + 1 FREE_TEXT).
    - The preview function reflects the pending edges before commit and writes zero edges (edges table count unchanged at preview); cancel before commit writes zero edges.
    - Commit fans exactly one memory_event flagged for all three surfaces; a reject-with-reason writes a REJECTED_BECAUSE edge carrying the reason property.
    - lib/hmi/gate-write.cjs contains ZERO ink/react references (grep for require('ink') / await import('ink') / require('react') returns 0) and opens no sqlite directly (check-substrate --diff clean); the test imports only lib/hmi/gate-write.cjs and passes with ink physically absent.
    - Maps to 136-VALIDATION Req 6 (tests/136-gate-write-node.test.cjs: exactly 3 edges via writeEdge; preview reflects pending; one fan-out event).
  </acceptance_criteria>
  <done>The pure write module turns a multi-select + free-text choice into the exact typed edges with a live read-only preview, a single fan-out event, and rejection-as-data, mirroring the selector contract and carrying zero ink dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate free-text -> edge property | raw user prose must not become a leakable edge field; only the routed enum/handle persists (Part 8) |
| gate-write -> room.db | the module must write only through navigation.cjs writeEdge (Part 9); lib/hmi/ is not substrate-allow-listed for direct sqlite |
| pre-commit preview -> graph | nothing is written until commit; cancel must leave zero edges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-06-LEAK | Information Disclosure | FREE_TEXT edge properties | mitigate | edge properties ENUM-ONLY per Part 8; the free text is routed by Larry, not stored as a leakable field; check-substrate on edges.cjs (Task 1/2) |
| T-136-06-PHANTOM | Tampering | a write before confirm / on cancel | mitigate | nothing writes until commit; the preview is read-only; cancel writes zero; test asserts zero edges on cancel and at preview (Task 2) |
| T-136-06-BYPASS | Tampering | the module opening room.db directly | mitigate | gate-write.cjs writes only via navigation.cjs writeEdge; lib/hmi/ is not substrate-allow-listed so check-substrate --diff flags any direct open (Task 2) |
| T-136-06-CONFIRM | Elevation of Privilege | agent confirming a truth-claim node at the gate | mitigate | a truth-claim confirm routes through navigation.confirmNode with a human byUser (Part 9 v1.5); agent-attributed confirm is rejected by the existing guard |
</threat_model>

<verification>
- node --test tests/136-gate-write-node.test.cjs passes with ink absent (3 edges on commit, read-only preview, zero-on-cancel, one fan-out event, REJECTED_BECAUSE on reject) - the suite imports only lib/hmi/gate-write.cjs.
- node scripts/check-substrate.cjs --diff passes (no direct sqlite open in lib/hmi/gate-write.cjs).
- lib/hmi/gate-write.cjs has zero ink/react references.
- ALLOWED_EDGE_TYPES delta is exactly +2 by named membership.
- SUBSTRATE-CONTRACT.md carries the 136-06 amendment naming the pure module + the Plan 05 shell; no em-dashes.
</verification>

<success_criteria>
The gate is the write node, with its WRITE logic pure and ink-free: DECISION + FREE_TEXT are canon-legal edge types, the pure module commits exactly the toggled set as typed edges through the chokepoint with a live read-only preview, zero-on-cancel, a single fan-out event, and rejection-as-data - and the Req 6 test runs green with ink physically absent, keeping this plan parallel with Plan 05 in Wave 3.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-06-SUMMARY.md` when done
</output>
</parameter>
</invoke>
