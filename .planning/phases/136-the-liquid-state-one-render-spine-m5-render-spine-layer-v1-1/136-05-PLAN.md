---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 05
type: execute
wave: 3
depends_on: ["136-02", "136-03", "136-04"]
files_modified:
  - package.json
  - lib/tui/boot.cjs
  - lib/tui/tree.cjs
  - lib/tui/detail.cjs
  - lib/tui/slot.cjs
  - lib/tui/breadcrumb.cjs
  - lib/tui/footer.cjs
  - lib/tui/gate.cjs
  - bin/mindrian-tools.cjs
  - tests/136-tui-boot.test.cjs
  - tests/136-fractal.test.cjs
  - tests/136-gate-widget.test.cjs
autonomous: false
user_setup: []
requirements: ["Req 4", "Req 8"]
must_haves:
  truths:
    - "ink/react/yoga are vendored as the opt-in tui tree with zero native addons (verified before install)"
    - "mos tui boots via React.createElement (no JSX, no build step), arrow-navigates, and restores the terminal cleanly on quit"
    - "The seamless default still works with ink physically absent (lazy import, graceful degrade)"
    - "The 4+-level tree renders fully; zoom re-roots and writes a focus_changed event; bud yields a registered sub-room; a cross-wall edge shows with tree order byte-identical"
    - "No tui file opens room.db directly; all reads go through the SSE read-API / navigation.cjs"
    - "The >4-option / persistent gate widget renders the pure lib/hmi/gate-write.cjs module via React.createElement, reaching ink only through a lazy await import, and holds no write logic of its own"
  artifacts:
    - path: "lib/tui/boot.cjs"
      provides: "ink lazy-boot entry + terminal restore + ink-absent graceful degrade"
      contains: "await import"
    - path: "lib/tui/tree.cjs"
      provides: "arrow-key fractal tree component (createElement)"
      min_lines: 30
    - path: "lib/tui/gate.cjs"
      provides: "ink presentation shell for the >4-option / persistent gate, rendering the pure lib/hmi/gate-write.cjs module"
      contains: "gate-write"
    - path: "bin/mindrian-tools.cjs"
      provides: "lazy-required tui subcommand"
      contains: "tui"
  key_links:
    - from: "lib/tui/boot.cjs"
      to: "ink (dynamic import)"
      via: "await import('ink') inside the tui entry only"
      pattern: "await import\\('ink'\\)"
    - from: "lib/tui/tree.cjs"
      to: "navigation.cjs getNeighborhood (via SSE read-API)"
      via: "thin SSE-read client, never a direct sqlite open"
      pattern: "getNeighborhood"
    - from: "lib/tui/boot.cjs"
      to: "lib/core/navigation/focus.cjs setFocus"
      via: "zoom writes focus_changed (D-04)"
      pattern: "setFocus"
    - from: "lib/tui/gate.cjs"
      to: "lib/hmi/gate-write.cjs"
      via: "the ink shell renders the pure write module; toggles/commit/cancel/reject delegate to it"
      pattern: "gate-write"
---

<objective>
Build the opt-in mos tui ink navigator + fractal navigation + the ink gate-widget surface (Req 4/8, D-12/D-04/D-03/D-13): vendor ink/react/yoga (gated by a blocking checkpoint:human-verify before install, slopcheck unavailable), build the lib/tui/ ink app (lazy boot, no JSX, terminal restore), wire it as a thin SSE-read client, implement fractal zoom (focus.cjs), bud (delegate to the confirmed orchestrator), cross-wall edges, AND build lib/tui/gate.cjs - the >4-option / persistent ink presentation shell that renders the pure lib/hmi/gate-write.cjs module (Plan 06 owns that pure module + its Req 6 test). Depends on Plan 02 (SSE) + Plan 03 (token core) + Plan 04 (engine tui-delivery payload). Owns lib/tui/* (INCLUDING gate.cjs) + bin/mindrian-tools.cjs.

Purpose: D-09 demotes the tui to opt-in but D-12 still requires a buildless, lazy, vendoring-clean ink app; D-04/D-03 reuse the focus event + the room-creation orchestrator rather than reimplementing; D-13 puts the richer gate widget on the ink surface where ink is installed. Plan 06 keeps the gate's PURE write logic (the Req 6 test target) ink-free in lib/hmi/; this plan owns ONLY the ink rendering of that contract, so the gate widget lands where ink legitimately lives. gate.cjs delegates every write to the pure module via a require path (verified by source-grep), so this plan keeps NO runtime dependency on Plan 06 executing and stays in Wave 3 parallel with it.
Output: vendored ink tree; lib/tui/{boot,tree,detail,slot,breadcrumb,footer,gate}.cjs; mos tui subcommand; tui-boot + fractal + gate-widget tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-RESEARCH.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-PATTERNS.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-UI-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-BUD-TARGET.md

<interfaces>
ink lazy-boot (PROVEN in RESEARCH Pattern 1, this session) - CJS, no JSX, no build step:
  const { render, Box, Text, useInput, useApp } = await import('ink')  // ESM, lazy
  const React = require('react')  // CJS-compatible
  const e = React.createElement
  render(e(App)) returns { waitUntilExit }  // ink restores the terminal on unmount/exit
  ink-absent graceful degrade: try/catch the import -> emit the UI-SPEC notice, exit code 2

Component-to-zone map (136-UI-SPEC Component Decomposition): App=root (boot.cjs), Breadcrumb=Zone1, Tree+Detail=Zone2, Slot=Zone3, Footer=Zone4. All pure SSE-read clients; NONE opens room.db.
Read seam: tree/detail data from navigation.getNeighborhood (neighborhood.cjs:48 getNeighborhood(db, focusNodeId, opts)) shaped into the enum/handle SSE payload (Plan 04 tui delivery); slot whisper from findRecentChanges + findSurfaceableTensions intent-filtered (D-07).

Zoom (D-04, reuse focus.cjs verbatim - no new event type):
  navigation.setFocus(db, sessionId, nodeId, 'user')  // writes focus_changed memory_event; VALID_SET_BY includes 'user'
  Larry's next inline turn reads navigation.getActiveFocus(db, sessionId) -> two-way sync (D-05)
  focus_changed is a system-bookkeeping node (Part 9 v1.5 carve-out) - NO human-confirm needed

BUD (D-03, confirmed in 136-BUD-TARGET.md - DELEGATE, never reimplement): the navigator surfaces "bud / open as room" and CALLS lib/core/room-auto-create.cjs::autoCreatePlaceholderRoom + scripts/room-registry create + lib/core/room-receipt-emit.cjs::emitReceiptWritten. No single SEED-001 callable exists; compose the confirmed orchestrator.

Gate widget (D-13, Plan 06 owns the pure logic): lib/hmi/gate-write.cjs (built in Plan 06) exposes the pure ink-free write functions - build the pending set from toggled options + free-text, the read-only "would write to room.db" preview, commit (writeEdge per pick + one fan-out memory_event), cancel (zero writes), reject-with-reason (REJECTED_BECAUSE edge). lib/tui/gate.cjs is the ink RENDERING of that contract for the >4-option / persistent surface; the inline <=4-option path stays native AskUserQuestion (no ink). gate.cjs holds NO write logic of its own - it requires lib/hmi/gate-write.cjs and delegates every toggle/commit/cancel/reject to it. Because the gate-widget test is a source-grep (not a runtime execution of the pure module), this plan carries no wave-level dependency on Plan 06 executing first; the runtime delegation resolves when both plans have shipped.

Key map (136-UI-SPEC, LOCKED keys): Up/Down move; Right/Enter expand; Left collapse; Tab cycle panes; q quit; shape hotkeys d/w/g/m/k/s; a zoom/re-root; b bud.
LOCKED constraints: lazy await import('ink') inside the tui entry ONLY (never top-level require) - this binds gate.cjs identically to boot.cjs; React.createElement only (no JSX); thin SSE-read client (no direct sqlite); token core (no hardcoded hex/glyph); cross-platform path.join/normwin.

From bin/mindrian-tools.cjs (lines 5-7, 26-71, 89): switch-case router; USAGE heredoc; lazy in-case require so ink/launcher code never loads for the seamless base.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify ink/react/yoga legitimacy before install (slopcheck unavailable)</name>
  <what-built>RESEARCH ran a real npm install of ink@7.0.5 + react@19.2.6 (yoga-layout@3.2.1 transitive): 38 packages, 19 MB, ZERO .node/.wasm/.gyp, ZERO install/postinstall/preinstall scripts, ws native peers optional + un-installed. slopcheck was NOT available, so per protocol all three are tagged [ASSUMED] until human confirmation.</what-built>
  <how-to-verify>
    1. Confirm ink at https://www.npmjs.com/package/ink (vadimdemedes/ink, mature, ESM, react peer >=19.2.0, engines node >=22).
    2. Confirm react at https://www.npmjs.com/package/react (facebook/react, 19.2.6).
    3. Confirm yoga-layout at https://www.npmjs.com/package/yoga-layout (facebook/yoga, 3.2.1, ships base64-inlined WASM in JS, zero deps, no install scripts).
    4. Confirm the three exact pinned versions (ink 7.0.5 / react 19.2.6 / yoga 3.2.1) are the intended targets.
  </how-to-verify>
  <acceptance_criteria>
    - Human types "approved" (or names the corrected versions). This gate is NEVER auto-approvable (workflow.auto_advance is ignored for package-legitimacy checkpoints) because slopcheck was unavailable.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the pinned install, or name corrected versions.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Vendor ink + build the lib/tui ink app (lazy boot, no JSX, terminal restore)</name>
  <files>package.json, lib/tui/boot.cjs, lib/tui/tree.cjs, lib/tui/detail.cjs, lib/tui/slot.cjs, lib/tui/breadcrumb.cjs, lib/tui/footer.cjs, bin/mindrian-tools.cjs, tests/136-tui-boot.test.cjs</files>
  <read_first>
    - 136-RESEARCH.md Pattern 1 (the PROVEN CJS boot harness) + the Lazy-load mechanism section (await import('ink') + require('react'), try/catch graceful degrade, exit 2) + Pitfall 1/2 (ESM-only ink; ink must not leak into the seamless base)
    - 136-PATTERNS.md "Net-New Files With No Role+Flow Analog" (boot.cjs buildable pattern + component-to-zone map + the read seam)
    - 136-UI-SPEC.md Component Decomposition + Interaction State Contracts (the key map + tree node states + the ink-absent notice copy)
    - bin/mindrian-tools.cjs lines 26-71, 89 (USAGE heredoc + the switch-case; lazy in-case require)
    - tests/136-seamless-no-ink.test.cjs (Plan 01 - the guard this plan must keep green: no top-level require('ink') in lib/tui/)
  </read_first>
  <action>Pin ink@7.0.5 + react@19.2.6 in package.json (yoga-layout@3.2.1 arrives transitively - do NOT add it directly) and install. Build lib/tui/boot.cjs: an async bootTui(roomDir) that lazy `await import('ink')` + `require('react')` inside a try/catch (on failure emit the UI-SPEC notice "[mos tui] ink not installed. The seamless inline + web surfaces work without it." and process.exit(2)); render the App root via React.createElement; ink restores the terminal on exit (waitUntilExit). The App routes the LOCKED keys (Up/Down/Right/Enter/Left/Tab/q + shape hotkeys d/w/g/m/k/s + a zoom + b bud) via useInput. Build the 5 zone components via React.createElement, each a PURE SSE-read client consuming the Plan 04 tui-delivery enum/handle payload (NONE opens room.db): breadcrumb.cjs (Zone1 room>section>node path, focus node bold), tree.cjs (Zone2 left, arrow-key fractal tree, 2-space indent per depth, the empty-section glyph, the cross-room glyph, the tension suffix, never reorder), detail.cjs (Zone2 right inspector), slot.cjs (Zone3 LazyGraph whisper, omitted when silent), footer.cjs (Zone4 key-hints). Consume the Plan 03 token core (no hardcoded hex/glyph). Add a 'tui' case to bin/mindrian-tools.cjs that lazy in-case requires lib/tui/boot.cjs and calls bootTui(roomDir) (NOT a top-level require) + a USAGE line. Cross-platform: path.join only (D-15). Write tests/136-tui-boot.test.cjs: boot against the seeded fixture, feed simulated arrow input, assert a clean exit (terminal restored); assert the seamless base still loads with ink absent (re-run the Plan 01 guard).</action>
  <verify>
    <automated>node --test tests/136-tui-boot.test.cjs && node --test tests/136-seamless-no-ink.test.cjs && find node_modules -iregex '.*\.\(node\|wasm\|gyp\)$' | grep -v '^$' | wc -l | grep -qx 0 && echo VENDOR_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - mos tui boots against the seeded room, arrow-navigates on simulated input, and restores the terminal cleanly on q (Req 4).
    - The seamless base test (136-seamless-no-ink) stays green: ink is referenced ONLY via await import('ink') inside lib/tui/, never a top-level require.
    - find node_modules -iregex '.*\.(node|wasm|gyp)$' returns empty (zero native addons in the vendored tree, Req 4/11).
    - node scripts/check-tokens.cjs --diff passes on lib/tui/* (no hardcoded hex/glyph); check-substrate --diff finds no direct sqlite open in lib/tui/*.
  </acceptance_criteria>
  <done>The opt-in ink navigator boots buildlessly, restores the terminal, degrades gracefully when ink is absent, and the vendored tree has zero native addons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fractal navigation - depth + zoom (focus.cjs) + bud (delegate) + cross-wall edges</name>
  <files>lib/tui/boot.cjs, lib/tui/tree.cjs, tests/136-fractal.test.cjs</files>
  <behavior>
    - Test 1: a 4+-level seeded room renders fully in the tree (all depth levels present).
    - Test 2 (D-04): zoom (a) re-roots at the selected node and writes a focus_changed memory_event via navigation.setFocus(db, sessionId, nodeId, 'user'); a breadcrumb shows the path from room root to the zoomed node.
    - Test 3 (D-03): bud (b) yields a REGISTERED sub-room by DELEGATING to autoCreatePlaceholderRoom + room-registry create (per 136-BUD-TARGET.md) - the navigator never reimplements promotion; assert a sub-room appears in the registry after bud.
    - Test 4 (Req 8): a cross-wall (cross-section) edge SHOWS in the graph/slot with the tree node order BYTE-IDENTICAL before/after (M5 hard rule 1).
  </behavior>
  <read_first>
    - lib/core/navigation/focus.cjs lines 13, 15-29, 36-81 (VALID_SET_BY includes 'user'; setFocus writes focus_changed; getActiveFocus for two-way sync; the audit-node carve-out documented at 61-70 - no human-confirm)
    - 136-BUD-TARGET.md (Plan 01 - the CONFIRMED composed BUD target: autoCreatePlaceholderRoom + room-registry create + emitReceiptWritten; NO single SEED-001 callable)
    - navigation.cjs getNeighborhood (neighborhood.cjs:48) - cross-wall edges + the fractal neighborhood; findContradictions/findSurfaceableTensions for the tension marker
    - tests/fixtures/136-seeded-room/seed.sql (Plan 01 - the 4+-level room with the CONTRADICTS cross-wall edge)
    - 136-UI-SPEC.md "Zoom / re-root + breadcrumb contract" + "Tree node states" (zoom key a, breadcrumb path, never-reorder rule)
  </read_first>
  <action>Implement fractal nav in boot.cjs + tree.cjs. Tree renders arbitrary depth (2-space indent per level) off the getNeighborhood neighborhood. Zoom (key a): call navigation.setFocus(db, sessionId, selectedNodeId, 'user') - this writes a focus_changed memory_event (reuse focus.cjs verbatim, no new event type; audit-node carve-out means no human-confirm); re-root the tree at that node and render the breadcrumb path root to node in Zone 1. Bud (key b): SURFACE "bud / open as room" and CALL the confirmed composed orchestrator from 136-BUD-TARGET.md (autoCreatePlaceholderRoom + scripts/room-registry create + emitReceiptWritten) - NEVER reimplement promotion (Canon Part 7 / D-03). Cross-wall edges: surface them from getNeighborhood in the graph view / Zone 3 slot with the cross-room glyph, NEVER by reordering the tree (M5 hard rule 1). Write tests/136-fractal.test.cjs covering the four behaviors (assert focus_changed row after zoom; assert a registered sub-room after bud; assert byte-identical tree order before/after a cross-wall edge surfaces).</action>
  <verify>
    <automated>node --test tests/136-fractal.test.cjs && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - The 4+-level fixture renders all depth levels; zoom writes a focus_changed memory_event (assert the row in the event log) and shows the root to node breadcrumb.
    - Bud produces a registered sub-room by delegating to the confirmed orchestrator (a grep proves the tui calls autoCreatePlaceholderRoom / room-registry, never a hand-rolled promotion).
    - A cross-wall edge surfaces in the graph/slot with the tree node order byte-identical (snapshot equality).
    - node scripts/check-substrate.cjs --diff finds no direct sqlite open in the tui (room.db touched only via navigation.cjs setFocus/getNeighborhood).
  </acceptance_criteria>
  <done>Fractal navigation works: depth renders, zoom re-roots and journals a focus event, bud delegates to the confirmed orchestrator, and cross-wall edges surface without reordering the tree.</done>
</task>

<task type="auto">
  <name>Task 4: The ink gate-widget shell (renders the pure lib/hmi/gate-write.cjs module)</name>
  <files>lib/tui/gate.cjs, tests/136-gate-widget.test.cjs</files>
  <read_first>
    - lib/hmi/gate-write.cjs (Plan 06 - the PURE write module this shell renders: build-pending-set, read-only preview, commit, cancel, reject-with-reason; gate.cjs holds NO write logic of its own and MUST delegate every action to this module via a require)
    - lib/tui/boot.cjs (Task 2 - the lazy await import('ink') + React.createElement idiom + the try/catch graceful-degrade exit 2; gate.cjs is bound by the SAME LOCKED ink discipline)
    - 136-PATTERNS.md "lib/tui/gate.cjs ... NET-NEW / MIRROR" + "Single room.db door" + "Graceful-degrade / defensive-default everywhere"
    - 136-UI-SPEC.md "Gate widget states" + "Copywriting Contract" (option OFF/ON glyphs, the "would write to room.db" preview label, the commit/cancel/reject copy, the inline-vs-widget split: <=4 + non-persistent = native AskUserQuestion, >4/persistent = this ink widget)
    - tests/136-seamless-no-ink.test.cjs (Plan 01 - the guard this widget MUST keep green: ink reached ONLY via await import('ink'), never a top-level require)
  </read_first>
  <action>Build lib/tui/gate.cjs as the >4-option / persistent ink presentation shell for the Part 3 Shape F selector, rendering the PURE lib/hmi/gate-write.cjs module (Plan 06) - gate.cjs is rendering ONLY and holds NO write logic. LOCKED ink discipline (identical to boot.cjs): ink is reached ONLY via a lazy `await import('ink')` inside the tui entry, NEVER a top-level require('ink'); wrap the import in try/catch and on failure emit the UI-SPEC ink-absent notice and process.exit(2) (graceful degrade); React.createElement only (no JSX). Require lib/hmi/gate-write.cjs and DELEGATE every action to it: render the gate states (arrow moves, Space toggles an option using the OFF/ON glyphs from the Plan 03 token core, Tab jumps to the free-text slot, Enter commits the SET, Esc cancels); on every toggle/keystroke call the pure module's read-only preview function and render the live "would write to room.db" preview (no write yet); on commit/cancel/reject call the pure module's commit/cancel/reject-with-reason functions. gate.cjs NEVER calls navigation.writeEdge itself and NEVER opens sqlite. Consume the Plan 03 token core (no hardcoded hex/glyph). Cross-platform: path.join only. Write tests/136-gate-widget.test.cjs as a SOURCE-GREP test (it must NOT runtime-import lib/hmi/gate-write.cjs or ink, so this suite passes whether or not Plan 06 has executed and never needs ink installed): fs.readFileSync gate.cjs and assert it references ink ONLY via await import('ink') (forbid top-level require('ink')), that it requires lib/hmi/gate-write.cjs (delegates), and that it contains no navigation.writeEdge call of its own; then re-run tests/136-seamless-no-ink.test.cjs as the acceptance gate. No em-dashes.</action>
  <verify>
    <automated>node --test tests/136-gate-widget.test.cjs && node --test tests/136-seamless-no-ink.test.cjs && node scripts/check-tokens.cjs --diff && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - lib/tui/gate.cjs references ink ONLY via await import('ink') (zero top-level require('ink')); the 136-seamless-no-ink guard stays green.
    - gate.cjs requires lib/hmi/gate-write.cjs and delegates commit/cancel/reject to it, containing no navigation.writeEdge call of its own and no direct sqlite open (check-substrate --diff clean).
    - gate.cjs renders the live "would write to room.db" preview from the pure module's read-only preview function and uses the Plan 03 token core glyphs (check-tokens --diff passes; no hardcoded hex/glyph).
    - The gate-widget test is a source-grep that needs neither ink installed nor Plan 06 executed (no runtime import), keeping this plan parallel with Plan 06 in Wave 3.
    - No em-dashes in gate.cjs.
  </acceptance_criteria>
  <done>The ink gate widget renders the pure write module on the >4-option / persistent surface with the locked lazy-import discipline, delegating every write to lib/hmi/gate-write.cjs and degrading gracefully when ink is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> vendored ink tree | a native addon or install script would break the cross-platform vendoring contract |
| tui process -> room.db | the tui must NOT open the DB; only navigation.cjs may (focus writes, neighborhood reads); the gate widget delegates writes to the pure module |
| ink import -> seamless base | a top-level ink require would pull ink into the inline/web surfaces and crash when absent - binds boot.cjs AND gate.cjs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-05-SC | Tampering | ink/react/yoga install | mitigate | blocking checkpoint:human-verify before install (Task 1, slopcheck unavailable, never auto-approvable); pinned exact versions; release-gate native-addon grep (Plan 01) |
| T-136-05-LEAK | Tampering | ink leaking into the seamless base | mitigate | await import('ink') inside lib/tui/ only (boot.cjs AND gate.cjs); the Plan 01 seamless-no-ink guard re-run as a gate (Task 2 and Task 4) |
| T-136-05-BYPASS | Tampering | a tui file opening room.db directly | mitigate | thin SSE-read client; zoom/neighborhood via navigation.cjs; the gate widget delegates writes to the pure lib/hmi/gate-write.cjs module; check-substrate --diff flags any direct open (Task 2/3/4) |
| T-136-05-BUD | Tampering | reimplementing sub-room promotion | mitigate | D-03/Part 7: delegate to the confirmed orchestrator (136-BUD-TARGET); grep asserts the tui calls autoCreatePlaceholderRoom, never hand-rolls promotion (Task 3) |
| T-136-05-NATIVE | Tampering (supply-chain) | a future bump adding a .node addon | mitigate | pin exact versions; the release.sh native-addon grep (Plan 01) runs on every release |
</threat_model>

<verification>
- node --test tests/136-tui-boot.test.cjs + tests/136-fractal.test.cjs + tests/136-gate-widget.test.cjs + tests/136-seamless-no-ink.test.cjs all pass.
- find node_modules -iregex '.*\.(node|wasm|gyp)$' returns empty.
- node scripts/check-tokens.cjs --diff + node scripts/check-substrate.cjs --diff pass on lib/tui/*.
- grep confirms ink referenced only via await import('ink') (boot.cjs AND gate.cjs); gate.cjs requires lib/hmi/gate-write.cjs and holds no writeEdge of its own; bud delegates to autoCreatePlaceholderRoom.
</verification>

<success_criteria>
The opt-in ink navigator boots buildlessly with a clean terminal restore and graceful ink-absent degrade, the vendored tree is native-addon-free, fractal navigation (depth + zoom + bud-by-delegation + cross-wall edges) works without reordering the tree or bypassing the chokepoint, and the ink gate widget renders the pure write module on the >4-option / persistent surface while delegating every write to lib/hmi/gate-write.cjs.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-05-SUMMARY.md` when done
</output>
</parameter>
</invoke>
