---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 08
type: execute
wave: 5
depends_on: ["136-02", "136-03", "136-04", "136-05", "136-06", "136-07"]
files_modified:
  - tests/run-all-136.sh
  - tests/136-canon-boundary.test.cjs
  - docs/CANON-PHASE-MAP.md
  - ~/MindrianRooms/mindrianOS/product-evolution/architectural-mandates/M5-liquid-state-fractal-sos.md
autonomous: true
requirements: ["Req 11", "Req 12"]
must_haves:
  truths:
    - "Every new file passes the Canon Part 8 boundary scan (zero user bytes to Brain)"
    - "Zero direct sqlite opens exist outside navigation.cjs + the core across all new files"
    - "The full 136 suite is green and the vendoring native-addon grep is empty"
    - "M5 is promoted from SEED to ADR"
    - "The Node 22+ floor is pinned and the cross-platform contract is asserted"
  artifacts:
    - path: "tests/136-canon-boundary.test.cjs"
      provides: "Part 8 + Part 9 + cross-platform capstone gate over all new files"
      min_lines: 20
    - path: "~/MindrianRooms/mindrianOS/product-evolution/architectural-mandates/M5-liquid-state-fractal-sos.md"
      provides: "M5 promoted SEED -> ADR"
      contains: "ADR"
  key_links:
    - from: "tests/136-canon-boundary.test.cjs"
      to: "scripts/check-substrate.cjs"
      via: "boundary + direct-sqlite scan over all new 136 files"
      pattern: "check-substrate"
    - from: "docs/CANON-PHASE-MAP.md"
      to: "M5 ADR"
      via: "Phase 136 rows flipped to shipped (Part 7/8/9/10)"
      pattern: "136"
---

<objective>
The canon + gates capstone (Req 11/12): assert every new 136 file passes the Canon Part 8 boundary scan (no user bytes to Brain) and the Part 9 single-door rule (zero direct sqlite opens outside the core), prove the full 136 suite is green with an empty vendoring native-addon grep, pin/assert the Node 22+ floor + cross-platform contract, promote M5 from SEED to ADR, and flip the CANON-PHASE-MAP Phase 136 rows to shipped. Depends on all prior plans (runs last).

Purpose: this is the phase release gate; it converts the per-plan boundary discipline into one aggregate assertion and discharges the M5 SEED -> ADR mandate (Canon Part 6).
Output: a canon-boundary capstone test; the run-all-136 harness finalized; M5 ADR; CANON-PHASE-MAP rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-RESEARCH.md
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
@scripts/check-substrate.cjs

<interfaces>
The new 136 files to scan (Part 8 + Part 9): lib/render/sse/read-api.cjs (Plan 02), lib/render/render-v2.cjs (Plan 04 additions), lib/tui/* (Plan 05/06), lib/launcher/* (Plan 07), lib/core/navigation/edges.cjs (Plan 06 additions), lib/core/room-db.cjs + navigation.cjs (Plan 02 additions).

Part 8 boundary scan: scripts/check-substrate.cjs is the grep gate (no separate brain-boundary file in this repo). Run it over all new files; assert zero user-content egress + zero direct sqlite opens outside the allow-listed lib/core/navigation/ surface. SSE payloads enum/handle only (Plan 02).

Cross-platform (Req 11, D-15): package.json engines.node already >=22.5.0 (RESEARCH Env table); assert it; the vendoring native-addon grep (find node_modules -iregex '.*\.(node|wasm|gyp)$' empty) is the cross-platform-safety assertion; the launcher platform-routing matrix (Plan 07) is the routing assertion; inline+web Windows parity is the manual verify (136-VALIDATION).

M5 SEED -> ADR (Req 12, Canon Part 6): ~/MindrianRooms/mindrianOS/product-evolution/architectural-mandates/M5-liquid-state-fractal-sos.md is the SEED; promote its status header SEED -> ADR and record this phase as the implementing phase. Flip the CANON-PHASE-MAP Phase 136 rows (Part 7 token consolidation, Part 8 boundary, Part 9 chokepoint, Part 10 render-is-surface) to shipped.

The full suite: bash tests/run-all-136.sh (Plan 01 harness) must enumerate all 10 136-*.test.cjs suites + the new canon-boundary suite and be green.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canon Part 8 + Part 9 + cross-platform capstone gate over all new files</name>
  <files>tests/136-canon-boundary.test.cjs, tests/run-all-136.sh</files>
  <behavior>
    - Test 1 (Part 8): check-substrate.cjs over every new 136 file reports zero boundary violations (no fetch/http/Brain call; no user-content egress).
    - Test 2 (Part 9): grep over all new files finds zero direct node:sqlite/DatabaseSync opens outside lib/core/navigation/ (read-api, lib/tui/*, lib/launcher/* all reach room.db only via the chokepoint).
    - Test 3 (Req 11): package.json engines.node is pinned >=22; the vendoring grep find node_modules -iregex '.*\.(node|wasm|gyp)$' is empty.
    - Test 4: the SSE read-API payload schema is enum/handle only (re-assert the Plan 02 contract aggregate).
  </behavior>
  <read_first>
    - scripts/check-substrate.cjs (the boundary + direct-sqlite grep gate; the allow-list at lines 63-85 and the --baseline/--diff modes)
    - tests/run-all-136.sh (Plan 01 - the harness CJS_SUITES array to finalize with all 10 suites + canon-boundary)
    - lib/render/sse/read-api.cjs + lib/tui/boot.cjs + lib/launcher/detect.cjs (the new files to scan - confirm each routes room.db through navigation.cjs only)
    - 136-RESEARCH.md Security Domain (the threat patterns: user content via SSE/render; thin client direct open; native addon; HTML injection) + Env Availability (Node 22 floor)
  </read_first>
  <action>Create tests/136-canon-boundary.test.cjs: enumerate the new 136 files (read-api.cjs, render-v2.cjs, all lib/tui/*, all lib/launcher/*, edges.cjs, room-db.cjs, navigation.cjs) and assert (1) check-substrate.cjs reports zero boundary violations on them (Part 8); (2) a grep finds zero direct node:sqlite/DatabaseSync opens outside lib/core/navigation/ (Part 9 single door); (3) package.json engines.node is >=22 and the vendoring native-addon grep is empty (Req 11); (4) the SSE payload schema is enum/handle only. Finalize tests/run-all-136.sh: ensure the CJS_SUITES array lists all 10 phase suites (engine-shapes, seamless-no-ink, thin-clients-converge, tui-boot, launcher-detect, gate-write-node, token-linter, fractal, lazygraph-overlay, dual-render) plus 136-canon-boundary, and that it exits non-zero on any fail. No em-dashes.</action>
  <verify>
    <automated>node --test tests/136-canon-boundary.test.cjs && bash tests/run-all-136.sh</automated>
  </verify>
  <acceptance_criteria>
    - check-substrate.cjs reports zero boundary violations across all new 136 files (Part 8).
    - Zero direct sqlite opens exist outside lib/core/navigation/ (Part 9 single door) - grep assertion over all new files.
    - package.json engines.node is >=22 and find node_modules -iregex '.*\.(node|wasm|gyp)$' is empty (Req 11 cross-platform).
    - bash tests/run-all-136.sh is GREEN (all 11 suites pass), exiting 0.
  </acceptance_criteria>
  <done>One aggregate gate proves the Part 8 boundary, the Part 9 single door, the cross-platform contract, and a green full suite.</done>
</task>

<task type="auto">
  <name>Task 2: Promote M5 SEED -> ADR + flip the CANON-PHASE-MAP Phase 136 rows</name>
  <files>~/MindrianRooms/mindrianOS/product-evolution/architectural-mandates/M5-liquid-state-fractal-sos.md, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - ~/MindrianRooms/mindrianOS/product-evolution/architectural-mandates/M5-liquid-state-fractal-sos.md (the SEED status header + the render-spine thesis; the promotion is a status flip + an implementing-phase note, not a rewrite)
    - docs/CANON-PHASE-MAP.md (the Part 7/8/9/10 tables + the v1.13.0/v1.14.0 milestone table + the Version history table - the forward-compatibility rule requires a same-commit map update)
    - docs/MINDRIAN-CANON.md Part 6 (Product-as-Venture dog-fooding mandate: the canon names the phase that implements the canon)
  </read_first>
  <action>Promote M5-liquid-state-fractal-sos.md from SEED to ADR: flip its status header (SEED -> ADR), add an "Implementing phase: Phase 136 (the render-spine layer)" line, and a short ratification note that Phase 136 shipped the one render engine + headless core + token core + opt-in mos tui + launcher per this mandate. Update docs/CANON-PHASE-MAP.md (Canon Part 6 dog-food mandate, same commit): flip the Phase 136 rows to shipped under Part 7 (7 commands -> 1 render spine + 1 token source), Part 8 (boundary clean), Part 9 (navigation.cjs chokepoint - read-only handle + SSE read-API), and Part 10 (render is the surface - inline+web seamless default + opt-in tui); add a Version history line noting Phase 136 + the M5 SEED->ADR promotion. No em-dashes (hyphens only).</action>
  <verify>
    <automated>grep -c "ADR" ~/MindrianRooms/mindrianOS/product-evolution/architectural-mandates/M5-liquid-state-fractal-sos.md && grep -c "136" docs/CANON-PHASE-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - M5-liquid-state-fractal-sos.md status is ADR (no longer SEED) and names Phase 136 as the implementing phase.
    - docs/CANON-PHASE-MAP.md has Phase 136 rows under Part 7/8/9/10 marked shipped + a Version history line.
    - No em-dashes in either file's changed content.
  </acceptance_criteria>
  <done>M5 is promoted to ADR and the canon map records Phase 136 as shipped across its four canon parts in lockstep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| all new files -> Brain | the aggregate boundary scan is the last gate that no user bytes reach the Brain |
| all new files -> room.db | the aggregate single-door scan confirms zero direct sqlite opens outside the core |
| vendored tree -> cross-platform install | the native-addon grep is the last cross-platform-safety gate before release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-08-LEAK | Information Disclosure | any new file egressing user bytes | mitigate | aggregate check-substrate boundary scan over all new files (Task 1); SSE payload enum/handle re-assertion |
| T-136-08-BYPASS | Tampering | a direct sqlite open slipping through | mitigate | aggregate grep over all new files for direct node:sqlite outside lib/core/navigation/ (Task 1) |
| T-136-08-NATIVE | Tampering (supply-chain) | a native addon in the vendored tree | mitigate | the native-addon grep is green here and wired as the release.sh Step 6.7 assertion (Plan 01) |
| T-136-08-DRIFT | Repudiation | canon drift (mandate not promoted) | mitigate | M5 SEED->ADR + CANON-PHASE-MAP flip in the same commit (Task 2, Canon Part 6 dog-food) |
</threat_model>

<verification>
- node --test tests/136-canon-boundary.test.cjs passes (Part 8 + Part 9 + cross-platform).
- bash tests/run-all-136.sh is green (all 11 suites).
- find node_modules -iregex '.*\.(node|wasm|gyp)$' empty; package.json engines.node >=22.
- M5 status is ADR; CANON-PHASE-MAP Phase 136 rows shipped.
</verification>

<success_criteria>
The phase release gate is green: every new file is Part 8 + Part 9 clean, the full suite passes with a native-addon-free vendored tree on a pinned Node 22+ floor, and M5 is promoted SEED -> ADR with the canon map updated in lockstep.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-08-SUMMARY.md` when done
</output>
