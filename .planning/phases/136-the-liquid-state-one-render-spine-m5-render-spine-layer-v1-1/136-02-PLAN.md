---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 02
type: execute
wave: 1
depends_on: ["136-01"]
files_modified:
  - lib/core/room-db.cjs
  - lib/core/navigation.cjs
  - lib/render/sse/read-api.cjs
  - lib/wiki/wiki-server.cjs
  - tests/136-thin-clients-converge.test.cjs
autonomous: true
requirements: ["Req 3", "Req 12"]
must_haves:
  truths:
    - "A read-only room.db handle exists and is reachable only through navigation.cjs"
    - "A thin SSE read-API is mounted on the EXISTING express server (no second HTTP server)"
    - "Three simulated clients reading off the SSE API see identical state (no divergence)"
    - "Zero direct node:sqlite opens exist outside navigation.cjs + the core"
    - "SSE payloads carry enum/handle only (no user bytes)"
  artifacts:
    - path: "lib/core/room-db.cjs"
      provides: "openRoomDbReadOnly + busy_timeout/synchronous pragmas on the writer"
      contains: "openRoomDbReadOnly"
    - path: "lib/core/navigation.cjs"
      provides: "read-only opener re-export + SSE-read accessors"
      contains: "openRoomDbReadOnlyForCaller"
    - path: "lib/render/sse/read-api.cjs"
      provides: "thin SSE endpoints on the existing express app, enum/handle payloads"
      contains: "mountReadApi"
  key_links:
    - from: "lib/render/sse/read-api.cjs"
      to: "lib/wiki/wiki-watcher.cjs addSSEClient/broadcast"
      via: "reuse of the existing SSE client registry"
      pattern: "addSSEClient"
    - from: "lib/core/navigation.cjs"
      to: "lib/core/room-db.cjs openRoomDbReadOnly"
      via: "additive re-export through the chokepoint"
      pattern: "openRoomDbReadOnly"
---

<objective>
Build the headless core read surface (Req 3, D-02/D-10): a read-only room.db handle exposed ONLY through navigation.cjs, and a thin SSE read-API mounted on the EXISTING express server (wiki-server.cjs) carrying enum/handle-only payloads. This is the single source every downstream surface (inline/web/tui) consumes - none opens room.db directly. Gates the engine's web/tui delivery and all thin clients.

Purpose: D-10 makes navigation.cjs the sole writer + sole door; this plan adds the read-only door and the SSE event surface without breaking the Part 9 chokepoint or the Part 8 boundary.
Output: openRoomDbReadOnly + pragmas in room-db.cjs; openRoomDbReadOnlyForCaller + SSE-read accessors re-exported through navigation.cjs; lib/render/sse/read-api.cjs mounted on the existing app; a thin-clients-converge test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-RESEARCH.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-PATTERNS.md
@docs/architecture/SUBSTRATE-CONTRACT.md
@docs/MINDRIAN-CANON.md

<interfaces>
From lib/core/room-db.cjs (lines 91-127):
  function openRoomDb(roomDir)  // sets PRAGMA journal_mode = WAL at line 100, foreign_keys = ON; runs initSchema + initMemorySchema + Phase 109 migrations
  function closeRoomDb(handle)  // tolerant of bare handle AND legacy {db} shape
  module.exports = { openRoomDb, closeRoomDb }  // line 127

From lib/core/navigation.cjs (the closed chokepoint surface, Phase 109+):
  module.exports = { ... }  // line 44; precedent pair at lines 134-135:
  openRoomDbForCaller: spineEvents.openRoomDbForCaller    // hand a caller a handle through the allow-listed door
  closeRoomDbForCaller: spineEvents.closeRoomDbForCaller
  getNeighborhood (line 50), findRecentChanges (line 61), getRoomHomeView (line 71),
  findSurfaceableTensions (line 58), findContradictions (line 54) // read fns the SSE accessors wrap

From lib/wiki/wiki-watcher.cjs (lines 13, 62-103):
  const sseClients = new Set()
  function addSSEClient(res)   // text/event-stream headers + keepalive + close handler
  function broadcast(data)     // data: ${JSON.stringify(data)}\n\n to all clients, drop on write failure
  module.exports = { startWatcher, addSSEClient, removeSSEClient }

From lib/wiki/wiki-server.cjs:
  function startWikiServer(roomDir, port)  // line 24; returns app; mounts app.get('/api/sse', ...) at line 141; startWatcher at line 608; module.exports = { startWikiServer } line 678

From scripts/check-substrate.cjs (lines 63-85, 108, 118):
  allow-list exempts ^lib/core/navigation/ and ^lib/core/navigation.cjs$ ; rules m3-direct-sqlite-require / opengraph-bypass flag direct sqlite opens elsewhere
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read-only room.db handle + writer pragmas, exposed through navigation.cjs</name>
  <files>lib/core/room-db.cjs, lib/core/navigation.cjs, tests/136-thin-clients-converge.test.cjs</files>
  <read_first>
    - lib/core/room-db.cjs lines 91-127 (openRoomDb: WAL already at line 100; closeRoomDb tolerance; module.exports at line 127 - the additive seam)
    - lib/core/navigation.cjs lines 44-176 (module.exports surface; the openRoomDbForCaller/closeRoomDbForCaller precedent at lines 134-135 - the exact additive-re-export idiom to copy with its comment block)
    - lib/core/navigation/spine-events.cjs (where openRoomDbForCaller lives - the precedent module the read-only opener should sit beside or be re-exported like)
    - tests/fixtures/136-seeded-room/build-fixture.cjs (the fixture built in Plan 01 - this test seeds from it)
    - docs/architecture/SUBSTRATE-CONTRACT.md (M11 allow-list amendment rule - any new navigation.cjs export needs an M11 line)
  </read_first>
  <action>Add to lib/core/room-db.cjs an additive openRoomDbReadOnly(roomDir): resolve the same .mindrian/room.db path, call auditBypassIfNeeded(roomDir) FIRST (same soft-defense as the writer), open new DatabaseSync(dbPath, { readOnly: true }) (RESEARCH A2: accepted by Node 22.22.2), set PRAGMA busy_timeout = 5000, and do NOT run initSchema/initMemorySchema/migrations (a read-only handle cannot write schema). Add the two D-10 writer-side pragmas to the existing openRoomDb after line 100: db.exec('PRAGMA busy_timeout = 5000') and db.exec('PRAGMA synchronous = NORMAL'). Export openRoomDbReadOnly from module.exports (line 127). Then re-export it through lib/core/navigation.cjs as openRoomDbReadOnlyForCaller, mirroring the openRoomDbForCaller comment-block idiom verbatim ("a thin re-export so [caller] can open a read-only handle through the allow-listed chokepoint; caller owns the handle and MUST close via closeRoomDbForCaller"). Add the corresponding SUBSTRATE-CONTRACT M11 allow-list line + a Phase 136-02 amendment paragraph naming the source module, the export, the consumers (Plan 04 web/tui delivery; Plan 05 tui), and the read-only invariant. Write tests/136-thin-clients-converge.test.cjs covering this task: assert openRoomDbReadOnly returns a usable handle, a write attempt against it is rejected (SQLITE_READONLY semantics, not a no-op - confirms A2), and the handle reads the seeded fixture identically to a writer handle.</action>
  <verify>
    <automated>node --test tests/136-thin-clients-converge.test.cjs && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - openRoomDbReadOnly opens with { readOnly: true } and a write (INSERT) against the handle throws/rejects (asserts SQLITE_OPEN_READONLY semantics per RESEARCH A2, not a silent no-op).
    - openRoomDb gains PRAGMA busy_timeout = 5000 and PRAGMA synchronous = NORMAL (WAL already present, unchanged).
    - navigation.cjs exports openRoomDbReadOnlyForCaller with the additive-re-export comment block; SUBSTRATE-CONTRACT.md gains the M11 line + 136-02 amendment.
    - node scripts/check-substrate.cjs --diff reports no new violation (the opener lives in room-db.cjs reached through the navigation chokepoint; the test file is not allow-listed and must not open sqlite directly - it uses the navigation re-export or openRoomDbReadOnly through the documented door).
  </acceptance_criteria>
  <done>A read-only handle exists, rejects writes, and is reachable only through the navigation chokepoint with the M11 contract amended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thin SSE read-API on the existing express server + thin-client convergence</name>
  <files>lib/render/sse/read-api.cjs, lib/wiki/wiki-server.cjs, lib/core/navigation.cjs, tests/136-thin-clients-converge.test.cjs</files>
  <read_first>
    - lib/wiki/wiki-watcher.cjs lines 13, 21-55, 62-103 (sseClients Set, the chokidar watcher config, addSSEClient + broadcast to reuse verbatim - the anti-pattern is starting a SECOND watcher)
    - lib/wiki/wiki-server.cjs lines 24-27, 140-143, 608-617, 678 (startWikiServer returns app; the existing app.get('/api/sse') mount; the watcher already started at 608-611 - reuse it; module.exports)
    - lib/core/navigation.cjs lines 50, 58, 61, 71 (getNeighborhood / findSurfaceableTensions / findRecentChanges / getRoomHomeView - the read fns the SSE accessors wrap into enum/handle payloads)
    - scripts/check-substrate.cjs lines 63-85 (allow-list: read-api.cjs is NOT allow-listed, so it must reach room.db ONLY via navigation.cjs, never a direct open)
    - RESEARCH.md Pattern 2 + Pitfall 4 (the enum/handle-only SSE payload schema {event_type, node_id, node_type, review_status})
  </read_first>
  <action>Create lib/render/sse/read-api.cjs exporting mountReadApi(app, roomDir): mount app.get('/api/room/sse', (req,res) => addSSEClient(res)) on the EXISTING express app (the one startWikiServer returns), reusing wiki-watcher's addSSEClient + the ALREADY-STARTED watcher (do NOT start a second watcher or a second server - SPEC + Part 7 forbid it). Add thin SSE-read accessors that wrap navigation.cjs read fns (getNeighborhood, findRecentChanges, findSurfaceableTensions) and shape their output to the Part 8 enum/handle-only payload schema {event_type, node_id, node_type, review_status} - NEVER artifact bodies, meeting text, or identifiers (tighter than wiki-watcher's {event,path,type} which carries a path; this carries node-id + enum only). Re-export the SSE-read accessors through navigation.cjs with the additive-re-export comment block (they are the canon-legal read door for thin clients). Wire mountReadApi into wiki-server.cjs's startWikiServer (call it after the app is built, before app.listen). Extend tests/136-thin-clients-converge.test.cjs (Req 3): start the wiki-server app against the seeded fixture, attach THREE simulated SSE clients, drive a read, and assert all three see IDENTICAL state (no divergence); run a concurrent read+write loop and assert no SQLITE_BUSY error; assert the SSE payload schema contains only enum/handle fields (no body string). All room.db access in read-api.cjs goes through navigation.cjs (no direct DatabaseSync).</action>
  <verify>
    <automated>node --test tests/136-thin-clients-converge.test.cjs && node scripts/check-substrate.cjs --diff && grep -c "new DatabaseSync\|require('node:sqlite')" lib/render/sse/read-api.cjs</automated>
  </verify>
  <acceptance_criteria>
    - mountReadApi mounts /api/room/sse on the express app returned by startWikiServer and reuses wiki-watcher.addSSEClient + the existing watcher (grep: zero second startWatcher/express() call in read-api.cjs).
    - Three simulated clients off the SSE API read identical state; the concurrent read+write loop produces no SQLITE_BUSY (Req 3 acceptance).
    - The SSE payload schema is enum/handle only: a test asserts the broadcast object keys are a subset of {event_type, node_id, node_type, review_status} and contains no free-text body field.
    - grep for new DatabaseSync / require('node:sqlite') in read-api.cjs returns 0 (it reaches room.db only via navigation.cjs); node scripts/check-substrate.cjs --diff reports no new violation.
  </acceptance_criteria>
  <done>The thin SSE read-API serves enum/handle-only events from the existing express server, three clients converge, and concurrency is clean - all without a direct sqlite open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| thin client (browser/tui) -> SSE read-API | untrusted consumers that could relay bytes onward; only enum/handle may cross |
| thin client -> room.db | clients must NOT open the DB; only navigation.cjs + core may |
| SSE payload -> any client that could forward to Brain | Part 8: user bytes must never enter the payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-02-LEAK | Information Disclosure | SSE read-api.cjs payload | mitigate | enum/handle-only schema {event_type,node_id,node_type,review_status}; check-substrate + brain-boundary scan on the new file; test asserts no body field (Task 2) |
| T-136-02-BYPASS | Tampering | a thin client opening room.db directly | mitigate | D-10 + Part 9: only navigation.cjs/core opens the DB; check-substrate --diff flags any direct open; read-api.cjs reaches room.db only via the chokepoint (Task 2) |
| T-136-02-CONCUR | Denial of Service | long-lived reader vs writer lock | mitigate | WAL + busy_timeout=5000 + synchronous=NORMAL; read-only handle; concurrency test asserts no SQLITE_BUSY (Task 1+2) |
| T-136-02-2SRV | Tampering | a second HTTP server / watcher | mitigate | reuse the existing express app + the already-started wiki watcher; grep asserts no second express()/startWatcher (Task 2) |
</threat_model>

<verification>
- node --test tests/136-thin-clients-converge.test.cjs passes (read-only handle rejects writes; three clients converge; concurrency clean; payload enum/handle-only).
- node scripts/check-substrate.cjs --diff reports no new violation on the staged changes.
- grep confirms zero direct sqlite opens in lib/render/sse/read-api.cjs.
- SUBSTRATE-CONTRACT.md carries the 136-02 M11 amendment.
</verification>

<success_criteria>
The headless core exposes a read-only handle and a thin enum/handle-only SSE read-API on the existing express server; thin clients converge with clean concurrency and zero direct sqlite opens, preserving the Part 9 chokepoint and the Part 8 boundary.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-02-SUMMARY.md` when done
</output>
