---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/run-all-136.sh
  - tests/fixtures/136-seeded-room/seed.sql
  - tests/fixtures/136-seeded-room/build-fixture.cjs
  - tests/136-seamless-no-ink.test.cjs
  - scripts/release.sh
  - .planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-BUD-TARGET.md
autonomous: true
requirements: ["Req 2", "Req 4", "Req 8", "Req 10", "Req 11"]
must_haves:
  truths:
    - "A 4+-level seeded room.db with a CONTRADICTS edge exists for downstream tests"
    - "The phase test harness runs all 136-*.test.cjs suites and exits non-zero on any fail"
    - "The seamless inline + web base loads with ink physically absent (no throw)"
    - "The exact BUD callable target is confirmed and recorded (not assumed)"
    - "release.sh asserts zero native addons in the vendored tree"
  artifacts:
    - path: "tests/run-all-136.sh"
      provides: "phase test harness mirroring run-all-130.sh"
      contains: "CJS_SUITES"
    - path: "tests/fixtures/136-seeded-room/seed.sql"
      provides: "4+-level room with a CONTRADICTS edge"
      contains: "CONTRADICTS"
    - path: "tests/136-seamless-no-ink.test.cjs"
      provides: "ink-absent seamless-base guard"
      min_lines: 20
    - path: ".planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-BUD-TARGET.md"
      provides: "confirmed BUD callable target for the fractal wave"
      contains: "autoCreatePlaceholderRoom"
  key_links:
    - from: "tests/run-all-136.sh"
      to: "tests/136-*.test.cjs"
      via: "CJS_SUITES array enumeration"
      pattern: "CJS_SUITES"
    - from: "scripts/release.sh"
      to: "node_modules native-addon scan"
      via: "find -iregex grep assertion in Step 6.7"
      pattern: "node\\|wasm\\|gyp"
---

<objective>
Build the Wave 0 substrate that gates every other 136 plan: the phase test harness, the seeded 4+-level room fixture with a CONTRADICTS edge, the load-bearing ink-absent seamless-base guard test, the release.sh native-addon vendoring assertion, and the CONFIRMED BUD callable target (RESEARCH Open Question 1 / A3 - the planner is forbidden from assuming a single SEED-001 callable exists).

Purpose: downstream waves cannot write meaningful tests without the fixture + harness, cannot vendor ink safely without the release-gate grep, and cannot build BUD (Plan 05) without knowing the exact delegation target. This plan removes those blockers.
Output: tests/run-all-136.sh, tests/fixtures/136-seeded-room/, tests/136-seamless-no-ink.test.cjs, a release.sh Step 6.7 assertion, and 136-BUD-TARGET.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-RESEARCH.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-PATTERNS.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase harness + seeded fixture</name>
  <files>tests/run-all-136.sh, tests/fixtures/136-seeded-room/seed.sql, tests/fixtures/136-seeded-room/build-fixture.cjs</files>
  <read_first>
    - tests/run-all-130.sh (the harness shape to mirror: CJS_SUITES array, per-suite PASS/FAIL line, set -uo pipefail, exit non-zero on any fail, SCRIPT_DIR resolution)
    - tests/fixtures/phase-130/sample-room/seed.sql (the trimmed-seed idiom: room + sections + artifacts + decision + FK anchor nodes so edge-write FKs do not fail under PRAGMA foreign_keys ON)
    - lib/core/room-db.cjs lines 91-114 (openRoomDb: the schema initialized via lazygraph.initSchema + memory.initMemorySchema the fixture must match)
    - lib/core/navigation/edges.cjs lines 32-128 (ALLOWED_EDGE_TYPES - confirm CONTRADICTS is a member before seeding a CONTRADICTS edge; if absent, seed the edge row directly in seed.sql as raw INSERT against the edges table since the fixture is test data, not a writeEdge call)
  </read_first>
  <action>Create tests/run-all-136.sh mirroring tests/run-all-130.sh exactly: set -uo pipefail, SCRIPT_DIR resolution, a CJS_SUITES array (initially listing the suites this phase will create: 136-seamless-no-ink, 136-engine-shapes, 136-thin-clients-converge, 136-tui-boot, 136-launcher-detect, 136-gate-write-node, 136-token-linter, 136-fractal, 136-lazygraph-overlay, 136-dual-render - tolerate missing files with a skip line so the harness is runnable from Wave 0 forward), per-suite PASS/FAIL output, START_TIME elapsed, exit non-zero on any fail. Make it executable (chmod +x). Create tests/fixtures/136-seeded-room/build-fixture.cjs: a helper that calls lib/core/room-db.cjs::openRoomDb against a temp roomDir (so the real schema is initialized via initSchema/initMemorySchema), then applies seed.sql via db.exec. Create tests/fixtures/136-seeded-room/seed.sql: a 4+-level ICM nesting (room > section > sub-section > artifact > claim depth>=4) with at least one empty section (0 entries, for the ○ glyph test), a decision node, two claim nodes, and ONE CONTRADICTS edge between two claims (with FK anchor nodes present so foreign_keys ON does not reject). Per Canon Part 8 the fixture carries only generic test strings, no real user content.</action>
  <verify>
    <automated>bash tests/run-all-136.sh; test $? -ge 0 && node tests/fixtures/136-seeded-room/build-fixture.cjs && echo FIXTURE_OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/run-all-136.sh is executable and runs without a bash syntax error (runnable even when most suites are still absent - missing suites print a skip line, present suites run).
    - node tests/fixtures/136-seeded-room/build-fixture.cjs builds a room.db with depth >= 4, an empty section, and exactly one CONTRADICTS edge (assert via a SELECT count on the edges table = 1 for edge_type='CONTRADICTS').
    - grep -c CONTRADICTS tests/fixtures/136-seeded-room/seed.sql >= 1.
  </acceptance_criteria>
  <done>The harness enumerates the planned 136 suites and the seeded fixture builds a 4+-level room with a CONTRADICTS edge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ink-absent seamless-base guard test (load-bearing)</name>
  <files>tests/136-seamless-no-ink.test.cjs</files>
  <behavior>
    - Test 1: requiring the seamless inline render entry (lib/render/render-v2.cjs) does NOT throw when node_modules/ink is absent (simulated by asserting the module graph of render-v2.cjs contains no top-level 'ink' require - a source-grep assertion plus a require() smoke that succeeds).
    - Test 2: the web-twin entry (lib/wiki/wiki-server.cjs) loads without referencing ink (source-grep: zero 'ink' references in wiki-server.cjs and render-v2.cjs).
    - Test 3 (forward guard, will pass trivially until Plan 05 lands): any file under lib/tui/ that references ink MUST do so via a dynamic await import('ink'), never a top-level require('ink') - a source-grep that, if lib/tui/ is absent, passes vacuously; once lib/tui/ exists it asserts zero top-level require('ink').
  </behavior>
  <action>Create tests/136-seamless-no-ink.test.cjs using node:test (const test = require('node:test'); const assert = require('node:assert/strict')). It is the load-bearing acceptance for Req 2 ("seamless default works with ink ABSENT") and Req 4. Assert the seamless base (render-v2.cjs + wiki-server.cjs) carries ZERO references to 'ink' (a fs.readFileSync source scan, not a runtime delete). Assert that any lib/tui/*.cjs that exists references ink only through a dynamic import('ink') pattern (regex: forbid /require\(['"]ink['"]\)/ , allow /await import\(['"]ink['"]\)/). The test must pass NOW (lib/tui/ does not yet exist - vacuous pass) and stay a guard once Plan 05 lands.</action>
  <verify>
    <automated>node --test tests/136-seamless-no-ink.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/136-seamless-no-ink.test.cjs exits 0.
    - The test asserts zero 'ink' references in lib/render/render-v2.cjs and lib/wiki/wiki-server.cjs (source-grep).
    - The test forbids top-level require('ink') in any lib/tui/*.cjs and allows await import('ink').
  </acceptance_criteria>
  <done>The seamless-base guard passes with ink absent and forbids any top-level ink require leaking into the seamless surfaces or the tui.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm BUD callable target + add release.sh native-addon assertion</name>
  <files>.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-BUD-TARGET.md, scripts/release.sh</files>
  <read_first>
    - lib/core/room-auto-create.cjs lines 135-318 (autoCreatePlaceholderRoom signature + its delegation to room-db.cjs::openRoomDb + room_auto_created memory_event via navigation chokepoint - the codebase-canonical atomic create+wire path)
    - lib/core/room-receipt-emit.cjs lines 39-63 (emitReceiptWritten(roomSlug, conversationId))
    - scripts/room-registry lines 9, 62-111 (create <name> <path> [venture_name] [venture_stage]; atomic tmp+mv writes; the shared room-bootstrap helper)
    - scripts/release.sh lines 203-217 (the Step 6.7 vendor production node_modules block: npm ci --omit=dev + integrity gate - the seam to add the native-addon grep assertion)
    - .claude/includes/release-process.md (the Vendored node_modules Rule: pure-JS only, no .node addon / binding.gyp / prebuilt binary / install lifecycle script may enter the single cross-platform tree)
  </read_first>
  <action>Resolve RESEARCH Open Question 1 / A3 (D-03). Grep lib/ + scripts/ for a single shipped "SEED-001 atomic sub-room" callable (createSubRoom / seedSubRoom / sub_room). Write 136-BUD-TARGET.md recording the verified conclusion: there is NO single SEED-001 callable; BUD (Plan 05) delegates to the composed room-creation orchestrator that DOES exist - lib/core/room-auto-create.cjs::autoCreatePlaceholderRoom (the atomic create that delegates to openRoomDb + emits room_auto_created via the navigation chokepoint), plus scripts/room-registry create for registry wiring, plus lib/core/room-receipt-emit.cjs::emitReceiptWritten. Record the EXACT signatures and the rule (Canon Part 7 / D-03): the navigator SURFACES "bud / open as room" and CALLS this orchestrator; it never reimplements promotion. Then add to scripts/release.sh Step 6.7 (after the npm ci --omit=dev vendor build, before git add) an assertion: run find node_modules -iregex '.*\.\(node\|wasm\|gyp\)$' and a no-install-script scan; HARD ABORT the release if either finds a hit (the vendored tree must stay pure-JS per the release-process rule + RESEARCH Pitfall 3). Use hyphens, not em-dashes, in all added text.</action>
  <verify>
    <automated>grep -q autoCreatePlaceholderRoom .planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-BUD-TARGET.md && grep -E "iregex|\.node|binding.gyp" scripts/release.sh | grep -v '^#' | grep -cE 'node|wasm|gyp'</automated>
  </verify>
  <acceptance_criteria>
    - 136-BUD-TARGET.md states there is no single SEED-001 callable and names autoCreatePlaceholderRoom + room-registry create + emitReceiptWritten as the composed BUD delegation target with exact signatures.
    - scripts/release.sh Step 6.7 contains a find -iregex native-addon scan that HARD ABORTs on a .node/.wasm/.gyp hit (grep over non-comment lines finds the assertion).
    - No em-dashes in either file's added content (grep for the em-dash character returns 0 on the added lines).
  </acceptance_criteria>
  <done>The BUD delegation target is confirmed and documented, and the release pipeline blocks any native addon entering the vendored tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> vendored node_modules tree | the ink/react/yoga install (Plan 05) crosses here; a native addon or install script would break the cross-platform vendoring contract |
| test fixture -> room.db schema | seed.sql writes raw rows; a malformed FK would mask real edge-write bugs downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-01-SC | Tampering | release.sh node_modules vendoring | mitigate | Step 6.7 native-addon grep (find -iregex .node/.wasm/.gyp) + no-install-script scan HARD ABORTs the release (this plan's Task 3) |
| T-136-01-FK | Tampering | seed.sql edges FK | mitigate | seed FK anchor nodes before the CONTRADICTS edge so foreign_keys ON does not reject (Task 1), mirroring the Phase 129/130 fixture idiom |
| T-136-01-ASSUME | Repudiation | BUD callable assumption | mitigate | Task 3 confirms the real callable in writing (136-BUD-TARGET.md); the planner is forbidden from assuming a single SEED-001 function exists |
</threat_model>

<verification>
- bash tests/run-all-136.sh runs (skips absent suites, passes present ones).
- node tests/fixtures/136-seeded-room/build-fixture.cjs builds a 4+-level room.db with a CONTRADICTS edge.
- node --test tests/136-seamless-no-ink.test.cjs exits 0.
- scripts/release.sh Step 6.7 native-addon assertion present.
- 136-BUD-TARGET.md names the confirmed delegation target.
</verification>

<success_criteria>
The four Wave-0 blockers (harness, seeded fixture, ink-absent guard, release vendoring assertion) plus the BUD-target confirmation exist, so every later wave can write fixture-backed tests, vendor ink safely, and build BUD against a known target.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-01-SUMMARY.md` when done
</output>
