---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 07
type: execute
wave: 4
depends_on: ["136-05"]
files_modified:
  - lib/launcher/detect.cjs
  - lib/launcher/compose.cjs
  - lib/launcher/tmux-hooks.cjs
  - bin/mindrian-tools.cjs
  - tests/136-launcher-detect.test.cjs
autonomous: true
requirements: ["Req 5", "Req 11"]
must_haves:
  truths:
    - "The launcher probes hosts in the locked precedence order and routes to the web twin when no multiplexer is present"
    - "The workspace is generated from the room graph (session=room, window=section, pane=surface)"
    - "When already inside tmux, the launcher adds windows to the current session, never spawns a rival session"
    - "A tmux window/pane focus emits a focus_changed memory_event through navigation.cjs"
    - "The launcher honors Windows path discipline and routes native Windows to WezTerm/mprocs"
  artifacts:
    - path: "lib/launcher/detect.cjs"
      provides: "host probe in the locked precedence order"
      contains: "detectHost"
    - path: "lib/launcher/compose.cjs"
      provides: "graph -> workspace layout generator"
      contains: "launch"
    - path: "lib/launcher/tmux-hooks.cjs"
      provides: "set-hook pane-focus-in -> focus_changed via navigation.cjs"
      contains: "setFocus"
  key_links:
    - from: "lib/launcher/compose.cjs"
      to: "lib/core/navigation.cjs (room graph read)"
      via: "session=room/window=section/pane=surface generated from the graph"
      pattern: "navigation"
    - from: "lib/launcher/tmux-hooks.cjs"
      to: "lib/core/navigation/focus.cjs setFocus"
      via: "pane-focus-in hook writes focus_changed (never a direct sqlite write)"
      pattern: "setFocus"
---

<objective>
Build the detect-and-adapt launcher (Req 5/11, D-11/D-15): a host probe in the locked precedence order (WezTerm running -> zellij installed -> tmux scripted; native Windows -> WezTerm/mprocs; else web twin), a graph -> workspace generator (session=room/window=section/pane=surface), and tmux hooks that emit focus_changed memory_events. Depends on Plan 05 (the tui pane content the launcher hosts). Adds the `launch` subcommand to bin/mindrian-tools.cjs (later wave than Plan 05, so no file conflict).

Purpose: D-11 makes tmux a second projection of the room graph and the opt-in host; the seamless default already works without it (web-twin fallback).
Output: lib/launcher/{detect,compose,tmux-hooks}.cjs; the launch subcommand; a launcher-detect matrix test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-RESEARCH.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-PATTERNS.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-VALIDATION.md

<interfaces>
RESEARCH Pattern 4 (the LOCKED probe order, D-11) - use verbatim:
  function which(bin)  // execSync(process.platform==='win32' ? `where ${bin}` : `command -v ${bin}`, {stdio:'ignore'}) -> true/false
  function detectHost():
    win32: which('wezterm') -> 'wezterm' ; which('mprocs') -> 'mprocs' ; else 'web'
    process.env.WEZTERM_PANE -> 'wezterm' ; which('zellij') -> 'zellij' ; which('tmux') -> 'tmux' ; else 'web'
  $TMUX-aware: if process.env.TMUX set, tmux new-window/split-window against the CURRENT session, never tmux new-session (RESEARCH Pitfall 5)

Workspace generation (D-11): navigation.cjs reads the room graph; emit session=room / window=section / pane=surface (Claude Code + mos tui + web). The tui pane runs `mindrian-tools tui` (Plan 05).

tmux hooks emit focus_changed: set-hook pane-focus-in runs a shell command that writes a focus_changed memory_event - route through navigation.setFocus(db, sessionId, nodeId, 'user') (focus.cjs), NEVER a direct sqlite write. (RESEARCH A4: tmux hook firing is MEDIUM-confidence, not live-tested - the live tmux assertion is a manual-justified verify per 136-VALIDATION; the env-var probe-order matrix is the automated gate.)

Cross-platform (D-15, RESEARCH Pitfall 6): path.join / normwin; `where` on win32 not `command -v`; tmux is Unix-only.
Anti-pattern: following the mockup's zellij-default. D-11 LOCKS tmux-scripted as default with zellij only "if installed" (RESEARCH Anti-Pattern; UI-SPEC line 51).

From bin/mindrian-tools.cjs: the switch-case (line 89) + the lazy in-case require pattern (Plan 05 added `case 'tui'`); add `case 'launch'` the same lazy way.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Host detect (locked probe order) + graph -> workspace compose + launch subcommand</name>
  <files>lib/launcher/detect.cjs, lib/launcher/compose.cjs, bin/mindrian-tools.cjs, tests/136-launcher-detect.test.cjs</files>
  <behavior>
    - Test 1: with WEZTERM_PANE set, detectHost() returns 'wezterm'.
    - Test 2: on a Unix host with no WezTerm, zellij present -> 'zellij'; only tmux present -> 'tmux'; nothing present -> 'web'.
    - Test 3: with process.platform forced to 'win32', WezTerm present -> 'wezterm', mprocs present -> 'mprocs', neither -> 'web' (tmux is never chosen on win32).
    - Test 4: compose() reads the room graph via navigation.cjs and emits a workspace plan with session=room / window=section / pane=surface (assert the layout shape, not a live multiplexer).
  </behavior>
  <read_first>
    - 136-RESEARCH.md Pattern 4 (the verbatim detectHost probe order) + Pitfall 5 ($TMUX-aware) + Pitfall 6 (Windows path discipline)
    - 136-PATTERNS.md "lib/launcher/{detect,compose,tmux-hooks}.cjs" (no in-repo analog; use RESEARCH Pattern 4 + D-11 probe order; the CONTEXT-over-mockup note)
    - bin/mindrian-tools.cjs lines 26-71, 89 (USAGE heredoc + the switch; the lazy in-case require Plan 05 used for `case 'tui'`)
    - lib/core/navigation.cjs read fns (getRoomHomeView / getNeighborhood - the room-graph source compose reads to emit window=section/pane=surface)
  </read_first>
  <action>Create lib/launcher/detect.cjs with which(bin) (using `where` on win32, `command -v` on Unix - D-15) and detectHost() in the LOCKED precedence order (RESEARCH Pattern 4 verbatim): win32 -> wezterm/mprocs/web; Unix -> WEZTERM_PANE then zellij then tmux then web. Make process.platform + env injectable for the test matrix (accept an opts override so the test can force win32/env without mutating the global). Create lib/launcher/compose.cjs exporting launch(roomDir): read the room graph through navigation.cjs and generate the workspace plan (session=room, window=section, pane=surface: Claude Code + a `mindrian-tools tui` pane + the web twin); when detectHost() returns a multiplexer, emit the scripted layout ($TMUX-aware: if process.env.TMUX is set, new-window/split-window against the current session, never new-session - Pitfall 5); when it returns 'web', route to the web twin (the no-multiplexer fallback). Do NOT follow the mockup's zellij-default (CONTEXT D-11 locks tmux-scripted default, zellij only if installed). Add `case 'launch': return require('../lib/launcher/compose.cjs').launch(roomDir);` to bin/mindrian-tools.cjs (lazy in-case require) + a USAGE line. Use path.join everywhere (D-15). Write tests/136-launcher-detect.test.cjs (Req 5/11) covering the env-var probe matrix + the compose layout shape.</action>
  <verify>
    <automated>node --test tests/136-launcher-detect.test.cjs && node scripts/check-substrate.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - The probe-order matrix passes: WezTerm-running -> wezterm; zellij-installed -> zellij; tmux-only -> tmux; nothing -> web; win32 routes to WezTerm/mprocs/web and never tmux.
    - compose() emits a session=room/window=section/pane=surface plan generated from the room graph (read via navigation.cjs, not a direct sqlite open).
    - The launcher uses path.join + `where` on win32 (no hardcoded forward-slash path or `command -v` on Windows).
    - `case 'launch'` is a lazy in-case require; node scripts/check-substrate.cjs --diff finds no new violation.
  </acceptance_criteria>
  <done>The launcher detects the host in the locked order, generates the workspace from the graph, falls back to the web twin, and honors Windows discipline.</done>
</task>

<task type="auto">
  <name>Task 2: tmux focus hooks -> focus_changed memory_event</name>
  <files>lib/launcher/tmux-hooks.cjs, tests/136-launcher-detect.test.cjs</files>
  <read_first>
    - 136-RESEARCH.md Pattern 4 ($TMUX-aware) + Assumption A4 (tmux set-hook pane-focus-in is MEDIUM-confidence, not live-tested - the live assertion is manual-justified)
    - lib/core/navigation/focus.cjs setFocus (the ONLY write path for focus_changed; route the hook through it, never a direct sqlite write)
    - 136-VALIDATION.md "Manual-Only Verifications" (the tmux-present live focus_changed assertion)
    - 136-PATTERNS.md "tmux hooks emit focus_changed" (set-hook pane-focus-in runs a shell command routed through navigation.setFocus)
  </read_first>
  <action>Create lib/launcher/tmux-hooks.cjs that registers tmux set-hook entries (pane-focus-in / window focus) at launch time: each hook runs a shell command that invokes `mindrian-tools` to write a focus_changed memory_event via navigation.setFocus(db, sessionId, nodeId, 'user') - NEVER a direct sqlite write (the hook shells out to the chokepoint). Require `focus-events on` to be set so the host terminal reports focus. The hooks are ephemeral per-session (registered each launch, not persisted to git - they live in the running tmux server). Because tmux hook firing is MEDIUM-confidence (RESEARCH A4) and CI lacks tmux, the automated test asserts the hook-COMMAND construction (the set-hook string routes through the navigation setFocus path and carries no direct sqlite write); the live "switch a tmux window -> focus_changed row appears" assertion is the manual-justified verify recorded in 136-VALIDATION. Extend tests/136-launcher-detect.test.cjs with the hook-command-construction assertion.</action>
  <verify>
    <automated>node --test tests/136-launcher-detect.test.cjs && grep -c "setFocus" lib/launcher/tmux-hooks.cjs</automated>
  </verify>
  <acceptance_criteria>
    - tmux-hooks.cjs constructs set-hook entries (pane-focus-in) whose command routes through navigation.setFocus, with `focus-events on` set.
    - The hook command carries NO direct sqlite write (grep: zero new DatabaseSync / require('node:sqlite') in tmux-hooks.cjs); the test asserts the hook-command construction.
    - The live tmux focus_changed assertion is recorded as the manual-justified verify in 136-VALIDATION (not an automated gate here, per RESEARCH A4).
  </acceptance_criteria>
  <done>tmux focus hooks emit focus_changed through the navigation chokepoint, with the live-firing assertion deferred to the manual verify (RESEARCH A4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-installed multiplexer -> launcher | the launcher probes but registers nothing at the OS level beyond ephemeral in-session tmux hooks |
| tmux hook -> room.db | the hook must write focus_changed only through navigation.cjs, never a direct sqlite write |
| env vars (TMUX/WEZTERM_PANE/platform) -> host routing | read-only; a missing multiplexer must degrade to the web twin, never crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-07-RIVAL | Tampering | tmux launcher spawning a rival session | mitigate | $TMUX-aware: new-window/split-window against the current session, never new-session (Task 1, Pitfall 5) |
| T-136-07-BYPASS | Tampering | tmux hook writing room.db directly | mitigate | the hook shells out to navigation.setFocus; grep asserts zero direct sqlite in tmux-hooks.cjs (Task 2) |
| T-136-07-WINPATH | Tampering (cross-platform) | Windows path/probe breakage | mitigate | path.join + `where` on win32; win32 routes to WezTerm/mprocs/web, never tmux (Task 1, Pitfall 6) |
| T-136-07-NOHOST | Denial of Service | no multiplexer present | mitigate | detectHost falls through to 'web' (the no-multiplexer fallback); the seamless default is unaffected |
</threat_model>

<verification>
- node --test tests/136-launcher-detect.test.cjs passes (probe matrix + compose layout shape + hook-command construction).
- node scripts/check-substrate.cjs --diff reports no new violation.
- grep confirms tmux-hooks.cjs routes through setFocus with zero direct sqlite.
- The live tmux focus_changed firing is the manual-justified verify in 136-VALIDATION.
</verification>

<success_criteria>
The launcher detects the host in the locked precedence order, generates the workspace from the room graph, falls back to the web twin with zero config, honors Windows discipline, and emits focus_changed through the chokepoint on a tmux focus.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-07-SUMMARY.md` when done
</output>
