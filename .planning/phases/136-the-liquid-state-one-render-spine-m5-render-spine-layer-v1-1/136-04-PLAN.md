---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 04
type: execute
wave: 2
depends_on: ["136-02", "136-03"]
files_modified:
  - lib/render/render-v2.cjs
  - commands/dashboard.md
  - commands/wiki.md
  - commands/present.md
  - commands/publish.md
  - commands/export.md
  - commands/visualize.md
  - commands/snapshot.md
  - tests/136-engine-shapes.test.cjs
  - tests/136-lazygraph-overlay.test.cjs
  - tests/136-dual-render.test.cjs
autonomous: true
requirements: ["Req 1", "Req 2", "Req 9", "Req 10"]
must_haves:
  truths:
    - "One engine renders any (shape x delivery); the 7 HTML commands are thin flag-aliases"
    - "No legacy command file retains its own render/HTML body"
    - "Inline delivery spawns zero processes; web delivery reuses the existing express server"
    - "The LazyGraph slot overlays the tree without reordering it, and is silent when nothing relevant changed"
    - "A graph-native relation renders as BOTH a graph view and a natural-language sentence"
  artifacts:
    - path: "lib/render/render-v2.cjs"
      provides: "shape x delivery dispatch engine (extended in place)"
      contains: "delivery"
    - path: "commands/dashboard.md"
      provides: "thin flag-alias (render --shape dashboard --deliver web)"
      contains: "render --shape"
  key_links:
    - from: "lib/render/render-v2.cjs"
      to: "lib/render/sse/read-api.cjs + navigation.cjs read fns"
      via: "web/tui delivery consumes the SSE read-API; inline reads via navigation.cjs"
      pattern: "delivery"
    - from: "lib/render/render-v2.cjs"
      to: "references/visual/palette.json"
      via: "token core consumption (check-tokens passes)"
      pattern: "palette"
---

<objective>
Build the ONE render engine + the seamless default (Req 1/2/9/10, D-09): extend render-v2.cjs into a shape x delivery dispatch engine, collapse the 7 HTML commands into thin flag-aliases, wire inline (zero-process) + web-twin (existing express) delivery off the Wave 1 core + Wave 2 token core, add the LazyGraph overlay slot (intent-filters/temporal-ranks, never reorders), and the dual render (graph + sentence). Depends on Plan 02 (SSE read-API) + Plan 03 (token core).

Purpose: D-09 makes inline + web the universal seamless default; Req 1 collapses the 7 divergent renderers into one engine (Canon Part 7).
Output: extended render-v2.cjs; 7 command flag-aliases; engine-shapes, lazygraph-overlay, and dual-render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-RESEARCH.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-PATTERNS.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-UI-SPEC.md
@lib/render/render-v2.cjs

<interfaces>
From lib/render/render-v2.cjs (the engine seed - EXTEND in place, do NOT fork a parallel formatter per Part 7):
  function render(args)  // lines 112-122; args = {zones, mode, operator, tier, jtbd, tokenBudget, roomDir, ...} with defensive defaults (line 30 design rule)
    // ADD: args.shape (deck|mermaid|wiki|grid|dashboard|snapshot), args.delivery (inline|web|tui|file)
  Numbered-hook discipline (lines 124-127): keep existing markers 1-8; add shape-dispatch as a NEW numbered step, do not renumber.
  composeZones (lines 325-361): Zone1 header / Zone2 body / Zone3 signals ('▷ ' prefix lines 346-348) / Zone4 footer
  JTBD_CLI_COLOR (lines 64-78) + ANSI (87-94): 5-color contract the token core extends, not replaces
  TTY-gating (lines 200-201): process.stdout.isTTY === true keeps non-TTY captures byte-clean
  Canon Part 8 by construction (lines 207-230): LOCAL-only _provenance, scalar passthroughs; never calls Brain

From navigation.cjs (Plan 02 read door + existing read fns): getNeighborhood (cross-wall edges + tree), findRecentChanges (temporal stream for D-07 intent filter), findSurfaceableTensions / findContradictions (slot whisper + the CONTRADICTS dual render), plus the Plan 02 SSE-read accessors.

From lib/render/sse/read-api.cjs (Plan 02): mountReadApi(app, roomDir) - the web/tui delivery transport.

The 7 commands to collapse (commands/*.md): dashboard, wiki, present, publish, export, visualize (already aliased to dashboard --mermaid), snapshot. Each becomes `render --shape X --deliver Y` + a one-line "powered by the render spine" note; no inline render/HTML body remains.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend render-v2 into the shape x delivery engine + collapse 7 commands to flag-aliases</name>
  <files>lib/render/render-v2.cjs, commands/dashboard.md, commands/wiki.md, commands/present.md, commands/publish.md, commands/export.md, commands/visualize.md, commands/snapshot.md, tests/136-engine-shapes.test.cjs</files>
  <behavior>
    - Test 1: render({shape:'dashboard', delivery:'inline'}) returns the 4-zone inline text and spawns zero child processes.
    - Test 2: render({shape:'wiki', delivery:'web'}) routes through the existing express app (binds the wiki-server app, does NOT instantiate a new express()).
    - Test 3: every (shape x delivery) pair the 7 commands needed resolves without throwing (deck/mermaid/wiki/grid/dashboard/snapshot x inline/web/file).
    - Test 4: a grep over the 7 command files finds the `render --shape` alias and NO inline HTML/render body.
  </behavior>
  <read_first>
    - lib/render/render-v2.cjs lines 30, 64-94, 112-127, 200-230, 325-361 (defensive-default design rule, JTBD color map, the render(args) entry seam, numbered-hook discipline, TTY gate, Part 8 provenance, composeZones)
    - lib/render/sse/read-api.cjs (Plan 02 - the web/tui delivery transport mountReadApi)
    - commands/visualize.md (the ALREADY-aliased command - the alias shape to copy: visualize == dashboard --mermaid)
    - commands/dashboard.md + commands/wiki.md (the two heaviest legacy render bodies to retire into aliases)
    - references/visual/palette.json (Plan 03 - the token core render-v2 must consume; check-tokens.cjs must pass on render-v2)
  </read_first>
  <action>Extend lib/render/render-v2.cjs render(args) by destructuring two new defensive-defaulted fields: args.shape (default 'dashboard') and args.delivery (default 'inline'). Add a NEW numbered shape-dispatch step (do not renumber the existing 1-8 hooks): inline delivery composes the 4-zone text via the existing composeZones (zero process); web delivery routes through the existing express app via lib/render/sse/read-api.cjs mountReadApi (no new express()); file delivery writes the rendered output to disk; tui delivery returns the enum/handle payload the Plan 05 ink app consumes. Preserve the Part 8 LOCAL-only _provenance construction and the TTY gate. Consume the Plan 03 token core (no hardcoded hex/glyph - check-tokens.cjs must pass). Collapse the 7 command .md files (dashboard/wiki/present/publish/export/visualize/snapshot) into thin flag-aliases: each body becomes `render --shape <X> --deliver <Y>` plus a one-line "powered by the render spine" note; remove every inline HTML/render body (visualize is already the alias model). Write tests/136-engine-shapes.test.cjs covering the four behaviors.</action>
  <verify>
    <automated>node --test tests/136-engine-shapes.test.cjs && grep -L 'render --shape' commands/dashboard.md commands/wiki.md commands/present.md commands/publish.md commands/export.md commands/visualize.md commands/snapshot.md; test $? -eq 1 && node scripts/check-tokens.cjs --diff</automated>
  </verify>
  <acceptance_criteria>
    - render({shape, delivery}) renders all 6 shapes x 4 deliveries without throwing; inline spawns zero child processes (asserted via a child_process spy count == 0).
    - web delivery binds the existing wiki-server express app (no new express() instance - grep assertion).
    - All 7 command files contain `render --shape` and NO inline render/HTML body (grep -L finds none missing the alias).
    - node scripts/check-tokens.cjs --diff passes on render-v2.cjs (zero hardcoded values).
  </acceptance_criteria>
  <done>One engine renders all (shape x delivery), the 7 commands are thin flag-aliases with no legacy render bodies, and inline is zero-process / web reuses the existing server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LazyGraph overlay slot (intent-filter, never reorder) + dual render (graph + sentence)</name>
  <files>lib/render/render-v2.cjs, tests/136-lazygraph-overlay.test.cjs, tests/136-dual-render.test.cjs</files>
  <behavior>
    - Test 1 (Req 9): injecting a LazyGraph suggestion leaves the tree node order BYTE-IDENTICAL (snapshot before == after); the suggestion renders into Zone 3 only (highlight-in-place + whisper).
    - Test 2 (Req 9): when nothing relevant to the active JTBD/operator changed, Zone 3 is OMITTED entirely (no empty box, silent slot).
    - Test 3 (D-07): intent FILTERS which recent events are eligible (relevant to the active JTBD/operator); temporal ranks within that filtered set.
    - Test 4 (Req 10): a seeded CONTRADICTS edge produces BOTH a visible graph edge AND a natural-language sentence naming it in the same render frame.
  </behavior>
  <read_first>
    - lib/render/render-v2.cjs lines 325-361 (composeZones - Zone 3 signals at 346-348 is where the LazyGraph whisper lands; Zone 1/2/4 unchanged)
    - navigation.cjs findRecentChanges + findSurfaceableTensions + findContradictions + getNeighborhood (the intent-filter source + the dual-render graph source)
    - tests/fixtures/136-seeded-room/seed.sql (Plan 01 - the CONTRADICTS edge this test asserts on)
    - 136-UI-SPEC.md "Tree node states" (LazyGraph-highlighted: in-place emphasis + Zone 3 whisper, tree order byte-identical, M5 hard rule 1) + "Loading/Empty/Silent States" (Zone 3 omitted when silent)
  </read_first>
  <action>Add the LazyGraph overlay slot to render-v2.cjs Zone 3: read findRecentChanges (the temporal stream) and FILTER it by the active JTBD/operator (D-07 intent gates) before temporal ranking within the filtered set; render the top signal as a highlight-in-place marker on the relevant tree node PLUS a whisper line in Zone 3 - NEVER reorder the tree (M5 hard rule 1). When the filtered set is empty (nothing relevant changed), OMIT Zone 3 entirely (no empty box). Add the dual render: when getNeighborhood/findContradictions surfaces a graph-native relation (e.g. a CONTRADICTS edge), emit BOTH the graph representation (the edge in the graph view) AND a natural-language sentence naming it, in the SAME render frame. Preserve Part 8 (no user bytes into provenance). Write tests/136-lazygraph-overlay.test.cjs (byte-identical tree snapshot + silent-on-no-change) and tests/136-dual-render.test.cjs (the seeded CONTRADICTS edge yields a visible edge + a sentence).</action>
  <verify>
    <automated>node --test tests/136-lazygraph-overlay.test.cjs && node --test tests/136-dual-render.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Injecting a suggestion leaves the tree node order byte-identical (snapshot equality before/after); the suggestion appears only in Zone 3.
    - Zone 3 is omitted entirely when no JTBD/operator-relevant event changed (no empty box rendered).
    - Intent filters the temporal stream (a test asserts an off-JTBD recent event is excluded; an on-JTBD one is included and temporally ranked).
    - The seeded CONTRADICTS edge renders as a visible graph edge AND a sentence in one render frame.
  </acceptance_criteria>
  <done>The LazyGraph slot overlays without reordering and stays silent when irrelevant; graph-native truths render as both a graph view and a sentence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine -> Brain | the renderer must never call Brain or put user bytes in provenance (Part 8) |
| web delivery -> browser | user-derived render output crossing to the web twin must be HTML-escaped |
| LazyGraph suggestion -> tree | agent-proposed intent must never reorder the human's tree (M5 hard rule 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-04-BRAIN | Information Disclosure | render-v2 provenance | mitigate | preserve the existing LOCAL-only _provenance scalar-passthrough construction (render-v2 lines 207-230); no Brain call; check-substrate on staged |
| T-136-04-XSS | Tampering/XSS | web-twin user-derived output | mitigate | reuse wiki-server escHtml for any user-derived bytes reaching the browser (ASVS V5); no new HTML-render body in the engine path |
| T-136-04-REORDER | Tampering | LazyGraph slot reordering the tree | mitigate | Zone 3 overlay only; byte-identical tree snapshot test asserts no reorder (Task 2, M5 hard rule 1) |
| T-136-04-2SRV | Tampering | a second express instance for web delivery | mitigate | web delivery routes through the existing wiki-server app via mountReadApi; grep asserts no new express() (Task 1) |
</threat_model>

<verification>
- node --test tests/136-engine-shapes.test.cjs + tests/136-lazygraph-overlay.test.cjs + tests/136-dual-render.test.cjs all pass.
- grep -L 'render --shape' over the 7 command files finds none missing the alias (no legacy render body).
- node scripts/check-tokens.cjs --diff passes on render-v2.cjs.
- node scripts/check-substrate.cjs --diff reports no new violation.
</verification>

<success_criteria>
One engine renders every (shape x delivery), the 7 commands are flag-aliases, inline is zero-process and web reuses the existing server, the LazyGraph slot overlays without reordering and stays silent when irrelevant, and graph-native truths render as both a graph and a sentence.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-04-SUMMARY.md` when done
</output>
