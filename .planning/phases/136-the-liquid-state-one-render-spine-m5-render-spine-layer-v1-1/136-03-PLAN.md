---
phase: 136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1
plan: 03
type: execute
wave: 1
depends_on: ["136-01"]
files_modified:
  - references/visual/palette.json
  - skills/ui-system/SKILL.md
  - docs/CANON-PHASE-MAP.md
  - scripts/check-tokens.cjs
  - tests/136-token-linter.test.cjs
autonomous: true
requirements: ["Req 7"]
must_haves:
  truths:
    - "palette.json carries surface-agnostic semantic color PAIRS (base hue + terminal variant)"
    - "Each semantic pair clears WCAG contrast (3:1 UI / 4.5:1 text) against its allowed background"
    - "The 4 new TUI glyphs are in the vocabulary, one meaning each"
    - "The CI linter fails on a hardcoded hex/glyph and passes on the engine"
    - "SKILL.md ruling is extended to the TUI surface and CANON-PHASE-MAP updated in the same commit"
  artifacts:
    - path: "references/visual/palette.json"
      provides: "semantic color PAIRS + 4 TUI glyphs"
      contains: "t-green"
    - path: "scripts/check-tokens.cjs"
      provides: "grep-gate CI linter mirroring check-substrate.cjs"
      contains: "scanStagedDiff"
    - path: "skills/ui-system/SKILL.md"
      provides: "TUI-surface ruling extension (glyphs + color routing row)"
      contains: "TUI"
  key_links:
    - from: "scripts/check-tokens.cjs"
      to: "references/visual/palette.json"
      via: "allow-list of legitimate token sources + hardcoded-value scan"
      pattern: "palette\\.json"
    - from: "skills/ui-system/SKILL.md"
      to: "docs/CANON-PHASE-MAP.md"
      via: "same-commit dog-food update (Canon Part 6)"
      pattern: "136"
---

<objective>
Build the single De Stijl token core (Req 7, D-14): extend palette.json into a surface-agnostic token graph of contrast-checked semantic color PAIRS (base hue + terminal-legible variant), add the 4 new TUI glyphs, extend the ui-system ruling to the TUI surface, and ship scripts/check-tokens.cjs - a CI linter that rejects any renderer hardcoding a value outside the token core. Parallel with Plan 02 (no file overlap). Gates every renderer's token compliance (Plans 04, 05, 06).

Purpose: D-14 makes one token source authoritative and CI-enforced so no surface invents its own values.
Output: extended palette.json; SKILL.md TUI ruling extension; CANON-PHASE-MAP row; scripts/check-tokens.cjs; a linter test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-UI-SPEC.md
@.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-PATTERNS.md
@references/visual/palette.json
@skills/ui-system/SKILL.md
@scripts/check-substrate.cjs

<interfaces>
The 8 semantic color PAIRS are FULLY SPECIFIED in 136-UI-SPEC.md "Semantic color PAIRS" table. Bind exactly these (base hue from palette.json -> terminal variant, contrast vs #0D0D0D):
  body/foreground: #F5F0E8 cream -> #E8E2D6 t-fg (15.07, text PASS) - default text, no glyph
  core/focus/affirmative: #2D6B4A success_green -> #5BBF8A t-green (8.57) - glyph checkmark / focus-triangle - confirmed nodes, focus row, ballot-check
  thin-client/command/link: #1E3A6E mondrian_blue -> #6E8AC6 t-blue (5.66) - glyph arrow / detail-hash - commands, paths, links
  warning/tension: #C8A43C mondrian_yellow -> #D8B24A t-yellow (9.61) - glyph warning - CONTRADICTS, tension rows
  error: #A63D2F mondrian_red -> #C9604F t-red (4.88) - glyph x - errors ONLY
  Larry/decision: #6B4E8B amethyst -> #9A7BBE t-amethyst (5.51) - glyph filled-square - gate title, free-text accent
  host CLI/floating: #B5602A sienna -> #CE7A45 t-sienna (6.01) - glyph diamond - host pane, floating gate border
  meta/hint (UI 3:1): #A09A90 gray_meta -> #7A756C t-dim (4.25 vs bg; RESTRICTED to #0D0D0D bg only - 3.98 vs #161514 fails) - counts, hints, timestamps

The 4 new glyphs (136-UI-SPEC Glyph Vocabulary): white-diamond U+25C7 cross-room/cross-wall ; white-circle U+25CB empty section ; ballot-box-with-check U+2611 multi-select ON ; ballot-box U+2610 multi-select OFF.

From references/visual/palette.json (lines 5-53): base (9 hues), palette_a_discovery, palette_b_build, extended (sienna, surface), ansi_5_color, derived_files, deferred_consumers (render-v2.cjs at line 52 - promote to wired).

From skills/ui-system/SKILL.md: Section 3 Symbol Vocabulary (lines 247-264, 12-glyph table, "one meaning each", NO EMOJI rule line 266); Section 4 Color Contract (lines 291-321, surface-routing table at line 319 with a CLI/TUI row).

From scripts/check-substrate.cjs (the grep-gate idiom to mirror): scanLine (147-179), scanFiles/scanRepo (188-204, 336-348), scanStagedDiff (276-311, flags ONLY added lines), CLI modes --baseline (exit 0) / --diff (exit 1) (376-411), allow-list regex array (63-85).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend palette.json into semantic pairs + 4 glyphs; extend SKILL.md ruling + CANON-PHASE-MAP</name>
  <files>references/visual/palette.json, skills/ui-system/SKILL.md, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - references/visual/palette.json lines 5-53 (base hues + extended + derived_files + deferred_consumers - render-v2.cjs at line 52 is a deferred consumer this phase promotes to wired)
    - skills/ui-system/SKILL.md lines 247-268 (the 12-glyph table + the NO EMOJI rule + statusline carve-out template) and lines 291-321 (Color Contract + the surface-routing table at line 319)
    - docs/CANON-PHASE-MAP.md (the forward-compatibility rule: any SKILL.md change updates this map in the same commit; the Part 7 table + the v1.13.0 milestone table)
    - 136-UI-SPEC.md "Semantic color PAIRS" + "Glyph Vocabulary" + "Ruling Extension" sections (the binding spec: exact hexes, contrasts, glyph codepoints, the EXTEND-not-except decision)
  </read_first>
  <action>Extend references/visual/palette.json with a new semantic_pairs token block binding each of the 8 meanings to {base, terminal, contrast, backing_glyph, reserved_for} per the 136-UI-SPEC table (verbatim hexes + contrast numbers from the interfaces block). Add a tui_glyphs block with the 4 new glyphs (white-diamond U+25C7, white-circle U+25CB, ballot-box-with-check U+2611, ballot-box U+2610), each {codepoint, meaning, surface}. Promote render-v2.cjs from deferred_consumers to a wired consumer per the file's own deferred-consumer discipline (line 51-52). Extend skills/ui-system/SKILL.md Section 3 with exactly 4 new glyph rows (one meaning each; the NO EMOJI rule still applies - the statusline carve-out does NOT extend to mos tui) and Section 4 with a third surface-routing row "CLI / TUI dark surface | semantic pairs (terminal variant)". Add a Ruling Extension paragraph: the TUI surface obeys the 4-zone anatomy (Breadcrumb/Tree+Detail/Slot/Footer) and the token core. Update docs/CANON-PHASE-MAP.md in the SAME commit (Canon Part 6 dog-food mandate): add a Part 7 row for Phase 136 token-core consolidation noting the SKILL.md ruling extension. No em-dashes anywhere (hyphens only).</action>
  <verify>
    <automated>node -e "const p=require('./references/visual/palette.json'); if(!p.semantic_pairs||!p.tui_glyphs) process.exit(1); console.log('OK')" && grep -c "TUI" skills/ui-system/SKILL.md && grep -c "136" docs/CANON-PHASE-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - palette.json parses as valid JSON and contains semantic_pairs (8 entries) + tui_glyphs (4 entries); render-v2.cjs is no longer in deferred_consumers.
    - SKILL.md Section 3 gains exactly 4 glyph rows and Section 4 gains the TUI dark-surface routing row; the NO EMOJI rule is preserved.
    - docs/CANON-PHASE-MAP.md gains a Phase 136 Part 7 row in the same change.
    - grep for the em-dash character across all three files returns 0.
  </acceptance_criteria>
  <done>palette.json is the surface-agnostic token graph with semantic pairs + 4 glyphs, the ruling extends to the TUI surface, and the canon map is updated in lockstep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: check-tokens.cjs CI linter + contrast assertion test</name>
  <files>scripts/check-tokens.cjs, tests/136-token-linter.test.cjs</files>
  <behavior>
    - Test 1: a planted fixture file containing a hardcoded #RRGGBB hex (outside the allow-listed token sources) FAILS the linter.
    - Test 2: a planted fixture containing a raw new glyph (e.g. the ballot-box) outside the token sources FAILS the linter.
    - Test 3: the real engine surface (render-v2.cjs) and palette.json PASS (palette.json is allow-listed; it contains the hexes by definition).
    - Test 4: each of the 8 semantic pairs clears its WCAG bar (7 text pairs >= 4.5:1, the meta/hint UI pair >= 3:1) computed against #0D0D0D; the meta/hint pair measured against #161514 is asserted to FAIL (proving the restriction-to-#0D0D0D rule).
  </behavior>
  <action>Create scripts/check-tokens.cjs mirroring scripts/check-substrate.cjs's grep-gate idiom: a pure scanLine returning {rule, match} hits for a hardcoded hex (#RRGGBB) or a raw glyph outside the allow-listed token sources; pure scanFiles(files, readContent) + scanRepo() (no process.exit in the API); a scanStagedDiff() that flags ONLY added lines (so pre-existing baselined values are not blocked); CLI modes --baseline (informational, exit 0) / --diff (blocking, exit 1) + programmatic exports. Allow-list references/visual/palette.json + lib/core/visual-ops.cjs + references/JTBD-PALETTES.md (the legitimate token sources that contain the hexes by definition - copy the allow-list-regex-array shape from check-substrate.cjs lines 63-85). Add a contrast helper (relative-luminance WCAG formula) so the linter can assert each semantic pair clears 4.5:1 (text) / 3:1 (UI) and enforce the meta/hint restriction-to-#0D0D0D rule (a meta/hint token on #161514 is a linter failure). Write tests/136-token-linter.test.cjs (Req 7) covering the four behaviors above.</action>
  <verify>
    <automated>node --test tests/136-token-linter.test.cjs && node scripts/check-tokens.cjs --baseline</automated>
  </verify>
  <acceptance_criteria>
    - The linter FAILS on a planted hardcoded hex and on a planted raw glyph outside the token sources, and PASSES on palette.json + render-v2.cjs.
    - The contrast assertion confirms 7 text pairs >= 4.5:1 and the meta/hint UI pair >= 3:1 against #0D0D0D, and that meta/hint FAILS against #161514 (restriction enforced).
    - --baseline exits 0 (informational) and --diff exits 1 only on a NEW added violation (mirrors check-substrate.cjs scanStagedDiff).
    - node scripts/check-tokens.cjs --baseline exits 0 on the current repo.
  </acceptance_criteria>
  <done>The CI linter blocks new hardcoded values, the semantic pairs are contrast-proven, and the meta/hint background restriction is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer source -> token core | a renderer hardcoding a value bypasses the single token source; the linter is the gate |
| glyph rendering -> terminal | a double-width or tofu glyph (Windows) degrades the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-03-DRIFT | Tampering | renderer hardcoding a hex/glyph | mitigate | check-tokens.cjs --diff blocks NEW added hardcoded values; allow-list scopes legitimate token sources (Task 2) |
| T-136-03-CONTRAST | Information Disclosure (legibility) | low-contrast semantic pair | mitigate | WCAG contrast assertion per pair (4.5:1 text / 3:1 UI); meta/hint restricted to #0D0D0D bg (Task 2) |
| T-136-03-GLYPH | Tampering (cross-platform) | new glyph double-width/tofu on Windows | accept | UI-SPEC A5 fallback to [x]/[ ] ASCII pair recorded; confirmed on Windows tester per the manual-verify in 136-VALIDATION (not this plan's gate) |
</threat_model>

<verification>
- node --test tests/136-token-linter.test.cjs passes (planted violations fail, engine passes, contrast proven).
- node scripts/check-tokens.cjs --baseline exits 0.
- palette.json parses with semantic_pairs + tui_glyphs; SKILL.md + CANON-PHASE-MAP updated in lockstep.
- grep for em-dash returns 0 across the touched files.
</verification>

<success_criteria>
One CI-enforced token core exists: palette.json carries contrast-checked semantic pairs + 4 glyphs, the ruling extends to the TUI surface in lockstep with the canon map, and check-tokens.cjs blocks any new hardcoded value.
</success_criteria>

<output>
Create `.planning/phases/136-the-liquid-state-one-render-spine-m5-render-spine-layer-v1-1/136-03-SUMMARY.md` when done
</output>
