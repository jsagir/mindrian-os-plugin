---
phase: 178-universal-gate-chokepoint
plan: 02
type: execute
wave: 2
depends_on: ["178-01"]
files_modified:
  - scripts/check-render-coverage.cjs
  - tests/test-render-coverage-gate-hardfail.cjs
  - tests/run-all-178.sh
autonomous: true
requirements: [RND-02, RND-PRED, RND-FLOOR]
canon_parts: [3, 6, 7, 11]
cirs_relationship:
  surfaces_added:
    - check-render-coverage
  surfaces_modified:
    - selector-dispatcher
  surfaces_removed: []
  spine_consumed:
    - lib/hmi/selector-dispatcher.cjs
  gate_impact: "ADDS scripts/check-render-coverage.cjs as the DETERMINISTIC, code-evaluated binary predicate (C-2): a pure code predicate over the render_coverage column on the SHARED CIRS ledger (data/connector-coverage-ledger.json, extended in 178-01) + the dispatcher's appendAskUserQuestionTrailer wiring that answers 'does this reachable surface route through the card-emission door?' with NO LLM-judge. The --check mode exits NONZERO on any card-emission surface that lacks card-emission routing (mirrors build-connector-registry.cjs:789-890). A FLOOR/hard-fail adversarial test (mirrors tests/test-coverage-gate-hardfail.cjs) proves the gate FAILS CLOSED on a synthesized undeclared render surface. The dispatcher's SEED-020 card-emission door (selector-dispatcher.cjs:537) is the wiring the predicate evaluates. No reach/posture/edge/node minted; no Brain wire (Part 8 LOCAL); no LLM-judge in the hard gate."
must_haves:
  truths:
    - "scripts/check-render-coverage.cjs is a PURE code predicate over the registry + the dispatcher card-emission wiring -- NO LLM-judge in the hard gate (C-2)"
    - "the predicate proves 'a card-emission surface routes through appendAskUserQuestionTrailer / emits an askuserquestion_marker' deterministically over the dispatcher wiring"
    - "--check exits NONZERO when any card-emission surface lacks card-emission routing (fail closed)"
    - "the FLOOR/hard-fail test proves the gate fails closed on a synthesized undeclared render surface, while the live repo stays GREEN"
    - "the gate is CI-stable and reproducible: same registry + same wiring -> same verdict, no network"
  artifacts:
    - path: "scripts/check-render-coverage.cjs"
      provides: "the deterministic card-emission-routing predicate + the --check hard-fail entry"
      contains: "renderCoverageReport"
    - path: "tests/test-render-coverage-gate-hardfail.cjs"
      provides: "the adversarial FLOOR test: a synthesized undeclared render surface trips the gate RED; the live repo is GREEN; an excluded-with-reason surface is conformant"
      contains: "hardfail"
  key_links:
    - from: "scripts/check-render-coverage.cjs"
      to: "data/connector-coverage-ledger.json (the render_coverage column) + scripts/build-connector-registry.cjs"
      via: "regenerate-in-memory + classify each card-emission surface against the dispatcher card-emission wiring"
      pattern: "render-coverage"
    - from: "the predicate"
      to: "lib/hmi/selector-dispatcher.cjs:537 appendAskUserQuestionTrailer"
      via: "evaluate whether a reachable surface routes through the askuserquestion_marker door"
      pattern: "askuserquestion_marker"
---

<objective>
Build scripts/check-render-coverage.cjs -- the deterministic, code-evaluated card-emission
predicate (C-2) + a FLOOR/hard-fail test that proves the gate fails closed on an undeclared
surface.

C-2 is the second of the three non-negotiables (D-178-03): "did a reached-gate render route through the single
card-emission door" must be a PURE CODE PREDICATE over the render_coverage column on the SHARED CIRS
ledger (data/connector-coverage-ledger.json, extended in 178-01) and the dispatcher wiring, with NO LLM-judge in the hard gate (LLM-judge is for soft quality only). This keeps
the gate CI-stable and reproducible. The predicate evaluates the SEED-020 single construction
door: selector-dispatcher.cjs:528-548 appendAskUserQuestionTrailer sets
rendered.askuserquestion_marker (:537). A card-emission surface routes through that door; a
render-only-excluded surface (declared in 178-01's two-state ledger) does not need to.

The deliverable is the GATE, not a contract test (D-178-01). The one-time tests (test-150-5,
ACPT-06) stay as unit smoke checks but are explicitly NOT the gate (RESEARCH sec 6). The
FLOOR/hard-fail test here is the adversarial proof the GATE fails closed -- it mirrors
tests/test-coverage-gate-hardfail.cjs (the CIRS hard-fail test for invocation), copying a
synthesized dark render surface into a walked scope, spawning --check, asserting nonzero exit,
and removing it in a finally.

Per D-178-04 additive only: no reach/posture/edge/node minted, Part 3 frozen contracts
untouched, no Brain wire (Part 8). The hard-FAIL WIRING into pre-commit/install-pre-commit/
release.sh/doctor --acceptance is 178-03; this plan delivers the predicate + the --check exit
contract + the FLOOR proof.

Purpose: make "is every reachable surface wired to emit a card" a deterministic CI binary
(C-2), fail-closed (C-3 begins here at the exit contract), with no agent in the loop.
Output: scripts/check-render-coverage.cjs, a green tests/test-render-coverage-gate-hardfail.cjs,
and the run-all-178.sh aggregator updated to carry both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md
@.planning/phases/178-universal-gate-chokepoint/178-RESEARCH.md
@.planning/phases/178-universal-gate-chokepoint/178-01-PLAN.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scripts/check-render-coverage.cjs -- the deterministic card-emission predicate + --check fail-closed exit (C-2, RND-PRED)</name>
  <read_first>
    - scripts/build-connector-registry.cjs + data/connector-coverage-ledger.json (178-01 added the render_coverage column to THIS shared ledger -- ONE ledger, two columns, no parallel registry; this predicate READS the render_coverage column rather than walking a second registry, dissolving R-3). The deliverable is the distinct scripts/check-render-coverage.cjs gate (178-RESEARCH sec 9) that consumes the shared ledger's render column
    - scripts/build-connector-registry.cjs:789-890 (the main() --check flip block: STALE byte-compare + the R9 gap HARD-FAIL loop that pushes a self-naming error + recovery line per gap surface and process.exit(1); the exact exit-code contract + recovery-line idiom to mirror)
    - lib/hmi/selector-dispatcher.cjs:528-548 (appendAskUserQuestionTrailer -- the SEED-020 single construction door; :537 sets rendered.askuserquestion_marker; pickShape :736-772 attaches it on the F.* return) -- this is the card-emission WIRING the predicate evaluates
    - scripts/intent-classifier.cjs:930-938 (the seam: the marker is read and concatenated as TEXT -- the engine arm that DOES route through the door; the predicate's positive case)
    - lib/hmi/dial-selector.cjs:216-229 (renderDialShape -- the F.7-dial surface that does NOT call appendAskUserQuestionTrailer today; the predicate must classify it as a card-emission surface LACKING routing unless 178-03 promotes the door to cover it, OR the registry declares its routing path)
    - .planning/phases/178-universal-gate-chokepoint/178-RESEARCH.md sec 6 C-2 (pure code predicate over the registry + the dispatcher wiring; NO LLM-judge in the hard gate)
  </read_first>
  <action>
    Create scripts/check-render-coverage.cjs as the distinct render-plane gate that READS the
    render_coverage column on the SHARED CIRS ledger (data/connector-coverage-ledger.json,
    extended in 178-01; regenerate in memory via build-connector-registry's exported
    coverageReport for the STALE byte-compare). Implement renderCoverageReport(): for EACH
    surface carrying render_coverage 'card-emission', evaluate the DETERMINISTIC
    card-emission predicate -- a pure code check (string/AST scan, NO LLM, NO network) that
    answers "does this card-emission surface route through the SEED-020 card-emission door?".
    The door is appendAskUserQuestionTrailer / the askuserquestion_marker emission
    (selector-dispatcher.cjs:528-548/537). Concretely: a surface is COVERED if it routes
    through the dispatcher's pickShape (which attaches the marker) OR directly invokes
    appendAskUserQuestionTrailer / threads an askuserquestion_marker (the intent-classifier.cjs
    engine-arm pattern at :930-938); a render-only-excluded surface (from 178-01's two-state
    ledger, with its required reason) is conformant WITHOUT routing; any OTHER card-emission
    surface is a render GAP (it can reach a gate but is not wired to emit a card -- exactly the
    F.7-dial slip). Return { surfaces:[{surface, disposition, routed}], counts:{covered,
    excluded, gap}, errors:[] } mirroring the connector coverageReport shape. Then add the
    --check main() branch mirroring build-connector-registry.cjs:789-890: a STALE byte-compare
    of data/connector-coverage-ledger.json (regenerate in memory, fail if drifted) AND the render
    GAP HARD-FAIL loop -- each gap surface pushes a self-naming error ('RENDER GAP: surface X
    can reach a Decision Gate but does not route through the card-emission door; wire it through
    pickShape/appendAskUserQuestionTrailer or declare it render-only-excluded with a reason')
    plus a recovery line, then process.exit(1). On clean: print 'render-coverage: OK' exit 0.
    NO LLM-judge anywhere in --check (C-2). NO Brain call, NO network (Part 8). Make the
    --check tolerant-by-construction so a clean repo stays exit 0 once 178-03 has wired the
    F.7-dial routing (until then F.7-dial may legitimately report as a known gap; the FLOOR
    test in Task 2 exercises the gap behavior on a SYNTHESIZED surface, not on F.7-dial, so the
    test does not depend on 178-03 having landed).
  </action>
  <behavior>
    - renderCoverageReport() returns counts {covered, excluded, gap} that sum to surfaces.length (the XOR invariant: every surface lands in exactly one bucket).
    - a render-only-excluded surface (with a reason) classifies excluded, never gap.
    - a card-emission surface that routes through pickShape / appendAskUserQuestionTrailer / an askuserquestion_marker classifies covered.
    - --check exits NONZERO when any surface classifies gap, naming the surface + a recovery line.
    - the predicate references no LLM/model call, no fetch/http, no brain-client (pure code, Part 8).
  </behavior>
  <verify>
    <automated>node -e "const c=require('./scripts/check-render-coverage.cjs'); const r=c.renderCoverageReport(); const t=r.counts.covered+r.counts.excluded+r.counts.gap; if(t!==r.surfaces.length) throw new Error('XOR invariant broken: '+t+' != '+r.surfaces.length); console.log('OK report covered='+r.counts.covered+' excluded='+r.counts.excluded+' gap='+r.counts.gap);"</automated>
    <automated>grep -nE "AskUserQuestion|model|anthropic|fetch\(|http\b|brain-client|llm|judge" scripts/check-render-coverage.cjs | grep -viE "askuserquestion_marker|appendAskUserQuestionTrailer|// |card-emission door" && { echo "C-2/Part8 BREACH: LLM-judge or network symbol in hard gate" >&2; exit 1; } || echo "OK C-2 pure-code + Part 8 clean"</automated>
  </verify>
  <done>scripts/check-render-coverage.cjs is a pure deterministic predicate over the render_coverage column on the shared CIRS ledger + the dispatcher card-emission wiring (no LLM-judge, no network); renderCoverageReport classifies every surface covered/excluded/gap (XOR); --check exits nonzero on any render gap with a self-naming error + recovery line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The FLOOR/hard-fail adversarial test -- the gate fails closed on an undeclared render surface (C-3 floor, RND-FLOOR)</name>
  <read_first>
    - scripts/check-render-coverage.cjs (Task 1: renderCoverageReport + the --check exit contract)
    - tests/test-coverage-gate-hardfail.cjs (the WHOLE file -- the CIRS hard-fail adversarial test to MIRROR for render: Test 1 a synthesized dark fixture classifies gap + --check exits nonzero; Test 2 an excluded-with-reason fixture is conformant; Test 3 the clean live repo passes --check exit 0; Test 4 the FAIL message names the offending surface + recovery line; the COPY-into-a-walked-dir-then-remove-in-a-finally idiom that never mutates a tracked file)
    - lib/hmi/dial-selector.cjs:216-229 (the F.7-dial surface; the test must NOT depend on F.7-dial's routing state -- exercise a SYNTHESIZED surface so the FLOOR test is independent of 178-03)
    - .planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md (D-178-01 the deliverable is the GATE; R-5 it MUST land hard-FAIL or it rots)
  </read_first>
  <action>
    Create tests/test-render-coverage-gate-hardfail.cjs mirroring
    tests/test-coverage-gate-hardfail.cjs verbatim in structure, for render: (Test 1) write a
    SYNTHESIZED dark render surface (a temp module containing a pickShape( call site, with no
    card-emission routing and not declared render-only-excluded) into a scope the 178-01 walk
    covers, regenerate, assert renderCoverageReport classifies it gap, AND spawn
    check-render-coverage.cjs --check and assert NONZERO exit; remove the fixture in a finally.
    (Test 2) a synthesized surface declared render-only-excluded WITH a reason classifies
    excluded and does NOT trip the gate. (Test 3) the clean live repo: assert --check behaves
    as the deterministic gate -- if 178-03 has not yet wired F.7-dial, assert the ONLY gap is
    F.7-dial and document it as the known pre-178-03 gap (so the test is honest and not flaky);
    once 178-03 lands, this asserts exit 0. (Test 4) the FAIL message names the offending
    surface + the recovery line. Use the copy-into-walked-scope-then-remove-in-a-finally idiom
    so NO tracked file is mutated; fixtures live under tests/fixtures/ and are never walked by
    the live generator. Register tests/test-render-coverage-gate-hardfail.cjs in
    tests/run-all-178.sh (alongside the 178-01 exhaustiveness suite). Exit 0 on pass. bash/CJS,
    no emoji, no em-dashes.
  </action>
  <behavior>
    - Test 1: a synthesized dark render surface classifies gap AND --check exits nonzero; the fixture is removed in a finally; no tracked file mutated.
    - Test 2: a synthesized render-only-excluded-with-reason surface classifies excluded and does NOT trip the gate.
    - Test 3: the clean live repo's --check is deterministic (exit 0 once 178-03 wires F.7-dial; before that the sole gap is F.7-dial, asserted explicitly so the test never flakes).
    - Test 4: the gate FAIL message names the offending surface + the recovery line.
  </behavior>
  <verify>
    <automated>node tests/test-render-coverage-gate-hardfail.cjs</automated>
    <automated>bash tests/run-all-178.sh; echo "(run-all-178 carries 178-01 exhaustive + 178-02 predicate + FLOOR; 178-03 wiring assertions land next wave)"</automated>
  </verify>
  <done>tests/test-render-coverage-gate-hardfail.cjs mirrors the CIRS hard-fail test for render: a synthesized undeclared render surface trips the gate RED, an excluded-with-reason surface is conformant, the live repo is deterministic, and the FAIL message names the surface + recovery line; registered in run-all-178.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger -> predicate | The predicate reads the render_coverage column on the shared CIRS ledger + the dispatcher card-emission wiring; the verdict is pure code, reproducible, no agent in the loop (C-2). |
| predicate -> CI exit | --check returns a deterministic nonzero exit on any render gap; the fail-closed contract that 178-03 wires into the four enforcement surfaces (C-3). |
| gate -> LLM | The HARD gate contains NO LLM-judge; LLM-judge is reserved for soft quality only (C-2), so the gate is CI-stable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-02-01 | Spoofing | the card-emission verdict | mitigate | The predicate is pure code over the registry + the appendAskUserQuestionTrailer/askuserquestion_marker wiring; the grep gate asserts no LLM/model/judge symbol in --check, so the verdict cannot be spoofed by a soft-quality model call (C-2). |
| T-178-02-02 | Tampering | the fail-closed exit | mitigate | The FLOOR/hard-fail test proves --check exits nonzero on a synthesized undeclared surface (mirrors test-coverage-gate-hardfail); a render gap can never silently pass (R-5). |
| T-178-02-03 | Repudiation | the gate verdict provenance | accept | The gate proves "every reachable surface is WIRED to emit a card", not "the model called the tool this turn" (R-1, the irreducible agent-honored terminal tool-call). Named, accepted; the PostToolUse-interceptor spike for R-1 is 178-04. |
| T-178-02-04 | Spoofing | the unreached-gate render | accept | Prose-mimicry (the model hand-painting interactive-looking glyphs on a turn the code never reached a gate) is OUT of scope (R-2, 150.5 hole #4): there is no marker to verify on an unreached turn. The SKILL doctrine line stays the named, accepted residual for the unreached case; the gate governs only REACHED-gate surfaces. |
| T-178-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO package installs; pure CJS predicate + adversarial test over existing files. No Package Legitimacy Gate triggered. |
</threat_model>

<verification>
- node scripts/check-render-coverage.cjs (or its --check) is a pure deterministic predicate; the XOR invariant holds (covered+excluded+gap === surfaces.length).
- The C-2/Part-8 grep gate confirms no LLM-judge / model / network symbol in the hard --check path.
- node tests/test-render-coverage-gate-hardfail.cjs exits 0: a synthesized undeclared render surface trips the gate RED; an excluded-with-reason surface is conformant; the live repo is deterministic; the FAIL message names the surface.
- bash tests/run-all-178.sh carries the 178-01 exhaustiveness suite + the 178-02 predicate + FLOOR test + the carried frozen-set drift fences (6 reaches, 3 postures still GREEN).
</verification>

<success_criteria>
- scripts/check-render-coverage.cjs is the deterministic, code-evaluated card-emission predicate (C-2): a pure code check over the render_coverage column on the shared CIRS ledger + the SEED-020 card-emission door, with NO LLM-judge in the hard gate.
- --check exits NONZERO on any card-emission surface that lacks card-emission routing (fail closed begins; C-3 wiring is 178-03).
- The FLOOR/hard-fail test (mirroring tests/test-coverage-gate-hardfail.cjs) proves the gate fails closed on a synthesized undeclared render surface while the live repo stays deterministic.
- Additive only: no reach/posture/edge/node minted; Part 3 frozen contracts untouched; no Brain wire; R-1 named as accepted residual.
</success_criteria>

<output>
Create `.planning/phases/178-universal-gate-chokepoint/178-02-SUMMARY.md` when done.
</output>
