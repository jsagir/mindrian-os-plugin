---
phase: 178-universal-gate-chokepoint
plan: 02
type: execute
wave: 2
depends_on: ["178-01"]
files_modified:
  - scripts/check-render-coverage.cjs
  - tests/test-render-coverage-gate-hardfail.cjs
  - tests/run-all-178.sh
autonomous: true
requirements: [C-2, C-3, D-178-01, D-178-04, D-178-07]
canon_parts: [3, 6, 7, 11]
cirs_relationship:
  surfaces_added:
    - check-render-coverage
  surfaces_modified:
    - selector-dispatcher
  surfaces_removed: []
  spine_consumed:
    - lib/hmi/selector-dispatcher.cjs
  gate_impact: "ADDS scripts/check-render-coverage.cjs as the DETERMINISTIC, code-evaluated binary predicate (C-2): a pure code predicate over the SEPARATE render-coverage registry (data/render-coverage-registry.json, minted in 178-01) + the dispatcher's pickShape host-append wiring, answering 'does this card-emission entry point route through the card-emission door?' with NO LLM-judge. The --check mode exits NONZERO on any card-emission entry that lacks card-emission routing (mirrors build-connector-registry.cjs:789-890). A FLOOR/hard-fail adversarial test (mirrors tests/test-coverage-gate-hardfail.cjs) proves the gate FAILS CLOSED on a synthesized undeclared render entry point. The SEED-020 card-emission door (selector-dispatcher.cjs:537) + the isFShape host-append (:931-933) is the wiring the predicate evaluates. No reach/posture/edge/node minted; no Brain wire (Part 8 LOCAL); no LLM-judge in the hard gate."
must_haves:
  truths:
    - "scripts/check-render-coverage.cjs is a PURE code predicate over the SEPARATE render registry + the dispatcher card-emission wiring -- NO LLM-judge in the hard gate (C-2)"
    - "the predicate credits a HOST-APPENDED marker as COVERED: an entry point is covered if its render entry point is reached by pickShape (whose isFShape branch appends the marker) OR if its file contains a direct appendAskUserQuestionTrailer call -- it does NOT demand an in-function marker assignment inside renderDialShape (MEDIUM-1)"
    - "--check exits NONZERO when any card-emission entry lacks card-emission routing (fail closed)"
    - "the FLOOR/hard-fail test proves the gate fails closed on a synthesized undeclared render entry point, while the live repo stays GREEN"
    - "the gate is CI-stable and reproducible: same registry + same wiring -> same verdict, no network"
  artifacts:
    - path: "scripts/check-render-coverage.cjs"
      provides: "the deterministic card-emission-routing predicate over the separate render registry + the --check hard-fail entry"
      contains: "renderCoverageReport"
    - path: "tests/test-render-coverage-gate-hardfail.cjs"
      provides: "the adversarial FLOOR test: a synthesized undeclared render entry point trips the gate RED; the live repo is GREEN; an excluded-with-reason entry is conformant"
      contains: "hardfail"
  key_links:
    - from: "scripts/check-render-coverage.cjs"
      to: "data/render-coverage-registry.json (178-01) + scripts/build-render-coverage.cjs"
      via: "regenerate-in-memory + classify each card-emission entry against the dispatcher card-emission wiring"
      pattern: "render-coverage-registry"
    - from: "the predicate"
      to: "lib/hmi/selector-dispatcher.cjs:537 appendAskUserQuestionTrailer + :931-933 isFShape host-append"
      via: "evaluate whether an entry point routes through the askuserquestion_marker door (host-appended counts)"
      pattern: "askuserquestion_marker|appendAskUserQuestionTrailer"
---

<objective>
Build scripts/check-render-coverage.cjs -- the deterministic, code-evaluated card-emission
predicate (C-2) over the SEPARATE render registry + a FLOOR/hard-fail test that proves the gate
fails closed on an undeclared entry point.

C-2 is the second of the three non-negotiables (D-178-03): "did a reached-gate render route
through the single card-emission door" must be a PURE CODE PREDICATE over the SEPARATE
render-coverage registry (data/render-coverage-registry.json, minted in 178-01) and the dispatcher
wiring, with NO LLM-judge in the hard gate (LLM-judge is for soft quality only). This keeps the
gate CI-stable and reproducible. The predicate evaluates the SEED-020 single construction door:
selector-dispatcher.cjs:528-548 appendAskUserQuestionTrailer sets rendered.askuserquestion_marker
(:537), and pickShape host-appends that marker to EVERY F.* shape via the isFShape branch
(:931-933). A card-emission entry routes through that door; a render-only-excluded entry (declared
in 178-01's registry, with its required reason) does not need to.

The deliverable is the GATE, not a contract test (D-178-01). The one-time tests (test-150-5,
ACPT-06) stay as unit smoke checks but are explicitly NOT the gate (RESEARCH sec 6). The
FLOOR/hard-fail test here is the adversarial proof the GATE fails closed -- it mirrors
tests/test-coverage-gate-hardfail.cjs (the CIRS hard-fail test for invocation), synthesizing a
dark render entry point, spawning --check, asserting nonzero exit, and removing it in a finally.

Per D-178-04 additive only: no reach/posture/edge/node minted, Part 3 frozen contracts untouched,
no Brain wire (Part 8). The hard-FAIL WIRING into pre-commit/install-pre-commit/release.sh/doctor
--acceptance is 178-03; this plan delivers the predicate + the --check exit contract + the FLOOR
proof.

Purpose: make "is every reachable render entry point wired to emit a card" a deterministic CI
binary (C-2), fail-closed (C-3 begins here at the exit contract), with no agent in the loop.
Output: scripts/check-render-coverage.cjs, a green tests/test-render-coverage-gate-hardfail.cjs,
and the run-all-178.sh aggregator updated to carry both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md
@.planning/phases/178-universal-gate-chokepoint/178-RESEARCH.md
@.planning/phases/178-universal-gate-chokepoint/178-01-PLAN.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scripts/check-render-coverage.cjs -- the deterministic card-emission predicate + --check fail-closed exit (C-2)</name>
  <read_first>
    - scripts/build-render-coverage.cjs + data/render-coverage-registry.json (178-01 minted THIS SEPARATE registry keyed on the 16 .cjs render entry points; this predicate READS the render registry, NOT the connector ledger). The deliverable is the distinct scripts/check-render-coverage.cjs gate (178-RESEARCH sec 9) that consumes the render registry
    - scripts/build-connector-registry.cjs:789-890 (the main() --check flip block: STALE byte-compare + the R9 gap HARD-FAIL loop that pushes a self-naming error + recovery line per gap surface and process.exit(1); the exact exit-code contract + recovery-line idiom to mirror)
    - lib/hmi/selector-dispatcher.cjs:528-548 (appendAskUserQuestionTrailer -- the SEED-020 single construction door; :537 sets rendered.askuserquestion_marker), :911-944 (pickShape's isFShape host-append: :931-933 -- ANY result whose shape starts with 'F' gets appendAskUserQuestionTrailer applied; this is the host-append the predicate credits as COVERED) -- this is the card-emission WIRING the predicate evaluates
    - scripts/intent-classifier.cjs:919-937 (the engine arm that DIRECTLY calls dispatcher.appendAskUserQuestionTrailer(rendered,'F.1') at :933 then reads rendered.askuserquestion_marker at :934 -- the direct-call pattern the predicate also credits as COVERED)
    - lib/hmi/dial-selector.cjs:216-225 (renderDialShape returns shape 'F.7-dial' -- it does NOT assign a marker itself; it is COVERED because pickShape's isFShape host-append appends the marker for any 'F.*' shape; the predicate must NOT demand an in-renderDialShape marker assignment -- MEDIUM-1)
    - .planning/phases/178-universal-gate-chokepoint/178-RESEARCH.md sec 6 C-2 (pure code predicate over the registry + the dispatcher wiring; NO LLM-judge in the hard gate)
  </read_first>
  <action>
    Create scripts/check-render-coverage.cjs as the distinct render-plane gate that READS the
    SEPARATE render registry (data/render-coverage-registry.json, minted in 178-01; regenerate in
    memory via build-render-coverage's exported walk for the STALE byte-compare). Implement
    renderCoverageReport(): for EACH entry carrying render_coverage 'card-emission', evaluate the
    DETERMINISTIC card-emission predicate -- a pure code check (string/AST scan, NO LLM, NO network)
    that answers "does this card-emission entry point route through the SEED-020 card-emission
    door?". PIN THE PREDICATE BODY EXACTLY (MEDIUM-1): an entry point classifies COVERED if EITHER
    (a) its render entry point is REACHED BY pickShape -- i.e. the entry calls pickShape( (the
    dispatcher's isFShape host-append at selector-dispatcher.cjs:931-933 unconditionally appends
    the askuserquestion_marker to every result whose shape starts with 'F', so any path that
    returns through pickShape for an F.* shape is host-appended and COVERED), OR (b) its file
    contains a DIRECT appendAskUserQuestionTrailer( call (the intent-classifier.cjs:933 engine-arm
    pattern). The predicate scans for exactly these tokens, file-scoped to the entry point's file:
    the literal call token `pickShape(` and the literal call token `appendAskUserQuestionTrailer(`
    (and, as the marker-presence corroborant, the literal `askuserquestion_marker`). The predicate
    does NOT demand an in-function marker assignment inside renderDialShape -- a host-appended
    marker is credited as COVERED (so the F.7-dial entry, which returns shape 'F.7-dial' and is
    host-appended by pickShape, classifies covered WITHOUT renderDialShape assigning a marker). The
    scan is string/AST over the entry point's own file ONLY (no cross-file inference, no LLM, no
    network). A render-only-excluded entry (from 178-01's registry, with its required reason) is
    conformant WITHOUT routing; any card-emission entry that matches NEITHER (a) nor (b) is a render
    GAP. Return { entries:[{entry, render_coverage, routed}], counts:{covered, excluded, gap},
    errors:[] } mirroring the connector coverageReport shape. Then add the --check main() branch
    mirroring build-connector-registry.cjs:789-890: a STALE byte-compare of
    data/render-coverage-registry.json (regenerate in memory, fail if drifted) AND the render GAP
    HARD-FAIL loop -- each gap entry pushes a self-naming error ('RENDER GAP: entry X is declared
    card-emission but does not route through the card-emission door; route it through
    pickShape/appendAskUserQuestionTrailer or declare it render-only-excluded with a reason') plus a
    recovery line, then process.exit(1). On clean: print 'render-coverage: OK' exit 0. NO LLM-judge
    anywhere in --check (C-2). NO Brain call, NO network (Part 8). The live registry baseline
    (178-01) is already gap=0 because the F.7-dial entry is host-appended-covered and every other
    card-emission entry calls pickShape, so a clean repo exits 0 immediately (the F.7-dial slip
    premise is FALSE on the live tree -- see 178-03 Task 1; the FLOOR test in Task 2 exercises the
    gap behavior on a SYNTHESIZED entry, never on a live entry).
  </action>
  <behavior>
    - renderCoverageReport() returns counts {covered, excluded, gap} that sum to entries.length (the XOR invariant: every entry lands in exactly one bucket).
    - a render-only-excluded entry (with a reason) classifies excluded, never gap.
    - an entry whose file calls pickShape( classifies covered (host-appended), even when no in-function marker assignment exists (the F.7-dial case -- MEDIUM-1).
    - an entry whose file directly calls appendAskUserQuestionTrailer( classifies covered (the engine-arm case).
    - --check exits NONZERO when any entry classifies gap, naming the entry + a recovery line.
    - the live repo is gap=0 (the F.7-dial entry is host-appended-covered); --check exits 0 on a clean repo.
    - the predicate references no LLM/model call, no fetch/http, no brain-client (pure code, Part 8).
  </behavior>
  <verify>
    <automated>node -e "const c=require('./scripts/check-render-coverage.cjs'); const r=c.renderCoverageReport(); const arr=r.entries||r.surfaces; const t=r.counts.covered+r.counts.excluded+r.counts.gap; if(t!==arr.length) throw new Error('XOR invariant broken: '+t+' != '+arr.length); if(r.counts.gap!==0) throw new Error('live repo should be gap=0 (F.7-dial host-appended), got gap='+r.counts.gap); console.log('OK report covered='+r.counts.covered+' excluded='+r.counts.excluded+' gap='+r.counts.gap);"</automated>
    <automated>node scripts/check-render-coverage.cjs --check; echo "render gate --check exit=$? (must be 0 on the gap=0 live baseline)"</automated>
    <automated>grep -nE "AskUserQuestion|model|anthropic|fetch\(|http\b|brain-client|llm|judge" scripts/check-render-coverage.cjs | grep -viE "askuserquestion_marker|appendAskUserQuestionTrailer|// |card-emission door" && { echo "C-2/Part8 BREACH: LLM-judge or network symbol in hard gate" >&2; exit 1; } || echo "OK C-2 pure-code + Part 8 clean"</automated>
  </verify>
  <done>scripts/check-render-coverage.cjs is a pure deterministic predicate over the SEPARATE render registry + the dispatcher card-emission wiring (no LLM-judge, no network); the predicate body is pinned (MEDIUM-1): an entry is covered if its file calls pickShape( (host-appended) OR appendAskUserQuestionTrailer( directly, scanning those exact tokens file-scoped, NOT demanding an in-renderDialShape assignment; renderCoverageReport classifies every entry covered/excluded/gap (XOR); the live repo is gap=0; --check exits nonzero on any render gap with a self-naming error + recovery line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The FLOOR/hard-fail adversarial test -- the gate fails closed on an undeclared render entry point (C-3 floor)</name>
  <read_first>
    - scripts/check-render-coverage.cjs (Task 1: renderCoverageReport + the --check exit contract)
    - tests/test-coverage-gate-hardfail.cjs (the WHOLE file -- the CIRS hard-fail adversarial test to MIRROR for render: Test 1 a synthesized dark fixture classifies gap + --check exits nonzero; Test 2 an excluded-with-reason fixture is conformant; Test 3 the clean live repo passes --check exit 0; Test 4 the FAIL message names the offending entry + recovery line; the COPY-into-a-walked-dir-then-remove-in-a-finally idiom that never mutates a tracked file)
    - data/render-coverage-registry.json + scripts/build-render-coverage.cjs (178-01: the registry the synthesized fixture is injected into)
    - .planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md (D-178-01 the deliverable is the GATE; R-5 it MUST land hard-FAIL or it rots)
  </read_first>
  <action>
    Create tests/test-render-coverage-gate-hardfail.cjs mirroring tests/test-coverage-gate-hardfail.cjs
    verbatim in structure, for render: (Test 1) inject a SYNTHESIZED dark render entry point (a temp
    module containing a card-emission-declared entry that does NOT route through pickShape /
    appendAskUserQuestionTrailer and is not declared render-only-excluded) into a scope the 178-01
    walk covers, regenerate, assert renderCoverageReport classifies it gap, AND spawn
    check-render-coverage.cjs --check and assert NONZERO exit; remove the fixture in a finally.
    (Test 2) a synthesized entry declared render-only-excluded WITH a reason classifies excluded and
    does NOT trip the gate. (Test 3) the clean live repo: assert --check exits 0 (the baseline is
    gap=0 -- the F.7-dial entry is host-appended-covered, so there is NO known pre-178-03 gap; the
    live tree is already green, the F.7-dial slip premise is FALSE). (Test 4) the FAIL message names
    the offending entry + the recovery line. Use the copy-into-walked-scope-then-remove-in-a-finally
    idiom so NO tracked file is mutated; fixtures live under tests/fixtures/ and are never walked by
    the live generator. Register tests/test-render-coverage-gate-hardfail.cjs in tests/run-all-178.sh
    (alongside the 178-01 exhaustiveness suite). Exit 0 on pass. bash/CJS, no emoji, no em-dashes.
  </action>
  <behavior>
    - Test 1: a synthesized dark render entry point classifies gap AND --check exits nonzero; the fixture is removed in a finally; no tracked file mutated.
    - Test 2: a synthesized render-only-excluded-with-reason entry classifies excluded and does NOT trip the gate.
    - Test 3: the clean live repo's --check exits 0 (baseline gap=0; the F.7-dial entry is host-appended-covered; no known live gap).
    - Test 4: the gate FAIL message names the offending entry + the recovery line.
  </behavior>
  <verify>
    <automated>node tests/test-render-coverage-gate-hardfail.cjs</automated>
    <automated>bash tests/run-all-178.sh; echo "(run-all-178 carries 178-01 exhaustive + 178-02 predicate + FLOOR; 178-03 wiring assertions land next wave)"</automated>
  </verify>
  <done>tests/test-render-coverage-gate-hardfail.cjs mirrors the CIRS hard-fail test for render: a synthesized undeclared render entry point trips the gate RED, an excluded-with-reason entry is conformant, the live repo is GREEN (gap=0, F.7-dial host-appended), and the FAIL message names the entry + recovery line; registered in run-all-178.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry -> predicate | The predicate reads the SEPARATE render registry + the dispatcher card-emission wiring; the verdict is pure code, reproducible, no agent in the loop (C-2). |
| predicate -> CI exit | --check returns a deterministic nonzero exit on any render gap; the fail-closed contract that 178-03 wires into the four enforcement surfaces (C-3). |
| gate -> LLM | The HARD gate contains NO LLM-judge; LLM-judge is reserved for soft quality only (C-2), so the gate is CI-stable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-02-01 | Spoofing | the card-emission verdict | mitigate | The predicate is pure code over the registry + the pickShape host-append / appendAskUserQuestionTrailer wiring, scanning exact tokens file-scoped; the grep gate asserts no LLM/model/judge symbol in --check, so the verdict cannot be spoofed by a soft-quality model call (C-2). |
| T-178-02-02 | Tampering | the fail-closed exit | mitigate | The FLOOR/hard-fail test proves --check exits nonzero on a synthesized undeclared entry (mirrors test-coverage-gate-hardfail); a render gap can never silently pass (R-5). |
| T-178-02-03 | Repudiation | the gate verdict provenance | accept | The gate proves "every reachable render entry point is WIRED to emit a card", not "the model called the tool this turn" (R-1, the irreducible agent-honored terminal tool-call). Named, accepted; the PostToolUse-interceptor spike for R-1 is 178-04. |
| T-178-02-04 | Spoofing | the unreached-gate render | accept | Prose-mimicry (the model hand-painting interactive-looking glyphs on a turn the code never reached a gate) is OUT of scope (R-2, 150.5 hole #4): there is no marker to verify on an unreached turn. The SKILL doctrine line stays the named, accepted residual for the unreached case; the gate governs only REACHED-gate entry points. |
| T-178-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO package installs; pure CJS predicate + adversarial test over existing files. No Package Legitimacy Gate triggered. |
</threat_model>

<verification>
- node scripts/check-render-coverage.cjs (or its --check) is a pure deterministic predicate; the XOR invariant holds (covered+excluded+gap === entries.length); the live repo is gap=0.
- The C-2/Part-8 grep gate confirms no LLM-judge / model / network symbol in the hard --check path.
- node tests/test-render-coverage-gate-hardfail.cjs exits 0: a synthesized undeclared render entry point trips the gate RED; an excluded-with-reason entry is conformant; the live repo is GREEN; the FAIL message names the entry.
- bash tests/run-all-178.sh carries the 178-01 exhaustiveness suite + the 178-02 predicate + FLOOR test + the carried frozen-set drift fences (6 reaches, 3 postures still GREEN).
</verification>

<success_criteria>
- scripts/check-render-coverage.cjs is the deterministic, code-evaluated card-emission predicate (C-2): a pure code check over the SEPARATE render registry + the SEED-020 card-emission door, with NO LLM-judge in the hard gate.
- The predicate body is pinned (MEDIUM-1): an entry is covered if its file calls pickShape( (host-appended) OR appendAskUserQuestionTrailer( directly, scanning those exact tokens file-scoped; a host-appended marker is credited as covered (F.7-dial), NOT demanding an in-renderDialShape assignment.
- --check exits NONZERO on any card-emission entry that lacks card-emission routing (fail closed begins; C-3 wiring is 178-03); the live repo is gap=0.
- The FLOOR/hard-fail test (mirroring tests/test-coverage-gate-hardfail.cjs) proves the gate fails closed on a synthesized undeclared render entry point while the live repo stays GREEN.
- Additive only: no reach/posture/edge/node minted; Part 3 frozen contracts untouched; no Brain wire; R-1 named as accepted residual.
</success_criteria>

<output>
Create `.planning/phases/178-universal-gate-chokepoint/178-02-SUMMARY.md` when done.
</output>
</output>
