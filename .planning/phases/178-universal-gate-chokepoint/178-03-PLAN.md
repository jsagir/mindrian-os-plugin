---
phase: 178-universal-gate-chokepoint
plan: 03
type: execute
wave: 3
depends_on: ["178-01", "178-02"]
files_modified:
  - scripts/install-pre-commit.sh
  - scripts/release.sh
  - scripts/doctor.cjs
  - tests/test-render-gate-wiring.cjs
  - tests/test-f7-dial-gap-zero-confirm.cjs
  - tests/run-all-178.sh
autonomous: true
requirements: [C-3, D-178-04, D-178-05]
canon_parts: [3, 6, 7, 11]
cirs_relationship:
  surfaces_added:
    - check-render-coverage
  surfaces_modified:
    - selector-dispatcher
  surfaces_removed: []
  spine_consumed:
    - lib/hmi/selector-dispatcher.cjs
    - lib/hmi/dial-selector.cjs
  gate_impact: "FLIPS check-render-coverage to HARD-FAIL from day one (C-3) by wiring its --check into the SAME FOUR enforcement surfaces the CIRS gate rides (the Phase 172-13 R9 flip precedent): pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance. CONFIRMS the Phase-177 F.7-dial surface is ALREADY gap=0 -- it returns shape 'F.7-dial' (starts with 'F'), so pickShape's isFShape host-append (selector-dispatcher.cjs:931-933) appends the askuserquestion_marker for it, and the engine arm (intent-classifier.cjs:933) appends it explicitly. The baseline is green WITHOUT editing the frozen Phase-177 dial-selector.cjs; the gate flips hard-FAIL on an already-green baseline. No reach/posture/edge/node minted; no Brain wire (Part 8 LOCAL); the frozen Phase-177 dial-selector.cjs is NOT modified."
must_haves:
  truths:
    - "check-render-coverage --check is wired into pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance (the same four surfaces as the CIRS R9 gate)"
    - "the gate is HARD-FAIL from day one (nonzero CI exit), not WARN (C-3, R-5)"
    - "the Phase-177 F.7-dial surface is CONFIRMED already gap=0 -- it is host-appended (shape 'F.7-dial' -> pickShape isFShape branch appends the marker; the engine arm appends it explicitly). The frozen Phase-177 dial-selector.cjs is NOT edited (the live slip premise is FALSE)"
    - "the SEED-020 door (selector-dispatcher.cjs:537) + the isFShape host-append (:931-933) is the registry-VERIFIED chokepoint the gate asserts every reachable entry point routes through (D-178-05 one governed path)"
  artifacts:
    - path: "scripts/install-pre-commit.sh"
      provides: "the install-pre-commit + pre-commit hook lines invoking check-render-coverage.cjs --check"
      contains: "check-render-coverage"
    - path: "scripts/release.sh"
      provides: "the release-gate step invoking check-render-coverage.cjs --check"
      contains: "check-render-coverage"
    - path: "scripts/doctor.cjs"
      provides: "the render gate added to the doctor --acceptance coverage-gate organ"
      contains: "check-render-coverage"
    - path: "tests/test-f7-dial-gap-zero-confirm.cjs"
      provides: "the proof the F.7-dial surface is already gap=0 via the PRODUCTION engine-arm path (host-appended), with dial-selector.cjs byte-unchanged"
      contains: "askuserquestion_marker"
    - path: "tests/test-render-gate-wiring.cjs"
      provides: "the wiring proof: each of the four enforcement surfaces references check-render-coverage --check"
      contains: "render-coverage"
  key_links:
    - from: "scripts/install-pre-commit.sh + release.sh + doctor.cjs"
      to: "scripts/check-render-coverage.cjs --check"
      via: "the same four enforcement-surface invocation idiom as build-connector-registry.cjs --check"
      pattern: "check-render-coverage.cjs --check"
    - from: "the production engine arm (intent-classifier.cjs:933)"
      to: "lib/hmi/selector-dispatcher.cjs:537 appendAskUserQuestionTrailer"
      via: "the F.7-dial render is host-appended through the card-emission door (already covered)"
      pattern: "appendAskUserQuestionTrailer|askuserquestion_marker"
---

<objective>
Flip check-render-coverage to HARD-FAIL from day one (C-3) and CONFIRM the F.7-dial surface is
already gap=0 (the live slip premise is FALSE) -- without touching the frozen Phase-177
dial-selector.cjs.

C-3 is the third of the three non-negotiables (D-178-03) and the one whose absence rotted
143.x/144.1 before the CIRS R9 flip: a WARN-only render gate is the SKILL fence with extra steps
(R-5). The render gate must ride the SAME FOUR enforcement surfaces the CIRS gate rides after the
Phase 172-13 flip: pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance. The exact
invocation idiom already exists for build-connector-registry.cjs --check and
build-orchestration-projection.cjs --check at all four surfaces; this plan adds
check-render-coverage.cjs --check alongside them.

CRITICAL CORRECTION (the F.7-dial 'live slip' premise is FALSE on the live tree). An earlier draft
of this plan asserted the F.7-dial surface was a live render GAP that had to be CLOSED by routing
renderDialShape through the card-emission door, and that the gate could not flip hard-FAIL until
that gap was closed. That premise is FALSE: renderDialShape (lib/hmi/dial-selector.cjs:216-225)
returns an envelope whose shape is 'F.7-dial', which starts with 'F', so the dispatcher's pickShape
host-append (selector-dispatcher.cjs:931-933, the isFShape branch) UNCONDITIONALLY applies
appendAskUserQuestionTrailer to it -- and the production engine arm (scripts/intent-classifier.cjs:933)
ALSO appends the marker explicitly. The F.7-dial entry is therefore ALREADY card-emission-covered;
178-01's registry records it as such and 178-02's predicate already reports gap=0 on the live tree.
So this plan does NOT edit the frozen Phase-177 dial-selector.cjs (no routing change, no marker
assignment, no contract touch). It CONFIRMS gap=0 by probing the PRODUCTION engine-arm path, then
flips the gate hard-FAIL on the already-green baseline (the same sequencing principle as the Phase
172-13 flip: the baseline is green BEFORE the flip, so CI never goes RED).

Per D-178-04 additive only: no reach/posture/edge/node minted, Part 3 frozen contracts (MAX_K=3,
DIAL_REACH_K=6, the 0.70/0.15 gate, the 6-reach bank, the glyphs) untouched, no Brain wire (Part 8),
and the frozen Phase-177 dial-selector.cjs is BYTE-UNCHANGED. Per D-178-05 Brain suggestions +
framework invocations render through the same verified chokepoint (Part 11 one governed path) -- the
F.7-dial surface already honors that path via the host-append.

Purpose: make the render gate non-bypassable at every merge/release/health surface (C-3) and
CONFIRM (not edit) that the F.7-dial baseline is green.
Output: the four enforcement surfaces wired, a green tests/test-f7-dial-gap-zero-confirm.cjs proving
the F.7-dial host-append via the production engine arm, a green tests/test-render-gate-wiring.cjs,
and run-all-178.sh fully green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md
@.planning/phases/178-universal-gate-chokepoint/178-RESEARCH.md
@.planning/phases/178-universal-gate-chokepoint/178-01-PLAN.md
@.planning/phases/178-universal-gate-chokepoint/178-02-PLAN.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONFIRM the F.7-dial surface is already gap=0 (host-appended) via the PRODUCTION engine-arm path -- do NOT edit the frozen Phase-177 dial-selector.cjs (C-3 baseline)</name>
  <read_first>
    - lib/hmi/selector-dispatcher.cjs:528-548 (appendAskUserQuestionTrailer -- the SEED-020 single construction door; :537 sets rendered.askuserquestion_marker), :911-944 (pickShape's isFShape host-append: :930-944 -- ANY result whose shape starts with 'F' gets appendAskUserQuestionTrailer applied at :933; this is WHY F.7-dial is already covered), :755-772 (the F.7-dial dispatch branch: safeRequire('./dial-selector.cjs'), fnName 'renderDialShape', the { shape, rendered } return)
    - lib/hmi/dial-selector.cjs:216-225 (renderDialShape returns { shape: 'F.7-dial', hud, zones } -- it does NOT assign a marker itself; it does NOT need to, because pickShape host-appends for any 'F.*' shape. This file is the FROZEN Phase-177 surface and MUST stay byte-unchanged)
    - scripts/intent-classifier.cjs:919-937 (the PRODUCTION engine arm: it builds the dial render, then calls dispatcher.appendAskUserQuestionTrailer(rendered,'F.1') at :933 and reads rendered.askuserquestion_marker at :934 -- the production path the confirmation test must probe, NOT just a synthetic pickShape call)
    - scripts/check-render-coverage.cjs (178-02: the predicate that already reports the F.7-dial entry as covered and the live repo as gap=0)
    - .planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md (D-178-05 Brain suggestions + framework invocations render through the same verified chokepoint -- Part 11 one governed path; D-178-04 additive only, no reach/posture minted, frozen Part 3 contracts untouched)
  </read_first>
  <action>
    CONFIRM (do not edit) that the Phase-177 F.7-dial surface is already gap=0. The F.7-dial 'live
    slip' premise is FALSE on the live tree: renderDialShape returns shape 'F.7-dial' (starts with
    'F'), so pickShape's isFShape host-append (selector-dispatcher.cjs:931-933) applies
    appendAskUserQuestionTrailer to it, and the production engine arm (intent-classifier.cjs:933)
    appends the marker explicitly. Therefore make NO edit to lib/hmi/dial-selector.cjs (it stays
    byte-unchanged -- it is a frozen Phase-177 contract: do not change the dial's two axes, the HUD
    glyphs, the keyboard, the renderDialShape return, or any frozen Part 3 contract; do not add a
    marker assignment inside renderDialShape). Instead, write tests/test-f7-dial-gap-zero-confirm.cjs
    that PROVES the F.7-dial surface is host-appended-covered by probing the PRODUCTION engine-arm
    path (NOT merely a synthetic pickShape call): exercise the intent-classifier dial-render seam
    (or, if that seam is not directly callable in isolation, drive the same path it drives --
    presenter.renderDial(...) then dispatcher.appendAskUserQuestionTrailer(rendered,'F.1') as the
    engine arm does at intent-classifier.cjs:919-937) and assert the resulting envelope carries a
    non-empty askuserquestion_marker. Add a second assertion that a direct pickShape call with
    requestedShape 'F.7-dial' also yields a non-empty askuserquestion_marker (the isFShape
    host-append), to prove BOTH the dispatcher host-append and the engine-arm path cover it. Add a
    third assertion that the 178-02 predicate reports counts.gap === 0 on the live repo (the F.7-dial
    entry classifies covered). Add a fourth assertion that lib/hmi/dial-selector.cjs was NOT modified
    by this plan (the frozen-contract guard: assert renderDialShape returns shape 'F.7-dial' with the
    same HUD shape it always did, and do not edit the file). Register the test in tests/run-all-178.sh.
    Re-run the 178-02 predicate and confirm counts.gap === 0 on the live repo.
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process'); const before=require('crypto').createHash('sha256').update(require('fs').readFileSync('lib/hmi/dial-selector.cjs')).digest('hex'); console.log('dial-selector.cjs sha256='+before+' (must be byte-unchanged by this plan)');"</automated>
    <automated>node -e "const d=require('./lib/hmi/selector-dispatcher.cjs'); const r=d.pickShape({requestedShape:'F.7-dial', investment_level:0.5, posture:'hold', tty:false}); const m=r&&r.rendered&&r.rendered.askuserquestion_marker; if(typeof m!=='string'||!m.length) throw new Error('F.7-dial not host-appended through the card-emission door'); console.log('OK F.7-dial host-appended via pickShape isFShape branch');"</automated>
    <automated>node -e "const c=require('./scripts/check-render-coverage.cjs'); const r=c.renderCoverageReport(); if(r.counts.gap!==0) throw new Error('render gap '+r.counts.gap+' -- F.7-dial baseline not green'); console.log('OK render baseline green: gap=0 (F.7-dial covered, no dial-selector edit)');"</automated>
    <automated>node tests/test-f7-dial-gap-zero-confirm.cjs</automated>
    <automated>bash tests/run-all-177.sh >/dev/null 2>&1; echo "run-all-177 exit=$? (frozen 177 dial contracts must stay green; dial-selector.cjs untouched)"</automated>
  </verify>
  <done>The F.7-dial surface is CONFIRMED already gap=0 -- host-appended via pickShape's isFShape branch AND the production engine arm (intent-classifier.cjs:933); tests/test-f7-dial-gap-zero-confirm.cjs probes the PRODUCTION engine-arm path (not just a synthetic call) and asserts the non-empty marker + predicate gap=0; lib/hmi/dial-selector.cjs is BYTE-UNCHANGED (the frozen Phase-177 contract is not edited; the live slip premise is FALSE); appendAskUserQuestionTrailer stays the single chokepoint (D-178-05).</done>
</task>

<task type="auto">
  <name>Task 2: Wire check-render-coverage --check into the four enforcement surfaces, HARD-FAIL (C-3) + the wiring proof</name>
  <read_first>
    - scripts/install-pre-commit.sh:37 (the hook-presence grep guard listing build-connector-registry.cjs --check + build-orchestration-projection.cjs --check), :89-118 (the two hook-line install blocks + the "Recovery on drift" comment idiom + the node ... --check || { echo ...; exit 1; } fail-closed line), :169-179 (the dev-clone manifest-surface install blocks -- the SECOND place both CIRS gates are wired) -- add check-render-coverage.cjs --check alongside each, mirroring the idiom exactly
    - scripts/release.sh:280-288 (the two release-gate blocks: if ! node "$PLUGIN_DIR/scripts/build-connector-registry.cjs" --check; then ... Recovery ... fi and the projection block) -- add a check-render-coverage.cjs --check block in the same idiom
    - scripts/doctor.cjs:2730-2770 (the 'coverage-gate' acceptance organ: the gates[] array of { id, script } spawned with --check via cp.spawnSync, results aggregated, ok = failed.length === 0) -- add { id: 'render', script: 'check-render-coverage.cjs' } to the gates[] array so doctor --acceptance runs the render gate too
    - scripts/check-render-coverage.cjs (178-02: the --check entry the four surfaces invoke)
    - .planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md + 178-RESEARCH.md sec 5 (the Phase 172-13 R9 hard-FAIL precedent: both CIRS gates' --check wired into pre-commit + install-pre-commit + release.sh + doctor --acceptance; the render gate rides the same four surfaces; C-3 / R-5 hard-FAIL not WARN)
  </read_first>
  <action>
    Wire scripts/check-render-coverage.cjs --check into the SAME FOUR enforcement surfaces the two
    CIRS gates ride, mirroring each idiom exactly (Part 7 reuse of the wiring pattern, the Phase
    172-13 R9 precedent): (1) scripts/install-pre-commit.sh -- add check-render-coverage.cjs --check
    to the hook-presence grep guard (:37) AND add the hook-line install block + the "Recovery on
    drift" comment + the node ... --check || { echo 'render-coverage drift / dark surface -- run:
    node scripts/check-render-coverage.cjs' >&2; exit 1; } fail-closed line, in BOTH the primary
    install blocks (:89-118) and the dev-clone manifest-surface blocks (:169-179). (2)
    scripts/release.sh -- add an if ! node "$PLUGIN_DIR/scripts/check-render-coverage.cjs" --check;
    then ...; Recovery ...; fi block alongside the two existing CIRS blocks (:280-288). (3)
    scripts/doctor.cjs -- add { id: 'render', script: 'check-render-coverage.cjs' } to the
    'coverage-gate' organ's gates[] array (:2737-2738) so doctor --acceptance spawns the render gate
    --check too. (4) The pre-commit hook itself is INSTALLED by install-pre-commit.sh, so wiring it
    there covers the pre-commit surface; if a tracked .git/hooks template or a committed hook stub
    exists, mirror it there too. Every wire is HARD-FAIL (nonzero exit), never WARN (C-3, R-5). Then
    create tests/test-render-gate-wiring.cjs proving each of the four surfaces references
    check-render-coverage.cjs --check (grep-assert install-pre-commit.sh, release.sh, and the doctor
    gates[] array include it; assert the install-pre-commit fail-closed line uses exit 1 not a warn).
    Register it in tests/run-all-178.sh. Re-run the full run-all-178.sh and confirm GREEN (the
    baseline is green from Task 1's confirmation -- F.7-dial host-appended, gap=0 -- so the hard-FAIL
    gate passes on a clean repo).
  </action>
  <behavior>
    - install-pre-commit.sh references check-render-coverage.cjs --check in the grep guard AND in both install-block families, each with an exit 1 fail-closed line.
    - release.sh has a check-render-coverage.cjs --check block that aborts the release on nonzero.
    - doctor.cjs's coverage-gate organ gates[] array includes { id:'render', script:'check-render-coverage.cjs' }.
    - test-render-gate-wiring.cjs asserts all four surfaces reference the gate and that the wiring is hard-FAIL (exit 1), not WARN.
    - run-all-178.sh is fully GREEN on the clean repo (baseline gap=0 from Task 1's confirmation).
  </behavior>
  <verify>
    <automated>node tests/test-render-gate-wiring.cjs</automated>
    <automated>node scripts/check-render-coverage.cjs --check; echo "render gate --check exit=$? (must be 0 on the green baseline)"</automated>
    <automated>grep -c "check-render-coverage.cjs --check" scripts/install-pre-commit.sh scripts/release.sh | grep -v ':0' >/dev/null && grep -q "check-render-coverage.cjs" scripts/doctor.cjs && echo "OK render gate wired into the four enforcement surfaces" || { echo "render gate not wired into all surfaces" >&2; exit 1; }</automated>
    <automated>bash tests/run-all-178.sh</automated>
  </verify>
  <done>check-render-coverage.cjs --check is wired HARD-FAIL into pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance (the same four surfaces as the CIRS R9 gate); test-render-gate-wiring proves all four reference it with exit-1 fail-closed; run-all-178.sh is fully GREEN on the already-green baseline (F.7-dial host-appended, gap=0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge/release/health -> render gate | check-render-coverage --check runs at pre-commit, install-pre-commit, release, and doctor --acceptance; a render gap cannot land at any of the four surfaces (C-3). |
| F.7-dial surface -> card-emission door | The F.7-dial render is ALREADY host-appended through the SEED-020 single construction door (appendAskUserQuestionTrailer via the isFShape branch + the engine arm); no edit to the frozen dial-selector.cjs is needed (D-178-05 one governed path). |
| gate tier -> WARN | The render gate lands hard-FAIL (exit 1), never WARN; the test asserts the fail-closed line (R-5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-03-01 | Tampering | the enforcement-tier (WARN vs FAIL) | mitigate | The wiring is hard-FAIL (exit 1) at all four surfaces; test-render-gate-wiring asserts the exit-1 fail-closed line, so the gate cannot silently regress to WARN and rot (R-5, the 143.x/144.1 failure mode). |
| T-178-03-02 | Tampering | the frozen Phase-177 dial-selector.cjs | mitigate | This plan CONFIRMS F.7-dial gap=0 via the host-append; it does NOT edit dial-selector.cjs (the live slip premise is FALSE). The confirmation test asserts the file is byte-unchanged and run-all-177 stays green, so the frozen Part 3 contract cannot be silently weakened. |
| T-178-03-03 | Denial of Service | a RED baseline blocking CI | mitigate | The baseline is ALREADY gap=0 (F.7-dial host-appended) BEFORE Task 2 flips the gate hard-FAIL -- the Phase 172-13 sequencing principle (the baseline is green first, then flip) so CI never goes RED. |
| T-178-03-04 | Repudiation | the terminal tool-call | accept | The gate enforces "every reachable entry point is WIRED to emit a card" at the four surfaces; the terminal LLM tool-call on a real turn stays agent-honored (R-1). Named, accepted; the PostToolUse-interceptor spike is 178-04. |
| T-178-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO package installs; shell + CJS wiring over existing files. No Package Legitimacy Gate triggered. |
</threat_model>

<verification>
- node -e probe confirms a rendered F.7-dial envelope carries an askuserquestion_marker (host-appended through the SEED-020 chokepoint via the isFShape branch + the production engine arm).
- lib/hmi/dial-selector.cjs is byte-unchanged by this plan (the frozen Phase-177 contract; the confirmation test asserts it).
- node scripts/check-render-coverage.cjs --check exits 0 on the green baseline (gap=0; F.7-dial host-appended).
- grep confirms check-render-coverage.cjs --check is referenced in install-pre-commit.sh, release.sh, and doctor.cjs (the four enforcement surfaces).
- node tests/test-render-gate-wiring.cjs exits 0: all four surfaces reference the gate with an exit-1 fail-closed line (hard-FAIL, not WARN).
- node tests/test-f7-dial-gap-zero-confirm.cjs exits 0: the F.7-dial host-append is proven via the production engine-arm path; gap=0; dial-selector.cjs untouched.
- bash tests/run-all-178.sh is fully GREEN; bash tests/run-all-177.sh stays GREEN (frozen 177 dial contracts intact).
</verification>

<success_criteria>
- check-render-coverage --check is wired HARD-FAIL into pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance (C-3), the same four surfaces as the CIRS R9 gate (Phase 172-13 precedent).
- The F.7-dial surface is CONFIRMED already gap=0 (host-appended via the isFShape branch + the production engine arm); the frozen Phase-177 dial-selector.cjs is BYTE-UNCHANGED (the live slip premise is FALSE); the live gate is GREEN.
- The gate is hard-FAIL, never WARN (R-5); test-render-gate-wiring proves the exit-1 fail-closed line at every surface.
- Additive only: no reach/posture/edge/node minted; Part 3 frozen contracts untouched; no Brain wire; R-1 named as accepted residual.
</success_criteria>

<output>
Create `.planning/phases/178-universal-gate-chokepoint/178-03-SUMMARY.md` when done.
</output>
</output>
