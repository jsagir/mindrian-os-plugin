---
phase: 178-universal-gate-chokepoint
plan: 03
type: execute
wave: 3
depends_on: ["178-01", "178-02"]
files_modified:
  - scripts/install-pre-commit.sh
  - scripts/release.sh
  - scripts/doctor.cjs
  - lib/hmi/dial-selector.cjs
  - tests/test-render-gate-wiring.cjs
  - tests/run-all-178.sh
autonomous: true
requirements: [RND-03, RND-WIRE, RND-CHOKEPOINT]
canon_parts: [3, 6, 7, 11]
cirs_relationship:
  surfaces_added:
    - check-render-coverage
  surfaces_modified:
    - selector-dispatcher
    - dial-selector
  surfaces_removed: []
  spine_consumed:
    - lib/hmi/selector-dispatcher.cjs
    - lib/hmi/dial-selector.cjs
  gate_impact: "FLIPS check-render-coverage to HARD-FAIL from day one (C-3) by wiring its --check into the SAME FOUR enforcement surfaces the CIRS gate rides (the Phase 172-13 R9 flip precedent): pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance. PROMOTES the SEED-020 single construction door (selector-dispatcher.cjs:537 appendAskUserQuestionTrailer) to the registry-VERIFIED chokepoint by wiring the Phase-177 F.7-dial surface (lib/hmi/dial-selector.cjs renderDialShape) through it -- closing the live slip so the gate goes green. No reach/posture/edge/node minted; no Brain wire (Part 8 LOCAL)."
must_haves:
  truths:
    - "check-render-coverage --check is wired into pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance (the same four surfaces as the CIRS R9 gate)"
    - "the gate is HARD-FAIL from day one (nonzero CI exit), not WARN (C-3, R-5)"
    - "the Phase-177 F.7-dial surface (lib/hmi/dial-selector.cjs) now routes through the SEED-020 card-emission door, so the live gate is GREEN (the slip closed)"
    - "the SEED-020 door (selector-dispatcher.cjs:537) is the registry-VERIFIED chokepoint the gate asserts every reachable surface routes through (D-178-05 one governed path)"
  artifacts:
    - path: "scripts/install-pre-commit.sh"
      provides: "the install-pre-commit + pre-commit hook lines invoking check-render-coverage.cjs --check"
      contains: "check-render-coverage"
    - path: "scripts/release.sh"
      provides: "the release-gate step invoking check-render-coverage.cjs --check"
      contains: "check-render-coverage"
    - path: "scripts/doctor.cjs"
      provides: "the render gate added to the doctor --acceptance coverage-gate organ"
      contains: "check-render-coverage"
    - path: "lib/hmi/dial-selector.cjs"
      provides: "the F.7-dial surface routed through the card-emission door so the gate is GREEN"
      contains: "askuserquestion_marker"
    - path: "tests/test-render-gate-wiring.cjs"
      provides: "the wiring proof: each of the four enforcement surfaces references check-render-coverage --check"
      contains: "render-coverage"
  key_links:
    - from: "scripts/install-pre-commit.sh + release.sh + doctor.cjs"
      to: "scripts/check-render-coverage.cjs --check"
      via: "the same four enforcement-surface invocation idiom as build-connector-registry.cjs --check"
      pattern: "check-render-coverage.cjs --check"
    - from: "lib/hmi/dial-selector.cjs renderDialShape"
      to: "lib/hmi/selector-dispatcher.cjs:537 appendAskUserQuestionTrailer"
      via: "the F.7-dial return routes through the card-emission door"
      pattern: "appendAskUserQuestionTrailer|askuserquestion_marker"
---

<objective>
Flip check-render-coverage to HARD-FAIL from day one (C-3) and promote the SEED-020 door to
the registry-verified chokepoint.

C-3 is the third of the three non-negotiables (D-178-03) and the one whose absence rotted 143.x/144.1 before the CIRS
R9 flip: a WARN-only render gate is the SKILL fence with extra steps (R-5). The render gate
must ride the SAME FOUR enforcement surfaces the CIRS gate rides after the Phase 172-13 flip:
pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance. The exact invocation
idiom already exists for build-connector-registry.cjs --check and
build-orchestration-projection.cjs --check at all four surfaces; this plan adds
check-render-coverage.cjs --check alongside them.

The gate cannot land hard-FAIL while the F.7-dial surface is a live gap (it would turn CI RED
on a clean repo). So this plan ALSO promotes the SEED-020 single construction door
(selector-dispatcher.cjs:528-548/537 appendAskUserQuestionTrailer) to the registry-verified
chokepoint by routing the Phase-177 F.7-dial surface (lib/hmi/dial-selector.cjs renderDialShape)
through it -- closing the slip that 178-CONTEXT names "the slip in real time". That makes the
178-02 predicate report gap=0, and the gate flips hard-FAIL on a green baseline (the same
sequencing the Phase 172-13 flip used: wire/exclude the baseline FIRST, then flip, so CI never
goes RED mid-sweep).

Per D-178-04 additive only: no reach/posture/edge/node minted, Part 3 frozen contracts
(MAX_K=3, DIAL_REACH_K=6, the 0.70/0.15 gate, the 6-reach bank, the glyphs) untouched, no
Brain wire (Part 8). Per D-178-05 Brain suggestions + framework invocations render through the
same verified chokepoint (Part 11 one governed path) -- this plan makes the F.7-dial surface
honor that path.

Purpose: make the render gate non-bypassable at every merge/release/health surface (C-3) and
close the live F.7-dial slip so the baseline is green.
Output: the four enforcement surfaces wired, lib/hmi/dial-selector.cjs routed through the
card-emission door, a green tests/test-render-gate-wiring.cjs, and run-all-178.sh fully green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md
@.planning/phases/178-universal-gate-chokepoint/178-RESEARCH.md
@.planning/phases/178-universal-gate-chokepoint/178-01-PLAN.md
@.planning/phases/178-universal-gate-chokepoint/178-02-PLAN.md
@docs/MINDRIAN-CANON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote the SEED-020 door to the verified chokepoint -- route the F.7-dial surface through card-emission so the baseline is GREEN (RND-CHOKEPOINT)</name>
  <read_first>
    - lib/hmi/selector-dispatcher.cjs:528-548 (appendAskUserQuestionTrailer -- the SEED-020 single construction door; :537 sets rendered.askuserquestion_marker) and :755-772 (the F.7-dial dispatch branch: safeRequire('./dial-selector.cjs'), fnName 'renderDialShape', the { shape, rendered } return -- where the trailer must be appended for F.7-dial, mirroring how the F.* branches attach it)
    - lib/hmi/dial-selector.cjs:141-229 (renderDialHud + renderDialShape -- the F.7-dial surface returning { shape, hud, zones }; Phase 177 minted it with NO askuserquestion_marker, the slip the gate caught)
    - scripts/intent-classifier.cjs:930-938 (the engine-arm pattern that calls appendAskUserQuestionTrailer(rendered,'F.1') then reads rendered.askuserquestion_marker -- the canonical routing the F.7-dial surface must now honor)
    - scripts/check-render-coverage.cjs (178-02: the predicate that currently reports F.7-dial as the known gap; routing it closes the gap)
    - .planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md (D-178-05 Brain suggestions + framework invocations render through the same verified chokepoint -- Part 11 one governed path; D-178-04 additive only, no reach/posture minted)
  </read_first>
  <action>
    Route the Phase-177 F.7-dial surface through the SEED-020 card-emission door so the 178-02
    predicate reports gap=0 (the green baseline the hard-FAIL flip requires). In the F.7-dial
    dispatch branch (selector-dispatcher.cjs:755-772) -- OR in renderDialShape
    (lib/hmi/dial-selector.cjs:216-229), whichever keeps the SEED-020 door as the single
    construction site -- ensure the F.7-dial rendered envelope routes through
    appendAskUserQuestionTrailer (or otherwise carries an askuserquestion_marker) exactly as
    the F.* shapes do, so a reached F.7-dial gate is WIRED to emit a card. Do NOT change the
    dial's two axes, the HUD glyphs, the keyboard, or any frozen Part 3 contract (MAX_K=3,
    DIAL_REACH_K=6, the 0.70/0.15 gate, the 6-reach bank, the body glyphs) -- this is a routing
    wire-up, not a render change. Keep appendAskUserQuestionTrailer the ONE construction door
    (do not mint a second marker path; D-178-05 one governed path). Mint NO reach, NO posture,
    NO edge/node, NO Brain wire. Then re-run the 178-02 predicate and confirm the F.7-dial
    surface now classifies covered and counts.gap === 0. Update/extend the relevant 177 dial
    render test only if the marker assertion is additive (do not weaken existing 177 contracts).
  </action>
  <behavior>
    - a rendered F.7-dial envelope carries a non-empty askuserquestion_marker after dispatch (it routes through the SEED-020 door).
    - renderCoverageReport().counts.gap === 0 on the live repo after the wire-up.
    - the dial's two axes, HUD glyphs, keyboard, and the frozen Part 3 contracts are byte-unchanged (no reach/posture minted).
    - appendAskUserQuestionTrailer remains the single card-emission construction door (no second marker path introduced).
  </behavior>
  <verify>
    <automated>node -e "const d=require('./lib/hmi/selector-dispatcher.cjs'); const r=d.pickShape({requestedShape:'F.7-dial', investment_level:0.5, posture:'hold', tty:false}); const m=r&&r.rendered&&r.rendered.askuserquestion_marker; if(typeof m!=='string'||!m.length) throw new Error('F.7-dial does not route through the card-emission door'); console.log('OK F.7-dial routed through SEED-020 chokepoint');"</automated>
    <automated>node -e "const c=require('./scripts/check-render-coverage.cjs'); const r=c.renderCoverageReport(); if(r.counts.gap!==0) throw new Error('render gap still '+r.counts.gap+' -- baseline not green'); console.log('OK render baseline green: gap=0');"</automated>
    <automated>bash tests/run-all-177.sh >/dev/null 2>&1; echo "run-all-177 exit=$? (frozen 177 dial contracts must stay green)"</automated>
  </verify>
  <done>The F.7-dial surface routes through the SEED-020 card-emission door (carries an askuserquestion_marker); the 178-02 predicate reports gap=0 on the live repo; the dial axes/glyphs/keyboard + the frozen Part 3 contracts are unchanged; appendAskUserQuestionTrailer stays the single chokepoint (D-178-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire check-render-coverage --check into the four enforcement surfaces, HARD-FAIL (C-3, RND-WIRE) + the wiring proof</name>
  <read_first>
    - scripts/install-pre-commit.sh:37 (the hook-presence grep guard listing build-connector-registry.cjs --check + build-orchestration-projection.cjs --check), :89-118 (the two hook-line install blocks + the "Recovery on drift" comment idiom + the node ... --check || { echo ...; exit 1; } fail-closed line), :169-179 (the dev-clone manifest-surface install blocks -- the SECOND place both CIRS gates are wired) -- add check-render-coverage.cjs --check alongside each, mirroring the idiom exactly
    - scripts/release.sh:280-288 (the two release-gate blocks: if ! node "$PLUGIN_DIR/scripts/build-connector-registry.cjs" --check; then ... Recovery ... fi and the projection block) -- add a check-render-coverage.cjs --check block in the same idiom
    - scripts/doctor.cjs:2730-2770 (the 'coverage-gate' acceptance organ: the gates[] array of { id, script } spawned with --check via cp.spawnSync, results aggregated, ok = failed.length === 0) -- add { id: 'render', script: 'check-render-coverage.cjs' } to the gates[] array so doctor --acceptance runs the render gate too
    - scripts/check-render-coverage.cjs (178-02: the --check entry the four surfaces invoke)
    - .planning/phases/178-universal-gate-chokepoint/178-CONTEXT.md + 178-RESEARCH.md sec 5 (the Phase 172-13 R9 hard-FAIL precedent: both CIRS gates' --check wired into pre-commit + install-pre-commit + release.sh + doctor --acceptance; the render gate rides the same four surfaces; C-3 / R-5 hard-FAIL not WARN)
  </read_first>
  <action>
    Wire scripts/check-render-coverage.cjs --check into the SAME FOUR enforcement surfaces the
    two CIRS gates ride, mirroring each idiom exactly (Part 7 reuse of the wiring pattern, the
    Phase 172-13 R9 precedent): (1) scripts/install-pre-commit.sh -- add check-render-coverage.cjs
    --check to the hook-presence grep guard (:37) AND add the hook-line install block + the
    "Recovery on drift" comment + the node ... --check || { echo 'render-coverage drift / dark
    surface -- run: node scripts/check-render-coverage.cjs' >&2; exit 1; } fail-closed line, in
    BOTH the primary install blocks (:89-118) and the dev-clone manifest-surface blocks
    (:169-179). (2) scripts/release.sh -- add an if ! node "$PLUGIN_DIR/scripts/check-render-
    coverage.cjs" --check; then ...; Recovery ...; fi block alongside the two existing CIRS
    blocks (:280-288). (3) scripts/doctor.cjs -- add { id: 'render', script:
    'check-render-coverage.cjs' } to the 'coverage-gate' organ's gates[] array (:2737-2738) so
    doctor --acceptance spawns the render gate --check too. (4) The pre-commit hook itself is
    INSTALLED by install-pre-commit.sh, so wiring it there covers the pre-commit surface; if a
    tracked .git/hooks template or a committed hook stub exists, mirror it there too. Every
    wire is HARD-FAIL (nonzero exit), never WARN (C-3, R-5). Then create
    tests/test-render-gate-wiring.cjs proving each of the four surfaces references
    check-render-coverage.cjs --check (grep-assert install-pre-commit.sh, release.sh, and the
    doctor gates[] array include it; assert the install-pre-commit fail-closed line uses exit 1
    not a warn). Register it in tests/run-all-178.sh. Re-run the full run-all-178.sh and confirm
    GREEN (the baseline is green from Task 1, so the hard-FAIL gate passes on a clean repo).
  </action>
  <behavior>
    - install-pre-commit.sh references check-render-coverage.cjs --check in the grep guard AND in both install-block families, each with an exit 1 fail-closed line.
    - release.sh has a check-render-coverage.cjs --check block that aborts the release on nonzero.
    - doctor.cjs's coverage-gate organ gates[] array includes { id:'render', script:'check-render-coverage.cjs' }.
    - test-render-gate-wiring.cjs asserts all four surfaces reference the gate and that the wiring is hard-FAIL (exit 1), not WARN.
    - run-all-178.sh is fully GREEN on the clean repo (baseline gap=0 from Task 1).
  </behavior>
  <verify>
    <automated>node tests/test-render-gate-wiring.cjs</automated>
    <automated>node scripts/check-render-coverage.cjs --check; echo "render gate --check exit=$? (must be 0 on the green baseline)"</automated>
    <automated>grep -c "check-render-coverage.cjs --check" scripts/install-pre-commit.sh scripts/release.sh | grep -v ':0' >/dev/null && grep -q "check-render-coverage.cjs" scripts/doctor.cjs && echo "OK render gate wired into the four enforcement surfaces" || { echo "render gate not wired into all surfaces" >&2; exit 1; }</automated>
    <automated>bash tests/run-all-178.sh</automated>
  </verify>
  <done>check-render-coverage.cjs --check is wired HARD-FAIL into pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance (the same four surfaces as the CIRS R9 gate); test-render-gate-wiring proves all four reference it with exit-1 fail-closed; run-all-178.sh is fully GREEN on the green baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge/release/health -> render gate | check-render-coverage --check runs at pre-commit, install-pre-commit, release, and doctor --acceptance; a render gap cannot land at any of the four surfaces (C-3). |
| F.7-dial surface -> card-emission door | The F.7-dial render now routes through the SEED-020 single construction door (appendAskUserQuestionTrailer); the slip is closed structurally (D-178-05 one governed path). |
| gate tier -> WARN | The render gate lands hard-FAIL (exit 1), never WARN; the test asserts the fail-closed line (R-5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-03-01 | Tampering | the enforcement-tier (WARN vs FAIL) | mitigate | The wiring is hard-FAIL (exit 1) at all four surfaces; test-render-gate-wiring asserts the exit-1 fail-closed line, so the gate cannot silently regress to WARN and rot (R-5, the 143.x/144.1 failure mode). |
| T-178-03-02 | Elevation of Privilege | the F.7-dial bypass | mitigate | The F.7-dial surface routes through the SEED-020 single construction door; no second marker path is introduced, so the dial cannot bypass the card-emission chokepoint (D-178-05 one governed path). |
| T-178-03-03 | Denial of Service | a RED baseline blocking CI | mitigate | Task 1 closes the F.7-dial gap (gap=0) BEFORE Task 2 flips the gate hard-FAIL -- the Phase 172-13 sequencing (wire/exclude the baseline first, then flip) so CI never goes RED mid-sweep. |
| T-178-03-04 | Repudiation | the terminal tool-call | accept | The gate enforces "every reachable surface is WIRED to emit a card" at the four surfaces; the terminal LLM tool-call on a real turn stays agent-honored (R-1). Named, accepted; the PostToolUse-interceptor spike is 178-04. |
| T-178-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO package installs; shell + CJS wiring over existing files. No Package Legitimacy Gate triggered. |
</threat_model>

<verification>
- node -e probe confirms a rendered F.7-dial envelope carries an askuserquestion_marker (routes through the SEED-020 chokepoint).
- node scripts/check-render-coverage.cjs --check exits 0 on the green baseline (gap=0 after Task 1).
- grep confirms check-render-coverage.cjs --check is referenced in install-pre-commit.sh, release.sh, and doctor.cjs (the four enforcement surfaces).
- node tests/test-render-gate-wiring.cjs exits 0: all four surfaces reference the gate with an exit-1 fail-closed line (hard-FAIL, not WARN).
- bash tests/run-all-178.sh is fully GREEN (178-01 exhaustive + 178-02 predicate/FLOOR + 178-03 wiring + the carried frozen-set drift fences); bash tests/run-all-177.sh stays GREEN (frozen 177 dial contracts intact).
</verification>

<success_criteria>
- check-render-coverage --check is wired HARD-FAIL into pre-commit + install-pre-commit.sh + release.sh + doctor --acceptance (C-3), the same four surfaces as the CIRS R9 gate (Phase 172-13 precedent).
- The SEED-020 single construction door is promoted to the registry-verified chokepoint: the Phase-177 F.7-dial surface routes through it, the slip is closed, and the live gate is GREEN (gap=0).
- The gate is hard-FAIL, never WARN (R-5); test-render-gate-wiring proves the exit-1 fail-closed line at every surface.
- Additive only: no reach/posture/edge/node minted; Part 3 frozen contracts untouched; no Brain wire; R-1 named as accepted residual.
</success_criteria>

<output>
Create `.planning/phases/178-universal-gate-chokepoint/178-03-SUMMARY.md` when done.
</output>
