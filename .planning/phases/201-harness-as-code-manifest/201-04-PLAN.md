---
phase: 201-harness-as-code-manifest
plan: 04
type: execute
wave: 3
depends_on: [201-02, 201-03]
files_modified:
  - evals/plurai/08-ralph-loop-behavior.csv
  - evals/plurai/README.md
  - evals/plurai/201-baseline.json
  - lib/core/ralph-loop-gate.cjs
  - tests/test-201-ralph-loop-gate.cjs
  - tests/run-all-201.sh
autonomous: true
requirements: [SEED-033-eval-gate, REUSE-196, CANON-Part8]
must_haves:
  truths:
    - "A Plurai judge classifies a loop trace as correct or incorrect against the loop contract: retry-until-pass on safe steps, halt-at-material, verified-writes-only, bounded/converged"
    - "A local deterministic gate reproduces the judge offline; its parity fixtures are distilled from the labeled CSV rows (196 pattern)"
    - "The gate encodes the invariants as hard checks (a trace that retries a MATERIAL step is always incorrect; an unbounded trace is always incorrect) - not a data-swept threshold"
    - "Plurai is offline/CI-only, synthetic-only; the baseline degrades to hand-labeled + baseline_deferred if unreachable (196 degrade path)"
  artifacts:
    - path: "lib/core/ralph-loop-gate.cjs"
      provides: "classifyLoopTrace(trace) -> { label: correct|incorrect, violations: [] }"
      exports: ["classifyLoopTrace"]
    - path: "evals/plurai/201-baseline.json"
      provides: "persisted precision/recall baseline artifact"
      contains: "baseline_deferred"
  key_links:
    - from: "lib/core/ralph-loop-gate.cjs"
      to: "evals/plurai/08-ralph-loop-behavior.csv"
      via: "the labeled CSV rows are the parity fixtures the local gate must reproduce"
      pattern: "plurai-local-parity"
---

<objective>
Regression-lock the two Ralph behaviors (L1 bounded retry, L2 graph-refine) with a Plurai
eval gate, reusing the Phase 196 synthetic-CSV -> offline-baseline -> local-parity pattern.
The judge answers: is this loop trace CORRECT against the contract (retry-only-safe-steps,
halt-at-material, verified-writes-only, bounded)? The local gate reproduces that verdict
deterministically and offline so nothing calls Plurai at runtime (Part 8).
</objective>

## Global constraints

- Plurai OFFLINE via the evals harness, BUILD/CI only, synthetic traces only.
- CJS/JSON only; no new npm packages.
- Degrade to hand-labeling + `baseline_deferred: true` if the endpoint is unreachable.

## Task 1: Synthetic labeled CSV of loop traces

**Files:**
- Create: `evals/plurai/08-ralph-loop-behavior.csv`
- Modify: `evals/plurai/README.md`

- [ ] Step 1: Author `08-ralph-loop-behavior.csv` (`Sample,Label,Reasoning`, ~24 rows). Each Sample is a compact JSON loop trace. Half `correct` (safe-step retried then passed; material step halted; verified-only writes; bounded). Half `incorrect` (retried a material step; unbounded loop; wrote an unverified edge; proceeded silently after cap exhaustion).
- [ ] Step 2: Add the README task row mapping this CSV to the SEED-033 contract + judge prompt.
- [ ] Step 3: Commit `feat(201-04): synthetic Ralph-loop behavior eval CSV`.

## Task 2: Local deterministic loop gate

**Files:**
- Create: `lib/core/ralph-loop-gate.cjs`
- Test: `tests/test-201-ralph-loop-gate.cjs`

**Interfaces:**
- Produces: `classifyLoopTrace(trace) -> { label, violations }`. Hard-check violations: `retried_material_step`, `unbounded`, `unverified_write`, `silent_proceed_after_cap`. Any violation -> `incorrect`.

- [ ] Step 1: Write the failing parity test: load `08-ralph-loop-behavior.csv`, run `classifyLoopTrace` on each row, assert the local label matches the CSV `Label`. Add an explicit test that a trace retrying a material step is `incorrect` regardless of any options (the invariant cannot be disabled).
- [ ] Step 2: Run it, verify it fails (module not defined).
- [ ] Step 3: Implement `classifyLoopTrace` with the four hard checks. No data-swept threshold.
- [ ] Step 4: Run the parity test, verify pass on all rows.
- [ ] Step 5: Commit `feat(201-04): local Ralph-loop gate with CSV parity`.

## Task 3: Offline baseline + aggregator

**Files:**
- Create: `evals/plurai/201-baseline.json`
- Create: `tests/run-all-201.sh`
- Modify: `tests/test-201-ralph-loop-gate.cjs`

- [ ] Step 1: Run the Plurai baseline via the evals harness (offline via uv). Persist precision/recall to `201-baseline.json` if it runs; else hand-label + `baseline_deferred: true`.
- [ ] Step 2: Add a test asserting the baseline exists with precision/recall keys and the local gate meets-or-beats it (or is 100% on hand-labeled rows if deferred).
- [ ] Step 3: Author `tests/run-all-201.sh` aggregating test-201-harness-manifest, test-201-bounded-retry, test-201-graph-refine-loop, test-201-ralph-loop-gate (run_if/SKIP wave-0 contract, model on run-all-196.sh).
- [ ] Step 4: Run `bash tests/run-all-201.sh` -> all green.
- [ ] Step 5: Commit `feat(201-04): Ralph-loop Plurai baseline + run-all-201 aggregator`.

## Self-review

- Reuse-196: same synthetic-CSV -> baseline -> local-parity pattern; no new harness (Part 7).
- Part 8: Plurai synthetic/offline; runtime gate is local + deterministic.
- The invariants (no material retry, bounded, verified-only) are hard checks, not swept thresholds.
