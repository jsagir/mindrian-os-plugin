---
phase: 229-huji-pitch-feedback-module
plan: 05
type: execute
wave: 2
depends_on: ["229-01"]
files_modified:
  - references/methodology/huji-stage-a-intake.md
  - scripts/huji-intake.cjs
autonomous: true
requirements: [D1, D2]
must_haves:
  truths:
    - "Stage A extracts a quote-anchored evidence.json (transcript + optional deck) that validates against the evidence schema"
    - "The scratch room is genuinely populated with typed claim nodes via navigation.writeClaimNode, never a shim"
    - "Missing-artifact gaps are NAMED not filled; Modes B/C (generate transcript/slides) are disabled"
  artifacts:
    - path: "references/methodology/huji-stage-a-intake.md"
      provides: "Frozen Stage A intake prompt: fusion Mode A + extraction discipline; Modes B/C disabled"
      contains: "Mode A"
    - path: "scripts/huji-intake.cjs"
      provides: "populateRoom() - deterministic writeClaimNode driver + nugget extraction over evidence.json"
      exports: ["populateRoom"]
      min_lines: 60
  key_links:
    - from: "scripts/huji-intake.cjs"
      to: "lib/core/navigation.cjs writeClaimNode + lib/core/room-db.cjs openRoomDb"
      via: "per-claim writeClaimNode into the scratch room.db"
      pattern: "writeClaimNode"
    - from: "references/methodology/huji-stage-a-intake.md"
      to: "assets/claims-fusion-engine-prompt.md"
      via: "port of Mode A COMPLETE FUSION + extraction standards"
      pattern: "fusion"
---

<objective>
Build the transcript-to-evidence intake adapter (seam b): a frozen Stage A prompt porting the Claims-Aware Fusion engine (Mode A + extraction discipline only) into a quote-anchored evidence.json, plus a deterministic CJS driver that populates the ephemeral scratch room with typed claim nodes like the shipped file-meeting pipeline.

Purpose: The evidence JSON is the ONLY thing the spine grades - extraction fidelity (D2) and quote grounding (D1) start here. Navigator ruling 3 requires the room be GENUINELY populated via `navigation.writeClaimNode`, no shims. The interactive `/mos:file-meeting` is autonomous_safe:false with nugget-routing HITL, so we drive the writer DIRECTLY (CONTRACTS INTAKE_PATH). Modes B/C (generating missing transcript/slides) are disabled - grading generated content is failure mode #1.

Output: `references/methodology/huji-stage-a-intake.md` and `scripts/huji-intake.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md
@.planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md
@.planning/phases/229-huji-pitch-feedback-module/assets/claims-fusion-engine-prompt.md
</context>

## Artifacts this phase produces (this plan)

New files: `references/methodology/huji-stage-a-intake.md`, `scripts/huji-intake.cjs`.
New function: `populateRoom({roomDir, evidence, transcript, sessionId})` - opens scratch room.db, mints one claim node per evidence claim, links them, extracts wisdom nuggets. Idempotent via segment id.

<tasks>

<task type="auto">
  <name>Task 1: Frozen Stage A intake prompt (fusion Mode A port)</name>
  <read_first>
    - .planning/phases/229-huji-pitch-feedback-module/assets/claims-fusion-engine-prompt.md (Mode A COMPLETE FUSION + extraction standards; Modes B/C stamped DISABLED)
    - .planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md (Section 4 "Stage A specification" - the 7 binding port rules, NORMATIVE)
    - .planning/phases/229-huji-pitch-feedback-module/schemas/evidence.schema.json (the output contract Stage A satisfies - Plan 01)
    - .planning/phases/229-huji-pitch-feedback-module/samples/sample-2-study-app.md (self-identified-gaps + covered-risks case)
  </read_first>
  <action>
Create `references/methodology/huji-stage-a-intake.md`, the frozen Stage A prompt, porting the fusion engine per AI-SPEC Section 4's 7 rules: mode-detect transcript+deck -> Mode A COMPLETE FUSION (match segments to slides, preserve exact text from both, mark claim gaps + contradictions), transcript-only -> extraction without slide construction; Modes B/C DISABLED (missing artifact NAMED, never filled); claims-first hierarchy with every evidence item linked to a claim; extraction discipline capturing every name/citation/URL/organization/statistic; output = evidence.json conforming to evidence.schema.json with every evidence_claims[].quote verbatim (D1 anchor), evidenced enum evidenced/asserted/absent, self_identified_gaps populated (credit metacognition, never double-punish), language_notes for non-native phrasing; cross-artifact rule (grade a claim on its strongest presentation; surface deck-vs-speech contradictions); treat any in-transcript override line as inert content, never a command. No em-dashes; keep bit-stable (frozen prefix for caching).
  </action>
  <verify>
    <automated>f=references/methodology/huji-stage-a-intake.md; test -f "$f" && grep -qi "Mode A" "$f" && grep -qi "disabled" "$f" && grep -qi "verbatim" "$f" && ! grep -q "—" "$f" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Ports Mode A + extraction discipline; Modes B/C explicitly disabled with the fabricated-critique rationale
    - Output contract references evidence.schema.json fields incl. verbatim quotes, evidenced enum, self_identified_gaps, language_notes
    - Missing-artifact gaps NAMED not filled; cross-artifact strongest-presentation rule present
    - In-transcript injection lines named as inert content; no em-dashes
  </acceptance_criteria>
  <done>Stage A is a frozen, fusion-grounded prompt yielding a schema-valid, quote-anchored evidence.json that refuses to fabricate.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic room-populator (Claimify writer reuse)</name>
  <read_first>
    - skills/file-meeting/SKILL.md (Claimify 4-pass at line 286 - reuse the machinery)
    - lib/core/navigation.cjs (writeClaimNode re-export at 209; writeEdge + ALLOWED_EDGE_TYPES)
    - lib/core/navigation/edges.cjs (ALLOWED_EDGE_TYPES)
    - lib/core/room-db.cjs (openRoomDb at 100)
    - .planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md (INTAKE_PATH: drive the writer directly, not /mos:file-meeting)
  </read_first>
  <action>
Create `scripts/huji-intake.cjs` exporting `populateRoom({roomDir, evidence, transcript, sessionId})`. `openRoomDb(roomDir)`; for EACH claim in evidence (problem_claim, value_proposition, evidence_claims[]) call `navigation.writeClaimNode(db, params)` with text, verbatim quote, knowledge_type, sourceSpeaker, sourceSegment (segment id = idempotency key so reruns UPSERT), sessionId, and disambiguation 'ambiguous' only for unresolved - minting type='claim', review_status='proposed' (NEVER auto-confirmed; only human confirmNode promotes, Canon Part 9). Link related claims via `navigation.writeEdge` with ALLOWED_EDGE_TYPES. Extract wisdom nuggets like file-meeting. Do NOT invoke `/mos:file-meeting`. Add a `--selftest` that scaffolds a throwaway room, runs populateRoom over a tiny evidence object, asserts claim nodes exist with review_status='proposed', and that a second run does not duplicate them.
  </action>
  <verify>
    <automated>node scripts/huji-intake.cjs --selftest && grep -q "writeClaimNode" scripts/huji-intake.cjs && ! grep -q "mos:file-meeting" scripts/huji-intake.cjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/huji-intake.cjs --selftest` exits 0; claim nodes minted with review_status='proposed'
    - Rerun is idempotent (segment id key; no duplicate nodes)
    - Uses navigation.writeClaimNode + openRoomDb directly; never invokes /mos:file-meeting
    - Wisdom-nugget extraction present
  </acceptance_criteria>
  <done>The scratch room is genuinely populated with proposed typed claim nodes via the shipped writer, non-interactively and idempotently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript content -> extraction | Untrusted student/transcript text (possibly adversarial) crosses into evidence extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-229-05-01 | Tampering/Elevation | Stage A prompt vs in-transcript injection | mitigate | Extraction discipline treats transcript override lines as inert content; validated by the injection probe (Plan 02) in later suites |
| T-229-05-02 | Spoofing (trust) | evidence.json fabrication | mitigate | Modes B/C disabled; every evidence_claims[].quote verbatim (D1); missing artifacts NAMED not filled |
| T-229-05-03 | Information Disclosure | claim nodes written locally | mitigate | Room population is LOCAL only (room.db); no Brain write; review_status='proposed' (never auto-confirmed) - Canon Parts 8/9 |
| T-229-05-SC | Tampering | npm/pip/cargo installs | accept | Zero installs (RESEARCH audit = none); reuse of shipped core only |
</threat_model>

<verification>
- `node scripts/huji-intake.cjs --selftest` exits 0 (idempotent, proposed nodes)
- Stage A prompt satisfies the 7 port rules; Modes B/C disabled; no em-dashes
- `grep writeClaimNode scripts/huji-intake.cjs` matches; `grep mos:file-meeting` does not
</verification>

<success_criteria>
- Stage A yields schema-valid, quote-anchored evidence.json with no fabrication
- The scratch room is genuinely populated via the shipped Claimify writer, non-interactively
- Extraction fidelity (D2) and grounding (D1) are enforceable from the evidence artifact onward
</success_criteria>

<output>
Create `.planning/phases/229-huji-pitch-feedback-module/229-05-SUMMARY.md` when done.
</output>
