---
phase: 229-huji-pitch-feedback-module
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/huji-run-one-async.cjs
  - lib/memory/huji-run-one-async-parity.test.cjs
  - tests/run-all-229.sh
  - scripts/huji-pin-smoketest.cjs
  - .planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md
autonomous: true
requirements: [D14]

must_haves:
  truths:
    - "An async-safe entry point (runOneAsync) exists that never calls the blocking spawnSync primitive, so a long-lived Node event-loop process (an MCP daemon, whether in this repo or an external wrapper) can call it without stalling other concurrent work for the full 8-12 minute Stage A/B duration."
    - "runOneAsync's failure envelope is provably identical in shape (ok, reason, detail) to runOne's failure envelope for the same injected non-zero-exit failure at both Stage A and Stage B, so external error-handling code written against one function's contract is never surprised by the other."
    - "The existing shipped batch orchestrator (scripts/huji-batch.cjs) is unaffected: it still imports only the synchronous runOne, never the new async twin, and scripts/huji-run-one.cjs itself is byte-for-byte unmodified."
    - "The public contract of runOneAsync (its parameters and the exhaustive set of return-shape reason codes) is documented in-source so a future edit cannot silently break it without a maintainer being told this is a stability contract external repos rely on."
    - "The claim that a git-tag-pinned checkout of this repo, loaded via `claude --plugin-dir` from OUTSIDE this repo's own working directory, resolves `/mos:pipeline PWS_grading` is verified against a REAL invocation, not assumed - and the specific tag (v1.15.2) already written into the separate mindrian-pitch-feedback-mcp repo's README is checked against the real git history rather than trusted at face value."
  artifacts:
    - path: "scripts/huji-run-one-async.cjs"
      provides: "runOneAsync (CASCADE-06 async twin of runOne) + runClaudeAsync helper"
      exports: ["runOneAsync", "runClaudeAsync"]
    - path: "lib/memory/huji-run-one-async-parity.test.cjs"
      provides: "D14 CI-blocking parity gate between runOne and runOneAsync failure envelopes"
    - path: "scripts/huji-pin-smoketest.cjs"
      provides: "repeatable tag-resolution + live claude --plugin-dir smoke test"
    - path: ".planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md"
      provides: "recorded, non-hypothetical findings from the live smoke-test run, including the v1.15.2 verification"
  key_links:
    - from: "lib/memory/huji-run-one-async-parity.test.cjs"
      to: "scripts/huji-run-one-async.cjs"
      via: "require + AsyncFunction/envelope-shape assertions"
      pattern: "require\\(.*huji-run-one-async"
    - from: "scripts/huji-run-one-async.cjs"
      to: "scripts/huji-run-one.cjs"
      via: "reuse-before-build require of exported helpers (no re-implementation)"
      pattern: "require\\(['\"]\\./huji-run-one\\.cjs['\"]\\)"
    - from: "tests/run-all-229.sh"
      to: "lib/memory/huji-run-one-async-parity.test.cjs"
      via: "run_if guarded leg"
      pattern: "huji-run-one-async-parity"
    - from: "scripts/huji-pin-smoketest.cjs"
      to: "claude CLI (--plugin-dir)"
      via: "spawnSync/execFile live invocation against a cloned tag"
      pattern: "--plugin-dir"
---

<objective>
Make the ONLY thing that must be true inside MindrianOS-Plugin for the separate,
standalone `mindrian-pitch-feedback-mcp` repo to safely wrap this plugin's grading
engine actually true: (1) an MCP-daemon-safe async twin of `runOne` exists and is
provably interchangeable with the sync original on the failure path (D14), (2) that
twin's contract is documented as stable so a future release cannot silently break
an external caller, and (3) the git-tag-pin mechanism the external repo depends on
(`claude --plugin-dir <tag-pinned-checkout>` resolving `/mos:pipeline PWS_grading`
from outside this repo's own working directory) is verified against a REAL
invocation, not assumed.

This plan does NOT build the MCP server, tool registration, job registry, or
guardrails G7-G10 - those belong entirely to the separate repo's own future
planning cycle (navigator decision, 2026-07-16, recorded in that repo's README
and in this phase's 229-AI-SPEC.md Sections 3/4). It also does not touch or
reopen any file shipped by 229-01 through 229-09 (all gate-clean on main);
`scripts/huji-run-one.cjs` in particular is read-only ground truth for this plan.

Purpose: close the one real, already-confirmed bug (spawnSync inside a would-be
long-lived event-loop process blocks the whole process, CASCADE-06 pattern) and
the one real, already-suspected infrastructure risk (the external repo's README
names v1.15.2 as its pin tag; that claim has never been checked against this
repo's actual tag history) before either becomes load-bearing for a real customer
batch.

Output: `scripts/huji-run-one-async.cjs` (new), `lib/memory/huji-run-one-async-parity.test.cjs`
(new, D14 CI gate), a new guarded leg in `tests/run-all-229.sh`, `scripts/huji-pin-smoketest.cjs`
(new), and `.planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md`
(new, real findings from a real invocation).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md
@.planning/phases/229-huji-pitch-feedback-module/229-PATTERNS.md
@.planning/phases/229-huji-pitch-feedback-module/DR-FRAMEWORK-ARCHITECTURE-DECISION.md
@lib/core/room-ops-async.cjs
@lib/core/room-ops-sync.cjs
@lib/memory/sync-async-entry-points.test.cjs
@scripts/huji-run-one.cjs
@lib/core/pitch-feedback-schemas.cjs

Scope note (supersedes 229-AI-SPEC.md Section 3/4's `lib/mcp/pitch-feedback-tool.cjs`
sketch): the navigator moved MCP tool registration, the job registry, and
guardrails G7-G10 into a separate repo (`github.com/jsagir/mindrian-pitch-feedback-mcp`,
pinned to a MindrianOS-Plugin git tag via `MINDRIAN_OS_ROOT`, currently documenting
v1.15.2 as that pin). This plan builds only the async engine twin and its stability
contract, plus a real verification of the pin mechanism - never the MCP surface
itself. Verified live during planning: `v1.15.2` does NOT contain the `PWS_grading`
recipe (`git show v1.15.2:lib/core/recipe-maps.cjs | grep -c PWS_grading` = 0); the
recipe first appears at `v1.15.3-beta.22` and is present through the newest tag,
`v1.15.3-beta.26`. No stable (non-beta) tag contains it yet - that gap is exactly
what 229-09's still-pending Task 3 ("git-tag the checkout before the first
200-student batch") exists to close, and this plan does not duplicate that action.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scripts/huji-run-one-async.cjs (CASCADE-06 async twin + stable contract)</name>
  <files>scripts/huji-run-one-async.cjs</files>
  <read_first>
    scripts/huji-run-one.cjs (full file - the sync engine being ported; every helper
    this task needs is already exported: scaffoldScratchRoom, buildStageAPrompt,
    buildStageAArgs, buildStageBArgs, parseEnvelope, renderFeedbackMarkdown,
    runGuardrails, resolveConfig, MAX_FEEDBACK_WORDS)
    scripts/huji-intake.cjs (populateRoom's signature - called identically in the
    async twin)
    lib/core/room-ops-async.cjs (the CASCADE-06 shape to mirror: file-header
    contract-note style, execFileAsync = util.promisify(execFile), per-function JSDoc)
    lib/core/room-ops-sync.cjs (the sync sibling's own header language, for the
    "NEVER call from..." cross-reference note)
  </read_first>
  <behavior>
    - runClaudeAsync(args, opts) on a spawned process exiting 0: returns
      { status: 0, stdout: <captured stdout>, stderr: <captured stderr>, error: null }.
    - runClaudeAsync(args, opts) on a spawned process exiting non-zero (execFile
      REJECTS the promise): returns { status: <numeric exit code>, stdout: <captured
      stdout from the rejection>, stderr: <captured stderr from the rejection>,
      error: null } - the rejection is caught internally and never propagates out of
      runClaudeAsync as an uncaught exception.
    - runClaudeAsync(args, opts) on a spawn killed by the timeout option: returns
      { status: 1 (or the numeric code if present), stdout, stderr, error: <the
      caught Error, non-null> } - mirrors spawnSync's populated `.error` field on
      spawn failure/timeout.
    - runOneAsync(opts) given the same synthetic non-zero-exit fixture at the Stage A
      spawn point that runOne(opts) receives: returns a { ok, reason, detail } triple
      structurally identical to runOne's for that fixture (reason: 'stageA_nonzero',
      detail containing the same stderr content, ok: false).
  </behavior>
  <action>
    Create scripts/huji-run-one-async.cjs as a NEW sibling file to
    scripts/huji-run-one.cjs. Do NOT modify huji-run-one.cjs itself (229-07's shipped,
    gate-clean file stays untouched - reuse before build, Canon Part 7). Require
    huji-run-one.cjs's already-exported helpers (scaffoldScratchRoom, buildStageAPrompt,
    buildStageAArgs, buildStageBArgs, parseEnvelope, renderFeedbackMarkdown,
    runGuardrails, resolveConfig) and huji-intake.cjs's populateRoom. Implement an
    internal helper runClaudeAsync(args, opts) using node:child_process execFile
    wrapped via node:util.promisify, reconstructing runOne's {status, stdout, stderr,
    error} shape exactly per the behavior block above (execFile REJECTS on non-zero
    exit; spawnSync RETURNS a .status field - this is the one translation point).
    Implement the exported async function runOneAsync(opts) that mirrors runOne(opts)
    line for line: identical input validation and reason codes (invalid_subId,
    invalid_transcriptPath, invalid_outDir), identical scaffoldScratchRoom call and
    scaffold_failed reason code, identical Stage A build (buildStageAPrompt +
    buildStageAArgs) and identical reason codes (stageA_spawn_failed, stageA_nonzero,
    stageA_unparseable_evidence) but calling `await runClaudeAsync(...)` instead of
    spawnSync, identical evidence.json write + populateRoom call (wrapped in the same
    try/catch-and-ignore runOne uses), identical Stage B build (buildStageBArgs) and
    identical reason codes (stageB_spawn_failed, stageB_nonzero,
    stageB_unparseable_result) via `await runClaudeAsync(...)`, identical feedback.md
    render + result.json write, identical runGuardrails call and .done-marker
    discipline (write .done only when gate.ok && feedbackNonEmpty), and an identical
    final return shape {ok, gate_failures, cost, roomDir, outDir, evidencePath,
    feedbackPath, resultPath, donePath}. Add exactly one new parameter beyond runOne's:
    an optional onStage(stageName) callback invoked with 'stageA' immediately before
    the Stage A spawn and 'stageB' immediately before the Stage B spawn (a no-op
    default when the caller supplies nothing) - forward-compatible plumbing for a
    future external progress-reporting caller; wrap every onStage call in its own
    try/catch so a throwing callback can never alter control flow or fail the run.
    Do not implement any MCP registration, job registry, or progress-notification
    transport in this file - those are out of scope. Author a file-header docblock
    mirroring room-ops-async.cjs's own style, explicitly naming this file as the
    CASCADE-06 (Phase 87-04 precedent) async twin and stating the contract-stability
    rule in prose: the function signature runOneAsync(opts) and its exhaustive set of
    return-shape reason codes (invalid_subId, invalid_transcriptPath, invalid_outDir,
    scaffold_failed, stageA_spawn_failed, stageA_nonzero, stageA_unparseable_evidence,
    stageB_spawn_failed, stageB_nonzero, stageB_unparseable_result) are the stable
    public contract external callers (e.g. the separate mindrian-pitch-feedback-mcp
    repo, pinned to a MindrianOS-Plugin git tag) depend on; changing this shape is a
    breaking change requiring an explicit coordination note, never a silent edit.
    Add full JSDoc (@param, @returns, @typedef for the return shape) on runOneAsync
    and runClaudeAsync at the same density already used in room-ops-async.cjs and
    lib/core/pitch-feedback-schemas.cjs. Export { runOneAsync, runClaudeAsync }. Zero
    em-dashes (CLAUDE.md hard rule). CJS only.
  </action>
  <verify>
    <automated>node -e "const m = require('./scripts/huji-run-one-async.cjs'); if (m.runOneAsync.constructor.name !== 'AsyncFunction') { console.error('runOneAsync is not an AsyncFunction'); process.exit(1); } if (typeof m.runClaudeAsync !== 'function') { console.error('runClaudeAsync missing'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    scripts/huji-run-one-async.cjs exists, exports runOneAsync as an AsyncFunction and
    runClaudeAsync, requires only huji-run-one.cjs's exported helpers plus
    huji-intake.cjs's populateRoom (no new npm dependency, no re-implementation of
    Stage A/B argument-building or guardrail logic), and scripts/huji-run-one.cjs
    itself is unmodified.
  </done>
  <acceptance_criteria>
    - `node -e "console.log(require('./scripts/huji-run-one-async.cjs').runOneAsync.constructor.name)"` prints `AsyncFunction`.
    - `git diff --stat scripts/huji-run-one.cjs` produces no output (file untouched).
    - `grep -c "spawnSync" scripts/huji-run-one-async.cjs` returns 0.
    - `grep -c "execFile" scripts/huji-run-one-async.cjs` returns >= 1.
    - `grep -Ec "stageA_nonzero|stageB_nonzero|stageA_unparseable_evidence|stageB_unparseable_result" scripts/huji-run-one-async.cjs` returns >= 4.
    - `grep -c "^" scripts/huji-run-one-async.cjs | awk '{print ($1>0)}'` returns 1 (non-empty file); `node -c scripts/huji-run-one-async.cjs` reports no syntax errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D14 parity test (runOne vs runOneAsync failure envelope) + wire into run-all-229.sh</name>
  <files>lib/memory/huji-run-one-async-parity.test.cjs, tests/run-all-229.sh</files>
  <read_first>
    lib/memory/sync-async-entry-points.test.cjs (full file - the CASCADE-06 test SHAPE
    to mirror: node:assert/strict usage, the grepCount helper pattern, top-level
    async run().catch() structure, process.exit(0)/1 convention)
    scripts/huji-run-one.cjs (runOne's full signature + reason codes - the fixture
    target)
    scripts/huji-run-one-async.cjs (Task 1's output - runOneAsync's signature)
    tests/run-all-229.sh (full file - the run_if leg convention: label string, guard
    file, node command, PASS/FAIL/SKIP counters)
  </read_first>
  <action>
    Create lib/memory/huji-run-one-async-parity.test.cjs as the D14 CI-blocking
    regression gate (229-AI-SPEC.md Section 5, dimension D14), mirroring
    lib/memory/sync-async-entry-points.test.cjs's shape (top-level async run()
    function, node:assert/strict, process.exit(0) on success / process.exit(1) with
    the error stack on failure). Implement four checks in order. (1) File-existence +
    AsyncFunction assertion: assert scripts/huji-run-one.cjs and
    scripts/huji-run-one-async.cjs both exist; require the async twin and assert
    runOneAsync.constructor.name === 'AsyncFunction'. (2) Caller-side audit, scoped to
    this phase's real callers (lib/mcp/* has no pitch-feedback consumer in this repo
    per this plan's scope note - do not assume one exists): grep
    scripts/huji-batch.cjs for a require of huji-run-one.cjs (must be present at
    least once) and assert it does NOT require huji-run-one-async.cjs (count must be
    0), matching 229-AI-SPEC.md Section 4's "scripts/huji-batch.cjs keeps requiring
    the sync runOne unchanged" rule. Guard any lib/mcp/ grep with an fs.existsSync
    check the same defensive way sync-async-entry-points.test.cjs's grepCount already
    guards missing directories; if lib/mcp/ exists, assert zero files there require
    scripts/huji-run-one.cjs directly (future-proofing against a same-repo MCP
    consumer bypassing the async contract, even though none exists today). (3) The
    D14 parity fixture: build a temporary executable script named `claude` in a fresh
    fs.mkdtempSync directory (fs.chmodSync 0o755), branching on whether its own argv
    contains the literal string '/mos:pipeline PWS_grading' (Stage B signal) or not
    (Stage A), and on two env vars (e.g. HUJI_FAKE_CLAUDE_STAGEA_EXIT,
    HUJI_FAKE_CLAUDE_STAGEB_EXIT) to decide whether to print a fixed marker string to
    stderr and exit non-zero, or print a minimal `--output-format json` envelope
    (structured_output satisfying EvidenceSchema's/FeedbackResultSchema's minimum
    required fields) and exit 0. Prepend the temp directory to process.env.PATH for
    the test's duration (save/restore the original PATH in a finally block so nothing
    leaks). Run FIXTURE-A (HUJI_FAKE_CLAUDE_STAGEA_EXIT=7): call both runOne (fresh
    require from scripts/huji-run-one.cjs) and runOneAsync (fresh require from
    scripts/huji-run-one-async.cjs) with matching opts (a real transcript file
    written into the temp dir, distinct outDir/subId per call to avoid collision) and
    assert both results have ok===false, reason==='stageA_nonzero', detail containing
    the fixture's stderr marker. Run FIXTURE-B (unset STAGEA_EXIT so Stage A succeeds,
    HUJI_FAKE_CLAUDE_STAGEB_EXIT=9) and assert both results have ok===false,
    reason==='stageB_nonzero', detail containing the Stage B marker - this is the
    concrete state-machine proof 229-AI-SPEC.md D14 names: "stub the child-process
    primitive to exit non-zero with fixed stdout/stderr for both runOne and
    runOneAsync, run both, assert the two returned objects are structurally
    identical." (4) After the checks pass, add a new guarded leg to
    tests/run-all-229.sh immediately after the existing D10 kill/resume leg (before
    the closing PASS/FAIL/SKIP summary block), following the exact run_if pattern
    every other leg uses: label 'REQ-D14 async/sync exit-code parity: runOneAsync
    failure envelope matches runOne [execFile regression guard]', guard file
    scripts/huji-run-one-async.cjs, command `node
    lib/memory/huji-run-one-async-parity.test.cjs`. Do NOT register this test in
    lib/memory/run-feynman-tests.cjs (that ~2000-line registry is the general
    cross-phase Feynman-MINTO suite; Phase 229's own tests are wired exclusively
    through tests/run-all-229.sh's run_if legs - follow that phase-local convention,
    not the general one). Zero em-dashes.
  </action>
  <verify>
    <automated>node lib/memory/huji-run-one-async-parity.test.cjs && bash tests/run-all-229.sh 2>&1 | tail -5</automated>
  </verify>
  <done>
    lib/memory/huji-run-one-async-parity.test.cjs exists and exits 0 when run
    directly, proving (a) file existence, (b) runOneAsync is an AsyncFunction, (c)
    scripts/huji-batch.cjs still requires only the sync runOne, (d) runOne and
    runOneAsync produce structurally identical {ok, reason, detail} envelopes for an
    injected Stage A failure AND an injected Stage B failure. tests/run-all-229.sh
    contains a new run_if leg invoking this test file and `bash tests/run-all-229.sh`
    still exits with FAIL=0.
  </done>
  <acceptance_criteria>
    - `node lib/memory/huji-run-one-async-parity.test.cjs` exits 0.
    - `grep -c "huji-run-one-async-parity" tests/run-all-229.sh` returns >= 1.
    - `bash tests/run-all-229.sh 2>&1 | grep -E "^Phase 229: PASS="` shows `FAIL=0`.
    - `grep -c "huji-run-one-async.cjs" scripts/huji-batch.cjs` returns 0.
    - `grep -c "huji-run-one.cjs" scripts/huji-batch.cjs` returns >= 1.
    - `grep -c "run-feynman-tests" lib/memory/huji-run-one-async-parity.test.cjs` returns 0 (this test is not self-registering into the unrelated general registry).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Real end-to-end pin smoke test (claude --plugin-dir resolves PWS_grading from outside this repo)</name>
  <files>scripts/huji-pin-smoketest.cjs, .planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md</files>
  <read_first>
    .planning/phases/229-huji-pitch-feedback-module/DR-FRAMEWORK-ARCHITECTURE-DECISION.md
    (Section 5's "plugin-version-pinning recommendation... git-tag the checkout
    before the real 200-student batch" - this task's finding directly informs that
    still-pending 229-09 Task 3 action; do not duplicate the tag-cutting itself, only
    prove or disprove the mechanism)
    lib/core/active-plugin-root.cjs lines 1-60 (the isPluginDir validity signal -
    presence of .claude-plugin/plugin.json - reused here as the clone-sanity check;
    this task resolves a DIFFERENT thing, a tag-pinned checkout consumed by the
    `claude` CLI's own --plugin-dir flag, not this repo's own install resolution)
    lib/core/recipe-maps.cjs (grep for PWS_grading to confirm the exact key/shape the
    recipe registration uses, for the pre-clone tag-verification step)
  </read_first>
  <action>
    Create scripts/huji-pin-smoketest.cjs, a standalone CJS script NOT wired into
    tests/run-all-229.sh's fast deterministic legs (it clones a real git tag and
    spawns a real, live `claude` CLI process - matching the existing precedent of
    huji-eval.cjs's `--suite anchors --judge` / `--suite demo` legs being separately
    invoked, model-calling, and excluded from the fast aggregator). The script does,
    in order: (1) Tag resolution - list local git tags matching v1.15.3-beta.* and
    any v1.16*/stable tag, sorted by version; for each candidate newest-to-oldest,
    `git show <tag>:lib/core/recipe-maps.cjs` and grep for the literal string
    PWS_grading; select the NEWEST tag where it is present as the resolved pin tag
    (accept an optional --tag <name> CLI override that skips auto-detection).
    Explicitly ALSO check tag v1.15.2 (the tag currently documented as the pin in the
    separate mindrian-pitch-feedback-mcp repo's README) the same way, and record in
    the findings output whether v1.15.2 contains PWS_grading or not - verify this
    live against this repo's actual git history (already confirmed present locally),
    never assume the answer. (2) If no candidate tag contains PWS_grading, fail
    closed: print a clear FAIL explaining no released tag (including v1.15.2) carries
    the recipe yet, write that finding to the output artifact, exit 1 without
    attempting any clone or live claude call. (3) If a candidate is found, shallow-clone
    it (git clone --branch &lt;tag&gt; --depth 1 &lt;this repo's own path or the
    origin remote URL&gt; &lt;a fresh mkdtemp directory OUTSIDE this repo, under
    os.tmpdir()&gt;) and verify the clone contains .claude-plugin/plugin.json before
    proceeding. (4) From a THIRD, separate empty scratch directory (also under
    os.tmpdir(), distinct from both this repo and the clone, so nothing about cwd can
    accidentally auto-discover this repo's own plugin), spawn `claude` with an args
    array (no shell): -p, '/mos:pipeline PWS_grading', --plugin-dir, &lt;clone
    path&gt;, --model, claude-haiku-4-5 (cheapest tier - this call only needs to
    prove command resolution, never grading quality), --output-format, json,
    --permission-mode, dontAsk, --allowedTools, '' (empty - no tool execution
    capability, so nothing beyond the model call itself can spend), --max-turns, 3,
    --max-budget-usd, 0.10, --no-session-persistence, with a spawnSync timeout of
    60000ms as a hard wall-clock backstop. If the spawn itself fails because no
    credentials are configured in this environment, classify the outcome as
    UNRESOLVED with reason 'auth_unavailable' rather than throwing or hanging. (5)
    Parse the JSON envelope's `result` text and `is_error` field: classify RESOLVED
    when the response shows the command was recognized and the plugin's PWS_grading
    chain began executing (references deep-grade, mullins, PWS_grading, or a
    venture-stage/room-context gating message - all of which prove routing worked
    even if grading itself is skipped for lack of a real room), versus UNRESOLVED
    when the response reads as plain conversational non-recognition of the slash
    command, or is_error is true with a plugin-load-failure message. (6) Write the
    full findings - resolved tag, whether v1.15.2 specifically contains PWS_grading,
    the clone path used, the raw claude invocation args, the classified outcome, and
    an explicit correction note when v1.15.2 is confirmed stale - to
    .planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md. (7) Exit
    0 only when a tag was found AND the live invocation classified as RESOLVED; exit
    1 otherwise, with the failure reason printed to stderr. Then actually RUN this
    script for real against this repo's real git tags and a real `claude` CLI call
    (both confirmed available in this environment: claude CLI 2.1.211, origin remote
    reachable, /mos:pipeline command shipped) - do not hand-write or fabricate the
    findings file's content; whatever the script's real output is, that is what
    229-10-PIN-SMOKETEST.md must contain, including an honest UNRESOLVED verdict if
    that is what actually happens. Zero em-dashes.
  </action>
  <verify>
    <automated>node scripts/huji-pin-smoketest.cjs; test -f .planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md && echo FINDINGS_FILE_PRESENT</automated>
  </verify>
  <done>
    scripts/huji-pin-smoketest.cjs has been executed for real against this repo's
    actual git tags and a real `claude --plugin-dir` invocation from a scratch
    directory outside this repo;
    .planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md exists
    and records the resolved tag, the live v1.15.2/PWS_grading finding, and a
    RESOLVED/UNRESOLVED verdict for the invocation - not a hypothetical or
    hand-written result.
  </done>
  <acceptance_criteria>
    - `node -e "require('fs').accessSync('scripts/huji-pin-smoketest.cjs')"` does not throw.
    - Running `node scripts/huji-pin-smoketest.cjs` produces
      .planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md
      containing the literal string `v1.15.2` and an explicit true/false statement
      about whether that tag contains PWS_grading.
    - `grep -E "v1\.1[5-9]" .planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md` matches at least one real git tag actually present in `git tag -l`.
    - The findings file records a RESOLVED or UNRESOLVED verdict in plain text (`grep -Ec "RESOLVED|UNRESOLVED" .../229-10-PIN-SMOKETEST.md` returns >= 1), never silently omitted.
    - The smoketest script's own exit code (captured in the executor's task log) is 0 if and only if the findings file's verdict is RESOLVED.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| External caller -> `runOneAsync` | A future MCP wrapper (the separate mindrian-pitch-feedback-mcp repo, or any future in-repo caller) supplies subId/transcriptPath/deckPath/config - untrusted shape crosses here |
| Smoke-test script -> live `claude` CLI + public GitHub tag | A network fetch of a git tag plus a real, model-backed subprocess invocation - external content/network boundary |
| Smoke-test findings -> navigator / external repo maintainer | The recorded verdict is read as ground truth for a real infrastructure decision (which tag to pin) - must never silently fabricate a PASS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-229-10-01 | Tampering | scripts/huji-run-one-async.cjs `runClaudeAsync` | mitigate | D14 parity test (Task 2) proves the reconstructed `{status,stdout,stderr,error}` shape is byte-structurally identical to `spawnSync`'s on the same injected non-zero exit, for both Stage A and Stage B |
| T-229-10-02 | Repudiation | scripts/huji-run-one-async.cjs contract | mitigate | file-header contract-stability note + full JSDoc naming every reason code as part of the public contract (Task 1), so a future silent edit is a documented rule violation, not an invisible drift |
| T-229-10-03 | Spoofing / Tampering | scripts/huji-pin-smoketest.cjs tag resolution | mitigate | the script verifies `PWS_grading` is actually present in the resolved tag's `recipe-maps.cjs` via `git show` BEFORE cloning or spawning `claude`, and fails closed (exit 1, explicit FAIL) rather than trusting the external repo's documented v1.15.2 pin or any hardcoded assumption |
| T-229-10-04 | Information Disclosure / cost overrun | scripts/huji-pin-smoketest.cjs live invocation | mitigate | hard-capped `--max-turns 3`, `--max-budget-usd 0.10`, `--allowedTools ''` (no tool execution), `--no-session-persistence`, plus a `timeout: 60000` spawn backstop - the probe can prove command resolution without ever running a real grading pass |
| T-229-10-SC | Tampering (supply chain) | npm/pip/cargo installs | accept (not applicable) | zero new package-manager installs in this plan; the only external fetch is `git clone` of this org's own public repo at a pinned tag, not a third-party dependency |
</threat_model>

<verification>
- `node lib/memory/huji-run-one-async-parity.test.cjs` exits 0 (D14 gate green).
- `bash tests/run-all-229.sh` still exits with `FAIL=0` (existing D1-D10 legs plus the new D14 leg all pass or SKIP cleanly - no regression).
- `node scripts/huji-pin-smoketest.cjs` produces
  `.planning/phases/229-huji-pitch-feedback-module/229-10-PIN-SMOKETEST.md` with a
  real, non-fabricated RESOLVED or UNRESOLVED verdict and an explicit v1.15.2 finding.
  A truthful UNRESOLVED outcome is a valid result of this plan (the job is to
  CONFIRM the mechanism, not to force a specific answer); the plan is NOT satisfied
  by an omitted or hand-written verdict.
- `git diff --stat scripts/huji-run-one.cjs scripts/huji-batch.cjs` is empty (neither
  shipped 229-01..229-09 file was touched by this plan).
</verification>

<success_criteria>
- scripts/huji-run-one-async.cjs exists, exports an AsyncFunction `runOneAsync` plus
  `runClaudeAsync`, never calls `spawnSync`, and documents its return-shape contract
  in-source.
- lib/memory/huji-run-one-async-parity.test.cjs exists, passes, and is wired into
  tests/run-all-229.sh as a new guarded D14 leg.
- scripts/huji-batch.cjs is unchanged and still imports only the synchronous `runOne`.
- scripts/huji-pin-smoketest.cjs exists and has been run for real, producing
  229-10-PIN-SMOKETEST.md with a genuine, evidenced verdict (including the
  v1.15.2-vs-actual-tag-history finding) rather than an assumption.
</success_criteria>

<output>
Create `.planning/phases/229-huji-pitch-feedback-module/229-10-SUMMARY.md` when done
</output>
