---
phase: 229-huji-pitch-feedback-module
plan: 04
type: execute
wave: 2
depends_on: ["229-01"]
files_modified:
  - lib/core/recipe-maps.cjs
  - pipelines/PWS_grading/CHAIN.md
  - pipelines/PWS_grading/01-deep-grade.md
  - pipelines/PWS_grading/02-mullins.md
  - pipelines/PWS_grading/03-build-thesis.md
  - pipelines/PWS_grading/04-structure-argument.md
  - references/methodology/build-thesis-scored.md
  - references/methodology/rubric-huji.md
autonomous: true
requirements: [D3, D5, D7]
must_haves:
  truths:
    - "A named recipe PWS_grading resolves to the chain deep-grade -> mullins -> build-thesis -> structure-argument in that exact order"
    - "The chain runs unattended without halting on the build-thesis 6/10 gate (score-and-continue)"
    - "/mos:pipeline PWS_grading resolves the chain via the shipped pipeline resolver"
  artifacts:
    - path: "lib/core/recipe-maps.cjs"
      provides: "NAMED_RECIPES map + recipeForName accessor + PWS_grading key"
      contains: "recipeForName"
    - path: "pipelines/PWS_grading/CHAIN.md"
      provides: "Shipped pipeline definition so /mos:pipeline PWS_grading resolves"
      contains: "PWS_grading"
    - path: "references/methodology/rubric-huji.md"
      provides: "Frozen course-tier rubric appended via --append-system-prompt-file; score-and-continue instruction"
      contains: "score all ten"
    - path: "references/methodology/build-thesis-scored.md"
      provides: "Scored non-gating variant of the Ten-Questions gate (fallback mechanism)"
      contains: "score-and-continue"
  key_links:
    - from: "lib/core/recipe-maps.cjs"
      to: "recipeForCause pattern (line 322)"
      via: "recipeForName mirrors the frozen-map + slice-copy contract"
      pattern: "NAMED_RECIPES"
    - from: "pipelines/PWS_grading/CHAIN.md"
      to: "the 4 stage contract files"
      via: "ordered stage sequence deep-grade -> mullins -> build-thesis(scored) -> structure-argument"
      pattern: "structure-argument"
---

<objective>
Register the `PWS_grading` recipe and neutralize the build-thesis 6/10 halt so the four-command chain runs unattended in score-and-continue mode. This is seams (d) and (c): the recipe registration (posture/order authority) plus the shipped pipeline definition, and the prompt-layer mechanism that turns the ten-questions binary gate into scored feedback input.

Purpose: The batch spawns `/mos:pipeline PWS_grading` per submission; that command must resolve to the navigator-locked native order (mullins BEFORE build-thesis; structure-argument last as Minto packaging). The 6/10 gate is a PROMPT-LEVEL instruction (RESEARCH verified - no CJS gate); left intact it aborts a headless run "half the class fails question 2 and learns nothing." Both mechanisms must exist before the single-submission runner (Plan 07) can drive the chain.

Output: `recipe-maps.cjs` NAMED_RECIPES + recipeForName + PWS_grading; a `pipelines/PWS_grading/` CHAIN + 4 stage contracts; `rubric-huji.md` (primary score-and-continue) and `build-thesis-scored.md` (fallback).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/229-huji-pitch-feedback-module/229-RESEARCH.md
@.planning/phases/229-huji-pitch-feedback-module/229-PATTERNS.md
@.planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md
@.planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md
</context>

## Artifacts this phase produces (this plan)

New symbols: `NAMED_RECIPES` const + `recipeForName(name)` accessor in `recipe-maps.cjs`; recipe key `PWS_grading`.
New files: `pipelines/PWS_grading/CHAIN.md` + `01-deep-grade.md`, `02-mullins.md`, `03-build-thesis.md`, `04-structure-argument.md`; `references/methodology/build-thesis-scored.md`; `references/methodology/rubric-huji.md`.

<tasks>

<task type="auto">
  <name>Task 1: Register PWS_grading recipe + shipped pipeline definition (seam d)</name>
  <read_first>
    - lib/core/recipe-maps.cjs (SENS10_CAUSE_RECIPES:282 + recipeForCause:322 - the frozen-map + slice-copy contract to mirror; module.exports at 377)
    - .planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md (RECIPE_HOME + PIPELINE_ARG decisions from Plan 01)
    - .planning/phases/229-huji-pitch-feedback-module/229-PATTERNS.md (the recipe-maps registration section - exact native order, no fabricated autonomous_safe literals)
    - pipelines/thesis/CHAIN.md (the shipped CHAIN.md frontmatter + stage-file shape to copy)
    - pipelines/thesis/03-build-thesis.md (a stage contract example)
  </read_first>
  <action>
In `lib/core/recipe-maps.cjs` add a new `const NAMED_RECIPES = Object.freeze({ PWS_grading: Object.freeze(['/mos:deep-grade','/mos:mullins','/mos:build-thesis','/mos:structure-argument']) })` and a `recipeForName(name)` accessor mirroring `recipeForCause` EXACTLY (returns a fresh `.slice()` copy, never the frozen source, never throws, unknown/empty name -> `[]`). Do NOT fabricate `autonomous_safe` literals - posture is sourced separately via `postureForCommand`. Export `recipeForName` and `NAMED_RECIPES` in `module.exports`. Then create the shipped pipeline `pipelines/PWS_grading/CHAIN.md` (copy the `pipelines/thesis/CHAIN.md` frontmatter shape: name PWS_grading, display_name, description, stages 4, venture_stages [Validation], hitl_stages for the 4 ordered stages) plus four stage contracts `01-deep-grade.md`, `02-mullins.md`, `03-build-thesis.md`, `04-structure-argument.md` in that native order, each declaring its room_section input/output contract like the thesis stage files. The CHAIN.md order MUST match the recipe-maps order. Regenerate any registry if a build step is required, but do NOT hand-edit `data/command-registry.json`.
  </action>
  <verify>
    <automated>node -e "const r=require('./lib/core/recipe-maps.cjs'); const c=r.recipeForName('PWS_grading'); if(JSON.stringify(c)!==JSON.stringify(['/mos:deep-grade','/mos:mullins','/mos:build-thesis','/mos:structure-argument']))process.exit(1); if(r.recipeForName('nope').length!==0)process.exit(1); c.push('x'); if(r.recipeForName('PWS_grading').length!==4)process.exit(1); console.log('OK')" && test -f pipelines/PWS_grading/CHAIN.md && test -f pipelines/PWS_grading/04-structure-argument.md && echo OK2</automated>
  </verify>
  <acceptance_criteria>
    - `recipeForName('PWS_grading')` returns the 4-command chain in native order; unknown name returns []; return is a fresh slice (mutating it does not affect the source)
    - Recipe name is exactly `PWS_grading` (never "pitch-feedback")
    - `pipelines/PWS_grading/CHAIN.md` + 4 stage files exist; CHAIN order matches recipe-maps order
    - No fabricated autonomous_safe literals in the recipe map
  </acceptance_criteria>
  <done>PWS_grading resolves both via recipe-maps (posture authority) and via the shipped /mos:pipeline resolver, in the navigator-locked order.</done>
</task>

<task type="auto">
  <name>Task 2: Score-and-continue mechanism (seam c)</name>
  <read_first>
    - commands/build-thesis.md (the 6/10 "Binary gate" prompt-level instruction at line 66; autonomous_safe:true at 15)
    - references/methodology/build-thesis.md (the Ten-Questions gate body the scored variant overrides)
    - .planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md (SCORED_MODE decision: rubric-file primary, scored-variant fallback)
    - .planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md (Section 4b prompt discipline + Section 1b course-tier calibration + Part 12 tone)
  </read_first>
  <action>
Create `references/methodology/rubric-huji.md` as the frozen course-tier rubric appended via `--append-system-prompt-file` at Stage B. It MUST: (1) instruct build-thesis to SCORE all ten questions and CONTINUE unconditionally, never halting below 6/10, emitting per-question scores as feedback input; (2) tier the validation battery to course depth per the CONTEXT calibration ruling (student in a 200-person course, NOT a founder at a thesis gate) - no investor-gauntlet demands (unit economics, moat, term sheets); (3) carry the Part 12 tone constitution (formative not summative, never grade-and-compliment theater, credit self-identified gaps and never double-punish, never punish disfluencies/diarization noise/non-native phrasing); (4) reserve a marked slot where the two approved Amnon few-shot samples get embedded after the demo (Plan 09). Keep the file bit-stable (frozen prefix for prompt caching). Then create `references/methodology/build-thesis-scored.md` as the FALLBACK: a scored, non-gating variant of the Ten-Questions body invoked by the PWS_grading recipe if the demo shows build-thesis still halts under the rubric override alone (CONTRACTS SCORED_MODE fallback). No em-dashes.
  </action>
  <verify>
    <automated>test -f references/methodology/rubric-huji.md && test -f references/methodology/build-thesis-scored.md && grep -qi "score all ten\|score-and-continue\|never halt" references/methodology/rubric-huji.md && grep -qi "formative\|metacognition\|self-identified" references/methodology/rubric-huji.md && ! grep -q "—" references/methodology/rubric-huji.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - rubric-huji.md instructs score-all-ten-and-continue (no 6/10 halt) and tiers to course depth
    - rubric-huji.md carries the Part 12 tone rules incl. crediting self-identified gaps and never punishing disfluencies
    - rubric-huji.md has a marked slot for the 2 approved few-shot samples (embedded in Plan 09)
    - build-thesis-scored.md exists as the fallback scored variant; neither file contains em-dashes
  </acceptance_criteria>
  <done>The chain can run unattended in scored-and-continue mode via the frozen rubric, with a scored-variant reference as the demo-verified fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe registration -> chain execution | A wrong order or fabricated posture would run the investor gauntlet un-tiered or halt the batch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-229-04-01 | Repudiation (fairness) | rubric-huji.md as frozen prefix | mitigate | The rubric is bit-stable (frozen prefix); a mid-batch edit changes grading conditions across the cohort, so it is treated as grade provenance (git tag the checkout) |
| T-229-04-02 | Tampering | recipe order / posture | mitigate | recipeForName returns a slice-copy of a frozen map; no fabricated autonomous_safe literal; posture sourced from postureForCommand only |
| T-229-04-SC | Tampering | npm/pip/cargo installs | accept | Zero installs (RESEARCH audit = none); markdown + one CJS map edit |
</threat_model>

<verification>
- `recipeForName('PWS_grading')` returns the 4-command native-order chain; unknown -> []
- `pipelines/PWS_grading/CHAIN.md` + 4 stage files exist, order matches recipe-maps
- rubric-huji.md + build-thesis-scored.md exist, carry score-and-continue + Part 12 tone, no em-dashes
- `node scripts/build-connector-registry.cjs --check` (or equivalent) passes if a registry rebuild was needed
</verification>

<success_criteria>
- PWS_grading resolves in native order via both recipe-maps and the shipped pipeline resolver
- The chain runs unattended (score-and-continue) with a demo-verified fallback path
- Course-tier + Part 12 tone are encoded in the frozen rubric
</success_criteria>

<output>
Create `.planning/phases/229-huji-pitch-feedback-module/229-04-SUMMARY.md` when done.
</output>
