# Phase 229-10 Pin Smoke Test - Live Findings

> Generated by `scripts/huji-pin-smoketest.cjs` against real git history and a real
> `claude --plugin-dir` invocation. Not hand-written, not hypothetical.

- Run at (UTC): 2026-07-16T12:42:01.500Z
- Host claude CLI: 2.1.211 (Claude Code)
- This repo: /home/jsagi/dev/MindrianOS-Plugin

## Verdict: RESOLVED

Reason: `chain_began_then_capped`

## Tag resolution

- Candidate tags considered (newest first): v1.15.3-beta.26, v1.15.3-beta.24, v1.15.3-beta.22, v1.15.3-beta.20, v1.15.3-beta.18, v1.15.3-beta.16, v1.15.3-beta.14, v1.15.3-beta.12, v1.15.3-beta.10, v1.15.3-beta.8, v1.15.3-beta.6, v1.15.3-beta.4, v1.15.3-beta.3, v1.15.3-beta.1
- Resolved pin tag: `v1.15.3-beta.26`
- Recipe key checked: `PWS_grading` in `lib/core/recipe-maps.cjs`

## The documented pin (v1.15.2) - checked live, never assumed

The external `mindrian-pitch-feedback-mcp` repo README currently names `v1.15.2` as its pin.

- Does `v1.15.2` contain the `PWS_grading` recipe? **false**

CORRECTION NOTE: `v1.15.2` does NOT contain `PWS_grading`. The recipe first
appears at `v1.15.3-beta.22` and is present through the newest
candidate tag. The external repo's README pin of `v1.15.2` is STALE and would fail to
resolve `/mos:pipeline PWS_grading`. It must be re-pinned to a tag that carries the recipe.
No stable (non-beta) tag carries it yet - that gap is what 229-09 Task 3 (git-tag the checkout
before the first 200-student batch) exists to close.

## Live invocation

- Clone path (outside this repo): `/tmp/huji-pin-clone-IaKbGb/checkout`
- Clone contains `.claude-plugin/plugin.json`: true
- Invocation cwd (scratch, outside repo + clone): `/tmp/huji-pin-scratch-4yB02D`
- Raw claude invocation args:

```
claude -p /mos:pipeline PWS_grading --plugin-dir /tmp/huji-pin-clone-IaKbGb/checkout --model claude-haiku-4-5 --output-format json --permission-mode dontAsk --allowedTools  --max-turns 3 --max-budget-usd 0.10 --no-session-persistence
```

- Classification reason: `chain_began_then_capped`
- Envelope/detail excerpt:

```
subtype=error_max_budget_usd is_error=true num_turns=6 permission_denials=3
result: (empty - turn/budget cap fired after the command began)
pipeline tool attempts (proof of routing):
  Read: {"file_path":"/tmp/huji-pin-clone-IaKbGb/checkout/references/pipeline/chains-index.md"}
  Read: {"file_path":"/tmp/huji-pin-clone-IaKbGb/checkout/pipelines/PWS_grading/CHAIN.md"}
  Bash: {"command":"find /tmp/huji-pin-clone-IaKbGb/checkout -name \"room\" -o -name \"STATE.md\" 2>/dev/null | head -5"}
```

## What this proves

A git-tag-pinned checkout of this repo, loaded via `claude --plugin-dir` from a scratch
directory OUTSIDE this repo, DOES resolve `/mos:pipeline PWS_grading`. The pin mechanism
the external MCP wrapper depends on is confirmed working - provided it pins `v1.15.3-beta.26`,
NOT the stale `v1.15.2`.

