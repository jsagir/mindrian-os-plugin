---
phase: 229-huji-pitch-feedback-module
plan: 03
type: execute
wave: 2
depends_on: ["229-01", "229-02"]
files_modified:
  - scripts/huji-eval.cjs
autonomous: true
requirements: [D1, D2, D3, D4, D5, D8, D9]
must_haves:
  truths:
    - "Every non-verbatim quote in evidence.json/feedback.md is caught by the quote-verifier (D1)"
    - "A labeled-inventory item absent from evidence.json fails inventory-recall (D2)"
    - "A Brain-query payload with any student-specific string fails the Part 8 hygiene scan (D4)"
    - "The harness runs as a switch-case CLI with --check, --suite code, --report"
  artifacts:
    - path: "scripts/huji-eval.cjs"
      provides: "Deterministic code-check harness (quote-verifier, inventory-recall, schema, drift, similarity, cost-ledger, part8-hygiene) + --suite code + --report"
      exports: ["quoteVerifier", "inventoryRecall", "similarityIndex"]
      min_lines: 120
  key_links:
    - from: "scripts/huji-eval.cjs"
      to: "lib/core/part8-egress-guard.cjs"
      via: "require classify/scanForContent for D4"
      pattern: "part8-egress-guard"
    - from: "scripts/huji-eval.cjs"
      to: "lib/core/pitch-feedback-schemas.cjs"
      via: "require FeedbackResultSchema.safeParse for D5"
      pattern: "pitch-feedback-schemas"
---

<objective>
Build the deterministic (no-model-call) half of the eval harness: the code checks that gate every commit and wave - quote grounding (D1), extraction recall (D2), cohort drift (D3), Minto schema (D5), individuation (D8), cost adherence (D9), Part 8 query hygiene (D4).

Purpose: These turn the eval dimensions into runnable commands the aggregator invokes. D1 (zero fabrication) is the hardest gate and must be code-first, not judge-first. D4 reuses the shipped constitutional guard, never a hand-rolled regex. The judge/anchor half (model calls) is built in Plan 06 (same file, next wave).

Output: `scripts/huji-eval.cjs` with a switch-case CLI and seven code checks + `--suite code` + `--report`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md
@.planning/phases/229-huji-pitch-feedback-module/229-VALIDATION.md
@.planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md
</context>

## Artifacts this phase produces (this plan)

New file: `scripts/huji-eval.cjs`.
New CLI: `--check {quote-verifier|inventory-recall|drift|schema|similarity|cost-ledger|part8-hygiene}`, `--suite code`, `--strict`, `--report`, `--selftest`, path args `--transcript/--evidence/--feedback/--inventory/--out`.
New functions: `quoteVerifier`, `inventoryRecall`, `driftStats`, `schemaCheck`, `similarityIndex`, `costLedger`, `part8Hygiene`.

<tasks>

<task type="auto">
  <name>Task 1: CLI router + grounding checks (D1 quote-verifier, D2 recall, D5 schema)</name>
  <read_first>
    - scripts/label-topic-forest.cjs (switch-case argv router; no Commander/yargs)
    - lib/core/pitch-feedback-schemas.cjs (FeedbackResultSchema for D5 - Plan 01)
    - .planning/phases/229-huji-pitch-feedback-module/eval/labeled-inventories/sample-1.inventory.json (D2 ground truth - Plan 02)
    - .planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md (Section 5 D1/D2/D5 PASS/FAIL rubrics)
  </read_first>
  <action>
Create `scripts/huji-eval.cjs` with a `process.argv` switch-case router (mirror `label-topic-forest.cjs`). `quoteVerifier({transcript,evidence,feedback})` (D1): whitespace+case-normalize, assert every `evidence_claims[].quote` and every markdown-quoted span in feedback.md is a substring of the normalized transcript, report each miss, exit non-zero on any miss. `inventoryRecall({inventory,evidence})` (D2): assert 100% of the inventory's entities/claims/self_identified_gaps appear in evidence.json, list misses. `schemaCheck({feedback})` (D5): `require('../lib/core/pitch-feedback-schemas.cjs')` and `FeedbackResultSchema.safeParse`, print zod issue paths on failure. Wire `--check quote-verifier|inventory-recall|schema` with path args. Add a `--selftest grounding` mode running each check over tiny in-file fixtures (valid case PASS, mutated case FAIL) so the leg is verifiable with no batch output present. Unknown `--check` prints usage and exits non-zero.
  </action>
  <verify>
    <automated>node scripts/huji-eval.cjs --selftest grounding && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/huji-eval.cjs --selftest grounding` exits 0, demonstrating PASS+FAIL for quote-verifier, inventory-recall, schema
    - quote-verifier flags a one-word-altered quote and a covered-element-marked-missing as misses
    - schema leg imports FeedbackResultSchema from lib/core/pitch-feedback-schemas.cjs (grep confirms require)
    - unknown `--check` exits non-zero with usage
  </acceptance_criteria>
  <done>The grounding checks run deterministically and catch fabrication, recall gaps, and schema violations.</done>
</task>

<task type="auto">
  <name>Task 2: Cohort checks (D3 drift, D8 similarity, D9 cost-ledger)</name>
  <read_first>
    - .planning/phases/229-huji-pitch-feedback-module/eval/probes/manifest.json (D3 positions; D8 near-dup pair - Plan 02)
    - .planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md (Section 5 D3/D8/D9 rubrics: within-1-band, shingle-Jaccard 0.5, total_cost_usd <= $3.00)
    - scripts/huji-eval.cjs (extend from Task 1)
  </read_first>
  <action>
Extend `scripts/huji-eval.cjs`. `driftStats({resultsDir,manifest})` (D3): read each `out/<id>/result.json`, assert all carry an identical pinned `model_id`, compare duplicate-anchor probe scores across positions 1/50/100/150/200 (fail if any deviates > 1 band from position 1), compute rolling mean/stdev per 25 units. `similarityIndex({feedbackDir})` (D8): dependency-free shingle-Jaccard (k=5 word shingles) over all feedback.md, report max+median, fail if any pair exceeds `--threshold` (default 0.5). `costLedger({resultsDir})` (D9): sum `total_cost_usd` per unit, fail if any unit > $3.00, report warm average vs $2.00 and batch total vs the $150-400 projection. Wire `--check drift|similarity|cost-ledger`. Extend `--selftest cohort` with a within-vs-out-of-band probe set, a duplicate-vs-distinct feedback pair, and a ledger with one over-budget unit.
  </action>
  <verify>
    <automated>node scripts/huji-eval.cjs --selftest cohort && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/huji-eval.cjs --selftest cohort` exits 0 demonstrating PASS+FAIL for drift, similarity, cost-ledger
    - drift fails on a mismatched model_id and on a >1-band probe deviation
    - similarity fails a duplicated feedback pair, passes distinct; `--threshold` respected
    - cost-ledger fails a ledger with a >$3.00 unit
  </acceptance_criteria>
  <done>The cohort-fairness and cost checks run deterministically against the probe manifest and result envelopes.</done>
</task>

<task type="auto">
  <name>Task 3: Part 8 hygiene (D4) + --suite code + --report</name>
  <read_first>
    - lib/core/part8-egress-guard.cjs (REUSE classify:231, scanForContent:97 - never hand-roll)
    - .planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md (Section 5 D4 zero-tolerance + Section 7 report fields)
    - scripts/huji-eval.cjs (extend from Tasks 1-2)
  </read_first>
  <action>
Extend `scripts/huji-eval.cjs`. `part8Hygiene({queryLog,evidenceDir})` (D4): `require('../lib/core/part8-egress-guard.cjs')`, run `classify(payload,{toolName:'brain_ask'})` over every logged Brain-query payload, AND grep each unit's evidence.json entity inventory against the payloads; ANY hit exits non-zero printing the offending payload (zero tolerance). Add `--suite code [--strict]` running all seven checks over an `--out <dir>` batch dir with a PASS/FAIL roll-up (strict = non-zero on any FAIL). Add `--report` rendering the cohort view from result.json + batch-state.json (cost curve, score distribution, drift-vs-probes, similarity matrix, failures, and an explicit "0 Part 8 violations in N queries" line). Extend `--selftest hygiene` (clean log PASS, log with a venture name FAIL).
  </action>
  <verify>
    <automated>node scripts/huji-eval.cjs --selftest hygiene && grep -q "part8-egress-guard" scripts/huji-eval.cjs && node scripts/huji-eval.cjs --suite code --out /tmp/nonexistent-229 --strict >/dev/null 2>&1; echo "router-ok $?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/huji-eval.cjs --selftest hygiene` exits 0 with a clean-PASS and a venture-name-FAIL case
    - D4 leg imports and calls part8-egress-guard.classify (grep confirms)
    - `--suite code` and `--report` are wired subcommands; `--strict` returns non-zero on any FAIL
    - No hand-rolled egress regex; the guard is the sole D4 authority
  </acceptance_criteria>
  <done>The Part 8 hygiene gate reuses the constitutional guard, and the suite roll-up + report render the whole cohort view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brain-query log -> hygiene scan | Any student-specific string in a logged payload is a Part 8 breach the scan must catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-229-03-01 | Information Disclosure | part8Hygiene (D4) | mitigate | Reuse lib/core/part8-egress-guard.classify + per-unit entity grep; zero-tolerance non-zero exit; never a hand-rolled regex |
| T-229-03-02 | Spoofing (trust) | quoteVerifier (D1) | mitigate | Verbatim substring check over normalized transcript; a fabricated quote cannot pass the substring gate |
| T-229-03-SC | Tampering | npm/pip/cargo installs | accept | Zero installs (RESEARCH audit = none); dependency-free CJS only |
</threat_model>

<verification>
- `node scripts/huji-eval.cjs --selftest grounding|cohort|hygiene` all exit 0
- `grep part8-egress-guard scripts/huji-eval.cjs` and `grep pitch-feedback-schemas scripts/huji-eval.cjs` both match
- `bash tests/run-all-229.sh` now runs the D1/D2/D5/D8/D9/D4/D3 legs (no longer all SKIP)
</verification>

<success_criteria>
- All seven code checks run deterministically with self-tests demonstrating PASS and FAIL
- D1 is code-first and D4 reuses the shipped guard
- `--suite code --strict` and `--report` are wired
</success_criteria>

<output>
Create `.planning/phases/229-huji-pitch-feedback-module/229-03-SUMMARY.md` when done.
</output>
