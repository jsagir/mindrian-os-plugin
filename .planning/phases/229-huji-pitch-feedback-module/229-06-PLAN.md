---
phase: 229-huji-pitch-feedback-module
plan: 06
type: execute
wave: 3
depends_on: ["229-03", "229-02"]
files_modified:
  - scripts/huji-eval.cjs
  - .planning/phases/229-huji-pitch-feedback-module/eval/judge-prompt.md
  - .planning/phases/229-huji-pitch-feedback-module/eval/judge-schema.json
autonomous: true
requirements: [D1, D3, D6, D7]
must_haves:
  truths:
    - "A headless sonnet judge scores feedback on Real/Win/Worth 1-5 + D1/D6/D7 violation lists against the normalized anchors"
    - "The judge is gated by a calibration protocol: Spearman >= 0.7 on the 6 graded anchors, Dental post > pre, DnATA < Lucid"
    - "The judge model is pinned DIFFERENT from the grading spine (sonnet judging opus) to reduce self-preference bias"
  artifacts:
    - path: "scripts/huji-eval.cjs"
      provides: "--suite anchors --judge spawner + calibration protocol (fails closed under 0.7)"
      contains: "spawnSync"
    - path: ".planning/phases/229-huji-pitch-feedback-module/eval/judge-prompt.md"
      provides: "Judge rubric: Real/Win/Worth 1-5 + D1/D6/D7 checklist grounded in the anchor-hygiene rules"
      contains: "Real"
    - path: ".planning/phases/229-huji-pitch-feedback-module/eval/judge-schema.json"
      provides: "JSON Schema for the judge structured output (--json-schema)"
      contains: "score"
  key_links:
    - from: "scripts/huji-eval.cjs (judge spawner)"
      to: "calibration/ fixtures (6 graded anchors) + INDEX.md normalization map"
      via: "judge scores anchors; protocol asserts Spearman >= 0.7 vs known ordering"
      pattern: "calibration"
---

<objective>
Build the model-call half of the eval harness: the LLM judge and its calibration protocol. The judge scores each feedback artifact on the corpus-stable spine (Is-it-Real / Can-we-Win / Is-it-Worth-it, 1-5) plus D1/D6/D7 violation lists, and is trusted ONLY after passing a calibration gate against the 6 usable graded anchors.

Purpose: D6 (formative tone, metacognition) and D7 (course-tier feed-forward) need judgment, not code. But an uncalibrated judge is noise. RESEARCH/AI-SPEC make anchor hygiene NORMATIVE: normalize three incompatible scales, exclude the fixture-08 projection and the meta fixtures, and require Spearman >= 0.7 on the 6 real anchors + human re-rank >= 0.7 before the judge may gate delivery. Judge = sonnet judging opus output (self-preference-bias dodge). Same file as Plan 03 (huji-eval.cjs), next wave.

Output: `eval/judge-prompt.md`, `eval/judge-schema.json`, and the `--suite anchors --judge` + calibration-protocol legs in `huji-eval.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md
@.planning/phases/229-huji-pitch-feedback-module/229-VALIDATION.md
@.planning/phases/229-huji-pitch-feedback-module/calibration/INDEX.md
</context>

## Artifacts this phase produces (this plan)

New files: `eval/judge-prompt.md`, `eval/judge-schema.json`.
New CLI/functions in `huji-eval.cjs`: `--suite anchors --judge`, `spawnJudge()`, `calibrationProtocol()`, `--report` extended with judge scores.
New convention: judge output shape `{ submission_id, real, win, worth, d1_violations[], d6_violations[], d7_violations[] }` (1-5 per dimension).

<tasks>

<task type="auto">
  <name>Task 1: Judge prompt + judge schema (anchor hygiene NORMATIVE)</name>
  <read_first>
    - .planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md (Section 5 "LLM Judge Design" - the 5 binding anchor-hygiene rules + normalization map)
    - .planning/phases/229-huji-pitch-feedback-module/calibration/INDEX.md (the 12 fixtures, which 6 are graded anchors, the 3 incompatible scales, fixture-08 projection exclusion, 05/06/12 meta exclusion)
    - .planning/phases/229-huji-pitch-feedback-module/229-VALIDATION.md (judge suite fails closed under 0.7 Spearman)
  </read_first>
  <action>
Create `eval/judge-schema.json` (a small JSON Schema; may be emitted from an inline zod JudgeSchema in huji-eval.cjs) with fields `submission_id`, `real` (int 1-5), `win` (1-5), `worth` (1-5), and `d1_violations`, `d6_violations`, `d7_violations` (string arrays). Create `eval/judge-prompt.md`: the judge scores ONLY the corpus-stable spine (Is-it-Real / Can-we-Win / Is-it-Worth-it, 1-5) plus explicit violation checklists for D1 (any ungrounded/fabricated claim, any covered element marked missing), D6 (grade-and-compliment theater, double-punished metacognition, disfluency/language penalized), D7 (investor-gauntlet demand, next step above course level, summary-without-path). Encode the normalization map from AI-SPEC (< 50% = 1, 50-64 = 2, 65-79 = 3, 80-89 = 4, 90+ = 5). Embed the judge-rubric ingredients from fixtures 05/06 (grounds every claim in checked evidence, flags cognitive bias, consistent rubric, reframes the problem). State that fixture-08's 90/100 is an "if-it-was-A+" projection, NOT ground truth. No em-dashes.
  </action>
  <verify>
    <automated>node -e "const s=require('./.planning/phases/229-huji-pitch-feedback-module/eval/judge-schema.json'); const t=JSON.stringify(s); if(!/real|win|worth/i.test(t)||!/d1_violations/.test(t))process.exit(1); console.log('OK')" && grep -qi "Real" .planning/phases/229-huji-pitch-feedback-module/eval/judge-prompt.md && grep -qi "projection\|not ground truth\|if.*A+" .planning/phases/229-huji-pitch-feedback-module/eval/judge-prompt.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - judge-schema.json carries real/win/worth (1-5) + d1/d6/d7 violation arrays
    - judge-prompt.md scores only the Real/Win/Worth spine + violation checklists; encodes the normalization map
    - fixture-08 projection explicitly excluded as ground truth; 05/06 ingredients embedded
    - No em-dashes
  </acceptance_criteria>
  <done>The judge scores the corpus-stable spine with violation checklists, grounded in the normative anchor-hygiene rules.</done>
</task>

<task type="auto">
  <name>Task 2: Judge spawner + calibration protocol in huji-eval.cjs</name>
  <read_first>
    - scripts/huji-eval.cjs (extend the Plan-03 harness; reuse its argv router + --report)
    - lib/core/room-auto-create.cjs (the args-array spawn discipline at line 240 - copy, no shell)
    - .planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md (AUTH_PATH: Stage A/judge use --bare + ANTHROPIC_API_KEY)
    - .planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md (Section 5 calibration protocol: 6 anchors 01/02/03pre+post/04/09/10; Spearman >= 0.7; Dental post > pre; DnATA < Lucid; human re-rank >= 0.7)
  </read_first>
  <action>
Extend `scripts/huji-eval.cjs`. `spawnJudge(feedbackPath)`: `spawnSync('claude', ['--bare','-p', judgePrompt, '--model', judgeModel, '--allowedTools','Read','--output-format','json','--json-schema', judgeSchemaJson, ...])` with an args array and NO shell (copy room-auto-create.cjs discipline; never the label-topic-forest execSync pattern); pin the judge model DIFFERENT from the spine (sonnet vs opus). `calibrationProtocol()`: run the judge over the 6 usable graded anchors, normalize per the map, assert (a) rank order matches the known ordering with Spearman >= 0.7, (b) Dental post-revision strictly above pre-revision, (c) DnATA (10) below Lucid (09) on every dimension; FAIL CLOSED (non-zero) under 0.7. Wire `--suite anchors --judge` to run the protocol then judge the current batch sample. Print the human re-rank instruction (Jonathan blind-ranks 10 artifacts; correlation >= 0.7 or revise the prompt). Extend `--report` to include judge scores. Guard: if `ANTHROPIC_API_KEY` is unset, skip with a clear message (do not fail structural runs).
  </action>
  <verify>
    <automated>grep -q "spawnSync" scripts/huji-eval.cjs && grep -q "calibrationProtocol\|--suite anchors\|anchors" scripts/huji-eval.cjs && ! grep -q "execSync(\`claude" scripts/huji-eval.cjs && node -e "require('./scripts/huji-eval.cjs')" 2>/dev/null; echo "loads $?"</automated>
  </verify>
  <acceptance_criteria>
    - Judge spawns via spawnSync args-array, no shell; judge model pinned different from spine
    - calibrationProtocol asserts Spearman >= 0.7, Dental post > pre, DnATA < Lucid; fails closed under 0.7
    - `--suite anchors --judge` wired; `--report` includes judge scores
    - Missing ANTHROPIC_API_KEY skips cleanly, never fails a structural run
  </acceptance_criteria>
  <done>The calibrated judge is gated behind the anchor protocol and only trusted after passing it; structural runs never depend on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| judge session -> feedback under review | Untrusted feedback content (built from student transcripts) flows into the judge session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-229-06-01 | Repudiation (fairness) | uncalibrated judge gating delivery | mitigate | calibrationProtocol fails closed under 0.7 Spearman + human re-rank; an uncalibrated judge's scores never gate delivery |
| T-229-06-02 | Tampering | judge spawn | mitigate | args-array spawnSync, no shell, Read-only tools; never the execSync shell-quote pattern |
| T-229-06-03 | Information Disclosure | judge session content | mitigate | Judge runs locally, Read-only, no network/Brain tools; feedback content never egresses (Part 8) |
| T-229-06-SC | Tampering | npm/pip/cargo installs | accept | Zero installs (RESEARCH audit = none); reuse claude CLI + existing core |
</threat_model>

<verification>
- `grep spawnSync scripts/huji-eval.cjs` matches; execSync shell-quote pattern absent
- judge-schema.json + judge-prompt.md exist and satisfy Task 1 asserts
- With ANTHROPIC_API_KEY set, `node scripts/huji-eval.cjs --suite anchors --judge` runs the protocol; without it, skips cleanly
</verification>

<success_criteria>
- The judge scores the Real/Win/Worth spine + D1/D6/D7 violations and is calibrated against the 6 real anchors
- Calibration fails closed under 0.7; delivery is never gated by an uncalibrated judge
- The judge model differs from the spine to dodge self-preference bias
</success_criteria>

<output>
Create `.planning/phases/229-huji-pitch-feedback-module/229-06-SUMMARY.md` when done.
</output>
