---
phase: 229-huji-pitch-feedback-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/pitch-feedback-schemas.cjs
  - .planning/phases/229-huji-pitch-feedback-module/schemas/evidence.schema.json
  - .planning/phases/229-huji-pitch-feedback-module/schemas/feedback-result.schema.json
  - .planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md
autonomous: true
requirements: [D1, D2, D5, D9]
must_haves:
  truths:
    - "The evidence contract exists as a single zod source that both the CLI --json-schema flag and the orchestrator safeParse consume"
    - "The feedback result contract encodes the Minto shape: governing thought first, 2-3 branches, each with a teachable next step"
    - "The 5 RESEARCH open questions are resolved to concrete build decisions, written down where every downstream plan reads them"
  artifacts:
    - path: "lib/core/pitch-feedback-schemas.cjs"
      provides: "EvidenceSchema, FeedbackResultSchema (zod) + toJsonSchemas() emitter"
      exports: ["EvidenceSchema", "FeedbackResultSchema", "toJsonSchemas"]
      min_lines: 60
    - path: ".planning/phases/229-huji-pitch-feedback-module/schemas/evidence.schema.json"
      provides: "JSON Schema for --json-schema Stage A extraction contract"
      contains: "evidence_claims"
    - path: ".planning/phases/229-huji-pitch-feedback-module/schemas/feedback-result.schema.json"
      provides: "JSON Schema for --json-schema Stage B result envelope"
      contains: "governing_thought"
    - path: ".planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md"
      provides: "Resolutions for the 5 Wave-0 open questions (pipeline arg, intake path, scored mode, auth, recipe home)"
      contains: "PWS_grading"
  key_links:
    - from: "lib/core/pitch-feedback-schemas.cjs"
      to: "schemas/*.schema.json"
      via: "toJsonSchemas() via require('zod/v4').z.toJSONSchema"
      pattern: "toJSONSchema"
    - from: "CONTRACTS.md"
      to: "commands/pipeline.md + scripts/pipeline-command.cjs + commands/build-thesis.md"
      via: "empirical grep/read resolution recorded per question"
      pattern: "pipelines/PWS_grading|recipeForName"
---

<objective>
Author the two zod contracts that anchor the whole pipeline (evidence in, feedback out), generate their JSON Schema serializations, then empirically resolve the 5 open questions RESEARCH.md flagged for Wave 0 so every downstream plan builds against settled decisions instead of assumptions.

Purpose: The evidence JSON is the ONLY thing the grading spine sees (anti-hallucination anchor, D1); the feedback result schema is the Minto delivery contract (D5). Both must exist as one zod source of truth before any harness leg, intake adapter, or orchestrator spawn can validate against them. The 5 open questions (A1-A5 in RESEARCH) gate correct wiring of seams (b), (c), (d), (e) - resolving them first prevents rework across 4 downstream plans.

Output: `lib/core/pitch-feedback-schemas.cjs` (zod source + emitter), two generated JSON Schema files, and `CONTRACTS.md` with the settled resolution of each open question.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md
@.planning/phases/229-huji-pitch-feedback-module/229-RESEARCH.md
@.planning/phases/229-huji-pitch-feedback-module/samples/sample-1-safescan.md
@.planning/phases/229-huji-pitch-feedback-module/samples/sample-2-study-app.md
</context>

## Artifacts this phase produces (this plan)

New files:
- `lib/core/pitch-feedback-schemas.cjs`
- `.planning/phases/229-huji-pitch-feedback-module/schemas/evidence.schema.json`
- `.planning/phases/229-huji-pitch-feedback-module/schemas/feedback-result.schema.json`
- `.planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md`

New symbols / exports:
- `EvidenceSchema` (zod) fields: `submission_id`, `problem_claim{stated,quote,timestamp}`, `value_proposition{stated,quote}`, `evidence_claims[]{claim,quote,evidenced}`, `self_identified_gaps[]`, `speaker_count`, `language_notes`
- `FeedbackResultSchema` (zod) fields: `submission_id`, `governing_thought`, `branches[]{point,support[],teachable_next_step}`, `scores{}`, `model_id`, `calibration_source`
- `toJsonSchemas(outDir)` - emitter writing both `.schema.json` files via `require('zod/v4').z.toJSONSchema`
- `CONTRACTS.md` decision keys: `PIPELINE_ARG`, `INTAKE_PATH`, `SCORED_MODE`, `AUTH_PATH`, `RECIPE_HOME`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author evidence + feedback zod schemas and emit JSON Schema</name>
  <read_first>
    - .planning/phases/229-huji-pitch-feedback-module/229-AI-SPEC.md (Section 4b "Structured Outputs with Pydantic" - the exact EvidenceSchema + FeedbackResultSchema field shapes are specified there; port them verbatim to zod)
    - .planning/phases/229-huji-pitch-feedback-module/samples/sample-1-safescan.md (grounds problem_claim / value_proposition / evidence_claims - e.g. "smart light sensor" = evidenced:'asserted')
    - .planning/phases/229-huji-pitch-feedback-module/samples/sample-2-study-app.md (grounds self_identified_gaps - "deeper market research, competitor analysis, user testing")
    - package.json (confirm zod ^3.25.76 is vendored; the v4 subpath ships in 3.25.x)
    - lib/core/model-profiles.cjs (CJS module conventions - module.exports shape to mirror)
  </read_first>
  <behavior>
    - EvidenceSchema.safeParse rejects an object missing evidence_claims (required array)
    - EvidenceSchema accepts evidenced enum only in {'evidenced','asserted','absent'}; rejects 'maybe'
    - problem_claim.timestamp accepts "1:10" and null; rejects "70 seconds"
    - FeedbackResultSchema rejects a payload with 1 branch or 4 branches (min 2, max 3); accepts 2 or 3
    - FeedbackResultSchema rejects a governing_thought under 20 chars
    - Each branch requires point + support (>=1) + teachable_next_step
    - toJsonSchemas() writes two JSON Schema files that JSON.parse cleanly and contain 'evidence_claims' and 'governing_thought'
  </behavior>
  <action>
Create `lib/core/pitch-feedback-schemas.cjs` as a CJS module. Define `EvidenceSchema` with the exact fields from AI-SPEC Section 4b: `submission_id` (non-empty string), `problem_claim` (object: stated boolean, quote string, timestamp string matching `/^\d+:\d{2}$/` or null), `value_proposition` (object: stated boolean, quote string), `evidence_claims` (array of objects: claim string, quote string, evidenced enum ['evidenced','asserted','absent']), `self_identified_gaps` (array of strings), `speaker_count` (positive int), `language_notes` (string). Define `FeedbackResultSchema` with: `submission_id`, `governing_thought` (min 20 chars), `branches` (array min 2 max 3 of objects: point, support array min 1, teachable_next_step), `scores` (record string to number 0-100), `model_id` (string), and `calibration_source` (enum ['local-anchors','brain+local-anchors']) per AI-SPEC Brain posture. Bound the free-text fields with `.max()` per AI-SPEC 4b token-bounds note (branch point, teachable_next_step, governing_thought) so feedback length is a contract. Export `EvidenceSchema`, `FeedbackResultSchema`, and `toJsonSchemas(outDir)` which requires `zod/v4`, calls `z.toJSONSchema` on each schema and writes `evidence.schema.json` + `feedback-result.schema.json` into outDir. Add a `require.main === module` self-test block asserting the behaviors above. Then run `toJsonSchemas` to generate the two files into `.planning/phases/229-huji-pitch-feedback-module/schemas/`. Do NOT hand-write the JSON Schema files - RESEARCH verified `require('zod/v4').z.toJSONSchema` resolves as a function under CJS on this machine.
  </action>
  <verify>
    <automated>node -e "const s=require('./lib/core/pitch-feedback-schemas.cjs'); if(s.EvidenceSchema.safeParse({}).success)process.exit(1); const b=s.FeedbackResultSchema.safeParse({submission_id:'x',governing_thought:'a'.repeat(25),branches:[{point:'p',support:['s'],teachable_next_step:'t'},{point:'p2',support:['s2'],teachable_next_step:'t2'}],scores:{real:80},model_id:'claude-opus-4-8',calibration_source:'local-anchors'}); if(!b.success){console.error(b.error.issues);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The `node -e` self-test above prints OK and exits 0
    - Both `.schema.json` files exist, JSON.parse cleanly, and contain `evidence_claims` (evidence) and `governing_thought` (feedback) respectively
    - `require('zod/v4').z.toJSONSchema` is the generation path (grep the module for `zod/v4` and `toJSONSchema`); no hand-written JSON Schema literal
    - `evidenced` field enum is exactly `['evidenced','asserted','absent']`
    - `branches` enforces min 2 max 3
  </acceptance_criteria>
  <done>Both contracts validate the AI-SPEC 4b field shapes, generate their JSON Schema serializations from the single zod source, and pass the deterministic self-test.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the 5 Wave-0 open questions into CONTRACTS.md</name>
  <read_first>
    - commands/pipeline.md (Q1: /mos:pipeline resolves a named pipeline by loading pipelines/{name}/CHAIN.md; the --from-problem-type path Brain-derives via scripts/pipeline-command.cjs)
    - scripts/pipeline-command.cjs (Q1: the argv contract, parseArgs at line 62)
    - skills/file-meeting/SKILL.md (Q2: Claimify 4-pass reference; confirm navigation.writeClaimNode is a plain function callable without the interactive command)
    - lib/core/navigation.cjs (Q2: writeClaimNode re-export at line 209)
    - lib/core/room-db.cjs (Q2: openRoomDb at line 100)
    - commands/build-thesis.md (Q3: the 6/10 "Binary gate" is prompt-level natural language at line 66; autonomous_safe:true at line 15 - no CJS gate to patch)
    - lib/core/recipe-maps.cjs (Q5: SENS10_CAUSE_RECIPES at line 282 + recipeForCause accessor at line 322 - the frozen-map + slice-copy shape to mirror for a new NAMED_RECIPES map)
    - .planning/phases/229-huji-pitch-feedback-module/229-RESEARCH.md (the Open Questions section + Assumptions A1-A5 - the exact questions to answer)
  </read_first>
  <action>
Create `.planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md` recording the settled resolution of each open question as a decision block keyed for downstream plans. Resolve empirically by reading the files above (do NOT guess). (Q1 PIPELINE_ARG) confirm `/mos:pipeline <name>` loads `pipelines/<name>/CHAIN.md` - therefore `PWS_grading` needs BOTH a `recipe-maps.cjs` NAMED_RECIPES registration (posture/order authority) AND a `pipelines/PWS_grading/CHAIN.md` directory so the shipped resolver runs it; record the exact invocation string the orchestrator spawns and how the transcript/evidence path is passed. (Q2 INTAKE_PATH) the intake adapter drives `navigation.writeClaimNode(openRoomDb(scratchRoomDir), params)` DIRECTLY from a headless Stage A prompt scoped to Read + Bash(node lib/core/*); it does NOT invoke the interactive `/mos:file-meeting` command (autonomous_safe:false, nugget-routing HITL) - record the tool-scope decision and confirm the writer works against a freshly scaffolded scratch room.db with a throwaway node spike. (Q3 SCORED_MODE) primary = a frozen `rubric-huji.md` via `--append-system-prompt-file` instructing build-thesis to score all ten questions and continue unconditionally; fallback = `references/methodology/build-thesis-scored.md` invoked by the recipe if the demo shows a residual halt - record both, mark the demo as the deciding test. (Q4 AUTH_PATH) Stage A uses `--bare` + `ANTHROPIC_API_KEY`; Stage B uses `--plugin-dir` (plugin needed) - record the API-key-vs-keychain source decision. (Q5 RECIPE_HOME) a new `NAMED_RECIPES` const + `recipeForName(name)` accessor in `recipe-maps.cjs` mirroring `recipeForCause` (frozen map of bare command strings, slice-copy return, never throws, unknown key -> []); recipe name is exactly `PWS_grading`. Each block: DECISION, EVIDENCE (file:line), CONSUMED-BY (downstream plan). Paste the Q2 writer-spike result into the INTAKE_PATH block.
  </action>
  <verify>
    <automated>f=.planning/phases/229-huji-pitch-feedback-module/CONTRACTS.md; test -f "$f" && grep -q "PIPELINE_ARG" "$f" && grep -q "recipeForName" "$f" && grep -q "pipelines/PWS_grading" "$f" && grep -q "SCORED_MODE" "$f" && grep -q "AUTH_PATH" "$f" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `CONTRACTS.md` exists with all 5 decision blocks: PIPELINE_ARG, INTAKE_PATH, SCORED_MODE, AUTH_PATH, RECIPE_HOME
    - PIPELINE_ARG names both the recipe-maps registration AND the pipelines/PWS_grading/CHAIN.md requirement
    - RECIPE_HOME specifies `recipeForName` + `NAMED_RECIPES` and the exact name `PWS_grading` (never "pitch-feedback")
    - INTAKE_PATH block contains the writer-spike result proving writeClaimNode runs against a scratch room.db
    - Each block cites file:line EVIDENCE and a CONSUMED-BY downstream plan
  </acceptance_criteria>
  <done>All 5 open questions are resolved to concrete, evidence-cited build decisions that plans 04/05/07/08 read directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema authoring -> downstream CLI/orchestrator | The schema shape decides what unvalidated data can reach a student; a permissive schema is a validation hole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-229-01-01 | Tampering | EvidenceSchema free-text fields | mitigate | Bound free-text with `.max()` and enum-constrain `evidenced`; a schema that accepts anything cannot catch fabrication downstream (D1/D5) |
| T-229-01-02 | Information Disclosure | schema field set | mitigate | Schema carries ONLY generic per-submission fields (no Brain handles, no cross-student refs); nothing in the contract invites egress (Part 8) |
| T-229-01-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (RESEARCH Package Legitimacy Audit = none required); zod already vendored. No supply-chain checkpoint applies |
</threat_model>

<verification>
- `node lib/core/pitch-feedback-schemas.cjs` self-test passes (behaviors in Task 1)
- Both JSON Schema files present under `schemas/` and parse cleanly
- `CONTRACTS.md` present with all 5 keyed decision blocks
- No package installed (git diff shows no package.json/package-lock change)
</verification>

<success_criteria>
- The evidence + feedback zod contracts exist as one CJS source and generate their JSON Schema files from that source
- The 5 Wave-0 open questions are resolved with cited evidence, unblocking plans 04, 05, 07, 08
- Recipe name is fixed as `PWS_grading` in CONTRACTS.md, matching the navigator ruling
</success_criteria>

<output>
Create `.planning/phases/229-huji-pitch-feedback-module/229-01-SUMMARY.md` when done.
</output>
