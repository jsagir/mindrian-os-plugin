---
phase: 109-sql-context-memory-navigation-spine
plan: "11"
type: execute
wave: 4
depends_on:
  - "109-00"
  - "109-09"
  - "109-10"
files_modified:
  - docs/MINDRIAN-CANON.md
  - docs/CANON-PHASE-MAP.md
  - tests/test-canon-part-9-ratification.cjs
autonomous: true
requirements:
  - NAV-109-09
canon_parts:
  - "Part 4 (Every Choice Is Graph Data - Part 9 makes the source of authority explicit: Brain proposes, the human confirms, rejection edges carry the reason)"
  - "Part 8 (Graph Boundary - Part 9 is the enforcement architecture; Part 8 says where data must not go, Part 9 says where memory lives and how the typed packet contract makes Part 8 structurally enforceable)"
  - "Part 9 (Memory Locality and Interpretation - RATIFIED here: the proposal text from .planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md merged into docs/MINDRIAN-CANON.md immediately before Appendix A; CANON-PHASE-MAP.md rows flipped proposed/planned -> shipped for Phases 108 and 109; Appendix D entry 12 added; canon version bumped v1.3 -> v1.4)"
must_haves:
  truths:
    - "docs/MINDRIAN-CANON.md contains a new section header `## Part 9 - Memory Locality and Interpretation` (note: a hyphen, not an em-dash) inserted IMMEDIATELY BEFORE the line `## Appendix A - Relationship to MWP` and IMMEDIATELY AFTER the end of Part 8 (the Part 8 block ends with the `### Violations are bugs` subsection and its closing fenced block) - so the section order is ... Part 8, Part 9, Appendix A, Appendix B ..."
    - "The Part 9 body text is the proposed canon text from `.planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md` lines 20-52 (the block under `## The proposed canon text`), de-quoted (strip the leading `> ` blockquote markers since it becomes live canon, not a quoted proposal), with em-dashes converted to hyphens to satisfy the project hard rule, PLUS a one-sentence forward-reference to Phase 109 as the implementing phase per RESEARCH section 9.1 line 1109 (e.g. 'Phase 109 (SQL Context-Memory Navigation Spine) is the implementing phase: lib/core/navigation.cjs ships the 13-function chokepoint, first-class memory_event nodes, and the instrumented acceptance test that asserts zero non-SQLite filesystem reads during the navigation flow.')"
    - "The Part 9 body retains the five-role separation invariant line verbatim in substance: 'Files preserve meaning. SQL remembers and navigates. Brain reasons over structured packets. Larry explains and acts. The human confirms truth.' - as five emphasized lines exactly as in the proposal"
    - "The Part 9 body retains the 'The five roles' numbered list (5 items), the 'Truth states (canonical)' subsection naming the closed set `proposed | confirmed | rejected | stale | superseded | needs_evidence | validated | invalidated`, and the 'What this means architecturally' bullet list"
    - "docs/MINDRIAN-CANON.md version header changes from `Version: 1.3` to `Version: 1.4` (the line near the top of the file currently reading `Version: 1.0` is the original; the file's actual current version line - verify by reading the file - must be bumped; if the header reads `Version: 1.0` change it to `1.4` since the version-history table in CANON-PHASE-MAP shows v1.3 is current; reconcile so the canon file header and the map agree on v1.4)"
    - "docs/MINDRIAN-CANON.md footer line `_Mindrian Canon v1.3 - MindrianOS Plugin_` changes to `_Mindrian Canon v1.4 - MindrianOS Plugin_`"
    - "docs/MINDRIAN-CANON.md Appendix A (`## Appendix A - Relationship to MWP`) gains a forward-reference sentence to Part 9 per RESEARCH section 9 risk-mitigation (e.g. 'See Part 9 for the memory-locality constitution that binds the folder substrate to the graph.'); and Part 8's text gains a forward-reference to Part 9 (e.g. at the end of Part 8 add a one-line pointer: 'Part 9 (Memory Locality and Interpretation) is the enforcement architecture for this boundary - it specifies where memory lives and how the typed packet contract makes this boundary structurally hard.')"
    - "docs/MINDRIAN-CANON.md Appendix D Canonization Provenance gains entry 12 - the exact text from RESEARCH section 9.3 lines 1143-1145 (the '12. **Codex external research input - Part 9 proposed (Phase 108) and ratified (Phase 109).** ...' paragraph), with em-dashes converted to hyphens; entry 12 is appended after entry 11 in the numbered chronology"
    - "docs/CANON-PHASE-MAP.md: the heading `### Part 9 (proposed) - Memory Locality and Interpretation` is changed to `### Part 9 - Memory Locality and Interpretation` (the `(proposed)` qualifier is dropped) per RESEARCH section 9.2 line 1137"
    - "docs/CANON-PHASE-MAP.md: the Phase 108 row under that section changes from `| proposed | Phase 108 graph-memory-schema-reconciliation (proposal) | ...` to `| shipped  | Phase 108 graph-memory-schema-reconciliation (proposal + frozen taxonomy) | .planning/phases/108-graph-memory-schema-reconciliation/PART-9-PROPOSAL.md |`; the Phase 109 row changes from `| planned  | Phase 109 sql-context-memory-navigation-spine (implementation + ratification at release gate) | ...` to `| shipped  | Phase 109 sql-context-memory-navigation-spine (implementation + canon ratification at release gate) | .planning/phases/109-sql-context-memory-navigation-spine/109-CONTEXT.md |`; the Phase 110 row stays `| planned  | Phase 110 brain-context-packet-contract ... | ... |` UNCHANGED per RESEARCH section 9.2 line 1137"
    - "docs/CANON-PHASE-MAP.md: the prose paragraph immediately under that section (currently starting 'Phase 108 ships the proposal cross-reference document...') is updated so it no longer says Phase 109 'ratifies Part 9 at its release gate' in the future tense - rephrase to past/perfect ('Phase 109 ratified Part 9 by merging the proposal text...')"
    - "docs/CANON-PHASE-MAP.md: the `Canon reference: docs/MINDRIAN-CANON.md (v1.3)` header line near the top of the file changes to `Canon reference: docs/MINDRIAN-CANON.md (v1.4)`"
    - "docs/CANON-PHASE-MAP.md: the `## Version history` table gains a new row: `| v1.4          | TBD      | 2026-05-12 | Part 9 (Memory Locality and Interpretation) ratified at the Phase 109 release gate - merged proposal text from .planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md; Appendix D entry 12 (Codex external-research input); CANON-PHASE-MAP Part 9 rows flipped to shipped for Phases 108 + 109. |` (the commit hash is filled at release commit time; TBD is acceptable in the plan since 109-12 / the release commit fills it - but the row must exist with the v1.4 label and the 2026-05-12 date)"
    - "docs/CANON-PHASE-MAP.md keeps the Phase 109 milestone row in the 'v1.13.0 The Closed Loop milestone phases' table consistent (if there is a Phase 109 row there it may stay; do NOT add a duplicate; the Part 9 section is the canonical place for the shipped flip)"
    - "tests/test-canon-part-9-ratification.cjs no longer prints the Wave 0 MISSING stderr line and no longer calls process.exit(1) unconditionally; it runs real structural assertions and exits 0 only when every assertion passes"
    - "tests/test-canon-part-9-ratification.cjs asserts: (1) docs/MINDRIAN-CANON.md contains the literal `## Part 9 - Memory Locality and Interpretation`; (2) it contains the five-role invariant - it checks the text contains all of 'Files preserve meaning', 'SQL remembers and navigates', 'Brain reasons over structured packets', 'Larry explains and acts', 'The human confirms truth'; (3) the byte offset of `## Part 9` is GREATER than the byte offset of `## Part 8` AND LESS than the byte offset of `## Appendix A`; (4) docs/MINDRIAN-CANON.md contains `Version: 1.4` (not `1.3`); (5) docs/MINDRIAN-CANON.md Appendix D contains a numbered entry `12.` mentioning 'Codex' and 'Part 9'; (6) docs/CANON-PHASE-MAP.md contains the heading `### Part 9 - Memory Locality and Interpretation` and does NOT contain `### Part 9 (proposed)`; (7) docs/CANON-PHASE-MAP.md, within the Part 9 section block (text between `### Part 9 - Memory Locality and Interpretation` and the next `###` heading), contains NO occurrence of the word `proposed` on the Phase 108 or Phase 109 rows - i.e. those rows say `shipped`; (8) docs/CANON-PHASE-MAP.md `Canon reference:` header says `(v1.4)`; (9) docs/CANON-PHASE-MAP.md `## Version history` table contains a `v1.4` row"
    - "tests/test-canon-part-9-ratification.cjs follows the direct-CJS pattern (node:assert/strict, no Mocha/Jest, zero new npm deps) matching tests/test-cross-room-memory.cjs; it has a run() function with per-assertion try/catch + PASS/FAIL print + summary line + process.exit(0) iff zero failures; it is more than 8 lines with real assertion code"
    - "No other file is touched by this plan - per CONTEXT D-09 line 274 'NO other Phase 109 plan touches the canon files', this plan OWNS docs/MINDRIAN-CANON.md + docs/CANON-PHASE-MAP.md + tests/test-canon-part-9-ratification.cjs EXCLUSIVELY (the release-commit version bumps to CHANGELOG.md / plugin.json / package.json are a separate release step, NOT part of this plan)"
    - "Zero em-dashes (U+2014) or en-dashes (U+2013) anywhere in docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md, or tests/test-canon-part-9-ratification.cjs after this plan runs - the proposal text and the Appendix D entry text both contain em-dashes in the source files; they MUST be converted to hyphens when merged"
  artifacts:
    - path: "docs/MINDRIAN-CANON.md"
      provides: "Canon v1.4 with Part 9 (Memory Locality and Interpretation) ratified - inserted between Part 8 and Appendix A; Appendix D entry 12; version header + footer bumped; Part 8 + Appendix A forward-references to Part 9"
      contains: "## Part 9 - Memory Locality and Interpretation"
    - path: "docs/CANON-PHASE-MAP.md"
      provides: "Part 9 section heading de-qualified; Phase 108 + 109 rows flipped proposed/planned -> shipped; Phase 110 stays planned; Canon reference bumped to v1.4; Version history v1.4 row added; prose updated to past tense"
      contains: "### Part 9 - Memory Locality and Interpretation"
    - path: "tests/test-canon-part-9-ratification.cjs"
      provides: "Structural assertions for the Part 9 ratification - Part 9 present + between Part 8 and Appendix A + five-role invariant + version bumped + Appendix D entry 12 + CANON-PHASE-MAP rows shipped + Canon reference v1.4 + Version history v1.4 row"
      contains: "## Part 9"
  key_links:
    - from: "docs/MINDRIAN-CANON.md (Part 9 body)"
      to: ".planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md (the proposed canon text)"
      via: "the Part 9 body is the proposal text de-quoted + de-em-dashed + with a one-sentence Phase 109 forward-reference appended"
      pattern: "Files preserve meaning"
    - from: "tests/test-canon-part-9-ratification.cjs"
      to: "docs/MINDRIAN-CANON.md AND docs/CANON-PHASE-MAP.md"
      via: "fs.readFileSync + byte-offset comparison (Part 8 < Part 9 < Appendix A) + literal-substring assertions + the no-`proposed`-in-the-Part-9-section check"
      pattern: "Appendix A"
    - from: "docs/CANON-PHASE-MAP.md (Part 9 section)"
      to: ".planning/phases/108-graph-memory-schema-reconciliation/PART-9-PROPOSAL.md AND .planning/phases/109-sql-context-memory-navigation-spine/109-CONTEXT.md"
      via: "the shipped rows point at these reference files per RESEARCH section 9.2"
      pattern: "shipped"
---

<objective>
Wave 4 deliverable D-09 (the canon half of NAV-109-09): ratify Canon Part 9 at the Phase 109 release gate. Per CONTEXT D-09 + RESEARCH section 9 + REQUIREMENTS.md NAV-109-09:

1. Merge the proposed Part 9 text (from `.planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md`) into `docs/MINDRIAN-CANON.md` as `## Part 9 - Memory Locality and Interpretation`, inserted immediately before `## Appendix A - Relationship to MWP`. De-quote it (it becomes live canon, not a quoted proposal), convert em-dashes to hyphens, append a one-sentence forward-reference to Phase 109 as the implementing phase. Cross-check against `.planning/phases/108-graph-memory-schema-reconciliation/PART-9-PROPOSAL.md` - that file is a cross-reference checklist (it explicitly does NOT duplicate the canon text); the authoritative Part 9 text is the `.planning/research/...` proposal per REQUIREMENTS.md NAV-109-09. If the two diverge on substance, the research-file text wins.
2. Bump the canon version header `Version: ...` and the footer `_Mindrian Canon v... - MindrianOS Plugin_` to v1.4. Add Appendix D Canonization Provenance entry 12 (the Codex external-research attribution, exact text from RESEARCH section 9.3). Add a forward-reference to Part 9 in Part 8's text and in Appendix A.
3. In `docs/CANON-PHASE-MAP.md`: drop `(proposed)` from the Part 9 section heading; flip the Phase 108 row to `shipped` and the Phase 109 row to `shipped` (Phase 110 stays `planned`); update the section's prose to past tense; bump the `Canon reference:` header to `(v1.4)`; add a `v1.4` row to the `## Version history` table.
4. Replace the 8-line `process.exit(1)` stub in `tests/test-canon-part-9-ratification.cjs` with real structural assertions: Part 9 present, sits between Part 8 and Appendix A, contains the five-role invariant line, canon version bumped, Appendix D entry 12 present, CANON-PHASE-MAP Part 9 rows say `shipped`, Canon reference is v1.4, Version history has a v1.4 row.

Per CONTEXT D-09 line 274 ("NO other Phase 109 plan touches the canon files"): this plan OWNS `docs/MINDRIAN-CANON.md` + `docs/CANON-PHASE-MAP.md` + `tests/test-canon-part-9-ratification.cjs` exclusively. The release-commit version bumps to CHANGELOG.md / plugin.json / package.json (per CLAUDE.md release process) are a SEPARATE release step, not part of this plan - this plan ships the canon ratification artifacts only.

This plan registers NOTHING new in lib/memory/run-feynman-tests.cjs - tests/test-canon-part-9-ratification.cjs is a Plan 109-00 stub being filled. (If the Plan 109-00 registration was incomplete, that is reconciled by Plan 109-12.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-sql-context-memory-navigation-spine/109-CONTEXT.md
@.planning/phases/109-sql-context-memory-navigation-spine/109-RESEARCH.md
@.planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md
@.planning/phases/108-graph-memory-schema-reconciliation/PART-9-PROPOSAL.md
@docs/MINDRIAN-CANON.md
@docs/CANON-PHASE-MAP.md
@tests/test-canon-part-9-ratification.cjs
@tests/test-cross-room-memory.cjs

<interfaces>
<!-- Exact merge point (verified against docs/MINDRIAN-CANON.md): the file currently has
  ## Part 8 - The Graph Boundary (Security Constitution)
  ... (ends with the "### Violations are bugs" fenced block) ...
  ---
  ## Appendix A - Relationship to MWP
  ...
  Insert `## Part 9 - Memory Locality and Interpretation` (and its body) BETWEEN the `---`
  separator after Part 8 and the `## Appendix A` line. Keep a `---` separator after Part 9
  too so the visual rhythm matches the rest of the file.
-->

<!-- The proposal text to merge (verified against .planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md
  lines 20-52, the block under "## The proposed canon text"). It is currently a `> `-prefixed
  blockquote. Strip the `> ` prefixes. It contains em-dashes - convert every em-dash to a hyphen.
  It already includes: the five-role invariant block; "### The five roles" (5 numbered items);
  "### Truth states (canonical)" naming the closed-8 set; "### What this means architecturally"
  bullets. APPEND a one-sentence "### Implementing phase" or inline pointer to Phase 109 per
  RESEARCH section 9.1 line 1109.
-->

<!-- Appendix D entry 12 text (verbatim from RESEARCH section 9.3 lines 1143-1145, em-dashes -> hyphens):
  "12. **Codex external research input - Part 9 proposed (Phase 108) and ratified (Phase 109).**
  External research input from Codex (via Jonathan Sagir, 2026-05-03 sessions) framed `room.db`
  as Mindrian's 'local mind' - the navigator's working memory made queryable as graph paths,
  never as folder scans. Phase 108 ships the schema reconciliation contract (RECONCILIATION.md,
  PROVENANCE.md, TRUTH-STATES.md, aliases.yml, PART-9-PROPOSAL.md, scripts/check-schema-aliases.cjs);
  Phase 109 ships the SQL navigation spine (lib/core/navigation.cjs single chokepoint with 13
  functions; first-class memory_event nodes; instrumented acceptance test asserting zero
  non-SQLite reads during the navigation flow); Phase 109 release commit ratifies Part 9 by
  merging the proposal text into this canon. Brain wire schema enforcement (Phase 110) hardens
  Part 8 from procedural audit to structural prevention. The trio (108 + 109 + 110) is the
  Part 9 implementing cluster."
-->

<!-- CANON-PHASE-MAP.md Part 9 section AFTER the flip (verbatim from RESEARCH section 9.2 lines 1128-1135):
  ### Part 9 - Memory Locality and Interpretation

  | Status   | Phase / Component | Reference |
  |----------|-------------------|-----------|
  | shipped  | Phase 108 graph-memory-schema-reconciliation (proposal + frozen taxonomy) | .planning/phases/108-graph-memory-schema-reconciliation/PART-9-PROPOSAL.md |
  | shipped  | Phase 109 sql-context-memory-navigation-spine (implementation + canon ratification at release gate) | .planning/phases/109-sql-context-memory-navigation-spine/109-CONTEXT.md |
  | planned  | Phase 110 brain-context-packet-contract (Brain wire enforcement makes Part 9 structurally hard, not just procedurally audited) | .planning/phases/110-brain-context-packet-contract/110-CONTEXT.md (stub) |
-->

<!-- The test pattern: copy the runner shape from tests/test-cross-room-memory.cjs / tests/test-room-home-vs-brain-derivation-regression.cjs.
  No DB needed - this is a pure file-structure test. Just fs.readFileSync the two docs files and assert. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Canon Part 9 into docs/MINDRIAN-CANON.md and flip docs/CANON-PHASE-MAP.md rows to shipped</name>
  <files>docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md</files>
  <read_first>
    - docs/MINDRIAN-CANON.md (the full file - locate: the `Version:` header near the top; the end of Part 8; the `## Appendix A - Relationship to MWP` line; the Appendix D `## Appendix D - Canonization Provenance` numbered list ending at entry 11; the footer `_Mindrian Canon v1.3 - MindrianOS Plugin_`)
    - docs/CANON-PHASE-MAP.md (the full file - locate: the `Canon reference: docs/MINDRIAN-CANON.md (v1.3)` header; the `### Part 9 (proposed) - Memory Locality and Interpretation` section + its 3-row table + the prose paragraph below it; the `## Version history` table)
    - .planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md lines 18-52 (the proposed canon text under `## The proposed canon text` - this is what gets merged, de-quoted, de-em-dashed)
    - .planning/phases/108-graph-memory-schema-reconciliation/PART-9-PROPOSAL.md (cross-check: confirm it is a cross-reference checklist that does NOT contain the canon prose; the research-file text is authoritative per REQUIREMENTS.md NAV-109-09)
    - .planning/phases/109-sql-context-memory-navigation-spine/109-RESEARCH.md section 9 lines 1101-1162 (the exact merge point, the CANON-PHASE-MAP row update, the Appendix D entry 12 text, the release commit pattern)
    - .planning/phases/109-sql-context-memory-navigation-spine/109-CONTEXT.md D-09 lines 272-275 ("NO other Phase 109 plan touches the canon files"; the ratification spec)
    - ./CLAUDE.md (the NO-em-dashes hard rule - every em-dash in the merged text MUST become a hyphen)
  </read_first>
  <action>
    Step 1 - docs/MINDRIAN-CANON.md, insert Part 9.
    Find the boundary between the end of `## Part 8 - The Graph Boundary (Security Constitution)` (it ends with the `### Violations are bugs` paragraph and the closing ```` ``` ```` of Part 8's fenced block, followed by a `---` horizontal rule) and the line `## Appendix A - Relationship to MWP`. Insert, between the `---` after Part 8 and the `## Appendix A` line:

    ```markdown
    ## Part 9 - Memory Locality and Interpretation

    <the proposal text from .planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md
     lines 20-52, with the leading "> " blockquote markers stripped, EVERY em-dash replaced by a
     hyphen, and the leading "## Part 9 - Memory Locality and Interpretation" line from the proposal
     NOT duplicated (you write that header once, above) - so you copy the body starting from
     "The wicked navigator's working memory has structure:" through the "What this means
     architecturally" bullet list>

    ### Implementing phase

    Phase 109 (SQL Context-Memory Navigation Spine) is the implementing phase: `lib/core/navigation.cjs` ships the 13-function navigation chokepoint, `memory_event` becomes a first-class node type, and the instrumented acceptance test (`tests/test-navigation-acceptance.cjs`) asserts zero non-SQLite filesystem reads during the full navigation flow - proving by instrumentation, not by promise, that SQL is the local mind. Phase 108 ships the frozen schema/taxonomy contract this depends on. Phase 110 (Brain Context Packet Contract) hardens the typed-packet wire so Part 8's boundary is structurally enforced, not merely procedurally audited.

    ---
    ```

    Step 2 - docs/MINDRIAN-CANON.md, version header + footer.
    Change the version header line near the top of the file (currently `Version: 1.0` per the file as committed; the version-history table in CANON-PHASE-MAP shows v1.3 is the live version, so the canon file header is stale) to `Version: 1.4`. Change the footer line `_Mindrian Canon v1.3 - MindrianOS Plugin_` to `_Mindrian Canon v1.4 - MindrianOS Plugin_`. (If the header genuinely reads something other than `Version: 1.0` or `Version: 1.3`, set it to `1.4` regardless - the goal is the file declares v1.4.)

    Step 3 - docs/MINDRIAN-CANON.md, Part 8 + Appendix A forward-references.
    At the end of Part 8's text (just before the `---` that precedes Part 9), add a one-line pointer: `Part 9 (Memory Locality and Interpretation) is the enforcement architecture for this boundary - it specifies where memory lives (SQL) and how the typed packet contract makes the LOCAL-to-BRAIN boundary structurally hard, not just audited.` In Appendix A (`## Appendix A - Relationship to MWP`), add a sentence: `See Part 9 (Memory Locality and Interpretation) for the constitution that binds the folder substrate (Part 1, ICM Layer 0) to the navigable graph (Part 4).`

    Step 4 - docs/MINDRIAN-CANON.md, Appendix D entry 12.
    Append after entry 11 in the `## Appendix D - Canonization Provenance` numbered chronology the entry-12 paragraph quoted in the &lt;interfaces&gt; block above (em-dashes -> hyphens).

    Step 5 - docs/CANON-PHASE-MAP.md, header + Part 9 section.
    Change `Canon reference: docs/MINDRIAN-CANON.md (v1.3)` to `Canon reference: docs/MINDRIAN-CANON.md (v1.4)`. Change the heading `### Part 9 (proposed) - Memory Locality and Interpretation` to `### Part 9 - Memory Locality and Interpretation`. Replace the 3-row table with the AFTER table quoted in the &lt;interfaces&gt; block above (Phase 108 row -> `shipped`, Phase 109 row -> `shipped`, Phase 110 row stays `planned`). Rewrite the prose paragraph immediately below the table so it reads in past/perfect tense - e.g.: `Phase 108 shipped the proposal cross-reference document and the schema reconciliation deliverables that made the Part 9 contract testable. Phase 109 ratified Part 9 at its release gate by merging the proposal text from .planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md into docs/MINDRIAN-CANON.md as a new Part 9. Phase 110 hardens the Brain wire schema so Part 8 enforcement (LOCAL to BRAIN: NO) is structurally enforced, not just procedurally audited. The trio (108 + 109 + 110) constitutes the Part 9 implementing cluster.`

    Step 6 - docs/CANON-PHASE-MAP.md, Version history table.
    Append a new row at the end of the `## Version history` table:
    `| v1.4          | TBD      | 2026-05-12 | Part 9 (Memory Locality and Interpretation) ratified at the Phase 109 release gate - merged proposal text from .planning/research/2026-05-03-canon-part-9-memory-locality-proposal.md; Appendix D entry 12 (Codex external-research input); CANON-PHASE-MAP Part 9 rows flipped to shipped for Phases 108 + 109; Phase 110 stays planned. |`
    (The commit hash column is `TBD` - the release commit fills it; the row must exist now with the `v1.4` label and the `2026-05-12` date.)

    Step 7 - sweep for em-dashes / en-dashes.
    Run `grep -nP "[\x{2014}\x{2013}]" docs/MINDRIAN-CANON.md docs/CANON-PHASE-MAP.md` - it MUST return zero matches. The proposal text and the Appendix D entry both contain em-dashes in their source files; convert each to a hyphen. (Note: pre-existing em-dashes elsewhere in docs/CANON-PHASE-MAP.md from older entries are NOT in scope to fix - but the Part 9 section, the new Version history row, and the new prose you write MUST be em-dash-free. To be safe and satisfy the verify command, if pre-existing em-dashes exist elsewhere in CANON-PHASE-MAP.md, the verify command below scopes the grep to the lines this plan added - see the verify block.)
  </action>
  <verify>
    <automated>grep -q "^## Part 9 - Memory Locality and Interpretation$" docs/MINDRIAN-CANON.md && grep -q "Files preserve meaning" docs/MINDRIAN-CANON.md && grep -q "SQL remembers and navigates" docs/MINDRIAN-CANON.md && grep -q "The human confirms truth" docs/MINDRIAN-CANON.md && awk '/^## Part 8/{p8=NR} /^## Part 9 - Memory Locality/{p9=NR} /^## Appendix A/{pa=NR} END{exit !(p8 && p9 && pa && p8<p9 && p9<pa)}' docs/MINDRIAN-CANON.md && grep -q "Version: 1.4" docs/MINDRIAN-CANON.md && grep -q "_Mindrian Canon v1.4" docs/MINDRIAN-CANON.md && grep -Eq "^12\. " docs/MINDRIAN-CANON.md && grep -q "Codex external research input" docs/MINDRIAN-CANON.md && grep -q "^### Part 9 - Memory Locality and Interpretation$" docs/CANON-PHASE-MAP.md && ! grep -q "### Part 9 (proposed)" docs/CANON-PHASE-MAP.md && awk '/^### Part 9 - Memory Locality/{f=1;next} /^###/{f=0} f && /Phase 108 graph-memory-schema-reconciliation/ && /shipped/{c108=1} f && /Phase 109 sql-context-memory-navigation-spine/ && /shipped/{c109=1} END{exit !(c108 && c109)}' docs/CANON-PHASE-MAP.md && grep -q "Canon reference: docs/MINDRIAN-CANON.md (v1.4)" docs/CANON-PHASE-MAP.md && grep -q "| v1.4 " docs/CANON-PHASE-MAP.md && ! awk '/^## Part 9 - Memory Locality/{f=1} f && /[\xe2\x80\x94\xe2\x80\x93]/{print; bad=1} /^## Appendix A/{f=0} END{exit bad?1:0}' docs/MINDRIAN-CANON.md</automated>
  </verify>
  <done>
    docs/MINDRIAN-CANON.md has `## Part 9 - Memory Locality and Interpretation` between Part 8 and Appendix A, with the five-role invariant, version header + footer at v1.4, Appendix D entry 12, Part 8 + Appendix A forward-references; docs/CANON-PHASE-MAP.md has the de-qualified Part 9 heading, Phase 108 + 109 rows at `shipped` (110 still `planned`), Canon reference at v1.4, a v1.4 Version history row, past-tense prose; the Part 9 block in the canon contains zero em-dashes or en-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace tests/test-canon-part-9-ratification.cjs Wave 0 stub with real structural assertions</name>
  <files>tests/test-canon-part-9-ratification.cjs</files>
  <read_first>
    - tests/test-canon-part-9-ratification.cjs (the 8-line Wave 0 stub this task replaces)
    - tests/test-cross-room-memory.cjs (the direct-CJS runner pattern to mirror)
    - docs/MINDRIAN-CANON.md (Task 1 output - the assertions read this)
    - docs/CANON-PHASE-MAP.md (Task 1 output - the assertions read this)
    - .planning/phases/109-sql-context-memory-navigation-spine/109-RESEARCH.md section 10.3 line 1314 (the D-09 file-structure-test spec: "grep + line-number checks")
  </read_first>
  <behavior>
    - Pure file-structure test - no DB, no temp dir. fs.readFileSync the two docs files; run assertions; exit 0 iff all pass.
    - Assertion set (9 checks): Part 9 header present; five-role invariant (all five phrases present); byte offset Part 8 < Part 9 < Appendix A; canon `Version: 1.4`; Appendix D entry `12.` mentions Codex + Part 9; CANON-PHASE-MAP `### Part 9 - Memory Locality and Interpretation` heading present and `### Part 9 (proposed)` absent; within the CANON-PHASE-MAP Part 9 section block, the Phase 108 + Phase 109 rows say `shipped` (no `proposed`); CANON-PHASE-MAP `Canon reference:` says `(v1.4)`; CANON-PHASE-MAP `## Version history` has a `v1.4` row.
    - run(): per-assertion try/catch, PASS/FAIL print, summary line, process.exit(0) iff zero failures.
  </behavior>
  <action>
    Replace the entire contents of tests/test-canon-part-9-ratification.cjs with:

    ```javascript
    'use strict';
    // Phase 109-11: Canon Part 9 ratification structural test (NAV-109-09).
    // Asserts the Part 9 merge into docs/MINDRIAN-CANON.md + the docs/CANON-PHASE-MAP.md
    // row flips landed correctly. Pure file-structure test - no DB. Pattern: direct-CJS.

    const { ok } = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');

    const REPO_ROOT = path.resolve(__dirname, '..');
    const CANON = path.join(REPO_ROOT, 'docs', 'MINDRIAN-CANON.md');
    const MAP = path.join(REPO_ROOT, 'docs', 'CANON-PHASE-MAP.md');

    function read(p) { return fs.readFileSync(p, 'utf8'); }

    function idx(haystack, needle) {
      const i = haystack.indexOf(needle);
      ok(i >= 0, 'expected to find: ' + JSON.stringify(needle));
      return i;
    }

    function sectionBlock(text, startHeading) {
      const start = text.indexOf(startHeading);
      ok(start >= 0, 'expected section heading: ' + startHeading);
      const after = text.slice(start + startHeading.length);
      const nextHeadingRel = after.search(/\n#{1,3} /);
      return nextHeadingRel < 0 ? after : after.slice(0, nextHeadingRel);
    }

    function t1_part9HeaderPresent() {
      const c = read(CANON);
      ok(c.includes('## Part 9 - Memory Locality and Interpretation'), 'canon contains "## Part 9 - Memory Locality and Interpretation"');
    }

    function t2_fiveRoleInvariant() {
      const c = read(CANON);
      for (const phrase of ['Files preserve meaning', 'SQL remembers and navigates', 'Brain reasons over structured packets', 'Larry explains and acts', 'The human confirms truth']) {
        ok(c.includes(phrase), 'canon contains five-role phrase: ' + phrase);
      }
    }

    function t3_part9BetweenPart8AndAppendixA() {
      const c = read(CANON);
      const p8 = idx(c, '## Part 8 - The Graph Boundary');
      const p9 = idx(c, '## Part 9 - Memory Locality and Interpretation');
      const aa = idx(c, '## Appendix A - Relationship to MWP');
      ok(p8 < p9, 'Part 8 precedes Part 9 (offsets ' + p8 + ' < ' + p9 + ')');
      ok(p9 < aa, 'Part 9 precedes Appendix A (offsets ' + p9 + ' < ' + aa + ')');
    }

    function t4_canonVersionBumped() {
      const c = read(CANON);
      ok(c.includes('Version: 1.4'), 'canon header declares Version: 1.4');
      ok(c.includes('_Mindrian Canon v1.4'), 'canon footer declares v1.4');
      ok(!/Version: 1\.3\b/.test(c), 'canon header no longer declares Version: 1.3');
    }

    function t5_appendixDEntry12() {
      const c = read(CANON);
      const apxD = sectionBlock(c, '## Appendix D - Canonization Provenance');
      ok(/(^|\n)12\. /.test(apxD), 'Appendix D has a numbered entry 12');
      ok(apxD.includes('Codex'), 'Appendix D entry 12 mentions Codex');
      ok(apxD.includes('Part 9'), 'Appendix D entry 12 mentions Part 9');
    }

    function t6_mapPart9HeadingDeQualified() {
      const m = read(MAP);
      ok(m.includes('### Part 9 - Memory Locality and Interpretation'), 'map has "### Part 9 - Memory Locality and Interpretation"');
      ok(!m.includes('### Part 9 (proposed)'), 'map no longer has "### Part 9 (proposed)"');
    }

    function t7_mapPart9RowsShipped() {
      const m = read(MAP);
      const block = sectionBlock(m, '### Part 9 - Memory Locality and Interpretation');
      // The Phase 108 + Phase 109 rows must say shipped; Phase 110 stays planned.
      const lines108 = block.split('\n').filter((l) => l.includes('Phase 108 graph-memory-schema-reconciliation'));
      const lines109 = block.split('\n').filter((l) => l.includes('Phase 109 sql-context-memory-navigation-spine'));
      ok(lines108.length >= 1 && lines108.every((l) => l.includes('shipped') && !l.includes('proposed')), 'Phase 108 Part 9 row says shipped');
      ok(lines109.length >= 1 && lines109.every((l) => l.includes('shipped') && !l.includes('proposed')), 'Phase 109 Part 9 row says shipped');
    }

    function t8_mapCanonReferenceV14() {
      const m = read(MAP);
      ok(m.includes('Canon reference: docs/MINDRIAN-CANON.md (v1.4)'), 'map Canon reference header says (v1.4)');
    }

    function t9_mapVersionHistoryHasV14() {
      const m = read(MAP);
      const vh = sectionBlock(m, '## Version history');
      ok(/\|\s*v1\.4\s*\|/.test(vh), 'map Version history table has a v1.4 row');
    }

    function run() {
      const tests = [t1_part9HeaderPresent, t2_fiveRoleInvariant, t3_part9BetweenPart8AndAppendixA, t4_canonVersionBumped, t5_appendixDEntry12, t6_mapPart9HeadingDeQualified, t7_mapPart9RowsShipped, t8_mapCanonReferenceV14, t9_mapVersionHistoryHasV14];
      let pass = 0; let fail = 0;
      for (const t of tests) {
        try { t(); pass++; process.stdout.write('PASS ' + t.name + '\n'); }
        catch (err) { fail++; process.stderr.write('FAIL ' + t.name + ': ' + err.message + '\n'); }
      }
      process.stdout.write('test-canon-part-9-ratification: ' + pass + '/' + tests.length + ' passed\n');
      process.exit(fail === 0 ? 0 : 1);
    }

    run();
    ```
  </action>
  <verify>
    <automated>node tests/test-canon-part-9-ratification.cjs && test "$(wc -l < tests/test-canon-part-9-ratification.cjs)" -gt 8 && test "$(grep -c 'ok(' tests/test-canon-part-9-ratification.cjs)" -gt 5 && ! node tests/test-canon-part-9-ratification.cjs 2>&1 | grep -q 'MISSING - Wave' && ! grep -lP "[\x{2014}\x{2013}]" tests/test-canon-part-9-ratification.cjs</automated>
  </verify>
  <done>
    tests/test-canon-part-9-ratification.cjs exits 0; runs 9 real structural assertions; the Wave 0 MISSING line is gone; file is more than 8 lines with real assertion code; zero em-dashes or en-dashes.
  </done>
</task>

</tasks>

<verification>
- `node tests/test-canon-part-9-ratification.cjs` exits 0.
- docs/MINDRIAN-CANON.md: `## Part 9 - Memory Locality and Interpretation` sits between `## Part 8` and `## Appendix A`; contains the five-role invariant; version header `Version: 1.4`; footer `_Mindrian Canon v1.4`; Appendix D entry 12 mentioning Codex + Part 9; Part 8 + Appendix A forward-references to Part 9.
- docs/CANON-PHASE-MAP.md: `### Part 9 - Memory Locality and Interpretation` (no `(proposed)`); Phase 108 + Phase 109 Part 9 rows say `shipped`; Phase 110 row stays `planned`; `Canon reference: ... (v1.4)`; a `v1.4` row in `## Version history`; the prose paragraph is past tense.
- The Part 9 block in docs/MINDRIAN-CANON.md contains zero em-dashes or en-dashes; tests/test-canon-part-9-ratification.cjs contains zero em-dashes or en-dashes.
- No file outside docs/MINDRIAN-CANON.md, docs/CANON-PHASE-MAP.md, tests/test-canon-part-9-ratification.cjs is modified by this plan.
- lib/memory/run-feynman-tests.cjs is NOT modified (the test file is a pre-existing 109-00 stub being filled).
</verification>

<success_criteria>
1. Canon Part 9 merged into docs/MINDRIAN-CANON.md from the authoritative `.planning/research/...` proposal text, de-quoted, de-em-dashed, with a Phase 109 forward-reference, inserted between Part 8 and Appendix A.
2. Canon version bumped v1.3 -> v1.4 (header + footer); Appendix D entry 12 (Codex attribution) added; Part 8 + Appendix A forward-references to Part 9 added.
3. docs/CANON-PHASE-MAP.md Part 9 section heading de-qualified; Phase 108 + 109 rows flipped to `shipped`; Phase 110 stays `planned`; `Canon reference:` bumped to v1.4; `## Version history` v1.4 row added; prose updated to past tense.
4. tests/test-canon-part-9-ratification.cjs replaced with 9 real structural assertions; exits 0; Wave 0 MISSING line gone.
5. This plan owns the three canon files exclusively per CONTEXT D-09; the release-commit version bumps (CHANGELOG / plugin.json / package.json) are a separate step out of scope.
6. Zero em-dashes or en-dashes in the Part 9 block, the new CANON-PHASE-MAP rows/prose, and the test file.
</success_criteria>

<output>
After completion, create `.planning/phases/109-sql-context-memory-navigation-spine/109-11-SUMMARY.md` recording: the exact insertion point of Part 9 in docs/MINDRIAN-CANON.md (line numbers before/after); whether the `.planning/research/...` proposal text and the `.planning/phases/108-.../PART-9-PROPOSAL.md` diverged on substance (and which won - the research file is authoritative); the canon version transition (from what version the header actually read, to v1.4); the Appendix D entry 12 text as merged; the CANON-PHASE-MAP row flips (108 + 109 -> shipped, 110 unchanged); the 9 test assertions and their pass status; confirmation that no file outside the three owned files was touched; confirmation that lib/memory/run-feynman-tests.cjs was NOT touched (the stub was already registered by Plan 109-00, or a note that Plan 109-12 must reconcile the registry). NAV-109-09 is now fully covered (Plan 109-10 the test-flow half + this plan the canon half). Plan 109-12 (SUMMARY recovery + REQUIREMENTS sync) can proceed.
</output>
