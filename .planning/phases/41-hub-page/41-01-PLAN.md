---
phase: 41-hub-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-snapshot.cjs
autonomous: true
requirements:
  - HUB-01
  - HUB-02
  - HUB-03
  - ATF-01
  - ATF-02

must_haves:
  truths:
    - "Stats bar always shows 5 counters including Grants even when 0"
    - "Breakthrough cards appear above fold when ADJACENT_POSSIBLE nodes exist in graph"
    - "Breakthrough section is completely absent when no breakthrough nodes exist"
    - "Opportunities render as scored list from funding-strategy/ articles"
    - "Opportunities show CTA when no funding-strategy articles exist"
    - "View cards grid shows 4 cards with conditional grayed-out state"
    - "Section cards render in 4-column grid with dashed empty-section indicators"
  artifacts:
    - path: "scripts/generate-snapshot.cjs"
      provides: "Data extraction for breakthroughs, opportunities, view cards, section grid refinement"
      contains: "extractBreakthroughs"
  key_links:
    - from: "scanRoom()"
      to: "renderBrandedHtml()"
      via: "model.breakthroughs, model.opportunities, model.views"
      pattern: "model\\.breakthroughs"
---

<objective>
Add above-the-fold content and data extraction to the snapshot generator: stats bar refinement (always show 5 counters per D-01/D-02), breakthrough angle cards from ADJACENT_POSSIBLE graph nodes (ATF-01/D-19), opportunities scored list from funding-strategy/ (ATF-02/D-20), view cards grid (HUB-02/D-03 to D-06), and section card grid refinement to 4-column with proper empty states (HUB-03/D-07 to D-09).

Purpose: The hub page needs forward-looking content (breakthroughs, opportunities) above the fold, plus navigable view cards and refined section overview.
Output: Updated generate-snapshot.cjs with 5 new data extractors and corresponding HTML template sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-hub-page/41-CONTEXT.md
@docs/EXPORT-DESIGN-BRIEF.md
@scripts/generate-snapshot.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data extractors to scanRoom and model</name>
  <files>scripts/generate-snapshot.cjs</files>
  <read_first>
    - scripts/generate-snapshot.cjs (full file -- understand scanRoom, model shape, renderBrandedHtml)
    - ~/demo-cancer-room/room/graph.json (understand node/edge data shape -- elements.nodes[].data.layer, elements.edges[].data.type)
    - ~/demo-cancer-room/room/RED-TEAM-REPORT.md (frontmatter with severity_summary, methodology: red-team)
    - ~/demo-cancer-room/room/funding-strategy/opportunity-scan.md (frontmatter with methodology: opportunity-scan)
  </read_first>
  <action>
Add 4 new extraction functions AFTER the existing scanRoom function and call them from within scanRoom before the return statement. Each function receives the already-scanned data and returns structured results added to the model object.

**1. extractBreakthroughs(graphNodes)** -- per D-19, ATF-01
- Filter graph nodes where `node.data.layer === 'breakthrough'`
- For each, extract: id, label, description (from node.data.description or ''), tag (from node.data.classes or 'Cross-Domain')
- Return array of {id, label, description, tag}
- If none found, return empty array (section silently skips per AD-8)

**2. extractOpportunities(roomDir, sections)** -- per D-20, ATF-02
- Find the funding-strategy or opportunity-bank section in the sections array
- For each article in that section, re-read the full file content
- Parse frontmatter for: relevance_score, funder, program, amount, deadline
- If no structured frontmatter fields, scan the markdown body for "## Results" or numbered list items matching pattern like `### {N}. {name}` or `**{N}/100**` or `Score: {N}`
- Sort by relevance_score descending
- Return array of {title, score, funder, program, amount, deadline}
- If no opportunities, return empty array (template will show CTA)

**3. extractViews(snapshotDir)** -- per D-03 to D-06, HUB-02
- Define 4 view objects: Intelligence Map (intelligence-map.html), Wiki (wiki.html), Doc Hub (dochub.html), Deck (deck.html)
- For each, check if the file exists in the same export folder (it won't yet -- Phase 42 creates them)
- Return array of {id, tag, title, description, filename, available: boolean, reason: string}
- When not available, reason = "Generated in future export" or similar

**4. Stats bar fix** -- per D-01, D-02, HUB-01
- Ensure model.stats.grantCount is ALWAYS present (currently conditional in template)
- The stats bar must always show all 5 counters: Sections, Articles, Connections, Gaps, Grants

Add to the model return object:
```js
breakthroughs: extractBreakthroughs(graphNodes),
opportunities: extractOpportunities(roomDir, sections),
views: extractViews(''),  // placeholder -- actual path set at write time
```

Also update the section grid: change card-grid to use `grid-template-columns: repeat(4, 1fr)` for HUB-03/D-07 (4-column grid). Add responsive fallback to 2-col at 768px and 1-col at 480px.
  </action>
  <acceptance_criteria>
    - grep -c "extractBreakthroughs" scripts/generate-snapshot.cjs returns >= 2 (definition + call)
    - grep -c "extractOpportunities" scripts/generate-snapshot.cjs returns >= 2
    - grep -c "extractViews" scripts/generate-snapshot.cjs returns >= 2
    - grep -c "model.breakthroughs" scripts/generate-snapshot.cjs returns >= 1
    - grep -c "model.opportunities" scripts/generate-snapshot.cjs returns >= 1
    - grep -c "model.views" scripts/generate-snapshot.cjs returns >= 1
    - grep "grantCount" scripts/generate-snapshot.cjs shows the stat is no longer conditional
  </acceptance_criteria>
  <verify>
    <automated>node scripts/generate-snapshot.cjs ~/demo-cancer-room/room 2>&1 | grep -q "Snapshot written" && echo "PASS: generator runs" || echo "FAIL: generator crashed"</automated>
  </verify>
  <done>scanRoom returns model with breakthroughs (5 items from demo room), opportunities (from funding-strategy), views (4 items, all unavailable), and stats always includes grantCount</done>
</task>

<task type="auto">
  <name>Task 2: Render above-fold sections and view cards in HTML template</name>
  <files>scripts/generate-snapshot.cjs</files>
  <read_first>
    - scripts/generate-snapshot.cjs (re-read after Task 1 changes -- understand updated model shape)
    - docs/EXPORT-DESIGN-BRIEF.md lines 240-290 (page structure wireframe)
    - ~/mindrian-website/website/src/components/shared/Card.tsx (READ ONLY -- card pattern)
    - ~/mindrian-website/website/src/components/shared/Tag.tsx (READ ONLY -- tag pattern)
  </read_first>
  <action>
Modify renderBrandedHtml() to add 4 new HTML sections in the template. Insert them in this order per the design brief wireframe:

**After stats bar, before section cards:**

**A. Breakthrough Angles section** (ATF-01, D-19) -- ONLY if model.breakthroughs.length > 0
- Section header: "BREAKTHROUGH ANGLES" with blue accent bar (--ds-blue)
- 3-column card grid (card-grid-3)
- Each card: blue left border (3px solid var(--ds-blue)), tag label (from breakthrough.tag, uppercase), title (breakthrough.label), description (breakthrough.description)
- Cards use .card class with hover effect
- Wrap entire section in conditional: `${model.breakthroughs.length > 0 ? '...' : ''}`

**B. Opportunities section** (ATF-02, D-20) -- always visible
- Section header: "TOP OPPORTUNITIES" with teal accent bar (--ds-teal)
- If model.opportunities.length > 0: render as a styled list/table
  - Each row: score badge (colored circle with number, JetBrains Mono), title, funder, amount, deadline
  - Score badge color: >= 90 green, >= 70 yellow, < 70 muted
  - Max 5 shown
- If model.opportunities.length === 0: render CTA card with text "Run /mos:opportunities scan to discover grants" in muted style

**C. View Cards section** (HUB-02, D-03 to D-06)
- Section header: "VIEWS" with no colored accent (use default blue)
- 3-column grid (per D-03)
- Each card per D-04: tag label, title, description, "Open >" link
- Available cards: normal styling, link to filename
- Unavailable cards per D-05: opacity 0.5, border dashed, reason text instead of link, grayed out

**D. Section cards refinement** (HUB-03, D-07 to D-09)
- Change existing card-grid to use `grid-template-columns: repeat(4, 1fr)` (per D-07)
- Already has colored left border and empty state -- verify D-08 (colored border + count + latest title) and D-09 (dashed border + muted + "No entries yet") match

**CSS additions:**
- `.card-grid-3 { grid-template-columns: repeat(3, 1fr); }` with responsive 1-col at 768px
- `.card-grid-4 { grid-template-columns: repeat(4, 1fr); }` with responsive 2-col at 768px, 1-col at 480px
- `.score-badge { ... }` for opportunity scores (inline-flex, 32px circle, JetBrains Mono, centered)
- `.opp-row { ... }` for opportunity list items
- `.view-card-disabled { opacity: 0.5; border-style: dashed; }` per D-05
- `.cta-card { ... }` for the empty opportunity CTA
- `.section-header--blue::before { background: var(--ds-blue); }`
- `.section-header--teal::before { background: var(--ds-teal); }`

**Stats bar fix:** Remove the conditional around the Grants stat. Always render all 5 counters per D-01.
  </action>
  <acceptance_criteria>
    - grep -c "BREAKTHROUGH ANGLES" scripts/generate-snapshot.cjs returns >= 1
    - grep -c "TOP OPPORTUNITIES" scripts/generate-snapshot.cjs returns >= 1
    - grep -c "VIEWS" scripts/generate-snapshot.cjs returns >= 1
    - grep -c "card-grid-3" scripts/generate-snapshot.cjs returns >= 1
    - grep -c "card-grid-4" scripts/generate-snapshot.cjs returns >= 1
    - grep -c "score-badge" scripts/generate-snapshot.cjs returns >= 1
    - grep -c "view-card-disabled" scripts/generate-snapshot.cjs returns >= 1
    - grep "grantCount" scripts/generate-snapshot.cjs does NOT show ternary/conditional wrapping the stat div
    - node scripts/generate-snapshot.cjs ~/demo-cancer-room/room runs without error
    - The output index.html contains "BREAKTHROUGH ANGLES" (demo room has 5 breakthrough nodes)
    - The output index.html contains "TOP OPPORTUNITIES"
  </acceptance_criteria>
  <verify>
    <automated>node scripts/generate-snapshot.cjs ~/demo-cancer-room/room 2>&1 && SNAP=$(ls -td ~/demo-cancer-room/room/exports/*/index.html | head -1) && grep -c "BREAKTHROUGH ANGLES" "$SNAP" && grep -c "TOP OPPORTUNITIES" "$SNAP" && grep -c "VIEWS" "$SNAP" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Hub page index.html shows: breakthrough cards (5 blue-accented cards from demo), opportunities section (with CTA or scored list), view cards (4 cards, grayed out), section cards in 4-column grid, stats bar with all 5 counters always visible</done>
</task>

</tasks>

<verification>
Run generator against demo room and verify output:
```bash
node scripts/generate-snapshot.cjs ~/demo-cancer-room/room
SNAP=$(ls -td ~/demo-cancer-room/room/exports/*/index.html | head -1)
# Check all 8 requirement areas present
grep "BREAKTHROUGH ANGLES" "$SNAP"      # ATF-01
grep "TOP OPPORTUNITIES" "$SNAP"        # ATF-02
grep "VIEWS" "$SNAP"                    # HUB-02
grep "ROOM SECTIONS" "$SNAP"            # HUB-03
grep "Sections" "$SNAP"                 # HUB-01 stats
grep "Grants" "$SNAP"                   # HUB-01 grants always
grep "card-grid-4" "$SNAP"              # HUB-03 4-col
```
</verification>

<success_criteria>
- Generator runs without errors on demo-cancer-room
- index.html contains breakthrough cards section with 5 cards
- index.html contains opportunities section
- index.html contains view cards section with 4 view cards
- index.html contains section cards in 4-column grid
- Stats bar shows all 5 counters (Sections, Articles, Connections, Gaps, Grants)
- Empty sections show dashed border and muted opacity
- Breakthrough section absent when no breakthrough nodes in graph
</success_criteria>

<output>
After completion, create `.planning/phases/41-hub-page/41-01-SUMMARY.md`
</output>
