---
phase: 160-temporal-awareness-spine
plan: 04
type: execute
wave: 2
depends_on: [160-01]
files_modified:
  - lib/core/migrations/phase-160-nodes-bitemporal.cjs
  - lib/core/migrations/phase-160-nodes-bitemporal.test.cjs
  - lib/core/navigation/transitions.cjs
  - lib/core/temporal/supersession.cjs
  - lib/core/temporal/supersession.test.cjs
  - lib/core/navigation.cjs
autonomous: true
requirements: [R7, R8]
canon_parts: [Part 4, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "The node bitemporal migration runs twice with no error and an identical result (idempotent)"
    - "Existing rows are backfilled valid_from=created_at, valid_to/invalidated_at/last_modified_at NULL"
    - "Reading a node does NOT change last_modified_at; writing does"
    - "Superseding B over A sets A.invalidated_at=now and A.valid_to=B.valid_from; A row persists, never deleted"
  artifacts:
    - path: "lib/core/migrations/phase-160-nodes-bitemporal.cjs"
      provides: "additive idempotent backfilled migration adding valid_from/valid_to/invalidated_at/last_modified_at"
      exports: ["runMigration", "SENTINEL_KEY"]
    - path: "lib/core/temporal/supersession.cjs"
      provides: "non-lossy supersession closing the old fact, never deleting"
      exports: ["supersede"]
  key_links:
    - from: "lib/core/migrations/phase-160-nodes-bitemporal.cjs"
      to: "nodes table"
      via: "ALTER TABLE ADD COLUMN (additive, duplicate-resilient) + backfill valid_from=created_at"
      pattern: "valid_from|valid_to|invalidated_at|last_modified_at"
    - from: "lib/core/temporal/supersession.cjs"
      to: "lib/core/navigation.cjs"
      via: "supersession writes through the navigation chokepoint, never a side-door delete"
      pattern: "promoteNodeStatus|logMemoryEvent|invalidated_at"
---

<objective>
Deliver Wave 4 part A - the bitemporal node model. R7 - an additive, idempotent, backfilled migration adds valid_from/valid_to/invalidated_at/last_modified_at (epoch ms) to the nodes table, mirroring the phase-109-nodes-provenance.cjs pattern; backfill valid_from=created_at, others NULL; last_modified_at updates on write only (disambiguating read vs write that last_seen_at conflates at transitions.cjs:258). R8 - non-lossy supersession: superseding B over A sets A.invalidated_at=now and A.valid_to=B.valid_from while A row persists (never deleted), written through the Part 9 chokepoint.

Purpose: Gives the system auditable temporal history and the two-axis storage the Wave 5 HITL gate needs. Generalizes the half-built Phase 150.8 edge valid-time pattern to nodes.
Output: the bitemporal migration, the last_modified_at write-only discipline, the supersession helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-temporal-awareness-spine/160-SPEC.md
@.planning/phases/160-temporal-awareness-spine/160-CONTEXT.md
@.planning/phases/160-temporal-awareness-spine/160-01-SUMMARY.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: additive idempotent bitemporal node migration + last_modified_at write discipline</name>
  <read_first>
    - lib/core/migrations/phase-109-nodes-provenance.cjs (the FULL additive-idempotent-backfill template to mirror: SENTINEL_KEY sentinel-row idempotency, addColumnsIdempotent with duplicate-column tolerance, backfillFromProperties, the BEGIN/COMMIT wrapper, the 12-step re-create-table recipe for CHECK constraints - per D the migration follows this pattern exactly)
    - lib/core/navigation/transitions.cjs:80-90 (the two UPDATE nodes ... last_seen_at writes - the write paths that must ALSO set last_modified_at; reading a node must NOT touch last_modified_at - SPEC R7 acceptance)
    - 160-CONTEXT.md "Claude's Discretion" (migration file naming/sequencing follows the phase-109 additive-idempotent-backfill pattern)
    - 160-SPEC.md R7 acceptance (run twice no error identical result; backfill valid_from=created_at others NULL; last_modified_at write-only)
  </read_first>
  <behavior>
    - runMigration(db) adds four columns valid_from / valid_to / invalidated_at / last_modified_at (all INTEGER epoch ms, nullable) via duplicate-resilient ALTER TABLE.
    - Backfill: valid_from = created_at for every existing row; valid_to, invalidated_at, last_modified_at stay NULL.
    - Idempotent: a sentinel row (identity key phase_160_bitemporal_migration_v1) short-circuits a second run; running twice yields no error and an identical result.
    - last_modified_at updates ONLY on a node write (the transitions.cjs UPDATE paths set it to getReferenceNow()); a node READ never changes it.
  </behavior>
  <action>
    Create lib/core/migrations/phase-160-nodes-bitemporal.cjs mirroring phase-109-nodes-provenance.cjs verbatim in structure (Part 7 - name the analog: phase-109). Add the four columns via addColumnsIdempotent (duplicate-column tolerant), backfill valid_from=created_at and leave the others NULL, guard idempotency with SENTINEL_KEY = 'phase_160_bitemporal_migration_v1', wrap in BEGIN/COMMIT/ROLLBACK, and add indices on valid_from + invalidated_at. Edit lib/core/navigation/transitions.cjs so every UPDATE nodes write path ALSO sets last_modified_at = the reference now (via the options.now seam, D-01a); leave all read paths untouched so reading never bumps last_modified_at. Register the migration in the migration runner wherever phase-109 is registered (find the openRoomDb composition that calls phase-109 runMigration and add phase-160 after it). Write phase-160-nodes-bitemporal.test.cjs FIRST: assert run-twice idempotency + identical result, backfill values, and the read-does-not-touch / write-does-touch last_modified_at behavior. House rule: hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node --test lib/core/migrations/phase-160-nodes-bitemporal.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test asserts runMigration runs twice with no error and an identical schema+data result (SPEC R7 idempotency).
    - A test asserts existing rows backfill valid_from=created_at with valid_to/invalidated_at/last_modified_at NULL (SPEC R7 backfill).
    - A test asserts reading a node leaves last_modified_at unchanged while a write sets it (SPEC R7 write-only).
    - Migration writes only to room.db, zero Brain queries (mirrors the phase-109 Part 8 invariant); Phase 157 boundary scan stays GREEN.
  </acceptance_criteria>
  <done>The bitemporal migration is additive/idempotent/backfilled; last_modified_at updates on write only; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: non-lossy supersession through the chokepoint</name>
  <read_first>
    - lib/core/navigation/transitions.cjs:80-90 (promoteNodeStatus - the status-transition chokepoint supersession composes; superseded -> review_status='superseded' is already in the closed status set)
    - lib/core/navigation.cjs:82-103 (promoteNodeStatus + logMemoryEvent + writeEdge re-exports - the doors supersession uses; never a side-door delete)
    - lib/core/navigation/edges.cjs (the SUPERSEDES edge type, already in ALLOWED_EDGE_TYPES; supersession writes a SUPERSEDES edge B->A)
    - 160-SPEC.md R8 acceptance (after supersession A is still present with invalidated_at set; a point-in-time query before supersession still returns A - the point-in-time helper is Plan 05, so this task asserts the row state + a SUPERSEDES edge, leaving the as-of query assertion to Plan 05)
  </read_first>
  <behavior>
    - supersede(db, oldNodeId, newNodeId, opts) sets A(old).invalidated_at = reference now and A.valid_to = B(new).valid_from, sets A.review_status='superseded', writes a SUPERSEDES edge B->A, and logs a memory_event - all through the navigation chokepoint.
    - A row is NEVER deleted: after supersession the old node still exists with invalidated_at populated.
    - Uses getReferenceNow() (options.now seam) for the now value so tests inject a fixed reference.
  </behavior>
  <action>
    Create lib/core/temporal/supersession.cjs exporting supersede(db, oldNodeId, newNodeId, opts). It reads B.valid_from, sets A.invalidated_at=referenceNow + A.valid_to=B.valid_from + A.review_status='superseded' via promoteNodeStatus / a chokepoint-routed UPDATE (NEVER a DELETE, NEVER a direct room.db open outside the allow-list - route through navigation.cjs), writes the SUPERSEDES edge B->A via navigation.writeEdge, and logs the supersession via navigation.logMemoryEvent (Part 9). Reuse getReferenceNow via the options.now seam. Write supersession.test.cjs FIRST: build A and B, call supersede, assert A still present with invalidated_at set and valid_to == B.valid_from, assert the SUPERSEDES edge exists, assert no DELETE happened. House rule: hyphens only.
  </action>
  <verify>
    <automated>node --test lib/core/temporal/supersession.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test asserts after supersede(A,B) the A row is still present with invalidated_at set and valid_to == B.valid_from (SPEC R8 acceptance, non-lossy).
    - A SUPERSEDES edge B->A exists; the supersession is logged via the Part 9 chokepoint (verified by grep showing no direct INSERT/DELETE INTO nodes in supersession.cjs - it routes through navigation.cjs).
    - Part 8: supersession touches only room.db scalars; Phase 157 boundary scan stays GREEN.
  </acceptance_criteria>
  <done>Supersession closes the old fact (invalidated_at/valid_to) without deleting, writes a SUPERSEDES edge, logs through the chokepoint; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> nodes table | LOCAL additive DDL + backfill; never egress |
| supersession -> room.db (via chokepoint) | LOCAL status close; row preserved, never deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-09 | Tampering | partial/double migration corrupting nodes | mitigate | sentinel-row idempotency + BEGIN/COMMIT/ROLLBACK + duplicate-column tolerance, mirroring the proven phase-109 recipe |
| T-160-10 | Repudiation | silent fact deletion losing audit trail | mitigate | supersession sets invalidated_at/valid_to and persists the row; logs a memory_event; never DELETEs (R8) |
| T-160-11 | Information Disclosure | migration/supersession egress | mitigate | both write only to room.db; zero Brain queries (phase-109 Part 8 invariant); boundary scan green |
| T-160-SC | Tampering | npm/pip/cargo installs | mitigate | this plan adds NO new dependency; slopcheck N/A |
</threat_model>

<verification>
- node --test lib/core/migrations/phase-160-nodes-bitemporal.test.cjs green (R7 idempotent/backfill/write-only).
- node --test lib/core/temporal/supersession.test.cjs green (R8 non-lossy + SUPERSEDES edge).
- Phase 157 Part 8 boundary scan stays GREEN.
</verification>

<success_criteria>
The node bitemporal migration is idempotent and backfilled; last_modified_at updates on write only, not read; supersession closes the old fact (invalidated_at/valid_to) without deleting and logs through the Part 9 chokepoint.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `valid_from` / `valid_to` / `invalidated_at` / `last_modified_at` - the four new bitemporal node columns (NEW columns)
- `phase-160-nodes-bitemporal.cjs` runMigration + SENTINEL_KEY phase_160_bitemporal_migration_v1 (NEW migration)
- `supersede()` - non-lossy supersession through the chokepoint (NEW symbol)
- last_modified_at write-only discipline in transitions.cjs
</artifacts>

<output>
Create `.planning/phases/160-temporal-awareness-spine/160-04-SUMMARY.md` when done
</output>
