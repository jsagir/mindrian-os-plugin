---
phase: 160-temporal-awareness-spine
plan: 03
type: execute
wave: 2
depends_on: [160-01]
files_modified:
  - lib/core/temporal/recency-decay.cjs
  - lib/core/temporal/recency-decay.test.cjs
  - lib/core/navigation/room-context.cjs
  - tests/fixtures/legd-recency-golden.json
  - tests/test-legd-recency-golden.cjs
  - lib/core/sensors/sensor-recency.cjs
  - lib/core/insight-sensors.cjs
autonomous: true
requirements: [R5, R6]
canon_parts: [Part 2, Part 3, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "Given two cortex nodes identical but for created_at, getRoomContext Leg D ranks the more recent first"
    - "A frozen-output golden-file test guards Leg D ranking determinism"
    - "A recently-touched node yields a higher reach score than an identical stale node in Engine 1 selection ordering"
    - "Recency decay is computed app-side in JS (0.995^delta-h) after a recency-ordered SQL fetch, per D-03"
  artifacts:
    - path: "lib/core/temporal/recency-decay.cjs"
      provides: "0.995^(delta-hours) decay blend, app-side, visible decay constant"
      exports: ["recencyScore", "rankByRecency", "DECAY_BASE"]
    - path: "lib/core/navigation/room-context.cjs"
      provides: "Leg D selects created_at/last_seen_at and orders recency-aware (replaces ORDER BY type, id)"
    - path: "lib/core/sensors/sensor-recency.cjs"
      provides: "recency contributes to reach-candidate ranking (Engine 1)"
      exports: ["sensorRecency"]
    - path: "tests/fixtures/legd-recency-golden.json"
      provides: "frozen node set + frozen expected ranking"
  key_links:
    - from: "lib/core/navigation/room-context.cjs legD"
      to: "lib/core/temporal/recency-decay.cjs rankByRecency"
      via: "app-side decay blend after the recency-ordered SQL fetch"
      pattern: "rankByRecency|recencyScore|created_at"
    - from: "lib/core/sensors/sensor-recency.cjs"
      to: "lib/core/insight-sensors.cjs SENSOR_REGISTRY"
      via: "registered as a reach signal producing a context_block candidate"
      pattern: "sensorRecency|SENSOR_REGISTRY"
---

<objective>
Deliver Wave 3 - recency in the spine and the Larry Reaches link. R5 - getRoomContext Leg D stops sorting alphabetically (ORDER BY type, id at room-context.cjs:162-165) and ranks recency-aware: it SELECTs created_at/last_seen_at and blends a 0.995^(delta-hours) decay app-side per D-03, guarded by a frozen-output golden-file test. R6 - recency becomes a reach signal in Engine 1 so recently-touched context can fire a reach (a recently-touched node outranks an identical stale node).

Purpose: Wires the spine and the reach engine to time. The alphabetical Leg D sort is the single highest-leverage, lowest-cost gap in the system (RESEARCH).
Output: the app-side decay module with a visible decay constant, the Leg D recency rewrite, the determinism golden-file guard, and the recency reach sensor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-temporal-awareness-spine/160-SPEC.md
@.planning/phases/160-temporal-awareness-spine/160-CONTEXT.md
@.planning/phases/160-temporal-awareness-spine/160-01-SUMMARY.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app-side recency decay module + Leg D recency rewrite</name>
  <read_first>
    - lib/core/navigation/room-context.cjs:120-183 (CORTEX_NODE_TYPES + the legD SELECT to rewrite; note legD currently does NOT select date columns and ORDERs BY type, id - the alphabetical sort to replace; keep the RAW-LOCAL no-egress discipline intact)
    - lib/core/temporal/reference-now.cjs (getReferenceNow - the delta anchor for decay; delta-h = (referenceNow - created_at)/3600000)
    - 160-CONTEXT.md D-03 (recency math placement: app-side JS after a recency-ordered SQL fetch; visible decay constant; frozen golden-file fixture - a decay constant buried in SQL cannot be frozen-tested)
    - 160-SPEC.md R5 acceptance (two-identical-but-for-created_at nodes -> more recent first; frozen-output guard)
  </read_first>
  <behavior>
    - recencyScore(createdAtMs, referenceMs) returns DECAY_BASE ^ deltaHours where DECAY_BASE = 0.995 (the Generative-Agents constant, exported as a named visible constant).
    - rankByRecency(nodes, referenceMs) returns the nodes sorted by recency score descending, deterministically (stable tiebreak on id so equal scores keep a fixed order).
    - Leg D SELECTs created_at and last_seen_at (adds them to the projection), fetches ORDER BY created_at DESC, then applies rankByRecency app-side; identical-but-for-created_at nodes order more-recent-first.
    - Leg D stays RAW-LOCAL: still returns id/type/properties/review_status (+ the new date scalars); still NEVER opens room.db itself, NEVER imports egress helpers (the existing Part 8 discipline is preserved).
  </behavior>
  <action>
    Create lib/core/temporal/recency-decay.cjs exporting recencyScore(createdAtMs, referenceMs), rankByRecency(nodes, referenceMs), and the named constant DECAY_BASE = 0.995 (D-03 - the visible decay constant, never buried in a SQL string). Edit lib/core/navigation/room-context.cjs legD to add created_at + last_seen_at to the SELECT projection and the returned object, change ORDER BY type, id to ORDER BY created_at DESC, and apply rankByRecency app-side using getReferenceNow() as the reference (the app-side blend per D-03). Preserve every existing Leg D invariant: caller-owned db, no room.db open, no egress import, RAW-LOCAL fields only, the single try/catch degrade-to-[] guard. Write recency-decay.test.cjs FIRST asserting recencyScore monotonic decreasing with age and rankByRecency ordering. House rule: hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node --test lib/core/temporal/recency-decay.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test asserts recencyScore(newer) > recencyScore(older) and rankByRecency orders the more-recent node first (SPEC R5 acceptance part 1).
    - Leg D SELECT includes created_at and last_seen_at and orders recency-aware (no longer ORDER BY type, id), verified by reading the rewritten SQL.
    - Leg D still returns RAW-LOCAL fields only and never opens room.db (Part 8 discipline preserved), verified by grep.
  </acceptance_criteria>
  <done>recencyScore/rankByRecency exist with a visible 0.995 constant; Leg D ranks recency-aware app-side; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Leg D determinism golden-file guard + recency reach signal (R6)</name>
  <read_first>
    - tests/ (find a sibling golden-file / frozen-fixture test to mirror, e.g. an existing run-all-*.sh-registered determinism test; reuse the fixture+assert idiom)
    - lib/core/sensors/sensor-lagging-component.cjs (the canonical sensorFn(turn, tuple, ctx) -> candidate-reach|null shape + the Part 8 enum-output discipline + the Phase 144 routing-fence comment block to mirror)
    - lib/core/insight-sensors.cjs:349-420 (SENSOR_REGISTRY + dispatchSensors - where sensorRecency registers; note the frozen REACH_IDS membership check)
    - lib/core/sensors/sensor-types.cjs:40-60 (REACH_IDS - the frozen 6-reach bank; R6 reuses 'context_block', mints NO new reach_id, and reuses the existing reach_presented event - honoring the frozen EVENT_TYPES + reach contracts per the SPEC open-question 2 resolution)
  </read_first>
  <action>
    Create tests/fixtures/legd-recency-golden.json: a frozen set of cortex nodes (fixed ids + fixed created_at values) and the frozen expected ranked id order. Create tests/test-legd-recency-golden.cjs: build a hermetic room.db with the fixture nodes, run Leg D with a fixed getReferenceNow injected via the options.now seam (D-01a), and assert the output id order byte-matches the golden fixture (the D-03 hot-path determinism guard - SPEC R5 acceptance part 2). Create lib/core/sensors/sensor-recency.cjs exporting sensorRecency(turn, tuple, ctx): a net-new reach signal that, when LOCAL recency scalars on ctx show a node touched within a recent window, produces a context_block candidate reach (reusing the existing reach_id - mints NO new reach_id and NO new EVENT_TYPE, honoring the frozen contracts; recency feeds the existing reach ranking). Mirror the sensor-lagging-component routing-fence + Part 8 enum-only discipline exactly. Register sensorRecency in the SENSOR_REGISTRY and export it from insight-sensors.cjs. Write a unit assertion that a recently-touched node yields a higher reach score / ranks above an identical stale node (SPEC R6 acceptance). House rule: hyphens only.
  </action>
  <verify>
    <automated>node tests/test-legd-recency-golden.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-legd-recency-golden.cjs passes: the Leg D ranked id order byte-matches the frozen golden fixture under a fixed injected reference (SPEC R5 determinism guard GREEN).
    - sensorRecency is registered in SENSOR_REGISTRY and produces a context_block candidate (reach_id in the frozen REACH_IDS; no new reach_id, no new EVENT_TYPE) - verified by a test asserting REACH_IDS membership.
    - A test asserts a recently-touched node outranks an identical stale node in the recency ordering (SPEC R6 acceptance).
    - Part 8: sensor output is enums + scalars only; the Phase 157 boundary scan stays GREEN; the existing lib/core/sensors Part-8 sweep spans sensor-recency.cjs.
  </acceptance_criteria>
  <done>The frozen golden-file guard is GREEN; recency is a registered reach signal reusing context_block; recent outranks stale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db cortex rows -> Leg D ranking | LOCAL read; RAW-LOCAL fields stay in-process, never egress |
| ctx recency scalars -> sensorRecency | LOCAL scalars produce a generic reach handle, never user content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-06 | Tampering | non-deterministic Leg D hot-path ranking | mitigate | frozen golden-file test with a fixed injected reference; app-side decay with a visible constant (D-03) so the ranking is testable |
| T-160-07 | Information Disclosure | recency reach signal leaking node content | mitigate | sensorRecency emits enums + a generic context_block handle only; lib/core/sensors Part-8 sweep gates it |
| T-160-08 | Denial of Service | Leg D blowing the NAV budget | accept | cortex node set is small + bounded (D-03 rationale); app-side blend is O(n log n) on a tiny set; existing legD try/catch degrades to [] |
| T-160-SC | Tampering | npm/pip/cargo installs | mitigate | this plan adds NO new dependency; slopcheck N/A |
</threat_model>

<verification>
- node --test lib/core/temporal/recency-decay.test.cjs green (R5 decay + ordering).
- node tests/test-legd-recency-golden.cjs green (R5 determinism guard).
- sensorRecency registered + REACH_IDS membership asserted; recent-outranks-stale asserted (R6).
- Phase 157 Part 8 boundary scan stays GREEN.
</verification>

<success_criteria>
getRoomContext Leg D ranks recent cortex nodes above stale with a frozen-output determinism guard GREEN; recency contributes to reach-candidate ordering (recent > identical stale) via a registered sensor that mints no new reach_id or EVENT_TYPE.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `recencyScore()` / `rankByRecency()` - app-side decay blend (NEW symbols)
- `DECAY_BASE` (0.995) - the visible decay constant (NEW named constant)
- Leg D recency-aware projection (created_at/last_seen_at selected; ORDER BY type,id replaced) in room-context.cjs
- `sensorRecency()` - recency-as-reach-signal, reusing the frozen context_block reach + reach_presented event (NEW sensor; NO new reach_id, NO new EVENT_TYPE)
- `tests/fixtures/legd-recency-golden.json` - the frozen determinism fixture (NEW artifact)
</artifacts>

<output>
Create `.planning/phases/160-temporal-awareness-spine/160-03-SUMMARY.md` when done
</output>
