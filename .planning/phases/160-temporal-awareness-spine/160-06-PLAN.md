---
phase: 160-temporal-awareness-spine
plan: 06
type: execute
wave: 3
depends_on: [160-04, 160-02]
files_modified:
  - lib/core/temporal/date-sync-gate.cjs
  - lib/core/temporal/date-sync-gate.test.cjs
  - commands/file-meeting.md
  - lib/mcp/prompts.cjs
  - lib/core/sensors/sensor-temporal-blindness.cjs
  - scripts/scout-cadence-runner.cjs
  - tests/test-temporal-blindness-sentinel.cjs
  - lib/core/navigation/memory-events.cjs
autonomous: false
requirements: [R11, R12]
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "Filing a meeting with no date BLOCKS until valid_at is set via the Shape F selector"
    - "A meeting given 'last Tuesday' resolves valid_at correctly against getReferenceNow()"
    - "Gate multi-select writes a sync edge and emits f_selector_sync_confirmed"
    - "The gate enforcement lives in ONE shared lib/core chokepoint that both the CLI meeting-file path and the MCP filing tool call"
    - "has_event_date is set explicitly at write time by node type (meeting always); pure ideas/assumptions stamp valid_at=filing time silently"
    - "The temporal-blindness sentinel returns exactly the undated real-world nodes; empty when none, surfacing nothing"
  artifacts:
    - path: "lib/core/temporal/date-sync-gate.cjs"
      provides: "the ONE shared tri-polar date+sync gate enforcement chokepoint (D-02)"
      exports: ["requireValidAt", "GATE_BLOCK", "GATE_PASS"]
    - path: "lib/core/sensors/sensor-temporal-blindness.cjs"
      provides: "scheduled sentinel listing real-world-event nodes where valid_at IS NULL; emits temporal_blindness_surfaced"
      exports: ["sensorTemporalBlindness"]
  key_links:
    - from: "commands/file-meeting.md"
      to: "lib/core/temporal/date-sync-gate.cjs requireValidAt"
      via: "the CLI meeting-file path calls the shared gate chokepoint"
      pattern: "requireValidAt|date-sync-gate"
    - from: "lib/mcp/prompts.cjs"
      to: "lib/core/temporal/date-sync-gate.cjs requireValidAt"
      via: "the MCP filing tool calls the same shared gate chokepoint (tri-polar)"
      pattern: "requireValidAt|date-sync-gate"
    - from: "lib/core/temporal/date-sync-gate.cjs"
      to: "lib/workflow/dial-close-reach.cjs f_selector_sync_confirmed"
      via: "multi-select relation writes a sync edge reusing f_selector_sync_confirmed"
      pattern: "f_selector_sync_confirmed"
---

<objective>
Deliver Wave 5 - the HITL enforcement layer. R11 - filing a meeting (and any node flagged has_event_date) requires valid_at; if absent, a Shape F (F.1) selector prompts "when?" (sets valid_at, accepts free-typed relative time resolved by chrono-node from Wave 2) AND multi-select "relates to?" writes sync edges via the existing f_selector_sync_confirmed machinery; created_at stays the filing moment; pure ideas/assumptions stamp valid_at=filing time silently. The gate enforcement lives in ONE shared lib/core chokepoint that the CLI meeting-file path and the MCP filing tool both call (D-02, tri-polar). R12 - a temporal-blindness sentinel (a scheduled backstop on the Phase 145 cadence per D-04) lists real-world-event nodes where valid_at IS NULL and surfaces them; emits temporal_blindness_surfaced. has_event_date is set explicitly at write time by node type, never inferred at read time.

Purpose: The human owns valid_at. The gate is the front line at filing; the sentinel is the standing backstop. Both root in one doctrine: human-asserted time beats the system clock for real-world events.
Output: the shared gate chokepoint, its CLI + MCP wiring, the scheduled sentinel, the new temporal_blindness_surfaced event type.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-temporal-awareness-spine/160-SPEC.md
@.planning/phases/160-temporal-awareness-spine/160-CONTEXT.md
@.planning/phases/160-temporal-awareness-spine/160-02-SUMMARY.md
@.planning/phases/160-temporal-awareness-spine/160-04-SUMMARY.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the ONE shared tri-polar date+sync gate chokepoint (D-02)</name>
  <read_first>
    - lib/workflow/dial-close-reach.cjs:344-371 (the f_selector_sync_confirmed emission + SELECTED_REACH/sync-edge machinery to REUSE for the "relates to?" multi-select - Part 7; name the analog explicitly)
    - lib/core/temporal/resolve-relative-time.cjs (Wave 2 output - resolves a free-typed "last Tuesday" to valid_at)
    - lib/core/temporal/dual-stamp.cjs (Wave 2 output - the created_at=filing-moment + valid_at write through the chokepoint)
    - lib/core/navigation.cjs:98-103 (writeEdge - the sync-edge writer) + lib/core/navigation/edges.cjs (the sync edge type already in ALLOWED_EDGE_TYPES)
    - 160-CONTEXT.md D-02 (enforcement in ONE shared lib/core chokepoint; Shape F selector RENDERS per surface; the rule lives in one place) + D-04 (has_event_date set explicitly at write time by node type, never inferred at read)
    - docs/MINDRIAN-CANON.md Part 3 (Shape F.1 selector contract)
  </read_first>
  <behavior>
    - requireValidAt(filingNode, opts) is the single enforcement rule: if the node is a real-world event (type 'meeting' always, or has_event_date===true) and valid_at is absent, it returns GATE_BLOCK with a selector spec (Shape F.1 "when?" + multi-select "relates to?"); otherwise GATE_PASS.
    - When the selector returns a date (explicit epoch ms OR a free-typed relative string resolved via resolveRelativeTime against getReferenceNow), valid_at is set to the resolved value; created_at stays the filing moment.
    - A multi-selected relation writes a sync edge via writeEdge and emits f_selector_sync_confirmed (reusing the dial-close-reach machinery).
    - Pure ideas/assumptions (not real-world events) stamp valid_at = filing time silently - GATE_PASS with valid_at defaulted, no selector.
    - has_event_date is read from the node (set at write time by type), never inferred here - the gate stays deterministic (D-04).
    - The rule is surface-agnostic: it returns a spec; the CLI and MCP layers RENDER it. Enforcement is one function.
  </behavior>
  <action>
    Create lib/core/temporal/date-sync-gate.cjs exporting requireValidAt(filingNode, opts), GATE_BLOCK, GATE_PASS. It is the single tri-polar enforcement chokepoint (D-02): the "no real-world-event artifact files without valid_at" rule lives ONLY here. It resolves a free-typed relative date via resolveRelativeTime (Wave 2), dual-stamps via dual-stamp (created_at=filing moment, valid_at=resolved), writes sync edges via navigation.writeEdge, and emits f_selector_sync_confirmed via navigation.logMemoryEvent reusing the dial-close-reach idiom verbatim (Part 7 - name the analog: lib/workflow/dial-close-reach.cjs:361). It reads has_event_date off the node (set at write time by type, D-04) and never infers it. It returns a Shape F.1 selector spec (not a rendered prompt) so CLI and MCP render per surface. Write date-sync-gate.test.cjs FIRST: assert a meeting with no valid_at returns GATE_BLOCK with a selector spec; a meeting given "last Tuesday" resolves valid_at against an injected reference; a multi-select writes a sync edge and the test observes an f_selector_sync_confirmed event; a pure idea passes silently with valid_at=filing time. House rule: hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node --test lib/core/temporal/date-sync-gate.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test asserts a meeting with no date returns GATE_BLOCK until valid_at is set via the selector spec (SPEC R11 acceptance 1).
    - A test asserts a meeting given "last Tuesday" resolves valid_at correctly against getReferenceNow() (SPEC R11 acceptance 2).
    - A test asserts a multi-selected relation writes a sync edge and emits f_selector_sync_confirmed (SPEC R11 acceptance 3).
    - A test asserts a pure idea stamps valid_at=filing time silently (no block).
    - The rule lives in exactly one function (requireValidAt); created_at stays the filing moment; Part 8 boundary scan stays GREEN.
  </acceptance_criteria>
  <done>The shared gate blocks undated meetings, resolves free-typed dates, writes sync edges via f_selector_sync_confirmed, passes pure ideas silently; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: wire the gate into CLI + MCP (tri-polar) and set has_event_date at write</name>
  <read_first>
    - commands/file-meeting.md:392-470 (Step 4 Confirm-Then-File + Create Filed Artifacts - where the CLI meeting path files; the gate call goes BEFORE the artifact write so an undated meeting blocks; has_event_date=true is set for meeting nodes at write)
    - lib/mcp/prompts.cjs (the MCP filing tool surface - the Desktop/Cowork door that must call the SAME requireValidAt chokepoint, D-02 tri-polar)
    - lib/core/temporal/date-sync-gate.cjs (Task 1 output - requireValidAt)
    - 160-CONTEXT.md D-02 (both surfaces call the one chokepoint; per-surface enforcement is where drift creeps in) + D-04 (has_event_date explicit by type at write)
  </read_first>
  <action>
    Edit commands/file-meeting.md Step 4 so the CLI meeting-file path calls requireValidAt BEFORE writing the meeting artifact: a meeting node is flagged has_event_date=true at write (D-04), and an undated meeting renders the Shape F.1 "when?" + "relates to?" selector (AskUserQuestion on CLI) and BLOCKS until valid_at is set; the doctrine text names the shared chokepoint as the single rule. Edit lib/mcp/prompts.cjs so the MCP filing tool calls the SAME requireValidAt chokepoint (Desktop/Cowork conversational render of the same selector spec) - the acceptance asserts the shared chokepoint, not a CLI-only path (D-02 tri-polar). Set has_event_date explicitly by node type at write (meeting always true; decision/claim flagged when the filing path detects an event date) - never inferred at read. House rule: hyphens only.
  </action>
  <verify>
    <automated>node --test lib/core/temporal/date-sync-gate.test.cjs && grep -q "requireValidAt" commands/file-meeting.md && grep -q "requireValidAt" lib/mcp/prompts.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both commands/file-meeting.md (CLI) and lib/mcp/prompts.cjs (MCP) call requireValidAt - the gate enforcement is the shared lib/core chokepoint, not a CLI-only path (D-02 tri-polar), verified by grep finding requireValidAt in both.
    - The meeting filing path sets has_event_date=true at write time by type (D-04), verified by reading the edited Step 4 doctrine.
    - The CLI renders the Shape F.1 selector via AskUserQuestion; the MCP renders it conversationally - both off the same selector spec.
    - Part 8 boundary scan stays GREEN.
  </acceptance_criteria>
  <done>Both the CLI meeting path and the MCP filing tool call the one shared gate chokepoint; has_event_date is set at write by type; the gate is tri-polar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: temporal-blindness sentinel on the Phase 145 cadence (R12)</name>
  <read_first>
    - lib/core/sensors/sensor-lagging-component.cjs (the sensorFn shape + Part 8 enum-output discipline + the routing-fence comment block to mirror)
    - scripts/scout-cadence-runner.cjs:1-50 (the Phase 145 cadence composition - the SCHEDULED backstop home, D-04; the runner COMPOSES existing sensors, builds no new fetch; add the temporal-blindness scan to the composed suite)
    - lib/core/navigation/memory-events.cjs:300-365 (the EVENT_TYPES additive idiom - add temporal_blindness_surfaced following the Phase 131-01 / 143.1 named-membership FLOOR-delta pattern, never a brittle absolute size)
    - 160-CONTEXT.md D-04 (sentinel fires as a SCHEDULED backstop on the Phase 145 cadence; the R11 gate is the front line) + 160-SPEC.md R12 acceptance (N undated real-world nodes -> returns exactly those N; zero present -> empty, surfaces nothing)
  </read_first>
  <behavior>
    - sensorTemporalBlindness(ctx) scans room.db (via the navigation chokepoint) for real-world-event nodes (type meeting OR has_event_date===true) where valid_at IS NULL and returns exactly those nodes for dating; emits a temporal_blindness_surfaced memory_event when it surfaces any.
    - With N undated real-world nodes present it returns exactly those N; with zero present it returns empty and surfaces nothing (emits no event).
    - It is a SCHEDULED backstop (D-04): composed into the scout-cadence-runner suite, not fired per-turn.
    - Pure LOCAL: reads room.db scalars, emits an enum/count event; zero network, zero Brain.
  </behavior>
  <action>
    Add temporal_blindness_surfaced to EVENT_TYPES in lib/core/navigation/memory-events.cjs following the Phase 131-01 additive named-membership block idiom (a comment block + the string; tests assert a FLOOR + named membership, never an absolute size). Create lib/core/sensors/sensor-temporal-blindness.cjs exporting sensorTemporalBlindness(ctx) that queries real-world-event nodes with valid_at IS NULL through the navigation chokepoint and emits temporal_blindness_surfaced via navigation.logMemoryEvent when it surfaces any; mirror the sensor-lagging-component routing-fence + Part 8 enum-only discipline. Compose it into scripts/scout-cadence-runner.cjs as a scheduled backstop step (D-04 - the runner composes existing surfaces; the sentinel is LOCAL scan only, no fetch). Write tests/test-temporal-blindness-sentinel.cjs FIRST: build a room with N undated real-world nodes + some dated ones + some non-real-world undated nodes, assert the sentinel returns exactly the N undated real-world nodes and emits the event; build a room with zero undated real-world nodes, assert it returns empty and emits no event. House rule: hyphens only.
  </action>
  <verify>
    <automated>node tests/test-temporal-blindness-sentinel.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node tests/test-temporal-blindness-sentinel.cjs passes: with N undated real-world nodes the sentinel returns exactly those N; with zero it returns empty and surfaces nothing (SPEC R12 acceptance).
    - temporal_blindness_surfaced is added to EVENT_TYPES via the additive named-membership FLOOR idiom (the existing EVENT_TYPES FLOOR test still green; no brittle absolute-size assertion).
    - The sentinel is composed into scripts/scout-cadence-runner.cjs as a scheduled backstop (D-04), not fired per-turn, verified by reading the composition.
    - Part 8: the sentinel reads room.db scalars and emits enum/count events only; zero network/Brain; the lib/core/sensors Part-8 sweep spans it; Phase 157 boundary scan stays GREEN.
  </acceptance_criteria>
  <done>The scheduled sentinel returns exactly the undated real-world nodes and emits temporal_blindness_surfaced; empty when none; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-typed date -> gate -> resolveRelativeTime | untrusted NL resolved LOCALLY to valid_at; human-asserted time wins |
| CLI + MCP filing surfaces -> shared gate chokepoint | both surfaces cross into ONE enforcement function (drift-proof) |
| scout cadence -> temporal-blindness sentinel | LOCAL scan; emits enum/count event only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-15 | Tampering | per-surface gate drift (CLI enforces, MCP does not) | mitigate | enforcement is ONE shared lib/core chokepoint (D-02); both surfaces call requireValidAt; acceptance asserts the shared path, not a CLI-only path |
| T-160-16 | Spoofing | system silently stamping now on a past event | mitigate | requireValidAt BLOCKS undated real-world events; the human sets valid_at; created_at stays the filing moment (human-asserted time wins) |
| T-160-17 | Tampering | has_event_date inferred at read time non-deterministically | mitigate | has_event_date set explicitly at write by node type (D-04); the gate reads it, never infers |
| T-160-18 | Information Disclosure | gate/sentinel egress of meeting content | mitigate | gate writes scalar valid_at + enum sync edges; sentinel emits enum/count events; raw meeting prose never crosses to Brain; Part 8 sweep green |
| T-160-SC | Tampering | npm/pip/cargo installs | mitigate | this plan adds NO new dependency; slopcheck N/A |
</threat_model>

<verification>
- node --test lib/core/temporal/date-sync-gate.test.cjs green (R11 block + resolve + sync edge + silent-pass).
- grep finds requireValidAt in BOTH commands/file-meeting.md and lib/mcp/prompts.cjs (D-02 tri-polar).
- node tests/test-temporal-blindness-sentinel.cjs green (R12 exact-N + empty).
- EVENT_TYPES FLOOR test green after the temporal_blindness_surfaced addition.
- Phase 157 Part 8 boundary scan stays GREEN.
</verification>

<success_criteria>
Filing a meeting with no date BLOCKS until valid_at is set via the Shape F selector; "last Tuesday" resolves valid_at against getReferenceNow(); a multi-select writes a sync edge + emits f_selector_sync_confirmed; the gate enforcement is ONE shared chokepoint the CLI and MCP both call; the scheduled temporal-blindness sentinel returns exactly the undated real-world nodes and emits temporal_blindness_surfaced.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `requireValidAt()` - the ONE shared tri-polar date+sync gate enforcement chokepoint (NEW symbol; D-02)
- `GATE_BLOCK` / `GATE_PASS` - the gate verdicts (NEW symbols)
- `has_event_date` - the write-time node flag set by type (NEW property key; D-04)
- `sensorTemporalBlindness()` - the scheduled temporal-blindness sentinel (NEW sensor)
- `temporal_blindness_surfaced` - the new EVENT_TYPE the sentinel emits (NEW event type, additive FLOOR)
- CLI (file-meeting.md) + MCP (prompts.cjs) wiring to the shared gate chokepoint
</artifacts>

<output>
Create `.planning/phases/160-temporal-awareness-spine/160-06-SUMMARY.md` when done
</output>
