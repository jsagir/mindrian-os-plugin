---
phase: 160-temporal-awareness-spine
plan: 02
type: execute
wave: 2
depends_on: [160-01]
files_modified:
  - package.json
  - package-lock.json
  - lib/core/temporal/resolve-relative-time.cjs
  - lib/core/temporal/resolve-relative-time.test.cjs
  - lib/core/temporal/dual-stamp.cjs
  - lib/core/temporal/dual-stamp.test.cjs
  - skills/context-engine/SKILL.md
autonomous: false
requirements: [R2, R3, R4]
canon_parts: [Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "A test table of at least 10 relative expressions resolves correctly against reference 2026-06-16 (last Tuesday -> 2026-06-09; next month -> 2026-07-..)"
    - "Larry's greeting renders a humanDelta() time delta for a topic node created 3 days before reference-now (contains '3 days ago')"
    - "A real-world-event memory_event logged from 'we met last Tuesday' carries valid_at = resolved date and created_at = now, both present and distinct"
    - "Relative-time resolution is anchored to getReferenceNow(), never raw Date.now()"
  artifacts:
    - path: "lib/core/temporal/resolve-relative-time.cjs"
      provides: "chrono-node resolver anchored to getReferenceNow(); forwardDate for future-biased phrases"
      exports: ["resolveRelativeTime"]
    - path: "lib/core/temporal/dual-stamp.cjs"
      provides: "dual-stamp valid_at + created_at through the Part 9 chokepoint"
      exports: ["logDualStampedEvent"]
    - path: "skills/context-engine/SKILL.md"
      provides: "Larry greeting carries a humanDelta() time delta"
  key_links:
    - from: "lib/core/temporal/resolve-relative-time.cjs"
      to: "lib/core/temporal/reference-now.cjs getReferenceNow"
      via: "ParsingReference instant anchored to getReferenceNow()"
      pattern: "getReferenceNow|ParsingReference|instant"
    - from: "lib/core/temporal/dual-stamp.cjs"
      to: "lib/core/navigation.cjs logMemoryEvent"
      via: "valid_at written through the navigation chokepoint"
      pattern: "logMemoryEvent|valid_at"
---

<objective>
Deliver Wave 2: the killer feature. R2 - a chrono-node resolver turns user NL-time ("yesterday", "last Tuesday", "next quarter") into epoch ms anchored to getReferenceNow() (forwardDate for future-biased phrases). R3 - Larry's greeting and responses speak time via the existing humanDelta() renderer ("you raised this 3 days ago"). R4 - real-world-event memory_events carry a resolved valid_at distinct from created_at, written through the Part 9 chokepoint.

Purpose: This is the one-second "it remembers when" effect and the Hooked variable-reward lever. It rides directly on the Wave 1 reference clock.
Output: the resolver module, the dual-stamp helper, the chrono-node dependency (legitimacy-gated), and the Larry greeting doctrine update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-temporal-awareness-spine/160-SPEC.md
@.planning/phases/160-temporal-awareness-spine/160-CONTEXT.md
@.planning/phases/160-temporal-awareness-spine/160-01-SUMMARY.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: chrono-node package legitimacy verification (blocking, never auto-approvable)</name>
  <action>
    Human verifies chrono-node 2.9.1 legitimacy on npmjs.com before the install task runs. chrono-node is the ONLY new dependency for this phase (SPEC constraint: v2.9.1, MIT, zero transitive deps). RESEARCH.md has NO `## Package Legitimacy Audit` table, so per the package legitimacy gate chrono-node is treated as [ASSUMED] and MUST be human-verified before install. This [ASSUMED]-package checkpoint blocks the install task and is never auto-approvable.
  </action>
  <what-built>
    chrono-node is the ONLY new dependency for this phase (SPEC constraint: v2.9.1, MIT, zero transitive deps). RESEARCH.md has NO `## Package Legitimacy Audit` table, so per the package legitimacy gate chrono-node is treated as [ASSUMED] and MUST be human-verified before install. This checkpoint is never auto-approvable.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/chrono-node and confirm: package exists, current version is 2.9.1 (or the latest 2.9.x), license is MIT, weekly downloads are substantial (not a typosquat), and the "Dependencies" section shows ZERO runtime dependencies.
    2. Confirm the publisher/repo is the canonical wanasit/chrono GitHub project, not a fork or lookalike.
    3. Confirm no install lifecycle scripts and no native bindings (pure JS) so the vendored-node_modules release rule (CLAUDE.md) stays cross-platform-safe.
  </how-to-verify>
  <verify>
    <human-check>Human confirms chrono-node 2.9.1 is MIT, zero-dependency, canonical-publisher on npmjs.com.</human-check>
  </verify>
  <done>Human approves the install or directs the hand-rolled regex fallback per the RESEARCH risk note.</done>
  <resume-signal>Type "approved" to authorize the chrono-node 2.9.1 install, or describe the discrepancy (then we hand-roll a regex resolver per the RESEARCH risk note).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: chrono-node resolver anchored to getReferenceNow()</name>
  <files>lib/core/temporal/resolve-relative-time.cjs, lib/core/temporal/resolve-relative-time.test.cjs, package.json, package-lock.json</files>
  <read_first>
    - lib/core/temporal/reference-now.cjs (Wave 1 output - getReferenceNow is the anchor; D-01a)
    - lib/core/dual-path-detector.cjs (the intake-classifier pattern + the Part 8 enum-output discipline to mirror; the resolver is a sibling LOCAL classifier)
    - 160-SPEC.md R2 acceptance (the >=10-expression test table contract)
    - 160-CONTEXT.md "Claude's Discretion" (resolver module location + import surface is planner choice, constrained by D-01a anchor + the verified zero-dep posture)
  </read_first>
  <behavior>
    - resolveRelativeTime(text, opts) returns { validAtMs, matched, forward } or null when no time expression is found.
    - Anchored to getReferenceNow() (passed via opts.referenceMs or resolved internally), NOT raw Date.now().
    - Given reference 2026-06-16: "last Tuesday" -> 2026-06-09; "yesterday" -> 2026-06-15; "next month" -> a date in 2026-07; "next quarter" uses forwardDate so it resolves to a future quarter; a table of at least 10 expressions resolves correctly (SPEC R2 acceptance).
    - Future-biased phrases ("next", "in 3 weeks") set chrono forwardDate true; past phrases resolve backward.
    - Pure LOCAL: operates only on the supplied user text; no network, no Brain. Returns scalars (epoch ms + a matched-span boolean/enum), never echoes raw user prose to any wire.
  </behavior>
  <action>
    After the legitimacy checkpoint is approved, install chrono-node@2.9.1 (npm install chrono-node@2.9.1) and commit the resulting package.json + package-lock.json in sync (the lockstep release rule requires the lock to match). Create lib/core/temporal/resolve-relative-time.cjs exporting resolveRelativeTime(text, opts) that builds a chrono ParsingReference with instant set from getReferenceNow() (D-01a anchor) and forwardDate enabled for future-biased phrases, returning resolved epoch ms. Keep the module pure and LOCAL per the dual-path-detector discipline. Write the test table FIRST in resolve-relative-time.test.cjs with at least 10 expressions resolved against a fixed reference of 2026-06-16, asserting the exact resolved calendar dates from SPEC R2. House rule: hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node --test lib/core/temporal/resolve-relative-time.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test passes a table of at least 10 expressions against reference 2026-06-16, including "last Tuesday" -> 2026-06-09 and "next month" -> a 2026-07 date (SPEC R2 acceptance).
    - The resolver reads its anchor from getReferenceNow(), asserted by a test that injects a fixed reference and shows resolution moves with it (not with the system clock).
    - package.json and package-lock.json are in sync (npm ci would succeed); chrono-node is the only added dependency.
  </acceptance_criteria>
  <done>resolveRelativeTime resolves the SPEC table correctly, anchored to getReferenceNow(); lock in sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dual-stamp memory_events + Larry speaks time</name>
  <files>lib/core/temporal/dual-stamp.cjs, lib/core/temporal/dual-stamp.test.cjs, skills/context-engine/SKILL.md</files>
  <read_first>
    - lib/core/navigation/memory-events.cjs:459-510 (logEvent - the Part 9 chokepoint + options.now seam; dual-stamp writes valid_at INTO the payload that lands in the node properties JSON, additive, no DDL change)
    - lib/core/navigation.cjs:85-90 (logMemoryEvent re-export - the only door)
    - lib/core/feynman/timeline-renderer.cjs:64-78 (humanDelta - reuse for R3, do NOT rebuild)
    - skills/context-engine/SKILL.md:24 (the greeting doctrine line "I see you were working on [topic]" with no time - the R3 edit target)
    - lib/core/temporal/resolve-relative-time.cjs (Task 1 output - supplies the resolved valid_at)
  </read_first>
  <behavior>
    - logDualStampedEvent(db, eventType, payload, opts) resolves valid_at from payload.relative_time (via resolveRelativeTime) OR an explicit payload.valid_at, sets created_at = getReferenceNow() (through the options.now seam), and writes BOTH onto the memory_event via the navigation chokepoint.
    - A memory_event logged from "we met last Tuesday" against reference 2026-06-16 carries valid_at = 2026-06-09 epoch ms AND created_at = the reference now, both present and distinct (SPEC R4 acceptance).
    - When no relative time is present and the event is not a real-world event, valid_at is omitted (pure ideas/assumptions are not dual-stamped here - that silent-stamp path is the Wave 5 gate's job).
    - R3: the greeting renderer composes a humanDelta() string for the last-touched topic node's created_at against getReferenceNow().
  </behavior>
  <action>
    Create lib/core/temporal/dual-stamp.cjs exporting logDualStampedEvent(db, eventType, payload, opts). It computes created_at from getReferenceNow() (supplied as the options.now value to keep the existing clock seam contract, D-01a), resolves valid_at via resolveRelativeTime when payload.relative_time is present or accepts an explicit payload.valid_at, and writes both as additive keys in the payload through navigation.logMemoryEvent (Part 9 - never a side-door INSERT; valid_at rides the properties JSON exactly like the Phase 150.8 valid_from precedent, no DDL column). Edit skills/context-engine/SKILL.md to make Larry's greeting render a humanDelta() delta for the last-touched topic ("you raised this 3 days ago") reusing the shipped humanDelta (Part 7 reuse - name the analog: lib/core/feynman/timeline-renderer.cjs humanDelta). Write dual-stamp.test.cjs FIRST: assert the "we met last Tuesday" event carries distinct valid_at (2026-06-09) and created_at (reference now), and assert the greeting composition for a 3-day-old node contains "3 days ago". House rule: hyphens only.
  </action>
  <verify>
    <automated>node --test lib/core/temporal/dual-stamp.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test asserts a "we met last Tuesday" memory_event has valid_at = 2026-06-09 epoch ms and created_at = reference-now, both present and distinct (SPEC R4 acceptance).
    - A test asserts the greeting render for a node created 3 days before reference-now contains the humanDelta() "3 days ago" string (SPEC R3 acceptance).
    - dual-stamp writes go through navigation.logMemoryEvent (the Part 9 chokepoint), verified by grep showing no direct INSERT INTO nodes in dual-stamp.cjs.
    - Part 8: valid_at + created_at are scalar epoch ms; no raw user prose crosses to Brain. Phase 157 boundary scan stays GREEN.
  </acceptance_criteria>
  <done>Real-world memory_events carry distinct valid_at/created_at through the chokepoint; Larry's greeting speaks time via humanDelta(); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text -> resolveRelativeTime | untrusted NL crosses here; resolved to scalar epoch ms LOCALLY, never echoed to a wire |
| dual-stamp payload -> room.db (via chokepoint) | LOCAL graph write of valid_at/created_at scalars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-04 | Information Disclosure | dual-stamp payload reaching Brain | mitigate | valid_at/created_at are epoch ms scalars; raw user prose stays on the LOCAL node, never on a Brain packet; Part 8 sweep gates it |
| T-160-05 | Tampering | malformed NL resolving to a wrong date | accept | resolver returns null on no-match; the Wave 5 HITL gate lets the human override the resolved valid_at (human-asserted time wins) |
| T-160-SC | Tampering | chrono-node install | mitigate | [ASSUMED] package: blocking-human legitimacy checkpoint before install; verify MIT + zero deps + canonical publisher on npmjs.com; lock kept in sync |
</threat_model>

<verification>
- node --test lib/core/temporal/resolve-relative-time.test.cjs green (R2 table, anchored to getReferenceNow).
- node --test lib/core/temporal/dual-stamp.test.cjs green (R4 distinct stamps + R3 greeting delta).
- package.json + package-lock.json in sync; chrono-node is the only added dep.
- Phase 157 Part 8 boundary scan stays GREEN.
</verification>

<success_criteria>
chrono-node resolves at least 10 relative expressions against a fixed reference; Larry's greeting includes a humanDelta() delta; real-world-event memory_events carry distinct valid_at and created_at written through the Part 9 chokepoint.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `resolveRelativeTime()` - chrono-node NL-time resolver anchored to getReferenceNow (NEW symbol)
- `logDualStampedEvent()` - dual-stamp (valid_at + created_at) through the Part 9 chokepoint (NEW symbol)
- `chrono-node` 2.9.1 - the ONLY new dependency (NEW dependency, legitimacy-gated)
- `valid_at` - the real-world-event axis carried additively in the memory_event properties JSON (NEW property key)
- Larry greeting doctrine carrying a humanDelta() delta (skills/context-engine/SKILL.md edit)
</artifacts>

<output>
Create `.planning/phases/160-temporal-awareness-spine/160-02-SUMMARY.md` when done
</output>
