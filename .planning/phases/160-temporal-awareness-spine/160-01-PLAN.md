---
phase: 160-temporal-awareness-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/temporal/reference-now.cjs
  - lib/core/temporal/reference-now.test.cjs
  - scripts/sessionstart-reference-now-seed.cjs
  - hooks/hooks.json
  - commands/correct-reference-now.md
  - tests/test-reference-now-part8.cjs
autonomous: true
requirements: [R1]
canon_parts: [Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "getReferenceNow() returns the injected currentDate calendar date when present, not the system clock date"
    - "getReferenceNow() returns a valid reference when the optional online time source is unreachable, with no thrown error"
    - "The online time-fetch rung carries zero user bytes (Part 8 SIGNAL, bare GET)"
    - "A SessionStart hook seeds ~/.mindrian/reference-now.json with a Date.now() floor"
    - "Larry can correct the seam via a tiny command when the model-known date diverges"
  artifacts:
    - path: "lib/core/temporal/reference-now.cjs"
      provides: "getReferenceNow() precedence ladder + seam read/write"
      exports: ["getReferenceNow", "readSeam", "writeSeam", "applyCurrentDate"]
    - path: "scripts/sessionstart-reference-now-seed.cjs"
      provides: "SessionStart seeding of the reference-now seam"
    - path: "commands/correct-reference-now.md"
      provides: "Larry-facing tiny tool to correct the seam"
  key_links:
    - from: "lib/core/temporal/reference-now.cjs"
      to: "lib/core/navigation/memory-events.cjs options.now seam"
      via: "getReferenceNow() supplied as the now function to logEvent opts"
      pattern: "options\\.now|getReferenceNow"
    - from: "scripts/sessionstart-reference-now-seed.cjs"
      to: "~/.mindrian/reference-now.json"
      via: "seed write on SessionStart"
      pattern: "reference-now\\.json"
---

<objective>
Establish R1: one authoritative reference clock. getReferenceNow() is a precedence ladder per D-01b: (1) injected Claude Code currentDate wins for the calendar date, (2) an OPTIONAL online time-fetch clock-skew corrector (NTP-style fetch-once-per-session + offset, Part 8 SIGNAL), (3) a Date.now() floor. A SessionStart hook seeds the local seam (~/.mindrian/reference-now.json) with a Date.now() floor (D-01); Larry corrects the seam via a tiny command when the model-known date diverges. The reader routes through the existing options.now clock seam (D-01a) so downstream waves and tests inject a fixed reference.

Purpose: Fixes the midnight delta-flip bug and gives every downstream wave (resolve, recency, bitemporal, gate) ONE trustworthy "now". This is the foundation - Waves 2, 3, 4 all anchor to getReferenceNow().
Output: lib/core/temporal/reference-now.cjs (the seam), the SessionStart seeder, the correction command, the Part 8 acceptance test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-temporal-awareness-spine/160-SPEC.md
@.planning/phases/160-temporal-awareness-spine/160-CONTEXT.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getReferenceNow() precedence ladder + local seam</name>
  <read_first>
    - lib/core/navigation/memory-events.cjs:459-495 (the options.now clock seam - the test-injection contract getReferenceNow must be compatible with, per D-01a)
    - lib/core/feynman/timeline-renderer.cjs:59-78 (isoSecond + humanDelta - the delta math getReferenceNow must feed; do NOT duplicate)
    - 160-CONTEXT.md D-01, D-01a, D-01b (the locked precedence ladder + seam decisions)
    - lib/memory/sessionstart-banner-formatter.cjs (existing Part 8 pure-formatter posture to mirror; the LOCAL-only fs discipline)
  </read_first>
  <behavior>
    - getReferenceNow({ currentDate, now, seamPath }) returns { referenceMs, source } where source is one of 'currentDate' | 'online_skew' | 'seam' | 'date_now'.
    - When a valid injected currentDate (YYYY-MM-DD) is provided, the returned referenceMs lands on that calendar date (UTC midnight floor + any available time-of-day from the skew corrector or seam), and source reflects currentDate precedence. Per D-01b currentDate wins for the calendar DATE.
    - When no currentDate and a fresh seam value exists, return the seam value (source 'seam').
    - When neither currentDate nor seam is usable, fall back to now() (default Date.now), source 'date_now'. Never throws.
    - The optional online skew corrector is a SEPARATE function fetchTimeOffsetOnce(opts) that performs at most ONE bare GET per session to a public time endpoint (HTTP Date header), computes offset = authoritativeMs - Date.now(), persists offset to the seam, and on ANY network/parse failure returns null silently (degrades). getReferenceNow consumes a cached offset only; it makes NO network call itself.
    - The now seam reader is compatible with the options.now contract: getReferenceNow returns a millisecond integer usable directly as the value an options.now closure would yield.
  </behavior>
  <action>
    Create lib/core/temporal/reference-now.cjs implementing the D-01b precedence ladder as pure, sync, LOCAL functions plus one optional async skew fetcher. Export getReferenceNow(opts), readSeam(seamPath), writeSeam(seamPath, partial), applyCurrentDate(currentDateStr, baseMs), and fetchTimeOffsetOnce(opts). The seam file default path is ~/.mindrian/reference-now.json holding { floorMs, offsetMs, lastFetchSession, source, updatedAt }. getReferenceNow precedence: injected currentDate (calendar date floor, blended with seam offset for time-of-day) beats a fresh seam value beats now() floor, per D-01b. fetchTimeOffsetOnce does a single bare GET (native fetch) reading ONLY the HTTP Date response header, computes the skew offset, writes it to the seam, and returns null on any failure (Part 8 SIGNAL, zero user bytes - send no body, no query params, no headers carrying user data). getReferenceNow itself performs NO network call; it reads the cached offset from the seam. Wrap every fs and network touch in try/catch that degrades to the next rung. House rule: hyphens only, no em-dashes. Reuse isoSecond from timeline-renderer.cjs for any ISO rendering rather than re-implementing. Write the tests first in reference-now.test.cjs: a fixed-currentDate-vs-divergent-system-clock case asserting the returned date matches currentDate, a seam-fallback case, a no-input Date.now floor case, and an offline fetchTimeOffsetOnce case asserting null + no throw.
  </action>
  <verify>
    <automated>node --test lib/core/temporal/reference-now.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test lib/core/temporal/reference-now.test.cjs passes all cases.
    - A test injecting currentDate=2026-06-16 with now() stubbed to a different calendar date asserts getReferenceNow().referenceMs falls on 2026-06-16 (SPEC R1 acceptance a + b).
    - A test stubbing fetchTimeOffsetOnce's fetch to throw asserts it returns null and getReferenceNow still returns a valid reference with no thrown error (SPEC R1 acceptance c).
    - grep confirms fetchTimeOffsetOnce sends no request body and no user-derived query string.
  </acceptance_criteria>
  <done>getReferenceNow resolves the precedence ladder; currentDate wins the calendar date; offline degrades cleanly; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: SessionStart seeder, Larry correction command, Part 8 acceptance</name>
  <read_first>
    - hooks/hooks.json (the SessionStart array - where the seeder registers; mirror the existing entry shape at line ~33)
    - scripts/sessionstart-coordinator.cjs (the existing SessionStart composition pattern to follow; LOCAL-only, no network)
    - commands/file-meeting.md frontmatter (a command frontmatter shape to mirror for the correction command)
    - lib/core/temporal/reference-now.cjs (Task 1 output - the seam writer this seeder and command call)
  </read_first>
  <action>
    Create scripts/sessionstart-reference-now-seed.cjs: on SessionStart it calls writeSeam to seed ~/.mindrian/reference-now.json with a Date.now() floor if absent or stale-by-session (D-01), and OPTIONALLY kicks fetchTimeOffsetOnce (fire-once-per-session, degrades silently). It performs zero Brain query and zero room-data read; it is pure LOCAL bookkeeping. Register it in hooks/hooks.json under SessionStart following the existing run-hook.cmd entry shape. Create commands/correct-reference-now.md: a tiny Larry-facing command (per D-01) that, when Larry sees Claude Code's injected currentDate diverging from the seeded value, calls applyCurrentDate + writeSeam to correct the seam's calendar date. The command frontmatter declares allowed-tools Bash + Read and a serves_jtbd of temporal-correction. Create tests/test-reference-now-part8.cjs: an adversarial sweep asserting reference-now.cjs and the seeder contain zero Brain egress (grep for brain/mcp/tavily/onrender against the source - filtered with grep -v '^#' so header prose does not self-invalidate) and that the only network surface (fetchTimeOffsetOnce) carries no user bytes. House rule: hyphens only.
  </action>
  <verify>
    <automated>node tests/test-reference-now-part8.cjs && node -e "require('./scripts/sessionstart-reference-now-seed.cjs')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-reference-now-part8.cjs exits 0; the Part 8 sweep over reference-now.cjs + the seeder finds zero forbidden Brain-egress tokens (using grep -v '^#' before the count, no bare ==0 on an unfiltered file).
    - hooks/hooks.json parses as valid JSON and contains the reference-now seeder registration under SessionStart.
    - commands/correct-reference-now.md has valid frontmatter (name, description, allowed-tools, serves_jtbd).
    - The Phase 157 Part 8 boundary scan stays GREEN after this plan (no new Brain wire).
  </acceptance_criteria>
  <done>SessionStart seeds the seam; Larry can correct it; the Part 8 sweep is green; the seeder loads without error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| online time endpoint -> plugin | untrusted public response (HTTP Date header) crosses here; SIGNAL only |
| plugin -> ~/.mindrian/reference-now.json | LOCAL seam write; never leaves the machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | Information Disclosure | fetchTimeOffsetOnce online GET | mitigate | bare GET, no body, no user-derived query/header; read only the Date header; Part 8 sweep test gates it |
| T-160-02 | Tampering | malformed/future-dated seam value | mitigate | getReferenceNow validates seam shape, degrades to Date.now() on parse failure; future-dated offset clamped |
| T-160-03 | Denial of Service | unreachable time endpoint blocking SessionStart | mitigate | fetchTimeOffsetOnce is fire-once-per-session, async, degrades to null silently; seeder never blocks on it |
| T-160-SC | Tampering | npm/pip/cargo installs | mitigate | this plan adds NO new dependency (native fetch only); slopcheck N/A |
</threat_model>

<verification>
- node --test lib/core/temporal/reference-now.test.cjs green (R1 acceptance a/b/c).
- node tests/test-reference-now-part8.cjs exits 0 (Part 8 SIGNAL, zero user bytes).
- hooks/hooks.json valid JSON with the seeder registered.
- Phase 157 Part 8 boundary scan stays GREEN.
</verification>

<success_criteria>
getReferenceNow() resolves to the injected currentDate calendar date when present; deltas computed downstream stop flipping at midnight; the online rung degrades cleanly offline with zero user bytes; the seam seeds on SessionStart and Larry can correct it.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `getReferenceNow()` - the precedence-ladder reference clock (NEW symbol)
- `fetchTimeOffsetOnce()` - the optional NTP-style skew corrector (NEW symbol)
- `readSeam` / `writeSeam` / `applyCurrentDate` - seam helpers (NEW symbols)
- `~/.mindrian/reference-now.json` - the LOCAL reference-now seam file (NEW artifact)
- `commands/correct-reference-now.md` - the Larry-facing seam correction tool (NEW command)
- `scripts/sessionstart-reference-now-seed.cjs` - SessionStart seeder (NEW script)
</artifacts>

<output>
Create `.planning/phases/160-temporal-awareness-spine/160-01-SUMMARY.md` when done
</output>
