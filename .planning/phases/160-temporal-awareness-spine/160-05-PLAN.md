---
phase: 160-temporal-awareness-spine
plan: 05
type: execute
wave: 3
depends_on: [160-04]
files_modified:
  - lib/core/temporal/point-in-time.cjs
  - lib/core/temporal/point-in-time.test.cjs
  - lib/core/navigation/insights.cjs
  - lib/core/navigation/insights-stale.test.cjs
  - lib/core/navigation.cjs
autonomous: true
requirements: [R9, R10]
canon_parts: [Part 5, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "For a constructed 3-version timeline the point-in-time helper returns the correct fact at three distinct (T_tx, T_v) points"
    - "A point-in-time query as-of before a supersession still returns the superseded fact"
    - "Generalized stale detection flags a 31-day-old confirmed claim and does NOT flag a 29-day-old one"
    - "Stale detection covers claims/assumptions/opportunities with a configurable window (default 30 days)"
  artifacts:
    - path: "lib/core/temporal/point-in-time.cjs"
      provides: "as-of (T_tx, T_v) bitemporal query helper"
      exports: ["queryAsOf"]
    - path: "lib/core/navigation/insights.cjs"
      provides: "generalized stale detection beyond decisions (findStaleClaims)"
      exports: ["findStaleClaims"]
  key_links:
    - from: "lib/core/temporal/point-in-time.cjs"
      to: "nodes bitemporal columns (Plan 04)"
      via: "the created_at<=T_tx AND (invalidated_at IS NULL OR invalidated_at>T_tx) AND valid_from<=T_v AND (valid_to IS NULL OR valid_to>T_v) WHERE clause"
      pattern: "invalidated_at|valid_from|valid_to|T_tx|T_v"
    - from: "lib/core/navigation/insights.cjs findStaleClaims"
      to: "lib/core/navigation.cjs"
      via: "re-exported on the navigation chokepoint surface"
      pattern: "findStaleClaims|findStaleDecisions"
---

<objective>
Deliver Wave 4 part B - historical reconstruction and generalized staleness. R9 - one point-in-time query helper implementing the canonical bitemporal WHERE clause (created_at <= T_tx AND (invalidated_at IS NULL OR invalidated_at > T_tx) AND valid_from <= T_v AND (valid_to IS NULL OR valid_to > T_v)) so historical state is reconstructable. R10 - generalize stale detection beyond findStaleDecisions to claims/assumptions/opportunities with a configurable window (default 30 days), generalizing the single 30-day computation at insights.cjs:109-116.

Purpose: Makes "what was true at phase X" answerable and closes the supersession round-trip from Plan 04 (an as-of query before supersession still returns the superseded fact). Generalizes the only relative-time computation in the system to all claim types.
Output: the point-in-time helper, the generalized stale detector, both on the navigation chokepoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-temporal-awareness-spine/160-SPEC.md
@.planning/phases/160-temporal-awareness-spine/160-CONTEXT.md
@.planning/phases/160-temporal-awareness-spine/160-04-SUMMARY.md
@docs/MINDRIAN-CANON.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: point-in-time (T_tx, T_v) query helper</name>
  <read_first>
    - lib/core/navigation/insights.cjs:109-140 (the existing query-helper idiom: db.prepare a SELECT, map rows to a typed object - the shape queryAsOf follows; these helpers are re-exported on navigation.cjs)
    - lib/core/migrations/phase-160-nodes-bitemporal.cjs (Plan 04 output - the valid_from/valid_to/invalidated_at columns the WHERE clause reads)
    - lib/core/temporal/supersession.cjs (Plan 04 output - queryAsOf must return a fact superseded AFTER T_tx but invalidated; the round-trip with R8)
    - 160-RESEARCH.md prior-art item 2 (the canonical point-in-time WHERE clause, NULL = open/unbounded; ports to SQLite with no graph engine)
    - 160-SPEC.md R9 acceptance (3-version timeline, correct fact at 3 distinct (T_tx, T_v) points)
  </read_first>
  <behavior>
    - queryAsOf(db, nodeKey, T_tx, T_v) returns the single fact version that was known-as-of T_tx and valid-at T_v, applying created_at <= T_tx AND (invalidated_at IS NULL OR invalidated_at > T_tx) AND valid_from <= T_v AND (valid_to IS NULL OR valid_to > T_v). NULL invalidated_at/valid_to = open/unbounded.
    - For a constructed 3-version timeline (three rows sharing a logical key with distinct valid_from/valid_to and invalidated_at), queryAsOf returns the correct version at three distinct (T_tx, T_v) points.
    - An as-of query with T_tx before a supersession still returns the superseded fact (closes the Plan 04 R8 round-trip).
    - Pure LOCAL read; caller-owned db; no egress.
  </behavior>
  <action>
    Create lib/core/temporal/point-in-time.cjs exporting queryAsOf(db, nodeKey, T_tx, T_v). Implement the canonical bitemporal WHERE clause verbatim (RESEARCH item 2) against the Plan 04 columns, returning the matching version (or null). Re-export queryAsOf on the navigation.cjs chokepoint surface following the existing additive-re-export idiom (name the analog: the logMemoryEvent / writeEdge re-export pattern). Write point-in-time.test.cjs FIRST: construct a 3-version timeline (insert three rows with stepped valid_from/valid_to/invalidated_at), assert queryAsOf returns the correct version at three distinct (T_tx, T_v) points, AND assert an as-of-before-supersession query (building A then superseding with B via Plan 04 supersede) still returns A. House rule: hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node --test lib/core/temporal/point-in-time.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test asserts queryAsOf returns the correct fact at three distinct (T_tx, T_v) points over a 3-version timeline (SPEC R9 acceptance).
    - A test asserts an as-of query before a supersession (using Plan 04 supersede) still returns the superseded fact (SPEC R8 round-trip closed).
    - queryAsOf is re-exported on navigation.cjs and reads only room.db (no egress); Phase 157 boundary scan stays GREEN.
  </acceptance_criteria>
  <done>queryAsOf reconstructs historical state via the canonical bitemporal WHERE clause; the supersession round-trip is closed; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generalized stale detection beyond decisions</name>
  <read_first>
    - lib/core/navigation/insights.cjs:110-125 (findStaleDecisions - the ONLY relative-time computation; the 30-day threshold + review_status='confirmed' + last_seen_at idiom to generalize, NOT duplicate, per Part 7)
    - lib/core/navigation.cjs:62-67 (the insights re-exports - findStaleClaims joins this surface)
    - 160-SPEC.md R10 acceptance (31-day confirmed claim flagged; 29-day not; configurable window default 30 days; covers claims/assumptions/opportunities)
  </read_first>
  <behavior>
    - findStaleClaims(db, roomId, opts) generalizes findStaleDecisions to node types in {claim, assumption, opportunity, decision} with a configurable opts.windowDays (default 30), flagging review_status='confirmed' (or 'validated') nodes whose last_modified_at (write-time, from Plan 04 - NOT last_seen_at which conflates read/write) is older than the window against getReferenceNow().
    - A 31-day-old confirmed claim is flagged; a 29-day-old one is not (SPEC R10).
    - The window is configurable; default 30 days; uses getReferenceNow() (options.now seam) so tests inject a fixed reference.
  </behavior>
  <action>
    Edit lib/core/navigation/insights.cjs to add findStaleClaims(db, roomId, opts) that generalizes the findStaleDecisions logic (Part 7 reuse - name the analog: findStaleDecisions:110-125) to claim/assumption/opportunity/decision types with a configurable windowDays (default 30) computed against getReferenceNow(), keying staleness on last_modified_at (the Plan 04 write-time stamp) so a node merely read does not look fresh-or-stale incorrectly. Keep findStaleDecisions intact for back-compat. Re-export findStaleClaims on navigation.cjs alongside findStaleDecisions. Write insights-stale.test.cjs FIRST: assert a 31-day-old confirmed claim is flagged and a 29-day-old one is not, across claim/assumption/opportunity types, with a fixed injected reference. House rule: hyphens only.
  </action>
  <verify>
    <automated>node --test lib/core/navigation/insights-stale.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test asserts a 31-day-old confirmed claim is flagged stale and a 29-day-old one is not (SPEC R10 acceptance).
    - A test asserts coverage across claim/assumption/opportunity types with a configurable window (default 30 days).
    - findStaleClaims is re-exported on navigation.cjs; findStaleDecisions is unchanged (back-compat); Phase 157 boundary scan stays GREEN.
  </acceptance_criteria>
  <done>Generalized stale detection flags 31-day and not 29-day claims across all claim types with a configurable window; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room.db bitemporal rows -> queryAsOf | LOCAL read of historical versions; never egress |
| room.db -> findStaleClaims | LOCAL staleness read; scalar flags only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-12 | Tampering | wrong as-of version returned (off-by-one on open intervals) | mitigate | the canonical WHERE clause uses strict > on invalidated_at/valid_to with NULL=open; the 3-version timeline test pins the boundaries |
| T-160-13 | Information Disclosure | stale-detection / as-of egress | mitigate | both read only room.db; scalar/enum output only; Phase 157 boundary scan green |
| T-160-14 | Tampering | staleness keyed on read time (last_seen_at) misflagging | mitigate | findStaleClaims keys on last_modified_at (Plan 04 write-time stamp), not last_seen_at |
| T-160-SC | Tampering | npm/pip/cargo installs | mitigate | this plan adds NO new dependency; slopcheck N/A |
</threat_model>

<verification>
- node --test lib/core/temporal/point-in-time.test.cjs green (R9 3-version timeline + R8 round-trip).
- node --test lib/core/navigation/insights-stale.test.cjs green (R10 31-vs-29-day across types).
- Phase 157 Part 8 boundary scan stays GREEN.
</verification>

<success_criteria>
The point-in-time helper returns correct facts for a 3-version timeline at 3 (T_tx, T_v) points and an as-of-before-supersession query still returns the superseded fact; generalized stale detection flags a 31-day claim and not a 29-day claim across all claim types.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)
- `queryAsOf()` - the point-in-time (T_tx, T_v) bitemporal query helper (NEW symbol, re-exported on navigation.cjs)
- `findStaleClaims()` - generalized stale detection beyond decisions (NEW symbol, re-exported on navigation.cjs)
</artifacts>

<output>
Create `.planning/phases/160-temporal-awareness-spine/160-05-SUMMARY.md` when done
</output>
