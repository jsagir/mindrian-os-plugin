---
phase: 193-brain-substrate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-193.sh
  - tests/no-hardcoded-framework-magnitude.test.cjs
autonomous: true
requirements: [BSH-08]

must_haves:
  truths:
    - "bash tests/run-all-193.sh exits 0 before any other Phase 193 plan lands, with every runtime leg reported SKIPPED"
    - "The hardcoded-magnitude guard exists and runs (not SKIP-gated) from the moment this plan lands, checking whatever Phase 193 artifacts already exist"
    - "Once later plans land their artifacts, the SAME run-all-193.sh flips each leg from SKIP to a real PASS/FAIL without needing to be re-edited"
  artifacts:
    - path: "tests/run-all-193.sh"
      provides: "SKIP-safe PASS/FAIL/SKIP aggregator for Phase 193 (run/run_if scaffold cloned from tests/run-all-196.sh)"
      contains: "run_if"
    - path: "tests/no-hardcoded-framework-magnitude.test.cjs"
      provides: "Always-on static guard: no Phase-193-owned artifact restates a framework-coverage magnitude as a fixed fact"
      contains: "43"
  key_links:
    - from: "tests/run-all-193.sh"
      to: "scripts/framework-live-coverage-report.test.cjs"
      via: "run_if guarded on the Plan-02 test file's presence"
      pattern: "run_if.*framework-live-coverage-report"
---

<objective>
Author the Wave-1 test harness FIRST (Nyquist rule: tests precede implementation). Build a SKIP-safe
PASS/FAIL/SKIP aggregator for Phase 193, plus a standalone, always-on guard that enforces the phase's
single hardest rule verbatim: the live framework-coverage magnitude is measured fresh every time, never
restated as a cached fact (CONTEXT.md hard guard: do NOT restate "43% of 176 dark" as fact).

Purpose: every later Phase 193 plan (02-05) is verified against the SAME run-all-193.sh without needing
to edit it again -- each plan's deliverable is a new file this script already references via a presence
guard, so the leg flips from SKIPPED to a real PASS/FAIL the moment that file lands. The magnitude guard
gives the "measure live, never hardcode" rule an automated, non-negotiable check from turn one.
Output: tests/run-all-193.sh + tests/no-hardcoded-framework-magnitude.test.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-brain-substrate-hardening/193-CONTEXT.md
@tests/run-all-196.sh
@docs/MINDRIAN-CANON.md
</context>

<rules>
RULES (restate every wave, non-negotiable):
- CONTEXT.md Scope items (1)/(2)/(3) each get their own plan (02, 04, 03 respectively); this plan (01)
  is shared infra only -- it creates no runtime module.
- Hard guard (CONTEXT.md): a Phase 193 artifact must never assert a framework-coverage magnitude as a
  cached fact ("43% of 176 dark"). The guard in this plan is the automated enforcement of that sentence.
- CJS/bash only. NO em-dashes anywhere (hyphens only). Zero new npm packages.
- Part 8: this plan makes ZERO Brain/network calls. It only reads local files.
- Resumable: this plan owns ONLY the two files in files_modified. Do not create runtime modules here.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: tests/run-all-193.sh SKIP-safe aggregator</name>
  <files>tests/run-all-193.sh</files>
  <read_first>
    - tests/run-all-196.sh (VERIFIED pattern: set -uo pipefail, ROOT resolution + cd, PASS/FAIL/SKIP
      counters, run() and run_if() functions guarded on a FILE PATH, the counts-plus-exit summary tail)
    - .planning/phases/193-brain-substrate-hardening/193-CONTEXT.md (the three scope items this harness
      must have a leg for)
  </read_first>
  <action>
    Clone the run-all-196.sh scaffold verbatim: set -uo pipefail, ROOT resolution + cd, PASS/FAIL/SKIP
    counters, run() and run_if() functions, and the counts-plus-exit-[ FAIL -eq 0 ] summary tail.
    Add one ALWAYS-ON leg (never SKIP-gated, runs from Wave 1 onward):
      - run "no-hardcoded-framework-magnitude guard (BSH-08)" node tests/no-hardcoded-framework-magnitude.test.cjs
    Add these run_if legs, each guarded on the FILE the later plan creates (all absent today, so all
    print SKIPPED until that plan lands -- guard on the deliverable file, not on a file that already
    exists in the repo, per the reasoning below):
      - run_if "framework-live-population snapshot (BSH-01)" scripts/build-command-registry-live-population.test.cjs node scripts/build-command-registry-live-population.test.cjs
      - run_if "framework-live coverage report (BSH-02)" scripts/framework-live-coverage-report.test.cjs node scripts/framework-live-coverage-report.test.cjs
      - run_if "orphan disposition gate (BSH-05/06)" scripts/check-orphan-disposition.test.cjs node scripts/check-orphan-disposition.test.cjs
      - run_if "canonicalize-at-ingest (BSH-03/04)" scripts/canonicalize-framework-alias.test.cjs node scripts/canonicalize-framework-alias.test.cjs
      - run_if "canonicalize-at-ingest ingestPlan integration (BSH-03)" tests/test-methodology-ingest.cjs node tests/test-methodology-ingest.cjs
      - run_if "Plurai eval baseline present (BSH-07)" evals/plurai/193-baseline.json test -f evals/plurai/193-baseline.json
    Note in a comment why the guard file differs from the 196 precedent: scripts/build-command-registry.cjs
    and lib/core/methodology-ingest.cjs (and its test tests/test-methodology-ingest.cjs) ALREADY EXIST
    pre-phase (they are Phase 122/171 shipped modules this phase EXTENDS), so guarding on their mere
    presence would never SKIP; guard on the NEW deliverable file each later plan creates instead. The
    tests/test-methodology-ingest.cjs leg is guarded on itself because it is an existing file that gains
    new Phase-193 assertions in Plan 04 -- it already passes today (existing Phase 171 assertions), and
    stays green through Plan 04 (additive assertions), so it is intentionally NOT SKIP-gated; run it
    unconditionally like the magnitude guard.
    No em-dashes, no emoji, bash only.
  </action>
  <verify>
    <automated>bash tests/run-all-193.sh; echo "exit=$?"</automated>
  </verify>
  <done>run-all-193.sh exists, is SKIP-safe, exits 0 with the five presence-guarded legs SKIPPED and the
  two always-on legs (magnitude guard + existing methodology-ingest test) PASSING, FAIL=0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: no-hardcoded-framework-magnitude.test.cjs (always-on guard)</name>
  <files>tests/no-hardcoded-framework-magnitude.test.cjs</files>
  <read_first>
    - docs/CANON-RECALIBRATION-PROPOSAL.md line ~73 ("State magnitude as a DIRECTIONAL gap, admin-gated
      -- never '43% of 176 are dark' as fact") -- this is the ANTI-PATTERN being documented, do NOT scan
      this file (a false positive: it correctly discusses the rule, it does not violate it)
    - .planning/phases/193-brain-substrate-hardening/193-CONTEXT.md (the exact hard-guard sentence)
  </read_first>
  <behavior>
    - Scans a FIXED, NARROW allowlist of files this phase is allowed to own (never the whole repo, which
      would false-positive on docs that correctly document the anti-pattern as an anti-pattern):
      data/framework-live-population.json, data/framework-live-coverage-report.json, and every
      *-SUMMARY.md under .planning/phases/193-brain-substrate-hardening/. A file absent from disk is
      skipped for that check (nothing to violate yet); this is NOT the same as a SKIP-gated leg -- the
      test itself always runs and always reports its outcome, it just has nothing to flag before Plan 02
      lands.
    - For each present file, read the full text and fail if it contains the digits "43" immediately
      followed by an optional space then "%", in the same file as either the substring "176" or the
      word "dark" (case-insensitive). This is the literal fossilized claim CONTEXT.md forbids restating.
    - Structural check: if data/framework-live-coverage-report.json exists, parse it as JSON and assert
      `dark_count` and `live_population_count` are typeof 'number' (never string-typed "43%"-style
      literals baked into the JSON).
    - Test 1: an allowlisted file that does not exist yet -> the check for that file passes trivially
      (0 violations), the overall test still exits 0.
    - Test 2 (fixture): write a throwaway temp file (NOT one of the tracked paths -- use
      node:fs mkdtemp under os.tmpdir()) containing the literal "43% of 176 frameworks are dark", feed
      its path through the SAME scanning function this test uses on the real files, and assert the
      scanning function flags it. This proves the detector itself works before trusting its silence on
      the real tracked paths.
  </behavior>
  <action>
    Write a plain-node assert script (no test framework, zero-dep, mirrors tests/test-methodology-ingest.cjs
    style: an ok(name, cond) helper wrapping assert.ok, process.exit(1) on any failure via a final check).
    Export the scanning function itself (e.g. containsBannedMagnitudeClaim(text)) is not required since
    this is a top-level script, but structure the file so the detector logic is a small named local
    function reused for both the fixture test and the real tracked-path scan (do not duplicate the
    regex). Exit 0 with a summary line when clean; exit 1 with the offending file path(s) and the matched
    snippet when a violation is found.
  </action>
  <verify>
    <automated>node tests/no-hardcoded-framework-magnitude.test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>Test file is valid node, runs unconditionally (not SKIP-gated), passes today (no Phase 193
  artifacts exist yet to violate the rule), and its self-test fixture proves the detector actually
  catches the banned phrasing when present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local test file -> local test file | No external or remote surface; this plan reads only files already on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-01 | Tampering | tests/run-all-193.sh | mitigate | Every leg name cites its BSH-NN requirement id, so a leg quietly removed or disabled in a future diff is visible in code review, not just in test output |
| T-193-02 | Repudiation | tests/no-hardcoded-framework-magnitude.test.cjs | accept | A guard that is itself silently deleted would go undetected by this plan alone; accepted here because Plan 05's Plurai eval and the standing tests/run-all-193.sh leg both independently re-assert the file's presence expectation via the phase gate |
</threat_model>

<verification>
`bash tests/run-all-193.sh` exits 0. The magnitude guard and the pre-existing methodology-ingest test
both PASS unconditionally; the five presence-guarded legs report SKIPPED (their target files do not
exist until Plans 02-05 land).
</verification>

<success_criteria>
tests/run-all-193.sh and tests/no-hardcoded-framework-magnitude.test.cjs exist, are committed, and the
aggregator exits 0. No runtime module (framework fetch, coverage report, canonicalize logic, orphan
gate, Plurai CSV) is created by this plan -- that is Plans 02-05.
</success_criteria>

<output>
Create `.planning/phases/193-brain-substrate-hardening/193-01-SUMMARY.md` when done
</output>
