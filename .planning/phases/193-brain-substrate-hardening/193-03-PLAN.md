---
phase: 193-brain-substrate-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/command-map/INDEX.md
  - scripts/check-orphan-disposition.cjs
  - scripts/check-orphan-disposition.test.cjs
autonomous: true
requirements: [BSH-05, BSH-06]

must_haves:
  truths:
    - "correct-reference-now, hmi-status, organize, and dogfood-flush are each documented and machine-verified as an ACCEPTED excluded utility (Canon Part 11 R1 conformant terminal state), not an open orphan question"
    - "memory-cortex-reach is corrected in the research record: it is WIRED to the connector spine (reach_id cross_room, sensor_triggers [SENS-08]), not an ungoverned near-orphan -- the prior INDEX.md framing measured graph-connectivity only and missed the connector-governance axis"
    - "A standing automated gate fails if any of the 5 commands silently loses its disposition (an excluded command re-enabled without a reason, or memory-cortex-reach losing connects_to_spine) in a future edit"
  artifacts:
    - path: "scripts/check-orphan-disposition.cjs"
      provides: "Standing regression gate over the 5 Phase 193 orphan-scope commands"
      contains: "checkDispositions"
    - path: ".planning/research/command-map/INDEX.md"
      provides: "Corrected ORPHANS section distinguishing graph-connectivity signal from connector-governance disposition"
      contains: "correct-reference-now"
  key_links:
    - from: "scripts/check-orphan-disposition.cjs"
      to: "scripts/build-command-registry.cjs"
      via: "reuse of the existing exported parseFrontmatter (no second frontmatter parser)"
      pattern: "parseFrontmatter"
---

<objective>
Dispose of Phase 193's orphan scope per CONTEXT.md Scope item (3): decide correct-reference-now (the
1 true command-level orphan) and clear/document the 4 near-orphans (hmi-status, organize, dogfood-flush,
memory-cortex-reach). Research (this session's investigation) found all 5 ALREADY carry a connector-layer
disposition in their own commands/*.md frontmatter -- 4 are `excluded: true` with a reason on file, and
memory-cortex-reach is actually WIRED (connects_to_spine: true, reach_id: cross_room). The
.planning/research/command-map/INDEX.md ORPHANS section measures a DIFFERENT axis (Neo4j Framework-node
presence + declared "related commands") and frames all 5 as if ungoverned, which is stale/incomplete, not
wrong on its own axis. This plan reconciles the record and adds a standing gate so the reconciliation
cannot silently drift.

Purpose: close CONTEXT.md Scope item (3) without inventing new dispositions where correct ones already
exist -- the fix is RECONCILING two measurements, not re-deciding governed commands.
Output: a corrected INDEX.md ORPHANS section + a standing check-orphan-disposition.cjs gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-brain-substrate-hardening/193-CONTEXT.md
@.planning/research/command-map/INDEX.md
@commands/correct-reference-now.md
@commands/hmi-status.md
@commands/organize.md
@commands/dogfood-flush.md
@commands/memory-cortex-reach.md
</context>

<rules>
RULES (restate every wave, non-negotiable):
- Do not re-decide a disposition that already exists in commands/*.md frontmatter. This plan documents
  and machine-verifies existing dispositions; it does not flip excluded->wired or invent a new reason.
- Part 7 reuse: reuse scripts/build-command-registry.cjs's exported parseFrontmatter for the connector
  block extraction; do not author a second hand-rolled frontmatter parser.
- Part 8: zero Brain/network calls in this plan. Purely local files.
- CJS/markdown only. NO em-dashes anywhere.
- Resumable: this plan owns ONLY the three files in files_modified.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile .planning/research/command-map/INDEX.md ORPHANS section</name>
  <files>.planning/research/command-map/INDEX.md</files>
  <read_first>
    - .planning/research/command-map/INDEX.md section "## 3. ORPHANS" (the current text: frames
      correct-reference-now as "the only fully isolated command" and lists hmi-status/organize/
      dogfood-flush/memory-cortex-reach as "near-orphans worth flagging" purely by Framework-node absence
      and low related-command count)
    - commands/correct-reference-now.md connector frontmatter (excluded: true, reason: "Lifecycle
      command. A reference-correction maintenance action the navigator runs deliberately to fix a
      citation; an upkeep surface, not a problem-state reach.")
    - commands/hmi-status.md connector frontmatter (excluded: true, reason: "Lifecycle command. Inspects
      HMI / dial render status; an internal diagnostics surface the maintainer runs deliberately, not a
      contextual reach.")
    - commands/organize.md connector frontmatter (excluded: true, reason references an INV-06 promotion
      candidate note)
    - commands/dogfood-flush.md connector frontmatter (excluded: true, reason: "Lifecycle command. A
      dog-fooding maintenance flush the maintainer runs deliberately; an internal upkeep action with no
      navigator problem-state trigger.")
    - commands/memory-cortex-reach.md connector frontmatter (connects_to_spine: true, sensor_triggers:
      [SENS-08], reach_id: cross_room, sub_mode: memory-cortex-bridge -- this command is WIRED, not
      excluded, and carries no `excluded` key at all)
  </read_first>
  <action>
    Rewrite section "## 3. ORPHANS" to keep the existing graph-connectivity finding (it is correct on its
    own axis: no Framework node, sparse related-command declarations) but ADD the connector-governance
    disposition for each of the 5 names, and correct the framing so a future reader does not mistake
    "graph-orphan" for "ungoverned". Structure:
    - Open with one paragraph distinguishing the two measurements: graph-connectivity (Neo4j Framework
      node + declared related commands, what this INDEX measures) versus connector-governance disposition
      (the `connector:` frontmatter block on the command file itself, Canon Part 11 R1 wired-XOR-excluded
      discipline). A command can be a graph-orphan and still be fully governed.
    - For correct-reference-now: keep the "1 true orphan by graph signal" finding, then state its
      disposition is ACCEPTED / EXCLUDED (Phase 193 disposition, 2026-07 cycle): its own connector
      frontmatter already carries excluded: true with the reason quoted above -- this is a Canon Part 11
      R1 conformant terminal state, not an open question. No further action.
    - For hmi-status, organize, dogfood-flush: same treatment -- each is a graph near-orphan, each
      already carries excluded: true with a reason on file, each is ACCEPTED. Quote each reason briefly.
      For organize specifically, retain the note that its reason itself flags an INV-06 future promotion
      candidate (a contextual trigger on filing pressure) -- that stays a forward-looking note, not a gap.
    - For memory-cortex-reach: CORRECT the classification. It is a graph near-orphan (no Framework node,
      few related commands) but it is NOT a connector-governance orphan -- it is WIRED
      (connects_to_spine: true, reach_id: cross_room, sensor_triggers: [SENS-08]). Remove it from the
      "near-orphan" framing; state plainly that the prior listing measured only the graph axis and missed
      that this command already rides the connector spine.
    - Keep the section title "## 3. ORPHANS" so cross-references elsewhere in the document still resolve;
      do not renumber other sections.
  </action>
  <verify>
    <automated>grep -q "connector-governance" .planning/research/command-map/INDEX.md && grep -c "excluded" .planning/research/command-map/INDEX.md | grep -qv '^0$' && echo OK</automated>
  </verify>
  <done>Section 3 documents both axes for all 5 commands; memory-cortex-reach is no longer framed as an
  ungoverned near-orphan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: check-orphan-disposition.cjs standing regression gate</name>
  <files>scripts/check-orphan-disposition.cjs, scripts/check-orphan-disposition.test.cjs</files>
  <read_first>
    - scripts/build-command-registry.cjs (the exported parseFrontmatter function -- reuse this exact
      parser rather than writing a second one; confirm its return shape by reading how buildRegistry()
      consumes it)
    - commands/correct-reference-now.md, commands/hmi-status.md, commands/organize.md,
      commands/dogfood-flush.md, commands/memory-cortex-reach.md (the 5 frontmatter blocks this gate
      asserts against, already read in Task 1)
  </read_first>
  <behavior>
    - checkDispositions() reads the 5 fixed command files (a hardcoded DATA array of the 5 slugs Phase
      193 disposed -- ['correct-reference-now', 'hmi-status', 'organize', 'dogfood-flush',
      'memory-cortex-reach'], not a magic-fact number, just the exact scope this phase closed), parses
      each command's frontmatter via the reused parser, and returns an array of
      { command, expected: 'excluded'|'wired', actual: 'excluded'|'wired'|'neither', ok: boolean,
        reason_present: boolean }.
    - For the 4 excluded-expected commands: ok is true iff connector.excluded === true AND
      connector.reason is a non-empty string.
    - For memory-cortex-reach: ok is true iff connector.connects_to_spine === true AND connector.reach_id
      is a non-empty string.
    - A command file that fails to parse, or whose connector block matches neither expected shape, is
      classified actual: 'neither', ok: false (fail closed, never silently pass an unreadable file).
  </behavior>
  <action>
    Write scripts/check-orphan-disposition.cjs. Require scripts/build-command-registry.cjs's exported
    parseFrontmatter (do not duplicate frontmatter parsing). Implement checkDispositions() per the
    behavior block as a pure-ish function (file reads only, no network) exported for the test. Implement
    a thin CLI: default mode prints each of the 5 rows (command, expected, actual, ok); `--check` mode
    exits 1 with a clear per-command failure message and a recovery note ("a disposed command's connector
    frontmatter changed -- restore excluded+reason or connects_to_spine+reach_id, or re-run Phase 193's
    disposition review") if ANY row is not ok, else prints "orphan-disposition: OK (5/5)" and exits 0.
  </action>
  <verify>
    <automated>node scripts/check-orphan-disposition.test.cjs; echo "exit=$?"</automated>
  </verify>
  <done>checkDispositions() is exported and unit-tested to assert all 5 rows ok: true against the CURRENT
  repo state; the CLI --check mode exits 0 today; a spawnSync smoke test confirms the CLI itself runs
  cleanly end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local command frontmatter -> local regression gate | No remote surface; purely local file reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-07 | Tampering | commands/*.md connector frontmatter | mitigate | scripts/check-orphan-disposition.cjs --check fails closed if any of the 5 commands' excluded/reason or connects_to_spine/reach_id fields are edited away; wiring this into the release gate is a documented follow-up, not in this plan's budget |
| T-193-08 | Repudiation | .planning/research/command-map/INDEX.md | accept | The research doc itself has no automated drift check (it is prose, not a machine-read artifact); the standing gate in Task 2 is the enforceable half, the doc is the human-readable half |
</threat_model>

<verification>
`node scripts/check-orphan-disposition.test.cjs` exits 0. `bash tests/run-all-193.sh` (from Plan 01)
flips the orphan-disposition leg from SKIPPED to PASSED.
</verification>

<success_criteria>
All 5 commands in Phase 193's orphan scope have an explicit, machine-checked disposition; the research
record no longer frames any of them as an open orphan question; a future silent regression is caught by
`node scripts/check-orphan-disposition.cjs --check`.
</success_criteria>

<output>
Create `.planning/phases/193-brain-substrate-hardening/193-03-SUMMARY.md` when done
</output>
