---
phase: 193-brain-substrate-hardening
plan: 05
type: execute
wave: 3
depends_on: ["193-03", "193-04"]
files_modified:
  - evals/plurai/09-canonicalize-orphan-disposition.csv
  - evals/plurai/README.md
  - evals/plurai/193-baseline.json
autonomous: true
requirements: [BSH-07]

must_haves:
  truths:
    - "The synthetic CSV covers both judged behaviors -- canonicalize-at-ingest action-correctness and orphan-disposition correctness -- with Sample/Label/Reasoning rows for each"
    - "evals/plurai/193-baseline.json records a precision/recall baseline (or a hand-labeled equivalent, method-flagged) the local gates in Plans 03/04 must match or beat"
    - "The phase does NOT block on Plurai availability: if /evals:eval cannot run, the rows are hand-labeled and the baseline is marked deferred, exactly as Phase 196 established"
    - "Every row is SYNTHETIC (fixture command slugs, fixture framework names) -- zero real Brain query content, zero real room content (this phase's own machinery is generic plugin plumbing, not user data, but the discipline is applied verbatim regardless)"
  artifacts:
    - path: "evals/plurai/09-canonicalize-orphan-disposition.csv"
      provides: "Synthetic Sample/Label/Reasoning fixture judging canonicalize-at-ingest action-correctness AND orphan-disposition correctness"
      contains: "kind"
    - path: "evals/plurai/193-baseline.json"
      provides: "Distilled precision/recall (or hand-labeled) baseline + method flag + row counts"
      contains: "precision"
  key_links:
    - from: "evals/plurai/193-baseline.json"
      to: "lib/core/methodology-ingest.cjs"
      via: "the CSV canonicalize rows are the parity fixtures canonicalizeAgainstPopulation's tests assert against"
      pattern: "09-canonicalize-orphan-disposition.csv"
    - from: "evals/plurai/193-baseline.json"
      to: "scripts/check-orphan-disposition.cjs"
      via: "the CSV orphan-disposition rows are the parity fixtures checkDispositions asserts against"
      pattern: "09-canonicalize-orphan-disposition.csv"
---

<objective>
Ship the Phase 193 Plurai eval GATE per CONTEXT.md / ROADMAP.md: "one judge for canonicalize-at-ingest +
orphan disposition correctness. No phase closes without a passing Plurai baseline." Reuse the Phase 196
plurai-baseline harness (the exact CSV schema, the fable judge model, the offline uv invocation, the
hand-label degrade path) rather than inventing a second eval mechanism.

Purpose: give Phase 193's two decision-making pieces (the canonicalize classifier from Plan 04 and the
orphan-disposition gate from Plan 03) an external judge baseline, following the same discipline as
Phase 196's Part 8 guardrail and Phase 200/201's baselines.
Output: evals/plurai/09-canonicalize-orphan-disposition.csv + evals/plurai/193-baseline.json + a README
row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-brain-substrate-hardening/193-CONTEXT.md
@evals/plurai/README.md
@evals/plurai/01-part8-boundary-guardrail.csv
@.planning/phases/196-part8-runtime-slm-guardrail/196-02-plurai-baseline-PLAN.md
@evals/plurai/196-baseline.json
</context>

<rules>
RULES (restate every wave, non-negotiable):
- Judge model: fable, per evals/plurai/README.md's navigator-set standing choice for every judge in this
  suite. Do not select a different model at the start_evaluator ask_user step.
- Every row is SYNTHETIC or fixture-based (D-04, Phase 196 precedent). No real command telemetry, no real
  Brain query payload, no real room content. Invented framework names and invented command slugs only.
- Plurai is BUILD/CI only, offline via uv, never on the runtime path. This plan writes no runtime code.
- Do NOT block the phase on Plurai availability. If /evals:eval cannot run, degrade to hand-labeling
  (deterministic from the CSV Label column) and mark the baseline method + baseline_deferred, exactly as
  evals/plurai/196-baseline.json already demonstrates.
- CJS/CSV/JSON only. NO em-dashes anywhere. Zero new npm packages.
- Resumable: this plan owns ONLY the three files in files_modified.
</rules>

<tasks>

<task type="auto">
  <name>Task 1: 09-canonicalize-orphan-disposition.csv + README row</name>
  <files>evals/plurai/09-canonicalize-orphan-disposition.csv, evals/plurai/README.md</files>
  <read_first>
    - evals/plurai/01-part8-boundary-guardrail.csv (header Sample,Label,Reasoning; Sample is a
      JSON-encoded string with doubled "" quote escaping -- the exact CSV mechanics to copy)
    - evals/plurai/README.md (the table row format + the "Judge model: fable" section + the Part 8
      synthetic-only rule -- add one new table row, do not restructure the file)
    - .planning/phases/193-brain-substrate-hardening/193-04-PLAN.md canonicalizeAgainstPopulation
      behavior block (the four classification outcomes: exact->alias, high-jaccard->alias,
      medium-jaccard->review, low/no-overlap->create)
    - .planning/phases/193-brain-substrate-hardening/193-03-PLAN.md checkDispositions behavior block
      (excluded-with-reason vs wired vs neither)
  </read_first>
  <action>
    Create evals/plurai/09-canonicalize-orphan-disposition.csv with header Sample,Label,Reasoning. Sample
    is a JSON-encoded object with a discriminator field "kind" set to either "canonicalize" or
    "orphan_disposition" (one judge, two sample kinds, per the ROADMAP wording "one judge for X + Y").
    For kind:"canonicalize", Sample carries { kind, new_name, top_candidate, jaccard, system_action } using
    INVENTED generic framework-style names (never real Brain framework names); Label is "correct" or
    "incorrect" depending on whether system_action is the right call given new_name/top_candidate/jaccard
    per the two-tier threshold (>=0.8 alias, [0.5,0.8) review, <0.5 create, exact-normalized-match alias).
    For kind:"orphan_disposition", Sample carries { kind, command, connector_excluded, connector_reason_present,
    connector_connects_to_spine, system_disposition } using INVENTED fixture command slugs (never the real
    5 command names, so the eval fixture never drifts if the real commands' frontmatter changes); Label is
    "correct" or "incorrect" depending on whether system_disposition (excluded|wired|neither) matches what
    the connector fields imply. Manufacture at least 20 rows total, roughly balanced between the two kinds
    and between correct/incorrect, including near-miss cases (a jaccard of exactly 0.5 or 0.8, a connector
    block with excluded:true but an EMPTY reason string) so the judge is stress-tested at the boundary.
    Every Reasoning cell states which rule fired (exact-match, jaccard-band, missing-reason, etc). No
    em-dashes in any cell.
    Add one new row to evals/plurai/README.md's table:
    | `09-canonicalize-orphan-disposition.csv` | Policy Compliance / Guardrail | Phase 193 (Brain Substrate
    Hardening, canonicalize-at-ingest + orphan disposition) | "Given a canonicalize-at-ingest decision or
    an orphan-disposition decision (the Sample's kind field says which), classify whether the system's
    action/disposition is correct or incorrect given the stated inputs. Label correct or incorrect." |
  </action>
  <verify>
    <automated>test $(tail -n +2 evals/plurai/09-canonicalize-orphan-disposition.csv | grep -c .) -ge 20 && grep -qi "canonicalize" evals/plurai/09-canonicalize-orphan-disposition.csv && grep -qi "orphan_disposition" evals/plurai/09-canonicalize-orphan-disposition.csv && grep -qi "correct" evals/plurai/09-canonicalize-orphan-disposition.csv && grep -qi "incorrect" evals/plurai/09-canonicalize-orphan-disposition.csv && echo OK</automated>
  </verify>
  <done>CSV has >= 20 synthetic rows across both sample kinds and both labels; README documents the new
  file per the existing table format.</done>
</task>

<task type="auto">
  <name>Task 2: Run Plurai eval offline, persist 193-baseline.json (degrade to hand-label)</name>
  <files>evals/plurai/193-baseline.json</files>
  <read_first>
    - .planning/phases/196-part8-runtime-slm-guardrail/196-02-plurai-baseline-PLAN.md Task 2 (the exact
      uv invocation, allowlist domains, degrade-to-hand-label path, and the baseline JSON shape)
    - evals/plurai/196-baseline.json (the FULL committed shape to mirror: schema_version, artifact_kind,
      runtime_loaded, method, baseline_deferred, deferred_note, source_csv, guardrail_prompt, canon_contract,
      synthetic_only, precision/recall/accuracy/f1, row_count, per-label counts, label_semantics,
      verdict_map, generated_at, generator)
  </read_first>
  <action>
    Attempt the Plurai eval OFFLINE against evals/plurai/09-canonicalize-orphan-disposition.csv using the
    installed plugin (uv run --directory ~/.claude/plugins/cache/plurai-plugins/evals/0.4.0
    python -m evals_mcp ...), judge model fable, prompt = the exact string added to evals/plurai/README.md
    in Task 1. If /evals:eval (or the underlying uv invocation) cannot run non-interactively -- the same
    limitation 196-02 hit (start_evaluator/ask_user/ScheduleWakeup is an interactive multi-turn flow) --
    degrade deterministically: label_semantics maps "correct" -> "correct" and "incorrect" -> "incorrect"
    (mirroring 196-baseline.json's compliant->allow / violation->block idiom, adapted to this CSV's own
    Label vocabulary), compute precision/recall/accuracy/f1 as 1/1/1/1 against the hand-labeled rows (the
    local gates ARE the ground truth these rows were authored against), set method:"hand-labeled",
    baseline_deferred:true, and write a deferred_note explaining why (verbatim reason from 196-02 if the
    same interactive-tool limitation applies, or the actual failure reason if different). Write
    evals/plurai/193-baseline.json with schema_version, artifact_kind:"ci-baseline", runtime_loaded,
    method, baseline_deferred, deferred_note (or omit if a real baseline landed), source_csv:
    "evals/plurai/09-canonicalize-orphan-disposition.csv", guardrail_prompt (the same prompt text added to
    README.md), canon_contract: "Part 8 (canonicalize-at-ingest generic-only) + Part 11 (orphan-disposition
    R1 wired-XOR-excluded)", synthetic_only:true, precision/recall/accuracy/f1, row_count,
    correct_count/incorrect_count, label_semantics, verdict_map (row index -> verdict), generated_at (ISO
    timestamp), generator (a one-line description of this plan's task).
  </action>
  <verify>
    <automated>node -e "const b=require('./evals/plurai/193-baseline.json'); if (typeof b.precision !== 'number' || typeof b.row_count !== 'number' || b.row_count < 20) process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>evals/plurai/193-baseline.json exists, parses, carries a numeric precision/recall/accuracy/f1 and
  a row_count matching the CSV, and is method-flagged honestly (hosted or hand-labeled).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local synthetic CSV -> Plurai hosted eval endpoint | Build/CI only; never sits on the runtime Brain-egress or ingest path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-193-14 | Information Disclosure | evals/plurai/09-canonicalize-orphan-disposition.csv | mitigate | Every Sample is fixture-authored (invented framework names, invented command slugs); no real Brain query payload, no real room content, no real command names ever enters the CSV, so nothing sent to the hosted Plurai endpoint can violate Part 8 even though this data is plugin-internal, not user data |
| T-193-15 | Denial of Service | Plurai hosted endpoint unavailable | mitigate | The task explicitly degrades to a deterministic hand-labeled baseline (method-flagged, deferred) rather than blocking the phase, mirroring the accepted Phase 196 precedent |
</threat_model>

<verification>
The CSV has >= 20 synthetic rows across both sample kinds; evals/plurai/193-baseline.json exists with a
numeric baseline (hosted or hand-labeled, honestly flagged). `bash tests/run-all-193.sh` (from Plan 01)
flips the Plurai-baseline-present leg from SKIPPED to PASSED.
</verification>

<success_criteria>
Phase 193 ships its required Plurai eval GATE: a judge covering both canonicalize-at-ingest and
orphan-disposition correctness, with a baseline on file, never blocking on external availability.
</success_criteria>

<output>
Create `.planning/phases/193-brain-substrate-hardening/193-05-SUMMARY.md` when done
</output>
