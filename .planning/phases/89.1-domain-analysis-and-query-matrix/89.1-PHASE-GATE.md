---
phase: 89.1
slug: domain-analysis-and-query-matrix
plan: "05"
doc_type: phase-gate-transcript
status: PASS
executed_at: "2026-04-26T13:56:22Z"
milestone: v1.11.0
bundled_release: true
canon_parts: [part-7, part-8]
---

# Phase 89.1 Phase Gate Transcript

Commands run verbatim. Mirror 89.1a Plan 04 G1-G9 pattern, extended with 4 new 89.1-specific E-asserts (E1 live A3, E2 canon edge, E3 ExternalEgressViolation surface map, E4 release artifacts untouched).

Base commit for diff: `1313777` (`docs(phase-89.1a): complete phase execution`, immediately before Phase 89.1 Plan 01 started).
HEAD at time of assert run: `3cfc58e` (`test(89.1-05): register rs-domain-analyzer + rs-query-matrix fixture suites in Feynman runner`).

## Gate Summary

- Total asserts: 13 (9 G + 4 E)
- Passed: 13
- Failed: 0
- Status: PASS

## Assert Transcript

### G1: Zero new runtime deps in package.json (across 89.1 commit range)

- Command: `git diff 1313777..HEAD -- package.json | grep -E '^\+.*"' | grep -v '"version"' | grep -v '^+++' | grep -v '^$' | wc -l`
- Expected: 0
- Observed: 0
- Status: PASS

The full `git diff 1313777..HEAD -- package.json` returned zero lines. Phase 89.1 added zero direct dependencies; the neo4j-driver consumed by `scripts/admin-brain-write.cjs` is loaded transitively via `mcp-server-brain/node_modules` per Plan 89.1-04 Pattern 2 (dual-transport admin-write).

### G2: BSL 1.1 headers on every new/modified .cjs

| File | Expected | Observed | Row Status |
|------|----------|----------|------------|
| lib/core/rs-brain-substrate.cjs | 1 | 1 | PASS |
| lib/core/rs-domain-analyzer.cjs | 1 | 1 | PASS |
| lib/core/rs-query-matrix.cjs | 1 | 1 | PASS |
| lib/memory/test-rs-domain-analyzer.cjs | 1 | 1 | PASS |
| lib/memory/test-rs-query-matrix.cjs | 1 | 1 | PASS |
| lib/memory/test-rs-brain-substrate-shape.cjs | 1 | 1 | PASS |
| lib/memory/validators/brain-substrate-invariants.cjs | 1 | 1 | PASS |
| scripts/admin-brain-write.cjs | 1 | 1 | PASS |

Command per file: `grep -c "BSL 1.1" <file>`. Every new production and test source file shipped in Phase 89.1 carries the mandatory BSL 1.1 header per CLAUDE.md non-negotiable. Modified files (rs-brain-substrate.cjs, brain-substrate-invariants.cjs) preserve their existing BSL 1.1 headers from Phase 89.1a.

- G2 Status: PASS

### G3: Zero em-dashes on every new/modified file

| File | Expected | Observed | Row Status |
|------|----------|----------|------------|
| lib/core/rs-domain-analyzer.cjs | 0 | 0 | PASS |
| lib/core/rs-query-matrix.cjs | 0 | 0 | PASS |
| lib/core/rs-brain-substrate.cjs | 0 | 0 | PASS |
| lib/memory/test-rs-domain-analyzer.cjs | 0 | 0 | PASS |
| lib/memory/test-rs-query-matrix.cjs | 0 | 0 | PASS |
| lib/memory/test-rs-brain-substrate-shape.cjs | 0 | 0 | PASS |
| lib/memory/validators/brain-substrate-invariants.cjs | 0 | 0 | PASS |
| scripts/admin-brain-write.cjs | 0 | 0 | PASS |
| commands/admin.md | 0 | 0 | PASS |
| lib/memory/run-feynman-tests.cjs | 0 | 0 | PASS |

Command per file: `grep -cP '[\x{2013}\x{2014}]' <file>`. Project hard rule (feedback_no_emdashes) upheld across every file shipped or modified in Phase 89.1.

- G3 Status: PASS

### G4: Canon Part 8 audit (forbidden field references) on production .cjs

| File | Expected | Observed | Row Status |
|------|----------|----------|------------|
| lib/core/rs-domain-analyzer.cjs | 0 | 0 | PASS |
| lib/core/rs-query-matrix.cjs | 0 | 0 | PASS |
| lib/core/rs-brain-substrate.cjs | 0 | 0 | PASS |
| lib/memory/validators/brain-substrate-invariants.cjs | 0 | 0 | PASS |
| scripts/admin-brain-write.cjs | 0 | 0 | PASS |
| lib/core/rs-brain-substrate-prompts.cjs | 0 | 0 | PASS |

Command per file: `grep -cE "governing_thought[^_]|identity_text|decision_log|meeting_transcript|artifact_body" <file>`. Canon Part 8 forbidden-field boundary held across all six production-surface files in Phase 89.1. Test fixture suites are deliberately permitted to reference forbidden names in adversarial assertion context (fixtures verify the boundary holds under hostile input) and are excluded from G4 per VALIDATION.md scope note inherited from Phase 89.1a.

- G4 Status: PASS

### G5: Chokepoint exclusivity preserved (89.1a primitives still load-bearing)

- Command: `grep -c "buildBrainSubstrateQuery" lib/core/rs-brain-substrate.cjs`
- Expected: >= 6 (matches 89.1a Plan 04 G5 baseline)
- Observed: 6
- Status: PASS

Cross-check: `grep -c "preSendAudit" lib/core/rs-brain-substrate.cjs` returned 6 (>= 5 required). Chokepoint preserved across Plan 89.1-01 adapter changes. Plan 89.1-01 added the post-network shape adapter (`adaptBrainSearchResponse`) but did NOT modify the pre-network chokepoint: every adapted entry STILL flows through buildBrainSubstrateQuery + preSendAudit before any brainClient.* call. The adapter runs strictly on the response shape, never on the outbound query.

### G6: New Feynman suites registered

- Command: `grep -c "test-rs-domain-analyzer" lib/memory/run-feynman-tests.cjs`
- Expected: 1
- Observed: 1
- Cross-check 1: `grep -c "test-rs-query-matrix" lib/memory/run-feynman-tests.cjs` returned 1 (expected 1).
- Cross-check 2: `grep -c "test-rs-brain-substrate-shape" lib/memory/run-feynman-tests.cjs` returned 1 (expected 1, registered by Plan 89.1-01).
- Status: PASS

Single-line appends per Plan 89.1-05 Task 1 design decision (no reordering, no existing TEST_FILES entry modified). Both suites land as the 65th and 66th entries, advancing Feynman baseline from 64 to 66 in one commit (`3cfc58e`).

### G7: Each new fixture suite green (direct invocation)

- `node lib/memory/test-rs-brain-substrate-shape.cjs; echo "exit: $?"` -> exit 0; final line `=== 89.1-01 shape adapter suite: 14/14 passed ===`
- `node lib/memory/test-rs-domain-analyzer.cjs; echo "exit: $?"` -> exit 0; final line `=== 89.1-02 domain-analyzer suite: 11/11 passed ===`
- `node lib/memory/test-rs-query-matrix.cjs; echo "exit: $?"` -> exit 0; final line `=== 89.1-03 query-matrix suite: 11/11 passed ===`
- 89.1a back-compat: `node lib/memory/test-rs-brain-substrate.cjs; echo "exit: $?"` -> exit 0; 14/14 passed (plan 89.1a-03 fixture suite unchanged by 89.1 work)
- Status: PASS

All four fixture suites exit 0 with their respective scenario counts intact. Plan 89.1-01 added 5 validator scenarios (T10-T14) on top of the 9 shape-adapter scenarios (T1-T9), totalling 14. Plans 89.1-02 and 89.1-03 each shipped 11-scenario suites + A1 cross-scenario sweep. 89.1a Plan 03's 14-scenario substrate fixture suite remains green (back-compat preserved).

### G8: Full Feynman runner green

- Command: `node lib/memory/run-feynman-tests.cjs > /tmp/89.1-feynman-run.log 2>&1; echo "exit=$?"`
- Expected: exit 0; baseline advanced to 66
- Observed: exit 0; final line `Feynman test runner: 66/66 passed, 0 skipped, 0 failed`; baseline advanced 64 -> 66
- Status: PASS

The runner reported 66/66 PASS, zero skips, zero failures, exit 0. The previously-documented debouncer wall-clock flake (89.1a-04 G8 footnote) was load-dependent and did not surface in this run on the canonical workspace at the time of assert. All four Phase 89.1 fixture suites (shape, domain-analyzer, query-matrix, plus the inherited 89.1a substrate suite) reported PASS.

### G9: ExternalEgressViolation surfaces only in domain-analyzer + query-matrix (not in 89.1a substrate loader)

- Command: `grep -c "ExternalEgressViolation" lib/core/rs-domain-analyzer.cjs`
- Expected: >= 1 (89.1-02 introduces this throwable)
- Observed: 11
- Cross-check 1: `grep -c "ExternalEgressViolation" lib/core/rs-query-matrix.cjs` returned 11 (>= 1 required; 89.1-03 introduces this throwable as a sibling class).
- Cross-check 2: `grep -c "ExternalEgressViolation" lib/core/rs-brain-substrate.cjs` returned 0 (== 0 required; 89.1a chokepoint stays Brain-inbound-only, never external-egress).
- Status: PASS

ExternalEgressViolation is a sibling of BrainBoundaryViolation (Plan 89.1-02 design decision, mirrored in Plan 89.1-03). The two surfaces (Brain inbound + external public-fetcher outbound) are semantically distinct, so each domain-egress module defines its own subclass. Brain substrate loader never throws ExternalEgressViolation because its only network surface is the Brain (Canon Part 8 -- never user-data-out, only methodology-in).

Bonus verification (G9 prompts module check): `grep -n "brain-client" lib/core/rs-brain-substrate-prompts.cjs` returned zero lines, preserving the 89.1a-04 G9 invariant (allow-list module stays decoupled from transport module so the allow-list cannot bypass its own chokepoint from the vocabulary side).

## New 89.1-Specific Asserts (E1-E4)

### E1: Live Mode A3 reachable post-canonization (re-verifies Plan 01 evidence post-canon-write)

- Command: `node -e "(async () => { const sub = require('./lib/core/rs-brain-substrate.cjs'); const r = await sub.loadSubstrate({ roomDir: process.env.SMOKE_ROOM, forceRefresh: true, limit: 25 }); console.log(JSON.stringify({ mode: r.mode, count: r.substrate?.length, has_score: r.substrate?.[0]?.score !== undefined }, null, 2)); })()"`
- Expected: `{"mode": "A3", "count": > 0, "has_score": true}`
- Observed:

```json
{
  "success": true,
  "mode": "A3",
  "substrate_length": 25,
  "cache_age_days": 0,
  "sample_id": "mindrian-knowledge_t2-trending-absurd_2cddc6ac2cf58ea0",
  "sample_score": 0.8089869022369385,
  "sample_metadata_keys": [
    "_source_index", "_source_namespace", "chunk_index", "content",
    "content_hash", "content_type", "framework_name", "source_file",
    "text", "tier", "title", "total_chunks"
  ]
}
```

- Status: PASS

Live Brain reachable from this session. Mode A3 confirmed (substrate-bearing live wire path). Cache landed at `$SMOKE_ROOM/.mindrian/brain-substrate-cache.json` with `embedding_count=25 substrate_length=25 schema_version=1.0 completeness=score-only` (Plan 89.1-01 score-only branch correctly populated). Zero orphan tmpfiles (atomic-write Pattern from 89.1a Plan 02 holds). Audit log final line carried `outcome=pass handles=["query","namespace","topK"]` (Canon Part 8 audit clean, no user content). Full evidence in `89.1-LIVE-A3-VERIFY.md`.

### E2: Canon edge live-read (USES_TECHNIQUE persists from Plan 89.1-04)

- Command: `node -e "(async () => { const bc = require('./lib/core/brain-client.cjs'); const r = await bc.query('MATCH (p:ProcessStep {id:\"rss-phase-1\"})-[r:USES_TECHNIQUE]->(t:Technique {id:\"tech-domain-analysis\"}) RETURN count(r) AS edges'); console.log(JSON.stringify(r, null, 2)); })()"`
- Expected: `edges == 1` (the edge filed by Plan 89.1-04 is live)
- Observed:

```json
[
  {
    "edges": {
      "low": 1,
      "high": 0
    }
  }
]
```

- Status: PASS

The canon edge filed by Plan 89.1-04 (Option A canonize-then-edge dual-MERGE: ProcessStep `rss-phase-1` + USES_TECHNIQUE -> Technique `tech-domain-analysis`) persists in live Brain Aura. The Neo4j Integer wire shape `{low:1, high:0}` decodes to `1` (count of edges). Both the source ProcessStep and the dependent USES_TECHNIQUE edge are reachable via brain-client.query. Phase 89.2 + 89.3 + 89.5 downstream methodology walks now have a Brain-resident anchor.

### E3: ExternalEgressViolation refs across analyzer + matrix

- `grep -c "ExternalEgressViolation" lib/core/rs-domain-analyzer.cjs` returned 11 (>= 1 required).
- `grep -c "ExternalEgressViolation" lib/core/rs-query-matrix.cjs` returned 11 (>= 1 required).
- Cross-check: `grep -c "ExternalEgressViolation" lib/core/rs-brain-substrate.cjs` returned 0 (== 0 required; 89.1a stays Brain-inbound-only).
- Status: PASS

Each module defines its own ExternalEgressViolation subclass. Stack traces stay legible because surface-specific throws never conflate Brain-inbound boundary breaches with external-outbound boundary breaches.

### E4: No release artifacts touched in 89.1

- Command: `git diff 1313777..HEAD -- CHANGELOG.md .claude-plugin/plugin.json package.json | wc -l`
- Expected: 0
- Observed: 0
- Status: PASS

`bundled_release: true` honored. v1.11.0 ships at Phase 91.x per milestone section 7. CHANGELOG.md, plugin.json, and package.json are byte-identical at HEAD vs base across the entire Phase 89.1 commit range.

## Invariant Coverage

### 89.1a Invariants (I1-I8) preserved by Plan 89.1-01 adapter

| Invariant | Enforced by Plan | Verified by | Evidence |
|-----------|------------------|-------------|----------|
| I1 (chokepoint Canon Part 8 enforcement: validateCtx + buildBrainSubstrateQuery + preSendAudit) | 89.1-01 (shape adapter preserves chokepoint exclusivity before adapt) | Plan 89.1-01 Test 7 (Canon Part 8 audit chokepoint preservation) | G7 exit 0; G5 grep returns 6 |
| I2 (validator Check E embedding-or-score relaxation) | 89.1-01 (validator Check E branch hasEmbedding || hasScore) | Plan 89.1-01 Tests 11-13 | G7 exit 0 |
| I3 (atomic write 7-step protocol; openSync 'wx' + fsync + rename) | 89.1a Plan 02 (unchanged) | Plan 89.1a-03 A2 sweep | G7 89.1a back-compat exit 0; E1 live SMOKE_ROOM observed 0 orphan tmpfiles |
| I4 (loadSubstrate top-level try/catch never throws) | 89.1-01 (try/catch unchanged; adapter throw caught by pullFromBrain wrapper) | Plan 89.1-01 Test 9 (graceful degradation on null response) | G7 exit 0 |
| I5 (RESULT_MODES frozen enum A1/A2/A3/B1/B2/B3) | 89.1-01 (enum unchanged; A3 reachable post-fix) | Plan 89.1-01 Test 6 (Mode A3 cache landing) + E1 live A3 | G7 + E1 PASS |
| I6 (TTL math + isCacheFresh; MINDRIAN_BRAIN_SUBSTRATE_TTL_DAYS env override bounded [1,3650]) | 89.1a Plan 02 (TTL math unchanged) | Plan 89.1a-03 Scenario 11 | G7 89.1a back-compat exit 0 |
| I7 (chokepoint single source of truth via grep) | 89.1-01 (chokepoint count unchanged via G5) | G5 grep | G5 PASS (6 references) |
| I8 (validator {id, severity_map, scope, validate} contract; Phase 88-13 guardian auto-discovery) | 89.1-01 (validator contract unchanged across Check E relaxation) | Plan 89.1-01 Tests 11-14 | G7 exit 0 |

### New 89.1 Invariants (E1-E4)

| New Invariant | Enforced by Plan | Verified by | Evidence |
|----------------|------------------|-------------|----------|
| E1 (analyzer output FORBIDDEN_PATTERNS audit -- pre-input + per-field + composite) | 89.1-02 (validateTopic + scrubScalar + auditOutput tripwires) | Plan 89.1-02 Tests 8-11 + A1 cross-scenario sweep | G7 exit 0; A1 sweep zero forbidden hits |
| E2 (query matrix per-string FORBIDDEN_PATTERNS audit -- pre-input + per-template) | 89.1-03 (validateAnalysis + auditQuery tripwires) | Plan 89.1-03 Tests 8-11 + A1 sweep | G7 exit 0; A1 sweep zero forbidden hits |
| E3 (60-query 4-category invariant: 4 x 15 fixed-order template determinism) | 89.1-03 (QUERIES_PER_CATEGORY guard + fixed-order template arrays) | Plan 89.1-03 Test 1 (60-query shape) + Tests 6-7 (determinism) | G7 exit 0 |
| E4 (analyzer + matrix determinism -- byte-identical JSON.stringify across calls) | 89.1-02 + 89.1-03 (no random shuffle, no hash-seeded selection) | Plan 89.1-02 Test 7 + Plan 89.1-03 Tests 6-7 | G7 exit 0 |

All eight 89.1a invariants and all four new 89.1 invariants have direct evidence pointers.

## Live Brain Wire Evidence

Two live probes against MINDRIAN_BRAIN_KEY-authenticated Brain Aura ran during this gate:

1. **E1 substrate probe** -- `loadSubstrate({forceRefresh:true, limit:25})` returned Mode A3 with substrate_length=25, sample score 0.808986 in [0,1], framework_name in metadata keys, audit log clean (handles only). Cache landed at `_completeness=score-only` per Plan 89.1-01 contract.
2. **E2 canon edge probe** -- `MATCH (p:ProcessStep {id:"rss-phase-1"})-[r:USES_TECHNIQUE]->(t:Technique {id:"tech-domain-analysis"}) RETURN count(r)` returned 1 (Neo4j Integer `{low:1, high:0}`). The Plan 89.1-04 Option A dual-MERGE (canonize-then-edge) persists.

Full evidence: `89.1-LIVE-A3-VERIFY.md`.

## Gate Signature

Executed by: Claude (autonomous plan execution; Task 3 inline because live Brain reachable from this session)
Executed at: 2026-04-26T13:56:22Z
Base commit: 1313777 (pre-89.1 milestone baseline; immediately after 89.1a phase-completion)
HEAD commit: 3cfc58e (post-Plan-89.1-05 Task 1: Feynman registration of two new fixture suites)
Gate status: PASS

All 9 G asserts and all 4 E asserts observed the expected values. All 8 89.1a invariants (I1-I8) plus 4 new 89.1 invariants (E1-E4) have evidence. Zero new runtime deps. Zero em-dashes. Zero forbidden-field references in production. Chokepoint exclusivity preserved (buildBrainSubstrateQuery=6, preSendAudit=6). Two new Feynman suites registered (baseline 64 -> 66; 66/66 PASS, 0 failed). Live A3 + canon edge re-verified against MINDRIAN_BRAIN_KEY-authenticated Brain. Live A3 verification at `.planning/phases/89.1-domain-analysis-and-query-matrix/89.1-LIVE-A3-VERIFY.md` (extended).

No release artifacts changed in this plan (bundled_release: true; v1.11.0 ships at Phase 91.x per milestone section 7). CHANGELOG.md, plugin.json, and package.json are untouched in the 89.1 commit range.
