---
phase: 262-floor-green-sweep-02-inversion
plan: 04
type: execute
wave: 3
depends_on: [262-01, 262-02]
files_modified:
  - docs/262-LIVE-MEASUREMENT-EVIDENCE.md
autonomous: false
requirements: [FLOOR-01, FLOOR-03]
tags: [floor-gate, live-measurement, floor-03, human-gated, dated-evidence]

must_haves:
  truths:
    - "A dated, window-fresh floor verdict exists with zero VOID rows, measured against the incumbent Brain at plan-execution time, not carried forward from research"
    - "The Scenario Planning canonical-match count is re-measured live rather than assumed, per FLOOR-03's own requirement text"
    - "The tripwire added in 262-02 is proven not to misfire against the incumbent Brain (0 VOID rows on a real run)"
    - "The Brain admin write surface is re-confirmed closed at measurement time, so the gap ledger's write-dependency column rests on a measurement"
    - "Every number recorded carries the command that produced it and the date it was measured"
  artifacts:
    - path: "docs/262-LIVE-MEASUREMENT-EVIDENCE.md"
      provides: "The dated raw-evidence record every downstream ruling and work order cites: floor verdict, per-row table, FLOOR-03 probe, alias topology, SEP counts, tool-surface check, Theo health probe"
      contains: "measured against the incumbent Brain on"
      min_lines: 60
  key_links:
    - from: "docs/262-LIVE-MEASUREMENT-EVIDENCE.md"
      to: "scripts/check-flagship-floor.cjs"
      via: "the verbatim command and its exit code"
      pattern: "check-flagship-floor.cjs"
    - from: "docs/262-LIVE-MEASUREMENT-EVIDENCE.md"
      to: "docs/262-FLOOR-01-GAP-LEDGER.md"
      via: "the evidence Plan 262-05 cites row by row"
      pattern: "GAP-LEDGER"
---

<objective>
Produce the phase's ground truth: one dated, trustworthy, window-fresh set of live
measurements against the incumbent Brain, and nothing else. This plan measures. It rules on
nothing and remediates nothing.

Purpose: 262-RESEARCH.md's Pitfall 2 is that FLOOR-03 has already been wrong twice by
carrying a number forward, and the research's own header says to re-run the gate at plan
lock rather than citing its numbers. D-04's dating rule says every floor number this phase
writes must carry "measured against the incumbent Brain on <date>" so a post-flip reader
never compares across populations. This plan is where those two rules are paid for.

It also discharges the one thing 262-02 could not prove hermetically: that the new
`unrecognized_shape` tripwire does not misfire against the incumbent Brain's real payloads.
A live run with 0 VOID rows is that proof.

Hard boundaries:
- READ-tier key only. The gate's own header says "This gate needs a READ-tier key... No
  admin key is used." Phase 261 spent a whole plan establishing the admin-window
  discipline; this phase opens no window and reaches for no admin key.
- `brain_query` only for graph inspection, never `text2cypher` (a known open exposure per
  the 2026-08-10 Brain service audit).
- Canon Part 8: the only thing that crosses the wire is a generic framework-name handle. No
  room content, no file paths, no user text, ever.
- D-08: a VOID run requires a HUMAN re-run. Nothing auto-retries.
- No graph writes from this repo (Pitfall 6). Remediation is Plan 262-05's written work
  order, filed into the Brain repo's own intake.

Output: one tracked evidence document. Zero code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-floor-green-sweep-02-inversion/262-CONTEXT.md
@.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md
@scripts/check-flagship-floor.cjs
@CLAUDE.md
</context>

## Artifacts this phase produces (Plan 04 share)

| Artifact | Kind | Notes |
|---|---|---|
| `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` | new tracked file | The dated raw-evidence record. Tracked (not under `.planning/`) because Plan 262-05's cross-repo work orders cite it and `.planning/` is gitignored, so it would not travel to the Brain repo or to another machine |

No new code symbols. No package installs.

<tasks>

<task type="auto">
  <name>Task 1: Take the live measurements and write the dated evidence record</name>

  <files>docs/262-LIVE-MEASUREMENT-EVIDENCE.md</files>

  <read_first>
    - `scripts/check-flagship-floor.cjs` in full (the exit-code contract: 0 green, 1 real MISS, 2 malformed override, 3 VOID; and the three distinct banners, so a VOID is never read as a RED)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md` sections "The Live Floor Run", "The eight MISS rows, with per-dimension root cause", "Root Cause 1: the SEP regression", "Root Cause 2: FLOOR-03, the Scenario Planning hop-depth-1 defect", "Code Examples", "Common Pitfalls" (Pitfalls 1, 2 and 7), and "Environment Availability"
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-CONTEXT.md` decisions D-01, D-03, D-04, D-07
    - `scripts/build-brain-census.cjs` lines 252-265 and 396-402 (the `brainCall` contract and the exported surface the probes reuse)
    - `lib/core/resolve-brain-key.cjs` (the single key chokepoint; `resolveBrainKey()` returns `{ available, key, reason }`)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-02-SUMMARY.md` (the tripwire's exact `failures[]` shape, so any VOID row in this run can be attributed to the right kind)
  </read_first>

  <action>
    Run from `/home/jsagi/dev/MindrianOS-Plugin/` (WORKSPACE GUARD: confirm with `pwd`
    first; never the `~/.claude/plugins/` install cache). Capture every command's verbatim
    stdout. Take the measurements in this order.

    Measurement 0, the staleness gate.
    `curl -s -o /dev/null -w '%{http_code}' https://theo-mcp.onrender.com/health`.
    262-RESEARCH.md's stated validity condition is "2026-09-09, OR the moment this returns
    200, whichever comes first." If it returns 200, record the code and raise it as an
    explicit item at the Task 2 checkpoint. Do NOT abandon the run: D-03 is explicit that
    this phase plans and remediates against the CURRENT Brain.

    Measurement 1, the floor verdict.
    `node scripts/check-flagship-floor.cjs; echo "EXIT=$?"`. Capture the full output: the
    `Brain URL:` line, the `Floor denominator: RATIFIED at N framework(s)` line,
    `Enumerated frameworks this run:`, every per-row
    `[PASS|MISS|VOID] <name> -- uses=N matches=N score=N/4` line, any VOID detail lines, the
    summary lines, and the exit code.

      - If `Enumerated frameworks this run:` is anything other than 28, STOP. That is
        Pitfall 3 firing and Plan 262-01's denominator test should already have caught it.
      - If the run reports any VOID row, do NOT re-run it yourself. D-08 forbids auto-retry.
        Record the VOID output verbatim, note that no floor verdict exists for this run, and
        carry it to the Task 2 checkpoint for a human re-run.
      - Never read a pass/miss figure from a run with `voidCount > 0` (Pitfall 1). Check the
        VOID line first.

    Measurement 2, the FLOOR-03 re-measurement. Use the exact reuse pattern from
    262-RESEARCH.md's "Code Examples": a `node -e` invocation requiring `brainCall` from
    `./scripts/build-brain-census.cjs` and `resolveBrainKey` from
    `./lib/core/resolve-brain-key.cjs`. Do not mint a second HTTP client (Canon Part 7).
    Probe:
      - `normalize_framework_name` with `{ raw: 'Scenario Planning' }`; print
        `result.canonical_matches` in full (the array itself, not just its length).
      - `orchestration_readiness` with `{ framework_name: 'Scenario Planning' }`; print
        `result.readiness` in full.
      - `normalize_framework_name` with `{ raw: 'Scenario planning methodology' }`. This is
        the independent confirmation of the hop-depth-1 mechanism: an alias node returning
        its own name alongside its canonical target.

    Measurement 3, the alias topology behind FLOOR-03. One `brain_query` read confirming the
    chain 262-RESEARCH.md traced: node 18880 "Scenario Planning Methodology" ALIAS_OF 23450
    "Scenario planning methodology" ALIAS_OF 34362 "Shell Scenario Planning Method". Record
    whether the chain still holds and record every node id observed. Use `brain_query`,
    never `text2cypher`.

    Measurement 4, the SEP state behind FLOOR-01 rows 1 and 2. Three `brain_query` reads:
    total `:Framework` count; count of `:Framework` whose `name` contains the separator
    token; the same count restricted to `id(f) >= 28000 AND id(f) <= 29000`. Then confirm
    the two phantom node ids 28757 (HSI Semantic Surprise Analysis Assistant) and 28775
    (PWS Triple Validation Compass) still exist and still carry a corrupted name. Record the
    ids and the label sets.

    Measurement 5, the per-dimension refresh. For every framework the run reported MISS,
    probe `orchestration_readiness` and record the four dimension values
    (`pattern_type`, `structure`, `techniques`, `flow`) plus `readiness_score` and
    `orchestration_status`. This refreshes 262-RESEARCH.md's eight-row root-cause table
    against today's graph rather than citing it.

    Measurement 6, the write-seam check. Call `tools/list` on the Brain and record the tool
    count and, explicitly, whether `brain_write` and `ingest_framework` are PRESENT or
    ABSENT. The gap ledger's entire write-dependency column rests on this being a
    measurement rather than an assumption.

    Rate-limit discipline (Pitfall 7): the gate alone makes 56 HTTPS calls and cold-start
    429s on the Render instance are real. If a TARGETED probe returns 429, wait for the
    `Retry-After` interval and retry that single probe by hand. Never re-run the full gate
    to work around a 429 on a targeted probe, and never auto-retry the gate itself.

    Then write `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` with these sections, in this order:

      1. Header: what this file is (raw dated evidence, no rulings), the exact date, the
         Brain URL measured, the key tier used (read), and the sentence
         "All numbers in this file were measured against the incumbent Brain on <date>"
         verbatim, per D-04's dating rule.
      2. Measurement 0: the Theo `/health` status code and what it implies for the freshness
         of 262-RESEARCH.md.
      3. Measurement 1: the floor verdict. The command, the full verbatim output in a fenced
         block, the exit code, and the three headline counts (PASS / MISS / VOID) restated
         as one line.
      4. The per-row table: framework, matches, score, verdict. One row per enumerated
         framework.
      5. Measurements 2 and 3: the FLOOR-03 probes and the alias topology, each with its
         command and its verbatim result.
      6. Measurement 4: the SEP counts and the two phantom node ids.
      7. Measurement 5: the refreshed per-dimension table for the MISS rows.
      8. Measurement 6: the tool-surface check.
      9. A "Deltas against 262-RESEARCH.md (2026-09-02)" section listing every number that
         MOVED since research, or the explicit sentence "no deltas" if none did. This is
         what makes the file worth writing rather than a transcript.
      10. A closing line stating that this file contains measurements only, that no ruling
          is made here, and pointing at `docs/262-FLOOR-01-GAP-LEDGER.md` (Plan 262-05) as
          where the attribution and routing live.

    Never paste a key value, a key file's contents, or an `Authorization` header into this
    document. Naming the file path `~/.mindrian.env` is fine; printing anything from it is
    not (ASVS V2, and `refusal-messaging.cjs`'s own V5 rule).

    Hyphens only. No em-dashes: `tests/run-all-262.sh`'s fence sweeps this path.
  </action>

  <verify>
    <automated>test -f docs/262-LIVE-MEASUREMENT-EVIDENCE.md &amp;&amp; grep -q "measured against the incumbent Brain on" docs/262-LIVE-MEASUREMENT-EVIDENCE.md &amp;&amp; grep -qE "brain_write.*(PRESENT|ABSENT)" docs/262-LIVE-MEASUREMENT-EVIDENCE.md &amp;&amp; bash tests/run-all-262.sh</automated>
  </verify>

  <acceptance_criteria>
    - `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` exists and is at least 60 lines.
    - It contains the literal phrase `measured against the incumbent Brain on` followed by an ISO date matching today (`date -I`).
    - It records `Enumerated frameworks this run: 28`. Any other value is a hard stop, not a note.
    - It records the floor gate's exit code explicitly, and the PASS / MISS / VOID counts as three separate numbers.
    - It records `brain_write` and `ingest_framework` each as `PRESENT` or `ABSENT` against a `tools/list` call, with the total tool count.
    - It records the live `normalize_framework_name('Scenario Planning')` array verbatim, not just its length.
    - It contains a "Deltas against 262-RESEARCH.md" section that is either a list of moved numbers or the literal sentence `no deltas`.
    - `grep -c "text2cypher" docs/262-LIVE-MEASUREMENT-EVIDENCE.md` returns 0 (only `brain_query` was used).
    - `grep -Ec "Bearer |MINDRIAN_BRAIN_KEY=" docs/262-LIVE-MEASUREMENT-EVIDENCE.md` returns 0 (no key material, no auth header).
    - `git diff --name-only` lists no file under `lib/`, `bin/`, `scripts/`, `tests/` or `data/` (this plan measures and writes one doc; it changes nothing else).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' docs/262-LIVE-MEASUREMENT-EVIDENCE.md` returns 0, and `bash tests/run-all-262.sh` exits 0 with its fence leg PASSED.
  </acceptance_criteria>

  <done>Every number Plan 262-05 will cite exists on disk with the command that produced it and the date it was taken.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator sign-off on the floor evidence (D-01, D-08)</name>

  <files>docs/262-LIVE-MEASUREMENT-EVIDENCE.md (read only at this checkpoint; no file is modified here)</files>

  <read_first>
    - `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` (Task 1's output, in full)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-CONTEXT.md` decisions D-01, D-03, D-05, D-08 reference in 262-RESEARCH.md's anti-patterns list
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-02-SUMMARY.md` (the `unrecognized_shape` failure shape, needed to attribute any VOID row correctly)
  </read_first>

  <action>
    Do not run the gate again before presenting. Present Task 1's evidence to the navigator
    and hold. Summarize, in the message, exactly six things and nothing more: the VOID count,
    the PASS / MISS split, the enumerated framework count, the live Scenario Planning
    canonical-match array, the `brain_write` / `ingest_framework` PRESENT-or-ABSENT result,
    and the Theo `/health` status code. Then pose the two confirmations named in
    `how-to-verify` below and wait for the resume signal.

    If the navigator reports that the run VOIDed and re-runs it, transcribe the fresh output
    into `docs/262-LIVE-MEASUREMENT-EVIDENCE.md`, append it as a dated re-run section rather
    than overwriting the VOID record, and attribute the failure kind from the VOID detail
    lines. Never auto-retry the gate yourself (D-08); the re-run is the human's action and
    the record must show it as such.

    If the navigator supplies a correction rather than an approval, record the correction
    verbatim in `262-04-SUMMARY.md` and re-run only the measurements the correction names.
  </action>

  <what-built>
    Claude ran the full live measurement set from the dev workspace against
    `https://pws-brain-mcp.onrender.com` on a READ-tier key and wrote
    `docs/262-LIVE-MEASUREMENT-EVIDENCE.md`: the floor gate verdict with its exit code and
    per-row table, the FLOOR-03 re-measurement plus the alias-topology confirmation, the SEP
    counts and the two phantom node ids, the refreshed per-dimension table for every MISS
    row, the `tools/list` write-seam check, the Theo `/health` staleness probe, and a deltas
    section against 262-RESEARCH.md's 2026-09-02 numbers.

    No graph writes were attempted. No admin key was resolved. Nothing was auto-retried.
  </what-built>

  <how-to-verify>
    1. Read `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` top to bottom.
    2. Confirm `VOID = 0`. This is the load-bearing one and it does double duty: it makes
       the run a trustworthy floor verdict under TRUST-02, AND it proves Plan 262-02's new
       `unrecognized_shape` tripwire did not misfire against the incumbent Brain's real
       payloads. If VOID is greater than 0, re-run the gate yourself
       (`node scripts/check-flagship-floor.cjs`) and check the VOID detail lines: kinds
       `hard-error` / `timeout` / `malformed` mean a transport blip (re-run again);
       kind `unrecognized-shape` means the tripwire misfired and Plan 262-02 must be revised
       before the phase proceeds.
    3. Confirm `Enumerated frameworks this run: 28`. Anything else means the ratified
       denominator drifted or was narrowed, which is the rejected option.
    4. Confirm the deltas section. If the PASS count moved off 20/28 since research, or if
       the Scenario Planning match count moved off 2, the downstream rulings in Plan 262-05
       must be written against these numbers, not against research's.
    5. Read the Theo `/health` line. If it returned 200, Theo is now serving and
       262-RESEARCH.md's own validity condition has expired. That does not change D-03 (this
       phase still targets the CURRENT Brain) but it does change urgency, and it is worth
       your explicit call before Plan 262-05 writes the work orders.
    6. Confirm no key value, key-file content or `Authorization` header appears anywhere in
       the document.

    Two confirmations this checkpoint asks for. Both are already ruled in 262-CONTEXT.md and
    are being re-confirmed against fresh numbers, not re-opened:
      - D-01: FLOOR-01 closes this milestone as "measured, attributed and routed" against
        this verdict, with a signed gap ledger. The green run itself moves to whichever
        phase reopens the Brain-repo write seam. The 28-name denominator is NOT narrowed.
      - D-05 / FLOOR-03: exactly-1 remains the correct assertion and the measured 2 is a
        resolver defect, not a legitimate multi-canonical result.
  </how-to-verify>

  <resume-signal>
    Type "approved" to proceed to Plan 262-05 (gap ledger and cross-repo work orders), or
    describe what needs re-measuring. If the run VOIDed, re-run the gate and paste the fresh
    output before approving.
  </resume-signal>

  <verify>
    <human-check>Navigator has read the evidence document, confirmed VOID = 0 and 28 enumerated frameworks, ruled on the Theo /health result, and re-confirmed D-01 and D-05 against the fresh numbers.</human-check>
    <automated>grep -qE "^\|?[^|]*VOID" docs/262-LIVE-MEASUREMENT-EVIDENCE.md; grep -q "Enumerated frameworks this run: 28" docs/262-LIVE-MEASUREMENT-EVIDENCE.md</automated>
  </verify>

  <acceptance_criteria>
    - An explicit approval (or a described correction) is recorded in `262-04-SUMMARY.md`, quoting the confirmed VOID count and PASS/MISS split.
    - If the Theo `/health` probe returned 200, the SUMMARY records the navigator's explicit call on whether that changes this phase's targeting (default per D-03: it does not).
    - If the run VOIDed, the evidence document carries BOTH the original VOID record and a dated human re-run section, and the SUMMARY names the attributed failure kind. No auto-retry appears anywhere in the transcript.
    - `grep -q "Enumerated frameworks this run: 28" docs/262-LIVE-MEASUREMENT-EVIDENCE.md` succeeds.
  </acceptance_criteria>

  <done>The phase has a signed, dated floor verdict, and the tripwire is proven non-misfiring against the live incumbent Brain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workspace to Brain HTTPS `/mcp` | Outbound: a generic framework-name handle plus a read-only Cypher string this repo authored. Inbound: untrusted response text printed to a terminal and pasted into a tracked doc |
| `~/.mindrian.env` to the probe process | A READ-tier bearer key is resolved in-process and must never reach stdout, a file, or a commit |
| operator terminal to `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` | Brain-returned text becomes a tracked, cross-repo-cited document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-19 | Information disclosure | A READ-tier key value pasted into the evidence doc or echoed in a transcript | mitigate | Key is resolved only through `lib/core/resolve-brain-key.cjs`; the action forbids printing key values or `Authorization` headers; acceptance greps for `Bearer ` and `MINDRIAN_BRAIN_KEY=` and requires 0 hits (ASVS V2) |
| T-262-20 | Elevation of privilege | Reaching for an admin key or calling an admin tool to "just fix" a MISS row | mitigate | READ tier only, stated as a hard boundary. Measurement 6 records `brain_write` / `ingest_framework` as ABSENT rather than attempting them. This phase opens no admin window (ASVS V4) |
| T-262-21 | Tampering | Cypher injection via a framework name or a Brain-returned string | mitigate | `normalize_framework_name` / `orchestration_readiness` take parameterized arguments; every `brain_query` string is authored in this plan and contains no interpolated Brain-returned or user-supplied value. `text2cypher` is forbidden and grepped for |
| T-262-22 | Tampering / Repudiation | Log injection via Brain-returned text landing in a tracked doc | mitigate | Gate output already passes through `_capDetail()` (300-char cap, whitespace collapse). Raw probe output is pasted inside fenced blocks and is bounded by the same 300-char convention when it is Brain-supplied prose |
| T-262-23 | Information disclosure | Canon Part 8 breach (LOCAL to BRAIN egress) | mitigate | Only generic framework-name handles and repo-authored read-only Cypher cross the wire. No room content, no file paths, no user turn text. Every probe argument in this plan is enumerated explicitly |
| T-262-24 | Repudiation | A VOID run recorded as a red floor, or a carried-forward number reported as fresh | mitigate | Pitfall 1 and Pitfall 2 named in the action; the checkpoint requires VOID = 0 before any pass/miss figure is trusted; D-04's dating sentence is a grepped acceptance criterion |
| T-262-25 | Denial of service | A 429 mid-gate producing a VOID that gets auto-retried into a misleading number | mitigate | D-08: no auto-retry. The gate is re-run only by a human at the checkpoint; targeted probes honor `Retry-After` individually |
| T-262-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs. Every probe reuses `brainCall` and `resolveBrainKey`, both already in the repo |
</threat_model>

<verification>
```bash
pwd    # must be /home/jsagi/dev/MindrianOS-Plugin
node scripts/check-flagship-floor.cjs; echo "EXIT=$?"
grep -n "measured against the incumbent Brain on" docs/262-LIVE-MEASUREMENT-EVIDENCE.md
grep -nE "brain_write.*(PRESENT|ABSENT)" docs/262-LIVE-MEASUREMENT-EVIDENCE.md
grep -cE "Bearer |MINDRIAN_BRAIN_KEY=" docs/262-LIVE-MEASUREMENT-EVIDENCE.md    # must be 0
grep -c "text2cypher" docs/262-LIVE-MEASUREMENT-EVIDENCE.md                     # must be 0
bash tests/run-all-262.sh
```
</verification>

<success_criteria>
- A window-fresh floor verdict with 0 VOID rows and 28 enumerated frameworks is recorded with its command, its exit code and today's date.
- The FLOOR-03 canonical-match count is a fresh live measurement, printed as the full array, never a carried-forward number.
- The write seam is re-confirmed by a `tools/list` call, so the gap ledger's write-dependency column is a measurement.
- The tripwire from 262-02 is proven non-misfiring against the incumbent Brain.
- Zero graph writes, zero admin-key resolutions, zero auto-retries, zero key material in the record.
- The navigator has signed off, and the sign-off is quoted in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/262-floor-green-sweep-02-inversion/262-04-SUMMARY.md` when done.
Quote the navigator's approval verbatim, restate the confirmed VOID / PASS / MISS counts and
the FLOOR-03 measured count, and state the Theo `/health` status code, so Plan 262-05 writes
its rulings against confirmed numbers rather than re-reading the evidence file.
</output>
