---
phase: 262-floor-green-sweep-02-inversion
plan: 05
type: execute
wave: 4
depends_on: [262-03, 262-04]
files_modified:
  - docs/262-FLOOR-01-GAP-LEDGER.md
  - docs/262-WORKORDER-brain-repo-floor-remediation.md
  - docs/262-NOTE-theo-adaptation-list-additions.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FLOOR-01, FLOOR-02, FLOOR-03]
tags: [gap-ledger, work-order, cross-repo, floor-03-ruling, theo-flip, d-02, d-05]

must_haves:
  truths:
    - "Every MISS row from the live run has a named root cause, a named owner, an explicit write-dependency answer, and dated evidence behind it"
    - "The six write-dependent rows exist as a written work order addressed to the Brain repo's own intake, never as an inline edit made from this repo"
    - "FLOOR-03 is re-ruled on fresh live evidence, with the ruling, the measured count, the command and the date all recorded together"
    - "The Brain-repo work order for FLOOR-03 cites Theo's existing ALIAS_FORK refusal shape as the target, not a from-scratch design"
    - "scripts/check-flagship-floor.cjs and scripts/build-brain-census.cjs are written up as additions to Theo's own adaptation list"
    - "Narrowing the ratified denominator is recorded as considered and REJECTED, so the record shows the gate was not gamed"
    - "The Pyramid Principle retarget-versus-enrich question is surfaced as an undecided card with both competing rulings intact, not decided here"
  artifacts:
    - path: "docs/262-FLOOR-01-GAP-LEDGER.md"
      provides: "The signed gap ledger D-01 requires: per MISS row measured state, root cause, owner, write-dependency, dated evidence, plus the D-07 finding, the Pyramid card, the rejected options and the FLOOR-03 ruling"
      contains: "write-dependency"
      min_lines: 90
    - path: "docs/262-WORKORDER-brain-repo-floor-remediation.md"
      provides: "The D-02 cross-repo work order: node ids, measured evidence, the FLOOR-03 Cypher guard framed against Theo's ALIAS_FORK shape, and the filing instruction"
      contains: "ProblemsWorthSolving-Brain"
      min_lines: 60
    - path: "docs/262-NOTE-theo-adaptation-list-additions.md"
      provides: "The D-04 written note adding the two unlisted plugin consumers to Theo's 7-file adaptation list, with the exact break and what 262 already mitigated"
      contains: "adaptation list"
      min_lines: 40
  key_links:
    - from: "docs/262-FLOOR-01-GAP-LEDGER.md"
      to: "docs/262-LIVE-MEASUREMENT-EVIDENCE.md"
      via: "every row cites the dated measurement rather than restating research"
      pattern: "262-LIVE-MEASUREMENT-EVIDENCE"
    - from: "docs/262-WORKORDER-brain-repo-floor-remediation.md"
      to: "/home/jsagi/Theo/src/mcp/content/normalize-framework-name.ts"
      via: "the ALIAS_FORK refusal shape cited as the target reference (D-05)"
      pattern: "ALIAS_FORK"
    - from: ".planning/REQUIREMENTS.md"
      to: "docs/262-FLOOR-01-GAP-LEDGER.md"
      via: "the FLOOR-01 and FLOOR-03 annotations point at the ledger"
      pattern: "262-FLOOR-01-GAP-LEDGER"
---

<objective>
Close Phase 262's disposition track: turn the dated measurements from Plan 262-04 into an
attributed, routed, signed record, and hand the parts this repo cannot execute to the two
repos that can.

Purpose: D-01 ratified 20/28 with a signed gap ledger this milestone. FLOOR-01 closes as
"measured, attributed and routed", not as "28/28 green"; the green run itself moves to
whichever phase reopens the Brain-repo write seam. D-02 routes the six write-dependent rows
to the Brain repo as a WRITTEN work order with node ids and measured evidence, filed into
that repo's own intake, never executed inline (Pitfall 6). D-05 says the FLOOR-03 work order
must be framed as "close the gap to the refusal-on-fork shape Theo's resolver already
ships", citing `Theo/src/mcp/content/normalize-framework-name.ts`, rather than as a
from-scratch design. D-04 says `scripts/check-flagship-floor.cjs` and
`scripts/build-brain-census.cjs` get added to Theo's own adaptation list as a written note.

This plan writes documents. It executes no graph write, opens no admin window, edits no file
in the Brain repo or in Theo, and decides no Brain-repo content question.

Explicitly NOT decided here (CONTEXT.md "Claude's Discretion", defaulting to surface rather
than decide): the Pyramid Principle retarget-versus-enrich ruling. Both competing 261-08
rulings go into the ledger as a card, intact and unadjudicated, and travel with the work
order. This phase does not own Brain-repo content decisions, by D-02's own routing rule.

Explicitly NOT absorbed here (D-02, deferred): the unowned `pattern_type` write-path
defect's own root-cause pass. It is named in the ledger with measured evidence and routed as
a Brain-repo scoping request; it needs its own phase.

Output: three tracked documents plus requirement annotations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-floor-green-sweep-02-inversion/262-CONTEXT.md
@.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md
@docs/262-LIVE-MEASUREMENT-EVIDENCE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

## Artifacts this phase produces (Plan 05 share)

| Artifact | Kind | Notes |
|---|---|---|
| `docs/262-FLOOR-01-GAP-LEDGER.md` | new tracked file | The gap-ledger container choice. `.planning/` is gitignored, so a ledger cited by two other repos has to be tracked to travel; this is the discretion call CONTEXT.md left open, recorded with its reason |
| `docs/262-WORKORDER-brain-repo-floor-remediation.md` | new tracked file | The D-02 cross-repo work order |
| `docs/262-NOTE-theo-adaptation-list-additions.md` | new tracked file | The D-04 Theo note (a message, not code) |
| `.planning/REQUIREMENTS.md` FLOOR-01 / FLOOR-02 / FLOOR-03 annotations | modified rows | Dated dispositions pointing at the ledger |

No new code symbols. No package installs. No Brain-repo or Theo-repo file is created or
edited from here.

<tasks>

<task type="auto">
  <name>Task 1: Write the signed FLOOR-01 gap ledger</name>

  <files>docs/262-FLOOR-01-GAP-LEDGER.md</files>

  <read_first>
    - `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` in full (Plan 262-04's output; every number in the ledger comes from here, not from research)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-04-SUMMARY.md` (the navigator's confirmed counts and sign-off)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-01-SUMMARY.md` (the measured D-07 answer: SEP occurrences and framework-node count in the orchestration projection, plus the honest section-affinity scope limit)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-CONTEXT.md` decisions D-01, D-02, D-04, D-05, D-07 and the "Claude's Discretion" block
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md` sections "The eight MISS rows, with per-dimension root cause", "Root Cause 1", "Root Cause 2", "Did Phase 261 change anything the floor validates against?", "Architecture Patterns" (Anti-patterns to avoid), and Open Questions 1, 3 and 7
    - `.planning/REQUIREMENTS.md` lines 109-125 (the FLOOR-01/02/03 rows this ledger annotates)
  </read_first>

  <action>
    Create `docs/262-FLOOR-01-GAP-LEDGER.md`. Structure it exactly as follows.

    Section 1, header and disposition. State the phase, the date, and the D-01 ruling in one
    paragraph: FLOOR-01 closes this milestone as "measured, attributed and routed", NOT as
    "28/28 green"; the green run moves to whichever phase reopens the Brain-repo write seam;
    the 28-name denominator was NOT narrowed. Restate the headline verdict from Plan 262-04
    with the sentence "measured against the incumbent Brain on <date>" and the exact command
    `node scripts/check-flagship-floor.cjs`, its exit code, and the PASS / MISS / VOID
    counts.

    Section 2, the row table. One row per MISS row from the live run, with exactly these
    columns and no fewer:

      | # | Framework | matches | score | Failing leg | Root cause | Owner | Needs a graph write? | Evidence |

      - `Owner` is one of exactly three values: `this repo`, `Brain repo`, `unowned`.
      - `Needs a graph write?` is `yes` or `no`, and `no` must be justified in the row
        (FLOOR-03 is the one row that needs NO admin window: it is a read-path guard).
      - `Evidence` cites `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` plus the specific
        measurement number within it, never 262-RESEARCH.md's numbers.
      - Every root cause must be one of the four traced classes, named explicitly: the SEP
        name regression (with node ids 28757 and 28775), the hop-depth-1 `ALIAS_OF` fork
        defect, the `pattern_type` write-path shortfall, or the Pyramid Principle
        name-target mismatch. If a live row does not fit any of the four, say so plainly and
        mark it `unowned, root cause not traced`, rather than forcing it into a class.

    Section 3, attribution to Phase 261. State plainly, with the before/after numbers, that
    Phase 261 was not floor-neutral: it went green in nine places (11/28 to 20/28) and
    regressed in two. Name the two harmful changes: the SEP relabel side effect (which cost
    PWS Triple Validation Compass a row the ceremony had just earned, because its readiness
    rose to 3 while its resolver count broke to 2 in the same window) and the systematic
    `pattern_type` write-path shortfall. Without this section the record reads as "the floor
    simply did not go green", which is not what happened.

    Section 4, the `pattern_type` arithmetic. Show the arithmetic that stops the obvious
    wrong conclusion: applying +1 for `pattern_type` to every affected row yields roughly
    22/28, not 28/28. Name the rows that still miss after that fix and why (Four Lenses and
    MECE carry honest sub-floor ceilings their own 261-05 and 261-07 payloads already
    disclosed; Pyramid is the name-target mismatch). Recompute this against the live numbers
    from Plan 262-04, do not copy research's arithmetic.

    Section 5, the D-07 finding. State the question ("do Phases 254 and 255 actually need
    28/28, or does 20/28 plus a known-gap list already unblock them?"), the measurement
    (from `262-01-SUMMARY.md`: SEP occurrences and framework-node count in
    `data/brain-orchestration-projection.json`), the mechanism (the projection is a
    Brain-DERIVED LOCAL cache, BOG-09, with no live Brain read anywhere in its generator, so
    a corrupted `name` on a Brain `:Framework` node has no path into it), and the honest
    scope limit: this answers the PROJECTION half only, and Phase 255's section-affinity
    ranking may read the live Brain and is NOT covered. Close with an explicit recommendation
    to the 254 and 255 planners and a note that the remaining half needs its own probe.

    Section 6, the FLOOR-03 re-ruling. Record: the fresh measured count with its command and
    date; the traced mechanism (the alias branch of `NORMALIZE_NAME_CYPHER` stops at one hop,
    so an intermediate alias is emitted as canonical, and `reduce` dedups by string rather
    than by node so both survive); the live contract violation (the tool's own shipped
    description promises every entry in `canonical_matches` is canonical); and the ruling
    itself, stated in the requirement's own terms: exactly-1 remains the correct assertion,
    the measured 2 is a resolver defect, and neither "update the assertion to 2" nor "weaken
    exactly-1 to >= 1" is acceptable. Add the D-05 line: Theo's own `resolveFramework`
    already treats this exact shape as an `ALIAS_FORK` refusal rather than silently returning
    two canonical matches, so the fix has a working reference implementation and is a
    gap-closure, not an invention.

    Section 7, the Pyramid Principle card. SURFACE, do not decide. Present both competing
    261-08 rulings intact and unadjudicated, name the two nodes (the ratified floor name
    "The Pyramid Principle" and the node `minto-pyramid.mjs` actually enriches, "Minto
    Pyramid") with their live measured readiness scores from Plan 262-04, and state that the
    ruling travels with the Brain-repo work order because this phase does not own Brain-repo
    content decisions (D-02's own routing rule). Do not pick a side.

    Section 8, options considered and REJECTED. Three entries, each with the reason:
      - Narrowing `data/flagship-floor-set.json` to the passing set. Rejected: the file is
        navigator-ratified and the gate prints `ratified_by` / `ratified_at`, so a narrowed
        file would print "RATIFIED at 20 framework(s)" and exit 0 while measuring nothing.
        This is gaming the gate. `tests/test-262-floor-denominator.cjs` now makes it a red
        test.
      - Weakening the exactly-1 rule to `>= 1`. Rejected: the gate's own header explains that
        a multi-match name makes every readiness probe ambiguous, so relaxing it makes the
        readiness column meaningless.
      - Adding a client-side SEP filter to the plugin-side gate. Rejected: it would green two
        rows today while hiding a real graph defect behind a band-aid on the wrong side of
        the wire, and would silently mask future corruption.

    Section 9, routing summary. A short table: for each disposition class, which document
    carries it forward (`docs/262-WORKORDER-brain-repo-floor-remediation.md` for the
    Brain-repo rows, `docs/262-NOTE-theo-adaptation-list-additions.md` for the Theo
    forward-compatibility gap, this repo for the tripwire and FLOOR-02 which already shipped
    in Plans 262-02 and 262-03).

    Section 10, sign-off. Record the navigator approval quoted in `262-04-SUMMARY.md`, with
    its date. This is the "signed" in "signed gap ledger".

    Every number in this file carries the sentence fragment "measured against the incumbent
    Brain on <date>" or an explicit citation into
    `docs/262-LIVE-MEASUREMENT-EVIDENCE.md`. No undated numbers (D-04).

    Hyphens only. No em-dashes: `tests/run-all-262.sh`'s fence sweeps this path.
  </action>

  <verify>
    <automated>bash tests/run-all-262.sh</automated>
  </verify>

  <acceptance_criteria>
    - `docs/262-FLOOR-01-GAP-LEDGER.md` exists and is at least 90 lines.
    - It contains all ten sections named above, in order, each with a markdown heading.
    - The row table header contains the literal column names `Root cause`, `Owner` and `Needs a graph write?`.
    - Every `Owner` cell is exactly one of `this repo`, `Brain repo`, `unowned`; `grep -oE '\| (this repo|Brain repo|unowned) \|' docs/262-FLOOR-01-GAP-LEDGER.md | wc -l` equals the number of MISS rows recorded.
    - `grep -c "262-LIVE-MEASUREMENT-EVIDENCE" docs/262-FLOOR-01-GAP-LEDGER.md` is at least 3.
    - `grep -c "28757" docs/262-FLOOR-01-GAP-LEDGER.md` and `grep -c "28775" docs/262-FLOOR-01-GAP-LEDGER.md` are each at least 1 (the SEP rows carry their node ids).
    - `grep -ci "rejected" docs/262-FLOOR-01-GAP-LEDGER.md` is at least 3 (the three rejected options).
    - `grep -c "ALIAS_FORK" docs/262-FLOOR-01-GAP-LEDGER.md` is at least 1 (the D-05 Theo analog is cited).
    - Section 7 contains no verdict language for the Pyramid question: `grep -Eci "we (rule|decide|choose)|the ruling is (retarget|enrich)" docs/262-FLOOR-01-GAP-LEDGER.md` returns 0.
    - `grep -c "measured against the incumbent Brain on" docs/262-FLOOR-01-GAP-LEDGER.md` is at least 1.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' docs/262-FLOOR-01-GAP-LEDGER.md` returns 0 and `bash tests/run-all-262.sh` exits 0.
  </acceptance_criteria>

  <done>FLOOR-01 has a signed, dated, per-row record naming root cause, owner and write-dependency, and the record shows the rejected shortcuts were considered and refused.</done>
</task>

<task type="auto">
  <name>Task 2: Write the two cross-repo handoffs (Brain-repo work order, Theo adaptation-list note)</name>

  <files>docs/262-WORKORDER-brain-repo-floor-remediation.md, docs/262-NOTE-theo-adaptation-list-additions.md</files>

  <read_first>
    - `docs/262-FLOOR-01-GAP-LEDGER.md` (Task 1's output; the work order restates its Brain-repo rows in an actionable form and must not contradict it)
    - `docs/262-LIVE-MEASUREMENT-EVIDENCE.md` (the node ids, alias topology and measured counts the work order quotes)
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/.planning/todos/pending/2026-09-02-fix-pattern-type-readiness-shortfall-on-existing-frameworks.md` (READ ONLY, cross-repo: the already-filed `pattern_type` todo this work order cross-references rather than duplicates)
    - `/home/jsagi/dev/ProblemsWorthSolving-Brain/src/arm1-orchestrator.mjs` around line 87 (READ ONLY: the `NORMALIZE_NAME_CYPHER` FIX-03 form, its direct branch's existing `NOT exists((f)-[:ALIAS_OF]->(:Framework))` guard, and the alias branch that lacks the mirrored guard)
    - `/home/jsagi/Theo/src/mcp/content/normalize-framework-name.ts` (READ ONLY: `resolveFramework`, `MAX_ALIAS_HOPS`, the cycle control, the fork refusal and the five refusal codes, including `ALIAS_FORK`)
    - `/home/jsagi/Theo/src/mcp/content/orchestration-readiness.ts` around lines 485-505 (READ ONLY: the payload shape and the hardcoded `unsynced_inputs: ['pattern_known']`)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md` sections "The Theo Flip" (all of it, including the two payload tables, the "Why the existing Theo fix does not cover this" subsection and the "Three more consequences" list), "Root Cause 2" (the recommended guard form and the blast-radius warning), and the Assumptions Log entries A1, A2, A6 and A7
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-02-SUMMARY.md` (the tripwire's exact emitted `failures[]` shape, which the Theo note quotes as the mitigation already in place)
    - `CLAUDE.md` "Open Handoffs" table and the `docs/2026-09-01-HANDOFF-phases-272-274-275-plus-theo-flip-coordination.md` row (the house style for a cross-repo handoff document, and the named 7-file adaptation list)
  </read_first>

  <action>
    WORKSPACE GUARD, restated because this task reads two other repos: read from
    `/home/jsagi/dev/ProblemsWorthSolving-Brain/` and `/home/jsagi/Theo/`, write ONLY under
    `/home/jsagi/dev/MindrianOS-Plugin/`. Create no file, edit no file and run no git command
    in either of those repos. Pitfall 6: Brain-repo edits made under a Plugin phase number are
    invisible to the Brain's own STATE and ledger, which is exactly the unattributable-write
    class RECON-01 exists to kill.

    Document A: `docs/262-WORKORDER-brain-repo-floor-remediation.md`. Sections:

      1. What this is and what it is not. A work order FROM MindrianOS-Plugin Phase 262 TO
         the ProblemsWorthSolving-Brain repo's own todo / phase intake. It is a request with
         evidence, not an executed change. Name the reason: the two repos have independent
         planning trees, and an edit made from here would be unattributable in the Brain's
         own ledger.
      2. The blocker, stated as a measurement. From Plan 262-04's `tools/list` check:
         `brain_write` and `ingest_framework` ABSENT, `BRAIN_HTTP_ADMIN=deny` since
         2026-09-01T20:54:40Z. Six of the rows below need a graph write and therefore need an
         operator-opened admin window. FLOOR-03 is the exception and needs none.
      3. Item 1, the SEP name repair. Node ids 28757 (HSI Semantic Surprise Analysis
         Assistant phantom) and 28775 (PWS Triple Validation Compass phantom), the live SEP
         counts from Plan 262-04, and the causal link stated plainly: these two nodes break
         two NAMED rows of the ratified 28-framework floor, so the repair has a named
         consumer and is not cosmetic hygiene. Cross-reference the Brain repo's own Phase 5
         Success Criterion 3, which already owns the archived-block name repair. Per
         assumption A2, write the acceptance condition as a BEHAVIOR, not a state:
         "`normalize_framework_name` returns exactly 1 canonical match for
         'HSI Semantic Surprise Analysis Assistant' and for 'PWS Triple Validation Compass'",
         NOT "the names are repaired". A truncated-but-still-substring-colliding repair would
         satisfy the state condition and still leave the floor rows red.
      4. Item 2, the `pattern_type` write-path shortfall. Cross-reference the Brain repo's
         existing pending todo by its full path, state that it was explicitly reviewed during
         that repo's own `/gsd-discuss-phase 5` and explicitly kept OUT of Phase 5's scope,
         and state the consequence: it is currently unowned by any planned phase in either
         repo. Attach the measured per-dimension evidence from Plan 262-04. Request scoping,
         not a fix: per D-02 this needs its own root-cause pass and probably its own Brain
         phase, and Phase 262 does not absorb it.
      5. Item 3, FLOOR-03, the hop-depth-1 alias fork. This is the one item that needs NO
         admin window: it is a read-path code change. Give the traced mechanism, the live
         alias topology with node ids from Plan 262-04, and the independent confirmation
         probe. Frame the ask in D-05's exact terms: "close the gap between the current
         Brain's silent-fork behavior and the refusal-on-fork behavior Theo's resolver
         already ships", citing `/home/jsagi/Theo/src/mcp/content/normalize-framework-name.ts`
         and its `ALIAS_FORK` refusal code by name. Offer the two candidate shapes: the
         mirrored terminal guard on the alias branch (the Part 7 reuse answer, since the
         direct branch already carries the same guard) and the variable-length
         `ALIAS_OF*1..3` form (riskier: cycle exposure and cost). Attach assumption A1's
         discharge procedure verbatim: both candidate forms must be executed READ-ONLY
         against canon before either is written into a Brain plan. Attach the blast-radius
         warning: the guard changes `normalize_framework_name` for every framework, so the
         full 28-row floor gate must be re-run before and after, and
         `NORMALIZE_NAME_CYPHER` is already exported from `src/arm1-orchestrator.mjs`
         (deliberately, by 260-03) so a before/after matrix is reproducible.
      6. Item 4, the Pyramid Principle card, carried across unadjudicated. Both competing
         261-08 rulings, both node ids, both live readiness scores. State explicitly that
         Phase 262 declined to decide it because it is a Brain-repo content decision.
      7. Security constraints the receiving repo must honor, restated so they travel with the
         request: parameterized `$raw` only, never string-interpolate a framework name into
         Cypher (this applies to the proposed fix itself); `brain_query` not `text2cypher`;
         and admin-disable is the LAST scripted item of any window that opens (the
         two-day-open lesson).
      8. Filing instruction. File this into the Brain repo's own
         `.planning/todos/pending/` intake or its phase intake, from a session working in
         THAT repo. Do not execute any of it from MindrianOS-Plugin.

    Document B: `docs/262-NOTE-theo-adaptation-list-additions.md`. Sections:

      1. The ask, up front: add `scripts/check-flagship-floor.cjs` and
         `scripts/build-brain-census.cjs` to Theo's named plugin adaptation list. Enumerate
         the current 7 files so the recipient can see these are additions to a known list
         rather than a new list: `scripts/probe-brain-contract.cjs`,
         `lib/brain/chain-recommender.cjs`, `lib/core/enrichment-queue.cjs`,
         `bin/mindrian-brain-mcp-client.cjs`, `lib/core/resolve-brain-key.cjs`,
         `data/brain-surface-contract.json`, and `BRAIN_TOOL_MATCHER` / `hooks/hooks.json`.
      2. The break, precisely. The gate reads exactly two paths,
         `result.canonical_matches` and `result.readiness.readiness_score`. Theo returns
         `{canonical, matched_via, coverage}` and `{framework, score, inputs, evidence,
         unsynced_inputs, coverage, diagnostics}`. Both reads degrade to `null`, both calls
         still report `ok: true`, so no `failures[]` entry is produced and the VOID branch
         never fires. Composed outcome before Phase 262's change: `0/28 PASS`, exit 1,
         `FLOOR DOES NOT HOLD`, indistinguishable from a genuinely red floor. Name it as the
         same class Theo's own doc calls "the single highest-risk line".
      3. Why the already-shipped Theo fix does not cover it. `lib/core/brain-client.cjs`'s
         `query()` was fixed for `{rows, diagnostics}` (commits `719f4499` RED, `21fdd7bc`
         GREEN). The floor gate does not use `brain-client.cjs` at all: it imports
         `brainCall` from `scripts/build-brain-census.cjs`, which requires only `node:fs`,
         `node:path` and `lib/core/resolve-brain-key.cjs`, and is an independent direct-fetch
         client for two different tools.
      4. What Phase 262 already did, and what it deliberately did not. Quote the tripwire's
         emitted `failures[]` shape from `262-02-SUMMARY.md`: kind `unrecognized_shape`,
         which routes an unreadable-but-successful payload through the existing VOID
         machinery (banner, exit 3, per-row detail, mandatory human re-run). State that the
         gate's READERS were deliberately NOT adapted to Theo's shape, per D-04, because
         `theo-mcp.onrender.com` could not be exercised (record the `/health` status code
         Plan 262-04 measured). That adaptation is flip-day work and belongs with the other
         listed files.
      5. Three consequences Theo's side should price in, each with its source:
         `pattern_known` is hardcoded into `unsynced_inputs` unconditionally, which caps
         readiness at 3 of 4 for every framework and leaves the floor's `>= 3` threshold with
         zero headroom, so the floor gets strictly harder after the flip; `brain_write`
         refuses unconditionally on Theo with no write path behind it, so every FLOOR-01
         graph remediation must land on the incumbent Brain BEFORE the flip or be
         re-expressed as a Theo-side ingestion (Theo Phase 10's framework-ingestion
         contract), which turns "schedule a window sometime" into a dated deadline; and the
         populations differ (Theo's measured framework count against the incumbent's), so
         whether all 28 ratified floor names resolve on Theo is unmeasured and unmeasurable
         until the origin serves. Tag that last one as assumption A7 and name it a navigator
         re-ratification question, not an engineering one.
      6. Delivery. State where this goes: to the parallel Theo-working session
         ("Brain-Theo graph reconciliation execution"), and check for a reply before assuming
         the open question about starting plugin-side adaptation is unanswered. Note that
         this is a message, not a code change, and that nothing in Theo's repo is edited from
         here.

    Both documents: hyphens only, no em-dashes. Both are swept by
    `tests/run-all-262.sh`'s fence.
  </action>

  <verify>
    <automated>bash tests/run-all-262.sh</automated>
  </verify>

  <acceptance_criteria>
    - Both files exist; the work order is at least 60 lines and the Theo note at least 40.
    - `git -C /home/jsagi/dev/ProblemsWorthSolving-Brain status --short` and `git -C /home/jsagi/Theo status --short` show no change attributable to this task (read-only cross-repo access, Pitfall 6).
    - `grep -c "ALIAS_FORK" docs/262-WORKORDER-brain-repo-floor-remediation.md` is at least 1, and the same file cites the path `Theo/src/mcp/content/normalize-framework-name.ts`.
    - The work order's FLOOR-03 item states the acceptance condition as a behavior: `grep -c "returns exactly 1" docs/262-WORKORDER-brain-repo-floor-remediation.md` is at least 1 (assumption A2's discharge).
    - `grep -c "read-only" docs/262-WORKORDER-brain-repo-floor-remediation.md` is at least 1 (assumption A1's discharge procedure travels with the ask).
    - `grep -c "parameterized" docs/262-WORKORDER-brain-repo-floor-remediation.md` is at least 1 and `grep -c "text2cypher" docs/262-WORKORDER-brain-repo-floor-remediation.md` is at least 1 (the security constraints travel with the request).
    - `grep -c "28757" docs/262-WORKORDER-brain-repo-floor-remediation.md` and `grep -c "28775"` are each at least 1.
    - `grep -c "check-flagship-floor.cjs" docs/262-NOTE-theo-adaptation-list-additions.md` and `grep -c "build-brain-census.cjs"` are each at least 1.
    - `grep -c "unrecognized_shape" docs/262-NOTE-theo-adaptation-list-additions.md` is at least 1 (the mitigation already in place is named).
    - `grep -c "pattern_known" docs/262-NOTE-theo-adaptation-list-additions.md` is at least 1.
    - `grep -cE "Bearer |MINDRIAN_BRAIN_KEY=" docs/262-WORKORDER-brain-repo-floor-remediation.md docs/262-NOTE-theo-adaptation-list-additions.md` returns 0 for both.
    - `LC_ALL=C.UTF-8 grep -clP '\x{2014}' docs/262-WORKORDER-brain-repo-floor-remediation.md docs/262-NOTE-theo-adaptation-list-additions.md` prints nothing, and `bash tests/run-all-262.sh` exits 0.
  </acceptance_criteria>

  <done>The two repos that can actually close these rows have a written, evidence-backed, security-constrained request each, and neither repo was touched from here.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate the requirements and composite the findings into the rethinking room</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 105-130 (the "Phase family E - Floor Green + SWEEP-02" block containing the FLOOR-01, FLOOR-02 and FLOOR-03 rows, and the surrounding annotation style used by the other requirement rows)
    - `docs/262-FLOOR-01-GAP-LEDGER.md` (Task 1's output, which these annotations point at)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-03-SUMMARY.md` (the FLOOR-02 evidence: the green acceptance-gate block and the git rename line)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-04-SUMMARY.md` (the confirmed floor verdict and the FLOOR-03 measured count)
    - `CLAUDE.md` section "Dev-Research Compositing (Rethinking Room)" (the both-places rule and what counts as incomplete)
    - `.planning/phases/274-*/274-*-SUMMARY.md` if present (the precedent for how a blocked or wrong-room write was recorded as an expected non-blocking outcome rather than a phase failure)
  </read_first>

  <action>
    Part A, requirement annotations in `.planning/REQUIREMENTS.md`. Edit only the three FLOOR
    rows in the "Phase family E" block. Do not reflow or reformat the rest of the file.

      - FLOOR-02: change the checkbox to `[x]` and append a dated annotation naming what
        shipped: the fixture repurposed by `git mv` to
        `tests/fixtures/127-03-acceptance/no-identity-refusal/` with history followed, the
        README inverted to assert refusal, gate 1 hardened with the honesty assertions plus
        the no-methodology-served negative assertion, the byte-locked
        `DIRECTOR_NOT_AVAILABLE` wire string unchanged, and all of `tests/run-all-127.sh`
        green. Add the D-06 note: this shipped decoupled from FLOOR-01's exit code, because
        there is no technical coupling and the fixture never contacts the Brain.

      - FLOOR-01: leave the checkbox as it is and append a dated D-01 annotation. State the
        re-scope in the requirement's own vocabulary: this milestone closes FLOOR-01 as
        "measured, attributed and routed" against a window-fresh, zero-VOID run; the
        "exits 0" condition itself moves to whichever phase reopens the Brain-repo write
        seam, because six of the rows need a graph write and `brain_write` /
        `ingest_framework` were measured ABSENT. Point at
        `docs/262-FLOOR-01-GAP-LEDGER.md` and
        `docs/262-WORKORDER-brain-repo-floor-remediation.md` by path. State explicitly that
        the 28-name denominator was NOT narrowed and that narrowing is a recorded rejected
        option. Leave the checkbox state itself to `/gsd-verify-work` and say so in the
        annotation, so a later reader knows the box was left deliberately rather than
        forgotten.

      - FLOOR-03: leave the checkbox as it is and REPLACE the stale
        "NOTE 2026-08-21 ... needs a fresh ruling before 262 runs the floor check" text with
        the dated ruling: the fresh live count with its command and date, the traced
        hop-depth-1 alias-branch mechanism, the ruling that exactly-1 remains correct and the
        measured 2 is a resolver defect, the D-05 note that Theo's `resolveFramework` already
        refuses on this shape as `ALIAS_FORK`, and the routing (a read-path fix in the Brain
        repo needing no admin window, carried by
        `docs/262-WORKORDER-brain-repo-floor-remediation.md`). Keep the original note's
        provenance line so the history of the two wrong carried-forward numbers is not
        erased; mark it as superseded rather than deleting it.

    Part B, Dev-Research Compositing (CLAUDE.md standing convention). Composite this phase's
    findings into `~/MindrianRooms/rethinking-mindrianos/research/<YYYY-MM-DD>-262-floor-green-theo-tripwire/`
    as a dated research entry: the SEP regression attribution, the hop-depth-1 alias defect
    and its Theo analog, the Theo-flip silent-false-RED finding and the tripwire that closes
    it, and the D-07 projection answer. Cross-link back to this phase directory and to the
    three documents in `docs/`.

    This leg is explicitly NON-BLOCKING. `274-*-SUMMARY.md` documents that the currently
    bound room may be wrong or stale. If the room path does not exist, is not writable, or
    is bound to a different room than expected: do NOT create a room, do NOT rebind, and do
    NOT hold the phase. Record the blocked outcome honestly in `262-05-SUMMARY.md` with the
    exact path attempted and the exact reason, exactly the way 271-05 and 274-06 recorded the
    identical situation. A blocked room write is an expected outcome to report, not a phase
    failure, and it is also not something to paper over by claiming the compositing happened.

    Hyphens only in every edit. No em-dashes.
  </action>

  <verify>
    <automated>grep -c "262-FLOOR-01-GAP-LEDGER" .planning/REQUIREMENTS.md &amp;&amp; grep -A2 "FLOOR-02" .planning/REQUIREMENTS.md | grep -c "no-identity-refusal" &amp;&amp; bash tests/run-all-262.sh</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` FLOOR-02 row is checked `[x]` and its annotation names `no-identity-refusal`, `DIRECTOR_NOT_AVAILABLE` and D-06.
    - The FLOOR-01 annotation contains the phrase `measured, attributed and routed`, cites `docs/262-FLOOR-01-GAP-LEDGER.md` by path, states the denominator was not narrowed, and says the checkbox is left to `/gsd-verify-work`.
    - The FLOOR-03 row no longer instructs a future reader to obtain a fresh ruling: `grep -c "needs a fresh ruling" .planning/REQUIREMENTS.md` returns 0. The original 2026-08-21 provenance line is retained and explicitly marked superseded.
    - The FLOOR-03 annotation contains the measured count, the command that produced it, the date, and the literal token `ALIAS_FORK`.
    - `git diff .planning/REQUIREMENTS.md` touches only lines inside the "Phase family E" block (the FLOOR-01/02/03 rows); no other requirement row is reflowed.
    - The compositing leg is recorded in `262-05-SUMMARY.md` either as a written path under `~/MindrianRooms/rethinking-mindrianos/research/` with the files created, or as an honest blocked outcome naming the exact path attempted and the exact reason. A silent omission fails this criterion.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' .planning/REQUIREMENTS.md` is not increased by this task (compare against the pre-task count).
    - `bash tests/run-all-262.sh` exits 0.
  </acceptance_criteria>

  <done>The requirement rows tell the truth about what shipped, what was re-scoped and why, and the reasoning trail is composited or its absence is recorded honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo to the Brain repo / Theo repo | READ-only across both. A write here would be an unattributable change in another repo's ledger (Pitfall 6, the RECON-01 class) |
| measured evidence to a tracked, cross-repo-cited document | Brain-returned text and node ids become durable artifacts other repos plan against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-26 | Tampering | An inline edit to the Brain repo or to Theo made from a MindrianOS-Plugin phase | mitigate | Both cross-repo reads are declared read-only in the action; acceptance runs `git -C <repo> status --short` against both and requires no attributable change (Pitfall 6) |
| T-262-27 | Tampering | Cypher injection reaching the Brain via the proposed fix | mitigate | The work order carries the constraint forward explicitly: parameterized `$raw` only, never string-interpolate a framework name into Cypher, `brain_query` not `text2cypher`. Grepped in acceptance |
| T-262-28 | Repudiation | The record reading as "FLOOR-01 just did not go green", erasing that Phase 261 gained nine rows and regressed two | mitigate | Ledger Section 3 requires the attribution with before/after numbers; Section 8 requires the three rejected options with reasons |
| T-262-29 | Repudiation | Silently claiming the rethinking-room compositing happened when the room write was blocked | mitigate | Task 3 acceptance requires either the written path or an explicit blocked outcome with the attempted path and the reason; a silent omission is a failed criterion |
| T-262-30 | Information disclosure | A key value or auth header pasted into a document that leaves this repo | mitigate | Acceptance greps all three new documents for `Bearer ` and `MINDRIAN_BRAIN_KEY=` and requires 0 (ASVS V2) |
| T-262-31 | Information disclosure | Canon Part 8 breach via a cross-repo document carrying room content | mitigate | Every artifact here carries generic framework names, node ids and gate output only. No room content, no user text, no local file content from any user workspace |
| T-262-32 | Elevation of privilege | A work order that invites the receiving repo to open an admin window casually | mitigate | The work order states admin-disable is the LAST scripted item of any window (the two-day-open lesson) and marks FLOOR-03 as the one item needing NO window at all. This phase opens none (ASVS V4) |
| T-262-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs. This plan writes markdown |
</threat_model>

<verification>
```bash
bash tests/run-all-262.sh
grep -n "measured, attributed and routed" .planning/REQUIREMENTS.md
grep -n "262-FLOOR-01-GAP-LEDGER" .planning/REQUIREMENTS.md
grep -c "needs a fresh ruling" .planning/REQUIREMENTS.md          # must be 0
grep -c "ALIAS_FORK" docs/262-WORKORDER-brain-repo-floor-remediation.md
git -C /home/jsagi/dev/ProblemsWorthSolving-Brain status --short   # must show nothing from this phase
git -C /home/jsagi/Theo status --short                             # must show nothing from this phase
bash tests/run-all-127.sh
node scripts/doctor.cjs --acceptance                               # no NEW regression vs the documented baseline
```
</verification>

<success_criteria>
- A signed, dated gap ledger exists with per-row root cause, owner, write-dependency and evidence, plus the D-07 finding, the Pyramid card left undecided, and the three rejected options recorded with reasons.
- The Brain-repo work order carries node ids, measured evidence, behavior-shaped acceptance conditions, the A1 read-only discharge procedure, the blast-radius warning, the security constraints, and the D-05 framing against Theo's `ALIAS_FORK` shape.
- The Theo note names both unlisted consumers, the exact break, the tripwire already shipped, the deliberate non-adaptation, and the three post-flip consequences.
- FLOOR-02 is marked complete; FLOOR-01 and FLOOR-03 carry dated dispositions pointing at the ledger, with the stale FLOOR-03 instruction superseded rather than deleted.
- Neither the Brain repo nor Theo was written to from here.
- The rethinking-room compositing either happened or its blocked outcome is recorded honestly with the path and the reason.
</success_criteria>

<output>
Create `.planning/phases/262-floor-green-sweep-02-inversion/262-05-SUMMARY.md` when done.
Record: the final ledger row count and the owner split (this repo / Brain repo / unowned),
the FLOOR-03 ruling in one sentence with its measured count and date, the two handoff
documents and where each is addressed, and the compositing outcome (written path, or blocked
with the exact path and reason).
</output>
