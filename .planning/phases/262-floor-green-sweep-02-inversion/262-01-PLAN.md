---
phase: 262-floor-green-sweep-02-inversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-262-floor-denominator.cjs
  - tests/test-262-sep-projection-probe.cjs
  - tests/run-all-262.sh
autonomous: true
requirements: [FLOOR-01]
tags: [floor-gate, denominator, orchestration-projection, test-harness]

must_haves:
  truths:
    - "A narrowed data/flagship-floor-set.json fails a test instead of silently printing a smaller RATIFIED denominator"
    - "A framework name that drifts out of command frontmatter fails loud instead of silently dropping out of the floor set"
    - "The D-07 question (does the orchestration projection surface any <SEP> node) is answered by a running assertion, not by an assumption"
    - "A phase-262 test harness exists that fails when it discovers zero test files"
  artifacts:
    - path: "tests/test-262-floor-denominator.cjs"
      provides: "Ratified-denominator integrity proof (28 names, ratified_at pinned, 1:1 against the live frontmatter scan)"
      contains: "flagship-floor-set.json"
    - path: "tests/test-262-sep-projection-probe.cjs"
      provides: "D-07 probe made hermetic: zero <SEP> in the orchestration projection, every ratified name present as a framework node"
      contains: "brain-orchestration-projection.json"
    - path: "tests/run-all-262.sh"
      provides: "Phase 262 aggregator with glob discovery, the found-eq-0 guard, and the no-em-dash fence"
      contains: "TEST_262_PREFIX"
  key_links:
    - from: "tests/test-262-floor-denominator.cjs"
      to: "scripts/build-brain-census.cjs"
      via: "scanMethodologyCommands() import"
      pattern: "scanMethodologyCommands"
    - from: "tests/test-262-floor-denominator.cjs"
      to: "scripts/check-flagship-floor.cjs"
      via: "parseOverrideFile() import (Part 7 reuse, no second parser)"
      pattern: "parseOverrideFile"
    - from: "tests/run-all-262.sh"
      to: "tests/test-262-*.cjs"
      via: "glob discovery"
      pattern: "PREFIX.*\\*\\.cjs"
---

<objective>
Stand up Phase 262's Wave 0 measurement floor: the two hermetic tests that make the
ratified denominator and the D-07 projection question machine-checkable, plus the phase
aggregator every later plan verifies through.

Purpose: 262-RESEARCH.md names "narrowing data/flagship-floor-set.json to make the gate
green" as the single most tempting way to fake FLOOR-01, and names Pitfall 3 (a name that
drifts by one character silently drops out of the floor rather than failing loud). Neither
has a test today. D-07 (CONTEXT.md) asks whether the orchestration projection surfaces any
`<SEP>`-corrupted node; that is a local, zero-network question and belongs in a test, not
in prose. Nothing in this plan touches the Brain.

Output: three new files under tests/. Zero production-code changes. Zero network.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/262-floor-green-sweep-02-inversion/262-CONTEXT.md
@.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md
@.planning/phases/262-floor-green-sweep-02-inversion/262-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces (Plan 01 share)

| Artifact | Kind | Notes |
|---|---|---|
| `tests/test-262-floor-denominator.cjs` | new file | Node `node:test` suite, zero network |
| `tests/test-262-sep-projection-probe.cjs` | new file | Node `node:test` suite, zero network, the D-07 probe |
| `tests/run-all-262.sh` | new file | Phase aggregator, executable bit set |

No new exported symbols. No production-code edits. No package installs (262-RESEARCH.md
"Package Legitimacy Audit": this phase installs zero external packages; an `npm install`
line anywhere in this phase means the plan drifted).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the ratified 28-name denominator against the live frontmatter scan</name>

  <files>tests/test-262-floor-denominator.cjs</files>

  <read_first>
    - `data/flagship-floor-set.json` (the ratified denominator; read its `_comment`, it documents the exact-string trap this test exists to close)
    - `scripts/check-flagship-floor.cjs` lines 50-80 and 243-276 (`OVERRIDE_PATH`, `parseOverrideFile`, and the `overrideNames.has(fw.name)` filter this test mirrors)
    - `scripts/build-brain-census.cjs` lines 190-250 (`scanMethodologyCommands` and its `{ commandCount, scannedAt, frameworks: [{name, uses}] }` return shape)
    - `tests/test-259-floor-void.cjs` lines 1-55 (the house style: `node:test` + `node:assert/strict`, header comment block, fixture builders, RED PROOF note)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md` sections "Common Pitfalls" (Pitfall 3) and "Architecture Patterns" (Anti-patterns to avoid, the narrowing anti-pattern)
  </read_first>

  <behavior>
    - Test 1: `parseOverrideFile(fs.readFileSync('data/flagship-floor-set.json','utf8'))` returns `ok: true`.
    - Test 2: `parsed.frameworks.length === 28` (the not-narrowed assertion).
    - Test 3: `parsed.meta.ratified_at === '2026-08-11'` and `parsed.meta.ratified_by` matches `/navigator/` (the ratification is pinned, so silently re-ratifying to a smaller set is a visible diff plus a red test).
    - Test 4: every name in `parsed.frameworks` is present, byte-exact, in `scanMethodologyCommands().frameworks` mapped to `.name` (1:1, zero mismatches, case-sensitive).
    - Test 5: the filter the gate actually performs, `scanned.frameworks.filter((fw) => new Set(parsed.frameworks).has(fw.name)).length`, equals 28 (Pitfall 3 made loud: a drifted frontmatter name silently drops out of the enumerated floor today).
    - Test 6 (negative / RED PROOF, in-test): running Test 2's assertion against a locally constructed 20-name copy of the parsed object must throw. Construct the 20-name object inline in the test; never write it to disk and never modify `data/flagship-floor-set.json`.
  </behavior>

  <action>
    Create `tests/test-262-floor-denominator.cjs` as a `node:test` suite using
    `node:assert/strict` (not the looser `node:assert`). Import `scanMethodologyCommands`
    from `../scripts/build-brain-census.cjs` and `parseOverrideFile` from
    `../scripts/check-flagship-floor.cjs`. Do NOT write a second JSON parser, a second
    frontmatter parser, or a second membership check: Canon Part 7 and
    262-RESEARCH.md's "Don't Hand-Roll" table both name these exact reuses.

    Resolve `data/flagship-floor-set.json` with `path.join(__dirname, '..', 'data',
    'flagship-floor-set.json')` so the suite runs from any cwd.

    Open with a header comment block in the house style that states, in one sentence
    each: what this suite proves, that "narrowing the ratified set to make the gate green"
    is the rejected option this test blocks, and that the suite is zero-network. End the
    header with the literal line `No em-dashes.` Hyphens only in this file; the phase
    aggregator sweeps it for U+2014.

    Record the pinned constants as named consts at the top: `EXPECTED_FRAMEWORK_COUNT = 28`,
    `EXPECTED_RATIFIED_AT = '2026-08-11'`. Cite the measurement date in the header:
    "28 names, diffed 1:1 against a live scanMethodologyCommands() run, measured against
    the incumbent Brain's command set on 2026-09-02" (D-04's dating rule: every floor
    number this phase writes carries the date it was measured).
  </action>

  <verify>
    <automated>node --test tests/test-262-floor-denominator.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/test-262-floor-denominator.cjs` exits 0 with 6 passing tests.
    - `grep -c "require('../scripts/build-brain-census.cjs')" tests/test-262-floor-denominator.cjs` is 1 (reuse, not reimplementation).
    - `grep -c "parseOverrideFile" tests/test-262-floor-denominator.cjs` is at least 1.
    - `git diff --stat data/flagship-floor-set.json` is empty (the test reads it, never edits it).
    - Tripwire proof, run manually and reverted byte-clean: temporarily removing one name from a scratch copy of the frameworks array inside the test makes Test 2 fail with a non-zero exit.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-262-floor-denominator.cjs` returns 0.
  </acceptance_criteria>

  <done>The ratified denominator cannot be narrowed, and a drifted frontmatter name cannot silently leave the floor set, without a red test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Answer D-07 with a running assertion (the SEP-projection probe)</name>

  <files>tests/test-262-sep-projection-probe.cjs</files>

  <read_first>
    - `scripts/build-orchestration-projection.cjs` lines 1-50 (the header: BOG-09 "zero live Brain", BOG-10 "generic machinery metadata only", and the `data/brain-orchestration-projection.json` output path)
    - `data/brain-orchestration-projection.json` (top-level keys are `ontology_ref`, `generated_note`, `chain_layer_note`, `nodes`, `edges`; framework nodes have shape `{ id: 'framework:<name>', kind: 'framework', methodology_tier: 'pws', name: '<name>' }`)
    - `data/flagship-floor-set.json` (the 28 ratified names this probe cross-checks against)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-CONTEXT.md` decision D-07
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md` "Root Cause 1: the `<SEP>` regression" and Open Question 3
  </read_first>

  <behavior>
    - Test 1: the raw text of `data/brain-orchestration-projection.json` contains zero occurrences of the literal substring `<SEP>`.
    - Test 2: no node in `projection.nodes` has a `name` or an `id` containing `<SEP>`.
    - Test 3: `projection.nodes.filter((n) => n.kind === 'framework').length` is at least 28 (measured exactly 28 on 2026-09-02).
    - Test 4: every one of the 28 ratified floor names from `data/flagship-floor-set.json` appears as a projection node with `kind === 'framework'` and a byte-exact `name` match.
    - Test 5: every framework node carries `methodology_tier === 'pws'` (Canon Part 8: the projection carries only generic machinery metadata, never a Brain-side blob).
  </behavior>

  <action>
    Create `tests/test-262-sep-projection-probe.cjs` as a `node:test` suite using
    `node:assert/strict`. Read `data/brain-orchestration-projection.json` once as text,
    assert on the text for Test 1, then `JSON.parse` it once and assert on the object for
    Tests 2 through 5. Resolve both data paths with `path.join(__dirname, '..', 'data', ...)`.

    Define the literal separator as `const SEP_MARKER = '<SEP>';` and use it in every
    assertion so a future reader can grep one token.

    The header comment block is the load-bearing half of this task. It must record the
    D-07 finding in prose, exactly this shape and no weaker:

      - the question D-07 asked ("does the orchestration projection surface any
        `<SEP>`-corrupted node, and therefore do Phases 254 and 255 actually need 28/28");
      - the mechanism that answers it: `scripts/build-orchestration-projection.cjs` is a
        Brain-DERIVED LOCAL cache (its own header, BOG-09: "no live Brain read and no live
        Brain write anywhere in this file"), built from `data/connector-registry.json` +
        `data/command-registry.json` + the local skills/agents walk, so a corrupted `name`
        property on a Brain `:Framework` node has no path into this file;
      - the measured answer, dated: "0 `<SEP>` occurrences and 28 framework nodes,
        measured 2026-09-02 against the committed projection";
      - the scope limit, stated honestly: this probe answers the PROJECTION half of D-07
        only. It does NOT prove Phase 255's section-affinity ranking is unaffected, because
        that path may read the live Brain. Name that as still open and route it to the gap
        ledger (Plan 262-05), do not silently generalize.

    End the header with the literal line `No em-dashes.` Hyphens only.

    Do NOT run `node scripts/build-orchestration-projection.cjs` and do NOT run it with
    `--check` from this test. The suite reads the committed artifact; regenerating it is a
    different gate that already exists.
  </action>

  <verify>
    <automated>node --test tests/test-262-sep-projection-probe.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/test-262-sep-projection-probe.cjs` exits 0 with 5 passing tests.
    - `grep -c "BOG-09" tests/test-262-sep-projection-probe.cjs` is at least 1 (the header cites the zero-live-Brain property by its own requirement id).
    - `grep -c "2026-09-02" tests/test-262-sep-projection-probe.cjs` is at least 1 (D-04 dating rule).
    - The header states the section-affinity scope limit; `grep -ci "section-affinity" tests/test-262-sep-projection-probe.cjs` is at least 1.
    - `git diff --stat data/brain-orchestration-projection.json` is empty (read-only probe).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-262-sep-projection-probe.cjs` returns 0.
  </acceptance_criteria>

  <done>D-07's projection half is a fact with a command behind it, and the honest remaining scope limit is written down rather than glossed.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the phase 262 aggregator with its discovery guard and em-dash fence</name>

  <files>tests/run-all-262.sh</files>

  <read_first>
    - `tests/run-all-259.sh` in full (the direct template: `PREFIX` variable, `run` / `run_may_skip`, glob discovery, `found -eq 0` guard, self-exclusion guard, the `grep -lP '\x{2014}'` fence with its `rc >= 2` SCAN BROKE branch)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-RESEARCH.md` section "Pattern: the phase-close aggregator" (the five load-bearing properties to copy)
    - `.planning/phases/262-floor-green-sweep-02-inversion/262-VALIDATION.md` section "Wave 0 Requirements"
  </read_first>

  <action>
    Create `tests/run-all-262.sh` by copying the structure of `tests/run-all-259.sh`
    wholesale (Canon Part 7: copy the proven runner, do not invent a bespoke one). Change
    exactly these things:

    1. `PREFIX="${TEST_262_PREFIX:-tests/test-262-}"`.
    2. Header block rewritten for this phase. State in one sentence each what Phase 262
       proves: (a) the ratified denominator is not narrowed and matches the live
       frontmatter scan 1:1; (b) the orchestration projection surfaces zero `<SEP>` nodes
       (D-07); (c) a successful probe carrying a payload the gate cannot read produces
       VOID with exit 3, never a false MISS (the Theo tripwire, D-04); (d) the keyless
       fixture asserts REFUSAL and was never deleted (FLOOR-02).
    3. Enumerate the mandatory tests by filename in the header even though the glob does
       discovery:
         `tests/test-262-floor-denominator.cjs`
         `tests/test-262-sep-projection-probe.cjs`
         `tests/test-262-unrecognized-shape-voids.cjs`
         `tests/test-262-refusal-fixture-retained.cjs`
    4. `EMDASH_TARGETS` array set to every tracked file Phase 262 touches, so a later plan
       needs no edit here (missing paths are skipped, not failed):
         `scripts/check-flagship-floor.cjs`
         `scripts/build-brain-census.cjs`
         `tests/test-262-floor-denominator.cjs`
         `tests/test-262-sep-projection-probe.cjs`
         `tests/test-262-unrecognized-shape-voids.cjs`
         `tests/test-262-refusal-fixture-retained.cjs`
         `tests/test-127-03-acceptance-gates.sh`
         `tests/test-127-02-doctor-class-m.sh`
         `tests/test-127-00-shim-handshake.sh`
         `tests/fixtures/127-03-acceptance/no-identity-refusal/README.md`
         `docs/262-FLOOR-01-GAP-LEDGER.md`
         `docs/262-LIVE-MEASUREMENT-EVIDENCE.md`
         `docs/262-WORKORDER-brain-repo-floor-remediation.md`
         `docs/262-NOTE-theo-adaptation-list-additions.md`
         `tests/run-all-262.sh`
    5. Final summary line reads `Phase 262: PASS=$PASS FAIL=$FAIL SKIP=$SKIP`.

    Keep every load-bearing property unchanged: `set -uo pipefail`, the `cd "$ROOT"`,
    `shopt -s nullglob`, the self-exclusion guard comparing basenames, the `found -eq 0`
    guard (do NOT soften it), `run_may_skip` for `.sh` legs, and the fence's `rc >= 2`
    SCAN BROKE branch that FAILS rather than silently passing. The runner itself must
    carry no literal U+2014, which is why the fence uses the codepoint escape.

    Set the executable bit: `chmod +x tests/run-all-262.sh`.

    Do NOT add `tests/run-all-262.sh` to `tests/run-all-127.sh` or to any other
    aggregator; phase runners are invoked directly.
  </action>

  <verify>
    <automated>bash tests/run-all-262.sh && TEST_262_PREFIX=tests/test-262-nonexistent- bash tests/run-all-262.sh; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    - `bash tests/run-all-262.sh` exits 0 and prints `discovered 2 test file(s) under tests/test-262-*` at this point in the phase (Plan 01's two suites; later plans raise the count without editing this file).
    - `TEST_262_PREFIX=tests/test-262-nonexistent- bash tests/run-all-262.sh` exits non-zero and prints `!!! no tests/test-262-nonexistent-* files discovered`.
    - The final summary line matches `^Phase 262: PASS=[0-9]+ FAIL=0 SKIP=[0-9]+$`.
    - `grep -c 'found -eq 0' tests/run-all-262.sh` is 1 and the guard is not wrapped in any conditional that can skip it.
    - `test -x tests/run-all-262.sh` succeeds.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/run-all-262.sh` returns 0, and the `262 no-em-dash fence` leg reports PASSED.
  </acceptance_criteria>

  <done>Every later 262 plan has one command to prove its wave, and a harness that discovers nothing fails instead of printing green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files to test process | Committed JSON and command frontmatter read by the suites; all repo-local, all already tracked |
| (none to the Brain) | This plan makes zero network calls. `scanMethodologyCommands` and `parseOverrideFile` are pure disk readers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-01 | Tampering | `data/flagship-floor-set.json` narrowed to fake a green gate | mitigate | Task 1 Tests 2 and 3 pin `frameworks.length === 28` and `ratified_at === '2026-08-11'`; narrowing turns a silent smaller RATIFIED banner into a red test |
| T-262-02 | Repudiation | Floor numbers quoted without a population or a date | mitigate | D-04 dating rule enforced in both suite headers; acceptance criteria grep for `2026-09-02` |
| T-262-03 | Information disclosure | A test printing a Brain key value | mitigate | No key is resolved anywhere in this plan; `resolveBrainKey` is not imported (ASVS V2) |
| T-262-04 | Elevation of privilege | A test reaching for an admin key or an admin tool | accept (structurally impossible here) | Zero network, zero `brainCall`, zero `resolveBrainKey`. `brain_write` / `ingest_framework` stay ABSENT; this phase opens no admin window (ASVS V4) |
| T-262-05 | Information disclosure | Canon Part 8 breach via test fixtures | accept | Nothing crosses the wire. The projection carries only generic machinery metadata (BOG-10), asserted by Task 2 Test 5 |
| T-262-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs this phase (262-RESEARCH.md "Package Legitimacy Audit": not applicable, every dependency is first-party). An `npm install` line appearing during execution is an immediate stop, not a judgement call |
</threat_model>

<verification>
```bash
node --test tests/test-262-floor-denominator.cjs
node --test tests/test-262-sep-projection-probe.cjs
bash tests/run-all-262.sh
TEST_262_PREFIX=tests/test-262-nonexistent- bash tests/run-all-262.sh; echo "guard EXIT=$? (must be non-zero)"
git diff --stat data/flagship-floor-set.json data/brain-orchestration-projection.json   # must be empty
```
</verification>

<success_criteria>
- Both new `.cjs` suites pass under `node --test` with zero network calls.
- `bash tests/run-all-262.sh` exits 0; the nonexistent-prefix run exits non-zero.
- `data/flagship-floor-set.json` and `data/brain-orchestration-projection.json` are unmodified.
- The `<SEP>` projection finding and its honest scope limit are written into the Task 2 header, dated 2026-09-02, ready for Plan 262-05's gap ledger to cite.
- No em-dashes in any file this plan creates.
</success_criteria>

<output>
Create `.planning/phases/262-floor-green-sweep-02-inversion/262-01-SUMMARY.md` when done.
Record the measured D-07 answer (occurrence count and framework-node count) verbatim, so
Plan 262-05 cites a measurement rather than re-deriving it.
</output>
