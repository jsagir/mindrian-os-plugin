---
phase: 274-bare-scripts-invocation-anchoring
plan: 04
type: execute
wave: 1
depends_on: ["274-01"]
files_modified:
  - skills/conversation-mode/SKILL.md
  - skills/mva-pipeline/SKILL.md
  - skills/room-passive/SKILL.md
  - agents/analogy-query-fetcher.md
  - scripts/check-plugin-path-anchoring.cjs
autonomous: true
requirements: [ANCHOR-03, ANCHOR-04, ANCHOR-06]
canon_parts: [Part 7, Part 11]
must_haves:
  truths:
    - "All 3 hand-authored skill sites use the fail-closed long form (a foreign Agent-Skills host with no plugin root set refuses loudly instead of resolving to a wrong path)."
    - "The 1 agent site uses the short form (agents only ever run inside Claude Code, which always injects CLAUDE_PLUGIN_ROOT)."
    - "The 6 deliberate ./scripts/ cwd-relative fallback lines in help.md/eureka.md are declared exceptions with a written reason, not silently anchored or silently left as unremarked violations."
    - "The commands/status.md permission-matcher/body drift is registered as a named followup with evidence, not guessed at."
  artifacts:
    - path: "scripts/check-plugin-path-anchoring.cjs"
      provides: "SCRIPT_ALLOWLIST populated with the help.md/eureka.md fallback exceptions; REGISTERED_FOLLOWUPS gains FOLLOWUP-274-R1 and FOLLOWUP-274-R2"
      contains: "FOLLOWUP-274-R1"
  key_links:
    - from: "scripts/check-plugin-path-anchoring.cjs SCRIPT_ALLOWLIST"
      to: "commands/help.md, commands/eureka.md"
      via: "file + pattern substring match, mirroring the existing citation ALLOWLIST mechanism"
      pattern: "file: 'commands/help\\.md'"
    - from: "skills/room-passive/SKILL.md:96"
      to: "scripts/wikilink-file.cjs"
      via: "the fail-closed long form"
      pattern: "MINDRIAN_OS_ROOT:-\\$\\{CLAUDE_PLUGIN_ROOT:\\?"
---

<objective>
Anchor the 4 sites the mirror generator cannot reach (3 hand-authored skills with the fail-closed
long form, 1 agent with the short form), and give the phase's deliberate exceptions a reasoned,
declared home in `SCRIPT_ALLOWLIST` and `REGISTERED_FOLLOWUPS` rather than leaving them as
unremarked, silently-tolerated matches once 274-01's widened predicate starts seeing them.

This plan is where the phase's three genuine judgment calls land, each pre-researched with a
recommended disposition -- apply the recommendation, do not re-litigate from scratch:

1. **`skills/conversation-mode/SKILL.md:17`** -- a dev-gate mention (`node
   scripts/build-connector-registry.cjs --check`) inside a `#`-comment YAML block, not an
   instruction Claude executes. Recommended disposition (a): anchor it anyway, uniformly, with
   the long form. Nothing executes a comment, and an anchored path inside a comment is still a
   *correct* path -- uniformity is cheaper than carving out an exception for zero benefit.
2. **The 6 deliberate `./scripts/...` cwd-relative fallback lines** in `commands/help.md`
   (lines 77, 87) and `commands/eureka.md` (line 76), plus their 3 generated mirrors (which the
   scanner structurally excludes -- do not edit the mirrors directly). These document behaviour
   for the `CLAUDE_PLUGIN_ROOT`-unset case and predate the `:?` fail-closed form. Disposition:
   **do NOT anchor them and do NOT change the documented fallback behaviour.** Add reasoned
   `SCRIPT_ALLOWLIST` entries (one per file) and register the supersession question --  should
   the newer `:?` form replace this prose-fallback convention -- as `FOLLOWUP-274-R2`, owned by
   the repo navigator.
3. **`commands/status.md:15` vs `:67` matcher/body drift** -- a pre-existing, already-live
   disagreement (the `allowed-tools` permission matcher still reads the bare `Bash(node
   scripts/mos-status.cjs:*)` form while the body already executes the anchored form). **Do NOT
   guess at a fix.** Whether an anchored `${...}`-bearing matcher pattern even matches correctly
   in Claude Code's matcher engine is unconfirmed, and this phase's own scope reminder is explicit:
   register it as `FOLLOWUP-274-R1` with the evidence, do not attempt a fix. This costs zero code
   changes to `commands/status.md` itself in this plan.

Purpose: close every remaining site the mirror-generated command sweep (274-02/03) cannot reach,
and turn every deliberate non-fix into a declared, owned exception instead of a silent gap.
Output: 4 markdown sites anchored; 2 `SCRIPT_ALLOWLIST` entries; 2 new `REGISTERED_FOLLOWUPS`
entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-RESEARCH.md
@.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anchor the 3 hand-authored skills (long form) and the 1 agent site (short form)</name>
  <files>skills/conversation-mode/SKILL.md, skills/mva-pipeline/SKILL.md, skills/room-passive/SKILL.md, agents/analogy-query-fetcher.md</files>
  <read_first>
    - `skills/export/SKILL.md:80` -- `bash "${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?MindrianOS
      install root not found. Set MINDRIAN_OS_ROOT (see lib/core/active-plugin-root.cjs) or run
      from Claude Code.}}/scripts/generate-standalone" ./room` -- the byte-identical message
      string already shipping in this repo for this exact token type. Copy the message
      verbatim, do not paraphrase (274-RESEARCH.md Pattern 2's explicit instruction).
    - `skills/conversation-mode/SKILL.md:10-20` -- the exact `#`-comment YAML block containing
      the dev-gate mention at line 17.
    - `skills/mva-pipeline/SKILL.md:50-53` -- "1. Run `/mos:mva-brief` (or invoke `node
      scripts/mva-run.cjs` via Bash directly)."
    - `skills/room-passive/SKILL.md:94-99` -- the bash code block starting `node
      scripts/wikilink-file.cjs "$ROOM_DIR" "$NEW_FILE_PATH" \`.
    - `agents/analogy-query-fetcher.md:42-45` -- "produced by `node
      scripts/analogy-fitness-report.cjs compose-queries <pattern.json>`."
    - `274-RESEARCH.md` Pattern 2 and Code Example 2 in full.
  </read_first>
  <action>
    Anchor the 3 hand-authored skill sites with the fail-closed long form, using the EXACT message
    string from `skills/export/SKILL.md:80` verbatim (do not invent a new message):
    - `skills/conversation-mode/SKILL.md:17` -- `node scripts/build-connector-registry.cjs
      --check` -> `node
      "${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?MindrianOS install root not found. Set
      MINDRIAN_OS_ROOT (see lib/core/active-plugin-root.cjs) or run from Claude Code.}}/scripts/build-connector-registry.cjs"
      --check`, applied inline inside the existing `#`-comment (keep the `#` prefix and the rest
      of the comment prose unchanged -- this is disposition (a) from the objective: anchor
      uniformly, do not carve an exception for a comment).
    - `skills/mva-pipeline/SKILL.md:52` -- `node scripts/mva-run.cjs` -> `node
      "${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?MindrianOS install root not found. Set
      MINDRIAN_OS_ROOT (see lib/core/active-plugin-root.cjs) or run from Claude Code.}}/scripts/mva-run.cjs"`,
      preserving the surrounding "(or invoke ... via Bash directly)" prose unchanged.
    - `skills/room-passive/SKILL.md:96` -- `node scripts/wikilink-file.cjs` -> `node
      "${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?MindrianOS install root not found. Set
      MINDRIAN_OS_ROOT (see lib/core/active-plugin-root.cjs) or run from Claude Code.}}/scripts/wikilink-file.cjs"`,
      preserving the trailing `"$ROOM_DIR" "$NEW_FILE_PATH" \` and following lines unchanged.

    Anchor the 1 agent site with the short form (agents only ever run inside Claude Code, which
    always injects `CLAUDE_PLUGIN_ROOT` -- Pattern 1, not Pattern 2):
    - `agents/analogy-query-fetcher.md:43` -- `node scripts/analogy-fitness-report.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/analogy-fitness-report.cjs"`, preserving `compose-queries
      <pattern.json>` unchanged.
  </action>
  <verify>
    <automated>node scripts/check-plugin-path-anchoring.cjs --report --include-scripts 2>&amp;1 | grep -E "conversation-mode|mva-pipeline|room-passive|analogy-query-fetcher"</automated>
  </verify>
  <acceptance_criteria>
    - The live scan shows all 4 sites tagged `ANCHORED`.
    - `grep -c 'MINDRIAN_OS_ROOT:-\${CLAUDE_PLUGIN_ROOT:?MindrianOS install root not found'
      skills/conversation-mode/SKILL.md skills/mva-pipeline/SKILL.md skills/room-passive/SKILL.md`
      returns 1 for each of the 3 files, and the message text is byte-identical to
      `skills/export/SKILL.md:80`'s across all three.
    - `grep -c '\${CLAUDE_PLUGIN_ROOT}/scripts/analogy-fitness-report.cjs'
      agents/analogy-query-fetcher.md` returns at least 1, and the file contains NO fail-closed
      long-form string (agents use the short form only).
    - `git diff --numstat` on all 4 files shows added == removed (pure substitution).
  </acceptance_criteria>
  <done>The 3 hand-authored skill sites use the byte-identical fail-closed long form already
  shipping elsewhere in the repo; the 1 agent site uses the short form; no other content on any
  touched line changed.</done>
</task>

<task type="auto">
  <name>Task 2: Populate SCRIPT_ALLOWLIST and register the two followups</name>
  <files>scripts/check-plugin-path-anchoring.cjs</files>
  <read_first>
    - `scripts/check-plugin-path-anchoring.cjs` lines 113-172 (the existing citation `ALLOWLIST`
      entry for `/mos:radar` and the `REGISTERED_FOLLOWUPS` entry for `FOLLOWUP-271-R1`) -- the
      exact shape (`file`, `pattern`, `reason`, `ruling`, `followup` keys on the allowlist entry;
      `id`, `title`, `raisedBy`, `owner`, `residualRisk`, `whyDeferred`, `recordedIn` keys on the
      followup entry) this task's new entries must match.
    - `commands/help.md:70-90` and `commands/eureka.md:70-80` (already read in full this session)
      -- the exact fallback prose this task declares an exception for, without editing.
    - `274-RESEARCH.md` Pitfall 3 ("Deliberate cwd-relative fallbacks becoming false violations"),
      Pitfall 5 ("allowed-tools permission matchers drifting from anchored bodies"), and Open
      Questions 1 and 2 -- the exact recommendations this task implements.
  </read_first>
  <action>
    Add two entries to `SCRIPT_ALLOWLIST` (the array 274-01 shipped empty), each with a non-empty
    `reason` (required by the reused `validateAllowlist()`) and a `followup` id:
    - `{ file: 'commands/help.md', pattern: './scripts/help-renderer.cjs', ruling: 'allowlisted
      (274-04)', followup: 'FOLLOWUP-274-R2' }` -- covers both fallback lines (77 and 87), since
      a single `file`+`pattern` entry suppresses every matching token in that file (the same
      mechanism the `/mos:radar` citation entry already relies on). Reason: these are the
      DOCUMENTED fallback behaviour for the `CLAUDE_PLUGIN_ROOT`-unset case
      ("Fall back to `node ./scripts/help-renderer.cjs ...` if `CLAUDE_PLUGIN_ROOT` is unset"),
      not an authoring mistake; anchoring a fallback for an unset anchor is nonsensical, and the
      newer fail-closed `:?` form may supersede this convention entirely, which is a design
      question this plan defers rather than guesses at.
    - `{ file: 'commands/eureka.md', pattern: './scripts/resolve-room', ruling: 'allowlisted
      (274-04)', followup: 'FOLLOWUP-274-R2' }` -- covers line 76's fallback, same reasoning.

    Add two entries to `REGISTERED_FOLLOWUPS`:
    - `FOLLOWUP-274-R1`: title "Confirm whether an anchored (`${...}`-bearing) `allowed-tools`
      permission matcher pattern matches correctly in Claude Code's matcher engine, then either
      anchor `commands/status.md:15`'s matcher to match its already-anchored body at line 67, or
      drop the specific matcher for a bare `Bash` grant." `raisedBy`: "plan 274-04 (Pitfall 5, the
      pre-existing matcher/body drift measured live this phase)." `owner`: "repo navigator."
      `residualRisk`: "a user with a strict tool-approval policy sees an unnecessary permission
      prompt on `/mos:status` (degraded UX, not a functional failure -- `commands/status.md:67`
      already executes the anchored form correctly)." `whyDeferred`: "matcher-engine `${...}`
      pattern semantics are unconfirmed and this phase's own scope explicitly forbids guessing at
      them; verified zero-blast-radius for the rest of this phase's sweep (only `status.md` and
      its mirror carry a `Bash(...scripts/...)` matcher in the whole tree)." `recordedIn`: this
      plan's own summary path once it exists (fill in at execute time, or reference
      `274-RESEARCH.md` Pitfall 5 if the summary path is not yet known when this line is written).
    - `FOLLOWUP-274-R2`: title "Decide whether the `${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?...}}`
      fail-closed form supersedes the older cwd-relative `./scripts/...` prose-fallback
      convention in `commands/help.md` and `commands/eureka.md`, repo-wide." `raisedBy`: "plan
      274-04 (Open Question 1)." `owner`: "repo navigator." `residualRisk`: "if
      `CLAUDE_PLUGIN_ROOT` is genuinely unset on a real surface, `help`/`eureka` fall back to a
      cwd-relative path that only resolves by coincidence, the same class of failure this whole
      phase exists to close, just deliberately scoped out of the sweep for these 2 files pending
      this ruling." `whyDeferred`: "changing documented fallback behaviour is a design decision,
      not a mechanical anchoring fix, and is out of scope for a phase whose job is anchoring, not
      redesigning fallback semantics." `recordedIn`: same as above.

    Add a short comment near `PERMISSION_MATCHER_RE` (or the `ALLOWLIST` block) cross-referencing
    `FOLLOWUP-274-R1` so a future reader who greps for the drift finds the followup, without
    changing `commands/status.md` itself in this plan.
  </action>
  <verify>
    <automated>node -e "const g=require('./scripts/check-plugin-path-anchoring.cjs'); console.log(g.SCRIPT_ALLOWLIST.length, g.REGISTERED_FOLLOWUPS.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-plugin-path-anchoring.cjs --check-scripts` (module load succeeds, no
      thrown error from `validateAllowlist`) still runs to completion.
    - `SCRIPT_ALLOWLIST.length === 2` and both entries carry a non-empty `reason` and a `followup`
      id that resolves in `REGISTERED_FOLLOWUPS`.
    - `REGISTERED_FOLLOWUPS` contains exactly one entry with `id === 'FOLLOWUP-274-R1'` and one
      with `id === 'FOLLOWUP-274-R2'`, each with a non-empty `owner` and `residualRisk`.
    - The live scan (`--report --include-scripts`) shows `commands/help.md` and
      `commands/eureka.md`'s `./scripts/...` fallback lines tagged `ALLOWLISTED`, not `VIOLATION`.
    - `git diff --numstat -- commands/help.md commands/eureka.md commands/status.md` shows ZERO
      lines changed in all three files -- this task edits ONLY
      `scripts/check-plugin-path-anchoring.cjs`.
  </acceptance_criteria>
  <done>SCRIPT_ALLOWLIST carries the two reasoned fallback exceptions, both registered followups
  exist with named owners and residual risk, help.md/eureka.md/status.md bodies are untouched, and
  the module still loads (validateAllowlist accepts both new entries).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a foreign Agent-Skills host with no plugin root set -> a hand-authored SKILL.md's script invocation | Without the fail-closed form, an unset `CLAUDE_PLUGIN_ROOT` silently resolves to `/scripts/<name>` and dies with a confusing module error instead of naming the fix |
| `SCRIPT_ALLOWLIST` exception -> silent suppression | An exception without a written reason or a resolvable followup id would silence the gate on the two allowlisted files with no trace |
| `commands/status.md`'s matcher/body drift | An unconfirmed guess at matcher-engine semantics could silently widen or narrow a permission grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-274-06 | Elevation of Privilege / Tampering | The 3 hand-authored skill sites and 1 agent site | mitigate | Anchored with the byte-identical fail-closed long form (skills) / short form (agent) already proven elsewhere in this repo, closing the same local code-execution shadowing vector for the surfaces the mirror generator cannot reach. |
| T-274-07 | Repudiation | `commands/status.md:15`'s matcher/body drift | accept (registered, not fixed) | Fixing it requires confirming matcher-engine `${...}` semantics, which this plan explicitly declines to guess at per the phase's own scope reminder; `FOLLOWUP-274-R1` names the evidence, the exact residual risk (an extra permission prompt, not a functional failure), and an owner, so the gap is owned rather than silently reintroduced. |

No package-manager installs occur in this plan, so no `T-274-SC` supply-chain row applies.
</threat_model>

<verification>
1. `node scripts/check-plugin-path-anchoring.cjs --report --include-scripts` shows all 4 sites in
   Task 1 as `ANCHORED` and the help.md/eureka.md fallback sites as `ALLOWLISTED`.
2. `node -e "require('./scripts/check-plugin-path-anchoring.cjs')"` does not throw.
3. `git diff --numstat -- commands/help.md commands/eureka.md commands/status.md` is empty.
</verification>

<success_criteria>
- Every site this phase deliberately does NOT anchor carries a written reason and a named owner,
  never a silent skip.
- The mirror generator's blind spot (hand-authored skills, agents) is closed with the correct
  form per surface.
- No guess was made at unconfirmed matcher-engine semantics.
</success_criteria>

<output>
Create `.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-04-SUMMARY.md` when done.
</output>
