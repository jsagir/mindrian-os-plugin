---
phase: 274-bare-scripts-invocation-anchoring
plan: 03
type: execute
wave: 1
depends_on: ["274-01"]
files_modified:
  - commands/mva-brief.md
  - commands/new-surface.md
  - commands/publish.md
  - commands/room.md
  - commands/skill.md
  - commands/snapshot.md
  - commands/vault.md
autonomous: true
requirements: [ANCHOR-02]
canon_parts: [Part 7]
must_haves:
  truths:
    - "Every bare scripts/<name> invocation in these 7 command files now resolves against the plugin install directory, not the invoking session's cwd."
    - "No line's arguments, quoting of pre-existing variables, or surrounding prose changed except the anchored path prefix itself."
  artifacts:
    - path: "commands/vault.md"
      provides: "All 4 vault-import.cjs invocation sites (149, 166, 190, 286) anchored"
      contains: "${CLAUDE_PLUGIN_ROOT}/scripts/vault-import.cjs"
    - path: "commands/snapshot.md"
      provides: "All 4 sites (generate-hub.cjs, generate-lobby.cjs x2, generate-snapshot.cjs) anchored"
      contains: "${CLAUDE_PLUGIN_ROOT}/scripts/generate-snapshot.cjs"
  key_links:
    - from: "commands/skill.md"
      to: "scripts/build-new-surface.cjs"
      via: "node ${CLAUDE_PLUGIN_ROOT}/scripts/build-new-surface.cjs"
      pattern: "node .\\{CLAUDE_PLUGIN_ROOT\\}/scripts/build-new-surface\\.cjs"
---

<objective>
Anchor every bare `scripts/<name>` invocation site in the second half of the affected command
files (by site count, roughly balanced against 274-02) with the quoted short form
`"${CLAUDE_PLUGIN_ROOT}/scripts/<name>"`, per ANCHOR-02 and Pattern 1. Same substitution rule as
274-02, applied to a disjoint file set, so this plan runs in parallel with 274-02 and 274-04 with
zero file-ownership conflicts.

**Why anchoring is safe here with no per-site write/read disposition needed (Pitfall 6, do not
re-litigate):** this phase anchors EXECUTABLE paths, not data paths a command writes to.
`node /abs/path/x.cjs ./room` leaves the process's cwd untouched, so every relative argument on
these lines still resolves against the user's own room exactly as before. All target scripts
resolve their own internals from `__dirname`, never `process.cwd()`.

**Re-verify before editing.** Re-run `node scripts/check-plugin-path-anchoring.cjs --report
--include-scripts` first. The file:line list below was live-measured 2026-09-01; reconcile
against the live output if it disagrees, and note any discrepancy in the summary.

Purpose: close the runtime-error-causing half of Phase 271's sibling defect for the second batch
of files (vault import, snapshot/publish generation, new-surface/skill scaffolding, mva-brief).
Output: 7 command files, ~16 sites, anchored.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anchor mva-brief.md, new-surface.md, publish.md, room.md</name>
  <files>commands/mva-brief.md, commands/new-surface.md, commands/publish.md, commands/room.md</files>
  <read_first>
    - `commands/mva-pipeline/SKILL.md:52`'s already-verified line as a style cross-check ("1. Run
      `/mos:mva-brief` (or invoke `node scripts/mva-run.cjs` via Bash directly)") -- do NOT edit
      this file in this task (it belongs to plan 274-04, hand-authored skills); it is cited here
      only so the same `mva-run.cjs` script name is recognizable when editing
      `commands/mva-brief.md:62`.
    - `commands/new-surface.md:89,104` and `commands/skill.md:82,95` (the latter belongs to Task
      2 below, both cite `scripts/build-new-surface.cjs` -- read both files' surrounding context
      so the two distinct call sites per file are not conflated).
    - `commands/publish.md:78,211` -- two `node scripts/generate-presentation.cjs` sites.
    - `commands/room.md:355` -- one `node scripts/generate-export.cjs` site.
  </read_first>
  <action>
    Anchor all 6 sites with the quoted short-form substitution (`${CLAUDE_PLUGIN_ROOT}/` prefix,
    whole path double-quoted, nothing else on each line changes):
    - `commands/mva-brief.md:62` -- `node scripts/mva-run.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/mva-run.cjs"`.
    - `commands/new-surface.md:89`, `:104` -- `node scripts/build-new-surface.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/build-new-surface.cjs"` on both lines, preserving each line's
      own distinct trailing arguments.
    - `commands/publish.md:78`, `:211` -- `node scripts/generate-presentation.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/generate-presentation.cjs"` on both lines.
    - `commands/room.md:355` -- `node scripts/generate-export.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/generate-export.cjs"`.
    Edit each site individually with its surrounding line visible; do not blind-sed the file.
  </action>
  <verify>
    <automated>node scripts/check-plugin-path-anchoring.cjs --report --include-scripts 2>&amp;1 | grep -E "mva-brief\.md|new-surface\.md|publish\.md|room\.md"</automated>
  </verify>
  <acceptance_criteria>
    - The live scan shows all 6 sites across these 4 files tagged `ANCHORED`, zero remaining bare
      matches for `mva-brief.md`, `new-surface.md`, `publish.md`, `room.md`.
    - `grep -c '\${CLAUDE_PLUGIN_ROOT}/scripts/build-new-surface.cjs' commands/new-surface.md`
      returns exactly 2.
    - `git diff --numstat -- commands/mva-brief.md commands/new-surface.md commands/publish.md
      commands/room.md` shows added == removed on each file.
  </acceptance_criteria>
  <done>All 6 sites across mva-brief.md, new-surface.md, publish.md, and room.md resolve against
  the plugin install directory; nothing else on any touched line changed.</done>
</task>

<task type="auto">
  <name>Task 2: Anchor skill.md, snapshot.md, vault.md</name>
  <files>commands/skill.md, commands/snapshot.md, commands/vault.md</files>
  <read_first>
    - `commands/skill.md:82,95` -- two `node scripts/build-new-surface.cjs` sites (same script as
      Task 1's new-surface.md sites, different command file and different trailing arguments --
      read each line before editing, do not assume identical args).
    - `commands/snapshot.md:96,97,100,177` -- one `node scripts/generate-hub.cjs`, two `node
      scripts/generate-lobby.cjs`, and one `node scripts/generate-snapshot.cjs`. Four distinct
      lines, two distinct script names repeated -- read all four lines in context before editing
      to avoid mixing up which line gets which anchor.
    - `commands/vault.md:149,166,190,286` -- four `node scripts/vault-import.cjs` sites at
      different points in the command's flow (likely different subcommand branches -- read each
      line's surrounding context, the trailing arguments differ per call site).
  </read_first>
  <action>
    Anchor all 10 sites with the same quoted short-form substitution as Task 1:
    - `commands/skill.md:82`, `:95` -- `node scripts/build-new-surface.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/build-new-surface.cjs"` on both lines.
    - `commands/snapshot.md:96` -- `node scripts/generate-hub.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/generate-hub.cjs"`.
    - `commands/snapshot.md:97`, `:100` -- `node scripts/generate-lobby.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/generate-lobby.cjs"` on both lines.
    - `commands/snapshot.md:177` -- `node scripts/generate-snapshot.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/generate-snapshot.cjs"`.
    - `commands/vault.md:149`, `:166`, `:190`, `:286` -- `node scripts/vault-import.cjs` -> `node
      "${CLAUDE_PLUGIN_ROOT}/scripts/vault-import.cjs"` on all four lines, preserving each line's
      own distinct trailing arguments.
    Edit each site individually with its surrounding line visible; do not blind-sed the file.
  </action>
  <verify>
    <automated>node scripts/check-plugin-path-anchoring.cjs --report --include-scripts 2>&amp;1 | grep -E "skill\.md|snapshot\.md|vault\.md"</automated>
  </verify>
  <acceptance_criteria>
    - The live scan shows all 10 sites across these 3 files tagged `ANCHORED`, zero remaining bare
      matches for `skill.md`, `snapshot.md`, `vault.md`.
    - `grep -c '\${CLAUDE_PLUGIN_ROOT}/scripts/vault-import.cjs' commands/vault.md` returns exactly
      4.
    - `git diff --numstat -- commands/skill.md commands/snapshot.md commands/vault.md` shows added
      == removed on each file.
  </acceptance_criteria>
  <done>All 10 sites across skill.md, snapshot.md, and vault.md resolve against the plugin install
  directory; nothing else on any touched line changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user session cwd -> the 16 sites this plan anchors | Each site, unfixed, would execute whatever file sits at that relative path in the invoking user's own Data Room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-274-05 | Elevation of Privilege / Tampering | The 16 sites in mva-brief.md, new-surface.md, publish.md, room.md, skill.md, snapshot.md, vault.md | mitigate | Anchored to `${CLAUDE_PLUGIN_ROOT}` in this plan, closing the local code-execution shadowing vector named in 274-01's threat model for this specific batch of files. |

No package-manager installs occur in this plan, so no `T-274-SC` supply-chain row applies.
</threat_model>

<verification>
1. `node scripts/check-plugin-path-anchoring.cjs --report --include-scripts` shows all 16 sites in
   these 7 files as `ANCHORED`.
2. `git diff --numstat` on all 7 files shows added == removed (pure substitution).
</verification>

<success_criteria>
- Zero unanchored `scripts/` invocation sites remain in these 7 command files.
- No collateral change to arguments, quoting of pre-existing variables, or prose.
- Combined with 274-02, all 31 command-surface sites (the widened-predicate count) are anchored.
</success_criteria>

<output>
Create `.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-03-SUMMARY.md` when done.
</output>
