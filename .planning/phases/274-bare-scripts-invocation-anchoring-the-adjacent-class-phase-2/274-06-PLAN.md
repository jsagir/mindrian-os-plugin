---
phase: 274-bare-scripts-invocation-anchoring
plan: 06
type: execute
wave: 3
depends_on: ["274-05"]
files_modified:
  - scripts/verify-release
  - CHANGELOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/debug/knowledge-base.md
  - .planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-VALIDATION.md
autonomous: true
requirements: [ANCHOR-09, ANCHOR-10]
canon_parts: [Part 7, Part 11]
must_haves:
  truths:
    - "A newly authored command invoking a bare scripts/<name> fails the release gate before it can ship, matching the exact treatment gate 10c already gives the citation class."
    - "The fix is recorded in CHANGELOG so the next release picks it up, since a dev-repo commit alone is not live."
    - "ANCHOR-01 through ANCHOR-10 are registered in .planning/REQUIREMENTS.md, and FOLLOWUP-274-R1/R2 are registered in ROADMAP with named owners."
    - "The Tri-Polar Desktop/Cowork runtime gap (D-02) is stated explicitly as a deliberate, named call, not implied to be fully proven."
    - "The reasoning trail exists in both the phase directory and the rethinking-mindrianos room, cross-linked in both directions."
  artifacts:
    - path: "scripts/verify-release"
      provides: "Gate 10f, the script-invocation anchoring gate, wired fail-closed, zero-tolerance"
      contains: "10f."
    - path: "CHANGELOG.md"
      provides: "Unreleased entry naming the sweep, the gate, and the local code-execution shadowing security framing"
      contains: "CLAUDE_PLUGIN_ROOT"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ANCHOR-01..10 registered as a new Phase 274 requirements section"
      contains: "ANCHOR-01"
  key_links:
    - from: "scripts/verify-release gate 10f"
      to: "scripts/check-plugin-path-anchoring.cjs --check-scripts"
      via: "node invocation, fail-closed"
      pattern: "check-plugin-path-anchoring.cjs.{0,20}--check-scripts"
    - from: ".planning/ROADMAP.md Phase 274 entry"
      to: "FOLLOWUP-274-R1, FOLLOWUP-274-R2"
      via: "named cross-reference with owner"
      pattern: "FOLLOWUP-274-R"
---

<objective>
Sequencing is the whole point of this plan being its own last wave (Pitfall 7 / DEVIATION-271-05-A
precedent). Gate 10f only gets wired here, after 274-05 already proved the sweep is completely
green. Writing this plan's acceptance criterion as "the gate emits a PASS line" is only meetable
because the dependency chain guarantees it: 271-05's own equivalent criterion was written before
its dependency waves closed, proved unmeetable, and left verify-release red for days. Do not
repeat that mistake. If 274-05's own verification somehow did not actually reach green, stop and
fix that first rather than wiring a gate that will immediately go red.

Make the guard structural (gate 10f, matching gate 10c's exact fail-closed shape), register what
this phase measured but explicitly does not fix (FOLLOWUP-274-R1, FOLLOWUP-274-R2), and close the
paper trail. Two standing project rules bear directly here. First, confirmed repeatedly: a commit
on main is NOT live until a release ships and is actually picked up, so the CHANGELOG entry is
how this fix reaches a real user. Second, CLAUDE.md's Dev-Research Compositing mandate: every
phase touching MindrianOS's own architecture files its findings in BOTH the phase directory and
the rethinking-mindrianos room, cross-linked in both directions.

Purpose: make the class unable to reappear, register requirements formally, and be honest about
the Tri-Polar Desktop/Cowork gap this phase deliberately did not close.
Output: gate 10f wired, REQUIREMENTS.md/CHANGELOG/ROADMAP/knowledge-base updated, the compositing
trail landed (or honestly recorded as blocked).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-05-SUMMARY.md
@.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-RESEARCH.md
@CLAUDE.md
</context>

<assumptions>
- Shared working tree, and this plan touches five high-traffic shared files. scripts/verify-release,
  CHANGELOG.md, .planning/REQUIREMENTS.md, and .planning/ROADMAP.md are files other sessions
  actively edit (per the standing 2026-08-27 handoff's own warning). Re-run git status --short
  and git log --oneline -3 immediately before each edit; if any file is already modified by
  something else, read the pending change first and append rather than overwrite.
- Known phase.add heading bug. Edit .planning/ROADMAP.md directly with the Edit tool, not via any
  phase.add-style automation; verify the resulting heading still renders as "### Phase 274: ..."
  matching its siblings.
- No release cut. Do not run release.sh without an explicit go-ahead (standing do-not list). This
  plan writes the CHANGELOG entry under [Unreleased] and stops there.
- Next free gate id. 10f was grep-verified free as of 2026-09-01 (verify-release currently defines
  10, 10b, 10c, 10d, 10e, then jumps to 11). Re-verify at execute time; another in-flight phase
  could have claimed it.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Wire gate 10f and register ANCHOR-01..10 in REQUIREMENTS.md</name>
  <files>scripts/verify-release, .planning/REQUIREMENTS.md</files>
  <read_first>
    - scripts/verify-release lines 335-362 (gate 10c's full block, including its comment), the
      exact structural sibling to copy.
    - 274-RESEARCH.md Code Example 4, the gate 10f block already drafted and reasoned during
      research; use its shape as the starting point rather than inventing a new one.
    - .planning/REQUIREMENTS.md's Traceability section (end of file), the exact phase-local-ID-
      minting registration pattern (CHOKE-01..06, PYPORT-01..07 precedent) this task follows.
  </read_first>
  <action>
    Add ONE gate block to scripts/verify-release, placed immediately after the existing gate 10e
    block (before "11. SCRIPTS EXECUTABLE"), confirming 10f is still the next free id before
    writing it. Follow gate 10c's comment convention: state in one sentence what a reader six
    months from now needs (a bare scripts/name invocation resolves against the session's cwd, not
    the plugin install dir, and dies loudly, exit 127 or MODULE_NOT_FOUND, unlike the citation
    class's silent degradation), cite this phase's own directory by path, and state zero-
    tolerance/fail-closed explicitly (per D-01 and the Claude's Discretion ruling: no grandfather
    clause, since 274-05 already proved the tree is fully clean). Body invokes
    check-plugin-path-anchoring.cjs with the --check-scripts flag against $PLUGIN_ROOT, with a
    pass/fail branch matching gate 10c's own shape, and recovery text naming both anchor forms
    plus the mirror-regeneration step.

    Prove the gate actually fires rather than assuming it: write a throwaway fixture under
    os.tmpdir() containing a bare node scripts/x.cjs line, point runScan/scanScriptInvocations at
    it via a small one-off Node snippet (never by editing the live checker), confirm a violation
    is detected; then confirm the real gate run against the actual repo exits 0. Do not leave any
    broken file in the tree.

    Then register ANCHOR-01 through ANCHOR-10 in .planning/REQUIREMENTS.md: add a new
    "### Phase 274" subsection (matching the CHOKE/PYPORT house shape) listing all 10 IDs with a
    one-line description each (reuse 274-RESEARCH.md's phase_requirements table as the source
    text), mark each done since the phase is complete by the time this task runs, and update the
    Traceability section's running total and phase list to include ANCHOR-01..10 and Phase 274,
    following the exact sentence shape the CHOKE-01..06 / PYPORT-01..07 entries already use there.
  </action>
  <verify>
    <automated>grep -c "check-plugin-path-anchoring" scripts/verify-release &amp;&amp; bash scripts/verify-release</automated>
  </verify>
  <acceptance_criteria>
    - grep for "check-plugin-path-anchoring.cjs" and "--check-scripts" together in
      scripts/verify-release finds at least one line pairing them (the new gate body).
    - grep -c "10f" scripts/verify-release returns at least 1.
    - bash scripts/verify-release runs to completion and its output contains a PASS line naming
      the new gate.
    - git diff --numstat -- scripts/verify-release shows 0 removed lines (pure addition).
    - .planning/REQUIREMENTS.md contains all ten literal strings ANCHOR-01 through ANCHOR-10.
  </acceptance_criteria>
  <done>Gate 10f runs inside scripts/verify-release, proven to fire against a fixture rather than
  assumed to, and ANCHOR-01..10 are registered in REQUIREMENTS.md's traceability record.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG, ROADMAP, knowledge-base, and the two followup registrations</name>
  <files>CHANGELOG.md, .planning/ROADMAP.md, .planning/debug/knowledge-base.md, .planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-VALIDATION.md</files>
  <read_first>
    - CHANGELOG.md lines 1-30, the current Unreleased entry, to match its heading level and its
      Added/Changed/Fixed subsection convention.
    - .planning/ROADMAP.md's Phase 274 entry (search for "### Phase 274"), including its current
      "Plans: 0 plans" line and "- [ ] TBD" placeholder.
    - .planning/debug/knowledge-base.md, the last two summary blocks, for the exact block shape
      this repo uses when closing a defect class.
    - 274-05-SUMMARY.md, for the live-reconciled final site count and any discrepancy it recorded.
  </read_first>
  <action>
    CHANGELOG.md: add a Fixed entry under the current Unreleased heading (create the heading only
    if none exists; invent no version number, do not run release.sh). Name the counts, the
    surfaces, and the security framing in the house Feynman register: the bare scripts/name
    invocation class across commands, hand-authored skills, and one agent is now anchored with
    CLAUDE_PLUGIN_ROOT (or the fail-closed MINDRIAN_OS_ROOT form for skills), so a Bash tool
    invocation resolves against the plugin install directory instead of the user's own cwd. Name
    the real security finding explicitly, not just "improves reliability": a bare invocation
    previously executed whatever file happened to exist at that relative path inside the user's
    own Data Room, a local code-execution shadowing vector, not only a portability bug. Use the
    live counts from 274-05-SUMMARY.md, not the plan-time estimates. No em-dashes.

    ROADMAP.md, Phase 274 entry: replace "Plans: 0 plans" with "Plans: 6/6 plans complete", and
    replace the "- [ ] TBD" placeholder with a wave-grouped checked list naming each of
    274-01 through 274-06 and its one-sentence contribution (instrument extension and Wave 0
    tests; the two parallel command-sweep batches; the skills/agent sweep plus allowlist and
    followup registration; mirror regeneration and full verification; gate wiring and close-out).
    Append a short outcome paragraph naming the two registered followups
    (FOLLOWUP-274-R1: the commands/status.md matcher/body drift, owner repo navigator;
    FOLLOWUP-274-R2: the help.md/eureka.md fallback-convention supersession question, owner repo
    navigator) and the Tri-Polar stated gap (D-02: Desktop/Cowork get static path-correctness
    checking only, no automated runtime execution proof, a deliberate call per this repo's own
    Tri-Polar Design Rule, not a silent omission).

    knowledge-base.md: add a summary block recording the generalizable pattern lesson this phase
    is the FOURTH pass at (intern-w1 fixed bash scripts/ in skills; the file-meeting RCA fixed
    references/ in one command; Phase 271 swept references/ repo-wide; this phase swept
    scripts/ repo-wide): a portability fix scoped by grep pattern instead of by resolution
    mechanism reliably leaves the sibling pattern untouched. Also record the three real defects
    this phase found and fixed IN the measuring instrument itself (the bash|node-only predicate
    missing python3, the raw match-counter with no anchored classification, the missing
    target-existence tag), since a gate that only measures what it was originally written to
    measure is itself a recurring failure shape worth a knowledge-base entry.

    274-VALIDATION.md: update its frontmatter (wave_0_complete: true, nyquist_compliant: true),
    check off every row in the Per-Task Verification Map and Wave 0 Requirements with the actual
    plan/task ids that satisfied them, and set Validation Sign-Off's Approval line to reflect
    phase completion.
  </action>
  <verify>
    <automated>grep -c "CLAUDE_PLUGIN_ROOT" CHANGELOG.md &amp;&amp; grep -c "6/6 plans complete" .planning/ROADMAP.md &amp;&amp; grep -c "FOLLOWUP-274-R1" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains a Fixed entry mentioning CLAUDE_PLUGIN_ROOT, under an unreleased
      heading, with no new version number invented, and the entry states the local
      code-execution-shadowing security framing explicitly (not just "improves reliability").
    - .planning/ROADMAP.md Phase 274 entry contains "6/6 plans complete" and all six
      274-0N-PLAN.md filenames.
    - .planning/ROADMAP.md contains both FOLLOWUP-274-R1 and FOLLOWUP-274-R2 with a named owner
      for each.
    - .planning/ROADMAP.md's Phase 274 entry states the D-02 Tri-Polar Desktop/Cowork gap
      explicitly (static path-correctness only, no automated runtime execution proof on those two
      surfaces).
    - .planning/debug/knowledge-base.md gained one summary block naming the fourth-pass pattern
      lesson and the three real defects fixed in the measuring instrument.
    - 274-VALIDATION.md's frontmatter reads nyquist_compliant: true and wave_0_complete: true.
    - `grep -v '^#' CHANGELOG.md | head -60 | grep -c $'\xe2\x80\x94'` returns 0 (no em-dash
      bytes in the new entry).
  </acceptance_criteria>
  <done>The changelog carries the Fixed entry with the security framing, ROADMAP records 6/6 plus
  both followups with named owners plus the stated Tri-Polar gap, the knowledge-base closes the
  pattern lesson honestly, and 274-VALIDATION.md reflects phase completion.</done>
</task>

<task type="auto">
  <name>Task 3: Dev-Research Compositing trail, cross-linked both directions</name>
  <files>.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-VALIDATION.md</files>
  <read_first>
    - CLAUDE.md, the "Dev-Research Compositing (Rethinking Room)" section, the mandate's exact
      terms: same finding, two homes, cross-linked, and neither substitutes for the other.
    - ~/MindrianRooms/rethinking-mindrianos/research/ (list its contents to find the most recent
      dated entry) for the house shape a room research entry should match.
    - 274-RESEARCH.md in full (already the phase's own authoritative reasoning trail) plus each of
      274-01 through 274-05's SUMMARY.md files, the material being mirrored.
    - 271-05-SUMMARY.md's own Task 3 record, since 271-05 already navigated the write-scope-check
      hook once; check whether the same stale-room-binding condition still applies before assuming
      this write will succeed.
  </read_first>
  <action>
    Create the durable reasoning trail at
    ~/MindrianRooms/rethinking-mindrianos/research/&lt;execution-date&gt;-bare-scripts-invocation-anchoring/
    (use the actual execution date, not 2026-09-01 if execution happens later). It carries the
    REASONING and EVIDENCE, not a copy of the executable plan: why a bare Bash-tool invocation of a
    plugin script is a local code-execution shadowing vector and not merely a portability bug, why
    this is the fourth pass at the same disease class in this repo (intern-w1's
    bash-scripts-in-skills fix, the file-meeting RCA's references-only fix, Phase 271's
    references-repo-wide sweep, and this phase's scripts-repo-wide sweep), and why scoping a
    portability fix by RESOLUTION MECHANISM rather than by the grep pattern that happened to
    surface it is the durable, generalizable answer. Name the three real defects found and fixed in
    the measuring instrument itself (missing python3/sh/npx verbs, no anchored classification, no
    target-existence tag) as evidence that even a purpose-built gate accretes its own blind spots
    and needs periodic re-audit, not just periodic re-use.

    Mirror the entry to mindrianOS/research/ as source-of-record per the mandate.

    Then close the loop in both directions: add a "Room mirror:" line, with the room path, to this
    phase's 274-VALIDATION.md (append a short closing section rather than editing its existing
    Wave 0 table); add a line in the room entry naming this phase directory path and the ROADMAP
    Phase 274 entry. Verify BOTH links resolve with test -e rather than assuming the paths are
    correct.

    If the write-scope-check PreToolUse hook blocks the room-side write (the exact 271-05
    precedent: the active room binding was stale, pointed at a different room than
    rethinking-mindrianos), do NOT bypass it. Record the honest, blocked state in this phase's
    summary and in 274-VALIDATION.md instead, naming the one navigator action that would unblock
    it (switching the active room binding), exactly as 271-05 did when it hit the same condition.
  </action>
  <verify>
    <automated>ls ~/MindrianRooms/rethinking-mindrianos/research/ 2>/dev/null | grep -i "bare-scripts\|script-invocation"; echo "room_search_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Either a dated research directory exists under
      ~/MindrianRooms/rethinking-mindrianos/research/ whose name contains bare-scripts or
      script-invocation (mirrored under mindrianOS/research/, both cross-links verified with
      test -e), OR the summary honestly records that the write-scope-check hook blocked the write
      and names the exact unblocking action, matching 271-05's own precedent for the identical
      situation.
    - If the room write succeeded: the room entry names intern-w1-rooms-skill-script-path.md,
      file-meeting-missing-reference-files.md, and the Phase 271 sweep, and states the
      scope-by-mechanism-not-by-string lesson explicitly as the fourth-pass finding.
    - 274-VALIDATION.md gained a closing section recording either the verified cross-links or the
      honest blocked state.
  </acceptance_criteria>
  <done>The reasoning trail exists in the room and its mirror with both cross-links verified, or
  the blocked state is honestly recorded with a named unblocking action, exactly as the 271-05
  precedent requires -- either way, nothing is silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-repo commit -> live user install | A fix that never reaches a release is invisible to every user, and a passing local gate creates false confidence that it shipped |
| concurrent session -> shared CHANGELOG / ROADMAP / verify-release / REQUIREMENTS.md | An overwrite silently destroys another session's in-flight entry |
| gate wired but never exercised | A gate added to verify-release that does not actually fire gives a false all-clear on every future release |
| a stated Tri-Polar gap read as a proof | D-02's Desktop/Cowork static-only scope, if not stated explicitly in the close-out record, could later be misread as full three-surface proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-274-09 | Spoofing | A verify-release gate that passes vacuously | mitigate | Task 1 requires proving gate 10f exits 1 against a temp fixture containing a bare invocation BEFORE accepting the green run on the real tree. |
| T-274-10 | Tampering | Concurrent-session edits to CHANGELOG.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, scripts/verify-release | mitigate | git status --short precondition per file in the plan's assumptions section; append rather than overwrite when a pending change exists. |
| T-274-11 | Repudiation | The Tri-Polar Desktop/Cowork gap silently read as proven rather than declared | mitigate | Task 2 states the gap explicitly in ROADMAP.md's Phase 274 entry and in 274-VALIDATION.md's Manual-Only Verifications table, per D-02's own instruction not to imply full three-surface proof. |
| T-274-12 | Repudiation | FOLLOWUP-274-R1 / FOLLOWUP-274-R2 dropped rather than owned | mitigate | Registered in ROADMAP.md with named owners (repo navigator) and the exact residual risk each defers, matching the FOLLOWUP-271-R1 precedent. |

No package-manager installs occur in this plan, so no T-274-SC supply-chain row applies.
</threat_model>

<verification>
1. grep -c "check-plugin-path-anchoring" scripts/verify-release returns at least 1, and
   bash scripts/verify-release completes with the new gate PASSing.
2. bash tests/run-all-274.sh still exits 0, all arms green after the gate-wiring change.
3. CHANGELOG.md carries the unreleased Fixed entry with the security framing; no version number
   was invented and release.sh was not run.
4. .planning/ROADMAP.md Phase 274 reads "6/6 plans complete" with both followups and the D-02 gap
   stated explicitly.
5. .planning/REQUIREMENTS.md contains ANCHOR-01 through ANCHOR-10.
6. Either both compositing cross-links resolve under test -e, or the blocked state is honestly
   recorded with a named unblocking action.
</verification>

<success_criteria>
- The defect class is blocked at release time, not just cleaned up once -- gate 10f matches gate
  10c's zero-tolerance, fail-closed treatment exactly.
- The fix has a CHANGELOG entry naming the real security consequence (local code-execution
  shadowing), which is the only thing that makes both the fix and its severity reachable by a real
  user or a future auditor.
- Both followups this phase deliberately did not fix have a name, an owner, and a residual-risk
  statement, so they are handed off rather than forgotten.
- The Tri-Polar Desktop/Cowork gap is a stated, deliberate call in the permanent record, not an
  implied and unverified claim of full three-surface proof.
- The reasoning trail lives in both homes with verified cross-links, or its blocked state is
  honestly recorded, per the compositing mandate.
</success_criteria>

<output>
Create `.planning/phases/274-bare-scripts-invocation-anchoring-the-adjacent-class-phase-2/274-06-SUMMARY.md` when done.
</output>
