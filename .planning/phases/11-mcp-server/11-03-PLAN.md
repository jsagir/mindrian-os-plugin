---
phase: 11-mcp-server
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - lib/parity/check-parity.cjs
autonomous: false
requirements: [CORE-03]

must_haves:
  truths:
    - "Every CLI command in commands/ has a corresponding MCP tool path"
    - "Parity check script exits 0 when all commands are mapped"
    - "Parity check script exits 1 if any CLI command is missing from MCP tools"
  artifacts:
    - path: "lib/parity/check-parity.cjs"
      provides: "CLI/MCP parity validation script"
      min_lines: 20
  key_links:
    - from: "lib/parity/check-parity.cjs"
      to: "commands/"
      via: "fs.readdirSync to list .md files"
      pattern: "readdirSync.*commands"
    - from: "lib/parity/check-parity.cjs"
      to: "lib/mcp/tool-router.cjs"
      via: "require() for ALL_TOOL_COMMANDS"
      pattern: "ALL_TOOL_COMMANDS"
---

<objective>
Create the CLI/MCP parity check script and verify the complete MCP server works end-to-end.

Purpose: CORE-03 requires that every CLI command has a corresponding MCP tool path. This script runs in CI to prevent parity drift. The checkpoint confirms the complete server is functional.

Output: Passing parity check + human-verified MCP server.
</objective>

<execution_context>
@/home/jsagi/.claude/get-shit-done/workflows/execute-plan.md
@/home/jsagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mcp-server/11-RESEARCH.md
@.planning/phases/11-mcp-server/11-01-SUMMARY.md
@.planning/phases/11-mcp-server/11-02-SUMMARY.md

<interfaces>
From lib/mcp/tool-router.cjs (created in Plan 01):
```javascript
// ALL_TOOL_COMMANDS: string[] -- flat array of all 41 command names
module.exports = { registerRouterTools, ALL_TOOL_COMMANDS };
```

CLI commands directory: commands/*.md (41 files, each filename minus .md = command name)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parity check script validating CLI commands match MCP tool commands</name>
  <files>lib/parity/check-parity.cjs</files>
  <action>
Create `lib/parity/check-parity.cjs`:

1. Read `commands/` directory, filter for `.md` files, strip extension to get command names
2. Import `ALL_TOOL_COMMANDS` from `../mcp/tool-router.cjs`
3. Compare:
   - `missingInMcp`: CLI commands not in ALL_TOOL_COMMANDS
   - `extraInMcp`: MCP commands not in CLI commands/ (warning only, not failure)
4. Output:
   - If `missingInMcp.length > 0`: print them to stderr and `process.exit(1)`
   - If `extraInMcp.length > 0`: print warning to stderr but do NOT fail
   - If all match: print `"Parity OK: {N} CLI commands, {M} MCP tool commands"` and exit 0
5. Handle edge cases:
   - commands/ directory not found: exit 1 with clear error
   - tool-router.cjs not found: exit 1 with clear error
6. Make it runnable standalone: `node lib/parity/check-parity.cjs`

Also add `"parity"` script to package.json if not already present (it should be from Plan 01, verify).
  </action>
  <verify>
    <automated>cd /home/jsagi/MindrianOS-Plugin && node lib/parity/check-parity.cjs</automated>
  </verify>
  <done>Parity check passes with 0 exit code, all 41 CLI commands have MCP tool paths, script is runnable as CI check</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete MCP server end-to-end</name>
  <action>Human verifies the complete MCP server works with all primitives registered.</action>
  <what-built>Complete MCP server with 6 hierarchical router tools (41 commands), 5 resources (room:// URI scheme), 5 prompts (methodology workflows with Larry personality), and parity validation script</what-built>
  <how-to-verify>
    1. Run parity check: `node lib/parity/check-parity.cjs` -- should print "Parity OK: 41 CLI commands, 41 MCP tool commands"
    2. Test MCP server starts: `echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"1.0"}}}' | timeout 5 node bin/mindrian-mcp-server.cjs`
    3. Verify tools/resources/prompts in response capabilities
    4. OPTIONAL (if Claude Desktop available): Add to claude_desktop_config.json:
       ```json
       {
         "mcpServers": {
           "mindrian-os": {
             "command": "node",
             "args": ["/home/jsagi/MindrianOS-Plugin/bin/mindrian-mcp-server.cjs"],
             "env": { "MINDRIAN_ROOM": "/path/to/your/room" }
           }
         }
       }
       ```
       Restart Claude Desktop and verify MindrianOS tools appear.
  </how-to-verify>
  <verify>Human confirms MCP server functions correctly</verify>
  <done>User approves MCP server with all tools, resources, and prompts working</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `node lib/parity/check-parity.cjs` exits 0
- MCP server responds to JSON-RPC initialize with tools, resources, and prompts capabilities
- No CLI command is orphaned (missing from MCP tool mapping)
</verification>

<success_criteria>
- Parity check script passes: all 41 CLI commands mapped to MCP tools
- Complete MCP server functional: tools + resources + prompts via stdio
- User confirms server works (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/11-mcp-server/11-03-SUMMARY.md`
</output>
