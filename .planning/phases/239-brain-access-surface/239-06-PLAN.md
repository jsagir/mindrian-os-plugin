---
phase: 239-brain-access-surface
plan: 06
type: execute
wave: 3
depends_on: ["239-05"]
files_modified:
  - lib/core/brain-client.cjs
  - lib/core/navigation/packet.cjs
  - docs/architecture/SUBSTRATE-CONTRACT.md
  - tests/test-150-brain-egress.cjs
  - tests/test-239-sendpacket-parked.cjs
autonomous: true
requirements: [BRAIN-03]

must_haves:
  truths:
    - "`sendPacket`'s fate is decided in writing, not left in silent limbo: it is PARKED, with a dated note carrying the census fact and the reason"
    - "The park note exists at the call surface AND in docs, so a maintainer meets it from either direction"
    - "The two contradictory in-repo claims about whether sendPacket has production consumers are reconciled to one true statement"
    - "The zero-production-caller census is machine-checked, not asserted, and it goes red if a caller appears"
  artifacts:
    - path: "lib/core/brain-client.cjs"
      provides: "Dated PARKED note at the sendPacket definition carrying the census fact and the ruling"
      contains: "PARKED"
    - path: "docs/architecture/SUBSTRATE-CONTRACT.md"
      provides: "The same park decision as an ADR amendment line"
      contains: "sendPacket"
    - path: "tests/test-239-sendpacket-parked.cjs"
      provides: "Census gate plus park-note presence assertions plus the contradiction-reconciled assertion"
      min_lines: 80
  key_links:
    - from: "tests/test-239-sendpacket-parked.cjs"
      to: "scripts/check-schema-aliases.cjs"
      via: "ALLOWED_SENDPACKET_FILES derived, not hand-copied"
      pattern: "check-schema-aliases"
    - from: "lib/core/navigation/packet.cjs"
      to: "lib/core/brain-client.cjs"
      via: "cross-reference from the H5 comment to the dated park note"
      pattern: "PARKED"
---

<objective>
Decide `sendPacket`'s fate explicitly and record it. BRAIN-03 is a decision, not a bug fix.

Purpose: a full census across `lib/`, `scripts/`, `bin/` and `pipelines/` finds ZERO production
`sendPacket(` call sites. The only definition is its own in `brain-client.cjs:1222`; the only
callers are its own test file, a lint guard, and comments. Meanwhile the repo says two opposite
things about this: `lib/core/navigation/packet.cjs:105` states plainly that "sendPacket has zero
production consumers today", while `tests/test-150-brain-egress.cjs:12` asserts "Phase 150 is
the FIRST real sendPacket consumer". Both cannot be true. A maintainer reading either one alone
gets a wrong answer, and the guarded-but-unreachable `sendPacket` PB8-10 belt gets mistaken for
live Part 8 coverage that it is not providing.

THE RULING, made by this unattended run and recorded openly in the Phase 238 precedent style
(RESEARCH.md assumption A3): PARK it. Wiring `sendPacket` to a real job is net-new feature work
inside a remediation-only milestone, which this milestone's own scope forbids. The census fact
is verified; the decision is the judgment call, and it is written down here rather than left
implicit.

Output: a dated park note at the call surface, the same decision as an ADR amendment in docs,
the two contradictory claims reconciled, and a machine-checked census that goes red if a
production caller ever appears.

CROSS-PHASE SCOPE FENCE: this plan touches zero files claimed by Phase 237 or Phase 238.
Neither `lib/mcp/tools/chain.cjs` nor any file under `lib/mcp/` appears in `files_modified`.
This plan shares `lib/core/brain-client.cjs` with sibling plan 239-05, which is why it runs in
Wave 3 and declares `depends_on: ["239-05"]`.

MUTATION SERIALIZATION FENCE (read before running any mutation leg). This repo's
`.planning/config.json` sets `workflow.use_worktrees: false`, so both Wave 3 plans run against
the SAME working tree. This plan mutates a scratch production file under `lib/core/` for its
census mutation leg (Task 3 LEG 6); its Wave 3 sibling 239-03 mutates `hooks/hooks.json` for its
stale-matcher leg. A mutation in flight in one plan can be observed by the other's regression
sweep. This is the exact coordination hazard recorded in `.planning/STATE.md`'s
Phase 242 entry.

Rules, mandatory for every mutation leg in this plan:
  1. Immediately before mutating, run `git status --porcelain` for the ENTIRE repo, not only
     your own paths. If any tracked file OUTSIDE this plan's `files_modified` is dirty, sibling
     239-03 has a mutation in flight: WAIT and retry a bounded number of times. Do NOT proceed
     and do NOT restore a sibling's file.
  2. Hold each mutation for the shortest possible window: mutate, run the single assertion,
     restore. Never leave a mutation in place across an unrelated command.
  3. Restore in a `finally`, then assert byte-identity against a backup held OUTSIDE the repo.
  4. Run any SHARED regression sweep as the LAST step of the plan, after every one of this
     plan's own mutations is restored.
  5. If a shared sweep goes red, check `git status --porcelain` FIRST, re-run once, and record
     in the SUMMARY whether the red reproduced. A red that does not reproduce is a
     sibling-collision artifact, not a finding, and must be labelled as such rather than chased.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/239-brain-access-surface/239-RESEARCH.md
@.planning/phases/239-brain-access-surface/239-PATTERNS.md
@.planning/phases/239-brain-access-surface/239-05-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 06 slice)

| Artifact | Kind | Status |
|----------|------|--------|
| dated PARKED note above `async function sendPacket` | new comment block in `lib/core/brain-client.cjs` | created here |
| `docs/architecture/SUBSTRATE-CONTRACT.md` amendment line | new doc content | created here |
| `lib/core/navigation/packet.cjs` H5 comment cross-reference | comment extended | changed here |
| `tests/test-150-brain-egress.cjs` header claim | corrected | changed here |
| `tests/test-239-sendpacket-parked.cjs` | new file (test) | created here |

<tasks>

<task type="auto">
  <name>Task 1: Write the dated park note at the call surface and reconcile the two contradictory claims</name>
  <files>lib/core/brain-client.cjs, lib/core/navigation/packet.cjs, tests/test-150-brain-egress.cjs</files>
  <read_first>
    - `lib/core/brain-client.cjs` lines 1200-1310. Line 1222 is `async function sendPacket(packet, opts)`;
      lines 1273-1298 are the PB8-10 classifier belt, which is correct code that no production
      path reaches. Also read lines 970-980 and 1160-1195, which carry the other in-code
      `sendPacket` prose (the "SOLE typed-packet wire path" claim at line 975 and the legacy
      migration warning at line 1187) so the park note does not contradict them.
    - `lib/core/navigation/packet.cjs` lines 98-112 IN FULL. This is the park-note PROSE ANALOG
      to clone: a dated, reasoned, finding-referenced in-code note ("Review finding H5: ...
      dormant only because sendPacket has zero production consumers today"). Its claim is already
      CORRECT; it needs a cross-reference, not a correction.
    - `tests/test-150-brain-egress.cjs` lines 1-25. Line 12's "Phase 150 is the FIRST real
      sendPacket consumer" is the FALSE claim, contradicted by the census and by packet.cjs:105.
    - `scripts/check-schema-aliases.cjs` lines 405-475 (`ALLOWED_SENDPACKET_FILES` and
      `checkSendpacket`) and `scripts/hooks/pre-commit` line 262, the D-08 layer-2 guard that
      already polices new `sendPacket(` call sites at commit time. The park note must reference
      this existing control rather than propose a new one (Canon Part 7).
    - `.planning/phases/239-brain-access-surface/239-RESEARCH.md` BRAIN-03 summary, assumption
      A3, and the "State of the Art" deprecated-in-repo list.
  </read_first>
  <action>
    Add a PARKED note as a comment block immediately above `async function sendPacket(packet, opts)`
    in `lib/core/brain-client.cjs`, cloning the prose idiom of `navigation/packet.cjs:100-110`
    (dated, reasoned, finding-referenced, states the plain fact rather than a euphemism).

    The note must carry, explicitly:
      - the date (the execution date, in ISO form)
      - the phase and requirement (Phase 239, BRAIN-03)
      - the CENSUS FACT, stated concretely: zero production call sites across `lib/`, `scripts/`,
        `bin/` and `pipelines/`; the only non-definition references are its own test file
        `tests/test-brain-packet-validation-per-job.cjs`, the D-08 lint guard in
        `scripts/check-schema-aliases.cjs`, and comments
      - the RULING and its REASON: parked, not wired, because wiring it to a real job is net-new
        feature work inside a remediation-only milestone
      - the CONSEQUENCE, stated plainly: the PB8-10 classifier belt inside this function is
        correct code on an unreached path, so it must NOT be counted as live Part 8 coverage.
        The live Part 8 coverage on the in-process door is the raw-field guard added by sibling
        plan 239-05 in `hatAwareRecommend` and `suggestValidationSteps`
      - the RE-OPEN CONDITION: the first real production consumer. Point at the existing D-08
        layer-2 pre-commit guard (`scripts/check-schema-aliases.cjs --check-sendpacket`) as the
        control that will surface one, and at `tests/test-239-sendpacket-parked.cjs` as the
        census that goes red when it happens

    In `lib/core/navigation/packet.cjs`, extend the existing H5 comment at line 105 with a single
    cross-reference clause naming the dated park note in `brain-client.cjs` and the ADR amendment
    in `docs/architecture/SUBSTRATE-CONTRACT.md`. Do NOT rewrite the H5 finding; its claim is
    already correct and it is the reason the contradiction was catchable. Comment lines only in
    this file, zero code change.

    In `tests/test-150-brain-egress.cjs`, correct the line-12 header claim. Do not delete the
    sentence, replace it: state that Phase 150's builder was designed as the first real
    `sendPacket` consumer but that the consumer never landed, that the census confirms zero
    production call sites, and that `sendPacket` was formally parked in Phase 239 with the note
    at the call surface. Header comment only; change no assertion in that file, and confirm it
    still passes.

    No em-dashes.
  </action>
  <acceptance_criteria>
    - `grep -c "PARKED" lib/core/brain-client.cjs` returns at least 1, and the match is within 30
      lines above `async function sendPacket(`. Prove the proximity with a `node -e` line-index
      check and transcribe both line numbers.
    - The park note contains an ISO date matching the execution date. Transcribe the note verbatim
      into the SUMMARY.
    - `grep -c "FIRST real sendPacket consumer" tests/test-150-brain-egress.cjs` returns 0.
    - `node tests/test-150-brain-egress.cjs` exits 0 with the SAME result as before this task
      (record before and after). If that file is environment-gated and SKIPs or fails
      pre-existing, record the pre-task baseline and show it is unchanged, and say so plainly
      rather than claiming a pass.
    - `git diff lib/core/navigation/packet.cjs` shows COMMENT LINES ONLY. Transcribe the diff.
    - `git diff lib/core/brain-client.cjs` shows COMMENT LINES ONLY in this task (no function body
      changed; 239-05 already landed its code changes and those must not be disturbed). Prove
      with `git diff` and confirm the 239-05 guard blocks are still present:
      `grep -c "part8-egress-guard" lib/core/brain-client.cjs` is unchanged from the value
      239-05-SUMMARY.md recorded.
    - `node tests/test-239-query-egress-canary.cjs` still exits 0 (239-05's work is unregressed).
    - `bash tests/run-all-196.sh` reports FAIL=0.
    - `grep -rlP '\x{2014}' lib/core/brain-client.cjs lib/core/navigation/packet.cjs tests/test-150-brain-egress.cjs` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-239-query-egress-canary.cjs && bash tests/run-all-196.sh</automated>
  </verify>
  <done>The park decision is written at the call surface with a date, a census fact and a re-open condition, and the repo now says one true thing about sendPacket instead of two opposite ones.</done>
</task>

<task type="auto">
  <name>Task 2: Record the same decision as an ADR amendment in docs/architecture/SUBSTRATE-CONTRACT.md</name>
  <files>docs/architecture/SUBSTRATE-CONTRACT.md</files>
  <read_first>
    - `docs/architecture/SUBSTRATE-CONTRACT.md` lines 60-120 IN FULL. It lists `buildBrainPacket`
      in the closed export surface and carries an explicit "Amendment rule": each future addition
      to this surface requires a new amendment line naming the export, the phase, and the
      consumer. Follow that rule's shape.
    - The park note authored in Task 1 (the two must not diverge).
    - A repo-wide check first: `grep -rln "sendPacket" docs/` currently returns NOTHING, meaning
      no doc anywhere records this. Confirm that before writing, and state the confirmation in
      the SUMMARY, because it is the reason this doc is the chosen home.
  </read_first>
  <action>
    Add an amendment entry to `docs/architecture/SUBSTRATE-CONTRACT.md` following that file's own
    Amendment rule shape. It records the Phase 239 BRAIN-03 ruling: `brain-client.sendPacket` is
    the sole typed-packet wire path, it has ZERO production consumers as of the execution date,
    and it is PARKED rather than wired.

    The entry must name: the export, the phase, and, in place of a consumer, the explicit
    statement that there is none and that this is the recorded decision. It must state the same
    consequence as the call-surface note (the PB8-10 belt inside `sendPacket` is correct code on
    an unreached path and must not be counted as live Part 8 coverage), point at
    `lib/core/brain-client.cjs`'s dated park note, and name the re-open condition.

    Place it where a reader of the closed-surface section will meet it, not appended at the end of
    the file where it would be missed. Match the file's existing heading level and prose voice.

    Do not modify the export list itself; `sendPacket` is not being added to or removed from any
    surface. This is a decision record, not a contract change.

    No em-dashes.
  </action>
  <acceptance_criteria>
    - `grep -c "sendPacket" docs/architecture/SUBSTRATE-CONTRACT.md` returns at least 1 (it
      returned 0 before; record both values).
    - The amendment names the phase (`239`), the requirement (`BRAIN-03`), an ISO date, and the
      word `PARKED`. Prove each with a grep and transcribe the added block verbatim.
    - The doc and the code note agree: transcribe both side by side in the SUMMARY and confirm
      the census fact, the ruling and the re-open condition are stated identically in substance.
    - `git diff --stat docs/architecture/SUBSTRATE-CONTRACT.md` shows an insertion only, zero
      deletions from the export list. Transcribe the stat.
    - `grep -cP '\x{2014}' docs/architecture/SUBSTRATE-CONTRACT.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "PARKED" docs/architecture/SUBSTRATE-CONTRACT.md && grep -q "BRAIN-03" docs/architecture/SUBSTRATE-CONTRACT.md && grep -q "sendPacket" docs/architecture/SUBSTRATE-CONTRACT.md && echo ok</automated>
  </verify>
  <done>The park decision exists in docs as well as in code, in the ADR's own amendment shape, so a maintainer meets it from either direction.</done>
</task>

<task type="auto">
  <name>Task 3: Author tests/test-239-sendpacket-parked.cjs, the machine-checked census</name>
  <files>tests/test-239-sendpacket-parked.cjs</files>
  <read_first>
    - `scripts/check-schema-aliases.cjs` lines 405-475. `ALLOWED_SENDPACKET_FILES` is the
      existing allowlist (`lib/core/brain-client.cjs`, `lib/core/navigation.cjs`,
      `lib/core/navigation/`, `tests/`, `scripts/`) and `checkSendpacket` is the existing
      staged-file guard. DERIVE the allowlist from this module; do NOT hand-copy the array, or
      the two will drift and the census will start lying.
    - `tests/run-all-196.sh`'s `noprivate_regex` grep-guard leg, the repo's idiom for a
      census-shaped assertion over tracked source.
    - `lib/core/mindrian-brain-shim.test.cjs` lines 165-185, which already runs a
      `sendPacket(`-count assertion for a different contract (Phase 110). Read it so this census
      complements rather than duplicates it, and say in the SUMMARY which is which.
    - The park note and the ADR amendment authored in Tasks 1 and 2.
  </read_first>
  <action>
    Create `tests/test-239-sendpacket-parked.cjs`, plain Node with `node:assert`.

    LEG 1, THE CENSUS: scan every `.cjs`, `.js` and `.mjs` file under `lib/`, `scripts/`, `bin/`
    and `pipelines/`, skipping `node_modules`, for a `sendPacket(` call site. Exclude, in this
    order and for these stated reasons: the definition line itself in
    `lib/core/brain-client.cjs`; any line that is a comment (`grep -vE '^[[:space:]]*(//|\*|/\*)'`
    equivalent, applied before counting, so a park note mentioning `sendPacket(` cannot
    self-invalidate the gate); and any path matched by the allowlist obtained from
    `scripts/check-schema-aliases.cjs`. Assert the remaining count is ZERO.

    LEG 2, ANTI-VACUITY for LEG 1: write a scratch file into a temp directory that DOES contain a
    bare `sendPacket(` call at a non-allowlisted-shaped path, run the same census function
    against a root that includes it, and assert it reports exactly 1. Without this, LEG 1 could
    pass because the scanner walks nothing. Clean up the temp file in a `finally`.

    LEG 3, park note at the call surface: assert `lib/core/brain-client.cjs` contains the word
    `PARKED` within 30 lines above the `async function sendPacket(` line, and that the note text
    contains an ISO-shaped date (`\d{4}-\d{2}-\d{2}`).

    LEG 4, park note in docs: assert `docs/architecture/SUBSTRATE-CONTRACT.md` contains
    `sendPacket`, `PARKED`, `BRAIN-03` and an ISO-shaped date.

    LEG 5, contradiction reconciled: assert `tests/test-150-brain-egress.cjs` does NOT contain
    the string `FIRST real sendPacket consumer`, and assert `lib/core/navigation/packet.cjs`
    still contains its correct `zero production consumers` claim (so the reconciliation went in
    the right direction: the false claim was corrected, the true one survived).

    LEG 6, MUTATION, performed by the executor and transcribed: append a bare
    `await require('./brain-client.cjs').sendPacket({origin:'x'});` line inside a scratch copy of
    a NON-allowlisted production file under `lib/core/` (choose one this phase does not otherwise
    touch), run `node tests/test-239-sendpacket-parked.cjs`, confirm NON-ZERO exit with LEG 1
    red and the offending path named, then restore and confirm `git diff --stat` EMPTY and the
    test green. This proves the census bites on a real new caller rather than only on a synthetic
    temp file.

    Print a per-leg PASS line. No em-dashes.
  </action>
  <acceptance_criteria>
    - `node tests/test-239-sendpacket-parked.cjs` exits 0, six legs.
    - `grep -c "ALLOWED_SENDPACKET_FILES\|check-schema-aliases" tests/test-239-sendpacket-parked.cjs`
      returns at least 1 (the allowlist is derived, not hand-copied). Confirm there is no
      literal re-declaration of the five allowlist patterns in the test file.
    - LEG 1 transcript: the exact list of files scanned (count) and the zero result.
    - LEG 2 transcript: the temp path seeded and the observed count of 1.
    - LEG 6 transcript: the file mutated, the observed non-zero exit with the offending path
      named, the post-restore `git diff --stat` showing EMPTY, and the test green again. No
      mutation may remain in the final diff.
    - The SUMMARY states how this census differs from `lib/core/mindrian-brain-shim.test.cjs`'s
      existing `sendPacket(` count assertion, so a reviewer does not read it as duplication
      (Canon Part 7).
    - `bash tests/run-all-239.sh` shows the sendpacket-parked leg flipped from SKIPPED to PASSED.
    - `grep -cP '\x{2014}' tests/test-239-sendpacket-parked.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-239-sendpacket-parked.cjs</automated>
  </verify>
  <done>The zero-production-caller fact is machine-checked with an anti-vacuity leg and a live mutation, and the park note's existence in both homes is asserted rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented claim to actual reachability | An in-repo statement about whether a wire path has consumers is an untrusted claim until censused; two opposite claims coexisted here |
| `sendPacket` PB8-10 belt to Part 8 coverage accounting | A guarded but unreached path can be mistaken for live coverage, which would overstate the boundary's actual enforcement |
| a future `sendPacket` caller to the Brain wire | Any new caller reopens a path whose guard has never run in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-T7 | Repudiation | contradictory in-repo claims about `sendPacket` | mitigate | `tests/test-150-brain-egress.cjs:12`'s false claim is corrected and `navigation/packet.cjs:105`'s true one survives and is cross-referenced. LEG 5 asserts both directions, so a future edit that re-introduces the false claim, or deletes the true one, turns red |
| T-239-06-A | Repudiation | unreached PB8-10 belt counted as live coverage | mitigate | Both the call-surface note and the ADR amendment state explicitly that the belt is correct code on an unreached path and must NOT be counted as live Part 8 coverage; the live in-process coverage is 239-05's raw-field guard. Acceptance criterion requires this sentence in both homes |
| T-239-06-B | Information Disclosure | a future production `sendPacket` caller | mitigate | LEG 1's census asserts zero non-allowlisted call sites and goes red when one appears, complementing the existing D-08 layer-2 pre-commit guard in `scripts/check-schema-aliases.cjs`. LEG 6 proves it bites on a real file, not only a synthetic one |
| T-239-06-C | Repudiation | a self-invalidating grep gate | mitigate | The census filters comment lines BEFORE counting, so the park note's own prose (which contains the string `sendPacket(`) cannot make the gate pass. LEG 2 is the anti-vacuity check that the scanner walks a real tree |
| T-239-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; the phase-level Package Legitimacy Audit records the phase as install-free |

T1, T2, T3 and T4 are owned by 239-02 through 239-05 and 239-07. This plan closes no boundary;
it records a decision and gates the fact the decision rests on.
</threat_model>

<verification>
1. `node tests/test-239-sendpacket-parked.cjs` exits 0, six legs.
2. `node tests/test-239-query-egress-canary.cjs` exits 0 (239-05 unregressed).
3. `node tests/test-150-brain-egress.cjs` result matches its pre-task baseline exactly.
4. `bash tests/run-all-196.sh` reports FAIL=0.
5. `bash tests/run-all-239.sh` shows the sendpacket-parked leg PASSED.
6. `git diff lib/core/navigation/packet.cjs` is comment-only; `git diff lib/core/brain-client.cjs` for THIS plan is comment-only.
7. `git diff --stat` at plan end shows no residual mutation.
8. `grep -rlP '\x{2014}'` over all five `files_modified` returns nothing.
</verification>

<success_criteria>
Covers ROADMAP Success Criterion 3 in full:
- `sendPacket`'s fate is decided explicitly and recorded. The branch taken is PARK: a dated note at the call surface AND in docs.
- A written decision exists, with its reason (net-new feature work inside a remediation-only milestone), its consequence (the PB8-10 belt is not live coverage) and its re-open condition.
- The decision is not merely prose: the census it rests on is machine-checked with an anti-vacuity leg and a live mutation.

RECORDED AS A NAVIGATOR-EQUIVALENT RULING: RESEARCH.md assumption A3 flags the park-versus-wire
choice as a judgment call, not a verified fact. This unattended run makes it, in the Phase 238
precedent style, and the SUMMARY must say so plainly so a navigator can overturn it cheaply if
they disagree.
</success_criteria>

<output>
Create `.planning/phases/239-brain-access-surface/239-06-SUMMARY.md` when done.
`.planning/` is gitignored in this repo, so commit it with `git add -f`.
</output>
