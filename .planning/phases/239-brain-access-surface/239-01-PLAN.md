---
phase: 239-brain-access-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-239.sh
  - tests/helpers/brain-capture-server.cjs
  - tests/test-brain-client-params.cjs
autonomous: true
requirements: [BRAIN-01, BRAIN-02, BRAIN-03]

must_haves:
  truths:
    - "A single command, `bash tests/run-all-239.sh`, reports PASS/FAIL/SKIP for every Phase 239 verification leg"
    - "The aggregator is NOT vacuously green in Wave 1: two named legs are RED on the day it lands and turn GREEN only when the real fixes arrive"
    - "A test that needs to capture the outbound Brain wire reuses one shared SSE-shaped capture server instead of standing up its own"
  artifacts:
    - path: "tests/run-all-239.sh"
      provides: "Phase 239 SKIP-safe verification aggregator with two anti-vacuity legs"
      contains: "WAVE 0 CONTRACT"
    - path: "tests/helpers/brain-capture-server.cjs"
      provides: "Reusable node:http loopback server replying in the SSE `data: ` shape callTool parses, plus the captured-request accessor"
      exports: ["startCaptureServer", "captured", "resetCaptured"]
  key_links:
    - from: "tests/test-brain-client-params.cjs"
      to: "tests/helpers/brain-capture-server.cjs"
      via: "require of the extracted helper"
      pattern: "require\\(.*helpers/brain-capture-server"
    - from: "tests/run-all-239.sh"
      to: "hooks/hooks.json"
      via: "dead-matcher literal census grep-guard"
      pattern: "mcp__brain_"
---

<objective>
Author the Phase 239 verification scaffolding BEFORE any fix lands (Nyquist: tests precede
implementation), and extract the SSE-shaped Brain capture server that already exists inside
one test file into a shared helper the BRAIN-02 canary test will consume.

Purpose: every later plan in this phase needs (a) one place to see the phase's whole gate and
(b) a way to watch the outbound Brain wire without inventing a fifth mock server. Both exist
in this repo already in the wrong shape: the aggregator idiom lives in `tests/run-all-196.sh`,
and the capture server lives inline inside `tests/test-brain-client-params.cjs`.

Output: `tests/run-all-239.sh`, `tests/helpers/brain-capture-server.cjs`, and a re-pointed
`tests/test-brain-client-params.cjs` that proves the extraction is behavior-preserving.

CROSS-PHASE SCOPE FENCE: this plan touches zero files claimed by Phase 237 or Phase 238.
Neither `lib/mcp/tools/chain.cjs` nor any file under `lib/mcp/` appears in `files_modified`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/239-brain-access-surface/239-RESEARCH.md
@.planning/phases/239-brain-access-surface/239-PATTERNS.md
@.planning/phases/239-brain-access-surface/239-VALIDATION.md
</context>

## Artifacts this phase produces (Plan 01 slice)

| Artifact | Kind | Status |
|----------|------|--------|
| `tests/run-all-239.sh` | new file (bash aggregator) | created here |
| `tests/helpers/brain-capture-server.cjs` | new file (test helper module) | created here |
| `startCaptureServer(...)` | new exported function | created here |
| `resetCaptured()` | new exported function | created here |
| `captured` | new exported array accessor | created here |
| `tests/helpers/` | new directory | created here |

Artifacts created by SIBLING plans that this aggregator declares legs for (do NOT create them
here): `scripts/check-brain-tool-liveness.cjs` (239-03), `tests/test-239-brain-tool-liveness.cjs`
(239-03), `tests/test-239-pii-sanitizer-liveness.cjs` (239-04),
`tests/test-239-query-egress-canary.cjs` (239-05), `tests/test-239-sendpacket-parked.cjs` (239-06),
`tests/test-239-verify-release-section-18.cjs` (239-07), `verify-release` section 18 (239-07).

<tasks>

<task type="auto">
  <name>Task 1: Extract the SSE capture server into tests/helpers/brain-capture-server.cjs</name>
  <files>tests/helpers/brain-capture-server.cjs, tests/test-brain-client-params.cjs</files>
  <read_first>
    - `tests/test-brain-client-params.cjs` IN FULL (344 lines). This is the source of the
      extraction. Pay attention to lines 57-125 (the server) and 168-183 (the env-then-require
      ordering).
    - `lib/core/brain-client.cjs` lines 20-30 (where `BRAIN_URL` is captured at MODULE LOAD
      time from `MINDRIAN_BRAIN_URL`) and lines 269-300 (`callTool`, specifically the SSE
      parse `text.split('\n').find(l => l.startsWith('data: '))`).
    - `.planning/phases/239-brain-access-surface/239-PATTERNS.md` section
      "tests/test-239-query-egress-canary.cjs + the shared capture helper" (the verbatim
      excerpts of what to extract).
  </read_first>
  <action>
    Create `tests/helpers/brain-capture-server.cjs` by LIFTING the existing `startMockServer`
    implementation and its `captured` array out of `tests/test-brain-client-params.cjs`. Do not
    write a new server from scratch. Per 239-PATTERNS.md Correction 2 this is an extract, not
    an invention.

    The helper exports exactly three things: `startCaptureServer()` returning a Promise of
    `{ server, port, url }` where `url` is `http://127.0.0.1:<port>`; `captured`, the array of
    `{ name, arguments }` records appended on every `tools/call`; and `resetCaptured()` which
    truncates `captured` in place (do not reassign the binding, callers hold the reference).

    Preserve every load-bearing property of the original verbatim: the server listens on
    `0` / `127.0.0.1` so the port is ephemeral; it answers `initialize` with a
    `text/event-stream` response body of `data: ` plus the JSON plus a trailing newline; it
    answers `tools/call` by pushing to `captured` and replying in the SAME `data: ` SSE shape;
    it answers anything else with 404 and `{ error: 'unknown_method' }`; it replies 400
    `{ error: 'bad_json' }` on an unparseable body. If the SSE shape is broken, `callTool`
    returns null and every consuming test silently proves nothing, so this is not cosmetic.

    Add a `stopCaptureServer(server)` convenience that wraps `server.close()` in a Promise so
    consumers can await teardown without duplicating the callback dance.

    Add a header comment block naming: the file it was extracted from, the reason (Phase 239
    BRAIN-02 needs the same transport capture, 239-PATTERNS.md Correction 2), and the ordering
    contract in one sentence, that `process.env.MINDRIAN_BRAIN_URL` and
    `process.env.MINDRIAN_BRAIN_KEY` must be set BEFORE `require`-ing `lib/core/brain-client.cjs`
    and that consumers must `delete require.cache[<resolved brain-client path>]` for isolation.

    Then edit `tests/test-brain-client-params.cjs` to `require` the helper and delete its now
    duplicate inline server. Keep its assertions byte-identical. If that file reads `captured`
    directly, re-point it at the helper's exported `captured`.

    No em-dashes anywhere. CJS only.
  </action>
  <acceptance_criteria>
    - `node tests/test-brain-client-params.cjs` exits 0, the SAME result it produced before this
      task. Record the before-run and after-run output in the SUMMARY. This is the proof the
      extraction is behavior-preserving rather than a rewrite.
    - `grep -c "http.createServer" tests/test-brain-client-params.cjs` returns 0 (the inline
      duplicate is gone, not merely shadowed).
    - `grep -c "helpers/brain-capture-server" tests/test-brain-client-params.cjs` returns at
      least 1.
    - `node -e "const h=require('./tests/helpers/brain-capture-server.cjs'); console.log(typeof h.startCaptureServer, typeof h.resetCaptured, Array.isArray(h.captured))"`
      prints `function function true`.
    - A live shape probe: start the helper, POST a body of
      `{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}` to its url, and assert the
      response body starts with the six characters `data: ` and ends with a newline. Transcribe
      the observed body into the SUMMARY.
    - `grep -cP '\x{2014}' tests/helpers/brain-capture-server.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-brain-client-params.cjs</automated>
  </verify>
  <done>The capture server exists once, in `tests/helpers/brain-capture-server.cjs`, is consumed by its original home, and that original test still passes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/run-all-239.sh with two anti-vacuity legs that are RED today</name>
  <files>tests/run-all-239.sh</files>
  <read_first>
    - `tests/run-all-196.sh` IN FULL. Copy the header contract wording, the `run`/`run_if`
      helpers, the grep-guard leg idiom (`noprivate_regex`), and the footer summary block.
      This is the repo's aggregator convention, not a suggestion.
    - `.planning/phases/239-brain-access-surface/239-VALIDATION.md` (the per-task verification
      map, which names every test file this aggregator must declare a leg for).
    - `hooks/hooks.json` lines 230-250 and 332-352 (the two matcher literals the census leg
      asserts against).
    - `lib/core/brain-response-sanitize.cjs` lines 70-90 (the `isBrainTool` prefix test the
      census leg asserts against).
  </read_first>
  <action>
    Create `tests/run-all-239.sh`, executable (`chmod +x`), bash only, no emoji, no em-dashes.

    Header comment block states the phase (239 Brain-Access Surface), the three requirements
    (BRAIN-01, BRAIN-02, BRAIN-03), and restates the WAVE 0 CONTRACT wording from
    `run-all-196.sh` lines 12-15 adapted to this phase.

    Copy `set -uo pipefail`, the `ROOT`/`cd` preamble, `PASS`/`FAIL`/`SKIP` counters, and the
    `run` and `run_if` helper functions verbatim from `run-all-196.sh`.

    Declare a `run_if` leg for each of the five phase test files, each guarded on the TEST FILE
    path so Wave 1 exits with clean SKIPs:
    `tests/test-239-brain-tool-liveness.cjs`, `tests/test-239-pii-sanitizer-liveness.cjs`,
    `tests/test-239-query-egress-canary.cjs`, `tests/test-239-sendpacket-parked.cjs`,
    `tests/test-239-verify-release-section-18.cjs`.
    Also declare a `run_if` leg for `lib/core/seam-liveness.test.cjs` (the shipped helper this
    phase is the first production consumer of, per 239-VALIDATION.md's per-task-commit sampling
    rate), and one for `tests/test-brain-response-sanitize.cjs`.

    Then add the TWO anti-vacuity legs. Guarding every leg on its own test file would let this
    phase report all-green with zero tests written, which is precisely the vacuous-coverage
    shape this milestone exists to catch, so these two bind immediately:

    Leg A, `BRAIN-01 dead-matcher literal census`, a shell function modeled on
    `run-all-196.sh`'s `noprivate_regex`: it fails if `hooks/hooks.json` still contains the
    dead literal `mcp__brain_.*`, and it fails if `lib/core/brain-response-sanitize.cjs` still
    contains the literal `indexOf('mcp__brain_')`. Filter comment lines before counting
    (`grep -vE '^[[:space:]]*(//|#)'`) so a header comment mentioning the old string cannot
    self-invalidate the gate.

    Leg B, `239 test-file completeness`, fails if any of the five `tests/test-239-*.cjs` files
    named above is absent.

    Both legs are EXPECTED RED on the day this plan lands. State that explicitly in the header
    comment: Leg A turns green when 239-02 lands, Leg B turns green when 239-07 lands, and a
    green Leg A/B with no sibling plan executed would itself be a bug.

    Footer: copy `run-all-196.sh`'s summary block verbatim, changing the label to
    `Summary (239 verification)`, keeping the final `[ "$FAIL" -eq 0 ]` exit expression.
  </action>
  <acceptance_criteria>
    - `bash -n tests/run-all-239.sh` exits 0 (syntax clean).
    - `test -x tests/run-all-239.sh` succeeds.
    - `bash tests/run-all-239.sh` on the CURRENT tree reports `Failed: 2` with exactly Leg A
      (`BRAIN-01 dead-matcher literal census`) and Leg B (`239 test-file completeness`) named
      as FAILED, and every `tests/test-239-*` leg reported SKIPPED. Transcribe the full summary
      line into the SUMMARY. This exact observed-RED state is the anti-vacuity proof and MUST be
      recorded, not asserted.
    - Anti-vacuity probe for Leg A itself: temporarily replace the `mcp__brain_.*` literal in a
      scratch COPY of `hooks/hooks.json` at `/tmp/hooks-probe.json` with the corrected pattern
      and confirm by hand that the leg's grep expression returns no match against that copy.
      Do NOT edit the real `hooks/hooks.json` in this plan (239-02 owns it); confirm
      `git diff --stat hooks/hooks.json` is empty at the end of this task.
    - `grep -c "WAVE 0 CONTRACT" tests/run-all-239.sh` returns at least 1.
    - `grep -cP '\x{2014}' tests/run-all-239.sh` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n tests/run-all-239.sh && bash tests/run-all-239.sh; test $? -ne 0</automated>
  </verify>
  <done>`bash tests/run-all-239.sh` runs, SKIPs every not-yet-authored leg, and reports exactly two named failures that later plans in this phase are contracted to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process to loopback HTTP | The capture helper stands up a real `node:http` listener; it binds `127.0.0.1` only, never `0.0.0.0` |
| test fixture content to disk | The helper holds captured request bodies in memory only; nothing is persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-T7 | Repudiation | `tests/run-all-239.sh` | mitigate | Leg A and Leg B bind immediately and are OBSERVED RED in this plan's acceptance criteria, so the aggregator cannot report green with zero real coverage. Directly mitigates RESEARCH.md T7 (vacuous test coverage) at the aggregator tier |
| T-239-01-A | Information Disclosure | `tests/helpers/brain-capture-server.cjs` | mitigate | Bind `127.0.0.1` and an ephemeral port `0`; the capture array is process-local and never written to disk, so canary and PII fixtures used by 239-05 cannot leak off-box |
| T-239-01-B | Tampering | `tests/test-brain-client-params.cjs` | mitigate | The extraction is proven behavior-preserving by re-running the untouched assertions before and after; a silent behavior change in the shared helper would surface as that file going red |
| T-239-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero packages. 239-RESEARCH.md's Package Legitimacy Audit records the phase as install-free, so the legitimacy gate is vacuous here |

T1, T2, T4 (the three HIGH severity threats) are NOT mitigated by this plan. They are owned by
239-02 plus 239-03 plus 239-07 (T1), 239-05 (T2 and T4). This plan builds the instrument those
plans are measured with; it does not close a boundary.
</threat_model>

<verification>
1. `node tests/test-brain-client-params.cjs` exits 0.
2. `bash -n tests/run-all-239.sh` exits 0.
3. `bash tests/run-all-239.sh` exits non-zero with `Failed: 2`, Leg A and Leg B named.
4. `git diff --stat hooks/hooks.json lib/core/brain-response-sanitize.cjs lib/core/brain-client.cjs` is EMPTY (this plan owns none of those files).
5. `grep -rlP '\x{2014}' tests/run-all-239.sh tests/helpers/brain-capture-server.cjs` returns nothing.
</verification>

<success_criteria>
- One command shows the whole Phase 239 gate.
- The gate is honestly RED where the work has not landed, and the two RED legs are named, transcribed, and contracted to specific sibling plans.
- The SSE capture server exists once in the repo, extracted rather than duplicated, with its original consumer still green.
</success_criteria>

<output>
Create `.planning/phases/239-brain-access-surface/239-01-SUMMARY.md` when done.
`.planning/` is gitignored in this repo, so commit it with `git add -f`.
</output>
