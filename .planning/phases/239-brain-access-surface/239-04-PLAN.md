---
phase: 239-brain-access-surface
plan: 04
type: execute
wave: 4
depends_on: ["239-02", "239-03"]
files_modified:
  - tests/test-239-pii-sanitizer-liveness.cjs
  - lib/core/security/agentshield-scanner.cjs
  - lib/core/grill-engine.cjs
  - lib/core/eureka/online-pattern-query.cjs
  - lib/core/part8-egress-guard.test.cjs
  - tests/part8-egress-guard-hook.test.cjs
  - lib/core/mva-detect.smoke.test.cjs
  - lib/core/mva-orchestrator.test.cjs
autonomous: true
requirements: [BRAIN-01]

must_haves:
  truths:
    - "The PostToolUse PII sanitizer hook actually rewrites a Brain response when the tool name is a LIVE Brain tool name, proven by running the hook script as a real child process"
    - "No tracked source file still synthesizes or asserts the dead `mcp__brain_` tool name as if it were real"
    - "Every superseded test fixture is inverted with a stated reason, not silently updated, so the inversion is itself the mutation gate"
  artifacts:
    - path: "tests/test-239-pii-sanitizer-liveness.cjs"
      provides: "PostToolUse half of BRAIN-01: hook fires on a live name, passes through on a foreign name"
      min_lines: 70
    - path: "lib/core/security/agentshield-scanner.cjs"
      provides: "Default toolName that resolves to a live Brain tool"
      contains: "mindrian-brain__"
  key_links:
    - from: "tests/test-239-pii-sanitizer-liveness.cjs"
      to: "scripts/brain-response-sanitize-hook.cjs"
      via: "spawnSync with a PostToolUse stdin envelope"
      pattern: "brain-response-sanitize-hook"
    - from: "tests/test-239-pii-sanitizer-liveness.cjs"
      to: "scripts/check-brain-tool-liveness.cjs"
      via: "tool names derived from the live enumeration, never hand-typed"
      pattern: "check-brain-tool-liveness"
---

<objective>
Finish BRAIN-01 by proving the INBOUND half works and by clearing every remaining copy of the
dead tool name out of tracked source.

Purpose: 239-02 fixed the outbound door (PreToolUse egress guard) and 239-03 built the gate.
The PostToolUse PII sanitizer is the other direction and has never been proven to fire on a
live name. Separately, 239-PATTERNS.md's Census Addendum found `mcp__brain_` literals the
research did not list, including one that is arguably LIVE rather than a fixture:
`lib/core/security/agentshield-scanner.cjs:156` uses `toolName: opts.toolName || 'mcp__brain_query'`
as a runtime default in a scanner call path. A dead default there means the scanner classifies
against a tool name that does not exist.

RESEARCH.md Pitfall 3 is the standing hazard: fixing one of three layers and declaring victory.
This plan is the census that proves there is no fourth.

Output: `tests/test-239-pii-sanitizer-liveness.cjs`, three source files with live defaults, and
four test files whose superseded fixtures are INVERTED rather than deleted.

CROSS-PHASE SCOPE FENCE: this plan touches zero files claimed by Phase 237 or Phase 238.
Neither `lib/mcp/tools/chain.cjs` nor any file under `lib/mcp/` appears in `files_modified`.

MUTATION SERIALIZATION FENCE (read before running any mutation leg). This repo's
`.planning/config.json` sets `workflow.use_worktrees: false`, so every plan in this wave runs
against the SAME working tree, and each Wave 2 plan performs a live mutate-and-restore on
shared tracked source. This plan mutates `lib/core/brain-response-sanitize.cjs`; its Wave 4
sibling 239-07 mutates `hooks/hooks.json` AND runs the full `bash scripts/verify-release`,
which reads the whole tree. This plan's mutation is the higher-risk of the two, because
`lib/core/brain-response-sanitize.cjs` is read by `bash tests/run-all-196.sh` and by the hook
scripts `verify-release` exercises. A mutation in
flight here can turn a sibling red for a reason that has nothing to do with that sibling's work.
This is the exact coordination hazard recorded in `.planning/STATE.md`'s Phase 242 entry.

Rules, mandatory for every mutation leg in this plan:
  1. Immediately before mutating, run `git status --porcelain` for the ENTIRE repo, not only
     your own paths. If any tracked file OUTSIDE this plan's `files_modified` is dirty, a
     sibling has a mutation in flight: WAIT and retry a bounded number of times. Do NOT proceed
     and do NOT restore a sibling's file.
  2. Hold each mutation for the shortest possible window: mutate, run the single assertion,
     restore. Never leave a mutation in place across an unrelated command, and never across a
     `run-all-196.sh` invocation.
  3. Restore in a `finally`, then assert byte-identity against a backup held OUTSIDE the repo.
  4. Run any SHARED regression sweep as the LAST step of the plan, after every one of this
     plan's own mutations is restored.
  5. If a shared sweep goes red, re-run it once before reporting, and record in the SUMMARY
     whether the red reproduced. A red that does not reproduce is a sibling-collision artifact,
     not a finding, and must be labelled as such rather than chased.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/239-brain-access-surface/239-RESEARCH.md
@.planning/phases/239-brain-access-surface/239-PATTERNS.md
@.planning/phases/239-brain-access-surface/239-02-SUMMARY.md
@.planning/phases/239-brain-access-surface/239-03-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 04 slice)

| Artifact | Kind | Status |
|----------|------|--------|
| `tests/test-239-pii-sanitizer-liveness.cjs` | new file (test) | created here |
| `agentshield-scanner.cjs` default `toolName` (lines ~146, ~156) | literals replaced | changed here |
| `grill-engine.cjs` synthesized `toolName` (lines ~172, ~286) | literals replaced | changed here |
| `eureka/online-pattern-query.cjs` doc comment (line ~22) | comment corrected | changed here |
| `part8-egress-guard.test.cjs` fixtures (lines 114-163, 7 sites) | fixtures re-derived | changed here |
| `part8-egress-guard-hook.test.cjs` fixtures (lines 49-68) | fixtures re-derived | changed here |
| `mva-detect.smoke.test.cjs` regex fixture (line ~181) | reconciled or justified in place | changed here |
| `mva-orchestrator.test.cjs` regex fixture (line ~332) | reconciled or justified in place | changed here |

<tasks>

<task type="auto">
  <name>Task 1: Author tests/test-239-pii-sanitizer-liveness.cjs, the PostToolUse proof</name>
  <files>tests/test-239-pii-sanitizer-liveness.cjs</files>
  <read_first>
    - `scripts/brain-response-sanitize-hook.cjs` IN FULL. The PostToolUse stdin envelope shape
      is `{ tool_name, tool_input, tool_response, session_id }` (confirm the exact field names
      from the source, do not assume); line 72 is the `isBrainTool` re-check that emits a
      passthrough on false.
    - `tests/part8-egress-guard-hook.test.cjs` lines 40-100 (the `envelope()` helper and the
      `spawnSync` invocation shape). Copy this shape; it is the repo's hook-integration idiom.
    - `lib/core/brain-response-sanitize.cjs` `PII_PATTERNS` and `ALLOWLIST` (what actually gets
      rewritten, so the fixture carries a value the sanitizer really matches).
    - `scripts/check-brain-tool-liveness.cjs` (authored by 239-03) for
      `enumerateLiveBrainTools` and `composeScopedNames`.
    - `.planning/phases/239-brain-access-surface/239-RESEARCH.md` Pitfall 5 (fixtures must be
      derived from the live enumeration, never hand-typed) and threat T7.
  </read_first>
  <action>
    Create `tests/test-239-pii-sanitizer-liveness.cjs`, plain Node with `node:assert`, driving
    `scripts/brain-response-sanitize-hook.cjs` as a real child process via `spawnSync` with the
    PostToolUse envelope on stdin.

    Every tool name used as a fixture is DERIVED at run time: require
    `scripts/check-brain-tool-liveness.cjs`, call `enumerateLiveBrainTools()`, pass the bare
    names through `composeScopedNames(...)`, and pick from the result. Do not type
    `mcp__plugin_mos_mindrian-brain__brain_query` anywhere; if the plugin or server is renamed
    tomorrow this test must follow, not rot. That is RESEARCH.md Pitfall 5's remedy applied
    literally.

    Legs:

    LEG 1, plugin-scoped live name fires the sanitizer: build an envelope whose `tool_name` is a
    live plugin-scoped Brain name and whose `tool_response` carries a PII value the sanitizer's
    own `PII_PATTERNS` matches (read the patterns and pick one, for example an email). Assert the
    hook exits 0 AND that its stdout is a sanitizer envelope in which the PII value is REWRITTEN,
    not echoed. Assert the raw PII value does not appear anywhere in stdout.

    LEG 2, project-scoped live name fires the sanitizer: identical assertion against the
    `mcp__<server>__<tool>` form (Pitfall 6, both scopes really occur across the three
    surfaces).

    LEG 3, non-Brain tool passes through: `tool_name` of `Write`, assert exit 0 and a passthrough
    envelope with the payload untouched. This is the existing defense-in-depth behavior and must
    not regress.

    LEG 4, threat T3 foreign server passes through: `tool_name` of
    `mcp__plugin_evil_evil-brain__brain_ask`, assert exit 0 and a PASSTHROUGH, with the PII
    value echoed unchanged. A foreign server's response must NOT be rewritten by a sanitizer
    tuned to a different contract.

    LEG 5, the superseded name is now inert: `tool_name` of `mcp__brain_query`, assert exit 0
    and a PASSTHROUGH. Add an inline comment naming Phase 239 and stating that this name never
    existed in production, so treating it as inert is the correction, not a regression.

    LEG 6, MUTATION: in a `try/finally`, revert `isBrainTool` in
    `lib/core/brain-response-sanitize.cjs` to the pre-239 `indexOf('mcp__brain_') === 0` body,
    re-run LEG 1's spawn, and assert the hook now emits a PASSTHROUGH (the PII value is echoed).
    That is the live gate reopening. The `finally` restores the file unconditionally; after it,
    assert `git diff --stat lib/core/brain-response-sanitize.cjs` is EMPTY and re-run LEG 1
    asserting it is green again.

    Print a per-leg PASS line and exit non-zero on any failure, matching the repo's plain-assert
    convention. No em-dashes.
  </action>
  <acceptance_criteria>
    - `node tests/test-239-pii-sanitizer-liveness.cjs` exits 0 with six legs reported.
    - No hand-typed live tool name:
      `grep -cE "mcp__plugin_[a-z]|mcp__mindrian-brain__" tests/test-239-pii-sanitizer-liveness.cjs`
      returns 0 (every live name is composed at run time). The two DELIBERATE literals for LEG 4
      (`mcp__plugin_evil_evil-brain__brain_ask`) and LEG 5 (`mcp__brain_query`) are negative
      fixtures and are exempt; name them explicitly in the SUMMARY as the only allowed literals.
    - LEG 1 transcript in the SUMMARY: the exact PII value seeded and the exact rewritten value
      observed in stdout.
    - LEG 6 transcript in the SUMMARY: the passthrough envelope observed under the reverted
      `isBrainTool`, the post-restore `git diff --stat` showing EMPTY, and LEG 1 green again.
      This before/after pair is the mutation proof that the PostToolUse half is load-bearing.
    - `grep -cP '\x{2014}' tests/test-239-pii-sanitizer-liveness.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-239-pii-sanitizer-liveness.cjs</automated>
  </verify>
  <done>The inbound PII sanitizer is proven to fire on both live scopes, to pass through on a foreign server and on the dead name, and to go inert again if the authority regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the three non-test source files that still carry the dead tool name</name>
  <files>lib/core/security/agentshield-scanner.cjs, lib/core/grill-engine.cjs, lib/core/eureka/online-pattern-query.cjs</files>
  <read_first>
    - `lib/core/security/agentshield-scanner.cjs` lines 130-170. Line 156 is
      `callOpts = { toolName: opts.toolName || 'mcp__brain_query' }` and line 146 carries a
      related literal. 239-PATTERNS.md's Census Addendum flags this as "the one that deserves a
      look as possibly live rather than fixture". Determine which it is by reading the call path
      and STATE the finding.
    - `lib/core/grill-engine.cjs` lines 160-185 and 275-295, where `toolName: 'mcp__brain_ask'`
      is synthesized and handed to the Part-8 classifier.
    - `lib/core/eureka/online-pattern-query.cjs` lines 15-30, which DOCUMENTS the dead matcher
      as current fact.
    - `lib/core/part8-egress-guard.cjs` `classify(payload, opts)` lines 231-275, specifically
      `_isFreeFormTool` and how `opts.toolName` is consumed, so you can tell whether these
      call sites need the FULL scoped name or the BARE `brain_ask` / `brain_query` form.
    - `scripts/check-brain-tool-liveness.cjs` (239-03) for the composed names.
    - `.planning/phases/239-brain-access-surface/239-PATTERNS.md` "Census Addendum".
  </read_first>
  <action>
    First, determine the CORRECT form for each site by reading `part8-egress-guard.cjs::classify`
    and `_isFreeFormTool`. This is the load-bearing judgment of the task and must be stated
    before any edit: `classify` receives `opts.toolName` and uses it to pick the free-form
    string path (`brain_ask`) versus the structured path. The SHIPPED precedents
    (`lib/core/bono/persona-research.cjs:208-233` and `brain-client.cjs`'s `sendPacket` belt at
    lines 1273-1298) both pass the BARE tool name (`'brain_ask'`, `'brain_packet'`), not the
    scoped MCP name. The hook scripts, by contrast, receive the SCOPED name from the host.

    So the decision rule is: an IN-PROCESS `classify()` call passes the BARE name; a HOOK or a
    tool-name matcher uses the SCOPED name. Apply that rule per site and record which rule each
    site fell under.

    `lib/core/security/agentshield-scanner.cjs`: read the call path to decide whether this
    default feeds `classify()` (in-process, so BARE `brain_query`) or a matcher comparison
    (scoped). State the verdict in the SUMMARY. If the finding is that the site is LIVE (a real
    scanner call path, not a fixture default), say so plainly, because that means the scanner has
    been classifying against a fabricated tool name.

    `lib/core/grill-engine.cjs` lines 172 and 286: these synthesize a toolName for a classifier
    call, so under the rule they become the BARE `brain_ask`. Verify by reading the surrounding
    call, do not apply the rule blind.

    `lib/core/eureka/online-pattern-query.cjs` line 22: comment only. Rewrite it to state the
    live matcher shape and cite Phase 239. Zero code change in this file.

    In every case, add a one-clause inline comment naming Phase 239 so the next maintainer
    reading a bare `brain_query` next to a scoped `mcp__...` elsewhere understands the two forms
    are deliberate, not drift.

    Do NOT touch `dist/generic-claude-dir/.claude/skills/intelligence-orchestrator/SKILL.md`
    (a build artifact, regenerated not hand-edited), `CHANGELOG.md` line 1589 (historical
    record, do not rewrite history), or `references/research/RESEARCH_16_*.md` (archived research
    prose, out of scope). All three are named in 239-PATTERNS.md's Census Addendum as
    deliberate exclusions; restate them in the SUMMARY so the verifier does not read them as
    misses.
  </action>
  <acceptance_criteria>
    - `grep -c "mcp__brain_" lib/core/security/agentshield-scanner.cjs lib/core/grill-engine.cjs lib/core/eureka/online-pattern-query.cjs`
      returns 0 for all three, OR every remaining occurrence is inside a sentence that
      explicitly labels it the superseded literal (name each, with its line, in the SUMMARY).
    - The SUMMARY states, per site, which decision rule applied (in-process BARE versus hook
      SCOPED) and quotes the line of `part8-egress-guard.cjs` or the shipped precedent that
      justifies it.
    - The SUMMARY states the agentshield-scanner verdict explicitly: live call path or fixture
      default.
    - Regression sweep, all must exit 0:
      `node lib/core/part8-egress-guard.test.cjs`,
      `bash tests/run-all-196.sh`,
      and whichever agentshield and grill-engine test files exist (find them with
      `ls lib/core/*grill* lib/core/security/*test* tests/*agentshield* tests/*grill*` and run
      every hit). List every file run and its result in the SUMMARY.
    - `git diff --stat lib/core/eureka/online-pattern-query.cjs` shows COMMENT LINES ONLY.
    - The three excluded surfaces are untouched:
      `git diff --stat dist/ CHANGELOG.md references/` is EMPTY.
    - `grep -rlP '\x{2014}' lib/core/security/agentshield-scanner.cjs lib/core/grill-engine.cjs lib/core/eureka/online-pattern-query.cjs`
      returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>node lib/core/part8-egress-guard.test.cjs && bash tests/run-all-196.sh</automated>
  </verify>
  <done>No non-test source file synthesizes or documents a Brain tool name that does not exist, and the bare-versus-scoped decision rule is recorded rather than left implicit.</done>
</task>

<task type="auto">
  <name>Task 3: Invert the remaining dead-name test fixtures, deriving names from the live enumeration</name>
  <files>lib/core/part8-egress-guard.test.cjs, tests/part8-egress-guard-hook.test.cjs, lib/core/mva-detect.smoke.test.cjs, lib/core/mva-orchestrator.test.cjs</files>
  <read_first>
    - `lib/core/part8-egress-guard.test.cjs` lines 100-175. Seven fixture sites at lines 114,
      117, 120, 130, 136, 147, 163 (239-PATTERNS.md enumerated these; RESEARCH.md said
      "fixtures" without listing them).
    - `tests/part8-egress-guard-hook.test.cjs` lines 40-100. The `envelope()` fixtures at lines
      49-68 use `mcp__brain_query` and `mcp__brain_ask`; line 91-95 is the non-Brain passthrough
      assertion that is CORRECT and must not change.
    - `lib/core/mva-detect.smoke.test.cjs` line ~181 and `lib/core/mva-orchestrator.test.cjs`
      line ~332, both `/mcp__brain_/` regex fixtures. READ THE SURROUNDING ASSERTION BEFORE
      EDITING: these may be asserting against the guard's own OUTPUT rather than against a tool
      name, in which case the correct action is a justified in-place comment, not a rewrite.
    - Task 2's recorded bare-versus-scoped decision rule.
    - `.planning/phases/239-brain-access-surface/239-RESEARCH.md` Pitfall 5 and its remedy: treat
      these as assertions to INVERT, following the Phase 243 precedent where superseded
      assertions were inverted rather than deleted and the inversion itself became the mutation
      gate.
  </read_first>
  <action>
    Apply Task 2's decision rule per site. `part8-egress-guard.test.cjs` tests `classify()`
    directly, so its `toolName` fixtures are IN-PROCESS and take the BARE form. The hook test
    drives a hook script over stdin, so its `tool_name` envelope fields are SCOPED.

    For the hook test, derive the scoped names at run time from
    `scripts/check-brain-tool-liveness.cjs` rather than typing them, exactly as Task 1 does.
    Hand-typing here would move the fiction rather than kill it, which RESEARCH.md's
    Anti-Patterns list names explicitly.

    For each changed assertion, add a one-clause comment naming Phase 239 and what was
    superseded. Invert, do not delete: where an assertion previously proved the dead name was
    ACCEPTED, keep the assertion and flip its expected value so it now proves the dead name is
    REJECTED. Add, where absent, a negative case for the foreign-server form
    `mcp__plugin_evil_evil-brain__brain_ask` (threat T3).

    For the two `mva-*` regex fixtures, decide by reading. If the regex is matching a TOOL NAME,
    update it to the live shape (or better, to the exported `BRAIN_TOOL_MATCHER`). If it is
    matching the guard's own emitted string or a telemetry field, leave the code and add a
    comment stating why it is correctly unchanged. State the verdict for each file in the
    SUMMARY. Do not change either file "just to be consistent"; an unnecessary edit to a passing
    test is a regression risk with no upside.

    Do not reduce the total assertion count in any of the four files.
  </action>
  <acceptance_criteria>
    - All four files exit 0 when run: `node lib/core/part8-egress-guard.test.cjs`,
      `node tests/part8-egress-guard-hook.test.cjs`, `node lib/core/mva-detect.smoke.test.cjs`,
      `node lib/core/mva-orchestrator.test.cjs`.
    - Assertion counts did not shrink in any of the four. Record the pre and post
      `grep -c "assert"` value for each in the SUMMARY.
    - `grep -c "mcp__brain_" lib/core/part8-egress-guard.test.cjs tests/part8-egress-guard-hook.test.cjs`
      returns 0 for both, EXCEPT for deliberately-retained inverted negative fixtures; name each
      retained occurrence and its inverted expectation in the SUMMARY.
    - The hook test contains no hand-typed live scoped name:
      `grep -cE "mcp__plugin_mos|mcp__mindrian-brain__" tests/part8-egress-guard-hook.test.cjs`
      returns 0.
    - The SUMMARY states, for `mva-detect.smoke.test.cjs` and `mva-orchestrator.test.cjs`
      SEPARATELY, whether the regex matched a tool name (updated) or the guard's own output
      (comment only), quoting the surrounding assertion that decided it.
    - MUTATION PROOF, transcribed: revert `isBrainTool` in
      `lib/core/brain-response-sanitize.cjs` to the pre-239 prefix body, run
      `node tests/part8-egress-guard-hook.test.cjs`, confirm NON-ZERO exit naming a live-name
      assertion, restore, confirm `git diff --stat lib/core/brain-response-sanitize.cjs` EMPTY
      and the test green.
    - Full no-regression sweep, transcribed: `bash tests/run-all-196.sh` PASS with FAIL=0, and
      `bash tests/run-all-239.sh` shows the PII-sanitizer leg flipped from SKIPPED to PASSED.
    - `grep -rlP '\x{2014}'` over all four files returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>node lib/core/part8-egress-guard.test.cjs && node tests/part8-egress-guard-hook.test.cjs && node lib/core/mva-detect.smoke.test.cjs && node lib/core/mva-orchestrator.test.cjs</automated>
  </verify>
  <done>The test corpus no longer locks in a tool name that never existed, every superseded assertion is inverted with a stated reason, and the two ambiguous regex fixtures got a read-first verdict rather than a reflex edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brain response to session context | Inbound; the PostToolUse PII sanitizer is the only control on this direction |
| foreign MCP server response to the sanitizer | An untrusted server must NOT have its response rewritten by a sanitizer tuned to the Brain contract |
| test fixture to production truth | A fixture naming a nonexistent tool is an untrusted claim that reads as coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-T1 | Information Disclosure | `scripts/brain-response-sanitize-hook.cjs` (PostToolUse half) | mitigate | Task 1 proves the sanitizer rewrites PII on BOTH live scopes by driving the real hook script over stdin, and LEG 6 proves it goes inert again if the authority regresses. Without this the inbound half of BRAIN-01 would be asserted, not shown |
| T-239-T3 | Spoofing / Tampering | `isBrainTool` applied to a foreign server | mitigate | Task 1 LEG 4 and Task 3's added negative case both assert `mcp__plugin_evil_evil-brain__brain_ask` PASSES THROUGH unrewritten, so a third-party server is never handled as the trusted Brain |
| T-239-T7 | Repudiation | the four test-fixture files plus three source defaults | mitigate | Every live fixture name is derived from `enumerateLiveBrainTools()`; acceptance criteria grep for hand-typed live names and require a justification for each retained literal. Superseded assertions are INVERTED so the inversion is the gate, per the Phase 243 precedent |
| T-239-04-A | Tampering | `lib/core/security/agentshield-scanner.cjs` runtime default | mitigate | The default `toolName` fed to the classifier is corrected to a name that resolves, and the executor must STATE whether this site was live or a fixture rather than fixing it silently. A dead default means the scanner has been classifying against a fabricated name |
| T-239-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; the phase-level Package Legitimacy Audit records the phase as install-free |

T2 and T4 are owned by 239-05. T1's blocking-gate half is owned by 239-03 plus 239-07.
</threat_model>

<verification>
1. `node tests/test-239-pii-sanitizer-liveness.cjs` exits 0, six legs.
2. `node lib/core/part8-egress-guard.test.cjs` exits 0.
3. `node tests/part8-egress-guard-hook.test.cjs` exits 0.
4. `node lib/core/mva-detect.smoke.test.cjs` and `node lib/core/mva-orchestrator.test.cjs` both exit 0.
5. `bash tests/run-all-196.sh` reports FAIL=0.
6. `bash tests/run-all-239.sh` shows the PII-sanitizer leg PASSED.
7. `git diff --stat dist/ CHANGELOG.md references/` is EMPTY.
8. `git diff --stat lib/core/brain-response-sanitize.cjs hooks/hooks.json` is EMPTY (this plan owns neither; every mutation restored).
9. `grep -rlP '\x{2014}'` over all eight `files_modified` returns nothing.
</verification>

<success_criteria>
Completes ROADMAP Success Criterion 1's inbound half and closes RESEARCH.md Pitfall 3:
- The PostToolUse PII sanitizer demonstrably fires on live names and demonstrably does not fire on a foreign server or on the dead name.
- A repo-wide census leaves zero tracked non-archival source carrying a Brain tool name that does not exist.
- Every superseded assertion is inverted with a stated reason and a live observed-RED mutation, so the corpus can no longer pass green against a fiction (RESEARCH.md Pitfall 5, threat T7).
</success_criteria>

<output>
Create `.planning/phases/239-brain-access-surface/239-04-SUMMARY.md` when done.
`.planning/` is gitignored in this repo, so commit it with `git add -f`.
</output>
