---
phase: 239-brain-access-surface
plan: 07
type: execute
wave: 4
depends_on: ["239-03"]
files_modified:
  - scripts/verify-release
  - tests/test-239-verify-release-section-18.cjs
autonomous: true
requirements: [BRAIN-01]

must_haves:
  truths:
    - "The Brain tool-name liveness check runs inside a gate that actually blocks, not only inside a phase-local test"
    - "A staled matcher makes `scripts/verify-release` report a failure and exit non-zero, so the release does not ship"
    - "The gate is proven to bite by mutating the real tree and observing the real release gate go red, then restoring"
  artifacts:
    - path: "scripts/verify-release"
      provides: "New numbered section 18, Brain Tool Liveness Gate, invoking scripts/check-brain-tool-liveness.cjs and branching on its exit code"
      contains: "18. Brain Tool Liveness"
    - path: "tests/test-239-verify-release-section-18.cjs"
      provides: "Structural assertion that section 18 exists and is wired to the FAIL counter, plus a live end-to-end bite proof"
      min_lines: 70
  key_links:
    - from: "scripts/verify-release"
      to: "scripts/check-brain-tool-liveness.cjs"
      via: "node invocation with exit-code capture, mirroring sections 16 and 17"
      pattern: "check-brain-tool-liveness"
    - from: "scripts/verify-release"
      to: "the SUMMARY block's exit 1"
      via: "the fail helper incrementing FAIL"
      pattern: "fail \""
---

<objective>
Make the SC1 liveness check load-bearing. A liveness assertion that lives only in a phase test
is decorative: it proves the seam was alive on the day the phase closed and nothing thereafter.
Threat T1 (silent matcher death after a future rename) is rated HIGH precisely because Claude
Code never validates a matcher against a live registry and gives no signal when one goes dead.
The only defence is a gate that blocks.

Purpose and a correction the executor must plan against: 239-RESEARCH.md's T1 mitigation and
Open Question 2 both say to mirror "how Phase 238-06 wired `checkMintRatifierLiveness` into
`verify-release` section 18". Per 239-PATTERNS.md Correction 1, THAT PRECEDENT DOES NOT EXIST
ON DISK. `scripts/verify-release`'s numbered sections stop at 17 (Kuzu Reintroduction Gate),
followed by an unnumbered PACKAGE-LOCK SYNC section and the SUMMARY. `checkMintRatifierLiveness`
exists only as a definition in `lib/core/seam-liveness.cjs` plus its own unit test, with zero
production callers. Phase 239 must AUTHOR section 18 from the section 16 and 17 shape.

Output: a new `verify-release` section 18 and a test that proves it blocks.

CROSS-PHASE SCOPE FENCE: this plan touches zero files claimed by Phase 237 or Phase 238.
Neither `lib/mcp/tools/chain.cjs` nor any file under `lib/mcp/` appears in `files_modified`.

MUTATION SERIALIZATION FENCE (read before running any mutation leg). This repo's
`.planning/config.json` sets `workflow.use_worktrees: false`, so both Wave 3 plans run against
the SAME working tree. This plan mutates `hooks/hooks.json` AND runs the full
`bash scripts/verify-release`, which reads the WHOLE tree and will therefore observe any
mutation its Wave 4 sibling 239-04 has in flight (239-04 mutates
`lib/core/brain-response-sanitize.cjs`, which the hook scripts `verify-release` exercises read). This is the exact coordination hazard recorded in
`.planning/STATE.md`'s Phase 242 entry, and it matters more here than anywhere else in the
phase because `verify-release` is the broadest-reading command in the repo.

Rules, mandatory for every mutation leg in this plan:
  1. Immediately before mutating, AND immediately before every `bash scripts/verify-release`
     invocation, run `git status --porcelain` for the ENTIRE repo. If any tracked file OUTSIDE
     this plan's `files_modified` is dirty, sibling 239-04 has a mutation in flight: WAIT and
     retry a bounded number of times. Do NOT proceed and do NOT restore a sibling's file.
  2. Hold each mutation for the shortest possible window: mutate, run the single assertion,
     restore. Never leave a mutation in place across an unrelated command.
  3. Restore in a `finally`, then assert byte-identity against a backup held OUTSIDE the repo.
  4. Record the `git status --porcelain` output captured immediately before each
     `verify-release` run into the SUMMARY, so any unrelated section failure can be attributed
     to a real pre-existing cause rather than to a sibling collision.
  5. If a shared sweep goes red, re-run it once before reporting, and record in the SUMMARY
     whether the red reproduced. A red that does not reproduce is a sibling-collision artifact,
     not a finding, and must be labelled as such rather than chased.

CROSS-PHASE COORDINATION NOTE, flagged loudly because it is real: Phase 238's own plan 238-06
declares that it will "give checkMintRatifierLiveness a production consumer wired into
verify-release as section 18" (see `.planning/ROADMAP.md` Phase 238 Wave 3). Phase 238 is
PLANNED but NOT EXECUTED. Both phases therefore intend to author a section numbered 18 in the
SAME file, `scripts/verify-release`. This is a genuine cross-phase collision on a file neither
phase's `files_modified` overlap check would have surfaced, because it is a collision on a
SECTION NUMBER inside a shared file rather than on the file list itself. Resolution rule for
whichever phase executes SECOND: renumber your own section to the next free integer, do not
overwrite the other phase's section, and reconcile the numbering in your SUMMARY. Whichever
executes FIRST takes 18. Task 1 below carries a mandatory read-first step that checks the
current state of the file and applies this rule.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/239-brain-access-surface/239-RESEARCH.md
@.planning/phases/239-brain-access-surface/239-PATTERNS.md
@.planning/phases/239-brain-access-surface/239-03-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 07 slice)

| Artifact | Kind | Status |
|----------|------|--------|
| `scripts/verify-release` section 18, `Brain Tool Liveness Gate` | new numbered section | created here (renumber if 238 landed first) |
| `BRAIN_LIVENESS_OUT` / `BRAIN_LIVENESS_CODE` shell variables | new | created here |
| `tests/test-239-verify-release-section-18.cjs` | new file (test) | created here |

<tasks>

<task type="auto">
  <name>Task 1: Author the verify-release Brain Tool Liveness Gate section</name>
  <files>scripts/verify-release</files>
  <read_first>
    - `scripts/verify-release` lines 400-511 IN FULL. Section 16 (STOP HOOK hookSpecificOutput
      SCHEMA GATE, line 427) and section 17 (KUZU REINTRODUCTION GATE, line 453) are the two
      templates. Section 17 is the newest and cleanest: a banner comment block explaining WHY the
      gate exists and citing the RCA or phase, an `echo -e "\n${BOLD}17. Kuzu Reintroduction Gate${NC}"`
      header, a `VAR_OUT=$(node "$PLUGIN_ROOT/scripts/check-X.cjs" 2>&1) && VAR_CODE=0 || VAR_CODE=$?`
      capture, and an if/else branching to `pass "..."` or `fail "..."` plus `echo "$VAR_OUT"`.
      Also read the SUMMARY block at lines 495-511, which exits 1 when `FAIL > 0`; that is what
      makes this shape genuinely load-bearing.
    - `scripts/check-brain-tool-liveness.cjs` as authored by 239-03, specifically its exit-code
      contract: 0 pass, 1 dead seam, 2 probe failure.
    - `scripts/check-kuzu-reintroduction.cjs` header block (the WHY-THIS-EXISTS prose voice).
    - `.planning/phases/239-brain-access-surface/239-PATTERNS.md` "scripts/verify-release - NEW
      section 18 (SC1 liveness gate)".
    - MANDATORY FIRST CHECK before writing anything: run
      `grep -n "^# 1[89]\.\|^# 2[0-9]\." scripts/verify-release` and
      `grep -n "MintRatifier\|seam-liveness" scripts/verify-release`. If Phase 238 has already
      landed a section 18, apply the collision rule from this plan's objective: take the next
      free integer, leave 238's section untouched, and record the renumbering in the SUMMARY.
      State the observed result of BOTH greps in the SUMMARY either way, including "no section
      18 present" if that is what you find.
  </read_first>
  <action>
    Insert a new numbered section AFTER section 17 and BEFORE the unnumbered PACKAGE-LOCK SYNC
    section, cloning section 17's shape exactly: the `# ====` banner, the WHY-THIS-EXISTS comment
    block, the `echo -e "\n${BOLD}N. Brain Tool Liveness Gate${NC}"` header, the capture into
    `BRAIN_LIVENESS_OUT` and `BRAIN_LIVENESS_CODE`, and the branch.

    The WHY-THIS-EXISTS comment block must state, in the section-17 voice:
      - the failure this gate exists to catch: Claude Code evaluates a hook matcher per tool-call
        event and NEVER validates it against a live tool registry; a matcher naming a tool that
        no longer exists does not warn and does not error, it silently stops firing, forever
      - the concrete instance: before Phase 239 this repo's two Part-8 hook matchers read
        `mcp__brain_.*` while the live registered names were
        `mcp__plugin_<plugin>_<server>__<tool>`, so the Canon Part 8 egress guard and the PII
        sanitizer had both been dead for the entire period the plugin shipped through the
        marketplace
      - the citation: the official Claude Code plugins reference states that a matcher written
        against the bare server key never fires
      - the two claim sources the gate checks: hook matchers (each must match at least one live
        name, since an empty claim set reads as vacuously live and is therefore treated as a
        FAILURE) and agent `allowed-tools` exact names
      - a pointer to Phase 239, BRAIN-01, and threat T1

    Branch on the exit code with THREE arms, not two, because the check script has a third state:
      - 0 -> `pass "Every Brain hook matcher and agent allowed-tools entry names a live MCP tool"`
      - 1 -> `fail "Brain tool liveness gate FAILED (a hook matcher or agent allowed-tools entry names a Brain tool that does not exist):"` then `echo "$BRAIN_LIVENESS_OUT"`
      - anything else -> `fail "Brain tool liveness gate could not run (probe failure):"` then
        `echo "$BRAIN_LIVENESS_OUT"`. A probe that cannot run proves nothing and must NOT pass.
        Do not use `warn` here; a gate that degrades to a warning when it cannot measure is the
        exact posture Phase 235 Decision (c) rejected.

    Do NOT renumber, reorder or otherwise touch sections 1 through 17, the PACKAGE-LOCK SYNC
    section, or the SUMMARY block.

    No em-dashes. Keep the file's existing bash idioms (`$PLUGIN_ROOT`, `${BOLD}`, `${NC}`,
    `pass`, `fail`).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/verify-release` exits 0.
    - The mandatory first-check grep results are transcribed into the SUMMARY, including whether
      a Phase 238 section 18 was present and what number this section therefore took.
    - `grep -c "check-brain-tool-liveness.cjs" scripts/verify-release` returns exactly 1.
    - `grep -n "Brain Tool Liveness Gate" scripts/verify-release` returns a line number GREATER
      than the line number of `17. Kuzu Reintroduction Gate` and LESS than the line number of
      `PACKAGE-LOCK SYNC`. Transcribe all three line numbers.
    - The section has three branches: `grep -c "BRAIN_LIVENESS_CODE" scripts/verify-release`
      returns at least 3, and both `fail "` arms are present.
    - `git diff scripts/verify-release` shows an INSERTION ONLY, with zero deletions and zero
      modifications to any existing line. Transcribe `git diff --stat`.
    - `node scripts/check-brain-tool-liveness.cjs` still exits 0 on the current tree (unchanged
      by this task).
    - `grep -cP '\x{2014}' scripts/verify-release` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/verify-release && grep -q "Brain Tool Liveness Gate" scripts/verify-release && grep -c "BRAIN_LIVENESS_CODE" scripts/verify-release</automated>
  </verify>
  <done>The release gate carries a new numbered section that invokes the liveness check, branches on all three of its exit states, and treats a probe failure as a failure rather than a pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/test-239-verify-release-section-18.cjs and prove the gate bites end to end</name>
  <files>tests/test-239-verify-release-section-18.cjs</files>
  <read_first>
    - `scripts/verify-release` as edited by Task 1, and its SUMMARY block (the `FAIL > 0` to
      `exit 1` path that makes the section load-bearing).
    - `hooks/hooks.json` lines 225-355 (the two Brain matcher groups the mutation targets).
    - `scripts/check-brain-tool-liveness.cjs` (239-03) and
      `tests/test-239-brain-tool-liveness.cjs` (239-03), specifically its LEG 3 backup and
      `try/finally` restore pattern. Reuse that pattern; do not invent a second one.
    - `.planning/STATE.md` Phase 242 entry, the crash-injection precedent for mutating a real
      tracked file inside a test with a guaranteed restore, and the process note about not
      leaving a mutation on a shared working tree.
  </read_first>
  <action>
    Create `tests/test-239-verify-release-section-18.cjs`, plain Node with `node:assert` and
    `child_process.spawnSync`.

    LEG 1, STRUCTURAL: read `scripts/verify-release` as text and assert it contains a numbered
    `Brain Tool Liveness Gate` header, invokes `scripts/check-brain-tool-liveness.cjs`, and has
    both a zero-exit `pass` arm and at least two `fail` arms tied to `BRAIN_LIVENESS_CODE`.
    Assert the section's index in the file is after the Kuzu section and before PACKAGE-LOCK
    SYNC. Do NOT hardcode the section NUMBER as a literal; derive it from the header line with a
    regex, so the Phase 238 renumbering collision cannot rot this test.

    LEG 2, WIRED TO THE FAIL COUNTER: assert that the section's failure arms call the `fail`
    shell helper (not `warn`, not a bare `echo`), and that the file's SUMMARY block still exits
    1 when `FAIL` is greater than 0. This is the assertion that distinguishes a load-bearing gate
    from a printed message.

    LEG 3, THE BITE PROOF, end to end on the real tree, inside a `try/finally`:
      1. Back up `hooks/hooks.json` to a scratch path outside the repo tree.
      2. Rewrite ONE of the two Brain matchers to the superseded `mcp__brain_.*`.
      3. Run `node scripts/check-brain-tool-liveness.cjs` with `spawnSync`; assert `status === 1`.
      4. Run `bash scripts/verify-release` with `spawnSync` and a generous timeout; assert its
         exit status is NON-ZERO and that its stdout contains both the section header and the
         `Brain tool liveness gate FAILED` text. This is the proof that the check reaches the
         real release gate's FAIL counter, not merely that a script exits 1.
      5. `finally`: restore `hooks/hooks.json` from the backup unconditionally.
      6. After the restore, assert the file is byte-identical to the backup and re-run
         `node scripts/check-brain-tool-liveness.cjs` asserting `status === 0`.

    If a full `scripts/verify-release` run is not viable in this environment (it runs Claude
    Code's own plugin validator, `npm ci --dry-run`, and a dozen other gates, any of which may be
    unavailable), do NOT fake it and do NOT skip silently. Instead assert on the SPECIFIC
    evidence available: that `verify-release`'s stdout contains the section header and the
    `Brain tool liveness gate FAILED` line, and that its exit status is non-zero, EVEN IF other
    unrelated sections also failed. Record in the SUMMARY exactly which other sections failed and
    why they are unrelated and pre-existing, in the honest-residual style. A pre-existing
    unrelated failure is a stated fact, not a reason to weaken the assertion.

    LEG 4, ANTI-VACUITY: run `bash scripts/verify-release` on the CLEAN tree and assert its
    stdout contains the section's `pass` text. Without this, LEG 3 could pass because the section
    fails unconditionally.

    Print a per-leg PASS line. No em-dashes.
  </action>
  <acceptance_criteria>
    - `node tests/test-239-verify-release-section-18.cjs` exits 0, four legs.
    - LEG 3 transcript in the SUMMARY: the `check-brain-tool-liveness.cjs` status under mutation
      (expected 1), the `verify-release` exit status under mutation (expected non-zero), the
      exact `Brain tool liveness gate FAILED` stdout line, the post-restore byte-identity
      confirmation, and the post-restore `check-brain-tool-liveness.cjs` status of 0.
    - `git diff --stat hooks/hooks.json` is EMPTY at the end of the plan. No mutation may remain.
    - LEG 4 transcript: the clean-tree `pass` line observed in `verify-release` stdout.
    - If `verify-release` reports failures in sections OTHER than the new one, every such section
      is named in the SUMMARY with a one-line reason it is pre-existing and unrelated, and with
      evidence (for example a `git log` date on the relevant file predating this phase's first
      commit). Do not describe them as "expected" without that evidence.
    - The test contains no hardcoded section-number literal: `grep -cE "'18'|\"18\"|== *18" tests/test-239-verify-release-section-18.cjs`
      returns 0.
    - `bash tests/run-all-239.sh` shows the verify-release leg flipped from SKIPPED to PASSED,
      AND Leg B (`239 test-file completeness`, authored RED in 239-01) flipped from FAILED to
      PASSED, because this plan lands the fifth and final `tests/test-239-*.cjs` file.
      Transcribe the full 239 aggregator summary line. That flip is the contract between plan 01
      and this plan.
    - `grep -cP '\x{2014}' tests/test-239-verify-release-section-18.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-239-verify-release-section-18.cjs</automated>
  </verify>
  <done>The liveness check is proven to reach the real release gate's FAIL counter under a real mutation of a real tracked file, with a guaranteed restore and an anti-vacuity clean-tree check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release artifact to installed users | `scripts/verify-release` is the last checkpoint before a release ships to every install; a gate that does not block here is decorative |
| check-script exit code to the release decision | Exit 2 (probe failure) is an UNKNOWN, not a pass, and must be treated as a failure |
| a future rename to the matcher | The recurrence path for B-1; nothing in the product warns about it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-239-T1 | Information Disclosure | hook matchers surviving a future rename | mitigate | Author a numbered `verify-release` section invoking `scripts/check-brain-tool-liveness.cjs` and calling the `fail` helper, which increments `FAIL`, which makes the SUMMARY block exit 1. Acceptance criterion: LEG 3 mutates the real `hooks/hooks.json`, observes the real `verify-release` go non-zero with the named failure line, and restores. This is the mitigation RESEARCH.md rates HIGH and requires |
| T-239-07-A | Repudiation | a gate that degrades to a warning when it cannot measure | mitigate | The exit-2 probe-failure arm calls `fail`, not `warn`. A probe that cannot run proves nothing; treating it as a pass would recreate the silent-no-op shape this whole phase exists to kill (Phase 235 Decision (c)) |
| T-239-07-B | Repudiation | a gate that fails unconditionally | mitigate | LEG 4 asserts the clean tree produces the `pass` text, so LEG 3's red is caused by the mutation rather than by an always-red section |
| T-239-07-C | Tampering | a mutation left behind on a shared working tree | mitigate | LEG 3's mutation is wrapped in `try/finally` with an unconditional restore from a backup outside the repo tree, followed by a byte-identity assertion and a `git diff --stat` EMPTY acceptance criterion. This follows the Phase 242 precedent and its recorded process lesson |
| T-239-07-D | Tampering | section-number collision with Phase 238 | mitigate | Task 1 carries a MANDATORY first-check grep for an existing section 18 and a stated renumbering rule; LEG 1 derives the section number by regex rather than asserting a literal, so whichever phase executes second cannot rot this test |
| T-239-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; the phase-level Package Legitimacy Audit records the phase as install-free |

T2, T3, T4, T5 are owned by 239-02 through 239-06.
</threat_model>

<verification>
1. `bash -n scripts/verify-release` exits 0.
2. `node tests/test-239-verify-release-section-18.cjs` exits 0, four legs.
3. `node scripts/check-brain-tool-liveness.cjs` exits 0 on the restored tree.
4. `node tests/test-239-brain-tool-liveness.cjs` exits 0 (239-03 unregressed).
5. `git diff --stat hooks/hooks.json` EMPTY.
6. `git diff scripts/verify-release` is an insertion only, zero deletions.
7. `bash tests/run-all-239.sh` reports FAIL=0 with both 239-01 anti-vacuity legs now PASSED.
8. `grep -rlP '\x{2014}' scripts/verify-release tests/test-239-verify-release-section-18.cjs` returns nothing.
</verification>

<success_criteria>
Completes ROADMAP Success Criterion 1's "load-bearing" clause. SC1 asks that the liveness check
be load-bearing and that the B-1 dead-matcher shape cannot silently recur. 239-03 proved the
check is correct; this plan proves it BLOCKS. Without it, the check would prove the seam was
alive on the day the phase closed and nothing after that, which is exactly the decorative
posture Phase 235 SC3 and RESEARCH.md threat T1 both warn against.

PHASE-CLOSING NOTE for the SUMMARY, restating the standing hard rule: none of Phase 239's fixes
are live for any installed user until a release ships AND that user runs
`/plugin marketplace update` followed by `claude plugin update mos@mindrian-marketplace`.
`~/.claude/plugins/mos/` install caches and `dist/` still carry the old matcher until then. This
is a release-distribution fact, not a data migration, and it must be stated plainly rather than
implied by a green gate.

CROSS-PHASE RECONCILIATION RISK, restated so the orchestrator reports it: Phase 238 plan 238-06
also intends to author a `verify-release` section 18, in the same file. Task 1's mandatory
first-check grep and renumbering rule handle it, but whichever phase executes second must record
the reconciliation in its SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/239-brain-access-surface/239-07-SUMMARY.md` when done.
`.planning/` is gitignored in this repo, so commit it with `git add -f`.
</output>
