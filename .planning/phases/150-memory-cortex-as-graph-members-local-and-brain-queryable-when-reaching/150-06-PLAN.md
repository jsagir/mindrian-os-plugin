---
phase: 150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching
plan: 06
type: execute
wave: 3
depends_on: ["150-01", "150-02"]
files_modified:
  - lib/hmi/cortex-reach-adapter.cjs
  - lib/hmi/selector-dispatcher.cjs
  - scripts/intent-classifier.cjs
  - tests/test-150-selector-graph-driven.cjs
  - tests/test-150-render-unlock.cjs
autonomous: true
requirements: [MEM-06]
canon_parts: [Part 2, Part 3, Part 4, Part 7, Part 9, Part 10]

must_haves:
  truths:
    - "A NEW getRoomContext->reachScores adapter (cortex-reach-adapter.cjs) folds the projected cortex (via the Wave-2 legD cortexNodes) into roomState.reachScores -- the flat-file side-channel is retired/demoted to fallback only"
    - "resolveArchetype gains an OPTIONAL second arg cortexState; projected node-type presence can ESCALATE a reach's archetype above its static default; the static reach-component-map stays the floor; all existing callers pass undefined and are byte-stable"
    - "buildReachList -> dial-presenter is wired into the LIVE response surface (the decide() path at intent-classifier.cjs) so the navigator SEES the grounded one-move (D-08 render unlock)"
    - "Frozen 148 contracts are byte-unchanged: MAX_K=3, the 0.70/0.15 recommend gate, DIAL_REACH_K=6"
    - "The dial DECIDES through the projected cortex (cortexNodes present -> reachScores shift -> a different one-move) and the navigator SEES the presented move on the live surface"
  artifacts:
    - path: "lib/hmi/cortex-reach-adapter.cjs"
      provides: "getRoomContext cortexNodes -> roomState.reachScores adapter (a new caller, NOT an orchestrator edit); retires the flat-file side-channel"
      exports: ["buildReachScoresFromCortex"]
      min_lines: 40
    - path: "tests/test-150-render-unlock.cjs"
      provides: "Proves buildReachList -> dial-presenter reaches the live decide() response surface (the C2 render arm)"
      min_lines: 30
  key_links:
    - from: "lib/hmi/cortex-reach-adapter.cjs"
      to: "lib/core/navigation/room-context.cjs"
      via: "reads the legD cortexNodes (Wave-2) and folds them into roomState.reachScores priors"
      pattern: "cortexNodes|reachScores"
    - from: "scripts/intent-classifier.cjs"
      to: "lib/hmi/dial-presenter.cjs"
      via: "the decide() path renders buildReachList -> dial-presenter into the live response (D-08 unlock)"
      pattern: "dial-presenter|buildReachList"
---

<objective>
Make the 148 selector graph-driven AND surface the render (MEM-06, D-08). Three moves: (a) a NEW getRoomContext->reachScores adapter (lib/hmi/cortex-reach-adapter.cjs) folds the projected cortex -- via the Wave-2 legD cortexNodes -- into roomState.reachScores, retiring the flat-file side-channel; (b) resolveArchetype gains an OPTIONAL cortexState arg so projected node-type presence can ESCALATE a reach's archetype above its static default (the frozen reach-component-map stays the floor; cortex only escalates); (c) THE UNLOCK: wire buildReachList -> dial-presenter into the LIVE response surface (the decide() path at scripts/intent-classifier.cjs:1329) so the navigator SEES the grounded one-move. The frozen 148 contracts (MAX_K=3, 0.70/0.15, DIAL_REACH_K=6) stay byte-unchanged -- 150 FEEDS the selector, it never re-architects it.

Purpose: Today buildReachList (dial-reach-orchestrator.cjs:211) and dial-presenter have ZERO production callers -- the engine decides the one next move and the navigator NEVER SEES it (the C2 BLOCKED render arm). 148 built the UI machinery; 150 supplies the grounded signal AND the 148+150 pair wires the render into the live response. They only deliver together.
Output: cortex-reach-adapter.cjs (the new caller, not an orchestrator edit) + the resolveArchetype optional-arg escalation + the intent-classifier render wiring + the two RED-first tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-CONTEXT.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-UNDERSTANDING.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-LOOP-MAP.md

# The dial scoring seam (reachScores priors + buildReachList; FROZEN gate + caps -- read, never raise):
@lib/hmi/dial-reach-orchestrator.cjs
# The pure renderer with ZERO production callers (the C2 render arm to surface):
@lib/hmi/dial-presenter.cjs
# resolveArchetype + the reach-component-map (the static floor; add the OPTIONAL cortexState escalation arg):
@lib/hmi/selector-dispatcher.cjs
# The getRoomContext legs (Wave-2 adds legD cortexNodes ~after relevantNodes; this adapter reads it):
@lib/core/navigation/room-context.cjs
---
CROSS-WAVE NOTE (for the checker): scripts/intent-classifier.cjs is ALSO touched by Wave-2's
150-04 (the sensor ctx producers around the runNavigationEngine caller). 150-04 is Wave 2 and
150-06 is Wave 3, so they are SEQUENTIAL (different waves), never concurrent -- no same-wave
file conflict. 150-06 adds ONLY the render wiring at the decide() tail (emitEngineDecisionBlock /
runNavigationEngine at intent-classifier.cjs:1329); it must not re-touch the 150-04 sensor-ctx
lines. Both land in the same file across two waves by design.
---

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests -- selector graph-driven (MEM-06) + render unlock (D-08)</name>
  <files>tests/test-150-selector-graph-driven.cjs, tests/test-150-render-unlock.cjs</files>
  <read_first>
    - lib/hmi/dial-reach-orchestrator.cjs lines 56, 63-65, 138-151, 196-225 (DIAL_REACH_K=6; OFFERED_CAP=MAX_K=3; _resolveReachScore reads roomState.reachScores; buildReachList(roomState) ranks desc by score, offered_count=min(reaches, MAX_K=3))
    - lib/hmi/selector-dispatcher.cjs lines 125-137 (resolveArchetype(reachIdOrSubMode) -> archetype, default 'select' on miss -- the signature that gains the OPTIONAL cortexState second arg)
    - lib/hmi/dial-presenter.cjs (the pure renderer; the presented "one thing most worth your attention" shape the live surface must carry)
    - tests/test-dial-reach-orchestrator.cjs + tests/test-148-component-map.cjs (the node:assert/strict PASS-line style + how a frozen-contract assertion is written)
    - 150-UNDERSTANDING.md section 0 lines 8-12 (decide() at intent-classifier.cjs:1329 IS live-wired; buildReachList + dial-presenter have ZERO production callers; the render is unsurfaced) + the claim table C2 row line 71
  </read_first>
  <behavior>
    - Graph-driven (MEM-06): given a roomState whose reachScores were built by cortex-reach-adapter from cortexNodes, buildReachList ranks a DIFFERENT one-move than the same call with empty/flat reachScores (the cortex shifts the ranking). Assert MAX_K=3, the 0.70/0.15 recommend gate, and DIAL_REACH_K=6 are byte-unchanged (read the constants, assert their values).
    - Archetype escalation: resolveArchetype(reachKey) (no second arg) returns the static-map archetype byte-stable (the floor); resolveArchetype(reachKey, cortexState) with contradiction node-type presence ESCALATES the archetype above its static default; the static map is never lowered.
    - Render unlock (D-08): drive the decide() render path; assert buildReachList -> dial-presenter output is surfaced onto the live response surface (a spy/seam on dial-presenter proves it is invoked on the decide() path, with routing_source==='engine'). Honest-negative: with the cortex absent the path still renders the engine's one-move (does not crash), proving the wire, not a fixture.
    - No em-dash / en-dash anywhere in the edited sources (reference dash codepoints via String.fromCharCode).
  </behavior>
  <action>Create tests/test-150-selector-graph-driven.cjs (MEM-06) and tests/test-150-render-unlock.cjs (D-08) as plain CJS node:assert/strict suites asserting the behaviors above. RED-by-design: cortex-reach-adapter.cjs is absent, resolveArchetype has no second arg yet, and dial-presenter has no live caller, so both suites FAIL until Tasks 2-3 land. Seam on dial-presenter (spy) for the render-unlock proof. NO em-dashes; reference dash codepoints via String.fromCharCode.</action>
  <verify>
    <automated>node tests/test-150-selector-graph-driven.cjs; rc1=$?; node tests/test-150-render-unlock.cjs; rc2=$?; test $rc1 -ne 0 -a $rc2 -ne 0 && echo "PASS (both RED-by-design)"</automated>
  </verify>
  <done>both tests exist and are RED-by-design (adapter absent, resolveArchetype unchanged, dial-presenter unwired); each reports FAILED without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: cortex-reach-adapter.cjs + resolveArchetype optional cortexState escalation (MEM-06, D-02)</name>
  <files>lib/hmi/cortex-reach-adapter.cjs, lib/hmi/selector-dispatcher.cjs</files>
  <read_first>
    - lib/core/navigation/room-context.cjs lines 111-166 (legC getNeighborhood; the relevantNodes return; the Wave-2 legD adds a cortexNodes field after relevantNodes ~line 166 -- this adapter reads cortexNodes)
    - lib/hmi/dial-reach-orchestrator.cjs lines 138-151 (_resolveReachScore(def, roomState) reads roomState.reachScores -- the prior map reach_id -> 0..1; the adapter PRODUCES that map; the orchestrator stays a PURE renderer, no room.db, no edit)
    - lib/hmi/selector-dispatcher.cjs lines 125-137 (resolveArchetype signature; ARCHETYPE_DEFAULT='select'; _loadReachComponentMap registry-is-the-table; the static map is the floor)
    - 150-UNDERSTANDING.md section 1 lines 20-21 (the getRoomContext->reachScores ADAPTER is a NEW caller, not an orchestrator edit; resolveArchetype gains an OPTIONAL second arg -- node-type presence ESCALATES above the static default; optional arg defaults undefined so all existing callers + frozen 148 contracts are byte-stable; the static map stays the floor, cortex only escalates) + D-02 line 71
  </read_first>
  <action>Create lib/hmi/cortex-reach-adapter.cjs exporting buildReachScoresFromCortex(cortexNodes) -> { reach_id: score } (a normalized 0..1 prior map). It reads the projected cortex node types surfaced by the Wave-2 legD cortexNodes (governing_thought freshness, gap signals, persona, Brain priors, contradiction presence) and folds them into reachScores priors that roomState.reachScores then carries into buildReachList. This is a NEW caller (the adapter), NOT an edit to dial-reach-orchestrator.cjs (the orchestrator stays a pure renderer). The flat-file roomState side-channel is retired/demoted to fallback only -- when cortexNodes are present the adapter's scores win; when absent, the prior flat-file path is the fallback (D-02 "demote to fallback only where a frozen-148 contract requires it"). Do NOT raise or lower DIAL_REACH_K, MAX_K, or the 0.70/0.15 gate; the adapter only supplies the priors the frozen ranker already consumes.

  In lib/hmi/selector-dispatcher.cjs extend resolveArchetype to accept an OPTIONAL second argument cortexState: resolveArchetype(reachIdOrSubMode, cortexState). With cortexState undefined (every existing caller) it returns the static-map archetype byte-stable (the floor is unchanged). When cortexState is provided and carries projected node-type presence that warrants escalation (e.g. contradiction nodes present -> a 'select' reach escalates to 'confirm'), return the escalated archetype; never lower an archetype below its static default. Keep _loadReachComponentMap (registry-is-the-table) as the floor source. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-150-selector-graph-driven.cjs</automated>
  </verify>
  <done>cortex-reach-adapter.buildReachScoresFromCortex produces the reachScores priors from cortexNodes (flat-file side-channel demoted to fallback); resolveArchetype(reachKey) is byte-stable and resolveArchetype(reachKey, cortexState) escalates above the static floor; MAX_K/0.70/0.15/DIAL_REACH_K unchanged; test-150-selector-graph-driven.cjs GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: THE RENDER UNLOCK -- wire buildReachList -> dial-presenter into the live decide() surface (D-08)</name>
  <files>scripts/intent-classifier.cjs</files>
  <read_first>
    - scripts/intent-classifier.cjs lines 1320-1340 (emitEngineDecisionBlock / runNavigationEngine -- the decide() path that fires on every UserPromptSubmit at :1329; routing_source flips legacy->engine on a fired reach; this is the LIVE response surface where the render must be surfaced)
    - lib/hmi/dial-reach-orchestrator.cjs lines 196-225 (buildReachList(roomState) -> ReachList: reaches ranked, offered_count=min(reaches, MAX_K=3))
    - lib/hmi/dial-presenter.cjs (the pure renderer -- present the "one thing most worth your attention"; this is the ZERO-production-caller surface to wire)
    - lib/hmi/cortex-reach-adapter.cjs (Task 2 -- buildReachScoresFromCortex feeds roomState.reachScores from the legD cortexNodes)
    - the CROSS-WAVE NOTE in <context> (150-04 also edits this file in Wave 2 around the same runNavigationEngine caller -- add ONLY the render tail here; do not re-touch the sensor-ctx lines)
  </read_first>
  <action>In scripts/intent-classifier.cjs, at the decide() tail (the emitEngineDecisionBlock / runNavigationEngine path at ~:1329 that already fires live on every prompt), wire the render: after the engine produces its decision, build roomState.reachScores from the projected cortex via cortex-reach-adapter.buildReachScoresFromCortex(cortexNodes from getRoomContext legD), call buildReachList(roomState), and pass the result through dial-presenter so the grounded one-move is PRESENTED onto the live response surface (the navigator SEES it). This is the C2 unlock: buildReachList -> dial-presenter gains its FIRST production caller. Keep the existing best-effort/never-block discipline of the decide() path (a render failure must never block the user turn; wrap and degrade). Do NOT raise MAX_K/DIAL_REACH_K or touch the 0.70/0.15 gate. Do NOT re-touch the 150-04 sensor-ctx lines (different wave, same file). NO em-dashes.</action>
  <verify>
    <automated>node -c scripts/intent-classifier.cjs && node tests/test-150-render-unlock.cjs</automated>
  </verify>
  <done>scripts/intent-classifier.cjs parses; the decide() path renders buildReachList -> dial-presenter onto the live response surface (the navigator SEES the grounded one-move); the render is best-effort (never blocks); frozen 148 contracts unchanged; test-150-render-unlock.cjs GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cortexNodes (legD) -> reachScores | the adapter reads projected node-type presence + enum/scalar signals; it must not pass cortex prose into the dial |
| resolveArchetype cortexState arg -> archetype | escalation only; the static reach-component-map stays the floor; an existing caller (undefined arg) must be byte-stable |
| decide() render -> live response surface | the render must be best-effort; a render failure must never block the user turn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-06-01 | Tampering | a frozen 148 contract silently changed (MAX_K / 0.70/0.15 / DIAL_REACH_K) | mitigate | test-150-selector-graph-driven asserts the three constants byte-unchanged; the carried 148 frozen-contracts + drift fences are re-run by run-all-150.sh (150-08) |
| T-150-06-02 | Denial of Service | the render wiring blocking the user turn | mitigate | the decide() render is wrapped best-effort and degrades; mirrors the existing never-block discipline of the decide() path |
| T-150-06-03 | Information Disclosure | cortex prose leaking through the adapter into the dial | accept | the adapter reads only projected node-type presence + enum/scalar signals (no prose); the Part-8 grep sweep (150-08) covers the new files |
| T-150-06-04 | Tampering | resolveArchetype escalation lowering a static archetype | mitigate | the escalation path only raises; the test asserts the no-arg call is byte-stable and the static map is the floor |
| T-150-06-SC | Tampering | npm/pip/cargo installs | accept | zero package installs (CJS files + tests + one wiring edit); no package surface |
</threat_model>

<verification>
- node tests/test-150-selector-graph-driven.cjs -- cortex shifts the one-move; MAX_K=3 / 0.70/0.15 / DIAL_REACH_K=6 byte-unchanged; resolveArchetype escalation above the static floor
- node tests/test-150-render-unlock.cjs -- buildReachList -> dial-presenter reaches the live decide() surface (C2 render arm); honest-negative renders without crash when cortex absent
- node -c scripts/intent-classifier.cjs -- the render wiring parses
- no em-dashes in any edited source
</verification>

<success_criteria>
- cortex-reach-adapter folds the projected cortex (legD cortexNodes) into roomState.reachScores; the flat-file side-channel is retired/demoted to fallback (MEM-06, D-02)
- resolveArchetype gains the OPTIONAL cortexState arg; node-type presence escalates above the static floor; existing callers byte-stable (D-02)
- buildReachList -> dial-presenter is wired into the live decide() response surface so the navigator SEES the grounded one-move (D-08, the C2 unlock)
- frozen 148 contracts byte-unchanged (MAX_K=3, 0.70/0.15, DIAL_REACH_K=6)
- both tests green; no em-dashes
</success_criteria>

<output>
Create `.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-06-SUMMARY.md` when done
</output>
