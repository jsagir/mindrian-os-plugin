---
phase: 150-memory-cortex-as-graph-members
plan: 04
type: execute
wave: 2
depends_on: ["150-01"]
files_modified:
  - lib/core/navigation/room-context.cjs
  - scripts/intent-classifier.cjs
  - lib/core/navigation/projections.cjs
  - lib/core/navigation-engine.cjs
  - lib/core/navigation-engine-shared.cjs
  - tests/test-150-cortex-local-query.cjs
  - tests/test-150-orphans.cjs
autonomous: true
requirements: [MEM-03, MEM-07]
canon_parts: [Part 2, Part 3, Part 8, Part 9]

must_haves:
  truths:
    - "getRoomContext returns a new cortexNodes field SELECTing the 150-01 projected cortex node types, so the dial + decide() see the cortex from the graph"
    - "decide() sees real lowFillSections and venture_stage threaded from the STATE/USER cortex projection, so sensor-lagging-component + sensor-gate-approach FIRE instead of abstaining"
    - "resolveActiveFrameworks populates brainAnchors from the projected BRAIN-derivation cortex node, closing the orphaned consumer at projections.cjs:108"
    - "The dead SECTION_WEIGHTS import is removed from navigation-engine.cjs:64 and its export from navigation-engine-shared.cjs (composition stays rule-based per CONTEXT)"
    - "getRoomContext stays Part-8-safe: cortexNodes is LOCAL raw, never hashed, never crossing the wire"
  artifacts:
    - path: "lib/core/navigation/room-context.cjs"
      provides: "A legD/cortexNodes leg added after relevantNodes (~line 166) SELECTing the projected cortex node types as a new returned field"
      contains: "cortexNodes"
      min_lines: 30
    - path: "scripts/intent-classifier.cjs"
      provides: "The decide() ctx producer threads lowFillSections + venture_stage (+ stage/problem_type/complexity where derivable) into the context object (~:1217) from the STATE/USER projection"
      contains: "lowFillSections"
    - path: "lib/core/navigation/projections.cjs"
      provides: "resolveActiveFrameworks populates roomState.brainAnchors from the projected BRAIN-derivation cortex node (the orphaned consumer at :108 gets a producer)"
      contains: "brainAnchors"
  key_links:
    - from: "scripts/intent-classifier.cjs"
      to: "lib/core/navigation-engine.cjs decide() sensorCtx.lowFillSections / sensorTuple.stage"
      via: "context.lowFillSections + context.stage threaded from the cortex projection"
      pattern: "lowFillSections|venture_stage|\\bstage\\b"
    - from: "lib/core/navigation/room-context.cjs legD"
      to: "lib/core/navigation/memory-artifacts.cjs MEMORY_ARTIFACT_TYPES"
      via: "a type-scoped SELECT over the projected cortex nodes (the 150-01 node types)"
      pattern: "cortexNodes"
    - from: "lib/core/navigation/projections.cjs brainAnchors producer"
      to: "the projected BRAIN-derivation cortex node"
      via: "roomState.brainAnchors derived from the BRAIN memory_artifact node"
      pattern: "brainAnchors"
---

<objective>
Make the projected cortex LOCAL queryable when reaching (MEM-03) and close the 4 orphans the audit found (MEM-07). This is the consumption half that makes the substrate the rest of the phase needs.

- getRoomContext legD: add a `legD`/`cortexNodes` leg to lib/core/navigation/room-context.cjs (after relevantNodes ~line 166) SELECTing the 150-01 projected cortex node types, returned as a new `cortexNodes` field -- so the dial + decide() see the cortex from the graph, not flat files.
- Produce the STARVED sensor inputs: thread `lowFillSections` (from the STATE projection / gaps) and `venture_stage` (from the USER/STATE projection) into the decide() ctx at scripts/intent-classifier.cjs (~:1217) so sensor-lagging-component + sensor-gate-approach FIRE instead of abstaining (the decide() side already reads ctx.lowFillSections at :622 and ctx.stage at :618 -- the producer never set them).
- Wire the orphaned brainAnchors producer: projections.cjs:108 consumes roomState.brainAnchors but nothing produces it; derive it from the projected BRAIN-derivation cortex node.
- SECTION_WEIGHTS: delete the dead import (navigation-engine.cjs:64) and remove the dead export (navigation-engine-shared.cjs:86/242) -- composition stays rule-based per the CONTEXT; do NOT revive a weighted scorer.

Purpose: Closes loop links L2 (getRoomContext can see the cortex) and L3 (decide() sensors get real signal) from the 150-LOOP-MAP, plus the brainAnchors + SECTION_WEIGHTS orphans. Without legD the cortex is projected-but-unread; without the sensor ctx the 2 dead sensors keep abstaining.
Output: the legD leg, the decide()-ctx producer threading, the brainAnchors producer, the SECTION_WEIGHTS deletion, and two RED suites (test-150-cortex-local-query, test-150-orphans).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-CONTEXT.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-UNDERSTANDING.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-LOOP-MAP.md

# UPSTREAM (Wave 1, 150-01) -- the projected node types legD SELECTs over:
# lib/core/navigation/memory-artifacts.cjs -- MEMORY_ARTIFACT_TYPES {ROOM,STATE,MINTO,BRAIN,FEYNMAN,USER}
#   + the governing_thought / navigator_persona / decision node types + the node-id helpers.
@lib/core/navigation.cjs

# legD seam -- the three-leg fusion to extend (add legD after relevantNodes ~:166); stays LOCAL-raw, Part-8-safe (no egress projection helpers):
@lib/core/navigation/room-context.cjs
# The decide() consumers ALREADY reading the starved inputs (sensorCtx.lowFillSections :622, sensorTuple.stage :618, buildIntentPersona.venture_stage :189) -- the producer side never set them:
@lib/core/navigation-engine.cjs
# The decide() ctx PRODUCER -- thread lowFillSections + venture_stage into the context object (~:1217); the getRoomContext call site is :1275, roomContext rides context (D-03a LOCAL lane only):
@scripts/intent-classifier.cjs
# The orphaned brainAnchors consumer (:108) needing a producer; the resolveActiveFrameworks signal precedence:
@lib/core/navigation/projections.cjs
# The dead SECTION_WEIGHTS import (:64) and its shared export (:86 def, :242 export) -- delete both:
@lib/core/navigation-engine-shared.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests -- legD cortex surfacing + the 4 orphan closures</name>
  <files>tests/test-150-cortex-local-query.cjs, tests/test-150-orphans.cjs</files>
  <read_first>
    - tests/test-149-navigable.cjs (the in-room SELECT-and-assert pattern for a projected node type)
    - lib/core/navigation/room-context.cjs (the getRoomContext return shape to assert the new cortexNodes field on; the Part-8 LOCAL-raw discipline)
    - lib/core/insight-sensors.cjs (sensor-lagging-component + sensor-gate-approach -- how they read ctx.lowFillSections / the stage tuple, so the test asserts they FIRE on populated ctx and ABSTAIN on empty ctx)
    - lib/core/navigation/projections.cjs (resolveActiveFrameworks -- assert brainAnchors flows into the weight-0.5 brain_md signal when the BRAIN cortex node is present)
  </read_first>
  <behavior>
    - test-150-cortex-local-query.cjs: build a fixture room.db, project the cortex (call the 150-01 writers or the Plan 150-03 reconcile to seed memory_artifact / governing_thought / persona nodes), call getRoomContext(db, roomId, {...}); assert the return carries a cortexNodes field that is a non-empty array of the projected cortex node types (governing_thought / persona / BRAIN-derivation surfaced), and assert it is RAW-local (no sha256 hashing, the strings are present verbatim -- the Part-8 LOCAL invariant getRoomContext already holds for its other legs). On an empty room, cortexNodes is [] (graceful).
    - test-150-orphans.cjs: FOUR arms. (a) sensor-fire: build a ctx with populated lowFillSections + venture_stage threaded as the producer now threads them; assert dispatchSensors fires sensor-lagging-component (gaps) and sensor-gate-approach (stage); then build a ctx with both empty and assert both ABSTAIN (the before/after of the starvation fix). (b) brainAnchors: call resolveActiveFrameworks with a roomState carrying a projected BRAIN-derivation anchor; assert the framework appears at the weight-0.5 brain_md source (the orphaned consumer now has a producer). (c) SECTION_WEIGHTS gone: grep navigation-engine.cjs and navigation-engine-shared.cjs; assert SECTION_WEIGHTS is no longer imported/exported (the dead-code deletion). (d) decide()-threading: assert that a decide() ctx built the new way carries lowFillSections + stage (a thin assertion over the producer-side shape, or a decide() smoke that the threaded ctx reaches sensorCtx).
  </behavior>
  <action>Create the two CJS suites implementing the behaviors above. Build temp-file DatabaseSync fixtures (guard node:sqlite-unavailable exit 77), use node:assert/strict. For the sensor before/after arm, the load-bearing assertion is that the SAME sensors abstain on empty ctx and fire on the cortex-populated ctx (proving MEM-07's starvation fix). For the SECTION_WEIGHTS arm, grep the two engine files and assert zero matches for the import/export tokens (use grep -v '^#'-style comment filtering if asserting via shell; in-process prefer reading the file and asserting the require/export line is gone). NO em-dashes; reference asserted forbidden dash codepoints via String.fromCharCode. Both suites are RED until Tasks 2-4 land. REGISTER both suites into tests/run-all-150.sh (OWNED by Plan 150-01 -- add the test lines; if the aggregator does not yet exist, note it in the SUMMARY for the 150-01 executor).</action>
  <verify>
    <automated>node -e "require('node:fs').accessSync('tests/test-150-cortex-local-query.cjs');require('node:fs').accessSync('tests/test-150-orphans.cjs')" && { node tests/test-150-cortex-local-query.cjs; test $? -ne 0; }</automated>
  </verify>
  <done>Both suites exist and are RED-by-design (legD + the orphan closures absent). They assert: cortexNodes surfaces RAW-local; the 2 sensors fire on populated ctx and abstain on empty ctx; brainAnchors flows into resolveActiveFrameworks; SECTION_WEIGHTS is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getRoomContext legD -- surface the projected cortex (MEM-03)</name>
  <files>lib/core/navigation/room-context.cjs</files>
  <read_first>
    - lib/core/navigation/room-context.cjs lines 37-44 (legA shape) + 164-186 (the Leg C focus-node + the structured return where legD slots in after relevantNodes)
    - lib/core/navigation/memory-artifacts.cjs (MEMORY_ARTIFACT_TYPES + the governing_thought / navigator_persona / decision node types legD SELECTs over -- 150-01 contract)
    - lib/core/navigation/neighborhood.cjs (the in-room SELECT/getNeighborhood idiom legD mirrors -- caller-owned db, zero room.db open)
  </read_first>
  <action>Add a legD(db, roomId, opts) function to lib/core/navigation/room-context.cjs mirroring legA: a try/catch-wrapped, caller-owned-db, in-process SELECT that returns the projected cortex nodes for the room -- the memory_artifact nodes (kinds ROOM/STATE/MINTO/BRAIN/FEYNMAN/USER) plus the richer governing_thought / navigator_persona / decision projection nodes from the 150-01 writers. Use the SAME in-room query idiom as the shipped legs (do NOT open room.db; do NOT import the egress projection helpers -- legD returns RAW-local node fields exactly like legA/legC, the Part-8 LOCAL-reasoning discipline the module header already mandates). Call legD after relevantNodes (~line 166) and add a `cortexNodes` field to the structured return object (~line 170-185), defensively defaulting to [] on any partial-leg failure. Add a legD timing entry to _meta.legTimingsMs for parity. The return stays additive: every existing field (summary, recentMessages, relevantNodes, _meta) is byte-stable; cortexNodes is purely new, so the Phase 142 getRoomContext->decide() wire and the dial's existing reads are unaffected. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-150-cortex-local-query.cjs</automated>
  </verify>
  <done>getRoomContext returns a non-empty cortexNodes array (RAW-local, no hashing) when the cortex is projected, [] on an empty room; the existing legs/fields are unchanged; legD opens no room.db and imports no egress helper (stays Part-8 LOCAL).</done>
</task>

<task type="auto">
  <name>Task 3: Thread the starved sensor inputs into the decide() ctx (MEM-07 link L3)</name>
  <files>scripts/intent-classifier.cjs</files>
  <read_first>
    - scripts/intent-classifier.cjs lines 1209-1234 (the context object the producer builds and hands to decide(); the roomDb handle opened via openRoomDbForCaller; the getRoomContext call at :1275 whose result rides context.roomContext)
    - lib/core/navigation-engine.cjs lines 607-635 (the CONSUMER: sensorTuple.stage :618, sensorCtx.lowFillSections :622, dispatchSensors :629 -- these read ctx.stage / ctx.lowFillSections / ctx.roomDir which the producer currently never sets)
    - lib/core/navigation-engine.cjs lines 183-193 (buildIntentPersona reads context.userPersona.venture_stage :189 -- already wired from userPersona; confirm the producer's userPersona carries venture_stage from the USER projection)
  </read_first>
  <action>In scripts/intent-classifier.cjs, extend the `context` object (the one built ~:1217 and passed to decide()) so it carries the inputs decide() already reads but never received: thread `lowFillSections` (the gaps / low-fill section list derived from the STATE cortex projection) and `stage` (the venture_stage from the USER/STATE projection -- the same value carried on userPersona.venture_stage), plus `roomDir` for sensorCtx.roomDir, and `problem_type`/`complexity` where derivable from the existing userPersona/quadruple (leave undefined when unknown -- decide() defaults each field safely). Derive lowFillSections + venture_stage from the projected cortex: prefer the cortexNodes surfaced by the getRoomContext call already happening at :1275 (the STATE/USER projection rides cortexNodes from Plan Task 2), so this reuses the SAME getRoomContext result -- no second read, no new room.db open. Keep the D-03a LOCAL-lane fence: these sensor inputs ride context ONLY (the routing lane); they are NEVER added to the turn object or any path reaching buildBrainPacket (Part 8). Preserve the existing finally-close of roomDb (no leak). NO em-dashes.</action>
  <verify>
    <automated>node tests/test-150-orphans.cjs && node -e "const s=require('node:fs').readFileSync('scripts/intent-classifier.cjs','utf8'); if(!/lowFillSections/.test(s))process.exit(1)"</automated>
  </verify>
  <done>The decide() ctx built by intent-classifier carries lowFillSections + stage (+ roomDir) from the cortex projection; sensor-lagging-component + sensor-gate-approach FIRE on real signal instead of abstaining; the inputs stay on the LOCAL routing lane (never reach the Brain packet); roomDb is still closed in finally.</done>
</task>

<task type="auto">
  <name>Task 4: Wire the brainAnchors producer + delete the dead SECTION_WEIGHTS (MEM-07 orphans)</name>
  <files>lib/core/navigation/projections.cjs, lib/core/navigation-engine.cjs, lib/core/navigation-engine-shared.cjs</files>
  <read_first>
    - lib/core/navigation/projections.cjs lines 82-127 (resolveActiveFrameworks -- the 4-signal precedence; brainAnchors is the weight-0.5 brain_md signal consumed at :108-115 but no caller populates roomState.brainAnchors)
    - lib/core/navigation-engine.cjs line 64 (const SECTION_WEIGHTS = shared.SECTION_WEIGHTS; -- the dead import; grep the file to confirm zero other uses before deleting)
    - lib/core/navigation-engine-shared.cjs lines 86-95 (the SECTION_WEIGHTS frozen def) + line 242 (the export) + line 22 (the doc-comment line)
  </read_first>
  <action>Two orphan closures.

(1) brainAnchors producer: add a small producer that derives roomState.brainAnchors from the projected BRAIN-derivation cortex node (the BRAIN memory_artifact node's derivation anchors -- the framework-name handles Phase 90 derives). Place the producer where roomState is assembled for resolveActiveFrameworks (the caller that builds roomState from the cortex projection -- follow the existing roomState assembly; if roomState is assembled in projections.cjs itself or its caller, populate brainAnchors there from the cortexNodes BRAIN entry). The consumer at projections.cjs:108 is unchanged; it now receives a populated array, so the weight-0.5 brain_md signal becomes live. Keep it generic framework-name handles only (Part 8). If the cleanest producer site is the getRoomContext->roomState adapter rather than projections.cjs, do it there and keep projections.cjs as the pure consumer -- document the chosen site in the SUMMARY.

(2) SECTION_WEIGHTS deletion (composition stays rule-based per CONTEXT D-03): confirm via grep that SECTION_WEIGHTS has no live consumer beyond the dead import, then DELETE line navigation-engine.cjs:64 (the import) and remove the def (navigation-engine-shared.cjs:86-95), the export (:242), and the doc-comment reference (:22). Do NOT implement a weighted scorer -- the CONTEXT locks composition as rule-based; this is the implement-or-delete decision resolved to delete.

NO em-dashes. Both changes are additive-or-subtractive only; no frozen 148 contract (MAX_K, the 0.70/0.15 gate, DIAL_REACH_K) is touched.</action>
  <verify>
    <automated>node tests/test-150-orphans.cjs && ! grep -n "SECTION_WEIGHTS" lib/core/navigation-engine.cjs && ! grep -n "SECTION_WEIGHTS" lib/core/navigation-engine-shared.cjs</automated>
  </verify>
  <done>resolveActiveFrameworks receives a populated brainAnchors from the BRAIN-derivation cortex node (the orphaned consumer has a producer; the brain_md weight-0.5 signal is live); SECTION_WEIGHTS is fully removed from both engine files with no remaining import/export/def; no frozen 148 contract changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getRoomContext legD -> Larry in-process reasoning | cortexNodes is RAW-local node fields for in-process routing; LOCAL only, MUST NOT cross the wire (the module's Part-8 mandate) |
| intent-classifier ctx -> decide() | lowFillSections + stage ride context (the LOCAL routing lane) ONLY; the D-03a fence keeps them off the turn object and out of any buildBrainPacket path |
| projected BRAIN cortex node -> brainAnchors | only generic framework-name handles flow into the brain_md signal; no BRAIN.md prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-04-01 | Information Disclosure | legD cortexNodes crossing the wire | mitigate | legD returns RAW-local fields exactly like legA/legC and imports NO egress projection helper; test-150-cortex-local-query asserts the strings are present verbatim (in-process) and the room-context module header's Part-8 zero-egress invariant covers the new leg |
| T-150-04-02 | Information Disclosure | starved sensor inputs leaking to the Brain packet | mitigate | the D-03a LOCAL-lane fence: lowFillSections + stage ride context ONLY, never the turn object, never a path reaching buildBrainPacket; the existing Phase 142 fence already isolates context.roomContext the same way |
| T-150-04-03 | Tampering | brainAnchors carrying BRAIN.md prose | mitigate | the producer derives only generic framework-name handles from the BRAIN-derivation cortex node; no prose enters roomState.brainAnchors |
| T-150-04-04 | Denial of Service | legD blocking the 1200ms NAV budget | mitigate | legD is a single in-room SELECT wrapped in try/catch defaulting to []; getRoomContext is benchmarked ~1ms (~1200x under budget per UNDERSTANDING); a legD fault degrades cortexNodes to [] |
| T-150-04-05 | Tampering | SECTION_WEIGHTS deletion breaking a live consumer | mitigate | grep-confirm zero live consumer beyond the dead import before deleting; test-150-orphans asserts the tokens are gone and the engine suites stay green |
| T-150-04-SC | Tampering | npm/pip/cargo installs | accept | zero package installs; in-process edits to shipped navigation/engine modules only |
</threat_model>

<verification>
- `node tests/test-150-cortex-local-query.cjs` -- getRoomContext surfaces cortexNodes RAW-local
- `node tests/test-150-orphans.cjs` -- the 2 sensors fire on populated ctx + abstain on empty; brainAnchors populated; SECTION_WEIGHTS gone
- `grep -n "SECTION_WEIGHTS" lib/core/navigation-engine.cjs lib/core/navigation-engine-shared.cjs` -- returns nothing (dead code removed)
- `node -e "const s=require('node:fs').readFileSync('scripts/intent-classifier.cjs','utf8'); process.exit(/lowFillSections/.test(s)?0:1)"` -- the producer threads the starved input
- `bash tests/run-all-150.sh` -- the Phase 150 aggregator picks up both new suites (owned by 150-01; this plan registers them)
</verification>

<success_criteria>
- getRoomContext returns cortexNodes SELECTing the 150-01 projected cortex node types; the dial + decide() see the cortex from the graph (MEM-03); stays Part-8 LOCAL-raw
- decide() sensors get real lowFillSections + venture_stage from the STATE/USER projection; sensor-lagging-component + sensor-gate-approach fire instead of abstaining (MEM-07)
- resolveActiveFrameworks receives a populated brainAnchors from the BRAIN-derivation cortex node (orphan closed, brain_md weight-0.5 signal live)
- SECTION_WEIGHTS import + export + def removed; composition stays rule-based (implement-or-delete resolved to delete per CONTEXT)
- No frozen 148 contract (MAX_K=3, 0.70/0.15 gate, DIAL_REACH_K=6) touched
- The sensor inputs ride the LOCAL routing lane only (never the Brain packet); roomDb still closed in finally
- Zero em-dashes
</success_criteria>

<output>
Create `.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-04-SUMMARY.md` when done. Note in the SUMMARY: (a) the exact 150-01 node types legD SELECTs over, (b) the chosen brainAnchors producer site (projections.cjs vs the getRoomContext->roomState adapter), (c) confirmation that the lowFillSections/venture_stage values derive from the SAME getRoomContext call at intent-classifier:1275 (no second room.db read), and (d) THE OVERLAP FLAG: this plan edits scripts/intent-classifier.cjs (~:1217 ctx producer); the Wave-3 render/selector plan ALSO touches intent-classifier.cjs (the buildReachList->dial-presenter render wire) -- the Wave-3 executor must rebase on this plan's ctx-producer edit. Flag the exact line region touched so the checker can verify no clobber.
</output>
