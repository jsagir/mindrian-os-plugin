---
phase: 150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching
plan: 08
type: execute
wave: 3
depends_on: ["150-01", "150-02", "150-05", "150-06", "150-07"]
files_modified:
  - tests/claim-harness/run-all-claims.sh
  - tests/claim-harness/build-fixture-room-db.cjs
  - tests/claim-harness/claim-c1.cjs
  - tests/claim-harness/claim-c2.cjs
  - tests/claim-harness/claim-c3.cjs
  - tests/claim-harness/claim-c4.cjs
  - tests/claim-harness/claim-c5.cjs
  - tests/claim-harness/claim-c6.cjs
  - tests/claim-harness/claim-c7.cjs
  - tests/claim-harness/fixtures/claim-room/ROOM.md
  - scripts/doctor.cjs
  - tests/run-all-150.sh
autonomous: true
requirements: [MEM-09]
canon_parts: [Part 6, Part 8, Part 10]

must_haves:
  truths:
    - "tests/claim-harness/ ships: run-all-claims.sh (two-group, clone of run-all-146.sh), build-fixture-room-db.cjs (real room.db via navigation.cjs, MINDRIAN_ROOMS_HOME tmpdir, NO mocked Brain), and claim-c1..c7.cjs drivers each with an honest-negative arm"
    - "The fixture is one obviously-fictional claim-room incl. the contradicting pair + poison-nodes; the room.db is built via navigation.cjs (NOT hand-stitched SQLite)"
    - "The Brain arms (C4/C6 live) self-skip via class-m-brain-smoke when Brain is unreachable; the semantic claims C2-good and C4-relevance are carved out to the human gate (named, not faked)"
    - "doctor --claims is added to scripts/doctor.cjs (sibling of --acceptance, self-contained exit code, DOCTOR_CLAIM_FAIL_POINT self-test)"
    - "tests/run-all-150.sh is FINALIZED: every 150 suite + run-all-claims.sh as a group + the Part-8 grep sweep registered; the phase gate is one command"
  artifacts:
    - path: "tests/claim-harness/run-all-claims.sh"
      provides: "The two-group claim aggregator (clone of run-all-146.sh): the C1..C7 drivers + the gates"
      min_lines: 40
    - path: "tests/claim-harness/build-fixture-room-db.cjs"
      provides: "Builds a real room.db via navigation.cjs in a MINDRIAN_ROOMS_HOME tmpdir (NO mocked Brain)"
      min_lines: 40
    - path: "tests/run-all-150.sh"
      provides: "The FINALIZED Phase 150 aggregator: all 150 suites + run-all-claims.sh + the Part-8 grep sweep"
      min_lines: 50
  key_links:
    - from: "tests/claim-harness/build-fixture-room-db.cjs"
      to: "lib/core/navigation.cjs"
      via: "the fixture room.db is built through the navigation chokepoint, never hand-stitched SQLite"
      pattern: "navigation"
    - from: "scripts/doctor.cjs"
      to: "tests/claim-harness/run-all-claims.sh"
      via: "doctor --claims runs the harness as a self-contained exit-code gate (sibling of --acceptance)"
      pattern: "--claims|claim-harness"
---

<objective>
Build the claim harness and the phase gate (MEM-09, D-09). Ship tests/claim-harness/: run-all-claims.sh (clone the run-all-146.sh two-group aggregator), fixtures/claim-room/ (one obviously-fictional fixture + the contradicting pair + poison-nodes), build-fixture-room-db.cjs (a REAL room.db via navigation.cjs, MINDRIAN_ROOMS_HOME tmpdir, NO mocked Brain), and claim-c1..c7.cjs drivers (each with an honest-negative arm; the Brain arms self-skip via class-m-brain-smoke; carve out the semantic C2-good / C4-relevance claims to the human gate). Add doctor --claims to scripts/doctor.cjs (sibling of --acceptance, self-contained exit code, DOCTOR_CLAIM_FAIL_POINT self-test). FINALIZE tests/run-all-150.sh (register every 150 suite + run-all-claims.sh as a group + the Part-8 grep sweep).

Purpose: D-09 -- the claim harness IS the acceptance gate. Each public site claim becomes a falsifiable test that goes GREEN as the 150+148 bridge delivers; the marketing site becomes true by instrumentation, not by promise (Part 6 dog-fooding). The semantic-quality claims (C2 "is it good", C4 "relevance") are carved to the Part-10 human empathy gate, never faked. This plan also FINALIZES run-all-150.sh (created skeletal by 150-01) so the phase gate is one command.
Output: the tests/claim-harness/ tree + doctor --claims + the finalized run-all-150.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-CONTEXT.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-UNDERSTANDING.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-LOOP-MAP.md

# The two-group acceptance aggregator to clone (proves by instrumentation, not promise):
@tests/run-all-146.sh
# The real-room fixture discipline (copy a committed fixture to a tmpdir, set MINDRIAN_ROOMS_HOME on every spawn, NO mocked Brain):
@tests/test-cascade-surface-e2e.cjs
# The live-Brain precondition that honestly self-skips (L1-L5):
@lib/core/doctor/class-m-brain-smoke.cjs
---

<tasks>

<task type="auto">
  <name>Task 1: The fixture + build-fixture-room-db.cjs (real room.db via navigation.cjs, no mocked Brain)</name>
  <files>tests/claim-harness/fixtures/claim-room/ROOM.md, tests/claim-harness/build-fixture-room-db.cjs</files>
  <read_first>
    - tests/test-cascade-surface-e2e.cjs lines 1-50 (the real-room fixture discipline: copy the committed fixture tree to a tmpdir; set MINDRIAN_ROOMS_HOME=<tmpdir> on EVERY spawnSync env; without it scripts/resolve-room falls through to the user's real ~/MindrianRooms/; assert the env binding before every spawn)
    - lib/core/navigation.cjs (the chokepoint: every node/edge write routes here; the fixture room.db is built THROUGH this, never hand-stitched SQLite)
    - CLAUDE.md decision 15 (ICM Layer 0: every directory gets a ROOM.md) -- the fixture claim-room needs a ROOM.md identity
    - 150-UNDERSTANDING.md section 5 line 78 ("fixtures/claim-room/ (one obviously-fictional fixture incl. the contradicting pair + poison-nodes)") + the C3/C7 contradiction rows (lines 72, 76) + the C6 poison-seed row (line 75)
  </read_first>
  <action>Create tests/claim-harness/fixtures/claim-room/ as one obviously-fictional committed fixture room: a ROOM.md identity (ICM Layer 0) plus the 6 memory MD files (ROOM/STATE/MINTO/BRAIN/FEYNMAN/USER) carrying obviously-fictional content, INCLUDING the contradicting pair (two artifacts that conflict -- for C3/C7 contradiction detection) and poison-nodes (nodes whose properties carry FORBIDDEN_SUBSTRINGS like 'SECRET CLAIM PROSE' / 'leak@example.com' -- for the C6 egress arm). Create tests/claim-harness/build-fixture-room-db.cjs that copies the committed fixture to a tmpdir, sets MINDRIAN_ROOMS_HOME=<tmpdir>, and builds a REAL room.db by projecting the fixture's 6 memory files THROUGH lib/core/navigation.cjs (the Wave-1/Wave-2 memory_artifact + reconcile path) -- NOT a hand-stitched SQLite file. Export buildFixtureRoomDb() -> {roomDir, dbPath, cleanup}. Guard node:sqlite-unavailable with exit 77. NO mocked Brain anywhere. NO em-dashes.</action>
  <verify>
    <automated>node -e "const b=require('./tests/claim-harness/build-fixture-room-db.cjs'); if(typeof b.buildFixtureRoomDb!=='function') throw new Error('buildFixtureRoomDb missing'); const r=b.buildFixtureRoomDb(); if(!r.dbPath) throw new Error('no dbPath'); r.cleanup&&r.cleanup(); console.log('PASS real fixture room.db built via navigation.cjs');"; rc=$?; test $rc -eq 0 -o $rc -eq 77</automated>
  </verify>
  <done>fixtures/claim-room/ exists (fictional, ROOM.md + 6 memory files + the contradicting pair + poison-nodes); build-fixture-room-db.cjs builds a real room.db via navigation.cjs in a MINDRIAN_ROOMS_HOME tmpdir; no mocked Brain; no em-dashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: claim-c1..c7.cjs drivers + run-all-claims.sh (the C1..C7 acceptance gate, RED-first)</name>
  <files>tests/claim-harness/claim-c1.cjs, tests/claim-harness/claim-c2.cjs, tests/claim-harness/claim-c3.cjs, tests/claim-harness/claim-c4.cjs, tests/claim-harness/claim-c5.cjs, tests/claim-harness/claim-c6.cjs, tests/claim-harness/claim-c7.cjs, tests/claim-harness/run-all-claims.sh</files>
  <read_first>
    - 150-UNDERSTANDING.md section 5 lines 66-80 (the 7-claim table -> falsifiable assertions: C1 resume focus-node identity; C2 decide() one-move references a real node + the verb CHANGES + the navigator SEES it presented; C3 contradiction edge minted; C4 Brain advisory chain handles Part-8-clean + degrades offline; C5 K artifacts -> >=K typed nodes (the completeness invariant); C6 poison-seeded nodes -> zero in the serialized packet; C7 file A then conflicting B -> INFORMS/CONTRADICTS edge minted mid-session) + the carve-out note line 80 (C2-good + C4-relevance are the human gate, not machine-tested)
    - tests/run-all-146.sh lines 47-97 (the two-group structure: CJS_SUITES + SHELL_SUITES; the PASS/FAIL loop that runs each to completion even on failure and exits non-zero on any failure; the honest-negative + self-skip discipline)
    - lib/core/doctor/class-m-brain-smoke.cjs lines 13-46 (L1-L5; the honest self-skip when Brain unreachable -- the Brain arms of C4/C6 gate on this)
    - tests/test-cascade-surface-e2e.cjs (the MINDRIAN_ROOMS_HOME spawn discipline the drivers reuse via build-fixture-room-db.cjs)
    - the Wave-1/2/3 deliverables under test: the memory_artifact writer (150-01/02), getRoomContext legD + the cortex packet (150-03/04), the render unlock (150-06), readQuintuple (150-07)
  </read_first>
  <behavior>
    - Each claim-cN.cjs drives a REAL shipped unit against the build-fixture-room-db.cjs real room.db (no mock of the unit under test) and carries an honest-negative arm (the claim can FAIL on a broken bridge, proving the assertion is real).
    - C1 (picks up where you left off): the resumed focus node id == the node id written the prior session (graph identity, not string-match).
    - C2 (next move grounded + SEEN): decide() returns a reach with routing_source==='engine' whose justification references a REAL fixture node AND the buildReachList -> dial-presenter render reaches the live surface (the D-08 render arm); the C2 "is it good" semantic arm is CARVED OUT to the human gate (the driver names it skipped, not faked).
    - C3/C7 (contradiction/cascade): filing the contradicting pair mints a CONTRADICTS / INFORMS edge in room.db, queryable.
    - C4 (Brain surfaces patterns): the cortex packet returns advisory chain handles, Part-8-clean, degrades gracefully offline; the live Brain arm self-skips via class-m-brain-smoke; the C4 "relevance" semantic arm is CARVED OUT to the human gate.
    - C5 (every claim indexed): K artifacts filed -> >= K typed nodes with Part-9 review_status (the completeness invariant).
    - C6 (Brain never sees content): the poison-seeded fixture nodes -> the serialized cortex packet contains NONE of the FORBIDDEN_SUBSTRINGS; source grep for forbidden require/fetch/http.
    - run-all-claims.sh runs the 7 drivers (group a) + the gates (group b), runs each to completion on failure, prints per-suite PASS/FAIL + a tally, exits non-zero on any failure.
    - No em-dash / en-dash anywhere (reference dash codepoints via String.fromCharCode).
  </behavior>
  <action>Write the seven tests/claim-harness/claim-cN.cjs drivers as plain CJS node:assert/strict suites asserting the per-claim behaviors above, each building the real fixture room.db via build-fixture-room-db.cjs (Task 1) and each carrying an honest-negative arm. The Brain arms (C4 live, C6 live-packet) gate on lib/core/doctor/class-m-brain-smoke.cjs and self-skip honestly when Brain is unreachable. The semantic claims (C2 "is it good", C4 "relevance") are NOT machine-asserted -- the driver prints a carved-out SKIP line naming the Part-10 human empathy gate, never a fake PASS. RED-first: the drivers are written before the full bridge is green, so a claim turns GREEN only as its 150 deliverable lands. Create tests/claim-harness/run-all-claims.sh by cloning the tests/run-all-146.sh two-group structure (set -uo pipefail, no emoji, no em-dashes): group (a) the C1..C7 drivers; group (b) the gates (the Part-8 egress sweep over the cortex packet path + class-m-brain-smoke as the live-Brain precondition). Run each to completion on failure, per-suite PASS/FAIL line, final tally, exit 1 on any failure. NO em-dashes.</action>
  <verify>
    <automated>for n in 1 2 3 4 5 6 7; do node -c tests/claim-harness/claim-c$n.cjs || exit 1; done && bash -n tests/claim-harness/run-all-claims.sh && ! grep -P "\xE2\x80\x94" tests/claim-harness/run-all-claims.sh tests/claim-harness/claim-c*.cjs && echo "PASS drivers parse + aggregator parses + no em-dashes"</automated>
  </verify>
  <done>claim-c1..c7.cjs exist (each drives a real unit on the real fixture room.db with an honest-negative arm; Brain arms self-skip via class-m-brain-smoke; C2-good + C4-relevance carved to the human gate, named not faked); run-all-claims.sh is the two-group aggregator; no em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: doctor --claims gate + FINALIZE run-all-150.sh (MEM-09 phase gate)</name>
  <files>scripts/doctor.cjs, tests/run-all-150.sh</files>
  <read_first>
    - scripts/doctor.cjs lines 211, 277-286, 2360, 2957-2963, 3594 (the --acceptance flag parse; the help text; buildAcceptanceChecklist; runAcceptance with its OWN self-contained exit-code contract; the flags.acceptance dispatch -- mirror this for --claims as a SIBLING with its own exit code) + the DOCTOR_TEST_FAIL_POINT self-test hook pattern (the precedent for DOCTOR_CLAIM_FAIL_POINT)
    - tests/run-all-146.sh lines 47-97, 130-136 (the CJS_SUITES + SHELL_SUITES arrays; the run-to-completion loop; the exit-1-on-any-failure tail; the standalone Part-8 grep sweep step)
    - tests/run-all-150.sh (the SKELETAL aggregator created by 150-01; this task FINALIZES it -- registers every 150 suite + the claim harness group + the Part-8 sweep)
    - the full 150 suite inventory: test-150-connector.cjs (05), test-150-selector-graph-driven.cjs + test-150-render-unlock.cjs (06), test-150-feynman-readback.cjs (07), plus the Wave-1/2 suites (the memory_artifact writer + reconcile + legD + the cortex Brain-egress test from 150-01..04) and the carried 148 frozen-contracts + reach-ids drift fences (re-run to assert MAX_K=3 / DIAL_REACH_K=6 unchanged)
  </read_first>
  <action>Add a doctor --claims gate to scripts/doctor.cjs as a SIBLING of --acceptance: parse the --claims flag (mirror line 211), add help text (mirror lines 277-286), and add a runClaims(opts) dispatch (mirror runAcceptance at 2957) that shells tests/claim-harness/run-all-claims.sh and returns its OWN self-contained exit code (not folded into the class roster). Add a DOCTOR_CLAIM_FAIL_POINT env self-test hook mirroring the DOCTOR_TEST_FAIL_POINT pattern so the gate's own failure path is testable. Then FINALIZE tests/run-all-150.sh (the skeletal aggregator 150-01 created): clone the run-all-146.sh two-group shape and register (a) every 150 CJS suite (the Wave-1/2 memory_artifact + reconcile + legD + cortex Brain-egress suites; test-150-connector; test-150-selector-graph-driven; test-150-render-unlock; test-150-feynman-readback); (b) tests/claim-harness/run-all-claims.sh as a SHELL_SUITES group; (c) the carried 148 frozen-contracts + reach-ids drift fences (re-run to assert MAX_K=3 / DIAL_REACH_K=6 byte-unchanged); (d) a standalone Part-8 grep sweep over ALL the new 150 artifacts (the cortex packet path + the new lib edits + the connector + the seed-writer) for forbidden user-content-to-Brain tokens. Run each to completion on failure; per-suite PASS/FAIL line; final tally; exit 1 on any failure. A suite not yet created gates to a FAIL line (not a crash) so the phase gate flags incompleteness. NO em-dashes.</action>
  <verify>
    <automated>node -c scripts/doctor.cjs && grep -q "claims" scripts/doctor.cjs && bash -n tests/run-all-150.sh && grep -q "run-all-claims.sh" tests/run-all-150.sh && grep -q "test-150-render-unlock" tests/run-all-150.sh && ! grep -P "\xE2\x80\x94" tests/run-all-150.sh && echo "PASS doctor --claims + finalized run-all-150.sh"</automated>
  </verify>
  <done>scripts/doctor.cjs has a --claims gate (sibling of --acceptance, self-contained exit code, DOCTOR_CLAIM_FAIL_POINT self-test); tests/run-all-150.sh is finalized (every 150 suite + run-all-claims.sh group + carried 148 fences + Part-8 sweep); the phase gate is one command; no em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture build -> room.db | the fixture room.db must be built via navigation.cjs (the chokepoint), never hand-stitched SQLite, or the harness tests a fiction |
| poison-seeded nodes -> cortex packet | the C6 arm seeds FORBIDDEN_SUBSTRINGS into fixture node props; the serialized packet must contain none (Part 8) |
| semantic claim -> machine assertion | C2-good + C4-relevance must be carved to the human gate, never faked as a machine PASS |
| MINDRIAN_ROOMS_HOME -> real user room | every spawn must bind MINDRIAN_ROOMS_HOME to the tmpdir or the cascade fires against the user's real room |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-08-01 | Spoofing | a hand-stitched SQLite fixture pretending to be a real room.db | mitigate | build-fixture-room-db.cjs builds through navigation.cjs; Task 1 verify asserts the dbPath came from the chokepoint path |
| T-150-08-02 | Information Disclosure | the cortex packet egressing poison-seeded fixture prose | mitigate | the C6 driver asserts JSON.stringify(packet) contains none of the FORBIDDEN_SUBSTRINGS + greps the builder for forbidden require/fetch/http; the run-all-150 Part-8 sweep covers all new files |
| T-150-08-03 | Repudiation | a faked PASS on a semantic claim (C2-good / C4-relevance) | mitigate | the drivers print a carved-out SKIP naming the Part-10 human empathy gate; no machine PASS is emitted for the semantic arms |
| T-150-08-04 | Elevation of Privilege | the harness firing against the user's real ~/MindrianRooms/ | mitigate | every spawn binds MINDRIAN_ROOMS_HOME to the tmpdir (the test-cascade-surface-e2e discipline); asserted before each spawn |
| T-150-08-05 | Denial of Service | a missing Brain blocking the gate | mitigate | the Brain arms self-skip honestly via class-m-brain-smoke; the hermetic arms carry the gate CI-green without a live Brain |
| T-150-08-SC | Tampering | npm/pip/cargo installs | accept | zero package installs (CJS drivers + bash aggregator + a doctor flag + a fixture tree); no package surface |
</threat_model>

<verification>
- node -c on all seven claim-cN.cjs drivers + bash -n on run-all-claims.sh and run-all-150.sh
- node -e build-fixture-room-db.buildFixtureRoomDb builds a real room.db via navigation.cjs (or exit 77 on no node:sqlite)
- node -c scripts/doctor.cjs + grep confirms the --claims gate
- grep confirms run-all-150.sh registers run-all-claims.sh + test-150-render-unlock + the carried 148 fences + the Part-8 sweep
- no em-dashes anywhere in the harness, the doctor edit, or run-all-150.sh
</verification>

<success_criteria>
- tests/claim-harness/ ships: run-all-claims.sh (two-group clone of run-all-146.sh), build-fixture-room-db.cjs (real room.db via navigation.cjs, MINDRIAN_ROOMS_HOME tmpdir, NO mocked Brain), fixtures/claim-room/ (fictional + contradicting pair + poison-nodes), claim-c1..c7.cjs (honest-negative arms; Brain arms self-skip; C2-good + C4-relevance carved to the human gate) (MEM-09, D-09)
- doctor --claims is a sibling of --acceptance (self-contained exit code, DOCTOR_CLAIM_FAIL_POINT self-test)
- tests/run-all-150.sh is finalized: every 150 suite + run-all-claims.sh group + carried 148 fences (MAX_K=3 / DIAL_REACH_K=6) + the Part-8 grep sweep; the phase gate is one command
- no em-dashes anywhere
</success_criteria>

<output>
Create `.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-08-SUMMARY.md` when done
</output>
