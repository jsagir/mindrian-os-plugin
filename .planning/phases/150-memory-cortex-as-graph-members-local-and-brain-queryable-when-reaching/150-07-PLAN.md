---
phase: 150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching
plan: 07
type: execute
wave: 3
depends_on: ["150-01", "150-02"]
files_modified:
  - lib/core/folder-memory.cjs
  - lib/core/folder-memory-async.cjs
  - lib/core/feynman/feynman-seed-writer.cjs
  - tests/test-150-feynman-readback.cjs
autonomous: true
requirements: [MEM-08]
canon_parts: [Part 4, Part 7, Part 9]

must_haves:
  truths:
    - "FEYNMAN.md joins the read contract: readQuintuple(sectionPath) -> {room, state, reasoning, brain, feynman} extends readQuadruple additively; readTriple + readQuadruple semantics are byte-preserved"
    - "readQuintuple extracts the human-authored FEYNMAN body (the bodyOutsideSentinels region), NOT the auto Timeline section"
    - "The missing seed-writer discover.md:170 promises ships (lib/core/feynman/feynman-seed-writer.cjs) so fresh rooms get a FEYNMAN body"
    - "The FEYNMAN body freshness + the timeline stale-flag rows are projected as typed graph signals (the write-only sink stops being write-only)"
    - "The sync and async folder-memory entry points stay key-set parity (every async export is an AsyncFunction)"
  artifacts:
    - path: "lib/core/folder-memory.cjs"
      provides: "readQuintuple(sectionPath) -> {room, state, reasoning, brain, feynman} (additive on readQuadruple; readTriple/readQuadruple byte-preserved)"
      exports: ["readQuintuple"]
    - path: "lib/core/feynman/feynman-seed-writer.cjs"
      provides: "The missing seed-writer (discover.md:170 promise) -- writes a FEYNMAN body for a fresh section via the shipped folder-memory writer"
      min_lines: 30
    - path: "tests/test-150-feynman-readback.cjs"
      provides: "Proves FEYNMAN read-back + the seed-writer + the body-freshness graph signal"
      min_lines: 30
  key_links:
    - from: "lib/core/folder-memory.cjs"
      to: "the FEYNMAN body (bodyOutsideSentinels)"
      via: "readQuintuple extracts the human-authored body region, not the auto Timeline"
      pattern: "readQuintuple|feynman"
    - from: "lib/core/feynman/feynman-seed-writer.cjs"
      to: "the FEYNMAN body freshness graph signal"
      via: "the projected body-freshness + stale-flag rows close the write-only sink"
      pattern: "feynman|freshness|stale"
---

<objective>
Stop the FEYNMAN.md write-only sink (MEM-08). Add readQuintuple (folding FEYNMAN into the read contract) in lib/core/folder-memory.cjs + folder-memory-async.cjs, extracting the human-authored body (the bodyOutsideSentinels region, NOT the auto Timeline). Ship the MISSING seed-writer that discover.md:170 promises (lib/core/feynman/feynman-seed-writer.cjs) so fresh rooms get a FEYNMAN body. Project the body freshness + the timeline stale-flag rows as typed graph signals so the timeline stops being a dead-end sink.

Purpose: FEYNMAN.md has 2 writers (timeline-runner + dial-memory-runner) and 0 genuine consumers -- it is not in readTriple/readQuadruple, and the seed-writer discover.md:170 claims doesn't exist, so fresh rooms get no auto-section at all. This plan closes the read-back loop: FEYNMAN joins the read contract, fresh rooms get seeded, and the body freshness becomes a navigable graph signal. Mostly an additive extension of the shipped readQuadruple (Part 7 reuse); it does NOT re-derive the FEYNMAN timeline (Phase 124 owns the renderer).
Output: readQuintuple (sync + async, additive) + the seed-writer + the body-freshness graph projection + the RED-first test-150-feynman-readback.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-CONTEXT.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-UNDERSTANDING.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-LOOP-MAP.md

# The read contract to extend (readTriple/readQuadruple; readQuintuple is additive on top):
@lib/core/folder-memory.cjs
# The async twin (key-set parity -- every export an AsyncFunction):
@lib/core/folder-memory-async.cjs
# The FEYNMAN sentinel-bounded body the renderer byte-preserves (the bodyOutsideSentinels region readQuintuple extracts):
@lib/core/feynman/timeline-renderer.cjs
# discover.md:170 -- the promise that FEYNMAN is seeded "via the shipped lib/core/folder-memory.cjs writer", never hand-written (the seed-writer this plan ships):
@commands/discover.md
---

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED test -- FEYNMAN read-back + seed-writer + body-freshness signal (MEM-08)</name>
  <files>tests/test-150-feynman-readback.cjs</files>
  <read_first>
    - lib/core/folder-memory.cjs lines 84-91, 185-255, 409-415 (readTriple {room, state, reasoning}; the Phase 90-04 readQuadruple additive extension that composes on readTriple and byte-preserves it; the module.exports object)
    - lib/core/folder-memory-async.cjs lines 73-79, 166-182, 331-336 (the async readTriple/readQuadruple twins; the AsyncFunction key-set-parity discipline; the exports)
    - lib/core/feynman/timeline-renderer.cjs (the sentinel-bounded ## Timeline (auto) section; the human-authored body OUTSIDE the sentinels -- readQuintuple extracts that region, NOT the auto Timeline)
    - tests/test-feynman-timeline-canon-part-9-invariant.cjs (the byte-preservation assertion style + the node:assert/strict PASS-line style)
  </read_first>
  <behavior>
    - readQuintuple shape: readQuintuple(sectionPath) returns {room, state, reasoning, brain, feynman}; the first four fields equal readQuadruple's output byte-for-byte (additive, never mutates the prior contract); feynman carries the human-authored body extracted from the bodyOutsideSentinels region (the auto Timeline section is excluded).
    - Async parity: the async folder-memory-async.readQuintuple returns the same shape; every async export is an AsyncFunction (constructor.name === 'AsyncFunction').
    - Seed-writer: feynman-seed-writer.cjs writes a FEYNMAN body for a fresh section via the shipped folder-memory writer (never hand-writes the file); after seeding, readQuintuple(sectionPath).feynman is non-empty.
    - Body-freshness signal: the projected body-freshness + timeline stale-flag rows are surfaced as typed graph signals (assert the projection emits a freshness scalar / stale-flag the cortex can read -- the sink is no longer write-only).
    - No em-dash / en-dash in any new source (reference dash codepoints via String.fromCharCode).
  </behavior>
  <action>Create tests/test-150-feynman-readback.cjs as a plain CJS node:assert/strict suite asserting the behaviors above. RED-by-design: readQuintuple and feynman-seed-writer.cjs do not exist yet, so the suite FAILS until Tasks 2-3 land. Use a tmpdir fixture section with a FEYNMAN.md carrying both a human body and a sentinel-bounded auto Timeline, and assert readQuintuple extracts the body region only. Guard node:sqlite-unavailable with exit 77. NO em-dashes; reference dash codepoints via String.fromCharCode.</action>
  <verify>
    <automated>node tests/test-150-feynman-readback.cjs; test $? -ne 0 && echo "PASS (RED-by-design)"</automated>
  </verify>
  <done>tests/test-150-feynman-readback.cjs exists and is RED-by-design (readQuintuple + seed-writer absent); the suite reports FAILED without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: readQuintuple -- fold FEYNMAN into the read contract, sync + async (MEM-08)</name>
  <files>lib/core/folder-memory.cjs, lib/core/folder-memory-async.cjs</files>
  <read_first>
    - lib/core/folder-memory.cjs lines 185-255 (the readQuadruple additive pattern: it calls readTriple then adds the brain field WITHOUT changing readTriple's bytes; mirror this EXACTLY for readQuintuple adding the feynman field on top of readQuadruple) + lines 409-415 (add readQuintuple to module.exports)
    - lib/core/folder-memory-async.cjs lines 166-182 (the async readQuadruple twin; the comment that every exported name must be an AsyncFunction) + lines 331-336 (the async exports)
    - lib/core/feynman/timeline-renderer.cjs (how the sentinel markers bound the ## Timeline (auto) section; the human body is everything OUTSIDE the sentinels -- readQuintuple extracts that bodyOutsideSentinels region)
    - 150-CONTEXT.md mechanism item 9 + Scope item (readQuintuple or fold; the human-authored body freshness + stale-flag rows projected as typed graph signals)
  </read_first>
  <action>In lib/core/folder-memory.cjs add readQuintuple(sectionPath) -> {room, state, reasoning, brain, feynman} as an ADDITIVE extension of readQuadruple (the exact additive idiom readQuadruple used on readTriple): call readQuadruple, spread its four fields UNCHANGED (byte-preserve the prior contract), and add a fifth feynman field carrying the human-authored FEYNMAN body. The body is the bodyOutsideSentinels region -- everything OUTSIDE the ## Timeline (auto) sentinel markers (reuse the sentinel boundaries the timeline-renderer.cjs already defines; do NOT extract the auto Timeline). Never throw (mirror readQuadruple's never-throw discipline; empty feynman on a missing file). Add readQuintuple to module.exports. Then add the async twin in lib/core/folder-memory-async.cjs mirroring the async readQuadruple wrapper so every exported name stays an AsyncFunction (key-set parity), and add it to the async exports. Do NOT alter readTriple or readQuadruple bytes. NO em-dashes.</action>
  <verify>
    <automated>node -e "const f=require('./lib/core/folder-memory.cjs'); if(typeof f.readQuintuple!=='function') throw new Error('sync readQuintuple missing'); const a=require('./lib/core/folder-memory-async.cjs'); if(a.readQuintuple.constructor.name!=='AsyncFunction') throw new Error('async readQuintuple not AsyncFunction'); console.log('PASS readQuintuple sync+async');"</automated>
  </verify>
  <done>readQuintuple exists in both sync + async folder-memory (additive on readQuadruple, key-set parity, async export is AsyncFunction); readTriple + readQuadruple bytes unchanged; the feynman field carries the bodyOutsideSentinels human body.</done>
</task>

<task type="auto">
  <name>Task 3: The missing seed-writer + the body-freshness graph signal (MEM-08, discover.md:170)</name>
  <files>lib/core/feynman/feynman-seed-writer.cjs</files>
  <read_first>
    - commands/discover.md lines 165-175 (DISC-10: "Seed the room's per-section FEYNMAN.md from it via the shipped lib/core/folder-memory.cjs writer -- never hand-write FEYNMAN.md" -- the promise this seed-writer fulfills)
    - lib/core/feynman/timeline-runner.cjs + lib/core/feynman/dial-memory-runner.cjs (the two EXISTING write-only FEYNMAN writers + the atomic-write-inside-the-sentinels idiom; the seed-writer mirrors their write discipline but seeds the human BODY region, not the auto Timeline)
    - lib/core/folder-memory.cjs (the shipped writer the seed-writer must route through per discover.md:170; never a hand-written file)
    - lib/core/navigation.cjs (the chokepoint -- the body-freshness + stale-flag rows project as typed graph signals through navigation.cjs, system-bookkeeping carve-out created_by=system; no Part-9 role-5 issue)
    - 150-LOOP-MAP.md cluster 1 line 45 (FEYNMAN is write-only; the discover.md:170 seed-writer doesn't exist; readQuintuple closes the read-back) + 150-CONTEXT.md mechanism item 9
  </read_first>
  <action>Create lib/core/feynman/feynman-seed-writer.cjs that ships the missing discover.md:170 seed-writer: given a fresh section path (and the plain-language seed content DISC-10 produces), it writes a FEYNMAN body for that section THROUGH the shipped lib/core/folder-memory.cjs writer (never hand-writes the file, per discover.md:170), so fresh rooms get a FEYNMAN body instead of no auto-section at all. It writes the human-body region, leaving any sentinel-bounded ## Timeline (auto) section to the Phase 124 timeline-runner (do not re-derive the timeline). Then project the body freshness + the timeline stale-flag rows as typed graph signals via lib/core/navigation.cjs (created_by=system system-bookkeeping carve-out) so the projected cortex (and the dial via getRoomContext) can read FEYNMAN freshness -- closing the write-only sink. Best-effort, never blocks. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-150-feynman-readback.cjs</automated>
  </verify>
  <done>feynman-seed-writer.cjs seeds a fresh section's FEYNMAN body via the shipped folder-memory writer (discover.md:170 promise fulfilled); the body-freshness + stale-flag rows project as typed graph signals via navigation.cjs; test-150-feynman-readback.cjs GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FEYNMAN sentinels -> readQuintuple | readQuintuple must extract only the human body region, never the auto Timeline (separation of human vs auto authorship) |
| seed-writer -> FEYNMAN.md | the seed-writer must route through the shipped folder-memory writer (discover.md:170), never hand-write the file |
| body-freshness rows -> graph | projected via navigation.cjs with the system-bookkeeping carve-out (created_by=system); not a truth-claim node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-07-01 | Tampering | readQuintuple mutating readTriple/readQuadruple bytes | mitigate | readQuintuple is additive (spreads the prior four fields unchanged); the test asserts the first four fields equal readQuadruple byte-for-byte |
| T-150-07-02 | Repudiation | the seed-writer hand-writing FEYNMAN.md outside the shipped writer | mitigate | the seed-writer routes through lib/core/folder-memory.cjs per discover.md:170; the test asserts the body lands via readQuintuple |
| T-150-07-03 | Information Disclosure | the body-freshness graph signal carrying FEYNMAN prose | accept | the projected signal is a freshness scalar + stale-flag (not the body prose); the Part-8 grep sweep (150-08) covers the new files |
| T-150-07-04 | Elevation of Privilege | a FEYNMAN body-freshness node written as a confirmed truth-claim | mitigate | the freshness/stale rows are system-bookkeeping (created_by=system carve-out), never truth-claim nodes -- Part 9 role 5 untouched |
| T-150-07-SC | Tampering | npm/pip/cargo installs | accept | zero package installs (CJS files + a test); no package surface |
</threat_model>

<verification>
- node tests/test-150-feynman-readback.cjs -- readQuintuple shape + byte-preservation of the prior four fields + the seed-writer body + the freshness signal
- node -e require sync + async readQuintuple presence + AsyncFunction parity
- no em-dashes in any new source
</verification>

<success_criteria>
- readQuintuple folds FEYNMAN into the read contract (sync + async, additive on readQuadruple, byte-preserving readTriple/readQuadruple, AsyncFunction parity) extracting the bodyOutsideSentinels human body (MEM-08)
- the missing discover.md:170 seed-writer ships and seeds a fresh section's FEYNMAN body via the shipped folder-memory writer (MEM-08)
- the body freshness + timeline stale-flag rows project as typed graph signals via navigation.cjs (system-bookkeeping carve-out); the write-only sink is closed
- test-150-feynman-readback.cjs green; no em-dashes
</success_criteria>

<output>
Create `.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-07-SUMMARY.md` when done
</output>
