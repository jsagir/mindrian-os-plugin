---
phase: 150-memory-cortex-as-graph-members
plan: 03
type: execute
wave: 2
depends_on: ["150-01"]
files_modified:
  - lib/core/memory/reconcile-memory-runner.cjs
  - scripts/memory-artifact-graph-hook.cjs
  - hooks/hooks.json
  - scripts/session-start
  - tests/test-150-reconcile.cjs
autonomous: true
requirements: [MEM-01]
canon_parts: [Part 6, Part 8, Part 9]

must_haves:
  truths:
    - "First run of the reconcile projects every per-folder memory MD file (ROOM/STATE/MINTO/BRAIN/FEYNMAN/USER) in the room as a typed memory_artifact node via navigation.cjs"
    - "Re-running the reconcile over an unchanged room leaves the node and edge count unchanged (idempotent upsert, no dup)"
    - "Writing a */STATE.md (Write|Edit|MultiEdit) on the CLI fires the PostToolUse memory-artifact hook and lands the cortex nodes immediately"
    - "Desktop and Cowork land the same cortex via the session-start reconcile slot (tri-polar -- no PostToolUse there)"
    - "The reconcile reads room data ONLY via navigation.cjs (zero non-SQLite fs reads of room data); the memory MD prose is read locally for classification only, never egressed"
  artifacts:
    - path: "lib/core/memory/reconcile-memory-runner.cjs"
      provides: "reconcileMemoryArtifacts(roomDir, opts) -- the ONE idempotent function that walks the room section folders, classifies each ROOM/STATE/MINTO/BRAIN/FEYNMAN/USER file, and upserts its memory_artifact node (+ governing_thought / persona / decision projection) via the 150-01 writers; the cortex spine"
      exports: ["reconcileMemoryArtifacts", "classifyMemoryFile", "discoverMemoryFiles"]
      min_lines: 150
    - path: "scripts/memory-artifact-graph-hook.cjs"
      provides: "PostToolUse hook on */{ROOM,STATE,MINTO,BRAIN,FEYNMAN,USER}.md -- the user-memory twin of gsd-artifact-graph-hook.cjs; calls the SAME idempotent reconcile, never reimplements it"
      exports: ["isMemoryMarkdown", "resolveRoomDir"]
      min_lines: 90
    - path: "hooks/hooks.json"
      provides: "PostToolUse registration of the memory-artifact hook (Write|Edit|MultiEdit), mirroring the gsd-artifact-graph-hook registration block"
      contains: "memory-artifact-graph-hook.cjs"
    - path: "scripts/session-start"
      provides: "Best-effort session-start reconcile cascade slot (clone of the Phase 149 planning-artifact reconcile slot) for Desktop/Cowork tri-polar coverage"
      contains: "reconcile-memory-runner.cjs"
  key_links:
    - from: "lib/core/memory/reconcile-memory-runner.cjs"
      to: "lib/core/navigation/memory-artifacts.cjs"
      via: "writeMemoryArtifactNode / writeDecisionNode (150-01 writers)"
      pattern: "writeMemoryArtifactNode|writeDecisionNode"
    - from: "scripts/memory-artifact-graph-hook.cjs"
      to: "lib/core/memory/reconcile-memory-runner.cjs"
      via: "require + reconcileMemoryArtifacts call (no reconcile logic duplicated)"
      pattern: "reconcile-memory-runner\\.cjs"
    - from: "scripts/session-start"
      to: "lib/core/memory/reconcile-memory-runner.cjs"
      via: "best-effort node -e cascade slot resolving the active room"
      pattern: "reconcile-memory-runner\\.cjs"
---

<objective>
Build the cortex spine + the hybrid trigger (MEM-01). ONE idempotent `reconcileMemoryArtifacts(roomDir, opts)` (in a NEW `lib/core/memory/reconcile-memory-runner.cjs`) that mirrors the Phase 149 `reconcile-runner.cjs`: walk the room's section folders (reuse the shipped section discovery), classify each per-folder memory MD file by filename into one of {ROOM, STATE, MINTO, BRAIN, FEYNMAN, USER}, upsert its `memory_artifact` node (plus the governing_thought / navigator_persona / decision projection where the content warrants) via the 150-01 writers, idempotently. Plus the hybrid trigger: a NEW `scripts/memory-artifact-graph-hook.cjs` (PostToolUse on `*/{ROOM,STATE,MINTO,BRAIN,FEYNMAN,USER}.md`, the user-memory twin of `gsd-artifact-graph-hook.cjs`) wired into `hooks/hooks.json`, AND a session-start reconcile slot (mirroring the Phase 124 / 143.1 / 149 slots) so Desktop and Cowork land the same cortex (tri-polar).

Purpose: This is the substrate the whole phase needs. Without the projected cortex, getRoomContext (Plan 04) has nothing to surface, the selector (Wave 3) has nothing to rank off, and the website's "picks up where you left off / every decision indexed" claims stay false. Per D-02 the reconcile is the single code path for backfill, ongoing sync, and tri-polar coverage; the hook is just a CLI immediacy optimization on top of it.
Output: `lib/core/memory/reconcile-memory-runner.cjs`, `scripts/memory-artifact-graph-hook.cjs`, the `hooks/hooks.json` registration, the session-start cascade slot, and the RED idempotency suite `tests/test-150-reconcile.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-CONTEXT.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-UNDERSTANDING.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-LOOP-MAP.md

# UPSTREAM (Wave 1, 150-01) -- the writers this reconcile calls. Read its exports before building:
# lib/core/navigation/memory-artifacts.cjs -- writeMemoryArtifactNode({section, kind, path, hash}),
#   writeDecisionNode, MEMORY_ARTIFACT_TYPES {ROOM,STATE,MINTO,BRAIN,FEYNMAN,USER}, the node-id helpers.
@lib/core/navigation.cjs

# The reconcile spine to mirror VERBATIM (idempotent backfill = sync; reads room data only via navigation.cjs):
@lib/core/planning/reconcile-runner.cjs
# The PostToolUse hook to mirror VERBATIM (strict path gate -> resolve room -> open db -> call the SAME reconcile -> exit 0 always):
@scripts/gsd-artifact-graph-hook.cjs
# The runner/orchestrator analog (atomic, idempotent, best-effort, reads only via navigation.cjs):
@lib/core/feynman/timeline-runner.cjs
# The session-start reconcile slot to clone (read scripts/session-start lines 1478-1541 -- the Phase 149 planning-artifact reconcile slot):
# (and lines 1432-1473 -- the Phase 143.1 dial-memory slot -- a second example of the same slot shape)
# The hooks.json PostToolUse registration block to mirror (read hooks/hooks.json lines 190-199 -- the gsd-artifact-graph-hook block):
@hooks/hooks.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED idempotency test for the cortex reconcile</name>
  <files>tests/test-150-reconcile.cjs</files>
  <read_first>
    - tests/test-149-backfill.cjs (the idempotency pattern to clone: build a fixture room tree, run reconcile, assert node+edge counts, run AGAIN on the unchanged tree, assert counts identical)
    - tests/helpers/fs-instrument.cjs (the install/calls/uninstall allow-list helper if a navigation-only arm is added)
    - lib/core/feynman/timeline-runner.cjs (the {refreshed, skipped, failed} report shape -- the reconcile mirrors {upserted, decision_nodes, edges, unchanged})
  </read_first>
  <behavior>
    - test-150-reconcile.cjs: builds a fixture room tree via fs.mkdtempSync with several section folders, each carrying one or more per-folder memory MD files (a problem-definition/ROOM.md + STATE.md, a section/MINTO.md with a governing-thought frontmatter, a BRAIN.md with derivation sections, a FEYNMAN.md, a USER.md with role_blend + journey_stage). Opens a temp-file DatabaseSync with the production nodes+edges schema. Runs reconcileMemoryArtifacts(roomDir, {db}); asserts the expected memory_artifact node count (one per discovered memory MD), that the kinds are correctly classified, and that the governing_thought / persona / decision projection nodes land where the content warrants. Then runs reconcileMemoryArtifacts AGAIN on the unchanged tree; asserts node count AND edge count are byte-identical to the first pass (idempotent upsert, no dup -- the D-02 / MEM-01 invariant).
    - Add a navigation-only arm (clone tests/test-149-navigation-only-invariant.cjs): grep reconcile-memory-runner.cjs for forbidden requires (brain-client/http/https) and forbidden calls (fetch/http.request); assert zero. The memory MD prose read for classification is the source-of-meaning LOCAL read and is allow-listed; room DATA writes flow only through navigation.cjs.
  </behavior>
  <action>Create tests/test-150-reconcile.cjs implementing the behaviors above. Build a temp fixture room via fs.mkdtempSync, applySchema to a temp-file DatabaseSync (guard node:sqlite-unavailable with exit 77), use node:assert/strict. The two-pass count-stability assertion is the load-bearing one (idempotency = MEM-01). NO em-dashes; reference any asserted forbidden dash codepoints via String.fromCharCode. This suite is RED until Task 2 lands reconcile-memory-runner.cjs. REGISTER this suite into tests/run-all-150.sh (that aggregator is OWNED by Plan 150-01 -- add the test line, do not recreate the file; if run-all-150.sh does not yet exist when this executes, leave a note in the SUMMARY and the 150-01 executor adds the registration).</action>
  <verify>
    <automated>node -e "require('node:fs').accessSync('tests/test-150-reconcile.cjs')" && node tests/test-150-reconcile.cjs; test $? -ne 0</automated>
  </verify>
  <done>tests/test-150-reconcile.cjs exists and is RED-by-design (reconcile-memory-runner.cjs absent). It builds the fixture cortex tree, runs the reconcile twice, and asserts count stability + correct classification + the navigation-only invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconcile-memory-runner.cjs (the idempotent cortex spine)</name>
  <files>lib/core/memory/reconcile-memory-runner.cjs</files>
  <read_first>
    - lib/core/planning/reconcile-runner.cjs (mirror: discoverPlanningArtifacts/classifyArtifactType/reconcilePlanningArtifacts shape; the caller-owned opts.db handle; the per-item best-effort try/catch; the {upserted, requirement_nodes, edges, unchanged} report; the planningDir override -- here a sectionRoot override)
    - lib/core/navigation/memory-artifacts.cjs (the 150-01 writers: writeMemoryArtifactNode, writeDecisionNode, MEMORY_ARTIFACT_TYPES, the node-id helpers -- CALL these, NEVER open room.db)
    - lib/core/feynman/timeline-runner.cjs (the section-folder walk + safeIsDir/safeIsFile guards + the memory_event log on each pass)
    - 150-CONTEXT.md mechanism items 1-3 (the kinds {ROOM,STATE,MINTO,BRAIN,FEYNMAN,USER}; governing_thought from MINTO STATES section; persona from USER role_blend x journey_stage DESCRIBES room; decision projection INFORMS section; BRAIN-derivation INFORMS section; the system-bookkeeping created_by=system carve-out so no Part-9 role-5 issue)
  </read_first>
  <action>Create lib/core/memory/reconcile-memory-runner.cjs. classifyMemoryFile(filename) maps a per-folder memory filename to one of MEMORY_ARTIFACT_TYPES by exact basename (ROOM.md -> ROOM, STATE.md -> STATE, MINTO.md -> MINTO, BRAIN.md -> BRAIN, FEYNMAN.md -> FEYNMAN, USER.md -> USER) and returns null for any other file. discoverMemoryFiles(sectionRoot) walks the room's section folders (reuse the SAME section-folder discovery the timeline-runner uses -- do NOT hand-roll a new walker; if a shared section-registry discovery helper exists, require it), returning [{section, kind, path}] for every classified memory MD found. reconcileMemoryArtifacts(roomDir, opts) -- the spine: resolves the section root (roomDir by default, overridable via opts.sectionRoot), takes opts.db (a caller-owned handle, mirror reconcile-runner), and for EACH discovered memory file calls navigation.writeMemoryArtifactNode({section, kind, path, hash}) where hash is a sha256 of the file body (generic handle, never the prose). For MINTO files, parse the governing-thought and project a governing_thought node via the 150-01 writer with a STATES edge to the section. For USER files, project a navigator_persona node from role_blend x journey_stage with a DESCRIBES edge to the room. For decisions (MINTO decision_log / decisions_index / the f_selector_decision memory_event ledger per the CONTEXT double-ledger note), project the long-promised decision node via navigation.writeDecisionNode with an INFORMS edge to the section -- this is the 108/109 EXTEND that closes the double-ledger (MEM-01 / D-03 substrate). All projection nodes carry created_by=system (the audit / system-bookkeeping carve-out -- they are derived bookkeeping, not human-confirmed truth claims, so no Part-9 role-5 violation). Idempotence is FREE: every write is an upsert keyed on a stable node/edge id (the 150-01 contract), so a second pass over an unchanged room changes no counts (D-02 -- backfill and sync are the same code path). Return a {upserted, decision_nodes, edges, unchanged} report. The module requires ONLY lib/core/navigation.cjs (or the memory-artifacts submodule via it), node:fs, node:path, and node:crypto (for the sha256 hash) -- NEVER brain-client, NEVER node:http/https, NEVER fetch (the Part-8 / MEM-01 floor; the memory MD prose stays LOCAL). NO em-dashes.</action>
  <verify>
    <automated>node tests/test-150-reconcile.cjs</automated>
  </verify>
  <done>test-150-reconcile.cjs GREEN: every per-folder memory MD projects a memory_artifact node of the right kind; governing_thought / persona / decision projections land; the second pass over the unchanged room changes no node or edge count (idempotent); reconcile reads room data only via navigation.cjs with zero Brain/network surface.</done>
</task>

<task type="auto">
  <name>Task 3: The hybrid trigger -- PostToolUse hook + hooks.json registration + session-start reconcile slot</name>
  <files>scripts/memory-artifact-graph-hook.cjs, hooks/hooks.json, scripts/session-start</files>
  <read_first>
    - scripts/gsd-artifact-graph-hook.cjs (the FULL hook to mirror: the strict path gate PLANNING_MD_RE -> here a MEMORY_MD_RE; readStdin; resolveRoomDir registry resolver; resolvePluginRoot; lazy node:sqlite open; the dog-food planningDir override -> here a sectionRoot override; the SAME-reconcile call; the ALWAYS-exit-0 wrapper)
    - hooks/hooks.json lines 190-199 (the gsd-artifact-graph-hook PostToolUse block: matcher "Write|Edit|MultiEdit", node command, timeout 3000 -- add a sibling block for the memory hook)
    - scripts/session-start lines 1478-1541 (the Phase 149 planning-artifact reconcile slot: the env node -e block, the registry resolver fallback, the room.db open/close, the best-effort `|| true` that never blocks startup)
    - scripts/session-start lines 1432-1473 (the Phase 143.1 dial-memory slot -- a second example of the SAME slot shape)
  </read_first>
  <action>Three coordinated edits, all mirroring the Phase 149 trigger.

(1) scripts/memory-artifact-graph-hook.cjs -- clone gsd-artifact-graph-hook.cjs. Replace the gate with MEMORY_MD_RE matching `*/{ROOM,STATE,MINTO,BRAIN,FEYNMAN,USER}.md` (a path whose basename is one of the six, cross-platform normalized for backslashes). isMemoryMarkdown(filePath) returns true only for those six basenames. On a matching Write|Edit|MultiEdit it resolves the active room (reuse the SAME registry resolver, no new resolver), lazy-opens roomDir/.mindrian/room.db (skip-fast if absent -- Tier 0), requires lib/core/memory/reconcile-memory-runner.cjs, calls the SAME idempotent reconcileMemoryArtifacts(roomDir, {db}) (full pass -- correctness over speed; idempotent so the hook write + a later session-start reconcile never duplicate), closes db in finally, and ALWAYS exits 0 (a hook failure must never block the user's write). Dog-food: when the active room IS the plugin workspace, pass opts.sectionRoot for the plugin's own memory files (same resolved-path compare as the gsd hook). Export isMemoryMarkdown + resolveRoomDir for the test.

(2) hooks/hooks.json -- add a new PostToolUse block (matcher "Write|Edit|MultiEdit") that runs `node "${CLAUDE_PLUGIN_ROOT}/scripts/memory-artifact-graph-hook.cjs"` with timeout 3000, immediately after the gsd-artifact-graph-hook block (~line 199). Keep the JSON valid (trailing-comma discipline).

(3) scripts/session-start -- add a best-effort cascade slot immediately AFTER the Phase 149 planning-artifact reconcile slot (after line ~1541), bounded by `# --- BEGIN memory-cortex reconcile (Phase 150, best-effort) ---` / `# --- END ...`. Clone the Phase 149 reconcile slot verbatim: an env node -e block that resolves the active room (prefer ROOM_DIR, else the MindrianRooms registry resolver), opens room.db if present, requires lib/core/memory/reconcile-memory-runner.cjs, calls reconcileMemoryArtifacts(roomDir, {db}), closes db, writes a one-line stderr summary when upserted>0, and ends `2>/dev/null || true` so a failure never blocks startup. This is the tri-polar net: Desktop/Cowork have no PostToolUse hook, so they land the cortex here. Dog-food the plugin's own memory files via opts.sectionRoot when roomDir is the plugin workspace (reuse the existing registry resolver; do NOT add a new one).

NO em-dashes anywhere. Pure CJS in the hook; bash + node -e in session-start.</action>
  <verify>
    <automated>node -e "const h=require('./scripts/memory-artifact-graph-hook.cjs'); if(!h.isMemoryMarkdown('/x/problem-definition/STATE.md')||h.isMemoryMarkdown('/x/notes.md'))process.exit(1)" && node -e "JSON.parse(require('node:fs').readFileSync('hooks/hooks.json','utf8'))" && grep -q "memory-artifact-graph-hook.cjs" hooks/hooks.json && bash -n scripts/session-start && grep -q "reconcile-memory-runner.cjs" scripts/session-start && ! grep -qP "$(printf '—')" scripts/memory-artifact-graph-hook.cjs</automated>
  </verify>
  <done>The PostToolUse hook gates strictly on the six memory basenames and exits 0 always; hooks.json stays valid JSON and registers the hook; session-start parses (bash -n), contains the best-effort memory-cortex reconcile slot referencing reconcile-memory-runner.cjs; no em-dashes in the added code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-folder memory MD -> reconcile-memory-runner | the reconcile READS the memory MD prose locally to classify + derive generic handles (kind, section, governing-thought, persona blend, decision id, sha256); LOCAL only, never egressed |
| reconcile-memory-runner -> room.db | every node/edge write routes through navigation.cjs (the 150-01 writers); zero direct room.db open in the runner |
| PostToolUse hook (attacker-influenceable file_path) -> reconcile | the file_path is gated by the strict six-basename MEMORY_MD_RE before any room work |
| session-start cascade -> reconcile | best-effort slot; a failure must never block Claude Code startup (tri-polar net for Desktop/Cowork) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-03-01 | Information Disclosure | reconcile reading memory MD prose | mitigate | the prose is read LOCALLY to derive generic handles only (kind, section, sha256, persona enum, decision id); node properties carry generic handles, never artifact body prose; the navigation-only arm of test-150-reconcile asserts no brain-client/http require and no fetch/http.request call |
| T-150-03-02 | Denial of Service | session-start slot blocking startup | mitigate | the slot is `2>/dev/null || true`, cloned from the proven Phase 124 / 149 slots; a reconcile failure is swallowed; the hook always exits 0 |
| T-150-03-03 | Tampering | duplicate cortex nodes on re-run (hook + session-start both fire) | mitigate | every write is an upsert on a stable id (150-01 contract); test-150-reconcile asserts node AND edge count stability across two passes; the hook calls the SAME idempotent reconcile (no second code path) |
| T-150-03-04 | Elevation of Privilege | PostToolUse acting on a non-memory file_path | mitigate | isMemoryMarkdown gates strictly on the six exact basenames; any other path is a no-op exit 0 |
| T-150-03-05 | Repudiation | system-bookkeeping projection nodes mislabeled as human truth | mitigate | governing_thought / persona / decision projection nodes carry created_by=system (Part-9 audit-node carve-out); they are derived bookkeeping, never confirmed truth-claims, so role-5 human-confirm is not bypassed |
| T-150-03-SC | Tampering | npm/pip/cargo installs | accept | zero package installs; node:fs/node:path/node:crypto/node:sqlite + the existing navigation submodule |
</threat_model>

<verification>
- `node tests/test-150-reconcile.cjs` -- idempotent cortex projection across two passes + correct classification + navigation-only invariant
- `node -e "require('./scripts/memory-artifact-graph-hook.cjs').isMemoryMarkdown('/x/STATE.md')"` -- the strict six-basename gate
- `node -e "JSON.parse(require('node:fs').readFileSync('hooks/hooks.json','utf8'))"` -- hooks.json stays valid JSON
- `bash -n scripts/session-start` -- the cascade slot parses
- `bash tests/run-all-150.sh` -- the Phase 150 aggregator picks up test-150-reconcile (owned by 150-01; this plan registers its suite)
</verification>

<success_criteria>
- reconcileMemoryArtifacts is the ONE function doing backfill + sync + the governing_thought/persona/decision projection for all 6 memory MD kinds (MEM-01)
- First run projects every per-folder memory MD as a memory_artifact node; second run over an unchanged room changes no counts (D-02 idempotence, no dup)
- The CLI hybrid trigger fires on */{ROOM,STATE,MINTO,BRAIN,FEYNMAN,USER}.md and lands the cortex immediately (D-01)
- Desktop/Cowork land the same cortex via the best-effort session-start reconcile slot (tri-polar)
- The reconcile reads room data only via navigation.cjs; the memory MD prose stays LOCAL; zero Brain/network surface
- Decision double-ledger collapses toward one graph-authoritative source via the decision-node projection (the 108/109 EXTEND substrate)
- Zero em-dashes
</success_criteria>

<output>
Create `.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-03-SUMMARY.md` when done. Note in the SUMMARY: (a) the exact 150-01 writer exports consumed (writeMemoryArtifactNode / writeDecisionNode / MEMORY_ARTIFACT_TYPES), (b) whether the test-150-reconcile registration into tests/run-all-150.sh was made by this plan or deferred to the 150-01 executor, and (c) any decision double-ledger source (MINTO decision_log vs decisions_index vs f_selector_decision memory_event) that the projection could not yet reach, for the Wave-3 / companion-phase follow-up.
</output>
