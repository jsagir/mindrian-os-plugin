---
phase: 150-memory-cortex-as-graph-members
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation/memory-cortex-packet.cjs
  - tests/test-150-brain-egress.cjs
autonomous: true
requirements: [MEM-04]
canon_parts: [Part 6, Part 8, Part 9]

must_haves:
  truths:
    - "buildMemoryCortexPacket returns a flat object of GENERIC HANDLES ONLY (governing_thought sha256, correlation_id, problem_type/complexity/persona enums, current_framework handle, stage, gap_count) when a reach fires"
    - "The packet is built from node IDs + correlation_id + enum scalars via navigation.getNeighborhood; it NEVER reads a node's properties JSON"
    - "An adversarial poisoned-cortex test proves zero raw memory prose (FORBIDDEN_SUBSTRINGS) survives in JSON.stringify(packet)"
    - "The builder requires zero network (no brain-client / node:http / node:https / fetch) -- it builds a packet, it does not send it"
    - "A cortex node carrying no correlation_id (a HELD / un-canonicalized node) degrades gracefully to a LOCAL-only handle, never crashes the build"
  artifacts:
    - path: "lib/core/navigation/memory-cortex-packet.cjs"
      provides: "buildMemoryCortexPacket -- the typed memory-cortex Brain packet projection (generic handles only) for any Brain query about the navigator's memory cortex when reaching"
      exports: ["buildMemoryCortexPacket"]
      min_lines: 90
    - path: "tests/test-150-brain-egress.cjs"
      provides: "Adversarial Part-8 zero-egress test (clone of test-149-brain-egress.cjs): poison cortex node properties, assert none survive + grep the builder for forbidden requires/calls + navigation-only invariant"
      contains: "FORBIDDEN_SUBSTRINGS"
  key_links:
    - from: "lib/core/navigation/memory-cortex-packet.cjs"
      to: "lib/core/navigation.cjs getNeighborhood"
      via: "reads room DATA only via navigation.cjs; never opens room.db; never reads node properties prose"
      pattern: "getNeighborhood"
    - from: "lib/core/navigation/memory-cortex-packet.cjs"
      to: "generic handles only"
      via: "governing_thought sha256, correlation_id, problem_type/complexity/persona enums, framework handle, stage, gap_count -- never memory prose"
      pattern: "correlation_id|governing_thought_hash"
---

<objective>
Build the Wave-1 REMOTE-queryability seal: a NEW navigation submodule `lib/core/navigation/memory-cortex-packet.cjs` that mirrors the shipped `lib/core/planning/artifact-brain-packet.cjs` (Phase 149-03) EXACTLY, repointed from the GSD planning artifacts to the navigator's memory cortex. It exports `buildMemoryCortexPacket(db, {section})` which builds a typed Brain packet from node IDs + correlation_id + enum scalars via `navigation.getNeighborhood` and NEVER reads a node's `properties` JSON.

The minimal Part-8-safe query shape (UNDERSTANDING section 3) is: `{governing_thought_hash (sha256), correlation_id, problem_type (UDP|IDP|WDP), complexity (Simple|Complex|Wicked), persona (P-enum), current_framework (public handle), stage, gap_count}`. Stamp the correlation_id on cortex nodes via the shipped lib/core/correlation.cjs + lib/core/navigation/spine-events.cjs `_withCorrelationId` idiom so a LOCAL cortex node and a REMOTE Brain node carrying the same cid are the same logical node with zero content crossing the wire.

Ship the adversarial egress test `tests/test-150-brain-egress.cjs` (clone of test-149-brain-egress.cjs): poison the cortex node properties with FORBIDDEN_SUBSTRINGS, assert JSON.stringify(packet) contains none, grep the builder for forbidden requires/calls, and assert the navigation-only invariant.

Purpose: This is the constitutional seal that makes "Brain-queryable when reaching" Part-8-safe. NOTE the load-bearing caveat from 150-LOOP-MAP.md / UNDERSTANDING section 4: Phase 150 is the FIRST real sendPacket consumer, so this builder inherits the weight of proving the Part-8 guards fire in production, not just in dormant fixtures. HELD nodes (no correlation_id, name > 80 chars) are un-joinable and MUST degrade to LOCAL-only handles, never crash the build. This builder ONLY builds the packet; sending stays the existing Brain-client path that already passes the sanitizer.

Output: `lib/core/navigation/memory-cortex-packet.cjs` and the adversarial `tests/test-150-brain-egress.cjs`.

This plan is files-DISJOINT from 150-01 (it touches neither memory-artifacts.cjs, edges.cjs, navigation.cjs, nor run-all-150.sh) so both Wave-1 plans run in parallel. It is self-contained: it requires only navigation.cjs (the Phase 109 chokepoint) + the packet.cjs sha256 helper + correlation.cjs, all SHIPPED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-CONTEXT.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-UNDERSTANDING.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-LOOP-MAP.md

# THE module to mirror EXACTLY (the 149 typed-packet builder this repoints; clone its build-from-ids discipline, the artifactTypeFromNodeId/requirementIdFromNodeId parse-from-stable-id helpers, the getNeighborhood union, the defensive minimal-packet fallback, the NEVER-read-properties rule):
@lib/core/planning/artifact-brain-packet.cjs
# THE adversarial test to clone (poison-seed FORBIDDEN_SUBSTRINGS + forbidden-require/call grep sweep + navigation-only invariant + the no-dash codepoint check):
@tests/test-149-brain-egress.cjs
# The packet.cjs sha256-by-default projection helper (hashText -- reuse it to hash the governing-thought handle; projectText hashes prose; the Part-8 leak-safe wire hasher):
@lib/core/navigation/packet.cjs
# The correlation_id idiom to stamp (computeCorrelationId name-based, embedding-independent; the LOCAL<->REMOTE join key):
@lib/core/correlation.cjs
# The _withCorrelationId stamping path to mirror (precedence: caller-supplied cid honored, else derive from canonical_name + primary_label, else leave untouched -- the graceful-degradation HELD-node path):
@lib/core/navigation/spine-events.cjs
# The node-write shape the cortex packet reads back (the 150-01 writers mint memory_artifact / governing_thought / navigator_persona / decision with stable ids; the packet parses handles from those ids, never the properties):
@lib/core/navigation/planning-artifacts.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 adversarial Brain-egress test (RED-by-design)</name>
  <files>tests/test-150-brain-egress.cjs</files>
  <read_first>
    - tests/test-149-brain-egress.cjs (the WHOLE file: the FORBIDDEN_SUBSTRINGS poison-seed pattern, applySchema(db) nodes+edges, the seedPoisoned* helpers that plant secret prose into EVERY field a writer would keep clean, the FORBIDDEN_REQUIRES / FORBIDDEN_CALLS grep sweep, the navigation-only invariant test, the no-dash codepoint check, the exit-77 node:sqlite SKIP guard -- clone this structure verbatim, repointed to the cortex nodes)
    - lib/core/navigation/packet.cjs lines 121-138 (hashText / projectText hashing to sha256 by default; the leak-safe path the cortex packet reuses for the governing_thought handle)
    - lib/core/navigation/spine-events.cjs lines 74-86 (_withCorrelationId: the cid stamping the test seeds onto the cortex nodes so the packet can carry the join key; and the HELD-node no-cid degradation path the test exercises with one un-stamped node)
  </read_first>
  <behavior>
    - Seed an in-memory graph with the FULL cortex shape the 150-01 writers + reconcile would produce, all POISONED: a memory_artifact node per kind, a governing_thought node, a navigator_persona node, a decision node -- each with properties/path/source_path carrying FORBIDDEN_SUBSTRINGS (raw governing-thought prose, an email, an absolute /home/jsagi/secret/ path, an injection token). Stamp correlation_id on the joinable nodes; leave ONE node WITHOUT a correlation_id (the HELD-node degradation case).
    - Call buildMemoryCortexPacket(db, {section}) and assert the serialized packet (JSON.stringify) contains NONE of the FORBIDDEN_SUBSTRINGS -- only generic handles (governing_thought_hash sha256, correlation_id, problem_type/complexity/persona enums, current_framework handle, stage, gap_count) survive.
    - Positive floor: assert the packet carries the generic handles so it is useful -- governing_thought_hash is a sha256 hex string, correlation_id is present, the enum fields are from their closed sets, gap_count is a number.
    - HELD-node floor: assert the build does not crash when a cortex node carries no correlation_id; it degrades that node to a LOCAL-only handle (absent from the remote-join fields) without throwing.
    - Grep buildMemoryCortexPacket source for forbidden requires (brain-client / node:http / node:https / http / https) and forbidden calls (fetch / http.request / https.request / .get) -- the Canon Part 8 zero-egress structural floor.
    - Navigation-only invariant: assert the builder requires ../navigation.cjs (the Phase 109 chokepoint) and does NOT require node:sqlite directly.
    - Assert no em-dash / en-dash in the packet output (reference codepoints via String.fromCharCode).
  </behavior>
  <action>Create tests/test-150-brain-egress.cjs cloning the adversarial-seed structure of tests/test-149-brain-egress.cjs verbatim, repointed to the cortex nodes. Set FILES_TO_SCAN to ['lib/core/navigation/memory-cortex-packet.cjs'] (the 150-01 memory-artifacts.cjs writer is NOT scanned here -- it is the 150-01 plan's own substrate-guard floor; this test is the EGRESS floor for the packet builder only). Build the nodes+edges in-memory schema (clone applySchema verbatim), seed the poisoned cortex nodes via seedPoisoned* helpers that plant FORBIDDEN_SUBSTRINGS into properties + path + source_path + source_section, stamp correlation_id on the joinable nodes and leave one node un-stamped (HELD case), call buildMemoryCortexPacket(db, {section}), and assert the JSON.stringify of the packet contains none of the FORBIDDEN_SUBSTRINGS. Add the forbidden-require + forbidden-call grep sweep over the packet builder, the navigation-only invariant test (requires ../navigation.cjs, no node:sqlite), and the no-dash codepoint check. Guard node:sqlite-unavailable with exit 77 SKIP. NO em-dashes; reference dash codepoints via String.fromCharCode(0x2014) and String.fromCharCode(0x2013). RED until Task 2 lands memory-cortex-packet.cjs.</action>
  <verify>
    <automated>bash tests/run-all-150.sh; test $? -ge 0</automated>
  </verify>
  <done>tests/test-150-brain-egress.cjs exists and is RED-by-design (memory-cortex-packet.cjs absent). run-all-150.sh finds it and reports FAILED (it was MISSING from plan 01's perspective; now present and RED until Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: memory-cortex-packet.cjs (typed memory-cortex packet, generic handles only)</name>
  <files>lib/core/navigation/memory-cortex-packet.cjs</files>
  <read_first>
    - lib/core/planning/artifact-brain-packet.cjs (the EXACT builder to mirror: build from node IDS + TYPE + correlation_id + enum scalars; the parse-handle-from-stable-id helpers; the getNeighborhood union over anchors; NEVER read a node's properties JSON; the defensive minimal-packet fallback when db is absent or anchor is unknown; requires ONLY ../navigation.cjs + the packet.cjs sha256 helper)
    - lib/core/navigation/packet.cjs lines 121-138 (hashText -- hash the GENERIC governing-thought handle, never prose; this is how governing_thought_hash is produced Part-8-safely)
    - lib/core/correlation.cjs (computeCorrelationId -- the name-based embedding-independent cid; the LOCAL<->REMOTE join key the packet carries)
    - lib/core/navigation/spine-events.cjs lines 74-86 (_withCorrelationId precedence: honor a node-carried cid, else derive, else leave absent -- the HELD-node graceful degradation the packet must honor: no cid -> the node contributes a LOCAL-only handle, omitted from the remote-join fields)
    - .planning/phases/150-.../150-UNDERSTANDING.md section 3 (the minimal Part-8-safe query shape: governing_thought_hash, correlation_id, problem_type/complexity/persona enums, current_framework handle, stage, gap_count)
  </read_first>
  <action>Create lib/core/navigation/memory-cortex-packet.cjs exporting buildMemoryCortexPacket(db, {section}). It reads the cortex nodes (memory_artifact / governing_thought / navigator_persona / decision) for the section VIA navigation.getNeighborhood (never opens room.db; never requires node:sqlite) and projects a typed packet containing ONLY the minimal Part-8-safe shape from UNDERSTANDING section 3:
- packet_version, job: 'memory_cortex_query'
- section (a generic LOCAL section handle, never prose)
- governing_thought_hash: the sha256 of the governing_thought node's HASH HANDLE (parsed from the stable node id or the cid, NEVER from properties prose) via the packet.cjs hashText helper
- correlation_id: the cid carried on the cortex nodes (the LOCAL<->REMOTE join key); derived via the spine-events _withCorrelationId precedence when a node carries canonical_name, honored when caller-supplied, ABSENT when the node is HELD (no cid)
- problem_type (UDP|IDP|WDP), complexity (Simple|Complex|Wicked), persona (P-enum from the 9-role taxonomy) -- closed enum scalars only; anything not in the closed set is dropped, never echoed
- current_framework: a public framework-name handle (from the connector/selector surface), never user content
- stage: the journey-stage / venture-stage enum scalar
- gap_count: a numeric scalar (the lowFillSections count surfaced by the STATE projection)
- origin: 'navigation_api', constraints: { privacy: 'no_raw_memory_text' }

Build the enum/handle fields by parsing them from the stable node IDS + the correlation_labels join (mirror artifact-brain-packet.cjs artifactTypeFromNodeId / requirementIdFromNodeId parse-from-id helpers); validate every enum against its closed set so a malformed node id carrying prose after a delimiter never survives. The builder MUST NOT include: any memory MD body, the raw file path, filenames-as-content, source_path/source_section, or ANY node properties prose. It is built from node IDS + TYPE + correlation_id + review_status enum only -- it NEVER reads a node's properties JSON, so no prose field can ever reach the wire even if a 150-01 writer regression let prose onto a node.

Graceful HELD-node degradation: when a cortex node carries no correlation_id (name > 80 chars / un-canonicalized), it contributes its LOCAL-only handles (enum presence) but is OMITTED from the correlation_id / remote-join fields; the build never throws on a missing cid.

The module requires ONLY ../navigation.cjs (plus ./packet.cjs sha256 helper + ../correlation.cjs + node:path) and NEVER brain-client / node:http / node:https / fetch (the Part 8 floor; this BUILDS a packet, it does NOT send it; sending stays the existing Brain-client path that already passes the sanitizer). Defensive: never throws on caller input; returns a minimal packet (generic handles, empty/absent join fields) when db is absent or the anchor is unknown. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-150-brain-egress.cjs</automated>
  </verify>
  <done>test-150-brain-egress.cjs GREEN: the packet carries only generic handles (governing_thought_hash sha256, correlation_id, problem_type/complexity/persona enums, current_framework handle, stage, gap_count); zero FORBIDDEN_SUBSTRINGS survive; the HELD-node case degrades to LOCAL-only without crashing; zero forbidden requires/calls; the builder reads only via navigation.cjs; no em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cortex nodes -> buildMemoryCortexPacket | the node properties may carry raw memory prose (a writer regression, or a hand-seeded poison node); the builder reads ids + types + correlation_id + review_status ONLY, never properties |
| buildMemoryCortexPacket -> Brain wire | the packet is the ONLY thing a Brain query about the memory cortex may carry; it must hold generic handles only (Part 8 absolute) |
| HELD cortex node -> remote join | a node with no correlation_id is un-joinable; it degrades to LOCAL-only, never crashes the build, never forces a fake cid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-06 | Information Disclosure | buildMemoryCortexPacket | mitigate | the packet emits ONLY governing_thought_hash (sha256), correlation_id, problem_type/complexity/persona enums, current_framework handle, stage, gap_count; the adversarial test-150-brain-egress seeds poison prose into every node field and asserts none survives in JSON.stringify(packet); the builder NEVER reads node properties JSON |
| T-150-07 | Information Disclosure | governing-thought prose leak | mitigate | governing_thought_hash is the sha256 of a generic handle via packet.cjs hashText; the raw MINTO governing-thought never lands on the packet (the 150-01 writer already hashed it onto the node; the packet re-hashes the handle, never reads the body) |
| T-150-08 | Tampering | network egress from the builder | mitigate | the builder requires zero network (grep sweep asserts no brain-client / node:http / node:https / fetch / http.request); it BUILDS a packet, it does NOT send it; sending stays the existing sanitized Brain-client path |
| T-150-09 | Denial of Service | HELD-node un-joinable crash | mitigate | a cortex node with no correlation_id degrades to a LOCAL-only handle; the build never throws on a missing cid (the 150 first-consumer caveat from UNDERSTANDING section 4 caveat 2) |
| T-150-10 | Tampering | direct room.db open in the builder | mitigate | the builder requires only ../navigation.cjs + ./packet.cjs + ../correlation.cjs; the navigation-only invariant test asserts no node:sqlite require; substrate guard allow-list /^lib\/core\/navigation\// covers it |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | this plan installs zero packages; pure node:sqlite + existing navigation submodules |
</threat_model>

<verification>
- `node tests/test-150-brain-egress.cjs` -- zero memory prose in the packet; zero forbidden requires/calls; HELD-node degrades; navigation-only invariant holds
- `node -c lib/core/navigation/memory-cortex-packet.cjs` -- builder parses
- `bash tests/run-all-150.sh` -- the egress suite reports its status (GREEN once Task 2 lands; the wave-2/3 suites remain MISSING until their owning plans land)
</verification>

<success_criteria>
- memory-cortex-packet.cjs exports buildMemoryCortexPacket
- The packet carries the minimal Part-8-safe shape ONLY: governing_thought_hash (sha256), correlation_id, problem_type/complexity/persona enums, current_framework handle, stage, gap_count
- The builder reads via navigation.getNeighborhood + correlation_id + enum scalars; it NEVER reads node properties JSON
- The adversarial poisoned-cortex egress test passes (zero FORBIDDEN_SUBSTRINGS survive) -- MEM-04
- A HELD cortex node (no correlation_id) degrades to LOCAL-only without crashing
- Zero network surface (no brain-client / http / https / fetch); the navigation-only invariant holds
- Zero em-dashes
</success_criteria>

<output>
Create `.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-02-SUMMARY.md` when done
</output>
