---
phase: 150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching
plan: 05
type: execute
wave: 3
depends_on: ["150-01", "150-02"]
files_modified:
  - commands/memory-cortex-reach.md
  - lib/core/sensors/sensor-memory-cortex.cjs
  - lib/core/insight-sensors.cjs
  - data/connector-registry.json
  - tests/test-150-connector.cjs
autonomous: true
requirements: [MEM-05]
canon_parts: [Part 2, Part 3, Part 7, Part 8, Part 9]

must_haves:
  truths:
    - "A NEW command surface (commands/memory-cortex-reach.md) declares a connector: frontmatter block that distills into data/connector-registry.json (a lib reach alone is NOT registry-eligible)"
    - "The connector reach_id is one of the FROZEN 6 (cross_room or context_block), framework:null, filing:memory_event_only -- the minimal Part-8-safe shape; no 7th reach is minted"
    - "A NEW sensor (sensorMemoryCortex) is registered in SENSOR_REGISTRY and fires the reach when the cortex signals (a stale governing thought OR a fresh contradiction)"
    - "build-connector-registry.cjs --check is clean after regeneration (the new connector is present and the tripwire passes)"
    - "The new sensor returns makeReach with a frozen reach_id + posture; a malformed input soft-fails to null (never throws)"
  artifacts:
    - path: "commands/memory-cortex-reach.md"
      provides: "The memory-cortex reach surface that joins the 143.3 connector spine (registry-eligible .md surface, D-05)"
      contains: "connects_to_spine"
    - path: "lib/core/sensors/sensor-memory-cortex.cjs"
      provides: "sensorMemoryCortex(turn, tuple, ctx) -> candidate-reach|null fired by a stale governing thought or a fresh contradiction"
      min_lines: 40
    - path: "data/connector-registry.json"
      provides: "Regenerated registry containing the memory-cortex connector (--check clean)"
      contains: "memory-cortex"
  key_links:
    - from: "commands/memory-cortex-reach.md"
      to: "data/connector-registry.json"
      via: "build-connector-registry.cjs distills connects_to_spine:true surfaces into the registry"
      pattern: "memory-cortex"
    - from: "lib/core/sensors/sensor-memory-cortex.cjs"
      to: "lib/core/insight-sensors.cjs"
      via: "sensorMemoryCortex appended to SENSOR_REGISTRY so dispatchSensors fires it in canonical order"
      pattern: "sensorMemoryCortex|SENSOR_REGISTRY"
---

<objective>
Spine-connect the memory cortex (MEM-05, D-05): declare a connector: frontmatter block on a NEW command surface (commands/memory-cortex-reach.md) so the cortex reach joins the Phase 143.3 intelligence-orchestrator spine, add a NEW sensor (sensorMemoryCortex) to SENSOR_REGISTRY that fires the reach when the cortex signals (a stale governing thought or a fresh contradiction), and regenerate data/connector-registry.json with --check clean. The minimal Part-8-safe declaration (UNDERSTANDING section 2): reach_id is one of the FROZEN 6, framework:null, filing:memory_event_only, posture from the frozen 3 -- so the surface joins by declaring ONE block, never by editing the orchestrator and never by minting a 7th reach.

Purpose: The cortex must be dispatchable. Phase 143.3 made the spine self-extending (read the registry, never a hardcoded table); a surface joins by declaring one connector block. This plan rides that spine with ONE connector + ONE sensor, it does NOT complete the 144.1 retrofit sweep (out of scope).
Output: commands/memory-cortex-reach.md (the registry-eligible reach surface) + lib/core/sensors/sensor-memory-cortex.cjs (the sensor that fires it) + its SENSOR_REGISTRY registration + the regenerated connector-registry.json + the RED-first test-150-connector.cjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-CONTEXT.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-UNDERSTANDING.md
@.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-LOOP-MAP.md

# The connector contract + generator + the --check tripwire (Phase 143.3 -- the spine this rides):
@docs/CONNECTOR-CONTRACT.md
# The frozen reach/posture banks + the makeReach factory (cross_room / context_block are the Part-8-safe reach_ids):
@lib/core/sensors/sensor-types.cjs
# The sensor registry + dispatchSensors chokepoint (append the new sensor here):
@lib/core/insight-sensors.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED connector + sensor test (test-150-connector.cjs)</name>
  <files>tests/test-150-connector.cjs</files>
  <read_first>
    - tests/test-148-engine-reaches.cjs (the node:assert/strict PASS-line style + how a reach test asserts reach_id membership in the frozen REACH_IDS bank)
    - lib/core/sensors/sensor-types.cjs lines 11-14, 43-60, 80-90 (REACH_IDS frozen length 6 {context_block, contradiction, cross_room, brain_consult, deep_research, hats}; POSTURE_IDS frozen length 3; makeReach validates reach_id + posture membership and returns null on a miss)
    - lib/core/insight-sensors.cjs lines 202-212 (SENSOR_REGISTRY canonical-order array; dispatchSensors iterates it; a sensor that throws is treated as did-not-fire)
    - docs/CONNECTOR-CONTRACT.md lines 21-34 (the 11-key connector: block; connects_to_spine:true is what the generator distills)
  </read_first>
  <behavior>
    - Registry assertion: load data/connector-registry.json after regeneration; assert an entry whose source is commands/memory-cortex-reach.md exists, its reach_id is in the frozen 6 (cross_room or context_block), its framework is null, and its filing is memory_event_only.
    - Sensor membership: require lib/core/insight-sensors.cjs; assert sensorMemoryCortex is present in SENSOR_REGISTRY (by function name) so dispatchSensors will fire it.
    - Sensor fire: call sensorMemoryCortex(turn, tuple, ctx) with a ctx carrying a stale-governing-thought signal AND separately a fresh-contradiction signal; assert each returns a candidate reach whose reach_id is in the frozen 6 and whose posture is in the frozen 3; assert a malformed ctx ({}) returns null (soft-fail, never throws).
    - No em-dash / en-dash in the new command .md or sensor source (reference dash codepoints via String.fromCharCode).
  </behavior>
  <action>Create tests/test-150-connector.cjs as a plain CJS node:assert/strict suite asserting the four behaviors above. RED-by-design: sensor-memory-cortex.cjs and commands/memory-cortex-reach.md do not exist yet, and the registry has not been regenerated, so the suite FAILS until Tasks 2-3 land. Guard a missing connector-registry.json with a clear FAIL message (not a crash). NO em-dashes; reference dash codepoints via String.fromCharCode.</action>
  <verify>
    <automated>node tests/test-150-connector.cjs; test $? -ne 0 && echo "PASS (RED-by-design)"</automated>
  </verify>
  <done>tests/test-150-connector.cjs exists and is RED-by-design (the surface, sensor, and registry entry are absent); the suite reports FAILED without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: The memory-cortex reach surface + connector frontmatter (MEM-05, D-05)</name>
  <files>commands/memory-cortex-reach.md, data/connector-registry.json</files>
  <read_first>
    - commands/discover.md lines 165-175 (DISC-10: the FIRST product command authored to ride the 143.3 spine -- the precedent for a command declaring exactly ONE connector block; mirror its connector: frontmatter shape)
    - docs/CONNECTOR-CONTRACT.md lines 21-34 (the 11-key connector: block schema -- connects_to_spine, sensor_triggers, reach_id, sub_mode, framework, posture, hierarchy_rank, filing, plan_gated, web_scope)
    - 150-UNDERSTANDING.md section 2 lines 29-45 (the minimal --check-clean shape: reach_id one of the frozen 6, framework:null + filing:memory_event_only avoids the framework-resolution requirement, posture one of the frozen 3, a new SENS id under sensor_triggers; "a lib reach alone is NOT registry-eligible")
    - scripts/build-connector-registry.cjs (the generator that distills connects_to_spine:true surfaces into data/connector-registry.json; run it with --check after editing)
  </read_first>
  <action>Create commands/memory-cortex-reach.md as a thin command surface (mirror commands/discover.md's frontmatter + body discipline) that declares exactly ONE connector: frontmatter block in the minimal Part-8-safe shape from UNDERSTANDING section 2: connects_to_spine:true; sensor_triggers naming the new sensor's SENS id (the id sensorMemoryCortex emits); reach_id set to one of the FROZEN 6 (cross_room -- the cortex reach is a cross-room/memory bridge per D-05; never a 7th reach); sub_mode: memory-cortex-bridge (a render label only); framework:null; posture: push_forward (one of the frozen 3); hierarchy_rank a low integer; filing: memory_event_only; plan_gated:false; web_scope:null. The body documents the reach as the navigator-facing "reach into your memory cortex" surface dispatched by the orchestrator -- it does NOT re-implement dispatch (the orchestrator routes it with zero edits). Then regenerate data/connector-registry.json via node scripts/build-connector-registry.cjs (the generator distills the new connector) and confirm node scripts/build-connector-registry.cjs --check is clean. NO em-dashes.</action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node -e "const r=require('./data/connector-registry.json'); const j=JSON.stringify(r); if(!/memory-cortex/.test(j)) throw new Error('connector not in registry'); console.log('PASS registry has memory-cortex connector');"</automated>
  </verify>
  <done>commands/memory-cortex-reach.md declares one connector: block (reach_id in the frozen 6, framework:null, filing:memory_event_only, posture in the frozen 3); data/connector-registry.json regenerated; build-connector-registry.cjs --check is clean; no 7th reach minted; no em-dashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The memory-cortex sensor + SENSOR_REGISTRY registration (MEM-05)</name>
  <files>lib/core/sensors/sensor-memory-cortex.cjs, lib/core/insight-sensors.cjs</files>
  <read_first>
    - lib/core/sensors/sensor-types.cjs lines 60-90, 123-125 (makeReach({reach_id, posture, ...}) -- the pure candidate-reach factory; reach_id must be in REACH_IDS, posture in POSTURE_IDS, else null; the exports)
    - lib/core/insight-sensors.cjs lines 195-212 (the SENSOR_REGISTRY canonical-order array; the wave-2 detectors sensorLaggingComponent / sensorGateApproach are the cortex-ctx consumers; the doc comment "a sensor that throws is treated as did not fire")
    - the Wave-2 sensor-ctx producers (150-04, upstream): the STATE/USER projection that populates the cortex signals this sensor reads (a stale governing thought freshness flag; a fresh-contradiction count) -- this sensor consumes that ctx, it does NOT produce it
    - 150-CONTEXT.md mechanism item 5 + D-05 (the cortex reach fires on a reach; the sensor is the trigger condition added to SENSOR_REGISTRY per UNDERSTANDING section 2)
  </read_first>
  <action>Create lib/core/sensors/sensor-memory-cortex.cjs exporting sensorMemoryCortex(turn, tuple, ctx) -> candidate-reach|null, mirroring the shape of the existing sensors in insight-sensors.cjs. It reads ONLY the projected cortex signals from ctx (a stale governing-thought freshness flag and a fresh-contradiction signal, both produced by the Wave-2 sensor-ctx producers in 150-04) and, when either fires, returns makeReach({reach_id:'cross_room', posture:'push_forward', sub_mode:'memory-cortex-bridge', ...}) using the lib/core/sensors/sensor-types.cjs makeReach factory (which validates the reach_id + posture against the frozen banks). It MUST soft-fail to null on a malformed or empty ctx (never throw -- one bad sensor cannot poison dispatchSensors). It emits a stable SENS id matching the sensor_triggers value declared in commands/memory-cortex-reach.md (Task 2). Then register sensorMemoryCortex in lib/core/insight-sensors.cjs by appending it to the SENSOR_REGISTRY array (canonical order, after the wave-2/wave-3 detectors) and requiring the new module at the top. The Part-8 sensor sweep auto-covers new lib/core/sensors/*.cjs; the orchestrator routes it with zero edits. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-150-connector.cjs</automated>
  </verify>
  <done>sensor-memory-cortex.cjs exports sensorMemoryCortex (fires on a stale governing thought or a fresh contradiction, soft-fails to null on bad ctx, returns a frozen-bank reach via makeReach); it is registered in SENSOR_REGISTRY; test-150-connector.cjs is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cortex ctx -> sensor | the sensor reads only projected enum/scalar cortex signals from ctx; a malformed ctx must soft-fail to null, never throw |
| connector frontmatter -> registry | the generator distills only the declared block; framework:null + filing:memory_event_only keeps the surface off the framework-resolution path |
| sensor reach -> orchestrator dispatch | the reach is one of the frozen 6 (no 7th); the orchestrator routes it via the registry with zero edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-05-01 | Tampering | a 7th reach_id smuggled into the connector or sensor | mitigate | makeReach validates reach_id against the frozen REACH_IDS (length 6); the connector reach_id is cross_room; the carried reach-ids drift fence (re-run in 150-08) asserts 6 |
| T-150-05-02 | Denial of Service | a throwing sensor poisoning dispatchSensors | mitigate | sensorMemoryCortex wraps its body and soft-fails to null on malformed ctx; dispatchSensors already treats a throw as did-not-fire |
| T-150-05-03 | Information Disclosure | the connector surface or sensor leaking cortex prose | accept | the connector block holds only enum/scalar/id handles (reach_id, posture, filing, SENS id); the sensor reads only enum/scalar ctx signals; the Part-8 grep sweep (150-08) covers the new files |
| T-150-05-SC | Tampering | npm/pip/cargo installs | accept | zero package installs (one command .md + one CJS sensor + a registry regeneration + one test); no package surface |
</threat_model>

<verification>
- node tests/test-150-connector.cjs -- registry entry present (frozen reach_id, framework:null, filing:memory_event_only); sensorMemoryCortex in SENSOR_REGISTRY; the sensor fires on a stale governing thought + a fresh contradiction and soft-fails to null on bad ctx
- node scripts/build-connector-registry.cjs --check -- the tripwire is clean after regeneration
- no em-dashes in commands/memory-cortex-reach.md or lib/core/sensors/sensor-memory-cortex.cjs
</verification>

<success_criteria>
- commands/memory-cortex-reach.md declares the minimal Part-8-safe connector block (reach_id in the frozen 6, framework:null, filing:memory_event_only, posture in the frozen 3); a lib reach alone would not have been registry-eligible (MEM-05, D-05)
- sensorMemoryCortex is registered in SENSOR_REGISTRY and fires the reach when the cortex signals (a stale governing thought or a fresh contradiction)
- data/connector-registry.json regenerated; build-connector-registry.cjs --check clean; no 7th reach minted
- test-150-connector.cjs green; no em-dashes
</success_criteria>

<output>
Create `.planning/phases/150-memory-cortex-as-graph-members-local-and-brain-queryable-when-reaching/150-05-SUMMARY.md` when done
</output>
