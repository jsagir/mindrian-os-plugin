---
phase: 89.5-engine-and-nl-graph
plan: "05"
type: execute
wave: 3
depends_on: ["89.5-01", "89.5-02", "89.5-03", "89.5-04"]
files_modified:
  - commands/rs-fetch.md
  - commands/rs-thesis.md
  - commands/rs-experts.md
  - commands/rs-explain.md
  - scripts/rs-fetch-command.cjs
  - scripts/rs-thesis-command.cjs
  - scripts/rs-experts-command.cjs
  - scripts/rs-explain-command.cjs
  - lib/memory/test-rs-explain-command.cjs
  - lib/memory/run-feynman-tests.cjs
  - .planning/phases/89.5-engine-and-nl-graph/89.5-PHASE-GATE.md
  - .planning/phases/89.5-engine-and-nl-graph/89.5-VERIFICATION.md
autonomous: true
canon_parts: [part-3, part-7, part-8]
milestone: v1.11.0
bundled_release: true
requirements:
  - SC-05
  - SC-06
  - SC-07
  - SC-08
  - SC-09
must_haves:
  truths:
    - "4 user-facing CLI commands shipped: /mos:rs-fetch + /mos:rs-thesis + /mos:rs-experts + /mos:rs-explain"
    - "/mos:rs-fetch <topic> runs the full pipeline (89.5-04 runDiscovery) and emits Phase Gate-style transcript"
    - "/mos:rs-thesis <discovery_id> reads thesis from room.db OR Aura (Tier 0/1 dispatch)"
    - "/mos:rs-experts <topic> runs Aura Cypher MATCH for expert network (Tier 1) OR graceful fallback message (Tier 0)"
    - "/mos:rs-explain <NL_query> runs the bidirectional NL-Graph entry point: rs-nl-to-query.translate -> graph execution -> rs-query-to-text.explain"
    - "Three-surface compatibility: each command works on CLI (Phase Gate transcript), Desktop MCP (structured JSON), Cowork (multi-user active room scope)"
    - "Feynman runner registration: 5 new test files registered (89.5-01 + 02 + 03 + 04 + 05) advancing baseline 85 -> 90"
    - "Phase Gate transcript filed at .planning/phases/89.5-engine-and-nl-graph/89.5-PHASE-GATE.md mirroring 89.4-PHASE-GATE.md format (9G + 5E asserts)"
    - "VERIFICATION.md filed mapping all 9 ROADMAP SCs to evidence; status=passed"
  artifacts:
    - path: "commands/rs-fetch.md"
      provides: "/mos:rs-fetch command markdown (allowed-tools + UI Format + body shape + frontmatter)"
      min_lines: 60
    - path: "commands/rs-thesis.md"
      provides: "/mos:rs-thesis command markdown"
      min_lines: 50
    - path: "commands/rs-experts.md"
      provides: "/mos:rs-experts command markdown"
      min_lines: 50
    - path: "commands/rs-explain.md"
      provides: "/mos:rs-explain command markdown (bidirectional NL-Graph entry; emphasizes Canon Part 8 chokepoint guarantee)"
      min_lines: 60
    - path: "scripts/rs-fetch-command.cjs"
      provides: "CLI wrapper invoking runDiscovery + emitting Phase Gate-style transcript"
      min_lines: 80
    - path: "scripts/rs-thesis-command.cjs"
      provides: "CLI wrapper for thesis lookup (room.db SELECT or Aura MATCH)"
      min_lines: 70
    - path: "scripts/rs-experts-command.cjs"
      provides: "CLI wrapper for expert network query (Aura Cypher MATCH or graceful Tier 0)"
      min_lines: 70
    - path: "scripts/rs-explain-command.cjs"
      provides: "CLI wrapper invoking rs-nl-to-query.translate + executing queries + rs-query-to-text.explain"
      min_lines: 100
    - path: "lib/memory/test-rs-explain-command.cjs"
      provides: "End-to-end CLI smoke test (NL in -> NL out via 3-graph triangulation); >=4 scenarios"
      min_lines: 120
    - path: "lib/memory/run-feynman-tests.cjs"
      provides: "5 new TEST_FILES entries appended (89.5-01 + 02 + 03 + 04 + 05); baseline 85 -> 90"
      min_lines: 615
    - path: ".planning/phases/89.5-engine-and-nl-graph/89.5-PHASE-GATE.md"
      provides: "Phase Gate transcript with 9G + 5E asserts mirroring 89.4 format"
      min_lines: 250
    - path: ".planning/phases/89.5-engine-and-nl-graph/89.5-VERIFICATION.md"
      provides: "Verification report mapping all 9 SCs to evidence; status=passed"
      min_lines: 200
  key_links:
    - from: "scripts/rs-fetch-command.cjs"
      to: "scripts/rs-discovery-engine.cjs (89.5-04)"
      via: "require + runDiscovery(topic)"
      pattern: "require.*rs-discovery-engine|runDiscovery"
    - from: "scripts/rs-explain-command.cjs"
      to: "lib/core/rs-nl-to-query.cjs (89.5-02) + lib/core/rs-query-to-text.cjs (89.5-03)"
      via: "require both; translate -> execute -> explain pipeline"
      pattern: "require.*rs-nl-to-query|require.*rs-query-to-text|translate|explain"
    - from: "lib/memory/run-feynman-tests.cjs"
      to: "5 new TEST_FILES entries"
      via: "appended after the 89.4 entries (lines 570-574); baseline advances 85 -> 90"
      pattern: "test-rs-commercial-assessor|test-rs-nl-to-query|test-rs-query-to-text|test-rs-discovery-engine|test-rs-explain-command"
---

<objective>
Close the phase. Ship the 4 user-facing CLI commands that wrap 89.5-01..04 modules + register 5 new fixture suites in the Feynman runner + file the Phase Gate transcript + file VERIFICATION.md.

Commands:
1. `/mos:rs-fetch <topic>` -- runs the full RS pipeline (89.5-04 runDiscovery); emits Phase Gate-style transcript.
2. `/mos:rs-thesis <discovery_id>` -- gets thesis for a prior discovery (room.db OR Aura via Tier dispatch).
3. `/mos:rs-experts <topic>` -- expert network query (Aura Cypher MATCH or Tier 0 graceful).
4. `/mos:rs-explain <NL_query>` -- bidirectional NL-Graph entry point: rs-nl-to-query.translate(nl) -> execute the 3 queries -> rs-query-to-text.explain(results) -> Larry-voiced NL output. THIS is the user-visible bidirectional loop.

Three-surface compatibility (CLI + Desktop MCP + Cowork): commands surface library output uniformly; CLI gets Phase Gate transcript, Desktop MCP gets structured JSON, Cowork honors multi-user active-room scope via folder-memory.

Phase Gate transcript: 9 G + 5 E asserts mirroring `89.4-PHASE-GATE.md`. E1-E5 cover orchestrator chain composition, NL-to-Query chokepoint exclusivity, NL-to-Query defense-in-depth (>=6 adversarial), Query-to-Text voice adherence, end-to-end CLI smoke.

VERIFICATION.md: maps all 9 ROADMAP SCs to evidence; status=passed.

Output: 4 command markdown files + 4 command CJS scripts + 1 end-to-end smoke test file + 5 Feynman registrations + 1 Phase Gate transcript + 1 VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89.5-engine-and-nl-graph/89.5-CONTEXT.md
@.planning/phases/89.5-engine-and-nl-graph/89.5-01-PLAN.md
@.planning/phases/89.5-engine-and-nl-graph/89.5-02-PLAN.md
@.planning/phases/89.5-engine-and-nl-graph/89.5-03-PLAN.md
@.planning/phases/89.5-engine-and-nl-graph/89.5-04-PLAN.md
@.planning/STATE.md
@.planning/ROADMAP.md

<!-- Reference Phase Gate format to mirror -->
@.planning/phases/89.4-chain-wiring/89.4-PHASE-GATE.md
@.planning/phases/89.4-chain-wiring/89.4-VERIFICATION.md

<!-- Reference command patterns -->
@commands/find-analogies.md
@commands/diagnostics.md
@scripts/diagnostics-command.cjs

<!-- Modules wired by the commands -->
@scripts/rs-discovery-engine.cjs
@lib/core/rs-nl-to-query.cjs
@lib/core/rs-query-to-text.cjs
@lib/core/rs-commercial-assessor.cjs

<!-- Feynman runner to extend -->
@lib/memory/run-feynman-tests.cjs

<!-- Library helpers for graph execution in commands -->
@lib/core/lazygraph-ops.cjs
@lib/core/brain-client.cjs
@lib/core/folder-memory.cjs

<!-- Defense-in-depth -->
@lib/core/rs-egress-violations.cjs
@lib/core/rs-egress-prompts.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship 4 command markdown files (commands/rs-fetch.md + rs-thesis.md + rs-experts.md + rs-explain.md)</name>
  <files>commands/rs-fetch.md, commands/rs-thesis.md, commands/rs-experts.md, commands/rs-explain.md</files>
  <action>
    Mirror the structure of `commands/find-analogies.md` (frontmatter: name + description + body_shape + ui_reference + allowed-tools) and `commands/diagnostics.md` for command patterns.

    For each of the 4 commands, the markdown body MUST include:
    - Command name and synopsis (e.g., `/mos:rs-fetch <topic>` -- one-liner purpose).
    - UI Format section (body shape + zone descriptions; mirror `commands/find-analogies.md` body shape D pattern OR `commands/diagnostics.md` Shape E (Action Report) for rs-fetch since it emits a Phase Gate-style transcript).
    - Allowed-tools list (Read + Write + Bash for invoking the script; mcp__mindrian-brain__brain_query when Brain involvement; mcp__neo4j-brain__read_neo4j_cypher when Aura involvement; per command).
    - "What it does" section.
    - "Three-surface notes" section (1-2 sentences each on CLI / Desktop MCP / Cowork behavior).
    - For rs-explain.md ONLY: explicit "Canon Part 8 guarantee" section noting that user NL is parameterized + chokepoint-audited; user content never reaches Brain.

    Specific descriptions:
    - rs-fetch: "Run full Reverse Salient discovery pipeline for <topic>. Domain analysis -> external research -> scoring -> thesis -> output layer -> chain emission. Mode A (Brain + Aura connected) shows full bundle; Mode B (Brain offline) shows local pipeline output. Tier 0 (Aura offline) writes to SQLite mirror."
    - rs-thesis: "Read thesis for prior <discovery_id>. Looks up in Aura first (Tier 1); falls back to local SQLite room.db (Tier 0)."
    - rs-experts: "Resolve expert network for <topic>. Aura Cypher MATCH on AUTHORED_BY edges (Tier 1); Tier 0 returns graceful 'Aura not connected' message with rs-fetch suggestion."
    - rs-explain: "Bidirectional NL-Graph entry point. Type a question; system queries 3 graphs (room.db + Aura + Brain); returns Larry-voiced explanation. User NL is parameterized + chokepoint-audited (Canon Part 8); user content never reaches Brain."

    Each file: BSL 1.1 (NOT applicable to .md; use a comment-style attribution in the frontmatter or footer if your command markdown convention supports it -- check `commands/find-analogies.md` for the project pattern; if no BSL on .md, omit). Zero em-dashes. Use `--` for prose dashes.

    Single commit: `feat(89.5-05): add 4 user-facing command markdowns (/mos:rs-fetch, /mos:rs-thesis, /mos:rs-experts, /mos:rs-explain)`.
  </action>
  <verify>
    <automated>ls commands/rs-fetch.md commands/rs-thesis.md commands/rs-experts.md commands/rs-explain.md && wc -l commands/rs-{fetch,thesis,experts,explain}.md | head -4 | awk '{if ($1 < 50) {print "TOO SHORT:", $0; exit 1}}'; grep -lP '[\x{2013}\x{2014}]' commands/rs-fetch.md commands/rs-thesis.md commands/rs-experts.md commands/rs-explain.md && exit 1; echo "PASS"</automated>
  </verify>
  <done>4 command files exist; each has frontmatter + body; zero em-dashes across all 4; line counts >=50.</done>
</task>

<task type="auto">
  <name>Task 2: Ship 4 command CJS scripts (scripts/rs-fetch-command.cjs + rs-thesis-command.cjs + rs-experts-command.cjs + rs-explain-command.cjs)</name>
  <files>scripts/rs-fetch-command.cjs, scripts/rs-thesis-command.cjs, scripts/rs-experts-command.cjs, scripts/rs-explain-command.cjs</files>
  <action>
    Mirror the structure of `scripts/diagnostics-command.cjs` (BSL 1.1 header + 'use strict' + dispatcher pattern + exit codes).

    Each script:
    - BSL 1.1 header.
    - `'use strict';`
    - Parse `process.argv[2]` as the primary input (topic / discovery_id / NL query).
    - Invoke the corresponding library module.
    - Emit Phase Gate-style transcript on success (CLI mode); emit JSON on `--json` flag (Desktop MCP mode); honor `MINDRIAN_ROOM` env var for active-room scope (Cowork mode).
    - Exit codes: 0=success, 1=invocation error, 2=prerequisite missing.
    - Zero em-dashes.

    SPECIFIC IMPLEMENTATIONS:

    **scripts/rs-fetch-command.cjs:**
    - require('../scripts/rs-discovery-engine.cjs') -> runDiscovery(topic, opts).
    - opts = {room_dir: process.env.MINDRIAN_ROOM || process.cwd(), problem_type: ..., stage: ...} (allow CLI flags for problem_type + stage; defaults from STATE.md when absent).
    - On success: print Phase Gate-style transcript (header line + per-phase summary + chain_metadata block).
    - On `--json`: `console.log(JSON.stringify(bundle, null, 2));`.
    - On pause state ({state: 'pause', missing_upstream}): print "PAUSED -- run upstream framework: <missing>" exit 0 (graceful pause is not an error).

    **scripts/rs-thesis-command.cjs:**
    - require('../lib/core/lazygraph-ops.cjs') for room.db SELECT.
    - require('../lib/core/brain-client.cjs') for Aura MATCH (when isAvailable).
    - Look up discovery_id; emit thesis text.
    - Tier 0/1 dispatch: try Aura first; fall back to SQLite mirror on AuraUnreachableError; emit DEGRADED_NOTE marker when in Tier 0.

    **scripts/rs-experts-command.cjs:**
    - require('../lib/core/brain-client.cjs').isAvailable; if true, run Aura Cypher MATCH `(a:Author)-[:AUTHORED_BY]->(p:Paper) WHERE p.topic = $topic RETURN a` with parameterized $topic.
    - If false: emit "Aura not connected. Run /mos:rs-fetch <topic> first to populate the local SQLite mirror, then retry." (graceful Tier 0 fallback per 89.3 pattern).
    - Topic parameter audited via auditQueryString before binding (Canon Part 8 input chokepoint).

    **scripts/rs-explain-command.cjs (bidirectional NL-Graph):**
    - require('../lib/core/rs-nl-to-query.cjs') -> translate(nl_query, opts) -> {cypher, sql, brain_query, sql_params, cypher_params}.
    - For each non-null query, execute via the appropriate runtime:
      - sql: lazygraph-ops.queryGraph(conn, sql, sql_params).
      - cypher: brain-client.query(cypher) (Aura) -- but only if cypher targets Aura, not Brain methodology; since rs-nl-to-query produces both Aura local Cypher AND Brain Cypher in distinct fields, keep them separate. Aura Cypher executes via brainClient.query (existing chokepoint reuse) when Aura is reachable; Tier 0 skips it gracefully.
      - brain_query: brain-client.query(brain_query.cypher) ONLY when brain_query is non-null (the chokepoint already stripped to allow-list scalars per 89.5-02).
    - Aggregate results into `query_results = {kind: <inferred from intent>, rows: [...combined], room_context: ...}`.
    - require('../lib/core/rs-query-to-text.cjs') -> explain(query_results, opts) -> Larry-voiced NL string.
    - Print the explanation. On `--json`: also emit the raw query_results.

    Single commit: `feat(89.5-05): add 4 command CJS scripts wrapping 89.5 library modules`.
  </action>
  <verify>
    <automated>ls scripts/rs-fetch-command.cjs scripts/rs-thesis-command.cjs scripts/rs-experts-command.cjs scripts/rs-explain-command.cjs && for f in scripts/rs-{fetch,thesis,experts,explain}-command.cjs; do node -c "$f" || exit 1; done; grep -cP '[\x{2013}\x{2014}]' scripts/rs-{fetch,thesis,experts,explain}-command.cjs | grep -v ":0$" && exit 1; echo "PASS"</automated>
  </verify>
  <done>4 CJS scripts exist; each parses with `node -c`; zero em-dashes across all 4; BSL 1.1 headers present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD end-to-end CLI smoke test for /mos:rs-explain (lib/memory/test-rs-explain-command.cjs; >=4 scenarios)</name>
  <files>lib/memory/test-rs-explain-command.cjs</files>
  <behavior>
    - Test 1 (happy path NL -> NL): mock the 3-graph executors (sqliteMirror queryGraph + brainClient.query); call rs-explain-command.cjs's main() with NL = "Show me reverse salients in my fintech room"; assert returned NL string is non-empty AND contains 'reverse salients' AND honors voice templates from rs-query-to-text.cjs.
    - Test 2 (Mode B Brain offline): mock brainClient.isAvailable to return false; call main() with NL = "What frameworks chain into RS Discovery?" (intent: feeds_into_query, normally Brain-only); assert graceful response: either (a) returns "Brain offline; cannot answer methodology chain queries; try /mos:rs-fetch first" OR (b) returns a fallback explanation using local-only data; NO throw.
    - Test 3 (Tier 0 Aura offline): mock brainClient.query to throw AuraUnreachableError on Aura Cypher (but Brain methodology query OK); call main() with NL = "Who are experts on quantum computing?" (Aura-only intent); assert graceful Tier 0 response.
    - Test 4 (Canon Part 8 adversarial): call main() with NL containing forbidden field name (e.g., "What's my governing thought on X?"); rs-nl-to-query.translate throws ExternalEgressViolation; rs-explain-command catches the throw and emits user-friendly error message ("Query rejected by Canon Part 8 audit: <surface>"); exit code 1.
    - Test 5: BSL header check on all 4 command CJS scripts (loop assertion).
    - Test 6: A1 sweep on all rendered NL outputs from Tests 1-4; assert ZERO matches against FORBIDDEN_PATTERNS in any output.
  </behavior>
  <action>
    Write the test file first (RED). Mock the 3-graph executors via Module._cache shim. Run: must exit non-zero. Commit: `test(89.5-05): RED -- failing test-rs-explain-command.cjs (>=4 scenarios; happy path + Mode B + Tier 0 + Canon Part 8 adversarial)`.

    Then ensure scripts/rs-explain-command.cjs (Task 2) handles all 4 scenarios. Run again: must exit 0. If issues found, fix the command script (NOT the test). Commit: `feat(89.5-05): GREEN -- rs-explain-command handles Mode B + Tier 0 + Canon Part 8 adversarial; all 4 e2e scenarios green`.

    Print `=== 89.5-05 rs-explain-command suite: N/M passed ===` at end. Exit 1 on any fail.
  </action>
  <verify>
    <automated>node lib/memory/test-rs-explain-command.cjs; test $? -eq 0</automated>
  </verify>
  <done>GREEN: `=== 89.5-05 rs-explain-command suite: N/N passed ===` (>=4); exit 0.</done>
</task>

<task type="auto">
  <name>Task 4: Register 5 new fixture suites in lib/memory/run-feynman-tests.cjs (baseline 85 -> 90)</name>
  <files>lib/memory/run-feynman-tests.cjs</files>
  <action>
    Read `lib/memory/run-feynman-tests.cjs`; locate the 89.4 entries (lines ~568-574). Append 5 new entries AFTER the 89.4 block:

    ```js
    // Phase 89.5-01: rs-commercial-assessor (deterministic 3-field commercial layer)
    path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-commercial-assessor.cjs'),
    // Phase 89.5-02: rs-nl-to-query (HARDEST Canon Part 8 surface; 6+ adversarial fixtures)
    path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-nl-to-query.cjs'),
    // Phase 89.5-03: rs-query-to-text (Larry-voiced NL explanation; voice templates)
    path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-query-to-text.cjs'),
    // Phase 89.5-04: rs-discovery-engine (top-level orchestrator; chain composition)
    path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-discovery-engine.cjs'),
    // Phase 89.5-05: rs-explain-command (end-to-end CLI smoke; bidirectional NL-Graph)
    path.join(REPO_ROOT, 'lib', 'memory', 'test-rs-explain-command.cjs'),
    ```

    Run `node lib/memory/run-feynman-tests.cjs` -> baseline advances 85 -> 90 (5 new entries); the 5 new suites must all PASS at the new positions; pre-existing 4 failures from 89.4 G8 (out-of-scope) persist but are tracked separately.

    Commit: `test(89.5-05): register 5 new 89.5 fixture suites in Feynman runner; baseline 85 -> 90`.
  </action>
  <verify>
    <automated>grep -cE "Phase 89\.5-0[1-5]" lib/memory/run-feynman-tests.cjs | xargs -I{} test {} -eq 5 && grep -cE "test-rs-(commercial-assessor|nl-to-query|query-to-text|discovery-engine|explain-command)\.cjs" lib/memory/run-feynman-tests.cjs | xargs -I{} test {} -eq 5 && echo PASS</automated>
  </verify>
  <done>5 new TEST_FILES entries appear after the 89.4 block (lines >=576); each new suite passes when run via the runner; runner shows 86/90 PASS (90 total minus the 4 pre-existing out-of-scope failures from 89.4 G8).</done>
</task>

<task type="auto">
  <name>Task 5: File 89.5-PHASE-GATE.md transcript (9 G + 5 E asserts mirroring 89.4-PHASE-GATE.md format)</name>
  <files>.planning/phases/89.5-engine-and-nl-graph/89.5-PHASE-GATE.md</files>
  <action>
    Mirror `89.4-PHASE-GATE.md` (read it first; copy the structure verbatim; substitute Phase 89.5 specifics).

    Frontmatter:
    ```
    ---
    phase: 89.5
    slug: engine-and-nl-graph
    plan: "05"
    doc_type: phase-gate-transcript
    status: PASS | CONDITIONAL PASS (depending on G8 result)
    executed_at: "<UTC ISO timestamp>"
    milestone: v1.11.0
    bundled_release: true
    canon_parts: [part-3, part-7, part-8]
    ---
    ```

    9 G asserts (mirrors 89.4):
    - G1: Zero new runtime deps (`git diff <89.5-base>..HEAD -- package.json | wc -l` == 0).
    - G2: BSL 1.1 headers on every new/modified .cjs file (8 files: 4 lib/core + scripts/rs-discovery-engine.cjs + 4 scripts/rs-*-command.cjs OR adjusted by what shipped).
    - G3: Zero em-dashes on every new/modified file.
    - G4: Canon Part 8 forbidden-field audit (zero matches in production .cjs files; tests excluded as adversarial fixtures per 89.4 G4 scope).
    - G5: Chokepoint exclusivity (ALL Brain queries from 89.5 modules + scripts route through brainClient.query OR rs-nl-to-query.buildBrainQueryFromNL; zero direct fetch in any 89.5 module/script).
    - G6: 5 new Feynman suites registered (grep "Phase 89.5-0[1-5]" returns 5).
    - G7: Each new fixture suite green when invoked directly (5 suites, all exit 0).
    - G8: Full Feynman runner (expected 86/90 PASS; the 4 inherited pre-existing failures persist; document them as inherited from 89.4 G8).
    - G9: ExternalEgressViolation surfaces only via the SHARED class (rs-egress-violations.cjs); no 89.5 module redefines.

    5 E asserts (89.5-specific):
    - E1: orchestrator chain composition -- runDiscovery returns bundle with all 11 required fields populated (topic, domain_analysis, query_matrix, fetched_results, preprocessed, scored, classified, breakthroughs, theses, commercial, output, chain_metadata) for a happy-path mocked input.
    - E2: NL-to-Query chokepoint exclusivity -- grep buildBrainQueryFromNL >= 1 in rs-nl-to-query.cjs; grep auditQueryObject >= 3 in rs-nl-to-query.cjs; grep fetch( == 0 in rs-nl-to-query.cjs.
    - E3: NL-to-Query defense-in-depth -- >=6 adversarial scenarios pass in test-rs-nl-to-query.cjs (verify by counting `Test 8` through `Test 13` PASS lines in the test output).
    - E4: Query-to-Text voice adherence -- explain() output contains Evidence -> Insight -> Warning structure markers; voice templates frozen and >=3 per kind.
    - E5: end-to-end CLI smoke -- /mos:rs-explain "Show me reverse salients in my fintech room" produces non-empty Larry-voiced NL output; full bidirectional loop verified.

    Include a Gate Summary table + Invariant Coverage table mirroring 89.4 format. Document inherited 89.1a + 89.1 + 89.2 + 89.3 + 89.4 invariants preserved by 89.5 (mirror 89.4-PHASE-GATE.md "Invariant Coverage" section).

    Commit: `docs(89.5-05): Phase Gate transcript -- 9G + 5E asserts; <PASS|CONDITIONAL PASS>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/89.5-engine-and-nl-graph/89.5-PHASE-GATE.md && grep -cE "^### G[1-9]:" .planning/phases/89.5-engine-and-nl-graph/89.5-PHASE-GATE.md | xargs -I{} test {} -eq 9 && grep -cE "^### E[1-5]:" .planning/phases/89.5-engine-and-nl-graph/89.5-PHASE-GATE.md | xargs -I{} test {} -eq 5 && echo PASS</automated>
  </verify>
  <done>89.5-PHASE-GATE.md exists with 9 G asserts + 5 E asserts; status field set; commit landed.</done>
</task>

<task type="auto">
  <name>Task 6: File 89.5-VERIFICATION.md (maps 9 ROADMAP SCs to evidence; status=passed)</name>
  <files>.planning/phases/89.5-engine-and-nl-graph/89.5-VERIFICATION.md</files>
  <action>
    Mirror `89.4-VERIFICATION.md` structure (read it first).

    Frontmatter:
    ```
    ---
    phase: 89.5-engine-and-nl-graph
    verified: <UTC ISO timestamp>
    status: passed
    score: 9/9 success criteria verified
    canon_parts: [part-3, part-7, part-8]
    milestone: v1.11.0
    bundled_release: true
    re_verification: false
    verifier: gsd-executor (Claude Opus 4.7 1M)
    phase_gate_status: <PASS | CONDITIONAL PASS> (per 89.5-PHASE-GATE.md G8 outcome)
    phase_gate_commit: <commit hash of phase gate>
    verification_head_commit: <commit hash at verification time>
    ---
    ```

    Body sections:
    1. Phase Goal (verbatim from ROADMAP entry).
    2. Verified date + Status + Re-verification flag.
    3. Goal Achievement Summary (1-2 paragraphs noting all 9 SCs verified; reference Phase Gate transcript).
    4. Success Criteria Verification (one section per SC, each with: Status: VERIFIED + Evidence subsection with file paths + grep counts + runtime smoke commands + Phase Gate cross-references).

    9 SCs (from ROADMAP entry):
    - SC-01: scripts/rs-discovery-engine.cjs exports runDiscovery(topic, opts) -- end-to-end pipeline; returns full RSDiscovery bundle.
    - SC-02: rs-commercial-assessor.cjs exports assess(rs_discovery, opts) -- deterministic market + value-prop + partnership output.
    - SC-03: rs-nl-to-query.cjs exports translate(nl_query, opts) -- returns {cypher, sql, brain_query} triangulated query bundle; Canon Part 8 audit on all 3 surfaces.
    - SC-04: rs-query-to-text.cjs exports explain(query_results, opts) -- returns Larry-voiced NL explanation with pedagogical framing.
    - SC-05: /mos:rs-fetch + /mos:rs-thesis + /mos:rs-experts + /mos:rs-explain commands shipped and three-surface compatible.
    - SC-06: End-to-end smoke test under live conditions -- full pipeline runs without error.
    - SC-07: Canon Part 8 verified -- Brain query audit on every Brain call from NL-to-Query; user content never crosses outbound boundary.
    - SC-08: Feynman test suite stays green + new tests registered (~85 -> ~90+).
    - SC-09: NO release gate -- bundled into v1.11.0 final ship at Phase 91.x per milestone §7.

    Each SC verification block carries:
    - File existence on disk + line counts.
    - Runtime export contracts (typeof checks).
    - Adversarial fixture coverage (for SC-03/SC-07 specifically: enumerate >=6 Canon Part 8 scenarios from test-rs-nl-to-query.cjs).
    - Phase Gate assert cross-reference (G_n / E_n).
    - Commit references for SUMMARY-level traceability.

    Conclude with "Verification Conclusion: passed" + "Phase 89.5 goal '<goal>' is achieved." block.

    Commit: `docs(89.5-05): VERIFICATION report -- 9/9 SCs passed; phase complete; v1.11.0-beta.1 readiness gate cleared`.
  </action>
  <verify>
    <automated>test -f .planning/phases/89.5-engine-and-nl-graph/89.5-VERIFICATION.md && grep -c "^### SC-0[1-9]" .planning/phases/89.5-engine-and-nl-graph/89.5-VERIFICATION.md | xargs -I{} test {} -eq 9 && grep -c "Status: VERIFIED\|Status:passed\|status: passed" .planning/phases/89.5-engine-and-nl-graph/89.5-VERIFICATION.md | xargs -I{} test {} -ge 1 && echo PASS</automated>
  </verify>
  <done>89.5-VERIFICATION.md exists with 9 SC sections; status=passed; commit landed.</done>
</task>

</tasks>

<verification>
- 4 command markdown files exist in commands/ (rs-fetch.md + rs-thesis.md + rs-experts.md + rs-explain.md); each >=50 lines; zero em-dashes.
- 4 command CJS scripts exist in scripts/ (rs-fetch-command.cjs + rs-thesis-command.cjs + rs-experts-command.cjs + rs-explain-command.cjs); each parses with `node -c`; BSL 1.1 headers; zero em-dashes.
- lib/memory/test-rs-explain-command.cjs exists; >=4 scenarios; exits 0.
- lib/memory/run-feynman-tests.cjs has 5 new TEST_FILES entries after the 89.4 block; grep "Phase 89.5-0[1-5]" returns 5.
- Feynman runner: 86/90 PASS at 89.5 phase HEAD (90 total minus 4 inherited pre-existing failures from 89.4 G8).
- 89.5-PHASE-GATE.md exists; 9 G asserts + 5 E asserts; mirrors 89.4 format.
- 89.5-VERIFICATION.md exists; 9 SC sections; status=passed.
- Zero new runtime deps in package.json across the entire 89.5 commit range.
- Zero release artifacts modified (CHANGELOG.md / .claude-plugin/plugin.json / package.json byte-identical at 89.5 phase HEAD vs base; bundled_release: true; v1.11.0 ships at the v1.11.0-beta.1 gate sub-phase ADDED AFTER 89.5).
</verification>

<success_criteria>
ALL 9 ROADMAP SCs verified:
- SC-01: rs-discovery-engine.cjs exports runDiscovery (verified by 89.5-04 GREEN + this Plan E1).
- SC-02: rs-commercial-assessor.cjs exports assess (verified by 89.5-01 GREEN).
- SC-03: rs-nl-to-query.cjs exports translate (verified by 89.5-02 GREEN + this Plan E2).
- SC-04: rs-query-to-text.cjs exports explain (verified by 89.5-03 GREEN + this Plan E4).
- SC-05: 4 commands shipped + three-surface compatible (verified by Tasks 1 + 2).
- SC-06: end-to-end smoke verified (verified by 89.5-04 Test 1 + this Plan Task 3 Test 1).
- SC-07: Canon Part 8 verified across all NL-to-Query and orchestrator paths (verified by 89.5-02 + 89.5-04 + this Plan E2 + E3).
- SC-08: Feynman 85 -> 90 baseline advance (verified by Task 4).
- SC-09: NO release gate (no CHANGELOG/plugin.json/package.json diffs in 89.5 commit range; verified by G1).

The phase is the v1.11.0-beta.1 readiness gate. The release gate itself is a SEPARATE sub-phase to be added between 89.5 and 91 per user release strategy. This plan does NOT ship a version bump.
</success_criteria>

<output>
After completion, create `.planning/phases/89.5-engine-and-nl-graph/89.5-05-SUMMARY.md` per the standard SUMMARY template.

Then verify the phase folder contains: 89.5-CONTEXT.md + 89.5-01..05-PLAN.md + 89.5-01..05-SUMMARY.md + 89.5-PHASE-GATE.md + 89.5-VERIFICATION.md (full closure).
</output>
