---
phase: 89.5-engine-and-nl-graph
plan: "03"
subsystem: rs-framework
tags: [reverse-salient, query-to-text, larry-voice, deterministic, canon-part-8, frozen-templates, no-runtime-llm, tdd, mode-a-mode-b]

requires:
  - phase: 88-feynman-minto-memory-layer
    provides: "folder-memory.readTriple (ROOM + STATE + MINTO sync read contract)"
  - phase: 90-brain-derivation-layer
    provides: "folder-memory.readQuadruple (additive ROOM + STATE + MINTO + BRAIN read contract; quadruple is Mode A; brain:null is Mode B)"
  - phase: 89.2-external-research-fetching
    provides: "rs-egress-violations.cjs (ExternalEgressViolation class) + rs-egress-prompts.cjs (auditQueryString + auditQueryObject + FORBIDDEN_PATTERNS) -- the egress chokepoint primitives"

provides:
  - "lib/core/rs-query-to-text.cjs with explain(query_results, opts) -> Larry-voiced NL explanation string"
  - "Frozen VOICE_TEMPLATES (5 kinds: rs_discovery=4, expert_network=3, methodology_chain=3, discovery_history=3, fallback=3 -- 16 total templates)"
  - "Deterministic FNV-1a hash-based template selection (same input -> same render across runs)"
  - "Venture context enrichment via folder-memory.readQuadruple: Mode A surfaces brain.sections.pattern_matches body when fresh; Mode B (brain null OR stale) suppresses Brain marker"
  - "12-scenario fixture suite: 4 positive-path result types + Mode A enrichment + Mode B fallback + no-LLM invariant proof + 2 adversarial Canon Part 8 + voice pedagogy + A1 sweep + VOICE_TEMPLATES surface"

affects: [89.5-04-rs-discovery-engine, 89.5-05-cli-commands, 91-navigation-engine]

tech-stack:
  added: []
  patterns:
    - "Larry-voiced rendering pattern: voice patterns extracted from skills/larry-personality SKILL.md at design time, frozen as VOICE_TEMPLATES; NO runtime LLM call; deterministic template selection via FNV-1a hash on (kind + first row id/thesis) so same input -> same render"
    - "Quadruple-aware Mode A/B/Tier-0 graceful degradation: Mode A (brain.exists + staleness fresh + sections.pattern_matches.body) surfaces 'Brain says: ...' marker; Mode B (brain null OR brain.staleness !== fresh) emits empty brain_line; Tier 0 (no room_dir) emits empty room_line + 'no active room context' default"
    - "2-seam Canon Part 8 defense: SEAM A (auditQueryObject on input bundle, with _test_quadruple_override stripped before audit so legitimate fixture bytes do not trip), SEAM B (auditQueryString on rendered output -- last line of defense against template-substitution leaks)"
    - "Frozen VOICE_TEMPLATES: 5 result kinds, each independently Object.freeze'd; expanding template count or adding a new kind requires a Plan-N revision (canon-amend discipline; new templates expand surface area Canon Part 8 must defend)"
    - "Graceful fillTemplate: missing {placeholder} keys -> empty string (no exceptions); double-space collapse + punctuation tidying ensures rendered string stays coherent even when many slots empty (e.g. when brain_line and room_line are both empty)"
    - "Test surface: _test_quadruple_override field on opts lets fixtures inject arbitrary quadruple shapes without touching disk; production callers never set this field (readQuadruple loads vetted local bytes); audit input scrub strips this field before SEAM A so legitimate fixture body text does not trip"

key-files:
  created:
    - lib/core/rs-query-to-text.cjs
    - lib/memory/test-rs-query-to-text.cjs
  modified: []

key-decisions:
  - "VOICE_TEMPLATES kept SMALL (3-4 templates per kind) intentionally. The deterministic hash routes inputs across templates; adding a fifth template doubles the surface area Canon Part 8 must defend. Future expansion comes via canon-amend Plan-N revisions, not ad-hoc additions."
  - "Brain enrichment surfaces in the rendered string ONLY when brain.exists === true AND brain.staleness === 'fresh' AND sections.pattern_matches (or cross_domain_analogies) carry a non-empty body. Stale brain (e.g. governing_thought_changed) renders as Mode B even though brain object is non-null -- the staleness signal is the gating field. Mirrors isQuadrupleFresh semantics (Phase 90)."
  - "_test_quadruple_override is the test injection seam. Production callers ONLY set room_dir; readQuadruple loads bytes through the Phase 88+90 read contract. The audit strip in explain() removes the override key from the audited input bundle so legitimate fixture body text (e.g. 'expand to APAC' governing_thought) does not trip SEAM A. SEAM A still audits query_results + every other opt field."
  - "FNV-1a 32-bit hash chosen over Math.random or modulo of timestamp for template selection. Determinism is non-negotiable: same input must render the same template across runs so consumers (test fixtures, MCP callers, snapshot diffs) are stable. FNV-1a is pure JS, fast, and well-distributed across small input variations."
  - "explain() returns a STRING (not JSON). Per CONTEXT lines 23-24: the orchestrator surface emits this as Phase Gate-style transcript content. Returning a string keeps the contract minimal; structured callers can wrap the string in their own envelope. Audit SEAM B operates on the string directly (auditQueryString) -- catches template-substitution leaks BEFORE return."
  - "Canon Part 8 chokepoint anatomy mirrors 89.4-01 / 89.4-02 / 89.5-02: 2 audit sites (input + output) on every public entry point. Module never imports brain-client.cjs (verified by source-level grep in T7). Module never calls fetch (verified by source-level grep + runtime fetch shim in T7). Audit sites count = 2 in body + 1 in import (require destructure)."
  - "Punctuation tidying via /\\s{2,}/g + /\\s+([.,;:!?])/g cleanup runs after every fillTemplate. Necessary because empty {brain_line} or {room_line} substitutions leave double-spaces that would otherwise render awkwardly (e.g. '...filed.  Worth Bank Opportunity.' becomes '...filed. Worth Bank Opportunity.')"

patterns-established:
  - "Query-to-Text as the rendering half of the Bidirectional NL-Graph Surface: rs-nl-to-query.cjs (89.5-02) translates user NL to {cypher, sql, brain_query}; rs-query-to-text.cjs (this plan) translates raw graph results back to Larry-voiced NL. The orchestrator (89.5-04) and /mos:rs-explain CLI (89.5-05) chain them end-to-end."
  - "NO RUNTIME LLM is a hard architectural invariant for the bidirectional surface. Voice patterns are frozen at design time; deterministic hash selection ensures stable output across runs. Adding LLM-aided rendering would re-introduce a different egress surface that would require its own Canon Part 8 review."
  - "Mode A/B/Tier-0 tier-awareness pattern (Canon Part 3): Mode A renders Brain enrichment; Mode B suppresses Brain marker but still renders coherent NL; Tier 0 (no room_dir) renders with safe defaults. The pattern mirrors Phase 91 Navigation Engine option-generation tier-awareness from the v1 frozen interface contract."

requirements-completed: [SC-04]

duration: 8m
completed: 2026-04-26
---

# Phase 89.5 Plan 03: rs-query-to-text Summary

**Bidirectional NL-Graph Surface (b) Query->Text: raw graph results translate to Larry-voiced NL via frozen VOICE_TEMPLATES (5 kinds x 3-4 templates = 16 total) + deterministic FNV-1a template selection + venture context enrichment via folder-memory.readQuadruple + 2-seam Canon Part 8 defense. NO runtime LLM. Mode A/B/Tier-0 graceful degradation across the BRAIN.md presence axis.**

## Performance

- **Duration:** ~8 min
- **Completed:** 2026-04-26
- **Tasks:** 2 (RED + GREEN)
- **Files created:** 2

## Accomplishments

- `lib/core/rs-query-to-text.cjs` (438 lines) -- exports `explain(query_results, opts) -> string`. Frozen `VOICE_TEMPLATES` (5 kinds: rs_discovery=4, expert_network=3, methodology_chain=3, discovery_history=3, fallback=3 = 16 total templates). Deterministic FNV-1a template selection. Mode A surfaces brain.sections.pattern_matches body via 'Brain says: ...' marker; Mode B suppresses it. Pure CJS, zero npm deps, BSL 1.1.
- `lib/memory/test-rs-query-to-text.cjs` (551 lines) -- 12-scenario fixture suite, 12/12 passed: 4 positive-path result types (T1-T4), Mode A quadruple enrichment (T5), Mode B fallback (T6), no-LLM invariant proof (T7), 2 adversarial Canon Part 8 (T8 email + T9 meeting fragment), voice pedagogy adherence (T10), A1 sweep across 5+ positive outputs (T11), VOICE_TEMPLATES surface conformance (T_extra).
- Phase 89.5-04 orchestrator now has the Query-to-Text rendering branch ready for Bidirectional Surface composition; Phase 89.5-05 /mos:rs-explain CLI command now has the explain() entry point ready.

## Task Commits

1. **Task 1 (RED):** test-rs-query-to-text.cjs (12 scenarios) -- `26b4997` (test)
2. **Task 2 (GREEN):** rs-query-to-text.cjs (frozen VOICE_TEMPLATES + deterministic FNV-1a kind detection + Mode A/B venture context enrichment via readQuadruple + 2-seam audit) -- `ab17c31` (feat)

## Files Created/Modified

- `lib/core/rs-query-to-text.cjs` -- Larry-voiced explainer. explain() top-level entry + detectKind() + loadVentureContext() + pickTemplate() + buildPlaceholderMap() + fillTemplate() + safeScalar() + frozen VOICE_TEMPLATES (5 kinds) + frozen KNOWN_KINDS + BSL 1.1 header.
- `lib/memory/test-rs-query-to-text.cjs` -- 12-scenario suite (T1-T11 + T_extra).

## VOICE_TEMPLATES Inventory (16 frozen templates)

### rs_discovery (4 templates)

1. `Found {n} reverse salients across {domain_count} domains. The strongest is {top_thesis} (breakthrough score {top_score}). Filed to opportunity-bank/. {brain_line}{room_line}Worth Bank Opportunity or Devils Advocate.`
2. `{n} discoveries surfaced. Top: {top_thesis}. Pattern: {classification}. {brain_line}{room_line}Next: Bank Opportunity or Run Methodology /mos:lean-canvas.`
3. `Reading the Room: {room_context}. {n} candidates ranked. Strongest: {top_thesis}. {brain_line}Worth filing.`
4. `{n} differential pairs cleared the dual floor. Top: {top_thesis} (score {top_score}). {brain_line}{room_line}Merits Run Methodology.`

### expert_network (3 templates)

1. `Mapped {n} experts across {institution_count} institutions. Top institutions: {institutions}. {brain_line}{room_line}/mos:persona may be warranted.`
2. `{n} authors resolved. Institutions: {institutions}. {brain_line}{room_line}Worth Run Methodology /mos:persona.`
3. `Found {n} domain experts. Leading institutions: {institutions}. {brain_line}Cross-reference your room: {room_context}.`

### methodology_chain (3 templates)

1. `{source} chains into {n} downstream methodologies: {chain_list}. {brain_line}{room_line}Given your room stage {state_stage}, the next hop is {recommended}.`
2. `Methodology graph: {source} -> {chain_list} ({n} hops). {brain_line}Synthesize next? Or Run Methodology on {recommended}.`
3. `{n} downstream paths from {source}. Pick one: {chain_list}. {brain_line}{room_line}Worth Synthesize.`

### discovery_history (3 templates)

1. `You have {n} RS discoveries logged in this room. Most recent: {recent_thesis}. The pattern: {convergence}. {brain_line}{room_line}Worth Synthesize via /mos:reflect.`
2. `{n} discoveries logged. Newest: {recent_thesis}. Convergence: {convergence}. {brain_line}{room_line}Worth Reflect.`
3. `Looking back: {n} discoveries. {recent_thesis} stands out. {brain_line}{room_line}Defer the rest? Synthesize?`

### fallback (3 templates)

1. `Searched 3 graphs and found {summary}. {brain_line}Relevant context: {room_context}. Next move: Run Methodology or Reformulate.`
2. `Triangulated across room.db + Aura + Brain. {summary}. {brain_line}{room_line}Worth reframing? /mos:beautiful-question.`
3. `{summary}. {brain_line}Given {room_context}, consider Run Methodology or Defer.`

## Canon Part 8 Audit Sites (2 seams)

| Seam | Location | Function | Defends Against |
|------|----------|----------|-----------------|
| SEAM A | explain() body, BEFORE rendering | auditQueryObject({query_results, opts_minus_test_override}, 'rs-query-to-text-input') | Forbidden bytes nested in row payload; opts.room_dir poisoning |
| SEAM B | explain() body, AFTER fillTemplate, BEFORE return | auditQueryString(rendered, 'rs-query-to-text-output') | Template-substitution leaks where assembled phrase trips a forbidden pattern even if individual row fields did not |

The 2-seam pattern mirrors 89.4-01 / 89.4-02 chokepoint anatomy. Module never imports brain-client.cjs (verified by source-level grep in T7). Module never calls fetch (verified by source-level grep + runtime fetch shim in T7).

## Verification

- `node lib/memory/test-rs-query-to-text.cjs` -> 12/12 pass; exit 0
- `grep -cP '[\x{2013}\x{2014}]' lib/core/rs-query-to-text.cjs lib/memory/test-rs-query-to-text.cjs` -> 0 0 (zero em-dashes)
- `grep -c "brain-client" lib/core/rs-query-to-text.cjs` -> 0 (Canon Part 8 invariant)
- `grep -c "fetch(" lib/core/rs-query-to-text.cjs` -> 0 (zero network surface)
- VOICE_TEMPLATES Object.isFrozen + 5 kinds + each kind length >= 3 (verified by T_extra)
- folder-memory.readQuadruple integration verified by T5 + T6
- Mode B fallback (BRAIN.md absent or stale) verified by T6
- No-LLM invariant verified by T7
- Canon Part 8 post-render audit verified by T8 + T9
- Voice pattern adherence (Evidence -> Insight -> Warning) verified by T10
- A1 sweep (zero FORBIDDEN_PATTERNS in positive outputs) verified by T11
- Zero new runtime deps in package.json (verified by `git diff package.json`)

## Self-Check: PASSED

- [x] lib/core/rs-query-to-text.cjs created (438 lines)
- [x] lib/memory/test-rs-query-to-text.cjs created (551 lines)
- [x] Commit 26b4997 (RED) found in git log
- [x] Commit ab17c31 (GREEN) found in git log
- [x] 12/12 tests pass; suite exit 0
- [x] VOICE_TEMPLATES frozen, 5 kinds, each kind >= 3 templates
- [x] 2 audit seams (auditQueryObject + auditQueryString)
- [x] Zero brain-client / fetch references
- [x] Zero em-dashes
- [x] Zero new runtime deps

## Deviations from Plan

None. Plan executed exactly as written. Minor template adjustment during GREEN: methodology_chain template index 1 originally lacked a `{n}` placeholder, which caused T3 to fail on the count assertion. Fix: added `({n} hops)` to the template. All other templates and tests passed first run.

The fix is consistent with the plan spec line 195 (`'Methodology graph: {source} -> {chain_list}. Synthesize next? Or Run Methodology on {recommended}.'`) being a starting suggestion -- the GREEN-time spec explicitly says "DO NOT add any new runtime dependencies. DO NOT use em-dashes." but does NOT freeze the exact template strings. Adding `({n} hops)` is a safe in-template enhancement that improves voice consistency (every methodology_chain template now references the count, mirroring the rs_discovery and discovery_history templates).
