---
phase: 89.5
slug: engine-and-nl-graph
plan: "05"
doc_type: phase-gate-transcript
status: CONDITIONAL PASS
executed_at: "2026-04-27T10:42:00Z"
milestone: v1.11.0
bundled_release: true
canon_parts: [part-3, part-7, part-8]
---

# Phase 89.5 Phase Gate Transcript

Commands run verbatim. Mirrors `89.4-PHASE-GATE.md` G1-G9 pattern, extended with 5 NEW 89.5-specific E asserts (E1 orchestrator chain composition + E2 NL-to-Query chokepoint exclusivity + E3 NL-to-Query defense-in-depth `>=6 adversarial` + E4 Query-to-Text voice adherence + E5 end-to-end CLI smoke).

Base commit for diff: `1248b56` (`docs(89.4-04): complete feynman-and-phase-gate plan`, immediately before Phase 89.5 first commit `59ff25f`). The 89.5 commit range starts at `59ff25f^` = `1248b56`.

HEAD at time of assert run: `b2ef4f3` (`test(89.5-05): register 5 new 89.5 fixture suites in Feynman runner; baseline 85 -> 90`). The Phase Gate + VERIFICATION.md commits land AFTER this transcript so the transcript captures the runtime state at the closure of the testing surface.

## Gate Summary

- Total asserts: 14 (9 G + 5 E)
- Passed: 13
- Conditional Pass: 1 (G8 -- 2 pre-existing failures predate Phase 89.5; not introduced by this phase; tracked separately. Same inheritance pattern as 89.2 + 89.3 + 89.4 PHASE-GATE G8. NET IMPROVEMENT: 4 -> 2 inherited failures; the debouncer-drain + vault-section-minto-generator timing flake tests self-resolved.)
- Failed: 0
- Status: CONDITIONAL PASS

## Assert Transcript

### G1: Zero new runtime deps in package.json (across 89.5 commit range)

- Command: `git diff 1248b56..HEAD -- package.json | wc -l`
- Expected: 0
- Observed: 0
- Status: PASS

`git diff 1248b56..HEAD -- package.json` returned zero lines. Phase 89.5 added zero direct dependencies. The orchestrator (89.5-04) and 4 user-facing CLI commands (89.5-05) consume Brain via the existing `brain-client.cjs` chokepoint (Phase 89.1a; Canon Part 7 reuse). Defense-in-depth flows through the existing `rs-egress-prompts.cjs` audit chokepoint (Phase 89.2-01 extraction). NL-to-Query chokepoint `buildBrainQueryFromNL` (89.5-02) is a deterministic intent classifier with NO runtime LLM. Query-to-Text voice templates (89.5-03) are frozen at design time. CLI scripts use process.argv parsing only; no npm CLI framework. Zero zod, zero cheerio, zero new packages.

### G2: BSL 1.1 headers on every new/modified .cjs

| File | Expected | Observed | Row Status |
|------|----------|----------|------------|
| lib/core/rs-commercial-assessor.cjs | >=1 | 1 | PASS |
| lib/core/rs-nl-to-query.cjs | >=1 | 1 | PASS |
| lib/core/rs-query-to-text.cjs | >=1 | 3 | PASS |
| scripts/rs-discovery-engine.cjs | >=1 | 2 | PASS |
| scripts/rs-fetch-command.cjs | >=1 | 1 | PASS |
| scripts/rs-thesis-command.cjs | >=1 | 1 | PASS |
| scripts/rs-experts-command.cjs | >=1 | 1 | PASS |
| scripts/rs-explain-command.cjs | >=1 | 1 | PASS |
| lib/memory/test-rs-commercial-assessor.cjs | >=1 | 1 | PASS |
| lib/memory/test-rs-nl-to-query.cjs | >=1 | 1 | PASS |
| lib/memory/test-rs-query-to-text.cjs | >=1 | 1 | PASS |
| lib/memory/test-rs-discovery-engine.cjs | >=1 | 1 | PASS |
| lib/memory/test-rs-explain-command.cjs | >=1 | 5 | PASS |

Command per file: `grep -cE 'BSL|BUSL' <file>`. All 13 new .cjs files in the Phase 89.5 commit range carry BSL 1.1 headers. The runner registration sweep at `lib/memory/run-feynman-tests.cjs` did not gain a new BSL header (file already carried one from prior phases) so it is not in this G2 table; G3 still verifies em-dashes on it.

- G2 Status: PASS (13/13 rows)

### G3: Zero em-dashes on every new/modified file

| File | Expected | Observed | Row Status |
|------|----------|----------|------------|
| lib/core/rs-commercial-assessor.cjs | 0 | 0 | PASS |
| lib/core/rs-nl-to-query.cjs | 0 | 0 | PASS |
| lib/core/rs-query-to-text.cjs | 0 | 0 | PASS |
| scripts/rs-discovery-engine.cjs | 0 | 0 | PASS |
| scripts/rs-fetch-command.cjs | 0 | 0 | PASS |
| scripts/rs-thesis-command.cjs | 0 | 0 | PASS |
| scripts/rs-experts-command.cjs | 0 | 0 | PASS |
| scripts/rs-explain-command.cjs | 0 | 0 | PASS |
| lib/memory/test-rs-commercial-assessor.cjs | 0 | 0 | PASS |
| lib/memory/test-rs-nl-to-query.cjs | 0 | 0 | PASS |
| lib/memory/test-rs-query-to-text.cjs | 0 | 0 | PASS |
| lib/memory/test-rs-discovery-engine.cjs | 0 | 0 | PASS |
| lib/memory/test-rs-explain-command.cjs | 0 | 0 | PASS |
| lib/memory/run-feynman-tests.cjs | 0 | 0 | PASS |
| commands/rs-fetch.md | 0 | 0 | PASS |
| commands/rs-thesis.md | 0 | 0 | PASS |
| commands/rs-experts.md | 0 | 0 | PASS |
| commands/rs-explain.md | 0 | 0 | PASS |

Command per file: `grep -cP '[\x{2013}\x{2014}]' <file>`. Project hard rule (`feedback_no_emdashes`) upheld across every file shipped + modified in Phase 89.5.

- G3 Status: PASS (18/18 rows)

### G4: Canon Part 8 forbidden-field audit on production .cjs

| File | Raw match count | Comment-stripped + LOCAL-context review | Row Status |
|------|----------------|------------------------------------------|------------|
| lib/core/rs-commercial-assessor.cjs | 0 | 0 | PASS |
| lib/core/rs-nl-to-query.cjs | 2 | 0 (both hits in block-comment header documenting Canon Part 8 boundary; explicit narrative, NOT field references) | PASS |
| lib/core/rs-query-to-text.cjs | 1 | 0 (the hit is `vc.room.identity_text` -- LOCAL read of the user's own Phase 88 ROOM.md frontmatter via folder-memory.readQuadruple; constitutional LOCAL data per Canon Part 8) | PASS |
| scripts/rs-discovery-engine.cjs | 1 | 0 (the hit is in the SEAM A doc-comment narrating `opts.meeting_transcript` adversarial scenario; explicit boundary documentation, NOT a field reference) | PASS |
| scripts/rs-fetch-command.cjs | 0 | 0 | PASS |
| scripts/rs-thesis-command.cjs | 0 | 0 | PASS |
| scripts/rs-experts-command.cjs | 0 | 0 | PASS |
| scripts/rs-explain-command.cjs | 0 | 0 | PASS |

Raw command per file: `grep -cE 'governing_thought[^_]|identity_text|decision_log|meeting_transcript|artifact_body' <file>`. Canon Part 8 forbidden-field boundary holds across all 8 production-surface files in Phase 89.5. The 4 raw hits (2 in rs-nl-to-query.cjs + 1 in rs-query-to-text.cjs + 1 in scripts/rs-discovery-engine.cjs) are documented case-by-case above and resolve to either documentation comments OR a legitimate LOCAL read of Phase 88 quadruple data. Test fixture suites are deliberately permitted to reference forbidden names in adversarial assertion context (fixtures verify the boundary holds under hostile input) and are excluded from G4 per the scope note inherited from Phase 89.1a + 89.1 + 89.2 + 89.3 + 89.4.

- G4 Status: PASS (8/8 rows; 4 raw hits all resolve to PASS via comment-stripped + LOCAL-context review)

### G5: Chokepoint exclusivity preserved (NL-to-Query buildBrainQueryFromNL only; orchestrator zero direct brain-client require)

- Per-file comment-stripped grep:
  - Command: per-file node script that strips block + line comments then greps `fetch\(` and `require\([^)]*brain-client`.

| File | fetch( (stripped) | brain-client require (stripped) | Row Status |
|------|-------------------|---------------------------------|------------|
| lib/core/rs-commercial-assessor.cjs | 0 | 0 | PASS |
| lib/core/rs-nl-to-query.cjs | 0 | 0 | PASS |
| lib/core/rs-query-to-text.cjs | 0 | 0 | PASS |
| scripts/rs-discovery-engine.cjs | 0 | 0 | PASS |
| scripts/rs-fetch-command.cjs | 0 | 0 | PASS |
| scripts/rs-thesis-command.cjs | 0 | 1 (CLI Aura session for Tier 1 thesis lookup; legitimate per command contract) | PASS |
| scripts/rs-experts-command.cjs | 0 | 1 (CLI Aura session for Tier 1 expert MATCH; legitimate per command contract) | PASS |
| scripts/rs-explain-command.cjs | 0 | 1 (CLI Aura session for bidirectional execute path; legitimate per command contract) | PASS |

- buildBrainQueryFromNL count in rs-nl-to-query.cjs:
  - Command: `grep -c "buildBrainQueryFromNL" lib/core/rs-nl-to-query.cjs`
  - Expected: `>= 1` (definition + at least one call site)
  - Observed: 8 (1 definition + 1 call site inside translate() + 6 supporting comment + reference lines)
  - Status: PASS
- Orchestrator chokepoint preservation:
  - `scripts/rs-discovery-engine.cjs` has 0 direct `brain-client` require (comment-stripped). The orchestrator's only Brain touch is `chain-feeder.lookupUpstream` (89.4-02) which already wraps `brainClient.query` with the Canon Part 8 audit + Mode A/B graceful path.
  - The 3 CLI command scripts (rs-thesis + rs-experts + rs-explain) DO require `brain-client.cjs` because their command contract explicitly is to issue parameterized Cypher MATCH against the user's local Aura instance. This is a different layer from the library chokepoint and is acknowledged as legitimate at the CLI dispatcher tier. The library layer (89.5-01 through 89.5-04) preserves the orchestrator chokepoint exclusivity invariant.
- Status: PASS (3 distinct invariants verified; library layer chokepoint preserved AT the orchestrator; NL-to-Query chokepoint preserved internally; CLI dispatcher Aura access acknowledged at the appropriate layer)

The Canon Part 7 chokepoint-exclusivity invariant from Phase 89.1a + 89.1 + 89.2 + 89.3 + 89.4 is preserved at the library layer. The new NL-to-Query chokepoint `buildBrainQueryFromNL` is the SINGLE chokepoint for any Brain query construction from NL input across the codebase per Plan 89.5-02 SUMMARY decisions[1]. The CLI command dispatchers wrap library output uniformly across surfaces; their Aura access is explicit and parameterized.

### G6: New Feynman suites registered (5 entries from 89.5)

- Command: `grep -cE "Phase 89.5-0[1-5]" lib/memory/run-feynman-tests.cjs`
- Expected: 5
- Observed: 5
- Cross-check (per-test entry verification):
  - `grep -cE "test-rs-(commercial-assessor|nl-to-query|query-to-text|discovery-engine|explain-command)\.cjs" lib/memory/run-feynman-tests.cjs`
  - Expected: 5
  - Observed: 5
- Status: PASS

Single-sweep registration per Plan 89.5-05 Task 4 design (mirrors Phase 89.4-04 Task 1 pattern; no parallel-plan contention on the TEST_FILES array; Plans 89.5-01 / 02 / 03 ran in parallel-friendly waves and never touched run-feynman-tests.cjs to avoid merge conflicts on the same array; Plan 89.5-04 also deferred registration to the closure plan). All 5 new suites land as the 86th through 90th entries, advancing Feynman baseline from 85 to 90 in one commit (`b2ef4f3`).

### G7: Each new fixture suite green (direct invocation)

- `node lib/memory/test-rs-commercial-assessor.cjs; echo "exit: $?"` -> exit 0; final line `=== 89.5-01 rs-commercial-assessor suite: 12/12 passed ===`
- `node lib/memory/test-rs-nl-to-query.cjs; echo "exit: $?"` -> exit 0; final line `=== 89.5-02 rs-nl-to-query suite: 16/16 passed ===`
- `node lib/memory/test-rs-query-to-text.cjs; echo "exit: $?"` -> exit 0; final line `=== 89.5-03 rs-query-to-text suite: 12/12 passed ===`
- `node lib/memory/test-rs-discovery-engine.cjs; echo "exit: $?"` -> exit 0; final line `=== 89.5-04 rs-discovery-engine suite: 9/9 passed ===`
- `node lib/memory/test-rs-explain-command.cjs; echo "exit: $?"` -> exit 0; final line `=== 89.5-05 rs-explain-command suite: 6/6 passed ===`
- 89.1a + 89.1 + 89.2 + 89.3 + 89.4 back-compat (sample): all inherited 89.x fixture suites remain green when invoked directly.
- Status: PASS

All 5 new 89.5 fixture suites exit 0 with their respective scenario counts intact. Total directly-owned scenarios across 89.5: 12 (89.5-01) + 16 (89.5-02) + 12 (89.5-03) + 9 (89.5-04) + 6 (89.5-05) = 55 PASS scenarios (including A1 sweeps in every suite).

### G8: Full Feynman runner green

- Command: `node lib/memory/run-feynman-tests.cjs > /tmp/89.5-05-feynman.log 2>&1; echo "exit=$?"`
- Expected: exit 0; baseline advanced to 90 (from post-89.4 baseline of 85)
- Observed: exit 1; final line `Feynman test runner: 88/90 passed, 0 skipped, 2 failed`; baseline advanced 85 -> 90 (registration target met) but 2 pre-existing failures persist (both inherited from 89.4 G8; net IMPROVEMENT from 4 -> 2 because the 2 timing-flake tests self-resolved).
- Status: CONDITIONAL PASS

The runner reported 88/90 PASS. The 5 new 89.5 fixture suites all PASS (verified via G7 direct invocation). The 2 failing test files are NOT in the Phase 89.5 commit range:

| Failing test | Last modified by | In 89.5 range? |
|--------------|------------------|----------------|
| `tests/test-self-update-platform.cjs` | `2c554de hotfix(v1.10.18)` + downstream v1.10.19 release | NO -- pre-89.2 hotfix |
| `test/84-smart-notebook-copilot.test.cjs` Test 15 (phase-83 regression guard) | cascades from `tests/test-self-update-platform.cjs` (runs feynman as a child) | NO -- transitive cascade |

Cross-check: `git log --oneline 1248b56..HEAD -- test/84-smart-notebook-copilot.test.cjs tests/test-self-update-platform.cjs` returned ZERO commits. Neither of the failing files was modified in the Phase 89.5 commit range.

Per Phase 89.4's PHASE-GATE.md G8 (which observed 4 failures from the same root causes plus 2 timing-flake tests) and Phase 89.3's PHASE-GATE.md G8 (which reported 79/82): "the failing test files are NOT in the Phase commit range; the cascade is from out-of-scope hotfixes". The same explanation applies here -- 89.5 inherits a SUBSET of 89.4's baseline drift; the 2 timing-flake tests (debouncer-drain + vault-section-minto-generator) self-resolved. Resolution path: the 2 remaining failures are tracked as a separate v1.10.19 / v1.10.18 hotfix follow-up; not introduced by Phase 89.5.

89.5's own contract (5 new fixture suites all green; baseline advanced 85 -> 90; total Feynman entries 90; ZERO new regressions introduced; NET IMPROVEMENT from 4 -> 2 inherited failures) is met. CONDITIONAL PASS reflects the inherited pre-existing failures only.

### G9: ExternalEgressViolation surfaces only via the SHARED class (NOT redefined in any 89.5 production module)

- Per-file grep for class redefinition:
  - `lib/core/rs-commercial-assessor.cjs`: `class ExternalEgressViolation` def=0
  - `lib/core/rs-nl-to-query.cjs`: `class ExternalEgressViolation` def=0
  - `lib/core/rs-query-to-text.cjs`: `class ExternalEgressViolation` def=0
  - `scripts/rs-discovery-engine.cjs`: `class ExternalEgressViolation` def=0
  - `scripts/rs-fetch-command.cjs`: `class ExternalEgressViolation` def=0
  - `scripts/rs-thesis-command.cjs`: `class ExternalEgressViolation` def=0
  - `scripts/rs-experts-command.cjs`: `class ExternalEgressViolation` def=0
  - `scripts/rs-explain-command.cjs`: `class ExternalEgressViolation` def=0
- Cross-check (89.1 + 89.2 + 89.3 + 89.4 invariant preservation):
  - `grep -c "class ExternalEgressViolation" lib/core/rs-egress-violations.cjs` returned 1 (the SHARED class).
  - 89.1 sibling subclasses in `rs-domain-analyzer.cjs` + `rs-query-matrix.cjs` PRESERVED unchanged.
  - 89.4 CanonVerbViolation in `rs-canon-violations.cjs` PRESERVED unchanged.
  - 89.5 modules require the shared class via the existing chain (rs-egress-prompts.cjs re-exports + rs-egress-violations.cjs definition).
- 89.1a substrate loader stays Brain-inbound-only:
  - `grep -c "ExternalEgressViolation" lib/core/rs-brain-substrate.cjs` returned 0 (89.1a chokepoint stays Brain-inbound-only, NEVER external-egress; required invariant preserved).
- Status: PASS (8/8 production .cjs files = 0 redefinitions)

The Canon Part 8 EXTRACTION pattern (Plan 89.2-01) consolidating `ExternalEgressViolation` at `lib/core/rs-egress-violations.cjs` continues. No new 89.5 production module redefines the class; all consume via the existing chain.

## New 89.5-Specific Asserts (E1-E5)

### E1: Orchestrator chain composition (happy path bundle shape)

- Command (inline node smoke):
  ```
  node -e "
    const m = require('./scripts/rs-discovery-engine.cjs');
    console.log('runDiscovery export:', typeof m.runDiscovery);
    console.log('AsyncFunction:', m.runDiscovery.constructor.name === 'AsyncFunction');
  "
  ```
- Observed:
  ```
  runDiscovery export: function
  AsyncFunction: true
  ```
- Cross-check: `node lib/memory/test-rs-discovery-engine.cjs` exits 0 with `=== 89.5-04 rs-discovery-engine suite: 9/9 passed ===`. The 9 tests verify the full bundle shape end-to-end with deterministic mocks: T1 happy path (12 fields populated: topic + domain_analysis + query_matrix + fetched_results + preprocessed + scored + classified + breakthroughs + theses + commercial + output + chain_metadata); T7 pause-state envelope bubble; T9 AsyncFunction surface.
- Status: PASS

The orchestrator chains 17 phase modules (89.1 + 89.1a + 89.2 + 89.3 + 89.4 + 89.5-01) end-to-end via require() composition. Per Plan 89.5-04 SUMMARY decisions[0] eager loading keeps the dependency graph mechanically grep-able for v1.11.0-beta.1 audit. Bundle shape is byte-stable across happy + Mode B + Tier 0 paths.

### E2: NL-to-Query chokepoint exclusivity (zero new fetch surface)

- Command 1: `grep -c "buildBrainQueryFromNL" lib/core/rs-nl-to-query.cjs`
- Expected: `>= 1` (definition + at least one call site)
- Observed: 8 (1 definition + 1 call site + 6 supporting comment + reference lines documenting the chokepoint reuse)
- Command 2: `grep -c "auditQueryObject(" lib/core/rs-nl-to-query.cjs`
- Expected: `>= 3` (per Plan 89.5-02 4-seam defense)
- Observed: 4 (3 explicit seams + 1 chokepoint internal seam)
- Command 3 (comment-stripped): `fetch\(` count in `lib/core/rs-nl-to-query.cjs` (block + line comments stripped, then grep)
- Expected: 0
- Observed: 0
- Status: PASS

Zero direct `fetch(` calls in `rs-nl-to-query.cjs`. The only outbound surface from the translator is the constructed Brain query payload (returned to the caller for execution by the orchestrator's chain-feeder which itself wraps `brainClient.query`). `buildBrainQueryFromNL` is THE single chokepoint for ANY Brain query construction from NL input across the codebase per Plan 89.5-02 SUMMARY decisions[1]. The 4 audit sites in `rs-nl-to-query.cjs` form a 4-seam defense (SEAM A on input + SEAM 1 on raw NL inside chokepoint + SEAM 2 on constructed payload inside chokepoint + SEAM C on returned bundle); plus 4 entity-extractor audits as a 5th tripwire layer.

### E3: NL-to-Query defense-in-depth (>=6 adversarial scenarios pass)

- Command: `node lib/memory/test-rs-nl-to-query.cjs 2>&1 | grep -E "^PASS T(8|9|10|11|12|13)" | wc -l`
- Expected: `>= 6`
- Observed: 6
- Status: PASS

The 6 adversarial scenarios (Plan 89.5-02 Test T8 through T13) each verify a different leak vector and each emits PASS. The full test suite (`=== 89.5-02 rs-nl-to-query suite: 16/16 passed ===`) includes these 6 plus T14 (SQL/Cypher injection prevention) + T15 (chokepoint exclusivity grep) + T16 (A1 sweep across 5+ positive outputs). Coverage breakdown per Plan 89.5-02 SUMMARY:

| Test | Leak-attempt vector | Outcome |
|------|---------------------|---------|
| T8 | NL prefix "What does my artifact body say about RS chains?" | brain_query=null OR throws |
| T9 | "Show me reverse salients for my Acme Quantum Brain Imaging venture" | venture name excluded from JSON.stringify of brain_query |
| T10 | opts.meeting_transcript = "private board minutes" | translate throws ExternalEgressViolation at SEAM A |
| T11 | "Find experts on $500M Series A failure modes" | throws at SEAM A (currency-magnitude pattern matched) |
| T12 | "Why did I reject opportunity X in my decision log?" | brain_query=null; intent classifier returns no match |
| T13 | "What's my governing thought on Mindrian moat?" | brain_query=null; intent classifier returns no match |

ZERO captured forbidden-pattern hits across all 6 adversarial scenarios.

### E4: Query-to-Text voice adherence (VOICE_TEMPLATES frozen + 5 kinds + >=3 each)

- Command (inline node smoke):
  ```
  node -e "
    const m = require('./lib/core/rs-query-to-text.cjs');
    console.log('VOICE_TEMPLATES:', typeof m.VOICE_TEMPLATES);
    console.log('isFrozen:', Object.isFrozen(m.VOICE_TEMPLATES));
    console.log('Kinds:', Object.keys(m.VOICE_TEMPLATES).length);
    for (const k of Object.keys(m.VOICE_TEMPLATES)) {
      console.log('  ', k, '->', m.VOICE_TEMPLATES[k].length, 'templates');
    }
  "
  ```
- Observed:
  ```
  VOICE_TEMPLATES: object
  isFrozen: true
  Kinds: 5
     rs_discovery -> 4 templates
     expert_network -> 3 templates
     methodology_chain -> 3 templates
     discovery_history -> 3 templates
     fallback -> 3 templates
  ```
- Status: PASS

VOICE_TEMPLATES is `Object.frozen` with 5 kinds (rs_discovery + expert_network + methodology_chain + discovery_history + fallback); each kind carries `>= 3` frozen voice templates (16 total). Per Plan 89.5-03 SUMMARY decisions[0] expanding the template count or adding a new kind requires a Plan-N revision (canon-amend discipline; new templates expand the surface area Canon Part 8 must defend).

Voice pattern adherence verified by Plan 89.5-03 Test T10 (Evidence -> Insight -> Warning structure markers): rendered outputs from the 4 result kinds each carry an explicit narrative arc (initial finding -> pattern interpretation -> optional warning marker). The 2-seam audit (SEAM A on input bundle + SEAM B on rendered output) catches both pre-render and post-render forbidden bytes.

### E5: End-to-end CLI smoke (rs-explain "Show me reverse salients...")

- Command: `node lib/memory/test-rs-explain-command.cjs 2>&1 | tail -3`
- Observed:
  ```
  PASS T6 A1 sweep on rendered NL outputs (zero FORBIDDEN_PATTERNS)

  === 89.5-05 rs-explain-command suite: 6/6 passed ===
  ```
- Status: PASS

The bidirectional NL-Graph loop verified end-to-end via 6 in-process e2e scenarios:

1. T1 happy path: NL "Show me reverse salients in my fintech room" -> nl-to-query.translate (SEAM A audit) -> 3-graph triangulation (SQLite mock returns 3 rows) -> query-to-text.explain (Larry-voiced template) -> rendered NL contains "reverse salients" + "Bank Opportunity" verb. Exit 0.
2. T2 Mode B Brain offline: nl-to-query returns brain_query non-null but brainClient.isAvailable returns false -> dispatcher OMITs Brain RPC -> sets `_brain_degraded: 'brain_unreachable'` marker -> explainer renders coherent fallback NL. NO throw. Exit 0.
3. T3 Tier 0 Aura offline: brainClient.isAvailable true but query throws AuraUnreachableError -> dispatcher catches -> sets `_cypher_degraded: 'aura_unreachable'` marker -> explainer renders Tier 0 graceful NL. NO throw. Exit 0.
4. T4 Canon Part 8 adversarial: nl-to-query.translate throws ExternalEgressViolation -> dispatcher catches -> emits "Query rejected by Canon Part 8 audit" to stderr -> exit 1 (the only e2e scenario that exits non-zero, and it does so by design).
5. T5 BSL 1.1 header check across all 4 command CJS scripts.
6. T6 A1 sweep: every captured NL output (T1 + T2 + T3) JSON.stringify scanned against FORBIDDEN_PATTERNS from rs-egress-prompts; zero matches.

The bidirectional surface is the v1.11.0 user-visible loop: user types question -> system queries graph -> raw results -> system explains in plain language. Raw data becomes insight per kickoff section 7 load-bearing description.

## Invariant Coverage

### 89.1a + 89.1 + 89.2 + 89.3 + 89.4 Invariants Preserved by 89.5

| Invariant | Origin | Preserved by 89.5 | Evidence |
|-----------|--------|-------------------|----------|
| Brain substrate chokepoint exclusivity (buildBrainSubstrateQuery) | 89.1a Plan 04 G5 | YES (89.5 doesn't touch substrate loader) | git diff 1248b56..HEAD -- lib/core/rs-brain-substrate.cjs returns 0 lines |
| Validator drop-in pattern | 89.1a Plan 02 | N/A (89.5 doesn't ship a new validator; reuses existing audit chokepoints) | n/a |
| FORBIDDEN_PATTERNS authoritative source = cross-room-aggregator | 89.1a Plan 01 | YES (rs-egress-prompts re-exports byte-for-byte; 89.5 modules consume via shared chokepoint) | E2 audit-site count = 4 in rs-nl-to-query; E5 A1 sweep PASS |
| ExternalEgressViolation extraction (89.2-01) | 89.2 Plan 01 | YES (all 8 production .cjs require the shared class; zero redefinitions) | G9 class-def counts = 0 in all 8 files |
| 89.1 sibling subclasses (domain-analyzer + query-matrix) | 89.1 Plan 02 + 03 | YES (siblings PRESERVED unchanged; 89.5 doesn't touch them) | git diff 1248b56..HEAD -- lib/core/rs-domain-analyzer.cjs lib/core/rs-query-matrix.cjs returns 0 lines |
| Single chokepoint URL builder per fetcher (buildXQuery) | 89.2 Plan 02/03/04 | YES (89.5 has no new fetchers; existing chokepoints untouched) | n/a |
| Pre-flight Canon Part 8 audit (Pattern 6) | 89.2 Plan 03 + 04 | YES (89.5 modules carry 2-seam OR 4-seam audit per module; rs-nl-to-query has 4 + 4 extractor seams; rs-query-to-text has 2 seams; orchestrator has SEAM A + per-module audit propagation) | E2 PASS + E4 PASS + Plan 89.5-04 T5 PASS |
| Telemetry sha256 hash | 89.2 Plan 01 | N/A (89.5 has no new outbound network surfaces) | n/a |
| Strict > comparisons in dual-floor + classifier | 89.2 Plan 06 + 07 | YES (89.5 doesn't touch differential-scorer or classifier) | n/a |
| 89.3 EDGE_TYPES extension | 89.3 Plan 01 | YES (89.5 doesn't touch EDGE_TYPES; chain composition is NODE-level) | git diff 1248b56..HEAD -- lib/core/lazygraph-ops.cjs returns 0 lines |
| 89.3 deterministic-id idempotency | 89.3 Plan 01 + 02 | YES (89.5 orchestrator consumes writer pair without modifying; idempotency preserved) | git diff 1248b56..HEAD -- lib/core/rs-neo4j-writer.cjs lib/core/rs-sqlite-mirror.cjs returns 0 lines |
| 89.3 tier-dispatch error class separation | 89.3 Plan 01 + 02 | YES (89.5 orchestrator catches AuraUnreachableError per Plan 89.5-04 SUMMARY; tier signal preserved as separate class from ExternalEgressViolation) | Plan 89.5-04 SUMMARY decisions[2]; Plan 89.5-05 rs-thesis-command + rs-experts-command + rs-explain-command catch AuraUnreachableError as Tier 0 fallback signal |
| 89.3 bridge-writer in-place enhancement | 89.3 Plan 05 | YES (bridge-writer untouched in 89.5; chain metadata integration is the orchestrator's concern via emitChainMetadata per discovery) | git diff 1248b56..HEAD -- lib/core/bridge-writer.cjs returns 0 lines |
| 89.3 5-branches frozen in BRANCHES export | 89.3 Plan 03 | YES (89.5 doesn't touch rs-mind-map.cjs) | git diff 1248b56..HEAD -- lib/core/rs-mind-map.cjs returns 0 lines |
| 89.3 2-seam defense-in-depth | 89.3 Plan 04 | YES (rs-query-to-text mirrors as 2-seam; rs-nl-to-query exceeds at 4-seam + 4 extractor seams) | E2 + E4 PASS |
| 89.4 CanonVerbViolation sentinel separate from ExternalEgressViolation | 89.4 Plan 01 | YES (rs-canon-violations.cjs untouched in 89.5; orchestrator reuses CanonVerbViolation transitively via chain-feeder) | git diff 1248b56..HEAD -- lib/core/rs-canon-violations.cjs returns 0 lines |
| 89.4 5-rule SKILL_SPAWN_RULES table | 89.4 Plan 03 | YES (rs-chain-feeder untouched in 89.5; orchestrator consumes via emitChainMetadata) | git diff 1248b56..HEAD -- lib/core/rs-chain-feeder.cjs returns 0 lines |
| 89.4 Brain query path uses existing brainClient.query chokepoint | 89.4 Plan 02 | YES (89.5 orchestrator routes Brain queries EXCLUSIVELY through chain-feeder.lookupUpstream; zero direct brain-client require in scripts/rs-discovery-engine.cjs) | G5 PASS (orchestrator brain-client require = 0; comment-stripped) |
| 89.4 auditQueryObject 3-seam in rs-chain-feeder | 89.4 Plan 02 + 03 | YES (chain-feeder seams preserved; 89.5 modules add their own seam layers on top) | E2 + E4 + 89.5-04 T5 PASS |

### New 89.5 Invariants

| Invariant | Origin | Verified by | Evidence |
|-----------|--------|-------------|----------|
| rs-commercial-assessor frozen 6-bucket MARKET_SIZE_BUCKETS table (1 unknown-bucket + 5 TAM/SAM proxies) | Plan 89.5-01 | G7 (12/12 PASS); Plan 89.5-01 Tests T6 + T8 | 89.5-01-SUMMARY.md key-decisions[0] |
| rs-commercial-assessor 4-entry frozen VALUE_PROP_TEMPLATES with 12-char stem distinctness | Plan 89.5-01 | G7 (T8 classification table coverage); Plan 89.5-01 SUMMARY § Sample Output | 89.5-01-SUMMARY.md tech-stack patterns[1] |
| rs-nl-to-query buildBrainQueryFromNL is THE single chokepoint for Brain query construction from NL | Plan 89.5-02 | E2 + Plan 89.5-02 Test T15 (chokepoint exclusivity grep) | E2 PASS (count = 8); Plan 89.5-02 SUMMARY decisions[1] |
| rs-nl-to-query 5-entry frozen ALLOW_LIST_INTENTS table | Plan 89.5-02 | G7 (T1 + T2 + T6); Plan 89.5-02 SUMMARY Sample Output | Plan 89.5-02 Tests T1-T7 |
| rs-nl-to-query 10-entry frozen FRAMEWORK_ALIASES table | Plan 89.5-02 | Plan 89.5-02 Tests T2 + T6 | 89.5-02-SUMMARY.md provides[2] |
| rs-nl-to-query SQL placeholder is ? (SQLite-style) NOT $1 (PostgreSQL) to sidestep currency-magnitude FORBIDDEN_PATTERN false-positive | Plan 89.5-02 | Plan 89.5-02 Test T2 + T14 + Plan 89.5-02 SUMMARY decisions[0] | E2 PASS (zero forbidden-pattern hits in positive bundles) |
| rs-nl-to-query Brain-query OMISSION (null) is the safe default for unrecognized intents | Plan 89.5-02 | Plan 89.5-02 Tests T8 + T12 + T13 | E3 PASS (3 of 6 adversarial scenarios verify OMISSION) |
| rs-query-to-text frozen VOICE_TEMPLATES (5 kinds; 16 templates total; each kind >=3) | Plan 89.5-03 | E4 + Plan 89.5-03 Test T_extra | E4 PASS |
| rs-query-to-text deterministic FNV-1a hash template selection (NO Math.random) | Plan 89.5-03 | Plan 89.5-03 SUMMARY tech-stack patterns[0] + Plan 89.5-03 Test T7 (no-LLM invariant) | T7 PASS in test-rs-query-to-text |
| rs-query-to-text Mode A/B/Tier-0 graceful via folder-memory.readQuadruple | Plan 89.5-03 | Plan 89.5-03 Tests T5 + T6 | 89.5-03-SUMMARY.md tech-stack patterns[1] |
| rs-discovery-engine eager require of 17 phase modules | Plan 89.5-04 | E1 + Plan 89.5-04 SUMMARY decisions[0] | E1 PASS (orchestrator import + AsyncFunction surface verified) |
| rs-discovery-engine SEAM A audit at the gate (auditQueryObject before any module invoked) | Plan 89.5-04 | Plan 89.5-04 Test T5 + Plan 89.5-04 SUMMARY decisions[3] | E5 T4 PASS (adversarial throws BEFORE library calls) |
| rs-discovery-engine pause-state envelope bubbled verbatim (NO further phases run) | Plan 89.5-04 | Plan 89.5-04 Test T7 + Plan 89.5-04 SUMMARY decisions[5] | T7 PASS in test-rs-discovery-engine |
| rs-discovery-engine Tier 0 fallback on AuraUnreachableError catch (NOT on detectTier alone) | Plan 89.5-04 | Plan 89.5-04 Test T2 + Plan 89.5-04 SUMMARY decisions[2] | T2 PASS in test-rs-discovery-engine |
| rs-discovery-engine zero direct fetch + zero direct brain-client require | Plan 89.5-04 | Plan 89.5-04 Test T6 + G5 (this transcript) | G5 PASS (orchestrator chokepoint exclusivity preserved) |
| rs-explain-command bidirectional loop (NL -> 3-graph -> NL) end-to-end | Plan 89.5-05 | E5 + Plan 89.5-05 Test T1 | E5 PASS (T1-T6 all green) |
| rs-explain-command Mode B graceful (Brain offline; NL output non-empty; NO throw) | Plan 89.5-05 | Plan 89.5-05 Test T2 | E5 T2 PASS |
| rs-explain-command Tier 0 graceful (Aura unreachable; NL output non-empty; NO throw) | Plan 89.5-05 | Plan 89.5-05 Test T3 | E5 T3 PASS |
| rs-explain-command Canon Part 8 adversarial caught at dispatcher (user-friendly error message; exit 1) | Plan 89.5-05 | Plan 89.5-05 Test T4 | E5 T4 PASS |

## Gate Signature

Executed by: Claude (autonomous plan execution under solo Wave-3 closure)
Executed at: 2026-04-27T10:42:00Z
Base commit: 1248b56 (`docs(89.4-04): complete feynman-and-phase-gate plan`; immediately before Phase 89.5 first commit `59ff25f`)
HEAD commit at assert run: b2ef4f3 (`test(89.5-05): register 5 new 89.5 fixture suites in Feynman runner; baseline 85 -> 90`)
Gate status: CONDITIONAL PASS

All 9 G asserts and all 5 E asserts observed the expected values, except G8 which reflects 2 pre-existing failures (both OUTSIDE the 89.5 commit range; documented separately per the same pattern Phase 89.1 + 89.2 + 89.3 + 89.4 used; NET IMPROVEMENT from 4 -> 2 inherited failures). All 89.1a + 89.1 + 89.2 + 89.3 + 89.4 inherited invariants plus 19 new 89.5 invariants have evidence. Zero new runtime deps. Zero em-dashes across 18 new + modified files. Zero forbidden-field references in 8 production .cjs files (4 raw hits all resolve to PASS via comment-stripped + LOCAL-context review). NL-to-Query chokepoint exclusivity preserved (`buildBrainQueryFromNL` is THE single chokepoint; `auditQueryObject` count = 4 sites). Orchestrator chokepoint exclusivity preserved (`scripts/rs-discovery-engine.cjs` zero direct brain-client require). 5 new Feynman suites registered (baseline 85 -> 90). Combined adversarial Canon Part 8 coverage = 6 (89.5-02 T8-T13) + 1 (89.5-04 T5) + 1 (89.5-05 T4) = 8 across 89.5 modules; ZERO captured forbidden-pattern hits across all 55 directly-owned PASS scenarios.

No release artifacts changed in the Phase 89.5 commit range (`bundled_release: true`; v1.11.0 ships at the v1.11.0-beta.1 release gate sub-phase ADDED AFTER 89.5 per kickoff section 7). CHANGELOG.md, .claude-plugin/plugin.json, and package.json are byte-identical at HEAD vs base across the entire Phase 89.5 commit range (1248b56 .. HEAD).
