---
phase: 167-harness-manifest-and-surface-generator
plan: 05
type: execute
wave: 5
depends_on: [167-01, 167-02, 167-03, 167-04]
files_modified:
  - tests/test-harness-167-verdict.cjs
  - tests/test-harness-167-part8-leak.cjs
  - tests/run-all-167.sh
autonomous: true
requirements: [HARN-01, HARN-02, HARN-03, D-167-01, D-167-02, D-167-03, D-167-04, D-167-05, D-167-06]
canon_parts: [3, 6, 7, 8, 9]

must_haves:
  truths:
    - "the adversarial verdict returns a structured {passed, findings[]} over HARN-01/02/03 + D-167-01..06 + the Part 8 boundary + the no-em-dash instrumentation (harness-as-code property 6), mirroring 166 W8 / 163 W6"
    - "the verdict proves by INSTRUMENTATION: the manifest names the three maps without merging (D-166-03), recipe-maps is NOT retired (D-167-02), fable-mode is posture-scoped with no convergence stop (D-167-04 / 166 B3), and /mos:new-surface emits + lands a well-formed entry without touching ignite (D-167-05)"
    - "a Part 8 leak scan over ALL Phase-167 surfaces (the manifest generator, recipe-maps, chain-executor, the new-surface generator + command, framework-runner) finds zero user-content-to-Brain / raw-fetch tokens, and re-runs the planted-secret manifest tripwire"
    - "the no-em-dash guard is by INSTRUMENTATION: a sweep over every Phase-167 file asserts zero U+2014 codepoints (the CLAUDE.md HARD RULE, proven not promised)"
    - "tests/run-all-167.sh is FINALIZED as the SINGLE PASS/FAIL phase gate: every suite + the manifest --check + the new-surface --check + the Part 8 leak scan + the verdict + the em-dash sweep, runs to completion"
  artifacts:
    - path: "tests/test-harness-167-verdict.cjs"
      provides: "the adversarial structured verdict over HARN-01/02/03 + D-167-01..06 + the canon guards (record(check, passed, detail) accumulator, exit non-zero on any finding)"
      min_lines: 110
    - path: "tests/test-harness-167-part8-leak.cjs"
      provides: "the Part 8 leak scan over every Phase-167 surface + the re-run planted-secret manifest tripwire"
      min_lines: 60
    - path: "tests/run-all-167.sh"
      provides: "the finalized single phase gate (all suites + both --checks + leak scan + verdict + em-dash sweep)"
      min_lines: 90
  key_links:
    - from: "tests/test-harness-167-verdict.cjs"
      to: "scripts/build-harness-manifest.cjs + lib/core/recipe-maps.cjs + lib/core/chain-executor.cjs + scripts/build-new-surface.cjs"
      via: "the verdict exercises each requirement surface and records a finding per check"
      pattern: "harness-manifest|recipe-maps|chain-executor|new-surface"
    - from: "tests/run-all-167.sh"
      to: "tests/test-harness-167-verdict.cjs"
      via: "CJS_SUITES registration as the final gate"
      pattern: "verdict"
    - from: "tests/test-harness-167-part8-leak.cjs"
      to: "data/harness-manifest.json"
      via: "the planted-secret tripwire re-runs over the manifest artifact"
      pattern: "harness-manifest"
---

<objective>
WAVE 5 VERIFY: the adversarial structured verdict (harness-as-code property 6 -- "adversarial verify returns
a structured verdict, not a vibe"), mirroring 166 W8 (tests/test-chain-executor-verdict.cjs) and 163 W6
(tests/test-trending-to-absurd-verdict.cjs). The verdict returns {passed, findings[]} over HARN-01/02/03 +
D-167-01..06 + the Part 8 boundary + the no-em-dash instrumentation, proving by INSTRUMENTATION (not by
promise) that the manifest (Wave 1), the enforcement wiring (Wave 2), fable-mode (Wave 3), and the
new-surface generator (Wave 4) honor the spec + the canon. Plus a Part 8 leak scan over every Phase-167
surface and the FINALIZED tests/run-all-167.sh that is the single PASS/FAIL phase gate.

Purpose: the harness-as-code 9-property mandate (and D-167-06) requires an adversarial verify wave that
proves the whole phase by instrumentation. This is the leak-net the 163 / 166 precedents proved catches real
findings (163 W6 caught FINDING-163-06-01).

The verdict checks (each pushes a {check, passed, detail} into findings[]; exit non-zero printing findings
if ANY fails):
1. HARN-01 / D-167-01: data/harness-manifest.json is generated + byte-stable + names the three maps by
   path+role+digest (NOT merged) + carries methodology_tier=mindrian-operation machinery metadata; the
   --check exits 0 clean and 1 on a forced STALE.
2. D-167-02: recipe-maps.loadManifest() returns the declared binding AND postureForCommand / wiringForReach /
   rankedNextReach are byte-unchanged (recipe-maps NOT retired, D-166-03 preserved).
3. D-167-03: the manifest --check is wired into BOTH the installable pre-commit template AND run-all-167.sh.
4. HARN-02 / D-167-04: fable-mode is posture-scoped (material steps critiqued, trivially-safe skipped),
   augments result.quality into the existing LOW_QUALITY halt, adds NO new loop / NO convergence stop (166
   B3 negative grep), and introduces NO fable model tier.
5. HARN-03 / D-167-05: /mos:new-surface emits an 11-key connector surface, refuses off-frozen reach/posture,
   lands the manifest entry, and the --check proves it well-formed; commands/ignite.md is untouched.
6. D-167-06 canon guards: the Part 8 boundary scan (planted-secret tripwire) is clean; Part 9 -- any
   truth-claim node along a fable-mode chain lands review_status proposed not confirmed; no em-dash across
   the phase.

Reuse before build (Canon Part 7): the verdict structure is the SHIPPED 163 / 166 pattern (the {passed,
findings[]} record(check, passed, detail) accumulator) and the aggregator is the SHIPPED run-all-156.sh
structure -- this wave MIRRORS both for Phase 167. A rewrite of the verify substrate is rejected; the proven
patterns are reused in shape.

Output: the verdict suite, the Part 8 leak suite, and the finalized run-all-167.sh. NO em-dashes (CLAUDE.md
HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-harness-manifest-and-surface-generator/167-SPEC.md
@.planning/phases/167-harness-manifest-and-surface-generator/167-CONTEXT.md
@tests/test-orchestration-projection-part8-boundary.cjs
@tests/run-all-156.sh
@lib/core/recipe-maps.cjs
@lib/core/chain-executor.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: the adversarial structured verdict + the Part 8 leak scan over every Phase-167 surface</name>
  <files>tests/test-harness-167-verdict.cjs, tests/test-harness-167-part8-leak.cjs</files>
  <action>Create tests/test-harness-167-verdict.cjs returning a structured {passed, findings[]} (mirror tests/test-orchestration-projection-part8-boundary.cjs / the 166 W8 verdict shape -- a record(check, passed, detail) accumulator that pushes {check, passed, detail} into findings[] and exits non-zero printing findings if ANY check failed) over the six requirement groups above: (1) HARN-01/D-167-01 -- require scripts/build-harness-manifest.cjs, assert buildManifest() is byte-stable across two calls, assert the manifest names the three maps by path+role+digest (NOT inlined contents) + carries methodology_tier === 'mindrian-operation', spawn node scripts/build-harness-manifest.cjs --check and assert exit 0 on the clean repo, then prove a forced in-memory STALE yields a finding (the validateManifest STALE branch). (2) D-167-02 -- require lib/core/recipe-maps.cjs, assert loadManifest() returns a binding with the three roles, assert postureForCommand / wiringForReach / rankedNextReach still return their shipped shapes (recipe-maps NOT retired), assert rankedNextReach still carries its CONTRACT-ONLY doc-comment (read the source). (3) D-167-03 -- read scripts/install-pre-commit.sh and assert the manifest guard is present AND read tests/run-all-167.sh and assert it runs build-harness-manifest.cjs --check (BOTH). (4) HARN-02/D-167-04 -- drive runChain with a material step + an injected low selfCritiqueFn and assert the chain halts via the existing LOW_QUALITY path; drive a trivially-safe push_forward step and assert selfCritiqueFn is NOT invoked; grep lib/core/chain-executor.cjs for NO convergence-stop branch (grep -v '^#' filtered) and NO 'fable' model alias. (5) HARN-03/D-167-05 -- require scripts/build-new-surface.cjs, emit a fixture surface to a tmp dir, assert all 11 connector keys present + reach_id in the frozen 6 + posture in the frozen 3, assert build-new-surface.cjs --check passes, assert an off-frozen reach is refused, assert commands/ignite.md is byte-unchanged by this phase (compare to git HEAD or assert no Phase-167 plan lists it). (6) D-167-06 -- spawn node tests/test-harness-manifest-part8-boundary.cjs and assert exit 0 (the planted-secret tripwire), assert a fable-mode chain writing a truth-claim node lands review_status 'proposed' (Part 9; assert via the navigation contract shape if exercised, else record a documented N/A-with-rationale finding rather than a false pass), and run the no-em-dash sweep as a verdict check. Then create tests/test-harness-167-part8-leak.cjs: a grep/scan over ALL Phase-167 surfaces (scripts/build-harness-manifest.cjs, lib/core/recipe-maps.cjs, lib/core/chain-executor.cjs, scripts/build-new-surface.cjs, commands/new-surface.md, agents/framework-runner.md) for forbidden tokens (no mcp__brain_(write|store|upsert|ingest), no writeBrain/sendToBrain/ingestToBrain, no raw fetch( egress in the lib/script code, no hardcoded external http(s) endpoint -- mirror the run-all-156.sh BRAIN_WRITE + RAW_FETCH regexes) with grep -v '^#' filtering so a comment cannot self-invalidate the count; never a bare unfiltered == 0 gate. Re-run the planted-secret manifest tripwire (seed a room/ path into an in-memory manifest copy, prove the sweep catches it, then prove the real artifact clean). NO fenced code in this action; mirror the cited verdict + boundary + run-all-156 logic. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-harness-167-verdict.cjs && node tests/test-harness-167-part8-leak.cjs && echo VERDICT_OK</automated>
  </verify>
  <done>the verdict returns {passed:true, findings:[...]} over HARN-01/02/03 + D-167-01..06 + the canon guards (manifest-not-merged, recipe-maps-not-retired, fable-mode posture-scoped + no convergence + no fable tier, new-surface emits + ignite untouched, Part 8 planted-secret, Part 9 proposed, no em-dash); the Part 8 leak scan finds zero forbidden tokens across every Phase-167 surface.</done>
</task>

<task type="auto">
  <name>Task 2: finalize tests/run-all-167.sh as the single phase gate</name>
  <files>tests/run-all-167.sh</files>
  <action>Finalize tests/run-all-167.sh (built up across Waves 1-4) into the complete phase aggregator, mirroring tests/run-all-156.sh structure end to end. The CJS_SUITES array MUST list every Phase-167 suite in dependency order: test-harness-manifest-check.cjs + test-recipe-maps-loadmanifest.cjs + test-harness-manifest-part8-boundary.cjs (Wave 1), test-harness-manifest-precommit-wiring.cjs (Wave 2), test-chain-executor-fable-mode.cjs (Wave 3), test-new-surface-generator.cjs (Wave 4), test-harness-167-verdict.cjs + test-harness-167-part8-leak.cjs (Wave 5). The per-suite loop runs to completion (a missing file gates to a FAIL line, never a crash -- mirror run-all-156.sh:54-60). Add the two GENERATOR --CHECK legs as gated steps: node scripts/build-harness-manifest.cjs --check (D-167-03 BOTH) and node scripts/build-new-surface.cjs --check on a known-good fixture (each non-zero records a FAIL line + flips the tally). Add the Part-8 grep sweep block over ALL Phase-167 lib + script + command + agent surfaces (scripts/build-harness-manifest.cjs, scripts/build-new-surface.cjs, lib/core/recipe-maps.cjs, lib/core/chain-executor.cjs, commands/new-surface.md, agents/framework-runner.md) using the run-all-156.sh BRAIN_WRITE + RAW_FETCH + external-http regexes with grep -v '^#' filtering. Add the em-dash sweep (U+2014 codepoint escape via printf or the codepoint, so the runner carries no literal em-dash) over EVERY Phase-167 file (the two generators, recipe-maps, chain-executor, the new-surface command, framework-runner, all test suites, and run-all-167.sh itself). Final tally + exit 1 on any failure. set -uo pipefail; bash only; no emoji. NO fenced code in this action; mirror run-all-156.sh. NO em-dashes.</action>
  <verify>
    <automated>bash tests/run-all-167.sh && echo PHASE_GATE_GREEN</automated>
  </verify>
  <done>run-all-167.sh runs every Phase-167 suite + the manifest --check + the new-surface --check + the Part 8 sweep + the verdict + the em-dash sweep to completion, prints a per-suite PASS/FAIL + a final tally, and exits 0 only when the entire phase is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict suite -> all Phase-167 surfaces | adversarial: assumes each surface is hostile until proven compliant by instrumentation |
| run-all-167 -> CI | the single phase gate; a RED here blocks the phase |
| manifest artifact -> Brain | the planted-secret tripwire re-asserts zero user-content egress by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-20 | Tampering | the manifest silently merging / retiring a map | mitigate | the verdict asserts the manifest names the three maps by path+role+digest (not inlined) and recipe-maps' three functions are byte-unchanged (D-166-03 / D-167-02) |
| T-167-21 | Elevation of privilege | a material low-quality step auto-running | mitigate | the verdict drives a material step + a low selfCritiqueFn and asserts the LOW_QUALITY halt fires (D-167-04) |
| T-167-22 | Tampering | a smuggled convergence stop or fable model tier | mitigate | the verdict grep-asserts no convergence-stop branch (166 B3) and no fable model alias in the executor / model-profiles |
| T-167-23 | Information disclosure | any Phase-167 egress path | mitigate | the Part 8 leak scan over every lib + script + command + agent surface finds zero Brain-write / raw-fetch / external-http tokens; the planted-secret manifest tripwire re-runs |
| T-167-24 | Spoofing | a hand-edited surface entry bypassing the generator | mitigate | the verdict runs build-new-surface.cjs --check and asserts MISSING_KEY / OFF_FROZEN / NOT_LANDED catch a malformed entry |
| T-167-25 | Repudiation | an em-dash slipping into the phase | mitigate | the no-em-dash sweep is a verdict check + a run-all-167.sh leg over every Phase-167 file (proven by instrumentation, not promised) |
| T-167-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages across the entire phase (bash + node built-ins); no install task |
</threat_model>

<verification>
- `node tests/test-harness-167-verdict.cjs` returns passed over HARN-01/02/03 + D-167-01..06 + the canon guards.
- `node tests/test-harness-167-part8-leak.cjs` finds zero forbidden tokens and re-runs the planted-secret manifest tripwire.
- `bash tests/run-all-167.sh` is green (every suite + the manifest --check + the new-surface --check + the Part 8 sweep + the verdict + the em-dash sweep).
- The manifest names the three maps without merging (D-166-03), recipe-maps is not retired (D-167-02), fable-mode is posture-scoped with no convergence stop (D-167-04 / 166 B3), and new-surface emits + lands without touching ignite (D-167-05).
- No em-dashes anywhere in the phase.
</verification>

<success_criteria>
The adversarial structured verdict proves by instrumentation that the harness-as-code completion honors the
spec + the canon end to end: the declared manifest names the three maps without merging (HARN-01 / D-167-01 /
D-166-03), recipe-maps wraps rather than becomes (D-167-02), the manifest --check is enforced in BOTH
pre-commit and CI (D-167-03), fable-mode is posture-scoped with no convergence stop and no fable model tier
(HARN-02 / D-167-04 / 166 B3), /mos:new-surface generates + lands well-formed wiring without touching ignite
(HARN-03 / D-167-05), and no Phase-167 surface egresses to the Brain or carries an em-dash (D-167-06).
tests/run-all-167.sh is the single green phase gate.
</success_criteria>

<output>
Create `.planning/phases/167-harness-manifest-and-surface-generator/167-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
