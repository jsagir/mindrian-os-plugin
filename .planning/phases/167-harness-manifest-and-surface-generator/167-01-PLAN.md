---
phase: 167-harness-manifest-and-surface-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-harness-manifest.cjs
  - data/harness-manifest.json
  - lib/core/recipe-maps.cjs
  - tests/test-harness-manifest-check.cjs
  - tests/test-harness-manifest-part8-boundary.cjs
  - tests/test-recipe-maps-loadmanifest.cjs
  - tests/run-all-167.sh
autonomous: true
requirements: [HARN-01, D-167-01, D-167-02, D-167-06]
canon_parts: [7, 8, 9]

must_haves:
  truths:
    - "data/harness-manifest.json is a GENERATED, versioned, byte-stable descriptor that NAMES the three existing maps as ONE entry: command-registry=posture, connector-registry=wiring, brain-orchestration-projection=ranked next-reach -- it does NOT merge or retire them (HARN-01, D-167-01, D-166-03 preserved)"
    - "scripts/build-harness-manifest.cjs mirrors the shipped generator idiom (deterministic sorted source walk -> build -> serialize JSON.stringify(...,2)+newline -> 3-branch main write/--check/refresh), adds NO new deps, emits NO YAML and NO TS"
    - "the manifest carries methodology_tier=mindrian-operation machinery metadata only and is a LOCAL artifact -- it is NOT folded into the Brain projection and opens NO Brain wire (Part 8 amendment entry 19)"
    - "the --check asserts byte-equality STALE + the three referenced maps RESOLVE + per-entry WELL-FORMEDNESS, exit-1 with a stderr recovery line (mirrors build-orchestration-projection.cjs runCheck)"
    - "recipe-maps.cjs gains a loadManifest() accessor returning the declared three-map binding; recipe-maps STAYS the live executable read-join (NOT retired, D-167-02); postureForCommand / wiringForReach / rankedNextReach are byte-unchanged"
    - "a build-time Part 8 boundary scan over the manifest + generator (planted-secret tripwire) proves zero user-content by construction, mirroring tests/test-orchestration-projection-part8-boundary.cjs"
  artifacts:
    - path: "scripts/build-harness-manifest.cjs"
      provides: "the manifest generator + the 3-branch main (write / --check / refresh) + the validateManifest STALE/UNRESOLVED/MALFORMED taxonomy + exported field allowlists"
      min_lines: 200
    - path: "data/harness-manifest.json"
      provides: "the generated declared descriptor naming the three maps as one entry, methodology_tier machinery metadata, byte-stable"
      contains: "methodology_tier"
    - path: "lib/core/recipe-maps.cjs"
      provides: "the additive loadManifest() / manifest() accessor; the three shipped read-join functions byte-unchanged"
      contains: "loadManifest"
    - path: "tests/test-harness-manifest-check.cjs"
      provides: "STALE byte-compare + three-maps-resolve + per-entry well-formed assertions over the generator"
      min_lines: 50
    - path: "tests/test-harness-manifest-part8-boundary.cjs"
      provides: "the planted-secret boundary scan over the manifest + generator (zero user-content by construction)"
      min_lines: 60
    - path: "tests/test-recipe-maps-loadmanifest.cjs"
      provides: "the loadManifest accessor + the three-functions-unchanged regression assertions"
      min_lines: 40
    - path: "tests/run-all-167.sh"
      provides: "phase aggregator scaffold (Wave 1 registers the three foundation suites + Part 8 sweep + em-dash sweep)"
      min_lines: 50
  key_links:
    - from: "scripts/build-harness-manifest.cjs"
      to: "data/command-registry.json + data/connector-registry.json + data/brain-orchestration-projection.json"
      via: "the manifest names each map by relative path + role; --check resolves each"
      pattern: "command-registry|connector-registry|brain-orchestration-projection"
    - from: "lib/core/recipe-maps.cjs"
      to: "data/harness-manifest.json"
      via: "loadManifest() reads the declared descriptor while the three read-join functions stay the executable join"
      pattern: "harness-manifest"
    - from: "tests/test-harness-manifest-part8-boundary.cjs"
      to: "scripts/build-harness-manifest.cjs"
      via: "field-allowlist + planted-secret + zero-live-Brain scan over the generator + artifact"
      pattern: "build-harness-manifest"
---

<objective>
WAVE 1 FOUNDATION (HARN-01, D-167-01/02/06): land the declared harness MANIFEST and its generator as a
unified READ LAYER OVER the three existing maps. Produce data/harness-manifest.json via a new
scripts/build-harness-manifest.cjs that MIRRORS the shipped generator idiom (build-connector-registry.cjs
/ build-orchestration-projection.cjs): a deterministic sorted source walk -> build -> serialize
(JSON.stringify(...,2)+newline, byte-stable) -> a 3-branch main (write / --check / refresh). The manifest
is the DECLARED, VERSIONED descriptor naming the three maps as ONE entry (command-registry=posture,
connector-registry=wiring, brain-orchestration-projection=ranked next-reach). It does NOT merge or retire
them (D-166-03 PRESERVED). recipe-maps.cjs gains a loadManifest() accessor returning the declared
three-map binding, while recipe-maps STAYS the live executable read-join (D-167-02). A --check (STALE byte
-compare + three-maps-resolve + per-entry well-formed) and a Part 8 boundary scan over the manifest +
generator complete the wave. Create tests/run-all-167.sh as the phase aggregator scaffold.

Purpose: Phase 166 shipped the EXECUTION runtime (runChain) and recipe-maps as the live three-map join.
This phase completes the CONTROL plane: a single declared descriptor the runtime reads as the entry point
to the harness wiring, generatable and drift-gated like every other shipped registry. Without it the
three maps have a live join (recipe-maps) but no declared, versioned, --check-gated descriptor.

D-167-01 (manifest is generated JSON, no new deps): no YAML (a parser dep or a second hand-rolled parser
is a drift surface), no TS (the build step breaks the edit-surface principle). Every shipped harness
registry is already generated JSON. Keep it LOCAL like the 157 projection; do NOT fold it into the Brain
projection.

D-167-02 (recipe-maps WRAPS, not becomes): recipe-maps.cjs (166 W1) stays the live read-join over the
three maps (must NOT be retired, D-166-03). The manifest is the declared descriptor; recipe-maps gains a
loadManifest() accessor returning the declared three-map binding. postureForCommand / wiringForReach /
rankedNextReach are byte-UNCHANGED; rankedNextReach stays CONTRACT-ONLY (live consumption deferred with
Phase 157/137).

Reuse before build (Canon Part 7): the generator + --check idiom is a VERBATIM mirror of the two shipped
generators (build-orchestration-projection.cjs runCheck:849-863, serializeProjection:693, validateProjection
:750; build-connector-registry.cjs main:594-662). The boundary scan is a verbatim mirror of
tests/test-orchestration-projection-part8-boundary.cjs (the 6-check planted-secret tripwire). A rewrite of
any of these is rejected: the idioms ship, are CI-tripwired, and are reused in shape. ~85 percent repoint;
the genuine net-new is the manifest schema (the three-map binding) + the loadManifest accessor.

Output: scripts/build-harness-manifest.cjs, data/harness-manifest.json, the recipe-maps loadManifest
accessor, three foundation test suites, and the run-all-167.sh aggregator scaffold. NO em-dashes anywhere
(CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-harness-manifest-and-surface-generator/167-SPEC.md
@.planning/phases/167-harness-manifest-and-surface-generator/167-CONTEXT.md
@.planning/phases/167-harness-manifest-and-surface-generator/167-RESEARCH.md
@scripts/build-orchestration-projection.cjs
@scripts/build-connector-registry.cjs
@lib/core/recipe-maps.cjs
@tests/test-orchestration-projection-part8-boundary.cjs
@tests/run-all-156.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build-harness-manifest.cjs + data/harness-manifest.json -- the declared three-map descriptor (HARN-01, D-167-01)</name>
  <files>scripts/build-harness-manifest.cjs, data/harness-manifest.json, tests/test-harness-manifest-check.cjs</files>
  <behavior>
    - Test 1 (well-formed): the generated manifest carries a top-level methodology_tier field equal to 'mindrian-operation' (D-167-01 machinery metadata) and a maps[] (or maps{}) section naming exactly three entries -- one keyed posture -> data/command-registry.json, one keyed wiring -> data/connector-registry.json, one keyed ranked_next_reach -> data/brain-orchestration-projection.json. None of the three maps is inlined / merged (the manifest carries each map's relative path + role + a generated digest, NOT the maps' contents) -- proving D-166-03 layering.
    - Test 2 (STALE): validateManifest over a regenerated-in-memory manifest byte-compares to the committed data/harness-manifest.json; an edited committed file yields a STALE finding with the recovery line; a clean repo yields zero STALE findings.
    - Test 3 (three maps resolve): validateManifest pushes an UNRESOLVED finding for any of the three named map paths that does not exist on disk; with all three present, zero UNRESOLVED findings.
    - Test 4 (per-entry well-formed): validateManifest pushes a MALFORMED finding for a manifest entry missing its required keys (path / role / digest); a complete manifest yields zero MALFORMED findings.
    - Test 5 (byte-stable + tier): two successive buildManifest() calls serialize byte-identically; the serialized form ends with a single trailing newline; every manifest entry carries methodology_tier machinery metadata, never user content.
  </behavior>
  <action>Create scripts/build-harness-manifest.cjs mirroring scripts/build-orchestration-projection.cjs end to end (the buildProjection:550 / serializeProjection:693 / validateProjection:750 / runCheck:849 / main:869 skeleton). buildManifest(): read the three committed maps (data/command-registry.json, data/connector-registry.json, data/brain-orchestration-projection.json) via a readJson degrade helper; emit a descriptor object { ontology_ref, generated_note, methodology_tier: 'mindrian-operation', version, maps } where maps NAMES the three sources as ONE binding -- each entry carries { role, path (the relative path), digest (a sha256 of the on-disk map bytes, machinery metadata not contents), source_count (a scalar count of the map's primary array length) } for roles posture / wiring / ranked_next_reach. CRITICAL: the manifest carries each map's PATH + ROLE + DIGEST + a scalar count, NEVER the maps' contents inlined -- it is a READ LAYER OVER the three, not a merge (D-166-03 / HARN-01). Export NODE_FIELD_ALLOWLIST + ENTRY_FIELD_ALLOWLIST frozen arrays (the manifest's generic-machinery key set: ontology_ref, generated_note, methodology_tier, version, maps, role, path, digest, source_count) for the Wave-1 boundary scan to assert against. serializeManifest(): JSON.stringify(clean, null, 2) + a single trailing newline, stable key order. validateManifest(manifest): return { stale, unresolved, malformed } categorized arrays -- STALE (serialized regeneration diverges byte-for-byte from the committed data/harness-manifest.json), UNRESOLVED (a named map path does not exist on disk), MALFORMED (a maps entry missing role / path / digest), each with a 'Run: node scripts/build-harness-manifest.cjs' recovery line (mirror validateProjection's RECOVERY idiom). runCheck(): regenerate in memory, run validateManifest, print each finding + recovery to stderr, exit non-zero on ANY finding, else 'harness-manifest: OK' (mirror runCheck:849-863). main(): 3-branch -- default writes data/harness-manifest.json (mkdirSync + writeFileSync(serializeManifest)), --check runs runCheck, --refresh re-derives the digests (the manifest's analogue of --refresh-names; idempotent re-write). require.main guard exports buildManifest / serializeManifest / validateManifest / NODE_FIELD_ALLOWLIST / ENTRY_FIELD_ALLOWLIST. Make ZERO Brain/network calls; require NO brain-client; emit NO YAML; add NO new deps (node built-ins only -- node:fs, node:path, node:crypto for sha256). Then run the generator once to produce data/harness-manifest.json (the committed artifact). Then create tests/test-harness-manifest-check.cjs (plain node assert harness; mirror tests/test-orchestration-projection-part8-boundary.cjs ok/fail accumulator style or the node:assert/strict style of the cited check test) covering the five behaviors. NO fenced code in this action; the change lives in the cited files following the cited line ranges. NO em-dashes.</action>
  <verify>
    <automated>node scripts/build-harness-manifest.cjs --check && node tests/test-harness-manifest-check.cjs && echo MANIFEST_CHECK_OK</automated>
  </verify>
  <done>data/harness-manifest.json is generated + byte-stable, names the three maps as one binding by path+role+digest (never merged), carries methodology_tier machinery metadata; --check asserts STALE + three-maps-resolve + per-entry well-formed with a recovery line; the test proves all five behaviors and zero Brain egress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recipe-maps loadManifest() accessor -- wrap, do not become (D-167-02)</name>
  <files>lib/core/recipe-maps.cjs, tests/test-recipe-maps-loadmanifest.cjs</files>
  <behavior>
    - Test 1: recipe-maps.loadManifest() returns the declared three-map binding read from data/harness-manifest.json -- an object exposing the posture / wiring / ranked_next_reach map paths + roles. A missing / malformed manifest degrades to a documented empty binding (never throws), mirroring the existing _loadProjection degrade idiom.
    - Test 2: the three shipped read-join functions are BYTE-UNCHANGED in behavior -- postureForCommand('/mos:think-hats') still returns the command-registry posture authority answer; wiringForReach(reachId) still returns the connector-registry surfaces; rankedNextReach() still returns the projection candidates and still carries its CONTRACT-ONLY doc-comment. recipe-maps is NOT retired (D-166-03).
    - Test 3: loadManifest is the DECLARED descriptor reader; the three read-join functions remain the EXECUTABLE join -- a doc-comment on loadManifest states the manifest is the declared/versioned descriptor while recipe-maps is the live join, so a future reader does not collapse one into the other.
    - Test 4: a forbidden-token scan over recipe-maps.cjs confirms the loadManifest addition introduces ZERO Brain calls (no mcp__brain_, no brain-client require, no raw fetch) -- the manifest is a LOCAL committed artifact (Part 8).
  </behavior>
  <action>Extend lib/core/recipe-maps.cjs ADDITIVELY (reuse, do not rewrite -- the three read-join functions ship at lines 144-225 and stay byte-unchanged). Add: (a) a DEFAULT_MANIFEST_PATH (data/harness-manifest.json) + a MINDRIAN_HARNESS_MANIFEST env override mirroring the existing _connectorPath / _projectionPath helpers at lines 94-102; (b) a _loadManifest per-process cache + degrade helper mirroring _loadConnectors / _loadProjection at lines 104-129 (degrade to a documented empty binding { maps: {} } on missing / malformed JSON); (c) a public loadManifest() (alias manifest()) returning the declared three-map binding from the cached parse, with a doc-comment stating per D-167-02 the manifest is the DECLARED / VERSIONED descriptor and recipe-maps stays the live EXECUTABLE read-join (D-166-03: recipe-maps is NOT retired); (d) wire loadManifest + manifest into module.exports and into __reset (clear the new cache). Do NOT touch postureForCommand / wiringForReach / rankedNextReach signatures or bodies. Make ZERO Brain calls. Then create tests/test-recipe-maps-loadmanifest.cjs covering the four behaviors plus a forbidden-token scan over recipe-maps.cjs for mcp__brain_ / brain-client / raw fetch (use grep -v '^#' style filtering so a comment cannot self-invalidate the count). NO fenced code in this action. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-recipe-maps-loadmanifest.cjs && echo LOADMANIFEST_OK</automated>
  </verify>
  <done>recipe-maps.loadManifest() returns the declared three-map binding and degrades cleanly; the three read-join functions are byte-unchanged (recipe-maps NOT retired, D-166-03 / D-167-02); the accessor doc-comment names the declared-vs-executable split; the test proves the Part 8 clean surface.</done>
</task>

<task type="auto">
  <name>Task 3: the Part 8 boundary scan + scaffold tests/run-all-167.sh</name>
  <files>tests/test-harness-manifest-part8-boundary.cjs, tests/run-all-167.sh</files>
  <action>Create tests/test-harness-manifest-part8-boundary.cjs as a VERBATIM-IN-SHAPE mirror of tests/test-orchestration-projection-part8-boundary.cjs (the 6-check planted-secret tripwire), scoped to the manifest surfaces: data/harness-manifest.json (the artifact) + scripts/build-harness-manifest.cjs (the generator). Reuse the cited test's forbiddenValueReason heuristic engine (ROOM_PATH_RX / ROOMS_STORE_RX / EMAIL_RX / FREE_TEXT_CAP), walkStringValues, and the ok/fail accumulator. The checks: CHECK 1 FIELD ALLOWLIST -- every manifest key (descending into maps entries) is in the generator's exported NODE_FIELD_ALLOWLIST / ENTRY_FIELD_ALLOWLIST. CHECK 2 TIER -- the manifest carries methodology_tier === 'mindrian-operation' (the boundary-keeper). CHECK 3 FORBIDDEN-VALUE + PLANTED-SECRET -- seed a room/ path + an email + an over-cap body into an in-memory manifest copy and prove the sweep catches each (RED), then prove the REAL artifact is clean (GREEN); the digests are sha256 hex (machinery metadata) so they never trip the heuristic. CHECK 4 ZERO LIVE BRAIN -- the generator source (comment lines filtered) carries NO brain-client require and NO fetch/http/curl/brain.query/brain.write call syntax. CHECK 5 GENERIC-MACHINERY -- every maps entry carries only { role, path, digest, source_count } generic machinery values (no room/ path, no email; role is an enum, path is a data/ relative path, digest is sha256 hex, source_count is an int). CHECK 6 NO EM-DASH -- neither the generator nor this scan contains the U+2014 codepoint (referenced by String.fromCharCode(0x2014), never as a literal). Then create tests/run-all-167.sh mirroring tests/run-all-156.sh end to end: a CJS_SUITES array registering test-harness-manifest-check.cjs + test-recipe-maps-loadmanifest.cjs + test-harness-manifest-part8-boundary.cjs (later waves append); the per-suite loop runs to completion (a missing file gates to a FAIL line, never a crash, mirror run-all-156.sh:54-60); a Part-8 grep sweep block over the Phase-167 lib + script surfaces present so far (scripts/build-harness-manifest.cjs, lib/core/recipe-maps.cjs) using the run-all-156.sh BRAIN_WRITE + RAW_FETCH regexes with grep -v '^#' filtering; the em-dash sweep written via the U+2014 codepoint escape over the Phase-167 files + the test suites + run-all-167.sh itself; final tally + exit 1 on any failure. set -uo pipefail; bash only; no emoji. NO fenced code in this action; mirror the cited boundary scan + run-all-156 logic. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-harness-manifest-part8-boundary.cjs && bash tests/run-all-167.sh && echo PHASE_GATE_SCAFFOLD_GREEN</automated>
  </verify>
  <done>the boundary scan proves zero user-content in the manifest + generator by construction (planted-secret RED then real-artifact GREEN), zero live Brain in the generator, and machinery-only maps entries; run-all-167.sh runs the three Wave-1 suites + the Part 8 sweep + the em-dash sweep to completion, prints a per-suite PASS/FAIL + final tally, and exits 0 when green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest generator -> three maps | the manifest names + digests the three maps; it must never inline their contents (a merge would reverse D-166-03) |
| manifest artifact -> Brain | the manifest is a LOCAL machinery-metadata cache; emitting it opens no Brain wire (Part 8 amendment entry 19) |
| manifest read -> recipe-maps | loadManifest reads the declared descriptor; the three read-join functions stay the executable join (no collapse) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-01 | Tampering | the manifest merging / retiring the three maps | mitigate | the manifest carries each map by path+role+digest+count, NEVER inlined; the boundary-scan field allowlist + the manifest-check well-formed assertion prove the three stay layered (D-166-03) |
| T-167-02 | Information disclosure | a user-content value smuggled into the manifest | mitigate | the planted-secret boundary scan (CHECK 3) seeds a room/ path + email + over-cap body and proves the sweep catches each, then proves the real artifact clean; digests are sha256 hex machinery metadata |
| T-167-03 | Spoofing | a fabricated map binding when a source map is missing | mitigate | validateManifest pushes an UNRESOLVED finding for any named map path absent on disk; --check exits 1 with a recovery line |
| T-167-04 | Tampering | a stale committed manifest | mitigate | the --check STALE byte-compare regenerates in memory and rejects a divergent committed file (Wave 2 wires this into pre-commit) |
| T-167-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (node:fs / node:path / node:crypto built-ins + existing JSON maps); no install task |
</threat_model>

<verification>
- `node scripts/build-harness-manifest.cjs --check` exits 0 on a clean repo and 1 with a recovery line on STALE / UNRESOLVED / MALFORMED.
- `node tests/test-harness-manifest-check.cjs` passes (well-formed + STALE + three-maps-resolve + per-entry well-formed + byte-stable + tier).
- `node tests/test-recipe-maps-loadmanifest.cjs` passes (loadManifest binding + three functions byte-unchanged + Part 8 clean).
- `node tests/test-harness-manifest-part8-boundary.cjs` passes (planted-secret RED then real-artifact GREEN + zero live Brain + machinery-only entries).
- `bash tests/run-all-167.sh` is green (three Wave-1 suites + Part 8 sweep + em-dash sweep).
- data/harness-manifest.json carries methodology_tier=mindrian-operation and names the three maps by path+role+digest, never inlined.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The declared harness manifest exists as a generated, byte-stable, versioned descriptor that names the
three existing maps as one binding without merging or retiring them (HARN-01, D-167-01, D-166-03
preserved); recipe-maps gains a loadManifest() accessor while staying the live executable read-join
(D-167-02); a --check (STALE + three-maps-resolve + per-entry well-formed) and a planted-secret Part 8
boundary scan gate the manifest by construction (D-167-06). All foundation suites and the phase
aggregator scaffold are green.
</success_criteria>

<output>
Create `.planning/phases/167-harness-manifest-and-surface-generator/167-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
