---
phase: 167-harness-manifest-and-surface-generator
plan: 03
type: execute
wave: 3
depends_on: [167-01]
files_modified:
  - lib/core/chain-executor.cjs
  - agents/framework-runner.md
  - tests/test-chain-executor-fable-mode.cjs
  - tests/run-all-167.sh
autonomous: true
requirements: [HARN-02, D-167-04, D-167-06]
canon_parts: [3, 7, 8, 9]

must_haves:
  truths:
    - "fable-mode is a POSTURE-SCOPED selfCritiqueFn(step, result) seam in lib/core/chain-executor.cjs, hooked between the onStep return (:359-361) and the previousOutput assignment (:389): its verdict AUGMENTS result.quality, feeding the EXISTING LOW_QUALITY halt path in makeGateFn (:195) so a failed self-critique maps autonomous_safe -> halt next hop (HARN-02, D-167-04)"
    - "self-critique fires only on MATERIAL / uncertain steps, NOT on every autonomous_safe step (respects the 166 token analysis: do not re-verify trivially-safe steps)"
    - "there is NO new loop and NO auto-retry-to-convergence -- the chain still HALTS on posture (the first material step, Part 3); 166 B3 is untouched (the self-critique is a gate INPUT, never a convergence stop condition)"
    - "fable-mode is net-new NAMING over the shipped quality machinery (the framework-runner Step-3 quality gate + the FRAMEWORK_RUNNER_RESULT quality enum) -- it invents NO fable model tier; model-profiles stays opus/sonnet/haiku"
    - "framework-runner.md NAMES the self-critique contract: every material chain step must VERIFY + SELF-CRITIQUE before its chain_output becomes the next step's previous_output (HARN-02)"
    - "the seam makes ZERO Brain calls and B2 is preserved: decide()'s return shape is unchanged by the self-critique hook (Part 8)"
  artifacts:
    - path: "lib/core/chain-executor.cjs"
      provides: "the posture-scoped selfCritiqueFn seam (opt; default a no-op) augmenting result.quality between onStep and the previousOutput fold; runChain stays the single door"
      contains: "selfCritique"
    - path: "agents/framework-runner.md"
      provides: "the fable-mode self-critique contract naming verify+self-critique on every material chain step, mapped onto the existing Step-3 quality gate + FRAMEWORK_RUNNER_RESULT quality enum"
      contains: "fable-mode"
    - path: "tests/test-chain-executor-fable-mode.cjs"
      provides: "asserts posture-scoped firing + quality augmentation -> existing LOW_QUALITY halt + no-new-loop + no-fable-model-tier + B2 unchanged"
      min_lines: 70
    - path: "tests/run-all-167.sh"
      provides: "the fable-mode suite registered in the aggregator"
      contains: "fable-mode"
  key_links:
    - from: "lib/core/chain-executor.cjs"
      to: "lib/core/chain-executor.cjs makeGateFn (:195)"
      via: "the selfCritique verdict augments result.quality which feeds the existing priorOutput.quality === LOW_QUALITY halt"
      pattern: "LOW_QUALITY"
    - from: "lib/core/chain-executor.cjs"
      to: "agents/framework-runner.md"
      via: "the self-critique contract is the per-step brick contract framework-runner declares (Step-3 quality gate)"
      pattern: "self-critique|quality"
    - from: "tests/test-chain-executor-fable-mode.cjs"
      to: "lib/core/chain-executor.cjs"
      via: "drives runChain with a material step + a self-critique verdict and asserts the halt"
      pattern: "runChain"
---

<objective>
WAVE 3 FABLE-MODE (HARN-02, D-167-04): make fable-mode an explicit, posture-scoped step discipline in the
runtime. Insert a selfCritiqueFn(step, result) seam in lib/core/chain-executor.cjs BETWEEN the onStep return
(:359-361, where result.chain_output + result.quality are captured) and the previousOutput assignment
(:389, where chain_output folds into the next step). The self-critique verdict AUGMENTS result.quality,
feeding the EXISTING LOW_QUALITY halt path in makeGateFn (:195, priorOutput.quality === LOW_QUALITY -> halt)
so a step that fails self-critique escalates from autonomous_safe to a halt at the next hop. Name the
self-critique contract on agents/framework-runner.md (mapping it onto the shipped Step-3 quality gate +
the FRAMEWORK_RUNNER_RESULT quality enum, framework-runner.md:69-77,121-136).

D-167-04 (POSTURE-SCOPED): the verify + self-critique fires only on MATERIAL / uncertain steps, NOT on
every autonomous_safe step. This respects the 166 token analysis: do not burn tokens re-verifying
trivially-safe steps. A step is material when its posture is not push_forward, or it is irreversible, or
the caller flags it material; trivially-safe push_forward reversible steps skip the critique.

HARD CONSTRAINTS (D-167-04 + 166 B3 preserved): NO new loop; NO auto-retry-to-convergence (the SEED-032
"loop until tests pass" convergence is REJECTED for the executor by 166 B3). The self-critique is a gate
INPUT (it augments result.quality, which the existing LOW_QUALITY path halts on), NEVER a convergence stop
condition. The chain still HALTS on posture at the first material step (Canon Part 3). fable-mode is net-new
NAMING over the shipped quality machinery -- there is NO fable-mode skill and NO fable model alias today
(167-RESEARCH C4); do NOT invent a fable model tier (model-profiles.cjs stays opus/sonnet/haiku).

Reuse before build (Canon Part 7): the gate machinery already exists -- makeGateFn (chain-executor.cjs
:185-207) ALREADY halts on priorOutput.quality === LOW_QUALITY (:195), and the framework-runner ALREADY
self-checks against three criteria and sets confidence/quality low on failure (framework-runner.md:69-77).
fable-mode WIRES these: the seam routes the per-step quality through a named self-critique verdict that
feeds the existing halt. A rewrite of the gate or a new quality enum is rejected; the LOW_QUALITY halt and
the FRAMEWORK_RUNNER_RESULT enum ship and are reused. ~90 percent repoint; the genuine net-new is the
posture-scoped seam + the named contract.

Output: the selfCritiqueFn seam in chain-executor.cjs, the fable-mode contract in framework-runner.md, the
fable-mode test suite, and its registration in run-all-167.sh. NO em-dashes anywhere (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-harness-manifest-and-surface-generator/167-SPEC.md
@.planning/phases/167-harness-manifest-and-surface-generator/167-CONTEXT.md
@lib/core/chain-executor.cjs
@agents/framework-runner.md
@tests/run-all-156.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the posture-scoped selfCritiqueFn seam in chain-executor.cjs (HARN-02, D-167-04)</name>
  <files>lib/core/chain-executor.cjs, tests/test-chain-executor-fable-mode.cjs</files>
  <behavior>
    - Test 1 (material step critiqued -> halt next hop): a chain of two steps where step 1 is MATERIAL (posture not push_forward, or flagged material) and the injected selfCritiqueFn returns a low verdict; runChain augments step 1's result.quality to LOW_QUALITY, the trace records the augmented quality, and the chain halts before / at step 2 via the EXISTING LOW_QUALITY gate path (no new halt reason invented -- it reuses quality_early_stop / the makeGateFn LOW_QUALITY branch).
    - Test 2 (trivially-safe step NOT critiqued): a push_forward + reversible step does NOT invoke selfCritiqueFn (the seam is posture-scoped); its quality is carried through unchanged and the chain proceeds (proves D-167-04 token-cost scoping -- the critique is skipped on trivially-safe steps).
    - Test 3 (default no-op): with NO selfCritiqueFn opt supplied, runChain behaves BYTE-IDENTICALLY to the Wave-166 contract -- the seam defaults to a no-op pass-through, so every shipped runChain caller is unaffected (regression guard).
    - Test 4 (no new loop / no convergence): the source of lib/core/chain-executor.cjs is grep-asserted to contain NO autonomous-convergence termination branch introduced by this seam (no "loop until", "until.*converge", "all.?passing" added) -- the self-critique is a gate INPUT, not a stop condition; 166 B3 holds (mirror the 166-08 B3 negative grep, grep -v '^#' filtered).
    - Test 5 (no fable model tier): a grep-assert that this change introduces NO 'fable' model alias / tier into model-profiles or the executor (fable-mode is naming over the quality machinery, not a model); model-profiles stays opus/sonnet/haiku.
    - Test 6 (B2 unchanged): decide()'s recorded decision_trace handle in the trace is byte-unchanged by the self-critique hook (the seam touches result.quality only, never the decision object).
  </behavior>
  <action>Extend lib/core/chain-executor.cjs ADDITIVELY (reuse, do not rewrite -- the loop ships at :237-393 and makeGateFn at :185-207; both stay structurally intact). Add: (a) a selfCritiqueFn opt to the runChain contract (and the _runChainResilient path) -- typeof o.selfCritiqueFn === 'function' ? o.selfCritiqueFn : a default NO-OP that returns null (so absent = byte-identical to the Wave-166 behavior). Document it in the runChain JSDoc callback list (:218-235) alongside the existing six callbacks. (b) A posture-scoped helper _isMaterialStep(step, posture) -- true when posture is not push_forward (the verdict.posture / verb is not 'run'), OR isIrreversibleStep(step), OR step.material === true; false for a trivially-safe push_forward reversible step. (c) The SEAM: between the onStep result capture (:359-361, after result.chain_output + result.quality are read) and the previousOutput fold (:389), when selfCritiqueFn is supplied AND _isMaterialStep(step, posture) is true, call selfCritiqueFn(step, result); if its verdict indicates a failed self-critique (e.g. verdict.quality === LOW_QUALITY or verdict.passed === false), AUGMENT the captured quality to LOW_QUALITY so the EXISTING quality_early_stop branch (:381-385) and the next-hop makeGateFn LOW_QUALITY halt (:195) fire -- do NOT add a new halt reason, do NOT add a retry, do NOT loop. The self-critique NEVER continues a halted chain and NEVER drives convergence (166 B3). Wrap the call so a selfCritiqueFn fault degrades to "no augmentation" (fail open on the critique itself -- a broken critic must not silently halt every step; but a LOW verdict halts). Keep runChain the SINGLE door (D-166-04): the seam is one opt, not a fork. Then create tests/test-chain-executor-fable-mode.cjs covering the six behaviors against the real runChain with injected postureFn / onStep / selfCritiqueFn stubs (mirror the 166 chain-executor test stub style); include the B3 no-convergence grep + the no-fable-model-tier grep with grep -v '^#' filtering. NO fenced code in this action; the change lives in chain-executor.cjs following the cited line ranges. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-chain-executor-fable-mode.cjs && echo FABLE_MODE_OK</automated>
  </verify>
  <done>the posture-scoped selfCritiqueFn seam augments result.quality on material steps only, feeding the existing LOW_QUALITY halt; absent selfCritiqueFn is byte-identical to Wave-166; no new loop / no convergence stop (B3 grep clean); no fable model tier introduced; decide()'s trace handle is unchanged (B2).</done>
</task>

<task type="auto">
  <name>Task 2: name the fable-mode self-critique contract on framework-runner.md (HARN-02)</name>
  <files>agents/framework-runner.md, tests/run-all-167.sh</files>
  <action>Edit agents/framework-runner.md to NAME the fable-mode self-critique contract, mapping it onto the EXISTING Step-3 quality gate (framework-runner.md:69-77) + the FRAMEWORK_RUNNER_RESULT quality enum (:121-136) -- do NOT invent a new quality vocabulary. Add a short subsection (near Step 3 or as an explicit contract note) stating: (1) fable-mode -- when a step runs as part of a chain (previous_output present) AND the step is material, the runner MUST VERIFY its output and SELF-CRITIQUE against the Step-3 criteria BEFORE its chain_output becomes the next step's previous_output (HARN-02); (2) the self-critique verdict is carried in the FRAMEWORK_RUNNER_RESULT quality field (high|medium|low) -- a failed self-critique sets quality:low, which the runtime maps to a halt (the chain-executor selfCritiqueFn seam augments result.quality -> the existing LOW_QUALITY gate); (3) this is NAMING over the shipped quality machinery -- it introduces NO new model, NO fable model tier, NO new quality enum value; (4) it NEVER auto-retries to convergence (166 B3) -- a low self-critique HALTS at the Decision Gate, it does not loop. Cross-reference lib/core/chain-executor.cjs selfCritiqueFn. Keep the edit additive and small; do NOT alter the Step-3 criteria or the FRAMEWORK_RUNNER_RESULT block. Then register tests/test-chain-executor-fable-mode.cjs in the tests/run-all-167.sh CJS_SUITES array and add agents/framework-runner.md + lib/core/chain-executor.cjs to the run-all-167.sh em-dash sweep + Part 8 sweep file lists. NO fenced code beyond the existing FRAMEWORK_RUNNER_RESULT block already present in the file (do not add new fenced blocks in prose). NO em-dashes.</action>
  <verify>
    <automated>grep -q 'fable-mode' agents/framework-runner.md && bash tests/run-all-167.sh && echo FRAMEWORK_RUNNER_CONTRACT_OK</automated>
  </verify>
  <done>framework-runner.md names the fable-mode verify+self-critique contract mapped onto the existing Step-3 quality gate + FRAMEWORK_RUNNER_RESULT enum (no new model, no new enum, no auto-retry); the fable-mode suite is registered in run-all-167.sh and the aggregator stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step output -> next step input | a material step's chain_output must be self-critiqued before it folds into the next step's previous_output (stops garbage propagation) |
| self-critique verdict -> gate | the verdict is a gate INPUT (augments quality); it must never become a convergence stop condition (166 B3) |
| fable-mode naming -> model layer | fable-mode names the quality machinery; it must not leak into model-profiles as a model tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-09 | Elevation of privilege | a material low-quality step auto-running undetected | mitigate | the posture-scoped selfCritiqueFn augments result.quality to LOW_QUALITY on a failed critique, feeding the existing makeGateFn halt (:195); the test drives a material step and asserts the halt |
| T-167-10 | Tampering | a smuggled auto-retry-to-convergence loop | mitigate | the seam adds no loop and no stop condition; the test grep-asserts chain-executor.cjs has no convergence-stop branch (166 B3 negative grep, grep -v '^#' filtered) |
| T-167-11 | Spoofing | a fabricated fable model tier | mitigate | the test grep-asserts no 'fable' model alias enters model-profiles / the executor; fable-mode is naming over the quality machinery only |
| T-167-12 | Denial of service | a broken self-critic halting every step | mitigate | a selfCritiqueFn fault degrades to no-augmentation (fail open on the critic); a LOW verdict still halts, but a thrown critic does not silently halt the whole chain |
| T-167-13 | Information disclosure | the seam reaching the Brain | mitigate | the seam touches result.quality only; the Wave-5 Part 8 leak scan over chain-executor.cjs finds zero Brain-write / raw-fetch tokens |
| T-167-14 | Tampering | decide()'s return shape mutated by the seam | mitigate | the seam never touches the decision object; the test deep-equals the recorded decision_trace handle (B2) |
| T-167-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (node built-ins + the shipped executor); no install task |
</threat_model>

<verification>
- `node tests/test-chain-executor-fable-mode.cjs` passes (material critiqued -> halt; trivially-safe not critiqued; default no-op byte-identical; no convergence branch; no fable model tier; B2 unchanged).
- `agents/framework-runner.md` names the fable-mode self-critique contract mapped onto the existing Step-3 quality gate + FRAMEWORK_RUNNER_RESULT enum.
- `bash tests/run-all-167.sh` is green with the fable-mode suite registered.
- chain-executor.cjs has NO new loop and NO convergence stop branch (166 B3 preserved); no fable model tier added.
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
fable-mode is a posture-scoped selfCritiqueFn seam that augments result.quality on material steps only,
feeding the existing LOW_QUALITY halt so a failed self-critique escalates autonomous_safe to a halt at the
next hop (HARN-02, D-167-04). There is no new loop, no auto-retry-to-convergence (166 B3), and no fable
model tier; fable-mode is naming over the shipped quality machinery, contracted on framework-runner.md.
</success_criteria>

<output>
Create `.planning/phases/167-harness-manifest-and-surface-generator/167-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
