---
phase: 167-harness-manifest-and-surface-generator
plan: 02
type: execute
wave: 2
depends_on: [167-01]
files_modified:
  - .git/hooks/pre-commit
  - scripts/install-pre-commit.sh
  - tests/test-harness-manifest-precommit-wiring.cjs
  - tests/run-all-167.sh
autonomous: true
requirements: [HARN-01, D-167-03, D-167-06]
canon_parts: [6, 7, 8]

must_haves:
  truths:
    - "the manifest --check is wired into the LIVE .git/hooks/pre-commit alongside the command-registry guard (:144-145) and the brain-packet-schema guard (:160-161) -- a staged manifest source or the manifest itself regenerates in memory and rejects the commit on drift (HARN-01, D-167-03)"
    - "this is STRONGER than the connector/projection precedent (whose --checks run only in test aggregators) -- D-167-03 closes that known drift gap for the manifest by enforcing in BOTH pre-commit AND run-all-167.sh"
    - "the pre-commit guard fires only when a manifest-relevant path is staged (the manifest, its generator, or any of the three named maps) -- mirroring the command-registry guard's path-scoped git diff --cached trigger"
    - "the guard makes ZERO Brain calls (the --check is a local byte-compare + map-resolve, Part 8) and degrades gracefully when node or the generator is absent (mirrors the shipped guards' command -v node + -f generator preconditions)"
    - "the manifest --check is also registered in run-all-167.sh so CI carries the same gate the hook carries (D-167-03 BOTH)"
  artifacts:
    - path: ".git/hooks/pre-commit"
      provides: "the manifest guard block (path-scoped git diff trigger -> node build-harness-manifest.cjs --check -> exit 2 on drift with a recovery line)"
      contains: "build-harness-manifest"
    - path: "scripts/install-pre-commit.sh"
      provides: "the manifest guard added to the installable pre-commit template so a fresh dev clone inherits it (the .git/hooks copy is not tracked)"
      contains: "build-harness-manifest"
    - path: "tests/test-harness-manifest-precommit-wiring.cjs"
      provides: "asserts the installable template carries the manifest guard with the correct path-scoped trigger + recovery line"
      min_lines: 40
    - path: "tests/run-all-167.sh"
      provides: "the manifest --check registered as a CI gate alongside the suites (D-167-03 BOTH)"
      contains: "build-harness-manifest"
  key_links:
    - from: ".git/hooks/pre-commit"
      to: "scripts/build-harness-manifest.cjs"
      via: "git diff --cached path-scoped trigger runs the --check, exit 2 on drift"
      pattern: "build-harness-manifest.*--check"
    - from: "scripts/install-pre-commit.sh"
      to: ".git/hooks/pre-commit"
      via: "the installable template is the tracked source the live hook is installed from"
      pattern: "build-harness-manifest"
    - from: "tests/run-all-167.sh"
      to: "scripts/build-harness-manifest.cjs"
      via: "the CI leg runs --check so the aggregator carries the same gate the hook does"
      pattern: "build-harness-manifest.*--check"
---

<objective>
WAVE 2 ENFORCEMENT (D-167-03): wire the manifest --check into BOTH the live .git/hooks/pre-commit (alongside
the command-registry guard at :144-145 and the brain-packet-schema guard at :160-161) AND the
tests/run-all-167.sh CI aggregator. D-167-03 is STRONGER than the connector/projection precedent: those
--checks run only in test aggregators (run-all-1433.sh / run-all-1441.sh), never the live pre-commit -- a
known drift gap this phase closes for the manifest. The manifest --check (built in Wave 1) asserts byte
-equality STALE + the three referenced maps resolve + per-entry well-formedness, exit-1 with a stderr
recovery line; this wave makes that gate fire at commit time AND in CI.

Purpose: a generated artifact is only as trustworthy as its drift gate. Wiring --check into pre-commit makes
a stale or malformed manifest impossible to commit; wiring it into run-all-167.sh makes the same gate carry
in CI. D-167-03 demands BOTH so the manifest is the FIRST harness registry guarded at commit time, closing
the connector/projection enforcement gap (167-RESEARCH C2).

Reuse before build (Canon Part 7): the pre-commit guard block is a VERBATIM mirror of the shipped
command-registry guard (.git/hooks/pre-commit:143-147) and the brain-packet-schema guard (:159-163) -- the
same git diff --cached --name-only | grep -qE path-scoped trigger, the same command -v node + -f generator
preconditions, the same node <generator> --check || { echo recovery >&2; exit 2; } shape. A rewrite is
rejected; the guard idiom ships and is copied in shape. The installable template (scripts/install-pre-commit.sh)
is the tracked source the live hook is installed from (the .git/hooks copy is not git-tracked), so the guard
lands in BOTH so a fresh dev clone inherits it (the Phase 123 install-pre-commit reuse precedent).

Output: the manifest guard block in .git/hooks/pre-commit + scripts/install-pre-commit.sh, the wiring test,
and the manifest --check registered in run-all-167.sh. NO em-dashes anywhere (CLAUDE.md HARD RULE).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-harness-manifest-and-surface-generator/167-SPEC.md
@.planning/phases/167-harness-manifest-and-surface-generator/167-CONTEXT.md
@.git/hooks/pre-commit
@scripts/install-pre-commit.sh
@tests/run-all-156.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: wire the manifest --check into the live pre-commit + the installable template (D-167-03)</name>
  <files>.git/hooks/pre-commit, scripts/install-pre-commit.sh</files>
  <action>Add a manifest guard block to BOTH .git/hooks/pre-commit AND scripts/install-pre-commit.sh, mirroring the command-registry guard (.git/hooks/pre-commit:143-147) and the brain-packet-schema guard (:159-163) VERBATIM in shape. The block: a path-scoped trigger -- if git diff --cached --name-only | grep -qE matches '^(scripts/build-harness-manifest\.cjs|data/harness-manifest\.json|data/command-registry\.json|data/connector-registry\.json|data/brain-orchestration-projection\.json)$' (the manifest, its generator, OR any of the three named maps, since a change to any source map can stale the manifest digests) -- then, guarded by command -v node >/dev/null 2>&1 && [ -f "$REPO_ROOT/scripts/build-harness-manifest.cjs" ], run node "$REPO_ROOT/scripts/build-harness-manifest.cjs" --check || { echo "harness-manifest drift -- run: node scripts/build-harness-manifest.cjs" >&2; exit 2; }. Add a comment header block above it in the same documentary style as the two cited guards (cite D-167-03, name it the Phase 167 guardian, state Canon Part 8: the check never touches the Brain, give the recovery line). Place it adjacent to the brain-packet-schema guard so the three harness-registry guards sit together. First read scripts/install-pre-commit.sh to locate where its template embeds the command-registry / brain-packet-schema guards and insert the manifest guard in the SAME relative position so the live hook and the installable template stay byte-consistent. Use hyphens, never em-dashes, in the echo recovery strings. NO fenced code in this action; the change lives in the two cited files following the cited guard line ranges. NO em-dashes.</action>
  <verify>
    <automated>grep -q 'build-harness-manifest.cjs --check' .git/hooks/pre-commit && grep -q 'build-harness-manifest.cjs --check' scripts/install-pre-commit.sh && bash -n .git/hooks/pre-commit && echo PRECOMMIT_WIRED_OK</automated>
  </verify>
  <done>both the live pre-commit and the installable template carry the manifest guard with the path-scoped git diff trigger (manifest + generator + three maps), the node-present + generator-present preconditions, and the exit-2 recovery line; bash -n parses both clean.</done>
</task>

<task type="auto">
  <name>Task 2: assert the wiring + register the manifest --check in run-all-167.sh (D-167-03 BOTH)</name>
  <files>tests/test-harness-manifest-precommit-wiring.cjs, tests/run-all-167.sh</files>
  <action>Create tests/test-harness-manifest-precommit-wiring.cjs (plain node assert harness): read scripts/install-pre-commit.sh (the TRACKED template -- the .git/hooks copy is untracked, so the test asserts on the template) and assert it contains the manifest guard: (a) the path-scoped grep -qE trigger matching the manifest + generator + the three named maps; (b) the node ... build-harness-manifest.cjs --check invocation; (c) the exit-2 recovery line naming 'node scripts/build-harness-manifest.cjs'; (d) the command -v node + -f generator preconditions. Also assert the template still carries the command-registry guard + the brain-packet-schema guard (a regression guard -- the new block is ADDITIVE, it does not displace the shipped guards). Add a no-em-dash assertion over the template + this test (U+2014 via String.fromCharCode(0x2014)). Then edit tests/run-all-167.sh: add a CI leg that runs node scripts/build-harness-manifest.cjs --check as a gated step (mirror the run-all-156.sh per-suite PASS/FAIL accounting -- a non-zero exit records a FAIL line and flips the final tally, never crashes the runner), so the aggregator carries the SAME gate the hook carries (D-167-03 BOTH). Register the new test-harness-manifest-precommit-wiring.cjs in the CJS_SUITES array. NO fenced code in this action. NO em-dashes.</action>
  <verify>
    <automated>node tests/test-harness-manifest-precommit-wiring.cjs && bash tests/run-all-167.sh && echo WIRING_OK</automated>
  </verify>
  <done>the test proves the installable template carries the manifest guard (path trigger + --check + recovery + node/generator preconditions) without displacing the two shipped guards; run-all-167.sh runs the manifest --check as a CI leg alongside the suites and stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged change -> pre-commit | a manifest source or map change must regenerate + byte-compare before it can land |
| live hook -> installable template | the untracked .git/hooks copy must stay byte-consistent with the tracked template so fresh clones inherit the guard |
| guard run -> Brain | the --check is a local byte-compare + map-resolve; it never touches the Brain (Part 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-05 | Tampering | a stale manifest committed past the hook | mitigate | the path-scoped pre-commit guard regenerates in memory + byte-compares + exits 2 on drift, alongside the command-registry + brain-packet-schema guards (D-167-03) |
| T-167-06 | Repudiation | a fresh dev clone missing the guard | mitigate | the guard is added to the TRACKED scripts/install-pre-commit.sh template, not only the untracked .git/hooks copy; the wiring test asserts the template carries it |
| T-167-07 | Denial of service | the guard crashing a commit when node is absent | accept | the command -v node + -f generator preconditions skip the guard gracefully when node or the generator is unavailable (mirrors the shipped guards) |
| T-167-08 | Information disclosure | the guard reaching the Brain | mitigate | the --check makes zero Brain calls (Wave-1 boundary scan CHECK 4 proves the generator clean); the guard comment states Part 8: the check never touches the Brain |
| T-167-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (bash + the Wave-1 node generator); no install task |
</threat_model>

<verification>
- `.git/hooks/pre-commit` and `scripts/install-pre-commit.sh` both contain the manifest guard (path trigger + `build-harness-manifest.cjs --check` + exit-2 recovery line).
- `bash -n .git/hooks/pre-commit` parses clean (no syntax error introduced).
- `node tests/test-harness-manifest-precommit-wiring.cjs` passes (template carries the manifest guard without displacing the command-registry + brain-packet-schema guards).
- `bash tests/run-all-167.sh` is green and runs `node scripts/build-harness-manifest.cjs --check` as a CI leg (D-167-03 BOTH).
- No em-dashes in any edited or created file.
</verification>

<success_criteria>
The manifest --check fires at commit time (live .git/hooks/pre-commit + the tracked installable template)
AND in CI (run-all-167.sh), enforced more strongly than the connector/projection precedent and closing the
known drift gap (HARN-01, D-167-03). The wiring test proves the guard is additive and correctly path-scoped.
</success_criteria>

<output>
Create `.planning/phases/167-harness-manifest-and-surface-generator/167-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
