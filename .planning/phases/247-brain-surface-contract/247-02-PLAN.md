---
phase: 247-brain-surface-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/brain-surface-contract.json
  - docs/BRAIN-SURFACE-CONTRACT.md
  - lib/core/brain-client.cjs
  - tests/test-247-brain-client-403.cjs
  - tests/test-247-contract-client.cjs
  - scripts/probe-brain-contract.cjs
  - tests/run-all-247.sh
autonomous: true
requirements: [CONTRACT-01]

must_haves:
  truths:
    - "The plugin vendors a machine-readable v1 contract (6 loop tools, 2 retired_remote, error semantics, index expectations) plus a prose doc"
    - "brain-client callTool returns a typed tier_denied sentinel on HTTP 403 and null ONLY on transport failure; 401 invalid_key behavior is unchanged"
    - "Every loop-contract tool has a thin read wrapper in brain-client that emits exactly the contracted tool name and arg keys, proven hermetically"
    - "The client fixture test derives its expectations from the vendored JSON, so a contract edit propagates without test rewrites"
    - "Each hermetic test demonstrates it CAN fail (red proof) - a test that cannot fail is not evidence"
  artifacts:
    - path: "data/brain-surface-contract.json"
      provides: "The machine half of the cross-repo contract (vendored copy 1 of 2)"
      contains: "contract_version"
    - path: "docs/BRAIN-SURFACE-CONTRACT.md"
      provides: "The prose half: what is promised, what is retired, what is non-contract"
    - path: "lib/core/brain-client.cjs"
      provides: "tier_denied sentinel + 5 loop-tool wrappers (Part 7: extend, never a fourth brain skill)"
      contains: "tier_denied"
    - path: "scripts/probe-brain-contract.cjs"
      provides: "Live drift probe (leg 3), run against Render only in 247-03"
  key_links:
    - from: "tests/test-247-contract-client.cjs"
      to: "data/brain-surface-contract.json"
      via: "expectations read from the vendored JSON at test time"
      pattern: "brain-surface-contract"
    - from: "lib/core/brain-client.cjs callTool"
      to: "403 MoatViolation body"
      via: "sentinel parse mirroring the invalid_key precedent in _ensureSession"
      pattern: "tier_denied"
    - from: "scripts/probe-brain-contract.cjs"
      to: "deployed tools/list"
      via: "raw HTTP brainCall pattern from scripts/build-brain-census.cjs"
      pattern: "tools/list"
---

<objective>
Declare the contract on the plugin side and make the client honest to code: author the vendored
contract JSON + prose doc, land the 403-vs-null transport fix in brain-client.cjs (the tier_denied
sentinel), add thin read wrappers for the 5 loop tools that have no client path today, and build
the hermetic client-fixture tests plus the live drift probe script.

Purpose: CONTRACT-01's conformance test legs 2 and 3 live in the plugin. Error semantics ARE the
contract - a client that cannot distinguish tier denial from outage cannot be conformance-tested,
and Phase 249 writes through these wrappers. The user-visible refusal is Phase 250's scope
(HONEST-01), NOT this plan's: this plan makes the client honest to CODE only.

Output: contract artifacts, patched brain-client, two hermetic test files, probe script, phase
runner. Plugin-repo work only; runs in parallel with 247-01 (zero file overlap, different repos).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/247-brain-surface-contract/247-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@lib/core/brain-client.cjs
@scripts/build-brain-census.cjs
@tests/run-all-246.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the vendored contract - data/brain-surface-contract.json + docs/BRAIN-SURFACE-CONTRACT.md</name>
  <files>data/brain-surface-contract.json, docs/BRAIN-SURFACE-CONTRACT.md</files>
  <action>
    Per CONTRACT-01 (247-RESEARCH.md conformance design). Author data/brain-surface-contract.json
    with exactly this v1 shape (names, tier, and required arg names/types only - schema-hash
    pinning is a v2 option, research open question 3):

    - contract_version: 1
    - loop_tools:
        normalize_framework_name: { tier: "read", args: { raw: "string" } }
        search:                   { tier: "read", args: { query: "string", topK: "int<=20" } }
        discover_structure:       { tier: "read", args: { framework_name: "string" } }
        orchestration_readiness:  { tier: "read", args: { framework_name: "string" } }
        feeds_into_chains:        { tier: "read", args: { seeds: "string[]", max_hops: "int<=3" } }
        brain_stats:              { tier: "read", args: {} }
    - retired_remote: ["text2cypher", "brain_ask_anything"]
    - error_semantics: { tier_denied: "HTTP 403 + MoatViolation body", invalid_key: "HTTP 401" }
    - non_contract_note: one line stating the remaining 15 read tools are neither promised nor
      removed (research Pitfall 6 - no adjudicating the other tools this phase)
    - indexes: { keep: ["mindrian_methodology_vec"], keep_retired: ["mindrian_methodology_vec_openai"],
      dropped: ["framework_embeddings", "concept_embeddings", "creativework_embeddings",
      "entity_embeddings", "person_embeddings", "product_embeddings", "vector"] }
      (gives 247-03's live gate a machine-readable expectation for the brain_stats re-probe)

    Arg names are authored from 247-RESEARCH.md's census; FINAL AUTHORITY is the brain-side
    hermetic self-test in 247-03, which reconciles against buildBrainServer()'s real zod schemas
    and updates BOTH vendored copies on mismatch. State this in a top-of-file "_note" key.

    docs/BRAIN-SURFACE-CONTRACT.md (prose half): what the 6-tool contract promises and why the
    surface is deliberately small (architectural argument - judgement visibility and drift
    surface, per research Grounding); the three conformance legs (server self-test, client
    fixture, live drift probe) and which drift class each catches; the retirement of the two
    server-side LLM tools with a pointer to the brain repo's decision record (created in 247-03);
    the non-contract status of the other 15 tools; Part 8 note (the contract is transport-level
    and changes nothing about WHAT crosses the wire). Feynman-plain, hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node -e "const c=require('./data/brain-surface-contract.json'); if(c.contract_version!==1) throw new Error('version'); if(Object.keys(c.loop_tools).length!==6) throw new Error('loop_tools'); if(c.retired_remote.length!==2) throw new Error('retired'); if(c.indexes.dropped.length!==7) throw new Error('indexes');" && grep -P '\x{2014}' docs/BRAIN-SURFACE-CONTRACT.md data/brain-surface-contract.json | wc -l | grep -qx 0</automated>
  </verify>
  <done>JSON parses with 6 loop tools, 2 retired_remote, error_semantics, indexes block; prose doc explains the contract, the three legs, and the non-contract 15; zero em-dashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: brain-client honesty - tier_denied sentinel in callTool + 5 loop-tool read wrappers</name>
  <files>lib/core/brain-client.cjs, tests/test-247-brain-client-403.cjs</files>
  <behavior>
    - Test 1: callTool receiving HTTP 403 with a MoatViolation JSON body returns { error: 'tier_denied', tool: name, message: server message } - never null
    - Test 2: callTool receiving HTTP 403 with an unparseable body still returns the tier_denied sentinel with the raw text (truncated) as message
    - Test 3: HTTP 401 keeps the existing invalid_key sentinel path unchanged
    - Test 4: network failure / timeout still returns null (transport-failure semantics unchanged)
    - Test 5: HTTP 200 returns the parsed result unchanged
    - Test 6: each new wrapper called with its args emits exactly the contracted tool name and arg keys through the transport seam
    - Test 7: query() passes the tier_denied sentinel through unchanged (the existing {error} passthrough precedent) - a read-key brain_query 403 is now distinguishable from an outage
    - Red proof: with the sentinel branch sabotaged to return null, Test 1 fails
  </behavior>
  <action>
    Per CONTRACT-01 error semantics + the 403 fix boundary (247-RESEARCH.md: transport-level
    distinction lands in 247; the LOUD user-visible refusal is Phase 250's HONEST-01 - do not add
    user-facing messaging here). Part 7: extend lib/core/brain-client.cjs; no new module, no
    fourth brain skill.

    1. In callTool's non-OK handling (the fetch near line 286): on status 403, read and drain the
       body (respect the existing un-drained-socket note near line 302), attempt JSON parse of the
       MoatViolation shape, and return { error: 'tier_denied', tool: <tool name>, message: <server
       message or truncated raw text> }. Mirror the invalid_key sentinel precedent at
       _ensureSession (line ~244) exactly - sentinels are plain objects returned through the
       promise, never throws. 401 handling stays as-is; every OTHER non-OK status keeps returning
       null (that remains the transport-failure signal). Do NOT touch isAvailable() semantics
       (research Pitfall 4: 82 degradation tests depend on it).
    2. Audit the direct callTool consumers (query, search, smartSearch, schema, stats, ask, askOp,
       write, sendPacket transport): query() already passes {error} shapes through unchanged -
       confirm each public wrapper either passes the sentinel through or handles it explicitly;
       add a regression assertion per wrapper in the test file.
    3. Add 5 thin read wrappers calling through callTool so the sentinel propagates:
       normalizeFrameworkName(raw) -> normalize_framework_name {raw};
       loopSearch(query, topK) -> search {query, topK} (named loopSearch because search() already
       wraps brain_search - record this naming rationale in the JSDoc);
       discoverStructure(frameworkName) -> discover_structure {framework_name};
       orchestrationReadiness(frameworkName) -> orchestration_readiness {framework_name};
       feedsIntoChains(seeds, maxHops) -> feeds_into_chains {seeds, max_hops}.
       brain_stats already has stats(). Export all five alongside the existing exports.
    4. Write tests/test-247-brain-client-403.cjs FIRST (CJS, node --test, hermetic): fake the
       transport via the existing test-only export block (lines ~1485-1498 expose _ensureSession
       and friends; follow how existing brain-client tests inject fakes). Cover every behavior
       above including the red proof.
  </action>
  <verify>
    <automated>node --test tests/test-247-brain-client-403.cjs</automated>
  </verify>
  <done>403 yields the typed sentinel, null means transport failure only, 401 unchanged, five wrappers emit contracted names/args, all existing brain-client tests still green (run the full plugin suite for the brain-client family), red proof demonstrated.</done>
</task>

<task type="auto">
  <name>Task 3: Conformance legs 2 and 3 - contract fixture test, live drift probe script, phase runner</name>
  <files>tests/test-247-contract-client.cjs, scripts/probe-brain-contract.cjs, tests/run-all-247.sh</files>
  <action>
    Per CONTRACT-01 conformance design (247-RESEARCH.md legs 2 and 3).

    1. tests/test-247-contract-client.cjs (CJS, node --test, hermetic): load
       data/brain-surface-contract.json and DERIVE every expectation from it - no hardcoded tool
       lists. For each loop_tools entry assert a brain-client wrapper exists (map tool name ->
       wrapper: normalize_framework_name -> normalizeFrameworkName, search -> loopSearch,
       discover_structure -> discoverStructure, orchestration_readiness -> orchestrationReadiness,
       feeds_into_chains -> feedsIntoChains, brain_stats -> stats) and that calling it through an
       injected fake transport emits exactly the contracted tool name and arg keys. Assert
       contract_version === 1 and retired_remote contains both LLM tools. Drift-detection red
       proof: run the checker against an in-memory MUTATED copy of the contract (one renamed tool)
       and assert the checker reports the mismatch - proving the test can fail.
    2. scripts/probe-brain-contract.cjs - the live drift probe (leg 3, release gate not commit
       gate; it RUNS against Render only in 247-03 Task 3). Reuse the brainCall + key-loading
       pattern from scripts/build-brain-census.cjs VERBATIM (raw HTTP, surfaces httpStatus and
       body verbatim, never null - the status-honest precedent); default URL from the single
       source at lib/core/brain-client.cjs line ~24. Probe legs, each printing PASS/FAIL with
       verbatim evidence and any failure setting a nonzero exit code:
       a. initialize a session, call tools/list with the read key; every loop_tools name must be
          present.
       b. call each retired_remote tool with a trivial arg; require httpStatus 403 with a
          MoatViolation body (registration may legitimately keep them listed - REACHABILITY is
          what retired means).
       c. call brain_query with the read key; require 403 MoatViolation (live proof of the
          tier_denied error semantics on the known admin-gated tool).
       d. call search and brain_search for "jobs to be done framework"; assert no string value in
          any served hit matches /^(?:[\/~]|[A-Za-z]:[\\\/]|\\\\)/ (the CONTRACT-03 live check).
       e. call brain_stats; assert every contract.indexes.dropped name is ABSENT and every keep /
          keep_retired name is PRESENT (the CONTRACT-04 live check).
       No secrets in the repo: key comes from the environment / ~/.mindrian.env exactly as the
       census script loads it.
    3. tests/run-all-247.sh: mirror tests/run-all-246.sh - glob discovery of tests/test-247-*
       (.cjs and .sh), the found-eq-0 guard MUST fail when nothing is discovered (load-bearing,
       never soften), and the em-dash fence sweep over every file this phase touches in the
       plugin repo (the U+2014 codepoint grep, same as 246's runner).
  </action>
  <verify>
    <automated>bash tests/run-all-247.sh && node --check scripts/probe-brain-contract.cjs</automated>
  </verify>
  <done>Fixture test green with expectations derived from the vendored JSON and drift red proof shown; probe script parses clean and is ready for the 247-03 live gate (not run against Render here); runner discovers both 247 tests and the em-dash fence passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server | 403/401 response bodies are untrusted input parsed into sentinels |
| repo -> live probe | probe handles the read key; must never write it into tracked files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-247-04 | Tampering | sentinel parse of 403 body | mitigate | defensive JSON parse with raw-text fallback and truncation (Task 2 behavior 2) |
| T-247-05 | Information Disclosure | probe script and read key | mitigate | key loaded from env/~/.mindrian.env only, census-script pattern; nothing written to repo |
| T-247-06 | Repudiation | probe claiming green without evidence | mitigate | verbatim httpStatus + body printed per leg; nonzero exit on any drift |
| T-247-SC | Tampering | npm/pip/cargo installs | accept | no packages installed this phase (research Package Legitimacy Audit: none) |
</threat_model>

<verification>
- node --test tests/test-247-brain-client-403.cjs and tests/test-247-contract-client.cjs green
- bash tests/run-all-247.sh green including the found-eq-0 guard and the em-dash fence
- Existing brain-client-dependent suites unaffected (isAvailable semantics untouched)
- data/brain-surface-contract.json parses; docs prose in place
</verification>

<success_criteria>
- CONTRACT-01 client leg complete: contract vendored, wrappers contracted, fixture test derives from the JSON, probe script ready
- The 403-vs-null conflation is dead at the transport layer; nothing user-facing changed (Phase 250 owns that)
- Every new test demonstrated it can fail
</success_criteria>

<output>
Create `.planning/phases/247-brain-surface-contract/247-02-SUMMARY.md` when done. Note for the
record: plugin-side changes are NOT live for users until a plugin release cuts on the normal
train (Gates 0/1 in ROADMAP Progress); do not claim otherwise.
</output>
