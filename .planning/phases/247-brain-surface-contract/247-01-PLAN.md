---
phase: 247-brain-surface-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/auth.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/src/server.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/rate-limit.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/allowlist.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/src/graph-client.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/search-provenance-hygiene.test.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/index-creation-guard.test.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/build-vector-index.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/migrate-neo4j-to-memgraph.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/migrate-memgraph-to-memgraph.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/compare-backends.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/VECTOR-INDEX-DISPOSITIONS.md
autonomous: true
requirements: [CONTRACT-02, CONTRACT-03, CONTRACT-04]

must_haves:
  truths:
    - "The brain checkout at /home/jsagi/dev/ProblemsWorthSolving-Brain sits on top of origin/main with a clean tree; each of the 4 dirty draft files was explicitly reviewed and either landed as a reviewed commit or discarded with a recorded reason"
    - "No value served through scopedVectorSearch contains an absolute filesystem path; the path leaf survives for provenance"
    - "The framework field decision (populate or remove) rests on a measured MENTIONS coverage number, never a guess, and the field is never silently empty"
    - "Recreating a non-1024-dim vector index through the migrate scripts without --include-foreign throws, fail closed"
    - "All 9 vector indexes have a recorded disposition with a reader-census grep proof filed"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/src/graph-client.mjs"
      provides: "source_file strip at the scopedVectorSearch seam (covers search, brain_search, arm2-expansion)"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/search-provenance-hygiene.test.mjs"
      provides: "Hermetic ABS_PATH leak test with red proof"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/index-creation-guard.test.mjs"
      provides: "Fail-closed creation guard test (384 recreate without flag throws)"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/VECTOR-INDEX-DISPOSITIONS.md"
      provides: "9-row disposition matrix with grep proofs (CONTRACT-04 record)"
  key_links:
    - from: "src/graph-client.mjs scopedVectorSearch"
      to: "sanitize helper (hoisted from the brain_search draft)"
      via: "metadata pass through the seam before return"
      pattern: "sanitize"
    - from: "scripts/build-vector-index.mjs"
      to: "src/contracts/e5-identity.mjs"
      via: "E5_DIM import replacing the local DIM literal"
      pattern: "e5-identity"
    - from: "scripts/migrate-neo4j-to-memgraph.mjs"
      to: "foreign-index refusal"
      via: "dimension check at the CREATE VECTOR INDEX site"
      pattern: "include-foreign"
---

<objective>
Reconcile the brain repo's hazardous state (15 commits behind origin/main, 4 files of UNREVIEWED
uncommitted drafts), then land the CONTRACT-03 provenance seam fix and the CONTRACT-04
index-creation guards and disposition record on the reconciled base.

Purpose: the contract cannot be declared over an unreconciled surface (247-RESEARCH.md, Repo-State
Hazard). Half of CONTRACT-02/03 already exists as unshipped drafts written against a stale base;
they must become reviewed commits BEFORE any new contract code, or the phase builds on sand.

Output: clean brain working tree on top of origin/main with up to 4 reviewed reconcile commits,
the seam strip + framework field decision, fail-closed creation guards, and the disposition matrix.
All commits stay LOCAL to the brain repo in this plan - the push is 247-03's operator checkpoint,
because pushing origin/main may auto-deploy Render (research assumption A2, unverified).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/247-brain-surface-contract/247-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Server repo (cross-repo work happens HERE, its own git, ESM .mjs convention):
/home/jsagi/dev/ProblemsWorthSolving-Brain

Repo mapping note (verified at plan time): the ROADMAP's name "jsagir/brain_ProblemsWorthSolving"
and the local checkout /home/jsagi/dev/ProblemsWorthSolving-Brain (origin =
github.com/jsagir/ProblemsWorthSolving-Brain.git) are the SAME repo; the name in ROADMAP is a
transcription variant. /home/jsagi/dev/mindrian-brain-local is a DIFFERENT repo
(github.com/jsagir/mindrian-brain-local.git) - do not touch it. Task 1 re-verifies this mapping
before any edit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the brain repo - review the 4 unreviewed drafts, land as reviewed commits on top of origin/main</name>
  <files>/home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/auth.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/src/server.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/rate-limit.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/src/ingest/allowlist.mjs</files>
  <action>
    Per CONTRACT-02/03 substrate (247-RESEARCH.md "Repo-State Hazard"). All commands run in
    /home/jsagi/dev/ProblemsWorthSolving-Brain.

    PREFLIGHT (stop and report if any check fails):
    1. `git remote -v` must show github.com/jsagir/ProblemsWorthSolving-Brain.git. If it shows
       anything else, STOP - wrong checkout (mindrian-brain-local is a different repo).
    2. `git fetch origin` then `git rev-list --count HEAD..origin/main` - expect roughly 15
       (research counted 15 on 2026-08-10; record the actual number). `git status --short` -
       expect exactly 4 modified files: src/http/auth.mjs, src/server.mjs, src/http/rate-limit.mjs,
       src/ingest/allowlist.mjs. A different dirty set means the repo moved since research;
       report the delta verbatim before proceeding.
    3. Read docs/2026-08-09-GSD-TAKEOVER-nl-answer-quality.md on origin/main
       (`git show origin/main:docs/2026-08-09-GSD-TAKEOVER-nl-answer-quality.md`): its
       "WHAT ALREADY SHIPPED - do not redo" and "EXPLICITLY NOT IMPROVEMENTS" lists BIND this
       reconciliation - do not re-land anything those lists mark shipped or rejected.

    EXPLICIT REVIEW (these drafts are UNREVIEWED - review each diff individually, never
    `git add -A` blind):
    4. `git diff -- src/http/auth.mjs`: the deny-by-default READ_TOOLS allowlist draft. Review
       assertions: (a) READ_TOOLS must include at minimum the 4 production plugin client tools
       (brain_ask, brain_search, brain_schema, brain_stats) AND the 6 loop-contract tools
       (normalize_framework_name, search, discover_structure, orchestration_readiness,
       feeds_into_chains, brain_stats) - a missing member breaks live users on deploy; add any
       missing one before landing. (b) PROTOCOL_METHODS pass-through must keep tools/list working
       on a read key (the 247-03 live probe depends on it). (c) Record the unknown-tool posture:
       hard deny by default, or conditioned on BRAIN_HTTP_STRICT_TOOL_GATE - 247-03's checkpoint
       needs to know what Render env to set. (d) The draft includes brain_ask_anything in
       READ_TOOLS and withholds text2cypher: LAND IT FAITHFULLY AS DRAFTED - removing
       brain_ask_anything is 247-03's CONTRACT-02 change, made together with the decision record,
       not silently here.
    5. `git diff -- src/server.mjs`: the sanitizeFields source_file strip inside the brain_search
       handler (working tree lines ~686-711). Verdict: land as drafted; note in the commit message
       that Task 2 hoists it to the scopedVectorSearch seam because the per-tool location leaves
       the `search` tool leaking (research CONTRACT-03).
    6. `git diff -- src/http/rate-limit.mjs` and `git diff -- src/ingest/allowlist.mjs`: review
       for coherence against the 15 newer commits. Land if they still apply and tests pass;
       discard with the reason written into the reconcile commit message if they duplicate or
       conflict with shipped work from step 3's list.

    RECONCILE (recoverable flow, drafts snapshotted before anything moves):
    7. `git checkout -b snapshot-pre-247 && git add -A && git commit -m "snapshot: unreviewed pre-247 drafts (auth allowlist, brain_search sanitize, rate-limit, ingest allowlist)"`
    8. `git checkout main` (tree is now clean at the stale HEAD), then
       `git pull --ff-only origin main` (lands the ~15 commits; research names among them the e5
       identity guards a3612fb/0e79704, the tool-description-honesty test, ingest fixes, a router
       lowercase fix).
    9. `git cherry-pick -n snapshot-pre-247` to re-apply the drafts uncommitted; resolve conflicts
       PREFERRING origin/main's shipped versions wherever a draft duplicates shipped work.
    10. Split into up to 4 reviewed commits (one per concern: auth allowlist, brain_search
        sanitize, rate-limit, ingest allowlist), each commit message carrying the review verdict
        from steps 4-6. A discarded draft gets its reason recorded in whichever commit message is
        nearest, or in the plan SUMMARY if nothing lands from that file.
    11. DO NOT PUSH. Pushing origin/main may auto-deploy Render (A2); the single push/deploy event
        is 247-03 Task 2's operator checkpoint.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && git status --short | wc -l | grep -qx 0 && node --test tests/</automated>
  </verify>
  <done>Working tree clean; `git log --oneline origin/main..HEAD` shows only the reviewed reconcile commits; full brain suite green; review verdicts recorded per file; nothing pushed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CONTRACT-03 - hoist the source_file strip to the scopedVectorSearch seam and decide the framework field from measured coverage</name>
  <files>/home/jsagi/dev/ProblemsWorthSolving-Brain/src/graph-client.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/src/server.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/search-provenance-hygiene.test.mjs</files>
  <behavior>
    - Test 1: a hit whose metadata contains {"source_file":"/home/jsagi/MindrianV2/prompts/jtbd.py"} comes out of the seam with source_file === "jtbd.py" (leaf kept, absolute prefix gone)
    - Test 2: Windows drive ("C:\\Users\\x\\y.md"), UNC ("\\\\srv\\share\\z.md"), and tilde ("~/notes/a.md") values are all stripped to their leaf
    - Test 3: no string value in any served hit matches the ABS_PATH regex /^(?:[\/~]|[A-Za-z]:[\\\/]|\\\\)/
    - Test 4: the framework field is either present and sourced from MENTIONS edges, or the key is absent entirely - an always-empty framework key fails the test
    - Red proof: running the same fixture through the raw (pre-sanitize) path DOES match ABS_PATH, proving the assertion has teeth (tests/helpers/red-proof.mjs pattern)
  </behavior>
  <action>
    Per CONTRACT-03 (247-RESEARCH.md leak trace). One seam covers all three consumers (search
    tool, brain_search, arm2-expansion); do NOT fix per-tool.

    1. Extract the sanitize function landed in Task 1's server.mjs commit (ABS_PATH regex plus
       keep-the-leaf: split on /[\\\/]/, take the last non-empty segment) into src/graph-client.mjs
       and apply it to every hit's metadata inside scopedVectorSearch (exported function, currently
       near line 208; locate by name after the pull) just before the normalized
       {id, score, rawCosine, metadata} return. The brain_search handler in src/server.mjs then
       drops its local copy and relies on the seam - exactly ONE implementation survives.
       arm2-expansion keeps working: it reads parsed.source_file for attribution and the leaf is
       preserved.
    2. Framework field precondition (research A4 - measure before building): run the coverage
       Cypher through the repo's own driver (reuse the connection bootstrap pattern from
       scripts/build-vector-index.mjs; credentials from the brain repo's own env; write the
       one-off runner in the session scratchpad, not the repo):
       MATCH (c:MethodologyChunk) WITH count(c) AS total
       OPTIONAL MATCH (c2:MethodologyChunk)-[:MENTIONS]->(:Framework)
       WITH total, count(DISTINCT c2) AS covered
       RETURN total, covered, toFloat(covered)/total AS coverage
       Record the number in the commit message and plan SUMMARY.
    3. If coverage >= 0.5: in the search tool handler in src/server.mjs, extend the EXISTING
       MENTIONS expansion Cypher with a direct leg collecting
       (chunk)-[:MENTIONS]->(f:Framework) | f.name and emit it as a `framework` array on each
       served hit (one extra collect on a round trip already paid for). If coverage < 0.5: remove
       the framework key from the served payload entirely. Either branch satisfies CONTRACT-03;
       only "silently empty" is forbidden. If the driver connection is unavailable from this
       machine, do NOT guess: implement the strip (step 1) fully, leave the framework decision
       flagged for 247-03 Task 2's operator checkpoint (the operator runs the same Cypher with
       admin access), and say so in the SUMMARY.
    4. Write tests/search-provenance-hygiene.test.mjs per the behavior block (node --test, .mjs,
       hermetic - poisoned fixture through the seam function directly, zero network).
    5. Commit with the coverage number and the populate-or-remove verdict in the message. No push.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/search-provenance-hygiene.test.mjs && node --test tests/</automated>
  </verify>
  <done>Strip lives at the scopedVectorSearch seam with a single implementation; framework field populated or removed per the measured number (or explicitly deferred to the 247-03 checkpoint if the driver was unreachable); hygiene test green including red proof; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CONTRACT-04 - wire e5-identity guards into index CREATE paths fail-closed and file the 9-index disposition matrix with grep proof</name>
  <files>/home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/build-vector-index.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/migrate-neo4j-to-memgraph.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/migrate-memgraph-to-memgraph.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/scripts/compare-backends.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/index-creation-guard.test.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/VECTOR-INDEX-DISPOSITIONS.md</files>
  <behavior>
    - Test 1: guard decision for {dimension: 1024} passes without any flag
    - Test 2: guard decision for {dimension: 384} WITHOUT --include-foreign throws an error naming the index and its dimension
    - Test 3: guard decision for {dimension: 384} WITH --include-foreign passes
    - Red proof: the throw in Test 2 is asserted with assert.throws - a guard that silently warns instead of throwing turns the test red
  </behavior>
  <action>
    Per CONTRACT-04 (247-RESEARCH.md index census). The query-time guard already shipped
    (src/contracts/e5-identity.mjs); this closes the CREATE-time gap. Do not hand-roll new
    dimension checks - import from e5-identity.mjs.

    1. scripts/build-vector-index.mjs: replace the local `const DIM = 1024` (line ~24) with
       `import { E5_DIM } from '../src/contracts/e5-identity.mjs'`; keep the verify logic intact.
    2. Extract a small pure guard helper (e.g. assertRecreateAllowed({name, dimension}, {includeForeign}))
       that throws unless dimension === E5_DIM or includeForeign is true. Wire it at BOTH index
       recreation sites: the vector-index loop in scripts/migrate-neo4j-to-memgraph.mjs (the
       CREATE VECTOR INDEX call near line 340) and the equivalent site in
       scripts/migrate-memgraph-to-memgraph.mjs (locate via grep for CREATE VECTOR INDEX). Add
       --include-foreign flag parsing to both scripts. Fail closed: generic recreate-everything
       behavior is exactly how the 8 foreign indexes reached Render.
    3. tests/index-creation-guard.test.mjs: hermetic unit test of the guard helper per the
       behavior block (node --test, .mjs, no network).
    4. scripts/compare-backends.mjs (line ~247): neutralize the framework_embeddings reader -
       check index existence first and skip with a loud message pointing at
       docs/VECTOR-INDEX-DISPOSITIONS.md, so the diagnostic degrades cleanly after the drop
       instead of erroring.
    5. Write docs/VECTOR-INDEX-DISPOSITIONS.md - the CONTRACT-04 record. One row per index, all 9,
       citing the repo's never-re-embed rule:
       - mindrian_methodology_vec (1024): KEEP - the corpus substrate, entire search path reads it.
       - mindrian_methodology_vec_openai (1536): KEEP-RETIRED - recorded reasoning: it backs
         scripts/capture-overlap-baseline.mjs (a filed eval baseline), is already schema-retired
         and egress-guarded; drop deferred until the overlap baseline is declared historical
         (research open question 2). Requirement satisfied by the recorded disposition.
       - framework_embeddings (384): DROP - sole reader compare-backends.mjs neutralized in step 4;
         grep proof filed.
       - concept_embeddings, creativework_embeddings, entity_embeddings, person_embeddings,
         product_embeddings, vector (all 384): DROP - zero readers; grep proof filed.
       Grep proof, run fresh and pasted verbatim into the doc: for each named index run
       `grep -rn "<name>" src/ scripts/` and show the empty (or neutralized-only) result. For the
       index literally named `vector`, grep the QUOTED identifier forms ("vector", 'vector') and
       index-name positions in Cypher strings, and record the disambiguation method in the doc -
       a bare-word grep proves nothing. State explicitly: drops EXECUTE at 247-03's operator
       checkpoint (admin key, snapshot first); this document records the decision and the proof.
    6. Commit. No push.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/index-creation-guard.test.mjs && node --test tests/ && test -f docs/VECTOR-INDEX-DISPOSITIONS.md && grep -v '^#' docs/VECTOR-INDEX-DISPOSITIONS.md | grep -c "DROP\|KEEP" | awk '{exit ($1>=9)?0:1}'</automated>
  </verify>
  <done>Both migrate scripts refuse non-1024 recreates without --include-foreign; build-vector-index.mjs imports E5_DIM; guard test green with red proof; disposition matrix filed with 9 rows and fresh grep proofs; compare-backends.mjs degrades cleanly post-drop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> read-key client | served search metadata crosses here (the leak path) |
| working tree -> origin/main | unreviewed drafts become deployed behavior on the next push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-247-01 | Information Disclosure | scopedVectorSearch metadata passthrough | mitigate | strip at the seam (Task 2); hermetic ABS_PATH test with red proof |
| T-247-02 | Tampering | reconciliation landing unreviewed security drafts | mitigate | per-file explicit review with named assertions (Task 1 steps 4-6); snapshot branch before any move |
| T-247-03 | Denial of Service | migrate scripts recreating foreign indexes onto a 2GB instance | mitigate | fail-closed guard at CREATE sites (Task 3); --include-foreign explicit override only |
| T-247-SC | Tampering | npm/pip/cargo installs | accept | no packages installed this phase (research Package Legitimacy Audit: none) |
</threat_model>

<verification>
- cd /home/jsagi/dev/ProblemsWorthSolving-Brain && git status --short is empty and HEAD is ahead of origin/main by only the reviewed 247 commits
- node --test tests/ fully green in the brain repo
- grep -rn "sanitize" src/graph-client.mjs shows the strip at the seam; the brain_search handler carries no second copy
- Both migrate scripts contain the include-foreign guard; build-vector-index.mjs imports E5_DIM
- docs/VECTOR-INDEX-DISPOSITIONS.md has 9 rows with pasted grep proofs
</verification>

<success_criteria>
- Repo-state hazard closed: 0 dirty files, reconciled on origin/main, every draft reviewed on the record
- CONTRACT-03 code complete server-side (strip at seam, framework decision measured)
- CONTRACT-04 code and record complete (creation guards fail closed, dispositions filed)
- Nothing pushed; nothing claimed live (deploy and live proof belong to 247-03)
</success_criteria>

<output>
Create `.planning/phases/247-brain-surface-contract/247-01-SUMMARY.md` when done. Include the
actual behind-count found, the per-file review verdicts, the measured MENTIONS coverage number,
and the framework populate-or-remove decision.
</output>
