---
phase: 247-brain-surface-contract
plan: 03
type: execute
wave: 2
depends_on: [247-01, 247-02]
files_modified:
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/auth.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/src/server.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/data/brain-surface-contract.json
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/BRAIN-SURFACE-CONTRACT.md
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/DECISION-remote-llm-tools-retired.md
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/surface-contract.test.mjs
  - /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/tool-description-honesty.test.mjs
  - data/brain-surface-contract.json
autonomous: false
requirements: [CONTRACT-01, CONTRACT-02, CONTRACT-03, CONTRACT-04]

must_haves:
  truths:
    - "A read key calling text2cypher or brain_ask_anything on the DEPLOYED service gets HTTP 403 with a MoatViolation body, not an ECONNREFUSED-shaped error; both tools stay registered for the local stdio context"
    - "The retirement decision and its architecture-of-record reasoning are written down in the brain repo, not in a chat log"
    - "Both repos carry byte-identical brain-surface-contract.json copies that match the deployed surface"
    - "The hermetic surface self-test goes red on injected drift, proven once via the red-proof pattern"
    - "The 6 reader-less 384-dim indexes plus the neutralized framework_embeddings are gone from live brain_stats; mindrian_methodology_vec and the keep-retired openai index remain"
    - "scripts/probe-brain-contract.cjs exits 0 against pws-brain-mcp.onrender.com - the phase does not close on a red probe"
  artifacts:
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/tests/surface-contract.test.mjs"
      provides: "Conformance leg 1: hermetic server self-test with tier-gate assertions and red proof"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/docs/DECISION-remote-llm-tools-retired.md"
      provides: "CONTRACT-02 recorded decision with reasoning"
    - path: "/home/jsagi/dev/ProblemsWorthSolving-Brain/data/brain-surface-contract.json"
      provides: "Vendored contract copy 2 of 2, byte-identical to the plugin copy"
  key_links:
    - from: "src/http/auth.mjs READ_TOOLS"
      to: "retired_remote enforcement"
      via: "deny-by-default allowlist with both LLM tools absent"
      pattern: "brain_ask_anything"
    - from: "tests/surface-contract.test.mjs"
      to: "data/brain-surface-contract.json"
      via: "buildBrainServer() enumeration asserted against the vendored JSON"
      pattern: "brain-surface-contract"
    - from: "scripts/probe-brain-contract.cjs (plugin)"
      to: "pws-brain-mcp.onrender.com"
      via: "live tools/list + per-tool probes, the release gate"
      pattern: "tools/list"
---

<objective>
Finish the contract server-side and prove it on the surface users actually reach: retire the two
server-side LLM tools from the remote read surface at the tier gate (CONTRACT-02, decision
recorded), vendor the contract into the brain repo with the hermetic self-test (CONTRACT-01
leg 1), then - through a single operator checkpoint - push, redeploy Render, snapshot, and execute
the CONTRACT-04 index drops with the admin key, and close the phase only when the live drift probe
runs green.

Purpose: four prior occurrences of "fixed in git, stale on Render" in three weeks
(feedback_dev_repo_fix_not_live_until_released). The cross-repo definition of done binds: a
requirement is not done until the surface a user reaches is fixed. This plan IS that proof.

Output: retirement + decision record + brain-side contract + self-test committed; everything
pushed and deployed under operator supervision; 7 indexes dropped with snapshot-first safety;
live conformance gate green and filed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/247-brain-surface-contract/247-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Server repo: /home/jsagi/dev/ProblemsWorthSolving-Brain (reconciled by 247-01; commits from 247-01
are local and unpushed - this plan pushes them together with its own under the Task 2 checkpoint).
Plugin contract copy + probe script come from 247-02.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CONTRACT-02 retirement at the tier gate + brain-side contract vendoring + hermetic surface self-test</name>
  <files>/home/jsagi/dev/ProblemsWorthSolving-Brain/src/http/auth.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/src/server.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/data/brain-surface-contract.json, /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/BRAIN-SURFACE-CONTRACT.md, /home/jsagi/dev/ProblemsWorthSolving-Brain/docs/DECISION-remote-llm-tools-retired.md, /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/surface-contract.test.mjs, /home/jsagi/dev/ProblemsWorthSolving-Brain/tests/tool-description-honesty.test.mjs, data/brain-surface-contract.json</files>
  <behavior>
    - Test 1: buildBrainServer() registers every loop_tools name from the vendored JSON, with input schemas whose required arg keys match the contract
    - Test 2: READ_TOOLS contains every loop_tools name; neither text2cypher nor brain_ask_anything is in READ_TOOLS (retired_remote unreachable on the read tier)
    - Test 3: both retired tools ARE still registered (local stdio capability preserved - retirement is reachability, not deregistration; deregistration would break the eval gate, research Pitfall 2)
    - Test 4: the vendored JSON parses and contract_version === 1
    - Red proof: with a sabotage seam renaming one contract tool, the self-test fails (tests/helpers/red-proof.mjs pattern)
  </behavior>
  <action>
    All brain-repo work on the base 247-01 reconciled. Commits local; no push (Task 2 owns it).

    1. RETIREMENT (CONTRACT-02): in src/http/auth.mjs, remove brain_ask_anything from READ_TOOLS
       (the reconciled draft still includes it) and confirm text2cypher remains absent. Both tools
       stay registered in src/server.mjs - do NOT deregister; the stdio/local context (where
       Ollama exists) keeps them, and compare-text2cypher.mjs's eval gate keeps working. Confirm
       the unknown-tool posture recorded by 247-01 Task 1: if hard-deny requires
       BRAIN_HTTP_STRICT_TOOL_GATE=true on Render, write that requirement into Task 2's
       checkpoint steps.
    2. DECISION RECORD (CONTRACT-02 "decision recorded"): write
       docs/DECISION-remote-llm-tools-retired.md - decision: text2cypher and brain_ask_anything
       are RETIRED from the remote read surface, retained locally. Reasoning, in this order:
       (a) architecture of record (navigator 2026-08-10): reasoning belongs to Larry, not the
       Brain - the loop's synthesizer is the client-side model operating the join; a server-side
       LLM is a second reasoning brain the design forbids; (b) traced failure mode: both route
       through src/llm-provider.mjs to local Ollama, absent on Render by design, so remote calls
       die ECONNREFUSED; (c) text2cypher's own probation record (1/22 on Brain-shaped questions);
       (d) sidecar option assessed and rejected (cost, latency vs the 20s client timeout,
       and the architectural argument decides regardless - 247-RESEARCH.md CONTRACT-02 section).
       Enforcement: the retired_remote list in the vendored contract + the live probe make the
       retirement permanent.
    3. VENDOR (CONTRACT-01): copy the plugin's data/brain-surface-contract.json to the brain
       repo's data/brain-surface-contract.json (create the dir; path symmetry keeps the byte-match
       trivial) and adapt docs/BRAIN-SURFACE-CONTRACT.md prose for the brain repo (same content,
       server-side framing). RECONCILE FIRST: enumerate the real registered tools and their zod
       schemas via buildBrainServer() in-process; if any contract arg name/type differs from the
       research-authored JSON, fix BOTH vendored copies (brain repo AND plugin
       data/brain-surface-contract.json) in this same change set - the server's real schemas are
       the authority, and the copies must stay byte-identical.
    4. SELF-TEST (CONTRACT-01 leg 1): write tests/surface-contract.test.mjs (node --test, .mjs,
       hermetic - build buildBrainServer() in-process, zero network) per the behavior block,
       following the tool-description-honesty.test.mjs precedent for asserting registered-surface
       properties and tests/helpers/red-proof.mjs for the red proof.
    5. DESCRIPTION HONESTY: update the two retired tools' descriptions in src/server.mjs if they
       promise remote capability the gate now refuses (e.g. append "local stdio only; retired
       from the remote surface - see docs/DECISION-remote-llm-tools-retired.md"), and keep
       tests/tool-description-honesty.test.mjs green with the new text.
    6. DROP RUNBOOK PREP for Task 2: verify the exact Memgraph statement for dropping a vector
       index against current Memgraph docs via Context7 (resolve memgraph, query vector index
       drop syntax) - do not guess syntax that an operator will paste into an admin session. List
       the 7 statements (framework_embeddings, concept_embeddings, creativework_embeddings,
       entity_embeddings, person_embeddings, product_embeddings, vector) plus the snapshot-first
       step, ready to present verbatim in the checkpoint.
    7. Commit everything locally. If 247-01 deferred the framework-field decision (driver
       unreachable), carry the coverage Cypher into the checkpoint steps too.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/surface-contract.test.mjs && node --test tests/ && cmp data/brain-surface-contract.json /home/jsagi/dev/MindrianOS-Plugin/data/brain-surface-contract.json</automated>
  </verify>
  <done>READ_TOOLS excludes both LLM tools while both stay registered; decision record filed with the architecture-of-record reasoning; vendored copies byte-identical across repos and reconciled against real schemas; self-test green with red proof; full brain suite green; drop runbook prepared with doc-verified syntax.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator checkpoint - push, Render redeploy, snapshot, admin-key index drops</name>
  <action>
    Everything automatable is already done and committed locally. The remaining steps touch the
    Render deployment and require the ADMIN key (not present on this machine - census Lane B
    precedent), so they are operator-supervised, never automated.

    Present to the operator, with the exact commands and the runbook from Task 1 step 6:
    1. Review the outgoing commits: cd /home/jsagi/dev/ProblemsWorthSolving-Brain and
       git log --oneline origin/main..HEAD (the 247-01 reconcile commits plus this plan's).
    2. Push: git push origin main.
    3. In the Render dashboard for pws-brain-mcp: confirm whether auto-deploy fired on the push
       (research assumption A2 - record the actual trigger setting for the SUMMARY), wait for the
       deploy to reach the new commit, and set BRAIN_HTTP_STRICT_TOOL_GATE if Task 1 recorded it
       as required for hard-deny.
    4. With the ADMIN key: run create_snapshot FIRST (research Pitfall 5 - snapshot bounds the
       damage of any unfound reader), then execute the 7 prepared DROP statements for
       framework_embeddings, concept_embeddings, creativework_embeddings, entity_embeddings,
       person_embeddings, product_embeddings, and vector. Do NOT touch mindrian_methodology_vec
       (the corpus) or mindrian_methodology_vec_openai (KEEP-RETIRED, recorded in
       docs/VECTOR-INDEX-DISPOSITIONS.md).
    5. If 247-01 deferred the framework-field coverage decision, run the coverage Cypher from the
       runbook now and report the number so the decision closes on a measurement.
  </action>
  <verify>
    <automated>MISSING - operator-gated by design; the automated proof is Task 3's live gate, which runs immediately after resume</automated>
  </verify>
  <resume-signal>Reply "deployed" with: the deployed commit hash, the Render deploy-trigger setting observed, snapshot confirmation, and which drops executed (expected: all 7).</resume-signal>
  <done>Commits pushed; Render serving the new commit; snapshot taken; 7 indexes dropped; admin key never stored on this machine or in any repo.</done>
</task>

<task type="auto">
  <name>Task 3: Live conformance gate - the phase closes green against the deployed surface or not at all</name>
  <files>none (runs the 247-02 probe and both suites; writes results to the SUMMARY only)</files>
  <action>
    Per CONTRACT-01 leg 3 and the standing rule that a fix is not live until the deployed surface
    proves it. Run from /home/jsagi/dev/MindrianOS-Plugin with the read key from ~/.mindrian.env:

    1. node scripts/probe-brain-contract.cjs - must exit 0. This single gate covers: every
       contract tool present in live tools/list; text2cypher and brain_ask_anything returning 403
       MoatViolation on a read key (CONTRACT-02 live); brain_query 403 MoatViolation (error
       semantics live); live search and brain_search responses free of absolute paths
       (CONTRACT-03 live); brain_stats showing the 7 dropped indexes absent and the 2 keepers
       present (CONTRACT-04 live).
    2. bash tests/run-all-247.sh (plugin hermetic suite) and
       cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/ (brain suite) - both
       fully green.
    3. On ANY red leg: report the drift verbatim in-turn (never silently), do not mark the phase
       complete, and return to the operator with the exact failing evidence - a red probe after
       deploy means deploy drift or a wrong assumption, and it is this phase's job to catch it.
    4. File the probe output (per-leg PASS lines with evidence) in the plan SUMMARY, alongside the
       deployed commit hash from Task 2's resume signal.
  </action>
  <verify>
    <automated>node scripts/probe-brain-contract.cjs && bash tests/run-all-247.sh && cd /home/jsagi/dev/ProblemsWorthSolving-Brain && node --test tests/</automated>
  </verify>
  <done>Probe exits 0 against pws-brain-mcp.onrender.com; both repos' suites green; per-leg evidence filed; phase closable with the user-reached surface proven fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read key -> tool surface | the tier gate is the enforcement point for the retired tools |
| operator -> live graph | admin-key drops mutate deployed state irreversibly without the snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-247-07 | Elevation of Privilege | READ_TOOLS allowlist membership | mitigate | deny-by-default set + hermetic self-test asserting retired tools absent from the read tier |
| T-247-08 | Denial of Service | dropping an index with an unfound out-of-repo reader (research A3) | mitigate | snapshot-first mandatory; shipped e5 guards fail CLOSED on unknown indexes so a stale reader errors loudly |
| T-247-09 | Information Disclosure | admin key handling at the checkpoint | mitigate | operator-supplied only, never stored on this machine or in any repo |
| T-247-10 | Repudiation | claiming done while Render serves the old commit | mitigate | Task 3 live gate is mandatory for phase close; deployed commit hash recorded |
| T-247-SC | Tampering | npm/pip/cargo installs | accept | no packages installed this phase (research Package Legitimacy Audit: none) |
</threat_model>

<verification>
- Hermetic: tests/surface-contract.test.mjs green with red proof; full brain suite green; vendored copies byte-identical (cmp)
- Live: scripts/probe-brain-contract.cjs exit 0 against pws-brain-mcp.onrender.com after deploy and drops
- Records on disk: DECISION-remote-llm-tools-retired.md, VECTOR-INDEX-DISPOSITIONS.md (from 247-01), both contract copies
</verification>

<success_criteria>
- CONTRACT-01: both repos declare the same contract; all three conformance legs exist and the live leg is green
- CONTRACT-02: retired from the remote surface with the decision and reasoning recorded; local capability intact
- CONTRACT-03 and CONTRACT-04: proven on the DEPLOYED surface (no paths served, dispositions executed), not just in git
- No false "done": phase close requires the live gate, honoring feedback_dev_repo_fix_not_live_until_released
</success_criteria>

<output>
Create `.planning/phases/247-brain-surface-contract/247-03-SUMMARY.md` when done. Include the
deployed commit hash, the observed Render deploy-trigger setting (closes research A2), the drop
confirmations, and the verbatim per-leg probe evidence.
</output>
