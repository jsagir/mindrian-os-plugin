---
phase: 273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a
plan: 03
type: execute
wave: 2
depends_on: ["273-01", "273-02"]
files_modified:
  - lib/core/navigation/edges.cjs
autonomous: true
requirements: [CHOKE-02, CHOKE-03, CHOKE-05]
canon_parts: [6, 7, 8, 9]

must_haves:
  truths:
    - "A write suppressed by the confirmed-review-status guard now reports written:false while ok stays true"
    - "writeEdge degrades gracefully against a base openGraph handle instead of throwing table edges has no column named review_status"
    - "Every occurrence of the misleading cross-room-aggregation-forbidden comment in edges.cjs now describes the actual structural mechanism"
    - "The 43-file/77-call-site blast radius of writeEdge's ok field is unaffected: bash tests/run-all-125.sh still passes"
  artifacts:
    - path: "lib/core/navigation/edges.cjs"
      provides: "changes-aware writeEdge (C1/C2) plus the corrected cross-room fence comment (M2), all 11 sites"
      contains: "edgesHasReviewStatus"
  key_links:
    - from: "lib/core/navigation/edges.cjs::writeEdge"
      to: "tests/test-273-writeedge-changes-aware.cjs"
      via: "written/reason/ok return-shape assertions"
      pattern: "written"
    - from: "lib/core/navigation/edges.cjs::writeEdge"
      to: "tests/test-273-writeedge-base-schema.cjs"
      via: "review_status_persisted return-shape assertions against a base openGraph handle"
      pattern: "review_status_persisted"
    - from: "lib/core/navigation/edges.cjs (11 comment sites)"
      to: "tests/test-273-cross-room-comment.cjs"
      via: "source-text regex assertion"
      pattern: "Cross-room aggregation is prevented structurally"
---

<objective>
Land Phase 273's highest-value fix: make `writeEdge` (`lib/core/navigation/edges.cjs`)
`changes`-aware (C1) and add the `PRAGMA table_info(edges)` fallback that lets it degrade
gracefully against a base `lazygraph-ops.openGraph` handle (C2), per D-01/D-01a/D-01b. Same
plan, same file: sweep the misleading `Cross-room aggregation forbidden` comment at all 11
confirmed sites to describe the real structural mechanism (M2/D-04/D-04a).

This turns three of Phase 273's six Wave-0 RED tests GREEN:
`tests/test-273-writeedge-changes-aware.cjs`, `tests/test-273-writeedge-base-schema.cjs` (both
273-01), and `tests/test-273-cross-room-comment.cjs` (273-02). No test file in this plan changes
-- the fix must make the ALREADY-WRITTEN assertions pass, not the reverse.

**`ok` never flips to `false`.** This is the single most important constraint in this plan
(RESEARCH Pitfall 1): 77 `writeEdge(` call sites across 43 files, ~30 branching on `.ok`,
including `room-birth.cjs:948` rolling back an entire room birth on `!ok`. The fix is additive
only -- a new `written` field, a new `reason` value, a new `review_status_persisted` field.

**MANDATORY standing consult (CLAUDE.md):** `lib/core/navigation/edges.cjs` is exactly the class
of file the `icm-architect` skill is bound to as a standing consult (any work touching
`navigation.cjs`, `navigation/*.cjs`, or `room-db.cjs`). Task 1 below requires reading the skill
in full and checking this fix against its ten invariants, its six-forms taxonomy, and its walk
test BEFORE the writeEdge change is committed -- not as a passing mention.

Purpose: close C1 (silent write loss reported as success) and C2 (a schema mismatch that throws
instead of degrading), the two Criticals the code review named as the single highest-value
combined fix. Correct M2's misleading enforcement claim in the same pass since it shares the
file.

Output: `lib/core/navigation/edges.cjs` modified in place. No new files.
</objective>

## Artifacts this phase produces

Cumulative across all Phase 273 plans; this plan lands the rows marked 273-03.

| Artifact | Kind | Created by plan |
|---|---|---|
| `tests/run-all-273.sh`, both C1/C2 test files | files (harness + tests) | 273-01 |
| `tests/test-273-ingestion-allowlist.cjs`, `-cross-room-comment.cjs`, `-substrate-baseline-honest.cjs` | files (tests) | 273-02 |
| `edgesHasReviewStatus(db)` | function in `lib/core/navigation/edges.cjs` | **273-03** |
| `written` | boolean field on `writeEdge`'s success return | **273-03** |
| `reason: 'suppressed_by_confirmed'` | new reason value on an `ok: true` return | **273-03** |
| `review_status_persisted` | boolean field on `writeEdge`'s success return | **273-03** |
| Corrected fence comment (11 sites, exact phrase `Cross-room aggregation is prevented structurally`) | doc-comment text in `lib/core/navigation/edges.cjs` | **273-03** |
| `ALLOWED_EDGE_TYPES` require + inline guard, `rejectedEdgeTypes` | code + field in `lib/core/navigation/ingestion.cjs` | 273-04 |
| Substrate baseline reconciliation + REQUIREMENTS.md registration + research mirror | doc edits | 273-05 |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-CONTEXT.md
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-RESEARCH.md
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: icm-architect consult, then implement the writeEdge C1/C2 fix</name>
  <files>lib/core/navigation/edges.cjs</files>
  <read_first>
    - `~/.claude/skills/icm-architect/SKILL.md` (full) -- the ten invariants ("The invariants"
      section), the six-forms table ("Choose a mode" / "Pick the form"), and "The walk test"
      section. This phase's target files (`lib/core/navigation/edges.cjs`,
      `lib/core/navigation/ingestion.cjs`, `lib/core/room-db.cjs`) are the "System map" form
      (a body of code later agents will edit) -- read `~/.claude/skills/icm-architect/references/
      system-map.md` for that form's specific audit checklist before writing the fix.
    - `lib/core/navigation/edges.cjs` lines 750-846 (the `writeEdge` function itself, its
      preceding doc-comment block at 754-787, the closed try/catch INSERT at 833-841, the return
      at 842).
    - `lib/core/node-insert.cjs` lines 68-120 (`isMigratedSchema` + `insertNode`'s branch) -- the
      literal pattern this fix clones. Read the real file, not just the excerpt quoted in
      PATTERNS.md.
    - `tests/test-273-writeedge-changes-aware.cjs` and `tests/test-273-writeedge-base-schema.cjs`
      (273-01, on disk) -- read both in full. These are the fixed target: every assertion in
      them must pass after this task, unmodified.
    - `.planning/phases/273-.../273-RESEARCH.md` "D-01 fix design" (lines ~284-330), "Blast
      radius" (lines ~332-353), "Pitfall 1" and "Pitfall 2" (lines ~577-589).
    - `.planning/phases/273-.../273-PATTERNS.md` "File 1" (lines ~11-133) -- the exact code shape
      (the `edgesHasReviewStatus` helper, the branched INSERT, the additive return fields).
  </read_first>
  <behavior>
    - Behavior already pinned by the existing RED tests (do not re-derive; make these pass):
      a suppressed write on a migrated handle returns `written === false`,
      `reason === 'suppressed_by_confirmed'`, `ok === true`, and the stored row is genuinely
      unchanged. A fresh insert, a proposed-row update, and an identical-properties update all
      return `written === true`. A confirmed `NESTED_WITHIN` re-write never throws.
    - Against a base `openGraph` handle: `writeEdge` succeeds (`ok === true, written === true`)
      instead of throwing `table edges has no column named review_status`; the row lands; a
      `review_status: 'proposed'` write on the base schema returns
      `review_status_persisted === false` (signalled, not silently dropped, not thrown -- D-06);
      the same write on a migrated handle still returns `review_status_persisted === true` and
      the column genuinely holds `'proposed'`.
  </behavior>
  <action>
First, perform the icm-architect consult and record the outcome. Read the skill in full per
`read_first` above. Check the planned `writeEdge` change against: invariant 1 (one folder/one
job -- `edgesHasReviewStatus` must be a narrowly-scoped sibling probe function, not logic folded
into `writeEdge` itself, mirroring `node-insert.cjs`'s existing separation of `isMigratedSchema`
from `insertNode`); invariant 4 (every contract is explicit -- the doc-comment block immediately
above `writeEdge`, lines 754-787, currently describes a return shape that does not include
`written`/`review_status_persisted`; that comment must be updated to describe the new contract,
not left describing stale behavior); invariant 8 (one home per fact -- `edgesHasReviewStatus`
must mirror `node-insert.cjs::isMigratedSchema`'s SHAPE without duplicating its MEANING, since
they probe different tables for different columns; `ALLOWED_EDGE_TYPES` stays exported exactly
once from this file, unchanged by this task); invariant 9 (state is derivable, not
hand-maintained -- `written`/`review_status_persisted` must be computed fresh from
`runResult.changes` and the PRAGMA probe on every call, never cached or hand-set). Also confirm
which of the six forms `lib/core/navigation/` itself is (System map: code later agents edit) and
run the walk test's "is any fact stored in two places" check against the corrected M2 comment
text this plan's Task 2 will write (one canonical sentence, not 11 independently-drifting
copies). Append one sentence to this task's commit message or the plan's eventual SUMMARY.md
recording the consult's outcome: either "no gap found" or the specific gap found and how it was
fixed as part of this task -- a consult that is never recorded did not happen.

Then implement the fix in `lib/core/navigation/edges.cjs`, following 273-PATTERNS.md File 1
exactly. Add the `edgesHasReviewStatus(db)` helper function immediately above `writeEdge`,
probing `PRAGMA table_info(edges)` for a column named `review_status`, defensively returning
`false` on any probe failure (matching `node-insert.cjs::isMigratedSchema`'s defensive-false
precedent). Inside `writeEdge`'s existing try block (currently the single hard-coded 5-column
INSERT at lines 833-838), branch on `edgesHasReviewStatus(db)`: when true, keep the existing
5-column `INSERT ... ON CONFLICT ... WHERE edges.review_status IS NOT 'confirmed'` statement;
when false, use a 4-column INSERT (`source, target, type, properties`) with
`ON CONFLICT(source, target, type) DO UPDATE SET properties = excluded.properties` and NO
`WHERE` guard (the column it would reference does not exist on this schema, so nothing can be
suppressed on this branch -- every write here reports `written: true`). Capture the statement's
`.run(...)` return value into a local (`runResult`) instead of discarding it. Compute
`written = Number(runResult.changes) > 0`. Build the return object additively: keep every
existing field (`ok: true, edge_id: edgeId, type: edge_type, source: source_id,
target: target_id`), add `written`, add `reason: 'suppressed_by_confirmed'` only when
`written === false`, and add `review_status_persisted: false` only when the caller supplied a
non-null `reviewStatusValue` AND `edgesHasReviewStatus(db)` is false (per D-06 -- never silently
mask the gap). Update the doc-comment block above `writeEdge` (lines 754-787) to describe this
new return contract in the same voice as the existing comments (explain WHY, reference the
Phase/Canon Part, per PATTERNS.md's "Conventions to follow" note). `module.exports` is
unchanged.
  </action>
  <acceptance_criteria>
    - `node tests/test-273-writeedge-changes-aware.cjs` exits 0, printing
      `PASS test-273-writeedge-changes-aware`.
    - `node tests/test-273-writeedge-base-schema.cjs` exits 0, printing
      `PASS test-273-writeedge-base-schema`.
    - `bash tests/run-all-125.sh` exits 0 (the 9 existing writeEdge scenarios and the 43-file
      blast-radius regression guard still pass -- proves `ok` was not flipped).
    - `node tests/test-200-graph-chokepoint.cjs` exits 0.
    - `node tests/test-224-backfill-idempotent.cjs` exits 0 and `node tests/test-224-proposed-only.cjs` exits 0.
    - `grep -c "review_status_persisted" lib/core/navigation/edges.cjs` is at least 1.
    - `grep -c "written" lib/core/navigation/edges.cjs` is at least 1 (the field is actually
      returned, not just described in prose).
    - `grep -c "edgesHasReviewStatus" lib/core/navigation/edges.cjs` is at least 2 (the function
      is both defined and called).
    - The plan's eventual SUMMARY.md records the icm-architect consult outcome in one sentence.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' lib/core/navigation/edges.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; node tests/test-273-writeedge-changes-aware.cjs && node tests/test-273-writeedge-base-schema.cjs && bash tests/run-all-125.sh && node tests/test-200-graph-chokepoint.cjs && node tests/test-224-backfill-idempotent.cjs && node tests/test-224-proposed-only.cjs && echo "ALL GREEN"'</automated>
  </verify>
  <done>writeEdge is changes-aware with the PRAGMA fallback; both C1/C2 RED tests are now GREEN; the 43-file blast-radius regression set (run-all-125.sh, test-200, test-224 x2) is green; ok semantics are unchanged; the doc comment above writeEdge describes the new contract; the icm-architect consult is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the cross-room fence comment at all 11 sites (M2/D-04/D-04a)</name>
  <files>lib/core/navigation/edges.cjs</files>
  <read_first>
    - `lib/core/navigation/edges.cjs` -- grep the whole file (post-Task-1 state) for the literal
      substring `Cross-room aggregation forbidden` to confirm all 11 sites before editing. Task 1
      only touches `writeEdge` (lines 790-846) and its immediately preceding doc-comment block;
      all 11 comment sites (lines 45, 64, 236, 269, 366, 419, 583, 629, 667, 705, 746 per
      273-RESEARCH.md's citation table) sit above line 790 and are unaffected by Task 1's edits,
      so these line numbers remain accurate. Confirm by grep rather than trusting the numbers
      blind, since Task 1 may have shifted content slightly if the doc-comment rewrite changed
      line counts.
    - `.planning/phases/273-.../273-RESEARCH.md` "D-04 scope correction" note (lines ~164-172).
    - `.planning/phases/273-.../273-PATTERNS.md` "File 2" (lines ~135-169) -- the exact corrected
      wording.
    - `tests/test-273-cross-room-comment.cjs` (273-02, on disk) -- read the exact assertion
      strings it checks. This task's edit must match that file's asserted phrase
      `Cross-room aggregation is prevented structurally` verbatim -- the test was written first
      and is authoritative for the exact wording, not this task's own judgment.
  </read_first>
  <action>
Replace the misleading phrase at all 11 confirmed locations in `lib/core/navigation/edges.cjs`
with the corrected structural-mechanism wording from 273-PATTERNS.md File 2, applied identically
at every site (same sentence, not reworded per-site, per D-04a's explicit "sweep all 11...it is
the same edit repeated"). The corrected wording explains the actual mechanism: `writeEdge(db,
params)` only ever holds the one db handle its caller passed in, so it is physically incapable
of opening or writing a second room's db; no runtime assertion exists because no code path has
been found or hypothesized where a handle is swapped mid-call (cite 273-RESEARCH.md D-04 in the
comment, matching PATTERNS File 2's exact text). Before editing, grep-count the old phrase to
confirm exactly 11 occurrences; after editing, grep-count both phrases to confirm 0 old and at
least 11 new.
  </action>
  <acceptance_criteria>
    - `grep -c "Cross-room aggregation forbidden" lib/core/navigation/edges.cjs` returns exactly 0.
    - `grep -c "Cross-room aggregation is prevented structurally" lib/core/navigation/edges.cjs`
      returns at least 11.
    - `node tests/test-273-cross-room-comment.cjs` exits 0, printing
      `PASS test-273-cross-room-comment`.
    - `bash tests/run-all-125.sh` still exits 0 (comment-only change, zero runtime behavior
      touched -- this is a pure regression guard that nothing else broke).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' lib/core/navigation/edges.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; old=$(grep -c "Cross-room aggregation forbidden" lib/core/navigation/edges.cjs || true); new=$(grep -c "Cross-room aggregation is prevented structurally" lib/core/navigation/edges.cjs || true); [ "$old" -eq 0 ] || { echo "OLD PHRASE STILL PRESENT: $old"; exit 1; }; [ "$new" -ge 11 ] || { echo "NEW PHRASE UNDERCOUNT: $new"; exit 1; }; node tests/test-273-cross-room-comment.cjs && echo "ALL GREEN"'</automated>
  </verify>
  <done>All 11 sites corrected to the exact structural-mechanism wording tests/test-273-cross-room-comment.cjs asserts; zero occurrences of the old misleading phrase remain; the D-04 test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-owned db handle -> `writeEdge` | The handle's provenance (`openRoomDb` migrated vs `openGraph` base) is unknowable to the callee; schema shape is untrusted input to the write site. This is exactly what C2's fix probes for. |
| `writeEdge` -> 43 caller files / 77 call sites | The return-contract change is additive-only. This boundary is the blast radius this plan must not cross with a breaking change. |
| doc comment -> actual code | M2's whole defect class: a comment claiming an enforcement that does not exist in code. This plan's Task 2 closes that gap by correcting the claim to match the real (structural) mechanism, rather than adding code to match the false claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-273-08 | Repudiation | `writeEdge`'s silent-discard-as-success (C1) | mitigate | This IS the phase's core fix. `written`/`reason` make a suppressed write observable instead of indistinguishable from a real one. |
| T-273-09 | Denial of Service (schema mismatch) | `writeEdge` against an `openGraph` base handle (C2) | mitigate | The PRAGMA fallback replaces a thrown exception with a graceful degrade; `review_status_persisted:false` signals the gap rather than silently dropping it (D-06) or crashing the caller. |
| T-273-10 | Elevation of Privilege | The `confirmed_requires_by_user` access-control gate (`edges.cjs:818-821`) | accept (already correct, do not weaken) | The C1 fix must not touch this gate's logic -- the suppression it causes IS the control working correctly; the fix only makes that suppression visible. Verified: Task 1's action does not modify lines 790-821. |
| T-273-11 | Repudiation | The 11-site misleading fence comment (M2) | mitigate | Corrected to describe the actual structural guarantee (function signature `(db, params)`), not a checked runtime invariant that does not exist. No new runtime assertion added -- D-04 explicitly scopes this to a documentation fix, since no failure mode has been reproduced or hypothesized for a handle swap mid-call. |
| T-273-SC | Tampering | npm / pip / cargo installs | n/a | This plan installs nothing. |
</threat_model>

<verification>
1. `node tests/test-273-writeedge-changes-aware.cjs` exits 0.
2. `node tests/test-273-writeedge-base-schema.cjs` exits 0.
3. `node tests/test-273-cross-room-comment.cjs` exits 0.
4. `bash tests/run-all-125.sh` exits 0 (43-file blast-radius regression guard).
5. `node tests/test-200-graph-chokepoint.cjs`, `node tests/test-224-backfill-idempotent.cjs`, `node tests/test-224-proposed-only.cjs` all exit 0.
6. `grep -c "Cross-room aggregation forbidden" lib/core/navigation/edges.cjs` returns 0.
7. `LC_ALL=C.UTF-8 grep -cP '\x{2014}' lib/core/navigation/edges.cjs` returns 0.
8. `git diff --stat` shows exactly one file changed: `lib/core/navigation/edges.cjs`.
</verification>

<success_criteria>
- `writeEdge` is changes-aware; C1 and C2 are closed without flipping `ok`'s meaning anywhere.
- All 11 misleading cross-room-fence comment occurrences are corrected to the same structural wording the D-04 test asserts.
- Three of Phase 273's six Wave-0 RED tests are now GREEN: writeedge-changes-aware, writeedge-base-schema, cross-room-comment.
- The 43-file/77-call-site blast-radius regression set is unbroken.
- The icm-architect consult ran and its outcome is recorded.
- Zero em-dashes introduced.
</success_criteria>

<output>
Create `.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-03-SUMMARY.md` when done.
Record the icm-architect consult's outcome verbatim (no gap found, or the specific gap and fix)
and the exact GREEN confirmation lines from all three now-passing test files.
</output>
