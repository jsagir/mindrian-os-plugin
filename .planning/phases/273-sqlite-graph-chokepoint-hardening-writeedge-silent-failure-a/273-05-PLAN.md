---
phase: 273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a
plan: 05
type: execute
wave: 3
depends_on: ["273-02", "273-03", "273-04"]
files_modified:
  - docs/architecture/SUBSTRATE-BASELINE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CHOKE-01, CHOKE-02, CHOKE-03, CHOKE-04, CHOKE-05, CHOKE-06]
canon_parts: [6, 7, 8, 9]

must_haves:
  truths:
    - "The documented substrate baseline equals the live measured baseline, with an honest explanation of why the C1/C2/C3 fixes did or did not move the count"
    - "All 5 Phase 273 test files are green and the aggregator itself exits 0"
    - "The full 43-file blast-radius regression set plus the acceptance rollup are green"
    - "CHOKE-01 through CHOKE-06 are registered in .planning/REQUIREMENTS.md with measured evidence per row"
    - "Phase 273's research trail is mirrored into the rethinking-mindrianos and mindrianOS/research dev-consult rooms, cross-linked back to the phase"
  artifacts:
    - path: "docs/architecture/SUBSTRATE-BASELINE.md"
      provides: "Dated 2026-08-31 re-measurement section reconciling the 195-documented / 208-measured drift"
      contains: "2026-08-31 re-measurement"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 273 section registering CHOKE-01..06 with measured evidence"
      contains: "CHOKE-01"
  key_links:
    - from: "docs/architecture/SUBSTRATE-BASELINE.md"
      to: "tests/test-273-substrate-baseline-honest.cjs"
      via: "the dated section's Result: **N** line, parsed by the test's regex"
      pattern: "Result: \\*\\*\\d+\\*\\*"
    - from: ".planning/REQUIREMENTS.md Phase 273 section"
      to: "CHOKE-01 through CHOKE-06"
      via: "checkbox rows citing measured test/command evidence"
      pattern: "CHOKE-0[1-6]"
---

<objective>
Close Phase 273. Three things, in dependency order: (1) re-measure the substrate baseline now
that C1/C2/C3 have landed (273-03, 273-04) and append the honest reconciliation note D-05
requires -- turning Phase 273's sixth and final Wave-0 RED test GREEN
(`tests/test-273-substrate-baseline-honest.cjs`, 273-02); (2) run the complete phase-gate
regression set and register CHOKE-01 through CHOKE-06 in `.planning/REQUIREMENTS.md`, following
the exact precedent the Phase 266/267.3/270 sections in that file already set; (3) mirror this
phase's research trail to the `rethinking-mindrianos` Data Room per CLAUDE.md's Dev-Research
Compositing rule -- named in 273-RESEARCH.md as "an execution-time task, not a research-time
one" but never assigned to a specific plan until now.

**Expect 208, unchanged, not a reduction.** 273-RESEARCH.md proved this two independent ways by
direct execution before any fix landed: `lib/core/navigation/` (where both `edges.cjs` and
`ingestion.cjs` live) is path-allowlisted in `check-substrate.cjs` at line 70, AND the guard's
`RE_RAW_WRITE` regex does not match `INSERT OR IGNORE INTO` syntax at all. C3's fix was
structurally incapable of moving this count regardless of fix quality. Task 1 below re-runs the
measurement live and records whatever the ACTUAL number is -- do not assume 208 without running
the check; the prediction is strong evidence, not a substitute for measuring.

This plan depends on 273-02 (needs the D-05 RED test), 273-03 (C1/C2 must have landed before
re-measuring), and 273-04 (C3 must have landed before re-measuring) -- all three are prerequisites
to an honest re-measurement, not just to the test file's existence.

Purpose: close the loop D-05 opened (burn debt down before touching the baseline number, then
document the honest post-fix reality) and make Phase 273's six requirement IDs a permanent,
auditable part of the project's requirements ledger, per Canon Part 6 dog-fooding.

Output: `docs/architecture/SUBSTRATE-BASELINE.md` and `.planning/REQUIREMENTS.md` modified in
place, plus one new file each under `~/MindrianRooms/rethinking-mindrianos/research/` and
`~/MindrianOS/research/` (outside this repo's git tree -- not part of `files_modified` above,
which tracks only this repo's dependency graph).
</objective>

## Artifacts this phase produces

Complete cumulative list across all five Phase 273 plans.

| Artifact | Kind | Created by plan |
|---|---|---|
| `tests/run-all-273.sh` | file (bash aggregator) | 273-01 |
| `tests/test-273-writeedge-changes-aware.cjs` | file (test) | 273-01 |
| `tests/test-273-writeedge-base-schema.cjs` | file (test) | 273-01 |
| `tests/test-273-ingestion-allowlist.cjs` | file (test) | 273-02 |
| `tests/test-273-cross-room-comment.cjs` | file (test) | 273-02 |
| `tests/test-273-substrate-baseline-honest.cjs` | file (test) | 273-02 |
| `edgesHasReviewStatus(db)`, `written`, `review_status_persisted`, corrected fence comment (11 sites) | fix in `lib/core/navigation/edges.cjs` | 273-03 |
| `ALLOWED_EDGE_TYPES` require + inline guard, `rejectedEdgeTypes` | fix in `lib/core/navigation/ingestion.cjs` | 273-04 |
| `## 2026-08-31 re-measurement (Phase 273)` | heading in `docs/architecture/SUBSTRATE-BASELINE.md` | **273-05** |
| `Result: **N** violations` | machine-parsed line under that heading | **273-05** |
| `### Phase 273` requirements section | heading in `.planning/REQUIREMENTS.md` | **273-05** |
| `CHOKE-01` .. `CHOKE-06` | registered requirement IDs | **273-05** |
| Dev-research mirror entry (2 copies, cross-linked) | files under `~/MindrianRooms/rethinking-mindrianos/research/` and `~/MindrianOS/research/` | **273-05** |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-CONTEXT.md
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-RESEARCH.md
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: M4/D-05 substrate baseline reconciliation</name>
  <files>docs/architecture/SUBSTRATE-BASELINE.md</files>
  <read_first>
    - `docs/architecture/SUBSTRATE-BASELINE.md` lines 1-40 (the "Status: INFORMATIONAL" header,
      the 195-violation headline claim) and lines 260-292 (the file's current end -- no dated
      re-measurement section exists yet, and the existing "## Summary" section's prose style to
      match).
    - `.planning/phases/273-.../273-RESEARCH.md` "Priority 2" section (lines ~355-423,
      `check-substrate.cjs` mechanics) and its "D-05: the premise is falsified" subsection (lines
      ~394-422, the recommended task shape).
    - `.planning/phases/273-.../273-PATTERNS.md` "File 4" (lines ~267-309) -- the exact markdown
      to append, including the literal heading and `Result: **N**` line shape.
    - `tests/test-273-substrate-baseline-honest.cjs` (273-02, on disk) -- read in full. Its regex
      `/## 2026-08-31 re-measurement[\s\S]*?Result: \*\*(\d+)\*\*/` is authoritative for the
      exact heading text and `Result:` line format this task must produce.
  </read_first>
  <action>
Run `node scripts/check-substrate.cjs --baseline` live and capture the actual `Found N
violation(s)` count plus its per-rule breakdown. Do not assume the number is 208 -- read it from
the live command output. Append (never replace or regenerate -- no generator script exists for
this file, and the per-row phase-ownership assignments elsewhere in the doc are hand-curated) a
new dated section at the end of `docs/architecture/SUBSTRATE-BASELINE.md`, heading exactly
`## 2026-08-31 re-measurement (Phase 273)`, following 273-PATTERNS.md File 4's exact structure:
state the measured result as `Result: **N**` (substituting the live-measured number), explain
that this is expected rather than a regression, cite the two independent reasons
(`lib/core/navigation/` path-allowlisted at `check-substrate.cjs:70`; `RE_RAW_WRITE` does not
match `INSERT OR IGNORE INTO`), state that Phase 273's C3 fix was therefore structurally
incapable of moving this count regardless of fix quality, and name the remaining delta from the
previously-documented 195 as owned by the explicitly-deferred fast-follow phase (C4/M5-M8
propagation) and the M3 regex-hole fix, both out of this phase's scope per D-02. Do NOT touch
the M3 regex in `check-substrate.cjs` in this task -- deferred, and widening it here would change
the count and make this reconciliation note uninterpretable.
  </action>
  <acceptance_criteria>
    - `node tests/test-273-substrate-baseline-honest.cjs` exits 0, printing
      `PASS test-273-substrate-baseline-honest (measured=N)`.
    - `grep -c "2026-08-31 re-measurement" docs/architecture/SUBSTRATE-BASELINE.md` returns
      exactly 1.
    - The appended section's `Result: **N**` number matches the live
      `node scripts/check-substrate.cjs --baseline` output's `Found N violation(s)` line exactly
      (re-run both and diff the numbers as part of verification, do not trust a cached number).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' docs/architecture/SUBSTRATE-BASELINE.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; live=$(node scripts/check-substrate.cjs --baseline 2>&1 | grep -oP "Found \K[0-9]+" | head -1); doc=$(grep -A2 "2026-08-31 re-measurement" docs/architecture/SUBSTRATE-BASELINE.md | grep -oP "Result: \*\*\K[0-9]+"); [ "$live" = "$doc" ] || { echo "MISMATCH: live=$live doc=$doc"; exit 1; }; node tests/test-273-substrate-baseline-honest.cjs; echo "RECONCILED: $live"'</automated>
  </verify>
  <done>The dated re-measurement section exists with the live-measured number, the D-05 test is GREEN, and the note honestly explains why the count did or did not move.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase-gate regression sweep + register CHOKE-01..06 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/273-.../273-VALIDATION.md` "Phase gate" tier -- the complete command list
      required before this phase can be called done.
    - `.planning/REQUIREMENTS.md` -- read the existing `### Phase 266`, `### Phase 267.3`, and
      `### Phase 270` sections in full (they are the three most recent phase-local-ID precedents)
      to match their exact heading format (`### Phase N - Title (minted at plan time DATE)`),
      checkbox-row shape (`- [x] **ID-NN**: behavior description. Measured: evidence.`), and the
      "Traceability" section's running total this task must update.
  </read_first>
  <action>
Run the complete Phase 273 gate: `bash tests/run-all-273.sh` (expect all 5 discovered test files
GREEN, `PASS=5 FAIL=0`, the aggregator itself exits 0 -- the correct fully-landed state, a
reversal of every prior plan's RED-by-design result), `bash tests/run-all-125.sh`,
`node tests/test-200-graph-chokepoint.cjs`, `node tests/test-224-backfill-idempotent.cjs`,
`node tests/test-224-proposed-only.cjs`, `node scripts/doctor.cjs --acceptance`, and
`node scripts/check-substrate.cjs --diff` (must exit 0). Record the actual output of each.

Then append a new `### Phase 273 - SQLite Graph Chokepoint Hardening (writeEdge silent-failure +
propagation-gap fixes) (minted at plan time 2026-08-31)` section to `.planning/REQUIREMENTS.md`,
placed after the existing `### Phase 267.3` section (matching the file's existing chronological
ordering), following the exact precedent format of the Phase 266/267.3/270 sections: a one-line
provenance note (these six IDs were minted in `273-01-PLAN.md`'s frontmatter, finalized to this
canonical one-per-fix-dimension mapping in `273-02-PLAN.md`'s objective, and are scoped to Phase
273 only), then six checkbox rows, each `[x]`, each citing the specific measured evidence from
this task's own regression run or an earlier plan's SUMMARY:
- **CHOKE-01**: the Wave 0 harness (glob discovery, hard-fail-on-zero-discovery). Evidence:
  273-01, `tests/run-all-273.sh`.
- **CHOKE-02**: C1, writeEdge changes-aware. Evidence: `node tests/test-273-writeedge-changes-
  aware.cjs` PASS (273-03).
- **CHOKE-03**: C2, PRAGMA fallback + review_status_persisted. Evidence: `node tests/test-273-
  writeedge-base-schema.cjs` PASS (273-03).
- **CHOKE-04**: C3, inline ALLOWED_EDGE_TYPES guard in ingestion.cjs. Evidence: `node tests/
  test-273-ingestion-allowlist.cjs` PASS (273-04).
- **CHOKE-05**: M2, cross-room fence comment corrected at all 11 sites. Evidence: `node tests/
  test-273-cross-room-comment.cjs` PASS (273-03).
- **CHOKE-06**: M4/D-05, substrate baseline reconciled. Evidence: `node tests/test-273-
  substrate-baseline-honest.cjs` PASS, the live-measured number from this plan's Task 1 (273-05).

Finally, update the file's "Traceability" section: add "plus CHOKE-01..06 (Phase 273)" to the
running list of phase-local ID families, and add 6 to the total active-requirement count.
  </action>
  <acceptance_criteria>
    - `bash tests/run-all-273.sh` reports `FAIL=0`.
    - `bash tests/run-all-125.sh`, `node tests/test-200-graph-chokepoint.cjs`,
      `node tests/test-224-backfill-idempotent.cjs`, `node tests/test-224-proposed-only.cjs`,
      `node scripts/doctor.cjs --acceptance`, `node scripts/check-substrate.cjs --diff` all
      exit 0.
    - `.planning/REQUIREMENTS.md` contains a `### Phase 273` heading with exactly 6 checkbox rows
      for CHOKE-01 through CHOKE-06, all `[x]`, each citing a specific test file or command as
      evidence.
    - `.planning/REQUIREMENTS.md`'s "Traceability" section text is updated to include the 6 new
      IDs in its running total.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' .planning/REQUIREMENTS.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; bash tests/run-all-273.sh; bash tests/run-all-125.sh; node tests/test-200-graph-chokepoint.cjs; node tests/test-224-backfill-idempotent.cjs; node tests/test-224-proposed-only.cjs; node scripts/doctor.cjs --acceptance; node scripts/check-substrate.cjs --diff; c=$(grep -c "CHOKE-0[1-6]" .planning/REQUIREMENTS.md); [ "$c" -ge 6 ] || { echo "only $c CHOKE rows found"; exit 1; }; echo "PHASE GATE GREEN"'</automated>
  </verify>
  <done>The full Phase 273 test suite is green, the wave-merge and phase-gate regression sets pass, and CHOKE-01 through CHOKE-06 are registered in REQUIREMENTS.md with measured evidence per row.</done>
</task>

<task type="auto">
  <name>Task 3: Dev-Research Compositing mirror to rethinking-mindrianos + mindrianOS/research</name>
  <files>none in this repo (creates files under ~/MindrianRooms/rethinking-mindrianos/research/ and ~/MindrianOS/research/)</files>
  <read_first>
    - `CLAUDE.md`'s "Dev-Research Compositing (Rethinking Room)" section -- the standing rule:
      every GSD phase touching MindrianOS's own architecture composites research in BOTH the
      phase's own CONTEXT.md AND `rethinking-mindrianos/research/<dated-entry>/`, mirrored to
      `mindrianOS/research/` as source-of-record, cross-referenced back to the phase.
    - `~/MindrianRooms/rethinking-mindrianos/research/2026-08-27-icm-semantic-substrate/2026-08-27-icm-semantic-substrate.md`
      lines 1-40 -- the exact convention to follow: a dated folder
      (`research/<slug>/<slug>.md`), YAML frontmatter (`methodology: research`, `title`,
      `created`, `status: active`, `room_section: research`), then a "Verified, not taken on
      trust" style body that distinguishes what was independently confirmed by execution from
      what was merely cited.
    - `.planning/phases/273-.../273-RESEARCH.md` in full (already the primary source -- this
      task summarizes and cross-links it, it does not re-derive it) and
      `.planning/phases/273-.../273-CONTEXT.md`'s `<decisions>` section (D-01 through D-06).
    - The live output of this plan's Task 1 (the actual measured substrate count) and Task 2
      (the actual regression-suite results) -- the mirror entry should report Phase 273's REAL
      outcome, not the pre-execution prediction.
  </read_first>
  <action>
Create one dated research entry at
`~/MindrianRooms/rethinking-mindrianos/research/2026-08-31-sqlite-graph-chokepoint-hardening-273/2026-08-31-sqlite-graph-chokepoint-hardening-273.md`,
following the frontmatter and body convention read above. Title names the core finding in one
sentence (a chokepoint that reports success while the write silently discarded, closed by an
additive changes-aware contract, plus the schema-mismatch and allowlist-bypass fixes landed in
the same phase). Body sections: what was verified by direct execution (cite the C1/C2/C3
reproduction transcripts from 273-RESEARCH.md, and this plan's own live substrate re-measurement
number from Task 1), the decisive design calls worth remembering across future phases (the
additive-field-not-flipped-ok pattern from D-01a, given the 43-file/77-call-site blast radius;
the inline-allowlist-not-writeEdge-routing call from D-03a, given the INSERT-OR-IGNORE-vs-
ON-CONFLICT-DO-UPDATE semantics trap), and an explicit cross-link back to
`.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/` in this
repo. Also name the SAG-paper connection 273-RESEARCH.md's Sources section already made
(arXiv 2606.15971v1, "chokepoint reports success while the actual data never moves" as a
recurring pattern, not a one-off bug) since that connection was made during research but not yet
composited to the room.

Mirror the SAME file content to
`~/MindrianOS/research/2026-08-31-sqlite-graph-chokepoint-hardening-273/2026-08-31-sqlite-graph-chokepoint-hardening-273.md`
(the source-of-record mirror CLAUDE.md's Dev-Research Compositing section requires), identical
frontmatter and body -- one fact, two homes, not two independently-drifting write-ups.
  </action>
  <acceptance_criteria>
    - `test -f ~/MindrianRooms/rethinking-mindrianos/research/2026-08-31-sqlite-graph-chokepoint-hardening-273/2026-08-31-sqlite-graph-chokepoint-hardening-273.md`
      succeeds.
    - `test -f ~/MindrianOS/research/2026-08-31-sqlite-graph-chokepoint-hardening-273/2026-08-31-sqlite-graph-chokepoint-hardening-273.md`
      succeeds.
    - Both files are byte-identical (`diff` reports no differences).
    - The file's frontmatter contains `methodology: research` and `room_section: research`.
    - The body contains the literal string
      `273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a` (the cross-link back to
      this repo's phase).
    - The body contains `arXiv 2606.15971` (the SAG-paper cross-reference).
  </acceptance_criteria>
  <verify>
    <automated>bash -c "A=$HOME/MindrianRooms/rethinking-mindrianos/research/2026-08-31-sqlite-graph-chokepoint-hardening-273/2026-08-31-sqlite-graph-chokepoint-hardening-273.md; B=$HOME/MindrianOS/research/2026-08-31-sqlite-graph-chokepoint-hardening-273/2026-08-31-sqlite-graph-chokepoint-hardening-273.md; test -f \"\$A\" || { echo MISSING_A; exit 1; }; test -f \"\$B\" || { echo MISSING_B; exit 1; }; diff \"\$A\" \"\$B\" || { echo MIRROR_DRIFT; exit 1; }; grep -q 273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a \"\$A\" || { echo NO_CROSS_LINK; exit 1; }; echo MIRROR_OK"</automated>
  </verify>
  <done>Both mirror copies exist, are byte-identical, cross-link back to the phase, and cite the SAG-paper connection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation vs. measured reality | The whole M4/D-05 defect class: a stale documented number silently diverging from what the guard actually measures. Task 1 closes this by re-measuring live and recording the truth, not by asserting a number in advance. |
| `.planning/REQUIREMENTS.md` vs. actually-shipped behavior | Registering CHOKE-01..06 with measured evidence per row (not just checkboxes) keeps the requirements ledger honest -- the same Canon Part 6 dog-fooding discipline the whole phase enforces on the codebase, applied to the project's own tracking. |
| this repo vs. the `rethinking-mindrianos` / `mindrianOS` rooms | Cross-repo boundary. Task 3 writes outside this git tree; nothing in this repo's dependency graph or pre-commit hooks governs those paths, so the acceptance criteria check them directly by path, not via this repo's test suite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-273-15 | Repudiation | The substrate baseline document's own claim | mitigate | Task 1's live re-measurement plus the self-detecting test (`test-273-substrate-baseline-honest.cjs`) close this permanently -- any future drift between the doc and the guard fails loudly at test time, not silently. |
| T-273-16 | Repudiation | `.planning/REQUIREMENTS.md`'s claim that CHOKE-01..06 are done | mitigate | Every row cites specific measured evidence (a test file's PASS output or a command's exit code), not an unverified checkbox. |
| T-273-17 | Tampering | Cross-repo mirror files (Task 3) | accept | Both mirror files live in the user's own local Data Room directories, not a shared or remote surface; no Brain call, no network egress (Canon Part 8 holds -- this is local file authoring only). |
| T-273-SC | Tampering | npm / pip / cargo installs | n/a | This plan installs nothing. |
</threat_model>

<verification>
1. `bash tests/run-all-273.sh` reports `FAIL=0` -- all 5 Phase 273 test files green.
2. `bash tests/run-all-125.sh`, `node tests/test-200-graph-chokepoint.cjs`, `node tests/test-224-backfill-idempotent.cjs`, `node tests/test-224-proposed-only.cjs` all exit 0.
3. `node scripts/doctor.cjs --acceptance` exits 0.
4. `node scripts/check-substrate.cjs --diff` exits 0.
5. `docs/architecture/SUBSTRATE-BASELINE.md` contains exactly one `2026-08-31 re-measurement` section whose `Result: **N**` matches the live `--baseline` count.
6. `.planning/REQUIREMENTS.md` contains a `### Phase 273` section with 6 `[x]` CHOKE rows, each with measured evidence.
7. Both dev-research mirror files exist and are byte-identical, cross-linked back to the phase.
8. `git diff --stat` (this repo) shows exactly two files changed: `docs/architecture/SUBSTRATE-BASELINE.md` and `.planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
- Phase 273 is fully closed: all 5 Wave-0 tests green, the aggregator exits 0, the 43-file blast-radius regression set and the acceptance rollup are green.
- The substrate baseline is honestly reconciled -- documented equals measured, with the reason recorded, not a laundered number.
- CHOKE-01 through CHOKE-06 are permanently registered in `.planning/REQUIREMENTS.md` with measured evidence.
- Phase 273's research trail is composited into both dev-consult room homes, cross-linked back to the phase, per CLAUDE.md's standing Dev-Research Compositing rule -- closing the gap 273-01's own SUMMARY.md flagged as not yet actioned.
- Zero em-dashes introduced anywhere.
</success_criteria>

<output>
Create `.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-05-SUMMARY.md` when done.
Record the final live substrate count, the full regression-suite results, and the two mirror
file paths. This SUMMARY is Phase 273's closing record.
</output>
