---
phase: 273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-273-ingestion-allowlist.cjs
  - tests/test-273-cross-room-comment.cjs
  - tests/test-273-substrate-baseline-honest.cjs
autonomous: true
requirements: [CHOKE-04, CHOKE-05, CHOKE-06]
canon_parts: [6, 7, 8, 9]

must_haves:
  truths:
    - "A failing-by-design test proves Brain-supplied edge types with no allowlist match land in the local graph unfiltered today"
    - "A failing-by-design test proves the cross-room-aggregation-forbidden comment still makes an unchecked-invariant claim at all 11 sites in edges.cjs"
    - "A failing-by-design test proves SUBSTRATE-BASELINE.md carries no honest re-measurement record yet"
  artifacts:
    - path: "tests/test-273-ingestion-allowlist.cjs"
      provides: "C3 regression guard: out-of-allowlist Brain edge types rejected and observable via rejectedEdgeTypes, INSERT OR IGNORE semantics preserved"
      contains: "rejectedEdgeTypes"
    - path: "tests/test-273-cross-room-comment.cjs"
      provides: "D-04 source-assert: zero occurrences of the misleading fence comment, at least 11 occurrences of the corrected structural wording"
      contains: "Cross-room aggregation is prevented structurally"
    - path: "tests/test-273-substrate-baseline-honest.cjs"
      provides: "D-05 checkpoint: measured substrate violation count equals the documented baseline, self-detecting drift"
      contains: "scanRepo"
  key_links:
    - from: "tests/test-273-ingestion-allowlist.cjs"
      to: "lib/core/navigation/ingestion.cjs::storeBrainSuggestions"
      via: "require + direct call with a hand-constructed packetResult"
      pattern: "storeBrainSuggestions\\(db"
    - from: "tests/test-273-cross-room-comment.cjs"
      to: "lib/core/navigation/edges.cjs"
      via: "fs.readFileSync source-text regex assertion"
      pattern: "Cross-room aggregation"
    - from: "tests/test-273-substrate-baseline-honest.cjs"
      to: "scripts/check-substrate.cjs::scanRepo + docs/architecture/SUBSTRATE-BASELINE.md"
      via: "require(scanRepo) + fs.readFileSync(doc) + regex extraction of the dated re-measurement section"
      pattern: "scanRepo\\(\\)"
---

<objective>
Close out Phase 273's Wave 0 by building the final three RED-by-design tests: C3 (Brain
edge-type allowlist bypass in `ingestion.cjs`), D-04 (the cross-room comment sweep in
`edges.cjs`), and D-05 (the substrate baseline honesty checkpoint). 273-01 already built
`tests/run-all-273.sh` plus the C1/C2 tests; this plan completes the six-test Wave 0 set that
273-RESEARCH.md's Validation Architecture names as required before any production fix lands.

**RED is the correct outcome of every test in this plan.** All three must fail today. A green
run means the test is not asserting the defect it claims to pin.

**Requirement ID scheme, now finalized (supersedes 273-01's exploratory range):** this phase
registers exactly six requirement IDs, CHOKE-01 through CHOKE-06, one per fix dimension:
CHOKE-01 = the Wave 0 test/verification harness (glob discovery + hard-fail-on-zero-discovery,
satisfied by 273-01), CHOKE-02 = C1 (writeEdge changes-aware), CHOKE-03 = C2 (PRAGMA
table_info(edges) fallback + review_status_persisted signal), CHOKE-04 = C3 (this plan's
ingestion allowlist test), CHOKE-05 = M2 (this plan's cross-room comment test), CHOKE-06 = M4
(this plan's substrate baseline honesty test). 273-01's task bodies referenced a wider
preliminary range (up to CHOKE-14) before this scheme was finalized -- those never became
distinct registered requirements (only CHOKE-01..06 ever appeared in a plan's `requirements:`
frontmatter array) and should be read as superseded prose, not a live ID. Plan 273-05
registers the final six in `.planning/REQUIREMENTS.md` at phase close.

Purpose: the same discipline 273-01 established -- prove the defect before touching production
code, so a later GREEN run is genuine evidence, not a test written to match the fix.

Output: `tests/test-273-ingestion-allowlist.cjs`, `tests/test-273-cross-room-comment.cjs`,
`tests/test-273-substrate-baseline-honest.cjs`.
</objective>

## Artifacts this phase produces

Cumulative across all Phase 273 plans (273-01 already shipped the first three rows).

| Artifact | Kind | Created by plan |
|---|---|---|
| `tests/run-all-273.sh` | file (bash aggregator) | 273-01 |
| `tests/test-273-writeedge-changes-aware.cjs` | file (test) | 273-01 |
| `tests/test-273-writeedge-base-schema.cjs` | file (test) | 273-01 |
| `tests/test-273-ingestion-allowlist.cjs` | file (test) | 273-02 |
| `tests/test-273-cross-room-comment.cjs` | file (test) | 273-02 |
| `tests/test-273-substrate-baseline-honest.cjs` | file (test) | 273-02 |
| `edgesHasReviewStatus(db)` | function in `lib/core/navigation/edges.cjs` | 273-03 |
| `written` | boolean field on `writeEdge`'s success return | 273-03 |
| `reason: 'suppressed_by_confirmed'` | new reason value on an `ok: true` return | 273-03 |
| `review_status_persisted` | boolean field on `writeEdge`'s success return | 273-03 |
| Corrected fence comment (11 sites) | doc-comment text in `lib/core/navigation/edges.cjs` | 273-03 |
| `ALLOWED_EDGE_TYPES` require + inline guard | code in `lib/core/navigation/ingestion.cjs` | 273-04 |
| `rejectedEdgeTypes` | array field on `storeBrainSuggestions`'s success return | 273-04 |
| `## 2026-08-31 re-measurement (Phase 273)` | heading in `docs/architecture/SUBSTRATE-BASELINE.md` | 273-05 |
| `### Phase 273` requirements section | heading in `.planning/REQUIREMENTS.md` | 273-05 |
| Dev-research mirror entry | file under `~/MindrianRooms/rethinking-mindrianos/research/` + `~/MindrianOS/research/` | 273-05 |
| `CHOKE-01` .. `CHOKE-06` | requirement IDs, registered in `.planning/REQUIREMENTS.md` | 273-05 |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-CONTEXT.md
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-RESEARCH.md
@.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test-273-ingestion-allowlist.cjs (C3, RED)</name>
  <files>tests/test-273-ingestion-allowlist.cjs</files>
  <read_first>
    - `lib/core/navigation/ingestion.cjs` (full, 83 lines) -- `storeBrainSuggestions`'s require
      block at line 16, `insightIds` declaration at line 27, the `BEGIN` transaction at line 28,
      the type-shape guard and raw `INSERT OR IGNORE` at lines 48-58, the success return at
      line 75.
    - `lib/core/navigation/edges.cjs` lines 32-52 (`ALLOWED_EDGE_TYPES` definition) and line 845
      (`module.exports = { ALLOWED_EDGE_TYPES, writeEdge };` -- already exported, no new export
      needed).
    - `.planning/phases/273-.../273-RESEARCH.md` "D-03 structural recommendation" section
      (lines ~241-282, the inline-vs-routing decision and the exact 6-line guard shape) and
      "tests/test-273-ingestion-allowlist.cjs (C3)" assert table (lines ~522-544).
    - `.planning/phases/273-.../273-PATTERNS.md` "File 5" (lines ~312-414) -- the complete test
      file to base this on, including the exact fixture (`TOTALLY_MADE_UP_TYPE`,
      `DROP TABLE nodes`, `INFORMS`) and the seven assertions.
    - `tests/test-273-writeedge-changes-aware.cjs` (273-01, already on disk) -- clone its
      header-comment convention (defect ID + requirement ID + RED-by-design banner), its
      `REPO`/require block shape, its `migratedDb()` setup helper (`fs.mkdtempSync` +
      `.mindrian` dir + `openRoomDb`), its `main().catch(...)` bottom, and its
      `console.log('PASS test-273-...')` success line.
  </read_first>
  <behavior>
    - Assert 1: one out-of-allowlist suggestion type does not fail the whole ingestion batch
      (`res.ok === true`).
    - Assert 2: the `brain_insight` node still lands (`res.insightIds.length === 1`).
    - Assert 3: the out-of-allowlist type `TOTALLY_MADE_UP_TYPE` does NOT appear in `edges`.
    - Assert 4: the adversarial type string `DROP TABLE nodes` does NOT appear in `edges` (it is
      an inert parameterized value either way -- this assert is about the allowlist, not SQL
      injection, which is already closed).
    - Assert 5: the allowlisted type `INFORMS` DOES land.
    - Assert 6: rejection is observable, not silent -- `res.rejectedEdgeTypes` is an array
      containing both `TOTALLY_MADE_UP_TYPE` and `DROP TABLE nodes`.
    - Assert 7 (Part 9 non-cannibalization guard, load-bearing): re-ingesting the identical
      `packetResult` a second time does NOT change the already-landed `INFORMS` edge's stored
      `properties` -- proves `INSERT OR IGNORE` semantics survive the fix (a routing-through-
      `writeEdge` implementation would go RED here even after "fixing" C3, because `writeEdge`
      uses `ON CONFLICT DO UPDATE`).
  </behavior>
  <action>
Create `tests/test-273-ingestion-allowlist.cjs` following 273-PATTERNS.md File 5 exactly: bare
`main()` + `node:assert/strict`, hyphens only. Header comment names Phase 273 D-03 / D-03a,
defect C3, requirement CHOKE-04, and states RED until plan 273-04 lands. Header records the
Canon Part 8 guard: no live Brain call anywhere in the file, `packetResult` is hand-constructed.

Setup helper `migratedDb()`: identical shape to the one in `tests/test-273-writeedge-changes-
aware.cjs` (`fs.mkdtempSync` under `os.tmpdir()`, `.mindrian` dir, `openRoomDb`). Require
`storeBrainSuggestions` from `lib/core/navigation/ingestion.cjs` via
`path.join(__dirname, '..', 'lib', 'core', 'navigation', 'ingestion.cjs')`.

Build the fixture `packetResult` with three `graph_updates_proposed` entries exactly as
273-PATTERNS.md File 5 specifies: `{source:'n1',target:'n2',type:'TOTALLY_MADE_UP_TYPE'}`,
`{source:'n3',target:'n4',type:'DROP TABLE nodes'}`, `{source:'n5',target:'n6',type:'INFORMS'}`.

Drive `storeBrainSuggestions(db, packetResult, 'sess1')` and implement the seven asserts above.
`storeBrainSuggestions` is synchronous (confirmed by reading `ingestion.cjs`: no `async`
keyword, no `await` inside it, returns a plain object) -- call it without `await` for the second
ingestion call in Assert 7, even though the surrounding `main()` may remain `async` for
uniformity with the file cleanup pattern.

Every assertion carries a message string naming what failed in plain words, matching the voice
of the existing C1/C2 test files (for example: `'C3: an out-of-allowlist type must not land'`).

Clean up the temp room in a `finally` (`closeRoomDb(db)` then `fs.rmSync(dir, {recursive:true,
force:true})`). Exit path: `main()` at module scope with
`.catch((e) => { console.error(e); process.exit(1); })`, final line
`console.log('PASS test-273-ingestion-allowlist')`.
  </action>
  <acceptance_criteria>
    - RED BY DESIGN: `node tests/test-273-ingestion-allowlist.cjs` exits NON-ZERO today, and the
      failure output names `TOTALLY_MADE_UP_TYPE` or `rejectedEdgeTypes` (proving it fails on
      the missing allowlist guard, not a harness/setup error).
    - `grep -c 'rejectedEdgeTypes' tests/test-273-ingestion-allowlist.cjs` is at least 3
      (referenced in an assertion on the array's existence and in assertions on both rejected
      types, not just mentioned once in a comment).
    - `grep -c 'INSERT OR IGNORE\|Part 9' tests/test-273-ingestion-allowlist.cjs` is at least 1
      (the non-cannibalization guard's intent is documented in the file, not just implemented
      silently).
    - `grep -c 'DROP TABLE nodes' tests/test-273-ingestion-allowlist.cjs` is at least 1 (the
      adversarial-string fixture is present).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-273-ingestion-allowlist.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'out=$(node tests/test-273-ingestion-allowlist.cjs 2>&1); rc=$?; if [ $rc -eq 0 ]; then echo "UNEXPECTED GREEN - the test is not asserting the defect"; exit 1; fi; printf "%s" "$out" | grep -qE "TOTALLY_MADE_UP_TYPE|rejectedEdgeTypes" || { echo "RED for the wrong reason:"; printf "%s\n" "$out"; exit 1; }; echo "RED AS DESIGNED"'</automated>
  </verify>
  <done>The file exists, fails non-zero citing the missing allowlist/rejectedEdgeTypes contract, asserts both bad-type-rejected and good-type-landed cases, and asserts the INSERT OR IGNORE non-cannibalization guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test-273-cross-room-comment.cjs (D-04, RED)</name>
  <files>tests/test-273-cross-room-comment.cjs</files>
  <read_first>
    - `lib/core/navigation/edges.cjs` lines 40-52 -- the exact current comment text at line 45:
      `//   never crosses to Brain. Cross-room aggregation forbidden (Phase 8 cross-room fence).`
    - `.planning/phases/273-.../273-RESEARCH.md` "D-04 scope correction" note (lines ~164-172) --
      confirms the phrase `Cross-room aggregation forbidden` appears 11 times in the file (lines
      45, 64, 236, 269, 366, 419, 583, 629, 667, 705, 746), not the 4 the original code review
      counted.
    - `.planning/phases/273-.../273-PATTERNS.md` "File 2" (lines ~135-169) -- the exact corrected
      wording plan 273-03 will write (`Cross-room aggregation is prevented structurally...`),
      and "File 6" (lines ~418-477) -- the complete test file to base this on.
  </read_first>
  <behavior>
    - Assert 1 (RED today): zero remaining occurrences of the literal misleading phrase
      `Cross-room aggregation forbidden` in `lib/core/navigation/edges.cjs`. Today it appears 11
      times, so this assertion fails.
    - Assert 2 (RED today, forward-looking pin): the corrected phrase
      `Cross-room aggregation is prevented structurally` appears at least 11 times in the same
      file. Today it appears 0 times.
  </behavior>
  <action>
Create `tests/test-273-cross-room-comment.cjs` following 273-PATTERNS.md File 6 exactly: a bare
SYNCHRONOUS `main()` (no db/fs-temp-dir setup needed -- this is a pure source-text assertion, no
`async`/`finally` cleanup required, unlike the other Phase 273 test files). Header comment names
Phase 273 D-04 / D-04a, defect M2, requirement CHOKE-05, states RED until plan 273-03 lands, and
records that the fence already holds structurally today (via `writeEdge(db, params)`'s function
signature) -- this test is pinning a comment-only defect with zero runtime behavior change.

Read `lib/core/navigation/edges.cjs`'s full source via `fs.readFileSync`, count occurrences of
`Cross-room aggregation forbidden` via a global regex match, assert the count is exactly 0
(message names the D-04 requirement), then count occurrences of
`Cross-room aggregation is prevented structurally` via a second global regex match, assert the
count is at least 11.

Bottom of file: bare `main();` call (not wrapped in `.catch`, since there is no async work),
final line before that `console.log('PASS test-273-cross-room-comment')`.

**Note for a future executor (273-03):** the exact corrected-phrase substring asserted here must
match whatever wording plan 273-03's comment edit lands with, verbatim. Write the comment fix
first in 273-03, then this test's assertion string is already fixed by this plan -- 273-03 must
match the phrase already committed here, not invent a new one.
  </action>
  <acceptance_criteria>
    - RED BY DESIGN: `node tests/test-273-cross-room-comment.cjs` exits NON-ZERO today, and the
      failure output mentions `Cross-room aggregation forbidden` or the count `11` (proving it
      fails on the un-swept comment, not a file-path error).
    - `grep -c "Cross-room aggregation is prevented structurally" tests/test-273-cross-room-comment.cjs`
      is at least 1 (the exact target phrase this test asserts against).
    - `grep -c "readFileSync" tests/test-273-cross-room-comment.cjs` is at least 1 (reads the
      real production file, not a hardcoded/mocked string).
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-273-cross-room-comment.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'out=$(node tests/test-273-cross-room-comment.cjs 2>&1); rc=$?; if [ $rc -eq 0 ]; then echo "UNEXPECTED GREEN - the test is not asserting the defect"; exit 1; fi; printf "%s" "$out" | grep -qE "Cross-room aggregation forbidden|11" || { echo "RED for the wrong reason:"; printf "%s\n" "$out"; exit 1; }; echo "RED AS DESIGNED"'</automated>
  </verify>
  <done>The file exists, fails non-zero citing the un-swept misleading comment, and asserts both the zero-old-phrase and at-least-11-new-phrase conditions against the real production file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/test-273-substrate-baseline-honest.cjs (D-05, RED)</name>
  <files>tests/test-273-substrate-baseline-honest.cjs</files>
  <read_first>
    - `scripts/check-substrate.cjs` lines 353-365 (`scanRepo` -- walks `lib/` + `scripts/`,
      returns a plain array via `scanFiles`), lines 393-400 (`runBaseline` -- confirms the return
      is consumed as `violations.length`, i.e. `scanRepo()` returns an ARRAY, not a `{count}`
      object -- already confirmed by direct execution during planning), and lines 430-438
      (`module.exports` -- `scanRepo` is exported).
    - `docs/architecture/SUBSTRATE-BASELINE.md` lines 1-40 and 280-292 -- the current end of the
      file. No dated re-measurement section exists yet; this is exactly why the test is RED
      today. Note the file's existing dated-note convention (a `##` heading naming the date and
      phase, prose underneath) so the regex this test writes matches the shape plan 273-05 will
      actually produce.
    - `.planning/phases/273-.../273-RESEARCH.md` "Priority 2" section (lines ~355-423) and
      "D-05 validation" (lines ~555-562).
    - `.planning/phases/273-.../273-PATTERNS.md` "File 7" (lines ~480-541) -- the complete test
      file to base this on, and "File 4" (lines ~267-309) -- the EXACT markdown plan 273-05 will
      append, including the literal heading `## 2026-08-31 re-measurement (Phase 273)` and the
      literal line shape `Result: **208**`. This test's regex must match that shape exactly.
  </read_first>
  <behavior>
    - Assert 1 (RED today): `docs/architecture/SUBSTRATE-BASELINE.md` contains a section
      matching `/## 2026-08-31 re-measurement[\s\S]*?Result: \*\*(\d+)\*\*/`. Fails today because
      no such section exists.
    - Assert 2 (design assertion, passes once the section exists): the live measured count from
      `scanRepo()` equals the number captured from that section. This is the self-detecting
      drift guard -- it is what stops the documented 195 from ever silently diverging from a
      measured 208 again.
  </behavior>
  <action>
Create `tests/test-273-substrate-baseline-honest.cjs` following 273-PATTERNS.md File 7 exactly:
bare synchronous `main()` (no fs-temp-dir setup, no db handles -- pure require + fs.readFileSync
work). Header comment names Phase 273 D-05 / M4, requirement CHOKE-06, states RED until plan
273-05 appends the dated section, and records the expected post-fix number from RESEARCH (208,
unchanged from the pre-fix measurement, because `lib/core/navigation/` is path-allowlisted in
`check-substrate.cjs` AND `INSERT OR IGNORE` evades the `RE_RAW_WRITE` regex -- so C3's fix
cannot move this count, per the doc's own reconciliation note that 273-05 will write).

Require `scanRepo` from `scripts/check-substrate.cjs` via
`path.join(REPO, 'scripts', 'check-substrate.cjs')`. Call `scanRepo()`, take `.length` as the
measured count (confirmed by reading `check-substrate.cjs:393-400`: the function returns a plain
array; do not add unnecessary defensive branching for an object-shaped return, since that shape
does not exist in the current code).

Read `docs/architecture/SUBSTRATE-BASELINE.md` via `fs.readFileSync`, match against
`/## 2026-08-31 re-measurement[\s\S]*?Result: \*\*(\d+)\*\*/`, assert the match exists (message
names the D-05 requirement and the missing section), extract the captured number, assert it
equals the live measured count from `scanRepo()` (message names both values on mismatch for
diagnosability).

Bottom of file: bare `main();` call, final line before that
`console.log('PASS test-273-substrate-baseline-honest (measured=' + measured + ')')`.
  </action>
  <acceptance_criteria>
    - RED BY DESIGN: `node tests/test-273-substrate-baseline-honest.cjs` exits NON-ZERO today,
      and the failure output mentions `2026-08-31 re-measurement` (proving it fails because the
      doc section does not exist yet, not a require-path error).
    - `grep -c 'scanRepo' tests/test-273-substrate-baseline-honest.cjs` is at least 2 (required
      and called).
    - `grep -c "SUBSTRATE-BASELINE" tests/test-273-substrate-baseline-honest.cjs` is at least 1.
    - `LC_ALL=C.UTF-8 grep -cP '\x{2014}' tests/test-273-substrate-baseline-honest.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'out=$(node tests/test-273-substrate-baseline-honest.cjs 2>&1); rc=$?; if [ $rc -eq 0 ]; then echo "UNEXPECTED GREEN - the test is not asserting the defect"; exit 1; fi; printf "%s" "$out" | grep -q "2026-08-31 re-measurement" || { echo "RED for the wrong reason:"; printf "%s\n" "$out"; exit 1; }; echo "RED AS DESIGNED"'</automated>
  </verify>
  <done>The file exists, fails non-zero citing the missing dated re-measurement section, and (once that section exists) will compare the live scanRepo() count against the documented number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same scope as 273-01: no network, auth, session, or browser surface. The entire attack surface
is local SQLite write-path integrity plus source-text honesty (the comment sweep and the
baseline reconciliation are governance controls, not runtime code).

| Boundary | Description |
|----------|-------------|
| caller-owned db handle -> `storeBrainSuggestions` | The `packetResult` object is treated as untrusted, remote-shaped input even though this plan constructs it by hand (Canon Part 8: no live Brain call). The C3 test's whole point is that `ep.type` is attacker-controllable. |
| test harness -> real production modules | These tests drive the real `storeBrainSuggestions`, the real `scanRepo`, and read the real `edges.cjs`/`SUBSTRATE-BASELINE.md` source. No fixture is created inside the repo tree; the ingestion test's temp room uses `fs.mkdtempSync` under `os.tmpdir()`. |
| documentation vs. code | The M2 comment sweep and M4 baseline reconciliation are governance/documentation controls whose failure mode is a false claim about the system's actual behavior (Canon Part 6 dog-fooding), not a runtime vulnerability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-273-04 | Tampering | `tests/test-273-ingestion-allowlist.cjs` fixtures on disk | mitigate | `fs.mkdtempSync` under `os.tmpdir()`, removed in a `finally`. No test writes to the repo tree or to `~/MindrianRooms`. |
| T-273-05 | Information disclosure | Canon Part 8 (LOCAL -> BRAIN) | mitigate | `packetResult` is hand-constructed inside the test file; no network call, no Brain client require. Covered by `run-all-273.sh`'s existing Part 8 source sweep once this file is glob-discovered. |
| T-273-06 | Repudiation | `docs/architecture/SUBSTRATE-BASELINE.md` claims | mitigate | This is the class of threat D-05/CHOKE-06 exists to close: a stale documented number silently diverging from the measured reality. `test-273-substrate-baseline-honest.cjs` is the self-detecting control -- it fails loudly (not silently) whenever the two diverge. |
| T-273-07 | Spoofing / Elevation of Privilege | Brain-supplied `ep.type` (remote vocabulary) | mitigate | This is C3 itself: `ALLOWED_EDGE_TYPES` is a closed, deny-by-default allowlist. This plan's test (`test-273-ingestion-allowlist.cjs`) proves the gap exists today; plan 273-04 closes it. |
| T-273-SC | Tampering | npm / pip / cargo installs | n/a | This plan installs nothing. No package-manager task exists; RESEARCH.md deliberately omits a Package Legitimacy Audit for that reason. |
</threat_model>

<verification>
1. `node tests/test-273-ingestion-allowlist.cjs` exits non-zero mentioning `TOTALLY_MADE_UP_TYPE` or `rejectedEdgeTypes`.
2. `node tests/test-273-cross-room-comment.cjs` exits non-zero mentioning `Cross-room aggregation forbidden` or `11`.
3. `node tests/test-273-substrate-baseline-honest.cjs` exits non-zero mentioning `2026-08-31 re-measurement`.
4. `bash tests/run-all-273.sh` now discovers 5 test files (2 from 273-01 + 3 from this plan). All 5 FAIL (RED by design); the aggregator itself exits non-zero, which is CORRECT for the still-incomplete Wave 0/1 boundary -- this phase's GREEN-making fixes land in 273-03/273-04.
5. `LC_ALL=C.UTF-8 grep -rlP '\x{2014}' tests/test-273-ingestion-allowlist.cjs tests/test-273-cross-room-comment.cjs tests/test-273-substrate-baseline-honest.cjs` prints nothing.
6. No production file was touched: `git status --porcelain lib/ scripts/ docs/` is empty.
</verification>

<success_criteria>
- Three new test files exist and ALL THREE fail, each for the specific defect it targets (not for a harness error).
- `tests/run-all-273.sh` discovers all 5 Phase 273 test files by glob with zero runner edits needed.
- Each test asserts its negative case explicitly: the unfiltered Brain edge type that DOES land today, the 11 un-swept comment occurrences, the missing baseline reconciliation section.
- Zero production files modified by this plan.
- Zero em-dashes in every file created.
</success_criteria>

<output>
Create `.planning/phases/273-sqlite-graph-chokepoint-hardening-writeedge-silent-failure-a/273-02-SUMMARY.md` when done.
Record the exact RED failure message of each of the three test files verbatim; plans 273-03,
273-04, and 273-05 use them as their before/after evidence.
</output>
