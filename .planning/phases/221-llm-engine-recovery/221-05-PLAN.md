---
phase: 221-llm-engine-recovery
plan: 05
type: execute
wave: 5
depends_on: ["221-04"]
files_modified:
  - CHANGELOG.md
  - README.md
  - package.json
  - .claude-plugin/plugin.json
  - .planning/phases/221-llm-engine-recovery/221-VERIFICATION.md
autonomous: false
requirements: [REQ-6]
user_setup:
  - service: npm
    why: "npm publish runs inside scripts/release.sh step 9.5"
    env_vars:
      - name: "npm auth (logged-in ~/.npmrc)"
        source: "existing npm login on this machine; release.sh fails loudly if absent"
must_haves:
  truths:
    - "The cut executes ONLY after the gates: 219-RELEASE-STAGING.md exists, 219-VERIFICATION.md carries the navigator's corepower confirmation, 220-RELEASE-STAGING.md + 220-VERIFICATION.md readiness recorded, and every 221 gate is green - a missing gate is a NAMED blocker, never staged around (REQ-6, D-10)"
    - "The joint CHANGELOG entry covers 219+220+221, assembled from the staged drafts - never re-derived from memory (D-10)"
    - "ALL READMEs are refreshed content-only: Feynman + JTBD voice, styling preserved - diffs prove content-only (REQ-6, D-10)"
    - "mindrian-os.com is updated in the relevant capability locations (opportunity follow-through, web ingestion, engine recovery) AND every hand-typed version string is reconciled per the VERSION-BUMP discipline (REQ-6)"
    - "scripts/release.sh <version> executes the five-gate lockstep: CHANGELOG + plugin.json + package.json + git tag + marketplace source.ref pin; npm publish; website FALLBACK_VERSION sync - NEVER a hand bump (D-10, CLAUDE.md HARD RULE)"
    - "The navigator approves the version and the assembled diffs at a blocking checkpoint BEFORE release.sh runs (autonomous: false)"
    - "Everything is recorded in 221-VERIFICATION.md: precondition evidence, gate outputs, npm version, marketplace pin, website updates + fact-check - the session goal (version cut after all needs met) CLOSES"
  artifacts:
    - path: ".planning/phases/221-llm-engine-recovery/221-VERIFICATION.md"
      provides: "the complete cut record: preconditions, gates, release.sh output, npm/marketplace/website verification, fact-check table"
      contains: "release.sh"
    - path: "CHANGELOG.md"
      provides: "the joint 219+220+221 entry finalized by release.sh ([Unreleased] -> [version] - date)"
      contains: "engine recovery"
    - path: "README.md"
      provides: "content-only refresh covering the three phases' capabilities, styling preserved"
  key_links:
    - from: "CHANGELOG.md"
      to: ".planning/phases/219-*/219-RELEASE-STAGING.md + .planning/phases/220-*/220-RELEASE-STAGING.md"
      via: "the joint entry assembled from the staged drafts (219's draft carries the marked 220 slot; 220's insert applies into it; 221 content added)"
      pattern: "engine recovery"
    - from: "scripts/release.sh"
      to: "~/mindrian-marketplace/.claude-plugin/marketplace.json"
      via: "source.ref pinned to the new tag (lockstep gate 5)"
      pattern: "source.ref"
    - from: "scripts/release.sh"
      to: "<website repo>/src/lib/version.ts"
      via: "Step 9.6b FALLBACK_VERSION sed + git push + live-poll mindrian-os.com (MINDRIAN_WEBSITE_DIR resolution)"
      pattern: "FALLBACK_VERSION"
---

<objective>
Execute REQ-6: THE JOINT CUT. One version cut covering Phases 219+220+221 (ROADMAP release note, navigator decision 2026-07-13, FINAL), executed HERE as this phase's last act, after all three phases' gates are green and the navigator's corepower confirmation is recorded. Pre-approved by the navigator ("run till and including 221 and full cut with updating all READMEs and mainly the mindrian website") - with the version approval and diff review held at one blocking checkpoint because a version cut is irreversible (autonomous: false at the cut gate, D-10).

Implements D-10 in full: verify 219 readiness (corepower confirmation recorded) + 220 readiness + all 221 gates; assemble the joint CHANGELOG entry from the staged drafts (219-RELEASE-STAGING.md + 220-RELEASE-STAGING.md); refresh ALL READMEs content-only (repo README.md primary + feature-touched surface docs); update mindrian-os.com in the relevant locations (new capabilities: opportunity follow-through, web ingestion, engine recovery) + reconcile every hand-typed version string (the VERSION-BUMP discipline, standing navigator memory rule); then scripts/release.sh <version> (default: the in-progress increment on the current line) + npm publish + marketplace source.ref pin; record everything in 221-VERIFICATION.md.

Version fact (verified at plan time): the tree is already staged at 1.15.3-beta.15 (package.json + plugin.json + CHANGELOG [Unreleased] header, the release.sh two-commit form; last tag v1.15.3-beta.14). The DEFAULT cut version is therefore 1.15.3-beta.15 unless the navigator directs otherwise at the checkpoint.

Website fact (verified at plan time, PATTERNS flag 3): src/lib/version.ts exists in BOTH $HOME/mindrian-website/website (release.sh's default WEBSITE_DIR) and $HOME/dev/mindrian-website/website. The deploy-canonical root must be resolved at execution (git remote -v + latest commits + Vercel linkage); set MINDRIAN_WEBSITE_DIR only if it differs from the default. mindrian-os.com is the single canonical web surface (minisite retired 2026-06-09).

Purpose: 219's lifecycle engine, 220's web ingestion, and 221's recovery architecture ship to users as ONE coherent release - and the session goal closes.
Output: joint CHANGELOG, refreshed READMEs, updated website, the executed five-gate release, 221-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/221-llm-engine-recovery/221-SPEC.md
@.planning/phases/221-llm-engine-recovery/221-CONTEXT.md
@.claude/includes/release-process.md
@scripts/release.sh
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut-gate preconditions + joint CHANGELOG assembly + README refresh + website capability copy</name>
  <files>CHANGELOG.md, README.md, .planning/phases/221-llm-engine-recovery/221-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/219-opportunity-follow-through-harvest-formula-explored-stage-de/219-RELEASE-STAGING.md AS LANDED by 219-07 (the joint CHANGELOG draft with the marked 220 slot, README refresh draft, marketplace instruction, website fact-check checklist, the handoff note)
    - .planning/phases/220-web-ingestion-agent-mindrian-web-agent-url-clean-markdown-ro/220-RELEASE-STAGING.md AS LANDED by 220-05 (the 220 CHANGELOG insert for the marked slot, README additions, website update drafts)
    - .planning/phases/219-*/219-VERIFICATION.md (the corepower confirmation - THE navigator-run gate) + .planning/phases/220-*/220-VERIFICATION.md (220 readiness)
    - CHANGELOG.md head (the entry format: bolded one-line summary + Feynman detail per item, ### Added / ### Fixed sections under [Unreleased])
  </read_first>
  <action>
    PRECONDITION GATE FIRST - every check recorded verbatim in a new 221-VERIFICATION.md precondition table; ANY failure STOPS this plan with the named blocker (never stage around it, D-10): (1) 219-RELEASE-STAGING.md exists; (2) grep -qi "corepower" .planning/phases/219-*/219-VERIFICATION.md succeeds AND the section reads as a recorded navigator confirmation, not a placeholder (read it - a pending/unfilled section is a BLOCKER: "219 corepower confirmation not recorded - the cut holds"); (3) 220-RELEASE-STAGING.md exists AND 220-VERIFICATION.md records readiness; (4) the 221 gates: bash tests/run-all-221.sh FAIL=0, regression legs green (211/215/216/219/220 - at cut time 220 MUST exist, no SKIP accepted for 219/220 legs at THIS gate), node scripts/doctor.cjs --acceptance green, node scripts/build-connector-registry.cjs --check green; (5) git diff --exit-code package.json .claude-plugin/plugin.json proves no premature hand-bump (CHANGELOG/README are ABOUT to be edited by this task, so gate the version files only). THEN assemble the joint CHANGELOG entry inside the existing [Unreleased] section (release.sh Step 6 finalizes the heading to [version] - date; do NOT touch the heading or version files): apply 219's staged draft, insert 220's staged content into the marked 220 slot, and add the 221 content - typed stage envelopes replacing failure-to-empty, the 6-tier recovery ladder, the bounded fenced LLM recovery controller + case file, honest result semantics + disclosure, the permanent vantage fixture - each item a bolded one-liner + Feynman detail per the house format. THEN refresh READMEs content-only (D-10, styling preserved - never restyled): repo README.md applies 219's + 220's staged README drafts and adds the 221 engine-recovery capability in the same Feynman + JTBD voice; sweep the surface docs the three phases touched (219-RELEASE-STAGING/220-RELEASE-STAGING name them; at minimum verify commands/research.md + the regenerated mirror already tell the recovery story from 221-04) - content updated, zero restyle, no em-dashes. THEN the website capability copy: resolve the deploy-canonical website root (default $HOME/mindrian-website/website per release.sh Step 9.6b; check git remote -v, latest commit recency, and Vercel project linkage across the two candidate roots; record the resolution + evidence in 221-VERIFICATION.md and export MINDRIAN_WEBSITE_DIR for Task 3 if it differs) and apply the staged website update drafts from BOTH staging docs plus the 221 capability copy (engine recovery: "when a research engine breaks, Mindrian recovers intelligently and tells you exactly what happened") in the site's existing voice and De Stijl design system - content-only edits in the relevant capability/feature locations, committed in the website repo working tree but NOT pushed yet (release.sh Step 9.6b pushes with the version sync; keep one deploy); touch NO version strings anywhere (Step 9.6b owns FALLBACK_VERSION; the hand-typed fact-check happens in Task 3 AFTER the bump). Record every action in 221-VERIFICATION.md as you go.
  </action>
  <verify>
    <automated>bash tests/run-all-221.sh && node scripts/doctor.cjs --acceptance && git diff --exit-code package.json .claude-plugin/plugin.json && grep -q "engine recovery" CHANGELOG.md && grep -qi "corepower" .planning/phases/221-llm-engine-recovery/221-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 221-VERIFICATION.md carries the full precondition table with verbatim evidence (staging docs found, corepower confirmation quoted, gate outputs)
    - CHANGELOG [Unreleased] carries the assembled joint entry covering all three phases; version files untouched (git diff clean on package.json + plugin.json)
    - README diffs are content-only (structure/styling markers unchanged - verify by diff inspection recorded in 221-VERIFICATION.md); website capability edits committed locally in the resolved repo, unpushed, zero version strings touched
  </acceptance_criteria>
  <done>Everything the cut needs is assembled, evidenced, and reversible - nothing irreversible has happened yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Navigator cut approval - version + diffs + gates</name>
  <action>Present the assembled release to the navigator and pause execution (blocking gate; never auto-approvable - the cut is irreversible). On approval, record the approved version verbatim in 221-VERIFICATION.md (Cut Approval section) and proceed to Task 3. On any described issue, route back to Task 1 - release.sh must not run until the diffs are approved.</action>
  <what-built>The assembled joint release: precondition table (219 corepower + 219/220 readiness + 221 gates all green), the joint 219+220+221 CHANGELOG entry, content-only README refreshes, website capability copy staged in the resolved repo. The default cut version is 1.15.3-beta.15 (the increment the tree is already staged toward; last tag v1.15.3-beta.14).</what-built>
  <how-to-verify>
    1. Read the precondition table in .planning/phases/221-llm-engine-recovery/221-VERIFICATION.md - confirm the corepower line quotes YOUR recorded confirmation and every gate row is green.
    2. Review git diff CHANGELOG.md README.md - the joint entry should read as one coherent release story (opportunity follow-through + web ingestion + engine recovery), READMEs content-only in the existing voice.
    3. Review the website repo diff (path recorded in 221-VERIFICATION.md) - capability copy only, no version strings.
    4. Approve the version: 1.15.3-beta.15 (default) or name another.
  </how-to-verify>
  <resume-signal>Type "approved" (cuts 1.15.3-beta.15) or "approved <version>" to direct a different version. Any issue described here routes back to Task 1 before anything irreversible runs - release.sh has NOT executed yet.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute scripts/release.sh + verify the five gates + website fact-check + close the record</name>
  <files>package.json, .claude-plugin/plugin.json, CHANGELOG.md, .planning/phases/221-llm-engine-recovery/221-VERIFICATION.md</files>
  <read_first>
    - scripts/release.sh (:199-245 the step plan; :402-410 Step 6 CHANGELOG finalize; :855-910 Step 9.6b website sync + WEBSITE_DIR resolution; step 9.5 npm publish; step 9.8 doctor)
    - docs/ VERSION-BUMP checklist surface (locate the maintained VERSION-BUMP-CHECKLIST; the standing memory rule: after version bumps, fact-check every hand-typed version surface on mindrian-website)
  </read_first>
  <action>
    From /home/jsagi/dev/MindrianOS-Plugin (the WORKSPACE GUARD - never the plugin cache), with MINDRIAN_WEBSITE_DIR exported if Task 1 resolved a non-default root, run scripts/release.sh <approved version> and let it enforce everything (D-10, D-14 lineage: NEVER hand-bump; the script owns all five gates): verify-release + doctor pre-flight, version bumps, CHANGELOG [Unreleased] -> [version] - date finalize, release commit + tag, marketplace source.ref pin, npm publish (step 9.5), website FALLBACK_VERSION sed + push + live-poll (step 9.6b - this also pushes Task 1's staged capability commits, one deploy), next-pre-release bump commit, doctor acceptance (step 9.8). If release.sh aborts at ANY gate: fix NOTHING by hand; record the abort verbatim in 221-VERIFICATION.md, route the failure to its owning surface, and re-run the script (a partial hand-finished release is the exact drift the five-gate rule exists to prevent). THEN verify independently and record each in 221-VERIFICATION.md: (1) scripts/verify-release green post-cut; (2) npm view @mindrian_os/cli versions includes the new version (npm shows it - REQ-6 acceptance); (3) git tag v<version> points at the release commit; (4) ~/mindrian-marketplace/.claude-plugin/marketplace.json source.ref reads v<version>; (5) https://mindrian-os.com live-poll reflects the version (Step 9.6b output) AND the capability pages render the new copy. THEN the VERSION-BUMP fact-check (the standing discipline): walk every hand-typed version surface on the website repo (grep the resolved repo for the OLD version string and for hand-typed version literals; reconcile each; commit + push any stragglers) and complete the VERSION-BUMP-CHECKLIST; record the fact-check table in 221-VERIFICATION.md. FINALLY close 221-VERIFICATION.md: the five-gate table, npm + marketplace + website evidence, README diff summary, the user upgrade path note (/plugin marketplace update + claude plugin update - the release-process include), and the closing line that the joint 219+220+221 cut executed and the session goal (version cut after all needs met) is CLOSED. Update ROADMAP.md phase 221 status via the normal execution flow.
  </action>
  <verify>
    <automated>scripts/verify-release && git describe --tags --abbrev=0 && grep -q "source.ref" "$HOME/mindrian-marketplace/.claude-plugin/marketplace.json" && grep -q "release.sh" .planning/phases/221-llm-engine-recovery/221-VERIFICATION.md && node scripts/doctor.cjs --acceptance</automated>
  </verify>
  <acceptance_criteria>
    - scripts/release.sh completed all steps (no aborted gate left hand-patched); scripts/verify-release green
    - npm shows the new version; the tag and marketplace source.ref match it; mindrian-os.com live-polls the version and renders the capability copy
    - The website hand-typed-version fact-check table is complete in 221-VERIFICATION.md (zero stale version strings remain)
    - 221-VERIFICATION.md closes the record: the joint cut is DONE and the session goal is marked CLOSED
  </acceptance_criteria>
  <done>One release, five gates, three phases, every surface reconciled - the joint 219+220+221 cut has shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> npm + marketplace + website | the release crosses from private tree to public surfaces; drift here reaches every user |
| staged drafts -> published CHANGELOG/READMEs | staged content becomes the public release story |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-221-25 | Tampering | hand-bumped versions drifting the five surfaces | mitigate | release ONLY via scripts/release.sh (CLAUDE.md HARD RULE); git diff --exit-code pre-gate; aborted gates never hand-finished |
| T-221-26 | Repudiation | cutting without the recorded human gates | mitigate | precondition gate BLOCKS on missing corepower confirmation / staging docs; blocking navigator checkpoint before release.sh; autonomous: false |
| T-221-27 | Tampering | wrong website repo updated (dual-root ambiguity) | mitigate | deploy-canonical root resolved with evidence (remotes, recency, Vercel linkage) and recorded; MINDRIAN_WEBSITE_DIR set explicitly |
| T-221-28 | Spoofing | stale hand-typed version strings on the live site | mitigate | VERSION-BUMP fact-check walk post-bump, table recorded in 221-VERIFICATION.md (standing memory rule) |
| T-221-29 | Denial of Service | releasing from the plugin install cache | mitigate | WORKSPACE GUARD: everything runs from /home/jsagi/dev/MindrianOS-Plugin; release.sh pre-flight |
| T-221-SC | Tampering | package installs | accept (n/a) | zero installs; npm publish only (outbound) |
</threat_model>

<verification>
- scripts/verify-release green post-cut; npm/tag/marketplace/website all show the approved version
- bash tests/run-all-221.sh + node scripts/doctor.cjs --acceptance green on the release commit
- 221-VERIFICATION.md complete: preconditions, checkpoint approval, five-gate evidence, fact-check table, session-goal CLOSED
</verification>

<success_criteria>
- SPEC acceptance 7 satisfied: release.sh executed, npm published, five lockstep gates + marketplace pin verified, READMEs content-only refreshed, mindrian-os.com updated + version fact-check recorded
- SPEC acceptance 8 satisfied: corepower validation recorded in 219-VERIFICATION.md BEFORE the cut (precondition-gated)
- The ROADMAP release note is discharged: ONE joint 219+220+221 cut, executed at Phase 221 completion
</success_criteria>

## Artifacts this plan produces

- The release itself: git tag v<version>, npm @mindrian_os/cli@<version>, marketplace source.ref pin, website FALLBACK_VERSION + capability copy live on mindrian-os.com
- Docs: joint 219+220+221 CHANGELOG entry; content-only README refreshes; website capability copy (opportunity follow-through, web ingestion, engine recovery)
- Record: .planning/phases/221-llm-engine-recovery/221-VERIFICATION.md (preconditions, approval, five-gate evidence, npm/marketplace/website verification, VERSION-BUMP fact-check table, session goal CLOSED)

<output>
Create `.planning/phases/221-llm-engine-recovery/221-05-SUMMARY.md` when done
</output>
