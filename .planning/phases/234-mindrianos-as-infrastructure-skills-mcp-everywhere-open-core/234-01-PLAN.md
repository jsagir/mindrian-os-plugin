---
phase: 234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-skill-spec.cjs
  - package.json
  - package-lock.json
  - tests/run-all-234.sh
  - lib/mcp/no-instructions.test.cjs
  - tests/test-234-adoption-engine-gate.cjs
autonomous: true
requirements: [D-01, D-02, D-03, D-08, D-10]

must_haves:
  truths:
    - "Running node scripts/check-skill-spec.cjs --check reports every hard Agent-Skills-spec violation across skills/*/SKILL.md (no violation is silently missed)."
    - "Running node scripts/check-skill-spec.cjs --catalog-budget reports the real byte total of name+description across all skills against the 50KB Zed budget."
    - "The MCP server's initialize response never carries an instructions key (D-03's already-accidental compliance is now a locked, tested invariant)."
    - "No skill or command in a /mos: methodology execution path references resolve-brain-key or brainPlan, except the three known legitimate setup/connector surfaces."
    - "The unaffiliated npm skills-ref package is no longer a project dependency."
  artifacts:
    - path: "scripts/check-skill-spec.cjs"
      provides: "Agent Skills spec validator (--check), Zed catalog budget (--catalog-budget), CLAUDE_PLUGIN_ROOT census (--plugin-root-census)"
      exports: ["validateSkill", "checkAll", "catalogBudget", "pluginRootCensus"]
    - path: "tests/run-all-234.sh"
      provides: "Phase 234 gate harness: glob-discovers tests/test-234-*, runs check-skill-spec.cjs, Part 8 tripwire with negative self-test"
    - path: "lib/mcp/no-instructions.test.cjs"
      provides: "Locks D-03: asserts initialize result never carries instructions"
    - path: "tests/test-234-adoption-engine-gate.cjs"
      provides: "Locks D-10: no methodology skill/command gates on a paid check"
  key_links:
    - from: "tests/run-all-234.sh"
      to: "scripts/check-skill-spec.cjs"
      via: "run \"check-skill-spec\" node scripts/check-skill-spec.cjs --check"
      pattern: "check-skill-spec\\.cjs --check"
    - from: "scripts/check-skill-spec.cjs"
      to: "gray-matter"
      via: "require('gray-matter') for READ-ONLY frontmatter parsing"
      pattern: "require\\(.gray-matter.\\)"
---

<objective>
Build the Wave-0 verification infrastructure this entire phase depends on: the in-repo Agent Skills spec validator (`scripts/check-skill-spec.cjs`), the phase's own test harness (`tests/run-all-234.sh`), and two cheap, high-value locked-invariant tests (D-03's `instructions` absence, D-10's adoption-engine-not-gated grep). Also removes the unaffiliated npm `skills-ref` package that is currently sitting in `package.json` as dead weight and a supply-chain identity hazard.

Purpose: every later plan in this phase (234-03 through 234-08) verifies its own work against `check-skill-spec.cjs` and reports through `run-all-234.sh`. Building this first, and getting it green against the CURRENT (still-non-compliant) skill catalog baseline, establishes the measurable starting point (9 hard failures, 112 allowed-tools deviations, 0/125 license fields) that later plans close out.
Output: `scripts/check-skill-spec.cjs`, `tests/run-all-234.sh`, `lib/mcp/no-instructions.test.cjs`, `tests/test-234-adoption-engine-gate.cjs`, `package.json`/`package-lock.json` with `skills-ref` removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-CONTEXT.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-RESEARCH.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-PATTERNS.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/check-skill-spec.cjs validator (--check, --catalog-budget, --plugin-root-census) + remove npm skills-ref</name>
  <files>scripts/check-skill-spec.cjs, package.json, package-lock.json</files>
  <read_first>
    scripts/check-shape-declaration.cjs (lines 1-46 header pattern, lines 853-940 CLI routing + exit-contract pattern -- copy this shape, but see the exit-code deviation noted below);
    scripts/check-render-coverage.cjs (lines 539-558 dual-export pattern);
    234-RESEARCH.md "Code Examples" section (the exact byte-measurement snippet for the Zed budget, and the exact `validateSkill` hard-rule snippet with NAME_RE);
    234-RESEARCH.md "Package Legitimacy Audit" section (why npm skills-ref must go);
    234-PATTERNS.md "NEW scripts/check-skill-spec.cjs" section;
    package.json (confirm `"skills-ref": "^0.1.5"` is present under dependencies and has zero require() call sites in the tree -- already confirmed during planning via `grep -rn "skills-ref" lib bin` returning nothing).
  </read_first>
  <action>
    Create scripts/check-skill-spec.cjs as a CJS gate script following check-shape-declaration.cjs's header/CLI-routing shape (file preamble comment naming Phase 234-01, the ONE validator for Agent Skills spec conformance, Canon Part 8 "reads process.env and local skills/ files only, zero network"). Use `gray-matter` (already a dependency) for READ-ONLY frontmatter parsing -- never write back through this file. Implement three pure, exported functions plus a CLI:
    - `validateSkill(skillMdPath)`: reads skills/&lt;dir&gt;/SKILL.md, returns an array of violation strings. Rules from agentskills.io/specification (RESEARCH's "Formal spec facts" table): `name` required, must match `^[a-z0-9]+(-[a-z0-9]+)*$`, 1-64 chars, and must equal `path.basename(path.dirname(skillMdPath))`; `description` required, 1-1024 chars; if `allowed-tools` is present it must be a string (not an array/object) -- flag `` `allowed-tools` must be a space-separated string `` when it is not; if `compatibility` is present it must be &lt;=500 chars.
    - `checkAll()`: globs `skills/*/SKILL.md` (NOT `.claude/skills/**` -- that is the project-local `docu-optimizer` dev skill, out of shipped-product scope per RESEARCH's baseline), runs `validateSkill` on each, returns `{ ok, total, failing, violations: [{skill, errors}] }`.
    - `catalogBudget()`: sums `Buffer.byteLength(String(name)+String(description), 'utf8')` across all 125 skills (RESEARCH's exact snippet), returns `{ totalBytes, budgetBytes: 51200, pct, ok: totalBytes < 51200 }`.
    - `pluginRootCensus()`: greps the BODY (content after the closing `---` of frontmatter, via `gray-matter`'s `.content`) of every `skills/*/SKILL.md` for the literal token `${CLAUDE_PLUGIN_ROOT}`, returns `{ count, files: string[] }` (informational only -- this function always reports, never fails; 234-06 owns the "count must be zero" assertion once migration lands).
    CLI routing via `process.argv.slice(2)`, mirroring check-shape-declaration.cjs's switch-on-includes shape, with ONE deliberate deviation from that file's advisory-by-default convention: this is a genuine spec validator (not the HITL-shape gate that was downgraded to advisory in Phase 210 for its own documented reason), so `--check` with ANY violation exits 1 by default (no `--strict` flag needed for this gate -- do not copy the WARN-then-exit-0 advisory branch). `--catalog-budget` exits 1 if `!ok`. `--plugin-root-census` always exits 0 and prints the JSON census. No flag or unknown flag prints usage and exits 2. Export `{ validateSkill, checkAll, catalogBudget, pluginRootCensus }` unconditionally (the check-render-coverage.cjs dual-export shape), gated by `require.main === module` for the CLI entry only.
    In the SAME task, remove the unaffiliated npm `skills-ref` package: run `npm uninstall skills-ref` from the repo root (updates both package.json and package-lock.json). This package is currently a live, unused dependency (zero require() call sites confirmed by grep across lib/ and bin/) -- it is the exact cross-ecosystem name-collision hazard RESEARCH.md's Package Legitimacy Audit flags (npm 0.1.5, no source repo, ~7-month stale, unaffiliated with the official PyPI `skills-ref` the spec actually names). This validator replaces its intended role in-repo, CJS-only, per Canon Part 7 / the CLAUDE.md CJS-only constraint.
  </action>
  <acceptance_criteria>
    - `node scripts/check-skill-spec.cjs --check` runs to completion and exits 1 (9 known hard failures still present at this point in the phase -- this is the correct, expected baseline reading, not a bug).
    - `node scripts/check-skill-spec.cjs --check` output enumerates at minimum the 7 missing-`name` skills (auto-explore, brain-derive, dial-memory-refresh, dogfood-flush, explain-decision, feynman-timeline-refresh, mva-report), `MOSDeckEngine` (charset), and `value-proposition` (name != dirname) by name.
    - `node scripts/check-skill-spec.cjs --catalog-budget` exits 0 and prints a byte total between 12000 and 14000 (matches RESEARCH's measured 12,966).
    - `node scripts/check-skill-spec.cjs --plugin-root-census` exits 0 and reports count between 45 and 55 (matches RESEARCH's measured 51).
    - `grep -c '"skills-ref"' package.json` returns 0.
    - `node -e "require('./package-lock.json')"` parses without error (lockfile stays valid JSON after uninstall).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-skill-spec.cjs --catalog-budget && node scripts/check-skill-spec.cjs --plugin-root-census && ! grep -q '"skills-ref"' package.json</automated>
  </verify>
  <done>check-skill-spec.cjs exists with all three functions working and CLI-routed; skills-ref is gone from package.json/package-lock.json.</done>
</task>

<task type="auto">
  <name>Task 2: tests/run-all-234.sh harness + no-instructions test + D-10 adoption-engine gate test</name>
  <files>tests/run-all-234.sh, lib/mcp/no-instructions.test.cjs, tests/test-234-adoption-engine-gate.cjs</files>
  <read_first>
    tests/run-all-233.sh (the full 219-line file, already read during planning -- copy its structure verbatim: `run`/`run_may_skip`/`strip_comments` helpers, glob-discovery loop, PART8_TARGETS array + PART8_RE + PART8_ALLOW + the negative-self-test-first idiom, tail PASS/FAIL/SKIP summary);
    tests/test-127-00-shim-handshake.sh (the JSON-RPC initialize/notifications-initialized/tools-list handshake pattern over stdio -- the exact `clientInfo: {name, version}` shape to mimic);
    lib/core/tier0-messaging.test.cjs (lines 31-45, the bare node:assert/strict + hand-rolled `pass`/`check(label,cond)` counter pattern -- NOT node:test);
    234-PATTERNS.md "NEW tests/test-234-*.cjs" and "NEW tests/run-all-234.sh" sections (the exact naming-convention correction: files must be named tests/test-234-*.cjs to be glob-discovered, NOT lib/mcp/*.test.cjs, EXCEPT this task's file is deliberately placed at lib/mcp/no-instructions.test.cjs to match VALIDATION.md's own naming -- add an EXPLICIT extra `run` leg for it in run-all-234.sh rather than relying on the tests/test-234-* glob, since the glob will not discover it);
    skills/setup/SKILL.md, skills/brain-connector/SKILL.md, commands/setup.md (the three files legitimately referencing resolve-brain-key/brainPlan -- the D-10 gate's allowlist, confirmed by grep during planning: `grep -rl "resolve-brain-key\|brainPlan" commands skills --include=*.md` returns exactly these three).
  </read_first>
  <action>
    Create tests/run-all-234.sh by copying tests/run-all-233.sh's structure byte-for-byte in shape (header prose rewritten for Phase 234's own gate, `set -uo pipefail`, `run`/`run_may_skip`/`strip_comments`, `shopt -s nullglob` discovery of `tests/test-234-*.cjs` and `tests/test-234-*.sh`). After the glob-discovery block, add ONE explicit extra leg: `run "no-instructions.test.cjs" node lib/mcp/no-instructions.test.cjs` (this file lives outside the glob pattern by design, matching VALIDATION.md's own path). Add a leg running `node scripts/check-skill-spec.cjs --check` (expected to FAIL/exit-1 until 234-03/234-04 land -- use `run`, not `run_may_skip`; a real FAIL count here is the honest, expected phase-in-progress signal, not something to suppress). Add the Part 8 tripwire section (self-test first, exactly as tests/run-all-233.sh does it) with an INITIAL `PART8_TARGETS` array containing: `scripts/check-skill-spec.cjs`, `lib/core/navigation.cjs`, `lib/core/resolve-brain-key.cjs`, `lib/mcp/tools/graph.cjs`, `lib/mcp/tools/chain.cjs`, `lib/mcp/tools/views.cjs` (these last five are pre-existing chokepoints this phase's later plans touch or rely on; sweeping them now against the SAME `PART8_RE`/`PART8_ALLOW` used in run-all-233.sh gives 234-05 a clean baseline to extend rather than re-deriving the pattern). Note: `lib/core/resolve-brain-key.cjs` legitimately contains the string `MINDRIAN_BRAIN_KEY` and env-var handling -- the `brain` token in `PART8_RE` is about egress calls/identifiers, not the mere existence of the env-var NAME; if the self-test's `must_not_catch` cases do not already cover this shape, add one: `must_not_catch "brain key env var name, no scheme" "  const key = process.env.MINDRIAN_BRAIN_KEY;"` should in fact be CAUGHT (it IS a `brain` identifier, matching run-all-233.sh's own `must_catch "brain identifier"` case) -- so instead add `resolve-brain-key.cjs`'s specific lines that reference the constant to `PART8_ALLOW` by exact line if the sweep flags them, with the reason written inline ("reads a local env var name, never a network call").
    Create lib/mcp/no-instructions.test.cjs: spawn `node bin/mindrian-mcp-server.cjs` via `node:child_process`, write a JSON-RPC `initialize` request over stdin (protocolVersion, empty capabilities, `clientInfo: {name: "test-234-no-instructions", version: "1.0.0"}`), read the response, assert the result object has NO `instructions` key (`assert.equal(Object.prototype.hasOwnProperty.call(result, 'instructions'), false)`). Kill the child process after. Use the bare `assert.ok`/`check(label, cond)` counter pattern (not `node:test`), non-zero exit on any failure.
    Create tests/test-234-adoption-engine-gate.cjs: grep every `commands/*.md` and `skills/*/SKILL.md` file for the literal strings `resolve-brain-key` or `brainPlan`, EXCLUDING the three allowlisted files (`skills/setup/SKILL.md`, `skills/brain-connector/SKILL.md`, `commands/setup.md`), assert the remaining match count is exactly 0. If a future skill legitimately needs to reference Brain-key resolution (e.g. a new connector-setup skill), it must be added to this file's explicit allowlist array with a written reason -- the test must fail loudly on an unexplained new match, never silently pass.
  </action>
  <acceptance_criteria>
    - `bash tests/run-all-234.sh` completes (nonzero total FAIL is expected right now because of the intentional `check-skill-spec.cjs --check` failing leg -- confirm the SUMMARY line prints `PASS=` with at least the Part-8-self-test and no-instructions legs among the passes).
    - `node lib/mcp/no-instructions.test.cjs` exits 0 standalone.
    - `node tests/test-234-adoption-engine-gate.cjs` exits 0 standalone (the 3-file allowlist means this passes today, before any other plan runs).
    - `grep -n "no-instructions.test.cjs" tests/run-all-234.sh` returns a match (the explicit extra leg is present, not relying on glob).
  </acceptance_criteria>
  <verify>
    <automated>node lib/mcp/no-instructions.test.cjs && node tests/test-234-adoption-engine-gate.cjs</automated>
  </verify>
  <done>run-all-234.sh exists and runs end to end; no-instructions and adoption-engine-gate tests both pass standalone today.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| skills/ catalog -> foreign host skill loader | Untrusted in the sense that any host's loader parses this content; a malformed frontmatter field is the failure mode this validator exists to catch before a host does. |
| package.json dependency tree -> npm install | Untrusted package identity (the skills-ref name-collision). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-234-01 | Tampering | npm dependency `skills-ref` (unaffiliated, no source repo, stale) | mitigate | Remove via `npm uninstall skills-ref` this task; validator built in-repo instead. |
| T-234-02 | Information Disclosure | check-skill-spec.cjs reading skills/ content | accept | Pure local fs read via gray-matter, zero network, zero fs write in this file. |
| T-234-SC | Tampering | npm installs during this task (none new added -- only a removal) | accept | No new package added; removal only. If a future task in this phase adds a package, it goes through the blocking human-verify legitimacy checkpoint per the Package Legitimacy Gate protocol. |
</threat_model>

<verification>
`bash tests/run-all-234.sh` runs end to end (some legs intentionally red at this point in the phase -- the `check-skill-spec.cjs --check` leg). `node lib/mcp/no-instructions.test.cjs` and `node tests/test-234-adoption-engine-gate.cjs` both green standalone.
</verification>

<success_criteria>
- scripts/check-skill-spec.cjs exists, exports validateSkill/checkAll/catalogBudget/pluginRootCensus, CLI-routed with the three flags.
- tests/run-all-234.sh exists, glob-discovers tests/test-234-*, runs the explicit no-instructions leg, runs the Part 8 tripwire with its self-test.
- npm skills-ref is removed from package.json and package-lock.json.
- D-03 (no instructions leak) and D-10 (no paid gate on methodology) are now locked, automated, and green.
</success_criteria>

<output>
Create `.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-01-SUMMARY.md` when done
</output>
