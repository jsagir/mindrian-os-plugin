---
phase: 234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp/tool-router.cjs
  - lib/mcp/tools/room.cjs
  - tests/test-234-tool-description-floor.cjs
autonomous: true
requirements: [D-03]

must_haves:
  truths:
    - "Every one of the 33 default-registered MCP tool descriptions reads as an instruction (what it does + when to use it), not a bare label, and is at least 120 characters."
  artifacts:
    - path: "tests/test-234-tool-description-floor.cjs"
      provides: "Drives tools/list over stdio JSON-RPC and asserts every description length >= 120 chars"
  key_links:
    - from: "tests/test-234-tool-description-floor.cjs"
      to: "bin/mindrian-mcp-server.cjs"
      via: "spawn + JSON-RPC initialize -> notifications/initialized -> tools/list"
      pattern: "tools/list"
---

<objective>
Close RESEARCH.md's Section F gap: 8 of the 33 default MCP tool descriptions (`meeting` 66 chars, `room_graph` 68, `room_list` 76, `analysis` 77, `room_content` 78, `room_search` 85, `export` 86, `orchestration` 91) read as bare labels, not instructions -- a direct violation of D-03's "tool descriptions are load-bearing product copy... written as instructions, not labels" since on a Tier-0 host, tool descriptions are the ONLY universally honored persona/guidance channel (persona itself ships as a SKILL per D-03, but the TOOLS still need instruction-quality copy for a model on a foreign host to use them well).

Purpose: rewrite the 8 short descriptions to instruction-quality prose (matching the in-repo exemplars `chain_run` 552 chars, `stop_gate_check` 460, `framework_run` 424), then lock the floor with an automated test so it cannot silently regress.
Output: 8 rewritten tool descriptions across 2 files; `tests/test-234-tool-description-floor.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-CONTEXT.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the 8 sub-120-char tool descriptions to instruction-quality prose + lock with a floor test</name>
  <files>lib/mcp/tool-router.cjs, lib/mcp/tools/room.cjs, tests/test-234-tool-description-floor.cjs</files>
  <read_first>
    lib/mcp/tool-router.cjs lines 661-664 (`room_state`, the dynamic-suffix exemplar -- do not touch, it is already long via `${compact.slice(0,80)}`), lines 731-734 (`room_content`, 78 chars), lines 893-896 (`room_graph`, 68 chars), lines 1111-1114 (`methodology`, already long, do not touch), lines 1156-1159 (`analysis`, 77 chars), lines 1210-1213 (`intelligence`, already long, do not touch), lines 1360-1363 (`meeting`, 66 chars), lines 1411-1414 (`export`, 86 chars), lines 1446-1449 (`orchestration`, 91 chars) -- these are the exact `server.tool('&lt;name&gt;', '&lt;description&gt;', {...` call sites to edit, description string is the SECOND argument, a plain JS string literal;
    lib/mcp/tools/room.cjs lines 218-219 (`room_list`, 76 chars) and lines 261-262 (`room_search`, 85 chars);
    234-RESEARCH.md "F. Tool descriptions that are labels, not instructions" table (exact current lengths) and the "in-repo model to follow" reference to `chain_run`/`stop_gate_check`/`framework_run` -- read `chain_run`'s full description in lib/mcp/tools/chain.cjs and `stop_gate_check`'s in lib/mcp/stop-gate-handler.cjs to match tone/structure (what it does, then when/why to use it);
    tests/test-127-00-shim-handshake.sh (JSON-RPC initialize/tools-list handshake pattern, same as 234-01's no-instructions.test.cjs).
  </read_first>
  <behavior>
    - Test: spawn `node bin/mindrian-mcp-server.cjs`, complete the initialize handshake, send `tools/list`, assert every returned tool's `description.length >= 120`. Before the rewrite, this test is RED for exactly the 8 named tools (verify the RED state by running it first and confirming the failure list matches the 8 names).
    - Test: assert the 8 rewritten descriptions each still start with a capital letter, end with a period, and do not exceed 600 chars (sanity bounds matching the exemplars' range).
  </behavior>
  <action>
    For each of the 8 tools, replace the description string literal in place (same call site, same second argument position, no signature change) with instruction-quality prose following the `chain_run`/`stop_gate_check`/`framework_run` pattern: state what the tool does, then state when/why a model should reach for it, in plain prose, no em-dashes (hyphens only per CLAUDE.md). Concretely:
    - `room_graph` (lib/mcp/tool-router.cjs:893): expand beyond "Knowledge graph operations, Minto reasoning, and room visualization." to name the specific sub-operations it routes to and when a model should prefer this over the standalone `graph_query` tool.
    - `room_content` (lib/mcp/tool-router.cjs:731): expand beyond "Manage room content: projects, opportunities, funding, personas, integrations." to state this is the CRUD surface for room entities and when to reach for it vs. `room_state`.
    - `analysis` (lib/mcp/tool-router.cjs:1156): expand beyond "Systems analysis, trend exploration, causal reasoning, and scenario planning." with guidance on when a model should invoke systems analysis vs. the `intelligence` tool's calibrated assessment.
    - `meeting` (lib/mcp/tool-router.cjs:1360): expand beyond "Meeting filing, intelligence pipeline, and speaker identification." to explain this is how a raw meeting transcript becomes structured Data Room content.
    - `export` (lib/mcp/tool-router.cjs:1411): expand beyond "Generate dashboards, wikis, presentations, snapshots, and radar charts from room data." with guidance on choosing between the export formats.
    - `orchestration` (lib/mcp/tool-router.cjs:1446): expand beyond "Autonomous execution, multi-room management, scout intelligence, and system administration." with a caution about when autonomous execution is and is not appropriate to invoke.
    - `room_list` (lib/mcp/tools/room.cjs:219): expand beyond "List room directories under MINDRIAN_ROOMS_HOME. Read-only, no side effects." with guidance on when to call this vs. `room_state`/`room_bind`.
    - `room_search` (lib/mcp/tools/room.cjs:262): expand beyond "Search markdown entries within this session's bound room. Read-only, no side effects." with guidance on what kinds of queries it handles well.
  </action>
  <acceptance_criteria>
    - `node tests/test-234-tool-description-floor.cjs` exits 0 (GREEN) after the rewrite, having been confirmed RED (with exactly the 8 named failures) before it.
    - `node -e "const {z}=require('zod'); require('./lib/mcp/tool-router.cjs')"` does not throw (no syntax regression from the string edits).
    - Every one of the 8 descriptions is now >=120 chars, matching D-03's floor.
    - No tool's third argument (the zod schema object) or fourth argument (the handler) was touched -- only the description string literal changed.
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-234-tool-description-floor.cjs</automated>
  </verify>
  <done>All 33 default MCP tool descriptions read as instructions; the floor test is green and glob-discovered by tests/run-all-234.sh once 234-01 lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Tool description string -> model reasoning | Not an attacker-controlled boundary; this is product-copy quality, not an input-validation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-234-03 | N/A (no security-relevant threat in this plan) | lib/mcp/tool-router.cjs, lib/mcp/tools/room.cjs | accept | Pure string-literal edits to tool metadata; no new input path, no new fs/network access introduced. |
</threat_model>

<verification>
`node tests/test-234-tool-description-floor.cjs` green. `bash tests/run-all-234.sh` (once 234-01 lands) glob-discovers this test automatically since it is named `tests/test-234-*.cjs`.
</verification>

<success_criteria>
All 33 default MCP tool descriptions are >=120 chars and read as instructions per D-03; the floor is locked by an automated regression test.
</success_criteria>

<output>
Create `.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-02-SUMMARY.md` when done
</output>
