---
phase: 234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core
plan: 03
type: execute
wave: 2
depends_on: ["234-01"]
files_modified:
  - scripts/migrate-skill-frontmatter.cjs
  - skills/**/SKILL.md (123 of 125 -- excludes MOSDeckEngine and value-proposition, handled in 234-04)
autonomous: true
requirements: [D-01, D-02, D-07, D-08]

must_haves:
  truths:
    - "check-skill-spec.cjs --check reports zero missing-name and zero allowed-tools-as-list violations across the 123 non-special skills."
    - "Every one of the 123 skills carries a license: field pointing at BSL-1.1 / LICENSE."
    - "The 15 disable-model-invocation skills and the hook-referencing skills carry a compatibility: field naming Claude Code / Tier-1 requirements."
    - "No hand-written YAML comment (e.g. connector.excluded blocks) in any touched SKILL.md was silently deleted by the migration."
  artifacts:
    - path: "scripts/migrate-skill-frontmatter.cjs"
      provides: "One-time targeted-text-edit codemod: insert missing name:, normalize allowed-tools list->string, add license:, add compatibility: to specific skills"
  key_links:
    - from: "scripts/migrate-skill-frontmatter.cjs"
      to: "scripts/check-skill-spec.cjs"
      via: "post-migration verification: node scripts/check-skill-spec.cjs --check"
      pattern: "check-skill-spec\\.cjs --check"
---

<objective>
Close 7 of the 9 hard Agent Skills spec failures (the 7 missing-`name` skills; `MOSDeckEngine` and `value-proposition` are excluded here and handled as their own decision-task in 234-04), normalize `allowed-tools` from a YAML list to the spec-required space-separated string across the 123 non-special skills, add the spec's optional `license:` field (making BSL-1.1 legible at the skill level, per RESEARCH's finding that 0/125 skills currently carry it), and add `compatibility:` to the skills whose bodies depend on Claude-Code-only mechanics (the 15 named `disable-model-invocation` skills, plus the hook-referencing skills).

Purpose: this is Wave 1's "mechanical, verifiable with the official skills-ref validator" work RESEARCH's Primary Recommendation names -- the highest-value, lowest-risk fix in the phase.
Output: `scripts/migrate-skill-frontmatter.cjs`; 123 SKILL.md files updated in place.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-CONTEXT.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-RESEARCH.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-PATTERNS.md
LICENSE
scripts/check-skill-spec.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codemod -- insert missing name:, normalize allowed-tools, add license: across 123 skills</name>
  <files>scripts/migrate-skill-frontmatter.cjs, skills/**/SKILL.md</files>
  <read_first>
    scripts/check-skill-spec.cjs (this plan's Task 1 output must make `--check` pass for these 123 skills -- read its exact `validateSkill` rules first);
    skills/auto-explore/SKILL.md (the exemplar shape for a missing-`name` skill, per 234-PATTERNS.md: `description` first, no `name`, `allowed-tools` as a mixed-quote YAML list);
    skills/larry-personality/SKILL.md (the exemplar for a COMPLIANT skill carrying a hand-written comment block `# --- Phase 172-06 CIRS R1 exclude (Canon Part 11) ---` immediately before its `connector:` key -- this comment MUST survive the migration byte-for-byte);
    LICENSE (the exact BSL-1.1 parameters block -- Licensor, Change Date 2030-04-16, Change License Apache-2.0 -- for the license: field text);
    234-RESEARCH.md "B. Spec deviations" table (the exact 63 non-spec top-level key census, the `allowed-tools` deviation count of 112/125, and the "important caution on the metadata: migration" paragraph -- do NOT act on that caution here, it is explicitly out of scope for this plan);
    scripts/skillopt-triggerloop.cjs lines 440-448 (the ONLY in-repo precedent for `matter.stringify()` write-back to a SKILL.md -- note it operates on a disposable SCRATCH COPY, never the canonical skills/ tree; this is why this task uses targeted text edits instead, not that pattern).
  </read_first>
  <action>
    Create scripts/migrate-skill-frontmatter.cjs as a one-time CJS codemod (not a permanent gate; does not need a `connectors` export or Part 8 sweep entry). CRITICAL correctness constraint: do NOT use `gray-matter`'s parse-then-`matter.stringify()` round-trip for any WRITE in this file -- `matter.stringify()` re-serializes from the parsed JS object via `js-yaml` and silently drops hand-written YAML comments (confirmed during planning: `larry-personality/SKILL.md`'s `# --- Phase 172-06 CIRS R1 exclude ---` comment and other skills' inline comments would be deleted on a naive round-trip; the only in-repo precedent for `matter.stringify()` write-back operates on a disposable scratch copy, not the canonical tree). Instead, perform TARGETED TEXT-LEVEL edits that touch only the specific lines being changed, leaving every other byte (including comments) untouched:
    - Missing `name:` (7 skills: auto-explore, brain-derive, dial-memory-refresh, dogfood-flush, explain-decision, feynman-timeline-refresh, mva-report): read the file as raw text, find the opening `---\n`, insert a new line `name: &lt;dirname&gt;\n` immediately after it (before whatever the current first frontmatter key is), write the file back with `fs.writeFileSync` (plain string concatenation, not gray-matter).
    - `allowed-tools` as a YAML list (112 skills, minus the 2 excluded): match the block via a regex anchored on `^allowed-tools:\s*$` through the following indented `- ` lines (handle both quoted `"Bash"` and bare `AskUserQuestion` list-item forms, per 234-PATTERNS.md's mixed-quoting example), extract the tool names, replace the whole matched block with a single line `allowed-tools: &lt;space-separated names&gt;\n`. Use `gray-matter` in READ-ONLY mode first (parse each file, read `.data['allowed-tools']`) to get the authoritative parsed list for each skill (this avoids hand-rolling YAML list parsing), then perform the actual file WRITE via the targeted regex-block-replace against the raw text, never via `matter.stringify()`.
    - `license:` (all 123 skills): insert a new line `license: BSL-1.1. See LICENSE for complete terms (Business Source License 1.1, Change Date 2030-04-16 to Apache License 2.0).\n` immediately after the `description:` field's block ends (handle both single-line and `description: >` folded-scalar forms -- for a folded scalar, insert after the fully-dedented block, i.e. after the last continuation line).
    - `compatibility:` for the 15 named `disable-model-invocation` skills (dial-memory-refresh, export, brain-derive, explain-decision, dogfood-flush, feynman-timeline-refresh, publish, memory, jtbd, mva-report, ingest-methodology, pws-brain, operator, vault, snapshot): insert `compatibility: Requires Claude Code (or a host implementing disable-model-invocation semantics); Tier-1 hook mechanics referenced in this skill.\n` after the license: line just added.
    - `compatibility:` for hook-referencing skills: grep every skill body (post-frontmatter content) for the literal strings `Stop gate`, `contradiction push`, or `hooks.json` (case-sensitive on the first two, matching RESEARCH's "Tier-1-only per D-02" framing); for each match not already covered by the disable-model-invocation list above, insert `compatibility: References Tier-1 hook mechanics (Claude Code, Grok Build, or OpenCode via the SEED-063 plugin); degrades to prose guidance on Tier-0 hosts.\n` in the same position. Print the discovered file list to stdout so the executor's SUMMARY.md can record the exact count found (RESEARCH estimated 16; the actual grep result is authoritative).
    Run the codemod once against `skills/*/SKILL.md` EXCLUDING `skills/MOSDeckEngine/SKILL.md` and `skills/value-proposition/SKILL.md` (pass an explicit exclude list, do not rely on the codemod happening to skip them). Commit the codemod script alongside its one-time output.
  </action>
  <acceptance_criteria>
    - `node scripts/check-skill-spec.cjs --check` output no longer lists any of the 7 named missing-`name` skills.
    - `grep -rL "^license:" skills --include=SKILL.md | grep -v MOSDeckEngine | grep -v value-proposition | wc -l` returns 0 (every non-excluded skill has a license line).
    - `grep -rlE "^allowed-tools:\s*$" skills --include=SKILL.md | grep -v MOSDeckEngine | grep -v value-proposition | wc -l` returns 0 (no remaining multi-line list form outside the two excluded skills).
    - `grep -c "Phase 172-06 CIRS R1 exclude" skills/larry-personality/SKILL.md` returns 1 (the hand-written comment survived, proving the targeted-edit approach did not destroy it) -- run this even though larry-personality itself needed no name/allowed-tools/license changes (it already has `name:`), as the canonical "did comments survive" tripwire; also spot-check one skill that DID need an `allowed-tools` rewrite and already carried an unrelated inline comment elsewhere in its frontmatter, confirming that comment is also still present.
    - `node scripts/check-skill-spec.cjs --catalog-budget` still exits 0 (license/compatibility fields do not enter the byte count, which is name+description only -- confirm the budget is unaffected).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-skill-spec.cjs --check 2>&1 | grep -c "auto-explore\|brain-derive\|dial-memory-refresh\|dogfood-flush\|explain-decision\|feynman-timeline-refresh\|mva-report" | grep -qx 0</automated>
  </verify>
  <done>123 skills carry name:/allowed-tools-as-string/license:; the 15+N compatibility-eligible skills carry compatibility:; zero hand-written comments were lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| skills/*/SKILL.md content -> foreign host skill catalog | The migrated frontmatter is what a foreign host's loader parses; malformed output here would break the catalog on every host simultaneously. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-234-04 | Tampering | scripts/migrate-skill-frontmatter.cjs writing 123 files | mitigate | Targeted text-level edits only (no full-file regeneration), verified post-run against check-skill-spec.cjs and an explicit comment-survival grep. |
| T-234-05 | Information Disclosure | license: field text | accept | Public license terms only, no secret material. |
</threat_model>

<verification>
`node scripts/check-skill-spec.cjs --check` no longer lists the 7 formerly-missing-name skills. `bash tests/run-all-234.sh` (once run) shows the check-skill-spec leg's failure count dropping from 9 to 2 (only MOSDeckEngine + value-proposition remain, owned by 234-04).
</verification>

<success_criteria>
123 of 125 skills are fully Agent-Skills-spec-compliant on name/allowed-tools, carry a license: field, and the Tier-1-dependent subset carries compatibility:.
</success_criteria>

<output>
Create `.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-03-SUMMARY.md` when done
</output>
