---
phase: 234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core
plan: 08
type: execute
wave: 4
depends_on: ["234-02", "234-05", "234-06", "234-07"]
files_modified:
  - tests/test-234-free-core-network-scan.cjs
autonomous: false

requirements: [D-06, D-08, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "All Wave-0 gates (W0-01 through W0-09 in 234-VALIDATION.md) are green in one full run of tests/run-all-234.sh."
    - "A real, non-Claude-Code host has been installed and observed loading the MindrianOS skill catalog and connecting to the mindrian-os MCP server -- not merely inferred from static analysis."
    - "No touched or newly-created SKILL.md carries genuinely proprietary logic (graph queries, scoring, ranking, grading) -- confirmed by human review, per D-06."
    - "The free/paid boundary (D-08/D-09) holds with zero network tokens in the local MCP tool surface, confirmed by an adversarial grep instance scoped to this phase's touched files."
    - "The Dev-Research Compositing entry is filed in both the phase directory and ~/MindrianRooms/rethinking-mindrianos/research/, cross-referenced."
  artifacts:
    - path: "tests/test-234-free-core-network-scan.cjs"
      provides: "234-scoped instance of the Canon Part 8 adversarial grep: zero network tokens in lib/mcp/tools/*.cjs, lib/core/navigation.cjs, lib/core/resolve-brain-key.cjs"
  key_links:
    - from: "tests/test-234-free-core-network-scan.cjs"
      to: "tests/run-all-234.sh"
      via: "glob-discovered as tests/test-234-*.cjs"
      pattern: "free-core-network-scan"
---

<objective>
Close the phase: run the full Wave-0-through-Wave-3 gate suite as one aggregate check, add the one still-missing automated instance (D-08/D-09's free-core network scan, scoped to this phase per VALIDATION.md), and clear the phase's single largest open caveat -- RESEARCH.md's own words: "no foreign host is installed on this machine... every Tier-0 portability claim in this research is derived from specifications and from static analysis of the repo, not from a live run on VS Code, Cursor, Goose, or Zed." Everything through 234-07 is plausible. This plan is what makes it observed.

Purpose: file the mandatory Dev-Research Compositing entry (CLAUDE.md's standing rule for any phase touching MindrianOS's own architecture) so the durable reasoning trail lives in the `rethinking-mindrianos` room, cross-referenced back to this phase.
Output: `tests/test-234-free-core-network-scan.cjs`; a cleared human-verify checkpoint with recorded foreign-host evidence; a filed Dev-Research Compositing entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-CONTEXT.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-RESEARCH.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-08/D-09 free-core network scan (234-scoped instance) + full-suite closing run</name>
  <files>tests/test-234-free-core-network-scan.cjs</files>
  <read_first>
    tests/run-all-233.sh lines 140-142, 175-186 (the `PART8_RE`/`PART8_ALLOW` pattern and the negative-self-test idiom -- reuse the SAME regex text, do not invent a new one, per VALIDATION.md's framing that this "pattern already exists... needs a 234-scoped instance");
    234-VALIDATION.md's Per-Task Verification Map row for D-08/D-09 (the exact three target files this scan must cover: `lib/mcp/tools/*.cjs`, `lib/core/navigation.cjs`, `lib/core/resolve-brain-key.cjs`);
    234-01-PLAN.md's Task 2 (whatever resolution it reached for the `resolve-brain-key.cjs`-carries-the-literal-string-`MINDRIAN_BRAIN_KEY` false-positive tension against the case-sensitive `brain` token in PART8_RE -- reuse that exact resolution here rather than re-deriving it).
  </read_first>
  <action>
    Create tests/test-234-free-core-network-scan.cjs as a standalone Node script (not shelling out to bash) implementing the same negative-self-test-first discipline as run-all-233.sh's Part 8 sweep: plant every forbidden token (`fetch(`, `https://`, `require('node:http`, `curl`, `wget`, `axios`, `onrender`, `api.anthropic`, case-sensitive `brain`) on a synthetic in-memory string and assert the pattern catches each one, plus a comment line and the `resolve-brain-key.cjs`-style `MINDRIAN_BRAIN_KEY` env-var-name shape that must NOT false-positive as a network call (only if 234-01's harness work surfaced this distinction as needing an explicit allow -- reuse whatever resolution 234-01 already established for this exact tension). Then sweep every file matching `lib/mcp/tools/*.cjs` (glob-discovered, not a fixed list -- unlike run-all-233.sh's fixed PART8_TARGETS array, `lib/mcp/tools/` is a directory of many small independent per-tool modules that grows over time, so a glob is the correct maintenance-free approach here and avoids a target list silently going stale as new tool modules are added in future phases), plus the two explicitly-named files `lib/core/navigation.cjs` and `lib/core/resolve-brain-key.cjs`, comment-stripped, against the same regex. Assert zero forbidden matches across all of them. Exit non-zero on any hit or on the self-test failing.
  </action>
  <acceptance_criteria>
    - `node tests/test-234-free-core-network-scan.cjs` exits 0.
    - The self-test section demonstrably catches all 9 forbidden-token shapes before the real sweep runs (printed to stdout, matching the run-all-233.sh convention of showing "caught: &lt;label&gt;" per case).
    - `bash tests/run-all-234.sh` (full run) shows PASS for every leg: check-skill-spec --check (now 0 failures, post-234-03/04), host-tier test, tool-description-floor test, dist-bundle test, plugin-root-migrated test, no-instructions test, adoption-engine-gate test, free-core-network-scan test, Part 8 tripwire.
    - `node scripts/build-connector-registry.cjs --check`, `node scripts/check-shape-declaration.cjs --check`, and `node scripts/doctor.cjs --acceptance` all run clean (or show only pre-existing, already-logged unrelated failures -- do not treat a pre-existing unrelated doctor failure as this phase's blocker, but DO record its exact identity in the SUMMARY so it is not silently conflated with phase-234 work).
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-234-free-core-network-scan.cjs && bash tests/run-all-234.sh</automated>
  </verify>
  <done>The free-core boundary is confirmed clean by an automated, self-proving scan; the full phase-234 gate suite is green end to end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Foreign-host live verification (VS Code/Copilot, Cursor, or Goose recommended)</name>
  <what-built>
    Everything in Phase 234: 125 spec-compliant skills (0 hard failures, license/compatibility fields added, allowed-tools normalized), a two-axis host-tier + write-path default (foreign hosts get graph_write/memory_event/artifact_file usable by default), instruction-quality tool descriptions, a MINDRIAN_OS_ROOT-first portable root resolution for every skill's shell-outs, and two generated distribution bundles (dist/generic-claude-dir/ for the ~30+ VS-Code-family hosts, dist/zed/ for Zed's flat loader). Every claim up to this point has been verified by static analysis and automated tests ONLY -- no foreign host has been installed on this machine during this phase.
  </what-built>
  <action>
    Present the fully-automated Phase 234 build to the navigator for the one verification step Claude cannot automate: installing a real third-party host on this machine and observing it. On approval, this is the phase's closing gate -- no further code changes follow. On a reported break, capture it as a gap item and route back to the owning plan (234-05 for tool-visibility/write-path issues, 234-01/03/04/06 for catalog-loading issues, 234-07 for bundle-structure issues) rather than patching ad hoc inside this checkpoint.
  </action>
  <how-to-verify>
    1. Install ONE non-Claude-Code Agent-Skills-compliant host locally. Recommended, in order (per RESEARCH.md's build-order and market-share framing): VS Code + GitHub Copilot, or Cursor, or Goose (the full-channel reference implementation).
    2. Point the host at either `dist/generic-claude-dir/.claude/skills/` (copy or symlink into the host's expected skills directory -- for VS Code/Cursor/Copilot this is typically a workspace-level `.claude/skills/` per the Agent Skills spec's discovery convention; confirm the exact path in that host's own docs) or at `skills/` directly if the host reads `.claude/skills/` relative to a project root and this repo's layout already satisfies it.
    3. Confirm the skill catalog loads without error in the host's UI (skill names + descriptions visible, no parse errors reported for any of the 125).
    4. Configure the host's MCP client to launch `dist/generic-claude-dir/.mcp.json`'s `mindrian-os` server (or point directly at `node bin/mindrian-mcp-server.cjs` with `MINDRIAN_OS_ROOT` set to this repo's absolute path). Confirm the host successfully connects and a `tools/list` call (via whatever UI the host exposes for inspecting available tools) shows the 33+ MindrianOS tools, INCLUDING `graph_write`, `memory_event`, and `artifact_file` (Gap D's fix -- these should be visible and, if the host is confidently non-Claude-Code, callable without setting MINDRIAN_MCP_FIRST by hand).
    5. Human review pass (D-06): spot-check 3-5 of the SKILL.md files this phase touched or created (e.g. `skills/mos-deck-engine/SKILL.md`, `skills/validate-proposition/SKILL.md`, any of the 123 mechanically-fixed skills) and confirm none carries genuinely proprietary logic (graph queries, scoring, ranking, grading algorithms) -- only frontmatter metadata and pre-existing methodology prose were touched.
    6. Axis-language review (D-12): skim this phase's 8 PLAN.md files (or their SUMMARYs) and confirm no task conflated host-capability tier, commercial tier, Tri-Polar surface, and model capability under a bare "tier 1"/"tier 0" without naming which axis.
  </how-to-verify>
  <acceptance_criteria>
    - The skill catalog loads on the tested host with zero parse errors across all 125 skills.
    - The mindrian-os MCP server connects and tools/list includes graph_write, memory_event, artifact_file.
    - D-06 proprietary-content spot-check comes back clean.
    - D-12 axis-language spot-check comes back clean.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the host name you tested (e.g. "approved, tested on VS Code + Copilot"), or describe what broke.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: File the Dev-Research Compositing entry</name>
  <files>~/MindrianRooms/rethinking-mindrianos/research/&lt;dated-entry&gt;/ (outside this repo -- filed per CLAUDE.md's Dev-Research Compositing mandate, mirrored to mindrianOS/research/)</files>
  <read_first>
    CLAUDE.md's "Dev-Research Compositing (Rethinking Room)" section (the exact two-homes requirement: the phase's own CONTEXT.md/PLAN set is the executable decision, the room entry is the durable reasoning trail; neither substitutes for the other);
    234-RESEARCH.md in full (already read this session -- this is the source material the room entry composites from: the "far closer to done than assumed" finding, the Package Legitimacy Audit's skills-ref name-collision, the getClientVersion() timing constraint, the Zed budget correction, the ~45-client ecosystem update).
  </read_first>
  <action>
    Create a dated research entry under `~/MindrianRooms/rethinking-mindrianos/research/2026-07-28-phase-234-infrastructure-open-core/` (or the next available dated slug if that path is taken) summarizing: the headline finding that Phase 234 was mostly-already-built infrastructure connection work, not new-build; the concrete gap list closed (9 hard spec failures, 112 allowed-tools deviations, 51 CLAUDE_PLUGIN_ROOT references, the Tier-0 write-path hole); the npm `skills-ref` name-collision finding (a real, previously-undetected dependency-hygiene issue this phase fixed); the `getClientVersion()` boot-time-vs-per-call timing constraint as a reusable MCP SDK lesson for future phases; and the honest open items (entitlement/pricing deferred per SEED-069, dist/ update-path resolved via staleness-detection rather than new infra). Cross-reference back to `.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/` by relative path. Mirror the same entry to `mindrianOS/research/` per CLAUDE.md's "source-of-record" instruction if that directory exists in this environment; if it does not exist or is unreachable from this session, note that explicitly in the SUMMARY rather than silently skipping it.
  </action>
  <acceptance_criteria>
    - The dated research entry file exists under `~/MindrianRooms/rethinking-mindrianos/research/` and is non-empty.
    - It contains an explicit relative-path cross-reference to this phase's `.planning/phases/234-.../` directory.
    - The SUMMARY.md for this plan states plainly whether the `mindrianOS/research/` mirror succeeded, was skipped with a reason, or is not applicable in this environment.
  </acceptance_criteria>
  <verify>
    <automated>test -d ~/MindrianRooms/rethinking-mindrianos/research 2>/dev/null && find ~/MindrianRooms/rethinking-mindrianos/research -newer .planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-CONTEXT.md -type f | grep -q .</automated>
  </verify>
  <done>The durable reasoning trail is filed in the rethinking-mindrianos room, cross-referenced to this phase, per CLAUDE.md's mandatory two-homes rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Foreign host runtime (Task 2) -> this repo's MCP server process | The first time this phase's server code runs under a REAL, uncontrolled third-party MCP client rather than a test harness driving JSON-RPC by hand. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-234-16 | Information Disclosure | Free core (local MCP tools) reaching the network under any code path | mitigate | tests/test-234-free-core-network-scan.cjs, self-proving adversarial grep, this task. |
| T-234-17 | Repudiation | Proprietary-content check being skipped or rubber-stamped | mitigate | Explicit named checklist item in the human-verify checkpoint (D-06), not folded silently into "approved" with no criteria. |
| T-234-18 | Elevation of Privilege | A real foreign host proving the write-path default grants MORE than intended | mitigate | Checkpoint step 4 explicitly directs the human to confirm write-path tools are visible AND to test the actual gated behavior, not just tool-list presence, closing the loop RESEARCH's Assumption A2 flags ("a tool can be present and still depend on a Claude-Code-only path at call time -- only a live foreign-host run settles this"). |
</threat_model>

<verification>
`bash tests/run-all-234.sh` full green run. Human-verify checkpoint cleared with a named real foreign host and recorded evidence. Dev-Research Compositing entry filed and cross-referenced.
</verification>

<success_criteria>
Phase 234 is verified, not merely plausible: every Wave-0 gate is green, the free-core/paid boundary scan passes (D-08/D-09), a real foreign host has been observed loading the catalog and connecting to the MCP server with a working write path, D-06/D-12 have been human-reviewed, D-13 is confirmed (not re-decided) by construction -- no methodology content lives behind the server, only knowledge (Brain) and currency (scouts/updates) do, exactly as this phase's license-field and free-core network-scan work demonstrates -- and the durable research trail is filed in both homes.
</success_criteria>

<output>
Create `.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-08-SUMMARY.md` when done
</output>
