---
phase: 234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core
plan: 06
type: execute
wave: 3
depends_on: ["234-03"]
files_modified:
  - scripts/migrate-plugin-root-refs.cjs
  - skills/**/SKILL.md (the 51 skills hardcoding ${CLAUDE_PLUGIN_ROOT})
  - tests/test-234-plugin-root-migrated.cjs
autonomous: true
requirements: [D-01, D-02]

must_haves:
  truths:
    - "Zero skills/*/SKILL.md bodies reference the literal token ${CLAUDE_PLUGIN_ROOT} without a MINDRIAN_OS_ROOT-first fallback."
    - "On a foreign host where CLAUDE_PLUGIN_ROOT is undefined, every affected shell-out either resolves via MINDRIAN_OS_ROOT or fails closed with a clear error, never silently expands to an empty-prefixed path."
    - "commands/*.md's own 51 CLAUDE_PLUGIN_ROOT references are explicitly confirmed out of scope (unreachable on any foreign host, since commands/ is a Claude-Code-only concept no host loads)."
  artifacts:
    - path: "scripts/migrate-plugin-root-refs.cjs"
      provides: "One-time targeted-text-edit codemod: wraps every skill-body ${CLAUDE_PLUGIN_ROOT} reference in a MINDRIAN_OS_ROOT-first, fail-closed fallback"
    - path: "tests/test-234-plugin-root-migrated.cjs"
      provides: "Asserts scripts/check-skill-spec.cjs --plugin-root-census reports zero bare ${CLAUDE_PLUGIN_ROOT} references"
  key_links:
    - from: "scripts/migrate-plugin-root-refs.cjs"
      to: "lib/core/active-plugin-root.cjs"
      via: "MINDRIAN_OS_ROOT precedence #1, the documented escape hatch"
      pattern: "MINDRIAN_OS_ROOT"
---

<objective>
Resolve RESEARCH.md's Open Question 5 (is the exposure a 51-file job or a 10-file job?) with a direct answer established during planning: the exposure is exactly 51 files, all in `skills/*/SKILL.md`. The parallel 51 references in `commands/*.md` are confirmed OUT OF SCOPE for Tier-0 portability, because `commands/` is a Claude-Code-only plugin concept -- no Agent-Skills-compliant host (VS Code, Cursor, Goose, Zed, or any of the ~45 in the showcase) loads or executes a `commands/` directory at all, so a `${CLAUDE_PLUGIN_ROOT}` reference there is unreachable on any foreign host by construction, not a Tier-0 risk. Only `skills/*/SKILL.md` bodies load into a foreign host's active skill instructions.

Purpose: make every skill-body shell-out resolve the plugin root portably (prefer `MINDRIAN_OS_ROOT`, the documented `lib/core/active-plugin-root.cjs` escape hatch, precedence #1) and fail closed -- not silently -- when neither variable is set, directly closing the Security Domain's named Tampering pattern ("${CLAUDE_PLUGIN_ROOT}/scripts/x becomes /scripts/x on a foreign host -- an absolute path an attacker could plant").
Output: `scripts/migrate-plugin-root-refs.cjs`; 51 SKILL.md files updated; `tests/test-234-plugin-root-migrated.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-CONTEXT.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-RESEARCH.md
scripts/check-skill-spec.cjs
lib/core/active-plugin-root.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codemod -- wrap every skill-body ${CLAUDE_PLUGIN_ROOT} reference in a MINDRIAN_OS_ROOT-first, fail-closed fallback</name>
  <files>scripts/migrate-plugin-root-refs.cjs, skills/**/SKILL.md</files>
  <read_first>
    lib/core/active-plugin-root.cjs lines 1-40 (the full header -- precedence #1 is `MINDRIAN_OS_ROOT`, "tests, dev boxes, hand clones... the escape hatch"; `resolveActivePluginRoot()`'s exact return shape and the `node active-plugin-root.cjs [--json]` CLI contract);
    skills/rooms/SKILL.md lines 71, 163, 176, 179, 229, 237, 244, 271 (8 occurrences in ONE file -- the highest-density exemplar, mixing `bash "${CLAUDE_PLUGIN_ROOT}/scripts/room-registry" list`, `cp "${CLAUDE_PLUGIN_ROOT}/templates/icm/CLAUDE.md" ...` forms);
    skills/hmi-status/SKILL.md line 54 (`node "${CLAUDE_PLUGIN_ROOT}/scripts/soft-alias-runner.cjs" ...` -- the `node` shell-out form, distinct from the `bash`/`cp` forms above);
    234-RESEARCH.md "Known threat patterns for this stack" table, the "Skill body instructing a shell-out to an unresolved ${CLAUDE_PLUGIN_ROOT} path" row (the exact Tampering rationale for the fail-closed requirement);
    scripts/check-skill-spec.cjs's `pluginRootCensus()` function (this task's post-migration verification target -- confirm it greps the frontmatter-stripped BODY only, matching where these references actually live).
  </read_first>
  <action>
    Create scripts/migrate-plugin-root-refs.cjs as a one-time CJS codemod (not a permanent gate, no `connectors` export needed). Use `scripts/check-skill-spec.cjs`'s `pluginRootCensus()` (require it, do not re-derive the file list) to get the authoritative 51-file list and skip any file outside `skills/*/SKILL.md` (commands/*.md is explicitly excluded from this codemod's scope per this plan's objective -- do not touch commands/ files). For each affected file, read the raw text and replace EVERY literal occurrence of the bare token `${CLAUDE_PLUGIN_ROOT}` with `${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?MindrianOS install root not found. Set MINDRIAN_OS_ROOT (see lib/core/active-plugin-root.cjs) or run from Claude Code.}}` -- this is valid POSIX/bash nested parameter expansion: when `MINDRIAN_OS_ROOT` is set (a foreign-host install exports it, matching `active-plugin-root.cjs`'s documented precedence #1), it is used; when unset, bash evaluates the inner `${CLAUDE_PLUGIN_ROOT:?msg}`, which uses `CLAUDE_PLUGIN_ROOT` if Claude Code set it (today's exact behavior, byte-identical on Claude Code), or aborts with a clear stderr message and non-zero exit if NEITHER is set (fail closed, per the Security Domain's Tampering finding -- no more silent empty-string path expansion). Use plain string `.replace()`/regex substitution on the raw file text, NOT a gray-matter round-trip (the same comment-preservation constraint as 234-03's Task 1 -- these are skill BODIES, and several skills' bodies contain their own prose comments/examples that a structural re-parse could disturb if this were done via any markdown-AST tool; a literal string replace is the safe, minimal-diff approach). After the substitution, print a per-file diff line count so the SUMMARY.md can report exactly how many replacements landed in which files (expect 51 files, occurrence counts varying 1-8+ per file as seen in the rooms/SKILL.md exemplar).
    Create tests/test-234-plugin-root-migrated.cjs: `require('../scripts/check-skill-spec.cjs').pluginRootCensus()` and assert `count === 0` for the BARE-token case -- but note the migrated files now correctly contain `${CLAUDE_PLUGIN_ROOT}` again (nested inside the fallback expression), so this test must NOT simply re-run the old census pattern verbatim; it must assert that no occurrence of `${CLAUDE_PLUGIN_ROOT}` appears WITHOUT being preceded by `${MINDRIAN_OS_ROOT:-` on the same logical expression. Implement this as: grep for `\$\{CLAUDE_PLUGIN_ROOT\}` (bare form, no preceding `MINDRIAN_OS_ROOT:-`) specifically, across all 125 skills (not just the 51), and assert zero matches -- catching both the 51 migrated files (should now use the wrapped form only) and confirming no OTHER, previously-unlisted skill also carries the bare form.
  </action>
  <acceptance_criteria>
    - `grep -rE '\$\{CLAUDE_PLUGIN_ROOT\}' skills --include=SKILL.md | grep -v 'MINDRIAN_OS_ROOT:-' | wc -l` returns 0 (every remaining literal occurrence is inside the new fallback wrapper).
    - `grep -c 'MINDRIAN_OS_ROOT' skills/rooms/SKILL.md` returns 8 (matches the 8 occurrences found during planning in this file).
    - `node tests/test-234-plugin-root-migrated.cjs` exits 0.
    - `git diff --stat commands/` shows zero changed files (commands/ is untouched, confirming the scoping decision).
    - Spot-check: `bash -c 'unset MINDRIAN_OS_ROOT CLAUDE_PLUGIN_ROOT; eval "echo \"${MINDRIAN_OS_ROOT:-${CLAUDE_PLUGIN_ROOT:?fail-closed test}}\""' ` exits non-zero with the custom message (proves the fail-closed syntax is valid, evaluable bash before trusting it inside 51 files).
  </acceptance_criteria>
  <verify>
    <automated>node tests/test-234-plugin-root-migrated.cjs && test "$(git diff --name-only commands/ | wc -l)" -eq 0</automated>
  </verify>
  <done>All 51 skill-body ${CLAUDE_PLUGIN_ROOT} references resolve via MINDRIAN_OS_ROOT first and fail closed with a clear message when neither variable is set; commands/ is confirmed untouched and out of scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Skill body shell-out -> filesystem path resolution | The exact boundary RESEARCH.md's Security Domain names: an unresolved variable expanding to an absolute path an attacker could plant on a foreign host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-234-11 | Tampering | 51 skill-body shell-outs resolving `${CLAUDE_PLUGIN_ROOT}` to an empty string on a foreign host | mitigate | This plan's entire scope: MINDRIAN_OS_ROOT-first resolution with a bash `:?` fail-closed abort when neither variable is set, replacing silent empty-prefix expansion. |
| T-234-12 | Tampering | commands/*.md's parallel 51 references, left unmigrated | accept | Explicitly confirmed unreachable on any foreign host (commands/ is never loaded outside Claude Code), so it carries zero Tier-0 exposure; migrating it would be scope creep with no security benefit. |
</threat_model>

<verification>
`node tests/test-234-plugin-root-migrated.cjs` green. `bash tests/run-all-234.sh` (once this test lands, glob-discovered automatically as `tests/test-234-*.cjs`) includes it in the phase gate.
</verification>

<success_criteria>
Zero bare `${CLAUDE_PLUGIN_ROOT}` references remain in any skill body; every affected shell-out fails closed rather than silently expanding to an exploitable path on a foreign host; commands/ scoping decision is documented and verified.
</success_criteria>

<output>
Create `.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-06-SUMMARY.md` when done
</output>
