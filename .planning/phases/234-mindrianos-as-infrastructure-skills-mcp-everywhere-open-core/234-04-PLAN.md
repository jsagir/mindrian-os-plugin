---
phase: 234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core
plan: 04
type: execute
wave: 2
depends_on: ["234-01"]
files_modified:
  - skills/MOSDeckEngine/SKILL.md
  - skills/mos-deck-engine/SKILL.md
  - skills/value-proposition/SKILL.md
  - skills/validate-proposition/SKILL.md
  - commands/deck.md
  - commands/show.md
  - commands/discover.md
  - skills/deck/SKILL.md
  - skills/show/SKILL.md
  - skills/discover/SKILL.md
  - skills/ui-system/SKILL.md
  - skills/ui-system/rules/design-system.md
  - skills/ui-system/design-system/M-OS-DESIGN-SYSTEM.md
  - docs/CANON-PHASE-MAP.md
  - docs/COMPONENT-INVENTORY.md
  - templates/discovery/discovery-brief-template.md
  - tests/test-deck-consolidation.cjs
  - tests/test-209-declared-implies-wired.cjs
  - tests/test-publish-needs-map.cjs
  - tests/test-publish-needs-flow.cjs
  - scripts/build-render-coverage.cjs
autonomous: true
requirements: [D-01, D-02, D-07, D-08]

must_haves:
  truths:
    - "skills/mos-deck-engine/SKILL.md's name: field matches its parent directory exactly, and the charset is lowercase-hyphen only."
    - "The value-proposition/validate-proposition name-vs-directory mismatch is resolved per the spec's must-match rule, with the RETRO-05 rationale comment preserved and extended (not deleted) to record the new resolution."
    - "Every reference to the old skills/MOSDeckEngine/ path across commands, skills, docs, and tests resolves to the new skills/mos-deck-engine/ path."
    - "The Phase-122 framework resolver's binding for validate-proposition and the connector generator's filename-based /mos: surface registration both still resolve correctly after the rename."
    - "All generated registries (data/connector-registry.json etc.) are regenerated via their owning build scripts, never hand-edited."
    - "D-07 holds: the MOSDeckEngine/value-proposition renames touch only skill directory names, frontmatter name: fields, and referencing paths -- no license-key check or entitlement gate is introduced, and the free/paid boundary stays a network boundary, untouched by this plan."
  artifacts:
    - path: "skills/mos-deck-engine/SKILL.md"
      provides: "Spec-compliant MOSDeckEngine skill (renamed directory + matching name:)"
    - path: "skills/validate-proposition/SKILL.md"
      provides: "Spec-compliant value-proposition skill (renamed directory to match its existing name: field)"
  key_links:
    - from: "scripts/build-connector-registry.cjs"
      to: "skills/mos-deck-engine/SKILL.md, skills/validate-proposition/SKILL.md"
      via: "regenerated data/connector-registry.json, data/mcp-tool-connectors.json"
      pattern: "mos-deck-engine|validate-proposition"
---

<objective>
Resolve the 2 of 9 hard spec failures RESEARCH.md and PATTERNS.md both explicitly flag as NOT mechanical: `skills/MOSDeckEngine/SKILL.md` (CamelCase directory AND name, violates the lowercase-hyphen charset rule) and `skills/value-proposition/SKILL.md` (directory `value-proposition` but `name: validate-proposition`, an INTENTIONAL, RETRO-05-documented mismatch with two live consumers keying off different fields: the connector generator keys off filename, the Phase-122 framework resolver keys off `name:`).

Purpose: unlike 234-03's mechanical batch, both fixes here require tracing the actual consumers before applying a rename, per PATTERNS.md's explicit instruction to treat these as decision tasks. This plan traces both, applies the spec-compliant resolution, and updates every downstream reference.
Output: both skills renamed to spec-compliant directories; ~17 referencing files updated; generated registries regenerated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-CONTEXT.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-RESEARCH.md
@.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-PATTERNS.md
scripts/check-skill-spec.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename skills/MOSDeckEngine -> skills/mos-deck-engine + update all references</name>
  <files>skills/MOSDeckEngine/SKILL.md, skills/mos-deck-engine/SKILL.md, commands/deck.md, commands/show.md, commands/discover.md, skills/deck/SKILL.md, skills/show/SKILL.md, skills/discover/SKILL.md, skills/ui-system/SKILL.md, skills/ui-system/rules/design-system.md, skills/ui-system/design-system/M-OS-DESIGN-SYSTEM.md, docs/CANON-PHASE-MAP.md, docs/COMPONENT-INVENTORY.md, templates/discovery/discovery-brief-template.md, tests/test-deck-consolidation.cjs, tests/test-209-declared-implies-wired.cjs, tests/test-publish-needs-map.cjs, tests/test-publish-needs-flow.cjs, scripts/build-render-coverage.cjs</files>
  <read_first>
    skills/MOSDeckEngine/SKILL.md (full file, including its frontmatter and any connector/hitl_shape declaration);
    scripts/build-connector-registry.cjs functions `parseConnectorFrontmatter`, `classifySurface`, `classifySurfaceClass` (how a skill's directory/name feeds the generated registries -- confirm whether it keys off directory name, `name:` field, or both, before assuming the rename is purely mechanical here);
    scripts/build-render-coverage.cjs (the file among the 17 references that is itself a BUILD SCRIPT, not generated output -- confirm whether it hardcodes the literal string "MOSDeckEngine" as a lookup key or just references it in a comment);
    tests/test-209-declared-implies-wired.cjs and tests/test-deck-consolidation.cjs (confirm whether either hardcodes the OLD path as a fixture path vs. discovers skills dynamically -- a hardcoded fixture path needs a literal string update, a dynamic discovery does not).
  </read_first>
  <action>
    Rename the directory `skills/MOSDeckEngine/` to `skills/mos-deck-engine/` (git mv, preserving history). Inside the renamed `SKILL.md`, change `name: MOSDeckEngine` to `name: mos-deck-engine` (targeted text edit, not a gray-matter round-trip -- preserve any existing comments in the file). Grep the whole repo (excluding `node_modules`, `.git`, `.claude/worktrees`, `.planning`, `room/`) for the literal string `MOSDeckEngine` and update every non-generated match to `mos-deck-engine`: the 3 commands/*.md files, the 3 skills/*/SKILL.md files (deck, show, discover), the 3 ui-system doc files, the 2 docs/ files, the 1 templates/ file, and the 4 tests/ files listed above -- for the tests, only change the string if it is a hardcoded fixture/lookup key; if a test discovers skills dynamically (e.g. by globbing `skills/*/SKILL.md`), it needs no edit and should be left untouched (confirm which via read_first before editing). Do NOT hand-edit any `data/*.json` file (connector-registry.json, connector-coverage-ledger.json, brain-orchestration-projection.json, help-groups.json, hitl-shape-backfill.json, render-coverage-registry.json, command-registry.json, deck-aliases.json, deck-styles.json, publish-needs.json, connector-out-of-spine-allowlist.json) -- these are generated. After all source edits, regenerate them by running `node scripts/build-connector-registry.cjs` and `node scripts/build-render-coverage.cjs` (their actual generate mode, not `--check`) and any other owning generator the read_first step identifies as touching these JSON files, then commit the regenerated JSON alongside the source changes.
  </action>
  <acceptance_criteria>
    - `node scripts/check-skill-spec.cjs --check` output no longer lists `MOSDeckEngine` or `mos-deck-engine` as a charset/name-mismatch failure.
    - `grep -rc "MOSDeckEngine" --include=*.md --include=*.cjs .` (excluding node_modules/.git/.claude/worktrees/.planning/room) returns 0.
    - `node scripts/build-connector-registry.cjs --check` exits 0 (regenerated registry matches source, no drift).
    - `bash tests/run-all-209.sh` (or the specific test-209-declared-implies-wired.cjs / test-deck-consolidation.cjs files directly) still pass after the rename.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-skill-spec.cjs --check 2>&1 | grep -c "MOSDeckEngine" | grep -qx 0 && node scripts/build-connector-registry.cjs --check</automated>
  </verify>
  <done>skills/mos-deck-engine/ is spec-compliant; every non-generated reference to the old name is updated; generated registries regenerated and drift-free.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve value-proposition/validate-proposition name-vs-directory mismatch (traced, not mechanical)</name>
  <files>skills/value-proposition/SKILL.md, skills/validate-proposition/SKILL.md</files>
  <read_first>
    skills/value-proposition/SKILL.md (full file -- the 6-line RETRO-05 comment block recording the mismatch as INTENTIONAL: "the generator registers the connector surface under /mos: + filename-base (/mos:value-proposition); the Phase-122 resolver keys the framework PWS Value Proposition off the name: field (/mos:validate-proposition, alongside /mos:build-thesis)");
    commands/value-proposition.md (the ACTUAL /mos:value-proposition slash command surface -- confirm this file is independent of the skill's directory name and is therefore UNAFFECTED by renaming the skill's directory; slash commands live in commands/*.md, not skills/*/SKILL.md, so this file needs no edit regardless of which side of the mismatch is resolved);
    scripts/build-connector-registry.cjs's `classifySurface`/`classifySurfaceClass`/`parseConnectorFrontmatter` functions (confirm precisely how the connector generator derives its `/mos:` filename-base surface id for a skill -- from the skill's directory basename, or from something else -- before renaming);
    the Phase-122 framework resolver (locate it via `grep -rl "validate-proposition\|PWS Value Proposition" lib/` -- read its exact lookup key, confirm it reads `name:` from the skill frontmatter and not the directory).
  </read_first>
  <action>
    Resolution: rename the DIRECTORY `skills/value-proposition/` to `skills/validate-proposition/` (git mv, preserving history) so it matches the EXISTING `name: validate-proposition` field, rather than changing the `name:` field to match the old directory. Rationale to record in the file: the spec's hard requirement is name-equals-parent-directory; `name:` is the more architecturally load-bearing of the two identifiers here because the Phase-122 framework resolver's methodology binding depends on it, while the connector generator's filename-based surface id is a presentation-layer concern that can be updated. Before applying, verify this rationale against what read_first actually found (if the trace reveals the connector generator's filename-base id is ALSO relied upon by something that cannot be easily updated, treat that as a blocking discrepancy and flag it in the SUMMARY rather than forcing the rename through). Inside the renamed SKILL.md, UPDATE (do not delete) the existing RETRO-05 comment block: append a dated note directly beneath it recording that Phase 234 resolved the spec's must-match-directory requirement by renaming the directory to validate-proposition, that name: itself is unchanged, and that /mos:value-proposition (commands/value-proposition.md) is untouched and continues to resolve independently since slash commands do not depend on the skill directory name. Grep the repo for any OTHER reference to the old `skills/value-proposition/` path (beyond commands/value-proposition.md, which is intentionally left alone) and update it. Regenerate `data/connector-registry.json` and any other generated registry touching this skill via its owning build script (never hand-edited).
  </action>
  <acceptance_criteria>
    - `node scripts/check-skill-spec.cjs --check` output no longer lists a name-vs-directory mismatch for this skill.
    - `skills/validate-proposition/SKILL.md` contains both the original RETRO-05 comment block (byte-identical) AND a new dated Phase-234 resolution note beneath it.
    - `commands/value-proposition.md` is byte-unchanged (`git diff --exit-code commands/value-proposition.md`).
    - `node scripts/build-connector-registry.cjs --check` exits 0.
    - Whatever mechanism resolves `/mos:validate-proposition` to the "PWS Value Proposition" framework (the Phase-122 resolver identified in read_first) still resolves correctly -- verified by running that resolver's own existing test suite if one exists, or by direct code inspection confirming it reads `name:` (unchanged) rather than the directory path.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-skill-spec.cjs --check 2>&1 | grep -c "validate-proposition\|value-proposition" | grep -qx 0 && git diff --exit-code commands/value-proposition.md</automated>
  </verify>
  <done>skills/validate-proposition/ is spec-compliant, the RETRO-05 rationale is preserved and extended, and both live consumers (connector generator, Phase-122 resolver) still resolve correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Skill directory identity -> two independent in-repo consumers | A rename here is a genuine breaking-change risk to whichever consumer is not traced correctly; this is why the task is scoped as trace-first, not mechanical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-234-06 | Tampering | Directory rename breaking an untraced consumer | mitigate | Explicit read_first trace of both consumers before renaming; acceptance criteria require both to still resolve; SUMMARY.md must flag any discrepancy found rather than forcing the rename through. |
| T-234-07 | Repudiation | Deleting the RETRO-05 rationale comment | mitigate | Action explicitly requires preserving and extending the comment, not deleting it; acceptance criteria grep for its literal survival. |
</threat_model>

<verification>
`node scripts/check-skill-spec.cjs --check` shows 0 remaining hard failures for both skills (down from 9 total across the phase to 0, combined with 234-03's 123). `node scripts/build-connector-registry.cjs --check` clean.
</verification>

<success_criteria>
Both non-mechanical hard spec failures are resolved with a traced, documented resolution; zero regressions in either live consumer; the phase's hard-failure count is now 0/125.
</success_criteria>

<output>
Create `.planning/phases/234-mindrianos-as-infrastructure-skills-mcp-everywhere-open-core/234-04-SUMMARY.md` when done
</output>
