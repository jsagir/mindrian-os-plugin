---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 12
type: execute
wave: 8
depends_on: ["359-11"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-VALIDATION.md
  - /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-missed-fork-declaration-359.md
  - /home/jsagi/MindrianOS/research/2026-09-23-missed-fork-declaration-359.md
autonomous: true
requirements: [FORK359-01, FORK359-02, FORK359-03, FORK359-04, FORK359-05, FORK359-06, FORK359-07, FORK359-08, FORK359-09, FORK359-10]
tags: [close-out, requirements, dual-filing, residuals]

must_haves:
  truths:
    - "Every FORK359-01..10 row is ticked only with measured proof named in one clause (test command and result, or SUMMARY reference), or left open with `Open: <reason>` (for example FORK359-09 on INCONCLUSIVE, DECLINED or FALSIFIED, FORK359-08 when R8 was reverted)"
    - "The R2 pre-change missed-fork number, the R5/R6/R7 counts, the R8 net bytes and the R9 outcome are all stated in the phase record (SPEC acceptance: numbers recorded in the SUMMARY)"
    - "The reasoning trail is filed in both research homes, cross-linked to 359-CONTEXT.md, with no transcript text and no names (CLAUDE.md Dev-Research Compositing, the 357-10 D-17 precedent)"
    - "Residuals and interpretations are recorded, not fixed: the hookless chat-only residual, the yes/no prefix quirk, Hebrew yes/no pairs not exempt, the Score-string guard gap, N-3's 4-label cap reading, I-1 and I-2 as ruled, and the Deferred Ideas list"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "FORK359 rows closed with proof or annotated"
      contains: "FORK359-10"
    - path: "/home/jsagi/MindrianOS/research/2026-09-23-missed-fork-declaration-359.md"
      provides: "source-of-record reasoning trail and execution outcome"
      contains: "Execution outcome (Phase 359 close"
  key_links:
    - from: "/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-missed-fork-declaration-359.md"
      to: ".planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md"
      via: "link line"
      pattern: "359-CONTEXT.md"
---

<objective>
Close the phase honestly: close or annotate every FORK359 requirement with proof, sign off the validation contract, file the reasoning and the outcome in both research homes, and record every residual and interpretation for the next reader.

Purpose: requirement closure, D-17-style dual filing (CLAUDE.md Dev-Research Compositing), SPEC acceptance items that require numbers in the SUMMARY.
Output: REQUIREMENTS and ROADMAP updates, the VALIDATION approval, two research-trail files, the close-out SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-SPEC.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-VALIDATION.md

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`. Read every PD/359-0N-SUMMARY.md and PD/359-1N-SUMMARY.md before writing.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; REQUIREMENTS.md and ROADMAP.md are shared: re-read from disk immediately before editing and change only the Phase 359 section and entry; explicit-path commits only (`git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`); the home repo `/home/jsagi` is very dirty: commit there only with `git -C /home/jsagi commit --only -- <two paths>`; never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md. No em-dashes or en-dashes. No transcript text and no real names in the research files.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 (this plan) |

<tasks>

<task type="auto">
  <name>Task 1: Close the FORK359 rows, update the roadmap entry, and approve the validation contract</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-VALIDATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the "Phase 359" section registered at plan time) and .planning/ROADMAP.md (the Phase 359 entry), both re-read from disk now
    - Every 359 SUMMARY (proof per requirement: 01 parser and inertness, 02 corpus, 03 arm and MCP, 06 rulings, 07 replay counts, 08 R2 number and mutations, 09 R8 bytes, 10 smoke, 11 R9 outcome)
    - PD/359-VALIDATION.md
  </read_first>
  <action>
    1. Run `bash tests/run-all-359.sh` and `node tests/test-359-inertness.cjs` once more and record the results (proof for the closure clauses).
    2. REQUIREMENTS.md, Phase 359 section only: for each FORK359-01..10 row, tick `- [x]` only with measured proof in one clause (for example `Proof: node tests/test-359-replay.cjs L2 green, catch rate 1, control false blocks 0 (359-07/08 SUMMARY)`), else leave `- [ ]` and append `Open: <reason>`. FORK359-09 is ticked only on `R9: PASS`; FORK359-08 is ticked only when R8 is still in place (or annotated `Reverted by the R9 falsification branch, <sha>`).
    3. ROADMAP.md, Phase 359 entry only: tick the plan checkboxes whose SUMMARY exists; add one `**Outcome:**` line (R2 number, R6 catch rate, R8 net bytes, R9 verdict, follow-on phase number if any).
    4. PD/359-VALIDATION.md: tick the remaining sign-off box and set `**Approval:** approved <date> (359-12 close: R9 <verdict>)`; set `status: complete` in its frontmatter.
    5. Commit: `git add -f .planning/REQUIREMENTS.md .planning/ROADMAP.md PD/359-VALIDATION.md` then `git commit --only -m "docs(359-12): close FORK359 rows with measured proof, roadmap outcome, validation approval" -- .planning/REQUIREMENTS.md .planning/ROADMAP.md PD/359-VALIDATION.md`.
  </action>
  <verify>
    <automated>test "$(grep -c 'FORK359-[0-9][0-9]\*\*' .planning/REQUIREMENTS.md)" -ge 10 && node -e "const t=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');const rows=t.split('\n').filter(l=>/^- \[[ x]\] \*\*FORK359-/.test(l));const bad=rows.filter(l=>l.startsWith('- [ ]')&&!/Open:/.test(t.slice(t.indexOf(l),t.indexOf(l)+900)));process.exit(rows.length===10&&bad.length===0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify exits 0: ten FORK359 rows, each ticked or carrying `Open:`
    - `git diff $PLAN_BASE -- .planning/REQUIREMENTS.md | grep '^[-+]' | grep -v '^[-+][-+]' | grep -vc "FORK359\|Proof:\|Open:"` is small and every changed line sits inside the Phase 359 section
    - `grep -c "status: complete" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-VALIDATION.md` is 1
    - `git status --short -- .planning/STATE.md` shows no change made by this plan
  </acceptance_criteria>
  <done>Every FORK359 requirement is closed with proof or left open with a reason, and the roadmap and validation contract reflect the measured outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Dual research filing and the close-out SUMMARY sections</name>
  <files>/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-missed-fork-declaration-359.md, /home/jsagi/MindrianOS/research/2026-09-23-missed-fork-declaration-359.md</files>
  <read_first>
    - /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-larry-extended-x-jev-gate-triad-replay.md (the 357 trail: format, frontmatter, cross-link style)
    - `git -C /home/jsagi status --short -- MindrianOS/research MindrianRooms/rethinking-mindrianos/research` (peer diffs)
    - PD/359-CONTEXT.md (D-01..D-23, N-1..N-6) and PD/359-RESEARCH.md Summary
  </read_first>
  <action>
    1. Write the SAME document to both paths (identical bytes), mirroring the 357 trail's format: title `Missed-fork detection: Larry declares, the hook checks (Phase 359)`; the problem in plain language (a prose fork trips neither PRIMARY nor BACKSTOP; the retired prose guesser was about 86% false positive); why the model declares instead of the hook guessing (inertness by construction); the grammar and the navigator rulings N-1 to N-6 (visible `Your call:` line, the What-if moonshot, Sonnet 5, the R9 fallback and floor, net 400 B); what was built (parser, declared arm, MCP card and dedup, replay metrics, prose rule, forward harness, moonshot scorer); then `## Execution outcome (Phase 359 close, <date>)`: the R2 pre-change missed-fork number, R5 (0 class diffs), R6 catch rate and control false blocks, R7 parity, R8 net bytes per surface, R9 verdict with per-arm counts and spend, moonshot score distribution, the follow-on phase when falsified; `## Residuals`: hookless chat-only turns stay uncaught (Deferred), the yes/no prefix quirk, Hebrew yes/no pairs not exempt, the Score-string guard gap, N-3's 4-label cap reading, I-1 and I-2 as ruled; a Theo-side analog statement (none: the declaration is local runtime, no Brain involvement); and a link line to `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md`. Counts and ids only, no transcript text, no names, hyphens only.
    2. Commit in the home repo by explicit path: `git -C /home/jsagi add MindrianOS/research/2026-09-23-missed-fork-declaration-359.md MindrianRooms/rethinking-mindrianos/research/2026-09-23-missed-fork-declaration-359.md` then `git -C /home/jsagi commit --only -m "rethinking-mindrianos: Phase 359 missed-fork declaration trail and outcome" -- MindrianOS/research/2026-09-23-missed-fork-declaration-359.md MindrianRooms/rethinking-mindrianos/research/2026-09-23-missed-fork-declaration-359.md`.
    3. SUMMARY required sections: `## Measured numbers` (R2, R5, R6, R7, R8, R9), `## Residuals and interpretations`, `## Deferred` (restating the CONTEXT Deferred Ideas), `## Known pre-existing reds (R-J)` unchanged, and `## Files changed` across the phase.
  </action>
  <verify>
    <automated>cmp /home/jsagi/MindrianOS/research/2026-09-23-missed-fork-declaration-359.md /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-missed-fork-declaration-359.md && grep -c "Execution outcome (Phase 359 close" /home/jsagi/MindrianOS/research/2026-09-23-missed-fork-declaration-359.md && git -C /home/jsagi log -1 --format=%s -- MindrianOS/research/2026-09-23-missed-fork-declaration-359.md</automated>
  </verify>
  <acceptance_criteria>
    - The verify exits 0 (identical files, one outcome section, the commit subject printed)
    - `git -C /home/jsagi show --stat --format= HEAD` lists only the two research files
    - `grep -c "$(printf '\xe2\x80\x94')" /home/jsagi/MindrianOS/research/2026-09-23-missed-fork-declaration-359.md` is 0, and the same for `$(printf '\xe2\x80\x93')`
    - `grep -c "359-CONTEXT.md" /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-09-23-missed-fork-declaration-359.md` is at least 1
    - The SUMMARY contains the five required sections of step 3
  </acceptance_criteria>
  <done>The reasoning and the measured outcome live in both research homes, cross-linked to the phase, and the SUMMARY states every number the SPEC asks to be recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase record -> future readers | an overstated closure misleads the next phase |
| home repo (`/home/jsagi`) -> research trail | a very dirty tree; a plain commit would sweep unrelated work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-55 | Repudiation | a requirement ticked without proof | mitigate | each tick names its proof; unmet rows carry `Open:`; FORK359-09 ticked only on R9 PASS |
| T-359-56 | Information disclosure | research trail carrying transcript text or names | mitigate | counts and ids only; acceptance checks; synthetic phrasing only |
| T-359-57 | Tampering | shared planning files and the home repo | mitigate | edits limited to the Phase 359 section and entry, re-read before editing; `git -C /home/jsagi commit --only -- <two paths>` |
</threat_model>

<verification>
- `bash tests/run-all-359.sh` exits 0
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts agents skills` prints nothing (close-out is docs only)
</verification>

<success_criteria>
Every FORK359 requirement is closed with proof or annotated, the validation contract is approved, and the phase's reasoning and outcome are filed in both research homes with every residual named.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-12-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`), with the sections required by Task 2 step 3.
</output>
