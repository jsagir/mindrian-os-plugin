---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 04
type: execute
wave: 2
depends_on: ["359-01"]
files_modified:
  - data/jev-policies/fork359-moonshot.json
  - scripts/jev-devtime-client.cjs
  - data/ROOM.md
  - scripts/score-moonshots-359.cjs
  - tests/test-359-moonshot-scorer.cjs
  - tests/test-353-tripwires.cjs
autonomous: true
requirements: [FORK359-09, FORK359-01]
tags: [jev, dev-time, moonshot, egress-guard, part8]

must_haves:
  truths:
    - "Moonshot quality is measured at dev time only: Jev scores each What-if moonshot with two independent Score questions, relevant_to_context and radical_departure, over synthetic text only (the 359 fixtures and the authored R9 scenarios plus Larry's declared labels); never in a hook, never at runtime, never on user or dogfood text (N-3, 2026-09-17 rulings, Part 8)"
    - "The shared client gains exactly one additive profile, fork359_moonshot, declared in 356's exact_state_v1 kind with its existing optional fields only; no other profile changes and the guard schema is not redefined (356 owns it; N-3 allows a 359 profile because card_fire_replay admits only the three Noul questions)"
    - "The profile pins top keys, model jev-latest, exactly four string state keys (context, practical_labels, moonshot, policy) with length caps, state.policy byte-identical to data/jev-policies/fork359-moonshot.json, exactly two question ids of type score, and a 400-char question cap; because 356's question_strings_from_file_key check is Noul-shaped (criteria.true and criteria.false), score rule, level and boundary strings are copied from the policy file by the builder and asserted deep-equal by the test, and this guard gap is recorded"
    - "The scorer refuses before building any request: any input that is not the synthetic-359 fixture or a results row whose scenario id exists in the authored scenario fixture; it never reads dogfood files; context always comes from authored text, never from Larry's full reply"
    - "Keyless runs mark every row unlabeled and exit 0; the report carries ids, scores, confidences, moonshot labels and distributions, never context text; scores are never auto-applied to any fixture"
    - "The scorer is banned from hooks/ by the shared HOOKS_BANNED_LEDGER_SCRIPTS tripwire list (one additive entry)"
  artifacts:
    - path: "data/jev-policies/fork359-moonshot.json"
      provides: "two Score policies with situation-describing levels and boundary cases"
      contains: "radical_departure"
    - path: "scripts/jev-devtime-client.cjs"
      provides: "EGRESS_PROFILES.fork359_moonshot (exact_state_v1, question_type score)"
      contains: "fork359_moonshot"
    - path: "scripts/score-moonshots-359.cjs"
      provides: "dev-only moonshot scorer: buildScoreBody, selectRows, mapScore, renderReport, main"
      exports: ["buildScoreBody", "selectRows", "renderReport", "main"]
      min_lines: 150
    - path: "tests/test-359-moonshot-scorer.cjs"
      provides: "keyless, guard, refusal, policy-string and report-hygiene legs"
      min_lines: 120
  key_links:
    - from: "scripts/score-moonshots-359.cjs"
      to: "scripts/jev-devtime-client.cjs"
      via: "makeEgressGuard(EGRESS_PROFILES.fork359_moonshot, {root}) then jev(body, {key, guard})"
      pattern: "fork359_moonshot"
    - from: "tests/test-353-tripwires.cjs"
      to: "scripts/score-moonshots-359.cjs"
      via: "HOOKS_BANNED_LEDGER_SCRIPTS entry"
      pattern: "score-moonshots-359"
---

<objective>
Give N-3's moonshot its dev-time quality instrument: a stated Score policy (relevant to context, radical departure), one additive egress profile on the shared Jev client, and a dev-only scorer that reads only synthetic or authored text, refuses everything else before building a request, and degrades to unlabeled without a key.

Purpose: N-3 (moonshot quality measured at dev time only), SPEC R9 reporting, the 2026-09-17 rulings, Canon Part 8.
Output: the policy file, the profile, `scripts/score-moonshots-359.cjs`, its test, one tripwire list entry, one data/ROOM.md row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-01-SUMMARY.md
@.claude/skills/spike-findings-MindrianOS-Plugin/SKILL.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/jev-typed-decisions-api.md
@.claude/skills/spike-findings-MindrianOS-Plugin/references/policy-execution-parity.md
@scripts/jev-devtime-client.cjs

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`.

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`; otherwise STOP.

Jev contract (spike reference): `POST /v1/systemone`, body `{model: 'jev-latest', state, questions}`; a `score` question takes `criteria` as an ARRAY of level descriptions (up to 10) that "describe situations, not degrees" and returns `score` (probability-weighted level index), `legend`, `probabilities`, `confidence`; state the policy, never let it be inferred. The key lives in `~/.secrets/typesafe.env`; never print it. The 400-not-422 quirk applies.

INERTNESS GATE: `node tests/test-359-inertness.cjs` exits 0 before and after (this plan touches no classifier file; the check proves it). If a sibling wave-2 plan has an uncommitted diff on scripts/check-card-fire.cjs and the gate fails, wait for its commit and re-run.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make (for scripts/jev-devtime-client.cjs and tests/test-353-tripwires.cjs, also message the 356 owner jsagi-a7 if a messaging tool exists, else record the message under `## Peer messages (to relay)` in the SUMMARY); explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md. No em-dashes or en-dashes. Never touch any existing EGRESS_PROFILES entry, `_checkExactStateV1`, `_applyOptionalFields` or `makeEgressGuard` (356 owns the schema).

Test hygiene: as in 359-01.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 (this plan) |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto">
  <name>Task 1: The two Score policies and the fork359_moonshot egress profile</name>
  <files>data/jev-policies/fork359-moonshot.json, scripts/jev-devtime-client.cjs, data/ROOM.md</files>
  <read_first>
    - scripts/jev-devtime-client.cjs (whole file: _checkExactStateV1, _applyOptionalFields including the Noul-shaped question_strings_from_file_key check, makeEgressGuard, jev, EGRESS_PROFILES and how material_step_ledger and card_fire_replay are frozen)
    - data/jev-policies/card-fire-replay.json and data/jev-policies/command-irreversibility.json (top-level field style to mirror)
    - data/ROOM.md (the "Files in this section" table and any jev-policies rows)
    - .claude/skills/spike-findings-MindrianOS-Plugin/references/policy-execution-parity.md (state the rule; boundary cases)
    - PD/359-CONTEXT.md N-3 (relevant to context, radical, lateral elevation, trending to the absurd)
  </read_first>
  <action>
    1. Create `data/jev-policies/fork359-moonshot.json`: `{ "policy_set": "fork359-moonshot", "version": "1", "phase": "359", "used_by": "scripts/score-moonshots-359.cjs (dev time only, never a hook, synthetic text only)", "policies": [ two entries ] }`. Each entry: `{ "policy_id": "relevant_to_context" | "radical_departure", "version": "1", "instructions": "<one paragraph naming the state keys it reads: context, practical_labels, moonshot>", "levels": [5 strings, each describing a situation, not a degree, ordered from least to most], "boundary_cases": [at least 4 strings of the form '<case> -> level <n>: <why>'] }`. relevant_to_context asks whether the moonshot still addresses the decision in `context` (an unrelated flight of fancy is low; a move that reframes the same decision is high). radical_departure asks how far the moonshot departs from every practical label (a rewording of a practical option is low; a lateral elevation or a trend pushed to the absurd that still points at the decision is high; N-3 names trending to the absurd as legitimate). Plain language, hyphens only, no room content, no real names; the two policies are independent and never compared.
    2. Add ONE frozen profile after the last existing entry, touching no other: `fork359_moonshot: Object.freeze({ id: 'fork359_moonshot', kind: 'exact_state_v1', top_keys: Object.freeze(['model','state','questions']), model: 'jev-latest', state_keys: Object.freeze(['context','practical_labels','moonshot','policy']), string_keys: Object.freeze([same four]), max_len_by_key: Object.freeze({ context: 2000, practical_labels: 300, moonshot: 100 }), must_equal_file: Object.freeze({ policy: 'data/jev-policies/fork359-moonshot.json' }), question_ids: Object.freeze(['relevant_to_context','radical_departure']), question_keys: Object.freeze(['type','instructions','criteria']), question_type: 'score', instructions_keys: Object.freeze(['question','rule','boundary_cases']), question_max_len: 400 })`. Deliberately omit `criteria_keys` and `question_strings_from_file_key` (both assume Noul criteria objects; a Score criteria is an array), with a comment stating that gap, that the builder copies every rule, level and boundary string from the policy file, that the test asserts it, and that 356 owns the schema (N-3). Record every added line in the SUMMARY.
    3. `data/ROOM.md`: if `git status --short -- data/ROOM.md` is clean, add ONE row for `jev-policies/fork359-moonshot.json` (hand-maintained, dev-time only; two Score policies for the Phase 359 moonshot; byte equality enforced by the fork359_moonshot profile; never read at runtime; Phase 359, canon parts 8 and 12); otherwise skip and record why.
    4. Guard smoke check (session scratchpad `node -e`): `makeEgressGuard(EGRESS_PROFILES.fork359_moonshot, {root: process.cwd()})` accepts a body built from the policy file (question text 'Q?', rule = instructions, boundary_cases, criteria = levels) and throws `EGRESS_REFUSED` for a one-byte policy change, an extra state key `room`, a 2001-char context and a question of type `noul`; record the four outcomes.
    5. Commit: `git add data/jev-policies/fork359-moonshot.json` then `git commit --only -m "feat(359-04): fork359_moonshot Score policies and egress profile (N-3, dev time only)" -- data/jev-policies/fork359-moonshot.json scripts/jev-devtime-client.cjs data/ROOM.md` (drop data/ROOM.md when step 3 was skipped).
  </action>
  <verify>
    <automated>node -e "const c=require('./scripts/jev-devtime-client.cjs');const p=c.EGRESS_PROFILES.fork359_moonshot;if(!p||p.kind!=='exact_state_v1'||p.question_type!=='score'||!Object.isFrozen(p))process.exit(1);const t=require('fs').readFileSync(p.must_equal_file.policy,'utf8');const P=JSON.parse(t).policies;const q={};for(const x of P)q[x.policy_id]={type:'score',instructions:{question:'Q?',rule:x.instructions,boundary_cases:x.boundary_cases},criteria:x.levels};const g=c.makeEgressGuard(p,{root:process.cwd()});if(g({model:'jev-latest',state:{context:'c',practical_labels:'a | b',moonshot:'What if x',policy:t},questions:q})!==true)process.exit(2);console.log('ok',Object.keys(c.EGRESS_PROFILES).join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok` followed by the profile list, which equals the pre-plan list plus `fork359_moonshot`
    - `git diff $PLAN_BASE -- scripts/jev-devtime-client.cjs | grep '^-' | grep -vc '^---'` is 0 (additive only)
    - `node -e "const p=require('./data/jev-policies/fork359-moonshot.json');process.exit(p.policies.length===2&&p.policies.every(x=>Array.isArray(x.levels)&&x.levels.length===5&&x.boundary_cases.length>=4)?0:1)"` exits 0
    - `grep -c "$(printf '\xe2\x80\x94')" data/jev-policies/fork359-moonshot.json scripts/jev-devtime-client.cjs` reports 0 for each
  </acceptance_criteria>
  <done>The moonshot has a stated two-question Score policy and one additive, guard-enforced egress profile on the shared client.</done>
</task>

<task type="auto">
  <name>Task 2: The dev-only moonshot scorer, its test, and the tripwire list entry</name>
  <files>scripts/score-moonshots-359.cjs, tests/test-359-moonshot-scorer.cjs, tests/test-353-tripwires.cjs</files>
  <read_first>
    - scripts/jev-devtime-client.cjs (loadKey, makeEgressGuard, jev, pool, EGRESS_PROFILES.fork359_moonshot)
    - scripts/label-card-fire-replay.cjs (the 357 labeler: header wording, keyless path, report hygiene, guard built once; mirror it)
    - lib/core/fork-declaration.cjs (MOONSHOT_PREFIX, parseForkDeclaration)
    - tests/test-353-tripwires.cjs (HOOKS_BANNED_LEDGER_SCRIPTS and the one-entry-per-line style) and `git log -3 --format='%h %s' -- tests/test-353-tripwires.cjs`
    - tests/test-357-labeler-refusal.cjs (legs to mirror: keyless, guard enforcement, spy fetch)
  </read_first>
  <action>
    1. Create `scripts/score-moonshots-359.cjs` ('use strict'; header: dev time only, navigator-invoked, never a hook, never CI, never required from lib/ or hooks/; Part 8: only the synthetic-359 fixture and the authored forward-scenario fixture plus Larry's declared labels are ever sent; dogfood is never read; hyphens only). Requires only Node built-ins, `./jev-devtime-client.cjs` and `../lib/core/fork-declaration.cjs`. Exports and behavior:
       - `selectRows({fixtures, fromResults, scenariosPath})`: with `fixtures`, read ONLY `tests/fixtures/card-fire-replay/prose-forks-359.json` and refuse (throw `refused: <path> is not the synthetic-359 fixture`) for any other path or any entry whose source is not synthetic-359; each prose_fork true entry gives a row `{id, context: envelope.output_text, labels: fork_labels}`; a missing fixture file yields zero rows (reported), never a crash. With `fromResults <jsonl>`, read rows where `declared === true` and `declared_labels` is an array; each must have a `scenario_id` present in `tests/fixtures/forward-fork-scenarios-359.json` (else throw `refused: unknown scenario <id>`); context = that scenario's authored turns joined by a blank line (never Larry's reply); labels = declared_labels; id = `<scenario_id>:<arm>:<run>`. Both refusals happen before any body is built. A row whose last label does not start with MOONSHOT_PREFIX is skipped as `no-moonshot`.
       - `buildScoreBody(row, policyText)`: `{model: 'jev-latest', state: {context, practical_labels: labels.slice(0,-1).join(' | '), moonshot: labels[labels.length-1], policy: policyText}, questions: {relevant_to_context, radical_departure}}` where each question is `{type: 'score', instructions: {question: <fixed sentence at most 400 chars naming the state keys>, rule: <policy.instructions>, boundary_cases: <policy.boundary_cases>}, criteria: <policy.levels>}` copied from the parsed policy file. Values over the profile caps are refused by the guard (refuse, never truncate).
       - `mapScore(answer)`: accepts `{type: 'score', score: finite number, confidence}` and returns `{score, confidence}`, else `error`.
       - `renderReport(rows, meta, {append, section})`: markdown with a header (timestamp, model, policy sha256, counts per status, the statement "Scores are dev-time evidence only and are never auto-applied (N-3)"), a per-row table (id, moonshot label, relevant score and confidence, radical score and confidence, status) and a distribution block (mean and a 5-bucket histogram per question); never any context text. `append` adds a new `## <section>` to an existing report.
       - `main(argv)`: switch-case flags `--fixtures`, `--from-results <path>`, `--report <path>` (default `PD/359-MOONSHOT-SCORES.md`), `--section <name>`, `--append`, `--dry-run` (build and guard every body, send nothing). Key via `loadKey({env: process.env, secretsPath: path.join(os.homedir(), '.secrets', 'typesafe.env')})`; keyless: every row `unlabeled`, write the report, print `unlabeled: no TYPESAFE key (keyless run)`, exit 0. Otherwise build the guard once with `makeEgressGuard(EGRESS_PROFILES.fork359_moonshot, {root})`, `pool(rows, 4, ...)`, one `jev(body, {key, guard})` call per row; an EGRESS_REFUSED throw or non-200 status marks the row `refused: <key>` or `error`. Never print the key or response text beyond status codes.
    2. Create `tests/test-359-moonshot-scorer.cjs` (header naming N-3 and FORK359-09; test hygiene). Legs: S1 keyless spawn with `HOME` set to a mkdtemp dir exits 0, prints `unlabeled`, writes the temp report, no NETWORK_ATTEMPT_359; S2 a body built for an in-test synthetic row (independent of plan 02's fixture, which lands in the same wave) passes the real guard; each of these throws EGRESS_REFUSED and, routed through `jev(body, {key: 'k', guard, fetchImpl: spy})`, leaves the spy count at 0: policy changed by one byte, an extra state key `room`, context of 2001 chars, a question of type `noul`, a third question id, a question sentence over 400 chars; S3 every rule, level and boundary string in a built body deep-equals the policy file; S4 refusals before any body: `--fixtures` pointed at tests/fixtures/card-fire-replay/dogfood.json, a results row with an unknown scenario id, and a fixture entry whose source is not synthetic-359 all throw `refused` with the spy count at 0; S5 renderReport over rows whose context carries a sentinel string never contains the sentinel and contains `never auto-applied`; S6 every EGRESS_PROFILES entry other than fork359_moonshot is JSON-identical to the one in `git show $PLAN_BASE:scripts/jev-devtime-client.cjs` (loaded from a mkdtemp copy); S7 `grep -rn api.typesafe.ai lib/ hooks/` is empty.
    3. `tests/test-353-tripwires.cjs`: run it first and record counts; if it has a diff you did not make, skip this step and record it. Append exactly one line `  'score-moonshots-359',` after the last entry of HOOKS_BANNED_LEDGER_SCRIPTS (skip if present); change nothing else. Leg 1 of that test writes a scratch file under lib/core/; run it only when `git status --short -- lib/core` shows no peer diff.
    4. Run `node tests/test-359-moonshot-scorer.cjs`, `node tests/test-353-tripwires.cjs`, `node tests/test-357-labeler-refusal.cjs`, `node tests/test-359-inertness.cjs`. Commit: `git add scripts/score-moonshots-359.cjs tests/test-359-moonshot-scorer.cjs` then `git commit --only -m "feat(359-04): dev-only moonshot scorer on synthetic text, keyless-safe (N-3)" -- scripts/score-moonshots-359.cjs tests/test-359-moonshot-scorer.cjs tests/test-353-tripwires.cjs`.
  </action>
  <verify>
    <automated>node tests/test-359-moonshot-scorer.cjs && node tests/test-353-tripwires.cjs && node tests/test-357-labeler-refusal.cjs && node tests/test-359-inertness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All four verify commands exit 0
    - `grep -v '^\s*//' scripts/score-moonshots-359.cjs | grep -c "fetch("` is 0 (all network goes through the shared client's jev)
    - `grep -v '^\s*//' scripts/score-moonshots-359.cjs | grep -c "dogfood"` is 0 (it never names or opens a dogfood file; the header uses `//` comment lines)
    - `grep -c "'score-moonshots-359'," tests/test-353-tripwires.cjs` is 1 and `git diff $PLAN_BASE -- tests/test-353-tripwires.cjs | grep '^-' | grep -vc '^---'` is 0
    - `env -u TYPESAFE_API_KEY HOME=$(mktemp -d) node scripts/score-moonshots-359.cjs --fixtures --report "$(mktemp -d)/m.md"; echo $?` prints `unlabeled` and 0
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/score-moonshots-359.cjs tests/test-359-moonshot-scorer.cjs` reports 0 for each
  </acceptance_criteria>
  <done>A keyless-safe, refusal-first scorer can grade moonshots for relevance and radicalness through one guarded profile, on synthetic and authored text only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> TypeSafe (Jev) | the only network egress in this plan; must carry synthetic or authored text only |
| shared Jev client -> 356 and 357 builders | a profile or schema change here moves other phases' guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-16 | Information disclosure | user or dogfood text reaching Jev | mitigate | selectRows refuses any path but the synthetic fixture and any results row not tied to an authored scenario; context comes from authored turns only; S4 legs with a spy fetch |
| T-359-17 | Tampering | score question strings smuggling text past the guard | mitigate | guard pins keys, types, caps, ids and policy bytes; the builder copies every rule, level and boundary string from the policy file; S3 asserts deep equality; the Noul-shaped file-string check gap is recorded, not papered over |
| T-359-18 | Elevation of privilege | EGRESS_PROFILES union or 356 schema redefinition | mitigate | one additive frozen profile; S6 proves every other profile JSON-identical to PLAN_BASE; no checker function edited |
| T-359-19 | Information disclosure | key printed or committed | mitigate | loadKey from ~/.secrets only; no key or response text printed; keyless path exits 0 |
| T-359-20 | Elevation of privilege | scorer loaded from a hook | mitigate | HOOKS_BANNED_LEDGER_SCRIPTS entry; header states never a hook; plan 08 dev-only tripwire |
</threat_model>

<verification>
- `node tests/test-359-moonshot-scorer.cjs && node tests/test-353-tripwires.cjs && node tests/test-359-inertness.cjs` all exit 0
- `! grep -rn "api.typesafe.ai" lib/ hooks/`
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks` prints nothing
</verification>

<success_criteria>
Moonshot relevance and radicalness can be scored at dev time on synthetic text through one additive, guarded profile, with every non-synthetic input refused before a request exists.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-04-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, the profile lines added, the four guard smoke outcomes, the guard gap statement (Score strings enforced by builder and test, not by question_strings_from_file_key), test-353-tripwires counts before and after, whether data/ROOM.md was edited, and `## Peer messages (to relay)` when no messaging tool was available.
</output>
