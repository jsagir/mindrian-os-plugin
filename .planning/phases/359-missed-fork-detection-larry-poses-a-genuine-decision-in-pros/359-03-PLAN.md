---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 03
type: execute
wave: 2
depends_on: ["359-01"]
files_modified:
  - tests/test-359-declared-arm.cjs
  - scripts/check-card-fire.cjs
  - tests/test-359-mcp-declared.cjs
  - lib/mcp/stop-gate-handler.cjs
autonomous: true
requirements: [FORK359-04, FORK359-05, FORK359-07]
tags: [card-fire, classifier, stop-hook, mcp, tri-polar, deterministic]

must_haves:
  truths:
    - "classifyCardFire keeps `card-fired` first; a turn with no PRIMARY, no BACKSTOP and no declaration still returns `no-gate-signal`; a declared turn is owned by the declared arm: session ceiling then retry ceiling degrade with today's exact reason strings, yes/no-shaped PRACTICAL labels pass as `gate-is-simple-binary`, otherwise intercept `declared-fork-no-card` (D-06, SPEC R4)"
    - "The declared arm skips backstop-uncorroborated, primary-existence, synthetic-preceding-source, gate-irrelevant-to-turn and gate-already-answered, and wins over a stale irrelevant PRIMARY hit on the same turn (D-06 step 4, D-07)"
    - "The yes/no exemption reads the practical labels only (the final What-if moonshot excluded), because under N-3 every declaration carries a moonshot and isYesNoShapedGate requires exactly 2 labels; this interpretation is surfaced for navigator confirmation at the plan-06 checkpoint (N-3, SPEC R4)"
    - "A turn without a declaration reaches byte-for-byte the same classifier code as pre-359 and gets the same verdict object and the same retry key (D-06 step 5, D-08, SPEC R5)"
    - "deriveTurnSignals threads `fork_declared` and `declared_labels` from parseForkDeclaration over THIS turn's final text: a direct `output_text` (MCP, replay direct mode), else the Stop stdin `last_assistant_message`, else the last text in the current-window `assistant_contents`; a declaration that sits only before the last user record is never read, and no direct `fork_declared` field is accepted (D-10, RESEARCH Pattern 1, Pitfall 1)"
    - "turnContextHash appends `|decl:<sorted NFKC-lowercased labels>` only for a declared turn, via an exported declaredIdentity(turn); non-declared keys are byte-identical to pre-359 (D-08)"
    - "buildEnforcementEnvelope is unchanged: the `declared-fork-no-card` slug appears only in the local intercept log, never in the user-facing reason or systemMessage (D-09, CR-06)"
    - "The MCP stop_gate_check fire payload's options equal `declared_labels` in order with the moonshot last, and gate dedup keys on `gate_signature || declaredIdentity(turn)`, so two distinct declared forks in one session both fire (D-10, N-3, RESEARCH Finding 5, SPEC R7)"
    - "The two box regex literals stay byte-identical and classifyCardFire reads output text only through the pre-359 helper call sites (tripwires, SPEC R5)"
  artifacts:
    - path: "scripts/check-card-fire.cjs"
      provides: "declared arm, declaredIdentity, retry-key suffix, deriveTurnSignals threading"
      contains: "declared-fork-no-card"
    - path: "lib/mcp/stop-gate-handler.cjs"
      provides: "declared labels as card options; dedup subject fallback"
      contains: "declaredIdentity"
    - path: "tests/test-359-declared-arm.cjs"
      provides: "R4 legs, R5 tripwires (--tripwires)"
      min_lines: 220
    - path: "tests/test-359-mcp-declared.cjs"
      provides: "R7 MCP payload and dedup legs"
      min_lines: 110
  key_links:
    - from: "scripts/check-card-fire.cjs"
      to: "lib/core/fork-declaration.cjs"
      via: "deriveTurnSignals calls parseForkDeclaration exactly once"
      pattern: "parseForkDeclaration\\("
    - from: "lib/mcp/stop-gate-handler.cjs"
      to: "scripts/check-card-fire.cjs"
      via: "deriveTurnSignals(ctx) threading and checkCardFire.declaredIdentity for the dedup subject"
      pattern: "declaredIdentity"
    - from: "scripts/check-card-fire.cjs"
      to: "lib/core/gate-relevance.cjs"
      via: "isYesNoShapedGate over practical labels mapped through normalizeOptionLabel"
      pattern: "normalizeOptionLabel"
---

<objective>
Teach the card gate to read Larry's own fork declaration: a third, deterministic gate signal in classifyCardFire fed by deriveTurnSignals, with a retry key that includes the declared labels, and an MCP stop_gate_check that renders the declared labels as the card and never dedups two different forks together.

Purpose: SPEC R4, R5 (tripwires), R7; D-06 to D-10; N-3; RESEARCH Findings 1, 2, 5, Pattern 1, Pitfall 1.
Output: changes to `scripts/check-card-fire.cjs` and `lib/mcp/stop-gate-handler.cjs`; `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-SPEC.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-RESEARCH.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-01-SUMMARY.md
@lib/core/fork-declaration.cjs

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`.

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`; otherwise STOP. 357-07 changed check-card-fire (the 'harness' guard, preceding_user_is_meta): re-read the whole classifier and deriveTurnSignals from disk; line numbers below are pre-357 hints only.

INERTNESS GATE: `node tests/test-359-inertness.cjs` exits 0 before the first edit and after each commit (the "0 new false blocks on the 357 corpus" gating check).

Pre-359 code root for comparison legs: `PRE359=$(node -p "require('./tests/fixtures/card-fire-replay/pre-359.json').pre_359_sha")`; the tests build it as `git archive $PRE359 scripts lib data package.json .claude-plugin | tar -x -C <mkdtemp>` plus a `node_modules` symlink to the repo's, and require the archived `scripts/check-card-fire.cjs` by absolute path. Clean up in finally.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md, lib/mcp/tools/stop-gate.cjs (read only). No em-dashes or en-dashes. Runtime stays pure local CJS: no new require of any network module, any `scripts/` dev tool or any ledger file; `MAX_FORCE_RETRIES` and `MAX_SESSION_INTERCEPTS` are reused, never re-declared (test-198 and test-209 pin them); do not refactor the existing ceiling checks into a helper (D-06 step 5 byte-for-byte rule); do not add `hookSpecificOutput` to the Stop envelope.

Known pre-existing reds (R-J, recorded by 357): record `node tests/test-card-fire-relevance-gate.cjs` pass/fail counts before and after; they must not get worse.

Test hygiene: as in 359-01.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 (this plan) |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declared arm, declaration source threading, retry-key suffix, and the R4 legs plus R5 tripwires</name>
  <files>tests/test-359-declared-arm.cjs, scripts/check-card-fire.cjs</files>
  <read_first>
    - scripts/check-card-fire.cjs (whole classifyCardFire; turnContextHash; computeBackstopHit; the ASCII_BOX_GLYPH_RE and ASCII_BOX_UNCONDITIONAL_RE literal lines; readTranscriptTurn and how it calls turnText.readTurnText and reads `assistant_contents`; deriveTurnSignals whole, including its direct-field precedence rules and the object it returns; main() from readStdinJson to the counters, appendInterceptLog and buildEnforcementEnvelope; module.exports)
    - lib/hmi/turn-text.cjs (readTurnText return fields: output_text, assistant_contents of the current window; extractAssistantText export; lastAssistantText is never reset at a user boundary)
    - lib/core/fork-declaration.cjs (parseForkDeclaration return shape, MIN_LABELS) and lib/core/gate-relevance.cjs (normalizeOptionLabel, isYesNoShapedGate)
    - tests/test-198-stop-gate-retry-ceiling.test.cjs (lines ~380-400: the constant source pins) and tests/test-209-primary-sidechannel.cjs (lines ~1260-1280)
    - PD/359-RESEARCH.md Finding 1, Finding 2, Finding 5, Pattern 1, Pattern 3, Code Examples 2 and 3, Pitfall 1, Anti-Patterns
  </read_first>
  <behavior>
    - A1 declared (3 labels) + no card + no PRIMARY/BACKSTOP -> {intercept: true, reason: 'declared-fork-no-card', degrade: false}
    - A2 declared + askuserquestion_fired true -> pass 'card-fired'
    - A3 declared `Yes, ship it | No, hold it | What if we let the users ship it` -> pass 'gate-is-simple-binary'; the same yes/no pair with a third practical label -> intercept (isYesNoShapedGate needs exactly 2)
    - A4 declared + retry_count === MAX_FORCE_RETRIES -> degrade with exactly the reason string the PRIMARY arm returns at the same count (both arms run in the test and compared)
    - A5 declared + session_count === MAX_SESSION_INTERCEPTS -> degrade with exactly the PRIMARY arm's session-ceiling string
    - A6 D-07: a turn whose PRIMARY hit is passed as gate-irrelevant-to-turn without a declaration -> the same turn plus a declaration intercepts 'declared-fork-no-card'
    - A7 inertness: over a matrix of non-declared turns (no signal; PRIMARY relevant; PRIMARY irrelevant; BACKSTOP corroborated; BACKSTOP uncorroborated; card fired; both ceilings), HEAD's classifyCardFire deep-equals the pre-359 archived classifier
    - A8 turnContextHash: every non-declared matrix turn hashes identically to the archived pre-359 function; a declared turn hashes differently from its undeclared twin; the declared hash is invariant to label order and to case and whitespace differences, differs for a different label set, and two Hebrew label sets that differ hash differently
    - A9 declaredIdentity: '' for undeclared; 'decl:' + sorted NFKC-lowercased, whitespace-collapsed labels joined by ',' for declared
    - A10 source precedence: a direct output_text declaration wins; with no direct output_text, `last_assistant_message` is parsed; with neither, the last text in the transcript's current-window assistant_contents is parsed; a transcript whose ONLY declaration is in an assistant record before the last user record gives fork_declared false; a transcript whose last assistant record (stale, previous turn) declares while last_assistant_message is plain prose gives fork_declared false
    - A11 an env carrying `fork_declared: true` and `declared_labels` with plain output_text yields fork_declared false (no direct field accepted)
    - A12 end to end: spawning `node scripts/check-card-fire.cjs` with Stop stdin `{hook_event_name: 'Stop', session_id, transcript_path: <temp jsonl with a human prompt then an assistant reply ending in a declaration>, last_assistant_message: <that reply>}` in a hermetic env prints the existing block envelope; stdout never contains `declared-fork-no-card`; the temp intercept log's last record reason is `declared-fork-no-card`
    - A13 `--tripwires` mode: both regex literal lines equal frozen strings byte-for-byte; classifyCardFire's non-comment source has 0 `.test(`, `.match(`, `.exec(`, `new RegExp`, and `outputText` appears only in the pre-359 contexts (its declaration, `computeBackstopHit(`, `gateRelevance.extractOptionLabels(`, the gateSubjectText fallback), counted equal to the frozen pre-359 count; deriveTurnSignals' non-comment regex-use counts equal frozen pre-359 counts and `parseForkDeclaration(` appears exactly once in the file; `MAX_FORCE_RETRIES = 3;` and `MAX_SESSION_INTERCEPTS = 12;` each appear exactly once
  </behavior>
  <action>
    0. Before editing: INERTNESS GATE green; record test-198, test-209, test-238-card-fire-corpus, test-357-harness-source and test-card-fire-relevance-gate results; from `git show $PRE359:scripts/check-card-fire.cjs` copy the two regex literal lines and measure the classifyCardFire `outputText` count and the deriveTurnSignals regex-use counts; these become frozen constants in the test.
    1. Write `tests/test-359-declared-arm.cjs` FIRST (header naming FORK359-04, FORK359-05, SPEC R4, R5, D-06 to D-09, N-3; test hygiene; hermetic MINDRIAN_HOME and CARD_FIRE_SIDECHANNEL_PATH per leg; the pre-359 archive helper from the context). Default mode runs A1 to A12; `--tripwires` runs A13 only. Build turns directly for A1 to A9 (fields `fork_declared`, `declared_labels`, `askuserquestion_fired`, `ran_entries`, `retry_count`, `session_count`, and whatever PRIMARY relevance fields the shipped classifier reads) and through deriveTurnSignals with temp jsonl transcripts for A10 to A12. Run it and record RED.
    2. `scripts/check-card-fire.cjs`, minimal additive edits: (a) require `../lib/core/fork-declaration.cjs` next to the existing gate-relevance require, following that require's style. (b) Add `declaredIdentity(turn)` per RESEARCH Code Example 3 (NFKC, lowercase, whitespace collapsed, trimmed, sorted, joined by ',', prefixed `decl:`, '' when not declared). (c) turnContextHash: compute `const decl = declaredIdentity(t);` and append `'|' + decl` to the hashed string only when decl is non-empty, so the undeclared input string is byte-identical to today (D-08). (d) readTranscriptTurn: additionally return `decl_source_text` = the text (via turnText.extractAssistantText) of the LAST message in `turn.assistant_contents` whose text is non-empty, or '' (reuse the same readTurnText result; never read the transcript twice). (e) deriveTurnSignals: `declSource` = `e.output_text` when it is a string given directly, else `e.last_assistant_message` when it is a string, else the transcript's `decl_source_text`, else ''; `const decl = forkDeclaration.parseForkDeclaration(declSource);` (the only call); add `fork_declared: decl.declared === true` and `declared_labels: decl.declared === true ? decl.labels.slice() : []` to the returned object. Every other returned field keeps its current derivation (output_text included). Ignore any `fork_declared` or `declared_labels` on the env. (f) classifyCardFire: right after backstopHit, add the comment line `// ANCHOR fork359-declared-arm (mutation M1 in tests/test-359-replay.cjs edits the next line)` then `const forkDeclared = t.fork_declared === true && Array.isArray(t.declared_labels) && t.declared_labels.length >= forkDeclaration.MIN_LABELS;`; change the no-gate-signal condition to `if (!primaryHit && !backstopHit && !forkDeclared)`; immediately after that return, add the declared block: when forkDeclared, check the session ceiling then the retry ceiling by copying the existing ceiling code's field reads and reason-string construction verbatim (same constants), then `if (gateRelevance.isYesNoShapedGate(t.declared_labels.slice(0, -1).map(gateRelevance.normalizeOptionLabel))) return {intercept: false, reason: 'gate-is-simple-binary', degrade: false};` then `return {intercept: true, reason: 'declared-fork-no-card', degrade: false};`, with a comment citing D-06, D-07, N-3 (practical labels only) and SPEC R4. Everything after is unchanged. (g) Export `declaredIdentity`. Do not touch buildEnforcementEnvelope, appendInterceptLog, the regex literals, the ceiling constants or any other function.
    3. GREEN: `node tests/test-359-declared-arm.cjs`, `node tests/test-359-declared-arm.cjs --tripwires`, the step-0 regression tests (no new reds; relevance-gate counts not worse), `node tests/test-359-inertness.cjs`. Commit: `git add tests/test-359-declared-arm.cjs` then `git commit --only -m "feat(359-03): declared-fork arm in the card gate, decl retry key, this-turn declaration source (R4, R5, D-06..D-09, N-3)" -- scripts/check-card-fire.cjs tests/test-359-declared-arm.cjs`.
  </action>
  <verify>
    <automated>node tests/test-359-declared-arm.cjs && node tests/test-359-declared-arm.cjs --tripwires && node tests/test-198-stop-gate-retry-ceiling.test.cjs && node tests/test-209-primary-sidechannel.cjs && node tests/test-238-card-fire-corpus.cjs && node tests/test-359-inertness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All six verify commands exit 0
    - `grep -v '^\s*//' scripts/check-card-fire.cjs | grep -c "declared-fork-no-card"` is 1 and `grep -c "ANCHOR fork359-declared-arm" scripts/check-card-fire.cjs` is 1
    - `grep -v '^\s*//' scripts/check-card-fire.cjs | grep -c "parseForkDeclaration("` is 1
    - `git diff $PLAN_BASE -- scripts/check-card-fire.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | grep -v '^[-+]\s*//' | grep -c .` is at most 45 (minimal code diff, comments excluded)
    - The buildEnforcementEnvelope function source is unchanged: `node -e` comparing its text extracted from `git show $PLAN_BASE:scripts/check-card-fire.cjs` and from HEAD (from `function buildEnforcementEnvelope` to the next top-level `function `) prints identical sha256 values
    - `node tests/test-card-fire-relevance-gate.cjs` fail count is no higher than before this task
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/check-card-fire.cjs tests/test-359-declared-arm.cjs` reports 0 for each
  </acceptance_criteria>
  <done>A declared fork with no card is intercepted deterministically with bounded escape and the yes/no exemption, every undeclared turn keeps its exact pre-359 verdict and key, and the tripwires freeze the no-free-text rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP stop_gate_check renders the declared labels and dedups per declared fork</name>
  <files>tests/test-359-mcp-declared.cjs, lib/mcp/stop-gate-handler.cjs</files>
  <read_first>
    - lib/mcp/stop-gate-handler.cjs (buildStopGateCard near :350; _resetForTest; handleStopEvent whole: deriveTurnSignals(ctx), counters, verdict branches, the gateContext dedup block near :521, the fire:true return shape and where the rendered gate_render card sits)
    - lib/mcp/gate-dedup.cjs (the sid|gate|subject key) and lib/mcp/gate-render.cjs (normalizeCard, the options shape)
    - lib/mcp/tools/stop-gate.cjs (the zod input schema; read only)
    - tests/test-198-stop-gate-retry-ceiling.test.cjs (lines ~60-80: MINDRIAN_ROOMS_HOME / MINDRIAN_ROOMS_ROOT isolation for the MCP path)
    - PD/359-01-SUMMARY.md `## 357 shipped contracts` item (b) (the rendered-options field path)
  </read_first>
  <behavior>
    - M1 handleStopEvent(sid, {session_id: sid, output_text: <reply ending in a 4-label declaration>}) returns fire true, and the rendered card's option labels deep-equal the declared labels in order with the What-if moonshot last; the header text is unchanged
    - M2 two distinct declared forks in one session, with no _resetForTest between them, both return fire true
    - M3 the identical declared fork sent twice in one session: the second returns `dedup-already-fired-this-session`
    - M4 undeclared turns keep today's behavior: plain prose -> fire false `no-gate-signal`; a bracket-box turn's options still come from rawExtractOptionLabels (compared with the pre-359 archived handler's buildStopGateCard output for the same turn)
    - M5 a declared yes/no practical pair -> fire false `gate-is-simple-binary`
    - M6 the stop_gate_check zod input schema declares no `fork_declared` or `declared_labels` field (a caller cannot assert a declaration without text)
    - M7 every leg runs with mkdtemp MINDRIAN_HOME, CARD_FIRE_SIDECHANNEL_PATH, MINDRIAN_ROOMS_HOME and MINDRIAN_ROOMS_ROOT, and every result's business room_dir is null or undefined (the real active room is never touched)
  </behavior>
  <action>
    1. Write `tests/test-359-mcp-declared.cjs` FIRST (header naming FORK359-07, SPEC R7, D-10, N-3, RESEARCH Finding 5; test hygiene) covering M1 to M7; RED.
    2. `lib/mcp/stop-gate-handler.cjs`: in buildStopGateCard, take the labels from `turn.declared_labels` (a copy) when `turn.fork_declared === true` and the array is non-empty, else from `rawExtractOptionLabels(turn && turn.output_text)` exactly as today; nothing else in that function changes (the header string, selectMode and the fallback options stay). In the dedup gateContext, change the subject to `turn.gate_signature || checkCardFire.declaredIdentity(turn) || ''` (use the module variable name the file already uses for check-card-fire). Add a short comment at each site citing D-10, N-3 and Finding 5. Do not re-declare MAX_FORCE_RETRIES or MAX_SESSION_INTERCEPTS; change nothing else.
    3. GREEN: `node tests/test-359-mcp-declared.cjs`, `node tests/test-198-stop-gate-retry-ceiling.test.cjs`, `node tests/test-357-replay.cjs`, `node tests/test-359-inertness.cjs`. Commit: `git add tests/test-359-mcp-declared.cjs` then `git commit --only -m "feat(359-03): stop_gate_check renders declared labels and dedups per declared fork (R7, D-10, N-3)" -- lib/mcp/stop-gate-handler.cjs tests/test-359-mcp-declared.cjs`.
  </action>
  <verify>
    <automated>node tests/test-359-mcp-declared.cjs && node tests/test-198-stop-gate-retry-ceiling.test.cjs && node tests/test-357-replay.cjs && node tests/test-359-inertness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All four verify commands exit 0
    - `grep -v '^\s*//' lib/mcp/stop-gate-handler.cjs | grep -c "declaredIdentity"` is 1 and `grep -v '^\s*//' lib/mcp/stop-gate-handler.cjs | grep -c "declared_labels"` is at least 1
    - `grep -v '^\s*//' lib/mcp/stop-gate-handler.cjs | grep -c "MAX_FORCE_RETRIES *=\|MAX_SESSION_INTERCEPTS *="` is 0
    - `git diff $PLAN_BASE -- lib/mcp/stop-gate-handler.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | grep -v '^[-+]\s*//' | grep -c .` is at most 12
    - `git diff --name-only $PLAN_BASE..HEAD -- lib/mcp/tools lib/mcp/gate-render.cjs lib/mcp/gate-dedup.cjs` prints nothing
    - `grep -c "$(printf '\xe2\x80\x94')" lib/mcp/stop-gate-handler.cjs tests/test-359-mcp-declared.cjs` reports 0 for each
  </acceptance_criteria>
  <done>Desktop and Cowork get the same declared verdict, a card built from the declared labels with the moonshot last, and one dedup slot per distinct declared fork.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output -> Stop hook verdict | a declaration is model text that now forces a card; it must never force one for a turn that posed no fork |
| MCP caller -> stop_gate_check | a Desktop/Cowork caller supplies output_text directly |
| hook -> user-visible block text | CR-06: no internal slug or model directive reaches the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-09 | Tampering (false block) | stale declaration re-read on a later turn | mitigate | declaration parsed from this turn's final text only (direct output_text, last_assistant_message, current-window assistant text); A10 legs for the before-last-user and lagging-transcript cases |
| T-359-10 | Denial of service | prompt injection making every turn end in a declaration | mitigate | existing per-gate (3) and session (12) ceilings apply first in the declared arm; the host also caps consecutive Stop blocks; A4/A5 legs |
| T-359-11 | Spoofing | a caller asserting fork_declared without text | mitigate | deriveTurnSignals ignores any direct fork_declared field (A11); the zod schema carries none (M6) |
| T-359-12 | Information disclosure | declared labels leaving the machine | mitigate | labels stay in-process and in the local intercept log; no new require of a network module; `! grep -rn api.typesafe.ai lib/ hooks/` |
| T-359-13 | Tampering | a free-text fork heuristic added to the classifier | mitigate | A13 tripwires: regex literals frozen, no regex use in classifyCardFire, outputText only at pre-359 call sites, one parseForkDeclaration call |
| T-359-14 | Repudiation | a verdict change on historic outputs | mitigate | A7/A8 compare against the archived pre-359 classifier; test-359-inertness before and after each commit |
| T-359-15 | Denial of service | MCP dedup suppressing later declared forks | mitigate | dedup subject falls back to declaredIdentity; M2/M3 legs |
</threat_model>

<verification>
- `node tests/test-359-declared-arm.cjs && node tests/test-359-declared-arm.cjs --tripwires && node tests/test-359-mcp-declared.cjs && node tests/test-359-inertness.cjs` all exit 0
- `! grep -rn "api.typesafe.ai" lib/ hooks/`
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
A declared prose fork with no card is intercepted on the CLI Stop hook and on the MCP stop_gate_check with the declared labels as the card, while every turn without a declaration keeps its exact pre-359 verdict, reason and retry key.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-03-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, both commit shas, the frozen tripwire constants, relevance-gate counts before and after, the inertness run counts, and a `## Interpretation to confirm` note (yes/no exemption over practical labels, N-3) for the plan-06 checkpoint.
</output>
