---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 10
type: execute
wave: 6
depends_on: ["359-05", "359-09"]
files_modified:
  - scripts/forward-fork-scenarios-359.cjs
  - tests/fixtures/forward-fork-stream-359/
  - tests/test-359-forward-harness.cjs
autonomous: false
requirements: [FORK359-09]
tags: [forward-run, smoke, paid, navigator-gated, headless]

must_haves:
  truths:
    - "No money is spent before the navigator approves the smoke run at a blocking checkpoint that shows the preflight result, the exact command lines, the model (claude-sonnet-5, never Fable), the pre and post shas and the ceiling of 2 runs x USD 0.40 (N-2, N-5, D-12)"
    - "The smoke run is one fork scenario, one run per arm, pre = R8_SHA^ and post = R8_SHA, from git archive snapshots, and it counts toward nothing in the R9 result (D-12, RESEARCH Open Question 1)"
    - "The smoke run verifies every open headless assumption from the observed stream: AskUserQuestion is offered (probe host works), exactly one `mos` plugin at the snapshot path, the agent is mos:larry-extended, no MODE_ROUTING `No room detected`, multi-turn stream-json input is processed turn by turn, Stop hook events split attempt 1 from the re-prompt, and the probe log records any card (RESEARCH A1 to A9)"
    - "The run mode for the batch is decided from evidence: probe mode when the host works, else the N-4 text fallback (declaration adoption only), recorded with the failing check (N-4)"
    - "The real per-run cost and duration from the result events replace the estimates, and the projected batch total (scenarios x runs x 2 arms x measured cost x 1.25) is recorded; above USD 60 the harness refuses and the plan-11 checkpoint must decide (N-2, N-5, RESEARCH Finding 12)"
    - "When the observed stream shape differs from the canned fixtures, the parser and the canned fixtures are corrected to the observed STRUCTURE with placeholder text only, and the offline harness test stays green; raw streams stay outside the repo"
  artifacts:
    - path: "tests/test-359-forward-harness.cjs"
      provides: "offline legs, green against the observed stream structure"
      contains: "parseRun"
  key_links:
    - from: "scripts/forward-fork-scenarios-359.cjs"
      to: ".planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-09-SUMMARY.md"
      via: "R8_SHA read for --r8-sha"
      pattern: "r8-sha"
---

<objective>
Spend the first, smallest amount of money only after the navigator says yes: one smoke run per arm that proves the headless instrument sees what production Larry does, fixes any stream-shape mismatch in the parser, picks probe or text mode from evidence, and replaces the cost estimate with a measured projection.

Purpose: SPEC R9 (forward adoption measurement), D-12, N-2, N-4, N-5, RESEARCH Findings 8 to 12 and Open Question 1.
Output: a recorded smoke verdict, mode and projection; parser and canned-fixture corrections only when the observed stream differs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-RESEARCH.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-05-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-06-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-09-SUMMARY.md
@scripts/forward-fork-scenarios-359.cjs

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`. R8_SHA = the value on the first line of PD/359-09-SUMMARY.md (`R8_SHA: <sha>`). RESULTS_DIR = `~/.cache/mindrian-dev/359-forward/`.

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`; `head -1 PD/359-09-SUMMARY.md` starts with `R8_SHA: `; `git cat-file -e <R8_SHA>` exits 0; `bash tests/run-all-359.sh` exits 0.

Spend rules (N-2, N-5, hard): model `claude-sonnet-5` only, never Fable; `--max-budget-usd 0.40` on every run; the smoke run is at most 2 runs; no run before the Task 2 approval; never `--bare`; never edit `~/.claude/settings*.json` or `~/.claude/agents/`.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md, and no runtime or prose file (lib/, hooks/, agents/, skills/, scripts/session-start, scripts/check-card-fire.cjs). Raw streams never enter git. No em-dashes or en-dashes.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 (this plan) |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto">
  <name>Task 1: Preflight and dry run for the smoke (spends nothing)</name>
  <files>scripts/forward-fork-scenarios-359.cjs</files>
  <read_first>
    - scripts/forward-fork-scenarios-359.cjs (flags, preflight, buildRunCommand, results path)
    - PD/359-09-SUMMARY.md (R8_SHA) and PD/359-05-SUMMARY.md (the command line recorded for one probe-mode run)
    - `ls -l ~/.claude/agents/larry-extended.md 2>&1` and `claude --version`
  </read_first>
  <action>
    1. Confirm `git show --stat --format= <R8_SHA>` lists only the six R8 paths, so pre (`<R8_SHA>^`) and post differ only by the declaration prose.
    2. Run `node scripts/forward-fork-scenarios-359.cjs --r8-sha <R8_SHA> --arm both --smoke --dry-run`. It must print the preflight result (model claude-sonnet-5, shadow-agent check passed, claude version at or above 2.1.259, per-run cap 0.40), the two snapshot tree paths, and the two exact command lines, and must spend nothing. If preflight refuses, STOP and report the refusal (no checkpoint is raised for a refused preflight; the fix is a gap-closure plan).
    3. Put the dry-run output (commands, shas, preflight lines, ceiling USD 0.80) in the checkpoint packet. This task edits no file unless the dry run exposes a harness bug; any fix is limited to scripts/forward-fork-scenarios-359.cjs, keeps `node tests/test-359-forward-harness.cjs` green, and is committed with `git commit --only -m "fix(359-10): <what>" -- scripts/forward-fork-scenarios-359.cjs`.
  </action>
  <verify>
    <automated>node scripts/forward-fork-scenarios-359.cjs --r8-sha "$(head -1 .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-09-SUMMARY.md | sed 's/^R8_SHA: //')" --arm both --smoke --dry-run && node tests/test-359-forward-harness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The dry run exits 0, prints two command lines containing `--model claude-sonnet-5`, `--max-budget-usd 0.40`, `--agent mos:larry-extended` and `--no-session-persistence`, and neither contains `--bare`
    - `ls ~/.cache/mindrian-dev/359-forward/streams 2>/dev/null | wc -l` is 0 (nothing ran)
    - The offline harness test exits 0
  </acceptance_criteria>
  <done>The navigator can see exactly what the smoke will run and what it can cost before any money is spent.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Navigator approves the smoke spend</name>
  <decision>Approve one smoke run per arm (2 runs, at most USD 0.80, Sonnet 5) to verify the headless instrument before the batch.</decision>
  <context>The docs say `claude -p` offers AskUserQuestion only with a permission host; the probe host, the stream shape, the scratch-room binding and the real cost are unverified until a paid run. The smoke result decides probe mode or the N-4 text fallback and produces the measured cost for the batch projection. The smoke runs count toward nothing in the R9 result.</context>
  <options>
    <option id="approve-smoke">
      <name>Approve the smoke (at most USD 0.80)</name>
      <pros>Verifies A1 to A9 on the real stack; replaces the cost estimate with a measurement; unblocks the plan-11 decision.</pros>
      <cons>Spends up to USD 0.80.</cons>
    </option>
    <option id="decline">
      <name>Decline</name>
      <pros>No spend.</pros>
      <cons>R9 is not measured; FORK359-09 stays open with the reason at close-out; R8 stays in place unmeasured.</cons>
    </option>
  </options>
  <resume-signal>Type "approve-smoke" or "decline"</resume-signal>
  <files>.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-10-SUMMARY.md</files>
  <read_first>
    - The Task 1 dry-run output (commands, shas, preflight lines, ceiling USD 0.80)
  </read_first>
  <action>Present the dry-run packet to the navigator (what runs, on which shas, with which model, at what maximum cost, and that the smoke counts toward nothing in the R9 result), then wait. Record the reply verbatim under `## Navigator ruling (smoke)` in the SUMMARY draft. Nothing is spent and no file other than the SUMMARY draft changes in this task.</action>
  <verify>
    <automated>grep -c "## Navigator ruling (smoke)" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-10-SUMMARY.md && test ! -e ~/.cache/mindrian-dev/359-forward/smoke.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY draft records `approve-smoke` or `decline` verbatim
    - No smoke results file exists before the approval (nothing spent)
  </acceptance_criteria>
  <done>The smoke spend is approved or declined by the navigator on the record, before any money moves.</done>
</task>

<task type="auto">
  <name>Task 3: Run the smoke, verify the instrument, fix parse mismatches, choose the mode, project the cost</name>
  <files>scripts/forward-fork-scenarios-359.cjs, tests/fixtures/forward-fork-stream-359/, tests/test-359-forward-harness.cjs</files>
  <read_first>
    - The navigator's resume message (Task 2); on `decline`, skip to step 6
    - scripts/forward-fork-scenarios-359.cjs (parseRun, evaluate, results format)
    - tests/fixtures/forward-fork-stream-359/*.jsonl (the provisional canned shapes)
    - PD/359-RESEARCH.md Assumptions Log A1 to A9
  </read_first>
  <action>
    1. Run `node scripts/forward-fork-scenarios-359.cjs --r8-sha <R8_SHA> --arm both --smoke --results ~/.cache/mindrian-dev/359-forward/smoke.jsonl` (sequential; at most 2 runs).
    2. From the two raw streams under RESULTS_DIR/streams, check and record each assumption as VERIFIED or FAILED with the evidence (event types and field names only, never reply text): A1 `system/init.tools` contains AskUserQuestion and a probe log exists when a card was attempted; A2 `system/init.plugins` has exactly one `mos` whose path is the arm tree and the user plugins are absent; A3 Stop hook events appear with --include-hook-events and split attempt 1; A7 the Stop hook saw `last_assistant_message` (the per-run intercept log or a declared-arm verdict is consistent with it); A8 the three stream-json user turns were processed in order with one result each; A9 the SessionStart output lacks `No room detected`; plus the agent name, `total_cost_usd` and `duration_ms` per run, and whether MINDRIAN_HOME stayed inside the temp dir (the real `~/.mindrian/card-fire-retries.json` mtime is unchanged before and after).
    3. If the observed event shapes differ from the canned fixtures, correct parseRun and replace the canned fixtures' STRUCTURE to match what was observed (placeholder text only; never copy reply text), keep every behavior case, and make `node tests/test-359-forward-harness.cjs` green. Commit only when something changed: `git add tests/fixtures/forward-fork-stream-359` then `git commit --only -m "fix(359-10): forward parser matches the observed stream-json shape (A1-A9)" -- scripts/forward-fork-scenarios-359.cjs tests/fixtures/forward-fork-stream-359 tests/test-359-forward-harness.cjs`.
    4. Mode: `probe` when A1 and A3 are VERIFIED; otherwise `text` (N-4: declaration adoption only, measured from text) with the failing check named. If A2 or A9 FAILED, the instrument measures the wrong Larry or a confounded turn 1: record `SMOKE: BLOCKED` with the reason and stop (a gap-closure plan fixes the harness before plan 11).
    5. Projection: `node scripts/forward-fork-scenarios-359.cjs --project --cost-per-run <max measured cost>` over the full scenario set (all scenarios x runs_per_scenario x 2 arms x 1.25); record the total and whether it is within USD 60.
    6. Write the SUMMARY: first line `SMOKE: PASSED mode=<probe|text>` or `SMOKE: BLOCKED - <reason>` or `SMOKE: DECLINED`; then the assumption table, the two runs' cost and duration, the projection, and the smoke's own outcome counts marked as not part of the R9 result.
  </action>
  <verify>
    <automated>node tests/test-359-forward-harness.cjs && head -1 .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-10-SUMMARY.md | grep -E "^SMOKE: (PASSED mode=(probe|text)|BLOCKED|DECLINED)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify exits 0
    - On approval, `wc -l < ~/.cache/mindrian-dev/359-forward/smoke.jsonl` is at most 2 and every row's `cost_usd` is at most 0.40
    - `git ls-files | grep -c "359-forward"` is 0 (no raw stream or results file in git)
    - The SUMMARY carries the A1 to A9 table with VERIFIED or FAILED per item and the projected total in USD
    - `git diff --name-only $PLAN_BASE..HEAD -- lib hooks agents skills scripts/session-start scripts/check-card-fire.cjs` prints nothing
  </acceptance_criteria>
  <done>The headless instrument is verified on the real stack (or its failure is named), the batch mode is chosen from evidence, and the batch cost is a measured projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> paid model API | the first real spend of the phase |
| headless session -> navigator machine state | a mis-isolated run could touch real rooms or counters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-46 | Denial of service (financial) | smoke spend | mitigate | blocking navigator checkpoint before any run; 2 runs max; --max-budget-usd 0.40 per run; acceptance checks every row's cost |
| T-359-47 | Spoofing | wrong Larry or shadowed agent measured | mitigate | preflight shadow check; A2 plugin assertion; SMOKE: BLOCKED stops the batch |
| T-359-48 | Tampering | real ~/.mindrian or rooms touched | mitigate | hermetic env; real retries-file mtime compared before and after |
| T-359-49 | Information disclosure | reply text entering git | mitigate | canned fixtures corrected to structure with placeholder text; streams stay in ~/.cache; acceptance greps git ls-files |
</threat_model>

<verification>
- `node tests/test-359-forward-harness.cjs` exits 0
- `git ls-files | grep -c "359-forward"` is 0
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
The smoke run happens only with navigator approval, costs at most USD 0.80, and leaves a verified or explicitly failed instrument, a chosen mode and a measured batch projection.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-10-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). FIRST line `SMOKE: PASSED mode=<probe|text>`, `SMOKE: BLOCKED - <reason>` or `SMOKE: DECLINED`. Then the A1 to A9 table, per-run cost and duration, the projection, and any parser or fixture correction.
</output>
