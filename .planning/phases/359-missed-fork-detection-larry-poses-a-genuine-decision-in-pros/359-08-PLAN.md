---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 08
type: execute
wave: 4
depends_on: ["359-05", "359-06", "359-07"]
files_modified:
  - tests/fixtures/card-fire-replay/baseline-359.json
  - tests/test-359-replay.cjs
  - tests/test-359-devonly-tripwire.cjs
  - tests/run-all-359.sh
  - tests/run-all-238.sh
  - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-VALIDATION.md
autonomous: true
requirements: [FORK359-02, FORK359-05, FORK359-06, FORK359-10]
tags: [baseline, standing-gate, mutation, tripwire, card-fire]

must_haves:
  truths:
    - "baseline-359.json is written by `replay-card-fire.cjs --code-root git:<pre_359_sha> --fork359 --baseline write-359` after the labels are ratified; its fork359.missed_forks is the recorded pre-change R2 number and 357's baseline.json is untouched (SPEC R2, D-22)"
    - "The R5 inertness replay, the R6 declared-variant replay, the R1 fixture legs and the R4 legs are a standing gate in tests/run-all-359.sh AND in the existing card-fire runner tests/run-all-238.sh (SPEC R10, D-23)"
    - "`node tests/test-359-replay.cjs --mutation` proves both deliberate mutations fail the suite: M1 replaces the anchored `const forkDeclared = ...` line with `false` and the declared replay exits non-zero with a catch rate below 1; M2 injects a free-text fork regex before the no-gate-signal return and the declared replay exits non-zero with control_false_blocks above 0; the unmutated HEAD copy exits 0 (SPEC R10, D-23, RESEARCH Pattern 3)"
    - "tests/test-359-devonly-tripwire.cjs fails if lib/ or hooks/ reference any 359 or 357 dev script (forward harness, permission probe, moonshot scorer, replay, corpus loader, labeler), if `api.typesafe.ai` appears under lib/ or hooks/, if hooks.json names a dev script, or if the forward harness is wired into CI (SPEC R9, R10 acceptance)"
    - "Pre-existing reds recorded by 357 (R-J) stay recorded and unfixed; 359 adds no new red to run-all-238, run-all-357 or run-all-359"
  artifacts:
    - path: "tests/fixtures/card-fire-replay/baseline-359.json"
      provides: "pre-359 fork359 metrics and verdicts per id"
      contains: "pre_359_sha"
    - path: "tests/test-359-replay.cjs"
      provides: "--mutation mode (M1 arm removed, M2 free-text regex)"
      contains: "--mutation"
    - path: "tests/test-359-devonly-tripwire.cjs"
      provides: "dev-only and vendor tripwires"
      min_lines: 70
    - path: "tests/run-all-359.sh"
      provides: "the 359 standing gate"
      contains: "run_if"
    - path: "tests/run-all-238.sh"
      provides: "359 legs in the card-fire suite runner"
      contains: "test-359-replay"
  key_links:
    - from: "tests/run-all-238.sh"
      to: "tests/test-359-replay.cjs"
      via: "run_if legs"
      pattern: "run_if .*test-359"
    - from: "tests/test-359-replay.cjs"
      to: "scripts/check-card-fire.cjs"
      via: "M1 edits the line after `ANCHOR fork359-declared-arm` in an archived copy"
      pattern: "ANCHOR fork359-declared-arm"
---

<objective>
Freeze the pre-change number and make the new arm permanent: write the 359 baseline against the pre-359 commit, prove that removing the arm or adding any free-text fork regex fails the suite, ban every dev script from runtime paths, and wire the 359 legs into both the phase runner and the existing card-fire runner.

Purpose: SPEC R2 (recorded baseline), R5 and R6 as standing checks, R10; D-22, D-23.
Output: `baseline-359.json`, the mutation mode, `tests/test-359-devonly-tripwire.cjs`, `tests/run-all-359.sh`, four run-all-238 legs, VALIDATION sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-SPEC.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-VALIDATION.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-01-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-06-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-07-SUMMARY.md
@tests/run-all-357.sh
@tests/run-all-238.sh

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`.

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`, and PD/359-06-SUMMARY.md records `I-1: confirmed` and `I-2: confirmed` with no `## Gap` section; otherwise STOP (the gap closure runs first).

INERTNESS GATE: `node tests/test-359-inertness.cjs` exits 0 before and after.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md, tests/fixtures/card-fire-replay/baseline.json, tests/run-all-357.sh (357 wrote it once). This plan changes no runtime file (lib/, hooks/, scripts/). Mutants live only in mkdtemp copies. No em-dashes or en-dashes.

Test hygiene: as in 359-01.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 (this plan) |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto">
  <name>Task 1: Write baseline-359 against the pre-359 commit and implement the two mutation legs</name>
  <files>tests/fixtures/card-fire-replay/baseline-359.json, tests/test-359-replay.cjs</files>
  <read_first>
    - scripts/replay-card-fire.cjs (`--baseline write-359`, `--code-root git:<sha>` and `<dir>`, `--fork359` exit rules)
    - tests/test-359-replay.cjs (the reserved `--mutation` flag, L7)
    - tests/test-357-replay.cjs (its `--mutation` implementation: archive, node_modules symlink, cleanup; copy the recipe)
    - scripts/check-card-fire.cjs (the `ANCHOR fork359-declared-arm` comment, the line after it, and the `if (!primaryHit && !backstopHit && !forkDeclared)` line)
    - PD/359-RESEARCH.md Pattern 3 and Pattern 4
  </read_first>
  <action>
    1. `PRE359=$(node -p "require('./tests/fixtures/card-fire-replay/pre-359.json').pre_359_sha")`; run `node scripts/replay-card-fire.cjs --code-root git:$PRE359 --fork359 --surface cli --baseline write-359`. Confirm the file's `meta.pre_359_sha` equals PRE359, `verdicts` covers every loaded id (default sources plus synthetic-359), and record `fork359.missed_forks` (the R2 number), `prose_forks`, and the synthetic and dogfood split in the SUMMARY. `git status --short -- tests/fixtures/card-fire-replay/baseline.json` must print nothing.
    2. Implement `--mutation` in tests/test-359-replay.cjs (replace the reserved SKIP when baseline-359.json exists): `git archive HEAD scripts lib data package.json .claude-plugin` into a fresh mkdtemp per mutant, symlink node_modules; M1: in the copy's scripts/check-card-fire.cjs find exactly one `ANCHOR fork359-declared-arm` line (fail loudly on 0 or 2+) and replace the NEXT line with `const forkDeclared = false;`; run `node scripts/replay-card-fire.cjs --code-root <mutant> --source synthetic-359 --fork359 --surface cli --json` from the repo and require a non-zero exit with `declared_catch_rate` below 1 or null. M2: in a fresh copy, insert immediately before the line containing `if (!primaryHit && !backstopHit && !forkDeclared)` the two lines `// ANCHOR fork359-mutation-M2 (free-text fork regex, must fail the suite)` and `if (/\bor\b[^\n]*\?/.test(outputText)) { return { intercept: true, reason: 'mutation-free-text-fork', degrade: false }; }`; run the same command and require a non-zero exit with `control_false_blocks` above 0 (a rhetorical X-or-Y control flips to block). Control: an unmutated HEAD copy run the same way exits 0. Clean every mkdtemp in finally. Print one line per mutant with its exit code and the deciding count.
    3. Run `node tests/test-359-replay.cjs` (L7 now active) and `node tests/test-359-replay.cjs --mutation`; both exit 0. Commit: `git add tests/fixtures/card-fire-replay/baseline-359.json` then `git commit --only -m "test(359-08): pre-359 fork baseline and the arm-removed / free-text-regex mutation legs (R2, R10, D-22, D-23)" -- tests/fixtures/card-fire-replay/baseline-359.json tests/test-359-replay.cjs`.
  </action>
  <verify>
    <automated>node tests/test-359-replay.cjs && node tests/test-359-replay.cjs --mutation && node -e "const b=require('./tests/fixtures/card-fire-replay/baseline-359.json'),p=require('./tests/fixtures/card-fire-replay/pre-359.json');if(b.meta.pre_359_sha!==p.pre_359_sha||!(b.fork359.missed_forks>=15))process.exit(1);console.log('R2 missed_forks',b.fork359.missed_forks)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify prints `R2 missed_forks <n>` with n at least 15 and every command exits 0
    - `git diff $PLAN_BASE --stat -- tests/fixtures/card-fire-replay/baseline.json` is empty
    - The mutation output shows M1 and M2 each with a non-zero replay exit and the HEAD control with exit 0
    - `ls -d /tmp/replay-359-root-* /tmp/replay-357-root-* 2>/dev/null | wc -l` is 0 after the mutation run
    - `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts` prints nothing
  </acceptance_criteria>
  <done>The pre-change missed-fork number is frozen in a 359-owned baseline, and removing the arm or adding a free-text regex is proven to fail the suite.</done>
</task>

<task type="auto">
  <name>Task 2: Dev-only tripwire, run-all-359.sh, the run-all-238 legs, and the VALIDATION sign-off</name>
  <files>tests/test-359-devonly-tripwire.cjs, tests/run-all-359.sh, tests/run-all-238.sh, .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-VALIDATION.md</files>
  <read_first>
    - tests/run-all-357.sh (run, run_if, exit 77 SKIP, em-dash guard loop, final exit) and tests/run-all-238.sh (run_if legs; the 357 leg 357-09 added)
    - hooks/hooks.json, package.json (scripts), `ls .github/workflows 2>/dev/null`
    - tests/test-353-tripwires.cjs (helpers isPureLineComment, listFilesRecursive: style to mirror, not to require)
    - PD/359-VALIDATION.md and PD/359-01-SUMMARY.md `## 357 shipped contracts` item (g) (known reds)
  </read_first>
  <action>
    1. Create `tests/test-359-devonly-tripwire.cjs` (header naming FORK359-09, FORK359-10, SPEC R9 and R10; test hygiene). Legs: D1 no non-comment line in any `.cjs`, `.js`, `.mjs`, `.sh` or `.json` file under lib/ or hooks/ (recursive, skipping node_modules) names `forward-fork-scenarios-359`, `fork359-permission-probe`, `score-moonshots-359`, `replay-card-fire`, `card-fire-replay-corpus` or `label-card-fire-replay`; D2 no file under lib/ or hooks/ contains `api.typesafe.ai`; D3 hooks/hooks.json names none of those scripts; D4 lib/core/fork-declaration.cjs requires only `../hmi/voice-color-mark.cjs`; D5 package.json `scripts` values and every `.github/workflows/*` file (when present) never name `forward-fork-scenarios-359`, `fork359-permission-probe` or `score-moonshots-359` (not in CI).
    2. Create `tests/run-all-359.sh` modeled on run-all-357.sh (`set -uo pipefail`, repo-root cd, PASS/FAIL/SKIP, `run` honoring exit 77, `run_if label file cmd...`; header: written once in 359-08, hyphens only). Legs in order: parser (`node tests/test-359-fork-declaration.cjs`, FORK359-03); declared arm (`node tests/test-359-declared-arm.cjs`, FORK359-04); R5 tripwires (`node tests/test-359-declared-arm.cjs --tripwires`, FORK359-05); MCP declared card (`node tests/test-359-mcp-declared.cjs`, FORK359-07); corpus (`node tests/test-359-corpus.cjs`, FORK359-01); inertness (`node tests/test-359-inertness.cjs`, FORK359-05); replay (`node tests/test-359-replay.cjs`, FORK359-02, -06, -07); mutation (`node tests/test-359-replay.cjs --mutation`, FORK359-10); dev-only tripwire (`node tests/test-359-devonly-tripwire.cjs`, FORK359-10); moonshot scorer (`node tests/test-359-moonshot-scorer.cjs`, N-3); forward harness offline (`node tests/test-359-forward-harness.cjs`, FORK359-09); `run_if "359: prose budget and declaration drift (FORK359-08)" tests/test-359-prose-budget.cjs node tests/test-359-prose-budget.cjs`; regression legs with `run_if` on file existence: tests/test-357-replay.cjs, tests/test-357-corpus-loader.cjs, tests/test-357-labeler-refusal.cjs, tests/test-198-stop-gate-retry-ceiling.test.cjs, tests/test-209-primary-sidechannel.cjs, tests/test-238-card-fire-corpus.cjs, tests/test-larry-voice-mark-182.cjs, tests/test-205-voice-mark-hybrid.cjs, tests/test-larry-handoff-seam.cjs, tests/test-gate-native-fire-w1.cjs, tests/test-251-skeleton-split.cjs, lib/mcp/no-instructions.test.cjs; vendor leg `run "359: no api.typesafe.ai under lib/ or hooks/" bash -c '! grep -rn "api.typesafe.ai" lib/ hooks/'`; an em-dash and en-dash guard over the 359 surfaces (lib/core/fork-declaration.cjs, scripts/check-card-fire.cjs, lib/mcp/stop-gate-handler.cjs, lib/core/gate-relevance.cjs, scripts/replay-card-fire.cjs, scripts/card-fire-replay-corpus.cjs, scripts/label-card-fire-replay.cjs, scripts/score-moonshots-359.cjs, scripts/forward-fork-scenarios-359.cjs, scripts/fork359-permission-probe.cjs, data/jev-policies/fork359-moonshot.json, agents/larry-extended.md, skills/larry-personality/SKILL.md, scripts/session-start, lib/mcp/runtime-instructions.cjs, tests/run-all-238.sh, `find tests -maxdepth 1 -name 'test-359-*.cjs'`, `find tests/fixtures -maxdepth 2 -name '*359*.json'`, `find PD -maxdepth 1 -name '359-*.md'`; a missing file is skipped). Do not add the known pre-existing red suites (R-J). Final `exit $(( FAIL > 0 ? 1 : 0 ))`; `chmod +x`.
    3. `tests/run-all-238.sh`: re-read from disk; add FOUR `run_if` legs right after the 357 replay leg, and change nothing else: `run_if "359 fork fixtures (FORK359-01, R10)" "tests/test-359-corpus.cjs" node tests/test-359-corpus.cjs`, `run_if "359 declared arm legs (FORK359-04, R10)" "tests/test-359-declared-arm.cjs" node tests/test-359-declared-arm.cjs`, `run_if "359 inertness on the 357 corpus (FORK359-05, R10)" "tests/test-359-inertness.cjs" node tests/test-359-inertness.cjs`, `run_if "359 declared-variant replay (FORK359-06, R10)" "tests/test-359-replay.cjs" node tests/test-359-replay.cjs`.
    4. Run `bash tests/run-all-359.sh`, `bash tests/run-all-238.sh` and `bash tests/run-all-357.sh`; record the three summaries. The only allowed red in run-all-238 is the pre-existing 238-03 leg (R-J), recorded as known.
    5. PD/359-VALIDATION.md: set `nyquist_compliant: true` and `wave_0_complete: true`, tick every Wave 0 checkbox whose file now exists and the first two sign-off boxes, and set `**Approval:** approved <date> (359-08 standing gate green; R8 and R9 pending)`.
    6. Commit: `git add tests/test-359-devonly-tripwire.cjs tests/run-all-359.sh` and `git add -f PD/359-VALIDATION.md`, then `git commit --only -m "test(359-08): 359 standing gate in run-all-359 and run-all-238, dev-only tripwire (R10, D-23)" -- tests/test-359-devonly-tripwire.cjs tests/run-all-359.sh tests/run-all-238.sh PD/359-VALIDATION.md`.
  </action>
  <verify>
    <automated>bash -n tests/run-all-359.sh && node tests/test-359-devonly-tripwire.cjs && bash tests/run-all-359.sh</automated>
  </verify>
  <acceptance_criteria>
    - All verify commands exit 0 (the prose-budget leg SKIPs until plan 09)
    - `grep -v '^#' tests/run-all-238.sh | grep -c "test-359-"` is at least 4 and `git diff $PLAN_BASE -- tests/run-all-238.sh | grep '^-' | grep -vc '^---'` is 0
    - `bash tests/run-all-238.sh` shows every 359 leg PASSED and no FAILED leg other than the recorded 238-03 known red
    - `test -x tests/run-all-359.sh` and `grep -v '^#' tests/run-all-359.sh | grep -c "run_if\|^run "` is at least 20
    - `grep -c "nyquist_compliant: true" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-VALIDATION.md` is 1
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-359-devonly-tripwire.cjs tests/run-all-359.sh` reports 0 for each
  </acceptance_criteria>
  <done>The declared arm, the missed-fork metric, the inertness replay and the dev-only boundary are permanent regression checks in two runners, and both deliberate mutations are proven to fail them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future edits -> the card gate | a later change could delete the arm or reintroduce a prose guesser |
| dev scripts -> runtime paths | a vendor or headless script reachable from a hook would break the 2026-09-17 rulings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-36 | Tampering | silent removal of the declared arm | mitigate | M1 mutation leg in run-all-359; the R6 replay leg in run-all-238 |
| T-359-37 | Tampering | a free-text fork heuristic added later | mitigate | M2 mutation leg; A13 tripwires; control legs in both runners |
| T-359-38 | Elevation of privilege | dev or vendor scripts reached from lib/ or hooks/ | mitigate | test-359-devonly-tripwire D1 to D5; api.typesafe.ai vendor leg |
| T-359-39 | Tampering | shared runner file | mitigate | four additive run_if lines in run-all-238; zero removed lines; explicit-path commit |
| T-359-40 | Repudiation | the R2 number claimed without proof | mitigate | baseline-359.json written by the replay from the pinned pre-359 sha; L7 re-derives it |
</threat_model>

<verification>
- `bash tests/run-all-359.sh` exits 0; `bash tests/run-all-238.sh` has no new red; `bash tests/run-all-357.sh` exits 0
- `node tests/test-359-replay.cjs --mutation` exits 0
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts tests/run-all-357.sh tests/fixtures/card-fire-replay/baseline.json` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
The pre-359 missed-fork count is recorded from the pinned commit, both runners guard the arm and the no-free-text rule, and the two SPEC R10 mutations are proven to fail the suite.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-08-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, `R2 missed_forks: <n>` (synthetic and dogfood split), the M1 and M2 results, the run-all-359 / 238 / 357 summaries, and the known pre-existing reds (R-J) unchanged.
</output>
