---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 06
type: execute
wave: 3
depends_on: ["359-02", "359-03", "359-04"]
files_modified:
  - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-JEV-LABEL-REPORT.md
  - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md
  - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-DOGFOOD-FORK-LABELS.md
  - tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json
  - tests/fixtures/card-fire-replay/prose-forks-359.json
autonomous: false
requirements: [FORK359-01, FORK359-09]
tags: [jev, dev-time, labeling, navigator-checkpoint, part8]

must_haves:
  truths:
    - "The synthetic-359 entries, and only those, are labeled by 357's labeler through the existing card_fire_replay profile and its is_fork Noul; a keyless run degrades to unlabeled with exit 0 and is recorded, never faked (SPEC R1, D-20)"
    - "is_fork is compared with the hand prose_fork label per entry using the 357 D-12 thresholds (>= 0.80 yes, <= 0.20 no, else uncertain); disagreements and uncertain ids go to 359-JEV-LABEL-REPORT.md for a navigator ruling and are never auto-applied (D-20)"
    - "The synthetic moonshots are scored for relevance and radicalness at dev time and the distribution is shown to the navigator; scores never auto-change a fixture (N-3)"
    - "One navigator checkpoint rules on: the dogfood prose_fork proposals (flipped local -> human only for ratified ids), every Jev disagreement, optional moonshot rewrites, and two interpretations: I-1 the yes/no exemption reads practical labels only, I-2 the R9 vacuity floor applies in both probe and text modes (D-21, N-3, N-4)"
    - "Dogfood text never goes to Jev and the 357 labeler refusal test stays green (Part 8, D-21)"
    - "If the navigator overrules I-1 or I-2, no code is improvised here: the ruling is recorded and routed to `/gsd-plan-phase 359 --gaps` before plan 07 relies on it"
  artifacts:
    - path: ".planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-JEV-LABEL-REPORT.md"
      provides: "is_fork labels for synthetic-359 with the 359 agreement section and rulings"
      contains: "is_fork vs prose_fork"
    - path: ".planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md"
      provides: "fixture moonshot Score distribution"
      contains: "never auto-applied"
    - path: "tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json"
      provides: "navigator-ratified dogfood prose_fork labels"
      contains: "\"ratified\": true"
  key_links:
    - from: "scripts/label-card-fire-replay.cjs"
      to: ".planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-JEV-LABEL-REPORT.md"
      via: "--source synthetic-359 --report"
      pattern: "359-JEV-LABEL-REPORT"
---

<objective>
Run the dev-time labelers on synthetic text only, put every open labeling question in front of the navigator once, and apply the rulings: dogfood prose_fork labels become human-ratified, Jev disagreements are ruled, and the two plan-level interpretations of N-3 and N-4 are confirmed before the metrics are frozen.

Purpose: SPEC R1 (labeled through 357's labeler, dogfood ratified at one navigator review), D-20, D-21, N-3, N-4.
Output: `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified overlay, any ruled fixture edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-02-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-03-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-04-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-DOGFOOD-FORK-LABELS.md

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`.

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`; otherwise STOP.

Part 8 and the 2026-09-17 rulings: Jev runs only here, at dev time, on the synthetic-359 fixture (through card_fire_replay) and on the synthetic moonshots (through fork359_moonshot). The dogfood overlay and dogfood.json are never passed to either script. The key is read by the scripts from ~/.secrets/typesafe.env; never print it.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md, and no file under lib/, hooks/ or scripts/ (this plan changes data and reports only). No em-dashes or en-dashes.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 (this plan) |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto">
  <name>Task 1: Dev-time label run on synthetic-359, fixture moonshot scoring, and the checkpoint packet</name>
  <files>.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-JEV-LABEL-REPORT.md, .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md</files>
  <read_first>
    - scripts/label-card-fire-replay.cjs (the `--source` flag added in 359-02, report row format, mapNoul thresholds from the policy file)
    - scripts/score-moonshots-359.cjs (`--fixtures`, `--report`)
    - tests/fixtures/card-fire-replay/prose-forks-359.json (ids and hand prose_fork labels)
    - PD/359-DOGFOOD-FORK-LABELS.md and tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json
    - PD/359-03-SUMMARY.md `## Interpretation to confirm`
  </read_first>
  <action>
    1. `node scripts/label-card-fire-replay.cjs --source synthetic-359 --report PD/359-JEV-LABEL-REPORT.md`; record the exit code, the status counts and whether it ran keyless.
    2. Append to that report a `## is_fork vs prose_fork (359)` section computed from its rows (by id): per synthetic entry the is_fork mapping (yes, no, uncertain, error, unlabeled) next to the hand prose_fork; `disagreement` when prose_fork true and is_fork no, or prose_fork false and is_fork yes; list disagreement ids and uncertain ids separately; state that nothing is auto-applied (D-20). No entry text.
    3. `node scripts/score-moonshots-359.cjs --fixtures --report PD/359-MOONSHOT-SCORES.md --section "Synthetic fixtures (plan 06)"`; record the distribution and the 3 lowest-scoring ids per question.
    4. Build the checkpoint packet in the SUMMARY draft: counts; Jev disagreement and uncertain ids with each hand label; the dogfood proposal table path and counts; the moonshot distribution and lowest ids; and the two interpretations: I-1 "the declared arm's yes/no exemption applies isYesNoShapedGate to the practical labels only, excluding the final What-if moonshot, because every N-3 declaration carries a moonshot and isYesNoShapedGate needs exactly 2 labels; without this the SPEC R4 exemption could never fire"; I-2 "the R9 vacuity floor (at least 6 pre-arm misses, or 20% of pre fork runs) applies in both probe mode and the N-4 text fallback; below it the result is INCONCLUSIVE".
    5. Confirm `node tests/test-357-labeler-refusal.cjs` still exits 0. Commit: `git add -f PD/359-JEV-LABEL-REPORT.md PD/359-MOONSHOT-SCORES.md` then `git commit --only -m "docs(359-06): dev-time is_fork labels and moonshot scores for synthetic-359 (D-20, N-3)" -- PD/359-JEV-LABEL-REPORT.md PD/359-MOONSHOT-SCORES.md`.
  </action>
  <verify>
    <automated>grep -c "is_fork vs prose_fork (359)" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-JEV-LABEL-REPORT.md && grep -c "never auto-applied" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md && node tests/test-357-labeler-refusal.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both reports exist and the verify command exits 0
    - `grep -c "dogfood-" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-JEV-LABEL-REPORT.md` is 0 (no dogfood row was built or sent)
    - The report states keyless or labeled explicitly, and the counts per status sum to the number of synthetic-359 entries
    - `grep -c "$(printf '\xe2\x80\x94')" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-JEV-LABEL-REPORT.md .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md` reports 0 for each
  </acceptance_criteria>
  <done>Every synthetic entry has a dev-time is_fork reading compared with its hand label, the fixture moonshots have scores, and one packet holds every open question for the navigator.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Navigator ruling on fork labels, Jev disagreements, moonshots and the two interpretations</name>
  <decision>Ratify the prose_fork labels and the two plan-level interpretations before the replay metrics and baseline are frozen.</decision>
  <context>These labels feed the pre-359 missed-fork baseline (R2), the declared-variant catch rate (R6) and the dogfood side of the metric. Jev labels are never auto-applied (D-20); dogfood labels move from local to human only here (D-21). I-1 decides whether the SPEC R4 yes/no exemption can ever fire under N-3; I-2 decides whether a low pre-arm miss count counts as a pass.</context>
  <options>
    <option id="approve-all">
      <name>Approve as proposed</name>
      <pros>All dogfood proposals ratified, hand labels kept on every Jev disagreement, fixture moonshots kept, I-1 and I-2 confirmed; plans 07 and 08 proceed unchanged.</pros>
      <cons>None beyond accepting the proposals as written.</cons>
    </option>
    <option id="approve-with-changes">
      <name>Approve with listed changes</name>
      <pros>The navigator lists ids to flip (dogfood or synthetic), moonshots to rewrite, and keeps or overrules I-1 and I-2.</pros>
      <cons>An I-1 or I-2 overrule routes to `/gsd-plan-phase 359 --gaps` before plan 07.</cons>
    </option>
  </options>
  <resume-signal>Type "approve-all", or "approve-with-changes" followed by the list (ids to flip, moonshot rewrites, I-1 keep or overrule, I-2 keep or overrule)</resume-signal>
  <files>.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-JEV-LABEL-REPORT.md (the `is_fork vs prose_fork (359)` section)
    - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md and .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-DOGFOOD-FORK-LABELS.md
  </read_first>
  <action>Present the Task 1 packet to the navigator in plain language (counts, disagreement ids with hand labels, the dogfood proposal table, the moonshot distribution with the lowest ids, and I-1 and I-2 each as one sentence with its consequence), then wait. Record the navigator's reply verbatim under `## Navigator ruling` in the SUMMARY draft. No file other than the SUMMARY draft changes in this task, and no Jev call is made.</action>
  <verify>
    <automated>grep -c "## Navigator ruling" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-06-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY draft carries `## Navigator ruling` with the verbatim reply naming approve-all or approve-with-changes
    - `git diff --name-only $PLAN_BASE -- tests lib hooks scripts` shows no change made during this task
  </acceptance_criteria>
  <done>The navigator's single ruling on labels, moonshots and both interpretations is recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the rulings (ratify the overlay, ruled fixture edits, recorded interpretations)</name>
  <files>tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json, tests/fixtures/card-fire-replay/prose-forks-359.json, .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-DOGFOOD-FORK-LABELS.md</files>
  <read_first>
    - The navigator's resume message (Task 2)
    - tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json and tests/fixtures/card-fire-replay/prose-forks-359.json
    - PD/359-DOGFOOD-FORK-LABELS.md
  </read_first>
  <action>
    1. Overlay: for every ratified dogfood id set `fork_label_origin: 'human'` and apply any listed flip (a flip to prose_fork true needs N-3 fork_labels, authored locally); set `meta.ratified: true` and `meta.ratified_at: <ISO date>`. Update the review sheet: a `## Ruling (<date>)` section listing the flips by id.
    2. Synthetic fixture: apply only the listed flips or moonshot rewrites; keep `label_origin: 'hand'`; append ` (ruled <date>)` to the `why` of each changed entry; keep every entry grammar-valid (re-run the plan-02 Task 1 grammar self-check).
    3. I-1 / I-2: when both are kept, record `I-1: confirmed` and `I-2: confirmed` in the SUMMARY. When either is overruled, record the ruling verbatim, do NOT edit code, and add `## Gap: route to /gsd-plan-phase 359 --gaps before 359-07` to the SUMMARY; the phase pauses there.
    4. Run `node tests/test-359-corpus.cjs`, `node tests/test-357-corpus-loader.cjs`, `node tests/test-357-labeler-refusal.cjs` and `node tests/test-359-inertness.cjs`; all exit 0. Commit: `git add tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json tests/fixtures/card-fire-replay/prose-forks-359.json` and `git add -f PD/359-DOGFOOD-FORK-LABELS.md`, then `git commit --only -m "test(359-06): navigator-ratified fork labels applied (D-20, D-21)" -- <the changed paths>`.
  </action>
  <verify>
    <automated>node -e "const o=require('./tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json');if(o.meta.ratified!==true)process.exit(1);const bad=Object.values(o.labels).filter(l=>l.fork_label_origin!=='human');console.log('non-human',bad.length);process.exit(bad.length===0?0:2)" && node tests/test-359-corpus.cjs && node tests/test-357-labeler-refusal.cjs && node tests/test-359-inertness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `non-human 0` and every test exits 0 (or the SUMMARY records the navigator's reason for any id left local)
    - The SUMMARY records `I-1: confirmed|overruled` and `I-2: confirmed|overruled`, and on an overrule contains the `## Gap` section
    - `git diff $PLAN_BASE --stat -- lib hooks scripts` is empty
    - `grep -c "$(printf '\xe2\x80\x94')" tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json tests/fixtures/card-fire-replay/prose-forks-359.json` reports 0 for each
  </acceptance_criteria>
  <done>The fork labels the metrics depend on are navigator-ratified, and the two interpretations are confirmed or routed to gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> TypeSafe (Jev) | label and score requests; synthetic text only |
| model-proposed labels -> the phase's measured truth | a label applied without a ruling would bias the baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-27 | Information disclosure | dogfood text to Jev | mitigate | only `--source synthetic-359` is labeled; the scorer reads only the synthetic fixture; acceptance asserts no dogfood row in the report; test-357-labeler-refusal stays green |
| T-359-28 | Tampering | Jev labels silently changing fixtures | mitigate | disagreements listed by id and ruled at the checkpoint; nothing auto-applied; changed entries carry a ruled note |
| T-359-29 | Repudiation | unratified dogfood labels used as ground truth | mitigate | overlay `ratified: true` and every label `human` only after the checkpoint; the verify gate fails otherwise |
| T-359-30 | Tampering | code changed to match an overruled interpretation mid-phase | mitigate | overrules route to gap closure; this plan edits no lib/, hooks/ or scripts/ file |
</threat_model>

<verification>
- `node tests/test-359-corpus.cjs && node tests/test-357-labeler-refusal.cjs && node tests/test-359-inertness.cjs` all exit 0
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
Synthetic entries carry dev-time is_fork readings, the fixture moonshots carry scores, the navigator has ruled once on every open label and interpretation, and the ratified labels are applied with no code change.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-06-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, labeler status counts (or keyless), agreement and disagreement counts, the navigator's ruling verbatim, ids flipped, moonshot distribution, `I-1:` and `I-2:` lines, and the `## Gap` section when an interpretation was overruled.
</output>
