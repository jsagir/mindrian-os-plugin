---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 09
type: execute
wave: 5
depends_on: ["359-08"]
files_modified:
  - tests/test-359-prose-budget.cjs
  - agents/larry-extended.md
  - skills/larry-personality/SKILL.md
  - scripts/session-start
  - lib/mcp/runtime-instructions.cjs
  - data/harness-manifest.json
autonomous: true
requirements: [FORK359-08]
tags: [larry, prose, byte-budget, tri-polar, revertible]

must_haves:
  truths:
    - "One rule, same meaning on four surfaces: at a navigator fork, fire the card; if the turn still ends in prose, the last line hands the call over as `Your call: A | B | What if C`, with 2 or 3 practical options and a final relevant, radical What-if moonshot (D-13, N-1, N-3)"
    - "The rule sits inside the measured span on each surface: the larry-extended `## Decision Gates` section, the SKILL item-5 line, NAV_CARD_FIRE_DOCTRINE and item 3 (DECISION GATES) of the MCP server instructions (SPEC R8, D-14)"
    - "The net byte delta summed across the four surfaces, measured against the post-357 files with the 357-10 span commands plus Buffer.byteLength, is at most 400 B; trims count against additions (N-6, D-14)"
    - "The byte pins hold: NAV_CARD_FIRE_DOCTRINE raw source under 900 B with its pinned tokens, the served MCP instructions at most 2000 B with BOUNDARIES frozen and last, and the SKILL item 5 still one line with its prefix (RESEARCH Finding 7)"
    - "357's pinned phrases survive (`## Decision Gates`, `no card, no picture (SEED-021)`, `AskUserQuestion`), `## Post-Gate Handoff` and SKILL :244 are byte-untouched, and the voice-mark, handoff, gate-native-fire and skeleton-split tests stay green (D-15)"
    - "A drift test ties every surface to the parser: each contains the example built from the exported DECLARATION_PREFIX, DECLARATION_SEPARATOR and MOONSHOT_PREFIX, and that example parses as declared (D-05)"
    - "The four surfaces, the regenerated harness manifest and the prose-budget test land as ONE commit after R3 to R7 are green, so the R9 falsification branch can revert exactly that commit (D-16)"
  artifacts:
    - path: "tests/test-359-prose-budget.cjs"
      provides: "net byte cap, byte pins, pinned phrases, untouched spans, D-05 drift"
      min_lines: 110
    - path: "agents/larry-extended.md"
      provides: "Decision Gates rule with the Your call line"
      contains: "Your call: A | B | What if C"
    - path: "lib/mcp/runtime-instructions.cjs"
      provides: "hookless-surface rule in item 3 within the 2000 B budget"
      contains: "Your call: A | B | What if C"
    - path: "data/harness-manifest.json"
      provides: "regenerated digests for the Larry surfaces and the MCP instructions"
  key_links:
    - from: "tests/test-359-prose-budget.cjs"
      to: "lib/core/fork-declaration.cjs"
      via: "the drift example is built from the exported constants and parsed"
      pattern: "DECLARATION_PREFIX"
    - from: "scripts/session-start"
      to: "tests/test-251-skeleton-split.cjs"
      via: "the doctrine stays under the 900 B raw pin"
      pattern: "NAV_CARD_FIRE_DOCTRINE"
---

<objective>
Tell Larry to declare: add the one-line fork declaration rule, with N-3's What-if moonshot, to the two Larry card spans, the session-start card-fire doctrine and the hookless MCP instructions, inside a 400 B net budget and every existing byte pin, as one revertible commit.

Purpose: SPEC R8; D-05, D-13 to D-16; N-1, N-3, N-6; RESEARCH Finding 7 and Pitfalls 2, 10, 12. This is the only change the R9 forward run attributes its delta to.
Output: four prose surfaces, a regenerated `data/harness-manifest.json`, `tests/test-359-prose-budget.cjs`, all in one commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-SPEC.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-RESEARCH.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-01-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-08-SUMMARY.md
@lib/core/fork-declaration.cjs
@data/harness-policies/contract-parity-larry.json

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`.

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`, and `bash tests/run-all-359.sh` exits 0 at PLAN_BASE (R3 to R7 green, D-16).

Measurement commands (identical before and after; 359-01 contracts item f names the working span command if 357-10 changed a boundary):
- LARRY: `awk '/^## Decision Gates/{f=1} /^## The Cardinal Sin/{f=0} f' agents/larry-extended.md | wc -c`
- SKILL: `grep '^5\. Never hand-draw the dial glyphs' skills/larry-personality/SKILL.md | wc -c`
- DOCTRINE: the raw capture `NAV_CARD_FIRE_DOCTRINE="([^\n]*)"` from scripts/session-start, measured with Buffer.byteLength (tests/test-251-skeleton-split.cjs Test 2 method; 874 B at research time)
- MCP: `Buffer.byteLength(require('./lib/mcp/runtime-instructions.cjs').RUNTIME_INSTRUCTIONS)` (1984 B at research time; budget 2000, host cap 2048)

Byte feasibility measured at planning time: removing the historic doctrine sentence ` That marker is the per-turn gate flag; only the imperative below moved here.` frees 77 B; a doctrine rule of about 85 B raw (including its `\n\n`) lands near 882 B; a 119 B rule would land at 916 B and FAIL the 900 B pin. The MCP item-3 rule needs compensating trims inside items 1, 2, 4, 5 and 6 only (for example item 2 `after answering, ` 17 B, item 6 ` or at checkpoints` 18 B); the persona paragraph, THEO and BOUNDARIES paragraphs are never trimmed. D-14's per-surface numbers are a guide; the binding caps are the 400 B net total, the 900 B doctrine pin and the 2000 B MCP budget.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- agents/larry-extended.md skills/larry-personality/SKILL.md scripts/session-start lib/mcp/runtime-instructions.cjs data/harness-manifest.json` before editing and STOP on any diff you did not make; explicit-path commits only (`git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md, the `## Post-Gate Handoff` section of agents/larry-extended.md, SKILL.md line ~244 (Voice Signature residual), `~/.claude/agents/larry-extended.md`, lib/mcp/no-instructions.test.cjs, tests/test-251-skeleton-split.cjs, tests/test-298-contract-parity.cjs. No em-dashes or en-dashes; no De Stijl glyph in any added text (Canon Part 12).
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 (this plan) |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure the post-357 surfaces and write the prose-budget and drift test (RED, not committed yet)</name>
  <files>tests/test-359-prose-budget.cjs</files>
  <read_first>
    - agents/larry-extended.md (`grep -n '^## ' agents/larry-extended.md`; the whole `## Decision Gates` section as 357-10 left it; `## Post-Gate Handoff` read only; where "End with a question or next step" sits)
    - skills/larry-personality/SKILL.md (item 5 line; the Voice Signature paragraph near :244, read only)
    - scripts/session-start (NAV_CARD_FIRE_DOCTRINE near :1794) and tests/test-251-skeleton-split.cjs (Test 2: raw capture, pinned tokens `[FIRE-IF-FORK`, `AskUserQuestion`, `SEED-021`, `NAV DECISION unchanged`, `verb_count`)
    - lib/mcp/runtime-instructions.cjs and lib/mcp/no-instructions.test.cjs (budget, host cap, BOUNDARIES frozen and last)
    - tests/test-gate-native-fire-w1.cjs (lines ~105-125: the pinned phrases and the `End with a question or next step[\s\S]{0,120}AskUserQuestion card` window), tests/test-larry-voice-mark-182.cjs (lines ~280-290), tests/test-larry-handoff-seam.cjs
    - lib/core/fork-declaration.cjs (DECLARATION_PREFIX, DECLARATION_SEPARATOR, MOONSHOT_PREFIX, parseForkDeclaration)
  </read_first>
  <behavior>
    - B1 net cap: after-bytes minus the frozen before-bytes, summed over LARRY, SKILL, DOCTRINE and MCP, is at most 400 (N-6); each surface's delta is printed
    - B2 doctrine raw bytes under 900 and the five pinned tokens present
    - B3 MCP served bytes at most 2000 (and at most 2048)
    - B4 drift (D-05): the example `DECLARATION_PREFIX + 'A' + DECLARATION_SEPARATOR + 'B' + DECLARATION_SEPARATOR + MOONSHOT_PREFIX + ' C'` appears in each of the four measured spans and `parseForkDeclaration` accepts it; the word `radical` appears in the LARRY, SKILL and DOCTRINE spans (and in MCP unless the SUMMARY records the budget exception)
    - B5 pins: `## Decision Gates` appears once, `no card, no picture (SEED-021)` and `AskUserQuestion` at least once in larry-extended; the gate-native-fire 120-char window regex still matches; SKILL item 5 is one line starting `5. Never hand-draw the dial glyphs`; the Post-Gate Handoff span sha256 and the SKILL counts of `DECLARED CONVENTION` and `not a per-token runtime guarantee` equal frozen PLAN_BASE values
    - B6 no MARK_GLYPHS character, U+2014 or U+2013 inside the added example or in any of the four files
  </behavior>
  <action>
    1. At PLAN_BASE, run the four measurement commands and record LARRY_BEFORE, SKILL_BEFORE, DOCTRINE_BEFORE, MCP_BEFORE in the SUMMARY; also record the Post-Gate Handoff span sha256 (`awk '/^## Post-Gate Handoff/{f=1} /^## Decision Gates/{f=0} f' agents/larry-extended.md | sha256sum`, or the 359-01 contracts boundary) and the two SKILL counts; run `node scripts/build-harness-manifest.cjs --check` and record its output (drift from other phases is recorded, not fixed).
    2. Create `tests/test-359-prose-budget.cjs` ('use strict', node:assert/strict, ok counter, header naming FORK359-08, SPEC R8, D-05, D-13 to D-16, N-3, N-6, and stating that this file ships in the same commit as the prose so a revert of that commit removes it). Freeze the step-1 values as constants and implement B1 to B6 with the same measurement methods (read files with fs; for LARRY and SKILL reproduce the awk and grep spans in JavaScript and assert they equal the shell measurements once in a comment). Run it: B1 to B3 and B5 pass, B4 fails (RED). Do not commit this file yet (Task 2's single commit carries it, D-16).
  </action>
  <verify>
    <automated>node tests/test-359-prose-budget.cjs; test $? -ne 0 && echo "RED as expected before the prose lands"</automated>
  </verify>
  <acceptance_criteria>
    - The verify prints `RED as expected before the prose lands`, and the test output shows only B4 failing
    - The SUMMARY draft records the four before-bytes, the Post-Gate Handoff sha256, the two SKILL counts and the manifest --check output
    - `git status --short -- tests/test-359-prose-budget.cjs` shows the file untracked (not committed in this task)
  </acceptance_criteria>
  <done>The post-357 bytes are frozen in a test that fails only because the declaration rule is not yet on the four surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Land the declaration rule on the four surfaces, regenerate the manifest, and commit it as one revertible unit</name>
  <files>agents/larry-extended.md, skills/larry-personality/SKILL.md, scripts/session-start, lib/mcp/runtime-instructions.cjs, data/harness-manifest.json, tests/test-359-prose-budget.cjs</files>
  <read_first>
    - The same files as Task 1, plus scripts/build-harness-manifest.cjs (regenerate and `--check` usage)
    - PD/359-RESEARCH.md Pitfalls 2, 10, 12 and Finding 7
  </read_first>
  <action>
    1. agents/larry-extended.md, inside `## Decision Gates` only: add one or two sentences, in Larry's voice, framing the line as the closing form of the question (Pitfall 10), for example: `If a fork still ends in prose, hand the call over on the last line: Your call: A | B | What if C, with 2 or 3 practical options and one relevant, radical What-if moonshot (lateral; trending to the absurd is fair).` Keep `## Decision Gates`, `no card, no picture (SEED-021)` and `AskUserQuestion`; do not place the text between "End with a question or next step" and "AskUserQuestion card" (the 120-char window); touch no other section.
    2. skills/larry-personality/SKILL.md item 5: append one clause to the same single line, for example ` A fork left in prose ends on one line: Your call: A | B | What if C, the last a relevant, radical moonshot.` The line keeps its `5. Never hand-draw the dial glyphs` prefix; nothing else in the file changes.
    3. scripts/session-start NAV_CARD_FIRE_DOCTRINE: delete ` That marker is the per-turn gate flag; only the imperative below moved here.` and add, as its own paragraph after the [FIRE-IF-FORK] paragraph, a rule of at most about 85 raw bytes including its `\n\n`, for example `\n\nProse fork? Last line: Your call: A | B | What if C (radical moonshot last).` Keep the string on one source line and keep all five pinned tokens; raw bytes must stay under 900.
    4. lib/mcp/runtime-instructions.cjs: append to item 3 (DECISION GATES) a short rule containing the example, for example ` Prose fork? End on: Your call: A | B | What if C (radical).`; trim wording in items 1, 2, 4, 5 and 6 only (never the persona paragraph, THEO or BOUNDARIES; never change a tool name or the item-5 "Skip chat-only turns" meaning) until the served string is at most 2000 B. If `radical` cannot fit, drop only that word on this surface and record the exception. Update the header comment's measured-bytes sentence to the new value (comments are not served).
    5. Measure: all four after-bytes; the net sum must be at most 400 B (N-6), else shorten wording and re-measure. Regenerate `node scripts/build-harness-manifest.cjs`, then `node scripts/build-harness-manifest.cjs --check`.
    6. Run: `node tests/test-359-prose-budget.cjs` (GREEN), `node tests/test-251-skeleton-split.cjs`, `node lib/mcp/no-instructions.test.cjs`, `node tests/test-gate-native-fire-w1.cjs`, `node tests/test-larry-voice-mark-182.cjs`, `node tests/test-205-voice-mark-hybrid.cjs`, `node tests/test-larry-handoff-seam.cjs`, `bash tests/run-all-251.sh`, `node tests/test-356-larry-contract.cjs` (when present), `node scripts/check-first-touch-drift.cjs` (when present), and `node tests/test-298-contract-parity.cjs` only when `git status --short` shows no peer diff on Larry surfaces or harness policies (else record it as not run with the reason, Pitfall 12). Then `bash tests/run-all-359.sh` and `node tests/test-359-inertness.cjs`.
    7. ONE commit (D-16): `git add tests/test-359-prose-budget.cjs` then `git commit --only -m "feat(359-09): Larry declares a prose fork on its last line, Your call with a What-if moonshot (R8, D-13, D-16, N-3; net <N> B)" -- agents/larry-extended.md skills/larry-personality/SKILL.md scripts/session-start lib/mcp/runtime-instructions.cjs data/harness-manifest.json tests/test-359-prose-budget.cjs`. Record `R8_SHA=$(git rev-parse HEAD)` as the FIRST line of the SUMMARY in the form `R8_SHA: <sha>` (plans 10 and 11 read it).
  </action>
  <verify>
    <automated>node tests/test-359-prose-budget.cjs && node tests/test-251-skeleton-split.cjs && node lib/mcp/no-instructions.test.cjs && node tests/test-gate-native-fire-w1.cjs && node tests/test-larry-voice-mark-182.cjs && node tests/test-205-voice-mark-hybrid.cjs && node tests/test-larry-handoff-seam.cjs && node scripts/build-harness-manifest.cjs --check && bash tests/run-all-359.sh</automated>
  </verify>
  <acceptance_criteria>
    - Every verify command exits 0
    - `git show --stat --format= <R8_SHA>` lists exactly the six paths of step 7 (one revertible commit)
    - `git show <R8_SHA> | git apply -R --check` exits 0 (the reverse patch applies cleanly; a check only, nothing in the tree or index changes)
    - `grep -c "Your call: A | B | What if C" agents/larry-extended.md skills/larry-personality/SKILL.md scripts/session-start lib/mcp/runtime-instructions.cjs` reports at least 1 for each file
    - `grep -c "$(printf '\xe2\x80\x94')" agents/larry-extended.md skills/larry-personality/SKILL.md scripts/session-start lib/mcp/runtime-instructions.cjs tests/test-359-prose-budget.cjs` reports 0 for each
    - The SUMMARY's first line is `R8_SHA: <40-hex sha>` and it records before, after and net bytes per surface
  </acceptance_criteria>
  <done>Larry is told, on every surface he reads, to end a prose fork with a Your call line whose last option is a radical What-if moonshot, inside every byte budget, in one commit that can be reverted alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Larry prose -> model behavior on CLI, Desktop and Cowork | a wording change moves every future turn |
| prose edits -> pinned tests and byte budgets owned by other phases | a silent overflow breaks another suite or truncates the MCP instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-41 | Tampering | prose edits breaking 357 pinned phrases or other phases' pins | mitigate | B2, B3, B5 legs duplicate the pins; test-251, no-instructions, gate-native-fire, voice-mark, handoff and run-all-251 run in verify; Post-Gate Handoff and SKILL :244 hashes frozen |
| T-359-42 | Denial of service | MCP instructions silently truncated by the 2048 B host cap | mitigate | served string held at or under 2000 B; no-instructions test in verify; BOUNDARIES never trimmed |
| T-359-43 | Tampering | the declaration drifting from the parser grammar | mitigate | B4 builds the example from exported constants and parses it (D-05) |
| T-359-44 | Repudiation | R8 not cleanly revertible for the R9 falsification branch | mitigate | one commit with the six paths; a revert dry run in acceptance |
| T-359-45 | Tampering | Part 12 voice contract broken by the new line | mitigate | no glyph in added text (B6); voice-mark tests in verify; the parser rejects glyph lines |
</threat_model>

<verification>
- `bash tests/run-all-359.sh` exits 0 with the prose-budget leg PASSED
- `node scripts/build-harness-manifest.cjs --check` exits 0
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD -- scripts/check-card-fire.cjs lib/core lib/mcp/stop-gate-handler.cjs` prints nothing (prose only)
</verification>

<success_criteria>
The declaration rule, with the What-if moonshot, is on all four Larry surfaces within 400 B net and every byte pin, all pinned phrases and tests stay green, and the change is one commit the falsification branch can revert.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-09-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). FIRST line `R8_SHA: <sha>`. Then: PLAN_BASE, a per-surface table (before, after, delta), the net total, the exact added and removed text per surface, any MCP `radical` exception, test results, and whether test-298 ran.
</output>
