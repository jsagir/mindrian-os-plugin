---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 11
type: execute
wave: 7
depends_on: ["359-04", "359-10"]
files_modified:
  - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md
  - agents/larry-extended.md
  - skills/larry-personality/SKILL.md
  - scripts/session-start
  - lib/mcp/runtime-instructions.cjs
  - data/harness-manifest.json
  - tests/test-359-prose-budget.cjs
  - .planning/ROADMAP.md
autonomous: false
requirements: [FORK359-09, FORK359-08]
tags: [forward-run, paid, navigator-gated, falsification, moonshot]

must_haves:
  truths:
    - "The full batch runs only after a blocking navigator approval that restates the mode, the scenario counts, runs per scenario (3), the measured projection (at most USD 60), the per-run cap (USD 0.40), the model (claude-sonnet-5) and the three outcomes with their consequences (N-2, N-5, D-12)"
    - "Pre = R8_SHA^ and post = R8_SHA, archived snapshots, identical harness, model and scenarios, so the delta is attributable to the R8 prose alone (D-12, D-16)"
    - "A forward missed fork is a fork-scenario run (by its authored label) with neither a non-mode-menu card nor a declaration in attempt 1 (text mode: without a declaration); budget-hit, timeout and error runs are re-run once and otherwise excluded, never counted as misses (SPEC R9, N-4)"
    - "PASS requires pre missed at or above the vacuity floor max(6, ceil(0.2 x pre fork runs)), post missed at most 50% of pre missed, and 0 declared-fork-no-card control blocks across all post runs; below the floor the result is INCONCLUSIVE, never a pass; otherwise FALSIFIED (SPEC R9, N-4, I-2)"
    - "On FALSIFIED the R8 commit, and only it, is reverted by explicit path, R3 to R7 stay shipped (inert without a declaration), run-all-359 stays green, and a follow-on phase is opened; on INCONCLUSIVE R8 stays and a navigator ruling is requested; on PASS R8 stays (SPEC R9, D-16)"
    - "Moonshots declared in post-arm forward runs are Jev-scored at dev time from authored scenario context plus the declared labels only, and the distribution is recorded (N-3)"
    - "Only counts and scenario ids reach the repo; raw streams stay in ~/.cache/mindrian-dev/359-forward/ (SPEC R9)"
  artifacts:
    - path: ".planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md"
      provides: "forward-run moonshot Score section"
      contains: "Forward run"
  key_links:
    - from: "scripts/forward-fork-scenarios-359.cjs"
      to: "~/.cache/mindrian-dev/359-forward/results.jsonl"
      via: "resumable batch rows, then --evaluate"
      pattern: "evaluate"
    - from: "scripts/score-moonshots-359.cjs"
      to: "tests/fixtures/forward-fork-scenarios-359.json"
      via: "--from-results rows tied to authored scenario ids"
      pattern: "from-results"
---

<objective>
Run the forward measurement that decides whether the declaration actually works: the approved pre/post batch, the floor-guarded verdict, the falsification branch when it fails, and the dev-time moonshot scores of what Larry actually declared.

Purpose: SPEC R9 (the only test of adoption on fresh output), D-12, D-16, N-2 to N-5, I-2.
Output: the R9 verdict and counts in the SUMMARY; a forward section in `359-MOONSHOT-SCORES.md`; on FALSIFIED, a revert commit of R8 and a new follow-on phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-SPEC.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-06-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-09-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-10-SUMMARY.md
@scripts/forward-fork-scenarios-359.cjs

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`. R8_SHA = first line of PD/359-09-SUMMARY.md. MODE = the `mode=` value on the first line of PD/359-10-SUMMARY.md. RESULTS = `~/.cache/mindrian-dev/359-forward/results.jsonl`.

Precondition: `head -1 PD/359-10-SUMMARY.md` starts with `SMOKE: PASSED`; otherwise STOP (DECLINED or BLOCKED leaves FORK359-09 open for close-out). `bash tests/run-all-359.sh` exits 0.

Spend rules (N-2, N-5, hard): model claude-sonnet-5 only, never Fable; USD 0.40 per run; USD 60 total including the smoke's spend; the harness refuses above the cap and stops the batch before the cumulative cap; no run before the Task 1 approval.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md. The six R8 paths and .planning/ROADMAP.md are touched ONLY on the FALSIFIED branch. No em-dashes or en-dashes.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 (this plan) |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Navigator approves the full forward batch</name>
  <decision>Approve the R9 batch: MODE from the smoke, all fork and control scenarios, 3 runs each, pre and post arms, at the measured projection (at most USD 60 in total including the smoke).</decision>
  <context>Show: MODE and the smoke's A1 to A9 table (PD/359-10-SUMMARY.md); the scenario counts; runs = scenarios x 3 x 2; the projected total and the remaining cap after the smoke spend; per-run cap USD 0.40; model claude-sonnet-5; pre = R8_SHA^, post = R8_SHA. Bars: the vacuity floor max(6, ceil(0.2 x pre fork runs)) (I-2 as confirmed at plan 06), post missed at most 50% of pre missed, 0 control declared blocks. Outcomes: PASS keeps R8; INCONCLUSIVE keeps R8 and asks the navigator how to proceed; FALSIFIED reverts only the R8 commit and opens a follow-on phase, with R3 to R7 still shipped.</context>
  <options>
    <option id="approve-batch">
      <name>Approve the batch at the projected cost</name>
      <pros>Measures adoption on fresh output; decides whether R8 stays.</pros>
      <cons>Spends up to the projected total (never above USD 60 in all).</cons>
    </option>
    <option id="approve-reduced">
      <name>Approve a reduced batch</name>
      <pros>The navigator names fewer runs or scenarios to fit a smaller budget.</pros>
      <cons>Below 10 fork scenarios x 3 runs the SPEC R9 set is not met and the floor is harder to reach; the result is recorded as reduced.</cons>
    </option>
    <option id="decline">
      <name>Decline</name>
      <pros>No spend.</pros>
      <cons>R9 unmeasured; FORK359-09 stays open with the reason; R8 stays unmeasured.</cons>
    </option>
  </options>
  <resume-signal>Type "approve-batch", "approve-reduced: <limits>" or "decline"</resume-signal>
  <files>.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-11-SUMMARY.md</files>
  <read_first>
    - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-10-SUMMARY.md (SMOKE line, MODE, A1 to A9 table, measured per-run cost, projection)
    - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-06-SUMMARY.md (`I-2:` line)
    - `node scripts/forward-fork-scenarios-359.cjs --r8-sha <R8_SHA> --arm both --mode <MODE> --dry-run` output (planned run count and projection; spends nothing)
  </read_first>
  <action>Run the dry run above, then present the approval packet to the navigator in plain language (mode, runs, projected and remaining budget, bars, the three outcomes and their consequences), then wait. Record the reply verbatim under `## Navigator ruling (batch)` in the SUMMARY draft. Nothing is spent in this task.</action>
  <verify>
    <automated>grep -c "## Navigator ruling (batch)" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-11-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY draft records `approve-batch`, `approve-reduced: <limits>` or `decline` verbatim
    - `wc -l < ~/.cache/mindrian-dev/359-forward/results.jsonl 2>/dev/null || echo 0` is unchanged from before this task (nothing spent)
  </acceptance_criteria>
  <done>The batch spend is approved, reduced or declined by the navigator on the record, before any batch run.</done>
</task>

<task type="auto">
  <name>Task 2: Run the batch, evaluate against the floor-guarded bar, and apply the outcome (PASS, INCONCLUSIVE or FALSIFIED)</name>
  <files>agents/larry-extended.md, skills/larry-personality/SKILL.md, scripts/session-start, lib/mcp/runtime-instructions.cjs, data/harness-manifest.json, tests/test-359-prose-budget.cjs, .planning/ROADMAP.md</files>
  <read_first>
    - The navigator's resume message (Task 1); on `decline`, record `R9: DECLINED` and go to Task 3 step 3
    - scripts/forward-fork-scenarios-359.cjs (batch, resume, evaluate output)
    - PD/359-09-SUMMARY.md (R8_SHA and the six R8 paths)
    - `git log --format='%h %s' <R8_SHA>..HEAD -- agents/larry-extended.md skills/larry-personality/SKILL.md scripts/session-start lib/mcp/runtime-instructions.cjs data/harness-manifest.json` (later commits on the R8 paths)
  </read_first>
  <action>
    1. Run `node scripts/forward-fork-scenarios-359.cjs --r8-sha <R8_SHA> --arm pre --mode <MODE> --results <RESULTS>` then the same with `--arm post` (sequential, resumable; apply any approved reduction with `--only` or `--runs`). Watch the cumulative cost; the harness stops before the cap.
    2. `node scripts/forward-fork-scenarios-359.cjs --evaluate --mode <MODE> --results <RESULTS> > ~/.cache/mindrian-dev/359-forward/r9-eval.json`. Record in the SUMMARY (counts and scenario ids only): per arm fork runs, non-menu cards, mode-menu cards, declarations, missed forks and their scenario ids; control runs; control declared blocks (post) with ids; error and excluded runs; the floor; hook_live_proof; total spend including the smoke; wall time; and the outcome.
    3. PASS: record `R9: PASS (pre <a>/<n>, post <b>/<m>, controls 0)`; R8 stays.
    4. INCONCLUSIVE: record `R9: INCONCLUSIVE (pre missed <a> below floor <f>)` and a `## Navigator ruling needed` section (extend within the remaining cap, or accept as directional); R8 stays; no revert.
    5. FALSIFIED (SPEC R9 falsification branch, D-16): record `R9: FALSIFIED (<which bar failed>)`. Check `git status --short` on the six R8 paths and STOP on any peer diff. When the log in read_first shows later commits on those paths, STOP and report (a manual revert would clobber another phase's work). Otherwise `git revert --no-commit <R8_SHA>`, confirm `git diff --cached --name-only` lists only the six R8 paths, run `node scripts/build-harness-manifest.cjs --check` (regenerate and re-check if it reports drift caused by the revert), `bash tests/run-all-359.sh` (the prose-budget leg SKIPs because its file is reverted) and `node tests/test-359-inertness.cjs`, then `git commit --only -m "revert(359-11): R8 declaration prose falsified by the forward run; R3-R7 stay inert (R9, D-16)" -- agents/larry-extended.md skills/larry-personality/SKILL.md scripts/session-start lib/mcp/runtime-instructions.cjs data/harness-manifest.json tests/test-359-prose-budget.cjs`. Then open the follow-on: `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" phase add "Fork declaration adoption failed in the forward run: next approach for missed prose forks (Phase 359 R9 falsification follow-on)"`, record the new phase number, and commit `.planning/ROADMAP.md` by explicit path only if the tool did not commit it.
  </action>
  <verify>
    <automated>node -e "const r=require(require('path').join(require('os').homedir(),'.cache/mindrian-dev/359-forward/r9-eval.json'));if(!['PASS','INCONCLUSIVE','FALSIFIED'].includes(r.outcome))process.exit(1);console.log(r.outcome,JSON.stringify(r.arms||r.counts))" ; bash tests/run-all-359.sh && node tests/test-359-inertness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records exactly one of `R9: PASS`, `R9: INCONCLUSIVE`, `R9: FALSIFIED` or `R9: DECLINED`, with the counts and scenario ids above
    - Total spend including the smoke is at most USD 60 and every row's cost_usd is at most 0.40 (`node -e` over RESULTS)
    - `bash tests/run-all-359.sh` and `node tests/test-359-inertness.cjs` exit 0 after the outcome is applied
    - On FALSIFIED: `git show --stat --format= HEAD` (the revert commit) lists only the six R8 paths, `grep -c "Your call:" agents/larry-extended.md` is 0, and the SUMMARY names the new follow-on phase number
    - On PASS or INCONCLUSIVE: `git diff --name-only $PLAN_BASE..HEAD -- agents skills scripts/session-start lib/mcp/runtime-instructions.cjs` prints nothing
    - `git ls-files | grep -c "359-forward"` is 0
  </acceptance_criteria>
  <done>The forward run has produced a floor-guarded verdict, and the outcome's consequence (keep R8, ask the navigator, or revert R8 plus a follow-on) is applied.</done>
</task>

<task type="auto">
  <name>Task 3: Dev-time moonshot scores for the forward declarations, and the final gate run</name>
  <files>.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md</files>
  <read_first>
    - scripts/score-moonshots-359.cjs (`--from-results`, `--append`, `--section`)
    - RESULTS (rows with declared true)
  </read_first>
  <action>
    1. When any row has `declared: true`: `node scripts/score-moonshots-359.cjs --from-results <RESULTS> --report PD/359-MOONSHOT-SCORES.md --append --section "Forward run (plan 11)"`. The scorer builds context from the authored scenario turns only and sends only the declared labels from model output (N-3, Part 8). Record the distribution per question and per arm, the share of declarations whose last label was a What-if moonshot, and keyless status if applicable. With no declared rows, append a section stating that no forward declaration exists to score.
    2. Commit: `git add -f PD/359-MOONSHOT-SCORES.md` then `git commit --only -m "docs(359-11): dev-time moonshot scores for forward-run declarations (N-3)" -- PD/359-MOONSHOT-SCORES.md`.
    3. Final gates: `bash tests/run-all-359.sh`, `bash tests/run-all-238.sh` (only the known 238-03 red), `bash tests/run-all-357.sh`, `bash tests/run-all-251.sh`, `node scripts/build-harness-manifest.cjs --check`; record the summaries.
  </action>
  <verify>
    <automated>grep -c "Forward run (plan 11)" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md && bash tests/run-all-359.sh && node scripts/build-harness-manifest.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - The verify exits 0
    - `grep -c "$(printf '\xe2\x80\x94')" .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-MOONSHOT-SCORES.md` is 0
    - The SUMMARY records the moonshot distribution (or keyless / none) and the five gate results
  </acceptance_criteria>
  <done>The moonshots Larry actually produced are scored at dev time, and every standing gate is green after the R9 outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> paid model API | the phase's main spend |
| forward run outcome -> shipped Larry prose | a failed bar must remove R8 without touching anything else |
| forward declarations -> Jev | model output going to a vendor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-50 | Denial of service (financial) | forward-run spend overrun | mitigate | blocking approval with the measured projection; per-run 0.40 cap; the harness stops before the USD 60 cumulative cap (smoke included); acceptance sums every row |
| T-359-51 | Repudiation | a vacuous or noisy pass | mitigate | vacuity floor; INCONCLUSIVE is never a pass; errors excluded, never counted as misses; counts and ids recorded |
| T-359-52 | Tampering | the revert clobbering another phase's edits | mitigate | peer-diff check and later-commit check on the R8 paths before reverting; cached diff limited to the six paths; explicit-path commit |
| T-359-53 | Information disclosure | forward output or user text to Jev | mitigate | the scorer sends authored scenario context plus declared labels only (at most 80 code points each); dogfood never read |
| T-359-54 | Information disclosure | raw streams in git | mitigate | results under ~/.cache; acceptance greps git ls-files |
</threat_model>

<verification>
- `bash tests/run-all-359.sh` exits 0 after the outcome
- `git ls-files | grep -c "359-forward"` is 0
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
The declaration approach is judged on fresh output against a floor-guarded 50% bar with zero control blocks, and the consequence is applied: R8 kept, a navigator ruling requested, or R8 reverted with a follow-on phase and R3 to R7 still shipped.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-11-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). FIRST line `R9: PASS|INCONCLUSIVE|FALSIFIED|DECLINED (<short reason>)`. Then the per-arm counts, scenario ids of misses and control blocks, the floor, hook_live_proof, total spend, the revert sha and follow-on phase number when FALSIFIED, and the moonshot distribution.
</output>
