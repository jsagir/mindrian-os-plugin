---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 02
type: execute
wave: 2
depends_on: ["359-01"]
files_modified:
  - tests/fixtures/card-fire-replay/prose-forks-359.json
  - tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json
  - .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-DOGFOOD-FORK-LABELS.md
  - scripts/card-fire-replay-corpus.cjs
  - scripts/label-card-fire-replay.cjs
  - tests/test-359-corpus.cjs
autonomous: true
requirements: [FORK359-01]
tags: [card-fire, replay-corpus, synthetic-fixtures, labeler, part8]

must_haves:
  truths:
    - "prose-forks-359.json holds at least 15 synthetic prose forks and at least 15 synthetic non-fork controls, every entry `source: 'synthetic-359'`, direct-field envelopes with `ran_entries: []`, `sidechannel_health: 'healthy'`, `reach_corroborated: false`, and a meta sanitization statement (SPEC R1, D-18, 357 R-I)"
    - "Every prose fork carries `prose_fork: true` and 3 or 4 `fork_labels` in the N-3 grammar (2 or 3 practical labels then one relevant, radical `What if` moonshot), which satisfies SPEC R1's 2-or-more floor; every control carries `prose_fork: false` (SPEC R1, N-3)"
    - "The composition follows D-19: the three sanitized intern-w1 two-way forks, 2-way and 3-way forks, question and recommendation-plus-alternative phrasings, at least 2 yes/no-shaped practical pairs, the yes/no prefix quirk and a Hebrew fork; controls cover the six backstop-benign shapes, answered rhetorical X-or-Y questions, closing yes/no offers and at least 5 near-miss declaration lines (D-19)"
    - "Raw prose forks are labeled `expected_verdict_class: 'block'` with a `known_miss` whose reason contains the word `text`, so 357's taxonomy reports KNOWN_MISS, never MISSED_FORK; controls are labeled `pass` (RESEARCH Finding 6)"
    - "synthetic-359 is an OPT-IN loader source: `loadCorpus({})` returns exactly the 357 entries it returned before (357 L7 bar untouched), and SOURCES / SOURCE_FILES are unchanged (RESEARCH Finding 6, Pitfall 3)"
    - "357 dogfood entries receive prose_fork labels through a local overlay file (`fork_label_origin: 'local'`) and one review sheet, flipped to human only at the plan-06 navigator checkpoint; dogfood text never goes to Jev (D-21)"
    - "The labeler accepts synthetic-359 through 357's existing card_fire_replay profile (no new egress profile for R1) and still refuses dogfood before building any request (SPEC R1, D-20)"
  artifacts:
    - path: "tests/fixtures/card-fire-replay/prose-forks-359.json"
      provides: "synthetic prose forks and non-fork controls"
      contains: "synthetic-359"
    - path: "tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json"
      provides: "local prose_fork overlay for every 357 dogfood id"
      contains: "fork_label_origin"
    - path: "scripts/card-fire-replay-corpus.cjs"
      provides: "OPT_IN_SOURCES, OPT_IN_SOURCE_FILES, FORK359_OVERLAY_FILE, prose_fork validation, optIn and fork359Overlay load options"
      contains: "OPT_IN_SOURCES"
    - path: "tests/test-359-corpus.cjs"
      provides: "FORK359-01 loader and fixture legs"
      min_lines: 140
  key_links:
    - from: "scripts/card-fire-replay-corpus.cjs"
      to: "tests/fixtures/card-fire-replay/prose-forks-359.json"
      via: "OPT_IN_SOURCE_FILES['synthetic-359'] loaded only when optIn lists it"
      pattern: "prose-forks-359"
    - from: "scripts/label-card-fire-replay.cjs"
      to: "scripts/jev-devtime-client.cjs"
      via: "LABELABLE_SOURCES gains synthetic-359; EGRESS_PROFILES.card_fire_replay reused"
      pattern: "synthetic-359"
---

<objective>
Give the replay corpus its missed-fork label dimension: a synthetic fixture file of prose forks and non-fork controls, a local prose_fork overlay for the 357 dogfood entries, an opt-in loader source that leaves 357's default load byte-identical, and a labeler that can send the synthetic entries (and only those) to Jev through 357's existing profile.

Purpose: SPEC R1, D-18 to D-21, N-3, RESEARCH Finding 6 and Pitfall 3.
Output: two fixture files, one review sheet, additive changes to the 357 loader and labeler, `tests/test-359-corpus.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-SPEC.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-RESEARCH.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-01-SUMMARY.md
@lib/core/fork-declaration.cjs

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`.

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`; otherwise STOP. Read its `## 357 shipped contracts` section first: where it names a loader option, source constant or labeler flag differently from this plan, follow the SUMMARY and record the mapping in this plan's SUMMARY.

INERTNESS GATE: `node tests/test-359-inertness.cjs` exits 0 before the first code edit and after each commit. If it fails while a sibling wave-2 plan (359-03) has an uncommitted diff on scripts/check-card-fire.cjs, wait for that commit and re-run; never touch its diff.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; explicit-path commits only (`git add <path>`, `git add -f` under `.planning/`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md. No em-dashes or en-dashes (hyphens only). Part 8: this plan sends nothing to Jev (the label run is plan 06); dogfood text never leaves the machine. The loader must keep requiring no `lib/` module (357-01 contract); grammar checks live in the test.

Test hygiene: as in 359-01 (delete TYPESAFE_API_KEY, fetch thrower writing NETWORK_ATTEMPT_359, preload for children, mkdtemp only).
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 (this plan) |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto">
  <name>Task 1: Author the synthetic prose-fork fixture, the dogfood overlay and the review sheet</name>
  <files>tests/fixtures/card-fire-replay/prose-forks-359.json, tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json, .planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-DOGFOOD-FORK-LABELS.md</files>
  <read_first>
    - .planning/debug/resolved/intern-w1-card-discipline-decay.md (the three two-way prose forks with no card; keep only their structural shape)
    - .planning/debug/resolved/backstop-benign-list-defeats-relevance-gate.md and .planning/debug/resolved/card-fire-relevance-check-gap.md (the benign shapes: clarifying-question pair, informational step list, the "NOT options to pick between" disclaimer, footnote list, Action Footer, caveat list)
    - tests/fixtures/card-fire-corpus-238.json (meta.sanitization_statement wording; Half A benign texts as shape models)
    - tests/fixtures/card-fire-replay/debug-cases.json (a direct-mode entry as the exact shape model: envelope keys, label_origin, why)
    - tests/fixtures/card-fire-replay/dogfood.json (read locally: ids and the final assistant text of each sanitized entry)
    - lib/core/fork-declaration.cjs (formatDeclaration, parseForkDeclaration, MAX_LABEL_CHARS) and lib/core/gate-relevance.cjs (normalizeOptionLabel, isYesNoShapedGate)
    - PD/359-CONTEXT.md D-18, D-19, D-21 and N-3
  </read_first>
  <action>
    1. Create `tests/fixtures/card-fire-replay/prose-forks-359.json` as `{ "meta": { "phase": "359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros", "source": "synthetic-359", "schema": "357 D-02 entry shape plus prose_fork and fork_labels (359 D-18); see scripts/card-fire-replay-corpus.cjs validateEntry", "grammar": "N-3: Your call: p1 | p2[ | p3] | What if m", "sanitization_statement": "<every value is authored for this corpus; the intern-w1 forks are paraphrased from the structural shape in the resolved RCA; no real person, organization, room or venture content; no user or dogfood text (Part 8)>" }, "entries": [...] }`. Every entry: `{ "id": "syn359-fork-NN" or "syn359-ctrl-NN", "source": "synthetic-359", "envelope": { "mode": "direct", "output_text": <authored Larry reply>, "preceding_user_text": <authored navigator message>, "ran_entries": [], "sidechannel_health": "healthy", "reach_corroborated": false }, "expected_verdict_class", "label_origin": "hand", "why", "prose_fork", "fork_labels" (forks only) }`. Each output_text opens with exactly one De Stijl voice glyph and a realistic Larry sentence, ends in prose, and carries NO valid declaration line (raw). Prose forks: `expected_verdict_class: "block"` plus `"known_miss": {"reason": "undeclared prose fork, text-dependent (359 R2)"}`. Controls: `"pass"`, no known_miss.
    2. Fork composition (at least 15, D-19): the three intern-w1 two-way forks paraphrased (research first vs build the plan; build the plan now vs file the evidence first; the third from the RCA); at least 4 more 2-practical forks phrased as a question; at least 3 3-practical forks; at least 3 recommendation-plus-alternative forks; at least 2 forks whose practical labels are yes/no-shaped (for example `Yes, ship the draft | No, hold it for review | What if ...`) so `isYesNoShapedGate(practical.map(normalizeOptionLabel))` is true; one yes/no prefix-quirk fork whose practical labels only accidentally normalize to yes/no prefixes (RESEARCH Pitfall 5, for example `Yesterday's draft | Now | What if ...`), with `why` naming it a known limitation; one fork with Hebrew practical labels (not yes/no exempt, must be caught). Every fork_labels list has 2 or 3 practical labels then one moonshot starting `What if ` that is relevant to the reply's context and a radical departure (N-3: lateral elevation; trending to the absurd is a legitimate source); every label is at most 80 code points and contains no `[`, `]`, `|` or voice glyph.
    3. Control composition (at least 15): the six backstop-benign shapes above; at least 3 rhetorical `X or Y?` questions the same reply answers; at least 3 closing yes/no offers (`Want me to draft it?`); at least 5 near-miss declaration last lines, each a parser negative: 1 label (`Your call: build it`), no moonshot (`Your call: Build it | Research it`), a bracket in a label, the declaration followed by one more prose line, `**Your call:** ...` emphasis, a lowercase `what if` moonshot, 5 labels. If any control shape trips the BACKSTOP on the current code (the Task 2 C8 leg classifies each entry), replace that control with a non-tripping variant of the same shape and record it.
    4. Grammar self-check (session scratchpad script, not the repo): for every fork, `parseForkDeclaration(formatDeclaration(fork_labels)).declared === true`; for every entry, `parseForkDeclaration(envelope.output_text).declared === false`; no string in the file contains U+2014 or U+2013. Fix and re-run until clean.
    5. Dogfood overlay (D-21). For every id in tests/fixtures/card-fire-replay/dogfood.json, read its final assistant text locally and decide whether it poses a genuine navigator fork in prose with no card. Write `tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json` as `{ "meta": { "phase", "source": "dogfood-overlay-359", "sanitization_statement": "labels authored locally over the already-sanitized 357 dogfood entries; never sent to Jev (Part 8, D-21)", "ratified": false }, "labels": { <dogfood id>: { "prose_fork": <bool>, "fork_labels": <N-3 list, only when true: practical labels paraphrased from the sanitized text plus one authored What-if moonshot>, "fork_label_origin": "local", "why": <one line> } } }` covering every dogfood id. Create `PD/359-DOGFOOD-FORK-LABELS.md`: a header stating the labels are proposals flipped to `human` only at the plan-06 navigator checkpoint and never sent to Jev, then a table `id | sanitized one-line gist | proposed prose_fork | proposed fork_labels | why`; no raw transcript text, no names.
    6. Commit: `git add tests/fixtures/card-fire-replay/prose-forks-359.json tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json` and `git add -f PD/359-DOGFOOD-FORK-LABELS.md`, then `git commit --only -m "test(359-02): synthetic prose-fork fixture and dogfood fork-label proposals (R1, D-18, D-19, D-21, N-3)" -- <those three paths>`.
  </action>
  <verify>
    <automated>node -e "const f=require('./lib/core/fork-declaration.cjs');const c=require('./tests/fixtures/card-fire-replay/prose-forks-359.json');const F=c.entries.filter(e=>e.prose_fork===true),C=c.entries.filter(e=>e.prose_fork===false);if(F.length<15||C.length<15)process.exit(1);for(const e of F)if(!f.parseForkDeclaration(f.formatDeclaration(e.fork_labels)).declared)process.exit(2);for(const e of c.entries)if(f.parseForkDeclaration(e.envelope.output_text).declared||e.source!=='synthetic-359')process.exit(3);const o=require('./tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json');const d=require('./tests/fixtures/card-fire-replay/dogfood.json');if(d.entries.some(e=>!(e.id in o.labels)))process.exit(4);console.log('ok',F.length,C.length)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok <forks> <controls>` with both at least 15
    - `node -e "const g=require('./lib/core/gate-relevance.cjs');const c=require('./tests/fixtures/card-fire-replay/prose-forks-359.json');const n=c.entries.filter(e=>e.prose_fork&&g.isYesNoShapedGate(e.fork_labels.slice(0,-1).map(g.normalizeOptionLabel))).length;process.exit(n>=3?0:1)"` exits 0 (at least 2 yes/no forks plus the quirk)
    - `node -e "const c=require('./tests/fixtures/card-fire-replay/prose-forks-359.json');const n=c.entries.filter(e=>!e.prose_fork&&/Your call/i.test(e.envelope.output_text)).length;process.exit(n>=5?0:1)"` exits 0 (near-miss controls)
    - `node -e "const c=require('./tests/fixtures/card-fire-replay/prose-forks-359.json');process.exit(c.entries.filter(e=>e.prose_fork).every(e=>e.expected_verdict_class==='block'&&/text/.test(e.known_miss.reason))&&c.entries.filter(e=>!e.prose_fork).every(e=>e.expected_verdict_class==='pass'&&!e.known_miss)?0:1)"` exits 0
    - `git diff $PLAN_BASE --stat -- tests/fixtures/card-fire-replay/dogfood.json` is empty (357's file untouched)
    - `grep -c "$(printf '\xe2\x80\x94')" tests/fixtures/card-fire-replay/prose-forks-359.json tests/fixtures/card-fire-replay/dogfood-fork-labels-359.json` reports 0 for each
  </acceptance_criteria>
  <done>The synthetic fork and control set exists in the N-3 grammar with its D-19 composition, and every dogfood entry has a local prose_fork proposal awaiting the navigator.</done>
</task>

<task type="auto">
  <name>Task 2: Opt-in loader source, dogfood overlay loading, labeler source, and the corpus test</name>
  <files>scripts/card-fire-replay-corpus.cjs, scripts/label-card-fire-replay.cjs, tests/test-359-corpus.cjs</files>
  <read_first>
    - scripts/card-fire-replay-corpus.cjs (whole file: SOURCES, SOURCE_FILES, LABEL_ORIGINS, validateEntry, loadCorpus, exports)
    - scripts/label-card-fire-replay.cjs (LABELABLE_SOURCES, assertLabelable, selectLabelable, buildRequestBody, main's flag switch)
    - scripts/jev-devtime-client.cjs (makeEgressGuard, EGRESS_PROFILES.card_fire_replay)
    - scripts/check-card-fire.cjs exports (deriveTurnSignals, classifyCardFire, loadRegistry) for the C8 leg
    - tests/test-357-corpus-loader.cjs and tests/test-357-labeler-refusal.cjs (what they assert, so both stay green)
    - PD/359-01-SUMMARY.md `## 357 shipped contracts` items (a) and (c)
  </read_first>
  <action>
    1. `scripts/card-fire-replay-corpus.cjs` (additive; SOURCES, SOURCE_FILES, LABEL_ORIGINS and ENVELOPE_MODES unchanged): add frozen `OPT_IN_SOURCES = ['synthetic-359']`, frozen `OPT_IN_SOURCE_FILES = {'synthetic-359': 'prose-forks-359.json'}`, and `FORK359_OVERLAY_FILE = 'dogfood-fork-labels-359.json'`. validateEntry: accept a source in SOURCES or OPT_IN_SOURCES; optional `prose_fork` must be boolean; when `prose_fork === true`, `fork_labels` must be an array of at least 2 non-empty strings (SPEC R1 floor; the N-3 grammar is asserted in the test, keeping the loader free of lib/); when false or absent, `fork_labels` must be absent; optional `fork_label_origin` must be in LABEL_ORIGINS; a synthetic-359 entry must be mode `direct` and must carry a boolean prose_fork. If the shipped validator rejects unknown keys, add `prose_fork`, `fork_labels` and `fork_label_origin` to its allowed set. loadCorpus: new option `optIn` (array, default `[]`) that loads an OPT_IN_SOURCE_FILES file only when its source is listed; new option `fork359Overlay` (boolean, default false) that reads FORK359_OVERLAY_FILE from the corpus dir and merges `prose_fork`, `fork_labels` and `fork_label_origin` onto the matching dogfood entries (an overlay id with no dogfood entry is an error string; a missing overlay file is an error only when the option is true). With both options at their defaults the returned entries are deep-equal to the pre-change function's output. Export the three new constants.
    2. `scripts/label-card-fire-replay.cjs`: add `'synthetic-359'` to the LABELABLE_SOURCES literal (one edit, stays frozen) and a `--source <src>` flag that filters the selected entries to that source and, when the source is in OPT_IN_SOURCES, loads it through `loadCorpus({optIn: [src]})`; any other opt-in value exits 2. assertLabelable, the dogfood refusal and the card_fire_replay profile are untouched (no new egress profile, SPEC R1, D-20). Nothing else changes.
    3. Create `tests/test-359-corpus.cjs` ('use strict', node:assert/strict, ok counter, header naming FORK359-01, SPEC R1, D-18 to D-21, N-3; test hygiene block). Legs: C1 the fixture exists, meta.sanitization_statement is non-empty, every entry is source synthetic-359, mode direct, label_origin hand, and passes validateEntry; C2 floors: at least 15 prose_fork true and at least 15 false (SPEC R1 acceptance); C3 every prose_fork true entry has at least 2 fork_labels AND `parseForkDeclaration(formatDeclaration(fork_labels)).declared` is true (N-3), and at least 2 entries have yes/no-shaped practical labels; C4 no entry's output_text is declared, and at least 5 controls end with a `Your call` near-miss that is not declared; C5 opt-in: `loadCorpus({})` contains no synthetic-359 entry and deep-equals `loadCorpus({optIn: [], fork359Overlay: false})`; `loadCorpus({optIn: ['synthetic-359']})` includes all of them with zero errors; C6 overlay: `loadCorpus({fork359Overlay: true})` gives every dogfood entry a boolean prose_fork, every overlay prose fork passes the N-3 grammar, and the overlay covers every dogfood id; C7 labeler: LABELABLE_SOURCES includes synthetic-359; selectLabelable over an opt-in load returns the synthetic entries and no dogfood entry; buildRequestBody for one synthetic entry passes `makeEgressGuard(EGRESS_PROFILES.card_fire_replay, {root})` and for one dogfood entry throws with `refused` and the id (SPEC R1 acceptance); C8 in a hermetic env (mkdtemp MINDRIAN_HOME and CARD_FIRE_SIDECHANNEL_PATH), `classifyCardFire(deriveTurnSignals(envelope + session_id), loadRegistry())` returns a non-intercept verdict for every synthetic entry (raw forks are missed today, controls pass); C9 no U+2014 or U+2013 in either fixture file, and no NETWORK_ATTEMPT_359.
    4. Run `node tests/test-359-corpus.cjs`, `node tests/test-357-corpus-loader.cjs`, `node tests/test-357-labeler-refusal.cjs`, `node tests/test-357-replay.cjs` and `node tests/test-359-inertness.cjs`; all exit 0. Commit: `git add tests/test-359-corpus.cjs` then `git commit --only -m "feat(359-02): opt-in synthetic-359 corpus source, dogfood fork overlay, labeler source (R1, D-20, D-21)" -- scripts/card-fire-replay-corpus.cjs scripts/label-card-fire-replay.cjs tests/test-359-corpus.cjs`.
  </action>
  <verify>
    <automated>node tests/test-359-corpus.cjs && node tests/test-357-corpus-loader.cjs && node tests/test-357-labeler-refusal.cjs && node tests/test-359-inertness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - All four verify commands exit 0
    - `node -e "const c=require('./scripts/card-fire-replay-corpus.cjs');process.exit(JSON.stringify(c.SOURCES)==='[\"238\",\"debug\",\"live\",\"dogfood\"]'&&c.OPT_IN_SOURCES.includes('synthetic-359')&&Object.isFrozen(c.OPT_IN_SOURCES)?0:1)"` exits 0 (adapt the SOURCES literal to the value recorded in 359-01 contracts item a)
    - `grep -v '^\s*//' scripts/card-fire-replay-corpus.cjs | grep -c "require('\.\./lib\|require(path.join(.*'lib'"` is 0
    - `git diff $PLAN_BASE -- scripts/label-card-fire-replay.cjs | grep '^[-+]' | grep -v '^[-+][-+]' | grep -c "EGRESS_PROFILES\.\|makeEgressGuard("` is 0 (profile use untouched)
    - `R=$(mktemp -d)/r.md; env -u TYPESAFE_API_KEY node scripts/label-card-fire-replay.cjs --source synthetic-359 --dry-run --report "$R"` exits 0, and `grep -c "syn359-" "$R"` is at least 30 while `grep -c "dogfood-" "$R"` is 0 (only synthetic rows are built; Part 8)
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/card-fire-replay-corpus.cjs scripts/label-card-fire-replay.cjs tests/test-359-corpus.cjs` reports 0 for each
  </acceptance_criteria>
  <done>The corpus can say whether an entry is a prose fork and with which labels, the synthetic source is opt-in so 357's gate is untouched, dogfood labels ride an overlay, and the labeler can reach Jev for synthetic text only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dogfood fixtures -> Jev labeler | dogfood is the navigator's own sanitized session text and must never leave the machine |
| 359 fixtures -> 357 standing gate | a default-loaded 359 file would change 357's corpus and break its L7 bar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-06 | Information disclosure | label-card-fire-replay reaching dogfood text | mitigate | assertLabelable still refuses dogfood first; dogfood prose_fork labels live in a local overlay the labeler never selects; C7 leg and test-357-labeler-refusal stay green |
| T-359-07 | Tampering | 357 default corpus load | mitigate | synthetic-359 is opt-in; C5 asserts `loadCorpus({})` deep-equals the default-option load; test-357-corpus-loader and test-357-replay stay green |
| T-359-08 | Tampering | shared 357 scripts edited by 359 | mitigate | additive constants and flags only; SOURCES, profile use and refusal code unchanged; explicit-path commits; STOP on peer diffs |
| T-359-18 | Information disclosure | fixture text carrying real names or room content | mitigate | authored text only with a sanitization statement; the review sheet carries sanitized one-line gists and no raw text |
</threat_model>

<verification>
- `node tests/test-359-corpus.cjs && node tests/test-357-corpus-loader.cjs && node tests/test-357-labeler-refusal.cjs && node tests/test-357-replay.cjs && node tests/test-359-inertness.cjs` all exit 0
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts/check-card-fire.cjs` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
The corpus carries the prose_fork / fork_labels dimension on an opt-in synthetic source and a dogfood overlay, meeting the 15/15 floors in the N-3 grammar, without changing a single 357 default-load entry or reaching Jev with dogfood text.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-02-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, fork and control counts by D-19 category, the yes/no and quirk ids, the near-miss ids, dogfood overlay counts (prose_fork true / false), any control replaced because it tripped the backstop, and any 357 contract name mapping used.
</output>
