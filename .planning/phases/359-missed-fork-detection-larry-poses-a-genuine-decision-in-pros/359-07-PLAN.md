---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 07
type: execute
wave: 3
depends_on: ["359-02", "359-03"]
files_modified:
  - scripts/replay-card-fire.cjs
  - tests/test-359-replay.cjs
autonomous: true
requirements: [FORK359-02, FORK359-05, FORK359-06, FORK359-07]
tags: [card-fire, replay, metrics, tri-polar, hermetic]

must_haves:
  truths:
    - "Every 359 metric lives under `counts.fork359` (plus a per-entry `fork359` object) behind a `--fork359` flag, so 357's `counts.missed_forks` and every existing count, flag, JSON key and exit rule keep their meaning; without --fork359 or an opt-in source the replay output for the 357 corpus is unchanged (RESEARCH Finding 6, SPEC R5)"
    - "fork359.missed_forks counts entries with prose_fork true, no card and raw CLI class pass (SPEC R2)"
    - "For every prose_fork true entry the replay derives a declared variant by appending formatDeclaration(fork_labels) as the final line (direct mode: to output_text; transcript modes: to the last assistant record's text) and expects block `declared-fork-no-card`, except yes/no-shaped practical labels, which expect pass `gate-is-simple-binary` and are counted separately; controls (prose_fork false and expected pass) replay unchanged and a block is a control false block (SPEC R6, D-06, N-3)"
    - "declared_catch_rate = caught / (variants minus simple-binary, card-fired, ceiling and unbuildable exclusions); a zero denominator is vacuous and fails; with --fork359 the replay exits 1 on a catch rate below 1, any control false block, any declared-variant missed fork, any declared CLI/MCP class mismatch or any MCP payload mismatch, except in `--baseline write-359` mode, which records (SPEC R6, R7)"
    - "On surface both, each block variant must also fire on MCP with rendered option labels equal to fork_labels in order, the What-if moonshot last (SPEC R7, D-10, N-3)"
    - "Expectations use HEAD's parser, normalizer and isYesNoShapedGate regardless of `--code-root`, so a pre-359 code root measures the missing arm rather than moving the target (RESEARCH Pattern 2)"
    - "`--code-root git:<sha>` archives any sha like the pre-phase form, and `--baseline write-359` accepts only `git:<pre_359_sha>` from pre-359.json and writes baseline-359.json, never 357's baseline.json (D-22, RESEARCH Pattern 4)"
  artifacts:
    - path: "scripts/replay-card-fire.cjs"
      provides: "--fork359 metrics, declared-variant derivation, git:<sha> code root, write-359 baseline"
      contains: "fork359"
    - path: "tests/test-359-replay.cjs"
      provides: "L1 pre-359 missed forks, L2 declared catch and controls, L3 parity, L4 payload, L5 dogfood overlay, L6 hygiene, L7 baseline (active once written), reserved --mutation"
      min_lines: 200
  key_links:
    - from: "scripts/replay-card-fire.cjs"
      to: "lib/core/fork-declaration.cjs"
      via: "formatDeclaration and constants loaded from the HEAD repo path, never the code root"
      pattern: "fork-declaration"
    - from: "scripts/replay-card-fire.cjs"
      to: "scripts/card-fire-replay-corpus.cjs"
      via: "loadCorpus({optIn: OPT_IN_SOURCES, fork359Overlay})"
      pattern: "fork359Overlay"
    - from: "tests/test-359-replay.cjs"
      to: "tests/fixtures/card-fire-replay/pre-359.json"
      via: "pre_359_sha for --code-root git:<sha>"
      pattern: "pre_359_sha"
---

<objective>
Turn the 357 replay into the 359 measuring instrument: a missed-fork count, declared variants that prove the new arm catches every prose fork on both surfaces, control false blocks, CLI/MCP payload parity, a generic git code root and a 359-owned baseline mode, all additive and behind an opt-in flag.

Purpose: SPEC R2, R5, R6, R7; D-22; RESEARCH Finding 6 and Patterns 2 and 4.
Output: additive changes to `scripts/replay-card-fire.cjs`, and `tests/test-359-replay.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-SPEC.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-RESEARCH.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-01-SUMMARY.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-03-SUMMARY.md
@scripts/replay-card-fire.cjs

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`.

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`; otherwise STOP. Use the flag, key and field names recorded in 359-01 `## 357 shipped contracts` items (a) and (b) where they differ from this plan's wording.

INERTNESS GATE: `node tests/test-359-inertness.cjs` exits 0 before the first edit and after each commit (this plan must not change any default replay result). Also `node tests/test-357-replay.cjs` and `node tests/test-357-replay.cjs --mutation` stay green.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md, tests/fixtures/card-fire-replay/baseline.json (357 owns it). No em-dashes or en-dashes. The replay stays network-free (357's fetch thrower), hermetic per entry per surface, and never required from lib/ or hooks/; scripts must not require anything under tests/.

Test hygiene: as in 359-01.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 (this plan) |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto">
  <name>Task 1: Additive --fork359 metrics, declared-variant derivation, MCP payload parity, git code root and the write-359 baseline</name>
  <files>scripts/replay-card-fire.cjs</files>
  <read_first>
    - scripts/replay-card-fire.cjs (whole file: main argv switch, prepareCodeRoot branches, loadCodeRootModules, withHermeticEnv, materializeEnvelope, runCliStop, runMcpStop and what it records, runEntry, classifyOutcome, runReplay counts, baseline write and compare, JSON output, exit codes, exports)
    - scripts/card-fire-replay-corpus.cjs (OPT_IN_SOURCES, loadCorpus optIn and fork359Overlay options from 359-02)
    - lib/core/fork-declaration.cjs (formatDeclaration, DECLARATION_SEPARATOR) and lib/core/gate-relevance.cjs (normalizeOptionLabel, isYesNoShapedGate)
    - lib/mcp/stop-gate-handler.cjs (the fire:true return shape and the rendered card options path; 359-01 contracts item b)
    - tests/fixtures/card-fire-replay/pre-359.json
    - PD/359-RESEARCH.md Finding 6, Patterns 2 and 4, Pitfall 3
  </read_first>
  <action>
    Edit `scripts/replay-card-fire.cjs` additively; no existing branch, count or exit rule changes meaning.
    1. Loading: when `--source` names a value in OPT_IN_SOURCES, or `--fork359` is present, call loadCorpus with `optIn: OPT_IN_SOURCES`; with `--fork359`, also `fork359Overlay: true`. Otherwise call it exactly as today.
    2. `--code-root git:<sha>`: a new prepareCodeRoot branch that archives `<sha>` exactly like the pre-phase branch (`git archive <sha> scripts lib data package.json .claude-plugin`, extract into a mkdtemp with prefix `replay-359-root-`, symlink node_modules, cleanup in finally); when `<sha>` equals pre-359.json's `pre_359_sha`, verify its `runtime_files` digests against the extracted files (throw naming the path on a mismatch). The existing branches are untouched.
    3. Expectation helpers loaded from HEAD, never from the code root: `const HEAD_DECL = require(path.join(__dirname, '..', 'lib', 'core', 'fork-declaration.cjs'))` and HEAD gate-relevance for `normalizeOptionLabel` and `isYesNoShapedGate`.
    4. `deriveDeclaredVariant(entry)`: deep-copies the entry, sets id `<id>#decl`; direct mode: `output_text` = original with trailing whitespace trimmed + `\n` + `HEAD_DECL.formatDeclaration(fork_labels)`; transcript modes (dogfood): append the same line as a final line of the last assistant record's text (string content, or the last text block of array content); when no assistant text record exists, return null (counted `variant_unbuildable`). Steps and counters are copied unchanged.
    5. With `--fork359`, after the normal per-entry run: for every entry with `prose_fork === true`: raw missed when the raw CLI class is pass and the envelope carries no card (direct `askuserquestion_fired !== true`; transcript: no AskUserQuestion tool_use in the final step's current window); derive the variant and run it on the requested surfaces with `runEntry` (fresh hermetic env and session per surface, as today); expected = pass `gate-is-simple-binary` when `isYesNoShapedGate(fork_labels.slice(0, -1).map(normalizeOptionLabel))`, else block `declared-fork-no-card`; a variant whose raw entry had a card is `card_fired_excluded`; a variant whose envelope counters are at or over MAX_FORCE_RETRIES or MAX_SESSION_INTERCEPTS (read from the code root's exports) expects the matching degrade reason and is `ceiling_excluded`. A block-expected variant counts `declared_caught` when the CLI class is block with reason `declared-fork-no-card`, else `declared_missed_forks`; a simple-binary variant must return that reason (else it is a `declared_missed_forks` mismatch too). On MCP: a block-expected variant must return `fire === true` (else `declared_parity_mismatches`), and its rendered option labels must deep-equal fork_labels in order (else `mcp_payload_mismatches`). For every entry with `prose_fork === false` and expected pass: `controls`++, and a raw CLI or MCP block is `control_false_blocks`++. `declared_catch_rate` = declared_caught / (variants - simple_binary_excluded - card_fired_excluded - ceiling_excluded - variant_unbuildable), or null when the denominator is 0.
    6. Output: `counts.fork359 = {prose_forks, missed_forks, controls, control_false_blocks, declared_variants, declared_caught, simple_binary_excluded, card_fired_excluded, ceiling_excluded, variant_unbuildable, declared_catch_rate, declared_missed_forks, declared_parity_mismatches, mcp_payload_mismatches}` and, per entry, `fork359: {raw_missed, variant: {cli, mcp, expected, outcome}}`; the human table gains a fork359 block. Without `--fork359` neither key appears.
    7. Exit: all existing rules first; then with `--fork359` exit 1 when declared_catch_rate is null or below 1, or control_false_blocks, declared_missed_forks, declared_parity_mismatches or mcp_payload_mismatches is above 0. `--baseline write-359`: require `--code-root git:<pre_359_sha>` with the sha from pre-359.json (else exit 2); write `tests/fixtures/card-fire-replay/baseline-359.json` as `{meta: {pre_359_sha, code_root, written_at, entry_count, sources}, fork359: {prose_forks, missed_forks, controls, control_false_blocks}, verdicts: {<id>: 'block'|'pass'}}` and exit 0; never write baseline.json.
    8. Export `deriveDeclaredVariant` in addition to the existing exports. Header comment gains two lines naming the 359 additions (SPEC R2, R6, R7; D-22; opt-in).
    9. Run the INERTNESS GATE, `node tests/test-357-replay.cjs` and `node tests/test-357-replay.cjs --mutation`. Commit: `git commit --only -m "feat(359-07): replay fork359 metrics, declared variants, MCP payload parity, git code root, write-359 baseline (R2, R6, R7, D-22)" -- scripts/replay-card-fire.cjs`.
  </action>
  <verify>
    <automated>node tests/test-359-inertness.cjs && node tests/test-357-replay.cjs && node scripts/replay-card-fire.cjs --source synthetic-359 --fork359 --surface both --json > /tmp/r359.json; echo "exit=$?"; node -e "const r=require('/tmp/r359.json');const f=r.counts.fork359;if(!f||f.declared_catch_rate!==1||f.control_false_blocks||f.declared_missed_forks||f.mcp_payload_mismatches||f.declared_parity_mismatches)process.exit(1);console.log('fork359',JSON.stringify(f))"</automated>
  </verify>
  <acceptance_criteria>
    - The verify prints `exit=0` and a fork359 line with declared_catch_rate 1 and every mismatch count 0
    - `node scripts/replay-card-fire.cjs --surface both --json` output has no `fork359` key (default mode unchanged) and `node tests/test-359-inertness.cjs` exits 0
    - `git diff $PLAN_BASE -- scripts/replay-card-fire.cjs | grep '^-' | grep -v '^---' | grep -v '^-\s*//' | wc -l` is at most 10 (additive: only condition lines extended)
    - `grep -v '^\s*//' scripts/replay-card-fire.cjs | grep -c "baseline-359.json"` is at least 1 and `grep -v '^\s*//' scripts/replay-card-fire.cjs | grep -c "write-359"` is at least 1
    - `ls -d /tmp/replay-359-root-* 2>/dev/null | wc -l` is 0 after a `--code-root git:<pre_359_sha>` run
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/replay-card-fire.cjs` is 0
  </acceptance_criteria>
  <done>The replay measures missed forks, proves the declared arm on both surfaces with payload parity, and can write a 359-owned pre-359 baseline, while every 357 output is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: The 359 replay test (pre-359 missed forks, declared catch, controls, parity, payload, overlay, hygiene)</name>
  <files>tests/test-359-replay.cjs</files>
  <read_first>
    - scripts/replay-card-fire.cjs (Task 1: flags, counts.fork359 keys, per-entry fork359 object, exit codes)
    - tests/test-357-replay.cjs (spawn helper, preload, temp corpora conventions; copy the style, do not require it)
    - tests/fixtures/card-fire-replay/prose-forks-359.json and tests/fixtures/card-fire-replay/pre-359.json
    - PD/359-06-SUMMARY.md (only if present at run time: ruled flips that change expected counts)
  </read_first>
  <action>
    Create `tests/test-359-replay.cjs` ('use strict', node:assert/strict, ok counter, header naming FORK359-02, -05, -06, -07 and SPEC R2, R5, R6, R7; test hygiene). Helper `replay(args)` spawns `node scripts/replay-card-fire.cjs ...args --json` and returns `{status, json, stderr}`. `PRE359` is read from pre-359.json. Legs:
    - L1 pre-359 missed forks (SPEC R2 acceptance): `--code-root git:$PRE359 --source synthetic-359 --fork359 --surface cli` reports `fork359.missed_forks` equal to the number of synthetic prose_fork true entries, `declared_caught` 0, and exit 1 (the arm does not exist there, so the bar fails as it must).
    - L2 HEAD declared variants (SPEC R6 acceptance): `--source synthetic-359 --fork359 --surface both` exits 0; declared_catch_rate 1; control_false_blocks 0; declared_missed_forks 0; simple_binary_excluded at least 2 and the per-entry list includes the yes/no prefix-quirk id (printed as a known limitation, RESEARCH Pitfall 5); the Hebrew fork's variant is caught.
    - L3 parity (SPEC R7 acceptance): the L2 run has declared_parity_mismatches 0 and parity_mismatches 0; `--surface both` over the default corpus (the R5 entries) exits 0 with parity_mismatches 0.
    - L4 payload (SPEC R7 acceptance): the L2 run has mcp_payload_mismatches 0; for one 4-label fork, print its variant's MCP rendered labels and assert they equal its fork_labels with the What-if label last.
    - L5 dogfood overlay: `--fork359 --surface both` over the default corpus plus overlay exits 0 when the overlay is ratified (else prints `SKIP L5: overlay not ratified until 359-06`), reports counts.fork359 for the dogfood prose forks, and stderr carries no NETWORK_ATTEMPT_359.
    - L6 hygiene: no `replay-359-root-*` or `replay-357-root-*` dir remains in os.tmpdir() after the legs; `git status --short -- tests/fixtures/card-fire-replay/baseline.json` is empty.
    - L7 baseline (active when `tests/fixtures/card-fire-replay/baseline-359.json` exists, else print `SKIP L7: baseline-359 lands in 359-08`): its meta.pre_359_sha equals PRE359, and its fork359.missed_forks equals the value a fresh `--code-root git:$PRE359 --fork359 --surface cli` run reports over the same sources (synthetic-359 plus the ratified dogfood overlay), which is at least the L1 value.
    - `--mutation` argv flag is reserved: when baseline-359.json is absent, print `SKIP mutation: lands in 359-08` and exit 0.
    Exit 1 on any failed leg. Run it (L7 and mutation SKIP). Commit: `git add tests/test-359-replay.cjs` then `git commit --only -m "test(359-07): replay legs for missed forks, declared catch, controls, parity, payload (R2, R6, R7)" -- tests/test-359-replay.cjs`.
  </action>
  <verify>
    <automated>node tests/test-359-replay.cjs && node tests/test-359-inertness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both verify commands exit 0; the output shows L1 to L6 passing (L5 may SKIP only before the plan-06 ratification) and SKIP lines for L7 and mutation
    - `grep -c "NETWORK_ATTEMPT_359" tests/test-359-replay.cjs` is at least 2
    - `grep -c "git:" tests/test-359-replay.cjs` is at least 1 and `grep -c "declared_catch_rate" tests/test-359-replay.cjs` is at least 1
    - `git status --short` after the run shows no file changed outside this plan's paths
    - `grep -c "$(printf '\xe2\x80\x94')" tests/test-359-replay.cjs` is 0
  </acceptance_criteria>
  <done>The standing replay test proves every prose fork is missed on pre-359 code, caught on HEAD once declared, with zero control false blocks and CLI/MCP payload parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay -> 357's standing gate and outputs | a shared harness; its default behavior is 357's contract |
| replay process -> navigator machine state and network | must stay hermetic and offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-31 | Tampering | 357 metrics and exit semantics | mitigate | all 359 metrics under counts.fork359 behind --fork359; default output unchanged (acceptance and test-359-inertness); test-357-replay default and --mutation stay green |
| T-359-32 | Repudiation | a vacuous 100% catch rate | mitigate | zero denominator is null and fails; exclusions counted separately; L1 proves the metric reads 0 caught on pre-359 code |
| T-359-33 | Tampering | 357's baseline.json overwritten | mitigate | write-359 writes only baseline-359.json and requires git:<pre_359_sha>; L6 asserts baseline.json untouched |
| T-359-34 | Information disclosure | network or real-room access during replay | mitigate | 357's fetch thrower and per-entry hermetic env reused for variants; NETWORK_ATTEMPT_359 legs |
| T-359-35 | Denial of service | leftover archives | mitigate | cleanup in finally; L6 checks os.tmpdir() |
</threat_model>

<verification>
- `node tests/test-359-replay.cjs && node tests/test-359-inertness.cjs && node tests/test-357-replay.cjs` all exit 0
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks scripts/check-card-fire.cjs tests/fixtures/card-fire-replay/baseline.json` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
</verification>

<success_criteria>
One replay command reports missed forks, the declared catch rate, control false blocks and CLI/MCP payload parity for 359, and writes a 359-owned baseline against the pre-359 commit, without changing anything 357 measures.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-07-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, the L1 pre-359 missed_forks value, the L2 counts.fork359 object, the simple-binary and quirk ids, the default-mode no-change evidence, and any 357 name mapping used.
</output>
