---
phase: 359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros
plan: 05
type: execute
wave: 2
depends_on: ["359-01"]
files_modified:
  - tests/fixtures/forward-fork-scenarios-359.json
  - scripts/fork359-permission-probe.cjs
  - scripts/forward-fork-scenarios-359.cjs
  - tests/fixtures/forward-fork-stream-359/
  - tests/test-359-forward-harness.cjs
autonomous: true
requirements: [FORK359-09]
tags: [forward-run, headless, dev-only, permission-host, measurement]

must_haves:
  truths:
    - "The forward scenario set is authored text only (no user, dogfood or transcript text): at least 10 fork-eliciting and at least 10 control scenarios, each a short multi-turn session whose LAST user turn is the measured turn, each with an authored `poses_fork` label (never Jev) and, for forks, reference fork_labels in the N-3 grammar (SPEC R9, D-11, RESEARCH Pitfall 7)"
    - "The harness is dev-only: it lives under scripts/, is never imported by lib/ or hooks/, never runs in CI, spends nothing without an explicit arm run, and keeps raw streams and results outside the repo under ~/.cache/mindrian-dev/359-forward/ (D-11, SPEC R9)"
    - "Every run is hermetic and faithful: a `git archive` snapshot tree per arm loaded with --plugin-dir, `--agent mos:larry-extended`, `--setting-sources project,local`, keychain auth (never --bare), `--no-session-persistence`, per-run temp MINDRIAN_HOME, MINDRIAN_ROOMS_HOME/ROOT with a scratch room bound by cwd, a per-run temp side channel, MINDRIAN_DISABLE_AUTO_REGISTER set and TYPESAFE_API_KEY removed (D-12, RESEARCH Findings 10 and 11, Pitfall 9)"
    - "In probe mode a dev-only stdio MCP permission host (`--permission-prompt-tool mcp__fork359probe__permission`) makes AskUserQuestion available under -p, denies every permission request with a neutral message, and logs each AskUserQuestion's option labels; text mode (N-4 fallback) runs without the host and measures declaration adoption only (RESEARCH Finding 8, N-4)"
    - "Preflight refuses to start when the model is not claude-sonnet-5 or matches fable, when ~/.claude/agents/larry-extended.md resolves to an existing file (shadowed agent), when claude is older than 2.1.259, or when the projected total exceeds USD 60; every run carries --max-budget-usd 0.40 (N-2, N-5, D-12)"
    - "Parsing splits the measured turn's attempt 1 at the first Stop hook event: card = an AskUserQuestion tool_use in attempt 1 whose options are not the MODE_MENU trio; declared = parseForkDeclaration over attempt 1's last assistant text; a control block is computed offline by the arm tree's deriveTurnSignals + classifyCardFire and corroborated by the per-run intercept log (D-12, RESEARCH Finding 9, Pitfall 8)"
    - "--evaluate reports, per arm, fork runs, cards, declarations and missed forks, control declared blocks, the vacuity floor max(6, ceil(0.2 x pre fork runs)) and an outcome PASS, INCONCLUSIVE or FALSIFIED, in counts and scenario ids only; budget-hit runs are ERROR, never misses (SPEC R9, N-4, RESEARCH Finding 12)"
  artifacts:
    - path: "tests/fixtures/forward-fork-scenarios-359.json"
      provides: "authored forward scenarios (fork and control) with poses_fork labels"
      contains: "poses_fork"
    - path: "scripts/fork359-permission-probe.cjs"
      provides: "dev-only stdio MCP permission host (deny-all, AskUserQuestion log)"
      contains: "permission"
      min_lines: 50
    - path: "scripts/forward-fork-scenarios-359.cjs"
      provides: "dev-only forward harness: preflight, buildRunCommand, runOne, parseRun, evaluate, main"
      exports: ["preflight", "buildRunCommand", "parseRun", "evaluate", "main"]
      min_lines: 300
    - path: "tests/test-359-forward-harness.cjs"
      provides: "offline legs: parse, preflight refusals, dry-run spends nothing, evaluate outcomes, probe round trip"
      min_lines: 180
  key_links:
    - from: "scripts/forward-fork-scenarios-359.cjs"
      to: "lib/core/fork-declaration.cjs"
      via: "parseForkDeclaration loaded from the ARM tree for the declaration count"
      pattern: "fork-declaration"
    - from: "scripts/forward-fork-scenarios-359.cjs"
      to: "scripts/fork359-permission-probe.cjs"
      via: "--mcp-config probe.json + --permission-prompt-tool mcp__fork359probe__permission"
      pattern: "mcp__fork359probe__permission"
---

<objective>
Build (and prove offline) the R9 forward-measurement instrument without spending anything: an authored scenario set, a dev-only permission host that makes the card observable under headless Claude Code, and a harness that runs, parses and evaluates pre/post arms under hard spend caps and a vacuity floor.

Purpose: SPEC R9, D-11, D-12, N-2, N-4, N-5, RESEARCH Findings 8 to 12 and Pitfalls 6 to 9. The paid runs happen only in plans 10 and 11 behind navigator checkpoints.
Output: the scenario fixture, the probe server, the harness, canned stream fixtures, `tests/test-359-forward-harness.cjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-SPEC.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-CONTEXT.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-RESEARCH.md
@.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-01-SUMMARY.md
@lib/core/fork-declaration.cjs

PD = `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros`. RESULTS_DIR = `~/.cache/mindrian-dev/359-forward/` (outside every git repo; created on demand).

Precondition: `head -1 PD/359-01-SUMMARY.md` prints `357 GATE: PASSED`; otherwise STOP.

This plan never runs a paid `claude -p` session. Every test stubs `claude` with a fake executable on PATH. The only real `claude` invocation allowed is `claude --version`.

Hermetic run environment (RESEARCH Pattern 5, Findings 10 and 11): per run a mkdtemp dir with `home/`, `rooms/scratch-359/ROOM.md` (a minimal Layer-0 identity: title and one sentence saying it is a scratch room for the 359 forward run), `scratch-359/` as the cwd, `card-fire-reached.json`, `probe.jsonl`; child env = process.env copy with `MINDRIAN_HOME=<tmp>/home`, `MINDRIAN_ROOMS_HOME=<tmp>/rooms`, `MINDRIAN_ROOMS_ROOT=<tmp>/rooms`, `CARD_FIRE_SIDECHANNEL_PATH=<tmp>/card-fire-reached.json`, `FORK359_PROBE_LOG=<tmp>/probe.jsonl`, `MINDRIAN_DISABLE_AUTO_REGISTER=1`, `MINDRIAN_OPEN_BROWSER_DISABLE=1`, and `TYPESAFE_API_KEY` deleted; HOME is NOT changed (keychain auth; never `--bare`). The scratch room resolves through `scripts/resolve-room` Strategy 0b (`$MINDRIAN_ROOMS_HOME/<cwd basename>`), which suppresses the session-start MODE_ROUTING card.

Parallel-session rules (every task): record `PLAN_BASE=$(git rev-parse HEAD)`; `git status --short -- <file>` before editing and STOP on a diff you did not make; explicit-path commits only (`git add <path>`, then `git commit --only -m "<msg>" -- <paths>`); never `git add -A`, `git commit -a`, `git stash`, or revert an unowned diff. Never write `.planning/STATE.md`. Never edit: lib/mcp/brain-router.cjs, lib/core/write-lock.cjs, lib/core/part8-egress-guard.cjs, scripts/doctor.cjs, lib/core/graph-ops.cjs, lib/core/navigation.cjs, scripts/eval-icm-writers.cjs, tests/test-353-grader-agreement.cjs, tests/test-353-ledger-shape.cjs, docs/OPEN-HANDOFFS.md, `~/.claude/agents/larry-extended.md`, any `~/.claude/settings*.json`. No em-dashes or en-dashes. No new package: the probe uses the installed `@modelcontextprotocol/sdk` (1.29.0, a declared dependency) and `zod`.

Test hygiene: as in 359-01.
</context>

## Artifacts this phase produces

| Artifact | Plan |
|----------|------|
| `tests/fixtures/card-fire-replay/pre-359.json`, `tests/test-359-inertness.cjs`, `lib/core/fork-declaration.cjs`, `tests/test-359-fork-declaration.cjs`, `normalizeOptionLabel` export | 01 |
| Opt-in `synthetic-359` loader source and dogfood overlay, labeler source, `prose-forks-359.json`, `dogfood-fork-labels-359.json`, `359-DOGFOOD-FORK-LABELS.md`, `tests/test-359-corpus.cjs` | 02 |
| Declared arm, `declaredIdentity`, retry-key suffix, MCP card and dedup changes, `tests/test-359-declared-arm.cjs`, `tests/test-359-mcp-declared.cjs` | 03 |
| `data/jev-policies/fork359-moonshot.json`, `fork359_moonshot` profile, `scripts/score-moonshots-359.cjs`, `tests/test-359-moonshot-scorer.cjs` | 04 |
| `scripts/forward-fork-scenarios-359.cjs`, `scripts/fork359-permission-probe.cjs`, `tests/fixtures/forward-fork-scenarios-359.json`, canned streams, `tests/test-359-forward-harness.cjs` | 05 (this plan) |
| `359-JEV-LABEL-REPORT.md`, `359-MOONSHOT-SCORES.md`, ratified dogfood fork labels (human checkpoint) | 06 |
| `--fork359` replay metrics, `--code-root git:<sha>`, `--baseline write-359`, `tests/test-359-replay.cjs` | 07 |
| `baseline-359.json`, `tests/run-all-359.sh`, run-all-238 legs, mutation legs, `tests/test-359-devonly-tripwire.cjs`, VALIDATION sign-off | 08 |
| R8 prose on 4 surfaces, `tests/test-359-prose-budget.cjs`, regenerated `data/harness-manifest.json` (one revertible commit) | 09 |
| R9 smoke run and cost projection (navigator-gated spend) | 10 |
| R9 full forward run, bar verdict or falsification branch, moonshot scores of forward declarations | 11 |
| Requirement closure, dual research filing, close-out | 12 |

<tasks>

<task type="auto">
  <name>Task 1: The authored forward scenario set and the dev-only permission-probe MCP server</name>
  <files>tests/fixtures/forward-fork-scenarios-359.json, scripts/fork359-permission-probe.cjs</files>
  <read_first>
    - .planning/debug/resolved/intern-w1-card-discipline-decay.md (the decay shape: card on turn 1, prose forks on turns 2 and later)
    - bin/mindrian-mcp-server.cjs and node_modules/@modelcontextprotocol/sdk/package.json (how this repo builds a stdio McpServer with zod on SDK 1.29.0: imports, registerTool or tool signature, StdioServerTransport)
    - PD/359-RESEARCH.md Finding 8 (permission host contract: input {tool_name, input, tool_use_id}; output text JSON {behavior: 'deny', message}), Findings 11 and 12, Pitfall 7
    - lib/core/fork-declaration.cjs (grammar for the reference fork_labels)
  </read_first>
  <action>
    1. Create `tests/fixtures/forward-fork-scenarios-359.json`: `{ "meta": { "phase": "359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros", "sanitization_statement": "authored for this phase; no user, dogfood or transcript text (SPEC R9, Part 8)", "model": "claude-sonnet-5", "runs_per_scenario": 3, "caps": { "per_run_usd": 0.40, "total_usd": 60 }, "vacuity_floor": { "min_pre_missed": 6, "min_fraction_of_pre_fork_runs": 0.2 } }, "scenarios": [...] }`. Each scenario: `{ "id": "fwd-fork-NN" | "fwd-ctrl-NN", "poses_fork": <bool>, "turns": [3 authored user messages, each at most 600 chars], "fork_labels": <reference N-3 list, forks only>, "why": <one line> }`. At least 10 fork scenarios whose last turn makes a genuine navigator fork the natural reply (two or three viable next moves in a generic venture or research situation, with the decision belonging to the navigator; include the intern-w1 shapes such as research first vs build the plan), with turns 1 and 2 as realistic warm-ups so the measured turn is turn 3 (where card discipline decays). At least 10 control scenarios whose last turn has one right answer or is an instruction (a factual question, a definition, "draft the summary", a thank-you, accepting an earlier offer). No real names, no room or venture specifics, hyphens only.
    2. Create `scripts/fork359-permission-probe.cjs` ('use strict'; header: dev-only permission host for the Phase 359 forward run, never imported by lib/ or hooks/, never in CI, denies EVERY permission request (ASVS V4), zero network, writes only to FORK359_PROBE_LOG). Build a stdio MCP server named `fork359probe` on the installed SDK (mirror bin/mindrian-mcp-server.cjs's import and registration style) with ONE tool `permission` whose input schema accepts `tool_name` (string), `input` (any, passthrough) and optional `tool_use_id` (string). Handler: when `process.env.FORK359_PROBE_LOG` is set, mkdir its dirname and append one JSON line `{ts, tool_name, options}` where `options` is the flat list of `questions[].options[].label` for `tool_name === 'AskUserQuestion'` (else omitted); then return `{content: [{type: 'text', text: JSON.stringify({behavior: 'deny', message: 'No one can answer in this run; continue.'})}]}`. Never throw out of the handler (log-write failures are swallowed).
    3. Probe self-check (session scratchpad): spawn the probe, send JSON-RPC `initialize`, `notifications/initialized`, `tools/list` (exactly one tool `permission`), `tools/call` for AskUserQuestion with two options (deny JSON returned; one log line with both labels); record the exchange shape in the SUMMARY.
    4. Commit: `git add tests/fixtures/forward-fork-scenarios-359.json scripts/fork359-permission-probe.cjs` then `git commit --only -m "feat(359-05): authored forward scenarios and deny-all permission probe (R9, D-11, N-4)" -- tests/fixtures/forward-fork-scenarios-359.json scripts/fork359-permission-probe.cjs`.
  </action>
  <verify>
    <automated>node -e "const f=require('./lib/core/fork-declaration.cjs');const s=require('./tests/fixtures/forward-fork-scenarios-359.json');const F=s.scenarios.filter(x=>x.poses_fork===true),C=s.scenarios.filter(x=>x.poses_fork===false);if(F.length<10||C.length<10||s.meta.model!=='claude-sonnet-5')process.exit(1);for(const x of s.scenarios){if(!Array.isArray(x.turns)||x.turns.length<2||x.turns.some(t=>t.length>600))process.exit(2)}for(const x of F)if(!f.parseForkDeclaration(f.formatDeclaration(x.fork_labels)).declared)process.exit(3);console.log('ok',F.length,C.length)" && node -e "require('child_process').execFileSync('node',['-c','scripts/fork359-permission-probe.cjs']);console.log('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok <forks> <controls>` (each at least 10) and `syntax ok`
    - `grep -c "behavior" scripts/fork359-permission-probe.cjs` is at least 1 and `grep -v '^\s*//' scripts/fork359-permission-probe.cjs | grep -c "'allow'\|\"allow\""` is 0 (deny-all)
    - `grep -v '^\s*//' scripts/fork359-permission-probe.cjs | grep -c "fetch(\|https\?\.request\|require('http"` is 0
    - `grep -c "$(printf '\xe2\x80\x94')" tests/fixtures/forward-fork-scenarios-359.json scripts/fork359-permission-probe.cjs` reports 0 for each
  </acceptance_criteria>
  <done>An authored multi-turn scenario set with its own fork labels exists, and a deny-all permission host can make AskUserQuestion observable and logged under headless runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The forward harness (preflight, hermetic run, parse, evaluate) and its offline test with canned streams</name>
  <files>scripts/forward-fork-scenarios-359.cjs, tests/fixtures/forward-fork-stream-359/, tests/test-359-forward-harness.cjs</files>
  <read_first>
    - scripts/skillopt-funnel.cjs (lines ~160-185: the repo's headless arg-array style) and scripts/huji-run-one.cjs (lines ~250-275)
    - scripts/check-card-fire.cjs exports (deriveTurnSignals, classifyCardFire, loadRegistry) as they will exist in the arm trees
    - scripts/session-start (the MODE_MENU string near :754: the labels Just Talk, Explore + Capture, Build a Room) and scripts/resolve-room (Strategy 0b)
    - scripts/replay-card-fire.cjs (the 357 git archive plus node_modules symlink recipe, to copy for arm trees)
    - PD/359-RESEARCH.md Pattern 5, Findings 9 to 12, Pitfalls 6 to 9, Assumptions A1 to A9
  </read_first>
  <behavior>
    - parseRun over canned streams: (a) AskUserQuestion tool_use in attempt 1 -> card true; (b) attempt 1 ends with a valid declaration, then a Stop hook event, then a card -> declared true, card false; (c) neither -> missed (for a fork scenario); (d) only a MODE_MENU card -> card false, card_is_mode_menu true; (e) init tools lack AskUserQuestion in probe mode -> status error_no_card_tool; (f) init lists two plugins named mos, or one whose path is not the arm tree -> error_wrong_plugin; (g) SessionStart output contains `No room detected` -> error_mode_routing; (h) a result event with the budget-exceeded subtype or cost at or over 0.40 -> error_budget; (i) no Stop hook event in the stream -> attempt 1 is the whole measured turn and `split: 'no-hook-events'` is recorded
    - preflight refuses (exit 2, naming the reason): model `claude-fable-anything`; model not `claude-sonnet-5`; a HOME whose `.claude/agents/larry-extended.md` is a real file; projected total over 60; per-run cap other than 0.40
    - --dry-run prints one command per planned run containing every required flag, never `--bare`, never `--dangerously-skip-permissions`, and invokes `claude` only for `--version`
    - evaluate over synthetic result rows: pre 12 of 30 missed, post 5 of 30, 0 control blocks -> PASS; pre 5 of 30 -> INCONCLUSIVE (below floor); pre 12, post 7 -> FALSIFIED; pre 12, post 4, 1 post control block -> FALSIFIED; error rows are excluded from both numerator and denominator and counted separately
    - the run env built for a run lacks TYPESAFE_API_KEY, carries the hermetic variables, and keeps HOME unchanged
    - the default results path is outside the repo
  </behavior>
  <action>
    1. Create canned streams under `tests/fixtures/forward-fork-stream-359/` (one `.jsonl` per behavior case a to i): minimal authored stream-json event sequences shaped after the documented types (`system` subtype `init` with `tools`, `plugins`, `model`, `session_id`; hook lifecycle events for SessionStart and Stop as emitted with --include-hook-events; `user`; `assistant` with text and tool_use content blocks; `result` with `total_cost_usd`, `duration_ms`, `subtype`). Placeholder text only. Record in the SUMMARY that these shapes are provisional until the plan-10 smoke run confirms or corrects them.
    2. Write `tests/test-359-forward-harness.cjs` FIRST (header naming FORK359-09, D-11, D-12, N-2, N-4, N-5; test hygiene) covering the behaviors plus a probe round trip leg (spawn scripts/fork359-permission-probe.cjs, JSON-RPC initialize, tools/list, tools/call AskUserQuestion -> deny JSON and a log line with the option labels). For dry-run and preflight legs, prepend PATH with a mkdtemp dir holding a fake `claude` script that answers `--version` with `2.1.280 (Claude Code)` and otherwise appends its argv to a file and exits 1; assert that file is absent after a dry run. RED.
    3. Create `scripts/forward-fork-scenarios-359.cjs` ('use strict'; header: dev-only R9 harness, never imported by lib/ or hooks/, never in CI, spends money only on an explicit `--arm` run after navigator approval (plans 10 and 11), hyphens only). Switch-case argv: `--arm pre|post|both`, `--r8-sha <sha>` (pre = `<sha>^`, post = `<sha>`), `--pre-sha`, `--post-sha` (explicit overrides), `--mode probe|text` (default probe), `--smoke` (first fork scenario, 1 run per requested arm), `--runs <n>` (default meta.runs_per_scenario), `--only <ids>`, `--results <path>` (default RESULTS_DIR/results.jsonl), `--dry-run`, `--project --cost-per-run <usd>`, `--parse <stream.jsonl> --scenario <id> [--tree <dir>]`, `--evaluate [--mode probe|text]`. Exports `preflight, buildRunCommand, parseRun, evaluate, main`. Behavior:
       - preflight(opts): the checks listed in the must_haves; projection = planned runs x cost per run (from `--cost-per-run` or the smoke value recorded in the results file) x 1.25; refuse above `meta.caps.total_usd`.
       - Arm trees: `git archive <sha> | tar -x -C <RESULTS_DIR>/trees/<sha>` once per sha (reuse when present), `node_modules` symlinked to the repo's.
       - buildRunCommand(run): the arg array `-p --input-format stream-json --output-format stream-json --verbose --include-hook-events --plugin-dir <tree> --agent mos:larry-extended --model claude-sonnet-5 --setting-sources project,local --max-turns 12 --max-budget-usd 0.40 --no-session-persistence`, plus in probe mode `--mcp-config <tmp>/probe.json --permission-prompt-tool mcp__fork359probe__permission` where probe.json names `node <repo HEAD>/scripts/fork359-permission-probe.cjs`; cwd `<tmp>/scratch-359`; the hermetic env from the context.
       - runOne(run): spawn claude; write turn 1 as a stream-json user message; after each `result` event write the next turn; close stdin after the last result; kill after 300 s (status error_timeout); tee every event to `RESULTS_DIR/streams/<arm>/<scenario>-<run>.jsonl`; then `parseRun`.
       - parseRun(events, scenario, {tree, mode}): the behaviors above; `declared` uses `require(<tree>/lib/core/fork-declaration.cjs).parseForkDeclaration`; `card_is_mode_menu` compares the card's option labels with the three MODE_MENU labels read from `<tree>/scripts/session-start`; `blocked_by_declared_arm` = the arm tree's `classifyCardFire(deriveTurnSignals({session_id, output_text: <attempt-1 final text>, ran_entries: [], sidechannel_health: 'healthy', reach_corroborated: false}), loadRegistry()).reason === 'declared-fork-no-card'` (run in the run's hermetic env); `intercept_log_declared` = count of `declared-fork-no-card` reasons in `<tmp>/home/card-fire-intercepts.log` (or the log path the tree's check-card-fire uses under MINDRIAN_HOME); `cost_usd`, `duration_ms` from result events. Row: `{scenario_id, arm, run, sha, mode, poses_fork, card, card_is_mode_menu, card_options, declared, declared_labels, blocked_by_declared_arm, intercept_log_declared, split, status, cost_usd, duration_ms}` (labels and options only; no reply text).
       - Batch: sequential; resumable (skip a (scenario, arm, run) already `ok`; re-run an error once; a second error stays ERROR); stop when cumulative cost would exceed `total_usd - per_run_usd`; append rows to the results JSONL.
       - evaluate(rows, {mode}): per arm, over `ok` rows: fork runs; cards (non-menu); declarations; missed = fork runs with neither a non-menu card nor a declaration (text mode: without a declaration); control runs; control declared blocks = post control rows with `blocked_by_declared_arm`; floor = max(meta.vacuity_floor.min_pre_missed, ceil(min_fraction x pre fork runs)); outcome PASS when pre missed is at or above the floor, post missed is at most 0.5 x pre missed and control declared blocks is 0; INCONCLUSIVE when pre missed is below the floor; FALSIFIED otherwise; plus `hook_live_proof` = at least one post fork row with declared true, card false and intercept_log_declared > 0. Output JSON of counts and scenario ids only.
    4. GREEN: `node tests/test-359-forward-harness.cjs`. Commit: `git add scripts/forward-fork-scenarios-359.cjs tests/test-359-forward-harness.cjs tests/fixtures/forward-fork-stream-359` then `git commit --only -m "feat(359-05): dev-only forward harness, preflight caps, attempt-1 parse, floor-guarded evaluate (R9, D-12, N-2, N-4, N-5)" -- scripts/forward-fork-scenarios-359.cjs tests/test-359-forward-harness.cjs tests/fixtures/forward-fork-stream-359`.
  </action>
  <verify>
    <automated>node tests/test-359-forward-harness.cjs && node tests/test-359-inertness.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both verify commands exit 0
    - `grep -v '^\s*//' scripts/forward-fork-scenarios-359.cjs | grep -c "\-\-bare\|dangerously-skip-permissions"` is 0
    - `grep -v '^\s*//' scripts/forward-fork-scenarios-359.cjs | grep -c "max-budget-usd"` is at least 1 and `grep -c "claude-sonnet-5" scripts/forward-fork-scenarios-359.cjs` is at least 1
    - `grep -rln "forward-fork-scenarios-359\|fork359-permission-probe" lib hooks` prints nothing
    - `ls tests/fixtures/forward-fork-stream-359/*.jsonl | wc -l` is at least 9
    - `grep -c "$(printf '\xe2\x80\x94')" scripts/forward-fork-scenarios-359.cjs tests/test-359-forward-harness.cjs` reports 0 for each
  </acceptance_criteria>
  <done>The forward instrument exists and is proven offline: it refuses unsafe configurations, isolates every run, separates the first attempt from the Stop re-prompt, and turns rows into a floor-guarded PASS, INCONCLUSIVE or FALSIFIED verdict, without spending a cent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> paid model API | every run spends money; spend must be capped and gated |
| headless Larry -> tools, Brain, rooms, disk | a real plugin session could write, call Brain or touch real rooms |
| run artifacts -> git | raw streams must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-359-21 | Denial of service (financial) | forward-run spend overrun | mitigate | preflight refuses a projection over USD 60; --max-budget-usd 0.40 on every run; the batch stops before the cumulative cap; budget-hit runs are ERROR; no paid run in this plan (fake claude in tests) |
| T-359-22 | Spoofing | a shadowed or wrong Larry being measured | mitigate | preflight refuses when ~/.claude/agents/larry-extended.md resolves to a file; --agent mos:larry-extended; --setting-sources project,local; parseRun rejects runs whose init plugins are not exactly one mos at the arm tree path |
| T-359-23 | Elevation of privilege | headless Larry writing files, calling Brain or answering its own card | mitigate | deny-all permission host; cwd is a temp scratch dir with no project settings; MINDRIAN_DISABLE_AUTO_REGISTER; hermetic MINDRIAN_HOME and rooms |
| T-359-24 | Information disclosure | user, dogfood or Jev-bound text in the forward run | mitigate | authored scenarios only; TYPESAFE_API_KEY stripped from the child env; rows carry labels and counts, never reply text |
| T-359-25 | Information disclosure | raw streams committed | mitigate | streams and results live under ~/.cache/mindrian-dev/359-forward/; a test asserts the default path is outside the repo |
| T-359-26 | Repudiation | a vacuous or confounded pass | mitigate | multi-turn scenarios with the fork last; MODE_MENU cards excluded; vacuity floor; offline control-block classification plus intercept-log corroboration and hook_live_proof |
</threat_model>

<verification>
- `node tests/test-359-forward-harness.cjs && node tests/test-359-inertness.cjs` both exit 0
- `grep -rln "forward-fork-scenarios-359\|fork359-permission-probe" lib hooks` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD | grep -E '(lib/mcp/brain-router|lib/core/write-lock|lib/core/part8-egress-guard|scripts/doctor|lib/core/graph-ops|scripts/eval-icm-writers|tests/test-353-grader-agreement|tests/test-353-ledger-shape|lib/core/navigation)\.cjs$|docs/OPEN-HANDOFFS\.md$'` prints nothing
- `git diff --name-only $PLAN_BASE..HEAD -- lib hooks package.json` prints nothing
</verification>

<success_criteria>
The R9 instrument is built and proven offline with hard spend caps, a shadow-agent refusal, hermetic runs, first-attempt separation, and a vacuity-floor verdict; no money is spent in this plan.
</success_criteria>

<output>
Create `.planning/phases/359-missed-fork-detection-larry-poses-a-genuine-decision-in-pros/359-05-SUMMARY.md` (`git add -f`, then `git commit --only -- <path>`). Record: PLAN_BASE, scenario counts, the probe JSON-RPC exchange shape, the canned-stream provisional-shape note, the exact command line `buildRunCommand` produces for one probe-mode run, and the evaluate outcome table from the test.
</output>
