---
phase: 238-decision-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-all-238.sh
  - tests/helpers/cardfire-hermetic-238.cjs
  - tests/test-238-retry-counter-fence.worker.cjs
autonomous: true
requirements: [GATE-01, GATE-03, GATE-04]

must_haves:
  truths:
    - "One command (bash tests/run-all-238.sh) reports PASS/FAIL/SKIP for every Phase 238 leg, and skips cleanly for legs whose files have not landed yet"
    - "Every Phase 238 test that touches a side file can isolate MINDRIAN_HOME and CARD_FIRE_SIDECHANNEL_PATH with one helper call and restore them afterward"
    - "A forked child process can drive the real bumpRetryCount N times against an isolated MINDRIAN_HOME"
  artifacts:
    - path: "tests/run-all-238.sh"
      provides: "Phase 238 aggregator with all seven 238 legs plus two regression legs pre-declared via run_if"
      contains: "run_if"
    - path: "tests/helpers/cardfire-hermetic-238.cjs"
      provides: "makeHermeticCardFireEnv() setting and restoring MINDRIAN_HOME + CARD_FIRE_SIDECHANNEL_PATH"
      exports: ["makeHermeticCardFireEnv"]
    - path: "tests/test-238-retry-counter-fence.worker.cjs"
      provides: "Forked-child worker calling the real bumpRetryCount M times under an isolated MINDRIAN_HOME"
  key_links:
    - from: "tests/test-238-retry-counter-fence.worker.cjs"
      to: "scripts/check-card-fire.cjs"
      via: "require of the real module, resolved with path.resolve(__dirname, ...)"
      pattern: "check-card-fire"
    - from: "tests/run-all-238.sh"
      to: "tests/test-238-*.cjs"
      via: "run_if file guard per leg"
      pattern: "run_if"
---

<objective>
Build the Phase 238 validation scaffolding that every later plan depends on, before any production code changes: the phase aggregator with all legs pre-declared, the shared hermetic-isolation helper, and the forked-child worker for the GATE-03 concurrency proof.

Purpose: `.planning/config.json` sets `workflow.nyquist_validation: true`, and `238-VALIDATION.md` lists four Wave 0 gaps. Three of them land here. The fourth (the GATE-04 fixture corpus) lands in 238-07 alongside its own consuming test, because building it is substantive sanitization work rather than scaffolding; the aggregator's `run_if` guard makes that ordering safe (documented decision, see Phase decisions below).

Output: one runnable phase gate, one isolation helper, one concurrency worker. Zero production code touched.
</objective>

<phase_decisions>
Decisions this plan applies (recorded, not silently chosen):

- **D-01 (aggregator ownership):** All Phase 238 legs are pre-declared in `tests/run-all-238.sh` by THIS plan. No later-wave plan edits this runner. Source: `tests/run-all-209.sh` header lines 16-19, the in-repo precedent, plus `238-PATTERNS.md`.
- **D-07 (no unverifiable instance count):** No Phase 238 artifact asserts "eight logged over-enforcement instances". `238-RESEARCH.md` measured that this worktree is missing the two commits that would corroborate that number. The corpus is built from RCA files actually present on disk plus the researcher's own measured adversarial fixtures.
</phase_decisions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/238-decision-gates/238-VALIDATION.md
@.planning/phases/238-decision-gates/238-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols and files created across ALL of Phase 238 (the source-grounding pass must treat every one of these as new, not as drift):

**New files:**
- `lib/mcp/gate-ledger.cjs`
- `scripts/check-gate-seam.cjs`
- `tests/run-all-238.sh`
- `tests/helpers/cardfire-hermetic-238.cjs`
- `tests/test-238-session-scoped-ledger.cjs`
- `tests/test-238-chosen-validation.cjs`
- `tests/test-238-chain-chosen-validation.cjs`
- `tests/test-238-one-ledger.cjs`
- `tests/test-238-mint-ratifier-seam.cjs`
- `tests/test-238-retry-counter-fence.cjs`
- `tests/test-238-retry-counter-fence.worker.cjs`
- `tests/test-238-card-fire-corpus.cjs`
- `tests/fixtures/card-fire-corpus-238.json`

**New exported symbols:**
- `lib/mcp/gate-ledger.cjs`: `mintGate`, `consumeGate`, `ledgerSessionKey`, `mintedGateKinds`, `ratifiableGateKinds`, `LEDGER_TTL_MS`, `NO_SESSION_PREFIX`, `_internal._ledger`
- `lib/mcp/gate-render.cjs`: `validateChosenAgainstCard`
- `lib/core/card-fire-sidechannel.cjs`: `sideChannelHealth`, `HEALTH_OK`, `HEALTH_UNAVAILABLE`
- `scripts/check-card-fire.cjs`: `withRetryStoreLock`, `retryStoreLockRoot`, `RETRY_LOCK_MAX_ATTEMPTS`, `RETRY_LOCK_SLEEP_MS`
- `tests/helpers/cardfire-hermetic-238.cjs`: `makeHermeticCardFireEnv`

**New turn-signal fields (on the object `deriveTurnSignals` returns and `classifyCardFire` reads):**
- `sidechannel_health`
- `reach_corroborated`

**New reason slugs:**
- `chosen_not_in_card_options` (gate_answer and chain resume reject paths)
- `session_mismatch` (unified ledger consume reject)
- `backstop-uncorroborated-by-side-channel` (classifyCardFire non-intercept reason)

**New verify-release section:** section 18, GATE LEDGER SEAM GATE.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the hermetic card-fire isolation helper</name>
  <files>tests/helpers/cardfire-hermetic-238.cjs</files>
  <read_first>
    - `tests/test-209-incident-replay.cjs` (lines 28-39 for the `CARD_FIRE_SIDECHANNEL_PATH` hermetic block and its explanatory comment; lines 103-117 for the save/restore `MINDRIAN_HOME` pattern)
    - `lib/core/card-fire-sidechannel.cjs` (lines 116-134, `sideFilePath`, to confirm which env var it honors)
    - `scripts/check-card-fire.cjs` (lines 165-180, `retryFilePath` and `interceptLogPath`, to confirm both resolve off `MINDRIAN_HOME`)
    - `tests/helpers/fixture-room-224.cjs` (the house shape for a `tests/helpers/*.cjs` module)
  </read_first>
  <action>
Create `tests/helpers/cardfire-hermetic-238.cjs`, a CJS module exporting one function `makeHermeticCardFireEnv(label)`.

Behavior: create a fresh temp dir via `fs.mkdtempSync(path.join(os.tmpdir(), 'gsd-238-' + (label || 'cardfire') + '-'))`. Save the CURRENT values of `process.env.MINDRIAN_HOME` and `process.env.CARD_FIRE_SIDECHANNEL_PATH` (including the `undefined` case). Set `process.env.MINDRIAN_HOME` to the temp dir and `process.env.CARD_FIRE_SIDECHANNEL_PATH` to `path.join(tmp, 'card-fire-reached.json')`. Return an object `{ dir, retryFile, sideFile, restore }` where `retryFile` is `path.join(dir, 'card-fire-retries.json')`, `sideFile` is the side-channel path, and `restore()` puts both env vars back exactly (using `delete process.env.X` when the original was `undefined`) and then removes the temp dir with `fs.rmSync(dir, { recursive: true, force: true })`.

Both env vars are mandatory on every leg, not optional: `MINDRIAN_HOME` covers `card-fire-retries.json` AND `card-fire-intercepts.log`; `CARD_FIRE_SIDECHANNEL_PATH` covers `card-fire-reached.json`. Carry a header comment that states WHY both are required, naming the trap `tests/test-209-incident-replay.cjs` discovered while being written: the side-channel `NO_SESSION_KEY` union lets stale real-machine records leak into an unrelated session lookup for up to `TTL_MS` (10 minutes), and without `MINDRIAN_HOME` the GATE-03 concurrency leg would shred the navigator's live retry store.

Node built-ins only (`node:fs`, `node:os`, `node:path`). No em-dashes anywhere in the file.
  </action>
  <verify>
    <automated>node -e "const h=require('./tests/helpers/cardfire-hermetic-238.cjs'); const e=h.makeHermeticCardFireEnv('probe'); const a=process.env.MINDRIAN_HOME===e.dir; const b=process.env.CARD_FIRE_SIDECHANNEL_PATH===e.sideFile; e.restore(); const c=!require('node:fs').existsSync(e.dir); if(!(a&&b&&c)) { console.error({a,b,c}); process.exit(1);} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - The probe command above prints `ok` and exits 0.
    - `grep -c "MINDRIAN_HOME" tests/helpers/cardfire-hermetic-238.cjs` is at least 2 (set plus restore).
    - `grep -c "CARD_FIRE_SIDECHANNEL_PATH" tests/helpers/cardfire-hermetic-238.cjs` is at least 2.
    - After `restore()` in a process where `MINDRIAN_HOME` was originally unset, `('MINDRIAN_HOME' in process.env)` is `false` (not the string "undefined").
    - `grep -v '^ *[*/#]' tests/helpers/cardfire-hermetic-238.cjs | grep -cP '\\x{2014}'` is 0 (no em-dashes in code lines), and a full-file `grep -cP '\\x{2014}'` is also 0.
  </acceptance_criteria>
  <done>`makeHermeticCardFireEnv` sets both env vars, returns the three resolved paths, and `restore()` returns the process environment to its exact prior state and deletes the temp dir.</done>
</task>

<task type="auto">
  <name>Task 2: Create the forked-child worker for the GATE-03 concurrency proof</name>
  <files>tests/test-238-retry-counter-fence.worker.cjs</files>
  <read_first>
    - `lib/memory/write-lock-atomic.worker.cjs` (the full file: the standalone-file rationale, `path.resolve(__dirname, ...)` module resolution, the distinct exit codes, the argv guard)
    - `lib/memory/write-lock-atomic.test.cjs` (lines 26-77, to see how the parent passes argv and reads exit codes and stderr)
    - `scripts/check-card-fire.cjs` (lines 760-880, the retry-store accessors: `readRetryStore`, `writeRetryStore`, `bumpRetryCount`, `clearRetryCount`, `readSessionCount`, `bumpSessionCount`, and `module.exports` at 1486 to confirm which are exported)
  </read_first>
  <action>
Create `tests/test-238-retry-counter-fence.worker.cjs`, a standalone forked-child worker modelled byte-for-byte on `lib/memory/write-lock-atomic.worker.cjs`.

Contract: `argv[2]` is the isolated `MINDRIAN_HOME` directory, `argv[3]` is the counter key to bump, `argv[4]` is the number of bumps (an integer string). Guard each: on a missing or non-numeric argument write a one-line diagnostic to stderr and `process.exit(3)`.

Set `process.env.MINDRIAN_HOME` to `argv[2]` BEFORE requiring the module under test, because `retryFilePath()` reads the env var at call time but the require also pulls in `lib/core/gate-relevance.cjs` and the registry path; setting it first keeps the child hermetic in all cases. Require the module as `require(path.resolve(__dirname, '..', 'scripts', 'check-card-fire.cjs'))` (never a relative literal, because the child's cwd is not guaranteed). Call the exported `bumpRetryCount(key)` exactly `argv[4]` times in a plain loop with no sleeps between calls, then `process.exit(0)`.

Exit codes, matching the Phase 87-02 worker's contract: `0` expected success, `1` expected contention (unused by this worker but reserved so the parent's code table stays identical to the precedent), `3` unexpected argv or require failure with the reason on stderr. Wrap the bump loop in a try/catch that writes the error message to stderr and exits `3`, so a swallowed lock failure inside `bumpRetryCount` can never masquerade as a clean child.

Standalone file, never an inline `-e` template string, for the same Windows/Linux path-escape reason the precedent's own header states. Node built-ins only. No em-dashes.
  </action>
  <verify>
    <automated>node -e "const {mkdtempSync,readFileSync}=require('node:fs'),{tmpdir}=require('node:os'),p=require('node:path'),{execFileSync}=require('node:child_process'); const d=mkdtempSync(p.join(tmpdir(),'w238-')); execFileSync(process.execPath,['tests/test-238-retry-counter-fence.worker.cjs',d,'k1','5'],{stdio:'inherit'}); const s=JSON.parse(readFileSync(p.join(d,'card-fire-retries.json'),'utf8')); if(s.k1.count!==5){console.error(s);process.exit(1);} console.log('ok single-child count=5');"</automated>
  </verify>
  <acceptance_criteria>
    - The probe above prints `ok single-child count=5` and exits 0 (a single child with no contention lands exactly 5 increments).
    - Running the worker with no argv (`node tests/test-238-retry-counter-fence.worker.cjs`) exits with code 3 and writes a non-empty line to stderr.
    - Running the worker with a non-numeric bump count (`node tests/test-238-retry-counter-fence.worker.cjs /tmp k1 abc`) exits with code 3.
    - `grep -c "path.resolve(__dirname" tests/test-238-retry-counter-fence.worker.cjs` is at least 1 (no relative-literal require of the module under test).
    - The probe leaves no file at `$HOME/.mindrian/card-fire-retries.json` newer than before the run: `find "$HOME/.mindrian/card-fire-retries.json" -newermt '-1 minute'` returns empty after the probe.
    - `grep -cP '\\x{2014}' tests/test-238-retry-counter-fence.worker.cjs` is 0.
  </acceptance_criteria>
  <done>A forked child drives the real exported `bumpRetryCount` against an isolated `MINDRIAN_HOME`, lands exactly the requested number of increments when uncontended, and exits 3 on any argv or require fault instead of silently exiting 0.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Phase 238 aggregator with every leg pre-declared</name>
  <files>tests/run-all-238.sh</files>
  <read_first>
    - `tests/run-all-209.sh` (the full file: `set -uo pipefail`, the `ROOT`/`cd` preamble, `run`, `run_if`, the PASS/FAIL/SKIP tally, the final `[ "$FAIL" -eq 0 ]`, and the header comment stating the pre-declaration convention)
    - `.planning/phases/238-decision-gates/238-VALIDATION.md` (the per-task verification map, so every mapped command has a leg here)
  </read_first>
  <action>
Create `tests/run-all-238.sh` by copying `tests/run-all-209.sh`'s structure verbatim and swapping the labels. Keep `set -uo pipefail`, the `ROOT` resolution and `cd "$ROOT"`, both `run` and `run_if` functions unchanged, the `PASS`/`FAIL`/`SKIP` counters, the trailing summary banner reading `Phase 238: PASS=$PASS FAIL=$FAIL SKIP=$SKIP`, and the final `[ "$FAIL" -eq 0 ]` exit expression.

Pre-declare exactly these nine legs, all via `run_if` guarded on the file each one runs, in this order:

1. label `238-02 session-scoped ledger (GATE-03 A)`, file `tests/test-238-session-scoped-ledger.cjs`
2. label `238-03 gate_answer chosen validation (GATE-01 G-2)`, file `tests/test-238-chosen-validation.cjs`
3. label `238-04 chain resume chosen validation (GATE-01 G-2)`, file `tests/test-238-chain-chosen-validation.cjs`
4. label `238-05 retry counter fence (GATE-03 B)`, file `tests/test-238-retry-counter-fence.cjs`
5. label `238-06 one ledger end to end (GATE-01 G-1)`, file `tests/test-238-one-ledger.cjs`
6. label `238-06 mint-ratifier seam liveness (GATE-01 G-1)`, file `tests/test-238-mint-ratifier-seam.cjs`
7. label `238-07/08 card-fire corpus (GATE-04)`, file `tests/test-238-card-fire-corpus.cjs`
8. label `regression: 209 backstop tuning (ASCII_BOX_GLYPH_RE stability)`, file `tests/test-209-backstop-tuning.cjs`
9. label `regression: 198 chain run halt`, file `tests/test-198-chain-run-halt.test.cjs`

Each leg's command is `node <file>`. Legs 8 and 9 are regressions this phase can break and are deliberately inside this runner so one command covers them.

Write a header comment block, in the same shape as `run-all-209.sh`'s, that states: which requirement each leg belongs to; that all nine legs are pre-declared here by plan 01 so no later-wave plan ever edits this runner; that legs will SKIP until their files land, which is expected mid-phase; and that leg 7 is expected to FAIL between 238-07 (which creates the corpus and its test) and 238-08 (which lands the classifier fix), because the corpus test is authored to be observed RED before the fix, matching the Phase 236-01 precedent. bash only. No em-dashes.

Make the file executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash tests/run-all-238.sh; echo "exit=$?"; bash -n tests/run-all-238.sh && echo "syntax ok"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/run-all-238.sh` exits 0 (valid bash).
    - Running `bash tests/run-all-238.sh` at this point in the phase prints a summary line matching `Phase 238: PASS=` and reports `SKIP=7` with `FAIL=0` (the seven 238 legs are missing; the two regression legs exist and pass).
    - `grep -c 'run_if' tests/run-all-238.sh` is at least 9.
    - `grep -c 'tests/test-238-card-fire-corpus.cjs' tests/run-all-238.sh` is at least 1, proving the corpus leg is pre-declared here even though its file lands in 238-07.
    - `test -x tests/run-all-238.sh` succeeds.
    - `grep -cP '\\x{2014}' tests/run-all-238.sh` is 0.
  </acceptance_criteria>
  <done>One command is the Phase 238 gate. All nine legs are declared, missing files SKIP instead of FAIL, and no later plan needs to edit this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process to user home | A test that does not isolate `MINDRIAN_HOME` writes into the navigator's live `~/.mindrian/` counters and diagnostic log |
| forked child to parent | The child drives real production accessors; a swallowed fault would report success to the parent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-238-01 | Tampering | `~/.mindrian/card-fire-retries.json`, `~/.mindrian/card-fire-reached.json` | mitigate | `makeHermeticCardFireEnv` sets BOTH `MINDRIAN_HOME` and `CARD_FIRE_SIDECHANNEL_PATH` to a fresh mkdtemp and restores them; every 238 leg is required to call it. Acceptance criterion asserts no fresh write to the real retry file. |
| T-238-02 | Repudiation | `tests/test-238-retry-counter-fence.worker.cjs` | mitigate | Distinct exit code 3 on argv or require fault, with the reason on stderr, so a child that never ran cannot exit 0 and be counted as a successful writer. |
| T-238-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages. `238-RESEARCH.md`'s Package Legitimacy Audit records "Not applicable"; every primitive is a Node built-in or already vendored. If any task proposes a new dependency, that alone is a signal it left the reuse-before-build lane and the legitimacy gate protocol applies before any install. |
</threat_model>

<out_of_scope>
Zero reads and zero writes under `.planning/phases/236-room-db-data-loss-fixes/`, `lib/core/lazygraph-ops.cjs`, `scripts/build-ecosystem-graph.cjs`, `tests/test-236-*`, `tests/helpers/fixture-room-236.cjs`. Zero writes under `.planning/phases/237-reach-mechanism/`. No production source file is touched by this plan.
</out_of_scope>

<verification>
- `bash -n tests/run-all-238.sh` exits 0.
- `bash tests/run-all-238.sh` exits 0 with `FAIL=0` and `SKIP=7`.
- `node tests/test-209-backstop-tuning.cjs` still passes (it is now a leg of this runner).
- `git diff --stat lib/ scripts/` is empty for this plan's commits (no production file touched).
</verification>

<success_criteria>
Three scaffolding artifacts exist and are proven working: the aggregator runs and skips cleanly, the hermetic helper sets and restores both env vars, and the worker lands exactly N increments as a single uncontended child. No production code changed.
</success_criteria>

<output>
Create `.planning/phases/238-decision-gates/238-01-SUMMARY.md` when done. Note: `.planning/` is gitignored, so commit the SUMMARY with `git add -f`.
</output>

