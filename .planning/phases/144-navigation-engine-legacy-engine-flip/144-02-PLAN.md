---
phase: 144-navigation-engine-legacy-engine-flip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/memory/skill-activation-router.test.cjs
autonomous: true
requirements: [NAV-01]
canon_parts: [Part 3, Part 9]

must_haves:
  truths:
    - "Tests 16/17 go GREEN: the populated-room fixture resolves through resolve-active-room.cjs so the engine block at intent-classifier.cjs:1369 runs and emits routing_source (ROADMAP SC2)"
    - "Test 16 (MOS_NAV_TEST_FIRE_SKILL='Run Methodology') asserts routing_source: engine through the EXISTING router with zero sensor code (proves the flip path)"
    - "A cold-room honest-negative assertion proves a legacy trace is correct, not a failure (ROADMAP SC3)"
  artifacts:
    - path: "lib/memory/skill-activation-router.test.cjs"
      provides: "makeRoomsFixture registry.json fixed to the {slug, abs_path} object shape + a cold-room honest-negative test"
      contains: "abs_path"
  key_links:
    - from: "lib/memory/skill-activation-router.test.cjs::makeRoomsFixture"
      to: "lib/core/resolve-active-room.cjs Array/Object branch"
      via: "registry.rooms carries {slug, abs_path} so resolveActiveRoom returns non-null"
      pattern: "abs_path"
---

<objective>
The fixture REPAIR. Tests 16/17 in `lib/memory/skill-activation-router.test.cjs` FAIL TODAY (15/17) -- not because of missing sensor code, but because `makeRoomsFixture` writes `.rooms/registry.json` with `rooms: ['fixture-room']` (array of bare STRINGS). Phase 127.3 routed `resolveActiveRoomDir` through `lib/core/resolve-active-room.cjs`, whose Array branch matches `r.slug === slug || r.name === slug`; a bare string has neither, so `resolveActiveRoom` returns null, the engine block at `intent-classifier.cjs:1369 (if (roomDir))` is skipped, stdout is empty, and the assertions fail.

The fan-out PROVED that patching the fixture registry to the `{slug, abs_path}` object shape makes `routing_source: engine` emit IMMEDIATELY through the EXISTING router with NO sensor code. This plan does exactly that one repair plus a cold-room honest-negative assertion.

Purpose: NAV-01. This is the single highest-leverage correction -- it proves the flip path (router contract) is sound end-to-end, independent of Plan 01's sensor wiring.

Output: a corrected `makeRoomsFixture` and a new cold-room negative test, turning the suite to 18/18 (16/17 GREEN + 1 new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-navigation-engine-legacy-engine-flip/144-FANOUT-CORRECTIONS.md
@./CLAUDE.md

<interfaces>
From lib/core/resolve-active-room.cjs (the resolver the fixture must satisfy):
  resolveActiveRoom({home?}) -> { slug, abs_path } | null
  // Reads MINDRIAN_ROOMS_HOME/.rooms/registry.json.
  // active slug = reg.active (current) or reg.active_room (legacy).
  // rooms ARRAY branch: entry = reg.rooms.find(r => r.slug===slug || r.name===slug)
  // rooms OBJECT branch: entry = reg.rooms[slug]
  // abs_path = entry.abs_path -> else entry.path (resolved vs home) -> else <home>/<slug>
  // Final gate: fs.existsSync(abs_path) -- a registry pointing at a missing dir returns null.

From lib/memory/skill-activation-router.test.cjs (the bug + the harness):
  makeRoomsFixture(label) writes registry.json at line ~333-336 as
    { active: 'fixture-room', rooms: ['fixture-room'] }   <-- BUG (array of strings)
  runHook sets MINDRIAN_ROOMS_HOME = fix.root (so home/.rooms/registry.json is read).
  Test 16 (line 363) expects 'routing_source: engine' given MOS_NAV_TEST_FIRE_SKILL='Run Methodology'.
  Test 17 (line 391) expects 'routing_source: legacy' given MOS_NAV_TEST_FIRE_SKILL='__NULL__'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix makeRoomsFixture registry to the {slug, abs_path} object shape so Tests 16/17 go GREEN</name>
  <read_first>
    - lib/memory/skill-activation-router.test.cjs lines 308-338 (makeRoomsFixture; the line-335 registry write is the bug)
    - lib/memory/skill-activation-router.test.cjs lines 340-352 (runHook -- sets MINDRIAN_ROOMS_HOME=root)
    - lib/memory/skill-activation-router.test.cjs lines 362-408 (Tests 16/17 assertions)
    - lib/core/resolve-active-room.cjs lines 120-160 (the slug + rooms Array/Object branch + abs_path resolution + fs.existsSync gate)
  </read_first>
  <acceptance_criteria>
    - makeRoomsFixture writes registry.json so resolveActiveRoom (reading MINDRIAN_ROOMS_HOME=fix.root) returns { slug:'fixture-room', abs_path:<fix.roomDir> }. Either the Array form rooms:[{ slug:'fixture-room', abs_path: roomDir }] OR the Object form rooms:{ 'fixture-room': { abs_path: roomDir } } satisfies the resolver; the absolute roomDir already exists on disk so the fs.existsSync gate passes.
    - Test 16 passes: stdout contains 'activated_skills: [Run Methodology]' AND 'routing_source: engine'.
    - Test 17 passes: stdout contains 'routing_source: legacy'.
    - The full suite reports 16/17 prior tests GREEN (no regression to Tests 1-15) -- i.e. all pre-existing tests still pass.
  </acceptance_criteria>
  <action>
    In makeRoomsFixture, replace the registry write at line ~333-336 so registry.rooms carries the {slug, abs_path} OBJECT shape that resolve-active-room.cjs resolves: set abs_path to the fixture's absolute roomDir (path.join(root,'fixture-room'), which the fixture already creates on disk). Keep active:'fixture-room'. Do NOT touch intent-classifier.cjs, navigation-engine.cjs, the router, or any sensor file -- this is a TEST-FIXTURE-ONLY repair. Run the suite and confirm Tests 16/17 flip from RED to GREEN with zero sensor code present.
  </action>
  <verify>
    <automated>node lib/memory/skill-activation-router.test.cjs</automated>
  </verify>
  <done>node lib/memory/skill-activation-router.test.cjs reports all pre-existing tests GREEN including Test 16 (routing_source: engine) and Test 17 (routing_source: legacy); only lib/memory/skill-activation-router.test.cjs changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add the cold-room honest-negative assertion (legacy stays legacy)</name>
  <read_first>
    - lib/memory/skill-activation-router.test.cjs lines 340-413 (runHook + Tests 16/17 + the classifierIntegrated gate)
    - .planning/phases/144-navigation-engine-legacy-engine-flip/144-FANOUT-CORRECTIONS.md (NAV-01 bar: cold-room legacy is the honest negative, not a failure)
  </read_first>
  <acceptance_criteria>
    - A new Test (Test 18) builds a COLD-room fixture (a resolvable room via the fixed makeRoomsFixture, but with NO engine fire stub set -- MOS_NAV_TEST_FIRE_SKILL='__NULL__' and no sensor signal) and asserts stdout contains 'routing_source: legacy'.
    - The test documents in a comment that a cold-room legacy trace is the CANONICAL honest negative for NAV-01 (a legacy trace is correct, not a failure), distinguishing it from Test 17 (which forces null via the stub).
    - The new test runs inside the existing classifierIntegrated() gate so it skips cleanly when the router integration is absent.
  </acceptance_criteria>
  <action>
    Add Test 18 alongside Tests 16/17 (inside the classifierIntegrated() block). Use makeRoomsFixture to build a resolvable room, run the hook with MOS_NAV_TEST_FIRE_SKILL='__NULL__' (engine silent) and no sensor signal, and assert 'routing_source: legacy' is present -- the honest negative. Add a comment naming this the NAV-01 cold-room honest-negative case. Clean up the tmp fixture in finally, mirroring Tests 16/17.
  </action>
  <verify>
    <automated>node lib/memory/skill-activation-router.test.cjs</automated>
  </verify>
  <done>node lib/memory/skill-activation-router.test.cjs passes with the new cold-room honest-negative test GREEN; the suite is 18/18; only lib/memory/skill-activation-router.test.cjs changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> resolver | The fixture registry must match the resolve-active-room.cjs contract; no production code path changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-06 | Tampering | router/production READ-ONLY | mitigate | repair is TEST-FIXTURE-ONLY; no edit to intent-classifier.cjs, navigation-engine.cjs, the router, or any sensor file |
| T-144-07 | Repudiation | false-green fixture | mitigate | the fix makes a previously-RED contract pass for the RIGHT reason (resolver resolves the room); Test 16 still requires the canonical-verb stub to flip, Test 17/18 still require silence to stay legacy |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | no package install in this plan |
</threat_model>

<verification>
- node lib/memory/skill-activation-router.test.cjs -> 18/18 GREEN (Tests 16/17 flipped from RED; Test 18 new)
- git diff --stat shows ONLY lib/memory/skill-activation-router.test.cjs changed
</verification>

<success_criteria>
- Tests 16/17 GREEN through the EXISTING router with zero sensor code (proves the flip path)
- Test 16 asserts routing_source: engine; Test 17 asserts routing_source: legacy
- A cold-room honest-negative test proves legacy is correct, not a failure
- No production file touched
</success_criteria>

<output>
Create `.planning/phases/144-navigation-engine-legacy-engine-flip/144-02-SUMMARY.md` when done
</output>
