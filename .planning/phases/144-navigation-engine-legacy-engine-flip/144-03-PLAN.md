---
phase: 144-navigation-engine-legacy-engine-flip
plan: 03
type: execute
wave: 2
depends_on: ["144-01", "144-02"]
files_modified:
  - tests/run-all-144.sh
  - tests/test-nav01-populated-room-engine-fires.cjs
autonomous: true
requirements: [NAV-01]
canon_parts: [Part 3, Part 8, Part 9]

must_haves:
  truths:
    - "A populated-room turn driven by a REAL fired sensor (not the MOS_NAV_TEST_FIRE_SKILL stub) produces routing_source: engine in the trace (ROADMAP SC2 + SC3)"
    - "A cold-room turn legitimately produces routing_source: legacy -- the honest negative (ROADMAP SC3)"
    - "All fence reruns stay GREEN: test-sensors-routing-fence.cjs + test-decide-part8-invariant.cjs + skill-activation-router.test.cjs Tests 1-18 (ROADMAP SC1)"
    - "The positive case is structured so Phase 146 ACPT-01 composes it without rework"
  artifacts:
    - path: "tests/run-all-144.sh"
      provides: "The Phase 144 scoped aggregator mirroring run-all-143.sh: populated engine positive + cold legacy negative + all fence reruns"
      contains: "run-all"
    - path: "tests/test-nav01-populated-room-engine-fires.cjs"
      provides: "The NAV-01 positive acceptance suite: a REAL fired sensor (first_material/artifact_filed) -> routing_source: engine end-to-end; cold-room -> legacy"
      contains: "routing_source"
  key_links:
    - from: "tests/run-all-144.sh"
      to: "tests/test-nav01-populated-room-engine-fires.cjs + the fence suites"
      via: "CJS_SUITES aggregation mirroring run-all-143.sh"
      pattern: "test-nav01-populated-room-engine-fires"
    - from: "tests/test-nav01-populated-room-engine-fires.cjs"
      to: "lib/core/navigation-engine.cjs::decide() sensor branch (Plan 01)"
      via: "a real sensor signal in the turn -> non-null fire_skill -> router flips to engine"
      pattern: "routing_source: engine"
---

<objective>
The acceptance HARNESS. Build `tests/run-all-144.sh` mirroring `tests/run-all-143.sh`, aggregating the Phase 144 acceptance proof: (a) a POPULATED-room POSITIVE case where `routing_source: engine` is produced via a REAL fired sensor (Plan 01's wiring), not merely the `MOS_NAV_TEST_FIRE_SKILL` stub; (b) a COLD-room NEGATIVE case producing `legacy` (the honest negative); (c) reruns of the three fences -- `test-sensors-routing-fence.cjs`, `test-decide-part8-invariant.cjs`, and `skill-activation-router.test.cjs` Tests 1-18 (now all green via Plan 02). The positive case is structured so Phase 146 ACPT-01 composes it.

Purpose: NAV-01 acceptance. SEED-008 calls this Phase 94-03's long-unmet criterion. 144 makes the flip POSSIBLE; this harness PROVES it; 146 reuses it.

Output: `tests/run-all-144.sh` + `tests/test-nav01-populated-room-engine-fires.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-navigation-engine-legacy-engine-flip/144-FANOUT-CORRECTIONS.md
@.planning/phases/144-navigation-engine-legacy-engine-flip/144-01-PLAN.md
@.planning/phases/144-navigation-engine-legacy-engine-flip/144-02-PLAN.md
@tests/run-all-143.sh
@./CLAUDE.md

<interfaces>
From tests/run-all-143.sh (the aggregator pattern to mirror EXACTLY):
  set -uo pipefail; SCRIPT_DIR resolution; SHELL_SUITES[] + CJS_SUITES[] arrays;
  per-suite PASS/FAIL loop (bash for SHELL_SUITES, node for CJS_SUITES);
  MISSING handling; runs to completion even on failure; exits non-zero if any failed.
  bash only, no emoji, no em-dashes.

From Plan 01 (lib/core/navigation-engine.cjs::decide() sensor branch):
  A turn { signals:['first_material'] } fires SENS-01 -> fire_skill 'Run Methodology'.
  A turn { signals:['artifact_filed'] } over a .mindrian/last-cascade.json CONTRADICT
  finding fires SENS-06 -> fire_skill "Devil's Advocate".
  decide() reads tuple/ctx from context; sensors fire in ANY tier.

From scripts/intent-classifier.cjs (the end-to-end emit path):
  resolveActiveRoomDir() (line 1368) -> if (roomDir) the engine block runs.
  deriveConversationSeed builds turn.userText (LOCAL seed lane).
  The turn's signals must arrive on the path decide() consumes -- thread them the way
  the classifier builds `turn`/`context` (the real sensor signal source, NOT the stub).
  Output lines: 'activated_skills: [...]' + 'routing_source: <source>' (line 825-826).
  Stub: MOS_NAV_TEST_FIRE_SKILL is the env override -- the POSITIVE case must NOT use it.

From lib/core/resolve-active-room.cjs: the {slug, abs_path} registry shape the fixture needs
  (reuse the Plan-02 corrected makeRoomsFixture pattern: rooms carries {slug, abs_path}).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the NAV-01 positive + negative acceptance suite driven by a REAL fired sensor</name>
  <read_first>
    - lib/memory/skill-activation-router.test.cjs lines 308-352 (the corrected makeRoomsFixture {slug,abs_path} pattern + runHook with MINDRIAN_ROOMS_HOME)
    - scripts/intent-classifier.cjs lines 1236-1294 (how turn/context is built; where a real sensor signal must be threaded; the stub injection at 1291-1294 that the POSITIVE case must avoid)
    - scripts/intent-classifier.cjs lines 1361-1390 (resolveActiveRoomDir -> emitEngineDecisionBlock -> router -> routing.source)
    - scripts/intent-classifier.cjs lines 816-826 (the 'activated_skills' + 'routing_source' output lines)
    - lib/core/insight-sensors.cjs lines 111-195 (SENS-01 first_material + SENS-06 artifact_filed signal shapes -- the REAL sensor triggers; SENS-06 reads <roomDir>/.mindrian/last-cascade.json)
    - .planning/phases/144-navigation-engine-legacy-engine-flip/144-FANOUT-CORRECTIONS.md (NAV-01 bar: >=1 engine trace per session in a POPULATED room; cold-room legacy = honest negative)
  </read_first>
  <acceptance_criteria>
    - The POSITIVE test builds a populated-room fixture (corrected {slug, abs_path} registry) whose state makes a REAL sensor fire (e.g. write <roomDir>/.mindrian/last-cascade.json with a CONTRADICT newFinding so SENS-06 fires through decide()'s Plan-01 sensor branch), runs the classifier hook WITHOUT MOS_NAV_TEST_FIRE_SKILL, and asserts stdout contains 'routing_source: engine'. The flip is driven by the real fired reach, not the stub.
    - If threading a real sensor signal end-to-end through the classifier hot path is not reachable from the hook input alone, the test asserts the flip at the decide() boundary directly: call lib/core/navigation-engine.cjs decide() with a turn carrying the real sensor signal + the populated-room context, assert decision.fire_skill is a canonical verb, then feed it to lib/core/skill-activation-router.cjs routeActivation and assert source==='engine'. Document which path was used and why (the hot-path tuple-threading caveat the fan-out flagged).
    - The NEGATIVE test builds a cold-room fixture (resolvable room, NO sensor signal, brain null/tier_0) and asserts 'routing_source: legacy' -- the honest negative.
    - The suite NEVER sets MOS_NAV_TEST_FIRE_SKILL on the positive case (that would prove the stub, not the sensor).
    - The suite is named and structured so Phase 146 ACPT-01 can require it as-is (a single node-runnable file with a clear pass/fail exit code, tmp fixtures cleaned in finally).
  </acceptance_criteria>
  <action>
    Create tests/test-nav01-populated-room-engine-fires.cjs. Reuse the Plan-02 corrected makeRoomsFixture {slug, abs_path} pattern (copy it locally or require a shared helper if one exists). POSITIVE: make a real sensor fire (write the SENS-06 CONTRADICT side-channel at <roomDir>/.mindrian/last-cascade.json, or trigger SENS-01 via first_material) and assert routing_source: engine WITHOUT the stub -- end-to-end through the classifier hook if the signal threads, else at the decide()->routeActivation boundary (document the choice per the fan-out's hot-path tuple caveat). NEGATIVE: cold room, no signal, assert routing_source: legacy. Clean tmp fixtures in finally. Exit non-zero on any assertion failure. bash/node house rules: hyphens only, no em-dashes.
  </action>
  <verify>
    <automated>node tests/test-nav01-populated-room-engine-fires.cjs</automated>
  </verify>
  <done>node tests/test-nav01-populated-room-engine-fires.cjs passes: a REAL fired sensor yields routing_source: engine (no stub), a cold room yields routing_source: legacy; tmp fixtures cleaned; file is a single node-runnable acceptance suite.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/run-all-144.sh mirroring run-all-143.sh, aggregating the positive + negative + fence reruns</name>
  <read_first>
    - tests/run-all-143.sh (the FULL aggregator pattern: set -uo pipefail, SCRIPT_DIR, SHELL_SUITES/CJS_SUITES arrays, the PASS/FAIL loop, MISSING handling, the summary block, exit-code semantics)
    - tests/test-nav01-populated-room-engine-fires.cjs (Task 1 -- the positive+negative suite to register)
    - tests/test-decide-sensor-fire.cjs (Plan 01 -- the sensor-fire suite to register)
    - tests/test-sensors-routing-fence.cjs + tests/test-decide-part8-invariant.cjs (the fences to rerun)
    - lib/memory/skill-activation-router.test.cjs (Tests 1-18; CJS_SUITES entries may be '../lib/...' per the 143 runner's relative-path note)
  </read_first>
  <acceptance_criteria>
    - tests/run-all-144.sh mirrors run-all-143.sh structurally (set -uo pipefail, SCRIPT_DIR resolution, SHELL_SUITES[]+CJS_SUITES[], the per-suite bash/node PASS-FAIL loop with MISSING handling, runs to completion on failure, prints a per-suite line + summary, exits non-zero if any failed). Header comment names per-suite plan ownership like the 143 header does.
    - CJS_SUITES aggregates AT LEAST: test-nav01-populated-room-engine-fires.cjs (positive+negative), test-decide-sensor-fire.cjs (Plan 01), test-sensors-routing-fence.cjs, test-decide-part8-invariant.cjs, and ../lib/memory/skill-activation-router.test.cjs (Tests 1-18).
    - Running bash tests/run-all-144.sh exits 0 with every registered suite PASS once Plans 01 + 02 are merged.
    - The runner is bash-only, no emoji, no em-dashes.
  </acceptance_criteria>
  <action>
    Create tests/run-all-144.sh by mirroring tests/run-all-143.sh exactly (copy the skeleton: shebang, set -uo pipefail, SCRIPT_DIR, the SHELL_SUITES/CJS_SUITES arrays, the two for-loops with MISSING handling and PASS/FAIL accounting, the summary block, the exit-code logic). Populate CJS_SUITES with the NAV-01 positive+negative suite, the Plan-01 sensor-fire suite, the two fences, and ../lib/memory/skill-activation-router.test.cjs. Write a header comment naming per-suite plan ownership (mirroring the 143 header). Run it and confirm exit 0.
  </action>
  <verify>
    <automated>bash tests/run-all-144.sh</automated>
  </verify>
  <done>bash tests/run-all-144.sh runs to completion and exits 0 with the NAV-01 positive (engine via real sensor) + negative (cold legacy) + all three fence reruns + the 18/18 router suite all PASS; structure mirrors run-all-143.sh; bash-only, no em-dashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acceptance harness -> production | The harness only READS production behavior; it must not weaken any fence or use the stub to fake the positive case. |
| positive case -> stub temptation | Using MOS_NAV_TEST_FIRE_SKILL on the positive case would prove the stub, not the sensor -- a false green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-08 | Repudiation | false-green positive | mitigate | the positive case fires a REAL sensor (no MOS_NAV_TEST_FIRE_SKILL); the done-criteria forbid the stub on the positive path |
| T-144-09 | Tampering | fence reruns | mitigate | run-all-144.sh reruns test-sensors-routing-fence + test-decide-part8-invariant + the 18/18 router suite unchanged; any regression fails the aggregator |
| T-144-10 | Information Disclosure | acceptance fixtures | mitigate | fixtures are LOCAL tmp dirs; no Brain call; SENS-06 side-channel is LOCAL JSON only |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | no package install in this plan |
</threat_model>

<verification>
- node tests/test-nav01-populated-room-engine-fires.cjs (positive engine via real sensor + negative cold legacy)
- bash tests/run-all-144.sh exits 0 with every suite PASS
- The 18/18 router suite, the sensor-routing fence, and the part8 invariant all rerun GREEN inside the aggregator
- Structure diff vs run-all-143.sh confirms the mirror (same skeleton, 144-scoped suites)
</verification>

<success_criteria>
- A populated-room turn driven by a REAL fired sensor produces routing_source: engine (no stub)
- A cold-room turn produces routing_source: legacy (honest negative)
- All fence reruns stay GREEN inside run-all-144.sh
- The positive case is a single node-runnable suite Phase 146 ACPT-01 can compose as-is
</success_criteria>

<output>
Create `.planning/phases/144-navigation-engine-legacy-engine-flip/144-03-SUMMARY.md` when done
</output>
