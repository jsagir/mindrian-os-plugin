---
phase: 144-navigation-engine-legacy-engine-flip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/navigation-engine.cjs
  - tests/test-decide-sensor-fire.cjs
autonomous: true
requirements: [NAV-01]
canon_parts: [Part 2, Part 3, Part 8, Part 9]

must_haves:
  truths:
    - "decide() reads the local graph + BRAIN.md + the trigger map -- a code path exists from each input into the decision (ROADMAP SC1)"
    - "A turn that fires a sensor reach produces a non-null fire_skill carrying a canonical verb, so the router flips routing_source to engine (ROADMAP SC2)"
    - "Sensors fire in ANY tier (tier_0 / mode_b included) -- the legacy-on-every-turn behavior is gone when a reach fires (ROADMAP SC3)"
    - "The sensor-source rationale names reach_id + posture only, never the sensor's internal detection string or any user content (Part 8)"
    - "trace.context_assembly + temporal scalars + latency telemetry are LOCAL trace JSON only -- nothing reaches the Brain (Part 8)"
  artifacts:
    - path: "lib/core/navigation-engine.cjs"
      provides: "dispatchSensors wired into decide(); resolveFireSkill 4-arg sensor branch; reachIdToSkillFamily table; Zep-shaped LOCAL trace.context_assembly + temporal + latency telemetry; line-537 fence comment flipped"
      contains: "reachIdToSkillFamily"
    - path: "tests/test-decide-sensor-fire.cjs"
      provides: "Acceptance suite: a fired sensor reach -> non-null fire_skill carrying a canonical verb, in tier_0 and mode_a; multi-sensor precedence; sensor-source rationale Part-8 clean; context_assembly LOCAL shape"
      contains: "reach_id"
  key_links:
    - from: "lib/core/navigation-engine.cjs::decide()"
      to: "lib/core/insight-sensors.cjs::dispatchSensors"
      via: "module-top require + a single dispatchSensors(turn, tuple, ctx) call before resolveFireSkill"
      pattern: "dispatchSensors\\("
    - from: "lib/core/navigation-engine.cjs::resolveFireSkill"
      to: "lib/core/skill-activation-router.cjs::routeActivation Precedence Rule 1"
      via: "resolveFireSkill returns a canonical verb (validateVerb===true) so the router flips source to engine"
      pattern: "reachIdToSkillFamily"
---

<objective>
The ONE genuine BUILD of Phase 144. Wire the shipped Phase 143 sensors into the shipped Phase 91 navigation engine `decide()` so a turn that should fire a reach produces a non-null `fire_skill` carrying a canonical verb. The router (READ-ONLY for 144) flips `routing_source: legacy -> engine` as a pure consequence of that non-null verb (its Precedence Rule 1 already does this -- confirmed by the fan-out and by router Tests 1/8/9).

Purpose: NAV-01. The single change SEED-008 says flips routing_source. Every input the engine needs already shipped (navigatedNeighborhood from CASC-02, brain_md_tier_mode from NAV-02); the gap is that the 143 sensors are NOT consumed and `resolveFireSkill` never returns a router-valid verb in real tier_0/mode_b turns.

Output: an extended `lib/core/navigation-engine.cjs` (sensor consumption + 5-reach mapping + documented multi-sensor precedence + Zep-shaped LOCAL trace + LOCAL latency telemetry + flipped line-537 fence comment) and a new acceptance suite `tests/test-decide-sensor-fire.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-navigation-engine-legacy-engine-flip/144-FANOUT-CORRECTIONS.md
@.planning/phases/144-navigation-engine-legacy-engine-flip/144-CONTEXT.md
@docs/MINDRIAN-CANON.md
@./CLAUDE.md

<critical_router_contract>
The router validates `fire_skill` against the 10 CANONICAL_VERBS, NOT against
skill-family slugs. Proof: lib/core/skill-activation-router.cjs::validateVerb
checks shared.CANONICAL_VERBS; router test Test 9 accepts all 10 canonical verbs,
Test 8 rejects 'Invent New Verb' to legacy.

Therefore the fan-out phrase "map the 5 REACH_IDS to the closed 10-verb skill
families" RESOLVES to: reachIdToSkillFamily() must return a CANONICAL VERB (one
of the 10), so the router's Precedence Rule 1 (validateVerb===true) flips to
engine. The pre-existing resolveFireSkill returns family slugs ('soft-systems',
'context-engine', 'methodology-router') that would FAIL validateVerb today -- that
is the latent reason the engine never flips even on a non-null return. The sensor
branch you add MUST return a canonical verb. Do NOT change the existing wicked /
mode_a return values in a way that regresses test-decide-part8-invariant or the
shipped 91 suites; ADD the sensor branch ahead of them and have IT return a verb.
</critical_router_contract>

<interfaces>
From lib/core/sensors/sensor-types.cjs (the frozen 5 reach ids, canonical order):
  REACH_IDS = ['context_block','contradiction','cross_room','brain_consult','deep_research']
  POSTURE_IDS = ['push_forward','hold','pull_back']

From lib/core/insight-sensors.cjs:
  dispatchSensors(turn, tuple, ctx) -> Array<reach>
  // reach = { reach_id, posture, dispatch, companions[], signal, evidence{} }
  // tuple = { problem_type, complexity, stage }  (the /mos:diagnose classification)
  // ctx   = LOCAL handles { roomDir, lowFillSections }
  // Pure / sync / LOCAL-first. Never throws on malformed input.

From lib/core/navigation-engine-shared.cjs:
  CANONICAL_VERBS (frozen 10): 'Run Methodology','Reformulate','Spawn Sub-Agent',
    'Navigate Graph',"Devil's Advocate",'Scenario Plan','Synthesize',
    'Bank Opportunity','Defer','Free-Text'

From lib/core/navigation-engine.cjs (the wiring site, current shape):
  resolveFireSkill(brain, weightApplied, tierMode) -> verb|family|null  (line ~273)
  verbToSkillFamily(verb) -> family-slug  (line ~303-319; mirror its switch shape)
  decide(turn, context): tierMode resolved ~449; navigatedNeighborhood built ~403-411;
    trace.navigated_neighborhood set ~538 with the line-537 Phase-144 fence comment;
    rationale composed ~540-589; decision.fire_skill = resolveFireSkill(...) ~592.
  Constants: WICKED_ESCALATION_THRESHOLD=8 (line 63); RECOMMENDED_CONFIDENCE_FLOOR=0.7.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire dispatchSensors into decide() and add the 5-reach -> canonical-verb mapping with documented multi-sensor precedence</name>
  <read_first>
    - lib/core/navigation-engine.cjs lines 273-319 (resolveFireSkill + verbToSkillFamily switch shape to mirror)
    - lib/core/navigation-engine.cjs lines 391-449 (decide() entry, ctx/turn defaults, tierMode resolve at ~449)
    - lib/core/navigation-engine.cjs lines 528-596 (rationale composition, line-537 fence comment, line-592 fire_skill assign)
    - lib/core/insight-sensors.cjs lines 214-245 (dispatchSensors signature + soft-fail contract)
    - lib/core/sensors/sensor-types.cjs lines 38-52 (the frozen REACH_IDS canonical order + POSTURE_IDS)
    - lib/core/skill-activation-router.cjs lines 79-105, 223-251 (validateVerb against CANONICAL_VERBS + Precedence Rule 1 -- READ-ONLY, do not edit)
    - lib/core/navigation-engine-shared.cjs lines 66-75 (CANONICAL_VERBS)
    - tests/test-sensors-routing-fence.cjs (the sensor-module fence that must stay green -- you may NOT edit any lib/core/sensors/* file)
  </read_first>
  <behavior>
    - dispatchSensors is require'd at navigation-engine.cjs module top (alongside the existing requires); decide() calls it ONCE with (turn, tuple, ctx), where tuple is read from context (problem_type/complexity/stage, defaulting to {} when absent) and ctx is { roomDir, lowFillSections } read from context (both LOCAL handles, null/empty when absent). The call lands AFTER tierMode resolve (~449) and BEFORE the line-592 resolveFireSkill call.
    - reachIdToSkillFamily(reachId) is a closed switch keyed on EXACTLY the 5 frozen REACH_IDS and returning a CANONICAL VERB: context_block -> 'Run Methodology'; contradiction -> "Devil's Advocate"; cross_room -> 'Navigate Graph'; brain_consult -> 'Run Methodology'; deep_research -> 'Spawn Sub-Agent'. Any non-member key returns null. (Verbs chosen so validateVerb===true at the router -> the flip fires.)
    - resolveFireSkill signature extends to resolveFireSkill(brain, weightApplied, tierMode, sensorReaches). New precedence INSIDE it, documented in a header comment: (1) wicked_escalation (wicked_score >= WICKED_ESCALATION_THRESHOLD) returns the existing soft-systems family FIRST; (2) ELSE if sensorReaches is non-empty, the TOP reach (by canonical REACH_IDS order) maps via reachIdToSkillFamily to a canonical verb and returns it -- this fires in ANY tier (tier_0 / mode_b included), NOT gated on mode_a; (3) ELSE the existing mode_a BRAIN pattern_matches verb path (unchanged); (4) ELSE the existing weightApplied>=0.9 context-engine fallback (unchanged). The multi-sensor tie-break (wicked > top reach by canonical REACH_IDS order > BRAIN verb > weightApplied>=0.9 fallback) is written as a comment block above the function.
    - decide() passes sensorReaches into resolveFireSkill at the line-592 call site.
    - When a sensor reach drove the fire_skill, a clause is appended to chosen_rationale naming reach_id + posture ONLY (e.g. "Sensor reach fired: contradiction/pull_back."). It NEVER includes reach.signal's user-derived value, reach.evidence bodies, reach.dispatch beyond the generic handle, or any user content (Part 8). The clause is added to the rationale array near the existing wicked/neighborhood clauses (~569-587).
    - The line-537 Phase-144 fence comment is updated from "routing_source is NOT touched (Phase 144 fence)" to "routing_source is NOT assigned here; flipped at the router layer when fire_skill is non-null (Phase 144)."
    - dispatchSensors throwing or returning [] degrades gracefully: resolveFireSkill behaves exactly as the pre-144 engine (no sensor branch contribution). decide() never throws.
    - Test: a turn carrying a 'first_material' signal (SENS-01 fires context_block/push_forward) yields decision.fire_skill === 'Run Methodology' EVEN in tier_0 (no quadruple / brain null).
    - Test: a turn carrying an 'artifact_filed' signal with a CONTRADICT finding in the side-channel (SENS-06 fires contradiction/pull_back) yields decision.fire_skill === "Devil's Advocate".
    - Test: when BOTH wicked_indicators >= 8 AND a sensor reach are present, fire_skill stays the wicked soft-systems return (wicked wins the tie-break).
    - Test: chosen_rationale on a sensor-driven turn contains the reach_id + posture but NONE of the forbidden Part-8 tokens and no user text from the turn.
  </behavior>
  <action>
    Require dispatchSensors from ./insight-sensors.cjs at the module top of navigation-engine.cjs. In decide(), after tierMode resolves (~449) and before the line-592 resolveFireSkill call, read tuple from context (problem_type/complexity/stage with {} default) and ctx from context (roomDir/lowFillSections), then call dispatchSensors(turn, tuple, ctx) inside a try/catch that degrades to [] on any fault. Add reachIdToSkillFamily(reachId) as a closed switch over the 5 REACH_IDS returning canonical verbs per the behavior block (mirror the verbToSkillFamily switch shape at 303-319; the NAME is reachIdToSkillFamily per the corrections, the VALUES are canonical verbs because the router validates against CANONICAL_VERBS -- see critical_router_contract). Extend resolveFireSkill to a 4th param sensorReaches and insert the sensor branch with the documented precedence (wicked FIRST, sensor reach SECOND firing in ANY tier, then the existing mode_a BRAIN path, then the weightApplied>=0.9 fallback). Pass sensorReaches at the line-592 call site. Append the reach_id+posture-only clause to chosen_rationale. Flip the line-537 fence comment. DO NOT edit skill-activation-router.cjs (READ-ONLY) and DO NOT edit any lib/core/sensors/* file. Create tests/test-decide-sensor-fire.cjs asserting the four behaviors above (use the insight-sensors signal shapes: turn={signals:['first_material']} and turn={signals:['artifact_filed']} with a tmp roomDir whose .mindrian/last-cascade.json carries a CONTRADICT newFinding).
  </action>
  <verify>
    <automated>node tests/test-decide-sensor-fire.cjs && node tests/test-sensors-routing-fence.cjs && node tests/test-decide-part8-invariant.cjs</automated>
  </verify>
  <done>A first_material turn yields decision.fire_skill === 'Run Methodology' in tier_0; an artifact_filed CONTRADICT turn yields "Devil's Advocate"; wicked >= 8 + a sensor reach keeps the soft-systems return; chosen_rationale carries reach_id+posture only; test-sensors-routing-fence.cjs and test-decide-part8-invariant.cjs stay GREEN; no lib/core/sensors/* file and no skill-activation-router.cjs edit.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Zep-shaped LOCAL trace.context_assembly + temporal scalars + latency telemetry</name>
  <read_first>
    - lib/core/navigation-engine.cjs lines 396-441 (trace shell + the tier_0 early-return trace assignments)
    - lib/core/navigation-engine.cjs lines 528-589 (the populated trace + rationale path where navigated_neighborhood is set)
    - lib/core/navigation-engine.cjs lines 403-411 (buildNavigatedNeighborhood from ctx.roomContext -- the top-3 scalars source)
    - tests/test-decide-part8-invariant.cjs lines 1-90 (FORBIDDEN_TOKENS: projectText/shortText/hashText/safeNodeProjection/safeContradictionProjection/safeUnsupportedProjection/resolvePrivacyMode/PRIVACY_MODES; FORBIDDEN_CALLS: sha256/createHash; FORBIDDEN_REQUIRES: *packet*/*brain-client*)
    - .planning/phases/144-navigation-engine-legacy-engine-flip/144-FANOUT-CORRECTIONS.md (the Zep-informed Part-8-safe recommendations, items 1/2/4)
  </read_first>
  <acceptance_criteria>
    - trace.context_assembly is set on BOTH the tier_0 early-return path and the populated path, shaped { user_summary, facts, decision_grounding }: user_summary = top-3 navigatedNeighborhood node ids/types/scores (scalars from buildNavigatedNeighborhood, or [] when null); facts = the fired sensor reach_ids + evidence scalars (counts/enums/phase_ids only); decision_grounding = which fact (reach_id or 'brain_verb' or 'wicked' or 'neighborhood' or null) justified the chosen fire_skill.
    - Each user_summary node + each fact carries LOCAL temporal scalars { first_seen, last_updated } (epoch ms or ISO; null when the source did not supply them) read from the neighborhood/reach scalars only -- never a fabricated value tied to user content.
    - trace._meta.latencies_ms = { dispatchSensors_ms, decide_total_ms } measured with a LOCAL monotonic clock (Date.now or process.hrtime). The dispatchSensors_ms measurement wraps the Task-1 dispatchSensors call. decide_total_ms is measured from decide() entry to just before return.
    - ALL of context_assembly + temporal + _meta.latencies_ms are LOCAL trace JSON only. No new require of any *packet* / *brain-client* module; none of the FORBIDDEN_TOKENS / FORBIDDEN_CALLS introduced; nothing is ever passed to a Brain query path.
    - test-decide-part8-invariant.cjs stays GREEN over navigation-engine.cjs with the additions.
  </acceptance_criteria>
  <action>
    In decide(), build a context_assembly object { user_summary, facts, decision_grounding } from the already-computed navigatedNeighborhood (top-3 scalar slice) and the Task-1 sensorReaches (reach_ids + evidence scalars), and assign it to trace.context_assembly on both the tier_0 early-return and the populated path. Attach { first_seen, last_updated } scalars to each user_summary node and each fact, sourced ONLY from existing neighborhood/reach scalars (null when absent -- never invent). Record trace._meta.latencies_ms { dispatchSensors_ms, decide_total_ms } using a LOCAL monotonic clock, wrapping the dispatchSensors call and the whole decide() body. Keep every byte LOCAL: do not require any packet/brain-client module, do not introduce any FORBIDDEN_TOKEN/CALL, do not thread any of this toward a Brain path. Guard the 1200ms NAV budget by NOT adding any await or I/O (dispatchSensors is sync; this is pure scalar shaping).
  </action>
  <verify>
    <automated>node tests/test-decide-sensor-fire.cjs && node tests/test-decide-part8-invariant.cjs && grep -v '^[[:space:]]*//' lib/core/navigation-engine.cjs | grep -c 'context_assembly'</automated>
  </verify>
  <done>trace.context_assembly{user_summary,facts,decision_grounding} + per-node temporal scalars + trace._meta.latencies_ms appear on both decide() return paths; test-decide-part8-invariant.cjs GREEN; zero packet/brain-client require and zero forbidden token/call added; the grep gate (comment-stripped) counts context_assembly >= 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decide() -> Brain | The ONLY egress risk. decide() must add no path that sends LOCAL bytes (reach evidence, neighborhood ids, user text) toward a Brain query. |
| sensor reach -> rationale string | reach.signal / reach.evidence may carry LOCAL scalars; the rationale clause must surface reach_id + posture ONLY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01 | Information Disclosure | chosen_rationale sensor clause | mitigate | clause names reach_id + posture only; Task-1 test asserts no user text and no forbidden token; test-decide-part8-invariant.cjs gates the file |
| T-144-02 | Information Disclosure | trace.context_assembly + temporal + latency | mitigate | LOCAL trace JSON only; no packet/brain-client require; FORBIDDEN_TOKENS/CALLS sweep stays green |
| T-144-03 | Tampering | sensor-module fence | mitigate | no edit to any lib/core/sensors/* file; test-sensors-routing-fence.cjs stays green |
| T-144-04 | Tampering | router READ-ONLY contract | mitigate | no edit to skill-activation-router.cjs; the flip is a consequence of a canonical-verb fire_skill |
| T-144-05 | Denial of Service | 1200ms NAV budget | mitigate | dispatchSensors is sync; latency telemetry is scalar shaping; no await/I/O added; trace._meta.latencies_ms records the budget headroom |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | no package install in this plan; nothing added to package.json |
</threat_model>

<verification>
- node tests/test-decide-sensor-fire.cjs (new acceptance suite passes)
- node tests/test-sensors-routing-fence.cjs (sensor-module fence GREEN)
- node tests/test-decide-part8-invariant.cjs (Part-8 invariant GREEN, including the positive navigation.cjs require anchor)
- bash tests/run-all-91.sh (or the shipped 91 suite) shows no regression in decide()'s existing behavior
- git diff --stat shows NO change under lib/core/sensors/ and NO change to lib/core/skill-activation-router.cjs
</verification>

<success_criteria>
- A sensor-driven turn produces a non-null decision.fire_skill carrying a canonical verb (router-validated) in ANY tier (proven in tier_0 by the new suite)
- The 5-reach -> canonical-verb mapping keys on EXACTLY the 5 frozen REACH_IDS
- The multi-sensor precedence (wicked > top reach by REACH_IDS order > BRAIN verb > weightApplied>=0.9 fallback) is documented in a comment block and exercised by the suite
- The Zep-shaped trace.context_assembly + temporal scalars + latency telemetry are LOCAL-only
- The line-537 fence comment is flipped to the router-layer phrasing
- All three fences (sensor-routing, part8-invariant, the shipped 91 suite) stay GREEN
</success_criteria>

<output>
Create `.planning/phases/144-navigation-engine-legacy-engine-flip/144-01-SUMMARY.md` when done
</output>
