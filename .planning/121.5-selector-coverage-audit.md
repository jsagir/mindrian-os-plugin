# Phase 121.5 Selector Coverage Audit + Brain-Suggestion Template Lock-In

## 1. Source-of-Truth Preamble

- CODE claims read against: `origin/main` HEAD @ `5bdb7667` (commit message: "docs: brain three-ticket reconciliation 2026-05-23 -- second stale-cache audit false-positive"), plugin version `1.13.0-beta.29`
- WIRE claims probe against: none (this audit reads source only)
- Date of audit: 2026-05-23
- Re-verification rule: every selector status claim was verified against the actual command file at `origin/main` HEAD before filing, via `grep -ln` sweep across `commands/*.md` for `AskUserQuestion`, `F.0` / `F.1` / `F.2` / `F.3` / `F.4` / `F.5` / `F.6` / `F.7`, `selector-dispatcher`, `pickShape`, `rankForSelector`, `chain-recommender`, and bespoke prompt patterns (`Ready to`, `Want to`, `Should I`, `Approve`, `Confirm`, `continue / stop`).

## 2. Executive Summary

**Total commands in scope:** 88 (`ls commands/*.md` = 88 files). All 88 have `body_shape:` frontmatter, but body_shape declaration is not the same as F-shape compliance.

**Classification breakdown:**

| Class | Count | % | What it means |
|------|------|----|----------------|
| COMPLIANT | 6 | 6.8% | Declares an F sub-shape AND uses AskUserQuestion (or routes through selector-dispatcher) AND invokes canonical verbs + glyphs |
| PARTIAL | 4 | 4.5% | Mentions F.N in body OR uses AskUserQuestion but does not fully wire selector-dispatcher; renders an example F-shape block but is implementation-deferred |
| BESPOKE | 6 | 6.8% | Has a decision moment but uses a non-AskUserQuestion prompt pattern (bracket-text, prose "Ready to ___ ?", custom `[GATE]`, picker tables) |
| NONE | 1 | 1.1% | Has a decision moment but no selector at all (silent dispatch); `/mos:suggest-next` is the named offender |
| N/A | 71 | 80.7% | Pure utility / status-reader / exporter / health-check / methodology commands whose decision moments are owned by the methodology body itself or by Larry's conversational follow-up (no inline F-shape required at command-file scope) |

> The 80.7% N/A class is NOT a coverage gap. Per the Phase 88.2 design, methodology commands legitimately delegate their decision moment to the closing F.1 selector that Larry surfaces from conversation (or to the Phase 116 / 117 agentic surfacing layer). The capstone gap is in the ~17% of commands that DO have a decision moment AND have not adopted the F-shape contract.

**Brain-suggestion consumer count + current divergence pattern:** 5 surfaces consume Brain-suggested next moves (`/mos:suggest-next`, `/mos:act --chain`, `lib/agents/tension-hook-agent.cjs`, `lib/agents/auto-explore-agent.cjs`, `lib/agents/reverse-salient-agent.cjs`). All five render their top-K differently -- different headers, different verb sets, different glyphs, different presence/absence of the RECOMMENDED marker. **Three already route through `pickShape`** (the three agentic surfaces). **Two do not** (`suggest-next` is a plain text list; `act --chain` is bespoke `[GATE]` text with `[continue]` / `[stop]` brackets). This is the single biggest leverage point in the capstone -- locking the template propagates fixes across all five consumers simultaneously and converts the two CLI-script non-adopters in one pass.

**The 5 biggest strengthening wins (highest leverage):**

1. **`/mos:suggest-next`** -- promote from NONE to F.1 via `pickShape`. This command literally exists to surface Brain-ranked next moves and currently renders a plain Shape B step-numbered list with zero AskUserQuestion plumbing. Propagation effect: every consumer that calls `scripts/suggest-next-command.cjs` (Larry's conversational suggest-next reflex, the `/mos:onboard` Step 6 fallback) inherits the fix.
2. **`/mos:act --chain` `[GATE]` rendering** -- promote bespoke `[continue]` / `[stop]` bracket text to canonical F.0 Mini Decision Gate via `pickShape({ requestedShape: 'F.0' })`. The act-command.cjs file at line 129 literally comments "render (Shape E / F.0 action report)" but emits prose, not the dispatcher contract. This is the second-biggest divergence and the second-easiest fix.
3. **Brain-suggestion content template lock-in (Section 5 of this audit)** -- one canonical chip / glyph / color / verb / footer shape applied across all 5 consumers. Single source of leverage; downstream phases (the v1.14.0 learned-ranker-weights plant-seed, future Brain enrichment) inherit the lock-in.
4. **`/mos:operator set` + `/mos:operator reset` + `/mos:jtbd set`** -- promote the "STRUCTURAL marker block" pattern (deferred to Phase 88.2 per the inline TODO at operator.md:138 and jtbd.md:220-222) to live AskUserQuestion dispatch via `pickShape`. Phase 88.2 IS shipped (the renderers exist), but these three commands still print the marker prose. PARTIAL -> COMPLIANT.
5. **`/mos:scenario-plan` + `/mos:compare-ventures`** -- these are the two methodology commands that produce parallel branches the user must resolve; today the resolution is conversational. Add an explicit F.5 Branch Resolution selector at the close of each, via `pickShape({ requestedShape: 'F.5' })`. Adopting F.5 here unlocks the shape's only two intended consumers in v1.13.0 and validates the F.5 renderer in production traffic.

**Thesis (one paragraph):** the coverage shape today is "Phase 88.2 shipped the renderers, Phase 125 shipped the ranker, but the command files in `commands/*.md` did not finish adopting either -- the three agentic surfaces (Phase 89-07 / 116 / 117) routed through `pickShape` because they were built AFTER 88.2, while the older CLI-script consumers (`suggest-next-command.cjs`, `act-command.cjs`) and the operator / jtbd / onboard / diagnose recommendation surfaces still render bespoke prose or 'STRUCTURAL marker' placeholders." The capstone fix is to (a) lock the Brain-suggestion content template (Section 5) so every consumer renders the same chip / glyph / color / verb / footer, (b) sweep the 4 PARTIAL commands from marker-prose to live `pickShape` dispatch (Phase 88.2's renderers are already shipped), and (c) promote the 6 BESPOKE commands to the appropriate F sub-shape. Net: ~10 command-file edits + 2 CLI-script rewrites (`suggest-next-command.cjs`, `act-command.cjs`), all of which extend already-shipped substrate (no new renderers, no new dispatcher branches, no new ranker logic). This is pure consolidation per Canon Part 7.

## 3. Coverage Matrix

> Sort: COMPLIANT first, then PARTIAL, BESPOKE, NONE, N/A. Within class, alphabetical by command. Verb / glyph / color recommendations use ONLY the canonical 10 verbs / 12 glyphs / 5 colors per `skills/ui-system/SKILL.md`.

| Command | Class | Today | Recommended F-shape | Verb(s) | Glyph(s) | Color | Strengthening notes |
|---------|-------|-------|---------------------|---------|----------|-------|---------------------|
| `/mos:auto-explore` | COMPLIANT | F.1 via auto-explore-agent.cjs surfaceFinding -> pickShape; BQ-anchored header; RECOMMENDED gate >= 0.7 (Phase 88.2 invariant) | F.1 | Run Methodology, Defer, Free-Text (currently surfaced as Explore / Later / Skip -- aliases of Run Methodology / Defer / Reject) | filled-square, right-triangle-filled, right-triangle-empty | cyan | Already canonical; only nit is verb-label alias drift (Explore vs Run Methodology). Defer to Sub-plan I or accept as agentic alias. |
| `/mos:explain-decision` | COMPLIANT | body_shape: F.1; pure read-only audit surface; renders decision-trace JSON in Shape F.1 layout | F.1 | n/a (audit lens; verbs not user-facing) | filled-square, checkmark, warning | gray | Audit-only; the F.1 declaration is for layout discipline, not a live selector. Keep as-is. |
| `/mos:find-bottlenecks` | COMPLIANT | F.0 via reverse-salient-agent.cjs surfaceFinding -> pickShape; persona-suffixed header | F.0 | Approve / Reject / Defer (the F.0 closed vocabulary; Approve collapses to Run Methodology, Reject captures REJECTED_BECAUSE) | filled-square, right-triangle-filled, checkmark, warning | yellow | Already canonical. The persona suffix is a Phase 89-07 extension; preserve. |
| `/mos:explore-domains` (when called by Phase 117 auto-explore path) | COMPLIANT | F.1 via auto-explore-agent.cjs (same pipeline as `/mos:auto-explore`) | F.1 | same as auto-explore | same | cyan | Compliant via agent; the command body itself is methodology-only and N/A for inline selector. |
| `/mos:explore-trends` (when called by Phase 117 path) | COMPLIANT | F.1 via the same agent | F.1 | same | same | cyan | Same as explore-domains. |
| `/mos:rs-fetch` (when called by reverse-salient-agent.cjs) | COMPLIANT | F.0 via reverse-salient-agent | F.0 | Approve / Reject / Defer | filled-square, right-triangle-filled, checkmark, warning | yellow | Same as find-bottlenecks. |
| `/mos:diagnose` | PARTIAL | body_shape: A; body declares Shape F.1 Next Move at line 62 with "Use AskUserQuestion" instruction at line 77; renders prose example block, not live dispatch | F.1 | Run Methodology, Defer, Free-Text | filled-square, right-triangle-filled, right-triangle-empty | cyan | Move from "render an example F.1 in markdown" to "call pickShape({ requestedShape: 'F.1', payload: { verbs: ['Run Methodology', 'Defer', 'Free-Text'], header: '[diagnose] -- NEXT MOVE' } })". Phase 121.5-08 Sub-plan J D-12 already locks this; capstone closes the loop. |
| `/mos:jtbd` (set subcommand) | PARTIAL | body declares F.1 picker; explicit note at line 220-222 defers to Phase 88.2 ("renders as STRUCTURAL marker block in stdout") | F.1 | the 12 JTBD ids as verbs + Free-Text | filled-square, right-triangle-filled, right-triangle-empty, bullet | cyan | Phase 88.2 is shipped. Sweep the marker-block prose to live pickShape dispatch with the 12 JTBD ids as verb labels. The F.1 renderer accepts an arbitrary `verbs: []` payload so no new dispatcher branch is needed. |
| `/mos:onboard` (Step 6) | PARTIAL | body declares F.1 Next Move at line 348 with "Use AskUserQuestion" instruction at line 365; renders prose example block, not live dispatch | F.1 | Run Methodology, Defer, Free-Text | filled-square, right-triangle-filled, right-triangle-empty | green | Same fix shape as `/mos:diagnose`. Phase 121.5-08 Sub-plan J D-12 already locks the contract; capstone wires the dispatcher call. |
| `/mos:operator` (set + reset subcommands) | PARTIAL | body declares F.1 (set) + F.4 (reset) at line 33-34; explicit Phase 88.2 deferral note at line 134-138 | F.1 (set), F.4 (reset) | F.1: JUST_TALK / EXPLORE_CAPTURE / BUILD_ROOM / METHODOLOGY / DECISION_GATE / Free-Text. F.4: closed Confirm / Cancel | filled-square, right-triangle-filled, right-triangle-empty | yellow | Same as `/mos:jtbd`. Sweep marker-block prose to live pickShape. F.4 reset confirmation requires no new renderer because F.0 Mini Decision Gate's closed vocab (Approve / Reject / Defer) can substitute for Confirm / Cancel via Approve = Confirm reset, Reject = Cancel (Defer = silent dismiss with a memory_event). If a closer match is wanted, F.4 Insight Extraction's Back option carries the Cancel semantic. |
| `/mos:act` (--chain mode) | BESPOKE | bespoke `[GATE]` text block at scripts/act-command.cjs:152-167 with `[continue]` / `[stop]` brackets; comment at line 129 says "render (Shape E / F.0 action report)" but emits prose, not the dispatcher contract | F.0 | Approve (= continue) / Reject (= stop) / Defer | filled-square, right-triangle-filled, checkmark, warning | yellow | Replace the bracket-text block with pickShape({ requestedShape: 'F.0', payload: { header: '[GATE] Chain step N: <cmd> for <fw>', body: 'This step is not autonomous_safe -- it needs your eyes.' } }). The dispatcher already accepts both header + body payload (per shape-f0-renderer.cjs signature in selector-dispatcher.cjs:373-379). Zero new code; just a payload migration. |
| `/mos:compare-ventures` | BESPOKE | methodology body asks "Want me to compare another?" conversationally; no F.5 Branch Resolution dispatch | F.5 | Continue / Merge / Compare / Park / Free-Text | filled-square, right-triangle-filled, right-triangle-empty | cyan | This is the canonical F.5 use case (multiple ventures, user must decide whether to continue one, merge insights, compare formally, park one). Add pickShape({ requestedShape: 'F.5' }) at session close. |
| `/mos:file-meeting` | BESPOKE | bespoke "File this to <section>?" prose prompt at the close of the methodology body | F.0 | Approve / Reject / Defer | filled-square, right-triangle-filled, checkmark, warning | green | Filing IS a Mini Decision Gate (Approve filing, Reject with reason, Defer to milestone audit). Replace prose with pickShape({ requestedShape: 'F.0' }). |
| `/mos:new-project` | BESPOKE | prose confirmation gates for room creation + section scaffolding decisions | F.0 | Approve / Reject / Defer | filled-square, right-triangle-filled, checkmark, warning | green | Same as file-meeting; the create-room gate is a binary Approve / Reject decision. |
| `/mos:pipeline` | BESPOKE | bespoke step-confirmation gates in scripts/pipeline-command.cjs; comparable to act --chain but per-step | F.0 (per step) | Approve / Reject / Defer | filled-square, right-triangle-filled, checkmark, warning | yellow | Same fix shape as act --chain. The pipeline state machine already tracks per-step transitions; only the surface needs to swap prose for dispatcher. |
| `/mos:scenario-plan` | BESPOKE | methodology body branches into scenarios with "Pick one to explore deeper" prose | F.5 then F.3 | F.5: Continue / Compare / Park / Drop / Free-Text. F.3 (depth on chosen scenario): Shallow / Medium / Deep / Extreme / Back | filled-square, right-triangle-filled, right-triangle-empty | cyan | Two-stage selector: F.5 picks which scenario, F.3 picks depth for the chosen scenario. Adopting both validates F.3 + F.5 in production traffic (today neither has a shipped caller). |
| `/mos:suggest-next` | NONE | scripts/suggest-next-command.cjs renders a plain Shape B step-numbered text list with `->` arrow glyphs; zero AskUserQuestion, zero pickShape | F.1 | Run Methodology (per top-ranked command), Navigate Graph (alternative), Defer, Free-Text | filled-square, right-triangle-filled, right-triangle-empty, arrow | cyan | This is THE highest-leverage promotion. The command literally exists to surface Brain-ranked next moves and has no selector dispatch. Wire scripts/suggest-next-command.cjs through lib/workflow/f-selector-ranker.cjs `rankForSelector` (per docs/F-SELECTOR-CONSUMER-GUIDE.md) and dispatch via pickShape({ requestedShape: 'F.1' }). Propagation effect: every conversational "what next?" Larry reflex inherits the fix. |
| 71 methodology / utility / status / export commands | N/A | body_shape declares A/B/C/D/E/methodology; decision moments (if any) are owned by methodology body or by Larry's conversational follow-up | n/a | n/a | n/a | n/a | The 71 commands include: admin, analyze-needs, analyze-systems, analyze-timing, beautiful-question, brain-derive, build-knowledge, build-thesis, causal, challenge-assumptions, dashboard, deep-grade, diagnostics, doctor, dogfood-flush, dominant-designs, dogfood-flush, export, feynman-timeline-refresh, find-analogies, find-connections, funding, grade, graph, hat-briefing, heal, help, hmi-status, leadership, lean-canvas, macro-trends, map-unknowns, memory, models, mos-reason, mos, mullins, mva-brief, mva-option, opportunities, organize, persona, present, publish, query, radar, reanalyze, research, room, rooms, root-cause, rs-experts, rs-explain, rs-thesis, scheduled-tasks, score-innovation, scout, setup, snapshot, speakers, splash, status, structure-argument, systems-thinking, think-hats, update, user-needs, validate, value-proposition, vault, visualize, whitespace, wiki. Most of these are correctly N/A; a handful (think-hats close-out, structure-argument close-out, grade follow-up, deep-grade follow-up, validate close-out) MAY benefit from an explicit F.1 Next Move at the close, but the methodology body's conversational close is acceptable per the Phase 88.2 design (closer F.1 is Larry's reflex, not a per-command obligation). Treat these as Phase 121.5 + 1 backlog. |

## 4. Per-Class Deep Dive

### 4.1 PARTIAL class (4 commands: diagnose, jtbd, onboard, operator)

**Shared pattern:** all four declare an F sub-shape in body prose, instruct the reader to "Use AskUserQuestion", and then render a prose example block in markdown that resembles the F-shape -- but the implementation defers actual dispatch to Phase 88.2 via inline TODO notes. Phase 88.2 IS shipped. These commands have not been swept.

**Strengthening template (the F-shape skeleton these four should adopt):**

```
1. Build the contract payload (verbs[], header, optional body).
2. Call:
     const result = pickShape({
       requestedShape: 'F.1',  // or F.4 for operator reset
       roomDir: roomDir,
       operator: currentOperator,
       payload: {
         verbs: ['Run Methodology', 'Defer', 'Free-Text'],
         header: '[CONTEXT] -- ' + commandSlug + ' -- NEXT MOVE',
         recommendedVerb: brainConfidence >= 0.7 ? 'Run Methodology' : null,
         emitTelemetry: true,
       },
     });
3. Render result.rendered.zones to stdout.
4. On user pick, call recordSelectorResponse(roomDir, { sub_shape, verb_chosen, ... }).
5. The dispatcher already attaches the AskUserQuestion structural-marker trailer, so the host (Larry) sees the contract and routes the answer correctly.
```

**Affected commands with specific strengthening notes:**

- **`/mos:diagnose`** -- already has the Phase 121.5-08 Sub-plan J D-12 lock that mandates the F.1 surface. Capstone wires the call.
- **`/mos:jtbd` (set subcommand)** -- 13-option F.1 with the 12 JTBD ids + Free-Text; F.1 renderer accepts arbitrary verbs[] payload.
- **`/mos:onboard` (Step 6)** -- same as `/mos:diagnose`; Sub-plan J D-12 already locks it.
- **`/mos:operator` (set + reset)** -- two surfaces: set = F.1 with the 5 operators + Free-Text; reset = F.4 confirmation OR F.0 (Approve = Confirm, Reject = Cancel, Defer = silent dismiss with memory_event). F.0 is the closer match since reset is binary-with-escape.

### 4.2 BESPOKE class (6 commands: act --chain, compare-ventures, file-meeting, new-project, pipeline, scenario-plan)

**Shared pattern:** decision moments rendered as prose ("Ready to ___?", "File this to ___?", `[GATE]` bracket text, "Pick one to explore deeper", "Want me to ___?"). None routes through pickShape. All would benefit from an F sub-shape.

**Strengthening template (the F-shape skeleton these six should adopt):**

```
For binary/trinary confirmation gates (file-meeting, new-project, pipeline per-step):
  pickShape({
    requestedShape: 'F.0',
    payload: {
      header: '[CONTEXT] -- ' + decisionLabel,
      body: oneLineRationale,
      parent_decision_id: priorStateId,  // optional; links the gate to a parent
    },
  });

For branch-resolution gates (compare-ventures, scenario-plan):
  pickShape({
    requestedShape: 'F.5',
    payload: {
      header: '[CONTEXT] -- BRANCH',
      verbs: ['Continue', 'Merge', 'Compare', 'Park', 'Free-Text'],
      recommendedVerb: null,  // F.5 typically renders no RECOMMENDED
    },
  });

For chain-step blocker gates (act --chain):
  pickShape({
    requestedShape: 'F.0',
    payload: {
      header: '[GATE] Chain step N: <cmd> for <fw>',
      body: 'This step is not autonomous_safe -- it needs your eyes.',
    },
  });
```

**Affected commands with specific strengthening notes:**

- **`/mos:act --chain`** -- comment at scripts/act-command.cjs:129 already says "Shape F.0 action report"; only the implementation lags. The bespoke `[continue]` / `[stop]` brackets become F.0 Approve / Reject (Defer being a third path the navigator gets for free).
- **`/mos:compare-ventures`** -- F.5 at session close; first production caller of F.5.
- **`/mos:file-meeting`** -- F.0 for "file this?" confirmation; first production caller of F.0 outside the agentic surfaces.
- **`/mos:new-project`** -- F.0 for create-room confirmation; second production caller of F.0 in the CLI surface family.
- **`/mos:pipeline`** -- F.0 per-step; aligns with act --chain pattern.
- **`/mos:scenario-plan`** -- F.5 (which scenario) followed by F.3 (depth on chosen). Validates both F.3 and F.5 in production simultaneously.

### 4.3 NONE class (1 command: suggest-next)

**Shared pattern:** decision moment exists, no selector at all, silent dispatch via plain text.

**Strengthening template:** WIRE THROUGH `rankForSelector` AND `pickShape`. The full path is documented in `docs/F-SELECTOR-CONSUMER-GUIDE.md` lines 57-109 (the "typical wiring pseudocode" block). The substrate is shipped; the consumer is missing.

**Affected commands with specific strengthening notes:**

- **`/mos:suggest-next`** -- scripts/suggest-next-command.cjs lines 123-198 renders Shape B prose. Replace with: (a) call `rankForSelector` with the room state to get a top-K of ranked commands, (b) dispatch via `pickShape({ requestedShape: 'F.1' })`, (c) on user pick call `recordSelectorDecision`, (d) if user picks the none-fit affordance call `recordSelectorMiss` and route to `/mos:do`. This is THE single highest-leverage promotion in the capstone -- it converts the canonical "what next?" surface from prose to canon-compliant dispatch.

## 5. Brain-Suggested-Next-Move Selector: The Locked Content Template

The most important new section. Today five surfaces consume Brain-ranked next moves; all five render differently. The capstone locks ONE shape.

### 5.1 Current Divergence

| Consumer | Current chip | Current header | Current verb labels | Current glyph(s) | Current color (Zone 1) | Has RECOMMENDED gate? | Routes through pickShape? |
|----------|---------------|------------------|----------------------|-------------------|------------------------|------------------------|----------------------------|
| `/mos:suggest-next` (scripts/suggest-next-command.cjs) | none | `-- <room slug> -- suggest-next [-- ProblemType: <pt>] --` | n/a (no verbs; renders framework -> command pairs) | `->` arrow per step | none | NO | NO |
| `/mos:act --chain` (scripts/act-command.cjs) | `[GATE]` literal | `-- MindrianOS -- act --chain -- <seed label> --` | `[continue]`, `[stop]` (bracket text) | none | none | NO | NO |
| Phase 116 tension hook (lib/agents/tension-hook-agent.cjs surfaceFinding) | none | `-- mindrianOS -- pending tension -- pick a verb --` | Resolve, Later, Skip | dispatcher-driven (filled-square, right-triangle-filled per shape-f1-renderer.cjs) | dispatcher-driven | NO (Phase 116 explicitly omits the RECOMMENDED gate per the agent's design note) | YES (pickShape F.1) |
| Phase 117 auto-explore (lib/agents/auto-explore-agent.cjs surfaceFinding) | none | BQ-anchored header (the verbatim Brain BQ name; per RESEARCH 8.5) | Explore, Skip, Later | dispatcher-driven | dispatcher-driven | YES (>= 0.7 gate per Phase 88.2 invariant) | YES (pickShape F.1) |
| Phase 89-07 ReverseSalientAgent (lib/agents/reverse-salient-agent.cjs surfaceFinding) | none | `-- mindrianOS -- reverse salient -- <persona suffix> --` | Approve, Reject, Defer (F.0 closed vocab) | dispatcher-driven (F.0 renderer) | dispatcher-driven | NO (F.0 specification: no RECOMMENDED in F.0 -- the shape itself IS the recommendation surface) | YES (pickShape F.0) |

**Divergence severity:** HIGH. Five surfaces, five header shapes, four different verb sets (`Resolve / Later / Skip` vs `Explore / Skip / Later` vs `Approve / Reject / Defer` vs `[continue] / [stop]` vs no-verbs-at-all), inconsistent RECOMMENDED policy, and two of the five do not even route through the canonical dispatcher.

### 5.2 The Locked Template

Two parts: the value table (each slot locked to one canonical value) and the visual mockup (what the rendered output looks like to the navigator). The uniqueness combination -- score-right plus filled/empty-triangle hierarchy plus two-line dense rows plus stat-strip footer -- is what makes Brain-suggestion selectors look distinct from every other F-variant at first glance, without breaking the AskUserQuestion chrome contract (which Anthropic owns and we cannot restyle).

#### 5.2.1 Slot values

| Slot | Locked value | Rationale |
|------|----------------|-----------|
| Header chip (<= 12 chars) | `[■ BRAIN]` | Single canonical glyph (filled-square, the "primary content marker" per SKILL.md Section 3) plus 5-char brand word. 9 chars including brackets. Navigator's eye learns "■ plus BRAIN = Brain is speaking" inside one session. Distinct from `[GATE]` / `[CONTEXT]` / `[NEXT MOVE]` family by the leading glyph; same bracket envelope keeps it inside the canonical chip vocabulary. |
| Question line | `Choose next move:` | Verb-first imperative. Three words, no parenthetical. "Choose" not "Pick" matches the dispatcher's other prompts. The ranked-ness is implied by the right-aligned scores in the option rows, so the parenthetical "(ranked)" is redundant signal. |
| Option label glyph (recommended) | `▶` (right-triangle-filled) | The 0.7+ confidence top pick from Brain. Filled triangle is the canonical "primary action" glyph per SKILL.md Section 3. One glyph carries the same signal as a verbose `(RECOMMENDED)` tag without consuming horizontal space. |
| Option label glyph (alternatives) | `▷` (right-triangle-empty) | Sub-0.7 alternatives, or any non-top-ranked option. Empty triangle is the canonical "secondary action / collapsed content" glyph. Together with `▶` it creates a one-glyph visual hierarchy the navigator scans in milliseconds. |
| Option row format | Two lines per option. Row 1: `<glyph> <N>. <Run Verb>` left-padded, `<conf>%` right-aligned to the 80-col boundary. Row 2: 5-space indent, `<framework category> · <graph relationship>` | Right-aligned percentage on the verb row is the unique scan signal: navigator's eye lands on the numbers first (right column), then the verb (left), then the meta (below). Confidence as `87%` not `0.87` because percent is universally legible across personas; decimal requires "normalized 0-1 score" tech literacy we should not assume. Meta row gives semantic context (category plus graph edge to next or previous framework) so the navigator sees WHY Brain ranks it. The `·` (middle-dot) is borrowed from the existing Zone 4 footer convention. |
| Zone 1 left-rail color | `cyan` default; `yellow` when any candidate carries a CONTRADICTS edge against an existing assumption (Part 4 cascade signal) | Cyan is the canonical "commands / paths / links" color per SKILL.md Section 4, and Brain picks ARE recommended commands. Yellow is the canonical "warning" color; a contradicting cascade IS a warning the navigator needs to see before clicking through. Red reserved for errors, green for success, both would over-cue. |
| Footer caption | `▶ Brain · top-<K> of <N> ranked · <color> = informing` -- example: `▶ Brain · top-3 of 12 ranked · cyan = informing` | Stat-strip line. Three signals in one line: provenance (▶ Brain), scale (top-3 of 12 telling navigator "12 candidates considered, 3 surfaced"), and color-legend (reminding navigator that the cyan rail means Brain is informing, not commanding -- Canon Part 3 sub-claim made visible). The leading `▶` matches the same triangle vocabulary as the recommended-option marker, visually linking the footer to the top option. |
| F-variant routing | F.1 default; F.6 when a non-null JTBD is set (per selector-dispatcher.cjs lines 313-345 JTBD lookup); F.0 for binary recommendation confirmation gates upstream of F.1 (e.g. ReverseSalientAgent's "is this finding worth my time?" pre-gate); F.5 for branch-resolution surfaces (compare-ventures, scenario-plan) | Matches shipped selector-dispatcher.cjs Phase 101-01 contract verbatim. Locking at "F.1 default, F.6 when JTBD, F.0 for pre-gates, F.5 for branches" requires zero dispatcher changes. |

#### 5.2.2 Visual mockup (what the navigator sees)

```
[■ BRAIN]
Choose next move:

▶ 1. Run SWOT                                    87%
     industry analysis · feeds Porter Five Forces

▷ 2. Run Lean Canvas                             74%
     opportunity decomposition · chains to JTBD

▷ 3. Run Five Whys                               61%
     root cause · diverges from current path

  Type something
  Chat about this
──────────────────────────────────────────────────
▶ Brain · top-3 of 12 ranked · cyan = informing
```

No other F-shape carries scores in the option row. No other F-shape uses the filled/empty triangle pair as the recommended/alternative marker. No other F-shape has a stat-strip footer. The navigator recognizes the Brain selector before reading a word.

#### 5.2.3 Anti-patterns explicitly rejected (UX reasoning)

All considered. All rejected:

- **Colored text inside option descriptions.** Anthropic strips most ANSI inside AskUserQuestion rows; would render inconsistently across surfaces.
- **Box-drawing borders around individual options.** Wraps badly below 80 columns; common on Wave-N tester Windows boxes.
- **More than 3 lines per option.** Pushes the auto-injected "Type something" / "Chat about this" rows off-screen.
- **Invented glyphs or invented chip patterns.** Breaks the 12-glyph + canonical-bracket-chip vocabulary.
- **Telemetry meta row** (`12 calls · 8d ago`). "Popular content" energy turns Brain into a trending feed; risks Canon Part 8 if telemetry crosses the LOCAL/BRAIN boundary; semantic meta is the safer canonical pick. Reserve telemetry for a v2 second-row option if ever needed.
- **Highlighted-bar focus indicator** (Lazygit-style). AskUserQuestion's cursor IS the `>` chevron in the left margin; we cannot repaint it as a colored bar without forking Claude Code. The triangle vocabulary inside the option rows is our compensation: it draws the eye to hierarchy independent of the platform cursor.

### 5.3 Adoption Diff per Consumer

**`/mos:suggest-next` (scripts/suggest-next-command.cjs)** -- highest-leverage change.
- Lines 123-198: replace `renderSequence` + `actionFooter` text-block emission with a call to `lib/workflow/f-selector-ranker.cjs rankForSelector` (returns top-K with confidence) then `lib/hmi/selector-dispatcher.cjs pickShape({ requestedShape: 'F.1', payload: { verbs: items.map(i => i.command), header: '[■ BRAIN]', recommendedVerb: items[0].score >= 0.7 ? items[0].command : null, optionRows: items.map(i => ({ glyph: i.score >= 0.7 ? '▶' : '▷', verb: i.command, confPct: Math.round(i.score * 100), meta: `${i.category} · ${i.graphRelationship}` })), footer: `▶ Brain · top-${items.length} of ${totalRanked} ranked · cyan = informing` } })`.
- Add post-pick wiring: `lib/workflow/selector-decisions.cjs recordSelectorDecision` on F.0 accept, `recordSelectorMiss` on none-fit.
- Estimated effort: 0.5 person-day (the substrate is shipped; only the consumer adapter is new).

**`/mos:act --chain` (scripts/act-command.cjs)** -- second-highest leverage.
- Lines 131-177 `renderChainReport`: replace the `[GATE]` bracket text block (lines 152-167) with `pickShape({ requestedShape: 'F.0', payload: { header: '[GATE] Chain step N', body: gateReason } })`.
- Wire the F.0 result back into the chain state machine: Approve = continue, Reject = stop with REJECTED_BECAUSE edge, Defer = milestone-audit row.
- Estimated effort: 0.5 person-day.

**Phase 116 tension hook (lib/agents/tension-hook-agent.cjs)** -- minor alignment.
- Line 52 `F1_HEADER`: rename from `-- mindrianOS -- pending tension -- pick a verb --` to `[■ BRAIN]` per the locked chip; surface the tension context in the question line ("Resolve pending tension:") rather than overloading the 12-char chip.
- Line 56 `F1_VERBS`: rename `Resolve / Later / Skip` to the locked label `Run Methodology / Defer / Free-Text` (or keep the contextual aliases as a Phase 121.5 + 1 v2 decision; document the alias mapping in the agent's source comment).
- Estimated effort: 0.25 person-day.

**Phase 117 auto-explore (lib/agents/auto-explore-agent.cjs)** -- minor alignment.
- `surfaceFinding` BQ-anchored header (line 116): replace the BQ-anchored header with the locked `[■ BRAIN]` chip; move the BQ anchor (the Phase 117 moat per RESEARCH Section 8.5) into the question-line slot directly beneath the chip. The chip + question-line two-row format preserves the BQ anchor signal without violating the 12-char chip rule.
- Verb-label alias: `Explore / Skip / Later` -> `Run Methodology / Defer / Free-Text` (same alias decision as Phase 116).
- Estimated effort: 0.25 person-day.

**Phase 89-07 ReverseSalientAgent (lib/agents/reverse-salient-agent.cjs)** -- minor alignment.
- Line 506 header composition: replace with the locked `[■ BRAIN]` chip; move persona suffix (the Phase 89-07 extension that survives template lock per Sub-plan I) into the question-line slot directly beneath the chip. Same two-row pattern as the Phase 117 adoption.
- F.0 closed vocabulary (Approve / Reject / Defer) stays verbatim; F.0's vocabulary IS the canon mapping for binary-with-escape gates.
- Estimated effort: 0.25 person-day.

**Total Brain-suggestion lock-in effort: ~2 person-days** (0.5 + 0.5 + 0.25 + 0.25 + 0.25 + tests).

## 6. Phase 121.5 Punch List (Ordered by Leverage)

1. **Lock the Brain-suggestion content template across all 5 consumers (Section 5).** Single chip / glyph / color / verb / footer shape applied to `/mos:suggest-next`, `/mos:act --chain`, tension-hook-agent, auto-explore-agent, reverse-salient-agent. Effort: 2 person-days. Highest leverage because the lock propagates to every Brain-touching selector simultaneously and converts the two NONE/BESPOKE CLI scripts in the same pass.
2. **Promote `/mos:suggest-next` from NONE to F.1 via `rankForSelector` + `pickShape`.** Files: `scripts/suggest-next-command.cjs`, `commands/suggest-next.md` (frontmatter body_shape: B -> F.1). Effort: 0.5 person-day (subsumed in item 1).
3. **Promote `/mos:act --chain` from BESPOKE to F.0 via `pickShape`.** Files: `scripts/act-command.cjs`, `commands/act.md`. Effort: 0.5 person-day (subsumed in item 1).
4. **Sweep the 4 PARTIAL marker-block commands to live `pickShape` dispatch.** Files: `commands/diagnose.md`, `commands/jtbd.md`, `commands/onboard.md`, `commands/operator.md`. Each command swaps its inline F.x prose example for an actual call to pickShape. The renderers are shipped (Phase 88.2). Effort: 1 person-day total (~2 hrs each).
5. **Promote the 6 BESPOKE commands to the appropriate F sub-shape.** Files: `commands/compare-ventures.md` (F.5), `commands/file-meeting.md` (F.0), `commands/new-project.md` (F.0), `commands/pipeline.md` (F.0 per-step), `commands/scenario-plan.md` (F.5 + F.3). Effort: 1.5 person-days.
6. **Add a CI tripwire to enforce the lock.** A grep test that fails CI if any new bespoke prompt pattern lands in `commands/*.md` or in any consumer of `chain-recommender.cjs` / `rankForSelector` (`grep -E "Ready to|Pick one|continue / stop|\[GATE\]" -- new code triggers a review block). Effort: 0.25 person-day. Prevents regression of the consolidation gain.
7. **Update `docs/F-SELECTOR-CONSUMER-GUIDE.md` to publish the locked template** as Section 4 ("The Locked Template for Brain-Suggested Selectors") with the same table from Section 5.2 of this audit. Effort: 0.25 person-day. Makes the lock discoverable to future consumers.

**Punch list count: 7 items. Rough total effort: 5.5 person-days** (within the Phase 121.5 budget of 3-4 days per CONTEXT.md if items 1 + 2 + 3 are bundled as the "Brain-suggestion lock-in sub-plan" and items 4 + 5 are bundled as the "PARTIAL + BESPOKE sweep sub-plan"; items 6 + 7 ride along).

## 7. Open Questions for Navigator

1. **Verb-label alias decision.** Phase 116 tension hook uses `Resolve / Later / Skip`; Phase 117 auto-explore uses `Explore / Skip / Later`. Both are pedagogically clearer than the canonical `Run Methodology / Defer / Free-Text` in their specific contexts. Should the lock force canonical labels (consistency) or allow per-surface aliases with a registered mapping (pedagogy)? The agents argue for aliases; the dispatcher argues for canonical. AskUserQuestion candidate: `[Force canonical / Allow registered aliases / Defer to v2 user test]`.
2. **F.4 vs F.0 for `/mos:operator reset` confirmation.** F.4 Insight Extraction has a closed `Back` option that maps to Cancel. F.0 has Approve / Reject / Defer which maps Approve = Confirm, Reject = Cancel, Defer = silent dismiss. F.0 is the closer architectural match; F.4 is the closer semantic match (the operator wants to "extract" their reset intent). AskUserQuestion candidate: `[F.0 (architectural fit) / F.4 (semantic fit) / Both with explicit routing]`.
3. **CONTRADICTS-driven color flip (cyan -> yellow).** The locked template proposes yellow when any candidate carries a CONTRADICTS edge against an existing assumption. This requires the consumer to walk the cascade graph BEFORE rendering -- adding a SQL call to the rank path. Is that latency acceptable, or should the cyan default stay constant and the contradicting signal land in Zone 3 (Intelligence Strip) instead? Trade-off: in-place color signal is louder, but Zone 3 signal is the existing convention.
4. **Whether `/mos:suggest-next`'s frontmatter `body_shape: B` should change to `body_shape: F.1`.** The body_shape field is a layout discipline marker, not a selector contract. F.1 is the selector contract. They can coexist (the SKILL.md design treats them as orthogonal: Shape B is the LIST layout, F.1 is the SELECTOR over that list). Or they can be unified (suggest-next is "Shape F.1 over a Shape B list"). AskUserQuestion candidate: `[Keep B + add F.1 selector / Change to F.1 / Document the orthogonality in SKILL.md and keep B]`.
5. **Coverage of the 71 N/A commands' close-out F.1.** Today the Phase 88.2 design says methodology commands legitimately delegate their decision moment to Larry's conversational follow-up. Five methodology commands in particular (think-hats, structure-argument, grade, deep-grade, validate) produce rich enough output that an explicit F.1 close-out would be load-bearing. Is the scope of Phase 121.5 to land these too, or is that Phase 121.5 + 1 backlog? AskUserQuestion candidate: `[In scope (add to punch list) / Out of scope (defer to v1.14.0) / Audit-only flag here, decide at /gsd:plan-phase]`.
