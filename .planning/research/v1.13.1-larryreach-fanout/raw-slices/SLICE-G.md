# Slice G -- Larry orchestration policy -- When-to-Reach Capability Dial + buildContext capability backing

Verdict: **PARTIAL**

## Current State

Larry's orchestration policy for WHEN to reach (pull a Context Block / surface a contradiction / reach cross-room / consult the Brain) DOES exist, and it was added exactly where the task suspected: a "When to Reach -- The Capability Dial" section at line 31 of skills/larry-personality/SKILL.md. It is a 5-row trigger-to-action table modeled on the same trigger-to-action shape as the pre-existing "Causal Reasoning Suggestions (v1.7.0)" table (lines 221-231), plus a 5-rule "Reach rules" block. The four reaches are each given a trigger, what Larry reaches for, and what he does next: (a) Context Block on prior-work/"do you remember" references or facts outside the last 4-6 messages; (b) contradiction surface on a conflicting/superseded claim, rendered as ONE line then a Decision Gate (APPROVE/REJECT/DEFER); (c) cross-room reach only after an acknowledged room switch, fenced to the target room's own graph; (d) Brain consult on a named framework / methodology next-move, carrying only generic handles per Canon Part 8. The Reach rules encode: GUIDED default (surface, never decide), one reach per beat, honesty gates every reach, "the HOW lives elsewhere" (defers query shape to brain-connector and context-budget to context-engine), and Part 8 as the floor.

IMPORTANT STATE FACT: this section is an UNCOMMITTED working-tree edit. It is NOT in HEAD (git show HEAD:...SKILL.md has zero "Capability Dial" matches; working tree has one; git status shows the file as " M" modified). A `git log -S` search for the heading returns no commit. So the policy currently exists only on Jonathan's working tree.

The underlying capabilities all exist in code AS LIVE FUNCTIONS, but NOT behind the buildContext path the task pointed at. lib/core/navigation.cjs (the Phase 109 chokepoint) exports LIVE implementations for all four reaches: getNeighborhood + getRoomHomeView (the Context Block / long-term-memory neighborhood), findContradictions + findSurfaceableTensions (the contradiction surface), buildBrainPacket + storeBrainSuggestions plus lib/core/brain-client.cjs (the Brain consult), and lib/core/cross-room-aggregator.cjs (the cross-room reach, fully Phase-83-scoped with sealed/opt-out/scope guards). None of these are notImplementedYet stubs -- the stub factory in navigation.cjs is defined but unused in the current export map; all 13+ exports point at real submodule functions.

The gap: lib/core/chat-context-builder.cjs (buildContext) is a SEPARATE, OLDER subsystem (Phase 87-09) that does its own raw better-sqlite reads against room.db (edges/nodes/stakeholders) and does NOT require or call navigation.cjs at all. Its only requires are node:path, node:fs, node:sqlite. Its classifyIntent() has five buckets (contradicts/converges/stakeholders/gaps/briefing) that PARTIALLY overlap the policy's four reaches (contradicts maps to reach (b); none of its buckets implement cross-room or a Brain consult or the dated-facts Context Block). So buildContext is not the executor of the When-to-Reach policy; the live executor surface is navigation.cjs, which buildContext bypasses.

There is ZERO code wiring from the policy text to any executor. Grepping lib/, scripts/, hooks/ for "When to Reach", "Capability Dial", and "Context Block" returns nothing. The policy is prompt-layer doctrine for the model only; no hook, classifier, or router consumes it.

## File Evidence

- `skills/larry-personality/SKILL.md:31-49` -- The 'When to Reach -- The Capability Dial' section. Line 31 heading; line 33 GUIDED-default framing; lines 35-40 the 5-row trigger-to-action table (Context Block / contradiction surface / cross-room reach / Brain consult); lines 42-48 the 5 Reach rules. This is the policy the task asked about, and it IS present at ~line 31 as predicted.
- `skills/larry-personality/SKILL.md:221-231` -- Pre-existing 'Causal Reasoning Suggestions (v1.7.0)' trigger-to-action table -- the precedent the Capability Dial table is modeled on (Signal -> Suggestion shape). Confirms the trigger-to-action table pattern already existed in this SKILL.
- `skills/larry-personality/SKILL.md:64,69,110` -- In-SKILL cross-references from the Capability Dial all resolve: 'Reading the Room' trace defined at line 64, 'Brain says' trace at line 69, 'Honesty about memory' section at line 110 (referenced by Reach rule 3 and the Context Block row). No dangling intra-file refs.
- `skills/larry-personality/SKILL.md:47` -- Reach rule 4 defers the HOW to two sibling skills: 'brain-connector skill' and 'context-engine skill'. Both exist on disk (skills/brain-connector/SKILL.md 7105 bytes; skills/context-engine/SKILL.md 8296 bytes), so the cross-skill references resolve.
- `lib/core/chat-context-builder.cjs:33-35,117-118` -- buildContext requires ONLY node:path, node:fs, node:sqlite. It opens room.db directly (line 119 dbPath, line 134 new DatabaseSync readOnly) and never requires navigation.cjs. It is a self-contained older subsystem, not the executor of the When-to-Reach policy.
- `lib/core/chat-context-builder.cjs:47-55` -- classifyIntent() five buckets: contradicts/converges/stakeholders/gaps/briefing. Overlaps the policy only on 'contradicts' (reach b). No bucket for cross-room reach, Brain consult, or the dated-facts Context Block, so this classifier does not realize the full four-reach policy.
- `lib/core/navigation.cjs:46-73` -- The Phase 109 chokepoint exports LIVE functions backing every reach: getNeighborhood (l52) + getRoomHomeView (l73) = Context Block; findContradictions + findSurfaceableTensions (l55,l60) = contradiction surface; buildBrainPacket + storeBrainSuggestions (l69-70) = Brain consult. Comments mark each 'LIVE'.
- `lib/core/navigation.cjs:40-44` -- notImplementedYet() stub factory is defined but NOT used anywhere in the export object (lines 46-196 all point at real submodule functions). So the navigation surface is fully implemented, not stubbed.
- `lib/core/navigation/insights.cjs:43-46,237-240` -- findContradictions (l43) and findSurfaceableTensions (l237) are real implementations that query the nodes table and walk the neighborhood -- the contradiction-surface capability is live, not a placeholder.
- `lib/core/navigation/neighborhood.cjs:48-51` -- getNeighborhood is a real implementation with maxDepth/topK options -- the Context-Block neighborhood reach is live.
- `lib/core/navigation/packet.cjs:293-295` -- buildBrainPacket is a real async implementation (the typed Brain Context Packet, Part 9 / Part 8 boundary). The Brain-consult capability is live.
- `lib/core/cross-room-aggregator.cjs:112-188` -- isRoomSealed (l112), isRoomOptedOut (l124), isRoomInScope (l149), isRoomUnreadable (l162), discoverRegisteredRooms (l188), sanitizeDetailScalar (l242) -- the cross-room reach is implemented WITH the Phase-83 scope fencing the policy row (SKILL line 39) demands.
- `lib/core/brain-client.cjs:268,331-365` -- callTool (l268) and query (l331+) implement the live Brain MCP client with cypher-param + sanitizeCypherInput. Backs the Brain-consult reach at the wire level.
- `git (HEAD vs working tree)` -- git show HEAD:skills/larry-personality/SKILL.md | grep -c 'Capability Dial' = 0; working-tree grep -c = 1; git status = ' M skills/larry-personality/SKILL.md'. The When-to-Reach section is an UNCOMMITTED working-tree edit, not yet in any commit.

## Gaps

- The When-to-Reach policy is prompt-layer doctrine ONLY -- grep of lib/, scripts/, hooks/ for 'When to Reach' / 'Capability Dial' / 'Context Block' returns zero. No hook, classifier, or router reads or enforces it. Larry-the-model is the sole executor; nothing in code dispatches a reach based on the trigger column.
- buildContext (the file the task named) is the WRONG executor for this policy. It is the Phase 87-09 raw-SQL chat-context path and does not call navigation.cjs, so the live Context Block / contradiction / cross-room / Brain functions are not reachable through it. The policy's capabilities live in navigation.cjs, which buildContext bypasses -- a dual-path split.
- The Capability Dial section is uncommitted (working-tree only, not in HEAD). It is not yet shipped, not versioned in CHANGELOG, and could be lost on a stash/checkout. No phase or canon_parts frontmatter ties it to a roadmap phase.
- The policy's 'Context Block (long-term memory: dated facts + a short summary, seeded by the last ~2 turns)' has no single function that returns that exact shape. getNeighborhood/getRoomHomeView return graph neighborhoods, not a dated-facts+summary block. The 'short summary seeded by last 2 turns' summarization step has no obvious code home.
- Reach rule 2 ('one reach per beat') and the GUIDED-default 'surface, never decide' are model-side behavioral constraints with no code enforcement -- nothing prevents multiple navigation calls per turn or an auto-decided contradiction.
- classifyIntent in buildContext (contradicts/converges/stakeholders/gaps/briefing) and the four policy reaches are not reconciled. If buildContext is ever meant to be the dispatcher, its bucket set would need cross-room + Brain-consult + dated-Context-Block intents added and a require of navigation.cjs.

## Raw Notes

SURPRISES / DEAD CODE / ORPHANS worth preserving:

1. The task's framing pointed at buildContext as the place to look for the reach capabilities -- but buildContext is a RED HERRING for this policy. It is an older, self-contained Phase 87-09 subsystem (header comment lines 3-31) that does raw better-sqlite reads and never touches navigation.cjs. The real reach machinery is in lib/core/navigation.cjs + lib/core/navigation/*. So 'do the underlying capabilities exist in code' = YES, but NOT on the buildContext path. This is a genuine dual-path: two independent ways to assemble room context, only one of which (navigation.cjs) realizes the policy.

2. notImplementedYet() at navigation.cjs:40-44 is DEAD CODE -- defined, exported-comment references 'this stub will be replaced by [plan]', but the actual module.exports block (46-196) wires every name to a real submodule function. Left over from the Phase 109-04 surface-establishment commit. Harmless but stale.

3. The Capability Dial section is on a DIRTY working tree (git status ' M'). The most recent committed SKILL.md change is f28e73e9 'feat(120-03): wire scanner + F.7 renderer + Larry skill (GREEN)'. The Capability Dial edit sits on top of that, uncommitted. Whoever runs the next release ceremony needs to commit it or it vanishes. No CHANGELOG entry, no version bump, no canon_parts frontmatter.

4. The policy is well-aligned with canon: the contradiction row's 'ONE line then Decision Gate APPROVE/REJECT/DEFER' is Canon Part 3 (Tri-Context Decision Gate) verbatim; the cross-room row's Phase-83 fencing matches cross-room-aggregator.cjs sealed/opt-out/scope guards; the Brain row's 'generic handles only, never user bytes' is Canon Part 8 verbatim; GUIDED default matches the memory rule feedback_larry_pedagogical_guided_first. So the DOCTRINE is canon-consistent even though the WIRING is absent.

5. The 'Honesty about memory' section (lines 110-177) is much older and richer than the new table -- it already encodes the four-condition rule for when 'I have that in memory' is true (graph-backed + active room + not sealed + within session window). The new Reach rule 3 just cross-references it. Good reuse; no duplication.

6. navigation.cjs is heavily over-commented with phase provenance (every export carries a 5-15 line comment naming the phase + plan that added it). The 'closed 13-function surface' is now effectively ~30 exports via 'additive re-export' justifications (logMemoryEvent, writeEdge, detectActiveRoom, spine helpers, confirmNode, lens nodes, evidence claim). The '13-function' claim in the file header is no longer literally true; it is defended as 'the DOCUMENTED 13-function API plus internal helper re-exports.' Not a bug, but the naming has drifted.

7. buildContext consumers: lib/mcp/tool-router.cjs, scripts/serve-dashboard-live, and the test. So buildContext IS live in production (Desktop/Cowork MCP path + live dashboard) -- meaning the policy-bearing navigation.cjs path and the policy-blind buildContext path are BOTH shipping simultaneously on different surfaces. That is the deepest finding: the When-to-Reach policy will be honored by whatever calls navigation.cjs, and silently ignored by the MCP chat panel + live dashboard that call buildContext.
