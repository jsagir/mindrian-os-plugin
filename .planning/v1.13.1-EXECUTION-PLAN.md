---
type: milestone-execution-plan
status: AMENDED 2026-05-19 -- Phase 127 + Phase 127.1 PROMOTED OUT of v1.13.1 INTO v1.13.0; this doc is retained for the v1.13.1 phases that REMAIN (128 + 129 + 130 + 131 + future) but its anchor narrative is superseded
target: v1.13.1 (residual phases only after promotion)
authority: jsagir@gmail.com
canon_parts: [Part 6, Part 7, Part 8, Part 9, Part 10, Part 11 (proposed)]
supersedes: scattered beta_target frontmatter in individual phase CONTEXT.md files
governs: phases 128 / 129 / 130 / 131 (and any new v1.13.1 phases scoped post-promotion)
---

# v1.13.1 Execution Plan -- AMENDED 2026-05-19

## PROMOTION AMENDMENT (2026-05-19)

**Phase 127 + Phase 127.1 PROMOTED INTO v1.13.0.** v1.13.0 redefined from "The Closed Loop" (Hooked Fixes + Canon Part 10) to **"The Closed Loop + Brain Goes Native"**. The two architectural anchors that previously gated v1.13.1 now ship as v1.13.0-beta.19 (Phase 127) and v1.13.0-beta.20 (Phase 127.1), with v1.13.0 final cutting after both land.

| Phase | Was | Now |
|---|---|---|
| 127 brain-mcp-local-stdio-shim | v1.13.1-beta.1 (architectural anchor) | **v1.13.0-beta.20** |
| 127.1 brain-graphrag-collapse-pinecone-neo4j-hnsw | v1.13.1-beta.2 (server-side substrate swap) | **v1.13.0-beta.21** |

**v1.13.1 residual scope** (post-promotion):
- Phase 128 substrate-contract-adr (was v1.13.1-beta.3 in old doc)
- Phase 129 spine-repair-memory-event (was v1.13.1-beta.3)
- Phase 130 lens-engine-skeleton + cognitive-family migration (was v1.13.1-beta.5)
- Phase 131 research-as-graph-aware-workflow (was v1.13.1-beta.5c)
- v1.13.1 milestone rename pending: "spine repair + lens engine + research workflow" (not "Brain native" anymore)

**v1.13.0 ship-date impact:** original v1.13.0 final was 1-3 hours away (just 121.5 fixes + cut). Promotion adds ~2 weeks (Phase 127 ~3-5 days + Phase 127.1 ~3-5 days + harness work). Trade-off: bigger release narrative, ONE adoption cycle for "Brain feels native end-to-end" instead of two.

---

## The destination (original framing -- now applies to v1.13.0 not v1.13.1)

**v1.13.0 (REDEFINED) = "The Closed Loop + Brain Goes Native"** -- the release where MindrianOS becomes a coherent product. The Brain bakes itself in (Phase 127 client-side stdio shim + Phase 127.1 server-side Neo4j collapse). Larry default-activates (Phase 114, shipped). Material lands → 30-second pipeline runs → room generates as side effect (Phase 118, shipped). Cross-session tensions persist (Phase 116, shipped). Own-breakthroughs surface (Phase 120, shipped). One terminal experience, wrapped by the capstone (Phase 121.5, shipped).

## The two anchor phases (NOW v1.13.0)

| Anchor | Why it anchors |
|---|---|
| **Phase 127 (v1.13.0-beta.20)** | Architectural gate. After 127, every v1.13.0 surface that uses Brain works zero-config -- no manual `claude mcp add`, no `~/.mindrian.env` editing beyond the API key drop. |
| **Phase 127.1 (v1.13.0-beta.21)** | Server-side counterpart. Pinecone retired. Brain unified under Neo4j HNSW. Tester experience: ONE bundle for "Brain feels native end-to-end". |
| **Phase 121.5 (shipped)** | User-facing wrapper, ALREADY SHIPPED 2026-05-16, re-verified 2026-05-19. |

Everything between Phase 127 + Phase 127.1 is paced by their dependencies in the same milestone window.

## Synthesis-Plan Absorption (2026-05-16)

Following the 5-cluster command audit (`~85` commands across SAPPhIRE / HSI / RS / methodology / spine / research / lifecycle clusters) and the v1.13.1 alignment audit, **four foundation phases are absorbed into the v1.13.1 train** to make this release the architectural coherence anchor instead of "eight surfaces talking through Brain over a half-wired spine":

| New phase | Synthesis map | Wave | Beta target | Days |
|---|---|---|---|---|
| **Phase 128 substrate-contract-adr** | P1 (Substrate Contract ADR + CI guards: navigation.cjs as the only door) | W4 Stream E (parallel) | v1.13.1-beta.3 | ~2 |
| **Phase 129 spine-repair-memory-event** | P2 (route 6 silent spine scripts -- status / suggest-next / act / pipeline / jtbd / operator / memory -- through navigation.cjs + emit `memory_event` on every transition) | W4 Stream F (parallel) | v1.13.1-beta.3 | ~3-4 |
| **Phase 130 lens-engine-skeleton + cognitive-family migration** | P4 (skeleton) + P8 (cognitive lens family: think-hats / persona / hat-briefing / challenge-assumptions move from filesystem to room.db typed nodes) | W6.5 (pre-capstone) | v1.13.1-beta.5 | ~4-5 |
| **Phase 131 research-as-graph-aware-workflow-step** | P13 PILOT (source-lens family pilot via /mos:research: context-aware + workflow-aware + graph-native; the other 13 research surfaces follow in v1.14.0 with the pattern proven here) | W6.7 (between 130 and 121.5) | v1.13.1-beta.5c | ~5-7 |

**Why:** the audit found 0 of 7 spine scripts emit `memory_event` and 0 of 14 research surfaces are Canon Part 9 compliant. Without 128 + 129, every v1.13.1 phase that DOES wire to navigation.cjs (116, 117, 121) accumulates evidence on a partial event stream that downstream work has to reconcile in v1.13.2. Phase 130 ships the lens-engine skeleton + the cheapest lens-family migration (cognitive). Phase 131 is the source-lens family's pilot: `/mos:research` becomes the canonical "callable workflow step" -- it pre-reads context via navigation.cjs (active workflow / JTBD / operator / section / memory_event tail / evidence gaps / prior research dedup / persona role_blend), runs the source-lens rotation, surfaces findings via F.1 selector with computed candidate target sections, and wires accepted findings as typed `EvidenceClaim` nodes with INFORMS / CONTRADICTS / SUPERSEDES / REJECTED_BECAUSE cascade edges. The other 3 lens families (framework / trend / domain-via-SAPPhIRE) + 13 remaining research surfaces (scout / opportunities / scheduled-tasks / radar / GSD researchers) defer to v1.14.0 as scheduled migrations on the now-proven pattern.

**Phase 121.5 scope expansion (in-place, zero new logic):** the capstone's "truth-telling" pillar absorbs the naming-drift + discoverability cleanup the cluster audits surfaced:
- `commands/heal.md` → fold into `/mos:doctor --heal-room` (class E orchestrator)
- `commands/query.md` → alias for `/mos:graph "<question>"`
- `commands/organize.md` → fold into `/mos:rooms organize <verb>`
- `commands/visualize.md` → retire or fold into `/mos:dashboard --mermaid`
- `commands/hmi-status.md` → demote to admin-only (`visibility: admin` frontmatter)
- `commands/mos.md` → create (plugin.json declares it; file is missing)
- `commands/diagnostics.md` → rename to `/mos:fingerprint` (kills the diagnose/diagnostics ambiguity)
- Add `doctor` / `heal` / `hmi-status` to `commands/help.md` Infrastructure group
- Add F.1 selectors to `commands/onboard.md` Step 6 + `commands/diagnose.md` recommendation (Canon Part 3 compliance)

**Source synthesis documents:**
- 5-cluster command audits (~85 commands, all 4 dimensions: intent / suggestion / Brain / workflow composability), 2026-05-15
- v1.13.1 alignment audit, 2026-05-16 (this absorption)
- Cross-cutting mandates: M1 Substrate Contract, M2 Local Graph Awareness, M3 Lens-Engine Contract, M4 Workflow Composability

**Deferred to v1.14.0 (correctly, per audit):** P9 framework-lens migration (5 commands), P10 trend-lens consolidation (4→1), P11 SAPPhIRE Stage A/B execution, **P13 source-lens migration REMAINDER** (the 13 surfaces NOT covered by Phase 131 pilot: scout / opportunities / scheduled-tasks / radar / rs-fetch's research arm / 4 GSD researchers / file-meeting research / reanalyze / find-analogies external arm), P14 audit-engine consolidation, P15 score-innovation reconnection to compute-hsi.py, P16 file-meeting graph rewrite. All quality/hygiene, not coherence -- and all become "copy the Phase 131 template" once /mos:research proves the pattern. **Plus SEED-009 (learned ranker weights from outcome edges)** -- the DEFER half of the 2026-05-16 dual-graph review verdict; trigger gate is cohort >= 30 testers AND F-selector outcome edge count >= 1000 events; filed at `.planning/seeds/SEED-009-learned-ranker-weights-from-outcome-edges.md`.

**Final shape: 12 phases in v1.13.1, ~11 weeks.** Up from 11 phases / 10 weeks before the Phase 131 absorption. Up from 8 phases / 7-8 weeks in the original pre-synthesis plan.

## Dual-Graph Review Absorption (2026-05-16) -- additive primitives + deferral

Following the external DGEKT-inspired dual-graph proposal received 2026-05-16, three independent architectural reviewers ran (Canon compliance, Execution-plan contract, Adversarial framing) and converged on a synthesized verdict logged at `.planning/phases/121.5-terminal-coherence-capstone/121.5-CONTEXT.md` (top section) + `.planning/research/2026-05-16-dual-graph-architectural-proposal.md` (Section 10). **The DGEKT lens-class taxonomy was REJECTED as architectural ceremony; two minimal additive primitives ride existing v1.13.1 phase scope; the learned-weights piece DEFERS to v1.14.0 (SEED-009).**

| Additive primitive | Rides | Beta | Lines | What it does |
|---|---|---|---|---|
| `bin/local-chain-recommender.cjs` | **Phase 127** | v1.13.1-beta.1 | ~80 | Plain Tier-LOCAL helper sibling to `lib/brain/chain-recommender.cjs`. Closes the Tier-LOCAL predictive-surface gap Phase 127 itself opens: when Brain is unreachable, `chain-recommender.cjs` falls through to `predictNextLocal()` walking local `memory_event` aggregates (enum-only projections per Canon Part 8 fence). No new substrate; no lens-class taxonomy; pure additive. |
| `FOLLOWS_FROM` cascade edge type | **Phase 129** | v1.13.1-beta.3 | ~30 | Becomes the 8th type in the canonical cascade vocabulary (INFORMS / CONTRADICTS / CONVERGES / INVALIDATES / ENABLES / REJECTED / DEFERRED + FOLLOWS_FROM). Emitted by spine repair when one memory_event clearly follows another in the proactive loop. Enum-only properties; rejected the proposal's parallel "association layer" framing in favour of additive extension. |

**Why the framing was rejected:** the DGEKT mapping is vocabulary, not architecture (no analog supervisory regime in MindrianOS); "association lens" flattens the existing 8-type cascade vocabulary into one bucket; learned weights on a 4-tester cohort overfits; the hold-flag (since retracted) manufactured the urgency. Full reject-reason captured at `.planning/research/2026-05-16-dual-graph-architectural-proposal.md` Section 10 + Canon-Part-4-style "rejection is data" memory_event slated to land when the v1.13.1 primitives ship (`architectural_proposal_decided` event-type, additive via the existing memory-events.cjs:54-85 extension pattern).

**Three review deliverables (canonical references):**
- `.planning/research/2026-05-16-dual-graph-review-A-canon-compliance.md` (CANON-COMPLIANT-WITH-CONSTRAINTS; 7 binding constraints; Part 8 fence must be schema-layer, not policy-layer)
- `.planning/research/2026-05-16-dual-graph-review-B-execution-plan-contract.md` (PLAN-COMPLIANT-WITH-CONSTRAINTS; 127.1 ACCEPT / 128 ACCEPT-WITH-EDITS / 129 ACCEPT / 130 ACCEPT-WITH-EDITS / 131 REJECT-AS-WRITTEN; 8 binding constraints)
- `.planning/research/2026-05-16-dual-graph-review-C-adversarial.md` (PROPOSAL-IS-OVERSPECIFIED; 6 attacks held under their own evidence)

**Effect on the 12-phase / ~11-week envelope:** ZERO. Both additive primitives ride existing phase scope; no new phases added; no beta targets shifted; no wave dependencies reordered. The proposal's lens-class amendments to phases 127.1/128/129/130/131 are REJECTED -- those CONTEXT.md files keep their current scope without adding ASSOCIATION_LENS / TRANSITION_LENS vocabulary.

## The wave-by-wave dependency chain

```
═══════════════════════════════════════════════════════════════════════════
   AUDIT-CORRECTED CURRENT STATE (as of 2026-05-14, post self-audit)
═══════════════════════════════════════════════════════════════════════════

   SHIPPED (verified via git log + STATE.md + phase summaries):
     Phase 110 packet contract ✓   Phase 123 install-harness ✓
     Phase 124 feynman-temporal ✓   Phase 122 workflow layer ✓
     Phase 104.1 per-command teaching content ✓ (86 commands gained `teaching:` field)
     Phase 125 F-Selector Ranker ✓ (all 9 plans, 12/12 must-haves, 8/8 GREEN)
     Phase 126 install-lifecycle-harness-gaps ✓ (beta.16, tag pushed)
     Phase 126.1 hotfix ✓ (beta.17 bundle -- 3 bugs: pre-flight tier / marketplace bump / Step 9.7 sandbox)
     Phase 118 30-second-MVA ✓ (beta.17 bundle -- 6 sub-plans: six-agents / progressive-streaming /
       feynman-deck-vercel / mva-option / rule-linter / dror-harness)

   SHIPPED VERSION (CURRENT, AUTHORITATIVE per git log 2026-05-16):
     v1.13.0-beta.17 ✓ (commit eb752cf5, 2026-05-16; tag pushed; marketplace synced;
       npm @mindrian_os/install@1.13.0-beta.17 published @next)
     HEAD at bf3a7a15 -- bumped to v1.13.0-beta.18 (Commit B, next pre-release WIP)
     7-place lockstep green except install-minisite (skipped per --no-minisite,
       deferred until Vercel-to-GitHub wiring -- known issue, not a regression)

   AUDIT 2026-05-14 (sub-agent verified all 6 SUMMARY files):
     Phase 88.2  SHIPPED -- 7 plans, F.0/F.1/F.2/F.3 selector blocks all live
     Phase 89    SHIPPED -- 11 plans, ReverseSalientAgent in v1.13.0-beta.4
     Phase 114   SHIPPED -- 3 plans, Larry default-activation in beta.2
     Phase 115   SHIPPED -- 5 plans, owned-emotion dual-path
     Phase 116   SHIPPED -- 5 plans + 89/89 tests, tension-hook in beta.5
     Phase 117   SHIPPED -- 6 plans + 49 tests, auto-explore in beta.8

     The CONTEXT.md `status:` frontmatter for each phase is STALE -- set at
     scoping time, never updated post-ship. Authoritative source is the
     last SUMMARY file (or git log).

     DO NOT trust phase CONTEXT.md `status:` going forward. Read the most
     recent SUMMARY file or grep git log for the phase's release commit.

   TRUE STUBS (remaining v1.13.0 work; confirmed remaining as of 2026-05-16):
     Phase 119  room-as-receipt-invariant         (next stub; deps 118 ✓)
     Phase 120  breakthrough-scan-category-g      (parallel; deps 117 ✓)
     Phase 121  trajectory-telemetry              (parallel; deps 109 ✓)
     Phase 121.5 terminal-coherence-capstone      (capstone; needs 119+120+121)

   KNOWN ISSUE -- LOGGED FOR 126.2-hotfix (recorded 2026-05-16):
     Step 9.7's HOME-override sandbox breaks npx's bin-linking.
     Two recommended fixes documented in .planning/quick/260515-rd1/deferred-items.md:
       - Option A2 (recommended for 126.2): replace HOME-override with
         npm install --prefix=$SANDBOX -- keeps sandbox in-repo, simpler test
       - Option B (v1.14.0+): extend ~/mindrianos-install-site/ npm-installer
         with --target=<dir> flag; retires the hack
     The release itself is unaffected. Real testers running
     `npx --yes @mindrian_os/install@1.13.0-beta.17` get a working install.
     Phase 126.2-hotfix ~1 day; unblocks clean Step 9.7 pass on beta.18+ cuts.

   SCOPE-LOCKED, READY FOR plan-phase (as of 2026-05-16):
     Phase 119  room-as-receipt-invariant (next; 3-5 days)
     Phase 126.2-hotfix (Step 9.7 sandbox redesign; ~1 day)
     Phase 121.5 statusline co-design (discuss-phase first; 30-60 min)
     Phase 127  brain-mcp-local-stdio-shim (the v1.13.1 architectural anchor)
     Phase 128  substrate-contract-adr (ABSORBED 2026-05-16; CONTEXT.md scoped)
     Phase 129  spine-repair-memory-event (ABSORBED 2026-05-16; CONTEXT.md scoped)
     Phase 130  lens-engine-skeleton + cognitive migration (ABSORBED 2026-05-16; CONTEXT.md scoped)
     Phase 131  research-as-graph-aware-workflow-step (ABSORBED 2026-05-16; CONTEXT.md scoped)

═══════════════════════════════════════════════════════════════════════════

   WAVE 0  (SHIPPED -- v1.13.0-beta.14 cut succeeded 2026-05-14)
   ─────────────────────────────────────────────────────────────────────
   Phase 125 + Phase 104.1 shipped together as v1.13.0-beta.14.
   Path A executed cleanly (commit c00e41f reverted stale pre-bump).

   ─────────────────────────────────────────────────────────────────────
   WAVE 1  (SHIPPED -- v1.13.0-beta.16, 2026-05-15)
   ─────────────────────────────────────────────────────────────────────
   Phase 126 install-lifecycle-harness-gaps ✓ SHIPPED beta.16
     7 plans landed; install-minisite version-sync work per
     feedback_install_minisite_lockstep.md (7-place lockstep code shipped).
     Family pre-mortem doc shipped at docs/install-cache-family-premortem.md.

   ─────────────────────────────────────────────────────────────────────
   WAVE 1.1  (SHIPPED -- v1.13.0-beta.17, 2026-05-16, hotfix + Phase 118 bundle)
   ─────────────────────────────────────────────────────────────────────
   Phase 126.1-hotfix ✓ SHIPPED beta.17
     3 bugs fixed:
       Bug 1: pre-flight tier restored + Test 2 restored
       Bug 2: marketplace.json bump moved to Commit B
       Bug 3: Step 9.7 sandbox added (HOME-override approach; KNOWN ISSUE
              -- breaks npx bin-linking; 126.2-hotfix scheduled)
   Phase 118 30-second-MVA ✓ SHIPPED beta.17 (bundled with hotfix)
     6 sub-plans:
       118-02 six-agents (dashboard-graph-neighborhood agent + ALL_AGENTS index)
       118-03 progressive-streaming
       118-04 feynman-deck-vercel (mva-deck-builder + Vercel deploy)
       118-05 mva-option-router + transitionViaMVAOption + /mos:mva-option
       118-06 rule-linter + dror-harness (Dror 2.0 acceptance test runner)
     HUMAN-UAT + VERIFICATION persisted.
   7-place lockstep green except install-minisite (--no-minisite skip).
   Working tree at beta.18 (Commit B next-bump per release.sh convention).

   ─────────────────────────────────────────────────────────────────────
   WAVE 2  (REMAINING v1.13.0 PART-10 PHASES -- post-beta.17 ship state)
   ─────────────────────────────────────────────────────────────────────
   Per ROADMAP.md, v1.13.0 final requires:
     Phase 118  30-second-mva                  ✓ SHIPPED beta.17 (see Wave 1.1)
     Phase 119  room-as-receipt-invariant      (true stub, 0 plans)
     Phase 120  breakthrough-scan-category-g   (true stub, 0 plans)
     Phase 121  trajectory-telemetry           (true stub, 0 plans)

   Dependency order (per ROADMAP + CONTEXT.md cross-references):
     118 ✓ SHIPPED beta.17 -- closed
     119 needs 118 ✓ -- unblocked
     120 needs 117 ✓ -- unblocked, parallel
     121 needs only 109 ✓ -- unblocked, parallel

   Plus Phase 126.2-hotfix (Step 9.7 sandbox redesign; ~1 day; known issue
   logged in .planning/quick/260515-rd1/deferred-items.md). Can slot into
   any beta.X cut as a low-risk hotfix.

   Stream A: /gsd:plan-phase 119 -> execute -> beta.18 or beta.19
   Stream B: /gsd:discuss-phase 120 -> plan -> execute -> next beta (parallel to A)
   Stream C: /gsd:discuss-phase 121 -> plan -> execute -> next beta (parallel)
   Stream D: /gsd:plan-phase 126.2-hotfix -> execute -> next beta (parallel, ~1 day)

   ┌─────────────────────────────────────────────────────────────────┐
   │ PHASE 118 DISCREPANCIES (discovered 2026-05-14 during the       │
   │ /gsd:discuss-phase 118 source-spec read; must resolve BEFORE    │
   │ plan-phase fires)                                                │
   ├─────────────────────────────────────────────────────────────────┤
   │ 1. Source spec relocated by 2026-05-06 restructure.             │
   │    Old path: ~/MindrianRooms/mindrian/mindrian-ecosystem/       │
   │              sub-rooms/website/mindrianos-conversion-fix/...    │
   │    New path: ~/MindrianRooms/mindrian/mindrianOS/sub-rooms/     │
   │              communications/conversion-fix/solution-design/     │
   │              the-30-second-mva.md + reward-before-investment-   │
   │              rule.md                                             │
   │    118-CONTEXT.md still references the old path. The new path   │
   │    is the only one with the actual spec on disk.                │
   │                                                                  │
   │ 2. The 6-agent list disagrees between source spec and           │
   │    118-CONTEXT.md.                                               │
   │    Source spec (the design doc, authoritative): Brain:similar-  │
   │      ventures / Brain:cross-domain-analogies / Brain:classic-   │
   │      traps / Tavily:funding-scan / Six-hats:red-black-flags /   │
   │      Dashboard:pre-render-graph-nodes.                          │
   │    CONTEXT.md (abstract, written later): research / persona /   │
   │      market / evidence / contradiction / synthesis.             │
   │    Source spec is more specific and concrete -- prefer it as    │
   │    the planning input. Discuss-phase must resolve before plans  │
   │    get written.                                                  │
   │                                                                  │
   │ 3. Bilingual scope (Hebrew + English) per source spec line 122  │
   │    vs CONTEXT.md "English only in v1.13.0" out-of-scope.        │
   │    Bilingual adds: language detection + Hebrew RTL deck         │
   │    rendering + bilingual Larry voicing. Pragmatic call needed.  │
   │                                                                  │
   │ 4. Phase 119 dependency arrow is CONTESTED.                     │
   │    CONTEXT.md says "119 depends on 118". Source spec says the   │
   │    3-option footer option 2 ("Build a room around this") is     │
   │    the Phase 119 invest path -- meaning 118 NEEDS 119 to land   │
   │    the option 2 wiring, OR 118 ships option 2 as a stub.        │
   │    Three sequencing options before plans:                        │
   │      A. Phase 118 first, option-2 stub; Phase 119 wires later.  │
   │         Sequential. Two betas (118->117, 119->18). Simplest.    │
   │      B. Phase 118 + Phase 119 ship as one phase or co-ordered   │
   │         cut. Beta.17 ships both. Cleaner UX, riskier scope.     │
   │      C. Phase 119 first (1d, smaller), then Phase 118 can wire  │
   │         option-2 against the now-existing wrapper. Sequential   │
   │         but reversed.                                            │
   │                                                                  │
   │ 5. Eight other gray areas captured for discuss-phase:           │
   │    (a) Vercel ephemeral deploy mechanism (CLI vs API; auth)     │
   │    (b) "Fast Claude mini model" for detection -- Haiku 4.5?     │
   │        Or heuristic regex? (cost + latency tradeoff)            │
   │    (c) Hard 45s budget enforcement: Promise.race +              │
   │        AbortController? per-agent vs global wall-clock?         │
   │    (d) Progressive streaming UX: agent-returns-trigger output,  │
   │        or fixed time slots per source spec timeline?            │
   │    (e) Failure detection: empty result, exception, timeout?     │
   │    (f) 3-option footer routing (esp. option 1 = "tell me more"  │
   │        stays in mode -- what mode, where renders, how exits?)   │
   │    (g) Brief output destination on option-2 invest: room.db?    │
   │        local file? Vercel-only persistence?                     │
   │    (h) Dror 2.0 acceptance test runner: synthetic? real user?   │
   │        wired to Phase 121 telemetry for measurement?            │
   │                                                                  │
   │ Ship-target impact: source spec says 3-5 days; CONTEXT.md says  │
   │ 5 days. Discrepancies + sequencing call may push to 5-7 days    │
   │ if bilingual + option-A sequential. Re-estimate in discuss.     │
   └─────────────────────────────────────────────────────────────────┘

   ─────────────────────────────────────────────────────────────────────
   WAVE 3  (THE CAPSTONE -- v1.13.0 FINAL gate)
   ─────────────────────────────────────────────────────────────────────
   Phase 121.5 terminal-coherence-capstone   (true stub, 0 plans)
     CONSOLIDATION ONLY. No new surface. Per Canon Part 7.
     Wraps everything shipped across v1.13.0 into ONE coherent terminal
     experience.
   → ships v1.13.0-beta.20 (or whatever the running beta is; adjusted from
     beta.19 per the 2026-05-14 shift-by-1 correction. The "running beta" hedge
     stays accurate -- the number is set by the sum of Wave 2 plan cuts, not
     by this plan.)
   v1.13.0 FINAL closes here.

   ─────────────────────────────────────────────────────────────────────
   WAVE 2  (CRITICAL -- the Brain MCP architectural shift; architectural unlock)
   ─────────────────────────────────────────────────────────────────────
   Phase 127 brain-mcp-local-stdio-shim   (scope-locked 2026-05-14)
     bin/mindrian-brain-mcp-client.cjs (~150 lines)
     DirectiveEnvelope typed packet (default mode: GUIDED)
     Two-tier model: LOCAL / CONNECTED
     Auto-migration removes legacy HTTP-transport entries
     Doctor Class K end-to-end Brain smoke test
     + bin/local-chain-recommender.cjs (~80 lines additive primitive
       per 2026-05-16 dual-graph review verdict; Tier-LOCAL helper)
   → ships v1.13.1-beta.1
   AFTER THIS POINT: existing testers' wiring auto-migrates; new installs do
   2 steps total (install + key); Brain works zero-config; every Part 10 phase
   ASSUMES Brain works.

   ─────────────────────────────────────────────────────────────────────
   WAVE 3  (the Part 10 GATE -- single phase, blocks 4 phases)
   ─────────────────────────────────────────────────────────────────────
   Phase 114 larry-default-activation
   → ships v1.13.1-beta.2
   UNLOCKS: 115, 117, 118, 119, 120

   ─────────────────────────────────────────────────────────────────────
   WAVE 4  (6 streams parallel, all build on 114)
   ─────────────────────────────────────────────────────────────────────
   Stream A: Phase 115 owned-emotion dual-path  (deps: 114)
   Stream B: Phase 116 unresolved-tension-hook   (no deps)
   Stream C: Phase 89-07 FINISH (1 plan) → Phase 117 auto-explore
             (117 needs 89-07 + 109 + 127 -- all available)
   Stream D: Phase 121 trajectory-telemetry instrument
             starts capturing through W4-W6
   Stream E: Phase 128 substrate-contract-adr  (ABSORBED 2026-05-16; deps: none)
             docs/architecture/SUBSTRATE-CONTRACT.md (M1 codification: Local SQLite via
             navigation.cjs / Aura / Brain / Pinecone -- never cross streams) +
             scripts/check-substrate.cjs as pre-commit CI guard scanning for direct
             fs.readFile(room.db) outside navigation.cjs + lib/**/*.cjs imports of
             node:sqlite outside navigation.cjs. ~2 days.
   Stream F: Phase 129 spine-repair-memory-event  (ABSORBED 2026-05-16; deps: 109)
             Refactor 6 silent spine scripts (mos-status.cjs, suggest-next-command.cjs,
             act-command.cjs, pipeline-command.cjs, jtbd-command.cjs, operator-command.cjs,
             memory-command.cjs) to read via navigation.cjs chokepoint and emit
             memory_event on every state transition. Closes the broken backward arc of
             the proactive loop. EVENT_TYPES additive: cap at 5 new types.
             + FOLLOWS_FROM cascade edge type as 8th additive type in the cascade
               vocabulary (per 2026-05-16 dual-graph review verdict; ~30 lines additive
               to lib/core/navigation/memory-events.cjs; enum-only properties per
               Canon Part 8 fence). ~3-4 days total.
   → all ship v1.13.1-beta.3

   ─────────────────────────────────────────────────────────────────────
   WAVE 5  (FUSION -- the visible payoff)
   ─────────────────────────────────────────────────────────────────────
   Phase 118 30-second-MVA  (deps: 114+115+116+117)
     HYBRID by default (autonomous prep + guided commit gate)
   Phase 120 breakthrough-scan-category-g  (parallel; deps: 117+109)
   → ships v1.13.1-beta.4

   ─────────────────────────────────────────────────────────────────────
   WAVE 6  (ROOM AS RECEIPT)
   ─────────────────────────────────────────────────────────────────────
   Phase 119 room-as-receipt-invariant  (deps: 114+115+117+118)
   → ships v1.13.1-beta.5

   ─────────────────────────────────────────────────────────────────────
   WAVE 6.5  (LENS-ENGINE FOUNDATION -- ABSORBED 2026-05-16)
   ─────────────────────────────────────────────────────────────────────
   Phase 130 lens-engine-skeleton + cognitive-family migration
     (deps: 109 + 128 + 129)
     lib/core/lens-engine.cjs ships with:
       - Interface: rotate({ lensType, lensSet, input, perLensFn,
         synthesize, surfaceSelector, persistence, onAccept, onReject })
       - 5 lens-family registry (domain/cognitive/source/framework/trend)
         -- only the cognitive family populated in v1.13.1
       - Built-in synthesizers: tension-map, comparison-matrix, convergence-map
       - F-shape selector dispatch (F.0/F.1/F.4) via existing 88.2 substrate
       - Mandatory memory_event emission per rotation + per selector outcome
       - Rejection-as-data edge (REJECTED_BECAUSE) per Canon Part 4
     Cognitive-lens family migrated (4 commands):
       - think-hats, persona, hat-briefing, challenge-assumptions become
         thin lens-engine clients (~15 lines of frontmatter each)
       - Hats state migrates from filesystem (.mindrian/hats/{color}/STATE.md)
         to room.db typed nodes via navigation.cjs (closes the latent Canon
         Part 9 violation flagged in the Cluster 2 audit)
     Other 4 lens families (framework/trend/source/domain-via-SAPPhIRE) are
     SCHEDULED for v1.14.0 migration; registry slots reserved, no clients yet.
     EXCEPT: source-lens family gets a PILOT in Wave 6.7 below (Phase 131).
     ~4-5 days.
   → ships v1.13.1-beta.5b

   ─────────────────────────────────────────────────────────────────────
   WAVE 6.7  (SOURCE-LENS PILOT -- ABSORBED 2026-05-16)
   ─────────────────────────────────────────────────────────────────────
   Phase 131 research-as-graph-aware-workflow-step
     (deps: 109 + 110 + 127 + 128 + 129 + 130)
     The source-lens family's PILOT via /mos:research. After this phase,
     /mos:research is no longer a fortune-cookie command -- it is the
     canonical workflow step that other methodologies can dispatch.

     PRE-FLIGHT (navigation.cjs reads, 8 inputs):
       1. Active workflow      ← /mos:act state / calling command
       2. Active JTBD          ← /mos:jtbd
       3. Operator mode        ← /mos:operator
       4. Current section      ← room location + STATE.md focus
       5. Recent memory_event  ← last 10 transitions (conversation arc)
       6. Evidence gaps        ← claims with review_status:needs_evidence
       7. Prior research       ← Pinecone semantic dedup on topic
       8. Persona role_blend   ← Phase 115 framing

     EXECUTION (lens-engine source-lens rotation):
       - Source lens set computed from context (e.g., financial-model gap
         → scholarly + industry + patent; thesis-build JTBD → add Brain
         methodology chain; persona=investor → add competitive-intel weight)
       - Pre-egress audit (Canon Part 8)
       - Parallel fetch (Tavily / OpenAlex / arXiv / PubMed / industry /
         Brain Cypher), deduped against prior research
       - HSI-scored findings

     FINDINGS PRESENTATION:
       - 5 findings ranked by relevance + evidence-tier (Canon Part 5)
       - Each finding pre-mapped to candidate room location(s) with %
         match score against the section's existing claim graph
       - Persona-aware framing per Phase 115 role_blend

     F.1 FILING SELECTOR (per Canon Part 3, tri-context):
       - File to <primary section>   (recommended, N% match)
       - File to <secondary section> (M% match)
       - Split: file primary to X, reference to Y
       - Defer to milestone audit
       - Reject (capture reason → REJECTED_BECAUSE edge per Canon Part 4)

     WIRING (navigation.cjs typed-node writes):
       ACCEPT path:
         - EvidenceClaim node written (review_status: proposed per Part 9)
         - INFORMS edge → target section's primary claim
         - CONTRADICTS edge if finding kills an existing claim
         - SUPERSEDES edge if better-evidence-tier version found
         - memory_event: research_filed (provenance: URL + timestamp + tier)
       REJECT path:
         - REJECTED_BECAUSE edge (reason captured per Part 4)
         - memory_event: research_rejected (teaches next run)
       DEFER path:
         - DEFERRED memory_event, queued to milestone audit

     POST-FILING (chain back to caller):
       - If called BY another methodology: return EvidenceClaim IDs so
         the calling command can resume with the evidence it needed
       - If called STANDALONE: surface F.1 next-move selector

     Deliverables:
       - lib/core/research-context-extractor.cjs (pre-flight reads)
       - lib/lens-engine/source-lens-driver.cjs (uses Phase 130 substrate)
       - lib/core/findings-wirer.cjs (typed-edge writer: INFORMS /
         CONTRADICTS / SUPERSEDES / REJECTED_BECAUSE)
       - commands/research.md rewritten to invoke the pipeline above
       - scripts/check-research-isomorphism.cjs (CI guard asserting
         output structure matches the typed-node contract)
       - Tests: 5 E2E (standalone / called-by-build-thesis /
         called-by-user-needs / rejection-as-data / dedup-against-prior)

     Pattern proven here = template for v1.14.0 migration of the other
     13 research surfaces (scout / opportunities / scheduled-tasks /
     radar / rs-fetch's research arm / 4 GSD researchers).
     ~5-7 days.
   → ships v1.13.1-beta.5c

   ─────────────────────────────────────────────────────────────────────
   WAVE 7  (THE CAPSTONE -- gives all of it user-facing life)
   ─────────────────────────────────────────────────────────────────────
   Phase 121.5 terminal-coherence-capstone
     CONSOLIDATION ONLY. NO new surface. Per Canon Part 7.
     Wraps EVERYTHING shipped across v1.13.0 + v1.13.1 into ONE
     coherent Claude Code terminal experience.

     Naming-drift + discoverability cleanup ABSORBED 2026-05-16 (zero new
     logic, fits the capstone's "truth-telling" pillar):
       - commands/heal.md → fold into /mos:doctor --heal-room (class E)
       - commands/query.md → alias for /mos:graph "<question>"
       - commands/organize.md → fold into /mos:rooms organize <verb>
       - commands/visualize.md → retire or fold into /mos:dashboard --mermaid
       - commands/hmi-status.md → demote (visibility: admin)
       - commands/mos.md → CREATE (plugin.json declares it; file missing)
       - commands/diagnostics.md → rename to /mos:fingerprint
       - help.md Infrastructure group → add doctor/heal/hmi-status entries
       - onboard.md Step 6 + diagnose.md → wire F.1 selectors (Part 3 fix)

     HARD REQUIREMENT (added 2026-05-16): bulletproof ASCII /mos:help renderer
       Current help.md uses ANSI 24-bit color escapes (e.g. \033[38;2;166;61;47m).
       These break on terminals without truecolor support, on Desktop's chat
       surface, and in any environment that pipes/copies output. The capstone
       MUST ship a renderer that:

       1. RENDERS IDENTICALLY across CLI / Desktop / Cowork
          - Pure ASCII fallback path that uses ONLY the 12-glyph vocabulary
            (■ ▼ ▶ ▷ ├─ └─ ✓ • ⚠ ⚡ ⬜ →) -- no ANSI codes
          - Color path remains for CLI when $TERM advertises truecolor AND
            stdout is a TTY; otherwise ASCII fallback
          - Detection logic in scripts/help-renderer.cjs (new), not inline prose

       2. EXPORT / PUBLISH / HUB section gets its OWN top-level Zone 2 group
          named "Ship + Share" with explicit JTBD "what's in it for me" lines:
          - /mos:export      → "Walk out with a PDF dataroom investors can read offline"
          - /mos:present     → "Open one URL that shows your venture in 6 views at once"
          - /mos:publish     → "Get a live URL stakeholders can visit -- no install needed"
          - /mos:dashboard   → "See your knowledge graph in a browser with chat alongside"
          - /mos:wiki        → "Browse your room like Wikipedia -- linked, searchable"
          - /mos:rooms new --github → "Back your room up to GitHub with one command"
          - /mos:room linkify       → "Export your room to an Obsidian-readable vault"
          - /mos:scheduled-tasks    → "Cowork: keep your room running while you sleep"

       3. EVERY description passes the "what's in it for me" test:
          - User reads the line → user thinks "I want that" -- not "what does it do?"
          - No framework names. No tool jargon. The outcome the user walks
            away with.
          - Banned: "generates X", "runs Y", "manages Z" -- replace with
            second-person outcome ("walk out with X", "see your Y", "ship Z")

       4. EVERY command in the plugin (~85 today) appears SOMEWHERE in /mos:help.
          The Cluster 5 audit found 3 commands (doctor/heal/hmi-status) silently
          missing from the Infrastructure group. CI guard: scripts/check-help-
          coverage.cjs scans commands/ glob + frontmatter for `visibility: admin`
          gate and asserts every visible command appears in help.md groups.
          Fails pre-commit on drift.

       5. ZERO RENDERING FAILURES under torture-test:
          - Test fixture: pipe /mos:help through tr -d $'\033', grep for sentinel
            glyphs in each group, assert no raw \033 in plain-ASCII path
          - Test fixture: capture /mos:help in non-TTY (heredoc, file redirect)
            and assert output is ASCII-only
          - Test fixture: render /mos:help on Desktop simulator (no ANSI) and
            verify human-readable

       Deliverables in 121.5:
       - scripts/help-renderer.cjs (truecolor-vs-ASCII switch)
       - commands/help.md rewritten to delegate rendering to scripts/help-renderer.cjs
       - data/help-groups.json (single source of truth for group→commands→JTBD lines)
       - scripts/check-help-coverage.cjs (CI guard)
       - tests/test-help-renderer-bulletproof.cjs (3 torture tests above)

       This requirement supersedes the lighter "add doctor/heal/hmi-status" bullet
       above -- the bulletproof renderer + data/help-groups.json + CI guard
       together fix that visibility gap structurally, not by hand-editing the
       prose.

     Housekeeping included in 121.5's "truth-telling" pillar
     (carried forward from the v1.13.0-beta.14 release session 2026-05-14):

       (a) Commit the 3 GSD toolchain patches sitting in
           ~/.claude/get-shit-done/ HOME git (cross-phase tool bugs caught
           during the beta.14 cut; affect every release in the family,
           not just MindrianOS-Plugin):
         - bin/lib/phase.cjs:256-258
             `parseInt(fm.wave, 10) || 1` coerced wave: 0 to wave: 1 via
             JS truthiness, causing dependency-violating parallel spawns
             when a phase had a wave-0 plan. Replaced with
             `Number.isFinite(waveParsed) ? waveParsed : 1`.
         - bin/lib/template.cjs:121-124
             Same wave: 0 truthiness bug in the plan-template generator
             (`--wave 0` on the CLI silently became wave 1 in generated
             frontmatter). Same Number.isFinite guard.
         - bin/lib/frontmatter.cjs:11-18
             extractFrontmatter matched ALL `---...---` blocks anywhere
             and used the LAST one (a CRLF-corruption heuristic) --
             markdown body content separated by --- horizontal rules
             was misidentified as frontmatter, shadowing real fields
             like `wave:`. Aligned with spliceFrontmatter's ^---
             file-start anchor.
         Suggested commit message:
           `fix(gsd-tools): wave: 0 truthiness coercion + unanchored frontmatter parser`

       (b) Wire up a git remote on ~/mindrianos-install-site/ so commits
           back up to GitHub (currently local-only; Vercel-CLI-driven
           deploys work but there's no offsite source-of-truth).

       (c) Replace the two hardcoded version strings in the install
           minisite (`lib/os.ts:149` + `app/page.tsx:30`) with a
           NEXT_PUBLIC_MINDRIAN_VERSION env var on the Vercel project
           OR a build-time `npm view @mindrian_os/install version` fetch.
           Either eliminates Step 9.6's manual surface in release.sh
           (the auto-sync still runs, but the action becomes a no-op
           when the runtime fetches from registry directly).

       See: feedback_install_minisite_lockstep.md (HARD enforcement code
       shipped in Phase 126; live from beta.16 onward via release.sh Step 9.6
       -- (a)/(b)/(c) above are the long-term cleanup that retires the
       manual surface. Note: beta.16 cut intentionally skipped Step 9.6 via
       `--no-minisite` opt-out because Vercel-to-GitHub wiring is still
       deferred; first real Step 9.6 end-to-end observation happens on the
       cut that follows the minisite-origin bootstrap.).
   → ships v1.13.1-beta.6

═══════════════════════════════════════════════════════════════════════════
   v1.13.1 FINAL RELEASE GATE
═══════════════════════════════════════════════════════════════════════════
```

## Critical path

```
125-finish ──► 126 ──► v1.13.0 ──► 127 ──► 114 ──► 117 ──► 118 ──► 119 ──► 121.5 ──► v1.13.1
   ranker    install   hardening   BRAIN   Larry  explore   MVA   receipt  capstone
            harness    closes      shift   gate
```

**9 sequential layers.** Phase 127 in the middle (architectural anchor). Phase 121.5 at the end (user-facing anchor).

## What can run in parallel right now

5 streams unblocked, can fire today:

| Stream | Phase | Status | Path |
|---|---|---|---|
| 1 | Phase 125 plans 07 + 08 | in flight | finish; the release-pipeline Path A cuts beta.14 |
| 2 | Phase 126 | design-locked | `/gsd:plan-phase 126` → execute |
| 3 | Phase 127 | design-locked (this conversation 2026-05-14) | `/gsd:plan-phase 127` → execute |
| 4 | Phase 114 | stub | `/gsd:discuss-phase 114` → context-lock → plan |
| 5 | Phase 116 | ready-for-planning | `/gsd:plan-phase 116` → execute |

Plus residuals (low priority):
- Phase 89-07 FINISH (1 plan) -- unblocks 117
- Phase 88.2 FINISH (3 plans) -- polish, no downstream blockers

## Calendar estimate

| Wave | Work | Days |
|---|---|---|
| W0 | 125-07 + 125-08 finish | 3 |
| W1 | 126 plan + execute | 5 |
| **v1.13.0 final ships** | stabilization | 2 |
| W2 | 127 (CRITICAL) | 5-7 |
| W3 | 114 | 3-5 |
| W4 | 4 streams parallel | 5-7 |
| W5 | 118 + 120 parallel | 5-7 |
| W6 | 119 | 3-5 |
| W7 | 121.5 (CAPSTONE) | 5-7 |
| **v1.13.1 final ships** | stabilization | 2 |
| **Total** | | **~38-50 days (5.5-7 weeks)** |

Aggressive: 6 weeks. Realistic: 7-8 weeks.

## Scope-trim levers (if calendar pressure is real)

| Defer | Save | Lose |
|---|---|---|
| Phase 120 breakthrough-scan to v1.14.0 | ~5 days off W5 | Hooked Fix 3 amplifier (118 still delivers reward-before-investment) |
| Phase 88.2 selector block residuals to v1.14.0 | ~3 days off W4 | Polish on selector block (core function ships in 117/118) |

Default: keep both. Defer only if W5/W6 slip past day 35.

## What this plan does NOT include (deferred to v1.14.0)

- **Local-first SQLite snapshot of Brain methodology** (Tier 0.5 offline mode). The 2-tier model (LOCAL = thinking partner without methodology / CONNECTED = with methodology orchestrator) is the v1.13.1 ship shape. Tier 0.5 with bundled methodology data is a future architectural layer.
- **Collapsing 6 user-facing Brain tools to brain_ask only.** All 6 stay live in v1.13.1 (admin tools survive in plain sight). The collapse is a UX simplification deferred to v1.14.0.
- **Phase 100 jtbd-inference-engine** (sub-claim 4 of Canon Part 10 -- commands fully internal). Until v1.14.0, commands stay user-facing as a fallback while Larry routes via heuristic.

## Known issues -- must close before v1.13.1 FINAL release gate

Caught outside the wave-by-wave phase plan (regressions in shipped code from v1.10.14 / Phase 88.6). Filed as `.planning/quick/` entries; slot into any beta.X cut as low-risk hotfixes, but the FINAL release gate does not pass until both close.

| Quick ID | Issue | Severity | Discovered |
|---|---|---|---|
| `260515-wsr` | `/mos:whitespace map` CLI renderer reads wrong JSON fields -- Zone shows `?`, Density shows raw log-likelihood (-49 not 0-1), Type defaults to `Un-Defined`, Nearest Frameworks blank, summary line `Sparsest: undefined` | medium | 2026-05-15 ador-ip-test demo |
| `260515-wsk` | `/mos:whitespace map` KNN nearest-neighbor collapsed -- all 77 gaps point to the SAME `nearest_room_artifacts[0]`; analyze-room emits silent "command failed" warning; 7 of 37 artifacts silently filtered. Misdiagnoses rooms by flagging frameworks the room already has (Legal DD / Root Cause / Red Team / Financial DD) as biggest gaps. Upstream of Phase 89-07 + Phase 117 (both need neighbor diversity). | high | 2026-05-15 ador-ip-test demo |

Both bugs are Canon Part 2 Engine 1 (Whitespace Map -- Act 1 intelligence surface) quality regressions. Fix terrain is plumbing-level (CLI field reads, KNN compute, analyze-room dependency); no design change required.

## Cross-references

- `docs/MINDRIAN-CANON.md` -- the canon parts this milestone serves (especially Parts 6, 8, 9, 10, 11)
- `docs/CANON-PHASE-MAP.md` -- the canon-to-phase mapping table; v1.13.1 phases roll into the existing map
- `docs/CAPABILITY-MAP.md` -- Brain ↔ Plugin contract; Phase 127 is the consumer of DirectiveEnvelope row #1
- `feedback_larry_pedagogical_guided_first.md` -- GUIDED-default canon for DirectiveEnvelope
- `project_brain_mcp_three_track_transition.md` -- the three-track transition plan (existing testers / new installs / auto-migration)
- `.planning/phases/127-brain-mcp-local-stdio-shim/127-CONTEXT.md` -- the architectural anchor
- `.planning/phases/121.5-terminal-coherence-capstone/121.5-CONTEXT.md` -- the user-facing anchor
- `.planning/quick/260515-wsr-whitespace-cli-renderer-fields/` -- whitespace CLI renderer bug
- `.planning/quick/260515-wsk-whitespace-knn-nearest-collapse/` -- whitespace KNN collapse bug
