---
milestone: v1.11.0
milestone_name: "Memory Triple + Navigation Engine + Reverse Salient Discovery"
status: in-progress
filed: 2026-04-26
purpose: cross-session handoff doc. Fresh session reads this BEFORE invoking /gsd:plan-phase 89.2 to recover full context.
preserves:
  - What shipped in this session (89.1a + 89.1, both verified passed)
  - Architectural decisions locked (Option 1 shape adapter, Option A canon-write, Rule-3 transport switch)
  - Live Brain state (USES_TECHNIQUE edge filed, Mode A3 reachable)
  - Push state (everything pushed except 1 docs commit)
  - What 89.2 needs to ship + which deferred items to resolve
  - Exact kickoff command for the new session
---

# v1.11.0 Resume Handoff: Phase 89.2 (External Research Fetching)

## TL;DR for Fresh Claude

You are resuming v1.11.0 milestone execution mid-stream. **89.1a and 89.1 are done.** Next phase is **89.2 (External Research Fetching, EXPANDED per kickoff Intent 6)** — the biggest single sub-phase of v1.11.0 (~7-9 plans per kickoff §7).

Workspace: `/home/jsagi/MindrianOS-Plugin/` (NOT the plugin cache at `~/.claude/plugins/mindrian-os/` — workspace guard rule).

## Read these BEFORE doing anything

1. `.planning/milestones/v1.11.0-KICKOFF.md` — full milestone authority (489 lines). Read sections §3, §5 (Phase 1.5 External Research Fetching), §6 (chain mechanism intent), §8 (Canon Compliance), §9 (Implementation Guidance), §10 (prerequisite artifacts).
2. `.planning/milestones/v1.12.0-LIVE-VALIDATION-2026-04-24.md` §4 — bidirectional NL-graph context (forward reference; informs 89.5 but grounds 89.2 fetcher contracts).
3. This file (you are here).
4. `.planning/phases/89.1a-brain-methodology-substrate-loader/` (full directory) — what 89.1a shipped, especially `89.1a-04-SUMMARY.md` Phase Gate template + `89.1a-LIVE-BRAIN-SMOKE.md` audit-trail discipline.
5. `.planning/phases/89.1-domain-analysis-and-query-matrix/` (full directory) — what 89.1 just shipped, especially `89.1-VERIFICATION.md` (7/7 SCs passed), `89.1-PHASE-GATE.md` (13/13 PASS), `89.1-CANON-WRITE-EVIDENCE.md` (live USES_TECHNIQUE edge proof).
6. `CLAUDE.md` — workspace guard, CJS-only, zero em-dashes, BSL 1.1, three-surface, MWP, GSD enforcement.
7. `docs/MINDRIAN-CANON.md` Part 7 + Part 8 (load-bearing for the entire RS engine work).
8. `docs/CANON-PHASE-MAP.md` — `canon_parts:` frontmatter forward-compat rule.

## What Was Just Shipped (this session, 2026-04-25 / 2026-04-26)

### Phase 89.1a — Brain Methodology Substrate Loader (4 plans, 9 tasks, 7/7 SCs PASS)

| File | LOC | Purpose |
|------|-----|---------|
| `lib/core/rs-brain-substrate.cjs` | 481 | 6-mode loadSubstrate (A1/A2/A3/B1/B2/B3) + atomic cache + chokepoint |
| `lib/core/rs-brain-substrate-prompts.cjs` | 129 | Allow-list + FORBIDDEN_PATTERNS re-export byte-for-byte from cross-room-aggregator |
| `lib/memory/validators/brain-substrate-invariants.cjs` | 240 | Drop-in guardian validator, Checks A-F |
| `lib/memory/test-rs-brain-substrate.cjs` | 636 | 14-scenario adversarial fixture suite + A1/A2 sweeps |
| `lib/memory/run-feynman-tests.cjs` | +1 line | Registered at line 438 |

### Phase 89.1 — Domain Analysis + Query Generation Matrix (5 plans, 11 tasks, 7/7 SCs PASS)

| File | LOC | Purpose |
|------|-----|---------|
| `lib/core/rs-brain-substrate.cjs` | +50 (extended) | `adaptBrainSearchResponse` post-network shape adapter (Option 1; resolved 89.1a deferred item) |
| `lib/core/rs-domain-analyzer.cjs` | 385 | `analyzeDomain(topic, opts)` deterministic 7-field decomposition (no LLM round-trip) + ExternalEgressViolation sibling |
| `lib/core/rs-query-matrix.cjs` | 316 | `generateQueryMatrix(domain_analysis, opts)` 60 queries fixed-order template (15 × 4 categories) |
| `lib/memory/validators/brain-substrate-invariants.cjs` | modified | Check E relaxed to "embedding-or-score required" |
| `lib/memory/test-rs-brain-substrate-shape.cjs` | 529 | 14 shape-fix scenarios |
| `lib/memory/test-rs-domain-analyzer.cjs` | 492 | 11 fixture scenarios + A1 sweep |
| `lib/memory/test-rs-query-matrix.cjs` | 11 fixtures | 60-query shape + determinism + adversarial |
| `scripts/admin-brain-write.cjs` | 189 + extensions | `/mos:admin brain-write` one-shot wrapper with --canonize-source flag |
| `commands/admin.md` | extended | brain-write subcommand routing |
| `lib/memory/run-feynman-tests.cjs` | +3 lines | Three new test registrations (lines 452, 459, 464) |

**Live Brain state after 89.1:**
- `MATCH (p:ProcessStep {id:"rss-phase-1"})-[r:USES_TECHNIQUE]->(t:Technique {id:"tech-domain-analysis"}) RETURN count(r)` returns **1**
- ProcessStep `rss-phase-1` exists with `name="Phase 1: Domain Analysis"`, `created_by="v1.11.0-89.1-04"`
- Idempotency proven (re-run produces 0 mutations)
- Canon Part 8 attestation captured in `89.1-CANON-WRITE-EVIDENCE.md`

## Critical Architectural Decisions Locked (do NOT re-open)

1. **Option 1 (shape adapter inside `pullFromBrain`)** chosen over Option 2 (new `searchWithVectors` method). Evidence: `mcp-server-brain/test-brain.cjs:133` enumerates 5 MCP tools only; `mcp-server-brain/lib/pinecone-tools.cjs:47` shows Pinecone `searchRecords` has no `include_values` flag. Option 2 would require modifying the deployed MCP server (out of repo scope).

2. **Substrate entries on live wire path carry `score` ∈ [0,1] not 1024-dim `embedding`.** Validator Check E is "embedding-or-score required". Strict 1024-dim cosine over local substrate is filed forward to 89.2/89.5.

3. **`embedding_count` field** in cache schema = `substrate.length` (always was — naming wart). Score-bearing entries satisfy the strict ROADMAP wording.

4. **`ExternalEgressViolation` is a sibling class** to `BrainBoundaryViolation` (NOT a replacement). Surfaces are semantically distinct: Brain queries vs external fetcher inputs. Both exist in `rs-domain-analyzer.cjs` and `rs-query-matrix.cjs`.

5. **Option A (canonize-then-edge dual-MERGE)** chosen by user for the canon-write. Pre-check found rss-phase-1 ProcessStep didn't exist in live Brain (kickoff §3 audit was based on different snapshot). Option A creates the ProcessStep + edge in 2 MERGEs. Both writes are methodology-canon evolution (no user data).

6. **Rule-3 transport switch.** Brain-write uses **direct neo4j-driver** (loaded from `mcp-server-brain/node_modules`) against the same Aura instance via plugin-root `.env`. Reads continue through `brain-client.query`. Reason: user-tier `MINDRIAN_BRAIN_KEY` doesn't carry admin plan on the deployed MCP server. Same payloads, different transport. Canon Part 8 attestation unchanged.

7. **Canon Part 8 5-tripwire pattern is load-bearing across all v1.11.0 work.** Every new code surface that touches Brain or external egress MUST inherit:
   - T1 schema doc (cache schemas explicit)
   - T2 chokepoint allow-list (single-function gateway)
   - T3 drop-in invariants validator (`lib/memory/validators/`)
   - T4 pre-send `JSON.stringify` audit against `FORBIDDEN_PATTERNS`
   - T5 cross-scenario adversarial sweep (≥6 scenarios per egress surface)

8. **Bundled release.** No version bump in 89.1a / 89.1 / 89.2 / 89.3 / 89.4 / 89.5. **v1.11.0 ships at Phase 91.x.** Recent tag `v1.10.19` (your patch promote) carries 89.1 work as a side-effect — that's fine, milestone announcement still comes at v1.11.0.

## Push State (verify before resuming)

- `origin/main` HEAD: `5116b66` (v1.10.19 release tag)
- Local `main` HEAD: `e09408f docs(phase-89.1): complete phase execution`
- **Local is 1 commit ahead of origin/main.** That commit is the 89.1 verification + roadmap update — push it before/during 89.2 at your discretion.
- Working tree: `M dashboard/graph.json`, `?? audit/` — both untracked / unstaged drift unrelated to 89.x work.

```bash
# Recommended first action in fresh session:
cd ~/MindrianOS-Plugin
pwd                                    # confirm /home/jsagi/MindrianOS-Plugin
git fetch origin main
git log origin/main..HEAD --oneline    # should show 1 commit (e09408f)
git push origin main                   # OPTIONAL — push the 89.1 closure docs commit
```

## What 89.2 Must Ship (per kickoff §5 + §7)

**Phase 89.2 — External Research Fetching (EXPANDED) + Preprocessing + Scoring + Thesis + Experts**
- ~7-9 plans (largest sub-phase of v1.11.0)
- Consumes: Phase 89.1's 60-query matrix from `rs-query-matrix.cjs::generateQueryMatrix`
- Per kickoff Intent 6 ("but lets also add the ability to fetch resaerch extenly"):

### Modules to ship

| Module | Purpose |
|--------|---------|
| `lib/core/rs-fetcher-academic.cjs` | OpenAlex + arXiv (existing v1.10.16 baseline) + NEW: Scopus + PubMed + IEEE + Nature/Science |
| `lib/core/rs-fetcher-patents.cjs` | NEW: Google Patents + USPTO |
| `lib/core/rs-fetcher-industry.cjs` | NEW: Crunchbase + corporate R&D + startup trackers (Tavily-orchestrated) |
| `lib/core/rs-fetcher-experts.cjs` | NEW: author + institution + ORCID mapping |
| `lib/core/rs-preprocessor.cjs` | NEW: technical terms + concepts + methods + relevance + boundary flag (Phase 2 of canonical framework) |
| `lib/core/rs-differential-scorer.cjs` | NEW: dual-floor filters (diff>0.3 AND LSA>0.2 AND BERT>0.2) (Phase 3 detection) |
| `lib/core/rs-innovation-classifier.cjs` | NEW: structural_transfer / semantic_implementation / hybrid (Phase 3 classification) |
| `lib/core/rs-breakthrough-scorer.cjs` | NEW: 0-10 rubric (feasibility + market + magnitude + advantage + impact) |
| `lib/core/rs-thesis-generator.cjs` | NEW: "By applying [X] to [Y], achieve [Z] because [mechanism]" template (Phase 4 synthesis) |
| `lib/core/rs-expert-mapper.cjs` | NEW: Cypher MATCH in user's Aura — authors + institutions + citations |

Existing v1.10.16 primitives to **REUSE** (Canon Part 7), not replace:
- `lib/core/rs_math.py` — Kwan 2023 LSA (keep)
- `lib/core/rs_cache.py` — Pinecone e5-large cache (keep, extend)
- `lib/core/rs_corpus.py` — existing 3-tier external fetcher (keep, expand)

### Critical constraints (per kickoff §9)

- **Zero new runtime deps.** New fetchers use native `fetch` (existing pattern).
- **CJS only.** Python retained for `rs_math.py` + `rs_cache.py` + `rs_corpus.py`. New JS = CJS. (Python is acceptable for new fetchers IF they share corpus infrastructure — but prefer CJS for plugin-architecture consistency.)
- **BSL 1.1 header on every new source file** (CJS + Python).
- **Zero em-dashes** (hyphens only — project hard rule).
- **Three-surface compatible** (CLI + Desktop MCP + Cowork).
- **`canon_parts: [part-7, part-8]`** + **`bundled_release: true`** frontmatter on every plan.
- **NO release gate in 89.2** — bundled into v1.11.0 final ship at Phase 91.x.

### New egress surface enforcement

Every fetcher introduces a NEW external-egress surface (queries flow OUT to public APIs). Each MUST inherit:
- Canon Part 8 chokepoint pattern (single function builds outbound query)
- `auditQuery` / `auditOutput` FORBIDDEN_PATTERNS scan via `ExternalEgressViolation`
- Adversarial fixture coverage (≥4 scenarios per fetcher proving leaked user content fails before egress)
- Drop-in validator (mirror `brain-substrate-invariants.cjs` pattern)
- Per-source rate-limit + graceful degradation (per Phase 88.6-03 pattern: per-source budget + telemetry persisted in `external-papers.json queries[]`)

### Open questions (kickoff §15) to resolve during planning

- **External fetcher rate-limit strategy:** per-source budget vs global budget? (Per 88.6-03: per-source with graceful degradation. Confirm during planning.)
- **Pre-filter substrate by Cynefin classification before pulling embeddings?** Defer to 89.5 engine orchestrator. 89.2 pulls full substrate via existing `loadSubstrate` API.
- **Expert mapping privacy:** extracting authors from public papers is fine per Canon Part 8. Public author emails from OpenAlex: fine. Anything behind login: skip.

## Deferred Items Inherited (must address in 89.2 OR file forward)

From 89.1a `deferred-items.md` (Phase 89.1 already resolved item #1 — the brain-client shape mismatch). Check if any new items are filed in 89.1's `deferred-items.md` (none expected as of this writing).

From 89.1 SUMMARYs and Phase Gate evidence:
- **Strict 1024-dim cosine over local substrate** filed forward to 89.2/89.5. The current substrate carries `score` not vectors. If 89.2's preprocessor / differential scorer needs actual vectors (likely yes for Phase 2 dual similarity), this is the moment to address. Two options:
  - (a) Add a separate Pinecone-direct vector pull in `rs_cache.py` (already exists — confirm it returns vectors via `include_values=true`)
  - (b) Defer dual-similarity to 89.3 if 89.2 can ship preprocessor + classifier + breakthrough scorer + thesis on score-only substrate
- **Brain-client admin-key plan tier.** v1.11.0 doesn't need any more admin Brain writes (the canonization edge is filed). Future Brain-writes (if any) can use the direct neo4j-driver Rule-3 pattern from `scripts/admin-brain-write.cjs`.

## Resume Command (paste this in the fresh session)

```
/gsd:plan-phase 89.2
```

With this briefing to the planner (paste verbatim after the slash command):

```
Plan Phase 89.2 (External Research Fetching EXPANDED + Preprocessing + Scoring + Thesis + Experts) for v1.11.0 milestone.

Working directory: /home/jsagi/MindrianOS-Plugin

CRITICAL CONTEXT FILES (read in order):
1. .planning/milestones/v1.11.0-RESUME-89.2.md (THIS HANDOFF DOC — start here, full session context)
2. .planning/milestones/v1.11.0-KICKOFF.md (especially §5 Phase 1.5 External Research Fetching, §6 chain mechanism, §9 implementation guidance)
3. .planning/phases/89.1a-brain-methodology-substrate-loader/ (substrate loader contract to consume)
4. .planning/phases/89.1-domain-analysis-and-query-matrix/ (60-query matrix to consume; ExternalEgressViolation pattern to mirror; Phase Gate template)
5. lib/core/rs-brain-substrate.cjs + rs-domain-analyzer.cjs + rs-query-matrix.cjs (existing primitives to consume; do NOT modify in 89.2)
6. lib/core/rs_corpus.py + rs_cache.py + rs_math.py (existing v1.10.16 fetcher + LSA + Pinecone cache to EXTEND, not replace, per Canon Part 7)
7. CLAUDE.md + docs/MINDRIAN-CANON.md Part 7 + Part 8 + docs/CANON-PHASE-MAP.md

Ship 7-9 plans covering:
- rs-fetcher-academic.cjs (extend v1.10.16 OpenAlex+arXiv+Tavily; ADD Scopus + PubMed + IEEE + Nature/Science via native fetch)
- rs-fetcher-patents.cjs (NEW: Google Patents + USPTO)
- rs-fetcher-industry.cjs (NEW: Crunchbase + corporate R&D + startup trackers via Tavily-orchestrated calls)
- rs-fetcher-experts.cjs (NEW: author + institution + ORCID mapping; feeds Phase 89's expert network mapping in 89.5)
- rs-preprocessor.cjs (NEW: technical terms + concepts + methods + relevance + boundary flag — Phase 2 of canonical framework)
- rs-differential-scorer.cjs (NEW: dual-floor filters per kickoff §3 — diff>0.3 AND LSA>0.2 AND BERT>0.2)
- rs-innovation-classifier.cjs (NEW: structural_transfer / semantic_implementation / hybrid)
- rs-breakthrough-scorer.cjs (NEW: 0-10 rubric)
- rs-thesis-generator.cjs (NEW: thesis template — "By applying [X] to [Y], achieve [Z] because [mechanism]")
- Canon Part 8 enforcement on every NEW egress surface (each fetcher = new outbound surface; inherit ExternalEgressViolation + auditQuery + adversarial fixtures ≥4 per fetcher)
- Per-source rate-limit graceful degradation (Phase 88.6-03 pattern)
- Phase Gate transcript (mirror 89.1-PHASE-GATE.md format; 9 G + N E asserts where N is fetcher count)
- Feynman registration for every new test file

All plans declare canon_parts: [part-7, part-8] + bundled_release: true frontmatter. NO release gate in 89.2 — bundled into v1.11.0 final ship at Phase 91.x.

Constraints: zero new runtime deps (native fetch + existing rs_corpus.py + existing brain-client + existing loadSubstrate); CJS only for new JS; BSL 1.1 header on all new source; zero em-dashes; three-surface compatible.

Recommended: --skip-research is OK because the kickoff + 89.1a/89.1 SUMMARYs + this RESUME doc provide authoritative spec. Or run with research enabled if you want the planner to verify external API endpoints (rate limits, auth requirements for Scopus/IEEE/USPTO/Crunchbase) before locking the plan.
```

## House-Keeping Reminders for the Fresh Session

1. **`.planning/` is gitignored but force-tracked.** When `gsd-tools.cjs commit` returns `skipped_gitignored`, fall back to `git add -f <file>` + `git commit`. Pattern proven across 89.1a + 89.1.
2. **Workspace guard:** confirm `pwd` shows `/home/jsagi/MindrianOS-Plugin` BEFORE any work. If you're in `~/.claude/plugins/mindrian-os/`, you're in the plugin cache (read-only mirror, commits diverge from GitHub).
3. **Roadmap analyzer parser quirk:** `node bin/gsd-tools.cjs init plan-phase "89.2"` may collapse `89.2` to a partial-match directory. The fix is to **manually create `.planning/phases/89.2-external-research-fetching/`** before invoking the planner — same pattern used for 89.1a and 89.1.
4. **Add 89.2 entry to ROADMAP.md before planning** (between Phase 89.1 entry and Phase 90 entry). Follow the format used for 89.1a and 89.1: Goal + Depends on + Requirements + Success Criteria + Plans + Authority.
5. **Live Brain MCP is reachable from this workspace** (proven in 89.1-04 + 89.1-05). If 89.2 needs any Brain reads (likely YES — fetchers may need substrate filter context), they go through `lib/core/brain-client.cjs::query` or `loadSubstrate` — already-audited paths.

## What's Left in v1.11.0 After 89.2

| Sub-phase | Plans | Status |
|-----------|-------|--------|
| ~~89.1a Brain Substrate Loader~~ | ~~4~~ | ✓ SHIPPED |
| ~~89.1 Domain Analysis + Query Matrix~~ | ~~5~~ | ✓ SHIPPED |
| **89.2 External Research Fetching (EXPANDED)** | **7-9** | **NEXT** |
| 89.3 5-branch Mind Map + Full Neo4j Schema | 4-5 | pending |
| 89.4 Chain Wiring upstream/downstream | 3-4 | pending |
| 89.5 Engine Top-Level Orchestrator + Bidirectional NL-Graph Surface | 4-5 | pending |
| 91 Navigation Engine | 11 (already filed at .planning/phases/91-navigation-engine/) | pending |
| 91.x v1.11.0 5-gate release | 1 | pending — SHIPS v1.11.0 |

Total remaining: ~30-35 plans, ~8-10 wall-clock days at the current pace.

---

**End of resume doc.** Filed 2026-04-26. Load this in any fresh session BEFORE invoking `/gsd:plan-phase 89.2`.
