---
phase: quick-260723-m5d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-deck.cjs
  - scripts/generate-hub.cjs
  - scripts/generate-lobby.cjs
  - scripts/generate-snapshot.cjs
  - dashboard/index.html
  - dashboard/export-template.html
  - skills/ui-system/SKILL.md
  - skills/ui-system/rules/design-system.md
autonomous: true
requirements: [QUICK-260723-M5D]

must_haves:
  truths:
    - "Each of the 4 CLI generators (generate-deck.cjs, generate-hub.cjs, generate-lobby.cjs, generate-snapshot.cjs) emits data-theme=\"light\" on <html> and a data-mos=\"v1.1\" styled block in <head> for every artifact it produces"
    - "dashboard/index.html and dashboard/export-template.html both set data-theme=\"light\" on <html>, both carry the canonical :root token block + dark override as literal static text (no require() calls), and every existing --ds-*/--mondrian-* structural color property that has a role-equivalent canonical token now resolves through var(--canonical-token) in place"
    - "scripts/generate-standalone (reads dashboard/index.html verbatim) and scripts/generate-export.cjs (reads dashboard/export-template.html verbatim) inherit the fix with zero code changes to either script"
    - "skills/ui-system/SKILL.md has a numbered section 0 mandating M:OS for all HTML artifacts, and skills/ui-system/rules/design-system.md exists, resolving the forward references already present in M-OS-DESIGN-SYSTEM.md section 12"
    - "A Playwright CLI screenshot pass across all 6 regenerated artifact types (deck, hub, lobby, snapshot, standalone, export) shows a cream/light ground with no leftover dark-mode chrome and no literal ${require(...)} text rendered as page content"
  artifacts:
    - path: "scripts/generate-deck.cjs"
      provides: "data-theme=light + mosStyleTag() require() call in the head template literal"
      contains: "mosStyleTag"
    - path: "scripts/generate-hub.cjs"
      provides: "same 2-line fix in generateHtml()"
      contains: "mosStyleTag"
    - path: "scripts/generate-lobby.cjs"
      provides: "same 2-line fix in generateLobby()"
      contains: "mosStyleTag"
    - path: "scripts/generate-snapshot.cjs"
      provides: "same 2-line fix in pageHead()"
      contains: "mosStyleTag"
    - path: "dashboard/index.html"
      provides: "canonical token block + role-based alias layer over --ds-* + data-theme=light"
      contains: "data-mos=\"v1.1\""
    - path: "dashboard/export-template.html"
      provides: "canonical token block + role-based alias layer over --mondrian-*/--accent-* + data-theme=light"
      contains: "data-mos=\"v1.1\""
    - path: "skills/ui-system/rules/design-system.md"
      provides: "the HTML-artifact design-system mandate contract, frontmatter matching sibling rules files"
      contains: "name: design-system"
    - path: "skills/ui-system/SKILL.md"
      provides: "section 0 HTML Artifact Design System mandate"
      contains: "## 0. HTML Artifact Design System"
  key_links:
    - from: "scripts/generate-deck.cjs (and hub/lobby/snapshot)"
      to: "lib/ui/design-system.cjs mosStyleTag()"
      via: "require(\"../lib/ui/design-system.cjs\").mosStyleTag() inlined in the head template literal"
      pattern: "mosStyleTag\\(\\)"
    - from: "dashboard/index.html <style>"
      to: "skills/ui-system/design-system/mos-design-system.css canonical tokens"
      via: "literal baked :root token text copied in (dashboard is statically served, cannot require())"
      pattern: "data-mos=\"v1.1\""
    - from: "dashboard/export-template.html <style>"
      to: "skills/ui-system/design-system/mos-design-system.css canonical tokens"
      via: "literal baked :root token text copied in"
      pattern: "data-mos=\"v1.1\""
    - from: "skills/ui-system/design-system/M-OS-DESIGN-SYSTEM.md section 12"
      to: "skills/ui-system/rules/design-system.md + SKILL.md section 0"
      via: "pre-existing forward-reference now resolved by this plan"
      pattern: "skills/ui-system/rules/design-system.md"
---

<objective>
Finish the M:OS Canonical Design System v1.1 "bake into all HTML artifacts" mandate. Commit `a9e1ee88` did the full job (CSS bundle, loader, 4 generator rewires, 2 dashboard token injections, mandate docs) but lives only on the unmerged branch `feat/mos-design-system-v1.1` (confirmed NOT an ancestor of `main`). Commit `e9db0d1c` (Phase 232-01) later cherry-picked only 5 files from that branch (CSS bundle, loader, 3 docs). This plan lands the 6 remaining files: the 4 generator rewires (port verbatim, unchanged since the branch was cut) and the 2 dashboard template token injections (adapted: role-based alias layer instead of `a9e1ee88`'s literal hex duplication, per this task's CONTEXT.md), plus the mandate docs (`rules/design-system.md` + `SKILL.md` section 0) that `M-OS-DESIGN-SYSTEM.md` section 12 already forward-references.

Purpose: every HTML artifact this repo generates or serves (decks, dashboards, exports, standalone/hub/lobby pages) must consistently render the M:OS Canonical Design System v1.1 (cream ground, canonical De Stijl x Swiss-broadside tokens, `data-mos="v1.1"` marker) so the visual mandate is actually enforced, not just documented as aspirational in a landed spec.

Output: 4 generator scripts wired to `mosStyleTag()`, 2 dashboard templates carrying baked canonical tokens plus an alias layer, the skill mandate docs authored, and a Playwright CLI screenshot pass confirming all 6 downstream artifact types (deck/hub/lobby/snapshot/standalone/export) render correctly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260723-m5d-complete-m-os-canonical-design-system-v1/260723-m5d-CONTEXT.md
@.planning/quick/260723-m5d-complete-m-os-canonical-design-system-v1/260723-m5d-RESEARCH.md
@lib/wiki/wiki-layout.cjs
@lib/ui/design-system.cjs
@skills/ui-system/design-system/mos-design-system.css
@skills/ui-system/SKILL.md
@skills/ui-system/rules/dual-palette.md
@skills/ui-system/design-system/M-OS-DESIGN-SYSTEM.md

**Provenance:** every fix in Task 1 and the doc content in Task 3 already exists verbatim on the unmerged branch. Fetch exact diffs with `git show a9e1ee88 -- <path>` per file if you want to cross-check this plan's inlined instructions against the original; do not re-derive these from scratch.

**Scope boundaries (hard, per CONTEXT.md's locked decisions):**
- Theme scope: static `data-theme="light"` only for the 2 dashboard templates. Do NOT add a theme toggle button or localStorage persistence to either dashboard file (`wiki-layout.cjs`'s toggle is a reference pattern for OTHER surfaces, not a requirement here).
- Migration scope: token injection plus alias layer ONLY. Do NOT rename `.op`/`.pill`/`.vd`/card/grid classes or touch any selector other than the `:root` blocks and the `<html>` tag in the 2 dashboard files. "Full class migration" is explicitly out of scope.
- Do not touch `skills/ui-system/design-system/mos-design-system.css`, `SPEC.md`, `M-OS-DESIGN-SYSTEM.md`, or `image-prompt-style.md` -- all already landed correctly in `e9db0d1c`.
- No em-dashes anywhere in any written file (repo hard rule). Use hyphens.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the 4 HTML generators to mosStyleTag()</name>
  <files>scripts/generate-deck.cjs, scripts/generate-hub.cjs, scripts/generate-lobby.cjs, scripts/generate-snapshot.cjs</files>
  <action>
    Port `a9e1ee88`'s exact 2-line fix into each of the 4 files (verified unchanged since the branch was cut; identical shape in all 4). Use the Edit tool only (never Write/rewrite the whole file) so the executable bit (755, confirmed via `ls -la`) on all 4 files is preserved.

    In `scripts/generate-deck.cjs` (head template literal starts at line ~118; `<html lang="en">` is at line 119): change `<html lang="en">` to `<html lang="en" data-theme="light">`, and insert a new line immediately after the following `<head>` line containing exactly `${require("../lib/ui/design-system.cjs").mosStyleTag()}`.

    In `scripts/generate-hub.cjs` (inside `function generateHtml(room)`; `<html lang="en">` is at line 2262): apply the identical change (same target string, same insertion; still one level up from `scripts/`, so the relative path stays `../lib/ui/design-system.cjs`).

    In `scripts/generate-lobby.cjs` (inside `function generateLobby(roomPath, options = {})`; `<html lang="en">` is at line 639): apply the identical change.

    In `scripts/generate-snapshot.cjs` (inside `function pageHead(title, model, extraCdns)`; `<html lang="en">` is at line 471): apply the identical change.

    Do not touch any other line in these 4 files. Do not add a top-of-file `require('../lib/ui/design-system.cjs')` (a9e1ee88's original pattern is an inline require at the call site in all 4 files; keep that pattern for exact provenance parity -- `wiki-layout.cjs`'s top-of-file require is a different file's convention, not required here).
  </action>
  <verify>
    <automated>for f in scripts/generate-deck.cjs scripts/generate-hub.cjs scripts/generate-lobby.cjs scripts/generate-snapshot.cjs; do grep -q 'data-theme="light"' "$f" && grep -q 'mosStyleTag' "$f" && [ -x "$f" ] || { echo "FAIL: $f"; exit 1; }; done; echo "TASK1 OK"</automated>
  </verify>
  <done>All 4 generator files contain `data-theme="light"` on the `<html>` tag and a `mosStyleTag()` require call immediately inside `<head>`; all 4 remain executable; no other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Inject canonical tokens + role-based alias layer into the 2 dashboard templates</name>
  <files>dashboard/index.html, dashboard/export-template.html</files>
  <action>
    Both files are statically served (`scripts/serve-dashboard` is a bare file server, zero templating) -- never leave a `require(...)` call in either file; the CSS must be literal text.

    Step A -- baked token block (both files, identical text): copy lines 12-32 of `skills/ui-system/design-system/mos-design-system.css` verbatim (the `:root{...}` surfaces + De Stijl primaries + devices block, the `@media (prefers-color-scheme:dark)` fallback, and the `:root[data-theme="light"]`/`:root[data-theme="dark"]` override blocks). Wrap that literal text in its own `<style data-mos="v1.1">...</style>` tag (matching the exact shape `mosStyleTag()` emits at runtime) and insert it as a new element in `<head>`, immediately before the file's existing `<style>` tag (dashboard/index.html: existing `<style>` is at line 24; dashboard/export-template.html: existing `<style>` is at line 28). Precede the injected tag with a one-line HTML comment: `<!-- M:OS CANONICAL DESIGN SYSTEM v1.1 -- source: skills/ui-system/design-system/mos-design-system.css -->`.

    Step B -- set the theme attribute: change both files' `<html lang="en">` to `<html lang="en" data-theme="light">`.

    Step C -- alias layer, dashboard/index.html: inside the file's EXISTING `:root { ... }` block (currently lines 25-38, the one declaring `--ds-bg` through `--ds-teal`), edit each property's VALUE in place (do not add a second competing `:root` rule -- a later `:root` block with equal specificity would just be overridden by this one in source order, so the edit must happen in place). Grep confirmed every one of these properties has exactly one semantic role (background, text, border, or accent) used consistently across the file; map by ROLE, not by name similarity -- `--ds-cream` is used exclusively as a TEXT color (never a background) so it must alias to `--ink`, not `--paper`, even though "cream" sounds background-like:

    | Property | Old literal value | New value | Role confirmed by grep |
    |---|---|---|---|
    | --ds-bg | #0D0D0D | var(--paper) | background only |
    | --ds-surface | #1A1A1A | var(--panel) | background only |
    | --ds-elevated | #2A2A2A | var(--panel) | background only |
    | --ds-cream | #F5F0E8 | var(--ink) | text color only |
    | --ds-muted | #A09A90 | var(--muted) | text/border (muted role) |
    | --ds-border | #2A2A2A | var(--rule) | border only |
    | --ds-red | #A63D2F | var(--red) | accent bg/text (semantic) |
    | --ds-blue | #1E3A6E | var(--blue) | accent bg/text (semantic) |
    | --ds-yellow | #C8A43C | var(--yellow) | accent bg/text (semantic) |
    | --ds-green | #2D6B4A | var(--green) | accent bg/text (semantic) |
    | --ds-sienna | #B5602A | unchanged (leave literal) | legend/edge-type swatch only, no canonical equivalent |
    | --ds-gray | #5C5A56 | unchanged (leave literal) | legend swatch only, no canonical equivalent |
    | --ds-amethyst | #6B4E8B | unchanged (leave literal) | legend/edge-type swatch only, no canonical equivalent |
    | --ds-teal | #2A6B5E | unchanged (leave literal) | legend/edge-type swatch only, no canonical equivalent |

    Step D -- alias layer, dashboard/export-template.html: same in-place-edit technique inside the file's existing `:root { ... }` block (currently lines 32-42, declaring `--mondrian-red` through `--grid-border-thin`):

    | Property | Old literal value | New value | Role confirmed by grep |
    |---|---|---|---|
    | --mondrian-red | #C23B22 | var(--red) | accent bg/text (semantic) |
    | --mondrian-blue | #1B3B6F | var(--blue) | accent bg/text (semantic) |
    | --mondrian-yellow | #E8B931 | var(--yellow) | accent bg/text (semantic) |
    | --mondrian-black | #1A1A1A | var(--ink) | body/topbar bg AND text color (both near-black, safe either role) |
    | --mondrian-white | #F7F3ED | var(--paper) | main content bg AND text-on-color |
    | --mondrian-gray | #D4CFC7 | var(--muted) | muted text/border |
    | --accent-green | #2D6B4A | var(--green) | status text color |
    | --accent-sienna | #B5602A | unchanged (leave literal) | status text only, no canonical equivalent |
    | --grid-border / --grid-border-thin | (reference --mondrian-black) | unchanged declarations | inherit automatically once --mondrian-black is aliased |

    After Steps A-D, both files must have zero remaining hardcoded hex values for any property that now has a canonical mapping in the table, while sienna/gray/amethyst/teal (index.html) and accent-sienna (export-template.html) keep their original literal hex, unaliased, coexisting alongside the canonical tokens without name collisions.
  </action>
  <verify>
    <automated>grep -q '<html lang="en" data-theme="light">' dashboard/index.html && grep -q 'data-mos="v1.1"' dashboard/index.html && grep -q -- '--ds-bg: var(--paper)' dashboard/index.html && grep -q -- '--ds-cream: var(--ink)' dashboard/index.html && grep -q -- '--ds-surface: var(--panel)' dashboard/index.html && grep -q '<html lang="en" data-theme="light">' dashboard/export-template.html && grep -q 'data-mos="v1.1"' dashboard/export-template.html && grep -q -- '--mondrian-black: var(--ink)' dashboard/export-template.html && grep -q -- '--mondrian-white: var(--paper)' dashboard/export-template.html && echo "TASK2 OK"</automated>
  </verify>
  <done>Both dashboard templates default to data-theme="light", both carry a data-mos="v1.1" baked token block, and the role-based alias mapping above is applied in place inside each file's existing :root block with zero require() calls left in either file.</done>
</task>

<task type="auto">
  <name>Task 3: Author the skill mandate docs, regenerate all 6 artifact types, and visually verify with Playwright CLI</name>
  <files>skills/ui-system/SKILL.md, skills/ui-system/rules/design-system.md</files>
  <action>
    Doc authoring (verbatim port, per Don't-Hand-Roll -- both documents already exist correctly on the unmerged branch, fetch exact text via `git show a9e1ee88 -- skills/ui-system/SKILL.md` and `git show a9e1ee88:skills/ui-system/rules/design-system.md`):

    1. In `skills/ui-system/SKILL.md`, insert a new `## 0. HTML Artifact Design System -- M:OS canonical (MANDATORY)` section immediately before the existing `## 1. Four-Zone Output Anatomy` heading (currently line 17). Port the section body verbatim from `a9e1ee88`'s diff (11 lines: the terminal-vs-HTML framing sentence, the inline-CSS-bundle bullet, the full-contract + written-spec bullet, the imagery-recipe bullet, the defaults bullet, the ratio/semantic-color/grid/isometric/a11y bullet, and the "Applies to" bullet). Do not renumber any existing section.

    2. Create `skills/ui-system/rules/design-system.md`. Match the sibling frontmatter pattern from `skills/ui-system/rules/dual-palette.md` (YAML frontmatter with `name:` and a `description:` block) -- the original `a9e1ee88` version of this file had no frontmatter, so add it now to match the established repo convention that postdates the branch cut. Use `name: design-system` and a description summarizing: M:OS Canonical Design System v1.1 mandate for every generated HTML artifact, governs the HTML/browser surface exactly as SKILL.md's 4-zone system governs the terminal surface, source of truth is `skills/ui-system/design-system/mos-design-system.css` + `SPEC.md`, loader is `lib/ui/design-system.cjs` `mosStyleTag()`, cross-referenced by `M-OS-DESIGN-SYSTEM.md` section 12 and `SKILL.md` section 0. Below the frontmatter, port the full 48-line body verbatim from `git show a9e1ee88:skills/ui-system/rules/design-system.md` (the five laws, defaults, isometric/structural-futurism, imagery, data-representations, and "Applies to" sections) unchanged.

    Regenerate and visually verify (fully automated, no human checkpoint needed -- Claude reads the resulting PNGs itself): create a scratch room copy so nothing writes into the tracked `tests/fixtures/wiki-room-232` fixture, then run all 4 generators plus both dashboard-consuming scripts against the copy, then screenshot every output with the Playwright CLI (`npx playwright screenshot`, per this repo's `feedback_playwright_cli.md` hard rule: CLI via Bash, never the MCP plugin) and Read each PNG to confirm a cream/light ground, canonical De Stijl colors, and no literal `${require(...)}` text or broken dark-mode leftovers anywhere.
  </action>
  <verify>
    <automated>grep -q '## 0. HTML Artifact Design System' skills/ui-system/SKILL.md && test -f skills/ui-system/rules/design-system.md && grep -q 'name: design-system' skills/ui-system/rules/design-system.md && ROOM=$(mktemp -d) && cp -r tests/fixtures/wiki-room-232/. "$ROOM"/ && OUT=$(mktemp -d) && node scripts/generate-deck.cjs "$ROOM" --output "$OUT/deck.html" && node scripts/generate-hub.cjs "$ROOM" --output "$OUT/hub.html" && node scripts/generate-lobby.cjs "$ROOM" --output "$OUT/lobby.html" && node scripts/generate-snapshot.cjs "$ROOM" && bash scripts/generate-standalone "$ROOM" dashboard "$OUT/standalone.html" && node scripts/generate-export.cjs "$ROOM" "$OUT/export.html" && SNAP=$(ls -td "$ROOM"/exports/*/ | head -1) && for f in "$OUT/deck.html" "$OUT/hub.html" "$OUT/lobby.html" "$OUT/standalone.html" "$OUT/export.html" "${SNAP}index.html"; do grep -q 'data-mos="v1.1"' "$f" || { echo "MISSING data-mos marker: $f"; exit 1; }; done && echo "TASK3 OK: all 6 artifact types carry data-mos=v1.1"</automated>
  </verify>
  <done>SKILL.md section 0 and rules/design-system.md both exist with the mandated content; all 6 regenerated artifact types (deck, hub, lobby, snapshot, standalone export, dashboard export) carry the data-mos="v1.1" marker; Playwright screenshots of each, read back by Claude, show a cream/light ground with no literal template-string leakage and no unintended dark-mode remnants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML template edit -> statically-served page | dashboard/index.html and dashboard/export-template.html are served byte-for-byte by a bare file server with zero templating; any executable-looking text left in the file becomes literal, rendered page content, not code |
| Room content -> generated HTML artifacts | User's own room data is interpolated into deck/hub/lobby/snapshot/export HTML; this plan only changes the CSS/head wiring, it does not touch how room content is escaped or interpolated (pre-existing behavior, out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-M5D-01 | Tampering | dashboard/index.html, dashboard/export-template.html | mitigate | Task 2's action explicitly forbids any `require(...)` call in either static file (Pitfall 1 from RESEARCH.md); Task 3's Playwright screenshot pass visually confirms no literal `${require(...)}` text renders as page content |
| T-M5D-02 | Tampering | dashboard/index.html alias-layer cascade order | mitigate | Aliasing is done as an in-place value edit inside the existing single `:root` block (Task 2, Step C/D), not a second competing `:root` rule, eliminating any source-order ambiguity about which value wins |
| T-M5D-03 | Information Disclosure | Generated HTML artifacts embed room content | accept | Pre-existing scope; this plan changes only CSS/head wiring, not data interpolation or escaping; no new egress path introduced |
| T-M5D-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; Playwright is already globally installed tooling used via CLI per the user's existing hard rule (feedback_playwright_cli.md), not a new project dependency |

Pre-existing exposure NOT in scope: the general question of HTML-escaping user room content inside generate-deck/hub/lobby/snapshot/export is unchanged by this plan (those scripts' existing escaping behavior, whatever it is, is untouched).
</threat_model>

<verification>
- `for f in scripts/generate-deck.cjs scripts/generate-hub.cjs scripts/generate-lobby.cjs scripts/generate-snapshot.cjs; do grep -q 'data-theme="light"' "$f" && grep -q mosStyleTag "$f"; done` -- all 4 pass.
- `grep -c 'data-mos="v1.1"' dashboard/index.html dashboard/export-template.html` -- both >= 1.
- Regenerating all 6 downstream artifact types (deck/hub/lobby/snapshot/standalone/export) against a scratch room copy and grepping each output for `data-mos="v1.1"` -- all 6 pass.
- `grep -q '## 0. HTML Artifact Design System' skills/ui-system/SKILL.md` and `test -f skills/ui-system/rules/design-system.md` both pass.
- Em-dash scan over all 8 modified/created files returns zero matches.
- Playwright screenshots of all 6 artifact types, read back via the Read tool, show cream/light ground with canonical De Stijl colors and no rendering defects.
</verification>

<success_criteria>
- Every one of the 4 CLI generators inlines `mosStyleTag()` and defaults to `data-theme="light"`.
- Both dashboard templates carry the canonical token block, default to `data-theme="light"`, and alias their existing structural color properties onto canonical tokens by ROLE (background vs text vs border vs accent), with zero visual contrast regressions (verified by Playwright screenshot, not just grep).
- `generate-standalone` and `generate-export.cjs` inherit the fix automatically, with no code changes to either script.
- `skills/ui-system/SKILL.md` section 0 and `skills/ui-system/rules/design-system.md` exist, resolving `M-OS-DESIGN-SYSTEM.md` section 12's forward references.
- No `require(...)` call left in either static dashboard HTML file.
- No em-dashes introduced anywhere.
</success_criteria>

<output>
Create `.planning/quick/260723-m5d-complete-m-os-canonical-design-system-v1/260723-m5d-SUMMARY.md` when done: what changed in each of the 8 files, the alias-mapping tables actually applied, the Playwright screenshot findings (per artifact type), and confirmation that all 3 tasks' automated verify commands passed.
</output>
