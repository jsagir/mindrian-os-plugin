---
kind: quick
quick_id: 260706-4cb
slug: append-opportunity-statement-formula-add
title: "Append the Opportunity Statement formula addendum to SEED-048 (SEED-049 generator -> SEED-050 critic -> SEED-048 bank, PWS Value Proposition backbone per Brain, confidence 0.90)"
date: 2026-07-06
status: planned
must_haves:
  truths:
    - "SEED-048 gains exactly ONE new section, appended after the existing 'Standalone research' section. git diff shows pure additions at end of file - zero lines above the new section change, nothing reordered."
    - "The Opportunity Statement formula appears verbatim in the new section: Combining [Technology A] (unmet need: X) and [Technology B] (unmet need: Y) creates a [novel application/market] that addresses [gap Z], for [who it's for]. Key risks / Next steps / Estimated potential / Score slots all present."
    - "The section grounds the formula as reuse, not invention (Part 7): SEED-049 generates the pair, SEED-050 must pass it as REAL before it becomes a statement, SEED-048's bank stores it in this shape one per ranked candidate, and the Brain's generic-methodology routing to PWS Value Proposition (confidence 0.90, Part 8 compliant query) is named as the structural backbone."
    - "The Eureka-vs-Opportunity-Statement distinction is stated plainly: curiosity one-off vs banked/ranked/repeatable, with the formula as the converter gated by the critic."
    - "Zero em-dashes in the appended text; hyphens only. Prose is Feynman-plain and roughly 15-25 lines, matching the density of the seed's 'Why it matters' and 'Relationship' sections."
  artifacts:
    - .planning/seeds/SEED-048-portfolio-scale-fusion.md (appended section only)
    - .planning/quick/260706-4cb-append-opportunity-statement-formula-add/260706-4cb-PLAN.md
    - .planning/quick/260706-4cb-append-opportunity-statement-formula-add/260706-4cb-SUMMARY.md (executor writes on completion)
  key_links:
    - .planning/seeds/SEED-049-mindrian-insight-engine-tri-modal-tri-source-hybrid-retrieval.md
    - .planning/seeds/SEED-050-eureka-eval-salient-verifier-judge-synthetic-trust.md
    - .planning/seeds/INDEX.md
---

# Quick Task 260706-4cb: append the Opportunity Statement formula to SEED-048

## Problem

SEED-048 describes the portfolio-scale FUSION bank (batch-score N technologies,
surface the hidden gem) and its research addendum already resolved WHAT the
score measures (3-dimension AHP scoring). But the seed never pins down the
SHAPE of the per-candidate output - the one-paragraph statement the bank
actually stores per ranked candidate. That shape now exists: it is the PWS
Value Proposition framework specialized for cross-domain pairs, confirmed by a
generic-methodology Brain MCP query (routed to "PWS Value Proposition",
confidence 0.90, Part 8 compliant - only the abstract question crossed the
wire). This task appends that formula plus its grounding to the seed, purely
additively.

## Constraints (MANDATORY)

- **Purely additive.** Do NOT rewrite, reorder, or touch any existing line of
  the seed. The new section goes at the very end, after the "Standalone
  research" section's closing "Net effect on scope" paragraph (currently the
  last line of the file).
- **No em-dashes anywhere** - hyphens only (repo hard rule).
- **Verbatim text below.** The appended section is spelled out in Task 2; the
  executor copies it exactly, no paraphrasing, no invention.

## Tasks

### Task 1: read SEED-048 in full and confirm the append anchor

- **files:** `.planning/seeds/SEED-048-portfolio-scale-fusion.md` (read only)
- **action:** Read the whole file (it is ~37 lines). Confirm: (a) exact
  filename is `SEED-048-portfolio-scale-fusion.md`; (b) the file currently
  ends with the "Standalone research" section, whose final paragraph begins
  "Net effect on scope:"; (c) no section titled anything like "Opportunity
  Statement" already exists (if one does, STOP - the task is stale, report
  instead of appending a duplicate).
- **verify:** `tail -3 .planning/seeds/SEED-048-portfolio-scale-fusion.md`
  shows the "Net effect on scope" paragraph as the current end of file.
- **done:** anchor confirmed, no duplicate section present.

### Task 2: append the new section (Edit tool, end of file only)

- **files:** `.planning/seeds/SEED-048-portfolio-scale-fusion.md`
- **action:** Using the Edit tool, append after the final "Net effect on
  scope..." paragraph (old_string = the tail of that closing paragraph,
  new_string = same tail + blank line + the section below). Append this text
  EXACTLY (one leading blank line before the heading; the formula sits in a
  blockquote so it reads as the canonical template):

```
## Opportunity Statement formula (the per-candidate output shape)

Every ranked candidate in this seed's bank is stored in ONE canonical shape:

> "Combining [Technology A] (unmet need: X) and [Technology B] (unmet need: Y) creates a [novel application/market] that addresses [gap Z], for [who it's for]. Key risks: [what could kill it]. Next steps: [validation / funding / partnership path]. Estimated potential: [market size or impact tier]. Score: [SEED-048's per-candidate rank]."

This is not a net-new invention (Part 7, reuse before build). The pipeline that produces it already exists across the sibling seeds:

- SEED-049 (the Eureka generator) proposes the raw surprising cross-domain pair.
- SEED-050 (the Critic) must pass the pair as REAL - not confident noise - before it is allowed to become an Opportunity Statement.
- SEED-048's own ranked bank (this seed) is what stores the statement in exactly this shape, one per ranked candidate, feeding the scoring + stage-gate routing described above.

Framework grounding: a generic-methodology query to the Brain MCP (Part 8 compliant - no room-specific or user data crossed the wire, only the abstract question) independently routed to "PWS Value Proposition" as the structural backbone, confidence 0.90. So this formula is PWS Value Proposition specialized for portfolio-scale cross-domain candidates, not a new framework.

The distinction the formula enforces: a Eureka is "whoa, those two connect" - curiosity, a one-off. An Opportunity Statement is "here's the market, the risk, and who funds it" - banked, ranked, repeatable. The formula above is what turns the former into the latter, gated by the critic.
```

  Do not modify anything above the appended block. Do not add a trailing
  section separator or footer.
- **verify:**
  - `git diff .planning/seeds/SEED-048-portfolio-scale-fusion.md` shows ONLY
    added lines (no `-` lines except none; every hunk is at end of file).
  - `grep -c '^## ' .planning/seeds/SEED-048-portfolio-scale-fusion.md`
    returns 6 (was 5: vision, why it matters, proposed scope, relationship,
    standalone research; +1 new).
  - Em-dash gate: `grep -n $'—' .planning/seeds/SEED-048-portfolio-scale-fusion.md`
    returns nothing.
  - Formula presence: `grep -c 'Combining \[Technology A\]' .planning/seeds/SEED-048-portfolio-scale-fusion.md`
    returns 1.
- **done:** the new section is the last section of the file, verbatim as
  specified, existing content byte-identical above it.

### Task 3: commit

- **files:** `.planning/seeds/SEED-048-portfolio-scale-fusion.md`,
  `.planning/quick/260706-4cb-append-opportunity-statement-formula-add/` (the
  PLAN.md and the SUMMARY.md the executor writes)
- **action:** Stage the seed file plus this quick-task directory (both paths
  are normally tracked in this repo - `.planning/seeds/` and
  `.planning/quick/` are already in git; use `git add -f` only if a path is
  reported ignored). Commit with exactly this message (recent `git log`
  confirms this repo DOES use the Co-Authored-By trailer, e.g. commits
  af24b69 and 3d1b27a, so include it):

```
docs(seed-048): add Opportunity Statement formula addendum (SEED-049 generator -> SEED-050 critic -> SEED-048 bank, PWS Value Proposition backbone per Brain, confidence 0.90)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```

- **verify:** `git log -1 --format='%s%n%n%b'` shows the subject + trailer;
  `git status` clean for the touched paths; `git show --stat HEAD` lists the
  seed file + the quick-task files, nothing else.
- **done:** one commit, message matches, working tree clean.

## Out of scope

- Any edit to SEED-049, SEED-050, or `.planning/seeds/INDEX.md` (the index
  describes seeds at title level; this addendum changes no title/status).
- Any change to existing SEED-048 content, including its research section.
- Rethinking-room compositing: the Brain routing evidence is cited inline in
  the appended section itself; no new room research entry is minted for a
  doc-only addendum.
- No version bump, no CHANGELOG entry.
