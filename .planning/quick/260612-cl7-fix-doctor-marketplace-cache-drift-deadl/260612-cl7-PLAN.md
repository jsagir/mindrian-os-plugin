---
phase: quick-260612-cl7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/doctor.cjs
  - tests/test-doctor-class-a-vestigial-legacy.cjs
  - scripts/check-version-and-sha.cjs
  - tests/test-check-version-latest-resolution.cjs
autonomous: true
requirements: [RCA-P0a, RCA-P0b, RCA-P2]
tags: [doctor, install-cache, marketplace-cache, update-checker]

must_haves:
  truths:
    - "On a box with BOTH a marketplace-cache install AND a stale legacy ~/.claude/plugins/mindrian-os dir, `doctor --fix --json` exits 0 and post-update activation succeeds (the deadlock is broken)"
    - "When recovery is skipped by design under marketplace-cache topology, the human render says recovery was SKIPPED with the topology reason -- the literal string 'recovery failed: unknown' can no longer render when no recovery was attempted"
    - "On legacy (non-marketplace-cache) topology, doctor drift detection, recovery, render, and exit codes are byte-identical to today"
    - "check-version-and-sha.cjs resolves LATEST from the marketplace catalog pin (the released version), not from main plugin.json's never-released next placeholder"
  artifacts:
    - path: "scripts/doctor.cjs"
      provides: "Topology-aware checkInstallVersion (active-root read even when legacy dir present), recoverySkipped report field, skipped-render branch, skipped-by-design exit-0 semantics"
    - path: "tests/test-doctor-class-a-vestigial-legacy.cjs"
      provides: "Hermetic fixture: legacy-dir + marketplace-cache coexistence asserting exit 0 + no-unknown render + skipped-render branch"
      min_lines: 80
    - path: "scripts/check-version-and-sha.cjs"
      provides: "LATEST resolution from mindrian-marketplace catalog with graceful fallback chain; exported pure helpers + require.main guard"
    - path: "tests/test-check-version-latest-resolution.cjs"
      provides: "Unit test for catalog-pin parsing and fallback ordering"
      min_lines: 40
  key_links:
    - from: "scripts/doctor.cjs checkInstallVersion()"
      to: "lib/core/active-plugin-root.cjs resolveActivePluginRoot()"
      via: "topology check BEFORE the legacy INSTALL_DIR read (not only inside the dir-missing branch)"
      pattern: "resolveActivePluginRoot"
    - from: "scripts/doctor.cjs _finalizeAndExit"
      to: "report.recoverySkipped"
      via: "drift exit-1 gate suppressed when recovery was skipped by design"
      pattern: "recoverySkipped"
    - from: "scripts/post-update-activation.cjs"
      to: "scripts/doctor.cjs --fix --json exit 0"
      via: "exit 0 + install.version === stagingVersion -> ok:true 'already on latest' (no source change needed in the activator)"
      pattern: "exitCode === 0"
    - from: "scripts/check-version-and-sha.cjs fetchLatestVersion"
      to: "raw.githubusercontent.com/jsagir/mindrian-marketplace/main/.claude-plugin/marketplace.json"
      via: "primary LATEST source: plugins[] entry name 'mos' -> .version"
      pattern: "mindrian-marketplace"
---

<objective>
Fix the doctor marketplace-cache drift deadlock root-caused in `.planning/debug/doctor-marketplace-cache-drift-deadlock.md`: break the exit-gate contradiction (RCA P0a), stop rendering the useless "recovery failed: unknown" string (RCA P0b), and repoint the update checker's LATEST resolution to the published marketplace catalog pin instead of main's never-released placeholder (RCA P2).

Purpose: `/mos:update` Step 7 (post-update activation) currently fails 100% on any box where a vestigial legacy `~/.claude/plugins/mindrian-os/` dir coexists with a marketplace-cache install. The deadlock is logical (two sources of truth disagree), so no restart or retry can clear it.

Output: patched `scripts/doctor.cjs` + `scripts/check-version-and-sha.cjs`, plus two new hermetic regression tests in the repo's existing test style.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/debug/doctor-marketplace-cache-drift-deadlock.md
@scripts/doctor.cjs
@scripts/post-update-activation.cjs
@scripts/check-version-and-sha.cjs
@tests/test-doctor-class-a-topology-drift.cjs
@lib/core/active-plugin-root.cjs
</context>

<verified_source_facts>
These were verified against current source during planning; line numbers are anchors, not contracts -- locate by the quoted code/comments:

1. `checkInstallVersion()` (doctor.cjs ~line 428): the marketplace-cache active-root read happens ONLY inside the `!fs.existsSync(INSTALL_DIR)` branch (~lines 429-449). When the legacy dir EXISTS (the RCA box: stale at 1.13.0-beta.30), the function falls to ~lines 451-456 and returns the LEGACY dir's version. That stale version then trips drift detection at ~lines 3964-3972 (`cmp < 0` against `cacheResult.latestParsed`).
2. Class A recovery gate (~line 4007): `if (flags.fix && report.drift.detected && !flags.installState && resolveActivePluginRoot().topology !== 'marketplace-cache')` -- recovery silently skipped under marketplace-cache; neither `classARecovered` nor `recoveryError` is set.
3. Renderer (~line 3327-3329): `if (report.fixRequested)` pushes `recovery failed: ${report.recoveryError || 'unknown'}` whenever drift detected + fix requested -- including when recovery was never attempted.
4. Exit finalizer `_finalizeAndExit` (~lines 4501-4538): documented contract 0/1/2/3/4 at ~4517-4522; `if (report.drift.detected) process.exit(1);` at ~4527 fires unconditionally.
5. `post-update-activation.cjs` (~lines 176-245): shells `doctor --fix --json`; treats `classARecovered` as success; treats `exitCode === 0 && report.install.version === stagingVersion` as "already on latest" success; anything else is "activation failed: <recoveryError || stderr tail || 'doctor exit N'>". NO source change needed there -- once doctor reports the active-root version and exits 0, the activator's existing exit-0 branch succeeds.
6. `check-version-and-sha.cjs fetchLatestVersion()` (~lines 87-107): fetches `raw.githubusercontent.com/jsagir/mindrian-os-plugin/main/.claude-plugin/plugin.json` -- on this repo's release train main always holds the next UNRELEASED placeholder (live bug: LATEST=1.13.1-beta.17 reported while 1.13.1-beta.16 was the real landing). The published source of truth is the marketplace catalog: `https://raw.githubusercontent.com/jsagir/mindrian-marketplace/main/.claude-plugin/marketplace.json`, whose `plugins[]` entry with `name: "mos"` carries `version: "1.13.1-beta.16"` and a `source.ref` tag pin (verified live during planning).
7. `check-version-and-sha.cjs` line 206 calls `main().catch(...)` unconditionally at module top level -- the file currently cannot be `require()`d without executing. A `require.main === module` guard is needed for hermetic unit testing (same idiom as doctor.cjs ~line 4548).
8. Test harness idiom: `tests/test-doctor-class-a-topology-drift.cjs` -- per-test `mkdtempSync` HOME + `USERPROFILE` + `MINDRIAN_PLUGIN_HOME` override, `makeMarketplaceCache(home, version)`, `makeInstalledPluginsJson(home, version, installPath)` (registry shape: `{version: 2, plugins: {'mos@mindrian-marketplace': [{scope, installPath, version, ...}]}}`), `runDoctorJson()` spawnSync wrapper, pass/failTest counters, exit 1 on any failure. New tests MUST mirror this file's structure so the `tests/run-all.sh` `test-*.cjs` glob picks them up.
</verified_source_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Break the drift/recovery contradiction and the unknown-render in doctor.cjs (RCA P0a + P0b)</name>
  <files>scripts/doctor.cjs, tests/test-doctor-class-a-vestigial-legacy.cjs</files>
  <behavior>
    New hermetic test file `tests/test-doctor-class-a-vestigial-legacy.cjs`, mirroring the structure, helpers, and registration idiom of `tests/test-doctor-class-a-topology-drift.cjs` (verified fact 8). Write these tests FIRST, confirm the deadlock tests fail against current source, then implement:
    - Test v.1 (THE RCA fixture -- coexistence deadlock broken): marketplace-cache dir at 1.13.1-beta.16 + installed_plugins.json pointing at it + a LEGACY ~/.claude/plugins/mindrian-os dir containing .claude-plugin/plugin.json at 1.13.0-beta.30. Assert: `doctor --json` exits 0; `report.drift.detected === false`; `report.install.version === '1.13.1-beta.16'` (active root wins over the vestigial legacy dir).
    - Test v.2 (fix path + activator contract): same fixture; `doctor --fix --json` exits 0; parsed report has `classARecovered === null` and `recoveryError === null`; stripped human render of `doctor --fix` (no --json) contains NEITHER 'recovery failed' NOR the bare word 'unknown'.
    - Test v.3 (post-update activation heals): same fixture; `node scripts/doctor.cjs --fix --post-update` exits 0 and output does NOT contain 'activation failed' (mirrors test a.4 in the reference file).
    - Test v.4 (skipped-by-design render + exit, exercises P0b directly): marketplace-cache fixture where the cache holds TWO version dirs (1.13.1-beta.15 and 1.13.1-beta.16) but installed_plugins.json pins the OLDER beta.15 dir as installPath/version. Active-root version (beta.15) < cache latest (beta.16) -> genuine class A drift under marketplace-cache topology. Assert: `doctor --fix --json` exits 0 (skipped-by-design is NOT failure); report carries a `recoverySkipped` object with a topology reason; stripped human render of `doctor --fix` contains 'recovery skipped' and 'marketplace-cache' and does NOT contain 'recovery failed: unknown'. Also assert read-only `doctor --json` (no --fix) on this fixture still exits 1 (drift signaling in read-only mode is preserved).
    - Test v.5 (legacy-topology byte-identical regression): legacy dir at an older version + populated cache + NO installed_plugins.json (resolver topology is NOT marketplace-cache; scrub MINDRIAN_OS_ROOT or pin it per the reference file's a.2 idiom). Assert: read-only `doctor --json` exits 1 with `drift.detected === true`; with `--fix` the recovery path still runs (exit 2 + `classARecovered` set, the atomic-swap behavior covered today by tests/test-doctor-recovery.cjs and test-doctor-atomic-swap.cjs).
  </behavior>
  <action>
    Three surgical changes to scripts/doctor.cjs, per RCA section 5 option (a) PLUS option (b) as defense-in-depth:

    1. P0a primary -- make checkInstallVersion() topology-aware regardless of legacy-dir presence. Hoist the existing marketplace-cache active-root read (currently only inside the `!fs.existsSync(INSTALL_DIR)` branch, verified fact 1) to the TOP of checkInstallVersion(): resolve `resolveActivePluginRoot()` once inside the existing try/catch idiom; if topology is 'marketplace-cache' and the active root's .claude-plugin/plugin.json parses, return `{ status: 'ok', version, parsed, topology: 'marketplace-cache', activeRoot, legacyDirPresent: fs.existsSync(INSTALL_DIR) }` -- the same return shape the dir-missing branch already produces, plus the new informational `legacyDirPresent` boolean (feeds the deferred P1 class I reap; harmless additive field). On ANY throw or non-marketplace-cache topology, fall through to the EXISTING legacy logic untouched -- this guarantees byte-identical behavior on legacy/dev-clone/not-found topologies (the resolver returns those topologies there, so the new early-return never triggers). With this change, the RCA box's install.version becomes the cache version, drift compare is 0, and the contradiction never forms. Reuse the exact comment style of the existing topology-aware block (~lines 430-438) and cite this RCA slug (doctor-marketplace-cache-drift-deadlock) in the comment.

    2. P0a defense-in-depth + P0b state -- record the skip instead of silently falling through. At the class A recovery gate (verified fact 2): keep the existing gate condition for RUNNING recovery exactly as-is, but add an adjacent branch: when `flags.fix && report.drift.detected && !flags.installState` AND the topology check is what blocked recovery (topology === 'marketplace-cache'), set `report.recoverySkipped = { reason: 'topology-marketplace-cache', detail: 'recovery skipped by design: active topology is marketplace-cache; the legacy install dir is vestigial and class A will not touch it (class I owns legacy migration)' }`. Resolve `resolveActivePluginRoot()` ONCE into a local const above the gate and reuse it in both the gate and the skip branch (avoid the current double-call pattern). Initialize `recoverySkipped: null` in the report literal (~line 3958 region) so the JSON shape is stable for consumers.

    3. P0b render + exit semantics. Renderer (verified fact 3): replace the single `recovery failed: ${report.recoveryError || 'unknown'}` line with a three-way branch inside the existing `if (report.fixRequested)` block: (a) if `report.recoveryError` is truthy, render the existing failed line WITHOUT the `|| 'unknown'` fallback text changing (keep `recovery failed: <recoveryError>`); (b) else if `report.recoverySkipped`, render a yellow-glyph line `recovery skipped (topology marketplace-cache -- legacy install dir is vestigial)` using the existing C.yellow ⚠ glyph vocabulary (NO emojis; repo glyphs only); (c) else render `recovery not attempted (gated)` as a deterministic dim line -- the literal 'unknown' string is deleted. Exit finalizer (verified fact 4): change `if (report.drift.detected) process.exit(1);` to suppress exit-1 when `report.recoverySkipped` is set (skipped-by-design exits 0, falling through to the trailing exit-0 path), and extend the documented exit-code contract comment at ~4517-4522 with one additive line: drift detected but recovery skipped by design (marketplace-cache topology) -> exit 0. `recoverySkipped` is only ever set under `flags.fix`, so read-only drift keeps exiting 1 and all legacy-topology codes (0/1/2/3/4) are untouched. NO change to scripts/post-update-activation.cjs: once doctor exits 0 with `install.version` equal to the staging version, its existing exit-0 branch (verified fact 5) returns ok:true 'already on latest'.

    Use the Write/Edit tools only; no heredocs. Register nothing new in runners -- the tests/run-all.sh `test-*.cjs` glob picks the new test up (verified fact 8).
  </action>
  <verify>
    <automated>node tests/test-doctor-class-a-vestigial-legacy.cjs && node tests/test-doctor-class-a-topology-drift.cjs && node tests/test-doctor-fix-renderer.cjs && node tests/test-doctor-class-i.cjs && node scripts/test-doctor-recovery.cjs && node tests/test-doctor-atomic-swap.cjs</automated>
  </verify>
  <done>All 5 new fixture tests pass; the RCA coexistence box (legacy beta.30 + cache beta.16) yields doctor --fix exit 0 with no 'unknown' render and a successful --post-update activation; every pre-existing class A / recovery / renderer / class I test passes unchanged (legacy topology byte-identical).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve LATEST from the marketplace catalog pin in check-version-and-sha.cjs (RCA P2)</name>
  <files>scripts/check-version-and-sha.cjs, tests/test-check-version-latest-resolution.cjs</files>
  <behavior>
    New hermetic unit test `tests/test-check-version-latest-resolution.cjs` (pass/failTest counter style of verified fact 8; zero network -- pure function + injected-stub tests only):
    - Test c.1: `resolveLatestFromCatalog(catalogObj)` returns '1.13.1-beta.16' for a fixture catalog shaped exactly like the live mindrian-marketplace marketplace.json (plugins array, entry name 'mos' with version + source.ref).
    - Test c.2: returns null for a catalog with no 'mos' entry, for `plugins` missing/not-an-array, and for a 'mos' entry missing `version`.
    - Test c.3: fallback ordering -- `fetchLatestVersion({ fetchJson: stub })` with a stub that rejects the catalog URL but resolves the plugin-repo main plugin.json URL returns the placeholder version AND the result object marks the source as degraded (so the REASON line can disclose it). With a stub that resolves the catalog URL, the catalog pin wins and the plugin.json URL is never fetched (assert stub call order/count).
    - Test c.4: with a stub that rejects BOTH URLs, `fetchLatestVersion` rejects (preserving the existing STATUS=NETWORK_ERROR exit-1 path in main()).
    - Test c.5: `require('scripts/check-version-and-sha.cjs')` does NOT execute main() (no stdout side-effect, no process.exit) -- the require.main guard works.
  </behavior>
  <action>
    Restructure scripts/check-version-and-sha.cjs minimally:

    1. Add `MARKETPLACE_REPO_OWNER = 'jsagir'` / `MARKETPLACE_REPO_NAME = 'mindrian-marketplace'` consts and a generic `fetchJson(url)` promise helper (factor the existing https.get + JSON.parse body pattern from fetchLatestVersion, reusing its User-Agent header).

    2. Add pure helper `resolveLatestFromCatalog(catalog)`: returns `catalog.plugins.find(p => p && p.name === 'mos').version` when present and a non-empty string, else null. No I/O.

    3. Rewrite `fetchLatestVersion(deps)` as a resolution chain (deps optional, defaulting to `{ fetchJson }`, for test injection): PRIMARY -- fetch `https://raw.githubusercontent.com/jsagir/mindrian-marketplace/main/.claude-plugin/marketplace.json`, parse via resolveLatestFromCatalog; if it yields a version, resolve `{ version, source: 'marketplace-catalog' }`. FALLBACK (degraded) -- on catalog fetch/parse failure or null pin, fetch the EXISTING `raw.githubusercontent.com/jsagir/mindrian-os-plugin/main/.claude-plugin/plugin.json` and resolve `{ version: manifest.version, source: 'main-plugin-json-degraded' }`. If both fail, reject with the catalog error (main()'s existing catch keeps emitting STATUS=NETWORK_ERROR and exit 1 -- verified offline behavior preserved). Rationale for catalog-over-tags: the marketplace pin IS what `claude plugin update` delivers to users; the newest git tag can briefly lead the catalog during a release cut. Cite the RCA slug + the live bug (reported LATEST=1.13.1-beta.17 while beta.16 was the real landing) in the header comment.

    4. In main(): adapt to the new `{version, source}` envelope; when `source === 'main-plugin-json-degraded'`, append ' (degraded: resolved from main plugin.json placeholder, marketplace catalog unreachable)' to the REASON value on the VERSION_DIFFERS / UP_TO_DATE lines. Output protocol keys (STATUS / LOCAL_VERSION / LATEST_VERSION / LOCAL_SHA / REMOTE_TAG_SHA / REASON) and exit codes 0/1/2 are unchanged -- /mos:update parses these lines.

    5. Wrap the top-level `main().catch(...)` (verified fact 7) in `if (require.main === module) { ... }` and add `module.exports = { resolveLatestFromCatalog, fetchLatestVersion, compareSemver }` -- same guard idiom as doctor.cjs. CLI behavior is unchanged; the module becomes requirable for the unit test.

    Out of scope here (note as deferred in the SUMMARY): the SHA-path inertness (LOCAL_SHA/REMOTE_TAG_SHA both 'unknown' on the RCA box) -- RCA P2(b) -- is an investigation item, not part of this fix.
  </action>
  <verify>
    <automated>node tests/test-check-version-latest-resolution.cjs && node -e "const m=require('./scripts/check-version-and-sha.cjs'); const v=m.resolveLatestFromCatalog({plugins:[{name:'mos',version:'9.9.9'}]}); if(v!=='9.9.9')process.exit(1); console.log('export-surface OK')"</automated>
  </verify>
  <done>resolveLatestFromCatalog + fetchLatestVersion chain unit-tested with zero network; catalog pin is the primary LATEST source; degraded fallback to main plugin.json discloses itself in REASON; offline behavior (NETWORK_ERROR exit 1) preserved; requiring the module has no side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw content -> checker parse | check-version-and-sha.cjs ingests remote JSON (marketplace catalog + plugin.json) over HTTPS |
| Local filesystem -> doctor report | doctor.cjs reads plugin.json files and installed_plugins.json from ~/.claude/plugins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q260612-01 | Tampering | fetchJson catalog ingest | mitigate | JSON.parse inside try/catch (existing idiom preserved); resolveLatestFromCatalog validates shape (plugins array, name match, non-empty string version) and returns null on anything malformed -> degraded fallback, never a crash or injected string in shell-parsed output |
| T-q260612-02 | Spoofing | catalog URL (HTTPS) | accept | HTTPS to github.com infrastructure; same trust level as the existing plugin.json fetch; no new trust surface |
| T-q260612-03 | DoS | doctor exit-0 on skipped recovery | mitigate | recoverySkipped only set under flags.fix AND marketplace-cache topology; read-only drift still exits 1, so monitoring/automation that watches read-only doctor never loses the drift signal |
| T-q260612-04 | Info Disclosure | Brain boundary (Canon Part 8) | accept | Both scripts are pure LOCAL + public-GitHub SIGNAL; zero Brain egress; no user content in any fetch URL or payload |
</threat_model>

<verification>
1. `node tests/test-doctor-class-a-vestigial-legacy.cjs` -- 5/5 PASS (RCA fixture exit 0, no-unknown render, skipped-render branch, read-only exit 1 preserved, legacy topology regression).
2. `node tests/test-check-version-latest-resolution.cjs` -- 5/5 PASS.
3. Full pre-existing class A surface green and unchanged: `node tests/test-doctor-class-a-topology-drift.cjs && node tests/test-doctor-fix-renderer.cjs && node tests/test-doctor-class-i.cjs && node tests/test-doctor-class-j.cjs && node scripts/test-doctor-recovery.cjs && node tests/test-doctor-atomic-swap.cjs && node tests/test-doctor-plugin-disabled-state.cjs`.
4. Grep gate for the dead string: `grep -n "recovery failed: " scripts/doctor.cjs | grep -v "^\s*//"` shows no `|| 'unknown'` fallback remaining on the render line.
5. No emojis introduced: changed output lines use only the existing glyph vocabulary (■ ✓ ⚠ →).
</verification>

<success_criteria>
- The RCA deadlock box (legacy 1.13.0-beta.30 dir + marketplace-cache 1.13.1-beta.16) gets exit 0 from `doctor --fix --json` and a succeeding `--post-update` activation, with no source change to post-update-activation.cjs.
- "recovery failed: unknown" is unreachable; skipped-by-design renders its reason.
- Legacy-topology behavior (drift detection, atomic recovery, exit codes 0/1/2/3/4) is byte-identical, proven by the existing test suite passing unchanged.
- LATEST resolves to the marketplace catalog pin (the version users actually receive), with disclosed degraded fallback and preserved offline behavior.
- Deferred (recorded in SUMMARY, NOT implemented here): P1 class I reap of the vestigial legacy dir + stale-backup GC (mindrian-os.stale-* / *.downgrade-attempt-*), P1 single-source-of-version-truth named check, Part 6 dogfood-acceptance ACPT leg for the coexistence fixture, P2(b) SHA-path inertness investigation.
</success_criteria>

<output>
Create `.planning/quick/260612-cl7-fix-doctor-marketplace-cache-drift-deadl/260612-cl7-SUMMARY.md` when done. Record the four deferred RCA items (P1 reap + GC, P1 version-truth check, Part 6 ACPT leg, P2b SHA inertness) in the SUMMARY's deferred section. On resolve, the RCA at `.planning/debug/doctor-marketplace-cache-drift-deadlock.md` moves to `.planning/debug/resolved/` with a knowledge-base entry per the repo's RCA discipline (the executor should do this as part of the final commit).
</output>
