---
phase: quick-260715-cu8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/entity-extract.cjs
  - tests/test-219-metadata.cjs
  - tests/test-219-low-confidence-disclosure.cjs
  - tests/run-all-219.sh
  - .planning/seeds/SEED-059-fallback-disclosure-convention.md
  - CHANGELOG.md
autonomous: true
requirements: [CU8-REQ-1, CU8-REQ-2, CU8-REQ-3, CU8-REQ-4, CU8-REQ-5]

must_haves:
  truths:
    - "A WHY term that landed via a low-confidence embedding best-guess (no LLM available) is visibly marked low-confidence INSIDE the artifact node's own props, not just in the aggregate status.json tier2_low_confidence counter"
    - "A confidently-classified WHY term (tier-2a confident, tier-2b model verdict, or tier-1 structural seed) carries NO low-confidence marker"
    - "framework_terms stays a comma-joined scalar string, byte-compatible for existing readers (the two 218 tests read it via substring checks); the disclosure is a sibling scalar prop, purely additive"
    - "tests/test-219-metadata.cjs Test 2 asserts the CORRECT post-fix behavior (Hexcel present AND disclosed low-confidence under the live zero-credit state), so the suite REDs again if the disclosure signal ever silently disappears"
    - "bash tests/run-all-221.sh finishes with FAIL=0 (the aggregator that caught this regression, chaining 219 and 218)"
    - "SEED-059 records this fix as a worked example of Site 4 closed for ONE case, without claiming Sites 1-3 or the general convention are resolved"
  artifacts:
    - path: "scripts/entity-extract.cjs"
      provides: "Per-term low-confidence threading through runTier2 -> whyByArtifact -> applyFrameworkTerms, writing the additive framework_terms_low_confidence sibling scalar prop"
      contains: "framework_terms_low_confidence"
    - path: "tests/test-219-low-confidence-disclosure.cjs"
      provides: "Dedicated hermetic proof of the disclosure mechanism: low-margin no-LLM candidate marked, high-margin confident candidate NOT marked, confident re-run removes the marker, subset invariant"
      min_lines: 80
    - path: "tests/test-219-metadata.cjs"
      provides: "Test 2 fixed to assert correct additive behavior + disclosure, not byte-identical props and not blind acceptance"
      contains: "framework_terms_low_confidence"
    - path: "tests/run-all-219.sh"
      provides: "run_if file-gated leg for the new disclosure test"
      contains: "test-219-low-confidence-disclosure"
    - path: ".planning/seeds/SEED-059-fallback-disclosure-convention.md"
      provides: "Worked-example note: Site 4 closed for the framework_terms case by quick 260715-cu8; aggregate counter existed before, per-result disclosure did not"
      contains: "260715-cu8"
    - path: "CHANGELOG.md"
      provides: "Unreleased Fixed entry in the same house style as the other entries from 2026-07-14/15"
      contains: "260715-cu8"
  key_links:
    - from: "scripts/entity-extract.cjs runTier2 (the modelLabels === null escalation branch)"
      to: "applyFrameworkTerms"
      via: "whyTerms entries carrying a per-term lowConfidence flag, aggregated per artifact in whyByArtifact"
      pattern: "lowConfidence"
    - from: "applyFrameworkTerms"
      to: "the memory_artifact node props"
      via: "insertNode UPSERT merge writing framework_terms (unchanged) + framework_terms_low_confidence (new sibling scalar)"
      pattern: "framework_terms_low_confidence"
    - from: "tests/run-all-221.sh"
      to: "tests/run-all-219.sh"
      via: "the 219 no-regression run_if leg (the chain that caught this regression)"
      pattern: "run-all-219"
---

<objective>
Fix the live HEAD regression caught by tests/run-all-221.sh (via its run-all-219.sh chain): test-219-metadata.cjs Test 2 ("no-frontmatter node props byte-identical") fails because the two-tier classifier (quick 260714-k44) now legitimately classifies "Hexcel" on the plain fixture, and under the zero-credit account state the honest-degrade path lands it in framework_terms as a low-confidence embedding best-guess. The degrade chain works as designed; the GAP is that low-confidence exists ONLY in the aggregate status.json counter (tier2_low_confidence), not per-term inside framework_terms itself. Once written, a low-confidence guess is structurally indistinguishable from a confidently-resolved WHY term. This is SEED-059 Site 4 (dependency-fallback disclosure) in miniature.

Purpose: close the per-result disclosure gap for framework_terms (and ONLY framework_terms; no scope expansion), fix the test to pin the correct behavior, and cross-reference SEED-059 with the worked example.

Output: per-term low-confidence disclosure on artifact nodes, a corrected Test 2, a dedicated mechanism test wired into run-all-219.sh, a SEED-059 note, a CHANGELOG entry, and a fully green run-all-221.sh.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@scripts/entity-extract.cjs
@lib/core/eureka/embedding-classifier.cjs
@tests/test-219-metadata.cjs
@tests/run-all-219.sh
@.planning/seeds/SEED-059-fallback-disclosure-convention.md
@.planning/quick/260714-k44-replace-the-tier-2-what-vs-why-classifie/260714-k44-SUMMARY.md
@CHANGELOG.md (lines 1-40, the Unreleased section, for house style)
</context>

<planning_findings>
Description item 1 (read the real code first) was completed during planning. Findings the executor builds on (re-confirm by reading the named line ranges, do not re-derive):

1. **framework_terms current shape:** a comma-joined SCALAR STRING plus framework_term_count (number), written by applyFrameworkTerms (scripts/entity-extract.cjs lines 591-628) under the T-219-05 scalar-only prop discipline ("TWO SCALAR PROPS ONLY"). It is NOT an array and carries NO per-term metadata. Per-term objects inside framework_terms would violate the scalar-only discipline and break the substring-read pattern; they are ruled out.
2. **Where the signal dies:** runTier2 (lines 475-581). The tier-2b escalation loop's no-LLM branch (lines 553-558) calls routeLabel(it.e, it.bestGuess, whatEntities, whyTerms) pushing bare { name, sourceArtifactId } and increments only the aggregate lowConfidence counter. whyByArtifact (lines 686-690) then reduces to artifactId -> [name], so applyFrameworkTerms receives bare names with no confidence signal.
3. **Reader audit (grep -rn framework_terms, node_modules and .planning excluded):** the ONLY writer is entity-extract.cjs; the ONLY readers are tests/test-218-what-why-classifier.cjs and tests/test-218-extend-to-artifacts.cjs, both via substring indexOf checks on the comma-joined string. NOTHING else in production code reads framework_terms today. An additive sibling prop cannot break any existing reader. This finding must be documented in the applyFrameworkTerms doc comment (description item 2's "document that finding" clause).
4. **Chosen mechanism (decision made, per description item 2):** a sibling scalar prop `framework_terms_low_confidence` (comma-joined string, always a subset of framework_terms) written alongside framework_terms by the same UPSERT. Fits the existing merge/write pattern exactly, zero schema churn, graceful for string readers.
5. **Regression reproduced live during planning:** the plain fixture node gains framework_terms: 'Hexcel' + framework_term_count: 1 (deepEqual diff confirmed). Mechanics: the local encoder is cached on this machine so tier-2a runs even under the offline preload (allowRemoteModels=false only blocks downloads); Hexcel scores margin 0.0126 < 0.10 so confident:false with bestGuess 'why'; the Anthropic key resolves but has zero credits so tier-2b fails; the k44 honest-degrade contract routes the best-guess to whyTerms with lowConfidence += 1. Note: the SAME low-confidence outcome occurs with NO key at all (both paths leave modelLabels null), so the disclosure branch is exercised on any cached-encoder machine without a working LLM.
6. **Adjacent gap, explicitly OUT OF SCOPE (description's no-scope-expansion directive):** a low-confidence WHAT best-guess becomes a proposed entity node with no per-node disclosure marker, the exact sibling of this bug on the WHAT side. Document as a future-seed candidate in the SEED-059 note (Task 3); do NOT fix here.
</planning_findings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread per-term low-confidence through runTier2 into a framework_terms_low_confidence sibling prop</name>
  <files>scripts/entity-extract.cjs</files>
  <behavior>
    - A WHY term routed through the no-LLM escalation branch (modelLabels null, the lowConfidence += 1 branch) lands in BOTH framework_terms and framework_terms_low_confidence on its source artifact node.
    - A WHY term from any confident path (tier-1 structural seed, tier-2a confident, tier-2b model verdict) lands in framework_terms only; framework_terms_low_confidence never names it.
    - A term previously marked low-confidence that arrives CONFIDENT on a later run is removed from framework_terms_low_confidence (confident upgrade wins); framework_terms itself stays unioned as today.
    - framework_terms_low_confidence is always a subset of framework_terms; when the subset is empty the prop is absent (deleted), never an empty string.
    - framework_terms and framework_term_count semantics are byte-compatible with today for all-confident inputs (the 218 suite proves this).
  </behavior>
  <action>
    Implement per-term low-confidence disclosure in scripts/entity-extract.cjs, additively:

    1. **routeLabel** (line ~399): accept an optional lowConfidence flag (5th param or an options object, executor's call); when the label is 'why' and the flag is set, push { name, sourceArtifactId, lowConfidence: true } instead of the bare pair. All existing call sites stay unflagged EXCEPT the runTier2 no-LLM escalation branch (the else branch at lines ~553-558 where lowConfidence += 1), which passes the flag. runDegradeHzx is untouched (its lowConfidence is 0 by design; the hzx encoder-unavailable degrade stays verbatim per the k44 contract).
    2. **whyByArtifact aggregation** (runExtraction, lines ~686-690): carry { name, lowConfidence } entries per artifact instead of bare names.
    3. **applyFrameworkTerms** (lines ~591-628): keep the framework_terms + framework_term_count merge EXACTLY as-is (comma-joined string, case-insensitive dedup, first-seen casing, FRAMEWORK_TERM_CAP). Add the sibling prop maintenance: parse existing props.framework_terms_low_confidence (comma-split, same idiom as the existing parse); union in the incoming low-confidence names; REMOVE any name that arrives confident in this call (upgrade semantics); prune the result to names present in the merged framework_terms (subset invariant, case-insensitive match consistent with the dedup); write it back comma-joined when non-empty, DELETE the prop when empty. Scalars only (T-219-05); review_status and provenance columns stay untouched (same UPSERT, same merge discipline); still returns 'applied' | 'skipped' | 'none' and NEVER throws.
    4. **Document the reader-audit finding** in the applyFrameworkTerms doc comment: framework_terms has no production readers outside this pipeline (only the two 218 tests, substring reads), so the sibling scalar prop is additive and safe (planning_findings item 3).
    5. Do NOT touch status.json keys (tier2_low_confidence already exists and stays), the report line, runDegradeHzx, embedding-classifier.cjs, or entity-classifier.cjs. No new network reach (the run-all-219 zero-network grep gate and test-219-metadata Test 6 must stay green). No em-dashes anywhere.

    Expected mid-plan state: tests/test-219-metadata.cjs Test 2 STILL fails after this task (its assertion is fixed in Task 2). That is correct; do not weaken this task to avoid it.

    Commit atomically: `fix: disclose low-confidence WHY terms per-term in framework_terms_low_confidence (quick 260715-cu8)`.
  </action>
  <verify>
    <automated>bash tests/run-all-218.sh</automated>
    Expect PASS=15 FAIL=0 (all-confident and injected-stub paths byte-compatible: the what-why-classifier integration legs and extend-to-artifacts leg still green). Also: grep -n "framework_terms_low_confidence" scripts/entity-extract.cjs returns hits in both runTier2 threading and applyFrameworkTerms.
  </verify>
  <done>Low-confidence WHY terms carry a per-term marker on the artifact node via framework_terms_low_confidence; confident terms and the existing framework_terms/framework_term_count writes are byte-compatible; run-all-218.sh fully green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dedicated disclosure test + fix test-219-metadata Test 2 to pin the correct behavior</name>
  <files>tests/test-219-low-confidence-disclosure.cjs, tests/test-219-metadata.cjs, tests/run-all-219.sh</files>
  <behavior>
    - Leg 1: a low-margin candidate with no LLM escalation available is visibly marked low-confidence in framework_terms_low_confidence (not just the aggregate counter).
    - Leg 2: a high-margin confident candidate lands in framework_terms and is NOT in framework_terms_low_confidence.
    - Leg 3: a second run where the same term returns confident REMOVES the marker (upgrade), and framework_terms unions without duplication.
    - Leg 4: subset invariant: every name in framework_terms_low_confidence appears in framework_terms.
    - Fixed Test 2: the Hexcel prop IS present (legitimate additive classification) AND disclosed low-confidence under the live zero-credit state; the leg REDs again if the disclosure signal silently disappears while framework_terms still gains terms.
  </behavior>
  <action>
    1. **New tests/test-219-low-confidence-disclosure.cjs** (hermetic, offline, mirrors test-219-metadata.cjs's mkRoom/seedRoom/readNode fixture pattern verbatim including the eureka-offline-preload require). Drive runExtraction directly with the EXISTING injection seams (no new seams): opts.embedClassifyImpl returning deterministic verdicts ({ ok: true, results: { Name: { label, margin, confident } } }) and opts._forceNoLlm: true for the no-LLM legs. Legs per the behavior block above. Console output and PASS-count style match the sibling 219 tests.
    2. **Fix Test 2 in tests/test-219-metadata.cjs** (lines ~160-164): replace the byte-identical deepEqual with the correct-behavior assertion. After the main([roomDir,'run']) call, read the run's status.json (roomDir/.mindrian/entity-extract/status.json). Assert unconditionally: the pre-existing props (title, path, section, kind) are unchanged, review_status untouched, and any ADDED keys are limited to { framework_terms, framework_term_count, framework_terms_low_confidence }. Then branch on the live state, k44-deviation-1 style so the leg is deterministic on every machine: IF framework_terms contains 'Hexcel' (the cached-encoder, no-working-LLM state that caught this regression, and the state on this machine): assert status.tier2_low_confidence >= 1 AND framework_terms_low_confidence contains 'Hexcel' (the disclosure pin: this is the assertion that REDs if the signal disappears). ELSE (encoder-absent hzx degrade, or a future funded key resolving Hexcel confidently as WHAT): assert framework_terms_low_confidence, if present, does not contain 'Hexcel'. Log which branch ran. Update the Test 2 console message and the leg comment to describe the new contract (additive classification is legitimate; UNDISCLOSED low-confidence is the bug).
    3. **Wire the new test into tests/run-all-219.sh** as a run_if file-gated leg directly after the "219-02 metadata slice" leg, matching the existing label/format idiom, e.g.: run_if "219 low-confidence disclosure (per-term, quick 260715-cu8)" "tests/test-219-low-confidence-disclosure.cjs" node tests/test-219-low-confidence-disclosure.cjs. Touch nothing else in the harness.
    4. No em-dashes anywhere in any of the three files.

    Commit atomically: `test: pin per-term low-confidence disclosure + correct 219-metadata Test 2 contract (quick 260715-cu8)`.
  </action>
  <verify>
    <automated>node tests/test-219-low-confidence-disclosure.cjs && node tests/test-219-metadata.cjs && bash tests/run-all-219.sh</automated>
    Expect all three green (run-all-219.sh FAIL=0 including the new leg and the previously-failing metadata leg).
  </verify>
  <done>The dedicated mechanism test proves marked-vs-unmarked and the upgrade path; Test 2 asserts Hexcel present AND disclosed under the live state; run-all-219.sh fully green.</done>
</task>

<task type="auto">
  <name>Task 3: SEED-059 worked-example cross-ref + CHANGELOG entry + full run-all-221.sh acceptance</name>
  <files>.planning/seeds/SEED-059-fallback-disclosure-convention.md, CHANGELOG.md</files>
  <action>
    1. **SEED-059 note:** append a short dated section (e.g. "## Worked example: Site 4 closed for one case (2026-07-15, quick 260715-cu8)") stating: Phase 218 / quick hzx / quick k44 shipped the tier2_low_confidence AGGREGATE counter in status.json, but per-result disclosure inside framework_terms (the data structure actually written onto artifact nodes and read downstream) did not exist until quick 260715-cu8 added framework_terms_low_confidence; this is one concrete Site 4 (dependency-fallback disclosure) instance closed. Explicitly state the seed's scope is NOT rewritten and Sites 1-3 plus the general convention remain open. Add one sentence naming the adjacent still-open instance found during this fix: a low-confidence WHAT best-guess becomes a proposed entity node with NO per-node disclosure marker (future-seed candidate, deliberately not fixed here per the no-scope-expansion directive). Do not edit any other section of the seed.
    2. **CHANGELOG.md:** add an entry under `## [Unreleased] -- v1.15.3-beta.19 (in progress)` / `### Fixed`, matching the house style of the existing entries (bold one-sentence headline, wrapped plain prose, honest-disclosure framing, no em-dashes, trailing "(Quick task 260715-cu8.)"). Substance: a low-confidence embedding best-guess landing in framework_terms was structurally indistinguishable from a confidently-resolved WHY term (the signal lived only in the aggregate status.json counter); each term that lands via the no-LLM degrade path is now disclosed per-term in an additive framework_terms_low_confidence prop, a confident later run removes the marker, existing readers of framework_terms are unaffected, and the 219 metadata test now pins the disclosure so it cannot silently disappear. Caught live by the run-all-221 chain.
    3. **Final acceptance:** run the FULL aggregator, bash tests/run-all-221.sh, and require FAIL=0. This chains 219 (which chains 218) and the 211/215/216/220 regression legs; several minutes runtime is expected. Do not substitute the narrower suites.
    4. Commit atomically: `docs: SEED-059 Site 4 worked example + CHANGELOG for per-term low-confidence disclosure (quick 260715-cu8)`. If .gitignore blocks the seed file, use git add -f (repo convention for tracked .planning files).
  </action>
  <verify>
    <automated>bash tests/run-all-221.sh</automated>
    Expect the final line: Phase 221: PASS=N FAIL=0 SKIP=M, exit 0. Also: grep -c "260715-cu8" .planning/seeds/SEED-059-fallback-disclosure-convention.md returns >= 1 and grep -c "260715-cu8" CHANGELOG.md returns >= 1.
  </verify>
  <done>SEED-059 carries the worked-example note without scope rewrite; CHANGELOG Unreleased has the house-style entry; run-all-221.sh green end to end; three atomic commits total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Fully local pipeline; no new network reach, no package installs, no new invocable surface (Canon Part 11 untouched) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CU8-01 | Tampering | applyFrameworkTerms props write | mitigate | Reuse the insertNode UPSERT merge discipline verbatim: scalars only (T-219-05), review_status + provenance never touched, subset invariant test-pinned |
| T-CU8-02 | Repudiation | framework_terms low-confidence provenance (the bug itself) | mitigate | Per-term framework_terms_low_confidence disclosure + Test 2 pin + dedicated mechanism test; the signal can no longer silently disappear |
| T-CU8-03 | Information disclosure | Part 8 graph boundary | mitigate | Zero new egress; run-all-219 zero-network grep gate + test-219-metadata Test 6 must stay green |
</threat_model>

<coverage_audit>
| Source item (task description) | Covered by |
|---|---|
| 1. Read real code path first, confirm framework_terms shape | Done during planning (planning_findings 1-5); executor re-confirms via named line ranges in Task 1 |
| 2. Design + implement per-term disclosure, additive, document reader audit | Task 1 (sibling scalar prop, mechanism decision recorded in planning_findings 4) |
| 3. Fix test-219-metadata Test 2 to correct behavior | Task 2 step 2 |
| 4. Dedicated low-confidence disclosure test | Task 2 step 1 + wiring step 3 |
| 5. SEED-059 cross-reference, no scope rewrite | Task 3 step 1 |
| 6. CHANGELOG Unreleased house-style entry | Task 3 step 2 |
| 7. GSD workflow, atomic commits, no em-dashes | All tasks (one commit each, em-dash rule restated per task) |
| 8. run-all-221.sh as final acceptance | Task 3 step 3 |
| No scope expansion; adjacent gaps documented not fixed | planning_findings 6 -> Task 3 step 1 (WHAT-side sibling gap named for a future seed) |
</coverage_audit>

<verification>
- bash tests/run-all-218.sh: PASS=15 FAIL=0 (byte-compatibility of the confident paths).
- bash tests/run-all-219.sh: FAIL=0 including the fixed metadata leg and the new disclosure leg.
- bash tests/run-all-221.sh: FAIL=0 end to end (the aggregator that caught the regression; the ONLY acceptable final acceptance).
- grep gates stay green (zero-network on entity-extract.cjs, no raw INSERT INTO, em-dash sweep).
- git log shows three atomic commits (fix / test / docs), no em-dashes in any touched file.
</verification>

<success_criteria>
- The live regression is resolved by pinning CORRECT behavior: Hexcel's framework_terms entry is present AND disclosed low-confidence, not reverted and not blindly accepted.
- Per-term disclosure exists on the artifact node itself; the aggregate counter is no longer the only trace of the honest-degrade path.
- framework_terms stays a plain comma-joined string; nothing that reads it today changes behavior.
- SEED-059 gains a concrete Site 4 worked example without any claim of full resolution; the adjacent WHAT-side gap is documented for a future seed, not fixed.
- run-all-221.sh green end to end.
</success_criteria>

<output>
Create `.planning/quick/260715-cu8-fix-a-live-regression-on-head-discovered/260715-cu8-SUMMARY.md` when done.
</output>
