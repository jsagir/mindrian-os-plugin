---
phase: quick/260716-rd2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/huji-run-one.cjs
  - .planning/phases/229-huji-pitch-feedback-module/229-STAGE-B-DISCOVERY-BUG-HANDOFF.md
autonomous: true
requirements: [229-STAGE-B-DISCOVERY-BUG]

must_haves:
  truths:
    - "Stage B's spawned session has Glob in --allowedTools, so it can list its room directory and discover pitch-intake-<subId>.md"
    - "Stage A's allowedTools string is byte-identical to before (Read,Bash(node lib/core/*)) - only Stage B changes"
    - "runOne and runOneAsync still return structurally identical failure envelopes (D14 parity gate green)"
    - "bash tests/run-all-229.sh reports PASS=10 FAIL=0 SKIP=0"
    - "The handoff doc records fix shape A as the navigator's chosen fix and the Neo4j MCP agent exposure as a resolved, navigator-approved risk acceptance"
  artifacts:
    - path: "scripts/huji-run-one.cjs"
      provides: "Widened Stage B allowedTools (shape A fix)"
      contains: "Read,Write,Edit,Glob,Bash(node lib/core/*)"
    - path: ".planning/phases/229-huji-pitch-feedback-module/229-STAGE-B-DISCOVERY-BUG-HANDOFF.md"
      provides: "Decision log: shape A chosen, Neo4j risk accepted"
      contains: "risk acceptance"
  key_links:
    - from: "scripts/huji-run-one-async.cjs"
      to: "scripts/huji-run-one.cjs"
      via: "require('./huji-run-one.cjs') destructuring buildStageBArgs (async twin lines 70-75)"
      pattern: "buildStageBArgs"
    - from: "tests/run-all-229.sh"
      to: "lib/memory/huji-run-one-async-parity.test.cjs"
      via: "REQ-D14 run_if leg"
      pattern: "huji-run-one-async-parity"
---

<objective>
Fix the Phase 229 Stage B discovery bug (confirmed root cause, 4 live runs, ~$5.21 evidence
already gathered in the handoff doc) by implementing the navigator's chosen fix shape A:
widen `buildStageBArgs`'s `--allowedTools` in `scripts/huji-run-one.cjs` so the Stage B
live session can discover `pitch-intake-<subId>.md` in its own room directory. Then log
both navigator decisions from this session (fix shape A chosen; Neo4j MCP agent exposure
risk-accepted) into the handoff doc so the decision trail is durable.

**Implementation choice within shape A (planner decision, per navigator's delegation):
add `Glob` to the allowedTools list, NOT a new discovery script.** Justification:

1. **Smaller, more targeted diff.** Adding `Glob` is a one-token change to one string
   literal (line 290). A discovery script would be a net-new `lib/core/*.cjs` invocable
   surface, which under Canon Part 11 (CIRS) must be born wired with a declared HITL
   shape and registered, and under Canon Part 7 must be justified against reuse - a far
   larger footprint for the same outcome.
2. **Exactly matches the confirmed failure mechanism.** The handoff's evidence is "No
   Glob -> cannot list the room directory to find pitch-intake-<subId>.md". Glob is
   read-only discovery; it grants no new write or execution capability. The Bash
   allowlist stays `Bash(node lib/core/*)` unchanged, which already permits querying
   `.mindrian/room.db` through `node lib/core/navigation.cjs` (the single SQL
   navigation chokepoint) if the session chooses that route.
3. **Parity by construction.** `scripts/huji-run-one-async.cjs` requires
   `buildStageBArgs` from the sync file (it does not duplicate the arg array), so one
   edit propagates to both twins with zero drift risk. `scripts/huji-batch.cjs` requires
   only the sync `runOne` (asserted by the parity test's caller-side audit) and carries
   no allowedTools string of its own - verified by grep before planning.

This change IS a deliberate, navigator-reviewed exception to the 229-10 byte-for-byte
lock on `scripts/huji-run-one.cjs` (the handoff's own words: "Do not touch them without
a deliberate, reviewed decision" - this is that decision). No automated test hashes the
file, but the D14 parity gate and the full phase suite MUST be run and green after the
edit.

Purpose: unblock Phase 229's grading pipeline - Stage B currently halts honestly with
NO-SUBMISSION-FOUND because it structurally cannot see its own populated input.
Output: fixed `buildStageBArgs`, updated handoff doc, green D14 + run-all-229.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/229-huji-pitch-feedback-module/229-STAGE-B-DISCOVERY-BUG-HANDOFF.md
@scripts/huji-run-one.cjs
@lib/memory/huji-run-one-async-parity.test.cjs
@tests/run-all-229.sh
</context>

<constraints>
- Do NOT re-run /tmp/huji-demo-run.cjs or any live HUJI-sample run in this plan. Each
  attempt costs real money (~$0.1-$2.5); the handoff already holds the evidence from 4
  runs. A live confirmation run happens only later, only once, and only if the navigator
  explicitly asks for it.
- Do NOT touch scripts/huji-batch.cjs or scripts/huji-run-one-async.cjs. The async twin
  inherits the fix via its require of buildStageBArgs; batch has no allowedTools string.
- Do NOT touch Stage A's allowedTools (line 261, 'Read,Bash(node lib/core/*)'). The bug
  is Stage B only.
- Do NOT plan or make any code change related to the Neo4j MCP agent wiring. Task 2 is a
  doc-only decision log.
- No em-dashes anywhere (code comments and doc edits alike) - hyphens only.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add Glob to Stage B allowedTools in buildStageBArgs and run both gates</name>
  <files>scripts/huji-run-one.cjs</files>
  <action>
In scripts/huji-run-one.cjs, function buildStageBArgs (line 280), change the
--allowedTools value on line 290 from 'Read,Write,Edit,Bash(node lib/core/*)' to
'Read,Write,Edit,Glob,Bash(node lib/core/*)'. This is the entire code change - one
token inserted into one string literal. Leave every other arg (model, schema, rubric,
permission-mode, max-turns, budget, session-persistence) byte-identical, and leave
buildStageAArgs (line 261) completely untouched.

Update the comment block directly above buildStageBArgs (lines 271-279) to record, in
the file's existing comment voice: (a) Glob was added 2026-07-16 as the shape-A fix for
the Stage B discovery bug - without it the live session could not list its own room
directory to find pitch-intake-<subId>.md and halted honestly with NO-SUBMISSION-FOUND
(4 live runs, evidence in .planning/phases/229-huji-pitch-feedback-module/
229-STAGE-B-DISCOVERY-BUG-HANDOFF.md); (b) this edit is a deliberate, navigator-reviewed
exception to the 229-10 byte-for-byte lock on this file; (c) Glob is read-only discovery
- the Bash allowlist stays node lib/core/* only. Use hyphens, never em-dashes, matching
the rest of the file.

After the edit, run the two required gates from the repo root
(/home/jsagi/dev/MindrianOS-Plugin):

1. node lib/memory/huji-run-one-async-parity.test.cjs - the D14 gate. Must exit 0 and
   print the all-checks-passed line, proving runOne and runOneAsync still return
   structurally identical { ok, reason, detail } failure envelopes at both stages (the
   async twin imports buildStageBArgs from this file, so the fix propagates by
   construction - this run proves it).
2. bash tests/run-all-229.sh - must end PASS=10 FAIL=0 SKIP=0. If ANY count differs,
   do not just report the new numbers: read the failing leg's output, identify the root
   cause, and either fix it (if caused by this change) or explain precisely why the
   count changed before proceeding.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && grep -n "allowedTools" scripts/huji-run-one.cjs | grep -q "Read,Write,Edit,Glob,Bash(node lib/core/\*)" && grep -n "allowedTools" scripts/huji-run-one.cjs | grep -q "'Read,Bash(node lib/core/\*)'" && node lib/memory/huji-run-one-async-parity.test.cjs && bash tests/run-all-229.sh</automated>
  </verify>
  <done>Line 290's allowedTools contains Glob between Edit and Bash; Stage A's line 261 string is unchanged; the comment block above buildStageBArgs records the dated, navigator-reviewed lock exception; D14 parity test exits 0; run-all-229.sh prints PASS=10 FAIL=0 SKIP=0 and exits 0. No live demo run was executed.</done>
</task>

<task type="auto">
  <name>Task 2: Log both navigator decisions into the Stage B handoff doc</name>
  <files>.planning/phases/229-huji-pitch-feedback-module/229-STAGE-B-DISCOVERY-BUG-HANDOFF.md</files>
  <action>
Update .planning/phases/229-huji-pitch-feedback-module/229-STAGE-B-DISCOVERY-BUG-HANDOFF.md
to reflect the two navigator decisions made in the 2026-07-16 rd2 session. Doc edits
only - keep the evidence sections (2 and the evidence trail) untouched, they are the
durable record. Four targeted edits:

1. Top status line (line 3-5): replace "Root cause CONFIRMED, not fixed. Navigator wants
   a new session with a different approach." with a status recording: root cause
   CONFIRMED; fix shape A chosen by the navigator (2026-07-16 rd2 session) and
   implemented - Glob added to Stage B's --allowedTools in
   scripts/huji-run-one.cjs::buildStageBArgs as a deliberate, reviewed exception to the
   229-10 byte-for-byte lock; D14 parity gate and run-all-229.sh re-run green after the
   change. Keep the existing "do not re-derive by re-running live demos" warning.

2. Section 3 heading and body: change the heading parenthetical "(the navigator wants to
   pick a different approach - present both, do not assume)" to record the decision is
   made. Mark shape A as CHOSEN (navigator ruling, 2026-07-16 rd2 session), with the
   one-line implementation note (Glob added, not a discovery script - smaller diff, no
   net-new Canon Part 11 surface, read-only capability only). Mark shape B as considered
   and NOT adopted. Keep both descriptions in place for the record.

3. Section 4: retitle from "Open, unresolved, separate from the bug above" to a RESOLVED
   heading recording navigator-approved risk acceptance (2026-07-16 rd2 session). Keep
   the factual description of what was found (verbatim full Larry system prompt in the
   agent's system_prompt, Text2Cypher with raw Cypher templates, is_mcp_enabled true,
   is_private false). Replace the "still unresolved - flag it again in the new session,
   do not let it drop" framing and the "Do NOT connect anything ... until the navigator
   confirms" instruction with: the navigator reviewed this exposure and explicitly chose
   to ACCEPT the risk rather than block on it; this is a closed, navigator-approved risk
   acceptance, not an open item; nothing was wired in and no code change is planned; any
   future decision to actually wire that agent in would be a new, separate decision.

4. Section 5 ("What the new session should do first"): mark items 3 and 5 as resolved
   (item 3: fix shape decided - A; item 5: Neo4j question resolved as risk acceptance,
   see section 4). Leave item 4 standing as-is - the single post-fix live confirmation
   run remains future, optional, and navigator-triggered only.

Style: hyphens only, never em-dashes; keep the file's existing plain, evidence-first
voice; do not renumber sections.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && F=.planning/phases/229-huji-pitch-feedback-module/229-STAGE-B-DISCOVERY-BUG-HANDOFF.md && ! grep -qP '\x{2014}' "$F" && grep -qi "risk acceptance" "$F" && grep -q "shape A" "$F" && grep -qi "CHOSEN\|chosen" "$F"</automated>
  </verify>
  <done>The handoff doc's status line says shape A was chosen and implemented; section 3 marks shape A CHOSEN and shape B not adopted; section 4 is retitled RESOLVED and records the navigator-approved risk acceptance with the factual findings preserved; section 5 items 3 and 5 are marked resolved; zero em-dash characters anywhere in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage B headless session -> local filesystem | Untrusted student transcript data flows through a spawned claude session whose tool surface this plan widens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q260716-01 | Elevation of Privilege | buildStageBArgs allowedTools | mitigate | Add ONLY Glob (read-only path discovery). Bash allowlist stays node lib/core/* exactly; no new write or exec capability; Stage A surface untouched. Verified by the Task 1 grep gate asserting both exact strings. |
| T-q260716-02 | Tampering | Stage B session acting on student-controlled file content it can now discover | accept | Session already had Read/Write/Edit inside roomDir with dontAsk; Glob adds listing only, no new mutation power. The existing G1-G6 gates (quote-grounding, schema, Part 8 hygiene) still gate the .done marker. |
| T-q260716-03 | Information Disclosure | Neo4j-hosted MCP agent (verbatim Larry prompt, is_private false) | accept | Navigator-approved risk acceptance, 2026-07-16 rd2 session (Task 2 records it). Nothing is wired in; no code change; any future wiring is a separate decision. |
</threat_model>

<verification>
From /home/jsagi/dev/MindrianOS-Plugin:

1. `grep -n "allowedTools" scripts/huji-run-one.cjs` shows exactly two lines: Stage A
   unchanged ('Read,Bash(node lib/core/*)') and Stage B now containing Glob
   ('Read,Write,Edit,Glob,Bash(node lib/core/*)').
2. `node lib/memory/huji-run-one-async-parity.test.cjs` exits 0 (D14 parity holds).
3. `bash tests/run-all-229.sh` ends PASS=10 FAIL=0 SKIP=0 and exits 0.
4. `grep -P '\x{2014}' .planning/phases/229-huji-pitch-feedback-module/229-STAGE-B-DISCOVERY-BUG-HANDOFF.md` returns nothing (no em-dashes).
5. `git diff --stat` touches only the two files in files_modified (plus this plan's
   SUMMARY) - huji-batch.cjs and huji-run-one-async.cjs are byte-identical to before.
6. No live HUJI demo run appears anywhere in the transcript or spend.
</verification>

<success_criteria>
- Stage B's spawn args include Glob; the discovery mechanism the handoff proved broken
  (cannot list roomDir to find pitch-intake-<subId>.md) is structurally unblocked.
- Both twins share the fix through the single buildStageBArgs source; D14 parity green.
- Phase suite stays PASS=10 FAIL=0 SKIP=0 (or any delta is root-caused and explained,
  never just reported).
- Both navigator decisions (shape A chosen; Neo4j exposure risk-accepted) are durably
  logged in the handoff doc with the open-item framing removed.
- Zero dollars of live-run spend in this plan.
</success_criteria>

<output>
Create `.planning/quick/260716-rd2-phase-229-stage-b-discovery-bug-fix-shap/260716-rd2-SUMMARY.md`
when done. Commit both modified files together (they are one decision):
`fix(229): widen Stage B allowedTools with Glob (discovery bug, shape A) + log navigator rulings in handoff`
</output>
