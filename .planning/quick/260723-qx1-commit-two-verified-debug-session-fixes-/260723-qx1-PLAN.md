---
phase: quick-260723-qx1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp/tools/room.cjs
  - scripts/gsd-graph-derive-drain.cjs
  - tests/run-all-198.sh
  - tests/test-graph-derive-sweep.cjs
  - tests/test-room-search-rank-before-cap.cjs
autonomous: true
requirements:
  - DEFECT-4a-graph-derive-silent-clear
  - room_search-rank-before-cap
must_haves:
  truths:
    - "tests/test-graph-derive-sweep.cjs runs green (re-verify) before the graph-derive commit"
    - "The graph-derive silent-clear fix (Defect 4a) is one atomic commit containing EXACTLY scripts/gsd-graph-derive-drain.cjs and tests/test-graph-derive-sweep.cjs"
    - "tests/run-all-198.sh runs green (re-verify) before the room_search commit"
    - "The room_search rank-before-cap fix is one atomic commit containing EXACTLY lib/mcp/tools/room.cjs, tests/test-room-search-rank-before-cap.cjs, and tests/run-all-198.sh"
    - "No file outside the two specified sets is staged or committed; .planning/debug/*.md, .planning/research/*.md, and graphify-out/ stay untracked/unchanged"
    - "Nothing is pushed (origin/main is not advanced)"
  artifacts:
    - path: "scripts/gsd-graph-derive-drain.cjs"
      provides: "keep-on-failure drain (reconcileQueue + appendFailureLog), commit 1"
    - path: "tests/test-graph-derive-sweep.cjs"
      provides: "4 new keep-on-failure regression checks, commit 1"
    - path: "lib/mcp/tools/room.cjs"
      provides: "rank-then-cap searchRoom (collectMatches + rankMatches), commit 2"
    - path: "tests/test-room-search-rank-before-cap.cjs"
      provides: "rank-before-cap regression fixture, commit 2"
    - path: "tests/run-all-198.sh"
      provides: "regression wired as SPEC-2 leg, commit 2"
  key_links:
    - from: "git add"
      to: "the two explicit file sets only"
      via: "per-file paths, never git add -A / -. / :/"
      pattern: "git add scripts/... tests/..."
---

<objective>
Commit two already-implemented, already-tested debug-session fixes as two atomic, single-fix commits. No new implementation, no scope creep, no push.

1. Defect 4a - graph-derive drain silent-clear: the SessionStart drain used to wipe the whole derive queue (including failed rooms) on any throw, with no log, destroying the retry signal. Now it keeps failed entries and logs them.
2. room_search rank-before-cap: `searchRoom` applied its 50-result cap in raw filesystem walk order before any relevance ordering, silently truncating relevant late-folder hits. Now it ranks every match, then caps.

Purpose: Land two verified fixes cleanly on `main` (local commits only), each reading like a real fix commit with root cause + what changed, WITHOUT touching the deferred siblings (4b-4e, whitespace_scan, the stale-closure surfaces) or dragging in the 27 untracked `.planning/debug/*.md` files.

Output: Two commits on the current branch (`main`). Nothing pushed.
</objective>

<context>
Both fixes are ALREADY WRITTEN and were tested green in the debug session. This is a commit-only task: re-verify, then commit. The full root-cause records live at:

@.planning/debug/graph-derive-silent-clear-dead-api-derivation.md
@.planning/debug/graph-query-results-unranked.md

Current unstaged state (confirmed at plan time):
- Modified: lib/mcp/tools/room.cjs, scripts/gsd-graph-derive-drain.cjs, tests/run-all-198.sh, tests/test-graph-derive-sweep.cjs
- Untracked (new test): tests/test-room-search-rank-before-cap.cjs

CRITICAL REPO FACT: `.planning/` is NOT gitignored in this repo. There are 27 untracked `.planning/debug/*.md` files plus `.planning/research/*.md` sitting in the working tree. A `git add -A`, `git add .`, or `git add :/` would sweep ALL of them into your commit. Use ONLY explicit per-file `git add` with the exact paths in each task. `graphify-out/` is a build artifact and is neither tracked nor dirty - leave it alone.

Commit-message style (repo convention): conventional-commit `fix:` prefix, a root-cause-then-fix body, ending with the `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` trailer. No em-dashes anywhere (hard repo rule - use hyphens).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify and commit the graph-derive drain silent-clear fix (Defect 4a)</name>
  <files>scripts/gsd-graph-derive-drain.cjs, tests/test-graph-derive-sweep.cjs</files>
  <action>
  Step 1 - re-verify (NOT a first run). Run `node tests/test-graph-derive-sweep.cjs` from the repo root (/home/jsagi/dev/MindrianOS-Plugin). Expect the suite to pass green (the debug session recorded 8/8, including the 4 new checks: keep-on-throw, failure-log-written, succeeding-retry-clears, retry-cap-drops-permanent). If it does NOT pass (non-zero exit or any reported failure), STOP immediately and report the failure to the user. Do NOT attempt any code fix - this is a re-verification of already-green work, so a red result means the working tree regressed and needs human eyes, not a patch.

  Step 2 - stage EXACTLY these two files, by explicit path, nothing else:
  `git add scripts/gsd-graph-derive-drain.cjs tests/test-graph-derive-sweep.cjs`
  Never use `git add -A`, `git add .`, `git add -u`, or `git add :/` - those would sweep the 27 untracked .planning/debug/*.md files into the commit.

  Step 3 - confirm the staged set before committing: run `git diff --cached --name-only` and verify it lists EXACTLY `scripts/gsd-graph-derive-drain.cjs` and `tests/test-graph-derive-sweep.cjs` and nothing else. If anything extra is staged, `git restore --staged <that-file>` before proceeding.

  Step 4 - commit using the message below, passed as separate `-m` segments so each becomes its own paragraph (avoids heredoc). Keep the wording verbatim; it must read like a real fix commit describing the actual defect and fix, per D-DEFECT-4a:
  - `-m` subject: `fix: keep graph-derive queue entries on failure instead of silent-clearing`
  - `-m` para 1 (root cause): `Root cause: the SessionStart drain (scripts/gsd-graph-derive-drain.cjs) caught a thrown runDerivation in both the default score-based loop and the legacy deriveRunner seam, pushed the failed room to drained, then clearQueue wiped every snapshot entry including the failed one, with no log and exit 0. A room's derive todo signal was destroyed on every SessionStart while zero semantic edges were produced. Defect 4a in the graph-derive-silent-clear RCA.`
  - `-m` para 2 (fix): `Fix: reconcileQueue(resolved, succeeded, failed) replaces the blanket clearQueue in both loops. Succeeded entries clear (preserves the existing round-trip contract); a thrown entry is kept for the next drain with a bumped failures counter; after MAX_DERIVE_ATTEMPTS (5) it drops with a permanent:true record. New appendFailureLog writes each failure to <room>/.mindrian/graph-derive-failures.json (bounded 200 rows, atomic tmp+rename, scalar-only, LOCAL disk). result.failed[] is now populated; exit stays 0. The disclosed encoder-skip path is untouched; the header failure-discipline comment is rewritten to document keep-on-failure; exports add MAX_DERIVE_ATTEMPTS and failureLogPath.`
  - `-m` para 3 (tests): `Tests: test-graph-derive-sweep.cjs adds keep-on-throw, failure-log-written, succeeding-retry-clears, and retry-cap-drops-permanent checks; 8/8 green.`
  - `-m` trailer: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`

  Do NOT push.
  </action>
  <verify>
    <automated>node tests/test-graph-derive-sweep.cjs; test $? -eq 0 && git show --stat --format='%s' HEAD | grep -q 'keep graph-derive queue entries on failure' && [ "$(git show --name-only --format='' HEAD | sort | tr '\n' ' ')" = "scripts/gsd-graph-derive-drain.cjs tests/test-graph-derive-sweep.cjs " ]</automated>
  </verify>
  <done>tests/test-graph-derive-sweep.cjs passes green; HEAD is a new commit whose subject is the graph-derive fix and whose changed files are EXACTLY scripts/gsd-graph-derive-drain.cjs and tests/test-graph-derive-sweep.cjs; nothing pushed; no .planning/ file committed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify and commit the room_search rank-before-cap fix</name>
  <files>lib/mcp/tools/room.cjs, tests/test-room-search-rank-before-cap.cjs, tests/run-all-198.sh</files>
  <action>
  Step 1 - re-verify (NOT a first run). Run `bash tests/run-all-198.sh` from the repo root. Expect the full suite green (the debug session recorded 12 pass / 0 fail / 0 skip, including the new rank-before-cap regression wired as a SPEC-2 leg). If it does NOT pass (non-zero exit or any reported failure/red leg), STOP immediately and report to the user. Do NOT attempt any code fix - red here means the already-green working tree regressed and needs human review, not a patch.

  Step 2 - stage EXACTLY these three files, by explicit path, nothing else:
  `git add lib/mcp/tools/room.cjs tests/test-room-search-rank-before-cap.cjs tests/run-all-198.sh`
  Same rule as Task 1: never `git add -A` / `.` / `-u` / `:/` - those would pull in the untracked .planning/debug/*.md files. `tests/test-room-search-rank-before-cap.cjs` is a NEW untracked file, so it must be named explicitly.

  Step 3 - confirm the staged set: run `git diff --cached --name-only` and verify it lists EXACTLY `lib/mcp/tools/room.cjs`, `tests/run-all-198.sh`, and `tests/test-room-search-rank-before-cap.cjs` and nothing else. `git restore --staged <file>` anything extra before committing.

  Step 4 - commit using the message below as separate `-m` segments (verbatim; real-fix wording per the room_search RCA):
  - `-m` subject: `fix: rank room_search results before the 50-result cap`
  - `-m` para 1 (root cause): `Root cause: searchRoom (lib/mcp/tools/room.cjs) applied its SEARCH_MAX_RESULTS=50 cap in raw filesystem directory-walk order, before any relevance ordering, and returned the instant the cap was hit. A genuinely relevant hit in a late-traversed folder was silently truncated while 50 incidental substring hits from early folders filled the payload, and one entity-heavy file could monopolize all 50 slots with near-duplicate same-entity lines. It is a graph-independent same-entity distractor-injection point (Shi et al. / Mirzadeh context-rot) that mis-ranks in every room today.`
  - `-m` para 2 (fix): `Fix: rewrote searchRoom to rank-then-cap. collectMatches walks every file up to the unchanged SEARCH_MAX_FILES=500 cost bound (deterministic name-sorted traversal, no arrival-order truncation), recording per-file match tally plus mtimeMs recency, deduping verbatim-identical lines, keeping at most SEARCH_MAX_MATCHES_PER_FILE=200 lines per file. rankMatches scores each file (log-damped term frequency x 0.7 + normalized recency x 0.3, tiebreak matchCount then path) and fills the 50-result budget in ranked order with a per-file slice cap SEARCH_MAX_PER_FILE=5. Each result now carries a match_count relevance signal.`
  - `-m` para 3 (tests): `Tests: new tests/test-room-search-rank-before-cap.cjs regression builds 60 incidental single-hit files in an early-walked folder plus one genuinely relevant many-hit file in a late-walked folder, and asserts the late relevant match survives, ranks first, and that no single file monopolizes the payload. Wired into tests/run-all-198.sh as a SPEC-2 leg; full 198 suite green (12/0/0).`
  - `-m` trailer: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`

  Do NOT push.
  </action>
  <verify>
    <automated>bash tests/run-all-198.sh; test $? -eq 0 && git show --stat --format='%s' HEAD | grep -q 'rank room_search results before the 50-result cap' && [ "$(git show --name-only --format='' HEAD | sort | tr '\n' ' ')" = "lib/mcp/tools/room.cjs tests/run-all-198.sh tests/test-room-search-rank-before-cap.cjs " ]</automated>
  </verify>
  <done>tests/run-all-198.sh passes green; HEAD is a new commit whose subject is the room_search fix and whose changed files are EXACTLY lib/mcp/tools/room.cjs, tests/run-all-198.sh, and tests/test-room-search-rank-before-cap.cjs; nothing pushed; no .planning/ file committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. Both committed changes are LOCAL-only:
- The drain fix writes only to `<room>/.mindrian/graph-derive-failures.json` on local disk (atomic tmp+rename, scalar-only, bounded 200 rows) - zero Brain egress.
- The room_search fix reads only local `.md` files under the room directory - zero Brain egress.

Both were already gate-cleared for Canon Part 8 (local-only floor) and Tri-Polar (shared core, no surface-specific code) in their RCAs. No package installs, no new dependencies, no network surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-qx1-01 | Tampering | `git add` scope (untracked .planning/debug/*.md in tree) | mitigate | Explicit per-file `git add` only; Step 3 stage-set assertion in both tasks; no `-A`/`.`/`:/` |
| T-qx1-02 | Information disclosure | graph-derive-failures.json log content | accept | Scalar-only, LOCAL disk, bounded 200 rows; no PII, no Brain wire (already Part-8 cleared in RCA) |
| T-qx1-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
- Both tasks re-verify BEFORE committing; a red suite is a STOP-and-report, never a patch.
- After both commits: `git log --oneline -2` shows the two fix commits at HEAD; `git status --porcelain` shows the two source file sets gone from the unstaged list while the 27 `.planning/debug/*.md` files, `.planning/research/*.md`, and `graphify-out/` remain untracked/unchanged.
- `git log HEAD..origin/main` and `git log origin/main..HEAD` confirm HEAD is 2 ahead of origin and nothing was pushed.
</verification>

<success_criteria>
- Exactly two new commits on the current branch, one per fix, in the order graph-derive (Task 1) then room_search (Task 2).
- Commit 1 changes EXACTLY: scripts/gsd-graph-derive-drain.cjs, tests/test-graph-derive-sweep.cjs.
- Commit 2 changes EXACTLY: lib/mcp/tools/room.cjs, tests/test-room-search-rank-before-cap.cjs, tests/run-all-198.sh.
- Both relevant test suites re-verified green before their respective commit.
- No `.planning/` file, no `graphify-out/`, and no file outside the two sets is staged or committed.
- Nothing pushed. No em-dashes in either commit message.
</success_criteria>

<output>
Create `.planning/quick/260723-qx1-commit-two-verified-debug-session-fixes-/260723-qx1-SUMMARY.md` when done, recording the two commit SHAs, the re-verify results (test suite pass counts), and confirmation that nothing outside the two file sets was committed and nothing was pushed.
</output>
