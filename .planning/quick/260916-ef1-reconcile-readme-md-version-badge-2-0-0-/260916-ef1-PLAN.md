---
phase: quick-260916-ef1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QUICK-260916-ef1
must_haves:
  truths:
    - "The README version badge reads the shipped release version 2.0.0-beta.41, matching the v2.0.0-beta.41 git tag, the CHANGELOG top released entry, and the marketplace pin"
    - "Every Brain node/framework figure in README.md reads 27,951 nodes and 452 frameworks"
    - "No occurrence of the old figures (28,325 nodes, 181 frameworks) survives anywhere in README.md"
    - "The README says 'the Brain' and never names Theo"
    - "README.md contains zero em-dash characters"
    - "The diff touches only numerals inside existing sentences: no section added, removed, reordered, or reworded"
  artifacts:
    - path: "README.md"
      provides: "Corrected version badge and Brain corpus figures"
      contains: "2.0.0--beta.41"
  key_links:
    - from: "README.md badge"
      to: "CHANGELOG.md released version / git tag v2.0.0-beta.41"
      via: "shields.io badge message string"
      pattern: "version-2\\.0\\.0--beta\\.41-1E3A6E"
    - from: "README.md Brain figures"
      to: "live graph catalog (27,951 nodes / 452 frameworks)"
      via: "inline prose numerals"
      pattern: "27,951 nodes"
---

<objective>
Reconcile three stale facts in README.md against what actually shipped: the version badge (still pinned at the very first beta) and the Brain corpus figures (node and framework counts that predate the current graph backend by a wide margin).

Purpose: the README is the public front door. A badge reading beta.1 next to a beta.41 release, and a framework count off by 271, both read as abandonment. This is a fact correction, not a rewrite.
Output: a corrected README.md whose diff contains numerals only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@README.md

Facts established during planning, already verified. Do NOT re-derive these:

| Fact | Value | Source checked |
|------|-------|----------------|
| Latest SHIPPED version | `2.0.0-beta.41` | `git tag` (v2.0.0-beta.41 exists), CHANGELOG.md top released entry dated 2026-09-16, `~/mindrian-marketplace/.claude-plugin/marketplace.json` pinned to 2.0.0-beta.41 |
| Working-tree version | `2.0.0-beta.42` | `node lib/core/repo-version.cjs`, plugin.json, package.json |
| Live graph nodes | `27,951` | live-verified 2026-09-16 via the Brain's own catalog probe and a direct health read |
| Live graph frameworks | `452` | same source |

The badge tracks the SHIPPED release (beta.41), not the in-progress working tree (beta.42). That
one-behind state is correct by design: the badge links to CHANGELOG.md, and beta.42 has no released
CHANGELOG entry yet. Do not "helpfully" set the badge to beta.42.

Three hard constraints from this repo's own doctrine, all binding on this edit:

1. **Never name Theo in README copy.** The public-facing term is "the Brain". The Theo name is
   disclosed only when a user asks for it by name in conversation, never in marketing copy. The
   word "Theo" must not appear in the diff.
2. **No em-dashes anywhere** (CLAUDE.md Conventions). Use hyphens. README.md currently has zero;
   keep it at zero.
3. **Fact correction only.** No new sections, no new sentences, no reworded clauses beyond the
   numerals themselves. Voice and tone stay exactly as written.

Note for the executor: the task description this plan came from said the framework count appears
twice. It appears THREE times. Line 79 carries a third occurrence ("knows 181 frameworks") that
the original description missed. All three are in scope; leaving line 79 stale would make the
README contradict itself.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the version badge and all three Brain corpus figures</name>
  <files>README.md</files>
  <action>
Make exactly four numeral corrections in README.md. Use Edit, one edit per site. Change nothing else on any of these lines.

Site 1, line 13 (version badge). The shields.io badge path is `badge/{label}-{message}-{color}`, and shields.io escapes a literal hyphen inside the message as a double hyphen. So the message `2.0.0-beta.41` is written `2.0.0--beta.41`. Replace the badge URL segment `version-2.0.0--beta.1-1E3A6E` with `version-2.0.0--beta.41-1E3A6E`. Preserve the surrounding markdown link to CHANGELOG.md, the `[![Version]` alt text, and the `1E3A6E` color token exactly.

Site 2, line 35 ("The loop, in 30 seconds", step 2). Replace `28,325 nodes and 181 frameworks` with `27,951 nodes and 452 frameworks`. The rest of the sentence, including "built from 20 years of teaching, holding WHEN to use WHICH method and in WHAT sequence", is untouched.

Site 3, line 79 ("What talking to Larry feels like", opening sentence). Replace `knows 181 frameworks` with `knows 452 frameworks`. This is the occurrence the original request did not name; it is in scope.

Site 4, line 98 ("The three layers" table, Brain row). Replace `28,325 nodes, 181 frameworks` with `27,951 nodes, 452 frameworks`. Preserve the table pipes, the bold `**Brain**` cell, and the "served over MCP" / "Served remotely, never distributed" cells verbatim.

Do NOT add a relationship count, a "of which N have problem-type coverage" parenthetical, or any other new figure. The live graph also holds 39,737 relationships and 86 frameworks with problem-type coverage, but neither number is in the README today and adding one is a rewrite, not a correction. The readiness nuance is already carried by the existing "honest refusal" section (readiness 4/4 vs 2/4); it does not need a second home.

Do not touch the `20 years of teaching` figure anywhere. It is not stale.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin &amp;&amp; test "$(grep -c 'version-2\.0\.0--beta\.41-1E3A6E' README.md)" = "1" &amp;&amp; test "$(grep -c '27,951 nodes' README.md)" = "2" &amp;&amp; test "$(grep -c '452 framework' README.md)" = "3" &amp;&amp; echo PASS</automated>
  </verify>
  <done>README.md line 13 badge reads `version-2.0.0--beta.41-1E3A6E`; the string `27,951 nodes` appears exactly twice (lines 35 and 98); the string `452 framework` appears exactly three times (lines 35, 79, 98).</done>
</task>

<task type="auto">
  <name>Task 2: Full-file staleness sweep and style gate</name>
  <files>README.md</files>
  <action>
Sweep the whole file for anything else stale or off-style that Task 1 did not cover, then prove the diff is numerals-only.

Step 1, residual stale figures. Grep README.md for `28,325`, `181`, and `version-2\.0\.0--beta\.1-` (note the trailing hyphen, which distinguishes the old `beta.1` badge from the new `beta.41` one). Each must return zero hits. If any hit survives, fix it the same way Task 1 did and re-run.

Step 2, other stale version strings. Grep the whole file for any remaining semver or beta string: pattern `beta\.[0-9]+|[0-9]+\.[0-9]+\.[0-9]+`. Planning found exactly one such site (the line 13 badge). If this sweep surfaces a second one that Task 1 did not touch, evaluate it: a version reference that names a shipped release gets corrected to 2.0.0-beta.41; a version reference that is part of something else (a Node floor, a dependency range, a URL) is left alone. Report in the SUMMARY whichever case applied.

Step 3, forbidden terms. Confirm `grep -ci theo README.md` returns 0. The Theo name must not have entered the copy.

Step 4, style gate. Confirm `grep -c '—' README.md` returns 0 (em-dash character, U+2014). The file had zero before this change; it must have zero after.

Step 5, diff discipline. Run `git diff README.md` and read it. Confirm every changed line differs from its original only in the numerals named in Task 1. If the diff shows a reflowed paragraph, a moved table row, a trailing-whitespace change, or a reworded clause, revert that hunk and redo it as a numeral-only edit. Confirm the diff touches exactly 4 lines (13, 35, 79, 98) and no others.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin &amp;&amp; test "$(grep -c '28,325' README.md)" = "0" &amp;&amp; test "$(grep -c '181' README.md)" = "0" &amp;&amp; test "$(grep -c 'version-2\.0\.0--beta\.1-' README.md)" = "0" &amp;&amp; test "$(grep -ci theo README.md)" = "0" &amp;&amp; test "$(grep -c '—' README.md)" = "0" &amp;&amp; test "$(git diff --numstat README.md | awk '{print $1}')" = "4" &amp;&amp; echo PASS</automated>
  </verify>
  <done>Zero hits for `28,325`, `181`, the old beta.1 badge string, `theo` (case-insensitive), and the em-dash character. `git diff --numstat README.md` reports exactly 4 added and 4 removed lines. The diff reads as numerals-only against the original.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub README | The only boundary this plan crosses. Text published to an unauthenticated public audience. No input is parsed, no code path changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ef1-01 | Information disclosure | README.md public copy | mitigate | The Brain backend's real name (Theo) is proprietary and must not appear in marketing copy. Task 2 Step 3 gates on `grep -ci theo README.md` returning 0. |
| T-ef1-02 | Tampering | README.md prose outside the four numeral sites | mitigate | Task 2 Step 5 pins `git diff --numstat` to exactly 4 changed lines, so an incidental reflow or reword cannot ride along with the fact fix. |
| T-ef1-03 | Repudiation | Version badge vs shipped artifact | accept | The badge is a human-readable claim only. It is not an entitlement check and gates nothing; the real version lockstep is enforced by `scripts/release.sh` across its five places. |
| T-ef1-SC | Tampering | npm/pip/cargo installs | accept | Not applicable. This plan installs no packages and adds no dependency; no supply-chain surface is touched. |
</threat_model>

<verification>
Run from `/home/jsagi/dev/MindrianOS-Plugin`:

```bash
grep -n 'version-2\.0\.0--beta\.41-1E3A6E' README.md   # exactly 1 hit, line 13
grep -nE '27,951|452 framework' README.md              # lines 35, 79, 98
grep -nE '28,325|181|—' README.md                      # zero hits
grep -ni theo README.md                                # zero hits
git diff --stat README.md                              # 4 insertions, 4 deletions
```

Read `git diff README.md` in full. Every hunk must be a numeral swap inside an otherwise identical sentence.
</verification>

<success_criteria>
- Badge on line 13 reads `2.0.0--beta.41`, matching the v2.0.0-beta.41 tag, the CHANGELOG released entry, and the marketplace pin.
- All three Brain figure sites (lines 35, 79, 98) read 27,951 nodes / 452 frameworks.
- Zero residual hits for `28,325`, `181`, or the old `beta.1` badge string.
- Zero occurrences of `theo` (case-insensitive) and zero em-dash characters.
- `git diff --numstat README.md` reports exactly 4 lines changed. No section added, removed, reordered, or reworded.
</success_criteria>

<output>
Create `.planning/quick/260916-ef1-reconcile-readme-md-version-badge-2-0-0-/260916-ef1-SUMMARY.md` when done.

In the SUMMARY, record: (a) that the line 79 framework-count occurrence was a third site the original request did not name, (b) the result of the Task 2 Step 2 sweep for other version strings, and (c) that the badge intentionally tracks the shipped release (beta.41) rather than the in-progress working tree (beta.42).
</output>
