---
phase: quick-260705-sd5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/ingest-methodology.md
  - commands/stance.md
  - data/command-registry.json
  - data/help-groups.json
  - scripts/check-help-coverage.cjs
  - tests/test-help-coverage-gate.cjs
  - commands/help.md
  - tests/test-help-selector-lanes.cjs
  - lib/memory/help-renderer.test.cjs
autonomous: true
requirements: [QUICK-260705-SD5]
tags: [help, jtbd, help-groups, coverage-gate, de-stijl-selector]
must_haves:
  truths:
    - "commands/ingest-methodology.md and commands/stance.md each carry a serves_jtbd frontmatter list drawn from the existing 15-tag controlled vocabulary (no invented tags), and data/command-registry.json reflects them"
    - "Every family in data/help-groups.json declares a jtbd: [] outcome list, and every non-deprecated command's serves_jtbd intersects its family's declared jtbd (traceable membership)"
    - "node scripts/check-help-coverage.cjs exits 0 AND now machine-enforces the family-JTBD coherence rule, so a future command dropped into a family with zero JTBD overlap fails the gate"
    - "Family count stays 11, ids unchanged, 3-card 4+4+3 AskUserQuestion layout unchanged (max 4 cards, max 4 options per card - the 260705-jeq contract)"
    - "Command union across groups stays byte-identical at 100 non-admin on-card commands; the 5 deprecated_aliases and 2 admin commands stay excluded exactly as today"
    - "All help-selector tests pass against the refined groups; node scripts/doctor.cjs --acceptance stays green"
  artifacts:
    - path: "data/help-groups.json"
      provides: "11 families with per-family jtbd declarations + 9-command JTBD-driven reassignment + Frame & Validate label"
      contains: "\"jtbd\""
    - path: "scripts/check-help-coverage.cjs"
      provides: "New coherence check: group.jtbd non-empty, vocab-valid, and every member command intersects it"
    - path: "commands/ingest-methodology.md"
      provides: "serves_jtbd frontmatter filled"
      contains: "serves_jtbd"
    - path: "commands/stance.md"
      provides: "serves_jtbd frontmatter filled"
      contains: "serves_jtbd"
    - path: "tests/test-help-coverage-gate.cjs"
      provides: "Assertions locking the new coherence check (pass + synthetic-violation fail)"
  key_links:
    - from: "scripts/check-help-coverage.cjs"
      to: "data/help-groups.json group.jtbd"
      via: "intersection check against each command's serves_jtbd frontmatter"
      pattern: "jtbd"
    - from: "commands/help.md"
      to: "data/help-groups.json"
      via: "renderer delegation, labels never hardcoded except card-composition lines"
      pattern: "help-groups|help-renderer"
---

<objective>
JTBD-driven refinement of the /mos:help 11-family map shipped earlier today by quick task 260705-jeq. Ground truth is each command's serves_jtbd frontmatter tags (15-tag controlled vocabulary). Today the families are navigational groupings whose membership is not traceable to JTBD outcomes; two commands (ingest-methodology, stance) have no serves_jtbd at all.

This is a REFINEMENT of jeq's design, not a competing redesign: fill the 2 tag gaps, reassign 9 commands across 3 move-groups so each family's membership shares a declared outcome, add a per-family jtbd declaration to data/help-groups.json, and extend scripts/check-help-coverage.cjs to machine-enforce the coherence rule forever after.

Purpose: make every family's membership traceable to a coherent JTBD outcome AND make that traceability a standing CI gate, so the map cannot drift back into vibes-based grouping.
Output: 2 frontmatter fixes, refined help-groups.json with jtbd declarations, extended coverage gate + gate test, aligned help.md body + selector tests, one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@data/help-groups.json
@commands/help.md
@scripts/check-help-coverage.cjs
@scripts/help-renderer.cjs
@.planning/quick/260705-jeq-doctor-diagnostic-for-claude-code-comman/260705-jeq-SUMMARY.md

Precedent: 260705-jeq (same day) established the 3-card/11-family AskUserQuestion structure, the data-driven single-source-of-truth rule (D-04: no family contents hardcoded in prose), the escape-hatch line for families over 4 commands, and the exclusion machinery (visibility:admin + deprecated_aliases registry). All of that is LOCKED and survives this task unchanged.

Hard AskUserQuestion contract (discovered in jeq): max 4 cards, max 4 options (families) per card. Card layout today: Card 1 = start-here, rooms-data-room, frame-the-problem, run-a-methodology; Card 2 = explore-futures-trends, intelligence-research, opportunities-funding-meetings, present-publish; Card 3 = orchestrate-automate, memory-state-engine, system-maintenance. This plan does NOT change card composition - same 11 ids in the same card slots.

jeq CONTEXT.md carried a user-approved verbatim placement list (e.g. setup/update in Start Here). Where JTBD purity conflicts with a same-day user placement decision, the user decision wins; the family's jtbd declaration absorbs the outlier instead (see update in start-here below).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the 2 missing serves_jtbd tags and regenerate the command registry</name>
  <files>commands/ingest-methodology.md, commands/stance.md, data/command-registry.json</files>
  <action>
Add a serves_jtbd frontmatter list to the only two non-admin commands missing one. Controlled vocabulary (the 15 existing tags, do NOT invent a new one): explore, audit-room, prepare-pitch, understand-market, find-problem, validate-idea, find-bottleneck, compare-options, plan-execution, connect-domains, decide-pursue, file-meeting, surface-contradiction, temporal-correction, build, navigate.

1. commands/ingest-methodology.md - read its description/teaching/body (maintainer pipeline that teaches the Brain a new methodology so every future user can reach it). Assign serves_jtbd: ["build"] - it extends what the system can build/do, the same outcome family as ignite. Verify this against the body before writing; if after reading you judge that genuinely nothing in the vocabulary fits, do NOT force a tag - stop and record the mismatch explicitly in the SUMMARY deviations section instead.
2. commands/stance.md - read its description/teaching/body (session-level override of Larry's conversational stance, a navigation-of-the-interaction affordance). Assign serves_jtbd: ["navigate"] - the same outcome as memory-cortex-reach (steering how the session moves), which also matches its memory-state-engine family placement. Same escape clause: if nothing fits after reading, say so explicitly rather than force it.
3. Place serves_jtbd adjacent to the other help-facing keys (near help_jtbd/teaching) in each file's frontmatter. Touch NOTHING else in either file - no body edits, no other frontmatter keys.
4. Regenerate the derived registry: node scripts/build-command-registry.cjs (jeq deviation #1 precedent: data/command-registry.json stores per-command frontmatter fields and the pre-commit drift gate blocks a commit if it staled).
  </action>
  <verify>
    <automated>node -e "const m=require('fs').readFileSync('commands/ingest-methodology.md','utf8');const s=require('fs').readFileSync('commands/stance.md','utf8');if(!/serves_jtbd/.test(m)||!/serves_jtbd/.test(s))process.exit(1)" &amp;&amp; node scripts/build-command-registry.cjs &amp;&amp; git diff --stat data/command-registry.json</automated>
  </verify>
  <done>Both files carry serves_jtbd from the 15-tag vocabulary (or an explicit documented refusal), registry regenerated, zero other content changed in either command file.</done>
</task>

<task type="auto">
  <name>Task 2: Refine data/help-groups.json - 3 move-groups, 1 label rename, per-family jtbd declarations</name>
  <files>data/help-groups.json</files>
  <action>
First extract live ground truth (do not trust this plan's snapshot blindly): write a scratch script (in the session scratchpad, not the repo) that parses every commands/*.md frontmatter and emits command -> serves_jtbd. Cross-check the moves below against it; if a listed tag differs on disk, the DISK value wins and you adjust the affected family's jtbd declaration (not the move itself, unless the move's rationale collapses - then document the deviation).

Apply exactly these membership changes (9 commands, ids and card slots untouched):

- M1: move grade, deep-grade FROM frame-the-problem TO rooms-data-room. Rationale: both tag audit-room - they grade the room's work, the same outcome as snapshot/status/wiki. rooms-data-room goes 8 to 10.
- M2: move validate, score-innovation, analyze-needs, user-needs FROM run-a-methodology TO frame-the-problem. Rationale: all four tag find-problem or validate-idea - the frame/validate outcome cluster where challenge-assumptions, map-unknowns, root-cause already live. frame-the-problem goes 8-2+4 = 10.
- M3: move systems-thinking, analyze-systems, find-analogies FROM run-a-methodology TO intelligence-research. Rationale: find-bottleneck and connect-domains - the exact outcome cluster of find-bottlenecks, causal, rs-*, find-connections, whitespace. intelligence-research goes 13 to 16 (escape-hatch line already handles over-4 families); run-a-methodology lands at 8.
- Rename: frame-the-problem label "Frame the Problem" -> "Frame & Validate" (id stays frame-the-problem - zero churn in card lists, resolution order, or renderer ids).
- Deliberately NOT moved: update stays in start-here and radar stays in present-publish despite off-family primary tags - update's Start Here placement is a same-day user decision from jeq CONTEXT, and radar also tags prepare-pitch so it is already coherent. Absorb update via the family jtbd list (below).

Add a "jtbd": [] key to EVERY group (this is the traceability artifact). Expected declarations - recompute against the live extraction and adjust only if disk disagrees:

- start-here: [explore, build, audit-room]  (audit-room exists solely for update - user-locked placement, honest declaration)
- rooms-data-room: [audit-room, prepare-pitch]
- frame-the-problem: [find-problem, validate-idea, surface-contradiction, decide-pursue]
- run-a-methodology: [prepare-pitch, validate-idea, understand-market, explore]
- explore-futures-trends: [understand-market, explore, plan-execution, find-problem]
- intelligence-research: [find-bottleneck, connect-domains, explore, decide-pursue, compare-options, find-problem]
- opportunities-funding-meetings: [explore, file-meeting, decide-pursue]
- present-publish: [prepare-pitch]
- orchestrate-automate: [plan-execution]
- memory-state-engine: [audit-room, navigate, explore, compare-options, temporal-correction]
- system-maintenance: [audit-room, explore, build]

Invariant checks before finishing: (a) union of all groups' commands is byte-identical to before - 100 non-admin commands, zero add/drop/dup; (b) every member's serves_jtbd intersects its family's jtbd (using Task 1's new tags for stance and ingest-methodology); (c) deprecated_aliases and _lanes blocks byte-untouched; (d) update the _note prose where it names "Frame the Problem" and bump/append the phase field to quick-260705-sd5. Keep lane: assignments as they are.
  </action>
  <verify>
    <automated>node -e "const g=require('./data/help-groups.json');const all=g.groups.flatMap(x=>x.commands);if(all.length!==100||new Set(all).size!==100)throw new Error('union broke: '+all.length);if(g.groups.length!==11)throw new Error('family count '+g.groups.length);for(const x of g.groups){if(!Array.isArray(x.jtbd)||x.jtbd.length===0)throw new Error(x.id+' missing jtbd')}console.log('OK')"</automated>
  </verify>
  <done>11 families, same ids, same card-slot order, 100-command union preserved, every group declares a non-empty jtbd list, every member intersects it, Frame & Validate label applied, deprecated_aliases/_lanes untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Extend check-help-coverage.cjs with the family-JTBD coherence gate + lock it in the gate test</name>
  <files>scripts/check-help-coverage.cjs, tests/test-help-coverage-gate.cjs</files>
  <action>
Read scripts/check-help-coverage.cjs fully first (189 lines) to reuse its existing frontmatter parsing and failure-reporting conventions - extend, do not restructure.

Add one new check class (name it in the script's existing failure-code style, e.g. jtbd_incoherent / jtbd_missing_declaration / jtbd_unknown_tag):

1. Every group in data/help-groups.json MUST declare a non-empty jtbd array (hard fail: jtbd_missing_declaration).
2. Build the controlled vocabulary at run time as the union of every non-admin command's serves_jtbd values read from commands/*.md frontmatter - never a frozen literal list (CLAUDE.md Part 11 spirit: enumerate from disk). Any group jtbd value outside that union hard-fails (jtbd_unknown_tag).
3. For every non-deprecated, non-admin command in a group: its serves_jtbd must intersect the group's jtbd (hard fail: jtbd_incoherent, message names the command, its tags, the family, and the family's declared list). A command with NO serves_jtbd at all in its frontmatter is also a hard fail here (this makes the Task 1 gap class unrepresentable in the future).
4. Deprecated-alias keys and visibility:admin commands stay exempt exactly as they are for the existing membership checks.

Then extend tests/test-help-coverage-gate.cjs (read it first, follow its existing assertion pattern): (a) live-tree assertion - the real repo passes the new check; (b) synthetic-violation assertion - a fixture/temp groups blob where one command's tags share nothing with its family's jtbd makes the gate exit non-zero with the jtbd_incoherent code; (c) missing group jtbd declaration fails. Reuse however the existing test builds synthetic inputs - if it only shells out against the live tree, add the synthetic cases via a temp-dir copy the same way other repo gate-tests do it (grep tests/ for an existing temp-dir pattern before inventing one).

No em-dashes in any new prose or messages.
  </action>
  <verify>
    <automated>node scripts/check-help-coverage.cjs &amp;&amp; node tests/test-help-coverage-gate.cjs</automated>
  </verify>
  <done>Coverage gate passes on the refined tree, machine-enforces group jtbd declarations + per-command intersection + vocab validity, and the gate test locks both the pass and the synthetic-fail paths.</done>
</task>

<task type="auto">
  <name>Task 4: Align commands/help.md and the selector tests with the refined map</name>
  <files>commands/help.md, tests/test-help-selector-lanes.cjs, lib/memory/help-renderer.test.cjs</files>
  <action>
commands/help.md: the body deliberately hardcodes almost nothing (jeq D-04), so the delta is small. Sweep for the two things that can stale: (1) the Card 1 composition line names family labels - update "frame-the-problem (Frame the Problem)" to the new "Frame & Validate" label; (2) confirm no other line names a family's contents, count, or the old label (the "More families:" line uses card-level shorthand words like Explore/Memory/System - those are unaffected). Family count literals ("11 families", "3 cards", "4 + 4 + 3") are all still true - leave them. Do not touch frontmatter (its teaching field feeds the registry; an unnecessary edit forces another regen).

Renderer: scripts/help-renderer.cjs is data-driven (labels/counts from loadGroups()); expect ZERO code change. Only if a comment or the --group unknown-id help text hardcodes a family LABEL (grep for "Frame the Problem") fix that literal; the "11" count literals in comments stay true.

Tests - read each before editing, change only what pins stale values:
- tests/test-help-selector-lanes.cjs: Assertion 5 pins the help.md body contract (3 cards, escape-hatch line, --group delegation). If it pins the old "Frame the Problem" label or Card 1 composition literals, update those pins; if it is label-agnostic, no change.
- lib/memory/help-renderer.test.cjs: jeq summary says it renders "11 new labels" - if labels are read via loadGroups() it passes unchanged; if any label is pinned as a literal, update to "Frame & Validate".
- tests/test-help-cards-render.cjs: data-driven per jeq (100 non-admin coverage), expect no change - run it to confirm rather than editing.
Do NOT weaken any assertion; label-pin updates only, same intent (jeq deviation #2 precedent).
  </action>
  <verify>
    <automated>node tests/test-help-selector-lanes.cjs &amp;&amp; node lib/memory/help-renderer.test.cjs &amp;&amp; node tests/test-help-cards-render.cjs &amp;&amp; ! grep -rn "Frame the Problem" commands/help.md scripts/help-renderer.cjs</automated>
  </verify>
  <done>help.md names the new label, renderer untouched (or literal-only fix), all three selector/renderer test suites pass with assertions equal in strength to before.</done>
</task>

<task type="auto">
  <name>Task 5: Full verification sweep + single atomic commit</name>
  <files>(no new files - staging and committing Tasks 1-4)</files>
  <action>
Run the full local verification battery from the repo root and require every one green:

1. node scripts/check-help-coverage.cjs
2. node tests/test-help-coverage-gate.cjs
3. node tests/test-help-selector-lanes.cjs
4. node tests/test-help-cards-render.cjs
5. node lib/memory/help-renderer.test.cjs
6. node scripts/doctor.cjs --acceptance  (must stay 14/14 or better)
7. Em-dash sweep of every touched file: grep for the em-dash character across commands/ingest-methodology.md commands/stance.md data/help-groups.json scripts/check-help-coverage.cjs commands/help.md tests/test-help-coverage-gate.cjs - zero hits in lines this task added (HARD RULE: hyphens only).
8. git diff sanity: commands/*.md diffs are frontmatter-only for the two tagged files and label/prose-only for help.md; deprecated_aliases and _lanes blocks show zero diff in help-groups.json.

Then ONE atomic commit of all repo changes (workspace guard: confirm pwd is /home/jsagi/dev/MindrianOS-Plugin, never the plugin cache). Commit message must frame this as a refinement of jeq, e.g.:

  feat(help): JTBD-driven regroup of the 11 help families (refines 260705-jeq)

  Refinement of today's 260705-jeq 3-card/11-family selector, not a redesign:
  fills the 2 missing serves_jtbd tags (ingest-methodology, stance), reassigns
  9 commands across 3 JTBD-coherent moves, renames frame-the-problem to
  Frame & Validate, declares a jtbd outcome list per family, and extends
  check-help-coverage.cjs to machine-enforce family-JTBD coherence.

The pre-commit hook runs the command-registry drift gate and the registration gate; if it blocks, fix the cause (usually a stale regen) and re-commit - never bypass with --no-verify.
  </action>
  <verify>
    <automated>node scripts/doctor.cjs --acceptance &amp;&amp; git log -1 --stat | head -30 &amp;&amp; git status --porcelain | wc -l</automated>
  </verify>
  <done>All 6 suites green, em-dash sweep clean, exactly one new commit containing every changed file, working tree clean (except gitignored .planning/).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commands/*.md frontmatter -> coverage gate | The gate parses repo-local YAML only; no user/room data crosses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-sd5-01 | Tampering | check-help-coverage.cjs new check | mitigate | Vocabulary enumerated from disk at run time, never a frozen literal a drifted edit could silently satisfy; synthetic-violation test locks the fail path |
| T-sd5-02 | Info disclosure | help map / gate output | accept | Repo-local command metadata only; zero room content, zero Brain wire (Canon Part 8 untouched) |
| T-sd5-SC | Tampering | package installs | accept | No package-manager installs in this plan; pure repo-file edits |
</threat_model>

<verification>
- node scripts/check-help-coverage.cjs exits 0 and now includes the coherence class
- All four help test suites pass (coverage-gate, selector-lanes, cards-render, renderer)
- node scripts/doctor.cjs --acceptance green
- git show --stat HEAD lists every file from files_modified that actually changed, in one commit
</verification>

<success_criteria>
- ingest-methodology and stance carry vocabulary-valid serves_jtbd tags (or an explicit documented refusal), registry regenerated
- 11 families, ids and card slots unchanged, 100-command union byte-identical, every family declares jtbd and every member intersects it
- Coherence is a standing CI gate with a locked fail path, not a one-time cleanup
- Selector contract from 260705-jeq fully preserved (3 cards, 4+4+3, escape hatch, D-04 single source of truth)
- One atomic commit framed as a refinement of jeq
</success_criteria>

<output>
Create `.planning/quick/260705-sd5-jtbd-driven-regroup-of-mos-help-command-/260705-sd5-SUMMARY.md` when done (git add -f, .planning is gitignored - or leave uncommitted per quick-task convention; follow how 260705-jeq handled it).
</output>
