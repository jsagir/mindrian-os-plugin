---
phase: quick-260714-k44
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/eureka/embedding-classifier.cjs
  - lib/core/eureka/entity-extractor.cjs
  - scripts/entity-extract.cjs
  - tests/test-218-what-why-classifier.cjs
  - tests/run-all-218.sh
  - docs/ENV-TUNING.md
autonomous: true
requirements: [K44-REQ-1, K44-REQ-2, K44-REQ-3, K44-REQ-4, K44-REQ-5, K44-REQ-6, K44-REQ-7, K44-REQ-8]

must_haves:
  truths:
    - "A candidate whose embedding margin clears the confidence threshold classifies WHAT or WHY with ZERO API calls; the LLM tier is never invoked for it (source honestly 'embedding')"
    - "Only candidates below the margin threshold escalate to the existing entity-classifier.cjs LLM tier, batched per artifact with ONLY the escalated names, never the whole batch"
    - "With no LLM available (no key, no credits), an escalated candidate resolves to the embedding pass's best-guess label even below threshold - never silently defaults to 'what' - and status.json discloses the low-confidence count honestly"
    - "lib/core/eureka/entity-classifier.cjs is byte-unchanged (git diff clean); it is demoted to the tier-2b escalation path, not removed or edited"
    - "The embedding tier is fully local, zero egress: the sibling-transport test still proves entity-classifier.cjs is the ONLY eureka module carrying the Anthropic transport, and the run-all-218.sh leg (d) zero-network grep on entity-extractor.cjs + entity-extract.cjs stays green"
    - "The expected API call reduction is MEASURED on the real aion-eureka-synergy room (baseline per-artifact call count vs post-change escalation call count) and the real numbers are reported, not asserted"
    - "The reference set is embedded once per process (cached), never recomputed per candidate"
    - "bash tests/run-all-218.sh fully green including the updated two-tier legs, all with zero live model calls"
  artifacts:
    - path: "lib/core/eureka/embedding-classifier.cjs"
      provides: "Tier-2a local embedding WHAT-vs-WHY classifier: curated reference sets, cached reference embedding, nearest-neighbor cosine scoring, confidence margin, degrade-never-throw"
      min_lines: 100
      exports: ["classifyByEmbedding"]
    - path: "lib/core/eureka/entity-extractor.cjs"
      provides: "Additive FRAMEWORK_TERMS export so the embedding classifier reuses the WHY seed vocabulary instead of copying it (Canon Part 7)"
      contains: "FRAMEWORK_TERMS"
    - path: "scripts/entity-extract.cjs"
      provides: "Two-tier routing in runTier2: embedding pass first, per-artifact LLM escalation only for the low-margin residual, honest per-tier counts in status.json"
      contains: "embedding-classifier"
    - path: "tests/test-218-what-why-classifier.cjs"
      provides: "Module-level embedding-classifier legs (deterministic injected vectors, cache proof, unavailable degrade) + the three new routing legs (embedding-alone, escalation, honest no-LLM degrade)"
      contains: "classifyByEmbedding"
    - path: "docs/ENV-TUNING.md"
      provides: "MINDRIAN_WHATWHY_MARGIN tunable documented in the existing env-tuning format"
      contains: "MINDRIAN_WHATWHY_MARGIN"
  key_links:
    - from: "scripts/entity-extract.cjs"
      to: "lib/core/eureka/embedding-classifier.cjs"
      via: "require + tier-2a pass BEFORE db.exec('BEGIN'), same never-hold-the-write-lock rule as the model tier"
      pattern: "embedding-classifier"
    - from: "lib/core/eureka/embedding-classifier.cjs"
      to: "lib/core/eureka/embedding-spine.cjs"
      via: "embedTexts for both the cached reference set and per-run candidates; cosineSimilarity re-export for scoring"
      pattern: "embedding-spine"
    - from: "lib/core/eureka/embedding-classifier.cjs"
      to: "lib/core/eureka/entity-extractor.cjs"
      via: "FRAMEWORK_TERMS reused as the WHY reference seed (exported additively, never copied)"
      pattern: "FRAMEWORK_TERMS"
    - from: "scripts/entity-extract.cjs"
      to: "lib/core/eureka/entity-classifier.cjs"
      via: "unchanged classifyArtifactCandidates call, now fed ONLY the escalated (low-margin) names per artifact"
      pattern: "entity-classifier"
---

<objective>
Replace the tier-2 WHAT-vs-WHY classifier's single LLM-only design (quick task 260714-hzx) with a locked two-tier design: a free, fully local embedding pass (tier-2a) resolves the clear majority of candidates; the existing LLM classifier (tier-2b, entity-classifier.cjs, byte-unchanged) is called ONLY for the low-confidence residual. This is a locked navigator decision from this session; do not relitigate the architecture.

Why: the hzx design calls Claude Haiku once per artifact for EVERY tier-1 survivor batch, and its live verification is currently blocked on a zero API-credit balance. The WHAT-vs-WHY question ("is this term a real-world entity name, or known methodology/framework vocabulary") is a semantic-similarity question for the clear majority of cases, and the room already ships a local encoder (MongoDB/mdbr-leaf-ir via lib/core/eureka/embedding-spine.cjs, the same encoder Eureka's ranking depends on). Resolving those cases locally costs zero API spend and likely unblocks the pending hzx Task 3 live verification without a funded key.

Requirement trace (from the task directive):
- K44-REQ-1: tier-2a embedding classifier with curated WHAT/WHY reference sets, cached reference embedding, per-candidate confidence margin.
- K44-REQ-2: margin threshold below which a candidate escalates to tier-2b (entity-classifier.cjs, unchanged) for that candidate only.
- K44-REQ-3: classifier_source reflects which tier actually resolved each candidate (embedding / model / mixed / fallback + honest low-confidence counts).
- K44-REQ-4: degrade-never-throw: no LLM available -> escalated candidates take the embedding best guess (even below threshold), disclosed, never a silent 'what' default.
- K44-REQ-5: entity-classifier.cjs untouched; measure and report the actual API-call-volume reduction on a real room.
- K44-REQ-6: tests/test-218-what-why-classifier.cjs + run-all-218.sh cover the three routing outcomes (embedding-alone, escalation, honest degrade).
- K44-REQ-7: summary states the new, correct live-acceptance criteria for hzx's pending Task 3 (do NOT resume it as originally scoped).
- K44-REQ-8: GSD workflow: atomic commits, no em-dashes, dev-research compositing UPDATES the existing 2026-07-14-eureka-ranking-bug-and-what-why-classifier entry (both homes), no duplicate entry.

Canon boundary: tier-2a stays fully local, zero network (embedding-spine's Part 8 posture: the only network touch anywhere is the one-time generic model-weight download by model id, no user bytes egress; on this machine the model is already cached from Eureka's own ranking runs). Only tier-2b (unchanged, already documented) makes a network call, now for a small residual subset instead of every candidate.

Output: new lib/core/eureka/embedding-classifier.cjs, additive FRAMEWORK_TERMS export, rewired runTier2 routing in scripts/entity-extract.cjs, updated test suite, measured call-volume reduction on aion-eureka-synergy, ENV-TUNING.md entry, research-entry update in both homes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@lib/core/eureka/entity-classifier.cjs
@lib/core/eureka/entity-extractor.cjs
@lib/core/eureka/embedding-spine.cjs
@scripts/entity-extract.cjs
@tests/test-218-what-why-classifier.cjs
@tests/run-all-218.sh
@docs/ENV-TUNING.md
@.planning/quick/260714-hzx-extend-the-phase-218-tier-1-entity-extra/260714-hzx-PLAN.md

Interface contracts the executor needs (extracted, do not rediscover):

- embedding-spine.cjs exports { getEncoder, embedTexts, encoderProvenance, resolveDim, cosineSimilarity }. embedTexts(texts, opts) -> Promise of { success:true, vectors: number[][] (L2-normalized), provenance:{model,dtype,dim} } | { success:false, error:'encoder_unavailable'|'embed_failed', detail }. Never throws. Test seams: opts.encodeFn (texts -> number[][], bypasses the model entirely, receives the FULL list in one call) and opts._forceUnavailable (simulates a failed dep import). The default model MongoDB/mdbr-leaf-ir is a 384-dim local ONNX encoder; resolveModel() reads MINDRIAN_EMBED_MODEL at call time.

- vector-store.cjs is db-table infrastructure (caller-owned sqlite handles, eureka_vec tables). It is the WRONG tool for a ~40-term in-memory reference set: every function needs a db handle the classifier does not have, and its own fallback leg is exactly a brute-force cosine scan. The natural fit here is embedTexts + the exported cosineSimilarity in memory. Document this choice in the new module header (the directive offered centroid-or-nearest-neighbor, whichever fits the existing infrastructure most naturally; nearest-neighbor max-similarity is the answer, and it also makes exact reference-set members trivially confident at similarity 1.0).

- entity-extractor.cjs exports { extractEntities, ENTITY_TYPES, DEFAULT_MAX_PER_ARTIFACT }. FRAMEWORK_TERMS (line ~86) is a module-internal frozen Set of lowercase methodology/framework vocabulary ('canon', 'icm layer', 'feynman', 'minto', 'tam', 'sam', 'som', 'jtbd', 'kpi', 'roi', 'mvp', ...). It is NOT currently exported. This plan exports it additively.

- entity-classifier.cjs (tier-2b, DO NOT EDIT): async classifyArtifactCandidates({ names, excerpt }, opts) -> { labels: { [name]: 'what'|'why'|'noise' }, source: 'model'|'fallback' }. Degrades to every-name-'what'/source-'fallback' on any failure, never throws. Its contract already accepts an arbitrary subset of names, so escalation-only routing needs zero changes to it.

- scripts/entity-extract.cjs::runTier2(artifacts, entities, tier1WhyTerms, classifyImpl, keyPresent) (line ~386) is the routing seam this plan rewires. Today it groups entities by sourceArtifactId and calls classifyImpl once per artifact for ALL of that artifact's candidates. runExtraction (line ~492) computes classifyImpl/keyPresent from opts.classifyImpl injection or resolveAnthropicKey(), calls runTier2 strictly BEFORE db.exec('BEGIN'), then partitions whatEntities/whyTerms/droppedNoise. artifactExcerpt(text) (line ~353) builds the per-artifact excerpt. status.json additive keys today: entities_what, terms_why, dropped_noise, classifier_source ('model'|'fallback'|'mixed'), framework_applied, framework_skipped. cmdReport prints them.

- LITERAL-SUBSTRING HYGIENE (hard requirement, unchanged from hzx): run-all-218.sh leg (d) greps entity-extract.cjs AND entity-extractor.cjs for fetch|https?\.|node:http as literal text, including comments. Every comment written in those two files must say "the tier-2b model transport" or "the classifier module", never the transport verb or protocol name. The require line require('../lib/core/eureka/embedding-classifier.cjs') is clean.

- tests/test-218-what-why-classifier.cjs: the sibling-transport leg dynamically scans ALL lib/core/eureka/*.cjs for the literal Anthropic host string after stripping block comments and asserts entity-classifier.cjs is the sole carrier. The new embedding module must not contain that host string anywhere (line comments are NOT stripped by that leg). The four existing integration legs (i)-(iv) call runExtraction with opts.classifyImpl injected and assert classifierSource === 'model' or 'fallback'; they MUST be updated (see Task 2) because the real embedding module could otherwise run nondeterministically on a machine with a cached encoder.

- The domain-agnostic grep gate (leg d of the module tests) applies to entity-classifier.cjs ONLY. The new embedding module deliberately carries reference vocabulary; that is its design (a semantic anchor set that also resolves NEAR matches, not a stoplist) and is exempt from that gate. Do not add the new module to that gate.

- Reference-set seed material (reuse, do not invent): WHAT exemplars from the existing synthetic non-MindrianOS fixtures in commit 7d98c9b8 and the hzx tests: 'Helix Biosciences', 'Genomix', 'Meridian Therapeutics', 'BioNova Labs', 'AION Labs', 'Recursion', 'Prodrive', 'Xtrac'. WHY exemplars: Array.from(FRAMEWORK_TERMS) (the natural seed per the directive) plus the three live 218-VERIFICATION.md residual-noise terms 'Larry', 'Governing Thought', 'Pyramid Logic' (the room's own persona/methodology vocabulary; exact membership makes them trivially confident WHY at zero cost, which is exactly what unblocks the pending hzx Task 3 without credits).

- Real room for measurement: ~/MindrianRooms/aion-eureka-synergy (the Phase 218 live-verification room). The encoder model is almost certainly already cached on this machine (Eureka ranking re-embedded this room on 2026-07-12).

- Dev-research compositing target (K44-REQ-8): UPDATE ~/MindrianRooms/rethinking-mindrianos/research/2026-07-14-eureka-ranking-bug-and-what-why-classifier/2026-07-14-eureka-ranking-bug-and-what-why-classifier.md AND its source-of-record mirror ~/MindrianOS/research/2026-07-14-eureka-ranking-bug-and-what-why-classifier/2026-07-14-eureka-ranking-bug-and-what-why-classifier.md. Do NOT create a new dated entry.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier-2a embedding classifier module + FRAMEWORK_TERMS export + module-level tests</name>
  <files>lib/core/eureka/embedding-classifier.cjs, lib/core/eureka/entity-extractor.cjs, tests/test-218-what-why-classifier.cjs</files>
  <action>
Three changes (K44-REQ-1).

(1) entity-extractor.cjs: export FRAMEWORK_TERMS additively (module.exports gains the key; nothing else moves). Add one sentence to its doc comment: the set is now ALSO the WHY reference seed for the tier-2a embedding classifier (embedding-classifier.cjs), reused there per Canon Part 7 rather than copied. Comment hygiene: no transport verbs or protocol names anywhere in this file (leg d grep).

(2) NEW lib/core/eureka/embedding-classifier.cjs, the tier-2a classifier. Contract:

  async classifyByEmbedding({ names }, opts)
    -> { ok: true,
         results: { [name]: { label: 'what'|'why', margin: number, confident: boolean } },
         provenance: { model, dim, threshold } }
     | { ok: false, error: 'encoder_unavailable'|'embed_failed', detail }

Never throws across the boundary. The embedding tier labels ONLY what/why - it structurally cannot detect NOISE (fragments); noise stays an LLM-tier verdict, state this in the header.

Reference sets (module-scope frozen arrays, provenance comments naming their sources):
- WHY_EXAMPLES = Array.from(require('./entity-extractor.cjs').FRAMEWORK_TERMS) concat ['Larry', 'Governing Thought', 'Pyramid Logic'] (the 218-VERIFICATION.md live residual-noise terms; cite that file in the comment). Reuse the export; NEVER copy the vocabulary.
- WHAT_EXAMPLES = ['Helix Biosciences', 'Genomix', 'Meridian Therapeutics', 'BioNova Labs', 'AION Labs', 'Recursion', 'Prodrive', 'Xtrac'] (the existing synthetic-fixture and verification-corpus names from commit 7d98c9b8 / quick 260714-hzx; comment says reused-not-invented per the directive).

Scoring (nearest-neighbor, per the infrastructure-fit rationale in the context section): embed the full reference set ONCE per process via embedTexts (module-scope promise cache keyed by resolveModel(), the embedding-spine _pipelineCache idiom; expose _test.resetRefCache()). For each candidate batch: one embedTexts call for all names, then per name simWhat = max cosineSimilarity to any WHAT exemplar vector, simWhy = max to any WHY exemplar vector, label = whichever is larger ('what' on an exact tie, the fail-open direction), margin = Math.abs(simWhat - simWhy), confident = margin >= resolveMargin().

resolveMargin(): MINDRIAN_WHATWHY_MARGIN env, validated positive-float parse mirroring resolveBatchSize's shape (non-empty trimmed string -> Number -> finite and > 0, else default). DEFAULT_MARGIN = 0.05 provisional; Task 3 calibrates it against the real encoder and updates the constant if the measured separation demands it (leave a comment saying the default is measurement-calibrated in quick 260714-k44 Task 3).

Seams and degradation: forward opts.encodeFn and opts._forceUnavailable to embedTexts (the existing spine seams). Any embedTexts failure (encoder_unavailable, embed_failed) returns { ok: false, ... } so the caller routes around the whole tier; empty/invalid names input returns ok:true with empty results. Header documents: Canon Part 8 fully local zero-egress posture (delegating to embedding-spine's documented boundary - generic model weights by id only, no user bytes); the module must never contain the Anthropic transport host string (the sibling-transport test scans every eureka module); no em-dashes.

(3) Extend tests/test-218-what-why-classifier.cjs with module-level legs for the new module (require it at the top alongside the existing requires):
- (m1) deterministic routing: inject opts.encodeFn returning hand-built 2-dim vectors by text lookup (reference WHAT names -> [0,1], reference WHY terms -> [1,0], candidate 'TestCorp' -> [0.1, 0.995], candidate 'Test Framework' -> [0.995, 0.1], candidate 'Ambiguous Term' -> [0.7071, 0.7071]); assert 'TestCorp' labels what/confident, 'Test Framework' labels why/confident, 'Ambiguous Term' has confident === false and a near-zero margin.
- (m2) reference cache: a counting encodeFn across TWO classifyByEmbedding calls proves the reference texts are embedded exactly once (first call embeds references + candidates, second call embeds candidates only). Use _test.resetRefCache() first.
- (m3) unavailable degrade: opts._forceUnavailable -> { ok: false, error: 'encoder_unavailable' }, no throw.
- (m4) Part 7 reuse gate: read the module source; assert it requires './entity-extractor' and references FRAMEWORK_TERMS, and assert require('.../entity-extractor.cjs').FRAMEWORK_TERMS is a Set (the export landed).
- (m5) zero-transport gate: assert the module source contains no Anthropic host substring and no brain-host substring (mirror the existing leg (e) shape).
The existing sibling-transport leg needs no edit (it scans the eureka dir dynamically and the new module carries no transport string).
  </action>
  <verify>
    <automated>node tests/test-218-what-why-classifier.cjs && node tests/test-218-extractor.cjs && bash -c "! grep -rnE \"fetch|https?\\.|require\\('node:http\" lib/core/eureka/entity-extractor.cjs" && git diff --exit-code lib/core/eureka/entity-classifier.cjs</automated>
  </verify>
  <done>embedding-classifier.cjs exists with the documented contract, cached reference embedding, and margin scoring; FRAMEWORK_TERMS exported and reused (not copied); all new module-level legs pass offline with injected vectors and zero model loads; all pre-existing legs still pass; entity-classifier.cjs byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Two-tier routing in the dispatcher + integration legs + suite comment updates</name>
  <files>scripts/entity-extract.cjs, tests/test-218-what-why-classifier.cjs, tests/run-all-218.sh</files>
  <action>
Rewire runTier2 in scripts/entity-extract.cjs to the locked two-tier flow (K44-REQ-2/3/4), keeping every model-free path byte-safe:

(1) Require the new module at the top (require('../lib/core/eureka/embedding-classifier.cjs')); comment hygiene per the leg (d) grep (say "the tier-2a embedding classifier", never a transport verb). runExtraction opts gains two additive seams mirroring the opts.classifyImpl idiom: opts.embedClassifyImpl (defaults to the real classifyByEmbedding) and opts._forceNoLlm (test-only; forces keyPresent false so the honest-degrade leg is deterministic regardless of the machine's real key state).

(2) New runTier2 flow (still strictly BEFORE db.exec('BEGIN'); neither tier ever holds the write lock):
  a. Collect the unique candidate names across all tier-1 survivors. ONE embedClassifyImpl({ names: uniqueNames }) call (the module batches embedding internally).
  b. If the embedding tier returns ok:false: the ENTIRE tier degrades to the current (hzx) behavior verbatim - per-artifact classifyImpl for all candidates when keyPresent, else the every-name-'what' fallback. classifier_source stays 'model'/'fallback'/'mixed' exactly as today on this branch.
  c. If ok:true: per candidate, confident results resolve immediately (label 'what' keeps the entity flowing; 'why' joins whyTerms; tally embeddingResolved). Unconfident candidates form the escalation set.
  d. Escalation (K44-REQ-2): group ONLY the unconfident candidates by sourceArtifactId. When keyPresent: one classifyImpl({ names: escalatedNamesOnly, excerpt: artifactExcerpt(...) }) per affected artifact; a returned 'what'/'why'/'noise' label is used as-is (tally modelResolved); a per-artifact failure (throw, bad shape, source 'fallback') routes THAT artifact's escalated candidates to the embedding best-guess labels (tally lowConfidence). When !keyPresent (K44-REQ-4): every escalated candidate takes its embedding best-guess label even below threshold (tally lowConfidence) - NEVER a silent 'what' default.
  e. classifier_source (K44-REQ-3): 'embedding' when only the embedding tier resolved candidates (confident or best-guess), 'model' when only the LLM tier resolved, 'mixed' when both, 'fallback' only when nothing resolved semantically (the ok:false + no-key branch, today's pass-through). Return additive counts from runTier2 alongside the existing fields: embeddingResolved, escalated, modelResolved, lowConfidence.

(3) status.json + report (K44-REQ-3): additive keys tier2_embedding, tier2_escalated, tier2_model, tier2_low_confidence riding both the success and degrade branches (the tier2Result object). cmdReport's done line appends them, printing the low-confidence count explicitly when > 0 (the room's standing fallback-disclosure concern: a low-confidence embedding-only resolution is disclosed, never dressed up as a full resolution).

(4) Export collectArtifacts additively from entity-extract.cjs (module.exports gains it) so Task 3's read-only measurement harness can count candidates without writing to the room. Update the module header's tier-2 paragraph honestly: tier-2a is the local embedding classifier (zero egress, delegating to embedding-spine's boundary); tier-2b is the unchanged classifier module, now escalation-only.

(5) Update the four EXISTING integration legs in tests/test-218-what-why-classifier.cjs for determinism (a cached encoder must never make them flaky): legs (i), (iii), (iv) additionally inject embedClassifyImpl returning ok:true with EVERY candidate { label: 'what', margin: 0, confident: false } (everything escalates, so the injected classifyImpl still decides everything and every existing assertion holds, including classifierSource === 'model'). Leg (ii) injects embedClassifyImpl returning { ok: false, error: 'encoder_unavailable' } alongside its throwing classifyImpl (full degrade chain -> 'fallback', assertions unchanged).

(6) Add the three NEW routing legs (K44-REQ-6), all hermetic temp rooms via the existing buildRoom idiom:
  - (v) embedding-alone: embedClassifyImpl labels Larry/Governing Thought/Pyramid Logic why/confident and AION Labs what/confident; classifyImpl is a counter stub that THROWS if ever invoked. Assert: zero LLM calls, the WHY terms land in framework_terms and never as entity nodes, AION Labs is a proposed entity node, result.classifierSource === 'embedding', result counts show embeddingResolved >= 4 and escalated === 0.
  - (vi) partial escalation: embedClassifyImpl marks exactly one candidate (e.g. 'Recursion') confident:false (best-guess 'what') and the rest confident; a counting classifyImpl asserts it is called exactly ONCE with names deep-equal ['Recursion'], returns { labels: { Recursion: 'what' }, source: 'model' }. Assert classifierSource === 'mixed', escalated === 1, modelResolved === 1, lowConfidence === 0.
  - (vii) honest degrade: embedClassifyImpl marks one WHY-leaning candidate confident:false with best-guess label 'why'; opts._forceNoLlm true and NO classifyImpl injected. Assert the candidate is routed to framework_terms (the best guess stood), it is NOT an entity node, lowConfidence === 1, and classifierSource === 'embedding' (the embedding tier resolved it, disclosed as low-confidence - not 'fallback', not a silent 'what').

(7) tests/run-all-218.sh: comment-only updates. The EGRESS RULE header paragraph and the tier-2 leg comment now describe the two-tier design honestly (tier-2a: local embedding classifier, zero egress, free; tier-2b: the Anthropic-transport classifier module, escalation-only, degrade-to-best-guess). Leg (d)'s grep file list stays FROZEN exactly as-is and must keep passing. No new legs (the test file is already leg 5).
  </action>
  <verify>
    <automated>bash tests/run-all-218.sh && git diff --exit-code lib/core/eureka/entity-classifier.cjs lib/core/eureka/vector-store.cjs lib/core/navigation/insights.cjs lib/core/graph-ops.cjs</automated>
  </verify>
  <done>Full Phase 218 aggregator green: all pre-existing legs unchanged (zero-network grep, REQ-3/REQ-4 zero-touch git-diff gates, connector-registry check), the four updated integration legs deterministic under injected embedding stubs, and the three new routing legs prove embedding-alone resolution, minimal escalation, and honest no-LLM degrade - all offline with zero live model or encoder loads. entity-classifier.cjs and the zero-touch modules byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Threshold calibration + measured call-volume reduction on the real room + docs + commits</name>
  <files>lib/core/eureka/embedding-classifier.cjs, docs/ENV-TUNING.md</files>
  <action>
Live local calibration and measurement (K44-REQ-5), then the documentation contract (K44-REQ-7/8). Everything here is local-CPU only; no API key is needed at any point.

(1) Calibration (real encoder, scratch harness in the session scratchpad directory, NOT committed): embed the full reference set plus a holdout list with the real local encoder (the WHY holdouts 'Larry', 'Governing Thought', 'Pyramid Logic' with themselves excluded from the reference set for this measurement, plus non-member holdouts like 'IND Filing' and 'Phase II Readout'; WHAT holdouts like 'Sabra' and 2-3 real company names visible in the aion room's artifacts). Print each holdout's simWhat, simWhy, margin, and verdict. Pick DEFAULT_MARGIN so every clear holdout is confident-and-correct while genuinely ambiguous terms escalate; update the constant in embedding-classifier.cjs and record the measured margin table in the SUMMARY. If the encoder model is not yet cached, the one-time generic-weights download is acceptable per embedding-spine's documented Part 8 posture.

(2) Measurement on the real room (K44-REQ-5, read-only - NO writes to aion-eureka-synergy): a scratch harness that requires { collectArtifacts } from scripts/entity-extract.cjs and { extractEntities } from the extractor, opens ~/MindrianRooms/aion-eureka-synergy read-style via openRoomDb, collects artifacts, runs tier-1 per artifact, then runs the REAL classifyByEmbedding over the unique survivors. It must NOT call runExtraction and must NOT open a transaction. Report: (a) baseline expected LLM calls under the hzx design = artifacts with >= 1 survivor; (b) post-change expected LLM calls = artifacts with >= 1 unconfident candidate; (c) candidate-level: total unique candidates vs escalated candidates; (d) the actual list of escalated terms (they are the honest picture of what the embedding tier cannot resolve). Compute and record the percentage reduction for (a)->(b) and (c). Report the real numbers whatever they are - if the reduction is NOT significant, that is a finding to surface, not to hide. Close the db handle when done.

(3) docs/ENV-TUNING.md: add a MINDRIAN_WHATWHY_MARGIN section following the existing format (what it tunes, default, when to change it, example export line). No em-dashes.

(4) Dev-research compositing (K44-REQ-8): UPDATE the existing entry - append a dated section ("2026-07-14 addendum: two-tier redesign, quick 260714-k44") to BOTH homes: ~/MindrianRooms/rethinking-mindrianos/research/2026-07-14-eureka-ranking-bug-and-what-why-classifier/2026-07-14-eureka-ranking-bug-and-what-why-classifier.md and the ~/MindrianOS/research/... mirror of the same name. Content: the locked two-tier architecture and why (semantic-similarity question, not language-reasoning, for the clear majority), the nearest-neighbor-over-centroid rationale, the calibrated threshold with the measured margin table, the measured call-volume reduction numbers from (2), the honest-degrade contract, and a cross-reference to this plan's path (.planning/quick/260714-k44-replace-the-tier-2-what-vs-why-classifie/260714-k44-PLAN.md). Do NOT create a new dated entry directory.

(5) SUMMARY note (K44-REQ-7, mandatory content for 260714-k44-SUMMARY.md): state that quick 260714-hzx's pending Task 3 must NOT be resumed as originally scoped, and write the new live-acceptance criteria for that verification: (a) the residual noise terms (Larry, Governing Thought, Pyramid Logic) must resolve WHY via the free embedding tier with classifier_source 'embedding' or 'mixed' and ZERO API spend (they are reference-set members, so this no longer requires a funded key at all); (b) framework_terms capture and the 0.0 percent structural-share hold as before; (c) a funded key is only needed to exercise the tier-2b escalation path on genuinely ambiguous candidates, and status.json's tier2_escalated/tier2_low_confidence counts must reconcile with whichever path ran. Note explicitly that this may fully unblock the original verification without any credits.

(6) Commits (K44-REQ-8): atomic per concern - (1) feat: tier-2a embedding classifier + FRAMEWORK_TERMS export + module tests; (2) feat: two-tier routing + integration legs + suite comments; (3) docs: ENV-TUNING + calibrated default + research compositing note. Conventional messages, no em-dashes anywhere. .planning/ is gitignored - git add -f for the plan/summary artifacts per repo convention. The research-entry files live outside this repo (room directories); update them on disk but do not try to commit them here.
  </action>
  <verify>
    <automated>bash tests/run-all-218.sh && grep -c "MINDRIAN_WHATWHY_MARGIN" docs/ENV-TUNING.md && bash -c "grep -q '260714-k44' ~/MindrianRooms/rethinking-mindrianos/research/2026-07-14-eureka-ranking-bug-and-what-why-classifier/2026-07-14-eureka-ranking-bug-and-what-why-classifier.md && grep -q '260714-k44' ~/MindrianOS/research/2026-07-14-eureka-ranking-bug-and-what-why-classifier/2026-07-14-eureka-ranking-bug-and-what-why-classifier.md"</automated>
    <human-check>Read the measured reduction numbers in the SUMMARY: baseline vs escalation call counts on aion-eureka-synergy and the list of terms that still escalate - confirm the reduction is real and the escalated list looks genuinely ambiguous</human-check>
  </verify>
  <done>DEFAULT_MARGIN calibrated from measured margins (table recorded); the API-call reduction measured on the real room with actual numbers (baseline calls, escalation calls, percentage, escalated-term list) in the SUMMARY; ENV-TUNING.md documents the tunable; the existing research entry updated in both homes with the addendum and cross-reference (no duplicate entry); SUMMARY states the corrected hzx Task 3 acceptance criteria; three atomic commits landed; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room prose -> tier-2a embedding | user room content (candidate names) is embedded LOCALLY; zero bytes leave the machine |
| room prose -> tier-2b prompt | escalated candidate names + 600-char excerpt cross to the Anthropic LLM transport (unchanged hzx boundary, now a smaller subset) |
| env -> threshold | MINDRIAN_WHATWHY_MARGIN influences which candidates escalate |
| LOCAL -> Brain MCP | must remain uncrossed - Part 8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-K44-01 | Tampering | embedding label -> write path | mitigate | tier-2a emits ONLY 'what'/'why' from a closed two-way comparison; 'noise' remains an LLM-tier-only verdict; unknown/malformed results route to escalation, and the ok:false branch degrades to the prior verbatim behavior |
| T-K44-02 | Tampering | MINDRIAN_WHATWHY_MARGIN env | mitigate | validated positive-float parse mirroring resolveBatchSize; any malformed value falls back to the calibrated default, so an operator env can never zero-out or invert routing |
| T-K44-03 | Denial of Service | encoder load blocking the write lock | mitigate | the entire tier-2a + tier-2b pass completes strictly BEFORE db.exec('BEGIN') (the existing hzx rule, preserved); encoder_unavailable degrades the whole tier without a throw |
| T-K44-04 | Information Disclosure | LOCAL -> Brain / network via the new module | mitigate | zero-transport test gate on the new module (no Anthropic host, no Brain host); the sibling-transport leg dynamically proves entity-classifier.cjs stays the sole eureka transport carrier; run-all-218 leg (d) grep list frozen |
| T-K44-05 | Repudiation | dishonest source reporting | mitigate | classifier_source extended to embedding/model/mixed/fallback + tier2_low_confidence count; a below-threshold best-guess is always counted and printed by cmdReport, never reported as a full resolution |
| T-K44-06 | Spoofing | tests reaching a live encoder or model | mitigate | every integration leg injects embedClassifyImpl (or ok:false) and classifyImpl; module legs use encodeFn/_forceUnavailable; the counter stub in leg (v) throws on any LLM invocation |
| T-K44-SC | Tampering | npm installs | accept | zero new dependencies - node builtins + the already-vendored embedding-spine/transformers path only |
</threat_model>

<verification>
- bash tests/run-all-218.sh fully green: every pre-existing leg unchanged (zero-network grep on tier-1 + dispatcher body, REQ-3/REQ-4 zero-touch git-diff gates, connector-registry check) plus the updated tier-2 legs covering the three routing outcomes.
- git diff --exit-code lib/core/eureka/entity-classifier.cjs stays clean throughout (K44-REQ-5: demoted, never edited).
- The sibling-transport leg proves entity-classifier.cjs is still the ONLY eureka module carrying the Anthropic transport; the new embedding module carries none.
- Measured on aion-eureka-synergy (read-only, key-less): baseline per-artifact LLM call count vs post-change escalation call count, candidate-level totals, escalated-term list, and percentage reduction recorded in the SUMMARY with real numbers.
- classifier_source vocabulary extended (embedding/model/mixed/fallback) with tier2_embedding/tier2_escalated/tier2_model/tier2_low_confidence counts in status.json and the report line.
- Research entry updated in BOTH homes with the addendum section (no duplicate entry); ENV-TUNING.md documents MINDRIAN_WHATWHY_MARGIN.
</verification>

<success_criteria>
- The locked two-tier design ships: a free local embedding pass resolves confident candidates with zero API calls; only the low-margin residual escalates to the unchanged entity-classifier.cjs, per artifact, escalated names only.
- Every degrade is honest: no LLM means embedding best-guess with a disclosed low-confidence count, never a silent 'what'; encoder-unavailable means the prior hzx behavior verbatim.
- The API-call-volume reduction is measured on a real room and reported with actual numbers; the SUMMARY redefines the pending hzx Task 3 acceptance criteria around the credit-free embedding path.
- All reasoning is filed in both homes by updating the existing 2026-07-14 research entry, cross-linked to this plan.
</success_criteria>

<output>
Create .planning/quick/260714-k44-replace-the-tier-2-what-vs-why-classifie/260714-k44-SUMMARY.md when done.
</output>
