---
phase: quick/260923-d9l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
autonomous: true
requirements: [R1, R2, R3, R4, R5]

must_haves:
  truths:
    - "R1: .planning/config.json has a top-level model_overrides object, placed immediately after _models_note, with exactly these 18 keys in this order, every value the string claude-opus-5-5: gsd-advisor-researcher, gsd-ai-researcher, gsd-assumptions-analyzer, gsd-codebase-mapper, gsd-doc-classifier, gsd-doc-synthesizer, gsd-domain-researcher, gsd-eval-planner, gsd-framework-selector, gsd-intel-updater, gsd-pattern-mapper, gsd-phase-researcher, gsd-planner, gsd-project-researcher, gsd-research-synthesizer, gsd-roadmapper, gsd-ui-researcher, gsd-user-profiler"
    - "R2: models.planning, models.discuss and models.research are the alias opus (fallback tier for any planning/discuss/research agent GSD adds later); models.execution/verification/completion stay sonnet"
    - "R3: model_profile_overrides is byte-identical to HEAD (no claude.opus entry added, the dead sonnet/haiku entries NOT removed); every other key except models, _models_note and the new model_overrides is byte-identical to HEAD, and the original key order is preserved"
    - "R4: _models_note records the navigator ruling of 2026-09-23 (Opus 5.5 via per-agent model_overrides, opus fallback, execution side on sonnet) and why the 2026-08-10 fable directive never took effect; zero em-dashes in the file"
    - "R5: both resolvers (node ~/.claude/gsd-core/bin/gsd-tools.cjs resolve-model AND gsd-sdk query resolve-model) return claude-opus-5-5 for gsd-planner, gsd-phase-researcher and gsd-assumptions-analyzer (and for all 18 pinned agents); gsd-executor and gsd-verifier resolve to bare sonnet under gsd-tools; what gsd-sdk returns for those two is recorded, not asserted"
    - "One commit containing only .planning/config.json (24 insertions, 4 deletions); the peer-owned ROADMAP.md/STATE.md diffs are never staged, stashed, checked out or reverted"
  artifacts:
    - path: ".planning/config.json"
      provides: "Per-agent Opus 5.5 pinning for the planning/discuss/research agent families, opus fallback tier, rewritten _models_note"
      contains: "\"model_overrides\""
  key_links:
    - from: ".planning/config.json model_overrides.<agent>"
      to: "~/.claude/gsd-core/bin/lib/core.cjs resolveModelInternal step 1 (lines 1343-1346)"
      via: "config.model_overrides[agentType] is returned verbatim before any tier or profile logic runs; gsd-sdk honors the same key"
      pattern: "\"gsd-planner\": \"claude-opus-5-5\""
    - from: ".planning/config.json models.{planning,discuss,research}"
      to: "core.cjs resolveModelInternal step 2 (VALID_TIERS opus|sonnet|haiku|inherit)"
      via: "fallback tier for agents not listed in model_overrides"
      pattern: "\"(planning|discuss|research)\": \"opus\""
---

<objective>
Replace the dead fable routing in this repo's GSD config with the navigator's 2026-09-23 ruling: planning, discuss and research agents run on Opus 5.5 (`claude-opus-5-5`), pinned per agent through `model_overrides`, with `models.planning/discuss/research = "opus"` as the fallback tier. Execution, verification and completion stay on sonnet.

Purpose, in plain terms. GSD picks a model for each agent in steps. Step 1 checks `model_overrides[<agent>]` and, if present, returns that exact string. Step 2 checks `models.<phaseType>` but only accepts `opus | sonnet | haiku | inherit`. The old value `"fable"` was not on that list, so it was silently thrown away and the agent fell back to the `quality` profile default (opus alias). The fable directive of 2026-08-10 therefore never took effect. `model_profile_overrides` (step 3) is only read when `runtime` is set to something other than `claude`; this repo has no `runtime`, so a `claude.opus` entry there would be ignored too. Step 1 (`model_overrides`) is the only existing mechanism that verifiably yields the exact id `claude-opus-5-5`, and it was proven at planning time to work in BOTH resolvers (`gsd-tools.cjs` and `gsd-sdk`) on a scratch copy of this config.

Agent list, enumerated at plan time from `~/.claude/agents/gsd-*.md` (33 files) crossed with `AGENT_TO_PHASE_TYPE` in `~/.claude/gsd-core/bin/lib/model-catalog.cjs`: exactly 18 agents map to planning, discuss or research. It matches the list proposed earlier exactly; no agent was added or dropped. Every on-disk gsd agent is in the catalog and every catalog planning/discuss/research agent is on disk.
- planning (5): gsd-eval-planner, gsd-framework-selector, gsd-pattern-mapper, gsd-planner, gsd-roadmapper
- discuss (1): gsd-assumptions-analyzer
- research (12): gsd-advisor-researcher, gsd-ai-researcher, gsd-codebase-mapper, gsd-doc-classifier, gsd-doc-synthesizer, gsd-domain-researcher, gsd-intel-updater, gsd-phase-researcher, gsd-project-researcher, gsd-research-synthesizer, gsd-ui-researcher, gsd-user-profiler

Scope locks from the coordinator: leave `model_profile_overrides` exactly as it is (no `claude.opus` entry, do not remove the dead `claude.sonnet` / `claude.haiku` entries). Commit only `.planning/config.json`.

Output: `.planning/config.json` with a 24-insertion / 4-deletion diff, committed alone, and a SUMMARY that carries the follow-ups listed in Task 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/config.json

Planning-time facts (already verified, do not re-derive):
- `.planning/config.json` is git-TRACKED even though `.gitignore` line 97 ignores `.planning/*`; plain `git add .planning/config.json` works because the file is already tracked. Use `git add -f .planning/config.json` only if git refuses.
- This quick-task directory IS ignored; the SUMMARY file needs `git add -f` if the orchestrator commits docs.
- At planning time the working tree had peer-owned uncommitted edits to `.planning/ROADMAP.md` and `.planning/STATE.md`. They are NOT yours. Never stage, stash, checkout or revert them.
- The file is exactly `JSON.stringify(obj, null, 2)` plus a trailing newline: parsing it and re-serializing that way reproduces it byte for byte (checked at planning time). So a parse / mutate / re-serialize edit touches only the lines that actually change.
- The config currently has no `runtime` key and no `model_overrides` key. Current `models` block: planning/discuss/research are `"fable"`, execution/verification/completion are `"sonnet"`.
- Resolvers: `node $HOME/.claude/gsd-core/bin/gsd-tools.cjs resolve-model <agent>` (core.cjs, reads the `models` block) and `gsd-sdk query resolve-model <agent>` (`~/.local/bin/gsd-sdk`, npx get-shit-done-cc; ignores the `models` block entirely). Both print JSON with a `model` field. gsd-tools prints a harmless stderr warning that `_models_note` is an unknown key.
- Planning-time simulation of the exact target config on a scratch copy: all 18 pinned agents resolved to `claude-opus-5-5` under both resolvers; gsd-executor resolved to `sonnet` (gsd-tools) and `opus` (gsd-sdk); gsd-verifier resolved to `sonnet` under both; the diff against the current file was 24 insertions, 4 deletions.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the 18 planning/discuss/research agents to claude-opus-5-5, set the opus fallback, rewrite _models_note</name>
  <files>.planning/config.json</files>
  <action>
Guard first. (a) Confirm `.planning/config.json` has no uncommitted changes (`git diff --quiet -- .planning/config.json`); if it is dirty, STOP and report, because the file belongs to someone else's in-flight work and the HEAD baseline for verification would be wrong. (b) Re-enumerate the agent list live: every `~/.claude/agents/gsd-*.md` whose basename maps to planning, discuss or research in `AGENT_TO_PHASE_TYPE` from `~/.claude/gsd-core/bin/lib/model-catalog.cjs`, sorted. If it differs from the 18 names in this plan's objective, STOP and report the added/dropped agent names. Do not silently adapt the list; the navigator approved these 18.

Then make the edit in one pass with a short `node -e` script run through Bash (parse the file, mutate, re-serialize with 2-space indent plus a trailing newline, write it back). Because the file round-trips byte for byte, only the changed lines move. The mutations, all per the 2026-09-23 navigator ruling:
1. Set `models.planning`, `models.discuss` and `models.research` to `"opus"`. Leave `models.execution`, `models.verification` and `models.completion` as `"sonnet"`. Do not reorder keys inside `models`.
2. Replace the value of `_models_note` with exactly this sentence pair (plain hyphen after the date, no em-dashes, no inner double quotes): Navigator ruling 2026-09-23 - planning/discuss/research agents are pinned to Opus 5.5 (claude-opus-5-5) through per-agent model_overrides, with models.planning/discuss/research = opus as the fallback for agents added later; execution/verification/completion stay on sonnet. The 2026-08-10 fable directive never took effect, because resolveModelInternal drops aliases it does not recognize and fell back to the quality-profile default (opus).
3. Insert a new top-level key `model_overrides` immediately after `_models_note` (build a fresh object by walking the original keys in order and emitting `model_overrides` right after `_models_note`, so every other top-level key keeps its position). Its value is an object whose keys are the 18 agent names in sorted order (gsd-advisor-researcher, gsd-ai-researcher, gsd-assumptions-analyzer, gsd-codebase-mapper, gsd-doc-classifier, gsd-doc-synthesizer, gsd-domain-researcher, gsd-eval-planner, gsd-framework-selector, gsd-intel-updater, gsd-pattern-mapper, gsd-phase-researcher, gsd-planner, gsd-project-researcher, gsd-research-synthesizer, gsd-roadmapper, gsd-ui-researcher, gsd-user-profiler), each set to the string `claude-opus-5-5`.

Do NOT touch `model_profile_overrides`: no `claude.opus` entry, and the existing `claude.sonnet` / `claude.haiku` entries stay even though they are never read under runtime=claude (they become a SUMMARY follow-up). Do NOT add a `runtime` key or `resolve_model_ids`. Do NOT touch `mode`, `granularity`, `parallelization`, `commit_docs`, `model_profile`, `workflow` or `graphify`.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && node -e "const cp=require('child_process'),fs=require('fs'),H=process.env.HOME,m=require(H+'/.claude/gsd-core/bin/lib/model-catalog.cjs');const P='.planning/config.json';const L='gsd-advisor-researcher,gsd-ai-researcher,gsd-assumptions-analyzer,gsd-codebase-mapper,gsd-doc-classifier,gsd-doc-synthesizer,gsd-domain-researcher,gsd-eval-planner,gsd-framework-selector,gsd-intel-updater,gsd-pattern-mapper,gsd-phase-researcher,gsd-planner,gsd-project-researcher,gsd-research-synthesizer,gsd-roadmapper,gsd-ui-researcher,gsd-user-profiler'.split(',');const E=(a,b)=>JSON.stringify(a)===JSON.stringify(b);const die=s=>{console.error('FAIL '+s);process.exit(1)};const live=fs.readdirSync(H+'/.claude/agents').filter(f=>/^gsd-.*[.]md$/.test(f)).map(f=>f.slice(0,-3)).filter(a=>['planning','discuss','research'].includes(m.AGENT_TO_PHASE_TYPE[a])).sort();if(!E(live,L))die('agent drift, live='+live.join(','));const h=JSON.parse(cp.execSync('git show HEAD:.planning/config.json'));const c=JSON.parse(fs.readFileSync(P,'utf8'));for(const k of ['planning','discuss','research'])if(c.models[k]!=='opus')die('models.'+k+'='+c.models[k]);for(const k of ['execution','verification','completion'])if(c.models[k]!=='sonnet')die('models.'+k+'='+c.models[k]);if(!E(Object.keys(c.models),Object.keys(h.models)))die('models keys changed');const mo=c.model_overrides||{};if(!E(Object.keys(mo),L))die('model_overrides keys='+Object.keys(mo).join(','));if(!Object.values(mo).every(v=>v==='claude-opus-5-5'))die('model_overrides value not claude-opus-5-5');const hk=Object.keys(h),want=hk.slice();want.splice(hk.indexOf('_models_note')+1,0,'model_overrides');if(!E(Object.keys(c),want))die('top-level key order');for(const k of hk)if(k!=='models'&&k!=='_models_note'&&!E(c[k],h[k]))die('collateral change in '+k);const n=c._models_note;for(const s of ['2026-09-23','Opus 5.5','claude-opus-5-5','model_overrides','2026-08-10','resolveModelInternal','quality-profile','sonnet'])if(!n.includes(s))die('_models_note missing '+s);if(n.includes('Fable plans'))die('_models_note still has the old text');console.log('OK')" && test "$(grep -c "$(printf '\342\200\224')" .planning/config.json)" = 0 && echo NO_EMDASH && test "$(git diff --numstat -- .planning/config.json)" = "$(printf '24\t4\t.planning/config.json')" && echo NUMSTAT_OK</automated>
  </verify>
  <done>Prints `OK`, `NO_EMDASH` and `NUMSTAT_OK`. That means: the live agent enumeration still equals the 18-name list; models planning/discuss/research are `opus` and execution/verification/completion are `sonnet`; `model_overrides` has exactly the 18 keys in sorted order, all `claude-opus-5-5`, sitting right after `_models_note`; `model_profile_overrides` and every other untouched key deep-equal HEAD; `_models_note` carries the 2026-09-23 ruling and the fable root cause; the file has no em-dash; the working-tree diff is exactly 24 insertions and 4 deletions. At planning time this gate was dry-run: it failed with `models.planning=fable` on the current file and passed (OK, NO_EMDASH, 24/4) on a simulated target, so it is a real RED-to-GREEN gate.</done>
</task>

<task type="auto">
  <name>Task 2: Prove both resolvers return claude-opus-5-5, commit config.json alone, record follow-ups</name>
  <files>.planning/config.json</files>
  <action>
Prove the change reaches actual agent routing in both resolvers before committing. For each of the 18 pinned agents (this includes the three the navigator named: gsd-planner, gsd-phase-researcher, gsd-assumptions-analyzer), run `node $HOME/.claude/gsd-core/bin/gsd-tools.cjs resolve-model <agent>` and `gsd-sdk query resolve-model <agent>` from the repo root and read the `model` field; both must be `claude-opus-5-5`. For the controls gsd-executor and gsd-verifier, gsd-tools must return bare `sonnet`; also run gsd-sdk for them and RECORD what it returns without asserting it (planning-time simulation predicts `opus` for gsd-executor and `sonnet` for gsd-verifier under gsd-sdk). If any pinned agent does not return `claude-opus-5-5` in either resolver, go back to Task 1; do not commit. Capture a full before/after table (all 33 gsd agents, both resolvers, after the change) for the SUMMARY.

Commit with an explicit pathspec so nothing else rides along (two-sessions-one-working-tree rule: the peer diffs in ROADMAP.md and STATE.md stay unstaged and untouched): `git add .planning/config.json` (fall back to `git add -f .planning/config.json` only if git refuses), then `git commit -m "<msg>" -- .planning/config.json`. Never `git add -A`, `git add .`, `git commit -a`, `git stash`, or `git checkout` of any other path. Commit subject: `fix(gsd): pin planning/discuss/research agents to Opus 5.5 via model_overrides, retire dead fable routing`. Body, plain hyphens only, no em-dashes: (1) root cause: fable is not a valid GSD tier, so resolveModelInternal silently fell back to the quality-profile default and the 2026-08-10 directive never applied; (2) the 2026-09-23 navigator ruling: 18 agents pinned to claude-opus-5-5 through model_overrides (the only mechanism both gsd-tools and gsd-sdk honor verbatim), models.planning/discuss/research = opus as fallback, execution side unchanged on sonnet; (3) model_profile_overrides deliberately left untouched. End the body with `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.

The SUMMARY must include the resolver table, the commit hash, and these clearly labelled FOLLOW-UPS (report them, do not fix them):
- F1: `model_profile_overrides.claude.sonnet` and `.claude.haiku` are dead config. core.cjs only reads `model_profile_overrides` in step 3, gated on `runtime` being set and not `claude`; this repo has no `runtime`, so gsd-executor gets bare `sonnet`, never `claude-sonnet-5`. Left in place per coordinator instruction.
- F2: The two resolvers disagree. `gsd-sdk query resolve-model` ignores the `models` block, so execution-side agents that gsd-tools sends to `sonnet` (gsd-executor, gsd-debugger, gsd-code-fixer, gsd-doc-writer, gsd-debug-session-manager, gsd-code-reviewer, gsd-eval-auditor, gsd-security-auditor, per the planning-time simulation; confirm against the recorded table) resolve to `opus` under gsd-sdk. Which binary the orchestrator's `gsd_run` actually calls decides the real model for those agents.
- F3: `model_overrides` upkeep. A future GSD update that adds a planning/discuss/research agent will not be in the 18-entry list and will fall back to the bare `opus` alias, not the exact `claude-opus-5-5` id.
- F4: gsd-tools warns on every run that `_models_note` is an unknown config key.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && mdl(){ node -e "let s='';process.stdin.on('data',x=>s+=x).on('end',()=>process.stdout.write(JSON.parse(s).model))"; }; for a in gsd-advisor-researcher gsd-ai-researcher gsd-assumptions-analyzer gsd-codebase-mapper gsd-doc-classifier gsd-doc-synthesizer gsd-domain-researcher gsd-eval-planner gsd-framework-selector gsd-intel-updater gsd-pattern-mapper gsd-phase-researcher gsd-planner gsd-project-researcher gsd-research-synthesizer gsd-roadmapper gsd-ui-researcher gsd-user-profiler; do t=$(node $HOME/.claude/gsd-core/bin/gsd-tools.cjs resolve-model $a 2>/dev/null | mdl); k=$(gsd-sdk query resolve-model $a 2>/dev/null | mdl); echo "$a tools=$t sdk=$k"; test "$t" = claude-opus-5-5 && test "$k" = claude-opus-5-5 || exit 1; done && for a in gsd-executor gsd-verifier; do t=$(node $HOME/.claude/gsd-core/bin/gsd-tools.cjs resolve-model $a 2>/dev/null | mdl); k=$(gsd-sdk query resolve-model $a 2>/dev/null | mdl); echo "$a tools=$t sdk=$k (sdk recorded, not asserted)"; test "$t" = sonnet || exit 1; done && test "$(git show --name-only --format= HEAD)" = ".planning/config.json" && test "$(git show --numstat --format= HEAD)" = "$(printf '24\t4\t.planning/config.json')" && test "$(git log -1 --format=%B | grep -c "$(printf '\342\200\224')")" = 0 && echo COMMIT_OK</automated>
  </verify>
  <done>Each of the 18 pinned agents prints `tools=claude-opus-5-5 sdk=claude-opus-5-5`; gsd-executor and gsd-verifier print `tools=sonnet` with their gsd-sdk value recorded; HEAD is a single-file commit of `.planning/config.json` with 24 insertions and 4 deletions and an em-dash-free message; `COMMIT_OK` prints. The resolver loop was dry-run at planning time against the simulated target config and printed its success marker. `git status --short .planning/ROADMAP.md .planning/STATE.md` still shows the same peer-owned ` M` lines it showed before this plan started. The SUMMARY contains the resolver table, the commit hash and follow-ups F1 to F4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/config.json` -> GSD resolvers (gsd-tools, gsd-sdk) -> every spawned GSD agent | This file decides which model every planner/researcher/executor runs on; a bad value silently changes routing repo-wide |
| This executor -> shared working tree | Another session holds uncommitted edits in the same tree (ROADMAP.md and STATE.md were dirty at planning time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260923-d9l-01 | Tampering | `.planning/config.json` keys outside scope, especially `model_profile_overrides` | mitigate | Parse / mutate / re-serialize on a file proven to round-trip byte for byte; Task 1 gate deep-compares every untouched key and the top-level key order against `git show HEAD:.planning/config.json` and requires an exact 24/4 numstat |
| T-260923-d9l-02 | Tampering | Peer-owned uncommitted diffs (ROADMAP.md, STATE.md) | mitigate | Pre-edit dirty check on config.json; pathspec-limited `git add` and `git commit -- .planning/config.json`; Task 2 gate asserts HEAD's file list is exactly `.planning/config.json`; forbidden: `git add -A`, `git commit -a`, `git stash`, `git checkout` on other paths |
| T-260923-d9l-03 | Elevation of Privilege | Model routing (a value one resolver ignores sends agents to an unintended model) | mitigate | Task 2 asserts `claude-opus-5-5` for all 18 pinned agents under BOTH gsd-tools and gsd-sdk; the known execution-side disagreement is recorded as follow-up F2 rather than hidden |
| T-260923-d9l-04 | Tampering | Agent list drift between planning and execution (a GSD update adds or removes an agent) | mitigate | Task 1 guard and gate re-enumerate `~/.claude/agents/gsd-*.md` against `AGENT_TO_PHASE_TYPE` and fail with the live list on any mismatch instead of silently adapting |
| T-260923-d9l-05 | Repudiation | Dead `model_profile_overrides.claude.*` entries imply routing that never happens | accept | Coordinator scope lock keeps them; recorded as follow-up F1 in the SUMMARY so the mismatch is visible |
</threat_model>

<verification>
- Task 1 gate prints `OK`, `NO_EMDASH`, `NUMSTAT_OK`.
- Task 2 gate prints 18 lines of `tools=claude-opus-5-5 sdk=claude-opus-5-5`, two control lines with `tools=sonnet`, then `COMMIT_OK`.
- `git show HEAD:.planning/config.json` contains no `fable` value in `models` and no `claude.opus` key under `model_profile_overrides`: `node -e "const c=JSON.parse(require('child_process').execSync('git -C /home/jsagi/dev/MindrianOS-Plugin show HEAD:.planning/config.json'));process.exit(Object.values(c.models).includes('fable')||'opus' in c.model_profile_overrides.claude?1:0)"` exits 0.
</verification>

<success_criteria>
- 18 planning/discuss/research agents (enumerated from disk at plan time, no adds or drops versus the approved list) are pinned to `claude-opus-5-5` through `model_overrides`, and both resolvers agree on that.
- `models.planning/discuss/research` are `opus` as the fallback tier; execution/verification/completion unchanged on `sonnet`.
- `model_profile_overrides` and every other untouched key are byte-identical to the pre-change HEAD.
- `_models_note` states the 2026-09-23 ruling and why the fable directive never applied; no em-dashes anywhere in the file or the commit message.
- One atomic commit containing only `.planning/config.json` (24 insertions, 4 deletions); peer-owned working-tree diffs untouched.
- SUMMARY carries the resolver table, commit hash and follow-ups F1 (dead model_profile_overrides entries), F2 (gsd-tools vs gsd-sdk disagreement), F3 (model_overrides upkeep), F4 (`_models_note` unknown-key warning).
</success_criteria>

<output>
Create `.planning/quick/260923-d9l-fix-fable-model-drift-in-planning-config/260923-d9l-SUMMARY.md` when done (directory is gitignored; use `git add -f` if the orchestrator commits it).
</output>
