---
phase: quick-260705-f6k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/doctor.cjs
  - tests/test-doctor-legacy-config-pin-drift.cjs
  - .planning/debug/windows-install-update-ux.md
autonomous: true
requirements: [F11-a]

must_haves:
  truths:
    - "doctor --install-state reports a finding when legacy plugins/config.json pins a mos version that differs from installed_plugins.json"
    - "doctor --install-state --fix reconciles config.json's mos.version to the installed_plugins.json version, after backing up the original"
    - "A machine with NO legacy config.json (fresh install, WSL) produces no finding and no behavior change"
    - "doctor --acceptance fails its install-state point when the drift exists (the F11 recurrence class is now gate-visible)"
  artifacts:
    - path: "scripts/doctor.cjs"
      provides: "readLegacyConfigPin helper + legacy-config-pin-drift finding in checkInstallState + fix branch in performClassIFix"
      contains: "legacy-config-pin-drift"
    - path: "tests/test-doctor-legacy-config-pin-drift.cjs"
      provides: "4-scenario test: agree / disagree+fix / config.json absent / installed_plugins.json absent"
      min_lines: 60
  key_links:
    - from: "checkInstallState findings"
      to: "buildAcceptanceChecklist install-state point"
      via: "the acceptance point passes only on status === 'healthy' (doctor.cjs ~line 2499), so the new finding automatically participates in doctor --acceptance"
      pattern: "legacy-config-pin-drift"
    - from: "performClassIFix"
      to: "~/.mindrian/backups/config.json.<ts>.bak"
      via: "backup-before-write, mirroring the conservative installed_plugins.json repair at doctor.cjs ~2153-2158"
      pattern: "config\\.json\\."
---

<objective>
Add a doctor.cjs check (with --fix) for the F11 bug class: the LEGACY v1-plugin-system file `~/.claude/plugins/config.json` pinning a `mos.version` that disagrees with the MODERN `~/.claude/plugins/installed_plugins.json` (schema v2, key `mos@mindrian-marketplace`). This drift has recurred TWICE on the same real Windows machine (pinned 1.8.2, then 1.15.1, while the modern install had moved on) and poisons command registration while every other subsystem looks healthy.

Purpose: turn the hand-run WSL diagnosis into a permanent acceptance-visible check, closing NEXT item (a) from the F11 finding in .planning/debug/windows-install-update-ux.md.
Output: new Class I finding `legacy-config-pin-drift` + --fix branch in scripts/doctor.cjs, a 4-scenario test file, and an appended FIXED note on F11.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/debug/windows-install-update-ux.md
@scripts/doctor.cjs (read the Class I block, lines ~1681-2192, and the acceptance block ~2460-2540, before editing)
@tests/test-doctor-class-h-fix.cjs (sandbox-HOME test harness to mirror)

Pre-verified facts (planner already confirmed, do not re-derive):
- doctor.cjs has ZERO existing handling of the legacy `plugins/config.json` (grep confirms). This check is net-new.
- `readInstalledPluginsVersion(home)` (doctor.cjs:1718) already resolves the active `mos@mindrian-marketplace` version from installed_plugins.json (handles the namespaced key, array entries, returns 'unknown' on absence). REUSE it, do not reimplement.
- The `install-state` acceptance point in `buildAcceptanceChecklist` (doctor.cjs:2488) calls `checkInstallState` and passes only when `status === 'healthy'`. Any new finding added to checkInstallState therefore ALREADY rides `doctor --acceptance` - that IS the wiring, no new acceptance point needed.
- Backup convention: `~/.mindrian/backups/<name>.<ISO-timestamp>.bak` via mkdirSync recursive + copyFileSync, per the conservative installed_plugins.json repair at doctor.cjs:2153-2158.
- This dev machine (WSL) has no `~/.claude/plugins/config.json`, so the release gate stays green after landing.
- The agentshield skill scans plugin config surfaces (MCP descriptions, hooks, skills, CLAUDE.md, package.json deps), NOT scripts/*.cjs internals - it does not apply to this change.
- Legacy config.json real-world shape (from the F11 diagnosis): `{"mos": {"version": "1.15.1", "enabled": true, "installedAt": "..."}}` - `.mos.version` at top level.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add legacy-config-pin-drift finding + --fix to Class I in scripts/doctor.cjs</name>
  <files>scripts/doctor.cjs</files>
  <action>
Three edits, all inside the existing Class I block (follow its comment style; no em-dashes, hyphens only):

1. New helper next to `readInstalledPluginsVersion` (~line 1735): `readLegacyConfigPin(home)`. Reads `path.join(home, '.claude', 'plugins', 'config.json')`. Returns `{ version, key }` where key is the matched top-level key, or `null` when the file is absent, unparseable, or carries no pin. Key resolution: check top-level `data.mos` then `data['mindrian-os']`; as a defensive fallback also check `data.plugins && data.plugins.mos` (some generations wrap). Extract `String(entry.version)` only if truthy. Wrap the whole body in try/catch returning null (the file is a legacy artifact, absence is the healthy fresh-install state - N/A, never an error).

2. New finding in `checkInstallState` (insert after the step-3 spot-check, ~line 1932): read `readLegacyConfigPin(home)` and `readInstalledPluginsVersion(home)`. Push a finding ONLY when the legacy pin is non-null AND the modern version is not 'unknown' AND they differ (STRING inequality, same convention as the 6-way version-of-record). Finding shape, matching siblings:
   `{ id: 'legacy-config-pin-drift', status: 'warn', recoverable: true, finding: 'legacy plugins/config.json pins mos <legacy> but installed_plugins.json says <modern> -- stale legacy pin can poison command registration (F11); run with --fix to reconcile', legacyVersion, modernVersion, legacyKey }`
   Skip paths (no finding): config.json absent, installed_plugins.json absent/unknown, versions equal. Add a short comment block above the helper citing the F11 recurrence (2026-07-02 pinned 1.8.2, 2026-07-05 pinned 1.15.1) and noting that the install-state acceptance point rides this finding into doctor --acceptance.

3. New branch in `performClassIFix` (~line 2027 loop, before the topology branch): on `f.id === 'legacy-config-pin-drift'`:
   - file = `~/.claude/plugins/config.json`; back it up FIRST to `~/.mindrian/backups/config.json.<new Date().toISOString()>.bak` using the exact mkdirSync-recursive + copyFileSync pattern from lines 2153-2158.
   - Re-read the modern version live via `readInstalledPluginsVersion(home)` (do not trust the stale finding payload); if 'unknown', push a skipped recovery and continue.
   - Parse config.json, set `version` on the entry at `f.legacyKey` (re-resolve the key defensively if absent), write back `JSON.stringify(data, null, 2)`. Do NOT touch any other field (conservative repair: reconcile the pin, leave enabled/installedAt alone).
   - Push recovery `{ class: 'install-state', surface: 'legacy-config-json', action: 'version-reconciled', ok: true, backup_path, target_value, note: 'restart Claude Code (full app restart) to re-register commands' }`; on any throw, push `{ ..., action: 'skipped', detail: err.message }`. All best-effort, never throw out of the fix loop.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/doctor.cjs','utf8'); if(!s.includes('legacy-config-pin-drift')||!s.includes('readLegacyConfigPin')) process.exit(1)" && node scripts/doctor.cjs --install-state --json >/dev/null && node tests/test-doctor-class-i.cjs</automated>
  </verify>
  <done>doctor.cjs carries the helper, the finding, and the fix branch; `doctor --install-state` still exits 0 on this machine (no legacy file here); existing class I test stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test file covering agree / disagree+fix / absent-legacy / absent-modern</name>
  <files>tests/test-doctor-legacy-config-pin-drift.cjs</files>
  <behavior>
    - Test 1 (agree): sandbox HOME where config.json mos.version === installed_plugins.json version -> NO `legacy-config-pin-drift` finding in `doctor --install-state --json` output.
    - Test 2 (disagree): versions differ -> finding with id `legacy-config-pin-drift` present; then `--install-state --json --fix` rewrites config.json's mos.version to the modern version AND a `config.json.*.bak` backup exists under sandbox `~/.mindrian/backups/`; re-run reports no drift finding (fix is convergent).
    - Test 3 (config.json absent): no finding (legacy artifact absent on fresh installs is healthy, not an error).
    - Test 4 (installed_plugins.json absent): no finding (nothing to compare against).
  </behavior>
  <action>
Mirror the tests/test-doctor-class-h-fix.cjs harness: `mkdtempSync` sandbox HOME, spawn `node scripts/doctor.cjs --install-state --json [--fix]` with `HOME` and `USERPROFILE` overridden, parse the JSON report, locate the install-state findings array and assert on presence/absence of the `legacy-config-pin-drift` id ONLY - the sandbox will legitimately produce OTHER findings (record-absent, topology not-found), do not assert overall health. Inspect the actual `--install-state --json` report shape in doctor.cjs while writing and key the lookup off it.

Fixtures per sandbox:
- `.claude/plugins/config.json`: `{"mos": {"version": "<v>", "enabled": true, "installedAt": "2026-04-06T00:00:00Z"}}`
- `.claude/plugins/installed_plugins.json`: `{"version": 2, "plugins": {"mos@mindrian-marketplace": [{"scope": "user", "version": "<v>", "installPath": "<sandbox path>", "lastUpdated": "...", "gitCommitSha": "abc"}]}}` (the namespaced key matters - a plain mos key is the bug F11's recurrence note warns about).
- Seed a minimal `.mindrian/install-state.json` in the --fix sandbox so the record-absent recovery does not spawn session-start during the test (keeps it fast and deterministic); if seeding proves brittle, alternatively assert only on the config.json rewrite + backup and tolerate the extra recovery entries.

Use the house test style: plain assert/chk counters, `console.log` summary line, `process.exit(fail === 0 ? 0 : 1)`, header comment locking the F11 repro (both real drift pairs: 1.8.2 vs 1.15.1, and 1.15.1 vs 1.15.3-beta.1). No em-dashes anywhere in the file.

RED first: write the test before Task 1's edits if executing in TDD order, or at minimum confirm Test 2 fails when the fix branch is commented out mentally (the test must be capable of catching a silent revert - include a source-grep assertion that doctor.cjs contains `legacy-config-pin-drift`, mirroring test-doctor-statusline-prefix-validator.cjs's guard).
  </action>
  <verify>
    <automated>node tests/test-doctor-legacy-config-pin-drift.cjs && node tests/test-doctor-acceptance-self-coverage.cjs</automated>
  </verify>
  <done>All 4 scenarios pass; the acceptance self-coverage aggregator (whose sandbox fixtures have no legacy config.json and must keep passing via the skip path) stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Append FIXED note to F11 and commit</name>
  <files>.planning/debug/windows-install-update-ux.md</files>
  <action>
APPEND a dated note at the END of the F11 section (after the RECURRENCE block, before F12) - do NOT rewrite or reflow any existing line in that file:

"**FIXED 2026-07-05 (quick 260705-f6k):** NEXT item (a) shipped. doctor now detects legacy config.json vs installed_plugins.json version drift as Class I finding `legacy-config-pin-drift`; `--fix` backs up config.json to ~/.mindrian/backups/ then reconciles mos.version to the installed_plugins.json resolved version (conservative repair, other fields untouched). Rides the install-state point of `doctor --acceptance`, so the drift is now release-gate-visible. Test: tests/test-doctor-legacy-config-pin-drift.cjs (agree / disagree+fix / both absent-file skips). Still open from F11: (b) /mos:update reconcile-or-retire of the legacy file, and the F8 loud-restart cue after the fix lands."

Then commit: `git add scripts/doctor.cjs tests/test-doctor-legacy-config-pin-drift.cjs && git add -f .planning/debug/windows-install-update-ux.md` (.planning is gitignored, force-add is the house convention for debug files) with message `fix(doctor): detect + fix legacy config.json vs installed_plugins.json version-pin drift (F11)` - hyphens only, no em-dashes, in both the note and the commit message.
  </action>
  <verify>
    <automated>grep -c "FIXED 2026-07-05 (quick 260705-f6k)" .planning/debug/windows-install-update-ux.md | grep -q "^1$" && ! grep -P "—" .planning/debug/windows-install-update-ux.md</automated>
  </verify>
  <done>F11 section carries exactly one appended FIXED note, existing history untouched, no em-dashes, changes committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor --fix -> user HOME | doctor writes to ~/.claude/plugins/config.json (a file Claude Code itself reads at startup) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f6k-01 | Tampering | performClassIFix legacy-config branch | mitigate | backup-before-write to ~/.mindrian/backups/; only the `version` field of the matched key is mutated; write happens only under explicit --fix |
| T-f6k-02 | Denial of Service | checkInstallState read path | mitigate | all reads try/catch to null/'unknown'; malformed config.json degrades to skip, never throws or blocks the class-flag exit-0 invariant |
</threat_model>

<verification>
- `node tests/test-doctor-legacy-config-pin-drift.cjs` - 4 scenarios green.
- `node tests/test-doctor-class-i.cjs` and `node tests/test-doctor-acceptance-self-coverage.cjs` - no regression (fixtures without config.json exercise the skip path).
- `node scripts/doctor.cjs --install-state` exits 0 on this machine (no legacy file present, Canon Part 8 clean: zero network touch added).
- No em-dashes introduced anywhere (code comments, test, doc note, commit message).
</verification>

<success_criteria>
- The F11 drift class (two confirmed real-world occurrences) is detected by doctor, repaired by --fix with a backup, and blocks doctor --acceptance when present.
- Fresh installs and machines without the legacy file see zero behavior change.
- F11 section documents the fix via an appended note; items (b) and F8 remain explicitly open.
</success_criteria>

<output>
Create `.planning/quick/260705-f6k-add-doctor-cjs-acceptance-check-for-lega/260705-f6k-SUMMARY.md` when done.
</output>
