---
phase: quick-260705-ob7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-skill-mirrors.cjs
  - skills/*/SKILL.md (105 new mirror files)
  - data/connector-coverage-ledger.json
  - data/brain-orchestration-projection.json
autonomous: true
requirements: [QUICK-260705-OB7]

must_haves:
  truths:
    - "Every commands/<name>.md except trending-to-absurd has a byte-identical skills/<name>/SKILL.md mirror (106 mirrors incl. the pilot's help)"
    - "All 107 commands/*.md are byte-for-byte untouched (git diff --stat -- commands/ is empty)"
    - "For every mirrored pair, the frontmatter F-shape declarations (body_shape and hitl_shape) extracted from commands/<name>.md and skills/<name>/SKILL.md match EXACTLY - verified by field-level extraction and diff, not merely implied by the byte-diff"
    - "skills/trending-to-absurd/SKILL.md (pre-existing hand-authored skill, phase 163) is byte-for-byte untouched"
    - "node scripts/build-skill-mirrors.cjs --check exits 0 after the run (idempotent, zero drift incl. against the pilot's skills/help/SKILL.md)"
    - "Full CIRS gate sweep green: build-connector-registry --check, check-shape-declaration --check, check-render-coverage, check-help-coverage, build-orchestration-projection --check, doctor --acceptance"
  artifacts:
    - path: "scripts/build-skill-mirrors.cjs"
      provides: "The generator: mirrors commands/*.md into skills/<name>/SKILL.md with a --check staleness mode"
      contains: "--check"
    - path: "skills/room-passive/SKILL.md"
      provides: "Spot-witness artifact: NOTE this is a COMMAND name colliding with nothing; see must_haves truths for the real full-set check"
    - path: "data/connector-coverage-ledger.json"
      provides: "Regenerated CIRS R1 ledger including ~105 new skill:<name> surfaces"
  key_links:
    - from: "scripts/build-skill-mirrors.cjs"
      to: "commands/*.md"
      via: "read-only enumeration; commands/ is the single source of truth"
      pattern: "commands"
    - from: "skills/*/SKILL.md"
      to: "scripts/build-connector-registry.cjs"
      via: "each mirror carries its command's connector frontmatter verbatim (wired XOR excluded, R1)"
      pattern: "connector"
    - from: "skills/*/SKILL.md"
      to: "scripts/check-shape-declaration.cjs"
      via: "each mirror's body_shape/hitl_shape declarations re-parsed per-surface by the R16 gate's own extraction logic"
      pattern: "hitl_shape"
---

<objective>
Scale the pilot's Windows commands-registration workaround (commit ee2fefac) from 1 command to all of them: build a generator script that mirrors every commands/<name>.md byte-for-byte into skills/<name>/SKILL.md, run it once, regenerate the CIRS data/ artifacts that go stale, get the full gate sweep green, and commit atomically.

Purpose: On a confirmed-affected Windows machine, this plugin's commands/*.md fail to register while skills/ from the SAME plugin load fine (host-side Claude Code bug, independently confirmed via a control test against an unrelated marketplace plugin). The pilot proved the byte-identical mirror mechanic end-to-end for commands/help.md. commands/*.md must stay untouched as the single source of truth because data/command-registry.json, the render-coverage md keyspace, and the help-coverage checker all read commands/*.md specifically.

Output: scripts/build-skill-mirrors.cjs (write + --check modes), 105 new skills/<name>/SKILL.md mirrors (106 total incl. the pilot's help), regenerated data/connector-coverage-ledger.json + data/brain-orchestration-projection.json, one atomic commit. NOT in scope: wiring the generator into pre-commit/CI/release.sh (explicit follow-up), any version bump, any release.
</objective>

<context>
@./CLAUDE.md
@.planning/quick/260705-nwr-pilot-migrate-commands-help-md-to-skills/260705-nwr-PLAN.md
@.planning/quick/260705-nwr-pilot-migrate-commands-help-md-to-skills/260705-nwr-SUMMARY.md
@scripts/build-connector-registry.cjs

**Pre-verified facts (confirmed against this repo on 2026-07-05 during planning; do NOT re-research):**

1. **Pilot state holds:** `diff commands/help.md skills/help/SKILL.md` is empty right now. The generator run must produce a ZERO diff against skills/help/SKILL.md (idempotency proof), not a duplicate.

2. **CRITICAL - name collision the pilot never hit:** `skills/trending-to-absurd/SKILL.md` ALREADY EXISTS and is a hand-authored, DIVERGENT skill (phase 163 auto-invocable Visionary Innovation Companion; 99 lines with explicit invocation-gating description, hitl_stages, orchestrator pointer) - it is NOT a mirror of commands/trending-to-absurd.md (115 lines, different frontmatter). Overwriting it would destroy a shipped surface. Decision (stated here, not silently applied): the generator carries an explicit `SKIP_LIST = ['trending-to-absurd']` and NEVER touches skip-listed names. This still satisfies the Windows goal for that name: per the pilot's verified docs facts, the skill takes precedence over the same-named command, so /mos:trending-to-absurd is ALREADY served by the existing skill on every machine - including the affected Windows box, where skills/ load fine. No other collision exists: of the 15 existing skills/ dirs (MOSDeckEngine, brain-connector, client-discovery-interview, context-engine, conversation-mode, help, intelligence-orchestrator, larry-personality, mullins-scaffold, mva-pipeline, pws-methodology, room-passive, room-proactive, trending-to-absurd, ui-system), only `help` (pilot mirror, byte-identical) and `trending-to-absurd` (hand-authored, divergent) share a name with a commands/*.md file. Verified by loop-checking every skills/ dir name against commands/.

3. **Tooling reads commands/ only (re-confirmed at line level):** `scripts/build-command-registry.cjs` scans commands/*.md only (header + buildRegistry ~line 224). `scripts/check-help-coverage.cjs` checks commands/*.md help_jtbd only. `scripts/build-render-coverage.cjs` builds its md keyspace from commands/*.md only (~line 333, surface = 'commands/' + f) and its header explicitly states the connector ledger is a different keyspace it never touches. So: commands/ untouched means data/command-registry.json and data/render-coverage-registry.json do NOT change and check-render-coverage + check-help-coverage pass with zero regeneration.

4. **CIRS gate impact at scale (same categories the pilot proved, ~105x the volume):**
   - `build-connector-registry.cjs` walks skills/*/SKILL.md; its `--check` byte-compares tracked data/connector-registry.json AND data/connector-coverage-ledger.json and exits non-zero on stale. Adding ~105 skill surfaces makes the ledger STALE - run the default WRITE mode, then --check. Every mirror carries its command's connector block verbatim (byte copy), and every command already passes the born-wired gate today, so every new skill surface classifies wired XOR excluded, never gap. The pilot only exercised an EXCLUDED surface (help); wired-command mirrors will add wired skill surfaces to the ledger - expected and correct. data/connector-registry.json did not change in the pilot ("skills are not connectors"); if it changes this time, inspect the diff and commit it only if the generator legitimately rewrote it.
   - `build-orchestration-projection.cjs --check` goes STALE (pilot: one new node `skill:help`, kind: skill). Expect ~105 new skill:<name> nodes this time. Regenerate (write mode), inspect that the diff is ONLY added skill nodes, then --check.
   - `check-shape-declaration.cjs` REQUIRES the `--check` flag (pilot deviation #1: bare invocation prints usage and exits 2). Declared count is enumerated from disk at run time, never a frozen literal (CLAUDE.md Part 11) - it will jump by ~105; do NOT treat the count delta as a failure. The Phase 209-03 body/grant predicates are scoped to klass === 'command' and skip skills. Advisory (WARN) since Phase 210, but any NEW violation or warning naming a skills/<name>/SKILL.md mirror is a failure for this task. Because this gate walks skills/*/SKILL.md with its OWN per-surface frontmatter parser (collectSurfaces), a clean run is an INDEPENDENT confirmation that the declared F-shapes on the mirrors parse to the same declarations as their source commands - it complements, but does not replace, the explicit field-level shape-parity check in Task 2.
   - `doctor.cjs --acceptance` runs AFTER the commit (pilot: its clean-tree gate correctly fails pre-commit).

5. **Byte-identity is the whole contract:** unknown custom frontmatter fields (help_jtbd, body_shape, hitl_shape, hitl_why, body_shape_detail, serves_jtbd, teaching, ui_reference, connector, canon_parts, kind, frameworks, produces, inputs) are tolerated in SKILL.md - validated by the pilot and by this repo's existing skills. The generator MUST NOT translate, drop, rewrite, or annotate anything - no "GENERATED" header comment either, since that would break byte-identity and the diff-based verification. 7 of 107 commands omit a frontmatter `name:` field; harmless, because a skill's command name comes from its DIRECTORY name and `name:` is display-only.

6. **Generator conventions (per CLAUDE.md):** CJS only, no TypeScript, no Commander/yargs - parse process.argv with the switch/includes pattern used by build-connector-registry.cjs. Default run = write mode; `--check` = compare-only, exit non-zero on any missing or divergent mirror (mirroring build-connector-registry's --check convention). No new npm dependencies (fs/path only).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/build-skill-mirrors.cjs (generator with write + --check modes)</name>
  <files>scripts/build-skill-mirrors.cjs</files>
  <action>
    Create scripts/build-skill-mirrors.cjs following the repo's generator conventions (CJS, fs/path only, process.argv.includes flag parsing like scripts/build-connector-registry.cjs, a header comment block explaining WHAT it generates and WHY commands/ stays the source of truth - cite the Windows commands-registration host bug and the pilot commit ee2fefac).

    Behavior, WRITE mode (default, no flags): enumerate commands/*.md sorted; for each basename `name` (strip .md): if `name` is in the hardcoded `SKIP_LIST = ['trending-to-absurd']` (with a comment explaining it is a pre-existing hand-authored phase-163 skill that must never be clobbered, and that the skill already shadows the same-named command per documented skill-over-command precedence), skip it; else ensure skills/<name>/ exists (mkdir recursive) and write skills/<name>/SKILL.md as an exact byte copy of the command file (read Buffer, write Buffer - no string round-trip, no newline normalization, no added header). Track and print a summary line: created N, unchanged N (target existed and was already byte-identical), overwritten N (target existed and differed - allowed regeneration semantics, but for THIS run must be 0), skipped N. Exit 0.

    Behavior, --check mode: same enumeration and SKIP_LIST; for each non-skipped name, fail if skills/<name>/SKILL.md is missing or byte-differs from commands/<name>.md; print every failing name; exit non-zero if any failed, else print an OK line with the mirror count and exit 0. Never write anything in --check mode.

    The script must NEVER read, write, or touch any file under commands/ except read-only, must never delete anything, and must never touch skills/ dirs that do not correspond to a commands/*.md basename (the 13 standalone skills like brain-connector, ui-system are out of its universe entirely).
  </action>
  <verify>
    <automated>node scripts/build-skill-mirrors.cjs --check; test $? -ne 0 && node -e "process.exit(0)" && echo CHECK-MODE-FAILS-CORRECTLY-PRE-RUN</automated>
  </verify>
  <done>Script exists; `node scripts/build-skill-mirrors.cjs --check` (pre-generation) exits non-zero and lists exactly the 105 missing mirrors (107 commands minus help which exists and matches, minus trending-to-absurd which is skip-listed); nothing on disk has been written by --check.</done>
</task>

<task type="auto">
  <name>Task 2: Run the generator once and prove byte-identity, F-shape parity, idempotency, and source-untouched</name>
  <files>skills/*/SKILL.md (105 new files)</files>
  <action>
    Run `node scripts/build-skill-mirrors.cjs` (write mode) exactly once. Expected summary: created 105, unchanged 1 (help - this is the zero-diff idempotency proof against the pilot's committed file), overwritten 0, skipped 1 (trending-to-absurd). If overwritten is not 0 or skipped is not exactly trending-to-absurd, STOP and report - an assumption is wrong.

    Then prove the four invariants:
    1. Byte-identity of every mirror: loop `for f in commands/*.md; do n=$(basename "$f" .md); [ "$n" = "trending-to-absurd" ] && continue; cmp -s "$f" "skills/$n/SKILL.md" || echo "DIVERGES: $n"; done` and require zero DIVERGES lines.
    2. F-SHAPE DECLARATION PARITY (explicit, named check - the navigator's ask; NOT satisfied by pointing at the byte-diff): for EVERY commands/<name>.md, extract the frontmatter `body_shape:` and `hitl_shape:` field lines from BOTH the command and its mirror and diff them field-for-field. Extraction must be scoped to the frontmatter block only (between the opening and closing `---` fences), never the body - define `shapes() { awk '/^---$/{c++; next} c==1' "$1" | grep -E '^(body_shape|hitl_shape):'; }` then loop over all 106 non-skipped pairs comparing `shapes commands/<name>.md` against `shapes skills/<name>/SKILL.md`, printing `SHAPE-DRIFT: <name>` on any mismatch, and require zero drift lines. Report the pair count checked and how many pairs declared each field (some commands declare hitl_stages instead of hitl_shape - those extract as empty-matches-empty here, and their full multi-line declarations are independently confirmed by check-shape-declaration.cjs --check in Task 3, whose collectSurfaces parser walks skills/*/SKILL.md per-surface). For trending-to-absurd (skip-listed, hand-authored, divergent BY DESIGN), do not compare - note it explicitly in the report instead of silently omitting it.
    3. Source untouched: `git diff --stat -- commands/` and `git status --porcelain commands/` both empty (all 107 command files byte-for-byte unchanged - the core reversibility property; rollback of this whole task is deleting the generated mirrors, nothing else).
    4. Hand-authored skill untouched: `git status --porcelain skills/trending-to-absurd/` empty.

    Then re-run `node scripts/build-skill-mirrors.cjs --check` and require exit 0 (post-run steady state), and run write mode a second time and require created 0 / unchanged 106 / overwritten 0 (true idempotency).

    Do NOT edit any generated SKILL.md by hand. Do NOT touch data/ yet - that is Task 3.
  </action>
  <verify>
    <automated>node scripts/build-skill-mirrors.cjs --check && shapes() { awk '/^---$/{c++; next} c==1' "$1" | grep -E '^(body_shape|hitl_shape):'; }; drift=0; for f in commands/*.md; do n=$(basename "$f" .md); [ "$n" = "trending-to-absurd" ] && continue; a=$(shapes "$f"); b=$(shapes "skills/$n/SKILL.md"); [ "$a" = "$b" ] || { echo "SHAPE-DRIFT: $n"; drift=1; }; done; test $drift -eq 0 && test -z "$(git status --porcelain commands/)" && test -z "$(git status --porcelain skills/trending-to-absurd/)" && ls skills/*/SKILL.md | wc -l | grep -qx "120" && echo MIRRORS-AND-SHAPES-OK</automated>
  </verify>
  <done>106 byte-identical mirrors exist (105 new + pilot's help unchanged); the explicit per-pair body_shape/hitl_shape extraction diff reports zero SHAPE-DRIFT across all 106 pairs; skills/*/SKILL.md totals 120 (106 mirrors + 14 standalone/hand-authored incl. trending-to-absurd); commands/ has zero working-tree changes; --check exits 0; a second write run is a no-op.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate CIRS artifacts, run the full gate sweep, commit atomically</name>
  <files>data/connector-coverage-ledger.json, data/brain-orchestration-projection.json</files>
  <action>
    Regenerate the two artifact categories the pilot identified as going stale, inspecting each diff before moving on:
    1. `node scripts/build-connector-registry.cjs` (write mode). Inspect `git diff --stat data/` then the ledger diff: it must consist ONLY of new skill:<name> surfaces (wired XOR excluded per each command's copied connector block, never gap) plus the generator's own count fields. If ANY pre-existing surface changed state, STOP and report. If data/connector-registry.json also changed (pilot: it did not - skills are not connectors), inspect it; commit it only if the generator legitimately rewrote it.
    2. `node scripts/build-orchestration-projection.cjs` (write mode - the pilot proved --check reports STALE until this runs). Inspect the diff: only ADDED skill:<name> nodes (kind: skill), no existing node or edge modified. data/orchestration-command-ledger.json must NOT change (walks commands/ only).

    Then the full gate sweep, all required clean (note the --check flag on shape-declaration - pilot deviation #1):
    1. `node scripts/build-connector-registry.cjs --check`
    2. `node scripts/check-shape-declaration.cjs --check` (declared count will jump by ~105; enumerated from disk, not a frozen literal - the requirement is OK with zero violations and zero NEW warnings naming any skills/<name>/SKILL.md mirror. This gate re-parses body_shape/hitl_shape/hitl_stages per-surface from skills/*/SKILL.md via its own collectSurfaces logic, so a clean run is the second, independent confirmation of Task 2's F-shape parity check)
    3. `node scripts/check-render-coverage.cjs` (must pass with zero regeneration - md keyspace is commands/*.md only, untouched)
    4. `node scripts/check-help-coverage.cjs` (must pass untouched - commands/ unchanged)
    5. `node scripts/build-orchestration-projection.cjs --check`
    6. `node scripts/build-skill-mirrors.cjs --check` (the new gate, proving the committed tree is mirror-consistent)

    If any gate fails in a way that requires changing files beyond the generator script, the generated mirrors, and the regenerated data/ artifacts, STOP and report it as a decision point (scope expansion needs user approval) - do not fix inline.

    Commit atomically: `git add scripts/build-skill-mirrors.cjs skills/ data/connector-coverage-ledger.json data/brain-orchestration-projection.json` (plus any other data/ file a generator legitimately rewrote; `git add skills/` is safe because Task 2 proved skills/trending-to-absurd/ has no working-tree changes), message: `feat(skills): mirror all commands into skills/ via build-skill-mirrors.cjs (Windows commands-registration workaround, scales pilot ee2fefac)`. Do NOT run scripts/release.sh, do NOT bump any version, do NOT wire the generator into pre-commit/CI (explicit follow-up).

    After the commit, run `node scripts/doctor.cjs --acceptance` (its clean-tree gate requires the commit to have landed) and require all points passed.
  </action>
  <verify>
    <automated>node scripts/build-connector-registry.cjs --check && node scripts/check-shape-declaration.cjs --check && node scripts/check-render-coverage.cjs && node scripts/check-help-coverage.cjs && node scripts/build-orchestration-projection.cjs --check && node scripts/build-skill-mirrors.cjs --check && node scripts/doctor.cjs --acceptance && git log -1 --stat | grep -q "build-skill-mirrors.cjs" && test -z "$(git status --porcelain)" && echo GATES-AND-COMMIT-OK</automated>
  </verify>
  <done>All six gates plus doctor --acceptance pass; one atomic commit contains the generator, all 105 new mirrors, and the regenerated data/ artifacts; working tree is clean; commands/ appears nowhere in the commit's file list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Mechanical byte-copy of already-shipped command surfaces onto the skills/ loading path; no new network endpoint, auth path, or input surface. The Brain boundary (Canon Part 8) is untouched - mirrors carry the same connector blocks that already passed the born-wired gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ob7-01 | Tampering | scripts/build-skill-mirrors.cjs overwriting hand-authored skills | mitigate | Hardcoded SKIP_LIST + Task 2's git-status proof that skills/trending-to-absurd/ is untouched; write mode reports overwritten count, required 0 this run |
| T-ob7-02 | Tampering | drift between commands/ and mirrors post-task | accept (this task) | --check mode exists and is the follow-up's pre-commit/CI hook; staleness prevention is explicitly out of scope here |
</threat_model>

<verification>
- `node scripts/build-skill-mirrors.cjs --check` exits 0 on the committed tree (106 byte-identical mirrors, trending-to-absurd skip-listed).
- **F-shape declaration parity (explicit, named):** per-pair frontmatter extraction of `body_shape:` and `hitl_shape:` from every commands/<name>.md vs its skills/<name>/SKILL.md mirror shows zero drift across all 106 pairs (Task 2 invariant 2), AND `check-shape-declaration.cjs --check` passes clean - its collectSurfaces parser independently re-derives every mirror's declared shapes from skills/*/SKILL.md, confirming no declared F-shape shifted. Two independent checks, neither merely inferred from the byte-diff.
- `git diff <pre-task-HEAD> HEAD -- commands/` is empty (all 107 sources byte-for-byte untouched).
- Gate sweep clean: build-connector-registry --check, check-shape-declaration --check (zero violations, zero new mirror-naming warnings), check-render-coverage, check-help-coverage, build-orchestration-projection --check, doctor --acceptance.
- Ledger diff contains only added skill:<name> surfaces; projection diff contains only added skill nodes.
- Exactly one atomic commit; no version bump, no release, no pre-commit/CI wiring.
</verification>

<success_criteria>
- Every /mos: command is now reachable via the skills/ loading path on the affected Windows machine: 105 new mirrors + the pilot's help + the pre-existing trending-to-absurd skill = full 107-name coverage.
- Every mirror's declared F-shapes (body_shape/hitl_shape) are proven identical to its source command by explicit field-level extraction, independently corroborated by the R16 shape-declaration gate.
- commands/ remains the untouched single source of truth; all internal tooling (command-registry, render-coverage keyspace, help-coverage) is provably unaffected.
- Rollback is a single revert (delete generated mirrors + generator + regenerated data/; zero source files changed).
- The generator's --check mode is ready for the follow-up task that wires staleness prevention into pre-commit/CI/release.
</success_criteria>

<output>
This is a quick task; no phase SUMMARY.md. On completion, report inline: the commit hash, the generator's write-mode summary counts (expect created 105 / unchanged 1 / overwritten 0 / skipped 1), the F-shape parity result (pairs checked, per-field declaration counts, zero drift), and the gate sweep results.

**Closing note (non-blocking, per task constraints):** recommend a human spot-check of a small sample (3-5) of newly mirrored commands on the affected Windows machine - e.g. `/mos:ignite`, one wired connector command, one of the 7 no-name-field commands - before this workaround is considered fully closed. This does NOT block the commit: skills/ loading on that machine was already independently confirmed by the pre-pilot control test, and the pilot validated the mirror mechanic end-to-end for one file. Note also that the pilot's own blocking Windows checkpoint (quick task 260705-nwr Task 3) is still formally open and can be folded into the same spot-check session. Follow-up task (out of scope here): wire `build-skill-mirrors.cjs --check` into pre-commit, CI, and scripts/release.sh so command edits cannot silently strand stale mirrors.
</output>
