---
quick_id: 260806-d0x
status: planned
---

# Quick Task 260806-d0x: grade-grant reviewer-panel examination mode (BONO-substrate reuse)

Full design already specified by the navigator (pasted verbatim into the task brief this
session) -- this PLAN records the execution breakdown against that locked design, not a
fresh design pass. Canon Part 7 (reuse before build): the fan-out/debate substrate
(`runCellFanout`, `runDebate`, `buildSteps`, `buildArtifactPairs`, `cellsForHat`,
`consolidatorForHat`, `assertHeterogeneity`, `lensDescriptor`, `wireAccept`, `wireReject`)
is consumed as-is, never re-implemented.

## Task 1 -- lib/core/bono/reviewer-governance.cjs (new)

`REVIEWER_GOVERNANCE` frozen 7-key map (eligibility/process/budget/legal/reporting/market/ip),
`governanceForCategory`, `enforceReviewerGovernance(category, argument)` (mirrors
`enforceGovernance`'s contract), `composeReviewerGovernedSeams(opts)` (mirrors
`composeGovernedSeams` but swaps `enforceGovernance(hat,...)` for
`enforceReviewerGovernance(category,...)`; does NOT carry forward the KAC-first onStep flag
per design section (c)). Re-exports (not duplicates) `assertHeterogeneity` + `lensDescriptor`
from `hat-governance.cjs`.

files: lib/core/bono/reviewer-governance.cjs
verify: node -e "require('./lib/core/bono/reviewer-governance.cjs')" exits 0
done: module exports REVIEWER_GOVERNANCE (7 frozen keys), governanceForCategory,
  enforceReviewerGovernance, composeReviewerGovernedSeams, assertHeterogeneity, lensDescriptor

## Task 2 -- lib/core/eureka/grade-grant-examine.cjs (new)

`buildReviewerSlots(rubric)`, `runReviewerFanout(rubric, sourceCtx, opts)` (batches
`CATEGORY_VALUES` into <=5-sized groups across two `runCellFanout` calls, concatenates
cells/dropped/plan -- the N2 fix), `defaultReviewerDispatchCell(cell, ctx)` (mechanical,
no LLM), `reviewerCellsToFindings(cells, rubric)` (pure reshape into scoreApplication's
findings[] shape), `buildReviewerDebateOptions(cells, hypothesis, rubric, opts)`.

files: lib/core/eureka/grade-grant-examine.cjs
verify: node -e "require('./lib/core/eureka/grade-grant-examine.cjs')" exits 0
done: batching never exceeds FUTURES_FANOUT_CAP per runCellFanout call; reviewerCellsToFindings
  output round-trips through scoreApplication unchanged

## Task 3 -- lib/core/eureka/grade-grant.cjs (additive only)

ONE new export `deriveRulingVerb(verdict, rubric)`: eligibility-all-evidenced/asserted +
score>=80 + zero absents -> supported; any eligibility criterion absent -> rejected +
escalation record; else score>=50 -> refined; else undecided. Byte-identical otherwise.

files: lib/core/eureka/grade-grant.cjs
verify: bash tests/run-all-tests.sh not required -- run node tests/test-grade-grant.cjs, 18/18 must stay green
done: deriveRulingVerb exported, 4 branches covered by new tests, existing 18 checks untouched

## Task 4 -- agents/grant-reviewer.md (new)

Sibling to persona-analyst.md, NOT a repurpose. Two roles: (a) per-category reviewer cell
(reads ONLY its category's criteria + the room section(s)/draft slice they map to,
adversarial "would a committee member reading only this section accept it"), (b) ruling
consolidator (reads across all 7 categories' findings, renders the holistic verdict --
closest living analog to the Notion "Tnufa Tech Assessment agent" stub). allowed-tools:
Read, Glob ONLY (no WebSearch/WebFetch/Brain -- local-only per N3/D5).

files: agents/grant-reviewer.md
verify: frontmatter parses; allowed-tools is exactly [Read, Glob]
done: two-role structure documented, Cell Return Shape section present, no em-dashes

## Task 5 -- tests/test-reviewer-governance.cjs + tests/test-grade-grant-examine.cjs (new)

Mirror tests/test-223-hat-governance.cjs's check() harness and tests/test-grade-grant.cjs's
real-room.db-round-trip convention. Cover: 7 REVIEWER_GOVERNANCE keys well-formed; all 7
enforceReviewerGovernance branches (eligibility hard_gate flag present but not enforced here,
process/legal/reporting cite_or_retract, budget reconciliation_required, market/ip
disconfirming_first); composeReviewerGovernedSeams shape + sync deriveFn; batched
runReviewerFanout never exceeds FUTURES_FANOUT_CAP per call and covers all 7 categories
combined; reviewerCellsToFindings parity against scoreApplication; deriveRulingVerb's 4
branches including the eligibility hard-gate.

files: tests/test-reviewer-governance.cjs, tests/test-grade-grant-examine.cjs
verify: node tests/test-reviewer-governance.cjs && node tests/test-grade-grant-examine.cjs
done: both exit 0, PASS lines printed

## Task 6 -- commands/grade-grant.md + skills/grade-grant/SKILL.md (additive)

Frontmatter: hitl_shape -> hitl_stages (file-verdict F.8 unconditional, hypothesis-confirm
F.1 panel-mode-only, ruling F.5 panel-mode-only), mirroring bono.md's hitl_stages shape.
Session Flow step 4 grows an opt-in [Single read] (default) vs [Reviewer panel examination]
sub-choice. SKILL.md is the mirrored copy (build-skill-mirrors.cjs owns byte-parity;
regenerate, never hand-edit both independently out of sync).

files: commands/grade-grant.md, skills/grade-grant/SKILL.md
verify: node scripts/check-shape-declaration.cjs (advisory, must not newly fail);
  node scripts/build-skill-mirrors.cjs --check after mirror regeneration
done: hitl_stages present and CIRS-shaped, panel-mode opt-in documented, no em-dashes

## Task 7 -- registries + full test pass + commit

Regenerate: build-connector-registry.cjs, build-orchestration-projection.cjs,
build-skill-mirrors.cjs (only if commands/grade-grant.md's connector block or skill mirror
actually changed -- the connector block itself is unchanged, only hitl_stages, so confirm
before assuming a regen is needed). Run tests/test-grade-grant.cjs (18/18),
tests/test-reviewer-governance.cjs, tests/test-grade-grant-examine.cjs. Stage files BY NAME.
Commit with Co-Authored-By trailer.

files: (registries only if a generator's own --check reports drift)
verify: all three test files exit 0; git status shows only intended files staged
done: single atomic commit, SUMMARY.md + STATE.md updated
