---
phase: quick/260728-8bw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/minto-debounce-consumer-dead-end.md
  - /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-07-28-minto-debounce-consumer-dead-end/2026-07-28-minto-debounce-consumer-dead-end.md
autonomous: true
requirements: [QUICK-260728-8bw]

must_haves:
  truths:
    - "A reader of the RCA can name the producer, every discard site, and the missing consumer, each with a file:line citation they can re-run."
    - "Every factual claim in the RCA has a matching Evidence entry naming the exact command that produced it."
    - "The RCA gives ONE reasoned recommendation among the candidate resolutions, not an unranked menu."
    - "The finding lives in both homes and each home links to the other."
    - "The RCA stays OPEN (status gathering or investigating). No source file under scripts/ or lib/ is modified."
  artifacts:
    - path: ".planning/debug/minto-debounce-consumer-dead-end.md"
      provides: "The executable half of the finding, written to docs/RCA-TEMPLATE.md"
      contains: "## Source-of-Truth Preamble"
    - path: "/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-07-28-minto-debounce-consumer-dead-end/2026-07-28-minto-debounce-consumer-dead-end.md"
      provides: "The durable reasoning trail per CLAUDE.md Dev-Research Compositing"
      contains: "minto-debounce-consumer-dead-end"
  key_links:
    - from: ".planning/debug/minto-debounce-consumer-dead-end.md"
      to: "the rethinking-mindrianos research entry"
      via: "Non-Code Follow-ups compositing line"
      pattern: "rethinking-mindrianos"
    - from: "the rethinking-mindrianos research entry"
      to: ".planning/debug/minto-debounce-consumer-dead-end.md"
      via: "Cross-link header block"
      pattern: "\\.planning/debug/minto-debounce-consumer-dead-end\\.md"
---

<objective>
File the RCA for the MINTO debounce consumer dead end: the guardian faithfully enqueues a regen intent on a critical MINTO violation, two separate stop-path sites drain that queue and throw the result away, and the consumer the debouncer's own docstring names (scripts/intent-classifier.cjs) does not reference the debouncer at all. Net effect: a critical MINTO violation is queued, then silently vacuumed. No regen has ever fired.

This is the same SHAPE as `.planning/debug/hedge-fold-has-no-production-trigger.md`: a producer wired at one end, inert at the other, invisible because the silent path is indistinguishable from a healthy idle path.

Purpose: get the finding into both homes (repo + rethinking room) with citations a coding agent can act on, without absorbing the fix.
Output: one RCA file under `.planning/debug/`, one dated mirror entry under the rethinking room, cross-linked both ways, both committed.

SCOPE HARD STOP: this is a FILING task. Do not modify any file under `scripts/`, `lib/`, `hooks/`, or `tests/`. Do not fix the defect. Status stays OPEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@docs/RCA-TEMPLATE.md
@.planning/debug/hedge-fold-has-no-production-trigger.md

Read the hedge-fold RCA as the STRUCTURAL AND QUALITY BAR, not as boilerplate to copy. What to match:
- A filled Source-of-Truth Preamble naming the exact sha and plugin version.
- Symptoms that name the SILENT correct-looking path as the reason the defect stayed invisible.
- An `Eliminated` section that pre-empts the obvious wrong hypotheses before a future reader wastes a cycle on them.
- Evidence entries that each name the literal command run, so any reader can re-run them.
- A `Technical Root Cause` that states the broken CONTRACT, not just the broken line.
- `Required Code Changes` that ends in ONE recommendation with reasoning, plus the risk to weigh.
- A closing section for any second, adjacent defect found by the same audit but deliberately not folded into the main finding.

Project rules that bind this artifact:
- CLAUDE.md "QA and RCA Reporting": `.planning/` is gitignored, so commit with `git add -f`. One defect uses `kind: rca`.
- CLAUDE.md "Dev-Research Compositing": the finding is filed in BOTH homes, cross-linked. A room entry that never lands in a repo artifact, or a repo artifact that only says "per the room's research", both count as incomplete.
- CLAUDE.md Conventions: NO em-dashes anywhere. Hyphens or commas.
- Two git repos are involved. `/home/jsagi/dev/MindrianOS-Plugin` owns the RCA. `/home/jsagi` (the home directory is itself a git repo) owns `~/MindrianRooms/rethinking-mindrianos/`. They commit separately.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every cited claim against the working tree and capture the evidence set</name>
  <files>none (read-only verification, no file writes)</files>
  <action>
Verify each claim below by reading the actual code. Do NOT accept any of them secondhand. Record, for each, the literal command run and the concrete fact found; this becomes the RCA's Evidence section verbatim.

Claims to verify (a pre-pass found these true unless noted, re-prove them):

1. PRODUCER. `scripts/feynman-minto-guardian.cjs` enqueues a regen on a critical violation. Expected shape: `enqueueRegenSafe` around :254-267, the `dbnc.enqueue(roomDir, section, reason)` call around :262, called from `runSessionStart` around :277 under a `result.severity === 'critical'` guard, setting `action = 'enqueued_regen'`. `DEBOUNCER_PATH` resolved around :51. Confirm the exact lines and function names.

2. PRODUCER IS LIVE. `scripts/session-start` around :1153 backgrounds `node "${PLUGIN_ROOT}/scripts/feynman-minto-guardian.cjs" session-start "${ROOM_DIR}"`. Confirm it is a real invocation on the live hook path, not a commented or gated one.

3. DISCARD SITE A. `lib/mcp/stop-gate-handler.cjs` `_closeOutMintoDrain` around :124-133 calls `debouncer.drain(roomDir, { timeoutMs: 1500, olderThanMs: 0 })` around :128 and does NOT assign the return value; the function returns a bare `true`. NOTE: the reported range was :124-131; confirm the real block boundary and cite what you find.

4. DOCSTRING PROMISE. `scripts/minto-debouncer.cjs` names its downstream consumers around :32-36, including "88-05 intent-classifier (drain at UserPromptSubmit for items > 30s old)", and around :15 names "intent-classifier drain" as a concurrent producer. NOTE: the reported range was :33-35; cite the real one.

5. MISSING CONSUMER. `scripts/intent-classifier.cjs` never references the debouncer. Prove with `grep -n "minto-debouncer\|debounc" scripts/intent-classifier.cjs` and record that it returns nothing.

6. UNTESTED BY DESIGN. `lib/memory/feynman-minto-guardian.test.cjs` Test 1 (around :148-165) asserts only that `.mindrian/minto-queue.json` contains the section entry. It never asserts anything drains it into a regen. Confirm, and check Test 2 does not close the gap either.

Then extend the audit. The pre-pass surfaced these; re-prove each and add anything further you find:

7. SECOND DISCARD SITE. `scripts/on-stop` around :345 runs the CLI form `node "${SCRIPT_DIR}/minto-debouncer.cjs" drain "${ROOM_DIR}" --timeout=1500 --older-than=0 >/dev/null 2>&1 || true`, discarding stdout. Its own header around :6 and :341-344 describes the drain as meaningful work. So the vacuum has TWO sites, not one, and the reported description understates the blast radius.

8. NO REGEN CONSUMER EXISTS ANYWHERE. Run `grep -rln "regenerateMinto\|regen-minto\|minto-regen\|regenMinto" --include=*.cjs --include=*.js --include=*.sh --include=*.md . | grep -v node_modules` and record the result. Also enumerate every caller of `debouncer.drain` repo-wide and classify each as production or test. Identify whether `scripts/vault-section-minto-generator.cjs` is the only surface that could regenerate a section MINTO, and whether anything on the drain path calls it. This is decisive for the resolution options: if no regen entry point exists, "act on the drained array" is not a one-line fix.

9. USER-VISIBLE FALSE SUCCESS. `scripts/on-stop` around :516 builds `"session snapshot saved, ${SECTION_COUNT} sections drained, health ..."` and the mindrian-core path around :110 emits `'... ' + sections + ' sections drained'` where `sections` comes from `business.sections`. Trace `business.sections` back through `lib/mcp/stop-gate-handler.cjs` (around :215 and :241) and confirm whether it is a folder-memory snapshot SECTION COUNT rather than a drained-entry count. If confirmed, the surface reports drained work that never happened, which is the same confident-success-over-empty-result shape Phase 233 was about.

10. SECOND-ORDER: A HEALTH ALARM THAT CANNOT FIRE. `lib/memory/validators/queue-health.cjs` (thresholds around :64 and :78, `action_hint: halt_enqueue_until_drained`) warns at 500 entries and errors at 1000. Reason about whether an unconditional `olderThanMs: 0` drain at every session stop makes those thresholds structurally unreachable. State the conclusion as reasoning, and label it as reasoning rather than as a measured fact unless you measure it.

11. WIDER BLAST RADIUS. `lib/memory/post-write-triple.test.cjs` around :249-250 asserts the post-write hook invokes `minto-debouncer.cjs enqueue`. Confirm the post-write hook is a SECOND producer, so the discard swallows every post-write regen intent, not only guardian criticals.

Also capture for the Preamble: `git rev-parse --short HEAD`, `git log -1 --format=%s`, the `version` from `.claude-plugin/plugin.json`, and confirm the working tree is clean enough that the citations are reproducible.

If ANY claim above turns out wrong or stale, the RCA reports what the code ACTUALLY shows and says so explicitly. Do not reproduce an error to stay consistent with this plan.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && grep -n "dbnc.enqueue" scripts/feynman-minto-guardian.cjs && grep -n "debouncer.drain" lib/mcp/stop-gate-handler.cjs && grep -c "minto-debouncer" scripts/intent-classifier.cjs; echo "intent-classifier hit count above must be 0"</automated>
  </verify>
  <done>Every claim 1-11 is either confirmed with an exact file:line and a re-runnable command, or corrected with the same rigor. The Preamble facts (sha, subject, plugin version) are captured. No file has been written yet.</done>
</task>

<task type="auto">
  <name>Task 2: Write the RCA to .planning/debug/ and commit it</name>
  <files>.planning/debug/minto-debounce-consumer-dead-end.md</files>
  <action>
Write `.planning/debug/minto-debounce-consumer-dead-end.md` following `docs/RCA-TEMPLATE.md` section-for-section, at the rigor of `.planning/debug/hedge-fold-has-no-production-trigger.md`.

Slug: keep `minto-debounce-consumer-dead-end`. Only change it if Task 1 invalidated the CORE claim (producer enqueues, nothing consumes). The widened blast radius from Task 1 items 7 through 11 belongs in Scope and Impact, not in the filename.

Frontmatter: `status: gathering`, `kind: rca`, `trigger: "minto-debounce-consumer-dead-end"`, `severity: medium`, `surfaces:` the ones Task 1 actually establishes (the guardian fires from the CLI session-start hook, while stop-gate-handler is the shared mindrian-core path, so justify the list rather than guessing), `brain_mode: local-only`, `canon_parts: [9]`, `created`/`updated` with a real UTC timestamp.

Sections, with what each must carry:

- Source-of-Truth Preamble: CODE claims read against branch `main` @ the sha from Task 1, plugin version from plugin.json, workspace `/home/jsagi/dev/MindrianOS-Plugin`. WIRE claims: none, this is a pure LOCAL code-reachability finding. Re-verification rule: every claim was produced by a grep or read against the working tree at that sha, and the commands are reproduced in Evidence.

- Current Focus: the hypothesis in one or two sentences, the test that checks it, what a confirmed hypothesis looks like, and one concrete next_action. The next_action is choosing among the resolutions, and it is explicitly OUT OF SCOPE for this filing task.

- Meta: repo, plugin version, "Reported by: quick task 260728-8bw", date first observed, and Related debug sessions naming `hedge-fold-has-no-production-trigger` as the SIBLING SHAPE (a producer wired, a consumer absent), not as the same defect.

- Problem Statement: one or two sentences. What is broken, for whom.

- Symptoms: expected vs actual, and lead the `errors` field with the fact that there is no error to observe. The queue drains cleanly, the hook exits 0, and on-stop even reports a success line. That silence is the defect's whole difficulty. Give numbered, deterministic reproduction steps a reader can actually run (produce a critical MINTO violation in a section, start a session so the guardian fires, peek the queue with `node scripts/minto-debouncer.cjs peek <roomDir>` and see the entry, end the session, peek again and see it gone, then show the section MINTO is byte-unchanged so no regen ran).

- Scope and Impact: affected surfaces, affected commands (the session-start hook, the Stop hook, both drain sites), affected users (all installs), version range (since the Phase 88 landing through the version Task 1 captured), severity with a one-line justification, and a blast radius that INCLUDES the post-write hook as a second producer per Task 1 item 11.

- Eliminated: pre-empt at minimum these, each with the evidence that ruled it out. (a) "the drain is the consumer, it just runs the regen internally" - `drain` is a pure queue partition plus atomic write-back, it calls nothing. (b) "intent-classifier consumes it, the docstring says so" - zero references, per Task 1 item 5. (c) "the queue-health validator would have caught a backed-up queue" - it cannot, per Task 1 item 10. Add any other wrong turn your own investigation nearly took, because that is what this section is for.

- Evidence: one APPEND-style entry per verified claim, each with timestamp, `checked:` the literal command or file range, `found:` the concrete fact, `implication:` what it means. Task 1's output maps here one-to-one.

- Technical Root Cause: name the broken CONTRACT, not only the broken line. The debouncer is a two-sided contract: producers enqueue, a named consumer drains and ACTS. The producer side was honored twice over (guardian plus post-write hook). The consumer side was declared in the docstring and never built, and the two stop-path drains discharge the queue without discharging the obligation, which converts a pending-work signal into a no-op that looks like completed work. Give the Site as `path:line-range` plus function name for each of: the producer, both discard sites, and the absent consumer. Under "Why it surfaces now", be honest: nothing changed, this shipped this way in Phase 88 and the audit that traced the hedge-fold shape is simply the first thing that had cause to look one link further down the same debounce-and-forget chain.

- Required Code Changes: give the candidate resolutions as separate blocks, each with Location, Current behavior, Required behavior, Short-term patch, Long-term fix, and the risk to weigh. Cover at least these three:
  - Option A: make the drain sites ACT on the returned array (call a regen per drained entry). State plainly what Task 1 item 8 found about whether a regen entry point even exists, and weigh the risk of running an LLM-backed regen inside the Stop hook's 1500ms budget.
  - Option B: honor the docstring. Wire `scripts/intent-classifier.cjs` as the promised UserPromptSubmit consumer draining with `olderThanMs: 30000`, and stop the Stop-path drains from unconditionally emptying the queue first (today `olderThanMs: 0` guarantees the consumer finds nothing).
  - Option C: truth-up and remove. If regen was deliberately abandoned, delete the drain from the Stop path, correct the docstring, correct the "N sections drained" surface message per Task 1 item 9, and let the queue exist purely as a health signal, which restores queue-health's reachability.

  Then state ONE recommendation with reasoning, the way the hedge-fold RCA did. The plan's own reading favors Option B paired with the minimal piece of Option C (stop the unconditional vacuum), because it honors a contract the code already declares rather than inventing a new surface (Canon Part 7), it puts the regen on a path where a model turn is already in flight rather than inside a hard Stop budget, and it restores the health alarm as a side effect. Adopt that recommendation if Task 1's evidence supports it. If Task 1 found something that makes a different option better, recommend that one instead and say why in one sentence.

- Tests to Add or Update: at least two. One that fails today: a census-style assertion that at least one PRODUCTION (non-`tests/`, non-`lib/memory/*.test.cjs`) site consumes the drained array. Explicitly require the census to EXCLUDE test paths, because the hedge-fold RCA's Test 1 note documents exactly how a test-only honorer makes such a gate vacuous. One end-to-end: given a room with a critical MINTO violation, when the full session-start-then-stop cycle runs, then the section MINTO is regenerated. Mutation-prove it: disabling the new wiring must turn the leg red. Name the runner registration for each.

- Non-Code Follow-ups: no CHANGELOG entry until a resolution ships (this filing changes no behavior); release lockstep not applicable; Canon Part 9 note (the queue is local memory, every option keeps it local, zero Brain egress either way); the Dev-Research Compositing line naming the mirror path written in Task 3; and an explicit scope note that this file is OPEN and deliberately out of scope for quick task 260728-8bw.

- Resolution: `root_cause: PENDING` and empty fields. Never fabricate a resolution to look finished.

- Closing section for the adjacent findings that are real but separate: the "N sections drained" false-success message (Task 1 item 9) and the structurally unreachable queue-health thresholds (Task 1 item 10). Name them, cite them, and say they are deliberately not folded into the main finding, mirroring how the hedge-fold RCA handled its second finding.

NO em-dashes. Use hyphens or commas throughout, including inside quoted code comments you paraphrase.

Then commit, from `/home/jsagi/dev/MindrianOS-Plugin`:
`git add -f .planning/debug/minto-debounce-consumer-dead-end.md .planning/quick/260728-8bw-file-rca-for-minto-debounce-dead-end-gua/`
`git commit -m "docs(quick-260728-8bw): file the MINTO debounce consumer dead end"` plus a body naming the producer, both discard sites, and the absent consumer. Stage those paths EXPLICITLY, never `git add -A`, since the tree carries unrelated generated drift.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && test -f .planning/debug/minto-debounce-consumer-dead-end.md && for s in "Source-of-Truth Preamble" "Current Focus" "Problem Statement" "Symptoms" "Scope and Impact" "Eliminated" "Evidence" "Technical Root Cause" "Required Code Changes" "Tests to Add" "Non-Code Follow-ups" "Resolution"; do grep -q "$s" .planning/debug/minto-debounce-consumer-dead-end.md || { echo "MISSING SECTION: $s"; exit 1; }; done && grep -qE "^status: (gathering|investigating)" .planning/debug/minto-debounce-consumer-dead-end.md && test "$(grep -cP '\x{2014}' .planning/debug/minto-debounce-consumer-dead-end.md)" = "0" && git diff --quiet HEAD -- scripts/ lib/ hooks/ tests/ && git log -1 --oneline -- .planning/debug/minto-debounce-consumer-dead-end.md && echo "RCA OK"</automated>
  </verify>
  <done>The RCA exists with every template section present, status is gathering or investigating, zero em-dashes, at least three candidate resolutions with one stated recommendation and its reasoning, `git diff HEAD` shows zero changes under scripts/ lib/ hooks/ tests/, and the file is committed.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror the finding to the rethinking-mindrianos room, cross-link both ways, commit in the home repo</name>
  <files>/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-07-28-minto-debounce-consumer-dead-end/2026-07-28-minto-debounce-consumer-dead-end.md, .planning/debug/minto-debounce-consumer-dead-end.md</files>
  <action>
CLAUDE.md's Dev-Research Compositing rule is mandatory, not optional. This writes OUTSIDE the repo root, which is expected and correct. Use absolute paths.

Read the precedent entry first: `/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-07-28-hedge-fold-no-production-trigger/2026-07-28-hedge-fold-no-production-trigger.md`. Match its shape exactly: a directory named `<YYYY-MM-DD>-<slug>/` containing a single `.md` of the same name, frontmatter carrying `methodology: research`, a full-sentence `title`, `created`, `status: active`, `room_section: research`, then a titled body that opens with a Cross-link block and a "Code read against" line naming branch, sha, plugin version and workspace.

Create `/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-07-28-minto-debounce-consumer-dead-end/2026-07-28-minto-debounce-consumer-dead-end.md`.

The cross-link block must name BOTH:
- `MindrianOS-Plugin/.planning/debug/minto-debounce-consumer-dead-end.md` as the executable half.
- `MindrianOS-Plugin/.planning/quick/260728-8bw-file-rca-for-minto-debounce-dead-end-gua/260728-8bw-PLAN.md` as the task that produced it.

The room entry is NOT a copy of the RCA. The repo gets the executable decision; the room gets the reasoning. Write the reasoning half:
- What the reported finding was, and why the report was too small (the pre-pass named one discard site; there are two, plus a second producer, so the queue is a wider dead end than reported).
- The generalizable lesson, stated as a quotable line the way the hedge-fold entry did. The candidate here: draining a queue is not the same act as consuming it. A drain that discards satisfies every observable property of a working consumer (queue shrinks, hook exits 0, health stays green) while doing none of the work. Sharpen or replace this line if your own reading suggests a truer one, but the entry must land ONE such line.
- The pattern relationship to the hedge-fold finding: two independent instances, days apart, of "producer wired, consumer absent, silence indistinguishable from health". Say what that repetition suggests about where else to look, and name at least one concrete next place to audit.
- Why it survived: name the test that exists (guardian Test 1 asserts the enqueue) and articulate precisely what it does not assert. A test that stops at the producer boundary certifies half a contract and reads as coverage.
- The verdict and the recommendation, matching the RCA's recommendation so the two homes do not disagree.

Then add the reciprocal link INTO the RCA if Task 2 did not already write the exact final path: the Non-Code Follow-ups compositing line must name `~/MindrianRooms/rethinking-mindrianos/research/2026-07-28-minto-debounce-consumer-dead-end/`. Both directions must resolve.

NO em-dashes in the room entry either.

Commit in the HOME repo, which is a DIFFERENT git repo from the plugin repo:
`git -C /home/jsagi add MindrianRooms/rethinking-mindrianos/research/2026-07-28-minto-debounce-consumer-dead-end/`
`git -C /home/jsagi commit -m "rethinking-mindrianos: file the MINTO debounce consumer dead end research trail"`
Stage ONLY that directory. The home tree carries a large amount of unrelated modified and untracked state; a blanket `git add` would sweep it in. Verify with `git -C /home/jsagi show --stat HEAD` that the commit touches only that one directory. If the commit accidentally picks up anything else, reset it and redo the staging.

If Task 2's RCA text needed the reciprocal-link edit, amend or add a follow-up commit in the plugin repo with `git add -f .planning/debug/minto-debounce-consumer-dead-end.md`.
  </action>
  <verify>
    <automated>M=/home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-07-28-minto-debounce-consumer-dead-end/2026-07-28-minto-debounce-consumer-dead-end.md; R=/home/jsagi/dev/MindrianOS-Plugin/.planning/debug/minto-debounce-consumer-dead-end.md; test -f "$M" && grep -q "methodology: research" "$M" && grep -q "minto-debounce-consumer-dead-end.md" "$M" && grep -q "260728-8bw-PLAN.md" "$M" && grep -q "rethinking-mindrianos" "$R" && test "$(grep -cP '\x{2014}' "$M")" = "0" && git -C /home/jsagi show --stat --oneline HEAD | grep -c "2026-07-28-minto-debounce-consumer-dead-end" && echo "MIRROR OK"</automated>
  </verify>
  <done>The dated mirror entry exists in the rethinking room with research frontmatter, links to both the RCA and this plan, the RCA links back to the mirror directory, neither file contains an em-dash, and the home-repo commit touches only the new mirror directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root -> home directory | Task 3 writes and commits outside `/home/jsagi/dev/MindrianOS-Plugin`, into a second git repo rooted at `/home/jsagi` that carries substantial unrelated dirty state. |
| planning artifact -> git history | `.planning/` is gitignored, so committing requires `git add -f`, which bypasses the ignore rule by design. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8bw-01 | Tampering | home-repo staging in Task 3 | mitigate | Stage ONLY the new dated directory by exact path; never `git add -A` or `git add .`; verify with `git -C /home/jsagi show --stat HEAD` that the commit touches one directory, and reset if it does not. |
| T-8bw-02 | Tampering | plugin-repo staging in Task 2 | mitigate | Stage the RCA and the quick-task directory by explicit path with `git add -f`; the automated verify asserts `git diff HEAD` is clean under `scripts/ lib/ hooks/ tests/`, so a source edit cannot ride along unnoticed. |
| T-8bw-03 | Information disclosure | RCA + mirror content | mitigate | Both files are pure code-reachability findings against this repo. No room content, no tester names, no keys, no Brain payloads. Canon Part 8 is untouched: zero Brain egress, no wire probe in either artifact. |
| T-8bw-SC | Tampering | package installs | accept | No npm, pip, or cargo install in this plan. No new dependency is introduced, so the package legitimacy gate does not apply. |
</threat_model>

<verification>
Run from `/home/jsagi/dev/MindrianOS-Plugin`:

1. Both artifacts exist and cross-link:
   `test -f .planning/debug/minto-debounce-consumer-dead-end.md && test -f /home/jsagi/MindrianRooms/rethinking-mindrianos/research/2026-07-28-minto-debounce-consumer-dead-end/2026-07-28-minto-debounce-consumer-dead-end.md`

2. Zero em-dashes in either file, and in this plan:
   `grep -rcP '\x{2014}' .planning/debug/minto-debounce-consumer-dead-end.md .planning/quick/260728-8bw-file-rca-for-minto-debounce-dead-end-gua/260728-8bw-PLAN.md` returns 0 for each.

3. No source touched (the hard scope stop):
   `git diff --quiet HEAD -- scripts/ lib/ hooks/ tests/ && git status --porcelain scripts/ lib/ hooks/ tests/` prints nothing.

4. The RCA stays OPEN:
   `grep -E "^status: (gathering|investigating)" .planning/debug/minto-debounce-consumer-dead-end.md`

5. Every citation in the RCA resolves. Spot-check at least four `path:line` citations by reading those exact ranges and confirming the cited construct is present.

6. Both commits landed and are scoped:
   `git log -1 --stat --oneline` in the plugin repo, and `git -C /home/jsagi show --stat --oneline HEAD`.
</verification>

<success_criteria>
- `.planning/debug/minto-debounce-consumer-dead-end.md` exists, follows `docs/RCA-TEMPLATE.md` section-for-section, and matches the hedge-fold RCA's rigor.
- Every claim in the task description was independently verified against the working tree; the two reported line ranges that were approximate are corrected in the filing rather than reproduced.
- The RCA documents the widened shape: two discard sites, two producers, an absent consumer, and whether any regen entry point exists at all.
- Required Code Changes carries at least three candidate resolutions and ONE stated recommendation with reasoning and the risk to weigh.
- The finding exists in both homes, cross-linked in both directions, per CLAUDE.md Dev-Research Compositing.
- Zero em-dashes in every artifact this plan produces.
- Zero changes under `scripts/`, `lib/`, `hooks/`, `tests/`. This is a filing, not a fix.
- Two scoped commits: one in the plugin repo, one in the home repo.
</success_criteria>

<output>
Create `.planning/quick/260728-8bw-file-rca-for-minto-debounce-dead-end-gua/260728-8bw-SUMMARY.md` when done.
</output>
