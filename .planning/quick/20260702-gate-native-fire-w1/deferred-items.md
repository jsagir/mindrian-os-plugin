# Deferred Items - gate-native-fire-w1

## Pre-existing, out of scope (NOT caused by E1)

- tests/test-selector-dispatcher-120-01.cjs T1: "F_SUBSHAPES contains F.7 as the 8th entry" fails on committed HEAD (verified via git stash). Unrelated to the trailer change; F_SUBSHAPES untouched by E1. Belongs to a separate F.7/F_SUBSHAPES catalog issue.
