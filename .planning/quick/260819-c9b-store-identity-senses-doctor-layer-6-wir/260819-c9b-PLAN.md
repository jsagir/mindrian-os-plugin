---
phase: quick-260819-c9b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/brain-client.cjs
  - lib/core/doctor/class-m-brain-smoke.cjs
  - lib/core/doctor/class-m-brain-smoke.test.cjs
  - tests/test-127-02-doctor-class-m.sh
  - scripts/doctor.cjs
  - scripts/check-dual-graph-health.cjs
  - lib/memory/dual-graph-health.test.cjs
  - data/dual-graph-health-baseline.json
autonomous: true
requirements: [WS-E1, WS-G]
canon_parts: [7, 8]

must_haves:
  truths:
    - "node scripts/doctor.cjs --brain-smoke --json reports SIX layers, the sixth with id store_identity"
    - "The store_identity layer carries a payload naming {endpoint, node_count, canon} read through the existing brain-client stats wrapper"
    - "A node_count of exactly 28325 is reported as the named failure stale_replica_signature, never as a generic count miss"
    - "A GraphRagMeta stamp is included when the graph carries one and omitted (not fabricated, not a failure) when it does not"
    - "node scripts/check-dual-graph-health.cjs completes a CONCLUSIVE live read of all five REQUIRED_METRIC_KEYS and exits 0 with outcome baseline_recorded"
    - "data/dual-graph-health-baseline.json exists, is committed, and carries all five metric keys measured live"
    - "The gate default mode stays baseline (report-only); --flip-blocking is NOT enabled and the gate is NOT added to verify-release"
    - "Every failure path stays fail-closed: an unreadable stat, an unreadable metric, or an unreachable Brain produces a named FAIL or inconclusive, never a fabricated number"
  artifacts:
    - path: "lib/core/doctor/class-m-brain-smoke.cjs"
      provides: "LAYERS with 6 entries plus the _layer6 store-identity probe"
      contains: "store_identity"
    - path: "lib/core/brain-client.cjs"
      provides: "getBrainUrl() accessor exposing the single-source-of-truth endpoint"
      contains: "getBrainUrl"
    - path: "lib/core/doctor/class-m-brain-smoke.test.cjs"
      provides: "6-layer hermetic coverage including the stale-replica signature case"
    - path: "tests/test-127-02-doctor-class-m.sh"
      provides: "shell wire-lock asserting 6 layers and the 6-deep skip cascade"
    - path: "scripts/doctor.cjs"
      provides: "6-layer renderer plus the --acceptance store-identity assertion"
    - path: "scripts/check-dual-graph-health.cjs"
      provides: "readLiveMetrics() - the live four-Cypher reader replacing the fail-closed stub"
      contains: "readLiveMetrics"
    - path: "data/dual-graph-health-baseline.json"
      provides: "the recorded live baseline all future gate runs compare against"
      contains: "m1_min_cohort"
  key_links:
    - from: "lib/core/doctor/class-m-brain-smoke.cjs"
      to: "lib/core/brain-client.cjs stats()"
      via: "require inside _layer6, mockable through opts.mockStats"
      pattern: "brain-client.*stats"
    - from: "lib/core/doctor/class-m-brain-smoke.cjs"
      to: "lib/core/brain-client.cjs getBrainUrl()"
      via: "require inside _layer6, mockable through opts.mockBrainUrl"
      pattern: "getBrainUrl"
    - from: "scripts/check-dual-graph-health.cjs"
      to: "lib/core/brain-client.cjs query()"
      via: "readLiveMetrics runs M1/M2/M4/M3_RAW through brainClient.query"
      pattern: "brainClient\\.query"
    - from: "scripts/doctor.cjs --acceptance"
      to: "store_identity layer"
      via: "spawned --brain-smoke --json payload inspection"
      pattern: "store_identity"
---

<objective>
Give the doctor a STORE-IDENTITY sense (WS-E1) and make the dual-graph health gate
actually read the live graph (WS-G).

Today two blind spots cost real money and real trust. First, no plugin surface can
tell canon from a stale replica: the retired mindrian-brain.onrender.com store froze
around July at 28,325 nodes and fooled three separate sessions in one day. Second,
scripts/check-dual-graph-health.cjs ships a documented fail-closed STUB for its live
reader, so the gate has never once measured the real graph and no baseline exists.

Purpose: no session should ever again mistake a copy for canon, and the gate that
guards dual-graph health should have a real number to guard.
Output: class-m-brain-smoke layer 6 store_identity (wire-locked across its three
consumers), a live four-Cypher reader in the gate, and a committed baseline file
recorded from a real conclusive read.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/2026-08-19-PLAN-dual-graph-release.md
@docs/2026-08-19-HANDOFF-brain-plugin-sync-release.md
@lib/core/doctor/class-m-brain-smoke.cjs
@scripts/check-dual-graph-health.cjs
</context>

<measured_facts>
Every number below was MEASURED live against pws-brain-mcp.onrender.com during
planning on 2026-08-19. They are evidence, not targets. The executor MUST re-read
live values at run time and never copy these into code or into the baseline file.

  brain_stats shape: { backend, totalRecordCount, relationshipCount, dimension,
    namespaces, vectorIndexes, vectorIndexSpaces }
  brain_stats live: backend=memgraph, totalRecordCount=29055, relationshipCount=24018
  data/brain-census.generated.json (2026-08-10 snapshot) carries totalRecordCount
    28325 - that snapshot is the STALE REPLICA SIGNATURE, preserved as history.

  brain_query IS admitted on the bounded read tier (verified live, HTTP 200):
    MATCH (f:Framework) RETURN count(f) AS n LIMIT 1  ->  { records: [ { n: 184 } ] }

  GraphRagMeta EXISTS (1 node) and today carries: betweenness_max, community_count,
    graphrag_betweenness, graphrag_community, graphrag_pagerank, node_count (747 -
    a graphrag-scope count, NOT the store total), pagerank_max, refreshed_at,
    singleton. It does NOT yet carry schema_version or last_reconciled. Their
    absence is EXPECTED today and is NOT a failure (the stamp is being introduced
    Brain-side per handoff section 7 item e).

  The four exported Cypher constants, run live through brain-client query():
    M1_CYPHER      -> { m1_min_cohort: 1 }                    OK
    M2_CYPHER      -> { m2_review_required: 23 }              OK, needs the bound
                        param { review_marker: 'REVIEW_REQUIRED' }
    M3_RAW_CYPHER  -> { m3_raw_cross_label_dups: 103 }        OK
    M4_CYPHER      -> { text: 'cypherReadOnly exceeded 5000ms' }   TIMES OUT

  M4 root cause and the fix, both measured: the server enforces a 5000ms
  cypherReadOnly budget. The pattern-comprehension degree test size((n)--()) = 0
  blows it (5031ms, fails). The identical-meaning built-in degree(n) = 0 completes
  in 688ms and returns m4_max_orphan_rate = 1. Swapping that ONE predicate is
  required for a conclusive read; it changes nothing about what M4 means (both
  express "this node has zero relationships") and keeps the constant a fixed,
  read-only, generic-methodology string.

  computeM3NoFork({ roomState: {} }) returns { no_fork: true,
    raw_cross_label_dups: 0, per_query: [...] } - verified, so the live path does
  not trip the recommender-fork branch.
</measured_facts>

<hard_constraints>
1. CJS only. No em-dashes anywhere in any file touched. No new npm dependencies.
2. Canon Part 8: every Brain read here is a generic methodology aggregate or a
   store-metadata stamp. Zero room content crosses the wire. No new network
   surface: every Brain call goes through lib/core/brain-client.cjs.
3. Canon Part 7: reuse the shipped seams. Do NOT mint a new server-side tool for
   the GraphRagMeta stamp - read it through the existing bounded brain_query path.
4. scripts/check-dual-graph-health.cjs is grep-gated by its own suite
   (lib/memory/dual-graph-health.test.cjs, the "Part 8: the gate Cypher is
   read-only" block). That gate asserts the gate SOURCE contains none of the
   uppercase tokens SET, MERGE, DELETE, DETACH, CREATE as whole words, and none
   of the lowercase words artifact, MINTO, roomDir, MindrianRooms, transcript.
   This applies to COMMENTS as well as code. Write around it exactly as the
   existing source does.
5. lib/core/brain-client.cjs is being edited CONCURRENTLY by quick task
   260819-c8j (adding a recommendChain wrapper plus part8 chokepoint coverage).
   Anchor by FUNCTION NAME, never by line number. Add the new accessor as an
   independent top-level function so the two diffs compose. A trivial conflict in
   the module.exports block is expected and acceptable.
6. Fail closed everywhere. Never fabricate a number. If the live gate run cannot
   complete, record NOTHING, leave data/dual-graph-health-baseline.json absent,
   and report the inconclusive honestly in the summary as a PARTIAL completion.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add the store_identity sense (layer 6) plus the endpoint accessor</name>
  <files>lib/core/brain-client.cjs, lib/core/doctor/class-m-brain-smoke.cjs</files>
  <action>
Part A - lib/core/brain-client.cjs. The module-level BRAIN_URL const (declared near
the top of the file, the documented single source of truth for the default endpoint)
is not exported, so any consumer that needs the resolved endpoint would have to
duplicate the canon literal and become a second source of truth. Add a small
independent top-level function getBrainUrl() that returns the resolved BRAIN_URL
value, placed immediately after the existing stats() function so the concurrent
260819-c8j diff does not collide with it. Give it a short doctrine comment naming
why it exists: the doctor store-identity layer must report which endpoint the Brain
wire actually resolved to, and must read it from the one place that decides it. Add
getBrainUrl to module.exports next to stats. Change nothing else in this file.

Part B - lib/core/doctor/class-m-brain-smoke.cjs. Add a sixth layer.

  1. Extend the wire-locked LAYERS array with a sixth frozen entry, AFTER
     e2e_brain_schema, with id 'store_identity' and name 'L6 store identity'.
  2. Add three frozen module constants with a doctrine comment explaining each:
     CANON_BRAIN_URL set to the canon default 'https://pws-brain-mcp.onrender.com';
     CANON_NODE_FLOOR set to 29000; STALE_REPLICA_NODE_COUNT set to 28325. The
     comment on STALE_REPLICA_NODE_COUNT must state plainly that this is the frozen
     roughly-July signature of the retired mindrian-brain.onrender.com replica,
     that seeing it means the wire is pointed at a copy, and that it is therefore a
     NAMED failure rather than a generic count miss.
  3. Write async function _layer6(opts) mirroring the existing _layer3 seam idiom:
     - endpoint via opts.mockBrainUrl or require('../brain-client.cjs').getBrainUrl()
     - stats via opts.mockStats or an async wrapper over
       require('../brain-client.cjs').stats()
     - stamp via opts.mockQuery or an async wrapper over
       require('../brain-client.cjs').query(cypher)
  4. Layer 6 logic, in this order:
     a. Resolve the endpoint. Compute override as a boolean for whether
        process.env.MINDRIAN_BRAIN_URL is set and non-empty, and canon as whether
        the resolved endpoint equals CANON_BRAIN_URL. An explicit override is
        ALLOWED and must be reported as such (payload carries override: true and
        canon: false); it is not by itself a failure. An endpoint that is neither
        canon nor an explicit override is a FAIL naming the resolved endpoint.
     b. Call stats. A null or non-object result is a FAIL with reason
        'brain_stats returned null (Brain unreachable or key rejected)'. Read the
        node count from the totalRecordCount field; if it is missing or not a
        finite number, FAIL with reason 'brain_stats carried no usable
        totalRecordCount' - never substitute a default.
     c. Check STALE_REPLICA_NODE_COUNT FIRST, before the generic floor, so the
        named signature always wins the reason line: if node_count equals
        STALE_REPLICA_NODE_COUNT exactly, FAIL with a reason string that CONTAINS
        the literal token stale_replica_signature and names the endpoint plus the
        count.
     d. Otherwise if node_count is below CANON_NODE_FLOOR, FAIL with a reason
        naming the count and the floor.
     e. Otherwise PASS.
  5. The GraphRagMeta stamp read: after the count verdict is decided, attempt ONE
     bounded read that projects only the stamp fields, of the shape
     MATCH (m:GraphRagMeta) RETURN m.schema_version AS schema_version,
     m.last_reconciled AS last_reconciled, m.refreshed_at AS refreshed_at LIMIT 1.
     Keep the cypher as a named module constant next to the other constants. Read
     the first entry of the records array. Include ONLY the fields that came back
     non-null, under a stamp key in the payload; if none came back, or the read
     errored, or the response was a text/error passthrough rather than a records
     array, OMIT the stamp key entirely. The stamp read must NEVER change the
     layer verdict and must never throw: wrap it so any failure degrades to
     "no stamp" silently. Doctrine comment: the stamp is being introduced
     Brain-side, so absence today is expected and is not a failure.
  6. Attach the evidence to the layer row. _runLayer currently returns only
     { ok, reason, ms }, and checkBrainSmoke builds each row from that. Thread an
     optional payload through: have _runLayer carry r.payload when the layer fn
     supplies one, and have checkBrainSmoke include payload on the row only when
     it is present. Layer 6 supplies { endpoint, node_count, canon, override } plus
     stamp when present. Do NOT add a payload to layers 1 through 5; the existing
     rows keep their exact shape.
  7. Register _layer6 in the layerFns array so the fail-fast cascade covers it: if
     layer 5 fails, layer 6 must report skipped-prior-layer-failed like every other
     downstream layer. The cascade loop is already length-driven off LAYERS, so
     confirm it needs no numeric edit.
  8. Update the file header doctrine block: the "5-layer composable probe" line
     becomes 6-layer, the taxonomy list gains an L6 store_identity row describing
     stale-replica detection, and the JSDoc on checkBrainSmoke that says "5-layer"
     is corrected. Part 7 and Part 8 notes gain a line: L6 reuses the brain-client
     stats and query chokepoints and reads store metadata only, zero user content.
  </action>
  <verify>
    <automated>node -e "const m=require('./lib/core/doctor/class-m-brain-smoke.cjs'); const ids=m.LAYERS.map(l=>l.id); if(ids.length!==6) throw new Error('want 6 layers, got '+ids.length); if(ids[5]!=='store_identity') throw new Error('layer 6 id is '+ids[5]); const b=require('./lib/core/brain-client.cjs'); if(typeof b.getBrainUrl!=='function') throw new Error('getBrainUrl missing'); console.log('OK', ids.join(','), b.getBrainUrl());"</automated>
    <automated>node -e "const m=require('./lib/core/doctor/class-m-brain-smoke.cjs'); const base={mockResolveRoot:()=>({root:'/tmp/x',source:'env',topology:'dev-clone'}),mockResolveKey:()=>({key:'k',source:'env',available:true,reason:null}),mockSchema:async()=>({labels:['Framework']}),mockSpawn:async()=>({ok:true,reason:'m'}),mockBrainUrl:()=>'https://pws-brain-mcp.onrender.com',mockQuery:async()=>null}; (async()=>{const stale=await m.checkBrainSmoke(Object.assign({},base,{mockStats:async()=>({totalRecordCount:28325})})); const L6=stale.layers[5]; if(L6.ok!==false) throw new Error('28325 must FAIL'); if(!/stale_replica_signature/.test(L6.reason)) throw new Error('missing named reason: '+L6.reason); const good=await m.checkBrainSmoke(Object.assign({},base,{mockStats:async()=>({totalRecordCount:29055})})); if(good.layers[5].ok!==true) throw new Error('29055 must PASS: '+good.layers[5].reason); if(!good.layers[5].payload||good.layers[5].payload.node_count!==29055||good.layers[5].payload.canon!==true) throw new Error('payload wrong: '+JSON.stringify(good.layers[5].payload)); const nul=await m.checkBrainSmoke(Object.assign({},base,{mockStats:async()=>null})); if(nul.layers[5].ok!==false) throw new Error('null stats must FAIL'); console.log('OK stale/canon/null all behave');})().catch(e=>{console.error(e.message);process.exit(1);});"</automated>
    <automated>node -e "const fs=require('fs'),D=String.fromCharCode(8212),E=String.fromCharCode(8211);for(const f of ['lib/core/doctor/class-m-brain-smoke.cjs','lib/core/brain-client.cjs']){const s=fs.readFileSync(f,'utf8');if(s.includes(D)||s.includes(E))throw new Error('dash violation in '+f);}console.log('OK no em-dashes or en-dashes');"</automated>
  </verify>
  <done>LAYERS has 6 entries ending in store_identity; getBrainUrl is exported from brain-client; a mocked 28325 count fails with a reason containing stale_replica_signature; a mocked 29055 count passes and carries payload {endpoint, node_count, canon, override}; a null stats result fails closed; no em-dashes in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Move the wire-lock - the three LAYERS consumers</name>
  <files>lib/core/doctor/class-m-brain-smoke.test.cjs, tests/test-127-02-doctor-class-m.sh, scripts/doctor.cjs</files>
  <action>
LAYERS is wire-locked by three consumers that all hard-code the count 5. They move
together or the suites go red. Nothing here changes behavior; it re-pins the
contract at 6.

A - lib/core/doctor/class-m-brain-smoke.test.cjs.
  1. HERMETICITY IS THE CRITICAL EDIT. Tests 2, 7, 8 and 10 currently pass a full
     all-mocks option bag and assert every layer passes. With layer 6 present and
     no stats seam supplied, those tests would hit the real network and become
     flaky and slow. Add mockBrainUrl, mockStats and mockQuery to EVERY existing
     option bag that expects layer 6 to run: mockBrainUrl returning the canon URL,
     mockStats returning an object with a totalRecordCount safely above the floor,
     mockQuery returning null so the stamp is simply absent. Confirm by inspection
     that no test in this file can reach lib/core/brain-client.cjs.
  2. Test 1: expected length becomes 6 and the expected id array gains
     'store_identity' as the sixth element.
  3. Test 2: layers length assertion becomes 6.
  4. Tests 3, 4, 5: the skip-cascade loops that read for (let i = N; i < 5; i++)
     become i < 6, so the new layer is asserted to carry
     skipped-prior-layer-failed too.
  5. Test 6: after asserting layer index 4 is skipped, add the same assertion for
     index 5.
  6. Test 7 and 8: the all-pass loop already iterates result.layers, so only the
     option bag and the "All 5 layers" label text change to 6.
  7. Add ONE new test (call it Test 11) covering the store-identity contract
     hermetically, with these cases: the stale-replica count 28325 fails and its
     reason contains the literal stale_replica_signature; a count above the floor
     passes and its payload carries endpoint, node_count and canon true; a count
     below the floor but not equal to the stale signature fails WITHOUT the
     stale_replica_signature token (so the two failure modes stay distinct); a
     mockStats returning null fails closed; a mockQuery returning a records array
     carrying a non-null schema_version puts a stamp on the payload, and a
     mockQuery returning null leaves the payload with NO stamp key.
  8. Update the header comment block listing what the file covers.

B - tests/test-127-02-doctor-class-m.sh.
  1. Header comment: the T2 line becomes 6 layers, and the T4 line becomes L3-L6
     skipped.
  2. T2: the assertion j.layers.length !== 5 becomes !== 6, with the error text
     updated to match.
  3. T4: add the same skipped-prior-layer-failed assertion for j.layers[5] that
     already exists for indexes 2, 3 and 4, using the next free exit code (25).
     This test runs with no key under a hermetic HOME, so layer 6 is always
     reached in the skipped state and never touches the network.
  4. Keep the file free of em-dashes as its own header demands.

C - scripts/doctor.cjs.
  1. Correct every stale "5-layer" description of Class M so the shipped help and
     the human report do not lie about the probe. The hits are at approximately
     lines 157, 373, 1338, 3011, 3017 and 3781; find them by grepping the file for
     5-layer rather than trusting those numbers. Line 3781 is the user-visible
     renderer string "Class M -- Brain end-to-end smoke (5-layer probe)" and line
     373 is the --help text; both become 6-layer. The 373 help line also lists the
     layer names, so append the store-identity layer to that list.
  2. In the class-M human renderer (classMBrainSmoke, around line 3775), after
     printing a layer row, print the store-identity evidence when the row carries
     a payload: an indented line naming endpoint, node_count and canon, plus the
     stamp fields when present. Guard on the payload being present so the other
     five rows print exactly as before. This is handoff section 7 item g: the
     doctor must REPORT which endpoint the wire resolved to and whether it is
     canon.
  3. The --acceptance point 'activation-reached-the-wire' (around lines 1336-1370)
     already spawns doctor --brain-smoke --json and inspects payload.layers. Add a
     store-identity assertion to that same point, reusing the payload it already
     parsed, placed AFTER the existing L4 version comparison so the current
     behavior is untouched on the pass path. The rule: locate the layer whose id
     is store_identity. If it is absent, return a not-ok finding naming
     'store_identity layer absent from brain-smoke payload' (the wire-lock guard).
     If it is present and not ok AND its reason contains stale_replica_signature,
     return a not-ok finding stating that the release is being cut against the
     stale replica, and carry the layer in detail. Any other store-identity
     failure (a transient count dip, an unreachable Brain) goes into the returned
     detail as information and does NOT flip ok, because an absolute count gate
     would brick the release train on graph churn - the same reasoning that keeps
     the dual-graph gate in report-only mode. Add a short doctrine comment saying
     exactly that.
  4. Update the comment near line 3018 that describes the canonical output shape
     as layers:[5].
  </action>
  <verify>
    <automated>node lib/core/doctor/class-m-brain-smoke.test.cjs</automated>
    <automated>bash tests/test-127-02-doctor-class-m.sh</automated>
    <automated>bash -c 'H=$(grep -c "5-layer" scripts/doctor.cjs lib/core/doctor/class-m-brain-smoke.cjs lib/core/doctor/class-m-brain-smoke.test.cjs | grep -v ":0$" | wc -l); if [ "$H" -ne 0 ]; then echo "stale 5-layer text remains:"; grep -n "5-layer" scripts/doctor.cjs lib/core/doctor/class-m-brain-smoke.cjs lib/core/doctor/class-m-brain-smoke.test.cjs; exit 1; fi; echo "OK no stale 5-layer text"'</automated>
    <automated>bash -c 'set -e; T=$(mktemp -d); HOME="$T" MINDRIAN_OS_ROOT="$PWD" env -u MINDRIAN_BRAIN_KEY node scripts/doctor.cjs --brain-smoke --json > "$T/o.json" 2>/dev/null; node -e "const j=require(\"$T/o.json\"); if(j.layers.length!==6) throw new Error(\"want 6 layers, got \"+j.layers.length); if(j.layers[5].id!==\"store_identity\") throw new Error(\"layer 6 id \"+j.layers[5].id); console.log(\"OK doctor emits 6 layers\");"; rm -rf "$T"'</automated>
    <automated>bash tests/run-all-127.sh</automated>
  </verify>
  <done>The class-m suite, the 127-02 shell harness and the full run-all-127 runner all pass; doctor --brain-smoke --json emits 6 layers with store_identity sixth; no "5-layer" text survives in the three code files; the class-m suite makes zero network calls.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the gate's live reader, then record and commit the baseline</name>
  <files>scripts/check-dual-graph-health.cjs, lib/memory/dual-graph-health.test.cjs, data/dual-graph-health-baseline.json</files>
  <action>
Re-read hard_constraints item 4 before typing in the gate file: its own suite greps
the SOURCE for uppercase write verbs and for user-content words, including inside
comments.

A - scripts/check-dual-graph-health.cjs.
  1. Fix M4_CYPHER so it fits the server budget. Replace the predicate
     size((n)--()) = 0 with degree(n) = 0. Everything else in the constant stays.
     Add a doctrine comment above it recording the measurement, since a future
     reader will otherwise "simplify" it back: the pattern-comprehension form
     exceeds the server's 5000ms cypherReadOnly budget and returns a text error
     rather than rows, while the built-in degree form answers in well under a
     second; both express "this node has zero relationships", so the metric's
     meaning is unchanged. Keep the leading-letter split idiom used by the
     neighbouring constants.
  2. Add a named constant for M2's bound parameter, REVIEW_MARKER set to
     'REVIEW_REQUIRED', with a one-line comment that it is a generic curation enum
     and the only bound param the gate sends.
  3. Add a small helper _firstRowValue(res, key) that returns a finite Number or
     null. It accepts ONLY the { records: [ ... ] } shape with a first entry
     carrying the key as a finite number. A null response, a text or error
     passthrough (which is exactly how the server reports a budget overrun), an
     empty records array, or a missing or non-finite key all return null. Comment
     it as the fail-closed seam: an unreadable metric is null, and one null makes
     the whole read inconclusive.
  4. Add async function readLiveMetrics(brainClient). It:
     - returns null immediately when brainClient is falsy, has no query function,
       or its isAvailable() is false or throws;
     - awaits the four reads through brainClient.query: M1_CYPHER with no params,
       M2_CYPHER with { review_marker: REVIEW_MARKER }, M4_CYPHER with no params,
       M3_RAW_CYPHER with no params, each passed through _firstRowValue keyed by
       its own return alias;
     - computes the no-fork contract locally via computeM3NoFork with an empty
       room state, taking its no_fork boolean (this touches no Brain, per the
       existing M3 doctrine that M3 measures recommender OUTPUT);
     - prefers the LIVE m3_raw_cross_label_dups number from M3_RAW_CYPHER over the
       local index count, matching the existing comment that the raw count may be
       "supplied by the live reader";
     - returns null if ANY of the four numbers is null;
     - wraps the whole body so a thrown error also returns null;
     - returns an object carrying exactly the five REQUIRED_METRIC_KEYS.
     Document the return contract in a JSDoc block mirroring the file's style.
  5. Change makeBrainReader to makeBrainReader(brainClient, prefetched). Keep the
     existing isAvailable guard and the existing null-returning default EXACTLY as
     it is when prefetched is not supplied, so every current caller and test keeps
     its behavior. When prefetched is a non-null object, the returned synchronous
     reader returns it. Replace the "intentionally a stub" paragraph with a note
     that the async wiring landed as readLiveMetrics, that runCheck stays
     synchronous by contract, and that the async read is therefore drained by
     main() before runCheck is called.
  6. Make main async: await readLiveMetrics(brainClient), pass the result as the
     second argument to makeBrainReader, leave the store and mode arguments alone.
     DEFAULT_MODE stays 'baseline'; the --flip-blocking argv branch stays exactly
     as it is. Update the require.main invocation at the bottom of the file to
     handle the returned promise and exit 1 on a rejection. Update the comment in
     main that says the live reader fails closed "until its async wiring lands".
  7. Export readLiveMetrics alongside the existing exports.

B - lib/memory/dual-graph-health.test.cjs. Append hermetic coverage using a fake
   brainClient object; this suite must never reach the network.
   - readLiveMetrics returns all five REQUIRED_METRIC_KEYS with the expected
     numbers when the fake client answers all four reads with well-formed records;
   - it returns null when the fake client answers M4 with the budget-overrun shape
     { text: 'cypherReadOnly exceeded 5000ms' } - this is the exact live failure
     mode and the reason the predicate was changed;
   - it returns null when isAvailable() is false, and null when a read throws;
   - the M2 read receives the bound review_marker param (assert on what the fake
     client captured);
   - makeBrainReader(client) with NO prefetched argument still returns a reader
     that yields null (the regression pin protecting the existing fail-closed
     default);
   - makeBrainReader(client, metrics) yields those metrics, and feeding that
     reader into runCheck against an empty in-memory store produces outcome
     baseline_recorded with exitCode 0.
   Also assert the source no longer contains the pattern-comprehension degree
   idiom, so nobody reverts the budget fix silently.

C - The live run. This is the deliverable, not a smoke test.
   1. Confirm data/dual-graph-health-baseline.json does not already exist. It did
      not at planning time.
   2. Run node scripts/check-dual-graph-health.cjs with NO flags and capture both
      stdout and the exit code.
   3. Expected designed outcome: baseline_recorded, exit 0, and the file written
      with all five metric keys plus recorded_at, mode baseline and version 1.
      Verify the file's five values against the numbers the run printed - they
      must be the measured live values, not the planning-time evidence numbers.
   4. If instead the outcome is inconclusive: do NOT retry with different Cypher,
      do NOT hand-write a baseline file, and do NOT commit anything under data/.
      Delete any partial file, report which of the four reads returned null and
      the raw response shape that caused it, and mark this task PARTIALLY COMPLETE
      in the summary. An honest inconclusive is the correct output of a
      fail-closed gate; a fabricated baseline is the worst possible outcome.
   5. Do NOT pass --flip-blocking. Do NOT add the gate to scripts/verify-release
      or to any CI step. State in the summary that the flip to blocking is a
      deliberate post-release decision, deferred until the baseline has proven
      stable across runs, and that DEFAULT_MODE is still 'baseline'.
   6. Commit the recorded baseline file together with the code changes.
  </action>
  <verify>
    <automated>node lib/memory/dual-graph-health.test.cjs</automated>
    <automated>bash tests/run-all-130.7.sh</automated>
    <automated>bash -c 'node scripts/check-dual-graph-health.cjs; echo "exit=$?"'</automated>
    <automated>node -e "const fs=require('fs'); const p='data/dual-graph-health-baseline.json'; if(!fs.existsSync(p)){console.log('INCONCLUSIVE: no baseline written - task is PARTIAL, report honestly'); process.exit(0);} const b=JSON.parse(fs.readFileSync(p,'utf8')); const need=require('./scripts/check-dual-graph-health.cjs').REQUIRED_METRIC_KEYS; for(const k of need){ if(!(k in b.metrics)) throw new Error('baseline missing '+k); } if(b.mode!=='baseline') throw new Error('baseline mode is '+b.mode); console.log('OK baseline', JSON.stringify(b.metrics));"</automated>
    <automated>bash -c 'G=$(node -e "console.log(require(\"./scripts/check-dual-graph-health.cjs\").DEFAULT_MODE)"); test "$G" = "baseline" && ! grep -q "check-dual-graph-health" scripts/verify-release && echo "OK gate still report-only and not in verify-release"'</automated>
  </verify>
  <done>readLiveMetrics is exported and returns the five REQUIRED_METRIC_KEYS from four live reads or null on any unreadable metric; the 130.7 suite passes; the live gate run either wrote data/dual-graph-health-baseline.json with all five measured keys and exited 0 with outcome baseline_recorded (committed), or reported inconclusive with nothing recorded and the task declared partial; DEFAULT_MODE is still baseline and the gate is not in verify-release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin to Brain (HTTPS) | Cypher strings and one enum param leave the machine; graph rows return |
| Brain response to doctor output | Untrusted remote payload is parsed and rendered to the operator |
| gate result to release decision | A gate verdict influences whether a release ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-c9b-01 | Information disclosure | readLiveMetrics egress | mitigate | Only the four frozen Cypher constants plus REVIEW_MARKER cross the wire; no caller-supplied string is interpolated; the suite's Part 8 forbidden-pattern sweep already scans every .query( line in this file |
| T-c9b-02 | Information disclosure | layer 6 stamp read | mitigate | Bounded LIMIT 1 read projecting only schema_version, last_reconciled and refreshed_at from a store-metadata node; no room content, no new server-side tool |
| T-c9b-03 | Tampering | Cypher constants | mitigate | Constants stay fixed strings; the M4 edit changes one degree predicate only and is pinned by a test asserting the slow idiom does not return |
| T-c9b-04 | Spoofing | stale replica answering as canon | mitigate | This is the whole point of layer 6: endpoint compared against CANON_BRAIN_URL, count floor at 29000, and the exact 28325 signature named as stale_replica_signature and surfaced as a blocker in --acceptance |
| T-c9b-05 | Repudiation | fabricated baseline numbers | mitigate | The baseline is only ever written by runCheck from a conclusive reader; no hand-authored file; an inconclusive read records nothing and is reported as partial completion |
| T-c9b-06 | Denial of service | 5s server read budget | mitigate | M4 moved to the measured 688ms degree form; every read failure degrades to null and inconclusive rather than hanging or retrying |
| T-c9b-SC | Tampering | supply chain | accept | Zero new npm/pip/cargo installs in this plan; no package legitimacy gate applies |
</threat_model>

<verification>
Run after all three tasks:

  node lib/core/doctor/class-m-brain-smoke.test.cjs
  bash tests/test-127-02-doctor-class-m.sh
  bash tests/run-all-127.sh
  node lib/memory/dual-graph-health.test.cjs
  bash tests/run-all-130.7.sh
  bash tests/test-127-03-no-em-dashes.sh
  node scripts/doctor.cjs --brain-smoke        (live, expect L6 PASS naming canon)

Then confirm nothing else regressed in the doctor surface:

  node scripts/doctor.cjs --help | grep -- --brain-smoke
</verification>

<success_criteria>
1. doctor --brain-smoke --json emits 6 layers; the sixth is store_identity and
   carries a payload with endpoint, node_count and canon.
2. A node count of exactly 28325 produces a FAIL whose reason contains
   stale_replica_signature; a count merely under the floor produces a different
   FAIL reason; a null stats result fails closed.
3. The GraphRagMeta stamp appears in the payload only when the graph supplies it,
   and its absence neither fails the layer nor fabricates fields.
4. All three LAYERS consumers (class-m suite, 127-02 shell harness, doctor.cjs)
   assert 6 and pass; the class-m suite makes no network calls.
5. readLiveMetrics returns all five REQUIRED_METRIC_KEYS from live reads, or null
   if any single metric is unreadable.
6. The live gate run recorded data/dual-graph-health-baseline.json with measured
   values and exited 0 with outcome baseline_recorded, and that file is committed.
   OR: the run was inconclusive, NOTHING was recorded, and the summary says so
   plainly with the failing read named.
7. DEFAULT_MODE is still 'baseline'; --flip-blocking was not used and the gate was
   not added to verify-release. The summary states that the flip is a deliberate
   post-release decision pending baseline stability.
8. No em-dashes in any touched file; no new npm dependencies; every Brain call
   routes through lib/core/brain-client.cjs.
</success_criteria>

<output>
Create `.planning/quick/260819-c9b-store-identity-senses-doctor-layer-6-wir/260819-c9b-SUMMARY.md` when done.

The summary MUST state explicitly:
  - the live node_count layer 6 observed and whether it was canon
  - whether a GraphRagMeta stamp was present, and which fields
  - the gate outcome (baseline_recorded or inconclusive) with the five recorded
    values if any, marked as measured not targeted
  - that --flip-blocking remains off and why the flip is deferred
</output>
