---
phase: quick-260705-m9g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-card-fire.cjs
  - tests/test-ga4-card-fire-interceptor.cjs
  - tests/test-card-fire-relevance-gate.cjs
autonomous: true
requirements: [RCA-FINDING-1, RCA-FINDING-2]
rca: .planning/debug/card-fire-block-surface.md

must_haves:
  truths:
    - "When the Stop hook intercepts an unfired gate, the user sees a calm human sentence, never the raw classification slug (e.g. ascii-box-backstop-no-card) framed as an error"
    - "The reason field still carries the raw slug for logs/telemetry, unchanged"
    - "A plain 2-option yes/no closer in prose no longer force-fires the card (classifies intercept:false, reason gate-is-simple-binary)"
    - "A genuine 3+-option fork with no fired card still intercepts (the Phase 209 floor guarantee is preserved)"
    - "An already-answered gate still passes as gate-already-answered and an irrelevant gate still passes as gate-irrelevant-to-turn (existing pass-reason precedence unchanged)"
  artifacts:
    - path: "scripts/check-card-fire.cjs"
      provides: "systemMessage on the intercept envelope branch + the gate-is-simple-binary pass-reason"
      contains: "gate-is-simple-binary"
    - path: "tests/test-ga4-card-fire-interceptor.cjs"
      provides: "envelope assertion: intercept envelope carries a human-readable systemMessage with no raw slug substring"
      contains: "systemMessage"
    - path: "tests/test-card-fire-relevance-gate.cjs"
      provides: "new leg asserting the binary exemption + Leg 3 floor fixture upgraded to a 3-option fork"
      contains: "gate-is-simple-binary"
  key_links:
    - from: "scripts/check-card-fire.cjs classifyCardFire"
      to: "lib/core/gate-relevance.cjs extractOptionLabels"
      via: "the gateLabels array already extracted at the relevance-gate block (reused, not re-extracted)"
      pattern: "gateLabels\\.length === 2"
    - from: "scripts/check-card-fire.cjs buildEnforcementEnvelope intercept branch"
      to: "ALLOWED_ENVELOPE_KEYS"
      via: "systemMessage key (already allowlisted at ~line 248, currently unused on this branch)"
      pattern: "systemMessage:"
---

<objective>
Fix two confirmed defects in scripts/check-card-fire.cjs (the SEED-021 Stop-hook card-fire interceptor), per the RCA at .planning/debug/card-fire-block-surface.md:

1. Finding 1 (leaked slug): the intercept branch of buildEnforcementEnvelope() sets only `reason` (an internal telemetry slug like `ascii-box-backstop-no-card`) and no `systemMessage`, so Claude Code renders the slug to the user as "Stop hook error: ascii-box-backstop-no-card" - a working-as-designed intercept that looks like a crash.
2. Finding 2 (binary over-trigger, navigator-decided 2026-07-05): the backstop intercepts a plain 2-option yes/no closer identically to a genuine 3+-way fork. Decision: exempt binaries via a new pass-reason `gate-is-simple-binary`, same shape as the two existing relevance pass-reasons.

Purpose: stop eroding user trust with fake error surfaces, and stop adding friction to simple yes/no exchanges, while preserving the Phase 209 floor (a genuine relevant unanswered 3+-way fork still force-fires).
Output: patched scripts/check-card-fire.cjs + extended existing test files (Part 7 reuse: tests/test-ga4-card-fire-interceptor.cjs and tests/test-card-fire-relevance-gate.cjs; no new test file).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/debug/card-fire-block-surface.md
@scripts/check-card-fire.cjs
@lib/core/gate-relevance.cjs
@tests/test-ga4-card-fire-interceptor.cjs
@tests/test-card-fire-relevance-gate.cjs
</context>

<interface_notes>
Current code facts the executor needs (verified against HEAD this session; do not rediscover):

- `ALLOWED_ENVELOPE_KEYS` (scripts/check-card-fire.cjs ~line 242-250) already includes `systemMessage`. No allowlist change needed for Finding 1.
- `buildEnforcementEnvelope(verdict)` (~line 505-541) has three branches: degrade (suppressOutput, do NOT touch), intercept (~line 515-528, the Finding 1 fix site), and neither/else (suppressOutput, do NOT touch).
- `classifyCardFire(turn, registry)` (~line 416-493): the relevance-gate block (~line 474-483) already computes `const gateLabels = gateRelevance.extractOptionLabels(outputText);` (line 477) and returns two pass-reasons: `gate-already-answered` (line 478-480) and `gate-irrelevant-to-turn` (line 481-483). The final intercept is minted at ~line 485-488.
- `extractOptionLabels` returns a normalized, deduped label array; it recognizes `[n]`, `n.`, and `n)` markers. A glyph-only text with no recoverable labels returns [] (length 0).
- Existing test fixtures that MUST stay green under the binary exemption: test-ga4-card-fire-interceptor.cjs intercept-true fixtures use 0-label texts ('Here are your options.', 'no card yet') or a 3-option box; test-ga4-card-fire-e2e-179.cjs uses 3-option boxes throughout; test-209-incident-replay.cjs expects intercept:false (unaffected); test-209-card-fire-gate.cjs targets lib/core/card-fire-gate.cjs (unaffected).
- The ONE fixture that regresses by design: test-card-fire-relevance-gate.cjs `ROOM_PICK_GATE` (2 options, shared by Leg 2 and Leg 3). Leg 3 asserts intercept:true on it, which the binary exemption would flip. Fix inside Task 2.
</interface_notes>

<tasks>

<task type="auto">
  <name>Task 1: Finding 1 - add systemMessage to the intercept envelope branch (stop leaking the slug)</name>
  <files>scripts/check-card-fire.cjs, tests/test-ga4-card-fire-interceptor.cjs</files>
  <action>
In scripts/check-card-fire.cjs, function buildEnforcementEnvelope(), intercept branch ONLY (the `v.intercept === true` arm, ~line 515-528): add a `systemMessage` key to the `raw` object with a calm, human, one-line string, e.g. 'Re-rendering your choices as a selectable card...'. Keep `reason` exactly as-is (the slug stays for logs/telemetry). Do NOT touch the degrade branch or the else branch (both correctly use suppressOutput: true and are not implicated). Do NOT modify ALLOWED_ENVELOPE_KEYS (systemMessage is already allowlisted). Update the function's header comment (~line 495-504) to note the intercept branch now carries systemMessage so Claude Code renders the human line instead of the raw reason slug (the 2026-07-05 leaked-slug incident). No em-dashes in any new text.

In tests/test-ga4-card-fire-interceptor.cjs, extend the existing ENVELOPE section (~line 151-175, the `ok(...)` idiom) with assertions on the already-built `interceptEnv` (or a fresh buildEnforcementEnvelope({ intercept: true, reason: 'ascii-box-backstop-no-card', degrade: false }) call): (a) `systemMessage` is a non-empty string, (b) it does NOT contain the raw slug substring 'ascii-box-backstop-no-card', (c) `reason` still equals the slug unchanged, (d) the degrade envelope still has NO systemMessage (suppressOutput path untouched).
  </action>
  <verify>
    <automated>node tests/test-ga4-card-fire-interceptor.cjs && node -e "const m=require('/home/jsagi/dev/MindrianOS-Plugin/scripts/check-card-fire.cjs'); const e=m.buildEnforcementEnvelope({intercept:true,reason:'ascii-box-backstop-no-card',degrade:false}); if(typeof e.systemMessage!=='string'||!e.systemMessage||e.systemMessage.includes('ascii-box-backstop-no-card')||e.reason!=='ascii-box-backstop-no-card'){console.error('FAIL',e);process.exit(1)};console.log('OK')"</automated>
  </verify>
  <done>The intercept envelope carries both the unchanged slug `reason` and a human-readable `systemMessage` containing no slug substring; the interceptor test file passes with the new assertions; degrade and else branches are byte-identical except comments.</done>
</task>

<task type="auto">
  <name>Task 2: Finding 2 - exempt binary yes/no closers via the gate-is-simple-binary pass-reason</name>
  <files>scripts/check-card-fire.cjs, tests/test-card-fire-relevance-gate.cjs</files>
  <action>
In scripts/check-card-fire.cjs, function classifyCardFire(): insert the new pass-reason AFTER the two existing relevance checks (after the `gate-irrelevant-to-turn` return, ~line 483) and BEFORE the final intercept mint (~line 485). Placement is load-bearing: putting it before `gate-already-answered` would change Leg 1 of the relevance test (its RELEASE_GATE is also 2-option) from reason 'gate-already-answered' to the new reason - do not do that. Reuse the `gateLabels` array already extracted at line 477 (per the RCA: do NOT build a new option-extraction path). Condition: `gateLabels.length === 2` exactly - NOT `<= 2`, because extractOptionLabels returns [] for glyph-only or PRIMARY-signal texts with no recoverable labels ('Here are your options.', 'no card yet'), and exempting length 0 would break the existing intercept-true predicate assertions; a 0-label detection stays conservative and intercepts. Return `{ intercept: false, reason: 'gate-is-simple-binary', degrade: false }`. Add a short comment block naming the 2026-07-05 navigator decision (simple binaries are exempt from the card-fire requirement; only genuine 3+-option forks force-fire) mirroring the tone of the Phase 210-05 relevance-gate comment above it.

In tests/test-card-fire-relevance-gate.cjs, update the shared `ROOM_PICK_GATE` fixture (~line 93-97) from 2 options to 3 by appending a third option line (e.g. '3. Start a new room') so Leg 3's PRESERVE FLOOR assertion (a genuine relevant unanswered fork still intercepts) remains true and correctly asserts the floor for 3+-way forks. Leg 2 (irrelevance) is unaffected by the extra option since gate-irrelevant-to-turn fires before the binary check and a 3-option gate is not binary anyway. Update the fixture's comment to note the third option exists because 2-option gates are now exempt (gate-is-simple-binary).
  </action>
  <verify>
    <automated>node tests/test-card-fire-relevance-gate.cjs && node -e "const m=require('/home/jsagi/dev/MindrianOS-Plugin/scripts/check-card-fire.cjs'); const v=m.classifyCardFire({ran_entries:[],askuserquestion_fired:false,output_text:'Want those?\n1. Yes, add them\n2. No, skip it',preceding_user_text:'tell me about the yes-add and no-skip tradeoff for those items'},{entries:[]}); if(v.intercept!==false||v.reason!=='gate-is-simple-binary'){console.error('FAIL',v);process.exit(1)};console.log('OK')"</automated>
  </verify>
  <done>A 2-option closer classifies intercept:false with reason gate-is-simple-binary; a 3-option fork still intercepts (Leg 3 green on the upgraded fixture); Legs 1 and 2 still return their original pass-reasons; the whole relevance-gate test file exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: New binary-exemption test leg + full card-fire regression sweep</name>
  <files>tests/test-card-fire-relevance-gate.cjs</files>
  <action>
In tests/test-card-fire-relevance-gate.cjs, add a fourth leg using the existing `leg()` + `classifyTranscript()` idioms: a genuine, RELEVANT, UNANSWERED 2-option binary closer (so neither existing relevance pass-reason can claim it - the preceding user text must topically overlap the gate labels but not answer them, e.g. user asks about the two named options, assistant emits a 2-option numbered closer with no tool_use). Assert `verdict.intercept === false` and `verdict.reason === 'gate-is-simple-binary'`. This proves the exemption is reached through the live transcript path, not only the direct-field unit shape from Task 2's verify. Update the file's header comment to name the new leg.

Then run the full card-fire regression sweep (all four card-fire test files plus the 179 aggregator that wraps the interceptor e2e) and confirm every file exits 0. If any e2e fixture unexpectedly trips the binary exemption, fix the FIXTURE only if it is a 2-option gate asserting intercept (per this plan's design none should be - the e2e-179 fixtures are all 3-option); never weaken the exemption condition itself.
  </action>
  <verify>
    <automated>node tests/test-card-fire-relevance-gate.cjs && node tests/test-ga4-card-fire-interceptor.cjs && node tests/test-ga4-card-fire-e2e-179.cjs && node tests/test-209-incident-replay.cjs && node tests/test-209-card-fire-gate.cjs && grep -c "gate-is-simple-binary" scripts/check-card-fire.cjs</automated>
  </verify>
  <done>All five card-fire-adjacent test files exit 0; the new transcript-path leg passes; gate-is-simple-binary appears in scripts/check-card-fire.cjs; no em-dashes introduced anywhere (spot-check new lines).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript file -> hook | untrusted model-emitted text is scanned by the Stop hook (existing boundary, unchanged) |
| hook stdout -> Claude Code UI | the envelope's systemMessage/reason are rendered to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m9g-01 | Information Disclosure | buildEnforcementEnvelope intercept branch | mitigate | systemMessage is a FIXED literal string, never interpolated from transcript content or the reason slug; the slug stays log-side only |
| T-m9g-02 | Elevation of Privilege (enforcement bypass) | classifyCardFire binary exemption | mitigate | condition is exactly gateLabels.length === 2; 0-label and 3+ paths keep intercepting; Leg 3 floor test pins the 3+ guarantee forever |
| T-m9g-03 | Tampering | package installs | accept | no new dependencies; Node built-ins only, per file house rule |
</threat_model>

<verification>
- `node tests/test-card-fire-relevance-gate.cjs` exits 0 (4 legs green: already-answered, irrelevant, 3-option floor, binary exemption).
- `node tests/test-ga4-card-fire-interceptor.cjs` exits 0 with the new systemMessage assertions.
- `node tests/test-ga4-card-fire-e2e-179.cjs`, `node tests/test-209-incident-replay.cjs`, `node tests/test-209-card-fire-gate.cjs` all exit 0 (no regression).
- Manual truth (from the RCA symptoms): an intercept now surfaces the human systemMessage line, not 'Stop hook error: ascii-box-backstop-no-card'.
- Constitutional floor untouched: MAX_FORCE_RETRIES=3, MAX_SESSION_INTERCEPTS=12, degrade envelope, Stop-block re-prompt text - all byte-unchanged.
- House rules: no em-dashes; CJS + Node built-ins only; Part 8 unchanged (pure local string work, zero new I/O).
</verification>

<success_criteria>
- Finding 1 closed: intercept envelopes carry a fixed human systemMessage; reason slug unchanged for telemetry; degrade/else branches untouched.
- Finding 2 closed per the navigator decision: exactly-2-option gates pass as gate-is-simple-binary, placed after the two relevance pass-reasons; 0-label and 3+-option detections still intercept.
- All five card-fire test files green; Leg 3 floor fixture upgraded to 3 options with an explanatory comment.
- RCA follow-up (post-plan, by the executor in the SUMMARY): note that Finding 2 resolved as a code exemption, so the RCA file can move to .planning/debug/resolved/ with a knowledge-base entry.
</success_criteria>

<output>
Create `.planning/quick/260705-m9g-fix-check-card-fire-cjs-leaked-slug-as-f/260705-m9g-SUMMARY.md` when done.
</output>
