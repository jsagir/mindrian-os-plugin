# Localhost workspace review

User mandate: rethink MindrianOS localhost UI around the intended workflow, not the old port-3131 UI. Deliver a review-ready report and end-to-end evidence, not an implementation claim.

1. Trace current launch, room data, rendering, session, chat, approvals, persistence and restart. Verify with isolated fixtures and browser where possible; never send live room content to a model.
2. Define target journeys, information architecture, session ownership, reuse boundaries, delivery stages and measurable acceptance gates. Ground Claude Code and OmniRoute claims in primary documentation.
3. Write report, QA appendix and evidence. Verify citations and report coverage. Record remaining implementation gates explicitly.

Scope: documentation and isolated audit scripts only. Existing UI is a baseline under review, not a product dependency. GSD initialized via query init.quick; sequential local execution adapts the workflow to this runtime. Existing unrelated edits preserved.
