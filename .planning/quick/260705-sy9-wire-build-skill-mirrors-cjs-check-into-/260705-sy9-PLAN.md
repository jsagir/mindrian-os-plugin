---
phase: quick-260705-sy9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-skill-mirrors.cjs
  - scripts/hooks/pre-commit
  - scripts/install-pre-commit.sh
  - scripts/verify-release
  - scripts/doctor.cjs
  - tests/test-skill-mirrors-tripwire.cjs
autonomous: true
requirements: ["260705-sy9"]
must_haves:
  truths:
    - "A staged commands/*.md or skills/*/SKILL.md change cannot commit while any skill mirror is missing or stale (both tracked and installed pre-commit hooks reject it)"
    - "A release cannot pass verify-release with a stale mirror (new step 10b fails)"
    - "doctor --acceptance fails if build-skill-mirrors --check fails (folded into the coverage-gate point; doctor stays 14/14)"
    - "--check also fails if a SKIP_LIST skill (trending-to-absurd) is deleted or reverted to a byte-identical copy of its command"
    - "A fresh hook install (setup-hooks.sh + install-pre-commit.sh) lands the new trigger without hand edits"
  artifacts:
    - path: "tests/test-skill-mirrors-tripwire.cjs"
      provides: "Wiring proof + trigger-regex behavioral cases + SKIP_LIST synthetic failure cases"
      min_lines: 80
    - path: "scripts/build-skill-mirrors.cjs"
      provides: "SKIP_LIST verification in --check + module.exports with require.main guard"
      contains: "checkSkipList"
  key_links:
    - from: "scripts/hooks/pre-commit"
      to: "scripts/build-skill-mirrors.cjs"
      via: "staged-path-triggered --check guard block"
      pattern: "build-skill-mirrors\\.cjs.*--check"
    - from: "scripts/doctor.cjs"
      to: "scripts/build-skill-mirrors.cjs"
      via: "coverage-gate gates[] entry"
      pattern: "id: 'skill-mirrors'"
    - from: "scripts/verify-release"
      to: "scripts/build-skill-mirrors.cjs"
      via: "step 10b gate"
      pattern: "build-skill-mirrors\\.cjs.*--check"
---

<objective>
Wire scripts/build-skill-mirrors.cjs --check (built in 260705-ob7) into every enforcement surface so a command edit can never silently strand a stale skills/<name>/SKILL.md mirror: pre-commit (tracked hook + installer + installed hook), verify-release (new step 10b), and doctor --acceptance (folded into the existing coverage-gate point). Also harden --check to verify the SKIP_LIST skill (trending-to-absurd) still exists and is still genuinely divergent from its command. Lock the wiring with a tripwire test mirroring the render-gate-wiring precedent.

Purpose: 260705-ob7 explicitly deferred this. Without it, the 106-mirror workaround for the Claude Code Windows registration bug rots on the first commands/*.md edit that skips regeneration.

Output: 5 modified scripts + 1 new test, all gates green, one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@scripts/build-skill-mirrors.cjs
@scripts/hooks/pre-commit
@scripts/install-pre-commit.sh
@scripts/verify-release
@tests/test-connector-tripwire.cjs
@tests/test-render-gate-wiring.cjs
</context>

<decisions_made_during_planning>
Two explicit decisions the constraints required stating, not silently picking:

1. Doctor acceptance: FOLD into the existing `coverage-gate` point, NOT a new acceptance point. Precedent: Phase 178-03 added the render gate as a gates[] sibling inside the same organ ("rides the SAME doctor --acceptance organ as the two CIRS gates", doctor.cjs line ~2962). build-skill-mirrors --check exits 1 on drift, so it fits the existing `r.status === 0` blocker semantics exactly (unlike shape-declaration, which is advisory). Doctor stays 14/14, matching the constraint's first branch.

2. verify-release placement: new lettered substep `10b. SKILL MIRRORS` immediately after section `10. SKILLS`. Precedent: 260705-jeq added `7b. COMMAND REGISTRATION PRECONDITIONS` as a lettered substep after its related numbered section. Skills is the thematically related section here. release.sh calls verify-release at Steps 2, 6.5, 6.6 (via doctor --pre-tag) and 9.8, so release coverage is automatic; release.sh itself is NOT touched.

Also noted: `.git/hooks/pre-commit` is currently STALE relative to the tracked hook (it lacks the connector/projection/render/corpus/shape/help/cmd-reg guards). The correct refresh path is the two installers in sequence (Task 2), which fixes that staleness as a side effect. Do not hand-edit the installed hook.
</decisions_made_during_planning>

<tasks>

<task type="auto">
  <name>Task 1: SKIP_LIST verification in --check + testable exports</name>
  <files>scripts/build-skill-mirrors.cjs</files>
  <action>
Extend scripts/build-skill-mirrors.cjs WITHOUT touching the core mirroring logic (commandNames, computeExpectedMirror, writeMirrors, the desensitize exception, and the byte-copy behavior all stay byte-identical in behavior).

1. Add a new function `checkSkipList(opts)` where opts defaults to `{ skipList: SKIP_LIST, commandsDir: COMMANDS_DIR, skillsDir: SKILLS_DIR }`. For each name in skipList it verifies BOTH: (a) `<skillsDir>/<name>/SKILL.md` exists on disk, and (b) its bytes are NOT identical to `<commandsDir>/<name>.md` (raw source bytes, a plain Buffer.equals compare, no desensitize transform). Returns an array of failure strings: `<name> (SKIP-LIST MISSING: hand-authored skill deleted)` for (a) and `<name> (SKIP-LIST NOT DIVERGENT: reverted to a plain copy of its command)` for (b). If the source command file itself is missing, treat condition (b) as passing (nothing to be a copy of) but still enforce (a). Empty array = healthy.

2. Wire checkSkipList() into checkMirrors(): after the existing per-mirror loop, append `checkSkipList()` results to the same `failing` array so --check exits 1 on either class of failure. Adjust the failure banner so skip-list failures print a distinct recovery line: `Recovery for SKIP-LIST failures: restore the hand-authored skills/<name>/SKILL.md from git history (the generator never writes skip-listed names)`. Keep the existing `Recovery: node scripts/build-skill-mirrors.cjs` line for mirror drift. Update the OK line to `build-skill-mirrors --check: OK (<n> mirrors match expected content; skip-list verified: <SKIP_LIST names>)`.

3. Make the module testable following the build-connector-registry.cjs precedent (its validateConnectors/buildRegistry exports are consumed by tests/test-connector-tripwire.cjs): add `module.exports = { SKIP_LIST, computeExpectedMirror, checkMirrors, checkSkipList };` and guard the entry point with `if (require.main === module) main();` so requiring the module never executes write or check modes. This is call-site wiring, not mirroring logic.

No em-dashes in any new comment or output string.
  </action>
  <verify>
    <automated>node scripts/build-skill-mirrors.cjs --check && node -e "const m=require('/home/jsagi/dev/MindrianOS-Plugin/scripts/build-skill-mirrors.cjs'); const errs=m.checkSkipList(); if(errs.length) {console.error(errs); process.exit(1)}; console.log('skip-list clean')"</automated>
  </verify>
  <done>--check still exits 0 on the clean tree, its OK line mentions the skip-list verification, requiring the module runs nothing, and checkSkipList() returns [] on the real tree. Write mode output is byte-identical in behavior to before (run `node scripts/build-skill-mirrors.cjs` and confirm `created 0, unchanged 106` class output with no overwrites).</done>
</task>

<task type="auto">
  <name>Task 2: Pre-commit tripwire in tracked hook, installer, and installed hook</name>
  <files>scripts/hooks/pre-commit, scripts/install-pre-commit.sh</files>
  <action>
1. scripts/hooks/pre-commit (tracked): insert a new guard block immediately after the Phase 157-04 orchestration-projection block (after its closing `fi`, before the Phase 110 brain-packet-schema block), mirroring the CONN-03 block's exact shape:

- Comment header: `Quick task 260705-sy9 guardian: skill-mirror staleness tripwire.` Explain in 3-5 lines: every commands/<name>.md is mirrored into skills/<name>/SKILL.md (the Windows commands-registration workaround, 260705-ob7); when any commands/*.md or skills/*/SKILL.md is staged, run the mirror --check and reject the commit if any mirror is missing/stale OR a SKIP_LIST skill was deleted or reverted to a plain copy. Canon Part 8: local byte compares only, zero Brain calls. Recovery on drift: node scripts/build-skill-mirrors.cjs.
- Trigger: `if git diff --cached --name-only | grep -qE '^(commands/.*\.md|skills/.*/SKILL\.md)$'; then`
- Body: `command -v node` + `[ -f "$REPO_ROOT/scripts/build-skill-mirrors.cjs" ]` guard, then `node "$REPO_ROOT/scripts/build-skill-mirrors.cjs" --check || { echo "skill-mirror drift -- run: node scripts/build-skill-mirrors.cjs" >&2; exit 2; }` (exit 2, the tracked hook's convention).

2. scripts/install-pre-commit.sh: three edits following the existing per-guard convention exactly:
- Add `grep -q "build-skill-mirrors.cjs --check" "$HOOK_PATH"` to the idempotency AND-chain (line ~37) and mention skill-mirrors in the already-installed echo.
- Add a new `HOOK_TRAILER_SKILL_MIRRORS` heredoc block (after the HOOK_TRAILER_CMD_REG block, guarded by `if ! grep -q "build-skill-mirrors.cjs --check" "$HOOK_PATH"`) with the same comment header, the same trigger regex, `$REPO_ROOT_PLACEHOLDER` paths, and `exit 1` on failure (the installer-family convention, not exit 2).
- Add the matching block to the fresh-install HOOK_BODY heredoc at the bottom (with `\$` escaped regex anchor like its siblings, since HOOK_BODY is an unquoted heredoc).

3. Refresh the installed hook (the .git/hooks/pre-commit on this machine is stale; this fixes that too): run `bash scripts/setup-hooks.sh` (copies the updated tracked hook to the effective hooks path) then `bash scripts/install-pre-commit.sh` (splices ALL missing trailer-family guards, including the new one, before the terminal exit 0). Then verify the new trigger landed BEFORE the terminal `exit 0` in the installed hook. If setup-hooks.sh declines to overwrite for any reason, install-pre-commit.sh's splice alone still lands the new guard; the grep verification below is the arbiter.

No em-dashes anywhere.
  </action>
  <verify>
    <automated>grep -c "build-skill-mirrors.cjs" scripts/hooks/pre-commit scripts/install-pre-commit.sh && HOOK=$(git rev-parse --git-path hooks/pre-commit) && grep -q "build-skill-mirrors.cjs\" --check" "$HOOK" && awk '/build-skill-mirrors/{found=NR} /^exit 0/{last=NR} END{exit !(found && found<last)}' "$HOOK" && echo "installed hook wired before terminal exit 0"</automated>
  </verify>
  <done>Tracked hook has the guard (exit 2); installer has grep-presence + trailer + fresh-install-body blocks (exit 1, >=2 invocations of build-skill-mirrors.cjs --check across families); installed hook contains the trigger before its terminal exit 0. A dry run proves the trigger fires: stage a temporary whitespace edit to any commands/*.md with a deliberately-broken mirror expectation is NOT needed here (the tripwire test in Task 4 locks behavior); just confirm `git stash`-safe staging of a real commands file plus `bash "$HOOK"` style execution is not required. Grep evidence is sufficient for this task.</done>
</task>

<task type="auto">
  <name>Task 3: verify-release step 10b + doctor coverage-gate fold-in</name>
  <files>scripts/verify-release, scripts/doctor.cjs</files>
  <action>
1. scripts/verify-release: add a new section `10b. SKILL MIRRORS` immediately after section 10 (Skills), following the 260705-jeq step-7b convention (lettered substep, boxed header comment, echo header). Comment header explains: quick task 260705-sy9; every command is mirrored into skills/ as the Windows registration workaround (260705-ob7); a stale mirror ships a wrong command body to affected machines, so drift blocks the release; the check also verifies the SKIP_LIST skill is present and divergent. Body:

```
MIRROR_OUT=$(node "$PLUGIN_ROOT/scripts/build-skill-mirrors.cjs" --check 2>&1) && MIRROR_CODE=0 || MIRROR_CODE=$?
if [ "$MIRROR_CODE" -ne 0 ]; then
  fail "Skill-mirror check FAILED (stale/missing mirror or broken skip-list skill):"
  echo "$MIRROR_OUT" | head -10
else
  pass "Skill mirrors in sync with commands/ (build-skill-mirrors --check)"
fi
```

2. scripts/doctor.cjs: in the `coverage-gate` acceptance point (line ~2950), add to the gates[] array, after the render entry and BEFORE the advisory shape-declaration entry (keeping blocker-semantics gates grouped):

```
// Quick task 260705-sy9: the skill-mirror staleness gate rides the SAME
// coverage-gate organ (the Phase 178-03 render-gate precedent). A stale
// skills/<name>/SKILL.md mirror ships a wrong command body to machines
// hit by the Claude Code registration bug (260705-ob7). Blocker semantics:
// the script exits 1 on any missing/stale mirror or a deleted/reverted
// SKIP_LIST skill. Canon Part 8: local byte compares only.
{ id: 'skill-mirrors', script: 'build-skill-mirrors.cjs' },
```

Update the point's `label` string to include `+ skill-mirrors` (e.g. `connector + orchestration-projection + render-coverage + skill-mirrors gates pass ...`). Do NOT add a new acceptance point; doctor stays 14/14 per the fold-in decision documented above. Touch nothing else in doctor.cjs.

No em-dashes.
  </action>
  <verify>
    <automated>bash scripts/verify-release 2>&1 | grep -A1 "10b\|Skill" | head -6 && node scripts/doctor.cjs --acceptance 2>&1 | tail -5</automated>
  </verify>
  <done>verify-release shows the 10b section passing on the clean tree; doctor --acceptance passes 14/14 with the coverage-gate label mentioning skill-mirrors; the gates[] array runs build-skill-mirrors.cjs --check with blocker semantics.</done>
</task>

<task type="auto">
  <name>Task 4: Tripwire test, full gate sweep, atomic commit</name>
  <files>tests/test-skill-mirrors-tripwire.cjs</files>
  <action>
1. Create tests/test-skill-mirrors-tripwire.cjs mirroring the structure of tests/test-connector-tripwire.cjs (assert helper, failure counter, exit 1 on any FAIL) blended with the static wiring proofs of tests/test-render-gate-wiring.cjs. Cases:

- Wiring proof, tracked hook: scripts/hooks/pre-commit source contains a guard invoking `build-skill-mirrors.cjs" --check` with fail-closed `exit 2`, path-scoped by the trigger regex `^(commands/.*\.md|skills/.*/SKILL\.md)$`.
- Trigger-regex behavioral cases (the constraint's synthetic staged-file cases): extract the trigger ERE from the tracked hook source (regex-match the `grep -qE '...'` line of the skill-mirrors block), construct `new RegExp(...)` from it, and assert: `commands/foo.md` MATCHES, `skills/foo/SKILL.md` MATCHES, and negative controls `lib/core/navigation.cjs`, `docs/foo.md`, `skills/foo/README.md` do NOT match. This proves an unrelated staged file never triggers the gate.
- Wiring proof, installer: scripts/install-pre-commit.sh has the grep-presence guard for `build-skill-mirrors.cjs --check`, invokes it in BOTH install-block families (>=2 `build-skill-mirrors.cjs" --check` invocations), each fail-closed with `exit 1` (the render-gate-wiring test's counting technique).
- Wiring proof, verify-release: source contains a `build-skill-mirrors.cjs" --check` invocation gated by fail().
- Wiring proof, doctor: scripts/doctor.cjs source contains `{ id: 'skill-mirrors', script: 'build-skill-mirrors.cjs' }` inside the coverage-gate gates[] region.
- Clean tree: spawnSync `node scripts/build-skill-mirrors.cjs --check` exits 0 (the SAME code path the hook runs, mirroring connector-tripwire case 5).
- SKIP_LIST synthetic cases via the Task 1 exports: build a temp fixture under fs.mkdtempSync (os.tmpdir) with `commands/fake.md` containing a few lines; (a) no skills/fake/SKILL.md and `checkSkipList({skipList:['fake'], commandsDir, skillsDir})` returns one failure containing `SKIP-LIST MISSING`; (b) write skills/fake/SKILL.md byte-identical to the command and assert one failure containing `SKIP-LIST NOT DIVERGENT`; (c) change one byte in SKILL.md and assert zero failures; (d) real-tree negative control: `checkSkipList()` with defaults returns [].

House rules in the test header comment: hyphens only, pure-local, zero Brain/network.

2. Full existing gate sweep, all must be green (nothing regressed):
- node scripts/build-connector-registry.cjs --check
- node scripts/check-shape-declaration.cjs --check
- node scripts/check-render-coverage.cjs --check
- node scripts/check-help-coverage.cjs
- node scripts/build-orchestration-projection.cjs --check
- node scripts/build-skill-mirrors.cjs --check
- node scripts/doctor.cjs --acceptance (must report 14/14)
- node tests/test-skill-mirrors-tripwire.cjs
- node tests/test-connector-tripwire.cjs and node tests/test-render-gate-wiring.cjs (regression controls: this task edited install-pre-commit.sh and the doctor coverage-gate organ those tests inspect)

3. Em-dash sweep over every touched file: `grep -n $'—' scripts/build-skill-mirrors.cjs scripts/hooks/pre-commit scripts/install-pre-commit.sh scripts/verify-release scripts/doctor.cjs tests/test-skill-mirrors-tripwire.cjs` must return nothing.

4. Atomic commit of all six files in ONE commit (the pre-commit hook itself will exercise the new trigger since commands/skills are not staged; scripts/ changes do not match the new trigger regex, which is itself a correct negative-control observation). Message: `quick(260705-sy9): wire build-skill-mirrors --check into pre-commit, verify-release, doctor acceptance + SKIP_LIST hardening`. Do not commit .planning/ artifacts unless the quick workflow requires it (`.planning/` is gitignored; use git add -f only for the quick-task dir if the workflow says so).
  </action>
  <verify>
    <automated>node tests/test-skill-mirrors-tripwire.cjs && node tests/test-connector-tripwire.cjs && node tests/test-render-gate-wiring.cjs && node scripts/doctor.cjs --acceptance 2>&1 | tail -3</automated>
  </verify>
  <done>All sweep gates green, doctor 14/14, new tripwire test passes with positive and negative trigger cases plus all four SKIP_LIST cases, em-dash sweep clean, exactly one commit containing the 5 modified scripts + 1 new test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | All changes are local git-hook / release-gate / test wiring; no untrusted input crosses a new boundary, no package installs, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-sy9-01 | Tampering | skills/*/SKILL.md mirrors drifting from commands/*.md | mitigate | this entire task: --check enforced at pre-commit (exit 2), verify-release (fail), doctor --acceptance (blocker) |
| T-sy9-02 | Tampering | hand-authored SKIP_LIST skill silently deleted or flattened to a copy | mitigate | Task 1 checkSkipList verification inside --check |
| T-sy9-03 | Denial of Service | hook false-fires on unrelated files, blocking commits | mitigate | path-scoped trigger regex + negative-control cases in the tripwire test |
</threat_model>

<verification>
- node scripts/build-skill-mirrors.cjs --check exits 0 and mentions skip-list verification
- Tracked hook, installed hook, installer, verify-release, and doctor all reference build-skill-mirrors.cjs --check (grep-provable)
- doctor --acceptance 14/14 (fold-in, no new point)
- Full gate sweep green: connector, shape-declaration, render-coverage, help-coverage, orchestration-projection, skill-mirrors, doctor
- tests/test-skill-mirrors-tripwire.cjs, test-connector-tripwire.cjs, test-render-gate-wiring.cjs all pass
- No em-dashes in any touched file; one atomic commit
</verification>

<success_criteria>
A commands/*.md edit staged without regenerating mirrors is rejected at commit time on this machine AND on any fresh clone that runs the installers; the same drift also blocks verify-release and doctor --acceptance; the trending-to-absurd hand-authored skill cannot silently vanish or revert to a plain copy; all pre-existing gates remain green at 14/14.
</success_criteria>

<output>
Create `.planning/quick/260705-sy9-wire-build-skill-mirrors-cjs-check-into-/260705-sy9-SUMMARY.md` when done.
</output>
