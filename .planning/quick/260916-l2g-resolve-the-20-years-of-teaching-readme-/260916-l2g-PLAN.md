---
phase: quick-260916-l2g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/THE-BRAIN.md
autonomous: true
requirements:
  - QUICK-260916-l2g
must_haves:
  truths:
    - "No bare duration-of-teaching claim of any form survives in README.md or docs/THE-BRAIN.md: not '20 years of teaching', not '30+ years of teaching', not 'decades of teaching'"
    - "All 6 rewritten phrases read naturally inside their own sentence shape: mid-sentence 'built from ___', standalone tagline 'built and tested through ___', list-item appositive, compact table cell, sentence-opening 'Built from ___.', and a parenthetical clause whose verb agrees with its new subject"
    - "Every rewritten sentence keeps the substance it had before: the methodology comes from real academic teaching practice and pedagogical rigor, not theory. Only the duration claim is gone"
    - "No sentence, heading, table row, or section outside the 6 target phrases is altered in either file"
    - "README.md and docs/THE-BRAIN.md both still pass tests/test-250-doctrine-fence.cjs (both are in its LIVING_DOCS_FILES set)"
    - "Both files still contain zero em-dash characters"
    - "The 5 out-of-scope internal docs carrying the same figure are listed in the SUMMARY as follow-up, and none is modified"
  artifacts:
    - path: "README.md"
      provides: "Public front door with every duration-of-teaching claim replaced by qualitative grounding language at all 5 of its sites"
      contains: "accumulated"
    - path: "docs/THE-BRAIN.md"
      provides: "Brain architecture doc with its single duration claim replaced by the same grounding language"
      contains: "accumulated"
  key_links:
    - from: "README.md"
      to: "tests/test-250-doctrine-fence.cjs"
      via: "LIVING_DOCS_FILES membership, so every edit here is gated by that test"
      pattern: "'README\\.md'"
    - from: "docs/THE-BRAIN.md"
      to: "tests/test-250-doctrine-fence.cjs"
      via: "LIVING_DOCS_FILES membership, so every edit here is gated by that test"
      pattern: "'docs/THE-BRAIN\\.md'"
---

<objective>
The repo makes contradicting claims about how long the PWS methodology has been taught: README.md says "20 years of teaching" in 4 places and "decades of teaching" in a 5th, while docs/THE-BRAIN.md says "30+ years of teaching". A reader who opens both sees the product contradict itself about its own provenance, which undercuts the exact claim those sentences exist to make.

The Navigator's ruling on which number to standardize on, verbatim: "Neither. Based on academic accumulated expertise and pedagogical rigor." So this is not a pick-a-number reconciliation. The ruling is against the bare duration-of-teaching claim as a class, which is why the unnumbered "decades of teaching" site is in scope alongside the five numbered ones. All 6 get qualitative language describing what the methodology is actually built from.

Purpose: a duration figure is a brittle, unverifiable way to assert authority, and this bug proves it drifts. Qualitative grounding says the same true thing without a claim that forks across files.

Output: 6 phrase-level edits across 2 files. Nothing else in either file changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@README.md
@docs/THE-BRAIN.md
</context>

<scope_fence>
IN SCOPE: exactly 6 phrase occurrences in exactly 2 files.

OUT OF SCOPE, do not touch, name in the SUMMARY as follow-up: references/personality/lexicon.md, docs/METHODOLOGY-SEQUENCE-ANALYSIS.md, docs/INTELLIGENCE-REPORT.md, docs/research/*.md, RESEARCH_16. These are internal or historical documentation and the task brief rules them out explicitly.

The task brief enumerated 5 sites. A 6th (README.md "decades of teaching") was found during planning and folded in by coordinator ruling, on the grounds that the Navigator's ruling targets the duration claim as a class, not two specific numerals. That is the only scope change from the brief; nothing else is added.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Replace the duration-of-teaching claim at all 6 sites</name>
  <files>README.md, docs/THE-BRAIN.md</files>
  <action>
Re-read both files fresh before editing. README.md was edited earlier today by quick task 260916-kfc (commits 2f1c8fc9e, 16234284f, c086706e4), so locate each site by its surrounding text, never by a remembered line number. Line numbers below are the planning-time reading and are informational only.

Make exactly 6 edits. Each replaces ONLY the quoted phrase; the rest of each sentence stays byte-identical, including punctuation, neighboring capitalization, parentheses, and the markdown table pipes.

Every find string below was checked at planning time to match exactly once in its file. Site 1's string is deliberately prefixed with "methodology graph" because the shorter form "built from 20 years of teaching" matches BOTH site 1 and site 3; do not shorten it.

Site 1, README.md (~line 10, mid-sentence, "built from ___"):
  find:    methodology graph built from 20 years of teaching
  replace: methodology graph built from accumulated academic teaching experience

Site 2, README.md (~line 12, the header provenance tagline, "built and tested through ___"):
  find:    built and tested through 20 years of teaching.
  replace: built and tested through accumulated academic teaching experience and pedagogical rigor.
  This is the one site carrying the full ruling phrase, because it is the standalone provenance sentence and has room for it.

Site 3, README.md (~line 37, appositive inside the numbered loop list):
  find:    452 frameworks built from 20 years of teaching,
  replace: 452 frameworks built from accumulated academic teaching expertise,

Site 4, README.md (~line 102, three-layer table cell, needs a short noun phrase because the cell is already a comma-separated attribute list):
  find:    452 frameworks, 20 years of teaching, served over MCP
  replace: 452 frameworks, accumulated teaching expertise, served over MCP

Site 5, README.md (~line 129, parenthetical clause inside the four-part "MindrianOS adds what a raw model structurally cannot be" sentence):
  find:    it knows when (decades of teaching calibrate which method fits which stage)
  replace: it knows when (accumulated teaching experience calibrates which method fits which stage)
  Verb agreement matters here and is already handled: the plural subject "decades" took "calibrate", the singular "experience" takes "calibrates". Do not leave the old plural verb behind. The other three parenthetical clauses in that sentence are untouched.

Site 6, docs/THE-BRAIN.md (~line 41, sentence opener, "Built from ___."):
  find:    Built from 30+ years of teaching.
  replace: Built from accumulated academic teaching expertise and pedagogical rigor.
  The two sentences that follow ("Relationships DISCOVERED through watching 100+ students apply frameworks. Chaining rules come from real classroom observation, not theory.") stay untouched. They already carry the real-practice-not-theory substance, which is why this site does not restate it.

Preserve "100+ students" and every non-duration figure (27,951 nodes, 452 frameworks). Only the teaching-duration claims go. Do not introduce em-dashes; house rule is hyphens only and both files are in the doctrine fence's LIVING_DOCS_FILES set.

Note on the verify gate: at planning time the combined duration pattern matched exactly 6 real prose lines in these 2 files (5 in README.md, 1 in docs/THE-BRAIN.md) and zero comment lines, so an absence gate is sound here.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin; ! grep -qE '(([0-9]+\+? years)|decades) of teaching' README.md docs/THE-BRAIN.md</automated>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin; ! grep -qP '\x{2014}' README.md docs/THE-BRAIN.md</automated>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin; git diff --stat README.md docs/THE-BRAIN.md</automated>
  </verify>
  <done>Zero matches for any duration-of-teaching pattern ("N years", "N+ years", "decades") in either file. Zero em-dashes in either file. git diff shows changes confined to 6 lines total, 5 in README.md and 1 in docs/THE-BRAIN.md, with no other line touched.</done>
</task>

<task type="auto">
  <name>Task 2: Run the doctrine fence, read the 6 sentences back, write the SUMMARY</name>
  <files>tests/test-250-doctrine-fence.cjs (read only, not modified)</files>
  <action>
Run the doctrine fence test. Both edited files sit in its LIVING_DOCS_FILES array, so this is the gate proving the edits introduced no banned doctrine phrase into a living doc. Baseline before this plan was 6 of 6 passing; it must still be 6 of 6.

Then read each of the 6 edited sentences back in full, in place. Confirm each reads naturally on its own. The six sites have different grammatical shapes on purpose: if any one reads awkwardly after substitution (a doubled "accumulated", a broken appositive, a table cell that no longer parses as a comma list, a subject-verb disagreement in the site 5 parenthetical), fix that ONE site's wording rather than forcing an identical string everywhere. The constraint is natural reading plus preserved meaning, not mechanical uniformity.

Confirm the substance survived at every site: each sentence must still tell the reader the methodology comes from real academic teaching practice, not theory. A site that now reads as a vague credential with no teaching grounding left is a failed reword, not a pass.

Then write the SUMMARY. It must record: the 6 before/after pairs verbatim; the Navigator's ruling quoted verbatim as the reason no number was chosen; that site 5 ("decades of teaching") was found during planning beyond the task brief's enumerated 5 and folded in by coordinator ruling because the Navigator's ruling targets the duration claim as a class; and the out-of-scope follow-up list (references/personality/lexicon.md, docs/METHODOLOGY-SEQUENCE-ANALYSIS.md, docs/INTELLIGENCE-REPORT.md, docs/research/*.md, RESEARCH_16), which still carry a year-count figure and are now inconsistent with the two public-facing files. That remaining inconsistency is a documented accepted state, not an oversight.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin; node --test tests/test-250-doctrine-fence.cjs</automated>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin; grep -cE 'accumulated (academic )?teaching (experience|expertise)' README.md docs/THE-BRAIN.md</automated>
    <human-check>Read the 6 rewritten sentences in place. Each reads naturally in its own grammatical shape and still says the methodology comes from real academic teaching practice, not theory.</human-check>
  </verify>
  <done>Doctrine fence reports zero failures. The grounding-phrase count returns 5 for README.md and 1 for docs/THE-BRAIN.md. Each of the 6 sentences has been read back in full and reads naturally, with site 5's verb agreeing with its new subject. SUMMARY records the 6 before/after pairs, the verbatim Navigator ruling, the site 5 scope note, and the out-of-scope follow-up list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo to public reader | README.md is the npm package front page and the GitHub landing page; every claim in it is a public assertion |

No new code paths, no new dependencies, no package-manager installs, no network surface. This is a prose edit to two tracked markdown files, so the Brain boundary (Canon Part 8), the tri-polar three-surface rule, and the release lockstep are all untouched by it.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l2g-01 | Information disclosure (false public claim) | README.md provenance sentences | mitigate | The change itself: an unverifiable duration claim that already forked into three variants is replaced with a qualitative claim that is true and does not drift |
| T-l2g-02 | Tampering (collateral edit to a living doc) | README.md, docs/THE-BRAIN.md | mitigate | git diff --stat gate in Task 1 asserts exactly 6 changed lines; doctrine fence in Task 2 asserts both living docs still pass |
| T-l2g-03 | Repudiation (silent scope drift) | out-of-scope docs still carrying the figure | accept | Fixed in scope_fence and required in the SUMMARY as named follow-up, so the remaining inconsistency is tracked rather than invisible |
| T-l2g-SC | Tampering (supply chain) | n/a | accept | No npm, pip, or cargo install in this plan, so no package legitimacy gate applies |
</threat_model>

<verification>
- `! grep -qE '(([0-9]+\+? years)|decades) of teaching' README.md docs/THE-BRAIN.md` exits 0
- `! grep -qP '\x{2014}' README.md docs/THE-BRAIN.md` exits 0
- `node --test tests/test-250-doctrine-fence.cjs` passes (baseline was 6 of 6)
- `git diff --stat README.md docs/THE-BRAIN.md` shows 6 changed lines total, 5 plus 1
- `grep -c 'calibrates which method fits which stage' README.md` returns 1, confirming site 5's verb agreement landed
</verification>

<success_criteria>
- No bare duration-of-teaching claim survives in either public-facing file, resolved by removing the claim class rather than picking a side, per the Navigator's ruling
- All 6 rewritten sentences read naturally and keep the real-teaching-practice substance
- Nothing outside those 6 phrases changed in either file
- The doctrine fence is still green
- The 5 out-of-scope internal docs are recorded as follow-up, not silently dropped
</success_criteria>

<output>
Create `.planning/quick/260916-l2g-resolve-the-20-years-of-teaching-readme-/260916-l2g-SUMMARY.md` when done
</output>
