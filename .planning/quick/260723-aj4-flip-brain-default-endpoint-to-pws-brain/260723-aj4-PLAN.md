---
phase: quick-260723-aj4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/brain-client.cjs
  - scripts/rs-thesis-command.cjs
  - scripts/rs-experts-command.cjs
  - scripts/sessionstart-post-update-preflight.cjs
  - lib/core/mcp-profiles.cjs
autonomous: true
requirements: [MEMGRAPH-MIG-STEP4]

must_haves:
  truths:
    - "A fresh brain-client.cjs process with MINDRIAN_BRAIN_URL unset talks to https://pws-brain-mcp.onrender.com, not the old mindrian-brain.onrender.com host"
    - "brain.schema() (or brain.stats()) called through the actual shipped module, new default, real MINDRIAN_BRAIN_KEY, no env override, returns real live data end-to-end (not null, not a raw-fetch stand-in)"
    - "MINDRIAN_BRAIN_URL env override still works exactly as before -- the override mechanism itself is untouched, only the fallback literal changed"
    - "All five brain-client-relevant test suites (test-resolve-brain-key.cjs, security-trifecta.test.cjs, brain-client-query-shape.test.cjs, brain-cache-lru.test.cjs, brain-server-resolution.test.cjs) still pass with the new default"
    - "No em-dashes introduced; no version files (package.json, .claude-plugin/plugin.json, CHANGELOG.md) touched; no release script run"
  artifacts:
    - path: "lib/core/brain-client.cjs"
      provides: "BRAIN_URL default flipped to pws-brain-mcp.onrender.com; header comment (lines 3-6) describes the new default instead of the stale brain.mindrian.ai future-migration mention"
      contains: "pws-brain-mcp.onrender.com"
    - path: ".planning/quick/260723-aj4-flip-brain-default-endpoint-to-pws-brain/260723-aj4-SUMMARY.md"
      provides: "Findings record: default flip landed, six gates + live module-level smoke test all green, flagged (not fixed) follow-ups documented"
  key_links:
    - from: "lib/core/brain-client.cjs BRAIN_URL constant"
      to: "_ensureSession() init handshake fetch + callTool() tool-call fetch"
      via: "both fetch(`${BRAIN_URL}/mcp`, ...) call sites read the same top-of-file constant"
      pattern: "BRAIN_URL"
    - from: "brain.schema() / brain.stats()"
      to: "the live pws-brain-mcp.onrender.com endpoint"
      via: "callTool() -> fetch -> tools/call -> SSE-parsed JSON payload"
      pattern: "pws-brain-mcp.onrender.com"
---

<objective>
Flip the Brain HTTP client's default endpoint from the legacy `mindrian-brain.onrender.com` (Neo4j Aura + Pinecone) to the new Memgraph-backed `pws-brain-mcp.onrender.com` -- step 4 of the approved phased Memgraph migration (steps 1-3: auth-header normalization landed in quick-260722-wom; live drop-in-compatibility confirmed this session against the real production endpoint with a real key).

This is a one-line default-literal change plus stale-comment cleanup. The investigation is done: `brain_search`/`brain_schema`/`brain_stats`/`brain_ask`/`brain_ask_anything` are drop-in compatible today (same response shapes brain-client.cjs already parses); `brain_query`/`brain_write` (raw Cypher) have ALWAYS been admin-gated for regular users on BOTH the old and new servers, so the flip introduces no regression there, and every caller already degrades gracefully to Tier 0.

Purpose: complete the phased cutover so regular users get the Memgraph-backed teaching graph by default, with zero behavior change to the admin-gated raw-Cypher surface and zero regression to the MINDRIAN_BRAIN_URL override mechanism.
Output: flipped default + synced comments + a live end-to-end smoke test proving the shipped module (not a raw fetch stand-in) round-trips real data through the new default.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260722-wom-fix-brain-client-cjs-auth-header-for-mem/260722-wom-SUMMARY.md
@.planning/quick/260722-wom-fix-brain-client-cjs-auth-header-for-mem/260722-wom-RESEARCH.md
@lib/core/brain-client.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the default Brain endpoint and sync the four stale-comment sites</name>
  <files>lib/core/brain-client.cjs, scripts/rs-thesis-command.cjs, scripts/rs-experts-command.cjs, scripts/sessionstart-post-update-preflight.cjs, lib/core/mcp-profiles.cjs</files>
  <action>
    In `lib/core/brain-client.cjs` line 23, change the default URL literal from `'https://mindrian-brain.onrender.com'` to `'https://pws-brain-mcp.onrender.com'`. The `MINDRIAN_BRAIN_URL ||` override mechanism itself is untouched -- only the fallback literal changes.

    Update the file's header doc comment at lines 3-6 (currently: "calls the Brain HTTP server (currently `https://mindrian-brain.onrender.com`, moving to `https://brain.mindrian.ai`; override via the MINDRIAN_BRAIN_URL env var for staging / self-hosted)"). That "moving to brain.mindrian.ai" framing is now stale and wrong -- correct it to describe `https://pws-brain-mcp.onrender.com` as the live default (Memgraph-backed, per the 2026-07-22 Memgraph migration brief step 4), with the same override-var mention preserved. Do not touch any other lines of the header comment (the "Replaces direct MCP tool calls..." Neo4j/Pinecone line at 8-9 is historical context about what this client replaced and stays as-is -- out of scope for this task).

    Re-grep the whole repo (`grep -rn "mindrian-brain.onrender.com" . --include="*.cjs" --include="*.js"`, excluding `node_modules` and `.claude/worktrees`) to re-verify quick-260722-wom's RESEARCH.md finding that the only other live-code hits are descriptive prose in four files (RESEARCH.md's own grep excluded `tests/`; confirm no NEW hardcoded `BRAIN_URL`-style constant exists in any non-test, non-doc, non-planning file). Update ONLY the descriptive-prose mention in each of these four files' comments -- literal string swap `mindrian-brain.onrender.com` to `pws-brain-mcp.onrender.com`, comments only, zero logic changes:
    - `scripts/rs-thesis-command.cjs` line 10 ("routes to the REMOTE Brain (mindrian-brain.onrender.com) when")
    - `scripts/rs-experts-command.cjs` lines 9-10 ("routes to the REMOTE Brain\n * (mindrian-brain.onrender.com) when")
    - `scripts/sessionstart-post-update-preflight.cjs` line 38 ("goes over HTTP to mindrian-brain.onrender.com but THIS preflight does")
    - `lib/core/mcp-profiles.cjs` line 22 ("- brain: mindrian-brain.onrender.com (remote, Streamable HTTP)")

    Do not touch `bin/mindrian-brain-mcp-client.cjs` (confirmed by quick-260722-wom's RESEARCH.md: it is a pure stdio-transport shim with zero network code and no `BRAIN_URL` constant -- nothing to change there). Do not touch `CHANGELOG.md`, `package.json`, or `.claude-plugin/plugin.json` (no version bump -- that happens later via `scripts/release.sh`, not here). No em-dashes in any touched file (hyphens only, repo hard rule).
  </action>
  <verify>
    <automated>grep -n "const BRAIN_URL" lib/core/brain-client.cjs | grep -q "pws-brain-mcp.onrender.com" && ! grep -rln "mindrian-brain\.onrender\.com" scripts/rs-thesis-command.cjs scripts/rs-experts-command.cjs scripts/sessionstart-post-update-preflight.cjs lib/core/mcp-profiles.cjs && node -e "require('./lib/core/brain-client.cjs')" && echo OK</automated>
  </verify>
  <done>brain-client.cjs's BRAIN_URL default literal and header comment (lines 3-6) both reference pws-brain-mcp.onrender.com; the four flagged files' comments are updated to match; bin/mindrian-brain-mcp-client.cjs, CHANGELOG.md, package.json, and .claude-plugin/plugin.json are byte-unchanged; the module still loads without throwing.</done>
</task>

<task type="auto">
  <name>Task 2: Regression gates, live end-to-end smoke test through the shipped module, and findings SUMMARY</name>
  <files>.planning/quick/260723-aj4-flip-brain-default-endpoint-to-pws-brain/260723-aj4-SUMMARY.md</files>
  <action>
    Run the six-gate regression mirror (all must hold), then the live smoke test this session's hand-testing did NOT yet cover, then write findings.

    Six gates:
    1. `node tests/test-resolve-brain-key.cjs` exits 0.
    2. `node lib/memory/security-trifecta.test.cjs` exits 0 -- brain-client's SEC-01/SEC-02 surface intact with the new default.
    3. `node -e "require('./lib/core/brain-client.cjs')"` loads without throwing.
    4. Brain-client-specific test files under `tests/` and `lib/memory/`: run `node lib/memory/brain-client-query-shape.test.cjs`, `node lib/memory/brain-cache-lru.test.cjs`, and `node lib/memory/brain-server-resolution.test.cjs` -- all exit 0 (none of these three hardcode the old hostname as an assertion target, confirmed during planning; they exercise transport/session/naming behavior unrelated to the literal value).
    5. Em-dash gate: grep the em-dash character in all five touched files (lib/core/brain-client.cjs, scripts/rs-thesis-command.cjs, scripts/rs-experts-command.cjs, scripts/sessionstart-post-update-preflight.cjs, lib/core/mcp-profiles.cjs) -- must find nothing.
    6. `grep -n "const BRAIN_URL" lib/core/brain-client.cjs` shows the NEW `pws-brain-mcp.onrender.com` default (byte-check that the flip actually landed).

    Live end-to-end smoke test (the one thing this session's hand-testing used raw `fetch()` for, not the shipped module -- do it properly here): with `MINDRIAN_BRAIN_URL` explicitly unset (`env -u MINDRIAN_BRAIN_URL`) and the real `MINDRIAN_BRAIN_KEY` already present in the environment, run a fresh `node` process that `require()`s the actual `lib/core/brain-client.cjs` module (not a raw fetch) and calls `brain.schema()`. Confirm the result is non-null and contains real schema content (node labels / relationship types), proving the shipped module round-trips through the new default end-to-end, not just a hand-rolled fetch against an overridden URL.

    Then write `260723-aj4-SUMMARY.md` covering: (1) the default flip and comment sync, with a byte-diff summary confirming only the five intended files changed; (2) the live module-level smoke test result (paste the real schema/stats response shape, redacted of nothing since it is generic methodology data, never user content, per Canon Part 8); (3) this session's already-completed research findings restated for the record -- `brain_search`/`brain_schema`/`brain_stats`/`brain_ask`/`brain_ask_anything` drop-in compatible, `brain_query`/`brain_write` admin-gated identically on both old and new servers (no regression), `write()`'s sole caller (`scripts/whitespace-to-brain.cjs`) already tolerant; (4) two FLAGGED (not fixed, out of scope) follow-ups: (a) the new server's `text2cypher` tool errors server-side with "No LLM configured" -- a `pws-brain-mcp` Render deploy-config issue on a different repo, nothing in this repo calls it directly; (b) the Canon Part 8 boundary/egress-detection test suites (`tests/test-169-brain-boundary.cjs`, `tests/test-unknowns-part8-boundary.cjs`, `tests/test-acpt-03-first-material-explore.cjs`, `tests/test-acpt-04-filing-cascade-surfaces.cjs`, `tests/test-acpt-05-brain-derive-tier-rise.cjs`, `tests/test-hmi-poll-primitive.cjs`, `lib/memory/brain-derivation-graceful-degradation.test.cjs`) key their egress-detection token/mock-error lists off the OLD hostname string as the canonical "Brain host" signature; discovered during this plan's whole-repo re-grep (their grep was not excluded from this pass, unlike RESEARCH.md's original test-excluded grep). Post-flip these do not fail -- they are negative-space checks that still pass -- but their detection coverage no longer includes the real new host, a widened blind spot for future Part-8 leak detection. Flag as a follow-up task, not fixed here (outside the explicitly given file-change scope for this quick task). No em-dashes in the SUMMARY.
  </action>
  <verify>
    <automated>node tests/test-resolve-brain-key.cjs && node lib/memory/security-trifecta.test.cjs && node lib/memory/brain-client-query-shape.test.cjs && node lib/memory/brain-cache-lru.test.cjs && node lib/memory/brain-server-resolution.test.cjs && ! grep -q "$(printf '\342\200\224')" lib/core/brain-client.cjs scripts/rs-thesis-command.cjs scripts/rs-experts-command.cjs scripts/sessionstart-post-update-preflight.cjs lib/core/mcp-profiles.cjs && grep -q "pws-brain-mcp.onrender.com" lib/core/brain-client.cjs && test -f .planning/quick/260723-aj4-flip-brain-default-endpoint-to-pws-brain/260723-aj4-SUMMARY.md</automated>
  </verify>
  <done>All six regression gates hold; the live module-level smoke test (fresh process, MINDRIAN_BRAIN_URL unset, real key, actual brain-client.cjs require) returns real schema/stats data through the new default; SUMMARY.md exists and records the flip, the live smoke-test proof, the restated drop-in-compatibility findings, and both flagged (not fixed) follow-ups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local process -> Brain endpoint | The default host regular users' Bearer key traffic goes to changes; the override mechanism itself does not |
| Canon Part 8 egress detectors -> real Brain host | Test suites that assert LOCAL artifacts never contain a Brain-host string now have a real host they were not written against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-aj4-01 | Spoofing | brain-client.cjs BRAIN_URL default | mitigate | byte-check gate (`grep "const BRAIN_URL"`) plus `git diff` scope limited to the five intended files proves exactly one literal changed, not an unrelated host |
| T-aj4-02 | Tampering | MINDRIAN_BRAIN_URL override path | accept | override mechanism itself is untouched this task (only the fallback literal moved); already covered by existing resolver tests |
| T-aj4-03 | Information disclosure | brain_query / brain_write admin gate | accept | confirmed this session via live testing: both old and new servers identically admin-gate raw Cypher for regular keys; the flip introduces no new disclosure surface |
| T-aj4-04 | Denial of service | new host cold-start / downtime | accept | existing BRAIN_REQUEST_TIMEOUT_MS + graceful null-return Tier-0 fallback already covers any endpoint; unchanged by this flip |
| T-aj4-05 | Information disclosure | Part 8 egress-detection test suites keyed to the old hostname | accept | flagged as a documented follow-up in SUMMARY.md (widened blind spot, not a new leak -- negative-space checks still pass); fixing the token lists is outside this quick task's given file-change scope |
| T-aj4-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed by this plan; no dependency surface touched |
</threat_model>

<verification>
- `grep -n "const BRAIN_URL" lib/core/brain-client.cjs` shows the pws-brain-mcp.onrender.com default
- Six regression gates (resolver test, security-trifecta, require-smoke, three brain-client-specific test files, em-dash grep) all exit 0
- Live smoke test through the actual brain-client.cjs module (MINDRIAN_BRAIN_URL unset, real key) returns real schema/stats data via the new default
- `git diff --stat` touches exactly the five intended files -- no CHANGELOG.md, package.json, or .claude-plugin/plugin.json changes
</verification>

<success_criteria>
- Regular users get the Memgraph-backed pws-brain-mcp.onrender.com endpoint by default with zero code change required for brain_search/brain_schema/brain_stats/brain_ask/brain_ask_anything
- MINDRIAN_BRAIN_URL override mechanism unchanged; only the fallback literal moved
- The four stale-comment files describe the new endpoint accurately; bin/mindrian-brain-mcp-client.cjs and all version-surface files remain byte-unchanged
- A live end-to-end test through the actual shipped module (not a raw fetch stand-in) proves the flip works before this quick task is considered done
- SUMMARY.md documents the flip, the live proof, and both flagged (not fixed) follow-ups (text2cypher server-config issue, Part 8 egress-detector blind spot)
</success_criteria>

<output>
Create `.planning/quick/260723-aj4-flip-brain-default-endpoint-to-pws-brain/260723-aj4-SUMMARY.md` when done
</output>
