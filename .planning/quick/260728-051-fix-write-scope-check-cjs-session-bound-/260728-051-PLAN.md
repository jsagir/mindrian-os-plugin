---
phase: quick-260728-051
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/session-binding.cjs
  - scripts/write-scope-check.cjs
  - tests/test-write-scope-sub-room-ancestor.cjs
autonomous: true
requirements: [QUICK-260728-051]

must_haves:
  truths:
    - "A session bound to a parent room (e.g. motj-ecosystem) can write into that parent's registered sub-rooms (e.g. jonathan-contractor-motj) without a second, separate room_bind call for the child"
    - "A session bound to a parent room still cannot write into an unrelated, unbound room -- ancestor widening never over-widens to siblings or unrelated rooms"
    - "Exact-match write authorization (session bound directly to the target room, no ancestor lookup needed) is byte-for-byte unchanged"
    - "A registered room with no `parent` field, or a registry read failure, degrades to zero ancestor widening and never crashes or hard-blocks the hook (fail-open preserved, matching the file's existing contract)"
    - "All existing 2-arg call sites of isRoomInWriteScope(room, binding) across the repo (test-write-scope-set-membership.test.cjs, test-room-birth.cjs, test-rooms-open-actually-switches.cjs, lib/core/navigation/room-birth.cjs) continue to behave identically with no code change required at those call sites"
  artifacts:
    - path: "lib/core/session-binding.cjs"
      provides: "isRoomInWriteScope(room, binding, ancestorChain) with a new optional 3rd param; a room is in scope if it is the exact bound room OR any entry in ancestorChain is bound"
      contains: "function isRoomInWriteScope(room, binding, ancestorChain)"
    - path: "scripts/write-scope-check.cjs"
      provides: "resolveAncestorChain(root, slug) -- walks registry.rooms[slug].parent upward, depth-bounded, cycle-safe, never throws -- wired into the session-bound branch of main()"
      contains: "function resolveAncestorChain"
    - path: "tests/test-write-scope-sub-room-ancestor.cjs"
      provides: "unit coverage of the 3-arg isRoomInWriteScope contract plus E2E fixture coverage (spawnSync) of the full write-scope-check.cjs hook proving parent-to-child widening, no over-widening, exact-match preservation, and fail-open on a missing parent field / corrupt registry"
      contains: "resolveAncestorChain"
  key_links:
    - from: "scripts/write-scope-check.cjs main() session-bound branch"
      to: "lib/core/session-binding.cjs isRoomInWriteScope"
      via: "sessionBinding.isRoomInWriteScope(targetRoom, binding, ancestorChain)"
      pattern: "isRoomInWriteScope\\(targetRoom, binding, ancestorChain\\)"
    - from: "scripts/write-scope-check.cjs resolveAncestorChain"
      to: "~/MindrianRooms/.rooms/registry.json rooms[slug].parent"
      via: "JSON.parse(fs.readFileSync(regPath)) walk of the parent chain"
      pattern: "resolveAncestorChain\\(realRoot, targetRoom\\)"
---

<objective>
Fix `scripts/write-scope-check.cjs` / `lib/core/session-binding.cjs::isRoomInWriteScope` so a session bound to a PARENT room automatically authorizes writes into that parent's REGISTERED sub-rooms, closing the nesting-unaware gap confirmed live today: `room_bind({room:"motj-ecosystem"})` succeeds, but a `Write` into `motj-ecosystem/sub-rooms/jonathan-contractor-motj/...` is blocked with "This session is bound to [motj-ecosystem]" until a SECOND, separate `room_bind({room:"jonathan-contractor-motj"})` call is made. `targetRoomUnderRoot` (the TARGET side of this hook) is already nesting-aware via SEED-004's `.room-root` walk-up; `isRoomInWriteScope` (the SCOPE side) is a flat SET-membership check with zero hierarchy awareness, even though the registry already carries a `parent` field on every sub-room entry (confirmed live: `jonathan-contractor-motj` -> `"parent": "motj-ecosystem"`).

Purpose: binding to a parent room should reasonably extend write scope to its own registered children, the same way the target-resolution side already treats the MindrianRooms tree as fractal/nested (SEED-004). This is a pure widening of an existing allow-path -- no existing block or allow decision may change for any room that has no ancestor relationship to the session's bound set.

Output: `isRoomInWriteScope` gains an optional 3rd param (`ancestorChain`, default effectively `[]`) that widens the set-membership check without touching any 2-arg call site's behavior. `write-scope-check.cjs` gains a small, depth-bounded, cycle-safe, never-throwing `resolveAncestorChain(root, slug)` helper (reusing the file's existing `ROOM_ROOT_MAX_DEPTH` constant and registry-read pattern) that resolves this chain and feeds it into the existing session-bound branch. A new fixture-based test file proves both the RED state (today's bug, reproduced via a spawned real hook process) and the GREEN fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/core/session-binding.cjs
@scripts/write-scope-check.cjs

**Root cause is already fully diagnosed and live-reproduced this session -- do NOT re-investigate.** `targetRoomUnderRoot` (write-scope-check.cjs) correctly resolves a nested write's REGISTERED slug via a `.room-root` walk-up + registry reverse-match (SEED-004). `isRoomInWriteScope` (session-binding.cjs) is a flat `binding.bound.indexOf(room)` check with no concept of the registry's `parent` field. Fix the SCOPE side to match the TARGET side's nesting-awareness, purely additively.

**Reference implementation to extract the read/walk pattern FROM (do not restructure, only add alongside):** `scripts/write-scope-check.cjs`'s `slugForRoomDir(root, sentinelDir)` (registry-read pattern: `path.join(root, '.rooms', 'registry.json')`, `JSON.parse(fs.readFileSync(...))`, try/catch returning null/[] on any failure) and `walkUpToRoomRoot` (depth-bounded walk using the existing `ROOM_ROOT_MAX_DEPTH = 12` constant, already defined at line 140). The new `resolveAncestorChain` helper reuses BOTH patterns: the registry-read shape of `slugForRoomDir` and the depth-bound/never-throw discipline of `walkUpToRoomRoot`.

**Design intentionally reads the CHILD's `.parent` field, not the parent's `sub_rooms` array.** Live registry data confirms `motj-ecosystem.sub_rooms` is a stale, incomplete list (missing at least `jonathan-contractor-motj`), while `jonathan-contractor-motj.parent === "motj-ecosystem"` is present and correct. This is a known, separate data-quality gap in the parent's list -- out of scope for this fix. Do NOT edit `~/MindrianRooms/.rooms/registry.json` as part of this plan; a one-line code comment noting the gap is sufficient.

**Existing tests that already call `isRoomInWriteScope` with 2 args and must keep passing unmodified:** `tests/test-write-scope-set-membership.test.cjs`, `tests/test-room-birth.cjs`, `tests/test-rooms-open-actually-switches.cjs`. None of these files should need editing -- the new 3rd param must be fully optional and backward compatible.

**Precedent test harness to mirror for the E2E fixture tests (Task 2):** `scripts/83-scope-injection.test.cjs`'s SEED-004 section (bottom third of the file) -- `mkFixtureDir`, `mkFixtureMindrianRoomsRoot(parent, {registry, rooms})`, `mkNestedRoom(root, relPath)` (creates the dir chain + drops a `.room-root` sentinel + `ROOM.md`), and `runWriteScopeCheck`-style `spawnSync('node', [WRITE_SCOPE_CHECK], {env, input: JSON.stringify(payload), encoding: 'utf8'})` returning `res.status` (0 = allow, 2 = block). `scripts/write-scope-check.cjs` has NO `module.exports` (it runs `main()` as a side effect on load via stdin), so this spawn-based approach is the ONLY way to exercise its behavior -- do not attempt to `require()` it directly.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend isRoomInWriteScope with an optional ancestor-chain param and unit-test the contract</name>
  <files>lib/core/session-binding.cjs, tests/test-write-scope-sub-room-ancestor.cjs</files>
  <behavior>
    New optional 3rd parameter `ancestorChain` on `isRoomInWriteScope(room, binding, ancestorChain)`:
    - Test 1: `room` not directly in `binding.bound`, but present in `ancestorChain` (e.g. `ancestorChain = ['motj-ecosystem']`, `binding.bound = ['motj-ecosystem']`) -> returns `true` (the widening this fix adds; this is the RED assertion -- fails against today's 2-param implementation).
    - Test 2: `room` not in `binding.bound`, and no entry of `ancestorChain` is in `binding.bound` either -> returns `false` (no over-widening to unrelated rooms).
    - Test 3: `room` IS directly in `binding.bound` (exact match), regardless of what `ancestorChain` contains -> returns `true` (pre-existing exact-match path untouched, direct match wins before any ancestor-chain lookup).
    - Test 4: called with only 2 args (`ancestorChain` omitted/undefined) -> identical behavior to the pre-fix 2-arg contract (mirrors the existing off-bound-room-returns-false assertion in tests/test-write-scope-set-membership.test.cjs; this proves backward compatibility, not a new capability).
    - Test 5: `room === '__no_room__'` -> `true` regardless of `binding` or `ancestorChain` contents (the pre-existing NO_ROOM_SLUG bypass fires before any ancestor-chain code runs).
    - Test 6: `binding.bound` is empty (unbound session) -> `true` regardless of `ancestorChain` (the pre-existing unbound-session allow-all fires before any ancestor-chain code runs).
    - Test 7: `ancestorChain` is not an array (e.g. a string, or `null`) -> does not throw, degrades to no widening (same result as `ancestorChain = []`).
  </behavior>
  <action>
    In lib/core/session-binding.cjs, extend the existing `isRoomInWriteScope(room, binding)` function signature to `isRoomInWriteScope(room, binding, ancestorChain)`. Preserve every existing early-return exactly in order (the `NO_ROOM_SLUG` room check, the `bound.indexOf(NO_ROOM_SLUG)` dev-repo-session check, the `bound.length === 0` unbound-session check) -- these three continue to fire before any ancestor logic, unchanged. After the existing `bound.indexOf(room) !== -1` exact-match check (which now returns `true` immediately on a hit, same as today), add: coerce `ancestorChain` to a safe array via `Array.isArray(ancestorChain) ? ancestorChain : []`, then check whether ANY entry of that coerced array is present in `bound` (e.g. via `.some(a => bound.indexOf(a) !== -1)` or an equivalent loop) -- return `true` on a hit, `false` otherwise. Keep the whole function body inside the existing outer try/catch (fail-open: any unexpected error still returns `true`, matching the current contract). Update the function's header comment (currently describing 3 fail-open carve-outs) to also document the new 4th case: ancestor-chain widening, referencing this quick task (260728-051) and the live root cause (a session bound to `motj-ecosystem` could not write into its own registered sub-room `jonathan-contractor-motj` without a second explicit bind call). Do not change `module.exports` -- `isRoomInWriteScope` is already exported by name; only its arity/behavior widens.

    Create tests/test-write-scope-sub-room-ancestor.cjs implementing the 7 behaviors above as the UNIT-test section of the file. Follow scripts/83-scope-injection.test.cjs's harness shape exactly: a `tests` array, a `test(name, fn)` helper that pushes to it, and a final `(async function main() { ... })()` runner that iterates `tests`, prints `  ok  <name>` or `  FAIL <name>` plus the error, tracks pass/fail counts, and calls `process.exit(failed === 0 ? 0 : 1)`. This exact harness shape is required because Task 2 will `Edit` this same file to append E2E fixture tests into the SAME `tests` array -- do not use a different assertion/runner style. Header comment: reference this quick task, no em-dashes, `node:assert` only for the unit section (require `../lib/core/session-binding.cjs` directly, resolved via `path.join(path.resolve(__dirname, '..'), 'lib', 'core', 'session-binding.cjs')`).
  </action>
  <verify>
    <automated>node tests/test-write-scope-sub-room-ancestor.cjs && node tests/test-write-scope-set-membership.test.cjs && node tests/test-room-birth.cjs && node tests/test-rooms-open-actually-switches.cjs && echo "TASK1 OK"</automated>
  </verify>
  <done>isRoomInWriteScope accepts an optional 3rd ancestorChain param; a room present in ancestorChain (but not directly bound) is now in scope; exact-match, NO_ROOM_SLUG bypass, and unbound-session bypass all remain unchanged and take priority over ancestor-chain checks; all 7 unit behaviors pass in the new test file; the 3 pre-existing 2-arg call sites in test-write-scope-set-membership.test.cjs, test-room-birth.cjs, and test-rooms-open-actually-switches.cjs still pass unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add resolveAncestorChain to write-scope-check.cjs, wire it in, and prove the fix end-to-end with a spawned hook</name>
  <files>scripts/write-scope-check.cjs, tests/test-write-scope-sub-room-ancestor.cjs</files>
  <behavior>
    New helper `resolveAncestorChain(root, slug)` in scripts/write-scope-check.cjs, exercised only via the spawned hook (no direct export):
    - Scenario (a): session bound to `['motj-ecosystem']`; target file resolves (via targetRoomUnderRoot) to registered slug `jonathan-contractor-motj`, whose registry entry carries `parent: 'motj-ecosystem'` -> hook exits 0 (ALLOW). This is the RED assertion -- fails (exits 2) against today's unwired code.
    - Scenario (b): same bound session `['motj-ecosystem']`; target resolves to an unrelated registered top-level room with no parent relation to motj-ecosystem -> hook exits 2 (BLOCK). Proves no over-widening.
    - Scenario (c): session bound directly to `['jonathan-contractor-motj']` (the child itself, not the parent); target resolves to that same child -> hook exits 0 (ALLOW). Proves the exact-match path (no ancestor lookup needed) is unaffected.
    - Scenario (d1): target resolves to a registered room whose registry entry has NO `parent` field at all (the common case -- roughly half of real registry entries lack one); session bound to a different, unrelated room -> hook exits 2 (BLOCK) cleanly, no crash, no hang. Proves a missing `parent` field degrades to zero widening, not a failure.
    - Scenario (d2): `.rooms/registry.json` itself is malformed JSON; any Write target under the fixture root -> hook exits 0 (ALLOW), matching the file's PRE-EXISTING behavior (readActiveRoom already returns null on malformed JSON, which allows before the session-bound branch is even reached) -- proves this fix introduces no new crash surface on total registry failure.
  </behavior>
  <action>
    In scripts/write-scope-check.cjs, add `resolveAncestorChain(root, slug)` immediately after `slugForRoomDir` (after line 208, before the `targetRoomUnderRoot` comment block). Implementation: wrap the whole body in try/catch returning `[]` on any failure (never throws, mirrors slugForRoomDir/walkUpToRoomRoot). Read `path.join(root, '.rooms', 'registry.json')` via `fs.readFileSync` + `JSON.parse` (same shape as slugForRoomDir); if `parsed.rooms` is not an object, return `[]`. Starting from `slug`, loop up to `ROOM_ROOT_MAX_DEPTH` hops (reuse the existing constant, do not redefine it): at each hop, look up `parsed.rooms[current]`, read its `parent` field, break if it is not a non-empty string; before pushing that parent, cycle-guard against a `Set` seeded with `slug` itself (break instead of pushing if the parent is already in the seen set); push the parent into the result array and continue the walk from there. Return the accumulated array (immediate parent first, furthest ancestor last), or `[]` if `slug` has no `parent` or the loop breaks immediately. Add a short header comment above the function: references this quick task (260728-051), documents that it deliberately walks the CHILD's `parent` field bottom-up rather than the parent's `sub_rooms` array top-down, because the live registry's `sub_rooms` lists are known-incomplete (one-line note, no registry.json edit) while each child's own `parent` field is authoritative.

    Wire it into `main()`: inside the existing `if (isBound) { ... }` block (around the current `sessionBinding.isRoomInWriteScope(targetRoom, binding)` call), compute `const ancestorChain = resolveAncestorChain(realRoot, targetRoom);` immediately before the `isRoomInWriteScope` call, and change that call to `sessionBinding.isRoomInWriteScope(targetRoom, binding, ancestorChain)`. Do not compute the ancestor chain outside the `isBound` branch (an unbound session never needs it). Do not change the block() message text, the sealed-room check, the root-level-file check, or the final `targetRoom !== activeRoom` fallback -- this fix touches only the session-bound branch.

    Extend tests/test-write-scope-sub-room-ancestor.cjs (Edit, not Write) by appending a new section BELOW the Task 1 unit tests but still using the SAME `tests` array / `test()` helper: add the `node:fs`, `node:os`, `node:crypto`, and `node:child_process` (`spawnSync`) requires, plus `PLUGIN_ROOT`/`WRITE_SCOPE_CHECK` path constants, mirroring scripts/83-scope-injection.test.cjs's `mkFixtureDir`, `mkFixtureMindrianRoomsRoot`, and `mkNestedRoom` helpers (clone their shape into this file rather than importing from the .test.cjs file, since that file is itself a standalone runner script, not a requirable module). Additionally require `writeSessionBinding` from `lib/core/session-binding.cjs` to seed each fixture's session binding file via `writeSessionBinding(sessionId, { bound: [...], primary: ..., sticky: false }, { home: root })`. In each spawned test's env, explicitly set `CLAUDE_CODE_SESSION_ID` to the SAME `sessionId` used for `writeSessionBinding` (do not rely on inherited host env or the payload's `session_id` field, since `resolveSessionId` in write-scope-check.cjs checks `process.env.CLAUDE_CODE_SESSION_ID` first) and set `MINDRIAN_ROOMS_ROOT`/`MINDRIAN_ROOMS_HOME` to the fixture root. Implement the 5 scenarios above ((a), (b), (c), (d1), (d2)) as `test(...)` entries, each building its own isolated fixture dir and cleaning it up in a `finally` block (mirror `mkFixtureDir`/`cleanup` exactly). For scenario (d2), write a raw invalid-JSON string (e.g. `'{not valid json'`) directly to the fixture's `.rooms/registry.json` instead of using `mkFixtureMindrianRoomsRoot`'s normal JSON.stringify path.

    After both tasks' edits are complete, run the pre-existing SEED-004 and PSB-07 regression suites to confirm zero behavior change on the paths this plan did not intend to touch.
  </action>
  <verify>
    <automated>node tests/test-write-scope-sub-room-ancestor.cjs && node scripts/83-scope-injection.test.cjs && bash tests/run-all-194.sh && bash tests/run-all-195.sh && echo "TASK2 OK"</automated>
  </verify>
  <done>resolveAncestorChain exists in scripts/write-scope-check.cjs, is depth-bounded (reuses ROOM_ROOT_MAX_DEPTH), cycle-safe, and never throws; it is wired into main()'s session-bound branch as the 3rd arg to isRoomInWriteScope; all 5 E2E scenarios (a, b, c, d1, d2) pass in tests/test-write-scope-sub-room-ancestor.cjs proving parent-to-child widening, no over-widening, exact-match preservation, and fail-open on both a missing parent field and a corrupt registry; scripts/83-scope-injection.test.cjs (SEED-004) and tests/run-all-194.sh / tests/run-all-195.sh show no new regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/MindrianRooms/.rooms/registry.json` -> write-authorization decision | The registry's per-room `parent` field is now read by `resolveAncestorChain` and used to WIDEN a session's write scope, in addition to its pre-existing use (via `slugForRoomDir`) for target-slug resolution. Local file, not attacker-remote-controlled (Canon Part 8: LOCAL only, no network egress touches this hook). |
| Session bound set (`~/MindrianRooms/.rooms/sessions/<id>.json`) -> write-authorization decision | Pre-existing boundary, unchanged by this plan: only `room_bind` (a separate, already-scoped tool) writes this file; `write-scope-check.cjs` only reads it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QT051-01 | Tampering | `resolveAncestorChain`'s registry read of `rooms[slug].parent` | accept | Registry.json is a local, user-owned file already fully trusted for target-slug resolution (`slugForRoomDir`, pre-existing since SEED-004); a session that could tamper with `registry.json`'s `parent` field to claim a false ancestor already has direct filesystem write access, which trivially bypasses this entire hook by other means (e.g. editing `registry.json`'s `active` field directly). This fix extends an already-accepted trust boundary, it does not create a new one. |
| T-QT051-02 | Denial of Service | `resolveAncestorChain`'s parent-chain walk on a cyclic or pathologically long registry (`A.parent = B`, `B.parent = A`) | mitigate | Depth-bounded to `ROOM_ROOT_MAX_DEPTH` (12) hops, reused from the existing `.room-root` walk-up constant; additionally cycle-guarded via a `Set` seeded with the starting slug, so a 2-node cycle breaks on the first repeat, well under the depth cap. |
| T-QT051-03 | Tampering | `resolveAncestorChain`'s JSON.parse of a corrupt/malformed `registry.json` | accept | Already mitigated by the file's existing fail-open contract: the whole function is wrapped in try/catch returning `[]`, matching `slugForRoomDir`'s and `readActiveRoom`'s existing pattern; a malformed registry additionally short-circuits the entire hook to allow() earlier via `readActiveRoom` returning null, before the session-bound branch (and therefore `resolveAncestorChain`) is ever reached. |
| T-QT051-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; zero new dependencies, only new functions inside two already-required in-repo modules (`lib/core/session-binding.cjs`, `scripts/write-scope-check.cjs`). |

</threat_model>

<verification>
- `node tests/test-write-scope-sub-room-ancestor.cjs` -- all 7 unit behaviors (Task 1) and all 5 E2E scenarios (Task 2) pass.
- `node tests/test-write-scope-set-membership.test.cjs`, `node tests/test-room-birth.cjs`, `node tests/test-rooms-open-actually-switches.cjs` -- all pre-existing 2-arg `isRoomInWriteScope` call sites still pass unmodified.
- `node scripts/83-scope-injection.test.cjs` -- SEED-004 nested-room resolution tests (10/10) still pass, proving `targetRoomUnderRoot`/`slugForRoomDir` are untouched.
- `bash tests/run-all-194.sh` and `bash tests/run-all-195.sh` -- no new FAIL relative to their pre-existing baseline.
- `grep -n "function isRoomInWriteScope" lib/core/session-binding.cjs` -- signature includes `ancestorChain`.
- `grep -n "function resolveAncestorChain" scripts/write-scope-check.cjs` -- helper present.
- `grep -n "isRoomInWriteScope(targetRoom, binding, ancestorChain)" scripts/write-scope-check.cjs` -- wiring present.
- `git diff --stat` shows only the 3 declared `files_modified` touched; `~/MindrianRooms/.rooms/registry.json` is untouched.
- No em-dashes introduced anywhere in the modified or new files (repo hard rule).
</verification>

<success_criteria>
- A session bound to `motj-ecosystem` can `Write` into `motj-ecosystem/sub-rooms/jonathan-contractor-motj/...` without a second `room_bind` call (the live-reproduced bug is closed).
- A session bound to `motj-ecosystem` still cannot write into an unrelated room outside that tree -- no over-widening.
- Every pre-existing 2-arg `isRoomInWriteScope` caller and every pre-existing write-scope-check.cjs behavior (sealed rooms, root-level files, SEED-004 nested resolution, the `targetRoom !== activeRoom` fallback) is unchanged.
- A room with no `parent` field, or a fully corrupt registry, degrades safely -- never a crash, never a new hard block.
- `~/MindrianRooms/.rooms/registry.json` is not edited by this plan.
</success_criteria>

<output>
Create `.planning/quick/260728-051-fix-write-scope-check-cjs-session-bound-/260728-051-SUMMARY.md` when done: the final `isRoomInWriteScope` and `resolveAncestorChain` signatures, confirmation all unit + E2E scenarios pass, confirmation SEED-004/PSB-07 regression suites show no new failures, and explicit confirmation `~/MindrianRooms/.rooms/registry.json` was left untouched.
</output>
