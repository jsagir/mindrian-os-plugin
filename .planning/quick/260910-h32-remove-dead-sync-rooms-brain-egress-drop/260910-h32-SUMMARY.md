---
quick_task: 260910-h32
title: Remove dead sync-rooms-brain Canon Part 8 egress
date: 2026-09-10
requirements: [QUICK-260910-h32]
canon_parts: [8, 9]
status: complete
---

# Quick Task 260910-h32: Remove dead sync-rooms-brain Canon Part 8 egress

**One-liner:** Deleted the script-level `sync-rooms-brain` writer that egressed room/venture/stage/status/path to the remote Brain on every session start and room create/archive, and added a default-deny regression census so it cannot come back silently.

## What Changed, Per Task

### Task 1: Drop the three detached invocations and delete the writer
**Commit:** `5b8757ce`

- Deleted the `node "${SCRIPT_DIR}/sync-rooms-brain" ... &` line at `scripts/session-start:375`, keeping the `sync-rooms-graph` (local SQLite) spawn intact on the line above.
- Deleted the same line in `scripts/room-registry`'s `create` stanza (line 337) and `archive` stanza (line 602), keeping both `sync-rooms-graph` spawns intact.
- Updated the "Fire-and-forget graph sync" comments at all three sites to name the local SQLite sync explicitly and record the 2026-09-10 retirement (Canon Part 8, quick task 260910-h32).
- `git rm scripts/sync-rooms-brain` (438-line writer deleted from disk and the git index).
- Fixed the now-stale comment in `tests/test-jtbd-auto-anchor-empirical.sh` (around line 85) that claimed `room-registry create` still spawns `sync-rooms-brain` DETACHED; reduced to `sync-rooms-graph` only and dropped the frozen line-number citation.
- `lib/core/brain-client.cjs` was not touched (confirmed via `git diff --stat 7fafdc12 HEAD -- lib/core/brain-client.cjs`, empty output).

**Root cause (from the plan, verified 2026-09-10):** `sync-rooms-brain` string-interpolated room name, venture name, stage, status and path into a Cypher `MERGE` and POSTed it to the live Brain via `brain.write()` -> `callTool('brain_write')`, gated only by `brain.isAvailable()`. Loopback capture observed one run producing 1,498 `brain_write` POSTs carrying 55 distinct Room names. The destination (`brain_write` is admin-only on the live incumbent) has refused these writes since roughly 2026-07-23, so the leak was silent for seven weeks. Evidence trail: `~/MindrianRooms/rethinking-mindrianos/research/2026-09-10-sync-rooms-brain-egress-observed.md`.

### Task 2: Retire the Brain hierarchy sync in the two docs that still advertise it
**Commit:** `73d51af3`

- `references/brain/room-hierarchy-schema.md`: added a dated RETIRED banner after the title block, marked the Sync Scripts table's Brain row RETIRED (trigger "none", frequency "never") while leaving the `sync-rooms-graph` row unchanged, rewrote the degradation text to describe only the two surviving tiers (KuzuDB local, filesystem Tier 0), and appended a "(RETIRED 2026-09-10 with the script; kept as history)" note to the `USES_FRAMEWORK` sourcing sentence that referenced `sync-rooms-brain`.
- `docs/BACKEND-MAP.md`: removed the `sync-rooms-brain` row from the BRIDGE table, decremented the heading from "(3)" to "(2)", and added a dated note recording the deletion and that the original BRIDGE classification understated what the script actually did (it wrote TO the Brain, not just to a room registry cache).

### Task 3: Add and register the no-brain-write-from-scripts regression test
**Commit:** `a23d8ee1`

- Authored `tests/test-quick-260910-h32-no-brain-write-from-scripts.cjs` (272 lines, Node CJS, `node:assert/strict`, no dependencies, runs in ~0.09s):
  - Arm 1: `scripts/sync-rooms-brain` absent on disk.
  - Arm 2: zero tracked `scripts/` files mention the raw string `sync-rooms-brain`.
  - Arm 3: default-deny census (comment-stripped) for `brain.write(` and `callTool('brain_write')` across every tracked file under `scripts/` (`git ls-files scripts/`, 335 files at run time).
  - Arm 4: ALLOWLIST reconciled both directions (every entry exists on disk AND still produces a hit).
  - Arm 5: mutation leg against synthetic in-memory fixtures, proving the matcher detects real calls and clears prose-only/commented mentions.
- ALLOWLIST carries exactly one entry: `scripts/whitespace-to-brain.cjs` (pre-existing dev-pipeline Brain writer invoked only from `scripts/discovery-cycle.cjs:446`, out of scope for this quick task, mirrors `tests/test-252-guard-census.cjs:179`).
- Registered as a `run_if` leg in `tests/run-all-257.sh` (the Part 8 egress aggregator), inserted after the "REGRESSION 254 composition census" leg.

## Test Results (exact commands + exit codes)

| Command | Exit code | Result |
|---|---|---|
| `node tests/test-quick-260910-h32-no-brain-write-from-scripts.cjs` | 0 | PASSED, all 5 arms reported |
| `bash tests/run-all-257.sh` | 1 (see note) | `QUICK 260910-h32 no brain-write egress from scripts/ (Part 8): PASSED`; aggregator summary `Passed: 8 Failed: 1 Skipped: 0` |
| `bash tests/run-all-341.sh` | 0 | `PASS=22 FAIL=0 SKIP=6 EXPECTED-RED=0` |
| `node scripts/doctor.cjs --acceptance` | 0 | `Acceptance full: 20/20 points passed.` |
| `bash -n scripts/session-start && bash -n scripts/room-registry && bash -n tests/test-jtbd-auto-anchor-empirical.sh` | 0 | clean |
| `grep -rn $'\xe2\x80\x94' <all 6 touched files>` | -- | 0 matches (no em-dashes) |

**Note on `tests/run-all-257.sh` exit code:** the aggregator itself exits non-zero (`[ "$FAIL" -eq 0 ]`) because of ONE pre-existing, unrelated failure: `Plan 08 (LOCUS-07) strict input shapes` FAILs on a frozen `brain_ask` tool-description snapshot ("...live Memgraph teaching graph using locally-embedded multilingual-e5-large vectors...") that predates the Phase 339 Theo cutover (2026-09-03) and no longer matches the live tool description. This is unrelated to `scripts/` or Brain-write egress. Confirmed pre-existing by reproducing the identical failure against the pre-change baseline commit `7fafdc12` (via a temporary detached `git worktree`, `node_modules` symlinked in read-only, removed afterward with `git worktree remove --force`, no destructive operation on `main`). The plan's own leg (`QUICK 260910-h32 ...`) is green; `Failed: 0` was not literally emitted because the pre-existing Plan 08 failure adds 1 to the aggregator's `$FAIL` counter, but the plan's verification step 4 target ("the QUICK 260910-h32 leg PASSED") is satisfied.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - blocking issue for the plan's own verify] Removed a stale `sync-rooms-brain` string mention in `scripts/whitespace-to-brain.cjs`**
- **Found during:** Task 1 verify (`grep -rl 'sync-rooms-brain' scripts/` returned 1, not 0)
- **Issue:** `scripts/whitespace-to-brain.cjs:40` had a comment "fire-and-forget pattern from sync-rooms-brain" that referenced the deleted script by name. This file was not in the plan's `files_modified` list, but the plan's own must-have truth ("scripts/sync-rooms-brain ... is referenced from no file under scripts/") and Task 1's automated verify command both require zero matches.
- **Fix:** Reworded the comment to describe the pattern generically and reference the retirement instead of the dead script name.
- **Files modified:** `scripts/whitespace-to-brain.cjs`
- **Commit:** `5b8757ce`

**2. [Not a deviation, a plan-directed exclusion] `scripts/admin-brain-write.cjs` excluded from ALLOWLIST**
- **Found during:** Task 3 authoring
- **Detail:** The plan's prose listed `scripts/admin-brain-write.cjs` as an "expected member" of the ALLOWLIST based on the original 2026-09-10 grep, but also explicitly instructed: "Derive the exact membership by running the census once before freezing it... Include an entry only if the census actually produces a hit for it." Running the actual matcher (`brain.write(` / `callTool('brain_write')`, comments stripped) against `scripts/admin-brain-write.cjs` produces zero hits: that script gates its write behind a direct Neo4j driver `session.run()`, never `brain.write()` or `callTool('brain_write')`. Its only "brain_write" mentions are inside comments describing the plan-gate ("remote Brain MCP gates `brain_write` behind a `plan='admin'` API key"). Per the plan's own stated rule (illustrated with the `scripts/seed-brain-commands.cjs` example), it is correctly excluded to avoid Arm 4 failing it as stale. ALLOWLIST carries only `scripts/whitespace-to-brain.cjs`.
- **Files modified:** none beyond the test file itself.
- **Commit:** `a23d8ee1`

No architectural changes, no auth gates, no package installs.

## TDD Handling Note (Task 3)

Task 3 was marked `tdd="true"`, but its deliverable is a self-verifying census test rather than a test/implementation pair for a separate feature (the codebase state it certifies was already achieved by Tasks 1-2). The RED/GREEN split was handled as: author the full test file (Arms 1-5) in one pass, run it (green, since Tasks 1-2 already satisfy the properties it checks), then perform the plan's own required "oracle can fail" sanity check by temporarily inverting the `BRAIN_WRITE_CALL` regex to a non-matching sentinel, confirming the run went red (Arm 4 stale-allowlist + Arm 5 both failed as expected), then reverting to the original content byte-for-byte before committing. This satisfies the plan's `<done>` criterion ("Arm 5 has been sanity-checked by temporarily inverting one fixture expectation locally... confirming the oracle is not vacuous, and the inversion has been reverted") without a separate RED commit, since there is no meaningful pre-implementation "failing" state distinct from the finished artifact for this kind of test.

## Known Stubs

None.

## Threat Flags

None. All new surface (the regression test) is defense-only (a census/matcher), and this quick task's `<threat_model>` STRIDE register already covers every file touched.

## Self-Check: PASSED

Files verified present on disk:
- FOUND: `scripts/session-start`
- FOUND: `scripts/room-registry`
- MISSING (intentional deletion): `scripts/sync-rooms-brain`
- FOUND: `scripts/whitespace-to-brain.cjs`
- FOUND: `tests/test-jtbd-auto-anchor-empirical.sh`
- FOUND: `references/brain/room-hierarchy-schema.md`
- FOUND: `docs/BACKEND-MAP.md`
- FOUND: `tests/test-quick-260910-h32-no-brain-write-from-scripts.cjs`
- FOUND: `tests/run-all-257.sh`

Commits verified in `git log --oneline --all`:
- FOUND: `5b8757ce` fix(quick-260910-h32): remove dead sync-rooms-brain Canon Part 8 egress
- FOUND: `73d51af3` docs(quick-260910-h32): retire Brain half of room-hierarchy sync docs
- FOUND: `a23d8ee1` test(quick-260910-h32): add default-deny census against scripts/ Brain-write egress
