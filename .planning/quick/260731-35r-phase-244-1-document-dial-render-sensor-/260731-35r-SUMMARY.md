---
phase: quick-260731-35r
plan: 01
subsystem: hmi-dial
tags: [documentation, scope-boundary, dial, sensors, phase-244-residual]
requires: []
provides:
  - "The named dial-visibility-vs-dial-ranking scope boundary, documented at buildReachList itself"
  - "A durable FINDING artifact with re-verifiable file:line citations and two falsifiable flip conditions"
affects:
  - lib/hmi/dial-reach-orchestrator.cjs
  - CHANGELOG.md
tech-stack:
  added: []
  patterns: ["Dev-Research Compositing (bidirectional dev-repo <-> rethinking-mindrianos cross-link)"]
key-files:
  created:
    - .planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md
  modified:
    - lib/hmi/dial-reach-orchestrator.cjs
    - CHANGELOG.md
    - ~/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/2026-07-31-dial-rethink-decoupled-from-sensor-bank.md
decisions:
  - "The sensor bank is a dial VISIBILITY gate, not a dial RANKING input. Named as a deliberate scope call on 2026-07-31, with two countable flip conditions, rather than left as a silent gap."
  - "244-RESIDUALS.md deliberately NOT edited: Phase 244 is closed and gated, so the finding lives in a new quick-task artifact instead of rewriting a closed phase's register."
  - "The CHANGELOG bullet was added INTO the v1.16.0-beta.2 Unreleased section that the concurrent release had already created, rather than inserting a duplicate header above it."
metrics:
  duration: ~25 min
  tasks: 3
  files_changed: 4
  completed: 2026-07-31
---

# Quick Task 260731-35r: Document the dial-render / sensor-bank disjointness Summary

**One-liner:** Named the previously undocumented boundary that `buildReachList` is scored 100 percent
by cortex-node scoring while sensor firings only gate WHETHER the Shape F.7 dial renders, landing it
as a doctrine comment at the function itself, a 208-line re-verifiable FINDING artifact, a CHANGELOG
bullet, and a bidirectional cross-link to the `rethinking-mindrianos` room. Zero executable lines changed.

## What Was Built

Three artifacts plus one back-link, all documentation:

1. **`260731-35r-FINDING.md` (208 lines, force-added past the `.planning/*` ignore).** Carries the
   one-line verdict, bidirectional cross-links, the six verified facts each with its file:line, what
   Phase 244 did and did not fix, the named scope boundary, both flip conditions with their counts,
   an explicit "what this is NOT" section, and a copy-pasteable verification block so a skeptical
   reader can re-prove every citation without trusting the artifact.

2. **One doctrine comment inside `buildReachList`'s existing JSDoc header.** Sixteen comment lines,
   zero non-comment characters.

3. **One Feynman-simplified CHANGELOG bullet** under the v1.16.0-beta.2 Unreleased section.

4. **The return leg of the cross-link** added to the room entry, so the two homes point at each other.

## Exact Insertion Point in `dial-reach-orchestrator.cjs`

Inserted between the existing header's final content line and its closing marker, exactly per
`<comment_contract>`:

- **After:** ` * Pure / synchronous / LOCAL-only. No Brain call, no fs write, no db write.` (was line 272)
- **Before:** ` */` (was line 273, now line 289)
- **Result:** 16 lines added (one blank ` *` continuation plus 15 content lines). `buildReachList`'s
  definition moved from line 290 to line 306; no other line in the file moved relative to its own block,
  and no existing comment was reworded, moved, or cleaned up (Rule Q honored: the file's pre-existing
  em-dashes in untouched blocks were left alone).

The greppable sentinel `DIAL VISIBILITY, NOT DIAL RANKING` survives verbatim, and the block's closing
line points at the FINDING artifact by path.

## Verbatim Baseline Runs (the proof nothing executable moved)

Both run AFTER the comment landed.

```
=== node tests/test-dial-reach-orchestrator.cjs ===
1..12
# tests 12
# suites 0
# pass 12
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 198.528774
EXIT=0
```

```
=== node tests/test-158-reach-byte-stable.cjs ===
test-158-reach-byte-stable.cjs (RJP-02): zero-reject reach list === captured baseline
  ok - buildReachList(fixedScores) is byte-identical to the captured baseline
  ok - buildReachList(fixedScores, suppressedReachIds:[]) === baseline (empty set is a no-op)
  ok - zero-reject fold path (computeReachPenalties -> buildReachList) === baseline

PASS test-158-reach-byte-stable.cjs (3 checks)
EXIT=0
```

The second is the load-bearing one. It asserts `buildReachList`'s output is byte-identical to a
captured baseline, so a green run after the edit is direct evidence the render output did not move,
rather than an assertion that it did not.

Machine-checked diff shape, both before and after the commit:

- `lib/hmi/dial-reach-orchestrator.cjs`: 0 removed lines, 0 non-comment added lines, `node --check` clean.
- `CHANGELOG.md`: 0 removed lines, 0 em-dashes on any added line.

## Concurrency Pre-Flight: the Hazard Had Resolved

The plan's `<concurrency_hazard>` described a live, observed hazard: another session mid-release in
this same worktree with `.claude-plugin/plugin.json`, `CHANGELOG.md` and a vendored `node_modules/`
tree staged in the index. **Re-checked live at execution time as instructed. That hazard is gone.**
The release completed between plan-writing and execution:

```
38544021 chore: bump to v1.16.0-beta.2 (next pre-release)
b43a6bcc release: v1.16.0-beta.1
```

Verbatim pre-flight immediately before staging:

```
=== git status --short (VERBATIM PRE-FLIGHT) ===
 M CHANGELOG.md
 M evals/plurai/211-baseline.json
 M lib/hmi/dial-reach-orchestrator.cjs
 M lib/statusline/ctx-window.cjs
 M scripts/context-monitor
 M scripts/statusline-fallback-echo.cjs
 M tests/test-context-monitor-d02-broadcast.cjs
 M tests/test-fallback-echo-compose.cjs
 M tests/test-statusline-context-aware.cjs
?? .planning/debug/beta13-curing-sequence-persona-and-commands-bisect.md
?? .planning/debug/brain-corpus-count-self-contradiction.md
?? .planning/debug/brain-schema-entropy-and-cooccurs-bloat.md
?? .planning/debug/intern-w1-card-discipline-decay.md
?? .planning/debug/intern-w1-research-reach-broken.md
?? .planning/debug/intern-w1-rooms-new-silent-fail.md
?? .planning/debug/intern-w1-rooms-skill-script-path.md
?? .planning/debug/intern-w1-state-not-recomputed.md
?? .planning/debug/leah-income-session-verify-after-pitch-rigid-card.md
?? .planning/debug/live-session-running-stale-plugin-cache-fixes-inert.md
?? .planning/debug/planned-vs-executed-drift-audit-260605.md
?? .planning/debug/release-pipeline-install-state-corruption-2026-05-25.md
?? .planning/debug/statusline-active-room-leah-gap.md
?? .planning/debug/statusline-context-pct-stale-post-compact.md
?? dist/zed/.agents/mcp_config.json
=== version files dirty? ===
(none - neither version file is dirty or staged)
=== already staged? ===
(index is empty)
=== ignore check ===
.gitignore:79:.planning/*	.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md
```

Per Task 3's branching logic: neither version file was dirty or staged, so step 3 proceeded normally.
**But the pathspec discipline was still necessary and still applied**, because a DIFFERENT concurrent
session's WIP is dirty in this worktree: 7 modified files (the statusline / context-monitor / evals
cluster, the same WIP `240.1`'s verifier already logged on 2026-07-30). No `git commit -a`, no
`git add -A`, no bare index commit was ever run. All 7 of those files are still dirty and untouched
after this commit, verified post-commit.

The FINDING artifact needed `git add -f` exactly as the plan predicted (`.gitignore:79`), and landed:
`create mode 100644 .planning/.../260731-35r-FINDING.md`. Pre-commit chain left intact
(`render-coverage: OK` fired), `COMMIT_NO_VERIFY` never set.

Commit `ea3ca510`, exactly 3 files, 235 insertions, 0 deletions, no `node_modules` / `package.json` /
`package-lock.json` / `plugin.json` entry.

## The Resolved CHANGELOG Header Discrepancy

The header name moved three times across the life of this task, exactly the moving target the plan
warned about:

| Stage | Top section name |
|-------|------------------|
| Dispatching brief | `## [Unreleased] -- v1.15.3-beta.51 (in progress)` |
| Plan-writing time | no `## [Unreleased]` section at all (folded into a cut `## [1.16.0-beta.1] - 2026-07-31`) |
| Execution time (now) | `## [Unreleased] -- v1.16.0-beta.2 (in progress)`, created by `release.sh` |

**None of them were touched.** `package.json` and `.claude-plugin/plugin.json` are both at
`1.16.0-beta.2` and were not edited; `scripts/release.sh` was never run; no version was bumped by hand.
The navigator's `v1.16.0-beta.2` instruction turned out to be exactly right, arrived at independently
by the release script.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The CHANGELOG section the plan wanted to create already existed**

- **Found during:** Task 2.
- **Issue:** The plan's `<changelog_contract>` was written on the premise that there were ZERO
  `## [Unreleased]` sections ("Rule U ... There are ZERO today"), so it instructed inserting a NEW
  `## [Unreleased] -- v1.16.0-beta.2` header as line 1. Between plan-writing and execution the
  concurrent release finished and `release.sh` itself created that exact section, with an empty
  `### Added` / `- ` stub. Following the literal instruction would have produced a DUPLICATE
  `## [Unreleased] -- v1.16.0-beta.2` header, failing the contract's own Rule U
  (`grep -c '^## \[Unreleased\]'` must equal 1) and corrupting the file for the next `release.sh` run.
- **Fix:** Added the bullet as the first entry INSIDE the existing v1.16.0-beta.2 Unreleased section,
  immediately after `### Added`. This satisfies every semantic must_have: one Feynman-simplified bullet
  under a new (unreleased, previously empty) v1.16.0-beta.2 section, exactly one Unreleased section,
  zero removed lines, zero em-dashes on added lines, every pre-existing section byte-identical.
- **Gate adaptation:** Task 2's `head -1 CHANGELOG.md | grep -qxF '## [Unreleased] -- v1.16.0-beta.2'`
  exact-match sub-check was run as a PREFIX match instead, because the live header carries release.sh's
  `(in progress)` suffix. Prefix match passed; every other sub-check in that gate was run verbatim and
  passed. Task 3's gate was run entirely verbatim and passed.
- **What was deliberately NOT done:** `release.sh`'s empty `- ` placeholder line below the new bullet
  was left in place rather than removed. Removing it would be a removed line, breaking Rule A and
  failing Task 3's machine-checked zero-removed-lines gate. The same empty stub already exists in the
  shipped `## [1.16.0-beta.1]` section, so it is an accepted repo pattern, not a wart introduced here.
- **Files modified:** `CHANGELOG.md`. **Commit:** `ea3ca510`.

### Verification Performed Beyond the Plan

The plan said not to re-investigate the code path. All 7 factual citations were nonetheless spot-checked
live against the checkout before writing the artifact, because the FINDING's whole value proposition is
that a reader can re-verify it. Every one held exactly:
`buildReachList` sole definition at `dial-reach-orchestrator.cjs:290`; `_resolveReachScore` at 199-201;
`cortexNodes` / `buildReachScoresFromCortex` at `intent-classifier.cjs:1273-1274`; the sole call site at
`:1341`; the `source !== 'engine'` early-return at `:1255-1260`; the verbatim "routing_source is NOT
assigned here" comment at `navigation-engine.cjs:1253`; and the sensor census at 19 `.cjs` files minus
2 support modules = 17 implementations.

## Out of Scope, Stated Rather Than Silently Skipped

- **`~/MindrianOS/research/` mirroring was deliberately NOT done.** The Dev-Research Compositing
  mandate names `mindrianOS/research/` as source-of-record mirror alongside the room entry. This plan
  scoped only the dev-repo artifact plus the room back-link. That mirror leg remains open; it is named
  here rather than quietly left undone.
- **`244-RESIDUALS.md` was never edited**, verified by `git diff --quiet HEAD` against that exact path
  in Task 1's gate. Phase 244 is closed and gated; its register records what that phase left open, and
  this finding is a separate, additional fact that belongs in its own artifact.
- **ROADMAP.md and STATE.md were not touched.** Quick tasks sit outside the planned-phase counters, and
  v1.16.0's phase progress is legitimately 59/59 complete; advancing a plan counter for a quick task
  would corrupt that. No `state.advance-plan` / `state.record-metric` / `roadmap.update-plan-progress`
  was run.

## Standing Reminder: None of This Is Live Yet

Per the standing hard rule, a commit on `main` in this repo is NOT live for any user until a release
ships AND is actually picked up. A running session never hot-reloads the plugin install cache even
after a release lands. This commit sits on `main` at `ea3ca510` behind the unreleased v1.16.0-beta.2
line. Cutting the release is the orchestrating session's separate, explicit step, and is not part of
this task.

## Self-Check: PASSED

- `FOUND: .planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md` (208 lines, tracked)
- `FOUND: lib/hmi/dial-reach-orchestrator.cjs` (sentinel present, 0 removed / 0 non-comment added)
- `FOUND: CHANGELOG.md` (1 Unreleased section, 0 removed lines)
- `FOUND: ~/MindrianRooms/.../2026-07-31-dial-rethink-decoupled-from-sensor-bank.md` (contains `260731-35r`)
- `FOUND: ea3ca510` (exactly 3 paths, no version files, no node_modules, no deletions)
