---
phase: quick-260731-35r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md
  - lib/hmi/dial-reach-orchestrator.cjs
  - CHANGELOG.md
  - ~/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/2026-07-31-dial-rethink-decoupled-from-sensor-bank.md
autonomous: true
requirements: [QUICK-DIAL-SENSOR-DISJOINT-01]

must_haves:
  truths:
    - "A future reader opening dial-reach-orchestrator.cjs learns, at the buildReachList header itself, that sensor-fired candidates never reach its scoring"
    - "The disjointness finding exists as a durable, committed artifact with every file:line citation re-verifiable by a reader who trusts nothing in it"
    - "The finding states plainly this is a DELIBERATE, NAMED scope boundary, not a silent gap"
    - "The finding names the two flip conditions that would reopen it for real work"
    - "The dev-repo artifact and the rethinking-mindrianos room entry each point at the other"
    - "244-RESIDUALS.md is referenced but never edited (it belongs to the closed, gated Phase 244)"
    - "CHANGELOG.md carries one Feynman-simplified bullet under a NEW v1.16.0-beta.2 section, with every pre-existing section byte-identical"
    - "buildReachList's runtime output is byte-identical to before this plan (comment-only edit, proven by the shipped byte-stability fence)"
    - "A concurrent session's in-flight release changes are never swept into this plan's commit"
  artifacts:
    - path: ".planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md"
      provides: "The durable dial-render / sensor-bank disjointness finding with verbatim file:line citations, the named scope boundary, and the flip conditions"
      min_lines: 60
      contains: "scripts/intent-classifier.cjs:1273-1274"
    - path: "lib/hmi/dial-reach-orchestrator.cjs"
      provides: "One doctrine comment appended to buildReachList's existing header block"
      contains: "DIAL VISIBILITY, NOT DIAL RANKING"
    - path: "CHANGELOG.md"
      provides: "New v1.16.0-beta.2 Unreleased section with one bullet"
      contains: "v1.16.0-beta.2"
  key_links:
    - from: ".planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md"
      to: "~/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/"
      via: "explicit cross-link line, Dev-Research Compositing mandate"
      pattern: "2026-07-31-dial-rethink-decoupled-from-sensor-bank"
    - from: "~/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/2026-07-31-dial-rethink-decoupled-from-sensor-bank.md"
      to: ".planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md"
      via: "back-link line added by this plan (the return leg of the bidirectional cross-link)"
      pattern: "260731-35r"
    - from: "lib/hmi/dial-reach-orchestrator.cjs"
      to: ".planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md"
      via: "doctrine comment's closing pointer to the full finding"
      pattern: "260731-35r-FINDING.md"
---

<objective>
Document, honestly and durably, a real still-open gap that Phase 244's own residual register does
not mention anywhere: `lib/hmi/dial-reach-orchestrator.cjs::buildReachList` (the actual Shape F.7
dial render function) is scored EXCLUSIVELY from `cortex-reach-adapter.cjs::buildReachScoresFromCortex`.
Zero merge from `dispatchSensors`' fired candidates or `decide()`'s output.

Purpose: today a reader who sees Phase 244-04 wire tier candidates into `rankForSelector` can
reasonably assume the sensor bank now influences the reach dial. It provably does not. Sensors gate
WHETHER the dial renders; they have zero influence on WHICH reach_id ranks top once it renders.
Naming that boundary out loud turns a silent gap into a stated scope call with a flip condition.

Output: one new committed FINDING artifact, one doctrine comment on `buildReachList`'s existing
header block, one CHANGELOG bullet under a new version line, and a back-link added to the
rethinking-mindrianos room entry so the cross-link runs both ways.

Zero functional change. Comments and markdown only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

Read-only inputs, already fully traced this session. Do NOT re-investigate the code path; every
line below was re-verified live against this checkout before this plan was written:
- `lib/hmi/dial-reach-orchestrator.cjs` lines 251-273 (the existing `buildReachList` JSDoc header)
  and 199-201 (`_resolveReachScore`)
- `scripts/intent-classifier.cjs` lines 1255-1260, 1273-1274, 1341
- `lib/core/navigation-engine.cjs` line 1253
- `.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md` (REFERENCE ONLY, NEVER EDIT: this
  file belongs to the closed, gated Phase 244)
- `~/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/2026-07-31-dial-rethink-decoupled-from-sensor-bank.md`
  (Sections 4, 5 and 10 carry the disjointness read, the countable gaps, and the flip conditions)
</context>

<verified_finding>
Do NOT re-derive this. Every claim was read directly off the live checkout before this plan was
written, and each is stated with the exact line it came from.

1. `buildReachList` has exactly ONE definition in the whole tree: `lib/hmi/dial-reach-orchestrator.cjs:290`.
2. Its only score input is `roomState.reachScores`, read only by `_resolveReachScore`
   (`lib/hmi/dial-reach-orchestrator.cjs:199-201`).
3. On the render path, `reachScores` is built exclusively by the cortex adapter:
   `scripts/intent-classifier.cjs:1273` assigns `cortexNodes`, `:1274` assigns
   `reachScores = adapter.buildReachScoresFromCortex(cortexNodes)`. The only later mutation is
   Phase 158-03's reject-penalty merge (`Object.assign(reachScores, reachPenalties.discountedScores)`),
   which discounts existing cortex scores; it introduces no sensor-derived score.
4. The only `buildReachList` call site on that path is `scripts/intent-classifier.cjs:1341`.
5. The one REAL channel from the sensor bank to the dial is a visibility gate, not a ranking input:
   `decide()` does not assign `routing_source` (`lib/core/navigation-engine.cjs:1253`, verbatim:
   "routing_source is NOT assigned here; flipped at the router layer when fire_skill is non-null
   (Phase 144)"), a fired sensor reach flips it `legacy -> engine` downstream, and
   `renderEngineDecisionWithDial` early-returns the base block unless `routing.source === 'engine'`
   (`scripts/intent-classifier.cjs:1255-1260`).
6. Phase 244-04's tier-fusion work landed on a DIFFERENT function on a DISJOINT path:
   `lib/workflow/f-selector-ranker.cjs::rankForSelector`'s optional `o.tierCandidates` seam plus
   `_applyTierFusion`, supplied by `lib/core/orchestration-candidate-lift.cjs::buildTierCandidates`.
   It does not touch `buildReachList` at all.
7. Sensor census today: `lib/core/sensors/` holds 19 `.cjs` files, of which 17 are sensor
   implementations (the other two, `sensor-types.cjs` and `hat-scoping-table.cjs`, are support
   modules). This is the baseline the second flip condition counts against.

Baselines captured before any edit (both green on this checkout, re-run them if in doubt):
`node tests/test-dial-reach-orchestrator.cjs` -> 12/12 pass, exit 0.
`node tests/test-158-reach-byte-stable.cjs` -> 3/3 checks, exit 0 (this one asserts
`buildReachList`'s output is byte-identical to a captured baseline, which is exactly the fence that
proves a comment-only edit changed nothing observable).
</verified_finding>

<concurrency_hazard>
READ BEFORE TOUCHING GIT. This is live, observed, and not hypothetical.

At the moment this plan was written, ANOTHER session was mid-release in this same worktree.
`git status --short` showed a staged, uncommitted release-in-flight: `.claude-plugin/plugin.json`
and `CHANGELOG.md` modified and staged, `package.json` already at `1.16.0-beta.1`, and a large
vendored `node_modules/` tree in the index. The CHANGELOG's `## [Unreleased]` section had already
been folded into a cut `## [1.16.0-beta.1] - 2026-07-31` header; as of this writing there is NO
`## [Unreleased]` section in the file at all.

Three consequences, all binding:

1. **A bare `git commit` would sweep the other session's entire staged release into this doc-only
   commit,** including vendored `node_modules` and a version bump this plan is explicitly forbidden
   from making. Never run `git commit` against the index, never `git commit -a`, never
   `git add -A`. Commit ONLY by explicit pathspec (Task 3).
2. **The CHANGELOG's top section name is a moving target.** The dispatching brief named it
   `## [Unreleased] -- v1.15.3-beta.51 (in progress)`; by the time this plan was written it had
   already moved through `v1.15.4-beta.1` to a cut `## [1.16.0-beta.1] - 2026-07-31`. Do not match
   on any specific existing header name. The changelog contract below is written to be
   name-agnostic: insert above whatever the current first line is, and leave everything from that
   line down untouched.
3. **`.planning/` is gitignored** (`.gitignore:79`, `.planning/*`, verified live with
   `git check-ignore -v`). Previously-committed files under it stay tracked, but the NEW FINDING
   artifact is not, so a plain `git add` on it silently does nothing. It needs `git add -f`. The
   repo's own CLAUDE.md already carries this rule ("`.planning/` is gitignored, so `git add -f`").

The navigator's `v1.16.0-beta.2` instruction is now clearly coherent rather than arbitrary:
`v1.16.0-beta.1` is being cut right now, so the next unreleased line genuinely is `v1.16.0-beta.2`.
</concurrency_hazard>

<comment_contract>
Exactly ONE doctrine comment. It is APPENDED INSIDE the existing `buildReachList` JSDoc block in
`lib/hmi/dial-reach-orchestrator.cjs`, after the existing final line
` * Pure / synchronous / LOCAL-only. No Brain call, no fs write, no db write.` (line 272) and
BEFORE that block's closing ` */` (line 273). Do not create a second comment block, do not move or
reword any existing line in that header, do not touch any other comment in the file.

Insert these lines verbatim (they are JSDoc continuation lines, each starting with ` * `):

 *
 * SCOPE BOUNDARY (documented 2026-07-31, quick-260731-35r): DIAL VISIBILITY, NOT DIAL RANKING.
 * Sensor-fired candidates from dispatchSensors NEVER reach this function's scoring. They control
 * only WHETHER the Shape F.7 dial renders at all: decide() does not assign routing_source
 * (lib/core/navigation-engine.cjs:1253, "routing_source is NOT assigned here; flipped at the
 * router"), a fired sensor reach flips it legacy -> engine downstream, and
 * renderEngineDecisionWithDial early-returns unless routing.source === 'engine'
 * (scripts/intent-classifier.cjs:1255-1260). WHICH reach_id ranks top, carries the recommended
 * marker, or appears in what order once the dial renders is 100 percent cortex-node scoring:
 * roomState.reachScores is built exclusively by cortex-reach-adapter.cjs::buildReachScoresFromCortex
 * (scripts/intent-classifier.cjs:1273-1274) and read only by _resolveReachScore (this file,
 * lines 199-201). Phase 244-04's tier-fusion seam (o.tierCandidates -> _applyTierFusion) lives in
 * lib/workflow/f-selector-ranker.cjs::rankForSelector, a DIFFERENT function on a DISJOINT path;
 * it does not touch buildReachList. This is a deliberate, named scope boundary, not an oversight.
 * Full finding, citations and flip conditions:
 * .planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md

Rules on this insert:
- Rule N (no logic change). Not one non-comment character may be added, removed or reordered in
  this file. The diff must show zero removed lines and zero added lines that are not comment lines.
- Rule S (greppable sentinel). The literal string `DIAL VISIBILITY, NOT DIAL RANKING` must survive
  verbatim; the Task 2 gate greps for it.
- Rule E (no em-dashes). Hyphens only, repo hard rule. The `->` arrows above are ASCII hyphen plus
  greater-than, not any Unicode arrow.
- Rule Q (do not "fix" the surrounding file). The file has pre-existing em-dashes in untouched
  comment blocks. Leave them; rewriting them expands a doc-only diff past the point of review.
</comment_contract>

<finding_contract>
Create `.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md`.
Markdown, no frontmatter required. Required sections, in this order:

1. **Title + one-line verdict.** Something in the shape of "The reach dial's ranking is not wired to
   the sensor bank, and that is a deliberate scope boundary as of 2026-07-31."
2. **Cross-links (both directions, stated up front).** The room's durable reasoning trail at
   `~/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/2026-07-31-dial-rethink-decoupled-from-sensor-bank.md`
   (Sections 4, 5 and 10), and `.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md` as a
   READ-ONLY reference. State explicitly that 244-RESIDUALS.md was deliberately NOT edited because
   Phase 244 is closed and gated, and that this artifact is where the finding lives instead.
3. **What is actually true in the tree today.** Reproduce items 1 through 6 of this plan's
   `<verified_finding>` section, each with its file:line, in prose a reader can re-verify line by
   line without trusting the artifact.
4. **What Phase 244 did and did not fix.** 244-04 wired `o.tierCandidates` into
   `lib/workflow/f-selector-ranker.cjs::rankForSelector` via
   `lib/core/orchestration-candidate-lift.cjs::buildTierCandidates`. That is a different function on
   a disjoint path. It does not touch `buildReachList`. Say plainly that 244-RESIDUALS.md Section 6
   item 3 already notes `rankForSelector` has exactly one live tier-candidate supplier, and that
   this finding is a separate, additional fact that register does not carry anywhere.
5. **The named scope boundary.** State without hedging: sensor bank = dial VISIBILITY gate only,
   not a dial RANKING input. This is a scope call taken deliberately on 2026-07-31, not a defect
   and not an oversight. Nothing is broken today; the boundary is simply undocumented until now.
6. **Flip conditions (what would reopen this for real work).** Both must be stated as falsifiable
   and countable, not as vibes:
   - **Flip 1, the seam patch count.** A THIRD reconciliation phase becoming necessary at the
     fired-candidate-composition seam. Four patches have already landed at that same seam across
     four different phases without ever unifying it: 172-07's dead `trigger_tier` field, 222-02's
     Hedge/MWU reranker, the 209-01 and 237-04 relevance gates, and 244-04's own `rankForSelector`
     tier fusion. A fifth patch at the same seam is the signal to stop patching and design the
     join.
   - **Flip 2, the sensor census.** The sensor count crossing roughly 25 to 30 files. Baseline
     today: 17 sensor implementations in `lib/core/sensors/` (19 `.cjs` files total, minus
     `sensor-types.cjs` and `hat-scoping-table.cjs`, which are support modules).
   State that absent either condition, the narrow already-scoped work is the right call and no
   fusion seam should be built into `buildReachList`.
7. **What this artifact explicitly is NOT.** Not a rewrite, not a new fusion seam, not a functional
   change of any kind. Zero logic changed anywhere in the repo by the plan that produced it.
8. **Verification block.** The exact grep and file:line commands a skeptical reader can run to
   re-confirm every citation, so the artifact carries its own proof rather than asking for trust.

Hard rules for the artifact: no em-dashes anywhere (hyphens only); Feynman-simplified prose, no
jargon left unexplained; every claim carries its file:line. Every one of these strings must appear
verbatim somewhere in the file, because the Task 1 gate greps for each one:
`lib/hmi/dial-reach-orchestrator.cjs:290`, `lib/hmi/dial-reach-orchestrator.cjs:199-201`,
`scripts/intent-classifier.cjs:1273-1274`, `scripts/intent-classifier.cjs:1341`,
`scripts/intent-classifier.cjs:1255-1260`, `lib/core/navigation-engine.cjs:1253`,
`244-RESIDUALS.md`, `2026-07-31-dial-rethink-decoupled-from-sensor-bank`,
`f-selector-ranker.cjs`, `orchestration-candidate-lift.cjs`.
</finding_contract>

<changelog_contract>
CHANGELOG.md gets a NEW section at the very TOP of the file, above everything currently there.

Read `<concurrency_hazard>` first. The contract is deliberately name-agnostic: whatever the current
first line of CHANGELOG.md is, the new section goes ABOVE it and that line plus everything below it
stays byte-identical. Do NOT match on, edit, or append to any existing section header regardless of
what it is currently called.

Insert exactly this, as the first three content lines of the file, followed by a blank line and
then the existing file content byte-identical:

## [Unreleased] -- v1.16.0-beta.2

### Added

- **A documented, honest line on what the sensor bank actually controls about the reach dial
  (quick-260731-35r).** When Larry surfaces the reach dial, the little ranked menu of next moves,
  two different things are happening and it is easy to think they are one thing. The sensors, the
  17 small detectors that watch a turn and fire when they recognise something, decide WHETHER that
  dial appears at all. They do not decide WHAT sits at the top of it. The ordering comes entirely
  from scoring your room's own graph nodes, on a completely separate code path that no sensor ever
  reaches. Nothing changed in how any of it runs; what changed is that the boundary is now written
  down at the exact function where a future reader would otherwise assume the opposite, plus a
  finding artifact with every file and line number so anyone can check it in a minute instead of
  tracing the code themselves. The practical payoff: nobody builds on the wrong mental model,
  and nobody mistakes "a sensor fired" for "a sensor ranked what you are seeing."

Rules on this insert:
- Rule A (purely additive). The CHANGELOG diff must show ZERO removed lines. Every pre-existing
  section stays byte-identical.
- Rule U (exactly one Unreleased section). After the edit,
  `grep -c '^## \[Unreleased\]' CHANGELOG.md` must equal 1. There are ZERO today, because the
  concurrent session already folded the previous one into a cut release header.
- Rule E (no em-dashes on added lines). The file contains ~107 pre-existing em-dashes in older
  entries. Do NOT mass-rewrite them; only the newly added lines must be clean.
- Rule V (no version bump). Do NOT touch `package.json`'s `version` or
  `.claude-plugin/plugin.json`'s `version`, and do NOT revert the concurrent session's in-flight
  changes to them. Those belong to `scripts/release.sh`, which this plan never runs.
</changelog_contract>

<tasks>

<task type="auto">
  <name>Task 1: Write the FINDING artifact and close the cross-link loop back from the room</name>
  <files>.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md, ~/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/2026-07-31-dial-rethink-decoupled-from-sensor-bank.md</files>
  <action>
    Write the FINDING artifact exactly per `<finding_contract>`, sourcing every factual claim from
    this plan's `<verified_finding>` section. Do NOT re-trace the code path to rediscover facts that
    are already stated there with their line numbers; that burns context for zero new information.
    Read the room entry's Sections 4, 5 and 10 for the reasoning and the flip-condition wording, and
    keep the artifact consistent with them rather than inventing a second, drifting account.

    Then close the return leg of the cross-link. The room entry currently points at the dev repo but
    predates this quick phase, so it cannot yet point here. Add ONE back-link line to
    `~/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/2026-07-31-dial-rethink-decoupled-from-sensor-bank.md`,
    alongside the existing `Cross-link:` lines near the top of that file (around lines 11 to 19),
    naming
    `MindrianOS-Plugin/.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md`
    as the dev-repo landing of this entry's Section 4 finding. One line added, nothing else in that
    file edited, no em-dashes.

    Explicitly do NOT edit `.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md`. It is a
    closed, gated phase's own register (a tracked file, so the gate below really can detect an edit);
    this artifact cross-references it instead. Also do NOT mirror anything into
    `~/MindrianOS/research/` in this plan; that mirror leg is out of scope here and its absence
    should be noted in the SUMMARY rather than silently filled.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && F=.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md && test -f "$F" && test "$(wc -l < "$F")" -ge 60 && for c in 'lib/hmi/dial-reach-orchestrator.cjs:290' 'lib/hmi/dial-reach-orchestrator.cjs:199-201' 'scripts/intent-classifier.cjs:1273-1274' 'scripts/intent-classifier.cjs:1341' 'scripts/intent-classifier.cjs:1255-1260' 'lib/core/navigation-engine.cjs:1253' '244-RESIDUALS.md' '2026-07-31-dial-rethink-decoupled-from-sensor-bank' 'f-selector-ranker.cjs' 'orchestration-candidate-lift.cjs'; do grep -qF "$c" "$F" || { echo "MISSING CITATION: $c"; exit 1; }; done && ! grep -qP '\x{2014}' "$F" && grep -qF '260731-35r' "$HOME/MindrianRooms/rethinking-mindrianos/research/2026-07-31-dial-rethink-decoupled-from-sensor-bank/2026-07-31-dial-rethink-decoupled-from-sensor-bank.md" && git diff --quiet HEAD -- .planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md && echo "FINDING + BIDIRECTIONAL BACKLINK OK"</automated>
  </verify>
  <done>The FINDING file exists, is at least 60 lines, contains all 10 required citation strings verbatim, has zero em-dashes, the room entry contains a `260731-35r` back-link, and 244-RESIDUALS.md is unchanged versus HEAD (staged or unstaged).</done>
</task>

<task type="auto">
  <name>Task 2: Add the doctrine comment and the CHANGELOG bullet, and prove nothing executable changed</name>
  <files>lib/hmi/dial-reach-orchestrator.cjs, CHANGELOG.md</files>
  <action>
    Edits only in this task. Do NOT commit anything here; Task 3 owns the commit because the commit
    needs its own concurrency guard.

    1. Apply `<comment_contract>` to `lib/hmi/dial-reach-orchestrator.cjs`: append the doctrine lines
       inside the existing `buildReachList` JSDoc block, after the `Pure / synchronous / LOCAL-only`
       line and before that block's closing marker. Honor Rule N (comment characters only), Rule S
       (the sentinel survives verbatim), Rule E (no em-dashes), Rule Q (do not clean up the
       surrounding file's pre-existing em-dashes).

    2. Apply `<changelog_contract>` to CHANGELOG.md: the new `## [Unreleased] -- v1.16.0-beta.2`
       section goes at the very top, purely additive, above whatever the current first line is.
       Read `<concurrency_hazard>` before touching this file: another session has staged, uncommitted
       changes to it, so the working-tree content you are editing already contains their in-flight
       release. Add above it, change nothing of theirs, revert nothing of theirs.

    3. Prove the code edit is comment-only rather than asserting it. Run the two shipped baselines
       captured in `<verified_finding>`: `node tests/test-dial-reach-orchestrator.cjs` (expect 12/12,
       exit 0) and `node tests/test-158-reach-byte-stable.cjs` (expect 3/3, exit 0). The second is
       the real proof: it asserts `buildReachList`'s output is byte-identical to a captured baseline,
       so a green run after the edit is direct evidence the render output did not move. Record both
       results verbatim in the SUMMARY.

    Scope guard: do NOT run `scripts/release.sh`, do NOT bump any version, do NOT edit
    `package.json` or `.claude-plugin/plugin.json`.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && node --check lib/hmi/dial-reach-orchestrator.cjs && grep -qF 'DIAL VISIBILITY, NOT DIAL RANKING' lib/hmi/dial-reach-orchestrator.cjs && test "$(git diff HEAD -U0 -- lib/hmi/dial-reach-orchestrator.cjs | grep '^-' | grep -cv '^---')" -eq 0 && test "$(git diff HEAD -U0 -- lib/hmi/dial-reach-orchestrator.cjs | grep '^+' | grep -v '^+++' | grep -cvE '^\+[[:space:]]*(\*|//|/\*)')" -eq 0 && node tests/test-dial-reach-orchestrator.cjs && node tests/test-158-reach-byte-stable.cjs && head -1 CHANGELOG.md | grep -qxF '## [Unreleased] -- v1.16.0-beta.2' && test "$(grep -c '^## \[Unreleased\]' CHANGELOG.md)" -eq 1 && test "$(git diff HEAD -- CHANGELOG.md | grep '^+' | grep -v '^+++' | grep -cP '\x{2014}')" -eq 0 && echo "COMMENT-ONLY + ADDITIVE CHANGELOG CONFIRMED"</automated>
  </verify>
  <done>`node --check` passes; the sentinel string is present; the orchestrator's diff versus HEAD has zero removed lines and zero non-comment added lines; both dial baselines are green (12/12 and 3/3); CHANGELOG's first line is exactly the new v1.16.0-beta.2 header, exactly one Unreleased section exists, and the added CHANGELOG lines contain zero em-dashes.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the three files by explicit pathspec, without swallowing the concurrent release</name>
  <files>.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md, lib/hmi/dial-reach-orchestrator.cjs, CHANGELOG.md</files>
  <action>
    Re-read `<concurrency_hazard>` before running any git command. Another session had a full
    release staged in this same worktree (vendored `node_modules`, `.claude-plugin/plugin.json`,
    `CHANGELOG.md`, a version bump) when this plan was written.

    1. Pre-flight, print it and act on it, do not skip it. Run `git status --short` and record the
       output verbatim in the SUMMARY. Determine whether a release is still in flight by checking
       whether `.claude-plugin/plugin.json` or `package.json` appear as modified or staged.

    2. Branch on that result, honestly:
       - **If neither version file is dirty or staged:** proceed to step 3 normally.
       - **If either IS dirty or staged:** the concurrent release is still in flight. Steps 3 and 4
         are already safe because they are pathspec-scoped, so proceed, and state plainly in the
         SUMMARY that a concurrent release was in flight, which files were left untouched, and that
         no version file entered this commit. If the pathspec commit cannot be made without touching
         their files for any reason, STOP: leave the three edits present and uncommitted, and report
         the block to the navigator. A blocked commit reported plainly is a correct outcome here; a
         commit that quietly carries someone else's release is not.

    3. Stage by explicit path, and FORCE-ADD the FINDING artifact. `.gitignore:79` ignores
       `.planning/*` (verified live with `git check-ignore -v`), so a plain `git add` on the FINDING
       path silently does nothing and the commit would land with only 2 files and a confusing gate
       failure. The repo's own CLAUDE.md already carries this rule. Run, in this order:
       `git add -f .planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md`
       then `git add lib/hmi/dial-reach-orchestrator.cjs CHANGELOG.md`.
       Then confirm with `git diff --cached --name-only` that EXACTLY those three paths are staged.
       If any of the concurrent session's files appear in that cached list, unstage them one path at
       a time (`git restore --staged <their-path>`), never by resetting the whole index.

    4. Commit by explicit pathspec ONLY. Never `git commit -a`, never `git add -A`, never a bare
       `git commit` against a mixed index. Name the three paths on the commit itself:
       `.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md`,
       `lib/hmi/dial-reach-orchestrator.cjs`, `CHANGELOG.md`.
       Message:
       `docs(dial): name the sensor-bank / buildReachList scope boundary (visibility, not ranking)`
       with a body stating that `buildReachList` is scored only by
       `cortex-reach-adapter.cjs::buildReachScoresFromCortex`, that sensors gate dial visibility via
       the routing_source engine arm only, and that this commit changes zero executable lines.
       Keep the pre-commit chain intact; do NOT set `COMMIT_NO_VERIFY`.

    5. After committing, confirm the commit's file list is EXACTLY those three paths. If anything
       else appears, especially `node_modules`, `package.json` or `.claude-plugin/plugin.json`, the
       commit swept the concurrent session's work: undo it with `git reset --soft HEAD~1`, restore
       the index, and report rather than patching over it.

    Note in the SUMMARY that, per the standing rule, nothing here is live for any user until a
    release ships AND is actually picked up; a running session never hot-reloads the plugin cache.
    The release itself is the orchestrating session's separate explicit step.
  </action>
  <verify>
    <automated>cd /home/jsagi/dev/MindrianOS-Plugin && test "$(git show --name-only --format= HEAD | grep -c .)" -eq 3 && git show --name-only --format= HEAD | grep -qxF 'lib/hmi/dial-reach-orchestrator.cjs' && git show --name-only --format= HEAD | grep -qxF 'CHANGELOG.md' && git show --name-only --format= HEAD | grep -qxF '.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-FINDING.md' && ! git show --name-only --format= HEAD | grep -qE 'node_modules|package.json|package-lock.json|plugin.json' && test "$(git show -U0 HEAD -- lib/hmi/dial-reach-orchestrator.cjs | grep '^-' | grep -cv '^---')" -eq 0 && test "$(git show -U0 HEAD -- lib/hmi/dial-reach-orchestrator.cjs | grep '^+' | grep -v '^+++' | grep -cvE '^\+[[:space:]]*(\*|//|/\*)')" -eq 0 && test "$(git show -U0 HEAD -- CHANGELOG.md | grep '^-' | grep -cv '^---')" -eq 0 && echo "COMMITTED: 3 FILES, COMMENT-ONLY, ADDITIVE, NO RELEASE BLEED"</automated>
  </verify>
  <done>HEAD contains exactly the three expected paths and nothing else; the FINDING artifact really landed (force-added past the `.planning/*` ignore); no `node_modules`, `package.json`, `package-lock.json` or `plugin.json` entry; the orchestrator's committed diff has zero removed lines and zero non-comment added lines; the CHANGELOG's committed diff has zero removed lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed at runtime | The change adds comments and markdown only. No input parsing, no network, no filesystem write at runtime, no new executable path. |
| Shared worktree, concurrent sessions | Another session's staged release lives in the same index. The git index is the shared mutable surface this plan must not corrupt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35R-01 | Tampering | `lib/hmi/dial-reach-orchestrator.cjs` (a frozen Shape F.7 render surface) | mitigate | Task 2 and Task 3 gates both prove the diff contains zero removed lines and zero non-comment added lines, and Task 2 re-runs `tests/test-158-reach-byte-stable.cjs`, which asserts `buildReachList`'s output is byte-identical to a captured baseline. A logic change cannot pass. |
| T-35R-02 | Tampering | the shared git index (concurrent session's in-flight release) | mitigate | Task 3 forbids `git commit -a` / `git add -A` / bare index commits, requires explicit pathspec plus a `git diff --cached --name-only` pre-commit check, and its gate asserts HEAD contains exactly 3 paths with no `node_modules`, `package.json`, `package-lock.json` or `plugin.json` entry. An accidental sweep fails the task. |
| T-35R-03 | Tampering | `.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md` (closed, gated phase register) | mitigate | Task 1's gate runs `git diff --quiet HEAD` against that exact tracked path, catching a staged or unstaged edit to the closed phase's register. |
| T-35R-04 | Information Disclosure | the new FINDING artifact and the room back-link | accept | Content is this repo's own architecture (file paths, line numbers, function names) already present in tracked source and in the room's own filed entry. No user room data, no credentials, no Brain IP. Canon Part 8 is not engaged: nothing egresses anywhere, both writes are local files. |
| T-35R-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO package-manager operations in this plan. No install task exists, so no package legitimacy audit is required. The vendored `node_modules` in the index belongs to the concurrent session and is explicitly excluded from this commit. |
</threat_model>

<verification>
```bash
cd /home/jsagi/dev/MindrianOS-Plugin

node --check lib/hmi/dial-reach-orchestrator.cjs
grep -n 'DIAL VISIBILITY, NOT DIAL RANKING' lib/hmi/dial-reach-orchestrator.cjs

node tests/test-dial-reach-orchestrator.cjs      # 12/12, exit 0 (unchanged from baseline)
node tests/test-158-reach-byte-stable.cjs        # 3/3, exit 0 (byte-identical buildReachList output)

head -3 CHANGELOG.md                             # new v1.16.0-beta.2 section on top
grep -c '^## \[Unreleased\]' CHANGELOG.md        # 1

git show --name-only --format= HEAD              # exactly the 3 expected paths
git show --stat HEAD                             # no node_modules, no version files
```

Canon gates for this change: no em-dashes on any added line (repo hard rule); Canon Part 8 not
engaged (zero network, zero Brain, local files only); Canon Part 7 honored (this documents an
existing boundary rather than building a second selection surface); Tri-Polar impact is
documentation-only and identical across CLI, Desktop and Cowork. Per the Dev-Research Compositing
mandate, the same finding now lives in both homes with a working cross-link in each direction.
</verification>

<success_criteria>
- `260731-35r-FINDING.md` exists, is at least 60 lines, and carries all 10 required citation strings
  verbatim so a skeptical reader can re-verify every claim without trusting the artifact.
- The finding states the scope boundary plainly (sensor bank = dial visibility gate, NOT a dial
  ranking input) as a DELIBERATE, NAMED call, and names both flip conditions with their counts:
  a fifth patch at the fired-candidate-composition seam after 172-07, 222-02, 209-01/237-04 and
  244-04; or the sensor count crossing roughly 25 to 30 files against today's baseline of 17.
- The cross-link runs BOTH ways: the finding names the room entry path, and the room entry names
  `260731-35r-FINDING.md`.
- `.planning/phases/244-semantic-trigger-tier/244-RESIDUALS.md` is byte-identical (never edited).
- Exactly ONE doctrine comment added to `lib/hmi/dial-reach-orchestrator.cjs`, inside the existing
  `buildReachList` header block, containing the literal `DIAL VISIBILITY, NOT DIAL RANKING`.
- ZERO executable change anywhere: no removed lines and no non-comment added lines in the
  orchestrator diff, `node --check` clean, and both shipped dial baselines still green (12/12 and
  3/3), including the byte-stability fence.
- CHANGELOG.md has a NEW `## [Unreleased] -- v1.16.0-beta.2` section as its first line with one
  Feynman-simplified, JTBD-oriented, em-dash-free bullet; every pre-existing section byte-identical
  (zero removed lines); exactly one Unreleased section exists.
- No version bump: `package.json` and `.claude-plugin/plugin.json` untouched by this plan, and the
  concurrent session's in-flight changes to them neither reverted nor committed here.
  `scripts/release.sh` never run.
- The commit contains EXACTLY three files (the FINDING artifact force-added past the `.planning/*`
  ignore) and no `node_modules`, `package.json`, `package-lock.json` or `plugin.json` entry, with
  the pre-commit chain intact.
</success_criteria>

<output>
Create `.planning/quick/260731-35r-phase-244-1-document-dial-render-sensor-/260731-35r-SUMMARY.md` when done.
Record: the exact insertion point used in `dial-reach-orchestrator.cjs`; the verbatim output of both
dial baseline runs; the verbatim `git status --short` pre-flight and what the concurrent release was
doing at commit time; the resolved CHANGELOG header discrepancy (the brief said
`v1.15.3-beta.51`, the live file had already moved through `v1.15.4-beta.1` to a cut
`## [1.16.0-beta.1]`, and none of them were touched); the fact that `~/MindrianOS/research/`
mirroring was deliberately left out of scope; and the reminder that none of this is live for any
user until a release ships and is actually picked up.
</output>
