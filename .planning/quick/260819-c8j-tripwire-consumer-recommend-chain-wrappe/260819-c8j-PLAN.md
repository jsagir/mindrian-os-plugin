---
phase: quick-260819-c8j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/brain-client.cjs
  - data/brain-surface-contract.json
  - tests/test-247-contract-client.cjs
  - lib/brain/chain-recommender.cjs
  - lib/core/navigation/memory-events.cjs
  - lib/mcp/tools/sensors.cjs
  - data/mcp-tool-connectors.json
  - data/connector-registry.json
  - tests/test-c8j-brain-wire.cjs
  - tests/test-c8j-chain-consumer.cjs
  - tests/test-c8j-suggest-next-offer.cjs
autonomous: true
requirements: [WS-C1, WS-C2, WS-C3, WS-D2]

must_haves:
  truths:
    - "A caller can ask the live Brain for an ordered framework chain from a problem_type enum alone, through one wrapper that is a sibling of feedsIntoChains."
    - "A fired reach carrying a brain_framework_chain companion produces a render-ready chain offer; a fired reach without one produces nothing."
    - "suggest_next returns the chain offer when the companion is present and the Brain is reachable, and still returns a normal suggestion when the Brain is absent."
    - "A Brain framework name missing from the local framework_index gets exactly one normalizeFrameworkName retry, then degrades honestly instead of being dropped or invented."
    - "A disagreement between the Brain's commands[] and the local resolution is recorded as a LOCAL memory_event, never silently reconciled."
    - "Part 8 in-process classify() coverage is complete at the single dispatch seam instead of 4-of-16 wrappers, with no site classified twice into a contradictory outcome."
    - "Posture authority stays LOCAL: nothing on this path computes or asserts autonomous_safe."
    - "Sensors stay pure: no new require lands in lib/core/sensors/ or lib/core/insight-sensors.cjs."
  artifacts:
    - path: "lib/core/brain-client.cjs"
      provides: "recommendChain wrapper + the callTool Part 8 belt"
      contains: "async function recommendChain"
    - path: "data/brain-surface-contract.json"
      provides: "recommend_chain loop_tools entry (the arg-shape authority the 247 conformance test derives from)"
      contains: "recommend_chain"
    - path: "lib/brain/chain-recommender.cjs"
      provides: "companion consumer (chainOfferForReach) + chain adapter (adaptChainToRunInput)"
      contains: "chainOfferForReach"
    - path: "lib/core/navigation/memory-events.cjs"
      provides: "brain_cmdmap_divergence event type"
      contains: "brain_cmdmap_divergence"
    - path: "lib/mcp/tools/sensors.cjs"
      provides: "suggest_next chain_offer field (omit-when-absent)"
      contains: "chain_offer"
    - path: "tests/test-c8j-brain-wire.cjs"
      provides: "wrapper shape + degradation + part8 coverage proof"
    - path: "tests/test-c8j-chain-consumer.cjs"
      provides: "companion parsing + adapter normalization + divergence event proof"
    - path: "tests/test-c8j-suggest-next-offer.cjs"
      provides: "chain-offer presence/absence + read-only fence proof"
  key_links:
    - from: "lib/core/brain-client.cjs recommendChain"
      to: "callTool('recommend_chain', ...)"
      via: "the single dispatch seam, no second transport"
      pattern: "callTool\\('recommend_chain'"
    - from: "lib/brain/chain-recommender.cjs chainOfferForReach"
      to: "brainClient.recommendChain"
      via: "the async offer layer, above decide(), never inside a sensor"
      pattern: "recommendChain\\("
    - from: "lib/brain/chain-recommender.cjs adaptChainToRunInput"
      to: "commandResolver.commandsForFramework"
      via: "the ONE governed framework-to-command door"
      pattern: "commandsForFramework\\("
    - from: "lib/brain/chain-recommender.cjs adaptChainToRunInput"
      to: "navigation.logMemoryEvent(db, 'brain_cmdmap_divergence', ...)"
      via: "the Part 9 SQL chokepoint, best-effort, db-gated"
      pattern: "brain_cmdmap_divergence"
    - from: "lib/mcp/tools/sensors.cjs suggest_next"
      to: "chainRecommender.chainOfferForReach"
      via: "the fired reach's companions array"
      pattern: "chainOfferForReach\\("
---

<objective>
Wire the tripwire consumer: the Brain's `recommend_chain` tool becomes a live plugin path
from a fired sensor reach to a chain offer, and the in-process Part 8 guard moves to the
one seam every Brain call already flows through.

Today the `brain_framework_chain:<problem_type>` companion is DECLARED by three sensors
(SENS-01 at `lib/core/insight-sensors.cjs:617`, SENS-05 methodology-decision, SENS-09
diffusion-adoption) and CONSUMED BY NOTHING. `.companions` has zero readers. The Brain
half shipped on 2026-08-18/19 (read tool #24, `recommend_chain`, live). This plan closes
the gap named in `docs/2026-08-19-PLAN-dual-graph-release.md` WS-C1/C2/C3 and WS-D2, and
in `docs/2026-08-19-HANDOFF-brain-plugin-sync-release.md` section 1b.

Purpose: the LOCAL room graph supplies WHEN (sensors fire on room state); the Brain canon
supplies WHICH and IN WHAT SEQUENCE. Without a consumer, the plugin has a tripwire with no
bell. This is the WHICH/SEQUENCE half of the dual-graph loop.

Output:
- `recommendChain()` in `lib/core/brain-client.cjs`, a straight sibling of `feedsIntoChains`
  (`:1306`), with the enum-only Part 8 gate at the wire.
- `chainOfferForReach()` + `adaptChainToRunInput()` in `lib/brain/chain-recommender.cjs`,
  replacing the deliberately inert "122-04 async wiring" placeholder narrative with the live
  path it always promised.
- `chain_offer` on the `suggest_next` MCP response (omit-when-absent), so the hookless
  surface reaches the chain without a hook.
- The Part 8 in-process classify() belt at `callTool`, taking coverage from 4 wrappers to 16.

NOT in scope (owned elsewhere, do not touch): the `gate_answer` / `chain_run` resume seam
in `lib/mcp/tools/chain.cjs`, `lib/mcp/tools/gate.cjs`, `lib/mcp/gate-ledger.cjs` -- that is
in-flight quick task 260819-c55 (WS-C4). This plan CONSUMES `chain_run`'s start contract
(an ordered framework-name array) and edits none of those three files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@docs/2026-08-19-PLAN-dual-graph-release.md
@docs/2026-08-19-HANDOFF-brain-plugin-sync-release.md

@lib/core/brain-client.cjs
@lib/brain/chain-recommender.cjs
@lib/mcp/tools/sensors.cjs
@lib/workflow/command-resolver.cjs
@lib/core/part8-egress-guard.cjs
@data/brain-surface-contract.json
@tests/test-247-contract-client.cjs
@tests/helpers/brain-capture-server.cjs
</context>

<interfaces>
The contracts this plan is built against, read from source at plan time. Executors do NOT
need to go find these again.

**`callTool(toolName, args)`** -- `lib/core/brain-client.cjs:412`. The single dispatch seam.
Return contract, test-pinned by ~82 degradation tests, DO NOT WIDEN:
- no API key after `_tryAutoRegister()` -> `null`
- HTTP 403 -> `{ error: 'tier_denied', tool, message }`
- invalid key (from `_ensureSession`) -> `{ error: 'invalid_key', ... }`
- transport failure / non-OK / unparseable -> `null`
- success -> the parsed JSON payload from the SSE `data:` line

**`feedsIntoChains(seeds, maxHops)`** -- `:1306`. The sibling pattern the new wrapper copies:
`return callTool('feeds_into_chains', { seeds, max_hops: maxHops });` -- ZERO result
reshaping, so sentinels and the transport-null pass through untouched (247-02 convention).

**`normalizeFrameworkName(raw)`** -- `:1205`. Exists, exported, currently zero runtime callers.
`callTool('normalize_framework_name', { raw })`.

**`part8EgressGuard.classify(payload, { toolName })`** -- `lib/core/part8-egress-guard.cjs:337`
-> `{ verdict: 'allow'|'block'|'ambiguous', class, reason }`. Pure LOCAL, zero network.
`block` fires ONLY on a CONTENT-SET forbidden-pattern hit (step 1 default-deny scan).
Everything unrecognized is `ambiguous`, never `block`.

**The 4 existing in-process classify sites** (leave byte-unchanged):
`query()` `:601`, `hatAwareRecommend()` `:942`, `suggestValidationSteps()` `:1056`,
`sendPacket()` `:1643`. The first three classify a RAW field BEFORE `sanitizeCypherInput`
and BEFORE interpolation. Their own docblocks prove why: classifying the ASSEMBLED string
instead lets the template's own word "Framework" launder an embedded canary from
`ambiguous` to `allow`, and `sanitizeCypherInput` strips the `@` the PII pattern keys on.

**`sendPacket()`'s belt posture** `:1643` -- the pattern to copy for ambiguous handling:
`block` -> refuse with a sentinel + best-effort log; `ambiguous` -> allow through, "do not
double-prompt", because a separate primary guard exists.

**`_logEventBestEffort(db, eventType, payload)`** `:1437` -- `if (!db) return;` then
lazy-requires `navigation.cjs` and calls `logMemoryEvent`. Never throws.

**`logEvent(db, eventType, payload, opts)`** -- `lib/core/navigation/memory-events.cjs:701`.
Returns `{ ok:false, reason:'invalid_event_type' }` for any `eventType` not in the frozen
`EVENT_TYPES` Set (`:10`). Honors `payload.dedupe_key` with a 60s TTL. Tests assert a FLOOR
plus named membership, never an exact count, so additive extension is safe.

**`makeReach(opts)`** -- `lib/core/sensors/sensor-types.cjs:237`. `companions` is a frozen
array of STRINGS on a frozen reach. Non-strings are dropped defensively.

**The three companion producers** (read-only reference, do NOT edit):
- `lib/core/insight-sensors.cjs:617` -> `'brain_framework_chain:' + pt`
- `lib/core/sensors/sensor-methodology-decision.cjs:108-110` -> `'brain_framework_chain:' + pt`
  and additionally `'brain_framework_chain:' + pt + ':' + fw` (the second segment)
- `lib/core/sensors/sensor-diffusion-adoption.cjs:218` -> `'brain_framework_chain:adoption-capacity'`
`pt` comes from `problemTypeOf(tuple)`, which returns `tuple.problem_type` verbatim or the
string `'undefined'`. It is NOT guaranteed to be a Brain ProblemType name -- that is exactly
why the wrapper carries a shape gate plus an alias projection.

**`commandsForFramework(name)`** -- `lib/workflow/command-resolver.cjs:85` -> `string[]`,
`[]` for unknown. `composeWorkflow(chain)` `:110` -> `[{ step, framework, command|null,
optional }]`, degrading unknown frameworks to `{ command: null, optional: true }`. This
module NEVER touches the Brain (grep-guarded).

**`chain_run` start contract** -- `lib/mcp/tools/chain.cjs:479`. Input `chain:
z.array(z.string().min(1)).min(1)` -- an ordered array of framework NAMES. It calls
`chainResolve(chain)` (= `composeWorkflow`) itself, then `chainRun(workflow, ...)`. The
adapter's job is therefore to produce the NAME ARRAY, nothing further.

**`data/brain-surface-contract.json` + `tests/test-247-contract-client.cjs`** -- the contract
file is the arg-shape AUTHORITY; the test derives every expectation from it and asserts the
captured `tools/call` arguments carry EXACTLY the contracted keys, no more, no fewer. It
holds TWO hand-maintained hardcodes, not one, and BOTH have to move when a loop tool is added:

  (a) `WRAPPER_MAP` (`:47`) plus `CALL_ADAPTERS` (`:60`). A contract entry with no matching
      map entry is REPORTED as drift, which is the point -- this one fails LOUDLY and
      descriptively.
  (b) A literal loop-tool COUNT assertion at `:165-169`:
      `assert.strictEqual(Object.keys(contract.loop_tools).length, 6)`, sitting inside a test
      whose own name is `'contract_version is 1 and loop_tools has exactly the 6 loop tools'`.
      This one is NOT a drift report and NOT descriptive: it is a bare `7 !== 6` hard failure
      that turns the entire 247 suite red the instant a seventh contract entry lands. The test
      NAME is a second hardcode inside the first, and it lies quietly if only the number moves.

Anything that adds to `loop_tools` must update (a) AND both halves of (b) in the same edit.
</interfaces>

<fences>
Hard constraints. Breaking any of these fails the task, not just a test.

1. **Sensor purity.** `tests/test-249-capture-seam.cjs` Test 14 greps
   `/require\(\s*['"][^'"]*(brain-client|enrichment-queue)\.cjs['"]\s*\)/` across every
   `lib/core/sensors/*.cjs` and `lib/core/navigation-engine.cjs`. Add NO require to those
   files. The Brain call lives at the async offer layer, above `decide()`.

2. **Guard census allowlist.** `tests/test-252-guard-census.cjs` census.1 fails the build if
   ANY file under `lib/ bin/ scripts/ hooks/` contains an executable `is/ensureAvailable(`
   call and is not in the frozen classification lists. `lib/brain/chain-recommender.cjs` and
   `lib/core/brain-client.cjs` are already ROUTE-classified -- that is WHY the consumer goes
   in `chain-recommender.cjs` and not a new file. `lib/mcp/tools/sensors.cjs` is NOT
   classified: it must never call `isAvailable()` / `ensureAvailable()` directly.

3. **Seam liveness.** census.2 requires every ROUTE seam-claim file (chain-recommender.cjs
   included) to `require()` `refusal-messaging.cjs` on an executable line. It already does at
   `:399`. Do not remove that.

4. **No command literals in `lib/brain/chain-recommender.cjs`.** The 122-02 grep guard. The
   file builds command strings only by splitting data on `':'`. The new adapter compares
   command arrays that arrive as DATA; it must not write a command prefix as a source literal.

5. **`suggest_next` stays write-free.** Its `hitl_why` asserts "no write of any kind: no
   schema migration, no memory_event, no weight-state snapshot". The offer path must pass NO
   db handle, so the divergence memory_event cannot fire there.

6. **Posture authority stays LOCAL.** Nothing added here computes, asserts, or forwards
   `autonomous_safe`. `recipe-maps.cjs` remains the one autonomy authority; the Brain
   recommends, never triggers.

7. **The `callTool` null contract does not widen.** No-key still returns `null`. Transport
   failure still returns `null`. The belt adds a NEW sentinel object for a genuine content
   block; it never converts an existing `null` path into something else.

8. **Do not touch** `lib/mcp/tools/chain.cjs`, `lib/mcp/tools/gate.cjs`,
   `lib/mcp/gate-ledger.cjs` (in-flight task 260819-c55), or `lib/core/insight-sensors.cjs`
   and `lib/core/sensors/*.cjs` (fence 1).

9. **No em-dashes anywhere.** CJS only. Doctrine-comment style: every non-obvious choice
   carries the reason a future reader would otherwise have to reconstruct.
</fences>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recommendChain wrapper (C1) + the Part 8 belt at callTool (D2)</name>
  <files>lib/core/brain-client.cjs, data/brain-surface-contract.json, tests/test-247-contract-client.cjs, tests/test-c8j-brain-wire.cjs</files>

  <behavior>
    recommendChain:
    - `recommendChain('Undefined Problem')` emits `tools/call` name `recommend_chain` with
      arguments EXACTLY `{ problem_type: 'Undefined Problem', max_steps: 6 }`. Both keys
      always present (the 247 conformance test asserts an exact key set, and an `undefined`
      value would be dropped by JSON.stringify).
    - `recommendChain('undefined')` projects the local sensor slug to `'Undefined Problem'`
      on the wire. Same for `'ill-defined'`/`'idp'` -> `'Ill-Defined Problem'` and
      `'well-defined'`/`'wdp'` -> `'Well-Defined Problem'`; matching is case-insensitive on
      the trimmed token.
    - An unmapped but well-shaped token (e.g. `'adoption-capacity'`) passes through
      UNCHANGED. The Brain answers unknown problem types with its own honest refusal; the
      plugin does not pre-judge 23 of the 26 live ProblemType nodes it cannot enumerate.
    - `max_steps` is clamped to an integer in 1..6; absent / NaN / out of range -> 6.
    - A problem_type that fails the generic-handle shape gate (see action) makes ZERO
      network calls and returns `{ error: 'invalid_problem_type', tool: 'recommend_chain' }`.
    - Every callTool return shape passes through UNRESHAPED: `null` stays `null`,
      `{error:'tier_denied'}` and `{error:'invalid_key'}` stay themselves, a success payload
      is returned verbatim.

    contract registration (the 247 suite must stay green, not just gain a case):
    - `data/brain-surface-contract.json` `loop_tools` holds SEVEN entries after this task; it
      held six.
    - `tests/test-247-contract-client.cjs` asserts SEVEN, in BOTH places it says a number: the
      `assert.strictEqual(..., 7)` and the test description string "exactly the 7 loop tools".
    - `node --test tests/test-247-contract-client.cjs` is green with the new entry present.
      A run that reports `7 !== 6` means the count assertion was not updated in the same edit.

    callTool Part 8 belt:
    - A payload carrying a CONTENT-SET forbidden pattern, sent to a previously-unguarded
      tool (e.g. `discover_structure`), is REFUSED before any network call and returns
      `{ error: 'egress_blocked', tool: '<name>', egress_class: '<class>' }`.
    - A clean generic-handle payload to the same tool still reaches the wire unchanged.
    - No-key still returns `null` (the belt sits AFTER the key gate).
    - An `ambiguous` verdict never blocks and never prompts.
    - The 4 pre-existing guard sites keep their exact current behavior.
  </behavior>

  <action>
Write the failing legs of `tests/test-c8j-brain-wire.cjs` FIRST, then implement.

**1a. `recommendChain` in `lib/core/brain-client.cjs`.** Place it immediately after
`feedsIntoChains` (`:1306`), inside the same "Loop-contract read wrappers" block, so the
sibling relationship is physical and not just claimed. Export it from `module.exports`
alongside `feedsIntoChains`.

Above it, add the two frozen Part 8 constants and one normalizer:

- `PROBLEM_TYPE_HANDLE_RE` -- a generic-handle shape gate: first character a letter, then
  letters, digits, single spaces and hyphens only, total length 1..49. This is the enum-only
  enforcement. It exists because `problemTypeOf(tuple)` returns `tuple.problem_type`
  VERBATIM off the /mos:diagnose tuple, so the companion string is not structurally
  guaranteed to be an enum. A handle-shaped token cannot carry prose, a sentence, a URL, an
  email, or a payload. Document that reasoning inline.
- `BRAIN_PROBLEM_TYPE_ALIASES` -- a frozen lowercase map projecting the local sensor slugs
  onto the Brain's canonical ProblemType node names: `undefined`/`udp` -> `Undefined
  Problem`, `ill-defined`/`ill_defined`/`idp` -> `Ill-Defined Problem`,
  `well-defined`/`well_defined`/`wdp` -> `Well-Defined Problem`. Doctrine comment: this is a
  PROJECTION, not an allow-list. Unmapped tokens pass through and the Brain rules on them
  (`data/brain-census.generated.json` shows 26 live ProblemType nodes but names only 3, so a
  local allow-list would silently block 23 valid types). State explicitly that this map is
  NOT `lib/brain/chain-recommender.cjs`'s `PROBLEM_TYPE_ALIASES` (`:71`), which projects onto
  the LOCAL router codes UDP/IDP/WDP -- different target, deliberately not shared, so a
  reader cannot conflate them.
- `_normalizeBrainProblemType(raw)` -> canonical string, or `null` when the input is not a
  string, is empty after trim, or fails `PROBLEM_TYPE_HANDLE_RE`. Alias lookup runs on the
  lowercased trimmed token; a hit returns the canonical name, a miss returns the trimmed
  token unchanged.

`recommendChain(problemType, maxSteps)` then: normalize; on `null` return
`{ error: 'invalid_problem_type', tool: 'recommend_chain' }` WITHOUT calling `callTool`
(no wire, no key consumption, no auto-register attempt); otherwise clamp `max_steps` and
`return callTool('recommend_chain', { problem_type, max_steps })`. NO result reshaping --
match `feedsIntoChains` exactly and say so in the docblock, naming the 247-02 convention.

Document the live output shape in the docblock so consumers do not have to probe the Brain:
`{ tool, backend, grounded, problem_type, seed_candidates, chain: [{ step, framework,
pagerank, edge_confidence, commands }], note, execution_hint }`. Mark it as the shape
verified live on 2026-08-19 AND now contractually pinned: the Brain team froze
`recommend_chain` as a v1 ADDITIVE-ONLY contract in their own repo
(`ProblemsWorthSolving-Brain`, `docs/RECOMMEND-CHAIN-CONTRACT.md`, commit `721d8e1`), so under
v1 these keys may gain siblings but will not be renamed or removed, and an unknown problem
type is GUARANTEED to come back as `{ grounded: false, chain: [] }` rather than an error or a
throw. Note that the wrapper still does not depend on any of it (pass-through), so a v2 or an
out-of-contract change degrades at the consumer, not here.

**1b. Contract registration.** Add to `data/brain-surface-contract.json` `loop_tools`:
`"recommend_chain": { "tier": "read", "args": { "problem_type": "string", "max_steps": "int<=6" } }`.
That makes `loop_tools` a SEVEN-entry object; it was six.

Then make THREE edits in `tests/test-247-contract-client.cjs`, all in the same pass, because
the file carries two independent hardcodes and only one of them fails descriptively:

  i.   `WRAPPER_MAP` (`:47`) gains `recommend_chain: 'recommendChain'`.
  ii.  `CALL_ADAPTERS` (`:60`) gains
       `recommend_chain: (brain) => brain.recommendChain('Undefined Problem', 6)`.
  iii. The hardcoded COUNT assertion at `:165-169` MUST move too, BOTH halves of it:
       - `assert.strictEqual(Object.keys(contract.loop_tools).length, 6);` -> `..., 7);`
       - the test description string
         `'contract_version is 1 and loop_tools has exactly the 6 loop tools'`
         -> `'contract_version is 1 and loop_tools has exactly the 7 loop tools'`

This is not optional tidying. The instant the contract entry from the paragraph above lands,
that assertion fails `7 !== 6` and this task's own verify command
(`node --test tests/test-247-contract-client.cjs`) can never go green, no matter how correct
the wrapper is. Updating the number while leaving the description at "6" is the same bug one
layer down: the next reader trusts the sentence over the assertion. Change both.

(i) and (ii) make the wrapper self-verifying against the same drift detector the other loop
tools use, and they are the reason both arg keys must always be emitted. (iii) is the one
place in the 247 suite that is a hand-maintained COUNT rather than a contract derivation, so
it is the one place a contract addition has to be mirrored by hand rather than being picked
up automatically.

**1c. The Part 8 belt in `callTool`.** Insert AFTER `if (!key) return null;` (`:422`) and
BEFORE `_ensureSession` (`:428`). Placement is load-bearing: after the key gate so the
no-key `null` contract is byte-unchanged; before the session so a blocked payload opens no
socket at all.

Behavior: lazy-`require('./part8-egress-guard.cjs')` inside a try/catch, call
`classify(args, { toolName: toolName })`. On `verdict === 'block'` return
`{ error: 'egress_blocked', tool: toolName, egress_class: verdict.class || 'content_set' }`.
On any other verdict, fall through unchanged. A belt-INTERNAL throw degrades to existing
behavior. The belt never throws and never opens a Brain wire to judge.

MECHANISM CHOICE, state this in the doctrine comment so the alternative is not relitigated:
the callTool chokepoint is chosen over per-wrapper guards because it is one guard in one
function covering 16/16 wrappers, versus ~12 near-identical edits that drift the moment a
17th wrapper lands. Double-classification is not double-prompting: `classify()` returns a
verdict, it does not elicit. The 4 existing raw-field guards are NOT removed and NOT
weakened -- their own docblocks prove the belt CANNOT replace them (classifying the
assembled string lets the template word "Framework" launder a canary, and
`sanitizeCypherInput` strips the `@` the PII pattern keys on), so this is a belt UNDERNEATH
them, never a substitute. Ambiguous stays allow-through, copying `sendPacket`'s belt posture
at `:1643` verbatim in spirit.

Also record why `egress_blocked` is a sentinel OBJECT and not `null`: `null` is the
test-pinned transport-failure signal across ~82 degradation tests, and conflating a
constitutional refusal with an outage would make the refusal invisible. The sentinel-object
idiom is already established by `tier_denied` (`:497`) and `invalid_key`.

**1d. `tests/test-c8j-brain-wire.cjs`.** Reuse `tests/helpers/brain-capture-server.cjs` (the
real loopback capture server, no fetch mocking) exactly as `tests/test-247-contract-client.cjs`
does; do not invent a second harness. Legs:
  1. arg shape: exactly `{problem_type, max_steps}`, canonical name on the wire.
  2. alias projection: `'undefined'` -> `'Undefined Problem'`; `'wdp'` -> `'Well-Defined Problem'`.
  3. pass-through: `'adoption-capacity'` reaches the wire unchanged.
  4. clamp: absent -> 6; `99` -> 6; `0` -> 6; `3` -> 3.
  5. shape-gate refusal: a prose/PII-shaped problem_type returns `invalid_problem_type` AND
     the capture server recorded ZERO calls (prove the absence, not just the return value).
  6. degradation: transport failure -> `null`; 403 -> `tier_denied` passthrough; success
     payload returned byte-identical.
  7. belt: `discoverStructure` with a forbidden-pattern framework name returns
     `egress_blocked` with zero captured calls; a clean name still reaches the wire.
  8. belt does not regress the no-key path: unset key -> `null`.
  </action>

  <verify>
    <automated>node --test tests/test-c8j-brain-wire.cjs</automated>
    <automated>node --test tests/test-247-contract-client.cjs</automated>
    <automated>node tests/test-249-brain-egress.cjs</automated>
    <automated>node tests/test-239-query-egress-canary.cjs</automated>
    <automated>node tests/test-245-egress-contentless.cjs</automated>
    <automated>node --test tests/test-247-brain-client-403.cjs</automated>
    <automated>node tests/test-252-guard-census.cjs</automated>
    <automated>grep -q recommend_chain data/brain-surface-contract.json</automated>
    <automated>node -e "const c=require('./data/brain-surface-contract.json');const n=Object.keys(c.loop_tools).length;if(n!==7)throw new Error('loop_tools has '+n+' entries, expected 7');console.log('loop_tools 7 ok')"</automated>
    <automated>grep -q "exactly the 7 loop tools" tests/test-247-contract-client.cjs</automated>
    <automated>test $(grep -o "\xe2\x80\x94" lib/core/brain-client.cjs | wc -l) -eq 0</automated>
  </verify>

  <done>
`recommendChain` exists, is exported, sits beside `feedsIntoChains`, emits exactly the two
contracted arg keys, projects the three local slugs onto canonical Brain ProblemType names,
passes unmapped handles through, refuses non-handle input with zero network, and reshapes no
result. `recommend_chain` is registered in `data/brain-surface-contract.json`, `loop_tools`
now holds seven entries, the 247 suite's hand-maintained count assertion AND its test
description string both read 7, and `node --test tests/test-247-contract-client.cjs` is green
end to end (conformance derivation included, no `7 !== 6`). `callTool` refuses a content-hit
payload for a previously unguarded tool with an `egress_blocked` sentinel while the no-key
`null` and transport `null` contracts are byte-unchanged and all four pre-existing guard sites
are untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: companion consumer (C2) + chain adapter (C3) in chain-recommender</name>
  <files>lib/brain/chain-recommender.cjs, lib/core/navigation/memory-events.cjs, tests/test-c8j-chain-consumer.cjs</files>

  <behavior>
    parseChainCompanions(reach):
    - reach null / not an object / no `companions` array -> `[]`
    - `['brain_framework_chain:Undefined Problem', 'whitespace']` -> `['Undefined Problem']`
    - `['brain_framework_chain:IDP:SWOT Analysis']` -> `['IDP']` (segments past the
      problem_type are ignored for now, and that is documented, not silently dropped)
    - duplicates collapse, first-seen order preserved
    - a companion that is not the handle, or is the bare handle with no second segment, is skipped

    chainOfferForReach(reach, opts):
    - no `brain_framework_chain` companion -> `null` (nothing to offer, field omitted upstream)
    - companion present, Brain unavailable -> `{ problem_type, chain: [], grounded: false,
      unmapped_count: 0, note: 'brain_unavailable' }` (a disclosure, never a throw, never silence)
    - companion present, transport null -> same shape with `note: 'brain_unreachable'`
    - companion present, `tier_denied` / `invalid_key` sentinel -> same shape, note carries
      the sentinel's own error token
    - companion present, Brain answers -> `{ problem_type, chain: [...], grounded: true,
      unmapped_count: N, chain_input: [names...], note: <the Brain's note or null> }`
    - exactly ONE Brain recommend_chain call per invocation (only the first companion is consulted)
    - never throws for any input

    adaptChainToRunInput(brainChain, opts):
    - chain order is preserved, one output name per input step
    - a framework the local `framework_index` knows is passed through untouched with zero
      normalize calls
    - a framework the local index misses triggers EXACTLY ONE `normalizeFrameworkName` call;
      if the normalized name hits the local index, the NORMALIZED name is used
    - if the normalized name still misses, the ORIGINAL name is kept and appears in `unmapped`
      (`composeWorkflow` will degrade it to `{command:null, optional:true}` downstream)
    - Brain `commands[]` disagreeing with the local resolution produces a divergence record
    - with `opts.db`, each divergence writes ONE `brain_cmdmap_divergence` memory_event;
      without `opts.db`, zero writes and the divergences still return as data
    - a repeat inside 60s dedupes via `payload.dedupe_key`
    - never returns or computes `autonomous_safe`
  </behavior>

  <action>
Write `tests/test-c8j-chain-consumer.cjs` legs first, then implement.

**2a. Register the event type.** In `lib/core/navigation/memory-events.cjs`, add
`'brain_cmdmap_divergence'` to the frozen `EVENT_TYPES` Set (`:10`), following the existing
additive-extension idiom exactly: a dated comment block naming this task, saying what emits
the event and why, and noting that `logEvent` rejects anything outside the Set so this string
is accepted ONLY because it is now in it. Mirror the Phase 110-02 / 124-02 three-string and
two-string block style. Tests assert a floor plus named membership, never an exact count, so
this is safe.

**2b. The consumer, in `lib/brain/chain-recommender.cjs`.** This file, not a new one:
`tests/test-252-guard-census.cjs` census.1 fails the build for any unclassified file
containing an executable `is/ensureAvailable(` call, and this file is already ROUTE-classified
(`:135` of that test). A new module would be an unclassified guard site. Say that in the
header comment so nobody "tidies" it into its own file later.

Update the file header: the `recommendFrameworkChain` docblock at `:331-351` currently
describes the live path as unbuilt "122-04 async wiring". That prose is now false in one
direction and true in another -- rewrite it to say the live Brain chain path SHIPPED as
`chainOfferForReach` below, while `recommendFrameworkChain` itself stays synchronous and
unchanged (14+ callers key on its `string[]` return; do not reshape it).

Add near the top:
- `CHAIN_COMPANION_HANDLE = 'brain_framework_chain'` (frozen). Parsing splits on `':'`, so
  no command literal is introduced -- fence 4.
- `const commandResolver = require('../workflow/command-resolver.cjs');` -- the ONE governed
  framework-to-command door (Part 7, R4: mint no second resolver).

`parseChainCompanions(reach)`: defensive reads only, `String.prototype.split(':')`, first
segment must equal the handle, second segment must be non-empty after trim, segments 3+ are
recorded as ignored in a comment (the `sensor-methodology-decision.cjs:110` producer emits a
`:<framework>` third segment; consuming it is a named follow-up, not a silent drop).

`chainOfferForReach(reach, opts)` -- async. Order of operations, each with its reason:
  1. parse companions; empty -> return `null` immediately. No Brain touch when there is
     nothing to ask about.
  2. take companions[0] ONLY. One Brain call per pull bounds the cost of a path that runs on
     every `suggest_next`; consuming every companion would multiply wire calls by reach
     breadth for no additional first-offer value. Document as a deliberate bound.
  3. `brainClient.isAvailable()` false -> return the `grounded:false` disclosure object with
     `note: 'brain_unavailable'`. Do NOT return `null` here: `null` means "nothing to offer",
     and conflating "no companion" with "Brain is down" is exactly the silent degrade the
     honesty rail forbids. The existing `refusal-messaging.cjs` require at `:399` stays
     (census.2 seam liveness).
  4. `await brainClient.recommendChain(problemType)`; map the result to a note token:
     `null` -> `brain_unreachable`; `{error:'tier_denied'|'invalid_key'|'invalid_problem_type'}`
     -> that token; `grounded === false` or a missing/empty `chain` array -> `unknown_problem_type`.
     Every one of these returns the disclosure shape, never a throw. Note in the code that the
     frozen v1 contract (see Task 1a) GUARANTEES an unknown problem type arrives as
     `{grounded:false, chain:[]}` and never as an error or an exception, so
     `unknown_problem_type` is a normal expected branch here, not an error path; do not build
     try/catch scaffolding for a case the contract says cannot throw.
  5. success -> call `adaptChainToRunInput(result.chain, opts)` and return the offer carrying
     the Brain's `chain` verbatim for rendering PLUS `chain_input` (the adapter's name array,
     ready to hand to `chain_run`) and `unmapped_count`.
Wrap the whole body so it never throws; a caught error returns the disclosure shape with
`note: 'offer_error'`.

`adaptChainToRunInput(brainChain, opts)` -- async. For each step (order preserved):
  - read `step.framework` (string, else skip the step and count it as unmapped)
  - `commandResolver.commandsForFramework(name)`; non-empty -> keep the name
  - empty -> `await brainClient.normalizeFrameworkName(name)` ONCE. Read the normalized
    string defensively across the plausible shapes (`result.canonical` / `result.name` /
    `result.normalized` / a bare string), because this wrapper has had zero runtime callers
    until now and its exact payload key is not pinned by any shipped consumer. A non-string
    or unchanged result means no retry is possible.
  - retry the local index with the normalized name; hit -> use the NORMALIZED name in the
    output chain (that is the whole point of the retry); miss -> keep the ORIGINAL name and
    push it onto `unmapped`. Never invent a name, never drop a step: `composeWorkflow`
    degrades an unknown framework to `{command:null, optional:true}` honestly and `chain_run`
    surfaces it as a manual step.
  - divergence: when local commands are non-empty AND `step.commands` is a non-empty array
    AND the two sets differ (compare sorted, de-duplicated copies), push
    `{ framework, brain_commands, local_commands }`.
  - Never compute posture. Add an explicit comment: posture authority stays LOCAL in
    `recipe-maps.cjs` via `chain_run`; the Brain recommends, never triggers.

The write leg, guarded on `opts.db`: for each divergence,
`navigation.logMemoryEvent(db, 'brain_cmdmap_divergence', payload)` inside try/catch, lazily
required to avoid a load cycle (copy `_logEventBestEffort`'s reasoning at
`lib/core/brain-client.cjs:1437`). Payload carries SCALARS only, matching the memory_event
scalar-bag discipline: `framework`, `brain_command_count`, `local_command_count`,
`brain_commands` and `local_commands` as comma-joined strings, `source_path:
'system:brain-cmdmap'`, and a `dedupe_key` of `'cmdmap:' + framework` so the 60s TTL keeps a
polled surface from spamming the graph. Command slugs are plugin-local generic handles, never
user content, so this is a LOCAL Part 9 write and not an egress concern.

Export `chainOfferForReach`, `adaptChainToRunInput`, `parseChainCompanions`, and
`CHAIN_COMPANION_HANDLE`, additively -- the four existing exports keep their current names
and shapes.

**2c. Tests.** Drive the Brain through the loopback capture server helper, not a stub of
`brain-client`, so the wire assertions are real. Cover every bullet in `<behavior>`. For the
memory_event legs, open a real temp room.db through `lib/core/room-db.cjs` `openRoomDb` and
read the row back, mirroring `tests/test-249-capture-seam.cjs`'s Test 7 pattern. Include an
explicit negative: adapter run WITHOUT `opts.db` writes zero rows.
  </action>

  <verify>
    <automated>node --test tests/test-c8j-chain-consumer.cjs</automated>
    <automated>node tests/test-252-guard-census.cjs</automated>
    <automated>node --test tests/test-249-capture-seam.cjs</automated>
    <automated>node tests/test-129-workflow-stage-events.cjs</automated>
    <automated>bash tests/run-all-122.sh</automated>
    <automated>node -e "const r=require('./lib/brain/chain-recommender.cjs');for(const k of ['recommendFrameworkChain','recommendCanonicalTargets','chainOfferForReach','adaptChainToRunInput','parseChainCompanions'])if(typeof r[k]!=='function')throw new Error('missing export '+k);console.log('exports ok')"</automated>
    <automated>node -e "const b=require('fs').readFileSync('lib/brain/chain-recommender.cjs','utf8').split('\n').filter(function(l){var t=l.trim();return t.indexOf('*')!==0&&t.indexOf('//')!==0;}).join('\n');if(b.indexOf('/mos:')!==-1)throw new Error('command literal leaked into chain-recommender');console.log('no command literal')"</automated>
    <automated>test $(grep -o "\xe2\x80\x94" lib/brain/chain-recommender.cjs lib/core/navigation/memory-events.cjs | wc -l) -eq 0</automated>
  </verify>

  <done>
A fired reach carrying `brain_framework_chain:<pt>` yields a render-ready offer; one without
yields `null`. Brain absence yields a `grounded:false` disclosure, never a throw and never
silence. The adapter preserves chain order, spends exactly one `normalizeFrameworkName` call
per locally-missing name, keeps rather than drops a still-unmapped name, reports divergences
as data, and writes `brain_cmdmap_divergence` memory_events only when handed a db. No
`autonomous_safe` is computed anywhere on the path. The guard census, the 249 purity fence
and the 122 suite are all green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: surface the offer on suggest_next, hookless and graceful</name>
  <files>lib/mcp/tools/sensors.cjs, data/mcp-tool-connectors.json, data/connector-registry.json, tests/test-c8j-suggest-next-offer.cjs</files>

  <behavior>
    - Top reach carries a `brain_framework_chain` companion and the Brain is reachable ->
      the `suggest_next` response includes `chain_offer` with `grounded: true` and a
      non-empty `chain`.
    - Top reach carries the companion and the Brain is unreachable / keyless -> the response
      includes `chain_offer` with `grounded: false` and a note token. `ok` stays `true`.
      `suggestion` is byte-identical to today's.
    - Top reach carries NO such companion -> `chain_offer` is absent from the payload
      entirely (omit-when-absent), so the normal response is byte-unchanged.
    - No candidate reach fired -> unchanged today's payload, no Brain call at all.
    - The call performs no room.db write of any kind.
    - `reach_candidates` is unchanged: no Brain call, no `chain_offer`.
  </behavior>

  <action>
Write `tests/test-c8j-suggest-next-offer.cjs` first.

In `lib/mcp/tools/sensors.cjs`, add
`const chainRecommender = require('../../brain/chain-recommender.cjs');` to the require block
at the top. Do NOT require `brain-client.cjs` and do NOT call `isAvailable()` /
`ensureAvailable()` here: `lib/mcp/tools/sensors.cjs` is not in the frozen guard-census
classification lists, so an executable guard call in this file fails `test-252-guard-census`
census.1. Availability is decided inside `chain-recommender.cjs`, which IS classified. Put
that reason in the comment beside the require.

In the `suggest_next` handler (`:170-186`), after `const top = ...` and before the return:
call `await chainRecommender.chainOfferForReach(top, {})` inside a try/catch that degrades to
`null`. Pass an EMPTY opts object on purpose -- no `db` key -- so the divergence write leg
cannot fire and the tool's declared "no write of any kind" stays literally true (fence 5).
State that in the comment.

Add `chain_offer: offer || undefined` to the response object, using the same
omit-when-absent idiom already used for `degraded` and `note` at `:182-183`, so a pull with
no companion is byte-identical to today's payload.

Do NOT change `reach_candidates`. One Brain call per pull, on the top pick only; fanning the
call across the whole candidate set would multiply wire cost by reach breadth for no
additional first-offer value. Record that as the deliberate bound.

Update the `connectors` metadata for `suggest_next` (`:360-365`): keep `hitl_shape: 'none'`
(the navigator-facing fork is still `chain_run`'s material gate, not this read), and extend
`hitl_why` to disclose the new behavior honestly: it may now make ONE optional read-only
Brain call when the top reach carries a `brain_framework_chain` companion, it passes no db
handle so it still performs no write of any kind, and a Brain absence degrades to a
`grounded:false` disclosure rather than an error. Then regenerate the derived registries with
`node scripts/build-connector-registry.cjs` and confirm with `--check`; never hand-edit
`data/mcp-tool-connectors.json` or `data/connector-registry.json`.

Tests: capture the real registered handlers off a fake MCP server exactly as
`tests/test-222-reach-wired.cjs:162-190` does -- reuse that harness idiom, do not invent a
second one. Drive the Brain leg through the loopback capture helper. Legs:
  1. companion + reachable Brain -> `chain_offer.grounded === true`, non-empty chain.
  2. companion + no key -> `chain_offer.grounded === false`, `ok:true`, `suggestion` intact.
  3. no companion -> `Object.prototype.hasOwnProperty.call(payload,'chain_offer') === false`.
  4. no reach fired -> today's payload, zero captured Brain calls.
  5. write-free proof: run the handler against a temp room and assert zero `memory_event`
     rows were added (count before and after).
  6. static fence: `lib/mcp/tools/sensors.cjs` contains no `require` of `brain-client.cjs`
     and no executable `is/ensureAvailable(` call.
  </action>

  <verify>
    <automated>node --test tests/test-c8j-suggest-next-offer.cjs</automated>
    <automated>node tests/test-222-reach-wired.cjs</automated>
    <automated>node tests/test-222-readonly-rank.cjs</automated>
    <automated>node --test tests/test-198-contract-schema.test.cjs</automated>
    <automated>node tests/test-252-guard-census.cjs</automated>
    <automated>node --test tests/test-249-capture-seam.cjs</automated>
    <automated>node tests/test-237-one-authority-fence.cjs</automated>
    <automated>node scripts/build-connector-registry.cjs --check</automated>
    <automated>node scripts/build-orchestration-projection.cjs --check</automated>
    <automated>node scripts/check-render-coverage.cjs</automated>
    <automated>test $(grep -o "\xe2\x80\x94" lib/mcp/tools/sensors.cjs | wc -l) -eq 0</automated>
  </verify>

  <done>
`suggest_next` returns `chain_offer` when the top reach carries a `brain_framework_chain`
companion, omits the field entirely when it does not, and discloses `grounded:false` instead
of failing when the Brain is down. The tool still performs zero room.db writes and still
declares `hitl_shape: 'none'` with an `hitl_why` that now names the optional Brain read.
`reach_candidates` is untouched. The connector registries are regenerated, not hand-edited,
and every born-wired / projection / render gate is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LOCAL room -> BRAIN (`callTool` HTTP POST) | The Canon Part 8 boundary. Only generic methodology handles may cross: problem_type enums and framework names. Room content crossing here is a constitutional breach. |
| Sensor reach -> offer layer | `companions` strings are attacker-influenceable in the sense that `tuple.problem_type` originates from a /mos:diagnose classification over room text. Treated as untrusted input at the wire. |
| Brain response -> LOCAL adapter | `chain[].framework` and `chain[].commands` are foreign strings consumed by a local resolver and written into a local memory_event. |
| MCP caller -> `suggest_next` | Foreign hosts (Desktop / Cowork / CLI) invoke the tool and receive the whole reach plus the new `chain_offer`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-c8j-01 | Information disclosure | `recommendChain` problem_type argument | mitigate | `PROBLEM_TYPE_HANDLE_RE` shape gate at the wrapper: letters/digits/space/hyphen, max 49 chars, letter-initial. A prose, URL, email, or payload-shaped token cannot pass, so a corrupted `tuple.problem_type` is refused locally with `invalid_problem_type` and ZERO network. Test leg 1d.5 asserts zero captured calls, not just the return value. |
| T-c8j-02 | Information disclosure | The ~12 previously unguarded `callTool` wrappers | mitigate | Task 1c: `part8-egress-guard.classify(args, {toolName})` at the single dispatch seam, after the key gate and before `_ensureSession`. Coverage becomes 16/16. `block` returns the `egress_blocked` sentinel with no socket opened. |
| T-c8j-03 | Information disclosure | The adapter's Brain retry (`normalizeFrameworkName`) | mitigate | Only a framework NAME is sent, and only for names that already arrived FROM the Brain in its own chain response. Nothing room-local is introduced. The Task 1c belt also classifies this call. |
| T-c8j-04 | Tampering | Brain-supplied `chain[].commands` used as local execution instructions | mitigate | The adapter NEVER passes Brain commands to an executor. It produces a framework-NAME array only; `composeWorkflow` (the one governed door, Brain-free by grep guard) resolves names to commands locally. A Brain/local disagreement is recorded as `brain_cmdmap_divergence`, never reconciled in the Brain's favor. |
| T-c8j-05 | Elevation of privilege | Brain influence over autonomous execution | mitigate | Fence 6: posture authority stays LOCAL in `recipe-maps.cjs`, reached through `chain_run`. Nothing in this plan computes, asserts, or forwards `autonomous_safe`. `chain_run` still halts at the first material step and mints its own gate. |
| T-c8j-06 | Denial of service | Brain call on a polled read surface (`suggest_next`) | mitigate | One Brain call per pull, on the top reach's FIRST companion only. `reach_candidates` is deliberately not wired. `isAvailable()` short-circuits before any socket when there is no key. Existing `callTool` retry budget and `BRAIN_REQUEST_TIMEOUT_MS` bound the call. |
| T-c8j-07 | Denial of service | `brain_cmdmap_divergence` flooding room.db from a polled surface | mitigate | `payload.dedupe_key = 'cmdmap:' + framework` uses the shipped 60s dedupe TTL in `memory-events.cjs`, and the write leg only fires when a caller supplies `opts.db` (which `suggest_next` deliberately does not). |
| T-c8j-08 | Repudiation | A silent degrade hiding a Brain outage from the navigator | mitigate | The offer returns a `grounded:false` disclosure with a closed note token (`brain_unavailable` / `brain_unreachable` / `tier_denied` / `invalid_key` / `unknown_problem_type` / `offer_error`) rather than `null`, so absence is visible and attributable. `null` is reserved for the honest "no companion, nothing to offer" case. |
| T-c8j-09 | Spoofing | An `egress_blocked` refusal misread as a transport outage | mitigate | A distinct sentinel OBJECT, never `null`. `null` stays the test-pinned transport signal across ~82 degradation tests; the `tier_denied` / `invalid_key` sentinel-object idiom is the established precedent. |
| T-c8j-10 | Tampering | Regression of the sensor purity fence by moving the Brain call inward | accept-with-control | `tests/test-249-capture-seam.cjs` Test 14 greps for the forbidden requires in `lib/core/sensors/*.cjs` and `navigation-engine.cjs`. It runs in all three task verifications, so a future inward move fails the build rather than degrading quietly. |
| T-c8j-SC | Tampering | npm/pip/cargo installs | not-applicable | This plan installs ZERO packages. No new dependency is added to `package.json`; every module used (`part8-egress-guard`, `command-resolver`, `navigation`, `brain-client`) already ships. The Package Legitimacy Gate therefore has no surface here. |
</threat_model>

<verification>
Run after all three tasks, before declaring the quick task done.

Constitutional and fence gates:
- `node tests/test-252-guard-census.cjs` (census.1 allowlist, census.2 seam liveness)
- `node --test tests/test-249-capture-seam.cjs` (sensor purity, Test 14)
- `node tests/test-237-one-authority-fence.cjs` (one posture authority)
- `node tests/test-239-query-egress-canary.cjs`, `node tests/test-245-egress-contentless.cjs`

Born-wired and projection gates:
- `node scripts/build-connector-registry.cjs --check`
- `node scripts/build-orchestration-projection.cjs --check`
- `node scripts/check-render-coverage.cjs`

Contract and behavior:
- `node --test tests/test-247-contract-client.cjs` (must be green INCLUDING the loop-tool
  count test, which now reads 7 in both the assertion and the test name)
- `node --test tests/test-c8j-brain-wire.cjs`
- `node --test tests/test-c8j-chain-consumer.cjs`
- `node --test tests/test-c8j-suggest-next-offer.cjs`
- `node tests/test-222-reach-wired.cjs`, `node tests/test-222-readonly-rank.cjs`
- `bash tests/run-all-122.sh`

Acceptance roll-up:
- `node scripts/doctor.cjs --acceptance`

Repo hygiene:
- zero em-dashes in every touched file
- `git diff --stat` shows only the files in `files_modified`; in particular ZERO lines in
  `lib/mcp/tools/chain.cjs`, `lib/mcp/tools/gate.cjs`, `lib/mcp/gate-ledger.cjs`,
  `lib/core/insight-sensors.cjs`, `lib/core/sensors/*.cjs`
</verification>

<success_criteria>
1. `brain.recommendChain('Undefined Problem')` puts exactly `{problem_type, max_steps}` on
   the wire as tool `recommend_chain`, and `tests/test-247-contract-client.cjs` derives that
   expectation from `data/brain-surface-contract.json` rather than from a hand-written list,
   with the suite's one hand-maintained count (assertion plus test name) moved 6 -> 7 so the
   whole file passes.
2. A reach carrying `brain_framework_chain:<pt>` produces a chain offer; a reach without one
   produces `null`; a Brain outage produces a `grounded:false` disclosure with a closed note
   token, and never an exception.
3. `suggest_next` carries `chain_offer` when and only when there is something to offer, still
   returns `ok:true` with an intact `suggestion` when the Brain is down, and still writes
   nothing to room.db.
4. The adapter preserves chain order, spends at most one `normalizeFrameworkName` call per
   locally-missing framework name, keeps a still-unmapped name so `composeWorkflow` degrades
   it honestly, and records Brain/local command-map divergence as a LOCAL memory_event.
5. In-process Part 8 classify coverage is 16/16 wrappers at one seam; the four pre-existing
   raw-field guards are byte-unchanged; the `callTool` no-key and transport `null` contracts
   are byte-unchanged.
6. Every fence in `<fences>` holds, proven by the suites in `<verification>`, not by assertion.
</success_criteria>

<output>
Create `.planning/quick/260819-c8j-tripwire-consumer-recommend-chain-wrappe/260819-c8j-SUMMARY.md`
when done. Record: the exact live `recommend_chain` response shape observed (if a live call
was made) and whether it matched the frozen v1 contract, the D2 mechanism actually shipped and
why, any Brain-side payload key the adapter had to accept defensively from
`normalize_framework_name`, and the named follow-ups this plan deliberately deferred (the
`:<framework>` third companion segment, `reach_candidates` not wired, SENS-09's
`adoption-capacity` token which is a companion enum with no matching Brain ProblemType node).
</output>
